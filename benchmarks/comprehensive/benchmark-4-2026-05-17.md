<!--
file: benchmarks/comprehensive/benchmark-4-2026-05-17.md
purpose: Frozen May 17 deep-architecture audit prompt run against the HomeSynapse Core Claude Project.
audience: Cowork
update-cadence: frozen
state-type: reference
status: CURRENT
last-verified: 2026-05-20 against commit 25bc23b
-->

# HomeSynapse Core — Deep Architecture Audit Prompt

You are being tested on your deep, accurate understanding of the HomeSynapse Core architecture. This is a comprehensive audit covering module structure, locked decisions, amendment interactions, implementation details, and gotcha areas that commonly trip up AI agents.

**Instructions:** Produce a single detailed response document answering ALL of the following sections. Be precise with numbers, field counts, type locations, and constraint sources. Do not hedge or add disclaimers — state facts directly. Where you are uncertain, say "UNCERTAIN" rather than guessing. Cite your sources (LTD-XX, AMD-XX, INV-XX, DEC-M3-XX) for every constraint you reference.

---

## Section 1: Module Dependency & Type Location (10 questions)

1. What module does `Availability` live in? What module do people commonly (incorrectly) assume it lives in?

2. `CheckpointStore` and `ViewCheckpointStore` are different interfaces in different modules. State: (a) which module each lives in, (b) what data type each stores, and (c) why they are not unified.

3. Where does `HomeSynapseException` live? Why is that surprising?

4. The `WriteCoordinator` class is package-private. What module is it in, and what are its 5 `WritePriority` levels in priority order (highest to lowest)?

5. How many modules apply the `test-fixtures-conventions` plugin? Name all of them.

6. What is the JPMS relationship between `integration-api` and the modules below it in the dependency graph? Specifically, what does `integration-api` do with `requires` directives that is unique?

7. `rest-api` has a unique characteristic in its Phase 2 `module-info.java`. What is it, and why?

8. List ALL 19 modules in the project (not counting build-logic or dashboard).

9. What module does `InMemoryEventStore` live in? Be precise about the source set and package. What happened to the old copy and when?

10. `ConfigurationChangeListener` and `config_changed` events — describe the exact ordering guarantee and which fires first.

---

## Section 2: Event Model Deep Dive (8 questions)

11. How many fields does `EventEnvelope` have? Name the one that was promoted from `CausalContext`. How many fields does `CausalContext` now have?

12. How many fields does `EventDraft` have? What are the last two fields added (field 8 and field 9), and which amendment added field 9?

13. `EventPublisher` has two methods with different semantics: `publish()` and `emit()`. Describe the durability and synchronicity guarantees of each. Which AMD locks the `publish()` guarantee?

14. `EventPublisher.publishRoot()` — how many parameters does it take? Where does `actorRef` come from if not a parameter?

15. Explain the `readBySubject` pagination trap. What does `EventPage.nextPosition` carry in this context, and why is it different from `readFrom`?

16. What does `InMemoryEventStore` assign as default categories for all events? How will this differ in production `SqliteEventStore`?

17. What is the V001 events table column count? List at least 8 of the "schema reservation" columns and which amendments introduced them.

18. What is the `idempotencyKey` constraint (max length, nullability)? Which AMD introduced it?

---

## Section 3: Virtual Threads & Platform Thread Discipline (5 questions)

19. Explain WHY sqlite-jdbc operations must run on platform threads, not virtual threads. Be specific about the mechanism (what "double-pins" means). Cite the source findings.

20. Does JEP 491 (Java 25) fix this problem? Explain precisely what JEP 491 fixes and what it does NOT fix regarding sqlite-jdbc.

21. What is the mandatory executor pattern for database calls? Specify thread counts for write and read pools.

22. Which ArchUnit rule enforces the "no synchronized" constraint? What does it catch and what does it NOT catch? How is the gap covered?

23. What modules/subsystems are affected by the platform-thread requirement? List at least 4 specific subsystems that must use platform thread executors for their SQLite operations.

---

## Section 4: M3 Architecture — Event Bus & State Projection (10 questions)

24. How many production types did M3.1 produce? Break down into public vs package-private. Name the key public types (at least 6).

25. What are the 5 values in the `SubscriberMode` FSM? What is the three-phase REPLAY→LIVE transition defined by DEC-M3-03?

26. Explain why `coalesceExempt=true` is critical for StateProjection. What happens if a state projection misses an event? Which DEC-M3 decision covers coalescing?

27. What are the AMD-38 `FixedCheckpointPolicy.HOME_DEFAULT` parameters? (3 values) Why is the 2-second `maxInterval` the "load-bearing safety mechanism"?

28. Describe the bounded-window reader pattern. What problem does it solve? What are the quantitative constraints on `ProjectionAdvancer.advance()`? What did the D1 spike prove about WAL growth?

29. Explain DEC-M3-08 (backpressure). What was rejected? What replaced it? What is the rate limit for StateProjection and why?

30. DEC-M3-02 specifies "self-produced event detection." What are the two mechanisms (primary + defense-in-depth)? What is the TTL on the primary mechanism?

31. Explain the INV-SUB-ISO invariants. How many are there? What does "per-subscriber" isolation mean concretely (list at least 5 things that are isolated per subscriber)?

32. `EventBusContractTest` — how many methods total? How many tiers? Which tiers are active after M3.1, and which are @Disabled for future milestones?

33. How does the bus notify subscribers of new events? Does it push `EventEnvelope` objects? What mechanism does it use instead?

---

## Section 5: Persistence & Schema (6 questions)

34. Describe the V001→V002→V003 migration progression. What does each version add?

35. What is `SqliteEventStore`'s INSERT bind count? What is special about its 5th constructor parameter and which AMD introduced it?

36. What is the `chain_hash` column's NOT NULL constraint strategy? What value is used before tamper-evidence is implemented? Which AMD governs this?

37. Explain the D1 WAL pathology. At 5 events/s with a continuous reader, what size does the WAL grow to in 120 seconds? What is the write amplification ratio and why?

38. What are the `DeploymentProfile` enum values? Is `journal_size_limit` different across profiles? Why? Cite the source.

39. Describe the `MaintenanceSubscriber` contract from AMD-40: execution thread, interval, batch size, and lock-hold duration per chunk.

---

## Section 6: Cross-Cutting Interactions & Gotchas (6 questions)

40. S5-CF1 (Snapshot freshness during duration timer expiry) — explain the failure mode. When AMD-25's temporal duration timer fires, what MUST the automation engine do and what must it NOT do?

41. S5-CF2 (Orphan transition timing) — explain why the orphan's Availability must be set to STALE immediately, not deferred to AMD-11's periodic scan.

42. `Ulid.compareTo()` — what comparison method must it use? What happens if you use `Long.compare()` instead? What encoding does `Ulid.toString()` use?

43. What are the ArchUnit rules (by name)? How many are there? Is there a rule preventing event-bus from importing JDBC types? If not, how is that constraint enforced?

44. `IEEEAddress` in the zigbee module — is it a ULID? What is its actual representation? Is `CoordinatorTransport` thread-safe?

45. When the project is complete, how will planned restart be communicated cross-layer? Through direct method calls, or through events? Give the specific event type if applicable.

---

## Section 7: Build & Governance (5 questions)

46. Name the 4 convention plugins in `build-logic/`. Which modules apply each?

47. What does `modules-graph-assert` enforce? Give one example of an intra-API-layer rule it enforces.

48. How is the CI pipeline configured? (JDK, timeout, trigger events, command)

49. What Jackson version is pinned? What is the floor version and which LTD specifies it?

50. What is the WUCP (Work Unit Completion Protocol)? Describe both phases and what happens if Phase 2 is not run.

---

**Output format:** Number your answers 1–50. Each answer should be 2–6 sentences, precise and factual. Total response should be approximately 3,000–5,000 words. Do not add introductory or concluding fluff — start directly with answer 1.
