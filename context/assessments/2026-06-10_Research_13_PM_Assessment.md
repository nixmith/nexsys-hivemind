<!--
file: context/assessments/2026-06-10_Research_13_PM_Assessment.md
purpose: PM 6-step A–F assessment of Research 13 (Config System Market Superiority — UX failure modes + runtime robustness). Folds the disposition table into the M6.4/M6.2 coding instructions; queues the FUTURE-AMD rows; parks the UI/cloud rows.
audience: PM, Nick
state-type: current
status: CURRENT — issued 2026-06-10 (return self-dated 2026-06-11; assessed the same evening)
ground-truth: homesynapse-core HEAD `7e0bce8` (docs-only on `9035110` — M6.1 COMPLETE whole); docs watermark AMD-87; projectionVersion 5
brief: context/instructions/Research_13_Config_System_Market_Superiority_Brief.md
return (archived per research-agenda §2 Step 0): homesynapse-core-docs/research/returns/2026-06-11_Research_13_Config_System_Market_Superiority.md
-->

# Research 13 — PM Assessment (6-step A–F)

**Assessor:** PM (Cowork, Mode 3 — Director; assessment precedes the M6.4/M6.2 instruction authoring it gates).
**Verification basis:** every ALREADY-COVERED citation and every gap claim spot-verified against the **docs repo working tree** (Doc 06 §3.3/§3.5/§3.6/§3.7/§4.3–§4.7/§5/§12.3–§12.5; AMD-66/67/68/70/71 full text; Doc 15 §2.3/§3.5/§4.1/§4.2/§7.3/§8/§9; AMD-17 existence confirmed via the CDR synopsis → Doc 02 §3.15) and against **source at HEAD** (config `module-info.java`, `ConfigChangedEvent`/`ConfigErrorEvent`, `SecretStore`, `StandardConfigurationService` staging, `YamlLoader` tag seam, the EventTypes 54-pin). Web re-verification of external quotes was attempted (ADR-0021) but the session's fetch tool timed out repeatedly; external evidence is therefore graded on citation specificity and internal consistency, the same basis as prior assessments.
**Numbering reconciliation:** REC-130..140 (11 RECs), high-water was 129 (Research 12). **No collision; append-only honored.**

---

## A. Format compliance — PASS

The return follows the mandated format with the §0 quote-back gate FIRST. **Quote-back gate: HONORED** — the §0.2 module declaration is character-exact against source at HEAD (all five third-party non-transitive `requires` + `requires transitive com.homesynapse.event` + the single `exports`; verified against `config/configuration/src/main/java/module-info.java`), the §0.3 charter rows and the three pinned M6.4 obligations are faithfully quoted. *Nuance, not a defect:* the embed's copyright/Javadoc header and the ruling comment were condensed to a parenthetical — the load-bearing identifiers (module name, edges, exports) are verbatim, which is what the gate exists to prove. Eleven RECs each carry failure-class citation, ≥1 primary source, gap-relative statement, recommendation, and S/M/L effort. **The disposition table is complete: every REC in exactly one bucket; no REC in two buckets; the REJECT bucket's emptiness is argued, not lazy** (the AMD-71 include-cap, the nearest candidate, is affirmatively defended by HA's sprawl evidence). One table defect found at adjudication (REC-134, see §E) — the only row whose bucket does not survive source verification. §7 stayed LIGHT as mandated: no module-info proposals, no new types, no contract drafts; all identifiers real.

## B. Executive summary (PM reading)

Research 13 is a **high-quality, zero-fabrication, confirmation-heavy** return: its central finding is that the ratified M6 machinery (INV-CE-01 single-file truth, AMD-66 classification, AMD-67 fail-closed migration, AMD-71 include cap, §3.6 three-tier validation, Doc 15 secrets posture, LTD-04 typed-ULID identity) is **well-matched to evidenced market pain**, with the genuinely open work concentrated where the charter already put it — M6.4 exercising what M6.1 defined. The flagship superiority claim (INV-CE-01 structurally eliminates the HA `.storage`-vs-YAML split-brain — HA issues #143/#283/#103256, openHAB #64682) is endorsed and is marketing-grade material for the M5-C website lane. The dual-audience thesis survives with one sharp residual: **write-back fidelity** (REC-131) — and source verification shows the Locked Doc 06 §3.5 already decided that tradeoff (comment loss = documented known limitation + pre-first-write timestamped backup mitigation), which narrows the obligation to *implementing the §3.5 mitigation*, not building a comment-preserving emitter. The over-engineering check is honest (AMD-67/AMD-71 defended by evidence; AMD-70 scope-growth resistance noted). **Verdict preview: 11 RECs — 9 ACCEPT (2 with narrowing), 1 RE-BUCKET (REC-134 → ALREADY-COVERED), 1 ACCEPT-AS-DEFER (REC-138 FUTURE-AMD with the now-or-never claim softened). Grade: A−** (the REC-134 bucket defect and the unread-§3.5/§3.6 misses are exactly the class its own §5 caveat predicted; everything else held).

## C. Cross-cutting analysis (PM corrections from source verification)

1. **REC-131's "or document the deviation" branch is already the Locked design.** Doc 06 §3.5 records comment loss as a **known limitation** verbatim ("SnakeYAML Engine does not preserve YAML comments… A write through the UI/API strips all comments"), defers comment-preserving round-tripping to post-MVP, and mandates the **mitigation**: WARNING log + timestamped backup `config.yaml.bak.{ISO8601}` before the first UI/API write. The M6.4 obligation is therefore the *mitigation + its test*, not an emitter.
2. **REC-134 is closed by Locked text the researcher couldn't read.** Doc 06 §3.6 WARNING tier: unknown key "detected because JSON Schema uses `additionalProperties: false` on known sections" — the decision REC-134 asks to pin is pinned, shipped at `9035110` (`JsonSchemaCompositeValidator`: `additionalProperties`→WARNING; `StandardSchemaRegistry`: root + integrations `additionalProperties:false`). HA's failure class is *silent* ignoring; HomeSynapse detects + surfaces in dashboard. Escalating WARNING→ERROR would contradict ratified §3.6 — a FUTURE-AMD if field evidence ever demands, not instruction work.
3. **REC-138's provenance gap is real but narrower than claimed, and not strictly now-or-never.** Doc 06 §4.4 already specifies a `trigger` field (`api`/`cli`/`signal`/`startup`) — channel provenance exists in the Locked payload sketch. Actor identity ("who") has no model until the auth boundary (Doc 09/M10) exists. And HomeSynapse's per-event `schema_version` + read-time upcast machinery (AMD-52 precedent) means a later payload addition is a versioned evolution, not a foreclosure. Still worth Nick's eyes before M6.4 freezes the `config.section_reloaded` component list (see §F).
4. **A tension the return could not see, surfaced by this verification pass:** Doc 06 §12.4 (Event Payload Safety — Locked) says `config_changed` payloads "never carry configuration values — old or new," but the **frozen** event record `ConfigChangedEvent(String configPath, String previousValue, String newValue)` is value-bearing by shape. M6.4's reload-notify step (§3.3 step 6) cannot publish that record with real values without violating §12.4, and cannot change the frozen shape without an AMD. PM resolution (ruling-requested at M6.4 issue, embedded in the instruction): the reload path publishes **`config.section_reloaded` only** (AMD-70's payload — names + counts + classification — is §12.4-safe and is §3.3 step 6's intent realized); `ConfigChangedEvent` stays registered but production-unpublished, its reconciliation queued as a FUTURE-AMD alongside REC-138 (same payload family).
5. **The RQ3 gap table's CLOSED/CLOSED-UNTESTED/OPEN partition survives verification** — every "ratified mechanism" cell cites real machinery (fileModifiedAt token §3.5 ✓; §3.3 reject-and-keep-prior-good ✓; AMD-67 fail-closed ✓; AMD-68 setAll ✓; LTD-04 ✓; AMD-17 ✓ Doc 02 §3.15). The CLOSED-UNTESTED rows are precisely the M6.4/M6.2 test obligations the instructions below encode.

## D. Source / evidence quality — STRONG (with honest soft edges, correctly labeled)

Primary sources dominate where it matters: HA ADR-0010/ADR-0021 with verbatim policy text, dated core issues (#103256 truncation, #31461 validation timing, #157984/#142639 migration), the SmartThings/webCoRE retirement timeline with named community posts and per-user migration counts, The Register/Hackaday for Insteon (≈100k figure correctly attributed as a CEO estimate, not audited), K8s/CNCF + nginx for reload prior art. The return's own §5 correctly downgrades Alexa/Google/Homey items to directional and flags that it never read Doc 06 §3.1–§3.7 full text — the two verdicts it asked to have source-verified (REC-136, REC-138) were verified here (REC-136 holds: §3.6 is schema-only, reference-integrity is genuinely open; REC-138 narrowed per §C.3). Mom-Test discipline held: the load-bearing RQ2 evidence is what users *did* (migrated pistons, abandoned platforms), not reviews.

## E. Per-REC adjudication (REC-130..140)

| REC | Verdict | Disposition (final) | PM notes |
|---|---|---|---|
| REC-130 | **ACCEPT** | **M6.4 INSTRUCTION OBLIGATION** | Write→file→re-read round-trip + `fileModifiedAt` stale-external-edit rejection test (Doc 06 §3.5 steps 1–8). Folded into M6.4 §Test Requirements. |
| REC-131 | **ACCEPT-WITH-NARROWING** | **M6.4 INSTRUCTION OBLIGATION** | Narrowed to the Locked §3.5 mitigation: pre-first-UI-write timestamped backup (`config.yaml.bak.{ISO8601}`) + WARNING log, with test. Comment-preserving emitter stays post-MVP per Locked §3.5 — do NOT build it. |
| REC-132 | **ACCEPT** | **M6.4 INSTRUCTION OBLIGATION** | The AMD-66 §5 test matrix (HOT in-place / INTEGRATION_RESTART / PROCESS_RESTART / unannotated→PROCESS_RESTART fallback / throwing-listener-rejects / sync-before-publish) exercised under the swap. Folded. |
| REC-133 | **ACCEPT** | **M6.4 INSTRUCTION OBLIGATION** | Concurrent-reader torn-read test on the atomic `ConfigModel` swap — already the charter done-when; the REC sharpens the test shape. Folded. |
| REC-134 | **RE-BUCKET** | **ALREADY-COVERED** (Doc 06 §3.6; shipped `9035110`) | The one table defect. `additionalProperties:false` + WARNING classification is Locked text and shipped code. Severity escalation would contradict ratified §3.6 → FUTURE-AMD at most, on field evidence. No instruction delta. |
| REC-135 | **ACCEPT** | **M6.4 INSTRUCTION OBLIGATION** | Crash-window test: kill between pre-migration backup and atomic rename → prior-good recoverable (R2 ruling scope, §3.7 step 7). Migration-failure observability narrowed: FATAL surfaces via `ConfigurationLoadException` + `config.validation_completed` severityCounts — **no new event type** (AMD-70 scope fence; the return's own §3.4 warned against payload growth). Folded. |
| REC-136 | **ACCEPT** | **FUTURE AMD** (queued for Nick) | Reference-integrity validation (automation→entity/area at load/reload) is genuinely open — §3.6 is schema-only. JPMS note for the eventual AMD: `config` cannot see the registries (zero-new-edge), so the pass lives at the composition root or the automation engine's own validation (M7/M8), not inside `com.homesynapse.config`. Sketch only; do not draft. |
| REC-137 | **ACCEPT** | **POST-MVP UI/cloud DESIGN INPUT** | Parked in the M10/M11/Doc 13 lane: surface name≠identity in UI. No runtime work (LTD-04 closes it). |
| REC-138 | **ACCEPT-AS-DEFER** | **FUTURE AMD** (queued; surface to Nick at M6.4 issue) | Provenance in persisted config-event payloads. Softened per §C.3: `trigger` exists in §4.4's sketch; actor identity awaits auth; versioned-payload evolution (AMD-52 machinery) means deferral does not foreclose. PM recommendation: defer; joins the §C.4 `ConfigChangedEvent`-reconciliation AMD when that opens. |
| REC-139 | **ACCEPT** | **POST-MVP UI/cloud DESIGN INPUT** | Auto reject-and-keep-prior-good (§3.3/§3.6) is the MVP rollback story; user-facing snapshot/restore UX parked M10/M11/Doc 13. |
| REC-140 | **ACCEPT** | **M6.2 INSTRUCTION OBLIGATION** | `setAll` all-or-nothing + durable-before-return + crash-frame tests — AMD-68 §5 already names them; the REC adds the crash-safety framing. Folded into M6.2 §Test Requirements. |

**Bucket totals (post-adjudication):** ALREADY-COVERED coverage attestation = the §2 verdicts + REC-134; M6.4 obligations = REC-130/131(n)/132/133/135(n); M6.2 obligation = REC-140; FUTURE-AMD queue = REC-136, REC-138 (+ the PM-surfaced `ConfigChangedEvent`/§12.4 reconciliation); POST-MVP parking = REC-137, REC-139; REJECT = none (endorsed as genuinely empty).

## F. Disposition fold + state actions (this session)

1. **M6.4 instruction AUTHORED** (`context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md`) — folds REC-130/131/132/133/135 + the three pinned obligations (R1 per-ERROR `config_error` publish; `config.section_reloaded` P2 survey re-run; CoreSchema on the reload re-parse) + the §C.4 publish-path resolution (ruling-requested flag at issue).
2. **M6.2 instruction AUTHORED** (`context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`) — folds REC-140 + the charter CARRY 1 E2 bridge verbatim + AMD-68 + Doc 15 §3.5/§7.3 fences; carries the PM-surfaced Doc 15 §4.1-vs-§3.8 `scope_keys` placement resolution (config-side file; ruling-requested flag at issue).
3. **FUTURE-AMD queue for Nick's prioritization:** (a) REC-136 reference-integrity pass; (b) REC-138 provenance + the `ConfigChangedEvent`-shape/§12.4 reconciliation (one AMD family). Neither gates M6.
4. **UI/cloud parking (M10/M11/Doc 13):** REC-137 (name≠identity surfacing), REC-139 (snapshot/restore UX), plus Hubitat's rollback-UX lesson from §2.4.
5. **Marketing input (M5-C lane, Nick-paced):** the §1/§3.1 superiority material (split-brain immunity, rename-safety, local-first survival evidence) is website-grade with citations.
6. **Return archived** per research-agenda §2 Step 0: `homesynapse-core-docs/research/returns/2026-06-11_Research_13_Config_System_Market_Superiority.md` (committed by Nick with this session's docs commit).
