<!--
file: context/assessments/2026-06-12_Research_16B_PM_Assessment.md
purpose: PM assessment of the Research 16-B return (LLM-Legible Output Engineering, REC-201–215) — quote-back verification, source verification vs core 01841ba/7c73c91 + Doc 07/Doc 11/LTD-15, external spot-checks, fabrication scan, grade.
audience: PM, Nick
state-type: assessment record
status: COMPLETE — folded FIRST per the R16 fold order (engineering constrains, human evidence prioritizes)
last-verified: 2026-06-12 (assessment session)
-->

# Research 16-B — PM Assessment (engineering register; folded FIRST)

Return: `context/instructions/Research_16B_LLM_Legible_Output_Engineering_RETURN.md` (271 lines, 41,016 B; md5-archived to docs `research/returns/2026-06-12_Research_16B_LLM_Legible_Output_Engineering.md`). Assessed against: brief embeds → ground (`01841ba` git objects for both scripts; `7c73c91` for `CausalContext` + module-info; Doc 07 §11.2; Doc 11 §3.4/§3.5/§11.2/§14; LTD-15 in `HomeSynapse_Core_Locked_Decisions.md`; DAS Register C) + live web spot-checks (2026-06-12).

## Step 0 — Quote-back gate: **HONORED (verbatim, zero adulteration)**

All four §0 blocks byte-match the brief's embeds: §0.2(i)/(ii)/(iii) (helpers, `KEY:value` + `get_val`, `READINESS:` lines), §0.3 (all 14 Doc 07 §11.2 names, in order, with levels), §0.4 (`CausalContext` record + javadoc contract + `com.homesynapse.event` module-info), §0.5 (Register C). The brief's embeds were themselves re-verified against ground this session: helpers/gate/`get_val`/`READINESS` byte-exact at `01841ba` (git objects, not worktree — truncated-tail artifact active); the 14 names + levels + the LTD-15 framing sentence exact vs Doc 07 §11.2 (lines 938–951); `CausalContext` javadoc params and module-info exact at `7c73c91`; Register C exact vs `governance/DAS_Consolidated_Reference_v1.md` §1.1. Nit (non-docking): the §0.2(iii) *surrounding prose* drops the brief's "(print the plan, do nothing)" gloss — the gate covers the fenced blocks, which are untouched. The seeded `automation.cascade.depth_exceeded` ground-precision hazard did NOT appear anywhere in the return (grep-verified) — the §4-step adjudication rule was moot, as intake predicted.

## Steps A–C — Format / Summary / Cross-cutting: **PASS, STRONG**

All seven mandatory sections present and substantive; §1 takes positions throughout ("worth investigating" absent); §3.2 (the agent-consumer contract table) and §3.3 (correlation narration map) are the demanded distinctive deliverables and both are disposition-ready; §3.4 over-engineering check is a real honesty section (five REJECT-candidates, each reasoned against LOCKED ground); §7 stays observation-only with zero new identifiers.

## Step D — REC dispositions (PM rulings; source-verified — see the merged disposition for final routing)

- **REC-201–203, 205–208 — ACCEPT, SCRIPT-STANDARD.** Every "SATISFIES" cell in §3.2 spot-verified against `01841ba`: verdict line on both exit paths with the agent-comment (434–442), `get_val` `^`-anchor + `cut -d: -f2-` (203–205), else-branch `RED='' …` color strip (33–35), exit 0/1 + `exit "${GRADLE_EXIT_CODE}"` (pi4 line 200), `--dry-run`/`-h|--help`/env-var block (pi4 15–86). "SILENT on `NO_COLOR`" confirmed (no `NO_COLOR` reference in either script).
- **REC-204 — ACCEPT, SCRIPT-STANDARD; the defect claim is TRUE.** Independently re-confirmed: **zero `>&2` in pi-health.sh AND pi4-validation.sh** at `01841ba`; the OQ-15-2 bench driver (`context/assessments/oq-15-2-harness/bench-oq15-2-driver.sh`) shares the defect (zero `>&2`; emits `FLAG:`/`GUARDRAW:` on stdout) → added to the retrofit list. This is the cycle's one genuine in-house defect discovery, and it was found from the embeds alone.
- **REC-209 — ACCEPT-WITH-MODIFICATION, SCRIPT-STANDARD.** The `--json`/`--kv` *option* is accepted as a documented extension point; the return's "auto-emit machine form when stdout is not a TTY" clause is **dropped** — content-switching on isatty contradicts the porcelain-grade stability the same return demands in REC-202/A3 (and clig.dev's own auto-detection caveat, which the return itself quotes in §2.5 failure modes). The shipped dual-encode-in-one-stream idiom (machine lines unconditional, color the only TTY-varying element) is the standard; `--json` waits for a real consumer.
- **REC-210 — ACCEPT, M12-INPUT.** The pin is correctly scoped INSIDE LTD-15/Doc 11 §3.4 (both verified: MDC `correlation_id`/`entity_id`/`integration_id` table is exact LTD-15 ground; §3.4 single-query + O(n) tree exact).
- **REC-211 — ACCEPT-WITH-MODIFICATION, M12-INPUT.** The hazard is real and well-sourced (Logback MDC manual primary; JEP 506 finalization + the `ScopedValue.orElse` no-null API change verified against the OpenJDK record). **PM correction the return missed: LTD-01 locks Java 21 — Scoped Values are not final on 21, so the M12 mechanism is explicit MDC copy (TaskDecorator-style) full stop; Scoped Values are a JDK-trajectory-conditional noted for the future, not an M12 option.** Spike obligation stands (`-Djdk.traceVirtualThreadLocals=true`, fork + pooled-reuse leak assertions). → Disagreement register.
- **REC-212/213 — ACCEPT, M12-INPUT.** Naming-convention candidate + deprecation discipline; OTel naming/governance quotes consistent with the spec; the §0.3/Doc 11 §11.2 tables already satisfy the proposal (verified — `observability.*` names real).
- **REC-214 — ACCEPT, DOC-11-CURRENCY; basis verified TRUE and UNDERSTATED.** Doc 11 §3.5 ground shows **ten** subsystem rows in at least **three** prefix styles (`hs_events_*`/`hs_state_store_*`/`hs_persistence_*`/`hs_automation_*` underscore; `hs.device.*`/`hs.integration.*`/`hs.zigbee.*`/`hs.api.*`/`hs.ws.*` dotted; bare `config.*`) — richer than the 3-exemplar seed. Currency note, never an amendment; rides the next Doc-11-touching amendment.
- **REC-215 — ACCEPT, FUTURE.** Correctly sketched, not drafted.
- **§3.4 anti-requirements (not minted as RECs — deliberate, reasoned): ACCEPT all five into the REJECT/anti-requirements register** — OTel/OTLP collector or shipping daemon (LTD-15 + §0.6 row 6 ground verified: "No Prometheus in MVP… No OpenTelemetry in MVP" verbatim), systemd-style `MESSAGE_ID` hex catalog, logfmt-as-wire-format (LTD-15 literally specifies "JSON lines" — the return's "implied" is actually *explicit*, strengthening its own case), full W3C `traceparent` machinery, OTel SeverityNumber normalization. Bucket-emptiness reasoning for M10-M13/M5-C-COPY/REJECT-as-REC accepted.

## Step E — Error inventory / fabrication check: **CLEAN**

Connector-blind DECLARED (§5) with an itemized gap list per the charter — the R14 standard met. All three declared gaps were closed TRUE at PM verification this session: (a) Doc 11 §3.5 prefixes (above), (b) Doc 11 §11.2 `observability.*` names (real, exact), (c) AMD-93 / projectionVersion 5 / pins 55/24/36 (current per pinned state). Every HomeSynapse identifier in the return traces to a brief embed; zero fabricated types, module names, event names, or doc sections. External spot-checks 100% PASS: JEP 506 final in JDK 25 incl. the precise `ScopedValue.orElse` no-null change (OpenJDK record); OTel semconv v1.41.0 docs / v1.41.1 release (k8s codegen fix only) — exact; Anthropic "Writing effective tools for AI agents" (Sep 11 2025) 25,000-token quote verbatim incl. the "effective context length" sentence; "Introducing advanced tool use" exists with the cited token-scale figures. Static-standards quotes (git porcelain v1, TAP, no-color.org, Google Shell Guide `err()`, clig.dev, grep exit status, jsonlines.org, Logback MDC, systemd journal-fields) all consistent with the primary texts. Secondary-source reliance (Java Code Geeks for the virtual-thread MDC synthesis) honestly graded SILVER and corroborated by primaries.

## Step F — Grade and process notes

**Grade: A.** The strongest return of the program to date: quote-backs perfect; zero fabrications; a genuine in-house defect (REC-204) discovered from embeds alone and confirmed TRUE against source; the declared KB-gap list exactly matched what needed PM verification and all three items verified TRUE; external claims survived every spot-check including a version-precise API detail most secondary sources get wrong; disposition table complete with reasoned emptiness. Docked to the bottom of the band rather than below it for: the REC-211 LTD-01/Java-21 collision left unflagged (the return supplied the JDK-25 fact but didn't run it against the locked toolchain — PM-adjudicable, registered), and REC-209's auto-emit clause contradicting its own REC-202 stability doctrine (caught and dropped at adjudication). Steelman honored: the return's load-bearing insight — *a pasted agent cannot see `$?`, so the verdict must be in the text* — is correct, primary-sourced, and is the organizing principle the OUTPUT_CONVENTIONS draft is built around.

**Disagreement register (16-B):** (1) REC-211 mechanism vs LTD-01 (above) — resolved PM-side, no re-research needed. (2) REC-209 auto-emit vs REC-202 stability — resolved PM-side (stability wins). (3) Session-prompt nit, not a return defect: the brief/prompt language crediting the OQ-15-2 harness with `READINESS:` lines overstates — the harness emits `RESULT:`/`FLAG:`; `READINESS:` is pi-health's. Recorded so the convention doc attributes patterns precisely.
