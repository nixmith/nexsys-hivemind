<!--
file: context/assessments/2026-07-11_go-no-go-criteria_draft.md
purpose: DRAFT criteria checklist for the mid-August (~Aug 16) go/no-go — every criterion binary-or-measured, evidence-defined, MUST/SHOULD-tagged, with current status and the named evidence producer. Ends with the gate-day protocol sketch. Exists so the mid-Aug call is made on evidence, not vibes (Nick's ruling, v27 beat 3).
date: 2026-07-11 (lane return date; authored 2026-07-10 America/Chicago, soak day 1)
audience: Nick (rules the gate); the hub (evidence steward; carries ratified criteria into orchestration); the bench/frontend lanes (they produce the missing evidence).
state-type: assessment / decision-support DRAFT — becomes the gate artifact only on Nick's ratification.
status: **RATIFIED 2026-07-12 (Nick — verbatim rulings: pm-handoff v30 beat 2). The §RATIFICATION block below is the OPERATIVE layer** (statuses of record + Q1–Q4 applied + the I3a/I3b split + J1 FROZEN); the draft body beneath it is preserved as authored with its 2026-07-10 statuses. Prior status: RETURNED by the executive-briefs lane (Track B lane (c), dispatched v27 beat 3). Authored 2026-07-10 (America/Chicago), soak day 1; filed under the lane's 2026-07-11 return date. All statuses are as-of 2026-07-10 and go stale with every beat — the hub re-derives at ratification and again at gate day.
grounding: PROJECT_SNAPSHOT v27 beats 1–3 + the certified M9.4/M9.5 backlog row + the 15-row ruled post-soak stack (backlog, 2026-07-10) · nexsys-bench/docs/2026-07-06_m9.4-bench-acceptance-record.md (iteration log + acceptance-run block + the 2026-07-10 correction) · MVP §2.2/§2.3/§4/§8 · Six_Battlefields_MVP_Strategy.md · context/planning/2026-06-28_mid-august-go-no-go_readiness-review.md (the four-gate frame this draft descends from) · pm-handoff v27 beats 1–3 (rulings verbatim).
-->

# Mid-August Go/No-Go — Criteria Draft (for ratification)

## RATIFICATION — 2026-07-12 (Nick; the operative layer; J1's process FROZEN)

RATIFIED by Nick 2026-07-12 (rulings verbatim: `context/handoff/pm-handoff.md` v30 beat 2). This block supersedes every in-table "Status 2026-07-10" cell per the draft's own §5. **J1 is FROZEN: no criterion is added, removed, re-tagged, or re-worded without an explicit Nick re-ruling recorded in pm-handoff.** The hub re-statuses once more at gate day, against artifacts only (§4).

**Rulings applied:** **Q1** — C1 = MUST N ≥ 200 · C1b = SHOULD N ≥ 500; ONE false CONFIRM at any N = automatic STOP + root-cause, overriding the count; the §1-C statistics + reachability assumption accepted as the basis of record. **Q2** — I3 SPLIT: **I3a [MUST]** = fix landed (✅ `1627978`+`3590fca`, CI-green) + B1's usb-reenumeration scenario green in the DIRECT-ATTACHED topology; **I3b [SHOULD]** = the hub-attached leg (Rosonway-gated — a MUST never depends on a third-party delivery). **Q3** — D1 = MUST 5 classes; D2 = SHOULD the button 6th (R2(B): the 01P adopts battery-only; presses log-visible, absent from the device model — deliberate scoping of record). **Q4** — the read = Sunday 2026-08-16; evidence-freeze = Friday 2026-08-14 EOD; nothing lands between freeze and read. **Carriers named** — A4 (kill −9): the first post-deploy bench session with spare minutes; H3 (clean-image fresh install): the named weekend slot Aug 8–9. **Tally after the split: 21 MUST · 8 SHOULD.**

**Adjudication of record (2026-07-12, ruled with the B1 contract ruling — rider 4; J1 stays FROZEN, nothing reopens):** I3a's frozen text ("B1's usb-reenumeration scenario green in the direct-attached topology") is **SATISFIED by B1's OPERATOR manual variant** — Nick, verbatim: *"the OPERATOR manual variant is one of B1's two tiers, so a green OPERATOR rep Monday satisfies the row as frozen. Record that adjudication with the ratification block so gate day reads it, not argues it."* Gate day reads this adjudication; it does not re-litigate the tier.

**Statuses of record as of 2026-07-12 (ratification re-status; the gate-day pass re-derives again):**

| Row | Tag | Status 2026-07-12 |
|---|---|---|
| A1 | M | IN PROGRESS — soak day 3 on `04f5f70`; exit ≥ 2026-07-13 ~07:50; RSS/log deltas recorded at close-out against the stated bounds · **✅ CLOSED 2026-07-13/14 (v31 intake; re-statused v42 beat 3):** the soak ran UNBROKEN to **3d 12h 04m** on `04f5f70` (single PID, `ps etime 3-12:04:03`; zero crashes; operator-attributed clean SIGTERM end); **RSS at close-out 204,608→212,240 KB ≈ 40% of the 512 MB §8.2 bound**; log growth linear (108,160 B / 3.5 d); store 13,790 events at exit, WAL clean, zero loss signatures. **Honest limitation carried verbatim: the entry-RSS sample was never recorded → the final-48h-delta sub-clause is UNRECONSTRUCTABLE as specified**; the absolute close-out reading at 2.5× headroom + the sample pair + linear logs stand as the no-runaway evidence. Artifact: `nexsys-bench/docs/2026-07-06_m9.4-bench-acceptance-record.md`, the SOAK EXIT + CERTIFICATION CLOSE-OUT block |
| A2 | M | PENDING — Monday, runbook Phase 7 steps 20–26 verbatim · **✅ CLOSED 2026-07-13 (re-statused v42 beat 3):** the exit spike ran ×3 pasted (+1 attributed extra, same healthy shape): per boot `projection_live devices=2 entities=2 position=14` · `adoption_maps_rehydrated devices=2` · `device_relinked` ×2 with deviceIds IDENTICAL every time · ZERO `device_proposed` · `/api/v1/entities` = exactly the two 5b ULIDs. **DUR+DURb CERTIFIED AT SOAK SCALE.** Artifact: the acceptance record, exit-spike block |
| A3 | M | PENDING — Monday, after A1+A2 (the hub writes the close-out) · **✅ CLOSED 2026-07-13/14 (re-statused v42 beat 3):** the CERTIFICATION: CLOSED block IS the close-out of record (A1 ✓ · A2 ✓ · A3 ✓ · I2 ✓ · B1 re-proven on 72 h-aged state ×3; deploy of `1aa809d` CLEARED at runbook step 26). Artifacts: the acceptance record close-out block; corroborated at the wave-2 return's live-state gate ("Certification is CLOSED … exit spike ×3 green") |
| A4 | S | ✅ BANKED 2026-07-26 — the deploy evening's Block-4 kill −9 rep (position 25065 preserved; six ULIDs identical; boot-health [PASS], bundle 20260726T211954Z) |
| B1 | M | ✅ DONE (banked 2026-07-09/10; re-proven on 72 h-aged state by A2) |
| B2 | M | PENDING — the first Wave-2 bench session (Monday's pinned order, post-deploy) · **✅ CLOSED (the joins night 2026-07-18/19 + the 04P adoption 07-20; re-statused v42 beat 3):** the two original identities are UNCHANGED through Wave-2 — `01KX1PA4HSJ581GASYB7DHE40F` (Hue) + `01KX1PB9AAB4VB3E10BD477TV3` (SNZB-03P) stand verbatim in the grown 6-ULID `remembered-ulids` set (the constants re-mint of record 2026-07-21, values read AT the instrument) and are re-proven API-to-API at EVERY boot-health 6/6 since — including all three suite runs of record (latest banner `@ 8bce651`; `/api/v1/entities` = exactly the remembered set). Artifacts: `nexsys-bench/scenarios/constants.yaml` (remembered-ulids + re-mint stamps) + the boot-health bundles |
| C1 | M | IN PROGRESS — ~25/200 recorded verdicts, ZERO false. RATIFICATION NOTE (honesty): the reachability assumption's scriptable suite executions now ride the CMD-API WU (discovered 2026-07-12 — the v1.1 HTTP surface is READ-ONLY; no REST command stimulus exists; AUTO command scenarios are blocked until it lands; N accumulates via waves/OPERATOR meanwhile) · **✅ N EXCEEDED — THE CITATION RECONCILIATION EXECUTED (v42 beat 3):** the PRIMARY event-level artifact is the acceptance record's verdict-envelope block: **19 `state_confirmed` · 1,024 `command_confirmation_timed_out` · 685 `command_result` ≈ 1,728 cumulative recorded verdicts, ZERO false CONFIRM** (store 13,790 events at exit; the arithmetic locks at ~345 organic runs × the class split; the commanded-to-current class exercised ~1,000× organically) — **8.6× the N≥200 MUST and 3.5× the N≥500 SHOULD as of 2026-07-13/14**, with the record itself stating both thresholds numerically exceeded on the ratified counting basis. Secondary corroboration ×2, grep-confirmed verbatim 2026-07-31: the wave-2 return live-state gate + the 2026-07-25 executive read (the v42 brief's third-record cite, the I3b report, carries no cumulative count — the corpus-×3 tally traces to these two + the record; reconciled honestly). Accumulation since: three suite runs of record, zero false CONFIRMs (never-false-CONFIRMED ×3) + the organic corpus; B3's nightly digest = the ongoing feed. **ONE form question for Nick's one-liner (REC yes): the record's counts-by-event-type form (positions citable on demand from the store) satisfies the row's 'positions cited' clause as ratified — rule it now so gate day reads it, not argues it** · **RULED YES (Nick's word, v43 beat 3; recorded at this v43 beat-5 ledger touch): the counts-by-event-type form with positions-citable-on-demand SATISFIES the positions-cited clause as ratified — gate day reads it. The nightly feed is LIVE (first digest 2026-08-01; zero false CONFIRMs — the fourth run of record).** |
| C1b | S | as C1 — **N ≈ 1,728 ≥ 500: EXCEEDED 3.5× (the same reconciliation, v42 beat 3)** |
| C2 | M | IN MOTION 2026-07-30 — organic proof ✅ banked; suite port: **device-absent class GREEN ON DEPLOYED** (`command-timeout-absent` [PASS], bundle `command-timeout-absent-20260730T000130Z`; deterministic premise post the Hue power-topology remedy — return §12.4/§12.6) · **commanded-to-current class GREEN ON DEPLOYED 2026-07-30** (`timeout-honesty-no-change` [PASS] 2/2 at the re-run of record — the `.value` fix proven live, honest TIMED_OUT + state unchanged; bundle `timeout-honesty-no-change-20260730T013843Z`) · **⚠ the REJOIN-RACE class port = the named OPEN residual** (no rejoin scenario exists in the suite; OPERATOR-tier port rides B3's authoring — flagged v40 beat 4 so the Aug-10 dry-run reads it settled, not surprised). Zero false CONFIRMs across the run of record (the C1 clause held) · **2026-07-31 the THIRD run of record (banner `@ 8bce651`, the H2 full-green run):** `command-timeout-absent` [PASS] + `timeout-honesty-no-change` [PASS] 2/2 — both ported classes re-proven · **the rejoin-race port residual RESTAMPED B3-OWNED** (the OPERATOR-tier port is a named B3-authoring rider, v42 charge 2; the Aug-10 dry-run reads this row 2-of-3-suite-green + rejoin-residual-owned, settled not surprised) |
| C3 | M | ✅ CLOSED 2026-07-30 (v40 beat 4) — BOTH legs GREEN in the first suite of record ON THE DEPLOYED `c09c61c`: `command-supersession` [PASS] 2/2 (ACKNOWLEDGED-terminal + `details.result` "superseded", stable on +30 s re-read; bundle `command-supersession-20260730T000136Z`) · `command-identify-honest` [PASS] (immediate honest "unconfirmed", 54–66 ms ×2 at probe, with the measured reason class quoted; bundle `command-identify-honest-20260730T000137Z`); the S-2/S-3 full-body probe pastes = the wire-shape evidence; the superseded disposition IS expressible on the frozen surface (no contract change). Return: `context/audits/2026-07-28_B2_suite-port_return.md` §12.2/§12.6/§12.10 |
| C4 | S | IN MOTION 2026-07-30 — the instrument-first fork RESOLVED: the command status read carries per-phase `lifecycle.<PHASE>.at` timestamps (measured in every probe body), so the latency arm reads straight off the frozen surface. Seven fully-timed lifecycles banked (accepted→dispatched 2.8–5.4 ms ×7; dispatched→terminal by class: device-confirmed 107.5 ms n=1 · adapter-immediate 54–66 ms n=2 · issuance-superseded 22.3 ms n=1 · window-expiry ~5.3 s n=2+) — n disclosed, NO fabricated percentiles; B3 nightly = the estimator source for gate-day p50/p99. Return §12.8 · **2026-07-31: the estimator feed UNBLOCKED** (H2 FULL GREEN; B3 authors at v42 beat 2; the nightly digest carries the ON-report-latency margin line, so the no-clock latency distribution builds nightly — feeding this row AND the CONFIRM-SOUNDNESS charter) |
| D1 | M | 2 of 5 classes; Wave-2 joins = Monday's pinned order; the four dossiers DELIVERED + audited (`ada8b46`) · **✅ 5 OF 5 CLASSES (re-statused v42 beat 3):** light (Hue LCA017 — adopted, `reporting_configured 3/3 verified` at acceptance it-4, the banked 0.33 s CONFIRMED; currently unpowered under the PARKED HUE-RESET, the absence load-bearing gate evidence per v40 beat 4) · motion (SNZB-03P — it-5a full join→TCLK→adopt→report→STAY; `reporting_configured 2/3` honest-degraded sleepy) · temp/humidity (SNZB-02P) + contact (SNZB-04P, the 07-20 LEARN-PERSIST adoption) + powered plug (S31 Lite zb) — joined 2026-07-18/19/20; fleet 6/6 @ 25065 across three+ boots (the 07-21 re-mint of record). Characterization: the four Wave-2 dossiers (`ada8b46`) + **the S31's corpus-grade confirmation characterization** (the F-12 arc: window 5.369 s · report 11.613 s · `reporting_configured verified=0 degraded=1` + `confirmation_downgraded … best_effort` — the platform's own prospective posture disclosure IS the characterization) + the two identify reason strings discriminating confirmation-DISABLED (S31) from tracked-then-silent (Hue). **Honesty note: the S31's on/off reporting is configured-UNVERIFIED (`best_effort`)** — platform-disclosed, recorded here; the `verified=0` root cause stands chartered post-gate |
| D2 | S | PENDING — same sessions; R2(B) governs the button's shape · **✅ AS-SCOPED (re-statused v42 beat 3):** the button (SNZB-01P) is ADOPTED battery-only per the R2(B) ruling of record (in the remembered-ulids set; presses log-visible, absent from the device model — the deliberate scoping). **The energy-telemetry clause resolves N/A AT THE INSTRUMENT: the S31 Lite zb exposes NO metering surface** (z2m: switch-state only; the capture shows no ElectricalMeasurement/Metering clusters — the dossiers return, which also flagged the corpus-README 'energy' archetype label for correction) |
| E1 | M | ✅ DONE — CODE-LANDED `a1f0f77` CI-green; full-bar two-layer audit ACCEPT (v28) |
| E2 | M | rail HELD to date; silicon leg PENDING — Monday (ONE fresh formation under the generated seed = the SD-5 discharge, composing with Wave-2 joins) · **✅ CLOSED (re-statused v42 beat 3):** the joins-night formation ran under the GENERATED seed — `network_formed channel=20 panId=0x774c, posture=generated_seed` (2026-07-18; the constants network block, re-mint of record) — the SD-5 silicon discharge composing with the Wave-2 joins exactly as ruled; the no-non-bench-network rail HELD throughout (the pre-E1 formations 0x9b65/0x2d83 were the bench's own, pre-custody era, superseded by the 07-18 re-formation). E1 = `a1f0f77` CI-green, two-layer ACCEPT (v28) |
| E3 | S | PENDING E2 · **WRITABLE-NOW (v42 beat 3):** E1 + E2 both banked — the bounded statement is truthful as scoped (generated, encrypted-at-rest key material on NEW formations; the well-known key never a production network key; the CURRENT bench network itself formed under the generated seed 07-18). The one close-out paragraph queues for the docs fold; writing it discharges the row |
| F1 | M | ✅ DONE — CODE-LANDED `1aa809d` CI-green; two-layer audit ACCEPT (v29) |
| F2 | M | PENDING — Monday silicon (SNZB-02P reports→AVAILABLE + a genuine offline leg; never-false-ALIVE both directions) · **⚠ RE-STATUSED v42 beat 3 — THE ONE GENUINELY-OPEN NON-SCHEDULED MUST, named now so Aug-10 confirms instead of discovers:** F1's tracker is landed and deployed since the `1aa809d` era, but NO silicon transition rep is filed in any audit/return — and one filed reading demands adjudication: **the 2026-07-27 G2 tile read `Available 6 of 6 · Offline none` while the Hue had been unpowered for days** (the F-4 power-topology finding) — either the tracker classifies the Hue outside the mains read-ping arm, or the offline transition never fired: a potential never-false-ALIVE exposure, undecidable from the desk. **THE DISCRIMINATOR (one authed curl; rides Nick's next Pi visit): read the Hue's availability NOW** (unpowered since the 07-29 unplug) — OFFLINE/honest-UNKNOWN ⇒ the offline half gains organic evidence and the 07-27 tile needs only a history explanation; AVAILABLE ⇒ a real F2 defect found twelve days early. **The rep design (one attended evening; pairs naturally with the B3 rejoin-race rep):** (1) SNZB-02P report→AVAILABLE read (the positive half) · (2) S31 wall-unplug → offline within the ~10-min mains window → replug → online-on-evidence · (3) the battery leg rides the nightly corpus (pull the 04P battery one evening, read the next). Producer: Nick (hands) + hub (the block, authored at the F2-rep dispatch) · **⛔ THE DISCRIMINATOR FIRED RED 2026-07-31 11:45Z (v42 beat 4): the deployed build claims `"availability":"AVAILABLE","stale":false` for the two-days-unpowered Hue** (full six-entity read pasted; viewPosition 55378). **ROOT CAUSE FOUND AT SOURCE the same session — F-14, the M-1 class:** the adapter constructs `StandardAvailabilityTracker` with a hardcoded EMPTY seed (`Map.of()` at the construction site) while the sidecar's `lastKnownAvailability` map is faithfully WRITTEN on every transition (`ZigbeeDeviceCache.setAvailability`; `zigbee-devices.json` carries it per its own §8.1 M-1 doc) and NEVER READ BACK — the construction-site comment itself names "sidecar snapshot-init + the boot-time view reconciliation" as the deferred WU. Consequence: a device unpowered across a restart never enters the tracker (`recordFrame` never fires; `evaluateTimeouts` iterates only tracked devices), the mains ping arm is structurally blind to it, and the projection replays the last logged `availability_changed` ("online", the 07-29 S-1 era) indefinitely. **Design aggravator, also source-confirmed: seeded `lastSeen = boot` ⇒ the 25 h battery path resets on every restart — structurally unfireable once B3 restarts nightly.** The tracker's arms are otherwise sound at source (ping-fail → UNAVAILABLE direct; the production loop drives the cycle — the no-driver theory REFUTED at source; instrument-first honored). **THE FIX = the named-deferred WU, now GATE-CRITICAL — WU-AVAIL-SEED** (seed-from-sidecar honoring Nick's ruled boot-UNKNOWN trap · the view reconciliation · the restart-clock ruling); **authors on the corroboration pastes** (the two-read block in the chat: the sidecar's Hue entry + the `availability_changed` event history — P1a sole-defect vs P1b ping-arm-leg, predictions pre-stated). The attended F2 rep then proves BOTH directions on the FIXED build — inside the two-week margin. **Not an audit miss: the M-1 deferral was explicit and Nick-ruled at F1's landing; what was under-priced is that a MUST row's mechanism depended on a deferred WU — the deferred-WU-vs-criteria-ledger sweep lesson rides pm-lessons at the docs fold** · **v3 ADJUDICATED 2026-07-31 (v42 beat 7):** the Hue is MAINS-classified (`powerSource: 1` in the sidecar; internally validated — both mains devices read 1, the battery 3, sleepies 0) ⇒ **P1b LIVE: the ping arm failed to conclude in a tracked, mains, silent window** (the 07-29 19:19→~20:01 boot; the log is inside the 7-day retention — the v4 read answers mechanism). The entity availability history: 14 events 07-14→07-19, **NONE after — including NONE at the 07-29 S-1 rejoin** (a publish that should have fired; conflict-swallow is the named candidate — `zigbee.availability_publish_conflict` exists for exactly this). **THE THIRD LAYER, SOURCE-PROVEN: the boot-rehydration relink publishes `AvailabilityChangedEvent("unknown","available")` under the DEVICE subject for EVERY registry device, EVERY boot** (`ZigbeeAdoptionSlice` relink emission — the P-4 six-burst matches to the millisecond) — an evidence-free availability assertion manufactured at boot. WU-AVAIL-SEED authors on the v4 pastes (the 07-29 log grep + the burst subject_type query, riding the install trip) · **v4 ADJUDICATED — F-14 CLOSED (v42 beat 8): P1b DISSOLVED.** The 07-29 boot log carries NO Hue tracker line at all (four transitions, all `available=true`, all OTHER devices) — the Hue was never tracked in ANY post-07-19 boot (its identify DefaultResponse does not ride `recordFrame`), so the ping arm has NO proven failure: it is UNEXERCISED against a dead device, and **the F2 rep's S31-unplug leg doubles as its first live exercise** (anti-vacuous pairing). The subjects query confirms the attribution end-to-end: the boot bursts are DEVICE-subject rows — **the Hue's evidence-free 'available' re-manufactured EVERY boot** — while the honest tracker fanout rides ENTITY-subject rows, none the Hue's since 07-19; ZERO publish-conflict WARNs. **Final shape: a two-layer defect (boot-manufactured availability + the written-never-read seed) + one unexercised arm. WU-AVAIL-SEED is fully authorable — zero open measurements; ONE ruling ask rides its authoring (DP-B: the relink DEVICE-subject 'available' emission's disposition — REC: stop manufacturing it; the seed WU's view-reconciliation owns boot state)** · **v5 (2026-07-31, v42 beat 11): WU-AVAIL-SEED LANDED at core `60d3ab5` — the hub two-layer audit ACCEPT (zero defects; the full diff read at host bytes), red-first→green (zigbee 557/0/0 · lifecycle 60/0/0 · full check GREEN ×2 incl. Nick's clean-tree runs), CI on the push = the gate of record; DP-B ruled STOP and EXECUTED (the relink publishes nothing). REMAINING for THIS row: the convergent deploy (core `60d3ab5` + the `4288a9d` FE SPA, one warm rebuild) + the attended both-directions rep — deploy-night: the discriminator re-read should flip the Hue honest-UNAVAILABLE within ~10 min; the one-time battery-transient is priced (DP-4), not a defect** |
| G1 | M | surface ✅ BUILT + LANDED (v35 beat 6, `f3bfd5c`, frontend.yml green); the gate-day ≤10-min demo = the remaining evidence act. **⚠ WATCH RESOLVED-TO-A-NAMED-FIX 2026-07-27 (v39 beat 9, the dev-tools glance):** the wire is PROVEN (WCAP-2: fresh chains 3/3 populated) and the glance CONVICTS THE CLIENT — the SPA crashes on a null string in the chain render (`TypeError: can't access property "toLowerCase", e is null`; the crash then kills the polling loop; the live payloads carry present-but-null optionals the mocks never emitted). **The demo blocker = FE-LIVE-V112 item 1** (null-guards + an error boundary + the honest empty state; SMALL; lands via frontend.yml + reaches the Pi on any warm rebuild+restart — well before gate day). Core side: NO change implied. Returns: `context/audits/2026-07-27_WCAP2_recent-run-chain_return.md` + `context/audits/2026-07-27_devtools-chain-glance_return.md` |
| G2 | S | ✅ CLOSED 2026-07-27 — the DASH-SERVE deploy evening's redux, LIVE on the deployed `c09c61c`: the tile rendered the four honest rows (`Available 6 of 6` · `Offline none` · `Not determined yet none` · `Stale readings none`) + the "not a live connection test" disclosure VERBATIM (return §2 Glance 1 — "the strongest possible G2 evidence"); the serve path proven the same evening (AuthGate at `/dashboard/`, token accepted, ● Live). Return: `context/audits/2026-07-27_dash-serve-deploy-evening_return.md` |
| H1 | M | ✅ green on `1aa809d` (both workflows; continuous obligation to gate day) · **CHAIN CURRENT (v42 beat 3):** every landing since is green at its own gate of record — `5b4797e`→`4bc1258`, `da11f46`, `c09c61c` ALL-ACTIONS-GREEN incl. install-smoke's serve-assert run, `4288a9d` frontend.yml GREEN (v41 beat 2); gate day reads the gate-day HEAD. **Optional cheap tightening: one Actions glance confirming ci.yml on `4288a9d`** closes the last per-commit gap before the freeze |
| H2 | M | IN MOTION 2026-07-30 — B0 ✅ · B1 ✅ (runner v0 landed, REV2 liveness) · **B2 desk+Pi RUN: the first suite of record executed end-to-end headless on the deployed `c09c61c`** (banner `runner B2-2026-07-28-rebind @ f3a628d`; 7/8 ran + `command-confirm` SKIPPED-honest; 6 [PASS] incl. the M9.6-RO harness's FIRST engine-driven live usb cycle; 2 [FAIL]s = the instrument catching real defects, both adjudicated + fixes RATIFIED v40 beat 4) — **the re-run of record (2026-07-30, banner `@ 5965151`): 6 [PASS] + SKIP-honest + 2 [FAIL]** — the s31 gap class (F-12) + the usb flake (F-13), both under the v41 beat-1 adjudication protocol (return §13); the full-green run is OWED behind that adjudication (small, instrument-first); the suite DEFINITION of record = the ratified NINE legs (split-settle ordering). H2 deliberately NOT claimed until the full-green run + B3's nightly cadence. Return §12.6/§12.7/§12.10 · **✅ FULL GREEN 2026-07-31 (v42 beat-1 intake; the run of record 2026-07-31, banner `@ 8bce651`; source: pm-handoff v41 beat 3)** — 8 decisive [PASS] + `command-confirm` SKIP-honest on `[hue-online]`, zero forbidden tokens; boot-health 6/6 (`projection_live devices=6 entities=6 position=25065`, failure sweep EMPTY); `command-confirm-s31` 2/2 — BOTH positives (`phase_terminal: CONFIRMED` **and** the OBS-CONFIRM read `data.attributes.on.value == true`) — **OBS-CONFIRM DISCHARGED, the `best_effort` posture recorded here as the honesty caveat** (the S31's on/off reporting is UNVERIFIED at source — `reporting_configured verified=0 degraded=1`, `confirmation_downgraded … outcome=best_effort`; the CONFIRMED is real and its evidence channel is native-cadence class, disclosed by the platform itself); `usb-reenumeration` 2/2 with the NARROW assert UNCHANGED (the masking law proven — the watchdog owned recovery, `transport_failed`/`port_unhealthy` + `zigbee.reopened`); F1 THE PARK production-validated (park 04:19:10Z → asserted turn_on 04:22:04Z CONFIRMED against a cleared clock); the suite definition of record = the NINE legs **RE-AMENDED park-LAST** (F1 ruling 2026-07-31; carrier: `nexsys-bench/scenarios/constants.yaml` THE AUTO SUITE OF RECORD block). **Two honest caveats, deliberately not rounded up:** (1) the run was **QUIET, not QUIESCED** — bench-hero was live-but-idle (the suite's own restart re-registers it from config); config-swap quiescence = B3 amendment (i), mechanism-not-intention; (2) **the nightly cadence is NOT YET RUNNING** — B3 authors at v42 beat 2; the row's cadence half completes at B3's first digest. **never-false-CONFIRMED held across THREE runs of record.** · **✅ THE ROW COMPLETES 2026-08-01 (v43 beat 5) — THE FIRST NIGHTLY DIGEST OF RECORD, QUIESCED:** `2026-08-01 quiesced AUTO floor: 8/9 PASS · 1 SKIP(hue-online) · bench-hero RESTORED ✓ · ON-latency 3.65s` — BOTH caveats retire: (1) QUIESCED, mechanism-proven at the instrument (hero-less boot; the ABSENT/PRESENT evidence pair `2026-08-01-quiesce-evidence.txt`; `[OK] quiesce ASSERTED` + `[OK] restore ASSERTED` in the wrapper log) and (2) THE NIGHTLY CADENCE RUNNING (systemd user timer; `OnCalendar=*-*-* 03:30:00 America/Chicago` — ZONE-PINNED, correct-by-construction; night-1 fire 04:30 EDT). Timeline corroborated instrument-first: the wrapper log's internal order (swap→hero-less boot→ASSERTED→9 legs→restore→ASSERTED→digest) + artifact mtimes 04:31 + the 45-event viewPosition delta ≈ fast-path emission; **suite wall-clock ≈60 s — the first filed suite duration (derived from the bracketing evidence reads)**; the Pi user-journal is volatile — the wrapper log + digest artifacts are the designed evidence surface. **never-false-CONFIRMED extends to FOUR runs of record.** |
| H3 | M | harness ✅ (install-smoke green standing); the clean-image rep = the NAMED slot Aug 8–9 |
| H4 | S | ✅ BANKED 2026-07-26 — the deploy evening's Block-3 rep on the deployed `2040a66` build (lawful signature; [PASS], bundle 20260726T211424Z; six lawful boots total) |
| I1 | M | ✅ banked; watch-rows OPEN by design, read at gate day (INV-CH22 recurrence = live-threat STOP) · **WATCH CURRENT (v42 beat 3): zero INV-CH22 recurrence through 2026-07-31** — the failure-token sweep (incl. `network_parameter_mismatch`) ran EMPTY at the boot-health of all three suite runs of record (latest banner `@ 8bce651`, boot-health 6/6, sweep EMPTY). The single-occurrence-artifact position stands honest |
| I2 | M | PENDING — Monday's close-out sweep (grep evidence, commands cited) · **The certification-era sweep BANKED (re-statused v42 beat 3):** the acceptance record's close-out block records I2 ✓ at certification close (custody files present + encrypted, certified; the soak logs swept). **The row's gate-day form = the PRE-FREEZE re-sweep at the named Aug-12–13 close-out slot** (grep evidence, commands cited, against the freeze-window logs) — scheduled, unchanged |
| I3a | M | fix ✅ landed CI-green; the direct-attached usb-reenumeration leg PENDING — Monday-achievable via the OPERATOR manual variant under the reduced-B1 ruling · **✅ CLOSED AS FROZEN 2026-07-18 (re-statused v42 beat 3):** the ONE Pi OPERATOR rep landed [PASS] — satisfying the row per the ratification adjudication above (gate day reads it, not argues it; adjudication of record pm-handoff v32 beat 2; the B1-REV1 cross-agent note restamped ARCHIVED-SERVED on it). Strengthened since, beyond the frozen bar: the M9.6-RO harness's engine-driven live usb cycles — first at the suite of record (v40 beat 4), then **2/2 with the NARROW assert unchanged at the 2026-07-31 full-green run** (the masking law proven) |
| I3b | S | PENDING — the hub-attached leg, Rosonway-gated · **✅ CLOSED 2026-07-25 (re-statused v42 beat 3):** `[PASS] usb-reenumeration-manual — 3/3 positive · 0 forbidden` BEHIND the powered Rosonway RSH-A107C (three USB hops from root; boot-health `[PASS] 6/6 · 0 forbidden` before AND after the move; identical 12 s RADIO-UP ×3; the topology STANDING). Artifact: `context/handoff/2026-07-25_rosonway-topology-move_I3b_bench-session-report.md` §0 |
| J1 | M | ✅ RATIFIED + FROZEN 2026-07-12 (this block) |

## 0. The frame, restated in one paragraph

Mid-August (~Aug 16) is a **readiness checkpoint toward the Nov 25 launch, not the launch** and not Tier-1 close-out (the 2026-06-28 review's frame, unchanged). What HAS changed since that review: gate 1 (engine) was already done; the bench has since run first-light through a full acceptance arc; **the moat is silicon-certified and in its 72 h soak on `04f5f70`**. So the June question — "is each gate credibly on track?" — matures at gate day into a harder, better question: **"is the evidence in hand?"** Every criterion below is written so the answer is yes/no or a number, with the artifact that proves it named in the row. Where a criterion is not yet met, the row says PENDING and names who produces the evidence — as of 2026-07-10 all but two PENDING rows have a ruled backlog row or dispatched lane behind them; the two exceptions (A4's kill −9 rep and H3's clean-image fresh-install rep) are cheap operator-session add-ons with **no scheduled carrier yet** — §5 flags them so ratification assigns carriers rather than gate day discovering the gap.

**Verdict rule (proposed):** all MUSTs green → **GO** (Nov 25 track holds). Any MUST red → **NO-GO or CONDITIONAL-GO** — conditional requires a named remediation, a date ≤ 2 weeks out, and a re-read of just the red rows; a second red read on the same row = NO-GO. SHOULD misses never block, but each is priced into the post-gate runway plan on the record — never silently dropped.

## 1. The criteria checklist

Legend: **[M]** = MUST (gate-blocking) · **[S]** = SHOULD (priced, non-blocking) · Status as of 2026-07-10 (soak day 1). "Producer" = who owns producing the missing evidence.

### A. Certification durability (the soak and its exit)

| # | Criterion (binary/measured) | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| A1 | The 72 h soak completes on the certified build `04f5f70`: zero crashes, zero event loss, zero un-triaged anomalies (any health-glance anomaly interrupts everything — Nick's rail); memory/log growth MEASURED, not eyeballed — RSS recorded at entry and close-out, bound: close-out RSS ≤ 512 MB (the MVP §8.2 idle budget) with a flat trend (final-48 h RSS delta < 10%); bench-log growth linear-with-traffic per the bench log-retention policy (no runaway class) | [M] | Soak logs (`bench-2026-07-10-075035.log`+) + the close-out intake block appended to the bench acceptance record, **including the recorded RSS + log-size deltas against the stated bounds** | IN PROGRESS — entered 07:50:35 Pi-local 2026-07-10; exit ≥ 2026-07-13 ~07:50; a restart resets the clock | Nick (glances) + hub (close-out intake) |
| A2 | NQ-6 exit-act restart spike ×3 **on the soaked build, before any pull/deploy** (Monday-pinned order): `projection_live` position ≥ the recorded aged P0 ("well beyond 14"), the SAME two 5b entity ULIDs API-to-API, `device_relinked` ×2, zero `device_proposed`, the rehydration INFO — all ×3 | [M] | Runbook Phase 7 paste-block results (banked 2026-07-10 — Monday is mechanical), appended to the record | PENDING — scheduled Monday 2026-07-13 | Nick (operator) + hub (intake) |
| A3 | Certification CLOSED on the record — the close-out block written, the M9.4/M9.5 backlog row flipped to certified-CLOSED | [M] | The bench acceptance record close-out block + the backlog row | PENDING — Monday, after A1+A2 | Hub |
| A4 | One deliberate `kill -9` rep on a post-soak bench session: zero event loss across the hard kill, clean rehydration to the same entity ULIDs (the MVP §8.1 write-ahead-durability invariant, exercised as SIGKILL rather than the pkill/restart class already proven) | [S] | A bench-session log block: kill −9 → boot → `projection_live` at the expected position + ULID continuity | ✅ BANKED 2026-07-26 — the deploy evening Block 4 (`kill -9` 39911, "nothing was running" confirming SIGKILL took it; `projection_live position=25065` ≥ P-kill at equality [no writes tonight — noted]; six ULIDs identical; [PASS] bundle 20260726T211954Z; return: context/audits/2026-07-26_deploy-evening_return.md) | Nick + hub (recipe rides the runbook) |

### B. Identity durability

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| B1 | Identity holds across process restart, full network loss + restore-from-custody, and fresh re-formation — same device/entity ULIDs, relink-not-repropose, API-to-API continuity | [M] | The acceptance record: DURb block + closing legs L1 (superior variant) + L2; "identity held across THREE networks in one night" | ✅ **DONE 2026-07-09/10** (and re-proven on 72 h-aged state by A2 when it runs) | — (banked) |
| B2 | Wave-2 joins do not disturb standing identities: after adopting the new classes (D1), the original two devices' ULIDs are unchanged API-to-API | [M] | `/api/v1/entities` before/after the Wave-2 bench session; adoption lines showing new IDs only for new devices | PENDING — rides the first Wave-2 bench session (Monday order, after deploy) | Nick (bench) + hub (intake) |

### C. The honest-verdict envelope (the moat, measured)

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| C1 | **Zero false CONFIRMs across ≥ N = 200 cumulative recorded verdicts** (certified §51 scorecards + scenario-suite runs + organic soak/bench verdicts). Any single false CONFIRM at any N = automatic gate STOP + root-cause — the one-way door outranks the count | [M] | §51-class scorecards in the record (event-store positions cited) + B2-ported suite assertion logs + the nightly digest once B3 lands | ~25 verdicts, zero false (certification night). N accumulates via B1→B3 + Wave-2 sessions | Bench sessions (Nick) + B1–B3 automation (Coder/hub) |
| C1b | Stretch envelope: N ≥ 500 by gate day | [S] | Same artifacts | as C1 | same |
| C2 | Timeout honesty stays organic-proven in three classes (device-absent · rejoin race · commanded-to-current-value) with the classes ported into the scenario suite and green on the deployed build | [M] | The acceptance-run block (three organic classes, cited) + B2 suite results | ✅ proven 2026-07-10 on `04f5f70`; suite port PENDING (B2) | Coder (B2 port) + bench |
| C3 | Supersession verdict-free + immediate honest-UNCONFIRMED (identify-class, with the measured reason string) reproduce in the suite | [M] | Record: supersession probe (pos 589/593, 1 ms) + identify 81 ms verdict (pos 644); B2 suite green | ✅ proven; suite port PENDING (B2) | Coder (B2) |
| C4 | Automation trigger-to-action p50/p99 measured on the bench-hero path from event timestamps (target: p99 < 50 ms engine-side per MVP §8.1; device RTT excluded and reported separately) | [S] | A B2 scenario assertion on event-store timestamps, or a close-out measurement block | PENDING — brightness CONFIRMED 0.33 s / identify verdict 81 ms exist as round-trip references; the engine-side split is unmeasured | Coder (B2 assertion) + hub |

**Why N = 200 (the proposal Nick was asked for):** at N = 200 with zero false CONFIRMs, a true false-CONFIRM rate of 1% would have been seen with ~87% probability (1 − 0.99²⁰⁰); at N = 500, ~99.3%. Reachability assumption, stated: the certification night yielded ~25 verdicts across ~15 automation runs (**~1.7 verdicts/run observed**) — N = 200 therefore needs on the order of ~120 automation runs ≈ 8 certification-night-scale suite executions, comfortable once B1/B2 make a suite execution scriptable even if B3 lands late; N = 500 needs ~19 further suite-nights, i.e., B3 running nightly from roughly the last week of July. Hence 200 MUST / 500 SHOULD. The count is cumulative-recorded (positions citable), never sampled.

### D. Fleet breadth (Wave-2 — "how many classes is a credible v1?")

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| D1 | **≥ 5 Zigbee device CLASSES adopted, reporting-configured, and confirmability-characterized** on the bench: light (Hue LCA017) · motion/IAS (SNZB-03P) · temp/humidity (SNZB-02P) · contact (SNZB-04P) · powered plug actuator (S31 Lite zb) — each with its dossier-grade confirmation characterization (lane (a)'s corpus standard) | [M] | Per-class `device_adopted` + `reporting_configured` + verdict lines in bench logs; the four Wave-2 dossiers (lane (a), Monday-blocking) | 2 of 5 classes today (light, motion). Wave-2 units unboxed; joins scheduled in Monday's pinned order | Lane (a) dossiers + Nick (bench joins) + hub (intake) |
| D2 | 6th class: button (SNZB-01P) adopted + characterized; S31 energy telemetry ingested if the unit exposes it (the battlefield-5 credibility start) | [S] | Same artifact classes | PENDING — same sessions | same |

**Why 5 classes is the credible-v1 floor (add/cut with argument, as briefed):** five classes cover the MVP §2.3 Tier-1 spread that Zigbee can carry alone — lighting (actuate+confirm), security-sensor primitives (motion, contact = the IAS/report-only side), environment (temp/humidity = sleepy reporting posture), and a mains actuator (plug = the second confirmable actuator class, breaking the single-actuator-class dependence on Hue firmware quirks). It exercises both moat directions (confirmable command + honest-can't-confirm) across sleepy and mains postures. Below 5, "works with devices" rests on two devices and one actuator class — demo-credible, not v1-credible. Above 6 is breadth-for-breadth's-sake: the D5 ruling already fenced M9 to the curated Wave-1/2 hero set, and 50+ devices is a Tier-1 *budget* goal (MVP §8.2), not this gate.

### E. Seed custody (SD-5 — the hard gate)

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| E1 | SEED-CUSTODY landed: generated TCLK seed on NEW formations, custodied encrypted via SecretStore (AMD-68 atomic key+seed write), restore reproduces AS-FORMED, transient join key unchanged (joiner bootstrap), zero module-info edges — CI green, **audit at the full bar regardless of tempo (Nick's rail: SD-5 never trades rigor for speed)** | [M] | The landed WU + CI on the pushed commit + the two-layer audit record; grounding already banked (`context/pre-verifications/WU-SEED-CUSTODY.md`, 12 DPs) | PENDING — instruction authoring was the hub's beat-4 act (2026-07-10); Track A as-ready dispatch | Coder + hub (audit) + Nick (CI glance) |
| E2 | The **no-non-bench-network hard gate honored end-to-end**: no network other than the bench's exists before E1 lands; the first post-E1 formation on silicon demonstrates the generated-seed path (bench leg in Monday's order or the first session after landing) | [M] | The standing SD-5 rail (backlog row + pm-handoff) + the post-E1 formation's log block (seed minted, custodied, never logged) | Rail HELD to date; silicon leg PENDING | Nick (bench leg) + hub |
| E3 | The public "generated-seed" security statement is writable truthfully and bounded: "production networks form with generated, encrypted-at-rest key material; the well-known key is never a production network key" (scoped to NEW formations; the bench network predates it) | [S] | E1+E2 evidence; one paragraph in the close-out | PENDING E1/E2 | Hub |

### F. Availability honesty (the ALIVE fix — never-false-ALIVE's other half)

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| F1 | AVAIL-ALIVE landed: the existing `StandardAvailabilityTracker` wired (construct at initialize · onFrame→recordFrame · timeout evaluation + the mains read-ping arm · per-entity transition publish), honoring the ruled MUST-NOTs (never TX-accept, never NCP keepalives, never join-handler pins, never emit at construction; the M-1 empty-persisted-map trap) | [M] | The landed WU + CI + the audit record (the verbatim watch-outs are in the backlog row) | PENDING — mechanism-without-driver #6 (grounded v27 beat 1); sequenced INSIDE the explainability push | Coder + hub |
| F2 | Availability honesty **demonstrated on silicon**: cut a device's power → honest offline transition within the ruled windows (the AVAIL-ALIVE row's constants: mains read-ping arm ~10 min; battery/sleepy 25 h — no false ALIVE ever); restore → online on evidence; the entity view stops reading UNKNOWN for live, reporting devices | [M] | A bench-session log block + `/api/v1` before/after + (once FE-7 lands) the availability tile | PENDING F1 | Nick (bench) + hub |

### G. The explainability surface (what minimum FE-7 demo counts)

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| G1 | **The minimum FE-7 demo**, live against the bench Pi over the FROZEN v1.1 read-API (never mocks, never improvised shapes): (a) *why did it fire* — the causal chain for a real bench-hero run rendered end-to-end; (b) *why didn't it* — a real non-firing explanation including the no-change class (ruling (b): log-derived, and the explanation NEVER upgrades or replaces a verdict); (c) *did it actually confirm* — the verdict vocabulary rendered: CONFIRMED with latency, honest-UNCONFIRMED with its measured reason, deliberately-superseded verdict-free | [M] | A scripted ≤10-minute demo run at gate day + screenshots; each tile's log token matching the underlying event (the log-token↔tile continuity from the positioning notes) | surface ✅ BUILT + LANDED (v35 beat 6); the gate-day demo = the remaining evidence act. ⚠ WATCH resolved-to-a-named-fix (v39 beat 9): the wire PROVEN + the client CONVICTED (a null-string crash in the chain render kills the card AND the polling loop); the demo blocker = FE-LIVE-V112 item 1 (null-guards + error boundary + empty state; small; deployable on any warm rebuild) — core untouched | Frontend lane + hub (audit) + bench (real data) |
| G2 | The availability tile renders F2's honest states (integrates F with G) | [S] | The same demo | ✅ CLOSED 2026-07-27 — rendered LIVE on `c09c61c` at the DASH-SERVE redux (all four honest rows + the disclosure line verbatim; return §2 Glance 1) | Frontend lane |

### H. Operability (the regression floor + install)

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| H1 | CI green on HEAD at gate day — both workflows (`ci.yml` + `install-smoke`), the gates of record | [M] | The Actions runs on the gate-day HEAD | ✅ green on `04f5f70` (certified); continuous obligation | CI + Nick's glance |
| H2 | B1 scenario runner landed and **the ported suite (B2: the §51 legs + timeout/supersession/identify classes) green on the deployed build**; the FULL suite runs pre-milestone as ruled — gate day reads the latest full-suite green | [M] | `bench.sh scenario/suite` output + the nightly digest (B3) if landed | B0 ✅ (charter + SCENARIO_FORMAT, 2026-07-10); B1 = first post-soak bench WU; B2/B3 sequenced behind it | Coder (B1/B2/B3) + bench |
| H3 | Install path reproducible: install-smoke green (standing) **plus one documented fresh install onto a clean Pi image** (install.sh or deb) reaching a healthy boot (health-probe green) with the runbook's steps as written | [M] | The install-smoke run + a fresh-install log block (runbook appendix) | Harness ✅ (gate #4 green per W28 entry); the clean-image rep PENDING — can ride any post-soak session; Monday's deploy is an update-path rep, not a fresh-install rep | Nick + hub (runbook) |
| H4 | Restart honesty re-rep ×1 on the DEPLOYED post-weekend build (NQ-6-class glance points) — proves the weekend WUs didn't regress the DUR proof | [S] | One restart's glance-point block | ✅ BANKED 2026-07-26 — the deploy evening Block 3 on `2040a66` (lawful signature: relinked ×6 · zero proposes · projection_live 25065; [PASS] bundle 20260726T211424Z; six lawful boots on the deployed build total) | Nick + hub |

### I. Security posture (the custody fence + statements)

| # | Criterion | Tag | Evidence artifact | Status 2026-07-10 | Producer |
|---|---|---|---|---|---|
| I1 | The custody fence stands as demonstrated: never-adopt/never-clobber under a real foreign-network incident (six honest PIE boots, ch22), recovery from own custody, core stayed up with the integration isolated. **Watch-rows honest at gate day:** INV-CH22 recurrence during the soak = live-threat STOP (interrupts everything, including this gate's timeline); zero recurrence = single-occurrence-artifact stands as the honest position ("we do not know the writer") | [M] | The acceptance record (incident + recovery blocks) + the standing INV-CH22 / WATCH-0xFFFF rows read at gate day | ✅ evidence banked 2026-07-10; watch-rows OPEN by design | Hub (watch) + Nick (rules a STOP if tripped) |
| I2 | Key-material hygiene re-swept at close-out: network key/seed never logged (INV-SE-03 class), custody files present and encrypted, the soak logs sweep clean | [M] | The close-out sweep block (grep evidence, commands cited) | Custody persistence ✅ (certified); the close-out sweep PENDING (Monday) | Hub + Nick |
| I3 | M9.6-RO (reopen identity-capture fix) landed + verified through B1's USB-interruption scenario **in both topologies** (direct-attached + hub-attached, Nick's rail) — the honest availability defect closed | [S] — see §3 Q2 | The landed WU + the B1 scenario run ×2 topologies | PENDING — root re-attributed + field-confirmed 2026-07-10; fix is field-hardening #1, sequenced post-soak; detection/backoff honesty already proven | Coder + bench |

### J. Non-criteria discipline

| # | Criterion | Tag | Evidence artifact | Status | Producer |
|---|---|---|---|---|---|
| J1 | The gate is read against THIS ratified checklist and its §2 non-criteria — no criterion added at the table, no Wave-3 item smuggled in as a blocker | [M] | The ratified version of this file, frozen pre-gate | PENDING ratification (Nick) | Nick (ratifies) + hub (freezes) |

**Tally: 20 MUST rows · 8 SHOULD rows.**

## 2. Explicit NON-criteria (gate-shaped things this gate does NOT ask)

Named so nobody re-litigates them at the table (each with the reason it's fenced):

1. **50+ devices / the 60-device home** — MVP §8.2 budget goal and the Tier-2 proof scenario respectively; the gate rides the curated Wave-1+2 hero fleet (D1). Scale validation is Tier-1-completion work on the Nov runway.
2. **Z-Wave, Matter/Thread, energy-monitor integration, local-WiFi breadth** — integration sequencing is lane (b)'s decision-brief and Nick's ruling; nothing multi-protocol gates mid-Aug. (The Shelly plugs in hand are bench *stimulus* hardware for B3, not a product integration claim.)
3. **The energy dashboard / six months of history** — battlefield 5, Tier 2 by charter.
4. **The full §8.1 latency-invariant measurement campaign under load** (event-publish p99 < 10 ms, replay > 10k/s at scale) — the invariants stay binding as architecture targets; the gate takes soak stability + the C4 hero-path measurement as its evidence tier. A formal load campaign belongs to Tier-1 close, on hardware, after breadth.
5. **Full crash-isolation demonstration across integrations** — structurally undemonstrable with one integration; the achievable half (integration PIEs honestly, core stays up — proven ×6 in the ch22 incident) is already banked under I1. The two-adapter demo is Tier 2.
6. **hsctl / HS-RADIO as product surfaces** — the product-surface crossing is a named STOP (ruled 2026-07-10); bench-tooling tier only.
7. **B4/B5 (labeled-tuple corpus, advisory-only learning)** — post-gate by their own charter; AIOT-INV-1 fences autonomy regardless.
8. **CMD-LANES / scene-group fanout + the §3.10 FIFO AMD** — ruled Wave-2-trigger (single-lane is defensible at the gate fleet size); CMD-DEADLINE and FRAME-CTR are sequenced field-hardening, after M9.6-RO.
9. **Website launch / public content / the LICENSE flip** — Nov-runway concerns with their own briefs and gates (naming blocks them, not this gate; LICENSE consumes lane (b)).
10. **The skills-constellation buildout** — design doc chartered; buildout explicitly post-go/no-go.
11. **The reopen defect as a trust question** — it never was one (zero false state through the deaf window; detection honest). It gates as availability hardening only, per I3/Q2.

## 3. Open questions for Nick (each collapses to one line at ratification)

- **Q1 — N for the verdict envelope.** REC: MUST N ≥ 200, SHOULD N ≥ 500, zero-false-CONFIRM at any N is the overriding STOP. (Rationale in §1-C.)
- **Q2 — M9.6-RO: MUST or SHOULD?** REC: **SHOULD** as written (availability-not-trust; detection already honest; an app restart recovers) — but if the mid-Aug story includes "unattended field posture," promote to MUST and it still fits the window (fix is scoped, B1 is its harness).
- **Q3 — fleet floor at 5 or 6 classes.** REC: MUST 5 + SHOULD the button (D2) — the button adds a class of *stimulus*, not a new confirmability posture.
- **Q4 — pin the gate date.** REC: **Sunday Aug 16** as the read, with Friday Aug 14 as the evidence-freeze (nothing new lands between freeze and read; the read is reading, not landing).

## 4. Gate-day protocol sketch (one page)

**When:** evidence freeze Fri 2026-08-14 EOD → the read Sun 2026-08-16 (Q4). **Who:** Nick (rules), the hub session (evidence steward — opens artifacts, never argues for a row it can't point to). **Time-box:** 60–90 min. **Standing rule:** labels are claims, artifacts are evidence — every row is read against its named artifact, never against memory or a summary.

**Read order:**

1. **Threshold check (5 min):** the soak/certification A-rows and the watch-rows (I1). A tripped INV-CH22-class live threat = the gate adjourns to a STOP-and-rule; don't read 22 rows into a live incident.
2. **The MUST ladder (40–60 min), in dependency order:** A1→A2→A3 (certification) · B1/B2 (identity) · E1/E2 (SD-5) · C1/C2/C3 (the envelope — open the scorecards, cite positions) · D1 (fleet, against the lane-(a) dossiers) · F1/F2 (availability) · H1/H2/H3 (CI, suite green, fresh install) · I1/I2 (posture) · G1 **last and live** — the FE-7 demo runs against the bench Pi in the room, ≤10 min, the three questions answered on real data.
3. **The SHOULD sweep (10 min):** A4 · C1b/C4 · D2 · E3 · G2 · H4 · I3 — each green/red noted; reds priced into the runway plan explicitly.
4. **The verdict (5 min):** apply §0's rule. The ruling + per-row tally is appended to this file (which was frozen at Aug 14), a snapshot beat records it, and the bench record gets a one-line pointer. CONDITIONAL-GO names its rows, remediations, dates, and the single re-read date.

**What GO means (so the word is bounded):** the four June gates read as evidence-in-hand for a curated-fleet v1, and the Nov 25 runway proceeds on the ruled post-gate stack (breadth, hardening, website/launch surfaces). **What it does not mean:** Tier-1 acceptance (50 devices), any multi-protocol claim, or any public-surface readiness beyond what G1/H3 demonstrated.

## 5. Honest gaps in this draft

- Statuses freeze at 2026-07-10 (soak day 1). The weekend Track-A WUs (seed-custody, M9.6-RO, AVAIL-ALIVE, explainability dispatch, B1) were ruled-but-unlanded at authoring; the hub should re-status every row at ratification rather than trusting this table.
- C4's engine-side latency split assumes event timestamps discriminate dispatch-vs-RTT cleanly; if they don't, C4 needs an instrument-first arm before it can be measured (the bench discipline: never theorize twice about the same silence).
- Two criteria have no scheduled carrier as of 2026-07-10: H3's "clean Pi image" fresh-install rep (~an hour of operator time and an SD card) and A4's kill −9 rep (~minutes inside any post-soak session) — flagged so each gets a named slot at ratification rather than a gate-day surprise.
- This lane read the spine and the record; it did not read code. Any row that contradicts the source at ratification loses to the source (truth hierarchy).
