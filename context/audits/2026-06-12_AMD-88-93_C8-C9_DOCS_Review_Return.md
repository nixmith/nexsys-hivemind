<!--
file: context/audits/2026-06-12_AMD-88-93_C8-C9_DOCS_Review_Return.md
purpose: Independent DOCS-Project review return — bundled dispatch §A (M7 automation block AMD-88..93) + §B (B2 schema-irreversibles C8 actorRef / C9 energy shape). Full review discipline (contract freezes + persisted-log schema decisions).
audience: Nick (ratify), PM (fold edits)
state-type: audit
status: RETURNED 2026-06-12 — verdicts below; three live-repo attestations listed as ratification conditions (§0.2)
reviewed-against: homesynapse-core HEAD `e5ea76f` (substantive `7c73c91` = M6.2) per the dispatch baseline + AMD-92 §7 verbatim embeds; homesynapse-core-docs `ed5cf91`+ (the six block files); DOCS knowledge base (Docs 01/02/03/05/07/09/15, Glossary, Architecture invariants, Design_Review_Amendments_v1 [AMD-04 original text], merged disposition 2026-06-12, R4/R14-A/R14-B returns, B2 decision record)
-->

# DOCS Review Return — M7 Automation Block (AMD-88..93) + B2 C8/C9

## 0. Verdict Summary

| Item | Verdict |
|---|---|
| **§A block-level** | **RATIFY-WITH-EDITS** (6 required edits + 3 minor, enumerated per AMD; none exceeds the AMDs' own §9 currency scope — no supersession territory entered) |
| AMD-88 | RATIFY-WITH-EDITS (E88-1 required; E88-2/E88-3 minor) |
| AMD-89 | RATIFY-WITH-EDITS (E89-1 required) |
| AMD-90 | **RATIFY-AS-IS** |
| AMD-91 | RATIFY-WITH-EDITS (E91-1 required) |
| AMD-92 | RATIFY-WITH-EDITS (E92-1, E92-2 required; E92-3 minor) |
| AMD-93 | **RATIFY-AS-IS** |
| **C8 (`actorRef`)** | RATIFY-WITH-EDITS (EC8-1 required; EC8-2 suggested) |
| **C9 (energy shape)** | **RATIFY-AS-IS** |
| **C8 ⊕ AMD-92 coupling** | **SAFE TO RATIFY TOGETHER — YES** (§B.3, independence claim verified) |

Watermark: ratification raises the on-disk ceiling **AMD-87 → AMD-93**. The navigation-index prospective note already states this correctly ("The block sits ABOVE the AMD-87 watermark — ratification raises it to 93"), explicitly distinguishing the AMD-66..71 reserved-slot pattern. **Verified.** The 11 invariant candidates (AMD-88..93-INV-xx) are correctly carried as PROPOSED-only in the invariants quick reference pending registration at §42+.

### 0.1 Review basis

Re-derivation was performed against the DOCS knowledge base plus the dispatch's source-baseline header, which per ground rules is the authoritative statement of code state at `e5ea76f`/`7c73c91`. Every Locked-doc claim cited by the block was checked against the Locked text held in the knowledge base, not against the amendments' prose. The AMD-92 §7 module-info embeds were used as the JPMS ground truth and checked for internal consistency against every type referenced in the block.

### 0.2 Ratification conditions (live-repo attestations this reviewer cannot perform)

The DOCS role does not hold the live repo. Three claims were verified structurally/semantically here but require a one-line attestation at the ratification commit:

1. **Module-info disk match.** That the AMD-92 §7 embeds are byte-faithful to `core/automation/src/main/java/module-info.java` and `core/event-model/src/main/java/module-info.java` at `7c73c91`/`e5ea76f`. (The embeds are internally consistent with every JPMS fact in the knowledge base, including the M4.0b-4a `requires com.homesynapse.value` design note; the M5-A JPMS change was in `persistence`, the M6 module-info ruling was config-side — neither touches these two files.)
2. **The zero-publish-site grep** for `AutomationTriggeredEvent`/`AutomationCompletedEvent` (folded into AMD-92 as edit E92-2, below).
3. **C8 line anchors at `7c73c91`.** `EventEnvelope:112` / `SqliteEventStore:330/:672` were verified at `6c6dd33`; nothing in M6.1/M6.2/M6.4 plausibly touched the `actorRef` envelope field or the event-store persist/hydrate sites (M6's event-module changes were the two config mints; M6.2's `PayloadCipher` bridge is held-not-consumed), so the **semantic** claims hold — but the exact line numbers may have drifted and should be re-anchored in the Doc 01 currency note at ratification rather than copied forward.

---

## §A — The six-amendment block

### A.1 Item 1 — `[R92-residency]`: type-residency ruling. **FLATTEN CONFIRMED.**

**The cycle is real.** From the §7 embeds: `com.homesynapse.automation` declares `requires transitive com.homesynapse.event`; `com.homesynapse.event` declares `requires transitive com.homesynapse.value` and `requires transitive com.homesynapse.platform` only. Any event-record reference to an automation-resident type forces `event → automation`, closing a cycle — the exact AMD-52/E70-1 class. Confirmed at the embeds.

**Adversarial relocation test.** I tested whether any plausible M7+ consumer needs *typed* run identifiers from payloads: (a) trace assembly (Doc 07 §4.2) is query-side grouping by `correlation_id` — `Ulid`/`String` comparison suffices and the trace is never materialized; (b) the M10 REST surface serializes to JSON, where ULIDs are strings regardless — and Doc 09 §4 states explicitly that response shapes "are serialization formats — not domain types," which is the project's own doctrine that payload fields do not need domain typing; (c) AMD-91's cross-hop chain reconstruction reads `cascade_depth` and lineage from events and performs membership checks automation-side, where `RunId`/`AutomationId` exist natively; (d) observability counts types, not identities. No consumer clears the bar. Conversely, relocating `RunId` to platform-api would invert a deliberate design fact (MODULE_CONTEXT: "Unlike AutomationId, RunId is automation-specific") and publish a private identity for a payload convenience. Note also that relocation remains *available later* as a forward move with no log migration (the wire bytes are identical either way) — so FLATTEN is the option-preserving choice, which is the correct posture for a frozen contract.

**Component-residency audit (every §2.2 row).** All 19 payload rows decompose into: `Ulid`, `EventId`, `EntityId`, `AutomationId` (platform — on the declared `requires transitive` edge), `String`/`int`/`long`/`boolean`/`Instant`/`List`/`Map`/`Set` (`java.base`), and the two event-resident nested records (`EvaluatedEntityState`, `ConflictEntry`). No row references an automation-resident type. **PASS.** The "zero module-info change in both modules" load-bearing claim holds at the embeds (condition §0.2-1 covers the disk match).

### A.2 Item 2 — `[R92-reshape]`: the two record reshapes. **ACCEPTED, with E92-2.**

The zero-producer claim is structurally sound at baseline: the automation module is Phase-2-only (AMD-88 §3 states it, the dispatch baseline confirms the module was last substantively touched M4.0b-4a, and no evaluator/RunManager/publish path exists anywhere in the fidelity record), and the M3.6 composition root wires persistence/state/config, not automation publishing. With zero production publish sites there are zero persisted production instances, and the in-place reshape is regret-proof now and a log migration later — riding this block is correct timing. The M1-era shapes (`triggerType, triggerDetail`; `status, failureReason, durationMs`) do not match the Locked §3.7 payload spec (verified against Doc 07 §3.7's run-lifecycle table: `run_id`, `matched_triggers[]`, `resolved_targets{}`, `definition_hash`; `final_status`, `duration_ms`, `action_count`, `command_count`) — the reshape is also a Locked-fidelity *correction*, not a novelty.

**E92-2 (required):** the regret-proof predicate must be attested, not assumed, at the live repo. Add to AMD-92 §9: "[ ] Zero production publish sites for `AutomationTriggeredEvent`/`AutomationCompletedEvent` attested by grep at the ratification commit (test/fixture references excluded)." If the grep surprises, the reshape needs schema-version machinery instead — ruled explicitly per the dispatch: **persisted production instance ⇒ stop the reshape, version it.** I expect the grep to be clean.

### A.3 Item 3 — `[R92-1/2/3]`: the three flagged payload calls.

**R92-1 — `automation_invoked`: mint CONFIRMED, Doc 07 §3.7 row addition CONFIRMED.** Necessity is structural, not stylistic: `ManualTrigger` (AMD-88 §2.2) is event-consuming by design — without a minted invocation event, a manual run has no `triggeringEventId`, breaking C1's trace completeness and P1 ("every execution step is an event"); `InvokeAutomationAction` (AMD-90 §2.3) needs the same hop to make invocation a chain edge for AMD-91. NORMAL priority is right (run-initiating, parallel to `automation_triggered`). The Doc 07 table addition at ratification via the AMD-67 banner pattern is the correct currency vehicle; this is a bounded edit within AMD-92's enumerated §9 scope.

**R92-2 — nested payload records: CONFIRMED.** The precedent question resolves *in the Locked text itself*: Doc 07 §3.7 specifies `evaluated_state{}` and `conflicts[]` as structured nested payloads ("array of `{automation_id, command_event_id, command_name, parameters}`") — the nested records are the faithful realization of Locked structure, not an innovation. The codec precedent is stronger still: `CapabilityAdded` already round-trips a nested `CommandDefinition` carrying a sealed `Expectation` (AMD-87). Neither `EvaluatedEntityState` nor `ConflictEntry` carries a sealed value type (`EvaluatedEntityState.value` is deliberately the String projection), so AMD-52's tagged-union discipline is not triggered. `EvaluatedEntityState`'s field set correctly delivers critical-review 2.2's read-time version tracking (`lastChangedAt`, `lastChangedByEventId`).

**R92-3 — `automation_disabled` priority: NORMAL survives; §6.2 gets the correction note.** The Doc-07-internal contradiction is verified in the Locked text: the §3.7 lifecycle table reasons NORMAL explicitly and deliberately ("NORMAL priority (not DIAGNOSTIC) because it represents a significant operational state change… and should survive the 7-day DIAGNOSTIC retention window"), while §6.2 says CRITICAL twice (prose and its "Events produced" line) with no rationale. Adjudication: the §3.7 row is the normative event-vocabulary surface and the only reasoned text; its rationale was an active priority decision (the comparison it argues is NORMAL-vs-DIAGNOSTIC, i.e. retention, which CRITICAL would also satisfy — so the table's choice of NORMAL over CRITICAL was a deliberate calibration, not an oversight). CRITICAL in this taxonomy marks system-integrity events (`system_*`, `subscriber_checkpoint_expired`, unroutable `command_result`); a single automation's auto-disable is operationally significant but bounded. **Ruling: NORMAL is the survivor; the correction note at ratification must cover BOTH §6.2 occurrences** (the prose sentence and the "Events produced: … `automation_disabled` (CRITICAL)" line). Steelman recorded: CRITICAL maximizes user notification for an event the §3.7 rationale itself says the user "needs to know" — if Nick weighs notification above taxonomy discipline, CRITICAL is defensible; the correction note would then flip direction. The PM default is the better-reasoned call.

### A.4 Item 4 — `[R89-1/R89-2]`: the naming deviation + grounding.

**R89-1 — `includedRoles` rename: CONFIRMED.** `EntityCategory` does not exist in source; the shipped M4 substrate is `EntityRole` (AMD-44, verified in the device-model record). The alternative — keeping the REC label `includedCategories` typed `Set<EntityRole>` — would freeze a permanent name/type mismatch into a public sealed-permit API *and* the YAML surface (`included_categories: [primary, …]` whose legal values are role names), violating Glossary discipline twice for the sake of a research label the researcher inherited from HA's `entity_category` vocabulary. Glossary-over-label is the standing Check-11 rule; the deviation is correctly flag-don't-fold (the merged disposition's ROUTING is untouched; the field-level shape is exactly this review's jurisdiction).

**R89-2 — convention-over-type: CONFIRMED.** `Entity.labels: List<String>` is the shipped substrate; Research-8 REC-26's `SemanticTag` migration never shipped (no such type appears anywhere in the device-model record). Defining the `namespace:value` convention on the existing field is forward-compatible by construction (a future first-class tag type re-binds resolution without permit shape change). One semantic note, no edit required: plain labels containing a literal `:` could incidentally match `NAMESPACE_PREFIX` queries — acceptable, since `EXACT` requires the full `namespace:value` form and the convention is documented; the M7.1 instruction's authoring guidance should mention it.

### A.5 Item 5 — `[R88-1/R88-2]`: trigger field sets.

**R88-1 — `CalendarTrigger` field set: CONFIRMED** (with minor E88-2). The PM shape `(EntityId calendarEntityId, CalendarEventTransition transition, Duration offset)` matches REC-31's letter modulo project conventions (`triggerId` handled uniformly per §2.5; no `sustainedFor` — correct, the AMD-25 `EventTrigger` instantaneous class; `EVENT_START/EVENT_END` naming per enum conventions; `offset` nullable-with-Javadoc instead of always-present). The Tier-1-shape-now/regret-proof pattern is appropriate for a permit with no MVP producer. **E88-2 (minor):** state the offset sign convention in the AMD text ("negative = before the transition," REC-31's letter) — "fire before/after by the offset" currently leaves the sign semantics to implementation, and this is a frozen YAML-visible contract.

**R88-2 — `availability_changed` binding + debounce→`forDuration`: BOTH CONFIRMED.** The subject claim is verified directly against Locked Doc 01 §4.3: `availability_changed` is listed with **Subject Entity/Device** — the device-level event exists in the taxonomy with no new mint needed; Doc 03 §3.4's device-subject `availability_changed` resolution path independently corroborates that device-subject instances are real, handled events. REC-32's claimed dependency (`device.reachable_changed`) indeed never shipped; re-grounding on the shipped taxonomy is the correct repair. The debounce substitution is sound: debounce *is* sustained-predicate semantics for this trigger, and the AMD-25 duration-timer machinery is the single mechanism — a parallel `debounce` field would be the duplicated-mechanism anti-pattern; demoting the 30 s default to authoring guidance is right.

**E88-3 (minor, research-fidelity register):** AMD-88 §2.2 flags the debounce substitution but *not* the second deviation from REC-32's letter: `ReachabilityTransition {REACHABLE_TO_UNREACHABLE, UNREACHABLE_TO_REACHABLE, ANY}` was replaced by `Availability targetAvailability`. The substitution is **endorsed** — it makes the permit field-consistent with the existing `AvailabilityTrigger`, edge semantics are preserved because evaluation is event-driven (the consumed `availability_changed` *is* the transition), and `ANY` remains expressible as two triggers — but per the block's own G1 research-fidelity discipline it should be recorded as a documented deviation in §2.2 or §10, exactly as the debounce one is.

**E88-1 (required):** the block-context line "Breaking only in the additive-sealed sense (new permits add switch cases); promotions are field-additions and add **no** new switch cases" understates the breaking surface. §2.5 adds `triggerId` to **all five existing non-empty Tier-1 permits** — a breaking canonical-constructor change to existing records, the same class AMD-89 labels BREAKING on its face. The §5 construction-site sweep already covers it and W0 §2.3's routing is satisfied (it *is* in the block), so this is a characterization fix, not a scope problem: reword the line to acknowledge both breaking surfaces (new switch cases AND existing-permit constructor changes). An AMD that freezes contracts must describe its own blast radius accurately.

Remaining AMD-88 checks, all PASS: permit arithmetic 9→12 with Tier-1 = 9/12 and Tier-2 = {Time, Sun, Presence} (internally consistent with the dispatch baseline and the 30-permit MODULE_CONTEXT correction: 12+7+9+... — the four-hierarchy sum re-derives only post-block; at baseline 9+7+8+6 = 30 ✓); DQ-1 honored (PresenceTrigger promotion-designation, zero code change, ZoneTrigger permanently rejected); `triggerId` additive-identity (internal keying stays positional; `DurationTimer` keying and `RunContext.matchedTriggers` untouched — and AMD-91 §2.2 confirms `matchedTriggers` is not in its swap); `ManualTrigger` nullable-over-Optional per the documented project convention; the webhook producer correctly fenced to M10 with the benign-no-match posture (the permit being Tier 1 post-promotion makes the no-producer state a non-warning, correctly distinguished from the §6.1 class).

### A.6 Item 6 — `[R90-1/R90-2]`: the named default + the snapshot stance.

**R90-1 — `BEST_EFFORT`-as-default: CONFIRMED.** The disposition's parenthetical "(off; on where `Expectation` is cheap)" *describes BEST_EFFORT's semantics verbatim* — it is a conditional default, which an OPTIMISTIC reading cannot satisfy (OPTIMISTIC is unconditionally off). REC-144's own text ("off for most, on where Expectation is cheap") settles the reading. Two further supports: (a) OPTIMISTIC-as-default would silently *regress* Locked Doc 07's posture, where the ledger machinery (§3.11.2) implicitly tracks expectation-bearing commands — BEST_EFFORT preserves that behavior while removing pretend-confirmation; (b) the frenck double-actuation hazard guards against REQUIRED-as-default, not BEST_EFFORT (no policy value triggers engine retry — AMD-90-INV-01). Opposing consideration, recorded: a capability-dependent default is less predictable than a uniform one; mitigated by trace observability, the pinned `ConfirmationDefaultTest`, and the REC-161 calibration spike. The two-layer default (interface default OPTIMISTIC for non-command permits; YAML-load default BEST_EFFORT on `CommandAction`, mirroring the `onUnavailable` convention) is clearly stated and coherent.

**R90-2 — AMD-03 inviolate + degenerate-case WARNING: CONFIRMED.** Per-iteration re-snapshot would breach AMD-03's single-snapshot-per-Run rule — a ratified decision whose re-opening is supersession territory, and whose purpose (the snapshot position as the determinism/replay anchor, INV-TO-02) per-iteration reads would destroy. The PM's reading correctly scopes the snapshot rule to *state* conditions (time conditions read the injected clock, which legitimately advances), making `WaitForAction`-composed patterns and `TimeCondition` the intended WHILE/UNTIL carriers and the state-based WHILE genuinely degenerate (0 or `maxIterations` executions) — the load-time WARNING is the right severity for a representable-but-degenerate authoring pattern. `maxIterations` hard ceiling all modes + nested-repeat rejection give INV-90-02 teeth against the HA #115042 class. **AMD-90: RATIFY-AS-IS.**

### A.7 Item 7 — `[R91-1]` + the AMD-04 supersession ledger.

**R91-1 — derived `depth()` + `ChainLink`: CONFIRMED over REC-36's literal shape.** The `(List<RunId>, int depth)` dual-field shape carries a redundancy invariant the derived form eliminates (one source of truth; `depth() ≡` the old int by construction, root 0 / child parent+1 preserved exactly). `ChainLink(runId, automationId)` is load-bearing, not cosmetic: the cycle check is "is automation A in this chain," which a bare `RunId` list can answer only via registry lookups — re-introducing external mutable state into the suppression decision, the precise non-determinism this AMD exists to remove. Dropping REC-36's "nullable for legacy traces" is sound (traces are events, not persisted `RunContext` instances; no production Run has ever executed; `causalChain` non-null from birth).

**Supersession ledger audit — all six rows VERIFIED element-by-element:**

| AMD-04 element | Ledger disposition | Audit |
|---|---|---|
| `cascade_depth` counter, parent+1 | CARRIED, re-typed | ✓ — semantics identical by construction |
| Max depth 8, config key | CARRIED as folded | ✓ — Doc 07 §9 carries `automation.max_cascade_depth` (8, 1–32); the Doc-07 key name correctly supersedes AMD-04's draft `automation_engine.*` name |
| `cascade_depth_exceeded` + WARNING + 3-in-60s DEGRADED | CARRIED unchanged | ✓ — §3.7.1 + §9 `cascade_depth_health_threshold: 3` verified |
| Duplicate suppression + `cascade_loop_detected` | CARRIED, mechanism upgraded | ✓ — the Locked §3.7.1 windowed `(correlation_id, automation_id)` set (60 min / 10,000-chain, evicted) is exactly as described; chain-membership replaces it as suppression authority; event name kept, payload honestly labeled as extending the Locked set by `chain` |
| **Rate limiting (50/s, queue 100, `cascade_rate_exceeded`)** | **NOT ADOPTED** | **✓ VERIFIED** — AMD-04's original text (Design_Review_Amendments_v1) contains the clause; Locked Doc 07 §3.7.1 enumerates exactly *three* mechanisms ("Together, the three mechanisms — natural termination, duplicate suppression, and depth limiting"), §9 carries no rate-limit key, and §6.7 (Event Storm Overload: `max_concurrent` + Doc 01 §3.6 backpressure + batching) owns the storm-response space. The clause was genuinely never body-folded. REC-168 (R14-B, FUTURE-AMD, do-not-draft) correctly parks the redesigned per-automation eval-rate form. The formal NOT-ADOPTED disposition inside the superseding AMD is the right governance vehicle — Nick ratifying AMD-91 ratifies the non-adoption explicitly rather than letting it lapse silently. |
| Doc 01 §4.4→§4.5 derived view | CARRIED unchanged | ✓ — label-only edit; `causality_depth_warning` (threshold 50, chain-*event* count) vs run depth ≤ 32 never alias — distinct measurands confirmed |

**INV-TO-02 / REPLAY determinism test — PASS, with one required edit (E91-1).** Chain-membership suppression is a pure function of (chain, config); chains are event-derivable (the reshaped `automation_triggered` carries `cascade_depth`; lineage is recoverable through the correlation chain); REPLAY rebuilds chains from the same immutable log ⇒ identical streams yield identical suppressions, which the windowed set could not guarantee (an evicted entry permits what replay would suppress — the stated defect, confirmed). The dedup map (C2) staying separate and untouched is correct — different mechanism, different keys.

**E91-1 (required):** §2.3 item 4 says cross-hop lineage "is recoverable from the correlation chain's `automation_triggered`/`cascade` events" — but does not pin *where that recovery reads from*. If an implementation recovered lineage from the windowed §4.5 in-memory correlation map (which "remains for trace queries"), windowed/evictable state would re-enter the suppression decision through the reconstruction path and AMD-91-INV-01 would be breached in fact while honored in letter. Add one sentence to §2.3.4: cross-event-hop chain reconstruction reads the **immutable event log** (`readFrom` by `correlation_id`) or the in-process parent `RunContext` — never the windowed §4.5 map. This makes INV-91-01 airtight and enforceable by inspection.

### A.8 Item 8 — `[R93-1]`: dangling-reference severity + the §2.3 reconciliation.

**R93-1 — failure-over-warning: CONFIRMED, with the forward-reference carve-out.** The decisive anchor is in the Locked text itself: §6.1's trigger list *already includes* "unknown entity slug" as a per-definition validation failure (definition not loaded; valid subset loads; `config_error` CRITICAL; health DEGRADED). AMD-93 extends an existing failure class to areas, automation slugs, and calendar refs — it does not invent a severity. An automation that silently can never fire is precisely the HA breakage class the evidence targets; WARNING would reproduce it. The two carve-outs are both correct: tombstone redirects per Identity Model §7.5 are valid-with-DIAGNOSTIC (`automation_slug_redirect`, AMD-92 row 11 — not dangling), and `InvokeAutomationAction` same-file forward references resolve against the post-parse whole-file set (YAML ordering must not be load-bearing; only cross-file/absent slugs fail). Label/type/tag selectors correctly excluded (empty resolution is their contract).

**§2.3 reconciliation: VERIFIED.** Registration as a Doc 06 §7 secondary document (Doc 07 §4.1 :584, attested-no-change) ✓; AMD-71 layout governance with the engine-managed `automations.ids.yaml` companion ✓; **HOT classification under AMD-66** with C7 in-progress-Run snapshot preservation and §3.7 timer hash-reconciliation, both Locked-unchanged ✓; **write-path inherits M6.2 R-1 fail-closed** (tag-bearing documents rejected until the queued tag-preserving-emitter AMD), with the M10 consequence stated plainly rather than papered over ✓ — that candor is exactly what the reconciliation obligation (W0 §4.2) demanded. The (major,minor) adoption of the AMD-67 substrate including INV-67-02's minor-tolerance rule verbatim, the (1,0)-assumed-at-absence INFO posture, and the forward-only/idempotent/backup-first/never-destructive guarantee (REC-151 made structural, AMD-93-INV-01) are all sound. The migration-guarantee attestation test (loader refuses a higher major today) makes the posture falsifiable at (1,0) — good. **AMD-93: RATIFY-AS-IS.**

### A.9 Item 9 — Manifest/fan-out arithmetic + slice placement + survey obligations. **RE-DERIVED, PASS.**

From the §2.2 table independently:

- **Constants:** rows 3–18 mint = **+16** → `EventTypes` 55 → **71** ✓ (rows 1/2/19 constants pre-exist).
- **Records:** rows 3–19 mint = **+17** → `CORE_PRODUCTION_EVENT_CLASSES` 24 → **41** ✓ (rows 1/2 reshape in place, count-neutral).
- **Category rows:** baseline automation rows = triggered/completed only ⇒ rows 3–18 (+16) plus row 19's missing mapping (+1) = **+17** → `EventCategoryMapping` 36 → **53** ✓. All `[AUTOMATION]` per Doc 01 §4.4's category definition; the dual-category command/ledger rows untouched ✓.
- **Per-slice split:** M7.1 = {1, 3, 11–16, 19} (1 reshape + 7 constants + 8 records); M7.2 = {2, 4–10, 17, 18} (1 reshape + 9 constants + 9 records); sums re-derive to 16/17 exactly. M7.3 mints nothing — the ledger/command events pre-exist, consistent with DQ-3's same-advancer/separate-registrations ruling (honored in §3). The placement matches the merged disposition's piece definitions, including REC-141's trace-detail pins riding M7.2 (so `automation_condition_evaluated` publishing in the M7.2 run-pipeline slice is the disposition-consistent reading of "M7.1 trigger/condition path" — M7.1 owns trigger evaluation, definition loading, and duration timers; Run-internal evaluation is M7.2).
- **One instruction-level note (not an AMD edit):** rows 1 (`automation_triggered`, M7.1) and 2 (`automation_completed`, M7.2) sit in different slices. C1 ("an `automation_triggered` is always followed by an `automation_completed`") binds the running system — the M7.1 P2 survey must confirm that no production `automation_triggered` publish path goes live before M7.2 delivers the completing side (registration-without-publish in M7.1 satisfies AMD-92-INV-02 and avoids a C1-violating interim). Carry this into the M7.1 instruction.
- **Survey obligations:** the P2-survey-at-instruction obligation including the behavioral publish-count-pin category (the M6.4 GF-1 lesson) is stated in AMD-92 §2.3 explicitly and in AMD-88 §9, AMD-89 §9, AMD-90 §8 (riding the shared sweep), AMD-91 §9. AMD-93's implementing WU is M7.1, whose survey is owned by AMD-88/89's statements and whose own §3 pre-authorizes the FIX-07 config wiring "so the survey expects it" — adequate; no edit required.

### A.10 Item 10 — anti-requirement placement. **NOTED, NOT A MISS.**

Confirmed realized as §6 explicit non-goals + §7 citations across the block: REC-155 (no templating DSL) in AMD-88/89/90/92/93; REC-162 (no engine retry) in AMD-90/91/92 — including the sharp formulation that no `command_retried` event type exists or will; REC-151 (no destructive migration) in AMD-93, made structural as INV-93-01. Per the AMD-70 section pattern, deliberate.

### A.11 Per-AMD edit registers (consolidated)

- **AMD-88:** E88-1 (required) — fix the breaking-characterization line per §A.5. E88-2 (minor) — offset sign semantics. E88-3 (minor) — record the `ReachabilityTransition`→`targetAvailability` substitution as a documented REC-32 deviation alongside the debounce one.
- **AMD-89:** E89-1 (required) — §4's "empty `includedRoles` is rejected at YAML load (… WARNING-class per the §6.1 path)" is internally inconsistent: rejected-at-load *is* the §6.1 per-definition FAILURE class (Locked §6.1: invalid definitions are not loaded), and AMD-93's severity model says so. Either it is rejected (failure; definition excluded, valid subset loads) or it warns-and-loads — pick one; recommend FAILURE, consistent with the stated misconfiguration rationale and AMD-93-INV-02's spirit.
- **AMD-90:** none. RATIFY-AS-IS.
- **AMD-91:** E91-1 (required) — pin the log-or-fast-path reconstruction source per §A.7.
- **AMD-92:** E92-1 (required) — row 15 (`trigger_duration_state_validated`) omits `triggerId`, contradicting AMD-88 §2.5's promise that "duration-timer events carry both index and id" (rows 12–14 comply). Add `String triggerId` to row 15; decide row 16 deliberately (recommend adding it for family consistency, or document the omission — the limit event identifies a specific trigger by index and the same fragility argument applies). E92-2 (required) — the grep-attestation checklist row per §A.2. E92-3 (minor) — row 17's "(field set unchanged from Locked)": Locked §3.7.1 does not visibly enumerate a `cascade_depth_exceeded` payload (and AMD-04's draft payload differs: `root_automation_id` vs this row's `correlationId`/`maxCascadeDepth`); if §3.7.1 is silent on fields, re-label the row PM-proposed per the AMD's own preamble convention rather than claiming Locked provenance.
- **AMD-93:** none. RATIFY-AS-IS.

None of these findings requires more than the AMDs' enumerated currency edits; no Locked doc is re-opened. No constraints-catalogue, INV-SE-02, or naming-discipline violation was found anywhere in the block. The merged disposition's routing was honored throughout, with every PM-beyond-the-letter point correctly self-flagged except the one E88-3 captures.

---

## §B — B2 C8/C9

### B.1 C8 — `actorRef` identity semantics. **RATIFY-WITH-EDITS.**

**Source re-verification.** The decision's claims, verified at `6c6dd33`, hold semantically at `7c73c91`: `actorRef` is a bare nullable `Ulid` on the 14-field `EventEnvelope` and 9-field `EventDraft`, persisted and hydrated through the event store, with derived-event inheritance the existing `EventDraft` rule — all corroborated independently in the knowledge base (the foundation fidelity record and the architectural analysis). The intervening M6 work (config pipeline, secret store, hot reload) touched neither the envelope field nor the persist/hydrate sites; `EventTypes` 53 → 55 across M6.1/M6.4 is fully accounted for by the two config mints. Line anchors: condition §0.2-3.

**Gate rows.** Round-trips ✓ (no serialized shape changes — the regret-proof core: convention and resolution rules are fixed, not bytes). Enforceable ✓ (the test set is well-formed, including the negative test rejecting unregistered ULIDs at the publish boundary — forward-enforcement by allocation discipline until `ActorRegistry` exists, which is itself a forward-add, never a log migration). Owner-doc ✓ (Doc 01, currency note now, enforcing amendment at M10/multi-user). The four-kind closed set maps the four real command sources exactly; Tier-1 API keys → `API_CLIENT`-never-`PERSON` is the audit-honesty keystone (attributing a client's actions to a person would be a false audit claim); automations-stamp-`AutomationId` closes the gap the Hubitat evidence names. **Regret-proofness: PASS** — no already-written byte changes; the kind taxonomy is recoverable by typed-ID provenance.

**EC8-1 (required) — Glossary §5.4 conflict, currently unlisted in the decision's edit set.** Glossary §5.4 (Actor) states: "The Actor always references a `user_ref`, not a `person_ref`," and defines the causal-reference semantic as the Actor *carrying the originating User's identity through automation chains* (the INV-MU-01 framing). C8 rules 2/5 contradict both: the closed set is four-kind (not always a user), and automation-originated events stamp the **`AutomationId`**, not the originating user. C8's choice is the right one — it is what answers "what turned on my light?" (the automation, by name; REC-146's evidence is precisely that users want the *automation* identified), and the originating human remains fully recoverable via the correlation chain to the user-actored root event, so INV-MU-01's who-caused-this intent survives intact through the trace rather than through field duplication. But the conflict must be disposed, not discovered later: **add Glossary §5.4 (and an INV-MU-01 wording check) to the ratification currency edits**, superseding the "always references a user_ref" sentence and restating the causal-reference semantic as chain-recoverable. This is the same Locked-text-disagrees-with-the-decision class as R92-3 and gets the same treatment.

**EC8-2 (suggested, non-blocking):** the envelope already carries `EventOrigin {DEVICE, USER, AUTOMATION, SYSTEM}` — a second, existing witness to actor kind. One sentence stating the agreement rule (when `actorRef` is non-null, its kind and `origin` must be consistent — e.g., kind `AUTOMATION` ⟺ `origin = AUTOMATION`; `PERSON`/`API_CLIENT` ⟺ `USER`) makes the stamping tests doubly enforceable and gives the pre-registry log a coarse kind discriminator for free.

**Observation (non-blocking):** any pre-C8 non-null `actorRef` values written before the convention (real exposure is near-nil pre-MVP — dev/test logs) sit outside the closed-set guarantee; the eventual `ActorRegistry` should tolerate unresolvable historical refs rather than assume total coverage. Worth one line in the M10 design notes.

**REC-146 (R14-A, Hubitat provenance) — weighed as ratification support, content unchanged**, exactly as the dispatch instructs: external community validation that the convention C8 freezes is the differentiator surface ("what turned on my light?" as the platform's top provenance complaint class). It strengthens the case for ratifying *now*, on the M7 critical path, before automations begin writing events.

### B.2 C9 — energy event shape. **RATIFY-AS-IS.**

**Source re-verification at `7c73c91`:** zero energy event types still true — the two post-`6c6dd33` `EventTypes` additions are both config events; `EventCategory.ENERGY` / `EntityType.ENERGY_METER` remain reserved-unpopulated; energy continues to ride generic `telemetry_summary`. The retrofit-avoidance rationale stands.

**Gate rows.** Round-trips ✓ — the four `power_measurement` attributes are `QuantityValue`s with canonical units (W/Wh/V/A) under the AMD-47 variant set and the AMD-52 typed-payload codec, both shipped and round-tripping; the six aggregates ride `telemetry_summary`; no new serialization. Enforceable ✓ (attribute-set + unit assertions, aggregate-field assertions, category + consent-scope assertions — all stated). Owner-doc ✓ (Doc 02 + data-readiness §4 + INV-EI-01; implementing amendment at the energy milestone; currency note now). **Regret-proofness: PASS** — rides existing types, mints no protocol vocabulary, changes no bytes; a Zigbee plug and a Modbus inverter become semantically identical at the event layer from the first energy event, which is exactly what NexSys Grid/Assure consumption requires.

**Doc 15 §3.4 reconciliation — correctly STAGED, confirmed not a re-open.** Doc 15 §9's Locked default `encrypted_scopes: [identity, presence_personal]` keeps energy plaintext-at-rest at MVP (§10: bulk telemetry plaintext); the data-readiness §6 alignment treats energy as a shred-scope. C9 stages the resolution on OQ-15-2 (the Pi-4 microbench) as a **list-tuning of the §9 default** — the identical mechanism the M6 charter itself ratified for the encrypted-scope set ("a TUNING of a Locked-doc default, NOT a Doc 15 re-open"), and the shape decision is explicitly invariant to the tuning's outcome. Correct, and correctly flagged for reconciliation when OQ-15-2 returns.

**Disjointness from the M7 block — CONFIRMED, no collision.** AMD-92 §6 fences energy out by name ("NO energy/C9 vocabulary — C9 rides existing types"); C9 mints no event types for AMD-92 to collide with; the only shared surface is `EventCategory`, where AMD-92 adds `[AUTOMATION]` rows and C9 populates `ENERGY` classification — orthogonal.

### B.3 C8 ⊕ AMD-92 coupling statement. **SAFE TO RATIFY TOGETHER: YES.**

The independence claim was verified, not accepted: (a) all nineteen AMD-92 §2.2 payload rows were audited — **no payload-level actor field exists anywhere** (REC-39 obligation 5 honored; the envelope owns attribution, so a C8 adjustment cannot strand a payload shape); (b) AMD-92 §2.4 stamps exclusively via the envelope seam, cites C8 as PROPOSED-pending throughout (never silently assumes ratification), and pre-commits the M7.2 stamping tests to re-pin if the convention adjusts; (c) `automation_invoked` correctly carries the *invoker's* actorRef with the resulting run inheriting it — coherent with C8 rule 5's inheritance clause. The bundle's logic therefore holds in both directions: ratifying the block with C8 floating would freeze stamping-test language against an unratified convention (the dispatch's stated hazard), while the payload contracts themselves survive any C8 outcome — which is what made a one-pass bundle reviewable at full discipline. Bundling: endorsed as exercised.

---

## §C — Prose-vs-source / Locked-text disagreement register (complete)

1. **Doc 07 §3.7 :326 (NORMAL, reasoned) vs §6.2 :709 (CRITICAL, ×2)** — adjudicated R92-3: NORMAL survives; correction note covers both §6.2 occurrences.
2. **Glossary §5.4 / INV-MU-01 vs C8 rules 2/5** — adjudicated EC8-1: C8 supersedes with a Glossary currency edit; human traceback preserved via the correlation chain.
3. **AMD-88 block-context breaking characterization vs its own §2.5** — E88-1.
4. **AMD-89 §4 "rejected… WARNING-class" vs the §6.1 failure class** — E89-1.
5. **AMD-92 row 15 (and arguably 16) vs AMD-88 §2.5's both-index-and-id promise** — E92-1.
6. **AMD-92 row 17 "unchanged from Locked" vs §3.7.1's apparent payload silence (and AMD-04's differing draft payload)** — E92-3.
7. **AMD-88 §2.2 unflagged REC-32 deviation (transition enum → `targetAvailability`)** — E88-3 (research-fidelity register; substitution endorsed).
8. **Pre-existing, not block-introduced (note only):** Doc 07 §8.2 lists `RunStatus` with six values; the source-verified enum has seven (incl. `INTERRUPTED`, per R14-A). A standing doc-currency drift for a future edit — and incidentally a second argument for AMD-92's `RunStatus.name()` flattening, which insulates the wire contract from enum-membership drift.

## §D — Opposing considerations and residual risks (steelman)

- **FLATTEN forecloses payload-level type safety.** If a typed cross-module Run query surface materializes (M10+), consumers will compare bare `Ulid`s. Accepted because payloads are serialization shapes by project doctrine (Doc 09 §4), field names carry the semantics, and relocation remains available later with zero log migration — the frozen bytes are identical under either residency.
- **BEST_EFFORT's data-dependence.** A user cannot predict ledger behavior without knowing the capability's `ExpectedOutcome`. Mitigated by trace observability, the pinned default test, and the calibration spike; the alternative defaults are each worse (REQUIRED risks double-actuation surprise at M8.2; OPTIMISTIC silently regresses Locked behavior).
- **NORMAL for `automation_disabled` trades notification salience for taxonomy discipline.** If field experience shows users missing disables, a priority bump is a cheap forward amendment; the reverse direction (demoting a CRITICAL) is socially harder. The reasoned Locked row still wins today.
- **The chain-membership rule is stricter than the windowed set in one direction:** a legitimate re-fire of automation A deep in a long chain (A→B→C→A where the second A is *intended*) is now suppressed permanently within the chain rather than window-dependently. This is the deterministic behavior the design wants (INV-TO-02), and the diagnostic makes it visible — but it is a real semantic narrowing users may notice; the M7.2 instruction's authoring guidance should state it.
- **Bundle-shaped risk, retrospective:** a C8 REJECT would have forced re-dispatch of AMD-92's stamping language. The verified C8-independence-by-construction is what neutralized this; future bundles should preserve that seam pattern.

## §E — Dispatch metadata note

The dispatch and block files are dated 2026-06-13 against a 2026-06-12 review date — consistent with the session's known local/UTC dating offset (per the snapshot's own "executed Jun 11 local / Jun 12 UTC" note). No action; recorded for the audit trail.

---

**Reviewer:** Independent DOCS-Project reviewer (HomeSynapse Core · Design & Governance).
**Disposition requested of Nick:** ratify per the table in §0, with the six required edits (E88-1, E89-1, E91-1, E92-1, E92-2, EC8-1) folded and the three §0.2 attestations recorded at the ratification commit. On ratification: watermark 87 → 93; invariants §42+ registered (11 candidates); AMD-04 SUPERSEDED banner; Doc 07 §3.4/§3.7(+invoked row, priority correction)/§3.9/§3.11.2/§3.12/§3.3/§4.1/§6.1/§6.2(correction note)/§8.2/§16, Doc 01 §4.3/§4.4/§4.5, and Glossary §5.4 currency edits applied; C8/C9 conventions govern from the next event written.
