<!--
file: context/audits/2026-06-30_frontend-dev-lane_T1.2-scenario-engine_return.md
purpose: Frontend build lane return for the 2026-06-30 pre-hardware-sprint session — the T1.2 mock scenario engine (the window keystone) + offline as a first-class state (FE-4 core) + the axe-core a11y gate (authored) + T1.3 realTransport audit & go-live checklist. Hub reconciles into the spine; one install hand-off for Nick.
audience: the PM hub (reconciles + hand-off); Nick (the one install command in §3; the go-live checklist owner).
state-type: lane return (frontend build lane).
status: RETURNED 2026-06-30. Write-isolated: wrote ONLY under web-ui/dashboard/** + this return. No Core, no spine, no Locked doc, no other lane.
anchors: context/planning/2026-06-30_frontend-pre-hardware-sprint.md (the authority this executes) · _amendment-1 + the 2026-06-29 master plan · contract freeze v1.1 (consumed, untouched) · web-ui/dashboard/{MODULE_CONTEXT,FE1_GO_LIVE}.md
-->

# Frontend Build Lane — Return (T1.2 scenario engine + offline + axe + T1.3, 2026-06-30)

## TL;DR
Executed the pre-hardware sprint's **Tier-1 keystone** and its swap-readiness spine. The mock is now a
**scenario engine**: every value the frozen v1.1 contract allows (all command outcomes incl. the honest
`UNCONFIRMED`, all origins incl. `UNKNOWN`, all four non-firing verdicts, all run statuses, cascades,
300-run/500-event lists, empty) and every transport condition (503-replaying / offline / slow / 401 /
403 / ETag-304) is **one click away** via a dev-only **DevPanel** + shareable `?scenario=&mock=` URL
params. **Offline is now a first-class honest state** end-to-end (the FE-4 state-matrix core). The
**axe-core** a11y suite is authored (one install for Nick). And **T1.3** is done: `realTransport` is
audited against contract §0 and the one-sitting **go-live checklist** is written — so FE-1 is hours.
Gate is **deferred to CI** (the sandbox mount truncates fresh writes, unchanged); code authored + reviewed host-side.

## 1. What shipped (all under `web-ui/dashboard/`)

**T1.2 — the mock scenario engine (the keystone).**
- `src/lib/api/mock/scenarios.ts` — a `MockDataset` type + contract-shaped **factories** (`makeChain`/`makeAction`/`makeRun`/`makeEvent`/`makeNonFiring`/…) + a **scenario registry**: `default` (coherent home), `all-outcomes` (every outcome + run status, click-through), `cascade` (parent→child, the "what triggered this" link), `all-verdicts` (the four "why didn't it fire?"), `all-origins` (incl. UNKNOWN), `large` (300 runs / 500 events — **lazily generated**, forces virtualization in T2.3), `empty` (empty-as-teaching). Bundle-conscious: variants are `build()` closures, large lists are generated by code, not literals.
- `src/lib/api/mock/mockState.ts` — the active **scenario + transport condition**, initialized from URL params, mutated live, subscribable; a `generation` counter yields stable-yet-invalidating ETags.
- `src/lib/api/mock/mockTransport.ts` — refactored to **serve the active dataset + inject the active condition** (offline throws a fetch-like error; replaying returns a `REPLAY` projection **and** 503 on `/api/*`; 401/403 forced; slow = latency; ETag 304 on revalidate). **The real/mock swap is still ONE switch** (`index.ts`) — scenarios only shape mock data.
- `src/components/DevPanel.tsx` (+ css) — a mock-only floating control (rendered only when `API_MODE === 'mock'`, **outside** the auth gate so 401/403 are recoverable); accessible radiogroups; writes the URL. `kickPoll()` added to `poll.tsx` for instant propagation.
- `contract.test.ts` extended: **every scenario dataset is validated** against `shapes.ts` — the sprint's "every scenario is contract-shaped", enforced in CI.

**Offline as a first-class state (FE-4 core).** `client.ts` wraps a transport network failure as a typed `network-unreachable` / `isOffline`; `poll.tsx` + `useApi` carry an `offline` phase/status; `feedback.tsx` `OfflineState` renders it **calmly** (polite live region, retry, "nothing in your home is affected") — never a generic error, never a fake success (INV-SA-03). `Resource` + the AppShell status pill fold it in. Exercise via `?mock=offline`.

**axe-core a11y gate (authored — see §3 for the install).** `src/a11y.test.tsx` runs axe over the hero, the theme toggle, the auth form, the status pills, and the honest feedback states; it runs inside `npm run test` (so `frontend.yml` already gates it). `vitest.config.ts` now enables the Preact JSX plugin (first component-rendering tests). `axe-core@^4.10.2` added to `package.json` devDeps.

**T1.3 — FE-1 readiness.** `FE1_GO_LIVE.md`: `realTransport`/`client` **audited against contract §0** (bearer/401/403, 503+backoff, ETag/304, RFC-9457, cursor echo, poll-on-`viewPosition`, offline) — each verified against a scenario-injected condition — plus the **one-sitting go-live checklist**. Verdict: contract-faithful; **two non-structural follow-ups** to close in FE-1 (cursor-follow for large real sets; optional dev-runtime response validation) and two nice-to-haves (ETag-cache cap; `problem+json` Accept). None blocks a curated-set demo.

**Files** — created: `mock/scenarios.ts`, `mock/mockState.ts`, `components/DevPanel.tsx` + `.module.css`, `a11y.test.tsx`, `FE1_GO_LIVE.md`. Modified: `mock/mockTransport.ts`, `api/index.ts`, `api/client.ts`, `lib/poll.tsx`, `components/{Resource,feedback,AppShell}.tsx`, `contract.test.ts`, `package.json`, `ci/frontend.yml`, `vitest.config.ts`, `MODULE_CONTEXT.md`. Plus this return. **Nothing else.**

## 2. Verification
- **Authored + reviewed host-side; the full `frontend.yml` gate is deferred to CI** — the sandbox mount still truncates freshly-written files when the shell reads them, so in-sandbox `tsc`/`vitest` only yield false positives (env-model, unchanged). Careful review points: the mock stays behind the one switch; every scenario is contract-shaped (and now test-enforced); strict-TS clean (typed factories, `Record` access guarded, no `any`); the DevPanel is mock-only + outside the gate.
- **Run on the clean checkout:** `cd web-ui/dashboard && npm ci && npm run verify` (after the §3 install). The new `contract.test` scenario block + `a11y.test` run inside it.

## 3. One install for Nick (the sanctioned hand-off)
axe-core needs the lockfile regenerated on a real machine:

```
cd web-ui/dashboard
npm install --save-dev axe-core@^4.10.2     # regenerates package-lock.json
npm run verify                               # confirm green (incl. the axe a11y suite)
```

`package.json` already lists it; this just materializes the lockfile + node_modules. If the a11y suite or the Preact-JSX-in-vitest wiring flags anything, it's a fix-forward config nudge (I couldn't run vitest in-sandbox) — report and I'll adjust.

## 4. Notes / watch-items
- **Bundle:** the scenario engine + DevPanel are **dev/demo code that currently ships in the real build too** (`index.ts` imports the mock transport statically). Estimated +~6–8 KB gzip. Still within budget, but the **highest-value T2.3 bundle action is to dynamic-import the mock transport** so it's excluded from a real-backend build entirely — recommend doing it in T2.3 (it makes `api` creation async, a contained refactor). Keep the Inter subset tight (unchanged).
- **Preview fidelity (carried):** any future sign-off mockup reproduces the real component SVGs, not sandbox placeholder glyphs.
- **Disciplines held:** frozen v1.1 consumed, never invented (the engine only shapes mock data to the contract); write-isolation; pointer-not-copy; honest-claim on all new copy (the offline/empty strings are calm + honest).

## 5. Next WU (refuse-to-close)
**Finish T1.1 / FE-4 against the scenario engine** — now that every state is one click away: the full honest **state matrix** across the hero *and* the supporting views (loading skeletons, empty-as-teaching, RFC-9457 error, calm-503, offline — offline's plumbing is done, the per-view treatments remain), **WCAG 2.2 AA re-verified in BOTH dark (default) and light**, the 2.2 trio (target-size ≥24px, focus-not-obscured on the Drawer, accessible-auth/allow-paste on AuthGate), color+shape+text on every status, a real **screen-reader pass**, and **mobile-first** layout (the hero excellent on a phone). Then **FE-PWA → FE-5** per the sprint. **Blocked until hardware:** FE-1 (run `FE1_GO_LIVE.md` the moment Core is up on loopback) and FE-7 (real-device confirm — bench first-light).
