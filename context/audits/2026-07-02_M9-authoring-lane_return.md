<!--
file: context/audits/2026-07-02_M9-authoring-lane_return.md
purpose: THE return of the write-isolated M9-authoring lane (dispatched 2026-07-01, launched 2026-07-02): deliverable paths, the proposed M9 split with per-unit done-whens, open [Design point]s for Nick, the preflight reconcile-report, drift findings for the hub, the prior-art research fold (Nick's deeper-research directive), and the lane self-review. REPORT — nothing committed; the v13 hub runs the independent source-audit; Nick issues.
audience: v13 hub (audit), Nick (rulings + issue).
state-type: lane return (write-isolated).
status: COMPLETE 2026-07-02. Lane wrote ONLY: this file · context/instructions/2026-07-02_M9.1_*.md · context/pre-verifications/WU-M9.1.md. NO spine edits, NO Locked-doc edits, NO core source edits, NO commits.
baseline re-derived at preflight: core `52824e9` (= pin) · docs `1509b34` (= pin; watermark AMD-97, invariants 172/51) · bench `5ceff3b` (= pin) · hivemind `07f3065` (beats 43–45; pin was cea7ae1 — ahead-as-anticipated) · skills `0825409` (frontend-currency carry; pin was 846fe56 — benign).
-->

# M9-Authoring Lane Return — 2026-07-02

## 1. Deliverables

| Artifact | Path | Status |
|---|---|---|
| M9.1 coding instruction (unit 1 of the proposed split) | `context/instructions/2026-07-02_M9.1_integration-spine_supervisor-and-command-routing_coding-instruction.md` | **ISSUE-READY**, conditional on hub audit + DP-A ruling |
| Pre-verification artifact | `context/pre-verifications/WU-M9.1.md` | VERIFIED at `52824e9` (33 observed-signature rows §A, 7 must-create rows §B, deferral verification §C) |
| This return (incl. the M9.2–M9.4 split charter §3, which the hub converts to instructions just-in-time) | `context/audits/2026-07-02_M9-authoring-lane_return.md` | COMPLETE |

**Grounding provenance:** full §0/§1 grounding-set read from source at the pinned SHAs (Doc 08 §3 complete as amended, Doc 02 §3.6–3.8 as amended, AMD-96/97 ratified texts, register §2 INV-ES-09 + §51 incl. the written AMD-97-INV-01 test shape, both fixtures + all corpus entries + bringup report, 10 modules' MODULE_CONTEXT + verbatim module-info, version catalog, backlog + lessons + both decision records). Three subagent passes: broad territory extraction → targeted verbatim signature extraction → **adversarial audit of the finished draft (~90 verifiable assertions: ZERO fabrications, zero BLOCKING findings, 5 minor findings — all folded before this return)**. External prior-art research pass per Nick's directive (§5).

## 2. Freshness preflight — reconcile-report (aggregate: STALE-benign, zero CONFLICTED; lane authorized to proceed per its charter)

C1 snapshot stamp PASS (2026-07-01, beat-44 entry matches) · C2 W27 plan PASS (IN_PROGRESS) · **C3 STALE-benign** — the snapshot's `Latest commits` field (line ~79) still narrates the v11-era state (core `eed477e`+uncommitted-AB-4, hivemind `77876ec`) while HEADs are `52824e9`/`07f3065`; every intermediate commit IS accounted for in the beat-39→45 session-log entries; field-level restamp is hub-owned · **C4 STALE-benign** — the backlog's M9 ROW still carries the pre-pivot scope ("Supervisor tree; health FSM…" with M14 as a separate Zigbee row) while the ruled live scope (beats 34/41/42/43, coder-handoff NEXT-WU) is "M9 = the real Zigbee adapter, hero on measured silicon"; the §3 split below is written to replace that row on ratification · C5 PASS (Open Risks current 2026-07-01; **no open deferred build gate** — ci.yml GREEN on `52824e9` verified at beat 43 subsumes AB-4's) · C6 PASS (coder-handoff names M9, refuse-to-close honored) · C7 PASS (all touched-module MODULE_CONTEXTs populated; two STALE-content findings → §6) · **C8 STALE-benign, known+recorded** (cross-agent archival is explicitly phantom-gated by hub decision, beat 43 item 5) · C9 PASS (both skill mirrors byte-identical — Nick's sync landed post-beat-45) · C10 PASS (spot-check) · C11 PASS (this lane's whole method was source round-trip; counts 71/41/53 re-derived from the pinning tests verbatim; invariant total 172/51 read from the register's own regeneration note).

## 3. The proposed M9 split (DP-A — Nick + hub rule it; P1 sizing: the gap is unambiguously >1 compile-and-commit unit)

**Why 4 units + a gated breadth unit:** the end-to-end gap spans (a) a supervisor+routing layer with ZERO production call sites today, (b) a serial/ASH/EZSP transport with no catalog dependency yet, (c) an interview/reporting/ingestion/profile layer, and (d) the confirmation-acceptance + hero E2E — each independently a M6.x/M7.4x-sized compile-and-commit unit. Order is **spine-first** (Nick's 2026-06-22 "live spine first" ruling; the M7.3 deferred-wiring lesson): the INV-ES-09 replay guard extends to the adapter seam BEFORE any real adapter I/O exists, and every later unit lands into a proven live path instead of accumulating breadth-first components. Each unit's instruction is authored just-in-time after the prior unit's WUCP Phase 2 (standing backlog discipline), from this charter.

### M9.1 — Integration spine (instruction AUTHORED, §1)
Supervisor impl (minimal FSM: HEALTHY↔restart→FAILED; DP-10), `command_dispatched`→`CommandHandler.handle` routing subscriber (causation-join, LIVE-only, bounded cache), Phase-6 composition wiring (7-arg ctor, skip-if-empty), recording fake adapter, **INV-ES-09 gate extension** (recorder-zero-on-replay + LIVE positive control). Zero mint (71/41/53). JPMS: integration-runtime `+requires transitive event.bus` `+requires org.slf4j`; lifecycle `+requires integration.runtime` (plain) + promotion `integration` → transitive (the 7-arg public ctor puts `IntegrationFactory` on exported API) — all with Gradle lockstep.
**Done-when:** T1–T21 green; targeted compileJava clean; `RunPipelineConfirmWiringTest`+`HomeSynapseCoreTest` pass UNMODIFIED; extended `RunPipelineReplaySafetyTest` proves adapter-recorder-zero-on-replay AND recorder-fires-LIVE; both MODULE_CONTEXTs updated; full gate deferred-and-flagged.

### M9.2 — Transport + EZSP protocol (hardware-facing; EZSP-first per DP-C)
jSerialComm catalog entry (≥2.10.x — `synchronized`→ReentrantLock internally since 2.10.0, LTD-11-consistent; JNI still pins carriers → the Doc 08 §3.2 platform-thread model stands; **Apache-2.0 license election** at the catalog line) · `EzspAshTransport` (destuffing, LFSR-0x42/0xB8 derandomization, CRC-CCITT 0x1021/0xFFFF, window=1 stop-and-wait, adaptive ACK 1.6 s start/0.4–3.2 s band, 5-timeouts→FAILED, RST+Cancel-byte/RSTACK ≥2 s timeout, RSTACK reset-code surfaced in diagnostics — flow-control-mismatch vs firmware-crash discrimination) · EZSP layer: **version cmd `0x0000` first, always legacy single-byte frame format**; band v13–v14 with below-13 WARN `zigbee.ezsp_legacy_version` / below-8 `PermanentIntegrationException` (Doc 08 §3.3 verbatim); **version read at stack-init, never a registry (AMD-96/E6)**; version-variant codec seam (v14 = 32-bit `sl_status_t` widths — synthetic-frame unit-tested; physical v14 = the AMD-96 batch contingency) · **single-in-flight paced command pipeline** (the bench's own EmberZNet dump: `CONFIG_APS_ACK_TIMEOUT=1600`; burst = the documented ASH-collapse mode) · auto-detect: minimal ZNP `SYS_PING` probe-encode (full ZnpTransport NOT built — DP-C) → flush/settle → ASH RST per §3.3 sequence; port identity = VID:PID `10c4:ea60` + by-id path + probe fingerprint, **never descriptor strings (AMD-96/E2 — measured SONOFF-branded)** · port-death handling (`isOpen()` lies on unplug — disconnect-listener where available + read-error + ASH-liveness watchdog; reopen by stable id; capped backoff) · network formation/resume (§3.13: two-tier channel preference, SecureRandom key, INV-SE-03 storage via the secrets infra, params persisted) · watchdog `nop()` ping (30 s).
**Done-when:** byte-level framing tests against verbatim-embedded external vectors (UG100/AN706 constants embedded per the external-standard rule); version-negotiation tests incl. synthetic v13 AND v14 responses + both rejection tiers; auto-detect probe tests (fake transport); formation/resume param round-trip; zero real serial I/O in unit tests (fake `CoordinatorTransport` — `NoRealIoExtension` gives no serial protection, verified); zero mint; module-info diff = jSerialComm automatic-module requires (non-transitive if `SerialPort` is kept OFF the exported API — **authoring note: resolve the Phase-2 `CoordinatorTransport.open(Object)` → `SerialPort` signature question with the §authoring lockstep check; keeping the exported interface `Object`-typed or internalizing the transport avoids a transitive jSerialComm edge — settle at instruction time**).

### M9.3 — Interview, reporting config, ingestion, profiles (fixture-heavy)
Resumable interview FSM (§3.4: announce→node-desc→active-EPs→simple-desc→Basic-read→profile-match→`device_discovered`→adoption→bind→configure-reporting; per-step 10 s/whole 60 s/3 retries 5-15-30 s; PARTIAL flag; sleepy pending-queue resumed on ANY frame, 24 h expiry; **read Basic from the first APPLICATION endpoint — the Hue light lives on EP 11 not EP 1 [measured]; Green Power EP 242 + unknown mfr clusters (0xFC01/0xFC04/0xFC57 measured) skipped gracefully, never interview failures**) · bind-then-configure-then-**verify**: ConfigureReporting per §3.7 defaults (**incl. the AMD-96 OccupancySensing row — the hero-trigger cluster**) followed by ReadReportingConfiguration read-back; per-capability posture classification (VERIFIED_REPORTS/READBACK_ONLY/NONE × ON_CHANGE/PERIODIC/SLEEPY/NONE) recorded as device facts feeding AMD-97 `confirmability`; **re-apply + re-verify on every rejoin/device-announce** (Hue power-loss rejoin + factory-reset config-wipe are the measured/documented failure modes) · cluster handlers: OnOff, LevelControl, **ColorControl-CT with mireds→Kelvin AT INGESTION (K=1,000,000/mireds; raw mireds retained per Doc 02 §3.7 auditability — AMD-96 Kelvin-canonical)**, OccupancySensing (bit 0 → `occupancy.occupied`), PowerConfiguration (raw/2 battery), IAS Zone **tolerate-not-require** (§3.12 CIE-write→enroll-response flow attempted, enrollment recorded as fact, NEVER an interview gate, `ZoneStatusChangeNotification` ingested regardless of enrollment state — the SNZB-03P is dual-path with occupancy active [measured]) · **ingestion dedup: consecutive-TSN + payload-equality, scoped per (device, endpoint, cluster), cleared on device-announce/rejoin** (measured: EVERY SNZB event ×2, 8–21 ms; TSN is 8-bit wrapping + resets on power-cycle) · **`DeviceProfile` 9→10 record-arity change: the AMD-97 8-field `confirmation[]` block** (enum `degradeRule` 4 values; empty for read-only) + full P2 ctor fan-out survey (the 5-nullable-collections gotcha becomes 6) · `DeviceProfileRegistry` impl + bundled `zigbee-profiles.json` carrying the MEASURED Hue-LCA017 5-block + SNZB-03P-empty values (corpus = pointer-not-copy: values cite `nexsys-bench/corpus/devices/*`) · **`WithinTolerance.evaluate()` implementation in device-model** (verified STUB today; CT confirmation is impossible without it; cross-module consumer survey mandatory) · device metadata cache (`zigbee-devices.json`, 30 s debounce) + AvailabilityTracker with restart-initialization from persisted pre-restart state (§8.1 M-1).
**Done-when:** BOTH bench fixtures replay green through ingestion (SNZB walk-test → deduped `occupancy.occupied` edge stream matching the fixture's derived block; Hue windows → Kelvin-converted CT reports incl. the ±1-mired drift rows); interview FSM unit-tested on synthetic descriptor sequences incl. sleepy-resume + PARTIAL; read-back verification tests (ACK-lies case: configure accepted, read-back differs → posture downgraded); profile load/match/override precedence; `WithinTolerance` tests incl. the ±50K CT band; **event-mint expectation pinned at authoring time** (state_reported/device_discovered/availability_changed all pre-exist; any `zigbee.*` mint moves 71/41/53 and must be declared, surveyed, and category-mapped — expectation for M9.3 remains ZERO with §3.11 telemetry deferred). **Authoring pre-verification must check: `DiscoveryPipeline`/adoption-path implementation state + `CapabilityRegistry` impl ABSENT (verified) — the adoption seam may need a minimal slice; scope it explicitly then.**

### M9.4 — Command path + confirmation acceptance + hero E2E (the moat lands)
`ZigbeeAdapter` composition (Phase-2 39-file surface goes live minus deferred breadth): `CommandHandler` → cluster-handler ZCL build → paced transport → DefaultResponse/ACK → `command_result` (round_trip_ms; QUEUE_FULL at the bounded 64-frame queue; dual priority) · **transition-aware expectation timeouts** (transition+2 s; CT window ≥ report-min-interval + margin — measured 6.7–8.4 s ⇒ 15000 ms recommended) · the four **AMD-97 engine-consumption caveats as first-class behaviors**: idempotent no-change⇒no-report → confirm-from-cache-or-readback (never report-wait; cache-confirm only if prior state is report-fresh); TOLERANCE confirms the settled value (transition transients stream mid-fade [measured 22→36 etc.]); superseded same-attribute expectations EXPIRE (never false-fail/false-confirm — measured coalescing 161→216→221⇒one report); readback path for READBACK_ONLY (`color_loop_active 0x4002` readable-never-reported [measured]) · **the AMD-97-INV-01 acceptance suite exactly per register §51's written test shape**: replay the Hue confirmation-windows fixture — `on_off`/`brightness`/`color_temperature` reach CONFIRMED only via authoritative `Report_Attributes`; `color_loop_set` + `identify` render immediate honest UNCONFIRMED and NEVER transition to CONFIRMED; superseded expectations expire verdict-free · hero E2E on fixtures (SNZB occupancy edge → hero automation → command → Hue confirm window → CONFIRMED with queryable causal chain) · `Main`/composition wiring of `ZigbeeAdapterFactory` · replay-gate extension #2 (fake-transport recorder: zero serial writes across replay) · then **Nick's live-bench acceptance** (re-pair both devices under HomeSynapse on the frozen 7.4.5.0/v13 baseline; **firmware-version-aware band with reflash-re-anchor contingency; Configure-Reporting as an explicit acceptance step — fixture replay structurally cannot catch its omission; reporting-config parity recorded OR re-measured under own §3.7 posture before trusting timeout bands; latency reference points pinned: corpus on_off = ACK→report, brightness/CT = command→report**) → 72 h soak entry.
**Done-when:** the §51 suite green on fixtures; hero E2E green; live-bench checklist executed (the beat-42 seven, all covered: #1 M9.3-build+M9.4-acceptance, #2 M9.4, #3 M9.4, #4 M9.3+hero rule, #5 M9.3, #6 M9.3, #7 M9.2+M9.4); soak entered.

### Deferred (post-hero; NOT M9.1–M9.4)
Supervisor breadth (DEGRADED/SUSPENDED/probe ladder/suspension cycles/JFR quotas/§3.14 planned-restart suppression/Kahn ordering/integration-scoped context wrappers) — **gated on NQ-6** (recommend: run the NQ-6 restart spike DURING the 72 h soak) · full `ZnpTransport` (Wave-2, with ZBDongle-P silicon — DP-C) · Tuya 0xEF00 + Xiaomi TLV codecs (DP-D) · §3.11 telemetry/topology + §3.15 route health (observational; seams reserved) · coordinator backup format (DP-E) · groups/scenes (already out per §3.10).

## 4. Open [Design point]s for Nick

- **DP-A — Ratify the split + refresh the backlog M9 row.** The row still says supervisor-tree-only (pre-pivot); §3 is written to replace it (M9.1–M9.4 sub-rows + the deferred list). Blocking: yes for issue (M9.1 is otherwise ready). Rec: ratify as proposed.
- **DP-B — Integration identity durability.** M9.1 ships `IntegrationIds.deriveStable(type)` (SHA-256-derived Ulid — restart-stable; a boot-random id would orphan every `Device.integrationId`). This deviates from LTD-04's `UlidFactory.generate()` guidance (wrapper preserved; not time-ordered). Options: (i) adopt deterministic derivation as the permanent policy via a one-line governance note/AMD; (ii) generate-once-persist (the `home_id` file pattern, app-side); (iii) event-log-derived recovery. **Rule before M9.3's real device adoption makes identity one-way.** Rec: (i). Blocking: no for M9.1 (interim is documented + reversible until M9.3).
- **DP-C — Wave-1 ZNP scope = probe-only.** Full `ZnpTransport` defers to Wave-2 (its validation silicon — ZBDongle-P — is a Wave-2 arrival; D5 curated-subset direction). Per the 2026-06-28 lesson, record explicitly: **V1-Wave-1 ships a deliberate subset of Locked Doc 08 §3.3** (EZSP path full; ZNP probe-only), subset-vs-eventual relationship on the record at the next docs-touching amendment. Rec: accept. Blocking: no.
- **DP-D — Tuya/Xiaomi codecs out of M9 entirely** (Doc 08 §3.8/§3.9 remain the Locked contract; implementation rides the Wave-2/M14-class breadth). Same on-the-record subset note. Rec: accept. Blocking: no.
- **DP-E — Coordinator backup/migration seam (new, from prior-art research §5).** zigpy+z2m's shared Open-Coordinator-Backup format exists precisely because un-backed-up coordinators strand whole networks; Doc 08 §3.13 persists network params but no exportable backup. Cheap now, existential later. Rec: reserve the seam in M9.2's instruction (a `[Design point]` note, zero code), AMD candidate post-hero. Blocking: no.
- **DP-F — First-network formation vs the bench's ZHA network.** M9's live acceptance re-pairs both devices under HomeSynapse's OWN network (fresh formation; the ZHA-formed channel-20 network on the stick is the reference capture's, not ours). This is assumed in §3/M9.4 — flagging because it consumes bench time (re-pair + re-verify reporting). Rec: confirm assumption. Blocking: no.

## 5. Prior-art research fold (Nick's deeper-research directive) + further-research routing

Full sourced report retained in the lane transcript; the load-bearing fold: **top-10 traps mapped → every one has a named guard in §3** (1 ACK≠state → AMD-97 hierarchy + §51 suite [M9.4]; 2 idempotent-no-report → confirm-from-cache/readback [M9.4]; 3 reporting-config-doesn't-persist → re-apply+re-verify-on-rejoin + read-back [M9.3]; 4 EZSP burst overload → single-in-flight paced pipeline [M9.2]; 5 flow-control-mismatch-as-flaky-hardware → explicit config + RSTACK-code diagnostics [M9.2]; 6 silent serial death → `isOpen()`-distrust + liveness watchdog + reopen-by-id [M9.2]; 7 sleepy interview fragility → resumable persisted FSM [M9.3]; 8 IAS hard-gating breaks pairing → tolerate-not-require [M9.3, measured-confirmed]; 9 TSN dedup scope errors → payload-equality + per-(device,ep,cluster) + clear-on-announce [M9.3]; 10 retry-storm amplification → bounded queues + backoff + at-most-once dispatch [M9.1/M9.2/M9.4]). Architecture-scale lessons folded: quirks-DB explosion → versioned declarative profile registry, UNPROFILED-graceful-degradation, fingerprint on (manufacturer,model) never product name [M9.3; the 2025 same-name-new-identity SNZB revision is the canonical case]; driver-core coupling → the adapter stays wholly behind integration-api (LTD-17 already enforces; the z2m ezsp→ember full rewrite is the counter-example); jSerialComm 2.10.x ReentrantLock migration validates LTD-11 compatibility and its maintainers confirm JNI pins regardless → platform-thread transport stands.

**Further-research routing (before authoring each unit):** M9.2 — embed UG100/AN706 ASH/EZSP constants verbatim (external-standard rule); pin the exact jSerialComm version + Apache-2.0 election; recheck z2m #30891 for any 7.4.5-line ASH reports; EZSP v13/v14 frame tables for the codec seam. M9.3 — source-verify the SNZB-03P converter in zigbee-herdsman-converters (occupancy-vs-IAS handling, marked UNVERIFIED in research; our own fixture is primary evidence and already decisive); verify Hue GP-endpoint interview-skip practice; pre-verify `DiscoveryPipeline` adoption-path implementation state. M9.4 — none external: **the corpus is the acceptance spec** (AMD-97 consumption contract).

## 6. Drift findings (hub-owned; this lane edited nothing outside its write-domain)

1. `lifecycle/MODULE_CONTEXT.md` ctor prose omits `configDir` (describes 4/5-arg; source is 5/6-arg) — M9.1's MODULE_CONTEXT update fixes it in-flow; noting for WUCP review.
2. `integration-api/MODULE_CONTEXT.md` still carries the "M9 must NOT publish command-bearing `CapabilityAdded` until the Expectation codec lands" gotcha — **stale**: AMD-87 landed at M5-A (`7f44bed`; backlog row: "M9 command-bearing-CapabilityAdded prerequisite CLEARED"). M9.3's instruction must state the cleared truth.
3. Backlog M9 row stale vs ruled scope (→ DP-A).
4. `integration-runtime/module-info.java` Javadoc mentions a `requires com.homesynapse.event` its directive block doesn't declare (pre-existing; Coder briefed not to "fix" directives to match stale prose).
5. `integration-runtime/MODULE_CONTEXT.md` internal 6-vs-7 file-count inconsistency (7 is correct).
6. Doc 02 §3.9 places standard capabilities "in the integration-api module"; code has the sealed hierarchy in device-model (pre-existing doc-vs-code residency drift; docs-repo disposition is Nick's/hub's — flag only).
7. Snapshot `Latest commits` field lags the pushed fleet (§2 C3) — restamp at next hub WUCP pass.

## 7. Lane self-review (vs `references/review-and-quality.md`, instruction-authoring posture)

**Constraint compliance:** every cited LTD/INV operationalized, not just cited (INV-ES-09 → T21 mechanism+test; INV-RF-01 → T6/T7/T20; LTD-11 → arch-rule + W-items; LTD-04 deviation surfaced as DP-B, not buried) — PASS. Independent register scan for uncited invariants: INV-PR-03/INV-ES-06/INV-RF-03/AMD-90-INV-01 pulled in; none found missing at M9.1's blast radius — PASS. **Source verification:** dependency scopes verified against actual `build.gradle.kts` (not handoffs); every implementation-level claim verified against source files; the adversarial pass returned zero fabrications across ~90 assertions; the one unbuildable spec it caught (`ConfigurationAccess` had no implementation anywhere) was folded as a new Files-table row + DP-12 correction + WU rows A31/B7 — PASS. **Precision:** contracts test-specific (T1–T21 each carries its assertion core); error taxonomy tabled; binary success criterion — PASS. **Consistency:** naming from source verbatim (no invented types beyond the declared CREATE set; subscriber id follows the `automation_engine` convention — flagged as settled DP-1 for audit attention); event types match the frozen constants; JPMS diffs carry the `requires transitive`↔`api` lockstep and the §authoring check caught the lifecycle `integration` promotion the draft itself initially needed — PASS. **Format compliance:** STOP-on-Mismatch gates (8) incl. the public-modifier/shape classes; settled DPs; forward pointers; P2 survey incl. publish-count-pin sweep + module-graph allow-list verification (confirmed allowed); §4c Clock reminder verbatim; record-collision STOP-check; Files-table-governs; minimum read set; build discipline + shift-left self-check; MODULE_CONTEXT update spec; Coder-pushback section with named freedom areas — PASS. **Known residuals, honest:** (a) the integration-tests harness's `HomeSynapseCore` ctor usage was not read in-lane — G8 makes it a mechanical execution-time gate with a STOP; (b) `RunPipelineConfirmWiringTest`'s YAML-based automation seeding was verified by the auditor but T18's seeding mechanics inherit that pattern by reference rather than embed — acceptable per minimum-read-set discipline, flagged for the hub's audit judgment; (c) M9.2–M9.4 are charters, not instructions — deliberate (just-in-time briefing per the backlog's own rule; each gets its own WU-M9.x pre-verification at authoring). **Sizing (P1):** M9.1 ≈ M7.4a+M7.4c-class; no hidden epic — the epic was split.

**Hand-back:** hub audits → Nick rules DP-A (+ DP-B timing) → Nick issues M9.1 to the Coder. Nothing committed by this lane; Nick commits host-side on the hub's word.
