<!--
file: context/audits/2026-07-31_B3_return.md
purpose: B3 BENCH-AUTOSTART — the DESK-half lane return (the returns-to-audits law). Carries: THE P-BLOCK (§2 — the first deliverable, hand to Nick), the STOP-gate record, the red-first evidence, per-DP delivery, the census-exact file list, severity-honest deviations, and every operator block authored (Pi install half §9, rejoin-race rep §10).
audience: the PM hub (two-layer audit precedes ANY commit); Nick (P-block §2 now; install §9 after pins mint).
status: DESK HALF DELIVERED 2026-07-31. Bench working tree at `8bce651` + exactly 9 porcelain entries (4 M + 5 ??; 11 file-level). ⛔PIN ×4 UNMINTED — awaiting the P-block pastes. NO commit made (the lane commits nothing; the hub orders staging post-audit).
-->

# B3 — BENCH-AUTOSTART: desk-half return (2026-07-31)

## 1. State in one paragraph

The nightly machinery is BUILT and desk-proven: `suite auto` resolves the new
`auto-suite:` constants key in key order (park LAST) and structurally refuses
OPERATOR-tier names pre-flight; `tools/nightly.sh` runs quiesce →
verify-quiesced → suite → restore → verify-restored → digest with restore
under a trap on EVERY exit path (proven by a TERM-mid-suite desk run with a
mid-run swapped-out assert); the digest formatter passes 21/21 fixture checks
including the DP-4 example line byte-exact; the scheduler pair (systemd user
timer REC + cron fallback) is authored; `rejoin-race-operator.yaml` lands
(loadable-for-scenario, REFUSED-for-auto — both desk-proven); `api_token` and
`digest` are dispatched bench.sh verbs; `.gitattributes` ratchets LF with the
anti-vacuous CR-scan pair on record. The wrapper REFUSES to run while the
constants `quiesce:` ⛔PIN slots are PLACEHOLDER (desk-proven, exit 2, zero
side effects) — so nothing can half-run before the P-block pastes mint the
pins. **Nick's next act is §2 (the P-block, ~5 min read-only).** The Pi
install half (§9) runs after the hub mints the pins from the pastes.

## 2. THE P-BLOCK — Nick, on the Pi, read-only, ~5 min (⏺ paste everything back)

**⛔ SUPERSEDED / P-BLOCK CONSUMED (2026-08-01, v43 beat 6): the §2 pins are MINTED at bench `7c8efbb` and §9 is EXECUTED — never re-run either. The night-1 closing return: `context/audits/2026-08-01_B3_night1_return.md`.**

Four blocks, any order; the app should be running (it normally is) for P-2
and P-4. Nothing here changes state. Paste each block's output back
either way — a paste that looks like failure is still instrument data.

**P-1 (⛔PIN-1: which file carries bench-hero — branch A vs B)**

```bash
# Pi terminal (read-only)
# GOAL: find WHICH config file carries the bench-hero automation. PIN-1
#       discriminates quiesce branch A (its OWN file -> mv-swap) vs
#       B (a section of a shared file -> generated-variant swap).
# DONE-WHEN: both listings printed; the grep names at least one file.
ls -la ~/hs-bench/config/
grep -l "bench-hero" ~/hs-bench/config/*
# Park-dependency premise check (P2 survey, same paste): bench-hero must
# command ONLY the Hue — the expected output of the next line is EMPTY:
grep -n "01KXW1W1SBJZERC9MBAMV2DWKE" ~/hs-bench/config/*
# ⏺ RECORD: paste ALL output either way. An EMPTY last grep is the
#   healthy answer (zero S31 references in config).
```

If the `grep -l "bench-hero"` line prints nothing (the automation is named
differently in config), run this fallback and paste it:

```bash
# Pi terminal (read-only) — P-1 fallback, only if the grep above was empty
grep -ril "hero" ~/hs-bench/config/
```

**P-2 (⛔PIN-2: the automations-list instrument — route + name field)**

```bash
# Pi terminal (read-only; app running)
# GOAL: measure the automations-list read the nightly's quiesce
#       ABSENT/PRESENT asserts will bind (the exact route + the field
#       carrying the automation name, and bench-hero's exact served name).
# DONE-WHEN: one JSON body printed. Expect >= 1 row; expect a name field.
curl -s -m 15 -H "Authorization: Bearer $(cat ~/hs-bench/config/initial_api_token)" http://127.0.0.1:7070/api/v1/automations
echo
# ⏺ RECORD: paste the FULL body either way. (Desk provisional, for the
#   auditor only — the PASTE is the measurement of record: GET
#   /api/v1/automations at RestFilters.java:308, rows data[] with
#   automationId/name/enabled at ListAutomationsEndpoint.java:106-108,
#   both read at the deployed c09c61c source.) A 401/404 body: paste it
#   and stop — route/token adjudication is the hub's.
```

**P-3 (⛔PIN-3: scheduler branch — systemd user timers + lingering?)**

```bash
# Pi terminal (read-only)
# GOAL: discriminate the scheduler branch (systemd user timer = REC; else cron).
# DONE-WHEN: both commands answered — output OR a clean error, both count.
systemctl --user list-timers
loginctl show-user $USER --property=Linger
# ⏺ RECORD: paste both either way. "0 timers listed" is a VALID systemd
#   answer (=> REC branch). "Linger=no" is fine (the install block enables
#   it). "Failed to connect to bus" on the first command => the cron branch.
```

**P-4 (⛔PIN-4: the current boot-log slice for the I5 fixture re-mint)**

```bash
# Pi terminal (read-only)
# GOAL: capture the CURRENT 6-device-era boot slice (the I5 boot-demo
#       fixture re-mint material).
# DONE-WHEN: ~60 lines printed, reaching past the device_relinked runs.
LOG=$(~/nexsys-bench/tools/bench.sh log)
echo "slice of: $LOG"
head -60 "$LOG"
# ⏺ RECORD: paste ALL output either way. Expected tokens inside:
#   registry.projection_live: devices=6 entities=6 position=25065 ·
#   zigbee.network_resumed: channel=20 panId=0x774c · device_relinked x6 ·
#   zigbee.port_identity_captured ... pinnedOnly=false.
# If projection_live shows anything OTHER than devices=6 entities=6
#   position=25065: paste and STOP — the fleet moved; the re-mint waits on
#   hub adjudication.
```

## 3. STOP-on-Mismatch gates (verified pre-code at `8bce651`)

| Gate | Result |
|---|---|
| bench.sh verbs exactly start/stop/restart/status/health/log/entities/runs/events/state + `scenario\|suite\|bundle`; `api_token()` at :18, NOT dispatched | **PASS** (read in full pre-edit) |
| constants.yaml AUTO SUITE OF RECORD block present, RE-AMENDED 2026-07-31, `command-s31-settle` LAST | **PASS** (:148–:202 pre-edit) |
| scenarios/ exactly 11 `.yaml` incl. `usb-reenumeration-manual.yaml` | **PASS** (count 11; post-change census is 12 — rejoin-race-operator adds one) |
| `.gitattributes` ABSENT | **PASS** (R6 creates it) |

Duplicate-dispatch check: newest coder-handoff entry = the B3 DISPATCH-READY
(v42 beat 2); no prior B3 return exists in `context/audits/`; bench porcelain
clean at `8bce651` = the instruction baseline. Fresh dispatch. Mirror-sync:
the loaded skill masthead carries the 2026-07-30 currency-pass-2 line (the
newest skills state) — sync evidenced.

## 4. Red-first evidence (desk gates)

**REDs at baseline `8bce651` (all quoted from the session):**

```
RED-1  bench.sh suite auto            -> runner B2-2026-07-28-rebind @ 8bce651
                                         [!!] scenario not found: ...scenarios/auto.yaml   exit=2
RED-2  suite auto --list              -> runner.py: error: unrecognized arguments: --list  exit=2
RED-3  nightly_digest.py --selftest   -> can't open file ... No such file or directory     exit=2
RED-4  bench.sh digest                -> usage: ... (verb not dispatched)                  exit=2
RED-5  bench.sh api_token             -> usage: ... (function exists, verb NOT dispatched) exit=2
RED-6  tools/nightly.sh               -> No such file or directory                         exit=127
```

**GREENs against the FINAL tree (every gate re-run after the last edit of
its file):**

1. **Digest formatter selftest: 21/21** — incl. the DP-4 example line
   byte-exact (`2026-08-01 quiesced AUTO floor: 9/9 PASS · bench-hero
   RESTORED ✓ · ON-latency 0.11s`), the SKIP-honest first-night floor
   (`8/9 PASS · 1 SKIP(hue-online)`), the failure form with bundle path,
   UNQUIESCED lead, RESTORE-FAILED glyph, `n/a(<verdict>)` never-fabricate
   arms, failed-bundles selection, and the config-env placeholder-refusal +
   branch-A/branch-B slot demands.
2. **`bench.sh suite auto --list` (real tree):** 9 legs, ALL load lawfully,
   zero OPERATOR — plus a MECHANICAL order assert (python compare of the
   printed order vs the yaml key): `resolved order == auto-suite: key
   order; park LAST; 9 legs`.
3. **OPERATOR-injection refusal, both modes** (scratch constants):
   `--list` with `rejoin-race-operator` appended → `[REFUSED] … UNLAWFUL in
   the auto suite …` exit 2; RUN mode with an operator-only list →
   `ran 0/1 — 1 REFUSED`, exit 2, **zero legs executed** (the C-1 hazard
   structurally averted pre-flight).
4. **rejoin-race-operator loadable for `scenario`:** dry `--against` an
   empty fixture → lint-clean load, substitutions resolved (POST path
   carries the real S31 ULID; same_line carries the IEEE), operator plan
   prints without blocking, honest FAIL-not-REFUSED. Matches the
   instruction's stated desk gate (load-only; REFUSED for auto, loadable
   for scenario).
5. **`digest` + `api_token` verbs:** missing-log → honest `[!!] … missing
   digest line by morning is a RED` exit 1; seeded log → tails N (3/1);
   `api_token` dispatches (sandboxed dummy file only).
6. **The nightly.sh stub-run harness — ALL 25 CHECKS GREEN** (scratch-
   sandboxed; stub bench.sh; the REAL curl+grep instrument against a
   scratch `http.server` in hero-ABSENT and hero-PRESENT variants):
   - **R1** (branch A, absent-server, 9-PASS suite): evidence class
     `quiesced` (ABSENT read asserted); trap restored the carrier; the
     verify-restored read could NOT see the hero → digest byte-exact
     `… RESTORE-FAILED ⛔ · ON-latency 0.11s`, **exit 3** — the
     honest-detection arm, with the LATENCY EXTRACTED FROM A REAL
     api-captures.json (DISPATCHED 02.100Z → CONFIRMED 02.211Z = 0.11s).
   - **R2** (present-server, 1-FAIL suite): `UNQUIESCED(QUIESCE-ASSERT-
     FAILED)` lead; floor `8/9 · FAIL boot-health · bundle <path>`;
     `RESTORED ✓`; quiesce-evidence COPIED into the FAIL bundle and NOT
     into the PASS bundle; exit 1 (suite exit rides through).
   - **R3** (hanging suite, TERM mid-suite): **mid-run assert: carrier
     swapped OUT (in hold) — the restore proof is non-vacuous**; trap fired
     immediately (background-suite + `wait` design), carrier restored,
     digest line ALWAYS lands (`no verdicts … treat as red` floor,
     `RESTORED ✓`), exit 1.
   - **R4** (branch B, live ≠ basis): CONFIG-DRIFT → NO swap, carrier
     byte-untouched, `UNQUIESCED(CONFIG-DRIFT)` lead, `PRESENT ✓ (never
     swapped)`, exit 0.
   - **R5** (the REAL repo constants, pins unminted): refusal exit 2,
     zero side effects, no digest anywhere.
7. **R6 CR scan (green-by-construction at baseline, DISCLOSED as such —
   its value is the ratchet + the control):** `git grep -I --untracked
   $'\r'` over the FULL final tree (tracked + all 7 new untracked files)
   → ZERO hits (exit 1); positive control (planted CRLF file, same
   instrument) → found (`_cr-control.tmp:2`, exit 0); removed → re-scan
   zero. `--untracked` is load-bearing: a tracked-only scan would skip
   exactly the files under review. (Observed: this clone has
   `core.autocrlf=true` — the working tree is LF anyway, and the new
   `.gitattributes` `eol=lf` overrides autocrlf per-repo in both
   directions, which is the rider's whole point.)
8. `bash -n` (bench.sh, nightly.sh) + `py_compile` (runner.py,
   nightly_digest.py) clean on final bytes.

## 5. Per-DP delivery

- **DP-1** — `auto-suite:` minted as a REAL key adjacent to THE AUTO SUITE
  OF RECORD block (the block stays as rationale; the key comment names its
  consumers and the two reasons `suite all` is unlawful). `suite auto`
  resolves the key IN KEY ORDER (never sorts — resolved==key by
  construction); pre-flight loads every leg and REFUSES OPERATOR-tier names
  (REFUSED, not DEFERRED — a poisoned list is a configuration defect, exit
  2) before ANY leg runs; lawful legs still run (DP-12 complete-and-report).
  `--list` gives the load-only listing (the desk gate + the operator glance
  before enabling the timer).
- **DP-2** — `tools/nightly.sh`: config-gate → quiesce → verify-quiesced →
  `"$BENCH_SH" suite auto` under `timeout --kill-after=60 45m` → restore →
  verify-restored → digest, with restore + digest inside the EXIT/INT/TERM
  trap. All bench operations route through bench.sh (`restart`,
  `api_token`, `suite auto`) — no second path to the app. The suite runs
  backgrounded under `wait` (bash defers traps while a foreground command
  runs — the signal-safety design note in the header).
- **DP-3** — both branches implemented behind the ⛔PIN-1 `quiesce.branch`
  slot. A: mv carrier→hold / mv back. B: cmp live vs `live-basis` FIRST —
  **different → CONFIG-DRIFT, NO swap, honest un-quiesced run** (desk-
  proven byte-untouched); equal → night-held byte-copy then the generated
  variant swapped in via cp-to-temp + atomic mv; the wrapper never performs
  YAML surgery (the operator regenerates the variant — §9 INSTALL-2b).
  Positive-evidence asserts: the authed automations read after each
  restart; ABSENT then PRESENT recorded with HTTP codes + full bodies in
  `<date>-quiesce-evidence.txt`; the file is copied into every failure
  bundle (desk-proven, FAIL-bundle-only). Both-reads-empty can never read
  as success: a failed read is `QUIESCE-UNVERIFIED(read-failed)`, never
  `quiesced` (amendment (ii) honored in the grammar — deviation I-1).
- **DP-4** — ONE line appended per night; grammar + all forms selftest-
  pinned (§4.1); `RESTORE-FAILED ⛔` is written whenever the PRESENT read
  (or the restore mv) fails, with wrapper exit 3; a config-refused night
  deliberately writes NO line (the missing line IS the red — README states
  it). `bench.sh digest [N]` tails the last N (default 3) and exits 1 with
  a named message when no digest log exists.
- **DP-5** — `tools/scheduler/`: user unit + timer (`OnCalendar=*-*-*
  03:30:00 America/Chicago`, `Persistent=true`, oneshot, bash-prefixed
  ExecStart, TimeoutStartSec 3900 / TimeoutStopSec 300 so the trap has
  room) + the cron fallback line (system-timezone caveat named). ⛔PIN-3
  picks the installed branch; wrapper logs to
  `~/hs-bench/nightly-logs/nightly-<date>.log` on both (tee → file AND
  scheduler journal).
- **DP-6** — `nightly_digest.py latency`: PASS on `command-confirm-s31` ⇒
  DISPATCHED→CONFIRMED from the bundle's api-captures
  (`data.lifecycle.<PHASE>.at` = ingestTime, source-verified at
  GetCommandStatusEndpoint.java:242, the deployed c09c61c surface);
  SKIP/FAIL/absent ⇒ `n/a(<verdict>)` — never fabricated (selftest-pinned
  + harness-proven end-to-end at 0.11s from a real captures file). Value
  rides the digest line AND `~/hs-bench/digests/on-latency.log`
  (`<date> <value>`, one row per night).
- **DP-7** — `scenarios/rejoin-race-operator.yaml` (OPERATOR, requires
  [command-api]): operator power-cycle with ENTER-at-power-on, then the
  POST fires immediately (the command lands mid-rejoin); positives =
  device-scoped relink (`same_line` on the S31 IEEE — the mid-rejoin
  premise EVIDENCED, killing the no-op-ENTER vacuous pass) + the
  disposition-agnostic terminal (`field_equals data.terminal true` — the
  command-s31-settle precedent; the disposition is deliberately open,
  never-false-CONFIRMED adjudicated from the pasted terminal+state reads);
  forbidden = device_proposed / network_parameter_mismatch (a re-propose
  fails the rep). The attended-session rep block is §10; the landing is
  B3's deliverable, the rep is the operator's (before Aug-10).
- **R3 (I5)** — deferred on ⛔PIN-4 by design: the re-mint procedure is
  prepped (P-4 captures the slice; the re-mint replaces
  `fixtures/runner-demo/synthetic-boot-pass.txt` content with the labeled
  slice, keeping the SYNTHETIC-marker header discipline). See OBS-3 for
  the red-arm siblings.
- **R4 (I6)** — README "The nightly (B3)" section: the wrapper chain, suite
  auto lawfulness, the digest grammar with missing-line=red and
  RESTORE-FAILED=red, ON-latency, the SAME-DAY RE-RUN TRAP (the
  instruction's sentence + pointer to the constants block — never a copy of
  the list), the expected first-night shape + margin-watch escalation, and
  the desk gates.
- **R5 (F-1)** — `api_token` dispatched; usage lines (header + fallthrough)
  updated in the same edit; every existing verb case line byte-untouched
  (additive-only law held).
- **R6** — `.gitattributes` exactly as ruled (`* text=auto eol=lf` +
  explicit sh/py/yaml/md) + the scan pair (§4.7).

## 6. What the desk could NOT execute (stated per the instruction)

The live quiesce/restore asserts against the REAL app + REAL bench-hero
config cannot run desk-side. The instruction's suggested REV2 sibling
`.api.yaml` route does not apply — the quiesce assert lives in the WRAPPER
(curl+grep), not in the engine's api-assert machinery. Instead the desk
proved the wrapper's assert logic against a scratch `http.server` serving
hero-present/hero-absent bodies through the REAL instrument path (curl →
grep -F), plus the full trap/restore/digest chain (§4.6). What remains
Pi-only: the real automations-list body shape (⛔PIN-2 measures it), the
real config-swap against the real carrier (INSTALL-3's attended first run),
and the real `bench.sh restart` interplay. The first attended run in §9
(INSTALL-5/6) is the closing gate, per the instruction's stated fallback.

## 7. File census

**nexsys-bench** (all changes; porcelain shows 4 M + 5 ?? = exactly these
11 files; `tools/scheduler/` is one ?? dir entry carrying three files):

| # | File | State | What |
|---|---|---|---|
| 1 | scenarios/constants.yaml | M | `auto-suite:` key (DP-1) + `quiesce:`/`nightly:` ⛔PIN-slot blocks (DP-2/3/5) + `command.s31-ieee` mint (DP-7 consumer) |
| 2 | tools/bench.sh | M | `api_token` + `digest [N]` verbs; usage lines (header + fallthrough); existing verb lines byte-untouched |
| 3 | tools/runner/runner.py | M | `resolve_auto_suite` + `auto_preflight_refusals` + auto mode in `cmd_suite` + `cmd_suite_list` + `--list` flag |
| 4 | tools/runner/README.md | M | Usage lines + "The nightly (B3)" section (R4) |
| 5 | tools/runner/nightly_digest.py | ?? | DP-4/DP-6 support tool + `--selftest` (standalone by design — the digest must survive a broken runner) |
| 6 | tools/nightly.sh | ?? | The DP-2/DP-3 wrapper (trap-restore; signal-safe background suite) |
| 7 | tools/scheduler/nexsys-bench-nightly.service | ?? | DP-5 REC branch |
| 8 | tools/scheduler/nexsys-bench-nightly.timer | ?? | DP-5 REC branch (03:30 America/Chicago, Persistent) |
| 9 | tools/scheduler/crontab-fallback.txt | ?? | DP-5 fallback branch |
| 10 | scenarios/rejoin-race-operator.yaml | ?? | DP-7 (OPERATOR; 12th scenario file post-change) |
| 11 | .gitattributes | ?? | R6 |

**nexsys-hivemind:** this return + the coder-handoff entry (+ one
coder-lessons append). **Zero core changes** (constraint held; the two
core-source reads were verification-only).

Sweep-guard: staging is safe IFF a fresh lock-free porcelain
(`git --no-optional-locks status --porcelain`) in nexsys-bench shows
exactly the 11 paths above (5 ?? expands `tools/scheduler/` to its three
files at add time). No bench-logs/, no bundles, no token-bearing file, no
`__pycache__` (gitignored), no engine.py churn, no fixture churn (the I5
re-mint waits on ⛔PIN-4).

## 8. Deviations (severity-honest — ZERO [REVIEW]/[BLOCKING]; twelve [INFO])

- **[INFO] I-1 — digest grammar extended with honest arms beyond the two
  ruled classes.** Ruled: `quiesced` / `UNQUIESCED(CONFIG-DRIFT)` /
  `RESTORED ✓` / `RESTORE-FAILED ⛔`. Added: `UNQUIESCED(QUIESCE-ASSERT-
  FAILED)` (swap happened, hero still served — claiming `quiesced` would be
  the vacuous-green amendment (ii) bars), `QUIESCE-UNVERIFIED(read-failed |
  carrier-absent | swap-failed | early-exit)` (the instrument could not
  produce positive evidence — neither `quiesced` nor `UNQUIESCED` is
  truthful), and for never-swapped nights the restore words `PRESENT ✓
  (never swapped)` / `PRESENT-UNVERIFIED (never swapped)` (the ruled
  RESTORED/RESTORE-FAILED pair describes a swap lifecycle that did not
  happen; the line still ALWAYS states bench-hero's status, per the law's
  intent). All selftest-pinned. One-word hub re-rule renames any of them.
- **[INFO] I-2 — `--list` runner flag added** (load-only listing): the
  desk-gate instrument the instruction's own language implies ("suite
  listing shows it REFUSED for auto"), additive, run-nothing.
- **[INFO] I-3 — auto-mode refusal is per-leg pre-flight, suite completes.**
  The whole list is tier-checked BEFORE any leg runs; poisoned legs get
  REFUSED verdicts (exit 2 at close); lawful legs still run (DP-12).
  Wholly-poisoned list ⇒ zero legs execute (harness: `ran 0/1`).
- **[INFO] I-4 — nightly.sh takes `--bench-sh`/`--constants` overrides**
  (the runner's own precedent; defaults resolve beside the script) — this
  is what made the stub harness possible; the scheduler invokes it bare.
- **[INFO] I-5 — CRLF hardening pair found by the harness:** Windows python
  writes `\r\n` to pipes; the tool now forces LF+UTF-8 output and the
  wrapper's read loop strips a trailing `\r` (the R6 class, applied to the
  wrapper's own plumbing). Pi behavior unchanged.
- **[INFO] I-6 — config-env expands ONLY leading-`~` path values.**
  `Path()` round-trips rewrite separators on foreign platforms (caught by
  the harness); absolute values pass byte-untouched; Pi `~` semantics
  unchanged.
- **[INFO] I-7 — no concurrency lock in nightly.sh.** A stale lock after a
  SIGKILL would block every future nightly (worse than the double-run it
  guards); the same-day re-run trap is documented, and the timer fires once
  nightly. Hub may rule a lock later.
- **[INFO] I-8 — latency mined only from a PASS latency leg** (DP-6's
  letter: SKIP/FAIL ⇒ `n/a(<verdict>)`); a FAIL leg's bundle still holds
  the lifecycle for hand adjudication.
- **[INFO] I-9 — the harness's five stub runs are DESK fixtures** (stub
  bench.sh + scratch http server), labeled as such throughout; they prove
  wrapper logic, never bench evidence.
- **[INFO] I-10 — executable bits:** the scheduler unit + cron line invoke
  via `/usr/bin/bash` (mode-bit-proof, the bench.sh chmod precedent);
  direct hand-runs want `chmod +x ~/nexsys-bench/tools/nightly.sh`
  (INSTALL-1). Commit-time: the hub may set the +x bit on
  tools/nightly.sh as with bench.sh.
- **[INFO] I-11 — `__pycache__` regenerated** during desk gates
  (gitignored; never in the census).
- **[INFO] I-12 — the R6 baseline scan is green-by-construction and
  disclosed as such** (the tree was LF-clean at `8bce651`); its value is
  the ratchet + the positive-control pair (§4.7).

**Observations for the hub (not deviations):**
- **OBS-1** — `docs/bench-log-retention-policy.md:33` anticipates "bundles
  copy off nightly with the digest; Pi keeps a 7-day window" when B3 lands.
  The B3 instruction's DP-2 wrapper shape does not order copy-off/prune and
  a copy-off needs a ruled destination — NOT built; flagging so the policy
  note and the wrapper converge on a hub ruling.
- **OBS-2** — ENGINE_VERSION unmoved (`B2-2026-07-28-rebind`): engine.py is
  byte-untouched this WU; the bump law is engine-touching-WU-scoped. The
  banner's SHA half self-identifies the B3 commit once committed.
- **OBS-3** — the boot-demo RED-arm siblings (`synthetic-boot-missing-
  relink.txt`, `synthetic-boot-proposed.txt`) are devices=2-era like the
  PASS fixture. Once the PASS fixture re-mints to 6/6@25065, the red arms
  would red for ERA reasons, not their NAMED mechanisms (fixture-paired
  assert doctrine). REC: derive all three from the P-4 slice (pass = the
  labeled slice; red arms = the slice minus one relink / plus a
  device_proposed line). One-word ratification requested with the PIN-4
  mint.
- **OBS-4** — P2 survey at execution: zero suite-size count-pins in
  `tools/` (re-grep quoted in-session); bench.sh existing verb lines
  byte-identical; `usb-reenumeration-manual` proven a NEGATIVE consumer of
  `suite auto` (refusal gates); the park-dependency premise check rides
  P-1's grep.

## 9. Pi INSTALL half (after the hub mints the pins from §2's pastes)

Sequencing: hub audit → commit → this block. INSTALL-2's exact values come
from the hub's pin mint; everything else is fill-in-free.

**INSTALL-1 (deploy + sanity)**

```bash
# Pi terminal
# GOAL: pull B3, make nightly.sh hand-runnable, prove the tool runs on Pi python.
# DONE-WHEN: pull fast-forwards; selftest prints "21 check(s), 0 failure(s)".
cd ~/nexsys-bench && git pull
chmod +x ~/nexsys-bench/tools/nightly.sh
python3 -B ~/nexsys-bench/tools/runner/nightly_digest.py --selftest
# ⏺ RECORD: paste the pull summary + the selftest tail either way.
```

**INSTALL-2 (the pin mint — hub supplies the exact constants edit)**

The hub edits `scenarios/constants.yaml` `quiesce:` slots from the P-block
pastes (branch, carrier, automations-route, hero-name-token; branch B adds
heroless-variant + live-basis), commits per its staging order. Then:

```bash
# Pi terminal — the config gate must flip from REFUSE to GREEN
# DONE-WHEN: the command prints NB_* lines and exits 0 (refusal = unminted
#            slots remain; paste the [!!] lines).
python3 -B ~/nexsys-bench/tools/runner/nightly_digest.py config-env --constants ~/nexsys-bench/scenarios/constants.yaml
echo "exit=$?"
# ⏺ RECORD: paste either way. The NB_HERO_NAME value must match the served
#   name from P-2's body EXACTLY.
```

**INSTALL-2b (branch B ONLY — generate the hero-less variant, operator act)**

```bash
# Pi terminal — ONLY if PIN-1 ruled branch B. <carrier> = the pinned file.
# GOAL: mint the hero-less variant + the live-basis byte-copy the drift
#       guard compares against. YOU do the YAML surgery ONCE — the wrapper
#       never edits YAML. Redo this block whenever you edit the live config.
cp <carrier> ~/hs-bench/quiesce-hold/live-basis-source-check.yaml
cp <carrier> <live-basis-path-from-constants>
cp <carrier> <heroless-variant-path-from-constants>
nano <heroless-variant-path-from-constants>
# in nano: DELETE the bench-hero automation section ONLY; save; exit.
# ⏺ RECORD: paste `diff <carrier> <heroless-variant-path>` — expect ONLY
#   the bench-hero block as removed lines, nothing else.
```

**INSTALL-3 (instrument dry-glance before any timer)**

```bash
# Pi terminal (app running)
# GOAL: the suite listing + one PRESENT read — the instruments the nightly
#       will trust, glanced by hand first.
# DONE-WHEN: 9 [LOAD] lines (park LAST) + a body containing the hero name.
~/nexsys-bench/tools/bench.sh suite auto --list
curl -s -m 15 -H "Authorization: Bearer $(~/nexsys-bench/tools/bench.sh api_token)" http://127.0.0.1:7070$(python3 -B ~/nexsys-bench/tools/runner/nightly_digest.py config-env --constants ~/nexsys-bench/scenarios/constants.yaml | grep NB_QUIESCE_ROUTE | cut -d= -f2- | tr -d "'")
echo
# ⏺ RECORD: paste both either way.
```

**INSTALL-4 (scheduler — ONE branch per ⛔PIN-3)**

REC branch (systemd user timer):

```bash
# Pi terminal
# DONE-WHEN: list-timers shows nexsys-bench-nightly with a NEXT fire time.
sudo loginctl enable-linger $USER
mkdir -p ~/.config/systemd/user ~/hs-bench/nightly-logs
cp ~/nexsys-bench/tools/scheduler/nexsys-bench-nightly.service ~/.config/systemd/user/
cp ~/nexsys-bench/tools/scheduler/nexsys-bench-nightly.timer ~/.config/systemd/user/
systemctl --user daemon-reload
systemctl --user enable --now nexsys-bench-nightly.timer
systemctl --user list-timers | head -5
# ⏺ RECORD: paste the list-timers lines either way.
```

Fallback branch (cron): `mkdir -p ~/hs-bench/nightly-logs`, then
`crontab -e` and paste the one uncommented line from
`~/nexsys-bench/tools/scheduler/crontab-fallback.txt` (verify `timedatectl`
shows America/Chicago first; ⏺ paste `crontab -l`).

**INSTALL-5 (NIGHT-1 BOOTSTRAP — success criterion 3, verbatim)**

```bash
# Pi terminal — park once by hand so the first nightly starts parked.
# DONE-WHEN: the scenario prints a verdict (EITHER disposition is lawful —
#            CONFIRMED or CONFIRMATION_TIMED_OUT both leave the relay OFF).
~/nexsys-bench/tools/bench.sh scenario command-s31-settle
# ⏺ RECORD: paste the verdict line + the bundle path either way.
# Then HANDS OFF the bench until morning — a same-evening suite re-run is
# the documented expected-red (constants block, the re-run trap).
```

**INSTALL-6 (morning 1 — the success-criterion read)**

```bash
# Pi terminal, next morning
# GOAL: the first digest line + the evidence pair (success criterion 4).
# DONE-WHEN: one line for last night reading quiesced + 9/9 PASS (or the
#            SKIP-honest 8/9 + 1 SKIP(hue-online)) + RESTORED ✓; the
#            evidence file shows the ABSENT read AND the PRESENT read.
~/nexsys-bench/tools/bench.sh digest
cat ~/hs-bench/nightly-logs/$(date +%F -d yesterday)-quiesce-evidence.txt 2>/dev/null || cat ~/hs-bench/nightly-logs/$(date +%F)-quiesce-evidence.txt
# ⏺ RECORD: paste BOTH either way. A missing digest line = red: paste
#   ~/hs-bench/nightly-logs/nightly-<date>.log and the journal
#   (journalctl --user -u nexsys-bench-nightly.service --since yesterday)
#   instead. A command-confirm-s31 red WITH the park verified = the margin
#   watch expressing itself — flag to the hub (HUE-RESET contingency),
#   never retry-loop.
```

## 10. The rejoin-race rep (attended session before Aug-10 — DP-7's operator half)

Prerequisites: B3 committed + pulled; a boot-health pass that day; quiet
bench (no motion-sensor traffic — the S-1 rep-2 lesson); an interactive
TTY (NOT `ssh pi '<cmd>'` — C-1).

```bash
# Pi terminal (interactive tty; hands at the S31's wall supply)
# GOAL: one C2 rejoin-race rep — a command issued while the S31 is
#       mid-rejoin must terminal HONESTLY (disposition open; never-false-
#       CONFIRMED is the contract).
# DONE-WHEN: the runner prints its verdict + bundle line.
# THE ONE ACT (the runner will prompt): flip OFF the S31's wall supply,
#   wait ~5 s, flip ON, then IMMEDIATELY press ENTER, then HANDS OFF —
#   the POST fires at ENTER and must land while the rejoin is in flight.
~/nexsys-bench/tools/bench.sh scenario rejoin-race-operator
# ⏺ RECORD: paste the FULL runner output either way (verdict, evidence
#   lines, bundle path). Note the wall-clock time of the power-ON to the
#   nearest second.
```

Post-rep, same session (the adjudication reads — timestamps and counts):

```bash
# Pi terminal — the two reads the hub adjudicates the disposition from.
# <command-id> = the "captured race_command_id" value in the runner output.
curl -s -m 15 -H "Authorization: Bearer $(~/nexsys-bench/tools/bench.sh api_token)" http://127.0.0.1:7070/api/v1/commands/<command-id>
echo
curl -s -m 15 -H "Authorization: Bearer $(~/nexsys-bench/tools/bench.sh api_token)" http://127.0.0.1:7070/api/v1/entities/01KXW1W1SBJZERC9MBAMV2DWKE/state
echo
# ⏺ RECORD: paste both bodies either way. Three questions, all counts/
#   timestamps: (1) the power-ON wall-clock time? (2) did the relay
#   physically click after power-ON? (3) does data.attributes.on.value
#   match the terminal the first body claims? A CONFIRMED whose state read
#   contradicts the relay is the never-false-CONFIRMED violation this rep
#   exists to catch — STOP and flag immediately.
```

What-ifs (max three): `[FAIL] … device_relinked … expected-not-seen` ⇒ the
power-cycle didn't register (breaker vs plug?) — re-run once; `[FAIL] …
device_proposed` ⇒ identity regression — STOP, bundle to the hub, do NOT
re-run; POST rejected non-2xx ⇒ paste the body — that itself is a C2
finding (the command plane refused mid-rejoin).

## 11. WUCP Phase 1 checklist

- [x] MODULE_CONTEXT updates — **N/A** (nexsys-bench carries none; the
      bench is not a JPMS module. Core MODULE_CONTEXTs untouched — zero
      core changes.)
- [x] `context/handoff/coder-handoff.md` — B3 DESK-HALF DELIVERED entry
      prepended (newest, authoritative), with the Deferred Build Gate
      section (**N/A-gradle**; desk gates of record ran in-session, all
      green; the Pi halves pending).
- [x] `context/lessons/coder-lessons.md` — two lessons appended
      (cross-platform bash↔python path/CRLF class; bash trap deferral →
      background+`wait` signal-safety).
- [x] Cross-agent note — not needed (the hub audits this return directly).
- [x] This checklist appended to the return (the Completion Report of
      record for the desk half).
- **Refuse-to-close:** next WU named in the handoff entry — B3's own
  P-block (Nick, §2) → pin mint + Pi install (§9) → first digest; nothing
  else dispatches while B3 is in flight (v42 charge order).
