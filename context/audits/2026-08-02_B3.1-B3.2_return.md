<!--
file: context/audits/2026-08-02_B3.1-B3.2_return.md
purpose: Coder return for B3.1 (nightly hardening — the night-1 §9 set + the
         night-2 additions) + B3.2 (rejoin-race evidence-anchor re-pin).
         BENCH repo, desk half only. Carries: per-item delivery, the four
         desk gates GREEN with red-first evidence quoted, the A-1 sweep
         enumeration, the A-9 pin-source citations, the 8-file census, and
         the deviation/OBS ledger.
audience: the PM hub (two-layer audit), then Nick.
status: DELIVERED 2026-08-02. Tree UNCOMMITTED on bench `a791c99` (porcelain
        = exactly the §7 census). ⚠ DEPLOY EMBARGO HOLDS: nothing here
        reaches the Pi before the Aug-3 morning-gate intake (the armed s31
        park experiment adjudicates first); the Pi pull + unit re-copy +
        daemon-reload ride a hub-authored post-gate operator block.
-->

# B3.1 + B3.2 — Coder return (2026-08-02, desk half)

## 1. Verdict in one paragraph

Every A-item (A-1…A-9) and every DP-B (B1…B3) is IMPLEMENTED; zero pushback
flags fired — every instruction pin matched source. All four desk gates ran
GREEN against the FINAL tree: (1) the rebuilt-and-extended nightly.sh stub
harness **50/50** (7 runs; baseline at `a791c99` under the SAME corrected
instrument: **10 RED / 40 GREEN** — the reds exactly the A-3/A-4/A-5
mechanisms); (2) the A-6/A-9 runner fixture harness **27/27** (baseline:
**19 RED / 8 GREEN**, the night-2 `app-log-slice ABSENT` class reproduced
RED first); (3) `nightly_digest.py --selftest` **21/21** byte-exact,
untouched by A-7 (no lockstep delta needed); (4) `bash -n` both scripts +
`py_compile` ×5 + `suite auto --list` (9 legs lawful, park LAST) +
`suite rejoin-race-operator --list` (loads lawfully, OPERATOR) + the
poisoned-injection preservation probe (OPERATOR name injected into a
SCRATCH constants copy → `[REFUSED] … UNLAWFUL in the auto suite`, exit 2).
The engine banner now reads `runner B3.1-2026-08-02-postwindow @ a791c99`
(the ENGINE_VERSION law honored — engine.py was touched for A-9). The CR
scan (`git grep -I -l --untracked $'\r'`) is CLEAN with the planted control
FOUND then re-cleaned (anti-vacuous pair). Census: **exactly 8 porcelain
entries, all M, zero ??** (§7). The lane commits NOTHING.

## 2. Launch state + STOP gates (all PASS, pre-code)

- Duplicate-dispatch check: newest coder-handoff entry was the B3.1+B3.2
  DISPATCH (not DELIVERED) — fresh work. Baseline re-verified at launch:
  bench HEAD `a791c99`, porcelain CLEAN.
- STOP-gate 1: `tools/nightly.sh` 276 lines; `exec > >(tee -a …)` at :72;
  `finish()` trap on EXIT/INT/TERM; exit contract 0/1/2/3/124/137 in the
  header. EXACT.
- STOP-gate 2: digest docstring examples carry `ON-latency 0.11s` (:18–19);
  selftest checks the DP-4 example byte-exact (:431–438). EXACT.
- STOP-gate 3: service has Type=oneshot / TimeoutStartSec=3900 /
  TimeoutStopSec=300 and NO KillMode line. EXACT.
- STOP-gate 4: rejoin-race first positive = `log: "zigbee.device_relinked"`
  + `same_line: ["${C.command.s31-ieee}"]` + `within: 120s` (:74–76). EXACT.

## 3. Part A delivery, item by item

- **A-1 (INSTALL-6 repair + the dialect sweep):** the repo-wide sweep for
  `journalctl --user -u ` found **ZERO occurrences at baseline** — the
  defective dialect never entered the bench repo; it lived only in the
  superseded hivemind B3 return (hub-owned, §9-item-9 stamped). The
  repaired red procedure is therefore RE-HOMED into the living doc: the
  runner README nightly section now carries "The red procedure" with
  `journalctl --user-unit …` (never `--user -u`, the F-3b mechanism named),
  `~/hs-bench/nightly-logs/nightly-<date>.log` named FIRST, and the ordered
  sentence verbatim: *"the journal may have rotated; the log file has
  not."* Full sweep enumeration in §5.
- **A-2 (the week-1 morning gate):** README nightly section, §8-compliant —
  GOAL/DONE-WHEN first, WHERE-label inside the block as a leading comment,
  FULL paths (`/home/homesynapse/nexsys-bench/tools/bench.sh` per the
  night-2 tool-path-of-record ruling), all four commands verified to exist
  (pgrep; the `digest` verb at bench.sh:83; `systemctl --user show -p` and
  `journalctl --user-unit` both night-1-proven on this box). **pgrep is the
  survival gate** with the F-1 rationale, plus both expected-benign notes
  (the KillMode `remains running` line = the fix WORKING; `Result=
  exit-code`/`Failed to start` on a FAIL night = the documented exit
  contract; the digest is the verdict surface).
- **A-3 (timestamp + line-buffer the wrapper stream):** the tee is replaced
  by a bash read-loop stamper inside the same `exec >(…)` seam: each line
  becomes `[YYYY-MM-DD HH:MM:SS] <line>` and is written TWICE — one
  `printf` (one write per line) to stdout so the journal is LIVE, one
  append per line to `nightly-<date>.log` so the durable record carries
  wrapper-side event times. bash + coreutils(`date`) ONLY (no ts/moreutils,
  no gawk, and deliberately no `stdbuf`/`tee` — tee's stdio block-buffering
  WAS the G-1 mechanism, and stdbuf is unproven in the desk-gate shell).
  Order preserved (single reader, one pipe); unterminated-tail guard;
  finish() drains the stamper (`exec 1>&- 2>&-` + `wait` on the procsub —
  bash ≥ 4.4, Pi has 5.2) so trap-path tails land in both sinks. The
  digest/evidence/latency files are appended DIRECTLY by their writers and
  never pass through the stamper — the digest grammar stays byte-exact
  (selftest-pinned). L3 holds: the stamper transforms lines, never adds
  content; the token still rides command substitution only.
- **A-4 (the closing line):** LAST act of finish(), after the digest:
  `[--] nightly <date> closing: exit path <normal|trap-INT|trap-TERM|
  restore-failed|pre-suite-crash>, exit code <N>`. INT/TERM traps record
  their path before the shared finish; classification precedence
  restore-failed > signal > pre-suite-crash > normal; the EXIT-CODE
  CONTRACT IS UNCHANGED (byte-for-byte the old decision tree — the closing
  line is observability, never control flow). Header + README both gained
  the exit-contract legibility text (the config-refusal exit-2 path exits
  pre-trap and lawfully has no closing line — its red is the missing
  digest line, stated in both).
- **A-5 (the drift positive):** branch-B quiesce() now emits
  `[OK] drift check: live == live-basis` exactly when the cmp guard passes;
  the CONFIG-DRIFT arm is untouched. Harness-proven on BOTH sides of the
  boundary (the [OK] on a clean night; CONFIG-DRIFT + NO [OK] on the
  planted-drift night).
- **A-6 (journal-slice DROP + app-log widening):** bundles.py §4 no longer
  invokes journalctl at all — the MANIFEST records the drop in ONE line:
  `journal-slice.txt DROPPED (B3.1 A-6): two nights of noise-only exhibits
  (night-1 D-6; night-2 tailscaled x2) — app-log-slice + api-captures are
  the targeted evidence` (the `subprocess` import removed with it). The
  app-log capture is now TIERED (`_app_log_slice`): tier 0 = the engine's
  window cache (unchanged, preservation-proven); tier 1 = a marker-offset
  RE-READ of the current boot log at bundle-write time — the night-2
  mechanism was the fail-fast verdict racing the app's own terminal log
  line by milliseconds, and the re-read lands the late lines; tier 2
  (marker window unavailable) = a time-window read over the current boot
  log from the run's start (the app log's measured `HH:MM:SS.mmm` local
  stamp, midnight-wrap tolerant, tail-capped at 400 with the cap DISCLOSED
  in the label). ABSENT survives only when every tier read zero lines, and
  the MANIFEST names the tiers — absence is no longer a designed outcome,
  only a measured one.
- **A-7 (SYNTHETIC-EXAMPLE label):** the digest docstring grammar block now
  states the 0.11s is SYNTHETIC-EXAMPLE desk-fixture data, names the real
  night-1 first value (3.65s, polling-granular class), points at the filed
  distribution (`on-latency.log`), and tags the example line itself.
  Docstring-only — the selftest is byte-untouched and stays **21/21** (no
  lockstep delta).
- **A-8 (KillMode folded in-repo):** `KillMode=process` now sits in
  [Service] with the mechanism comment (the night-1 cgroup-kill two-second
  proof, the Aug-2 banked gate line `Unit process <pid> (java) remains
  running after unit stopped` quoted, and the benign `Found left-over
  process` follow-on named). The header install notes gained the refresh
  law (re-copy requires `systemctl --user daemon-reload`) and the drop-in
  note (Nick's live `killmode.conf` is a harmless duplicate until removed
  at a future trip).
- **A-9 (the post-window state read; wire-pin per format #20 — citations
  §6):** `engine.capture_post_window_state(line)` fires on a command-class
  terminal-read FAIL — both failure shapes (the `phase_terminal` exact-fail
  at the terminal read, i.e. the CONFIRMATION_TIMED_OUT class, AND the
  deadline-timeout of a terminal-class line) and both modes (live
  `run_evidence` + the fixture-scripted desk path). Classifier: assert set
  carries `phase_terminal`, or `field_equals` bound to the constants
  command-lifecycle terminal-field (the disposition-agnostic rejoin shape —
  the DP-B2 consumer). The target entity is parsed from the scenario's own
  resolved command stimulus (`/api/v1/entities/<ulid>/commands`); the read
  is ONE GET of `/api/v1/entities/<ulid>/state`, first-FAIL-wins (one-read
  law harness-pinned), no retries, no new config; a read failure degrades
  with a note and NEVER disturbs the FAIL verdict; dry-run without a
  scripted fixture path is a safe no-op (never a live read, never a fixture
  refusal). The capture lands in the bundle as `post-window-state.json` +
  one MANIFEST line, body VERBATIM and untruncated (the live dialect —
  nested `data.attributes.<attr>.value` — is the hub's to adjudicate, never
  reshaped runner-side). engine.py touched ⇒ **ENGINE_VERSION bumped** to
  `B3.1-2026-08-02-postwindow` (rider-#4 law; banner quoted in §1).
- **(§9 item 9:** hub-side, untouched here, per the instruction.)

## 4. Part B delivery (B3.2)

- **DP-B1:** the `zigbee.device_relinked` positive (with its `same_line`
  IEEE scope and `within: 120s`) is DELETED. The header's mid-rejoin-premise
  and token-sources paragraphs are rewritten around the silent-resume
  finding WITH provenance: silent-resume ×4 incl. the 13-minute outage (the
  F2 close-out reps, pm-handoff v43 beat 7); relink fires ONLY at boot-time
  rehydration (the Aug-2 04:31:43 journal, ×6 in one boot burst, zero
  mid-session); the two 2026-08-01 [FAIL]s named as the structural
  consequence with the substance banked at C2. The identity-law `forbidden`
  pair (`device_proposed`, `network_parameter_mismatch`) STAYS, with its
  unprefixed-grep-form note intact.
- **DP-B2:** the header now states the DESIGN as primary contract (no
  "degrade path" language survives): the machine asserts are the
  disposition-agnostic terminal read (present, unchanged) PLUS the A-9
  post-window state read on the S31 entity on terminal-read FAIL; the
  power-cycle premise is adjudicated HUB-SIDE from the operator act
  timestamps + bundle markers. No short-cycle log token is pinned as the
  premise (none is proven to fire: the availability flip is minutes-scale —
  honest-UNAVAILABLE in-window, replug→AVAILABLE ≤~80 s filed; the
  `zigbee.availability_ping` surface returned EMPTY under the live glance,
  shelved — both cited to v43 beat 7). The operator note now orders
  NOTE-THE-CLOCK at OFF and ON (playbook §8: the timestamps ARE instrument
  data — under this design they are the premise evidence).
- **DP-B3:** the long-cycle variant (≥10 min off) is documented in the
  header as an operator option with its anchors NAMED (the filed-proven
  `zigbee.availability_changed` available=false / available=true pair, the
  ~80 s replug bound, the S31 IEEE for a device-scoped hand-grep of the
  bundle's app-log-slice) — and the note SAYS the v0 engine cannot express
  an alternative positive set in one scenario file (no conditional evidence
  groups; deliberately not grown — zero runner-engine change for this).
  The scenario still loads/lints lawfully and `suite auto` still refuses
  it structurally (both harness-proven, §8-gate 4).

## 5. The A-1 sweep enumeration (repo-wide, at baseline)

Pattern `journalctl --user -u ` (the defective dialect): **0 occurrences.**
All `journalctl` occurrences checked and dispositioned:

| Location | Form | Disposition |
|---|---|---|
| tools/runner/bundles.py :64–:83 | plain `journalctl --since` (the bundle slice) | REMOVED by A-6 (the drop) |
| tools/runner/README.md :75 | bundle listing row | REWRITTEN by A-6 (drop documented; `--user-unit` named as the hand path) |
| docs/bench-log-retention-policy.md :29 | `journalctl --since … --until …` (system journal incident export) | CORRECT DIALECT — untouched |
| docs/2026-07-10_bench-automation-charter.md :18 | prose mention ("journalctl slice" in the B1 bundle description) | historical charter text — untouched |
| scenarios/SCENARIO_FORMAT.md :43 | illustrative parenthetical on `bundle: always` | format doc, outside the named scope — flagged OBS-1 for the hub |

New occurrences written by this WU all carry the CORRECT `--user-unit`
form (README morning gate + red procedure; bundles.py drop comment).

## 6. The A-9 pin-source citations (format #20 — the FILED corpus, checked before any source read)

- **Route + payload dialect:** `context/audits/2026-07-27_WCAP_detail-read-
  wire-capture_return.md` — capture table row 5 (:27): `GET /api/v1/
  entities/01KY12MQW954E4XYNKH0Y5H8VX/state` → 200 fully populated; the
  VERBATIM live payload (:76): nested `{"attributes":{"battery_pct":
  {"value":100},…}`, epoch-second instants, msb/lsb entityId; the §3-F3
  three-dialect-facts adjudication (:96–:104).
- **The dialect-sweep record:** `context/audits/2026-07-28_B2_suite-port_
  return.md` :276 (F-6 STATE-DIALECT — `/state` serves `{"value":…}`
  nesting, full-corpus sweep executed; "pin at the ENDPOINT actually read").
- **The S31-specific state path:** `scenarios/constants.yaml` :109–:114
  (the Pi-stage re-mint stamp: `data.attributes.on.value`, "the LIVE
  /state dialect, measured 2026-07-29").
- **The route's standing operator form:** `tools/bench.sh` :77 (the `state`
  verb, same route).
The mechanism stores the payload VERBATIM — no field extraction — so the
pin's load-bearing half is the ROUTE; the dialect citations are what the
hub will read the discriminator through (`data.attributes.on.value`).

## 7. Census (the sweep-guard)

Exactly **8 porcelain entries, ALL M, zero ??** — safe IFF a fresh
`git --no-optional-locks status --porcelain` shows exactly these:

```
 M scenarios/constants.yaml
 M scenarios/rejoin-race-operator.yaml
 M tools/nightly.sh
 M tools/runner/README.md
 M tools/runner/bundles.py
 M tools/runner/engine.py
 M tools/runner/nightly_digest.py
 M tools/scheduler/nexsys-bench-nightly.service
```

`tools/bench.sh`, `tools/runner/{runner,drivers}.py`, the timer unit,
`scenarios/command-confirm-s31.yaml`, `command-s31-settle.yaml`, and every
fixture are BYTE-UNTOUCHED (the out-of-scope law held; exec bits
unchanged — bench.sh stays 100755). Desk harnesses live in the session
scratchpad only (the B3 I-9 precedent) and never appear in porcelain.

## 8. Desk gates — evidence of record (red-first per format #18)

**Gate 1 — the nightly.sh stub harness** (7 runs through the REAL curl+grep
instrument against a scratch carrier-driven http server; stub bench.sh):

- Baseline at `a791c99` (git-extracted wrapper + digest, run under the SAME
  corrected instrument as the final): **10 RED / 40 GREEN.** The reds,
  verbatim: `r1 A-5 [OK] drift check line` · `r1 A-3 every log-file line
  stamped` · `r1 A-3 every journal line stamped` · `r1 A-4 closing line
  last: normal/0` · `r2 A-4 closing line last: normal/1` · `r3 A-3 stamps
  survive the trap path` · `r3 A-4 closing line last: trap-TERM/1` ·
  `r4 A-4 closing line last: normal/0` · `r6 A-4 closing line last:
  restore-failed/3` · `r7 A-4 closing line last: trap-INT/1`. The 40
  greens are the preservation floor (quiesce/restore asserts, digest
  grammar incl. RESTORE-FAILED ⛔ and UNQUIESCED(CONFIG-DRIFT) leads,
  evidence-copy into the FAIL bundle, carrier-byte-untouched on drift,
  refusal exit-2 zero-side-effects, TERM/INT trap-restore with the
  NON-VACUOUS mid-suite swap proof, restore-failed exit 3).
- Final tree: **50/50 GREEN.** Exhibits (verbatim from the run artifacts):

```
[2026-08-02 18:51:46] [OK] drift check: live == live-basis
[2026-08-02 18:51:47] [--] digest: 2026-08-02 quiesced AUTO floor: 9/9 PASS · bench-hero RESTORED ✓ · ON-latency 0.11s
[2026-08-02 18:51:47] [--] nightly 2026-08-02 closing: exit path normal, exit code 0
[2026-08-02 18:51:55] [--] nightly 2026-08-02 closing: exit path trap-TERM, exit code 1
[2026-08-02 18:52:00] [--] nightly 2026-08-02 closing: exit path restore-failed, exit code 3
```

  (every line of both sinks stamp-prefixed; the digest file itself
  UNSTAMPED and byte-exact — `2026-08-02 quiesced AUTO floor: 9/9 PASS ·
  bench-hero RESTORED ✓ · ON-latency 0.11s`, latency mined from a real
  api-captures fixture; the RESTORE-FAILED night's digest reads
  `… bench-hero RESTORE-FAILED ⛔ …` with exit 3.)
- Green-by-construction, disclosed: `r4 CONFIG-DRIFT line` (the A-5
  false-verdict boundary — the line pre-exists at baseline; its pair-tooth
  is the new [OK] line NOT appearing on the drift night, which is vacuous
  at baseline and toothed at final) and the 38 preservation checks (their
  tooth is regression).

**Gate 2 — the runner fixture harness (A-6/A-9):**

- Baseline at `a791c99` (git-extracted runner modules, corrected
  instrument): **19 RED / 8 GREEN** — RED: A1–A3 (post-window-state file/
  body/manifest), A5 (DROP line), A6–A9 (the night-2 `app-log-slice
  ABSENT` class REPRODUCED RED first, the marker-offset re-read absent),
  B1/B2/B4 (tier-2), D2 (honest-ABSENT wording), E2–E5/F2/G1/I1 (the whole
  A-9 hook). GREEN disclosed: A4 (desk-side journalctl is absent, so the
  BASELINE degrade already wrote no file HERE — on the Pi baseline it
  writes the noise file; the desk red for the drop is A5's wording), B3
  (vacuous while no file exists), C1/C2 (the tier-0 preservation pair),
  D1 (same file-absence outcome, different manifest wording — D2 carried
  the red), E1/F1/H1 (existing FAIL behavior + the classifier boundary).
- Final tree: **27/27 GREEN.** The synthetic FAIL bundle's MANIFEST lines,
  verbatim:

```
app-log-slice.log — <workdir>/applog-night2.log, re-read at bundle time from the first marker's offset — the fail-fast window race (night-2 class) (1 lines)
journal-slice.txt DROPPED (B3.1 A-6): two nights of noise-only exhibits (night-1 D-6; night-2 tailscaled x2) — app-log-slice + api-captures are the targeted evidence
post-window-state.json — the A-9 one-shot GET of the command target's /state at terminal-read FAIL (the late-report-vs-no-edge discriminator)
```

  plus: tier-2 time-window slice lands with pre-window lines excluded and
  the tier named; the all-tiers-zero night writes NO slice and the ABSENT
  line names both tiers; E2–E5 pin the fail-fast capture + the one-read
  law (`exactly ONE state read`, second call a no-op); F2 the deadline
  path; G1 the field_equals-terminal rejoin shape; H1 a non-terminal api
  FAIL captures NOTHING; I1 dry-run no-op.

**Gate 3 — digest selftest:** `python3 -B tools/runner/nightly_digest.py
--selftest` → `selftest: 21 check(s), 0 failure(s)` at baseline AND final
(A-7 is docstring-only; green-by-construction, disclosed).

**Gate 4 — lint/load:** `bash -n tools/nightly.sh tools/bench.sh` clean;
`py_compile` ×5 clean; `suite auto --list` → 9 legs `[LOAD]`, "all load
lawfully", banner `runner B3.1-2026-08-02-postwindow @ a791c99`;
`suite rejoin-race-operator --list` → `[LOAD] rejoin-race-operator
tier=OPERATOR requires=command-api` (THIS is the named load/lint path for
the B3.2 yaml — green-by-construction structural, disclosed: it also
passed at baseline); the injection probe (rejoin-race-operator prepended
to a SCRATCH constants copy's auto-suite) → `[REFUSED] … OPERATOR-tier
scenario is UNLAWFUL in the auto suite …`, `listed 10 leg(s) — 1 REFUSED`,
exit 2 — the structural refusal is preserved and non-vacuously proven.

**CR/LF law:** `git grep -I -l --untracked $'\r'` → zero hits; planted
control FOUND (`cr-control-planted.txt`) then removed and re-scanned to
zero — the instrument is proven, the tree is LF-clean.

## 9. Deviations (severity-honest: ZERO [REVIEW]/[BLOCKING], ten [INFO])

- **[INFO] I-1** — the A-1 sweep found zero in-repo occurrences of the
  defective dialect (§5): the instruction's "fix every occurrence"
  resolves to the ZERO enumeration + the README re-homing of the repaired
  procedure. Not a pushback — the sweep result IS the deliverable.
- **[INFO] I-2** — A-3's mechanism is a bash read-loop stamper with direct
  per-line writes rather than `stdbuf -oL tee`: tee's stdio buffering was
  the G-1 mechanism itself, and stdbuf is not a proven-present tool in the
  desk-gate shell (Git Bash) — the loop needs only bash + `date`, and adds
  the finish()-side drain so trap-path tails land. Per-line `date` fork ≈
  100/night — negligible.
- **[INFO] I-3** — A-4's `pre-suite-crash` path is classified in code but
  not harness-simulated (no natural trigger short of faking a bash abort);
  the instruction's named three (normal / TERM / restore-failed) plus INT
  are all harness-proven. The classifier's precedence (restore-failed >
  signal > pre-suite-crash > normal) is stated in-code.
- **[INFO] I-4** — A-6's widening realized as THREE tiers with an honest
  zero-lines end state: after the tiers, ABSENT can only mean "the app
  wrote nothing in the window", said so with tiers named — writing an
  empty file would have been the dishonest alternative. Tier-2's 400-line
  tail cap is DISCLOSED in the manifest label (no-silent-caps).
- **[INFO] I-5** — A-9's classifier covers `field_equals` on the
  lifecycle terminal-field in addition to `phase_terminal`: DP-B2
  explicitly makes the rejoin scenario (whose terminal read is exactly
  that shape) an A-9 consumer. The captured body is stored UNTRUNCATED
  (unlike the 2000-char api_captures clamp) — the discriminator must not
  be cut mid-attribute; it is one entity's state, bounded.
- **[INFO] I-6** — ENGINE_VERSION bumped (`B3.1-2026-08-02-postwindow`)
  per the rider-#4 engine-touch law; banner quoted in §1/§8.
- **[INFO] I-7** — the README bundle listing was rewritten to match the
  A-6/A-9 bundle truth (it documented `journal-slice.txt` as a standing
  member). `scenarios/SCENARIO_FORMAT.md:43`'s illustrative "journalctl
  slice" parenthetical was NOT touched (format spec, outside the named
  file set) — OBS-1.
- **[INFO] I-8** — `scenarios/constants.yaml`: the `s31-ieee` COMMENT's
  consumer sentence was amended (DP-B1 deleted its named first consumer;
  the stale pointer would have failed the next currency pass). The pin
  VALUE and every other byte of constants.yaml are untouched — the ⛔PIN
  out-of-scope law held.
- **[INFO] I-9** — three desk-harness instrument corrections were made
  mid-session (an awk `-v` dynamic-regex mangling in the stamp check; INT
  delivery to a backgrounded shell is impossible under the async-ignore
  rule, so the harness foregrounds the wrapper and delivers INT from a
  killer subshell; Windows `write_text` newline translation shifted
  synthetic byte offsets — fixtures now `write_bytes`). The BASELINE reds
  quoted in §8 are from RE-RUNS against git-extracted `a791c99` modules
  under the CORRECTED instruments — never from the pre-correction runs.
- **[INFO] I-10** — the B3.2 rewrite keeps the `verdict:` block and the
  evidence comment now names the A-9 rider; the operator `note:` gained
  the §8 note-the-clock instruction (under DP-B2 the operator timestamps
  ARE the premise evidence).

## 10. Observations for the hub (recommend; the hub disposes)

- **OBS-1** — `scenarios/SCENARIO_FORMAT.md:43` still lists "journalctl
  slice" in the `bundle: always` illustrative comment; one-line hub-owned
  fold candidate at the next format-doc touch.
- **OBS-2** — a PASS-night bundle carries no `post-window-state.json` by
  design (failure-path only); recorded here so its absence on a PASS
  bundle is never read as a defect.
- **OBS-3** — `git grep --untracked` must precede the PATTERN or git
  parses it as a revision (`fatal: unable to resolve revision`) — the
  flag-order sibling of the SPELL-THE-FLAG lesson; minted to coder-lessons.
- **OBS-4** — the A-9 desk proof exercises the engine hook through
  monkeypatched `api_get` responders and the bundle writer through
  synthetic runs; the full LIVE seam (real FAIL → real bundle on the Pi)
  first exercises on the next FAIL night after the post-embargo deploy —
  the Aug-3+ morning gates should glance for `post-window-state.json` in
  the first FAIL bundle as the closing evidence.

## 11. Compliance notes

- Deploy embargo HELD: zero Pi contact this session; desk-only.
- The armed experiment untouched: `command-confirm-s31.yaml` and
  `command-s31-settle.yaml` byte-identical; no retuning anywhere.
- L3 held: no token appears in any harness, fixture, log, or this return
  (the stub token is a labeled synthetic).
- Exec bits: no mode changes (bench.sh remains 100755; no new executables).
- LF law: §8's CR-scan pair.

## 12. Next WU (refuse-to-close pointer)

The hub's two-layer audit precedes ANY commit (this return + the
coder-handoff DELIVERED entry are the audit inputs; staging = EXPLICIT
paths, exactly the §7 eight-file census). The Aug-3 morning gate
adjudicates the armed s31 experiment FIRST (instrument-first; predictions
pre-stated in the night-2 evidence read); the post-gate hub-authored
operator block then carries the Pi pull + unit re-copy + daemon-reload.
After the audit → the S-5 small-fix stack per the dispatch entry (core
lane: the sqlite-jdbc bump at `gradle/libs.versions.toml:17` FIRST + the
float-serialisation grep + full check, then STATE-DIALECT P2 · FE lane:
FE-LIVE-V112 (f)/(g)). Nothing else dispatches from this lane while the
audit is open.
