<!--
file: context/audits/2026-08-04_B3.3_return.md
purpose: B3.3 return — the s31 suite-position amendment (constants-only; command-confirm-s31 3rd -> 8th, park LAST unchanged). Census exactly 1 M. All four desk gates GREEN in-session against the final tree, quoted below. The lane committed NOTHING.
audience: Hub (v45), Nick
state-type: work-unit return (point-in-time)
status: FILED 2026-08-05 (desk session on the host CC lane; the instruction and its named return path carry the 2026-08-04 arc date per the crossed-date filename law)
provenance: instruction context/instructions/2026-08-04_B3.3_s31-suite-position_bench-instruction.md (Nick's 2026-08-04 fix ruling); baseline bench 41a7a3c verified CLEAN at launch; measured basis read at the three cited audits + the beat-6 blockV adjudication (post-instruction, same thread).
-->

# B3.3 Return — the s31 suite-position amendment (bench, constants-only)

## 1. Summary

`command-confirm-s31` moves from position 3 to position 8 of the AUTO suite — immediately before the park (`command-s31-settle`, which REMAINS LAST per the F1 ruling). One data file changed: `scenarios/constants.yaml` — (1) the `auto-suite:` key reordered, (2) THE AUTO SUITE OF RECORD block gains the dated `RE-AMENDED 2026-08-04 (Nick's suite-position ruling)` rationale paragraph, (3) the in-comment `bench.sh suite …` example line updated to the new order. Zero scenario/engine/window changes — a precondition amendment in the F1 lineage, not a retune. Duplicate-dispatch check ran first: no prior return on file, dispatch entry unconsumed, baseline clean — a fresh execution, not a re-verify.

## 2. STOP-gate verification (pre-edit, at the instrument)

| Check | Expected | Read |
|---|---|---|
| bench porcelain | CLEAN at `41a7a3c` | `git --no-optional-locks status --porcelain` empty; `rev-parse HEAD` = `41a7a3c8b47bc0355172fab7f01013c21baf64f5` ✓ |
| `constants.yaml` `auto-suite:` | the exact 9-leg list, s31 3rd, park LAST | read at :224–:233 — exact match ✓ |
| `runner.py:83–:88` | KEY ORDER / never sorted / park-LAST rides the order | docstring verbatim: "resolved in KEY ORDER (never sorted: the park-LAST law rides the order, so resolved order == key order by construction)" ✓ |
| `nightly_digest.py:42` | `LATENCY_LEG = "command-confirm-s31"` name-keyed | verbatim match; the parse region (:83–:100) is leg-NAME-keyed, order-agnostic ✓ |

Consumer sweep beyond the pins: repo-wide grep for the leg name — the suite ORDER appears nowhere outside `constants.yaml` (digest fixtures and README cite the leg by NAME; README's same-day-re-run and margin-watch bullets are position-agnostic; the wrapper never carries the list). Zero lockstep files — the 1-M census is structurally sound.

## 3. The change (census EXACTLY 1 M)

`git --no-optional-locks status --porcelain` after all edits:

```
 M scenarios/constants.yaml
```

Diffstat: `37 insertions(+), 4 deletions(-)`. Three edit sites, per instruction: the `auto-suite:` reorder (s31 row moved to 8th with an inline `# 8th — the 2026-08-04 suite-position ruling` marker; park row byte-identical), the example line replaced in place with the new order, and the new dated paragraph inserted between the example line and the F1 "WHY command-s31-settle IS NOW LAST" paragraph. **DP-3 held at zero bytes: the 2026-07-30/31 ruling text (the fused title+chain sentence AND the WHY/MECHANISM/TRAP/MARGIN-WATCH paragraphs) is byte-untouched** — see §5 D-1 for the placement reasoning. The paragraph carries every mandated item: the measured mechanism (turn_on into a mesh up only 0.772 s — `network_resumed` 04:30:51.004 → ACCEPTED 04:30:51.776, the Aug-3 suite-boot log — TIMED_OUT three park-verified nights; settle off-edge CONFIRMED 143 ms / 3.59 s in the +40–55 s post-resume band, on two of those nights ~10 s after the usb leg's deliberate dongle power-cycle recovery — position-8 physics proven under the harshest in-suite condition); the three-boot choreography note (previously fired 3rd, ~12 s into the SUITE boot); the audit pointers BY PATH; the explicit WINDOW/SCENARIOS/park-LAST-all-UNCHANGED statement; and MARGIN WATCH + pre-ruled HUE-RESET REMAIN IN FORCE (the Hue re-power an H3 physical act, complementary, not superseded).

**Sweep-guard:** the commit stages exactly `scenarios/constants.yaml`. `command-confirm-s31.yaml`, `command-s31-settle.yaml`, all runner/digest/wrapper code, both units, README — byte-untouched (porcelain proves it). No retune anywhere.

## 4. Verification gates (desk; ALL GREEN in-session against the final tree)

Gate quotes are content-faithful; the Windows desk console renders the runner's em dashes as mojibake (a console-decode display artifact — the byte gates below are the byte truth).

**Gate 0 (red-first instrument pair, beyond the ordered disclosure — see D-5):** pre-edit `suite auto --list` at `41a7a3c` printed the OLD order (`…command-confirm,command-confirm-s31,command-timeout-absent…`) — the mechanical order assert against DP-1's line was RED by construction before the edit.

**Gate 1 — `python3 -B tools/runner/runner.py suite auto --list --bench-sh tools/bench.sh` → exit 0:**
```
runner B3.1-2026-08-02-postwindow @ 41a7a3c
[--] suite auto: 9 legs from constants auto-suite: boot-health,command-confirm,command-timeout-absent,command-supersession,command-identify-honest,usb-reenumeration,timeout-honesty-no-change,command-confirm-s31,command-s31-settle
[LOAD] boot-health tier=AUTO requires=-        … (all 9 [LOAD] lines, in list order)
[LOAD] command-s31-settle tier=AUTO requires=command-api
listed 9 leg(s) — all load lawfully
```
Mechanical order assert: the printed CSV extracted and string-compared against DP-1's exact line → **PASS (printed == DP-1 line)**. 9 legs, park LAST.

**Gate 2 — the poisoned-injection preservation probe (B3.1 shape) → exit 2:** `rejoin-race-operator` (tier OPERATOR verified at its :81) PREPENDED to a SCRATCH constants copy (scratchpad, outside the repo), run via `--constants <scratch>`:
```
[REFUSED] rejoin-race-operator — OPERATOR-tier scenario is UNLAWFUL in the auto suite (headless operator window, the C-1 hazard) — remove 'rejoin-race-operator' from the constants auto-suite: key; run it hands-on: bench.sh scenario rejoin-race-operator
listed 10 leg(s) — 1 REFUSED
```
`EXIT=2`; the lawful 9 still list (DP-12). The pre-flight refusal survived the reorder.

**Gate 3 — `python3 -B tools/runner/nightly_digest.py --selftest` → exit 0:** `selftest: 21 check(s), 0 failure(s)` — **21/21, byte-exact by the selftest's own construction; zero delta** (order-agnostic confirmed: nothing out of scope was touched).

**Gate 4 — the CR/LF scan, anti-vacuous pair (D-6):** planted control (`_crlf-control.tmp`, untracked, one `\r`) → `git grep -I -l --untracked $'\r' -- _crlf-control.tmp` FOUND it (exit 0) → control removed → the ordered scan `git grep -I -l --untracked $'\r' -- scenarios/constants.yaml` → **zero hits (exit 1)**. Porcelain re-verified clean of the control at the §3 census.

**Gate 5 — red-first disclosure (format #18):** as pre-declared by the instruction, this is a DATA amendment with no red-able fixture — the amendment itself is green-by-construction, disclosed; Gate 0 supplements it with a true before/after instrument pair on the order assert.

## 5. Deviations (severity-honest): ZERO [REVIEW] — six [INFO]

- **[INFO] D-1 (placement under DP-3):** the new dated paragraph sits between the updated example line and the F1 "WHY…" paragraph rather than splitting the fused title+chain sentence to insert at the literal chain head. Reason: the block's chain sentence and title share one paragraph (and one physical line at the seam) — a literal chain-head insertion would force re-wrapping the 2026-07-30/31 sentences, mutating history bytes DP-3 forbids. As realized, history is byte-untouched and the dated-paragraph chain reads newest-first (08-04 before the 07-31 WHY/MECHANISM material). The F1 colon now introduces the new-order example line; the misattribution seam is killed explicitly — the new paragraph opens "the suite line above quotes THIS ruling's order."
- **[INFO] D-2 (fourth audit pointer + night-4 clause):** the paragraph adds `2026-08-04_B3_night4_blockV-adjudication_no-edge-verdict.md` beyond the mandated three, and two clauses carrying its verdict revision (nights 2–3 edge-proven at ~1e-5 coincidence; night-4 NO reported edge; the window hostile end-to-end; position 8 cures BOTH observed shapes). The blockV filing (v45 beat 6) post-dates the instruction (beat 5), revises the per-night mechanism, and explicitly STRENGTHENS this fix — omitting it would leave the standing block implying all three nights were edge-proven. The MUST-carry floor is fully present; this is additive truth.
- **[INFO] D-3:** inline marker on the moved key row (`# 8th — the 2026-08-04 suite-position ruling`), mirroring the park row's marker style. Not mandated; makes the ruling visible at the key itself.
- **[INFO] D-4:** the settle-adjacency pricing (DP-1's rationale) carried into the paragraph — with s31 now immediately before the park, a clock-held TIMED_OUT park becomes the COMMON case; one sentence keeps that red triage-free (the park's terminal assert is disposition-agnostic, the settle header's own contract).
- **[INFO] D-5:** Gate-0 pre-edit baseline run added (the true-red half of the order assert). The ordered gates are unchanged.
- **[INFO] D-6:** Gate-4 run as the established planted-control pair rather than the bare zero scan (anti-vacuous; control created untracked, proven FOUND, removed before the census).

## 6. Observations for the hub (no action from this lane)

- **OBS-1 (calendar drift on the rider):** the instruction's "tonight's 04:30 fire" was authored 2026-08-04; this desk session ran 2026-08-05. The verification-gate fire is the first 04:30 fire AFTER the hub's audit → commit → Pi pull block lands, whichever night that is. The pre-stated prediction stands unchanged (blockV §3 re-affirmed it): `8/9 PASS · 1 SKIP(hue-online) · ON-latency present`; a position-8 FAIL escalates to the pre-ruled HUE-RESET contingency, never a retry.
- **OBS-2 (pre-deploy fires read against the OLD order):** any 04:30 fire that ran before the Pi pull (e.g. an Aug-5 fire) ran position-3 s31 on the old constants — an s31 FAIL there is the known pre-fix mechanism, not a position-8 finding; the morning-gate intake should read it as such.
- **OBS-3 (banner/engine):** the runner banner still reads `runner B3.1-2026-08-02-postwindow @ 41a7a3c` — ENGINE_VERSION lawfully unmoved (constants-only change; the engine-touch law not triggered). The `@` stamp tracks HEAD, so it reads `41a7a3c` until the amendment commits.

## 7. WUCP Phase 1 checklist

- [x] MODULE_CONTEXT.md — N/A (bench repo carries none; no core module touched)
- [x] `context/handoff/coder-handoff.md` — B3.3 DELIVERED entry prepended (bench lane; the S-5a core-lane dispatch entry untouched and still newest-authoritative for its lane); Deferred Build Gate: **N/A-gradle** (bash/python/yaml repo; the desk gates of record ran in-session against the final tree, ALL GREEN — §4)
- [x] `context/lessons/coder-lessons.md` — one entry appended (the fused-paragraph amendment-chain pattern)
- [x] `context/handoff/cross-agent-notes.md` — N/A (no cross-lane consumption; the parallel S-5a core lane shares no files with this WU)
- [x] Return filed (this file) with census, gate outputs quoted, deviations severity-honest
- [x] **Refuse-to-close — NEXT WU identified:** the hub's two-layer audit FIRST (this return + the handoff entry are the inputs) → commit order (staging = exactly the 1-file census) → the hub-authored Pi pull block → the first post-deploy 04:30 fire IS the verification gate (G-E form: one full NIGHTLY, never a manual run). **Nothing else dispatches from the bench lane until the audit closes.** Parallel core lane (S-5a) unaffected; queued behind per the masthead: S-5b STATE-DIALECT P2 · FE-LIVE-V112 (f)/(g).
