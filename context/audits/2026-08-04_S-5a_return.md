<!--
file: context/audits/2026-08-04_S-5a_return.md
purpose: Coder return for S-5a — the sqlite-jdbc WAL-corruption-class bump (3.51.2.0 → 3.51.3.0) + the four-class float-serialisation exposure survey. Audit input for the hub's two-layer audit; the survey section is the standing record every future driver/engine bump inherits.
audience: hub/PM (audit), Nick (commit order + H3 deploy)
status: DELIVERED — awaiting the hub two-layer audit. The lane committed NOTHING. Session executed 2026-08-05 (the WU keeps its 2026-08-04 dated identity — the B3.3 calendar-drift class, same disposition).
baseline: core `60d3ab5` (re-verified clean at launch); instruction: context/instructions/2026-08-04_S-5a_sqlite-bump-and-float-sweep_coding-instruction.md
-->

# S-5a Return — sqlite-jdbc 3.51.3.0 + the float-serialisation exposure survey

## §1 Outcome

DELIVERED. `gradle/libs.versions.toml:17` now reads `sqlite-jdbc        = "3.51.3.0"` (the named fixed release; alignment spaces preserved). Zero Java source changes, zero module-info changes, sibling catalog lines byte-untouched. **Census EXACTLY 1 M** (` M gradle/libs.versions.toml`, lock-free porcelain, flag spelled). All verification gates GREEN in-session against the final tree, outputs quoted in §4. Survey verdict: **ZERO float-serialisation exposure** (§5). **ONE [REVIEW]** rides this return — the STOP-gate version census found a third, unlisted `3.51.2` hit, adjudicated report-and-proceed with the full evidence in §2; the hub ratifies or reverses at audit.

## §2 STOP-on-Mismatch gates

| Gate | Expected | Observed | Verdict |
|---|---|---|---|
| `gradle/libs.versions.toml:17` | exactly `sqlite-jdbc        = "3.51.2.0"` | byte-exact match (Read-tool verified pre-edit) | PASS |
| core porcelain | CLEAN at `60d3ab5` | `git rev-parse HEAD` = `60d3ab5861899a02cd9bed422b305372ed1c41ab`, porcelain empty | PASS |
| repo-wide `3.51.2` census | ONLY toml:17 + `docs/archive/project-state-reports/…2026-04-08.md:267` | **THREE hits — the two expected PLUS `spike/dispatch-latency-and-log-growth/lib/meta.xml:113`** | **DIVERGED → [REVIEW] R-1** |

### [REVIEW] R-1 — the third census hit, evidence and adjudication

**What the file is.** `spike/dispatch-latency-and-log-growth/lib/meta.xml` is a vendored **Maven-Central metadata snapshot** for `org.xerial:sqlite-jdbc` — the artifact's complete published-version ENUMERATION (`3.5.7` → `3.53.2.0`, 107 `<version>` entries, `<lastUpdated>20260604031521</lastUpdated>`). Line 113 is the `3.51.2.0` entry; **line 114 is `3.51.3.0` itself**. It is a record of what versions EXIST, not a declaration of what the project USES.

**Provenance and reachability.** Committed in `b296e76` alongside the spike's vendored jars (`sqlite-jdbc.jar`, `slf4j-api.jar`, `slf4j-nop.jar` in the same `lib/` dir — a self-contained offline spike with its own `run.sh`). The spike dir is **not in the build graph**: `settings.gradle.kts:72-74` includes only `spike:wal-validation`, under the comment "Spike subprojects (throwaway validation — not part of production build)". A repo-wide grep of every `*.gradle.kts`/`*.gradle` for `dispatch-latency` returns zero hits. Phase-1 spikes are throwaway by role definition.

**Classification.** A point-in-time version-enumeration snapshot — the same disposition class as the archive doc: **leave byte-untouched**. It is NOT a live version-string consumer; the bump leaves no stale consumer anywhere.

**Adjudication.** The instruction says any other hit is a STOP-and-report. The instruction's own pushback standard says "evidence-based pushback outranks the instruction; STOP-and-report beats silent adaptation." This is the loud middle: **report-and-proceed**, not silent adaptation — the gate's substance (no live consumer goes stale on the bump) is PROVEN satisfied by the three facts above; a full-stop would have spent the Aug-8 safety runway this WU was split from S-5 to protect, to re-confirm something already proven at source. The hub's two-layer audit precedes any commit and is the ruling point; the edit is one reversible line if the hub rules otherwise. (Precedent: the B2 desk half carried instruction-census divergences as reasoned [REVIEW] deltas and was audited ACCEPT.)

**Standing note for every future bump (inherit this):** meta.xml enumerates EVERY published version, so any future old-version grep census (this survey's own class) will hit it again. Pre-list it beside the archive doc in the next gate's expected set.

## §3 The edit

```
gradle/libs.versions.toml:17
-sqlite-jdbc        = "3.51.2.0"
+sqlite-jdbc        = "3.51.3.0"
```

One line. Nothing else in the file touched (jackson, snakeyaml-engine, and every sibling line byte-identical).

## §4 Verification gates (all in-session, final tree)

**G1 — shift-left, with a pre-edit baseline (the WU-AVAIL-SEED baseline discipline).**
- Baseline at HEAD `60d3ab5` pre-edit, forced-fresh (`:core:persistence:test --rerun`, 44/44 tasks executed): `BUILD SUCCESSFUL in 43s`; XML-aggregated **412 tests / 0 failures / 0 errors across 54 test classes**. Output clean of any PRAGMA/journal/warning line.
- Post-edit `./gradlew :core:persistence:compileJava`: `BUILD SUCCESSFUL in 20s` — `:core:persistence:compileJava` EXECUTED (classpath invalidation), 22 siblings up-to-date. **This was the resolution moment**: `3.51.3.0` was not in the local Gradle cache pre-edit (only `3.51.2.0/` present); it resolved from Maven Central without error → the STOP-on-resolution-failure clause never armed. The module-info compile passing also proves the jar's `Automatic-Module-Name` is stable across the bump — a changed module name would have failed `requires` resolution here.
- Post-edit `./gradlew :core:persistence:test --rerun`: `BUILD SUCCESSFUL in 11s`; **412 / 0 / 0 across the same 54 classes** — the anti-vacuous pairing with the baseline (identical suite shape, zero drift). Forced-freshness proven from the result-file mtimes (`16:24:55` stamps read at `16:25:15`), per the v39 `--rerun`-binding lesson — never from the green summary.

**G2 — full `./gradlew check` (this WU's CC-lane grant), quoted verbatim:**

```
BUILD SUCCESSFUL in 1m 5s
156 actionable tasks: 110 executed, 46 up-to-date
```

**156 actionable == the WU-AVAIL-SEED in-session record == Nick's post-clean DASH-SERVE record** — zero task-count drift, nothing to reconcile. The executed-count asymmetry vs AVAIL-SEED's 87 (110 here) is the expected mechanism: the classpath swap invalidates compile/test tasks in every sqlite-reachable module, so more of the graph re-executed. CI on the pushed commit remains the gate of record (D1) after the hub's commit order.

**G3 — dependency-resolution truth.** Instructed command plus a belt-and-braces second leg on the artifact the Pi actually consumes:
- `:core:persistence:dependencies --configuration runtimeClasspath | grep -i sqlite` → `+--- org.xerial:sqlite-jdbc:3.51.3.0`
- `:app:homesynapse-app:dependencies --configuration runtimeClasspath | grep -i sqlite` → `|    +--- org.xerial:sqlite-jdbc:3.51.3.0`
- `grep -c "3.51.2.0"` over BOTH full resolution outputs → **0**. No residue anywhere in either resolution.
- Local Gradle cache now holds `3.51.3.0/` (fetched this session) alongside the pre-existing `3.51.2.0/` (normal cache retention — OBS-1).

**G4 — red-first disclosure (per instruction gate 4).** A version bump has no red-able fixture; the existing suite IS the preservation gate. Predicted green-by-construction, disclosed pre-run; observed green (G1/G2).

**G5 — the PRAGMA/journal watch (the instruction's §What-to-watch-out-for).** The FULL check log was captured to file and scanned: `grep -inE "warn|pragma|journal|deprecat"` → **ZERO hits**. No changed-default signal, no deprecation chatter, nothing to quote as a delta; the pre-edit baseline run's output was equally clean, so the comparison is same-session instrument-to-instrument, not memory.

**G6 — census.** `git --no-optional-locks status --porcelain` → exactly ` M gradle/libs.versions.toml`. 1 M, zero ??.

## §5 The float-serialisation exposure survey (the B-1 rider — read-only)

Premise re-confirmed at source before the survey: the exposure profile named in the instruction is real — `DatabaseExecutor.start()` opens 1 write connection + `readThreadCount` read connections on ONE database file with `journal_mode = WAL` applied first (`connectionPragmas()` order-pinned), plus per-subscriber read connections via `SqliteSubscriberReadConnectionFactory` — a pooled Java writer with ≥2 connections on one WAL file, exactly the fixed bug's shape.

| # | Class | Census (source-read, this session) | Disposition |
|---|---|---|---|
| 1 | REAL columns | All 5 event-store migrations line-read: V001 (`events` 25 cols + `subscriber_checkpoints` + `view_checkpoints`), V002 (`subscriber_dead_letters`), V003 (`snapshots` + index drop), V004 (indices only), V005 (`payload_iv BLOB` + `dek_ref TEXT`). Every column is INTEGER, TEXT, or BLOB; timestamps are INTEGER micros; payloads are BLOBs. **Zero REAL/FLOAT/DOUBLE columns.** | **NO-EXPOSURE** |
| 2 | SQLite-rendered float text | Persistence main grep: `CAST(`/`printf(`/`quote(` → **zero**. `GROUP BY` → **zero**. `ORDER BY` → 7 sites, every operand INTEGER (`last_attempt_at`, `dlq_id`, `global_position` ×4, `subject_sequence`). `getString(` → 14 sites, every read column TEXT-typed (`checksum`, `description`, `view_name`, `subscriber_id`, `sequence_key`, `cause_class`, `cause_message`, `diagnostics`, `event_type`, `subject_type`, `priority`, `origin`, `event_category`, `dek_ref`). `getDouble`/`getFloat`/`setDouble`/`setFloat` across the ENTIRE persistence `src/` (main AND test) → **zero occurrences** — no float ever crosses the JDBC boundary in this module today. With zero float columns (class 1), no `=`/comparison on a float-typed expression can exist. | **NO-EXPOSURE** |
| 3 | Hash/diff over SQLite output | Repo-wide main-source digest census — every site's INPUT identified: `MigrationRunner` (SHA-256 over migration FILE bytes read from the classpath; compared string-to-string against the TEXT `checksum` column — the input is never SQLite-rendered), `IdempotencyCache` (HTTP body bytes), `OpaqueTokenStore` (raw token bytes), `IntegrationIds` (constant namespace string), `DefinitionHashes` (in-memory definition strings), `Hkdf` (key material). `chain_hash` is Java-side and the 32-byte zero vector today (AB-4); when live its input is the payload BLOB bytes, not rendered text. The toolchain's ONLY SQLite-rendered text output (the non-vacuous leg — it exists and was checked): `distribution/update/update.sh:35` + `update-smoke.sh:35` run `sqlite3 … 'SELECT COUNT(*) FROM events;'` (INTEGER rendering), `update-smoke.sh:36` runs `PRAGMA integrity_check` (TEXT), and `install-smoke.yml:72-73` installs sqlite3 for the zero-event-loss ROW-COUNT assertion (INTEGER). **Zero float-typed values rendered by SQLite anywhere in the toolchain.** | **NO-EXPOSURE** |
| 4 | Jackson float paths | Context line, for the record: payload float formatting is Jackson's (`PersistenceObjectMapper`/`EventPayloadCodec`; the AMD-52/AMD-87 discipline anchors float identity to `Double.doubleToLongBits` with non-finite sentinels — the text need only round-trip), fully independent of the driver/engine; floats reach SQLite only inside the opaque payload BLOB, which SQLite stores and returns byte-identically and never renders. | Context filed |

**Verdict: ZERO exposure** — as expected. The engine 3.53.0 float-to-text change (15 → 17 significant digits; NOT in this bump) would perturb nothing in this codebase as it stands. This survey is the standing record; future bumps inherit it instead of re-deriving.

**Two standing pointers for future bumps:** (a) the spike `meta.xml` will hit every old-version grep census (§2 R-1 note) — pre-list it; (b) the telemetry migration track (`db/migration/telemetry/`) is still empty — the FIRST real REAL-column candidate (`telemetry_samples.value`; `TelemetrySample.value` is `double` by contract) arrives when the M2.8-class ring-store SQLite half ships. Binary storage/retrieval via `setDouble`/`getDouble` will remain non-exposed even then; only SQLite-side float→text rendering of it would re-open class 2.

## §6 P2 consumer/pin survey

Version-string consumers: the toml line (updated) + the archive doc (byte-untouched, per the instruction's disposition) + the spike `meta.xml` (byte-untouched, R-1). No enum/registry/sealed-set/count-pin touched; no module boundary crossed; **ARCH-RULE-REACH: N/A** (zero source changes — confirmed by the 1-M census); no module-info change (none possible from a catalog bump). §4c not engaged: zero test files touched.

## §7 Deployed-runtime note (context, not action)

The Pi runs the OLD jar (the deployed build of record predates this bump) until the next installDist deploy — **the H3 trip (Aug-8/9) carries it**. This desk green is NOT a deployed fix; nobody should read it as one. The WAL-corruption exposure window on the deployed hub closes only at that deploy.

## §8 Deviations

**[REVIEW] R-1** — the STOP-gate census third hit, adjudicated report-and-proceed; full evidence and rationale in §2. The audit's first stop.

**[INFO] I-1** — a pre-edit forced-fresh baseline run of `:core:persistence:test` was executed (not explicitly ordered by this instruction; the WU-AVAIL-SEED baseline discipline). Reason: it grounds G5's "differ from baseline" watch in a same-session instrument and proves the suite green at HEAD before the edit. Impact: none — read-only build state.

**[INFO] I-2** — the dependency-truth check was run against the app's `runtimeClasspath` in addition to the instructed persistence-scoped command. Reason: the app's resolution is the one `installDist` ships to the Pi — it is the deployment-relevant truth. Impact: none; both quoted, both clean.

**[INFO] I-3** — the post-edit targeted run reported "3 executed, 41 up-to-date" (vs the baseline's 44/44): the warm daemon plus Gradle ABI-based compile avoidance kept sibling compiles up-to-date (the driver's public ABI is unchanged across the patch bump); the test task itself EXECUTED, proven by result-file mtimes, not the summary. Disclosed so the asymmetry is explained, not discovered.

**[INFO] I-4** — this return files under the instruction's dated path (`2026-08-04_S-5a_return.md`) from a 2026-08-05 session — the B3.3 calendar-drift class, same disposition (the WU keeps its dated identity).

**OBS-1** — the local Gradle cache retains `3.51.2.0/` alongside `3.51.3.0/`. Normal cache behavior; no action. (The residue-free claim in G3 is about the RESOLUTION, which is clean.)

**OBS-2** — the telemetry migration track's future REAL column (§5 pointer b) — for the hub's awareness when the ring-store WU is eventually chartered.

NO [BLOCKING] deviations.

## §9 Census and sweep-guard

The commit stages **exactly one path: `gradle/libs.versions.toml`**. Safe IFF a fresh lock-free porcelain shows exactly that 1 M — `spike/dispatch-latency-and-log-growth/lib/meta.xml`, the archive doc, and every `.java`/`.sql`/`module-info` file NEVER appear in the staging. Anything else in porcelain at commit time is a STOP.

## §10 Verification commands for Nick (optional re-runs; the in-session outputs are quoted above)

```
git --no-optional-locks status --porcelain            # expect exactly: M gradle/libs.versions.toml
./gradlew check                                       # expect BUILD SUCCESSFUL, 156 actionable
./gradlew -q :app:homesynapse-app:dependencies --configuration runtimeClasspath | grep -i sqlite
                                                      # expect org.xerial:sqlite-jdbc:3.51.3.0, no 3.51.2.0
```

## §11 WUCP Phase 1 checklist

- [x] MODULE_CONTEXT.md updated for: **none — no module touched** (catalog-only change; `core/persistence/MODULE_CONTEXT.md` carries no driver-version pin — the hub-verified claim re-verified this session by the §2 census grep, which did not hit it; an update would also break the exactly-1-M census)
- [x] coder-handoff.md updated (S-5a DELIVERED entry prepended, newest-authoritative for the CORE lane; masthead updated)
- [x] Deferred build gate flag: **NO** — full `./gradlew check` ran in-session, GREEN, quoted in §4
- [x] coder-lessons.md appended: **No new patterns** — every discipline applied this session (baseline-first, `--rerun` mtime proof, lock-free porcelain, evidence-based pushback) is an existing minted lesson; the one novel finding (the meta.xml census class) is a survey/return artifact, filed in §2/§5 where future bumps inherit it by design
- [x] Cross-agent note posted: **Not needed** — this return + the handoff entry are the audit inputs; R-1 is routed to the hub through them
- Timestamp: 2026-08-05 21:27 UTC

**NEXT WU (refuse-to-close):** the hub's two-layer audit FIRST — no commit until its order. Then, per the masthead queue: **S-5b STATE-DIALECT P2** (authors separately after its grounding read) · FE-LIVE-V112 (f)/(g) (FE lane). Nothing else dispatches from the core lane until the audit closes.
