<!--
file: context/audits/2026-08-05_S-5a_audit_v46-beat-2.md
purpose: The hub's two-layer audit of the S-5a return (sqlite-jdbc 3.51.3.0 + the float-serialisation exposure survey) — ACCEPT; R-1 ratified; the core commit order issues on this verdict.
audience: Hub, Nick
state-type: audit adjudication (point-in-time)
status: FILED 2026-08-05 (v46 hub, beat 2)
provenance: Return = context/audits/2026-08-04_S-5a_return.md + the Coder's completion report (pasted at intake). Layer 2 = host-byte spot-checks this beat, core worktree at 60d3ab5 + the two delivered-uncommitted lanes.
-->

# S-5a Audit — ACCEPT (v46 beat 2)

## 1. Verdict

**ACCEPT — the one [REVIEW] (R-1) RATIFIED as filed; four [INFO] + two OBS ratified.** Layer 1: the STOP gates handled correctly — two PASS byte-exact, one DIVERGED into the loud-middle report-and-proceed with full evidence filed (§2 of the return; the B2-precedented class — correct, not silent adaptation); the baseline/post anti-vacuous pairing held (412/0/0 across the same 54 classes both sides, forced-fresh, result-file-mtime proven per the v39 lesson); red-first lawfully disclosed green-by-construction (no red-able fixture exists for a version bump — arc-discipline 3's carve-out); the full `./gradlew check` ran in-session under the CC-lane grant (156 actionable == the WU-AVAIL-SEED record, zero task-count drift, the executed-count asymmetry explained by the classpath invalidation mechanism); dependency truth proven on BOTH `:core:persistence` and `:app:homesynapse-app` runtimeClasspath (I-2 — the deploy-relevant leg, exactly the right instinct) with zero `3.51.2.0` residue in either full resolution; the PRAGMA/journal watch ran instrument-to-instrument over captured logs, zero hits.

**Layer 2 at host bytes (this beat):** `gradle/libs.versions.toml:17` = `sqlite-jdbc        = "3.51.3.0"` (alignment preserved) · `spike/dispatch-latency-and-log-growth/lib/meta.xml:113` verified an ENUMERATION entry (`3.51.2.0`) with `3.51.3.0` itself at :114 — a vendored Maven-Central metadata snapshot, byte-untouched · `settings.gradle.kts:74` includes only `spike:wal-validation` under the "throwaway validation — not part of production build" comment — the spike is outside the build graph · the five event-store migrations carry ZERO REAL columns (dir-grep, empty) · persistence `src/` carries ZERO `getDouble|getFloat|setDouble|setFloat` (grep, empty) · the return + the WUCP trio present and exact at hivemind porcelain (2 M + 2 ?? against `147de72`). Core FULL porcelain was NOT run at the hub's instrument — two mount-ceiling timeouts this beat (the §12 repo-size class; flag spelled: `git --no-optional-locks status --porcelain`); the whole-tree census proof rides the operator's NATIVE glance STOP-gate at commit time (expect exactly 3: the toml + the two S-5c paths).

## 2. Ruling

**R-1 RATIFIED** — report-and-proceed was the correct adjudication: the third census hit is a version ENUMERATION outside the build graph, not a live version-string consumer; byte-untouched is the right disposition; the gate's substance (no stale consumer survives the bump) is proven at source. **The standing note ADOPTED:** `spike/dispatch-latency-and-log-growth/lib/meta.xml` pre-lists beside the archive doc in every future old-version census expected set.

## 3. Effects

The S-5a commit order issues this beat: **stages exactly 1 M (`gradle/libs.versions.toml`); msg `_scratch/2026-08-05_core_S-5a_commit-msg.txt`; commits BEFORE S-5c; the meta.xml, the archive doc, and every source file NEVER appear in the staging.** The §5 survey is the standing float-exposure record future driver/engine bumps inherit; OBS-2 (the telemetry track's future REAL column, `telemetry_samples.value`) is noted for the ring-store WU's eventual charter. Deployed-runtime: the Pi keeps the old jar until the H3 deploy (Aug-8/9) carries this — desk green is not a deployed fix. D1: CI on the pushed commit is the gate of record — watched at the push.
