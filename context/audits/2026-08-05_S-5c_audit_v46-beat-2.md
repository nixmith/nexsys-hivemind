<!--
file: context/audits/2026-08-05_S-5c_audit_v46-beat-2.md
purpose: The hub's two-layer audit of the S-5c return (atomic sidecar write — the REV-1 F-3 fix under Nick's R-B(a) fence) — ACCEPT; OBS-1 shelved by name; the core commit order issues on this verdict.
audience: Hub, Nick
state-type: audit adjudication (point-in-time)
status: FILED 2026-08-05 (v46 hub, beat 2)
provenance: Return = context/audits/2026-08-05_S-5c_return.md + the Coder's completion report (pasted at intake). Layer 2 = host-byte spot-checks this beat, core worktree at 60d3ab5 + the two delivered-uncommitted lanes.
-->

# S-5c Audit — ACCEPT (v46 beat 2)

## 1. Verdict

**ACCEPT — zero [REVIEW]; six [INFO] + three OBS adjudicated.** Layer 1: the baseline was re-derived at launch exactly as the instruction ordered (the S-5a-first arm in delivered-uncommitted form; the single porcelain divergence byte-verified as exactly the S-5a toml line and disclosed as D-1 — lawful, the R-1 loud-middle class); TRUE red-first — the mechanism pin failed RED with the XML quoted verbatim, and tests 1/3 were disclosed green-by-construction rather than dressed as reds (D-2 — exactly the arc-discipline-3 preservation-fixture carve-out); green ×2 forced `--rerun`, mtime-proven, 560/0/0 (the 557+3 arithmetic exact against the AVAIL-SEED baseline); spotless zero reformats; full check 156 actionable, zero task-count drift; `:app` 19/0/0 + `:lifecycle` 60/0/0 forced `--rerun` against FINAL bytes so the ArchUnit leg (incl. `NO_DIRECT_TIME_ACCESS`) re-scanned the new production code instead of quoting an up-to-date fingerprint (D-6 ratified — the load-bearing instinct, and exactly the §4c self-enforcement this review is the only gate for); §4c ordered grep zero hits, injected `TestClock` only.

**Layer 2 at host bytes (this beat):** `ZigbeeDeviceCache.java:482–483` = `Files.writeString(temp, …)` → `Files.move(temp, file, StandardCopyOption.REPLACE_EXISTING)`; the `StandardCopyOption` import at :18; **ZERO `ATOMIC_MOVE` / `fsync` / `force(` hits anywhere in the file — NICK'S FENCE HELD AT BYTES.** The three pins present by name: `tornSidecar_discardedWithOneWarn` :540 with the `zigbee.device_cache_corrupt` WARN token at :532 · `writeLandsAtTempSibling_blockedMoveLeavesFinalUntouched` :576 with the `Files.createDirectories(file)` move-blocker at :583 and the red-first assert message at :591 verbatim to the quoted failure XML · `completedMove_replacesWholesale_leavesNoTempResidue` :622; 23 `@Test` in the class. One adjudicated note (no defect): `DirectoryNotEmptyException` appears in the return as the JDK-SPECIFIED failure mode of the blocked move — it is not, and need not be, an asserted type in test source; the assertions bind the observable invariant (temp complete and loader-parse-proven, final path untouched, recovery tail consumes the stale temp). Accurate as written.

## 2. Dispositions

**OBS-1 → the POST-GATE SHELF BY NAME** ("S-5c/OBS-1 — the `close()`-flush racing-writers residue," `ZigbeeIntegrationAdapter.close()` :421–428): a pre-existing class, NARROWED by this fix to the move instant, never widened; full closure (unique-per-write temp names or an io-serialization lock) exceeds the ruled fence and would trade orphan accumulation or new locking for a window the pre-fix code carried anyway. It joins the REV-1 F-4/F-5 hardening family at the charter/shelf record. **OBS-2 RATIFIED** (same-directory `Files.move` without ATOMIC_MOVE is a plain atomic-to-observers rename on both platforms; the sidecar now shares the parameter store's exact crash posture; N-4's documented `synchronous=NORMAL` contract continues to govern power-cut tail durability — the fence's own rationale). **OBS-3 RATIFIED** (at most one orphan `.tmp`, loader-ignored, next-write-consumed — self-healing and bounded, recovery-tail-proven). **D-3's MODULE_CONTEXT one-line F-14-row fold QUEUED as a hub touch** ("+ writes are temp-then-move, F-3/S-5c" on the :397 row) — it rides the next hub-owned core beat, NEVER these commits (census integrity).

## 3. Effects

The S-5c commit order issues this beat: **stages exactly 2 M (`integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/ZigbeeDeviceCache.java` · `integration/integration-zigbee/src/test/java/com/homesynapse/integration/zigbee/ZigbeeDeviceCacheTest.java`); msg `_scratch/2026-08-05_core_S-5c_commit-msg.txt`; commits AFTER S-5a; the toml, `PersistentNetworkParameterStore.java`, and MODULE_CONTEXT.md NEVER appear in the staging.** The H3 deploy (Aug-8/9) now carries S-5a + S-5c together in the jar. D1: CI on the pushed commits is the gate of record — watched at the push.
