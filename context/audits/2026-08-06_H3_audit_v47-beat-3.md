<!--
file: context/audits/2026-08-06_H3_audit_v47-beat-3.md
purpose: The hub's two-layer audit of the H3 Pi-trip operator return (filed beside it at context/audits/2026-08-06_H3_pi-trip_operator-return.md). Verdict, banked products, anomaly dispositions A1–A12, the hub-owned defect census, and the open items HA-1..HA-4 with their authored closures.
audience: hub; the charter; the playbook fold.
status: FILED 2026-08-06, v47 beat 3.
-->

# H3 AUDIT — v47 beat 3 · VERDICT: **ACCEPT**

## 1. Layer 1 — return integrity

Verdict table complete B0–B8 with every gate's disposition; the DONE-WHEN scorecard is honest (element 4 recorded NOT MET with the lawful alternative and its structural root cause, not papered over); the anti-action attestation is complete and specific; every STOP was routed to the hub with zero conductor adjudications and zero improvised commands (the one proposed improvisation, `digest 1`, was offered to operator authority, struck by the hub as the wrong instrument, and never run). Conductor discipline: exemplary — three loud flags (B1 anomaly, B8 gate mechanism, A9 timestamp arithmetic), all of which survived hub adjudication as either correct or correctly-routed. Filing note: the as-received paste truncates mid-B7 (seam marked in the filed return); the missing S-4 grep bytes are held from the operator's raw terminal transcript, which the hub received directly in the same intake — no evidence is lost.

## 2. Layer 2 — hub spot-checks at available instruments

- **A3 root cause CONFIRMED AT SOURCE by the hub independently** (before the return assembled): `scenarios/constants.yaml` :137–:144 `capabilities.hue-online.available: false`, static, never probed; :50 defines HUE-RESET = wall power; the constants block pre-documents "SKIPPED-honest while HUE-RESET pending."
- **A2 chain CONFIRMED AT SOURCE:** digest lines are written only by `tools/nightly.sh` → `nightly_digest.py` → `~/hs-bench/digests/nightly.log`; `suite auto` writes none.
- **A10 hypothesis CONFIRMED at the B7 body bytes:** the bundle JSON backslash-escapes quotes (`\"ACCEPTED\"`); the hub's H-A2 pattern demanded unescaped quotes — a hub-authored instrument defect; corrected read authored (§5).
- **A9 RATIFIED:** the timestamp arithmetic is timeline-consistent (log lines carry no date; each "early" stamp is the PRIOR night's surviving app torn down by the next nightly). This extends the REV-1 shutdown-window race from 2 confirmed instances to one-hit-per-surviving-app across the sampled corpus — the race is systematic, bounded, and benign at stop time.
- **B2/B3/B8 records corroborated** against the operator's raw terminal transcript received in the same intake (byte-consistent).

## 3. Banked products (the trip's yield)

1. **S-5a + S-5c are DEPLOYED and instrument-proven** (classpath pair 1/0 at pid 88724→89068): the WAL-exposure window on the deployed hub CLOSED 2026-08-06 ~19:37 local; the sidecar's torn-write window closed with it. Desk-green → deployed, verified, done.
2. **No-regression rep:** 8 run legs PASS · 0 FAIL · 0 forbidden on `3723e31`; s31 PASS 2/2 at position 8; park re-set LAST. The deploy regresses nothing.
3. **Survival fix byte-pinned live** (killmode.conf bytes + `KillMode=process`/`Type=oneshot` at systemctl show) — the K:(a) fold's before-state is on the record.
4. **Settle terminals** (completeness): Aug-4 `CONFIRMATION_TIMED_OUT` @ +5.59 s (honest), Aug-5 `CONFIRMED` @ 158 ms (`match_type: exact`). Both terminal:true; the disposition-agnostic assert held both nights.
5. **S-4 residual: CLOSED FOR THE SAMPLED WINDOW** — trigger uniformly `cause=read-error` across the last-30 corpus (Jul-31 → Aug-6 nightly era + two Jul-8 samples), including both live shutdown-window instances observed today. A8's exhaustiveness caveat is valid; the full-corpus census is authored (§5) — a cause-census one-liner, uncapped.
6. **The Hue physical half executed** (HUE-RESET per constants :50; direct wall outlet, one power-up, 19:54:56 Pi-local, clock-synced). **Alive-proof PENDING:** zero radio-layer activity observed post-act (H-A1 clean negative, formally past T+5, stale-symlink excluded).

## 4. The hub-owned defect census (this trip; all acknowledged, none defended)

- **D-1 (A1):** package GOAL implied the deploy was the s31 fix vehicle — premise-provenance omitted. Rule minted: *every deploy package states in one line what is ALREADY fixed/verified vs what THIS run adds.*
- **D-2 (A2):** a digest-surface token asserted on the live suite surface — the standing glance-semantics class, re-fired.
- **D-3 (A3, the headline):** a capability-gate MECHANISM asserted without a source read — the C-class extended to gates: *every asserted gate/skip behavior is verified at the constants/engine source at authoring.*
- **D-4 (A10/H-A2):** an extraction pattern shipped unverified against the actual instrument bytes — *patterns are fixtures: verify against a real captured file before issuing.*
- **D-5 (A4, minor):** "self-reports HEALTHY" named a token that does not exist (the bench.sh header's own vocabulary, not the emission). The verified-verb rule extends to VERDICT VOCABULARY.
- Plus the pre-trip **morning-of re-derivation rule** (the beat-2 finding). **All six fold into the playbook §8 as one addendum block on the consolidated micro-beat** — the C-class counter grows and the checklist grows with it.

## 5. Open items and their authored closures

- **HA-1** → the full-corpus cause census (read-only, uncapped): `grep -h -oE "port_unhealthy: cause=[a-zA-Z_-]+" ~/hs-bench/bench-*.log ~/hs-bench/current.log | sort | uniq -c`. Pre-stated: expect `cause=read-error` dominant/exclusive; any second cause value is a NEW DATUM (an intake, not a crisis).
- **HA-2** → corrected lifecycle read (read-only): `grep -oE "(CONFIRMATION_TIMED_OUT|ACCEPTED|DISPATCHED|CONFIRMED).{0,45}" ~/hs-bench/bundles/command-confirm-s31-20260807T001211Z/api-captures.json` — matches the phase names regardless of escaping and carries the `at` stamp in the trailing window. The hub computes ACCEPTED→CONFIRMED for the turn_on and rules the datum INTO C4 with attended-rep provenance noted (routine PASS datum from the standing suite at the standing position).
- **HA-3** → ESCALATED to Nick (decision block in-chat): the Hue has not proven alive. REC (a): ONE plain wall power-CYCLE (the §6-verified procedure: power-cycle ⇒ re-announce; NOT the 6×), 60 s hands-off, re-run the H-A1 grep. (b) passive wait + tomorrow's log read. (c) defer to the Aug-8/9 window.
- **HA-4** → SEQUENCED: the HUE-UNSKIP micro-edit (constants.yaml `hue-online: available: true` + reason re-grounded to the 2026-08-06 wall-power act) authors ONLY after an observed announce/rejoin (alive-proof), and rides the CONSOLIDATED MICRO-BEAT with the K:(a) unit-file fold and the playbook §8 addendum block. Until it deploys, **the nightly bar remains 8/9 · 1 SKIP(hue-online)** — 9/9 becomes the bar at the first post-deploy fire after the flip.

## 6. Standing corrections this audit makes

The beat-2 spine line "the Aug-7 digest may read 9/9 a night early" is CORRECTED: the gate is static; the Aug-7 bar is 8/9 · 1 SKIP. The original Aug-8/9 H3 trip is RETIRED as a scheduled item — its agenda is fully executed or explicitly re-homed (HUE-UNSKIP + folds → the micro-beat; MODULE_CONTEXT F-14 row → the next core order; the killmode fold → the micro-beat). Aug-8/9 revert to buffer days, authoring-free beyond intakes per the charter-runway ruling.
