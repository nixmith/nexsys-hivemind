<!--
file: context/audits/2026-08-07_M9.5-DURd_return.md
purpose: M9.5-DURd Coder return — PHASE-A STOP under the instruction's own clause ("if still green ×5, STOP — report, do not proceed to Phase B on an unreproduced mechanism"). The pinned instrument did not reproduce the CI-red at desk (11/11 green, fidelity-proven); the label-wipe mechanism is additionally SOURCE-CLOSED at 8955e23 for these flows; a new source-grounded candidate (H-E, the replay-purity edge-swallow) is filed with the CI-record discriminator that localizes the failing leg. Zero code deltas; porcelain clean; nothing committed.
audience: hub (re-scope); Nick
status: PHASE-A STOP — WU OPEN, awaiting hub re-scope. Filed 2026-08-07.
baseline: core 8955e23 (verified at launch AND at close; porcelain clean both times)
instruction: context/instructions/2026-08-07_M9.5-DURd_heroloop-ci-red_diagnose-and-label-log_coding-instruction.md
-->

# M9.5-DURd Return — Phase A STOP: unreproduced at the pinned instrument; label-wipe source-closed; H-E filed

## 1. Verdict

**STOP at Phase A step 1, per the instruction's own clause.** The pinned repro instrument (`:lifecycle:lifecycle:test --tests '*HeroLoopHardwareFreeIT*'`, test JVM under `-XX:ActiveProcessorCount=2`, then `=1`, forced-fresh ×5 each) went **11/11 GREEN** (5 at APC=2, 6 at APC=1 including the fidelity run). The fork pair was never reached — no timeout fired, so no captures exist. Phase B was **not entered** (pre-authorized only for the instrument-confirmed label-wipe arm). Zero production deltas, zero test deltas, zero commits; porcelain clean at `8955e23` at close.

Beyond the mandated STOP, this return files two findings the re-scope needs:
- **§5: the label-wipe mechanism is structurally closed at `8955e23` for these test flows, at source** — no post-barrier registry-rewriting delivery exists.
- **§6–§7: the CI record itself localizes the failing leg to the motion-edge chain** (state_reported → state projection → state_changed → trigger), and a source-verified mechanism exists there (**H-E**: the replay-purity edge-swallow) that matches every fact on record.

## 2. STOP-gates (all passed at launch)

- **G-1:** HEAD `8955e23ded8769379333328592b8b4ecb0d4b0b9`, porcelain clean. PASS.
- **G-2 (label-site census, full enumeration):**
  - `HeroLoopHardwareFreeIT.java` — :372 `label(snzbEntity, "motion")` · :373 `label(hueEntity, "hero-light")` · :387 helper decl · :389 `entityRegistry().updateEntity(...)` · :507 javadoc mention (barrier doc).
  - `RestartHonestyIT.java` — :168 `label(hueEntity, "hero-light")` · :171 helper decl · :173 `updateEntity(...)` · :249 javadoc mention.
  - `ZigbeeReplaySafetyIT.java` — :127 `label(hueEntity, "hero-light")` · :205 helper decl · :207 `updateEntity(...)` · :251 javadoc mention.
  - `ProcessRestartIdentityIT` — **zero hits** (label-free, matching the DURc record).
  - No site outside the expected three. PASS.
- **G-3:** :115 is the `awaitTrue` on `sentFrame(0x0006, 0x01)` ("the On frame reaching the scripted NCP"). PASS.

## 3. The instrument record (Phase A step 1)

| Run | Constraint | Command | Verdict |
|---|---|---|---|
| A1 | `-XX:ActiveProcessorCount=2` | `:lifecycle:lifecycle:test --tests '*HeroLoopHardwareFreeIT*' --rerun` | PASS |
| A2–A5 | `=2` | same, forced-fresh each | PASS ×4 |
| B1–B5 | `=1` | same, forced-fresh each | PASS ×5 |
| B6 | `=1`, `-i` (fidelity) | same + scratch `availableProcessors` print | PASS |

**Instrument fidelity PROVEN (anti-vacuous):** the B6 `-i` log shows the Gradle Test Executor command line carrying `-XX:ActiveProcessorCount=1`, and the in-JVM scratch print recorded `phaseA.availableProcessors=1` in **all five** test methods. The constraint governed the test JVM; the greens are not vacuous.

**Plumbing (reverted):** a scratch `tasks.test { jvmArgs("-XX:ActiveProcessorCount=…") }` block in `lifecycle/lifecycle/build.gradle.kts`; a scratch try/catch around the :115 await calling a `dumpPhaseADiagnostics()` (checkpoint + both entities' live labels + full event census with per-`entity_registered` payload labels — the fork pair plus wiping-delivery identification, ready to fire at timeout); a one-line `availableProcessors` print in `bootAndAdopt`. **None fired a capture** (no timeout occurred). All reverted via `git checkout`; porcelain empty.

**The fidelity gap, stated honestly:** `-XX:ActiveProcessorCount` shapes JVM-visible sizing (FJ/VT carrier parallelism, GC threads, common pool) but does **not** constrain OS scheduling — on this 24-logical-core desk every runnable thread still gets a physical core immediately. The CI starvation axis (OS-level timeslicing of 2 shared vCPUs against the Gradle daemon + cold-JIT compile storm, plus whatever the Aug-7 runner image changed) is not reproducible by this flag on this hardware. The instrument was faithful to its pin and the pin cannot open the window here — the same reason DURc's own red never had a desk leg ("a nondeterministic scheduling race has no honest red leg").

## 4. Phase A steps 2–3: not reached

The fork pair (label present? `command_issued` present?) requires the timeout to fire. It never did. No captures exist; none are fabricated. The wiping-delivery identification (step 3) is answered instead at source, negatively — §5.

## 5. The label-wipe mechanism is SOURCE-CLOSED at `8955e23` for these flows

Every path that could rewrite the entity registry after the DURc barrier was walked at source. All four legs are fenced:

1. **No cycle work happens off the test thread.** Driven mode: `ZigbeeIntegrationAdapter.run()` :396–:403 opens the channel, negotiates, and **parks on the stop latch** — "the gates own the §G cycle cadence via `runCycleOnce()`". Interviews, adoption, ingestion, availability all run on the test thread.
2. **The posture F1 re-emit (the H-A candidate) is unreachable in these flows.** `applyPostureRouting` (:965) runs only from `driveReporting` (:947), which runs only from `adoptIfAccepted` — either the accept-listed FRESH arm (:909, `adoptAcceptList` from config at `initialize()`, :206 "**empty means nothing ever adopts**"; the IT YAMLs carry **no** `integrations:` section, so the list is empty and the arm returns) or the LINKED re-announce arm (:905–:906, absent in HeroLoop — no announce follows adoption; present only in RestartHonestyIT post-restart, on the test thread, and label-preserving since the re-emit snapshot reads the **current** registry record :971–:983). The ITs adopt via `rig.adopt()` → `adoptionSlice().adopt(...)` directly (test thread), which never drives reporting.
3. **The publisher census is closed.** Main-source `ENTITY_REGISTERED` publishers are exactly two: `ZigbeeAdoptionSlice.adopt()` (pre-barrier, test thread) and the posture path (unreachable, above). There is no third.
4. **The bus boundary cannot re-deliver a registration fact post-barrier.** The TRANSITION drain fences duplicates: `TransitionCoordinator` :91–:93 skips any queued entry `<= lastReplayedPosition` (INV-BUS-01, "no spurious duplicates at the boundary"). The LIVE/offer paths dedup against the persisted checkpoint (`InProcessEventBus` :262–:283). Overflow recovery (`ReplayDriver` :127–:133) re-pages **from the persisted checkpoint** — the same monotone checkpoint the DURc barrier awaited `>= lastRegistrationFact`, so once the barrier has passed, a registration fact at or below the target cannot be re-delivered.

Consequence: **at `8955e23`, with the DURc barrier in place, no delivery exists in these flows that can wipe a test-set label.** The DURc-diagnosed class is closed as far as the source shows. (This also means the instruction's §What-This-Implements candidate — "a post-barrier `entity_registered` arrival the barrier's max-matching-fact target predates" — has no producer in the failing flow.)

## 6. The CI record's own discriminator (no instrument needed)

Each red run reported **60 tests completed, 1 failed** — only `heroLoop_motionToHonestConfirmed`, 3/3. In the **same JVM**, on each red run, all of the following passed: HeroLoop's four sibling tests (identical `bootAndAdopt`, identical `label()` calls, `fireManual` actions targeting `label: "hero-light"` through the same LabelSelector → dispatch → real handler → scripted-NCP path) and both other label-site ITs. **Label resolution, the automation engine, dispatch, the zigbee handler, and the rig all provably functioned in the failing JVM minutes apart.** The only leg unique to the failing test is the **motion edge**: `state_reported` → state projection → `state_changed` → `StateChangeTrigger` (`StandardAutomationRegistry` :176–:178 binds every trigger kind to `STATE_CHANGED`). A label wiped by a boot-time race in one test's boot but never in the four sibling boots of the same shape, deterministically 3/3, is not a coherent read of that vector; a stall in the motion-edge leg is.

## 7. H-E — the replay-purity edge-swallow (source-verified mechanism; NOT instrument-confirmed)

- `StateProjection` publishes the derived `state_changed` **LIVE-only**: ":57 PUBLISH (LIVE only)"; :399–:400 "In `REPLAY`/`TRANSITION`, derivation runs and state is applied but **publishes are suppressed**"; :475–:478 replay-path re-derivations land "ONLY in in-memory state — **NEVER published** (INV-WRITER-01)". A `state_reported` processed by the projection before it reaches LIVE yields **no** `state_changed`, permanently — there is no LIVE backfill publish, by design (replay purity).
- The failing test's occupancy awaits (:98/:102) read **`STATE_REPORTED` from the event store** — they prove ingestion published the reports, **not** that the projection processed anything. (The v48 evidence read's "H-B REFUTED" holds for H-B as defined — reports arriving — but does not cover the projection leg downstream of them.)
- `awaitRuntimeSubscribersLive()` (:481–:491) gates `automation_engine`, `command_dispatch_service`, `pending_command_ledger`, `integration_supervisor` — **not** `state_projection` (`HomeSynapseCore` :183) and not `registry_projection`. Nothing orders "state projection LIVE" before the motion edge is pumped.
- **Failure shape if the projection is still pre-LIVE when `occupied=true` is processed:** the edge is derived-and-suppressed; the trigger never fires; no `automation_triggered`, no `command_issued`, no frame; ten seconds of total pipeline silence; `:115` exhausts at the poll ceiling. Exactly the R-1 signature.
- **Environment fit:** deterministic wherever the runner is slow enough that the projection systematically remains pre-LIVE through boot + interviews + adoption + barrier (a runner-image change can flip this systematically — the §7-addendum trigger pin); green on any faster environment — matching green ×2 (Aug-5) → red ×3 (Aug-7) on code identical up to one `.md`, and 11/11 desk green even at APC=1.
- **The honest weakness:** substantial wall-clock passes between `core.start()` and the first report pump (session negotiation, two scripted interviews, two adoptions, the DURc barrier), while the projection's empty-log replay is a single page read. H-E requires the projection's VT to lag that entire window — plausible under a 2-vCPU cold-JIT compile storm with unfair carrier scheduling (the four gated subscribers passing their await does not gate the fifth), but **unproven**. This is exactly why it is filed as a candidate, not a diagnosis.
- **The discriminating instrument (hub-owned; distinct from the exhausted re-run class):** one instrumented push whose test scratch captures, at :98/:102 and at the :115 timeout, (a) the `state_projection` `SubscriberSnapshot.mode()` + checkpoint, (b) whether `STATE_CHANGED(occupied)` exists in the log, (c) the fork pair as originally ordered (both entities' live labels; `command_issued` presence). One red run with that capture forks H-E vs any residual label-arm vs an engine-side stall conclusively — it is the Phase-A fork pair generalized one subscriber upstream.
- **If H-E confirms, the natural retirement** is the DURc-fix class, test-only: gate `state_projection` LIVE in the IT harness before the first report pump (the four-subscriber await at :481 already exists; this is one more id, or a dedicated await). **LC-LABEL-LOG remains a worthwhile structural promotion on its own merits** (it retires the direct-`updateEntity` residual by construction) — but §5 + §6 say it would **not** have closed this red; sequencing both is the hub's call.

## 8. P2 Consumer/Pin Survey

- G-2 census: delivered in §2, every hit enumerated.
- (a) publish-count / event-count pins, (b) checkpoint/position pins: **N/A — zero deltas landed** (no re-emit was added; the sensitive families — ZigbeeReplaySafetyIT's `hasSize(1)` frame pin, RestartHonestyIT's `DEVICE_ADOPTED == 1` — are untouched and were not re-priced).
- (c) enum/registry/schema sets: untouched, as the instruction states.

## 9. Cleanup + gates record

- All three scratches reverted (`git checkout`); `git --no-optional-locks status --porcelain` **empty**; HEAD `8955e23` unchanged; **nothing committed** (standing law).
- Phase C gates: **not armed** — no fix landed; there are no touched files for the §4c grep; the full-`check` gate belongs to the eventual fix WU.
- Scratch artifacts (run logs, fidelity log) live in the session scratchpad only — outside every repo.

## 10. Next work unit

Awaiting the hub's re-scope of the CI-red thread on this return (the pre-ruled path: the evidence read `2026-08-07_CI-red_HeroLoop-613_evidence-read_v48-beat-4.md` stays OPEN — its close condition, CI green on a fix push, is unmet). Recommended shape per §7: one hub-owned instrumented CI push carrying the generalized fork capture; then the fix WU for whichever arm the capture confirms (H-E ⇒ the projection-LIVE gate, test-only; label-arm ⇒ LC-LABEL-LOG as already authored). This Coder lane holds until that instruction issues.

---

## §A2 — DURd-A2 Part 1 DELIVERED: the fork-capture instrumentation (2026-08-07, the same lane resumed)

**Authority:** `context/instructions/2026-08-07_M9.5-DURd-A2_instrumented-branch-capture_packet.md` (hub-ratified v48 beat 7 — the §7/§10 recommendation executed as a DIAGNOSTIC-BRANCH capture; the branch never merges).

**G-1 re-verified at resume:** HEAD `8955e23`, porcelain clean (the Phase-A scratches stayed reverted).

**What was instrumented (test-only, one file):** `HeroLoopHardwareFreeIT` — the three awaits the thread names (orig :98 occupied-baseline · :102 motion edge · :115 On frame) are each wrapped in a try/catch that, on `AssertionError`, calls `dumpForkCapture(...)` and rethrows the ORIGINAL error. The capture prints one `=== DURD-A2 FORK CAPTURE ===` … `=== END DURD-A2 FORK CAPTURE ===` block to stdout (the test-reports artifact route the hub already reads) carrying, per the packet's (a)–(f): (a) **every** registered subscriber snapshot's `subscriberId` + `mode` + `checkpoint` — the engineering-clean reading of "(a) state_projection + the four gated ids for contrast": iterating `eventBus().subscribers()` wholesale captures those five AND `registry_projection` with less code; (b) `stateChangedCount`; (c) `motionEntityStateReportedCount` (STATE_REPORTED filtered to the SNZB entity's subject); (d) `snzbLabels` + `hueLabels` from the LIVE registry records (via `findEntity`, printing `ENTITY-ABSENT` instead of throwing); (e) `commandIssuedCount`; (f) `storeLatestPosition` (via `EventStore.latestPosition()`). The `timedOutAwait` label opens the block so the capture names which await exhausted.

**Instrument-integrity properties (packet §3):** `awaitTrue` itself is byte-unchanged (same 500×20 ms, same messages, same throw); the wraps add zero behavior on the pass path; no new awaits, no ordering changes, no time APIs (§4c — the capture reads subscriber snapshots, the store, and the registries only); zero new imports; the capture body is exception-guarded (`capture-failed=` line) so a capture failure can never mask or replace the real timeout signature; the delimiter END line prints regardless. Checked before shipping: no ArchUnit rule reaches `System.out` in lifecycle test code (the only `System.out` in scan scope is the whitelisted app `Main`), and Spotless carries no formatting/import rules my edit could trip — the branch CI can only red AT THE TEST, which is the instrument working.

**Desk verification (this lane's grant, resumed):** `:lifecycle:lifecycle:compileTestJava` + `:lifecycle:lifecycle:spotlessCheck` BUILD SUCCESSFUL; the instrumented class run once forced-fresh — GREEN, and `grep -c "DURD-A2 FORK CAPTURE"` over the class's test XML = **0** (the instrument is inert on the pass path; it fires only at a timeout).

**Predictions restated (pre-stated, packet GOAL):** at the `:115` timeout — **H-E:** `state_projection` NOT LIVE (or checkpoint trailing the motion report) + `stateChangedCount=0` + both labels INTACT + `commandIssuedCount=0`. **Residual-label arm:** a label wiped (`snzbLabels`/`hueLabels` missing the set value). **Engine stall:** `stateChangedCount>0` + labels intact + `commandIssuedCount=0`. The added `automationTriggeredCount` ([INFO] below) sub-forks the engine-stall arm (triggered-but-no-command vs never-triggered).

**Census + the commit message:** working tree left DIRTY with **exactly 1 M**: `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/HeroLoopHardwareFreeIT.java`. Nothing committed (unchanged law — Nick commits per Part 2). The branch commit message is at `../_scratch/2026-08-07_core_durd-a2-diag_commit-msg.txt` (first line + `Stages exactly 1:` census per the packet; written after the diff existed).

**Deviations, both [INFO]:** (A2-1) `automationTriggeredCount` added beyond the packet's (a)–(f) — one observing-only line that sub-forks the pre-ruled third arm; remove at the hub's word if unwanted. (A2-2) the (a) capture iterates ALL subscriber snapshots rather than picking the five named ids — strictly more contrast data, less code; `registry_projection` rides along, serving the label arm.

**Next act:** Nick's Part 2 (Blocks N-1…N-3: census verify → the `diag/durd-a2-instrumented-capture` branch + push → the DO-NOT-MERGE PR → CI runs → the `test-reports` artifact). Expected: RED at the hero-loop test with the capture block in stdout — that is the instrument working; three greens across two lawful re-runs is itself a datum (environment variance returned). The hub adjudicates per the packet's pre-ruled fork; the branch closes unmerged.

---

## §B′ — M9.5-DURd-B′ DELIVERED: the state_projection liveness window-closure (2026-08-07, the same lane resumed; Nick's v49 beat-1 GO)

**Authority + baseline:** `context/instructions/2026-08-07_M9.5-DURd-Bprime_state-projection-liveness-window-closure_coding-instruction.md`, executed on core **`8955e23`** MAIN (HEAD `8955e23ded8769379333328592b8b4ecb0d4b0b9`, porcelain clean at launch). The `diag/durd-a2-instrumented-capture` branch verified ABSENT from the branch list at launch (disposed per the beat-1 ruling) — nothing recreated, nothing cherry-picked. Line anchors re-verified at execution, ALL EXACT: HeroLoop :481 · RestartHonesty :223 · ZigbeeReplaySafety :225 · RunPipelineReplaySafety :420 · `HomeSynapseCore` :183 (`PROJECTION_SUBSCRIBER_ID = "state_projection"`, private — the string literal used, per instruction). The three identical helper bodies confirmed byte-identical pre-edit; the variant's shape (per-id locals + `failOnSuspended` + the `// ~5s at 20ms` bound comment) exactly as the instruction describes — no `[REVIEW]`-triggering shape drift anywhere.

### Phase A — per-file conflict census: ZERO CONFLICTS, four edits armed

1. **HeroLoopHardwareFreeIT** — the helper is called only from `bootAndAdopt()` (:360), the first statement of all five test methods; every report pump (`reportOccupied` :96/:100, `reportOnOff` :128, `reportColorTemperatureMireds` :203, `reportBrightnessLevel` :286) and every derived-`state_changed` dependence (:150–:154 trigger-cause assert; :299–:308 materialized attributes) sits strictly downstream of the call. No test exercises PRE-LIVE projection semantics anywhere. PASS.
2. **RestartHonestyIT** — called from `boot()` (:160); single test; the file contains NO report pump at all (the in-flight window resolves by clock-stepped timeout BY DESIGN) and no derived-edge dependence (manual triggers only). The mid-test `restartIntegration` restarts the ADAPTER (supervisor-managed), not any bus subscriber — the five modes are untouched by it. PASS.
3. **ZigbeeReplaySafetyIT** (careful read) — called at :105 directly after `core.start()`. The seeded `state_reported` IS replayed pre-LIVE (the test's designed replay-purity semantics), but that window closes at/before the await returns: the REPLAY assertions (:112–:117 — frames empty · `command_result` 0 · `command_dispatched` 1 · `pendingCount` 0) and the LIVE-control frame pin (`hasSize(1)` :134) depend on NO derived `state_changed`; the projection replays the 4-event seed from checkpoint 0 and lawfully reaches LIVE by the await point; its LIVE-reach adds no events (replay re-derivations are NEVER published — INV-WRITER-01, no backfill), so the strengthened gate can neither mask nor alter any assert. PASS.
4. **RunPipelineReplaySafetyTest** (careful read) — called from `seedBootAndAwaitReplayComplete()` (:296), first in all four tests; no rig, no pump — the only `state_reported` is the seeded log row; every REPLAY/TRANSITION assertion is a store-count or recorder read with zero `state_changed` sensitivity (the replay lever is an `EventTrigger` on type `state_reported`; the positive control is `manual`); the projection lawfully reaches LIVE by the await point — hard-gated inside `start()`, exactly as the file's own helper javadoc (:415–:416) has stated all along (source-verified, B′-R1 below). PASS.

### Phase B — the four deltas (exactly the Files table)

The three identical copies each gain the fifth conjunct `&& subscriberMode("state_projection") == SubscriberMode.LIVE`, appended after `integration_supervisor`. The variant gains `SubscriberMode projection = subscriberMode("state_projection");` + `failOnSuspended("state_projection", projection);` + `&& projection == SubscriberMode.LIVE`, each line in its idiom-block position. Poll bound (250), `sleepBriefly()`, the throws, every message: byte-unchanged. Diff shape: 4 files, +10/−4, every hunk the conjunct delta — zero production/assertion/timeout/message bytes (success criterion 5, proven at the diff AND the §4c grep). **Red-first disclosure (addition #18):** green-by-construction at desk as pre-ruled — no red manufactured; the red leg of record stays the 3/3 Aug-7 CI reds (DP-3). B′-R1 sharpens the impossibility: `start()` hard-gates the fifth id LIVE before any helper poll can observe otherwise on a healthy boot.

### Phase C — gates (all GREEN in-session; the CC-lane full-`check` grant; times desk-local UTC−5)

- `:lifecycle:lifecycle:test --rerun` **run 1** (started 20:41:53): BUILD SUCCESSFUL 24s — `compileTestJava`/`testClasses`/`test` EXECUTED (3 executed / 56 up-to-date); XML sum **60 tests / 0 skipped / 0 failures / 0 errors** across 15 classes; result-file mtimes 20:42:18.
- **Run 2** (`--rerun`, started 20:42:41, FINAL bytes): BUILD SUCCESSFUL 15s — `test` re-EXECUTED; XML sum **60/0/0**; HeroLoop + RunPipelineReplaySafety result mtimes 20:42:57. Forced-freshness proven per-task from executed-state lines + result-file mtimes, never the green summary (convention (5)).
- `:lifecycle:lifecycle:spotlessCheck` BUILD SUCCESSFUL — **zero reformats**.
- Full `./gradlew check` BUILD SUCCESSFUL — **156 actionable: 1 executed, 155 up-to-date** — == the 156 record, **zero task-count drift**. The lifecycle `test` leg's up-to-date state is backed by run 2's forced execution on identical final bytes seconds prior ([INFO] B′-I2).
- **§4c ordered grep** over the four touched files (`Instant.now | System.currentTimeMillis | System.nanoTime | Clock.systemUTC | Clock.systemDefaultZone | new Date( | LocalDateTime.now | OffsetDateTime.now | ZonedDateTime.now`): **ZERO hits** — the edit adds no time API, proven not just constructed.
- **Census:** `git --no-optional-locks status --porcelain` = **EXACTLY 4 M** — the Files table's paths, nothing else. **Nothing committed** (standing law); the tree is left dirty at exactly 4 M for the hub's two-layer audit.

### P2 survey (confirmed at the diff)

No enum/registry/event-type/sealed/counted-or-pinned set touched; zero count pins live in the helpers (ZigbeeReplaySafetyIT's `hasSize(1)` and every sibling pin sit in test bodies, unreached by the delta); zero publish sites added/removed/relocated; no module-info/JPMS delta (test-source-only); no ArchUnit rule reaches these helper bodies — zero collisions with the full-`check` criterion, as the instruction's survey stated.

### [REVIEW] B′-R1 — evidence-framing correction for the thread of record (edits unaffected; report-and-proceed)

The premise sentence this lane's own §7 filed and the instruction carries ("nothing orders the projection LIVE before the first report pump") is contradicted at the strict letter at `8955e23`: **`HomeSynapseCore.start()` :589 calls `awaitProjectionLive()` (:1238–:1254) — a HARD gate (1,500 × 20 ms ≈ 30 s; THROWS `IllegalStateException("state projection did not reach LIVE within ~30s during startup")` on exhaustion) — before the four runtime subscribers register and before `start()` returns.** (The RunPipelineReplaySafetyTest helper javadoc :415–:416 has said exactly this all along.) What survives verbatim — and is what this WU closes: the IT helpers' readiness conjunction omitted the id, so nothing RE-confirms projection liveness after `start()` at the tests' own synchronization point.

Walked at source for the regression question: **`SubscriberMode` cannot leave LIVE except to SUSPENDED.** `ReplayDriver`'s overflow-recovery (:127–:133) lives INSIDE the boot `run()` loop (REPLAY-phase only); `InProcessEventBus` :387 is `resume()` (SUSPENDED→REPLAY, manual-only — nothing in these flows calls it); the LIVE delivery path (:299–:306) offers to `pendingPositions` with no overflow-regression arm. Consequences for the traveling caveat:

- The H-E micro-mechanism AS WORDED ("the projection's VT lags the entire boot window", never-yet-LIVE at the pump) is **source-closed at `8955e23`**: a projection that slow reds the BOOT at :589 with a different signature (an `IllegalStateException` during `start()`) than the #206 record (boot completed cleanly; :115 `AssertionError` after 10.344 s).
- The arms coherent with every fact on record: **(a)** a post-boot **LIVE→SUSPENDED trip** of the projection (silent to every pre-B′ assert; the new conjunct — and the variant's `failOnSuspended` — now converts any trip landing at-or-before the helper into an honest readiness red; a trip AFTER the helper but before the pump still surfaces only at the downstream await — the closure is the readiness window, not the whole run), or **(b)** LIVE-mode delivery lag exceeding the :115 10 s ceiling (the edge then publishes LATE, not never — the record captures only the in-window silence, so this is unfalsified in both directions). Each arm carries its own tension (a SUSPENDED trip would likely have logged a supervisor ERROR — the R-1 stdout read adjudicated pipeline-event silence, not the presence/absence of breaker lines; a re-read of the archived #206 stdout for `SUSPENDED`/circuit-breaker lines is a free desk-side discriminator if the hub wants it). Neither arm is instrument-confirmed.
- The standing **UNCONFIRMED-PROBABLE-H-E** caveat therefore travels intact and this filing SHARPENS it; the window-closure's justification is unchanged — the readiness contract now covers all five runtime subscribers. The hub adjudicates the framing at audit; nothing in this finding changes any shipped byte.

### [INFO] items

- **B′-I1:** the variant's throw message (a four-id parenthetical) and its helper javadoc (three ids, plus the "already LIVE (gated inside start())" sentence B′-R1 rests on) are stale-by-enumeration after this WU — left byte-untouched per the instruction's message/scope freeze; pre-existing class (that javadoc already omitted `integration_supervisor`).
- **B′-I2:** the full-`check` freshness shape quoted in the gates record (the up-to-date test leg backed by the immediately-preceding forced run on identical bytes; no production delta exists, so no consumer-suite force is owed — test-source-only, and `NO_DIRECT_TIME_ACCESS` does not reach this module's test sources; the §4c grep holds the convention instead).
- **B′-I3:** conjunct placement = appended fifth, after `integration_supervisor`, in all four files; the variant's local is named `projection` per the instruction's literal.

### WUCP + next act

MODULE_CONTEXT untouched (the exactly-4-M census pin governs; any documentation fold queues hub-side — the `8955e23` F-14-row precedent). coder-handoff DELIVERED entry prepended (Deferred Build Gate: **NO** — the full `check` ran in-session). coder-lessons +1 (the composition-root await-inventory walk + the LIVE-regression census as a pre-filing discipline). Next act: the hub's two-layer audit on this §B′ → the core commit message (post-diff) → Nick's commit + main push → **CI green on that push = the gate of record; it closes `2026-08-07_CI-red_HeroLoop-613_evidence-read_v48-beat-4.md` with the caveat traveling verbatim.** After the audit closes: S-5b STATE-DIALECT P2 + FE-LIVE-V112 (f)/(g) resume their queue positions; the Aug-14 EOD freeze outranks both. Filed 2026-08-08 01:46 UTC.

---

## §B′-AUDIT — the hub's two-layer verdict: ACCEPT (v49 beat 2, 2026-08-08)

**Layer 1 (process): PASS.** Baseline held `8955e23`/main, zero lane commits; census exactly 4 M at porcelain TWICE (the lane's final census + Nick's independent post-`clean.sh` status — identical set); gates as filed (60/0/0 forced-fresh ×2 with executed-state + mtime proof · spotless zero reformats · full `check` 156 actionable zero drift · §4c grep zero hits); Deferred Build Gate **NO** ratified; red-first green-by-construction as pre-ruled (#18/DP-3 — the 3/3 Aug-7 CI reds remain the red leg). Nick's from-clean reproduction (test 59/59 executed; `check` 95 executed / 61 up-to-date, 156 actionable) outranks the in-session runs as freshness evidence and is banked as such.

**Layer 2 (content, adjudicated at the DIFF per D2 — never the report): PASS.** All four hunks are exactly the ordered delta (three appended fifth conjuncts; the variant's `projection` local + `failOnSuspended("state_projection", projection)` + conjunct, each in idiom-block position); the +10/−4 arithmetic reconciles (3 × (+2/−1) + 1 × (+4/−1)); the `"state_projection"` literal is the correct form against the private `PROJECTION_SUBSCRIBER_ID` :183 and consistent with the four existing literals; zero drift into production, assertions, timeouts, or messages. The three [INFO] (B′-I1/I2/I3) are RATIFIED as filed — the stale-by-enumeration throw-message/javadoc stay byte-frozen per the message freeze, queue-eligible for a future WU.

**B′-R1: ACCEPTED** (§4a — evidence-cited, contract untouched, the right kind of pushback). The premise sentence was the hub's own §7 filing carried into the instruction; it is corrected on the record: `start()` :589 hard-gates `awaitProjectionLive()` (:1238–:1254) pre-return, and the FSM walk (LIVE exits only to SUSPENDED; overflow recovery boot-loop-internal; `resume()` manual) is ratified at the cited lines. The edit's value re-derives honestly — a post-boot LIVE→SUSPENDED trip landing at-or-before the helper now reds readiness honestly, and the projection joins the tests' explicit readiness contract; zero shipped bytes affected. The H-E boot-lag arm AS WORDED is source-closed at `8955e23`; the **UNCONFIRMED-PROBABLE-H-E** caveat travels SHARPENED (surviving arms: post-boot SUSPENDED trip · LIVE-mode lag past the 10 s ceiling) into the evidence read's §11 closure.

**Disposition.** Commit **`ca0f41d`** landed (msg post-diff; stages-exactly-4 verified at porcelain) and pushed `8955e23..ca0f41d`; **CI #208 GREEN (3m 53s) + install-smoke #28 GREEN (2m 58s) on `ca0f41d` = THE GATE OF RECORD (law-16 bank)** — `2026-08-07_CI-red_HeroLoop-613_evidence-read_v48-beat-4.md` is **CLOSED at its §11** with the caveat verbatim-sharpened and the :115-recurrence re-open clause standing. The lifecycle MODULE_CONTEXT B′ fold executed hub-side this beat (the F-14-row precedent; its own doc-only core commit, stages exactly 1 M). The A2 disposal is EXECUTED (PR #4 closed unmerged via head-branch deletion; `diag/durd-a2-instrumented-capture` deleted local + origin, was `af82c16`). **M9.5-DURd is CLOSED end-to-end** (DURc → DURd Phase-A STOP → A2 → B′). S-5b STATE-DIALECT P2 + FE-LIVE-V112 (f)/(g) resume their queue positions; the Aug-14 EOD freeze outranks both.
