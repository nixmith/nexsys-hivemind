<!--
file: context/audits/2026-08-09_WCOHERE_navigation-audit_return.md
purpose: The W-COHERE lane return — Charge A (the per-role navigation audit: role × task-class dispatch-path traces, currency/procedure verdicts, gaps at file+line, the ranked top-10 gap list) + Charge B (the living-skills survey: the update metabolism at source, currency self-awareness present/absent, the information-seeking posture, the fences with exhibits, and 2–4 PROPOSED design-input shapes priced under A-14). Findings only — this return proposes; it never performs. Feeds the post-gate truth-map/register WUs, W-SKILLS-3, and the S-10 living-skills adjudication.
audience: the PM mission-control hub (v50+), for two-layer audit; Nick; the S-10 charter session
state-type: audit / lane return
status: FILED 2026-08-09 (ahead of the Aug-12 target; off the Aug-11 09:00 intake window). The lane COMMITTED NOTHING — the ONLY repo-tree write is this file at its named path; the hub audits and orders the commit.
brief: context/instructions/2026-08-08_audit-lane_WCOHERE-navigation-and-living-skills_brief.md
laws-held: READ-ONLY audit (zero edits, zero fixes — every defect is a finding, not a patch) · findings at file+line with verbatim quotes as evidence (labels are claims; quotes are evidence) · pointer-not-copy (this return points at homes; it restates nothing into itself) · no silent caps (§4 names everything sampled and everything skipped) · lock-free-porcelain untouched (no git writes of any kind; HEAD reads only via `git log`).
baseline-at-launch (re-derived 2026-08-09 at the device, per brief §0.6): hivemind `1613205` (at-or-after `5729cdf` ✓ — the v50 beat-1 commit itself) · skills `f0b7a43` ✓ · core `d26777c` ✓ · bench `16e672d` ✓ · docs `a53f474` ✓. All five match or supersede the brief's dispatch baseline. Line numbers below are at these HEADs; line cites shift — re-derive any line you act on.
disclosed-out-of-return-writes: ONE read-workaround temp — `ClaudeFolder/_scratch/WCOHERE_stewardship-delib.tmp.md` (a byte-copy of the hardlink-refused `nexsys-skills/design/2026-06-27_skills-build-triggers-and-recursive-stewardship_deliberation_return.md`, made because the device bridge refuses to stage nlink>1 files — the same class W-SKILLS-2 §6.5 hit on the README). `_scratch` is outside all repos; Nick deletes at leisure. The SOURCE file was not modified (its nlink=2 state is untouched and remains the open §6.5 identification question).
-->

# W-COHERE — Per-Role Navigation Audit + Living-Skills Survey (lane return)

**Lane:** fresh Cowork session, remote (device bridge), read-only. **Date:** 2026-08-09.
**Headline, Charge A:** the dispatch paths that are FRESHLY AUTHORED (orchestrator prompts, lane briefs, operator packets, the three SKILL.md files) are in strong shape — self-contained, state-pointer-clean, verified at recent passes. The dispatch paths that are STANDING FILES are where the archaeology lives: **the mandatory currency instruments themselves (both freshness preflights, the WUCP) validate against spine structures that were retired in June–July**, the three wayfinding registries (canonical-paths, strategy/README, the rationale index) are 1–2.5 months behind the practice they index, and two repo front doors (nexsys-skills/README, nexsys-bench/README) describe worlds that no longer exist. The system's *newest* truth is excellent; its *pointers to* newest truth are the defect class.
**Headline, Charge B:** the update metabolism exists and demonstrably works (four dedicated passes, censuses catching real losses every pass), but it is entirely **event-driven and hub-authored** — nothing in the system *schedules* currency, no instrument watches the wayfinding layer, and the "seek new information" posture exists today only as mission-frame prose in banked prompts. Four PROPOSED shapes are priced in §3.5; S-10 adopts or declines.

---

## §0 Dispatch integrity

- Brief read whole; baseline re-derived at launch (masthead above) — all five repos at-or-after the dispatch baseline; the hivemind HEAD **is** the v50 beat-1 commit that dispatched this lane.
- Named sources (brief §2, items 1–11): **all read**, in order, with two mechanical adaptations disclosed: (a) the giant spine files were read as *frontmatter chain + newest beats + whole-file targeted greps* rather than end-to-end (their line-8 chains are single 47,588- and 57,384-char lines — itself a §1 finding); (b) the hardlinked stewardship-deliberation prior-art file was read via the disclosed `_scratch` byte-copy. Nothing named in §2 was inaccessible.
- Sampling discipline (brief §0.5): one-session, representative-path audit. §4.1 is the full sampled/skipped census — no silent caps.

---

## §1 Charge A — the per-role navigation audit

Row form per brief: **role × task-class → entry point · currency verdict · procedure verdict · gaps**. Verdicts: **CURRENT** (path lands on current truth) · **STALE** (path lands on superseded truth) · **DEAD-END** (path lands nowhere / requires archaeology). Every verdict cites its evidence.

### 1.1 PM (hub)

**A. Session launch / intake-audit.**
- *Entry:* `nexsys-hivemind/START_HERE.md` :16–19 → the newest non-archived `*_PM-mission-control_v*_orchestrator_session_prompt.md` (rule stated at START_HERE :17 and strategic-context-map :235) → the v50 prompt §1 launch reads → spine.
- *Currency:* **CURRENT at the prompt layer** — the v50 prompt is banked per-arc, carries its own "the spine outranks this section" fence (v50 prompt :3, :51), and its launch-read list (§1) is precise (which files, which sections, how many beats).
- *Procedure:* **STALE at the instrument layer — the highest-severity finding in this audit.** The mandatory first act (`project-manager/references/freshness-preflight.md`, mandated at SKILL.md :84) validates against spine structures that no longer exist:
  - Check 1 (:35, :37) reads a `Last Sync` timestamp and matches it against "the most recent entry in the Recent Session Log." **PROJECT_SNAPSHOT.md contains neither**: whole-file grep for `Last Sync` = 0 hits; the `## Recent Session Log` heading survives only as a tombstone (:311–313 — "The masthead beats above ARE the session record").
  - Check 3 (:51) compares git HEAD to "the `Latest Commit` field in PROJECT_SNAPSHOT.md" — whole-file grep = 0 hits.
  - Check 2 (:43–47) requires a current-ISO-week plan in `planning/weeks/` — the newest file is `2026-W31_jul27-aug02.md`; no W32 file exists as of 2026-08-09, so Check 2 has returned STALE-as-written for a week. Either the weekly-plan practice was retired by the beat cadence (nothing on disk says so) or the hivemind is genuinely missing an artifact its instrument demands.
  - The preflight's own masthead (:8) is `last-verified: 2026-06-07` — the currency instrument has had no currency pass of its own through four skills passes that each updated its Check 9.
  A fresh PM running the preflight literally gets false-STALE/CONFLICTED on 3 of 11 checks and must improvise the mapping onto the beat-pair spine — improvisation at the exact point the system most wants determinism.
- *Gaps:* the spine files do not describe their own reading order. PROJECT_SNAPSHOT's two "newest" signals disagree — the frontmatter chain (:8) is at v50 beat 1 while the newest masthead pair in the body (:13) is v48 beat 9, six beats older, and nothing tells a reader the pair region lags the chain. An obsolete chain segment ("this file keeps the newest FOUR block-pairs," a v42-era clause inside :8) contradicts the 57 pairs present. The "read the newest 2 pairs / newest 5 beats" rule exists ONLY inside banked prompts (e.g., pm-handoff :301 quoting the v47 prompt), not in either spine file's own header. And both line-8 chains are single mega-lines (47,588 / 57,384 chars) that defeat line-based readers — pm-handoff :17 itself flags the rotation as urgent.

**B. Brief-authoring / dispatch.**
- *Entry:* the practiced form — recent lane briefs in `context/instructions/` (W-MARKET, L-E, L-F, this brief).
- *Currency + procedure:* **CURRENT, and the strongest pattern in the system.** Every 2026-08 brief is self-contained: return path named with the on-disk-verification law (W-MARKET brief :13; L-E brief :13), baseline pinned with a re-derive instruction (:20 both), named sources in read order (§2 both), skill requirement stated (:4 both). The operator-packet class (PM SKILL arc-discipline 35) extends the same form to helper sessions.
- *Gaps:* (i) the brief FORM lives nowhere as a template/procedure — it is re-derived from the previous brief each time (works while the hub carries it; fails the fresh-lanes test the day a different session must author one). (ii) The return-intake half is undocumented: who commits a read-only lane's return file, where the intake/two-layer-audit procedure lives, and how the hub discovers a `2026-08-XX`-dated return filename are all practice-only (the H3 return :3 shows the answer — "FILED BY THE HUB verbatim-as-received" — recorded per-incident, never as procedure).

**C. Spine-write + commit-order.**
- *Entry:* env-model §§10–12 + PM SKILL arc-disciplines 29–33 + D14.
- *Currency + procedure:* **CURRENT** — `cowork-environment-model.md` is the best-maintained standing file in the fleet (last-verified 2026-08-03 :8; §11 carries the ghost-commit/OVERTAKEN-ORDER fold; §12 addenda dated 2026-07-31). The census-exact commit-order form is fully specified (§10 :62–72).
- *Gaps:* none material. This is the exemplar the other file classes should be held to.

**D. WUCP Phase 2 closeout.**
- *Entry:* PM CLAUDE.md :31 → `context/protocols/work-unit-completion-protocol.md`.
- *Currency:* **STALE — the most-cited procedure is the least-maintained.** Masthead `last-verified: 2026-05-20` (:8); body "Last updated 2026-04-11" (:16). Specific defects at file+line:
  - Step 2 template pointer (:189) → `nexsys-hivemind/context/traceability/TEMPLATE.md`, which PM CLAUDE.md :35 says was **removed in the 2026-04-11 overhaul** — the protocol contradicts the CLAUDE.md that routes to it.
  - Step 6 (:231, :233) orders updates to the "Recent session log" and "Last sync timestamp" — retired structures (above).
  - Step 7 (:237) orders weekly-plan updates — see Check 2.
  - Step 10 (:271–278) names **two** mirror pairs — the third (FE) pair was RULED into Check 9 on 2026-08-06 (R-2; preflight :122–125) and never reached the WUCP.
  - Step 11 (:294) routes `[FORESIGHT-NOTE]` to `coder-handoff.md §Foresight Notes` — **no such section exists** in the live coder-handoff (whole-file grep: 0 hits).
  - The worked examples (:402, :423) say the preflight has "10 checks" — it has 11 (its §2 heading), and the coder preflight's masthead (:8) records that this exact copied-count class was already fixed ONCE, elsewhere, on 2026-07-28.
  - The protocol is written to a two-repo world (hivemind + core throughout; grep for bench/docs/frontend: 0 hits) while the operative model is five repos (PM SKILL D10).
- *Gaps:* PM CLAUDE.md :49 ("PROJECT_SNAPSHOT incl. its Recent-Session-Log row … weekly-plan incl. its Current-state footer") and `references/review-and-quality.md` :235 (same clause) carry the same retired-structure references into the skill tree itself — the one fact-contradicted line the reference scan found (it survived v44's "§1–§6 byte-unchanged" because that pass's charter never opened it).

**E. Wayfinding registries (used by every PM task class).**
- `context/canonical-paths.md` — **STALE, and actively misdirecting.** last-verified 2026-05-21 (:8). Its "Directories that NO LONGER EXIST (catch stale references)" section (:47–51) lists `context/governance/` and `context/research/` as removed 2026-04-11 — **both exist and are load-bearing today** (`governance/project-instructions.md`; 13 files in `research/` including the A-14 and physics-seed charter inputs this arc runs on). The registry built to catch stale references is the stale reference. It also omits every directory class minted since May: `process/` (the cold-boot layer), `strategy/` + its three program subtrees, `pre-verifications/`, `programs/`, `instructions/` (present only via the research-brief rule :39).
- `context/strategic-context-map.md` — **CURRENT in §2/§6–§8, STALE in its own quick-reference:** §3 (:297) still says "~94 invariants across ~18 categories" while §2A (:96) of the same file says 174/52 (register truth at docs `a53f474`: 175/53) — a pointer-not-copy violation living inside the map that teaches pointer-not-copy. §7 coder step 4 (:408) says Phase-3 instructions "live in the PM→Coder handoff" while the practiced home is `context/instructions/*_coding-instruction.md`.
- `governance/project-instructions.md` — **DEAD-END masquerading as CURRENT.** Status "CURRENT" (:6), last-verified 2026-05-27 (:5). Teaches the three-surface model (:35), "Claude Code — transition in progress" (:38), watermark "AMD-01 through AMD-45" (:17), docs "01–14" (:17) — vs the five-repo lane model, AMD-99+, 18 Locked docs. It is the ONLY file in `context/governance/` and the closest thing to a governance front door; a session that boots on it inherits May.

### 1.2 Coder

**A. Implement-from-instruction.**
- *Entry:* launch line (START_HERE :25) → named `context/instructions/*_coding-instruction.md` + pre-verification + `/nexsys-coder`.
- *Currency:* **CURRENT.** `coder/SKILL.md` last-verified 2026-08-06 (:13); instructions are per-WU fresh; `coder-handoff.md` is newest-first with the refuse-to-close pointer live (:15, :21) and the Deferred-Gate line present (:17). The return-on-disk rule landed in §7a (:322).
- *Procedure:* **CURRENT** for the coding loop (references table :52–57; MODULE_CONTEXT law :59–69; WUCP Phase 1 in §7a).
- *Gaps:* (i) coder CLAUDE.md :23/:89 order a read of "the current week's plan" — unsatisfiable this week (no W32 file). (ii) The coder freshness preflight inherits the PM instrument's retired-structure defects: Check 1 (:36) matches a "Last Completed Milestone" field across both files; Check 2 (:44) reads "the `Latest Commit` field in PROJECT_SNAPSHOT.md" — neither field exists in the beat-pair snapshot (grep: 0 hits). (iii) MODULE_CONTEXT currency was NOT verified at source by this audit (out of sampled scope, §4.1) — the coder path's rank-1 anchors are unaudited here.

**B. Test-first.**
- *Entry + procedure:* **CURRENT** — testing-standards.md + the §4c/Clock enforcement-reach chain, which the v44 pass verified and corrected at its one shipping home (v44 return §4.4 — the exhibit that a fifth uncensused copy carried a FALSE enforcement claim into every instruction).
- *Currency:* the four coder references carry `last-verified: 2026-06-07 against commit 8028337` (63 days) — **adjudicated lawfully-left** by W-SKILLS-2 (return §1 stale-sweep: immutable historical verification anchors) but see §3.2: age-as-signal is thereby dead, and nothing else watches these files.
- *Gap (cross-file contradiction of record):* `project-manager/references/repo-state-protocol.md` :83 — "only **`web-ui/dashboard`** remains a stub … no compiled Java" presented as scaffold status — vs coder SKILL.md :151 "**`web-ui/dashboard`** (populated across the FE-0…FE-1b beats … the frontend lane maintains it)". Same fact, two homes, opposite readings; the PM side is the stale one (masthead :8 = 2026-06-07).

**C. WUCP Phase 1 / handoff channels.**
- *Procedure:* **CURRENT** in the skill (§7a) but the message-protocol layer it hands into is **half-dead**: `[FORESIGHT-NOTE]` routes to a section that does not exist (1.1-D above); `context/open-questions.md` has had no write since 2026-06-13 (masthead :8 = 2026-06-04; OQ-05-07 open since 2026-05-28, the unnumbered NO_DIRECT_TIME_ACCESS reach item still "remains OPEN" at :74) while PM CLAUDE.md :119 and WUCP Step 11 still gate every instruction on sweeping it; `cross-agent-notes.md` census reads "ACTIVE: 0 as of 2026-07-27" (:21). The channels exist on paper, are mandated by procedure, and have been superseded in practice by the beat spine — with no file recording the supersession.

### 1.3 Frontend

**A. Component work against the frozen read-API.**
- *Entry:* dispatch prompt (per START_HERE :26) → `/nexsys-frontend` → FE preflight → the `truth:` block sources.
- *Currency:* **CURRENT — the cleanest skill in the fleet.** v1.10, 2026-08-06 (:12); the `truth:`/`verify:` frontmatter (:14–25) is the state-pointer law made structural; §4a–§4e carry per-line re-derive fences (:147 "Re-derive the currently-deployed build … at preflight, never from this line"); the v44 SK-INV-01 re-grounding removed the volatile particulars (v44 return §2.3, §4.3).
- *Procedure:* **CURRENT** (references table :38–44; §6 work-unit order; §7 lane return).
- *Gaps:* (i) the FE preflight labels its mirror check "(Check-9)" while being Check 7 of its own 8 (freshness-preflight.md :60 vs §2 heading :25) — inherited PM numbering that a fresh lane must decode. (ii) The FE reference mastheads carry NO `last-verified:` field at all — all six say "v1.0 2026-06-27" while bodies carry 2026-07-01 and 2026-07-22 folds (contract-consumer :28; explainability :26/:30; preflight :56 citing the 2026-07-22 brand package) — the one skill family whose masthead convention cannot express currency.

**B. The skills-repo front door.**
- **STALE to the point of self-denial.** `nexsys-skills/README.md` :6 — "SKELETON ONLY … **ZERO skill content is built**"; :14 — "The two live role skills (`nexsys-coder`, `nexsys-project-manager`)"; :35 — Tier-1 roster "project-manager · coder · [session-conductor]". The repo has carried the **third live role skill** at `orchestrators/nexsys-frontend/` since 2026-06-27 — 40 KB, 39 censused laws, edited by three W-SKILLS passes. The README was even touched on 2026-08-06 (the SD-5 citation, chartered citation-only per the W-SKILLS-2 brief §1.1) without the Phase-0/zero-content claims being openable. A fresh frontend lane reading its own repo's front door is told the skill it just loaded does not exist.
- *Adjacent:* `sync/README.md` :7 accurately says the sync script is unbuilt and Nick's manual sync governs — but the mirror's actual host location is written NOWHERE outside session-relative topology (`$SESSION_ROOT/mnt/.claude/skills/…`, preflight Check 9 :111–128). A remote session can name the mirror; no file lets anyone else find it on Nick's disk. (Check 9's own refinement :136–148 handles the remote case correctly — "STALE (mirror unverified from here)" — this gap is about the map, not the check.)

### 1.4 Bench-operator

**A. Nightly digest glance.**
- *Entry:* **none exists.** The playbook masthead routes "the PM hub … and the Coder" (:3–4), not the operator; `nexsys-bench/README.md` has no masthead at all and orients to "Phase 0 (Pi → durable bench host…)" (:37). The operator's de-facto entry is whatever §8-form packet the hub last issued.
- *Currency:* **the protocol the operator runs EVERY MORNING is written nowhere as one procedure.** Its fragments: the 8/9 bar at H3 package :42 (a dated packet, not a standing file); bundle-stamp-first at playbook :18 (§1 addendum); the digest surface of record at playbook :101; the one-line bank form ONLY in pm-handoff beats (outside the bench corpus entirely). The adopted glance protocol (bundle-stamp first · survival gate · latency→C4 · one-line bank · divergence⇒evidence-read) is quoted in the v50 prompt (:57) — i.e., its most complete statement lives in a file that rotates to archive every arc.
- *Procedure verdict:* **DEAD-END for a fresh operator session** — recoverable only from the newest packet + prompt archaeology.
- *Exhibit that this costs real defects:* the H3 package shipped a **9/9 bar that was wrong BY CONSTRUCTION** (hue-online is a static constant, never probed — operator return :51–52; codified as a hub defect at playbook :102). Scattered procedure knowledge produced a defective dispatch from the hub itself.

**B. Pi trip.**
- *Entry:* the §8-form operator package — **CURRENT and excellent as a class** (H3 package: goal/done-when :11–13, anti-actions :15, full-path tool law :17, return path :6). The §8 contract itself (playbook :54–105) is the second-best procedure document in the fleet after env-model.
- *Gaps (fresh-operator knowledge not on disk anywhere):* the Pi's `~/hs-bench/` tree has no map (referenced piecemeal at package :114/:129, playbook :101, B3 instruction :48–52); `infrastructure-map.md` — the chartered "one-page map" (:3), status CURRENT, last-verified 2026-07-03 (:6–7) — **omits the entire nightly stack** (no `~/hs-bench/`, no systemd user units, no nightly.sh, no digests); the as-installed scheduler branch (systemd vs cron, DP-5) is recorded only in a pm-handoff beat; the Pi journal rule (`journalctl --user` DEAD; use `systemctl --user status`) lives only in the v50 prompt (:49) pending its named W-SKILLS-3 harvest slot.

**C. Evidence read.**
- *Procedure:* **CURRENT** — instrument-first, predictions-filed-first, per-hypothesis form is well-carried (PM arc-disciplines 20/28; playbook §§1, 8; the B3-night exhibits).
- *Gap:* `nexsys-bench/README.md` (:17–25 layout listing neither `tools/` nor `scenarios/`) predates the runner/nightly era entirely — the repo's front door describes a repo that stopped existing ~six weeks ago (B3 instruction :38–40 gates on both directories; the charter :20 describes the nightly as B3-future while it has run since 2026-07-31).

### 1.5 Executive / strategy

**A. Charter input / positioning.**
- *Entry:* **none exists.** No launch line, skill, or START_HERE branch names an executive path; `working-with-nick.md` :4 and `decision-rationale-index.md` :4 both defer to "the PM boot sequence." The only file that says "Start here" for anything strategic is `project-instructions.md` :20 — pointing at a May-era navigation index. (W-EXEC is chartered post-gate and its absence is RULED — this row records the interim shape, not a defect in the ruling.)
- *Currency of the strategy wayfinding layer:* **STALE at every level.**
  - `context/strategy/README.md` :8 last-verified 2026-05-21; indexes exactly 5 files (:19–23). Missing: `Substrate_Thesis_v0.md` and the north star (both LOAD-BEARING — the PM skill's strategy-layer :53 and both research briefs bind to them), the moat-watch standing directive (same directory), and all three program subtrees (`fusion-program/` · `brand-program/` · `counsel-package/`). The stale five-file count propagated into `decision-rationale-index.md` :13.
  - `decision-rationale-index.md` — the file whose masthead says read it "before touching any settled decision" (:4) — last-verified 2026-07-06 (:8), newest ruling row 2026-07-08 (:64). **A month of load-bearing rulings has no row**: R-1..R-6 (v44 beat 5), the HOLD ruling (v49 beat 3), A-14, R-A(a), HA-5, the D5-as-standing-law promotion, the menu-word dispositions. An executive defending settled ground from the index alone would miss every August fence.
- *Procedure:* the D5 language law — which binds "every sentence" of strategy text (v50 prompt :36 law 14; moat-watch :3; both research briefs :14) — **has no findable definition home.** Its definitional carrier is cited as "(D5, v40 beat 2)" (PM SKILL :53), and the v40 beats were rotated to `archive/pm-handoff-beats-v38-v40-rotated-2026-07-31.md` — the law of record for all strategy language lives in an archive file reachable only by archaeology. (The sibling label collision — strategy-layer "D5" vs durable-discipline D5 — is already flagged at W-SKILLS-2 §6.7, still open.)

**B. Research-lane dispatch (the executive's instrument).**
- **CURRENT — the model form** (see 1.1-B). Two additional gaps beyond 1.1-B's: (i) both briefs' return paths are `2026-08-XX_…` with XX = completion day — the hub cannot know the expected path in advance, and no discovery procedure is written for the return-on-disk verification it must perform; (ii) the counsel gate that both briefs bind competitor claims to (W-MARKET :18; L-E :18) is cited but never located — its home (the brand-program voice/messaging platform §6) is findable only via the W-SKILLS-2 return §6.3, an audit file.

---

## §2 Charge A close — THE RANKED TOP-10 GAP LIST

Ranked by (severity of wrong dispatch) × (frequency the path is walked). Each row: the gap → the evidence anchor → what the post-gate truth-map/register WU should do with it.

1. **Both freshness preflights validate against retired spine structures.** PM preflight :35/:37 (`Last Sync`, "Recent Session Log"), :43–47 (weekly plan; newest = W31), :51 (`Latest Commit`); coder preflight :36 ("Last Completed Milestone"), :44 (`Latest Commit`) — all 0-hit against the live PROJECT_SNAPSHOT (beat-pair structure; session log retired at :311–313). The mandatory first instrument of every PM and coder session returns false verdicts as written. → W-SKILLS-3: re-ground Checks 1–3 (PM) and 1–2 (coder) to the frontmatter-chain/beat-pair spine; rule on the weekly-plan practice (retire the check or resume the artifact).
2. **The WUCP — the most-cited procedure — is 2.5 months stale on a five-front drift.** :189 (removed template), :231/:233 (retired structures), :237 (weekly plan), :271–278 (two mirror pairs vs the ruled three), :294 (nonexistent §Foresight Notes), :402/:423 ("10 checks"), two-repo worldview throughout. → its own chartered currency pass (it is not a skill file, so no W-SKILLS pass has ever owned it — that ownership gap is the root cause).
3. **`canonical-paths.md` actively misdirects.** :47–51 declares `context/governance/` and `context/research/` nonexistent while both are load-bearing; last-verified 2026-05-21; omits every post-May directory class. It is the natural HOME of the chartered role-indexed truth-map — and currently the wrongest file in the wayfinding layer. → rebuild as (or replace with) the truth-map; make it the registry the preflight's Check 10 actually sweeps.
4. **The spine does not describe its own reading order, and its two newest-truth signals disagree.** Snapshot chain (:8, v50) vs newest body pair (:13, v48 beat 9, six beats behind, unmarked); the obsolete "newest FOUR block-pairs" clause inside :8; launch-read counts living only in banked prompts (pm-handoff :301); 47.6/57.4 KB single-line chains defeating line-based readers (self-flagged at pm-handoff :17). → the truth-map WU + the already-named chain-rotation beat: give each spine file a 5-line self-describing header (ordering, where newest lives, how many to read, where archives are).
5. **The executive path has no entry point and its wayfinding layer is frozen in May–early-July.** strategy/README :8/:19–23 (5 files, 2026-05-21); decision-rationale-index :8/:64 (newest row 2026-07-08; August rulings absent); project-instructions.md :6 "CURRENT" teaching the three-surface model (:35), AMD-45 (:17). → truth-map executive section + a rationale-index catch-up append + retire-or-restamp project-instructions.md (it is one `status:` line away from harmless).
6. **The bench-operator's daily protocol exists only as fragments, and both bench front doors are pre-era.** Digest-glance protocol scattered (H3 package :42, playbook :18/:101, one-line bank only in pm-handoff beats; fullest statement in the rotating v50 prompt :57); nexsys-bench/README :17–25/:37 (no tools/, no scenarios/, "Phase 0"); infrastructure-map :3/:6 CURRENT-stamped yet omitting the whole nightly stack and `~/hs-bench/`. The 9/9-bar defect (playbook :102) is the recorded cost. → an edit-procedure-register row per bench file class + ONE standing digest-glance procedure file in the bench corpus + a README/infra-map refresh.
7. **The message-protocol layer is mandated but half-dead.** `[FORESIGHT-NOTE]` → nonexistent section (WUCP :294; PM CLAUDE.md :117; canonical-paths :45); open-questions.md dormant since 2026-06-13 while PM CLAUDE.md :119 gates instructions on it; cross-agent-notes ACTIVE:0 since 2026-07-27. → rule once: retire/repoint the channels (the beat spine absorbed them) or revive them; either way, make the procedure files agree with the ruling.
8. **`nexsys-skills/README.md` denies its own live content.** :6 "ZERO skill content", :14 "two live role skills", :35 tier-1 roster omitting nexsys-frontend — vs the third live skill resident since 2026-06-27. Plus: the mirror's host location is written nowhere in absolute terms (Check 9 gives session-relative topology only). → a chartered README currency touch (the W-SKILLS-2 precedent shows one-item charters work) + one line in the truth-map naming the mirror's host home.
9. **Named laws with no findable definition home.** D5 defined only in a rotated archive (PM SKILL :53 → v40 beat 2 → `archive/pm-handoff-beats-v38-v40-rotated-2026-07-31.md`); "the returns-to-audits law," "law 15/16," the L1/L3 auth tiers, "the constants law" — all cited at point of use, defined only in prompt/spine prose that rotates. → the edit-procedure register should carry a LAW INDEX: every named law → its one definitional home in a non-rotating file.
10. **Copied-state contradictions still live inside the always-loaded layer.** strategic-context-map :297 (~94/~18) vs :96 (174/52; register now 175/53); repo-state-protocol :83 ("dashboard remains a stub") vs coder SKILL :151 (populated, FE-maintained); review-and-quality :235 + PM CLAUDE.md :49 (Recent-Session-Log row, weekly-plan footer). Each is the F2 class the truth-hierarchy law predicts, surviving because no pass's charter has opened those exact lines. → fold as named items into the W-SKILLS-3 harvest list (they are one-line deltas each, census-safe).

**Cross-cutting observation for the truth-map WU:** the defect distribution is not random. Files that are *re-authored per use* (prompts, briefs, packets) are clean; files that are *standing and owned* by a pass discipline (the three SKILL.md trees, env-model, the playbook) are clean; files that are *standing and owned by nobody* (WUCP, canonical-paths, the two preflights' spine-facing checks, strategy/README, project-instructions, both repo READMEs, infrastructure-map, open-questions) are where 9 of the 10 gaps live. The register's most valuable column is therefore **owner + which pass class re-verifies this file** — the truth-map should assign every wayfinding file to a maintenance loop, because every file outside a loop drifted.

---

## §3 Charge B — the living-skills survey (findings + PROPOSED design inputs; designs nothing in-place)

### 3.1 The current update metabolism, mapped at source

- **The pass mechanism (the only skill-write path):** a dedicated FRESH lane, chartered by a hub-authored brief with a writable-tree fence and STOP gates (W-SKILLS-2 brief §§0–3), executing **delta-edit-never-rewrite under a rule census N-in/N-out** (v44 brief §0 law 2), adjudicated by a **two-layer hub audit at host bytes** (2026-08-06 audit :14–16), landed by **hub-ordered census-exact commits**, and reaching sessions only after **Nick's three-pair mirror re-sync** (audit §4; skills README :60 — "Editing a skill source leaves the mirror STALE until the sync runs"). The hub never edits its own operating rules in-session (v50 prompt :19 — re-ratified v49 beat 3).
- **Cadence, observed from the mastheads (PM SKILL :14 pass-history + :13):** 2026-07-07 · 07-08 · 07-11 · 07-18 · 07-19 · 07-26 (in-hub beats) → 2026-07-28 · 07-30 · 08-03 · 08-06 (dedicated lanes). Two structural facts: the passes moved OUT of the hub on 2026-07-28 and never went back; and every pass was **event-triggered** (a harvest accumulating, a lull opening — v44 brief :8 "the Aug-3–4 Pelton lull"), never scheduled. There is no standing cadence rule anywhere on disk.
- **The harvest path today:** incident → pm-handoff beat (exhibit) → lessons append (`pm-lessons`/`coder-lessons`, append-only, oldest-first) → **hub re-derives a named harvest list into the next brief's §2** (both briefs) → fold as indexed pointer-form rules → census → audit → commit → mirror sync. The re-derivation step is the manual bottleneck: each brief's harvest list is reconstructed from spine archaeology at authoring time. The one queue mechanism that exists — "queued for the NEXT skills touch per R-2's own wording" — worked exactly once and the return called it out as the thing needing a mechanism (W-SKILLS-2 return §5.1: "the named-queue mechanism … is what needs the chartered lane, and this pass is the proof it functions").
- **What the metabolism does NOT cover:** everything §2 rows 1–3, 5–7 name. The pass discipline's writable-tree fences are exactly right for safety, but they also mean the WUCP, the preflights' spine-facing checks, canonical-paths, and the strategy wayfinding layer have never been inside any pass's charter. The living-skills mechanism, whatever shape S-10 adopts, must own a **file-class list**, not a skill list — the rot concentrated precisely in the files outside the current fence.

### 3.2 Currency self-awareness — present and absent

**Present on disk today:**
- The **state-pointer law** in all three SKILL.md mastheads (PM :54, coder :38, FE `truth:`/`verify:` :14–25) — the skills declare their own state claims stale-by-construction; the FE skill adds per-line fences (:147, :163).
- The **last-verified frontmatter chain** convention on every hivemind file — a machine-greppable currency stamp (and the spine chains carry per-beat dates).
- The **freshness preflights** (11/7/8 checks) — session-start drift detection… whose spine-facing checks are themselves stale (§2 row 1). **The currency instrument has no currency instrument** — the sharpest self-awareness gap in the system.
- **Check 9 + the three-pair mirror discipline** — including the honest "STALE (mirror unverified from here)" remote posture (PM preflight :136–148) and the STALE-by-construction post-edit state (v44 return §8).
- The **closed-loop token instrument** across passes (W-SKILLS-2 return §5.2 — before-values reproduce v44's after-values to the token) — proof the record can carry measured continuity when the instrument is named.
**Absent / silent:**
- **Nothing watches the wayfinding layer.** Check 10 verifies that map-cited paths EXIST (:152–156), not that any claim is current; no check touches canonical-paths, WUCP, strategy/README, the rationale index, the repo READMEs, or infrastructure-map. Every §2 gap sat invisible to every instrument.
- **Age is no longer a usable staleness signal** — the `2026-06-07` reference cohort was adjudicated lawfully-left (W-SKILLS-2 §1 stale-sweep: historical anchors), which is correct per-file and simultaneously means a scan of last-verified dates cannot distinguish "still true" from "never re-checked." The FE reference convention (no last-verified field at all; v1.0 mastheads over 2026-07-22 bodies) can't even express the question.
- **Currency signals that already exist on disk and are consulted by nothing:** the spine frontmatter chains (newest-segment date per file); `git log -1 -- <path>` per file (already the RULED recency truth for MODULE_CONTEXTs — strategic-context-map :155); the dated audits corpus (a complete, machine-listable record of what was verified when); the `planning/weeks/` gap itself (a one-`ls` drift detector nobody runs); `MANIFEST.yaml` (semver+SHA provenance — a stub since Phase 0); and the PARKED rot-sentinel taxonomy (`FRESH | STALE | DRIFTED | OVERSIZE | CONFUSABLE`, harness/README :41, design v1 :238) — a designed vocabulary with no running instrument.

### 3.3 The information-seeking posture — where a standing "seek" instruction could live lawfully

The constraint pair: skills carry NO project state (state-pointer law), and strategy text is posture-and-verified-fact only (D5). A "constantly seek new information" mechanism therefore cannot put feeds, watch-results, or market claims INTO a skill — but it can put **standing pointers to hub-owned seek registries** into the launch path. The lawful homes found at source:

- **The moat-watch pattern is the template** (`context/strategy/2026-08-06_company-scale-moat-watch_standing-directive.md`): a STANDING DIRECTIVE file — rank-3, hub-written, D5-held — with the skill/prompt layer carrying only "every intake gets the quiet question" (v50 prompt :18). The mission/market posture entry (PM SKILL strategy-layer (3), landed 2026-08-06 as H-7) is the existing skill-side hook: "stay constantly market-aware" is already POSTURE in the always-loaded layer; what's missing is the instrument list it should point at.
- **Launch-read hooks:** each role's §0/preflight is the natural carrier for ONE pointer line ("when a research window opens, consult the seek list at <home>") — a pointer, not state; census-safe (+1 rule each).
- **Standing research-candidate queues have precedent:** `planning/research-agenda.md` (cited at canonical-paths :40), `planning/2026-06-13_side-research-candidates_CORE-DOCS.md`, and the research-fleet posture itself (v50 prompt :17 — every wait-state buys research; every extract lands with a NAMED CONSUMER). The fleet law already supplies the consumption discipline a seek list needs.
- **Role-relevant external instruments already named in the record** (seed entries, not proposals): the HA release watch (FE SKILL :103 — "the watch continues on future HA releases"); vendor skill/context-engineering guidance re-verification per model release (the 5,000/25,000-token budgets are [VENDOR] facts that move — research return §1.1–1.2, §4.1); dependabot/CVE feeds (law 15; CRA Art-14 wait-state, 2026-09-11); TSDR cadence; Matter/Thread + local-first competitor currency (the W-MARKET charge classes); the ~30%-tokenizer-shift class of platform changes (skills-pass-2 return §96–97).

### 3.4 The fences any automation must survive — and what breaks if bypassed (from the recorded exhibits)

1. **Fresh-lanes-only (the hub never edits its own rules in-session).** Bypass ⇒ a context-deep session rewrites the laws that govern it, with no independent census and degraded late-context attention. Exhibits: the ruling itself re-ratified v49 beat 3 (v50 prompt :19); the v44 brief's own rationale (:3 — "the context-deep hub does not run skills passes in-session"); externally, the multi-turn-drift numbers the research return verified (§2.4 — 39% average drop; "when LLMs take a wrong turn… they get lost and do not recover"). A "self-updating skill" that edits itself mid-session is this bypass in its purest form.
2. **The rule census (delta-edit-never-rewrite, N-in/N-out).** Bypass ⇒ silent law deletion. Exhibits — every pass caught real losses: v44 §2.1 **rescued items 29–32** (four durable rules invisible to two prior passes, living as changelog prose); v44 §6.5's catch list on the pass's OWN work (a deleted G-2 law restored; item-32's clause drifted to half its meaning; a §4d self-contradiction; D12/D16 qualifiers dropped; an arithmetically impossible census in the first draft); the 07-28 pass's disclosed tense-fix (return :78) showing even a "compression" carries semantic edits the census must see. Independent validation: ACE/ICLR-2026 — "iterative rewriting erodes details over time" (research return §2.3). **Any automated updater must emit a census or it is a context-collapse machine.**
3. **The two-layer audit (labels are claims; quotes are evidence).** Bypass ⇒ plausible-but-false returns bank into the spine. Exhibits: the W-SKILLS-2 audit re-verified all nine after-bytes and every claimed edit at host bytes before commit orders (audit :16); the frontend census number of record is **the auditor's, not the lane's** (v44 §6.5); the D18 origin exhibits (the M9.3 "only 4 public types" refutation; the paraphrased-mandate phantom-DEFECT class). An automation whose output is trusted at label is the same defect at machine speed.
4. **Check 9 / the three-pair mirror discipline.** Bypass ⇒ source and mirror fork silently — sessions load law that no longer matches the record, or (worse) an automation writes the mirror directly and the writable source stops being the source. Exhibits: the README **hardlink** (nlink=2, W-SKILLS-2 §6.5 — an edit would have propagated to an unknown twin; the bridge refused, the lane broke the link byte-identically; the twin's identity is STILL open); the third-location remote-cache confusion that required its own refinement (preflight :136–148, 2026-07-27); the FE mirror running **unguarded by any preflight** for six weeks until R-2 (v44 return §4.2/§6.2). Any self-update mechanism must write SOURCE only and leave the mirror to the ruled sync.
5. **The truth hierarchy / state-pointer law.** Bypass (an updater that "keeps the skill current" by writing fresh state INTO it) ⇒ recreates the F2 rot class at machine speed, with fresher-looking timestamps making the copies MORE trusted. Exhibits: the retired hand-maintained Status column (map :123 — drifted to M3-era claims AND missing a module); the map §6 copied block that drifted for two months (:383); the FE "Pelton results ~Aug-5" staleness bomb, two days from false when caught (v44 §4.3); above all the **Clock paste-block** (v44 §4.4): five homes, four corrected in June, the fifth — the only copy that ships to the Coder — carried a FALSE enforcement claim for seven weeks. Multi-home volatile facts are where automation would do the most damage fastest.
6. **The gate-side fences the designed automation already carries** (must survive into any adopted shape): SK-INV-08 — stewardship auto-edits are human-gated (design v1 :135; recursive-error-amplification is the named risk, :150); the harness activation gate — "no half-working automation reporting noise … the daily check *reports*; Nick acts" (harness/README :37, :48); and SK-INV-05 — no skill change past a red eval (design v1 :132) — currently unenforceable because zero evals exist (research return §3F), which is itself a fence-shaped hole: **any auto-edit proposal sequenced before an eval harness is sequenced wrong.**

**The closed path, restated per the brief's hazard line:** no mechanism, under any shape below, has the hub editing its own operating rules in-session. Standing law; nothing in this survey's evidence weakens it — the census-catch record (fence 2) is affirmative evidence FOR it.

### 3.5 Design-input options — PROPOSED ONLY, priced under A-14 (S-10 adopts or declines)

Pricing basis: A-14 — the 15 h/wk weekend-anchored FLOOR; "AI-assisted / fully-automated deep internet research" named as reliable even in thin weekday windows (A-14 :15, :22); every commitment must survive a weekends-only week (the holdable-posture law, :20).

**PROPOSED Shape 1 — the standing currency-check beat class ("the pulse").**
A small, read-only, chartered lane (W-COHERE's own form, miniaturized) run on a FIXED cadence — weekly, weekend-anchored — executing a WRITTEN check registry over exactly the file class §2 shows drifts: the wayfinding layer + the procedure layer that no W-SKILLS pass owns (canonical-paths, WUCP, both preflights' spine-facing checks, strategy/README, rationale-index currency, both repo READMEs, infrastructure-map, the message-protocol channels, the weeks/ practice). Output: a one-screen drift report, findings-at-file+line, fixes chartered separately (W-SKILLS-N or a hygiene lane picks them up). This generalizes the freshness-preflight pattern from spine-vs-code drift to **procedure-vs-practice drift** — the exact class every instrument currently misses (§3.2).
*Cost:* ~1 attended h/wk (dispatch + intake; the lane itself is unattended) — fits the floor with margin. *Risk:* LOW — read-only; the failure mode is report fatigue, bounded by the one-screen form. *Fences leaned on:* fresh-lanes (it IS one), return-on-disk, two-layer audit. *This is also the self-awareness mechanism:* the system learns it is stale on a cadence instead of at collision.

**PROPOSED Shape 2 — the standing harvest ledger (the skills-harvest wait-state, accrued per arc).**
One hub-written, append-only file (suggested home: `context/lessons/skills-harvest-ledger.md` — rank 3, rotating like the other lessons files) where every beat that mints a rule, exhibit, or currency defect appends ONE line: rule name · carrier pointer · destination file. W-SKILLS-N briefs then cite ledger sections instead of re-deriving harvest lists from spine archaeology (the current §2-authoring bottleneck, §3.1), and un-folded items become visible wait-states instead of memories. Precedent: R-2's named queue worked and the W-SKILLS-2 return asked for exactly this generalization (§5.1); the v50 prompt already carries a proto-ledger inline (:59 — the W-SKILLS-3 harvest list).
*Cost:* one line per minting beat (hub-side minutes); NEGATIVE net cost at brief-authoring time. *Risk:* LOW-MED — one more hot-path file (the env-model §8 token tax); bounded by one-line entries + rotation. *Fences leaned on:* the census (the ledger becomes the in-census source), chat-is-not-a-storage-tier (it is that law applied to harvests), pointer-not-copy (name + pointer only).

**PROPOSED Shape 3 — per-role seek lists (the "constantly seek new information" half, made lawful).**
One standing, role-sectioned registry (suggested home: `context/research/seek-lists.md`, or per-role files beside the moat-watch) of named EXTERNAL instruments: instrument · trigger condition · named consumer. Seed entries already in the record (§3.3): HA releases → the FE positioning watch; vendor skills/context-engineering docs per model release → the W-SKILLS pass class (the 5k/25k budget facts are verified-but-movable [VENDOR] numbers); dependabot/CVE + CRA Art-14 dates → the vuln-response loop; Matter/Thread + local-first competitor currency → the quarterly competitive-watch lane the fusion program already charters; the tokenizer/platform-shift class → budget re-measurement. Role skills gain ONE pointer line each in their launch path ("a research window consults the seek list") — posture + pointer, zero state, D5-held, census-safe (+1 rule per skill, folded by W-SKILLS-3 alongside its existing harvest).
*Cost:* ~2–3 h to author once (post-gate); maintenance rides existing beats; consumption is exactly the wait-state/thin-weekday capacity A-14 names, spent only when a lull exists (the research-fleet law already governs intake). *Risk:* MED — scope creep into standing obligations that outrun the floor; mitigations: every entry carries a trigger CONDITION (not a cadence), the list length is an S-10-owned number, and entries without a named consumer are unlawful by the fleet's own extract law.

**PROPOSED Shape 4 — rot-sentinel Rung-0, descoped to report-only.**
The PARKED daily-check design (harness/README :35–53), stripped to what needs no harness: a Nick-fired (not scheduled) read-only sweep producing the one-screen health board — mirror-sync state (the three pairs), hot-path file sizes (env-model §8), a dead-pointer scan over canonical-paths + map §9 cites, and a last-verified-vs-threshold table. Commodity grep/diff only; the Gradle drift-check stays unbuilt; auto-edits stay forbidden (SK-INV-08); the activation gate (:53) is honored by keeping it report-only and human-fired.
*Cost:* one chartered build lane (a weekend slot), then ~15 min/wk intake. *Risk:* MED-HIGH relative to Shapes 1–3 — it is the first automation in a layer with ZERO evals (SK-INV-05 unenforceable today), and the harness README's own warning applies: half-working automation reporting noise trains the operator to ignore the board, which is worse than no board. *Sequencing input:* only after S-10 prices the eval question — or explicitly as the eval fixture FOR that question.

**Interaction note (PROPOSED):** Shapes 1+2 are near-free, reversible, and directly consume this return's §2 list; Shape 3 is the directive's "constantly seek" clause realized inside D5 and the state-pointer law; Shape 4 is the only one that automates, and the fences (§3.4, item 6) say it goes last. All four keep every write human-ordered and every skill edit inside the existing pass discipline — the survey found no evidence supporting any loosening of it, and one strong exhibit per fence for keeping it (§3.4).

---

## §4 Return integrity — the self-audit

### 4.1 Sampled vs skipped (no silent caps)

**Read whole, by this session directly:** the brief · truth-hierarchy · canonical-paths · strategic-context-map · both START_HEREs · v50 orchestrator prompt · PM SKILL.md + CLAUDE.md + freshness-preflight + repo-state-protocol · coder SKILL.md + freshness-preflight · FE SKILL.md · nexsys-skills README + MANIFEST + sync/ + stewardship/ + harness/ READMEs · cowork-environment-model · A-14 · both W-SKILLS briefs · the v44 return · the W-SKILLS-2 return + its hub audit · the skills-context-engineering research return.
**Read whole or to stated depth, by four read-only subagents (their line cites spot-verified at the bytes for every load-bearing claim in §§1–2):** WUCP (whole) · cross-agent-notes (whole) · open-questions (whole) · playbook (whole) · H3 package (whole) · bench README/charter (whole) · infrastructure-map (whole) · working-with-nick (whole) · moat-watch (whole) · strategy README (whole) · W-MARKET brief (whole) · FE preflight + FE CLAUDE.md + coder CLAUDE.md (whole) · PROJECT_SNAPSHOT (top 250 lines + whole-file greps) · pm-handoff (top 400 + targeted probes at :301/:335/:551 + whole-file greps) · coder-handoff (top 150 + whole-file greps) · pm-lessons/coder-lessons (tops + structure) · decision-rationale-index (first 120) · L-E brief (first 80) · B3 instruction (first 105) · H3 operator return (first 120) · project-instructions (whole) · physics seed (first 60) · 12 role-reference mastheads + a 10-pattern stale-signal grep across all 15 skill-family files.
**Greped only:** the two July skills-pass returns (loss-catch/census exhibits) · design v1 (SK-INV/rot-sentinel anchors) · the stewardship deliberation return (section skeleton).
**Skipped entirely, named:** the whole `homesynapse-core-docs` tree (docs-repo wayfinding is UNAUDITED — a hostile reviewer starts here; see 4.3) · `homesynapse-core` source + all MODULE_CONTEXT bodies (the coder path's rank-1 anchors — currency asserted by the skills, not verified here) · the spine bodies beyond the stated tops · all `archive/` trees · `programs/matter-design/` · the counsel/fusion/brand program bodies · `benchmarks/` · `project-knowledge/` bodies · bench `tools/`/`scenarios/`/`harness/` bodies · the planning backlog body · `_archive`/`_scratch` (except my own temp) · the `.docx` strategy files.

### 4.2 Sources: all 11 brief-named sources read; ONE access workaround disclosed (the hardlinked stewardship deliberation → `_scratch` byte-copy, masthead). Nothing was inaccessible outright. Line numbers cited at the baseline HEADs; they shift — re-derive before editing against them.

### 4.3 The three weakest findings in this return

1. **§1.1-A's "false-STALE" claim about preflight Check 2 assumes the weekly-plan practice was retired rather than lapsed.** If Nick simply hasn't written W32 yet (a freeze-week choice), the check is doing its job and the finding inverts from "stale instrument" to "current instrument, missing artifact." The file+line facts stand either way; the CHARACTERIZATION needs one word from Nick.
2. **The §2 ranking is judgment, not measurement.** Severity × frequency was scored from one session's reading, not from incident counts; a defensible re-ranking could swap rows 4–7 freely. The row CONTENTS are evidence-anchored; the ORDER is the audit's least-defensible layer.
3. **Charge B's §3.1 cadence claim ("nothing schedules currency") is an absence-of-evidence claim** across a corpus I sampled, not exhausted — a standing cadence rule could exist in an unread planning/program file. I found none in the launch paths any role walks, which is the operative sense, but the universal phrasing outruns the sampling.

### 4.4 What a hostile reviewer attacks first

- **The unaudited docs repo.** Charge A promises "per repo" coverage and this return's docs-repo evidence is entirely secondhand (other files' claims about watermark/register state). If the docs repo's own wayfinding (its READMEs, the register's §17 self-description) is as stale as the hivemind's, the top-10 is missing rows — deliberately traded under A-14 sizing and disclosed here rather than papered over.
- **Line-cite fragility.** Six files in §§1–2 are cited at subagent-reported line numbers; I byte-verified every load-bearing one (§4.1) but not every peripheral one. Any consumer editing against a cite must re-derive it (the L-E brief's own law, adopted in the masthead).
- **The §2 cross-cutting claim ("everything outside a maintenance loop drifted") is a clean narrative a reviewer should probe for survivorship bias** — I did not audit clean-and-unowned files that would falsify it (candidates: `working-with-nick.md`, `ci-as-gate-of-record.md`, both adjudicated current-enough in passing). Two counter-examples would soften it from law to tendency.
- **Charge B's shapes are priced in attended hours only** — token/context cost per shape (the env-model §8 tax of one more hot-path file; the +1-rule census cost per skill of Shape 3) is named but not measured. The v44 return's lesson (+710 tokens for a "compression" pass) says measure before adopting; S-10 should demand those numbers at adoption time, not take these estimates.
