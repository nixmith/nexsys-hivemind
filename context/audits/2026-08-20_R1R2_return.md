<!--
file: context/audits/2026-08-20_R1R2_return.md
purpose: Coder-lane return for WU R-1/R-2 — the packaged-runtime write-path assert (red-first) + the jlink fix (full-lib jdeps + FLOOR jdk.jfr + build-time floor-presence assert) + the CI-twins sqlite3 step. Instruction: context/instructions/2026-08-20_R1-R2_packaged-write-path_red-first-and-jlink-fix_coding-instruction.md.
audience: the hub (audit), Nick (commit + the §OP Saturday card legs).
status: DELIVERED-UNCOMMITTED at core c091f7c + exactly 4 M. The lane commits NOTHING; CI on Nick's push = the gate of record (law 16). The RED/GREEN hardware legs are §OP (Sat 2026-08-22); intake slots in §6.
filed: 2026-08-20 (America/Chicago), R-1/R-2 Coder lane (host-CC, per D12).
-->

# WU R-1/R-2 Return — Packaged Write-Path Assert + jlink Fix

**Baseline verified at launch:** core HEAD `c091f7c`, porcelain clean except the known `?? _scratch/`. Twins byte-identical pre-edit; all four edit targets `nlink=1` (no hardlink break needed). No prior R1/R2 work in tree or log (duplicate-dispatch check clean; no existing `*R1R2*` return).

## §0 P1–P9 re-verification (all rows verified at c091f7c BEFORE implementing; zero STOP-grade mismatches)

| # | Verdict | What the bytes said at my checkout |
|---|---|---|
| P1 | ✓ | `HomeSynapseCore.java:485` exactly: `BusMetrics jfrMetrics = BusMetrics.jfr();` — immediately after `PersistenceFactory.start(...)` (:482–:483), passed into the canonical `new InProcessEventBus(...)` (:486–:493) and `DerivedWriteRateLimit` (:495). `noop()` wiring sites: the package-private convenience ctor (`InProcessEventBus.java:93`, javadoc'd "for tests that don't exercise M3.3 paths"), testFixtures factory, IT harness, and (per the event-bus MODULE_CONTEXT `NoopBusMetrics` row) `InMemoryEventBus` — the hub row's phrasing is accurate; production wires `jfr()` unconditionally. |
| P2 | ✓ | `git grep -ln "jdk.jfr\|BusMetricsJfr" -- "*.java"` nets **10** files at my checkout: the hub's EIGHT main-source consumers (BusMetrics, BusMetricsJfr, SIX `Bus*Event` JFR classes — six confirmed, matching "the bytes say six today") **plus** `module-info.java` (`requires jdk.jfr`, non-transitive, platform module — the exact JPMS fact that makes the jlink omission fatal) and `BusMetricsJfrTest`. Count delta = enumeration scope (main consumers vs all matches), not drift. The fix covers the full set regardless (full-lib jdeps ∪ FLOOR). |
| P3 | ✓ | Pre-edit: the :75–:77 comment claimed "the full jar set"; :79–:83 passed ONE root (`homesynapse-app*.jar` via `find … | head -1`) with `--class-path "${IMAGE}/lib/*"`; FLOOR at :85 had NO `jdk.jfr`. Exact line numbers matched the row. Now remediated by R-2. |
| P4 | ✓ | Checks 1–8 exactly as enumerated (install · active · health probe · token · auth · dashboard · stop · uninstall); nothing probed persistence or the publish path. The new check landed as **check 4** directly after the health probe (see §5 D-1 for the numbering reading). |
| P5 | ✓ | `common.sh`: `HS_DATA=/var/lib/homesynapse`; **`HS_DB_FILE="${HS_DATA}/data/homesynapse-events.db"`** (common.sh:34) — pinned FROM SOURCE, not guessed: `Main.java:61` `baseDir.resolve("data").resolve("homesynapse-events.db")` with `HOMESYNAPSE_HOME=${HS_DATA}` (common.sh:32). Token at `${HS_CONFIG_DIR}/initial_api_token`; stdout log `/var/log/homesynapse-stdout.log` (dump_logs + install.sh:87). The probe binds `${HS_DB_FILE}` directly. |
| P6 | ✓ | `CREATE TABLE IF NOT EXISTS events` at V001:28 (table name `events`). The boot-time emission: **the integration-lifecycle publish set** — `publishStarted(:1065)`/`markFailed(:851)` → `publishLifecycle(:1094)` → `publishRoot`; the H3 journal PROVES these are CALLED six times per packaged boot even hardware-less (coordinator unplugged + `PrivateDevices=yes` the whole rep — the launch attempt is unconditional). On a healthy runtime they persist (persist precedes notify — see P7). `update-smoke.sh:8` corroborates at the letter: "boot writes lifecycle events, so the store is non-empty". **≥1-row-on-fresh-install form SOUND — no STOP.** (Nuance: `SystemStartedEvent` exists and is registered in `CORE_PRODUCTION_EVENT_CLASSES` but NOTHING publishes it today — the "`system_started`-class emission" is the integration-lifecycle set. §5 D-5b.) sqlite3 precedented twice: the workflow update-smoke step (apt idiom) + `update-smoke.sh:35` count() (`sqlite3 "file:${HS_DB_FILE}?mode=ro" 'SELECT COUNT(*) FROM events;'` — reused verbatim). |
| P7 | ✓ | `publishLifecycle` (StandardIntegrationSupervisor:1094–:1104) catches ONLY `SequenceConflictException` — a `NoClassDefFoundError` escapes uncaught and kills the integration VT ("Exception in thread \"integration-zigbee-0\""); boot continues (INV-RF-01), health goes green — post-health/async confirmed. **The REAL signature, pinned from the H3 journal (6×/boot):** `java.lang.NoClassDefFoundError: jdk/jfr/Event` + `Caused by: java.lang.ClassNotFoundException: jdk.jfr.Event`, frames at :1099/:1065/:851 and `NotifyingEventPublisher.publishRoot:56`. The probe greps the §OP-identical triple `NoClassDefFoundError|jdk.jfr|BusMetrics` case-insensitively — the deliberately-unescaped dot nets BOTH the `jdk/jfr` (NCDFE message) and `jdk.jfr` (ClassNotFound/module) spellings; the bare `NoClassDefFoundError` arm is the generic second net. **Sharpening (evidence, travels to §OP):** `NotifyingEventPublisher.publishRoot` persists at :55 and notifies at :56 — the H3 trace places the throw AT :56, i.e. AFTER the durable append. The held card's DB therefore likely HAS rows, and the RED most likely shows at probe 2 — the "AND/OR" second arm §OP already prices, and the exact sub-case the D-6 line names probe 2 for. F-23's "cannot persist events" holds at the SYSTEM level: the notify path dies on every publish, subscribers/projections never advance (H3 10.2: `viewPosition: 0`), and each publishing VT is killed mid-flight. |
| P8 | ✓ | `diff` PRE-edit: byte-identical (including the WIRING-SEAM header — present in both copies). Hardlink pre-check: `stat -c %h` = **1** on all four targets (no FE-twin-style nlink=2; no break needed). Both twins edited identically; POST-edit diff re-run: byte-identical. |
| P9 | — | Operator-side (the §OP card sitting). No coder action; the block ships as authored. Desk cross-check only: `distribution/deb/build-deb.sh` exists, and §OP's `select count(*) from events` matches the V001 table name. |

**Post-edit re-read:** P1/P2/P5/P6/P7 facts untouched by the edits; P3/P4 described the defect state the edits remediate (the instruction's intent); P8 re-verified on the final bytes. Still true throughout.

## §1 Per-file diffs (before → after per hunk)

**`distribution/smoke/run-smoke.sh` (M; +55/−7 class):**
- Hunk 1 (after check 3's `fi`): NEW check block `╔══ 4. EVENT WRITE PATH IS LIVE ═…╗` (banner byte-matched to the file's 79-display-column style). Head comment names the F-23 class ("assert the property, not the proxy") + the D-6 line verbatim (§2). **Probe 1:** sqlite3-absent → `bad` (honest tooling fail, never a skip); DB-absent at `${HS_DB_FILE}` → `bad` + dump_logs; count via the update-smoke idiom `sqlite3 "file:${HS_DB_FILE}?mode=ro" 'SELECT COUNT(*) FROM events;'`; case-armed: empty/non-numeric → `bad` unreadable (+dump), `0` → `bad` "event write path DEAD" (+dump), ≥1 → `ok` **printing the observed count** (arc-19 positive evidence). Restart-surviving total-count form, no position-delta (per instruction "do not over-engineer"). **Probe 2:** `THROW_RE='NoClassDefFoundError|jdk.jfr|BusMetrics'` (the §OP-identical triple; pinned-signature comment cites the H3 journal lines); scans `journalctl -u ${HS_UNIT} -b` (systemd-guarded) + `/var/log/homesynapse-stdout.log` (existence-guarded) via `grep -icE`, summing hits AND counting scanned sources; zero sources → `bad` (the probe cannot run — no vacuous arm), zero hits → `ok` **printing the grep'd-zero line + source count**, else `bad` with hit count + dump_logs. All arms count via `ok`/`bad` — never exits early (`set -uo pipefail` respected; no `die`).
- Hunks 2–5: digit-only banner renumbers 5→6 (AUTH), 6→7 (DASHBOARD), 7→8 (STOP), 8→9 (UNINSTALL) — zero logic/text changes beyond the digit (see §5 D-1). Verdict block untouched ("gate #4" there is the go/no-go register, not check numbering).

**`distribution/image/build-image.sh` (M; +38/−14 class):**
- Hunk 1 (:75–:87 region): the §2-comment REWRITTEN to describe the actual invocation (R-2.4 — names the F-23 single-root defect and the resolve-vs-analyse distinction the H3 return established); the jdeps invocation replaced: `find "${IMAGE}/lib" -maxdepth 1 -name '*.jar' -print0 | sort -z | xargs -0 "${JDEPS}" --print-module-deps --ignore-missing-deps --multi-release "${JFEATURE}" --class-path "${IMAGE}/lib/*"` — **every bundled jar a root** (R-2.1; flags kept; `--class-path` retained for cross-jar resolution; deterministic sort per the file's own :69 idiom; a multi-invocation xargs split would emit multiple lines and the existing `tr/sed/sort -u/paste` union normalizes multi-line input by construction). FLOOR comment gains the JFR-registration rationale; **FLOOR gains `jdk.jfr`** (R-2.2, appended). Union with FLOOR byte-unchanged.
- Hunk 2 (after the jlink `du` log): NEW block `── 2b. Floor-presence assert ──` (R-2.3): `LINKED="$(runtime/bin/java --list-modules | cut -d@ -f1)" || die`; loops every `${ADD_MODULES}` entry through `grep -qxF` (fixed-string whole-line — no regex-dot false matches); collects ALL absentees; `die "jlinked runtime is MISSING requested module(s):${MISSING}"` printing the names; green path logs the module count (anti-vacuous positive line). Carries the mandated passes-but-false comment verbatim (§2).

**`.github/workflows/install-smoke.yml` + `distribution/ci/install-smoke.yml` (M+M, byte-identical):** one NEW step "Install smoke prerequisites (sqlite3)" between "Assemble .deb" and "Install-smoke" — `sudo apt-get update -qq && sudo apt-get install -y -qq sqlite3`, the update-smoke step's own idiom verbatim. **Duplicate, not move** (§5 D-2): update-smoke's inline install stays for step self-sufficiency (the re-install is an apt no-op); the new step's comment says so. No other workflow change; the `distribution/**` paths-trigger already covers this WU.

## §2 The D-6 / passes-but-false lines as shipped (verbatim, comment-wrapped)

run-smoke.sh check 4 (4-line wrap, words verbatim):
> `# passes-but-false input: an artifact that persists events but throws only on swallowed JFR paths (probe 2 exists for it); a runtime whose boot legitimately persists zero rows (P6 refutes this at source — if P6 fails, this check's form is wrong: STOP).`

build-image.sh block 2b (3-line wrap, words verbatim):
> `# passes-but-false input: a module listed but broken at link — bounded by jlink's own --dedup-legal-notices=error-if-not-same-content and the R-1 runtime probe downstream.`

## §3 Desk gates (in-lane; NO gradle — zero Java, per the instruction)

- `bash -n` **CLEAN** on both touched scripts (run twice — after the initial landing and after the probe-2 hardening edit, on the final bytes).
- shellcheck: **not installed on this host** — flagged, not silently skipped. Mitigation: CI's own "Static lint" step re-runs `bash -n` over every `distribution/**/*.sh` on the push.
- **Fragment dry-run** (scratchpad, mocked inputs — the new logic paths exercised in isolation): probe-1 case arms behave exactly (empty→unreadable-bad, `abc`→unreadable-bad, `0`→dead-bad, `42`→ok-with-count, embedded-newline→unreadable-bad); probe-2 arithmetic sums empty/0/6 correctly under `${VAR:-0}`; the `grep -icE … || true` count idiom returns 0 on a clean log and 3 on a pasted H3-signature sample; the floor-presence loop passes complete sets (printing the count) and dies naming exactly the missing modules on an incomplete set.
- Twins `diff` post-edit: **byte-identical**.
- Banner geometry: the new banner is 79 display columns (175 bytes — identical to the same-ASCII-length check 2), aligned with all nine.
- **Red-first accounting (#18, disclosed as ruled):** the CI leg is green-by-construction (assert+fix land in one push); the RED instrument is the held card (H3 Stage-2, `0.1.0+gd26777c`) — §OP records RED before the fix reaches it, then GREEN on the rebuilt artifact. That ⏺ pair is this WU's red-first evidence of record. **#22 live-wire: N/A-disclosed** — no FE surface; the packaged-runtime probes are the real-wire exercise.

## §4 Census at porcelain (lock-free)

```
 M .github/workflows/install-smoke.yml
 M distribution/ci/install-smoke.yml
 M distribution/image/build-image.sh
 M distribution/smoke/run-smoke.sh
?? _scratch/
```
**Exactly 4 M** = the instruction's §4 table, nothing else dirty. `?? _scratch/` is the instruction-acknowledged pre-existing untracked entry (present at launch, untouched by this lane). Nothing staged, nothing committed (the lane commits NOTHING).

## §5 Deviations / disclosures (evidence over instruction — none silent)

- **D-1 [INFO] Check numbering:** "after the health-probe check" + "next number" (instruction §2 / P4) are only jointly coherent as **insert-at-position-4 + renumber 4–8→5–9** — a `9` between `3` and `5`, or an appended block violating the stated position, would each break one constraint. Shipped: the new check is 4; five banners changed by ONE DIGIT each, zero other bytes in those checks (the delta-only fence read as "no logic/idiom changes to unrelated checks", which holds). If the hub intended the literal-9 reading, the retune is one digit + four reverts.
- **D-2 [INFO] Twins sqlite3 — duplicate, not move:** additive-only diff; the update-smoke step stays standalone-correct; second install is an apt no-op. "Move/duplicate" were both offered; duplicate chosen for the smaller blast radius.
- **D-3 [INFO] Probe-2 zero-source guard (a hair beyond the letter):** if NEITHER the journal NOR the stdout log is scannable, the check `bad`s ("the throw probe cannot run") instead of passing on an empty sum — the arc-17 anti-vacuous floor. Unreachable in both real modes today (systemd → journal; no-systemd → install.sh:87 creates the stdout redirect) — it exists so the probe can never rot into silence.
- **D-4 [INFO] Probe-1 tooling posture:** sqlite3-absent is an honest `bad`, never a skip (a gate that skips its probe when the tool is missing is the F-23 false-confidence class itself). CI guarantees the tool via the twins step; the Pi gets it via §OP block 1's lawful additive install.
- **D-5 Evidence sharpenings (for the hub's fold; no action):** **(a)** persist-precedes-notify (NotifyingEventPublisher :55/:56) sharpens the §OP RED prediction — most likely rows-PRESENT + ~6 throw signatures (probe-2 red), the disjunction's second arm; both-clean remains the F-23-refutation STOP as authored. **(b)** `SystemStartedEvent` is minted + registered but unpublished today — the boot-row guarantee rides the integration-lifecycle publishes (H3-proven called hardware-less) + update-smoke.sh:8's standing comment; if a future change makes integration launch conditional, P6's guarantee needs re-derivation (noted for R-3's rig). **(c)** P2 grep nets 10 `.java` at my checkout vs the row's "EIGHT" — enumeration scope (main consumers vs +module-info+test), not drift. **(d)** shellcheck absent locally (§3) — CI's static-lint re-covers parse-cleanliness on the push.
- **Pushback: none required** — no instruction pin contradicted the bytes anywhere it mattered; the two ambiguities (D-1, D-2) were resolvable inside the instruction's own text and are disclosed, not silently adapted.

## §6 §OP ⏺ intake slots (the hub folds Nick's pastes; the block itself ships as authored in the instruction — unmodified)

**Filed pre-read (the F-23 prediction, sharpened per D-5a):** Block 1 expects EITHER zero/absent rows OR nonzero throw hits — most likely **rows present AND `grep -ciE` ≥ 6** on the held card. BOTH clean = F-23's upper bound REFUTED at the instrument → STOP, ⏺, hub adjudicates (a finding, not a failure).

- **⏺ Block 1 — RED on the held card (`0.1.0+gd26777c`):** _[intake slot — systemctl status head · data/ listing · per-DB events count · grep -ciE count · grep head -5]_
- **⏺ Block 2 — rebuilt artifact + GREEN:** _[intake slot — `git log --oneline -1` at the Pi checkout · build-deb's jlink `--add-modules` line (jdk.jfr MUST appear) · the floor-presence assert line · run-smoke verdict incl. check 4's two positive-evidence lines]_
- **⏺ Block 3 — restore:** _[intake slot — held card OUT/labeled (it is R-3/R-4's rig) · bench card boot · `bench.sh scenario boot-health` [PASS] floor]_

## §7 Next-WU pointer

**R-3 / PKG-E2E-1** (weekend 2, MINTED DISTINCT): PrivateDevices loosening · the rehearsal rig (the held card, preserved by §OP block 3) · integration-under-load. The D-1 claim fences stand verbatim until R-4; nothing in THIS WU lifted any claim — `distribution/README.md:117` untouched, all claim surfaces untouched.

---

### WUCP Phase 1 checklist
- [x] MODULE_CONTEXT.md updates: **N/A-disclosed** — zero Java modules touched (scripts + workflow YAML only; `distribution/` carries no MODULE_CONTEXT.md).
- [x] `context/handoff/coder-handoff.md` updated — R1R2 DELIVERED entry prepended, **Deferred Build Gate spelled** (no in-session gradle by design; the gate of record = install-smoke CI green on Nick's push of these 4 files at/atop `c091f7c`).
- [x] `context/lessons/coder-lessons.md` — one entry appended (log-signature probes: pin from the real incident journal; unescaped-dot dual-spelling net; the zero-source scan guard).
- [x] Cross-agent notes: none needed (this return is the hub's intake).
- [x] Next WU identified (§7 — refuse-to-close satisfied).
