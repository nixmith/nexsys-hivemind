<!--
file: context/audits/2026-08-21_RS4-FE_intake_two-layer-audit_v55-beat-3.md
purpose: THE TWO-LAYER INTAKE AUDIT of the two lane returns that landed on 2026-08-21 evening (both dispatched at v55 beat 2, both returned inside ~90 minutes on Fable 5 Extra): the RS-4 CRA readiness memo (`2026-08-21_RS4_cra-readiness-memo_return.md`) and the FE fixture/F-S3 return (`2026-08-21_FE-lane_never-triggered-fixture_F-S3_return.md`). Layer 1 = presence + shape at the named paths (law 37); layer 2 = independent adversarial agents — the FE diff verified file-by-file against HEAD exports and the sitting record's literal body; the RS-4 citations RE-FETCHED at EUR-Lex / labeled mirrors / the staged repo files — every agent verdict adjudicated by the hub at the primary text (D18). One record for two returns (the LE-LF precedent).
audience: the hub (the verdicts of record); Nick (the rulings + the strategic findings); the next FE touch + the R-6/R-8 lane (the folded rows).
state-type: audit record.
filed: 2026-08-21 (v55 hub, beat 3 — filing-day convention, America/Chicago).
-->

# RS-4 + FE intake — the two-layer audit (v55 beat 3)

## §1 Layer 1 — presence and shape

Both returns EXIST on disk at their named paths (`git --no-optional-locks status --porcelain` on the hivemind: exactly the two `??` lines), filing-day dated 2026-08-21, one file each, uncommitted as the briefs require. The uploaded copies Nick pasted are byte-identical to the on-disk files (sizes 114,592 B and 42,103 B; md5 matched). The FE lane's core working tree carries EXACTLY the 7 dashboard files its census names (3 M + 4 A; zero `.java`; core `.git/index.lock` absent — the lane's disclosed move of a stale lock to `_scratch/_to_delete/` verified at the instrument).

## §2 The FE return — layer 2 (agent a578e93c; 26 tool uses; the diff, the record literal, and the detector all re-executed)

**Verdict: ACCEPT.** 1 DEFECT (citation-only) · 8 NOTE · 10 CONFIRMED · 4 UNVERIFIABLE-HERE.

- **The fixture is VERBATIM** — `JSON.stringify(fixture) === JSON.stringify(recordLiteral)` with the record's bold markers stripped; key order identical at every level and equal to Core's `LinkedHashMap` put order (`GetNonFiringEndpoint.java:93–:99`, `:111–:121`). **Byte counts re-derived: 395 B (08-16) / 396 B (08-20)** — the return's claim holds to the byte; the +1 is the sixth digit of `viewPosition`.
- The provenance header mirrors the 08-16 form; `not captured` where the record is silent; nothing claimed beyond the record (one trimmed quotation inside quotation marks — cosmetic).
- `fixtures.stability.test.ts`: 14 `it(` sites (7 + 7); the three mandated arms present (key sets recursive incl. envelope; null-ness; the value-diff set EXACTLY `{data.automationId, meta.viewPosition, meta.timestamp}` via order-sensitive `toEqual` on sorted arrays); teeth on spread copies asserting the NAMED throw; the two stricter pins (key order, byte size) separately named. The agent PORTED the detector and reproduced the return's mutated-fixture red transcript line for line (9 failed / 5 passed; mutated size 406).
- `client.ts`: exactly three hunks; the predicate `status === 404 && FROZEN_UNBUILT_PATHS.has(path) && mediaType(contentType) === 'application/json'`; the set holds ONLY `/api/v1/events` (exact match); the verdict never reads `res.body`; the minted copy carries no product name. `EventsView.tsx`: ONE code-line change (`slug === 'not-found'` → `isUnservedEndpoint`). The 13-test suite = 6 · 4 · 3 as described. The headers-only fixture is labeled as claimed (status 404 · `application/json` · `content-length` "159" · body `null`; request path `/api/v1/events` with `query: { sort: 'DESC', limit: 50 }` in the RawRequest form).
- Every Core citation quote-verified: the STRONG `"<runId>"` ETag (`GetRunCausalChainEndpoint.java:100–:102`); `EndpointResponses.problem()` :40–:43 writes `ctx.status` + `ctx.json` and NO content type; weak position-keyed ETags at `GetNonFiringEndpoint.java:106` and `ListRunsEndpoint.java:118`.
- **The one DEFECT:** the return's "`isUnservedEndpoint404` … (`:110` post-change)" — the function is at `client.ts:119` (`:110` is inside the `FROZEN_UNBUILT_PATHS` doc comment). Citation-only; no code impact; noted here, the return stands unedited.
- **NOTES folded into the dashboard MODULE_CONTEXT (hub-owned, the F-14-row precedent — 4 rows):** (i) the two-capture stability detector + **its array blind spot** (`walk()` does not descend arrays; two captures both carrying an array at the same path with different contents diff SILENT — material before the `/state` reuse the return's §11 proposes; fix = descend by index or compare the array leaf via `JSON.stringify`); (ii) the run-detail `Updated` stamp ages BY CONSTRUCTION (strong content-constant ETag → 304 → cached `meta`), lists reset via weak ETags, entities set none; (iii) the Activity teaching state keyed on the wire discriminator, the path-set law (must shrink on the mock→real swap), the missing unit pin of `api.listEvents`'s path literal (a cheap strengthener); (iv) the Core-side content-type observation (F-V2, below). The "decided BEFORE any body read" wording is loose (the body is materialized by the transport; the verdict never CONSULTS it) — the return's own second clause is the precise one.

**Rulings (hub, H10-form, recorded here):**
- **F-S3 — the relabel `As of <time> · live` (date-qualified via `clockTimeWithDate`; the trailing word = the poll PHASE label the footer already owns): ADOPTED as a RIDER on the next FE touch (the FE-STATE-DIALECT WU per the return's §11), together with the tooltip/aria-description mechanism note and the 304-path cursor refresh from `X-HomeSynapse-View-Position`. Not a standalone lane; not this commit.** The mechanism answer (viewPosition-driven refetch + a strong ETag + the client's cached-meta 304 path) is banked as the F-S3 closure — the v54 hub's fetch-once hypothesis was wrong in mechanism and right in effect.
- **F-V2 MINTED (Core conformance):** `EndpointResponses.problem()` emits endpoint-level problems as `application/json` while Doc 09 §3.8 + `contract.ts:58` promise `application/problem+json`; only the exception path (`RestFilters.java:551`) conforms. No FE impact (the client keys on the body slug). **Routed as rider R-C on the R-6/R-8 WU** (rest-api is open there): IN if the `EndpointContext` seam already carries a content-type setter (P11), else OUT → its own micro-WU. The FE's NEW-7 discriminator is unaffected either way.
- The `ListEntitiesEndpoint` no-ETag observation → the R-10 consistency docket (a NOTE, not a defect).
- The H8 accounting stands as written: the Activity teaching card is REPO-COMPLETE, LIVE-VERIFICATION PENDING THE DEPLOY; the WhyNot surface's VERIFIED-LIVE status (2026-08-20) is untouched (fixture + tests only).

**Census for the core order:** the lane's 7 (3 M + 4 A) + the hub's MODULE_CONTEXT fold (1 M) = **8**.

## §3 The RS-4 return — layer 2 (agent a0f4324495ea23849; 63 tool uses; EUR-Lex + two/three labeled mirrors per item + the staged repo files)

**Verdict: ACCEPT, with six NOTES folded as the hub's addendum (the return file stands unedited — pointer-not-copy).** 0 DEFECT · 6 NOTE · the headline readings CONFIRMED as sound readings of the text (not legal advice).

Source-of-record status (the same limit the return's §9 discloses): EUR-Lex 32024R2847 served only the preamble to the fetch tools, so recitals (15)/(18) are verified AT THE OJ TEXT and the articles/annexes on ≥2 agreeing labeled mirrors; Regulation (EU) 2026/1744 (the Digital Omnibus on AI — recital (40): Annex III high-risk obligations 2027-12-02; Annex I embedded 2028-08-02; in force 2026-07-27) and Implementing Regulation (EU) 2025/2392 (the Annex III product descriptions — items 11 "Operating systems", 16 "Smart home general purpose virtual assistants", 17 "Smart home products with security functionalities…") WERE served by EUR-Lex directly. The Commission guidance C(2026) 5252 (27 Jul 2026) is reachable only through the third-party PDF copy the lane also used (the official download is robots-blocked); its pt 23 ("a natural or legal person that shares free and open-source computer code on publicly accessible repositories is generally not considered to be placing that code on the EU market"), pt 14, pt 40, pt 52, pt 71 all verified at that copy — **the pt-23 sentence is limited to FREE AND OPEN-SOURCE code; a proprietary-licensed repo leans on "not supplied for use" + pt 14 instead (the return's row (a) already does).**

**CONFIRMED at text:** Art. 3(21)/(22) ("whether in return for payment or free of charge"); recital (15)'s "a software platform through which the manufacturer monetises other services" (OJ); Art. 14(2)(a)–(c) 24 h / 72 h / 14 days and 14(4)(a)–(c) 24 h / 72 h / one month; Art. 71 — 2027-12-11 general, **Art. 14 from 2026-09-11**, Chapter IV from 2026-06-11; Art. 69(3) (Art. 14 applies to products placed on the market before 2027-12-11); Art. 3(14) + Art. 24 (the steward regime, three paragraphs); Art. 64(10)(a)/(b); Annex III items 7/11/16/17; the repo citations (`core/persistence/MODULE_CONTEXT.md` :5 / :109 / :477 — `chain_hash` is the 32-byte ZERO vector today, hash chaining "a future task"; `control.in:11` "smart-home operating system"; `control.in:4` Maintainer `<homesynapse@nexsys.example>` + `:9` Homepage `nexsys.example` — placeholders).

**The six NOTES (the hub's addendum; each a refinement, none a reversal):**
1. **Art. 3(13)'s tail was omitted** — the manufacturer definition ends "…and markets them under its name or trademark, **whether for payment, monetisation or free of charge**". The omission STRENGTHENS headline 2 (a free release can make us the manufacturer); restore it in any restatement.
2. **Recital (15) ellipsis** — the return's elided quote misplaces an "or"; the platform clause is an INSTANCE of "an intention to monetise". Substance unchanged.
3. **THE STEWARD REGIME IS DEFINITIONAL, NOT ELECTIVE (the one finding that changes a branch):** a legal person meeting Art. 3(14) IS a steward by operation of law; the guidance's Examples 29/31 conclude for a COMPANY publishing non-monetised FOSS under its own name "Company A is not its manufacturer, but is its steward"; pt 82 says stewards "providing engineering resources" must notify actively exploited vulnerabilities per Art. 14(1) (to the extent involved in development), fine-free under Art. 64(10)(b) but NOT duty-free. Consequence for the applicability tree: branch (b) ("free FOSS release, none mandatory") holds for a NATURAL person and is only PROBABLE for an LLC — **the LLC (the brand track's "branch-independent acceleration") + a public FOSS repo = steward duties attach: a documented cybersecurity policy (Art. 24(1)), cooperation on request (24(2)), Art. 14(1)-class reporting to the extent we develop the product.** Whether Art. 24(3)'s import of Art. 14(1) bites from 2026-09-11 (Art. 14's date) or 2027-12-11 (Art. 24's own date) is unresolved at the text reachable here → **COUNSEL ITEM C-2′ (added to the counsel package)**.
4. **Art. 64(10)'s cross-reference oddity** — the derogation reads "from paragraphs 3 to 9" while the Art. 13/14 fine sits in paragraph 2 on every mirror; the evident intent (and every commentary) is that the 24-h early-warning fine falls away for micro/small enterprises — state "unfineable on the intended reading; confirm at the OJ" (counsel line).
5. **Annex I Part I (2)(l) is CONJUNCTIVE: "recording AND monitoring relevant internal activity … with an opt-out mechanism for the user"** — "and/or" was the 2022 proposal's wording (the brief carried it; the return repeated it). Consequence for the thesis: the event log's RECORDING leg alone does not discharge (2)(l); a MONITORING leg (alerting/inspection of the recorded activity) and the user opt-out are part of the requirement — one sentence for §5.2's build list. Also verified: (2)(c) automatic security updates "enabled as a default setting, with a clear and easy-to-use opt-out", and Part II (1) the SBOM "covering at the very least the top-level dependencies".
6. **Annex III item 16's quotation drops "the functions of"** ("control the functions of connected devices in residential settings"); and a mirror hazard: craact.eu's Annex III page renders 18 items with SHIFTED numbering — never take Annex III numbering from it (EUR-Lex 2025/2392 settles it).

**Two strengtheners the return missed (banked for the counsel package + the charter):** the guidance's pt 54 / Example 15 — a free FOSS app whose PAID servers are reached THROUGH it "is considered placed on the market in the course of a commercial activity" — versus Example 18 (separately offered optional consultancy → not placed on the market): **the line is services delivered THROUGH the software versus services offered ALONGSIDE it, which is exactly the line a cloud/remote-access/sync offering crosses;** and Example 24 / recital (18) — a NOT-FOR-PROFIT legal person monetising a FOSS product is "not considered to be placed on the market … subject to the obligations applicable to stewards" — the Open-Home-Foundation-shaped structural alternative to the monetising-LLC branch.

**The three headline findings, adjudicated:** (1) NOTHING BINDS ON 2026-09-11 for an unreleased, proprietary-licensed repository — SOUND (certain for a natural person; probable for an LLC, whose residual is steward duties, fine-free). The L-E line "the CRA process clock starting NOW" is true for the FIELD, not for us; I-5 survives as a launch-readiness item. (2) A FREE release by the COMPANY under a proprietary LICENSE is probably commercial activity → MANUFACTURER from release day — SOUND (3(22) "free of charge"; 3(13)'s omitted tail; recital (18)'s carve-out is expressly for non-monetised FOSS). (3) The Nabu-Casa-shaped monetisation pulls a free FOSS engine into full scope the day a paid service is sold THROUGH it — SOUND, and the guidance is more direct than the return lets on (pt 54 / Ex. 15). **(4) The thesis's tamper-evidence leg is NOT evidence-grade today — `chain_hash` is the zero vector on every row — CONFIRMED at the staged MODULE_CONTEXT; the north star's "hash-chained event log" is DESIGNED-FOR; the return's three lawful / three unlawful sentence pairs stand as the readiness-rail refinement → the claim rails at R-10.**

**Dispositions:** the return's dated checklist + the Art. 14 runbook draft + the ten "does not exist yet" items (security contact · advisory page · CVD policy · the `control.in` Maintainer/Homepage placeholders · SBOM toolchain · …) → the LAUNCH-READINESS track (the counsel package + the R-10 charter docket), NOT a Sept-11 act; the LICENSE decision is now load-bearing for CRA scope and rides the G-2/LLC track with the steward finding attached; the AI Act row stays "not plausibly in scope"; the RED delegated act and GDPR rows per the return (counsel-flagged). **RS-4 CLOSED at intake.** The research slate: RS-1/RS-2/RS-4 CLOSED; RS-3 W-MARKET-2 on cadence (~Sep-1); RS-5 post-charter.

## §4 Harvest rows (for the next skills/lessons touch)

- **A lane-return's line citations are re-pinned at the diff, never at the prose** — the FE return's one defect was a line number; the rest held because the lane cited file:line everywhere (the habit makes layer 2 cheap).
- **Detectors that compare captures must declare what they do NOT traverse** (arrays) — the blind spot is the class, not the instance.
- **Regulatory returns carry a source-of-record line in the frontmatter** (which text was verified at the OJ, which at mirrors) — both lanes did; keep it mandatory.
- **"Definitional, not elective"** — when a regulation defines a status (steward) by facts, the tree must test the facts, not the company's intent; the LLC act itself can flip a branch.
