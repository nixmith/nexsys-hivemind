<!--
file: context/audits/2026-08-22_R7_intake_two-layer-audit_v55-beat-6.md
purpose: The hub's two-layer audit of the R-7 / W2-1 arm64-artifact-channel return (the coder's: context/audits/2026-08-22_R7_arm64-artifact-channel_return.md) at the BYTES — the 7-file diff against core 62dbca3 exported from git objects, the new test read whole, the twins cmp'd at the instrument, the hivemind diff (handoff + lessons) read whole. Layer 1 = the coder's claims; layer 2 = the hub's re-execution against the primary text. Then the hypercritical layer (what the return under-states), the CI predictions filed BEFORE the push (H12; law 16 banks whatever lands), the rulings on the two pushback items, and the harvest.
audience: the hub (v55 beat 6 → v56); Nick (the commit order + the CI read); the R-9 / R-7b lanes (the rulings they inherit).
status: VERDICT ACCEPT — zero hub bytes in the core tree; ONE order-level addition (the exec bit on the new test). Filed 2026-08-22 (America/Chicago), v55 beat 6 — the hub session that took over after the prior hub session was cut off mid-audit (its scratch exports at _scratch/v55-audit/ were NOT reused: every input here was re-exported fresh to _scratch/v55-b6/ from git objects + the worktree, per env-model §12's fresh-temp-name rule).
-->

# R-7 / W2-1 intake — the two-layer audit (v55 beat 6, 2026-08-22)

## §0 Verdict

**ACCEPT.** The lane delivered exactly the R-D-IN / R-E-IN census (7 = 6 M + 1 A), every claim in the return holds at the bytes, the two riders landed as ruled, the twins are byte-identical at the instrument (`cmp` exit 0 this beat), zero Java / zero build-file / zero README, no token material anywhere (L3 clean across the diff, the test, the return, the handoff entry, the lesson). The pre-verification found ONE off-by-one (P5 line numbers, content exact) and ZERO STOP-grade mismatches. The coder's two pushback items are both CORRECT and are RULED below (§5) — neither changes this WU.

**Hub bytes in the core tree: NONE.** The only addition is at the ORDER: `git add --chmod=+x distribution/smoke/version-grammar-test.sh` is made mandatory, not optional (arc-discipline 15 — every other `distribution/**/*.sh` is `100755`; commit `604440b` exists because a `100644` script once broke install-smoke with exit 126; the workflow invokes this one through `bash`, so the bit is hygiene today and a trap tomorrow).

## §1 Layer 1 vs layer 2 — the claims at the bytes

| # | The return's claim | Layer 2 (the hub, at the primary text) | Verdict |
|---|---|---|---|
| 1 | Baseline `62dbca3` ≠ the instruction's `33861ad`; the census trees byte-identical | `git log` HEAD = `62dbca3`; `62dbca3` touched `distribution/deb/homesynapse-token` + `distribution/docs/token-rotation.md` (+ Java + MODULE_CONTEXTs) — none in R-7's census. Report-and-proceed was pre-ruled at beat 5 (ruling 5). | CONFIRMED |
| 2 | Porcelain = exactly the 7 (6 M + 1 `??`) | `git --no-optional-locks status --porcelain` at the instrument this beat: the 6 M + `?? distribution/smoke/version-grammar-test.sh`, nothing else. `git diff --stat HEAD`: 6 files, +219/−26 (the return's figure exact). | CONFIRMED |
| 3 | The twins byte-identical post-edit | `cmp .github/workflows/install-smoke.yml distribution/ci/install-smoke.yml` → exit 0 at the instrument. The WIRING-SEAM header (lines 1–17) unchanged in the diff. | CONFIRMED |
| 4 | `common.sh` arm → `*.*)` pass-through / `*)` wrap | Diff hunk @@ -54,14 +54,19 @@: exactly the two-arm `case`; `:50` (`HS_VERSION`) and `:52–:53` (the `VERSION` lookups) untouched; the comment rewritten with the `7c9e4fa`-bare / `0.1.0+gd26777c` exhibit; mode `100755` preserved (`index 50e6772..8bb6025 100755`). POSIX: no bashism introduced. | CONFIRMED |
| 5 | The grammar assert placed after `die()` because `die` is undefined at `:37` | HEAD `build-image.sh`: `VERSION=` `:35`, `ARCH=` `:36`, `log`/`die` `:43–:44`, first use of `VERSION` at `:58` (`log "version=…"`); between `:36` and `:44` nothing reads `VERSION`. The assert block lands at `:46–:60` (new), outside any `$( )`, before `:58`. The instruction's two strings verbatim. The `case` charset `*[!0-9A-Za-z.+~-]*\|''` is valid POSIX/bash (the `-` last in the bracket = literal). | CONFIRMED — the coder's placement is the lawful reading of "immediately after `VERSION=`" |
| 6 | The workflow: matrix, `runs-on` a string, job name with `${{ matrix.arch }}`, per-arch artifact names, two new steps after "Assemble .deb", the test in Static lint, `on.push.paths` + the workflow file | The diff, hunk by hunk: all present and in the stated order (Assemble → Arch-truth → Version-grammar echo → sqlite3 prereq → run-smoke → update-smoke → upload). `fail-fast: false` present. Every other step byte-identical to HEAD. | CONFIRMED |
| 7 | The version-grammar echo reads `distribution/image/build/opt/homesynapse/VERSION` | `build-image.sh:37–:38`: `OUT="${HERE}/build"`, `IMAGE="${OUT}${HS_OPT}"` = `distribution/image/build/opt/homesynapse`; `:153` writes `${IMAGE}/VERSION`. `build-deb.sh` copies the tree (`cp -a "${IMAGE_TREE}"`) and does NOT remove it — the file exists when the echo step runs. | CONFIRMED (a path claim, re-derived) |
| 8 | `build-image.sh` and `build-deb.sh` each compute `hs_version` independently (the stamped == image-VERSION assert's rationale) | `build-deb.sh:24`: `VERSION="$(bash -c '. "…/common.sh"; hs_version')"` — the same idiom, a second evaluation. On the runner both run from the repo root with an unchanged tree → equal; the assert is a real consistency check, not decoration. | CONFIRMED |
| 9 | The test: 23 checks, the rig defeats both bypasses, the stub `git` answers `rev-parse`/`describe` | Read whole. `run_hs_version` cd's into `${WORK}/tree/cwd` with `HS_VERSION=` and the stub first on PATH; `_dist_dir` under `bash -c` is `dirname bash` = `.` = the hermetic cwd; `./VERSION` and `../VERSION` (= `${WORK}/tree/VERSION`) absent by construction. 7 arm + 7 accept + 3 reject + 3 charset-reject + 3 carrier = 23. The carrier literal `grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+'` is present verbatim in `build-image.sh` (diff `:57`) and in both twins (the echo step). | CONFIRMED |
| 10 | Mutant 1 (HEAD's arm) fails exactly 2 | HEAD's `[0-9]*)` passes `7c9e4fa` and `7c9e4fa-dirty` through; wraps `d26777c` (a–f-leading); passes `1.2.3`, `1.2.3-5-gabc1234`, `1.2.3-dirty`; the empty describe falls to the skeleton. = 2 FAIL rows. The header comment corrected at the instrument — the right move. | CONFIRMED |
| 11 | R-E: the three majors EXIST; `setup-node`/`upload-artifact` left at v4 | `git ls-remote --tags` table in the return (checkout v5 `fbc6f39`, gradle/actions v5 `4c12511`, setup-java v5 `b6effb0`). Not re-fetched by the hub (no network need — CI is the proof by the pre-ruled revert-on-red law). `ci.yml` diff = exactly the three `uses:` lines. | CONFIRMED (by the return's evidence + CI) |
| 12 | `architecture.md` +80: no claim language, README untouched | Read whole: the section states build facts; "it lifts no claim fence" is explicit; the D-1 pair absent; `README.md` not in the porcelain. The `gh run download -n … -D …` flags are real `gh` flags. "E1 option (c) taken" matches `escalations.md` E1 (c) = native arm64 runner. | CONFIRMED |
| 13 | Hivemind: the handoff entry prepended + the frontmatter chain extended; one lesson appended | The diff: the R-7 DELIVERED entry above the R-6/R-8 entry; the `last-verified:` line prefix-prepended; lessons +8 lines at the tail. L3: zero 64-hex runs, zero 43-char token-shaped runs. | CONFIRMED |

**Citations/figures recorded, not edited:** the return's P1 row says `upload-artifact@v4 … if: always()` — true at HEAD. The return's "14 steps" count (PyYAML) = 12 at HEAD + 2 new — arithmetic holds. No stale figure found.

## §2 The hypercritical layer — what the return under-states

**H-1 — the grammar assert's REAL passes-but-false input is the skeleton fallback, not "an old tag".** `0.1.0-skeleton` matches `^[0-9]+\.[0-9]+\.[0-9]+` and the charset arm, so every assert in this WU goes GREEN on a build whose version of record is the fallback. Two real paths reach it: (a) pushback item 8 — `hs_version` invoked from `distribution/` or `distribution/image/` as cwd finds the tracked `distribution/VERSION` (`0.1.0-skeleton`); (b) a tree without `git` (a tarball export, a container without git). In CI neither can happen (repo root; `actions/checkout` leaves `.git`), and the bench packet runs from the root — so this is NOT a defect in R-7, it is a fence the NEXT distribution touch adds: the workflow's echo step asserts `V != 0.1.0-skeleton` (in CI the skeleton is never lawful). Ruled into R-7b (§5).

**H-2 — the version scheme must be settled BEFORE any CI-built artifact is installed on a card.** The coder's item 9 is exact: `0.1.0+g<sha>` builds do not order among themselves (dpkg compares `g7c9e4fa` vs `gd26777c` as strings — `g` < `gd`). If R-3b installs the R-7 commit's CI artifact `0.1.0+g<sha1>` and a later commit's sha sorts lower, the NEXT install needs `--allow-downgrades` again — the "exactly once" promise breaks on the second install. The hub's ruling (§5, R-V): the scheme changes to `0.1.0+git<commit-date>.g<sha>` in R-7b, which (i) sorts ABOVE every existing `+g<sha>` artifact (`+git…` > `+g…` because `g` is a proper prefix of `git`), (ii) is monotone along `main` by committer timestamp (UTC, `YYYYMMDD.HHMMSS`), (iii) needs `fetch-depth: 1` only (the commit carries its own date — the coder's `rev-list --count` alternative silently reads 1 on every shallow CI checkout and would have shipped a constant), (iv) is reproducible (commit date, never build date). The one-time `--allow-downgrades` on the two `7c9e4fa` cards stands (any `0.x` is below a bare `7c9e4fa`); after R-7b no second one is ever needed. **Sequencing consequence: R-7b lands BEFORE R-3b installs anything** — the R-7 commit's own CI artifact is an instrument, not an install source.

**H-3 — the job NAME changed.** "Build image + .deb, install-smoke on a clean machine" → "… on a clean amd64 machine" / "… arm64 machine". Any GitHub branch-protection rule or required-status-check that names the OLD job would stop matching. This repository pushes to `main` directly with no required checks on record (none has ever been cited in the spine) — noted, not a defect; if a ruleset ever names jobs, it names both legs.

**H-4 — the Node-20 annotation does not fully clear.** `actions/setup-node@v4` and `actions/upload-artifact@v4` remain (outside R-E's named list — the coder read the ruling exactly). The install-smoke run will still carry the "forced to run on Node.js 24" annotation for those two. Cosmetic; R-7b carries the two bumps (v5 majors verified to exist in the return's P8).

**H-5 — the arm64 leg's two unmeasured quantities: schedulability and wall-clock.** Both are decided by the push (the pre-ruled STOP: queued >10 min or an unavailable-label error ⇒ the hub rules the QEMU fallback, never the lane). The expected-output artifact for the leg is Block 0's log (`version=0.1.0+g<sha> arch=arm64 jdk=21` — the only line that differs from Block 0's bare `7c9e4fa` — · `bundled 55 jars` · the 16-module `--add-modules` line · `jlinked runtime → 61M` · `floor-presence assert green: all 16 …` · `MANIFEST.sha256 (147 entries)`), then the two green assert lines, then 18 `[smoke] PASS` lines, then update-smoke. `timeout-minutes: 25` stands until the wall-clock is on file.

**H-6 — the artifact-name change is consumer-visible and the doc says so.** Nothing consumes `distribution-artifacts` today (no downstream workflow, no script); the R-3/R-4 packets will name `distribution-artifacts-arm64`. Recorded.

**H-7 — the hub's own mechanics (disclosed).** This session replaced a hub session cut off mid-audit; its exports under `_scratch/v55-audit/` (stamped 13:43Z) were left untouched and NOT read — every input was re-exported to `_scratch/v55-b6/` under fresh names (the §12 stale-cache rule). One export-loop slip caught at the instrument: three `MODULE_CONTEXT.md` files exported under one basename overwrote each other (the app's survived); re-exported under distinct names before any citation was written. The local Filesystem-MCP surface was not used (the v54 finding stands; the bridge is the path).

## §3 CI predictions — filed BEFORE the push (H12; law 16 banks whatever lands)

- **`ci.yml` Build & Check — GREEN, ~3 min**, on `checkout@v5` + `setup-java@v5` + `setup-gradle@v5` (the three bumps' proof; `upload-artifact@v4` untouched). Zero Java delta ⇒ test counts unchanged from the `62dbca3` run (rest-api 130/0/0 · app 24 · lifecycle 60). The only plausible RED = an action-major incompatibility; if so the job log names the action in its first lines → revert THAT ONE bump (pre-ruled), disclose, never retune blind.
- **`install-smoke.yml` — RUNS (the push touches `distribution/**` and the workflow file) as TWO jobs**, display names "Build image + .deb, install-smoke on a clean amd64 machine" and "… arm64 machine". **amd64 leg GREEN ~2–3 min** (its 18 `[smoke] PASS` lines byte-identical to `62dbca3`'s; the two new steps print `arch-truth assert green: … Architecture=amd64 == matrix.arch=amd64` and `version-grammar echo green: 0.1.0+g<sha> (tag-shaped; .deb Version=0.1.0+g<sha>; image VERSION=0.1.0+g<sha>)`; Static lint prints the 23 PASS lines + `VERSION-GRAMMAR-TEST PASSED ✓`). **arm64 leg GREEN, wall-clock UNMEASURED (predicted 3–6 min: a cold Gradle + npm + jlink on a 4-vCPU Cobalt runner)**; the Block-0 six lines with `arch=arm64`; `homesynapse_0.1.0+g<sha>_arm64.deb` ≈ 61.8 MB (differs from Block 0's 61,788,352 B only by the version string + build stamps). Artifacts: `distribution-artifacts-amd64` + `distribution-artifacts-arm64`, 7 days.
- **Frontend CI — NOT triggered** (no `web-ui/**` path).
- **The plausible REDs, ranked:** (1) the arm64 leg never schedules (label unavailable for this repo) → the pre-ruled STOP; (2) an arm64-specific toolchain miss (a Node 22 or Corretto aarch64 download hiccup) → adjudicate at the job log, re-run ONCE only if the failure is a download 5xx (a transient, not a defect — the sole re-run exception, named here); (3) `setup-gradle@v5` behavior change → revert that bump. Anything else = adjudicate-first, no re-run.

## §4 The rulings (hub; the H10 forms named)

- **(R-7-ACCEPT)** The 7 land as delivered. The ORDER adds `git add --chmod=+x distribution/smoke/version-grammar-test.sh` (mandatory). CI on the push = the gate of record, BOTH legs + Build & Check. The WU CLOSES at the three green verdicts banked (law 16); the arm64 leg's wall-clock is banked beside them.
- **(R-V — the version scheme, hub-ruled as engineering approach; Nick may overrule in one word):** `0.1.0+git<YYYYMMDD.HHMMSS>.g<sha>` (committer date, UTC, from `git log -1 --format=%cd --date=format-local:%Y%m%d.%H%M%S` under `TZ=UTC`; `-dirty` appended as today) replaces `0.1.0+g<sha>` in **R-7b**. Rationale in §2 H-2. The first tag (`0.1.0`) is deliberately NOT minted now: a tag at HEAD would describe as bare `0.1.0`, which dpkg sorts BELOW every `0.1.0+…` artifact — tags begin when a release does, with a tagging rule written then.
- **(R-7b — a distribution-only micro-WU, host-CC, ≤1 h, BEFORE R-3b installs any CI artifact):** (a) the R-V scheme in `hs_version` + the fixtures (`version-grammar-test.sh`: the arm rows become `git<date>.g<sha>` shapes; a stub `git log` arm) + the doc's version-string section; (b) the skeleton fence in the twins' echo step (`[ "${V}" != "0.1.0-skeleton" ]` → `::error::`); (c) item 8: both build scripts export `HS_DIST_DIR="${DIST}"` and `_dist_dir` prefers it (the tracked `distribution/VERSION` stays — it is the no-git fallback's honest carrier — but it can no longer win by cwd accident); (d) `setup-node@v5` + `upload-artifact@v5` in the twins (H-4); (e) `escalations.md` E1 → "(c) taken at R-7". Census ≈ 6 (the twins · `common.sh` · `build-image.sh` · `build-deb.sh` · `version-grammar-test.sh` · `architecture.md` · `escalations.md` — the coder trims to the real count). The hub authors it at the v56 launch beat; it rides R-9's dispatch slot order: **R-9 first** (the availability fix has the larger blast radius and needs Nick's two words), R-7b second — or R-7b first if Nick's R-H words are not in yet. Never both at once on the checkout.
- **(R-9 = E3-HEALTH)** authored this beat (`context/instructions/2026-08-22_R9_E3-HEALTH_unauthenticated-loopback-health_coding-instruction.md` + `context/pre-verifications/WU-R9.md`); it inherits H-2's sequencing note and carries the R-7 item-8/9 riders only as POINTERS to R-7b (not as its own census).
- **The R-3/R-4 packets** carry the one-time `--allow-downgrades` line for the two `7c9e4fa` cards; after R-7b lands, the artifact they install is a `0.1.0+git…` build and no later install needs the flag.

## §5 Harvest (one line each; the v56 prompt carries the standing ones)

- A grammar assert's passes-but-false input is found by asking which LAWFUL value of the function satisfies the grammar while being wrong — here the fallback constant, not the hypothetical tag (the coder's comment named the tag; the spine names the skeleton).
- A monotone build-number scheme is chosen against the CI checkout's DEPTH: `rev-list --count` is constant 1 on a shallow clone; the commit date is depth-free.
- A lane's "optional" hygiene item (the exec bit) becomes the order's mandatory line when the repo's history already carries the failure it prevents (`604440b`).
- A job rename is a consumer-visible change for required-status-check rulesets — name it at authoring even when no ruleset exists.
- A fresh hub session never reads a predecessor's scratch exports — it re-exports; the stale-cache rule applies to people as well as to the bridge.
