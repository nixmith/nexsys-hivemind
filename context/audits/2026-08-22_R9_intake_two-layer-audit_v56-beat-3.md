<!--
file: context/audits/2026-08-22_R9_intake_two-layer-audit_v56-beat-3.md
purpose: the hub's two-layer intake audit of the R-9 / E3-HEALTH delivery (the coder's return: context/audits/2026-08-22_R9_E3-HEALTH_return.md, 43,739 B, on disk — law 37). Layer 1 = the return's claims; layer 2 = the hub at the diff (D2: the 24-path worktree delta exported per file via `git --no-optional-locks diff`, the two new files read whole, the unit/probe/helper hunks read whole, L3 scanned). Verdict, the one hub correction (pre-existing, mechanical), the CI predictions filed BEFORE the push (H12/law 16), the routing of the coder's out-of-census findings, and the orders.
audience: the hub (the beat-3 record) · Nick (the correction + the orders) · the v57 hub.
status: FILED (v56 beat 3, 2026-08-22 ~19:10 CDT). Core `89a912e` + 24 dirty paths; hivemind `1797cf3` + 3 (the lane's closeout).
-->

# R-9 / E3-HEALTH intake — the two-layer audit (v56 beat 3)

## §0 Verdict

**ACCEPT.** Census **EXACTLY 24 = 22 M + 2 A** at porcelain (`-uno` 22 M verbatim + the scoped untracked scan 2 A) = the instruction's 23 + the disclosed +1 (`StandardAuthMiddlewareTest.java` — a `revoke()` caller the hub's P2 survey missed; the "+1 clause" form; hub authoring miss owned). Both rulings executed as RULED (R-H1 LOOPBACK-ONLY · R-H2 IN); both riders executed (R-9-a `SUSPENDED` + a `SubscriberMode` set pin; R-9-b the DANGER block with the R10-IN-L source pointer). **Zero hub bytes in the coder's delta.** One hub MECHANICAL CORRECTION, pre-existing since `c09c61c` (July): `RestFiltersAuthTest.java` carries a raw NUL byte in a string literal, so git treats the file as BINARY — replaced by the `\u0000` escape (identical runtime string) via `_scratch/2026-08-22_R9_nul-fix.py` before the commit (§3). L3 clean (zero 64-hex / 43-char runs across every diff, both new files, the return).

## §1 Claims re-executed at the bytes (layer 2)

| # | Claim (layer 1) | At the bytes (layer 2) | Verdict |
|---|---|---|---|
| 1 | `HealthEndpoint`: GET/HEAD `/health`, 200 ⇔ LIVE else 503 + the gate's two headers, body exactly `{"status":"<mode>"}`, `Cache-Control: no-store`, mode read per call | `HealthEndpoint.java` 110 lines: `apply(EndpointContext)` reads `readinessSource.mode()` every call; 200 arm / 503 arm with `ReadinessFilter.PROJECTION_STATE_HEADER` + `Retry-After` = `RETRY_AFTER_SECONDS`; `ctx.json(Map.of("status", mode.name()))` in both arms; `no-store` always; DEBUG only | CONFIRMED |
| 2 | `installHealthEndpoint` registers GET **and** HEAD explicitly (the [REVIEW]) | `RestFilters.java` +`app.get(PATH, health); app.head(PATH, health);` with the bytecode rationale (Javalin 6's HEAD-for-GET fallback = bare 200 without the handler) | CONFIRMED — **ACCEPT the deviation**: the instruction's yes/no had a third answer; an automatic HEAD that never runs the handler is a passes-but-false "ready" during REPLAY; explicit HEAD is the only honest reading; e2e pins HEAD 200 empty-body on loopback, 401 off |
| 3 | The second exemption in `authorize()` sits AFTER `isPathSafe`, after the shell exemption, before `authenticate` | the hunk at `:543–:557`: traversal gate → `isPublicShellRequest` → `isHealthProbeRequest(ctx.method().name(), ctx.path(), ctx.ip())` → `authenticate` | CONFIRMED (the ORDER contract intact) |
| 4 | `isHealthProbeRequest`: GET/HEAD × exactly `/health` × a loopback LITERAL; null → false | `:390–:398`; exact `HealthEndpoint.PATH.equals(path)`; the 52-literal matrix in `RestFiltersAuthTest` incl. `/health/`, `/healthz`, `/api/health`, `/HEALTH`, traversal spellings, `"get"` lowercase | CONFIRMED |
| 5 | `isLoopbackLiteral` never hands a hostname to the resolver | the IPv4 arm parsed locally (`isLoopbackDottedQuad`: 4 octets, no leading zeros, first = 127); the IPv6 arm charset-guarded `[0-9A-Fa-f:.]` + first-char hex-or-colon, then `InetAddress.getByName` — JDK 21's colon-bearing path is parse-or-throw, never resolve; brackets and `%zone` stripped; empty → false | CONFIRMED — **ACCEPT** (stricter than specified, for the instruction's own reason; `cafe.babe`, `deadbeef`, `1e3`, `127.1`, `2130706433` all pinned false) |
| 6 | The wiring: ONE line between `app.get("/")` and `installReadinessGate` | `HomeSynapseCore.java` `+RestFilters.installHealthEndpoint(app, this);` at `:935`, 5 comment lines; nothing else moves | CONFIRMED |
| 7 | R-H2: `revoke()` returns `RevokeOutcome`; refuses the last active full-access token; `rotate` untouched | `OpaqueTokenStore.java`: `public enum RevokeOutcome {REVOKED, NOT_FOUND, REFUSED_LAST_FULL_ACCESS}`; `isLastActiveFullAccess` under the write lock (reference identity; the `activeKeyCount` predicate narrowed to `SCOPE_ALL`); ONE WARN with the key id only; `rotate` hunk absent from the diff | CONFIRMED |
| 8 | 409 `token-revoke-refused` at HTTP; `skipped` at the request file; nothing audited on refusal | `ProblemType.TOKEN_REVOKE_REFUSED("token-revoke-refused", 409, …)` (13 → 14); `TokenAdminEndpoints.revoke` → `switch` with the audit line in the REVOKED arm only; `processOperatorRequests` → `skipped.add(... "refused — the last active full-access token (use rotate)")` (key id public by construction) | CONFIRMED |
| 9 | The distribution sweep: the unit `--health-path /health`; 5 script sites; check 3b (18 → 19 PASS); the helper's `rm -f` gone; the caveat retired; E3 CLOSED; `HS_HEALTH_PATH` default → `/health`; the probe's code untouched | `diff.distribution` read whole: unit `:53` ✓ · `install.sh:89`, `update.sh:60/:77`, `update-smoke.sh:44/:65` ✓ · run-smoke 3b with its own passes-but-false line ✓ · the helper's `:110–:121` block gone, the CAUTION → NOTE ✓ · `token-rotation.md` caveat → History + the 409 row ✓ · E3 "CLOSED at R-9" with the return path ✓ · `common.sh:40–:45` one hunk, the R-7 arm untouched ✓ · `health-probe.sh` header-only ✓ | CONFIRMED |
| 10 | R-9-b: the unit's M13 block rewritten as a DANGER note, comment-only; the header `:8–:13` reworded to match | `:100–:117` the DANGER block naming the precondition + the R10-IN-L §3 f1 source; the eventual shape kept commented (`Type=notify` added as a commented line); the only non-comment delta in the unit = `ExecStartPost` | CONFIRMED |
| 11 | Tests: rest-api 130 → 147, lifecycle 60 → 62; red-first captured | +17 = `HealthEndpointTest` 5 + `RestFiltersAuthTest` +6 + `OpaqueTokenStoreTest` +5 + `TokenAdminEndpointsTest` +1 (+ the re-fixtured `StandardAuthMiddlewareTest`); lifecycle +2 (`healthServesHeaderlessOnLoopback…`, `healthStaysGuardedOffLoopback`); the red captures quoted (`expected: 200 but was: 401`; 31 compile errors on the four absent symbols) | CONFIRMED |
| 12 | MODULE_CONTEXT ×2; module-info / build files untouched; the twins untouched | rest-api MC: the 14-value `ProblemType` row, `HealthEndpoint`, `installHealthEndpoint`, `RevokeOutcome`, `TOKEN_REVOKE_REFUSED`, the test census; lifecycle MC the blockquote; porcelain shows no `module-info.java`, no `build.gradle.kts`, no `.github/`, no `distribution/ci/` | CONFIRMED |
| 13 | Census 24 = 23 + 1 | §0 | CONFIRMED |

**L3:** zero 64-hex, zero 43-char runs beyond identifiers across the five diff exports, both new files, the test file, the return, the coder-handoff/lessons diffs.

## §2 The hypercritical layer — what the return under-states, and the hub's own misses

- **(H-1) The file git calls binary.** `RestFiltersAuthTest.java` has carried `"/api/has<NUL>nul"` with a RAW 0x00 byte since `c09c61c`; the R-9 diff of the auth classifier's test is therefore unreadable in git (`Bin 5164 -> 11717 bytes`) and `git grep` reports "binary file matches". Not the lane's defect — but the lane edited the file and the hub audits diffs; a security classifier's test must diff as text. **Corrected by the hub (mechanical; §3).**
- **(H-2) The hub's P2 survey missed a caller** (`StandardAuthMiddlewareTest:84`) — the "+1 clause" caught it, the coder re-fixtured to assert `REVOKED` (non-vacuous). The survey must `git grep` the METHOD NAME, not the consumers the author remembers (the addition-#1 internal-consumer rule, re-learned).
- **(H-3) `LogDirectory=` (return §5 7a).** The unit has carried a non-directive since the skeleton; `systemd-analyze verify` names it in one line and nothing in CI ran that verb. **Routed to R-7b** as riders i (the one-token fix) + k (the verify line in Static lint, failing only on `Unknown key name`) — CI-proven on both legs.
- **(H-4) `boot-contract-map.md` §Health is false after R-9** (return §5 7b) — a doc the R-9 census should have carried (the arc-25 sweep covered probe CALL SITES, not the map that explains the posture). **Routed to R-7b** as rider j. Doc 09 §15 Q1 (7c) → the R-10 docket (already there).
- **(H-5) The R-H1 e2e on the runner.** `healthStaysGuardedOffLoopback` binds a site-local address (`assumeTrue`-gated on the interface existing, NOT on the bind succeeding). GitHub runners have a 10.x interface, so the test RUNS there for the first time on this push. A bind refusal on the runner would be a RED of the rig, not of R-9 — adjudicate at the log, no re-run (§4).
- **(H-6) The probe's `--timeout 30` in check 3b** is shorter than the unit's 90 — lawful (check 3 already waited out readiness; 3b measures the path, not the boot).

## §3 The one correction (hub; mechanical; the OR-GATE-M7.5c-a form)

`python ../_scratch/2026-08-22_R9_nul-fix.py` from the core root replaces the single raw NUL in `RestFiltersAuthTest.java:48` with `\u0000` (+5 bytes; the JLS translates unicode escapes before lexing — the literal is byte-identical at runtime). The script STOPs unless exactly one NUL exists inside that literal and writes nothing otherwise (tested in the container on the exported bytes: `11717 → 11722`, `file` = "Java source, UTF-8 text", second run STOPs). Nick re-runs `./gradlew :api:rest-api:test` (≈30 s) — **147/0/0 expected** — then `git diff --numstat HEAD -- <file>` prints numbers, not `-	-`. Census unchanged (the file is already M).

## §4 CI predictions — filed BEFORE the push (H12; law 16 banks what lands)

- **`ci.yml` Build & Check — GREEN ~3 min.** rest-api **147 / 0 / 0** (the three POSIX pins run on Linux), app 24, lifecycle **62** (both new e2e rows run; `healthStaysGuardedOffLoopback` on the runner's 10.x interface). Spotless clean.
- **`install-smoke.yml` — TWO jobs GREEN** (amd64 ~2m45s · arm64 ~3m): the unit's `ExecStartPost … --health-path /health` is the runner's first live-wire proof that the packaged unit starts on `/health` with NO token read; run-smoke prints **19 `[smoke] PASS` lines** (3b: `unauthenticated loopback /health green (HTTP 200 — the unit's probe path, E3)`); update-smoke GREEN through `--health-path /health` ×2; the two Node-20 annotations persist until R-7b. Artifacts `distribution-artifacts-{amd64,arm64}`. Frontend CI NOT triggered.
- **Plausible REDs, ranked:** (1) the R-H1 e2e's site-local bind refused on the runner → a rig RED, adjudicate at the log (the fix is `assumeTrue` on the bind, not on the interface) — no re-run; (2) the runner's probe reaches `/health` with a non-literal remote address (it cannot — Jetty reports `127.0.0.1`) → would show as `401` on `/health` in the journal capture; (3) anything else = adjudicate-first, no re-run.

## §5 Rulings

(1) **R-9 → LANDING now**; the core order = 24 paths after the NUL correction; CI BOTH workflows = the gate of record; the WU CLOSES at **§OP-A+H** on the bench (one visit: the R-6/R-8 rotate proof + the three R-9 lines — the queue carries the combined block). (2) **The INTERIM OPERATOR LAW retires at the beat that banks CI GREEN + §OP-H PASS**; the PACKAGED-unit proof (artifact absent → restart → active) stays R-3's Block E3-GREEN. (3) **R-7b gains riders i/j/k** (the unit's `LogsDirectory=`; the boot-contract-map fold; the `systemd-analyze verify` lint) — census 9 → **11 M**; dispatches after R-9's CI is GREEN. (4) `OR-E3-PROBE` → "CLOSING — the root fix is on the push; closes at §OP-H + E3-GREEN" (Open Risks edit at this beat). (5) The coder's lessons (the HEAD-for-GET bytecode pin; the refusal-guard re-fixture rule; the DNS-safe literal classifier) ride into the next skills pass as candidates.

## §6 Harvest

- A file git calls BINARY cannot be audited at the diff — check `--numstat` for `-	-` on every touched source file before reading hunks; a raw control byte in a test literal is a hygiene defect even when javac accepts it.
- A fallback the framework applies silently (HEAD-for-GET) is an instrument with its own semantics — pin it from bytecode before relying on the route table.
- `systemd-analyze verify` is a one-line lint the unit never had; a directive typo survived five WUs because nothing ran it.
- The P2 survey greps the method name across the repo; memory of the callers is not a survey.
