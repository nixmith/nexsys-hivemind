<!--
file: context/audits/2026-08-27_W-SKILLS-4_and_FE-SWAP-GATE_intake_two-layer-audit_v57-beat-2.md
purpose: the hub's two-layer audit of TWO lane returns intaken at v57 beat 2 (Thu 2026-08-27, 06:40–08:00 CT): (A) W-SKILLS-4 — the skills launch-cost + conventions pass (`context/audits/2026-08-26_W-SKILLS-4_return.md`, 21,025 B) and (B) FE-SWAP-GATE — the brand-token pin + the false-green fix (`context/audits/2026-08-27_FE-SWAP-GATE_return.md`, 8,640 B). Layer 1 = the returns' own claims; layer 2 = every claim re-executed by the hub at the bytes / the git diff / the primaries (D2, D17, D18: labels are claims, quotes are evidence). Also banks (C) the `dec35be` install-smoke run detail from Nick's paste (law 16, detail level) and scores the H12 prediction.
audience: the hub (the rulings of record) · Nick (the two commits + Check 9) · the W-SKILLS-5 charter (harvest).
status: FILED (v57 beat 2). Verdicts: (A) ACCEPT — ratified with two rulings · (B) ACCEPT — one disclosed deviation adjudicated lawful · (C) H12 CONFIRMED exactly; the arm64 line still to bank.
-->

# v57 beat 2 — the W-SKILLS-4 + FE-SWAP-GATE intake (two-layer audit)

## §A W-SKILLS-4 — ACCEPT (60-in / 60-out; 33-in / 33-out; one pre-ratified retirement by name; zero project state)

**A.1 Law 37 + census at porcelain.** The return exists at the named path (filed 2026-08-26, the operator-day re-derived at the instrument — arc-4 held; the lane's harvest 5 names the UTC-banner hazard that would have mis-dated it). Hivemind porcelain after the beat-1 landing = EXACTLY the lane's six files (5 M + 1 A) + the two returns; `nexsys-skills` porcelain EMPTY (the FE tree 0-byte, as claimed); no `.claude/skills/**` under the connected folder (the mirrors are outside it — §A.6).

**A.2 `project-manager/SKILL.md` — re-counted by the hub.** `git diff -U0`: EXACTLY one hunk `@@ -13,2 +13,2 @@` (the return: `13,14c13,14` ✓). Census re-count HEAD vs worktree by regex over the masthead: arc-disciplines **37 → 37** · durable **D18 → D18** · state-pointer **1 → 1** · strategy-layer line byte-identical (its count carries) ✓. Lines `:15–EOF` md5 **`640e3cf6…` in BOTH** — the four rule lists are byte-unchanged, so "every name surviving" is true by construction, not by reading. Bytes 53,020 → 49,676 ✓. The two replacement lines measure **379 B / 293 B** ✓ (both ≤ 400).

**A.3 The moved provenance, `cmp`'d at the bytes.** HEAD `:13` (2,950 B) vs `references/pass-history.md` from offset 1,704: **IDENTICAL**; HEAD `:14` (1,066 B) vs offset 4,784: **IDENTICAL** — the return's offsets reproduce exactly. The carrier (5,850 B) carries no rule; its `update-cadence:` sentence is the one convention it introduces (disclosed by the lane; ratified — it is P2's pointer form applied to a skill masthead).

**A.4 `coder/SKILL.md` — re-counted.** Hunks EXACTLY `@@ -13 +13 @@` + `@@ -315,2 +315,2 @@` ✓; arc-conventions **21 → 21** by regex; everything else byte-identical by construction (no other hunk). `coder/CLAUDE.md` hunks `8 · 23 · 34,35 · 49a50 · 57 · 89 · 118` ✓ (7 = the return's list); `java-patterns.md` `8 · 626a627,632` ✓; `testing-standards.md` `8 · 465a466,469` ✓. The diffs read by the hub match the return's §3 quotes VERBATIM (P7's three rows, the P8 convention at CLAUDE.md :34 + :50 and SKILL.md :315, the lesson-size convention at :119 with the ≤ 16,000 B floor, the two R-9 patterns as §15 and the refusal-guard rule as §11 — each pattern one line, each with its R-9 return pointer).

**A.5 Fences, measured by the hub.** State-pointer scan over EVERY added line (7–40-hex tokens · `M<n>.<n>` slots · watermark · projectionVersion · next-slot): the only hits are `8028337` ×2 = the RETAINED prior segments "against commit 8028337" — exactly as disclosed ✓. CR = 0 · NUL = 0 · trailing newline present on all six ✓. Attribution trailers: nothing staged (the lane committed nothing) ✓. Write-isolation: nothing under `context/**` but the return ✓.

**A.6 The mirrors — Check 9 executed at the THIRD location, 23/23.** Nick's "skills have been synced" = the account-synced skill trees (the copies a remote hub loads). The hub md5'd every file in the three synced trees (9 + 7 + 7 = 23 files, incl. the new `pass-history.md`) against the three writable sources on disk: **23/23 IDENTICAL** (PM SKILL.md `3d475025…` 49,676 B · coder SKILL.md `b0b96b23…` 36,674 B · FE SKILL.md `e0b8a048…` 41,636 B). The sync ran BEFORE ratification (the brief said after) — a harmless order inversion because the audit accepts with zero corrections; recorded, not re-asked. The host-side `.claude/skills` mirror (outside the connected folder) is Nick's 30-second `diff -rq` ×3 in the queue.

**A.7 Rulings.** (1) **§4.1 — RATIFIED at 49,676 B**; the (a) target restated as "masthead provenance ≤ 700 B" (met: 672). (2) **§4.3 — the ≤ 16,000 B floor STAYS** (it is the ratified P6 text; the hub's own 2026-08-26 rotation already executed it). (3) **P7's retirement** of the coder Tier-1 week-plan row ×3 — RATIFIED by name (Nick 2026-08-09; W-HIVE-1 P7; the five-lane intake §1). (4) The `pass-history.md` update-cadence convention — RATIFIED. (5) Harvest 1 is a REAL standing defect in the PM tree: `references/freshness-preflight.md` Check 2 ("Current week's plan exists" → STALE) is a false-STALE at every literal hub launch; the 22 references across 5 files are the census for **W-SKILLS-5** (charter: the PM-side P7 fold + the coder masthead provenance move (2,305 B) + the FE body pass (85 % body); fence by TREE not count — harvest 4). Not chartered this week; authored ahead when the hub has an idle beat, dispatched post-R-4.

**A.8 Lane closeout.** ACCEPT. Nick's commit rides the beat-2 hivemind order (13, below); the mirrors are already identical at the synced location; Check 9's host-side pair rides the queue.

## §B FE-SWAP-GATE — ACCEPT (2 files at the bytes; one disclosed deviation adjudicated lawful; the flip count corrected to 3 M)

**B.1 Law 37 + census at porcelain.** The return exists at the named path. Core porcelain (tracked `-uno` + untracked scoped to `web-ui`): EXACTLY ` M web-ui/dashboard/src/views/EventsView.unserved404.test.tsx` + `?? web-ui/dashboard/src/lib/i18n.test.ts` ✓; core HEAD `dec35be` ✓; `git diff --quiet` on `i18n.ts` · `index.html` · `README.md` · `tokens.css` = CLEAN ✓ (the four fenced files).

**B.2 The two files, read whole by the hub.** `i18n.test.ts` (16 lines): `import { BRAND } from './i18n'` (BRAND is exported at `i18n.ts:16`; `productName: 'HomeSynapse'` at `:18` — the flip site) · one `describe`/`it` · `expect(BRAND.productName).toBe('HomeSynapse')` at `:14` under the W-11 comment ✓. `EventsView.unserved404.test.tsx` diff = EXACTLY 2 insertions / 2 deletions: `:34` the import gains `BRAND`; `:153` `.not.toContain('HomeSynapse')` → `.not.toContain(BRAND.productName)` ✓ — meaning preserved (the assertion still says "the minted problem copy does not name the product"), now token-relative. Literal census over the whole test corpus: **`i18n.test.ts:14` is the ONLY `HomeSynapse` literal left** ✓ (the return's "zero literal hits remain" + "no second failure" claims agree with the instrument).

**B.3 The sandbox run — a DISCLOSED deviation, adjudicated LAWFUL.** The instruction said "no build in a sandbox; Nick runs `npm run verify` on his host; `frontend.yml` on the push is the gate of record". The lane ran verify + the red arm in a reconstructed sandbox (Windows-built `node_modules` cannot serve a Linux vitest; `npm ci` over Nick's tree would have clobbered his binaries — NOT done, correctly), with the three WU-relevant files hash-matched host ↔ sandbox and deps lockfile-exact. Adjudication: the instruction's "no sandbox" guard exists so that a sandbox build never SUBSTITUTES for the gate of record — it does not here (frontend.yml on the push remains the gate; Nick's host `npm run verify` remains the instruction's own host-side step and rides the queue). The red arm (exactly ONE failure — the pin — with 210 green under `ZZ-PLACEHOLDER`) is the #18 proof; it transfers because the inputs are byte-identical. **Host-side red arm: NOT required** (H10 ruling below).

**B.4 The rulings the lane asked for.** (i) **The render rows (AppShell `:36`, AuthGate `:23`) stay OUT** — of the gate AND of the flip: the reach map + `npm run verify` at build are the proof; a render pin on the wordmark slots is the first thing a B-1 lockup change would break for no gain (the lane's recommendation, adopted). (ii) **The flip count: 3 M in FE territory** (`i18n.ts` · `index.html` · `i18n.test.ts`) = 5 IN lines; the README rows ride the hub's own patch; tests 211 → 211. The hub's "5" was right as LINES, wrong as FILES — owned; the runbook's H+2–6 line now carries the count of record (M this beat). (iii) The `.git/index.lock` deletion: a zero-byte lock the lane's OWN un-flagged `git status` created on the mount, removed under Nick's delete grant the same minute, `GIT_OPTIONAL_LOCKS=0` thereafter; Nick's `ls -la .git` confirms no lock (the index at 218,513 B / Aug 26 21:02 is the lane's status refresh — normal). Harmless; the hub's standing form (`mv` to `_scratch/_to_delete/`, never `rm`) is restated for lanes, and the FE preflight's porcelain line should SPELL `--no-optional-locks` (W-SKILLS-5 FE-half harvest).

**B.5 The core-checkout fence, read.** "NOTHING on the core checkout until R-3b" protects the distribution/unit baseline R-3b edits and the tree R-3a measures against. This commit is two test files under `web-ui/dashboard/**`; `install-smoke.yml`'s `paths:` filter does NOT include `web-ui/**`, so the push builds NO new artifact and R-3a's Block I still installs the `dec35be` artifact by run id; the unit file is untouched. The FE gate was scheduled by the banked program itself (v57 §3 Mon; the plan §1 R-FE-GATE). **Ruling: LAND (rec)** — the fence's object is untouched; holding it only delays the red-first flip.

**B.6 CI predictions, FILED pre-push (H12).** `Frontend CI / Lint · Typecheck · Test (a11y) · Build · Budget · Contract` — GREEN: `Tests 211 passed (211)` · 15 test files · budget 63.2 KB / 100 KB · contract 11 endpoints v1.1.2-2026-07-26 (the sandbox tail reproduced on Node 20). `CI / Build & Check` — RUNS (it has no paths filter) and is GREEN by subsumption (arc-9: a zero-Java, zero-Gradle-source-set delta; ~2 m). `install-smoke` — **ZERO runs** (the paths filter); a run appearing = a finding.

## §C The `dec35be` run detail — banked (law 16, detail level)

Run **32672999145** (`https://github.com/nexsys-io/homesynapse-core/actions/runs/32672999145`); the amd64 job `97276587975`'s "Version-grammar echo" step, pasted by Nick: `hs_version=0.1.0+git20260823.231355.gdec35be` · `version-grammar echo green: 0.1.0+git20260823.231355.gdec35be (scheme …; .deb Version=0.1.0+git20260823.231355.gdec35be; image VERSION=0.1.0+git20260823.231355.gdec35be; sha256 f68e31d57a5bd247763548dd683114ca86a91717caf1b4dacec4f27179b72455  homesynapse_0.1.0+git20260823.231355.gdec35be_amd64.deb)`. **H12: the beat-1 prediction `0.1.0+git20260823.231355.gdec35be` CONFIRMED to the byte** (derived from `common.sh` `hs_version()` + `TZ=UTC git log -1 --format=%cd`); `.deb Version == image VERSION` — the R-7b symmetry assert green on the real run. **The amd64 hash is NOT the Block-I origin hash:** each matrix leg builds its own `.deb` (`name: … on a clean ${{ matrix.arch }} machine`); the arm64 job's line is the one the packet compares against — read on the run page at fetch time (the packet now says so) and banked at the next beat from Nick's paste. "Two runs of the amd64 summary" = two attempts/views of the same job, not the arm64 leg (its line would name `_arm64.deb`).

## §D Orders this beat

**CORE — 2 = 1 M + 1 A** (`web-ui/dashboard/src/views/EventsView.unserved404.test.tsx` · `web-ui/dashboard/src/lib/i18n.test.ts`); msg `../_scratch/2026-08-27_core_FE-SWAP-GATE_commit-msg.txt`; preceded by Nick's host `npm run verify` (predict 211/211); followed by the CI read (§B.6). **HIVEMIND — 13 = 9 M + 4 A** (the spine ×2 · the five skills-lane files · the R-3a packet (the run id filled) · the runbook line; the `pass-history.md` carrier · the two returns · this audit); msg `../_scratch/2026-08-27_hivemind_v57-beat-2_commit-msg.txt`. **SKILLS repo: nothing** (FE tree 0-byte). **Mirrors:** the synced location 23/23 identical (§A.6); the host `.claude/skills` pair = Nick's `diff -rq` ×3.
