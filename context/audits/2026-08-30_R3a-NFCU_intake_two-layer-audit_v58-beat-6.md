<!--
file: context/audits/2026-08-30_R3a-NFCU_intake_two-layer-audit_v58-beat-6.md
purpose: The hub's two-layer audits of (1) the R-3a return set (the hub-return card + the 93 KB operator record + the Sat deferral return + the escalation card — all copied VERBATIM, cmp-verified, from _scratch/ into context/audits/ this beat) and (2) the NFCU navigator return (context/handoff/2026-08-28_NFCU_application_return.md). Verdicts + the eight rulings of record. Nick's charge: assess critically, not receptively — the self-grading hazard named; mechanisms verified independently at source.
audience: hub · Nick · the R-4 packet (inherits the rulings)
state-type: audit record
status: CLOSED — R-3a ACCEPT · NFCU ACCEPT. v58 beat 6, 2026-08-30.
-->

# R-3a + NFCU intake — two-layer audit (v58 beat 6)

## §1 R-3a — VERDICT ACCEPT (the self-grading hazard addressed by independent re-execution)

**Layer 1:** the return set is complete and honest — 32 predictions exact · 1 half · 5 misses/refutations OWNED with the refuted claims recorded rather than dropped; §8 refuses to characterize its own open items; the operator record carries the full verbatim transcript with the stated rule that the transcript outranks the adjudication. Core porcelain EMPTY at `5051fa5` throughout; nothing was committed by the lane.

**Layer 2 — the hub's independent source reads (beyond the two-finding bar):**

1. **§9-B: MECHANISM ESTABLISHED, not correlation.** `rehydrateAdoptionMaps()` enumerates `deviceRegistry.listAllDevices()` — the Phase-3-rebuilt projection of the EVENT STORE (AMD-99 / REG-INV-1) — and consults the device cache ONLY for `matchedProfileId` (`ZigbeeIntegrationAdapter` ~:1002–:1038; the Javadoc says it outright). An empty registry therefore yields `rehydrated=0` NECESSARILY; the three filed forecasts are corollaries of the code path, not three correlated observations. The load-bearing claim (clones carry the network, not adoptions, not working automations) is BANKED as mechanism; the automation half rests on ULID-bound `entity_ref`s observed dangling at both rosters — consistent and accepted.
2. **§10-Q semantics CONFIRMED (charge 3: the correction is RIGHT, not merely less wrong).** `ingestion_unknown_sender` fires per HA-profile frame when `resolver.deviceForNetworkAddress()` misses the adoption maps (`ZclIngestionUnit` :386–:392, frame skipped). Unadopted fleet ⇒ every transmission lands as exactly that WARN — the 165 observed ARE the fleet speaking, and the refutation of §10-B coheres mechanically with §9-B.
3. **§10-P: MECHANISM VERIFIED.** `homesynapse.service:99–100` routes stdout/stderr → journal; NO `sd_journal`/`SyslogLevelPrefix`/priority-prefix emission exists in app/core/config/distribution ⇒ every application line lands at journald's default INFO priority. The observed 2-vs-158–201 gap is structural, not incidental. → OR-JOURNALD-PRIO opened (pm-handoff Open Risks).
4. **M9.4-KEYb: VERIFIED LANDED at HEAD → CLOSED.** The M9.4-KEY vocabulary fold + the established/progress/failed 0x009B separation are in source (`EzspCoordinatorProtocol` :310–:330 incl. the "KEYb confirmation" iteration note; `ZclIngestionUnit` :345–:363). Today's single clean `key_established` + zero spurious failures is the free regression confirmation. No open OR row existed; the verification banks here.
5. **§10-E: NOW CHARACTERIZED AT SOURCE (was OPEN).** `ZigbeeDeviceCache` persists via temp-write (`Files.writeString`, :482) + move with NO permission preservation ⇒ any save rewrites at umask-default 0644 (the observed 0600→0644); the cache is rewritten from the live adoption view (save on adapter shutdown; 30 s debounce, per the class docs) ⇒ a cloned cache is overwritten with only the rig's own adopted devices at the first orderly shutdown. Residual (immaterial to R-4): which save fired first in today's timeline.
6. **§10-M: emission path read; CAUSE STAYS OPEN.** The WARN is a faithful relay of NCP callback 0x009B when `!established && !progress`; the cause of the `0xFFFFFFFFFFFFFFFF` event is NOT characterized (the record's status name discriminates if revisited). Not banked.
7. **The config-issue ×3: class read (the schema validator's generic relay, `StandardConfigurationService` :818–:822); CONTENT STAYS OPEN** — verbatim capture commissioned into the R-4 packet greps.
8. The remaining §8 refuted-claim dispositions (Δ+4 row model; the §9 fleet-arm inference; transport_failed=0) are sound as recorded; none crosses into banked fact.

## §2 The rulings of record (R-1..R-8)

- **R-1 · R-4 criterion (4): OPTION (b) ADOPTED** — rewrite the cloned automation's `entity_ref`s to the rig's re-adopted entity ULIDs as an EXPLICIT rehearsal step, then fire. (a) REJECTED (cloning the event store destroys the rig's `home_id` identity); (c) REJECTED (moves the proof off the packaged rig — the criterion's meaning is the packaged path). D5 rider: the lift sentence for (4) must SAY the automation was re-bound post-adoption.
- **R-2 · criterion (2) RESTATED:** "≥1 device Available; freshness evidenced AT THE STORE (fresh `state_reported`/`availability_changed` rows via read-only sqlite)" — the Devices list's Last-reported render is NOT the instrument (§10-G is a READ-path gap; the data exists). The read-path fix + §10-J (the explain surface concealed a dangling ref it could see) → ONE NEW FE ROW for the FE lane post-R-4: the explain surface renders resolved entity identity and flags unresolvable refs LOUDLY; the Devices list never claims freshness without evidence. Register-adjacent: the "why didn't it" hero depends on it.
- **R-3 · OR-E3-PROBE: CLOSED** (this beat; the block moves to the resolved archive with the closure stamp). The third leg landed on hardware — single-variable, same card, artifact absent: `7c9e4fa` 90 s → TIMEOUT/143/restart-loop vs `dec35be` 4 s → active + 200/401; sole delta `--health-path /health`; probe binary comments-only different at source. §3-A's wording adopted (systemd tears down a HEALTHY process); §4-C noted (the installer + helper instructed the failure pre-R-9).
- **R-4 · M9.4-KEYb: CLOSED** (§1.4).
- **R-5 · TWO NEW OR ROWS OPENED:** OR-FAILCHAN (healthy outcomes reported through failure channels — §6-B exit-143, §10-O transport_failed on orderly close, §10-M, §10-I `Current`; the sweep WU charters at the September plan; §6-B root-correct fix = exit 0 on caught-SIGTERM, the first candidate) · OR-JOURNALD-PRIO (§1.3).
- **R-6 · the ★ packet amendments: ADOPTED IN FULL** — applied to the R-4 packet as ONE amendment pass (beat 7, before any dispatch), together with: the §9-B census rewrite (re-adoption, not 5+1) · R-1/R-2's criterion changes · the uncontrolled-power-loss leg ADOPTED as a deliberate R-4 step, positioned AFTER the four lift criteria are ⏺'d (evidence-first; a mishap cannot cost the lift) · the config-issue verbatim capture + `ingestion_unknown_sender`/`permit_join_opened`/`ncp_configured` greps · the token-helper line fix. **F-A5 needs NO rerouting: R-3b §2(1) already replaces the RAMP comment block** — the rider is IN the locked spec.
- **R-7 · THE WEEKEND RE-PRICE: R-4 = MONDAY, not tonight.** Grounds: the amendment pass must settle; criterion-4 adds new choreography deserving a fresh operator (today's >5 s button-hold is the fatigue exhibit); Monday's Pelton word OUTRANKS and its same-day card may consume discretionary hours. The day still moves: **R-3b DISPATCHES NOW** (stamped this beat; host-side; zero hardware) → hub audits (H13 byte-diff) → core commit/push → CI builds R-4's artifact tonight → **the hub authors THE SEPTEMBER PLAN OF RECORD TODAY** (moving §5(e) forward so Monday is review-and-word) → Mon: the NFCU call (calendar block ~1 h) + the Pelton word + R-4 evening window. PKG-SEC-1 stays HELD post-R-4.
- **R-8 · NFCU: ACCEPT** (§3 below).

## §3 NFCU — VERDICT ACCEPT

**Fences:** §0.2 reproduced verbatim at the top and GOVERNING ✓ · L3 scan: the only digit-strings are dates, public URLs, and the two marked `⟨pub-id⟩` carve-outs (NAICS 541511; the packet's own IRS form designation) — **carve-outs ACCEPTED as written, no strike** · no browser, no authenticated surface, no credential, no §0.2 datum, health data explicitly declined (§C.3.3) · nothing committed by the lane. **The firm-today act SATISFIED:** application SUBMITTED + documents UPLOADED Sun; the member-initiated call Mon 2026-08-31 is row 3's completed-act form (no appointment exists in the real process — §D.2).

**Adjudications:** §D.1 (NFCU wants a ≤60-day Certificate of Good Standing, NOT the Articles) → the banking-packet template stages the certificate, Articles demoted to reference — lesson class, with §C.3.5 (the SSN-default override) and §D.6 (the full BSA/AML block) folded into the same template note · **§G's screenshot carve-out ADOPTED** for future navigator packets ("blank form fragments containing no §0.2 datum may be shown; anything displaying a value may not") · §D.7 (the doubled entity-type suffix in the application UI) + §F.4's queued questions → THE MONDAY-CALL CHECKLIST (§F is it; nothing re-authored) · **§E → FIN-1 RIDERS:** never-negative + a buffer above the largest recurring debit (the joint-liability carve-out past the shield) · daily business-day debit review + alerts (the one-banking-day ACH window) · no personal use (now contractual, not just hygiene) · the share-account minimum joins the 2026-10-01 quarterly checkpoint (silent membership-termination mode) · the OA-succession confirm rides the counsel-wave email as its fifth item · the second-banking-relationship redundancy → P2/P3 parking · the foreign-transaction % noted against tooling spend. §C.4's capitalization-to-function framing (adequate capitalization as a liability-shield factor) → the Monday call's funding act. **This return is APPENDABLE: FIN-1's "account opened" row closes only at the call's state word.**
