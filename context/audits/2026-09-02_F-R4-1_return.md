<!--
file: context/audits/2026-09-02_F-R4-1_return.md
purpose: Coder-lane return for F-R4-1 — interview-on-rejoin (the silent-rejoiner adoption gap); R-10 Row 10 RULED (a). 12 M + 1 A, integration-zigbee only.
instruction: context/instructions/2026-09-02_coder-lane_F-R4-1_interview-on-rejoin_coding-instruction_RULING-SLOTTED.md
lane: host-side Claude Code Coder, Windows desk (Git Bash; JDK 21.0.4; Gradle wrapper --offline), 2026-09-02 22:40–23:20 CDT.
-->

# F-R4-1 return — interview-on-rejoin

## §0 One-screen summary

**Verdict: DELIVERED, uncommitted, in-lane gates GREEN. §0 honored as (a):** relink ≠ adopt. Two admission TRIGGERS — **H-ii** (primary, the evidenced case: an unknown-sender HA frame while `isPermitJoinActive()` → resolve the IEEE → admit) and **H-i** (an ACCEPTED 0x0024 `SECURED_REJOIN`/`UNSECURED_REJOIN` for a device not in the adoption maps, window open) — feed the ONE existing path: EXACTLY `onDeviceAnnounce`'s two calls (`cache.recordAnnounce` + `interviewQueue.schedule`), never a bypass. Outside a window: noted ONCE per (window epoch, nwk), ignored. Denied/left/fresh joins raise nothing. The M9.4-TCJ §A.2 pin amended for exactly the accepted-rejoin-during-window case (source, MODULE_CONTEXT, the re-pin below). **ZERO commits by the lane.**

**Census: exactly 13 (12 M + 1 A)** at HEAD `f519f42` (clean at launch; duplicate-dispatch check: no prior return, no dirty paths). `git --no-optional-locks status --porcelain`, prefix `integration/integration-zigbee/`:
```
 M MODULE_CONTEXT.md
 M src/main/java/com/homesynapse/integration/zigbee/{CoordinatorProtocol,EzspCoordinatorProtocol,PendingInterviewQueue,ZclIngestionUnit,ZigbeeAdoptionSlice,ZigbeeIntegrationAdapter}.java
 M src/test/java/com/homesynapse/integration/zigbee/{EzspProtocolTest,FixtureReplayTest,PendingInterviewQueueTest,ZclIngestionUnitTest,ZigbeeTrustCenterJoinTest}.java
?? src/test/java/com/homesynapse/integration/zigbee/ZigbeeInterviewOnRejoinTest.java
```
vs "≈ 6–8 M + N test files A": main 6 M + MODULE_CONTEXT = 7 (inside); the 5 test M are forced — 2 by the abstract listener methods (java-patterns §12: no `default` for optional behavior), 1 the mandated TCJ re-pin, 1 T8's home, 1 the queue-source pins. ZERO module-info / build / catalog / schema / event-mint / payload diffs.

**Gates (in-session, the instruction's targeted allow-list):** `./gradlew :integration:integration-zigbee:compileJava :integration:integration-zigbee:test :integration:integration-zigbee:spotlessCheck --offline` → BUILD SUCCESSFUL · zero `-Werror` warnings · **581 tests, 0 failures, 0 errors** (560 → 581, +21: 12 new-class + 4 detection + 3 T8 + 2 queue) · task executed fresh (results XML 23:05:59 CDT) · Spotless clean · convention grep clean (no `Instant.now`/`currentTimeMillis`/`systemUTC`/`synchronized` in any touched file) · LF.

**Deferred Build Gate: YES.** `./gradlew check` (ArchUnit `NO_DIRECT_TIME_ACCESS` / `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` / the app + lifecycle suites) NOT run on this desk — owed against Nick's commit of exactly these 13 paths atop `f519f42`; the gate of record = CI on the push (law 16).

**Red-first (staged, the M9.6-RO idiom):** HEAD main code → compile-red on EXACTLY the four missing seams (`FRAME_LOOKUP_EUI64_BY_NODE_ID` ×12 sites · `lookupIeee(int)` ×5 · `Source`/`Pending.source()` ×14 · the two `onRejoinCandidate` overrides ×4). Stage A (seams declared, hooks inert, `lookupIeee` stub-empty) → **581 run, 15 RED** for the right reasons. Stage B → **581/581**.

| T | Scenario | Predicted | A | B |
|---|---|---|---|---|
| T1 | unknown sender + OPEN + HIT → 0x0061 once (`9A 6B`) · `rejoin_candidate … source=unknown_sender` · ONE interview · `device_proposed … source=rejoin` · listed ⇒ `device_adopted` · admitting frame skipped · next frame routes (`state_reported`), no 2nd lookup | RED | RED | ✓ |
| T2 | no window → no schedule/lookup; `rejoin_ignored_window_closed` once per nwk (2 nwks, 3 frames); WARN ×3 · T2b H-i closed ⇒ DEBUG-noted | RED | RED ×2 | ✓ |
| T3 | OPEN + MISS (0x01) → no schedule; `rejoin_candidate_unresolved` once; protocol `lookup_eui64_failed … status=0x1`; asked once for 2 frames | RED | RED | ✓ |
| T4 | accepted SECURED_REJOIN, unknown, OPEN → `source=tc_join`, ONE interview, no 0x0061, `source=rejoin`, unlisted ⇒ no adoption · T4b UNSECURED_REJOIN · T4c rejoin+announce ⇒ ONE interview, `source=announce` | RED | RED ×3 | ✓ |
| T5 | accepted rejoin for an ADOPTED (rehydrated) device → NO schedule, DEBUG `already_adopted` · T5b H-ii resolving to an adopted device: lookup once, no write (§3) | GbC predicted; DEBUG note made it RED | RED ×2 | ✓ |
| T6 | DENIED rejoin / DEVICE_LEFT → observability only | GREEN-by-constr. | GREEN | ✓ |
| T7 | reopen clears BOTH sets: the closed-note logs again; the nwk is looked up again | RED | RED | ✓ |
| T8 | 0x0061 encode nodeId u16 LE + decode status/EUI64 LE; status≠0 ⇒ empty + WARN; short ⇒ `EzspFormatException`; range IAE | RED | RED ×3 | ✓ |
| det. | `ZclIngestionUnitTest`: H-ii/H-i raise ×2 RED; never-raise pins (known, non-HA, fresh, denied, left, malformed) GbC | as stated | as stated | ✓ |
| plumb. | queue `Source` ×2 (declaration-level) | GREEN-by-constr. | GREEN | ✓ |

**The re-pin, verbatim** (`ZigbeeTrustCenterJoinTest` class Javadoc; every assertion BYTE-UNCHANGED, wording only):
> The join handler NEVER creates a device and NEVER publishes an event — and NEVER schedules an interview WITHOUT an open permit-join window, for a device already in the adoption maps, or on a denied join. Re-pinned at F-R4-1 (R-10 Row 10 (a), 2026-09-02): the M9.4-TCJ §A.2 pin ("adoption stays Device_annce-gated; the join handler NEVER schedules an interview") is AMENDED for exactly one case — an ACCEPTED rejoin (SECURED_REJOIN/UNSECURED_REJOIN) during an open window is an admission TRIGGER into the SAME announce path, pinned in ZigbeeInterviewOnRejoinTest; every other sentence of the pin stands and is pinned here.

§A-3's `.as` → "a fresh join never starts an interview — the F-R4-1 amended pin admits only an accepted REJOIN inside an open window" (its scenario is `UNSECURED_JOIN`, still never scheduling); §A-3/§A-4 DisplayNames carry the clause. **Refutable-by:** Doc 08 §3.4 :178 ("detected via ZDO Device Announce or triggered by permit-join") does NOT define rejoin-during-window as adoption — (a) stands as doctrine, :178 consistent with H-i/H-ii as its realization.

**INV/LTD sweep (every citation in the touched files):** LTD-11 MET (no `synchronized`; queue/protocol locks unchanged; the two new `HashSet`s run-thread confined, documented) · LTD-15 MET (5 new structured tokens, key sets test-asserted) · LTD-17 UNAFFECTED · Doc 15 zero-new-deps MET · INV-CE-04 MET (`lookupIeee` coordinator-neutral) · INV-RF-01/RF-03 UNAFFECTED (no I/O at initialize; the NCP timeout still reaches the watchdog arms) · INV-SE-03 UNAFFECTED · INV-ES-04/06/08 UNAFFECTED (no new event; payloads byte-unchanged) · SK-INV-02 MET (no wall clock; the epoch sets use none; tests inject `TestClock`) · never-false-ALIVE MET (`isPermitJoinActive` the only window truth; neither hook feeds availability) · never-synthesize MET · REG-INV-1 UNAFFECTED (`relink` byte-untouched) · Doc 08/02 adoption doctrine MET (adoption only via interview → proposal → adopted; consent still `adopt_devices`).

**Instrument limits:** the 0x0061 hop is UNMEASURED on silicon — the bellows-derived fixture is the only ground truth until R-4b's first ⏺ (criterion 0); `./gradlew check` not runnable in-lane (deferred). **CT filing date: 2026-09-02** (23:30 CDT; local clock + Windows tz db agree; msys `TZ=` fallback avoided).

## §1 The shape (detector / policy)

`ZclIngestionUnit` DETECTS (raises on every unknown-sender frame before the standing WARN; raises from `handleTrustCenterJoin` when the new `TrustCenterJoin.rejoined()` holds); `ZigbeeIntegrationAdapter` DECIDES in cost order — window FIRST, the cache's NWK→IEEE view, the once-per-nwk-per-epoch set, `protocol.lookupIeee` LAST (the command timeout / a dead transport still PROPAGATE to the watchdog arms — the `pingBasic` precedent), then adopted ⇒ DEBUG only, else `recordAnnounce` + `schedule(…, Source.REJOIN)`. Both sets clear in `openPermitJoinWindow()` (epoch = window; `attemptReopen()` untouched); provenance rides the queue entry into `onDeviceDiscovered(…, source)` and the `device_proposed` line only. Full deltas + gotchas + the R-4b log grammar: MODULE_CONTEXT §F-R4-1.

## §2 The bellows verification (the pre-write half, filed)

Fetched 2026-09-02 from bellows `dev`: `ezsp/v4/commands.py` — `"lookupEui64ByNodeId": (0x61, {"nodeId": t.EmberNodeId}, {"status": t.EmberStatus, "eui64": t.EUI64})`; `v13/commands.py` `from ..v12.commands import COMMANDS as COMMANDS_v12`, defining/overriding NEITHER lookup; `v14/commands.py` inherits v13 with `EmberStatus` → `sl_Status` (the `decodeStatus` width seam; outside the band). Cross-refs re-derived in the same pass (0x60/0x26/0x22) = the pinned constants. Encoded: nodeId u16 LE; status (version-keyed width) + EUI64 u64 LE; a miss ⇒ empty + WARN `zigbee.lookup_eui64_failed: nwk= status=` (the criterion-0 instrument — no second run to read the byte).

## §3 Deviations and pushback

- **[REVIEW] the adopted-but-cache-unindexed corner (T5b).** Spec §5 honored literally: the hook resolves the IEEE, finds it adopted, DEBUG-notes, writes nothing — that device's frames stay skipped for the epoch. Honest follow-on: `cache.recordAnnounce` (re-index only, no schedule) on that arm — hub-observable, so proposed, not shipped.
- **[REVIEW] `CoordinatorProtocol` +1 public method** (`lookupIeee`) — instruction-sanctioned (the M9.4-TCJ precedent); no shape test exists (grep clean).
- **[INFO] Tokens beyond the instruction's three:** WARN `zigbee.lookup_eui64_failed` (protocol layer) and DEBUG `zigbee.rejoin_candidate_ignored` (`reason=window_closed` for H-i outside a window — the unit's `device_join` INFO is the once-per-event observable; `reason=already_adopted`); `rejoin_candidate_unresolved` adds `cluster=`/`reason=`.
- **[INFO] Spec §5 dedup:** no `isPending` guard — the queue cannot hold two entries per IEEE; a rejoin re-admission resets the ladder exactly as a re-announce does. T4c pins the single drive.
- **[INFO] No fake `CoordinatorProtocol` exists in test-support** (repo-wide: `EzspCoordinatorProtocol` is the only implementer); the rig does not script 0x0061 — driven mode never opens a window (MODULE_CONTEXT gotcha).
- **[INFO] `InterviewAttempt` has no reason slot** — provenance rides the queue entry (+1 package-private NESTED enum, no new type file) and the log line. No `[BLOCKING]`.

## §4 Follow-ons (proposed, not shipped) + R-4b intake slots

(1) the T5b cache re-index · (2) an additive `source` key on the `device_discovered` payload (zero-cost under the event-model rule; the frozen contract keeps it a proposal) · (3) the ZDO IEEE_addr_req second surface if 0x0061 misses · (4) dedup/handler hygiene on a silent rejoin of an ADOPTED device (`handleAnnounce` clears both; the hooks do neither — pre-existing) · (5) the rig's 0x0061 script. **⏺ predictions:** `permit_join_opened` → `zigbee.rejoin_candidate: device=0x<mains EUI64> nwk=0x<live> source=unknown_sender` (the first mains device to speak — the held card's 0xf87d 5-minute-cadence device) → `device_proposed … source=rejoin` → `proposal_accepted` → `device_adopted`; if criterion 0 fails: one `zigbee.lookup_eui64_failed: nwk= status=0x…` + one `rejoin_candidate_unresolved … reason=lookup_miss` per nwk per window — the status byte adjudicates.

## §5 Commit message

Hub-authored at the audit (`_scratch/2026-09-02_core_F-R4-1_commit-msg.txt`); the "Stages exactly 13 (12 M + 1 A)" line = the §0 porcelain block; the one-sentence core: "F-R4-1 — interview-on-rejoin (R-10 Row 10 (a), relink ≠ adopt): two admission TRIGGERS (unknown-sender-in-window over 0x0061, accepted 0x0024 rejoin) into the ONE existing interview path; the M9.4-TCJ §A.2 pin amended for exactly that case; ./gradlew check deferred to this commit."
