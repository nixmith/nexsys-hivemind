<!--
file: context/audits/2026-09-03_intake_two-layer-audit_v61-beat-11_s31-read+PKG-SEC-2.md
purpose: ONE combined intake audit (v61 beat 11, Thu 2026-09-03 ~07:30 CT) of the two returns that landed after the v61 final close: (A) the read-only s31 evidence read of the 2026-09-02 nightly FAIL (return + raw-outputs exhibit, filed ~06:48 CT) and (B) the PKG-SEC-2 Coder return (8 M + 2 A on core a1c6966, filed ~07:30 CDT). Layer 1: each return read critically against its instruction. Layer 2: the hub's own re-execution — arithmetic, epoch conversions, the adapter's reopen path at source, the two locked-doc quotes at the docs repo, the fragment's defaults vs the adapter's reads, the census at porcelain, the diff at the bytes.
audience: Nick (one commit is his hands; two rulings are recorded) · the v62 hub (three follow-ons chartered here) · the lanes
state-type: intake audit (two-layer)
status: FILED. VERDICTS: (A) ACCEPT — with one refinement of its discriminator and one correction OF THE HUB'S OWN premise ("the 09-03 nightly MISSED" was wrong: it fired late and passed). (B) ACCEPT-WITH-RULINGS — R1 the permit-join default removal ACCEPTED (security-relevant); R2 Doc 12 §3.3 :133 PROCEED + a correction note chartered; R3 the three integration-zigbee files outside the file table ACCEPTED as the consequence of R1. The core msg file for Nick's hands: ../_scratch/2026-09-03_core_PKG-SEC-2_commit-msg.txt (stages exactly 10 = 8 M + 2 A).
-->

# Intake — two returns, two layers (v61 beat 11)

## Part A — the s31 evidence read (`context/audits/2026-09-03_s31-nightly-0902_evidence-read_return.md` + `…_raw-outputs.md`)

### A.0 Verdict: ACCEPT
The read is read-only (the §4 attestation lists eight `ssh pi` lines, all reads; the raw exhibit reproduces every output), verdict-first, and it adjudicates the five pre-filed hypotheses against evidence rather than narrative: **H-D SUPPORTED** (the s31 `turn_on` was dispatched 6.4 s after the suite's own `usb-reenumeration` leg killed the port and the watchdog reopened it), H-B/H-C/H-E REFUTED, H-A's surface shape subsumed. It also **refuted the hub's own premise**: the 09-03 nightly did not go missing — it fired late (~11:12Z, a catch-up on the bench card's return) and PASSED 8/9. The floor is back. The instrument was honest: the session ran without a path to the card and Nick pasted every output; disclosed in the return's frontmatter.

### A.1 Layer 2 — what the hub re-executed
| # | Claim | Re-executed | Result |
|---|---|---|---|
| 1 | Window 5.498771 s | 21.601076 − 16.102305 | **5.498771** ✓ |
| 2 | Dispatch 6.411 s after the reopen | 16.102 − 09.691 | **6.411** ✓ |
| 3 | Settle confirmed in 0.488 s | 22.689506 − 22.201111 | **0.488395** ✓ |
| 4 | `lastReported` = 08:28:55.870Z (predates the command) | 1788337735.86976 − 1788307200 (= 2026-09-02T00:00Z, re-derived: 1767225600 + 244 d) = 30535.870 s = 08:28:55.870 | ✓ |
| 5 | "No `network_resumed`/`network_up` after the reopen" — anomaly or construction? | `ZigbeeIntegrationAdapter.java` :423–:431 (boot: `resumeOrForm()` THEN `protocol.awaitNetworkUp()`) vs :841–:874 (`attemptReopen()`: `resetSession → startSession → resumeStored()` then `zigbee.reopened` — NO `awaitNetworkUp()`, no `network_up` log) | **By construction.** The reopen path re-initializes the network (`resumeStored`, :855) but does NOT wait for EMBER_NETWORK_UP before the production loop resumes, and logs no network line — so the return's proposed discriminator ("does a post-reopen `network_up` line appear on PASS nights") can never fire; only the gap discriminator stands. This is also the likeliest sub-mechanism of H-D: a unicast dispatched before the stack reported NETWORK_UP. |
| 6 | The adapter emitted no line for either s31 command | `ZigbeeCommandHandler.java` :351 logs `zigbee.command_result` only on the outcomes it owns; `sendUnicastLocked` returns a boolean that nothing logs | The silence is the instrument gap (convention 17 / arc 19): whether the NCP accepted the `turn_on` unicast is unrecorded. |
| 7 | The 09-03 nightly line | raw exhibit command 1–2: `2026-09-03 quiesced AUTO floor: 8/9 PASS … ON-latency 0.09s`; bundles stamped `20260903T111217Z` | ✓ — the hub's beat-10 "MISSED" was WRONG; corrected in the spine this beat. |

### A.2 Rulings and follow-ons
- **OR-NIGHTLY-0902-S31 → ADJUDICATED (class named), held OPEN until its rows charter.** Class: transport collateral from the suite's own port-kill leg, on a reopen path that resumes without a NETWORK_UP gate. Two candidate rows for the v62 docket / the R-5 charter: **REOPEN-NETUP** (Core, adapter: `awaitNetworkUp()` — or an equivalent NETWORK_UP gate — on `attemptReopen()` before the loop resumes, plus one anti-vacuous line for the unicast accept/reject at the drive site) and **S31-SETTLE-AFTER-REENUM** (bench: gate `command-confirm-s31` on a post-reopen settle or a NETWORK_UP token — the s31/nightly fence lifts at R-5; the row waits there). Neither dispatches before Nick's word.
- The discriminator, refined: across the PASS-night bundles, extract `port_reopened → s31 ACCEPTED` gaps only; an outlier on 09-02 (6.4 s) vs the PASS nights pins the timing class. A read-only follow-on when a quiet hour exists; not urgent — the floor is back.
- Two flagged-not-adjudicated items ride into the R-5 charter's read-set: the no-edge `turn_off` that CONFIRMED (the 08-02 "no edge ⇒ no report" story needs the readback channel accounted for) and the zero-line journal despite a `uhubctl` cycle in-window (the transport record rests on the app log alone).
- Hub self-correction, recorded: the hub asserted a fact about the card ("MISSED") from an inference (the card was out at 03:32) instead of from the instrument; the nightly's scheduler catches up on boot. Mint: **a missed-event claim about the card is a claim; the card decides.**

## Part B — PKG-SEC-2 (`context/audits/2026-09-03_PKG-SEC-2_return.md`, 10,237 B; core `a1c6966` + 8 M + 2 A)

### B.0 Verdict: ACCEPT-WITH-RULINGS
The return is §0-first, census-exact, red-first staged (HEAD 13/13 RED by the guard → stage A 11 RED for the right reasons → stage B 75/75 · 27/27 · 581/581), and it stopped on the right question instead of shipping past it. The code is small, correctly ordered and correctly locked. **The lane found a security-relevant defect the instruction had not seen**: composing the fragment at Phase 1 would have made its `permit_join_duration: 120` default operative — `mergeDefaults` (Doc 06 §3.1 stage 4) inserts every fragment default into the model on every boot, so every unconfigured boot would have opened the Zigbee network for joins for 120 s, which is the moment the network key is handed to any device that asks. The lane removed the one default, pinned absence at the model and at the supply site, and mutation-proved the pins. That is the right call and the hub confirms the mechanism at source (B.1 #4).

### B.1 Layer 2 — what the hub re-executed
| # | Claim | Re-executed | Result |
|---|---|---|---|
| 1 | Exactly 10 at porcelain on `a1c6966`, nothing staged | `git --no-optional-locks status --porcelain` + `log -1` | **8 M + 2 A**, HEAD `a1c6966`, paths match the return's block ✓ |
| 2 | 208 insertions / 38 deletions across the 8 M; the new tests 501 + 80 lines | `git diff --stat` · `wc -l` | 208(+)/38(−) · 501 · 80 ✓ |
| 3 | The queue/drain ordering is race-free and never held across I/O | `HomeSynapseCore.java` diff: `registerIntegrationSchema` (queue or direct under `schemaLock`); `installSchemaRegistry` (drain, then publish, under the same lock, before `load()`) | Correct: publication and the queue share one lock, so a racing call lands in the queue (drained) or on the visible registry — never lost; the registry's parse is in-memory; a malformed fragment propagates with the registry unpublished (FATAL, loud). `Main` supplies before `start()`; the post-start W10 call removed ✓ |
| 4 | The default would have become operative; the adapter treats absence as "no window" | `ZigbeeIntegrationAdapter.java` :85/:93/:130 — the adapter reads exactly THREE keys by name: `serial_port`, `permit_join_duration`, `adopt_devices`; `openPermitJoinWindow()` returns on an empty Optional | Mechanism confirmed; the removal is the correct fix. **The other defaults now operative (`watchdog_interval_seconds` 30 · `availability.*` 10/25 · `topology_scan_interval_hours` 0 · `telemetry_threshold_seconds` 10 · `route_health.*`) are read by NOTHING in the adapter — harmless today, a dead-config class for a hygiene row (declare only what the code reads, or wire them).** |
| 5 | Doc 12 §3.3 :133 carries the post-Phase-6 sentence; Doc 06 §3.2 :127 prescribes once-at-startup composition | `homesynapse-core-docs/design/12-*.md` :132 · `06-*.md` :127 | Both quotes verbatim-true. The two LOCKED docs disagree with each other; the code before PKG-SEC-2 matched neither (no revalidation ever existed). |
| 6 | INV-CE-02 met | `config/configuration/MODULE_CONTEXT.md` :177: "Zero-configuration first run. Every config key has a schema default. Empty or absent file → complete ConfigModel from defaults." | Read with its purpose (a complete, correct model with zero configuration), MET: the conservative default for a key whose ABSENCE carries meaning lives in code and is documented in the schema description; T7/T7b pin the zero-config boot. Read literally ("every key has a schema default"), the fragment now has seven scalar/array keys without one (`serial_port`, `baud_rate`, `adapter_type`, `channel`, `permit_join_duration`, `adopt_devices`, `profiles_path`) — as it already did for six of them before this WU. **Ruling: the purposive reading governs; a one-line INV-CE-02 gloss is chartered for the config MODULE_CONTEXT (B.2).** |
| 7 | LF, no wall clock, no `synchronized`, no catalog/module-info change | greps at the bytes | ✓ |
| 8 | 75/75 · 27/27 · 581/581; the 4-test mutation proof | not re-runnable at the bridge | DISCLOSED — CI adjudicates (law 16). |

### B.2 Rulings
- **R1 — the `permit_join_duration` default removal: ACCEPT.** Security-relevant; mechanism confirmed at source; the pins are mutation-proven by the lane. Recorded as a minted class: **a schema default is dormant until its fragment composes — audit every default's absence semantics before composing a fragment earlier.**
- **R2 — Doc 12 §3.3 :133: PROCEED; a correction note is CHARTERED (docs repo, Nick's hands).** The sentence describes a recompose-and-revalidate mechanism that was never built, on a discovery premise DECIDE-04 retired; Doc 06 §3.2 :127 and C7 :477 prescribe exactly what (a′) does; the code moves from conforming to neither to conforming to Doc 06. Not silent: this ruling, the commit message and the note. The note's text (the AMD-71 correction-note form, one paragraph): "*[CORRECTION NOTE — 2026-09-03, PKG-SEC-2 / R-10 Row 13 (a′)]: Step 1.1's 'core-only schema composition … registered after Phase 6' and the post-Phase-6 recomposition/revalidation describe a mechanism never built on the retired ServiceLoader-discovery premise (DECIDE-04). Since core `a1c6966`+PKG-SEC-2, every hosted integration's schema fragment is supplied to `HomeSynapseCore.registerIntegrationSchema` before `start()` and composes in Phase 1 ahead of validation, per Doc 06 §3.2 and C7; nothing revalidates after Phase 1.*" — v62 hands Nick the docs commit line.
- **R3 — three integration-zigbee files outside the file table: ACCEPT** (the fragment, the factory Javadoc, the MODULE_CONTEXT are the consequence of R1).
- **[INFO] accepted as filed:** the queue shape (no ctor overload) · `stage=direct` token · the package-private supply seam + its 3-test pin · `HeroLoopHardwareFreeIT:357` untouched. **Follow-ons chartered for v62:** (1) the Doc 12 correction note (Nick's hands) · (2) a one-line INV-CE-02 gloss + the dead-config observation in the config MODULE_CONTEXT · (3) the Doc 08 §9 :871 "Default 120" row · (4) re-time the hero IT's registration pre-start · (5) the return-cap lesson (B.3).

### B.3 Process notes
- The lane spent six compression passes fitting a 10 KB cap (14.7 → 10.2 KB). **Mint: a return cap is a per-section budget stated in the instruction (§0 ≤ 3 KB · the table ≤ 2.5 KB · the rest), with a hard ceiling ~40 % above the target — not a ceiling to trim toward after the fact.**
- **R-4b coupling (load-bearing):** with the default gone, an ABSENT `permit_join_duration` means NO join window — and F-R4-1's H-ii admits only inside an open window. **R-4b's packet must SET `permit_join_duration` on the held card for the re-adoption run and remove it after.** And because the fragment now validates the real section at Phase 1, step 0 of R-4b pastes the held card's `integrations/zigbee.yaml` for a pre-validation against the fragment (unknown keys WARN; out-of-range values ERROR + key REMOVED — the boot continues either way, but a removed key on the day is a wasted hour).

## §C What rides forward
1. **Nick's commit** (his hands): `../_scratch/2026-09-03_core_PKG-SEC-2_commit-msg.txt`; stages exactly the 10; push; the CI verdict banks as one line (the deferred gate closes on green).
2. **v62:** the Doc 12 note's commit line for Nick · the FAILCHAN instruction after PKG-SEC-2 lands · the R-4b packet with the two step-0 items above · the REOPEN-NETUP + S31-SETTLE-AFTER-REENUM rows in H10 form · the INV-CE-02 gloss.
3. **The OR entry** stays OPEN as "adjudicated, rows pending".
