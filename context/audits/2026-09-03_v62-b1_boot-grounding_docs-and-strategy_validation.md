<!--
file: context/audits/2026-09-03_v62-b1_boot-grounding_docs-and-strategy_validation.md
purpose: THE v62 BOOT GROUNDING READ — Nick's brief ("second-check and validate all homesynapse-core-docs and context documentation; develop an executive-level understanding of the plan, strategy and objectives"). Four read-only grounding lanes (in-conversation agents, ledger D12) over the docs repo, the strategy layer, the planning corpus, the founder documents and the core tree as built — audited two-layer: every load-bearing finding the hub re-executed at the bytes is marked ✓HUB; the rest is the lane's word, quoted with its citation. Chat is not a storage tier (arc 34): this file is the evidence base for the docket addendum (rows 17–21), the docs-repo correction commit, and the README currency row.
audience: the hub (v62+) · Nick (§0 + §6)
state-type: audit (filed)
status: FILED at v62 beat 1 (Thu 2026-09-03, instrument 19:2xZ). Lane returns were in-conversation (not on disk); this file IS the record. Nothing here is adopted until a word or a beat says so.
-->

# v62 beat 1 — the boot grounding read: docs · strategy · planning · founder plan · core as built

## §0 The executive card (the hub's synthesis; every line traceable below)

1. **What the system IS (the docs' own words):** a local-first, event-sourced smart-home runtime — Java 21 / JPMS / SQLite WAL / in-process virtual-thread bus; Zigbee the sole MVP adapter (LTD-12); Preact observability dashboard; Pi 5 reference, Pi 4 the floor (`README.md:3-5`). Constitution = `Architecture_Invariants_v1.md` (175 invariants / 53 categories, watermark AMD-99 ✓HUB); implementation register = 19 LTDs; docs 01–18 all Locked.
2. **The north star:** *"the harness enforces; the model only proposes … the worst outcome of a bad inference is a rejected action, never an unsafe physical state"* (north-star `:76`). Market frame = the Substrate Thesis (occupy the layer that compounds with model progress).
3. **The language law (D5):** the deterministic floor is MISSING from the field, not SUPERIOR; never "superior"; the four-qualifier law on every sensing sentence; no claim outside the register (H9).
4. **The wedge (v61-b2 card, R-1(b) PROPOSED):** THE MEASURED CORPUS — device profiles admitted as data and published with their bench verdicts (`confirmed|unconfirmed`, never rounded up) + the four presence metrics as the corpus's second surface; the explain surface is its demo face; the code SDK a rung.
5. **Household goal (R-4a):** SHORT — one home lived in on the SHIPPED artifact (R-4b · R-4.5/R-5 · the FE honesty rows); LONG — the three explain questions answered from a never-evicted log · the four presence metrics measured in THIS home · zero cloud dependence. **Company goal (R-5a):** SHORT — Nov-25: a product, a name, a surface, a fleet story, all evidence-backed; LONG — hold the reference implementation AND the measured corpus of the enforcement floor when the floor becomes a purchase requirement; the moat = corpus + register + spine (not the code — Apache gives it away by design).
6. **The register:** C-001 LIVE (NARROW, `7c57d7f`, by delegation, refutable by REVERT); C-002 SLOT fenced to R-4b. The register only grows; a claim is never edited.
7. **Licensing today vs the plan:** both repos' LICENSE files are PROPRIETARY ("All rights reserved"; SPDX `LicenseRef-NexSys-Proprietary`) ✓HUB-by-lane; the Apache-2.0 flip is strategy-locked and PUBLISH-GATED; its chain = confirmatory IP assignment → OA §VIII(4) Member consent → LICENSE flip IN THE SAME WINDOW as the rename in-tree; 10-01 quarterly = the gate check; fallback: rename slips past 10-31 → flip on the un-renamed tree.
8. **Capital posture:** bootstrap-first; a dedicated five-figure fund covers P0–P3 with headroom; credit is reserve; no investor conversation before P2; NSF SBIR Phase I (4 Mar 2027, ≤$305 K, zero equity) is the non-dilutive anchor (FOP `:409`, absorbed by v1.2 A-4).
9. **The program of record:** the September plan (RATIFIED + §0-bis) — wk 2 (09-07→13) the WUs land + the brand loop; wk 3 (09-14→20) the fences fall inward (R-4b four-of-four → C-002; R-4.5/R-5; F-S8 + FE-STATE-DIALECT; B-5 story kit begins; B-1 identity on the opinion); wk 4 the story kit's first draft. Standing: ≥1 outward-chain act per week (v1.1 §3.6); the corpus read ~1/wk.
10. **The sitting (Fri–Sat 09-05/06):** R-1..R-5 + 16 docket rows; the answer shape = the deliberation guide Part 5 (one message). Rows 10 and 13 are already worded by dispatch; Row 6's premise is measured (O-2). The hub's own call: ADOPT-ALL-RECS with Nick's eyes on Row 2 and Row 12's fallback date.
11. **Fences (standing):** no public brand use before a written-opinion-backed R-1 (G-2; hard stop Fri 09-18) · `README.md:117` until W2-3 · s31/nightly HANDS OFF until R-5 · one lane on the core tree at a time · the hub never implements · no community/beta before R-10 + the written privacy posture · bench floor 8/9.
12. **What this read ADDS (new, source-verified):** ONE boot-honesty defect (§1.1 EXITCODE-UNWIRED, the FAILCHAN class) · README/CONTEXT currency drift ×4 at HEAD · five docs-repo defects beyond the ruled Doc 12/06 conflict · two strategy-file tensions with D5 · three planning inconsistencies · four founder-plan tripwires the spine does not carry.

## §1 The core tree as built — hub re-executions (✓HUB) and the lane's findings

### 1.1 ✓HUB — EXITCODE-UNWIRED: the unit's exit-code contract is unimplemented (a FAILCHAN-class boot-honesty defect)
- `app/homesynapse-app/src/main/java/com/homesynapse/app/ExitCode.java:25` defines `CONFIGURATION_FAILURE(10)` (also 11 PERSISTENCE · 12 EVENT_BUS · 13 SUBSYSTEM_INIT_TIMEOUT · 99 UNEXPECTED). **`grep -rln ExitCode` over `lifecycle/…/src/main`, `app/…/src/main`, `platform/` returns ONLY `ExitCode.java`.** ✓HUB
- `Main.java:63` `public static void main(String[] args) throws Exception`; `:148` `manager.start();` unwrapped; the only `System.exit` is the token-CLI dispatch at `:74`. ✓HUB
- `HomeSynapseCore.start()` `:441-448`: catches the fatal, logs `"Fatal error during startup; tearing down initialized subsystems"`, calls `shutdown("startup failure: …")`, **re-throws** (`throw fatal;` `:448`). ✓HUB → the exception escapes `main` → the JVM exits **1**.
- `distribution/systemd/homesynapse.service:61-66`: `Restart=on-failure` · `RestartSec=10` · **`RestartPreventExitStatus=10`** with the comment *"CONFIGURATION_FAILURE (10) is deterministic — a bad config will fail identically on every restart, so do NOT loop on it"*; `:19-20` `StartLimitIntervalSec=300` / `StartLimitBurst=5`. `SuccessExitStatus` appears nowhere under `distribution/`. (lane; the unit was read whole)
- **Consequence:** a deterministic bad config exits 1, matches `on-failure`, restarts every 10 s, and lands start-limited DOWN after five tries in 300 s — the exact loop the unit's own comment says it prevents. The mirror of OR-FAILCHAN's §6-B (there: a healthy stop reports as failure; here: a failure reports through a channel the unit cannot read). **Disposition:** OR-FAILCHAN instance 5 → rides the FAILCHAN charter's classification table (Block 2) as its own row with an H10 (wire `ExitCode` at `Main` — catch at `main`, map the FATAL subsystem → code, `System.exit(code)` after the hook has run — vs. retire the unit line). The C12-04 contract (Doc 12 `:457`: non-zero exit + a structured log entry naming subsystem/phase/recommendation) is half-built: the log line exists (`:442`), the code does not.

### 1.2 ✓HUB — README / CONTEXT currency drift at HEAD `ef02d13` (Nick's hands; a docs-hygiene micro-row)
- `README.md:262-263` *"There is no unauthenticated health endpoint"* — false since R-9 `e845cd9` (`homesynapse.service:11-12`, `:53` the `/health` probe in ExecStartPost). ✓HUB (README lines read)
- `README.md:264-266` *"ships with `PrivateDevices=yes` … cannot see a USB dongle"* — false since R-3b `7c57d7f` (`homesynapse.service:100-104` `PrivateDevices=no` + `DeviceAllow=char-ttyUSB/ttyACM` + `SupplementaryGroups=dialout`). ✓HUB
- `README.md:288-294` describes the pre-`beb664e` dashboard (the null-field throw "is the first queued frontend item") — landed. (lane)
- `CONTEXT.md:51-53` *"the Java toolchain downloads the correct JDK"* vs `README.md:32` *"must already be installed … no toolchain auto-download"*; the catalog has plain `java-language = "21"`. (lane)
- `README.md:117` is the SQLite-WAL single-writer row of the architecture diagram; the string `W2-3` is not in the file — the fence lives in commit `9f99368`'s message. (lane) Rider: `.github/workflows/frontend.yml:43,45` still `@v4` while `89a912e` claims all three workflows moved to `@v5`. (lane)
- MODULE_CONTEXT lag: `web-ui/dashboard/MODULE_CONTEXT.md` has zero `FE-HONEST` tokens though `f519f42` added `src/lib/registry.ts`; `api/rest-api/MODULE_CONTEXT.md` has zero `CG-1/2/3` tokens (consistent with "filed as proposals, not edits" — but the CG instruction's read-set must cite git, not the module doc). (lane)

### 1.3 The reopen path (the s31 adjudication, re-confirmed) ✓HUB
`ZigbeeIntegrationAdapter.java:429-431` boot = `startSession · resumeOrForm · awaitNetworkUp`; `:841-874` `attemptReopen()` = `resetSession · startSession · resumeStored` — no `awaitNetworkUp`, `permitJoinDeadline = null` (reopen ≠ boot; the window is NOT renewed). `sendUnicastLocked` (`EzspCoordinatorProtocol.java:1669`) logs only the FALSE arm (`zigbee.aps_unicast_rejected` `:1694`); callers `:1810/:1856/:1926` convert `false` to `Optional.empty()` silently; the success arm is never logged. (lane, consistent with the b11 audit A.1 #5/#6) → REOPEN-NETUP's instrument line is confirmed as a real gap.

### 1.4 As built, in one line each (lane; heads read)
Boot: 7 startup phases 0–6 inside a 10-state machine (`HomeSynapseCore.java:129-157`; `installSchemaRegistry` `:493` between `registerCoreSchema` `:491` and `load()` `:495` ✓HUB) · 22 Gradle includes (19 production + 2 testing + 1 spike) · Javalin 6.7.0 / sqlite-jdbc 3.51.3.0 / Jackson 2.18.6 / SnakeYAML Engine 2.9 / JUnit 5.14.3 / ArchUnit 1.4.0 / jSerialComm 2.11.2 (`gradle/libs.versions.toml`) · CI: `ci.yml` "Build & Check" (`./gradlew check`, the gate of record) · `install-smoke.yml` (amd64+arm64 matrix) · `frontend.yml` (`npm run verify`) · tests at HEAD: zigbee 581/581 · lifecycle 75/75 · app 27/27 (commit `ef02d13`); dashboard 211; rest-api 125 `@Test`. jqwik is NOT a dependency.

## §2 The docs repo (homesynapse-core-docs `a53f474`) — validation findings

### 2.1 ✓HUB — confirmed at the bytes
- `governance/Architecture_Invariants_v1.md`: `## 40.` `:1571` · `## 41.` `:1581` · **`## 48.` `:1628`** — no `## 42.`…`## 47.` headings exist; the §17 index (`:1082-1091`) attributes AMD-88..93's ten invariants to §42–§47; their bodies sit unheaded inside §41 (`:1595-1626`). Cross-references to §42–§47 are unresolvable.
- `foundations/HomeSynapse_Core_v1_Glossary.md:362` — *"`event_id` — ULID … (LTD-04: `UlidCreator.getMonotonicUlid()`)"* — the library LTD-04 REMOVED on 2026-03-20 (DECIDE-02; hand-rolled `UlidFactory`, `Locked_Decisions:151`). The glossary is Locked v1.0 (2026-02-22) and never folded.
- `governance/HomeSynapse_Core_Locked_Decisions.md:6` *"amendments require the process defined in §18"* (also `:17`) — the amendment process is `## 14.` at `:912`; `:925` says §14 correctly. Two of three references dangle. §15 sits at `:748` between §9 and §10 (LTD-19 lives there).
- `design/00-navigation-index.md`: the header (`:4`) and the watermark-currency line (`:46`) are behind the register — the amendment table's last row is AMD-95 (`:100`); on-disk `design/amendments/` holds AMD-98 and AMD-99; the register's own total line (`:1094`) says 175/53 @ AMD-99. An agent following the index's instruction (`:7`, verify amendments cited as authority) cannot find AMD-96..99 in it.
- Doc 12 `:133` vs Doc 06 `:122,:127` — the ruled conflict (v61 b11 R2: PROCEED per Doc 06/C7; the correction note chartered). Both quotes verbatim-true (re-read by the lane; the hub's b11 audit already re-executed).

### 2.2 The lane's further findings (quoted; not re-executed by the hub)
- `Refined_Repo_Architecture_v2.md:595-617` "19 modules total" vs 22 includes — `core:value-model` (zero mentions doc-wide), `testing:integration-tests` (DEC-M3-13, post-doc), `spike:wal-validation` (in the tree at `:457`, not in the table).
- `00-navigation-index.md:24` cites AMD-96's EZSP band "v13–v14" as Doc 08's status; AMD-98 narrowed it to ==13 (v14 silicon "silently deaf").
- `Design_Review_Amendments_v1.md` is the frozen 2026-03 review artifact (AMD-01..24 only); the live ledger is `design/amendments/` (55 files, top AMD-99). The "correction-note form" is a convention exercised at Doc 06 `:129` ("per ratified AMD-71 §2.1 … body text preserved per the correction-note convention") — never defined in a governing document; AMD-71 itself is the hybrid config directory layout.
- Glossary: no entries for Adoption (inline in Discovery §2.7/§3.23), Interview (absent), Confirmation (carried by Pending Command Ledger §3.14) — the three words the R-4/R-4b program uses most. A Glossary amendment candidate rides the R-4b close.
- INV-CE-02 (`:518-520`): *"Every configuration option must have a sensible default"* — PKG-SEC-2 R1 removed `permit_join_duration`'s default on security grounds; the b11 audit read INV-CE-02 purposively (a gloss line chartered → Row 20).
- Licence posture: both LICENSE files proprietary (docs: "NexSys Technologies 2025-2026"; core: "NexSys 2026" — different wording/years); no AGPL/BUSL/source-available anywhere in governance; Doc 18 records "community-content-never-paywalled" as a standing constraint.

**Disposition:** ALL docs-repo items are Nick's hands. Batch as ONE correction commit with the Doc 12 §3.3 note (Block 4 grows from 1 M to ~5 M): the Doc 12 note (the b11 audit R2 paragraph) · `Architecture_Invariants_v1.md` §42–§47 headings restored (heading-only; no body change) · Glossary `:362` corrected to `UlidFactory` (a one-line correction note, Locked-body preserved) · Locked Decisions `:6`/`:17` "§18" → "§14" · the nav-index watermark/currency lines to AMD-99 175/53 + AMD-98's band correction on the Doc 08 row. The module-table row (19 → 22) and the Glossary term gaps are AMD-class (a Refined-Repo-Architecture currency note; a Glossary amendment) — docket rows, not this commit.

## §3 The strategy layer — tensions the lane found (quoted); the hub's dispositions
- `Six_Battlefields_MVP_Strategy.md:22` *"It wins by demonstrating superiority on six battlefields where every competitor fails"* and `:54` (Battlefield 3: the win is answering "why did the porch light turn on at 3am?") vs D5 (`strategy-of-record:6`; `claim-register:14` *never "superior"*) and the RS-3 preamble (`claim-register:13`: HA 2026.9 answers "why did it fire" — any surface implying otherwise is refutable from 09-02). The file is `status: CURRENT`, D-4 (Nick's long-form; leave it alone). **Disposition:** v1.2 A-8 candidate — a one-line "read under D5; internal scope tool only" banner in `strategy/README.md` (hub-writable), never an edit to the D-4 file.
- `strategy/README.md` (last-verified 08-18) omits the strategy of record, the claim register and the two September v61 files from its reading order — a reader lands on the July files first. **Disposition:** README census refresh at the v1.2 splice (hub; 1 M).
- `Revenue_Model_and_Licensing_Strategy.md:21` "Apache 2.0 … forever" vs OA §VIII(4) PolyForm-NC — reconciled only by the entity ruling + R-2's consent instrument (nothing to change; noted so no lane quotes the July file as the flip's authority).
- The north-star overview's agent-seam language reads present-tense (`:94-98`) while its own filing note (`:5`) marks it DESIGNED-FOR, not TRUE-TODAY — every outward use applies the note.
- The two July prioritization tests (six battlefields; three compounding assets) coexist without cross-reference; v1.1 is operative.
- NFCU: the entity assessment carries no NFCU row; the NFCU return (`context/handoff/2026-08-28_NFCU_application_return.md`) is the source; the pelton scoping ruling `:16` records FIN-1's NFCU row CLOSED 09-01. The "10-01 quarterly" is the flip's gate check (the card `:16,:36`), not an entity filing.

## §4 The planning corpus — inconsistencies (quoted); dispositions
- **Row 12 (R-4b) says "wk 3"; the program (v61 b9, newer) schedules S4 = Sat/Sun 09-06/07 and the brief asks Nick to pick the day.** The program governs (newer; F-R4-1 landed early); Row 12's text is stale by a week. **Disposition:** the docket addendum carries a one-line Row-12 currency note; the ruling is unchanged ((a), with (b) as the dated fallback if F-R4-1 had slipped — it did not).
- The deliberation guide `:44` still says O-2 is "a step in R-4b's packet"; the O-2 card says CLOSED/MEASURED and step 0 no longer owed. **Disposition:** the guide is a Nick-facing read for the sitting — one currency line at its head (hub; rides beat 2 with the R-4b packet).
- The September plan §1 bills the docket at "~3–4 h deep" (`:25`) and "docket words 1" (`:28`); the program prices it at ~1 h. **Disposition:** the program's figure stands (the cards are on disk; the sitting is words).
- The Pelton-week plan (`2026-08-23_pelton-week_plan-of-record.md:5` "FILED") predicts a docket four rows of which never entered the 16 (Doc 09 §15 Q1 · the JDK trajectory · the IR emit question · the events endpoint) and is never marked superseded. **Disposition:** a one-line status flip to "EXPIRED BY SPAN (Aug 24–30); the September plan governs" + the four rows named as docket-addendum candidates (rows 21+; Nick's word whether they enter now or at the R-5 charter).
- The operator brief §A (DONE) keeps an un-run porcelain assert reading "8 ' M', 2 '??'" beside a census card reading "2 A" — cosmetic; superseded by the v62 brief (this beat).
- Row 16 is ruled AT H8 (S5, wk 2) while its docket card asks for a word at the sitting — the card's own text says PRE-LEAN, rule at H8; no conflict, but the Part-5 shape should say "Row 16: rule at H8 (no word needed)".

## §5 The founder documents — what the spine does not carry (quoted)
- `Founder_Operating_Plan_v1.md` (compiled 2026-08-10; "Living document. Revise quarterly"): the presence-quality benchmark = *"latency-to-detect, latency-to-clear, false-clear-while-stationary, false-hold-after-vacancy"* (`:54`); *"If you do exactly one thing from this document: build the presence-quality benchmark and dataset"* (`:709`) — absorbed by the v61-b2 card (A-1: the corpus's second surface; the dataset at P3/P4). No NexSys price or revenue model in the FOP; no "Nov-25 runway" line — Nov-25 is the fusion plan's MVP-launch target (`fusion-program/2026-07-24_phase-plan-of-record.md:20`).
- **Dated tripwires the spine does not carry:** CEDIA Expo Sept 1–4 Denver (`:386`, now passing; the card enters it as a P3 watch row — installer discovery, not launch theater) · **EU market posture decision 11 Sep 2026** (`:410`, CRA reporting duties) · **Activate Fellowship Cohort 2027 opens 15 Sep 2026** (`:411`) · NSF Project Pitch Sept/Oct 2026 (`:408`; 2 pitches/company/year cap `:423`) · SAM.gov "start now, 6 weeks" (`:407`). **Disposition:** an H10 to Nick (in the v62 brief §G): whether v1.2 A-4 gains the EU-posture and Activate rows beside the NSF row, or they are declined by name (the R-3(b) form).
- `Technical_Roadmap_v1.md` is a personal EE study curriculum ("Scope: technical only"), not a product roadmap: no phases dated, nothing marked done; detection/QCD theory as the hinge; Tier A–C lab hardware. It does not conflict with the spine; it competes for the same hours (A-14 = 15 h/wk floor). Not a hub matter beyond the corpus-read cadence.
- The FOP's "smart home operating system" Class-I risk note (`:462`) vs `nexsys-hivemind/START_HERE.md` ("smart-home OS") — a register/language item for the story kit (B-5), not code.

## §6 The hub's re-execution table (what was re-executed; what was not)
| Claim | Instrument | Result |
|---|---|---|
| ExitCode unreferenced; main throws; start() unwrapped; start() re-throws | grep + sed at `Main.java:63,74,148`, `ExitCode.java:25`, `HomeSynapseCore.java:441-448` | ✓ CONFIRMED |
| README :262-266 stale (health endpoint; PrivateDevices) | sed `README.md:262-266` | ✓ CONFIRMED (the unit lines from the lane's whole-file read) |
| Invariants §42–§47 absent | `grep -n '^## 4[0-9]\.'` | ✓ CONFIRMED (40, 41, 48, 49) |
| Glossary :362 UlidCreator | sed | ✓ CONFIRMED |
| Locked Decisions :6 "§18"; process at §14 | grep | ✓ CONFIRMED |
| nav-index behind the register | sed `:4,:46` | ✓ CONFIRMED (currency lines cite AMD-93/94/96) |
| Operator brief §A assert vs card | sed `:16,:28-29,:33` | ✓ CONFIRMED (cosmetic) |
| FOP dates :386/:410/:411 | sed | ✓ CONFIRMED |
| installSchemaRegistry order; permit_join_duration no default; attemptReopen no awaitNetworkUp | preflight Check 11 (12:56Z) | ✓ CONFIRMED |
| Module table 19 vs 22; AMD-96/98 band; CONTEXT.md JDK line; frontend.yml @v4; MODULE_CONTEXT token gaps; strategy/planning quotes | — | NOT re-executed (lane's word; cited) |

**Not read by anyone this pass:** design docs 01–05, 07–11, 13–18 bodies; the invariants §1–§16 bodies; the three strategy .docx files; `counsel-package/`; the FQ rulings and lane charters; the Technical Roadmap Parts 2–3, 5–6. A second grounding pass is chartered only if a brief pulls on them.
