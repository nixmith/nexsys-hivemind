<!--
file: context/audits/2026-09-05_CG-123_intake_two-layer-audit_v65-b5.md
purpose: The hub's two-layer audit of the CG-1/2/3 return (`context/audits/2026-09-05_CG-123_return.md`, 11,969 B) at the bytes of the core working tree at `e5fa035` — the pre-filed rules R1–R8 and predictions P1–P5 (the v65 b1 audit §6) adjudicated FIRST; the one `[REVIEW]` ruled; the code second-checked for maintainability and scalability on Nick's ask; the msg file + census card for Nick's hands; the FE-113 wire slots fixed from the landed bytes.
audience: Nick (§0 · §5 the card · §6 the FE slots) · the v66 hub (the ruling of record on R1; the two rows this audit opens)
state-type: intake audit
status: FILED v65 beat 5 (post-close; Sat 2026-09-05 ~22:55 CT; instrument 2026-09-06T03:55Z). Layer 2 at 03:42–03:5xZ on the lane's tree (porcelain 17 M, 0 A, 0 D).
-->

# CG-123 — the intake audit, two layers

## §0 Verdict card
- **VERDICT: ACCEPT-WITH-ONE-RULING (R1 → ACCEPT the yield as implemented).** Census **17 = 17 M + 0 A + 0 D — EXACT at `git --no-optional-locks status --porcelain` on the lane's tree at 03:42Z**, paths identical to the return's §0 and the instruction's Files table; `module-info.java` untouched (0 in `diff --name-only`); `build.gradle.kts` untouched; the ITs untouched; `web-ui/**` untouched. Desk gates: the lane's claim (automation 203 → 207 · rest-api 147 → 152, 3 skipped unchanged · `:lifecycle:lifecycle:compileJava` · `spotlessCheck`, 0 warnings under `-Werror`) — NOT re-run by the hub (disclosed); **CI on Nick's push is the gate of record**; the Deferred Build Gate (ArchUnit in `:app`) rides it.
- **Predictions adjudicated first (H12):** **P1 MET** (17 M exact) · **P2 MET** (zero module-info hunks; the DP-3 default held — `EntityRegistry` on the exported gateway compiled clean under `-Xlint:exports`) · **P3 MET** — the lane found the instruction's permit list incomplete at source (`NumericCondition.java:27` carries a `Selector`; §DP-2's list omitted it) and five `[INFO]` corrections with line cites (`componentsOf` at `:440` not ~:770; +5 rest-api tests not +4; the two `@see` FQNs; arch Rules 4/5 as subject scope) — the fourth core lane in a row to out-verify the hub · **P4 MISSED** (the return is 11,969 B, under the 12 KB cap — the lane wrote to the cap; the FIX-1 base rate did not transfer) · **P5 MISSED** — the hub predicted "rest-api +11"; the instruction's T1–T11 split across two modules (T1–T4 automation, T5–T11 rest-api, three of them extending existing shape tests) → automation +4, rest-api +5; the hub's prediction was written without reading which module hosts which test. Owned.
- **The wire literals (for FE-113 §2, fixed at the bytes):** `triggerRef` / `components[].ref` = `{"type": "entity", "id": "<ULID>"}` — **lowercase `entity`**, the causal chain's OWN literal since M7.5a (`StandardExplanationService.subjectRefView("entity", …)` `:745` `:773`; `RunExplanation.java:95` "lowercase category, e.g. \"entity\""); the dashboard's `contract.ts:140` comment "e.g. ENTITY" is a stale FE comment, not a wire fact → an FE-113 `[INFO]` doc fix. `lastReported` = `Instant.toString()` (ISO-8601 UTC, nanos when present, e.g. `2026-09-06T02:45:29.123456Z`), pinned by the lane's test "ISO-8601 string (never epoch seconds), or JSON null".
- **Disclosed non-re-executions:** no Gradle run; the red-first table read as the lane's claim (compile-red 11 + 11 → behaviour-red 3 + 7 → green) against the test files' assertions, not re-run; `-Xlint:exports` not re-run.

## §1 Layer 1 — the return's claims, read critically
The §0 card is complete and honest: the census by path, the two words honoured, the gates with counts, the staged red-first, ONE `[REVIEW]` with its flip cost named (one `case` line + one assertion), six `[INFO]`s each with a line cite, the DP-3 choice stated with its evidence, the instrument limit (a 24-core desk, not the runner). §3 lists EVERY permit's yield across the three sealed hierarchies — the enumeration the instruction should have carried. The completion register is REPO-COMPLETE, LIVE-VERIFICATION PENDING (law #22) as required. The handoff entry is prepended with the Deferred Build Gate flag and the NEXT WU pointer (the hub's audit → the msg file → Nick's commit of exactly the 17 → CI → FE-113 → H8); one lesson appended (the rule-vs-list class). Nothing in the return over-claims.

## §2 Layer 2 — at the bytes (each rule re-executed)
- **R1 census:** 17 M exact (§0). `git diff --stat`: +641/−64 across the 17. The two A2/A3 files (`GetEntityEndpoint` · `GetEntityStateEndpoint`) carry ONE `@see` javadoc line each (the signature gained `EntityRegistry`) — no serializer touched → **R5 (A1-only) HOLDS**; `GetEntityEndpoint:125`'s record-direct `data` untouched.
- **R3 module-info:** 0 hunks ✓. **R6 Row 30:** the only `firingValue`/`resultOutcome` line in the diff is the rest-api MODULE_CONTEXT GOTCHA (Row 30 as-ruled) — zero core lines ✓.
- **R4 additive, JSON null, order:** `GetNonFiringEndpoint.toWire` — `LinkedHashMap(10)`, `triggerRef` put LAST after `noCommandsIssued` ✓; `ListAutomationsEndpoint` — `ref` put after `summary` ✓; `ListEntitiesEndpoint.summarise` — `deviceId` then `lastReported` after `stale` ✓, pinned by the test `containsExactly("entityId","availability","stale","deviceId","lastReported")`; null paths: `findEntity(...).map(Entity::deviceId).map(DeviceId::toString).orElse(null)` (a helper entity's null `deviceId` → null through the second `map`) ✓; `state.lastReported() == null ? null : …toString()` ✓; `EndpointResponses.subjectRefMap(null) → null` (the key stays present) ✓. `RunEndpointsTest` absent from the diff ✓ — the `subjectRefMap` hoist is wire-byte-identical by that proof.
- **R2 red-first:** the shape tests gain a key → red by construction (the lane says so); the behaviour tests (`deviceId_fromRegistry_andNullWhenUnregistered` asserting `containsExactly(DEVICE_ULID, null, null)`; `lastReported_iso8601_orNull`; the `triggerRef` `containsEntry("type","entity")` pins) cannot pass at HEAD (the keys do not exist) ✓ by reading.
- **R8 mismatches first:** the `[REVIEW]` adjudicated in §4 before any card line.

## §3 The code second-check — maintainability and scalability (Nick's ask), read at the diff
**What is right, and why it will still be right in 2029:** (1) **Exhaustive sealed switches with NO `default`** over `TriggerDefinition` (12 permits), `ConditionDefinition` (7), `ActionDefinition` (8) and `Selector` (7) — a new permit is a compile error in `refOf`/`selectorRef`, never a silent `null` (the same discipline `deriveNonFiring` already uses over `RunStatus`). (2) **One rendering of `{type, id}`** hoisted to `EndpointResponses.subjectRefMap` and consumed by three reads — the causal chain, the non-firing read and the list cannot drift; the hoist is proven byte-identical by `RunEndpointsTest` untouched. (3) **The ref derived ONCE** beside `triggerSummary` and threaded through every construction (DISABLED · NEVER_TRIGGERED · the five `deriveNonFiring` arms · the three `completedVerdict` arms) — no arm can forget it. (4) **The registry read is O(1) and lock-free by design:** `InMemoryEntityRegistry.findEntity` is a `get` on a `volatile` immutable map snapshot (LTD-11: reads lock-free, writes copy-on-write under a `ReentrantLock`, no `synchronized`) — a 300-row list costs 300 map gets on Javalin worker threads with zero contention; the LIVE instance is the composition root's (`HomeSynapseCore:296/:590/:1029`), never a fresh registry. (5) **Honesty by construction:** `ReachabilityTrigger` → null with the limitation STATED in the javadoc (a device-typed ref is a later additive bump); a registry exception is NOT swallowed (a projection defect surfaces as today's 500); the C8 `name` deliberately not populated.
**What to watch (rows, not blockers):** (a) **The convenience-constructor ladder.** `NonFiringExplanation` now carries 10 components + two convenience ctors (9-arg, 8-arg); `ComponentView` 3 + one. Each additive bump adds a ctor; by v1.1.5 the record has four. The javadoc already says "test-convenience; production constructs the canonical form" — so at the NEXT additive bump, retire them to a test-only factory (`AutomationTestSupport`) and let production call the canonical form only. Docket row **REFACTOR-1** (zero wire change; a test-tree move). (b) **`EndpointResponses` now imports `com.homesynapse.automation.RunExplanation`** — the generic envelope utility gained an automation-typed helper. Acceptable today (package-private; one type); if a second domain view needs the same treatment, split a package-private `WireViews` helper so the envelope class stays domain-free. Same row. (c) **A consistency window, named for the freeze note:** `deviceId` is read from the LIVE registry at request time while the row's `availability`/`stale`/`lastReported` come from the state SNAPSHOT at `viewPosition` — two instants. An entity registered or removed between the snapshot and the lookup renders a `deviceId` the `X-HomeSynapse-View-Position` header does not bound (removed → null, honest; registered-after → a device the view did not yet know). Because the registry is a projection of the same log (REG-INV-1), the durable fix is a registry read at the snapshot's position — a design row, not this WU. **Freeze-doc v1.1.3 note + a MODULE_CONTEXT gotcha (hub, at the landing); FE-113 renders as-is.** (d) `CompoundSelector([DirectRef E])` → null (I6): the rule as written withholds a ref that could name no other entity; keep as written until the FE needs it — a one-line change with a ruling (which member wins on disagreement).

## §4 Rulings
- **R1 `[REVIEW]` — `NumericCondition(DirectRef E)` yields `("entity", E)`: ACCEPT AS IMPLEMENTED.** The instruction's law is the RULE ("a component contributes a ref iff it addresses exactly ONE entity by identity"); the enumerated permit list was the hub's census, and it was incomplete at source — the lane applied the rule, pinned the yield in T4, and named the one-line flip. A `NumericCondition` on a direct ref addresses exactly one entity; the ref is honest, not fabricated (D5). The hub's miss, owned; the lane's lesson (rule-vs-list) is the right lesson.
- **I1–I6: ACCEPT.** I5 (arch Rules 4/5 name `device..` as subject scope only) adjudicated as the lane read it — not a fifth reaching rule; ArchUnit on Nick's push is the proof.
- **Hub-originated (§3 c):** the consistency window → the freeze note; **REFACTOR-1** → the docket (Lane B's hivemind half).

## §5 The msg file + the census card (Nick's hands; the ONE core lane lands)
Msg file: `../_scratch/2026-09-05_core_CG-123_commit-msg.txt`. **Card:** in `~/Desktop/Code/ClaudeFolder/homesynapse-core` — `git --no-optional-locks status --porcelain | wc -l` must read **17** (the first line of the card; STOP if not: the hivemind's `??` files are NOT in this repo). Stage exactly the 17 with `git add -u` (the core porcelain carries no `??` — every entry is one of the 17 tracked files); `git diff --cached --name-status | wc -l` = 17 (STOP if not); commit with `-F` the msg file; push `e5fa035..<sha>`. **CI = the gate of record; REPORT `CG: landed <sha> · CI <green | red: the FAILED line>`.** A red on THIS push is a sample on CG's bytes AND a gate on them — read against the pre-registered HeroLoop prediction first.

## §6 FE-113 — the ⟨slots⟩, filled from the landed bytes
`lastReported` = `Instant.toString()` ISO-8601 UTC · `triggerRef.type` / `ref.type` = **`"entity"`** (lowercase; the causal chain's literal; the FE comment at `contract.ts:140` is stale → `[INFO]` doc fix inside the additive law) · baseline sha = CG's landed sha (Nick fills it in the dispatch line from his push) · the CG audit path = this file. **Dispatches only after CI is green on CG's push.**
