<!--
file: context/audits/2026-09-05_CG-123_return.md
purpose: Coder return for CG-1/2/3 — the read-API v1.1.3 additive keys; Row 30 adjudicated at source. §0 first; the hub audits; Nick commits.
status: DELIVERED — REPO-COMPLETE, LIVE-VERIFICATION PENDING (law #22)
-->

# CG-123 return — v1.1.3 additive keys, Row 30 adjudicated (filed 2026-09-05 CT)

## §0 The card
- **Verdict: DELIVERED — REPO-COMPLETE, LIVE-VERIFICATION PENDING** (verified only when H8's real-wire read shows the four keys). Baseline `e5fa035` clean at launch. Words: **`CG2-SCOPE: A1-only` · `ROW30: as-ruled`**.
- **Census (lock-free porcelain): 17 = 17 M + 0 A + 0 D — the Files table EXACT.** `core/automation/`: `MODULE_CONTEXT.md` · `src/main/…/automation/{NonFiringExplanation,AutomationSummary,StandardExplanationService}.java` · `src/test/…/automation/NonFiringExplanationServiceTest.java`. `api/rest-api/`: `MODULE_CONTEXT.md` · `src/main/…/api/rest/{EndpointResponses,GetRunCausalChainEndpoint,GetNonFiringEndpoint,ListAutomationsEndpoint,ListEntitiesEndpoint,RestFilters,GetEntityEndpoint,GetEntityStateEndpoint}.java` · `src/test/…/api/rest/{AutomationEndpointsTest,ListEntitiesEndpointTest}.java`. `lifecycle/lifecycle/src/main/…/lifecycle/HomeSynapseCore.java`. NOT touched: any `module-info.java` / `build.gradle.kts` / `web-ui/**` / the freeze doc / the ITs. **ZERO commits, ZERO staged.**
- **Gates (desk, `--offline`, `--rerun` ×2): BUILD SUCCESSFUL, 0 warnings under `-Werror`.** `:core:automation:test` 203 → **207/207** · `:api:rest-api:test` 147 → **152/152** (3 skipped, unchanged) · `:lifecycle:lifecycle:compileJava` executed · `spotlessCheck` clean everywhere · `RunEndpointsTest` untouched, green. **Deferred Build Gate: YES** — `./gradlew check` (ArchUnit in `:app`) owed to CI on Nick's push of exactly the 17 atop `e5fa035`.
- **Red-first, staged:** HEAD + tests → compile-red 11 + 11 → stage A (seams declared, hooks inert) → behavior-red 3 + 7 → stage B → green (§1 table).
- **Deviations:** **`[REVIEW]` R1** — `NumericCondition(Selector, …)` is selector-bearing at source (`NumericCondition.java:27`) but absent from §DP-2's enumerated permit list; the rule as written ("iff exactly ONE entity by identity") is applied, so `NumericCondition(DirectRef E)` yields `("entity", E)` — pinned in T4; the flip is one `case` line + one assertion. **`[INFO]`** I1 rest-api +5 tests (T6 · T8 · T9 · T10 · T11), not "+4" · I2 `componentsOf` sits at `:440`, not ~:770 · I3 `HomeSynapseCore:1029` unshifted · I4 the two code-unchanged `@see` lines use the FQN `com.homesynapse.device.EntityRegistry` · I5 arch Rules 4/5 name `device..` as SUBJECT scope only — not a fifth reaching rule (§2) · I6 `CompoundSelector([DirectRef E])` observation, rule kept as written (§3).
- **DP choices where a fallback was offered:** DP-3 — the DEFAULT held: `EntityRegistry` on the exported gateway compiled clean under `-Xlint:exports` (runs 2 + 3); the `requires transitive device` + `api(device-model)` fallback NOT taken, census stays 17. Row 15 double — the rest-api test tree's existing `FakeEntityRegistry`, not `InMemoryEntityRegistry`.
- **Instrument limit:** 24-core Windows 11 desk, warm daemon, `--offline`; not the 2-vCPU runner; no ArchUnit here. `date -u` at boot **2026-09-06T02:46Z ⇒ CT = UTC−5 = 2026-09-05 21:46 CDT** (once); filed **2026-09-05 CT**.

## §1 What changed (17 M) + the red-first table
**core/automation.** `NonFiringExplanation` 9→10 (+ nullable `SubjectRefView triggerRef`, not null-checked; the 8-arg and a new 9-arg convenience ctor delegate `null` — "test-convenience; production constructs the canonical form"). `ComponentView` 2→3 (+ nullable `ref`; 2-arg kept). `StandardExplanationService`: `triggerRef = triggers.isEmpty() ? null : refOf(triggers.get(0))` derived ONCE beside `triggerSummary`, threaded via `deriveNonFiring`/`completedVerdict` into all **8** constructions (:228 :238 :263 :271 :284 :315 :331 :338 at HEAD — every one canonical 10-arg; grep 8/0 convenience forms); `componentsOf` passes `refOf(x)`; four private static exhaustive sealed switches, no `default` (`refOf` ×3 + `selectorRef`; `entityRef` → the existing `subjectRefView("entity", …)`). Test +T1–T4. MODULE_CONTEXT: the CG-123 banner (the `refOf` rule verbatim = the projection's contract) + one `Selector`-row sentence; header counts unchanged.
**api/rest-api.** `EndpointResponses.subjectRefMap` hoisted (the exact 6-line body); `GetRunCausalChainEndpoint` calls it twice, private copy gone — byte-identical. `GetNonFiringEndpoint`: `triggerRef` after `noCommandsIssued` (10 keys). `ListAutomationsEndpoint`: `ref` after `summary`. `ListEntitiesEndpoint`: ctor `(StateQueryService, EntityRegistry, LongSupplier, Clock)`; `summarise` an instance method; `deviceId` = `findEntity(id).map(Entity::deviceId).map(DeviceId::toString).orElse(null)`, then `lastReported` = `Instant.toString()`|null → 5 keys; no swallow. `RestFilters.installEntityQueryEndpoints` 5-param; registry to the list endpoint ONLY. `GetEntityEndpoint`/`GetEntityStateEndpoint`: `@see` only. Tests: T5/T7 extended (+ key-ORDER and present-null pins), +T6/T8; 9 ctor sites carry `FakeEntityRegistry`, +T9/T10/T11. MODULE_CONTEXT: header, three rows, the `### CG-123` section (keys · positions · sources · null semantics · pins · the 5th param · the hoist · the Row 30 GOTCHA).
**lifecycle.** `HomeSynapseCore:1030` — `entityRegistry` as the 3rd argument, ONE line.

| # | Test | Pred. | Run 1 HEAD | Run 2 stage A | Run 3 |
|---|---|---|---|---|---|
| T1 | `…triggerRef_directRefSelector` (5 verdict paths) | RED | c-red `triggerRef()` | RED `expected SubjectRefView[…] but was: null` | GREEN |
| T2 | `…triggerRef_nullForGroupSelector` (6 group permits) | RED | c-red | **g-b-c** (an inert seam yields null; teeth: stage B holds all six at null while T1 turns green) | GREEN |
| T3 | `…triggerRef_nullForSubjectlessTrigger` | RED | c-red | RED on the Calendar arm | GREEN |
| T4 | `listAutomations_componentRef_perComponent` | RED | c-red `ComponentView::ref` | RED `[null×4]` vs `[E1,E2,E3,null]` | GREEN |
| T5 | `nonFiring_v11ShapeTest` (M) | RED | (set failed) | RED keys lack `triggerRef` | GREEN |
| T6 | `nonFiring_triggerRefOnWire` | RED | c-red 10-arg ctor | RED NPE, key absent | GREEN |
| T7 | `automations_v11ShapeTest` (M) | RED | (set failed) | RED keys lack `ref` | GREEN |
| T8 | `automations_componentRefOnWire` | RED | c-red 3-arg ctor | RED NPE, key absent | GREEN |
| T9 | `rows_v113Keys_inOrder` | RED | c-red 4-arg ctor ×9 | RED `[entityId, availability, stale]` | GREEN |
| T10 | `deviceId_fromRegistry_andNullWhenUnregistered` | RED | c-red | RED `[null×3]` vs `[D,null,null]` | GREEN |
| T11 | `lastReported_iso8601_orNull` | RED | c-red | RED `not to be null` (key absent) | GREEN |
| T12 | 6 existing `ListEntitiesEndpointTest` | g-b-c | c-red (ctor) | GREEN | GREEN |
| — | `RunEndpointsTest` untouched | GREEN | — | GREEN | GREEN (hoist proof) |

(c-red = compile-red; g-b-c = green-by-construction, disclosed.)

## §2 Gates, counts, freshness, sweeps
- **Runs (UTC):** 0 HEAD 02:50 `:core:automation:test --rerun :api:rest-api:test --rerun --offline` → 203/203 · 147/147 (3 skip). 1 02:58 `compileTestJava` ×2 → FAILED 11 + 11. 2 03:00 tests `--rerun` ×2 + `:lifecycle:lifecycle:compileJava` → 207 run/3 fail · 152 run/7 fail; rest-api + lifecycle `compileJava` executed, 0 warnings ⇒ DP-3 default holds. **3 03:05:39–:45** `./gradlew :core:automation:test --rerun :api:rest-api:test --rerun :lifecycle:lifecycle:compileJava spotlessCheck --offline` → **BUILD SUCCESSFUL**; the three named tasks + the touched modules' `spotlessJavaCheck` EXECUTED (8 executed / 34 up-to-date spotless tasks); 0 `warning:`/`error:` lines; XML 207/0 · 152/0/3; the four result files' mtimes 03:05:41.989–03:05:44.145Z, inside the window (freshness from executed-state lines + mtimes, not the summary). Logs in the session scratchpad (not filed).
- **Preflight:** `CODER FRESHNESS PREFLIGHT — 2026-09-06 02:50 UTC — PASS (all 7)`; Check 3 one cite off (I2); Check 6 `diff -rq` hivemind `coder/` vs the loaded skill: empty.
- **ARCH-RULE-REACH (law #16), re-grepped; `HomeSynapseArchRules.java` unchanged since `dc3328b`:** Rule 8 `:266–:272` (`api.rest..` ↛ `persistence..`; `EntityRegistry` is `device..`) · Rule 9 `:306–:317` (no `EventPublisher`; untouched) · Rule 10 `:337–:348` (rest-api not listed) · Rule 11 `:410–:424`, mutators `:372–:375` (`findEntity` is a read) — **zero collisions**. Rules 4 `:143` / 5 `:191` name `device..` only as the SUBJECT of core's outbound constraints — they do not reach a read FROM `api.rest` (I5).
- **INV/LTD at source:** LTD-04 `Locked_Decisions.md:147` (ULID strings via `toString()`; never `{msb,lsb}`) ✓ · LTD-08 `:287` (JSON stays in the endpoint maps) ✓ · `NO_DIRECT_TIME_ACCESS` `HomeSynapseArchRules:98` (no clock read added; diff-only sweep for `Instant.now`/`systemUTC`/`currentTimeMillis`/`nanoTime`/`synchronized`/`System.out`/`Thread.sleep`/`TODO`: none) ✓ · INV-SE-02 `Architecture_Invariants_v1.md:599` (no new route; auth + readiness gate unchanged) ✓ · the freeze's four-constraint law (freeze `:5`; v1.1.2 precedent `:117`/`:140`) and its A1 shape `:46` (no `name` added) ✓. Glossary: `entity_id`/`device_id` (LTD-04) · `subject_ref` · Entity/Device/Automation/Trigger ✓; `Selector`/`Calendar`/`lastReported` are Doc 07/AMD-88/Doc 03 terms, not glossary entries; the wire keys follow the freeze's camelCase law.
- **P2 re-run:** `new NonFiringExplanation(` 8 main + 5 test ✓ · `ComponentView(` 1 + 2 ✓ · `subjectRefMap(` 3 → 5 (1 def + 4 calls) · `new ListEntitiesEndpoint(` 1 + 6 → 1 + 9 · `installEntityQueryEndpoints` 1 call + 3 `@see` ✓ · `ref`/`triggerRef` collision check: none ✓.

## §3 Pushback / observations
- **R1 `[REVIEW]` — every permit and its yield.** Triggers: `StateChange`/`State`/`NumericThreshold`/`Availability` → selector rule; `Calendar` → `calendarEntityId`; `Reachability(DeviceId)` → `null` (a DEVICE; no device read to census — the javadoc states the limitation); `Event`/`Manual`/`Webhook`/`Time`/`Sun`/`Presence` → `null`. Conditions: `State` → selector rule; **`Numeric` → selector rule (R1)**; `Time`/`Zone` → `null`; `And`/`Or`/`Not` → `null`, never descended. Actions: `Command.target` → selector rule; `Delay`/`EmitEvent`/`ActivateScene`/`InvokeIntegration`/`Parallel` → `null`; `WaitFor`/`ConditionBranch` → `null`, never descended (a condition, not a selector). Selectors: `DirectRef` → `("entity", id)`; `Slug`/`Area`/`Label`/`Type`/`SemanticTag`/`Compound` → `null`. Contract impact: yes (one observable `components[].ref` value class). Flip: `case NumericCondition c -> null;` + T4's second `containsExactly`.
- **I6 `[INFO]` — `CompoundSelector([DirectRefSelector(E)])`** (`CompoundSelector.java:24`): its intersection is `⊆ {E}`, so `null` is the one shape where the rule withholds a ref that could name no other entity. Implemented AS WRITTEN (T2 pins it): descending a compound (which member wins when two direct refs disagree?) is the hub's ruling, not the lane's. No action unless ruled.
- Observation: the per-condition ref lands on the automation list only; `RunExplanation.ConditionView` (the chain) still renders `expression` — consistent with the three-read scope.

## §4 WUCP Phase 1: Coder Closeout
- [x] MODULE_CONTEXT.md updated for: core/automation, api/rest-api
- [x] coder-handoff.md updated
- [x] Deferred build gate flag: YES
- [x] coder-lessons.md appended: the rule-vs-enumerated-list lesson (implement the rule over the sealed hierarchy at source; pin and `[REVIEW]` the un-enumerated permit)
- [x] Cross-agent note posted: Not needed (channel retired; this return + the handoff entry carry it)
- Timestamp: 2026-09-06 03:15 UTC
