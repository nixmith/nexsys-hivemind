<!--
file: context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md
purpose: The hub's TWO-LAYER AUDIT of the FAILCHAN-FIX-1 lane's return (context/audits/2026-09-05_FIX-1_return.md), its loop corpus (…_FIX-1_loops/, 13 files) and its FIX-1b patch (…_FIX-1b.patch) — layer 1 the return's claims read critically; layer 2 the hub's own re-execution at the bytes (porcelain · `git apply --check --stat` · `git diff` at the emission sites, the CI change, the WARN route · the patch's mechanism hunks · the corpus counts, the RED lines, the `Token:` grep), with every non-re-execution DISCLOSED. The pre-filed rules R1–R6 (the v64 intake audit §4) governed; mismatches were adjudicated FIRST (§2). Output: the verdicts, the two msg files + census cards for Nick's hands (§5, §6), the Open Risks lines (§8).
audience: the hub · Nick (the cards) · the v65 hub
state-type: audit (hub-authored)
status: FILED — v64 beat 5 (15:15 CT = 20:15Z). Verdicts: FIX-1a ACCEPT · FIX-1b ACCEPT-WITH-RULINGS (R1 · R2 · R3 below).
-->

# FAILCHAN-FIX-1 — the intake two-layer audit (v64 beat 5)

## §0 Verdict card
- **FIX-1a (THE INSTRUMENTS): ACCEPT.** Census **9 = 7 M + 2 A**, exactly the Files table, verified at `git status --porcelain` on the lane's tree at 20:05Z (the FIX-1b + stamp hunks reverted to a patch, as the return declares). T1–T4 red-first at TWO stages (compile-red at HEAD 18:02Z; behaviour-red with the record present and the hooks inert 18:05Z) → green; desk gates GREEN (event-bus 221 → 225 / 30 skipped; lifecycle 79; `-Werror` + spotless clean) — the lane's claim, not re-run here (disclosed).
- **FIX-1b (THE MECHANISM): ACCEPT-WITH-RULINGS.** Branch per class: **HeroLoop → (c)** (0/70 pre-fix across four desk shapes; branch (a) stands on I-1) → DP-2 retry-then-SUSPEND, no other change · **ReplayIT → (a)**, the mechanism located AT SOURCE and pinned by a deterministic test (T-R1) — NOT the hub's theory (§2 M-2, §7 R2). Census **7 M + 0 A as a patch** (`context/audits/2026-09-05_FIX-1b.patch`, 80,821 B; `git apply --check -p1` OK at 20:05Z; `--stat` = exactly the 7 declared files, +493/−106). T5/T6/T7/T-R1/T-R2 red-first (5/5 red 19:26Z → 9/9 green 19:29Z); T8 20/20 in the reproducing shape (pre-fix 2/36) · T9 20/20 (pre-fix 0/20) · +10/10 extra.
- **The corpus:** 175 loop runs + 1 smoke = 176 (the SUMMARY's own TOTALS line; the return §1's "125" is the pre-fix subset); **RED 2, both ReplayIT, both PHASE 1, both pinned WITHOUT load, zero anomaly tokens in every run**; the corpus 60 KB; **`grep -rl 'Token:'` → 0 hits (R4 clean)**.
- **Mismatches adjudicated first (§2):** three of the hub's predictions MISSED (pressure produces the red · ReplayIT phase 2 · the `:302` window); two MET (no anomaly token on ReplayIT's red · I-1); one uninformative (ef02d13 0 = fix1a 0).
- **Disclosed non-re-executions:** no Gradle run by the hub (CI is the gate); no loop re-run; the patch's md5 round-trip not re-derived (only `--check`/`--stat`); AMD-45's gating semantics read at its title/refines line only; the SUMMARY read by row labels + the RED lines + the TOTALS line, not every line individually.
- **The hub's own instrument error, owned:** at beat 1 (18:34Z) the hub read `ReplayIT/fix1a/` as "empty, pre-created" — it held `run-0-smoke/`, the FIRST red (18:18Z, phase 1). Nothing downstream changed; the mis-read is recorded.
- **Cards:** §5 FIX-1a → `../_scratch/2026-09-05_core_FIX-1a_commit-msg.txt` · §6 FIX-1b → `../_scratch/2026-09-05_core_FIX-1b_commit-msg.txt`.

## §1 Layer 1 — the return's claims, read critically
§0 declares both censuses with exact paths, the branch per class from the table, a two-stage red-first record, and seven tagged deviations (R1–R3 [REVIEW], I1–I7 [INFO]) — the §0-first shape as ordered. §1 fills every §A.5 row and adds three extra rows (8, 9–11) with the pre-fix rate beside each post-fix count. §3 states the gates + counts and the patch's round-trip. §4 carries the two mechanism arguments with line cites (`drainAndPromote :83–:150`; `IntegrationSupervisorAssembly:81`; `:151` the read-executor hop) and the recipe adaptations. §5 the WUCP checklist complete; `coder-handoff.md` (+9) carries the DEFERRED GATE and a NEXT WU pointer that matches the plan of record; `coder-lessons.md` (+6) the measurement-shape inversion. **Claims the hub could not accept at face value and re-executed:** the censuses (§4), the patch's applicability (§4), the RED count and shapes (§3), the redaction (§3), the code at the emission sites and the CI change (§4). **One imprecision:** "HeroLoop 0/90 across four shapes" counts T9's 20 post-fix runs; the pre-fix count is 0/70 (fix1a 20 · cpu-shape 10 · ef02d13 20 · fix1a-noload 20) — [INFO], the branch unchanged.

## §2 Layer 2 — the §A.5 table: Predicted vs Measured, mismatches FIRST (rule R5)
| Instrument | The hub predicted (09-04) | Measured | Verdict | Adjudication |
|---|---|---|---|---|
| HeroLoop ×20 fix1a pinned+load | ≥1 RED (0/20 → (c)) | 0/20 (test 10–32 s) | **MISSED** | the desk's 2-carrier pin + load is not the runner's scheduling; (c) as the instruction foresaw |
| the RED run's tokens | `route_join_miss` + ≥1 anomaly on `integration_supervisor` | no RED; 0 anomaly tokens in 60 fix1a HeroLoop runs | not observable | §7 R3 gives the reason the tokens would be ZERO even on a red (a reorder, not a drop) |
| ReplayIT ×20 fix1a pinned+load | ≥1 RED, PHASE 2, no token, resting 1499/<1499 | 0/20 loaded; **2/36 UNLOADED (smoke 1/1 · noload 1/20 · stamp 0/15), both PHASE 1, no token** | phase **MISSED**; token **MET** | **M-1** pressure hid the window (a starved subscriber is still in REPLAY when the burst ends and REPLAY checkpoints its tail); **M-2** phase 1 — the stall is the TRANSITION drain's missing checkpoint writes, not a publisher race; the `:302` window cannot open (`setVirtualThread` precedes every `notifyEvent`) — §7 R2 |
| HeroLoop ×20 ef02d13 | similar rate → (a) | 0/20 | MET, uninformative | (a) for HeroLoop stands on I-1 alone; the knob is inert on ef02d13 (predates the property) |
| CPU-shape ×10 unpinned, load only | → 0 | 0/10 (test 3.0–3.1 s vs 10–32 s pinned) | MET | the pin costs 5–10× wall; the discriminator is carrier count, as the mechanism claim said |
| I-1 | ≥1 of #169/#183/#206 on these ITs | MET before the loops | MET | — |
| the `dc3328b` artifact (Act 3) | phase 2 | **OPEN** — this desk says phase 1 | pending | **Act 3 now DECIDES whether FIX-1b closes ReplayIT's CI red or only the desk's**: phase 1 → closed by the drain fix; phase 2 → a second mechanism remains, and the stamps (`checkpoint=` · `mode=`) make the next red self-describing |
| T8 ReplayIT ×20 FIX-1b, the reproducing shape | 20/20 | **20/20** (pre-fix 2/36) | MET | weak power (≈1/18 → 0/20 has ~30 % chance by luck); the deterministic T-R1 is the proof, T8 the corroboration — say so on the card |
| T9 HeroLoop ×20 FIX-1b pinned+load | 20/20 | **20/20** (pre-fix 0/20) | MET, uninformative | 0 → 0 |

## §3 Layer 2 — the corpus at the bytes (rules R3, R4)
`SUMMARY.md` 211 lines; header lines define the instrument (Win11 24 cores; `start /affinity 3` = cores 0,1; the JVM measures `availableProcessors=2`; two CPU-bound PowerShell loops on the same mask; every run a fresh `--rerun --no-daemon --offline` JVM) and each sequence (`noload` = pin + knob WITHOUT busy loops; `stamp` = FIX-1a bytes + the ReplayTransitionIT stamps, pinned no load; `cpushape` = unpinned, load only). Row census by label: HeroLoop ef02d13 20 · fix1a 20 · fix1a-noload 20 · fix1b 20 · `HeroLoop-cpushape` fix1a 10 · ReplayIT fix1a 20 · fix1a-noload 20 · fix1a-stamp 15 · fix1b 10 · fix1b-noload 20 = **175 + the smoke = 176** (the TOTALS line agrees). Every run line: class · tree · run-NN · verdict · start→end UTC · rc · suite counts + time · tokens [· RED: method :: message]. **The two RED lines:** `run-0-smoke` (18:18:06→18:18:37Z, pinned + knob, no load) and `ReplayIT · fix1a-noload · run-20` (19:11:46→19:12:16Z) — both `did not reach 1000 within 15000 ms`, `tokens: none`; each has its folder with the XML (1,205 B; `system-out` EMPTY — the bus is SLF4J-free; the stamp is the only diagnostic this class can carry) + `gradle.log`. **Redaction:** 0 `Token:` hits across all 13 files. **T8/T9 cite the FIX-1b tree** (`fix1b`, `fix1b-noload` labels; sequence stamps 19:25Z→19:56Z after the 19:29Z green) — R3 satisfied. **The ruled CPU-shape arm WAS run** (`mode=unpinned … HeroLoop-cpushape`, 10 runs, 18:52Z) — the hub's interim note at beat 2 ("the ruled arm not run") was a label-regex miss, corrected here.

## §4 Layer 2 — the censuses at porcelain and the code at the bytes
- **FIX-1a's census:** porcelain at 20:05Z = 7 M (`ci.yml` · the conventions · both MODULE_CONTEXTs · `InProcessEventBus` · `TransitionCoordinator` · `HomeSynapseCore`) + 2 `??` (`DeliveryAnomaly.java` · `DeliveryAnomalyEmissionTest.java`) = **9, exactly the table**; `git diff --stat` 7 files, +215/−20.
- **FIX-1b's census:** `git apply --check -p1` OK; `--stat`: `core/event-bus/MODULE_CONTEXT.md` +12 · `DeliveryAnomaly.java` +15 · `InProcessEventBus.java` 205 · `TransitionCoordinator.java` 128 · `DeliveryAnomalyEmissionTest.java` +224 · `ReplayTransitionIT.java` +13 · `lifecycle/lifecycle/MODULE_CONTEXT.md` 2 = **7 files**, as declared.
- **Spot-reads (FIX-1a):** `ci.yml` — `workflow_dispatch:` · `--continue` · `if: always()` · `name: test-reports-<run_number>` · the two-path `path:` · `retention-days: 14` (each with a FIX-1a comment) ✓. The conventions — `reports.junitXml.isOutputPerTestCase = true` · `testLogging { events("failed"); exceptionFormat = FULL; showStandardStreams = false }` · the property-gated `vtParallelism` jvmArgs ✓. `InProcessEventBus` — `anomalyEmitter` field; `ANOMALY_ALL_SUBSCRIBERS = "*"`; the legacy ctors delegate `anomaly -> { }`; `emitAnomaly(NOTIFY_NOT_VISIBLE…)` :321 · `LIVE_READ_EMPTY` :558 · `LIVE_READ_FAILED` :569; `emitAnomaly` :620 swallows (`anomalyEmitter.accept` inside) ✓. `TransitionCoordinator` — the emitter param; `emitAnomaly(pos, "drainAndPromote: no envelope at position")` :113 before the skip ✓. `HomeSynapseCore` — `Consumer<DeliveryAnomaly> anomalyHandler = anomaly -> LOG.warn("bus.delivery_anomaly: kind={} subscriber={} position={} detail={} at={}", …)` passed as the bus ctor's last arg ✓. `DeliveryAnomaly` — the record, four `requireNonNull` (I2), `Kind` five values with `LIVE_READ_EXHAUSTED` reserved ✓.
- **Spot-reads (the patch):** the drain gains a `CheckpointStore` and writes a checkpoint per SUCCESS delivery gated on `!runtime.info().atomicCheckpoint()` (AMD-45 — Atomic Subscriber+View Checkpoint Coupling, refines INV-ES-05 at-least-once; the gate keeps atomic-checkpoint subscribers self-managed) · `LIVE_READ_EXHAUSTED` after the bounded retry → `transitionTo(SUSPENDED)` + `setMode(SUSPENDED)` · `TRANSITION_READ_EXHAUSTED` (the sixth kind, I3) with the same honest terminal state · `notifyEvent` offers an invisible page to LIVE subscribers behind a checkpoint comparison · the five tests `liveReadRetried_thenDelivered` · `liveReadExhausted_suspendsHonestly` · `notifyNotVisible_stillOffersToLive` · `transitionDeliveries_advanceCheckpoint` (T-R1) · `transitionReadExhausted_suspendsHonestly` (T-R2) · the two stamps in `ReplayTransitionIT` (the resting checkpoint; the resting mode) · a MODULE_CONTEXT GOTCHA naming the drain as the FOURTH bus-side checkpoint writer ("re-run the grep when auditing AMD-45") ✓.
- **Rule R1 (two mechanisms expected): CONFIRMED** — DP-2 for the read-failure class + the drain's checkpoint writes for ReplayIT's class; and a THIRD (R3, HeroLoop's reorder) named at source and correctly NOT implemented. **Rule R2:** HeroLoop is branch (c) → the gate clears on FIX-1b's push green; OR-BUS-SILENT-DROP closes only on the passive instrument.

## §5 FIX-1a — the msg file + the census card (Nick's hands; SAMPLE #4, NOT a gate)
Msg file: `../_scratch/2026-09-05_core_FIX-1a_commit-msg.txt` (hub-authored). **Census: 9** — 7 M + 2 A. The `git add` line (exact paths, from the core root):
```
git add .github/workflows/ci.yml build-logic/src/main/kotlin/homesynapse.java-conventions.gradle.kts core/event-bus/MODULE_CONTEXT.md core/event-bus/src/main/java/com/homesynapse/event/bus/InProcessEventBus.java core/event-bus/src/main/java/com/homesynapse/event/bus/TransitionCoordinator.java lifecycle/lifecycle/MODULE_CONTEXT.md lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java core/event-bus/src/main/java/com/homesynapse/event/bus/DeliveryAnomaly.java core/event-bus/src/test/java/com/homesynapse/event/bus/DeliveryAnomalyEmissionTest.java
```
Pre-conditions: `git log -1 --format=%h` = `dc3328b`; `git status --porcelain | wc -l` = 9. Post: `git diff --cached --name-status | wc -l` = 9. The push's run = sample #4 (with `--continue`, per-method XML, the tokens): a red here is the instrument working.

## §6 FIX-1b — the msg file + the census card (Nick's hands; THE GATE; then three veto-only samples)
Msg file: `../_scratch/2026-09-05_core_FIX-1b_commit-msg.txt`. **Pre-conditions:** `git log -1 --format=%h` = FIX-1a's sha; `git status --porcelain | wc -l` = 0. **Apply:** `git apply -p1 ../nexsys-hivemind/context/audits/2026-09-05_FIX-1b.patch` → `git status --porcelain | wc -l` = 7 (all M). **Census: 7 M.** The `git add` line:
```
git add core/event-bus/MODULE_CONTEXT.md core/event-bus/src/main/java/com/homesynapse/event/bus/DeliveryAnomaly.java core/event-bus/src/main/java/com/homesynapse/event/bus/InProcessEventBus.java core/event-bus/src/main/java/com/homesynapse/event/bus/TransitionCoordinator.java core/event-bus/src/test/java/com/homesynapse/event/bus/DeliveryAnomalyEmissionTest.java core/event-bus/src/test/java/com/homesynapse/event/bus/ReplayTransitionIT.java lifecycle/lifecycle/MODULE_CONTEXT.md
```
Post: `git diff --cached --name-status | wc -l` = 7. The push's run GREEN = the clearance; then Actions → CI → Run workflow ×3 (veto-only).

## §7 Rulings on pushback (evidence-based pushback accepted; the hub's misses owned)
- **R1 (patch-shaped FIX-1b): ACCEPT.** Two commits from one uncommitted tree need separable bytes; the lane produced them (the tree = FIX-1a; the delta = the patch; round-trip proven by the lane, applicability re-executed by the hub). The stamps ride FIX-1b rather than FIX-1a (Act A asked for FIX-1a so sample #4 would carry them): accepted — the mechanism is located by T-R1 deterministically; the stamps are confirmatory for the runner's future reds, and FIX-1b follows FIX-1a by minutes.
- **R2 (ReplayIT = the TRANSITION checkpoint gap, phase 1): ACCEPT; the hub's `:302` lost-unpark / phase-2 theory REFUTED at source and OWNED.** The lane read `drainAndPromote :83–:150`: it wrote no checkpoint while `ReplayDriver` checkpoints its paged tail and `liveLoop` checkpoints per delivery — a burst ending inside TRANSITION is fully delivered with the checkpoint resting at the REPLAY tail (`phase1Delivered == 1000` would pass; `awaitCheckpoint(1000)` cannot). It reproduces only UNLOADED because a starved subscriber is still in REPLAY when the burst ends. The `:302` window cannot open in either phase. **Honest characterization for the msg file:** no event was lost; the checkpoint under-reported delivered positions until the next LIVE delivery — on a restart the drained burst would REDELIVER (lawful under INV-ES-05 at-least-once; noisier). The fix makes the drain checkpoint each SUCCESS (LIVE parity). **Act 3 decides whether this closes the `dc3328b` CI red** (phase 1 → yes; phase 2 → a mechanism this desk did not see remains, now self-describing).
- **R3 (HeroLoop = `notifyEvent` fan-out REORDERING; unmeasured; not implemented): ACCEPT as filed — docketed FIX-2, instrument-first.** The argument at source: `notifyEvent(P)` fans out per subscriber with a `readCheckpoint` hop to the read executor per subscriber (`:151`); the dispatch service, offered P early, publishes `command_dispatched`, whose `notifyEvent` can reach `integration_supervisor` BEFORE `notifyEvent(P)` (the `command_issued`) does → dispatched-before-issued → `route_join_miss` with ZERO drops — hence zero FIX-1a tokens, and consistent with #183/#206/#225 and 0/90 here (µs hops). **Pre-registered runner prediction (H12):** a post-FIX-1b HeroLoop red on `main` shows `integration.route_join_miss ×1` and ZERO `bus.delivery_anomaly` lines in that method's XML → R3 CONFIRMED → FIX-2 charters (the instrument first: a per-subscriber monotonic `lastOffered` check → `LIVE_OUT_OF_ORDER` + a monotonic guard on the per-delivery checkpoint, which can REGRESS on reorder today; then the mechanism — ordered per-subscriber offers, or the AMD-42 §3.4.2 step-4 pull-from-cursor LIVE loop; the deterministic pin the lane names: block `readCheckpoint(router)` inside `notifyEvent(P)` until `notifyEvent(P+1)` has offered). **The H1 consequence, stated plainly to Nick:** FIX-1b may NOT stop HeroLoop's runner flake; the gate law still clears on its push green; a later HeroLoop red re-opens the gate WITH its mechanism named.
- **I1–I7: ACCEPT** (Kotlin property name · four `requireNonNull` · the sixth `Kind` · 5 reads/4 parks · retry in place on a `LinkedBlockingQueue`, order-equivalent · the recipe adaptations, each filed · the census file present). **[INFO] 5 (INV-BUS-02 cites an ArchUnit rule name absent from `HomeSynapseArchRules.java`):** a docs correction candidate → Block 6's block.

## §8 Open Risks lines (for the spine) + what the hub owns
- **OR-FAILCHAN — the return audited ACCEPT (FIX-1a) / ACCEPT-WITH-RULINGS (FIX-1b); the two cards in Nick's hands; LANDING PENDING his two pushes.** Closes when FIX-1b's push run is GREEN (+ the three veto-only samples not red) AND the §6-B/EXITCODE hardware proof rides the next card session on FIX-1b's artifact.
- **OR-BUS-SILENT-DROP — the four drop points tokenized (FIX-1a) and retried-then-SUSPENDED (FIX-1b); a FIFTH silent path found and fixed (the TRANSITION drain's missing checkpoint writes — a checkpoint that lied low, not a loss); a SIXTH named at source, unmeasured (the fan-out reorder, R3 → FIX-2).** Closes on the passive instrument (rule R2): the `bus.delivery_anomaly` token absent AND both ITs green across the next 20 ordinary `main` runs; a HeroLoop red with a miss and zero tokens re-opens it as FIX-2.
- **Owned by the hub:** the `:302`/phase-2 theory (wrong at source); the "pressure raises the rate" assumption (inverted by measurement); the beat-1 mis-read of the smoke folder; the beat-2 "ruled arm not run" note (a label-regex miss). Each becomes a mint at the close.
