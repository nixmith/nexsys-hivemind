<!--
file: context/audits/2026-09-05_FIX-1_return.md
purpose: THE FAILCHAN-FIX-1 LANE RETURN — FIX-1a (the instruments) in the tree; the §A.5 table filled from 215 loop runs + 1 smoke; FIX-1b (the mechanism, on the measurement) as a patch beside this file. Instruction: context/instructions/2026-09-04_coder-lane_FAILCHAN-FIX-1_CI-nondeterminism_coding-instruction.md (FIX1: class).
audience: the hub (two-layer audit) · Nick (two commits)
state-type: lane return (filed evidence)
status: FILED Sat 2026-09-05 2026-09-05 21:55 UTC (instrument: the host clock via `date -u`; CT = UTC−5 ⇒ operator day Sat 09-05; loops 18:21Z→19:56Z, the hub-stamp re-run 21:28Z→21:53Z). v2 — re-filed after the hub note (v64 b1) on the awaitCheckpoint stamp.
-->

# FAILCHAN-FIX-1 — return

## §0 The card
**DELIVERED, both parts; ZERO commits, NOTHING staged.** Baseline `dc3328b` clean at launch (17:51Z).
- **FIX-1a census (the tree NOW, `git --no-optional-locks status --porcelain`) = 10 = 8 M + 2 A** — the Files table's 9 + `core/event-bus/src/test/java/com/homesynapse/event/bus/ReplayTransitionIT.java` (ONE stamp, the hub note v64 b1 — I8): M `.github/workflows/ci.yml` · `build-logic/src/main/kotlin/homesynapse.java-conventions.gradle.kts` · `core/event-bus/MODULE_CONTEXT.md` · `core/event-bus/src/main/java/com/homesynapse/event/bus/InProcessEventBus.java` · `…/event/bus/TransitionCoordinator.java` · `lifecycle/lifecycle/MODULE_CONTEXT.md` · `lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java`; A `…/event/bus/DeliveryAnomaly.java` · `core/event-bus/src/test/java/com/homesynapse/event/bus/DeliveryAnomalyEmissionTest.java`.
- **FIX-1b census = 6 M + 0 A** in **`context/audits/2026-09-05_FIX-1b.patch`** (`git apply -p1` on the FIX-1a tree; round-trip proven, §3): both bus classes · `DeliveryAnomaly.java` · `DeliveryAnomalyEmissionTest.java` · both MODULE_CONTEXTs. Neither IT.
- **Branch per class (§1):** **HeroLoop → (c)** — 0/90 on this desk across four shapes; I-1 says pre-existing → FIX-1b = DP-2 retry-then-SUSPEND, no other change. **ReplayIT → (a)** — reproduced **3/56 pinned WITHOUT load** (0/40 with load), all **PHASE 1**, no anomaly token; the mechanism read at source, pinned by T-R1 and read live (`resting checkpoint 28`): `drainAndPromote` delivered queued positions but never checkpointed them. Fix: the drain checkpoints each SUCCESS delivery (LIVE parity, AMD-45-gated) + retry-then-SUSPEND on its empty page.
- **Red-first:** HEAD: T1–T4 compile-red (`DeliveryAnomaly` absent, 18:02Z) → stage A (record + ctor, hooks inert) **4/4 behaviour-red** (T1/T2 `timed out awaiting LIVE_READ_*`, T3 `Expected size: 1 but was: 0`, T4 `emitter invoked expected 1 but was 0`, 18:05Z) → green. FIX-1b on the FIX-1a bytes (+ the `Kind` seam): **T5/T6/T7/T-R1/T-R2 5/5 red**, each `timed out awaiting …` its mechanism (T-R1: `…the checkpoint reaching …41`), T1–T4 green (19:26Z) → **9/9 green** (19:29Z). T8/T9 → §1 rows 9–11.
- **Deviations:** [REVIEW] R1 FIX-1b patch-shaped (two commits from one uncommitted tree need separable bytes) · [REVIEW] R2 ReplayIT's mechanism ≠ the hub's `:302`/phase-2 theory (§4-1) · [REVIEW] R3 a second, unmeasured HeroLoop mechanism at source — `notifyEvent` fan-out reordering — not implemented (§4-2) · [INFO] I1 Kotlin `isOutputPerTestCase` · I2 `requireNonNull` ×4 · I3 `Kind` +`TRANSITION_READ_EXHAUSTED` · I4 retry = 5 reads, 4 parks 1→2→4→8 ms · I5 retry in place (no head insert on `LinkedBlockingQueue`; order-equivalent) · I6 recipe adaptations (§4-4) · I7 the census file was present (read) · **I8 `awaitCheckpoint`'s throw gains the hub's ONE stamp `(resting checkpoint N)` (v64 b1 note) — FIX-1a 9 → 10; bounds byte-unchanged; my FIX-1b message stamps withdrawn.**

## §1 The measurement table (§A.5) — 215 loop runs + 1 smoke; corpus `2026-09-05_FIX-1_loops/` (SUMMARY.md · RED runs' XML+log · `instrument/`)
Desk: Win11, 24 cores; WSL has no JDK. pinned = `start /affinity 3` (the JVM measures `availableProcessors()=2`) + `vtParallelism=2`; load = two busy loops on the same mask; every run a fresh `--rerun --no-daemon --offline` Gradle JVM.
| Row | Predicted | Measured |
|---|---|---|
| 1 HeroLoop ×20 fix1a pinned+load | ≥1 RED (0/20 → (c)) | **0/20**; test wall 10–32 s; tokens none |
| 2 the RED run's tokens | `route_join_miss` + ≥1 anomaly on `integration_supervisor` | no RED → not observable; 0 anomaly tokens in 60 fix1a HeroLoop runs |
| 3 ReplayIT ×20 fix1a pinned+load | ≥1 RED, PHASE 2, no anomaly; resting 1499 / <1499 | **0/40 with load; 3/56 WITHOUT** (smoke 1/1 · no-load 1/20 · my-stamp row 0/15 · hub-stamp row 1/20), both **PHASE 1** (`did not reach 1000 within 15000 ms`), no anomaly token (as predicted). **Phase prediction MISSED.** Resting value: T-R1 shows it deterministically = the REPLAY tail, all events delivered. **Re-run with the hub's stamp (v64 b1): pinned+load **0/20**; pinned no-load **1/20** — the RED (run-07, 21:48Z) read **`(resting checkpoint 28)`**: the REPLAY tail; positions 29..1000 went through the TRANSITION drain, which delivered them without a checkpoint (T-R1 proves the delivery half; a 'late, not lost' reading would have been ≥ 1000). Corpus: `ReplayIT/fix1a-hubstamp-noload/run-07/`** |
| 4 HeroLoop ×20 `ef02d13` pinned+load | similar → (a) | **0/20** (test 6–8 s, one 97 s green outlier); both trees 0 → silent between (a)/(c); the knob is inert on `ef02d13` |
| 5 CPU-shape ×10 unpinned, load only | → 0 | **0/10**, test 3.0–3.1 s (vs 10–32 s pinned: the pin costs 5–10×) |
| 6 I-1 (audit §7-A) | ≥1 of #169/#183/#206 on these ITs | **MET** — #206 HeroLoop `:613`; #183 HeroLoop ×2 `:562`; #169 a compile miss |
| 7 `dc3328b` artifact | phase 2 | **OPEN (Act 3)**; this desk says phase 1 — adjudicate at the HTML |
| 8 extra: HeroLoop ×20 fix1a pinned no-load | — | **0/20**, test ~4.1 s |
| 9 **T8** ReplayIT ×20 FIX-1b pinned no-load (the reproducing shape) | 20/20 | **20/20 GREEN** (pre-fix 3/56) |
| 10 **T9** HeroLoop ×20 FIX-1b pinned+load | 20/20 | **20/20 GREEN** (pre-fix 0/20) |
| 11 extra: ReplayIT ×10 FIX-1b pinned+load | — | **10/10 GREEN** (pre-fix 0/20) |

## §2 What changed (line numbers on the FIX-1a bytes)
**FIX-1a.** `ci.yml`: `workflow_dispatch:`; `check --no-daemon --continue`; upload `if: always()`, `test-reports-${{ github.run_number }}`, `**/build/reports/tests/` + `**/build/test-results/`, 14 d. Conventions: `reports.junitXml.isOutputPerTestCase = true`; `testLogging { events("failed"); exceptionFormat = FULL; showStandardStreams = false }`; the desk-only `vtParallelism` knob. `DeliveryAnomaly`: record(subscriberId, globalPosition, Kind, detail, timestamp) + `Kind{NOTIFY_NOT_VISIBLE, LIVE_READ_EMPTY, LIVE_READ_FAILED, TRANSITION_READ_EMPTY, LIVE_READ_EXHAUSTED}`, `java.base` only. `InProcessEventBus`: 8-arg public ctor (`:211`) with trailing `Consumer<DeliveryAnomaly>`; the 7-arg delegates `anomaly -> { }` (the 3 test-side sites untouched: `InProcessEventBusTest:83`, `ReplayTransitionIT:96/:129`, testFixtures `InProcessEventBusFactory:143`); emits at `notifyEvent`'s empty page `:321` (`"*"`), `liveLoop` `:558` EMPTY / `:569` FAILED (`Simple: msg`); `emitAnomaly` `:620` (clock stamp, `RuntimeException` swallowed). `TransitionCoordinator`: ctor +emitter (`:77`); `TRANSITION_READ_EMPTY` at `:113`. `HomeSynapseCore:557`: `anomalyHandler` → `LOG.warn("bus.delivery_anomaly: kind={} subscriber={} position={} detail={} at={}", …)`, the 8th ctor arg. `ReplayTransitionIT.awaitCheckpoint:214`: the one stamp `(resting checkpoint N)`, bounds byte-unchanged (I8). Test: T1–T4 + `ScriptedReadStore` (scripts only single-position reads on a VIRTUAL thread — the loops' reads; `Clock.fixed`; 50 ms polls). MODULE_CONTEXTs: 33→34 types, 19→20 public, the two rows, 3 gotchas, a Phase-3 note; lifecycle blockquote + contract + gotcha 12. Byte-unchanged (`git diff --quiet`): both `module-info.java`, `HeroLoopHardwareFreeIT`, both `build.gradle.kts`, `libs.versions.toml`; `ReplayTransitionIT`'s waits/intervals/targets. ARCH-RULE-REACH zero collisions; no `download-artifact` consumer.
**FIX-1b (the patch).** `InProcessEventBus`: `LIVE_READ_ATTEMPTS = 5`, `LIVE_READ_BACKOFF_FIRST_NANOS` (1 ms); `liveLoop` → `readLivePosition` (one anomaly per failed attempt, `parkNanos` 1→2→4→8 ms, then `LIVE_READ_EXHAUSTED` + SUSPEND + `setMode`, loop exits); `notifyEvent` empty page → emit, then `offerUnfiltered` (active subscribers below the position via `routeByMode`, the lock-fused routing extracted from the filtered path; passive registrations get one `NOTIFY_NOT_VISIBLE` each — cannot be filtered without the envelope). `TransitionCoordinator`: ctor +`CheckpointStore`; `readQueuedPosition` (`DRAIN_READ_ATTEMPTS = 5`, same parks, `TRANSITION_READ_EXHAUSTED` + SUSPEND + `return false`; a read exception still SUSPENDs at once); **after each SUCCESS delivery `writeCheckpoint`, gated on `!atomicCheckpoint()`** — the fourth bus-side writer (AMD-45 gotcha updated). `Kind` +1. Neither IT touched. Tests +5: T5 `liveReadRetried_thenDelivered` · T6 `liveReadExhausted_suspendsHonestly` · T7 `notifyNotVisible_stillOffersToLive` · T-R1 `transitionDeliveries_advanceCheckpoint` (the burst runs INSIDE the driver's tail page read — one thread, real order) · T-R2 `transitionReadExhausted_suspendsHonestly`; stub +`emptyTimes`/`emptyOnceForPublisher`/`onPageReadRunThenEmpty`. INV-BUS-02 kept (no lock added to the publish path); T5 pins at-most-once; T6/T-R2 pin "checkpoint never past an undelivered position".

## §3 Gates + counts
- FIX-1a: `./gradlew :core:event-bus:compileJava :core:event-bus:test :lifecycle:lifecycle:compileJava :lifecycle:lifecycle:test spotlessCheck --offline` → **BUILD SUCCESSFUL 1m 5s**; `-Werror` clean; spotless clean everywhere; **event-bus 221 → 225 / 30 skipped / 0 · lifecycle 79 / 0 / 0**; XML 18:10:09Z / 18:10:27Z; LF ×9.
- FIX-1b: same → **BUILD SUCCESSFUL 36s**; **event-bus 225 → 230 / 30 / 0 · lifecycle 79 / 0 / 0**; XML 19:29:24Z / 19:29:40Z (both suites re-executed).
- The patch (v2, six files): `patch -p1 --dry-run` on the 10-file FIX-1a stand-in clean; applied → md5-identical to the gated FIX-1b bytes for all 6, `ReplayTransitionIT` untouched by it; LF. The tree restored to the FIX-1a bytes (md5 vs the snapshot). FIX-1a re-gated on the stamped bytes: **BUILD SUCCESSFUL 35s**, event-bus 225 / 30 / 0 · lifecycle 79 / 0 / 0, XML 21:54Z.
- **Deferred Build Gate: YES** — `./gradlew check` (incl. the app's 11 ArchUnit rules) NOT run here; owed to CI on the FIX-1a push (sample #4, now `--continue`) and the FIX-1b push (the gate of record; §B.4 samples veto-only).

## §4 Pushback / observations (source reads outrank the prose)
1. **[REVIEW] R2 — ReplayIT: a TRANSITION checkpoint gap in phase 1, not a lost unpark in phase 2.** `drainAndPromote` (`:83–:150` at `dc3328b`) wrote NO checkpoint; `ReplayDriver` checkpoints its paged tail, `liveLoop` per delivery. A burst ending inside TRANSITION is fully delivered (`phase1Delivered == 1000` would pass) while the checkpoint rests at the REPLAY tail → `:212`. It reproduces only UNLOADED: a starved subscriber is still in REPLAY when the burst ends and REPLAY checkpoints its tail — more pressure hid it. The `:302` `vt == null` window cannot open in either phase (`setVirtualThread` precedes every `notifyEvent` of the phase), and phase 2 cannot fail by this gap (10 ms spacing). If Act 3 says phase 2, a mechanism this desk did not see remains — the stamps make the next red carry `checkpoint=`/`mode=`.
2. **[REVIEW] R3 — HeroLoop: `notifyEvent` fan-out REORDERING, at source, unmeasured.** The router's filter is `{command_issued, command_dispatched}` (`IntegrationSupervisorAssembly:81`); `cacheIssued` runs in all modes. `notifyEvent(P)` fans out per subscriber with a `SqliteCheckpointStore.readCheckpoint` hop to the read executor per subscriber (`:151`) — the publishing VT parks at each. The dispatch service, offered P early in the fan-out, publishes `command_dispatched` and ITS `notifyEvent` can reach `integration_supervisor` before `notifyEvent(P)` does → dispatched before issued → `route_join_miss` with zero drops, hence zero FIX-1a tokens. Consistent with #183/#206/#225 (method-agnostic, one miss, no eviction) and with 0/90 here (µs checkpoint hops). Not implemented (the (c) row). FIX-2 candidate: instrument first (a per-subscriber monotonic `lastOffered` check → `LIVE_OUT_OF_ORDER`; a monotonic guard on the per-delivery checkpoint, which today can REGRESS on reorder), then the mechanism (ordered per-subscriber offers, or the AMD-42 §3.4.2 step-4 pull-from-cursor LIVE loop). Deterministic pin: block `readCheckpoint(router)` inside `notifyEvent(P)` until `notifyEvent(P+1)` has offered.
3. **[INFO]** the knob's `maxPoolSize=2` is stricter than a 2-vCPU runner's default 256; under the pin `parallelism` is already 2. Kept as specified; `ef02d13` ran without it (predates the property) — the rows agree at 0.
4. **[INFO] I6 recipe adaptations:** `taskset -c 0,1` → `start /affinity 3` (probe filed); busy loops → two CPU-bound PowerShell loops pinned to mask 3; `--rerun-tasks` → `--rerun`; `-q` → `--console plain` (a RED log carries the FULL assertion); `-PvtParallelism=2` → `ORG_GRADLE_PROJECT_vtParallelism=2` (cmd's batch tokenizer splits `=`); the `dirname` path trick → literal module dirs. Extra rows labelled `fix1a-noload` / `fix1a-stamp` / `fix1b*`.
5. **[INFO]** INV-BUS-02's text cites `EVENT_PUBLISHER_HAS_NO_DEPTH_GATED_LOCK`, absent from `HomeSynapseArchRules.java` (11 rules) — a docs correction candidate; DP-2 adds no publish-path lock regardless.

## §5 WUCP Phase 1: Coder Closeout
- [x] MODULE_CONTEXT.md updated for: core/event-bus, lifecycle/lifecycle (FIX-1a in the tree; FIX-1b deltas in the patch)
- [x] coder-handoff.md updated (the FIX-1 DELIVERED entry prepended; Deferred Build Gate at its top; NEXT WU pointer)
- [x] Deferred build gate flag: YES
- [x] coder-lessons.md appended: the measurement-shape inversion + the burst-inside-the-read pin
- [x] Cross-agent note posted: Not needed (channel retired; this return + the handoff entry carry it)
- Timestamp: 2026-09-05 21:55 UTC
