<!--
file: context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md
purpose: The v64 hub's beat-1 record — the boot at the instrument (§0), the intake of Nick's dispatch with every slot verified (§1), the executive model CHECKED at its cites and not re-derived (§2), the hub-originated contribution in H10 form — a stamp defect in the hub's own FIX-1 instruction, found at source before the loops ran (§3), the Block-1 adjudication rules PRE-FILED before the return exists (§4; H12 applied to the hub's own audit), and the weekend plan second-checked at three horizons on Nick's rider (§5). Every claim here has an instrument reading or a file path; nothing is from memory.
audience: the v64 hub · Nick · the v65 hub · any helper session
state-type: audit (hub-authored; appended-to at later v64 beats under dated §§)
status: FILED 2026-09-05 (v64 beat 1, 13:33 CT = 18:33Z). The Block-1 return audit will be its own file: context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md (named here so the path exists before the return does).
-->

# v64 beat 1 — the boot, the intake, the contribution, the pre-filed rules

## §0 The boot at the instrument (the v64 prompt §1, executed)
`date -u` first: **`Sat Sep  5 18:07:35 UTC 2026`** = 13:07:35 CDT (instrument: the linked computer's clock via device_bash; CT re-derived once as UTC−5). The §1 reads done whole and in order: the v64 prompt · pm-handoff `:8` + the newest three blocks (v63 b3 · b2 · b1) · PROJECT_SNAPSHOT whole · the grounding audit §0 + §3–§7 · the v63 brief §CLOSE + §CONTEXT + §NOW + §QUEUE · the ten v63 mints · the v63 dispatch prompt whole (the posture of record).

| Repo | Expected (the v64 prompt §1.2) | Read at 18:09:19Z | Porcelain | ahead/behind origin/main |
|---|---|---|---|---|
| homesynapse-core | `dc3328b` | **`dc3328b`** Merge pull request #5 … | **the FIX-1 lane in flight** — 18:09Z: 5 M + 2 A (Files-table rows 1–7); **18:25Z: 9 = 7 M + 2 A** (rows 1–9 — FIX-1a complete per the table; `ReplayTransitionIT.java` untouched). Observed, never staged, never touched. | 0 / 0 |
| homesynapse-core-docs | `a53f474` | **`a53f474`** AMD-99 RATIFIED | clean | 0 / 0 |
| nexsys-hivemind | the v63 beat-3 close commit | **`00c2d7f`** v63 beat 3 — THE v63 CLOSE | clean (the return ABSENT; no `??`) | **0 / 0 — in sync; `2e3f733..00c2d7f` = 3 (b1 `08aaa69` · b2 `99af3d1` · b3 `00c2d7f`)** |
| nexsys-skills | `f9c0bf4` | **`f9c0bf4`** W-SKILLS-6 (the FE half) | clean; no `.git/*.lock` (v63's stale `HEAD.lock` is gone) | 0 / 0 |
| nexsys-bench | `4539f13` | **`4539f13`** | clean | 0 / 0 |

**Zero drift on five HEADs.** The fold: `grep -c 'THE v63 CLOSE MINTS' context/lessons/pm-lessons.md` = **1**. The return: `context/audits/2026-09-05_FIX-1_return.md` and `…_FIX-1_loops/` — **ABSENT at 18:09Z** → the v64 prompt §8.3's else-branch: Block 2 (CG drafted ahead) until it lands.

**FRESHNESS PREFLIGHT — 2026-09-05 18:11 UTC — PASS (all 11 checks).** Notes, disclosed: Check 2 both spine chains name v63 beat 3; `context/planning/2026-09_september_plan-of-record.md` resolves (13,164 B). Check 3 the snapshot's core sha `dc3328b` = `log -1`. Check 5 six OPEN rows; five carry an explicit `Owner:`; **OR-JOURNALD-PRIO's owner was implicit** (the ruling line "RULED Row 7 (a) … charter queued behind FAILCHAN") — made explicit at this beat's Open Risks touch (hub charters / Coder implements). Check 6 `coder-handoff.md`'s newest entry (FAILCHAN, DELIVERED 09-03) points at Row 7 as "next on this desk" — superseded by the spine's FIX-1 insertion on the red CI (a recorded supersession in the newest beats, not a contradiction; the FIX-1 lane's own entry lands with its return). Check 7 21 MODULE_CONTEXT files; event-bus 59,473 B · lifecycle 80,951 B · integration-zigbee 194,226 B · rest-api 101,590 B — populated. Check 8 the channel is a retired pointer stub (ARCHIVED-WITH-POINTER, 0 active). **Check 9: 28 of 28 md5s EQUAL across the three pairs** (PM 10 · Coder 9 · FE 9; SOURCE trees on the device vs the account-synced copies this session loaded) — three-of-three at the bytes, matching v63's read. Check 10 97 `.md` references in `strategic-context-map.md`; the only three that do not resolve are template placeholders (`YYYY-MM-DD_topic.md` · `months/YYYY-MM_month.md` · `weeks/YYYY-WNN_monDD-monDD.md`) — not references. (The hub's first pass mis-resolved 10 docs-repo files against hivemind paths — an instrument error, corrected by resolving basenames against the three repos' `git ls-files`; disclosed.) Check 11 eight cited types resolve at source (`InProcessEventBus` · `TransitionCoordinator` · `CommandRoutingSubscriber` · `HeroLoopHardwareFreeIT` · `ReplayTransitionIT` · `ZigbeeIntegrationAdapter` · `IntegrationSupervisorAssembly` · `OpaqueTokenStore`); `settings.gradle.kts` 22 `include(` lines.

## §1 The intake — Nick's dispatch, at the bytes
Filed verbatim: `context/handoff/2026-09-05_v64_dispatch-text_verbatim.md`. It is the v63 brief §CLOSE Act C2 template with **every slot FILLED** — the first filled dispatch since the v56 shape was introduced, and the v63 mint ("a dispatch's STATE slots may arrive unfilled") honoured from the other side. Each slot verified at the instrument (§0's table): the lane `still running` ✓ (the return absent; FIX-1a's census complete on the tree at 18:25Z) · Act 3 `not read` — stays owed · the 09-05 nightly `not read` — stays owed · `FENCE-BUS: later` — the word stays open, the register untouched · `pushed through 00c2d7f (2e3f733..00c2d7f, in sync)` ✓ exact. **Nick's rider, quoted:** *"Take your time second-checking and validating all information — especially regarding how we plan/strategize out all the coding and research we are doing over this long weekend."* — read as a standing instruction on this session's tempo: verification before velocity; §5 answers it.

## §2 The executive model — checked at its cites, not re-derived
The grounding audit §0 names a source line for every model line. Each was read at the bytes this beat (bounded `sed -n`): strategy v1.2 `:6` (the D5 law line — "MISSING from the field, not superior") · `:15` (the two goals, register-shaped) · `:28–:29` (P3 THE FLEET · P4 BEYOND post-Nov-25) · `:56–:59` (Sep 11 `EU: ship|defer` · Sep 15 `Activate: apply|hold` · the NSF pitch · Oct the LICENSE FLIP on counsel, 10-31 fallback) · `:69` (the do-not list: no public claim outside the register) — **every line holds.** `claim-register.md` `:6` (status: C-002 LIVE, minted v62 b7 at `ef02d13`; C-001 narrow at `7c57d7f`) · `:9` (the header) · `:22–:24` (C-001 · C-002 · C-003 SLOT) · `:26` (the mint protocol) — **holds.** The plan §0-ter exists at `:21` (v62 b2, Nick's words 09-03); `:49` carries the R-4.5/R-5 charter + the FE rows — **holds.** No line of the model is contradicted by any document read this beat or by the spine as of v63 b3. The model is not re-derived; this section is the check the dispatch ordered.

## §3 The hub-originated contribution (H10) — a stamp defect in the hub's OWN instruction, found at source before the loops
**Source-first (the v63 mint), applied to the hub's own work product.** The FIX-1 instruction's §A.5 ReplayIT row predicts "NO `bus.delivery_anomaly` (the in-memory store is lock-protected); **the checkpoint's resting value in the message's neighbourhood is the diagnostic (1499 ⇒ the last unpark lost at `:302`; < 1499 ⇒ a drain gap)**", and Part B (a) says of ReplayIT's stall: "fix the handshake … **on the stamp evidence only**." The hub read the helper at `dc3328b` (the file untouched by the lane at 18:22Z — `git diff --stat` empty):
```java
// ReplayTransitionIT.java :200–:214 (dc3328b)
    private static void awaitCheckpoint(InMemoryCheckpointStore store, String subscriberId, long target, long maxWaitMillis) throws InterruptedException {
        long intervals = Math.max(1L, maxWaitMillis / 50L);
        for (long i = 0; i < intervals; i++) {
            if (store.readCheckpoint(subscriberId) >= target) { return; }
            Thread.sleep(50L);
        }
        throw new AssertionError("Checkpoint for '" + subscriberId + "' did not reach " + target + " within " + maxWaitMillis + " ms");
    }
```
**The message carries `target` and `maxWaitMillis` only — never the resting checkpoint.** So a red ReplayIT run's XML (even with `outputPerTestCase` + `exceptionFormat FULL`) yields the PHASE (`1000/15000` vs `<finalPosition>/30000`; `finalPosition = highestEmitted.get()` at `:166`) but NOT the stall's LOCATION. The §A.5 diagnostic is unmeasurable as instrumented, and FIX-1b's handshake fix would be authored on theory — a `[BLOCKING]` deviation by the instruction's own INSTRUMENT LAW — or deferred to a second core lane. The sibling `awaitMode` (`:219–:236`) has the same shape but is not implicated (the `dc3328b` red was `:212`). HeroLoop needs no stamp: its mechanism rides the per-method stdout (`route_join_miss` + the new `bus.delivery_anomaly` tokens).

**ESCALATION TO NICK** (handed in chat at ~18:27Z because the lane runs HeroLoop's loops FIRST — ≈1 h — then ReplayIT's; filed as the v64 brief's Act A, the copy-source of record).
**Task:** the running FIX-1 lane's ReplayIT instrument. **Question:** does the lane receive a one-paste stamp now, or does the hub adjudicate ReplayIT's mechanism from phase-only evidence at the audit?
**Options:** **(a) PASTE NOW** — one stamp, never a bound: `awaitCheckpoint`'s throw appends `+ " (resting checkpoint " + store.readCheckpoint(subscriberId) + ")"`; every wait, interval and target unchanged; `ReplayTransitionIT.java` becomes an M in FIX-1a's census (**10 = 8 M + 2 A**, declared `[INFO]` under §0 deviations — the Files table's own rule: "then only to add a stamp, never a bound"). Cost: one paste; if the ReplayIT row has already run stamp-less, that ONE row re-runs (~30 min). Buys: the stall located by measurement; FIX-1b's second mechanism authored on evidence. **(b) HOLD** — the loops run stamp-less; at the audit ReplayIT's location is unknown → the class takes branch (c) by construction (DP-2 anyway) and the handshake fix becomes its own row behind a second instrument run — a second core lane after FIX-1b, or a `main` that keeps flipping on ReplayIT (CG/FE/H8 each pay it). **(c) ASSUME the lane catches it** — refused: a slot is an act, never an assumption (the v63 mint); the lane had not touched the file at 18:25Z.
**PM recommendation: (a).** Horizon: H1 (the next commit's mechanism located, not theorized); costs H2 nothing measurable (≤30 min in the worst case, inside the lane's own band) and H3 nothing. **Refutable-by:** the return showing the lane added the stamp on its own (the paste is then a no-op), or ReplayIT reading 0/20 on the desk (no stamp would have helped; the class takes branch (c) and R2 below governs). **Blocking:** no — the hub proceeds to Block 2 either way. **Owned:** v63 beat 1 authored the row's diagnostic without reading the helper's throw — the same class of miss the FAILCHAN lane corrected twice; recorded for the v64 close mints.

## §4 The Block-1 adjudication rules — PRE-FILED before the return exists (H12, applied to the hub's own audit)
- **R1 — two classes, two mechanisms is the EXPECTED shape, not a deviation.** HeroLoop's store is SQLite (the empty/throwing read → DP-2); ReplayIT's `InMemoryEventStore` is lock-protected (§4-S4 of the grounding audit) → its stall is the park/unpark or drain handshake, which DP-2 does not touch. A FIX-1b whose census carries both a DP-2 hunk and a handshake hunk is on-instruction; a FIX-1b that fixes only DP-2 and declares ReplayIT "covered" is audited as branch (c) for that class and R2 applies.
- **R2 — branch (c) for either class: the gate still clears on FIX-1b's push green (the law), but OR-BUS-SILENT-DROP does NOT close on it.** Its closure condition re-cuts to a PASSIVE instrument: the `bus.delivery_anomaly` token ABSENT and both classes GREEN across the next 20 ordinary `main` runs (every push a sample; never a `main` re-run; `workflow_dispatch` samples veto-only). Nick's own arithmetic: at a 15 % flake rate four greens carry ~50 % of no information; 20 do not.
- **R3 — the corpus is ≥110 runs, not 60.** §A.3 (20 + 20) + §A.4 (20, or 10 per Act 5b) + the CPU-shape row (10) = 60–70 BEFORE FIX-1b; T8 + T9 add 40 on the FIX-1b tree. The audit reads `SUMMARY.md` line-by-line against BOTH tables and refuses a T8/T9 row that cites the pre-fix loops as its 20/20.
- **R4 — the corpus is redacted before it is staged.** Every `gradle.log` and XML in `…_FIX-1_loops/` is grepped for `Token:` (the v63 mint: a census that tails a log carries whatever the log carries); a hit is redacted `<REDACTED test-token TOKLEN=…>` with the redaction stated in `SUMMARY.md`'s header BEFORE the folder appears in any card. `TOKLEN-OK` binds in form.
- **R5 — mismatches FIRST.** The §A.5 table's Measured column is read against the Predicted column row by row before a single card line is written; every miss is adjudicated in the return audit's §1 (what the prediction assumed; which assumption the measurement refutes; what the fix's branch becomes) — never explained away. The hub's own prediction most at risk: "ReplayIT ≥1 RED in 20" — the CI sample is 1 red in 3 runs on a 2-vCPU runner; a fast desk under `taskset 0,1` + load may read 0/20.
- **R6 — FIX-1a's card is sample #4, not a gate; FIX-1b's is the gate.** FIX-1a's census: 9 = 7 M + 2 A as the table, or 10 = 8 M + 2 A if Act A landed — the return's §0 declaration governs, checked against `git status --porcelain` on Nick's tree at commit time (the card's first line).

## §5 The weekend plan, second-checked (Nick's rider) — three horizons, at the instrument
**H1 — the next commit (Sat → Sun morning).** The lane's tree at 18:25Z = FIX-1a complete; the loops are next. A derived band, assumptions stated: `./gradlew … --rerun-tasks --no-daemon` on two `taskset` cores under two busy loops ≈ 30–60 s of Gradle overhead per run; HeroLoop's class boots five cores (SQLite + Javalin) — CI's whole lifecycle module ran in ≈2–3 min on 2 vCPU (#206: 3m 12s for the build), so ≈2–4 min per run under load → HeroLoop ×20 ≈ 40–80 min; ReplayIT (15 s + 30 s waits at worst) ≈ 1–2 min per run → ×20 ≈ 20–40 min; the `ef02d13` row ≈ 30–80 min (10 or 20 runs); the CPU-shape row ≈ 20 min → **loops ≈ 2–3.5 h**; FIX-1b (DP-2 + T5–T7, plus the handshake hunk on R1) ≈ 1–1.5 h; T8/T9 ≈ 1–1.5 h → **the return ≈ 18:30–20:30 CT Saturday** if the lane runs uninterrupted — inside the v63 brief's 4–8 h band, at its far end. Then: the hub's two-layer audit (≤90 min; R1–R6) → Act 7 (FIX-1a, sample #4; ~5 min CI) → Act 8 (FIX-1b, the gate; CI + three veto-only samples ≈ 30 min) → **the gate CLEARED Saturday night or Sunday morning.** Risk named: a lane running ~110 Gradle runs in one Claude Code window is a context-health risk; the instruction routes every run's output to files and `SUMMARY.md` is appended per run, so a compaction loses nothing on disk — the return's §0 must say whether the lane compacted.
**H2 — the September plan (Sun → Tue).** CG-1/2/3 dispatches on the green (the instruction drafted at Block 2 this session) → the CG lane Sunday → the FE fast-follow (Block 4, drafted after CG lands) Sun/Mon → H8 real-wire on FIX-1b's CI-built artifact with the §6-B/EXITCODE proof riding (Block 5; Mon/Tue) → the docket addendum 17–34 + the docs correction block (Block 6; Nick's hands in `homesynapse-core-docs` — NOT the core tree) → P-1 + F-R4-1b (Block 7) → R-4c/C-003 next weekend. **One re-ordering the hub proposes for Nick's word, not this beat's:** Block 6's docs block is the only Nick-hands work that waits on nothing and lives outside the core tree; if the return is still absent when Block 2 closes, the hub pulls Block 6 ahead of Block 3 so Nick's Saturday evening lands it while the lane runs (cost: THE BEYOND INPUT starts ≈60 min later; gate: Block 1 preempts everything the moment the return exists). Word: `BLOCK6: pull | hold` — default `hold` (nothing changes without the word).
**H3 — the runway.** Unchanged: THE BEYOND INPUT (Block 3, deep work; docket Row 26; the bus's delivery-guarantee row the hub believes in) · `FENCE-BUS: add | hold` (open; `later` is Nick's word today) · the brand's dated words (Erik Mon 09-07 evening · E1 Mon · 09-09 · 09-11 · 09-15 · 09-17 · 09-18 the hard stop · 10-01 · 10-31). Nothing this weekend touches a fence.
