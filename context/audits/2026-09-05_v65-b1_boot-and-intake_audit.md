<!--
file: context/audits/2026-09-05_v65-b1_boot-and-intake_audit.md
purpose: The v65 hub's boot at the instrument (the v65 prompt §1, executed) + the intake of Nick's dispatch at the bytes + the drift adjudication + the preflight record + the executive model CHECKED (not re-derived) + the hub-originated contribution (H10) + the PRE-FILED adjudication rules and predictions for the three returns this session will audit (CG-1/2/3 · RS-10 · RS-11) — written BEFORE any return exists (ritual 5: the prediction first, then the corpus).
audience: the v65 hub (its own record) · Nick (§1 the slot table; §5 the ruling) · the v66 hub (what v65 pre-registered, so its adjudications can be checked against this file)
state-type: intake audit (append-only addenda per beat)
status: FILED v65 beat 1 (Sat 2026-09-05 ~21:36 CT; instrument 2026-09-06T02:36Z). Addenda append below §7.
-->

# v65 beat 1 — the boot at the instrument, the intake at the bytes, the rules pre-filed

## §0 The boot (the v65 prompt §1, executed in order; every reading from the instrument)
- **`date -u` FIRST: `Sun Sep 6 02:22:26 UTC 2026`** = Sat 2026-09-05 21:22 CT (UTC−5). The CT filing date of every file this beat writes is 2026-09-05.
- **The reads, in the prompt's order:** the v65 prompt whole (14,460 B) · `pm-handoff.md` line 8 (the chain: newest segment **v64 beat 10**, 2026-09-06T02:12Z) + the newest THREE beat blocks — **v64 b10 · b9 · b8** (the prompt §1 names b6 · b5 · b4: it was filled at the close, four post-close beats ago; the spine outranks it and the newer three were read; b7 read besides for the §9-L mint) · `PROJECT_SNAPSHOT.md` whole (3,488 B) · the FIX-1 audit §0 + §7 + §8 · the v64 intake audit §4 (R1–R6) · the v64 brief §CLOSE + §NOW · the v64 mints (`pm-lessons.md` :267–end, ten) · the v63 dispatch prompt whole (10,096 B; the posture of record) · the executive model §0 (§4 below) · the refinement charter §3 + §4 + §6 · the pre-spend charter §0.2 + §0.3 · the CG instruction's tail (the dispatch line) · the guarded-splice exemplar (the shape).
- **The five HEADs at 02:22:52Z (`--no-optional-locks status --porcelain` + `log -1`):** core **`e5fa035`** porcelain 0, ahead 0 ✓ (= the prompt's "FIX-1b's sha (clean)") · docs `a53f474` clean ✓ · skills `f9c0bf4` clean ✓ · bench `4539f13` clean ✓ · **hivemind `6542ef3` = `origin/main`** (ahead 0 — the v64 b10 commit, PUSHED by Nick; the dispatch text's "pushed through `dcf52d0`" is one commit behind the record — it was true when b10 wrote it; **the record wins: `6542ef3`**), porcelain **1** → §2.
- **The fold:** `grep -c 'THE v64 CLOSE MINTS' context/lessons/pm-lessons.md` = **1** ✓. **The push:** `rev-list --count origin/main..HEAD` = **0** on all five ✓.
- **The window's stale lock sweep:** no `.git/*.lock` present in the hivemind at boot (the RENAME sweep ran; nothing to move).

## §1 The intake — Nick's dispatch, at the bytes (the slot table; every value from the instrument or OWED)
**The paste = the on-disk text, byte-identical:** the fenced block of `context/handoff/2026-09-05_v65_dispatch-text_for-Nick.md` (lines 12–30) and Nick's message both read **7,444 B, md5 `bcb927bad4192d1dd85685dd5515332b`** (diff empty). **Every ⟨slot⟩ arrived UNFILLED** — Nick pasted the template as authored. So no slot is "given"; each is read at the instrument where one exists, and OWED where the instrument is GitHub or the Pi:

| Slot | At the instrument (02:22–02:33Z) | State |
|---|---|---|
| hivemind pushed through | `6542ef3` (b10), in sync — not `dcf52d0` | verified; the text is one commit stale |
| core HEAD / the gate | `e5fa035`, clean, ahead 0; the gate CLEARED banked at v64 b8 on Nick's word "all checks pass" (law 16) | verified (the sha); the verdict is the banked line |
| sample #4 (`5ed9178`'s CI) | GitHub — not reachable from the bridge | **OWED** (one line; informative only — never a gate) |
| the three veto-only samples | GitHub | **OWED** (`not yet | k/3`; lawful now) |
| Act 3 (the `dc3328b` ReplayTransitionIT message) | GitHub | **OWED** — decisive for ReplayIT's CI-red closure (phase 1 → closed by the drain fix; phase 2 → a mechanism the desk never saw) |
| Act 10 (the 09-05 nightly) | the Pi (`~/hs-bench/digests/nightly.log`) | **OWED** (read-only; s31 HANDS OFF) |
| CG-1/2/3 | core tree clean at `e5fa035`; no worktree; no `context/audits/*CG-123_return.md`; no coder-handoff entry | **NOT DISPATCHED at the instrument** (a running lane that has not yet edited would read the same — Nick's word decides; the line is handed again as Act 1) |
| RS-10 · RS-11 | no return file under `context/strategy/brand-program/` or `context/research/` newer than b10 | **not returned**; dispatch state = Nick's word (the charter's calendar says Sat night) |
| `BLOCK6:` | no word | OPEN — the rec `hold` stands (v64 b8: one docs touch after CG) |
| `ERIK: hold-for-RS10` | given (v64 b9) | HELD ✓ |

## §2 Drift — two items, adjudicated at `git log` before the first spine write
1. **hivemind HEAD `6542ef3` ≠ "the v64 close commit"** (the prompt §1's expectation, written at b6): four post-close beats (b7–b10) followed, each pushed by Nick (`00c2d7f..110fe88` · `110fe88..c8977f0` · `c8977f0..35040cb` · `35040cb..dcf52d0` · `dcf52d0..6542ef3`). The chain and the blocks carry them. **No conflict; the spine is the record.**
2. **The v65 dispatch-text file was DELETED in the working tree** (porcelain ` D context/handoff/2026-09-05_v65_dispatch-text_for-Nick.md`, unstaged, post-push; the file is in HEAD at 10,277 B). An operator act on the hub-run tree after Nick copied the paste from it. **Adjudication: restored from HEAD (`git checkout -- <path>`, 02:2xZ; porcelain → 0).** Grounds: the b10 block and the snapshot point at it as (A); the paste is byte-identical to its fenced block (§1), so nothing of Nick's is lost by restoring; a hub-run tree; a zero-content act. **Refutable by Nick's word** (`DISPATCH-FILE: delete`) → the hub removes it in a census-exact commit and re-points the b10 pointer at the verbatim file below. Not repeated in chat as a question — the default is the record.

## §3 The preflight — `FRESHNESS PREFLIGHT — 2026-09-06 02:27 UTC — PASS (all 11 checks)`
C1 snapshot `last-verified: 2026-09-05 (v64 beat 10` ✓ · C2 both spine files name v64 b10; `2026-09_september_plan-of-record.md` and `2026-08-23_pelton-week_plan-of-record.md` resolve ✓ · C3 snapshot Core `e5fa035` = `log -1` ✓ · C4 `phase-3-milestone-backlog.md` unchanged since `498c693` (08-18); PASS carried from the v64 reading of the same bytes (disclosed) · C5 six OPEN rows (NIGHTLY-0902-S31 · FAILCHAN · BUS-SILENT-DROP · JOURNALD-PRIO · REHOMED-OQ · M13-SDNOTIFY), each with an Owner and a commit/milestone, BUS-SILENT-DROP's Deferred Build Gate explicit, the section touched 09-05 ✓ · C6 `coder-handoff.md` newest entry (FAILCHAN-FIX-1 DELIVERED) carries the NEXT WU pointer (the landing → CG) ✓ · C7 21 `MODULE_CONTEXT.md` tracked, none template-sized ✓ · C8 `cross-agent-notes.md`: zero active entries above `## Archived` ✓ · **C9 28/28 md5-equal at the bytes** (PM 10 · coder 9 · FE 9; SOURCE on the device vs this session's synced trees `~/.claude/skills/synced/…`) ✓ · C10 every `.md` the strategic-context map cites resolves by basename against `git ls-files` of the five repos (templates/placeholders excluded); the map unchanged since `d20f75d` (07-25) ✓ · C11 `InProcessEventBus` 27 · `ReplayTransitionIT` 4 · `HeroLoopHardwareFreeIT` 3 · `ExitCode` 5 · `drainAndPromote` 4 files in core source ✓.

## §4 The executive model — checked at its cites, not re-derived
The grounding audit §0 (four lines: what the product IS · the WEDGE · the FENCES · the CRITICAL PATH) re-read; two cites re-executed at the instrument: `claim-register.md` `:6` (status: two rows LIVE, C-002 minted v62 b7 at `ef02d13`) and `:9 :22–:24 :26` (the register title; C-001 · C-002 · the C-003 slot; the mint protocol) ✓; strategy v1.2 `:6` (D5 — MISSING, not "superior") and `:15` (the two goals, register-shaped) ✓. **One line of the model has MOVED since v64 checked it, by the record, not by re-derivation:** the critical path's first node "FAILCHAN-FIX-1's green" is DONE (`e5fa035`, the gate cleared) — the path now reads **CG → FE fast-follow → H8 → R-4c/C-003 → the R-4.5/R-5 charter**, with FIX-2 a conditional insert (fires only on the pre-registered HeroLoop runner signal). The fences line gains **FENCE-BUS** (`claim-register.md:28`). Nothing else moved. The brand line's dated words as the b9 charter §0.2 re-cut them (the hold; Mon audits; Tue go-ahead; 09-09; 09-10; 09-18).

## §5 The hub-originated contribution (H10) — one number both documents assume and neither measured: the EXPEDITED turnaround in days
**The finding, at the bytes.** Erik's quote (v64 b8, the Pelton card): *$1,200 · 5 business days · expedite "a few hundred" extra* — **no day-count for expedite.** The b8 card then reasoned "with expedite the opinion lands ≈09-11"; the b9 charter §0.2 reasons "≈ Mon 09-14 → Tue 09-15: the expedited opinion (5 business days from 09-08)" and pins Thu 09-10 as "the last commissioning date that leaves ANY room between an expedited opinion (≈09-17) and the hard stop". The two documents price the same unmeasured number at 3 and at 5 business days; the charter's is the later and the conservative one, and it governs the calendar of record. **Why it matters now:** the pre-registered decision frame (beat 2) prices every iteration day after Tuesday in business days of slack before 09-18; that price is a function of the expedite turnaround. At 5 days the slack from a 09-08 commissioning is 3 business days (09-16/17/18); at 3 days it is 5. The whole RS-12 question (Wed 09-09 the last day) sits on that difference.
- **(a) Price the frame at 5 business days (the charter's reading) and ask Erik for the number in Tuesday's go-ahead email as one sentence** — cost: one line in the draft; risk: none; buys: a frame that cannot be optimistic, corrected upward by Erik's answer, never downward.
- **(b) Price it at 3 and ask** — buys two apparent days of RS-12 room that may not exist; the hard stop does not move to absorb a miss.
- **(c) Ask Erik now, before Tuesday** — the desk is closed Sat–Mon (Labor Day); the answer cannot arrive before the frame is committed; it costs a message and buys nothing the go-ahead email does not.
- **PM recommendation: (a).** The frame at beat 2 carries "expedite = 5 business days until Erik says otherwise" as a pre-registered assumption with its refutation named. **Refutable-by:** Erik's day-count on Tuesday; if ≤3, the RS-12 last day moves from Wed 09-09 to Fri 09-11 and the frame's cost-per-day line is re-cut on his number, not the hub's. **Blocking:** no. **Word:** none needed — this is inside the hub's authoring scope; Nick sees the sentence in the go-ahead draft on Tuesday and may strike it.

**The second contribution, in ruling form (the leverage line's concrete shape):** the order in which Nick opens tonight's windows is a dependency order, not a preference. **CG first** (host-side, in core — the only lane on the critical path; the FE fast-follow, H8, Block 6 and the freeze note all wait on its landing; ≈2–4 h of lane time and it runs unattended). **RS-10 second** (load-bearing for Tuesday; the b9 ruling: "if one window, run RS-10"). **RS-11 third** (insurance for Tuesday). **W-SKILLS-7 fourth** — after Lane C is committed (this session, the next commit) and after the CG line is in Nick's hands (guard 4; it is, by the v64 brief §CLOSE C3 and by Act 1 of the v65 brief). Refutable-by: a CG-lane STOP at its baseline check (the tree not at `e5fa035`/not clean) — then RS-10 opens first while the STOP is read.

## §6 PRE-FILED — the CG-1/2/3 return: the adjudication rules and the hub's predictions (written before the lane has run a byte)
**Rules (each re-executed by the hub at the bytes; the return's claims read critically first):**
- **R1 — census.** `git status --porcelain` on the lane's tree = exactly the instruction's Files table (**17 M + 0 A**) or the deviation declared in the return's §0 with its cause; a `??` file that is the return itself or a corpus folder is not a deviation.
- **R2 — red-first at two stages.** T1–T11 red at HEAD `e5fa035` (compile-red where a key does not yet exist; behaviour-red where it does), then green; T12 green-by-construction DISCLOSED; the evidence is the lane's test-run lines with counts, not the sentence "red-first".
- **R3 — `module-info.java` untouched** in every target module (the instruction: ZERO module-info change expected; the fallback embedded). A module-info hunk is adjudicated as R8, never accepted silently.
- **R4 — the additive contract.** Every existing key's presence, type and order unchanged (`RunEndpointsTest` untouched and green); the four new keys (`deviceId` · `lastReported` · `triggerRef` · `components[].ref`) render per §Wire shapes and read JSON `null` when unknown — the hub reads the wire fixtures the tests assert, not the prose.
- **R5 — one serializer, once.** CG-2 = A1-only (`CG2-SCOPE: A1-only`); a hunk on A2/A3's record-direct serializer is a scope breach → ACCEPT-WITH-RULINGS at best, the hunk reverted to a patch for Row 8's WU.
- **R6 — Row 30 as ruled.** Zero core lines for `firingValue`/`resultOutcome` (`ROW30: as-ruled`); a MODULE_CONTEXT gotcha + the FE sentence are the only outputs.
- **R7 — the desk gates with counts** (`:core:automation:test` · `:api:rest-api:test` · `:lifecycle:lifecycle:compileJava` · `spotlessCheck`) — the lane's claim, disclosed as not re-run by the hub; **CI on Nick's push is the gate of record** (law 16); the completion register stays REPO-COMPLETE, LIVE-VERIFICATION PENDING until H8 reads the keys on the card (law #22).
- **R8 — mismatches first.** A `[REVIEW]` that refutes an instruction sentence WITH line cites is instrument-first working (the v64 mint); the hub adjudicates it before any card line and owns the miss in writing.
**Predictions (H12; adjudicated first at the return):** **P1** the census lands exactly 17 M + 0 A (refuted at porcelain) · **P2** zero `module-info.java` hunks · **P3** the lane finds ≥1 defect or gap in the instruction at source and files it with a line cite (base rate: the last three core lanes each out-verified the hub at least once) · **P4** the lane's return is within the 12 KB cap at the first filing (base rate: FIX-1's was 13.5 KB — the hub expects a breach and will rule it on content, not size) · **P5** desk counts: `:api:rest-api:test` grows by exactly the T-count the instruction names (11), `:core:automation:test` unchanged or +≤2.

## §7 PRE-FILED — RS-10 and RS-11: what makes either return ACCEPT (the decision FRAME itself is beat 2's deliverable; these are the audit rules under it)
- **R1 — the verdict word is one of the three pre-registered** (RS-10: SPEND-JUSTIFIED · SPEND-NOT-JUSTIFIED · UNDECIDABLE) **and its stated conditions are met at the cited record** — the hub re-fetches the ★ rows (the LOCKIN prosecution record; the citation census of the four serials) at the pane, 2–3 items minimum, and names what it could not re-fetch.
- **R2 — R2-the-temporal-test is the load-bearing row** (which X-DOMO formatives were examined AFTER LOCKIN's filing and passed uncited): a return that argues the family without this row is UNDECIDABLE by construction, whatever word it carries.
- **R3 — the spend model is in MONTHS, not dollars** (the b9 ruling (i)); a return that re-argues the $1,200 has missed the axis and is ruled on the months row alone.
- **R4 — RS-11's S2 arrives in two parts** (contains DOMO entire · shares DOM-) so the hub can combine with RS-10; a single S2 is a gap row, not a fail.
- **R5 — VERDOMO is the control row**; every cell is read against it; a cell scored without the control beside it is re-scored by the hub or marked unscored.
- **R6 — no name is graded in chat.** The audits and the card carry names; this session's chat carries row IDs and verdict words only. Nick's taste rulings enter on the card, after both audits, never before (the charter §0.3).
- **R7 — a partial return Sunday outranks a complete one Tuesday** (the long-form prompt's clock); a lane still running at Monday 09:00 CT is audited on its living §0 card as filed.
**Predictions (H12):** **P1** RS-10 returns a verdict word other than UNDECIDABLE (the record is rich: RS-9 §A1/§A5 already table LOCKIN and the X-DOMO precedents) · **P2** RS-11's top cell is one of Nick's three variants (Job A) rather than a grid cell (Job B) — refuted if the S1–S2 kills take all three · **P3** at least one of the two returns breaches its cap (16 KB / 30 KB) at the first filing.

---
*Addenda append below, one per beat, headed `## §N Addendum — v65 beat K (<CT> = <Z>)`.*
