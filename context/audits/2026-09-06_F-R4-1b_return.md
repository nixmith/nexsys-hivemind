<!--
file: context/audits/2026-09-06_F-R4-1b_return.md
purpose: Coder return for F-R4-1b — the ZDO IEEE_addr_req second surface for interview-on-rejoin (the router-parented sleepy device). §0 card first: census · deviations by tag · the ZDP constants re-derived · the red-first table; then §1 per-file · §2 gates · §3 pushback · §4 WUCP.
audience: the hub (audit) · Nick (commits)
state-type: lane return
status: DELIVERED 2026-09-06 CT — REPO-COMPLETE, LIVE-VERIFICATION PENDING (R-4c)
-->

# F-R4-1b return — the ZDO `IEEE_addr_req` second surface (2026-09-06 CT)

## §0 The card

**Verdict: DONE (repo-complete); 2 [REVIEW] + 4 [INFO]; nothing STOP-grade.** Instruction: `context/instructions/2026-09-06_coder-lane_F-R4-1b_zdo-ieee-addr-req_second-surface_coding-instruction.md`. Baseline: core `main` HEAD `d192d17`, porcelain empty at launch (22:19Z). All 8 STOP gates matched byte-exact (the adapter/protocol/codec/ISM/T3 cites, module-info 4 requires + 1 exports, suite 582, `git grep lookup_miss` = exactly the 3 files). Preflight PASS 7/7. Instrument: `date -u` 22:19Z at launch, 22:44Z at filing; CT = UTC−5 → **2026-09-06**. Duplicate-dispatch check: no prior F-R4-1b return — not a re-paste.

**Census — CORE (`git --no-optional-locks status --porcelain`): EXACTLY 6 = 6 M + 0 A + 0 D — the Files table exact; ZERO staged; ZERO commits by the lane.** M `integration/integration-zigbee/MODULE_CONTEXT.md` · `…/src/main/java/com/homesynapse/integration/zigbee/ZdoCodec.java` · `…/EzspCoordinatorProtocol.java` · `…/ZigbeeIntegrationAdapter.java` · `…/src/test/java/com/homesynapse/integration/zigbee/ZdoCodecTest.java` · `…/ZigbeeInterviewOnRejoinTest.java`. `module-info.java` byte-unchanged (sha256 `4b7d8424…` before and after). ZERO build/catalog/schema/event/frozen-interface (`CoordinatorProtocol`, `InterviewOps`)/rig diffs. HIVEMIND: 1 A (this file) + 2 M (`coder-handoff.md` prepend · `coder-lessons.md` append); clean at `33f2127` before.

**Deviations by tag.**
- **[REVIEW] R1 — T7 re-fixtured (a pre-existing test beyond T3).** The instruction: "every existing test stays green; the ONE pre-existing assertion that changes is T3's." At the instrument (run 3: 589 run / 1 failed) T7's reopen leg — `lookupStatus = 0x01`, expecting `rejoin_candidate_unresolved` ×1 and nothing scheduled — RESOLVED through the air (the harness's default IEEE_addr_rsp is SUCCESS: a device asked for its own EUI64 answers). The P2 survey grepped `lookup_miss`; T7 asserts the WARN's COUNT. Re-fixtured with `ieeeAddrStatus = 0x81` beside the table miss and STRENGTHENED to pin DP-3 for the pair (the air re-asked after each reopen: 1, then 2 bodies). Test-only; production exactly as specified.
- **[REVIEW] R2 — +1 test beyond the table (589, not 588): T3e `…airAnswersWithAnotherNwk_admitsOnTheResponsePair`.** DP-6's last sentence + "log the pair on the INFO line" are log-observable arms; LTD-15 and law 19 need the conditional `response_nwk=` key pinned. Drop it and the count is 588.
- **[INFO] I1** — the return shape: `Optional<ZdoCodec.IeeeAddressResponse>` from `requestIeeeAddress` (the instruction's "simplest lawful shape"); the adapter reads BOTH fields, admits on the RESPONSE's pair.
- **[INFO] I2** — the response-nwk rendering: `zigbee.ieee_addr_rsp: nwk= device=` byte-exact when equal (the R-4c glance line); a trailing `response_nwk=0x{}` ONLY when it differs (T3e).
- **[INFO] I3** — the Files table's "+1 INFO token" on the adapter has no DP-7 counterpart ("NEW ×4 protocol lines"); none invented; `zigbee.rejoin_candidate` byte-unchanged.
- **[INFO] I4** — T3d is its OWN test (`…tableHit_neverAsksTheAir`), green-by-construction at HEAD, disclosed.

**The ZDP constants — re-derived before pinning from two references, both fetched 2026-09-06:** zigpy `zigpy/zdo/types.py` (dev) — `IEEE_addr_req = 0x0001` / `IEEE_addr_rsp = 0x8001`; request `(NWKI, RequestType, StartIndex)` with `AddrRequestType.Single = 0x00`; response `(STATUS, IEEE, NWK, Optional NumAssocDev/StartIndex/list)`; `SUCCESS 0x00 / INV_REQUESTTYPE 0x80 / DEVICE_NOT_FOUND 0x81`. zigbee-herdsman `src/zspec/zdo/definition/clusters.ts` + `buffaloZdo.ts` (master) — `IEEE_ADDRESS_REQUEST 0x0001` / `IEEE_ADDRESS_RESPONSE 0x8001`; the builder writes `UInt16 target · UInt8 reportKids · UInt8 childStartIndex`; the parser reads `status`, then ONLY on SUCCESS `eui64 · UInt16 nwk`; the tsn is the leading overhead byte. Both agree with DP-5/DP-6: `[tsn][nwk lo][nwk hi][0x00][0x00]` (5 B) / `[tsn][status][ieee LE 8][nwk LE 2]` (12 B min). Pinned in `ZdoCodec`'s BENCH-VERIFY block with the citation; herdsman's SUCCESS-only read is the residual (§3 O1).

**Red-first (predicted → observed):**
| Test | Predicted | Run 1 (HEAD production) | Run 2 (codec in; protocol/adapter absent) | Run 4 |
|---|---|---|---|---|
| C1 encode 5 B | compile-red | RED `encodeIeeeAddressRequest` absent | GREEN | GREEN |
| C2 SUCCESS + Extended tail | compile-red | RED `parseIeeeAddressResponse` ×3 · `IeeeAddressResponse` ×2 | GREEN | GREEN |
| C3 11 B empty / 12 B 0x81 present | compile-red | RED (same symbols) | GREEN | GREEN |
| T3 re-pin (double miss) | red | RED `CLUSTER_IEEE_ADDR_REQ` ×6 | RED `[exactly ONE IEEE_addr_req…] actual not to be null` | GREEN |
| T3b air HIT adopts | red | RED (compile) | RED `actual not to be null` (no 0x0001 sent) | GREEN |
| T3c air SILENT, 10 s | red | RED (compile) | RED `actual: 0L … ≥ 10000L` (no exchange) | GREEN |
| T3d table HIT, no air | green-by-construction | — (class compile-red) | GREEN | GREEN |
| T3e response nwk differs | red | RED (compile) | RED `actual not to be null` | GREEN |
| T7 (pre-existing) | green | — | GREEN | run 3 RED (R1) → re-fixtured → GREEN |
Run 1 = exactly 12 compile errors (6 codec-test + 6 harness). Run 2: 589 / 4 failed (T3 · T3b · T3c · T3e); every pre-existing test green. Run 3 (protocol + adapter in): 589 / 1 failed (T7). Run 4: 589 / 0.

**Deferred Build Gate: YES** — `./gradlew check` (the app's ArchUnit sweep incl. `NO_DIRECT_TIME_ACCESS`) NOT run on this desk; owed to CI on Nick's push of exactly the 6 atop `d192d17` (the gate of record). **Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING** — R-4c (⟨R4C⟩) on the held card.

## §1 What changed, per file (detail: `MODULE_CONTEXT.md` §F-R4-1b)
- **`ZdoCodec.java`** (+86/−1): the BENCH-VERIFY block `CLUSTER_IEEE_ADDR_REQ = 0x0001` · `CLUSTER_IEEE_ADDR_RSP = 0x8001` (citation in the comment); nested package-private `record IeeeAddressResponse(int status, IEEEAddress ieeeAddress, int networkAddress)` (collision check clean); `encodeIeeeAddressRequest` → 5 B; `parseIeeeAddressResponse` → empty < 12 B, else present WITH the status (never logs, never judges).
- **`EzspCoordinatorProtocol.java`** (+77): package-private `Optional<ZdoCodec.IeeeAddressResponse> requestIeeeAddress(int nwk, long timeoutMillis)` beside `lookupIeee` — IAE range check → INFO `ieee_addr_req` → `zdoUnicastExchange` → the four DP-7 outcomes exactly (empty ⇒ `ieee_addr_req_unanswered`; < 12 B ⇒ `EzspFormatException`; status ≠ 0 ⇒ `ieee_addr_rsp_failed`; else `ieee_addr_rsp` + present). Time only via the seam's injected clock; `CoordinatorProtocol` untouched.
- **`ZigbeeIntegrationAdapter.java`** (+84/−28): `IEEE_ADDR_REQ_TIMEOUT_MILLIS = InterviewStateMachine.STEP_TIMEOUT_MILLIS` (DP-4); the H-ii arm per the instruction's shape ((3) only after a clean (2) empty; `reason=<message>` / `reason=zdo_miss`; the RESPONSE's pair into `admitRejoinCandidate`); the stale comment rewritten; the two propagating exceptions NOT caught; H-i, the admission, the window, the payloads untouched.
- **`MODULE_CONTEXT.md`** (+29/−2): the `## F-R4-1b Implementation` section (deltas · 9 gotchas · the R-4c glance grammar · tokens) in the established position; the two in-place pointers (`:744` `lookup_miss` RETIRED · `:752` `[SUPERSEDED at F-R4-1b …]`); header type count unchanged.
- **`ZdoCodecTest.java`** (+65): `@Nested IeeeAddressExchange` C1–C3 (the R-4b device as the vector). **`ZigbeeInterviewOnRejoinTest.java`** (+240/−16): harness `zdoRequests` (per-cluster REQUEST capture), `ieeeAddrStatus` / `ieeeAddrSilent` / `ieeeAddrResponseNwk`, the `zdoReply` 0x0001 case (12 B; zeroed EUI64 on a miss); T3 re-pinned; T3b/T3c/T3d/T3e; T7 (R1). `TestClock` kept; no wall clock.

## §2 Gates run + counts (Windows 11 · JDK 21.0.4 · Gradle 8.8 · `--offline`)
Run 1 `:integration:integration-zigbee:test --rerun` at HEAD production → `compileTestJava FAILED`, 12 errors. Run 2 (codec in) → 589 / 4 failed. Run 3 (all production in) → 589 / 1 failed (T7). **Run 4 = the allow-list on the final bytes: `:integration:integration-zigbee:compileJava :integration:integration-zigbee:test --rerun spotlessCheck --offline` → BUILD SUCCESSFUL in 6s; `compileJava` UP-TO-DATE (executed clean in run 3 on byte-identical production, 0 `warning:` under `-Werror`); `compileTestJava` + `test` EXECUTED (the `--rerun` bound to `test`); `spotlessCheck` executed on the module; 0 warnings / 0 errors / 0 FAILED.** Freshness at the instrument: 77 result XMLs, every mtime **2026-09-06T22:39:52Z**; XML aggregate **tests=589 failures=0 errors=0 skipped=0** (the CI count). **Suite 582 → 589** (`grep -rc '@Test'` = 589; ZdoCodecTest 10 → 13; ZigbeeInterviewOnRejoinTest 12 → 16). LF ×6 (CR = 0); the production hygiene greps (`synchronized`, wall clocks, `System.*`, `Thread.sleep`) clean; `git grep lookup_miss` post = MODULE_CONTEXT only.

## §3 Pushback / observations
- **O1 (BENCH-VERIFY — the likeliest wire disagreement; not a deviation):** zigbee-herdsman reads the EUI64/nwk fields ONLY on SUCCESS; a stack answering a failure with `[tsn][status]` (2 B) surfaces here as `EzspFormatException` → `reason=IEEE_addr_rsp …` instead of `ieee_addr_rsp_failed` + `zdo_miss` — the same outcome, a different vocabulary. DP-6 implemented as pinned; the one-place flip (status-first read; the 12 B floor for SUCCESS only) is in the gotcha. The device answers for its OWN address, so a non-success status is the exotic arm.
- **O2 (the instruction's pushback slot — `timeout_ms=` on the rejection arm):** the seam returns empty for BOTH an NCP rejection and the deadline, so the WARN on a rejection names a bound that never elapsed. Not misleading in practice: `sendUnicastLocked`'s WARN `zigbee.aps_unicast_rejected: cluster=0x1 nwk= status=` immediately precedes it. The split (`reason=ncp_rejected|deadline`) needs `zdoUnicastExchange` to return a typed 3-way result touching the interview + reporting callers — not done; the hub rules.
- **O3 (DP-6 vs `recordAnnounce` at source):** no conflict — `ZigbeeDeviceCache.recordAnnounce(ieee, nwk)` "creates the initial record or re-indexes the NWK address" (`:111`). No STOP-note.
- **O4:** sleepy-device physics is NOT modelled on the desk — T3b/T3e's synchronous reply is the frame-shape pin; the wire at R-4c decides; an `ieee_addr_req_unanswered` on the SNZB-02P re-opens DP-4, not the WU.
- **O5:** the Files table's "+1 INFO token" (adapter row) has no DP-7 counterpart — I3; the cell may want correcting.
- **O6 (lesson filed):** a resolver added AFTER a miss changes the meaning of every scenario that scripts that miss — grep the harness for the prior surface's miss script, not the reason string (coder-lessons 2026-09-06).

## §4 WUCP Phase 1: Coder Closeout
- [x] MODULE_CONTEXT.md updated for: integration-zigbee (the section + the two in-place pointers)
- [x] coder-handoff.md updated (the DELIVERED entry prepended, newest-first; Deferred Build Gate + the NEXT WU pointer)
- [x] Deferred build gate flag: YES (`./gradlew check` → CI on Nick's push = the gate of record)
- [x] coder-lessons.md appended: the miss-script re-fixture lesson (1,080 B)
- [x] Cross-agent note posted: Not needed (the channel is retired; this return + the handoff entry carry it)
- [x] NEXT WU named: the hub's audit (rules R1/R2) → the msg file → Nick's commit + push (stages EXACTLY the 6) → CI green = the gate → **R-4c on the held card (the C-003 slot)** → P-1's charter
- Timestamp: 2026-09-06 22:44 UTC
