<!--
file: context/audits/2026-09-06_FE-113_return.md
purpose: FE-113 lane return — the read-API v1.1.3 mirror (CG-123 fast-follow) in web-ui/dashboard/. §0 card first; the hub audits (§8 rules); Nick commits.
status: DELIVERED — REPO-COMPLETE, LIVE-VERIFICATION PENDING (law #22 / H8)
-->

# FE-113 return — the v1.1.3 mirror: four additive keys, rendered honestly (filed 2026-09-06 CT)

## §0 The card
- **Verdict: DELIVERED — REPO-COMPLETE, LIVE-VERIFICATION PENDING** (verified only when H8's real-wire read shows the keys rendered). Baseline `093d5b4`, porcelain EMPTY at launch. `date -u` at boot **2026-09-06T15:44Z ⇒ CT = UTC−5 = 10:44 CDT** (once); filed 2026-09-06 CT. Instrument: the container, not the desk VM (I4).
- **Census (lock-free porcelain on Nick's tree, 16:08Z): 16 = 14 M + 2 A — ALL under `web-ui/dashboard/`; ZERO staged; ZERO commits.** M: `MODULE_CONTEXT.md` · `scripts/contract-check.mjs` ★R1 · `src/lib/api/{contract.ts, contract.test.ts, shapes.ts, mock/mockData.ts}` · `src/lib/{format.ts, format.test.ts, i18n.ts}` · `src/views/{DevicesView.tsx, DevicesView.honesty.test.tsx, WhyNotView.tsx, WhyNotView.nullability.test.tsx, AutomationsView.tsx}`. A: `src/lib/api/v113-additive.test.ts` · `src/views/AutomationsView.ref.test.tsx` ★R2. Untouched: `fixtures/*` (md5 `896f861d…`/`05b02fbc…`), `realTransport.ts`, `client.ts`, `scenarios.ts`, CSS.
- **Predictions (H12):** **P1 MISSED** — 14 M + 2 A: the table's 10 M rows + its 3 named test files (M) + "its test" for AutomationsView (absent at HEAD → A) + `contract-check.mjs` (R1); owned. **P2 MET** (fixtures byte-unchanged; both validate; `'triggerRef' in data === false` pinned). **P3 MET** (verify GREEN in one round). **P4 MET** (five stale reads, §3 S1–S5; the load-bearing one: §5 says `EXPECTED_VERSION` "does not exist" — it is `scripts/contract-check.mjs:13` and gates `verify`).
- **Red-first:** HEAD pristine 230/230 → +tests at HEAD **22 RED / 252 / 274** + tsc 20 type-errors on the keys → edits → **274/274, tsc 0** (§1 table).
- **Gate: run 3 `npm run verify` 16:04:09–:31Z GREEN** — tokens:check · lint 0 · typecheck 0 · test **20 files / 274** · build 34.69 kB gzip js · bundle **64.9 KB / 100** (was 64.3) · contract-check **v1.1.3-2026-09-06**. Gate of record: `frontend.yml` on Nick's push (Deferred Build Gate: YES).
- **Deviations:** **[REVIEW] R1** +1 M `contract-check.mjs` — `EXPECTED_VERSION` v1.1.2→v1.1.3 (§5's fence is stale; `check:contract` runs inside `verify`; the v1.1.2 precedent moved it too). **[REVIEW] R2** +1 A `AutomationsView.ref.test.tsx` — no AutomationsView test exists at HEAD. **[INFO]** I1 `registry.ts` cites off by 2–3 · I2 `contract.ts:140` "ENTITY" fixed as doc; lowercase `entity` pinned, never normalized · I3 §7's `coder-handoff.md` entry NOT written (§0's fence; the hub is the spine-writer) — drafted in §4 · I4 the desk VM cannot run the toolchain (Windows `node_modules`; disk 99% full → `npm ci` ENOSPC): the pre-gate ran in the container on `git archive HEAD web-ui/dashboard` (Node 22.22.2, `npm ci` lockfile-exact); the 16 files went back through the device bridge, **md5-identical both sides**; the tarball `_scratch/2026-09-06_FE-113_dashboard-src-at-093d5b4.tgz` (208 KB) is scratch — delete at will.

## §1 Per-file deltas + the red-first table
**Mirror.** `contract.ts`: `CONTRACT_VERSION = 'v1.1.3-2026-09-06'`; a v1.1.3 block in the :15 style (four keys, nullability, the two-instants note, the H8 warning); `EntitySummary += deviceId?: string|null; lastReported?: string|null`; `NonFiringExplanation += triggerRef?: SubjectRef|null`; a named `ComponentSummary {type, summary, ref?}` (additive, same shape); the `SubjectRef.type` doc corrected. `shapes.ts`: `refOrNull(v, path)` beside `strOrNull` (null passes; object → `subjectRef` string type + string id; else `ContractError`); A1 `if ('deviceId' in e) strOrNull` / `if ('lastReported' in e) strOrNull` (a number = the epoch class, FAIL); B3nf `if ('triggerRef' in d) refOrNull`; B3auto per component `if (isObj(c) && 'ref' in c) refOrNull`. `contract-check.mjs`: the pin. **Copy.** `i18n.ts` +3 keys (`devices.freshness.noClaimTitle` · `devices.freshness.nullTitle` = "This hub has no report time on record for this entity." · `devices.deviceIdLabel` = "Device"); `format.ts`: `LIST_FRESHNESS_NO_CLAIM_TITLE` re-scoped to the pre-v1.1.3 hub and keyed; `LIST_FRESHNESS_NULL_TITLE` added. **Views.** `DevicesView`: the Reading cell — stale → pill (unchanged); key ABSENT → em-dash + no-claim title; PRESENT-null → em-dash + NULL title; PRESENT-string → `lastReportedCell`; a muted mono `Device <ulid>` line under the entity id only for a string. `WhyNotView`: `useRefResolver()`; a `Watching: <TriggerEntity>` line under `triggerSummary` only for a PRESENT-object `triggerRef` — resolved → `refLabel` as a link to `/devices/<id>`; dangling → `entity <ulid> — not in this hub's registry` + the FE-HONEST-1 `StatusPill(error, UNRESOLVED_REF_PILL, UNRESOLVED_REF_HELP)`; null/absent → the sentence alone. `AutomationsView`: `useRefResolver()`; per component `ComponentLine` — dangling → the same LOUD render; resolved → `(name)` unless the summary already contains it; null/absent → the summary. **Mock.** `mockData.ts`: 6 rows × both keys (4 ULID device ids, 2 `deviceId: null`; 5 stamps mirroring the A3 state, 1 `lastReported: null`); `triggerRef` on all 3 non-firing entries (object on CONDITION_NOT_MET/DISABLED, null on NEVER_TRIGGERED); `ref` on all 8 components (2 null); ONE dangling: `DANGLING_LAMP_ULID = 01KX1PB9AAB4VB3E10BD477TVX` on the disabled night light's action. **MODULE_CONTEXT.md**: the FE-113 banner; the contract reference line (v1.1.3, the freeze-doc stamp still v1.1.2 — hub-owned); two Gotchas: THE TRI-STATE IDIOM and the §DP-5 `CONFIRMED` + `resultOutcome: null` happy path.

| # | Test file | added | Run 1 HEAD | Run 2 post-edit |
|---|---|---|---|---|
| T1 | `contract.test.ts` version pin | 0 (1 M) | RED by construction | GREEN |
| T2 | `v113-additive.test.ts` (A) | 25 | **12 RED** (8 "throws" — the v1.1.2 validators ignore unknown keys; 4 mock pins) · 13 g-b-c (typed/null/absent passes ×9, P2 ×2, mock validates, the typed mirror — **type-red under tsc**) | GREEN 25/25 |
| T3 | `format.test.ts` | 2 | 1 RED (`LIST_FRESHNESS_NULL_TITLE` undefined) · 1 g-b-c (nanos parse) | GREEN |
| T4 | `DevicesView.honesty.test.tsx` | 6 | **3 RED** (PRESENT-null title · PRESENT-string stamp · Device line) · 3 g-b-c (absent cell · null→nothing · absent→nothing) | GREEN |
| T5 | `WhyNotView.nullability.test.tsx` | 5 | **2 RED** (resolved · dangling) · 3 g-b-c (incomplete census · null · absent) | GREEN |
| T6 | `AutomationsView.ref.test.tsx` (A) | 6 | **3 RED** (resolved · dangling · mixed) · 3 g-b-c (incomplete · null · absent) | GREEN |
| — | the other 14 files (230) | 0 | GREEN | GREEN (a11y axe suite included) |

(g-b-c = green-by-construction, disclosed: the absent/null arms MUST NOT change the surface. Two test-side type nits were fixed after run 2 — not source red.)

## §2 The gates, counts, sweeps
- **Preflight (15:46Z):** 1 PASS · 2 STALE-by-design (the contract advanced core-side — this WU is the fold) · 3 PASS · 4 STALE-minor (no FE-HONEST-1 beat for `lib/registry.ts` in MODULE_CONTEXT — now named) · 5–6 PASS (`DESIGN: hold`, name-light) · 7 PASS (skill mirror md5 9/9) · 8 PASS (literals re-read at the Java: `subjectRefView("entity", …)` `:573/:745/:773` · `ListEntitiesEndpoint:197–:204` · `GetNonFiringEndpoint:129`).
- **Runs (UTC, container):** 0 15:51:16 HEAD `npm run verify` GREEN 18 files/230, bundle 64.3 KB · 1 15:56:31 HEAD+tests `vitest run` **22 F / 252 P / 274**; `tsc` 20 errors (TS2339/TS2353 on `deviceId`/`lastReported`/`triggerRef`/`ref`) · 2 16:02:13 post-edit `vitest` **274/274**; `tsc` 0; `eslint` 0 · **3 16:04:09–:31 `npm run verify` GREEN** (§0). Logs in the session scratchpad (not filed).
- **Visual (6 Playwright screenshots on a `VITE_USE_MOCKS=true` build, light + dark):** Devices — the Device line only on the 4 rows with a string, em-dash on the null row, stamps on the rest; Automations — the LOUD pill + phrase on the night light in BOTH themes, `(Frontdoor Contact)`/`(Livingroom Lamp)` parentheticals where the humanized fallback differs from the summary; WhyNot — `Watching: Bedroom Motion` (DISABLED) / `Front Door` (CONDITION_NOT_MET) as links; zero page errors.
- **Laws swept:** no CSS added (existing tokens inline) · no dependency · no new landmark (a11y suite green) · state by label + glyph + text, never hue · name-light (`t()` keys) · no epoch parse (`lastReportedCell` only) · no case normalization of `entity` · nothing phones home.

## §3 Pushback / observations
- **S1–S5 (P4, stale at source):** S1 §5 `EXPECTED_VERSION` (R1) · S2 row 9 "+ its test" (R2) · S3 `registry.ts` cites (I1) · S4 `WhyNotView.tsx :63–:95` — the `triggerSummary` row is :80–:83 · S5 §7 vs §0's fence (I3).
- **O1 [INFO, not changed — outside the table]:** the default mock's two CONFIRMED actions (`mockData.ts:260,:367`) and `scenarios.ts:75` carry `resultOutcome: 'acknowledged'`; per CG §DP-5 the live happy path serves `null` there (the adapter publishes only unroutable/rejected/unconfirmed) — an H8 false-VALUE candidate; the `live-nulls` scenario carries the real arm; recorded in the MODULE_CONTEXT gotcha for the next mock touch.
- **O2:** `scenarios.ts` `buildDanglingRef` still says the contract gap "is what would let this surface go loud too" and serves no `triggerRef` — the gap is closed; the next scenario touch should add `triggerRef: {type:'entity', id: GHOST_TRIGGER}` so the dangling-ref story exercises the WhyNot LOUD render on its own scenario.
- **O3:** the `/devices/:id` route renders `DevicesView` without opening the drawer for the id (`app.tsx:80–:82`) — the new "Watching:" link lands on the list; drawer-by-route is a small hero-charter follow-up.
- **O4:** a stale row still renders the "Stale" pill alone (the tri-state named the non-stale cell only); the pill title could carry the stamp. **O5:** `contract-consumer.md` has no additive-procedure section yet — this instruction served as it. **O6:** `contract.test.ts`/CausalChain tests still author `type: 'ENTITY'` (uppercase) in their chain fixtures — test fiction vs the wire; harmless; cleanup candidate.
- **Cross-lane asks (hub):** the freeze-doc v1.1.3 note (+ the two-instants window); the `EXPECTED_VERSION` wording in the instruction template (three pins move together).

## §4 WUCP Phase 1 (the handoff entry, drafted for the hub to fold — I3)
- [x] MODULE_CONTEXT.md updated (web-ui/dashboard) · [x] Deferred Build Gate: **YES** — `frontend.yml` on Nick's push (`cd web-ui/dashboard && npm ci && npm run verify`) · [x] fixtures READ ONLY, md5 unchanged · [x] register: REPO-COMPLETE, LIVE-VERIFICATION PENDING · [ ] `coder-handoff.md` — NOT written by this lane (§0 fence); entry text: *"FE-113 delivered 2026-09-06 CT: v1.1.3 mirrored (14 M + 2 A under web-ui/dashboard/, two [REVIEW]s: contract-check.mjs pin; AutomationsView.ref.test.tsx). Gate: desk-equivalent verify GREEN 274/274; CI on Nick's push = the gate of record. NEXT: the hub's audit → the msg file → Nick's commit of exactly the 16 → CI green → H8-a real-wire reads `deviceId`/`lastReported`/`triggerRef` on the card (the v1.1.3 capture → FE-113b fixture) → the explainability-hero charter."* · lesson (if the hub wants one): **a fence that names a constant must be grepped at source before dispatch** — `EXPECTED_VERSION` existed and gated the very gate the lane was told to keep green.
- **Skill DoD (§9):** preflight run ✓ · contract consumed field-for-field, mocks carry the live shapes incl. present-null ✓ · every state honest and distinct, AA in both themes (existing tokens), no color-only meaning, copy keyed + test-locked ✓ · bundle 64.9/100, gate green (pre-gate) ✓ · name-light ✓ · return filed with the register + the next WU ✓.

RETURNED nexsys-hivemind/context/audits/2026-09-06_FE-113_return.md 12223
