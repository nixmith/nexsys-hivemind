<!--
file: context/audits/2026-09-07_TR-1_intake_two-layer-audit_v67-b5.md
purpose: The hub's two-layer intake audit of the TR-1 return (the position census as a bench verb — a read-only design lane; charter: `context/planning/2026-09-06_v65-b6_post-landing_program_and_four-charters.md` §5 "TR-1"): the claims read critically, then the hub's own re-execution at the bytes (five checks), the predictions adjudicated, the one ruling the lane asks for taken under the DELEGATE frame (REVERT-able), the precondition docketed as an owed line, and the follow-on WU named.
audience: the hub (this beat + the next bench charter) · Nick (one owed line; one REVERT word if he refuses the ruling) · the TR-1b lane (reads §3)
state-type: intake audit
status: FILED v67 beat 5 (Mon 2026-09-07 ~19:3x CT; instrument 2026-09-08T00:27Z). Return: `context/research/2026-09-07_TR-1_position-census_bench-verb_design_return.md` (12,243 B; dispatched 18:30 CT, returned ~19:2x CT — ~1 h against ≤3 h; read-only fences held: core `39c8dd3` and bench `4539f13` unmodified, the return the only write — VERIFIED at porcelain both repos).
-->

# TR-1 — intake audit (two layers)

## §0 Verdict
**ACCEPT.** The return answers Q1–Q4 with `file:line` receipts, adjudicates its own predictions FIRST and honestly (two of three SPLIT against itself), and its two design-changing findings re-execute at the bytes. Cap respected (12,243 B ≤ 12 KB + 243 B — under the cap sentence's spirit; the §0 card is 2.9 KB). Nothing staged; nothing run. **One convention miss, owned by the lane and accepted on the bytes:** the `RETURNED <path> <bytes>` line was printed to the chat (Nick's paste) and NOT written as the file's last line — the file ends on the D5 sentence; the hub verified the byte count (12,243) against the paste and the mtime static since 23:42Z, and treats the file as the return. The charter's "your last line" means the FILE's last line; W-SKILLS-9 adds the sentence to the lane template. **One ruling taken under DELEGATE (§3); one owed line (§4); the follow-on WU named (§5).**

## §1 Layer 1 — the claims read critically
- **P1 SPLIT** (the lane's own reading, accepted): no Core read seam — but THREE tables (`events` · `subscriber_checkpoints` · `subscriber_dead_letters`) plus a FOURTH input that lives in no store: the filter manifest built at registration and never persisted. The literal prediction is wrong; the meaning ("nothing new in Core need be built") holds. *Consequence:* a census that runs OUTSIDE the process must carry the manifest as a fixture — which is exactly why the verb's design (below) runs it IN-process.
- **P2 SPLIT:** < 1 s for one household night on the card (a rowid range scan; the spike's 0.4–0.5 s full-replay figure is the upper bound); the BENCH night's 40 process-scoped censuses are seconds. Accepted as read; the number is a bound, not a measurement — the verb will measure it.
- **P3 MET exactly** — `V001:68-72`, a current-position row, no history. The lane's rider is the important sentence: the census is a post-drain high-water-mark COMPARISON, never a per-position delivery ledger.
- **Finding 1 (design-changing):** naive `checkpoint ≥ P` OVER-REPORTS delivery: LIVE checkpoints write only on `SUCCESS`, so a DLQ-parked position is jumped by the next success; REPLAY advances its position past non-matching rows. Therefore the census must SUBTRACT the dead-letter rows and special-case the State Projection's atomic checkpoint (AMD-45 §2.2 Option A — the bus does not write its checkpoint at all). *Read critically:* correct, and it is the reason a naive "checkpoint ≥ position" verb would have minted a FALSE B-1 sentence. This finding alone pays for the lane.
- **Finding 2 (design-changing):** the bench format forbids a `sqlite:` assertion by design and has no test-runner stimulus; further format changes are STOP-gated. So the verb needs neither: the ITs compute the census in-process and print ONE frozen token that the existing `log:` / `extract:` / `min:` mechanic asserts — the `boot-health.yaml:40-43` shape. *Read critically:* this respects two fences at once (the format gate; s31/nightly hands-off) and keeps the bench a consumer of frozen tokens, which is the bench's law.
- **B2 — the stimulus grammar (the lane's one ask):** running the ITs needs a stimulus the format lacks (`test-runner`). The lane recommends NOT reopening the gated format: run the verb as a `fix1-loop.sh`-lineage DRIVER outside the scenario format, contributing one extra digest line, until R-5 re-baselines the floor. *Read critically:* the cheaper shape and the one that touches nothing gated; the cost is one more script in the bench's `tools/` lineage, which R-5 can fold.
- **Precondition (no receipt in either repo):** the card's `bootstrap.sh` installs no JDK or Gradle → whether the card can run `:test` tasks at all is UNKNOWN; `./gradlew --version` on the card settles it. *Read critically:* an honest gap, correctly not guessed (arc 1 — never author on an unmeasured hop).

## §2 Layer 2 — the hub's re-execution at the bytes (five checks; `39c8dd3` · `4539f13`)
1. `core/persistence/src/main/resources/db/migration/events/V001__initial_event_store_schema.sql:68-72` — `subscriber_checkpoints (subscriber_id TEXT PRIMARY KEY, last_position INTEGER NOT NULL, last_updated INTEGER NOT NULL)` — **VERIFIED verbatim.**
2. `core/event-bus/src/main/java/com/homesynapse/event/bus/InProcessEventBus.java:557` — `if (result == SubscriberSupervisor.DeliveryResult.SUCCESS)` gates the write; `:565-566` — `if (!runtime.info().atomicCheckpoint()) { checkpointStore.writeCheckpoint(subscriberId, envelope.globalPosition()); }` with the AMD-45 §2.2 comment — **VERIFIED**: the checkpoint is SUCCESS-only and the atomic subscriber writes none here (Finding 1's two mechanisms).
3. `core/event-bus/src/main/java/com/homesynapse/event/bus/ReplayDriver.java:191` — `currentPosition = envelope.globalPosition();` after the delivery branch, i.e. the replay position advances for every envelope in the scan (`eventsSinceCheckpoint++` only for delivered ones) — **VERIFIED** as the line reads; the checkpoint-write path that consumes `currentPosition` was NOT re-read (disclosed).
4. `nexsys-bench` `SCENARIO_FORMAT.md:48` — "**No `sqlite:` asserti[on]**" in §2.1's assertion-surfaces rule — **VERIFIED**; `engine.py` `KNOWN_STIMULUS_KEYS = {"bench", "api", "usb", "plug", "operator"}` (no test-runner) — **VERIFIED**; `boot-health.yaml:40-43` — the `log:` + `extract: "position=(\d+)"` + `min:` + `within:` mechanic — **VERIFIED** (Finding 2's premise and its target shape).
5. `bootstrap.sh`: `git ls-files | grep -i bootstrap.sh` returns nothing in `homesynapse-core` — the file the lane read is elsewhere (the bench or the distribution tree); the hub did NOT locate it and does not contradict the claim — **NOT RE-EXECUTED (disclosed)**; the precondition line (§4) settles the question at the instrument regardless.
**Not re-executed:** the filter-manifest call sites (`HomeSynapseCore.java:604…846`), the spike's timing figure, the DLQ table `V002:11-23`. Three of five re-executions hit the load-bearing claims (Findings 1 and 2, P3).

## §3 The ruling on B2 — taken under DELEGATE (D4's frame; `REVERT TR1-B2` flips it)
**TR1-B2 = DRIVER.** The census verb runs as a `fix1-loop.sh`-lineage driver in `nexsys-bench/tools/`, outside the scenario format, contributing ONE extra digest line per run (`census: subscribers=N delivered=D dlq=X missed=M`), until R-5 re-baselines the floor and folds it. *Why:* it reopens no STOP-gated format, adds no stimulus key, and keeps the bench a consumer of frozen tokens. *Refutable-by:* R-5's charter finding the driver lineage unmaintainable (then the format gains `test-runner` as its own row). *Cost:* one script; *buys:* OR-BUS-SILENT-DROP's evidence closure and B-1's measured clause without touching s31/the nightly.

## §4 The owed line (Nick; not a gate; when next at the card — NOT tonight, the rig is the email's evening off)
`card-gradle: <the first line of ./gradlew --version on the card | absent>` — settles whether the card can run `:test` tasks; if absent, TR-1b's driver runs the ITs on the desk against the card's exported DB instead (the design allows it: the census reads one SQLite file).

## §5 The follow-on WU (docketed; not authored tonight)
**TR-1b — the census ITs + the driver** (a Core TEST WU on the Java domain + a bench script): the two ITs compute the census in-process per Finding 1 (subtract DLQ rows; special-case the atomic checkpoint; report SUSPENDED-with-N-behind honestly) and print the frozen token; the driver runs them ×K on the card and appends the digest line. Dispatches AFTER HONESTY-1 lands (one lane per PATH-DOMAIN, D4) and before P-1's charter; its pre-registered "complete" criterion = the census's definition in the return §1 (Q1), verbatim. Instrument-first: the first run is a MEASUREMENT of P2's bench-night cost, not a claim.

## §6 Rows opened / closed
Opens: TR-1b (the bench lane's next charter) · the owed line `card-gradle:` · the word `REVERT TR1-B2` (only if refused). Closes: TR-1 (ACCEPT). Touches: OR-BUS-SILENT-DROP gains its closure INSTRUMENT (the verb), not yet its evidence — the Open Risks row is edited to say so at this beat.
