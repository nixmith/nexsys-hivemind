<!--
file: context/audits/2026-09-07_W-SKILLS-8_return.md
purpose: THE W-SKILLS-8 RETURN — the skills pass (W-SKILLS-7 §2 + the W-SKILLS-8 §2b folds) executed on the three references/ trees; SKILL.md ×3 byte-unchanged; stage nothing, commit nothing.
audience: the hub (audits under W-SKILLS-7 §6 R1–R6 + §6b R7–R8; commits hivemind + skills) · Nick re-syncs once after the commits
state-type: lane return
status: RETURNED 2026-09-07 (filed ~10:5x CT; instrument date -u 2026-09-07T15:34Z at boot, CT = UTC−5). Dispatch: context/instructions/2026-09-05_W-SKILLS-7_skills-pass_lane-dispatch.md + context/instructions/2026-09-07_W-SKILLS-8_skills-pass_lane-dispatch.md.
-->

# W-SKILLS-8 — return

## §0 The card
**Predictions, adjudicated first.** P1 MET — 5 of the 22 candidates became addenda on EXISTING entries ((34) (35) D3 D18×2) and 4 more folded as clauses of sibling new arcs; 9 of 22 took no new number. P2 HELD-WITH-LOSS — no SKILL.md needed a byte for any law to land, but three COUNT strings in PM SKILL.md ("#1–#22" §2.5 · "Checks 1–11" §6 · "37 arc-disciplines" §6) and one in coder SKILL.md ("21 arc-conventions" §5) are now stale-by-count → `[REVIEW]` R1. P3 MET — 15,628 / 12,373 / 15,763 B before = after (md5 equal). P4 MISSED by §2b's own orders — 14 M, not 11 (coder + FE `freshness-preflight.md` for Check 12; FE `field-evidence-and-rulings.md`), none silent. P5 MET on the numbered total (18 PM arcs + 7 coder conventions = 25; addenda 8: (29) (34) (35) D3 D5 D18×2 §3), MISSED on PM arcs alone (18 < 20). P6 MET. P7 MISSED on growth (PM ledger +18,849 B; 18 arcs each with mechanism + exhibit), MET on the ceiling (39,874 B; R3). P8 MET — §2b(a) THE OPERATOR-LOAD LAW absorbs v63 candidate 8 (the §CONTEXT-preamble brief), folded as arc (52) + an addendum on (35), not a second arc.

**Census, exact, at `git --no-optional-locks status --porcelain`, 15:49Z.** `nexsys-hivemind` (HEAD `7ae6a63`): **10 M + 0 A** = `project-manager/references/{laws-ledger, coding-instruction-format, freshness-preflight, review-and-quality, pass-history}.md` + `coder/references/{laws-ledger, testing-standards, deviation-and-quality, freshness-preflight, pass-history}.md` — plus this return (1 A under `context/audits/`, the §0-named exception to §4b). NOT MINE: HIVE-CLEAN-1's porcelain under `context/**` (0 at my 15:34Z boot; ≈129 staged `git mv` renames + M's by 15:55Z) — untouched; see R6. `nexsys-skills` (HEAD `f9c0bf4`): **4 M + 0 A** = `orchestrators/nexsys-frontend/references/{contract-consumer, field-evidence-and-rulings, freshness-preflight, pass-history}.md`.

**SKILL.md md5 before = after (R1):** PM `33de46080be516e3efde2dd30d93e5af` · coder `4813d9096a26cef7f0728cbe735aff14` · FE `b6d461c6e937dff0e8f3a156a23b3776`.

**Mint → arc (PM `laws-ledger.md` §1 unless noted; "c" = folded as a clause of that arc; "add" = the addendum form on an existing entry).** v63 (pm-lessons bullets 1–10): 1→(38) · 2→(39) · 3→(40) · 4→(41) · 5→(42) · 6→(38)c · 7→(40)c · 8→(52)c + add (35) · 9→(46) · 10→(43). v64 (bullets 1–10): 1→(44) · 2→(45) · 3→(46)c · 4→(47) · 5→(48) · 6→add D18(4) · 7→add D18(5) · 8→add D3 · 9→(49) · 10→add (34). §9-L→(50). b9→(51). §2b: (a)→(52) · (b)→(53) · (c)→add (29) · (d)→(54) · (e)→add §3 state-pointer · (f)→(55) PROVISIONAL · (g)→add D5. Format #23–#29 = (47) (45) [#25 sibling of (44)] (50) (53) [#28 the miss-script sweep] (54). Coder ledger (22)–(28): separable bytes · diagnostic readability · desk≠runner · miss-script sweep · fence-constant grep · sleepy-device physics · the cap. Check 12 ×3 identical (md5 `c00ff471…`, R8).

## §1 What was written, per file (untouched with reason: PM `constraint-enforcement` · `cross-subsystem-awareness` · `repo-state-protocol`; coder `homesynapse-mental-model` · `java-patterns`; FE `brand-and-design-system` · `build-and-ci-discipline` · `explainability-and-accessibility` — no delta names them. Every delta an ADDITION; every ruled sentence of (1)–(37), D1–D18, §2, §3 and every prior numbered law byte-unchanged — `git diff` re-executed: the only removed lines are mastheads re-prefixed with this pass + `Prior:`, the three preflights' check-count sentences, and the seven addendum-target lines, each re-emitted with its original text as an exact prefix)
- **PM `laws-ledger.md`** 20,961 → 39,874 B: arcs (38)–(55) after (37) with a hoisted detail-source line; addenda on (29) (34) (35) D3 D5 D18 and the §3 state-pointer (the stable prompt); masthead + TOC. Every new arc: operative sentence + mechanism + exhibit path — 99 cited paths across the write-set resolved on disk by script (the return path excepted; it exists now).
- **PM `coding-instruction-format.md`** 46,905 → 53,034 B: `## Additions (2026-09-07 …)` #23–#29; masthead.
- **PM `freshness-preflight.md`** 18,735 → 24,479 B: Check 9 "the instrument AS RUN" (per-file md5 of the three SOURCE trees vs the synced trees; the count reported as N/N, re-derived, never copied) beside the `diff -rq` block; Check 10's `git ls-files` basename instrument; Check 12; "eleven" → "twelve" in the heading, aggregate, output format and §7, and the "11-check superset" phrase re-pointed at the §2 heading (the coder file's own pointer-not-copy rule) — no verdict logic touched.
- **PM `review-and-quality.md`** 21,230 → 25,296 B: §3 + THE ADJUDICATION ORDER (mismatches first; the theory owned; the lane's mechanism re-executed at source) + THE OPERATOR BRIEF's four-region shape and THE DELEGATE ruling form.
- **PM `pass-history.md`** +2,019 B: the `### Moved/Added 2026-09-07 by W-SKILLS-8` entry (sections touched; the counts a census reads; the stale-count note).
- **Coder `laws-ledger.md`** 11,178 → 16,986 B: conventions (22)–(28) (paths in the new block resolve from `coder/references/`, stated in its header, unlike (1)–(21)'s SKILL.md-relative paths). **`testing-standards.md`** +5,817 B: §12 the SLF4J-free-module gotcha · §13 the Windows desk instrument block AS RUN (`cmd.exe /c start "" /affinity 3 /B /WAIT …`, `ORG_GRADLE_PROJECT_<prop>` env form, `--console plain`, the `Probe.java` `availableProcessors` proof — re-cut from the filed `…_FIX-1_loops/instrument/` scripts, not from memory) · §14 the miss-script sweep procedure + the sleepy-device physics note. **`deviation-and-quality.md`** +3,493 B: §7 the patch-shaped delivery · the corpus convention (`SUMMARY.md` header + one line per run + TOTALS; RED runs' XML + log only; ≤400 KB; the redactor) · the two-stage red-first. **`freshness-preflight.md`** +3,179 B: Check 12 + its lines. **`pass-history.md`** +1,513 B.
- **FE `contract-consumer.md`** 7,301 → 11,757 B: §5 THE v1.1.x ADDITIVE PROCEDURE (six steps: both pins one value · optional-nullable types · `strOrNull`/`refOrNull` present-null arms · mocks that do NOT always populate + one dangling ref · three renders per key, g-b-c disclosed · the gate and FE-DESK-1) with v1.1.3's four keys as the worked example and the `outcome: CONFIRMED` + `resultOutcome: null` honest-happy-path sentence (exhibit: the CG instruction §DP-5). **`field-evidence-and-rulings.md`** +4,964 B: §3 — HERO-0 F2 the manufactured-type class measured (+ the `| null` / validator / null-mock rule) · the tri-state idiom as FE-113 shipped it · the null census as the hero's design ground (the four empty states) · FE-DESK-1 (+ the `package-lock.json` grep, not `npm ls`) · the cap sentence. **`freshness-preflight.md`** +3,189 B: Check 12. **`pass-history.md`** +1,654 B. No candidate brand name in any added line (grepped); no SHA, HEAD or next-WU in any added line (grepped); LF only.

## §2 `[REVIEW]` — for the hub (each with the line cite; none blocks the commit)
- **R1 — stale counts in SKILL.md, guard 2 held.** PM SKILL.md §2 step 5 "numbered laws #1–#22", §6 "Checks 1–11" and "37 arc-disciplines"; coder SKILL.md §5 "21 arc-conventions". True as of each file's date; stale against the ledgers as of this pass. A one-line edit each is Nick's guard to lift, not this lane's; recorded in both `pass-history.md` files.
- **R2 — Check 12's exclusion clauses are STATE; written as parameters.** The HIVE-CLEAN-1 dispatch §4 greps carry today's exclusions (lane basenames, a date prefix, a prompt version) — project state under the W-SKILLS-7 §4 fence. Check 12 keeps the three greps with `<running-lane-basenames>` · `<today-CT-date>` · `<newest-prompt-version>` slots re-derived at the instrument, and cites the dispatch §4 as the exhibit for the concrete form. If the hub wants the literal greps, they are one paste from that §4.
- **R3 — two files sit above P7's 40 KB line, one before this pass.** `coding-instruction-format.md` was 46,905 B at baseline (a format reference whose `#N` numbers are cited from three ledgers) — a split re-homes cited carriers and is a hub ruling, not a lane act; left whole (+6,129 B). The PM `laws-ledger.md` is at 39,874 B (97 %); the next fold must split by section (`laws-ledger.md` → `laws-ledger-arcs-38-onward.md` or the like) — say so in that dispatch.
- **R4 — v67 prompt §0 vs the ledger text: one difference, the exhibit decides.** The prompt states the operator-load refinements as three; the assessment §2.5(a) adds "the `RETURNED` line is the whole report-back" — folded as refinement (iv) of arc (52) with §2.5 as its exhibit. Everything else in the prompt's §0 list agrees with the ledger text (the census computed → (29) add; the instrument beside the claim → (53); the cap → (54); the per-domain law → (55) PROVISIONAL; the stable prompt → §3 add).
- **R5 — one §5-listed item not in §2b, not folded:** the assessment §5 names "the F-R4-1 pattern: two triggers, one path, never a bypass" for the coder ledger; the W-SKILLS-8 dispatch §2b (the governing addendum) omits it. Not written; one convention if the hub wants it (exhibit: coder-lessons 2026-09-02).
- **R6 — three exhibit paths track HIVE-CLEAN-1's staged `git mv` (its recipe §1–§2).** The CG-123 instruction, the F-R4-1b instruction and the v65 operator brief are staged renames into `context/instructions/archive/` and `context/handoff/archive/2026-09/` on the device at 15:55Z; the nine cites of them in my additions (PM ledger · review-and-quality · coder ledger · testing-standards · FE contract-consumer) were re-pointed to the ARCHIVE paths (all 99 cited paths resolve on the working tree now). If CLEAN's moves are reverted, those nine cites are one `sed` back; the hub's R3 resolves by basename (`git ls-files | grep -F`) either way — Check 10's own instrument.
- **R7 — pre-existing, outside my fence:** FE `freshness-preflight.md` Check 6 and `field-evidence-and-rulings.md` §1 carry candidate brand names from earlier passes (the ledger's own status line already flags the name note). Untouched — the fence forbids me writing a name, not reading one; the hub decides whether a future pass strips them.

## §3 Instrument limits, closeout
No commit, no stage (porcelain re-read at 15:55Z; `git add` never run). HIVE-CLEAN-1's porcelain (staged renames + M's under `context/**`) is disclosed, not audited; my lane staged nothing (`git add` never run — the `--cached` content is CLEAN's). The write-set's paths were resolved by a script over the added lines only (99 resolved, 0 missing). Check 12's three blocks md5-equal (`c00ff471b44e47de2795884208582549` over `### Check 12` → "amend all three or none."). Clock: boot 15:34Z → filed ~15:5xZ (≈25 min of the 2–3 h; the read-set was on disk and the folds are additions). Next: the hub's R1–R8 audit → hivemind + skills commits at the bridge → Nick's one re-sync → Check 9 at the next boot expects **N/N with N re-derived** (28 files at v65; this pass adds none).
