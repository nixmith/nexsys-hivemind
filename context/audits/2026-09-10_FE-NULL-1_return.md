<!--
file: context/audits/2026-09-10_FE-NULL-1_return.md
purpose: FE-NULL-1 lane return — the causal chain's four REQUIRED-NULLABLE arms + the F4 row on the v1.1.3 mirror (the HERO-0 F2/F4 fast-follow on FE-113). Nothing staged or committed; coder-handoff.md untouched.
status: RETURNED 2026-09-10 (instrument 23:29Z open; CT = UTC−5 → filed 2026-09-10 CT); reports to the hub.
-->

# FE-NULL-1 — the causal chain's null arms (return)

## §0 Card
- **Predictions first (H12): P1 MET · P2 MET · P3 MET · P4 MET.** P1: census **8 M + 0 A** exactly §3 (`scenarios.ts` untouched, declared §1.4). P2: fixtures byte-identical — md5 `896f861d…` · `6f502c7b…` · `05b02fbc…` — and validate unchanged. P3: `npm run verify` GREEN in ONE round (instrument §2). P4: three stale reads, §3 S1–S3.
- **Baseline held:** HEAD `39c8dd3`; `-- web-ui/dashboard` porcelain EMPTY at open (Java-lane `core/**` dirt untouched). At close exactly: `M MODULE_CONTEXT.md · src/components/CausalChain.hardening.test.tsx · src/components/CausalChain.tsx · src/lib/api/contract.ts · src/lib/api/mock/mockData.ts · src/lib/api/shapes.ts · src/lib/api/v113-additive.test.ts · src/lib/format.ts` (+446/−28).
- **Red-first table (row → RED-at-HEAD by → now):** `subjectRef` null → `shapes.ts:256` THREW + TS2322 → passes · `command` null → `:272 isStr` THREW + TS2322 → passes · `targetRef` null → validator never looked, TYPE forbade → passes · `value` null → `:265` array-only, TYPE forbade → passes · `targetRef` MISSING/`{type}`/string, `value`·`entityId`·`attribute` MISSING, `value: 42`, non-object entry → PASSED at HEAD → throw · 5 render sentences + F4 → RED (5×TS2305 missing exports, 8×TS2322 fixtures) → in the DOM. **Green-by-construction, disclosed:** `subjectRef`/`command` MISSING + wrong-type (already threw), depth-0-renders-nothing, the non-null-run guard. Executed: 18 tsc errors at HEAD with the new tests, 0 after.
- **Deviations by tag:** `[DELTA-headline]` `causalSentence` (row-6 file, beyond row 5's letter) carries the same HERO-0 fragments on a null subject / null target — §3 S3. `[SHIM]` early desk vitest via a stand-in runner — §2. No `[REVIEW]` file outside the table.
- **`npm run verify` (Nick's desk, Windows; log `_scratch/fe-null-1_verify.log`):** tokens:check OK · eslint 0 · tsc 0 · **vitest 20 files / 307 tests, 0 failed** (`v113-additive` 50 vs 27 at HEAD · `hardening` 27 vs 19 · `contract` 48 · `format` 27 · `a11y` 6) · build OK · **bundle 65.1 KB / 100 KB** · contract 11 endpoints `v1.1.3-2026-09-06` · `VERIFY_EXIT 0`. `CONTRACT_VERSION` + `contract.test.ts` untouched.
- **Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING** (FE-113b's real-wire SKIPPED run). Not executed: no Java/Gradle; Core read only via `git show HEAD:` for the §2 cites.

## §1 Per-file deltas
1. **`contract.ts`** — `subjectRef: SubjectRef | null` · `observedState[].value: string | null` · `targetRef: SubjectRef | null` · `command: string | null`, each commented with the emitter line (`StandardExplanationService.java:644–:649`/`:771`/`:776`, `RunExplanation.java:137`) + "HERO-0 F2 2026-09-06"; a `cascade` note (`:213–:219`, null ≠ root). Version line untouched.
2. **`shapes.ts` B3:causalChain** — `:256` → `refOrNull(req(…'subjectRef'))`; `observedState` walked: entry object · `entityId` `isStr` · `attribute` `isStr` · `value` `strOrNull`, all `req` (MISSING throws); `:272` → `strOrNull(req(…'command'))`; ADDED `refOrNull(req(…'targetRef'))`. Nothing else changed; the OPTIONAL `'key' in a` idiom not copied.
3. **`v113-additive.test.ts`** — four `describe("FE-NULL-1 — …")` blocks (PRESENT-typed · null · MISSING · wrong-type/shape per key; `value` also entry-key MISSING + non-object entry), fixtures from a deep-cloned `run_eh_001`, `as unknown as` confined to one commented `loosen()`; an R4 block pinning the mock's four arms + depth-1 + all-chains-validate; a compile-level block pinning `CONTRACT_VERSION`. Every title states RED-at-HEAD or green-by-construction.
4. **`mockData.ts`** — `run_eh_003` (SKIPPED, the OLDEST run, `iso(610)`): `trigger.subjectRef: null` + the `:776` action `command: null`, `targetRef: null` (`resultOutcome: null`, `settled: true`) — the H8 `command: 'turn_on'` on a SKIPPED action is gone; `run_fd_001`: one added entry `{ent_livingroom_lamp, brightness, value: null}` + `cascade: {parentRunId: null, depth: 1}`. `run_eh_001/002` byte-unchanged. **`scenarios.ts` not changed — why:** its factories always populate, but every scenario spreads `defaultDataset` and the `default` scenario IS `base.causalChains`, so the arms reach the transport and the "every chain in EVERY scenario" sweep; a `null-arms` scenario is a factory change for the next mock touch (with the `resultOutcome: 'acknowledged'` false-value FE-113 filed). All 353 scenario+mock chains re-validate (executed).
5. **`CausalChain.tsx`** — trigger: `subjectRef === null` → `unrecordedTriggerLine(clockTimeWithDate(matchedAt))`, info/●, no pill, no `refLabel`; Trigger detail kept. "At the time": `value === null` → `noReadingLine`. Action line: `command === null` → `SKIPPED_BEFORE_COMMAND` (first, so a dangling target keeps its pill + help) · dangling unchanged · `targetRef === null` → `${actionPhrase} ${UNNAMED_TARGET}.`; Command detail `?? NOT_RECORDED` kept. Cascade: parent id → link · else `depth > 0` → `<p class={styles.cascade}>{CASCADE_PARENT_UNRECORDED}</p>` · else nothing. No new CSS.
6. **`format.ts`** — beside `NOT_RECORDED`: `unrecordedTriggerLine(when)`, `NO_READING_YET` + `noReadingLine`, `SKIPPED_BEFORE_COMMAND`, `UNNAMED_TARGET`, `CASCADE_PARENT_UNRECORDED` — §2's strings verbatim. `[DELTA-headline]` `causalSentence`'s `because` clause / target use the same fragments on the null arms. `format.test.ts` pins none of these — untouched.
7. **`CausalChain.hardening.test.tsx`** — one render per arm asserting the §2 sentence as a LITERAL and via the constant; `/\bnull\b/` never; no `UNRESOLVED_REF_PILL` under a COMPLETE census on a null ref; F4 sentence + no `a[href*="/explain/run/"]`; depth 0 nothing; `run_eh_001` renders as before with none of the new sentences; the mock's `run_eh_003` renders every arm and passes axe.
8. **`MODULE_CONTEXT.md`** — the `*2026-09-10 beat (FE-NULL-1)*` banner: REQUIRED-NULLABLE vs OPTIONAL-NULLABLE (two idioms, one table), `targetRef` never validated before, the F4 sentence, the emitter lines, the mock arms, the register.

## §2 The gates
- **Gate of record:** `frontend.yml` on Nick's push — **Deferred Build Gate YES**; the desk run in §0 is the pre-gate, on the real toolchain, exit 0.
- **The instrument, disclosed:** neither shell could run vitest/vite — the cloud container's egress policy refuses `registry.npmjs.org` (403 every package; not routed around), the local VM has no network, and the Windows `node_modules` carries only win32 natives (`rollup/dist/native.js` MODULE_NOT_FOUND on Linux). So `tsc`, `eslint .`, `tokens:check`, `check:contract` ran in the VM on the mounted tree (all 0); a `[SHIM]` runner (tsx + jsdom + a 40-line vitest stand-in, container-side, on md5-identical copies) ran the touched suites early (220 green) and a tsx script validated all 353 chains + the 16 validator arms; then the REAL `npm run verify` ran on Nick's desk by double-clicking `_scratch/fe-null-1_verify.bat` in File Explorer (computer use granted click-only; terminals not typeable). The shim's gaps (`vi.spyOn`, `toThrow(regex)`) hit only untouched suites, all green under real vitest.
- **RED at HEAD, executed:** tsc with the new tests against HEAD → 18 errors; after → 0. Throw-class rows are by construction (the hub's R2 read).
- **Scratch on Nick's disk, outside both repos, deletable:** `ClaudeFolder/_scratch/fe-null-1_*.tgz`, `fe-null-1_verify.bat`, `fe-null-1_verify.log`. Under `web-ui/dashboard/` nothing beyond the 8 files (`dist/` gitignored; `tokens.css` regenerated identical).

## §3 Pushback / observations (mismatches first)
- **S1 (P4)** — §0's "every new assertion RED at HEAD" overstates: `subjectRef`/`command` MISSING and wrong-type were already thrown at `shapes.ts:53/:102/:62`; only their PRESENT-null rows are red. Labelled per test (the FE-113 disclosed-green idiom).
- **S2 (P4)** — `mockData.ts` cites: the chains span `:232–:374` (`run_fd_001` at `:341–:373` is outside "~:236–:340"); the SKIPPED action is `:327–:336`, `command` at `:330`.
- **S3 (P4)** — the headline was not in the table but reads the same keys: `causalSentence` (`format.ts:451`) at HEAD rendered a null subject as "…because Something not on record changed at …" and a null target as "Something not on record turned on …" — a label fallback that reads like a registry accusation. Applied the HERO-0 fragments there (`[DELTA-headline]`); revert is two hunks if the hub disagrees.
- **O1 `[REVIEW]`, not fixed (hero design, §5):** `causalSentence` ignores `action.outcome`/run status — at HEAD the default mock's SKIPPED `run_eh_003` headlined **"Hallway Light turned on because Hallway Motion detected motion at …"** (`format.ts:484` `commandVerb('turn_on')` regardless of SKIPPED; `format.test.ts:39` pins only the happy path). It now reads "A device the run didn't name acted because something set it off at {time} — what isn't recorded." — honest about the nulls, still "acted" (`commandVerb(null)`, `:499`). The hero charter should own the L1 sentence per outcome; an interim `commandVerb` arm for `outcome === 'SKIPPED'` is one line if wanted first.
- **O2** — apostrophes: §2's `'` kept for verbatim R5 matching; the file's other copy uses `’` — a two-file, test-locked normalisation if preferred.
- **O3** — the toolchain gap is structural for a Linux-side lane on this Windows tree; one Windows-side `npm i --no-save @rollup/rollup-linux-x64-gnu @esbuild/linux-x64` would make the desk gate runnable from the VM without a prompt.

## §4 WUCP Phase 1 (no `coder-handoff.md` write — §0)
- [x] Preflight: HEAD `39c8dd3` · porcelain empty at open · `CONTRACT_VERSION` unchanged · the §2 emitter cites re-read at HEAD and confirmed.
- [x] Contract consumed field-for-field; four keys REQUIRED-NULLABLE; no invented shape; no bump.
- [x] Mock carries the live shapes: four null arms + depth-1, one each; fixtures READ ONLY (P2).
- [x] Every arm honest and distinct; `null` never as text; no pill on a null ref; no new CSS; copy centralised, test-locked, name-light.
- [x] Bundle 65.1/100 KB; desk gate green; **Deferred Build Gate YES (`frontend.yml` on Nick's push)**.
- [x] Staged nothing, committed nothing; no `git add/stash/checkout`; `coder-handoff.md` untouched.
- **Register:** REPO-COMPLETE, LIVE-VERIFICATION PENDING.
- **NEXT WU:** the hub's audit (R1–R7) → the `git add -A web-ui/dashboard` card → CI → **FE-113b** (the H8-a real-wire SKIPPED-run capture as a fixture — "verified" is earned there) → the hero charter (O1; the `null-arms` scenario + the `'acknowledged'` false-value at the next mock touch).

RETURNED nexsys-hivemind/context/audits/2026-09-10_FE-NULL-1_return.md 11218
