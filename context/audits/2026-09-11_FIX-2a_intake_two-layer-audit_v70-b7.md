<!--
file: context/audits/2026-09-11_FIX-2a_intake_two-layer-audit_v70-b7.md
purpose: The hub's two-layer intake audit of FIX-2a's return (context/audits/2026-09-11_FIX-2a_return.md, 12,175 B): the claims read critically, then the hub's own re-execution at the bytes on the device; the rulings on R1 and the [INFO] rows; what the finding (the class does not reproduce in-process on the desk) does to FIX-2b's and EXPLAIN-114a's sequencing.
audience: the hub · Nick (§0 is the verdict; §4 is what changes in his hands)
state-type: intake audit (filed; never rewritten)
status: FILED at v70 beat 7 (Fri 2026-09-11 ~20:2x CT; instrument 2026-09-12T01:18:39Z)
-->

# FIX-2a intake — two layers (v70 beat 7)

## §0 Verdict
**ACCEPT.** Repo-complete and test-only, exactly as chartered: 6 = 2 M + 4 A under `lifecycle/lifecycle/`, nothing under `src/main`, module-info untouched, the return under the cap with its `RETURNED` line last. P1, P3, P4 HELD; **P2 came back on its inverse arm and the lane reported it first and whole:** eight soak samples (7 × K=20 + 1 × K=3; 143 loops) on two topologies (24 cores unpinned; 2 carriers pinned with VT parallelism 2) — zero timeouts, zero anomaly lines, zero scored misses. The class that produced the `1e26912` red and the August red does not reproduce in-process on this desk. The instruments are what land: the await diagnostic on the hero IT's failure path, the position-census IT on TR-1's tokens, and the soak that CI now runs on every push (20 loops per sample, with the diagnostic in the failure message). One `[REVIEW]` (the `-D` passthrough) ruled below; the `[INFO]` rows accepted.

## §1 Layer 1 — the claims, read critically
The §0 card adjudicates P1–P4 before anything else, names the artifact for every cell, and reports the inverse arm of P2 as a first-class result rather than a failure of the WU (the instruction's §11 asked exactly that). The measurement table carries seven K=20 runs with their p50/p99/max and the anomaly grep. The census claim is the porcelain's, not a typed count. The lane ran the full suite fresh (`--rerun`) at HEAD before writing (79) and after (82). The `[REVIEW]` is measured, not asserted (`-D…=2` ran 20; the env var `=3` ran 3). The lane did not stage, did not commit, and wrote the hivemind rows its skill owns (the return, a coder-handoff entry, one lesson).

## §2 Layer 2 — the hub's re-execution at the bytes (device, 2026-09-12T01:18:39Z)
1. **Census at porcelain (scoped to the source trees):** `M lifecycle/lifecycle/MODULE_CONTEXT.md` · `M …/HeroLoopHardwareFreeIT.java` · `?? …/BusAwaitDiagnostic.java` · `?? …/BusAwaitDiagnosticTest.java` · `?? …/BusPositionCensusIT.java` · `?? …/BusSoakIT.java` — 6 = 2 M + 4 A; `src/main` 0; `module-info` 0. Numstat: MODULE_CONTEXT +10/−0; the hero IT +65/−3. Line counts: 64 · 65 · 593 · 419.
2. **The XML set on the desk** (`lifecycle/lifecycle/build/test-results/test/`, the 00:56Z run): 20 suites, 82 `<testcase>`, 0 suites with failures; `BusSoakIT` 00:56:14Z: `bus.soak: loops=20 ok=20 timed_out=0 p50_ms=26 p99_ms=28 max_ms=28 anomalies=0`; `bus.position_census_total: runs=20 subscribers=6 missed=0 unscored_missed=1`; `grep -c bus.delivery_anomaly` over all 20 XML = 0.
3. **The wall-clock rule:** `System.nanoTime` ×4 in `BusSoakIT` only; `nanoTime|currentTimeMillis|Instant.now()|Clock.system` = 0 in the other three new files and in the hero IT.
4. **The token grammar:** the new files emit `bus.await_timeout`, `bus.await_subscriber`, `bus.position_census`, `bus.position_census_total`, `bus.soak`, `bus.soak_host`. The frozen prefixes of TR-1 §3 are byte-identical (`bus.position_census: subscriber=<id> matched=<m> delivered=<d> missed=<n> first_missed=<p|none>`; `bus.position_census_total: runs=<k> subscribers=<K> missed=<n>`); the lane's keys are TRAILING (`checkpoint= dlq= [atomic=true|dlq_unscored=true]`; `unscored_missed=`).
5. **The render, read whole** (`BusAwaitDiagnostic.java`): a pure `StringBuilder` over `what · store_head · awaited|none` then one `bus.await_subscriber` line per snapshot with `behind = head − checkpoint`; no I/O.
6. **The hero IT's diff, read whole (code lines):** `awaitTrue` keeps its two-argument shape (now an instance method — `[INFO] I1`, zero static callers); the three-argument sibling takes a `LongSupplier`; on timeout the diagnostic is rendered from `events()` (the head) and `core.eventBus().subscribers()`, printed, and thrown; the gather is guarded by a `RuntimeException` fallback so it can never mask the timeout; the On-frame await passes `newestReportedPosition("occupied","true")` (the `StateReportedEvent.value()` is a String — the equality holds). **No assertion changed.**
7. **The census predicate** (`BusPositionCensusIT`): `scored = !atomic && dlqDepth == 0` (`:189`); the atomic projection is reported with `atomic=true` and never asserted (`:150–:151`); the manifest is a record of `(subscriberId, filter, atomic)` built from the registration sites.
8. **The soak loop** (`BusSoakIT`): `t0 = System.nanoTime()` after `reportOccupied(true)` (`:150–:152`); the confirm is awaited the hero test's way — `command_issued(turn_on)` count, then `state_confirmed` joined to that command (`:161–:167`) — then `occupied` back to false and its report awaited (`:173–:175`); nearest-rank percentiles (`:239–:248`); the loop count from the property, then the env var (`:101–:102`).
9. **The build-logic claim behind R1:** `homesynapse.java-conventions.gradle.kts` declares `jvmArgs` (`:50`, `:70`) and no `systemProperty` passthrough — the measurement stands on its mechanism.
**Not re-executed (disclosed):** the eight soak runs themselves (the XML on the desk is the last run's; the earlier runs' XML sit in the lane's scratch copies, cited by timestamp in the return); the Spotless run; the pinned-affinity run's topology line is taken from the return.

## §3 Rulings (DELEGATE — Nick's `REVERT <row>` flips any one)
- **R1 — ACCEPT as shipped.** The env var `HOMESYNAPSE_SOAK_LOOPS` is the desk override; `-Dhomesynapse.soak.loops` stays the documented spelling for a JVM that receives it. The one-line `providers.systemProperty` passthrough in the conventions' `Test` block is DOCKETED for the next build-logic touch (FIX-2b carries it if nothing lands sooner). No re-run.
- **I3 — ACCEPT the trailing keys.** The frozen TR-1 prefixes are byte-identical; the bench's positive assertion is `log:` + `same_line:` (substring), so trailing keys break no consumer. The extended line is the grammar of record for TR-1b's driver; TR-1b's charter cites this audit.
- **I1, I2, the `bus.soak_host` line, the env fallback's MODULE_CONTEXT entry — ACCEPT.**
- **O3 (`awaitEnvelope` still throws the bare message) — DOCKETED for FIX-2b** (one line each).
- **The finding:** the desk does not reproduce the class in 143 loops on two topologies; the two reds on record are CI-runner reds. The next reading comes from the runner itself: every push now runs `BusSoakIT` (K=20) and the hero IT with the diagnostic; a red there carries the per-subscriber checkpoints in its `<failure message>`. TR-1b's card driver is the second instrument (Saturday's `card-gradle:` shapes it). **FIX-2b (checkpoint-driven LIVE delivery; the AMD to Doc 01 §3.4) is authored on the first diagnostic-bearing red or on TR-1b's reading — not before.**
- **EXPLAIN-114a's gate:** the instruction dispatches on FIX-2a's CI green (the Java slot frees at the landing; the gate of record at the new HEAD is that run). Its paste gains an amended baseline clause (FIX-2a's test-only files and the HERO-1b lane's `web-ui/dashboard/` dirt are lawful). Refutable by `EXPLAIN: hold`.

## §4 What lands, in Nick's hands (the order)
1. Core: `git add -A lifecycle/lifecycle` → 6 files → the commit from `_scratch/2026-09-11_core_FIX-2a_commit-msg.txt` → push. CI on that push is the gate of record and sample #10 of the class (the first with the soak).
2. Hivemind: one card for beats 3–7 plus the lane's three rows. 3. Docs: AMD-100. 4. HERO-1b's paste (web-ui; independent of CI). 5. On `ci` green: EXPLAIN-114a's paste. 6. On a red at `BusSoakIT`/`HeroLoop`: the run's reports under `_scratch/v70/ci-<sha>/` (the Act-0 shape) — the diagnostic is FIX-2b's first input.
