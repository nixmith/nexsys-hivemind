<!--
file: context/audits/2026-09-11_HERO-1b_return.md
purpose: HERO-1b lane return — SPEC §11 rows B1–B7 + B9 of the explainability hero landed in web-ui/dashboard/src, tests first; B8 untouched (gated). Nothing staged or committed; nothing outside web-ui/dashboard/src touched.
status: RETURNED 2026-09-11 CT (instrument 2026-09-12T01:47Z open · 02:2xZ close; CT = UTC−5); reports to the hub.
-->

# HERO-1b — the hero of record, B1–B7 + B9 (return)

## §0 Card
- **Predictions first: P1 MISSED · P2 MET · P3 MET · P4 MET on this desk / PENDING on Nick's push · P5 MET.** P1: census **16 M + 1 A**, not 9+1 / 10+0 — the charter's own §3 table names 15 files (`scenarios.ts` is one file, so no A there); the A is `views/WhyNotView.hero.test.tsx`; two files beyond the table are `[REVIEW]` below. P2: `format.test.ts` +54 `it`s (30 cells · 6 overrides · 11 slot arms · 4 §10 sentences · 3 guards), **47 RED at HEAD** (executed: 47 failed / 34 passed before B1; 81/81 after); 7 green-by-construction, each named in the file. P3: `grep -rn BRAND` over `format.ts · i18n.ts hero keys · CausalChain.tsx · StatusPill.tsx · WhyNotView.tsx · scenarios.ts` → 0; `\bwe\b` over the 140 hero strings → 0 (test-locked). P5: §3 D1–D3.
- **Baseline held:** HEAD `cc05a54` (main, one past `1e26912`; SPEC byte-unchanged, 57,427 B); dashboard porcelain EMPTY at open. At close, exactly: `M a11y.test.tsx · components/CausalChain.hardening.test.tsx · CausalChain.module.css · CausalChain.tsx · StatusPill.tsx · lib/api/mock/mockData.ts · scenarios.ts · lib/format.test.ts · format.ts · lib/i18n.test.ts · i18n.ts · styles/tokens.css · styles/tokens/tokens.dtcg.json · views/WhyNotView.module.css · WhyNotView.nullability.test.tsx · WhyNotView.tsx` + `?? views/WhyNotView.hero.test.tsx` (+1,337/−285). Fixtures md5 `896f861d…` · `6f502c7b…` · `05b02fbc…` unchanged. No git write command run.
- **Red-first (row → RED-at-HEAD how → GREEN):** B1 → 47/54 fail on text (HEAD's `causalSentence` writes "{Target} {verbPast} because …" for every status) → 81/81 · B2 → 3/5 fail (no §7 key resolves; `hero.permanence` present) → 6/6 (two Register-C greps vacuous at HEAD, disclosed) · B3 → 11/11 fail (HEAD's L1 is the wire's `explanation`; N3 in ok) → 11/11 · B4 → 3/6 fail (one `genuinelyEmpty`; "changed at" trigger line) → 6/6 · B5 → 3/4 fail (no hidden step text, no provisional suffix, no `role=status`) → 4/4 · B6 → preservation class, the diff (+22) is the receipt · B7 → 4/4 fail (no `hero-states`; CONFIRMED mock carries `'acknowledged'`) → 4/4 · B9 → `tokens:check` DRIFT → `npm run tokens` → OK; no tokens test at HEAD, ratios in §4.
- **Gates (this desk, real toolchain, §2):** tokens:check OK · eslint 0 · tsc 0 · **vitest 21 files / 392 tests / 0 failed** (307 at HEAD) · build ✓ · **bundle 69.9 / 100 KB** · contract-check OK · `VERIFY_EXIT 0`. Gate of record `frontend.yml`: pending Nick's push.
- **Deviations by tag:** `[REVIEW]` `WhyNotView.module.css` (+2, the `.body` class) · `[REVIEW]` `WhyNotView.nullability.test.tsx` (2 assertions, §3 D4) · `[DELTA]` the current-instance empty chain keeps `EMPTY_CHAIN_NOTE` beside the `completed.none` headline · `[DEFAULT]` D5–D7 · no `[SHIM]`.
- **Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING** (`hero-states` is a mock; no real-wire row exercised). No Java/Gradle run.

## §1 Per-file deltas
1. **`format.ts`** (+162/−55) — `causalSentence` = `causalHeadline(chain, resolve).text`: COMPLETED device-led (one key per leading outcome; no action → `completed.none` / `.silentSkip` by the counters), every other status `{status}.frame` + `tail.{outcome}` (`tail.none` = frame alone); the mode override from `actionVerdict().mode` only. Slot arms as functions: `becauseClause` (subject null/absent · dangling · firingValue null → "changed"), `targetSlot` (null · dangling), `commandVerbs` (turn_on/off, dim · `run "{command}"` · act/acted), `headlineTime` (unparseable → "an unrecorded time"). `commandVerb()` removed; `fillSlots`/`heroCopy` exported (D5 for the unknown arms).
2. **`format.test.ts`** (+206) — the 30 cells as `it.each` at the sample slots (`{time}` = today 21:42 local, so text is exact), overrides, slot arms, ≤21 words, keys, §10 (1)(2)(5)(6).
3. **`i18n.ts`** (+171/−1) — the 140 §7 rows extracted from `SPEC.md:136–:275` by script; `hero.permanence` removed, `explain.permanence` = Q3 (a). One edit: the registry phrase keeps the test-locked `hub’s`. **`i18n.test.ts`** (+188): keys inline; resolve · no `BRAND` · no `{{NAME}}` · no `\bwe\b` · footer pinned.
4. **`WhyNotView.tsx`** (+94/−51) — `whyNotCard(nf)`: N1–N7 L1 + body + link + pill (N3 `info`, Q1 a; glyphs on N4/N6/N7). No-time: N1/N3 the `.noTime` keys; N4/N6 drop " at {time}". "Last checked": `conditionsResult` null beside a time → `…lastChecked.unclean` (`StandardExplanationService.java:281–:288`). Wire `explanation` off the card (D4). `.module.css` +`.body`.
5. **`CausalChain.tsx`** (+201/−162) — steps as `StepSpec[]`; each `<li>` carries `explain.a11y.step` (`.sr-only`; the label is the state the marker carries). `eraSkeleton` = empty ∧ (name null ∨ `trigger.type` null) → `noDetail.title` headline + `noDetail.body` step; else `completed.none` + `EMPTY_CHAIN_NOTE`. Trigger line: firingValue null → `explain.trigger.readingNotRecorded`. `role="status"` (polite) always present; a held action settled on a later render announces `explain.a11y.live` once. L2/`<ol>`/footer keyed. Action LINES unchanged (D6). **`StatusPill.tsx`** (+4): provisional → `.sr-only` `explain.a11y.provisional`.
6. **`CausalChain.module.css`** (+22) — L2 body secondary (already) noted; `.detailBody a` body-text underlined; settle transition on `.stepLine > *`/`.marker` + the reduced-motion rule (rail too). **`a11y.test.tsx`** (+52) · **`hardening.test.tsx`** (+133/−1; one existing fixture re-read: `trigger.type: null` IS the era class, the current-instance variant added beside it).
7. **`scenarios.ts`** (+85/−1) — `hero-states`: 12 runs, one era each, anchored at the latest 21:42 local — COMPLETED × each outcome + superseded · silent skip · era skeleton · SKIPPED×SKIPPED · FAILED×FAILED · CANCELLED×CONFIRMED · INTERRUPTED×expired_on_restart (the set is test-pinned); `firingValue` null ×12; CONFIRMED → `resultOutcome: null`; SKIPPED actions `command: null` with the planned target kept (`:771` nulls it only when the plan names none); why-not rows N3 (Core's DP-B2 sentence verbatim) · N2 · N1. `ACTION_RESULT_OUTCOME.CONFIRMED` → `null`. **`mockData.ts`** (+6/−3): the two CONFIRMED actions → `resultOutcome: null`.
8. **`tokens.dtcg.json`** (+3/−3) + **`tokens.css`** regenerated — T1 light `textMuted` `#5a6675`; T2 light `ok500` `#13703b`, `error500` `#b33326` (the FE-VERDICT-2 mode-paired precedent; no component touched).

## §2 The gates
- **Instrument:** `node` v22.23.2 / npm 10.9.8 in the Linux VM. The mount's `node_modules` is the Windows install (`@rollup/rollup-linux-x64-gnu` missing — FE-NULL-1 O3's class), so `npm ci` ran in `/tmp/hsdash` and every gate ran there on an `rsync` copy of the tree; `tokens.css` was generated there and copied back (`tokens:check` OK on both sides).
- **`npm run verify` after B9:** tokens:check OK · eslint 0 · tsc 0 · vitest 21/392/0 (`format` 81 · `i18n` 6 · `WhyNotView.hero` 11 · `hardening` 37 · `a11y` 10) · build ✓ (js 38.8 KB · font 24.8 · css 5.5 · html 0.8) · bundle 69.9/100 KB · contract-check OK (`v1.1.3-2026-09-06` untouched) · **EXIT 0**. Each row's RED was executed before its edit (§0). **Deferred Build Gate YES** for the gate of record (`frontend.yml` on the push); desk command: `cd web-ui/dashboard && npm run verify`.
- **Housekeeping:** a stale `.git/index.lock` (an early `git status`; later reads used `--no-optional-locks`) and one discarded draft file were moved to `ClaudeFolder/_scratch/_to_delete/` — Nick's to delete.

## §3 Pushback and observations
- **§10 read off the `hero-states` screen (verbatim):** (1) "Hallway Light turned on because Hallway Motion changed at 9:42 PM on Sep 11." (2) "Evening Lights skipped this run when Hallway Motion changed at 9:02 PM on Sep 11. Nothing was sent to Hallway Light." (3) "It hasn't run yet." / "Nothing has set it off since this automation was loaded. It runs on motion on the porch. Nothing is wrong — it's waiting." (4) "It was set off at 9:02 PM on Sep 11, but a condition was false, so it didn't act." (5) "Hallway Light was asked to turn on because Hallway Motion changed at 9:32 PM on Sep 11 — it never confirmed." (6) "Hallway Light was asked to turn on because Hallway Motion changed at 9:17 PM on Sep 11, then a newer command replaced it." Word for word with §10 except the two slots the wire owns — `{triggerVerb}` "changed" (firingValue null on every run today, audit O1/O2) and `{time}` — while the exact §10 sentences are pinned in `format.test.ts` ((1)(2)(5)(6)) and `WhyNotView.hero.test.tsx` ((3)(4)).
- **D1 [P5]** charter §8 R5 ("word for word") contradicts §3 B7 (`firingValue` null everywhere): the screen cannot say "detected motion"; the tests carry the literal check.
- **D2 [P5]** SPEC §4 (`:88`) names a terminal line "Done, recorded no steps" with no §7 key; the era skeleton renders the existing `terminalLine`. **D3 [P5]** N4/N6 (`:75`, `:77`) define no-time arms but §7 (`:220`, `:222`) has no `.noTime` keys for them (rendered per the `:215` slot note); `SPEC.md:11`'s `contract.ts :346–:353` starts at `:347`.
- **D4 [REVIEW]** the wire's `explanation` string is off the why-not card: SPEC §2 gives it no slot, and Core's DP-B2 sentence (`StandardExplanationService.java:355–:356`, "last fired and confirmed at …") claims what Q1 (a) refuses to infer from a run id — both on one card contradict. Two `nullability.test` assertions now pin the keyed N2 sentence. One-line revert if ruled otherwise.
- **D5 [DEFAULT]** no §7 headline cell for a null/unknown action outcome: rendered as `explain.mode.notRecorded.line` — honest, never success; a §7 row is asked.
- **D6 [NEXT]** action step LINES still read "Turned on Hallway Light." for every outcome (`actionPhrase`) — on UNCONFIRMED/FAILED a claim the wire did not make; the `explain.mode.*.line`/`.help` swap (into `CausalChain.tsx`/`verdicts.ts`) is named by no B-row. **Next WU: HERO-1c** — the step lines and helps from §5/§7, tests first; also retires `format.ts:208`'s "We sent it" (`outcomeMeta`) and wires the catalog keys whose surfaces are outside the B-rows (`explain.hub.*`, `error/offline/replaying/loading`, `whyNot.kv.neverChecked` — D7).
- **Cross-lane asks (docketed):** Q6(b) skeleton marker (v1.1.5); the §7 rows in D2/D3/D5.
- **`MODULE_CONTEXT.md`** not written (write-set is `src/**`; the HERO-1 audit §5 said the build lane updates it — the charter wins). Banner for the hub: the §7 catalog in `i18n.ts` (140 keys, test-locked); `causalHeadline()` the L1 grammar; `hero-states` the scenario of record; T1/T2 landed.
- **For the skill:** the Linux VM CAN run the real toolchain with `node_modules` installed outside the mount (O3's "cannot" was the Windows-natives class).

## §4 WUCP Phase-1 checklist
- [x] Preflight: `cc05a54` ≥ `1e26912`, SPEC unchanged, porcelain empty; volatile facts re-derived at source.
- [x] Contract FROZEN v1.1 only; validator, fixtures, `contract.test.ts`, `CONTRACT_VERSION` untouched; mocks carry the live nulls.
- [x] 30 cells + 7 why-not rows + 2 empties honest and distinct; no color-only meaning (hidden step text, provisional suffix, `role=status`); AA — T1 light muted 5.49 bg / 5.85 surface / 5.07 sunk; T2 light ok 5.43 okBg / 6.16 white, error 5.28 errBg / 6.14 white (dark unchanged).
- [x] Copy centralized + test-locked (140 keys); name-light; no "we"; no positioning claim.
- [x] Bundle 69.9/100 KB; `verify` EXIT 0 here; `frontend.yml` pending the push; nothing phones home.
- [x] Return filed; register REPO-COMPLETE, LIVE-VERIFICATION PENDING; next WU named (HERO-1c).
RETURNED nexsys-hivemind/context/audits/2026-09-11_HERO-1b_return.md 12242
