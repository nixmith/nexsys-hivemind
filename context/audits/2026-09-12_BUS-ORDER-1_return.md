<!--
file: context/audits/2026-09-12_BUS-ORDER-1_return.md
purpose: Coder return for BUS-ORDER-1 — AMD-101 §2's realization (the read-forward LIVE delivery). §0 card first; the hub audits against AMD-101 §2 sentence by sentence; Nick commits.
audience: the hub (intake) · Nick (commits)
state-type: lane return
status: DELIVERED 2026-09-12 CT — REPO-COMPLETE, LIVE-VERIFICATION PENDING (CI `check` on the push = the gate of record; the first `bus-soak` sample after the landing = P5)
-->

# BUS-ORDER-1 return — read-forward LIVE delivery (filed 2026-09-12 CT)

## §0 The card

**Verdict: DONE (repo-complete). CORE census (`git --no-optional-locks status --porcelain`): 16 = 14 M + 2 A + 0 D; 0 staged; ZERO commits by the lane; `git diff --stat -- '*module-info.java'` EMPTY. P1–P4 HELD; P5 owed to CI. 4 [REVIEW] · 8 [INFO] · O1 (a lost-wake mechanism the K=200 soak exposed — fixed in-lane, pinned).** Baseline `5f918c7`, tree clean, `72efb42..HEAD` = one commit (EXPLAIN-114a) at 01:55Z; instruction read whole, AMD-101 §2 first. `date -u` 2026-09-13T01:55:13Z ⇒ CT = UTC−5 = 20:55 Sat 2026-09-12; filed ~22:3x CT. Instrument: the 24-core Windows desk, JDK 21.0.4, Gradle 8.8, `--offline` — not the runner.

| # | Predicted | Observed |
|---|---|---|
| P1 | T1 red at baseline — `[P+1, P]` or P skipped with kind E; force one | **HELD, the skip arm forced:** T1 awaits `checkpoint == P+1` between `notifyEvent(P+1)` and `notifyEvent(P)`; on the baseline loop (02:26Z) `[1L, 3L] to contain exactly [1L, 2L, 3L]` — P never delivered |
| P2 | T2 red only if an append is forced between the last page and the CAS | **HELD, forced** (the REPLAY tail-read hook, FAILCHAN §10-O): three appends notified in REVERSE → `timed out awaiting the four positions` — at `5f918c7` the drain delivered `P+2` only: its gap detection (`TransitionCoordinator:117`, `position <= lastReplayedPosition → continue`) skipped P+1 and P once P+2 lifted the mark — a two-position SILENT DROP at the flip, kind E's drain-side twin. T2b (an append between the drain's read and the CAS): g-b-c at baseline, disclosed |
| P3 | T3 red (no notify → never delivered) | **HELD:** `timed out awaiting delivery of 2 without a notification within 400 ms` (tick 200 ms, budget 2 × tick) |
| P4 | gates green; the ITs untouched+green; soak K=200 two carriers green, `anomalies=0`, `missed=0`, `delivered_max = checkpoint` per non-atomic subscriber | **HELD, with §5's own exception:** gates below; `ReplayTransitionIT` · `EventBusTest` · `ReplayWindowQueueTest` · `EventBusContractTest` untouched, green. The verbatim soak line 03:04:52Z → `bus.soak_host: available_processors=24 vt_parallelism=2` · `bus.soak: loops=200 ok=200 timed_out=0 p50_ms=26 p99_ms=35 max_ms=35 anomalies=0` · census `missed=0 unscored_missed=0` · kind E 0 · `run_handoff` 200. `delivered_max = checkpoint` for the all-filter subscribers (`automation_engine` 2414/2414 · the ledger 2413/2413); the type-filtered ones sit above it by the trailing non-matching span (`command_dispatch_service` 2408/2407 · `integration_supervisor` 2410/2409 · `registry_projection` 2386/9) — the checkpoint is written only after a matching delivery (§5) |
| P5 | `check` green on the push; the first `bus-soak` sample shows `delivered_max`, no kind E | **OWED to CI** — `./gradlew check` not run in-lane (Deferred Build Gate) |

**AMD-101 §2's five corollaries, each with its holder:** (1) *the cursor is the source of truth* — `SubscriberRuntime.cursor()`/`advanceCursor` (CAS-max), paged by `liveLoop` and `drainAndPromote` through the shared `deliverStep`; `ReadForwardDeliveryTest` **T1** (order, once) · **T2/T2b** (the flip); the census reads it (`lastDelivered` → `delivered_max=`). (2) *the checkpoint is the floor, never a guard* — written in `deliverStep` after a matching SUCCESS, only when the cursor advanced (**T1/T2**: the recording store's writes `isSorted()`); no checkpoint read on the active notify path; kind E unreachable (**T1**). (3) *a wake hint + a bounded tick* — `wake(long)` unconditional for LIVE; **T3** (no notify → delivered within 2 × tick) · **T5** (a wake during the loop's own read honored before the tick — O1) · `EventBusConfigLiveFieldsTest`. (4) *exactly once across the flip* — `ReplayDriver` advances the cursor per paged row; the drain reads forward from it and CASes under the queue lock; **T2** · **T2b** · `ReplayTransitionIT` 1..1500 · `transitionDrainsReplayWindowQueue` `[1,2,3,4]`. (5) *append order is delivery order; an unseen position ends the page early* — `DeliveryAnomalyEmissionTest` **T1/T5/T6** (an empty page is caught-up: no `LIVE_READ_EMPTY`/`_EXHAUSTED`, no SUSPEND, the tick re-reads) · **T2** (`LIVE_READ_FAILED` once per failed page, at `cursor+1`) · **T-R2** (a drain page read that throws SUSPENDs; no `TRANSITION_READ_*`).

**Gates:** the instruction's line verbatim 03:03:32Z → BUILD SUCCESSFUL, `--rerun` bound to the last task only (`:core:event-bus:test UP-TO-DATE` from its 03:01Z run on identical bytes — convention (5)). Forced, `--rerun` after EACH task, 03:04:02–03:04:52Z, every `:test` EXECUTED, XML 03:04:23/32/47/51Z: **event-bus 240 / 30 skipped / 0** (230 → 240: +5 `ReadForwardDeliveryTest`, +5 `EventBusConfigLiveFieldsTest`) · **persistence 413/0/0** · **lifecycle 81/0/0** · **app 30/0/0** (the arch rules) · `spotlessCheck` ×2 green · zero `warning:` under `-Werror`.

**Red-first** (compile-red 02:23:21Z event-bus 16 errors · 02:23:47Z lifecycle 6 — the unbuilt seams; behavior-red with the seams inert 02:26:21Z, 18 run / 9 failed; green 02:34:50Z; T5 red 02:59:51Z → green 03:01Z):
| Test | Stage-2 red (the message) | Now |
|---|---|---|
| RF T1 · T2 · T3 (as P1–P3) · T2b (g-b-c) · T5 (`… not on the 2 s idle tick within 500 ms` — O1) | as stated | green |
| DAE T1 (was LIVE_READ_EMPTY → an empty page is caught-up, re-read on the tick) · T5 (was 2 × EMPTY → 0 anomalies, delivered once) · T6 (was EXHAUSTED + SUSPENDED → never SUSPENDs, P undelivered while the store shows nothing) | `timed out awaiting the LIVE loop paging from the cursor` ×2 · `… the idle tick re-reading …` | green |
| DAE T2 (was LIVE_READ_FAILED per single read → once per failed PAGE at `cursor+1`) · T4 (b) (`emptyOnce(p)` → `throwPageOnce(p1)`) | `timed out awaiting LIVE_READ_FAILED at position 2` · `… the emitter` | green |
| DAE T-R2 (was TRANSITION_READ_EXHAUSTED + SUSPEND → a drain page read that throws SUSPENDs, no `TRANSITION_READ_*`) | `timed out awaiting the honest SUSPEND out of TRANSITION` | green |
| DAE T7 (`emptyOnce(p)` → `emptyPageOnce(p1)`) · T3 · T-R1 (byte-unchanged) · `BusAwaitDiagnosticTest` (` cursor=`, ` hints=`) | green ×3 · the text mismatch | green |

**The 16:** `core/event-bus/` M `SubscriberRuntime` · `InProcessEventBus` · `TransitionCoordinator` · `EventBusConfig` · `DeliveryAnomaly` (javadoc) · `ReplayDriver` (R1) · `DeliveryAnomalyEmissionTest` · `MODULE_CONTEXT.md`; A `ReadForwardDeliveryTest` · `EventBusConfigLiveFieldsTest`. `lifecycle/lifecycle/` M `src/test/…/BusPositionCensusIT` · `BusAwaitDiagnostic` · `BusAwaitDiagnosticTest` · `BusSoakIT` (one call site) · `HeroLoopHardwareFreeIT` (I2) · `MODULE_CONTEXT.md` (I3). Nothing under `core/automation`, `api/`, `web-ui/`, `HomeSynapseCore`, build files.

## §1 Deviations — 4 [REVIEW] · 8 [INFO] · O1
- **[REVIEW] R1 — `ReplayDriver.java` +2 lines, outside the table.** Row 1 initialises the cursor "at activation" (`ReplayDriver.run():109` is that read) and row 3 pages the drain "from `runtime.cursor()`" — the flip can hand the cursor only if REPLAY advanced it; otherwise the drain re-delivers every REPLAY-delivered position (the duplicate option (a) feared). `advanceCursor` after the checkpoint read and per paged row; the paging (§6) untouched.
- **[REVIEW] R2 — `EventBusConfig` gains a public 2-arg convenience constructor.** Row 4's "four `new EventBusConfig(` sites … updated" vs row 7's fence: three sites are `EventBusConfigTest:47/:55/:63`. The 2-arg form supplies the AMD-101 defaults (the `SubscriberInfo` 3→4 precedent); `EventBusConfigTest` byte-unchanged; equality with `HOME_DEFAULT` pinned.
- **[REVIEW] R3 — two more kinds retire; T-R2 changes; the coordinator's emitter goes.** Row 3 makes the drain a read-forward, so its single-position read and the FIX-1b retry retire: `TRANSITION_READ_EMPTY`/`_EXHAUSTED` are retained kinds (INV-GA-02) beside row 5's LIVE trio; T-R2 (a TRANSITION-path pin, outside row 7's "LIVE-path" clause) now pins "a drain page read that throws SUSPENDs honestly, no anomaly" (the pre-FIX-1b arm). Nothing emits from the drain, so the coordinator's `Consumer<DeliveryAnomaly>` parameter is removed: ctor `(runtime, eventStore, checkpointStore, clock, int readBatch)`.
- **[REVIEW] R4 — INV-BUS-01's mechanism clause** (`Architecture_Invariants_v1.md:1167`, the transition "MUST track `lastReplayedPosition` so that events delivered during catch-up are not re-delivered during drain"): the cursor is that gate now; `lastReplayedPosition` stays REPLAY's own mark. One clause for the docs card — AMD-101 §4 edits Doc 01 only.
- **[INFO]** I1 `wake(long)` carries the position as a diagnostic hint (the instruction wrote `wake()`). I2 `HeroLoopHardwareFreeIT:700`, the one `render` caller row 9 did not list, passes `((InProcessEventBus) core.eventBus())::lastDelivered`; `BusSoakIT`'s one census site (row 8's condition met). I3 `lifecycle/lifecycle/MODULE_CONTEXT.md` +1 paragraph (the grammar; WUCP step 1). I4 the census line keeps ` pending=` (row 8 names only what it gains; row 9 renames the diagnostic's) — un-consumed hints now, said in both MODULE_CONTEXTs. I5 `bus.head: position=<H> type=<T>` then ≤ 3 `bus.tail: position=<P> type=<T>` (below the head, oldest first; `type=none` on an empty store). I6 `LIVE_READ_FAILED.globalPosition = cursor + 1`; `lagEvents` = remaining-in-page + un-consumed hints. I7 T3's tick 200 ms (budget 400 = 2 × tick — a 100 ms tick's budget judged too tight for a 2-vCPU runner); T5 added beyond T1–T4. I8 the concrete bus via `BusPositionCensusIT.concreteBus(core)` (a cast) — STOP gate 4 did not fire.
- **O1 — the spent unpark permit (found by the soak, fixed, pinned).** The first K=200 soak on the read-forward read `p50_ms=29 p99_ms=553 max_ms=557`: a per-loop print showed 49/200 hero loops at one idle tick (~275 ms), 4 at two, identical pinned and unpinned; `state_projection` ended `pending=1`. Cause (a standalone JDK 21.0.4 probe): a permit that lands while the virtual thread is parked in its OWN `Future.get()` (production's `SqliteSubscriberReadExecutor` shape) or a re-entrant write wait is spent by that park's resume — only a permit on a running thread survives — so the loop parked with the hint queued and the tick delivered. Fix: after the page, park only if the hint queue is still empty (the queue is the durable signal; a permit landing between the check and the park is kept). Re-measured `p50 27 / p99 35 / max 69` pinned, `26 / 35 / 75` unpinned, 199/200 loops < 50 ms, the projection `pending=0`. T5 pins it (a future-backed read executor + a 50 ms gap between the wake and the read's completion — back to back, the JDK's race hid it and the test stayed green twice). Lesson filed.

## §2 Sweeps and limits
- **INV/LTD at source:** AMD-101 §2 ✓ · INV-BUS-01 `:1165` → R4 · INV-BUS-02 `:1169` (notify metrics untouched) · INV-ES-04 `:194` (notify after commit) · INV-ES-05 `:200` · INV-PROJ-04 `:1185` (monotonic — by construction now) · INV-GA-02 `:809` · AMD-45 §2.2 `:60` (the atomic gate in `deliverStep` + `ReplayDriver` ×2 = the three `writeCheckpoint` sites, grep-verified) · AMD-42 §3.4.3 `:48` (`onCaughtUp` single-shot) · LTD-11 (`synchronized` 0 in 482 added main lines; an `AtomicLong` CAS-max) · NO_DIRECT_TIME_ACCESS (0 clock reads added; `parkNanos` is a park). Glossary ×5 resolve: Subscriber §3.11 · Checkpoint §3.6 · Event Bus · Event Log · `global_position`.
- **STOP gates:** 1 ✓ · 2 `new EventBusConfig(` = 4 (3 in `EventBusConfigTest`; no loader — `HomeSynapseConfig` passes `HOME_DEFAULT`) → R2 · 3 the three named tests untouched, green · 4 reachable (I8) · 5 `MinimalEventBusStub` constructs no `SubscriberRuntime`.
- **Added-line sweeps:** main 482 — `synchronized` · `Instant.now` · `nanoTime` · `currentTimeMillis` · `systemUTC` · `System.out` · `Thread.sleep` · `TODO` all 0; test lines likewise; every changed file LF; the retired FIX-1b symbols 0 references; `emitAnomaly(…NOTIFY_SKIPPED_LIVE` 0 in main. Not a duplicate dispatch (no return on disk; tree clean).
- **Preflight:** 1–5, 7 PASS; 6 = the loaded plugin copy IS the skill on this desk; 12 STALE — hub hygiene. `coder-lessons.md` is 27.2 KB — the hub's rotation is owed (26.1 KB before this lane).
- **Instrument limits:** the desk is not the runner (P5 is CI's); `./gradlew check` not run in-lane; `max_ms` 69–75 in two of three post-fix samples (one loop of 200), 35 in the final one.

## §3 WUCP Phase 1: Coder Closeout
- [x] MODULE_CONTEXT.md updated for: core/event-bus (the § BUS-ORDER-1 section; inventory rows; the three checkpoint writers; the `pendingDepth` and permit gotchas), lifecycle/lifecycle (the census/diagnostic grammar)
- [x] coder-handoff.md prepended (the Deferred Build Gate flag at its top)
- [x] Deferred build gate flag: **YES** — `./gradlew check` on `5f918c7` + the 16, owed to CI on Nick's push
- [x] coder-lessons.md appended: the spent unpark permit (O1)
- [x] Cross-agent note: not needed (retired channel)
- [x] NEXT WU named: the hub's intake against AMD-101 §2 → Nick's commit (exactly the 16) + push → `check` GREEN = the gate of record → the first `bus-soak` artifact (`delivered_max=` on every line, no kind E — P5) → OR-BUS-SILENT-DROP's closure counter restarts (twenty green, kind E zero — the hub's count) → `AMD101: ratify` with R4's clause → HERO-1c / EXPLAIN-114b
- Timestamp: 2026-09-13 03:3x UTC (Sat 2026-09-12 ~22:3x CT)

RETURNED context/audits/2026-09-12_BUS-ORDER-1_return.md 14215
