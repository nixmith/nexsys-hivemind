<!--
file: context/audits/2026-09-12_CI-1_return.md
purpose: Coder return for CI-1 — the bus-soak amplifier OFF the gate (the tag · the exclusion · the non-gating job); §0 card first, P1–P3 first.
audience: the hub (audit) · Nick (commits)
state-type: lane return
status: DELIVERED 2026-09-12 CT — REPO-COMPLETE, LIVE-VERIFICATION PENDING (CI on the push = the gate of record)
-->

# CI-1 return — the soak off the gate, kept as a sample source (2026-09-12 CT)

## §0 The card

**Verdict: DONE (repo-complete) — 4 = 4 M + 0 A; 0 [REVIEW] + 3 [INFO]; nothing STOP-grade. P1–P3 all HELD.** Instruction read whole + the §0 read-set. Baseline: core `main` HEAD `6af76f7`, porcelain EMPTY. Every §3 gate held at the instrument: `ci.yml:17–:20` · `:39` · `:46–:52` with `**/build/test-results/` · exactly one `useJUnitPlatform()` · `git grep -c '@Tag('` = 0 · the HEAD suite **83** testcases / 21 suites / 0 failures (`--rerun`, 14:36Z). Not a duplicate dispatch (no CI-1 return on disk). Preflight PASS except check 6 (unverified from the plugin cache) and check 12 STALE — hub hygiene: the FIX-2a / FIX-2b-i / HERO-1b / RS3-WMARKET-2 instructions still carry a live `status:`. 14:34Z launch → 14:5xZ filing; CT = UTC−5 → **2026-09-12**.

**Census — CORE (`git --no-optional-locks status --porcelain`): EXACTLY 4 = 4 M + 0 A + 0 D; ZERO staged; ZERO commits by the lane; zero `src/main`; every file LF.** M `.github/workflows/ci.yml` (+39/−0: the `bus-soak` job; `check` byte-untouched) · M `homesynapse.java-conventions.gradle.kts` (+13/−1: `useJUnitPlatform()` → the block + its comment) · M `BusSoakIT.java` (+2/−0: the import + `@Tag("bus-soak")`) · M `BusPositionCensusIT.java` (+2/−0, the same two lines). HIVEMIND: 1 A (this file) + 1 M (`coder-handoff.md` prepended); two pre-existing hub rows (the untracked CI-1 instruction, `OPERATOR-BRIEF_for-Nick.md` M) untouched.

**P1–P3, adjudicated first:**
| # | Verdict | Read from |
|---|---|---|
| P1 | **HELD both ways** — R1 `./gradlew :lifecycle:lifecycle:test --rerun --offline` → **81** / 19 suites / 0 failures (`:build-logic:compileKotlin` EXECUTED — the conventions change took); R2 the same line + `-PincludeBusSoak` → **83** / 21 / 0; `BusSoakIT` + `BusPositionCensusIT` are exactly the two XMLs R1 lacks. R2: `bus.soak: loops=20 ok=20 timed_out=0 p50_ms=26 p99_ms=27 max_ms=27 anomalies=0` | the R1/R2 XML sets (`grep -o '<testcase '`) |
| P2 | **HELD** — `grep -n 'bus-soak' ci.yml` → `:54–:55` the comment (the tag `@Tag("bus-soak")`) · `:61 bus-soak:` the job id · `:83` the upload step · `:87 name: bus-soak-${{ github.run_number }}` (`-PincludeBusSoak` has no hyphen — unmatched). PyYAML 6.0.2 parses: `jobs = [check, bus-soak]`, `continue-on-error: True`, `timeout-minutes: 15`, the three setup steps list-EQUAL to `check`'s, `check`'s keys unchanged | the grep · `yaml.safe_load` |
| P3 | **HELD** — `TEST-com.homesynapse.lifecycle.HeroLoopHardwareFreeIT.xml` is in R1's 19 (untagged, green) | the R1 XML set |

**Deviations by tag.**
- **[INFO] I1 — a doc delta owed, outside the census.** `lifecycle/lifecycle/MODULE_CONTEXT.md:245` ("How to run": `--tests '*HeroLoop*' --tests '*BusPositionCensus*' --tests '*BusSoak*' …`, and the runner-shaped recipe beside it) now needs `-PincludeBusSoak`: MEASURED (R3) `--tests '*BusSoakIT*'` without it → `BUILD FAILED` · `No tests found for given includes: [*BusSoakIT*](--tests filter)`. The 4-file census forbids a fifth file; the one-token addition is the hub's at commit or docket.
- **[INFO] I2** — `spotlessCheck` NOT run (the instrument limit); one import + one annotation per file, header untouched — Spotless-neutral by construction.
- **[INFO] I3** — the exclusion reaches every module's `Test` task (§5): harmless, the tag lives in the two lifecycle classes only.

**Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 4 atop `6af76f7` — the first `check` WITHOUT the two ITs AND the `bus-soak` job's first artifact (`bus-soak-<run_number>`) = the next sample of the class. **Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING.**

## §1 What changed
`@Tag("bus-soak")` directly under each IT's `@DisplayName`; the hero IT and both bodies untouched. The conventions' `Test` block: `useJUnitPlatform { if (!project.hasProperty("includeBusSoak")) { excludeTags("bus-soak") } }` — a PROJECT property (`-P`, not `-D`). `ci.yml`: the `bus-soak` job exactly as §1.3 (the three setup steps byte-identical to `check`'s, the `run:` line verbatim, `if: always()`, `continue-on-error: true`).

## §2 Gates run (JDK 21.0.4 · Gradle 8.8 · `--offline --rerun`, each XML set copied aside)
R0 14:36Z HEAD 83/21 green · R1 14:45Z 81/19 green · R2 14:46Z 83/21 green (`-PincludeBusSoak`) · two supplementary filtered runs on the same task, disclosed: R3 the filter without the property → "No tests found" (I1) · R4 the job's `run:` line verbatim + `--rerun --offline` → exactly 2 suites / 2 testcases green.

## §3 Observations
- O1 — under `continue-on-error: true` a red `bus-soak` job stays visible in the run's job list while the run's conclusion is green: read the sample from the artifact's two XMLs (`pending=` … `bus.thread:`), never from the check mark; `check`'s artifact no longer carries them.

## §4 WUCP Phase 1: Coder Closeout
- [x] MODULE_CONTEXT.md: NOT touched — the 4-file census governs; the one-line delta is I1
- [x] coder-handoff.md: the DELIVERED entry prepended (Deferred Build Gate + the NEXT WU pointer) · gate flag YES · no lesson · no cross-agent note
- [x] NEXT WU named: the hub's audit → Nick's commit (exactly the 4) + push → `check` green without the two ITs = the gate of record; the `bus-soak` artifact = the next sample → FIX-2b-ii on the first depth+dump-bearing red → EXPLAIN-114a
- Timestamp: 2026-09-12 14:5x UTC (Sat 2026-09-12 ~09:5x CT)

RETURNED context/audits/2026-09-12_CI-1_return.md 6036
