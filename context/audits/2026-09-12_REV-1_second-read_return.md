<!--
file: context/audits/2026-09-12_REV-1_second-read_return.md
purpose: REV-1 — the read-only adversarial second read of the v70 frame audit, the v71 b1 source-read audit and the two ci samples (#11 bd5d35e, #12 6af76f7), run as an in-conversation agent at v71 beat 2 on Nick's condition ("if the hand-off reading is ambiguous after the §2 reads, charter REV-1"). Filed verbatim by the hub; the hub's own re-execution of its load-bearing findings is in the b2 intake audit §3.
audience: the hub (FIX-2b-ii (ii)-B rests on F3/F4) · Nick (§0 of the b2 audit carries the reading)
state-type: lane return (an in-conversation agent; read-only; 17 tool calls; no file written by the lane)
status: FILED v71 beat 2 (Sat 2026-09-12 ~14:0x CT; instrument 2026-09-12T19:08:23Z). The agent read the WORKING tree, which already carried FIX-2b-ii (i)'s uncommitted edits — its line numbers for StandardRunManager are the post-edit ones; the bus and coordinator cites are unchanged by (i).
-->

# REV-1 return — 2026-09-12 (verbatim, as returned to the hub)

**Preliminary (affects every line number below):** the core working tree is NOT clean. `git status --porcelain` shows uncommitted edits in `InProcessEventBus.java` (+10: the `NOTIFY_SKIPPED_LIVE` anomaly), `DeliveryAnomaly.java`, `StandardRunManager.java` (+43, `logHandoff`), `StandardActionExecutor.java`, `BusSoakIT/BusPositionCensusIT/HeroLoopHardwareFreeIT.java`. `git diff bd5d35e HEAD -- InProcessEventBus.java` is empty and `git show bd5d35e:…InProcessEventBus.java | grep NOTIFY_SKIPPED_LIVE` = 0 → both CI samples ran with the silent guard. The audit's `:369–:373` cites the committed file; the run-manager cites (`:319–:320`) are now `:331–:333`.

**F1 (Q1).** No XML line names an event type at any position (`grep -o` for `triggered|_started|_completed|type=` yields only log noise). But the soak's `awaited` is defined by the test itself: `BusSoakIT.java:163` passes `() -> newestReportedPosition(events(), "occupied", "true")`, printed at `:285`. So **182 IS loop 14's motion `state_reported`**, not an inference. 183's type is not determinable from the artifacts; by the subscriber filters it is NOT command_issued/command_dispatched (`CommandDispatchAssembly:95–:99`, `IntegrationSupervisorAssembly:81–:85`; their checkpoints stop at 175/176) and NOT command_result/state_reported/state_confirmed (`PendingCommandLedgerAssembly:119–:124`; ledger checkpoint 182). Census IT: `awaited=24` = head; 24 is likewise non-ledger, non-command; 23 is a ledger type. Note `integration_supervisor checkpoint=19` in #11 vs `=20` in #12: adjacent positions swap run-to-run → concurrent publishers in the loop. What would show it: the SQLite `events` rows 181–183 (or a `bus.head: position= type= ingest=` line in `timeoutDiagnostic`).

**F2 (Q2).** Confirmed at the bytes: `PlatformThreadWriteCoordinator.submit` builds a `CompletableFuture`, `queue.offer(item); return future.get();` (`:69–:97`). Production wires it: `DatabaseExecutor.java:250–:251 new PlatformThreadWriteCoordinator()`; the decorator hook (`:80`, `:149`, `:248`) is test-only; `grep WriteCoordinator lifecycle/lifecycle/src/main` = 0 hits (no override in the lifecycle composition; whether the ITs' test rig installs `ThrottledWriteCoordinator` not read). `SqliteCheckpointStore.writeCheckpoint:166–:184` = one `submit(STATE_PROJECTION, upsert)` per call — no batching, coalescing or drop. BUT the upsert is `INSERT OR REPLACE … (subscriber_id, last_position, …)` (`:91–:95`) — **no monotonic guard**: a later write of a lower position overwrites.

**F3 (Q3).** Confirmed. `NotifyingEventPublisher:44–:47` = `delegate.publish(...)` then `bus.notifyEvent(...)`, no lock across the pair; `SqliteEventStore.publish:376` returns from `submit` on the caller's thread (grep for `synchronized|lock()` in that file: none) — two callers' wake-ups are unordered, so notify order ≠ append order. `notifyEvent:316–:386` offers per subscriber in `activeRegistry` iteration order under `rwLock.readLock` (no ordering); `liveLoop:526–:566` polls `pendingPositions` FIFO and writes `envelope.globalPosition()` after each SUCCESS (`:557–:559`). With queue order [P+1, P] the engine writes P+1 then **P — yes, a lower position after a higher one**, and the guard is then re-opened. The O1 interleaving (P skipped because checkpoint already ≥ P) is also real and was silent at bd5d35e.

**F4 (Q4).** The reorder DOES produce a miss-at-head signature — because the census is checkpoint-derived: `BusPositionCensusIT:284–:290 delivered += (globalPosition <= checkpoint)`, `missed = matched − delivered`. Delivery order [head, head−1] leaves checkpoint = head−1, pending 0, dlq 0, LIVE, no anomaly: byte-identical to both samples. Conversely, the harmful O1 skip (head−1 never delivered, checkpoint = head) is counted by this census as **delivered** — the token catches the benign case and hides the harmful one. Every other path checked: `SubscriberSupervisor.deliver` returns only SUCCESS/PARKED(dlq.park `:122`)/CIRCUIT_BREAKER_TRIPPED/INFRASTRUCTURE_FAILURE (`:136–:149`, all → SUSPENDED) — excluded by dlq=0/LIVE; `readLivePosition:668–:708` null → SUSPENDED or thread exit — excluded by the parked `hs-sub-automation_engine`; `vt == null` at offer (`:617–:621`, volatile set `SubscriberRuntime:238`) leaves the position queued — excluded by pending=0; LinkedBlockingQueue offer (capacity not verified); COLD/REPLAY/TRANSITION routing (`:607–:612`) — mode is LIVE; filter `SubscriptionFilter.all()` confirmed `HomeSynapseCore:735`. `grep -c bus.delivery_anomaly` = 0 in all six lifecycle XMLs. Hence: **from the source read, [head, head−1] delivery is the only path to the observed tuple for an all() subscriber.** For the soak this requires 183 to be published by a thread not causally after the engine's delivery of 182 (i.e. NOT `automation_triggered` from `onEvent(182)`) — e.g. loop 13's `automation_completed` from its run VT (`finalizeRun:550`, after `activeRuns.remove:529`). The stall then needs the delivery of 182 (after 183) to trigger nothing silently: `decide():614 SINGLE → skip("mode_busy")` if `countActive` (`:632`, not read) still counts the finishing VT (`liveRunThreads.remove` runs at `:515`, after publishCompleted).

**F5 (Q5).** Knowledge, not measurement: JDK 21 `Thread.dump_to_file` (plain) lists VTs tracked in thread containers; direct `Thread.ofVirtual().start()` VTs are in the root container when `jdk.trackAllThreads` is unset (default true) — the `hs-sub-*` VTs appearing with `virtual` in the dump confirms tracking here. A STARTED-not-yet-mounted VT has an empty stack (`VirtualThread.tryGetStackTrace`: NEW/STARTED/TERMINATED → empty). It would print as `#<tid> "automation-run-…" virtual` with zero frames; `BusThreadDump` HEADER `:76` parses it, filter `:246` (`hs-` prefix or marker in 12 frames) hides it. `grep -c 'automation-run-'` = 0 on the XML is therefore not evidence of absence.

**F6 (Q6).** Sample #11 soak: `bus.thread_dump: threads=36 shown=20 file=/tmp/junit-4276918000642861622/threads-1789183416258.txt`; the 20 shown are the 8 `hs-sub-*` VTs (all `state=unknown` — `:273`, state read only for platform threads), 6 `hs-sub-read-*`, `hs-read-0`, `hs-write-0`, `hs-sched-0`, `hs-http-95`, `hs-health-0`, `integration-zigbee-0`. No non-`hs-` VT printed; hidden names are not emitted. `find _scratch/v71/ci-bd5d35e -name 'threads-*.txt'` = nothing. Also: #12's `BusSoakIT.xml` has 0 `bus.thread` lines (green, no dump).

**Open:** 183/24's event type; `countActive` (`StandardRunManager:632+`) and whether `skip("mode_busy")` publishes/logs; queue capacity in `SubscriberRuntime`; whether the IT rig decorates the write coordinator.

**Recommendation:** make the census read the bus's in-memory max-delivered position (or `MAX`-guard the checkpoint upsert) and print the head's event type in `timeoutDiagnostic` — one CI run then separates "reorder artefact" from "real miss" without a dump.
