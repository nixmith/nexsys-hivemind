<!--
file: context/audits/2026-09-12_v71-b2_FIX-2b-ii-i_intake_two-layer-audit.md
purpose: The two-layer intake of FIX-2b-ii part (i)'s return (9,978 B): the lane's claims read critically, then the hub's own re-execution at the bytes (the diffs, the desk's test results, the samples' anomaly lines); the verdict; the card; what REV-1's second read changed in the record (the census's second face is a notify-order artefact of a non-monotonic checkpoint, and the same reorder has a harmful, invisible arm); the lessons for how a coder session is prompted.
audience: Nick (§0, §5) · the hub (the WUCP Phase 2 record) · the v72 hub
state-type: audit (filed evidence)
status: FILED v71 beat 2 (Sat 2026-09-12 ~14:1x CT; instrument 2026-09-12T19:08:23Z). Verdict ACCEPT. Core card: 10 = 10 M + 0 A (the lane's 8 + the hub's two Phase-2 MODULE_CONTEXT deltas), msg `_scratch/2026-09-12_core_FIX-2b-ii-i_commit-msg.txt`.
-->

# FIX-2b-ii (i) — intake audit (v71 beat 2)

## §0 Verdict surface
- **ACCEPT.** Every §2 row of the instruction is in the diff at the site named; zero behavior change beside the lines and the kind; module-info untouched; no `synchronized`, no wall-clock read, no `System.out` added. P1 (red-first) and P2 (compile + module gates) held on the lane's desk; Nick then ran `./scripts/clean.sh && ./gradlew check` — BUILD SUCCESSFUL, 156 tasks — and the tagged pair — BUILD SUCCESSFUL; the hub re-read the pair's XML: `bus.soak: loops=20 ok=20 timed_out=0 p50_ms=26 p99_ms=28 max_ms=28 anomalies=0`, 20 `run_handoff` · 20 `run_body_entered` · 20 `action_step_started`, 0 `run_thread_died`, 0 `NOTIFY_SKIPPED_LIVE`.
- **The card (Nick's hands): 10 = 10 M + 0 A** — the lane's 8 plus `core/event-bus/MODULE_CONTEXT.md` (kind 6 → 7; the notify-order gotcha) and `lifecycle/lifecycle/MODULE_CONTEXT.md` (the parent-logger capture; the handoff-census grammar), both spliced by the hub at this beat with asserted anchors. CI on the push is the gate of record; its `bus-soak` artifact is sample #14, the first with the instrument.
- **What REV-1 changed (F3/F4, filed beside this):** notify order ≠ append order; the checkpoint upsert is `INSERT OR REPLACE`; delivering `[head, head−1]` regresses the persisted checkpoint to `head−1` and TR-1's census then prints exactly `missed=1 first_missed=<head> checkpoint=<head−1>` — the tuple of samples #11 and #12 — while both positions were delivered. The same reorder has a harmful arm the census cannot see: `head` delivered before `head−1`'s notify → the guard drops `head−1` for good; (i)'s kind `NOTIFY_SKIPPED_LIVE` now names it. The soak's stall (the run not continuing) stays a separate question, instrumented by (i)'s four lines; REV-1 could not determine the event type at 183 and names the read that would (the head's type in the timeout text — B-i row 5).
- **Next on the Java slot:** FIX-2b-ii (ii)-B-i (`…FIX-2b-ii-B_notify-order_census-truth_coding-instruction.md`), after `CORE: LANDED` on a clean tree; its §7 is an H10 to Nick on the delivery-discipline fix (`BUSFIX: a|b|c`; the hub recommends b).

## §1 Layer one — the lane's claims, read critically
The §0 card is complete and in the CI-1 form: baseline and porcelain at launch; the four STOP gates each with its instrument; the census from porcelain; P1/P2 with timestamps and the XML fields read. Seven [INFO] deviations, each a shape choice inside the instruction's intent (counts instead of `0|1`; the shared statics hoisted into the census IT; a `logHandoff` helper; the runId keyed on the store; javadoc at the changed sites; the two MODULE_CONTEXTs left to the hub as §2 said). Observation O1 is the lane's own reading of the notify path and is correct (REV-1 F3 confirms it at source). One claim the hub cannot re-execute: the red at HEAD (18:21:06Z, `Expected size: 1 but was: 0`) — the lane's XML was overwritten by later runs; accepted on the transcript Nick pasted (the assertion text and the `failures="1"` field are quoted there) and disclosed.

## §2 Layer two — the hub's re-execution
- `git diff --numstat` in core = the eight files with the lane's counts (`StandardRunManager` +42/−1 · `StandardActionExecutor` +5/−0 · `DeliveryAnomaly` +15/−4 · `InProcessEventBus` +10/−0 · the three ITs · `core/automation/MODULE_CONTEXT.md` +2/−0); `git diff --stat -- '*module-info.java'` = empty.
- The production diffs read whole: line A via `logHandoff(run, mode, thread)` at the four sites, the admitted and drained calls placed between `publishTriggered` and `vt.start()`; line B the first statement inside `runBody`'s outer `try`; line C on the builder as `.uncaughtExceptionHandler(...)` logging at ERROR and returning; line D before `publishStarted` at `:183`; kind E emitted at the guard only when `runtime.mode() == SubscriberMode.LIVE`, then the unchanged `continue`. `DeliveryAnomaly.Kind` gains the constant last, with its contract.
- The test diffs by grep: the hero IT attaches a `ListAppender` to `LoggerFactory.getLogger("com.homesynapse")`, keys on the `AutomationTriggeredEvent` payload's runId, asserts `hasSize(1)` for `run_handoff` and `run_body_entered`, `anySatisfy(index=0)` for `action_step_started`, `isEmpty()` for `run_thread_died`, `noneSatisfy(kind=NOTIFY_SKIPPED_LIVE)`; the census IT owns `homesynapseLogger()`, `attachLineCapture()`, `handoffCensus(...)` and the widened `timeoutDiagnostic(core, dumpDir, captured, what, awaited)`; the soak imports them and drops its two private helpers; the `startsWith("bus.delivery_anomaly")` filter is unchanged.
- Nick's desk after `clean` + `check` + the tagged pair: `lifecycle/lifecycle/build/test-results/test/` holds the two tagged XMLs (the `--tests` re-run replaced the gate-shape set — the hero IT's XML from the `check` run is gone; `check`'s BUILD SUCCESSFUL is its record); the soak XML's counts as in §0.
- Samples #11 and #12 re-grepped: `bus.delivery_anomaly` = 0 lines in both red suites; `bus.soak_host: available_processors=2 vt_parallelism=default` in both; the soak line of #11 `loops=20 ok=14 timed_out=1 … anomalies=0`.
- Not re-executed (disclosed): the red-first run; the lane's module-test counts (211 · 230 · 81 · 30) beyond `check`'s success; the coder's memory-file edits (outside the repos).

## §3 REV-1's findings, re-checked by the hub
- F2 (the blocking `submit`; `INSERT OR REPLACE`; `DatabaseExecutor:250` wires `PlatformThreadWriteCoordinator`) — the SQL and the coordinator were read by the hub at beat 1 and this beat; confirmed.
- F3 (no lock across append + notify; the loop writes every delivered position's checkpoint in delivery order) — the hub read `NotifyingEventPublisher:44–:49` and `liveLoop:557–:566` at beat 1; confirmed.
- F4 (the census rule `position <= checkpoint`, `BusPositionCensusIT:284–:290`) — read by the hub this beat; the arithmetic follows. The hub's own beat-1 sentence "the offer path's one silent branch is the guard" stands; its sentence "the engine did not receive the head" is WITHDRAWN as a certainty — the tuple is also produced by the head delivered first. The `integration_supervisor` checkpoint 19 (#11) vs 20 (#12) is the reorder's fingerprint on a subscriber with two matched positions.
- F4's soak reading (183 = loop 13's `automation_completed`; the stall from `countActive` still counting the finishing run) — the hub read `countActive` at beat 1 (`:604–:612`, iterates `activeRuns`) and `finalizeRun` removes the run from `activeRuns` before `publishCompleted` per REV-1's cite, so that stall path is unlikely; a second candidate the hub records without evidence: the engine delivering the motion event AFTER the projection applied it, so a transition trigger sees no change. Both are settled by (i)'s `handoff=` count and B-i's `bus.head:` line on the next red; neither is authored on.
- F5 (a started, unmounted VT prints with zero frames and is hidden by the filter) — knowledge, as marked; it makes `grep -c 'automation-run-'` = 0 non-evidence, which is why line A is logged before `start()`.

## §4 What changes in the record
- OR-BUS-SILENT-DROP gains its first mechanism proven at source: the notify-reorder drop through the checkpoint guard (harmful arm) and the checkpoint regression (benign arm, the census artefact). The v70 frame audit's "the bus is idle and correct" holds for the dump it read and no longer for the class.
- TR-1 §1's census: "the PERSISTED checkpoint" is not a delivery truth while the checkpoint can regress; B-i reads the in-memory max and prints both; TR-1b's card driver inherits the same rule.
- FIX-2b-ii (i)'s §7 fix table: the row "census: `NOTIFY_SKIPPED_LIVE` present" and the row "absent, `missed=1` again" both route to B-i first; B-ii is Nick's ruling.
- `core/event-bus/MODULE_CONTEXT.md` and `lifecycle/lifecycle/MODULE_CONTEXT.md` carry the deltas (in the core card).

## §5 How the coder session was prompted — what the transcript teaches (pm-lessons entry of this date)
1. **The return cap was set without arithmetic.** 10 KB for an 8-file WU with a P-table and seven [INFO]s cost three trim-and-refit passes (11,311 → 10,715 → 10,149 → 9,978 B). Rule: cap = 3 KB + ~1 KB per file in the Files table, rounded up to the next KB; the cap sentence stays.
2. **Name the gate tasks exactly.** The lane guessed `:app:test` (no such task) and lost one run; the instruction should carry the gate line verbatim (`:app:homesynapse-app:test`), the same way the hero IT's line is carried.
3. **Long quoted heredocs fail in the host-side harness.** A 22-line `python3 - <<'EOF'` with quotes broke the shell; the lane's own memory said heredocs "work" after a short re-test. The coder skill should say: short heredocs only; a script over ~15 lines is written with the Write tool and run by path.
4. **A test-home change the instruction did not name.** The capture had to move to the `com.homesynapse` parent logger; the instruction said so in row 5 — the lane found the leaf-logger trap anyway and filed it as a lesson. When an instruction moves a capture point, it should say WHY the old one is blind (the logger name), so the lane does not re-derive it.
5. **Transcripts belong under `_scratch/`, not in chat** (THE OPERATOR-LOAD LAW (iv)): the pasted transcript was ~50 KB of hub context; the return's §0 already carried every line the audit used. Next time: `_scratch/v71/<lane>_transcript.md` + the one-line report.
6. **What went right and should be kept:** the §0 card's shape (baseline · gates · census · predictions with the XML field each was read from); the red-first run before the production edit; the fixed-point `NNNN` byte count; the WUCP Phase 1 checklist with the Deferred Build Gate flag; the observation O1 filed as an observation, not acted on.
