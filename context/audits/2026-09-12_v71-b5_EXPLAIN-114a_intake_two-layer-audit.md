<!--
file: context/audits/2026-09-12_v71-b5_EXPLAIN-114a_intake_two-layer-audit.md
purpose: The two-layer intake of EXPLAIN-114a's return (11,999 B): the lane's claims read critically, the hub's own re-execution at the bytes (the diffs read whole for the projection and the three endpoints; the census, the module boundary, the test counts from the desk's XML), the four [REVIEW] rulings, the verdict and the one correction before the v1.1.4 freeze, the maintainability reading Nick asked for, and what the WU leaves for EXPLAIN-114b. Also sample #14's read (the first with the FIX-2b-ii instrument).
audience: Nick (§0, §3, §5) · the hub (the WUCP Phase 2 record; the 114b rows) · the frontend lane (FE-114 consumes the four facts in §4)
state-type: audit (filed evidence)
status: FILED v71 beat 5 (Sat 2026-09-12 ~17:4x CT; instrument 2026-09-12T22:36:16Z). Verdict: ACCEPT WITH ONE CORRECTION (R3 — `settledAt != null ⇔ settled`; the correction paste is `context/instructions/2026-09-12_EXPLAIN-114a_AUDIT-CORRECTION_R3-settledAt.md`); the core card (13 M) follows the correction's §5. The return itself was swept into hivemind `7ecaffa` before this audit (the card's `expect 22` read 23 and Nick proceeded) — recorded, no harm: the audit is the record that makes it a return.
-->

# EXPLAIN-114a — intake audit (v71 beat 5)

## §0 Verdict surface
- **ACCEPT WITH ONE CORRECTION.** The WU is what the instruction ordered — the v1.1.4 additive keys, every one appended LAST on its map, every one nullable and never guessed, the verdict enum grown LAST, the runs list left out on the source's own evidence (DP-5: `automation_completed` carries no hash), `module-info` ×2 untouched, nothing outside `core/automation` and `api/rest-api` (`git status --porcelain | grep -v 'core/automation/\|api/rest-api/'` = 0), the FIX-2b-ii lines byte-untouched (`git diff` on `StandardRunManager`/`StandardActionExecutor` = 0 changed lines). Red-first ran in two stages (compile-red, then behavior-red with inert seams), which is the right shape for additive record components. The desk's XML after the lane's last run: `core/automation` 228 tests, 0 failures; `api/rest-api` 156 tests, 0 failures, 1 skipped (pre-existing). The hub could not re-run the gate from the bridge (the Gradle wrapper has no distribution in the bridge's VM); the XML and Nick's `check` on the push are the record.
- **The correction (R3), before the freeze:** a superseded DISPATCHED action renders `settled: true` with `settledAt: null`. v1.1.4 freezes at the landing (CG-123's pattern: additive keys are frozen once served), so the pair is made consistent now — `settledAt != null ⇔ settled` — by a four-line edit and a test split, then the same gate line. The lane's R3 named it and offered the flip; the hub rules it in.
- **The card, after the correction's §5:** `git add -A` → 13 M + 0 A → `git commit -F ../_scratch/2026-09-12_core_EXPLAIN-114a_commit-msg.txt` → push; CI is the gate of record and the `bus-soak` job is sample #15.
- **Sample #14 (`72efb42`, run 34717732026):** both suites GREEN — `bus.soak: loops=20 ok=20 timed_out=0 p50_ms=40 p99_ms=48 max_ms=48 anomalies=0`; `bus.position_census_total: runs=20 subscribers=6 missed=0 unscored_missed=0`; 20 `run_handoff` · 20 `run_body_entered` · 20 `action_step_started` · 0 `run_thread_died` · 0 `NOTIFY_SKIPPED_LIVE`; `available_processors=2`. The first sample with the instrument is clean; the kind-E watch stands at 1 of 10 silent samples toward the (c) revert.

## §1 Layer one — the lane's claims, read critically
The §0 card is complete: baseline and porcelain at boot; the census from lock-free porcelain with the untabled carrier declared under P1 (`AutomationSummary` is the only record that reaches the list endpoint — right); P2–P4 each with the run and the timestamp; DP-5 and DP-6 taken from the source with the line cited; the F3 measurement in one sentence (`correlation_id` inherited at `StandardRunManager:218`, so the triggering envelope is in `readByCorrelation` for every retained run — the only class outside the chain is a retention-trimmed one, and T3 pins that shape). Four [REVIEW]s, each with the source line and a proposed disposition; five [INFO]s, each a shape choice inside the intent. The lane's own summary (pasted by Nick) matches the return; nothing in it is new to the record.

## §2 Layer two — the hub's re-execution
- `git diff --numstat` in core: the thirteen files with the lane's counts (`StandardExplanationService` +178/−59 · `NonFiringExplanation` +62/−12 · `RunExplanation` +48/−6 · `AutomationSummary` +19/−2 · the three endpoints +37/−5 · the four tests +547/−20 · the two MODULE_CONTEXTs +23); `module-info` diff-stat empty.
- `StandardExplanationService` read whole at the diff: `latestDisabled` pages the `automation_disabled` type index ascending with the `latestTerminalRun` idiom (`SCAN_BATCH`, `hasMore`/`nextPosition`), matched on the payload's `automationId`, read only on the DISABLED verdict — O(retained auto-disables), never O(log); `firingValueOf` is an exhaustive-by-default switch over the triggering payload (`StateChangedEvent.newValue` is `requireNonNull`'d in its compact constructor, and `AttributeValues.asString` null-guards anyway, so no NPE path); `deriveOutcome` keeps the classifying ENVELOPES and reads their instant through one helper (`instantOf`: eventTime else ingestTime — the `matchedAt` rule); `buildActions` keeps the `action_completed` envelope so a command-less action settles at it; `definitionKey` is threaded through all eight `NonFiringExplanation` constructions and the list rows compute it with `DefinitionHashes.forDefinition` — the engine's function, so the key agrees across the three reads (T12 pins it).
- The endpoints: every new key `put` LAST on a `LinkedHashMap` sized for the new count (13 · 9 · 10 · 6), instants rendered `Instant.toString()` or JSON null, the verdict still `name()`; the javadoc on each names the amendment and the order.
- The desk's test results (`build/test-results/test`, mtime 22:06Z): `core/automation` 228/0, `api/rest-api` 156/0/1 skipped. `grep -c 'automation.run_handoff' StandardRunManager.java` unchanged — the FIX-2b-ii lines are not in the diff.
- Not re-executed (disclosed): the red-first runs (the lane's timestamps and assertion texts are quoted in its table); Spotless (in the lane's run 3/4 line); the full `check` (Nick's push).

## §3 The four [REVIEW]s, ruled
- **R1 — `firingValue`'s string dialect: ACCEPT.** The instruction's DP-3 named a rendering source that has no string form; the lane used `AttributeValues.asString`, the one dialect already on the explain wire (`observedState[].value`, the ledger's `state_confirmed.expectedValue`). One dialect for every value the hero shows is the maintainable choice; T1b pins the numeric case (`"72"`).
- **R2 — the DISABLED walk reaches less than the instruction's T10 read: ACCEPT AS DELIVERED; a 114b row.** `explainNonFiring` derives DISABLED from the registry's `enabled`; an auto-disable (`StandardRunManager.disabled`, add-only, no `automation_enabled` event) never touches the registry, so an auto-disabled automation whose YAML still says enabled reports from its last terminal run, never DISABLED. The honest fix — DISABLED when `latestDisabled != null` and the automation has not run since, plus a re-enable rule the log lacks (a restart? a reload? the log must say) — changes a verdict's reach and needs a design sentence in Doc 16. Row for EXPLAIN-114b: **DISABLED-2**. The hero's copy is not wrong today (it says what the verdict says), only quieter than the truth.
- **R3 — `settledAt` null for a superseded DISPATCHED: CORRECT NOW** (the paste above). The pair `settled: true` / `settledAt: null` contradicts itself on a frozen wire; the superseding result's instant is on the log; the fix is four lines and a test split, and it lands inside the same census.
- **R4 — the instruction's cite ("LTD-09 / `NO_DIRECT_TIME_ACCESS`"): the hub's error**, no code impact; LTD-09 is the YAML rule, the clock law is `HomeSynapseArchRules:98`. Recorded against the instruction's status line.

## §4 Maintainability and scale — the reading Nick asked for
- **Shape.** The projection stays a pure function of the log (INV-SA-03): every new fact is read from an envelope already in the correlation or from the registry definition; nothing is stored, nothing is guessed, a missing fact is null. That is the property that keeps the explainability reads replay-safe and testable with an in-memory store, and the WU kept it.
- **Growth points, named honestly.** (1) `latestDisabled` and `latestTerminalRun` each walk a type index from position 0 per request; on a home log with retention that is bounded and cheap, but a third such walk should share one cursor helper rather than a third copy of the paging idiom — a 114b/c refactor note, not a defect. (2) `NonFiringExplanation` is now a 13-component record with three convenience constructors (8/9/10-arg) kept for test sites; the next additive bump should collapse the convenience constructors to one builder or a test fixture, or the constructor chain becomes the maintenance cost. (3) The verdict switch stays exhaustive over `RunStatus` with no `default` — the right guard; the new `FIRED_CONFIRMED` is appended last with its sentence unchanged, so no FE copy changes. (4) The endpoints size their `LinkedHashMap`s by hand (13, 9, 10, 6); harmless, but a shape test already pins the key order — the sizes could go.
- **What FE-114 can rely on:** `firingValue` (string or null), `settledAt`/`confirmedAt` (ISO-8601 or null; after R3, `settledAt` present exactly when `settled`), `definitionKey` on the chain, the non-firing read and the list (equal across the three for one definition), `disabledAt`/`disabledReason` only on `DISABLED`, and the verdict `FIRED_CONFIRMED` for a clean confirmed run (the sentence unchanged).

## §5 What changes in the record
- v1.1.4 is DELIVERED pending the R3 correction and Nick's card; the freeze note in the docs (the contract's version catalog) is the hub's next docs act after the landing (a Nick card).
- EXPLAIN-114b's docket gains: DISABLED-2 (R2) · the shared type-index cursor helper (§4.1) · the constructor collapse (§4.2).
- The instruction file's status line: EXECUTED at the landing; R4's cite corrected in place.
- pm-lessons: the correction-before-freeze rule — an additive wire key with a self-contradicting null is corrected before the landing, never after, because the CG-123 pattern freezes it on the push.
