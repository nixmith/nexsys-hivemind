<!--
file: context/audits/2026-09-13_HERO-1c_return.md
purpose: HERO-1c lane return — rows C0–C7 of the step lines, helps and catalog wiring charter landed under web-ui/dashboard (src/**, SPEC.md C0, MODULE_CONTEXT.md C7), tests first per row. Nothing staged or committed; nothing outside the domain touched.
status: RETURNED 2026-09-13 CT (instrument 2026-09-13T21:24:34Z open · 21:55Z close; CT = UTC−5); reports to the hub.
-->

# HERO-1c — the step lines, the helps, the catalog wired (return)

## §0 Card
- **Predictions first: P1 MISSED · P2 MET · P3 MET · P4 MET on this desk / PENDING on Nick's push · P5 MET.** P1: census **18 M + 3 A**, not §3's 14 M + 3 A — four beyond the table, every one `[REVIEW]` below: `CausalChain.hardening.test.tsx` (the `:313` flip the row names), `lib/api/e5-semantics.test.ts` (P5: a third `outcomeMeta` caller), `app.errorPosture.test.tsx` + `views/EventsView.unserved404.test.tsx` (the spinner pins, token-relative). P2: nine mode assertions, **8 RED at HEAD** (every named-target mode renders "Turned on Hallway Light." — not-recorded included); the ninth (skipped, no target) GREEN = HEAD's SKIPPED_BEFORE_COMMAND (hardening:265), named; the file at HEAD 11/14 red. P3: both greps 0 (my own provenance comments counted first — reworded). P5: §3 D1.
- **Baseline held:** HEAD `fed99e8` (main, one past `a458a64`; `git diff --stat a458a64..HEAD -- web-ui/dashboard` empty); dashboard porcelain EMPTY at open; SPEC 57,427 B md5 `307a07ba…`. Instrument: native Windows `node` v22.6.0 / npm 10.8.2 / vitest 4.1.10 with the mount's `node_modules` — the whole toolchain runs here, nothing deferred. No git write command run; no `index.lock`.
- **Red-first (row → RED at HEAD how → GREEN):** C0 → 1/8 fail (`t()` of the four keys undefined; the Register-C row green by construction) → 8/8 · C1 → 11/14 fail (HEAD's one sentence) → 65/65 over modes+hardening+a11y+dangling; **flip named:** hardening:313 `'Turned on Hallway Light.'` → `'Hallway Light turned on.'` · C2 → 14/51 fail (12 helps + `.unconfirmable` + skipped `''`; labels + not-recorded green, named) → 221/221 · C3 → 2/68 fail (the moved pin, pure + render) → 212/212; greps 0/0 · C4 → 3/4 fail (lede, both card texts, the `’` kicker/text; title · kickers · titles · go-lines · noRuns green, named) → 4/4 · C5 → 5/5 fail → 58/58 (retry text, roles, label passthrough green sub-assertions, named) · C6 → 2/14 fail after the FLIP (nullability:49 + :83 `not.toContain('Last checked')` → `toContain('Last checked')` + `toContain(t('whyNot.kv.neverChecked'))`; the `at: null` object row preserved) → 25/25 · C7 documentation.
- **Gates (this desk, §2):** tokens:check OK · eslint 0 · tsc 0 · **vitest 24 files / 435 tests / 0 failed** (21/392 at HEAD) · build ✓ · **bundle 70.0 / 100 KB** · contract-check OK · `VERIFY_EXIT 0`. Gate of record `frontend.yml`: pending Nick's push.
- **Deviations by tag:** `[REVIEW]` the four census files above · `[DELTA]` `commandKind` moved to `verdicts.ts` (re-exported by `format.ts`; callers unchanged) · `[DELTA]` `ErrorState` keeps the hub's problem title/detail as a muted line beneath the keyed body · `[DEFAULT]` D2–D6 · no `[SHIM]`.
- **Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING** (no real-wire row has rendered a mode line). No Java/Gradle run; nothing under `core/**`, `api/**`, `lifecycle/**`.

## §1 Per-file deltas (+334/−205 before C7)
1. **`design/hero-v1/SPEC.md`** (+4) — the charter block after `:275`, byte-exact (extracted from the charter file, not retyped): `:276`–`:279` diff empty; `## §8 Accessibility` at `:281`. **`i18n.ts`** (+9) — the four keys beside their families; R3: catalog strings == SPEC strings. **`i18n.test.ts`** (+32) — the four pinned verbatim + Register C (140 + 4 = 144).
2. **`CausalChain.tsx`** (+43/−15) — `actionStepLine(mode, a, target)`: `MODE_LINE_KEY` (eight modes → `explain.mode.<key>.line`) + the skipped pair keyed on `targetRef` (`skipped.line` / `.lineNamed`); slots `Target`/`target` = `refLabel` (never lower-cased), `verb`/`verbPast` = `commandVerbs`, `reasonClause` = `" — {reason}"` or `""`. The HERO-1b arms keep their sentences: dangling → `danglingTargetLine`; `targetRef` null → `${actionPhrase} a device the run didn't name.`; `command` null with no resolvable target → the skipped sentence (FE-NULL-1's :776 arm, now `heroCopy('explain.mode.skipped.line')`). The acked-silent `unconfirmableHint` duplicate dropped (the help carries it). `SKIPPED_BEFORE_COMMAND` import gone; the StepSpec comment reworded (it was a §6.3 hit). **`CausalChain.modes.test.tsx`** (A, 15 `it`) — the nine lines, `lineNamed`, no-reason, the colour-class held step (hint beneath, provisional suffix), the unnamed arm, a11y intact, the C3 render.
3. **`verdicts.ts`** (+80/−87) — `import { t } from './i18n'` (format.ts would cycle — the charter's §4 arm); every mode's `label`/`help` = `explain.mode.<key>.label`/`.help`; acked-silent on `commandKind(a.command) === 'effect'` → `.unconfirmable` (`command?` added to `ActionVerdictInput`; callers pass the whole action); settled-failed keeps the ten-value sub-label with `settledFailed.help`; skipped `help: ''` (SPEC §5 "—"); not-recorded keeps HEAD's help (§7 has no key — D2); the `default` arm split from `FAILED`: an unknown OUTCOME string → not-recorded mode, `unknownOutcome.label/.help` (SD-7's conservative FAILED now governs only an unknown `resultOutcome` on FAILED, verdicts.test:198 unchanged). `commandKind` lives here. **`verdicts.test.ts`** (+92) — twelve help rows, the effect-class row, the sub-labels, skipped `''`, not-recorded and labels (preservation, named), the moved pin.
4. **`format.ts`** (+10/−43) — `outcomeMeta` deleted (`:197`–`:220`), `ActionOutcome` import dropped; `commandKind` re-exported; `unconfirmableHint` and `SKIPPED_BEFORE_COMMAND` read their §7 rows (byte-identical). **`format.test.ts`** (−6+2) the block + import; **`format.wire-nullability.test.ts`** (−7+2) the block (moved); **`e5-semantics.test.ts`** (+5/−4) `outcomeMeta` → `actionVerdict` (tone info on held; no digits in the helps).
5. **`ExplainHubView.tsx`** (+15/−17) — title, lede, both kickers/titles/texts/go-lines and `:64` `explain.hub.autos.noRuns` (byte-identical) through `t()`; `:62`/`:66` links and the subhead have no §7 key — left, said. **`ExplainHubView.test.tsx`** (A, 4) — the card spans equal the rows in order.
6. **`feedback.tsx`** (+16/−12) — `Loading` default `explain.loading`; `ErrorState` keyed title/body/retry + the hub's words (`title — detail`, or `Error.message`) muted beneath; `OfflineState` keyed + `.retry`; `ReplayingBanner` keyed (the product name leaves the banner — `boot.startingBody` now has no consumer, key kept). **`feedback.test.tsx`** (A, 5). The four "never the eternal spinner" pins → `t('explain.loading')`.
7. **`WhyNotView.tsx`** (+14/−7) — `lastEvaluation === null` → the "Last checked" row with `whyNot.kv.neverChecked`; `{at: null}` keeps the suppression. **`WhyNotView.nullability.test.tsx`** (+6/−4) — the two flips named above + `t` import.
8. **`MODULE_CONTEXT.md`** — the 2026-09-13 beat (the hub's five items) + the centralized-copy gotcha extended.

## §2 The gates
- `cd web-ui/dashboard && npm run verify` → tokens:check OK · eslint 0 · tsc 0 · vitest 24/435/0 (`modes` 15 · `feedback` 5 · `ExplainHubView` 4 · `verdicts` 53 · `hardening` 37 · `a11y` 10 · `i18n` 8 · `format` 80 · `nullability` 14) · build ✓ (js 38.8 KB · font 24.8 · css 5.5 · html 0.8 = 70.0 KB gz) · contract-check OK (`v1.1.3-2026-09-06` untouched) · EXIT 0. Each row's RED was executed before its edit (§0). Deferred gate of record: `frontend.yml` on the push.
- **§6 instruments, quoted:** (1) porcelain = the 21 files of §1 (18 M + 3 A), nothing outside the domain · (2) `grep -rn "We sent it" src | wc -l` = 0 · `grep -rn outcomeMeta src | wc -l` = 0 · (3) the exact §6.3 grep over the three files: no output, exit 1 (= 0 hits; at HEAD it had 3 — the hub lede `:20` and two comment lines) · (4) above · (5) SPEC `:276`–`:279` diff empty, §8 follows. R4: `grep -rn 'BRAND.productName\|"we '` over the touched sources = 0.
- **Preflight:** 1 PASS (snapshot v73 b4 ↔ HERO-1c handed) · 2 PASS (client v1.1.3; the v1.1.4 keys are 114a's, none read) · 3 PASS · 4 STALE → C7 · 5 PASS · 6 PASS (name-light; one name removed from a hero surface) · 7 PASS (mirror diff empty) · 8 PASS · 12: `2026-09-06_H8a_…navigator-packet.md` carries a live status and is not dated today — whether the spine lists it RUNNING is the hub's to confirm.

## §3 Pushback and observations
- **D1 [P5]** `outcomeMeta` spans `format.ts:197`–`:220`, not `:216`; its callers were THREE tests — `src/lib/api/e5-semantics.test.ts:19/:50/:96–:97` escaped the hub's `**/*.tsx` grep; `.help` exists for seven of the nine modes (skipped: none by design; notRecorded: none), not "all nine".
- **D2 [ASK]** two §7 gaps: no `explain.mode.notRecorded.help` (HEAD's sentence kept: "What happened to this step was not recorded. The step itself is preserved."); a null `command` on a named-target FAILED step renders the `act` arm — "The command to act Hallway Light failed — {reason}." (the SPEC's own null verb; reads oddly). Two rows asked.
- **D3 [REVIEW — the hub's ruling asked, non-blocking]** `feedback.tsx` is mounted by `Resource` on EVERY view: the devices/health/automations/runs pages now read "Loading this run…" and "This explanation couldn't be loaded." on their loading/error states — the row as written, honest, but hero-scoped wording off the hero. Options: (a) `Resource` takes optional labels and the hero views pass the `explain.*` rows while a generic pair (`state.loading`/`state.error.*`) is a SPEC §7 amendment; (b) keep. Rec (a); one-line revert either way.
- **D4 [DEFAULT]** the `.noTime` and `terminal.noSteps` keys are ADDED, not consumed (no row): `whyNotCard`'s `timed()` already yields the byte-identical N4/N6 text (a zero-visible swap), `terminalLine` still writes literals; D5's headline key waits on the hub's audit as §5 says.
- **D5 [NEXT]** the §6.3 instrument is blind to JSX text nodes and template literals; hero sentences still outside the catalog: `CausalChain.tsx` `terminalLine` (`explain.terminal.*` keys exist), the condition line (`explain.condition.line` exists), the cascade link text (`explain.cascade.parent` exists), the do-nothing step + hint, the recovered-reason note, the trigger verb line; `ExplainHubView` `:62`/`:66` links + "Your automations" + On/Off pills; `WhyNotView` page titles/ledes, "← Pick another automation", the "It did run" / "Ran, but sent nothing" pills; `Resource.tsx` "Signing in…"; `pendingHint`'s "Color" vs the catalog's "Colour". **Next WU: HERO-1d** — the existing keys wired first (zero-visible), then one §7 amendment for the keyless set.
- **D6 [DEFAULT]** `explain.hub.autos.noRunsSinceLoad` stays unconsumed (`noRuns` matched `:64` byte-for-byte; which of the two the null cell should say is the hub's).
- **[DEFAULT]** the held-DISPATCHED help collapses HEAD's acked/bare pair into the one §7 row (the `acknowledged` disposition stays in L2).

## §4 WUCP Phase-1 checklist
- [x] Preflight run; volatile facts re-derived at source (HEAD, SPEC bytes, the catalog, the callers).
- [x] Contract FROZEN v1.1 (+ additive) consumed; no key read that 114a did not freeze; validators, fixtures, `CONTRACT_VERSION` untouched.
- [x] Every mode line/help honest and distinct; a11y intact (hidden step text, provisional suffix, `role=status`); Register C — no "we", no name (one removed).
- [x] Copy centralized + test-locked (144 keys; ten test files); no positioning claim.
- [x] Bundle 70.0/100 KB; `verify` EXIT 0 here; `frontend.yml` pending the push; nothing phones home.
- [x] Return filed; register REPO-COMPLETE, LIVE-VERIFICATION PENDING; next WU named (HERO-1d); D3 asked.

## §5 Correction (2026-09-13; instrument 22:27:10Z open · 22:32:15Z close) — D3 + D2 on the uncommitted tree
- **§1 D3:** `ui.loading` · `ui.error.title/.body/.retry` in `i18n.ts`; `feedback.tsx` `Loading` defaults `ui.loading`, `ErrorState` takes `title`/`body`/`retry` (defaults `ui.error.*`), offline/replaying unchanged; `Resource.tsx` `labels?`; the four hero mounts (`RunChainView:31`, `WhyNotView:61`/`:159`, `ExplainHubView:42`) pass the `explain.*` rows; MODULE_CONTEXT's sentence. Tests: `feedback.test` flips named (Loading `explain.loading` → `ui.loading`; ErrorState `explain.error.*` → `ui.error.*` ×2) + the hero-override row (green by construction, named); `app.errorPosture:103` + `EventsView.unserved404:175` → `ui.loading`; `nullability:90` stays; NEW in `app.errorPosture`: the DEVICES page loads with `ui.loading` (RED: it rendered `explain.loading`) + the run page with `explain.loading` via labels (green by construction, named). **RED 4/33 → GREEN 76/76** (8 files).
- **§2 D2:** SPEC `:280`–`:281` = the instruction block (diff empty; blank `:282`; §8 `:283`); the two keys in `i18n.ts` (catalog == SPEC); `verdicts.ts` not-recorded help = `t('explain.mode.notRecorded.help')` — the `actionVerdict` awk count of `help: '` = 1 (the skipped `''`); `CausalChain.tsx` `actionStepLine`: FAILED + `command` null + a named target → `settledFailed.lineNoCommand`. Tests: the not-recorded pin was RED only because the key was absent (same sentence — preservation, said); `modes.test` +2 (RED: "The command to act …"); `i18n.test` +1 (RED: keys absent). **RED 4/79 → GREEN 126/126** (5 files). MODULE_CONTEXT's "no help key" clause corrected.
- **Census: 23 = 20 M + 3 A** (+ `Resource.tsx`, `RunChainView.tsx`); nothing outside the domain; no git write.
- **Verify:** tokens:check OK · eslint 0 · tsc 0 · vitest **24 / 441 / 0** (435 before) · build ✓ · bundle **70.1 / 100 KB** · contract-check OK · `VERIFY_EXIT 0`; `frontend.yml` on the push is the gate of record.
RETURNED nexsys-hivemind/context/audits/2026-09-13_HERO-1c_return.md 14157
