<!--
file: context/audits/2026-09-13_HERO-1d_return.md
purpose: HERO-1d lane return — the last literal hero sentences keyed byte-identically (24 keys), IR-8's literal lint inside `npm run verify`, the SPEC §7 amendment (24 rows, the charter's form), the "colour" sweep at the five named sites; P1–P5 adjudicated; the lint's before/after lists.
audience: the v74 hub (intake) · Nick (lands the card)
state-type: lane return (frontend; the web-ui path-domain)
status: RETURNED — Sun 2026-09-13 ~21:5x CT (instrument 2026-09-14T02:24:09Z); the tree UNCOMMITTED on core `93390f0`; charter `context/instructions/2026-09-13_frontend-lane_HERO-1d_keyless-hero-sentences_literal-lint_charter.md`
-->

# HERO-1d — return

## §0 Card
- **Instrument:** Windows desk, native — node v22.6.0 · npm 10.8.2 · vitest 4.1.10 · tsc 5.9.3 · ESLint 9.39.4 installed (the lock pins 10.7.0, also run: exit 0). CT = UTC−5. **Nothing deferred**: `npm run verify` GREEN. Baseline `93390f0` held; whole-tree porcelain = the dashboard's.
- **Census — 17 = 14 M + 3 A, all under `web-ui/dashboard/`.** M: `MODULE_CONTEXT.md` · `design/hero-v1/SPEC.md` · `eslint.config.js` · `src/components/{CausalChain.tsx, CausalChain.hardening.test.tsx, CausalChain.modes.test.tsx}` · `src/lib/{i18n.ts, i18n.test.ts}` · `src/views/{ExplainHubView.tsx, ExplainHubView.test.tsx, WhyNotView.tsx, WhyNotView.hero.test.tsx, RunsView.tsx, RunChainView.tsx}`. A: `src/components/CausalChain.terminal.test.tsx` · `src/views/RunsView.test.tsx` · `src/views/RunChainView.test.tsx`. `Resource.tsx` untouched (D0 flags nothing — preservation); `i18n.test.ts` M (the catalog's test home).
- **Red-first:** D0 — 11 lint hits at HEAD (§2) → 0. D1 — `terminal.test`: 8 keyless arms RED (key undefined; the literal pin passes) → 19/19; preservation: `.completed` · `.completed.open` · `.completed.nothing` · `.skipped` ×2 · `.failed` ×2 · `.cancelled` · never-"0.0s" · the open count. D2 — `hardening.test` §D2: 4 RED (`step.nothing.*` ×3, the recovered suffix) → 7/7; preservation: the condition line ×2, the cascade link (existing keys already equal the literals; the charter predicted red — disclosed). D3 — the hub test 4/4 RED → 4/4. D4 — why-not 2/2 · runs 3/3 · run-chain 1/1 RED → 6/6. D6 — `i18n.test` §D6 RED → 1/1. Catalog — `i18n.test` §HERO-1d (24 rows) RED → 2/2 (Register-C row green by construction — disclosed). D5/D7: doc rows (§2).
- **Predictions:** P1 HELD (two deltas above). **P2 FAILED on the count** — 11 at HEAD, not ≥ 12: the pattern misses nine of the hub's sites, sees two it omitted (§3 (a)); 0 after. P3 HELD (27 files / 480 tests = 441 + 39). P4 HELD (the §6.6 grep = 0; 43 `expect` lines added; the one removed test line is D6's `it(` title). **P5 FAILED** — 16 lines at HEAD, not 5; 11 after D6: the five swept, the eleven REPORTED not changed (D6's own rule, §6.5), so §6.3 reads 11 — the hub's call (§3 (b)).
- **Gates:** `npm run verify` GREEN — tokens:check · lint · typecheck · test 27/480 · build · bundle **70.3 / 100 KB** · contract 11 endpoints v1.1.3-2026-09-06; the six-file lint exit 0 under 9.39.4 and 10.7.0. `frontend.yml` on Nick's push.
- **Deviations:** [DEFAULT] no `.skipped.noReason` / `.failed` twin — the `{reasonClause}` empty arm already renders "Skipped." / "Failed." (one sentence, one key). [DEFAULT] `explain.terminal.status` "{label}." for HEAD's tail: INTERRUPTED renders "Interrupted.", so `explain.terminal.interrupted` and `.noSteps` stay unconsumed (text changes, filed). [DEFAULT] `explain.run.title` for RunChainView's flagged title. [DEVIATION-MINOR] `MODULE_CONTEXT.md` :51's guard clause → the lint (else stale); `i18n.test.ts` M. [OBS] desk ESLint drift.

## §1 Per-file deltas (+350/−39 tracked, +3 A: 8,487 · 1,773 · 1,375 B)
- `eslint.config.js` (+26): one flat-config block, `files:` = the six hero SOURCE files, `no-restricted-syntax` with the charter's three selectors (`JSXText[value=/…/]` · `TemplateLiteral > TemplateElement[value.raw=/…/]` · `JSXAttribute[name.name=/^(title|lede|label|emptyLabel|placeholder)$/] > Literal[value=/…/]`), message "hero copy lives in i18n.ts (HERO-1d)". esquery 1.7.0 honours the regex attribute form under 9.39.4 and 10.7.0 — the ESLint path, no script fallback (T1).
- `src/lib/i18n.ts` (+40/−1): 24 keys — D1 `explain.terminal.unrecorded` · `.completed.noTime` · `.completed.nothing.noTime` · `.completed.open.one` · `.completed.open.noTime` · `.completed.open.one.noTime` · `.status`; D2 `explain.step.nothing.one` / `.many` / `.hint` · `explain.action.detail.outcome.recovered` (leading space kept); D3 `explain.hub.autos.title` / `.whyFire` / `.whyNot` (’) · `ui.on` / `ui.off`; D4 `explain.whyNot.pick.title` / `.pick.lede` / `explain.whyNot.title` / `.back` · `explain.runs.title` / `.lede` / `.empty` · `explain.run.title`; D6 :108 "Colour" → "Color".
- `src/components/CausalChain.tsx` (+26/−20): :161 → `heroCopy('explain.condition.line', {condition, verdict})`; :251 → `t('…outcome.recovered')`; :269 → `.nothing.one` / `.many` at `{count}`; the hint → `t('explain.step.nothing.hint')`; :307 → `t('explain.cascade.parent')`; `terminalLine` — signature kept, every arm `heroCopy(...)`: `.unrecorded` at `{notRecorded: NOT_RECORDED}` · the COMPLETED arms with their `.noTime` twins (the `secs` guard intact) · `.open.one` vs `.open` at `{count}` · `.skipped` / `.failed` at `{reasonClause}` · `.cancelled` · the open-vocabulary tail `.status` at `{label: runStatusMeta(s).label}`.
- `ExplainHubView.tsx` (+8/−7): the subhead, the two links, the On/Off pills; the C4 comment updated. `WhyNotView.tsx` (+3/−3): picker title/lede, detail title, back link. `RunsView.tsx` (+3/−2): title/lede/emptyLabel + the `t` import. `RunChainView.tsx` (+1/−1): the title.
- Tests: `CausalChain.terminal.test.tsx` A (19 rows, each pins the literal AND the key); `hardening.test` +78 (§D2, 7); `ExplainHubView.test` +36 (§D3, 4); `WhyNotView.hero.test` +33 (§D4, 2); `RunsView.test` A (3); `RunChainView.test` A (1); `i18n.test` +64 (§HERO-1d: the 24 rows verbatim + Register C; §D6 pin); `modes.test` :100 title "colour-class" → "color-class".
- `design/hero-v1/SPEC.md` (+28/−4): :282–:305 the 24 rows in the charter's exact four-cell form (`| key | sentence | surface | HERO-1d |`), :307 the amendment line verbatim (a blank line between, so GFM does not fold it into the table); D6 at :33 · :70 · :178 (both words of :178). No other line.
- `MODULE_CONTEXT.md` (+3/−1): the HERO-1d paragraph at :25 (the rule, the lint's three selectors and home, the 24 keys, the test homes, the spelling of record, the unconsumed rows); :51's guard clause → the lint.

## §2 The gates
- **D0 at HEAD (P2 baseline), `npx eslint <six files>` — 11 errors, exit 1:** `CausalChain.tsx` 269:21 (Template "Nothing was changed:") · 271:40 (JSXText, the hint) · 307:71 (JSXText "← See what triggered this run") · `ExplainHubView.tsx` 60:67 ("Why did it fire?") · `RunChainView.tsx` 22:17 (title "Why this happened") · `RunsView.tsx` 19:17 (title) · 19:50 (lede) · 27:26 (emptyLabel) · `WhyNotView.tsx` 60:46 (lede "Choose the automation you expected to run.") · 155:17 (title "Why this didn't happen") · 157:44 ("← Pick another automation"). An independent typescript-estree walk (scratchpad) agrees: 11.
- **After D4:** the same command — no output, exit 0 (9.39.4 and 10.7.0). `npm run lint` green.
- `npm run verify`: GREEN (log: scratchpad `verify.log`) — 27 files / 480 tests; bundle 70.3 KB gzipped (headroom 29.7 KB); contract v1.1.3-2026-09-06, 11 endpoints.
- `grep -rni colour web-ui/dashboard/src web-ui/dashboard/design | wc -l`: 16 → **11** (§3 (b)).
- `git --no-optional-locks status --porcelain -- web-ui/dashboard` = the 17 of §0; the whole tree = the same 17.

## §3 Pushback and observations
- **(a) The prescribed pattern's reach [FINDING, instrument]:** `/[A-Z][a-z]+ [a-z]+ [a-z]+/` needs three bare words after a capital, so it cannot see `CausalChain :161` (`The rule "` — two words before the interpolation), `:251` (no capital; a plain string), `:401` · `:413` · `:420–:427` (a capital then an interpolation, or no capital), `ExplainHubView :41` ("Your automations", two words) · `:64` (the apostrophe in "didn’t"), the On/Off pills, `WhyNotView :60`'s title (the apostrophe). All were keyed anyway by rows D1–D4 (the rows govern; the lint guards). Residual literals the pattern still cannot see: `RunChainView :24` "← All runs", `Resource.tsx :31` "Signing in…", the `WhyNotView` pills 'Ran, but sent nothing' / 'It did run' (object properties), `CausalChain :79` 'Completed, nothing changed', `actionPhrase`'s verbs (:360–:372), the trigger verb line (:138). A widened pattern (≥ 2 words, apostrophes allowed, a capital before an interpolation) reads 19 at HEAD and catches the first two; a fourth selector on `Property > Literal` reaches the pills — a T1 widening for the hub to rule. Shipped: the charter's pattern, verbatim.
- **(b) P5's residual eleven** (SPEC prose :92 · :96 · :110 · :311 · :313 · :323 at the returned numbering, :285 · :287 · :297 at HEAD; `states.html` :10 · :142 · :509 · :528 · :543) — D6's "report, do NOT change" and §6.5. Other variants, counted, not changed: `Cancelled` 17 lines exact-case (`states.html` 36 incl. lower-case · SPEC 9 · `format.ts` :352 label · `i18n.ts` `explain.headline.cancelled.frame` + `explain.terminal.cancelled` · `scenarios.ts` 4 · tests 5); `recognise` 2 (`i18n.ts` :151 · SPEC :207); `behaviour` 2 (`DevicesView.tsx` :97 · `DevicesView.honesty.test.tsx` :74, both comments); `neighbours` 2 (SPEC :96 · `states.html` :509). The hub decides the register.
- **(c)** After D6 the catalog's `explain.action.pending.color` is byte-identical to `format.ts` `pendingHint`'s literal (:217) — a zero-visible swap for a later lane (`format.ts` is out of scope here).
- **(d)** INTERRUPTED renders "Interrupted." (HEAD's tail) through `explain.terminal.status`; the §7 sentence "Cut off before it finished." and `.noSteps` wait on a text-change ruling (FE-114 or the headline audit).
- **(e) Charter nits:** §1's line numbers for `explain.condition.line` / `explain.cascade.parent` were :204 / :199; at HEAD :208 / :202. Preflight: the `~/.claude/skills/nexsys-frontend` mirror path does not exist on this desk (the loaded copy diffs clean against `nexsys-skills/`); Check 12 still lists `2026-09-06_H8a_…_navigator-packet.md` as ISSUE-READY-class.
- **Next WU (refuse-to-close):** FE-114 (the v1.1.4 keys' rendering) with the two unconsumed terminal rows and (a)'s widening ruled in its charter; the hub's landing card for these 17.

## §4 WUCP Phase-1 checklist
- [x] Preflight run at boot; volatile facts re-derived (contract FROZEN v1.1.4, the mirror at v1.1.3 by FE-114's ruling; snapshot v74 b4; skill copy clean).
- [x] No contract shape touched; no key read; no mock changed.
- [x] Every keyed sentence byte-identical (each test pins the literal and the key); the only text change is D6's five sites; nulls keep their keyed sentences.
- [x] Bundle 70.3 KB / 100 KB; the gate GREEN on the desk, not deferred; nothing phones home.
- [x] Name-light: no product name in any added row (Register C test); no positioning claim.
- [x] Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING (no real-wire row rendered by this lane). Next WU named. Never `git add/commit/push`.

RETURNED nexsys-hivemind/context/audits/2026-09-13_HERO-1d_return.md 11613
