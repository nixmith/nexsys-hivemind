<!--
file: context/audits/2026-09-13_P-1_return.md
purpose: The P-1 bench lane's return — the power-harness primitive as a driver with enforced safety limits, a reserved role, and an honest limit on what it proves. Desk only; the live leg was never fired.
audience: the hub (audits) · Nick (lands the card, rules the gate).
state-type: lane return.
charter: context/instructions/2026-09-11_bench-lane_P-1_power-harness-primitive_charter.md
baseline: nexsys-bench 4539f13, porcelain empty at entry. HEAD unchanged at exit; staging empty; never `git add`/`commit`.
-->

# P-1 — bench lane return (Sun 2026-09-13 CT · UTC−5)

## §0 CARD

**DONE against §6.** Census matches §3 exactly. Tests RED at HEAD, GREEN
after. `--dry-run` prints the plan, the guards, the refusals and the honest
limit. The doc carries the six uses with their R-4b lines, the safety table,
the R-5 fold and the shopping line.

| | |
|---|---|
| Census | **3 A + 1 M** — as predicted |
| Gate at HEAD | `16 check(s), 16 failure(s)` · exit 1 |
| Gate after | `19 check(s), 0 failure(s)` · exit 0 |
| Live commands fired | **0** — `--dry-run` was the only executed mode |
| `NETWORK_CALLS`, whole session | **0** (instrumented chokepoint) |
| Design doc | 8176 B (ceiling 8192, 16 B headroom) |
| Regression | `nightly_digest.py --selftest` still `21 check(s), 0 fail` |
| HEAD | `4539f13` · staged: 0 · `1 file changed, 57 insertions(+)` |

**Read first:** an adversarial self-audit against my own driver found
**seven defects, six of them real**. All six are fixed, each with a
regression check. The worst was **D1 — my own test suite could not detect
deletion of the factory-reset guard**, the most safety-critical guard in the
file. Details in §5.

---

## §1 PREDICTIONS, ADJUDICATED

**P1 — census is `3 A + 1 M`. ✅ HOLDS, exactly.** See §2.

**P2 — safety tests RED at HEAD, GREEN after; the three refusals each tested
with no network. ✅ HOLDS, and exceeded.** RED at HEAD: `16 check(s), 16
failure(s)`, exit 1 (`ModuleNotFoundError: No module named 'harness'`).
GREEN after: `19 check(s), 0 failure(s)`, exit 0. The three charter-named
refusals (`maxCyclesPerWindow`, `minSecondsBetweenCycles`, unknown profile)
each have their own check; six further refusals and the fence do too. The
gate is sabotage-verified non-vacuous — nine mutated copies under `/tmp`
(repo untouched) each drive it RED.

**P3 — every plan timestamp is an offset from window-open, never a wall
clock, and `state_reported` is named as the proof. ✅ HOLDS — but it did NOT
hold in my first draft.** Over 29 dry-run invocations, 197
`harness.plan.step:` lines: zero wall-clock or ISO matches. But a negative
`--at` printed `t=+-5s`, unvalidated. Fixed (D2); `t_invalid_offsets` now
asserts `t=+-` never reaches a plan.

**P4 — at least one charter cite is wrong at the baseline. ✅ HOLDS — five
findings. The corrected lines:**

**P4-a — §3 row 2's test invocation is ungrounded (the substantive one).**
The charter says *"run with `python3 -m pytest tools/harness` or `python3 -m
unittest` — ground which the bench uses at `tools/runner/README.md`"*. That
README names **neither**; its only test invocation is **line 202**, `python3
-B tools/runner/nightly_digest.py --selftest`. The repo carries **zero**
`test_*.py` files — the bench's idiom of record is an in-module `selftest()`
(`nightly_digest.py:419`, dispatched at `:542`) — and pytest is **not
installed** on the desk, so the charter's first spelling would not run.
> **CORRECTED LINE.** `tools/harness/test_harness.py` | A | the refusals;
> the plan printer — run with **`python3 -B tools/harness/test_harness.py`**,
> the bench's own idiom, grounded at `tools/runner/README.md:202`. A stdlib
> `unittest` bridge ships as a second spelling (`python3 -m unittest
> discover -s tools/harness -t .`); **pytest is not a bench dependency.**

**P4-b — the charter's `baseline:` names a superseded core commit.** It says
`core 1e26912`; the working core is **`a458a64`**. Both named cites survive
the drift and were re-verified at source today: `RestFilters.java:482` is
`app.post("/api/v1/entities/{entityId}/commands", …)` and `:196` is
`app.get("/api/v1/entities", …)`, both exact.
> **CORRECTED LINE.** `baseline: … core a458a64 (POST …/commands at
> RestFilters.java:482; the entity read at :196 — both re-verified at source
> 2026-09-13; 1e26912 is superseded).`

**P4-c — §3's constants row does not know about a live namespace.**
`constants.yaml` already carries a **top-level `plugs:` at :336** — a MAP
keyed by name (`{hue-wall: {ip: PLACEHOLDER}}`) feeding the format's `plug:`
stimulus and `requires: [plug]`. §3 orders `harness.plugs` as a **LIST**. No
YAML collision (different paths, checked with a duplicate-key loader), but
one file now holds two `plugs` collections of different shapes — flagged in
the block's comment so a reader reads the path, not the leaf.

**P4-d — "stdlib only" and "read the profile from `constants.yaml`" cannot
both hold.** YAML is not stdlib. `harness.py` uses `yaml.safe_load` **exactly
as `tools/runner/engine.py:107` does**: two parsers over one constants file
is a drift defect, and PyYAML already ships to the Pi with `tools/`.
Everything else is stdlib. **Filed as a deviation for the hub to rule.**

**P4-e — the word "refused" now carries two exit codes.** The charter sets
the harness's refusals at **exit 3**; the engine's own `REFUSED` is **exit
2** (`tools/runner/README.md`, Verdicts and exit codes). No collision — 3 is
free — but the vocabulary is now ambiguous across the repo. Left as-is
(charter governs); raised in §6.

*(Cites that verified correct: `command-confirm-s31.yaml:128`,
`constants.yaml:42–50`, `command.s31-entity` at `:115`, and `bench.sh:18`
`api_token() { cat "$HOME/hs-bench/config/initial_api_token"; }`.)*

---

## §2 THE CENSUS (exact paths)

| Path | M/A | Bytes | Note |
|---|---|---|---|
| `nexsys-bench/tools/harness/harness.py` | A | 22749 | the verb; one instrumented chokepoint |
| `nexsys-bench/tools/harness/test_harness.py` | A | 15249 | 19 checks, no network |
| `nexsys-bench/docs/2026-09-13_P-1_power-harness_design.md` | A | 8176 | ≤ 8192 |
| `nexsys-bench/scenarios/constants.yaml` | M | 21811→25129 | +57 lines, **0 del** |

**Append-only, proven:** the first 378 lines are byte-identical to
`git show HEAD:scenarios/constants.yaml`. Nothing else in `scenarios/`,
`tools/bench.sh`, `tools/nightly.sh` or `tools/runner/`; `git status
--porcelain -uall` returns exactly those four paths.

**One honest note.** My read-only `git status` left a 0-byte
`nexsys-bench/.git/index.lock`, which would have blocked your commit; I
requested delete permission and removed it. Nothing else under `.git/` was
touched; HEAD, staging, stash and reflog are unchanged.

---

## §3 THE GATES

```
RED at HEAD  [X] import harness — ModuleNotFoundError: No module named 'harness'
             [X] ×15 "blocked: tools/harness/harness.py is absent"
             selftest: 16 check(s), 16 failure(s)   exit 1
GREEN after  selftest: 19 check(s), 0 failure(s)    exit 0
REGRESSION   nightly_digest.py --selftest → 21 check(s), 0 failure(s)
```

---

## §4 THE DRY-RUN TRANSCRIPT (counts)

`cycle --plug <s31> --window r4c-hue-silence-1 --at 36 --off-for 5
--dut-profile philips_hue_white_color_a19 --dry-run` → **exit 0, 23 lines:**

`harness.guard:` **9** (each `result=ok` / `result=REFUSE`) · `harness.plan:`
**1** · `harness.plan.step:` **7** (all `t=+Ns` offsets) ·
`harness.plan.proof-line:` **2** (the `state_reported` proof, off and on) ·
`harness.plan.dut:` **1** · `harness.plan.ledger:` **1** ·
`harness.settle: unmeasured` **1** · `harness.proves:` **1**.

Wall-clock or ISO matches in any plan line: **0**. Malformed offsets: **0**.
No ledger file or directory was created. Nine refusals each print
`harness.refused: reason=…` and exit **3**: `max-cycles-per-window`,
`min-seconds-between-cycles`, `unknown-profile`, `role-not-harness`,
`factory-reset-hazard`, `plug-not-declared`, `dut-is-harness-plug`,
`invalid-offset`, `malformed-limit`.

---

## §5 THE SELF-AUDIT — 7 defects found, 6 fixed

An adversarial pass was run against my own driver (repo read-only; all
mutations under `/tmp`). Every fix carries a regression check.

| | Defect | Fix |
|---|---|---|
| **D1** | **The gate could not detect deletion of `factory-reset-hazard`** — the fixture's `maxCyclesPerWindow: 2` refused first and the assertion was a disjunction, so the guard could be deleted and the suite stayed green. | High-cap fixture; exact-reason assert. **Verified:** deleting the guard now drives the gate RED. |
| **D2** | Negative `--at` / `--off-for` unvalidated; `--off-for -30` commanded power **off** then raised on `time.sleep`, **leaving the DUT dark**. | New `invalid-offset` guard, pre-network, exit 3. |
| **D3** | Live never honoured `--at` — it fired at window-open while the plan printed `t=+36s`: a fiction live would not keep. | Live sleeps the offset, printing `harness.window: open_at=… offset=+Ns`. |
| **D4** | A caller-chosen `--window` label **reset the factory-reset budget** (4 cycles banked, fresh label ⇒ approved). | The hazard is now **device-scoped**: counted across every label over `minSecondsBetweenCycles × resetCycles` (360 s shipped). Regression check `t_hazard_is_device_scoped`. |
| **D5** | A `Refusal` inside the live path escaped as a traceback (exit 1), and a **spent** cycle went unrecorded. | Wrapped → `harness.refused:` exit 3; `record_cycle` moved **before** the first transition, so an aborted cycle still spends its budget. |
| **D6** | A non-numeric limit (`maxCyclesPerWindow: "two"`) crashed the guards — failing **open, loudly**. | `malformed-limit` refusal; limits fail **closed**. |
| **D7** | Design doc had 3 B of headroom. | Trimmed to 8176 (16 B). |

**Not a defect, worth knowing:** `--window=--dry-run` (likewise `--dut`,
`--ready-token`, `--state-dir`) makes argparse swallow the literal string as
that option's *value*, so `--dry-run` is not set. The flag is never
bypassed, and against shipped constants all four still refuse at
`role-not-harness` with 0 network calls.

---

## §6 OPEN QUESTIONS

1. **`rating_w: 1800` is PROVISIONAL.** The repo holds no S31 corpus entry
   and no datasheet; 15 A / 1800 W is the vendor + ETL-listed retail figure
   (itead.cc; Amazon B082PSKRSP), **not** read off the unit's label — I
   declined to treat a retail listing as a safety fact. The block pairs the
   verification with the `HARNESS-PLUG:` promotion. **Does the hub accept
   that pairing, or should the rating get its own gate word?**
2. **The R-5 fold.** `SCENARIO_FORMAT.md` §1 already reserves a commented
   `plug:` stimulus and `requires: [plug]`. The doc recommends **extending
   that key** over minting a parallel `harness:` key — two spellings for one
   act. Hub to rule at R-5.
3. **Exit-code vocabulary** (P4-e): harness `REFUSED`=3 vs engine `REFUSED`=2.
4. **The scoping split.** `maxCyclesPerWindow` is window-scoped by name;
   only the hazard is device-scoped. `--window` is caller-supplied and
   required (never defaulted — a guessed label silently resets a counter).
   Should the per-window cap also carry a time bound?
5. **Label collision.** `constants.yaml:283` says *"MINTED 2026-07-31 from
   the P-1 paste"* — a different P-1 (the quiesce arc).
6. **Live mode has never executed.** D3/D5 are proof that desk review of an
   unexecuted path finds real bugs. The first live run needs its own
   operator packet and pre-stated verification, after R-5 or `HARNESS-PLUG:`.

---

## §7 WHAT DID NOT HAPPEN (the fence)

No command was fired at any device. `--dry-run` was the only executed mode;
`NETWORK_CALLS` is **0**, re-proven with `socket.connect`,
`create_connection`, `getaddrinfo` and `urlopen` monkeypatched to raise —
including a **live-mode** invocation, which refused at `role-not-harness`
before reaching the chokepoint. No `bench.sh`, no suite, no nightly.
`s31`/nightly untouched. No `scenarios/*.yaml` but `constants.yaml`. No new
stimulus key, no `sqlite:`. No `git add`, no `git commit`. Nick lands the card.

RETURNED nexsys-hivemind/context/audits/2026-09-13_P-1_return.md 12286
