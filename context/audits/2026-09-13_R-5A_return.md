<!--
file: context/audits/2026-09-13_R-5A_return.md
purpose: The R-5 PART A bench lane's return — P-1's driver folded into the scenario format and the runner, the exit-code and time-bound rulings, the ULID-provenance fix, the fleet line. Desk only.
audience: the hub (audits) · Nick (lands the card)
state-type: lane return.
charter: context/instructions/2026-09-13_R-5_fleet-floor-rebaseline_P-1-fold_and-the-four-open-measurements_charter.md, PART A
baseline: nexsys-bench 1201368, porcelain empty at entry; HEAD unchanged at exit; staging empty.
-->

# R-5 Part A — bench lane return (Sun 2026-09-13 CT · UTC−5)

## §0 CARD

**DONE against §A6.** Instrument limit: the desk only; the bench card was
never in my hands. `date -u` at entry `2026-09-13T21:26:39Z`.

**Census: N = 9 = 8 M + 1 A — §A3's table exactly, no deviation.**
M `SCENARIO_FORMAT.md` · M `engine.py` · **A `runner/test_engine.py`** · M
`harness.py` · M `test_harness.py` · M `constants.yaml` · M
`nightly_digest.py` · M `P-1_design.md` · M `runner/README.md`.

**P1 — MET.** Porcelain is those 9 paths, nothing else.
**P2 — MET.** Every row's new checks RED at HEAD by construction, GREEN after.
Preservation checks, named, green at HEAD *and* after: the
anti-vacuous empty-`positive:` REFUSAL; the RESERVED §1 `plug:` grammar
(`{target, act, settle}` + `requires: [plug]`) still lints and SKIPs; the 19
pre-existing harness refusal checks (they read `harness.REFUSED`, so 3→2
passed through untouched).
**P3 — MET.** `grep -n '^REFUSED = ' harness.py` → `2`; `test_harness.py`
parses the `| REFUSED | 2 |` row out of the README's new exit-code table and
pins `harness.REFUSED` to it — no second literal.
**P4 — MET: §A1's ULID-audit line** (§3 A). Two further premises also failed
at HEAD — SD-A6's EUI64 and DP-1's device list (§3 B).

**Gates:** `test_harness.py` **19 → 28** · `--selftest` **21 → 30** · new
`test_engine.py` **14**; 0 failures, twice. Tally **0**.

**DP-1 taken:** count-and-ids — not merely as a fallback: the EUI64 read is
impossible *and* would be vacuous (§3 B). **DP-2:** `windowSeconds` in the
plug's row (§3 C). **One self-inflicted regression, found and fixed before
exit** (§3 D). Fence intact; the live leg stays REFUSED until `HARNESS-PLUG:`.

## §1 Per-file deltas

- **`SCENARIO_FORMAT.md`** +33 — §1 gains `harness-plug` and the harness
  `plug:` form beside the reserved one (**one spelling, two grammars**; each
  way to mix them a named lint REFUSAL); §5 the dated reopen/close.
- **`engine.py`** +210 — routes on the payload's keys; lint enforces flag↔act
  both ways; `plan_act` → marker → `perform` (**guards before the marker**);
  the proof enters the bundle; `check_ulid_provenance`.
- **`test_engine.py`** A, 434 L — 13 checks + import, 2 of them named
  preservation checks; temp dirs only.
- **`harness.py`** +188 — `REFUSED = 2`; `window-seconds` in the same safety
  table; `EXIT_CODES` on every `--dry-run`; the door `plan_act`/`perform`/
  `guarded_act`; `seconds()` reads `36`/`36s` one way.
- **`test_harness.py`** +198 — 19 → 28: exit code from the README table;
  three `windowSeconds` arms, four door arms.
- **`constants.yaml`** +81 — `:283` year-month; `minted-by:`; `windowSeconds:
  300`; the `harness-plug` capability; the `provenance:` block.
- **`nightly_digest.py`** +111 — 21 → 30: the fleet formatter, the id-set
  split, `compose --fleet-*` all-or-none; pre-R-5 line byte-identical.
- **P-1 design doc** +12 (§4's FOLDED block) · **`runner/README.md`** +61 —
  the exit-code table the test reads + the 3→2 note; the `fleet:` field; the
  `foreign-card-ulid` refusal.

## §2 The gates

| gate | at `1201368` | after | fail |
|---|---|---|---|
| `test_harness.py` | 19 | **28** | 0 |
| `nightly_digest --selftest` | 21 | **30** | 0 |
| `test_engine.py` | — (RED 10/13) | **14** | 0 |

**RED-first.** Harness: 9 new checks failed at HEAD (`AttributeError:
'guarded_act'`; absent guard; absent table); 19 preserved green.
Engine: 10 of 13 failed, the 3 green being the preservation checks. Digest:
RED as a hard `NameError: 'fleet_text' is not defined` — its `check()` takes
computed values, so an absent pure function aborts the gate instead of
counting a failure. **One vacuous green caught while RED:** the grammar-mix
check passed at HEAD because `at:` alone tripped `_check_keys` and the
allowed-list printout contained the word `settle`; tightened to demand it
*name the mix*.

**§A6:** census = §A3 ✓ · gates green with counts ✓ · `^REFUSED = ` reads
`2` ✓ · `grep -c 'minted-by:'` = 3 ✓ · the foreign-card refusal has a test ✓ ·
a `--dry-run` plan over a `plug:` scenario printed 11 guard lines and 8 steps,
**every instant `t=+Ns`, zero wall clocks**, tally **0** ✓.
**Sweep:** `suite auto --list` 9/9 lawful, the two s31 legs untouched ·
`suite all --list` 11/11 · all six README-documented desk-demo PASS/FAIL
fixture pairs reproduce their verdicts · `empty-positives` REFUSES.

## §3 Pushback and observations

**A. P4 — §A1's ULID-audit line is wrong at HEAD.** It claims `grep -rn
'01KXW0156Z\|01M2DKJWVD'` over the bench = **0 hits outside records and
audits**. At `1201368` it returns **2**, both outside records and audits:
`fixtures/runner-demo/synthetic-boot-pass.txt:8` and `…-proposed.txt:8`, each
carrying `deviceId=01KXW0156Z1GJ3WCV2G516AKWS` in a `zigbee.device_relinked`
line — desk-demo input the runner reads. **Corrected line:** *2 hits, both in
`fixtures/runner-demo/`; the id is the BENCH card's own deviceId for the
SNZB-02P (R-4c §834), so nothing foreign sits in the tree. The conclusion
survives with a qualifier: `constants.yaml:115` is the one card-minted id
persisted as a CONSTANT; a second sits in two fixtures, asserted by nothing.*

**B. SD-A6's mechanism cannot work, and DP-1's read does not exist.** SD-A6
routes the check through an EUI64 from the registry's device list; both
halves fail. (i) **No device list:** at core `fed99e8` the frozen v1.1
surface is ten endpoints (health · entities +3 · commands/{id} · runs
+causal-chain · automations +non-firing) — none a device list — and
`grep -rl 'eui64|ieeeAddress|ieee_addr' api/` = **0 files**. (ii) **An EUI64
could not decide it anyway:** R-4c proved
`stableId=…0ae2dd7cecf8ef11b80168135c2a50c9…` byte-identical across two
sessions and four card swaps — one radio, never unseated. The EUI64 belongs
to the silicon, which is what the two cards SHARE, so the check reads
identical on both: **vacuous by construction**, the class the engine's own
doctrine bars. What differs per card is the id SET it reports (F-R4-2), so
count-and-ids over `/api/v1/entities` is taken — not a fallback but the only
sound instrument.

**C. DP-2 — `windowSeconds` in the plug's row, not a CLI flag.** A limit a
caller can widen from the command line is not a limit; the other two live
there, and a flip is a constants re-mint, not a code edit. `300` is
**PROVISIONAL and labelled so** — a ceiling against absurdity (a mis-typed
`at: 3600s`), not a measured envelope (worst case ~61 s). An absent key
reports **`absent (UNBOUNDED)`** and refuses nothing.

**D. A regression I caused, found by the sweep, fixed before exit.** My first
`constants.yaml` edit added a top-level `fleet:` block. The file **already
has one** at `:52` (`devices: 6`, `entities: 6`), PyYAML takes the last
duplicate key, and `boot-health` went `[REFUSED] unresolved
${C.fleet.devices}`. Fixed by using the existing key as the denominator, no
`expected:` synonym minted. **`suite --list` did not catch it**: `--list`
lints without resolving `${C.*}`, so only a dry-run did. A duplicate-key
sweep is now an exit check. *`--list` is not a constants gate.*

**E. Judgement calls, and scope not taken.** (i) The provenance check fails
**OPEN on the instrument, CLOSED on the finding**: an unperformed read is
noted `provenance: unverified`, never a refusal — a blip must not re-grade a
floor (§A5), and a foreign card answers its API fine, so only a *successful*
read arms it. (ii) **"First-class positive"** I read as evidence in the
BUNDLE, not a satisfied `positive:` line — making the harness satisfy a line
nobody wrote would fabricate one. (iii) `nightly.sh` is outside §A3:
nights read `fleet: unread` until a wrapper WU wires it.

**F. Two pre-existing defects.** (i) `test_harness.py`'s second invocation
spelling `unittest discover -s tools/harness -t .` **fails at `1201368`**,
`Start directory is not importable` — no `__init__.py` under `tools/`
(reproduced against HEAD copies); both now read
`-t tools/harness` / `-t tools/runner`: 27 and 13 tests OK. (ii) A `git
status` through the bridge **stranded `.git/index.lock`** — the hazard
`engine.py:58` documents. **Cleared**; later calls used
`--no-optional-locks` — worth making the standing lane idiom.

**G. For Part B.** SD-A4 bounds `--at` only; a cycle with `at` inside the
window but `at + off_for` past its close leaves the DUT dark. I implemented
the ruling as written and **report the restore instant in the guard line**
(`restore_at=+41s` beside `windowSeconds=300`) rather than silently widening
it. A one-line amendment if the hub wants it bounded.

## §4 WUCP Phase-1 checklist

- [x] PART A read whole before any edit; every §A1 cite re-read at `1201368`
      — 17 of 17 line cites accurate, the failures being premises (§3 A/B).
- [x] Write-set = the §A3 table only: no `scenarios/*.yaml` but
      `constants.yaml`; no `homesynapse-core/` edit.
- [x] Tests first, RED at HEAD each row, counts recorded; preservation checks
      named; one vacuous green caught and tightened. Fence: no live command
      anywhere; `--dry-run` + selftests only; tally 0.
- [x] Never `git add`/`commit`. HEAD `1201368` unchanged, staging empty,
      porcelain = the 9 paths. Register C; no product name.
- [x] P1–P4 adjudicated first (§0); DP-1/DP-2 taken with evidence; sweep run
      — the one regression found was mine, and fixed.
- [ ] **Hub:** `nightly.sh` fleet wiring · whether to bound `at + off_for`
      (§3 G) · the demo fixtures' card-minted id (§3 A).

RETURNED context/audits/2026-09-13_R-5A_return.md 10237
