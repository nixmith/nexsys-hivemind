<!--
file: context/audits/2026-09-13_v72-b2_BUS-ORDER-1_intake_two-layer-audit.md
purpose: The hub's two-layer intake of BUS-ORDER-1's return (the read-forward LIVE delivery, AMD-101's realization): the return read critically, the hub's own re-executions at the bytes, the rulings on the four [REVIEW] items and O1, the hub-owned misses, the landing order.
audience: Nick (§0, §3, §5) · the hub (every beat until the landing banks) · the next Coder lane on core/event-bus (§3 R3, §4)
state-type: intake audit
status: FILED Sun 2026-09-13 ~06:3x CT (instrument 2026-09-13T11:31:49Z); verdict ACCEPT; the core card handed
-->

# BUS-ORDER-1 — intake, two layers

## §0 The verdict
**ACCEPT.** The return (`context/audits/2026-09-12_BUS-ORDER-1_return.md`, 14,215 B, `RETURNED` last line) is repo-complete on `5f918c7`: 16 files (14 M + 2 A), nothing staged, no commit by the lane, `module-info` untouched. P1–P4 held with the reds forced; P5 (CI) is owed to the push. The four [REVIEW] items: R1, R2, R3 accepted on the evidence below; R4 is the hub's and becomes an AMD-101 §4 rider. O1 (the spent unpark permit) is accepted with its pin. The core card is handed; CI on the landing sha is the gate of record and its first `bus-soak` artifact is the closure counter's first sample.

## §1 Layer 1 — the return, read critically
The card leads with the census from porcelain, the baseline check (`72efb42..HEAD` one commit at 01:55Z), and the instrument (the 24-core desk, JDK 21.0.4, Gradle 8.8, `--offline`). P1: T1 forced the skip arm and at baseline delivered `[1, 3]` where `[1, 2, 3]` was due — P never delivered, the kind-E harm made visible. P2: T2 forced three reversed notifications at the flip and found the drain's gap detection (`TransitionCoordinator:117`, `position <= lastReplayedPosition → continue`) delivering only P+2 — a two-position silent drop the ruling had not named; T2b is green-by-construction and says so. P3: T3 timed out at two ticks with no notification. P4: the gate table and the K=200 soak (`ok=200 p50_ms=26 p99_ms=35 anomalies=0`, every scored subscriber `missed=0`, kind E absent, `delivered_max = checkpoint` for the all-filter subscribers). The five §2 corollaries each name their holder. Red-first is timestamped (compile-red 02:23Z, behavior-red 02:26Z, green 02:34Z, T5 red 02:59Z → green 03:01Z). The gate line's `--rerun` binding is disclosed and the per-task rerun is the recorded evidence. The return names its own limits (T2b, the runner's tick budget).

## §2 Layer 2 — the hub's re-executions at the bytes
1. **The tree.** `git status --porcelain` in core: 16 rows = 14 ` M` + 2 `??`; `diff --cached` empty; HEAD `5f918c7`; `git diff --stat -- '*module-info.java'` empty; `git diff --stat` = 14 files, +810/−530. Production changes only under `core/event-bus/src/main` (six files); the lifecycle changes are all under `src/test` plus its MODULE_CONTEXT.
2. **AMD-101 §2 against the tests.** `ReadForwardDeliveryTest` carries five `@Test` with `@DisplayName` T1 (:118, order and once under out-of-order notifications), T2 (:159, reversed appends during REPLAY's tail read), T2b (:194, an append between the drain's read and the CAS), T3 (:222, delivery within 2 × `liveIdleTick` with no notification), T5 (:243, a wake during the loop's own read); `EventBusConfigLiveFieldsTest` five `@Test`. Corollary 1 → T1/T2; 2 → T1/T2 (the checkpoint written after delivery); 3 → T3/T5; 4 → T2/T2b + `ReplayTransitionIT` (untouched, green); 5 → `DeliveryAnomalyEmissionTest` T1/T5/T6/T2/T-R2.
3. **The seams.** `EventBusConfig` has the components `int liveReadBatch` and `Duration liveIdleTick` (:70–71) with javadoc naming the page read and the bounded park; `InProcessEventBus` parks with `LockSupport.parkNanos(idleTickNanos)` (:576) behind the hint-queue check the comment at :604–610 describes (the queue is the durable signal, the permit the accelerator); `lastDelivered` appears twice (the census's `delivered_max=` source). `TRANSITION_READ_*` in `src/main`: three mentions in the `DeliveryAnomaly` enum and one javadoc line (`TransitionCoordinator:53`) — no emission site. `ReplayDriver`'s diff is javadoc plus two `runtime.advanceCursor(currentPosition)` calls: at activation (the durable floor) and per paged position. `EventBusConfig` has the 2-arg constructor at :133 (`replayQueueCapacity, publisherBlockedDepthThreshold`).
4. **The gate, from Nick's own run.** His `./gradlew check` (Sun ~06:1x CT) left `build/test-results/test/*.xml` with timestamps 2026-09-13T11:18:46 → 11:20:16Z: `core/event-bus` tests=240 failures=0 errors=0 skipped=30 (40 files) · `core/persistence` 413/0/0 · `lifecycle/lifecycle` 81/0/0 · `app/homesynapse-app` 30/0/0. The lane's four numbers, re-derived from a different run on the final bytes. His transcript: `clean.sh` + the four-module `--rerun` line BUILD SUCCESSFUL 1m 17s (75 tasks executed); the K=200 soak line BUILD SUCCESSFUL 14 s (2 executed); `check` BUILD SUCCESSFUL 38 s.
5. **The census claim vs the Files table.** The instruction's §2 said ≈8 M + 3 A; the tree says 14 M + 2 A. The difference is the lifecycle test call sites (`BusPositionCensusIT`, `BusAwaitDiagnostic`, `BusAwaitDiagnosticTest`, `BusSoakIT`, `HeroLoopHardwareFreeIT`), both MODULE_CONTEXTs and `ReplayDriver` (R1); no file outside the two modules. The census law held at porcelain.

**Not re-executed, disclosed:** the K=200 soak line itself — Nick's later `check` regenerated `lifecycle/lifecycle/build/test-results/` without the soak IT (it runs only under `-PincludeBusSoak`), so the desk holds no XML for his 14 s run; the lane's quoted line and his green build are the evidence, and CI's first `bus-soak` artifact prints the next one. The JDK 21 probe behind O1 (the mechanism is pinned by T5, which the gate ran). INV-GA-02's retention rule for minted kinds, read through the lane's citation.

## §3 Rulings
- **R1 — `ReplayDriver` +2 lines, outside the table: ACCEPT.** Corollary 4 needs the flip to hand a cursor that REPLAY advanced; without the two calls the drain would re-deliver every REPLAY-delivered position. The paging is untouched (the diff is javadoc and the two calls).
- **R2 — the public 2-arg `EventBusConfig` constructor: ACCEPT.** It supplies the AMD-101 defaults and keeps `EventBusConfigTest` byte-unchanged; equality with `HOME_DEFAULT` is pinned. `core/event-bus` is an internal module and the module descriptor is unchanged, so no new export follows. [INFO]: production constructs the full form; the convenience form is a test-facing seam.
- **R3 — `TRANSITION_READ_EMPTY`/`_EXHAUSTED` stop being emitted; T-R2 re-pinned; the coordinator loses its emitter parameter: ACCEPT.** The drain is a read-forward now, so the per-position read-and-retry it emitted from is gone; the kinds stay defined (INV-GA-02) — the enum still carries them and nothing in `src/main` emits them. §2's fifth corollary said "the LIVE path"; the drain's read-forward extends the same rule across the flip, which is the invariant's own words ("exactly once across the mode flip"). T-R2 now pins the honest SUSPEND on a throwing drain page read.
- **R4 — INV-BUS-01's mechanism clause (`governance/Architecture_Invariants_v1.md:1167`): the hub's.** The clause "MUST track `lastReplayedPosition` so that events delivered during catch-up are not re-delivered during drain" names the mechanism BUS-ORDER-1 replaced. Applied this beat as an AMD-101 §4 rider (the docs working tree); it executes at `AMD101: ratify` with the Doc 01 edit. An INV register edit takes the formal path; extending a PROPOSED amendment before its ratification is that path.
- **O1 — the spent unpark permit: ACCEPT.** The first K=200 soak read p99 553 ms with 49/200 loops delivered by the tick; the cause (a permit landing while the virtual thread is parked in its own `Future.get`) is a JDK fact the lane measured with a standalone probe; the fix (park only if the hint queue is still empty after the page) restored p99 35 ms; T5 pins it with a future-backed read executor and a 50 ms gap. The lesson is in `context/lessons/coder-lessons.md` (this card lands it).
- **[INFO] I1–I8: ratified.** I7 is a watch: T3's 400 ms budget (2 × a 200 ms tick) on the 2-vCPU runner — if CI flakes on T3, widen to 3 × tick in 114b's card, never loosen the assertion.

## §4 Hub-owned misses
1. The Files table under-counted by six M and over-counted by one A; the lane's census from porcelain is what the card stages. The next event-bus instruction lists the lifecycle call sites and the MODULE_CONTEXTs as rows, not as a sentence.
2. INV-BUS-01's clause was not caught when AMD-101 was authored; the register grep (`lastReplayedPosition`) belongs in the authoring sweep for any bus amendment.
3. The rest-api MODULE_CONTEXT phrase owed since v71 (`settledAt`'s "JSON null when" cell) stays owed; it rides 114b's landing (the same module), not this card.

## §5 The landing
The core card: `git add --` the 16 paths (14 M + 2 A), `staged: 16`, the trailer grep, `git commit -F ../_scratch/v72/2026-09-13_core_BUS-ORDER-1_commit-msg.txt`, push. Nick's lines: `CORE: LANDED <sha>`, then `CI: <sha> check <green|red> bus-soak <green|red>`. The Deferred Build Gate (`check` not run in-lane) is closed as evidence by Nick's desk run and closes as record when CI is green on the landing sha; OR-BUS-SILENT-DROP's closure counter restarts at that landing (twenty green samples, kind E 0, `delivered_max=` on every census line). The instruction flips EXECUTED at the landing intake. Nick's report (his transcript) is saved verbatim at `_scratch/v72/2026-09-13_v72-b2_nick-report_BUS-ORDER-1.md` and was read on the device, not pasted.

## §6 WUCP Phase 2 (this beat)
MODULE_CONTEXT: both updated by the lane (`core/event-bus`, `lifecycle/lifecycle`); the hub's review is the grep in §2.3. Deferred gate: recorded in OR-BUS-SILENT-DROP's status line with the closure condition. Drift: none (nothing outside the two modules). Check 9: unchanged (the known file). The next WU on the Java domain: 114b, after this landing.
