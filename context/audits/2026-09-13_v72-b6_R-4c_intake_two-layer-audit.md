<!--
file: context/audits/2026-09-13_v72-b6_R-4c_intake_two-layer-audit.md
purpose: The hub's two-layer intake of R-4c (Sun 2026-09-13 13:57–15:03Z on the held card, core a458a64's .deb): the navigator's adversarial brief answered point by point — the ten claims re-derived at the bytes, the §5 attacks run, the §6 systemic finding ruled, the §7 forward questions answered — and what was not banked.
audience: Nick (§0, §3, §5) · the hub (v72 b6 onward; v73's R-5 charter and the next rig packet) · the bench lane (P-1, TR-1b) · the coder lane (the ULID audit)
state-type: intake audit
status: FILED Sun 2026-09-13 ~10:3x CT (instrument 2026-09-13T15:34:09Z); the return: the record (98,455 B) + the source audit (60,472 B) + the brief (17,391 B)
-->

# R-4c — intake, two layers, against the navigator's own attack list

## §0 The verdict
**ACCEPT the return; C-003 MINTED with a bound; the ZDO surface LIVE-VERIFIED as a resolver; F-R4c-A MINTED as a finding; O-2 PROVISIONALLY CLOSED; the null arm NOT BANKED.** The fleet is complete on the held card (six of six in the adopt list) and becomes the soak's fixture. The design fence on B-2/B-3 lifts for design with F-R4c-A as a stated constraint; code stays fenced by OR-BUS-SILENT-DROP's counter. P-1's paste is the next act (the cap allows it: no lane is running); H8-a takes the next rig slot. The pre-LIVE ledger gate is adopted.

## §1 Layer 1 — the return, read critically
The navigator's record leads with a verdict surface, quotes the chain with timestamps (F-13's fix made that writable), separates the two LASTREPORTED-1b arms and refuses to bank the null arm twice over (D-9, D-10), files eleven deviations the same minute each and writes an attack list against its own work. Two gradings it caught in-session would have banked wrong conclusions from a literal reading of the packet's EXPECTED (D-9, D-10); one it caught pre-flight would have installed a nine-day-old artifact through a block whose assertions all passed (D-2). The record names its own substitutions (the auth-gated grammar line replaced by a four-surface custody chain, D-1/D-3) and its own timestamps' offset (EDT on the card; Z in the prose). Its weakest prose is in the causal language around C-003 and in "6.8× idle load"; its own §5 says so.

## §2 Layer 2 — the hub's re-executions at the bytes
1. **The artifact (§3.A).** Read independently from the public run page: install-smoke run **#53**, commit `a458a64`, both jobs Success, the arm64 artifact digest `883c5f8d…6468` (and amd64 `9be1d076…5e97`, which the record also carries once). In the record: the same 64-hex string appears four times and `1f46c5c8…eed1` eight times, character-identical; the §1 attempt-2 ⏺ chooses the newest zip, hashes it (≡ GitHub's digest), expands it into a freshly made directory in the same shell sequence and hashes the one `*_arm64.deb` it finds; B1's on-card `sha256sum` prints the same hex. The chain 1≡2, 2→3 in one block, 3≡4 binds the installed bytes to run #53. What it does not bind: that GitHub's digest is of the bytes CI built — that is CI's own custody, outside this session.
2. **The C-003 chain (§3.B).** From the harvest ⏺ (11 of 11 lines shown, no truncation): `zigbee.ieee_addr_rsp: nwk=0x15ac device=0xF044D3FFFED2A201` ×1; `zigbee.device_adopted: device=0xF044D3FFFED2A201` ×1; `network_formed`, `rejoin_candidate_unresolved`, `proposal_incomplete`, `ieee_addr_rsp_failed` ×0 each; the census `0xF044D3FFFED2A201 adopted_this_invocation=1`, five others 0, six `in_adopt_list=1`. Also in the chain: the SNZB-01P (`nwk=0xa5da`, `0xF044D3FFFE1C1E8E`) resolved at 265.036 ms and never proposed.
3. **The window (§3.C).** `permit_join_opened` at 10:41:46.727636 EDT = 14:41:46.727Z; +254 s closes at 14:46:00.727Z; `device_adopted` at 10:43:48.783944 EDT = 14:43:48.783Z → +122.06 s, inside. The countdown's printed column is not used (F-12).
4. **The two reads (§3.D).** Read 1: 3 rows, `null: 0`, three instants. Read 2: 4 rows; the new entity's line reads `lastReported None -> 2026-09-13T14:50:28.702945Z` — the "after" value is the first instant, so read 2 ran after 14:50:28Z, after the 399.9 s null window closed; the `None` is the diff's default for a row absent from read 1. The navigator's refusal stands on the bytes.
5. **The restore (§3.E).** `[PASS] boot-health — 6/6 positive · 0 forbidden`; `PAN 0x774c`; the bundle stamped 150256Z; the stop graded `Result=success · ActiveState=inactive · ExecMainStatus=143`.
6. **The source claims (§5.8).** `ZigbeeIntegrationAdapter.java:1152` is `log.debug("zigbee.adopt_list_loaded: entries={}", …)`; `app/homesynapse-app/src/main/resources/logback.xml` has `<root level="INFO">` and no `<logger` override — F-1 is right and the packet's EXPECTED `1` was wrong on these bytes. D-7's arithmetic: 228 − 219 = 9 rows over ~14.5 min = 0.62 rows/min against R-4b's 0.643 — the gate's own words ("zero row loss · integrity ok") are what it tests.
7. **The four modified blocks (§5.7).** The ⏺s carry outputs, not the modified commands, so the diff is against the source audit's F-6/F-12/F-13 texts and the outputs: the harvest prints "chain lines total: 11 (display capped at 60) [no truncation]" and ISO timestamps with offset — the described additive changes, and none of the grep patterns, the census loop or the assertions changed. The B0 `dpkg-query` quoting fix (D-5) and B2's subshell (F-6) are instrument-only by their nature. Disclosed: the exact pre/post command strings were not re-diffed at the bytes.
8. **The adapter, for §5.1 and §5.2.** `onUnknownSender…` ends in `admitRejoinCandidate(resolved.get(), admittedAddress, "unknown_sender")` (:1300); the F-R4-1 H-i hook `onRejoinCandidate` ends in `admitRejoinCandidate(device, networkAddress, "tc_join")` (:1315) when the window is open; `admitRejoinCandidate` is the same two calls for both — `cache.recordAnnounce` and `interviewQueue.schedule(…, REJOIN)`, the queue keyed by IEEE (a later announce is a put-replace). So the `tc_join` at 10:43:47.681 re-scheduled the same interview the ZDO resolution had scheduled 72.43 s earlier; the proposal followed the second schedule in 1.04 s. R-4b's record carries no `tc_join` line at all.

**Not re-executed, disclosed:** the on-card reads (the rig is Nick's hands; the ⏺s are the evidence); the pre/post command diff of the four modified blocks; the counterfactual for C-003; the Hue's mains state; GitHub's CI-side custody of the digest.

## §3 The ten claims — rulings, with the attack that was run
1. **C-003 — MINTED, BOUNDED.** What the chain shows: the device that defeated R-4b (`nwk=0x15ac status=0x1`, byte-identical miss) was identified on the air in 516 ms, announced a Trust Center join 72 s later, was interviewed and adopted 1.1 s after that, 122 s into the window; the fleet is six of six. What it does not show: that the ZDO resolution was necessary. The source read makes the hostile reading concrete — the `tc_join` path admits and schedules by itself, and the proposal followed it, not the resolution. The exit criterion "the sleepy SNZB-02P adopted" is met; the phrase "by the ZDO surface" is bounded to "identified by the ZDO surface; adopted after its own tc_join". **The counterfactual is R-5's first window:** provoke a device that tc_joins without a preceding unknown-sender frame, or run one window with the ZDO arm disabled by config; either result is a one-line finding.
2. **F-R4-1b LIVE-VERIFIED (as a resolver) — MINTED.** Two independent on-air resolutions, 516 and 265 ms, against a 10 s deadline. F-R4b-F's boundary is retired for resolution; adoption is claim 3's business.
3. **F-R4c-A — the finding MINTED, the explanation PROVISIONAL.** In the bytes: resolved, admitted, no proposal, no error. At source: identical admission for both sources, so the difference is downstream of admission — consistent with the interview needing an awake device, which a tc_join signals and a passive resolution does not. Against it: the operator pressed most devices repeatedly, so the two devices' provocations are not known to differ in kind (§5.2). The cheap test: a battery pull-and-reinsert on the 01P inside an open window. Until then: a design assumption is not made on it; a design constraint is (R-5 and the rehearsals must not equate "seen on the air" with "onboarded").
4. **O-2 — PROVISIONALLY CLOSED.** One clean stop grading `success` on `a458a64` against two `exit-code/failed` on two prior artifacts. One reading against two; the next rig restore is a free second reading and closes it.
5. **LASTREPORTED-1b instant arm — MINTED.** A 2026-09-04 instant un-refreshed across nine days, a power-cycle and a version upgrade beside `UNAVAILABLE`; the SNZB-04P driven `UNAVAILABLE`→`AVAILABLE` with its instant moving in-window (+200.4 s). Nothing moved instant→null.
6. **LASTREPORTED-1b null arm — NOT BANKED.** Checked hard: read 2 ran after the first instant; no ⏺ carries the new entity's API value inside the 399.9 s window; the `None` is the script's. The corroboration stands as corroboration: the first instant is 400 s after registration, neither the adoption time nor the boot time — adoption fabricated nothing.
7. **`bus.delivery_anomaly` 0 — BOUNDED, weakly.** 4.21 rows/min is a rig at rest with one adoption; the soak drives thousands. OR-BUS-SILENT-DROP's row records "a rig-load zero on `a458a64`", not a sample; the counter stays 1/20.
8. **The Hue — RECORDED.** Fourth silence, first with the door open and a second resolution surface live; the power event uninstrumented (§5.5). P-1's live leg has its canonical case.
9. **F-R4-2 — MINTED as an observation.** Two registries mint two ULIDs for one IEEE. The audit it implies (below, §7.4) is assigned.
10. **The root cause — BOUNDED at four of eleven.** D-5, D-11 (verbatim inheritances of defects R-4b's ask #5 named), D-9 and D-8 (contradicted by R-4b's own §0 and restore note) are the un-re-derived record's cost; D-2 and D-6 are authoring defects of the same family. The recommendation stands unchanged.

## §4 The systemic finding (§6) — ruled
**THE PRIOR-LEDGER GATE, adopted.** A packet cut from a prior record goes LIVE only after its author greps that record's deviations ledger (the D-table) and its findings card's asks for every command string and every named witness the packet reuses, and either fixes each hit in the packet or carries it as a named deviation with the reason. Minted in pm-lessons this beat; it becomes a numbered law in `coding-instruction-format.md` at W-SKILLS-9 (the packet and navigator forms are authored under that file) and a line in the WUCP Phase-2 checklist for rig WUs. **Why ask #5 produced no correction:** the R-4c packet was authored at v70 beat 3 "by range from the R-4b record" — its blocks were re-used, its ledger and asks were not read; a return's asks were banked in the spine as a count ("ten asks") and had no artifact the next author was required to open. The gate makes the ledger and the asks that artifact. The same mechanism is why D-8 (R-4b's charger note) did not reach B3: notes in a record's prose are not a checklist until the gate reads them.

## §5 The forward questions (§7)
1. **The B-2/B-3 fence.** C-003 is met in the sense the plan needs: the fleet is complete and is the soak's fixture. The design fence LIFTS with one constraint carried from F-R4c-A (a device identified on the air is not thereby onboarded; onboarding needs the device's own join or announce). The code fence stays until OR-BUS-SILENT-DROP's counter closes (the plan's second condition).
2. **R-5 and the rehearsals** must not assume resolution ⇒ onboarding, and must count "device re-seen" and "device adopted" as two different numbers after every kill, disconnect and power-cycle. R-5's fixture is the six devices as adopted today; re-adoption after a reset is measured, never assumed.
3. **The null-arm instrument.** A read triggered by `device_adopted`: on that line, GET `/api/v1/entities` and print the new deviceId's `lastReported` raw (expected `null`) within 60 s; after its first `state_reported`, GET again and print the instant. It is one rig block (~20 lines), authored by the hub into the next rig packet (H8-a's re-cut or R-5's) and run by the operator; TR-1b's driver may carry it later as a verb. The read-2 diff script prints raw API values for new rows from now on (D-10's fix).
4. **The ULID collision.** Anything that persists a `deviceId` or `entityId` outside the registry that minted it is unsafe across cards: the bench's `constants.yaml` (`command.s31-entity`), the hero's fixtures and mocks, any docs exhibit used as a fixture, the H8-a packet's expected ids. The audit is a grep task before R-5; assigned to the bench lane (P-1's return carries the first grep; the hub re-derives it at intake).
5. **P-1** is chartered (09-11) and its dispatch condition is met (R-4c returned; no lane running). Its paste is the one act this beat. The Hue's fourth silence is the live leg's first target after R-5 or `HARNESS-PLUG:`.
6. **`card-gradle: absent`** confirms TR-1b's shape: cross-build on the desk or CI and ship the verb; nothing builds on the card.
7. **v73's window and the critical path.** R-4c's record shortens v73's authoring: the R-5 charter (the counterfactual window, the null-arm block, the re-seen/adopted split, the ULID audit's result) is v73's first authored artifact after its two hand-offs; H8-a's rig slot is the next rig act; the pre-LIVE gate applies to every packet v73 cuts. The deliverable of v73 stays: the two lanes landed and R-5 chartered on a complete fleet.

## §6 What was not banked
The null arm (no observation inside the window); O-2 (one reading); the ZDO surface's causal role in the adoption (no counterfactual); the Hue's mains event (uninstrumented); "6.8×" as a bound on the soak class; the explanation of F-R4c-A (provocation not controlled). Each has its instrument named above.
