<!--
file: context/audits/2026-09-13_v73-b3_EXPLAIN-114b_intake_two-layer-audit.md
purpose: The hub's two-layer intake of EXPLAIN-114b's return (context/audits/2026-09-13_EXPLAIN-114b_return.md, 9,978 B): the lane's claims read critically, the hub's own re-execution at the bytes (porcelain, the three instruments, the five diffs read whole, the T1–T3 assertions, the fixture's defaults against every migrated site), the [INFO]s ruled, the findings that go to the improvement register, and the landing (Nick's card; CI the gate of record).
audience: Nick (§0, §4, §6) · the hub (WUCP Phase 2; the 114c docket) · the Coder (the register rows it will meet)
state-type: audit (filed once)
status: FILED — v73 beat 3, Sun 2026-09-13 ~15:1x CT (instrument 2026-09-13T20:17:34Z)
-->

# EXPLAIN-114b — intake, two layers

## §0 Verdict
**ACCEPT.** The refactor lands what the instruction ordered, under the v1.1.4 freeze, with the wire provably unchanged: the census is the seven Files-table paths exactly (6 M + 1 A); `readByType(` 5 → 1; `public NonFiringExplanation(` 3 → 0; zero assertion lines changed outside the automation module's three new tests; the gate line green in one round on the lane's desk (automation 232 = 229 + 3 · rest-api 156, 3 skipped · lifecycle `compileJava` · `spotlessCheck`). No `[REVIEW]`; five `[INFO]`s, all accepted (§3). Three findings, none blocking, go to the improvement register (§4). The landing is Nick's card (§6); `./gradlew check` on his push is the gate of record and its `bus-soak` sample advances the closure counter to 2/20 on green.

## §1 Layer one — the claims, read critically
The §0 card adjudicates P1–P4 first and sorts the tests per format law #18 honestly: T1–T3 are declared green-by-construction (run at HEAD with production untouched, 19:39:31Z, 37 → 40), and T4's red was SEEN (the six short-form sites failing after the deletion with the fixture in place — the deletion's red alone, the fixture-then-delete order the lane chose over the dispatch line's order, disclosed as I5). P4 is honest in the other direction: HEAD was the pinned sha so no cite moved, but three of the instruction's cites were imprecise and the corrected lines are filed (the class javadoc `:44–:92`, the compact constructor `:108–:112`, the test's FIXED clock at `:7–:8`/`:76`) — the hub's imprecision, owned. DP-1 was taken from the source (`InMemoryEventStore.readByType` honours `maxCount`; `hasMore = totalMatching > maxCount`). The §3 observation about the 114a Tests sentence in the rest-api MODULE_CONTEXT ("the 8-arg and 5-arg conveniences keep the older fixtures compiling") being now historical for the `NonFiringExplanation` half is correct and correctly left as a dated record. The transcript Nick pasted (≈50 KB) carried nothing the §0 card did not; THE OPERATOR-LOAD LAW (iv) stands — a transcript goes to `_scratch/`.

## §2 Layer two — the hub's re-execution at the bytes (device, 2026-09-13T20:17:34Z)
1. Core porcelain: exactly the seven paths (` M` ×6, `??` ×1); index empty; HEAD `a458a64`; nothing under `module-info.java`, `*.gradle.kts`, `web-ui/`.
2. The instruments re-run: `grep -c 'eventStore.readByType('` = 1 · `grep -c 'public NonFiringExplanation('` = 0 · `git diff -U0 -- 'api/rest-api/src/test/*Test.java' | grep -E '^[-+]\s*(assert|then|verify)' | wc -l` = 0 (11 over all tests = T1–T3's own lines) · the flagged `settledAt` phrase = 0.
3. `StandardExplanationService.java` diff read whole (+108/−96): `scanType` is the one loop (`SCAN_BATCH` pages from 0; the visitor's `false` ends the walk; `hasMore` ends it otherwise); `listRuns` keeps the `before` filter, the automation filter and the `cap + 1` window verbatim; `latestTerminalRun` and `latestDisabled` are `lastMatching` walks with their match rules verbatim (the unrecognized-status warn kept); `latestRunByAutomation` keeps its map put; `locateTriggered` returns `false` on its match — the early stop preserved. `Slot` is a per-call cell. `java.util.function.Predicate` is `java.base`; module-info untouched.
4. `NonFiringExplanation.java` diff read whole (−43/+6): the three convenience constructors and their javadocs removed; the record header, the compact constructor and its five null checks byte-unchanged.
5. `NonFiringExplanations.java` read whole (115 lines): `of(8)` defaults the five tail components to `null` — exactly what the deleted 8/9/10-arg forms passed; four withers set one fact each; `build()` calls the canonical constructor. `AutomationEndpointsTest.java` diff read whole (+25/−20): each of the 8 sites carries its former literal values into `of(...)` and the withers — `Boolean.TRUE` → `withNoCommandsIssued`, the `SubjectRefView` → `withTriggerRef`, `Instant.parse("2026-01-01T00:00:11Z"), "repeated_failure"` → `withDisabledFacts`, `DEFINITION_KEY` → `withDefinitionKey`; the two formerly canonical 13-arg sites carry the same values. No default differs from what a site passed.
6. T1–T3 read at their assertions: T1 seeds 501 `automation_completed` for one automation and asserts the newest five lead the page with `hasMore` true and `reads(AUTOMATION_COMPLETED) == 2`; T2 seeds 500 markers of another automation then the target's and asserts `DISABLED` with that marker's instant and reason and `reads(AUTOMATION_DISABLED) == 2`; T3 seeds the run first then 500 triggered markers and asserts `explainRun` present with `reads(AUTOMATION_TRIGGERED) == 1` and `lastMaxCount() == 500` — the test-local `SCAN_BATCH` is pinned to the production constant. The `CountingEventStore` decorator delegates every read and reads no clock.
7. The two MODULE_CONTEXT diffs read: the rest-api B3 row's null cell now reads "a bare or acknowledged `DISPATCHED` (…a superseded one carries the superseding result's instant — R3, `5f918c7`)"; the fixture paragraph names `NonFiringExplanations`; the automation status note states the one scan, the one constructor and the boundary tests.
8. **Not re-executed here, disclosed:** the Gradle gate — the device VM cannot install the wrapper distribution offline (`./gradlew --offline --version` fails at `Install.createDist`); the lane's desk run (19:43:42–:47Z, fresh XML at 19:43:45–:46Z) is the evidence, and CI on Nick's push is the gate of record.

## §3 The [INFO]s, ruled
I1 (the `with…` fixture style) ACCEPT — a 114c component becomes a field and a wither, never a constructor on the record. I2 (`Slot` over a one-element array) ACCEPT. I3 (one assertion each beyond §7's letter: the page counts; `lastMaxCount == SCAN_BATCH`) ACCEPT — the stronger pins. I4 (the 3-arg `definition` delegating to a 4-arg `enabled` form) ACCEPT. I5 (the fixture-then-delete order to make T4's red the deletion's alone) ACCEPT — the better order; the dispatch line's order would have shown no red.

## §4 Findings — the review Nick asked for (none blocking; filed in `context/planning/improvement-register.md`)
- **IR-1 `locateTriggered` scans from position 0.** The early stop is at the FIRST match in ascending order, so the newest run — the common "why did it just fire?" ask — walks nearly the whole retained `automation_triggered` index (`SCAN_BATCH` pages of it). The by-id read on 114c's docket (K2) is the fix; the register asks for the measurement first (a seeded index of realistic size, the page count read by T3's instrument).
- **IR-2 `listRuns` can stop at its cursor.** Positions ascend, so once `e.globalPosition() >= before` nothing later can be below the cursor; the visitor returns `true` today (behaviour preserved as ordered) where `false` would make a back-page request `O(pages below the cursor)`. One line, one T1-style test (`reads == 1` for a cursor inside page 1); a 114c row.
- **IR-3 one `Predicate<EventEnvelope>`, two meanings.** `scanType`'s predicate means continue/stop; `lastMatching`'s means matches/does not. Adjacent private methods with the same type and opposite readings invite a wrong pass; a named functional interface (`Visitor.visit` → `CONTINUE | STOP`) or an enum return removes the trap. A 114c row.
- Also noted, lower: the fixture's withers repeat 13 arguments each (a mutable builder halves it when 114c adds a component); `RunExplanation`/`AutomationSummary` still carry convenience constructors (the same collapse when next touched); the endpoints' 27 hand-sized `LinkedHashMap`s (114a's growth point 4).

## §5 What changes in the record
- 114b is DELIVERED pending Nick's card; the closure counter advances on the `bus-soak` sample of his push (kind E 0 = one more green toward 20).
- 114c's docket: the v1.1.5 keys (`parentRunId`, condition text, the by-id read, `forDuration` `firingValue`) · D4's DP-B2 sentence (`StandardExplanationService` "last fired and confirmed at …" — `FIRED_CONFIRMED` evidence or drop the claim; the HERO-1b audit's Core item, missed from 114b's OUT list and owned here) · IR-2 · IR-3 — after the freeze note (the docs' version catalog, a hub card) and HERO-1c's needs.
- DISABLED-2 stays on the formal path (D-v73-1). The coder-handoff entry is filed by this beat; the instruction's status line goes EXECUTED at the landing.

## §6 The landing (Nick's hands)
The core card in `_scratch/v73/2026-09-13_v73-b3_operator-packet.md` block 1: `git add --` the seven explicit paths, `staged: 7`, the trailer grep `0`, `git commit -F ../_scratch/v73/2026-09-13_core_EXPLAIN-114b_commit-msg.txt`, push; say back `CORE: LANDED <sha>`, then `CI: <sha> check <green|red> bus-soak <green|red>` when the run finishes; a red `bus-soak` → the artifact unzipped to `_scratch/v73/ci-<sha>/` and `SAMPLE: <sha>`.
