<!--
file: context/audits/2026-09-13_v73-b5_R-5A_and_HERO-1c_intake_two-layer-audit.md
purpose: One combined two-layer intake of the two lane returns of v73 beat 4 — R-5 Part A (context/audits/2026-09-13_R-5A_return.md, 10,237 B) and HERO-1c (context/audits/2026-09-13_HERO-1c_return.md, 12,134 B): the claims read critically, the hub's re-executions at the bytes, the pushback ruled, what is corrected before either landing, what goes to the register, the landings.
audience: Nick (§0, §3, §5) · the hub (WUCP Phase 2 for both) · the two follow-up lanes (§3 names their rows)
state-type: audit (filed once)
status: FILED — v73 beat 5, Sun 2026-09-13 ~17:2x CT (instrument 2026-09-13T22:23:28Z)
-->

# R-5 Part A and HERO-1c — intake, two layers each

## §0 Verdicts
**R-5 Part A — ACCEPT.** 9 = 8 M + 1 A, the Files table exactly; the three gates re-run on the device: `test_harness.py` 28/0 · `nightly_digest --selftest` 30/0 · `test_engine.py` 14/0; the chokepoint tally 0; `REFUSED = 2`; three `minted-by:` provenance comments; one top-level `fleet:` key. Two of the hub's premises were wrong and the lane said so (§1). Lands on ONE bench card together with the small follow-up R-5A-ii (§3), so the nightly's first `fleet:` reading is a value, not `unread`.
**HERO-1c — ACCEPT WITH ONE CORRECTION BEFORE THE LANDING (D3) and two §7 rows (D2).** 18 M + 3 A, all under `web-ui/dashboard`; `npm run verify` exit 0 on the desk (24 files, 435 tests; 70.0 of 100 KB); the SPEC rows `:276`–`:279` byte-identical to the charter block, `## §8` at `:281`; "We sent it" 0; `outcomeMeta` 0; the mode line keyed at 34 sites. The row C5 as the hub wrote it put a hero sentence ("Loading this run…") on every page's spinner — the lane saw it and asked; it is corrected before the push (§3), not after, because a known-wrong sentence on `main` is the debt the register exists to prevent.

## §1 Layer one — the claims, read critically
- R-5A adjudicated P1–P4 first; P4 landed on the hub's own ULID-audit line (the grep returns two hits in `fixtures/runner-demo/` — the bench card's own id; the conclusion survives qualified). Two further hub premises failed at HEAD and the lane refused to build a vacuous check: SD-A6's EUI64 belongs to the coordinator dongle (identical across cards per R-4c) and the frozen v1.1 surface exposes no device list and no per-device EUI64 — so the foreign-card refusal is grounded on the id SET, which is what actually differs per card (DP-1 taken as the fallback the charter named, for a stronger reason than the charter gave). A self-inflicted regression (a duplicate top-level `fleet:` key; PyYAML keeps the last; `boot-health` went REFUSED) was caught by the lane's own sweep and fixed, with the finding that `suite --list` lints without resolving `${C.*}` — not a constants gate. A `.git/index.lock` stranded by a bridge `git status` (the hazard `engine.py:58` documents) was cleared; `--no-optional-locks` thereafter. Two items left for the hub: `tools/nightly.sh`'s fleet wiring (outside the write-set) and whether SD-A4 bounds `at + off_for`.
- HERO-1c adjudicated P1–P5 first: P1 MISSED honestly (four files beyond the table, each tagged — three spinner pins and `e5-semantics.test.ts`, which the hub's `**/*.tsx` grep did not see); every row's red seen (C1 eleven red of fourteen); one flipped assertion named with old and new text; D1–D6 filed with line cites; D5 names the next WU (HERO-1d) and the instrument's blind spot (JSX text nodes and template literals escape the §6.3 grep).

## §2 Layer two — the hub's re-executions at the bytes (device, 2026-09-13T22:23:28Z)
1. Bench porcelain: exactly the nine paths (8 ` M` + 1 `??`); index empty; HEAD `1201368`. The three gates run on the device: 28/0 · 30/0 · 14/0 (the harness fence line "zero network calls" printed twice). `grep -n '^REFUSED = ' harness.py` = `58:REFUSED = 2`; `grep -c 'minted-by:' constants.yaml` = 3; `grep -c '^fleet:'` = 1; `windowSeconds` in the constants block and the harness.
2. Core porcelain: 21 rows, every one under `web-ui/dashboard/` (18 M + 3 A); index empty; HEAD `fed99e8`; no `.git/*.lock`. SPEC `:276`–`:279` are the four C0 keys in order; `## §8 Accessibility` at `:281`. `git grep -c "We sent it" -- web-ui/dashboard/src` = nothing; `grep -rn outcomeMeta src` = 0; `explain.mode.` referenced at 34 non-test, non-catalog sites; `actionVerdict` keeps two `help:` literals — the skipped mode's `''` (by design) and `notRecorded`'s sentence (no §7 row exists — D2, corrected below). The C1 mode→key map and `actionStepLine(mode, a, target)` read in the diff: `{Target, target, verb, verbPast, reasonClause}` filled at the resolved label; the skipped pair keyed on `targetRef`.
3. D3 confirmed at source: `feedback.tsx:17` `Loading({ label = t('explain.loading') })` is mounted by `Resource.tsx:17` on every view (13 mounts: Automations, Devices, Events, Health ×2, Overview ×3, Runs, and the three hero views) and by `DevicesView.tsx:134` directly.
4. **Not re-executed, disclosed:** `npm run verify` (no `node_modules` in the device VM); `frontend.yml` on the push is the gate of record.

## §3 Rulings
- **R-5A:** SD-A6-as-built ACCEPT (the id set; the charter's EUI64 mechanism was the hub's error — owned). **SD-A4 gains the cycle-end bound:** a cycle whose `at + off_for` falls outside `windowSeconds` is REFUSED (a cycle that starts inside and ends outside spends the budget outside the window). **`nightly.sh` wiring:** lands with Part A on one bench card, fail-safe (`fleet: unread` on any read failure, never a red) — additive to the digest line, so the s31/nightly HANDS-OFF's intent (no re-grade before the re-baseline) is kept; the first nightly after the card is the pre-baseline `fleet:` datum. **`suite --list` constants lint:** resolve `${C.*}` and refuse a duplicate top-level key (the class the lane hit). The three are one small follow-up, **R-5A-ii** (`context/instructions/2026-09-13_bench-lane_R-5A-ii_nightly-fleet-wiring_cycle-end-bound_constants-lint.md`), to run now; then the bench card lands both.
- **HERO-1c:** D1 ACCEPT (three test callers, the hub's grep scope — owned). **D2 — two §7 rows, the hub's text:** `explain.mode.notRecorded.help` "What happened to this step was not recorded. The step itself is preserved." and `explain.mode.settledFailed.lineNoCommand` "No command was sent to {target} — this step failed{reasonClause}." (FAILED with `command` null and a named target; the `act` arm reads as the SPEC's null verb and is wrong copy). **D3 — (a):** the feedback primitives default to a generic app pair (`ui.loading` "Loading…" · `ui.error.title` "This page couldn't be loaded." · `ui.error.body` "The hub answered with an error. Try again." · `ui.error.retry` "Try again"); `Resource` takes optional labels; the three hero views pass the `explain.*` rows; offline/replaying stay generic as written. Not a SPEC §7 row — §7 is the hero's table; the `ui.*` pair is the app's (the MODULE_CONTEXT hero section says so). D2 + D3 = **the AUDIT CORRECTION** (`context/instructions/2026-09-13_HERO-1c_AUDIT-CORRECTION_D3-generic-labels_D2-two-rows.md`) — run before the push. D4 ACCEPT (added keys, zero-visible). **D5 → HERO-1d** (the existing keys wired first, then one §7 amendment for the keyless set; the `pendingHint` "Color"/"Colour" dialect fixed with the corpus swept — law #21); the §6.3 blindness → a lint (register IR-8). **D6:** `noRuns` stands — the list's null means "no retained terminal run", not "none since load"; `noRunsSinceLoad` waits for a since-load fact on the wire. The held-DISPATCHED help collapse ACCEPT. P1's four extra files ACCEPT as declared.

## §4 What goes to the register
IR-8 the hero-literal lint (an ESLint rule or a script over the hero files that flags JSX text nodes and template literals containing a sentence — the instrument §6.3 could not be) · IR-9 the constants lint in `suite --list` lands in R-5A-ii (retired on its landing).

## §5 The landings (Nick's hands, after the two follow-ups return)
The web-ui card: `git add -A web-ui/dashboard` (HERO-1c + the correction; the census re-derived at the intake) → `frontend` CI. The bench card: explicit paths (Part A's nine + R-5A-ii's) → `BENCH: LANDED <sha>`. Neither is staged before its follow-up's audit.
