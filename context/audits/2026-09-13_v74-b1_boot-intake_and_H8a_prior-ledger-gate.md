<!--
file: context/audits/2026-09-13_v74-b1_boot-intake_and_H8a_prior-ledger-gate.md
purpose: The v74 beat-1 record — Nick's dispatch text VERBATIM (§1), the boot and the preflight at the instrument (§2), THE PRIOR-LEDGER GATE run on the H8-a packet with every grep and its disposition (§3), the disclosed non-re-executions (§4), the one re-order of the window on Nick's word (§5). The packet's re-cut is the diff at `context/instructions/2026-09-06_H8a_real-wire_v113-keys_and_failchan-proof_navigator-packet.md` (30,119 → 34,997 B; md5 338fae0b… → 314fe411…).
audience: the hub (the gate's evidence; the next H8-a author) · Nick (what the re-cut changed and why)
state-type: intake + gate audit (FILED at v74 beat 1)
status: FILED — Sun 2026-09-13 ~19:1x CT (instrument 2026-09-14T00:20:13Z)
last-verified: 2026-09-13 (v74 beat 1)
-->

# v74 beat 1 — the boot intake and THE PRIOR-LEDGER GATE on H8-a

## §0 Card
- **Boot:** `date -u` 2026-09-13T23:56:42Z; 41,959 B read in eight ranges (the v67 prompt 13,017 · the chain 2,151 · the v73 b7 beat 2,471 · the snapshot 3,453 · the brief 11,559 · the v66 DR §3 1,969 · the plan §3 1,955 · the plan §8 5,384); pm-lessons' eleven mints since 09-06 held as headings (bodies read only when a beat needs one — THE PRIOR-LEDGER GATE's was read for §3).
- **HEADs (one call):** core `93390f0` · hivemind `98e2f39` · skills `c630c5c` · bench `f3631cb` · docs `7221ddc`; porcelain 0 ×5; `origin/main..HEAD` 0 ×5; no `.git/*.lock`; no `??` return file. Every HEAD equals Nick's dispatch line. `CI: 93390f0 all checks green` was banked at v73 b7 (3/20) — not re-banked.
- **Preflight:** PASS with Check 9 STALE 27/28 (`project-manager/SKILL.md`: SOURCE `bda340b1…` vs the synced `76bf4ce6…` — the same one file as v73's boot; Nick's sync clears it). Detail §2.
- **The gate:** 18 greps over the packet against R-4c's §9 (D-1…D-11), its card's asks #5/#6 and items 7/8/10/11; four hits fixed, two already fixed on 09-06, one added (`S31-LABEL:`), the pin re-derived. Detail §3.
- **The pin:** `install-smoke` runs on every `main` push (`install-smoke.yml:2–7`; the `paths:` filter belongs to `pull_request`) → HERO-1c's push built run #55, id 34788417946, event push, created 2026-09-13T23:00:24Z, conclusion success; the run page read anonymously: amd64 green · arm64 green · `distribution-artifacts-arm64` 123 MB, published digest `sha256:78ee52fb6aaf8a572fc5f2fb9bcc989ecd41815525ea4cd9cf37e2a9c3a6f76f`. The .deb of record: `homesynapse_0.1.0+git20260913.230020.g93390f0_arm64.deb` (93390f0's committer date 2026-09-13T18:00:20-05:00 = 23:00:20Z; `distribution/common.sh:84–87`).
- **The re-order (§5):** W-SKILLS-9 pasted at b1 (the plan §8 had it at b5) on Nick's dispatch word; the lane RETURNED inside the beat (`context/audits/2026-09-13_W-SKILLS-9_return.md`, 8853 B; 10 M + 1 A in the hivemind's porcelain at splice time); no b1 card stages its rows — the intake and its two cards are b2's.

## §1 Nick's dispatch text, verbatim
You are the v74 PM MISSION-CONTROL hub for NexSys / HomeSynapse. Boot from `nexsys-hivemind/context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` (the STABLE form — no state in it) and execute its §1 EXACTLY, inside its boot byte budget: `date -u` first; `pm-handoff.md` line 8 + the newest ONE beat; `PROJECT_SNAPSHOT.md`; `context/handoff/OPERATOR-BRIEF_for-Nick.md` whole — its §HELD-BY-THE-HUB is the wait-state ledger; the decision record §3; the plan of record §3; the five HEADs in one call; the preflight as one line per check. THE OPERATOR-LOAD LAW and THE CONTEXT-BUDGET LAW (§1b) bind every message and every call. Beat 1: name the window's ONE deliverable, the intake at the bytes, this text verbatim, §HELD re-printed, hand me ONE act. STATE AT DISPATCH (the record wins): core `93390f0` (HERO-1c + its correction; CI all checks green; the closure counter 3/20); hivemind `98e2f39`; bench `f3631cb` (R-5 Part A + ii); docs `7221ddc`; skills `c630c5c` (Check 9 STALE by one file until my sync, may be worth investing time/effort into skills). The plan of record is `context/planning/2026-09-13_v73_PROGRAM-PLAN_after-114b_two-weeks-and-October.md` — read its §8 by range at beat 1: it carries my brief at the v73 close (Pelton's result assumed to permit the trademark; the entity NexSys LLC; a second product-name candidate filed in the naming docket — never graded in chat; the repos cleaned; the skills folded; the week planned), the week weighed in eight rows, D-v74-1 packaged (the second candidate through the naming frame's knockout BEFORE the swap program; rec (ii)), and v74's window shape: b1 H8-a's packet re-cut through THE PRIOR-LEDGER GATE (re-pinned to 93390f0's green .deb; S31-LABEL: and O-2's second reading added) → b2 the v1.1.4 freeze note + C-003's narrow row (C003: mint) + the Activate: H10 (Mon; the word Tue) → b3 HERO-1d and 114c authored, one dispatched → b4 the brand/entity prep (the token-parameterised swap card set; the counsel wave as one whole-paste email; the knockout charter) → b5 W-SKILLS-9 dispatched (its charter is on disk) and R-5 Part B's packet on the sitting → b6–b8 the intakes and the close. The window's ONE deliverable: H8-a's and R-5 Part B's packets cut through the gate and the sitting named (Sat 09-19 or Sun 09-20; H8: / R5:), or run if the calendar allows. My lines: CI: 93390f0 all checks green is already banked, let me know what other information you need (and provide any instructions on how/where to find or get it for you).

## §2 The preflight, one line per check (the instrument, 2026-09-14T00:0xZ)
- C1 PASS — the snapshot's `last-verified:` 2026-09-13 = today CT.
- C2 PASS — pm-handoff line 8 and the snapshot both name v73 beat 7; the plan of record resolves at `context/planning/2026-09-13_v73_PROGRAM-PLAN_after-114b_two-weeks-and-October.md`.
- C3 PASS — core HEAD `93390f0` = the snapshot's cited sha.
- C4 PASS — `phase-3-milestone-backlog.md` present, `status: CURRENT`, 29 DONE rows (the sha cross-reference not re-run this boot — a light check, disclosed).
- C5 PASS — `## Open Risks` at line 71; its newest date 2026-09-13.
- C6 PASS — coder-handoff's newest entry names the next WU: EXPLAIN-114c.
- C7 PASS — 22 modules in `settings.gradle.kts`; every MODULE_CONTEXT.md present and >1,500 B except `spike/wal-validation` (a spike; no Phase 2).
- C8 PASS — `cross-agent-notes.md` 1,024 B, no active entries (newest date 2026-08-18).
- C9 STALE — 27/28 identical per file (the three SOURCE trees `nexsys-hivemind/project-manager`, `nexsys-hivemind/coder`, `nexsys-skills/orchestrators/nexsys-frontend` vs the session's synced copies); `nexsys-project-manager/SKILL.md` differs (SOURCE bda340b1… / synced 76bf4ce6…). Authoring rides SOURCE.
- C10 PASS — 100 `.md` cites in `strategic-context-map.md`; five unresolved basenames are the map's own patterns (`*_orchestrator_session_prompt.md`, `YYYY-MM-DD_topic.md`, `*_session_prompt.md`, `YYYY-MM_month.md`, `YYYY-WNN_monDD-monDD.md`).
- C11 PASS — spot: `fleet:` in 4 bench files; `bus-soak` in `ci.yml`; `outcomeMeta` 0 files in web-ui (HERO-1c retired it).
- C12 PASS — 0 / 0 / 0 (the LIVE/READY instructions are the H8-a packet and the W-SKILLS-9 charter, both the spine's dispatch-ready lanes; one LIVE prompt, v67; no tracked `weeks/`).

## §3 THE PRIOR-LEDGER GATE — the greps over the H8-a packet (before the re-cut) and each disposition
The rule (pm-lessons 2026-09-13): before a packet cut from a prior record goes LIVE, grep that record's deviations ledger and its findings card's asks for every command string and every named witness the packet reuses; fix each hit or carry it as a named deviation. The prior record here is R-4c's (`context/audits/2026-09-12_R-4c_measurement-only_operator-record.md` §9 lines 838–871; the card's items 7, 8, 10, 11, 14 lines 896–937); R-4b's ledger reaches this packet through R-4c's citations of it (D-5 = R-4b D-2; D-11 = R-4b D-13; D-8 = R-4b's P-1 rule) and through the packet's own purpose line ("every R-4b deviation D-1…D-13 folded in", 09-06) — R-4b's ledger was NOT re-grepped this beat (disclosed, §4).

| # | grep (pattern) | hits in the packet | prior-ledger entry | disposition |
|---|---|---|---|---|
| 1 | `dpkg-query` | lines 43, 61 — `dpkg-query -W homesynapse` | R-4c D-5 (R-4b D-2): `${Version}` eaten inside a single-quoted ssh arg | ALREADY FIXED on 09-06 (the fixed form); `${Version}` absent (grep 2: 0 hits) |
| 3 | `sudo ls` of the config tree | line 56 — `sudo ls -l …/config/homesynapse.yaml …/initial_api_token` | R-4c D-6 (a non-sudo `ls` on a root-only tree) | ALREADY sudo'd; PASS |
| 4 | `date "+%Z %z"` | line 43 | R-4c D-6 (the TZ calibration dropped) | PRESENT; PASS |
| 5 | `shutdown -h` | lines 38 (the bench halt, alone) and 138 — `systemctl stop …; sleep 3; systemctl show …; sudo shutdown -h now` in ONE block | R-4b D-13 / R-4c D-11: the stop-grade read shares a block with the halt; a raced shutdown loses the O-2 evidence | **HIT → FIXED:** B6 split into three blocks — the stop (alone) · the grade READ (O-2's second reading, its own paste) · the halt |
| 6 | `ROWS-pre` | lines 57 (`≥ ROWS-A`), 67 (`ROWS ≥ ROWS-pre`) | R-4c D-7 (`ROWS-pre = ROWS-A` while the fleet reports) | ALREADY `≥`; PASS. B0's ROWS-A note re-derived: R-4c closed at 230+, ≈0.62 rows/min |
| 7 | `button`, `S31` | no button press; no permit-join (the packet adopts nothing) | R-4c D-8 (the S31 cycles a load; a charger was on it) | NOT APPLICABLE to the acts; the new `S31-LABEL:` block is READ ONLY and says so |
| 8 | `Downloads` (the fetch) | line 25 — a fixed name `~/Downloads/distribution-artifacts-arm64.zip`, no mtime selection, no identity before the hash | R-4c D-2 ("packet-class defect across R-3/R-4/R-4b/R-4c/H8-a") | **HIT → FIXED:** newest `distribution-artifacts-arm64*.zip` by mtime; `ls -l` identity printed BEFORE `sha256sum`; a clean expand dir; the stale tree moved aside |
| 9 | `digest` / the origin | §0/§1 bound the .deb to the auth-gated "Version-grammar echo" step line | R-4c D-1 + D-3, ask #5: THE ANONYMOUS ORIGIN GATE (the published artifact digest vs the downloaded zip) | **HIT → FIXED:** §1's gate is the zip's sha256 = the PUBLISHED digest `78ee52fb…` (read by the hub from the run page); the echo line is secondary and skippable |
| 10 | `sed 's/^.*INFO *//'` | 0 hits | R-4c card §10.1 (a stripped journal timestamp) | PASS |
| 11 | `adopt_list_loaded` | 0 hits | R-4c card §10.3 / F-1 (a debug token asserted at INFO) | PASS |
| 12 | `permit_join` | line 43 (`grep -c permit_join_duration`, the no-window guard) | R-4c card §10.2 (the window anchor) | NOT APPLICABLE — no window today; PASS |
| 13 | `Result=` / `NRestarts` | lines 111–112 (B3, its own block), 138–139 (B6) | R-4c card §11 (O-2 read once on `a458a64`) | B3 already its own block; B6 fixed at grep 5; §H now banks O-2 CLOSED on the second reading |
| 14 | the incumbent (`ef02d13`) | line 37 (the label), 44 (the version twice), 11/117 (historical, kept) | R-4c card §7/§8: the held card runs `a458a64` since 14:17Z on 09-13 | **RE-DERIVED:** the incumbent `0.1.0+git20260913.113754.ga458a64`; the label "R-4c DONE — a458a64" with the VERSION line as the truth |
| 15 | the expected entity witness (`rows=3`) | line 94 | R-4c D-9, card §6: the registry moved 3 → 4 (the SNZB-02P adopted 14:43:48Z) | **RE-DERIVED:** rows=4, K1 non-null 4; a 3 is a FINDING |
| 16 | the pin (`f25291b`, `gf25291b`, the path-filter sentence) | lines 3, 11, 18, 19, 26, 28, 52, 62, 137 | — (the artifact of record moved twice since 09-06: `a458a64` at R-4c, `93390f0` now) | **RE-PINNED** to run #55 (§0 card); `gf25291b` 0 hits after; `f25291b` survives only in two "unchanged since" clauses |
| 17 | the bench digest read (`tail -3`) | lines 32–34 | R-5 Part A + ii `f3631cb`: the nightly prints `[--] fleet: <adopted>/<expected> · re-seen <n>` | **WIDENED** to `tail -8`; the first `fleet:` datum is a ⏺ (absence or `unread` is a ⏺, never a stop); the retired owed-lines clause dropped |
| 18 | `S31-LABEL:` / `label` | 0 hits before | the plan §2.1 and R-5B's packet: `S31-LABEL: <A> <W>` → `HARNESS-PLUG:` → P-1's live leg; P-1's `rating_w` "read off the unit's own label" | **ADDED** at B0 (a read-only block while the bench card is OUT); §H banks it |

Also changed: §N's prior-record read (the R-4b audit §0 → R-4c's record §0 + card items 7/8/11) and its paste time (`~14:50 CT` → ten minutes before `H8:`); the frontmatter `status:` (the sitting is Nick's word; the 09-11 slot recorded as NOT RUN with R-4c §8 as the settlement).

## §4 What the hub could not re-execute (disclosed)
- The GitHub jobs and artifacts API endpoints answered 403 to the hub's fetch; the run LIST endpoint and the run's HTML page answered — the pin's facts (run id, conclusion, both jobs, both artifacts, the arm64 zip's digest) are from those two surfaces. Nick re-reads the same page at §0 (a ⏺, not a trust).
- The .deb's own sha256 is read at §1 on the day (the digest binds the zip; the .deb's hash is the hop-to-hop token from there).
- R-4b's deviations ledger (D-1…D-13) was not re-grepped directly; it reaches the packet through R-4c's citations and the packet's 09-06 fold. A direct re-grep is one call if the next author wants it.
- Whether the bench card's tree carries `f3631cb` (the `fleet:` line) is unknown to the hub; B0's EXPECTED treats absence as a ⏺.

## §5 The one re-order of the window
The plan §8 put W-SKILLS-9 at b5. Nick's dispatch line said the skills "may be worth investing time/effort into"; at 23:5xZ both desk slots were free, the hivemind and skills trees were clean (the charter's baseline), and the lane's write-set (the three skill trees, `pm-lessons.md` headings, the WUCP and the environment model) is disjoint from every b1–b4 hub write. The paste was handed as b1's ONE act; Nick pasted at ~19:0x CT; the lane was editing seven hivemind files by 00:10Z and RETURNED by 00:2xZ (8853 B; Nick's relay: hivemind 10 M + 1 A, skills 4 M, zero rule names lost, the three Check 12 sections byte-identical, P4 on four wrong cites) — audited at b2, not here (a lane's return is staged only in the beat that files its audit). HERO-1d/114c at b3 dispatch one lane (the plan said "one dispatched") — the cap of two holds.
