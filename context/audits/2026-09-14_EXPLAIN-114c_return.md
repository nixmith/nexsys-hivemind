<!--
file: context/audits/2026-09-14_EXPLAIN-114c_return.md
purpose: Coder return for EXPLAIN-114c (v1.1.5 additive). §0 first; the hub audits; Nick commits.
status: DELIVERED — REPO-COMPLETE, hub audit pending
-->

# EXPLAIN-114c return — the by-id read, the condition definition on the wire, FIRED_CONFIRMED on evidence (filed 2026-09-14 CT)

## §0 The card
- **Verdict: DELIVERED.** Baseline `6bd8508`, porcelain empty at boot. `date -u` 2026-09-14T22:31:18Z ⇒ CT = UTC−5 = 17:31 CDT, once. Instrument: a 24-core Windows desk, JDK 21, `--offline`; not the runner. Preflight PASS on every adjudicable check (Check 6: no `~/.claude/skills` mirror on this desk).
- **Census (lock-free porcelain): 10 = 8 M + 2 A + 0 D — the Files table exactly (P1 HELD; DP-2 default).** `core/automation/`: M `MODULE_CONTEXT.md` · M `StandardExplanationService.java` · M `RunExplanation.java` · **A** `ConditionDefinitionRenderer.java` · M `StandardExplanationServiceTest.java` · M `NonFiringExplanationServiceTest.java` · **A** `ByIdReadPageCountTest.java`; `api/rest-api/`: M `MODULE_CONTEXT.md` · M `GetRunCausalChainEndpoint.java` · M `RunEndpointsTest.java`. Nothing else; every fenced path (module-info, build files, event-model, persistence, web-ui, payloads) untouched; staged/committed nothing. Hivemind: this return + one `coder-lessons.md` entry.
- **P2 HELD:** T8 at HEAD (22:41:39Z): **2 / 40 / 100** type pages (1,000 / 20,000 / 50,000 envelopes) for the newest run at N = 1k / 20k / 50k on all three seeds — IR-1 byte-for-byte. After K2: ticking seed **1** window page, **0** type pages at every N; displaced-id seed (the fallback) one empty window read then **2 / 40 / 100**; same-instant fixed-clock seed **3 / 41 / 101** window pages, 0 type — a hint HIT at the old cost, not a fallback (R1). **P3 HELD:** the gate line verbatim, green in one round 22:54:16–:22Z — automation **243** (232 + 11) · rest-api **157** (156 + 1; 3 skip) · lifecycle `compileJava` executed · `spotlessCheck` ×2 executed · zero `warning:`; re-run green after the two review fixes 23:09:25–:30Z (R6). **P4 HELD, corrected:** `causalChain_v114KeysLastInOrder_iso8601OrNull` byte-UNCHANGED (it pins root + action lists); the condition pin is `causalChain_v11ShapeTest:199` (`containsOnlyKeys`), which gained `"definition"` last. **P5 HELD:** `readByType(` 1 · `readByTimeRange(` 1 (0 at HEAD). **P6:** three cites moved (§2).
- **§12:** (1) rest-api 157 green; changed `containsExactly` lines **0** (+4 added by T9); the one changed pin is `:199`'s `containsOnlyKeys`. (2) the after-counts above; `readByTimeRange(` **1** · `readByType(` **1**. (3) `"fired and confirmed at"` **0** (was 2: `:390` + the javadoc `:351`); T2/T2b/T2c green. (4) removed assertion lines **2**: `RunEndpointsTest:199` (T6) and `NonFiringExplanationServiceTest:179` `contains("last fired and confirmed")` → `contains("last fired at")` (R2); T5's flip added one line, removed none.
- **DP-1 (taken):** the ticking seed drives `AutomationTestSupport.MutableClock` (1 s per marker) and mints the newest run's id AT its marker's instant — `ulidAt` = `new Ulid((millis << 16) | n, n)`, never `UlidFactory` (its STATIC monotonic guard leaks across test classes; lesson filed); the fallback = a run id an hour after its marker. **DP-2 (taken):** the default renderer class.
- **Tests sorted (#18):** T1–T4, T9 RED at HEAD, T8 RED at HEAD, T5/T6 flipped, T7 preserved — the three-stage red in §2. **Deviations:** `[REVIEW]` R1–R2 · `[INFO]` I1–I8 (§3).

## §1 What changed per file (order written: 7 → 5/6 → 8 → 2/3 → 1 → 4 → 9/10)
1. **`StandardExplanationService`** (1,069 → 1,216; the class javadoc gains a v1.1.5 bullet, its cost paragraph rewritten). **K2:** `HINT_LEAD = 1 s` / `HINT_LAG = 60 s` (design constants; javadoc per O1); `locateTriggered` = `triggeredInWindow(runId, t − lead, t + lag)` — the one `readByTimeRange` site, `SCAN_BATCH` per page, `isTriggeredOf` (type + payload class + run id) — then the HEAD scan verbatim on a miss. **IR-3:** nested `Visitor` / `Verdict { CONTINUE, STOP }`; `scanType(String, Visitor)`; `lastMatching` keeps `Predicate matches` in an always-`CONTINUE` visitor. **IR-2:** `listRuns` returns `STOP` at `globalPosition >= before`. **K3:** `buildConditions(chain, runId, definition, triggerPayload.definitionHash())` — the vouched condition list resolved ONCE per run (`DefinitionHashes.forDefinition(d).equals(hash)`, strings, the run never re-hashed), per event `render(vouched.get(index))` in range, else `null`. **DP-B2:** marker guard `commandCount() == 0`; ACTED_BUT_UNCONFIRMED (the existing sentence) when any action is UNCONFIRMED/FAILED/DISPATCHED or none is CONFIRMED (`latestConfirmedAt` null); else FIRED_CONFIRMED naming `evaluatedAt` and the latest `confirmedAt`; javadoc rewritten to the rule.
2. **`RunExplanation`** (290 → 355): `ConditionView` 4 → 5 (+ nullable `definition`, NOT null-checked; a 4-arg convenience delegates `null`); nested public `ConditionDefinitionView(type, selector, attribute, value, above, below, after, before, children)` — `type`/`children` checked, `children` copied.
3. **`ConditionDefinitionRenderer`** (A, 104): `static render(ConditionDefinition)` — exhaustive over the 7 condition permits (`type` = the simple name) and, in a private `selector`, the 7 selector permits: ULID text · slug · `<kind>:<value>/<roles>` for area/label/type · `tag:<namespace>/<value>/<matchMode>/<roles>` · compound parts `+`-joined; roles sorted, `,`-joined (I3). No I/O, no clock.
4. **`GetRunCausalChainEndpoint`** (215 → 252): `conditionsList` `LinkedHashMap<>(5)`, `"definition"` LAST; recursive `definitionMap` in the SD-2 order, `children` always a list, null → JSON null; the v1.1.5 javadoc.
5. **`StandardExplanationServiceTest`** (1,143 → 1,465): T1 `locateTriggered_findsNewestRunByIdHint` (2,001 markers a second apart; asserts the window `[t − 1 s, t + 60 s)` well-formed and exact — §4) · T3 `listRuns_stopsAtCursor` (501 markers, the cursor inside page 1, `reads == 1`) · T4 `buildConditions_definitionVouchedByHash` · T4b `_compoundDefinitionRendersChildren` · T4c `_nullWhenDefinitionChanged` (hash mismatch AND registry miss) · T4d `_nullWhenIndexOutOfRange` · T4e `conditionDefinitionRenderer_selectorForms`; T5 re-fixtured to the fallback arm (I2). New helpers only (`ulidAt`, `seedRunAt`, `seedTriggeredMarkerAt`, `publishRootAt`, `seedRunWithDefinition(hash, index)`, `definitionWithConditions`); `CountingEventStore` gains `timeRangeReads`.
6. **`NonFiringExplanationServiceTest`** (736 → 832): T2 `completedVerdict_firedConfirmedRequiresConfirmation` (the sentence VERBATIM, two distinct instants — I8) · T2b `_dispatchedIsActedButUnconfirmed` · T2c `_zeroActionsIsNoCommands` (0/0 → the marker, `noCommandsIssued TRUE`); the `:179` flip (R2).
7. **`ByIdReadPageCountTest`** (A, 376): IR-1's shape × three seeds (`TICKING` · `DISPLACED_ID` · `SAME_INSTANT`) × N ∈ {1k, 20k, 50k}; nine `IR1-GUARD` lines (with `newestFound`) printed BEFORE any assertion; the per-seed bounds of §0 P2 asserted, `maxCount == 500`, the oldest run found. No `System.nanoTime`, no file output.
8. **`RunEndpointsTest`** (430 → 492): `:199` gains `"definition"`; T9 `conditionDefinition_v115NestedOrderAndPresentNull` — the condition key ORDER, the nested ORDER at two depths, `children []` on a leaf, the 4-arg fixture's `definition` PRESENT as null.
9. **`core/automation/MODULE_CONTEXT.md`** (+2): the `EXPLAIN-114c status` blockquote after 114b's; the GOTCHA after 114a's (the fixed-clock seed is a hint hit; the ticking seed; the displaced id; `ulidAt` vs the `UlidFactory` guard; the window is a constant).
10. **`api/rest-api/MODULE_CONTEXT.md`** (+3): the v1.1.5 row in the §EXPLAIN-114a table; an `EXPLAIN-114c` paragraph after 114b's (map capacity, DP-B2's effect on the non-firing sentence, T15's fixture literal, 156 → 157).

## §2 Gates, freshness, sweeps
- **Red-first, three honest stages (UTC, `--offline --console plain`):** R0 22:41:28–:39 T8 alone at HEAD → 1/1 FAILED on the ticking-seed bound, the nine before-lines in `<system-out>`. R1 22:44:26–:30 **stage A, behaviour-red at HEAD** → 71/7 failed: T1 (`timeRangeReads` 0), T3 (2 vs 1), T5 re-fixtured (0 vs 1), T2 (the sentence), T2b/T2c (`FIRED_CONFIRMED`), the `:179` flip. R2 22:46:24–:28 **stage B, compile-red at HEAD** → 14 + 6 errors (the absent view/renderer symbols). R3 22:47:55–:48:02 **stage C, behaviour-red** with the record + renderer in, the service/endpoint inert → 77/10 + 12/2 failed: T4/T4b (`definition` null), T9 + `v11ShapeTest` (key absent); T4c/T4d/T4e GREEN under the inert seam (null guards; the renderer's own spec — disclosed). R4 22:51:13–:18 four classes → green (47 · 29 · 1 · 12). **R5 22:54:16–:22 the gate line verbatim** `./gradlew :core:automation:test :api:rest-api:test :lifecycle:lifecycle:compileJava spotlessCheck --offline` → SUCCESSFUL, 9 executed: automation XML 22:54:19Z (243/0/0/0), rest-api XML 22:54:20Z (157/3 skip/0/0), `HomeSynapseCore.class` 22:54:21Z, `spotlessJavaCheck` ×2 executed. **R6 23:09:25–:30 the gate line again** after the two review fixes (§3) → SUCCESSFUL, 5 executed (automation `compileTestJava` · `test` 243/0 · spotless), rest-api and lifecycle up-to-date on their 22:54 outputs; instrument 4 still 2; porcelain unchanged.
- **Deferred Build Gate: YES** — `./gradlew check` (ArchUnit in `:app`) owed to CI on Nick's push; no rule's reach changes (reads only, no clock read).
- **P6, the moved cites:** the test's FIXED clock is `:7`/`:88`, not `:39–:60`; the FIRED_CONFIRMED javadoc opens `NonFiringExplanation:160`, not `:159`; the condition key list is `RunEndpointsTest:199`, not `:262–:294`. Every other cite matched.
- **Sweeps:** the touched tests grep `Instant\.now|currentTimeMillis|new Date|Clock\.systemUTC|LocalDate.*now|nanoTime` = 0. Diff `+` sweep `synchronized|Thread\.sleep|TODO` = 0; `System.out` = 1 (I5). SD-6 `grep -c 'requires\|exports'` 13 / 8 unchanged. `new RunExplanation.ConditionView(` = 1 production + 3 test sites.

## §3 Pushback / observations
- **R1 `[REVIEW]` — DP-1's fallback mechanism, corrected by measurement:** the instruction (DP-1, P2, the §8 GOTCHA wording) has the fixed-clock seed reach the fallback "because the window holds them all". It does not: with every marker and the run id at ONE instant the hint walks the whole log through `readByTimeRange` and HITS at the end (3 / 41 / 101 pages, `readByType` 0 — T8's `SAME_INSTANT`, asserted). The fallback is a run id the window cannot reach (`DISPLACED_ID`; T5). The GOTCHA is written to the measured mechanism; the window stays the constant (§9). Contract impact: none.
- **R2 `[REVIEW]` — one assertion line beyond T5/T6:** `NonFiringExplanationServiceTest:179` — T7's "no assertion line edited except T5/T6" could not survive SD-4's sentence. Instrument 4 = 2.
- **I1** `ulidAt` and the counting store duplicated in the two test classes (`AutomationTestSupport` is outside the write-set). **I2** T5 keeps its `readByType == 1` / `maxCount` lines, gains `timeRangeReads == 1`, names the fallback arm. **I3** every group selector carries `includedRoles`: rendered `<kind>:<value>/<roles>`, roles sorted; pinned by T4e. **I4** T4e added beyond §7. **I5** T8 prints its readings (the lifecycle `BusThreadDumpTest` precedent). **I6** private helpers `triggeredInWindow`, `isTriggeredOf`, `latestConfirmedAt`. **I7** the class javadoc's identity list gained a v1.1.5 bullet. **I8** T2 confirms at +3 s so both instants are discriminable.
- **O1 — SD-1's direction:** under DP-G the marker's `eventTime` IS the triggering event's instant (`StandardRunManager:221` → `:692`), BEFORE `t`, not "at or after"; only a null-`eventTime` trigger sits at/after `t`. The 1-s lead covers the measured bus latency (soak p99 35 ms); a subscriber > 1 s behind, or a device-stamped stale instant, misses → the fallback. If production shows misses, the LEAD is the knob. **O2** `AutomationEndpointsTest:224` (T15) still carries the v1.1.4 sentence literal — a fake's canned string; harmless. **O3** the coder-handoff entry is the hub's (the 114b precedent).
- **Adversarial review (3 lenses × 2 refuters, 19 agents, this session):** 8 findings; 5 refuted by both skeptics (the SQLite plan for the window read — a skeptic ran the V001 schema through SQLite and confirmed the `COALESCE` index serves it; T15's fixture literal, O2; the `containsOnlyKeys` reading, §0; the `:179` cite; "T1 could also assert the page bound" — T8 carries it); 3 confirmed: (i) T8's javadoc promised every reading printed before any assertion while `measure()` asserted presence first — FIXED: `newestFound` rides the `Reading` and is asserted after the nine lines print; (ii) §4's "assert `from < to` in T1 anyway" was unmet — FIXED: T1 asserts the window well-formed and its exact bounds (`CountingEventStore` records `from`/`to`); (iii) **O4 — a pre-existing `DefinitionHashes` property, a hub IR candidate:** `forDefinition` hashes `AutomationDefinition.toString()`, and a group selector's `Set.copyOf(includedRoles)` iterates in a per-JVM-launch order (`ImmutableCollections.SALT32L`), so an automation whose selector names ≥ 2 roles can hash differently after a restart — its pre-restart runs then read `definition: null`, and v1.1.4's `definitionKey` cross-read equality breaks the same way; the loader's one-role default is unaffected; SD-3's string compare is implemented as written — the fix is a canonical (sorted) rendering inside `DefinitionHashes`, its own WU.
- **Next WU:** ENERGY-READ (IR-15 first) after the hub's intake; DUR-1 on Nick's word (§11, IR-22); the v1.1.5 stamp is the hub's at the landing.

## §4 WUCP Phase 1: Coder Closeout
- [x] MODULE_CONTEXT.md updated for: core/automation (the 114c blockquote + the GOTCHA), api/rest-api (the v1.1.5 row + the 114c paragraph)
- [ ] coder-handoff.md — not by this lane (the hub files the entry, the 114b precedent)
- [x] Deferred build gate flag: YES (`./gradlew check` on the landed SHA owed to CI)
- [x] coder-lessons.md appended: the `UlidFactory` static guard vs timestamp-keyed tests (`ulidAt`)
- [x] Cross-agent note posted: Not needed
- Files 10 = 8 M + 2 A (§0) · gates §2 · instruments and DP-1/DP-2 §0 · deviations §3 · next WU §3
- Timestamp: 2026-09-14 23:12 UTC

RETURNED nexsys-hivemind/context/audits/2026-09-14_EXPLAIN-114c_return.md 14568
