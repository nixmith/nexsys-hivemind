<!--
file: context/audits/2026-09-14_FE-114_return.md
purpose: FE-114 lane return — the v1.1.4 keys rendered (EXPLAIN-6/8/9), the literal lint's reach (IR-12), the dialect (IR-13), the terminal rows, the twins (IR-14). Charter: context/instructions/2026-09-14_frontend-lane_FE-114_v114-keys_lint-reach_dialect_terminal-rows_charter.md.
audience: the hub (intake) · Nick (the landing card)
state-type: lane return
status: RETURNED — 2026-09-14 (instrument 2026-09-14T23:14:04Z at boot; CT = UTC−5 → Mon 2026-09-14 ~18:1x–19:xx CT); core 6bd8508 + the 114c working tree (untouched)
-->

# FE-114 return — the v1.1.4 keys, the lint's reach, the dialect, the terminal rows

## §0 The card
**Instrument:** the Windows desk runs the toolchain natively (node v22.6.0 · npm 10.8.2 · vitest · eslint); nothing deferred; `frontend.yml` on Nick's push. Dashboard porcelain EMPTY at start; the 114c files never touched.
**Census: 25 = 24 M + 1 A.** M: `MODULE_CONTEXT.md` · `design/hero-v1/SPEC.md` · `design/hero-v1/states.html` · `eslint.config.js` · `scripts/contract-check.mjs` · `src/components/CausalChain.tsx` · `CausalChain.modes.test.tsx` · `CausalChain.terminal.test.tsx` · `CausalChain.hardening.test.tsx` · `Resource.tsx` · `src/lib/api/contract.ts` · `contract.test.ts` · `shapes.ts` · `v113-additive.test.ts` · `mock/scenarios.ts` · `src/lib/format.ts` · `format.test.ts` · `i18n.ts` · `i18n.test.ts` · `src/views/WhyNotView.tsx` · `WhyNotView.hero.test.tsx` · `RunChainView.tsx` · `DevicesView.tsx` · `DevicesView.honesty.test.tsx`. A: `src/lib/api/v114-additive.test.ts`.
**Red-first:** D0 11 RED at HEAD (wrong-type ×8 · FIRED_CONFIRMED · the pin; 18 by-construction green, disclosed) → 29 · D1 4 RED → GREEN (absent/null arms = preservation) · D2 3 RED → GREEN (absent = preservation) · D3 3 RED (the unknown arm) → GREEN (N6-wins = preservation) · D4 the lint 6 → 0 · D5 1 RED (a NEW pin) + P5 17 → 1 · D6 preservation (`e5:53` · `modes:103` · `i18n:315`, string-relative) · D7 5 RED → GREEN (2 preservation) · D8 preservation (bytes equal; the fold locked) · D9 docs.
**P1** DEVIATES ×3: +`scenarios.ts` (type-forced by the union — 1 map entry + the run-id arm) · +`hardening.test.tsx` (STOP-CLASS) · −`verdicts.ts` (the sentence needs `clockTimeWithDate`; the HERO-1c cycle forbids the import — D1's slot is filled in `CausalChain.tsx`). **P2** MET: 6 at HEAD = the hub's list, 0 false positives, 0 after; whole-src 361 → 370. **P3** MET: 28 files / 535 tests (HEAD 27 / 480); bundle 70.7 KB; contract `v1.1.4-2026-09-13`. **P4** DEVIATES: 5 `-expect` lines — the named `recognise` pin never existed (a NEW pin); two unnamed flips (§3). **P5** MET: 17 → 1 (`MODULE_CONTEXT.md:25`, the quotation). **P6** MET: four md5s unchanged.
**Gates:** `npm run verify` EXIT 0; `npm run lint` green; §6 (1)–(6) all as stated (§2).
**Deviations:** `[STOP-CLASS — PROCEEDED]` a fifth pinned fact changed: `hardening.test.tsx:356` `not.toContain('recorded no steps')` → `('and recorded no steps')` — SPEC §4 :88 puts "Done, recorded no steps." on the era skeleton's terminal step; the old whole-text assertion guarded the headline (`completed.none` ends "and recorded no steps."). Revert path in §3. `[COPY]` D3's `{time}` sits in the run clause ("It ran at {time}, and the record says…"): `lastEvaluation.at` is the EVALUATION instant; the charter's `…confirmed it{time}` misattributes it. `[DEFAULT]` D3 body null, link `whyNot.link.ranFine`, the check glyph; D7's arm gates on `actionCount === 0 && actions.length === 0`. `[SCOPE]` `UNNAMED_TARGET` folded (a sixth twin).

## §1 Per-file deltas
| File | Δ | What |
|---|---|---|
| `contract.ts` | +68/−3 | `CONTRACT_VERSION` v1.1.4-2026-09-13 + the amendment block; `settledAt?`/`confirmedAt?` on the action, `definitionKey?` ×3, `disabledAt?`/`disabledReason?`, `FIRED_CONFIRMED` in the union |
| `shapes.ts` | +22/−1 | VERDICT + FIRED_CONFIRMED; seven `if ('key' in o) strOrNull(...)` lines (tri-state) |
| `contract-check.mjs` · `contract.test.ts` · `v113-additive.test.ts` | +9/−6 · +5/−2 · +4/−2 | the three version pins (+ `licence`→`license` comment) |
| `v114-additive.test.ts` (A) | 237 | 29 rows: tri-state per key, FIRED_CONFIRMED, the fixtures unchanged, assignability, the pin |
| `i18n.ts` | +35/−1 | 13 keys (D1 ×2 · D2 ×1 · D3 ×3 · D4 ×7); `recognise`→`recognize` |
| `CausalChain.tsx` | +43/−9 | D1 `confirmedAtLine` + help slot + hint line; D4 the pill label / `actionPhrase` keyed; D7 the two arms |
| `WhyNotView.tsx` | +34/−12 | D2 `disabledBody`; D3 the FIRED_CONFIRMED case; D4 pills keyed; D8 `timed()` retired for the `.noTime` keys |
| `format.ts` | +17/−13 | D6 `pendingHint` → `t()`; D8 `NOT_RECORDED` · `noReadingLine` · `CASCADE_PARENT_UNRECORDED` · `UNNAMED_TARGET` · `runName` · `NULL_NAME_NOTE` read the catalog |
| `Resource.tsx` · `RunChainView.tsx` | +3/−1 · +2/−1 | `ui.signingIn` · `explain.run.back` |
| `eslint.config.js` | +19/−7 | `HERO_SENTENCE` (≥2 words, apostrophes, punctuation-tolerant); template capital-beside-`${`; `Property > Literal`; scope unchanged |
| `scenarios.ts` | +6/−1 | type-forced FIRED_CONFIRMED entry + run-id arm (no scenario constructs it) |
| tests: `modes` +65/−2 · `hero` +93/−2 · `terminal` +42/−4 · `hardening` +4/−1 · `i18n.test` +56/−0 · `format.test` +30/−1 | | §D1 · §D2/§D3 · §D7 + the flip · the narrowing · §D5 + the 13-row block · §D8 |
| `SPEC.md` +25/−10 · `states.html` +5/−5 · `MODULE_CONTEXT.md` +3/−1 | | 13 §7 rows + amendment; §6 EXPLAIN-6/8/9 LANDED; :207 text change; the dialect sweep; the FE-114 beat |
| `DevicesView.tsx` · `DevicesView.honesty.test.tsx` | 1/1 each | `behaviour`→`behavior` (comments) |

## §2 The gates and the instruments
- **verify:** EXIT 0 — `Test Files 28 passed · Tests 535 passed`; `dist/assets/index-6Tu_jg4G.js 134.65 kB │ gzip 40.58 kB`; `70.7 KB initial bundle`; `✓ Contract coverage complete: 11 endpoints, version v1.1.4-2026-09-13.`
- **The lint at HEAD (the widened rule, six files):** `CausalChain.tsx:79:17` `'Completed, nothing changed'` · `:366:24` `` `Ran ${command} on` `` / `'Ran an unrecorded command on'` · `Resource.tsx:31:29` `"Signing in…"` · `RunChainView.tsx:24:41` `← All runs` · `WhyNotView.tsx:101:22` `'Ran, but sent nothing'` · `:118:28` `'It did run'` — TOTAL 6. **After:** `npx eslint <six>` = 0 (proof the rule fires: HEAD's six files via `git show | eslint --stdin` report lines 79,366 · 101,118 · 24 · 31). **Whole non-test src, same rule:** 361 at HEAD (`i18n.ts` 111 · `scenarios.ts` 70 · `format.ts` 28 · `mockData.ts` 26 · `verdicts.ts` 23 · `shapes.ts` 21 …) → 370 after (the new catalog rows).
- **P5 at HEAD (17 lines):** `v113-additive.test.ts:16` · `i18n.ts:151` · `DevicesView.honesty.test.tsx:74` · `DevicesView.tsx:97` · `SPEC.md:92,:96,:110,:207,:311,:313,:323` · `states.html:10,:142,:509,:528,:543` · `MODULE_CONTEXT.md:25`. **After (1):** `MODULE_CONTEXT.md:25` (the HERO-1d quotation — bytes kept). `Cancelled` untouched.
- **P6:** `896f861d…` · `6f502c7b…` · `05b02fbc…` · `8acb204f…` before and after (pasted from `md5sum` both times).
- **P4 (`git diff -U0 -- '*.test.ts*' | grep -E '^-\s*expect'`):** `hardening:356` · `terminal:116` · `terminal:117` · `contract.test:35` · `v113-additive:403` — 5 lines.
- **Preflight:** Check 2 STALE as chartered (the freeze stamp v1.1.4, the mirror v1.1.3 — folded here); Check 7 mirror diff empty; Check 12: 1 instruction ISSUE-READY-class (`2026-09-06_H8a_…navigator-packet.md`, LIVE by the v75 b1 record), 1 LIVE prompt (v67), weeks tree 0.

## §3 Pushback and observations
- **STOP-CLASS revert path:** restore `hardening.test.tsx:356`, drop the `actionCount === 0` arm in `terminalLine`, delete the three §D7 no-steps rows and the era-skeleton row; INTERRUPTED's flip stands on its own.
- **The flips, counted honestly (the hub's four vs the tree's five):** named + real: `contract-check.mjs:15` (not a test) · `contract.test.ts:35` · `terminal.test.tsx:116–:117` (+ its second home, the `:77` data row: `'explain.terminal.status', {label:'Interrupted'}, 'Interrupted.'` → `'explain.terminal.interrupted', {}, 'Cut off before it finished.'`). Named but non-existent: the `i18n.test.ts` `recognise` pin (added new, red-first). Unnamed: `v113-additive.test.ts:403` (a THIRD `CONTRACT_VERSION` pin — the same fact as :35; the FE-NULL-1 lane added it) and `hardening.test.tsx:356` (the STOP-CLASS narrowing). Five pinned facts changed; each carries its old and new text in place.
- **The dead-key table (zero non-test consumers after this lane; the catalog is 186 → 199 keys; ten former dead keys are consumed by D6/D7/D8: `explain.action.pending.color` · `explain.terminal.interrupted` · `.noSteps` · `explain.slot.automation.earlier` · `explain.nullName.note` · `explain.cascade.parentUnrecorded` · `explain.condition.noReading` · `explain.detail.notRecorded` · `whyNot.headline.actedButUnconfirmed.noTime` · `.sentNothing.noTime`):**
  | Key | Line |
  |---|---|
  | `boot.startingBody` | app copy, not §7; no surface reads it (`feedback.tsx` carries its own replaying literals) → RETIRE-PROPOSED, or consume in `ReplayingBanner` (a text change; hub rules) |
  | `explain.headline.completed.notRecorded` | ruling-gated (the headline audit) — KEEP |
  | `whyNot.neverTriggered.title` | "It hasn't run yet." = `whyNot.headline.neverTriggered` (consumed) — two keys, one sentence → RETIRE-PROPOSED |
  | `explain.action.unconfirmed.title` / `.body` | superseded by `explain.mode.timedOut.label/.help` and `.ackedSilent.*` (HERO-1c) → RETIRE-PROPOSED |
  | `explain.action.pending.title` / `.body` | superseded by `explain.mode.heldDispatched.label/.help` → RETIRE-PROPOSED |
  | `explain.hub.autos.noRunsSinceLoad` | the hub row renders `explain.hub.autos.noRuns` for `lastRunId` null; consuming it is a TEXT change ("No runs yet" → "Hasn't run since it was loaded") → hub rules: consume (a §7 row) or retire |
- **The freeze doc's status line claims "the FE mirror HERO-1b `6af76f7` (`EXPECTED_VERSION` → v1.1.4; frontend green)"** — false at `6bd8508` (`grep` found v1.1.3 in all four pins); this lane is the mirror. A hub docs row.
- **The default mock is still a pre-v1.1.4 hub** on the four reads (the keys absent — lawful, pinned in `v114-additive.test.ts` and disclosed): a `v114` mock touch (every action/chain/entry/automation carries the keys PRESENT, ≥1 null each; one FIRED_CONFIRMED entry; one DISABLED with `disabledAt`) is the next mock WU; the DevPanel cannot reach D1–D3 today. `settledAt` is typed and validated, not rendered (no row asks; a "settled at" line for the timed-out/failed modes is a design row).
- **Still literal, by the lint's design:** `actionPhrase`'s `'Turned on'` / `'Turned off'` / `'Dimmed'` (bare Literals in a switch) and `format.ts` `EMPTY_CHAIN_NOTE` (no §7 key; rendered at `CausalChain.tsx` :282) — a `ReturnStatement > Literal` / `ConditionalExpression > Literal` selector and one §7 row would close both. `verdictMeta` has no FIRED_CONFIRMED arm (unused by D3's pill; only `WhyNotView` calls it).
- **The charter's line for `verdicts.ts:385`/`recognise`:** `verdicts.test.ts:385` pins `v.help === t('explain.mode.unknownOutcome.help')`, key-relative — the reason the "third named flip" did not exist.
- **Next WU (refuse-to-close):** FE-115 after 114c's intake (`conditions[].definition`, v1.1.5 — EXPLAIN-4's "someone is home" rule text), carrying the `v114` mock touch and the H8 real-wire read of the three sentences; the hub's dead-key rulings land as register rows.

## §4 WUCP Phase-1 checklist
- [x] Preflight run; volatile facts re-derived (the freeze stamp, the pins, the P5 lines, the md5s); none copied.
- [x] Contract consumed field-for-field: seven keys + one value, tri-state validators against the rest-api table's null rules line by line; no invented shape; the fixtures byte-identical.
- [x] Every new state honest and distinct (null / absent keep HEAD's sentence; a value renders its row); label + shape on the new pill; no color-only meaning; every sentence a §7 row, test-locked; Register C on all 13.
- [x] Bundle 70.7/100 KB; the gate GREEN on the desk; nothing phones home.
- [x] Name-light; no positioning claim; US dialect of record.
- [x] Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING (no real-wire v1.1.4 body has rendered any of the three sentences — H8). The next WU named.

RETURNED nexsys-hivemind/context/audits/2026-09-14_FE-114_return.md 12705
