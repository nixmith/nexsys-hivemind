<!--
file: context/audits/2026-09-14_v75-b1_boot-intake_IR-1_H8a-repin_and_R-5B_prior-ledger-gate.md
purpose: THE v75 BEAT-1 AUDIT — the boot at the instrument (the read budget, the five HEADs, the preflight one line per check, the b8 card verified at diff-tree); IR-1's instrument (the test authored by an in-conversation agent; the run BLOCKED by the container's egress — the number lands on Nick's host via the b1 packet's block 2, filed before 114c is authored); the H8-a re-pin to `6bd8508`'s install-smoke run #56 (every edit listed); THE PRIOR-LEDGER GATE over R-5B's packet (the greps and every disposition); the two source findings behind the two additions (the S31's cluster count is in the corpus — F-R4b-C; the ingestion unit emits no per-frame token — the H10 D-v75-1); what the hub could not re-execute.
audience: the hub (the record; the next hub's boot) · Nick (§0 and §6)
state-type: intake audit + gate record
status: FILED v75 beat 1 (Mon 2026-09-14; instrument 2026-09-14T18:14:25Z at boot)
-->

# v75 beat 1 — the boot intake, IR-1, the H8-a re-pin, R-5B through THE PRIOR-LEDGER GATE

## §0 Card
- **The window's ONE deliverable (plan §11):** the two sitting packets through THE PRIOR-LEDGER GATE and the sitting named — Sat 09-19, one sitting, H8-a then R-5B; both packets in Nick's hands by Friday. **At this beat:** H8-a RE-PINNED to `6bd8508` (§3); R-5B's packet CUT through the gate (§4) — `context/instructions/2026-09-14_R-5B_fleet-floor-rebaseline_and-the-four-measurements_navigator-packet.md`, 39,081 B, md5 `fb32a2f3…`. Both packets are on the disk; the sitting's word (`H8:`/`R5:` hh:mm) is the only thing open.
- **The boot:** `date -u` = 2026-09-14T18:14:25Z (CT = UTC−5 → ~13:14 CT). The read budget: 13,017 (the prompt) + 1,722 (the chain) + 2,464 (the newest beat) + 3,367 (the snapshot) + 10,714 (the brief) + 1,955 (the plan §3) + 5,146 (the plan §11) + ~600 (the DR §3 headings) + 6,300 (the DR §3, TRIMMED at 6,300 of 9,308 B — D-v74-4's tail, the criterion 2 rest and the fence, unread at boot; the brief carries the ruling) = **≈45.3 KB** — at the budget by trimming, not over it by reading on. pm-lessons entries newer than the ledger: 0 (every mint since 09-10 is marked FOLDED W-SKILLS-9).
- **The five HEADs (one call, 18:14Z):** core `6bd8508` · hivemind `9795c2c` · skills `180375f` · bench `f3631cb` · docs `7221ddc` — every porcelain 0, every push count 0, no `.git/*.lock` — **= Nick's STATE AT DISPATCH line, exactly.**
- **The intake at the bytes:** the v74 b8 card verified at diff-tree — `9795c2c` = 6 files changed (the order was 6 M + 0 A); the chain line 8 names `e5fd594` as the b7 landing and the b8 card produced `9795c2c` (the snapshot's "`e5fd594` + b8 (the card)" resolves to it).
- **The preflight (§2): 12/12 PASS; Check 9 PASS 28/28** (the SOURCE list md5 `01789c084724` = the synced list md5 `01789c084724`).
- **Nick's dispatch text:** filed verbatim at `context/handoff/2026-09-14_v75_dispatch-text_verbatim.md`.
- **Two findings that shape the packet (§5):** (i) the S31's interview cluster count IS in the corpus — R-4b's F-R4b-C (`reporting_configured: device=0x00124B002FA8D1C5 clusters=1 verified=0 degraded=1`, 09-04, artifact ef02d13): the map's "no corpus entry" was a miss at the map's read-set; the packet's block is a RE-read, prediction `clusters=1`. (ii) The ingestion unit emits NO per-frame token at HEAD; Nick's premise for the counting block ("the token the ingestion unit already emits per incoming message") is false at `6bd8508` — the packet counts the unit's OUTPUT as a lower bound and the counter is an H10 (§6, D-v75-1).
- **IR-1 (§3a):** the instrument authored (`_scratch/v75/Ir1PageCountMeasurementTest.java`, 16,413 B); the run blocked in the container (Gradle's hosts 403 at the egress proxy: services.gradle.org, plugins.gradle.org, repo1.maven.org); the number comes from Nick's host — the b1 packet's block 2 — and is filed into IR-1's row before 114c is authored (b2). By construction from the source: pages = ceil(N/500) → 2 / 40 / 100 at N = 1k / 20k / 50k; the wall time is the unknown.

## §1 Nick's dispatch text — see the verbatim file (`context/handoff/2026-09-14_v75_dispatch-text_verbatim.md`)

## §2 The preflight, one line per check (the instrument, 2026-09-14T18:1xZ)
1. PROJECT_SNAPSHOT last-verified 2026-09-14 = today → PASS.
2. The plan of record resolves: the chain and the snapshot name `context/planning/2026-09-13_v73_PROGRAM-PLAN_after-114b_two-weeks-and-October.md` §11; the file exists → PASS.
3. core `log -1` = `6bd8508` = the snapshot's `core \`6bd8508\`` → PASS.
4. `context/planning/phase-3-milestone-backlog.md` exists (88,464 B; 29 DONE rows) → PASS (consistency read at the snapshot's digest, not re-derived row by row — disclosed §7).
5. pm-handoff `## Open Risks` present, 57 rows; the snapshot names seven risks + two fences → PASS.
6. coder-handoff line 21: `NEXT WU (refuse-to-close pointer): EXPLAIN-114c` = the brief's Java-slot row → PASS.
7. MODULE_CONTEXT.md: 21 tracked in core, 0 template-empty → PASS.
8. cross-agent-notes: 0 active entries above `## Archived` → PASS (nothing to be stale).
9. Check 9: 28 source files (nexsys-hivemind/project-manager, nexsys-hivemind/coder, nexsys-skills/orchestrators/nexsys-frontend) md5-listed on the device; 28 synced files md5-listed in the session; the two lists' md5 equal (`01789c084724`) → **PASS 28/28 identical at the bytes.**
10. strategic-context-map: 74 distinct cited basenames; 71 resolve across the five repos' `ls-files`; the 3 unresolved are naming-pattern placeholders (`YYYY-MM-DD_topic.md`, `YYYY-MM_month.md`, `YYYY-WNN_monDD-monDD.md`) → PASS.
11. Source round-trip: `CountingEventStore` resolves in core (1 file, `StandardExplanationServiceTest.java:1094–1142`); `SCAN_BATCH = 500` at `StandardExplanationService.java:102` → PASS.
12. The archive convention: (1) LIVE instructions not dated today = the H8-a packet only (the sitting's lane, recorded); (2) exactly one LIVE orchestrator prompt (the v67 file); (3) `context/planning/weeks/*` tracked = 0 → PASS.

## §3 The H8-a re-pin (the file edited by a guarded python replace on the device; md5 `314fe411…` before → `cf46c124…` after; 34,997 → 36,307 B)
**The pin, read at the instrument:** install-smoke run **#56**, id **34840877827**, commit `6bd8508` "feat(dashboard): HERO-1d …", event push, started 2026-09-14 11:58 CT, 2 min 40 s, both jobs green; artifacts `distribution-artifacts-amd64` 124 MB (`3c0bfbf4…bc25`) and `distribution-artifacts-arm64` 123 MB (**`11f3a79e6370b6e8e8155a7b3deaaced3eaf6fde3bdfa7a482137579d2aa8a75`**). **The method checked against a known value:** the same read on run #55's page returned the arm64 digest `78ee52fb…f76f` = the v74 pin's PUBLISHED line byte-for-byte (§7 names what was not re-executed). **The version string re-derived from the grammar** (`distribution/common.sh:84` — `%cd` in UTC as `%Y%m%d.%H%M%S`; the two known cases re-derive: `93390f0` → `20260913.230020`, `a458a64` → `20260913.113754`): `6bd8508`'s committer date 2026-09-14T11:58:03Z → **`0.1.0+git20260914.115803.g6bd8508`**.
**The edits, each asserted to occur exactly once before the write:** line 3 (the purpose: `6bd8508`, run #56) · line 6 (the status: the re-pin clause prepended; the v74 text kept after "Was:") · line 11 (the GOAL) · line 18 (the tree, HERO-1d, run #56's URL/time, the digest, "HERO-1c and HERO-1d were web-ui-only") · line 19 (the run to open, the digest prefix `11f3a79e`, the `ARTIFACT:` ⏺ line, the STOP prefix, `CI-main: 6bd8508 green` (the counter 4/20), the newest-run clause) · line 25 (the `PUBLISHED` digest) · line 26 (the .deb name and committer date; the name guard) · line 28 (STOP-GATE H8a-1: `g6bd8508`) · line 57 (B1's Version) · line 67 (B1's upgrade sentence: "a later date prefix, 20260914 > 20260913; the incumbent's stamp 20260913.113754") · line 152 (the re-label). **Two non-pin sentences, on Nick's "one sitting" word (disclosed as the only non-pin edits):** B6's heading gains "SKIPPED when R-5B follows in the same sitting … run B6 only if R-5B does not follow"; §N's navigator paste gains "then R-5B's packet in the same sitting — at B5's close read <its path> whole and continue; B6 is R-5B's". After: `93390f0` ×1 (the "Was:" clause), `230020` ×0, `run #56` ×5, `6bd8508` ×17.

## §3a IR-1 — the instrument (the in-conversation agent's return, audited)
- **The agent** (general-purpose; read the source at the clone `6bd8508`; wrote one test; did not run it). **Claims re-executed by the hub:** `SCAN_BATCH = 500` at `StandardExplanationService.java:102` (grep); `scanType` at `:964–978` with `readByType(eventType, after, SCAN_BATCH)` the class's only `readByType` call site (grep); `locateTriggered` at `:1007–1017` returning on the first `runId` match ascending from position 0 (read); `CountingEventStore` at `StandardExplanationServiceTest.java:1094–1142`, counting `readByType` calls only, not envelopes (read). **The by-construction count:** for the newest run, every retained `automation_triggered` marker is visited → pages = ceil(N/500), envelopes = N; the oldest run → 1 page. The wall time on `InMemoryEventStore` is fixture-dominated (its `readByType` is a linear scan per page — the agent's caveat, verified at the fixture) — the count is the store-independent number; the wall time is filed as the fixture's, not SQLite's.
- **The run:** `./gradlew` → the wrapper download 403 at the proxy; `/opt/gradle/bin/gradle` → `plugins.gradle.org` 403; `--offline` fails identically (empty caches). The agent stopped, as briefed. **The hub's decision:** the test file passes to Nick's host (`_scratch/v75/Ir1PageCountMeasurementTest.java`; the output path patched by the hub to `<java.io.tmpdir>/ir1_measurement.txt`, the stdout lines carried by Gradle's `-i`); the b1 packet's block 2 runs it in `homesynapse-core` (a copy into the test source set for the run, moved out after — core porcelain clean at the end); the three `IR1` lines are filed into IR-1's row at b2 BEFORE 114c is authored. The test is the instrument 114c carries as its tests-first row (tagged, so the suite does not pay 50k seeds nightly).
- **Not verified:** compilation under the module path with the real JUnit/AssertJ (the agent compiled with three stubs against the real main sources, `-Xlint:all -Werror`, exit 0); the run's heap at N=50k.

## §4 THE PRIOR-LEDGER GATE — the greps over R-5B's packet (39,081 B) and each disposition
The prior records: R-4c's (`context/audits/2026-09-12_R-4c_measurement-only_operator-record.md` §9 D-1..D-11 at lines 838–871; the card's items 6/7/10/11/12/13/14 at 893–938) and — re-grepped this time, the v74 b1 disclosure closed — R-4b's (`context/audits/2026-09-04_R-4b_re-rep_operator-record.md` §9 D-1..D-13 at 689–740; its asks 1–10 at 48–57). The v74 b1 audit §3 table is the template; the grep numbers continue it.

| # | grep (pattern) | hits in the packet (lines) | prior-ledger entry | disposition |
|---|---|---|---|---|
| 1 | `dpkg-query` | 20 — `dpkg-query -W homesynapse` | R-4c D-5 / R-4b D-2 | the fixed form; `${Version}` 0 hits (fixed-string grep) — PASS |
| 3 | `sudo ls` of the config tree | 0 — the tree is read with `sudo cat -n` (33) and `sudo grep` (20, 33, 62, 115) | R-4c D-6 | every read sudo'd — PASS |
| 4 | `date "+%Z %z"` | 20 | R-4c D-6 (the TZ calibration) | PRESENT at B0, beside the Z clock — PASS |
| 5 | `shutdown -h` | 133, alone in its block; the grade read at 128–129 in its own block | R-4b D-13 / R-4c D-11 | three blocks (the stop · the grade · the halt) — PASS |
| 6 | `ROWS-pre` / an `=` on a live counter | 0; B0 reads ROWS-0 and the disarm asserts `ROWS-W ≥ ROWS-0` | R-4c D-7 | `≥` — PASS |
| 7 | `press`, `S31` | 11, 13, 15, 47, 49, 77 (the SNZB-01P's button — a wireless end device switching no load); 132 ("press nothing") | R-4c D-8 (a relay cycles a load) | the S31 is never pressed, unplugged or cycled; the fence is stated at line 15 and in the S31 block — PASS |
| 8 | `Downloads` / a fetch | 0 | R-4c D-2 | the packet installs nothing; H8-a's §1 gate (D-3) is inherited by reference (line 15) — NOT APPLICABLE |
| 9 | `digest` / the origin | 0 (the artifact is H8-a's, gated at STOP-GATE H8a-2; B0 re-prints the version as the handover's guard) | R-4c D-1/D-3, ask 5 | inherited from H8-a; B0's version STOP catches a missed upgrade — PASS |
| 10 | `sed 's/^.*INFO *//'` (a stripped timestamp) | 0; the chain prints with `-o short-iso-precise` and the gaps from `-o short-unix` | R-4c card §10.1 | timestamps KEPT and the gaps computed from them — FIXED by construction |
| 11 | `adopt_list_loaded` | 0 | R-4c card §10.3 / F-1 | PASS |
| 12 | `permit_join` — the anchor | 47–49, 56: `OPEN` read from the `permit_join_opened` line (`-o short-unix`), `T0` derived from it; `T0=$(date` 0 hits; a fallback to `date` prints itself as a ⏺ | R-4c card §10.2 / R-4b D-8 | anchored to the journal's own instant — FIXED |
| 13 | `Result=` / `NRestarts` | 128–129, own block | R-4c card §11 (O-2) | the second reading of the day; §H banks O-2 CLOSED — PASS |
| 14 | the incumbent (`ga458a64`, `113754`, `93390f0`) | 0; the version is `g6bd8508` (11, 20–21) = H8-a's B1 upgrade | R-4c card §7/§8 | RE-DERIVED from the sitting's order — PASS |
| 15 | the expected entity witness (`rows=4`) | 26, 44 — with "rows ≠ 4 is a ⏺, not a stop" and the 5-branch named | R-4c D-9, card §6 | re-derived (3 → 4 at R-4c; H8-a's B2 reads the same) — PASS |
| 16 | `(a.get(k) or {}).get('lastReported')` (the diff default) | 0; the diff prints `NEW-ROW`; the null arm reads RAW from a 5-s poll started BEFORE the window (38) | R-4c D-10, ask 3 | the instrument D-10 asked for — FIXED |
| 17 | `tail -3` (the digest read) | 0; B6 compares the digest's line COUNT before/after and prints the new line whole | H8-a b1 grep 17 | PASS |
| 18 | `S31-LABEL:` | 15, 132, 174, 183 — read at B4's swap only if H8-a's B0 line is missing | the plan §2.1 | PASS |
| 19 | `head -4` (presence vs. uniqueness) | 140 — a display cap on a line LIST whose counts are printed beside it (`relinked=` `adopted=` `announced=`) | R-4b D-6 | the counts carry the assertion — PASS |
| 20 | `/etc/homesynapse` | 0 (every path `/var/lib/homesynapse/…`) | R-4b D-3 | PASS |
| 21 | `hs-fresh.local` | 0 (the handover rides H8-a's pinned `<IP>`, line 15) | R-4b D-4 | PASS |
| 22 | `sudo nano` | 0 | R-4b D-11 | PASS |
| 23 | `/api/v1/runs` | 0 | R-4b D-12 | PASS |
| 24 | `1a permit_join_duration` without a guard | 33 — inside the `grep -qE … && echo GUARD || sed -i` form | R-4b D-7 | the guarded form byte-for-byte — PASS |
| 25 | `source=rejoin` on `device_adopted` | 0 — the packet's EXPECTED reads `device_adopted: device=… deviceId=… entities=1` (no source field: `ZigbeeAdoptionSlice.java:455`) and puts the provenance on `rejoin_candidate … source=` (`ZigbeeIntegrationAdapter.java:1338`) | R-4c B3's own EXPECTED (`source=rejoin`) — a witness that never existed on the line | RE-DERIVED at the source — FIXED |
| 26 | `network_formed` | 15, 20, 48–49, 115, 140, 153 — counted at every service start and LAST on the bench card | the standing fence | PASS |
| 27 | `TOKLEN-OK` | 15, 20, 26, 28, 38, 44 — before every API read | the standing fence | PASS |
| 28 | `allow-downgrades` | 15 only, as the fence's text | the standing fence | PASS |

**Carried as named deviations, pre-registered in the packet:** the frame-count block counts the ingestion unit's OUTPUT (the store's rows by subject in the window) — a LOWER BOUND, the per-frame counter being D-v75-1 (§6); the S31's cluster LIST is not observable at HEAD — the packet reads the COUNT (F-R4b-C's instrument); the second nightly line comes from the 03:30 CT timer (Sunday's one paste), not a second manual run inside the sitting's 2.5 h.

## §5 The two source findings behind the additions (re-executed at the clone `6bd8508`)
1. **The S31's interview.** The map return (line 18) says "Could not read. The S31's own interview (no corpus entry — re-derive at the sitting)". The corpus has it at the resolution the artifact prints: R-4b's F-R4b-C, `13:51:20.649 … zigbee.reporting_configured: device=0x00124B002FA8D1C5 clusters=1 verified=0 degraded=1` (the R-4b record line 393; ask 9). `clusters=` counts the device's input clusters that sit in `ReportingConfigurator.DEFAULTS` (`ReportingConfigurator.java:63–71`: 0x0006 · 0x0406 · 0x0008 · 0x0300 · 0x0402 · 0x0405 · 0x0001 · 0x0B04 · 0x0702), driven at first adoption and at every re-link (`ZigbeeIntegrationAdapter.java:982/996 → 1009–1020`). A plug with 0x0B04 AND 0x0702 would print `clusters=3`; the S31 printed 1. The refutable-by for `PLUG: two` did not fire in the corpus; the packet re-reads it on `6bd8508` (P-B6). The LIST is neither logged nor persisted: `ZigbeeAdoptionSlice.java:374–386` consumes `endpoint.inputClusters()` at classification; `zigbee-profiles.json`'s S31 entry withholds its fingerprint ("fingerprint deliberately withheld pending …"); the profile match is `Fingerprint.matches(manufacturerName, modelIdentifier)` only. → IR-20.
2. **The frame count.** Nick's brief (DR §1): "one counting block on the token the ingestion unit already emits per incoming message, per device". At `6bd8508`: `ZclIngestionUnit.route` (`ZclIngestionUnit.java:436–470`) logs only the skips — `ingestion_profile_skipped` (DEBUG), `ingestion_unknown_sender` (WARN) — and hands an accepted frame to `listener.onFrame(device)` with no log; `onFrame` (`ZigbeeIntegrationAdapter.java:1201–1212`) records last-seen and availability, no log; the EZSP incoming handler (`EzspCoordinatorProtocol.java:1804–1806`) parses without a log; `grep log.trace` over the module = 0; `logback.xml` is root INFO with no runtime knob ("raise per-logger at runtime via the future LogLevelController"). The DEBUG-only tokens (`ingestion_duplicate` — the consecutive-TSN twins Nick named — `ingestion_unhandled_cluster`) are invisible at INFO. **So no packet block can count frames at the ingestion unit on this artifact.** The packet's B3 counts the unit's output — `events` rows by `subject_ref` and `event_type` in `[open, open+254 s]` (`ingest_time` in microseconds, `SqliteEventStore.java:456`; `subject_ref` the ULID's 16 bytes, `:463`) — plus the WARN/INFO tokens, and names the bound. The counter is one Core change: a per-device `LongAdder` in `route()` after resolution and one INFO line per device at `permit_join_closed` (or per 10 min) — ≤40 lines + a test in `integration-zigbee`, a module 114c does not touch. → D-v75-1 (§6).

## §6 The H10 for Nick — D-v75-1 (non-blocking; the packet stands either way)
```
ESCALATION TO NICK
Task: the frame-count block for R-5B (your brief at the v74 close: "one counting block on the token the ingestion unit already emits per incoming message, per device, over the window")
Question: The ingestion unit emits no such token at 6bd8508 (§5.2) — does the true per-frame count wait for LINK-READ (October), or land as a micro-WU before Saturday?
Options:
(a) R-5B's B3 as cut: the store's rows by subject over the window (a LOWER BOUND, the twins and ZDO frames invisible) + the WARN/INFO tokens; the per-frame counter is LINK-READ's first row (the intake §38 already ruled LINK-READ measures the rate FIRST). Cost: nothing now. Risk: LINK-READ's sampling rule is chosen in October from its own first measurement, not from Saturday's. Buys: 114c dispatches tonight on the plan's order; the sitting's artifact stays 6bd8508 (already pinned, both packets).
(b) FRAME-COUNT, a micro-WU on integration-zigbee (a per-device LongAdder in ZclIngestionUnit.route after resolution; one INFO line per device at permit_join_closed and every 10 min; a test) to host-side Claude Code BEFORE 114c, landed by Thursday; Friday the hub re-pins BOTH packets to its green .deb and R-5B's B3 counts the token. Cost: ≈1 coder-hour + one CI run + one re-pin beat; 114c slips ~a day. Risk: a late red leaves the pin on 6bd8508 and (a) runs — no sitting risk. Buys: the frame rate per device on Saturday; LINK-READ's rule sized three weeks early; a permanent instrument beside the claim.
(c) DEBUG for the window via a logback override on the card — rejected: no runtime knob exists, the DEBUG tokens still miss the accepted frames, and Jetty DEBUG echoes bearer credentials (the logback.xml comment).
PM recommendation: (a) — the plan's timeline loses nothing (LINK-READ measures first by its own charter), 114c keeps the slot tonight, and both packets stay pinned to a green artifact already read at the instrument. (b) is the right instrument and the wrong week.
Refutable-by: you wanting the per-device frame rate BEFORE LINK-READ's charter is written (then (b), and the re-pin is Friday's minutes).
Blocking: no — R-5B's packet carries B3 as (a) now; on `FRAME: b` the hub charters the micro-WU at b2 ahead of 114c. The word: `FRAME: a|b`; silence = (a).
```

## §7 What the hub could not re-execute (disclosed)
- The GitHub run pages were read through the session's fetch tool (a model-summarised read), not raw bytes: the run #56 digest was read once; the method was checked against run #55's page, whose digest re-read equal to the v74 pin — a second independent transcription of #56's digest was not obtained. **The sitting's §1 gate is the operator's own `sha256sum` against the PUBLISHED line; a mismatch there is a STOP that names this read.**
- Check 4's DONE rows were not cross-referenced row by row against the snapshot (the digest's milestone line was read); Check 5's 57 rows were counted, not read.
- The IR-1 test was not run (§3a); its by-construction numbers are the hub's reading of the source, to be replaced by the run's lines.
- R-5B's packet was not dry-run on any card (no rig in the hub's hands); every command string is either byte-identical to a string that ran at R-4c/H8-a's cut or re-derived at the source in §5; the two new python blocks (the poll; the ULID hex→text) were syntax-checked in the session's python3 only.

## §8 The next act
The b1 packet (`_scratch/v75/2026-09-14_v75-b1_operator-packet.md`): block 1 the hivemind card (this beat's census); block 2 the IR-1 run on the host (minutes; `IR1: RETURNED <path> <bytes>`). Then b2: 114c (K2 sized by IR-1's lines) and FE-114 authored and pasted.
