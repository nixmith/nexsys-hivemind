<!--
file: context/audits/2026-09-14_v75-b2_114c-and-FE-114_grounding_two-layer-audit.md
purpose: THE v75 BEAT-2 AUDIT — the b1 card and the IR-1 run verified at the instrument; Nick's `FRAME: a` and his sharpenings banked; the two in-conversation grounding agents (114c on `core/**`; FE-114 on `web-ui/dashboard/**`) audited two-layer — their claims re-executed by the hub at the clone `6bd8508`, one claim REFUTED and corrected (the expiry arm), the K2 design alternatives tabled, K1 and K4 re-classified from "keys" to engine facts; what the hub could not re-execute.
audience: the hub · the 114c Coder (§3 is the K2/K1/K4 grounding the instruction cites) · Nick (§0, §4)
state-type: intake audit (two subagent returns) + design grounding
status: FILED v75 beat 2 (Mon 2026-09-14; instrument 2026-09-14T19:28:03Z)
-->

# v75 beat 2 — the grounding for 114c and FE-114, audited

## §0 Card
- **Verified at the instrument (19:28Z):** hivemind `cc17ce7` (the b1 card: 9 files, `9795c2c..cc17ce7`, push done), porcelain 0, ahead 0; core `6bd8508` porcelain 0 (the IR-1 copy removed after the run, as the card said).
- **IR-1 MEASURED** (`_scratch/v75/2026-09-14_IR-1_measurement.txt`, 576 B; `BUILD SUCCESSFUL in 11s`, 19:07:16Z → 19:07:29Z): `pages=2 envelopes=1000 wallMs=0.66` at N=1k · `pages=40 envelopes=20000 wallMs=10.17` at 20k · `pages=100 envelopes=50000 wallMs=19.14` at 50k; `firstRunPages=1` at every N. The counts equal the by-construction numbers (ceil(N/500)); the wall time is the in-memory fixture's (its `readByType` is a linear scan per page — the hub's b1 caveat stands) and is filed as such, never as the store's.
- **Nick's words, banked verbatim (the DR §1):** `FRAME: a` with the reasoning ((b) is "the tail-of-a-tired-session hazard in another costume"); two sharpenings — the bound split by frame class with the SNZB 2× multiplier (M9.3's deduplicated twins); the counter's home is `onFrame` (a per-IEEE counter with one periodic INFO summary line, never per-frame logging) — and a register row for the missing runtime log-level knob (not before Saturday); the S31 re-read ⏺'s `verified=`/`degraded=` beside `clusters=` so the sitting closes or carries F-R4b-C. All four applied: R-5B's EXPECTED/§H lines (no command string changed — `diff` shows the five prose lines only), IR-20 amended, IR-21 added.
- **The two lanes authored:** `context/instructions/2026-09-14_coder-lane_EXPLAIN-114c_by-id-read_condition-definition_DP-B2_coding-instruction.md` (K2 · K3 · DP-B2 · IR-2 · IR-3; K1 and K4 OUT — §3) and `context/instructions/2026-09-14_frontend-lane_FE-114_v114-keys_lint-reach_dialect_terminal-rows_charter.md` (D0–D9; IR-12/13/14 and the two terminal rows). Both dispatch on Nick's paste tonight; disjoint write-sets (D4).
- **One agent claim refuted at the source (§2):** "no consumer of `TriggerDurationExpiredEvent`" — there is one, `AutomationEngineSubscriber.java:103`; the hub read it: the REPLAY timer bookkeeping only. The substance survives the correction: no run is initiated on expiry (§3, K4) → IR-22, a DEFECT candidate, MEASURE FIRST.

## §1 Layer 1 — the returns, read critically
The 114c agent (≈3,500 words, 84 tool uses) cited every claim file:line; its by-id designs table and the causation-chain walk were internally consistent; its K4 conclusion rested on a grep that missed the subscriber's arm (it grepped `core api integration app` for the type and read the registry and evaluator, but reported "the subscriber has no expiry arm" from the evaluator's `triggerMatchesEvent` alone). The FE-114 agent (≈3,000 words, 40 tool uses) ran the real toolchain (`npm ci`, `npm run verify` GREEN: 27 files / 480 tests; bundle 70.3/100 KB; contract v1.1.3) and a real ESLint experiment with a temporary untracked config (reverted; `git status` clean — verified by the hub: porcelain shows only the untracked IR-1 test the hub itself left); its widened-pattern hit counts (4 / 6 / 9 by regex reading) were labelled as its own reading.

## §2 Layer 2 — the hub's re-executions at the clone (`git status --porcelain` = the untracked IR-1 test only)
1. `grep -rn 'TRIGGER_DURATION_EXPIRED\|TriggerDurationExpiredEvent' --include=*.java core api integration app | grep -v /test/` → `EventCategoryMapping.java:121`, `StandardTriggerEvaluator.java:33,:435–:436`, `AutomationEngineSubscriber.java:17,:103`. Read `:60–:135`: the `:103` arm is inside `accumulateReplayTimer` (`case TriggerDurationExpiredEvent expired -> replayTimers.remove(...)`); run initiation is `runInitiator.initiateRuns(evaluator.evaluateMatches(event), event)` in LIVE only. **The agent's sentence was wrong; its conclusion stands.**
2. `StandardTriggerEvaluator.java:166–:197` (`evaluateMatches`: a `for_duration` trigger goes to `maybeStartDurationTimer`, never to `matched`), `:415–:455` (`expire` → `publishExpired` + `validateAtExpiry`; no initiator in the class), `:482–:500` (`triggerMatchesEvent`: no expiry arm); `StandardAutomationRegistry.java:174–:188` (`consumedEventType`: no trigger maps to `trigger_duration_expired`); `grep -rn 'expir' --include=*Test.java core/automation/src/test | grep -i 'run\|trigger'` = 0; `DurationTimerTest.java:206` "This REPLAY-mode test never initiates a run". **Verified: at `6bd8508` a `for_duration` expiry initiates no run.**
3. `sed -n '372,395p' StandardExplanationService.java`: the DP-B2 guard is `anyMatch(UNCONFIRMED || FAILED)` → else `FIRED_CONFIRMED` with "last fired and confirmed at " + `evaluatedAt`. Verified. `sed -n '340,372p'`: the zero-command marker branch requires `commandCount() == 0 && actionCount() > 0`. Verified.
4. `EventStoreTest.java:28–:31` pins 6 declared methods. Verified. `grep -rn '\.extend(' --include=*.java core/automation/src/main` = the `RunManager.java:66` javadoc only. Verified.
5. FE: `contract.ts:14` = `'v1.1.3-2026-09-06'`; `i18n.ts:159/:161` the two terminal strings; `CausalChain.tsx:433–:435` the `.status` fallback; `eslint.config.js:42` the three-word pattern. All verified byte-for-byte.
6. `Ulid.java:164 public Instant extractTimestamp()`; `InMemoryEventStore.java:236–:252` and `SqliteEventStore.java:221–:226` — both `[from, to)` over `COALESCE(event_time, ingest_time)`. Verified (K2's hint rests on this).
7. `DefinitionHashes.java:29 static String forDefinition(AutomationDefinition)`; `StandardExplanationService.java:132` holds the registry; `RunEndpointsTest.java:345` builds a 4-arg `ConditionView`; `GetRunCausalChainEndpoint.java:140–:160` `conditionsList` (capacity 4). Verified (K3's guard and the wire's last key).

## §3 The design grounding the instruction cites
**K2 — the by-id read: the alternatives at `6bd8508`.** (a) `readByCorrelation(runId)` — the correlation is the TRIGGERING event's chain root, the run id a third ULID (`StandardRunManager.java:218,:219,:257`); returns nothing; making them equal is an engine change that breaks `buildTrigger` (`:676–:683`) and `firstForRun` (`:1019–:1030`). (b) `readById(EventId)` — `event_id` has no index (V001 `:57–:64`; V003 drops `idx_events_subject`), the interface is pinned at 6 methods with 7 implementors, and the run id is not an event id. (c) a scan bounded by a hint — supported today: `RunId` is a time-ordered ULID from the engine's clock; `readByTimeRange` over `COALESCE(event_time, ingest_time)` on both stores; the window `[t − 1 s, t + 60 s)`; the full ascending scan kept as the counted fallback. **Chosen: (c).** The instrument: the IR-1 test as a guard (≤ 2 pages per N on a ticking-clock seed; the old counts on the fixed-clock seed = the fallback proven).
**K3 — the condition on the wire.** The event carries `conditionIndex` and `conditionType` only (`AutomationConditionEvaluatedEvent.java:46–:53`); the registry's definition is index-aligned and hash-checkable (`definitionHash` on the triggered payload vs `DefinitionHashes.forDefinition`); no `describe()` exists on the sealed hierarchies. **Chosen: a STRUCTURED view (`ConditionDefinitionView`), hash-guarded, present-null** — Core prose would violate the dashboard's catalog rule (`web-ui/dashboard/MODULE_CONTEXT.md:51`) and repeat the sentence the dashboard already refuses (`WhyNotView.tsx:173–:176`).
**K1 — `cascade.parentRunId`: an engine fact, OUT.** Every run is a root (`RunInitiator.java:96–:101`; `RunCausalChain.extend` uncalled); the chain breaks at the device (`ZclIngestionUnit.java:611 publishRoot`; `StateProjection.java:460–:462` chains `state_changed` to the report); only `state_confirmed.reportEventId` (`StateConfirmedEvent.java:26–:33`, the ledger's correlation) bridges two runs, for the confirmed case, by a type scan. Where parentage is minted is a design decision → **D-v75-3** (the DR).
**K4 — `forDuration` `firingValue`: not a key, a defect candidate.** §2 items 1–2. The MODULE_CONTEXT sentence (`core/automation/MODULE_CONTEXT.md:29`, "a forDuration run's triggering event is the trigger_duration_expired envelope") describes a run the engine never starts. → **IR-22**, MEASURE FIRST (DUR-0: one RED test in `DurationTimerTest` — a `for_duration` automation, the clock advanced past expiry, `initiateRun`/`automation_triggered` asserted); the fix (DUR-1) initiates the run on expiry with the expired envelope as the triggering event (the firing value via `startingEventId` in the same correlation) — its order in the series is **D-v75-4** (the DR).
**DP-B2.** Two leaks under "fired and confirmed": a `DISPATCHED` (in-flight) action and a zero-action run; the enum's own contract is "every device action confirmed" (`NonFiringExplanation.java:159–:165`). The rule in the instruction SD-4; the sentence names both instants.

## §4 What the hub could not re-execute (disclosed)
- No Gradle run in the container (the egress); 114c's counts (232 automation tests at `6bd8508` tracked; rest-api 156) are `grep -c '@Test'` sums, re-derived by the Coder at P3.
- The FE agent's ESLint experiment was not repeated by the hub; its six-hit list is the charter's P2 baseline, which the lane re-derives at HEAD first.
- No live v1.1.4 capture exists; FE-114's D0 validators are tested on hand-built objects (the charter says so; the surface banks as MIRRORED until a capture).
- The 60-s hint window is a design constant, not a measurement of publish latency; the fallback makes a miss correct, and T8's fixed-clock arm proves the fallback.
