<!--
file: context/audits/2026-09-15_v75-b3_114c-and-FE-114_intake_two-layer-audit.md
purpose: THE v75 BEAT-3 INTAKE — EXPLAIN-114c (`context/audits/2026-09-14_EXPLAIN-114c_return.md`, 14,568 B) and FE-114 (`context/audits/2026-09-14_FE-114_return.md`, 12,705 B) audited two-layer: the returns read critically; the hub's re-executions at the bytes — both working-tree patches (10 + 25 files) applied clean to a clone of `6bd8508`, every §12/§6 instrument re-run there, `npm run verify` re-run GREEN in the hub's own container, the K2/DP-B2/renderer regions and the FE arms read in the applied tree; the rulings on every [REVIEW], STOP-class and census deviation; the findings into the register; the two landings prepared (one commit per lane, one push each; CI is the gate of record).
audience: the hub · Nick (§0, §3) · the next lanes (FE-115, ENERGY-READ — §4 carries what they inherit)
state-type: intake audit
status: FILED v75 beat 3 (Tue 2026-09-15 ~00:xx UTC = Mon 2026-09-14 ~19:xx CT; instrument 2026-09-15T00:00:17Z)
-->

# v75 beat 3 — EXPLAIN-114c and FE-114, intaken two-layer

## §0 The verdicts
- **EXPLAIN-114c — ACCEPT.** Census 10 = 8 M + 2 A = the Files table (P1 HELD); the gate line green in one round (automation 243 = 232 + 11 · rest-api 157 = 156 + 1, 3 skip · lifecycle compiled · spotless ×2) and green again after two review fixes; every §12 instrument reads as asked with ONE honest correction (the condition-key pin is `causalChain_v11ShapeTest:199`'s `containsOnlyKeys`, not a `containsExactly`); K2 measured 1 window page / 0 type pages at every N after (2 / 40 / 100 before = IR-1 byte-for-byte). Two [REVIEW] items ruled §3 (R1 — the hub's DP-1 mechanism was wrong and the coder measured the right one; R2 — one assertion line beyond the named flips, forced by the sentence SD-4 ordered). One finding docketed (O4 → IR-23). The freeze's v1.1.5 stamp is written at the CI verdict (the 114a precedent).
- **FE-114 — ACCEPT.** Census 25 = 24 M + 1 A with three declared deviations (P1: +`scenarios.ts` type-forced, +`hardening.test.tsx` STOP-class, −`verdicts.ts` an import cycle); `npm run verify` EXIT 0 on the desk AND re-run by the hub in its container on the applied tree (28 files / 535 tests; bundle 70.7 / 100 KB; contract `v1.1.4-2026-09-13`); P2 MET (6 lint hits at HEAD = the hub's list, 0 after, whole-src 361 → 370 reported); P5 17 → 1 (the quotation); P6 the four fixture md5s unchanged (re-derived by the hub). The STOP-class proceed ruled §3 (ACCEPT — SPEC-backed, one token, the revert path filed); the D3 copy ruled §3 (ACCEPT — the lane's honesty beat the charter's form). The charter's own two cite errors filed §3.
- **Both lanes wrote nothing outside their domains**; the tree at Nick's desk carries both, disjoint (`git status --porcelain -- core api lifecycle app` = the ten 114c paths; `-- web-ui` = the 25 FE paths; nothing else). **The landing (§5):** two commits, two pushes, in order — 114c then FE-114 — each its own CI sample (the counter 5/20 → 6/20 if green); the hivemind card third.
- **The bridge note:** a zero-byte `homesynapse-core/.git/index.lock` (23:59:47Z) was left by the hub's own timed-out `git diff` over the mount; no git process alive; swept by RENAME to `_scratch/v75/index.lock.swept-000041` (THE COMMIT-BOUNDARY LAW's lock clause); named in the card.

## §1 Layer 1 — the returns, read critically
**114c** (§0 card 2.9 KB; §1 per file; §2 gates with the three-stage red; §3 R1–R2, I1–I8, O1–O4 and the 19-agent adversarial review's 8 findings, 5 refuted, 3 fixed; §4 the closeout). The card leads with the verdict, the census by path, P2–P6 with numbers, the four instruments with their readings, DP-1/DP-2 as taken. Its P4 correction (the `containsOnlyKeys` pin) is the kind of honesty the law wants — the instruction's instrument 1 named the wrong assertion form and the coder said so instead of bending the count. O1 corrects the instruction's SD-1 rationale: under DP-G the run's `automation_triggered` inherits the TRIGGERING event's instant (`StandardRunManager:221 → :692`), so the marker sits BEFORE the run id's mint, not after — the 1-s LEAD is the operative half of the window, the 60-s LAG the safety; the constants stand.
**FE-114** (§0 card; §1 the per-file table; §2 the gates and the six instruments; §3 the STOP-class revert path, the flips counted honestly, the dead-key table, the freeze-doc finding, the mock finding, the still-literal list; §4 the checklist). The lane found that the charter's "fourth named flip" (an `i18n.test.ts` pin on `recognise`) did not exist (the pin is key-relative at `verdicts.test.ts:385`) and that two unnamed flips did (`v113-additive.test.ts:403`'s third `CONTRACT_VERSION` pin; the hardening line) — five pinned facts changed, each with its old and new text in place. It proceeded through a STOP-class line with a one-token narrowing rather than stopping, and said so first in its card with the revert path — a judgment the hub rules below.

## §2 Layer 2 — the hub's re-executions (the clone at `6bd8508`; the two patches from Nick's tree)
1. **The patches** — `git diff -- core api` + the two untracked files as creation diffs (10 `diff --git`); `git diff -- web-ui` + the one untracked test (25) — copied off the desk, `git apply --check` CLEAN for both, applied; porcelain 35 = 10 + 25.
2. **114c's instruments in the applied tree:** `grep -c 'eventStore.readByTimeRange('` = 1 · `'eventStore.readByType('` = 1 · `'fired and confirmed at'` = 0; `git diff -U0 -- RunEndpointsTest.java | grep contains(Exactly|OnlyKeys)`: one `containsOnlyKeys` line changed (+`"definition"` last), four `containsExactly` lines ADDED (T9); `git diff -U0 -- '*Test.java' | grep '^-\s*assert'` = 2 lines — `:199` (the pin) and `NonFiringExplanationServiceTest:179` `contains("last fired and confirmed")` (R2); `module-info` / build files / `core/event-model` / `core/persistence` in the diff = 0.
3. **K2 read in the tree:** `HINT_LEAD = 1 s`, `HINT_LAG = 60 s` (`StandardExplanationService.java:128–:129`); `locateTriggered` → `triggeredInWindow(runId, t − lead, t + lag)` then the HEAD scan through the typed `Visitor` with `Verdict.STOP` at the first match (`:1119–:1131`); `triggeredInWindow` the class's only `readByTimeRange` site, paging `[from, to)` with `SCAN_BATCH`. **DP-B2 read:** the marker branch on `commandCount() == 0` alone; `notConfirmed` = any UNCONFIRMED/FAILED/DISPATCHED; `latestConfirmedAt` null (no CONFIRMED action) → ACTED_BUT_UNCONFIRMED; the FIRED_CONFIRMED sentence "last fired at <evaluatedAt> and its device confirmed at <confirmedAt>; no non-firing was detected in the requested window." **The renderer read:** exhaustive over the 7 condition permits and the 7 selector permits; roles sorted and `,`-joined; `children` always a list; no I/O, no clock. All as SD-1/SD-2/SD-4 ordered.
4. **FE-114's instruments in the applied tree:** `recognise` in `i18n.ts` = 0 · `'An earlier automation'` in `format.ts` = 0 · `replace(' at {time}'` in `WhyNotView.tsx` = 0 · `CONTRACT_VERSION` = `v1.1.4-2026-09-13` in `contract.ts` and `contract-check.mjs` · the four fixture md5s `8acb204f… / 896f861d… / 6f502c7b… / 05b02fbc…` unchanged · `git diff -U0 -- '*.test.ts*' | grep '^-\s*expect'` = 5 lines (the hardening line; the INTERRUPTED pair; the two version pins).
5. **`npm run verify` in the hub's container** (the FE agent's `node_modules` from b2; node 22): lint clean · tsc clean · `Test Files 28 passed (28)` · `Tests 535 passed (535)` · `dist/assets/index-6Tu_jg4G.js 134.65 kB │ gzip 40.58 kB` · `✓ Within budget (70.7 KB / 100 KB)` · `✓ Contract coverage complete: 11 endpoints, version v1.1.4-2026-09-13` · EXIT 0. **The lane's gate is the hub's gate.**
6. **The STOP-class narrowing at the source:** `SPEC.md:88` is the era-skeleton entry ("No detail recorded … `actionCount` 0 …"); the old assertion `not.toContain('recorded no steps')` guarded `explain.headline.completed.none` (which ends "…and recorded no steps."); the new `not.toContain('and recorded no steps')` still guards exactly that headline while the terminal step now reads the `explain.terminal.noSteps` row. Verified. **The new sentences:** `whyNot.headline.firedConfirmed` "It ran at {time}, and the record says the device confirmed it." (+ `.noTime`), `whyNot.body.disabled.at`, `explain.action.detail.confirmedAt` "Confirmed at {time}, {delta} after it fired." (+ `.noDelta`), `explain.mode.unknownOutcome.help` now "recognize". The FIRED_CONFIRMED arm sits AFTER the `noCommandsIssued === true` check (`WhyNotView.tsx:99`, `:139–:143`). The seven tri-state validators (`shapes.ts:303–:353`, `if ('key' in o) strOrNull(...)`). Verified.

## §3 The rulings
- **114c R1 — ACCEPT; the hub's error.** DP-1 said the fixed-clock seed "reaches the fallback because the window holds them all"; it does not — a window that holds every marker is a HIT at the old cost (3 / 41 / 101 window pages, measured). The fallback is a displaced run id (an hour from its marker). The coder wrote the GOTCHA to the measured mechanism and kept the window constants. The instruction's DP-1/P2/§8 wording stands in the record as the hub's misstatement, corrected by the return.
- **114c O1 — ACCEPT; a MODULE_CONTEXT fact.** The marker precedes `t`; the LEAD is the knob; a subscriber > 1 s behind, or a device-stamped stale instant, misses into the fallback — correct and cheap. If production shows misses, the LEAD grows; nothing else.
- **114c R2 — ACCEPT.** `NonFiringExplanationServiceTest:179`'s `contains("last fired and confirmed")` could not survive the sentence SD-4 ordered; the flip is named old → new. Instrument 4 = 2, both named.
- **114c O4 → IR-23 (DEFECT candidate).** `DefinitionHashes.forDefinition` hashes `AutomationDefinition.toString()`; a group selector's `Set.copyOf(includedRoles)` iterates in a per-JVM-launch order, so a definition with ≥ 2 roles hashes differently after a restart: its earlier runs read `definition: null` (SD-3 works as written — the guard is honest; the hash is not) and v1.1.4's `definitionKey` stops being stable across reloads (the "stable definition key" the hero waits on is unstable for exactly that class). The fix is a canonical (sorted) rendering inside `DefinitionHashes` — its own WU (HASH-1), with the note that existing multi-role hashes change once.
- **FE-114 STOP-class proceed — ACCEPT, with the letter noted.** §7 said STOP; the lane narrowed one token, kept the headline guard intact, filed the revert path first. The narrowing is SPEC-backed (§2 item 6) and the alternative (dropping the no-steps arm) would have left the SPEC's own sentence unrendered. The record keeps the deviation as the lane's call, not the hub's charter; the hub's charter erred by listing "a fifth pinned fact" as a STOP when the charter's own row D7 created that fact.
- **FE-114 D3's copy — ACCEPT.** `lastEvaluation.at` is the evaluation instant; "It ran at {time}, and the record says the device confirmed it." attributes the time to the run and the claim to the record. Better than the charter's form.
- **FE-114's census deviations — ACCEPT ×3** (`scenarios.ts` type-forced by the union; `verdicts.ts` untouched — the HERO-1c import cycle; the hardening test).
- **The charter's own errors (the hub's):** the `recognise` pin did not exist (`i18n.test.ts:315` pins the pending string; `verdicts.test.ts:385` is key-relative); the third `CONTRACT_VERSION` pin (`v113-additive.test.ts:403`) was missed by the grounding — both are filed here as the hub's, and the lane's count of five flips is the count of record.
- **The dead-key table — the hub's rulings (reversible by REVERT, inside FE-115):** RETIRE `whyNot.neverTriggered.title` (a twin of the consumed headline), `explain.action.unconfirmed.title/.body`, `explain.action.pending.title/.body` (superseded by HERO-1c's mode keys); KEEP `explain.headline.completed.notRecorded` (ruling-gated); `boot.startingBody` → RETIRE (the banner carries its own literal; a text change is not owed); `explain.hub.autos.noRunsSinceLoad` → RETIRE (the "since it was loaded" nuance is not something the wire can vouch for — `lastRunId` null is "no runs on record", which is what renders).
- **`i18n.ts:167`** carries a stale comment ("`explain.terminal.interrupted` stays unconsumed …") now that D7 consumed it — a one-line FE-115 row, not a correction before the landing (a comment, zero bytes on screen).

## §4 The findings, filed
- **IR-23** (the register): the definition hash's set-order instability — HASH-1.
- **The freeze doc's status line** claims "the FE mirror HERO-1b `6af76f7` (`EXPECTED_VERSION` → v1.1.4; frontend green)" — false at `6bd8508` (all four pins read v1.1.3); FE-114 is the mirror. The hub corrects the line at the v1.1.5 stamp beat (the freeze doc is the hub's).
- **The default mock is a pre-v1.1.4 hub** on the four reads (the keys absent — lawful, pinned); the DevPanel cannot reach D1–D3 today → the `v114` mock touch is FE-115's second row; `settledAt` typed and validated, not rendered (no row asks) → a design row.
- **Still literal by the lint's design:** `actionPhrase`'s three bare `Literal`s and `EMPTY_CHAIN_NOTE` — a `ReturnStatement > Literal` / `ConditionalExpression > Literal` selector + one §7 row (FE-115 or a hygiene row).
- **`AutomationEndpointsTest:224`** still carries the v1.1.4 sentence literal in a fake's canned string (O2) — harmless; a hygiene row.
- **114c's coder-lessons entry** (the `UlidFactory` static monotonic guard vs timestamp-keyed tests; `ulidAt`) — filed by the lane; the coder-handoff entry filed by the hub this beat (the 114b precedent).

## §5 The landing (three blocks, in order; the card `_scratch/v75/2026-09-15_v75-b3_operator-packet.md`)
1. core — EXPLAIN-114c: the ten paths by explicit `git add`, `staged: 10`, the trailer grep, `git commit -F ../_scratch/v75/2026-09-15_core_EXPLAIN-114c_commit-msg.txt`, `git push`; then `CI: <sha> green|red` from Actions (the `ci` run; the counter's next sample).
2. core — FE-114: the 25 paths, `staged: 25`, `-F ../_scratch/v75/2026-09-15_core_FE-114_commit-msg.txt`, push; `frontend.yml` + `ci` on the same push → `CI: <sha> green|red`.
3. hivemind — the b3 card (this audit, the two returns, the coder-handoff entry, the register, the two instruction status lines, the spine, the brief).
The v1.1.5 stamp on the freeze doc, IR-1/2/3/12/13/14's retirement and the counter's advance bank at the CI verdicts (the next beat).

## §6 What the hub could not re-execute (disclosed)
Gradle (the container's egress; the desk's Java 11): the 114c gate is the coder's two green runs + CI on Nick's push. The 19-agent adversarial review's refutations were read, not repeated (one — the SQLite `COALESCE` index serving the window read — the hub had verified at b2 from the V001 DDL). No browser render of the three new FE sentences (the mock cannot reach them; H8's live-wire rule holds them as MIRRORED until a v1.1.4 capture).
