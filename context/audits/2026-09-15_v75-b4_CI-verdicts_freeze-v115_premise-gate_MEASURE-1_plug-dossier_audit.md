<!--
file: context/audits/2026-09-15_v75-b4_CI-verdicts_freeze-v115_premise-gate_MEASURE-1_plug-dossier_audit.md
purpose: THE v75 BEAT-4 AUDIT — (§1) the two CI verdicts read at the instrument, not from the chat; (§2) the freeze doc's v1.1.5 stamp and the false "mirror at v1.1.4" line corrected in one edit, every replacement exact-once; (§3) THE PREMISE GATE minted (pm-lessons) and APPLIED — MEASURE-1's premise table grepped at `3d40b5f` (one cite corrected before dispatch) and the plug dossier's disqualifying claims re-executed at the source; (§4) the dossier two-layer; (§5) HASH-1 ranked as a defect (D-v75-5); (§6) the register retirements at the shas; (§7) the ClaudeFolder consistency sweep — what was checked, what was changed, what still reads stale and where. Written so a stranger can re-run every check.
audience: the hub (the next beats; W-SKILLS-10 folds §3's gate into coding-instruction-format.md) · Nick (§1, §5, §7 — the facts behind the b4 card) · the Java lane (MEASURE-1 carries §3's table in its own §0)
state-type: audit (two-layer where a lane returned; the hub's own edits at the bytes)
status: FILED v75 beat 4 (Mon 2026-09-14 ~21:xx CT; instrument 2026-09-15T02:1xZ). Findings: F-b4-1 (the freeze doc misstated the mirror for one window — corrected); F-b4-2 (MEASURE-1's `AreaSelector` cite was two lines off — corrected before dispatch; the gate's first catch); F-b4-3 (the v73 plan still read as the plan of record — superseded in its status line); F-b4-4 (the salted-order idiom is not only the selectors' — `Map.copyOf` in `CommandAction:51`, `EmitEventAction:37`, `EventTrigger:43` and `Set.copyOf` in `WebhookTrigger:55`; a two-parameter command action is predicted per-JVM today, so IR-23 is LIVE — HASH-0 widened to probe it; the freeze note, the register row and the plan widened in the same beat).
-->

# v75 beat 4 — the CI verdicts, the v1.1.5 stamp, THE PREMISE GATE, MEASURE-1, the plug dossier

## §0 The beat's claims (each verified below)
Core `e56f555` (EXPLAIN-114c) and `3d40b5f` (FE-114) are CI green → IR-1/2/3 and IR-12/13/14 RETIRE at those shas and the closure counter reads **6/20**. The read-API stamp is **v1.1.5**. THE PREMISE GATE is minted with Nick's three exhibits and applied to the two things leaving this beat (MEASURE-1; the dossier). The plug question closes on the dossier: the Gen4 units Nick owns qualify; the purchase is the reference meter alone. HASH-1 is ranked a DEFECT and sequenced (D-v75-5). The new plan of record is `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md`.

## §1 The CI verdicts — read at the instrument
| Sha | Nick's line | The hub's read | Verdict |
|---|---|---|---|
| `e56f555` (114c; 10 = 8 M + 2 A) | "passed all checks GREEN" | `github.com/…/homesynapse-core/actions/workflows/ci.yml?query=…` — run **#249** on `e56f555`, conclusion **success** | GREEN — the deferred gate in `coder-handoff.md` RESOLVED; IR-1/2/3 RETIRED at `e56f555` |
| `3d40b5f` (FE-114; 25 = 24 M + 1 A) | "passed all checks GREEN" | the same workflow page — run **#250** on `3d40b5f`, conclusion **success**; `frontend.yml` on this push is Nick's line (the hub read `ci.yml`'s listing; `frontend.yml`'s listing was not opened separately — the hub's own `npm run verify` on the identical tree at b3 was green: 28 files / 535 tests) | GREEN — IR-12/13/14 RETIRED at `3d40b5f` |
The counter: `a458a64 · fed99e8 · 93390f0 · 6bd8508 · e56f555 · 3d40b5f` = **6/20**, `SAMPLES: passive`. Both HEADs re-verified over the mount before any write: hivemind `553bf5b` clean, ahead 0; core `3d40b5f` clean, ahead 0; the hub's container clone reset to `3d40b5f` for every grep in §3.

## §2 The freeze doc — the v1.1.5 stamp and the corrected mirror line (Nick's first act for b4)
`context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md` — `_scratch/v75/freeze_edit_b4.py` (md5 `d2e7df9b…`), four exact-once replacements, the md5 asserted before (`ad417284…`, 29,642 B) and printed after (`ef9bb2e1…`, 34,475 B):
1. **Line 5 (state-type):** the stamp **v1.1.5** (ONE additive key on ONE read; landed `e56f555`, CI run #249) with the FE mirror of v1.1.5 marked NOT YET CUT (FE-115, after HASH-1 — D-v75-5); **the false clause corrected** — it said "the FE mirror HERO-1b `6af76f7` (`EXPECTED_VERSION` → v1.1.4; frontend green) + HERO-1c `93390f0`"; the dashboard's `CONTRACT_VERSION` stayed at **v1.1.3** through HERO-1b/1c/1d (FE-114's own commit message: "the dashboard's mirror still said v1.1.3"), and **FE-114 `3d40b5f`** is the first mirror at v1.1.4 (`v1.1.4-2026-09-13`; CI run #250). The correction is written INTO the line as a dated CORRECTION, not silently — a reader of the v74 b3 → v75 b4 window's copies is told what stood there. **F-b4-1.**
2. **The B3 causal-chain shape:** `conditions[].definition` as the fifth condition key (`<object|null>`), the nested shape in the endpoint's own order (`GetRunCausalChainEndpoint.definitionMap`, `LinkedHashMap<>(9)`: type, selector, attribute, value, above, below, after, before, children).
3. **The `[v1.1.5 amendment]` note** under the causal-chain read: present-null; the vouch rule (the registry hash equals the run's stamped `definitionHash`, the index in range); the selector rendered to one string (`ConditionDefinitionRenderer` — roles name-sorted); the ORDER pin (`RunEndpointsTest.conditionDefinition_v115NestedOrderAndPresentNull`); **the HASH NOTE** Nick asked whether the stamp needs — yes, written as a PREDICTION (HASH-0 measures it): `DefinitionHashes.java:30` hashes `toString()`; the four role-carrying records store `Set.copyOf` (`AreaSelector:47`, `LabelSelector:46`, `TypeSelector:48`, `SemanticTagSelector:57`) and four more store `Map.copyOf`/`Set.copyOf` (F-b4-4 — the note widened in the splice); a ≥ 2-role selector's or a ≥ 2-parameter action's `definitionKey` (both reads) and this key's null/value arm are not stable across restarts until HASH-1; single-role definitions (every tracked YAML) are stable; pre-fix logs keep their stamped hashes.
4. **The `[v1.1.5 note]` under the non-firing read:** the verdict RULE corrected by 114c, no shape change — FIRED_CONFIRMED only on confirmation evidence (≥ 1 CONFIRMED, none DISPATCHED/UNCONFIRMED/FAILED — DP-B2 as ruled at b3); an in-flight run and a zero-command run report ACTED_BUT_UNCONFIRMED (the latter with the v1.1.2 `noCommandsIssued` marker — `StandardExplanationService.completedVerdict`, `commandCount() == 0` alone, re-read at `3d40b5f`); the two-instant sentence verbatim.
Every fact in 2–4 was re-read at `3d40b5f` in the clone before it was written (`GetRunCausalChainEndpoint.java:56–62, :173–:195`; `ConditionDefinitionRenderer.java:23–34, :82–:101`; `RunExplanation.java:203–:206`; `api/rest-api/MODULE_CONTEXT.md:325, :335`). The git index lock the edit's `git status` left over the mount (a zero-byte `.git/index.lock`, 02:14Z; `pgrep git` = 0) was swept by RENAME to `_scratch/v75/index.lock.swept-b4-021400` and is named in the card.

## §3 THE PREMISE GATE — minted, then applied to the two things leaving this beat
**Minted** in `context/lessons/pm-lessons.md` (OPEN; folds into `coding-instruction-format.md` and the charter template at W-SKILLS-10) with Nick's three exhibits verbatim: the ambition map's fence (2) ("the plug's read side already exists" — false at HEAD; the lane's P4); 114c's DP-1 ("the fixed-clock seed reaches the fallback" — false; the coder measured a hint hit 3/41/101); FE-114's `recognise` pin (did not exist) and the third `CONTRACT_VERSION` pin (`v113-additive.test.ts:403`, missed). **The rule:** every `file:line` and every "already exists / does not exist" claim a charter makes is grepped at the pinned HEAD before the charter goes DISPATCH-READY; the greps are listed in the authoring beat's audit; a premise that fails is corrected, or written as a PREDICTION the lane adjudicates in its §0.
**Applied to MEASURE-1** (`context/instructions/2026-09-15_coder-lane_MEASURE-1_DUR-0_and_HASH-0_two-red-probes_coding-instruction.md`, its own §0 carries THE PREMISE TABLE). Every row grepped in the clone at `3d40b5f`: `StandardTriggerEvaluator` — `expire()` :415 · `publishExpired` :433 · `validateAtExpiry` :446 · the javadoc "the trigger still fires" :444 · `maybeStartDurationTimer(automation, index, …)` :186 · `triggerMatchesEvent` :482 with **0** `TriggerDurationExpired` arms · `pollExpirations()` :258 (package-private; the test driver) · `StandardAutomationRegistry.consumedEventType` :174 with **0** `TRIGGER_DURATION_EXPIRED` cases · `AutomationEngineSubscriber.java:103` inside `accumulateReplayTimer` and `runInitiator.initiateRuns` :70 · `DurationTimerTest.java` 6 `@Test`, the fixture :51–:92, `firesAtBoundary` :94–:111 (drives expiry via `clock.advance` + `evaluator.pollExpirations()`), the W2 replay test :201–:230 · `AutomationTestSupport.RecordingRunManager.calls()` :316 returning `List<InitiateCall(…)>` :297; `MutableClock.advance(Duration)` · tests asserting a run after expiry: **0** · `DefinitionHashes.forDefinition` :29–:31 (`sha256Hex(definition.toString())` :30) · `AreaSelector` `Set.copyOf` **:47** · `LabelSelector` :46 · `TypeSelector` :48 · `SemanticTagSelector` :57 · the loader default `Set.of(EntityRole.PRIMARY)` `AutomationDefinitionLoader.java:339`, `included_roles` → `LinkedHashSet` :342–:344 · `EntityRole` PRIMARY :46, DIAGNOSTIC :49 · `grep -rln included_roles --include=*.yaml .` = **0** · the gate's widening grep (`Set.copyOf|Map.copyOf|new HashSet|new HashMap` over `core/automation/src/main/java/com/homesynapse/automation/*.java`): `CommandAction.java:51` `parameters = Map.copyOf(parameters)` · `EmitEventAction.java:37` `payload = Map.copyOf(payload)` · `EventTrigger.java:43` `payloadFilters = Map.copyOf(payloadFilters)` · `WebhookTrigger.java:55` `allowedMethods = Set.copyOf(allowedMethods)` · `AutomationDefinition.java:85–:87` `List.copyOf` ×3 (ordered — not a source) — **F-b4-4**: the defect's surface is eight records, not four, and a two-parameter command action makes it LIVE; HASH-0 now probes definition B (a two-parameter action) beside definition A (the two-role selector). **One failed premise, corrected before dispatch: the table cited `AreaSelector` `Set.copyOf` at `:45`; the grep says `:47` (F-b4-2 — the gate's first catch, on the hub's own charter). The three sibling records' lines were added.** The predictions P1–P4 in MEASURE-1's §0 are written as predictions the lane adjudicates (P1 the census exact; P2 DUR-0 RED; P3 HASH-0 ≥ 2 distinct hashes across eight launches and the two roles rendered in BOTH orders; P4 a cite moved at the lane's HEAD, or none did). A second cite corrected in the same pass: P3's `AreaSelector` compact-constructor range `:42–:46` → `:44–:48`.
**Applied to the dossier** — §4.

## §4 The plug dossier — two-layer
**Layer 1 (the lane's return, `context/research/2026-09-15_PLUG-DOSSIER_return.md`, 10,536 B with the frontmatter):** §0 the recommendation (the Gen4 units Nick owns; buy no plugs; the reference meter P4460, fallback PN2000); §1 the grading table; §2 the disqualified models with the reason each; §3 the reference meter; §4 what it did not verify (the divisor's value — predicted 10 from a Hubitat ×10 report; the firmware note).
**Layer 2 (the hub's re-executions):** (i) the Shelly feature matrix (`shelly-api-docs.shelly.cloud/gen2/Integrations/Zigbee/DeviceFeatures`), the "Shelly Plug US" row re-fetched: device type On/Off Plug-in Unit (0x010A); **Zigbee Router**; On/Off 0x0006 EP1 Input; **Metering 0x0702 EP1 Input**; **Electrical Measurement 0x0B04 EP1 Input** — TRUE, the two criteria met on the manufacturer's own page. (ii) The z2m converter re-fetched at the tag the lane cited (`Koenkk/zigbee-herdsman-converters` `v25.80.0` `src/devices/shelly.ts`): `zigbeeModel "Plug US"`, `model "S4PL-00116US"`, `extend: [m.onOff({powerOnBehavior: false}), m.electricityMeter(), ...shellyModernExtend.shellyCustomClusters(), shellyModernExtend.shellyWiFiSetup()]` — **plain `m.electricityMeter()`, no custom converter, no custom divisor** — TRUE. (iii) Not re-executed by the hub: the two meter prices and stock (a listing read once by the lane; they move), the Gen4 firmware release note (the lane's read; carried into the shopping card as a step, not a claim), and the divisor's VALUE — a PREDICTION (acPowerDivisor 10) adjudicated at ENERGY-READ's first read, which is exactly where THE MEASUREMENT RECORD is minted. **Verdict: ACCEPT.** `PLUG: two` closes as: two Gen4 units (owned) + one reference meter (bought). The plan of record §7 DROPS the second plug purchase on this dossier. THE ADOPTION FENCE stands.

## §5 HASH-1 ranked — a DEFECT on a frozen key (D-v75-5; the H10 in the DR §3)
Nick's ranking is accepted: `definitionKey` is a v1.1.4 wire key on two reads and the hero is about to trust it; a value that changes at restart for a class of definitions is a correctness defect, not a register row. Where it sits: **114c → MEASURE-1 (DUR-0 + HASH-0, tests first, RED) → DUR-1 + HASH-1 (one coder session; two files, tests first) → ENERGY-READ; FE-115 renders `definition` only after HASH-1 is green.** The stamp note: yes (§2 item 3), as a prediction until HASH-0 lands. The fixture that proves stability across two JVMs: HASH-0's eight-launch loop (`cleanTest` + the probe test per launch; the sixteen `HASH0` lines and the distinct-hash count filed) is the RED instrument; HASH-1's GREEN fixture is the same loop returning ONE hash plus a golden-hash test for the two-role definition (`DIAGNOSTIC, PRIMARY` supplied in that order and in the reverse — one hash). The one place the hub's recommendation differs from a canonical serializer: shape **(a)** — an ORDERED immutable view (`EnumSet`/`TreeSet`/`TreeMap` behind `unmodifiable…`, the same `toString()` format) inside the eight records F-b4-4 names — leaves every single-element hash UNCHANGED (every run in every log today), where shape (b) (a serializer in `DefinitionHashes`) moves every `definitionKey` once and makes every pre-fix run read `definition: null`. Refutable-by: HASH-0 returning one hash across eight launches (IR-23 closes as the hub's misread).

## §6 The register — retired at the shas
IR-1, IR-2, IR-3 → RETIRED at core `e56f555` (CI run #249). IR-12, IR-13, IR-14 → RETIRED at core `3d40b5f` (CI run #250). IR-22 → `DUR: measure` RULED; DUR-0 dispatches in MEASURE-1. IR-23 → a DEFECT (D-v75-5); HASH-0 dispatches in MEASURE-1; the fix shape (a). The status line re-cut.

## §7 The ClaudeFolder consistency sweep (Nick's last ask)
Checked, over the mount: (1) **the plan of record** — `context/planning/2026-09-13_v73_PROGRAM-PLAN_after-114b_two-weeks-and-October.md` still carried a LIVE status; its status line now says SUPERSEDED by the v75 plan (its §10 and §11 stand as history), and the snapshot's digest names the v75 plan by path so the boot's read (f) lands on it (**F-b4-3**). (2) **`card-gradle:`** — dropped from the brief's owed words and the snapshot (R-4c answered `absent`; TR-1b is a desk WU after the run — the plan §7). (3) **The freeze doc** — §2. (4) **The instruction statuses** — `DISPATCH-READY` after this beat: the H8-a packet, the R-5B packet, MEASURE-1; 114c and FE-114 read EXECUTED with their landing shas added. (5) **`coder-handoff.md`** — the 114c entry's DEFERRED GATE → RESOLVED at CI run #249; NEXT WU → MEASURE-1 then DUR-1 + HASH-1 then ENERGY-READ. (6) **The root `ClaudeFolder/START_HERE.md`** is a pointer to the hivemind's (unversioned; the hivemind copy wins) — unchanged. (7) **The skills** — Check 9 PASS 28/28 at boot; the premise gate is a pm-lessons entry (OPEN) until W-SKILLS-10 folds it, so a session dispatching from the skill alone reads the lessons file's newer entries per the boot prompt's (g). Not swept this beat: the core repo's MODULE_CONTEXTs (the lanes' own, audited at b3) and `homesynapse-core-docs` (no change since `7221ddc`); the root folder's legal/strategy files (not the hivemind's; nothing dispatches from them).

## §8 What was not re-executed, in one place
Gradle (the container's egress; MEASURE-1 runs on Nick's host); `frontend.yml`'s own listing for `3d40b5f` (Nick's line + the hub's identical-tree `npm run verify` at b3); the meter prices; the Gen4 firmware note; the divisor value.
