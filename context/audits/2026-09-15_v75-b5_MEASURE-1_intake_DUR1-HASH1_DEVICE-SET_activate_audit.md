<!--
file: context/audits/2026-09-15_v75-b5_MEASURE-1_intake_DUR1-HASH1_DEVICE-SET_activate_audit.md
purpose: THE v75 BEAT-5 AUDIT — (§1) MEASURE-1's return intaken two-layer (the tee'd HASH-0 file re-tallied by the hub; T1's code read at the bytes; the return's numbers checked line by line); (§2) DUR-1 + HASH-1 chartered THROUGH THE PREMISE GATE (every cite grepped at `3d40b5f`; three cites corrected before DISPATCH-READY); (§3) DEVICE-SET chartered through the gate (the Zigbee read side grepped at `3d40b5f`); (§4) the Activate `/apply` page read on the day (D-v74-2) and the email handed; (§5) the spine rotation (v74 b1–b8 → the archive, bytes asserted); (§6) what was not re-executed.
audience: the hub · Nick (§1, §4) · the Java lane (DUR-1 + HASH-1's premise table is the charter's §0)
state-type: audit (two-layer)
status: FILED v75 beat 5 (Tue 2026-09-15 ~18:xx CT). Findings: F-b5-1 (MEASURE-1's premise table filed `consumedEventType` under the evaluator; it is the registry's — the lane's P4 correction, accepted); F-b5-2 (the DUR-1 charter's first draft cited `publishExpired`/`validateAtExpiry` at `:428/:430` and `dedup` at `:158`; the grep says `:427/:429` and `:159` — corrected before dispatch; the gate's second catch); F-b5-3 (a THIRD expiry site — `rebuildTimer`'s downtime arm `:295–:296` — was not in MEASURE-1's table; added to DUR-1's).
-->

# v75 beat 5 — MEASURE-1 intaken; DUR-1 + HASH-1 and DEVICE-SET chartered; the Activate email

## §1 MEASURE-1 — two-layer, ACCEPT
**Layer 1 (`context/audits/2026-09-15_MEASURE-1_return.md`, 5,107 B):** DELIVERED, both measured; census 2 = 1 M + 1 A, nothing under `src/main`, nothing staged; DUR-0 RED (`Expected size: 1 but was: 0`); HASH-0 salted on both sources; P1–P4 adjudicated (P4: one cite mis-filed, its claim holds); zero [REVIEW]; O1 (the two salts flip independently — a fix on one source proves nothing about the other), O2 (nothing in `expire()` reaches an initiator AND the redelivered expired event matches no trigger — T1 covers both paths).
**Layer 2 (the hub's re-executions):** (i) the tee'd file `_scratch/v75/2026-09-15_HASH-0_eight-jvms.txt` (4,208 B, 32 lines) re-tallied by an independent script over the mount: **8 distinct pids** (367224 … 371644); **A: 2 hashes** (`67464952…` ×5 with `includedRoles=[DIAGNOSTIC, PRIMARY]`, `202ada4d…` ×3 with `[PRIMARY, DIAGNOSTIC]`); **B: 2 hashes** (`ba46bcf1…` ×5, `303c1b94…` ×3) — the return's numbers exactly. (ii) T1 read at the bytes (`git diff` over the mount, 38 insertions): LIVE mode via `setMode(SubscriberMode.LIVE)`; the light-on state event through `onEvent`; the preservation lines (timer 1, started 1, no run yet); `clock.advance(30 min + 1 s)` → `pollExpirations()`; expired 1, timer 0; the expired envelope redelivered through `onEvent`; the measurement `assertThat(calls).hasSize(1)` — exactly MEASURE-1 §4's shape; the seam is the subscriber's `onEvent`, as the instruction allowed. (iii) The core porcelain over the mount: ` M DurationTimerTest.java` · `?? DefinitionHashOrderProbeTest.java` — 2 = 1 M + 1 A; `git status --porcelain -- core api lifecycle app` shows nothing else; HEAD `3d40b5f`. Not re-executed: the Gradle runs (the container's egress); the JUnit XMLs (the lane's `245 completed, 1 failed`). **Verdict: ACCEPT.** IR-22 is a DEFECT (measured); IR-23 is a DEFECT on both sources (measured). The tests stay on the desk uncommitted and land with their fixes, as the instruction said.
**The lane's I2 (the probe's `System.out`):** ruled — the probe may stay as test code under the `-i` grep contract or fold into T3; the DUR-1 + HASH-1 charter carries both forms and asks the lane to declare which.

## §2 DUR-1 + HASH-1 — chartered through the gate
`context/instructions/2026-09-15_coder-lane_DUR-1_and_HASH-1_the-run-on-expiry_and-the-ordered-views_coding-instruction.md` (22,881 B). Every row of its §0 premise table grepped in the hub's clone at `3d40b5f`: `evaluateMatches` :166 · `processCancellations(event)` :170 · the `triggerMatchesEvent` call :181 · `forDurationOf` :184 · `maybeStartDurationTimer` :186 · the immediate add :188 · `pollExpirations()` :256 · `rebuildTimer`'s downtime arm :295–:296 · `expire` :415 with `publishExpired` :427 and `validateAtExpiry` :429 · `publishExpired` :433 · the javadoc "the trigger still fires" :444 · `validateAtExpiry` :446 · `triggerMatchesEvent` :482 · `forDurationOf` :583–:587 · `TriggerMatch` :645 · `Snapshot.build` :122, `consumedEventType(trigger)` :131, `computeIfAbsent` :135, `dedup` :159, `consumedEventType` :174 · `candidatesForEventType` :85 · the subscriber `:60 / :65 / :69–:70 / :103` · `RunInitiator.initiateRuns` :80, `runManager.initiateRun` :96–:101 · `EventTypes.TRIGGER_DURATION_EXPIRED` :152 · `TriggerDurationExpiredEvent` :29 · the eight records' lines (:47 · :46 · :48 · :57 · :51 · :37 · :43 · :55) · `AutomationDefinition` `List.copyOf` :85–:87 · `AggregateValue` :59 · `MODULE_CONTEXT.md` :15 / :29 / :33 / :137–:138. **Three cites failed on the first draft and were corrected (F-b5-2); one site was missing and was added (F-b5-3).** The design: (1a) the registry indexes a for_duration trigger ALSO under `trigger_duration_expired`; (1b) the evaluator's candidate loop matches an expired payload to the trigger it names (`automationId` + `triggerIndex`) as an IMMEDIATE match — replay-safe because initiation stays behind `if (!replay)`; (2) the eight compact constructors store an ordered immutable view (`EnumSet`/`TreeSet`/`TreeMap` behind `unmodifiable…`) in the same `toString()` format — `DefinitionHashes` untouched; T4 pins single-element hashes recorded at the BASELINE before any edit (the preservation proof). Predictions P1–P5 pre-registered. DISPATCH-READY on Nick's `HASH1: a` (silence = (a)).

## §3 DEVICE-SET — chartered through the gate
`context/instructions/2026-09-15_research-lane_DEVICE-SET_the-benchmark-fleet_charter.md`. Its §1 claims about HEAD grepped at `3d40b5f`: `ClusterHandlers.java:36–:52` eight handlers (OnOff, Level, Color, Occupancy 0x0406, PowerConfiguration, Temperature 0x0402, Humidity 0x0405, IasZone 0x0500) — none for 0x0B04 / 0x0702 / 0x0400; `ReportingConfigurator.java:71–:72` knows 0x0B04 (`0x050B`, 5 s, 3600 s, change 10) and 0x0702 (`0x0000`, change 5); `EndpointClassifier.java:41` 0x0051 → SMART_PLUG, `:95` 0x0107 → the occupancy sensor, `:158 / :175` `iasCapability`. The method binds the lane to purposes-first grading, two vendors, a Class-0.5-or-better reference meter, a stable resistive known load, repeatability in one sitting, refusals in writing, and two priced tiers. The b4 shopping card is WITHDRAWN on Nick's word (`METER: ordered` retired); the plan's §7 row "the second plug purchase DROPPED" is re-cut in the plan's §10 addendum: the purchase question is re-opened as a benchmark-fleet question and closes on DEVICE-SET's return with `DEVICES: core|extended|<edits>`.

## §4 Activate (D-v74-2) — the `/apply` page read on the day
Fetched 2026-09-15: applications OPEN; **close Oct 30, 12 p.m. PT**; eligibility "bachelor's degree and 4+ years post-baccalaureate scientific research, engineering, or technology development experience"; "leading the commercial development of a hardware-based technology innovation for the first time"; full-time; US work authorization; < $2M raised; tracks Anywhere / Berkeley / Boston / Houston / New York; questions to `apply@activate.org`; $100K/yr stipend + $100K R&D + $12K/yr travel/PD + health + relocation. **The refutable-by of D-v74-2 did not fire:** the close is six weeks out (not inside two weeks), so `hold` stands and the email is one paste (the b5 card, block 4). Two eligibility facts the email must ask about, plainly: the bachelor's confers December 2026 (so "post-baccalaureate" years do not yet exist on paper, though the technical years do — the Navy nuclear years and the NRL years); and whether a software platform for home hardware fits "hardware-based technology innovation". A note for the plan: Oct 30 is the run's weekend — if a yes comes, the application is written by Oct 23 or not this cycle.

## §5 The spine rotation
v74 b1–b8 (8 blocks, lines 47–111 of the live file) → `context/handoff/archive/pm-handoff-beats-v74b1-v74b8-rotated-2026-09-15.md` verbatim; bytes(kept) + bytes(archived body) = bytes(before), asserted inside the b5 splice; the archive map row added; live after this beat: v75 b1–b5 = 5.

## §6 Not re-executed
Gradle (any); the JUnit XMLs; the retailer prices (DEVICE-SET's job); Activate's FAQ page (only `/apply` was read — the FOP's recorded FAQ conflict "PhD required" is left for the email's answer, not re-fetched).
