<!--
file: context/audits/2026-09-17_v75-b6_DUR1-HASH1_DEVICE-SET_intakes_activate-recut_audit.md
purpose: THE v75 BEAT-6 AUDIT — (§1) DUR-1 + HASH-1 intaken two-layer (the diff read at the bytes over the mount; the 64-line loop file re-tallied; Nick's own re-run read; R1 ruled); (§2) DEVICE-SET intaken two-layer (the P4460 manual re-fetched; the 0x010A classification gap grepped at HEAD; the reporting-threshold finding read at the source; D-v75-6 packaged); (§3) the Activate plan RE-CUT on Nick's critique — the b5 email's false entity line, its two register-unsupported claims and its two low-yield questions named as the hub's errors; the live FAQ read; the corrected email; the skip-default rule with a date; (§4) ZIGBEE-GAPS and FOP-1 chartered through the premise gate; (§5) the landing card; (§6) not re-executed.
audience: the hub · Nick (§1's ruling, §2's word, §3) · the lanes
state-type: audit (two-layer)
status: FILED v75 beat 6 (Wed 2026-09-16 ~20:xx CT). Findings: F-b6-1 (the b5 Activate email — four hub errors, each named in §3; THE PREMISE GATE now binds outward company text: every fact and every product sentence grepped at the live source and the claim register before it is handed); F-b6-2 (the plug dossier and the b4 intake missed that the Gen4 identifies as device type 0x010A while `EndpointClassifier` reaches the plug branch on 0x0051 only — DEVICE-SET caught it; IR-24); F-b6-3 (`ReportingConfigurator`'s reportable-change values are raw attribute units, the divisor unapplied — IR-25); F-b6-4 (the "acPowerDivisor 10" prediction had no reading behind it — withdrawn; the first read decides).
-->

# v75 beat 6 — DUR-1 + HASH-1 and DEVICE-SET intaken; the Activate plan re-cut; two research lanes chartered

## §1 DUR-1 + HASH-1 — two-layer, ACCEPT; R1 ruled
**Layer 1 (`context/audits/2026-09-15_DUR-1_HASH-1_return.md`, 7,074 B):** DELIVERED; census 14 = 12 M + 2 A, the Files table exactly; the gate line green in one round (automation 250, rest-api 157/3 skipped, lifecycle compiled, spotless green); T1/T1b/T1c red at the baseline → green; T3 red → green; T4 the preservation pins recorded at the untouched baseline (C `6fa283b7…`, D `c419d1c4…`) and equal after; the loop 64 lines, 8 pids, one hash per definition; P1–P5 held (P5: `pollExpirations()` :256 is the javadoc, the signature :258 — a cite correction, accepted); [REVIEW] R1; I1–I8; one coder-lessons entry (hash fixtures mint identities with `Ulid.parse`).
**Layer 2 (the hub's re-executions):** (i) `_scratch/v75/2026-09-15_HASH-1_eight-jvms.txt` re-tallied over the mount by an independent script: 64 lines, 8 distinct pids, A = A-twin = `202ada4d…` ×8 each, B = B-twin = `ba46bcf1…` ×8 each — one hash per definition, the ORDERED values HASH-0 had shown as one of two. (ii) Nick's own re-run, pasted at b6: the gate line `BUILD SUCCESSFUL in 30s` after `./scripts/clean.sh` (96 tasks executed — a clean build, stronger than the lane's incremental one) and a second eight-launch loop, one hash per definition, the twins equal. (iii) The diff read at the bytes over the mount (path-limited `git diff`): the registry's expired bucket exactly as chartered (§4 row 1a; `dedup` collapses the double entry); the evaluator's arm before `triggerMatchesEvent`, keyed on `automationId` + `triggerIndex` + `forDuration != null`, `continue` after — with the R1 carve-out `&& !(trigger instanceof EventTrigger)`; `forDurationOf` hoisted to package-private static; the eight constructors: `Collections.unmodifiableSet(roles.isEmpty() ? EnumSet.noneOf(…) : EnumSet.copyOf(roles))` over a `Set.copyOf` (the null/duplicate handling kept), `Collections.unmodifiableMap(new TreeMap<>(Map.copyOf(…)))`, `Collections.unmodifiableSet(new TreeSet<>(Set.copyOf(…)))`; `DefinitionHashes` untouched (not in the diff). (iv) Core porcelain over the mount: 12 M + 2 A, nothing outside `core/automation`; HEAD `3d40b5f`; no lock. Not re-executed: Gradle (the container's egress); the JUnit XMLs.
**R1 — RULED: ACCEPT the carve-out as implemented.** An `EventTrigger` whose `event_type` is `trigger_duration_expired` is a loadable definition today (`requireKnownEventType` admits every `EventTypes` constant) and fires at HEAD by type equality; the charter's literal rule would have silently stopped it — a behaviour change the charter did not intend and no test pinned. The lane's `continue` still shields every other permit (a `ManualTrigger` shares the automation subject the expired envelope carries and must not fire on it). The contract impact is stated in the commit message. The second-redelivery finding (two `InitiateCall`s; dedup is `StandardRunManager`'s C2 key `(automationId, triggeringEventId)` `:222–:225`) is a fact about the recorder, not a defect — pinned, not fixed; no register row.
**Verdict: ACCEPT.** The landing: ONE core commit of the 14 files (the card, block 1); CI on the push is the gate of record; at CI green IR-22 and IR-23 RETIRE at the sha, the freeze doc's HASH NOTE is re-cut from a prediction to a fact, and FE-115 is unblocked.

## §2 DEVICE-SET — two-layer, ACCEPT; D-v75-6 packaged
**Layer 1 (`context/research/2026-09-15_DEVICE-SET_return.md`, 13,995 B):** `DEVICES: core` at $104.78 (Third Reality Smart Plug Gen3 $14.99 · Poniie PN2000 $28.99 · P3 P4460 $42.43 at Walmart · a clamp light + two GE 40 W appliance lamps $18.37); EXTENDED +$73.89 (SONOFF SNZB-06P24 mmWave · Hue motion SML003 — fenced until IR-18; held); P1 confirmed, P2 confirmed, **P3 REFUTED** (the P4460's 0.2 % is its voltage row; power is 0.5 % typ / 2 % max; no Class-0.5 meter found under $100 — so A = PN2000 Class 1.0 with 0.01 W resolution, B = P4460 as the cross-check), P4 split, P5 partly (no 60–100 W incandescent is sold new; a 40 W appliance lamp; P ∝ V^1.6); the benchmark method (three simultaneous readings; `within` / `outside` / `divisor-error` / `reference-disputed`; the tolerance built up from the meter's class, the plug's quantization, the lamp's voltage sensitivity and the maker's stated accuracy — ≈2.7 % on a divisor-1 Gen4 at 40 W); four findings about HEAD.
**Layer 2 (the hub's re-executions):** (i) the P4460 manual re-fetched (`p3international.com/manuals/p4460_manual.pdf`): "Active Power 0~1875 Watt 0.5% 2%", "RMS Voltage 85-125 Vrms 0.2% 1%", "RMS Current 0.3% 1%", operating 0.2–15 A — P3's refutation is TRUE; the b4 dossier's "±0.2 %" (which the hub accepted) was the voltage row. (ii) The classification gap grepped at `3d40b5f`: `EndpointClassifier.java:41` `DEVICE_TYPE_SMART_PLUG = 0x0051` is the only plug branch; the Shelly matrix row the b4 dossier itself quoted says "On/Off Plug-in Unit (0x010A)" — so the Gen4 would classify as a plain switch today: TRUE, and a hub miss at b4 (F-b6-2) → **IR-24: ENERGY-READ attaches metering by CLUSTERS present (0x0B04/0x0702), not by device type.** (iii) `ReportingConfigurator.java:71–:72`: `DefaultRow(0x050B, 0x29, 5, 3600, 10)` and `(0x0000, 0x25, 5, 3600, 5)` — the reportable change is a raw attribute value; with `acPowerDivisor` 1 a change under 10 W is not reported until the 3600-s maximum: TRUE (F-b6-3) → **IR-25: scale the reportable change by the divisor/multiplier read at interview.** (iv) The "divisor 10" prediction: no reading behind it (F-b6-4) — withdrawn; the first read at ENERGY-READ decides, as a P-row. Not re-executed: the retailer prices and stock (dated in the return; PN2000 stock unverified — the lane says so); the bulb rule (the DOE general-service-lamp rule; the appliance-lamp exemption — the lane's reading, plausible, not re-fetched). **Verdict: ACCEPT.** The purchase is Nick's hands after his word; THE ADOPTION FENCE stands; the Gen4 firmware update precedes ENERGY-READ.

## §3 The Activate plan — re-cut on Nick's critique (F-b6-1: four hub errors)
1. **A false statement.** The b5 email said the LLC "is forming now"; the entity record under `legal/NexSys-LLC/` dates formation to 2025-07-17 (Nick). The hub wrote a company fact without reading the record — the exact failure the premise gate was minted to stop, applied to code but not to company text. **Rule extended (pm-lessons, the b7 fold): every fact in outward text is grepped at its source — the entity record, the live program page, the claim register — before the text is handed; a claim the register does not hold does not leave the building.** (A note the hub carries, not a fact filed: the entity's name is changing for commercial use — the new name is in no tracked file, per law 9; the DR §1 records it as [name redacted].)
2. **Two register-unsupported claims.** "Runs a Zigbee device fleet" — C-001's scope fence excludes the fleet ("NOT the six-device bench fleet"), and C-003's fleet/ZDO clause is fenced on R-5B (Saturday); "explains every automation run" — an "every" claim no row grants (FENCE-BUS's class). The corrected email's product sentence is built clause-by-clause from C-001/C-002/C-003 (§3.5 below) — "adopted a mains device and a battery sensor, and fired an automation whose command the device itself confirmed".
3. **Two low-yield questions.** "Is there a track" invites a vague reply — the apply page says the principal applicant must meet 100 % of the requirements; "should I plan on Cohort 2028" rested on a wrong sum (a December 2026 degree meets four post-baccalaureate years in December 2030); the software question was already answered in the FAQ the hub did not read (re-fetched at b6: "designed for hard-tech, and we typically do not support projects that are purely software", with listed exceptions — software built into hardware products, capex-heavy, discovery-rooted). The corrected email asks two questions each quoting the live rule, so the answer is a written yes/no.
4. **The "yes" rule was incomplete.** Re-cut: **skip is the default;** apply only on a WRITTEN yes to BOTH questions by **Fri Oct 9**, and only with hours ADDED on top of the run schedule — never taken from the rehearsal or soak weeks (≈7 h/week is the whole budget; the plan's slip rule would move the run). Activate goes on a dated watch list (the plan §11): Cohort 2028's open, ~Sep 2027, and the FAQ's exception list re-read then.
5. **The corrected email** (`_scratch/v75/2026-09-17_activate_email_v2.txt`; the card, block 3): the entity as "an LLC formed in July 2025" (no name — the rename is in progress); the degree date; the experience; the product in register clauses only; question 1 the literal four-year reading with the December 2030 sum stated; question 2 the FAQ's own exception language; the Oct 9 date and the reason (the run is not displaced). The FOP's stale rows (the PhD line; the NSF fellowship listed separately) → FOP-1.
6. **The pitch item restored.** "Built to and for the market as it exists and as it is developing" enters the strategy pass (D-v75-2, after R-5B) as its first row; the plan's §11 carries it so it does not drop again.

## §4 ZIGBEE-GAPS and FOP-1 — chartered through the gate
`context/instructions/2026-09-17_research-lane_ZIGBEE-GAPS_coverage-against-the-field_charter.md`: §1's claims grepped at `3d40b5f` — `ClusterHandlers.java:36–:52` eight handlers; `ReportingConfigurator.java:65–:72`; `EndpointClassifier.java:39/:41/:95/:158/:175`; the IR rows IR-15..IR-21 present in the register; `homesynapse-core-docs/design/08-zigbee-adapter.md` exists; the ambition map at `context/research/2026-09-14_AMBITION-MAP_return.md`. The method makes "any Zigbee device in the world" a coverage number against the field's corpus at one tag (read / classify / adopt) with a gap list ranked by corpus share × fix size and an instrument per row. `context/instructions/2026-09-17_research-lane_FOP-1_company-facts-against-live-sources_charter.md`: the FOP (78,166 B at the folder root) checked claim-by-claim against live sources; the entity record under `legal/NexSys-LLC/` (exists); the known-stale rows first; the profile column read literally. Both read-only; both run beside the Java slot; neither touches Saturday.

## §5 The landing card
Block 1: core, 14 files by explicit paths (12 M + 2 A), one push → `CORE: DUR1 <sha>` → `CI: <sha> green|red`. Block 2: the hivemind card. Block 3: the corrected Activate email. Block 4: the ZIGBEE-GAPS paste. Block 5: the FOP-1 paste (a free window; not the sitting's).

## §6 Not re-executed
Gradle; the JUnit XMLs; the retailer prices; the DOE lamp rule; Activate's decision timeline ("rolling"); the entity record itself (Nick's statement — FOP-1 cites the document).
