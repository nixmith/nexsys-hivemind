<!--
file: context/audits/2026-09-17_v75-b7_CI-red-read_CI-2_ZIGBEE-GAPS_FOP-1_intakes_audit.md
purpose: THE v75 BEAT-7 AUDIT — (§1) the CI red on core `be4788a` READ AT THE INSTRUMENT: run #251's two jobs failed at their UPLOAD steps ("Artifact storage quota has been hit"), not at `check` — the DUR-1 + HASH-1 landing is not shown red by its code; the verdict stays OPEN until the `Run check` step is read (Nick's one glance) or `check` is re-run; CI-2 chartered and spliced (uploads never gate; retention cut; the .deb's 7 days kept); the artifact cleanup as Nick's hands, run #56's artifacts KEPT for Saturday's GUARD 1; (§2) ZIGBEE-GAPS intaken two-layer (five source cites re-read at HEAD; the corpus numbers accepted as the lane's with the band stated); (§3) FOP-1 intaken two-layer (HotMobile's deadline re-fetched TRUE; the entity date; Activate = NSF EF); (§4) the Activate email v3 on Nick's critique — what it may say and on which register row each sentence rests; (§5) the words packaged (D-v75-7 `GAPS:`, D-v75-8 `HOTMOBILE:`); (§6) not re-executed.
audience: the hub · Nick (§1 first) · the lanes
state-type: audit (two-layer)
status: FILED v75 beat 7 (Thu 2026-09-17 ~15:xx CT). Findings: F-b7-1 (the CI red is a storage-quota failure at the upload steps — every push reads red until the quota clears; a verdict-independent step was gating the verdict — CI-2); F-b7-2 (the b4 dossier's "±0.2 %" and the b4 divisor prediction — already F-b6; carried); F-b7-3 (the FOP's HotMobile row was wrong by four months — the deadline is Oct 9, 2026, 22 days out; a decision, not a note); F-b7-4 (ZIGBEE-GAPS: `InitializationWrite` is parsed at `ZigbeeProfileLoader.java:237` and executed by nothing in main — Doc 08 §3.6's Aqara example cannot run; a register row).
-->

# v75 beat 7 — the CI red read; CI-2; ZIGBEE-GAPS and FOP-1 intaken; the email re-cut

## §1 The CI red on `be4788a` — read at the instrument (F-b7-1)
The repository was private for a window (Nick's line; made public again Thu ~14:xx CT). Read after: the `ci.yml` listing shows run **#251** on `be4788a` (push, 2026-09-17 01:17, 3 m 55 s) — overall FAILURE; jobs **Build & Check** FAILURE (3 m 52 s) and **Bus soak (non-gating sample)** FAILURE (2 m 12 s). The run's ONLY annotations, verbatim, twice: *"Failed to CreateArtifact: Artifact storage quota has been hit. Unable to upload any new artifacts. Usage is recalculated every 6-12 hours."* — one per job, from `actions/upload-artifact` at the "Upload test reports" / "Upload bus-soak reports" steps (both `if: always()`, neither `continue-on-error`). No "Process completed with exit code 1", no failing-task or failing-test annotation. The step list itself is behind a login; **the `Run check` step's own colour is Nick's one glance (block 0 of the card), or the desk's `./gradlew check --continue`.** What the desk already showed: the gate line green in one round (the lane), and Nick's own clean build of the same tree (`./scripts/clean.sh` + the gate line, 96 tasks, `BUILD SUCCESSFUL`) — the modules `check` adds (lifecycle tests, app arch rules, config, persistence, event-model, device-model, integration) were not run on the desk, so the verdict is OPEN, not green, until the step is read. install-smoke runs on every push to main and uploads image + .deb per arch at 7 days — the likeliest bulk of the quota; its run on `be4788a` will have failed at the same step.
**CI-2 (spliced over the mount; `_scratch/v75/ci2_splice.py`; the card block 2):** `continue-on-error: true` on the three upload steps (ci.yml ×2; install-smoke.yml + its byte-identical mirror `distribution/ci/install-smoke.yml`); test-report and bus-soak retention 14 → 3 days; the .deb's 7 days KEPT (a sitting pins a run's .deb up to a week ahead and downloads it on the day — H8-a's GUARD 1 does exactly that with run #56); the architecture doc's retention sentence. Census 4 = 4 M. No code; `check` unaffected. **The quota itself** clears only by deleting artifacts — Nick's hands (the card block 1, `gh api`): everything except run #56's (`workflow_run.id 34840877827`, the arm64 zip whose sha256 GUARD 1 compares to the published `11f3a79e…`). Not chartered now, a register row: a pin-gated .deb upload (upload the artifact only when a commit or a manual run asks) — IR-27, October.
**The counter:** run #251's bus-soak XML was not stored and its step colour is unread — NOT a sample; the counter stays 6/20 until a readable green sample lands (CI-2's push is the next candidate, uploads permitting). **Saturday:** unaffected — the packets are pinned to `6bd8508`'s run #56; its artifacts exist (uploaded 09-14, 7-day retention → expire ~09-21) and must not be deleted.

## §2 ZIGBEE-GAPS — two-layer, ACCEPT
**Layer 1 (`context/research/2026-09-17_ZIGBEE-GAPS_return.md`, 15,972 B):** HEAD read at `be4788a` (nothing under `integration/` changed since `3d40b5f`) against zigbee-herdsman-converters `v26.110.0`, 4,499 definitions, the "every capability" rule: read 34.7 % · classify 33.4 % · adopt 79.7 % · **all three 29.7 %** (band 27.7–31.0 with the two assumptions stated: the 0x0302 temperature device type; `tamper` as a capability). P1 confirmed narrowly / clause 2 refuted (only dimmable lights clear 60 %; 2 of 6 rig devices pass); **P2 refuted** — full color is the largest gap (blocks 15.0 %), metering third (10.4 %); P3 partly (the quirk layer: ceiling 93.6 % with it, 72.9 % without; 565 EF00 definitions, 180 lumi, 184 vendor-cluster); P4/P5 partly. Twelve ranked rows reach 88.7 % for ≈27.5 lane-days; the two largest unlocks (color +14.5 pp, buttons +6.9) are fenced by SCOPE rulings (AMD-96, BTN-AMD), not code. The market column rests on Best Buy alone (the other retailers block fetching) — disclosed.
**Layer 2 (the hub's re-reads at HEAD, five cites):** `ColorControlHandler.java:22–23` "Full color (hue/sat, xy) is post-MVP (AMD-96); those attributes are deliberately ignored here" and `:44–50` the mireds-only arm — TRUE; `ZclIngestionUnit.java:577–579` the `else { return; }` drop of other cluster-specific commands — TRUE; `TuyaDpCodec.java:27` a marker interface with no members — TRUE; `ZigbeeProfileLoader.java:237` `parseInitializationWrites(...)` with NO consumer of `initializationWrites()` anywhere in main — TRUE (**F-b7-4 → IR-26**); `PowerConfigurationHandler.java:25/:41–53` 0x0021 only, always ÷2 — TRUE. Not re-executed: the corpus counts (the lane's script over 4,499 definitions — accepted as the lane's with its band; a re-count is a lane-day, not a hub-hour); the Best Buy column. **Verdict: ACCEPT.** The ranking is the October charters' input; the two fences are Nick's word (D-v75-7); IR-24 widens to classify-by-clusters as the lane's row 7 says.

## §3 FOP-1 — two-layer, ACCEPT
**Layer 1 (`context/research/2026-09-17_FOP-1_return.md`, 9,982 B):** 34 rows — TRUE 17 · STALE 1 · FALSE 13 · UNVERIFIABLE 3; P1 partly (both Activate lines FALSE, not stale: NSF's own page routes the Entrepreneurial Fellowship through Activate's application; no live page requires a PhD), P2 holds (formed 2025-07-17, Louisiana charter 46564936K, from the legal drafts; the FOP still plans "EIN + entity"), P3 refuted (no NSF figure moved; 26-510 predates the FOP), P4 partly. Top three: Activate = NSF EF, one application, closes Oct 30 12:00 PT; the LLC exists; **HotMobile 2027 is due Oct 9, 2026 AoE — not Jan–Mar 2027.** Other FALSE: ioXt Apr 13; SAM.gov ≤10 business days / NSF ≤3 weeks; the micro-entity provisional fee $65; CSA Adopter $3,000 per product; CEDIA supplier membership. Unverified: the LLC's current standing (a registrar web form — the lane does not submit forms; a one-minute lookup by charter number is Nick's), the IP assignment (an unsigned draft).
**Layer 2:** the HotMobile CFP re-fetched (`hotmobile.org/2027/index.php?id=cfp`): *"Hard deadline for submissions: Friday October 9th, 2026, 11:59pm AoE"*, ≤6 pages, Feb 24–25 2027, Tucson — TRUE (F-b7-3). The Activate `/apply` and FAQ facts were the hub's own reads at b5/b6 — consistent. Not re-executed: the USPTO fee schedule, the FCC ioXt date, SAM.gov, the CSA and CEDIA pages (each a dated link in the return). **Verdict: ACCEPT.** The FOP is Nick's file at the folder root; the corrections are his to apply (`FOP: apply|hold`); the hub carries the watch list into the plan §12.

## §4 The Activate email v3 — Nick's critique applied, each sentence on its source
Nick's two asks: the product paragraph must not read as "yet another smart-home project" — it should read as security, policy enforcement and tracking, auditable tracing and sourcing, and room for what comes next; and the shape is "what I'm building → who I am → where the ambiguity lies", not "make an exception for me". v3 (`_scratch/v75/2026-09-17_activate_email_v3.txt`, block 3): the field sentence uses the claim register's §0 preamble 1 only (what the field does not keep: a confirmation verdict, a reason for a non-fire, a non-purging record) — it does NOT say the field lacks a "why did it fire" answer (refutable since 09-02); the product sentences describe the DESIGN as built (event-sourced; the durable attributable record; the explain reads' three questions) and the enforcement layer as "the layer I'm building" — never "every", never a comparative, never a hardware claim beyond C-001..C-003 (installs and runs on the Pi; a Zigbee radio; third-party devices); the run date as a plan. The two questions quote the live rules; the Oct 9 date and its reason stand.

## §5 The words packaged
- **D-v75-7 `GAPS: color|buttons|both|neither`** — the two fenced unlocks (AMD-96 full color +14.5 pp; BTN-AMD buttons +6.9 pp) are scope rulings; the hub's recommendation: **neither before the run** — the critical path (R-5B → rehearsal 1 → ENERGY-READ → LINK-READ → rehearsal 2 → soak → run) has no color or button row, and each is 2–3 lane-days on the Java slot the run needs; both enter November's re-cut first. Refutable-by: a run-day scenario that needs a color bulb or a button press.
- **D-v75-8 `HOTMOBILE: submit|skip`** — a 6-page workshop paper due Oct 9 (22 days), Tucson Feb 2027; the hub's recommendation: **skip** — the paper's evidence IS the run (Oct 30 – Nov 2), which lands after the deadline; a paper written before it rests on three narrow register rows; the hours are the rehearsal weeks'. Refutable-by: Nick wanting the venue for reasons the plan does not weigh (a first publication; the advisor track). The watch list carries the 2028 cycle.

## §6 Not re-executed
The `Run check` step's colour (login-walled — Nick's glance); the artifact list and sizes (Nick's `gh` line prints them); the corpus counts; the retailers; the USPTO/FCC/SAM/CSA/CEDIA pages.
