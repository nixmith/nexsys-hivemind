<!--
file: context/audits/2026-09-17_v75-b8_close_verdict-at-the-instrument_audit.md
purpose: THE v75 CLOSE AUDIT (beat 8) — (§1) the DUR-1 + HASH-1 verdict on core `be4788a` read at the instrument: run #251's `Run check` step GREEN ("BUILD SUCCESSFUL in 3m 14s · 156 actionable tasks: 156 executed"; Nick's screenshot of the job's step list) with only "Upload test reports" red on the artifact-storage quota; the desk's `./scripts/clean.sh && ./gradlew check --no-daemon --continue` BUILD SUCCESSFUL twice — IR-22 and IR-23 RETIRE at the sha; the freeze doc's [v1.1.5] HASH NOTE re-cut from a prediction to a measured-and-fixed fact; FE-115 unblocked; (§2) the b7 card's block 1 re-cut — the GitHub CLI is not on the host (`gh: command not found`): install, sign in, list, delete; run #56's artifacts kept; (§3) the window measured; (§4) the v76 dispatch text and the plan's §13; (§5) not re-executed.
audience: the hub (v76 boots past this) · Nick (§1–§2)
state-type: audit (the close)
status: FILED v75 beat 8 (Thu 2026-09-17 ~16:xx CT). Findings: none new — F-b7-1 (the quota) closed as a CI-2 fix + the cleanup in Nick's hands.
-->

# v75 beat 8 — the close: the verdict at the instrument; the window measured

## §1 `be4788a` — GREEN at the instrument
Nick's screenshot of run #251 → "Build & Check" → the step list: **"Run check" ✓** — the log's tail `BUILD SUCCESSFUL in 3m 14s` / `156 actionable tasks: 156 executed` (the last tasks listed: `:testing:integration-tests:check`, `:testing:test-support:check`, `:web-ui:dashboard:check UP-TO-DATE`); **"Upload test reports" ✗** — `actions/upload-artifact@v6` with `name: test-reports-251`, `retention-days: 14`, "there will be 1451 files uploaded", then `Error: Failed to CreateArtifact: Artifact storage quota has been hit. Unable to upload any new artifacts. Usage is recalculated every 6-12 hours.` The desk, twice: `./scripts/clean.sh && ./gradlew check --no-daemon --continue 2>&1 | grep -E 'FAILED|BUILD' | head` → `BUILD SUCCESSFUL in 1m 44s` (no FAILED line). **Verdict: GREEN.** Consequences banked this beat: IR-22 and IR-23 RETIRED at `be4788a`; the freeze doc's [v1.1.5] HASH NOTE re-cut — the salt MEASURED (MEASURE-1) and FIXED (HASH-1, `be4788a`); the consequence for pre-fix logs stated (a definition with ≥ 2 elements in a set or map carries a per-launch `definitionKey` before `be4788a`); FE-115 unblocked (chartered at v76 b3 after the H8 real-wire read). The counter: run #251's bus-soak XML was not stored and its step colour was not read — not a sample; 6/20 stands; CI-2's push is the next candidate.

## §2 The cleanup re-cut (block 1 failed: `bash: gh: command not found`)
The GitHub CLI is not installed on Nick's host. The re-cut card: `winget install --id GitHub.cli -e` (once) → a NEW Git Bash window (PATH) → `gh auth login` (GitHub.com · HTTPS · the browser flow) → the list line (read-only; prints the count, the MB and how many of run #56's it will keep) → the delete line (permanent; the filter keeps `workflow_run.id 34840877827`; the deleted ids tee'd to `_scratch/v75/2026-09-17_artifacts-deleted.txt`). The quota recalculates within 6–12 h; with CI-2 landed, uploads failing in the meantime never redden a run. Not blocking Saturday (run #56's artifacts exist through the weekend).

## §3 The window measured (v75, Mon 09-14 ~13:1x CT → Thu 09-17 ~16:xx CT)
8 beats · the deliverable (both Saturday packets through THE PRIOR-LEDGER GATE; the sitting named) MET at b1–b2 and held untouched · 8 lanes accepted (EXPLAIN-114c, FE-114, MEASURE-1, DUR-1 + HASH-1; the plug dossier, DEVICE-SET, ZIGBEE-GAPS, FOP-1) · core landings green at the instrument: `e56f555`, `3d40b5f`, `be4788a` (+ CI-2 on the close card) · hivemind: `cc17ce7`, `a8c987d`, `553bf5b`, `0eef472`, `13e8d64`, `d02557d` + the close card · THE PREMISE GATE minted (b4) and extended to outward text (b6) · the plan of record re-cut (§0–§9) with §10–§13 · the register IR-20..IR-27 added, IR-1/2/3, IR-12/13/14, IR-22/23 retired at their shas · one scheduled navigator (Sat 09:50 CT) · hub errors owned in the record: DP-1 (114c), the `recognise` pin (FE-114), the ±0.2 % meter row and the 0x010A miss (b4), the b5 Activate email (four faults), three cites the gate caught on the hub's own charters · the boot ≈45.3 KB; the brief ≤ 10.2 KB throughout; live beats rotated once (v74 → the archive).

## §4 What v76 opens with
`context/handoff/2026-09-17_v76_dispatch-text.md` (the block Nick pastes) and the plan's §13: b1 the boot + ENERGY-READ chartered through the premise gate (the Java slot; IR-15 first; the two handlers with the divisors read at interview; IR-24 classify by clusters; IR-25; THE MEASUREMENT RECORD; the fixture = `DEVICES: core` + ZIGBEE-GAPS rows 3/7); b2 the knockout charter; Saturday silent; b3 Sunday's intakes, `HARNESS-PLUG:`, FE-115; b4 rehearsal 1's packet; the strategy pass. ENERGY-READ was deliberately NOT chartered in this close beat: a charter through the gate is an hour of source reads (`ClusterHandlers`, `EndpointClassifier`, `ReportingConfigurator`, `InterviewStateMachine`, `StandardCapabilities`, the handler template) and belongs to a fresh window, not the tail of a full one (Nick's own v74 rule).

## §5 Not re-executed
The bus-soak job's step colour (unread; not a sample); the artifact list (needs `gh`); the CI-2 push's verdict (after block 2).
