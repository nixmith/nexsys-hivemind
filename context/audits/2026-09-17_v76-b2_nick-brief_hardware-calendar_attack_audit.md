<!--
file: context/audits/2026-09-17_v76-b2_nick-brief_hardware-calendar_attack_audit.md
purpose: THE v76 BEAT-2 AUDIT — Nick's v76 b2 brief (filed beside this audit) read whole and attacked at the bytes: the words banked; the hardware calendar accepted as the window's shape; the four DEVICE-SET edits audited (one band correction — verdicts are claimed at the guaranteed 2 % figure, not the 0.5 % typical; the refutable-by did not fire); the CHAR method edit accepted with four additions and the three-meter chain confined to CHAR; §4 (the reporting volume) attacked — the arithmetic holds, three mechanism claims corrected at `3af6213`, both folds accepted (fold 2 re-homed to a core test on the hardware-free rig); §5 (the Monday divisor read) refused on a premise Nick did not flag — a HEAD interview reads no metering attribute; §6 accepted; §7 (the narrowed strategy brief) accepted. Every `file:line` grepped at core `3af6213`.
audience: the hub (the record) · Nick (§0, §3, §4, §5)
state-type: intake audit + rulings
status: FILED v76 beat 2 (Thu 2026-09-17 ~21:xx CT; instrument 2026-09-18T02:01:03Z at the beat's open)
-->

# v76 beat 2 — Nick's brief attacked: the calendar, the meters, the reporting volume, the Monday read

## §0 Card
- **`HIVE: LANDED 5bbbd14`** verified: `5bbbd14` = 7 at diff-tree (5 M + 2 A, the order); porcelain 0; push count 0. The other four HEADs unmoved.
- **The words banked (§1):** `DEVICES: extended <edits>` (the order placed, $202.21) · `GAPS: neither` · `HOTMOBILE: skip` · `FOP: apply` · `STRATEGY: after-R5B` with the narrowed brief · `CASCADE: engine` · `AMD100: ratify` · `NAME:` silence = ii · `REPIN: none`. D-v75-2/3/6/7/8 RULED in the decision record. `PROTECT: done` promised tonight — chased at the next beat if absent. `ERIK:` a line with a date goes out tonight; **the Fri 09-25 rule** (no reply → plan around the opinion) is banked; the knockout charter is token-parameterized and never waited on the opinion — the opinion gates public use, not the charter.
- **The hardware calendar (§2): ACCEPTED as the window's shape.** Wed 09-24 is the operative date; Saturday's box is inventory. No beat in this window assumes a metering act before Wednesday. Every hour to Wednesday is desk work: ENERGY-READ and the bench scenario land first.
- **DEVICE-SET's four edits (§3): ACCEPTED on stock, with one correction that reverses one sentence.** The P4460's power figure is 0.5 % TYPICAL / 2 % MAX; a class is a maximum, so the band a `within` verdict is claimed against uses 2 %: **3.03 % at 80 W** (1.53 % at the typical figure, recorded beside it as the likely band). D-v75-6's refutable-by ("Class 0.5 or better on POWER") did NOT fire; A = P4460 stands because the PN2000 is out of production, not because the spec beat it.
- **The CHAR method edit: ACCEPTED** as a change to THE MEASUREMENT RECORD's first row, with four additions (§3). **Three meters in series: for CHAR only**; every rep runs wall → A → PLUG → LAMP (one tare); B enters a rep only to resolve a dispute.
- **§4 attacked (§4 below):** the arithmetic holds (720/h per flooding leg; 155k–311k over 72 h for three plugs). Three mechanism claims corrected at `3af6213`: the flood is a POST-ADOPTION fact (HEAD configures reporting only on adoption or re-link — an unlisted device is interviewed and never configured), an adopted plug's metering frames die at `ingestion_unhandled_cluster` (not the unadopted guard), and the "certain" leg is certain for the TR3 (seMetering divisor 3,600,000 per the dossier) and conditional for the Gen4 (any divisor ≥ ~45,000 floods at 80 W; 1,000 gives 16/h). None of the four refutable-by clauses fires. **Fold 1 ACCEPTED** (IR-25 re-cut as a SIZED decision). **Fold 2 ACCEPTED and re-homed:** the instrument is a core test on `ZigbeeHardwareFreeRig` (MEASURE-2, IR-28), ridden by ENERGY-READ's coder session — a bench YAML cannot drive the ingestion path without hardware. OR-METERING-VOLUME opened.
- **§5 (the Monday read): REFUSED — `GEN4-READ: rehearsal-2`, sharpened.** The premise Nick flagged holds by construction (an IEEE absent from `adoptDevices` is interviewed, proposed, never adopted, never configured). The premise that collapses §5 is another: the interview reads NODE_DESCRIPTOR, ACTIVE_ENDPOINTS, SIMPLE_DESCRIPTOR and BASIC_ATTRIBUTES only — no 0x0B04/0x0702 attribute read exists at HEAD, so a Monday interview cannot return the divisor. The first read is the first ADOPTION after ENERGY-READ is green: Wednesday's CHAR session if the lane lands by then, else rehearsal 2. No join on the network before it.
- **§6 ACCEPTED:** ENERGY-READ chartered at b3, dispatched Friday; the bench lane's charter (the three files + CHAR + the re-written tares) at b3/b4; FE-115 opens at Sunday's intake when a lane slot is free (the cap is TWO); the Gen4 firmware 2.0.0 over Wi-Fi on Friday is free of the rig and changes nothing in ENERGY-READ (the classifier classifies by clusters present).
- **§7 ACCEPTED:** D-v75-2 RULED `after-R5B` with the one-question brief — "which number does a stranger ask for first, and which weighting makes it honest" — retail position against corpus percentage for the November ranking.
- **The ONE act:** the b2 hivemind card → `HIVE: LANDED <sha>`. Owed tonight, Nick's own list: `PROTECT: done` · the LLC lookup · the line to Erik.

## §1 The words, verbatim from the brief's §1 block
`HIVE: LANDED 5bbbd14` · `DEVICES: extended <edits — §3>` · `GAPS: neither` · `HOTMOBILE: skip` · `FOP: apply` · `STRATEGY: after-R5B — with a narrowed brief (§7)` · `CASCADE: engine` · `AMD100: ratify` · `NAME: (silence = ii, unchanged)` · `REPIN: none — Saturday stays pinned to 6bd8508's run #56`. Applied: the DR's five open rows carry the rulings; `FOP: apply` becomes a hub act (a guarded edit of `Founder_Operating_Plan_v1.md` on FOP-1's thirteen rows — queued b4); `AMD100: ratify` becomes a docs card after the hub reads AMD-100 whole (queued; not before Sunday).

## §2 The calendar and the fences
Nick's table stands as his record. Two notes. (i) THE ADOPTION FENCE names metering plugs; the SNZB-06P24 is an occupancy device and falls under the other standing rule — nothing joins the network between R-5B's re-baseline on six and the run's fleet declaration except by packet (the floor is the reason, not the fence). (ii) The Hue SML003's absence from the order is not blocking: ZIGBEE-GAPS row 7's instrument is unit tests on recorded signatures; it costs IR-18 its second vendor in October, as Nick says.

## §3 The DEVICE-SET edits and the method edit
| Edit | Ruling | The audit |
|---|---|---|
| (a) PN2000 and UT230B-US discontinued; "the spec'd consumer plug-meter tier no longer exists" | ACCEPTED on stock; the tier claim carried as an OBSERVATION with two exhibits, not a market row | Two makers' pages are two data points; a third instrument in the tier checked at its maker makes it a row. Not re-executed here (the maker pages). |
| (b) A = P4460 | ACCEPTED, with the band corrected | DEVICE-SET's own P4460 row reads "W 0.5 % typ, 2 % max" [p3m]. The BAND formula (DEVICE-SET :55) takes the meter's CLASS, a maximum; the PN2000's Class 1.0 is a maximum. So: **max band 2.0 + 0.5/80 + 1.6·0.3/120 = 2.00 + 0.63 + 0.40 = 3.03 %**; typical band 1.53 %; as-written (PN2000, 40 W) 2.65 %. A `within` verdict is claimed at 3.03 %; 1.53 % is recorded as the likely band. The TR3 adds its maker's 1 % (DEVICE-SET :55) — Nick's formula dropped that term; the Gen4 adds 0. "The refutable-by fired from inside the order" is withdrawn: 0.5 % typical is not Class 0.5. |
| (c) B = two no-spec generics of different brands | ACCEPTED, one caveat | The 100× ambiguity needs no spec — right. "If both Bs agree and disagree with A, A is the odd one out" holds only if B1 and B2 are independent; sub-$25 meters often share an OEM board under different brands. CHAR records whether B1's and B2's offsets from A are the same number — a shared-design tell — before either is trusted as a second vote. |
| (d) the load 80 W | ACCEPTED | 0.67 A at 3.3× the P4460's 0.2 A floor. The clamp-socket check (ceramic; rated above the lamp) is Nick's Wednesday line. |
| CHAR replaces XCHK | ACCEPTED as a change to THE MEASUREMENT RECORD's first row | Four additions: (i) the threshold is named — a rep is `reference-disputed` when \|B − A\| exceeds B's recorded offset by more than 2× B's recorded spread (k = 2, refutable at CHAR if the spread is not stable); (ii) CHAR's 20 readings start after the lamp's thermal settle (≥ 5 min on), separate from the hour's burn-in; (iii) the B1/B2 offset correlation recorded (row (c)); (iv) CHAR brackets each rep session — once before, once after — so drift is a number. "Record the offset; never correct for it" stands as written. |
| The chain gains a meter (three tares) | CHAR ONLY | The reps run wall → A → PLUG → LAMP: one tare (the plug's self, the lamp unplugged), A's reading against the plug's. B1 (then B2) enters a rep only when a reading sits outside the band, to attribute the dispute. T1 is re-written by the bench lane for two chains: the CHAR chain (three tares) and the rep chain (one). |

## §4 The reporting volume — the row attacked
**Verified at `3af6213`:** `ReportingConfigurator.java:71–:72` — `0x0B04 → DefaultRow(0x050B, 0x29, 5, 3600, 10)`, `0x0702 → DefaultRow(0x0000, 0x25, 5, 3600, 5)` — as Nick reads them. `ClusterHandlers.forDevice` (:35–:52) holds eight handlers (0x0006, 0x0008, 0x0300, 0x0406, 0x0001, 0x0402, 0x0405, 0x0500); neither metering cluster. The divisor grep over main sources hits TWO files, `BackoffParameters.java` and `StandardIntegrationSupervisor.java` — both backoff multipliers, neither a ZCL scale; the substance ("no scaling layer") holds. `BusSoakIT.java` is K = 20 (`DEFAULT_LOOPS`, :110) hero loops printing p50/p99/max, anomalies and the position census (:62–:68): no rate, growth or heap dimension. The bench's `scenarios/*.yaml` (twelve files) carry no rate or growth scenario. `interviewSkips` is a per-profile YAML field (`ZigbeeProfileLoader.java:235`, `DeviceProfile.java:66`); `SKIP_CONFIGURE_REPORTING = "configure_reporting"` (`ReportingConfigurator.java:57`, consumed :102–:104).

**Corrected at `3af6213`:**
1. **The flood is a post-adoption fact.** `ZigbeeIntegrationAdapter.adoptIfAccepted` (:979–:996): a LINKED outcome (a re-join of an adopted device) drives `onRejoin`; a fresh proposal whose IEEE is NOT in the config's `adoptDevices` list returns before any drive; a listed, COMPLETE interview adopts (:995) and then `driveReporting` (:1009) runs `configureDevice` (:1015). So HEAD asks nothing of a plug it has not adopted; "HEAD already asks a metered plug to flood it" is false today and becomes true the day ENERGY-READ adopts one. THE ADOPTION FENCE does exactly the work its name says.
2. **Where an adopted plug's frames die:** `ZclIngestionUnit.java:540` (`zigbee.ingestion_unhandled_cluster`) — the no-handler branch — not the unadopted guard at :586–:593, which fires only for a device with no adopted entity.
3. **"CERTAIN" is certain for one plug of three.** The 0x0702 leg reports at the 5-s floor when 5 raw accrues inside 5 s at 80 W, i.e. when the seMetering divisor is ≥ ~45,000 (kWh base). The TR3's divisor is 3,600,000 per z2m's override (PLUG-DOSSIER row 29) — certain. The Gen4's divisor is unread (the dossier: `m.electricityMeter()` reads it at join) — at 1,000 the leg gives ~16/h; at 1,000,000 it floods. The "1e6" in the brief is an assumption, not a read. The 0x0B04 leg stays as Nick has it: 720/h at divisor 100, quiet at 1, edges only at 10.

**The four refutable-by clauses:** (1) no device profile puts `configure_reporting` in `interviewSkips` for a metering plug, and an unadopted device is never configured regardless — does not fire (and would not shrink the post-adoption row); (2) unread — the first adoption reads it; (3) no existing instrument — does not fire; (4) no headroom number exists — the hub cannot say so with a number. The row stands as OR-METERING-VOLUME (pm-handoff Open Risks).

**The folds:** fold 1 ACCEPTED — IR-25's row re-cut: the charter states predicted frames/h/device at each candidate divisor before and after the scaling and chooses the engineering-unit thresholds deliberately (the max interval too — the run's fourth question wants a reading inside a bounded window after a command, not once an hour). Fold 2 ACCEPTED and re-homed as IR-28 / MEASURE-2: a test on `ZigbeeHardwareFreeRig` (core testFixtures) driving N synthetic metering reports per second for M minutes through the ingestion path, recording events/s sustained, store growth per hour and heap over time — ridden by ENERGY-READ's coder session once the handlers exist (the Java slot runs in series). "Metering is the large majority of the run's event volume" is compared against R-5B's fleet floor on Sunday, when that number exists.

## §5 The Monday divisor read
The interview state machine's steps at `3af6213` are `NODE_DESCRIPTOR, ACTIVE_ENDPOINTS, SIMPLE_DESCRIPTOR, BASIC_ATTRIBUTES` (`InterviewStateMachine.java:52–:57`); `readBasic` (:160) returns manufacturer, model and power source. No step reads 0x0B04 or 0x0702 attributes; ENERGY-READ adds that read. A Monday interview therefore yields the device type (0x010A, the print IR-20(a) wants), the cluster list at firmware 2.0.0 (the 0x0400 question) and the Basic strings — and not the divisor. The device type and the cluster list are already in the record (PLUG-DOSSIER row 16, the feature-matrix row; the ZHA signature); the classifier will classify by clusters present, so 0x0400's presence changes no code. What the act would cost: a join and a leave on the production network after the sitting, before C-003's clause, and a fence exception minted for a read that returns nothing the charter needs. **RULED `GEN4-READ: rehearsal-2`, sharpened:** the divisor is read at the first adoption after ENERGY-READ is green — Wednesday's CHAR session if the lane has landed, else rehearsal 2. The interview/adoption distinction is recorded as HEAD's behavior (an unlisted device is interviewed, proposed, never adopted) and mints no new exception. Nick's #4 (a join before C-003's ZDO read) is moot under this ruling; if a join ever precedes the clause, the nightly's samples after it are flagged. Refutable-by: a HEAD path that reads a metering attribute at interview — the grep says none.

## §6 The window and the lanes
Two lanes + hands, as the cap says. **The Java slot:** ENERGY-READ chartered at b3 through THE PREMISE GATE (the greps: `EndpointClassifier.java:41` `DEVICE_TYPE_SMART_PLUG = 0x0051`, `:99–:101` the SWITCH branch, `:175–:178` `iasCapability` CONTACT-else-MOTION, `:186` the fallback's `hasOnOff` — all read at `3af6213` this beat; the handler template, `StandardCapabilities`, the module-info at b3), dispatched Friday, landing before Wednesday; IR-15 and IR-24 in the one classifier pass as Nick says; IR-25 sized; MEASURE-2 rides. **Slot 2, the bench lane:** `field_within`, `metering-known-load.yaml` (CHAR, the two chains, the re-written T1, `requires: [operator]`), the `link-quality.yaml` skeleton — chartered at b3/b4, dry-run and selftests only. **FE-115** opens at Sunday's intake when a slot is free. **The knockout charter** on a free beat; it does not wait on Erik. **The firmware update** Friday over Wi-Fi: accepted; the dossier's signature was read below 2.0.0 and the first adoption re-reads it.

## §7 What the hub could not re-execute (disclosed)
The maker pages behind (a); the P4460 manual's 0.1 W resolution (Nick verifies at the display 09-24); the two Amazon orders; the Gen4's own divisor (the first adoption). Every code claim above was grepped at `3af6213`; every record claim at the filed return.
