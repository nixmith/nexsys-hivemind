<!--
file: context/audits/2026-09-18_v76-b5_ENERGY-READ_intake_two-layer-audit.md
purpose: THE v76 BEAT-5 AUDIT — ENERGY-READ's return intaken two-layer: the lane's claims read critically, then the hub's own re-execution at the bytes from the device VM (the tree at porcelain, the JUnit XMLs, the MEASURE-2 tee, the diffs of every production file, an independent emulation of the HEAD encoding defect); the six [REVIEW] deviations ruled; the hub's own charter misses owned (the P1 arithmetic, the Files-table omission behind R-1, the posture-note conflict behind R-2, the `-D` line behind R-3, two cites off by two lines); the verdict ACCEPT; the core card cut (22 = 16 M + 6 A); THE MEASUREMENT RECORD's instrument cells corrected and two rows added; OR-METERING-VOLUME re-cut on the wire finding; `PROTECT: done` and `LETTERS: RETURNED` banked.
audience: the hub (the record) · Nick (§0; the card) · the next Java-slot session (§5 — ENERGY-READ-b)
state-type: intake audit (two-layer)
status: FILED v76 beat 5 (Fri 2026-09-18 ~15:5x CT; instrument 2026-09-18T20:43:25Z at the beat's open)
-->

# v76 beat 5 — ENERGY-READ intaken, ACCEPT; the core card

## §0 Card
- **Verdict: ACCEPT.** The return (`context/audits/2026-09-18_ENERGY-READ_return.md`, 17,555 B, `RETURNED` last line) exists at the named path; the tree is exactly the Files table's 22 core rows (16 M + 6 A; the main diff = rows 1–11 only); the gate line's instruments verified at the bytes (§1); every production diff read (§2); the six deviations ruled (§3); the hub's own misses owned (§4). **The card: core 22 = 16 M + 6 A → `CORE: ER <sha>` → `CI: <sha> green|red`** — the closure counter's eighth sample rides the push.
- **What the lane found that the record did not know — R-4, a HEAD wire defect, verified independently:** `EzspReportingOps.configureReporting` shifted an `int` by `8 × i` for a six-byte field; Java takes the shift distance mod 32, so bytes 4–5 repeated bytes 0–1. The hub's own emulation (Python, the JLS rule): a change of 5 encodes as `05 00 00 00 05 00` at HEAD (= 21,474,836,485 raw); 5,000 as `88 13 00 00 88 13`; the fix (`long`) gives `05 00 00 00 00 00` and `88 13 00 00 00 00`. **Consequence for the record:** HEAD's 0x0702 row never configured the 5-raw threshold the b2/b3 record reasoned about — the device would have received 2.1 × 10¹⁰ raw and reported only at the 3600-s maximum; the read-back parse folded the same way, so a mis-encoded threshold read back as `verified` (the vacuous-verify pairing, arc 19). The flood arithmetic stands as the INTENDED configuration; the wire fact is corrected in OR-METERING-VOLUME. The uint48 width has never met silicon — THE MEASUREMENT RECORD gains row G4-4 (the read-back at the first adoption).
- **MEASURE-2 (IR-28), read at the bytes:** `rate=24031.1 frames=3600 events=3600 dropped=0 … heap 9 → 11 MB … store=in_memory_recording_publisher`. The rate leg of OR-METERING-VOLUME closes with margin — the adapter path ingests ~24,000 metering reports per second JIT-cold on the desk against the run's worst case of ~1.2 per second (311k over 72 h). **M2-2 is NOT a store measurement:** the "store" is the test tree's `RecordingEventPublisher` and the bytes are a UTF-8 payload floor (30 B per event) — SQLite is unreachable from this module (LTD-17). The storage leg moves to THE SOAK, with a lifecycle-module MEASURE-2b (row M2-2b, OPEN) as the desk instrument if the soak's number is wanted earlier.
- **The design held at the bytes:** the formatting is read on the adoption drive (`ReportingConfigurator.meteringFormatting`, one frame per cluster, the cache on re-link — T4 asserts the frame ORDER and zero reads on re-link); a handler is constructed only with a present pair (`ClusterHandlers.forDevice` 10/9/8); `MeteringFormatting`'s canonical constructor zeroes any invalid pair (a zero multiplier or divisor, a value above uint16/uint24) so a bad answer is "unknown", never a scale; the scaled change is `round(1 W × div / mult)` capped at the int16 field's 0x7FFF and `round(5 Wh × div / (1000 × mult))` in the kWh base, never below 1; the raw rides beside every scaled value (`rawProtocolValue`, the formatting note in `rawProtocolUnit`).
- **Words:** `PROTECT: done` BANKED — the screenshot shows the ruleset on `main` with `Restrict deletions` and `Block force pushes` checked (one repo at the instrument; the second on Nick's word — disclosed). `LETTERS: RETURNED _scratch/v76/2026-09-18_EXT-REVIEW-1_letters-verbatim.md 114222` — filed verbatim beside the packet at `context/research/2026-09-18_EXT-REVIEW-1_letters-verbatim.md`.
- **The ONE act:** the packet — block 1 the core card (`CORE: ER <sha>`); block 2 the b5 hivemind card (`HIVE: LANDED <sha>`). Then `CI: <sha> green|red` when the run finishes (the run number and the `Run check` step colour).

## §1 Layer 2 — the instruments re-read from the device VM
| Claim (the return) | The hub's re-execution | Result |
|---|---|---|
| the census: core 22 = 16 M + 6 A, rows 1–22; the main diff rows 1–11 | `git status --porcelain` in core; `git diff --stat -- '*/src/main/*'` | 16 ` M` + 6 `??`, every path under `integration/integration-zigbee/`; 8 M + 3 new main files (rows 1–11): 869 insertions, 55 deletions |
| the gate line green; zigbee 659 tests, 1 skipped; app 30 incl. ArchRules; XMLs at 19:39:03Z | the 80 JUnit XMLs under `integration/integration-zigbee/build/test-results/test/` summed; the app's ArchRules XML | tests 659 · skipped 1 · failures 0 · errors 0 · newest timestamp 2026-09-18T19:39:03; `HomeSynapseArchRulesTest` 11/0/0 at 19:39:07; `MeteringRateMeasurementTest` XML 1/1 skipped (the gate run) |
| MEASURE-2's line | `_scratch/v76/2026-09-18_MEASURE-2_rate.txt` | the one `MEASURE2` line, verbatim as the return quotes it |
| M2-1..3 filled | `measurement-record.md` at `6384e64` (Nick's b4 card carried the coder's edit) | filled with the values and their limits disclosed in the cells |
| R-4's arithmetic | a Python emulation of `(int) >> (8*i)` with the distance mod 32 for width 6 | change 5 → `05 00 00 00 05 00`; 5,000 → `88 13 00 00 88 13`; the `long` form → upper bytes zero — the claim holds |
| the return exists, `RETURNED` last | `wc -c`; `tail -1` | 17,555 B; `RETURNED nexsys-hivemind/context/audits/2026-09-18_ENERGY-READ_return.md 17555` |
Not re-executed: the gate line itself (Gradle runs on Nick's host; the device VM has a JDK but no offline Gradle cache — the XMLs are the instrument); the red-first run (657 → 50 red at 19:22:35Z — the return's own tails).

## §2 Layer 2 — the diffs read
- **`EzspReportingOps`** — `readAttributes` on the `zclGlobalExchange` seam (one frame, the ids little-endian, a failed record absent from the decoded map — the codec's discipline); the encode widened to `long` before the shift; the read-back accumulates in `long` and saturates at `Integer.MAX_VALUE` (a saturated read-back can never equal a configured change — it surfaces as read-back-differs, the honest outcome). Two javadoc lines on `analogChangeWidth` rewritten to say the uint48 width is unverified on silicon.
- **`ReportingConfigurator`** — the two metering clusters REMOVED from `DEFAULTS` (a raw change is a different wattage per divisor); `METERING_ROWS` (0x0B04 `ActivePower` int16 5–600 s; 0x0702 summation uint48 5–3600 s); `FormattingStore` (cached / learned) with a `NO_STORE` default; `meteringFormatting()` reads only the clusters present and only what the cache lacks, logs `zigbee.metering_formatting_read` (source=device|cache) or WARN `metering_formatting_unreadable`, and hands the read pair to the store; `configureMetering` refuses an unreadable pair (a NONE-class fact, R3's text verbatim) and a non-kWh unit (WARN `metering_unit_unsupported`); profile overrides apply to min/max only. A `configure_reporting`-skip profile gets NO read either — consistent with the no-commands contract.
- **`MeteringFormatting`** — the record with the sanitizing constructor; `ofReads` bounds (uint16 for the 0x0B04 pairs, uint24 for the 0x0702 pairs, enum8 for the unit); `filledFrom` (the cache filled from a read); `agreedAcross` (R-6); the notes.
- **The two handlers** — the template's shape; `power_w`/`voltage_v`/`current_a` scaled `raw × mult / div`; the int16 sentinel 0x8000 and band; the uint16 sentinel 0xFFFF; the uint48 band; the unit guard emits nothing and says why.
- **`ClusterHandlers.forDevice`** — a `LinkedHashMap` (the `Map.of` ten-entry limit noted in the charter's §9); the eight existing handlers in their order; the two metering handlers only with a present pair.
- **`EndpointClassifier`** — `iasCapability` as a `switch` over the five zone types (CONTACT → contact, MOTION → motion, the three → `binaryState`; `null` → motion, as before); `withMeters` after the type switch and the fallback, before `withIdentify`; the metering-only case → `SENSOR` (R-5); the INFO line in `classify` (R-1).
- **`ZclIngestionUnit`** — the per-endpoint formatting seed and hook; `recordLearnedMeteringFormatting` invalidates the device's table itself; `deviceFormatting` = `agreedAcross` the endpoints; `nameUnscaledOnce` (one DEBUG per device+cluster).
- **`ZigbeeDeviceCache`** — the `learnedMeteringFormatting` JSON section (per IEEE → per endpoint, `TreeMap` for a stable order), loaded with the absent-section tolerance; the rehydration INFO `zigbee.learned_metering_formatting_rehydrated: count=`.
- **`ZigbeeIntegrationAdapter`** — the seed passed at construction; the `FormattingStore` wired to both the cache and the ingestion unit (the ingestion hook's own invalidation makes the order safe: `onAdopted` invalidates before the drive reads; the learned pair invalidates again).
- **The tests** — the DP-6 limitation pin (`iasNonContactLearned_staysMotion`) RE-PINNED to `binary_state`, its old assertion removed in the open (the diff shows it); T4's frame order asserted as a byte sequence (`0x00, 0x0B, 0x05, 0x29, 0x05, 0x00, 0x58, 0x02, 0x64, 0x00` — the two reads, then the configures) and the six-byte change `88 13 00 00 00 00`; P7's zero read frames on re-link; the boot glance-point "nothing persisted before the first read".
- **`MODULE_CONTEXT.md`** — the ENERGY-READ section: +3 package-private types; the two paragraphs the charter asked for; the uint48 caveat.

## §3 The six [REVIEW] deviations, ruled
| Row | The lane | Ruling |
|---|---|---|
| R-1 the R6 line's site | the Files table (which governs) had no row for `ZigbeeAdoptionSlice.java`; the line is emitted from `classify` without `device=`; the adjacent `zigbee.proposal_accepted: device=` names the device on the same thread | ACCEPT — the hub's omission (§4). **ENERGY-READ-b row 1:** the six-line call-site log in the slice with `device=`; rides the next Java-slot session (LINK-READ). G4-3's read on Thursday uses the adjacency. |
| R-2 a verified fact keeps `note == null` | `ConfirmationOverrideInstaller.verifiedFact` counts note-free VERIFIED facts; a note would have rendered every healthy meter `degraded` | ACCEPT — the hub's conflict (§4). The instrument for G4-1/G4-2 is the INFO line `zigbee.metering_formatting_read` — THE MEASUREMENT RECORD's instrument cells corrected this beat. |
| R-3 the MEASURE-2 line skips | `-D` never reaches the forked test JVM (build-logic forwards only `homesynapse.soak.loops`); the env `HOMESYNAPSE_MEASURE2=true` + `--rerun` produced the reading | ACCEPT — the hub's line (§4). The lesson: a test knob in a charter's command is an ENV VAR (the BusSoakIT idiom) and a re-run carries `--rerun`; pm-lessons this beat; W-SKILLS-10 folds it into the coder-session prompt checklist as (e). |
| R-4 the uint48 wire defect | fixed in the lane, measured at the wire; the read-back widened and saturating | ACCEPT as a DEFECT FOUND AND FIXED — verified by the hub's own emulation (§0). OR-METERING-VOLUME's status re-cut; row G4-4 (the read-back on silicon) added; the b2/b3 flood arithmetic annotated as the intended configuration. |
| R-5 `SENSOR` for a metering-only endpoint | the model defines `ENERGY_METER` (required `EnergyMeter`, optional `PowerMeter`); nothing validates composition today (AMD-44 deferred) | RULED: **`ENERGY_METER` when the endpoint lists 0x0702 and no other arm classified it; `SENSOR` when it lists 0x0B04 only** (a power measurement without an energy register). One line in `withMeters` → **ENERGY-READ-b row 2**. No fleet device is metering-only; the Gen4 and the TR3 are switches with meters. |
| R-6 per-device table, per-endpoint formatting | `agreedAcross`: a pair is present iff every endpoint holding it agrees; a split pair is silence + the DEBUG | ACCEPT; **IR-32** (a per-endpoint handler table for multi-endpoint metering devices — a power strip; October). Exact for every device in the fleet. |
The nine [INFO] rows: read; none changes a ruling. (1) `readAttributes` abstract, not `default` — accepted, two implementers. (4) the MEASURE2 line's four extra self-describing fields — accepted; the record's cells name them. (6) a skip profile gets no read — accepted. (7) the int16 cap — accepted.

## §4 The hub's charter misses, owned
1. **P1's arithmetic:** "23 = 13 M + 10 A" — the table's own rows are 17 M + 6 A. A count typed, not computed from the table.
2. **The Files table omitted `ZigbeeAdoptionSlice.java`** while §4 R6 and §6 placed the log there — the table governs, so the line landed without `device=` (R-1).
3. **R3's note-always wording** collided with `verifiedFact`'s note-free rule the charter's own read-set (`ConfirmationOverrideInstaller`) would have shown (R-2).
4. **The MEASURE-2 command used `-D`** against a build that forwards only one property; the BusSoakIT idiom in the same tree was the model (R-3).
5. **Two cites off by two lines:** `encodeReadAttributes` is `:163–:173`, not `:165–:174`; the second the return names in its §0.
None changed the outcome; each cost the lane a paragraph. The lesson is one line: compute the census from the table; grep the read-set's own rules before writing a rule against them.

## §5 ENERGY-READ-b (the next Java-slot session carries it; ≤ 1 hour)
Row 1 — the call-site log in `ZigbeeAdoptionSlice.java:377–:378` with `device=`, the `classify` line kept or demoted to DEBUG. Row 2 — `withMeters`: an empty classification with 0x0702 → `EntityType.ENERGY_METER`; with 0x0B04 only → `SENSOR`; one test each. Row 3 — none. Rides LINK-READ's session as its §4 row 0; no separate card.

## §6 The landing
Core: 22 = 16 M + 6 A by explicit paths; the message `feat(integration-zigbee): ENERGY-READ — …` in Nick's form (`_scratch/v76/2026-09-18_core_ENERGY-READ_commit-msg.txt`); the push → CI (`ci.yml` + `install-smoke`) → `CI: <sha> green|red`; the counter's eighth sample on the bus-soak step. THE ADOPTION FENCE lifts when CI is green: the first metering adoption is Thu 09-24's CHAR session (G4-1..4, REP-1).
