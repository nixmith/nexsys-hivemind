<!--
file: context/audits/2026-09-18_v76-b7_BENCH-METER-1_intake_two-layer-audit_and-the-re-close.md
purpose: THE v76 BEAT-7 AUDIT, RE-CUT BEFORE EITHER CARD LANDED (b7c, Fri 2026-09-18 evening) — BENCH-METER-1's return intaken two-layer (the tree at porcelain 4 M + 2 A; the three instruments RE-RUN by the hub: 31/0, 43/0, 13 legs), then Nick's critique of the hub's first cut taken as the audit's third layer and verified at the bytes: the hub's claim that the datum's simultaneity "holds by construction" was FALSE (the engine orders the read within 10 s of ENTER; it cannot know the value is fresh) — WITHDRAWN and replaced by a per-rep load step in the operator's hands; F-1 re-ruled (drop `operator` from `requires:`); F-2 split into the record's two subtractions (the plug's no-load OFFSET from the field, the TARE from the reference); F-3 kept with VOID as a per-rep result; F-4 WITHDRAWN (`provenance:` is global); the Thursday order stated ONCE (the Pi's clone pulled Mon/Tue and one nightly watched → adoption → the constants re-mint incl. the fleet counts and ULIDs + the hero-less variant and basis regenerated → the scenario in tmux, CHAR-BEFORE is the CHAR); BENCH-METER-1b re-chartered wholesale (a half-day); the bench commit message re-cut (`bench:` subject; no walk claim); the card's trailer grep made a gate.
audience: the hub (the record; v77's boot) · Nick (§0; the packet) · the bench lane (§4)
state-type: intake audit (two-layer + the critique layer) + close
status: FILED v76 beat 7 (first cut Fri 2026-09-18 ~19:4x CT, instrument 2026-09-19T00:31:30Z); RE-CUT b7c (Fri 2026-09-18 ~20:5x CT, instrument 2026-09-19T01:56:18Z) — nothing had landed; both cards re-issued.
-->

# v76 beat 7 — BENCH-METER-1 intaken, ACCEPT; the hub's first cut corrected on Nick's critique; the cards re-issued

## §0 Card
- **`HIVE: LANDED 11a684b`** verified (11 at diff-tree). **Verdict on BENCH-METER-1: ACCEPT** — the lane's tree is exactly its Files table (4 M + 2 A at `f3631cb`), the return exists at its path (8,948 B, `RETURNED` last), and the three instruments reproduce on the device VM: `test_engine.py` → `31 check(s), 0 failure(s)`; `nightly_digest.py --selftest` → `43 check(s), 0 failure(s)`; `runner.py suite all --list` → 13 legs, `all load lawfully`. It lands AS RETURNED; its known rows go to BENCH-METER-1b (§4) — landing the return and fixing on top is the honest history.
- **The hub's error, owned (layer 3 = Nick's critique, every point verified at the bytes — §2):** the first cut said the datum's simultaneity "holds by construction". It does not. `ensure_operator_lets` (`engine.py:944`) captures the typed entry before the line's `within:` clock, so the platform's read lands within 10 s of ENTER — that is ORDER. Freshness is a different property: the plug reports `ActivePower` on a ≥ 1 W change or a 5–600 s window (IR-25 as sized at ENERGY-READ), `EntityState` carries entity-level `lastReported`/`lastUpdated` only (`core/state-store/.../EntityState.java:87` — no per-attribute stamp; an energy report refreshes it too), and the TR3's power is "delayed for almost 1 minutes" (DEVICE-SET return `:25`, z2m). So rep 1 after T1 can read the ~0 W era with the lamp lit, inside the 10 s, and PASS or FAIL on a stale value. The lane's walk proved order only, and it walked a synthetic scenario (`test_engine.py:691 OPERATOR_SCENARIO`) — the real file's walk was scratch (the return `:57`), so the first bench message's "the scenario walk" was wrong too. DEVICE-SET §2 (`:47–:56`) had the answer all along: a relay cycle per rep and an anchor frame ≥ 5 s after the on-edge. **The fix needs no engine feature:** every rep begins with a LOAD STEP in the operator's hands (the plug's relay OFF on the dashboard, ≥ 15 s, ON — or the lamp out and back), the operator WAITS until the dashboard's `power_w` for that plug shows the lit value again (the TR3 up to a minute), then ≥ 5 s more, then reads A, types, ENTER — the engine's 10-s read then reads a post-step value. The scenario's prompts carry it (1b, §4); the receipt records the entity's timestamp beside `read_at` as a witness, never an assert.
- **The rulings, re-cut:** **F-1** `operator` is NOT a capability and never needed to be: an OPERATOR-tier leg never enters the nightly (`runner.py:104 auto_preflight_refusals`; `suite auto` refuses the tier at `:119–:121`), so the flag was a phantom gate — 1b DROPS `operator` from `requires:` (`metering-known-load.yaml:78`); no `operator: true` at the re-mint. **F-2** the first cut conflated two offsets. The record's DIV row is `r = (power_w − OFFSET) / (A_W − TARE)` (DEVICE-SET `:54`): OFFSET is the PLUG'S OWN METER with no load (T2: "the platform's power_w = OFFSET (expect 0.0)"), subtracted from the FIELD; TARE is the plug's own draw as A sees it, subtracted from the REFERENCE; B's CHAR offsets are a third thing and are never subtracted. So `field_within` gains TWO keys with the side in the name — `field_subtract:` and `reference_subtract:` — never one `subtract:`. And the tare sits below A's 0.2 A spec floor (the b2 audit `:35`; 80 W is 0.67 A, the plug's own draw is ~0.005 A), so T1 measures it BY DIFFERENCE at the lit load (A with the lamp direct → W1; A with the lamp through the plug, relay ON → W2; the tare = W2 − W1, typed, `min: 0`); the plug's no-load `power_w` is typed from the dashboard at T1; A's volts twice 10 s apart are typed for the band's ΔV term (recorded, not asserted). A lint cannot check typed values; a non-positive effective reference is decided at EVALUATION as a per-rep VOID — never REFUSED: a mid-run `LintRefusal` returns before the bundle is written (`engine.py:2036–:2040` vs the write at `:2050`) and the typed session would be lost. The subtraction is exact-decimal on both operands (`as_decimal`, `engine.py:1927` — `Decimal(repr(float))`; the typed value is a float from `parse_operator_number:1978`): A = 79.6, TARE = 0.7, field = 81.29067 is r = 1.0303 EXACTLY (3.0300 %, WITHIN at 3.03, the edge inclusive); a float subtraction gives 78.89999999999999 → OUTSIDE by rounding alone — 1b's test. **F-3** kept: `on_outside: record` (OPERATOR tier only; default `fail`); OUTSIDE and VOID are per-rep RESULTS printed as read and recorded (the receipt's `verdict: WITHIN|OUTSIDE|VOID` — the engine's key at `:1511`, never `within: false`); a missing `power_w` inside the line's `within:` is VOID under `record`; the run continues; the close FAILs if any rep is OUTSIDE or VOID; B attributes AFTER CHAR-AFTER. **F-4 WITHDRAWN:** `check_ulid_provenance` is GLOBAL (`engine.py:729–:758`: every run refuses when a declared ULID is absent from `/api/v1/entities`) — plug ULIDs there would refuse every nightly leg the day a plug is re-adopted; the three ULIDs go into `metering.plug-entity` (the scenario's only consumer) and nowhere else in `provenance:`.
- **Thursday turns Friday's nightly red unless the re-mint is whole:** `boot-health.yaml:40/:44` assert `devices=${C.fleet.devices} entities=${C.fleet.entities}`, `:57–:58` assert `rows: ${C.fleet.entities}` and `ulids: ${C.remembered-ulids}` — and `ulids:` is SET EQUALITY (`engine.py:1419 if seen != set(arg)`). Adopting three plugs changes all four. Adoption itself is a config edit — `integrations.zigbee.adopt_devices` (`ZigbeeIntegrationAdapter.java:146`) in the carrier `~/hs-bench/config/homesynapse.yaml` (`constants.yaml:369`) — so the nightly's branch-B drift guard (`nightly.sh:172–:176`, `cmp` carrier vs live-basis) reports `UNQUIESCED(CONFIG-DRIFT)` until the hero-less variant and the live-basis are regenerated by the operator (`constants.yaml:373–:378`). And nothing had copied the new engine to the Pi: the deploy method of record is a pull of `~/nexsys-bench` (the B2 return `:160`), and the Pi's banner `runner <ENGINE_VERSION> @ <sha>` (`engine.py:96`) names what runs — the sha, since R-5 never bumped the label.
- **THE THURSDAY ORDER (one order; every other text points here):** (0) Mon/Tue, after 1b's intake: the Pi's clone pulled to 1b's sha; the banner read on the Pi; ONE nightly watched green on the new engine before hardware day. (1) Thu: adopt the three plugs (`adopt_devices` +3); G4-1..G4-4 are read from the adoption's JOURNAL lines (the record's cells, unchanged — not the bundle). (2) The constants re-mint (the hub's edit, Nick's card, the Pi's pull): `metering-plug: true`; the three ULIDs into `metering.plug-entity`; `fleet.devices`, `fleet.entities`, `remembered-ulids` as `/api/v1/entities` lists them AFTER the adoption — never predicted; `provenance:` untouched; then the hero-less variant + live-basis regenerated. (3) The scenario at an attended TTY inside `tmux`: CHAR-BEFORE IS THE CHAR (no separate CHAR session) → per plug T1 (tare by difference, the no-load `power_w`, the volts) + three reps (a load step each) → CHAR-AFTER → the bundle; OUTSIDE/VOID reps attributed with B after. (4) Fri: the digest read — boot-health green on the new counts, `[OK] drift check`.
- **The cards, re-issued:** the bench message's subject is `bench: BENCH-METER-1 — …` (the repo's convention — 1b's baseline check reads it) and no longer claims a scenario walk; the trailer grep on both cards is now a GATE (`if grep -q … then STOP else commit`), not a print before `;` — the lesson filed for `splice_lib_v2`. **THE MEASUREMENT RECORD:** REP-1's cell names the bundle's receipts and the sheet only — there is no "journal `state_reported`" line, and no key that does not exist yet.
- **The ONE act:** the packet `_scratch/v76/2026-09-18_v76-b7c_packet.md` — block 1 the bench card (6) → `BENCH: BM1 <sha>`; block 2 this beat's hivemind card (11 = 8 M + 3 A) → `HIVE: LANDED <sha>`; block 3 the BENCH-METER-1b paste after block 1 lands (a half-day lane; any time before Tuesday); block 4 Sunday the v77 paste.

## §1 Layer 1 — the lane's claims, read
P1–P4 as claimed, with the lane's own two corrections (P3: the listing shows `requires`, not availability; P4: `git diff` never shows untracked files — porcelain does). The red-first arithmetic honest (12 red on the old engine; 2 left waiting on the files; then 31/0). The dry run: `SKIPPED: [operator], [metering-plug]`. Its four findings were right to raise; the hub's first rulings on three of them were wrong (§0).

## §2 Layer 2 and 3 — beyond the instruments; the critique verified
- `engine.py`: `ENGINE_VERSION = "BENCH-METER-1-2026-09-18-field-within"` (`:91`); `FIELD_WITHIN_KEYS` (`:81`); `lint_field_within` (`:262`); `ensure_operator_lets` / `capture_operator_let` (`:944` / `:984` — lazy, before the line's clock; a non-number refused and asked again); `eval_field_within` (`:1490`; OUTSIDE → `"fail"` at `:1486` — fail-fast); `field_within_check` (`:1942`, `Decimal`); `unmet_requirements` (`:542` — an undeclared capability is unmet).
- `test_engine.py`: T3 (`:992 t_bm1_new_scenarios_skip`) loads the LIVE constants (`:993 REPO_CONSTANTS`) and asserts the flags `is False` (`:998`) — Thursday's flip turns the selftests red; 1b re-cuts it against a copy with the flags forced.
- `metering-known-load.yaml`: `requires: [operator, metering-plug, command-api]` (`:78`); the header's "ONE DATUM PER REP … OUTSIDE ends the run" and "THE RATIO, AS CHARTERED: r = power_w / A_W" (`:44–:53`) and the REQUIRES paragraph (`:63–:68`) are stale on landing — 1b rewrites them; every rep prompt says "Read A AT THE INSTANT you press ENTER" (nine times) with no load step.
- `constants.yaml`: `fleet: devices: 6, entities: 6` (`:60–:61`); `remembered-ulids` six rows (`:67–:73`); `provenance: card: "hs-dev-1"` (`:561–:568`); `metering:` block with the URL-safe placeholders; `capabilities.metering-plug: false`, `link-read: false`.
- Not re-executed: a live run (Thursday); the bundle's receipt shape on a real run.

## §3 The findings — the rulings of record
| F | Ruling (b7c) | Where |
|---|---|---|
| F-1 | `operator` dropped from `requires:`; the tier alone keeps the leg out of the nightly (`runner.py:104`, `:119`) | 1b |
| F-2 | `field_subtract:` (the plug's no-load `power_w`, typed at T1) and `reference_subtract:` (the tare by difference, typed at T1, `min: 0`); exact-decimal; a non-positive effective reference → VOID at evaluation | 1b |
| F-3 | `on_outside: record` (OPERATOR only; default `fail`): OUTSIDE/VOID recorded, printed as read, the run continues, the close FAILs, B attributes after CHAR-AFTER | 1b |
| F-4 | WITHDRAWN — `provenance:` is global; the ULIDs into `metering.plug-entity` only | the Thursday re-mint |

## §4 BENCH-METER-1b (dispatch-ready; `context/instructions/2026-09-18_bench-lane_BENCH-METER-1b_subtract_on-outside_charter.md`, re-chartered wholesale)
Four files, 4 M + 0 A, a half-day: the two subtractions, `on_outside`, VOID, the per-rep print, `min:` on the operator binding, the timestamp witness, the version bump; T3 re-cut against test constants; T2 live-path comparing the modes; the real file walked live-path as a committed test with Nick's inputs; the per-plug wiring pinned; the scenario's header, prompts and asserts rewritten (the load step; tmux; the two offsets; `operator` dropped); the format's §5.

## §5 The re-close
v76 closes at seven: the bench block opened in-window is intaken and landing; nothing new opens. The v77 dispatch text carries THE THURSDAY ORDER by pointer. Sunday: `R5B-2:` + the v77 paste.

## §6 The greps (THE PREMISE GATE — every cite above, run on the device VM at `f3631cb` + the lane's tree, core `d1c2cbc`, hivemind `11a684b`)
`engine.py`: `:81 FIELD_WITHIN_KEYS` · `:91 ENGINE_VERSION` · `:96 def emit_version_banner` · `:262 def lint_field_within` · `:542 def unmet_requirements` · `:729 def check_ulid_provenance` (`:754–:758` the declared rows) · `:944 def ensure_operator_lets` · `:974 def read_operator_line` · `:984 def capture_operator_let` · `:1419 if seen != set(arg)` · `:1486 return "fail", capture, receipt["evidence"]` · `:1490 def eval_field_within` · `:1511 "verdict": state.upper()` · `:1927 def as_decimal` · `:1942 def field_within_check` · `:1971 def parse_operator_number` (`:1978 value = float(raw)`) · `:2036 except LintRefusal as refusal` → `:2040 return Verdict(name, "REFUSED", …)` · `:2050 verdict.bundle_dir = bundles.write_bundle(…)`. `runner.py:104 def auto_preflight_refusals`, `:119 if scenario.get("tier") == "OPERATOR"`. `test_engine.py:691 OPERATOR_SCENARIO`, `:992 def t_bm1_new_scenarios_skip`, `:993 load_constants(REPO_CONSTANTS)`, `:998 .get("available") is False`. `boot-health.yaml:40`, `:44`, `:57`, `:58`. `nightly.sh:172–:176` (CONFIG-DRIFT). `constants.yaml:60–:61`, `:67–:73`, `:369 carrier`, `:373–:378`, `:561–:568`. `metering-known-load.yaml:44–:53`, `:63–:68`, `:78`. Core: `ZigbeeIntegrationAdapter.java:146 ADOPT_DEVICES_KEY = "adopt_devices"`; `core/state-store/src/main/java/com/homesynapse/state/EntityState.java:87 public record EntityState(` (entity-level stamps). Hivemind: DEVICE-SET return `:25` (TR3 delay), `:47–:56` (§2 method); the b2 audit `:35` (the 0.2 A floor); the B2 suite-port return `:160` (the Pi pulls); the BENCH-METER-1 return `:57` (the walk was scratch).
