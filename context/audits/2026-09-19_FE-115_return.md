<!--
file: context/audits/2026-09-19_FE-115_return.md
purpose: FE-115's lane return — the v1.1.5 mirror (`conditions[].definition` typed · validated · rendered as keyed sentences), the three H8-a real-wire bodies filed as fixtures, the mock brought to the v1.1.5 shape with two DevPanel scenarios, the hub's dead-key rulings landed, the lint's reach closed.
audience: the hub (the intake audit) · Nick (the §0 card)
state-type: lane return (file of record; the diff is the truth — every claim re-derivable from the tree)
status: RETURNED — Sat 2026-09-19 ~20:35 CT (instrument 2026-09-20T01:3xZ); core `d1c2cbc`; nothing staged, nothing committed. Charter: context/instructions/2026-09-19_frontend-lane_FE-115_v115-mirror_definition-sentences_real-wire-fixtures_dead-keys_charter.md.
-->

# FE-115 — return

## §0 The card
**Instrument:** the device's Linux shell, a `/tmp/fe115` copy with `node_modules` outside the mount (O3): node v22.23.2 · npm 10.9.8 · vitest 4.1.11 · eslint · tsc. Nothing deferred; edit set md5-identical mount ↔ copy (22/22). Preflight in §2.
**Census: 22 = 18 M + 4 A** (under `web-ui/dashboard/`). M: `MODULE_CONTEXT.md` · `design/hero-v1/SPEC.md` · `eslint.config.js` · `scripts/contract-check.mjs` · `src/components/CausalChain.tsx` · `CausalChain.hardening.test.tsx` · `src/lib/api/{contract,shapes}.ts` · `contract.test.ts` · `v113-additive.test.ts` · `v114-additive.test.ts` · `fixtures/fixtures.stability.test.ts` · `mock/{mockData,scenarios}.ts` · `src/lib/{format,i18n}.ts` · `format.test.ts` · `i18n.test.ts`. A: `src/lib/api/v115-additive.test.ts` · `fixtures/wire-2026-09-19-h8a-{entities,automations,nonfiring}.ts`.
**Red-first (RED at HEAD → GREEN):** D0 3 pins · D1 validator 10 (+5 preservation: absent/null/typed/unknown/zero-children) · renderer 13 · row 5 · catalog 2 · D2 fixtures: the imports fail at HEAD (12 new, 26 → GREEN) · mock pins 6 (+3 vacuous at HEAD) · D3 1 (+1 preservation) · D4 5 (+1 preservation) · D5 docs.
**P1** DEVIATES ×3: −`DevPanel.tsx` (renders the registry; nothing to edit) · −`RunChainView.tsx`/test · −`client.ts` (no change needed). **P2** MET, one note: the grep = **8 lines = 5 pins + 3 prose** (`MODULE_CONTEXT.md:27`, `contract.test.ts:36`, `v113-additive.test.ts:404` — the prior flip's history; named, not flipped); `check:contract` → `v1.1.5-2026-09-19`. **P3** MET (0 at HEAD; 13 cases). **P4** MET at HEAD's `shapes.ts` (md5 `ce1db670…`): `entities.json` 759 B `29e04def…` → A1 **0** · `automations.json` 629 B `6fc36639…` → B3:automations **0** · `nonfiring.json` 581 B `01bf6f29…` → B3:nonFiring **0** ContractError; Bearer 0 ×3. **P5** MET (the default dataset validates as a v1.1.5 hub; `v115-keys` reaches every arm; `legacy-hub` keeps the absent one). **P6** MET (`896f861d…` / `05b02fbc…` = HEAD). **P7** MET: verify EXIT 0 — **29 files / 606 tests** (HEAD 28 / 535); bundle 73.5 / 100 KB.
**Gates:** §6 (1) 22 files · (2) v1.1.5 · (3) above · (4) green · (5) `-expect` lines = **4** (three named pins + `SPEC7_KEYS.length` 140 → 134, D3's named flip) · (6) D3 greps 0; D4 at HEAD **2** → 0 · (7) unchanged.
**Deviations:** `[CHARTER-ERROR]` D4's "four hits" are two (`CausalChain.tsx:369/:371`); "Dimmed" is one word, `EMPTY_CHAIN_NOTE` outside the six files — all four keyed anyway, bytes unchanged. `[RE-POINTED PIN]` `v114-additive.test.ts`'s four absence rows now read `resolveScenario('legacy-hub')` — one import moved, every `expect` line byte-identical (the charter named no flip; the default mock is a v1.1.5 hub by D2). `[DEFAULT]` the sentence renders UNDER the observed state (the L1 keeps the wire's `expression`); the ULID-selector rule (§1). `[SCOPE]` `boot.startingBody`'s one raw hit is a history comment, 0 consumers.

## §1 Per-file deltas
- **D0** `contract.ts:14` · `contract-check.mjs:18` · the three test pins → `v1.1.5-2026-09-19`; a v1.1.5 paragraph above the v1.1.4 one; the "not read yet" tail reworded.
- **D1 types** `contract.ts`: `ConditionDefinition` (readonly, recursive, nine keys in the wire order) + `CausalCondition.definition?: ConditionDefinition | null`. **Validator** `shapes.ts`: `conditionDefinition()` — object-or-null; every one of the nine keys REQUIRED inside the object (`definitionMap` writes all nine); `type` string; `selector`/`attribute`/`value`/`after`/`before` string-or-null; `above`/`below` `numOrNull` (a quoted Double is drift); `children` an array, recursive, `MAX_DEFINITION_DEPTH` 8 (the ninth level throws `…nested past depth 8`); `type` OPEN. Wired at the tri-state (`if ('definition' in c)`). **Renderer** `format.ts` `definitionSentence(def, label = s => s)` + `definitionClause` + `groupedClause`: null → `explain.condition.def.notRecorded`; the frame over one clause per permit; numeric `between`/`above`/`below`/`unbounded`, time `between`/`after`/`before`/`unbounded`; `and`/`or` join by the keyed connective, one operand renders bare, zero → `emptyGroup` (`.sentence` at the top); `not` → "it is not true that {clause}", several operands → the plain `list.join`; an and/or operand with >1 children inside a compound is `group`ed, a leaf or a negation is not; `ZoneCondition` and an unknown `type` each have a whole-sentence row and a nested clause row; a null `selector` → `entity.unnamed`, a null `attribute` → `NOT_RECORDED`, a null `value` → `.StateCondition.noValue` (totality on shapes the emitter never writes). Numbers rendered by `String()` (no unit), strings as recorded. **Row** `CausalChain.tsx`: `definitionLine` present-only; a `<p class=hint>` after the "At the time" `<Detail>` (or alone when there is no observed state); `selectorLabel(selector, resolveRef)`: `[:+]` in the selector → as recorded (a group/compound); else the census — the name when resolved, LOUD (`<ulid> (not in this hub’s registry)`) only for a dangling ULID, a slug the census lacks humanized, never accused. **Catalog** 24 `explain.condition.def.*` rows; **SPEC** §6 EXPLAIN-4 → LANDED (as a sentence UNDER the line — the wire's `expression` is still the type, so the L1 cannot say "someone is home" yet), 24 §7 rows tagged FE-115.
- **D2 fixtures** three files in the 08-20 convention (capture 2026-09-19T17:30:54Z at B2-1; artifact `6bd8508`, install-smoke #56; the request path; sha256; Bearer 0; WHY IT EXISTS). `fixtures.stability.test.ts` +12: each validates (P4 permanent), `wireBytes` 759/629/581, `sha256(JSON.stringify(obj))` = the record's hashes (the compact serialization IS the captured file), the v1.1.3 keys in both arms, the v1.1.4 keys (`definitionKey` EQUAL across the automations and non-firing reads — DP-5, real; `disabledAt`/`disabledReason` present-null; no v1.1.5 key), the detector 08-20 → 09-19 (key set +6 paths exactly, null-ness identical, values differ only in the lawful set, the older keys' order kept, `orderDiff` = `['data']` because the list grew), teeth on the new pair. **Mock** `mockData.ts`: `settledAt`/`confirmedAt` on every action (CONFIRMED both values; UNCONFIRMED settledAt = the timeout beside `confirmedAt: null`; SKIPPED command-less settledAt = its completion), `definition` on every condition (3 objects — the "after sunset" rule as a NumericCondition on `sys_sun` elevation below 0, `run_fd_001` an AndCondition — and null on `run_eh_003`, the OLDEST run, `definitionKey` null too), `definitionKey` on every chain / non-firing entry / automation (`DEFINITION_KEY_*` — 64-hex mock values, EQUAL across the reads), the DISABLED entry `disabledAt` 3 h ago + `repeated_failure`. `scenarios.ts`: `def()` + `AFTER_SUNSET_DEF`, `definitionKeyFor(seed)` (deterministic 64-hex; a shape, not a hash of anything), `makeCondition` carries `definition` (explicit `undefined` strips), `makeChain` carries `definitionKey` + `withV114Instants` (IN PLACE — the e5 CT action's getters flip its instants live; a pre-v1.1.2 action is left alone), `legacy` opt-out on `verdict-vocabulary`, `makeNonFiring`/`makeAutomation` carry the trio / the key; `buildV115Keys` (11 runs: 7 permits + nested + null + unknown `PresenceCondition` + a held DISPATCHED; DISABLED + FIRED_CONFIRMED entries) and `buildLegacyHub` (deep copy, keys STRIPPED) registered after `dangling-ref`.
- **D3** `i18n.ts`: the seven lines removed with a one-line note each; the `:165–:167` comment reworded to the D7 fact. `i18n.test.ts`: the six §7 keys out of `SPEC7_KEYS`, the pin 134, a "GONE" block + the kept twins. SPEC: six §7 rows struck through, §4/§5 prose amended.
- **D4** `eslint.config.js`: `ReturnStatement > Literal` + `ConditionalExpression > Literal` with the counts in the comment. `CausalChain.tsx` `actionPhrase` → `explain.action.phrase.turnedOn/.turnedOff/.dimmed`; `format.ts` `EMPTY_CHAIN_NOTE = t('explain.chain.empty')`; four rows in the catalog, the test and SPEC.
- **D5** `MODULE_CONTEXT.md`: the FE-115 beat (`:29`) + the Design-Doc line's v1.1.5 tail.

## §2 The gates and the instruments
**Preflight:** Check 2 STALE→folded (freeze stamp v1.1.5, client v1.1.4 — this lane's fold) · 4/5/8 PASS · 7 PASS (9/9 skill files md5-identical source ↔ synced copy) · 12 STALE (the H8a navigator packet + the R-5B packet still carry an active `status:` — the hub's hygiene) · 1/3/6 not re-read this sitting (name-light held: no new string carries the name). `/sessions` (the mount's disk) is 100 % full — npm/vitest caches redirected to `/tmp`; no file of this lane touched by it. `lifecycle/**` + `integration/**` (MEASURE-2b's dirty domains at start) untouched; nothing staged.
`npm run verify` EXIT 0 on the edit set (tokens:check OK · `eslint .` 0 · `tsc --noEmit` 0 · vitest 29 files / 606 tests · vite build 43.48 KB gz js · bundle 73.5 KB / 100 · contract v1.1.5-2026-09-19); the composite re-run after the doc beat was lost with the shell, so its seven steps were re-run singly on the final tree: 7/7 green. HEAD baseline this instrument: 28 / 535 green (an earlier full run showed 22 files failing on `ENOSPC` — the `/sessions` disk, not the tree; caches moved to `/tmp` and it went green). The widened lint over ALL non-test `src/` (all six selectors): 389 at HEAD → 399 (i18n.ts 123 → 127, scenarios.ts 72 → 79 — the catalog and the mock blurbs lead) — reported, never fixed. D3 consumer greps (outside `i18n.ts` + `i18n.test.ts`): `whyNot.neverTriggered.title` 0 · `explain.action.unconfirmed.title` 0 · `.body` 0 · `explain.action.pending.title` 0 · `.body` 0 · `boot.startingBody` raw 1 / `t()` 0 · `explain.hub.autos.noRunsSinceLoad` 0 · KEEP `explain.headline.completed.notRecorded` 0. `frontend.yml` on Nick's push.

## §3 Pushback and observations
- **The live-wire bar for D1 is OPEN.** No real v1.1.5 causal-chain body exists; `6bd8508` predates 114c. `conditions[].definition` is MIRRORED against `GetRunCausalChainEndpoint.java` / `ConditionDefinitionRenderer.java` / 114c's T9, not VERIFIED. The next sitting's read: `GET /api/v1/runs/{id}/causal-chain` on a ≥ 114c artifact for a run whose registry definition still hashes (an object) and one that does not (null) — filed as the open row. The H8-a bodies do move FE-113's K1–K4 and the v1.1.4 `definitionKey`/`disabledAt`/`disabledReason` VALIDATION to VERIFIED; the three v1.1.4 SENTENCES (EXPLAIN-6/8/9) are still unrendered by a real body.
- **Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING** (the definition sentence).
- **Observed on the real wire, not corrected:** the `A1` entities list body carries NO `pagination` key while the automations list does (`Envelope.pagination` is optional in the mirror, so nothing broke) — a Doc 09 / freeze-text question for the hub.
- **The pin's date convention moved:** FE-114 pinned the freeze doc's amendment date (`-2026-09-13`); the charter pins the mirror's date (`-2026-09-19`). Followed as ordered; the hub's v1.1.5 stamp beat should say which convention holds.
- **The charter's write-set says "`eslint.config.js` (row D5 only)"** while its table puts the lint at D4 — read as D4.
- **A pre-existing mock fiction, untouched:** `scenarios.ts`/`mockData.ts` write `targetRef.type: 'ENTITY'` (uppercase) where the wire serves `entity` (the H8-a bodies, `triggerRef`/`components[].ref`); the validators do not case-normalize. Not this lane's row — a hygiene row.
- **`settledAt`** stays typed + validated, not rendered (no row asks; a "settled at" line for the timed-out / failed modes is a design row).
- **Next WU (refuse-to-close):** the H8-b real-wire read of a ≥ 114c artifact's causal chain (the D1 bar) with `v115-keys` as the desk rehearsal; then the `ENTITY` → `entity` mock hygiene row and the whole-src literal count (the catalog is the honest bulk of it).

## §4 WUCP Phase-1 checklist
- [x] Preflight run; volatile facts re-derived from source (the emitter, the renderer, the record, HEAD's validators); none copied.
- [x] Contract consumed field-for-field; the mock carries the live shapes (present-but-null on every key; `legacy-hub` the absent arm); no invented shape (the nine keys, the depth bound, the open `type`).
- [x] Every state honest and distinct (absent / null / object render three ways; an unknown kind as recorded; never a throw); copy is §7 rows, test-locked; no color-only meaning touched; AA untouched (text-only additions).
- [x] Bundle 73.5 KB under budget; gate green; nothing phones home.
- [x] Name-light: no new string carries the name; no positioning claim.
- [x] Return filed with the verification register and the next WU.

RETURNED nexsys-hivemind/context/audits/2026-09-19_FE-115_return.md 13649
