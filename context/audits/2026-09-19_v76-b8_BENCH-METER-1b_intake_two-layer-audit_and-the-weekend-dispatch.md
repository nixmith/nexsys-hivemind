<!--
file: context/audits/2026-09-19_v76-b8_BENCH-METER-1b_intake_two-layer-audit_and-the-weekend-dispatch.md
purpose: THE v76 BEAT-8 AUDIT (Sat 2026-09-19 morning, before the sitting) — BENCH-METER-1b's return intaken two-layer: the bench tree at porcelain (4 M + 0 A on `764e537`, `constants.yaml` and `provenance:` untouched), the three instruments RE-RUN by the hub on the device VM (36/0; 43/0; 13 legs; the banner `BENCH-METER-1b-2026-09-19-subtract-record @ 764e537`), the engine and the scenario read at the bytes (the two subtractions Decimal per operand, VOID before the value, `record` OPERATOR-only, the bare `subtract:` refused by name, `min:`, the witness recorded never asserted, the deadline VOID in both arms, the close naming the lines; 22 entries, a LOAD STEP in all nine rep prompts, `operator` gone); P1–P6 adjudicated; one lane claim not at the bytes (the README carries no `field_within` at all); verdict ACCEPT → the bench card (4 M); THE THURSDAY ORDER's step 0 moved to SUNDAY after `R5B-2:` (two watched nightlies, not one; no conflation with R-5B's verification night); the v77 dispatch text re-cut for a SATURDAY start — the weekend order (MEASURE-2b before the sitting; the knockout lane Saturday night; Sunday the intakes, the Pi pull, LINK-READ, FE-115, the strategy pass; PELTON-READY for early next week).
audience: the hub (the record; v77's boot) · Nick (§0; the packet) · the bench lane (§2, closed)
state-type: intake audit (two-layer) + the weekend dispatch
status: FILED v76 beat 8 (Sat 2026-09-19 ~07:5x CT; instrument 2026-09-19T12:49:06Z). v76 CLOSES at eight; v77 opens on the paste, Saturday.
-->

# v76 beat 8 — BENCH-METER-1b intaken, ACCEPT; the bench card; the weekend dispatch

## §0 Card
- **`BENCH: BM1 764e537` · `HIVE: LANDED 90e3348`** verified at the instrument (both HEADs; both porcelains as expected — the hivemind's one `??` is the lane's return). **Verdict on BENCH-METER-1b: ACCEPT.** The return exists at its path (`context/audits/2026-09-19_BENCH-METER-1b_return.md`, 5,780 B, `RETURNED` last); the bench tree is exactly the charter's table — 4 M + 0 A (`engine.py` +369, `test_engine.py` +490, `metering-known-load.yaml` 260 lines touched, `SCENARIO_FORMAT.md` +4); `git diff --name-only -- scenarios/constants.yaml` is empty; nothing staged.
- **Layer 2, the instruments RE-RUN by the hub (the device VM, 12:3xZ):** `python3 -B tools/runner/test_engine.py` → `selftest: 36 check(s), 0 failure(s)`; `nightly_digest.py --selftest` → `43 check(s), 0 failure(s)`; `runner.py suite all --list` → `runner BENCH-METER-1b-2026-09-19-subtract-record @ 764e537` · `[LOAD] metering-known-load tier=OPERATOR requires=metering-plug,command-api` · `listed 13 leg(s) — all load lawfully`. The lane's numbers reproduce.
- **The engine, read (the diff whole):** `FIELD_WITHIN_OPTIONAL_KEYS = {reference_subtract, field_subtract, on_outside}` (`engine.py:93`); a bare `subtract:` is REFUSED by name (`:327`); `on_outside: record` lawful on `tier: OPERATOR` only (`:366`); `field_within_check(value, reference, tolerance_pct, reference_subtract=None, field_subtract=None)` (`:2213`) — `as_decimal` on EACH operand, `ref_eff = ref − ref_sub` (`:2250`), `ref_eff ≤ 0 → "void"` BEFORE the value is looked at (`:2253`), the receipt carrying `reference_effective` / `value_effective`; `print_rep` (`:1616`) prints the record's DIV row as the datum is decided; `void_at_deadline` (`:1710`) in the live loop AND the fixture arm; `record_result` / `recorded_close` (`:1744` / `:1755`) — the run continues, the close FAILs naming `positive[i] <let> <verdict> (<figure>)`; `min:` on the operator binding (`OPERATOR_LET_KEYS :107`; the floor resolved before anything is typed, `:1084`; a typed value below it refused and asked again); the witness `FRESHNESS_WITNESS_KEY = "data.lastReported"` (`:106`, pinned from the WCAP capture-5 read; recorded beside `read_at`, never asserted; re-pinned by Thursday's first rep); `ENGINE_VERSION = "BENCH-METER-1b-2026-09-19-subtract-record"` (`:116`). Default `fail` is BENCH-METER-1's path byte-for-byte; VOID under `fail` ends the run as FAIL (OUTSIDE's class) — the lane's [INFO], accepted.
- **The scenario, read whole (348 lines):** `requires: [metering-plug, command-api]` (`:80`); the header's "HOW THE FORMAT CARRIES IT" rewritten (a)–(g) — freshness is the operator's, one datum per rep under `record`, the DIV row with both sides named, the tare by difference below the 0.2 A floor, tmux, the receipt's fields, the two wire pins; the METHOD block verbatim; the `let:` list is 22 entries per the charter's order (per plug: `tare_watts_P` and `plug_offset_w_P` with `min: 0`, `a_volts_P_1/2`, `a_watts_P_r1..3`; then `char_after_readings`); **all nine rep prompts begin with THE LOAD STEP** (relay OFF ≥ 15 s → ON, or the lamp out and back; WAIT for the dashboard's lit value, the TR3 up to a minute; ≥ 5 s more; read A, type, ENTER) and their notes say WITHIN/OUTSIDE/VOID prints at once and B attributes after CHAR-AFTER; the nine asserts each carry `reference_subtract: ${let.tare_watts_P}`, `field_subtract: ${let.plug_offset_w_P}`, `on_outside: record`, the tolerances and `within: 10s` unchanged. This is Thursday's script as the record wrote it.
- **The tests (T1–T6 present, `test_engine.py:1133/:1215/:1307/:1398/:1450`; T5 = the re-cut `t_bm1_new_scenarios_skip :1013` on `repo_constants_with(...)` `:995` — a deep copy with the flags FORCED, never the live values):** T1 pins the float defect (`79.6 − 0.7 != 78.9` asserted as the premise, then `reference_effective == "78.9"` and WITHIN); T3 walks the REAL file live-path with the charter's inputs — the nine REP lines the return quotes reproduce under the hub's run, eight WITHIN, one OUTSIDE (`a_watts_g4_2_r1`, 4.472 %), TR3 rep 1 at 3.755 % tared (4.591 % alone), the close `FAIL — 8/9 positive WITHIN; 1 recorded OUTSIDE/VOID …`; T4 pins the per-plug wiring; T6 the `min:` refusal.
- **P1–P6:** P1 HELD (4 M + 0 A). P2 HELD in substance — red-first shown (36/7, each for the predicted reason), then green; the "≥ 10 checks" was the charter's wording for cases and the lane counted `check_fn` rows (+5, ≈ 16 cases): the hub's wording, owned. P3–P6 HELD at the instrument. Estimate: the lane reports ≈ 25 min against the half-day — noted, not a finding.
- **One lane claim NOT at the bytes:** "[REVIEW] `tools/runner/README.md` describes `field_within` at BM1". The README (18,469 B) contains no `field_within`, no BENCH-METER, no operator-entered binding — its assert vocabulary predates BENCH-METER-1 entirely. Corrected: a docs row (the README's §asserts brought to BM1 + 1b) for a later bench card; not a Thursday blocker. The lane's cite drift (`requires:` at `:75`, not the charter's `:78`) is the hub's — owned.
- **The deviation accepted:** the desktop file tool was down for the lane, so it edited in its session workspace and wrote the four files back with checksums; the instruments ran on the tree and the hub re-ran them on the tree — the tree is what is judged.
- **THE THURSDAY ORDER, step 0 moved to SUNDAY:** the Pi's clone is pulled to 1b's sha after `R5B-2:` is read Sunday morning — not Mon/Tue. Why: the sitting's packets are untouched and the nightly is hands-off until R-5B's B6 (Saturday), and pulling Saturday night would put the engine change on R-5B's own verification night; Sunday morning leaves Monday's AND Tuesday's nightlies on the new engine — two watched, not one. The word: `PI: pulled <sha>`. Steps 1–4 unchanged (the b7 audit §0).
- **The ONE act:** the packet `_scratch/v76/2026-09-19_v76-b8_packet.md` — block 1 the bench card (4 M) → `BENCH: BM1B <sha>`; block 2 this beat's hivemind card (8 = 6 M + 2 A) → `HIVE: LANDED <sha>`; block 3 the v77 paste (the dispatch text re-cut for a SATURDAY start — `context/handoff/2026-09-18_v77_dispatch-text.md`; paste it right after block 2, before the sitting; v77's first act is MEASURE-2b's coder paste, or nothing until after the sitting if 09:45 comes first). Saturday at the rig: nothing else.

## §1 Layer 1 — the lane's claims, read
P1–P6 as claimed; the red-first arithmetic honest (7 red for the predicted reasons on `764e537`; 4 red after the engine — the scenario rows; 36/0 after the files). The walk's nine lines quoted; the close quoted. The [INFO] rows sound (VOID under `fail` = FAIL; a non-numeric subtract = FAIL-now, a defect; the bare `subtract:` refused). The [REVIEW] row corrected in §0.

## §2 Layer 2 — the greps (THE PREMISE GATE; the device VM, bench `764e537` + the lane's tree)
`engine.py`: `:93 FIELD_WITHIN_OPTIONAL_KEYS` · `:106 FRESHNESS_WITNESS_KEY = "data.lastReported"` · `:107 OPERATOR_LET_KEYS … "min"` · `:116 ENGINE_VERSION` · `:327 if "subtract" in spec` · `:366 on_outside: record is lawful on tier: OPERATOR only` · `:1084 minimum = as_decimal(self.resolve(spec["min"]))` · `:1616 def print_rep` · `:1710 def void_at_deadline` · `:1744 def record_result` · `:1755 def recorded_close` · `:2213 def field_within_check(value, reference, tolerance_pct, reference_subtract=None, field_subtract=None)` · `:2250 ref_eff = ref - ref_sub` · `:2253 return "void", receipt`. `metering-known-load.yaml`: `:80 requires: [metering-plug, command-api]` · `:96 let:` · 22 `- name:` rows · 9 `THE LOAD STEP` prompts · 9 asserts with `on_outside: record`. `test_engine.py`: `:995 def repo_constants_with` · `:1013 def t_bm1_new_scenarios_skip` · `:1133 t_bm1b_subtractions` · `:1215 t_bm1b_on_outside_modes` · `:1307 t_bm1b_real_file_walk` · `:1398 t_bm1b_real_file_wiring` · `:1450 t_bm1b_operator_let_min`. `SCENARIO_FORMAT.md:120–:123` the four §5 bullets. `tools/runner/README.md`: 0 matches for `field_within|BENCH-METER|operator-entered`. Not re-executed: a live run (Thursday); the witness key on a real `/state` body (the first rep re-pins it).

## §3 The weekend dispatch (v77, re-cut for a Saturday start)
The v76 window closes at eight: its deliverable (Sunday's intakes handed to v77; ENERGY-READ landed green) exceeded; the bench block opened in-window is intaken twice and landing. v77 opens on the paste Saturday morning with THE WEEKEND ORDER: **Sat b1 (before the sitting):** MEASURE-2b chartered through the premise gate → the coder paste as the ONE act, or held to after the sitting if 09:45 comes first — the sitting is never delayed. **Sat, Nick at the rig (no asks):** the knockout lane (D-v74-1, silence = ii — the second candidate through the naming frame's knockout; read-only; no name graded in chat) DISPATCH-READY; VERDICT-VOCAB-1 drafted from the record; PELTON-READY drafted (§3a). **Sat evening (`H8A:` / `R5B: RETURNED`):** the two returns read at the bytes (the digest line closes them Sunday); the knockout paste as the night's lane. **Sun (`R5B-2:`):** the intakes closed → `PI: pulled <sha>` (THE THURSDAY ORDER step 0) → MEASURE-2b's return intaken → LINK-READ chartered through the gate and pasted → FE-115 chartered on H8-a's real-wire read (the web-ui slot; the cap is TWO lanes — LINK-READ + FE-115 once the knockout returns) → the strategy pass (`STRATEGY: after-R5B`: the one question; THE SPOKEN CARD and its embargo side with the Pelton contingency; the buyer script through the claim register). **Mon:** the digest on the new engine; LINK-READ's return; `ERIK:` if it comes → the G-2 ruling card → BRAND-G2-EXEC re-cut for the current name; rehearsal 1's packet (kill −9; wk 09-28); `FOP: apply`; AMD-100's docs card. **Tue:** the second digest; THE THURSDAY PACKET cut as one-command cards (the adoption edit; the re-mint; the variant/basis regeneration; the tmux run) → v78's text.
### §3a PELTON-READY (the deliverable v77 drafts, Nick rules on when the results come)
The written opinion / comprehensive-search results on the FINAL candidate (`SEARCH-NAME` FINAL, the RS-13 ruling `context/audits/2026-09-09_RS-13_PALOKI_intake_two-layer-audit_and_RULING_v67-b6.md`; the engagement `context/strategy/counsel-package/2026-07-21_engagement-tracker.md`; the G-2-day shape `context/assessments/2026-07-22_brand-program_lane_return.md` — BRAND-G2-EXEC and the Deliverable-4 checklist, written for the prior candidate, re-cut for this one) arrive early next week. Ready before they do: (1) the H10 ruling card with three forms — CLEAR · CLEAR-WITH-CONDITIONS (which conditions, and what each costs) · NOT CLEAR (→ the knockout lane's second candidate, already run) — and the acts each form unlocks (the `.com` at Nick's risk today → at the record's; the handles' 48-h run; the LLC line; THE SPOKEN CARD's embargo side; the pilot's page; C-14 "no name outward" lifted or kept); (2) the results-consult question list (what "public use" means for parked handles and domains; the filing basis and timing; the companion company mark; what an Office Action on the likely citations would cost); (3) the knockout return in hand as the fallback. No name is graded in chat (law 9); no public use before the written opinion.
