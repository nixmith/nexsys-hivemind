<!--
file: context/audits/2026-09-19_v77-b2_H8a-and-R5B_returns-read-at-the-bytes_MEASURE-2b-and-KO-2_chartered_audit.md
purpose: v77 beat 2 — the two sitting records (H8-a, R-5B) READ at the bytes with the hub's own re-executions and its pre-registered verdicts (the CLOSE waits on Sunday's `R5B-2:` line); the rulings the rig needs before Sunday 03:30 CT; THE THURSDAY ORDER's missing step found (the bench card's Core); two hub errors owned; MEASURE-2b and KO-2 chartered through the gate; the weekend re-cut.
audience: the hub (Sunday's close reads §2 first) · Nick (§0; §6 the words)
state-type: intake audit, layer 1 + layer 2 (dated; never edited after the beat)
status: FILED Sat 2026-09-19 ~14:2x CT (instrument 2026-09-19T19:29:22Z)
-->

# v77 beat 2 — H8-a and R-5B read at the bytes; MEASURE-2b and KO-2 chartered

## §0 One screen
- **The sitting:** H8-a 17:14→17:43Z (28 min of ≤60), R-5B 17:43→18:59Z; navigator up 16:41Z, closed 19:01Z; **zero STOPs in either record.** H8-a: four of four stop-gates MET, K1–K4 ✓ on the real wire, the stop-proof clean twice. R-5B: the restore MET 5/5, six gates MET as measurements, the first `fleet:` datum (`fleet: 6/6 · re-seen 0`); P-B2/P-B3/P-B6 NOT TESTED (the provocation protocol was not run as written — D-3); bench-hero fired 4× and confirmed 4/4 in 77–228 ms, unplanned.
- **Re-executed by the hub (layer 2):** the three H8-a capture files re-hashed — `29e04def…`, `6fc36639…`, `01bf6f29…`, Bearer 0 each, 759/629/581 B — identical to the record; K1–K4 re-asserted on the filed bodies by the hub's own script (rows 4, `deviceId` on every row, 4 non-null ULIDs; `lastReported` 4 ISO-Z; 1 automation, 3 components, 2 non-null refs of shape `{type:"entity", id}`; `triggerRef` = the StateChangeTrigger component's ref, `01M1PRQN03X8H4MNEZQ62F76F1`) — **✓ all four**; the R-5B capture (5 files, 42,092 B, Bearer 0) hashed and filed. The bench tree at `fa01cad`: the record's B6-1 line (`git pull --ff-only` 16e672d → fa01cad; `selftest: 43 check(s), 0 failure(s)` = the b8 audit's own count).
- **One lane claim REFUTED at the bytes (R-5B B6-2b, "the scenario or its uhubctl dependency arrived with tonight's fast-forward"):** `git diff 16e672d fa01cad -- scenarios/usb-reenumeration.yaml tools/runner/drivers.py` = 0 lines; `drivers.py` at 16e672d already calls `uhubctl` by bare name (5 sites); and the 09-19 08:32Z TIMER run on 16e672d read `8/9 PASS · 1 SKIP(hue-online)` — usb-reenumeration RAN and PASSED on this card this morning. The binary is on the card. **The mechanism (a claim with its code path):** `drivers.py:77` builds `["uhubctl", "-l", …]` and `subprocess` resolves the bare name through `PATH`; the timer runs under the systemd user manager (`tools/scheduler/nexsys-bench-nightly.service`, `ExecStart=/usr/bin/bash %h/nexsys-bench/tools/nightly.sh`), whose default `PATH` carries `/usr/sbin`; the manual run was launched by `ssh pi 'nohup bash ~/nexsys-bench/tools/nightly.sh …'` (the packet's B6-2 line 153), a non-interactive sshd shell whose Debian default `PATH` does not — `uhubctl` installs to `/usr/sbin/uhubctl`. **Pre-registered:** Sunday's 03:30 CT timer line reads `8/9 PASS · 1 SKIP(hue-online) · fleet: 6/6 · re-seen 0` with NO install. Refutable-by: a `7/9 · FAIL usb-reenumeration` on Sunday's line → Monday's first act is `dpkg -s uhubctl; ls -l /usr/sbin/uhubctl` and the install if absent. **Ruling on ask 1: NO install tonight.** A rig change on a diagnosis the record refutes is not a fix.
- **`PI: pulled fa01cad` BANKS from the record (B6-1) — THE THURSDAY ORDER step 0 is DONE**, a day before the plan said; the banner line was not read (disclosed) — the sha and the 43/0 selftest identify the engine. Sunday's, Monday's and Tuesday's nightlies are three watched runs on the new engine.
- **THE THURSDAY ORDER lacks a step:** the adoption (its step 1) edits the carrier `~/hs-bench/config/homesynapse.yaml` — the BENCH card's Core — and that Core prints `deviceId=None` (R-5B B5, the pre-CG-123 shape): older than 6bd8508, without ENERGY-READ. The metering scenario reads `power_w` from THAT Core's API. **Step 0b, added (§4): the bench card's Core → `d1c2cbc`'s arm64 .deb (install-smoke #61) BEFORE the adoption, with ONE nightly watched green on the new Core + new engine.** Recommended Sunday daytime (`BENCH-CORE: sun|mon|tue`, §6), so Monday's and Tuesday's lines are both on the full stack.
- **Two hub errors owned:** (1) the b8 hub moved step 0 to Sunday without grepping R-5B's packet, whose B6-1 has pulled the bench tree since v75 b1 — the pull ran inside the sitting the relocation was meant to spare (pm-lessons, this beat); (2) the v73 hub wrote "the second candidate filed in the naming docket" — no docket exists in the tree (`git ls-files | grep -i docket` → the R10 dockets only; `grep -rl 'naming docket'` → the three texts that cite it). The candidate's name is not in the record; KO-2's paste carries a slot Nick fills.
- **Chartered through the gate:** MEASURE-2b (`context/instructions/2026-09-19_coder-lane_MEASURE-2b_…coding-instruction.md`, 27,371 B; the paste handed at 14:2x CT — the lane is RUNNING on the Java slot) and KO-2 (`context/instructions/2026-09-19_research-lane_KO-2_…charter.md`, 9,037 B; the paste in this beat's packet).
- **The window's deliverable, re-named:** the two intakes CLOSED two-layer on `R5B-2:` (the rulings of §6 given) AND MEASURE-2b LANDED green. Step 0 is done; the Pi pull card is retired.

## §1 Layer 1 — the records' claims, read
| Claim | Where | Read |
|---|---|---|
| FE-113 → VERIFIED on K1–K4 | H8-a §0 THE FOUR KEYS; §H | the asserts quoted; re-executed by the hub ✓ — **VERIFIED at the Sunday close** (the status flip rides the close beat) |
| OR-FAILCHAN → CLOSED on two `success · 143 · inactive` readings | H8-a §0 THE STOP-PROOF | the grade defect (§6-B) is fixed on the wire twice on `6bd8508` — but the OR's own text names the unit-side `SuccessExitStatus=143` as a MASK of a genuine kill, and F-1 shows the OR's instance 2 (the orderly serial close) STILL emits `transport_failed`: the §10-O path never runs. **Not closed — RE-SCOPED at the Sunday close:** instance 1 fixed-and-measured; instance 2 measured-still-open (IR-33); the mask question ruled with the §6-B commit re-read |
| O-2 → CLOSED on three readings / two artifacts | both §0 | ✓ pending the close |
| FE-113b's fixture EXISTS | H8-a §C | three files, hashed by the hub ✓ |
| F-1: the orderly transport close never runs (2/2) | H8-a §0 (S); R-5B §0 | the journal counts quoted (`transport_closed_orderly` 0; `transport_failed` 3–6 ms into the hook; `port_reopened` +1.6 s) — a design finding → **IR-33** |
| Row 7: 26 vs 0; Row 16: 0 | H8-a §R | banked at the close |
| the fleet floor: re-seen 6 · adopted 0; fleet 6/6 | R-5B R5B-4/-5 | ✓ |
| P-B2 / P-B3 / P-B6 NOT TESTED (D-3) | R-5B §0 | `C003:` cannot be ruled; F-R4b-C carried; `HARNESS-PLUG:` Monday on the label (below) |
| D-2 the S31 fence breach (the vacuum) | R-5B §9 | the S31 cut itself off 1–2 s into a vacuum's start, twice — consistent with its overcurrent cut-out under the inrush (the hero's command comes 10 s after a trigger, not 1–2 s); a fact row; the vacuum comes OFF the S31 at the next rig touch |
| bench-hero 4/4 confirmed in 77–228 ms; the journal silent on automations at INFO | R-5B P-B5; ask 4 | a fact row for the record (C-3's first wire datum, not outward); **IR-34** (the journal trace) |
| the bench card's Core `deviceId=None` | R-5B ask 5 | → THE THURSDAY ORDER step 0b (§0) |
| the records ~40/~45 KB over the ~24 KB target | both ask 6 | accepted: every ⏺ verbatim as the packets asked; no harvest |

## §2 What the Sunday close reads (so it is one screen)
`R5B-2:` line 2 vs the pre-registration (§0); FE-113 → VERIFIED; O-2 → CLOSED; OR-FAILCHAN re-scoped (instance 1 closed; instance 2 = IR-33; the mask ruling); Row 7 · Row 16 banked; the fleet floor row (re-seen 6, adopted 0, 6/6); P-B5's per-device lower bounds as the frame table row; `S31-LABEL: 15A 1800W — Input 120V @ 60Hz` banked; the S31 fact row (the cut-out); D-1..D-7 carried into rehearsal 1's packet through THE PRIOR-LEDGER GATE (`cut -c1-240`; the S31 row by deviceId; the ~/r5b copy; the ONE-device three-acts say-it-back choreography); the residue (two empty dirs on hs-dev-1; an empty tree + a known_hosts line on hs-fresh) moved aside on a card, never deleted.

## §3 Not re-executed (disclosed)
The journal counts (B3-2b, D-5) — the invocation's journal is on the card; the record's quoted counts stand as the lane's. The bench banner on the Pi. The 08:32Z timer run's .out (its digest line is quoted in H8-a B0-1 and is the instrument used above). Sunday's line, by definition.

## §4 THE THURSDAY ORDER — step 0b (the plan §19 carries it; the b7 audit §0 stays the one statement, pointed at from §19)
(0) DONE Sat 09-19 at R-5B B6-1: the Pi's clone at `fa01cad`; three nightlies to watch (Sun · Mon · Tue). **(0b) the bench card's Core to `d1c2cbc`** — an H8-a-shaped card (fetch the arm64 .deb from install-smoke #61 with the four-surface hash custody; `dpkg -i`; the integrity gate; `[PASS] boot-health 6/6` on the OLD counts; formed 0), cut through THE PRIOR-LEDGER GATE against H8-a's D-1/D-2/D-4 (the ssh-session slip; `cut -c1-240`; the desktop-prompt discipline) — the fleet is unchanged by an upgrade, so the nightly's set-equal asserts hold; the risk it retires: the metering scenario reading `power_w` from a Core that has no metering path. (1)–(4) as the b7 audit §0.

## §5 The lanes, this beat
MEASURE-2b — dispatched 14:2x CT (the paste file `_scratch/v77/2026-09-19_v77-b2_MEASURE-2b_coder-paste.md`; the charter md5 `576f41b30448`); returns `M2B: RETURNED`. KO-2 — the paste in this beat's packet; returns `KO: RETURNED`; the `NAME:` H10 card is cut from its return, never from chat. The cap: two lanes + Nick's hands (free). VERDICT-VOCAB-1 and PELTON-READY: NOT drafted this beat (the two records and the two charters were the beat; they are b3's, Saturday evening, hub-only).

## §6 The words (H10 rows; silence = the recommendation)
| Word | Options | Rec | Refutable-by |
|---|---|---|---|
| `BENCH-CORE: sun|mon|tue` | when the bench card's Core goes to `d1c2cbc` (step 0b; ~25 min at the rig on a card the hub cuts Sunday morning) | **sun** — Monday's and Tuesday's nightlies then both read the full stack | Nick's Sunday hours |
| `REARM: reh1|thu|drop` | where R-5B's B1c–B2 window re-runs with the ONE-device, three-acts, say-it-back choreography (P-B2/P-B3/P-B6) | **reh1** — rehearsal 1's sitting (wk 09-28) gains a 20-min block; Thursday stays CHAR + adoption | a free half-hour Thursday before the adoption |
| `SHAKE: mon|thu` | B1 + B2 in series on the lamp with the 40 W bulbs Monday evening, 20 min, no Zigbee (the settle time clocked; the B pair read against each other) | **mon** | the bulbs or B2 not arriving Monday |
| `HARNESS-PLUG:` | Monday, on `S31-LABEL: 15A 1800W` — the label admits a power-cycle harness for the held card's supply; the vacuum finding says nothing against it (a Pi supply draws ~15 W); bench-hero's CommandAction on the S31 is the design question rehearsal 1's packet must answer before the S31 powers the card the hero runs on | the card Monday | — |
| uhubctl | no act | — | Sunday's line |

## §7 The weekend, re-cut (the plan §19)
Sat evening (b3): VERDICT-VOCAB-1 + PELTON-READY drafted; `KO: RETURNED` and `M2B: RETURNED` intaken as they come (the core card on MEASURE-2b → CI → the counter). Sun (b4/b5): `R5B-2:` → the two intakes CLOSED (§2) → the step-0b card (`BENCH-CORE:`) → LINK-READ chartered against the fixture at HEAD + FE-115 on H8-a's real-wire read → one packet, two pastes. Sun evening (b6/b7): the strategy pass; the close on the deliverable; Mon–Tue to the next window's §HELD (the digests; LINK-READ's return; `HARNESS-PLUG:`; rehearsal 1's packet with `REARM`; `SHAKE`; PELTON-READY on `ERIK:`; `FOP: apply`; AMD-100's card; Tue THE THURSDAY PACKET with step 0b banked; VERIFY-72H).
**Leverage line:** the refuted uhubctl claim saves a rig change and a red herring; step 0b found now costs 25 minutes Sunday instead of a dead Thursday.
**The alternative shape, rejected:** installing uhubctl tonight "to be safe" — a change to the card under a wrong diagnosis leaves the true cause (the launch environment) unlearned and the next manual run failing the same way.
**2029 test:** why the Pi pull happened Saturday when the plan said Sunday; why the bench card's Core was upgraded before the first metering adoption; why the S31 carries a vacuum note.
