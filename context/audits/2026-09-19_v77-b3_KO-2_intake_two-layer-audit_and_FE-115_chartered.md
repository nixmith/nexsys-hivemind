<!--
file: context/audits/2026-09-19_v77-b3_KO-2_intake_two-layer-audit_and_FE-115_chartered.md
purpose: v77 beat 3 — KO-2 (the second candidate through the naming frame's knockout) intaken two-layer: ACCEPT; the verdict KILL adopted as the hub's recommendation with the ruling card `NAME2:`; the consequence for PELTON-READY's fallback; FE-115 chartered through the gate on the H8-a real-wire bodies; MEASURE-2b found NOT STARTED at the instrument and re-handed. NO NAME IS WRITTEN HERE — the return under `context/research/` carries it.
audience: the hub · Nick (§0; §2 the card)
state-type: intake audit, layer 1 + layer 2
status: FILED Sat 2026-09-19 ~17:5x CT (instrument 2026-09-19T22:51:57Z)
-->

# v77 beat 3 — KO-2 intaken (ACCEPT; KILL); FE-115 chartered; MEASURE-2b re-handed

## §0 One screen
- **The return:** `context/research/2026-09-19_KO-2_<the candidate>_knockout_return.md`, 21,196 B, the `RETURNED` line last; the method is RS-13's STEP 0 with the canaries re-run first (9 PASS, 1 MISSED and disclosed as a gap); every cell with its instrument, query verbatim and stamp; the verdict words defined before use; the fence attestation (read-only; no account, no purchase flow; no name in chat).
- **Layer 2 (the hub's re-fetches):** ★1 `rdap.verisign.com/com/v1/domain/<the candidate>.com` → created `2015-07-13T22:52:09Z`, expires `2027-07-13`, Name.com, Inc., `client transfer prohibited` — the lane's S5 cell holds. ★4 TTABVUE `91235072` → Inovio Pharmaceuticals v. NAB Holdings (INOVIO PAYMENTS, SN 86898563), filed 2017-06-14, **dismissed with prejudice 2018-06-19 on the opposer's withdrawal** — the lane wrote "settled"; the precise reading is the record's (a precision correction, not a verdict change: the enforcement propensity the cell asserts — the owner opposed a letter-plus-string mark — stands). **Not re-executed (disclosed):** the WIPO GBD and EUIPO pages (client-side decrypted; JS forms) — the two EU marks (018664506 cl. 9 "Software…", 2022; 019295156 cl. 9/11, reg. 2026-06-14) stand on the lane's reads with their canaries PASSED; the tmsearch POST counts (0/0; 1,795) likewise.
- **The verdict, adopted as the recommendation:** KILL on two facts, neither a matter of degree — (S5) the exact `.com` is an operating business's since 2015 with no sale path, and the program's own law values the exact `.com` "above any specific string" (`context/strategy/2026-07-05_naming-and-domain-strategy.md` §3); (S2) the candidate's SOUND is a senior EU class-9 mark for software, and its one-V spelling — the spelling every rig rendering collapses to — was registered in classes 9/11 for home appliances three months ago. The best facts (0 in the connected-home channel; the US exact string empty) do not reach either.
- **The named gaps stand as gaps:** S4 (the ASR rig's controls landed outside ±4 of RS-13's in both configurations; scores indicative only — a rig finding for the next knockout, IR-35); no national register beyond EUIPO; Maven unreachable; the ccTLDs unverified. None would rescue a name that fails S5.
- **The consequence:** D-v74-1 (ii) resolves — the swap program runs ONCE on PALOKI when the written opinion lands; **PELTON-READY's fallback ("the knockout return") is VOID.** A NOT CLEAR opinion on PALOKI now has no name behind it. The contingency of record is NAMING-B (`context/strategy/brand-program/2026-09-07_NAMING-B_contingency-naming-and-brainstorming-lane_charter_GATED.md`, RETIRED-OVERTAKEN at RS-13) — re-armed as the fallback lane, dispatched ONLY on `ERIK:` reading NOT CLEAR (or on Nick's word before). PELTON-READY's card (b4) carries it.
- **MEASURE-2b NOT STARTED at 22:41Z:** core porcelain 0; no `RealCoreFixture.java`; no tee file; no worktree (`git worktree list` = main only). The 14:2x CT paste rode a mid-turn message; re-handed as block 0 of this beat's packet. The Java slot's critical item; the intake Sunday.
- **FE-115 chartered through the gate** (`context/instructions/2026-09-19_frontend-lane_FE-115_…charter.md`, 15,627 B): the v1.1.5 mirror (five pin sites grounded at `d1c2cbc`); `conditions[].definition` typed/validated/rendered by ONE pure `definitionSentence` over the emitter's seven `type`s (`ConditionDefinitionRenderer.java:56–:67`: State · Numeric · Time · And · Or · Not · Zone); the three H8-a bodies as REAL-PAYLOAD fixtures with P4 = the live-wire bar (they parse through HEAD's validators with zero `ContractError`, run before any edit); the mock/DevPanel to v1.1.5; the dead-key rulings landed behind consumer greps; the lint's reach closed. D4 holds: the web-ui path-domain beside the Java slot.

## §1 Layer 1 — the return's claims, read
| Claim | Read | Verdict |
|---|---|---|
| P1 the canaries | 9 PASS with stamps; the ASR controls MISSED (24·35 padded; 6·20 unpadded vs 10·30) | MET, the gap named honestly |
| P2 US exact 0/0; GBD 0 | the queries verbatim (`WM:<c>`, `WM:*<c>*`) | MET |
| P3 the embedded strings | INNOV(ate)/-IO parsed; the crowd 1,795 in 9/42; the sound-owners named with numbers | MET |
| P4 the rig | MISSED → S4 indicative only | disclosed; IR-35 |
| S5 the .com | RDAP + the 301 to an operating business; no marketplace listing | ✓ (re-fetched) |
| S2 EU | two EUTMs by number, class, date, goods line | on the lane's read (not re-fetched) |
| the fence | GETs/POSTs and page loads only; the name never in chat | ✓ (the chat transcript Nick pasted carried the name only inside a URL the lane listed as a source — the lane's own §2 form; noted, not a breach of the lane's rule; the hub's messages carry none) |

## §2 The ruling card (H10)
**ESCALATION TO NICK · Task: the second candidate's knockout returned KILL.**
**Question:** accept the knockout and retire the candidate?
**Options:** (a) **ACCEPT** — the candidate retired on S5 + S2(EU); PALOKI stays the candidate of record; the swap program waits on the written opinion; NAMING-B re-armed as the fallback, dispatched only on a NOT CLEAR opinion — cost: nothing now; buys: the hours back. (b) send the candidate to counsel anyway — cost: ~$1,200 + a week; buys: nothing the `.com` fact does not already decide (a name whose exact `.com` is another business's fails the program's own rule before counsel reads a register). (c) a third candidate through the knockout now — cost: Nick's naming hour + a lane-evening; buys: a fallback in hand before `ERIK:` (Fri 09-25) instead of after.
**PM recommendation:** (a), with (c) available on the word `NAMING-B: go` at any time — the knockout lane is cut, its canaries run in an hour, and the S4 rig is the one instrument to fix first (IR-35).
**Refutable-by:** the `.com` owner selling at a price Nick accepts AND counsel reading the EU software mark as coexistable — two facts, both outside the record today.
**Blocking:** no — nothing on the map waits on it; the word is `NAME2: accept|counsel|third` (silence = accept).

## §3 The register
IR-35 — the ASR forced-choice rig: RS-13's control numbers do not reproduce (the lane's two configurations bracket them; the "(none)" artefacts were zero-padding); the instrument for the next knockout is a rig whose controls are re-run FIRST and whose padding is a recorded parameter — a research-lane hygiene row, no code.

## §4 Not re-executed
The GBD/EUIPO reads; the tmsearch POSTs; the channel indexes (the canaries the lane re-ran are the instrument); the rig.
