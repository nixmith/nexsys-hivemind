<!--
file: context/audits/2026-09-19_v77-b4_MEASURE-2b_and_FE-115_intakes_two-layer-audit.md
purpose: v77 beat 4 — MEASURE-2b and FE-115 intaken two-layer, both ACCEPT; the two core cards (separable bytes, one commit each); the findings landed as register rows and one Java work unit (AUTO-ID-1, AMD-93 §2.3) put ahead of LINK-READ; the hardware-setup plan for the box (the plan §20); the meter's manual facts banked.
audience: the hub · Nick (§0; §5 the words)
state-type: intake audit, layer 1 + layer 2
status: FILED Sat 2026-09-19 ~20:5x CT (instrument 2026-09-20T01:50:14Z)
-->

# v77 beat 4 — MEASURE-2b and FE-115 intaken (ACCEPT ×2); the cards; the plan for the box

## §0 One screen
- **MEASURE-2b — ACCEPT.** The census at porcelain = the return's (core 8 M + 2 A; hivemind the record M + the return A). The tee'd file's three `MEASURE2B` lines are the JUnit XML's `<system-out>` (3 matches; the true-500k line re-read from its own XML). Re-derived: 503.4 B/row at 500k = (247,332,864 + 4,338,392 + 32,768) / 500,013 ✓; q1 ×51 from 10k to 500k (1.769 → 90.481 ms) ✓; the rate 500,013 / 1,053 s ≈ 475 rows/s ✓ (the lane's 468.9 excludes its awaits). The gate's XMLs re-summed: zigbee 663 (1 skipped), app 30, 0 failures; **lifecycle's 82 NOT re-executable** — the later measurement runs (`--rerun`) overwrote its XMLs (1 test, 00:00:12Z); the gate log's task line and `BUILD SUCCESSFUL in 30s` stand, and CI on the push is the gate of record. The row-0 production diff read whole (27 + / 6 −): the slice's `zigbee.endpoint_classified: device=… endpoint=… entityType=… capabilities=…`; `withMeters`' `orElse(measurementOnly)` — `MeteringHandler.CLUSTER_ID` present → `ENERGY_METER`, else `SENSOR`. The fixture's boot sequence read against `bootAndAdopt` at `d1c2cbc` — the same calls in the same order plus Hero's three boot-smoke asserts moved verbatim (R1). **Every deviation ACCEPTED** (§2); the hub's own misses owned: P1's arithmetic (the table summed 8 M + 3 A while §0 said 9 M + 2 A; T1's home had no row), P3's T3 (already green at HEAD), the four "shared helpers" that were the rig's own (§11's premise, corrected by the lane at the bytes).
- **The two findings decide the Java slot's order.** F-1: `HomeSynapseCore.java:677` wires `InMemoryAutomationIdentityStore` — every restart mints new `AutomationId`s, and `explainNonFiring` answers `NEVER_TRIGGERED` of an automation with confirmed runs in the log (`q2_verdict=FIRED_CONFIRMED/NEVER_TRIGGERED hero_id_stable=false` on every line). Rehearsal 1 (kill −9, wk 09-28) IS a restart; one restart inside the 72-hour run files a false verdict against THE RECORD'S HONESTY. AMD-93 §2.3 already names the fix (the engine-managed identity companion `automations.ids.yaml` beside the definition file, AMD-71's layout). **AUTO-ID-1 goes BEFORE LINK-READ** (`JAVA-NEXT: autoid|linkread`, rec autoid; the fixture makes its test one method call: `restart()` then the id by slug equal and `explainNonFiring` still `FIRED_CONFIRMED`). F-2: the shipped sqlite-jdbc 3.51.3 plans `readByTimeRange` as a rowid walk (`SEARCH events USING INTEGER PRIMARY KEY (rowid>?)`), so `explainRun` is O(N): 90 ms at 500k — at the run's rows (≤ ~300k) under 60 ms; a register row (IR-40), fixed after the run.
- **FE-115 — ACCEPT.** 22 = 18 M + 4 A at porcelain, all under `web-ui/dashboard/`; the five pins read `v1.1.5-2026-09-19` and the three `v1.1.4` mentions left are the prose the lane named; the seven retired keys survive only in the test's retired list and two comments; the three fixtures carry the capture's sha256 prefixes (1 hit each); `git diff --stat` 18 M files, +1,021 / −57. **P4 is the finding of the lane:** the shipped artifact's three real bodies parse through HEAD's frozen validators with zero `ContractError` — FE-113's K1–K4 and the v1.1.4 validation are VERIFIED on the wire; the definition sentence is MIRRORED (no ≥ 114c causal-chain body exists yet — the H8-b read, the next frontend unit). `npm run verify` (29 files / 606 tests) NOT re-run from here (the device VM's `/sessions` disk is at 100 % — the lane's `/tmp` toolchain filled it; nothing on the mount is affected); `frontend.yml` on the push is the gate. Two observations become rows: the `A1` entities body carries no `pagination` key while the automations list does (a Doc 09 / freeze-text question, IR-42); the mock's `targetRef.type: 'ENTITY'` where the wire serves `entity` (hygiene, IR-43). The pin convention: from v1.1.5 the date is the MIRROR's landing date (FE-114's `-09-13` stays as history); the freeze doc's stamp line says so at its next edit.
- **The landing: TWO core commits, separable bytes, one push each** (no batched push; CI on each): card A MEASURE-2b (10 = 8 M + 2 A) → `CORE: M2B <sha>`; card B FE-115 (22 = 18 M + 4 A) → `CORE: FE115 <sha>`; `CI: <sha> green|red` ×2 (the counter's ninth and tenth samples on the bus-soak step).
- **The box (the plan §20):** nothing joins before Thursday; Thursday = the three plugs (CHAR; the b7 audit §0 + step 0b); the two sensors join at rehearsal 1 (wk 09-28), beside `REARM` — not on Thursday's tail; the presence sensor's place is beside the SNZB-03P (`PRESENCE: pair|bath`, rec pair — the corroboration pair the hero's "someone is home" condition is written for; the bathroom after the run); the Hue motion's place is the run's second trigger source, decided with VERIFY-72H's automation set (`HUE-MOTION:` carried). B1's manual facts banked for Thursday's script (§4).

## §1 Layer 1 — MEASURE-2b's claims, read
| Claim | Read | Verdict |
|---|---|---|
| census 12 = 9 M + 3 A | porcelain: core 10 rows (8 M + 2 A), hivemind the record M + the return A | ✓ |
| the gate green in one round | the gate log; the zigbee/app XMLs re-summed; lifecycle's XML overwritten later | ✓ (lifecycle disclosed) |
| T4/T5 green by the bus-soak tag | `_scratch/v77/…_T4_BusSoakIT.xml` (29,707 B) | ✓ (not re-run) |
| the four measured runs | the four tee'd files + the three JUnit XMLs; the stamps 23:39→00:20Z | ✓ |
| F-1 the identity re-mint | `HomeSynapseCore.java:677–:678` read: `new InMemoryAutomationIdentityStore(clock)` | ✓ at source |
| F-2 the rowid walk | the lane's `EXPLAIN QUERY PLAN` through the shipped driver; python's SQLite picks the index | on the lane's read (the plan not re-run here) |
| the rig-blindness sweep | 8 shared surfaces, 7 with an instrument, 1 (`EzspReportingOps` constants) none; the charter's four helpers are the rig's own | ✓ — the hub's premise corrected |

## §2 The deviations, ruled
R-a the stepped clock — ACCEPT (the frozen run shows q1 at 641 ms at 100k when every event shares one instant; the run's events carry real time; a knob keeps both). R-b adoption by config — ACCEPT; its foresight is a row (IR-41: a REST adopt path that calls the slice adopts meters silent). R-c the 12th file — the hub's. R-d T3 green at HEAD; T2's re-pin of an ENERGY-READ test that asserted `SENSOR` for the 0x0702-only signature — the b5 ruling R-5 governs; ACCEPT. R-e the gate cannot run T4 — the conventions; ACCEPT. R-f `BusPositionCensusIT`'s third boot copy — a row of AUTO-ID-1's table. R-g the classifier's line kept at INFO (two INFO lines per endpoint) — ACCEPT for Thursday (G4-3 reads `deviceType=`/`inputClusters=` from it); the fold to one line after CHAR. R-h the coder-handoff entry — filed by the hub this beat. R-i the 500k cap — ACCEPT; the true-500k row fills M2b. The [INFO] six — read, no act.

## §3 Layer 1 — FE-115's claims, read
The deviations: `[CHARTER-ERROR]` D4's "four hits" were two — the hub's over-count; `[RE-POINTED PIN]` the four v114 absence rows read `legacy-hub` (one import; every `expect` byte-identical) — ACCEPT, named; `[DEFAULT]` the sentence under the observed state — ACCEPT; P1's three fewer files (DevPanel renders the registry; RunChainView and client.ts needed nothing) — ACCEPT, the table over-named. The count-pin flip 140 → 134 was licensed by row D3. **Not re-executed:** `npm run verify` (see §0); the D4 lint run. **Register:** IR-42 (the `pagination` asymmetry), IR-43 (the mock's `ENTITY` case); the H8-b read (the definition sentence on a ≥ 114c artifact) is the frontend slot's next unit, on the bench card once step 0b lands `d1c2cbc` there.

## §4 B1 — the SURAIELEC UPWMA01A, from its manual (Nick's screenshots, Sat 09-19)
Modes by the button: 1 W + cost · 2 kWh + days · 3 V + Hz · 4 A + power factor · 5 min W ("Lo") · 6 max W ("Hi") · 7 price. Resolution as displayed: 0.1 W below 100 W (`3.3 W`), 0.001 A (`0.029 A`), 0.1 V, 0.01 PF. Ranges: W 0–1950 (the "OVERLOAD at 3680 W" line is the 230 V manual's; the US unit is 15 A / 120 V), A 0.00–15.00, V 110–130. **The backlight goes out 15 s after the last button press — Thursday's read step presses MODE to the wanted screen and reads within 15 s.** No accuracy spec is stated anywhere: B1 is the "no-spec generic" the meter chain designed (D-v75-6) — it attributes a dispute, it never carries a `within` verdict. Modes 5/6 hold the min/max since plug-in (an inrush tell for `SHAKE`); the kWh and day counters run from plug-in; settings persist ~3 months unpowered. For `SHAKE: mon`: B1 and B2 in series on the lamp, Mode 1 on both, a button press before each read, the settle = the first 30 s in which Mode 1 moves ≤ 0.1 W, the pair's difference in W and %.

## §5 The words (H10; silence = the recommendation) — the v77 DR carries them
`JAVA-NEXT: autoid|linkread` (autoid) · `PRESENCE: pair|bath` (pair) · `HUE-MOTION: second-area|pair|hold` (hold — decided with VERIFY-72H's automation set) · the standing four: `NAME2:` · `BENCH-CORE:` · `REARM:` · `SHAKE:`.

## §6 Not re-executed (disclosed)
lifecycle's gate count (overwritten); `npm run verify`; the EXPLAIN QUERY PLAN; T4/T5's run (the XML read only).
