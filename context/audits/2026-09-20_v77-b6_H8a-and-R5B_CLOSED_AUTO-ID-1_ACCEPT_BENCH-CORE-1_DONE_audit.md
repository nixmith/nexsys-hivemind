<!--
file: context/audits/2026-09-20_v77-b6_H8a-and-R5B_CLOSED_AUTO-ID-1_ACCEPT_BENCH-CORE-1_DONE_audit.md
purpose: v77 beat 6 — the sitting's two records CLOSED two-layer on Sunday's digest line (the pre-registration adjudicated: usb-reenumeration passed on the timer with nothing installed; a new 7/9 on command-confirm-s31, the vacuum on the S31 the pre-registered cause for Monday); AUTO-ID-1 intaken two-layer, ACCEPT; BENCH-CORE-1 intaken on its outputs file — step 0b DONE; the second deploy today.
audience: the hub · Nick (§0; §5 the acts)
state-type: intake + close audit, layer 1 + layer 2
status: FILED Sun 2026-09-20 ~10:1x CT (instrument 2026-09-20T15:15:24Z)
-->

# v77 beat 6 — H8-a and R-5B CLOSED; AUTO-ID-1 ACCEPT; BENCH-CORE-1 DONE

## §0 One screen
- **`R5B-2:` read at 12:33Z:** `2026-09-20 quiesced AUTO floor: 7/9 · FAIL command-confirm-s31 · bundle …/command-confirm-s31-20260920T083121Z · 1 SKIP(hue-online) · fleet: 6/6 · re-seen 6 · bench-hero RESTORED ✓ · ON-latency n/a(FAIL)`. **The pre-registration (the b2 audit §0) is CONFIRMED on its claim:** usb-reenumeration is not in the FAIL — it ran and PASSED on the timer with nothing installed; the manual run's FAIL was the sshd shell's `PATH`, as the mechanism said; no install, ever. **The floor is 7/9 for a new reason:** `command-confirm-s31` — the OR-NIGHTLY-0902-S31 class (09-12, 09-18, now 09-20: three of the last nine nights). This is the first nightly since the vacuum was plugged into the S31 (R-5B D-2/D-3: the S31 cut itself off 1–2 s into the vacuum's start, twice, Saturday) and the scenario commands the S31 ON. **Pre-registered for Monday:** with the vacuum OFF the S31 (Nick's act today), Monday's line reads `8/9 PASS · 1 SKIP(hue-online)`; refutable-by a fourth `FAIL command-confirm-s31` on Monday — then the class is the intermittent and its own instrument (the bundle's `api-captures` vs the journal) is chartered. Tonight's bundle verdict is read today (one ssh line, §5) so the two hypotheses are told apart at the bytes, not by the calendar. `re-seen 6` (not the manual line's `re-seen 0`) is the nightly's own restart re-linking six — P-B1's fleet clause `6/6` is what the gate grades; MET on both lines.
- **H8-a — CLOSED (hub-audited):** FE-113 → **VERIFIED** on K1–K4 (re-asserted by the hub at b2; doubled by FE-115's P4 — the same three bodies parse through the frozen validators with zero errors); O-2 → **CLOSED** (three clean readings on two artifacts); the capture is FE-113b's fixture set, now IN THE TREE as `wire-2026-09-19-h8a-*`; Row 7 banked (26 app WARN vs 0 journald warning-or-worse); Row 16 banked (0 `sys_*` refs); `S31-LABEL: 15A 1800W — Input 120V @ 60Hz` banked. **OR-FAILCHAN RE-SCOPED:** instance 1 (§6-B) and instance 5 (EXITCODE) CLOSED on the wire — two clean `success · 143 · inactive` grades on `6bd8508` beside R-4c's on `a458a64`; the `SuccessExitStatus=143` form was ruled at v62 b3 with the mask caveat as a constraint (the FAILCHAN instruction's H10) and is not re-litigated; instance 2 (the orderly serial close) is MEASURED STILL OPEN as IR-33 — §10-O's path never runs on the wire; the OR stays open on that instance. D-1..D-5 carried to rehearsal 1's packet through THE PRIOR-LEDGER GATE (`cut -c1-240`; the ssh-session slip; the desktop-prompt discipline); the residue (two empty dirs on hs-dev-1; an empty tree + a known_hosts line on hs-fresh) LEFT, named — nothing deleted.
- **R-5B — CLOSED (hub-audited):** the six gates as measurements; the fleet floor row = `fleet: 6/6 · re-seen 6 · adopted 0`, registry rows 6 byte-stable (the bench-minted ids re-read again today on the new Core, §3); P-B5's per-device lower bounds banked (SNZB-02P 23 · S31 4 · SNZB-03P 3 · SNZB-04P 0 in 254 s; the SNZB 2× twin multiplier); P-B2/P-B3/P-B6 NOT TESTED → `REARM: reh1` (D-v77-2); F-R4b-C carried; `C003:` unruled until `REARM`; the S31 fact row (the overcurrent cut-out under a vacuum's inrush; the vacuum comes off it today); bench-hero 4/4 confirmed in 77–228 ms on the real fleet (the record's first wire datum of "did it actually confirm" — C-3 stays unassertable outward, the register's supersession law); IR-34 (the journal silent at INFO); D-1..D-7 → rehearsal 1's packet (D-5 the S31 row by deviceId; D-6 the `~/r5b` copy; the ONE-device three-acts say-it-back choreography as the packet's form).
- **AUTO-ID-1 — ACCEPT.** The return (12,987 B) read whole; layer 2 from here: core porcelain = 8 M + 6 A exactly as the return's census; P4 `grep InMemoryAutomationIdentityStore` in the root = 0; P5 as re-read — ONE `requires org.snakeyaml.engine.v2;` and ONE `implementation(libs.snakeyaml.engine)`, both on lifecycle, none on automation; `HomeSynapseArchRules.java` untouched (diff 0); the root's diff = the two-line swap + the `beginLoad()`/`endLoad()` bracket with its two comments, nothing else; the gate's XMLs re-summed — automation 267 · lifecycle 93 · app 30, 0 failures; `MeasureReadPathIT`'s smoke XML reads `hero_id_stable=true`. The fork was raised with T1 red and no store written, ruled, and built as ruled (the seam, MAP form). **The seven [REVIEW]s, ruled:** (1) an identity asked outside the bracket throws — ACCEPT (fail-loud beats silent non-persistence); (2) a load re-stamps `last_seen` and writes once per boot — ACCEPT (retention counts from the last sighting, Doc 07's "removed" semantics; P6's byte-stability holds under a frozen clock, which is what the test proves); (3) the three extra rows (two javadoc sentences made false; `HomeSynapseCoreStartupFailureTest` as T8/T9's home) — ACCEPT, the hub's table under-named them; (4) `recommendationFor` has no `automation` arm (the FATAL report shows the default sentence, exit 99) — a row, IR-44; (5) `beginLoad` reads once per store — ACCEPT; (6) the store sorts, the companion preserves — ACCEPT; (7) the `INV-GA-02` mis-cite — the hub's, corrected by the lane to LTD-04. The staged-red predictions filed 12 s after the run ended, before its output was read — disclosed by the lane; noted, not a defect of the evidence (the reds are in the XML). The blank-slug bug found in self-review, fixed and mutation-checked — the kind of finding the return form exists for. **Deploy note R5 stands:** the first boot on this build mints every id once and writes the companion.
- **BENCH-CORE-1 — DONE (step 0b).** The outputs file (5,467 B, five sections) read: the bench clone was at `e845cd9` (R-9, 08-22; 38 behind; porcelain 0); node v22.23.1 · npm 10.9.8 · Java 21.0.11; 99 GB free; the backup (`~/hs-backup/20260920T150109Z`: the store 89.7 MB by `.backup`, the config, the old install tree); the pull fast-forwarded to `c819a02`; `BUILD SUCCESSFUL in 1m 59s` (33 executed — the dashboard's npm build included); `~/bench.sh restart` → RADIO UP after 14 s; `[PASS] boot-health — 6/6 positive · 0 forbidden`; `network_resumed: channel=20 panId=0x774c`; formed=0 resumed=1 relinked=6 adopted=0 config_issue=0; rows 186,270 → 186,279; integrity ok; **every registry row's `deviceId` a ULID** (the old Core printed `None`) — the proof; `deployed=c819a02`. One instrument defect of the card's, owned: block 2's `echo` printed `log=~/hs-bench/build-.log` — `$S` was set inside the backgrounded subshell and read in the foreground; the poll found the log by mtime, so nothing was lost; the fix is one line (set `S` before the `&&` chain) — carried into the re-run. **The second deploy is today, not Wednesday:** a pull + `installDist` is two minutes, so once AUTO-ID-1 is green at CI the same card runs again and Monday's nightly is the first line on durable ids (`BENCH-CORE-2: today|wed`, rec today, D-v77-8).
- **The window's deliverable is MET:** the two intakes CLOSED; MEASURE-2b and FE-115 GREEN at CI. v77 closes at b7 with v78's text once `CORE: AID` and its CI bank.

## §1 Layer 2 — what was re-executed and what was not
Re-executed: the AUTO-ID-1 census, grep, diffs and XML sums (above); the BENCH-CORE-1 outputs file's five sections against the card's EXPECTED lines (every line met). Not re-executed (disclosed): the nightly bundle's verdict (Nick's one line today); the timer run itself; the AUTO-ID-1 gate (the XMLs are the instrument; CI on the push is the gate of record).

## §2 The register
IR-44 — `app/…/StartupFailureReport`'s `recommendationFor` has no `automation` arm: a FATAL identity companion prints the default recommendation and exit 99 (the AUTO-ID-1 return [REVIEW] 4); one arm + one test; Trivial; OPEN (v77 b6).

## §3 The open risks, edited this beat
OR-FAILCHAN — the status line above (instances 1 + 5 CLOSED on the wire; instance 2 = IR-33 open). OR-NIGHTLY-0902-S31 — the 09-20 reading and the vacuum pre-registration for Monday.

## §4 The words banked
`R5B-2: <the line>` · `HIVE: LANDED ca2ff90` · `AID: RETURNED` (12,987) · `BENCH-CORE: deployed c819a02 · boot-health 6/6 · rows 186270→186279`.

## §5 The acts (the b6 packet)
Block 1 — the vacuum OFF the S31 (a physical act; the S31 itself untouched) + one read-only line for tonight's bundle verdict → `VACUUM: off · <the verdict line>`. Block 2 — core card A: AUTO-ID-1 (14 = 8 M + 6 A) → `CORE: AID <sha>` → `CI: <sha> green|red`. Block 3 — the hivemind card (9 = 7 M + 2 A) → `HIVE: LANDED <sha>`. Block 4 — after `CI: green`: BENCH-CORE-1's blocks 1–3 again on the new sha (`BENCH-CORE-2`) → `BENCH-CORE: deployed <sha> · boot-health 6/6 · rows … · ids.yaml present`.
