<!--
file: context/audits/2026-09-20_v77-b7_close_audit.md
purpose: v77 beat 7 — THE CLOSE: the weekend's last words banked at the bytes (`VACUUM: off`, `CORE: AID 13d439f`, `CI: 13d439f green`, `HIVE: LANDED d21e502`, `BENCH-CORE: deployed 13d439f`); the 09-20 bundle's verdict and the S31's state adjudicated; BENCH-CORE-2 DONE with one read skipped (`automations.ids.yaml` → Monday's `IDS:`); the knowledge base consolidated (the plan §21 STATE OF THE SYSTEM); the window's context size noted; the v78 text filed; v77 CLOSED at seven beats.
audience: the hub (v78's boot reads §0 by pointer) · Nick (§0; §6 the act)
state-type: close audit, layer 1 + layer 2
status: FILED Sun 2026-09-20 ~11:1x CT (instrument 2026-09-20T16:14:54Z)
-->

# v77 beat 7 — THE CLOSE

## §0 One screen
- **The window's deliverable (named b1, re-named b2) is MET and banked:** the two intakes CLOSED two-layer on `R5B-2:` (b6); MEASURE-2b `a6b56d6` and FE-115 `c819a02` GREEN at CI (b5); and past it AUTO-ID-1 `13d439f` GREEN on Nick's line ("all checks passed GREEN") → **the closure counter 11/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508` · `e56f555` · `3d40b5f` · `3af6213` · `d1c2cbc` · `a6b56d6` · `c819a02` · `13d439f`; the last four on Nick's line, the bus-soak XMLs unread from here).
- **The HEADs at the instrument (16:01Z):** core `13d439f`, hivemind `d21e502`, porcelain 0 ×2, no `.lock`; the splice library md5 `d6780d13…` unchanged; the chain archive 148 counted headings before this beat's rotation (→ 149).
- **THE THURSDAY ORDER step 0b is DONE TWICE (BENCH-CORE-1 → BENCH-CORE-2):** the bench card (hs-dev-1) runs core `13d439f` from its own `installDist` tree — the outputs file (`_scratch/v77/2026-09-20_BENCH-CORE-1_outputs.txt`, 10,191 B, nine sections) reads `deployed=c819a02` at 15:04Z (BUILD 1m 59s; the backup `~/hs-backup/20260920T150109Z`) and `deployed=13d439f` at 15:52Z (BUILD 7 s — Gradle's cache on a 14-file diff; the backup `…T155037Z`); `[PASS] boot-health — 6/6 positive · 0 forbidden` both times; `zigbee.network_resumed: channel=20 panId=0x774c`; `formed=0 resumed=1 relinked=6 adopted=0`; `registry rows=6` (every `deviceId` a ULID; two rows' `lastReported` predate September — 07-19 and 08-30 — a datum, not adjudicated here); the event rows 186,270 → 186,279 → 186,353 → 186,359 (the store live across both restarts). **One read the packet asked for was not run:** `ls -l ~/hs-bench/config/automations.ids.yaml && head -8 …` — the outputs file carries no such line; the companion's presence on the bench card is UNVERIFIED → Monday's `IDS:` line (the v78 text names it as the first message). The mechanism claim stands unread, not refuted: `HomeSynapseCore.java` wires `new CompanionAutomationIdentityStore(new FileAutomationIdentityCompanion(configDir.resolve("automations.ids.yaml")), clock)` and `endLoad()` replaces the file on the first load (the AUTO-ID-1 return; T1 on `RealCoreFixture` green at the gate).
- **`VACUUM: off · CONFIRMATION_TIMED_OUT`, read twice (15:41:20Z with the vacuum still on the S31 and the S31 ON; 15:42:15Z after Nick switched the S31 OFF and unplugged the vacuum):** the bundle `command-confirm-s31-20260920T083121Z`'s `verdict.txt` reads `FAIL — terminal phase mismatch: expected CONFIRMED, read CONFIRMATION_TIMED_OUT` on commandId `01M2YZ1RQQBTTBQTK765169TZR`, identical on both reads (a file from 08:31Z; the second read could add nothing). CONSISTENT with the pre-registered cause (the S31 under the vacuum's inrush — the b6 audit §0): the command left Core and no confirmation came back inside the window. NOT proven by this bundle — it cannot tell an overcurrent-tripped relay from a radio miss; Monday's line is the instrument. **The S31 press:** the b6 packet's block 1 said the S31 stays powered and untouched; Nick switched its relay OFF (~15:42Z). Ruled HARMLESS on the scenario's own text — `nexsys-bench/scenarios/command-confirm-s31.yaml:1–:2, :61–:62`: the leg asserts ONE `turn_on` that must be a REAL ON-edge; a relay already OFF is the precondition the leg wants, not a breach of it. The S31 stays OFF with nothing in it until rehearsal 1's packet says otherwise (the v78 text's REFUSE line).
- **Monday's pre-registration (unchanged from b6):** `R5B-3:` = `8/9 PASS · 1 SKIP(hue-online)`; refutable-by a fourth `FAIL command-confirm-s31` (09-12, 09-18, 09-20 so far) → the intermittent becomes its own OR row with an instrument (the bundle's `api-captures.json` against the journal's zigbee lines).
- **No word is owed on the v77 DR:** D-v77-1 DONE, D-v77-5 EXECUTED, D-v77-8 DONE; D-v77-2/3/4/6/7's recs stand by silence into v78 (`REARM: reh1` · `SHAKE: mon` · `NAME2: accept` · `PRESENCE: pair` · `HUE-MOTION: hold`). The DR's status → CLOSED.
- **The knowledge base (Nick's ask):** the plan **§21 STATE OF THE SYSTEM** is the organized digest — the five artifacts, the two cards, the fleet, the store and the read path, the identity companion, the dashboard contract, the hardware and its order, the open risks and fences, the words and the calendar — every line re-read from disk this beat; the snapshot re-cut on it; the coder-handoff stamped (AUTO-ID-1 LANDED; NEXT LINK-READ); the v78 text filed at `context/handoff/2026-09-20_v78_dispatch-text.md`.
- **The context window (Nick's ask):** the harness COMPACTED this conversation before this beat — the prior context (the hub's estimate ≈650K tokens over b1–b6, most of it the lanes' returns and the two records read at the bytes) was replaced by a summary; the live context at this beat is ≈45–50K tokens. The harness counter (`total_tokens … left`, ≈14.8M) is a remaining BUDGET, not the context size, and never was. Disclosed consequence: every claim in this beat was re-read from disk after the compaction (the HEADs, the outputs file, the bundle file, the spine, the brief, the scenario text) — nothing rests on pre-compaction memory. v77 closes at seven beats as planned at b6; "≤8 beats per window" held; a compacted window is one more reason the v78 text opens a FRESH conversation Monday.

## §1 Layer 2 — what was re-executed and what was not
Re-executed at the bytes this beat: the two HEADs and porcelains; the outputs file's nine section stamps, both `deployed=` lines, both `BUILD SUCCESSFUL` lines, both `[PASS] boot-health` lines, the `relinked=6 adopted=0` line, the four row counts, the six registry rows; the bundle file's two reads (both `CONFIRMATION_TIMED_OUT`, one commandId); the scenario's precondition text (`command-confirm-s31.yaml:1–:2, :61–:62`); the chain archive's heading count (148); the brief's byte count against its cap. NOT re-executed: CI on `13d439f` (Nick's line; the run's XMLs unread from here — the same disclosure as `c819a02`); the bench card's `automations.ids.yaml` (unread by anyone); the AUTO-ID-1 gate XMLs (267/93/30 at b6, not re-summed).

## §2 The register
No new row. IR-33..44 are the weekend's; the BENCH-CORE-1 card's `$S` echo defect (block 2; fixed in the b6 packet's block 4) is a card defect corrected in place, not a register row. IR-44 (the `StartupFailureReport` arm) stays OPEN for the next core session that touches `app/`.

## §3 The open risks, edited this beat
OR-NIGHTLY-0902-S31: one status line — the 09-20 bundle's verdict read; the S31 OFF and unloaded; Monday's line the instrument. No row opened or closed.

## §4 The words banked this beat
`VACUUM: off · CONFIRMATION_TIMED_OUT` (15:41Z/15:42Z) · `CORE: AID 13d439f` · `CI: 13d439f green` · `HIVE: LANDED d21e502` (the b6 card, 10 = 8 M + 2 A) · `BENCH-CORE: deployed 13d439f · 6/6 · 186353→186359` (`ids.yaml` NOT READ). Banked at b6, carried: `R5B-2:` · `AID: RETURNED` · `HIVE: LANDED ca2ff90` · `BENCH-CORE: deployed c819a02`.

## §5 The plan from here (the v78 text is the record; this is its shape)
Monday (v78 b1–b3): `R5B-3:` + `IDS:` intaken; LINK-READ through the gate on the fixture at `13d439f`; the strategy pass; VERDICT-VOCAB-1 + PELTON-READY; `HARNESS-PLUG:`; `SHAKE: mon` in the evening; `FOP: apply`; AMD-100's docs card. Tuesday: `R5B-4:`; LINK-READ's intake → the core card; rehearsal 1's packet through the prior-ledger gate with `REARM` + the two sensors' join; THE THURSDAY PACKET; VERIFY-72H's charter; v79's text. Wednesday: `R5B-5:`; the H8-b read chartered; `BOX:`; `ERIK:` if it comes. Thursday 09-24 (v79): the P4460 → THE THURSDAY ORDER → CHAR. The watts go/no-go Sun 10-11 20:00 CT; the run Oct 30 – Nov 2.

## §6 The acts (the b7 packet — ONE card)
The hivemind card, 10 = 8 M + 2 A → `HIVE: LANDED <sha>`. Then nothing until Monday morning: read `R5B-3:` and `IDS:`, paste the v78 text into a FRESH conversation, give the two lines as its first message.

## §7 Hub errors owned this beat
(1) The b6 packet's block 4 put "`ids.yaml present`" into the say-back before anyone had read the file — a prediction in the shape of a report line; Nick answered what he ran, and the read moved to Monday. A say-back names what the block READS, never what the hub expects. (2) The same packet asked for the vacuum's cord out "with the S31 untouched" without saying why the relay mattered — it did not (§0, the scenario's own text); an "untouched" carries its reason or is dropped. Both fold into THE SURVEY IS RUN, NOT ASSIGNED (pm-lessons 2026-09-20).
(3) The beat's splice put the packet's count assertion (`TRAILER-PRESENT-STOP == 2`; it is 3 — the card's Read line names it) in PHASE 2, after PHASE 1 had written: attempt d wrote the spine whole and stopped on the packet; attempt e (`v77b7_splice_e_packet.py`) re-verified the HEADs, the archive count and the census at porcelain (10 = 8 M + 2 A) and wrote the packet only — nothing double-written. The b3 lesson repeated: every assertion on a text the splice generates is computed in PHASE 0 on that text, before the first byte. Attempts a–b stopped at the snapshot probe (3,770 / 3,606 > 3,500), c at the trailer guard reading pm-lessons' own 2026-09-18 prose (the guard now runs on the hub's new text and the spine, as at b6) — all three before any write.
