<!--
file: context/audits/2026-09-21_IR40-IR44_return.md
purpose: Coder return for IR-40 + IR-44 (instruction 2026-09-21_coder-lane_IR40-IR44_…_coding-instruction.md) — the time-range read pinned to its index; the startup report's automation arm.
status: RETURNED 2026-09-21 (CT) — filed by the Coder lane; awaits the hub's intake.
-->

# IR-40 + IR-44 — return

## §0 Verdict and census
**DELIVERED, uncommitted.** Baseline re-verified `d2cddb1` (porcelain clean at start); `date -u` 2026-09-21T23:24:48Z. `./gradlew check --offline` **GREEN** — `BUILD SUCCESSFUL in 53s`, exit 0; the persistence/lifecycle/app test tasks EXECUTED (not UP-TO-DATE); XML: persistence 416 · lifecycle 95 (94 + T10) · app 30 · automation 267 (UP-TO-DATE, untouched) — 0 failures; filtered green-run XML mtimes 23:46:59Z / 23:47:03Z. Porcelain (`git --no-optional-locks status --porcelain`), EXACTLY §3's files — **5 M + 1 ??**:
```
 M core/persistence/MODULE_CONTEXT.md
 M core/persistence/src/main/java/com/homesynapse/persistence/SqliteEventStore.java
 M lifecycle/lifecycle/MODULE_CONTEXT.md
 M lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java
 M lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/HomeSynapseCoreStartupFailureTest.java
?? core/persistence/src/test/java/com/homesynapse/persistence/SqliteEventStoreTimeRangePlanTest.java
```
Zero commits, nothing staged; six files LF. Hivemind: this file (+1 ??). No Deferred Build Gate (`check` ran in-lane; CI on the landing sha is the gate of record). Cap arithmetic 3 KB + 5 rows ≈ 8 KB ≤ 12,000 B.

**The mechanism finding that reshaped T1 (read first).** The plan is neither size-dependent nor the walk unconditionally: sqlite-jdbc 3.51.3 reads the value bound to `LIMIT ?` on re-prepare and plans against it. On the migrated schema, at 0 AND at 100k rows, without `sqlite_stat1` (the store never runs `ANALYZE`): bound `maxCount` 1…400 (12 values swept) and UNBOUND → `SEARCH events USING INDEX idx_events_event_time (<expr>>? AND <expr><?)` + `USE TEMP B-TREE FOR ORDER BY`; bound 500 · 1000 → `SEARCH events USING INTEGER PRIMARY KEY (rowid>?)`. `triggeredInWindow` binds `SCAN_BATCH = 500` (`StandardExplanationService.java:115`, `:1146`) — one step past the flip — so F-2's walk is production's plan; the premise holds. `INDEXED BY` lands: the plan becomes invariant under the bound page size (instrument: a scratch probe test, created and deleted in-lane; log `_scratch/v78/2026-09-21_IR40_plan-limit-probe.txt`). The `:771–:774` sibling (`LIMIT 1` literal, no ORDER BY) is already `SEARCH events USING COVERING INDEX idx_events_event_time (<expr>>? AND <expr><?)` at 0 and 100k rows → left unchanged per §1.

## §1 What changed
| File | Change |
|---|---|
| `SqliteEventStore.java` | `:220–:234` Javadoc (the walk under a bound LIMIT ≥ 500; F-2; the loud failure); `:235` `private static final` → **`static final`** (§4: package-private so the plan test reads the exact SQL — said here); `:237` `+ " INDEXED BY idx_events_event_time"` between `FROM events` and `WHERE`. Nothing else. |
| `SqliteEventStoreTimeRangePlanTest.java` | NEW, 293 lines: T1–T3 on a `@TempDir` store built by the real V001–V005 migrations via `DatabaseExecutor.start` (the `…DecryptFailClosedTest` idiom); `EXPLAIN QUERY PLAN` on the store's own read connection (shipped driver, production PRAGMAs); fixed clock. |
| `HomeSynapseCore.java` | `:1646–:1653` the `automation` arm in `recommendationFor` (the subsystem string: the literal `initializing = "automation"` at `:676`, the only one). |
| `HomeSynapseCoreStartupFailureTest.java` | `AUTOMATION_RECOMMENDATION` pinned verbatim (`:86–:93`, the file's shape); T10 `automationCompanionFatal_recommendsTheFileAndTheAct` (`:289–:323`). |
| `core/persistence/MODULE_CONTEXT.md` `:531` · `lifecycle/lifecycle/MODULE_CONTEXT.md` `:247` | one row each. |
No module-info, Gradle, migration, configuration or `core/automation` change. `HINT_LEAD = 1 s`, `HINT_LAG = 60 s` (`:128–:129`): the window's few rows are sorted by position for the LIMIT.

**The arm's text (C12-04 voice):** "restore automations.ids.yaml (the engine-managed automation identity companion beside homesynapse.yaml in the config directory) from the newest ~/hs-backup/<stamp>/config/ copy, or delete it so the next boot mints fresh ids and logs automation.identity_loaded first_boot=true (past runs then no longer link to their automations); never hand-edit it — the exception names the path". "says so" = `CompanionAutomationIdentityStore.java:172–:175` logs `first_boot=true` on an absent companion; the consequence clause is MEASURE-2b F-1. `~/hs-backup/<stamp>/config/` is the BENCH-CORE-1 card's layout — the only backup convention on record (zero hits in `homesynapse-core-docs`).

## §2 The tests (red first; predictions filed 23:40:14Z in `_scratch/v78/2026-09-21_IR40-IR44_red-predictions.txt`)
Staged red: seams inert (the constant package-private, SQL unchanged; no arm) → run → the changes → run.
| Test | Predicted | Observed at `d2cddb1` | After |
|---|---|---|---|
| T1 `plan_usesTheEventTimeIndex` (bound maxCount 1/7/400/500/1000 + unbound) | RED | **RED** at `maxCount=500`: `[SEARCH events USING INTEGER PRIMARY KEY (rowid>?)]`, "to contain: USING INDEX idx_events_event_time" (1/7/400 already the index — the finding) | GREEN |
| T2 `plan_semanticsUnchanged` | GREEN | GREEN (the guard; vacuous of the change by design) | GREEN |
| T3 `plan_failsLoudlyWithoutTheIndex` | RED | **RED** "Expecting code to raise a throwable" (the walk needs no index) | GREEN: `RuntimeException` → root `SQLException` "no such index: idx_events_event_time" |
| T4 (file T10) `automationCompanionFatal_recommendsTheFileAndTheAct` | RED | **RED** `recommendation=inspect the log and the JFR recording; report with the phase and subsystem named` | GREEN |
**Disclosed miss:** my FIRST red run bound `maxCount = 7` only → T1 GREEN at HEAD (blind to F-2). The probe found the LIMIT axis; T1 was reshaped to sweep it and re-run red before row 1 (`_scratch/v78/…_red-run*.txt`, `…_green-run.txt`). T2: 1,000 `publishRoot` rows (`event_time` stepping 3.6 s; every tenth NULL with the fixed clock INSIDE the window, so the `COALESCE` fallback rides `ingest_time`), 175 window rows; the page after the window's midpoint at `maxCount` 7 (`hasMore` true) and 500 (all remaining, `hasMore` false) equals the list computed from the inserted envelopes. T3 drops the index via the write coordinator on the live store. T10 asserts the REPORT (§9): `contains(new StartupFailureReport(CORE_DOMAIN, "automation", AUTOMATION_RECOMMENDATION))`, the file/act tokens, ONE `lifecycle.startup_failed` line with the same keys, the file's bytes untouched; the exit code follows from `subsystem()` alone (`ExitCodes.java:51–:56`: `automation` → 99) — pinned by the subsystem (app/ out of scope §11).

## §3 Deviations
- **[INFO] visibility:** `SELECT_BY_TIME_RANGE_SQL` package-private (§4 sanctioned). **[INFO] T1's shape:** a sweep over bound `maxCount` values, not the one EXPLAIN §7 named — a single small binding is green at HEAD.
- **[INFO]** T2's second act at `maxCount = 500` (the walking side at HEAD) beside the specified 7; T4 carries the instruction's method name under `@DisplayName` "T10 (IR-44 T4)" (the file already holds T4–T9).
- **[INFO] the arm's wording** makes "says so" the concrete token `automation.identity_loaded first_boot=true` and names the F-1 consequence; `<stamp>/config/` refines `~/hs-backup/` from the card. Operator text — the hub's to re-cut.
- **[INFO] instrument slip:** the measurement was launched under the Bash tool (10-min ceiling); my "stop" to relaunch it detached killed only the shell — the original `gradlew`/daemon/worker ran on and the relaunch failed in 8 s on the locked `output.bin`, truncating the shared log. The ORIGINAL run (the command verbatim) completed; its JUnit XML is the record (§7).
- No `[REVIEW]`/`[BLOCKING]`. No frozen token touched (`FROZEN`: none in persistence, lifecycle's only hit `bus.position_census`; `containsExactly` in the failure test: none; the default recommendation pinned nowhere).

## §4 The survey as found (`d2cddb1`)
`readByTimeRange(`: production — `EventStore.java:141`, `SqliteEventStore.java:672`, `StandardExplanationService.java:1146` (the ONE caller); tests — `InMemoryEventStore:225`, `FakeEventStore:68`, four delegating doubles (`ByIdReadPageCountTest`, `StandardExplanationServiceTest`, `DeliveryAnomalyEmissionTest`, `ReadForwardDeliveryTest`), `EventStoreContractTest:487/:564`. `SELECT_BY_TIME_RANGE_SQL`: `:221` + `:690` only. `recommendationFor`: `:1638` def, `:465` call; `StartupFailureReport` built at `:467` only; `recommendation()` read by `Main.java:184`. `initializing` literals: unknown · configuration · persistence · event-bus · device-model · state-store · **automation** (`:676`) · observability · rest-api · integration. `ids.yaml` fixture reused: T8's `writeCompanion`.

## §5 Findings for the register
- **F-A `LIMIT ?` plans by bound value.** The other `LIMIT ?` reads (`SELECT_FROM_SQL`, `SELECT_BY_SUBJECT_SQL`, `SELECT_BY_TYPE_SQL`) have indexes matching their ORDER BY, likely stable but unpinned; a plan test per query (the `explain` idiom) closes it.
- **F-B no product backup convention.** The arm cites the bench card's `~/hs-backup/<stamp>/config/`; the docs repo names none — if a layout is formalized, the string follows it (one arm, one pin).
- **F-C the sibling's plan is unpinned** (`hasMoreForTimeRangeAfterPosition`: a covering-index plan by the planner's choice; runs only when a page fills).
- **F-D doc drift:** `EventStore.java:124–:125` names REST/retention callers; the only production caller is `explainRun`'s hint.
- **Lesson (for coder-lessons):** *A plan test must bind the production caller's values.* SQLite re-prepares on a bound `LIMIT ?` and plans against it: the same SQL is index-planned at LIMIT 7 and rowid-walked at LIMIT 500. Read the plan with the caller's constants, sweep the axis, pin with `INDEXED BY`. Detail: this return §0, §2.

## §6 The coder-handoff entry (for the hub to splice)
**IR-40 + IR-44 DELIVERED (2026-09-21, uncommitted on `d2cddb1`): 5 M + 1 A.** `SELECT_BY_TIME_RANGE_SQL` gains `INDEXED BY idx_events_event_time` (package-private); `SqliteEventStoreTimeRangePlanTest` T1 (the plan at bound maxCount 1/7/400/500/1000/unbound) · T2 (semantics vs a data-computed list) · T3 (index dropped → `no such index`); `recommendationFor` gains the `automation` arm, `HomeSynapseCoreStartupFailureTest` T10 pins it verbatim. `check` GREEN in-lane (persistence 416 · lifecycle 95 · app 30). Finding: the planner flips on the BOUND `LIMIT` value (index ≤ 400, walk ≥ 500) — `SCAN_BATCH = 500` walked. MEASURE-2b re-read: §7. NEXT WU: the hub's (IR-45 per the v78 plan); the Coder stays blocked on the next instruction.

## §7 The measurement (read, not gated) and instrument limits
Command verbatim (§7), the ORIGINAL run; record `_scratch/v78/2026-09-21_IR40_MEASURE-2b_readpath_junit.xml` (1 test · 0 failures · 880 s); the three lines: `…_readpath.txt`. **q1_ms median (F-2's):** 10k **1.204** (1.769) · 100k **1.007** (10.633) · 243k capped **1.988** (42.267; 90.5 true-500k); cold q1 1.923 / 1.291 / 2.105 (47.918 at 243k). Pre-registered q1(243k) ≤ 3 × q1(10k): **HELD** (1.988 ≤ 3.61) — q1 is flat in N. q2–q6 within noise: at 243k q2 1.160 (1.055) · q4 0.910 (0.915) · q5 1.431 (0.948; min 0.929) · restart 933 (924). Every row `hero_id_stable=true` (AUTO-ID-1 in effect).
Limits: one desk run per N, medians of k = 5 warm reads; the desk is not the runner; the 500k row is capped at 242,999 rows by the IT's 10-minute seeding cap, as F-2's record run was (true-500k: F-2's supplementary 90.5 ms). The probe's rows were raw single-transaction inserts; T2's are real publishes. CT filing date 2026-09-21.
