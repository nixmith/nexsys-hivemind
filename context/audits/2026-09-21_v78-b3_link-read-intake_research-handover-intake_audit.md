<!--
file: context/audits/2026-09-21_v78-b3_link-read-intake_research-handover-intake_audit.md
purpose: The v78 beat-3 audit — LINK-READ's return intaken two-layer (ACCEPT; the lane's five deviations ruled; the hub's two AUDIT CORRECTIONS owned); the core card cut; the research Project's FINAL HANDOVER intaken (layer 1 whole, layer 2 sampled at the tree); today's three knowledge checks read as returns and their K-set folds listed; the second upload round's stray; D-v78-3's recommendation after the ADR read; D-v78-5 RULED; the b2 card OVERTAKEN.
audience: the hub · Nick (§0, §3) · the v79 hub
state-type: audit (filed)
status: FILED v78 beat 3 (Mon 2026-09-21 ~13:3x CT; instrument 2026-09-21T18:36:51Z)
-->

# v78 beat 3 — LINK-READ ACCEPT; the research layer intaken

## §0 Verdict
**LINK-READ — ACCEPT.** The return (`context/audits/2026-09-21_LINK-READ_return.md`, 13,678 B, §0 first) delivers the plan's row 5 as chartered and corrected: the counter, the reading on every transition on the sibling line, the sampling rule; the frozen DP-8 line byte-identical; `check` green on the lane's tree and forced fresh (zigbee 679 · lifecycle 94 · app 30; 0 failures). The core card is cut: 12 = 9 M + 3 A → `CORE: LR <sha>` → `CI: <sha> green` → the counter 12/20. **The handover — ACCEPT as input** (nothing in it has charter standing until the hub charters it); the research layer is placed inside the plan (§22) with the words RULED today. **UPLOAD-2 — one stray:** the prefixed check found `00_README.md` still in the Project's knowledge; the round completes when it is deleted and the same check prints `none`; `LAUNCH: 7a` waits on that line.

## §1 LINK-READ — layer 2 (the hub's re-execution at the bytes)
- Porcelain in core = the twelve files the return lists = §3's eleven rows (row 7 two files); nothing staged; HEAD `13d439f`.
- The frozen line: `git diff -- StandardAvailabilityTracker.java` touches no `availability_changed` text; the statement stands at `:393`; the wiring test's diff removes 0 lines (+117) and its two pins are present verbatim (5 occurrences of the pinned string); the new token is pinned (9 occurrences).
- The tokens at the sites named: `zigbee.availability_link` (`ZigbeeIntegrationAdapter.java:1495`), `zigbee.link_summary` (`:618`), `zigbee.link_reading_suspect` (`ZclIngestionUnit.java:662`, once per device), `LINK_SUMMARY_PERIOD = Duration.ofMinutes(10)` with its Javadoc (`StandardAvailabilityTracker.java:62–:79`).
- `LinkReading`: `public record LinkReading(int lqi, int rssiDbm)` (`:37`), the wire domain in its Javadoc (`:16`), `public static Optional<LinkReading> fromWire(int, int)` (`:73`); the seam signatures carry `Optional<LinkReading>` (`ZclIngestionUnit.java:160`, `AvailabilityTracker.java:45`, `StandardAvailabilityTracker.java:184`, `ZigbeeIntegrationAdapter.java:1315`).
- The tests: the result XMLs re-tallied from disk — zigbee 81 files / 679 tests / 1 skipped / 0 failures, mtimes 18:16:23Z; lifecycle 23 / 94 / 0 / 0 at 18:16:47Z; app 7 / 30 / 0 / 0 at 18:16:51Z — inside the forced run's window (18:16:18–18:16:51Z); `LinkReadIT`'s XML carries the sibling token. The evidence files exist under `_scratch/v78/2026-09-21_LINK-READ_*` (predictions, red runs, green runs, two mutation runs, the two check logs).
- Clock hygiene: no `Instant.now()` / `Clock.systemUTC()` / `nanoTime` / `currentTimeMillis` in the added lines.
- Not re-executed: `./gradlew check` itself (the lane's log is the evidence; CI is the gate of record); the mutation runs (their logs read, not re-run).

## §2 The deviations ruled
D-1 [INFO] `route()` package-private for T8 — ACCEPT (the module's test seam is package-private by convention: `runCycleOnce()`, `evaluateTimeouts()`). **D-2 [REVIEW] — KEEP as implemented:** one summary line per CACHED device, `frames=0` rows and `-` placeholders included. The silent device's `frames=0` is the datum LINK-READ exists to show; a spoken-only line would hide the silence the run is about. The cost is N lines per ten minutes (six devices today; the fleet freezes at rehearsal 2); the knob is §11's later unit if a sitting asks. D-3, D-4 [INFO] — noted; T6e (256 × 256 through the real parse) is the §12 clause made exhaustive. D-5 — the pre-dedup count is the radio-level count (the ×2 twin counts twice; T4 pins it); `LinkReading` public — as §16 says.
**The hub's errors owned (the return §4, §16, §15):** the P2 count of `recordFrame` test call sites was read off a `head -12`-truncated grep listing (eight printed of eleven); the RSSI bound written as a type invariant on the hot path; the frozen token absent from the P2 survey. Two mints for pm-lessons at the close: a frozen log token is a pin (the P2 survey greps `FROZEN` in MODULE_CONTEXT for every file it touches); a count is read from an uncapped listing or not stated.

## §3 The core card (Nick's hands) — `_scratch/v78/2026-09-21_v78-b3_packet.md` block 1
The message `_scratch/v78/2026-09-21_core_LINK-READ_commit-msg.txt` (Nick's form; no trailer); the card adds the twelve paths, checks `staged: 12`, greps the message for a trailer, commits, pushes. Say back `CORE: LR <sha>`, then `CI: <sha> green` when the run reports — the counter's twelfth. LINK-READ deploys to the bench card only after THE THURSDAY ORDER (the entity count is untouched by this unit, but the card's Core is pinned to `13d439f` until step 0b is re-run by design).

## §4 The research handover — intaken
Layer 1: read whole (§0–§4, §7–§11; §5–§6 by their headings). Layer 2 at the tree (b1): the claim register's three rows, the ADR's bytes and unruled word, the watermark, the empty `context/research/` — all HOLD; the one wording divergence (DRAFT vs PROPOSED) noted. The four returns it names are now filed, but as today's re-runs: Q1 10,662 B, Q2 14,285 B, Q3 26,628 B (the handover's 14,006 · 14,977 · 16,322 were Sunday's first round, unfiled). The re-runs ran as full returns — the prefix was not in front of them — and are read here as such: the answers pass file 10's criteria; the findings below are owed as K-set folds. The prefixed check, run after: 15/15 indexed files present with exact char counts (534,953 = 534,953); one stray, `00_README.md` (a package file; file 17 §4 said delete it); K-00 unrowed by construction (noted, not a mismatch).
**The K-set folds owed (the research program's rows; the hub moves one field per finding, dated, at the next K-refresh beat — the build is Nick's script):** R-1 K-04:79 "causal, not coincidental" overstates — a Σ_u residual remains (Q3 §3.1). R-2 the bound presented as a bound is `min(1, W/T_d)` under uniform phase; `1 − e^(−W/T_d)` sits below it (Q3 §3.3). R-3 "change-triggered" is a hub-side classification with a loss-scaled error term; K-11's `state_changed` seam names where it is decided (Q3 §3.2). R-4 K-13:89's S3 kill placeholder needs its denominator fixed to state-changing commands (Q3 §3.4). R-5 the researcher skill's pointer "the D5 form (K-09 §2.3)" does not lead to the words — K-08 §0 item 4 does (Q2 item 3). R-6 K-04:128's "one sense" claim is refuted at the bytes — 30 language-law uses, 8 ADR uses, 2 in "D1–D5" (Q2 item 4). R-7 the skill reference's three-question test says "three internal rows"; K-05 carries seven (Q1). R-8 SP-5 is mapped to P-01/P-03 in K-05's cross-reference and appears in neither Internal line (Q1, out-of-fence note). None moves a construct; g3's place in the lattice stands; no research number enters any sentence.

## §5 D-v78-3 — the B-7 ADR, read whole (5,641 B) — the recommendation
`B7: ratify` all five rows as written. D1 (reserve nothing; AMD-37 already reserved the NOT NULL column with the zero-vector genesis), D3 (the activation position recorded in a one-row `chain_metadata`, V006), D4 (startup verification from the activation position, bounded by a verified-through checkpoint; a mismatch is exit 11), D5 (the independent verifier is a bench verb over a copied database) are decisions with no cost until the activation WU. D2 already names `canonical(row)`'s set (event id · type · subject · sequence · payload bytes · the timestamps) and the genesis rule; C-S0-2's column census is its refutable-by, not its prerequisite — a column the census finds that must be chained and is not in the set is `edit D2 <text>` then. Ratifying commits no code (DECIDE, DON'T BUILD; the activation is one WU, post-C-003). `B7-DOCS: adr-dir` — the first file under `design/adr/` on Nick's next docs card (bundled with AMD-100's); the strategic map's planned `0001-adr-adoption.md` decides the number at the bytes. The language-law consequence is already in force (K-set: chain-ready, never hash-chained).

## §6 The words of the day, recorded
RULED: `UPLOAD-2: go` (one stray outstanding) · `LAUNCH: 7a` (after `none`) · `K15: add` · `RESEARCHER-SKILL: hold` · `RESEARCH-SURFACE: split` (D-v78-5: desk charters as Cowork lanes from C-S0-1 on; web-facing scans in the Project; 7a in the Project tonight) · by silence: D-v78-1 `ir40+ir44`, D-v78-2 `verify72h`. The next three charters after 7a, with the earliest date each may run: C-S0-1 (P-01(a): Σ first; a Cowork lane; from Mon 09-28, after 7a's return is filed) · C-S0-11 (P-05/E-2 observed-or-reconstructed; Cowork; from Mon 10-05, after C-S0-1's return) · C-S0-9 (the October currency re-read; the Project, Research ON; on/after Wed 10-07).

## §7 The b2 card OVERTAKEN
Block 3 of the b2 packet was not run before this beat edited the same files; an unrun order is retired, never a ghost: `_scratch/v78/2026-09-21_hivemind_v78-b2_commit-msg.txt` is retired unused; ONE card commits beats 2 and 3 (`_scratch/v78/2026-09-21_hivemind_v78-b2b3_commit-msg.txt`). Census computed from porcelain inside the splice: hivemind 19 = 7 M + 12 A.
