<!--
file: context/audits/2026-09-21_v78-b4_landings-banked_ir40-ir44-charter_audit.md
purpose: The v78 beat-4 audit — the three landings banked at the instrument (core `d2cddb1` = LINK-READ, CI green on Nick's line → the closure counter 12/20; hivemind `70727f5` = beats 2–3; the Project's knowledge set 15/15 with no stray → `LAUNCH: 7a` unlocked); IR-40 + IR-44 chartered through the gate with every mechanism row grepped at `d2cddb1`; the IR-44 row's path corrected; the close planned (b5) with the token-hygiene program measured at the bytes.
audience: the hub · Nick (§0, §3) · the v79 hub
state-type: audit (filed)
status: FILED v78 beat 4 (Mon 2026-09-21 ~14:0x CT; instrument 2026-09-21T19:05:44Z)
-->

# v78 beat 4 — the landings banked; IR-40 + IR-44 chartered; the close planned

## §0 Verdict
Core `d2cddb1` (12 files, +1,179/−39) on `13d439f`, pushed; Nick's line `CI: d2cddb1 green` → **the closure counter 12/20** (`a458a64` … `13d439f` · `d2cddb1`). Hivemind `70727f5` (19 files) on `63baa87`, pushed; porcelain 0 in both at the instrument. The prefixed knowledge check after the stray's deletion: 15/15 indexed files present at exact char counts, `index/disk mismatch: none` → `UPLOAD-2` COMPLETE; `LAUNCH: 7a` unlocked for tonight. IR-40 + IR-44 DISPATCH-READY (`context/instructions/2026-09-21_coder-lane_IR40-IR44_time-range-plan-on-the-index_startup-report-automation-arm_coding-instruction.md`, 14,153 B). The window closes at b5 on Monday's landings; THE THURSDAY PACKET's date holds and its cutter is v79's fresh window (§4).

## §1 IR-40 + IR-44 — the premise rows, each grepped at `d2cddb1`
- `SqliteEventStore.java:220–:226` `SELECT_BY_TIME_RANGE_SQL` (`WHERE global_position > ? AND COALESCE(event_time, ingest_time) >= ? AND … < ? ORDER BY global_position ASC LIMIT ?`); the Javadoc `:216–:219` names the index key; `readByTimeRange` at `:672`; the `LIMIT 1` sibling at `:773–:774`.
- `V001__initial_event_store_schema.sql:61` `idx_events_event_time ON events(COALESCE(event_time, ingest_time))` — the expression matches the predicate; the fix is the plan, not the schema.
- `StandardExplanationService.java:1146` — the class's only `readByTimeRange` call, in `triggeredInWindow` (`:1140–:1155`) over `[t − HINT_LEAD, t + HINT_LAG)` (`:1120–:1121`), `SCAN_BATCH = 500` (`:115`).
- `MeasureReadPathIT.java:316` times q1 = `explainRun(lastRun)`; `:78–:92` — the whole-log window is the frozen-clock artifact, never the default (so F-2's 90.5 ms is the narrow-window case, the run's case); `HOMESYNAPSE_MEASURE2B_ROWS` `:101`.
- `gradle/libs.versions.toml:17` sqlite-jdbc `3.51.3.0`.
- `HomeSynapseCore.java:1638` `private static String recommendationFor(String subsystem)`; called at `:465` into `new StartupFailureReport(phase, subsystem, recommendation)` `:467`; the companion wired at `:677–:678`. **The IR-44 row's path was wrong** (`app/homesynapse-app/.../StartupFailureReport`): the record is `lifecycle/lifecycle/.../StartupFailureReport.java` (2,194 B) and the arm lives in `HomeSynapseCore` — corrected in the register this beat.
- `HomeSynapseCoreStartupFailureTest.java` — 6 tests; T4's home.
Not re-executed: `EXPLAIN QUERY PLAN` itself (the row's premise is F-2's measured plan; §10 of the instruction carries the refutation path).

## §2 The frozen-token rule, applied before it is a law
The instruction's §0 orders the lane to grep `FROZEN` in every MODULE_CONTEXT it touches and `containsExactly` on every log line it changes before writing a byte — the LINK-READ lesson applied one unit early; pm-lessons mints it at the close.

## §3 The acts in Nick's hands (`_scratch/v78/2026-09-21_v78-b4_packet.md`)
Block 1 the IR-40 + IR-44 paste (§14) → `IR40: RETURNED <path> <bytes>` (tonight or tomorrow; the lane's run is its own). Block 2 the hivemind card (9 = 7 M + 2 A) → `HIVE: LANDED <sha>`. Block 3 tonight: `BOX: B2 <time>` · `SHAKE: <B1 W> <B2 W> <settle s>` · the 7a paste (package file 16; Research OFF; its state line re-derived: core `d2cddb1`, hivemind the block-2 sha) → `7A: RETURNED <path> <bytes>` on its filing day.

## §4 The close planned for b5, and the token-hygiene program measured at the bytes
**Why v78 closes tonight at b5:** the plan §7 refuses a packet cut on the tail of a tired session; this window is at b4 of ≤ 8 with the two heaviest cuts of the week ahead (THE THURSDAY PACKET and rehearsal 1's packet, both through THE PRIOR-LEDGER GATE with two prior ledgers' `cut -c1-240` greps); they deserve v79's fresh window Tuesday morning on `R5B-4:`. THE THURSDAY PACKET's date (DISPATCH-READY by Tuesday night) holds; the cutter changes. Refutable-by: Nick's word `V78: continue`.
**Measured (bytes, at the instrument):** the boot read-set is at its budget (the brief 11,894 of 12,288; the snapshot 3,454 of 3,500; the plan's boot sections §21+§22 9,708 + §2 3,406; pm-handoff line 8 2,057 of 3,000). **`coder-handoff.md` is 119,009 B and every Coder lane reads it whole at boot** (the LINK-READ lane did); its line 8 is 4,936 B with no rotation rule. **MODULE_CONTEXT.md:** integration-zigbee 227,994 B · core/persistence 195,645 · core/automation 144,726 · api/rest-api 119,513 · lifecycle 112,671 · core/state-store 95,356 — the coder skill says "read MODULE_CONTEXT.md"; a lane that obeys literally spends ≈ 50K tokens on one file (the LINK-READ lane grepped instead; the IR-40 instruction orders grep-only). The skills: PM `SKILL.md` 18,936 + references 210,969 (read by grep); coder 13,271 + 142,012; frontend 16,534 + 96,601. `context/instructions/` 44 files (802 KB) is never read as a directory (Check 12); `pm-lessons.md` 100,775 B is read by the newest entries only.
**The program, ranked by tokens saved per hour, with its owner and beat:** (1) `coder-handoff.md` → ≤ 12 KB: line 8 capped at 3,000 B with the pm-handoff rotation form; the historical entries rotated verbatim to `archive/coder-handoff-entries-rotated-2026-09-21.md` (bytes asserted); the pointer, the deferred-gate block and the newest three entries kept — the hub, at b5 (a spine-class file the hub writes). (2) MC-ROTATE-1: the six MODULE_CONTEXT files ≥ 95 KB — the per-WU history sections rotated verbatim to a `MODULE_CONTEXT-history.md` beside each, the type inventory / contracts / gotchas / FROZEN tokens kept current, bytes asserted; a hygiene lane on the core tree BETWEEN Java units (the path-domain law: `core/persistence/MODULE_CONTEXT.md` is IR-40's row 5) — v79/v80, after IR-40 lands. (3) W-SKILLS-10, the skills fold: THE PREMISE GATE + THE MOVED-STEP GATE + THE SURVEY lesson + the two mints of today (a frozen token is a pin → the P2 survey's row #32; a count is read from an uncapped listing) + the coder skill's read rule for MODULE_CONTEXT ("whole only under 40 KB; else by grep; the history file never") + `nexsys-researcher`'s placement word — an idle beat of v79 or v80; the skills card. (4) The plan of record's tail: §23 STATE OF THE SYSTEM re-cut at the v78 close so the boot reads ONE section (≤ 6 KB) — the hub, b5. (5) The v79 dispatch text ≤ 6 KB — the hub, b5. (6) The K-set refresh (R-1..R-8; K-13 v0.2 after `B7:`; K-05 P-06's instrument) — Nick's build + upload + the three checks; a v79 beat after THE THURSDAY PACKET. (7) `_scratch/v78/` (36 files, 640 KB) stays — never tokenized unless read.

## §5 Order
hivemind 9 = 7 M + 2 A (computed from porcelain inside the splice): the spine (the v78 b2 chain segment rotated as rotation 153), the snapshot, the brief, the coder-handoff (line 8; the pointer → dispatched), the register (IR-44's path; the counter), the DR (D-v78-1 EXECUTED as chartered), the instruction, this audit.
