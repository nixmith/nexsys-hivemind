<!--
file: context/audits/2026-09-23_v78-b5_close_ir40-ir44-intake_coder-handoff-rotation_audit.md
purpose: THE v78 CLOSE (beat 5) — IR-40 + IR-44 intaken two-layer (ACCEPT; the core card); coder-handoff.md rotated to a lane-sized record; the two pm-lessons mints and the coder-lessons entry; the plan §23 STATE OF THE SYSTEM; the v79 dispatch text; v78 CLOSED at five with THE THURSDAY PACKET handed to v79's fresh window (Wednesday) for Thursday.
audience: the hub · Nick (§0, §3) · the v79 hub (boots on the plan §23 and the v79 text)
state-type: audit (filed)
status: FILED v78 beat 5 (Wed 2026-09-23 ~00:2x CT; instrument 2026-09-23T05:20:53Z)
-->

# v78 beat 5 — THE CLOSE: IR-40 + IR-44 ACCEPT; the record made lane-sized

## §0 Verdict
**IR-40 + IR-44 — ACCEPT.** The return (`context/audits/2026-09-21_IR40-IR44_return.md`, 11,998 B, §0 first) delivers both rows as chartered and corrected: `SELECT_BY_TIME_RANGE_SQL` carries `INDEXED BY idx_events_event_time` (`SqliteEventStore.java:235–:237`, package-private for the plan test, the mechanism in its Javadoc `:221`); the plan test (3 tests, `2026-09-21T23:47:26`) sweeps the bound `maxCount` — the finding worth the unit: sqlite-jdbc 3.51.3 plans a bound `LIMIT ?` by its VALUE (the index at ≤ 400, the rowid walk at ≥ 500, independent of row count), and production's `SCAN_BATCH = 500` sat one step past the flip, so F-2's premise held exactly and `INDEXED BY` makes the plan invariant; the `automation` arm at `HomeSynapseCore.java:1648–:1652` names the companion, the two operator acts and the consequence (`automation.identity_loaded first_boot=true`); T10 pins it. `check` green in-lane (persistence 416 · lifecycle 95 · app 30). The measurement, read not gated: q1 median 1.204 / 1.007 / 1.988 ms at 10k / 100k / 243k (the IT's 10-minute seeding cap, as F-2's record run) against the walk's 1.769 / 10.633 / 42.267 — flat in N; the pre-registration (≤ 3× the 10k figure) HELD at 243k; the true-500k point stays F-2's supplementary 90.5 ms, unmeasured here (the cap; a later run with `SEED_CAP` raised is owed, not blocking). The core card: 6 = 5 M + 1 A → `CORE: IR40 <sha>` → `CI: <sha> green` → 13/20.
**THE CLOSE.** v78 CLOSED at five. Its deliverable, THE THURSDAY PACKET DISPATCH-READY by Tuesday night, was NOT met by this window: Tuesday passed with no hub beat (the lane ran; Nick's day ran); it is Wednesday 00:xx CT and Thursday is tomorrow. The packet is v79's ONE deliverable, cut Wednesday in a fresh window — the plan §7 refuses it on this window's tail. Monday's landings stand: 12/20 → 13/20 at CI, LINK-READ and IR-40 delivered in one day, the research layer placed, PELTON-READY armed (the opinion arrived Tuesday — Nick reports it "very good"; the letter and the class word open v79), the record made cheaper for every lane.

## §1 IR-40 + IR-44 — layer 2 (the hub's re-execution at the bytes)
Porcelain in core = the six files the return lists (5 M + 1 `??`); HEAD `d2cddb1`; nothing staged. `INDEXED BY idx_events_event_time` at `:237`; `SELECT_BY_TIME_RANGE_SQL` package-private at `:235`. The plan test file exists with three `@Test`s and no wall-clock call; its XML: 3 / 0 / 0 / 0 at 23:47:26Z. T10 in `HomeSynapseCoreStartupFailureTest` (`COMPANION_FILE = "automations.ids.yaml"` `:73`; the pinned text `:88`). The arm at `:1648–:1652`. `HINT_LEAD = 1 s`, `HINT_LAG = 60 s` (`StandardExplanationService.java:128–:129`) — the window is 61 s, so the sort after the index range is a few rows. Not re-executed: `check` (the lane's log; the lifecycle results directory on disk now holds only the measurement run's XML — the check run's XML was overwritten by the later `--tests` run; CI is the gate of record); the measurement (its XML at `_scratch/v78/2026-09-21_IR40_MEASURE-2b_readpath_junit.xml`, read as the return quotes it).
**The deviations, ruled:** all [INFO], accepted — the visibility; T1's sweep shape (better than the single EXPLAIN the charter named: a single small binding is green at HEAD, which the lane found by probing before it wrote T1); T2's second act; the arm's wording (Register C; kept); the instrument slip (disclosed, the original run's XML the record). **The findings → the register:** IR-48 (F-A: the other `LIMIT ?` reads — `SELECT_FROM_SQL`, `SELECT_BY_SUBJECT_SQL`, `SELECT_BY_TYPE_SQL` — have matching indexes but unpinned plans; one plan test per query, binding the caller's constants), IR-49 (F-C + F-D: the `LIMIT 1` sibling's plan unpinned; `EventStore.java:124–:125` names REST/retention callers that do not exist — the only caller is `explainRun`'s hint), IR-50 (F-B: no product backup-layout convention in the docs; the arm cites the bench card's `~/hs-backup/<stamp>/config/`; the string follows a layout when one is formalized). The lane's lesson → coder-lessons (§4).

## §2 coder-handoff.md rotated (the token win for every Coder lane)
Before: 119,426 B, 214 lines, line 8 five segments (5,409 B); every Coder lane read it whole at boot (the LINK-READ and IR-40 lanes did). After: the header, the CURRENCY STAMP, the ENERGY-READ block with the NEXT WU pointer and the LINK-READ and IR-40 entries, and one pointer paragraph; line 8 the newest three segments + the archive pointer (≤ 3,000 B). The entries older than ENERGY-READ (lines 26–214) and the four older segments went VERBATIM to `context/handoff/archive/coder-handoff-entries-rotated-2026-09-23.md` (§0 the segments; §1 the entries). The invariant asserted inside the splice: every rotated byte string is contained in the archive; the kept file's size is printed in the beat block. The rotation rule for this file from here: ≤ 4 entries live; line 8 ≤ 3,000 B; the rest rotated at the beat that breaches.

## §3 The acts (Nick's hands) — `_scratch/v78/2026-09-23_v78-b5_packet.md`
Block 1 the core card (IR-40 + IR-44, 6 = 5 M + 1 A; `_scratch/v78/2026-09-23_core_IR40-IR44_commit-msg.txt`) → `CORE: IR40 <sha>`, then `CI: <sha> green` — the counter's thirteenth. Block 2 the hivemind card (15 = 11 M + 4 A) → `HIVE: LANDED <sha>` — v78 CLOSES on it. Block 3 Wednesday morning: the v79 paste (`context/handoff/2026-09-23_v79_dispatch-text.md`, the fenced block whole, a FRESH conversation with ClaudeFolder connected) with its first message: `R5B-4:` + `R5B-5:` (the Tue and Wed digest lines) · `ERIK: RETURNED <path> <bytes>` + the class word off the PELTON-READY card (the letter saved verbatim first) · `CORE: IR40 <sha>` + `CI:` · `SHAKE:` / `BOX: B2` / `7A:` as they stand.

## §4 The lessons minted
pm-lessons: **A FROZEN LOG TOKEN IS A PIN** (the P2 survey greps `FROZEN` in every MODULE_CONTEXT a unit touches and `containsExactly` on every log line it changes; the LINK-READ fork of 2026-09-21) · **A COUNT IS READ FROM AN UNCAPPED LISTING OR NOT STATED** (eight of eleven call sites read off a `head -12`; the same morning). coder-lessons: **A PLAN TEST MUST BIND THE PRODUCTION CALLER'S VALUES** (the IR-40 return §0/§2: SQLite plans a bound `LIMIT ?` by its value; read the plan with the caller's constants, sweep the axis, pin with `INDEXED BY`).

## §5 The plan §23 and the v79 text
§23 is the one-screen STATE OF THE SYSTEM v79 boots on (it replaces §21 + §22 as the boot read; those stand by pointer). The v79 text (`context/handoff/2026-09-23_v79_dispatch-text.md`, ≤ 6 KB) names the deliverable, the first message, Wednesday's order (THE THURSDAY PACKET through THE PRIOR-LEDGER GATE; rehearsal 1's packet; VERIFY-72H + the bench lane; the PELTON ruling on the letter; IR-40's landing banked; the strategy pass, VERDICT-VOCAB-1, `FOP: apply`, the docs card as the beats allow) and Thursday (THE THURSDAY ORDER, v79 or v80).

## §6 Order
core 6 = 5 M + 1 A (the lane's tree; Nick's card). hivemind 15 = 11 M + 4 A (computed from porcelain inside the splice).
