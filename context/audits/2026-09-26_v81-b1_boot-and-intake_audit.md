<!--
file: context/audits/2026-09-26_v81-b1_boot-and-intake_audit.md
purpose: THE v81 BEAT-1 AUDIT — the boot at the instrument (§1 of the stable prompt, the bytes counted, the budget overage disclosed), the preflight (12 lines), the intake of Nick's first message at the bytes, the dispatch text's identity with the paste, the drift found at porcelain (HIVE-CLEAN-2), the bench selftest run and read RED, the scenario's prompts read whole, what was not re-executed.
audience: the v81 hub (its own record) · Nick (the verdict line) · the v82 hub (the trim of D-v81-6)
state-type: audit (beat 1, boot and intake)
status: FILED — v81 beat 1 (Sat 2026-09-26 ~08:5x CT; instrument 2026-09-26T13:58:41Z).
-->

# v81 beat 1 — the boot and the intake

## §0 Card
**Verdict.** The boot PASS (12/12) at 2026-09-26T13:40Z; the five HEADs = the record (every push count 0; no lock); the paste = the dispatch file's fenced block at the bytes; the intake CONSISTENT on three lines and OPEN on one (`SAT-NIGHTLY:` pasted as its placeholder, then read at 08:50 — one premise of the v80 record corrected on it: `re-seen` is not a membership instrument, D-v81-8); ONE drift at porcelain (the HIVE-CLEAN-2 charter, untracked and unrecorded — D-v81-4); ONE new red found by the hub's own re-execution (the bench selftest 1/36 at `df4a2d7` — D-v81-5); the boot read-set 48.8 KB against ≤ 45 KB (D-v81-6). **The window's ONE deliverable:** rehearsal 1's packet DISPATCH-READY on the metered fleet (D-v81-1).

## §1 The boot at the instrument (§1 of the stable prompt; the bytes)
`date -u` → `Sat Sep 26 13:36:39 UTC 2026` (08:36 CT; CT = UTC−5 re-derived once). The read-set, each by range, bytes as printed: (a) the stable prompt whole 13,017 · (b) `pm-handoff.md` :8 1,847 + the newest beat block 2,240 · (c) `PROJECT_SNAPSHOT.md` whole 3,405 · (d) the brief whole 11,200 · (e) the v80 DR §3 (:34–:45) + §3b–§3d (:78–:95) 12,826 — §1b and §2b (Nick's Friday words; the facts) skipped by range · (f) the plan §27 (:202–:207) 2,137 · (g) `pm-lessons.md` :370–:374 2,253. **Total 48,925 B — 3.9 KB over the ≤ 45 KB budget.** The overage is the DR read: the v81 dispatch text names "§3–§3d" (12.8 KB) where the stable prompt's (e) reads "the decisions section only"; §3's twelve rows are already carried in the dispatch text's WORDS GIVEN line. The trim (D-v81-6): v82's text reads §3d + the Carried rows of the newest DR. The five HEADs in ONE call at 13:40Z: core `d22a8a4` · hivemind `3fa0683` (porcelain 1: `?? context/instructions/2026-09-26_HIVE-CLEAN-2_hygiene-lane_charter.md`) · skills `180375f` · bench `df4a2d7` · docs `7221ddc`; `rev-list --count origin/main..HEAD` 0 in all five; no `.git/*.lock`; every repo on `main`.

## §2 The preflight, one line per check (run from the reference's blocks; the instruments named)
C1 PASS (both spines' `last-verified:` name v80 beat 4) · C2 PASS (the plan of record resolves at `context/planning/2026-09-15_v75_PROGRAM-PLAN_…md`; no `*plan-of-record.md` named by either spine) · C3 PASS (core `log -1` = `d22a8a4` = the snapshot) · C4 PASS (`phase-3-milestone-backlog.md` bannered STALE BASELINE — a historical record, D-v79-10) · C5 PASS (Open Risks at :76; 42 entries; newest date in the section 2026-09-23) · C6 PASS-with-note (`coder-handoff.md` :22 names LINK-READ-2 = IR-45 or MC-ROTATE-1 "the hub names it"; the v80 DR §3d puts IR-18 FIRST — the pointer is re-cut when IR-18's instruction is chartered Sunday; a stale candidate list, not a contradiction) · C7 PASS (21 `MODULE_CONTEXT.md` tracked in core; 0 empty templates) · C8 PASS (`cross-agent-notes.md` a RETIRED channel, 1,024 B) · **C9 PASS — 28/28 identical at the bytes** (the three SOURCE trees on the device vs the session's synced copies, per-file md5; the two sorted lists' md5 `efbc8d72aca77c6a4cafd3fe7a0f21c7` on both sides) · C10 PASS (78 cited basenames in `strategic-context-map.md`; 73 resolve in the five repos' `ls-files`; the 5 non-resolving are the RETIRED template patterns `YYYY-MM-DD_topic.md` · `YYYY-MM_month.md` · `YYYY-WNN_…md` · `_orchestrator_session_prompt.md` · `_session_prompt.md` — historical rows) · C11 PASS (the counter's 13 shas resolve as commits in core) · C12 PASS (0 · 0 · 0; the exclusion list re-derived: today's prefix `2026-09-26_` excludes the HIVE-CLEAN-2 charter; the Half-2 card is PARTIAL; the one LIVE orchestrator prompt is v67's).

## §3 The intake at the bytes (Nick's lines vs the instrument)
- `HIVE: LANDED 3fa0683` — CONSISTENT (`log -1 --oneline` = `3fa0683 hivemind: v80 beats 2–4 …`; ahead 0).
- `SAT-NIGHTLY: <the nightly line>` — the placeholder, verbatim at 08:36; read at 08:50 CT by the one act (`ssh pi '~/bench.sh digest 1'`; `tools/bench.sh` `digest)` tails `~/hs-bench/digests/nightly.log`): `7/9 · FAIL command-confirm-s31 · … · 1 SKIP(hue-online) · fleet: 9/9 · re-seen 6 · bench-hero RESTORED ✓ · ON-latency n/a(FAIL)`. Layer 2 on `re-seen`: `nightly_digest.py` :150–:172 read — `re_seen = len(now & prior)` over two nights' captured id lists; the prior night held 6; the three plugs joined after it; 6 is the arithmetic, not a membership read. The v80 record's premise ("`re-seen <n>` of 9 is the Gen4-membership instrument") was an unmeasured hop (arc 1) — corrected in D-v81-8; the pm-lesson's sentence is amended at the next lessons write, the plan §27 by this beat's pointer. The S31 FAIL is by construction (the S31 unplugged, per the RIG line); the word `S31:` asked.
- `RIG:` — CONSISTENT with the plan §27 and the brief's rig row, plus three things the record did not hold: the cord is ONE 1-ft single (the record's re-cut assumed "one cord" — confirmed); B2 (the MECHEER) is not named; the plugs' outlets are not named. Asked as words (`B2:` · `PLUGS:` · `BULB:`), THE SIMPLER WAY (2) — answered 08:54 CT: B2 on the desk, unplugged; the plugs in separate wall sockets near the Pi, movable; two clamp lamps, each a 40 W bulb (one is the load; the pair needs the absent strip).
- `HOURS:` — CONSISTENT with the window as cut (§27); the rename clause ruled D-v81-3.
- The dispatch text = the paste: the fenced block of `context/handoff/2026-09-26_v81_dispatch-text.md` (:10–:26) and the pasted attachment normalise to the same md5 `0d3d272e944d291c15940b4e9ea12e19` (trailing whitespace and blank lines stripped; 8,017 B on disk vs 8,030 B pasted — line endings only). Filed verbatim by that identity; Nick's own lines verbatim in the DR §1.
- **The scenario's prompts read WHOLE before any card** (`nexsys-bench/scenarios/metering-known-load.yaml` :85–:241, 14,205 B): CHAR-BEFORE `confirm: enter` with the three-meter chain and ~20 readings after ≥ 5 min; per plug seven typed entries (TARE by difference · OFFSET · volts ×2 · REP ×3, each REP behind its own LOAD STEP, "toggle on the dashboard … or unplug the LAMP for ≥ 15 s"); CHAR-AFTER `type: number` (the count of readings); 22 entries, the order pinned by BM1b T4. The 80 W wording: the header comment :15–:16, the CHAR-BEFORE note ("the two 40 W lamps"), the REP goals' hard-coded "band 3.03 %" / "4.03 %" (the constants now read 3.65 / 3.53). The TR3 and G4-2 REP prompts are the G4-1 text with the plug's name substituted (read by pattern; the goals and notes read in full).

## §4 The drift at porcelain — HIVE-CLEAN-2 (D-v81-4)
`?? context/instructions/2026-09-26_HIVE-CLEAN-2_hygiene-lane_charter.md` — 13,183 B, mtime 2026-09-26T03:54:37Z (the close beat's instrument stamp 03:40:57Z; the charter came fourteen minutes after it and after the b2b3b4 card's census of 53). Its purpose line quotes Nick's ask; its status DISPATCH-READY; its return path `context/audits/2026-09-26_HIVE-CLEAN-2_return.md`; its §0 forbids the spine, the newest DR, the plan, the stable prompt and the dispatch text. Not in `pm-handoff.md`, the DR, the brief or the v81 text (0 hits). Adjudicated: a living card, un-run; tracked at this beat's card so the tree carries no unrecorded file; dispatched at the v81 close.

## §5 The bench selftest, run by the hub (D-v81-5; IR-57)
`cd nexsys-bench/tools/runner && python3 -B test_engine.py` on the device at `df4a2d7`: `selftest: 36 check(s), 1 failure(s)` — `[X] BM1b T3 — the REAL metering-known-load.yaml walked LIVE-PATH with the charter's inputs …`, `AssertionError: ['WITHIN', 'WITHIN', 'WITHIN', 'OUTSIDE', 'WITHIN', 'WITHIN', 'OUTSIDE', 'WITHIN', 'WITHIN']` — index 3 (TR3 rep 1) OUTSIDE where the test expects WITHIN: the scripted inputs are 80 W figures (`80.3 − 0.0 vs 81.0 − 0.5`), the bands are read from the real `constants.yaml` (`band_pct: 3.65`, `band_pct_tr3: 3.53` since T4b), and the TR3's 3.755 % after the tare clears 4.03 but not 3.53. T4b's card (v80 b4) did not run the selftest; the bench has no CI workflow (`.github/workflows/` absent) — the runner's tests are hand-run, so nothing else would have caught it. The v80 close audit and DR carry no selftest line (0 hits for `selftest` / `test_engine` / `36 check`).

## §6 Not re-executed (disclosed)
The nightly's bundle `command-confirm-s31-20260926T083122Z` (its body; the FAIL is by construction) · the three plugs' availability (card 1) · the tmux session's state on the Pi · Nick's meter reads · the HIVE-CLEAN-2 charter's §1 census rows (the lane re-measures at dispatch) · the TR3 and G4-2 REP prompt bodies beyond the G4-1 pattern (§3).
