<!--
file: context/coding-instructions/M6.1_Config_Pipeline.md
purpose: Coding instruction — M6.1 config pipeline (YAML load + JSON-Schema validation + ConfigurationAccess/ConfigurationService load path + AMD-66/67/70/71 contracts). The non-crypto first piece of M6.
audience: Coder (Claude Code), PM, Nick
status: READY TO ISSUE (AMD-66/67/70/71 RATIFIED 2026-06-09) — the ⛔ gate is LIFTED; not yet issued to the Coder. Not crypto-gated.
baseline: homesynapse-core HEAD `6c6dd33` (2026-06-08) — re-verify at issue; the config module surface is source-verified at this commit.
amd-number-note: PINNED 2026-06-09 — AMD-66, AMD-67, AMD-70, AMD-71 are RATIFIED (2026-06-09, DOCS review RATIFY-WITH-EDITS folded at docs `aedff55`; invariants §37/§38/§40/§41); AMD-68 RATIFIED same block (M6.2 scope); AMD-69 DEFERRED (Tier-2/OQ-15-3). These are the final numbers — cite them directly.
-->

# Coding Task: M6.1 — Configuration Pipeline (YAML load → validate → access)

**Subsystem:** Configuration (`com.homesynapse.config`, Doc 06 — LOCKED)
**Design Doc:** Doc 06 Configuration System (LOCKED) + AMD-66 (listener), AMD-67 (schema major/minor), AMD-70 (config.validation_completed), AMD-71 (directory layout) — **all four must be RATIFIED before this instruction issues (⛔ gate).**
**Phase:** 3-Implementation
**Task Brief Reference:** W24 M6 entry-gate / `context/planning/2026-06-08_M6-charter.md` (piece M6.1)

## ✅ Gate LIFTED (2026-06-09)

**AMD-66, AMD-67, AMD-70, AMD-71 are RATIFIED (2026-06-09 block ratification; DOCS review return: `context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md`; edits folded at docs `aedff55`).** The contracts this milestone implements (the listener, the `(major,minor)` schema model, the config observability events, the directory layout) are frozen — DP-3 below is the ratified [AMD-71-A]/E71-2 resolution. M6.1 is **not** crypto-gated — it does not touch Doc 15 / the key manager / at-rest encryption (those are M6.2/M6.3). AMD-68 (`SecretStore.setAll`, RATIFIED — M6.2 scope) and AMD-69 (DEFERRED, Tier-2/OQ-15-3) are **out of M6.1 scope**. **Issue whole (Fork-2): the split decision is the Coder's Phase-2 file-touch-inventory checkpoint — if the unit sprawls, split at the M6.1a load/validate / M6.1b access/events seam BEFORE writing code; do not pre-split at issue.**

## What This Implements

M6.1 is the **non-crypto config pipeline** — the first of M6's four first-class pieces. It turns the on-disk YAML configuration into the validated in-memory `ConfigModel` and serves it to subsystems. Concretely: parse YAML 1.2 (safe-by-default), compose + validate against JSON Schema, build the `ConfigModel`, and expose it through `ConfigurationService` (load/read) and the integration-scoped `ConfigurationAccess`. It also lands the M6-config-block contracts that belong to the load path: the `(major,minor)` schema model (AMD-67), the hybrid directory layout + path-traversal guard (AMD-71), the `ConfigurationChangeListener` interface + registration (AMD-66 — *defined* here, *exercised under swap* in M6.4), and the `config.validation_completed` observability event (AMD-70 — the load/validate half). The **reload** path (atomic swap, `config.section_reloaded`, listener invocation) is **M6.4**.

## Files to Read Before Starting

| File | Why |
|---|---|
| `config/configuration/MODULE_CONTEXT.md` | Complete 22-type inventory, cross-module contracts, gotchas (esp. the `ConfigurationValidationException`-in-event-model and `requires transitive` ones) |
| `config/configuration/src/main/java/module-info.java` | Verbatim JPMS module name + graph (embedded below; re-verify at issue) |
| `config/configuration/src/main/java/com/homesynapse/config/*.java` | The frozen interfaces/records this milestone implements against (`ConfigurationService`, `ConfigurationAccess`, `ConfigModel`, `ConfigMigrator`, `ConfigValidator`, `SchemaRegistry`, `ConfigSection`, `ReloadClassification`, `ConfigIssue`, `Severity`) |
| `core/event-model/MODULE_CONTEXT.md` + `core/event-model/src/main/java/module-info.java` | `EventPublisher`, the event-type manifest sites (AMD-70), `ConfigurationValidationException` home |
| `platform/platform-api/MODULE_CONTEXT.md` + its `module-info.java` | `PlatformPaths.configDir()` — but see DP-3: M6.1 takes the resolved `Path`, it does NOT add a config→platform edge |
| `homesynapse-core-docs/design/amendments/AMD-66/67/70/71_*.md` | The frozen contracts — the spec detail lives there; this instruction does not duplicate it |
| `homesynapse-core-docs/design/06-configuration-system.md` §3.1/§3.3/§3.6/§3.7/§8 | The loading/validation/migration pipeline this implements |

## Verbatim `module-info.java` (`com.homesynapse.config`, at `6c6dd33`)

```java
module com.homesynapse.config {
    requires transitive com.homesynapse.event;

    exports com.homesynapse.config;
}
```

**Proposed diff: NONE for M6.1.** The pipeline implementation is package-private inside `com.homesynapse.config`; it consumes `EventPublisher` (already `requires transitive com.homesynapse.event`), the `snakeyaml-engine` and `json-schema-validator` libraries (already on the `implementation` classpath — verified in `build.gradle.kts`), and the injected config-dir `Path` (DP-3 — no platform edge). **If the Coder finds a genuine need for a new `requires`, STOP and report** (it would contradict the `[REVIEW-FLAG AMD-71-A]` resolution; the no-new-edge property is load-bearing for M6.2's E2 bridge).

## Files to Create or Modify

| Action | File Path | Description |
|---|---|---|
| MODIFY | `config/.../ConfigModel.java` | AMD-67: `schemaVersion:int` → `configSchemaMajor:int` + `configSchemaMinor:int` (5→6 components, guards `major>=1`/`minor>=0`) |
| MODIFY | `config/.../ConfigMigrator.java` | AMD-67: 3→5 methods (`fromMajor/fromMinor/toMajor/toMinor` + `migrate`) |
| MODIFY | `config/.../MigrationPreview.java`, `MigrationResult.java` | AMD-67: carry `(major,minor)` (verify exact current shape at impl) |
| CREATE | `config/.../ConfigurationChangeListener.java` | AMD-66: plain non-generic interface (`sectionPath()`, `onSectionChanged(prev,cand)→ReloadClassification`) |
| CREATE | `config/.../YamlLoader.java` (package-private) | snakeyaml-engine safe-by-default `LoadSettings`; one-level `!include` + path-traversal guard (AMD-71) |
| CREATE | `config/.../JsonSchemaCompositeValidator.java` (package-private) | `ConfigValidator` impl over networknt 1.5.6; allErrors; maps to `ConfigIssue` |
| CREATE | `config/.../StandardSchemaRegistry.java` (package-private) | `SchemaRegistry` impl; compose-after-merge; **JSON-text-`String` params only** (no library types leak) |
| CREATE | `config/.../StandardConfigurationService.java` (package-private) | `ConfigurationService` impl — `load()`, `getCurrentModel()`, `getSection()`; listener registration; fires `config.validation_completed` |
| CREATE | `config/.../ScopedConfigurationAccess.java` (package-private) | `ConfigurationAccess` impl — integration-scoped read over the active `ConfigModel` |
| CREATE | event records for `config.validation_completed` | AMD-70: in `com.homesynapse.event` (flat package) — see P2 survey for the manifest sites |
| CREATE | tests | per Test Requirements |

## Settled Decisions (do not re-open)

- **DP-1 (schema version shape):** `(configSchemaMajor, configSchemaMinor)` per AMD-67. The on-disk YAML top-level key is `schema_version: { major: N, minor: M }` (object form — explicit, no string-parse ambiguity). Migrator triggers on **major** mismatch only (AMD-67-INV-02).
- **DP-2 (validation error model):** allErrors mode (collect all `ConfigIssue`s in one pass — the frozen `ConfigValidator` contract). On `load()`: `Severity.ERROR` → revert that key to its schema default (degraded-but-functional start); `Severity.FATAL` → `ConfigurationLoadException` (Doc 06 §3.6, source-verified `ConfigurationService` Javadoc).
- **DP-3 (config-dir resolution — the `[AMD-71-A]` resolution):** `StandardConfigurationService` takes the **resolved config-dir `Path` as a constructor parameter**, supplied by the composition root (`com.homesynapse.app`, which already `requires` both platform + config) from `PlatformPaths.configDir()`. **M6.1 adds NO `requires com.homesynapse.platform`** — this keeps config's module-info unchanged and preserves the no-new-edge property the M6.2 E2 bridge depends on. (Alternative `config→platform` edge is REJECTED.)
- **DP-4 (listener registration):** constructor-injected `Map<String, ConfigurationChangeListener>` keyed by `sectionPath()`, no `ServiceLoader` (DEC-M3-16); duplicate section path → construction-time `IllegalArgumentException` (AMD-66 §2.4).
- **DP-5 (Clock):** the load path stamps `ConfigModel.loadedAt`/`fileModifiedAt` from an **injected `Clock`** (NOT `Instant.now()`) — required for the §4c arch rule and deterministic tests.

## STOP-on-Mismatch Gates

Before writing code, read and confirm. If any diverged from the expected state, STOP and report.

| File | Expected State | Check |
|---|---|---|
| `config/.../ConfigModel.java` | record, 5 components, `int schemaVersion` first | the AMD-67 edit target; confirm before mutating |
| `config/.../ConfigMigrator.java` | interface, 3 methods (`fromVersion`/`toVersion`/`migrate(Map)`) | the AMD-67 edit target |
| `config/.../SecretStore.java` | interface, 4 methods | **must be UNCHANGED by M6.1** (`setAll`/`credentialsFor` are M6.2/AMD-68 — out of scope) |
| `config/.../module-info.java` | `requires transitive com.homesynapse.event;` only | confirm the no-new-edge property before/after |
| `config/.../ConfigValidator.java` / `SchemaRegistry.java` | `String`-param contracts (no `JsonSchema` library type) | implement behind these; never leak library types |
| `core/event-model/.../EventTypes.java` + manifest sites | the P2 survey set | the AMD-70 registration targets |

## P2 Consumer/Pin (Fan-Out) Survey — AMD-70 event types (mandatory; enumerate before issue)

Adding `config.validation_completed` (and, in M6.4, `config.section_reloaded`) touches the event-type manifest set. Run a repo-wide grep and modify EVERY site or the build breaks (the M4.C lesson — an unregistered event type fails `encode` and trips count-pins):

- `EventTypes` — add the string constant(s) + any count-pin.
- the core production event-class manifest (`CORE_PRODUCTION_EVENT_CLASSES` / `AllEventClasses.ALL_EVENTS`) — add the record(s).
- `EventCategoryMapping.TABLE` (+ `EventCategoryMappingTest`) — map to `[SYSTEM]` (observability; PM-confirmable at review).
- `EventTypeRegistry` (+ test), `JacksonWarmup` (+ test) — counts.
- the production composition root + integration-test harness aggregation **if** they pin counts (the M4.C straggler sites — `HomeSynapseCore`, `IntegrationTestHarness`).

Emit the resolved list as a `## P2 Consumer/Pin Survey` block in your Completion Report. For M6.1 only `config.validation_completed` lands (the single config event of the load path); `config.section_reloaded` is M6.4.

## Test Requirements (M6.1 scope)

Test classes (inject `Clock`): `ConfigModelTest` (major/minor guards), `ConfigMigratorChainTest` (major-mismatch migrates; minor-only does not), `YamlLoaderTest` (safe-load; rejects unsafe tags), `ConfigLayoutTest` (root+includes merge; nested-include FATAL; path-traversal FATAL with path named; compose-after-merge), `JsonSchemaCompositeValidatorTest` (allErrors; ERROR→default, FATAL→exception), `ScopedConfigurationAccessTest` (integration-scoped; empty section for zero-config — INV-CE-02), `ConfigurationChangeListenerTest` (registration; duplicate-path rejected; the interface contract), `StandardConfigurationServiceTest` (`load()` happy path; `config.validation_completed` fired once with `(major,minor,issueCount,severityCounts)`), `ConfigValidationCompletedEventTest` (round-trips through the event codec). Cover the AMD §5 test tables for AMD-66/67/70/71 (load-path subset).

## What to Watch Out For

- **`Clock` injection (§4c — `com.homesynapse.config` is NOT whitelisted).** Do NOT use `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, or `System.currentTimeMillis()` in this module's main OR test code — the `NO_DIRECT_TIME_ACCESS` ArchUnit rule scans non-whitelisted classes and fails `./gradlew check`. Inject `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` via constructor/`@BeforeEach`. (M2.4/M2.5 tripped this; `coder-lessons.md` 2026-04-10.)
- **`ConfigurationValidationException` is in `com.homesynapse.event`, NOT config (MODULE_CONTEXT gotcha).** Do not duplicate it; reference via import. `ConfigurationLoadException`/`ConfigurationReloadException` ARE in config.
- **`ConfigChange` vs `MigrationChange` are different types** (the Doc-06 name collision; resolved by rename). Don't confuse the reload-diff record with the migration record.
- **`SchemaRegistry`/`ConfigValidator` take JSON-text `String`s, never library types** (JPMS hygiene — the networknt `JsonSchema` type must never appear in the public API). Parse JSON strings internally.
- **`requires transitive com.homesynapse.event` is correct, not a bug** (event-model types — `HomeSynapseException`, `ConfigurationValidationException` — leak through the public API; the `requires transitive`↔`api` lockstep is already satisfied by `api(project(":core:event-model"))` in `build.gradle.kts`). Do not "fix" it to plain `requires`.
- **Path-traversal guard must be canonicalization-based** (`Path.toRealPath()` containment under `configDir()/integrations/`), NOT string-prefix matching — symlink-safe (AMD-71 §2.3). This is the security-load-bearing detail.
- **`!include` is one level deep** — invoke the included-file parser **without** the `!include` tag handler registered, so a nested include is an unknown-tag FATAL error (AMD-71 §2.2).
- **Derived/observability event time:** `config.validation_completed` uses null or inherited `eventTime`, never `Instant.now()` (the standing event-time rule). `ingestTime` is the publisher's concern.
- **Do NOT touch `SecretStore`, the key manager, or any at-rest/crypto path** — that is M6.2/M6.3. M6.1 is non-crypto.

## Dependencies and Integration Points

- Consumes: `EventPublisher` (`com.homesynapse.event`) for `config.validation_completed`; the injected config-dir `Path` (composition root); `snakeyaml-engine` + `json-schema-validator` (implementation classpath).
- Produces: the `ConfigModel` read surface (`ConfigurationService`, `ConfigurationAccess`) every subsystem consumes; the `config.validation_completed` event.
- Cross-module: none new at the JPMS level (DP-3). The composition root (`com.homesynapse.app`) wires the config-dir `Path` and the listener map.

## Build Discipline

You produce files. You do NOT run `./gradlew`. Nick runs the build gate after review. **Deferred build gate: flag it in `coder-handoff.md` + the WUCP Phase 1 checklist.** If the sandbox can run Gradle, run a targeted `./gradlew :config:configuration:compileJava` (and `:compileTestJava`) on `-Werror`-sensitive surfaces before handoff (P5 shift-left) — but the full `check` stays the deferred gate.

## Coder Pushback Welcome

If any contract here is impractical, contradicts a MODULE_CONTEXT gotcha, or could be done better at the same behavioral contract — raise it (escalation format in your skill doc). In particular: if you find the load + validate + access + 4-amendment surface is too large for one clean compile-and-commit unit, **flag it for a split** (P1 — M6.1a load/validate, M6.1b access/events) rather than letting it sprawl.

## Out of Scope

- The **reload path** — atomic `ConfigModel` swap, `config.section_reloaded`, listener *invocation* under swap → **M6.4**.
- `SecretStore.setAll` / `SecureCredentialBundle` / credentials → **M6.2 / AMD-68**.
- The key manager, `PayloadCipher`, at-rest payload encryption, `scope_keys` → **M6.2 / M6.3** (crypto-gated).
- Argon2id / passphrase root / BouncyCastle → **AMD-69 (deferred, Tier-2)**.
- Typed subsystem config records (`EventBusConfig`, etc.) — Phase-3-later (MODULE_CONTEXT key decision #1).

## Success Criterion

M6.1 is DONE when: (1) all listed files exist with the specified content; (2) `ConfigModel` is 6-component `(major,minor,…)` and `ConfigMigrator` is 5-method; (3) `config/configuration` `module-info.java` is **unchanged** (no new `requires`); (4) the load pipeline loads + validates + serves a zero-config and a populated config; (5) `config.validation_completed` is registered across the full P2 manifest set and round-trips; (6) the AMD-66/67/70/71 load-path §5 tests are GREEN; (7) MODULE_CONTEXT.md updated (type count, new types public/package-private, gotchas, Phase-3 notes); (8) WUCP Phase 1 checklist complete + deferred-gate flag.

## Work Unit Completion (WUCP Phase 1)

After the compile gate (or its deferral), execute WUCP Phase 1 per `context/protocols/work-unit-completion-protocol.md`: update `config/configuration/MODULE_CONTEXT.md`, update `coder-handoff.md` (with the Deferred Build Gate flag + next-WU pointer), append to `coder-lessons.md` if applicable, post a cross-agent note, and append the WUCP Phase 1 checklist to your Completion Report.
