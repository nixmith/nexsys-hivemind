<!--
file: context/coding-instructions/M6.1_Config_Pipeline.md
status: TOMBSTONE — this instruction is CONSUMED and was moved to `context/coding-instructions/archive/M6.1_Config_Pipeline.md` (2026-06-10, sha-reconciliation + cleanup session).
note: The sandbox VM was down this session, so the file tools could copy but not delete. Nick: `git rm` this tombstone at the next nexsys-hivemind commit — the archived copy (with the EXECUTION RECORD appended) is authoritative.
-->

# MOVED — see `archive/M6.1_Config_Pipeline.md`

M6.1 is COMPLETE (whole): M6.1b `b7bc65c` + M6.1a `9035110` (incl. ⟳ gate-fix round 1), GREEN, PM WUCP Phase 2 APPROVE. The consumed instruction, its RULING ADDENDUM, and the execution record live in the archive copy.
