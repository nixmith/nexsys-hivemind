<!--
file: context/decisions/2026-07-22_nick-rulings-1-5_verbatim.md
purpose: Nick's five rulings/orders of 2026-07-22, recorded VERBATIM per his directive ("Rulings and orders, in your priority sequence — record each verbatim"). The hub's adjudications/consequences ride pm-handoff v36 beat 6 (pointers, not copies).
status: OF RECORD.
-->

# Nick's Rulings 1-5 — 2026-07-22 (verbatim)

**(1) GAP-1 / GAP-2 — RULED: approve the REC on both, as the v1.1.2 ADDITIVE contract, under four constraints that are law, not preferences.**
(a) **Additive-only.** Both gaps resolve as new data keys/objects inside existing shapes — the `brightness_percent` precedent. If either gap, at authoring, turns out to require changing or removing ANY existing v1.1 field, casing, or nesting, that half comes back to me as a contract conversation — it is not covered by this ruling.
(b) **Wire-casing pinned at the endpoint.** The new keys follow the LIVE per-endpoint camelCase reality, read at the endpoint, never assumed from the module row.
(c) **The emitter leads, the consumer follows.** SKIP-VIS's core half (CORE-P1/P2) is the producer of record for the skip-visibility data; FE-VERDICT-2 consumes OBSERVED payloads, never anticipated ones — if the FE lane reaches a surface the core hasn't emitted yet, it mocks to the ruled v1.1.2 shape and flags, never invents beyond it.
(d) **Version discipline.** The contract stamps v1.1.2, the read-API doc carries the delta as an amendment note, and the frozen v1.1 base remains byte-stable for existing consumers. G2's re-status at FE-VERDICT-2 authoring is approved as proposed.
With that: **GAP-1 ruled -> fold CORE-P1/P2 into SKIP-VIS; GAP-2 ruled -> FE-VERDICT-2 dispatches on your authoring.** Both lanes unblock today and I want to get started working as hard, optimally and efficiently as possible.

**(2) The third plug — ORDER PLACED NOW (ruled, executing today).** I ordered two (because getting only 1 would take until Aug. 1st at least) Shelly Plug US (just like what we already have); expect it to arrive in ~2 days. Standing law restated so nothing drifts: this third unit is the **M14 DUT-of-record**; the two Gen4s in hand remain **stimulus-only, radios never provisioned, forever**. On arrival the third plug stays boxed/staged until the M14 WUs author (schedule separation governs); its sect-5.3 provisioning is WiFi-side out-of-band, never the bench Zigbee network, and the capture-verbatim rule applies at step 1 (`gen`/`model`/`app`/`fw_id` into the driver constants).

**(3) The Rosonway session — planned; author it as the operator package with these blocks.** One evening, single-variable, NOT the same evening as the CMD-API deploy session (A4/H4 fold there per your plan — keep the variables separated).
- **Preconditions:** boot-health GREEN standing; no deploy, no config edit, no other physical change in flight.
- **Block 0 — the floor, before:** `bench.sh scenario boot-health` -> expect `[PASS]` ⏺. The single-variable discipline: prove the floor green BEFORE the topology change.
- **Block 1 — the move (app STOPPED, clean SIGTERM):** coordinator dongle out of the Pi's direct port -> into the Rosonway; Rosonway (on its own PSU — powered mode) -> into a Pi **USB-2** port; the dongle keeps its extension cable and antenna separation (the Intel/AN1017 discipline — never stacked on a USB-3 port, ~1 m from any WiFi radiator). No other cabling changes.
- **Block 2 — boot + identity glance:** expect `/dev/zigbee` resolving (the udev symlink survives the new path — if it does NOT, STOP + paste; that's a udev-rule finding, not a retry), RADIO UP in the practiced envelope, `adoption_maps_rehydrated: devices=6`, ZERO proposes/relinks (no rejoin occurred — pure transport-path change), availability returning on the warm envelope. ⏺ the boot block.
- **Block 3 — the floor, after:** boot-health again -> `[PASS]` ⏺ = the topology change proven clean, single-variable.
- **Block 4 — I3b [S], the hub-attached reenumeration leg:** run `usb-reenumeration-manual` with the physical pull/replug at the ROSONWAY port. Expect: instant detection, autonomous reopen in the 16-17 s envelope, `[PASS] 3/3 · 0 forbidden`, outcomes quoted. ⏺ either way -> **I3b [S] closes on this paste.**
- **Block 5 — close:** the topology is now STANDING (record the flip in the bench constants at the next re-mint — the `usb-power` item); the AUTO usb scenario (scripted power-cut via a stimulus plug feeding the Rosonway) stays post-CMD-API as sequenced. The session's pastes are corpus material — ratchet them.

**(4) Lane cadence — confirmed: I launch lanes as you author them.** Standing mechanics so the fan-out stays clean: Core stays SERIALIZED (one host-CC Coder lane at a time — CMD-API, then SKIP-VIS, then the M14 WUs); Cowork lanes (FE-VERDICT-2, the Matter program's Phase-C sessions) run parallel-safe; every return routes to its authoring hub for the two-layer audit before any commit; and **every commit I run is explicit-paths from your stated census — the ABSOLUTE rule — no exceptions while this many writers are live.** Hand me each launch line and expected-porcelain census in the dispatch beat and I'll fire them same-day.

**(5) HUE-RESET — RULED: (a) buy the dimmer (~$25), ordered alongside the third plug; the TOUCHLINK-RESET WU is DECLINED.** Grounds, recorded: the dimmer is the zero-code path for a one-time need; a touchlink WU would spend a core slot inside the freeze window AND put ad-hoc touchlink transactions on the production coordinator next to the live fleet — new risk, zero product value, and touchlink is not a capability we ship. The dimmer is also a reusable bench tool for every future Hue-class reset. On arrival, any bench evening: factory-reset the lamp via the dimmer -> ONE permit window -> it fresh-joins and RELINKS as its registered identity (`device_relinked`, ULID stable) -> the D1 light class upgrades from banked-historical to LIVE — a bonus hardening, explicitly NOT gate-required (D1 stands ratified on the banked corpus either way; no gate-criteria interaction, J1 untouched).

_Hub annotation of record (beat 6): ruling 3's Block-2 "ZERO proposes/relinks" line carries an instrument-semantics correction in the authored package — `device_relinked` x6 IS the lawful normal-boot signature (boot-health asserts >=2); the zero-expectation tokens are proposes/joins/permit-windows. Ruling 2's SECOND arriving unit: role UNASSIGNED (spare) — assignment ruled at M14 WU authoring; the dual-role constraint (M14 DUTs never stimulus) binds it whichever way it resolves._
