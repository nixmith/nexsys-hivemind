<!--
file: context/handoff/2026-05-30_M4.0b-3_review_session_prompt.md
purpose: Session-start prompt for the FRESH Cowork conversation that runs WUCP Phase 2 review of the Claude Code M4.0b-3 output (typed change-detection comparator, AMD-51). Paste this into the new window; attach/point to the Coder's completion report + diff.
audience: PM (next Cowork session) ← Nick
state-type: handoff
status: CURRENT — 2026-05-30
-->

# Session Prompt: M4.0b-3 WUCP Phase 2 Review (typed comparator — AMD-51)

**You are the PM in Mode 3 (Director), running the WUCP Phase 2 review** of the Claude Code implementation of **M4.0b-3** (the typed `AttributeValue` change-detection comparator). Nick will paste the Coder's completion report and/or you will read the working-tree diff. Your job: verify the implementation against AMD-51 **by reading the changed source files, not the report**, adjudicate any deviations, confirm the build-gate posture, and execute WUCP Phase 2 closeout. Load the `nexsys-project-manager` skill first.

---

## 0. Session-start discipline (do first)

1. **Load the `nexsys-project-manager` skill** and **run the freshness preflight** (`project-manager/references/freshness-preflight.md`, 10 checks). Expect PASS — AMD-51 ratification + closeout was committed across all three repos (`homesynapse-core` `099a33c`, `homesynapse-core-docs` `696c414` "AMD-51 RATIFIED…", `nexsys-hivemind` `a69a809`). If STALE, reconcile before reviewing.
2. **Source-verification rule (hard-won, still in force):** the in-sandbox `git`/`grep`/working-tree mount **truncates files** on this synced folder. Verify every type/behavior claim with the **Read tool on the working tree** (or `git show <sha>:<path>` on Nick's machine) — **never trust in-sandbox `git`/`grep`, and never trust the Coder's completion report over source.** This is the rule that caught the StringValue prior-side issue during AMD-51 authoring.
3. **Ground truth at review time:** baseline HEAD `60b4185` (M4.B3/AMD-47) + AMD-51 ratification commits above. The M4.0b-3 code commit is what Nick provides. After M4.0b-3, **`projectionVersion` must be 3**. On-disk amendment watermark = **AMD-51**.

---

## 1. What is settled — the contract you verify against (do NOT re-litigate)

The implementation must satisfy **AMD-51** (RATIFIED) — `homesynapse-core-docs/design/amendments/AMD-51_Typed_AttributeValue_Change_Detection_Comparator.md`. Read §2 (spec), §4 (AMD-51-INV-01..05), §5 (tests), §10 (review disposition). The coding instruction is `context/coding-instructions/M4.0b-3_Typed_Change_Detection_Comparator.md` — its **Settled Decisions DP-A..DP-J** and the **Key Design Decision** (schema-source seam vs AMD-50-INV-03 determinism) are the spec the Coder built to.

Frozen (do not reopen): AMD-50 backfill/supersession/cursor-determinism; `DerivationContext` has no `Clock`; no `ServiceLoader`; D-01 (event-type-scoped); AMD-47 typed hierarchy + canonicalize-at-construction; the AMD-51 ratified calls; String `StateChangedEvent` payload preserved; deadband deferred; AMD-52 staged behind OQ-05-08.

---

## 2. WUCP Phase 2 review checklist — verify each against SOURCE

Read every changed `.java` file against the prior source. Confirm:

- [ ] **Comparator (AMD-51-INV-01/04):** external `AttributeValueComparator` in `com.homesynapse.state`, package-private impl + public `static structural()` factory (DEC-M3-16). Exhaustive `switch` over the 8 variants with **NO `default`** (confirm by reading the switch — a `default` arm is a fail). Pure (no clock/registry/I/O/randomness).
- [ ] **Per-variant semantics (§2.2):** exact for Boolean/Int/Enum/String; Array size-then-order-sensitive deep compare; Quantity canonical-unit dimension check + magnitude epsilon with **no unit conversion in the comparator**; Degraded per DP-F.
- [ ] **Epsilon (AMD-51-INV-02):** total form `|a−b| > max(absEps, relEps·max(|a|,|b|))`, default `1e-9`; IEEE table correct; **`Inf−Inf` handled before the arithmetic**; `−0.0`/`+0.0` canonicalized. Confirm the §5 #5b conversion-noise tests exist and pin the epsilon.
- [ ] **Symmetric reconstruction (AMD-51-INV-05):** BOTH operands reconstructed to schema-typed form; the prior `StringValue` is reconstructed too (verify a prior `StringValue("21.5")` vs inbound `"21.5"` does NOT spuriously emit). QUANTITY uses event `unit` with `canonicalUnitSymbol` fallback + **WARNING log on fallback**. Reconstruction is distinct from the `AttributeValueUpcaster` SPI (SPI unchanged).
- [ ] **Schema source (DP-K) — the highest-risk verification point.** The decision was pre-made (DP-K): a new production `StandardCapabilities` factory in `com.homesynapse.device` (lifted from `TestCapabilityFactory`), aggregated at `HomeSynapseCore` into an immutable `Map<String,AttributeSchema>` resolver keyed by `attributeKey`, injected via `DerivationRule.production(comparator, policy, schemas)`. Verify: (1) the resolver is an **injected immutable snapshot**, NOT a live registry read inside `evaluate()` (AMD-50-INV-03 — the `QuantityValue.CATALOGUE` posture); reconstruction re-executes deterministically on the 2→3 backfill; (2) `StandardCapabilities` lives in device-model **main** (not testFixtures) and `TestCapabilityFactory` delegates to it (dedup); (3) the attributeKey-keyed resolver assumption holds (no standard key with a differing type); (4) **`DerivationContext` was NOT changed** and no `module-info` change. **If the Coder hit the DP-K confirm-step and escalated** (e.g. `TestCapabilityFactory` wasn't liftable, a key collision, or a forced `DerivationContext` change) — that escalation is the central thing to adjudicate; it may need Nick. **Reminder:** no standard attribute is typed `QUANTITY` (temperature/power/etc. are FLOAT-with-unit-metadata), so the `QuantityValue` path should be unit-tested but not production-wired — confirm the Coder did not mis-type FLOAT attributes as QuantityValue.
- [ ] **String payload preserved (DP-G):** the emitted `StateChangedEvent` still carries String `oldValue`/`newValue`; `CheckpointSerializer` and the event-store shape are untouched.
- [ ] **Both paths (AMD-47-INV-02):** reconstruction+compare holds on both `onEvent` and `processBatch`.
- [ ] **`projectionVersion` 2→3 (DP-I):** bumped at `HomeSynapseCore`; the AMD-50 reconciliation/backfill triggers on the mismatch; the supersession test (AMD-50 §5 #5) still guards the typed rule (§5 #6).
- [ ] **`shouldPublishDerived` coherence (§5 #10):** string dedup not deleted; never suppresses a genuine typed-changed emit.
- [ ] **Tests (§5 #1–#10) written and meaningful;** tests inject `Clock` — **no `Instant.now()`/`systemUTC()`/`nanoTime()`/`currentTimeMillis()`** in `com.homesynapse.state` test code (`NO_DIRECT_TIME_ACCESS`, §4c). The §5 tests should genuinely fail without the implementation.
- [ ] **No `module-info` / Gradle / `CheckpointSerializer` change** (or a justified, flagged exception).
- [ ] Deviations adjudicated (`[REVIEW]`/`[INFO]`/`[BLOCKING]`); MODULE_CONTEXT updated by the Coder.

---

## 3. Build gate + closeout

- **Build gate is deferred to Nick** (sandbox can't run Gradle reliably). Confirm the Coder flagged it in `coder-handoff.md` with the commands (`./gradlew :core:state-store:check`, `:lifecycle:lifecycle:check`, full `./gradlew check`) and the target commit. Log it under Open Risks until Nick reports GREEN.
- On a clean review: execute **WUCP Phase 2** — update `core/state-store/MODULE_CONTEXT.md` (new types + Phase-3 note; AMD-51-INV rows already present), PROJECT_SNAPSHOT (M4.0b-3 status, `projectionVersion`=3), pm-handoff, design-track-map (M4.0b-3 → in-progress/done), the cross-agent-notes pointer; run the dual skill-location `diff -rq`; record the freshness preflight result.
- **Next forward WU after M4.0b-3:** revisit Workstream C (Research 6 NQ-1..6, P4 Doc-05 currency) and the AMD-52 serializer/replay design beat (OQ-05-08) when prioritized. Do NOT author AMD-52 until OQ-05-08 closes.

## 4. Pointers

- Amendment: `homesynapse-core-docs/design/amendments/AMD-51_Typed_AttributeValue_Change_Detection_Comparator.md` (esp. §5, §7, §10).
- Coding instruction: `context/coding-instructions/M4.0b-3_Typed_Change_Detection_Comparator.md`.
- Frozen backfill: `…/AMD-50_Version_Transition_Backfill_and_Cursor_Determinism.md` (§2, scenario 3.3 = this 2→3 case).
- Source to re-verify: `ProductionDerivationRule.java`, `DerivationRule.java`, `StateProjection.java` (evaluate sites, `applyToState`, `shouldPublishDerived`), the new comparator/reconstructor/policy files, `HomeSynapseCore.java` (wiring + version bump), `core/state-store/MODULE_CONTEXT.md`.

**Bottom line:** verify the Coder built the AMD-51 contract against source (not the report); the two highest-risk checks are the **schema-source/determinism** resolution (did the STOP-gate fire?) and the **symmetric reconstruction** (prior side is `StringValue`). Adjudicate, confirm the deferred build gate, run WUCP Phase 2.
