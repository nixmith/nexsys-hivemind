<!--
file: context/handoff/2026-05-31_M4.0b-4b_session_prompt.md
purpose: Kickoff prompt for the FRESH Cowork conversation that drives M4.0b-4b (the AMD-52 typed StateChangedEvent payload) via Claude Code. Paste this into the new window to start.
audience: PM (next Cowork session) ← Nick
state-type: handoff
status: CURRENT — 2026-05-31
-->

# Session Prompt: M4.0b-4b — Typed `StateChangedEvent` Payload (AMD-52), via Claude Code

**You are the PM (Mode 3, Director).** This session drives **M4.0b-4b** — the AMD-52 typed `StateChangedEvent` payload + `AttributeValue` serializer + schema-versioned replay + typed materialization — implemented by Claude Code on `main`. You issue the coding instruction, receive Claude Code's output, run the WUCP Phase 2 review, and close out. **Load the `nexsys-project-manager` skill first.**

---

## 0. Session-start discipline (do first)

1. **Load `nexsys-project-manager`** and **run the freshness preflight** (`project-manager/references/freshness-preflight.md`, 10 checks). Expect **PASS** — everything is committed and pushed: `homesynapse-core` `971cfa1` (M4.0b-4a relocation), `homesynapse-core-docs` `2dbeaf3` (AMD-52 + erratum + relocation note + invariants §22 + Doc 01/03/04 currency), `nexsys-hivemind` `451cc63` (M4.0b-4a WUCP Phase 2). Check 9 (skill-mirror) may be STALE-pending-sync if the external mirror sync hasn't run — that's the normal post-edit state, non-blocking. If anything else is STALE, reconcile before issuing the instruction.
2. **Source-verification rule (standing):** the in-sandbox `git`/`grep`/`wc` truncates and line-ending-churns this synced folder (it showed a ~50-file spurious "modified" list during the M4.0b-4a doc commit; the host git correctly showed ~5). Verify every type/module/version claim with the **Read tool on the working tree** at HEAD `971cfa1` — never trust in-sandbox `git`/`grep` for source facts, and never trust a completion report over source.
3. **Ground truth (confirm at session start):** HEAD `971cfa1`. `projectionVersion` = **3** (this WU bumps it **3→4**). On-disk amendment watermark = **AMD-52**. The `AttributeValue` hierarchy (interface + 8 variants + `AttributeType`) lives in the **new `com.homesynapse.value` leaf module** (relocated by M4.0b-4a; `AttributeSchema`/`AttributeValueUpcaster` stayed in `com.homesynapse.device`). `StateChangedEvent` still carries **String** `oldValue/newValue` (this WU makes them typed). Workstream A otherwise complete.

---

## 1. What is settled — the contract (do NOT re-litigate)

The implementation must satisfy **AMD-52** (RATIFIED 2026-05-31) — `homesynapse-core-docs/design/amendments/AMD-52_Typed_StateChangedEvent_Payload_Serializer_and_Replay.md`. Read §2 (spec), §4 (AMD-52-INV-01..07, already registered in `Architecture_Invariants_v1.md` §22), §5 (tests), §7 (source anchors), §9 (ratification — F1/F2 confirmed), **§11 (erratum — the `com.homesynapse.value` relocation)**. The coding instruction is `context/coding-instructions/M4.0b-4_Typed_StateChangedEvent_Payload_Serializer.md` (titled M4.0b-4b; **its top banner carries the post-relocation deltas — read it**).

**Frozen (do not reopen):** the two ratified forks — **F1** (bit-anchored `Double.doubleToLongBits` float identity; `chain_hash` stays the inert AMD-37 zero-reservation) and **F2** (Path B legacy `schema_version=1` reads → defined `DegradedEvent`, NO `AttributeValueUpcaster`/schema resolver in the decode path); the codec mechanism (compact `{"t":…,"v":…}` envelope, exhaustive no-`default` switch, no `@JsonTypeInfo`, Jackson confined to persistence); the `schema_version` 1→2 seam; `projectionVersion` 3→4 on AMD-50's **frozen** backfill; AMD-50/AMD-51 unchanged; no `Clock` in derivation. The relocation (M4.0b-4a) is done — do not move types again.

---

## 2. The work — issue M4.0b-4b to Claude Code (on `main`, no worktree)

This is a normal coding WU like M4.0b-1/2/3 — Claude Code implements on `main` and produces files; you do NOT run a worktree (the worktree was a one-off for the M4.0b-4a cycle de-risk, now resolved). Hand Claude Code the refreshed instruction. It implements AMD-52 §2.1–§2.7:

- `StateChangedEvent.oldValue/newValue` String → `com.homesynapse.value.AttributeValue` (`oldValue` nullable = first report).
- A custom `JsonSerializer`/`JsonDeserializer` pair in `core/persistence` — compact `{"t":<AttributeType>,"v":…}` envelope, exhaustive switch over the 8 variants, no `@JsonTypeInfo`, bit-anchored float + JSON-valid non-finite sentinels (`"NaN"`/`"+Inf"`/`"-Inf"`, `−0.0`→`+0.0`), registered via `PersistenceJacksonModule`.
- `ProductionDerivationRule` emits the typed `StateChangedEvent` it already reconstructs, at `EventDraft(…, schemaVersion=2, …)`.
- `StateProjection.applyToState`/backfill materialize the typed value; `shouldPublishDerived` → typed-coherent compare.
- `CheckpointSerializer` → typed envelope per entry (preserve the `ALWAYS` null round-trip).
- `EventPayloadCodec.decode` Path-B version gate: `state_changed` + `schema_version==1` → `DegradedEvent`.
- `projectionVersion` **3→4** at the composition root (rides AMD-50's frozen backfill).
- **No `module-info.java` change** — M4.0b-4a already added every `requires com.homesynapse.value` edge (event-model has it, so `StateChangedEvent` referencing `AttributeValue` compiles with no new edge; persistence has the codec's edge). The §4c arch-rule test-`Clock` reminder applies (persistence + state-store both non-whitelisted).

**Before issuing:** spot-verify the instruction's source anchors against HEAD `971cfa1` with the Read tool (the standing §7 discipline) — confirm `StateChangedEvent` is still String, `AttributeValue` is in `com.homesynapse.value`, and `event-model`/`persistence` module-info already `requires com.homesynapse.value`. The instruction's banner asserts these; verify, don't assume.

---

## 3. Build gate + review + closeout

- **Build gate deferred to Nick.** Confirm Claude Code flagged it in `coder-handoff.md` with the commands (`:core:event-model:check`, `:core:device-model:check`, `:core:state-store:check`, `:core:persistence:check`, `:lifecycle:lifecycle:check`, full `./gradlew check`) against the M4.0b-4b tree on `971cfa1`. Log under Open Risks until GREEN.
- **WUCP Phase 2 review:** use `context/handoff/2026-05-31_M4.0b-4b_review_session_prompt.md` (the review checklist — verify against source, not the report). Risk #3 there (the event-model→device edge) is **resolved by 4a** — verify instead that the typed field sits on the legal `event → value` edge.
- **On GREEN + clean review, execute WUCP Phase 2 closeout:** PROJECT_SNAPSHOT (M4.0b-4b COMPLETE, `projectionVersion`=4, watermark still AMD-52), pm-handoff, the cross-agent pointer, MODULE_CONTEXTs (the new codec types in persistence; `StateChangedEvent` typed in event-model; typed materialization in state-store), and **fold the Doc 01/03/04 bodies** (the masthead currency notes said the fold happens at this WU). AMD-52-INV-01..07 are already registered (§22) — no new invariant registration needed. Run the dual skill-location `diff -rq`. Record the preflight result.

---

## 4. Next forward after M4.0b-4b

Workstream A is then truly complete (typed end-to-end). The open M4 fronts: **Workstream B** (device-model breadth — AMD-44 Floor/EntityRole, Research 8 REC-23–30; not gated on AMD-52); **Workstream C** (integration-api freeze, Research 6 REC-41–51; P3-gated on NQ-1..6 + P4 Doc-05 currency); the **timestamp-model unifier** (needs its own design beat). Confirm priority with Nick.

## 5. Pointers

- Amendment: `homesynapse-core-docs/design/amendments/AMD-52_*.md` (esp. §2, §4, §5, §7, §11 erratum).
- Coding instruction: `context/coding-instructions/M4.0b-4_Typed_StateChangedEvent_Payload_Serializer.md` (M4.0b-4b; read the banner).
- Relocation design note: `homesynapse-core-docs/design/2026-05-31_AttributeValue_Module_Relocation_Design_Note.md` (the `com.homesynapse.value` home).
- Review prompt: `context/handoff/2026-05-31_M4.0b-4b_review_session_prompt.md`.
- Invariants: `Architecture_Invariants_v1.md` §22 (AMD-52-INV-01..07).
- Source to re-verify at `971cfa1`: `StateChangedEvent.java`, `AttributeValue.java` (+ variants, now in `com.homesynapse.value`), `EventPayloadCodec.java`, `CheckpointSerializer.java`, `ProductionDerivationRule.java`, `StateProjection.java`, `PersistenceJacksonModule.java`/`UlidSerializer.java`, the composition root (version bump), the `module-info.java` set, the three MODULE_CONTEXTs.

**Bottom line:** the graph is clean (cycle resolved by M4.0b-4a, committed `971cfa1`), AMD-52 is ratified, and the 4b instruction is refreshed for the relocated packages. Run the preflight → spot-verify the instruction's anchors → hand it to Claude Code → review against source → bump to `projectionVersion` 4 → close out. This completes the typed-payload work AMD-51 §2.7 staged.
