<!--
file: context/handoff/2026-05-31_M4.0b-5_review_and_next-batch_session_prompt.md
purpose: Kickoff prompt for the FRESH Cowork conversation that receives the M4.0b-5 (timestamp-unifier) Claude Code results, runs WUCP Phase 2, closes out, issues Workstream B Stage 1, and plans the next batch. Paste into the new window.
audience: PM (next Cowork session) ← Nick
state-type: handoff
status: CURRENT — 2026-05-31
-->

# Session Prompt: M4.0b-5 Review + Closeout → Workstream B Stage 1 → Next-Batch Planning

**You are the PM (Mode 3, Director).** This session receives the **M4.0b-5 (timestamp-model unifier, AMD-53)** Claude Code results, runs the WUCP Phase 2 review against source, closes out, then issues **Workstream B Stage 1** and plans the next batch. **Load the `nexsys-project-manager` skill first.**

---

## 0. Session-start discipline (do first)

1. **Load `nexsys-project-manager`** and **run the freshness preflight** (10 checks). Expect **PASS**. State as of the handoff:
   - `homesynapse-core` HEAD `72596cb` (M4.0b-4b) — **plus** the M4.0b-5 working tree Nick is bringing back from Claude Code (verify HEAD; if Nick committed M4.0b-5, it's a new SHA bumping `projectionVersion` to 5).
   - `homesynapse-core-docs`: AMD-53 **RATIFIED** (watermark **AMD-53**, AMD-53-INV-01/02 in Architecture_Invariants §23, Doc 03 §4.1 AMD-53 currency note) — committed by Nick from the cleanup session.
   - `nexsys-hivemind`: M4.0b-5 instruction issued; PROJECT_SNAPSHOT/backlog/cross-agent-notes reflect "AMD-53 RATIFIED + M4.0b-5 issued/in-flight"; the two-Project playbook + CORE/DOCS briefs committed.
   - Check 9 (skill-mirror) may read STALE-pending-sync if Nick hasn't run the external mirror sync — normal. If a `.gitattributes` line-ending normalization was applied (cleanup recommendation), expect a one-time large but content-neutral diff — not real change.
2. **Source-verification rule (standing):** the in-sandbox `git`/`grep`/`wc` line-ending-churns this synced folder (the cleanup session saw the whole hivemind + docs `git status` falsely inflated, and a `git diff` that showed `checkAll` deleted). **Verify every type/field/line claim with the Read tool on the working tree; commit from host git.**
3. **Ground truth to confirm:** on-disk watermark **AMD-53**; `projectionVersion` **4** pre-M4.0b-5, **5** once it commits; Workstream A is the typed end-to-end line, with M4.0b-5 as its honest last mile.

---

## 1. Receive + review M4.0b-5 (the immediate task)

Nick is/has run `context/coding-instructions/M4.0b-5_Timestamp_Model_Unifier.md` (implements AMD-53). Receive the Claude Code completion report + the build-gate result, then **run WUCP Phase 2 against source, NOT the report**:

- **The §5 #1 gate:** the headline LIVE ≡ replay-from-zero test must assert **all three** activity timestamps (`lastChanged`/`lastUpdated`/`lastReported`) identical per entity, with a fixed test `Clock` offset from the corpus's `eventTime`/`ingestTime`. Confirm it exists and genuinely fails a wall-clock regression.
- **The §2.4 load-bearing caveat:** verify the 4→5 reconciliation heal re-derives **all three** fields from event-time — not just `lastChanged`. The heal is complete only because `applyToState`'s `state_reported` branch AND `initialEntityState` adoption seeding were fixed; the backfill helper alone does not touch `lastUpdated`/`lastReported`. The §5 #5 heal test guards this.
- **Confirm:** event-time sourcing in every `applyToState` branch (L788–857 region at `72596cb`); `initialEntityState` seeds from event-time; `staleAfter`/`stale` untouched (AMD-53-INV-02); `clock` retained only for `reconciledAt`/checkpoint/replay-metric/staleness; `projectionVersion` literal 5; no `module-info`/typed-pipeline/attribute-value/`stateVersion` change (AMD-53 §6); the §4c arch-rule (`NO_DIRECT_TIME_ACCESS`) holds over the new tests.
- **Build gate:** confirm Nick ran `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` GREEN; log under Open Risks until confirmed.
- **Inaugural two-Project dogfood:** route this review through the **CORE Project** (a CORE-Project second opinion on the implementation) as the first real exercise of the new two-Project setup. Embed the changed `StateProjection`/`HomeSynapseCore` excerpts + the AMD-53 §2.1/§4 text as the source companion.

**On GREEN + clean review, execute WUCP Phase 2 closeout:** PROJECT_SNAPSHOT (M4.0b-5 COMPLETE, `projectionVersion`=**5**, watermark AMD-53, Workstream A truly complete), backlog M4.0b-5 → DONE + SHA, W22 (or the new week's) plan, the `core/state-store` MODULE_CONTEXT (event-time sourcing + `projectionVersion` 5 + AMD-53-INV-01/02), pm-handoff, cross-agent-notes pointer, dual skill-location `diff -rq`. AMD-53-INV-01/02 already registered (§23) — no invariant work.

---

## 2. Then issue Workstream B Stage 1 (after M4.0b-5 commits)

**Do not issue until M4.0b-5 is committed** (WUCP one-uncommitted-milestone-at-a-time). The B Stage 1 coding instruction was authored in the AMD-53 window — **locate it** (search `context/coding-instructions/` for the Workstream-B Stage 1 / Floor / `Set<HardwareIdentifier>` instruction; per cross-agent-notes it covers `FloorId` + `Floor`/`Area` records + `FloorRegistry`/`AreaRegistry` interfaces + `AreaId` Javadoc cleanup + `List→Set<HardwareIdentifier>` with the §2.6 blast-radius survey; EntityRole + `EntityType`/`Entity`/`ProposedEntity` fenced to Stage 2). **Verify it against source at the post-M4.0b-5 HEAD before issuing** (AMD-44 is RATIFIED-pending-impl; embed the device-model `module-info.java` + MODULE_CONTEXT). Route any design question through the **DOCS Project**.

---

## 3. Plan / research the next batch (parallel PM work)

- **Workstream B Stage 2 (EntityRole)** — sequence after Stage 1; same AMD-44, second stage.
- **Workstream C (integration-api freeze)** gates to clear *before* C: **Research 6 NQ-1..6** (Nick's calls) + **P4 Doc-05 (Integration-Runtime) currency**. Tee these up now so C isn't a cold start when B finishes. **C must freeze against the post-B device model** (Research 6's capability surface — `CapabilityAdded`/`CapabilityRemoved`, capability identity, `Entity.capabilities` — depends on B's Entity/EntityRole shape, per the Research 6 PM assessment NQ-3/NQ-4).
- **Integration-AMD renumber ripple (cheap, do early):** AMD-53 consumed `53`; the Research 6 assessment used `AMD-53/56/59` as *placeholders* for the integration amendments. Annotate the Research 6 assessment's `AMD-53` reference as stale/renumber-at-milestone (the P2 renumbering decision already re-bases the integration block to 54+) so a future C session doesn't trip on it.
- **Two-Project setup is live** — use **DOCS** for design/governance/amendment/research second opinions and **CORE** for code/JPMS/test reviews; Cowork orchestrates and embeds the per-question source companion (the connector is best-effort, never load-bearing). Spine-upload manifest: playbook §2/§3/§4.

---

## 4. Pointers

- Coding instruction: `context/coding-instructions/M4.0b-5_Timestamp_Model_Unifier.md`. Amendment: `homesynapse-core-docs/design/amendments/AMD-53_*.md` (§2.1, §4, §5, §7). Invariants: Architecture_Invariants_v1.md §23.
- Two-Project architecture: `context/process/2026-05-31_two-project-claude-architecture.md` + `project-knowledge/CLAUDE_PROJECT_{CORE,DOCS}_custom-instructions.md`.
- Source to re-verify at the post-M4.0b-5 HEAD: `core/state-store/.../StateProjection.java` (`applyToState`, `initialEntityState`, `backfillTimestamp`), `lifecycle/.../HomeSynapseCore.java` (`projectionVersion`), `EntityState`.
- Current state hub: `context/status/PROJECT_SNAPSHOT.md`; sequence + Open Risks: `context/handoff/pm-handoff.md`; latest pointer: `context/handoff/cross-agent-notes.md`.

**Bottom line:** AMD-53 is ratified and fully registered; M4.0b-5 is the in-flight implementation. Receive it, review against source (the all-three-fields replay-determinism test is the gate; dogfood the CORE Project), bump `projectionVersion` to 5 at closeout, then issue Workstream B Stage 1 and tee up C's gates. This closes Workstream A for real.
