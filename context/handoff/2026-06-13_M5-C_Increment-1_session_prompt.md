<!--
file: context/handoff/2026-06-13_M5-C_Increment-1_session_prompt.md
purpose: Next-PM-session brief — M5-C INCREMENT 1 (the W25 Lane-4 structural gate + M7 entry-gate row 4): website skeleton + a reviewable config-superiority page draft. CONTENT REGISTER (market-facing copy), not code. Runnable as a PARALLEL Cowork conversation to any evidence/governance session.
audience: PM (next Cowork session), Nick (content veto authority)
status: READY — authored 2026-06-12 at the Pi-evidence session closeout. Two consecutive windows missed M5-C (the P6 failure); the Lane-4 gate is STRUCTURAL: no new Core coding instruction until Increment 1 is DONE.
-->

# Session Brief — M5-C Increment 1: Website Skeleton + Config-Superiority Page Draft

PM session (nexsys-project-manager skill). **Read `context/process/cowork-environment-model.md` FIRST**, then the freshness preflight vs the PINNED STATE below — one glance per check. This is a CONTENT session: the register is market-facing prose (the R13/R14/R15 evidence base), not engineering documentation. The three copy guardrails below are inviolate.

## STEP -1 (before Step-0): Nick rules the D-1..D-7 strategy-draft vetoes

`context/planning/2026-06-13_strategy-refresh-drafts_R15.md` — seven insert-ready items, per-item veto-or-default (default = ACCEPT-as-drafted). The ruled set feeds this session's copy directly (esp. the segment-messaging rule and the Matter economics/trademark fence). If Nick defers, proceed with the drafts AS PM DEFAULTS, marked provisional in the page draft.

## PINNED STATE (verified 2026-06-12 at the Pi-evidence closeout)

- **Hivemind:** `d70f8d1` (hygiene rotation, PUSHED) **+ ONE returns-intake commit expected on top** — reconcile the actual HEAD at Step-0. NOTE: the R16 returns are BACK and intake-validated — if the R16 assessment session has already produced its merged disposition, its M5-C-COPY bucket (REC-193/194/195: no-ring-buffer attestation, the porch-light/B3 dossier, the logs-survive-an-LLM-paste property) feeds THIS session's page draft directly; if not, proceed without (fold at Increment 2). Core **`01841ba`** (scripts-only pair on `e5ea76f`; substantive `7c73c91` — GREEN 147, pins 55/24/36, `projectionVersion` 5). Docs **`d7ea212`**. Check 9: the known one-file STALE acceptable. Hot-path post-rotation: snapshot ~59K / pm-handoff ~17K (cheap session start by design).
- **Gate ground (post-Pi-evidence):** OQ-15-2 ✅ RESOLVED (encrypted set CONFIRMED `[identity, presence_personal]`); M6.3 1-of-3 closed (interviews = the sole open evidence gate); M7 entry-gate rows 1–2+5 ✅, row 3 ⛔ (interview half), **row 4 ⛔ = THIS SESSION**.
- **R16 cycle:** dispatched-or-pending (Nick veto-or-default) — its M5-C-COPY bucket lands LATER; do NOT block on it (fold at Increment 2).

## TASK

1. **Step-0:** light preflight vs pinned (one glance each); reconcile new HEADs into the snapshot masthead; archive this prompt when consumed.
2. **Venue ruling (Nick, at session start):** no site repo exists → PM default `homesynapse-core-docs/website/` per the W25 Lane-4 charter (veto = Nick names a repo/dir).
3. **Website skeleton:** minimal, reviewable structure (landing + the config-superiority page + stubs for the §2e backlog pages: ledger-gap dossier [the FLAGSHIP, Increment 2], no-cloud-account, explainability). Plain Markdown/HTML — no framework decision this session (that is a Nick call, logged as an open item).
4. **The config-superiority page draft (the Increment-1 deliverable):** a reviewer can read it end-to-end. Content sources, in authority order: Research-13 §1/§3.1 (the config market-superiority evidence) · INV-CE-01 (split-brain immunity — the structural claim) · merged-disposition §2e backlog items (no-cloud-account claim; event-sourced explainability with the R14-A pain citations) · the ruled D-1..D-7 drafts · **NEW: the OQ-15-2 resolution as an evidence point** (sensitive-PII encrypted-by-default CONFIRMED on measured hardware evidence — usable for trust/explainability copy, but see guardrail (b)).
5. **The three copy guardrails (merged disposition §2e — inviolate):** (a) the Matter trademark fence; (b) **never lead with commodity encryption** (the OQ-15-2 point SUPPORTS a trust narrative, it is never the headline); (c) the segment messaging rule (per D-1..D-7 as ruled).
6. **Self-review** per `references/review-and-quality.md` adapted to the content register: every factual claim traces to a source artifact (REC numbers, INV numbers, measured results); no fabricated metrics; anti-requirements respected (no claims implying engine retry, templating DSL, or destructive migration).
7. **Closeout:** Lane-4 gate → DONE-when check (reviewer-readable draft on disk + Nick has the review pointer); entry-gate row 4 state per Nick's review verdict (DONE flips it; review-pending = drafted-awaiting-veto); snapshot/pm-handoff/W25 appends; commit message handed over.

## TOKEN DISCIPLINE

Do NOT re-read the R13/R14/R15 returns — the assessments and the merged disposition §2e carry the dispositive content (quote REC numbers from there). Doc reads only for INV-CE-01's exact wording and any directly quoted invariant. The window's precision item is the PAGE DRAFT — spend it there.

## STANDING NICK-PACED ITEMS (surface, don't block on)

Energy/erasure interviews (the sole open M6.3/row-3 evidence gate) · optional Pi-4 confirmation run (advisory) · the pi4-validation.sh script fix (F1/F3) · spike-dir `git rm` + DOCS-connector deselection · backlog DONE-row compression · FUTURE-AMD queue · R16 returns (fold the M5-C-COPY bucket at Increment 2).
