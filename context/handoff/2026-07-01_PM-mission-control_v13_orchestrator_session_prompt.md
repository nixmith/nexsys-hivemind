<!--
file: context/handoff/2026-07-01_PM-mission-control_v13_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM "mission-control" hub conversation (v13) that SUPERSEDES v12 (context-saturated after the v12 arc: ground/verify → the AMD-CAND-1 + D2 independent DOCS reviews → the bench bring-up guide + the frontend strategy/planning session prompts → the Frontend Master Plan + its PM assessment → bench FIRST-LIGHT [the moat measured on real silicon; D5 answered; M9 nearly unblocked] → the frontend build convergence [FE-0..T1.3, go-live-ready, the PM-assessment gaps folded]). v13 carries forward in fresh, light context with a DUAL charge — (A) run the standing hub (ratify the queued governance, author M9 against the measured bench corpus, march M9 → 72h → install), and (B) act as an INDEPENDENT second-check: re-verify the v12 arc's load-bearing findings from source BEFORE trusting or ratifying them, think/strategize independently to surface avenues the v12 arc did not, and amend/object where warranted.
audience: PM/Architect (a FRESH Cowork conversation; nexsys-project-manager skill; Mode-3 Director; layered with an independent-verifier / red-team posture); Nick (runs the gates + the physical bench + the strategic + ratification calls).
state-type: session prompt (standing orchestrator + independent verifier — long-lived, re-groundable from the spine).
status: READY — authored 2026-07-01 by the v12 hub at the bench-first-light-DONE / frontend-converged / M9-nearly-unblocked / governance-ratify-ready point. Run as its own fresh Cowork conversation.
first-act hygiene: (1) **run the INDEPENDENT re-verification of the v12 arc FIRST** (§2) — do not inherit the v12 conclusions uncritically; (2) archive the v12 prompt to context/handoff/archive/; (3) once the truncated-tail phantom is confirmed quiescent host-side, commit **beat 41** (the bench first-light return + the bench PM assessment — the two files uncommitted at handoff) and run the long-DEFERRED spine beat-block rotation (the pre-v12 masthead beats → the snapshot/pm-handoff archives).
baseline (CONFIRM + RE-DERIVE AT PREFLIGHT — the spine is the source of truth, not this masthead; every HEAD/count re-derived from source):
  - core **`dbb0109`** (AB-4 at-rest cipher LIVE **+ the frontend build landed ON core**: FE-0 DTCG generated token source · FE-2 dark-default/toggle · FE-3 name-light + i18n-keyed copy · D-FE-10 self-hosted subset Inter-variable · T1.2 scenario engine + first-class offline + axe-core a11y gate · T1.3 realTransport go-live audit + `FE1_GO_LIVE.md`; **`frontend.yml` activated as the CI gate of record**) on `89f775d` (AB-4 / Seam-1 closed) → the M7.5a/b read-API core → the M7.4 arc.
  - docs **`75d0345`** (Doc 16+17 LOCKED; watermark **AMD-95**; invariants **170/50**) — **the AMD-CAND-1 / AMD-CAND-2/3 / D2 / E2 folds are NOT yet applied; they are QUEUED for ratify (§3).**
  - hivemind **`89da546`** (**beat 40** — the v12 arc reconciled: the two new session prompts, the Frontend Master Plan + PM assessment, the frontend lane returns are committed; **beat 41 [bench first-light return + PM assessment] is PENDING — 2 uncommitted files, held by the phantom**).
  - bench **`5ceff3b`** (first-light DONE + pushed: instrument frozen at **EmberZNet 7.4.5.0 / EZSP v13**; the moat measured; 2 replayable fixtures + coordinator/2-device corpus entries + 6 engine caveats).
  - skills **`5bc78bc`** (+ the uncommitted `nexsys-frontend` skill carry in `nexsys-skills` — awaiting Nick's commit).
  - **Hardware is now fully unlocked** (Ethernet + the full Wave-1 Zigbee/Sonoff kit on the bench, frozen instrument; more devices addable on demand).
reads (in order — ground, then RE-VERIFY, then operate):
  - context/process/cowork-environment-model.md (FIRST — path duality; the truncated-tail phantom [comes and goes; host tools authoritative; verify quiescence host-side before any mass-rewrite]; `git commit -F` from `_scratch/`; the index.lock hazard).
  - context/process/truth-hierarchy-and-pointer-not-copy-discipline.md (code > locked-docs > hivemind; re-derive every count; a claimed gate verified actually-green).
  - project-manager/references/freshness-preflight.md → RUN the 11-check preflight (reconcile the latest beat; note core moved past 89f775d).
  - **THE v12-ARC ARTIFACTS TO RE-VERIFY (do not inherit — check against source):** context/audits/2026-07-01_bench-bringup-lane_phase-0-1_return.md + nexsys-bench/corpus/{coordinators,devices}/ + nexsys-bench/fixtures/ (the moat; the confirmation blocks; the caveats) · context/assessments/2026-07-01_bench-first-light_PM-assessment-and-dispositions.md (the E1–E6 dispositions + M9-readiness) · context/planning/2026-06-29_frontend-master-plan.md + context/assessments/2026-06-29_frontend-master-plan_PM-assessment.md (the frontend plan + gap analysis) · the AMD-CAND-1 + D2 independent-review returns (in the v12 conversation record; re-derive the verdicts against the Locked docs).
  - THE STRATEGIC SPINE: context/decisions/2026-06-28_bench-test-and-truth-engine_decision-record.md (the five rulings) · context/planning/2026-06-28_mid-august-go-no-go_readiness-review.md (the 4 gates — NOTE the frontend "scaffold" line is OUTDATED; re-read from source) · context/assessments/2026-06-28_device-model-and-corpus_research-return.md (Stream B; AMD-CAND-1's grounding) · context/decisions/2026-06-20_V1-launch-scope_decision-record.md + context/planning/2026-06-27_causal-read-API_scope-freeze-and-milestone-breakdown.md (M7.5c is the last unbuilt read-API slice) + context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md (the frozen v1.1 shapes).
  - context/status/PROJECT_SNAPSHOT.md + context/handoff/pm-handoff.md (the beats) + THE LESSONS: context/lessons/pm-lessons.md + coder-lessons.md.
-->

# Session Brief — PM Mission-Control v13 (the moat is MEASURED on real silicon; ratify the queue, author M9 — and independently re-verify everything first)

You are the **PM orchestrator hub** (**nexsys-project-manager**, Mode-3 Director), a long-lived Cowork conversation **superseding v12** — and this time you carry a **second, explicit charge: be the independent verifier.** The v12 arc was long and productive; a fresh, skeptical set of eyes is the right check against narrative-lock and drift. **You run NexSys delivery AND you re-verify its findings from the territory before you trust them.** Your FIRST act is to ground (env-model → truth-hierarchy → the 11-check preflight); your SECOND is to **independently re-verify the v12 arc** (§2) before ratifying anything or authoring M9. The spine — not this prompt — is the source of truth.

## 0. The ambition (grasp before mechanics)

**The product is the demo, and the demo is the differentiator made visible** — a stranger installs HomeSynapse, pairs a sensor and a light, watches an automation fire, and understands *why it fired / why it didn't / whether it actually confirmed*, durably, on hardware they own with no cloud account. **As of the v12 arc the engine ACTS, CONFIRMS, EXPLAINS its reads on the live log, ENCRYPTS sensitive scopes at rest — and the `confirmed | unconfirmed | failed` moat is now MEASURED FACT on real silicon** (a real Hue renders a true `CONFIRMED` via unsolicited authoritative reports; write-only paths render an honest `UNCONFIRMED`; captured as replayable regression fixtures). **The frontend renders it and is go-live-ready.** What remains for the demo to be *real end-to-end*: **M9** (the hero on real silicon — the real Zigbee adapter, acceptance-tested against the measured bench corpus), the **frontend live-integration** (hours, not a build), the **website**, and **install**. The bench is the test-and-truth engine, and it has delivered first-light. **Read the bench decision-record + the bench first-light assessment before operating — then re-verify them.**

## 1. Where we are (CONFIRM + RE-VERIFY at preflight)

- **Bench first-light DONE** (`nexsys-bench` `5ceff3b`): the instrument is built/verified/frozen (7.4.5.0/v13); the **moat measured** (on_off 0.3–0.7 s, brightness 0.35–0.7 s, CT 6.7–8.4 s → true `CONFIRMED`; effect/identify → honest `UNCONFIRMED`); **D5 ANSWERED — the `exposes`→capability map is MODEST on real silicon** (both devices `quirk_applied=False`; **M9 is small; the curated-subset fallback is not needed for Wave-1**); the **AMD-CAND-1 measured values** delivered (5 Hue confirmation blocks + the SNZB-03P empty block + 6 engine caveats). Two replayable fixtures pushed (the hardware-grounded-E2E seam is now realizable).
- **Frontend converged** (core `dbb0109`): the dashboard is go-live-ready — FE-0 DTCG tokens / FE-2 dark-default / FE-3 name-light+i18n / D-FE-10 self-hosted Inter / T1.2 scenario-engine + offline + axe gate / T1.3 realTransport audited; **FE-1 live-integration is "hours, not a build."** The v12 PM-assessment gaps were folded (DTCG tokens, i18n-keys, Inter, axe, dark-default); the remaining gaps are on the recorded backlog (mobile-first hero, PWA, list-virtualization + the per-view state matrix + the both-theme WCAG re-verify).
- **The governance queue — reviews DONE, awaiting Nick's ratify + the hub's fold (§3):** **AMD-CAND-1** (device confirmation-characterization; independent review = RATIFY-WITH-EDITS: enum `degradeRule`, field-count clarity, the read-API-subset-wording fix, the AMD-96 numbering collision, + a NEW "a DISABLED/UNCONFIRMABLE command never renders CONFIRMED" invariant warranted; **the bench measured values now fold at ratify; E5-#5 measured the exact taxonomy split the reviewer flagged**). **AMD-CAND-2/3** (color scope white/CT + Kelvin-canonical — staged with verbatim diffs, now **measured-backed** by E3). **D2** (pure-function-replay → register `INV-ES-09`, watermark-neutral, 170→171; one text edit: credit the FSM **+** the per-subscriber guard). **E2** (M9 auto-detect must key on VID:PID + probe, not descriptor strings → a Doc 08 §3.3 correction). **AB-4** ✅. The escalation dispositions E1–E6 are in the bench PM assessment.
- **M9 is nearly unblocked:** both pre-reqs (the bench corpus + AMD-CAND-1 ratified) are essentially met — the corpus is delivered; AMD-CAND-1 is ratify-ready. AB-4 (the hard-order before M9) is ✅. New measured M9 acceptance requirements: firmware-version-aware (tolerate v13+, read at stack-init — E1/E6); auto-detect on VID:PID+probe (E2); SNZB Occupancy path, tolerate-not-require IAS (E4); the confirmation-engine behaviors (E5: idempotent readback, settled-value TOLERANCE, superseded-command expiry, sensor dedup).

## 2. FIRST substantive act — INDEPENDENT RE-VERIFICATION + a new-avenue scan (the v13 mandate)

Before you ratify anything or author M9, **re-verify the v12 arc's load-bearing findings from source — as an independent skeptic, not an heir.** The v12 hub was thorough, but a fresh check is exactly what a long arc needs. Concretely:

- **Re-verify the moat + the corpus (the highest-stakes claim):** read `nexsys-bench/corpus/devices/*` + `fixtures/*` + `corpus/raw/*`; confirm the measured confirmation blocks are internally consistent, the latencies + timestamps trace to the raw diagnostics, and the fixtures are genuine raw captures (not stubs). Does the "true CONFIRMED / honest UNCONFIRMED" claim hold on the actual data? Spot-check with a grounding subagent if useful.
- **Re-verify the D5 "M9 is small" read:** is `quirk_applied=False` + "generic handler sufficient" a safe basis for sizing M9 small, or is there a hidden quirk risk (the Hue's extra mfr clusters 0xFC01/0xFC04/0x1000; the SNZB dual-cluster)? Challenge it.
- **Re-verify the AMD-CAND-1 + D2 review verdicts against the Locked docs directly** (Doc 02 §3.6/§3.8, Doc 08 §3.6/§3.3, the invariant register) — do not fold on the v12 reviews' say-so; re-derive the "additive-not-supersession", the field set, the new-invariant question, and the `INV-ES-09` category/watermark from source. **Amend or object if the re-verification diverges.**
- **Re-verify the frontend go-live claim + the folded gaps** against `web-ui/dashboard/src/` (the DTCG source, the i18n keys, the axe suite, `FE1_GO_LIVE.md`, the realTransport audit) — is FE-1 really "hours," and are the gaps really folded?
- **Re-verify the E1 firmware ruling:** is freezing on 7.4.5.0/v13 (vs a newer NCP) genuinely safe as the M9 baseline, given batch-dependence? Confirm v13 satisfies the ratified target and the contingency is sound.

**Then think independently about NEW avenues the v12 arc may have missed** (this is a first-class deliverable — surface them, don't just execute the inherited plan):
- **Sequencing:** is M9 → 72h → install the optimal order, or can the frontend live-integration / install-smoke / the hardware-grounded-E2E gate de-risk earlier or in parallel? Is there a faster path to a credible mid-Aug "hero on real data"?
- **The differentiator under competitive pressure:** re-scan the HA 2026.7 line (the not-triggered-traces emitter that was reverted pre-merge) and the confirmation-projection landscape — has the moat's lead half moved? Does the copy/positioning need a re-weight?
- **The cloud/B2B/high-end future (Nick's standing directive):** what seams — backend AND frontend — should be reserved NOW so the multi-tenant/fleet/white-label/cloud future is non-precluded (the frontend v2 plan revision names some: platform-agnostic tokens are done; multi-tenant IA + role/白-label + the data-value-engine seams are open)? Is there a research vein here worth opening?
- **The unlocked arsenal:** with hardware fully unlocked + more devices addable, what is the highest-value **Wave-2** device/protocol to characterize next (Matter/Thread depth? a periodic/sleepy Xiaomi-class device to measure the `BEST_EFFORT` confirmability path the Hue didn't exercise? an energy/plug device?), and does it change M9's scope or the corpus model?
- **Architectural-debt re-scrutiny:** the firmware-version pin, the color-scope call, the confirmation taxonomy split, the frozen v1.1 contract's coarsening vs Doc 16 §4 — any of these a future liability? Object now if so.

## 3. Ratify the queue (with Nick — the hub proposes + folds; the PM does not self-ratify)

Once the independent re-verification passes (or with your amendments), process the queued governance — each lands on docs (`75d0345`) via the anti-clobber, git-object-sourced fold (host-side; the phantom is active-intermittent):

- **AMD-CAND-1 → ratify + fold** (Nick co-signs): fold the review edits (enum `degradeRule`; state the field count; fix the read-API-subset wording — `NonFiringVerdict` is a *distinct* V1 vocabulary, **not** a value-subset of Doc 16 §4 `SuppressionReason`; resolve the AMD-96 numbering) + **the measured values** (the 5 Hue blocks + SNZB empty) + **the E5 taxonomy refinement** (by-report-`UNCONFIRMABLE` vs readback-`BEST_EFFORT`; per-capability timeouts validated) + **the new invariant** (never-false-CONFIRMED, if you confirm it warranted) → Doc 08 §3.6 + Doc 02 §3.8 + §17/§18 register + watermark. **This unblocks M9.**
- **AMD-CAND-2/3 → Nick's word** (now measured-backed): the color scope (white/CT for V1; the §3.10-vs-§3.6 internal-inconsistency fix) + Kelvin-canonical-at-ingestion. Apply the staged verbatim diffs on ruling.
- **D2 → co-sign + fold:** `INV-ES-09`, watermark-neutral (lead with the decision-record's line-44 authorization), the FSM+guard text edit, the AIOT-INV-1 citation fix (drop the stale "M7.4b"), 170→171.
- **E2 → Doc 08 §3.3 currency correction** (bundle with the governance pass): auto-detect keys on VID:PID + the probe sequence, never descriptor strings.
- **The A4.5 Doc 15 §3.4/§6 note** (backup-key seam + rotate-on-restore) still awaits Nick.

## 4. The forward work (post-ratify)

- **Core critical path:** **M9** (real Zigbee — authored against the **measured bench corpus** as its acceptance spec + the ratified AMD-CAND-1 confirmation slot + the new measured requirements E1/E2/E4/E5; **small per D5**; use the grounding-subagent-before-authoring discipline — author against measured ground, not inference) → **72h-stable soak** (the bench is frozen and ready; a wall-clock floor) → **install-smoke green on HEAD**. The **hardware-grounded-E2E seam** (real-capture→replay as a CI gate extending M7.4d) is now realizable — the fixtures exist; decide when to build it.
- **The lanes (parallel, write-isolated):** the **frontend** (FE-1 live-integration against running Core — the mid-Aug gate; then the **v2 plan revision** folding the remaining gaps [mobile-first hero, PWA, list-virtualization + the E5 rendering semantics — CT confirms slowly ~7–8 s, idempotent commands may never visually confirm without readback, effect/identify render UNCONFIRMED immediately]; then the **website build** [Astro+Preact per D-FE-2]); the **bench** (Z2M `exposes` cross-check = D5's 2nd oracle; the Phase-2 zigpy/bellows harness, cross-validated vs today's ZHA captures; **Wave-2 devices** as Nick adds them — grow the corpus + fixtures); skills (the `nexsys-frontend` carry commit; a future `nexsys-bench` skill once the pattern is proven).
- **Residuals (carry):** the OR-GATE-M7.4 + AB-4 + frontend-gate CI-green Actions glance; the Dependabot 1-critical shipped-vs-dev triage; the D2 + AMD-CAND-1 ratify→fold; AMD-CAND-2/3 (Nick's word); the A4.5 Doc 15 note; **beat 41 commit + the deferred spine rotation** (once the phantom is quiescent); the `nexsys-frontend` skill commit + the Check-9 mirror sync.
- **The running loop:** keep the **mid-August go/no-go** on the calendar (~Aug 16, ~6.5 weeks). The 4 gates: engine LIVE ✅; hero-on-real-data (read-API ✅ + frontend go-live-ready + the bench real-data substrate — **much closer**); hardware-validating (M9 small + nearly unblocked → 72h); install-proven (install-smoke green). **Both v12 pacing risks (frontend velocity, M9 size) are retired for Wave-1** — the honest read is materially improved.

## 5. The disciplines you enforce (carry from v12)

Env-model (host tools authoritative; **verify phantom quiescence host-side before any mass-rewrite/rotation** — it comes and goes; `git commit -F` from `_scratch/` for `!`/inner-`"`/backtick messages; clear stranded index.locks; prefer `git --no-optional-locks` for read-only state). Truth-hierarchy / pointer-not-copy (code > locked-docs > hivemind; **re-derive every count/HEAD**; a claimed gate verified actually-green on the PUSHED commit — Nick's terminal/Actions). Freshness preflight at start. **WUCP Phase 2 at every build return — source-verify the mechanical claims against the git DIFF + the green gate, never the completion report** (the v11/v12 lesson — the one this whole session re-proved). **The P2 consumer/pin fan-out** on any ctor/enum/registry/record-arity/event-type change. **CI is the gate of record** (now core `check` + `frontend.yml`). **The FIVE-repo sync + SINGLE-SPINE-WRITER:** core / docs / hivemind / skills / bench; YOU are the SOLE spine-writer; every worker stream (Coder, bench, frontend, research) write-isolated + reports returns to you; commit + push all touched repos before launching any session; route every return through this hub. **Build non-precluding of the upscale / cloud / B2B / AIoT seams** (Nick's day-one directive — hold it in view for M9, the corpus model, and the frontend v2). **And the v13 addition: stay an independent verifier — re-derive, challenge, and object; do not let a confident narrative substitute for a source check.**

## 6. Operating cadence / horizon

A standing session — operate until V1 ships or you hand off to a fresh v14 hub re-grounded from the spine. **Success:** the queue ratified (AMD-CAND-1/2/3 + D2 + E2 folded, cleanly re-verified); M9 authored + built + GREEN against the measured bench corpus (the hero on real silicon); 72h validation run; the frontend rendering the hero on the live backend; the website built; the hardware-grounded-E2E seam realized; install proven; the mid-Aug go/no-go taken honestly on measured ground; and **at least one genuinely new avenue surfaced by the independent-verifier posture that the v12 arc did not see.** If this conversation saturates, author the v14 hub prompt + hand off (capture-then-execute, never execute-then-lose-it).
