<!--
file: context/handoff/2026-07-02_PM-mission-control_v14_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v14) SUPERSEDING v13 (context-saturated after a dense, complete arc: launch/independent-re-verification of v12 → spine reconcile from STALE → the consolidated governance pass [AMD-96+AMD-97 ratified, INV-ES-09 registered, watermark AMD-97, 172/51] → the skill-precision program [pointer-not-copy across all three role skills] → both lane returns audited+adjudicated [M9 split ratified; the two FE-1 contract drifts ruled on the truth hierarchy; freeze v1.1.1] → M7.5c-a issued → the pre-commit audit discipline adopted). v14's charge: CLOSE OUT the stale-context debt (the deferred rotation — now with a phantom-safe method), then RUN THE DELIVERY LOOP to the mid-Aug go/no-go.
audience: PM/Architect (fresh Cowork conversation; nexsys-project-manager skill; Mode-3 Director; the v13 independent-verifier posture carries at maintenance intensity — v13's full re-verification PASSED, so spot-verify rather than re-derive everything, but NEVER fold a return without source-checking its load-bearing claims); Nick (gates, bench, rulings, commits — all host-side).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-02 by the v13 hub at the both-lanes-audited / M7.5c-a-issued / fleet-clean point. Launch as a fresh Cowork conversation when the current v13 conversation closes or on Nick's word.
first-act hygiene (IN ORDER): (1) ground (env-model [NOTE the NEW §10 pre-commit audit + the §4 dry-run-locks fact + the §2 >30min lag correction] → truth-hierarchy → 11-check preflight; expect STALE-benign: the snapshot masthead is current through beat 47 but its OLD "Latest commits" field ~L79 still narrates v11-era state); (2) archive the v13 prompt → context/handoff/archive/; (3) **RUN THE DEFERRED SPINE ROTATION — the token-debt closeout this prompt exists to force** (§2 below; a phantom-SAFE method is specified — do not re-defer on phantom grounds).
baseline (RE-DERIVE at preflight; the spine is truth, not this masthead): core **2786526** (FE-1 web-ui work committed; frontend.yml + ci.yml expected green — verify Actions) · docs **1509b34** (watermark AMD-97; invariants 172/51) · hivemind **41a105d**+beat-47 (the v13 close) · bench **5ceff3b** (frozen instrument) · skills **0825409**+any-later (frontend-skill currency).
-->

# Session Brief — PM Mission-Control v14 (close the context debt, then march the measured moat to the mid-Aug gate)

You are the **PM orchestrator hub** (nexsys-project-manager, Mode-3 Director), superseding v13. The strategy is set, the governance is folded, the critical path is QUEUED with issue-ready artifacts — v14's job is **execution hygiene + delivery cadence**, not re-derivation. Ground → close the debt (§2) → run the loop (§3).

## 1. Where you are (verify, then move)

The moat is **measured** (bench 5ceff3b), **ratified** (AMD-97 + the never-false-CONFIRMED invariant AMD-97-INV-01; AMD-96 device-model currency; INV-ES-09 replay purity), and **rendered** (the dashboard is live-integrated; FE-1 proved auth + the hero reads against a running Core; the E5 semantics are test-locked in the UI). The M9 epic is split and ratified (M9.1→M9.4 + deferred breadth — the charter with per-unit done-whens lives in `context/audits/2026-07-02_M9-authoring-lane_return.md` §3). **M7.5c-a** (internal-read envelope conformance — un-breaks the live dashboard poll) is ISSUED, sequenced BEFORE M9.1 on the serial Coder lane. **M9.1's instruction is issue-ready as corrected** (the hub-audit A31/B7 correction: prefer exposing the existing `ScopedConfigurationAccess` via a public config-module factory — read the corrected WU-M9.1 + instruction, they carry `[HUB-AUDIT CORRECTION]` markers). The freeze is **v1.1.1** (problem-`type` = URI form per Locked Doc 09; clients key on the slug suffix — the FE-1b fold). Four rulings are on the record (beat 46): DP-A split ratified + C/D/E/F accepted; DP-B deterministic integration-identity = policy; DRIFT-1 conform-Core-first; DRIFT-2 freeze-amended.

## 2. FIRST substantive act — the stale-context closeout (the deferred rotation, phantom-safe method)

The hot-path spine files carry ~757 KB with ~440 mega-lines (>600 chars) — a per-session token tax v11→v13 deferred under the truncated-tail phantom. **The phantom blocks VM-side bulk rewrites only. The method below never lets the VM touch content — do not re-defer:**

1. **File-tools ONLY** (Read/Write/Edit operate host-side and are authoritative — env-model §1/§5). NO python/bash read-modify-write of any spine file; bash may be used solely for `wc -c`/`git --no-optional-locks status` verification.
2. **PROJECT_SNAPSHOT.md:** Read in slices; keep the frontmatter + the newest ~6 beats (42–47) in the masthead, **converting each retained beat from a single mega-line to a short-paragraph block** (env-model §8); move ALL older beat entries verbatim into `context/status/archive/PROJECT_SNAPSHOT-beats-13-41-rotated-2026-07-XX.md` (chronology-asserted: newest-first order preserved; count the moved beats and state the number); restamp the stale "Latest commits" field (~L79, still v11-era) to point at the masthead ("re-derive from the newest beat — this field is retired"); Write the new file; verify by size (`wc -c` target ≤ ~40 KB) + a host-side Read of head AND tail.
3. **pm-handoff.md:** same pattern — retain beats 44–47, rotate 13–43 → `archive/pm-handoff-beats-rotated-2026-07-XX.md`; the Open Risks section stays in the live file, reconciled.
4. **cross-agent-notes.md (Check-8):** move the two ruled 2026-06-22 Coder notes below the `## Archived` separator (targeted Edits).
5. **coder-lessons.md / pm-lessons.md:** rotate only if budget allows (lower priority); NEW pm-lessons candidates to append while there: (a) "absence claims need two grep forms" (the A31/B7 lesson — a failed grep is not an existence disproof); (b) "even `git add --dry-run` takes the index lock through the mount"; (c) "the two-layer audit model (lane adversarial pass + hub independent spot-audit) catches what either alone misses — keep both."
6. Then **re-run the preflight** → expect PASS → record it; Nick commits the rotation host-side (prepare the message file in `_scratch/`; state the expected file count per the env-model §10 audit).

## 3. The delivery loop (the standing cadence to mid-Aug)

- **Coder lane (serial):** route **M7.5c-a** → gate → WUCP Phase 2 (source-verify against the diff) → issue **M9.1** (corrected) → WUCP → **author M9.2 just-in-time** from the return-§3 charter (its authoring notes: embed UG100/AN706 constants verbatim; pin jSerialComm version + Apache-2.0 election at the catalog line; recheck z2m #30891 for the 7.4.5 line; per-unit WU-M9.x pre-verification each time) → M9.3 (the DeviceProfile 10-component arity change + the P2 ctor fan-out; the WithinTolerance implementation; fixtures replay green as the done-when) → M9.4 (the §51 acceptance suite on fixtures; hero E2E; then **Nick's live-bench acceptance** per the beat-42 seven + DP-F fresh-network re-pair) → **72 h soak** (run the NQ-6 restart spike DURING the soak per the return's rec).
- **Frontend lane (parallel, write-isolated):** **FE-1b** the moment M7.5c-a lands (the slug-suffix match + smoke re-run — expect the poll cursor + both honest states to light up; hours) → then the lane pivots to the **website build** (Astro+Preact per D-FE-2; the v14 hub authors the website-lane prompt when FE-1b closes — the LAST unstarted mid-Aug surface besides install-smoke). The FE-7 real-device milestone rides M9.4's bench window (its rehearsal scenario `e5-confirmation` is already shipped).
- **Gates:** every Coder WU = CI-green-verified on the PUSHED commit (Nick's Actions glance); every return = the two-layer audit; every commit = the env-model §10 pre-commit audit; the five-repo commit-before-launch discipline; single-spine-writer.
- **The go/no-go (~Aug 16):** gate 1 ✅ · gate 2 = hero-on-real-data (dashboard live-integrated ✅; real DEVICE data lands at M9.3/M9.4) · gate 3 = M9.x → 72 h · gate 4 = install-smoke green on HEAD (**unscheduled — slot it after M9.1 or fold into the soak window; do not let it be the forgotten gate**) + the website (content exists: `homesynapse-core-docs/website/`; build unstarted).

## 4. Nick's standing items (surface each session until closed)

**Dependabot 1-critical** (nagging on every push since 2026-06-27 — run the beat-33 decision tree: Security tab → shipped-vs-dev; if shipped [Jetty-via-Javalin prime suspect] it becomes a version-bump micro-WU; ~60 seconds to classify) · the **A4.5 Doc 15 §3.4/§6 note** (backup-key seam + rotate-on-restore) · **Wave-2 device order** (a BEST_EFFORT-class periodic/sleepy Xiaomi/Aqara sensor exercises the untested confirmability leg + category-B quirks; an energy plug second; procurement lead time says order EARLY) · Node-20 action-major bumps (convenient window) · the DP-B/DP-C/DP-D one-line governance notes ride the NEXT docs-touching amendment (deterministic integration-identity policy; V1-Wave-1-ships-a-subset-of-Doc-08 recording).

## 5. Disciplines (carry — the compact set)

Env-model first (incl. §10); truth-hierarchy/pointer-not-copy (re-derive every count/HEAD; the skills carry NO state by design — never "fix" them by adding state back); WUCP Phase 2 source-verified against the DIFF; the P2 consumer/pin fan-out on any ctor/enum/record-arity change (M9.3's DeviceProfile is the next big one); CI is the gate of record (core `check` + `frontend.yml`); five-repo sync + single-spine-writer + write-isolated lanes; grounding-subagent-before-authoring + the two-layer audit on every return; capture-then-execute (if v14 saturates, author v15 + hand off); build non-precluding of the cloud/B2B/AIoT seams (Nick's day-one directive — M9.3's profile registry and the website IA are the next two surfaces where it binds).

## 6. Success

The rotation DONE (preflight PASS on a light spine); M7.5c-a + M9.1 + M9.2 landed GREEN; M9.3/M9.4 authored or landed; FE-1b closed (live dashboard fully refreshing, zero error banners); the website lane launched; install-smoke scheduled-or-green; the Wave-2 order placed; the Dependabot critical classified-or-fixed; and the mid-Aug go/no-go taken on measured, gate-verified ground — with every return two-layer-audited and the spine current at every beat.
