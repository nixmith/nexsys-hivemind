<!--
file: context/handoff/2026-07-03_Doc18_independent-DOCS-review_session_prompt.md
purpose: Dispatch-ready brief for a FRESH, CONTEXT-ISOLATED Cowork session that runs the INDEPENDENT DOCS-Project second-opinion review of the Doc 18 "Extension & Plugin Architecture" DRAFT before Lock — the Doc 15/16/17 pre-Lock discipline. Distinct value here: independent verification of CHARTER FIDELITY (including two deliberate charter-citation corrections the author made — adjudicate them from SOURCE, not from either party's claim), the EXT-INV-1/2 minting-test verdicts, source-resolution of every cited id AND every core-source claim, and rulings-fidelity of the four SETTLED decisions against the beat-57 record. Review ONLY — do not fold, do not edit the doc.
audience: a FRESH Cowork conversation acting as an independent DOCS reviewer (nexsys-project-manager skill, Mode-1 review discipline); Nick (co-sign + Lock after the hub folds).
state-type: session prompt (independent design-doc review).
status: READY — authored 2026-07-03 by the v16 hub. OFF the M9.x critical path only until M9.3 wants issuing (Doc 18 must Lock first) — run promptly, as its OWN fresh conversation.
baseline: docs `1509b34` + a DIRTY working tree that is NOT yours to touch: (a) the 26-path website set (another lane's pending Commit A — website/ only), (b) the untracked DOCUMENT UNDER REVIEW at `design/18-extension-and-plugin-architecture.md`. Pre-authorized baseline-shift: if Commit A lands mid-session, the docs delta is website/-only — disjoint from design/ and from every read in this brief; re-verify nothing, note it in the return. Hivemind baseline: the newest PROJECT_SNAPSHOT masthead beat. Confirm both at a light preflight.
known hazards (env-model, read it FIRST): truncated-tail mount phantom (host file tools are truth — never "fix" from the VM view) · VM git takes index locks — read-only state checks via `git --no-optional-locks status --porcelain` / `git log` ONLY; NEVER `git add` (including `--dry-run`) · interactive question tools can fail mid-stream — veto-or-default and record the default · anchor hygiene §5 if you script anything (you should not need to write anything but your return).
write-isolation: your ENTIRE write surface is ONE new file — `nexsys-hivemind/context/audits/2026-07-03_Doc18_independent_DOCS_Review_Return.md`. ZERO docs-repo writes, zero edits to the doc under review, zero shared-file appends (the hub folds your return; cross-agent traffic is the hub's).
reads (in order):
  - nexsys-hivemind/context/process/cowork-environment-model.md (FIRST)
  - nexsys-hivemind/context/status/PROJECT_SNAPSHOT.md (newest masthead beat — grounding only)
  - homesynapse-core-docs/design/18-extension-and-plugin-architecture.md (THE DOCUMENT UNDER REVIEW — in full)
  - nexsys-hivemind/context/planning/2026-07-02_Doc-18_requirements-charter.md (the authoring authority — pressure-test fidelity to it, AND its own citations against source)
  - nexsys-hivemind/context/assessments/2026-07-02_plugin-ecosystem-wars_research-dossier.md (§3 lessons ledger L-1..L-33 + spot-follow any L-n the doc leans hardest on: L-3, L-11, L-12, L-16, L-24, L-29, L-30)
  - nexsys-hivemind/context/assessments/2026-07-02_extensibility-and-plugin-ecosystem_PM-assessment.md (§2 gaps, §3 watchlist, R-1..R-5)
  - homesynapse-core-docs/design/17-aiot-and-cloud-readiness.md (the mold + the AIOT-INV minting precedent + §7-6 claim-language boundary)
  - homesynapse-core-docs/design/16-superior-automation.md §3.2/OQ2 (component model, AX-7 ownership) · design/05-integration-runtime.md (the SPI; §3.7) — targeted
  - homesynapse-core-docs/governance/Architecture_Invariants_v1.md (§17 index + EVERY id the doc cites; independently scan for relevant UNCITED invariants) · governance/HomeSynapse_Core_Locked_Decisions.md (LTD-16 + LTD-17 verbatim) · governance/DESIGN_DOC_TEMPLATE.md (readiness-doc conditional-section compliance, judged against the Doc-17 precedent)
  - nexsys-hivemind/context/handoff/pm-handoff.md — the beat-57 rulings record ONLY (DP-B, DP-18-A/B/C verbatim rationale; rulings-fidelity check) + nexsys-hivemind/context/process/decision-rationale-index.md
  - the PM skill's references/review-and-quality.md §1 + references/constraint-enforcement.md
-->

# Session Brief — Independent DOCS-Project Review of the Doc 18 "Extension & Plugin Architecture" DRAFT (pre-Lock)

You are a **fresh, independent DOCS reviewer** for Doc 18 — a DRAFT builds-nothing seam-reservation doc submitted for the project's standard pre-Lock independent review (the Doc 15/16/17 discipline). Read the env-model FIRST, run a light preflight (docs HEAD `1509b34`-or-CommitA-landed; the DRAFT on disk; the dirty-tree facts in the masthead above), then review. **Review only. You edit nothing. Your single artifact is the return file.**

## What is already ruled (do not re-litigate — verify fidelity instead)

Unlike the Doc-17 review, the scope question is settled: Nick authorized Doc 18 as a new doc (assessment R-A, ruled beat 53→57), and four decisions are RULED with recorded rationale (beat 57): **DP-B** (deterministic identity permanent), **DP-18-A** (AX-7 direction: semver + compat range + forward-only migration + named deprecation floor), **DP-18-B** (wave-1 curated IN_JVM; RESERVED_SUBPROCESS the intended non-curated rung; honesty rule as doc text), **DP-18-C(a)** (community content never paywalled; monetize trust, not access). Your job on these is **fidelity**: does the doc restate each ruling accurately, at full strength, without drift or silent extension? Quote any divergence. (`working-with-nick.md`: recorded rulings are defended, not re-litigated — but a MIS-recorded ruling is exactly what you exist to catch.)

## Your charge — five parts

1. **Charter fidelity + the two corrections (the headline).** The doc was authored against the charter's section map and content requirements. Verify section-by-section coverage (charter §1 table → doc §§0–7; charter §3 seven seams → doc §4 rows; charter §5 → doc §5). **Then adjudicate the two deliberate corrections the author made to charter citations** (flagged in doc §6's italic note): (a) EXT-INV-1's static-composition parent — charter said "DECIDE-04/LTD-16," the doc cites **LTD-17**; (b) EXT-INV-2's parent — charter said "AMD-56 error-code registry discipline," the doc cites the descriptor freeze + **AMD-55-INV-01**. Read LTD-16, LTD-17, and `design/amendments/AMD-55_*.md`/`AMD-56_*.md` yourself and rule each correction RIGHT or WRONG from source. Do not accept either party's claim.
2. **Assess the EXT-INV-1/2 candidates (§6) under the register minting test** (adds a constraint the cited parents do not impose; cites those parents; CI-testable): mint-at-Lock vs keep-as-principle, per candidate. Also rule the three kept-as-principles: is any of them actually mintable now (or any minted candidate actually a near-duplicate — the INV-GA-02 warning)?
3. **Source-verify every citation.** (a) Every invariant/LTD/AMD id the doc cites resolves in the governance registers and says what the doc claims (the Research-6 fabricated-id lesson); tabulate each: VERIFIED / MIS-CITED / ABSENT. (b) Every core-source claim resolves in `homesynapse-core` (read-only): `IntegrationFactory`/`IntegrationAdapter` as the two third-party-implemented types; `IntegrationDescriptor` fields (requiredServices, dataPaths, configSchemaMajor/Minor, isolationLevel); `IsolationLevel.RESERVED_SUBPROCESS`; `InvokeIntegrationAction` reserved-empty; `SchemaRegistry.registerIntegrationSchema` built-but-unwired; unscoped `EntityRegistry`/`StateQueryService` injection; full `EventPublisher` injection. (c) Spot-follow ≥5 L-n citations into the dossier and confirm the doc's use matches the lesson's actual content.
4. **The genuinely-non-precluding test, per §4 row.** Does any "reserved" seam smuggle in a design decision that should be its own doc or milestone (especially rows 1 [the LTD-17 amendment shape — proposed, not assumed?], 6 [does the artifact/manifest shape over-specify?], 7 [is the floor a reservation or a covert marketplace spec?])? Is every RESTATES row correctly tagged with its owning doc (no re-reservation drift — the Doc-17 S1 lesson)?
5. **M9.3-binding coherence (§3.5) + template compliance.** Is §3.5 (a)–(d) precise enough to constrain the M9.3 profile registry (namespaced identity keying, fingerprint-priority, Zigbee-scoped-with-general-seam-note) without over-constraining M9.3's own design freedom? Are the OQ marks right (OQ-1 BLOCKING-for-Lock with the N=6 proposal evidence-checked against L-11/HA ADR-0021; OQ-2/3 NON-BLOCKING)? Template: judge conditional-section exclusions against the Doc-17 readiness-doc precedent, and the honesty rule's presence as doc text (§2 P3, §3.3, §4 rows 4/7).

## Anti-requirements to verify the doc holds

Community content never paywalled stated as a CONSTRAINT (not aspiration) · no sandbox/isolation claim beyond what is built (the honesty rule as text) · builds NOTHING (no section specifies buildable scope) · the frozen SPI restated, never redesigned · static composition remains the V1 truth (the amendment proposed, not assumed) · claim language inside Doc 17 §7-6's counsel-gated boundary (no "safest," no "can never," no superlatives).

## Done-when

An **independent review return** on disk at `nexsys-hivemind/context/audits/2026-07-03_Doc18_independent_DOCS_Review_Return.md` with: **(1)** charter-fidelity verdict + the two corrections each ruled RIGHT/WRONG from source; **(2)** EXT-INV-1/2 verdicts (mint-at-Lock vs keep-as-principle) + the three principles re-checked; **(3)** the full citation table — every id and core-source claim: VERIFIED / MIS-CITED / ABSENT — plus any relevant UNCITED invariant found in the §17 scan; **(4)** rulings-fidelity result for the four SETTLED decisions (quote any drift); **(5)** per-row non-preclusion verdicts; **(6)** document verdict — **RATIFY-TO-LOCK / RATIFY-WITH-EDITS / REVISE** — with a consolidated, individually-actionable edit list (each edit: location anchor + exact proposed text + reason + severity BLOCKING/NON-BLOCKING). **Do NOT fold edits into the doc** (review-separate-from-fold; the hub folds after its two-layer audit of your return). Env-model §10 pre-commit audit in the return: your session created exactly **1 new file** (the return); the prepared host-side command stages exactly that 1 path (`git add context/audits/2026-07-03_Doc18_independent_DOCS_Review_Return.md`), message staged bang-free/backtick-free in `ClaudeFolder/_scratch/` for `git commit -F`. If your porcelain shows anything else attributable to you, STOP and say so in the return.
