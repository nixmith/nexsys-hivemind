<!--
file: context/handoff/2026-07-03_PM-mission-control_v16_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v16) SUPERSEDING v15 (context-saturated after a dense, complete arc: launch hygiene + preflight → M9.2 authored → DP-B + DP-18-A/B/C all ruled → website lane dispatched → M9.2's full double-arc [CC delivery → 2 gate rounds hub-fixed (the checked-PIE 3-file sweep; the substring-replace recursion) → R1–R7 ruled → post-green two-reviewer hardening H1–H5 → LANDED core 6ea6912] → the skills folds → the website-lane audit [ACCEPT; the dist refutation; the license publish-gate] → the cross-agent splice repair → the zero-JS gate Windows fix). v16's charge: DOC 18 AUTHORED → REVIEWED → LOCKED BEFORE M9.3 (the docs pass rides the Lock) + the dossier-content lane + M9.3 JIT on Lock — the march to the mid-Aug go/no-go continues with gate 2 nearly complete and gate 4 down to content + hosting.
audience: PM/Architect (fresh Cowork conversation; nexsys-project-manager skill; Mode-3 Director — Mode 1 for Doc 18; two-layer audit on EVERY return, INCLUDING lane observations that route actions — the FE-1b dist lesson); Nick (gates, bench, rulings, commits — all host-side).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-03 by the v15 hub at beat 60. Launch as a fresh Cowork conversation on Nick's word.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — porcelain + both spine frontmatters BEFORE any spine write; a failed edit anchor mid-batch = prior-writer signature: HALT). Expect the KNOWN truncated-tail mount phantom on recently-edited hivemind files (env-model §2 — host file tools are truth; do NOT "fix" from the VM view). (1) Ground (env-model [§5 anchor hygiene — THREE incidents on 2026-07-03 alone: line-anchored headings, replace_all substring collision, read-back-after-bulk; §10 audit; §11 restart] → **the cold-boot trio: `context/process/working-with-nick.md` (the operator contract) + `context/process/infrastructure-map.md` + `context/process/decision-rationale-index.md` (defend rulings, don't re-litigate them)** → truth-hierarchy → 11-check preflight; expect PASS-or-STALE-benign; **Check 9 may be STALE (PM + coder mirrors) if Nick's sync hasn't run — his ledger item, not CONFLICTED**). (2) Archive the v15 prompt → context/handoff/archive/. (3) Check-8: the WEBSITE-LANE note is ACTIVE — archive it once Nick's residuals (docs Commit A) are confirmed landed; the M9.2 note is already served+archived (beat 59 splice repair — the frontmatter deliberately no longer quotes the separator string; keep it that way).
baseline (RE-DERIVE at preflight; the spine is truth, not this masthead): core **6ea6912** (M9.2 LANDED — the transport substrate on main; 39 files; clean-tree check GREEN ×2) · docs **1509b34 + Commit A pending-or-landed** (the 26-path website set incl. the zero-JS-gate Windows fix; verify which) · hivemind **79659f6**+beat-60 · bench **5ceff3b** (frozen instrument) · skills **ae434ca**. Watermark AMD-97, invariants 172/51. Zero unresolved deferred build gates.
-->

# Session Brief — PM Mission-Control v16 (lock the ecosystem's constitution; then put real devices on the spine)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director, entering **Mode 1 for Doc 18**), superseding v15. The transport substrate is on main and hardened; every ruling Doc 18 needs is banked; the website skeleton exists in the truthful posture. v16 converts the banked rulings into the **Locked extension constitution** before M9.3 freezes the surfaces it governs. Ground → first acts → run the loop.

## 1. Where you are (verify, then move)

M9.2 is LANDED (`6ea6912`): SerialByteChannel → ASH codec/session → EzspAshTransport → EzspCoordinatorProtocol → probe/port-identity/watchdog → §3.13 formation — AN706-derived, double-derivation-verified, wraparound-and-width tested (hardening H1–H5), all package-private, zero mint. The M9.1 spine + M9.2 substrate await M9.3 (interview/ingestion/profile registry) and M9.4 (adapter wiring + confirmation + hero E2E on the bench MG24 @ 7.4.5.0/v13). **The checked-PIE seam rule and the classifier-wrap trap are M9.4-binding** (zigbee MODULE_CONTEXT gotchas — permanent failures propagate BARE via the checked seams; `ExceptionClassifier` is bare-`instanceof` BY DESIGN). The website skeleton is built (Astro @ docs `website/site/`, 6 pages, zero-JS enforced, the R-3 developer page with its 13-row claim→truth table); **gate 4's residue = dossier CONTENT (W-5/PG-1) + hosting/deploy (Nick's)**. **Doc 18 authorities, all FINAL:** the charter (`context/planning/2026-07-02_Doc-18_requirements-charter.md` — THE authoring authority) · the dossier (L-1..L-33) · the assessment (D1–D4, R-1..R-4) · **beat-57 rulings: DP-B CONFIRMED permanent · DP-18-A RATIFIED (semver + compat range + forward-only migration + named deprecation floor) · DP-18-B RATIFIED (wave-1 curated IN_JVM; RESERVED_SUBPROCESS = the intended non-curated rung; the no-sandbox-claims honesty rule as doc text) · DP-18-C RULED (a) (community content NEVER paywalled — the principle is ruled TODAY; Doc 18 states it as a CONSTRAINT; the Connect reword rides Doc 18's docs pass, Nick's refinement (i))**. Lane routing stands: backend M-WUs → Claude Code (**verify the allow-list fix BEFORE dispatching M9.3 — denied twice, cost a gate round each time**; `"Bash(./gradlew:*)"` in core `.claude/settings.json`); frontend/research → Cowork lanes; the hub authors/audits/records, single spine-writer.

## 2. First substantive acts (after hygiene; order matters)

1. **Author Doc 18 — Extension & Plugin Architecture (Mode 1)** against the charter: Doc-17 mold, all 13 template sections substantive; **builds NOTHING** (a seam-reservation constitution); EXT-INV-1/2 taken through the register minting test; the seven seams as §4 reservation rows; the four rulings folded as SETTLED decisions (cite beat-57 + the pm-handoff rationale record); the isolation ladder with the honesty rule as doc text; §3.5 namespace/identity governance (publisher-scoped two-level identity; the collision ruling [HA-shape: shadow-with-consent + UI flag, core wins brand]; fingerprint-priority profile disambiguation) — **these bind M9.3, which is why the Lock precedes it**; the explicit what-Doc-18-does-NOT-attempt section. Then: **DOCS second-opinion review lane** (fresh conversation, review-only) → fold → **Nick co-sign → LOCK BEFORE M9.3 issues**.
2. **The Lock's docs pass carries the whole queue in ONE watermark advance:** the DP-B/DP-C/DP-D one-liners · the A4.5 Doc-15 §3.4/§6 backup-key note · the strategic-context-map currency refresh (the standing Check-10 debt) · the Connect-line reword (DP-18-C(i) — strategy + governance coherent in one commit) · the R4 (>v14 band-edge policy) + R5 (§3.13 resume-mismatch rule) Doc-08 currency notes · the Doc-08 W6 touch-up (jSerialComm ships a real module descriptor) · the D-4 R15 strategy-file fold gap (Six_Battlefields).
3. **Author the dossier-content lane prompt** (fresh Cowork, nexsys-frontend skill; parallel-safe vs everything): the three stubs (`explainability` · `no-cloud-account` · `ledger-gap`) to config-superiority reviewer grade — the W-5/PG-1 binding gate-4 content gap. Inputs: the preserved in-file Increment-2 planning blocks + their receipt obligations; the claim→truth table discipline inherited from the website lane's return (§4 shape); competitor specifics allowed WITH receipts (the C2 register direction); publish gates honored (no LICENSE claim before the flip, W-5 noindex stands).
4. **M9.3 JIT authoring on Doc-18 Lock** (CC lane; grounding-subagent-before-authoring; pre-verification pins BOTH graph knobs): interview pipeline (§3.4–§3.7) + attribute ingestion + `DeviceProfileRegistry` impl + adoption (identity now UNGATED — DP-B permanent). Inheritances (all banked in the zigbee MODULE_CONTEXT + backlog): the §3.5 namespace convention + third-party-profile data channel as DESIGN CONSTRAINTS · profile-registry non-preclusion (assessment R-2) · DeviceProfile 10-component arity → run the P2 ctor fan-out sweep · WithinTolerance · `drainPendingCallbacks()` as the ingestion feed **+ the bound-and-drain obligation** · the checked-PIE seam rule · the `adapter_type`/config-key binding + schema registration · **queue for Nick in this window: the classifier cause-walk-for-PIE-only hardening candidate + hashed-TCLK (both M9.4-adjacent)**.

## 3. The delivery loop (standing cadence to the mid-Aug gate, ~Aug 16)

- **Hub serial path:** Doc 18 → review → Lock + docs pass → M9.3 authored/dispatched (CC) → gate → WUCP P2 vs the DIFF → **M9.4 JIT** (adapter wiring + ReopenAction ordering [close→reopen→resetSession→startSession] + NETWORK_UP await + confirmation acceptance + hero E2E; the §51 acceptance suite on fixtures) → **Nick's live-bench acceptance** (the beat-42 seven + DP-F fresh-network re-pair + the FE-1b finding-(a) re-pair/token story; **FE-7 rides this window**) → **72 h soak** (NQ-6 restart spike DURING).
- **Frontend lane (parallel, write-isolated):** dossier content build-out per §2.3 → website.yml CI (once Nick rules adopt-now) → FE T2 backlog (ExplainHub zero-state, hero differentiation, mobile-first) rides after.
- **Hardware (Nick + bench):** Wave-2 arrival → the hub produces **Nick's unboxing runbook** FROM the bench sources (bench-bringup guide + corpus model + device-model research return) — inventory/label first; **bench characterization BEFORE any production pairing**; the BEST_EFFORT sleepy sensor is priority (the untested confirmability leg); the MG24 stays untouched (frozen instrument); ZBDongle-P waits for its second-transport slot (and NOTE: two same-VID:PID sticks trip the PortLocator limitation — the probe-fingerprint disambiguation is M9.4-pinned).
- **Gates:** CI green on the PUSHED commit · two-layer audit on every return INCLUDING observations that route actions · env-model §10 selective staging on shared trees · single-spine-writer · restart-resume on every resume · capture-then-execute (author v17 at depth — do not die mid-arc).
- **Go/no-go board:** gate 1 ✅ · gate 2 = dashboard ✅ + real device data (lands M9.3/M9.4) · gate 3 = M9.3 → M9.4 → soak · gate 4 = install-smoke ✅ continuous + website skeleton ✅ + **dossier content (the §2.3 lane) + hosting/deploy (Nick)**.

## 4. Nick's standing ledger (surface each session until closed)

**ci.yml + install-smoke glance on core `6ea6912`** · **docs Commit A** (exactly 26 paths incl. the fixed zero-JS script — expect "zero-JS gate OK" on the re-run first) · **skill mirror sync** (Check 9 STALE: project-manager + coder, from beat 58) · **Dependabot CORE: 5 enumerated (1 critical, 1 high, 3 moderate) — COMPOUNDING since 06-27; the 60-second Security-tab triage; M9.2 added no npm surface (jSerialComm is Maven), so candidates remain `web-ui/dashboard/package-lock.json` [→ frontend micro-WU] or actions bumps** · **site npm audit: 2 (1 high) in the BUILD tree** — zero-JS site ships no runtime JS, so exposure ≈ build-time only; `npm audit` names it; bump at leisure or fold into the dossier lane's window · `npx astro telemetry disable` (one-time host hygiene; build-time telemetry, consistent with the no-tracking posture) · **CC allow-list fix BEFORE M9.3** (`"Bash(./gradlew:*)"`, core `.claude/settings.json`, restart the CC session after) · **website.yml ruling** (adopt-now recommended; ~20 lines, docs repo) · **LICENSE-flip timing** (blocks developer-page PUBLISH only; strategy-execution act) · **hashed-TCLK election** (M9.4 window; bench evidence should inform) · Wave-2 arrival watch · Node-20 action-majors (convenient window) · the frontend.yml glance residual (cf3733e-era).

## 5. Disciplines (carry — the compact set)

Env-model first (§2 truncated-tail phantom [trust host tools]; §4 index-lock/`--no-optional-locks`/never-`add --dry-run`; **§5 anchor hygiene — the 2026-07-03 triple: match headings LINE-ANCHORED, never find-first; replace_all is SUBSTRING matching [indentation prefixes collide with deeper indents]; verify bulk edits by READING the region, with checks that distinguish the worlds they claim to distinguish**; §9 esbuild-native SIGSEGV + the /tmp-md5-copy+wasm-shim pattern [lane green ≠ gate of record]; §10 pre-commit audit + exact counts; §11 restart-resume). Truth-hierarchy/pointer-not-copy (re-derive every count/HEAD; skills carry NO state). Two-layer audit on every return — **deliverable claims AND observations that route actions** (the FE-1b dist finding survived one audit as a false premise). Grounding-subagent-before-authoring. WUCP P2 source-verified against the DIFF. **Checked-exception seam maps in every instruction that mandates a checked type** (format addition 7; the classifier consequence stated). **Wire constants = double-derivation** (pre-write derive + post-write live-source residual check — the 0x17 catch). Stub-tripwire arithmetic excludes instruction-anticipated stubs (addition 8). Module-info pins validated against every mandated behavior (addition 9). P2 consumer/pin fan-out (M9.3's DeviceProfile ctor is the big one). Lane routing per §1; write-isolation + selective staging; build non-precluding (Doc 18 IS the named authority for the plugin seam class once Locked).

## 6. Success

Doc 18 authored → DOCS-reviewed → Nick co-signed → **LOCKED before M9.3**, with the docs pass landed in one watermark advance (map refreshed, Connect reworded, one-liners + currency notes in); the dossier-content lane launched (and audited if returned); M9.3 authored + dispatched to a WORKING CC allow-list and landed ≤1 gate round with WUCP P2 complete; the Dependabot critical classified-or-fixed; docs Commit A landed with the zero-JS gate proven green on host; mirrors synced (Check 9 PASS); every return two-layer-audited; the spine current at every beat; v17 authored the moment this hub nears depth — and the mid-Aug go/no-go approached with gates 1+4-install banked, gate 2 completing on real silicon, gate 3 on the march, and gate 4's content lane running.
