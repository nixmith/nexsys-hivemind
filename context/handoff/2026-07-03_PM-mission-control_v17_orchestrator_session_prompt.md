<!--
file: context/handoff/2026-07-03_PM-mission-control_v17_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v17) SUPERSEDING v16 (context-saturated after a complete, dense arc: launch hygiene + preflight-reconciled → DOC 18 AUTHORED (Mode 1, charter-bound, two charter mis-citations caught from source) → the DOCS-review lane dispatched + RETURNED RATIFY-WITH-EDITS + two-layer audited [two reviewer baseline observations REFUTED as index-phantoms with git-object evidence] + B-1/N-1..N-4 FOLDED [co-sign-ready] → the dossier-content website lane authored [start-gate MET at docs 78e0c5c] → the device-profile-registry research lane authored + RETURNED + audited ACCEPT [Q1–Q10, 13 ✓✓ re-fetches; the 9-component finding verified in source; §A–§K banked as M9.3 inputs; DP-1/2/3 framed] → 4 beats, 6 commits landed across 2 repos, zero unresolved defects, zero open deferred gates). v17's charge: COLLECT THE FIVE RULINGS → LOCK Doc 18 + the consolidated docs pass in ONE watermark advance → M9.3 JIT-authored FROM the dossier → dispatched to a WORKING CC allow-list → M9.4 → bench acceptance → 72 h soak — the march to the mid-Aug go/no-go with gate 2 completing on real silicon.
audience: PM/Architect (fresh Cowork conversation; nexsys-project-manager skill; Mode-3 Director — Mode 2 briefly for the AMD-97-realization AMD at M9.3 authoring); Nick (the five rulings, gates, bench, commits — all host-side).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-03 by the v16 hub at beat 64. Launch as a fresh Cowork conversation on Nick's word.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — porcelain + both spine frontmatters BEFORE any spine write; failed edit anchor mid-batch = prior-writer signature: HALT). (1) Ground (env-model FIRST — §2 now carries the ATTRIBUTION COROLLARY [three lanes tripped in one day: M-flags on recently-committed spine files are PRESUMED PHANTOM until git log says otherwise; the index-read variant fabricates staged-looking D/R flags with rename targets cut mid-filename] + §5 anchor hygiene + §10 + §11 → the cold-boot trio (working-with-nick + infrastructure-map + decision-rationale-index) → truth-hierarchy → 11-check preflight; expect PASS-or-STALE-benign. Check 10 stays STALE-benign until the docs pass lands the map refresh). (2) Archive the v16 prompt → context/handoff/archive/. (3) Check-8: cross-agent ACTIVE should be 0; verify. (4) READ the five-decision state in pm-handoff beat-64: if Nick ruled any/all of the FIVE in the v16 conversation after beat 64, the rulings are recorded there — inherit as SETTLED, preserve his verbatim rationale in the Lock records; collect the remainder FIRST (nothing else starts until the five are ruled — they gate everything).
baseline (RE-DERIVE at preflight; the spine is truth): core **6ea6912** (M9.2 landed; clean) · docs **78e0c5c** + the Doc-18 REVIEW-FOLD **UNCOMMITTED in the working tree (7 edits — deliberately HELD for the Lock commit, the Doc-17 precedent; do NOT let any other docs commit sweep them)** · hivemind **2216da6 + beat-64 pending-or-landed (verify)** · bench **5ceff3b** (frozen instrument) · skills **ae434ca**. Watermark **AMD-97, invariants 172/51** (→ **174/52 at the Doc-18 Lock**: EXT-INV-1/2 mint as the new INV-EXT category, register §52, the §50 new-doc-Lock precedent — watermark-neutral). Zero unresolved deferred build gates.
-->

# Session Brief — PM Mission-Control v17 (five rulings → Lock → the registry milestone, grounded)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v16. Doc 18 is authored, independently reviewed, folded, and co-sign-ready; the M9.3 design space is researched to project standard (the profile-registry dossier); every input the Lock and the milestone need is banked. v17 converts five rulings into a Locked constitution + a dispatched M9.3. Ground → first acts → run the loop.

## 1. Where you are (verify, then move)

**Doc 18** (`design/18-extension-and-plugin-architecture.md`): DRAFT—REVIEW-FOLDED on disk; review verdict RATIFY-WITH-EDITS with B-1 (EXT-INV near-duplicate completion vs INV-CE-05/INV-SE-04) + N-1..N-4 already folded; the 7 fold edits sit UNCOMMITTED (ride the Lock commit). **The profile-registry dossier** (`context/assessments/2026-07-03_device-profile-registry_research-dossier.md`, audited ACCEPT beat 64): §A–§K are the M9.3 instruction's binding design constraints — §A grow `DeviceProfile` 9→10 with AMD-97's `confirmation[]` (realizes the RATIFIED amendment via the AMD-correction discipline, citing AMD-97 — brief Mode-2 entry) · §B sealed `MatchCriteria` (ExactModel | ModelWildcard | reserved Fingerprint; precedence + priority; findProfile widened to consume `InterviewResult`) · §C namespace convention on `profileId` (bare = first-party; `publisher.profile` = third-party) · §D repair-logic-in-named-codecs, NEVER eval-in-data · §E the honest degrade ladder (AMD-97-INV-01 inviolable) · §F index-first match-before-parse load · §G bound-and-drain `pendingCallbacks` BEFORE live NCP · §H loader-owned schemaVersion · §I user-override file = the reserved data channel; nothing couples corpus to core release · §J Wave-1 fixture-replay = the acceptance gate (Hue LCA017 + SNZB-03P render their recorded confirmability) · §K the Doc-08 §3.4 sleepy queue now, adversarial leg = Wave-2; `DeviceProfile` javadoc-scoped to Zigbee (INV-CE-04). Lane routing stands: backend M-WUs → Claude Code (**allow-list `"Bash(./gradlew:*)"` in core `.claude/settings.json` VERIFIED + CC restarted BEFORE dispatch — denied twice, a gate round each**); frontend/research → Cowork lanes; the hub authors/audits/records, single spine-writer.

## 2. First substantive acts (after hygiene; order matters)

1. **The FIVE rulings (Nick, one turn — collect any not already recorded at beat 64+):** **OQ-1** deprecation-floor N (recommended 6 release cycles + Repairs-class surface; L-11/HA ADR-0021) · **N-2** EXT-INV-2 scoping (recommended: mint UNSCOPED with the enforcement-arrival note — the AIOT-INV-1 precedent) · **DP-1** realize `confirmation[]` 9→10 at M9.3 (recommended (a) — freezing without the moat schema forces a re-amendment) · **DP-2** sealed `MatchCriteria` now, `Fingerprint` reserved (recommended (a) — the sealed-type choice is the one-way door; variants grow additively) · **DP-3** the Wave-1-ratified build line (recommended (a) — the bench is the oracle; codec/adversarial-sleepy/OTA-rematch legs = Wave-2-validated). Preserve every verbatim rationale.
2. **LOCK Doc 18 + the consolidated docs pass, ONE watermark advance:** Doc 18 status → Locked (fold OQ-1's ruled N into DP-18-A; the N-2 ruling into EXT-INV-2) · register **§52 INV-EXT** minting (EXT-INV-1/2 with the completed parent sets; 172→174, 51→52; §0.3 + §17 index + §18 traceability + nav-index currency) · the DP-B/DP-C/DP-D one-liners · the A4.5 Doc-15 §3.4/§6 backup-key note · the **strategic-context-map refresh** (clears standing Check-10; adds the four wipe-test files + Docs 18 + the new dossiers/prompts) · the **Connect reword** (DP-18-C(i) — strategy file; coherent in one commit) · R4 (>v14 band-edge) + R5 (§3.13 resume-mismatch) Doc-08 currency notes · the Doc-08 W6 jSerialComm touch-up · the D-4 R15 Six_Battlefields fold gap · the FE-1b-finding correction note · decision-rationale-index rows for the five rulings + F-1 → the R-4 backlog rows. Commits staged with EXACT counts (docs + hivemind, likely strategy paths inside docs — selective staging per env-model §10; the Doc-18 fold edits ride THIS docs commit).
3. **M9.3 JIT authoring (CC lane):** grounding-subagent-before-authoring (re-verify the frozen shapes at issue-time HEAD) → `context/pre-verifications/WU-M9.3.md` (pin BOTH module-graph knobs; embed integration-zigbee `module-info.java` VERBATIM; pin the 9-component ctor signature + every §A/§B touched type + the P2 ctor fan-out consumer list) → the instruction: interview pipeline (Doc 08 §3.4–§3.7) + attribute ingestion + `DeviceProfileRegistry` impl + adoption (identity UNGATED — DP-B permanent) + the dossier §A–§K as numbered design constraints + the DP-1/2/3 rulings + the AMD-97-realization AMD text (Mode 2; cites AMD-97) + inheritances (checked-PIE seam rule · `WithinTolerance` · `drainPendingCallbacks()` feed + §G bound-and-drain · `adapter_type`/config-key binding + `registerIntegrationSchema` wiring · §3.5 namespace convention · the §4c test-Clock reminder) + done-when = **Wave-1 fixtures replay green rendering recorded confirmability** (§J). Dispatch ONLY after the allow-list is verified + CC restarted. **Queue for Nick in the M9.3/M9.4 window:** the classifier cause-walk-for-PIE-only hardening candidate + hashed-TCLK (bench-informed).
4. **Audit returns as they land:** the dossier-content website lane (three stubs → reviewer grade; claim→truth registers; receipts) — two-layer, INCLUDING observations that route actions (the attribution corollary is live: check git log before believing any lane's porcelain claims).

## 3. The delivery loop (standing cadence to the mid-Aug gate, ~Aug 16)

- **Hub serial path:** five rulings → Lock + docs pass → M9.3 authored/dispatched (CC) → gate → WUCP P2 vs the DIFF → **M9.4 JIT** (adapter wiring + ReopenAction ordering [close→reopen→resetSession→startSession] + NETWORK_UP await + confirmation acceptance + hero E2E; §51 acceptance suite on fixtures) → **Nick's live-bench acceptance** (the beat-42 seven + DP-F fresh-network re-pair + the OTA-rematch/§Q10 re-characterize story + FE-7 rides this window) → **72 h soak** (NQ-6 restart spike DURING).
- **Frontend lane (parallel, write-isolated):** the dossier-content lane (in flight or launchable — start gate MET) → website.yml CI (on Nick's adopt ruling) → FE T2 backlog rides after.
- **Hardware (Nick + bench):** Wave-2 arrival → the unboxing runbook FROM the bench sources (inventory/label first; characterization BEFORE production pairing; the BEST_EFFORT sleepy sensor = the §K adversarial-leg + untested-confirmability priority; the MG24 stays untouched; ZBDongle-P waits for its second-transport slot — the PortLocator same-VID:PID limitation is M9.4-pinned).
- **Gates:** CI green on the PUSHED commit · two-layer audit on every return including observations · env-model §10 selective staging + exact counts · single-spine-writer · restart-resume on every resume · **capture-then-execute (author v18 at depth — do not die mid-arc)**.
- **Go/no-go board:** gate 1 ✅ · gate 2 = dashboard ✅ + real device data (lands M9.3/M9.4) · gate 3 = M9.3 → M9.4 → soak · gate 4 = install-smoke ✅ continuous + website skeleton ✅ + dossier content (lane) + hosting/deploy (Nick).

## 4. Nick's standing ledger (surface each session until closed)

**The FIVE rulings (if not yet given — they gate everything)** · **Dependabot CORE: 5 (1 critical) — COMPOUNDING since 06-27, the 60-second Security-tab triage** · ci.yml + install-smoke glance on `6ea6912` · **CC allow-list fix + session restart BEFORE M9.3 dispatch** · `npx astro telemetry disable` (one-time) · site npm audit 2 (1 high, BUILD tree — leisure) · website.yml adopt-now ruling · LICENSE-flip timing (blocks developer-page publish only) · hashed-TCLK election (M9.4, bench-informed) · classifier cause-walk candidate (M9.3/M9.4 window) · frontend.yml glance on `cf3733e` (residual) · Wave-2 arrival watch · Node-20 action-majors · the constraint-enforcement skill LTD-table touch-up (next skills pass; Check 9 goes STALE then) · F-1 IntegrationContext-javadoc reconciliation (rides the R-4 rows).

## 5. Disciplines (carry — the compact set)

Env-model first (§2 phantom family incl. the NEW attribution corollary; §4 index-lock/`--no-optional-locks`/never-`add --dry-run`; §5 anchor hygiene [line-anchored headings; replace_all is substring; read-back bulk edits with checks that distinguish worlds]; §9 esbuild-SIGSEGV + gate-copy; §10 audit + exact counts; §11 restart-resume). Truth-hierarchy/pointer-not-copy (re-derive every count/HEAD; skills carry NO state). Two-layer audit on every return — claims AND observations. Grounding-subagent-before-authoring. WUCP P2 source-verified against the DIFF. Checked-exception seam maps in any instruction mandating a checked type. Wire constants = double-derivation. Stub-tripwire arithmetic excludes instruction-anticipated stubs. Module-info pins validated against every mandated behavior. P2 consumer/pin fan-out (§A's ctor change is the big one — sweep every `new DeviceProfile(` site). Dispatch-craft: lossy compression in a brief propagates as false "corrections" (the beat-64 arity lesson) — quote spine phrases EXACTLY in lane briefs. Lane routing per §1; write-isolation + selective staging; never issue M9.4 while an M9.3 deferred gate is unresolved (§4b).

## 6. Success

The five ruled (verbatim rationale preserved) → Doc 18 **LOCKED** + the docs pass landed in ONE watermark advance (174/52; map refreshed — Check 10 clears; Connect reworded) → M9.3 authored from the dossier + dispatched to a WORKING allow-list + landed ≤1 gate round with WUCP P2 complete → the dossier-content return audited (if landed) → the Dependabot critical classified-or-fixed → every return two-layer-audited with the attribution corollary applied → the spine current at every beat → v18 authored the moment this hub nears depth — and the mid-Aug go/no-go approached with gate 3 on the march and gate 2 completing on real silicon.
