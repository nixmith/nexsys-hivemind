<!--
file: context/handoff/2026-07-03_dossier-content-lane_session_prompt.md
purpose: Dispatch-ready brief for a FRESH Cowork session (nexsys-frontend skill) that brings the three website dossier stubs — explainability · no-cloud-account · ledger-gap-dossier — from their SHORT-HONEST versions to config-superiority reviewer grade. This is the binding gate-4 CONTENT gap (the W-5 publish gate: all four flagship dossiers reviewer-grade; referenced in the spine as W-5/PG-1). Content lane, receipts-first; write-isolated to exactly three page files + its return.
audience: a FRESH Cowork conversation (nexsys-frontend skill loaded — its build-and-ci-discipline + lane-return references govern mechanics; this brief does not restate them); Nick (start-gate + host build gate + commits).
state-type: session prompt (frontend/content lane).
status: READY — authored 2026-07-03 by the v16 hub. Parallel-safe vs M9.x, the Doc-18 review lane, and all core work. NOT parallel-safe vs docs Commit A (see the start gate).
**START GATE (hard):** docs **Commit A must be LANDED** before this lane edits anything — the three stub pages are themselves inside Commit A's 26-path set; editing them earlier entangles this lane's work with Nick's staged explicit-list commit. Preflight check: docs HEAD has advanced beyond `1509b34` with the website set (`git -C homesynapse-core-docs --no-optional-locks log --oneline -3` shows the website-lane commit; `website/pages/explainability.md` is TRACKED and clean). If not landed: STOP, return one line ("start gate not met — Commit A pending"), do nothing else.
baseline: docs = Commit A's HEAD (re-derive at preflight) · the untracked `design/18-extension-and-plugin-architecture.md` may sit in the docs tree (the Doc-18 DRAFT — NOT yours; never stage, never edit) · hivemind = newest PROJECT_SNAPSHOT masthead beat. Known hazards (env-model, read FIRST): truncated-tail phantom (host file tools are truth) · index-lock (`--no-optional-locks` reads only; NEVER `git add`, incl. `--dry-run`) · esbuild-native SIGSEGVs in this sandbox class — if you run the site build in-lane, use the §9 /tmp-md5-copy + wasm-shim-on-the-copy pattern; **your green is NOT the gate of record — Nick's native host run is** · anchor hygiene §5 (line-anchored, read-back-after-bulk).
write-isolation: exactly THREE docs-repo files — `website/pages/explainability.md` · `website/pages/no-cloud-account.md` · `website/pages/ledger-gap-dossier.md` — plus ONE hivemind return file (`context/audits/2026-07-03_dossier-content-lane_return.md`). Nothing else. No site/ code, no tokens, no index.md, no CI files, ZERO core writes.
reads (in order):
  - nexsys-hivemind/context/process/cowork-environment-model.md (FIRST) → PROJECT_SNAPSHOT newest beat (grounding)
  - the three stub pages IN FULL — each carries (a) the shipped short-honest version with its guardrail comments and (b) the ORIGINAL INCREMENT-2 PLANNING STUB preserved verbatim in-file **with its receipt obligations enumerated** — that in-file block is your per-page brief
  - homesynapse-core-docs/website/pages/config-superiority.md (THE GRADE BAR — structure, voice, structural-claims style, the provenance/citation pattern, the "what we deliberately left out" honesty)
  - nexsys-hivemind/context/audits/2026-07-03_website-lane_return.md §4 (the claim→truth table shape your pages inherit) + §9 (recorded defaults) + §11
  - nexsys-hivemind/context/planning/2026-06-29_frontend-master-plan.md (§W-1 receipts-audience, W-2/W-4/W-5 gates, D-4 segment rule, D-FE-6 hero-showcase gate) + the design-system canon under homesynapse-core-docs/website/ (voice-and-tone + visual — the claims/voice registers; resolve the spine's "C2 register" shorthand there at ground time)
  - homesynapse-core-docs/design/17-aiot-and-cloud-readiness.md §7-6 ONLY (the counsel-gated claim-language boundary)
-->

# Session Brief — Dossier-Content Lane (three stubs → reviewer grade; receipts first)

You are a **fresh frontend/content lane** (nexsys-frontend skill). Your product: the three flagship dossier pages at **config-superiority reviewer grade** — the W-5 publish gate's outstanding content. The audience reads receipts and distrusts marketing (master plan W-1); *evidence beats adjectives*. Env-model first, light preflight, **verify the START GATE**, then build.

## The charge, per page (the in-file Increment-2 planning block is the brief; discharge its receipt obligations)

1. **`explainability.md` — "Ask Your Home Why" (flagship, REC-194 class).** The porch-light story; the eviction contrast **with receipts** (the in-file obligation names the corrected baselines: HA persists traces to disk, its default cap is `stored_traces: 5`, cap ≠ in-memory — re-verify BOTH against current HA docs lane-direct before writing; no-trace-on-never-match); what an always-answerable home changes. **D-FE-6 holds:** the hero UI is built and live-integrated, but it is NOT shown (no screenshots, no embed) until demo-ready on real device data — claim the record and the shipped surfaces; the "rendering, not data" guardrail's 2026-07-03 in-file note is your boundary.
2. **`no-cloud-account.md` — "No Cloud Account. Really."** Prosumer/EU-facing per D-4 (privacy-forward permitted HERE; named + dated regulation only — the Data Act line stays Regulation (EU) 2023/2854, applicable 2025-09-12; still no compliance/legal-advice claim; CRA stays uncited). The cloud-shutdown narrative graduates from unnamed to **named with receipts**: the Insteon 2022 arc with the CEO-estimate attribution (REC-179) — attribute the estimate, date the events, cite sources fetched lane-direct. REC-171's claim matrix folds in as the structural-comparison spine.
3. **`ledger-gap-dossier.md` — the confirmation-ledger gap (REC-142 class).** The "to our knowledge, no other platform in the category maintains this record" superlative KEEPS its counsel-safe form and now gets its receipts: the surveyed-platform absence evidence, per platform, cited. The shipped mechanics it claims (confirmed/failed/expired; unconfirmed never rendered as success) trace to Doc 07 §3.11.2 + AMD-97/AMD-97-INV-01 — state them as the website-lane return §4 rows do (claim → shipped-truth source).

## Non-negotiable disciplines (all pages)

- **Claim→truth register per page** (the return-§4 shape): every load-bearing claim rows out to its shipped-truth source or external receipt. Competitor specifics are PERMITTED on these full dossiers — **only** with receipts (live-fetched, dated, VERIFIED-tagged; the anti-fabrication discipline of the 2026-07-02 research dossier applies). An unverifiable claim is cut, not hedged.
- **Publish gates stand:** NO Apache-2.0/LICENSE claim anywhere (publish-gated on Nick's flip) · W-4 install-story embargo (no "plug-and-play") · W-5 posture — these pages land in-repo reviewer-grade but nothing "publishes"; noindex/unpublished posture unchanged · no dates-as-promises, no device-count marketing, no sandbox claims (DP-18-B honesty rule) · Doc 17 §7-6 claim boundary (no "safest," no "can never") · name-light W-11 tokens throughout ({{productName}}/{{companyName}}).
- **Voice:** config-superiority IS the register — structural claims, quiet confidence, "what we deliberately left out" honesty. Zero-JS content pages (markdown only; no scripts, no embeds).
- **Scaffolding hygiene:** once a planning block's obligations are discharged into the page, REMOVE the in-file planning stub and replace with a compact provenance comment (guardrails kept one-line; the receipts live in the visible register now). The page ships clean.

## Return contract (the skill's lane-return protocol governs; extras here)

`nexsys-hivemind/context/audits/2026-07-03_dossier-content-lane_return.md` with: per-page claim→truth registers (re-derivable); the receipt-fetch evidence list (URL + date + what it verified — including the HA-baseline and Insteon re-verifications); a guardrail self-audit table (each discipline above: HELD/violated-and-why); honest gaps; env-model §10 pre-commit audit — **docs commit stages exactly 3 paths** (the three pages; the untracked Doc-18 DRAFT and anything else stays OUT), **hivemind commit stages exactly 1** (the return), messages bang-free/backtick-free via `_scratch/` for `git commit -F`, expected counts stated for Nick's porcelain glance. Deferred gate: Nick's host `npm run build` (expect 6/6 pages + "zero-JS gate OK"). If your porcelain shows anything beyond your writes, STOP and say so in the return. Refuse-to-close: name the next content unit (candidates: the REC-174 dated-regulation page; the D-FE-6 hero-showcase fold once real-device data exists).
