<!--
file: context/handoff/2026-07-08_PM-mission-control_v26_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v26) SUPERSEDING v25 (retired at: AMD-99 RATIFIED as the first act ["ratify as recommended — (A), and R-B through R-E as written"; REG-INV-1 minted 174→175, +REG 52→53, watermark AMD-98→AMD-99; folds F1–F3 applied pre-ratification] → M9.5-DUR authored/dispatched/audited ACCEPT/landed core `ef1d2c5` CI-GREEN-both-workflows [the registries are PROJECTIONS in code; ProcessRestartIdentityIT; the REGISTRY_MUTATION_ONLY_VIA_PROJECTION rule with permanent teeth; R1 module-info promotion adjudicated ACCEPT/hub-miss-owned; R2 half-registration gap CONFIRMED → DURb] → **ITERATION 5b: THE DUR PROOF ON SILICON** [`registry.projection_live: devices=2 entities=2 position=14` across a full process restart; view = exactly 2; the M9.4-KEYb silicon confirmation CLOSED — one `key_established`, zero failed; device-id continuity log-proven; the two closing legs = physical pin-2 + entity-ULID API-to-API, run at v25 close — VERIFY their paste landed in the bench record before treating 5b closed] → **THE DURb ROUTING RULED verbatim at pm-handoff v25 beat-4: (a) BEFORE THE SOAK, 5b-FIRST — "you soak the code you ship"** → M9.5-DURb AUTHORED ISSUE-READY [half-registration repair + the packed seam rows] → v26 banked at beat 5). v26's charge: **verify the 5b closing legs recorded → dispatch M9.5-DURb on Nick's word → audit → land CI-green → THE ACCEPTANCE RUN (P3.11–12 → P4 bench-hero automation [the hub AUTHORS the config — entity IDs are durable] + restart RESUME leg → P5 §51 legs → P6 reopen + PJ key OUT) → the 72 h soak ON FINAL CODE (the ruled sequencing; NQ-6 restart spike DURING = the DUR proof at soak scale; W2-VAL chartered at entry) → soak-window WUs (seed-custody [SD-5 HARD GATE — before ANY non-bench network] + the explainability-lane return audit) → Wave-2 unboxing on arrival → v27 before depth.**
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-08 by the v25 hub at beat 5 (rotation banked on Nick's session-length flag, before the DURb dispatch depth). Launch when v25 retires.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — LOCK-FREE porcelain ×5 [--no-optional-locks ONLY] + BOTH spine frontmatters BEFORE any spine write; ONE hub session at a time; NOTE: the v25 arc ran REMOTE-Cowork — host truth came via the device bridge [device_stage_files reads + SendUserFile→device_commit_files writes, git-object sourcing per env-model §5(b)]; the local Filesystem MCP re-registered mid-arc scoped to homesynapse-core ONLY — do not assume it reaches the hivemind; verify tool scope before relying on it). (1) Ground: env-model FIRST → the bench-troubleshooting playbook IN FULL (two NEW §6-class lessons live in the RUNBOOK's operational block: per-launch TOKEN re-cat + sleep-after-pkill) → the cold-boot trio → truth-hierarchy → the 11-check preflight. (2) Archive the v25 prompt → context/handoff/archive/. (3) READ IN FULL: pm-handoff v25 beats 1–5 (beat-1 = Nick's AMD-99 ratification assessment VERBATIM + the DUR-WU carry-list C1–C4; beat-4 = THE DURb ROUTING RULING VERBATIM — the binding sequencing record) + AMD-99 (docs `design/amendments/AMD-99_Registries-Projection.md`, RATIFIED) + the M9.5-DURb instruction (`context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md`) + the bench record iterations 1–5b + the runbook field-validated block + coder-handoff NEXT-WU. Treat every HEAD/count as a CLAIM to RE-DERIVE; source wins over this prompt.
baseline (RE-DERIVE at preflight): core **ef1d2c5** (M9.5-DUR; CI GREEN both workflows — gate CLOSED) · docs **a53f474** (AMD-99 ratification commit) · hivemind **6fd6ddc + the beat-5 commit (exactly 4: 2 M + 2 new — verify landed)** · bench **aa45edc + the 5b commit (exactly 2 M — verify landed, and verify the closing-legs paste was appended to the 5b block BEFORE it committed)** · skills **0c00ece**. Watermark **AMD-99, 175/53** — re-derive from the register's regeneration line, never copy. §4b: CLEAR at authoring (all core gates GREEN through ef1d2c5).
-->

# Session Brief — PM Mission-Control v26 (DURb → THE ACCEPTANCE RUN → the 72 h soak → seed-custody)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v25. **The moat is architecturally complete and silicon-proven durable**: the full join→key→adopt→report→STAY pipeline (iteration 5a), an instrument honest to DEBUG granularity (KEYb, silicon-closed at 5b), and **identity that survives process restarts as a pure projection of the event log** (AMD-99 + M9.5-DUR + iteration 5b's `devices=2 entities=2 position=14`). Everything remaining is certification and hardening, not building. **The hub never implements — it intakes, audits, records, and rules WITH Nick.**

## 1. FIRST SUBSTANTIVE ACT — verify 5b fully closed, then the DURb dispatch

Verify the two 5b closing legs (physical pin-2: Hue power-on → `device_relinked`, NO `device_proposed`; entity-ULID continuity: same two strings across one more restart) were pasted and appended to the bench record's 5b block. If NOT recorded, chase them first — 5b is not closed until captured (never-false-CONFIRMED applies to our own records). Then **dispatch M9.5-DURb on Nick's word** — paste: *"Read nexsys-hivemind/context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* — two-layer audit on delivery (quote-the-instruction-verbatim prompts; labels-are-claims) → gates → commit/push/CI. **The ruled sequencing is BINDING (beat-4 verbatim): DURb lands BEFORE the soak — never during or after.** Acceptable variant per the ruling: acceptance run first, then DURb, then soak.

## 2. THE ACCEPTANCE RUN (opens when Nick says; DURb per the ruled sequencing)

Resume the runbook: P3 steps 11–12 (amended, positive-evidence-first; TWO new operational lessons in the runbook block: per-launch TOKEN re-cat + sleep-after-pkill) → **P4 the bench-hero automation** — the hub AUTHORS the automation config at that beat (entity IDs are durable now; use the 5b-recorded ULIDs) + the restart RESUME leg → **P5 §51 live** (CONFIRMED only via authoritative reports · identify = immediate honest UNCONFIRMED · superseded expires verdict-free · latencies vs corpus · IAS ×2 twins · Q10 · FE-7 · optional bulb-off timeout-honesty probe) → **P6 reopen + REMOVE the PJ key**. ⏺ RECORD every leg; playbook §8 paste-block discipline (self-contained blocks; TOKEN per block; sleep-after-pkill).

## 3. The 72 h soak (ENTERS on acceptance, ON FINAL CODE) → the queue

PJ key OUT first. **NQ-6 restart spike DURING the soak = the DUR proof at soak scale** (and DURb's validation — the ruling's whole point). A soak restart resets the clock; W2-VAL synthetic legs chartered at entry. **Soak-window WUs (idle bench):** the **seed-custody WU** (SD-5 HARD GATE — no non-bench network until it lands) + **the explainability-lane return** (chartered v21, STILL OUTSTANDING — two-layer audit on arrival). **Queued rows (fold on seams, never author while a gate is open):** R3 designed-posture WARN re-rate · ReplayTransitionIT deflake (its own micro-WU on recurrence) · the backlog M9.4 mega-row compaction + M9.5 row addition at the next currency pass · partial multi-entity repair (recorded out-of-scope in DURb) · the boot root-schema WARN cosmetic row. Wave-2 arrival → the unboxing runbook (MG24 UNTOUCHED — the frozen instrument).

## 4. Nick's standing ledger (surface until closed)

frontend.yml `199324e` confirm-once (the one open glance) · the repo-briefly-public security glance · telemetry disable · website.yml adopt · LICENSE-flip · Node-20 CI deprecation · the naming thread (no surface renames until ruled) · Wave-2 watch · un-wired `adapter_type` (low) · two `_scratch/*.HEAD.tmp.md` staging temps deletable at leisure.

## 5. Disciplines (carry — the compact set)

Env-model first; in REMOTE-Cowork the device bridge is host truth (stage-reads + commit-writes; git-object sourcing for spine edits; container re-stage at the SAME path serves a STALE cache — copy to a fresh temp name on-device first, the v25 lesson). The playbook governs bench work; the runbook's operational block carries the token/pkill lessons. Truth-hierarchy/pointer-not-copy (re-derive every count/HEAD/watermark). Two-layer audit on EVERYTHING incl. own subagents. Verbatim-preserve Nick's rulings (beat-1 ratification + beat-4 sequencing are the models). Bench-iterative WUs: unit-green ≠ done-when. Capture-then-execute: author v27 before depth. **The north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE, held through six iterations, now extends to identity itself.**

## 6. Success

The 5b closing legs recorded → DURb landed CI-green BEFORE the soak (the ruled sequencing honored) → **THE ACCEPTANCE RUN recorded end-to-end: motion → light → honestly-confirmed over MEASURED reporting, on DURABLE identity — the thesis, complete** → the 72 h soak RUNNING on final code (PJ key out; NQ-6 doubles as the DUR-at-scale proof) → seed-custody landed (SD-5 honored) → the explainability surface rendering honest verdicts → v27 authored before depth — the mid-Aug go/no-go approached with the moat proven, adopted, reporting, honest, durable, and certified.
