<!--
file: context/handoff/2026-07-10_executive-briefs_lane_session_prompt.md
purpose: Session prompt for the executive-briefs lane (Track B lane (c) — Nick's ruled internal order: go/no-go criteria draft + naming decision-brief FIRST; the LICENSE-flip brief is GATED on lane (b)'s return; website content goes LAST and is out of this lane's scope).
audience: a fresh Cowork research/strategy lane (write-isolated; returns to the hub; the briefs go to Nick for rulings).
state-type: lane dispatch prompt (one session; retires on return).
status: READY — launch in a FRESH Cowork conversation; attach this file; paste: "Read and follow all instructions contained within the executive-briefs lane session prompt file."
-->

# Lane Brief — Executive Briefs (go/no-go criteria + the naming decision)

## 0. Mission and authority

You are a STRATEGY LANE for HomeSynapse. Produce TWO decision briefs in this order: **(1) the mid-August go/no-go criteria draft** and **(2) the naming decision-brief.** Nick's rationale, verbatim: "naming blocks every public surface; criteria defined now make mid-Aug evidence-based instead of vibes." You are WRITE-ISOLATED: your ONLY repo writes are the two return files in §3. You do NOT draft website content (it consumes naming + positioning downstream) and you do NOT write the LICENSE-flip brief (it consumes the integration-roadmap lane's conclusions — a later dispatch); if you finish early, you may APPEND a §-stub to the naming return listing what the LICENSE brief should inherit from your trademark findings, nothing more.

## 1. Brief 1 — the mid-Aug go/no-go criteria draft

Required reads first: `nexsys-hivemind/context/status/PROJECT_SNAPSHOT.md` (newest 3 beats — the certified state), `nexsys-bench/docs/2026-07-06_m9.4-bench-acceptance-record.md` (what "certified" means in evidence), the MVP spec's §8 (in `homesynapse-core-docs/` — performance/scope budgets), `context/strategy/Six_Battlefields_MVP_Strategy.md`, and `context/planning/phase-3-milestone-backlog.md` (the ruled post-soak stack — the 2026-07-10 rows).

Deliverable: a criteria CHECKLIST where every criterion is (a) binary or measured, (b) evidence-defined (WHAT artifact proves it — a bench record entry, a CI run, an API response, a scenario-suite result), and (c) tagged MUST/SHOULD. Candidate dimensions to develop (add/cut with argument): certification durability (the 72 h soak + NQ-6 closed on the record ✓ expected), identity durability (held), the honest-verdict envelope (zero false CONFIRMs across N verdicts — propose N), fleet breadth (Wave-2 adopted: how many device CLASSES suffice for a credible v1?), seed-custody landed (the SD-5 gate — non-negotiable before any non-bench network), availability honesty (ALIVE fix landed + demonstrated), the explainability surface (what minimum FE-7 demo counts), operability (bench.sh/scenario-suite green = the regression floor; install path reproducible), security posture (the custody fence + generated-seed statement), and the explicit NON-criteria (things that look gate-like but are ruled Wave-3 — name them to prevent scope creep at the gate). For each: the measurement, the current status (evidence pointer or PENDING), and who produces the missing evidence. End with a one-page "gate day" protocol sketch (who reads what, in what order, to say go).

## 2. Brief 2 — the naming decision-brief

Required reads first: `context/strategy/2026-07-05_naming-and-domain-strategy.md`, `context/strategy/2026-07-05_product-name-candidates.md`, `context/strategy/asymptote-name-recommendation.md`, and the positioning notes (`context/strategy/2026-07-10_acceptance-arc-positioning-notes.md`) — the name must carry the honesty/trust thesis.

Deliverable: a ONE-TURN-RULABLE brief. §0 = the REC (one name + one fallback, with the three decisive reasons). §1 = the shortlist table (≤5 candidates incl. the incumbent working names): for each — domain availability RE-VERIFIED NOW (the .com and the sensible alternates; note price class if premium), USPTO knockout search re-run NOW (live marks in class 9/42 that collide — you are NOT a lawyer and say so; this is a screen, not clearance), collision scan (GitHub/product namespaces/HA-ecosystem collisions), pronunciation/spelling risk, and the thesis-fit sentence. §2 = what changes on ratification (repo names? binary name? the docs watermark? — enumerate the rename blast radius so the ruling is priced). §3 = the recommendation's risks stated honestly. Every availability/trademark claim carries its check-date and source URL — these facts go stale in days, so mark them "as of 2026-07-11".

## 3. Return protocol

TWO files: `nexsys-hivemind/context/assessments/2026-07-11_go-no-go-criteria_draft.md` and `nexsys-hivemind/context/assessments/2026-07-11_naming-decision-brief.md`. Frontmatter each (purpose/status/date). State "RETURNS READY for hub audit" + a 10-line summary in-conversation when done. Do NOT commit; the hub orders commits. Honest uncertainty beats padded confidence everywhere — both briefs exist so a RULING can be made on evidence, not so the lane can perform completeness.
