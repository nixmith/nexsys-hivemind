<!--
file: context/handoff/2026-07-19_PM-mission-control_v35_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v35) SUPERSEDING v34 (retired at close-out after: the v34 launch [PASS all 11 + the 3cc423b sweep reconciliation + the explicit-paths rule minted] → W2-LEARN [authored grounding-first with the S31-ROUTE desk closure → delivered → two-layer audit ACCEPT zero-[REVIEW] with in-session mutation verification → landed 554e18c CI-green → deployed] → the Matter program consumed as pointers [A1–A5 audited ACCEPT ×5 at three layers · B1–B5 RULED: (a) sidecar+riders / scope cut / phase-1 DUTs+Option-C / (a)→(c)@Hub-trigger / O1+spike · Phase C OPEN] → the 04P adoption arc + BENCH-CONST [status at v34 close — RE-DERIVE] → G1 refreshed 2026-07-19 + dispatched on Nick's word → v35 banked).
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick.
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-19 by the v34 hub. Launch on Nick's word AFTER the v34 close-out beat lands; ONE hub at a time; v34 retires on this launch.
first-act hygiene (IN ORDER): (0) env-model §11 restart-resume (lock-free porcelain ×5 [--no-optional-locks ONLY] + BOTH spine frontmatters BEFORE any spine write). (1) ENV §§1–5 + §10–§12 — §12 governs if remote; Filesystem-MCP scope VOLATILE (list_allowed_directories FIRST — it read CORE-ONLY through the whole v34 arc; the bridge stage/commit path is the fallback; same-path re-stages serve a STALE cache; the stage endpoint 400-rejects .java). (2) Cold-boot trio + truth-hierarchy + the 11-check preflight (Check 2: W30 current from Mon 07-20 · Check 8: ACTIVE 0 expected · Check 9: skill edits MAY be pending sync — the v34-era folds; score per-proxy honestly). (3) Archive the v34 prompt → context/handoff/archive/. (4) **THE ROTATION — the deferred hot-path hygiene, EARLY and BEFORE depth:** pm-handoff (~393 KB) pre-v32 beats + the masthead Prior-chain → context/handoff/archive/ per env-model §8; snapshot masthead-chain trim likewise; git-object-sourced, assert-all-before-write (§5), tails verified via host file tools after — a dedicated beat with its own commit order. (5) REQUIRED READS, priority-ordered: pm-handoff v34 beats 1–3 (the working law: the two ride-along reconciliations + the ABSOLUTE explicit-paths rule · the W2-LEARN audit record · the Matter consumption) → the W2-LEARN instruction + integration-zigbee MODULE_CONTEXT §W2-LEARN + §M9.7-W2 → the 04P mini-package + its pastes (the D1/BENCH-CONST record) → the criteria RATIFICATION block (unchanged; J1 FROZEN) → the doctrine brief → the Matter program's 00_PROGRAM_STATUS §-beats (pointers ONLY — its tree is write-isolated). Treat every HEAD/count as a CLAIM to re-derive.
baseline (RE-DERIVE at preflight — v34-close-time claims): core `554e18c` (deployed; installDist fresh at the Pi) · docs `a53f474` · hivemind = the v34 beat-3 close-out commit (verify landed; the program tree may carry ITS OWN uncommitted artifacts lawfully — they never ride main-hub commits) · bench `1c705f6` + BENCH-CONST if landed (re-derive; boot-health GREEN expected post-re-mint) · skills `dccc85f` (+ any v34-era folds pending sync) · watermark AMD-99, 175/53. FLEET: 6 devices/6 entities expected (the 04P adopted BINARY_SENSOR/contact — D1 [M] 5/5; re-derive at the API; if the mini-package did NOT close, IT is charge 0). THE GATE: 21 MUST / 8 SHOULD · evidence-freeze Fri 2026-08-14 EOD · the read Sun 2026-08-16 · J1 FROZEN. **The M14 sweep (~Jul-28, boundary EOD Jul-31) — G1 was dispatched at v34 close; verify its OUT status = the last condition.** THE CMD-API TRIPWIRE: ~Jul-24 — days away; it is charge 1.
-->

# Session Brief — PM Mission-Control v35 (THE ROTATION → CMD-API vs ~Jul-24 → SKIP-VIS → the M14 sweep)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v34. Every v28–v34 discipline carries — the v34 additions now standing: **EXPLICIT-PATHS COMMITS ARE ABSOLUTE** (two ride-along occurrences in one arc; every hivemind commit order states `git add <paths>` from the census, never `-A`, while multiple writers are live) · **name mutants against control-flow reality** (an unrealizable named mutant is a tooth that cannot bite — classify before claiming) · **verify-first questions resolve at desk when the source can answer them** (the S31-ROUTE closure: route()'s gate field read at source + the night's ingest evidence beat a speculative micro-WU) · **operator packages state their in-memory-state hazards and put the human gate BEFORE the one-way door** (the two-window 04P shape) · **instrument re-mints consume values captured AT the instrument, never banked claims** (BENCH-CONST from Block-6 capture). The hub never implements. The north star: never-false-CONFIRMED / never-false-ALIVE.

**Charges, priority order:**

**(1) THE ROTATION (first-act 4 — before any depth):** the hot-path tax is real (pm-handoff ~393 KB; the masthead line alone is tens of KB). Rotate pre-v32 beats + the Prior-chain to archive; trim the snapshot chain; one dedicated beat, one commit order, exact counts. Every later beat this arc gets cheaper.

**(2) CMD-API vs the ~JUL-24 TRIPWIRE (the week's center of gravity):** author full-bar, grounding-first, on the banked debts — **auth on the POST** (the OR-APP-AUTH `installAuth` posture extends to the write surface; token-freeze day one) · **IdempotencyEntry honored** · **ProblemType complete** · **wire-casing pinned vs the LIVE camelCase precedent** (per-endpoint reality — read at the endpoint; coder-lessons 2026-07-12) · **the WebhookTrigger route census** (the 4-grep endpoint-existence census — route strings over src/main · `app.(get|post|…)(` census · type-reference sweep · MODULE_CONTEXT outstanding-list; types are contract, never wiring) · the command-lifecycle assertion surface B1 was reduced around. Pre-verification mandatory (≥3 source-state assumptions). Dispatch to a host-CC Coder lane → two-layer audit → land CI-green. Honest posture stands: C1's N already numerically exceeded organically; what stacks behind CMD-API: **C2/C3 suite ports · H2's full AUTO suite · B2/B3 (BENCH-AUTOSTART rules at B3) · the S31 confirm legs (+ the sanctioned best_effort posture re-emit on a re-drive) · the two SKIPPED-stub flips · the AUTO usb scenario.** Rider 3: sequenced with the B2 port ahead of M14; Jul-31 prices any slide — no re-ruling.

**(3) SKIP-VIS authored full-bar** behind CMD-API (or first — Nick's call at dispatch): the silent-skip run class (COMPLETED with actionCount>0/commandCount=0) gains surfaces; the G1 lane's return may carry the rendering half's contract gaps — fold them.

**(4) THE M14 TRIGGER SWEEP (~Jul-28; boundary EOD Fri 2026-07-31):** conditions at v34 close: certification ✓ · RO ✓ · AVAIL ✓ · zero open [REVIEW]s ✓ · SEED ✓ · D1 5/5 (re-derive) · **G1 dispatched — verify OUT**. Run on evidence; miss ⇒ post-launch arc #0, no re-ruling; the third-plug rider governs (a ~$25–35 plug just-in-time the day the trigger clears).

**(5) The programs (pointers, never copies):** the **Matter design program** runs Phase C parallel (consume `00_PROGRAM_STATUS.md` §-beats; **Q-A4-1 confirm-before-ack escalates through THIS hub if any Doc-01-class prose must change**; the spike = post-gate-read; B3 hardware = Nick's discretion; the program's commits ride ITS orders) · the **counsel program**: Pelton **Mon 07-21** (C-1..C-4 handed) · TSDR #2 07-31 · veromo/LLC · R-1 HELD for G-2.

**(6) Standing ledger (surface until closed):** **HUE-RESET (Nick: dimmer ~$25 vs the TOUCHLINK-RESET WU)** · **LEARN-PERSIST** (persist/re-read learned zoneTypes across restarts — the backlog row from the 04P arc) · the **Rosonway dongle-behind-hub move + I3b [S]** (single-variable, boot-health re-run FIRST — eligible once BENCH-CONST greens) + the usb-power flip · Shelly Gen4 §5.3 provisioning (out-of-band; ident-gate expects `S4PL-00116US`) · **the doc-drift ×3 batch** (Doc 12 §3.8 ServiceLoader + its watchdog/sd_notify text · Doc 18 "built, unwired" ×2 places — a convenient docs commit) · corpus entries from the joins night + the wave2 return §4 fill · skills currency folds (the arc's classes: one-way-door choreography · enroll-fixtures-learn · mutant realizability · instrument-constants staleness) · H3 = Aug 8–9 · E3's public statement writable · the ZBDongle-P stays STOWED · frontend.yml confirm-once · LICENSE (consumes R1a) · Node-20 · the V1.x charter row (post-gate) · **v36 BANKED BEFORE DEPTH.**

**Success:** the rotation done and every beat cheaper → CMD-API authored + dispatched + landed CI-green with real margin before the ~Jul-24 tripwire → SKIP-VIS authored → the G1 return intaken (two-layer) → the ~Jul-28 sweep run on evidence with NOTHING outstanding → every MUST row's producer running for the Aug-14 freeze — the doctrine compounding: every incident a scenario, every scenario a permanent asset.
