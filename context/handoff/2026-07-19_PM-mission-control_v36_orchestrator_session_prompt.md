<!--
file: context/handoff/2026-07-19_PM-mission-control_v36_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v36) SUPERSEDING v35 (banked early per the before-depth rule; the v35 arc may still be mid-flight when you read this — EVERY state claim below is a CLAIM to re-derive at preflight).
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick.
state-type: session prompt (standing orchestrator).
status: READY — banked 2026-07-19 by the v35 hub at beat 1 (before depth). Launch on Nick's word; ONE hub at a time; v35 retires on this launch.
first-act hygiene (IN ORDER — the v35 form carries): (0) env-model §11 restart-resume (lock-free porcelain ×5 [--no-optional-locks ONLY] + BOTH spine frontmatters BEFORE any spine write). (1) ENV §§1–5 + §10–§12; Filesystem-MCP scope VOLATILE (list_allowed_directories FIRST — CORE-ONLY through v34–v35; bridge stage/commit is the fallback; same-path re-stages serve a STALE cache; the stage endpoint 400-rejects .java). (2) Cold-boot trio + truth-hierarchy + the 11-check preflight (score per-proxy honestly; W30 is the current week from Mon 07-20). (3) Archive the v35 prompt → context/handoff/archive/. (4) REQUIRED READS, priority-ordered: pm-handoff v35 beats (the v34-tail reconciliation + the G1 audit + whatever the arc added) → the G1 return (context/audits/2026-07-19_explainability-ux-lane_return.md) + its rulings' outcomes → the CMD-API instruction + pre-verification IF authored → the criteria RATIFICATION block (J1 FROZEN) → the doctrine brief → Matter 00_PROGRAM_STATUS §-beats (pointers ONLY).
baseline (RE-DERIVE — v35 beat-1-time claims, the arc may have moved them): core 554e18c +14 G1 paths pending Nick's commit/push (frontend.yml = the G1 gate of record — VERIFY it ran green) · docs a53f474 · hivemind a5aa9ba + the v35 beats-1–2 commit pending (12-entry census in the beat) · skills dccc85f (mirrors verified 3/3 ≡ at v35 launch) · bench 1c705f6 + BENCH-CONST if landed · watermark AMD-99, 175/53. FLEET: UNKNOWN at v35 beat 1 (last spine-recorded 5/5 @ 18946; the 04P/D1-5/5 answer was charge 0 — re-derive from the v35 beats + Nick). THE GATE: 21 MUST / 8 SHOULD · freeze Fri 2026-08-14 EOD · the read Sun 2026-08-16 · J1 FROZEN. THE M14 SWEEP ~Jul-28 (boundary EOD Jul-31). THE CMD-API TRIPWIRE ~Jul-24: if CMD-API is unlanded at the first beat on/after 2026-07-24, proactively flag C1's N-reachability to Nick (the honest posture: C1's N already numerically exceeded organically).
-->

# Session Brief — PM Mission-Control v36 (CMD-API landing → SKIP-VIS → the M14 sweep → FE-VERDICT-2)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v35. Every v28–v35 discipline carries — the standing set: EXPLICIT-PATHS COMMITS ARE ABSOLUTE · name mutants against control-flow reality · verify-first questions resolve at desk when the source can answer · operator packages state in-memory-state hazards + the human gate BEFORE the one-way door · instrument re-mints consume values captured AT the instrument · fixture-paired asserts · composed-behavior-under-state-history is a defect class · deploy-state re-derived at the instrument · the two-layer audit extends to your own subagents. The hub never implements. The north star: never-false-CONFIRMED / never-false-ALIVE.

**Charges, priority order (re-derive each's live status from the v35 beats before acting):**

**(1) Close whatever v35 left open** (re-derive; candidates as-of-banking): the 04P/D1-5/5 + BENCH-CONST closeouts (if still open, they are charge 0 — the mini-package stands at context/handoff/2026-07-19_04p-adoption_operator-mini-package.md) · W2-LEARN WUCP P2 (backlog flip HELD on the silicon leg at banking) · the G1 gate-of-record confirm (frontend.yml green on Nick's pushed SHA) · the GAP-1/GAP-2 (v1.1.2) + GAP-3/GAP-5 rulings intake.

**(2) CMD-API vs the ~JUL-24 TRIPWIRE** — if not yet landed: author/dispatch/audit/land CI-green with real margin (the banked debts: auth on the POST [OR-APP-AUTH installAuth posture; token-freeze day one] · IdempotencyEntry honored · ProblemType complete · wire-casing pinned at the endpoint vs the live camelCase precedent · the WebhookTrigger 4-grep route census · the B1-reduced command-lifecycle assertion surface; pre-verification ≥3 assumptions; host-CC lane; sequenced with the B2 port ahead of M14 per rider 3 — Jul-31 prices any slide, no re-ruling). What stacks behind it: C2/C3 suite ports · H2's AUTO suite · B2/B3 · the S31 confirm legs + the sanctioned best_effort re-emit · the two SKIPPED-stub flips · the AUTO usb scenario.

**(3) SKIP-VIS** (+ CORE-P1/P2 if Nick ruled GAP-1 — same file, one instruction: the resultOutcome carry + superseded-never-FAILED + honest-unconfirmed derivation at StandardExplanationService.java:617-:656 + the completedVerdict fix :288-:309). **FE-VERDICT-2** dispatches on the contract ruling (zero bench dependency).

**(4) THE M14 TRIGGER SWEEP (~Jul-28; boundary EOD Fri 2026-07-31):** conditions at v35 banking: certification ✓ · RO ✓ · AVAIL ✓ · zero open [REVIEW]s ✓ · SEED ✓ · G1 dispatched + returned + audited ACCEPT (verify the gate-of-record leg) · D1 5/5 (re-derive). Run on evidence; miss ⇒ post-launch arc #0, no re-ruling; the third-plug rider governs.

**(5) The programs (pointers, never copies):** Matter Phase C parallel (00_PROGRAM_STATUS §-beats; Q-A4-1 escalates through THIS hub if Doc-01-class prose must change; the spike = post-gate-read) · counsel: Pelton Mon 07-21 outcomes · TSDR #2 07-31 · veromo/LLC · R-1 HELD for G-2.

**(6) Standing ledger (surface until closed):** HUE-RESET · LEARN-PERSIST · the Rosonway dongle-behind-hub move + I3b [S] (boot-health re-run FIRST; eligible once BENCH-CONST greens) + the usb-power flip · Shelly Gen4 §5.3 provisioning (ident-gate S4PL-00116US) · the doc-drift ×3 docs commit · corpus entries (joins night + wave2 §4) · skills currency folds (the v34/v35-era classes) · H3 Aug 8–9 · E3's public statement · ZBDongle-P STOWED · frontend.yml confirm-once · LICENSE (consumes R1a) · Node-20 · the V1.x charter row (post-gate) · **v37 BANKED BEFORE DEPTH.**

**Success:** v35's tail closed clean → CMD-API landed CI-green pre-tripwire → SKIP-VIS authored/landed → the sweep run on evidence with NOTHING outstanding → every MUST row's producer running for the Aug-14 freeze — every incident a scenario, every scenario a permanent asset.
