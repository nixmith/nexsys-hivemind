<!--
file: context/handoff/2026-07-28_skills-currency-pass_lane_session_prompt.md
purpose: Session prompt for THE SKILLS CURRENCY + OPTIMIZATION PASS (Nick-authorized v39 beat 4; executed as a dedicated lane per the full-bar-at-fresh-context law). Re-grounds all three role skills to the post-v39 deployed truth, folds the v39 arc's disciplines, encodes the long-term north star as pointers, and COMPRESSES the mastheads for token economy — no rule dropped, every claim at its honest tense.
audience: a fresh Cowork session (the skills-pass lane). Load NO role skill as authority — the live mirrors are the OLD versions by definition; the SOURCES you edit are the truth you produce.
write-isolation (ABSOLUTE): exactly the SIX named target files across two repos + ONE return file (`context/audits/2026-07-28_skills-pass_return.md` — returns live in audits/). Nothing else. No spine writes (the hub stamps the intake), no core/bench writes, no mirror writes (`.claude/skills/` is read-only; Nick syncs).
laws: pointer-not-copy (a skill NEVER carries volatile project state — HEADs, counts, milestone status; it carries rules + pointers; `context/process/truth-hierarchy-and-pointer-not-copy-discipline.md` governs) · line-anchored edit anchors + post-edit seam reads + tail verification (env-model §5) · lock-free porcelain only · EXPLICIT-PATHS commits, exact counts, `git commit -F` from `_scratch/` · NO attribution trailers · L3 (nothing in this pass touches credentials — keep it so).
status: READY — authored 2026-07-28 (v39 hub, beat 10). Launch: `Read nexsys-hivemind/context/handoff/2026-07-28_skills-currency-pass_lane_session_prompt.md and execute it.` Nick's mirror sync follows the commits; v40 launches AFTER the sync (its Check 9 expects it).
-->

# Session Brief — The Skills Currency + Optimization Pass (post-v39; pre-v40)

**Why now:** the v39 arc changed ground truth under all three skills — the dashboard went from unservable to LIVE (G2 closed), the detail-read cluster was measured into precise mechanisms, and the company filed its long-term north star. Skills that teach yesterday's truth are worse than silence. Your second charge is token economy: these files are loaded every session by some agent — compress without losing one rule.

**Read first, in order:** (1) this brief, fully. (2) `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` incl. its filing frontmatter. (3) the three 2026-07-27 returns in `context/audits/` (deploy-evening · WCAP-2 · devtools-chain-glance — skim WCAP-1 too). (4) pm-handoff v39 beats 1–9 (the frontmatter chain suffices for most; beats 5–9 in full). (5) EACH target file COMPLETELY before editing it.

## The six targets and their deltas

### T1 — `nexsys-skills/orchestrators/nexsys-frontend/SKILL.md` → **v1.7** (the highest-value fix: §4d currently teaches a FALSE serve-path)

Re-ground to the deployed truth, and DELETE superseded interim text outright (git history preserves it; expired caveats are token waste):

- **§4d THE SERVE-PATH REALITY → THE SERVING REALITY:** DASH-SERVE landed (`c09c61c`) AND deployed 2026-07-27; the dashboard SERVES at **`/dashboard/`** behind the RULED posture-(A) exemption (GET/HEAD `/`, `/dashboard`, `/dashboard/**` only; every `/api/*` + `/internal/*` token-guarded); the **AuthGate paste-once flow + Bearer-on-every-fetch is PROVEN LIVE**; production builds default the REAL transport at `window.location.origin`; **G2 CLOSED** on the live tile (the four honest rows verbatim); F5 re-pairs BY DESIGN (tab-memory token; posture-(B) cookie-session = the named post-gate successor); Node + warm seconds-class builds are standing Pi state.
- **NEW §4e-class truths (the lane's live-payload law):** **(1) THE PRESENT-BUT-NULL TRI-STATE SEAM** — live optionals arrive as `null`-beside-present-siblings (`resultOutcome: null` with `settled: true`; `reason`, `trigger.type`, `automationName`, `firingValue` all observed null) where mocks emit full-string or fully-absent; a fixture-green `.toLowerCase()` crashed the chain render AND killed the polling loop (the filed glance return) — **FE-LIVE-V112 item 1** = null-guards + an error boundary + the honest empty state, with **present-but-null test rows for EVERY optional key** and present-but-null mock exhibits. **(2) THE ERA BOUNDARY** — pre-~Jul-25-era runs serve skeleton chains lawfully forever (history is append-only): render the honest "no detail recorded for this run" state, never a silent empty card. **(3)** `actions[]` ⊂ `actionCount` (delay actions uncounted in the array — expect subset, never assert equality). **(4) IDENTITY ROTATION** — `automationName`/`trigger.type` null on rotated identities is the CURRENT lawful wire (render the honest fallback; AUTO-IDENT is the post-gate fix; never treat the nulls as errors). **(5) STATE-DIALECT pending** — `/state` is core-nonconformant TODAY (fractional epoch-SECOND instants · `{"value":…}` nesting · raw `{msb,lsb}` entityId); the drawer stays honest-degraded until BOTH halves land; the FE half (h) consumes the contract's AMD-52 `{"t","v"}` + ISO-8601 + string ULIDs; **never parse epoch-seconds as milliseconds** (the arithmetically-proven 9:52 AM lesson) and qualify displayed timestamps with dates (g). **(6) THE ERROR-POSTURE LAW (f)** — one surface family, one posture: the honest error card (the Activity 404 is the exemplar); the eternal spinner and the silent empty card are the two filed anti-exhibits. **(7)** riders: the `/dashboard/`-scoped favicon (never an allowlist widening) · the npm-audit `1 high` advisory rides the next deps touch · the five-modes vocabulary is now OBSERVED RAW live (the verbatim "DefaultResponse SUCCESS +90 ms, then no report, ever" — cite the WCAP-2 return as the wire exhibit).
- Status line → v1.7 with a compact delta record; every claim above carried as POINTERS to the three filed returns + pm-handoff v39 beats (never inline HEADs/counts beyond what the rule itself needs).
- **Compression:** sweep the whole file for expired interim guidance (the v1.4/v1.5-era "until it lands" caveats, the pre-deploy mock-only FLAG framing where retired) — delete, don't stamp; the standing graceful-degrade guidance for pre-v1.1.2 payloads STAYS (it is current law for old history).

### T2 — `nexsys-hivemind/project-manager/SKILL.md` (masthead currency + THE COMPRESSION)

- **+v39-arc disciplines line** (pointer style, detail in pm-lessons + pm-handoff v39): **(1) NEVER AUTHOR ON AN UNMEASURED HOP** — L1's sharpening; instrument-first paid ×3 in one arc (WCAP-1/WCAP-2/the dev-tools glance split one "broken dashboard" into a client crash + a history artifact + a dialect drift + a design discovery; theory would have shipped a wrong P1). **(2) THE BUILD-GRAPH SEAM CLASS** — "X stays out of the gate" needs a MECHANISM, not an intention (test classpaths extend runtimeClasspath and resolve project jars; the npm-into-check catch: staging out of `src/main/resources`, npm-on-the-jar-task-only, the consumer-side classpath exclusion, and a task-absence grep as the gate assert). **(3)** red-first prediction sentences CARVE OUT preservation fixtures (a fixture whose asserts ARE the preserved behaviors cannot red — predict it green-by-construction, disclosed). **(4)** RETURNS FILE TO `context/audits/` — state it in every brief's frontmatter (two same-day duplicate displacements taught it). **(5)** L2 addendum: any placeholder in an operator command gets an explicit fill-in-before-running warning line (the RUNID slip). **(6)** Check-9's remote-cache refinement — pointer to the preflight reference (T3).
- **+the north-star pointer** (one line, pointer-not-copy): the long-term research/strategy north star is the agent-substrate thesis — *the harness enforces, the model only proposes* — at `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` (read when a brief touches strategy/positioning/the agent layer; its filing frontmatter carries the verified-at-filing honesty state; gate sovereignty governs until Aug-16).
- **THE COMPRESSION (the token-economy half):** the masthead's three arc blocks (v35-arc · v32-arc · bench-arc) collapse into ONE compact indexed list — each rule keeps exactly its operative sentence + its detail pointer (pm-lessons date / the playbook / the doctrine file); no rule is dropped, no rule loses its pointer. **The verification is a RULE CENSUS: enumerate the named rules before the edit and after — the counts MUST be equal and every name must survive.** Do the same trim inside the durable-disciplines paragraph where a sentence merely restates a pointed-to document.

### T3 — `nexsys-hivemind/project-manager/references/freshness-preflight.md` (Check 9 refinement)

Check 9 gains the v39 beat-4 finding: **a REMOTE hub's in-session skill copies are a THIRD location** (account-synced into cloud sessions, lag-prone) distinct from Nick's local `.claude/skills` mirror — the location Check 9 actually governs and the one host-CC lanes read. A remote hub observing stale in-session copies must adjudicate WHICH location is stale before flagging; Nick's `diff -rq` remains the mirror's instrument of record; a remote session may md5 SOURCE-vs-its-own-cache only to characterize its OWN staleness, never to conclude the mirror's. Fold as a short subsection under Check 9 + one line in the output-format example.

### T4 — `nexsys-hivemind/project-manager/references/coding-instruction-format.md` (additions #18–#19, appended beside #16/#17)

**#18 — Red-first predictions carve out preservation fixtures.** A test whose assertions ARE the HEAD-preserved behaviors cannot red at HEAD by construction; the instruction predicts it green-by-construction (disclosed) instead of "all new tests red." Origin: DASH-SERVE I4 (the spec itself named the fixture "the preservation fixture," then predicted it red — hub imprecision, Coder disclosure lawful).
**#19 — The unmeasured-hop gate (instrument-first, at authoring).** An instruction premised on a LIVE-behavior claim (what the deployed system serves/emits/renders) either cites the measurement (a filed capture/return) or ORDERS the instrument first and authors on its pastes. Theory never substitutes; a wrong premise ships a wrong P1. Origin: CHAIN-HYDRATE — minted P1 on a plausible server-side theory, dissolved by two ten-minute capture blocks (WCAP-2 + the dev-tools glance) into a small FE fix + a P3 history note.

### T5 — `nexsys-hivemind/coder/SKILL.md` (masthead pointer line)

+one v39-arc line (pointer style): the two 2026-07-27 coder-lessons entries are STANDING (Kotlin-DSL `java`-extension shadowing — import, never inline-FQN, `java.*` in build scripts; `--rerun` is per-task — force EVERY task, prove forced-fresh from executed-state lines + result mtimes, never the green summary; git-invisible empty dirs flip NO-SOURCE states machine-to-machine — reconcile any state variance to its mechanism before quoting a gate). +the same one-line north-star pointer as T2 (the coder reads it only when a WU touches the agent layer).

### T6 — `nexsys-hivemind/coder/references/freshness-preflight.md` (mirror the Check-9 refinement)

The Coder-side subset gains the same remote-cache note in its skill-sync check (shared-protocol law: both preflight files carry Check-9-class changes).

## Execution protocol

1. Porcelain ×2 (hivemind + nexsys-skills; lock-free) — both trees must be CLEAN at start; record HEADs.
2. Per target: FULL read → edits (line-anchored; for T2's compression, draft the compressed block in `_scratch/` first, run the rule census old-vs-new, then apply) → post-edit seam read + tail check.
3. **The honesty sweep:** grep every edited file for volatile state (SHA-looking strings, milestone statuses, counts) accidentally introduced — a skill carries rules and pointers, never state. Also grep for the superseded phrases you deleted (zero hits = clean).
4. The return (`context/audits/2026-07-28_skills-pass_return.md`): per-target delta table · the T2 rule census (before/after, equal) · the honesty-sweep evidence · exact porcelain censuses.
5. Draft both commit messages in `_scratch/` (`2026-07-28_hivemind_skills-pass_commit-msg.txt` · `2026-07-28_skills_frontend-v1.7_commit-msg.txt`), each with "Stages exactly N" (expected: hivemind 5 M + the return ?? = 6 entries; nexsys-skills exactly 1 M) and hand Nick the two commit orders + **the mirror-sync order (⚠ before v40 launches)** in the return's closing block.

**Out of scope:** MODULE_CONTEXT files (Coder-owned, current) · the spine (the hub stamps the intake at v40 beat 1) · any hivemind/skills file not named above · pm-lessons/coder-lessons (already written) · the north-star document itself (its honesty table is the research lane's Charge 6).
