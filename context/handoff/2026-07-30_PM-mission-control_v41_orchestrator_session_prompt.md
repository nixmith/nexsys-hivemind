<!--
file: context/handoff/2026-07-30_PM-mission-control_v41_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v41) SUPERSEDING v40 (retired at launch after five beats: LAUNCH+B2-authored → the thesis filed+routed → the desk-return audit ACCEPT → the Pi-half intake [the evidence engine live; C3 CLOSED; the Hue power-topology finding] → the re-run + close [6 PASS + 2 instrument-caught FAILs under protocol; C2 2/3; the skills-pass-2 lane + this prompt banked]).
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick.
state-type: session prompt (standing orchestrator).
status: READY — banked 2026-07-30 (v40 beat 5). Launch on Nick's word; ONE hub at a time; v40 retires on this launch.
first-act hygiene (IN ORDER): (0) env-model §11 (lock-free porcelain ×5 — `git --no-optional-locks status --porcelain`, SPELL THE FLAG) + BOTH spine frontmatters BEFORE any spine write. (1) ENV §§1–5 + §10–§12 — §12 governs if remote; Filesystem-MCP scope VOLATILE (`list_allowed_directories` FIRST — it collapsed to core-only mid-v40 after a host-CC run); same-path re-stages serve a STALE cache (fresh-temp-name copies defeat it); the stage endpoint 400-rejects `.java`. (2) Cold-boot trio + truth-hierarchy + the 11-check preflight — EXPECTED ADJUDICATIONS: Check 2 = W31 through Aug-2 (W32 pre-mints at the Aug-3 boundary); Check 4 = backlog newest currency note v40 beat 1 (the beat-4/5 cell flips live in the criteria ledger + pm-handoff, deliberate); Check 8 ACTIVE expected 0; Check 9 per the third-location refinement (the skills-pass-2 lane edits skill sources — adjudicate WHICH location is stale; Nick's mirror sync is step 3 of that lane's order). (3) Archive the v40 prompt (context/handoff/2026-07-27_…v40… → archive/). (4) REQUIRED READS, priority-ordered: pm-handoff v40 beats 1–5 → the B2 return §§12–13 (context/audits/2026-07-28_B2_suite-port_return.md — the evidence corpus + the F-12/F-13 protocols) → the criteria ledger (C3 ✅ CLOSED · C2 2/3 · C4/H2 in-motion · G1→FE-LIVE-V112 item 1 · G2 ✅) → the TWO strategy documents (the north star + context/strategy/Substrate_Thesis_v0.md — the standing frame; the language law) → the skills-pass-2 return if landed (context/audits/2026-07-30_skills-pass-2_return.md) → the backlog head notes. Treat every HEAD/count as a CLAIM to re-derive.
baseline (RE-DERIVE at preflight — v40 beat-5-time claims): core **`c09c61c` DEPLOYED + SERVING** (G2 closed; the AuthGate live; fleet 6/6 @ 25065, ch20/0x774c) · bench **`5965151`** (the 9-leg suite of record; the rebind silicon-proven agree=True; ENGINE_VERSION banner live) · docs `a53f474` · hivemind = the v40 beat-5 commit (expected census 6: 4 M + 2 ??) · skills `fbb1be6` (FE v1.7 on main; +the pass-2 landing when it runs) · watermark AMD-99, 175/53 · GATE 21 M / 8 S · J1 FROZEN · **freeze Fri 2026-08-14 EOD · read Sun 2026-08-16** · TSDR #2 = 07-31 (intake Nick's report) · Pelton ~Aug-5 → G-2 = TWO acts → BRAND-G2-EXEC · HUE-RESET REC-PARKED through the gate · pending one-liners: AUTO-IDENT (REC post-gate) · the F-12 suite-order amendment (REC, evidence-conditional).
-->

# Session Brief — PM Mission-Control v41 (the B2 adjudication → B3 nightly → the small-fix stack → the docs fold → M14 → H3 → the Aug-10 dry-run → Aug-14 FREEZE → Aug-16 THE READ)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v40. Every v28–v40 discipline carries — headline additions of the v40 arc: **FILED-MEASUREMENT-FIRST RE-PIN** · **FULL-CORPUS SWEEP on dialect findings** · **fixture predictions from the POST-change code path** · **operator-block hygiene ×4** (STOP-gates own block · in-block WHERE · full paths · verified verbs) · **INSTRUMENT-FIRST ON REPEAT FAILURE** (never rule a third theory before the bundle read) · the STATE-DIALECT↔scenario-library sweep coupling · returns to audits/ · L1/L2/L3 · EXPLICIT-PATHS COMMITS ABSOLUTE · NO ATTRIBUTION TRAILERS · two-layer audits extend to your own subagents. The hub never implements. North star (strategy): the agent-substrate thesis + the Substrate Thesis companion — *the harness enforces, the model only proposes*; **the language law: the deterministic floor is MISSING, not SUPERIOR.** North star (engineering): never-false-CONFIRMED / never-false-ALIVE — held across both suite runs of record.

**Charges, priority order:**

**(1) Beat-1 reconciliation:** the v40 beat-5 landing census-exact (6: 4 M + 2 ??) · the skills-pass-2 landing intake IF run (two commits + ⚠ mirror sync; audit its return) · the v40 prompt archived · the preflight with the pre-declared adjudications · drift check.

**(2) THE B2 ADJUDICATION (F-12 + F-13; the return §13 carries the protocols + per-hypothesis predictions — the instrument speaks before any ruling).** Nick's evidence block (Pi, one paste, ~3 min):
```
ls ~/hs-bench/bundles/command-s31-settle-20260730T013737Z ~/hs-bench/bundles/command-confirm-s31-20260730T013743Z ~/hs-bench/bundles/usb-reenumeration-20260730T013837Z
for f in ~/hs-bench/bundles/command-s31-settle-20260730T013737Z/* ~/hs-bench/bundles/command-confirm-s31-20260730T013743Z/* ~/hs-bench/bundles/usb-reenumeration-20260730T013837Z/*; do echo "===== $f"; head -80 "$f"; done
```
then the F-13 discriminator (eyes on the log; full paths):
```
LOG=$(~/nexsys-bench/tools/bench.sh log)
uhubctl -l 3-2.4 -p 2 -a cycle -d 10
sleep 40
grep -E "transport_failed|port_unhealthy|reopened" "$LOG" | tail -8
```
Adjudicate per §13's predictions → rule the fix (F-12 REC: the suite-order amendment — settle to slot 3, confirm-s31 to slot 7, the intervening Hue legs = the proven ~20 s-class gap; amends Nick's immediately-before ruling → HIS one-liner; F-13 per the discriminator) → ONE small ratified bench commit → **the suite re-run → H2 full-green + OBS-CONFIRM retires + the C4 estimator feed begins.**

**(3) B3 NIGHTLY AUTHORED (the C1 engine; the rider-#1 gate is SATISFIED).** BENCH-AUTOSTART ruled there; the first night = the (re-ruled) 9-leg suite; the morning one-line digest; the flight-recorder bundle on failure; full suite pre-milestone (charter B3). RIDERS: the C2 rejoin-race OPERATOR port (the named third class — criteria C2 cell) · the I5 boot-demo fixture re-mint (6-device era) · the I6 runner-demo README · the F-1 `bench.sh api_token` verb (one case line) · C1 arithmetic restated at authoring (~200 target; nightly yield). **M14 yields to gate rows on ANY contention — always.**

**(4) The small-fix stack, priority order:** **STATE-DIALECT** (core P2 + FE (h); the A3 conformance per the DRIFT-1 precedent; **MUST carry the bench scenario-sweep rider** — every `.value` path re-pins at its landing; `firingValue: null` priority ruled at its authoring) · **FE-LIVE-V112** (item 1 FIRST [null-guards + error boundary + honest empty state — the G1 blocker] + (a)–(i) + the favicon rider + the npm-audit advisory) · the era-boundary bounded core look (P3).

**(5) THE DOCS FOLD + pm-lessons (one docs commit + one hivemind fold):** INV-SE-02 in-place + Doc 13 §3.2–§3.3 as-built + the doc-drift ×3 · env-model §12 gains spell-the-flag + the Filesystem-scope-collapse exhibit · pm-lessons gains the v40 fold (the five-lesson queue: filed-measurement-first · corpus-sweep · post-change fixture predictions · operator-block hygiene · instrument-first-on-repeat).

**(6) THE RESEARCH-LANE LAUNCH + INTAKE (Nick runs it at leisure — RECOMMEND EARLY: the SCITT clock is the one short window).** The brief is amended with BOTH inputs (`context/handoff/2026-07-27_agent-substrate-research_lane_session_prompt.md` + its 2026-07-28 addendum — Charges 1–7 incl. the thesis verification + the SHORT-clock adjudication). Two-layer audit the return; the honesty table routes any north-star edits; the 3–5 research rows queue for the launch-runway charter.

**(7) THE M14 WUs** — margin-gated behind B3 running + the stack triaged; ident gate `S4PL-00116US` capture-verbatim at unbox; DUT-of-record + ONE unassigned spare; Gen4s stimulus-only forever.

**(8) THE GATE CALENDAR: Aug-8/9 H3 [M]** (clean-image fresh install; NO Node on the target — the .deb ships built artifacts) → **~Aug-10 THE DRY-RUN** (all 29 rows re-statused vs ARTIFACTS) → **Aug-12–13 I2 [M] + close-out + v42 BANKED + THE LAUNCH-RUNWAY CHARTER** (REC-LAUNCH-GATE: Nov-25 run like the gate; candidates: AGENT-SEAMS · AUTO-IDENT · posture-(B) · EVENTS-API (+/health) · **STD-SCITT** · the research rows; + the I2-adjacent: CMD-API-ACTOR carries the reason-surfacing candidate note) → **Aug-14 EOD FREEZE → Aug-16 THE READ** (labels are claims, artifacts are evidence).

**(9) Counsel + G-2 wait-states:** TSDR #2 report intake (07-31) · Pelton ~Aug-5 → the results consult → **G-2 = exactly TWO acts (R-1 + the filing go)** → BRAND-G2-EXEC · tamoro.com doctrine stands · HUE-RESET parked through the gate (post-gate: wall power + the named re-points).

**(10) THE LANE MAP (standing; dispatch per the routing matrix):** host-CC Claude Code = core/bench desk WUs (targeted allow-lists; mirror-sync-gated) · fresh Cowork = FE lane (FE-LIVE-V112), research/strategy lanes, skills passes (dedicated fresh lanes at the full bar) · in-conversation agents = micro-WUs + read-only grounding · the hub = authors, dispatches, audits, NEVER implements. Deep-research charters lawful now at zero gate cost: the agent-substrate lane (ready); post-gate candidates: a market/GTM evidence lane (the thesis §5-unproven #2 — willingness-to-pay) + the Wi-Fi-sensing application-layer scan (both queue for the launch-runway charter; NO new lanes pre-freeze without Nick's word). **v42 BANKED BEFORE DEPTH.**

**Success:** the two FAILs adjudicated on bundle evidence and the suite full-green within days → B3 compounds C1 nightly → the stack lands in priority order → docs/skills currency re-grounded → every MUST producer green with margin at the Aug-10 dry-run → **the Aug-16 read is a formality** → the launch-runway charter stands ready with the agent-substrate program + STD-SCITT shaped behind it. Every incident a scenario, every scenario a permanent asset.
