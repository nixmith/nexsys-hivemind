<!--
file: context/handoff/2026-07-30_skills-currency-pass-2_lane_session_prompt.md
purpose: Session prompt for SKILLS CURRENCY PASS 2 — fold the v40 arc's lessons + THE RESEARCH-AMBITIONS LAYER (the two strategy documents as the standing frame) into the role skills and their references. Dedicated fresh lane per the full-bar-at-fresh-context law (the beat-10 precedent).
audience: a fresh Cowork session. Load NO role skill as identity; you EDIT the skill SOURCES. The writable sources: nexsys-hivemind/{project-manager,coder}/ + nexsys-skills/orchestrators/nexsys-frontend/ + nexsys-hivemind/context/process/bench-troubleshooting-playbook.md.
write-isolation (ABSOLUTE): exactly the SEVEN target files below + ONE return at context/audits/2026-07-30_skills-pass-2_return.md. No spine writes, no core/bench/docs writes, no mirror writes. Lock-free porcelain = `git --no-optional-locks status --porcelain` (SPELL THE FLAG). NO attribution trailers. Returns file to context/audits/ (state it in the return frontmatter).
laws: pointer-not-copy (NO volatile state enters a skill — no HEADs, no cell statuses, no dates-as-current-state; the honesty sweep on ADDED LINES is mandatory, per-file, in the return) · every edit cites its carrier record (pm-handoff v40 beats 1–5 + the B2 return §§12–13 + the two strategy docs) · any compression proves an EQUAL RULE CENSUS · read every target file IN FULL before editing it.
status: READY — authored 2026-07-30 (v40 hub, beat 5). Launch: `Read nexsys-hivemind/context/handoff/2026-07-30_skills-currency-pass-2_lane_session_prompt.md and execute it.`
-->

# Session Brief — Skills Currency Pass 2 (the v40 lessons + the research-ambitions layer)

Read FIRST, in order: `context/handoff/pm-handoff.md` v40 beats 1–5 (the arc record) · `context/audits/2026-07-28_B2_suite-port_return.md` §§12–13 · `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` · `context/strategy/Substrate_Thesis_v0.md` (its §11 precedence law governs). Then execute the seven targets. You report; the hub audits at v41.

**T1 — `nexsys-hivemind/project-manager/SKILL.md`.** (a) The `arc-disciplines` list gains the v40 items, each ONE operative sentence + pointer (census grows 23 → 28; every existing name survives — prove it): **(24) FILED-MEASUREMENT-FIRST RE-PIN** — a wire re-pin checks the filed measurement corpus before any source read; an endpoint-feed source pin never outranks a wire measurement (the F-6 /state dialect miss; carrier: B2 return §12/F-6). **(25) FULL-CORPUS SWEEP ON ANY DIALECT FINDING** — the license to fix a wire-dialect defect extends to a same-beat corpus sweep for the class (§12.7-A: occurrence six survived to the suite). **(26) FIXTURE PREDICTIONS DERIVE FROM THE POST-CHANGE CODE-PATH ORDER** — never the pre-change semantics (the R1 empty-chain parenthetical; carrier: format #18's sibling). **(27) OPERATOR-BLOCK HYGIENE IS FOUR RULES** — STOP-gates get their OWN paste block · WHERE-labels ride INSIDE the block as a leading comment · commands use FULL PATHS, never PATH assumptions · every named verb/flag is verified to exist before it ships (the F-1/F-5/F-10/F-11 quartet; carrier: playbook §8). **(28) INSTRUMENT-FIRST ON A REPEAT FAILURE** — the same leg failing twice under two theories buys a bundle/evidence read BEFORE any third ruling; never retune blind (F-12/F-13). (b) **The strategy-layer pointer AMENDED** to name BOTH documents: the north star (the technical thesis) AND `context/strategy/Substrate_Thesis_v0.md` (the market/paradigm layer: the substrate bet, the L0–L3 layering, the pattern library, §5/§9 as the evidence discipline) — read when a brief touches strategy, positioning, the agent layer, or research; **the language law rides the pointer: the deterministic floor is MISSING from the field, not SUPERIOR — L2/L3 without L1 are unsound, L1 without L2 is insufficient; never restate otherwise.** Gate sovereignty through Aug-16 stays in the line.

**T2 — `nexsys-hivemind/project-manager/references/coding-instruction-format.md`.** Two additions in a dated 2026-07-30 section: **#20** the filed-measurement-first re-pin law (instructions that pin wire paths MUST cite the filed measurement or order one — the measure-then-pin pattern is the compliant shape); **#21** the dialect-sweep obligation (any instruction fixing a wire-dialect defect carries the full-corpus sweep as an in-scope act).

**T3 — `nexsys-hivemind/coder/SKILL.md`.** Masthead only: + a v40-arc conventions line (the #20/#21 pointers · operator-block hygiene when authoring paste-blocks · instrument-first-on-repeat-failure) + the SAME two-document strategy pointer with the language law (one line).

**T4 — `nexsys-hivemind/coder/references/freshness-preflight.md`.** One line if absent: the spell-the-flag porcelain form in any command example.

**T5 — `nexsys-hivemind/context/process/bench-troubleshooting-playbook.md`.** §8 gains the four operator-block hygiene rules (27 above) as numbered addenda with their incident pointers; plus ONE correction wherever the playbook or its examples assert a blanket "15,000 ms tuned confirmation window": the per-command `ExpectedOutcome` windows at source are the instrument of record (on/off measured ~5 s — B2 return §12.8/F-8); plus the interim auth form for operator curls: `$(cat ~/hs-bench/config/initial_api_token)` inline (the `bench.sh api_token` verb does not exist yet — B3 adds it; never echo the token).

**T6 — `nexsys-skills/orchestrators/nexsys-frontend/SKILL.md`.** Small: + the two-document strategy pointer (one line, with the language law) noting the FE's strategic role in it — the explainability hero IS the false-positive-cost mitigation the Substrate Thesis names (§6): honest explanations are what make an enforcement gate socially survivable. No other FE truth changed in v40.

**T7 — `nexsys-hivemind/project-manager/references/freshness-preflight.md`.** Only if a command example lacks the spelled flag; else untouched (state which in the return).

**Return format** (`context/audits/2026-07-30_skills-pass-2_return.md`): per-target delta table (bytes/lines before→after) · the T1 rule census BEFORE/AFTER with every name listed · the honesty sweep per file (added lines only: volatile-state phrases / SHAs / count-claims — expected NONE beyond immutable landing anchors) · exact porcelain censuses per repo · the two commit orders (hivemind: the edited hivemind files + the return; nexsys-skills: 1 M) + ⚠ THE MIRROR SYNC order before any host-CC lane · route-back: intakes at v41 beat 1.
