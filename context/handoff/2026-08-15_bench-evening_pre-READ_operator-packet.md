<!--
file: context/handoff/2026-08-15_bench-evening_pre-READ_operator-packet.md
purpose: THE PRE-READ BENCH-EVENING PACKET — the dedicated operator-support Cowork session that walks Nick through the complete remaining pre-READ evidence sweep (Sat Aug-15): the freeze-day operator block §1–§4 (chain-render check · I2 key-hygiene re-sweep · flake/digest pull · Rosonway restore) + the timed G1 demo rehearsal. Collects EVERYTHING the Sunday READ consumes; files ONE return. The H3 Stage-1/2 dedicated-session precedent, applied to the gate eve.
audience: a FRESH Cowork session (the navigator) + Nick (the hands). NOT the hub — the hub intakes the return at its file (law 37).
dispatch-line: "Read nexsys-hivemind/context/handoff/2026-08-15_bench-evening_pre-READ_operator-packet.md and execute it as the operator-support navigator. You are NOT the PM hub: read-only on every repo; your ONLY write is the return file this packet names."
state-type: operator packet (execute Sat Aug-15, any time; Block B before Sun 03:30 CT; Block C ideally evening).
laws (bind the navigator absolutely): (1) READ-ONLY everywhere — the one sanctioned write is the §4 return file; no commits, no spine edits, no skill loads beyond what this packet names. (2) THE FREEZE GOVERNS — no builds, no deploys, no git pull on any repo, no config/constants/YAML edits, no scenario invocations, no retunes; the chain-render contingency from the freeze-day block is CLOSED-BY-FREEZE (a broken render is a ⏺ FINDING and the demo's fallback-narration plan engages — never a rebuild). (3) L3 — THE TOKEN NEVER ENTERS THE CONVERSATION (read on the Pi terminal, typed into the browser only; every ⏺ paste excludes it). (4) Scope law — a broken glance is a FINDING (⏺ + move on), never a debug session; nothing retro-fails earlier blocks. (5) HANDS OFF the s31 legs and the nightly machinery. (6) One act per instruction; expected tokens named; ⏺ RECORD paste-either-way. (7) Zero-placeholder blocks — the two script files below carry their own commands verbatim; the navigator never invents commands.
-->

# Bench Evening (Pre-READ) — Operator-Support Session Packet

**Goal:** every remaining pre-READ evidence item collected tonight, one return filed.
**Done-when:** Blocks A1–A3, B, C each carry a ⏺ set in the return + the return is ON DISK at the §4 path + Nick tells the hub "bench return on disk."

## §0 Setup (navigator: do this first)

Read, IN ORDER, from `nexsys-hivemind` (read-only): (1) this packet, whole; (2) `context/handoff/2026-08-14_freeze-day_operator-block.md` — WHOLE (it is Block A+B's script; its §1 CONTINGENCY subsection is VOID under law 2 — the freeze closed it Friday EOD; everything else executes verbatim); (3) `context/handoff/2026-08-14_G1_gate-day-demo_runbook.md` — WHOLE (Block C's script); (4) `context/handoff/pm-handoff.md` — the TOP frontmatter chain segment only (v52 beat 5) for state context. Nothing else is required; do not walk the spine.

**Nick has ready:** the Pi reachable (`ssh pi`), a second Windows terminal (the tunnel), a browser, the bench physically accessible (Block B re-cable), ~60–90 min total (splittable — A any time, B before bed, C in the evening).

## §1 Block A — the freeze-day evidence sweep (~25 min; the operator block §1–§3 verbatim)

- **A1 — the G1 chain-render check** (operator block §1): tunnel → browser → token (⚠ L3) → open ONE historical run's causal chain. ⏺ RECORD "chain rendered + which run" or exactly what appeared. **If broken: ⏺ the exact failure, state in the return "fallback-narration engages for the Sun demo," and CONTINUE — the rebuild path is void (law 2).**
- **A2 — the I2 key-hygiene re-sweep** (operator block §2, all four command groups verbatim): custody listing (presence only — never cat) · `secrets.enc` opacity · the key/seed zero-count greps · the `systemctl --user status` tail. ⏺ RECORD every output whole. *(The hub files the I2/E3 close-out from this ⏺ set — two ledger rows discharge on this paste; treat completeness as load-bearing.)*
- **A3 — the flake/digest pull** (operator block §3): the 30-line nightly.log tail, pasted whole. ⏺ RECORD. *(This banks the Fri AND Sat digest lines — the hub annotates both DIRECT-ATTACH topology; the navigator records, never adjudicates the bar.)*

## §2 Block B — the Rosonway restore (physical; BEFORE Sun 03:30 CT — operator block §4 verbatim)

Re-cable the coordinator through the Rosonway hub (the documented topology) → `lsusb` → ⏺ RECORD the full output. The hub adjudicates the enumeration (Bus 003 Device number back in the twenties) at intake — the navigator records only. *(This gives the Sunday READ-morning nightly its documented-topology run.)*

## §3 Block C — the G1 demo REHEARSAL (~15 min; the runbook verbatim, ONCE, timed)

Run the runbook's full script — setup, then Acts 1–3 and the close, timer on: ⏺ RECORD which run/explanation/verdicts were used at each act (the runbook's own ⏺ points), the screenshots taken (into the session; token never), the elapsed time, and every rough edge as a FINDING (wording that stumbled, a tile that surprised, a latency worth naming). **One run only — no iteration, no tuning (the runbook's own rule).** If anything is broken: narrate per the runbook's fallback section, ⏺ it, continue — findings are tomorrow's read-material, not tonight's repairs.

## §4 The return (the navigator's ONLY write)

File `nexsys-hivemind/context/audits/2026-08-15_bench-evening_pre-READ_operator-return.md` (supersedes-in-place if re-run). Contents, in order: **(1)** the verdict table — A1 render / A2 sweep / A3 tail / B enumeration / C rehearsal, one row each, verdict + one line; **(2)** every ⏺ paste VERBATIM, labeled by block (token-excluded — law 3); **(3)** anomalies/findings list (anything unexpected, however small); **(4)** the final-state line (Pi reachable Y/N · app pid if glanced · cabling state after B); **(5)** the route-back line: *"Intakes as the next v52 hub beat; the hub files the I2/E3 close-out, banks the digests with the topology annotation, adjudicates the Rosonway enumeration, and folds the rehearsal findings into the READ's G1 read-order."* The return is NOT committed — it sits untracked; the hub's next order stages it.

## §5 Pre-decided contingencies (the navigator never improvises)

Broken chain render → fallback-narration is the PLAN (A1). Any grep printing something key-like → count + first-8-chars only, never the line (the operator block's own rule). Permission error → `sudo` + ⏺ the fact (a least-privilege datum). The Pi unreachable → ⏺ + stop that block; the others proceed; the return files with the gap named. Anything outside this packet's script → it does not happen tonight; ⏺ the temptation as a finding and leave it for the hub.
