<!--
file: context/handoff/2026-08-16_THE-READ_navigator-packet.md
purpose: THE READ (Sun 2026-08-16) — the dispatch packet for the dedicated gate-walk session: the navigator/evidence-steward at Nick's side for the 21-MUST ledger walk on frozen evidence + the G1 live demo (LAST) + the verdict per the ledger's own §0 rule. Self-contained per the operator-packet law: everything the session needs is in this file or at a named path — it reads no chat.
audience: the fresh READ session (navigator/evidence-steward — NOT the hub); Nick (gate authority — he rules every row).
state-type: dispatch packet (gate walk; one return write).
dispatch-line: Read nexsys-hivemind/context/handoff/2026-08-16_THE-READ_navigator-packet.md and execute it. - /nexsys-project-manager
laws (BINDING, frontmatter-level): (1) you are the NAVIGATOR, never the hub — the single-spine-writer law holds; your ONE sanctioned write is the return at context/audits/2026-08-16_THE-READ_gate-record_and_verdict.md (uncommitted; the hub stages it). (2) READ-ONLY on every repo and every host surface beyond that one write. (3) L3 — THE TOKEN NEVER ENTERS THE CONVERSATION (Nick reads it on the Pi terminal and types it into the browser only). (4) THE FREEZE GOVERNS — no builds, deploys, git operations, config/constants/YAML edits, scenario invocations, retunes, or restarts; a broken surface is a FINDING narrated honestly, never a fix. (5) HANDS OFF the s31 legs and the nightly machinery. (6) Labels are claims, artifacts are evidence — every row is read against its named artifact, never memory. (7) The session date banner is UTC — the operator's calendar day is America/Chicago; re-derive before any calendar-gated statement. (8) Nick rules; you steward evidence and record. (9) Deviations are DECLARED in the return, never silent. (10) Chat is not a storage tier — everything the hub needs lands in the return file.
-->

# THE READ — Navigator Packet (gate walk, Sun 2026-08-16)

## §0 Launch reads (IN ORDER, before any act)

1. This packet, whole.
2. `context/assessments/2026-07-11_go-no-go-criteria_draft.md` — **THE RATIFIED LEDGER** (J1 FROZEN), WHOLE: the RATIFICATION block, every row, §0's verdict rule, §4's gate-day protocol. This is the gate artifact; this packet operationalizes it and never overrides it.
3. `context/assessments/2026-08-09_S9_dry-run_criteria-confirmation_v50-beat-4.md` — the dry-run pass (per-row statuses as of Aug-9; 18/21 closed + 3 scheduled-by-design; projects GO).
4. `context/audits/2026-08-16_G1_rehearsal_complete-record_and_gate-day-brief.md` — the rehearsal record: **§10 IS the G1 read-order of record (as amended in §2 below)**; §5 the DX findings; §15 the addendum.
5. `context/audits/2026-08-16_G11_pre-fire_recovery_instrument-record.md` — the rig recovery + the banked D-2 evidence line.
6. `context/handoff/2026-08-14_G1_gate-day-demo_runbook.md` — the demo mechanics (setup, L3, break-narrate-don't-fix).
7. On demand: `context/audits/2026-08-15_bench-evening_pre-READ_operator-return.md` (F-0, the rig truth of record) · `context/assessments/2026-08-13_S10_decision-skeleton_v52-beat-1.md` §D (the D-1/D-2/C-1 ruling text verbatim) · `context/assessments/2026-08-14_S10_close_ranked-program.md` (the post-gate program the verdict feeds).

## §1 Settled inputs — read them, do not re-adjudicate them

- **The Aug-16 digest (the final pre-READ instrument):** `2026-08-16 quiesced AUTO floor: 7/9 · FAIL command-confirm-s31 · bundle …/command-confirm-s31-20260816T083217Z · 1 SKIP(hue-online) · bench-hero RESTORED ✓ · ON-latency n/a(FAIL)`. **THE v53 FALSIFIABLE PREDICTION CONFIRMED** — the first radio-present fire on the hop-verified rig; all four coordinator-absence legs (boot-health · command-supersession · command-identify-honest · usb-reenumeration) returned to PASS; the only FAIL is the chronic s31 honest flake. Hub-adjudicated CONFIRMED (v53 beat 2).
- **The evidence window at the read:** the product record runs **THROUGH AUG-13 + AUG-16**. **Aug-14/15 are COORDINATOR-ABSENT / RIG-INVALID (G-6) and EXCLUDED from the flake distribution (G-7) — the exclusion is STATED ALOUD at the read**; the 3/9 nights are a POSITIVE honesty exhibit (the suite reported a missing peripheral exactly correctly).
- **The recovery chain (narrate ONCE, at D-2 or H2):** F-0 found at the instrument (the coordinator absent Aug-13 08:17 → Aug-15 16:38) → G-11 sanctioned recovery fired 01:08 CT (boot-health [PASS] 6/6 · 0 forbidden; pid 14718 holds `/dev/ttyUSB0`; boot-time acquisition PROVEN, the gap isolated to hotplug — DX-13) → the 03:32 nightly 7/9. **The banked D-2 line: the first radio-present positive on hop-verified cabling since Aug-13 — the full boot floor green at 01:08, re-proven by the nightly at 03:32.**
- **The s31 flake bound (D-2's number):** radio-present nights only — **FAIL 8 · PASS 6**; measured ON-latencies on file: 3.65 · 0.30 · 0.17 · 0.36 · 0.16 · 0.30 s. The consecutive-FAIL fresh read runs POST-GATE on the redesigned instrument (R-5) — never at the read.
- **The rulings in force:** D-1 POSITION 1 + HARDENED FENCE + the dagger stated at the read (the two DO-NOT-SAY items stand: "runs integrations" / "publishes events" — lifted only by R-4 post-gate) · D-2 YES with the caveats + the bound above · C-1 tripwire language · G-9 no read-order rebuild · **DX-B/DX-C: G1 runs per the complete record §10 AS AMENDED (§2)** · **DX-D STATE-IT + DX-E ADOPT (the composed Close line, §2)** · C1's counting basis is RULED (counts-by-event-type, positions citable on demand — gate day reads it, never argues it) · I3a's tier adjudication is RULED (the ratification block carries it — read, don't re-litigate).

## §2 The G1 amendments of record (fold into §10 as you run it)

1. **The Act-2 substitution line, stated BEFORE the exhibit (§15.3):** *"The row asks for a non-firing explanation including the no-change class; our why-not surface is down — we found it last night: the API answered perfectly in seventeen milliseconds, the dashboard threw on one unguarded null. So here is the no-change class from the run surface instead — I'm telling you it's a substitution rather than letting it read as the original."*
2. **The Close's final line (DX-D + DX-E composed — VERBATIM):** *"…across roughly seventeen hundred recorded verdicts of every class on this bench — of which the confirmations are one — this system has never once said a device acted when it hadn't. The layer that makes claims about actions is sound and measured. The layer that makes claims about itself claims more scope than it measures — we found that ourselves, under freeze, and it's now the first row in the post-gate program."*
3. Standing: skip the condition hop (DX-24) · never click Activity (DX-23) · exhibit 1 is ledger-bound — state the offline-light framing FIRST, before the amber wall is seen · break-narrate-don't-fix.

## §3 The walk (the ledger §4 protocol, operationalized)

0. **Pre-flight (before anything):** verify the gate-day HEADs read-only — `git log --oneline -1` per repo; expect core `d26777c` · bench `16e672d` · skills `f0b7a43` · docs `a53f474` · hivemind at the beat-2 commit (record its SHA). Any mismatch: STOP, record, route to the hub.
1. **Threshold check (~5 min):** the A-rows + the I1 watch (an INV-CH22-class live threat = the gate adjourns to a STOP-and-rule; don't walk 22 rows into a live incident).
2. **The MUST ladder (40–60 min), dependency order:** A1→A2→A3 · B1/B2 · E1/E2 · C1/C2/C3 · D1 · F1/F2 · H1/H2/H3 · I1/I2 · I3a — each row read against its NAMED artifact (the row text carries the path; S-9's 8-artifact sweep confirmed presence); Nick rules each row aloud; record the word + the artifact cited. Row notes: **H1** = the gate-day HEAD from step 0 (`d26777c`, green at CI #208 + install-smoke #28 on `ca0f41d`, banked law-16). **H2** = the digest line in §1 IS the cadence evidence; the recovery chain narrates here or at D-2. **I2** = PARTIAL-CLOSED as ruled (G-5: 3 of 4 legs; the opacity leg pends G-4 post-READ) — state it; it is not a red.
3. **The SHOULD sweep (~10 min):** A4 · C1b · C4 (report n and the classes; NO fabricated percentiles — the row's own mandate) · D2 · E3 (discharged with the I2 close-out note) · G2 · H4 · I3b — each green/red noted aloud; reds priced into the runway plan explicitly, never dropped.
4. **G1 — LAST AND LIVE (≤10 min):** the complete record §10 as amended (§2). Setup per the runbook (tunnel + token BEFORE the clock; L3). ⏺ RECORD each act + screenshots + the elapsed time.
5. **The verdict (~5 min):** apply the ledger §0 rule VERBATIM — all MUSTs green → **GO**; any MUST red → **NO-GO or CONDITIONAL-GO** (named remediation, a date ≤2 weeks out, a re-read of just the red rows; a second red read on the same row = NO-GO). Record Nick's word + every dagger annotation (claims fenced at the read for later lift).

## §4 The return (your ONE write)

`context/audits/2026-08-16_THE-READ_gate-record_and_verdict.md` — UNCOMMITTED (the hub stages it). Required contents, in order: **(1)** the per-row tally table — row · tag · status-at-read · artifact cited · Nick's ruling word; **(2)** the G1 ⏺ set — per-act records, screenshots transcribed, elapsed time; **(3)** THE VERDICT + dagger annotations, Nick's words verbatim; **(4)** the DRAFT ledger-append text (the ruling + per-row tally in the form the ledger §4 names — the HUB executes the actual append; you never edit the ledger); **(5)** declared deviations; **(6)** the route-back line: *"Intakes as the next v53 hub beat; the hub appends the ledger, points the bench record, files the verdict beat, and opens the ratification."*

## §5 Non-acts (bright lines)

No ledger edit · no spine writes (pm-handoff / PROJECT_SNAPSHOT are the hub's) · no commits or pushes · no fixes of anything · no Pi commands beyond the runbook's setup block (tunnel + token read — the digest is already banked; the bench is not touched again before the walk) · if a surface breaks mid-demo: narrate, ⏺ record, continue (three broken acts still complete as a FINDINGS record; nothing retro-fails a closed row).

*The gate is Nick's. The evidence is on disk. Walk it against the artifacts, record the words, file the return.*
