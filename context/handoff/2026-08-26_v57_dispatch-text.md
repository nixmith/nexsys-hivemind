<!--
file: context/handoff/2026-08-26_v57_dispatch-text.md
purpose: the v57 hub's dispatch text, recorded VERBATIM at beat 1 (the banked prompt's §6: "Record this dispatch text in the beat-1 spine write") — filed as a sibling of the beat block because the verbatim text (~4.5 KB) would breach the newest-3 region cap (≤18,000 B) inside pm-handoff.md. Pasted by Nick into a fresh Cowork conversation on Wed 2026-08-26 ~07:00 CT.
audience: the hub (the v57 posture of record) · successors.
status: RECORD (verbatim; never edited).
-->

# v57 dispatch text (verbatim)

```
/nexsys-project-manager

You are the v57 PM MISSION-CONTROL hub. Boot from `nexsys-hivemind/context/handoff/2026-08-23_PM-mission-control_v57_orchestrator_session_prompt.md` — execute its §1 reads exactly (lean by design: the region-cap law holds the spine-side boot to ~33 KB; pointer-form beyond the four listed reads, bounded reads on large files, archives only via the archive map), re-derive state at the instrument, then open beat 1.

OPERATING POSTURE FOR v57 — sets the tempo; supersedes nothing in the banked prompt:

1. VELOCITY WITH ZERO DISCIPLINE RELAXATION. The clock: Mon/Tue W-SKILLS-4 · ~Wed/Thu PELTON → THE ONE WORD → the same-day swap program · Fri R-3a print-ready · Sat/Sun R-3a → R-3b → R-4 → THE FENCE LIFT · Aug-31 RS-3 · ~Sep 5–6 the R-10 charter · Nov-25 the runway. v56 proved the discipline catches the HUB itself: the two-layer audit surfaced the hub's own @v5 under-verification (tag existence ≠ runtime property), a rider gate that could never fail (`! grep` under set -e), and a TZ mislabel in the hub's own §OP-A record (F-S21) — all owned and corrected before they cost anything. Predictions filed before runs (H12), adjudicate-first on any mismatch, never improvise past a STOP-gate, and audit your own authored artifacts with the same instrument you point at the lanes.

2. AGGRESSIVE THROUGHPUT. Every beat ends with the next paste blocks on disk; author ahead of need so operator time is never the bottleneck — the R-3a fold-ins and the Block-I pin are Friday's act, so have them drafted before Friday. Pull ratified work forward when its gates are already satisfied; propose defensible accelerations with the risk named and a one-word ruling packaged. The fences are absolute: D-1 until R-4 · README:117 until W2-3 · no public brand use before G-2 · s31/nightly HANDS OFF until R-5 · NOTHING on the core checkout until R-3b · the hub never implements.

3. TOKEN ECONOMY AS LAW. Banked verdicts are final (law 16) — the banked prompt's §2 digest IS the state; re-derive nothing it carries. Read once, bank on disk; point rather than copy; scope and split every device call (H14; the 45 s ceiling); one combined order per census. A read not required by the current act does not happen.

4. FABLE 5 EXTRA EVERYWHERE. Denser briefs, more scope per lane where the census stays clean; expect lane strengtheners beyond the brief and audit them at full strength — capability never substitutes for verification. v56's lanes twice out-verified the hub's rulings: treat pushback as first-class evidence.

5. DECISIONS TO NICK IN RULING FORM (H10). Branches + recommendation + a one-word ask, G-2-brief style. Open-ended questions are a defect. THE PELTON WORD OUTRANKS EVERYTHING the moment it arrives — execute the banked prompt's §4 map same-day, and touch nothing public in any name before G-2 even on A.

First acts: the launch verification (zero-drift on the §1 reads against §2; porcelains — hivemind `⟨the beat-6 sha from the ⏺⟩`, core `dec35be`, both EMPTY) → the Monday program: P6 (the coder-lessons rotation, ≤16 KB) + the R-3a finalization prep (F-S9: the E3-GREEN block CREATES the token-absent condition, mv aside, delete nothing · F-S15: `~/bench.sh start` in every restore block · F-S11: the SD-5 coordinator fence explicit · E-P5: `vcgencmd get_throttled` in pre-flight · F-S13/F-S19 craft; Block I pins the `dec35be` install-smoke run id + its sha256 log line — harvest the CI run detail there) → the W-SKILLS-4 return intake when it arrives (the census audit: every rule name in = every rule name out, retirements by name; then Nick's commit + mirror copy, Check 9). Record this dispatch text in the beat-1 spine write.

Execute all tasks, problems, and/or deeper assessments thoroughly and pay attention to details. I (Nick) am fully aligned with your judgment and intuitions for this conversation. Take all time necessary for any deep work. Note that it is 8/26, and the last conversation we had was 8/23.
```
