<!--
file: context/handoff/2026-09-12_v71_dispatch-text_verbatim.md
purpose: THE DISPATCH OF RECORD for v71 — Nick's first message to the v71 hub, filed VERBATIM at beat 1 per the standing order (his edits are the dispatch of record, filed verbatim by the hub that receives them).
audience: the v71 hub · the v72 hub (what v71 was asked, exactly)
state-type: operator paste, as received
status: FILED v71 beat 1 (Sat 2026-09-12 ~11:1x CT; instrument 2026-09-12T15:57:44Z at boot). Identity: the §3 line of the STABLE v67 prompt with <N> = 71, the STATE AT DISPATCH slot filled with the v70 close, PLUS the window's deliverable spelled out (FIX-2b-ii in two parts), the read order (the frame audit whole, then its §2 reads at source by range, sample #12 by the same greps), the follow-on acts (EXPLAIN-114a's re-baselined paste after (i) lands; HERO-1c; R-4c today), the REV-1/REPRO-1 charter condition, the do-not-re-read list, and the owed-lines slot (empty at dispatch).
-->

# v71 dispatch text — verbatim

You are the v71 PM MISSION-CONTROL hub for NexSys / HomeSynapse. /nexsys-project-manager  Boot from `nexsys-hivemind/context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` (the STABLE form — no state in it) and execute its §1 EXACTLY, inside its boot byte budget: date -u first; pm-handoff.md line 8 + the newest ONE beat; PROJECT_SNAPSHOT.md; `context/handoff/OPERATOR-BRIEF_for-Nick.md` whole (make sure instructions and guidance are unambiguous and completely, carefully written out for Nick) — its §HELD-BY-THE-HUB is the wait-state ledger; the decision record §3; the plan of record §3; the five HEADs in one call; the preflight as one line per check. THE OPERATOR-LOAD LAW and THE CONTEXT-BUDGET LAW (§1b) bind every message and every call. Beat 1: name the window's ONE deliverable, the intake at the bytes, this text verbatim, §HELD re-printed, hand me ONE act.

STATE AT DISPATCH (the record wins; verify each line at the instrument before you write): core ff1a6e1 (CI-1) — check GREEN, install-smoke GREEN ×2, the bus-soak job GREEN = the bus class's sample #13 — after bd5d35e (FIX-2b-i) and 6af76f7 (HERO-1b); hivemind <LANDED sha from act 1> (v70 beat 10, the close); docs 73c204b; all clean, pushed. THE WINDOW'S ONE DELIVERABLE: FIX-2b-ii's coding instruction, authored on the sample-#11 frame — read context/audits/2026-09-12_CI-bd5d35e_frame-read_bus-idle-run-handoff_v70-b10.md whole (6.3 KB): the bus is idle and correct (pending=0 on every subscriber, the engine's virtual thread parked at InProcessEventBus.liveLoop:536, every reader and the writer idle, no pinned trace, no thread executing the run); the automation run was initiated (automation_triggered is the head of the store, published at StandardRunManager:651 on the engine's own thread) and its first action never started — the class is the run pipeline's hand-off in core/automation, not the bus; SqliteCheckpointStore.writeCheckpoint is fire-and-forget, so the census's missed=1 is a token artefact. Then do the audit's §2 reads at source by range: StandardRunManager:600–:720 (what follows the publish at :651 — the condition step and HOW and ON WHICH THREAD the actions are handed off), SubscriberSupervisor.deliver's non-SUCCESS results, and sample #12 under _scratch/v71/ci-6af76f7/ by the same greps. Author FIX-2b-ii in two parts: (i) the instrument — two INFO lines in core/automation (automation.run_handoff naming the hand-off taken, automation.action_step_started at the executor's entry) plus the census's checkpoint-lag caveat, a fifteen-minute lane dispatched FIRST on the Java slot so every later push's bus-soak job samples it; (ii) the fix on the step the reads (or the next red) name. After (i) lands: hand the EXPLAIN-114a paste (context/instructions/2026-09-11_coder-lane_EXPLAIN-114a_read-API-v1.1.4-additive_coding-instruction.md §14 — re-cut its baseline clause to `git log --oneline eabdbb1..HEAD` listing exactly the landed subjects). Charter HERO-1c on web-ui (the HERO-1b return §3 D6/D7; the audit's SPEC §7 amendment; web-ui/dashboard/MODULE_CONTEXT.md). R-4c is TODAY at the rig (context/instructions/2026-09-11_R-4c_measurement-only_zdo-surface-C-003_navigator-packet.md §N; its record is v71's intake). If the hand-off reading is ambiguous after the §2 reads, charter REV-1 (a read-only adversarial second read of FIX-2a/2b-i and the frame) and REPRO-1 (a desk reproduction: the soak at K=200 under two carriers with a concurrent full check as load). Do not re-read the instructions already audited (FIX-2a, FIX-2b-i, HERO-1b, CI-1 — their audits under context/audits/ carry what matters). Owed lines I may paste: H8A: RETURNED <path> <bytes> or H8A: not run · FE: 6af76f7 frontend <green|red> · AMD100: ratify · PROTECT: done · ERIK: <his line>. My lines: <paste each one-line report here>.
