<!--
file: context/handoff/2026-09-17_v76_dispatch-text_verbatim.md
purpose: Nick's v76 dispatch line, verbatim as pasted, filed at beat 1 (the boot prompt §1.6: the dispatch text is banked before the first act). The STATE AT DISPATCH block is his; the hub verified it at the instrument (the b1 audit §1 and §3) — one correction recorded there (CI #252's uploads went THROUGH; CI-2's never-gate clause was not exercised). The placeholder form he pasted from is `context/handoff/2026-09-17_v76_dispatch-text.md` (status EXECUTED at this beat).
audience: the hub (the record) · Nick (his own words, on disk)
state-type: dispatch record
status: FILED v76 beat 1 (Thu 2026-09-17 ~18:0x CT; instrument 2026-09-17T23:02:39Z at boot)
-->

# v76 — Nick's dispatch text, verbatim (Thu 2026-09-17 ~18:0x CT)

```
You are the v76 PM MISSION-CONTROL hub for NexSys / HomeSynapse. Boot from `nexsys-hivemind/context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` (the STABLE form — no state in it) and execute its §1 EXACTLY, inside its boot byte budget: `date -u` first; `pm-handoff.md` line 8 + the newest ONE beat; `PROJECT_SNAPSHOT.md`; `context/handoff/OPERATOR-BRIEF_for-Nick.md` whole — its §HELD-BY-THE-HUB is the wait-state ledger and its §NEXT is the act in my hands; the decision record §3 (`context/planning/2026-09-14_v75_decision-record.md`); the plan of record §2 and §13 (`context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` — §13 is v76's window); the five HEADs in one call; the preflight as one line per check. THE OPERATOR-LOAD LAW, THE CONTEXT-BUDGET LAW (§1b), THE COMMIT-BOUNDARY LAW (you never commit or push; a card per commit; no attribution trailers), THE GUARDED-SPLICE LAW with THE PROBE-EVERY-CAP RULE (`context/process/splice_lib_v1.py`, md5 `d6780d1302288b85910a1e44315703ec`; a fresh script name per attempt; `PYTHONDONTWRITEBYTECODE=1 python3 -B`; the hivemind's `_scratch` is `ClaudeFolder/_scratch/`, NOT inside the repo; a stale `.git/index.lock` is swept by RENAME, never rm), THE PRIOR-LEDGER GATE and THE PREMISE GATE (pm-lessons 2026-09-15 — every `file:line`, "exists" and mechanism claim grepped at the pinned HEAD before DISPATCH-READY, the greps in the beat's audit; since v75 b6 it binds OUTWARD TEXT too: every company fact at its source, every product sentence from `context/strategy/claim-register.md`) bind every message and every call. Beat 1: name the window's ONE deliverable, the intake at the bytes, this text verbatim, §HELD re-printed, hand me ONE act.

STATE AT DISPATCH (the record wins): core `3af6213` (CI-2 — artifact uploads never gate; CI green with the quota still hit, so the fix is proven; on `be4788a` = DUR-1 + HASH-1, `Run check` green at the instrument; IR-22/IR-23 RETIRED; the read-API stamp v1.1.5; the FE mirror v1.1.4) · hivemind `08b8474` (v75 CLOSED at beat 8; the b8 audit and the plan's §13 are the close) · bench `f3631cb` · docs `7221ddc` · skills `180375f` (Check 9 PASS 28/28 at v75 b1). The closure counter 7/20 on the precedent (`3af6213` "all checks green"; run #251 was not a readable sample) — confirm the bus-soak step at the instrument before you write 7. The org's artifact-storage quota: 62 old artifacts deleted 09-17 ~16:xx CT (9,195 MB listed; run #56's two KEPT); GitHub recalculates within 6–12 h; until then uploads fail harmlessly (CI-2). `ACTIVATE: sent` 09-17 — skip-by-default; apply only on a WRITTEN yes to both questions by Fri Oct 9, hours added on top.

THE SITTING: Sat 09-19 10:00 CT, one sitting, H8-a then R-5B — both packets through THE PRIOR-LEDGER GATE and UNTOUCHED since v75 b2, pinned to `6bd8508`'s install-smoke run #56 (id 34840877827; the arm64 zip's sha256 `11f3a79e…`; its artifacts exist through the weekend); the navigator is SCHEDULED as a one-shot task at Sat 09:50 CT (a fresh session with ClaudeFolder; it reads the packet and waits for my `go`; `NAV: manual` cancels; the hub asks nothing on Saturday). NO re-pin on `be4788a`/`3af6213` without my `REPIN: <sha>`. Sunday: `R5B-2: <the digest line>` → the two intakes (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C).

THE NEXT WUs, in order (v76's window, the plan §13): (1) ENERGY-READ chartered THROUGH THE PREMISE GATE on the Java slot — IR-15 first (the IAS zone types); the 0x0B04/0x0702 handlers with the divisors read at interview; IR-24 classify by CLUSTERS present (the Shelly Plug US Gen4 is device type 0x010A, not 0x0051); IR-25 the reportable change scaled by the divisor; the divisor VALUE read at the first read (no prediction); THE MEASUREMENT RECORD minted; the fixture = `DEVICES: core` (`context/research/2026-09-15_DEVICE-SET_return.md`) and ZIGBEE-GAPS rows 3 and 7 (`context/research/2026-09-17_ZIGBEE-GAPS_return.md` §2); the sources to grep at HEAD before DISPATCH-READY: `ClusterHandlers.java:36–52`, `EndpointClassifier.java:41/:91–104/:150–165/:180–203`, `ReportingConfigurator.java:65–72`, `InterviewStateMachine.java`, `StandardCapabilities.java`, `TemperatureMeasurementHandler.java` as the handler template. (2) FE-115 on the web-ui slot after Sunday's H8-a intake (`conditions[].definition` rendered — the v1.1.5 mirror; the `v114` mock touch; the dead-key retirements per `context/audits/2026-09-15_v75-b3_114c-and-FE-114_intake_two-layer-audit.md` §3; the H8 real-wire read). (3) The knockout charter for name candidate ii (D-v74-1; token-parameterized; no name in any tracked file) on a free slot. (4) Rehearsal 1's packet (kill −9; wk 09-28) through the gate after Sunday's intake; `HARNESS-PLUG:` on H8-a's `S31-LABEL:`. (5) The strategy pass (D-v75-2, after R-5B) opening with the pitch row: "built to and for the market as it exists and as it is developing".

WORDS GIVEN: `H8: Sat 09-19 10:00` · `DUR: measure` (measured RED, fixed) · `HASH1: a` (by silence, fixed) · `FRAME: a` · `PLUG: two` → DEVICE-SET · Activate skip-by-default (the email v3 SENT 09-17; the Oct 9 rule) · THE PREMISE GATE (code + outward text) · `SCOPE: lane` + the three fences · THE ADOPTION FENCE (no metering plug pairs before ENERGY-READ is green). WORDS OPEN (silence = the recs): `DEVICES: core|extended|<edits>` (rec core) · `GAPS: color|buttons|both|neither` (rec neither before the run) · `HOTMOBILE: submit|skip` (rec skip; due Oct 9) · `FOP: apply|hold` (my file) · `NAME:` silence = ii · `STRATEGY:` (rec after-R5B) · `CASCADE:` (rec engine, Nov) · `AMD100: ratify`. OWED: `PROTECT: done` · `ERIK: <his line>` (PALOKI search started 09-13; no public name before the opinion) · the LLC's standing (charter 46564936K, a one-minute registrar lookup, mine).

READ BEYOND §1 ONLY WHEN A BLOCK SENDS YOU THERE: the b7 and b8 audits (`context/audits/2026-09-17_v75-b7_…` and `…_v75-b8_close_…`); the improvement register (IR-24..IR-27 are ENERGY-READ's and October's rows); `context/research/2026-09-17_FOP-1_return.md` §3 (the dated watch list); pm-lessons' two newest entries (THE PREMISE GATE; THE CARD-GREP EXEMPTION). The file `context/handoff/2026-09-17_v76_dispatch-text.md` carries placeholders for the two shas above — this pasted text is the dispatch of record; file it verbatim at beat 1.
```
