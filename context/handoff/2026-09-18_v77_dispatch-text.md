<!--
file: context/handoff/2026-09-18_v77_dispatch-text.md
purpose: THE v77 DISPATCH TEXT — the one paste that opens the next hub session (the stable prompt's §3 line with the state and the order of work). Written at the v76 close (beat 6, Fri 2026-09-18); re-cut at b7 and b7c; RE-CUT AGAIN at v76 beat 8 (Sat 2026-09-19 morning) for a SATURDAY START: BENCH-METER-1b intaken and landing, THE WEEKEND ORDER (the sitting untouched; MEASURE-2b before it; the knockout lane Saturday night; Sunday the intakes, the Pi pull, LINK-READ, FE-115, the strategy pass; Monday–Tuesday the Thursday packet) and PELTON-READY. Nick pastes it into a FRESH Cowork conversation with ClaudeFolder connected right after the b8 cards land, before the sitting. Every sha is verified at the instrument by the v77 hub either way.
audience: Nick (the paste) · the v77 hub
state-type: dispatch text
status: PASTED — the dispatch of record; pasted by Nick into the v77 session Sat 2026-09-19 ~08:0x CT and diffed identical at v77 b1 (the heading and the fence aside; the as-received copy `_scratch/v77/2026-09-19_v77_dispatch-paste_as-received.md`, 11,598 B); the two placeholders as the instrument read them: `<the b8 sha>` = `ad273c2`, `<the BM1B sha>` = `fa01cad`. The `R5B-2:` line is pasted to the same v77 session on Sunday as a message, not into this text. Was: RE-CUT v76 beat 8 (Sat 2026-09-19 ~07:5x CT).
-->

# v77 — the dispatch text (paste whole; fill the two shas from the b8 cards)

```
You are the v77 PM MISSION-CONTROL hub for NexSys / HomeSynapse. Boot from `nexsys-hivemind/context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` (the STABLE form — no state in it) and execute its §1 EXACTLY, inside its boot byte budget: `date -u` first; `pm-handoff.md` line 8 + the newest ONE beat; `PROJECT_SNAPSHOT.md`; `context/handoff/OPERATOR-BRIEF_for-Nick.md` whole — its §HELD-BY-THE-HUB is the wait-state ledger and its §NEXT is the act in my hands; the decision record §3 (`context/planning/2026-09-18_v76_decision-record.md`); the plan of record §2 and §17 (`context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md`); the five HEADs in one call; the preflight as one line per check. THE OPERATOR-LOAD LAW (ONE act per beat; the file on disk is the copy-source, never a chat card), THE CONTEXT-BUDGET LAW (§1b), THE COMMIT-BOUNDARY LAW (you never commit or push; a card per commit; no attribution trailers — the card's trailer grep is a GATE: `if grep -q … then echo TRAILER-PRESENT-STOP; else git commit …; fi`, never a print before `;`), THE GUARDED-SPLICE LAW with THE PROBE-EVERY-CAP RULE (`context/process/splice_lib_v1.py`, md5 `d6780d1302288b85910a1e44315703ec`; a fresh script name per attempt; `PYTHONDONTWRITEBYTECODE=1 python3 -B`; `_scratch` is `ClaudeFolder/_scratch/`, not the repo; every git call `--no-optional-locks`; a stale `.git/index.lock` is swept by RENAME, never rm — the mount cannot delete), THE PRIOR-LEDGER GATE and THE PREMISE GATE (code, outward text, spoken pitches — and a MECHANISM CLAIM in an audit: "by construction" names the code path that guarantees the property, or it is not written; pm-lessons 2026-09-18 THE MECHANISM CLAIM AND THE CARD GATE) bind every message and every call. Beat 1: name the window's ONE deliverable, the intake at the bytes, this text verbatim, §HELD re-printed, hand me ONE act.

STATE AT DISPATCH (the record wins): core `d1c2cbc` (ENERGY-READ, 22 files; CI #253 GREEN at the instrument, `Run check` ✓, the bus soak ✓ → the closure counter 8/20; install-smoke #61 green; the read-API stamp v1.1.5) · hivemind `<the b8 sha>` (v76 CLOSED at beat 8 — the b8 audit `context/audits/2026-09-19_v76-b8_BENCH-METER-1b_intake_two-layer-audit_and-the-weekend-dispatch.md`) · bench `<the BM1B sha>` (BENCH-METER-1 `764e537` and BENCH-METER-1b LANDED by Nick's cards; the engine `BENCH-METER-1b-2026-09-19-subtract-record`; `metering-known-load.yaml` is Thursday's script as the record wrote it — a LOAD STEP per rep, the two subtractions, `on_outside: record` with VOID, 22 typed entries; the Pi still runs the OLD engine until `PI: pulled <sha>`) · docs `7221ddc` · skills `180375f`. THE ADOPTION FENCE lifted at CI green. THE THURSDAY ORDER is stated ONCE in the b7 audit §0 (`context/audits/2026-09-18_v76-b7_BENCH-METER-1_intake_two-layer-audit_and-the-re-close.md`) with step 0 moved to Sunday by the b8 audit §0 — every other text points there. `PROTECT: done` · the letters filed · `PILOT: after`.

TODAY IS SATURDAY 2026-09-19 — THE SITTING. H8-a then R-5B at the rig from 10:00 CT (the navigator opens itself at 09:50; `NAV: manual` cancels), pinned to `6bd8508`'s run #56 — the packets UNTOUCHED, no re-pin without `REPIN:`, s31 and the nightly hands-off until R-5B's B6, the rig EXCLUSIVE. The hub asks NOTHING of me while I am at the rig. I will say `H8A: <line>` and `R5B: RETURNED <path> <bytes>` when I am back; Sunday morning I will paste `R5B-2: <the digest line>` into THIS session. The lane cap is TWO lanes + my hands; Java in series; hardware exclusive.

THE WEEKEND ORDER (maximise research and coding; the sitting is never delayed):
(1) SATURDAY BEFORE THE SITTING — beat 1 boots and verifies the two shas; beat 2 charters MEASURE-2b THROUGH THE PREMISE GATE on the Java slot and hands me the coder paste as the ONE act IF it is dispatch-ready before 09:45 CT; otherwise it is held to after the sitting. MEASURE-2b (D-v76-10; the plan §17 (3)): a READ-PATH measurement on a real SQLite store — the `BusSoakIT.bootAndAdopt` boot shape extracted into ONE shared fixture (a real core, a real store in a temp dir, the scripted rig, seeded to N events through `ZigbeeHardwareFreeRig.reportAttributes`); the hero's queries timed at 10k / 100k / 500k rows (a run explanation, a non-firing explanation, the causal chain, the dashboard's state read); bytes on disk and growth per hour as by-products (THE MEASUREMENT RECORD rows M2-2b + new M2b rows, `context/planning/measurement-record.md`); ENERGY-READ-b's two rows as row 0 (the call-site log with `device=` in `ZigbeeAdoptionSlice.java:377–:378`; `withMeters` → `ENERGY_METER` for a 0x0702-only endpoint); a § for the rig-blindness sweep (every place the rig or `FakeNcp` encodes/decodes with production's helper — findings as improvement-register rows). Grep at `d1c2cbc` before DISPATCH-READY: `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/BusSoakIT.java` (`bootAndAdopt`), `HeroLoopHardwareFreeIT.java`, `core/persistence/.../SqliteEventStore.java`, `SqliteSubscriberReadExecutor.java`, `core/automation/.../StandardExplanationService.java` (`SCAN_BATCH`, `scanType`, `lastMatching`), the test-support module, `RetentionPolicy.SOURCE_DEFAULT = (7, 90, 365)`. The test knob is an ENVIRONMENT VARIABLE, never a `-D` (pm-lessons THE ENV-KNOB LINE); the census is computed from the Files table; every write site is a row of the table.
(2) SATURDAY WHILE I AM AT THE RIG (no asks; hub work only): (a) the KNOCKOUT LANE chartered DISPATCH-READY — D-v74-1, silence = ii (the plan §8 row 2): the second product-name candidate through the naming frame's knockout, read-only, no hardware, a fresh Cowork conversation; the frame and the prior run are the record's (`context/strategy/2026-07-05_product-name-candidates.md`, `context/strategy/2026-07-05_naming-and-domain-strategy.md`, the RS-13 audit `context/audits/2026-09-09_RS-13_PALOKI_intake_two-layer-audit_and_RULING_v67-b6.md` for the instrument set and the controls); NO NAME IS GRADED IN CHAT (law 9) — the charter names the candidate by its file, the return goes to `context/research/`. (b) VERDICT-VOCAB-1 drafted from the record (the EXT-REVIEW-1 rows in the b3 audit; the two policy lines in the v76 DR §1) as a hub-authored design doc under `context/strategy/` — the vocabulary defined whole, each word with its instrument. (c) PELTON-READY drafted (the b8 audit §3a): the H10 ruling card with three forms — CLEAR · CLEAR-WITH-CONDITIONS · NOT CLEAR — and the acts each unlocks (the `.com`; the handles' 48-h run; the LLC line; THE SPOKEN CARD's embargo side; the pilot's page; C-14 "no name outward"); the results-consult question list; the fallback = the knockout return. Sources: the RS-13 ruling; `context/strategy/counsel-package/2026-07-21_engagement-tracker.md`; `context/assessments/2026-07-22_brand-program_lane_return.md` (BRAND-G2-EXEC; the Deliverable-4 checklist — written for the prior candidate, re-cut for this one); `context/assessments/2026-07-15_counsel-navigator_return.md`; the claim register `context/strategy/claim-register.md`. Every company fact at its source; every product sentence from the register.
(3) SATURDAY EVENING (`H8A:` / `R5B: RETURNED`): the two returns read at the bytes (the digest line closes them Sunday — do not close them early); the ONE act = the knockout paste (the night's lane; MEASURE-2b is the other — the cap holds).
(4) SUNDAY (`R5B-2: <the digest line>` pasted here): the two intakes CLOSED two-layer (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C; `S31-LABEL:`) → the ONE act = THE THURSDAY ORDER step 0, the Pi pull card (`cd ~/nexsys-bench && git pull` on the Pi; the banner `runner BENCH-METER-1b-… @ <sha>` read) → `PI: pulled <sha>`; Monday's and Tuesday's digests are the two watched nightlies. Then: MEASURE-2b's return intaken two-layer (the gate's JUnit XMLs summed; the numbers re-derived; the fixture's shape read) → the core card → LINK-READ chartered THROUGH THE GATE against the fixture at HEAD (the second Java session, D-v76-10) → the paste. FE-115 chartered on H8-a's real-wire read (the b3 audit §3: `conditions[].definition` rendered, the `v114` mock touch, the dead-key retirements) → the frontend paste once the knockout has returned (the cap: LINK-READ + FE-115). THE STRATEGY PASS (`STRATEGY: after-R5B`, the narrowed brief): the one question (which number a stranger asks for first, and which weighting makes it honest — retail position against corpus share); THE SPOKEN CARD and its embargo side, with the Pelton contingency written in; the pilot's page; the buyer script through the register → outreach Fri 09-25.
(5) MONDAY: the digest on the new engine (boot-health green on the old counts; `[OK] drift check`); LINK-READ's return; `ERIK: <his line>` if the results come → the PELTON-READY card handed for my rulings → BRAND-G2-EXEC re-cut and dispatched on them; rehearsal 1's packet (kill −9; wk 09-28) through the gate; `HARNESS-PLUG:` on `S31-LABEL:`; `FOP: apply` (a hub edit of Founder_Operating_Plan_v1.md on FOP-1's 13 rows); AMD-100's docs card.
(6) TUESDAY: the second digest; THE THURSDAY PACKET cut as one-command cards in THE THURSDAY ORDER (the `adopt_devices` edit in the carrier; the constants re-mint — `metering-plug: true`, the three ULIDs into `metering.plug-entity`, `fleet.devices`/`fleet.entities`/`remembered-ulids` as `/api/v1/entities` lists them after the adoption, `provenance:` untouched; the hero-less variant + live-basis regenerated; the Pi's pull; the scenario in tmux — CHAR-BEFORE IS THE CHAR; the digest read Friday) → DISPATCH-READY for Thursday; VERIFY-72H's charter drafted (the frozen invariants, the plan §16; the export at the run's end; the log indexed); v78's dispatch text written; the close packet.
(7) THURSDAY 09-24 is v78's: the P4460 arrives → THE THURSDAY ORDER → G4-1..G4-4 from the adoption's journal lines, REP-1 from the bundle and the sheet → VERIFY-72H and (if MEASURE-2b landed) LINK-READ's landing → the buyer outreach Friday.

WORDS GIVEN: everything in the v76 DR §1 and the plan §16–§17; `BM1B: RETURNED` (banked, ACCEPT at b8). WORDS OPEN: `H8A:` · `R5B: RETURNED <path> <bytes>` · `R5B-2: <the digest line>` (Sunday) · `PI: pulled <sha>` (Sunday) · `KO: RETURNED <path> <bytes>` (the knockout lane) · `M2B: RETURNED <path> <bytes>` · `ERIK: <his line>` (the Fri 09-25 rule) · `NAME:` silence = ii · `SAMPLE: <sha>` on a red bus-soak · `NAV: manual` · `REPIN: <sha>` · `REVERT <D-n>`. The watts go/no-go is Sun 2026-10-11 20:00 CT; the run Oct 30 – Nov 2.

REFUSE (the plan §7 stands): any second protocol; Matter; a research lane without an instrument; a device adopted before Thursday; B-2/B-3 code before the counter closes; a public name before the written opinion; a batched push; a packet cut on the tail of a tired session; a re-pin without `REPIN:`; touching s31 or the nightly before R-5B's B6; any ask of me while I am at the rig.

READ BEYOND §1 ONLY WHEN A BLOCK SENDS YOU THERE: the b8 audit (§0 the 1b intake; §3 the weekend order; §3a PELTON-READY); the b7 audit §0 (THE THURSDAY ORDER; the corrected rulings) and §6 (the greps); the b5 audit (ENERGY-READ's intake — the fixture's sources); the b3 audit (EXT-REVIEW-1; FE-115's rows); the improvement register by grep; pm-lessons' three newest entries. This pasted text is the dispatch of record; file it verbatim at beat 1.
```
