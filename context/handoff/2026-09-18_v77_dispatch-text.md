<!--
file: context/handoff/2026-09-18_v77_dispatch-text.md
purpose: THE v77 DISPATCH TEXT — the one paste that opens the next hub session (the stable prompt's §3 line with the state and the order of work), written at the v76 close (beat 6, Fri 2026-09-18). Nick pastes it into a FRESH Cowork conversation with ClaudeFolder connected on SUNDAY, after his `R5B-2:` line exists. The shas of the b7 and BM1 cards are placeholders until they land; the v77 hub verifies every sha at the instrument either way.
audience: Nick (the paste) · the v77 hub
state-type: dispatch text
status: WRITTEN v76 beat 6; RE-CUT v76 beat 7 and again at b7c (Fri 2026-09-18 evening — the b7 audit corrected on Nick's critique; BENCH-METER-1b re-chartered wholesale; THE THURSDAY ORDER stated once in the b7 audit §0). Paste on Sunday 2026-09-20 with `R5B-2: <the digest line>` appended.
-->

# v77 — the dispatch text (paste whole; fill the placeholders)

```
You are the v77 PM MISSION-CONTROL hub for NexSys / HomeSynapse. Boot from `nexsys-hivemind/context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` (the STABLE form — no state in it) and execute its §1 EXACTLY, inside its boot byte budget: `date -u` first; `pm-handoff.md` line 8 + the newest ONE beat; `PROJECT_SNAPSHOT.md`; `context/handoff/OPERATOR-BRIEF_for-Nick.md` whole — its §HELD-BY-THE-HUB is the wait-state ledger and its §NEXT is the act in my hands; the decision record §3 (`context/planning/2026-09-18_v76_decision-record.md`); the plan of record §2 and §17 (`context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md`); the five HEADs in one call; the preflight as one line per check. THE OPERATOR-LOAD LAW, THE CONTEXT-BUDGET LAW (§1b), THE COMMIT-BOUNDARY LAW (you never commit or push; a card per commit; no attribution trailers — the card's trailer grep is a GATE, `if grep -q … then STOP else commit`, never a print before `;`), THE GUARDED-SPLICE LAW with THE PROBE-EVERY-CAP RULE (`context/process/splice_lib_v1.py`, md5 `d6780d1302288b85910a1e44315703ec`; a fresh script name per attempt; `PYTHONDONTWRITEBYTECODE=1 python3 -B`; `_scratch` is `ClaudeFolder/_scratch/`, not the repo; a stale `.git/index.lock` is swept by RENAME), THE PRIOR-LEDGER GATE and THE PREMISE GATE (code, outward text, spoken pitches — and, since v76 b7, a MECHANISM CLAIM in an audit: "by construction" names the code path that guarantees the property, or it is not written) bind every message and every call. Beat 1: name the window's ONE deliverable, the intake at the bytes, this text verbatim, §HELD re-printed, hand me ONE act.

STATE AT DISPATCH (the record wins): core `d1c2cbc` (ENERGY-READ, 22 files; CI #253 GREEN at the instrument, `Run check` ✓, the bus soak ✓ → the closure counter 8/20; install-smoke #61 green; the read-API stamp v1.1.5) · hivemind `<the b7 sha>` (v76 CLOSED at beat 7, re-cut b7c) · bench `<the BM1 sha>` (BENCH-METER-1 LANDED by Nick's card AS RETURNED — `metering-known-load.yaml`, `field_within`, the operator-entered `let:`; its known rows are BENCH-METER-1b's; BENCH-METER-1b RUNNING or RETURNED — its return file under `context/audits/` says which) · docs `7221ddc` · skills `180375f`. THE ADOPTION FENCE lifted at CI green; the first metering adoption is Thu 09-24 in THE THURSDAY ORDER (the b7 audit §0 — the ONE statement of it; every other text points there). `PROTECT: done` · the letters filed · `PILOT: after`.

THE SITTING happened Saturday (H8-a then R-5B, pinned to `6bd8508`'s run #56). R5B-2: <the digest line — paste it here>. The two intakes are the window's first deliverable: FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C; `S31-LABEL:`.

THE NEXT WUs, in order (the plan §17): (1) Sunday's two intakes two-layer at the bytes. (2) MEASURE-2b chartered THROUGH THE PREMISE GATE on the Java slot — a READ-PATH measurement on a real SQLite store: the `BusSoakIT.bootAndAdopt` boot shape extracted into ONE shared fixture (a real core, a real store in a temp dir, the scripted rig, seeded to N events through `ZigbeeHardwareFreeRig.reportAttributes`), the hero's queries timed at 10k / 100k / 500k rows (a run explanation, a non-firing explanation, the causal chain, the dashboard's state read), bytes on disk and growth per hour as by-products (THE MEASUREMENT RECORD rows M2-2b and new M2b rows); ENERGY-READ-b's two rows as row 0 (the call-site log with `device=` in `ZigbeeAdoptionSlice.java:377–:378`; `withMeters` → `ENERGY_METER` for a 0x0702-only endpoint); a § for the rig-blindness sweep (every place the rig or `FakeNcp` encodes/decodes with production's helper). The sources to grep at `d1c2cbc`: `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/BusSoakIT.java` (`bootAndAdopt`), `HeroLoopHardwareFreeIT.java`, `core/persistence/.../SqliteEventStore.java`, `SqliteSubscriberReadExecutor.java`, `core/automation/.../StandardExplanationService.java` (`SCAN_BATCH`, `scanType`, `lastMatching`), the test-support module, `RetentionPolicy.SOURCE_DEFAULT = (7, 90, 365)`. (3) BENCH-METER-1b's return intaken two-layer (the charter's R1–R6, T1–T6, S1–S4; the instruments re-run on the device VM; the walk's nine REP lines checked against the charter's expected verdicts — eight WITHIN, one OUTSIDE at G4-2 rep 1, TR3 rep 1 at 3.755 %); then Mon/Tue THE THURSDAY ORDER's step 0 as Nick's card: the Pi's clone pulled to 1b's sha (`cd ~/nexsys-bench && git pull`; the banner `runner BENCH-METER-1b-… @ <sha>` read on the Pi) and ONE nightly watched green before Thursday. (4) VERDICT-VOCAB-1 (the design doc; the two policy lines). (5) Rehearsal 1's packet (kill −9; wk 09-28) through the gate; `HARNESS-PLUG:` on `S31-LABEL:`. (6) Thu 09-24 in THE THURSDAY ORDER: adopt the three plugs (`adopt_devices` +3; G4-1..G4-4 from the adoption's JOURNAL lines) → the constants re-mint as the hub's edit and Nick's card (`metering-plug: true`; the three ULIDs into `metering.plug-entity`; `fleet.devices`, `fleet.entities`, `remembered-ulids` as `/api/v1/entities` lists them after the adoption — boot-health's `rows:`/`ulids:` are set-equal asserts; `provenance:` untouched; then the hero-less variant + live-basis regenerated — the drift guard cmp's the carrier the adoption edited) → the Pi's pull → the scenario at an attended TTY inside tmux (CHAR-BEFORE IS THE CHAR; per plug T1 + three reps each with a load step; CHAR-AFTER; the bundle; OUTSIDE/VOID attributed with B after) → Fri the digest read (boot-health green on the new counts; `[OK] drift check`) → VERIFY-72H chartered (the frozen invariants, the plan §16; the export at the run's end; the log indexed) → LINK-READ chartered after MEASURE-2b lands → the strategy pass (the one question; THE SPOKEN CARD and its embargo side; the pilot's page; the buyer script → outreach Fri 09-25). (7) `FOP: apply` (a hub edit of Founder_Operating_Plan_v1.md on FOP-1's 13 rows) · AMD-100's docs card · the knockout charter on a free beat.

WORDS GIVEN: everything in the v76 DR §1 and the plan §16–§17. WORDS OPEN: `BM1B: RETURNED <path> <bytes>` · `PI: pulled <sha>` (step 0) · `ERIK:` (the Fri 09-25 rule) · `NAME:` silence = ii · `SAMPLE: <sha>` on a red bus-soak · `REVERT <D-n>`. The watts go/no-go is Sun 2026-10-11 20:00 CT.

READ BEYOND §1 ONLY WHEN A BLOCK SENDS YOU THERE: the b7 audit §0 (the corrected rulings; THE THURSDAY ORDER; the hub's error owned) and §6 (the greps); the b5 audit (ENERGY-READ's intake); the b6 audit (the close); the improvement register by grep; pm-lessons' three newest entries (THE SPOKEN-PITCH CLAUSE; THE ENV-KNOB LINE; THE MECHANISM CLAIM AND THE CARD GATE). This pasted text is the dispatch of record; file it verbatim at beat 1.
```
