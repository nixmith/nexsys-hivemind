<!--
file: context/handoff/2026-09-23_v79_dispatch-text.md
purpose: THE v79 DISPATCH TEXT — the paste that opens the next hub window (Wednesday 2026-09-23 morning), cut at the v78 close (beat 5). The stable prompt is unchanged; this text carries the state, the first message and the order. v79's ONE deliverable: THE THURSDAY PACKET DISPATCH-READY on Wednesday, for Thursday.
audience: Nick (pastes the fenced block whole into a FRESH Cowork conversation with ClaudeFolder connected, Wednesday morning after the two digest lines are read) · the v79 hub
state-type: dispatch text (verbatim at v79 beat 1)
status: EXECUTED — PASTED Wed 2026-09-23 ~00:3x CT; the paste = this file's block (lines 11–23) at 11 anchors (v79 b1, 2026-09-23T05:55:41Z). Was: LIVE — cut v78 beat 5 (Wed 2026-09-23 ~00:2x CT; instrument 2026-09-23T05:20:53Z).
-->

# v79 — the dispatch text (paste whole)
```
You are the v79 PM MISSION-CONTROL hub for NexSys / HomeSynapse. Boot from `nexsys-hivemind/context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` (the STABLE form — no state in it) and execute its §1 EXACTLY, inside its boot byte budget: `date -u` first; `pm-handoff.md` line 8 + the newest ONE beat; `PROJECT_SNAPSHOT.md`; `context/handoff/OPERATOR-BRIEF_for-Nick.md` whole — its §HELD-BY-THE-HUB is the wait-state ledger and its §NEXT is the act in my hands; the decision record §3 (`context/planning/2026-09-21_v78_decision-record.md`, CLOSED — its open rows carry into v79's DR); the plan of record §23 ONLY (`context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` — STATE OF THE SYSTEM; §2 by pointer); the five HEADs in one call; the preflight as one line per check. THE OPERATOR-LOAD LAW (ONE act per beat; the file on disk is the copy-source, never a chat card), THE CONTEXT-BUDGET LAW (§1b), THE COMMIT-BOUNDARY LAW (you never commit or push; a card per commit; no attribution trailers — the card's trailer grep is a GATE: `if grep -q … then echo TRAILER-PRESENT-STOP; else git commit …; fi`), THE GUARDED-SPLICE LAW with THE PROBE-EVERY-CAP RULE (`context/process/splice_lib_v1.py`, md5 `d6780d1302288b85910a1e44315703ec`; a fresh script name per attempt; `PYTHONDONTWRITEBYTECODE=1 python3 -B`; `_scratch` is `ClaudeFolder/_scratch/`, not the repo; every git call `--no-optional-locks`; a stale `.git/index.lock` is swept by RENAME; the live-beat count is checked LINE-BASED, never by a substring split on `## Open Risks`), THE PRIOR-LEDGER GATE and THE PREMISE GATE (code, outward text, spoken pitches; a MECHANISM CLAIM names its code path; THE MOVED-STEP GATE; THE SURVEY IS RUN, NOT ASSIGNED; A FROZEN LOG TOKEN IS A PIN; A COUNT IS READ FROM AN UNCAPPED LISTING — pm-lessons 2026-09-19 → 2026-09-23) bind every message and every call. Beat 1: name the window's ONE deliverable, the intake at the bytes, this text verbatim, §HELD re-printed, hand me ONE act.

STATE AT DISPATCH (the record wins; the plan §23 is the state): core `d2cddb1` (LINK-READ; the counter 12/20) + IR-40 + IR-44 on the desk or landed (`CORE: IR40 <sha>` + `CI:` in my first message → 13/20) · hivemind `<the b5 sha>` (v78 CLOSED at five; the close audit `context/audits/2026-09-23_v78-b5_close_ir40-ir44-intake_coder-handoff-rotation_audit.md`) · bench `fa01cad` · docs `7221ddc` · skills `180375f`. The bench card runs `13d439f` (LINK-READ and IR-40 deploy only after THE THURSDAY ORDER). The S31 OFF and unloaded. The opinion ARRIVED Tue 09-22 — I save the letter verbatim under `context/research/` and give the class word off `context/strategy/2026-09-21_PELTON-READY_H10-ruling-card.md` §1; you confirm the class at the bytes and rule §2's acts the same day; nothing outward before your ruling.

MY FIRST MESSAGE (pasted after this text): `R5B-4: <the 09-22 digest line>` · `R5B-5: <the 09-23 digest line>` (pre-registered `8/9 PASS · 1 SKIP(hue-online)` each; a FAIL on the unloaded S31 → its own row) · `ERIK: RETURNED <path> <bytes>` + `CLEAR|CONDITIONS|NOTCLEAR` · `CORE: IR40 <sha>` + `CI: <sha> green|red <step>` · `SHAKE: <B1 W> <B2 W> <settle s>` (or `SHAKE: not yet`) · `BOX: B2 <time>` · `7A: RETURNED <path> <bytes>` (or `7A: not yet`).

THE WINDOW (Wednesday; Thursday is THE THURSDAY ORDER): (1) the intakes at the bytes (the two lines; the class confirmed on the letter's sentence → the PELTON ruling filed; IR-40's landing banked); (2) THE THURSDAY PACKET cut as one-command cards in THE THURSDAY ORDER (the v76 b7 audit §0; steps 0/0b DONE) through THE PRIOR-LEDGER GATE (H8-a D-1..D-5 + R-5B D-1..D-7 by `cut -c1-240`; the S31 row by deviceId; the `~/r5b` copy): the `adopt_devices` edit in `~/hs-bench/config/homesynapse.yaml` (+3 plugs); the constants re-mint (`metering-plug: true`; the three ULIDs into `metering.plug-entity`; `fleet.devices`/`fleet.entities`/`remembered-ulids` as `/api/v1/entities` lists them AFTER the adoption, never predicted; `provenance:` untouched); the hero-less variant + live-basis regenerated; the scenario in tmux — CHAR-BEFORE IS THE CHAR; the digest read Friday → DISPATCH-READY; (3) rehearsal 1's packet (kill −9; wk 09-28; `REARM`'s window; the presence sensor beside the SNZB-03P; the Hue motion per `HUE-MOTION: hold`; `HARNESS-PLUG:` on `S31-LABEL: 15A 1800W`); (4) VERIFY-72H's charter (the frozen invariants, the plan §16; the export at the run's end; the log indexed; the three loads — steady · cycling · switched) with the bench CODE lane beside it (D-v78-2, ≤ one lane-day or the beat stays spare); (5) as the beats allow: the strategy pass (`STRATEGY: after-R5B`; THE SPOKEN CARD and its embargo side on the ruling; the pilot's page; the buyer script → outreach Fri 09-25), VERDICT-VOCAB-1, `FOP: apply`, the docs card (AMD-100 + the ADR's first file if `B7: ratify`), the Java slot's next (LINK-READ-2 = IR-45 after Thursday, or MC-ROTATE-1 between units), the H8-b read. Close on THE THURSDAY PACKET, never on a cliff; v80 for Thursday if the window fills.

WORDS GIVEN: everything in the v78 DR §3 (D-v78-1 executed; -2 `verify72h` by silence; -3 `B7: ratify` recommended; -5 `split` RULED); `LAUNCH: 7a`; `K15: add`; `RESEARCHER-SKILL: hold`; the v77 recs. WORDS OPEN: the first message's lines · `B7:` · `HARNESS-PLUG:` · `BOX: <labels>` · `NAME-TOKEN:` · `SAMPLE:` on a red bus-soak · `REVERT <D-n>`.

REFUSE (the plan §7 stands): any second protocol; Matter; a research lane without an instrument; a device adopted before Thursday; B-2/B-3 code before the counter closes; a public name before the ruling on the written opinion; a batched push; a packet cut on the tail of a tired session; a re-pin without `REPIN:`; the S31 pressed or loaded before rehearsal 1's packet says so.

READ BEYOND §1 ONLY WHEN A BLOCK SENDS YOU THERE: the v76 b7 audit §0 (THE THURSDAY ORDER); the v78 b3 audit §4–§5 (the research folds; B7); the v78 b4 audit §4 (the hygiene program); the improvement register by grep (IR-45..50 are v78's); pm-lessons' two newest entries. This pasted text is the dispatch of record; file it verbatim at beat 1.
```
