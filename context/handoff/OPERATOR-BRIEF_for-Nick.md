<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v75 beat 6 (Wed 2026-09-16 ~20:1x CT; instrument 2026-09-17T01:10:31Z). §NEXT = the b6 packet (the DUR-1 + HASH-1 landing → the hivemind card → the corrected Activate email → the ZIGBEE-GAPS paste → the FOP-1 paste); §HELD: both intakes ACCEPT; Activate re-cut to skip-by-default.
-->

# Operator brief (one act at a time)

## §NEXT — ONE act: the v75 beat-6 packet — block 1 the core landing (DUR-1 + HASH-1, 14 files, one push) → `CORE: DUR1 <sha>` then `CI: <sha> green|red`; block 2 the hivemind card (16 files) → `HIVE: LANDED <sha>`; block 3 the corrected Activate email (your mail) → `ACTIVATE: sent`; block 4 the ZIGBEE-GAPS paste (a fresh Cowork window) → `GAPS: RETURNED <path> <bytes>`; block 5 the FOP-1 paste (a free window, not Saturday's) → `FOP1: RETURNED <path> <bytes>` (Wed 2026-09-16 ~20:1x CT)
Run `_scratch/v75/2026-09-17_v75-b6_operator-packet.md` blocks 1 → 5 in order (blocks 4 and 5 run in parallel once pasted; block 5 can wait until after the sitting). Your word on the fleet: `DEVICES: core|extended|<edits>` (rec **core**, $104.78 — then the purchase is your hands; update the Gen4 firmware before ENERGY-READ). **Your words, in order:** `CORE: DUR1 <sha>` · `CI:` · `HIVE:` · `DEVICES:` · `ACTIVATE: sent` · `GAPS:` · `FOP1:` · silence on `NAME:` = ii · `H8: Sat 09-19 10:00` GIVEN (the navigator opens by itself 09:50 CT; `NAV: manual` cancels). **Owed whenever:** `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` · `STRATEGY:` · `CASCADE:`.

## §HELD-BY-THE-HUB (the wait-state ledger; edited every beat)
**Lanes (the cap: TWO + your hands; hardware exclusive; Java in series):**
| Lane | State (v75 b6) | Next |
|---|---|---|
| **The rig (EXCLUSIVE): H8-a then R-5 Part B — one sitting, Sat 09-19 10:00 CT** | both packets UNTOUCHED since b2 (H8-a pinned to `6bd8508`'s run #56 — the DUR-1 landing does NOT re-pin; `REPIN: <sha>` only on your word); the navigator send SCHEDULED Sat 09:50 CT (a fresh session with ClaudeFolder; one line, then it waits for `go`); `S31-LABEL:` and O-2's second reading ride the sitting; Sunday's nightly line is one paste | Saturday → `H8A:` / `R5B: RETURNED <path> <bytes>` → Sunday `R5B-2:` → the intakes (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C) → rehearsal 1 chartered |
| **The Java slot (DUR-1 + HASH-1 LANDING → ENERGY-READ → LINK-READ)** | DUR-1 + HASH-1 RETURNED 00:06Z, ACCEPT two-layer (b6): T1/T1b/T1c and T3 red → green; T4 the single-element hashes byte-identical to the baseline; the loop 64 lines, one hash per definition (re-tallied; your clean build and second loop read); R1 (the `EventTrigger` carve-out) ACCEPTED; a second redelivery is the RunManager's C2 dedup — pinned, not a defect | block 1 → `CORE: DUR1 <sha>` → `CI: <sha> green` (the counter 7/20) → IR-22/IR-23 RETIRE; the freeze doc's HASH NOTE becomes a fact (b7) → ENERGY-READ chartered on DEVICE-SET's rows (IR-15 first; IR-24 classify by clusters; IR-25 the divisor-scaled thresholds; the divisor read at the first read) → LINK-READ |
| **The web-ui slot (FE-115 after HASH-1's CI green)** | FE-114 LANDED `3d40b5f`; the freeze doc v1.1.5 | FE-115 chartered at `CI: <DUR1 sha> green`: `conditions[].definition` rendered, the `v114` mock touch, the dead-key retirements (the b3 audit §3), the H8 real-wire read |
| **The research lanes (read-only; beside the Java slot)** | DEVICE-SET RETURNED 03:2xZ, ACCEPT (b6): `DEVICES: core` $104.78 — a second-vendor plug, a Class-1.0 reference with 0.01 W resolution, the P4460 as cross-check (its power accuracy 0.5 % typ / 2 % max — P3 refuted), a 40 W tungsten load; EXTENDED held until IR-18; four HEAD findings (IR-24, IR-25; the divisor prediction withdrawn; the Shelly button report → rehearsal 2). **ZIGBEE-GAPS** (block 4): coverage against the field's corpus, a ranked gap list with an instrument per row. **FOP-1** (block 5): the founder operating plan checked claim-by-claim against live sources | `DEVICES:` → the purchase → the Gen4 firmware update → ENERGY-READ's fixture · `GAPS: RETURNED` → the intake → `GAPS: <top-N to charter>` (the October rows) · `FOP1: RETURNED` → the intake → `FOP: apply|hold` |
| **The bench lane (free)** | R-5 Part A + ii `f3631cb` landed; owed: `field_within`, `metering-known-load.yaml` with `requires: [operator]` + DEVICE-SET's §2 operator blocks (the three-reading rep; `within`/`outside`/`divisor-error`/`reference-disputed`), the `link-quality.yaml` skeleton — dry-run and selftests only | after the sitting, on a free slot |
| **The closure counter 6/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508` · `e56f555` · `3d40b5f`, all green, `SAMPLES: passive`) | the DUR-1 push is sample 7; a red `bus-soak` → `SAMPLE: <sha>` | passive |
| **Company, dated** | **Activate (D-v74-2, RE-CUT b6 on your critique): SKIP is the default; apply only on a WRITTEN yes to both questions by Fri Oct 9 and only with hours added on top of the plan; the corrected email is block 3 (an LLC formed July 2025 — no name; the product in register clauses only; the two questions quote the live rules); dated watch list — Cohort 2028 ~Sep 2027;** the entity name change for commercial use — no name in any tracked file (law 9); the knockout charter for candidate ii (D-v74-1; silence = ii; b7 on a free slot); Silabs Thu 09-17; Erik — PALOKI search STARTED 09-13 (no line since); `PROTECT: no-force` — your six clicks; AMD-100 PROPOSED; the counsel wave rides Erik's opinion | `ACTIVATE: sent` → their written answer (or none by Oct 9 = skip) · `FOP1:` · `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` |
| **The plan of record + the v75 DR** | `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` (b4; §10 b5; §11 b6 — the fleet, Activate skip-by-default, the pitch row restored, the two lanes): the run Oct 30 – Nov 2; the critical path unmoved. The DR: D-v75-4 measured, DUR-1 LANDING · D-v75-5 measured, HASH-1 LANDING (`HASH1: a` by silence) · D-v75-6 `DEVICES:` (rec core) · D-v75-2 `STRATEGY:` (rec after-R5B; the pitch row first) · D-v75-3 `CASCADE:` (rec engine, Nov) | `DEVICES:` now; `STRATEGY:`/`CASCADE:` whenever (silence = the recs) |
| W-SKILLS-9 `180375f` · HERO-1d `6bd8508` · P-1 `1201368` · R-4c (C-003 MINTED) · AMD-101 `7221ddc` · HONESTY-1 · FE-NULL-1 · TR-1 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · landed | W-SKILLS-10 folds THE PREMISE GATE + its outward-text extension (an idle beat); TR-1b a desk WU after the run |

**Open words (asked only when they gate an act):** `CORE: DUR1 <sha>` · `CI: <sha> green|red` · `HIVE: LANDED <sha>` · `DEVICES: core|extended|<edits>` (rec core) · `ACTIVATE: sent` · `GAPS: RETURNED <path> <bytes>` · `FOP1: RETURNED <path> <bytes>` · `NAME: i|ii|iii` (silence = ii) · `STRATEGY: now|after-R5B|fold` (silence = after-R5B) · `CASCADE: engine|bridge|drop` (silence = engine, Nov) · `AMD100: ratify` · `NAV: manual` · `REPIN: <sha>` · `REVERT <D-n>` only to refuse a standing ruling.

**Given (banked; no act):** `H8: Sat 09-19 10:00` · `DUR: measure` → measured RED → DUR-1 landing · `HASH1: a` (by silence; HASH-1 landing) · `Activate:` skip-by-default with the Oct 9 rule (D-v74-2 re-cut) · `FRAME: a` + the two sharpenings · `PLUG: two` → DEVICE-SET (the fence stands) · THE PREMISE GATE (code and, from b6, outward company text) · `SCOPE: lane` + the three fences · the copy dialect US · THE MEASUREMENT RECORD (minted once at ENERGY-READ) · the frame rate counted at the ingestion unit · `requires: [operator]` for reference-meter scenarios · `HERO1: Q1–Q7 a` · `EXPLAIN: three` · `PROTECT: core + docs` · `PROTECT: no-force` · `SAMPLES: passive` · `SEARCH-NAME: PALOKI` FINAL · `TR1-B2 = DRIVER` · `EU-DEFER` (provisional) · D4's seven recs · THE OPERATOR-LOAD LAW · D1–D7 · D-v74-1..4.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68:** in this file's history at `ea96351`. **v69–v70:** at `3209101`. **v71:** at `256c26b`. **v72–v73:** at `98e2f39`. **v74 (Sun 09-13 → Mon 09-14):** at `9795c2c`. **v75 (Mon 2026-09-14 ~13:1x CT →):** b1 the boot · IR-1 measured · H8-a re-pinned · R-5B cut through the gate · D-v75-1 → `cc17ce7`; b2 `FRAME: a` · 114c and FE-114 authored · IR-21/22 · D-v75-2..4 → `a8c987d`; b3 both ACCEPT two-layer · IR-23 → `553bf5b` → core `e56f555` + `3d40b5f`; b4 CI green ×2 (6/20) · the freeze doc v1.1.5 + the mirror line corrected · THE PREMISE GATE minted · MEASURE-1 cut · the dossier ACCEPT · HASH-1 a defect over eight records · the six-week plan · six rows retired · the navigator scheduled → `0eef472`; b5 MEASURE-1 ACCEPT (both defects measured) · DUR-1 + HASH-1 chartered · the shopping card withdrawn → DEVICE-SET · Activate `/apply` read · the spine rotated → `13e8d64`; b6 DUR-1 + HASH-1 ACCEPT (R1 ruled) · DEVICE-SET ACCEPT (`DEVICES: core` rec; IR-24/IR-25; P3 refuted at the manual) · the Activate plan re-cut on your critique (four hub errors named; the corrected email; skip-by-default, Oct 9) · the pitch row restored · ZIGBEE-GAPS and FOP-1 chartered → the b6 packet.

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · adopt a water-leak, smoke or vibration device before IR-15 lands · adopt a metering plug before ENERGY-READ is green · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion · pair the Gen4 plugs to the coordinator before ENERGY-READ · send outward text the register does not support · re-pin Saturday on the DUR-1 sha without `REPIN:`.
