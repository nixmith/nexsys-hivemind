<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v68 beat 6, THE CLOSE (Thu 2026-09-10 ~19:5x CT; instrument 2026-09-11T00:52:21Z). §NEXT = push the close + boot v69 (the dispatch line whole). v69's ONE deliverable = the EXPLAIN v1.1.4 instruction; the hero design lane beside it; R-4c Sat (measurement-only); P-1 after R-4c.
-->

# Operator brief (one act at a time)

## §NEXT — push the close, then boot v69 (5 minutes)
**1. Git Bash — one line; paste me only a surprise:**
```bash
cd /c/Users/Nick/Desktop/Code/ClaudeFolder/nexsys-hivemind && git --no-optional-locks status --porcelain | wc -l && git log --oneline -1 | cut -c1-80 && git push 2>&1 | tail -1
```
**Done looks like:** `0`, the v68 beat-6 close line, `062080d..<sha>`.
**2. A FRESH Cowork conversation with `ClaudeFolder` connected — paste this whole block as the first message:**
```
You are the v69 PM MISSION-CONTROL hub for NexSys / HomeSynapse. Boot from nexsys-hivemind/context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md (the STABLE form — no state in it) and execute its §1 EXACTLY, inside its boot byte budget: date -u first; pm-handoff.md line 8 + the newest ONE beat; PROJECT_SNAPSHOT.md; context/handoff/OPERATOR-BRIEF_for-Nick.md whole — its §HELD-BY-THE-HUB is the wait-state ledger; the decision record §3; the plan of record §3; the five HEADs in one call; the preflight as one line per check. THE OPERATOR-LOAD LAW and THE CONTEXT-BUDGET LAW (§1b) bind every message and every call. Beat 1: name the window's ONE deliverable, the intake at the bytes, this text verbatim, §HELD re-printed, hand me ONE act. STATE AT DISPATCH (the record wins): core eabdbb1 clean, both slots free · the v68 close pushed · DESIGN: start · PROTECT: no-force · SAMPLES: passive · R-4c measurement-only · P-1 after R-4c · H8-a Fri 19:00 CT — re-arm the 18:45 send at boot.
```
**One line back:** in v69's first reply — nothing here. v68 is closed on this act.
**Owed, time-insensitive (v69 hands it as a card when a slot in your day fits):** the `PROTECT: no-force` ruleset on GitHub — Settings → Rules → Rulesets → New branch ruleset → name `main-no-force` → Enforcement Active → target the default branch → tick ONLY "Restrict deletions" and "Block force pushes" → Create. No PR requirement; the direct-push cards stand.

## §HELD-BY-THE-HUB (edited every beat; you read it, you never remember it)
**Lanes (the cap: TWO + your hands; hardware exclusive):**
| Lane | State (v68 beat 6, 00:47Z — THE CLOSE) | Next |
|---|---|---|
| **HONESTY-1 (Core Java)** | **LANDED `94ae99d` · ci GREEN · install-smoke RAN BY ITSELF (P4 MET)**; OR-HONESTY-1-GATE CLOSED; Act 12 RETIRED | closed; LASTREPORTED-1b rides R-4c (read `lastReported` on one never-reported entity after the `.deb`; a `projectionVersion` bump only if the stamp persists) |
| **FE-NULL-1 (web-ui)** | **LANDED `eabdbb1` · frontend GREEN** (all four checks) | closed; O1 (the L1 headline per outcome) = the hero charter's first row |
| **EXPLAIN v1.1.4 (Core Java; slot 1)** | not yet authored — **v69's ONE deliverable** | v69 authors it (the ten keys, the CG-123 pattern, the emitter leads) → your paste into a host-side Claude Code session Fri → its landing → FE-114 |
| **The hero design lane (web-ui; slot 2) — `DESIGN: start`** | not yet chartered — v69 charters it at boot (`{{NAME}}`; HERO-0's four empty states outward; first row = the L1 headline per outcome; EXPLAIN keys as honest placeholders; FE-113b's capture folds in when H8-a returns) | your paste into a FRESH Cowork conversation; runs BESIDE EXPLAIN |
| **H8-a (the rig; EXCLUSIVE)** | packet on disk: `context/instructions/2026-09-06_H8a_real-wire_v113-keys_and_failchan-proof_navigator-packet.md` | **Fri 19:00 CT** (`H8:` only to move it); v69 re-arms the 18:45 send at boot; **if v69 is not booted by 18:45, you open the packet yourself at 19:00** |
| **R-4c (the rig; MEASUREMENT-ONLY on the core tree)** | packet: v69 authors it by Fri evening | **Sat 09-12** (`R4C: Sun` to move it); no Core write Saturday — EXPLAIN keeps slot 1 through it; C-003 is the exit; `card-gradle:` read once at the card |
| **P-1 (the power-harness primitive as a bench verb)** | charter: v69, directly after R-4c | the October 72-h rehearsals gate on it |
| **Erik — the PALOKI search (wait-state)** | packet sent Thu ~12:00 CT | `ERIK: <his line>` when it comes; no public use before the opinion |
| TR-1 · Act 12 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · RETIRED · landed ×8 | TR-1b after `card-gradle:` (Saturday) |

**v69's blocks, in order (the hub's list, not yours; the map: `context/planning/2026-09-10_v68_MOMENTUM-MAP_two-weeks_critical-path-and-Nicks-hours.md`, re-cut at v69 b1 with your three edits):** re-arm the H8-a send → the hero design charter (slot 2; `DESIGN: start`) → **the EXPLAIN v1.1.4 instruction (the ONE deliverable; slot 1; dispatches Fri)** → R-4c's packet (measurement-only; Sat) → **P-1's charter (directly after R-4c)** → the H8-a paste at 18:45 Fri → the PROTECT card (time-insensitive) → TR-1b (on `card-gradle:`) → the B-7 ADR word → W-SKILLS-9 (+ O3 the FE toolchain line · the lock-flag sentence in every lane line · the RETURNED-line template). **v68 CLOSED at beat 6:** the packet SENT · HONESTY-1 + FE-NULL-1 dispatched, audited, LANDED · the map filed.

**Owed lines (none gates an act):** `card-gradle: <the first line of ./gradlew --version on the card | absent>` — Saturday, at the card (TR-1b's shape depends on it). *(`samples:` · `sample4:` · `act3:` · `nightly 09-05/09-06:` RETIRED by your `SAMPLES: passive`.)*

**Open words (asked only when they gate an act):**
- `H8: <day> <hh:mm>` — only to MOVE the default (Fri 09-11 19:00 CT); silence = it stands.
- `R4C: Sun` — only to move R-4c off Sat 09-12; silence = Saturday.
- `ERIK: <his line>` — when he replies (sight-read · date · surcharge); verbatim is fine, one line.
- `REVERT TR1-B2` · `REVERT EU-DEFER` — only if you refuse those rulings; silence = they stand. · `Activate: apply|hold` (09-15) — not yet due.

**Given (banked; no act):** **your five words of Thu 09-10 evening — `DESIGN: start` · `PROTECT: no-force` · `SAMPLES: passive` · R-4c MEASUREMENT-ONLY on the core tree · P-1 directly after R-4c · the H8-a fallback (you open the packet at 19:00 if v69 is not booted by 18:45)** · `EAR-PALOKI: pass` → `SEARCH-NAME: PALOKI` FINAL · the packet SENT Thu · `V69: now` · your availability all week · HONESTY-1 `94ae99d` · Dependabot `3f3f5cc` · FE-NULL-1 `eabdbb1` · `TR1-B2 = DRIVER` · `EU-DEFER` (provisional) · D4's seven recs STAND · Erik = U.S. only · `BEYOND-LETTERS: keep` · `BLOCK6: pull` (landed) · THE OPERATOR-LOAD LAW · the v66 decisions D1–D7.

## §DONE — the ledger (Sun 09-06 → Wed 09-09)
v66 booted 15:29:56Z → FE-113 dispatched 10:42, LANDED `d192d17` → TR-0 dispatched 10:50, ACCEPT → the H8-a packet on disk 10:53 → the F-R4-1b instruction 11:03 → BLOCK 6 applied to the docs tree 11:12 → HERO-0 ACCEPT; the B-7 ADR; the assessment filed 12:05 → RS-12-F dispatched 13:16, ACCEPT (no displacement) → the rig missed (the hub's) → D1–D7 filed 16:25 → F-R4-1b dispatched 17:19, returned 17:44, ACCEPT, LANDED `39c8dd3` → the v67 stable prompt → HIVE-CLEAN-1 + W-SKILLS-8 dispatched ~10:33 CT Mon, returned ~11:00, audited, LANDED `30f800d` · `90529d1` · skills `c630c5c` → the v67 prompt re-cut for context; the spine rotated (v66 beat 10) → Act A run by Nick (five pushes verified; Check 9 28/28; the cleanup) → **v67 booted 23:22:01Z Mon (~18:22 CT)**: the dispatch filed verbatim; Block 4 pulled to tonight by Nick's word; TR-1's paste handed as the one act (dispatched 18:30 CT); 8 blocks rotated (v67 beat 1) → **Block 4 authored + handed** — the Erik go-ahead re-cut for tonight + Appendix B (v67 beat 2, ~18:4x CT) → NAMING-B chartered as one file, gated (v67 beat 3) → the email made ONE paste file on Nick's word; HONESTY-1 authored; the AMD-53 correction on the docs tree (v67 beat 4) → TR-1 returned ~19:2x, audited ACCEPT; the Erik packet built as PDF + DOCX on Nick's second word (v67 beat 5) → **Wed 07:00: the email NOT sent; VERDOMO retired by Nick's word; RS-13 (PALOKI) audited ACCEPT; the ruling filed — proceed, do not skip the search (v67 beat 6) → **v67 CLOSED on context health, 7 beats, no compaction; the stable prompt re-cut in place (arc 52 (v)); the close card handed (v67 beat 7) → **v68 booted 00:04:38Z Thu (~19:04 CT Wed)**: the dispatch + Erik's 09-05 note filed verbatim; the close card's steps 1–3 found NOT RUN (hivemind 7 ahead · docs 1 M · HONESTY-1 unverified) and re-queued as Act 2; the PALOKI packet authored (PDF 8 pp + DOCX + the one paste) and handed with the ear test as its gate; EXPEDITE = the 09-18 date rule (v68 beat 1) → **Thu: the ear test PASSED; Nick sent Erik the packet ~12:00 CT; the momentum map filed; the close card handed whole (v68 beat 2) → **Thu ~17:5x CT: Nick ran the close card (hivemind + docs pushed; the stale lock swept) and launched HONESTY-1; FE-NULL-1 authored and handed as the second lane (v68 beat 3) → **Thu ~18:2x CT: FE-NULL-1 launched; HONESTY-1 RETURNED 23:16Z, audited ACCEPT at the bytes, R1 applied; the landing card handed (v68 beat 4) → **~18:5x CT: HONESTY-1 LANDED `94ae99d`, ci GREEN; Dependabot `3f3f5cc` merged + pulled; FE-NULL-1 RETURNED, audited ACCEPT; its landing card handed; the close named for beat 6 (v68 beat 5) → **~19:4x CT: FE-NULL-1 LANDED `eabdbb1`, frontend GREEN; install-smoke ran by itself on both pushes (P4 MET); your five words banked; v68 CLOSED at beat 6 (6 beats, no compaction); v69 dispatched (v68 beat 6).**

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion (the .com is yours to buy at your own risk; the plan buys it the day the opinion is clean).
