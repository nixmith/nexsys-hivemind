<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v76 beat 5, ENERGY-READ ACCEPT (Fri 2026-09-18 ~15:5x CT; instrument 2026-09-18T20:54:29Z). §NEXT = the b5 packet (block 1 the core card, 22 files → `CORE: ER <sha>`; block 2 the hivemind card, 13 files); §HELD: the lane returned and accepted; a HEAD wire defect fixed; `PROTECT: done` and the letters banked; the bench charter at b6.
-->

# Operator brief (one act at a time)

## §NEXT — ONE act: the v76 b5 packet — block 1 the CORE card (22 files = 16 M + 6 A) → `CORE: ER <sha>` · block 2 the hivemind card (13 files = 9 M + 4 A) → `HIVE: LANDED <sha>` (Fri 2026-09-18 ~15:5x CT)
Run `_scratch/v76/2026-09-18_v76-b5_operator-packet.md` blocks 1 → 2. Then, when the CI run on the core sha finishes, one line: `CI: <sha> green|red` (the run number and the `Run check` step's colour; a red `bus-soak` → `SAMPLE: <sha>`). The lane is ACCEPT on one screen: `context/audits/2026-09-18_v76-b5_ENERGY-READ_intake_two-layer-audit.md` §0 — the tree is the table, the XMLs verified, every diff read; the lane found a HEAD wire defect (the uint48 change field) and fixed it; MEASURE-2 closed the rate question with margin; two small follow-ups (a log field, an entity type) ride LINK-READ. THE ADOPTION FENCE lifts on `CI: green` — the first metering adoption is Thu 09-24's CHAR session. **Saturday untouched.** **Words open:** `ERIK:` · `NAME:` silence = ii.

## §HELD-BY-THE-HUB (the wait-state ledger; edited every beat)
**Lanes (the cap: TWO + your hands; hardware exclusive; Java in series):**
| Lane | State (v76 b5) | Next |
|---|---|---|
| **The rig (EXCLUSIVE): H8-a then R-5 Part B — one sitting, Sat 09-19 10:00 CT** | both packets UNTOUCHED since v75 b2, pinned to `6bd8508`'s run #56 (artifacts uploaded 09-14, 7-day retention — KEPT by the cleanup's filter; GUARD 1 downloads the arm64 zip on the day); the navigator SCHEDULED Sat 09:50 CT (a fresh session with ClaudeFolder; one line, then it waits for `go`); `S31-LABEL:` and O-2's second reading ride the sitting; Sunday's nightly line is one paste; NO re-pin on `be4788a`/`3af6213` without `REPIN:`; run #56's pair CONFIRMED at the instrument 09-17 23:0xZ, expiring Mon 09-21 ~07:00 CT | Saturday → `H8A:` / `R5B: RETURNED <path> <bytes>` → Sunday `R5B-2:` → the v76 intakes (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C) → rehearsal 1 chartered |
| **The Java slot — ENERGY-READ RETURNED, ACCEPT (b5); the core card in your hands; LINK-READ next** | the tree 22 = 16 M + 6 A on the desk (the return 17,555 B; the b5 audit); the gate green in one round (659/1 skipped/0/0; the ArchUnit rules); a HEAD wire defect found and fixed (the uint48 reportable-change field — HEAD sent 2.1 × 10¹⁰ raw for 5, the read-back folded the same way); MEASURE-2 `rate=24031.1` events/s in memory (the store's growth → the soak / M2-2b); ENERGY-READ-b (the call-site log with `device=`; `ENERGY_METER` for a metering-only endpoint) rides LINK-READ | `CORE: ER <sha>` → `CI: <sha> green|red` (the counter's eighth sample) → the fence lifts → the first adoption Thu 09-24 (G4-1..4; REP-1) → LINK-READ chartered (wk 09-28) with ENERGY-READ-b as row 0 |
| **The web-ui slot (FE-115 after Sunday's H8-a intake)** | FE-114 LANDED `3d40b5f` (the v1.1.4 mirror); the freeze doc v1.1.5; unblocked by `be4788a` | FE-115 chartered at v76 b3 (the H8 real-wire read in hand): `conditions[].definition` rendered, the `v114` mock touch, the dead-key retirements (the b3 audit §3) |
| **The research lanes (all ACCEPT)** | DEVICE-SET · ZIGBEE-GAPS (29.7 % today, a ranked path to 88.7 %; color/buttons fenced) · FOP-1 (13 false rows; the LLC exists since 2025-07-17) · EXT-REVIEW-1 (the b3 audit) | the order placed (the bulbs Sun 09-20; the second meter Mon; the P4460 Thu 09-24 → CHAR; a Hue motion sensor in the box) → the Gen4 firmware Fri · `FOP: apply` → the hub's edit (b7) · the LLC lookup (yours, one minute) |
| **The bench lane (slot 2 — chartered b5)** | owed: `field_within`, `metering-known-load.yaml` (CHAR + the two chains + the re-written T1; `requires: [operator]`), the `link-quality.yaml` skeleton — dry-run and selftests only; desk work, no hardware | the charter at b5; the scenario before Thu 09-24; then VERIFY-72H Thu 09-24 (the frozen invariants §16; gates Sun 10-11 and Wed 10-28; the run's log indexed under the bench corpus) |
| **The closure counter 7/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508` · `e56f555` · `3d40b5f` · `3af6213`) | #252's `Run the bus soak` ✓ at the instrument (the sample `bus-soak-252` stored, 28,916 B; its XML unread from here — the download needs a token); run #251 was not a readable sample; the next push is the next; a red `bus-soak` → `SAMPLE: <sha>` | passive |
| **Company, dated** | Activate SKIP by default (a WRITTEN yes to both questions by Fri Oct 9; hours on top); the email v3 SENT 09-17; HotMobile SKIP; the knockout charter (D-v74-1; a free beat; not waiting on Erik); EXT-REVIEW-1: the pilot in November (`PILOT: after`), the buyer outreach from Fri 09-25 behind the register-checked script, THE SPOKEN CARD + its embargo side (the strategy pass Thu 09-24), C-13 data-next, C-14 no name outward; Erik — Nick's dated line 09-17, the Fri 09-25 rule; `PROTECT: done` (the ruleset on `main`: deletions restricted, force pushes blocked); the letters filed verbatim under `context/research/`; AMD-100 PROPOSED; no name in any tracked file | `ERIK: <his line>` · AMD-100's docs card |
| **The plan of record + the v75 DR (CLOSED)** | `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` (§14 the calendar; §15 EXT-REVIEW-1; **§16 the eight additions — the watts go/no-go Sun 10-11 20:00 CT; VERIFY-72H's gates; the frozen invariants**): the run Oct 30 – Nov 2; row 2 DONE; the critical path unmoved. The v75 DR CLOSED; **the v76 DR** `context/planning/2026-09-18_v76_decision-record.md`: D-v76-1..9 RULED (`PILOT: after`) | v76 re-cuts the plan on Saturday's facts |
| The landed lanes (W-SKILLS-9 `180375f` · HERO-1d `6bd8508` · P-1 · R-4c · AMD-101 `7221ddc` · the rest in the v75 DR) | ACCEPT · landed | W-SKILLS-10 folds THE PREMISE GATE + its two extensions (an idle beat); TR-1b after the run; IR-27 (October) |

**v76 (OPENED Thu 09-17 ~18:0x CT):** the deliverable — the sitting's two records intaken two-layer by Sunday night and ENERGY-READ DISPATCH-READY through THE PREMISE GATE. b1 the boot at the instrument; the b8 packet banked (one correction: #252's uploads went through — CI-2 stands by construction, not "proven"); the counter 7/20; b2 Nick's brief attacked at the bytes (the audit §0 is the one screen) — the calendar re-cut to Wed 09-24, the band to 3.03 %, `GEN4-READ: rehearsal-2`, OR-METERING-VOLUME opened; b3 EXT-REVIEW-1 adjudicated — the pilot in November, the run as two artifacts, three register rows, the spoken clause; 09-24 a Thursday; b4 ENERGY-READ through the gate, dispatched; THE MEASUREMENT RECORD minted; the eight additions dated (§16); b5 ENERGY-READ returned and ACCEPT — a HEAD wire defect fixed on the way; the core card cut; `PROTECT: done`; the letters filed. **v75 (CLOSED b8, Thu 09-17 ~16:xx CT):** eight lanes accepted; three core landings green; THE PREMISE GATE minted and extended; the plan re-cut; the hub errors owned in the b3, b4, b6, b7 audits.

**Open words (asked only when they gate an act):** `CORE: ER <sha>` · `HIVE: LANDED <sha>` (the b5 card) · `CI: <sha> green|red` · `ERIK: <his line>` · `NAME:` (silence = ii) · `SAMPLE: <sha>` · `NAV: manual` · `REPIN: <sha>` · `REVERT <D-n|GEN4-READ>`.

**Given (banked; no act):** `PROTECT: done` · `LETTERS: RETURNED` · `ER: RETURNED` (17,555) · `PILOT: after` · `EXPORT: letters` · `DEVICES: extended <edits>` · `GAPS: neither` · `HOTMOBILE: skip` · `FOP: apply` · `STRATEGY: after-R5B` (narrowed) · `CASCADE: engine` · `AMD100: ratify` · `REPIN: none` · `GEN4-READ: rehearsal-2` (ruled) · `ARTIFACTS: 62 deleted, 9,195 MB` · `CORE: CI2 3af6213` · `CI: 3af6213 green` (#252 at the instrument) · `HIVE: LANDED 08b8474` · `ACTIVATE: sent` (09-17) · `H8: Sat 09-19 10:00` · `DUR: measure` and `HASH1: a` (fixed at `be4788a`) · Activate skip-by-default (the Oct 9 rule) · `FRAME: a` · `PLUG: two` · THE PREMISE GATE (code, outward text, spoken) · `SCOPE: lane` + the three fences · THE ADOPTION FENCE · THE MEASUREMENT RECORD · `PROTECT: core + docs` · `PROTECT: no-force` · `SAMPLES: passive` · `SEARCH-NAME: PALOKI` FINAL · the earlier words in the v74 and v75 DRs.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68:** at `ea96351`. **v69–v70:** at `3209101`. **v71:** at `256c26b`. **v72–v73:** at `98e2f39`. **v74:** at `9795c2c`. **v75 (Mon 09-14 → Thu 09-17):** in this file's history at `d02557d` and the b8 sha — b1 the boot · IR-1 measured · H8-a re-pinned · R-5B through the gate → `cc17ce7`; b2 114c and FE-114 authored → `a8c987d`; b3 both ACCEPT → `553bf5b` → core `e56f555` + `3d40b5f`; b4 CI green ×2 · the freeze doc v1.1.5 · THE PREMISE GATE · MEASURE-1 · the dossier · the six-week plan → `0eef472`; b5 MEASURE-1 ACCEPT · DUR-1 + HASH-1 chartered · DEVICE-SET chartered · the spine rotated → `13e8d64`; b6 DUR-1 + HASH-1 and DEVICE-SET ACCEPT · the Activate plan re-cut · ZIGBEE-GAPS and FOP-1 chartered → `d02557d` → core `be4788a`; b7 the CI red read (the storage quota) · CI-2 spliced · ZIGBEE-GAPS and FOP-1 ACCEPT · the email v3; b8 `be4788a` GREEN at the instrument · IR-22/23 retired · the HASH NOTE a fact · the v76 dispatch text · the close packet → `08b8474` + core `3af6213`. **v76 (Thu 09-17 →):** b1 the boot at the instrument · the b8 packet's verdicts intaken (CI #252 green, uploads through; the counter 7/20; run #56's pair to Mon) · the dispatch text filed verbatim → `5bbbd14`; b2 Nick's brief attacked · nine words banked, the DR ruled · the calendar §14 · CHAR · IR-25/IR-28 · OR-METERING-VOLUME · `GEN4-READ: rehearsal-2` → `c0158a1`; b3 EXT-REVIEW-1 adjudicated (the packet filed; the v76 DR; the plan §15; the lesson; IR-29/30/31) → `7605dfc`; b4 ENERGY-READ through the gate, dispatched · THE MEASUREMENT RECORD · the eight additions dated (§16) → `6384e64`; b5 ENERGY-READ intaken two-layer, ACCEPT (the wire defect; MEASURE-2; six deviations ruled; five misses owned) · the letters filed · the spine rotated → the b5 packet (the core card + the hivemind card).

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · adopt a water-leak, smoke or vibration device before IR-15 lands · adopt a metering plug before ENERGY-READ is green · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion · pair the Gen4 plugs before ENERGY-READ · send or SAY a sentence the register does not hold (a post, a pitch, a recruitment page) · re-pin Saturday on `be4788a`/`3af6213` without `REPIN:` · delete run #56's artifacts.
