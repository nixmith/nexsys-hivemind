<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v75 beat 1 (Mon 2026-09-14 ~14:0x CT; instrument 2026-09-14T19:01:21Z). §NEXT = the b1 packet (the hivemind card; the IR-1 run); §HELD: both sitting packets on the disk; D-v75-1 open.
-->

# Operator brief (one act at a time)

## §NEXT — ONE act: the v75 beat-1 packet — block 1 the hivemind card (9 files) → `HIVE: LANDED <sha>`; block 2 the IR-1 run in `homesynapse-core` (minutes) → `IR1: RETURNED <path> <bytes>` (Mon 2026-09-14 ~14:0x CT)
Run `_scratch/v75/2026-09-14_v75-b1_operator-packet.md` block 1 in Git Bash in `nexsys-hivemind` (`staged: 9`) and say back `HIVE: LANDED <sha>`. **Then block 2** in `homesynapse-core`: the IR-1 measurement (a copy of the test into the source set, one Gradle run, the copy removed — core porcelain 0 at the end) and say back `IR1: RETURNED _scratch/v75/2026-09-14_IR-1_measurement.txt <bytes>`. Both Saturday packets are on the disk: H8-a re-pinned to `6bd8508`'s run #56; R-5B cut through THE PRIOR-LEDGER GATE (one navigator window, both packets; H8-a's B6 skipped — R-5B carries the restore). **Your words, in order:** `H8: Sat 09-19 <hh:mm>` (one word names the sitting; R-5B follows in it) · `FRAME: a|b` (D-v75-1 — the audit §6; silence = a) · `Activate: hold` Tuesday · silence on `NAME:` = ii. **Owed whenever:** `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify`.

## §HELD-BY-THE-HUB (the wait-state ledger v75 inherits; edited every beat)
**Lanes (the cap: TWO + your hands; hardware exclusive; Java in series):**
| Lane | State (v74 close, 18:0xZ) | Next |
|---|---|---|
| **The rig (EXCLUSIVE): H8-a then R-5 Part B — one sitting** | H8-a RE-PINNED to `6bd8508`'s run #56 (v75 b1; the digest `11f3a79e…`; the one-sitting clause at B6 and §N); R-5B's packet CUT through THE PRIOR-LEDGER GATE (v75 b1; 28 greps, the audit §4; the S31 re-read — F-R4b-C's `clusters=1` IS in the corpus, prediction `clusters=1`; the frame block as a LOWER BOUND — D-v75-1); `S31-LABEL:` and O-2's second reading ride the sitting; the second nightly line is Sunday's one paste | `H8: Sat 09-19 <hh:mm>` (one word) → the navigator paste scheduled ten minutes before → `H8A:` and `R5B: RETURNED <path> <bytes>` → Sunday `R5B-2: <digest line>` → the intakes (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → the C-003 ZDO clause; P-B6 → the plug card) |
| **The Java slot (free; in series 114c → ENERGY-READ → LINK-READ)** | 114b `fed99e8` green; the v1.1.4 freeze note WRITTEN (v74 b3); IR-1's instrument AUTHORED (v75 b1: `_scratch/v75/Ir1PageCountMeasurementTest.java`; by construction pages = ceil(N/500); the run is the b1 packet's block 2 — the container cannot run Gradle); 114c = the v1.1.5 keys · D4's DP-B2 sentence · IR-2 · IR-3, K2 sized by IR-1's lines | v75 b2: IR-1's lines filed into the register → 114c authored (the test carried as its tests-first row, tagged) and pasted (host-side Claude Code) → then ENERGY-READ (IR-15 FIRST) → LINK-READ (IR-20's counter first, unless `FRAME: b`) |
| **The web-ui slot (free): FE-114** | HERO-1d `6bd8508` green (the counter 4/20; the literal lint in `verify`); FE-114 = the v1.1.4 keys' rendering + IR-12 (the lint's reach) · IR-13 (the dialect US; the residual spellings) · IR-14 (`pendingHint`) · the two unconsumed terminal rows | v75 b2: authored and pasted (a fresh Cowork conversation) |
| **The bench lane (free)** | R-5 Part A + ii `f3631cb` landed; owed: `field_within` (the STOP-gate word; `within\|outside`), `metering-known-load.yaml` with `requires: [operator]` + the reference-meter operator blocks, the `link-quality.yaml` skeleton — dry-run and selftests only | v75 b5, when the Java slot frees |
| **The plugs — `PLUG: two` GIVEN (D-v74-4)** | two plugs of ONE STANDARD_ZCL model with 0x0B04 AND 0x0702 as server clusters (multiplier/divisor present; the divisor into the fixture first) + one plug-in reference meter; the Shelly Plug US Gen4 units you own are graded FIRST on the same criteria; **THE ADOPTION FENCE: no metering plug joins any card before ENERGY-READ is green** | v75 b3: the read-only dossier micro-lane (ONE file; no grading in chat) → the one-paste shopping card |
| **The closure counter 4/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508`, all green, `SAMPLES: passive`) | every push from `a458a64` counts; a red `bus-soak` → `SAMPLE: <sha>` | passive |
| **Company, dated** | `Activate:` D-v74-2 — the word Tuesday (rec hold → the `/apply` page read + ONE email paste, v75 b4); the knockout charter for the second product-name candidate (D-v74-1, silence = ii; v75 b4; token-parameterized; no name in any tracked file); Silabs Thu 09-17; Erik — PALOKI search STARTED 09-13 (no line since; no public name before the opinion); `PROTECT: no-force` — your six clicks; AMD-100 PROPOSED | `Activate: hold` Tue · `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` |
| **The ambition map (D-v74-3, ACCEPT)** | the plan §10 + §11; ENERGY-READ · LINK-READ · CORROBORATE · TIER2-FIRST wait on R-5B + rehearsal 1 (no word due); IR-15 (a DEFECT; the IAS fence) · IR-16..IR-18 (the seams) · IR-19 (the three `.docx` → `.md`, an idle beat); your market/IoT strategy note recorded (the DR §4) — the v75 hub packages its H10 | v75 b6–b7: IR-19; IR-15 in ENERGY-READ; October authored ahead, dispatched by nothing |
| W-SKILLS-9 `1b1eb98`/`180375f` · HERO-1d `6bd8508` · P-1 `1201368` · R-4c (C-003 MINTED) · AMD-101 `7221ddc` · HONESTY-1 · FE-NULL-1 · TR-1 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · landed | TR-1b after `card-gradle:` (Saturday, at the card) |

**v74 (CLOSED b8, Mon 09-14 ~13:0x CT; OPENED Sun 09-13 ~18:5x CT):** the deliverable HALF-MET — H8-a's packet through the gate; R-5B's packet handed to v75 b1 (no packet on the tail of a tired session); the sitting Sat 09-19 by default. Four lanes returned and accepted; one core landing green; eight hivemind commits; one skills commit.

**Open words (asked only when they gate an act):** `H8: Sat 09-19 <hh:mm>` (R-5B follows in the sitting) · `FRAME: a|b` (D-v75-1; silence = a) · `Activate: apply|hold|skip` (Tue; rec hold) · `NAME: i|ii|iii` (silence = ii) · `AMD100: ratify` · `REVERT <D-n>` only to refuse a standing ruling.

**Given (banked; no act):** `PLUG: two` + the two criteria + THE ADOPTION FENCE · `SCOPE: lane` + the three fences · the copy dialect US (v74 b5) · THE MEASUREMENT RECORD (minted once at ENERGY-READ) · the frame rate counted at the ingestion unit · `requires: [operator]` for reference-meter scenarios · `HERO1: Q1–Q7 a` · `EXPLAIN: three` · `PROTECT: core + docs` · `PROTECT: no-force` · `SAMPLES: passive` · `SEARCH-NAME: PALOKI` FINAL · `TR1-B2 = DRIVER` · `EU-DEFER` (provisional) · D4's seven recs · THE OPERATOR-LOAD LAW · D1–D7 · D-v74-1..4.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68 (Sun 09-06 → Thu 09-10):** compressed in this file's history at `ea96351` (FE-113 · TR-0 · the H8-a packet · F-R4-1b · BLOCK 6 · HERO-0 · RS-12-F · D1–D7 · HIVE-CLEAN-1 · W-SKILLS-8 · TR-1 · NAMING-B · the Erik packet · RS-13 PALOKI · the momentum map · HONESTY-1 `94ae99d` · Dependabot `3f3f5cc` · FE-NULL-1 `eabdbb1`). **v69 (Thu 09-10 → Fri 09-11):** in this file's history at `3209101` (the H8-a send; the trailers and hands words; PROTECT scoped; HERO-1 landed `1e26912`; EXPLAIN-114a authored). **v70 (Fri 09-11 → Sat 09-12 ~10:5x CT):** in this file's history at `3209101` (R-4c's packet; FIX-2a, FIX-2b-i; HERO-1b `6af76f7`; P-1 chartered; CI-1 landed `ff1a6e1`; closed at b10). **v71 (Sat 09-12 ~11:2x → ~18:5x CT):** in this file's history at `256c26b` (FIX-2b-ii; `72efb42`; `BUSFIX: b`; AMD-101 and BUS-ORDER-1 authored; EXPLAIN-114a returned, corrected (R3), landed `5f918c7`; sample #15 red, unread; closed at b5, the post-close intake b6). **v72 (Sat 09-12 ~19:3x → Sun 09-13 ~13:2x CT):** in this file's history at `98e2f39` (BUS-ORDER-1 `a458a64` green; AMD-101 ratified; R-4c returned and audited — C-003 MINTED-BOUNDED, `H8A: not run`; P-1 returned ACCEPT; closed at b7). **v73 (Sun 09-13 ~13:5x → ~18:1x CT):** in this file's history at `98e2f39` (114b `fed99e8` green — the deliverable; HERO-1c + its correction `93390f0` green; R-5 Part A + ii `f3631cb`; the plan re-cut; the improvement register minted; W-SKILLS-9 chartered; closed at b7). **v74 (Sun 09-13 ~18:5x → Mon 09-14 ~13:0x CT):** in this file's history at `9795c2c` (the boot 41,959 B; W-SKILLS-9 `180375f`; the H8-a re-cut through the gate; the freeze note; C-003 MINTED-BOUNDED; HERO-1d `6bd8508` green (4/20); the spine rotated; the ambition map ACCEPT with the critique; IR-15..IR-19; `PLUG: two` with the criteria and the fence; the plan §11; closed at b8). **v75 (Mon 2026-09-14 ~13:1x CT →):** the boot (≈45.3 KB; the preflight 12/12; Check 9 PASS 28/28; the five HEADs = your line; the b8 card verified at diff-tree, 6 files); your dispatch text filed verbatim; IR-1's instrument authored by an in-conversation agent (the run blocked at the container's egress → your host, the b1 packet's block 2); the H8-a packet re-pinned to `6bd8508`'s run #56 (the digest read at the run page, the method checked on run #55's); R-5B's packet cut through THE PRIOR-LEDGER GATE (28 greps; R-4b's ledger re-grepped this time) with the S31 re-read and the frame block; IR-20; D-v75-1 packaged → the b1 card.

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · adopt a water-leak, smoke or vibration device before IR-15 lands · adopt a metering plug before ENERGY-READ is green · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion (the .com is yours to buy at your own risk; the plan buys it the day the opinion is clean).
