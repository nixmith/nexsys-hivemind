<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v69 beat 3 (Fri 2026-09-11 ~16:2x CT; instrument 2026-09-11T21:28:56Z). §NEXT = land HERO-1 (core) and the spine (hivemind), two cards; then the HERO1 ruling batch and the EXPLAIN word. H8-a is tonight 19:00 CT; the 18:45 send is armed. Then b4: the EXPLAIN v1.1.4 instruction.
-->

# Operator brief (one act at a time)

## §NEXT — land HERO-1 and the spine (two cards, 2 minutes), then seven words
**Card 1 — core (Git Bash).** Commits the three design files (0 M + 3 A) and pushes; `frontend.yml` runs and is unaffected (nothing under `src/`):
```bash
cd /c/Users/Nick/Desktop/Code/ClaudeFolder/homesynapse-core && git --no-optional-locks status --porcelain; if [ "$(git --no-optional-locks status --porcelain | wc -l)" = 1 ] && ! grep -qi 'co-authored\|claude-session' ../_scratch/2026-09-11_core_HERO-1_commit-msg.txt; then git add -A web-ui/dashboard/design && [ $(git --no-optional-locks diff --cached --name-status | wc -l) -eq 3 ] && git commit -q -F ../_scratch/2026-09-11_core_HERO-1_commit-msg.txt && git log -1 --format='%h %s' | cut -c1-80 && git --no-optional-locks status --porcelain | wc -l && git push 2>&1 | tail -1; else echo 'STOP: unexpected porcelain or message check failed'; fi
```
**Done looks like:** `?? web-ui/dashboard/design/`, the `docs(dashboard): HERO-1` subject, `0`, `eabdbb1..<sha>`.
**Card 2 — hivemind (Git Bash).** Commits beats 2 and 3 together (the charter, the return, the audit, the rotated archive; 4 M + 4 A) and pushes:
```bash
cd /c/Users/Nick/Desktop/Code/ClaudeFolder/nexsys-hivemind && M=$(git --no-optional-locks status --porcelain | grep -c '^ M'); A=$(git --no-optional-locks status --porcelain | grep -c '^??'); echo "M=$M A=$A"; if [ "$M" = 4 ] && [ "$A" = 4 ] && ! grep -qi 'co-authored\|claude-session' ../_scratch/2026-09-11_hivemind_v69-beat2-3_commit-msg.txt; then git add -A && [ $(git --no-optional-locks diff --cached --name-status | wc -l) -eq 8 ] && git commit -q -F ../_scratch/2026-09-11_hivemind_v69-beat2-3_commit-msg.txt && git log -1 --format='%h %s' | cut -c1-80 && git --no-optional-locks status --porcelain | wc -l && git push 2>&1 | tail -1; else echo 'STOP: census or message check failed'; fi
```
**Done looks like:** `M=4 A=4`, the `hivemind: v69 beats 2-3` subject, `0`, `19dd310..<sha>`.
**Words back (one line):** `LAND: <core sha> <hivemind sha>` · the ruling batch `HERO1: Q1 a · Q2 a · Q3 a · Q4 a · Q5 a · Q6 a · Q7 a` (the hub's recommendations — one word per row, or `HERO1: all recs`; Q1–Q6 are SPEC §12, Q7 is the audit §4: ratify "the hub" as the UI term) · `EXPLAIN: three` or `EXPLAIN: one` (three coder WUs with one contract bump each — v1.1.4, .5, .6 — is the hub's recommendation and the default if silent; one WU under one bump is the map's original label).
**Tonight:** at 18:45 CT a scheduled message hands you the H8-a paste for 19:00 at the rig (≤60 min; hardware exclusive; nothing else while there). If nothing arrives by 18:45, open `nexsys-hivemind/context/instructions/2026-09-06_H8a_real-wire_v113-keys_and_failchan-proof_navigator-packet.md` and follow its §0. `PROTECT: done` stays owed whenever it fits.

## §HELD-BY-THE-HUB (edited every beat; you read it, you never remember it)
**Lanes (the cap: TWO + your hands; hardware exclusive):**
| Lane | State (v69 beat 1, 01:12Z — THE BOOT) | Next |
|---|---|---|
| **HONESTY-1 `94ae99d` · FE-NULL-1 `eabdbb1`** | LANDED Thu; ci + frontend GREEN; install-smoke per push (P4 MET); both instructions flipped to EXECUTED at this beat | closed; LASTREPORTED-1b rides R-4c (read `lastReported` on one never-reported entity after the `.deb`); O1 (the L1 headline per outcome) = the hero charter's first row |
| **HERO-1 — the hero design (web-ui; slot 2)** | RETURNED Fri 15:48 CT (7,294 B) and audited **ACCEPT** at the bytes (`context/audits/2026-09-11_HERO-1_intake_two-layer-audit_v69-b3.md`): 3 files under `design/hero-v1/`, P1–P4 met, the modes rendered and inspected, no name but `{{NAME}}` | **§NEXT** — card 1 lands it; `HERO1:` rules SPEC §12 + Q7; then **HERO-1b** (SPEC §11 B1–B7; B1 = the headline per outcome with its test) in the web-ui slot |
| **EXPLAIN v1.1.4 (Core Java; slot 1)** | **v69's ONE deliverable** — authored next (b4). The hub's read of the ten keys: they span the engine, the config loader, the explanation service and the REST endpoints — more than one compile-and-commit unit. Recommendation: three coder WUs, one bump each (114a the keys the emitter already holds in memory · 114b parent run id + condition text + the stable definition key · 114c per-permit trigger refs); `EXPLAIN: three` is the default | your paste into a host-side Claude Code session (114a first; handed with the instruction, tonight after H8-a or Saturday morning) → its landing → HERO-1b B8 rows |
| **H8-a (the rig; EXCLUSIVE)** | **TONIGHT** — the 18:45 CT send is armed (`trig_01AHRS3Q29MtP6q39yT24ogs`, fires 2026-09-11T23:45Z); the packet is on disk | **19:00 CT** (`H8:` only to move it); no paste by 18:45 → you open the packet yourself; `H8A: RETURNED <path> <bytes>` when done; FE-113b's capture folds into HERO-1b |
| **R-4c (the rig; MEASUREMENT-ONLY on the core tree)** | packet: v69 authors it Fri (beat 4) | **Sat 09-12** (`R4C: Sun` to move it); no Core write Saturday — EXPLAIN keeps slot 1; C-003 is the exit; `card-gradle:` read once at the card |
| **P-1 (the power-harness primitive as a bench verb)** | charter: v69 beat 5, directly after R-4c's packet | the October 72-h rehearsals gate on it |
| **`PROTECT: no-force` (GitHub; your hands)** | your word (b2): core + docs only, the public repos; the six clicks handed in chat for both | `PROTECT: done` |
| **The skill mirror sync (Check 9)** | STALE after this commit — `project-manager/SKILL.md` changed (the trailer correction) | your usual sync, whenever; no lane waits on it |
| **Erik — the PALOKI search (wait-state)** | packet sent Thu ~12:00 CT | `ERIK: <his line>` when it comes; no public use before the opinion |
| TR-1 · Act 12 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · RETIRED · landed ×8 | TR-1b after `card-gradle:` (Saturday) |

**v69's blocks, in order (the hub's list, not yours; the map RE-CUT at v69 b1 with your three edits: `context/planning/2026-09-10_v68_MOMENTUM-MAP_two-weeks_critical-path-and-Nicks-hours.md`):** the H8-a send re-armed (b1 — DONE) → the map re-cut (b1 — DONE) → the hero design charter (b2 — DONE) → HERO-1 returned + audited ACCEPT (b3 — DONE; landing in your hands) → **the EXPLAIN v1.1.4 instruction — 114a first (b4; the ONE deliverable; slot 1; dispatches tonight after H8-a or Saturday morning)** → R-4c's packet (b4; measurement-only; Sat) → **P-1's charter (b5; directly after R-4c)** → the H8-a paste at 18:45 Fri (scheduled) → TR-1b (on `card-gradle:`) → the B-7 ADR word → W-SKILLS-9 (+ O3 the FE toolchain line · the lock-flag sentence in every lane line · the RETURNED-line template). The window closes at or before beat 8; what remains goes to v70's §HELD. **Also at b1:** HONESTY-1 and FE-NULL-1 instructions set to EXECUTED (Check 12); the H8-a packet's status names its Friday slot; the trailer regression corrected in the PM skill and the boot prompt (68 past commits carry trailers; history stays); your second word taken — the hub never commits or pushes, in any repo; every landing is a card for your hands.

**Owed lines (none gates an act):** `card-gradle: <the first line of ./gradlew --version on the card | absent>` — Saturday, at the card (TR-1b's shape depends on it). *(`samples:` · `sample4:` · `act3:` · `nightly 09-05/09-06:` RETIRED by your `SAMPLES: passive`.)*

**Open words (asked only when they gate an act):**
- `H8: <day> <hh:mm>` — only to MOVE the default (Fri 09-11 19:00 CT); silence = it stands.
- `R4C: Sun` — only to move R-4c off Sat 09-12; silence = Saturday.
- `ERIK: <his line>` — when he replies (sight-read · date · surcharge); verbatim is fine, one line.
- `REVERT TR1-B2` · `REVERT EU-DEFER` — only if you refuse those rulings; silence = they stand. · `Activate: apply|hold` (09-15) — not yet due.

**Given (banked; no act):** `PROTECT: core + docs` (Thu ~21:5x CT) · your two words on trailers and on hands (Thu ~20:4x/~21:0x CT) · **your five words of Thu 09-10 evening — `DESIGN: start` · `PROTECT: no-force` · `SAMPLES: passive` · R-4c MEASUREMENT-ONLY on the core tree · P-1 directly after R-4c · the H8-a fallback (you open the packet at 19:00 if v69 is not booted by 18:45)** · `EAR-PALOKI: pass` → `SEARCH-NAME: PALOKI` FINAL · the packet SENT Thu · `V69: now` · your availability all week · HONESTY-1 `94ae99d` · Dependabot `3f3f5cc` · FE-NULL-1 `eabdbb1` · `TR1-B2 = DRIVER` · `EU-DEFER` (provisional) · D4's seven recs STAND · Erik = U.S. only · `BEYOND-LETTERS: keep` · `BLOCK6: pull` (landed) · THE OPERATOR-LOAD LAW · the v66 decisions D1–D7.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v67 (Sun 09-06 → Wed 09-09), compressed:** FE-113 `d192d17` · TR-0 · the H8-a packet on disk · F-R4-1b `39c8dd3` · BLOCK 6 · HERO-0 · RS-12-F · D1–D7 · HIVE-CLEAN-1 `30f800d` · W-SKILLS-8 `90529d1` + skills `c630c5c` · the v67 stable prompt (re-cut for context) · TR-1 ACCEPT · NAMING-B · the Erik packet as PDF + DOCX · VERDOMO retired · RS-13 (PALOKI) ACCEPT · v67 closed at 7 beats → **v68 (Wed 09-09 evening → Thu 09-10 ~19:4x CT), compressed:** the PALOKI packet authored and sent (the ear test passed); the momentum map filed; HONESTY-1 dispatched, audited, LANDED `94ae99d` (ci GREEN; install-smoke per push, P4 met); Dependabot `3f3f5cc`; FE-NULL-1 authored, dispatched, audited, LANDED `eabdbb1` (frontend GREEN); your five words banked; v68 closed at beat 6, no compaction; v69 dispatched → **v69 booted 01:00:18Z Fri (~20:00 CT Thu)**: the dispatch + your terminal paste filed verbatim; five HEADs clean, ahead 0 (the v68 close PUSHED — verified at porcelain); preflight 12/12 after two Check-12 flips; the H8-a 18:45 send ARMED; the map re-cut on your three edits; your word on trailers taken: the PM skill and the boot prompt corrected, the message re-written, two lessons minted; your second word taken (the hub never commits or pushes; every repo is your hands); the landing card handed as the one act (v69 beat 1) → **~21:0x–22:0x CT: beat 1 LANDED `19dd310` (your hands); PROTECT scoped to core + docs on your word and the steps handed; HERO-1 chartered; its dispatch handed as the one act (v69 beat 2) → **Fri ~16:3x CT: HERO-1 returned (Fri 15:48 CT) and audited ACCEPT at the bytes — sizes, md5s, greps, cites, the mockups rendered; the two landing cards + the HERO1 ruling batch + the EXPLAIN word handed (v69 beat 3).**

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion (the .com is yours to buy at your own risk; the plan buys it the day the opinion is clean).
