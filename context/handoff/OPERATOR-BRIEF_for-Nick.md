<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v75 beat 8, THE v75 CLOSE (Thu 2026-09-17 ~16:4x CT; instrument 2026-09-17T21:42:02Z). §NEXT = the close packet (the cleanup with the GitHub CLI → the CI-2 landing → the hivemind card → the email v3 → the v76 dispatch paste); §HELD: `be4788a` GREEN at the instrument; IR-22/IR-23 retired; the sitting Saturday untouched.
-->

# Operator brief (one act at a time)

## §NEXT — ONE act: the v75 close packet — block 1 the artifact cleanup (install the GitHub CLI once: `winget install --id GitHub.cli -e`, a NEW terminal, `gh auth login`, then the list, then the delete; run #56's artifacts KEPT) → `ARTIFACTS: <n> deleted, <MB>`; block 2 the CI-2 landing (core, 4 files) → `CORE: CI2 <sha>` then `CI: <sha> green|red`; block 3 the hivemind card (16 files) → `HIVE: LANDED <sha>`; block 4 the Activate email v3 → `ACTIVATE: sent`; block 5 the v76 dispatch — paste `context/handoff/2026-09-17_v76_dispatch-text.md`'s block into a FRESH hub session (Thu 2026-09-17 ~16:4x CT)
Run `_scratch/v75/2026-09-17_v75-b8_operator-packet.md` blocks 1 → 5. The verdict on `be4788a` is banked from your screenshot and your two `check` runs (`Run check` green; only the upload step red) — nothing to say back for it. The v76 hub opens on block 5 and charters ENERGY-READ; this session closes. **Your words, whenever (silence = the recs):** `DEVICES: core|extended|<edits>` (rec core) · `GAPS: color|buttons|both|neither` (rec neither before the run) · `HOTMOBILE: submit|skip` (rec skip; due Oct 9) · `FOP: apply|hold` (your file) · `NAME:` silence = ii · `H8: Sat 09-19 10:00` GIVEN (the navigator opens by itself 09:50 CT; `NAV: manual` cancels; Sunday `R5B-2:`). **Owed whenever:** `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` · `STRATEGY:` · `CASCADE:`.

## §HELD-BY-THE-HUB (the wait-state ledger; edited every beat)
**Lanes (the cap: TWO + your hands; hardware exclusive; Java in series):**
| Lane | State (v75 close) | Next |
|---|---|---|
| **The rig (EXCLUSIVE): H8-a then R-5 Part B — one sitting, Sat 09-19 10:00 CT** | both packets UNTOUCHED since v75 b2, pinned to `6bd8508`'s run #56 (artifacts uploaded 09-14, 7-day retention — KEPT by the cleanup's filter; GUARD 1 downloads the arm64 zip on the day); the navigator SCHEDULED Sat 09:50 CT (a fresh session with ClaudeFolder; one line, then it waits for `go`); `S31-LABEL:` and O-2's second reading ride the sitting; Sunday's nightly line is one paste; NO re-pin on `be4788a` without `REPIN:` | Saturday → `H8A:` / `R5B: RETURNED <path> <bytes>` → Sunday `R5B-2:` → the v76 intakes (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C) → rehearsal 1 chartered |
| **The Java slot (free → ENERGY-READ at v76 b1 → LINK-READ)** | DUR-1 + HASH-1 LANDED `be4788a`, **GREEN at the instrument** (`Run check`: BUILD SUCCESSFUL, 156/156; the desk's `check` ×2); IR-22/IR-23 RETIRED; the freeze doc's HASH NOTE a fact; CI-2 on the card (uploads never gate; report retention 3 days; the .deb 7) | v76 b1 charters ENERGY-READ through the gate: IR-15 first; the two handlers with the divisors read at interview; IR-24 classify by clusters; IR-25 the divisor-scaled thresholds; the divisor read at the first read; THE MEASUREMENT RECORD minted; the fixture = `DEVICES: core` + ZIGBEE-GAPS rows 3/7 → LINK-READ |
| **The web-ui slot (FE-115 after Sunday's H8-a intake)** | FE-114 LANDED `3d40b5f` (the v1.1.4 mirror); the freeze doc v1.1.5; unblocked by `be4788a` | FE-115 chartered at v76 b3 (the H8 real-wire read in hand): `conditions[].definition` rendered, the `v114` mock touch, the dead-key retirements (the b3 audit §3) |
| **The research lanes — all returned, all ACCEPT** | DEVICE-SET (`DEVICES: core` $104.78 rec) · ZIGBEE-GAPS (29.7 % of the field's 4,499 definitions handled today, band 27.7–31.0; twelve rows to 88.7 %; color and buttons are scope fences → `GAPS:`; IR-26) · FOP-1 (13 of 34 FOP claims FALSE; Activate = the NSF EF, one application, no PhD rule; the LLC exists since 2025-07-17; HotMobile due Oct 9 → `HOTMOBILE:`) | `DEVICES:` → your purchase → the Gen4 firmware update · `GAPS:` → October's charters · `FOP:` (your file) · the LLC's standing: charter 46564936K on the Louisiana SoS site (one minute, yours) |
| **The bench lane (free)** | owed: `field_within`, `metering-known-load.yaml` (DEVICE-SET's §2 operator blocks; `requires: [operator]`), the `link-quality.yaml` skeleton — dry-run and selftests only | after the sitting |
| **The closure counter 6/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508` · `e56f555` · `3d40b5f`) | run #251 was not a readable sample (its XML never stored); the CI-2 push is the next; a red `bus-soak` → `SAMPLE: <sha>` | passive |
| **Company, dated** | Activate (D-v74-2 re-cut b6): SKIP by default; a WRITTEN yes to both questions by Fri Oct 9, hours added on top; the email v3 (block 4) — the product in security/policy/audit terms, every sentence on the register; HotMobile 2027 Oct 9 AoE (rec skip); the FOP's 13 false rows (yours); the knockout charter (D-v74-1; v76 b2); Silabs Thu 09-17; Erik — no line since 09-13; `PROTECT: no-force` — your six clicks; AMD-100 PROPOSED; the entity rename for commercial use — no name in any tracked file | `ACTIVATE: sent` · `HOTMOBILE:` · `FOP:` · `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` |
| **The plan of record + the v75 DR (CLOSED)** | `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` (§0–§9; §10–§12 addenda; **§13 v76's window**): the run Oct 30 – Nov 2; row 2 DONE; the critical path unmoved. The DR: D-v75-1/4/5 RESOLVED; D-v75-6/7/8 and D-v75-2/3 open on your words (silence = the recs) | v76 re-cuts the plan on Saturday's facts |
| W-SKILLS-9 `180375f` · HERO-1d `6bd8508` · P-1 `1201368` · R-4c · AMD-101 `7221ddc` · HONESTY-1 · FE-NULL-1 · TR-1 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · landed | W-SKILLS-10 folds THE PREMISE GATE + the outward-text extension (an idle beat); TR-1b after the run; IR-27 a pin-gated .deb upload (October) |

**v75 (CLOSED b8, Thu 09-17 ~16:xx CT; OPENED Mon 09-14 ~13:1x CT):** the deliverable MET at b1–b2 (both Saturday packets through the gate; `H8: Sat 09-19 10:00`) and held untouched; eight lanes accepted; three core landings green at the instrument; seven hivemind commits + the close card; THE PREMISE GATE minted and extended; the plan re-cut; one navigator scheduled; hub errors owned in the record (the b3, b4, b6, b7 audits).

**Open words (asked only when they gate an act):** `ARTIFACTS: <n> deleted, <MB>` · `CORE: CI2 <sha>` · `CI: <sha> green|red` · `HIVE: LANDED <sha>` · `ACTIVATE: sent` · `DEVICES:` · `GAPS:` · `HOTMOBILE:` · `FOP:` · `NAME:` (silence = ii) · `STRATEGY:` · `CASCADE:` · `AMD100: ratify` · `NAV: manual` · `REPIN: <sha>` · `REVERT <D-n>`.

**Given (banked; no act):** `H8: Sat 09-19 10:00` · `DUR: measure` → RED → fixed at `be4788a` · `HASH1: a` → fixed at `be4788a` · Activate skip-by-default (the Oct 9 rule) · `FRAME: a` · `PLUG: two` → DEVICE-SET · THE PREMISE GATE (code and outward text) · `SCOPE: lane` + the three fences · THE ADOPTION FENCE · the copy dialect US · THE MEASUREMENT RECORD · `requires: [operator]` · `HERO1: Q1–Q7 a` · `EXPLAIN: three` · `PROTECT: core + docs` · `PROTECT: no-force` · `SAMPLES: passive` · `SEARCH-NAME: PALOKI` FINAL · `TR1-B2 = DRIVER` · `EU-DEFER` · D4's seven recs · THE OPERATOR-LOAD LAW · D1–D7 · D-v74-1..4.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68:** at `ea96351`. **v69–v70:** at `3209101`. **v71:** at `256c26b`. **v72–v73:** at `98e2f39`. **v74:** at `9795c2c`. **v75 (Mon 09-14 → Thu 09-17):** in this file's history at `d02557d` and the b8 sha — b1 the boot · IR-1 measured · H8-a re-pinned · R-5B through the gate → `cc17ce7`; b2 114c and FE-114 authored → `a8c987d`; b3 both ACCEPT → `553bf5b` → core `e56f555` + `3d40b5f`; b4 CI green ×2 · the freeze doc v1.1.5 · THE PREMISE GATE · MEASURE-1 · the dossier · the six-week plan → `0eef472`; b5 MEASURE-1 ACCEPT · DUR-1 + HASH-1 chartered · DEVICE-SET chartered · the spine rotated → `13e8d64`; b6 DUR-1 + HASH-1 and DEVICE-SET ACCEPT · the Activate plan re-cut · ZIGBEE-GAPS and FOP-1 chartered → `d02557d` → core `be4788a`; b7 the CI red read (the storage quota) · CI-2 spliced · ZIGBEE-GAPS and FOP-1 ACCEPT · the email v3; b8 `be4788a` GREEN at the instrument · IR-22/23 retired · the HASH NOTE a fact · the v76 dispatch text · the close packet.

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · adopt a water-leak, smoke or vibration device before IR-15 lands · adopt a metering plug before ENERGY-READ is green · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion · pair the Gen4 plugs before ENERGY-READ · send outward text the register does not support · re-pin Saturday on `be4788a` without `REPIN:` · delete run #56's artifacts.
