<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v77 beat 3, KO-2 INTAKEN (Sat 2026-09-19 ~17:5x CT; instrument 2026-09-19T22:51:57Z). §NEXT = the b3 packet (block 0 the MEASURE-2b paste if not yet running; block 1 the hivemind card, 9 files → `HIVE: LANDED <sha>`; block 2 the FE-115 paste); §HELD: KO-2 ACCEPT/KILL (`NAME2:` silence = accept); four words open (silence = the recs); the close Sunday on `R5B-2:`.
-->

# Operator brief (one act at a time)

## §NEXT — ONE act: the v77 b3 packet — block 0 the MEASURE-2b coder paste (if Claude Code is not already running it) · block 1 the hivemind card (9 = 5 M + 4 A) → `HIVE: LANDED <sha>` · block 2 the FE-115 paste (a FRESH Cowork conversation) → `FE115: RETURNED <path> <bytes>` (Sat 2026-09-19 ~17:5x CT)
Run `_scratch/v77/2026-09-19_v77-b3_packet.md` blocks 0 → 2. Block 0 first if the core tree is still untouched (at 17:4x CT it was): MEASURE-2b is the critical Java item; `M2B: RETURNED <path> <bytes>` when its last line prints. Block 2 runs beside it (another path-domain). KO-2 returned KILL — accept it (the b3 audit §2; PALOKI stays; NAMING-B is the fallback). Nothing on the rig tonight. Tomorrow: `R5B-2: <the digest line>` here → the close → the bench card's Core card (`BENCH-CORE: sun`). **Words open (silence = the rec):** `NAME2: accept|counsel|third` (accept) · `BENCH-CORE: sun|mon|tue` (sun) · `REARM: reh1|thu|drop` (reh1) · `SHAKE: mon|thu` (mon) · `ERIK:`.

## §HELD-BY-THE-HUB (the wait-state ledger; edited every beat)
**Lanes (the cap: TWO + your hands; hardware exclusive; Java in series):**
| Lane | State (v77 b1) | Next |
|---|---|---|
| **The rig (EXCLUSIVE): the sitting DONE Sat 09-19 (H8-a 28 min · R-5B 76 min; zero STOPs)** | both records RETURNED and READ at the bytes (the b2 audit): K1–K4 ✓ (re-asserted); the stop-proof clean ×2 on `6bd8508`; the restore MET (`[PASS] 6/6`, PAN 0x774c, formed 0, re-seen 6/adopted 0); the first `fleet:` datum; P-B2/P-B3/P-B6 NOT TESTED (D-3); the S31 cut out under the vacuum (D-2, the vacuum comes off it); the Pi pulled to `fa01cad` at B6-1; the manual run `7/9 · FAIL usb-reenumeration` = the ssh PATH (pre-registered: Sunday's timer line passes it) | Sunday `R5B-2:` → the close (FE-113 VERIFIED; O-2 CLOSED; OR-FAILCHAN re-scoped, IR-33; the fleet floor; `S31-LABEL` banked; D-1..D-7 → rehearsal 1's packet) → step 0b's card (`BENCH-CORE:`) → `REARM` in rehearsal 1 (wk 09-28) |
| **The Java slot — MEASURE-2b HANDED 14:2x CT; NOT STARTED at 17:4x CT (the tree untouched) → block 0 of the b3 packet** | the charter through the gate (`context/instructions/2026-09-19_coder-lane_MEASURE-2b_…coding-instruction.md`): ONE `RealCoreFixture`; the six reads at 10k/100k/500k on a real temp-dir SQLite; the env knob; row 0 (ENERGY-READ-b); the rig-blindness § | `M2B: RETURNED` → the intake two-layer (the XMLs summed; the numbers re-derived; the fixture read) → the core card → CI → LINK-READ chartered against the fixture at HEAD → Thu 09-24 the first metering adoption |
| **The web-ui slot — FE-115 DISPATCH-READY on the b3 packet (beside the Java slot; D4)** | FE-114 LANDED `3d40b5f` (the v1.1.4 mirror); FE-115 chartered through the gate (`context/instructions/2026-09-19_frontend-lane_FE-115_…charter.md`): the v1.1.5 pins (five sites); `conditions[].definition` typed/validated/rendered by ONE pure `definitionSentence` over the emitter's seven types; the H8-a bodies as REAL-PAYLOAD fixtures (P4 = the live-wire bar, run before any edit); the mock/DevPanel to v1.1.5; the dead keys behind consumer greps; the lint's reach | `FE115: RETURNED` → the intake → the core card (web-ui only) → `frontend.yml` on the push |
| **The research lanes — KO-2 RETURNED and ACCEPT (KILL); the rest ACCEPT** | KO-2 (21,196 B): the canaries re-run; S5 (the `.com`) + S2-EU (the sound in cl. 9) → KILL; the hub re-fetched ★1/★4; the ASR rig's controls did not reproduce (IR-35); PALOKI stays the candidate of record; NAMING-B re-armed as the fallback (dispatched only on a NOT CLEAR opinion, or `NAMING-B: go`) | `NAME2:` (silence = accept) · the box arrived Sat, closed until Sunday's close; `SHAKE: mon` · `FOP: apply` (Mon) |
| **The bench lane — the Pi at `fa01cad` (R-5B B6-1; step 0 DONE); step 0b OPEN** | the new engine ran once by hand (`fleet: 6/6 · re-seen 0`; `selftest 43/0`); the bench card's Core is pre-CG-123 (`deviceId=None`) — the metering scenario reads `power_w` from it, so it goes to `d1c2cbc`'s .deb BEFORE the adoption (step 0b; the b2 audit §4); a docs row open (the runner README) | `BENCH-CORE: sun` → the H8-a-shaped card Sunday → Mon + Tue nightlies on the full stack → Tue THE THURSDAY PACKET → Thu 09-24 in THE THURSDAY ORDER (the b7 audit §0 + step 0b) |
| **The closure counter 7/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508` · `e56f555` · `3d40b5f` · `3af6213`) | #252's `Run the bus soak` ✓ at the instrument (the sample `bus-soak-252` stored, 28,916 B; its XML unread from here — the download needs a token); run #251 was not a readable sample; the next push is the next; a red `bus-soak` → `SAMPLE: <sha>` | passive |
| **Company, dated** | Activate SKIP by default (a WRITTEN yes to both questions by Fri Oct 9; hours on top); the email v3 SENT 09-17; HotMobile SKIP; the knockout charter (D-v74-1; a free beat; not waiting on Erik); EXT-REVIEW-1: the pilot in November (`PILOT: after`), the buyer outreach from Fri 09-25 behind the register-checked script, THE SPOKEN CARD + its embargo side (the strategy pass Thu 09-24), C-13 data-next, C-14 no name outward; Erik — Nick's dated line 09-17, the Fri 09-25 rule; `PROTECT: done` (the ruleset on `main`: deletions restricted, force pushes blocked); the letters filed verbatim under `context/research/`; AMD-100 PROPOSED; no name in any tracked file | `ERIK: <his line>` · AMD-100's docs card |
| **The plan of record + the v75 DR (CLOSED)** | `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` (§14 the calendar; §15 EXT-REVIEW-1; **§16 the eight additions — the watts go/no-go Sun 10-11 20:00 CT; VERIFY-72H's gates; the frozen invariants**): the run Oct 30 – Nov 2; row 2 DONE; the critical path unmoved. The v75 DR CLOSED; **the v76 DR** `context/planning/2026-09-18_v76_decision-record.md`: D-v76-1..10 RULED; **the v77 DR** `context/planning/2026-09-19_v77_decision-record.md`: D-v77-1..4 OPEN (silence = the recs) | the plan §19 |
| The landed lanes (W-SKILLS-9 `180375f` · HERO-1d `6bd8508` · P-1 · R-4c · AMD-101 `7221ddc` · the rest in the v75 DR) | ACCEPT · landed | W-SKILLS-10 folds THE PREMISE GATE + its two extensions (an idle beat); TR-1b after the run; IR-27 (October) |

**v77 (OPENED Sat 09-19 ~08:0x CT):** the deliverable, re-named at b2 — the two intakes CLOSED two-layer on `R5B-2:` AND MEASURE-2b LANDED green (step 0 is done). b1 the boot at the instrument (12/12; the spine rotated) · b2 the sitting read at the bytes (K1–K4 re-asserted; one claim refuted; step 0 banked, step 0b found), MEASURE-2b dispatched, KO-2 chartered · b3 KO-2 ACCEPT (KILL; NAMING-B re-armed), FE-115 chartered, MEASURE-2b re-handed · b4 VERDICT-VOCAB-1 + PELTON-READY; the returns as they come · Sun the close, step 0b, LINK-READ, the strategy pass. **v76 (Thu 09-17 → Sat 09-19, CLOSED b8):** ENERGY-READ chartered → LANDED `d1c2cbc` GREEN in one day; EXT-REVIEW-1 adjudicated; BENCH-METER-1 + 1b ACCEPT and LANDED; the b7 rulings re-cut on Nick's critique, the hub's error owned. **v75 (CLOSED b8, Thu 09-17):** eight lanes accepted; THE PREMISE GATE minted and extended.

**Open words (asked only when they gate an act):** `HIVE: LANDED <sha>` (the b3 card) · `M2B: RETURNED <path> <bytes>` · `FE115: RETURNED <path> <bytes>` · `R5B-2:` (Sunday, here) · `NAME2: accept|counsel|third` · `BENCH-CORE: sun|mon|tue` · `REARM: reh1|thu|drop` · `SHAKE: mon|thu` · `NAMING-B: go` (optional) · `ERIK: <his line>` · `NAME:` (silence = ii) · `SAMPLE: <sha>` · `NAV: manual` · `REPIN: <sha>` · `REVERT <D-n|GEN4-READ>`.

**Given (banked; no act):** `HIVE: LANDED 415873e` (b2) · `KO: RETURNED` (21,196) · `H8-a: RETURNED` (42,844) · `R5B: RETURNED` (50,494) · `PI: pulled fa01cad` (R-5B B6-1) · `NAV: manual` · `HIVE: LANDED 7287601` (b1) · `BENCH: BM1B fa01cad` + `HIVE: LANDED ad273c2` (both at porcelain, v77 b1) · `BM1B: RETURNED` (5,780) · `HIVE: LANDED 90e3348` · `BENCH: BM1 764e537` · `HIVE: LANDED 11a684b` · `BM1: RETURNED` (8,948) · `CORE: ER d1c2cbc` · `CI: d1c2cbc green` (CI #253 at the instrument) · `HIVE: LANDED cfecdd2` · `PROTECT: done` · `LETTERS: RETURNED` · `ER: RETURNED` (17,555) · `PILOT: after` · `EXPORT: letters` · `DEVICES: extended <edits>` · `GAPS: neither` · `HOTMOBILE: skip` · `FOP: apply` · `STRATEGY: after-R5B` (narrowed) · `CASCADE: engine` · `AMD100: ratify` · `REPIN: none` · `GEN4-READ: rehearsal-2` (ruled) · `ARTIFACTS: 62 deleted, 9,195 MB` · `CORE: CI2 3af6213` · `CI: 3af6213 green` (#252 at the instrument) · `HIVE: LANDED 08b8474` · `ACTIVATE: sent` (09-17) · `H8: Sat 09-19 10:00` · `DUR: measure` and `HASH1: a` (fixed at `be4788a`) · Activate skip-by-default (the Oct 9 rule) · `FRAME: a` · `PLUG: two` · THE PREMISE GATE (code, outward text, spoken) · `SCOPE: lane` + the three fences · THE ADOPTION FENCE · THE MEASUREMENT RECORD · `PROTECT: core + docs` · `PROTECT: no-force` · `SAMPLES: passive` · `SEARCH-NAME: PALOKI` FINAL · the earlier words in the v74 and v75 DRs.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68:** at `ea96351`. **v69–v70:** at `3209101`. **v71:** at `256c26b`. **v72–v73:** at `98e2f39`. **v74:** at `9795c2c`. **v75 (Mon 09-14 → Thu 09-17):** at `cc17ce7` · `a8c987d` · `553bf5b` · `0eef472` · `13e8d64` · `d02557d` · `08b8474` (core `e56f555` · `3d40b5f` · `be4788a` · `3af6213`). **v76 (Thu 09-17 → Sat 09-19, CLOSED b8):** at `5bbbd14` · `c0158a1` · `7605dfc` · `6384e64` · `cfecdd2` · `11a684b` · `90e3348` · `ad273c2` (core `d1c2cbc`; bench `764e537` · `fa01cad`) — b1 the boot · b2 Nick's brief · b3 EXT-REVIEW-1 · b4 ENERGY-READ chartered · b5 ENERGY-READ ACCEPT, the core card · b6 CI green, Nick's re-cut adopted, BENCH-METER-1 chartered · b7 BENCH-METER-1 ACCEPT, 1b chartered, the re-close (re-cut b7c) · b8 BENCH-METER-1b ACCEPT, the weekend dispatch. **v77 (Sat 09-19 →):** b1 the boot at the instrument; the spine rotated; Check 12 reconciled → `7287601` · b2 the sitting read, MEASURE-2b dispatched, KO-2 chartered, step 0b found → `415873e` · b3 KO-2 ACCEPT, FE-115 chartered, the DR opened.

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · adopt a water-leak, smoke or vibration device before IR-15 lands · adopt a metering plug before ENERGY-READ is green · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion · pair the Gen4 plugs before ENERGY-READ · send or SAY a sentence the register does not hold (a post, a pitch, a recruitment page) · re-pin Saturday on `be4788a`/`3af6213` without `REPIN:` · delete run #56's artifacts.
