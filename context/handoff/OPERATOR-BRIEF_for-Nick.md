<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v75 beat 4 (Mon 2026-09-14 ~21:3x CT; instrument 2026-09-15T02:31:35Z). §NEXT = the b4 packet (the hivemind card → the MEASURE-1 paste → the shopping card); §HELD: the plan of record re-cut — `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md`.
-->

# Operator brief (one act at a time)

## §NEXT — ONE act: the v75 beat-4 packet — block 1 the hivemind card (14 files) → `HIVE: LANDED <sha>`; block 2 the MEASURE-1 paste (host-side Claude Code, the Java slot) → `MEASURE1: RETURNED <path> <bytes>`; block 3 the shopping card (one meter, no plugs) → `METER: ordered` (Mon 2026-09-14 ~21:3x CT)
Run `_scratch/v75/2026-09-15_v75-b4_operator-packet.md` blocks 1 → 2 → 3 in order (block 2 can run while you do block 3; the housekeeping `rmdir` line first). What b4 did: both CI verdicts read at the instrument (6/20); the freeze doc stamped v1.1.5 with its false mirror line corrected; THE PREMISE GATE minted and applied; MEASURE-1 cut through it; the plug dossier ACCEPT (your Gen4 units qualify); HASH-1 ranked a DEFECT with its surface widened to eight records; the six-week plan is the plan of record. **Your words, in order:** `HIVE:` · `MEASURE1:` · `HASH1: a|b` (the H10 is D-v75-5; silence = (a)) · `METER: ordered` · `Activate: hold` Tue → the email paste · silence on `NAME:` = ii · `H8: Sat 09-19 10:00` GIVEN (the navigator opens by itself at 09:50 CT; `NAV: manual` cancels). **Owed whenever:** `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` · `STRATEGY:` · `CASCADE:`.

## §HELD-BY-THE-HUB (the wait-state ledger; edited every beat)
**Lanes (the cap: TWO + your hands; hardware exclusive; Java in series):**
| Lane | State (v75 b4) | Next |
|---|---|---|
| **The rig (EXCLUSIVE): H8-a then R-5 Part B — one sitting, Sat 09-19 10:00 CT** | both packets through THE PRIOR-LEDGER GATE, UNTOUCHED since b2 (H8-a pinned to `6bd8508`'s run #56; R-5B's frame bound per class, `verified=`/`degraded=` beside the S31's count); the navigator send SCHEDULED Sat 09:50 CT (a fresh session with ClaudeFolder; it says one line and waits for `go`); no re-pin without `REPIN: <sha>`; `S31-LABEL:` and O-2's second reading ride the sitting; Sunday's nightly line is one paste | Saturday → `H8A:` / `R5B: RETURNED <path> <bytes>` → Sunday `R5B-2:` → the intakes (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C) → rehearsal 1 chartered |
| **The Java slot (MEASURE-1 → DUR-1 + HASH-1 → ENERGY-READ → LINK-READ)** | 114c LANDED `e56f555`, CI #249 green; the freeze doc v1.1.5; IR-1/2/3 RETIRED; **MEASURE-1 DISPATCH-READY** (`context/instructions/2026-09-15_coder-lane_MEASURE-1_…`): DUR-0 the run-after-expiry test (predicted RED), HASH-0 the eight-launch hash probe over a two-role selector AND a two-parameter command action — no production line; the return ≤5 KB | block 2 → `MEASURE1:` → `HASH1:` → the DUR-1 + HASH-1 charter (one session; the eight `Set.copyOf`/`Map.copyOf` records get ordered views; the run initiated on expiry) → ENERGY-READ (IR-15 first; the Gen4's predicted divisor as a P-row; THE MEASUREMENT RECORD minted) → LINK-READ |
| **The web-ui slot (FE-115 after HASH-1)** | FE-114 LANDED `3d40b5f`, CI #250 green (the mirror v1.1.4-2026-09-13); IR-12/13/14 RETIRED; the freeze doc's mirror line corrected (HERO-1b/1c/1d were v1.1.3; FE-114 is the v1.1.4 mirror) | FE-115 chartered after HASH-1 is green: `conditions[].definition` rendered (v1.1.5 mirror), the `v114` mock touch, the dead-key retirements (the b3 audit §3 table), the H8 real-wire read |
| **The bench lane (free)** | R-5 Part A + ii `f3631cb` landed; owed: `field_within`, `metering-known-load.yaml` with `requires: [operator]` + the reference-meter operator blocks, the `link-quality.yaml` skeleton — dry-run and selftests only | after MEASURE-1's intake, on a free slot |
| **The plugs — CLOSED on the dossier (`context/research/2026-09-15_PLUG-DOSSIER_return.md`)** | your two Shelly Plug US Gen4 (S4PL-00116US) qualify on both criteria (Zigbee router; 0x0702 + 0x0B04 as EP1 Input on Shelly's own matrix; z2m plain `m.electricityMeter()`, no quirk) — the second plug purchase DROPPED (the plan §7); buy the reference meter only (block 3); acPowerDivisor predicted 10 — adjudicated at ENERGY-READ's first read; **THE ADOPTION FENCE stands** | `METER: ordered` → update the Gen4 firmware before ENERGY-READ → `HARNESS-PLUG:` at the sitting's intake |
| **The closure counter 6/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508` · `e56f555` · `3d40b5f`, all green, `SAMPLES: passive`) | every push from `a458a64` counts; a red `bus-soak` → `SAMPLE: <sha>` | passive |
| **Company, dated** | `Activate: hold` Tue (D-v74-2 — the `/apply` read + ONE email paste, b5); the knockout charter for candidate ii (D-v74-1, silence = ii; b5 on a free slot; token-parameterized; no name in any tracked file); Silabs Thu 09-17; Erik — PALOKI search STARTED 09-13 (no line since; no public name before the opinion); `PROTECT: no-force` — your six clicks; AMD-100 PROPOSED; the counsel wave rides Erik's opinion | `Activate: hold` Tue · `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` |
| **The plan of record + the v75 DR** | `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` (b4): the run Oct 30 – Nov 2; the seven-row critical path with an instrument beside each (two EXIST, two PARTIAL, three MISSING); your hours ≈ four sittings + the run's start/stop + one word per row; DROPPED: TR-1b at the card, the second plug, a FRAME-COUNT micro-WU. The DR: D-v75-1 `FRAME: a` · D-v75-4 `DUR: measure` RULED · D-v75-5 `HASH1:` (rec (a)) · D-v75-2 `STRATEGY:` (rec after-R5B) · D-v75-3 `CASCADE:` (rec engine, Nov) | `HASH1:` now; `STRATEGY:`/`CASCADE:` whenever (silence = the recs) |
| W-SKILLS-9 `180375f` · HERO-1d `6bd8508` · P-1 `1201368` · R-4c (C-003 MINTED) · AMD-101 `7221ddc` · HONESTY-1 · FE-NULL-1 · TR-1 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · landed | W-SKILLS-10 folds THE PREMISE GATE (an idle beat); TR-1b a desk WU after the run |

**Open words (asked only when they gate an act):** `HIVE: LANDED <sha>` · `MEASURE1: RETURNED <path> <bytes>` · `HASH1: a|b` (silence = (a)) · `METER: ordered` · `Activate: apply|hold|skip` (Tue; rec hold) · `NAME: i|ii|iii` (silence = ii) · `STRATEGY: now|after-R5B|fold` (silence = after-R5B) · `CASCADE: engine|bridge|drop` (silence = engine, Nov) · `AMD100: ratify` · `NAV: manual` (cancels the scheduled navigator) · `REVERT <D-n>` only to refuse a standing ruling.

**Given (banked; no act):** `H8: Sat 09-19 10:00` · `DUR: measure` (the instrument named first) · `FRAME: a` + the two sharpenings (D-v75-1) · `PLUG: two` + the two criteria + THE ADOPTION FENCE (closed on the dossier) · THE PREMISE GATE (pm-lessons, OPEN) · `SCOPE: lane` + the three fences · the copy dialect US · THE MEASUREMENT RECORD (minted once at ENERGY-READ) · the frame rate counted at the ingestion unit · `requires: [operator]` for reference-meter scenarios · `HERO1: Q1–Q7 a` · `EXPLAIN: three` · `PROTECT: core + docs` · `PROTECT: no-force` · `SAMPLES: passive` · `SEARCH-NAME: PALOKI` FINAL · `TR1-B2 = DRIVER` · `EU-DEFER` (provisional) · D4's seven recs · THE OPERATOR-LOAD LAW · D1–D7 · D-v74-1..4.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68 (Sun 09-06 → Thu 09-10):** in this file's history at `ea96351`. **v69–v70 (Thu 09-10 → Sat 09-12):** at `3209101`. **v71 (Sat 09-12):** at `256c26b`. **v72–v73 (Sat 09-12 → Sun 09-13 ~18:1x CT):** at `98e2f39`. **v74 (Sun 09-13 ~18:5x → Mon 09-14 ~13:0x CT):** at `9795c2c` (W-SKILLS-9 `180375f`; the H8-a re-cut; HERO-1d `6bd8508` green; the ambition map ACCEPT; IR-15..IR-19; `PLUG: two`; closed at b8). **v75 (Mon 2026-09-14 ~13:1x CT →):** b1 the boot (≈45.3 KB; 12/12; 28/28) · your dispatch filed verbatim · IR-1 measured on your host (2/40/100) · H8-a re-pinned to run #56 · R-5B cut through the gate (28 greps) · IR-20 · D-v75-1 → `cc17ce7`; b2 `FRAME: a` banked · 114c and FE-114 authored on grounding agents · R-5B's EXPECTED/§H sharpened · IR-21/22 · D-v75-2..4 → `a8c987d`; b3 both returned and ACCEPT two-layer (the patches applied to the hub's clone; verify green) · the coder-handoff entry · IR-23 → `553bf5b` → core `e56f555` + `3d40b5f`; b4 CI green ×2 at the instrument (6/20) · the freeze doc v1.1.5 + the mirror line corrected · THE PREMISE GATE minted and applied · MEASURE-1 cut · the dossier ACCEPT (no plugs) · HASH-1 a defect over eight records (D-v75-5) · the six-week plan · six rows retired · the navigator send scheduled → the b4 packet.

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · adopt a water-leak, smoke or vibration device before IR-15 lands · adopt a metering plug before ENERGY-READ is green · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion (the .com is yours to buy at your own risk; the plan buys it the day the opinion is clean) · pair the Gen4 plugs to the coordinator before ENERGY-READ.
