<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v70 beat 2 (Fri 2026-09-11 ~18:0x CT; instrument 2026-09-11T23:02:50Z). Beat 1 landed `89de5b3`. The `1e26912` red is read: FIX-2 opens with the instrument. §NEXT = one card. The 18:45 message carries the H8-a paste by itself.
-->

# Operator brief (one act at a time)

## §NEXT — one card (beat 2); the H8-a paste reaches you at 18:45 by itself
**Landed:** beat 1 `89de5b3`. **Read:** the `1e26912` red at your artifact — no anomaly token anywhere; the motion edge reached the store and nothing moved for ten seconds; FIX-2 therefore opens with the instrument (its instruction is b4; your word `FIX2:` is asked when it is on disk). Nothing to fetch. **At 18:45 CT** one message hands you the desktop guard, the rig rules and the navigator paste as one block; the rig is exclusive; the hub asks nothing until `H8A:`.
**Card — hivemind (Git Bash):** lands beat 2 (the red-read audit + the spine; 4 M + 1 A):
```bash
cd /c/Users/Nick/Desktop/Code/ClaudeFolder/nexsys-hivemind && M=$(git --no-optional-locks status --porcelain | grep -c '^ M'); A=$(git --no-optional-locks status --porcelain | grep -c '^??'); echo "M=$M A=$A"; if [ "$M" = 4 ] && [ "$A" = 1 ] && ! grep -qi 'co-authored\|claude-session' ../_scratch/2026-09-11_hivemind_v70-beat2_commit-msg.txt; then git add -A && [ $(git --no-optional-locks diff --cached --name-status | wc -l) -eq 5 ] && git commit -q -F ../_scratch/2026-09-11_hivemind_v70-beat2_commit-msg.txt && git log -1 --format='%h %s' | cut -c1-80 && git --no-optional-locks status --porcelain | wc -l && git push 2>&1 | tail -1; else echo 'STOP: census or message check failed'; fi
```
**Done looks like:** `M=4 A=1`, the `hivemind: v70 beat 2` subject, `0`, `89de5b3..<sha>`. **One line back:** `LANDED v70b2 <sha>` — or after the rig; the card keeps. `PROTECT: done` whenever it fits.

## §HELD-BY-THE-HUB (edited every beat; you read it, you never remember it)
**Lanes (the cap: TWO + your hands; hardware exclusive):**
| Lane | State (v70 beat 2, 23:02Z) | Next |
|---|---|---|
| **HONESTY-1 `94ae99d` · FE-NULL-1 `eabdbb1`** | LANDED Thu; ci + frontend GREEN; install-smoke per push (P4 MET); both instructions flipped to EXECUTED at this beat | closed; LASTREPORTED-1b rides R-4c (read `lastReported` on one never-reported entity after the `.deb`); O1 (the L1 headline per outcome) = the hero charter's first row |
| **HERO-1 — the hero design (web-ui)** | LANDED core `1e26912`; audited ACCEPT; the charter flipped to EXECUTED (v70 b1); ruled (a) ×7 | **HERO-1b** (SPEC §11 B1–B7, B1 first: the headline per outcome with its test; the Glossary row for "the hub" via the amendment path; B8 rows as v1.1.4 keys land) — charter at v70 b5, into the web-ui slot |
| **EXPLAIN-114a (Core Java; slot 1)** | AUTHORED, ISSUE-READY (30 KB; 13 M + 0 A expected) | **HELD behind FIX-2:** the `1e26912` red is the CI-613 class on the FIX-1b bytes; FIX-2 takes the Java slot first; EXPLAIN dispatches on FIX-2's green |
| **FIX-2 (Core Java; slot 1) — next Java WU** | the red READ (b2): no anomaly token, ten silent seconds after adoption, no dispatch — `context/audits/2026-09-11_CI-1e26912_red-read_HeroLoop-lifecycle_v70-b2.md` | instruction at b4, instrument-first: the IT's timeout diagnostic · TR-1b's position-census ITs · the bench `bus-soak` verb; the structural fix (checkpoint-driven LIVE delivery; AMD to Doc 01 §3.4) on the instrument's reading; your word `FIX2:` when the instruction is on disk |
| **H8-a (the rig; EXCLUSIVE)** | **TONIGHT 19:00 CT** — the 18:45 send is on v70 (`trig_01UFmAs97CvSBzVXB6apVWLd`, 23:45Z); v69's deleted; the paste file `_scratch/v70/2026-09-11_H8a_paste_18-45CT.md` | the artifact of record stays `f25291b` (the packet, unchanged); `H8A: RETURNED <path> <bytes>` when done; the hub audits at intake; FE-113b's capture folds into HERO-1b |
| **R-4c (the rig; MEASUREMENT-ONLY on the core tree) — THE DELIVERABLE** | packet: v70 beat 3 (from R-4b's packet + held card, the H8-a form, F-R4-1b's criteria) | **Sat 09-12** (`R4C: Sun` to move it); the .deb from the green install-smoke build (HONESTY-1's Java); the six devices re-adopted; C-003 (the sleepy SNZB-02P by the ZDO surface) is the exit; LASTREPORTED-1b read once after the .deb; `card-gradle:` read once at the card; DeliveryAnomaly tokens counted in the journal |
| **P-1 (the power-harness primitive as a bench verb)** | charter: v70 beat 6, after R-4c's packet and FIX-2 | the October 72-h rehearsals gate on it |
| **`PROTECT: no-force` (GitHub; your hands)** | your word (b2): core + docs only, the public repos; the six clicks handed in chat for both | `PROTECT: done` |
| **The skill mirror sync (Check 9)** | STALE: one file of 28 (`project-manager/SKILL.md`) | your usual sync, whenever; no lane waits on it |
| **Erik — the PALOKI search (wait-state)** | packet sent Thu ~12:00 CT | `ERIK: <his line>` when it comes; no public use before the opinion |
| TR-1 · Act 12 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · RETIRED · landed ×8 | TR-1b after `card-gradle:` (Saturday) |

**v70's blocks, in order (the hub's list, not yours; ≤8 beats, no new block after b6):** the boot (b1 — DONE, `89de5b3`) → the red read at the artifact (b2 — DONE; instrument-first) → **R-4c's packet (b3; the deliverable; Saturday)** → FIX-2's coding instruction (b4) → HERO-1b's charter (b5; web-ui) → P-1's charter (b6) → the H8-a intake when `H8A:` comes → TR-1b on `card-gradle:` → W-SKILLS-9 later. Two lanes at once once both are on disk: FIX-2 on Java, HERO-1b on web-ui. Owed, no lane waits: `PROTECT: done` · the mirror sync.

**Owed lines (none gates an act):** `card-gradle: <the first line of ./gradlew --version on the card | absent>` — Saturday, at the card (TR-1b's shape depends on it). *(`samples:` · `sample4:` · `act3:` · `nightly 09-05/09-06:` RETIRED by your `SAMPLES: passive`.)*

**Open words (asked only when they gate an act):**
- `H8: <day> <hh:mm>` — only to MOVE the default (Fri 09-11 19:00 CT); silence = it stands.
- `R4C: Sun` — only to move R-4c off Sat 09-12; silence = Saturday.
- `ERIK: <his line>` — when he replies (sight-read · date · surcharge); verbatim is fine, one line.
- `REVERT TR1-B2` · `REVERT EU-DEFER` — only if you refuse those rulings; silence = they stand. · `Activate: apply|hold` (09-15) — not yet due.

**Given (banked; no act):** `HERO1: Q1–Q7 a` · `EXPLAIN: three` · `PROTECT: core + docs` · your words on trailers and on hands (the hub never commits or pushes) · `DESIGN: start` · `PROTECT: no-force` · `SAMPLES: passive` · R-4c MEASUREMENT-ONLY · P-1 after R-4c · the H8-a fallback · `SEARCH-NAME: PALOKI` FINAL · the Erik packet SENT · `TR1-B2 = DRIVER` · `EU-DEFER` (provisional) · D4's seven recs STAND · Erik = U.S. only · `BEYOND-LETTERS: keep` · THE OPERATOR-LOAD LAW · D1–D7.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68 (Sun 09-06 → Thu 09-10):** compressed in this file's history at `ea96351` (FE-113 · TR-0 · the H8-a packet · F-R4-1b · BLOCK 6 · HERO-0 · RS-12-F · D1–D7 · HIVE-CLEAN-1 · W-SKILLS-8 · TR-1 · NAMING-B · the Erik packet · RS-13 PALOKI · the momentum map · HONESTY-1 `94ae99d` · Dependabot `3f3f5cc` · FE-NULL-1 `eabdbb1`). **v69 (Thu 09-10 evening → Fri 09-11 ~17:2x CT):** the H8-a send armed; the map re-cut; your words on trailers and hands taken; beat 1 `19dd310`; PROTECT scoped; HERO-1 chartered, returned, audited ACCEPT, landed `1e26912`; beats 2–3 `d0bcc1e`; `HERO1:` (a) ×7 and `EXPLAIN: three` banked; EXPLAIN-114a authored; beat 4 `ea96351`; the `1e26912` red named. **v70 (Fri 09-11 ~17:5x CT →):** booted 22:47Z; your Act 0 verified at the bytes; the H8-a send moved to v70 (v69's deleted); the deliverable named: R-4c's packet (beat 1, landed `89de5b3`) → the red read at run 34650129654: no anomaly token; FIX-2 instrument-first; the audit filed (beat 2).

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion (the .com is yours to buy at your own risk; the plan buys it the day the opinion is clean).
