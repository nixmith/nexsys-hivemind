<!--
file: context/handoff/OPERATOR-BRIEF_for-Nick.md
purpose: Nick's operator brief in THE OPERATOR-LOAD LAW's shape: §NEXT carries exactly ONE act — one paste or one command card, what "done" looks like, the one line he says back; §HELD-BY-THE-HUB carries everything else and is EDITED (by row) every beat by the hub; §DONE is one line per act. STABLE PATH since v66 beat 10 (2026-09-07): one file, edited in place, git history is the past — the boot prompt names this path. Nick reads; he never remembers.
audience: Nick (§NEXT, then nothing until he reports) · the hub (edits §HELD every beat; hands the next act on each report)
state-type: operator queue (the file on disk is the copy-source of record, never a chat card)
status: LIVE — edited at v75 beat 5 (Tue 2026-09-15 ~18:0x CT; instrument 2026-09-15T23:09:46Z). §NEXT = the b5 packet (the hivemind card → the DUR-1 + HASH-1 paste on `HASH1: a` → the DEVICE-SET paste → the Activate email); §HELD: MEASURE-1 ACCEPT (both defects measured); the shopping card withdrawn.
-->

# Operator brief (one act at a time)

## §NEXT — ONE act: the v75 beat-5 packet — block 1 the hivemind card (14 files) → `HIVE: LANDED <sha>`; block 2 the DUR-1 + HASH-1 paste (host-side Claude Code; paste it after `HASH1: a`, or say nothing and paste — silence = (a)) → `DUR1: RETURNED <path> <bytes>`; block 3 the DEVICE-SET paste (a fresh Cowork window) → `DEVSET: RETURNED <path> <bytes>`; block 4 the Activate email (one paste into your mail, from you) → `ACTIVATE: sent` (Tue 2026-09-15 ~18:0x CT)
Run `_scratch/v75/2026-09-15_v75-b5_operator-packet.md` blocks 1 → 4 in order; blocks 2 and 3 run in parallel once pasted. What b5 did: MEASURE-1 intaken two-layer (the 32 HASH-0 lines re-tallied; T1 read) — both defects are measured facts; DUR-1 + HASH-1 chartered through the premise gate (three cites corrected, a third expiry site added); the shopping card WITHDRAWN on your word and DEVICE-SET chartered (two vendors, a Class-0.5 meter, a known load, two priced tiers); the Activate `/apply` page read (closes Oct 30 — `hold` stands) and the email drafted; the spine rotated (v74 → the archive). **Your words, in order:** `HIVE:` · `HASH1: a|b` (silence = a) · `DUR1:` · `DEVSET:` → `DEVICES: core|extended|<edits>` · `ACTIVATE: sent` · silence on `NAME:` = ii. **Owed whenever:** `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` · `STRATEGY:` · `CASCADE:`.

## §HELD-BY-THE-HUB (the wait-state ledger; edited every beat)
**Lanes (the cap: TWO + your hands; hardware exclusive; Java in series):**
| Lane | State (v75 b5) | Next |
|---|---|---|
| **The rig (EXCLUSIVE): H8-a then R-5 Part B — one sitting, Sat 09-19 10:00 CT** | both packets UNTOUCHED since b2 (H8-a pinned to `6bd8508`'s run #56; R-5B's frame bound per class, `verified=`/`degraded=` beside the S31's count); the navigator send SCHEDULED Sat 09:50 CT (a fresh session with ClaudeFolder; it says one line and waits for `go`; `NAV: manual` cancels); no re-pin without `REPIN: <sha>`; `S31-LABEL:` and O-2's second reading ride the sitting; Sunday's nightly line is one paste | Saturday → `H8A:` / `R5B: RETURNED <path> <bytes>` → Sunday `R5B-2:` → the intakes (FE-113 VERIFIED; OR-FAILCHAN; O-2 CLOSED; the fleet floor; P-B2 → `C003:`; `HARNESS-PLUG:`; F-R4b-C) → rehearsal 1 chartered |
| **The Java slot (DUR-1 + HASH-1 → ENERGY-READ → LINK-READ)** | MEASURE-1 RETURNED 22:06Z, ACCEPT (b5): DUR-0 RED (`Expected size: 1 but was: 0`) — IR-22 a DEFECT; HASH-0 two hashes per source across eight launches (sets AND maps, flipping independently) — IR-23 a DEFECT on eight records; the two test files sit UNCOMMITTED on your desk as the fixes' starting tree; **DUR-1 + HASH-1 DISPATCH-READY** (`context/instructions/2026-09-15_coder-lane_DUR-1_and_HASH-1_…`): the registry's expired bucket + the evaluator's expired-payload arm; eight ordered views; `DefinitionHashes` untouched; T4 pins single-element hashes at the baseline | block 2 on `HASH1: a` → `DUR1:` → the intake → the landing card (16 files) → CI → the freeze doc's HASH NOTE re-cut to a fact → ENERGY-READ (IR-15 first; DEVICE-SET's two plugs as the fixture) → LINK-READ |
| **The web-ui slot (FE-115 after HASH-1)** | FE-114 LANDED `3d40b5f` (the v1.1.4 mirror); the freeze doc v1.1.5 with its mirror line corrected | FE-115 chartered after HASH-1 is green: `conditions[].definition` rendered, the `v114` mock touch, the dead-key retirements (the b3 audit §3), the H8 real-wire read |
| **The research lane: DEVICE-SET (read-only; block 3)** | your word at b5: a scientific setup — multiple models, a reference, repeatable as devices and plugins grow; the b4 shopping card WITHDRAWN (`METER: ordered` retired); the charter grades candidates purposes-first on one table (two vendors; a Class-0.5 meter; a stable resistive known load; refusals in writing; CORE and EXTENDED tiers, priced) | `DEVSET: RETURNED` → the intake → `DEVICES: core|extended|<edits>` → the purchase (your hands) → the Gen4 firmware update → ENERGY-READ's fixture; THE ADOPTION FENCE stands |
| **The bench lane (free)** | R-5 Part A + ii `f3631cb` landed; owed: `field_within`, `metering-known-load.yaml` with `requires: [operator]` + the reference-meter operator blocks (DEVICE-SET's §2 sketch feeds it), the `link-quality.yaml` skeleton — dry-run and selftests only | after DEVICE-SET's intake, on a free slot |
| **The closure counter 6/20** (`a458a64` · `fed99e8` · `93390f0` · `6bd8508` · `e56f555` · `3d40b5f`, all green, `SAMPLES: passive`) | every push from `a458a64` counts; a red `bus-soak` → `SAMPLE: <sha>` | passive |
| **Company, dated** | **Activate (D-v74-2): the `/apply` page read 09-15 — open; closes Oct 30 12 p.m. PT (the run's weekend); bachelor's + 4 years post-baccalaureate; "hardware-based technology innovation"; `apply@activate.org` — `hold` stands, the email is block 4;** the knockout charter for candidate ii (D-v74-1, silence = ii; b6 on a free slot; token-parameterized; no name in any tracked file); Silabs Thu 09-17; Erik — PALOKI search STARTED 09-13 (no line since; no public name before the opinion); `PROTECT: no-force` — your six clicks; AMD-100 PROPOSED; the counsel wave rides Erik's opinion | `ACTIVATE: sent` → their answer decides apply/skip (by Oct 23 if yes) · `PROTECT: done` · `ERIK: <his line>` · `AMD100: ratify` |
| **The plan of record + the v75 DR** | `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md` (b4; §10 addendum b5 — the benchmark fleet replaces the one-meter card): the run Oct 30 – Nov 2; the seven-row critical path with an instrument beside each; your hours ≈ four sittings + the run's start/stop + one word per row. The DR: D-v75-1 `FRAME: a` · D-v75-4 `DUR: measure` RULED, measured RED · D-v75-5 `HASH1:` (rec (a); HASH-0 measured both sources) · D-v75-2 `STRATEGY:` (rec after-R5B) · D-v75-3 `CASCADE:` (rec engine, Nov) | `HASH1:` now; `STRATEGY:`/`CASCADE:` whenever (silence = the recs) |
| W-SKILLS-9 `180375f` · HERO-1d `6bd8508` · P-1 `1201368` · R-4c (C-003 MINTED) · AMD-101 `7221ddc` · HONESTY-1 · FE-NULL-1 · TR-1 · HIVE-CLEAN-1 · W-SKILLS-8 · FE-113 · F-R4-1b · TR-0 · HERO-0 · RS-12-F · RS-13 | ACCEPT · landed | W-SKILLS-10 folds THE PREMISE GATE (an idle beat); TR-1b a desk WU after the run |

**Open words (asked only when they gate an act):** `HIVE: LANDED <sha>` · `HASH1: a|b` (silence = (a)) · `DUR1: RETURNED <path> <bytes>` · `DEVSET: RETURNED <path> <bytes>` → `DEVICES: core|extended|<edits>` · `ACTIVATE: sent` · `NAME: i|ii|iii` (silence = ii) · `STRATEGY: now|after-R5B|fold` (silence = after-R5B) · `CASCADE: engine|bridge|drop` (silence = engine, Nov) · `AMD100: ratify` · `NAV: manual` (cancels the scheduled navigator) · `REVERT <D-n>` only to refuse a standing ruling.

**Given (banked; no act):** `H8: Sat 09-19 10:00` · `DUR: measure` (measured RED — DUR-1 chartered) · `Activate: hold` (the email; the close Oct 30 filed) · `FRAME: a` + the two sharpenings (D-v75-1) · `PLUG: two` + the two criteria + THE ADOPTION FENCE (re-opened as DEVICE-SET on your word; the fence stands) · THE PREMISE GATE (pm-lessons, OPEN; applied to every charter since) · `SCOPE: lane` + the three fences · the copy dialect US · THE MEASUREMENT RECORD (minted once at ENERGY-READ) · the frame rate counted at the ingestion unit · `requires: [operator]` for reference-meter scenarios · `HERO1: Q1–Q7 a` · `EXPLAIN: three` · `PROTECT: core + docs` · `PROTECT: no-force` · `SAMPLES: passive` · `SEARCH-NAME: PALOKI` FINAL · `TR1-B2 = DRIVER` · `EU-DEFER` (provisional) · D4's seven recs · THE OPERATOR-LOAD LAW · D1–D7 · D-v74-1..4.

## §DONE — the ledger (one line per act; the past is this file's git history + the pm-handoff archive)
**v66–v68 (Sun 09-06 → Thu 09-10):** in this file's history at `ea96351`. **v69–v70 (Thu 09-10 → Sat 09-12):** at `3209101`. **v71 (Sat 09-12):** at `256c26b`. **v72–v73 (Sat 09-12 → Sun 09-13 ~18:1x CT):** at `98e2f39`. **v74 (Sun 09-13 ~18:5x → Mon 09-14 ~13:0x CT):** at `9795c2c` (W-SKILLS-9 `180375f`; the H8-a re-cut; HERO-1d `6bd8508` green; the ambition map ACCEPT; IR-15..IR-19; `PLUG: two`; closed at b8). **v75 (Mon 2026-09-14 ~13:1x CT →):** b1 the boot · your dispatch filed verbatim · IR-1 measured (2/40/100) · H8-a re-pinned · R-5B cut through the gate · IR-20 · D-v75-1 → `cc17ce7`; b2 `FRAME: a` banked · 114c and FE-114 authored · IR-21/22 · D-v75-2..4 → `a8c987d`; b3 both returned and ACCEPT two-layer · the coder-handoff entry · IR-23 → `553bf5b` → core `e56f555` + `3d40b5f`; b4 CI green ×2 at the instrument (6/20) · the freeze doc v1.1.5 + the mirror line corrected · THE PREMISE GATE minted · MEASURE-1 cut · the dossier ACCEPT · HASH-1 a defect over eight records (D-v75-5) · the six-week plan · six rows retired · the navigator send scheduled → `0eef472`; b5 MEASURE-1 RETURNED and ACCEPT (both defects measured) · DUR-1 + HASH-1 chartered through the gate · the shopping card withdrawn on your word → DEVICE-SET chartered · Activate `/apply` read (Oct 30), the email drafted · the spine rotated (v74 → the archive) → the b5 packet.

## §WHAT YOU DO NOT DO
Open a second lane on the same core path-domain · adopt a water-leak, smoke or vibration device before IR-15 lands · adopt a metering plug before ENERGY-READ is green · take a sample's green as a GRANT · touch s31 or the nightly · `--allow-downgrades` · anything else while at the rig · grade a name in chat · paste a transcript (save it under `_scratch/`, tell me the path) · file or use PALOKI publicly before the written opinion (the .com is yours to buy at your own risk; the plan buys it the day the opinion is clean) · pair the Gen4 plugs to the coordinator before ENERGY-READ · commit the MEASURE-1 tests alone (they land with DUR-1 + HASH-1).
