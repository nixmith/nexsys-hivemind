# Cowork Session Prompt — M3.7 Coding Instruction Development

You are the PM for NexSys/HomeSynapse Core. You operate in **Mode 3 (Director)** for Phase 3 implementation. This session has a single primary deliverable:

**Produce a complete, executable Claude Code prompt for M3.7 (End-to-End Integration Tests) — the final M3 milestone.**

The Coder will paste your output directly into a Claude Code session and execute against it. The prompt must be precise enough that no further PM input is required during Coder execution, comprehensive enough that the Coder's STOP-on-Mismatch gates catch any drift between brief and source, and bounded enough that the WU fits in a single 6–8 hour Coder block.

Invoke the `nexsys-project-manager` skill to load your full operating procedures.

---

## State at Session Start

**Project:** HomeSynapse Core — local-first, event-sourced smart home runtime. Java 21 on Amazon Corretto, targeting Raspberry Pi 4/5. Multi-module Gradle project with JPMS enforcement. SQLite WAL mode. Virtual threads.

**Phase:** Phase 3 (implementation). **M3.6 COMPLETE** at commit `76288af` (2026-05-22). **M3.7 NEXT** — End-to-end integration tests. **M4.0 planned after M3.7.**

**Repos to orient from:**
- `nexsys-hivemind` — PM/governance repo (selected folder). Contains all assessments, planning, instructions, handoff files.
- `homesynapse-core` — The Java codebase. MODULE_CONTEXT.md files in each module are authoritative for type inventories; `module-info.java` files are authoritative for JPMS module names; `gradle/libs.versions.toml` is authoritative for library versions and locked dependency choices.
- `homesynapse-core-docs` — Design documents, amendments, research archive.

**M3 milestone trail (for context):**
- M3.1 InProcessEventBus core (`a5d4b2a` final state)
- M3.2 REPLAY→TRANSITION→LIVE bus-side (`0bade6a`)
- M3.3 Backpressure, metrics, observability (`a5d4b2a`)
- M3.5a StateProjection vertical slice (`a2aff9c`)
- M3.5b StateProjection production persistence (`08d0136`)
- M3.4a integration-tests module + harness + first 2 IT tests (`5ae7912`)
- M3.4b Sustained-load + crash-recovery ITs (`adf04d2`)
- M3.6a–M3.6e.2 composition-root cohort (`17c40b6` → `76288af`, seven sub-WUs, seventeen Claude Code WUs total)
- **M3.7 NEXT** — exercise the full subsystem graph end-to-end: SQLite event store → InProcessEventBus → StateProjection → MaterializedStateQueryService → Javalin HTTP → JSON response → assertion.

**Why M3.7 matters:** No existing HomeSynapse test exercises the full stack with a real HTTP client. M3.4a/M3.4b cover persistence + bus + projection but stop short of HTTP. M3.6e.2 added HTTP endpoint handlers but tested them only through the package-private `EndpointContext` SPI with a `RecordingEndpointContext` stub. **M3.7 is the first milestone that proves the 16-step `HomeSynapseCore` bootstrap produces a system that an HTTP client can actually use.** Without this, ~6 months of subsystem implementation are unverified at the integration level.

**Research input — fully complete for M3.7:** Research 3 (Integration Testing Patterns for Event-Sourced HTTP Systems) was processed pre-PM-pipeline and all 10 RECs (REC-13 through REC-22) ACCEPTED. The patterns to apply:

| REC | Pattern | Disposition |
|---|---|---|
| REC-13 | `bus.isLive()` + `LiveModeAwaiter` | ACCEPT |
| REC-14 | Awaitility 4.3.1 dependency | ACCEPT |
| REC-15 | `DeploymentProfile.testing()` + `boundHttpPort()` for ephemeral port allocation | MODIFY + ACCEPT |
| REC-16 | `HomeSynapseE2eHarness` (the M3.7 capstone harness) | ACCEPT |
| REC-17 | `json-unit-assertj` for JSON response assertions | ACCEPT |
| REC-18 | `TestEvents` static-factory for test event creation | ACCEPT |
| REC-19 | (Crash-recovery at the HTTP level) | ACCEPT |
| REC-20 | `MinimalProjectionAdvancer` to close OR-M3-17 placeholder | ACCEPT — explicitly the M3.7-scoped advancer |
| REC-21 | (In-flight request behavior during shutdown) | ACCEPT |
| REC-22 | (Test data factory patterns) | ACCEPT |

Research 3 v1 is at `context/assessments/2026-05-22_Research_3_PM_Assessment.md` (note: Research 3 was processed prior to the v-numbered assessment-file convention; the format is older than Research 8 v2's template).

**Open risks blocking M3.7:**
- **OR-M3-17 (NO_OP_DERIVATION placeholder in `HomeSynapseCore` step 5):** must be resolved before M3.7. Per Research 8 v2's M3.7 Impact section, M3.7 ships with `MinimalProjectionAdvancer` (Research 3 REC-20); OR-M3-17 stays open through M3.7 and closes at M4.0 when `DispatchingProjectionAdvancer` (Research 8 REC-28) lands.
- **OR-M3-18 (NO_OP_ADVANCER placeholder in `HomeSynapseCore` step 5):** same resolution path as OR-M3-17 — `MinimalProjectionAdvancer` is the M3.7 closure mechanism.

**Optional pre-M3.7 work units (Nick scoping decisions you'll make in Phase 2 below):**
- HTTP integration test for the M3.6e.2 endpoints (real Jetty + real client) — currently deferred. Closes the M3.6e.2 in-session test-coverage gap. Could fold into M3.7's first scenarios or split as a tiny pre-M3.7 closeout WU (M3.6f).
- DLQ `oldestParkedAt` field plumbing in `SubscriberDlq` — closes the M3.6e.2 brief's response-shape deviation (D-2). Could fold into M3.7 or split.

---

## Phase 1: Mandatory Orientation (no deviation)

Per the `nexsys-project-manager` skill, **run the freshness preflight first**. It is at `nexsys-hivemind/project-manager/references/freshness-preflight.md`. Expected result given prior session work: **PASS on all 10 checks** (the 2026-05-22 session ended with PASS; no skill-file edits between then and now). If you see anything else — including unexpected STALE on Check 9 — escalate to Nick before any forward work.

After preflight, read these governance files in order:

1. **`context/handoff/pm-handoff.md`** — PM state, Next Tasks list, Open Risks. Note: Next Tasks #5 is "PM: Pre-M6 spikes" (not M3.7-blocking). M3.7 itself is Next Tasks #6.
2. **`context/status/PROJECT_SNAPSHOT.md`** — Current operational state, latest commit (`76288af`), milestone table, code state (~721+ production Java files, ~1,594+ tests), recent session log (3 entries on 2026-05-22).
3. **`project-knowledge/HomeSynapse_Knowledge_Primer.md`** — Architectural mental model. Pay particular attention to the "M3.6e.2 corrections" and "M3.6e.1 corrections" subsections in §Critical Gotchas — they describe the full composition-root state that M3.7 exercises.
4. **`context/planning/phase-3-milestone-backlog.md`** — Phase 3 backlog. M3.6e.2 marked DONE at `76288af`; M3.7 marked NEXT.
5. **`context/assessments/2026-05-22_Research_3_PM_Assessment.md`** — Research 3 (Integration Testing). Re-read REC-13 through REC-22; these are the patterns M3.7 must apply. Especially REC-15 (DeploymentProfile.testing() + boundHttpPort()), REC-16 (HomeSynapseE2eHarness scope), REC-20 (MinimalProjectionAdvancer).
6. **`context/handoff/coder-handoff.md`** — Coder state. The M3.6e.2 closeout section enumerates the seven Pre-M3.7 prerequisites and explicitly lists the two NO_OP placeholders as required-before-M3.7. Confirms M3.7 is the next-unit-by-ID.
7. **`project-manager/references/coding-instruction-format.md`** — The Coder-brief template you'll produce. Pay attention to the Files-to-Read section structure (must include MODULE_CONTEXT + `module-info.java` for every involved module + `libs.versions.toml` per Research 5 v2 lessons) and the STOP-on-Mismatch gate convention.
8. **`context/lessons/coder-lessons.md`** — Recent Coder lessons from M3.6 work, especially the Xlint:exports gateway pattern (M3.6e.1 lesson), Gradle/JPMS scope alignment (`requires transitive` → `api(...)`; plain `requires` → `implementation(...)`), and the M3.5a G4 visibility-promotion lesson.

### MODULE_CONTEXTs to read (for the Files-to-Read section of your coding instruction)

- `homesynapse-core/lifecycle/lifecycle/MODULE_CONTEXT.md` — 16-step `HomeSynapseCore.start()` bootstrap; existing `HomeSynapseCoreTest` patterns.
- `homesynapse-core/api/rest-api/MODULE_CONTEXT.md` — the 38-type post-M3.6e.2 inventory; `RestFilters` gateway methods (3 public); 5 endpoint handlers (all package-private); `EndpointContext` SPI + `RecordingEndpointContext` test helper.
- `homesynapse-core/core/state-store/MODULE_CONTEXT.md` — `MaterializedStateQueryService` static factory; `ReadinessSource` interface; `StateProjection.cursorPosition()`.
- `homesynapse-core/core/persistence/MODULE_CONTEXT.md` — `PersistenceFactory.start(...)` public gateway; `DeploymentProfile` 8 fields including `javalinMinThreads`/`javalinMaxThreads`.
- `homesynapse-core/core/event-bus/MODULE_CONTEXT.md` — `InProcessEventBus` public 7-arg constructor; `EventBusConfig.HOME_DEFAULT`; `SubscriberMode` 5-value FSM; `QueueSaturationHealthCheck` + `HealthSignal` + `HealthLevel` public chain.
- `homesynapse-core/testing/integration-tests/MODULE_CONTEXT.md` — M3.4a/M3.4b scaffolding pattern; `IntegrationTestHarness`; `PersistenceTestHarness` + `InProcessEventBusFactory` testFixtures; Pi-profile gating convention (`-PpiProfile=throttled`).
- `homesynapse-core/gradle/libs.versions.toml` — verbatim. Jackson 2.18.6 (LTD-08 + LTD-19). Javalin 6.7.0. SLF4J 2.0.17. JUnit 5.14.3. AssertJ 3.27.7. **Awaitility is NOT currently pinned** — REC-14 requires adding it.

---

## Phase 2: Four Scoping Decisions

Before producing the Claude Code prompt, make four decisions in this order. Each has a PM-recommended default; document deviations from the default with rationale.

### Decision 1: Placeholder strategy (OR-M3-17 + OR-M3-18)

**Question:** Does M3.7 ship with `MinimalProjectionAdvancer` (Research 3 REC-20) to close the two placeholders during M3.7, or with the real M4.0 `DispatchingProjectionAdvancer` (Research 8 REC-28)?

**PM recommendation (default): ship with `MinimalProjectionAdvancer`.** Research 8 v2 explicitly designed for this — its M3.7 Impact section states "M3.7 proceeds as planned with `MinimalProjectionAdvancer` (REC-20). OR-M3-17 stays open through M3.7; closes at M4.0 with REC-28." This preserves the established M3.7 → M4.0 sequencing and keeps M3.7 unblocked by M4 amendment-deliberation work.

**Decision artifact:** The M3.7 coding instruction's first piece of work is replacing `NO_OP_DERIVATION` and `NO_OP_ADVANCER` in `HomeSynapseCore` step 5 with `MinimalProjectionAdvancer`. Specify exact location (which constants in which file, which step in the bootstrap), exact replacement (constructor signature, derivation function), and verification (the existing `HomeSynapseCoreTest` assertions need updating to verify the new wiring rather than the placeholders).

### Decision 2: Pre-M3.7 sub-milestone bundling

**Question:** Does M3.7 absorb (a) the HTTP integration test for the M3.6e.2 endpoints (real Jetty + real client) and (b) the DLQ `oldestParkedAt` field plumbing — or are these split as a separate pre-M3.7 closeout WU (M3.6f)?

**PM recommendation (default): absorb both into M3.7's first test scenarios.** The HTTP integration test is *exactly* what M3.7 exists to do — the M3.6e.2 endpoint handlers are the obvious first thing M3.7's `HomeSynapseE2eHarness` should test. The DLQ field plumbing is a tiny delta (one record field + one writer-side capture) that fits naturally into the M3.7 scope. Splitting M3.6f would add session overhead without architectural benefit.

**Decision artifact:** If absorbed, the M3.7 coding instruction must explicitly include: (a) E2E tests against the three entity query endpoints + two admin endpoints (`/api/v1/entities`, `/api/v1/entities/{id}`, `/api/v1/entities/{id}/state`, `/internal/dlq`, `/internal/projection`) using real Jetty + Java's `HttpClient`, (b) `SubscriberDlq` modification to capture `parkedAt` timestamp + `DlqStatusEndpoint` response shape extension to include `oldestParkedAt`. If split, document M3.6f scope explicitly and produce a separate (smaller) Claude Code prompt for M3.6f first.

### Decision 3: M4-amendment-window blocking posture

**Question:** Does M3.7 wait for Nick to resolve the M4-amendment-deliberation window (Research 4 DQ-1/2/3/5 + Research 6 NQ-1..6 + Research 5 REC-56 deferred on REC-41) before M3.7 starts, or proceeds in parallel?

**PM recommendation (default): proceed in parallel — M3.7 does NOT block on M4 amendments.** M3.7 exercises the M3 stack only. None of the M4-bound amendments change anything M3.7 touches (no event-model changes that aren't already in `EventTypes.CORE_PRODUCTION_EVENT_CLASSES`; no rest-api or websocket-api changes; no lifecycle changes; no state-store changes beyond what M4.0's REC-28 will add). The current pm-handoff Next Tasks #6 framing ("M3.7 coding instruction... after the M4 amendment-deliberation window closes") was conservative; the actual technical coupling is zero. **Update pm-handoff Next Tasks #6 in this session** to reflect the new posture.

**Decision artifact:** A pm-handoff update (small Edit) removing the "after the M4 amendment-deliberation window closes" gate from Next Tasks #6. The remaining gates (OR-M3-17/18 placeholder resolution — handled by Decision 1; pre-M3.7 sub-milestone bundling — handled by Decision 2) are addressed by the M3.7 coding instruction itself.

### Decision 4: Claude Code workflow specifics

**Question:** What's the exact workflow shape for the Coder to execute against the produced prompt?

**PM recommendation (default — matches the M3.6a–M3.6e.2 established pattern):** Claude Code in `acceptEdits` mode, Opus 4.7 xhigh, deny `git commit`/`git push`/`./gradlew` per the standing CLAUDE.md policy. PM-generated coding instruction → Claude Code executes (writes files, runs tests internally if possible) → Nick reviews diff via `git status`/`git diff` → Nick runs `./gradlew check` externally → Nick commits if GREEN. The Coder does NOT run the build gate; that's Nick's job.

**Decision artifact:** The coding instruction's "What to Watch Out For" section reiterates the workflow constraints (don't run gradlew, don't commit, don't push), and the "Coder Handoff Deliverables" section lists exactly what the Coder produces (file list + completion report + STOP-gate results) and what Nick does next (build gate + commit + WUCP Phase 2 handoff).

---

## Phase 3: Produce the M3.7 Claude Code Coding Instruction

Once the four decisions are documented, produce the coding instruction at `context/instructions/M3.7_E2E_Integration_Tests.md` following `project-manager/references/coding-instruction-format.md` exactly. The instruction is the deliverable that Nick pastes into Claude Code.

### Mandatory sections (per coding-instruction-format.md)

1. **Header metadata** — file path, purpose, audience (Coder), update-cadence (single-use), state-type (instruction), status (ACTIVE), last-verified date + commit (`76288af`).

2. **Coding Task title and metadata** — Subsystem (testing/integration-tests + lifecycle + persistence + rest-api as needed), Design Doc references (PLAN-M3-CONSOLIDATED-02 §M3.7 + Research 3 PM Assessment + the assessments for Research 8 REC-20 reference), Phase (3-Implementation), Task Brief Reference (M3.7 — final M3 milestone).

3. **What This Implements** — concise paragraph describing the M3.7 scope: E2E test harness + first ~5–10 E2E scenarios + placeholder closure (MinimalProjectionAdvancer) + bundled M3.6f items (HTTP IT for endpoints + DLQ field plumbing per Decision 2). Reference M3.6's seventeen-CC-WU completion and the fact that M3.7 is the final M3 milestone.

4. **Files to Read Before Starting** — table format. Mandatory entries:
   - `testing/integration-tests/MODULE_CONTEXT.md` (M3.4a/M3.4b harness pattern)
   - `lifecycle/lifecycle/MODULE_CONTEXT.md` (16-step bootstrap; `HomeSynapseCoreTest` patterns)
   - `core/state-store/MODULE_CONTEXT.md` (MaterializedStateQueryService factory)
   - `api/rest-api/MODULE_CONTEXT.md` (38-type inventory; `RestFilters` gateway methods)
   - `core/event-bus/MODULE_CONTEXT.md` (`SubscriberMode` FSM; `EventBus.subscribers()`)
   - `core/persistence/MODULE_CONTEXT.md` (`DeploymentProfile`; `PersistenceFactory`)
   - **`lifecycle/lifecycle/src/main/java/module-info.java`** (verbatim — Research 5 v2 lesson)
   - **`testing/integration-tests/build.gradle.kts`** (Pi-profile gating pattern; testFixture deps)
   - **`gradle/libs.versions.toml`** (verbatim — Research 5 v2 lesson — Awaitility 4.3.1 + `json-unit-assertj` need adding per REC-14 + REC-17)
   - `context/assessments/2026-05-22_Research_3_PM_Assessment.md` (the 10 patterns this work applies)
   - `context/lessons/coder-lessons.md` (recent Xlint:exports + Gradle scope lessons)

5. **Scope — Files to Create or Modify** — exhaustive list. Expected entries (refine during Phase 2 reading):
   - `testing/integration-tests/src/test/java/com/homesynapse/it/HomeSynapseE2eHarness.java` (NEW — Research 3 REC-16) — wires the full composition root (`HomeSynapseCore`) against a real `@TempDir` SQLite file, binds Javalin to port 0 (ephemeral), exposes `boundHttpPort()` for tests, includes start/stop lifecycle.
   - `testing/integration-tests/src/test/java/com/homesynapse/it/LiveModeAwaiter.java` (NEW — Research 3 REC-13) — Awaitility-based `awaitLive(Duration)` helper.
   - `testing/integration-tests/src/test/java/com/homesynapse/it/TestEvents.java` (NEW — Research 3 REC-18) — static factory methods for common test events (`entity_registered`, `state_changed`, etc.).
   - `testing/integration-tests/src/test/java/com/homesynapse/it/EndpointE2eIT.java` (NEW) — E2E tests for the five M3.6e.2 endpoints with real Jetty + `java.net.http.HttpClient` + `json-unit-assertj` assertions. Bundled per Decision 2.
   - `testing/integration-tests/src/test/java/com/homesynapse/it/CrashRecoveryHttpIT.java` (NEW — Research 3 REC-19) — start core → publish events → abandon → restart → wait LIVE → hit endpoints → assert state survived.
   - `testing/integration-tests/src/test/java/com/homesynapse/it/RestApiIT.java` or similar (NEW) — additional scenarios per Research 3 REC-21 (in-flight request behavior during shutdown).
   - `lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java` (MODIFY) — replace `NO_OP_DERIVATION` + `NO_OP_ADVANCER` with `MinimalProjectionAdvancer` wiring (Decision 1). Update bootstrap step 5 documentation.
   - `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/HomeSynapseCoreTest.java` (MODIFY) — update existing assertions about NO_OP placeholders to assert real advancer wiring instead.
   - `state-store` or `lifecycle/lifecycle` (MODIFY — placement TBD per Research 8 v2 conventions) — `MinimalProjectionAdvancer` package-private class per Research 3 REC-20 + Research 8 v2 REC-20 acceptance.
   - `core/persistence/src/main/java/com/homesynapse/persistence/DeploymentProfile.java` (MODIFY — Research 3 REC-15) — add `DeploymentProfile.testing()` factory + `boundHttpPort()` accessor. **Coordinate with M3.6e.1 thread-pool fields** (`javalinMinThreads`/`javalinMaxThreads`) — testing profile should have tight bounds (1/2) for fast test startup.
   - `core/event-bus/src/main/java/com/homesynapse/event/bus/EventBus.java` or `InProcessEventBus.java` (MODIFY — Research 3 REC-13) — add `boolean isLive()` default method (if it doesn't already exist — verify against MODULE_CONTEXT).
   - `core/event-bus/src/main/java/com/homesynapse/event/bus/SubscriberDlq.java` (MODIFY — Decision 2 bundling) — capture `parkedAt: Instant` per parked entry.
   - `api/rest-api/src/main/java/com/homesynapse/api/rest/DlqStatusEndpoint.java` (MODIFY — Decision 2 bundling) — extend response shape to include `oldestParkedAt`.
   - `gradle/libs.versions.toml` (MODIFY) — add `awaitility = "4.3.1"` and `json-unit-assertj = "3.5.0"` (verify latest in the awaitility/json-unit project versions before committing).
   - `testing/integration-tests/build.gradle.kts` (MODIFY) — add testImplementation deps for awaitility and json-unit-assertj.

6. **STOP-on-Mismatch Gates** — exhaustive list. Coder MUST verify each before proceeding. Examples:
   - **G1:** `HomeSynapseCore.start()` is a 16-step bootstrap with `NO_OP_DERIVATION` at step 5 and `NO_OP_ADVANCER` at step 5 — verify exact line numbers and constant names; if either is missing, STOP.
   - **G2:** `RestFilters.installReadinessGate(Object javalinApp, ReadinessSource readinessSource)` exists with that exact signature — verify; if missing, STOP.
   - **G3:** `RestFilters.installEntityQueryEndpoints(Object javalinApp, StateQueryService queryService, LongSupplier viewPositionSupplier, Clock clock)` exists — verify; if missing, STOP.
   - **G4:** `RestFilters.installAdminEndpoints(Object javalinApp, Object bus, ReadinessSource readinessSource, StateQueryService queryService, LongSupplier viewPositionSupplier)` exists — verify; if missing, STOP.
   - **G5:** `DeploymentProfile` has the 8 fields documented in MODULE_CONTEXT (3 original + 3 M3.6a + 2 M3.6e.1) — verify; if different, STOP.
   - **G6:** `EventBus.subscribers() → List<SubscriberSnapshot>` exists; `SubscriberSnapshot` has 5 fields (`subscriberId`, `mode`, `checkpoint`, `dlqDepth`, `crashCount`) — verify; if different, STOP.
   - **G7:** `MaterializedStateQueryService` has a public static factory `materialized(StateStore, ReadinessSource, Clock, LongSupplier)` — verify; if signature differs, STOP.
   - **G8:** `gradle/libs.versions.toml` has `jackson = "2.18.6"`, `javalin = "6.7.0"`, `slf4j = "2.0.17"`, `junit = "5.14.3"`, `assertj = "3.27.7"` — verify these are the current pins before adding new dependencies; if differ, STOP and report.
   - Additional gates per any cross-module signature the coding instruction relies on.

7. **What to Watch Out For** — pitfalls section. Must include:
   - **Arch-rule reminder** (per coding-instruction-format §4c): `NO_DIRECT_TIME_ACCESS` applies to test code in non-whitelisted packages. Tests must inject `Clock`; use `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` for deterministic tests.
   - **Xlint:exports gateway pattern (DEC-M3-16, M3.6e.1 lesson):** any new public type in rest-api that references `io.javalin` types in its signature will fail with `-Xlint:exports`. Use the `RestFilters` gateway pattern (`Object`-typed parameters) for any new gateway methods.
   - **Gradle/JPMS scope alignment (M3.6e.1 lesson):** `requires transitive` in module-info ↔ `api(project(...))` in build.gradle.kts; plain `requires` ↔ `implementation(project(...))`.
   - **Port 7070 vs ephemeral port (Research 3 REC-15):** `HomeSynapseCore` currently hardcodes port 7070. M3.7 requires ephemeral ports for parallel test execution — `DeploymentProfile.testing()` must produce a profile that wires Javalin to `start(0)`, and `HomeSynapseE2eHarness` must expose the assigned port via `boundHttpPort()`.
   - **`DispatchingProjectionAdvancer` vs `MinimalProjectionAdvancer` (Research 8 vs Research 3):** M3.7 uses `MinimalProjectionAdvancer` (per Decision 1). DispatchingProjectionAdvancer is M4.0 scope. Do NOT preemptively implement the dispatching variant.
   - **Idempotency of test setup:** `HomeSynapseE2eHarness` must be safely re-startable across tests — each test gets a fresh `@TempDir` SQLite file, fresh port, fresh `HomeSynapseCore` instance.
   - **`@TempDir(cleanup = CleanupMode.NEVER)` on Windows (M3.4b lesson):** crash-recovery tests that abandon a `HomeSynapseCore` instance leave SQLite file handles held on Windows. Use `CleanupMode.NEVER` to avoid test-cleanup failures; OS reclaims temp dirs eventually.
   - **No `synchronized` (LTD-11 — wait, verify exact LTD number against the register):** use `ReentrantLock` if any test-side synchronization is needed.
   - **Awaitility default 30s timeout is too long for unit-level tests:** use explicit `.atMost(Duration.ofSeconds(5))` per Research 3 REC-13 recommendation.

8. **Dependencies and Integration Points** — describe the cross-module surface area. M3.7 touches: lifecycle (HomeSynapseCore modification), state-store (MinimalProjectionAdvancer placement), rest-api (no new types, but bundled DLQ change touches DlqStatusEndpoint), persistence (DeploymentProfile.testing()), event-bus (`isLive()` default + SubscriberDlq parkedAt), testing/integration-tests (new harness + 4–5 new IT files), gradle/libs.versions.toml (new deps), testing/integration-tests/build.gradle.kts (new deps).

9. **Coder Handoff Deliverables** — what the Coder produces:
   - File-by-file diff summary (list of files created/modified with one-line scope)
   - Completion report (per coding-instruction-format)
   - STOP-gate results (G1–G8+ verification outcomes)
   - Deferred build gate flag (Coder does NOT run `./gradlew check`; Nick does, per Decision 4)
   - Next-work-unit pointer (M3.7 = final M3 milestone → next is M4.0; coder-handoff should update Next Work Unit to M4.0 once M3.7 commits)

10. **Coder Pushback Welcome** — explicit invitation. If the Coder finds source-vs-brief mismatches during execution (the M3.6d-a pattern: 7 mismatches discovered, resulting in M3.6d-a + M3.6d-b split), STOP and report rather than guessing. Sub-divide if scope grows beyond ~18 file entries.

---

## Phase 4: Post-Instruction Hygiene

After the coding instruction is at `context/instructions/M3.7_E2E_Integration_Tests.md`:

1. Update `context/handoff/pm-handoff.md` Next Tasks #6 to remove the "after the M4 amendment-deliberation window closes" gate (Decision 3). Update "Current Task" to "M3.7 coding instruction issued — awaiting Coder execution."
2. Update `context/status/PROJECT_SNAPSHOT.md` Recent Session Log with a new entry summarizing: 4 scoping decisions made, M3.7 coding instruction produced, Decision 2 bundling outcome (M3.6f absorbed or split).
3. Append the four scoping decisions (with rationale) to `context/decisions/phase-3-cross-module-decisions.md` as new D-XX entries — these are cross-module decisions that future PM sessions need to reference.
4. Re-run the freshness preflight. Expect PASS on all 10 checks (no skill-file edits in this session).

---

## End-of-Session Deliverables

1. **Coding instruction file** — `context/instructions/M3.7_E2E_Integration_Tests.md`. Complete, executable, ~400–700 lines per the coding-instruction-format reference.
2. **pm-handoff.md updated** — Next Tasks, Current Task, Decisions Made This Session.
3. **PROJECT_SNAPSHOT.md updated** — Recent Session Log entry.
4. **Cross-module decisions register updated** — four new D-XX entries.
5. **Final freshness preflight: PASS.**

---

## What This Session Does NOT Do

- It does NOT execute the M3.7 coding work. That's Claude Code's job after Nick pastes the instruction.
- It does NOT modify any `homesynapse-core` source files. Only hivemind artifacts get touched.
- It does NOT resolve any Nick decisions on Research 4 DQs, Research 5 NQs (mostly resolved already in v2), or Research 6 NQs. M3.7 is decoupled from those per Decision 3.
- It does NOT process new research. Research 7 is in flight separately per the Research 7 brief at `context/instructions/Research_7_API_Design_Brief.md`.
- It does NOT touch the dual skill-location mirrors. No skill-file edits in this session.

If anything in this scope is unclear or seems wrong, escalate to Nick before proceeding. The cost of producing the wrong coding instruction is one wasted Claude Code session; the cost of asking is one Cowork message.
