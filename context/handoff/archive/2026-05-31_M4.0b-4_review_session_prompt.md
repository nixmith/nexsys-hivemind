<!--
file: context/handoff/2026-05-31_M4.0b-4_review_session_prompt.md
purpose: Session-start prompt for the FRESH Cowork conversation that runs WUCP Phase 2 review of the Claude Code M4.0b-4 output (typed StateChangedEvent payload + AttributeValue serializer + replay, AMD-52). Paste into the new window; attach the Coder's full response + `./gradlew` output + `git status`.
audience: PM (next Cowork session) ← Nick
state-type: handoff
status: CURRENT — 2026-05-31
-->

> **⚠ RE-TARGETED 2026-05-31 → this reviews M4.0b-4b (typed payload), which is GATED on M4.0b-4a (the `AttributeValue` → `com.homesynapse.value` relocation; AMD-52 §11 erratum).** If Nick hands you the **M4.0b-4a relocation** output instead, review it as a pure behavior-preserving refactor: confirm the new `com.homesynapse.value` module exists, the 10 types moved (package rename, contracts unchanged), the `requires com.homesynapse.value` edges are present and minimal, **the graph is acyclic and `event` does NOT require `device`** (the whole point), all `import com.homesynapse.device.{AttributeValue,…}` updated to `…value.*`, no semantics/`projectionVersion` change, build GREEN. For the **M4.0b-4b** review below, risk #3 (the event-model→device-model edge) is **resolved by 4a** — instead verify the typed field now sits on the legal `event → value` edge.

# Session Prompt: M4.0b-4b WUCP Phase 2 Review (typed payload + serializer — AMD-52)

**You are the PM in Mode 3 (Director), running the WUCP Phase 2 review** of the Claude Code implementation of **M4.0b-4** (the typed `StateChangedEvent` payload, the `AttributeValue` Jackson serializer, the schema-versioned replay, and the typed materialization — AMD-52). Nick will paste the Coder's full response plus `./gradlew` output and `git status`. Your job: verify the implementation against **AMD-52 by reading the changed source files, not the report**, adjudicate deviations (especially the expected module-edge STOP), confirm the build-gate posture, and execute WUCP Phase 2 closeout. **Load the `nexsys-project-manager` skill first.**

---

## 0. Session-start discipline (do first)

1. **Load the `nexsys-project-manager` skill** and **run the freshness preflight** (`project-manager/references/freshness-preflight.md`, 10 checks). Expect PASS — the AMD-52 ratification round was committed (Research 11 assessment + `AMD-52_*.md` RATIFIED + `Architecture_Invariants_v1.md` §22 + `00-navigation-index.md` watermark AMD-52 + Doc 01/03/04 currency + the hivemind state docs). If STALE, reconcile (retroactive WUCP Phase 2) before reviewing. If CONFLICTED, escalate to Nick.
2. **Source-verification rule (hard-won, still in force):** the in-sandbox `git`/`grep`/`wc` **truncates and line-ending-churns** files on this synced folder — it truncated `Architecture_Invariants_v1.md` to §19 during the AMD-52 ratification session (the Read tool showed §20/§21/§22 present). Verify every type/behavior claim with the **Read tool on the working tree** (or `git show <sha>:<path>` on Nick's machine) — **never trust in-sandbox `git`/`grep`, and never trust the Coder's completion report over source.**
3. **Ground truth at review time:** baseline HEAD `98f705b` (M4.0b-3/AMD-51). The M4.0b-4 code commit (or working tree) is what Nick provides. After M4.0b-4, **`projectionVersion` must be 4**. On-disk amendment watermark = **AMD-52**. AMD-52 is RATIFIED; this review verifies the *implementation* of it.

---

## 1. What is settled — the contract you verify against (do NOT re-litigate)

The implementation must satisfy **AMD-52** (RATIFIED 2026-05-31) — `homesynapse-core-docs/design/amendments/AMD-52_Typed_StateChangedEvent_Payload_Serializer_and_Replay.md`. Read §2 (change spec), §4 (AMD-52-INV-01..07), §5 (tests), §7 (verbatim source anchors), §9 (ratification — F1/F2 confirmed). The coding instruction is `context/coding-instructions/M4.0b-4_Typed_StateChangedEvent_Payload_Serializer.md` — its **Settled Decisions DP-1..DP-7**, **STOP-on-Mismatch Gates**, and the **PM note about the event-model→device-model module edge** are the spec the Coder built to.

**Frozen (do not reopen):** AMD-50 backfill/supersession/cursor-determinism (reused unchanged for 3→4); `DerivationContext` has no `Clock`; no `ServiceLoader`; D-01 (event-type-scoped — exhaustive switch over the sealed `AttributeValue` IS allowed); the `@JsonTypeInfo` ban + Jackson-isolation HARD RULE; the AMD-52 ratified fork calls (F1 bit-anchored identity / `chain_hash` inert; F2 Path B = defined `DegradedEvent`, no decode-path upcaster); `chain_hash` stays the AMD-37 zero-reservation; `AttributeValueUpcaster` SPI unchanged.

---

## 2. WUCP Phase 2 review checklist — verify each against SOURCE

Read every changed `.java` file against the prior source. Confirm:

- [ ] **The `AttributeValue` codec (AMD-52-INV-02) — highest-risk #1.** A custom `JsonSerializer<AttributeValue>`/`JsonDeserializer<AttributeValue>` pair (NOT `StdSerializer`) in `com.homesynapse.persistence`, registered via `PersistenceJacksonModule` (mirror `UlidSerializer`). The serialize dispatch is an **exhaustive `switch` over the 8 variants with NO `default`** (read the switch — a `default` arm or an `instanceof` chain that could silently miss a variant is a fail). Envelope is `{"t":<AttributeType.name()>,"v":…}`; `ArrayValue` recurses per element in order; fields in fixed order. **No `@JsonTypeInfo`; no Jackson annotation on `AttributeValue`/`StateChangedEvent`; no `com.fasterxml.jackson.*` import outside `core/persistence`** (confirm via the ArchUnit assertions the instruction asked for, and by reading the changed files' imports).
- [ ] **Float identity + IEEE edges (AMD-52-INV-03/-04) — highest-risk #2.** Finite doubles render **round-trippable** (`parseDouble(render(x))` recovers the bits) — NOT a frozen canonical text renderer; identity is over `Double.doubleToLongBits` after `−0.0`→`+0.0` + canonical-NaN. `NaN`/`+Inf`/`-Inf` → sentinel strings `"NaN"`/`"+Inf"`/`"-Inf"`; strict decoder rejects unknown tokens; `−0.0` serializes as `+0.0`. **`ALLOW_NON_NUMERIC_NUMBERS` stays disabled** (no bare `NaN`/`Infinity`). The canonicalization **must match AMD-51 §2.3** (so a comparator-"unchanged" value never serializes two ways). `chain_hash` untouched (still zero-default — read `SqliteEventStore` to confirm no write was added). `QuantityValue` arm has no sentinel branch (construction rejects non-finite).
- [ ] **`StateChangedEvent` type change + the module edge (AMD-52-INV-01 / §2.1) — highest-risk #3 (expected STOP).** `oldValue`/`newValue` are `AttributeValue`; **`oldValue` nullable** (first report), compact ctor non-null on `attributeKey`/`newValue`/`triggeredBy` only; no Jackson annotation. **Critical:** `StateChangedEvent` (event-model) now references `AttributeValue` (device-model). **Did the Coder hit the instructed STOP and report whether `core/event-model/module-info.java` needs `requires com.homesynapse.device`?** Read `core/event-model/module-info.java` yourself. If a new `requires` was added: confirm it is the minimal edge, that it does not create a cycle (device requires event — so event requiring device would be a **cycle**; if so, the resolution is NOT a new event→device edge — escalate, the fix may be relocating the field type or an interface seam), and adjudicate against AMD-52 §6's "no new module edge" claim. **This is the single most important thing to get right** — a naive `requires com.homesynapse.device` in event-model is a JPMS cycle (device→event already exists). If the Coder silently added it and it somehow compiled, scrutinize hard; if they escalated, that escalation likely needs Nick.
- [ ] **Typed emit (AMD-52-INV-01 / §2.6).** `ProductionDerivationRule.evaluate` emits `StateChangedEvent(key, priorTyped /*nullable*/, inboundTyped, eventId)` using the values it **already reconstructs** (no re-stringify), and the `EventDraft` is built with **`schemaVersion = 2`** (the 2nd positional arg — was `1`). The rule stays pure (no clock/I/O). `priorTyped == null` for first report → `oldValue == null`.
- [ ] **Typed materialization (S2, AMD-52-INV-01).** `StateProjection.applyToState` `state_changed` branch writes `sc.newValue()` (typed), not `new StringValue(...)`; `applyBackfillAttribute` writes typed during the 3→4 backfill (**both paths** — `onEvent` and `processBatch`/backfill).
- [ ] **`shouldPublishDerived` typed coherence (§2.6 / test #9).** The guard now compares typed-to-typed (reuse the comparator or `Objects.equals` on typed), not string-to-string; it only *suppresses*, and must never suppress a genuine typed-changed emit. Confirm by reading it.
- [ ] **Typed checkpoint envelope (S2, AMD-52-INV-06).** `CheckpointSerializer` serializes `attributes` as a typed envelope per entry (reuses the codec); **the `Include.ALWAYS` null round-trip is preserved** (null attribute value + null `staleAfter` survive; rebuild via `HashMap.put`, never `Map.copyOf`). A deserialize failure still throws `IllegalStateException` → clear-and-replay. **No `view_checkpoints` row migration.** Watch for a `NON_NULL`-vs-`ALWAYS` regression (the most likely silent bug).
- [ ] **Path B legacy read (AMD-52-INV-05 / F2).** `EventPayloadCodec.decode` version-gates: `state_changed` + `schemaVersion == 1` → `DegradedEvent` (raw payload preserved, reason set); `≥ 2` → typed decode. **No `AttributeValueUpcaster` or schema resolver wired into the codec/decode path** (that would be the layering inversion F2 rejected — a fail). No event is mutated (confirm no `UPDATE`/`DELETE` on `events`; the instruction asked for an ArchUnit/test assertion).
- [ ] **`projectionVersion` 3→4 (AMD-52-INV-07).** Bumped at the composition root (the same site M4.0b-3 set to 3); the AMD-50 reconciliation/backfill triggers on the mismatch; the supersession test (AMD-50 §5 #5) still guards the typed rule (test #8). **No new backfill mechanism.**
- [ ] **AMD-51 coherence (§2.7 / test #10).** The comparator's symmetric reconstruction + the no-schema `StringValue` fallback still behave now that the materialized prior can be the schema variant (reconstruction idempotent on an already-typed value; comparator total). Confirm a test covers a natively-typed prior.
- [ ] **No `module-info`/Gradle/`libs.versions.toml` change** beyond the (adjudicated) event-model edge if any; **no new Jackson artifact**; `AttributeValueUpcaster` SPI unchanged; `DerivationContext` unchanged; `chain_hash`/AMD-50 untouched.
- [ ] **Tests (AMD-52 §5 #1–#11) present and meaningful;** they must genuinely fail without the implementation. **Tests inject `Clock`** — **no `Instant.now()`/`systemUTC()`/`nanoTime()`/`currentTimeMillis()`** in `com.homesynapse.persistence` OR `com.homesynapse.state` test code (`NO_DIRECT_TIME_ACCESS`, §4c — both non-whitelisted; this WU spans both).
- [ ] **Deviations adjudicated** (`[REVIEW]`/`[INFO]`/`[BLOCKING]`); MODULE_CONTEXTs (persistence, state-store, event-model) updated by the Coder.

---

## 3. Build gate + closeout

- **Build gate is deferred to Nick** (sandbox can't run Gradle reliably). Confirm the Coder flagged it in `coder-handoff.md` with the commands — `:core:event-model:check`, `:core:device-model:check`, `:core:state-store:check`, `:core:persistence:check`, `:lifecycle:lifecycle:check`, then full `./gradlew check` — against the M4.0b-4 working tree on HEAD `98f705b`. **Nick is providing the `./gradlew` output this session** — read it: if GREEN, the gate is satisfied; if RED, triage the failures against the checklist (the likely culprits are the module-edge/JPMS cycle, the ArchUnit Jackson-isolation/Rule-7 rules, or a `Clock` arch-rule trip in tests). Log the gate under Open Risks until GREEN is confirmed.
- **On a clean review + GREEN build:** execute **WUCP Phase 2** — update the persistence/state-store/event-model `MODULE_CONTEXT.md` (new codec types, typed `StateChangedEvent`, typed materialization; flip the state-store AMD-52 forward-pointer to implemented); PROJECT_SNAPSHOT (M4.0b-4 COMPLETE, `projectionVersion`=4, watermark AMD-52); pm-handoff (WU table + Open Risks); W22 (or the then-current week) plan; the design-track note (AMD-52 track → implemented/closed); the cross-agent-notes pointer; **fold the Doc 01/03/04 bodies** (the masthead currency notes said the fold happens at M4.0b-4 — now do it: §4.6 typed `StateChangedEvent`, §3.2/§4.1 typed emit/materialization, §3.6/§3.12 typed checkpoint); run the dual skill-location `diff -rq`; record the freshness preflight result.
- **Next forward after M4.0b-4:** the remaining M4 fronts — **Workstream B** (device-model breadth: AMD-44 Floor/EntityRole, Research 8 REC-23–30; not gated on AMD-52), **Workstream C** (integration-api freeze, Research 6 REC-41–51; P3-gated on NQ-1..6 + P4 Doc-05 currency), and the **timestamp-model unifier** (needs its own design beat). Confirm priority with Nick.

## 4. Pointers

- Amendment: `homesynapse-core-docs/design/amendments/AMD-52_Typed_StateChangedEvent_Payload_Serializer_and_Replay.md` (esp. §2, §4, §5, §7).
- Coding instruction: `context/coding-instructions/M4.0b-4_Typed_StateChangedEvent_Payload_Serializer.md` (STOP-gates + the module-edge PM note).
- Invariants: `homesynapse-core-docs/governance/Architecture_Invariants_v1.md` §22 (AMD-52-INV-01..07).
- Frozen backfill: `…/AMD-50_*.md` (§2, scenario 3.3 — the N→M pattern, now 3→4). Prior typed work: `…/AMD-51_*.md` (§2.3 canonicalization, §2.6 reconstruction, §2.7 staging).
- Source to re-verify: `AttributeValueSerializer.java` + `AttributeValueDeserializer.java` (new), `PersistenceJacksonModule.java`, `StateChangedEvent.java`, `ProductionDerivationRule.java`, `StateProjection.java` (`applyToState`/`applyBackfillAttribute`/`shouldPublishDerived`), `CheckpointSerializer.java`, `EventPayloadCodec.java`, `SqliteEventStore.java` (schema_version + chain_hash), the composition root (`HomeSynapseCore`/lifecycle, version bump), all four `module-info.java`, the three MODULE_CONTEXTs.

**Bottom line:** verify the Coder built the AMD-52 contract against source (not the report). The three highest-risk checks are (1) the **codec totality + no-`@JsonTypeInfo` + Jackson isolation**, (2) **float bit-identity / sentinels matching the AMD-51 comparator's canonicalization**, and (3) the **event-model→device-model module edge** — a naive `requires com.homesynapse.device` in event-model is a JPMS cycle, so if the Coder didn't STOP-and-report it, find out how `StateChangedEvent` compiles. Then the `ALWAYS`-vs-`NON_NULL` checkpoint round-trip and the Path-B `DegradedEvent` gate. Adjudicate, read Nick's `./gradlew` output, run WUCP Phase 2 (including the Doc 01/03/04 body fold).
