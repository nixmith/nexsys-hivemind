<!--
file: context/handoff/2026-06-11_M6.2-return_W25-charter_session_prompt.md
purpose: Next-PM-session brief — receive + Phase-2-review the M6.2 return, close the W24 window (W25 charter), run the post-M6 deliberation beat (M7/M8 runway + Research-14 scoping). Carries a PINNED STATE block so the session spends its window on review precision and forward planning, not state re-derivation.
audience: PM (next Cowork session), Nick
status: READY — authored 2026-06-11 at the M6.4 WUCP Phase 2 closeout (PM). M6.2 dispatched to the Coder same day.
-->

# Session Brief — M6.2 Return Processing → W25 Charter → Post-M6 Deliberation

PM session (nexsys-project-manager skill). Run the freshness preflight against the PINNED STATE below as the answer key — one verification glance per check; investigate only divergent lines. **Task order is flexible on one axis:** if the M6.2 return/gate output is in hand at session start, do Task 1 first; if M6.2 is still in flight, do Tasks 2–3 first and receive M6.2 when Nick pastes it.

## PINNED STATE (verified 2026-06-11 at the M6.4 closeout — trust, then spot-verify)

- **homesynapse-core:** HEAD **`62a81e6`** = M6.4 COMPLETE (on `7e0bce8` docs-only / `9035110` M6.1a / `b7bc65c` M6.1b). Full `./gradlew check` GREEN (**147 tasks**). `projectionVersion` **5**. Event pins now **55 / 24 / 36** (EventTypes constants / CORE_PRODUCTION_EVENT_CLASSES / EventCategoryMapping). **Step-0 at session start: reconcile `62a81e6` into any tracker line still reading "commit pending" and CLOSE the pm-handoff Open-Risks M6.4 gate entry** (its stated resolution condition). If M6.2 was committed before this session, the chain extends — read coder-handoff/cross-agent first.
- **M6 state:** M6.1 ✅ (`b7bc65c`+`9035110`) · M6.4 ✅ (`62a81e6`) · **M6.2 DISPATCHED 2026-06-11** (instruction `context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`; [R6] CONFIRMED; R1 done in M6.4; its G7 gate adapts to the post-M6.4 service) · **M6.3 triple-gated — do NOT issue** (OQ-15-2 Pi-4 microbench + AMD-86 §3 interview signal + OR-M6-NONCE; both evidence gates are Nick-paced and are now the M6 critical path).
- **nexsys-hivemind:** committed `82c8068` (M6.4 Phase 2 closeout; M6.4 instruction at `context/coding-instructions/archive/` with EXECUTION RECORD). **Check 9:** the PM edited `project-manager/references/coding-instruction-format.md` (the new publish-count-pins survey category) — STALE until Nick's mirror sync; CONFLICTED is abnormal. **Queued housekeeping (this session):** rotate cross-agent's resolved 06-10/06-11 trail below `## Archived` (only the newest pointer stays active); flip W24 → COMPLETE at rollover.
- **homesynapse-core-docs:** `919e72f` (Research 13 return archived). Docs-lane carries (non-blocking, route at next docs touch): Doc 06 §3.5 `config.yaml` naming predates AMD-71's `homesynapse.yaml` (ties to ruling R-A); §3.6/§4.5 may cite the implemented §12.4 fence semantics. Watermark AMD-87; AMD-69 DEFERRED (don't re-open); Doc 15 LOCKED + inviolate (§8.1 + §4.1-scope_keys nits parked for the next Doc-15-touching amendment).
- **Rulings in force (don't re-derive):** [R4] `ConfigChangedEvent` production-unpublished (§12.4); [R5] AMD-70-family metadata = DIAGNOSTIC/SYSTEM/null-eventTime; [R6] `scope_keys` = config-side `scope_keys.json`; R-A backup `homesynapse.yaml.bak.{basic-ISO}`; R-B/R-C/R-D per the M6.4 EXECUTION RECORD. **FUTURE-AMD queue:** REC-136 (reference-integrity); REC-138 + `ConfigChangedEvent`/§12.4 reconciliation (one family). Research high-water: **Research 13 / REC-140**.

## TASK (recommended order; see flexibility note above)

1. **M6.2 WUCP Phase 2** (when the return + Nick's gate output are in hand). Expected-state answer key for the review — source-verify, don't trust the completion report:
   - **Zero-new-edge (the E2 done-when):** all three `module-info.java` files (config / persistence / app) **byte-unchanged** vs the instruction embeds; `persistence ∌ config`, `config ∌ persistence`, only `app ⊇ both`. NO catalog/`build.gradle.kts` change anywhere (G8 — HKDF is hand-rolled over `javax.crypto`).
   - **Surfaces:** config +`ScopeKeyManager`/`ScopeCipherResult`/`ScopeKey` (public) + package-private impls; `SecretStore` 4→5 (`setAll`, AMD-68 §2.1 Javadoc); persistence +`PayloadCipher`/`EncryptedPayload` (java.base-only signatures, NO consumer yet); lifecycle `HomeSynapseCore` +nullable `PayloadCipher` ctor param (DP-6 — watch the `[exports]`-lint question on lifecycle's transitive posture, a flagged possible-pushback site); app `Main` adapter wiring.
   - **Behavior:** `!secret`/`!env` resolve in BOTH load and reload paths (M6.4 landed first — the sequencing watch-out); CoreSchema regression pin still GREEN; LTD-15 — no secret value in any message/log/issue; AMD-16 backup rotation (max 5); zero-config touches NO key files; `.root-key` 0400 (`Assumptions`-guarded test).
   - **Tests:** AMD-68 §5 five + REC-140 crash-frame; HKDF RFC-5869 A.1/A.2 vectors; the Doc 15 §8.2 freeze-gate encrypt→destroy→**unreadable**; cross-scope decrypt fails. **Event manifests: ZERO changes expected** (no new events in M6.2) — any 55/24/36 pin diff is a red flag.
   - Gate-failure protocol: the M6.1a/M6.4 pattern — PM diagnoses from source, rules, Coder applies; tests bend only to ratified contracts, never to the implementation.
2. **W25 charter** (`context/planning/weeks/2026-W25_jun15-jun21.md`) + W24 → COMPLETE. W24 scorecard input: M6.1 whole + M6.4 + the full Research-13 cycle landed in-window; M5-C floor did NOT start (P6 — escalate the structural fix in the charter, don't re-state the aspiration). W25 candidate lanes: M6.2 closeout→M6 wrap (less M6.3); the M6.3 evidence gates (Nick: microbench + interviews — calendar-bound); M7/M8 runway opening; Research-14 dispatch; M5-C floor (structural, non-preemptable per P6); B2 C8/C9 ratification.
3. **Post-M6 deliberation beat:** (a) **M7/M8 automation runway** — what the charter needs before instructions can exist (Doc 02/Doc 07 re-read scope, Research 4 currency check against post-M4/M5/M6 reality, AMD-block sizing per the P1 multi-piece discipline); (b) **Research 14 scoping → author the brief(s)** on the Research-13 pattern (gap-relative, §0 quote-back, mandatory disposition table, REC-141+): PM-ranked candidates — **R14 = Automation Engine Market Superiority** (HA/Node-RED/Matter automation UX failure modes + runtime robustness, Mom-Test evidence; feeds the M7/M8 charter + instructions; Research 4 (05-22) was design-prior-art, predates M4–M6 learnings) and **R15 = ecosystem/competitive positioning scan** (Matter/Thread certification dynamics, platform trajectories, commercial landscape; feeds M5-C website + the strategy layer; may run as generic deep research — the disposition pass never does). Guardrails: no re-researching Locked ground; no research without a consuming milestone.
4. Standard closeout: WUCP drift check, handoffs, commit messages handed over.

## TOKEN DISCIPLINE

Do not re-derive the sha chain, the rulings, or the M6 gate history — pinned above. Spend the window on: the M6.2 review's contract precision (the zero-new-edge verification and the crypto-surface hygiene are the high-stakes items), the W25 charter's lane structure, and the Research-14 brief's gap-relative register. High-value reads: the M6.2 instruction (the answer key), config + persistence MODULE_CONTEXTs (post-M6.2 state), the M6 charter, Research 4's exec summary (currency check only — not a full re-read).

## STANDING NICK-PACED ITEMS (surface, don't block on)

**Pi-4 microbench (OQ-15-2) + energy/erasure interviews (AMD-86 §3) — THE M6.3 gates, now the M6 critical path** · M5-C website/docs floor (P6 — Research 13 §1/§3.1 is direct input) · B2 C8/C9 ratification · skill-mirror sync (Check 9) · GraalVM/GenZGC spikes → LTD-01 · FUTURE-AMD queue (REC-136; REC-138/§12.4) at a quiet governance window.
