<!--
file: context/handoff/2026-06-15_TRACK4_M7.1-issue-and-forward-execution_session_prompt.md
purpose: Track 4 — a fresh Cowork PM session that PROGRESSES homesynapse-core in the interview-INDEPENDENT lane: issue M7.1 (the next buildable milestone), land the cheap fix-firsts, and run the parallel research/spike dispatches — while the crypto/app-bootstrap lane matures behind Nick's energy/erasure interviews (landing Thu/Fri). The whole point: keep real code moving this week without waiting on the interviews.
audience: PM (nexsys-project-manager hat), Nick, and (downstream) the Coder
state-type: session prompt (forward execution)
status: READY — paste into a fresh Cowork conversation. Run it after Nick closes entry-gate row 4 (the M5-C veto); that is the one precondition for issuing M7.1.
-->

# Track 4 — Issue M7.1 + Forward Execution (the interview-independent lane)

**You are the PM and most-senior engineer (`nexsys-project-manager` hat).** Your job this session is to **keep homesynapse-core moving while the crypto/app-bootstrap lane waits on Nick's interviews.** The most logical next code is **M7.1 (the automation trigger/condition path)** — it is READY, it touches no crypto and no composition root, and it does **not** depend on the interviews or the M6.3-vs-M7 ordering call. App-bootstrap cannot start yet anyway (its charter is gated on R-γ + the interviews + CC-1), so the Coder's best use this week is M7.1. Issue it, land the cheap fix-firsts, fire the parallel dispatches, and stand ready for closeout.

## Step 0
Invoke `nexsys-project-manager`; run the freshness preflight. **Baseline: M6 COMPLETE 4-of-4, core HEAD `1eddd9a`; watermark AMD-93.** New since the last spine reconcile (2026-06-15, all committed/pushed): the Track-3 red-team audit + R-γ challenge brief; the R-δ competitive deep-dive return (rehomed to `homesynapse-core-docs/research/returns/`) + its PM assessment; the M7.1 instruction pinned with the red-team F7/C4 wire-commitment note; the app-bootstrap charter (authored, gates pending); CC-1 spike spec; two research returns rehomed. Host Read tools authoritative; Cowork cannot run Gradle; sandbox git quarantined.

**Read (ground for this session):**
- `context/audits/2026-06-15_core-review_CONVERGE_synthesis.md` — the M7-forward readiness package, the recommended M7 entry sequence, the fix-firsts (C3 before M7.2; C4 the placeholder records before the F5 slice), and the standing carry-pins.
- `context/instructions/2026-06-15_M7.1_trigger-condition-path_coding-instruction.md` — the drafted + **red-team-pinned** instruction you will issue. Re-read its Issue Gate, the P2 survey, and the "What to Watch Out For" (incl. the new F7/C4 wire-commitment pin).
- `context/audits/2026-06-15_redteam_reversibility-audit.md` (F7/C4 — the run-initiation field set is the one-way wire commitment) and `context/assessments/2026-06-15_Research_R-delta_PM_Assessment.md` (the M7-routed findings: AX-5 fail-closed-at-load gate + modes vocabulary + continue_on_error; AX-8 reachability vocabulary).
- MODULE_CONTEXT.md for `event-model`, `automation`, `persistence`, `device-model`, `state-store`; the relevant `module-info.java`.

## The one precondition (gating M7.1 issue)
**Entry-gate row 4 — the M5-C Increment 1 veto (the P6 Lane-4 structural gate) must be CLOSED before any new Core coding instruction issues.** Nick's APPROVE on `homesynapse-core-docs/website/pages/config-superiority.md` flips it and lifts the no-new-Core-instruction condition. **If row 4 is still open at session start, do NOT issue M7.1** — surface it to Nick as the single blocker, and confine the session to the non-Core items (C3 doc-fix, the dispatches) until he closes it.

## The job (interview-INDEPENDENT — do not wait on Thu/Fri)
1. **Verify M7.1's dependencies + run the P2 consumer/pin survey at issue** (the M6-block discipline): fix the exact M7.1-slice manifest/count numbers (`EventTypes` +7, records +8, `EventCategoryMapping` +8, etc. — derive precisely from AMD-92 rows 1/3/11–16/19); enumerate the AMD-88/89 construction-site sweep set; confirm the composition-root manifest fold + the lifecycle wiring pin. Confirm zero unresolved deferred build gates from M6.
2. **Issue M7.1 to the Coder.** Carry the standing pins (type-residency/FLATTEN, the C1-interim no-publish pin, AMD-52 codec, §4c Clock-injection, LTD-11) **plus the red-team F7/C4 pin** (the published diagnostic-event field sets are a one-way wire commitment — complete before first publish) **plus the R-δ AX-5 input** (a definition-validation-at-load fail-closed gate: an automation referencing an unknown trigger-ID/entity is rejected-and-surfaced, never silently loaded — consistent with AMD-89 E89-1 / Locked §6.1).
3. **Land C3** (the cascade-governance doc-fix, `context/instructions/2026-06-15_C3_cascade-governance-doc-fix_coding-instruction.md`) — cheap, doc-only, must precede M7.2; not blocked by row 4 the way a Core instruction is.
4. **Fire the parallel dispatches** (none competes for the Coder or the critical path):
   - **CC-1** (bind-address spike, `context/instructions/2026-06-15_CC-1_bind-address_spike_spec.md`) → Code. Calibrates the app-bootstrap C1 bind posture.
   - **R-γ** (crypto-agility envelope-versioning challenge, `context/instructions/2026-06-15_Research_R-gamma_...CHALLENGE_Brief.md`) → DOCS-Project. Returns the F1 decision the app-bootstrap charter is gated on.
   - **The key-portability follow-up brief** (R-δ §5 #1 — recommended HIGH in the R-δ assessment): author it this session if not yet authored, then → DOCS-Project. Feeds the app-bootstrap AB-4 backup-key-portability seam.
5. **On M7.1 return: execute WUCP Phase 2** — review against `references/review-and-quality.md`, evaluate any Coder pushback, update `automation` + `event-model` MODULE_CONTEXT.md, run the drift check + the dual skill-location sync check, log any deferred build gate, and name the next work unit (M7.2 — but only after C3 is landed).

## Explicitly PARKED behind the interviews (do NOT block on these)
- **The app-bootstrap charter FINALIZATION** (`context/planning/2026-06-15_app-bootstrap_charter.md`): its F2 (encrypted-scope width) gate and the **M6.3-vs-M7 ordering / milestone-placement** call are resolved by the interviews + R-γ + CC-1. Leave the charter authored-and-pending; a post-interview follow-on session finalizes it.
- **M7.2-vs-app-bootstrap sequencing.** M7.2 (run/action/dispatch) can follow M7.1 once C3 is landed; whether app-bootstrap interleaves before M7.2 is the ordering call (post-interview). Do not pre-empt it.

## After the interviews (tee up the follow-on, do not do it here)
A subsequent session: assess the interview returns → resolve **F2** (scope width, default-to-encrypt bias) + the **ordering call** → fold R-γ's return + CC-1's result into the app-bootstrap charter → **finalize the charter** and clear its entry-gate. Then app-bootstrap is issuable.

## Standing escalations (carry to Nick; do not decide)
The auth model (app-bootstrap AB-1); the milestone placement (interviews); the **retry-vs-REC-162 tension** (R-δ AX-5 — re-open as an M7.2 action-model design question, do not flip REC-162 silently); the versioning/deprecation-policy commitment (R-δ AX-7 — before users author definitions).

## Output discipline / Done-when
Lead with the preflight result and the row-4 status. **Done-when:** M7.1 is issued to the Coder with the finalized P2 survey + all carry-pins (or, if row 4 is open, that single blocker is surfaced to Nick and the non-Core items proceed); C3 is landed; CC-1 + R-γ + the key-portability brief are dispatched; the crypto/app-bootstrap lane's pending gates are tracked, not advanced; and M7.1's WUCP Phase 2 closeout is executed when the Coder returns. Commit messages handed over (bang-free, quote-free — deliver as files for `git commit -F`). Success = real homesynapse-core progress this week (M7.1 in the Coder's hands) with the crypto lane cleanly parked behind the interviews.
