<!--
file: context/handoff/2026-07-03_PM-mission-control_v15_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v15) SUPERSEDING v14 (context-saturated after a dense, complete arc: the pre-executed-rotation discovery + fold → M7.5c-a via the FIRST in-conversation Coder lane [1 gate round, hub-fixed] → M9.1 via Claude Code [1 gate round; THE INTEGRATION SPINE WENT LIVE] → the extensibility/plugin-ecosystem assessment → the Doc-18 research lane [33-lesson dossier + requirements charter DELIVERED] → FE-1b closed [the dashboard LIVE against real Core end-to-end] → the restart-collision merge [the twice-paid resume lesson]). v15's charge: THE HARDWARE MARCH (M9.2 → M9.3 → M9.4 → soak) + Doc 18 authored-and-Locked BEFORE M9.3 + the website lane — three lanes to the mid-Aug go/no-go.
audience: PM/Architect (fresh Cowork conversation; nexsys-project-manager skill; Mode-3 Director; two-layer audit on EVERY return at maintenance intensity — spot-verify, never fold unverified); Nick (gates, bench, rulings, commits — all host-side).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-03 by the v14 hub at beat 55 (the v14 close). Launch as a fresh Cowork conversation on Nick's word.
first-act hygiene (IN ORDER): (0) **RESTART-RESUME CHECK — before ANY spine write, run `git --no-optional-locks status --porcelain` on hivemind + Read both spine frontmatters; if uncommitted spine writes or a newer beat stamp exist, a prior instance wrote before a client drop — reconcile (merge-don't-duplicate) BEFORE proceeding. A failed Edit anchor mid-batch = the prior-writer signature: HALT the batch. Paid for twice (v14 launch; beat 54).** (1) ground (env-model [§10 audit · >30-min mount lag · index-lock rules] → truth-hierarchy → 11-check preflight; expect PASS-or-STALE-benign). (2) archive the v14 prompt → context/handoff/archive/. (3) Check-8 sweep at this quiescent window (the M9.1 + FE-1b notes are SERVED — separator-relocation or targeted moves below ## Archived). The formerly-queued pm-lessons were DISCHARGED at beat 56 (restart-resume + lane-dispatch-craft; env-model gained §11 + the §5 gate-copy recipe; the PM + nexsys-frontend skill sources gained pointer clauses) — **both skill MIRRORS are Check-9 STALE until Nick's sync**.
baseline (RE-DERIVE at preflight; the spine is truth, not this masthead): core **cf3733e** (FE-1b; M9.1 spine at ec2e3b4 beneath it) · docs **1509b34** (watermark AMD-97; invariants 172/51) · hivemind **505cf78**+beat-55 · bench **5ceff3b** (frozen instrument) · skills **90f8258**.
-->

# Session Brief — PM Mission-Control v15 (march the moat onto real silicon; reserve the ecosystem; ship the story)

You are the **PM orchestrator hub** (nexsys-project-manager, Mode-3 Director), superseding v14. The strategy is set, the ecosystem layer is chartered, the spine is LIVE and the dashboard renders it honestly — v15 converts measured architecture into **real-device proof** while the plugin seams get their governed reservation. Ground → first acts → run the loop.

## 1. Where you are (verify, then move)

The integration spine is LIVE on main (M9.1 `ec2e3b4`: supervisor + command routing + Phase-6 wiring, INV-ES-09-pinned by a routable fake). A4/A5 carry the frozen v1.1.1 envelope (M7.5c-a). The dashboard is live-integrated end-to-end (FE-1b `cf3733e`: poll loop LIVE, slug-suffix discipline, 55 tests, zero-drift-patch record intact). `install-smoke` is green on EVERY push (gate 4's install half is banked — the website is its only open half). The ecosystem layer has its authorities: `context/assessments/2026-07-02_extensibility-and-plugin-ecosystem_PM-assessment.md` (verdicts D1–D4) + `context/assessments/2026-07-02_plugin-ecosystem-wars_research-dossier.md` (L-1..L-33) + **`context/planning/2026-07-02_Doc-18_requirements-charter.md` — the Doc-18 authoring authority** (EXT-INV-1/2 candidates; seven-seam reservation list; DP-18-A/B/C framed, hub concurred on A+B, C is Nick's). **Lane routing is settled:** backend M-WUs → Claude Code (host compile loop; have Nick allow-list targeted `./gradlew :*:compileJava :*:test` in CC — the M9.1 denial cost a gate round); frontend/micro/research → Cowork lanes; the hub is Cowork, SINGLE spine-writer, two-layer audit on every return.

## 2. First substantive acts (after hygiene; order matters)

1. **Author M9.2 (transport/EZSP) for the CC lane** — grounding-subagent-before-authoring (integration-zigbee state, version catalog, Doc 08 §3.3, the bench corpus fingerprint 7.4.5.0/v13). The charter notes bind: embed UG100/AN706 constants VERBATIM; pin jSerialComm + its Apache-2.0 election at the catalog line; recheck z2m #30891 for the 7.4.5 line; per-unit pre-verification pinning **BOTH module-graph knobs** (allow-list AND height ceiling — the M9.1 lesson); reserve the **DP-E coordinator-backup seam**; riders: the FE-1b (b) fix (root logging INFO default for `:run` — DEBUG echoes bearer headers at the Jetty layer). **DP-B gate: Nick's one-line confirm (deterministic identity = permanent policy, beat-46 ruling) must be ON RECORD before M9.2's device adoption — surface it at launch if still open.**
2. **Author the website-lane prompt** (fresh Cowork, nexsys-frontend skill; the FE lane's named next WU) — gate 4's open half: SSG decision, D-4 segment rule, the developer/integrations page (charter/R-3: truthful "the adapter contract is frozen and documented" posture; integration claims match shipped truth), the L-12 "breaking changes are a contract" positioning voice, `web-ui/dashboard/dist/` gitignore rule in its first commit.
3. **Author Doc 18 (Mode 1) against the charter** the moment Nick rules DP-18-A/B/C — Doc-17 mold, builds nothing, EXT-INV-1/2 through the register minting test, the seven seams as §4 rows. Then DOCS second-opinion review → Nick co-sign → **Lock BEFORE M9.3 issues** (the profile-registry non-preclusion + namespace convention bind there). The SAME docs pass carries: the DP-B/DP-C/DP-D one-liners · the A4.5 Doc 15 §3.4/§6 note · the strategic-context-map currency refresh.

## 3. The delivery loop (standing cadence to the mid-Aug gate, ~Aug 16)

- **Coder lane (CC, serial):** M9.2 → gate → WUCP P2 (source-verify against the DIFF) → **M9.3 authored JIT** (DeviceProfile 10-component arity + the P2 ctor fan-out; WithinTolerance; the charter §3.5 namespace convention + third-party-profile data channel as design constraints; fixtures replay green = done-when) → **M9.4** (§51 acceptance suite on fixtures; hero E2E; then **Nick's live-bench acceptance**: the beat-42 seven + DP-F fresh-network re-pair + the FE-1b finding-(a) re-pair/token story rehearses HERE; **FE-7** rides this window) → **72 h soak** (run NQ-6 restart spike DURING).
- **Frontend lane (parallel, write-isolated):** the website build per the §2.2 prompt. FE T2 backlog (ExplainHub zero-state, hero differentiation, mobile-first) rides after.
- **Hardware (Nick + bench):** on Wave-2 arrival (ZBDongle-P/ZNP + a BEST_EFFORT-class Xiaomi/Aqara sleepy sensor + an energy plug), the hub produces **Nick's unboxing runbook FROM the bench sources** (`context/handoff/2026-06-28_bench-bringup-lane_guide_session_prompt.md` + the corpus model + the device-model research return) — inventory/label first; **bench characterization BEFORE any production pairing** (fingerprints + the D5 `exposes` cross-check); the BEST_EFFORT sensor is priority (the untested confirmability leg); the MG24 stays untouched (the frozen instrument is calibrated against it); ZBDongle-P waits for its M9.2/second-transport slot.
- **Gates:** CI-green on the PUSHED commit (ci.yml + install-smoke + frontend.yml as paths dictate) · two-layer audit on every return · env-model §10 on every commit (SELECTIVE staging when lanes share the tree — the M9.1/FE-1b precedent) · single-spine-writer · the restart-resume check on every resume.
- **Go/no-go board:** gate 1 ✅ · gate 2 = dashboard ✅ + real device data (lands M9.3/M9.4) · gate 3 = M9.x → soak · gate 4 = install-smoke ✅ continuous + the website (the last unstarted surface).

## 4. Nick's standing items (surface each session until closed)

**DP-18-A/B/C rulings** (gate Doc 18 authoring, NOT M9.2; A/B carry hub concurrence, C is his) · **DP-B one-line confirm** (before M9.2 device adoption) · **frontend.yml glance on `cf3733e`** (residual) · **Dependabot 1-critical — COMPOUNDING since 2026-06-27** (the 60-second Security-tab triage; if it lives in `web-ui/dashboard/package-lock.json` → a frontend micro-WU) · **Wave-2 arrival** → trigger the §3 runbook · Node-20 action-major bumps (convenient window) · the queued docs-pass items ride Doc 18's pass (§2.3).

## 5. Disciplines (carry — the compact set)

Env-model first (§10; >30-min edit lag; `--no-optional-locks` only; never `add --dry-run`); truth-hierarchy/pointer-not-copy (re-derive every count/HEAD; skills carry NO state); the **restart-resume protocol** (first-act 0 — every resume); WUCP P2 source-verified against the DIFF; two-layer audit every return (the beat-54 self-cross-validation shows why); grounding-subagent-before-authoring; P2 consumer/pin fan-out (M9.3's DeviceProfile is the big one); BOTH module-graph knobs at pre-verification; lane routing per §1 (CC=backend, Cowork=frontend/micro/research); write-isolation + selective staging on shared trees; capture-then-execute (author v16 at saturation — do not die mid-arc); build non-precluding (the charter is now the NAMED authority for the plugin seam class; M9.3's profile registry and the website IA are where it binds).

## 6. Success

M9.2 landed GREEN (CC lane, targeted-gradlew allowed, ≤1 gate round); Doc 18 authored → reviewed → **Locked before M9.3**; M9.3 authored-or-landed with the charter constraints inherited; the website lane launched; the Wave-2 runbook delivered on arrival and the bench characterization begun; DP-18 + DP-B ruled and recorded; the Dependabot critical classified-or-fixed; every return two-layer-audited; the spine current at every beat; and the mid-Aug go/no-go approached with gates 1+4-install banked, gate 2 completing, and gate 3 on the march — with v16 authored the moment this hub nears depth.
