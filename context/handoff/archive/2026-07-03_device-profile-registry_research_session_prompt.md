<!--
file: context/handoff/2026-07-03_device-profile-registry_research_session_prompt.md
purpose: Dispatch-ready brief for a FRESH Cowork research lane producing the device-profile-registry research dossier — the grounding-before-authoring input the M9.3 coding instruction requires. M9.3 freezes the profile registry (the highest-consequence freeze in the current arc); its CONSTRAINTS are Locked (Doc 18 §3.5) but its DESIGN space is not yet researched to the project's own standard. The Doc-17/Doc-18 dossier discipline applies: primary-source receipts, anti-fabrication tags, per-claim source tables, honest gaps. Research ONLY — this lane designs nothing, authors no instruction, and never relitigates a ruled decision.
audience: a FRESH Cowork conversation (nexsys-project-manager skill for discipline; this is a research lane — Mode-agnostic, read-only); Nick (launches; receives framed decision points); the v16 hub (audits + folds; authors the M9.3 instruction FROM this dossier).
state-type: session prompt (research lane).
status: READY — authored 2026-07-03 by the v16 hub (beat 63), concurring with the M9.3-research recommendation and extending its six questions with four hub vectors. Parallel-safe vs everything (read-only; single-artifact return). Launchable immediately; the M9.3 instruction waits on THIS return + the Doc-18 Lock + the CC allow-list fix.
baseline (re-derive at a light preflight): core `6ea6912` (M9.2 landed — the transport substrate incl. `drainPendingCallbacks()`, the M9.3 feed) · docs `78e0c5c`-or-later (Doc 18 DRAFT-review-folded on disk; **pre-authorized baseline shift: the Doc-18 LOCK commit may land mid-lane** — status flip + governance/strategy edits; §3.5's constraint CONTENT will not change; re-verify nothing, note it) · hivemind = newest PROJECT_SNAPSHOT masthead beat · bench `5ceff3b` (frozen instrument).
known hazards (env-model FIRST — non-negotiable): **BOTH repos' VM porcelains are currently showing truncated-`.git/index` read phantoms** (staged-looking D/R flags, rename targets cut mid-filename — the bench-class signature, re-diagnosed twice at beats 62–63): NEVER trust VM git status; host file tools are truth; read-only git via `--no-optional-locks` log/show ONLY; NEVER `git add` (incl. `--dry-run`) · truncated-tail phantom on recently-edited files (host Read is truth) · interactive tools can fail mid-stream (veto-or-default, record the default).
write-isolation: your ENTIRE write surface is ONE new file — `nexsys-hivemind/context/assessments/2026-07-03_device-profile-registry_research-dossier.md`. Zero repo writes elsewhere, zero shared-file appends (the hub folds your return).
reads (in order — ground INTERNAL truth before external survey):
  - nexsys-hivemind/context/process/cowork-environment-model.md (FIRST) → PROJECT_SNAPSHOT newest beat
  - homesynapse-core-docs/design/18-extension-and-plugin-architecture.md §3.5 + §4 row 5 (the LOCKED-or-locking CONSTRAINTS your dossier serves: namespaced identity keying · bare namespace first-party · collision = consent+UI-flag+core-brand · fingerprint-priority + rebrand mapping · Zigbee-scoped-with-general-seam-note · third-party DATA channel first-class). These are RULED — validate against them, never relitigate; if field evidence CONTRADICTS one, flag it with receipts as a decision point, do not soften it.
  - homesynapse-core-docs/design/08-zigbee-adapter.md §3.3 (probe/baseline) + §3.4–§3.7 (interview, reporting config, ingestion, attribute mapping — AMD-96/97-folded: the `confirmation[]` block, the OccupancySensing row, the reporting-config parity items) — the ratified requirements the registry must serve
  - homesynapse-core-docs/design/02-device-model-and-capability-system.md §3.12 (discovery/adoption pipeline + hardware-identifier matching — DEVICE-MODEL-owned; map the boundary: the profile registry is protocol-side identification/normalization, adoption identity is Doc 02's) + §3.8 (per-device confirmability override)
  - homesynapse-core/integration/integration-zigbee/MODULE_CONTEXT.md (the Phase-2 `DeviceProfileRegistry` + `DeviceProfile` scaffolds; the 10-component DeviceProfile arity note + P2 ctor fan-out; `WithinTolerance`; `drainPendingCallbacks()` + bound-and-drain; the checked-PIE seam rule) + the actual `DeviceProfile.java`/`DeviceProfileRegistry.java` source (frozen Phase-2 shapes — the design must land inside them or NAME the AMD need)
  - nexsys-bench/ corpus model + fixtures (the Wave-1 captured devices: Hue LCA017 confirming, SNZB-03P motion — **the M9.3 acceptance spec is "fixtures replay green"**; your recommendations must be exercisable against this corpus) + the 2026-06-28 device-model/corpus research return in nexsys-hivemind/context/ (locate via grep; anti-fabrication: if absent, say so)
  - nexsys-hivemind/context/assessments/2026-07-02_plugin-ecosystem-wars_research-dossier.md §1.2 (Z2M) + L-22/L-29/L-30 (do not re-research what it settled; EXTEND it)
  - governance register entries INV-CE-04 (protocol agnosticism) + INV-CE-05 (extension model) + the INV-PR performance section (Pi-class budgets)
-->

# Session Brief — Device-Profile-Registry Research Dossier (ground M9.3 before it freezes)

You are a **fresh research lane**. M9.3 (interview → ingestion → profile registry) is the next Core milestone and **freezes the profile registry — a one-way door**. Doc 18 §3.5 Locked the *constraints*; nobody has yet researched the *registry design space* to the standard this project demands (the Doc-17/Doc-18 dossier bar). Your product: the dossier the hub authors the M9.3 instruction FROM. Env-model first, light preflight, then research. **You design nothing and relitigate nothing.**

## The ten questions (answer each with primary-source receipts + an M9.3-facing recommendation)

**The six from the field survey (validate, deepen, receipt):**

1. **Matching model.** Lightweight (modelID+manufacturerName — Z2M `fingerprint`, deCONZ DDF pairs) vs deep signature (endpoints+profiles+device-types+clusters — ZHA quirks, Hubitat inClusters) vs layered. Receipt the documented failures: Tuya generic-modelID collisions (Z2M duplicate fingerprints; deCONZ wrong-DDF #5788), ZHA `MultipleQuirksMatchException` #1508 (built-in-vs-custom precedence — the exact collision class Doc 18 §3.5(c) rules), Hubitat's too-coarse cluster matching. Recommend the layered shape + the exact precedence order, mapped onto §3.5(d)'s fingerprint-priority + rebrand mapping.
2. **Identify vs normalize.** Does a profile merely SELECT capability mapping, or also REPAIR protocol deviations (ZHA's Replacement step; Z2M converter fragments; deCONZ DDF subdevices)? This decides what `DeviceProfile` carries and what stays in converter/ingestion code. Check the frozen 10-component `DeviceProfile` shape against your recommendation — name any AMD need explicitly.
3. **Reporting-config robustness.** How does each system survive `UNSUPPORTED_ATTRIBUTE`, unreportable attributes, misbehaving reporting across firmware versions (SmartThings Edge `configure()`; Z2M reporting failures)? Recommend the degrade ladder for M9.3's config path, tied to `WithinTolerance` and the AMD-97 `confirmation[]` semantics (a device that can't report per spec must degrade its confirmability class HONESTLY — never-false-CONFIRMED is inviolable).
4. **Profile-format versioning.** ZHA quirks v1→v2 (the rewrite's cost + what forced it); Z2M modernExtend migration; deCONZ DDF status/maturity field. Recommend how profile-format evolution rides the AMD-54 `(major, minor)` schema-versioning discipline so the corpus survives format changes forward-only (DP-18-A alignment).
5. **Constrained-hardware performance.** deCONZ's bundle index-header (match-before-full-parse); Z2M's lazy loading; startup cost at thousands-of-profiles scale. Recommend the load/lookup shape for Pi-class INV-PR budgets — registry init must not blow the boot budget when the corpus is 5,000 profiles.
6. **The Matter-shaped generic seam.** Matter has NO quirks corpus (self-description via standard device types at commissioning). Concretize Doc 18 §3.5(d)'s "Zigbee-scoped-with-a-general-seam-note": the generic contract is per-protocol-family PROFILE SOURCES (Zigbee needs a corpus; Matter needs none; the seam must not force Matter through a quirks shape). Receipt from the Matter data-model docs.

**The four hub vectors (the thousands-of-devices scaling half — Nick's explicit charge):**

7. **Corpus update cadence + distribution.** Z2M ships converter updates near-per-merge; ZHA quirks ride HA's monthly train; deCONZ DDFs hot-load. At scale, the profile corpus updates FASTER than code releases — how does a HomeSynapse device-profile update reach users WITHOUT a core release, inside LTD-16's additive-only discipline and Doc 18 seam 6's versioned-artifact + compat-range shape? (The data channel is Doc-18-reserved; recommend the V1-safe posture: what M9.3 must not preclude so profile-corpus-as-updatable-data works later.)
8. **Contributed-profile proof discipline.** Concretize L-22's staging-area model for profiles: what must a submitted profile PROVE? (herdsman-converters CI; ZHA quirks tests.) Map onto our own pattern: **the bench-fixture replay IS the natural acceptance gate** — a profile submission carrying a captured fixture that must replay green. Recommend the M9.3-era shape (internal-only now, community-ready later).
9. **Sleepy/battery-device interview.** Wake-window constraints on interview + reporting config (Z2M pairing instructions "keep the device awake"; ZHA/deCONZ battery-device failure modes; SmartThings Edge battery quirks). M9.3's interview state machine must handle a device that sleeps mid-interview — timeout/resume/partial-profile policy. This is the BEST_EFFORT confirmability leg (Wave-2's sleepy sensor is its bench validation vehicle — recommend what M9.3 builds vs what waits for that hardware).
10. **Identity + rejoin stability.** Match results persist keyed on what (IEEE address)? What re-triggers interview/matching — rejoin, nwk-address change, OTA firmware update changing the fingerprint (documented cases in Z2M/ZHA)? Recommend the pin-vs-rematch policy and its interaction with DP-B deterministic identity + the DP-F re-pair story (M9.4 bench rehearsal).

## Deliverable + discipline

ONE dossier: `nexsys-hivemind/context/assessments/2026-07-03_device-profile-registry_research-dossier.md`. Shape: per-question findings with **per-claim source tables** (URL + date + VERIFIED/INFERRED/UNVERIFIED tag; the ≥6 most load-bearing claims re-fetched lane-direct, ✓✓-marked) → a consolidated **M9.3 design-constraint recommendations** section (each rec traces to a Q-finding AND checks against the frozen Phase-2 shapes + Doc 18 §3.5 — name every AMD need rather than assuming one) → **decision points for Nick** framed evidence/options/recommendation/mark (only where genuinely his: one-way doors, security posture, scope) → honest gaps + corrections-to-this-brief. Prose ≤ ~8K words, source tables excluded (the Doc-18 dossier envelope precedent). **Internal grounding beats external survey where they conflict — our corpus + ratified AMDs are the acceptance spec; the field is advisory.**

## Return contract

Env-model §10 audit in the dossier's tail: this lane creates exactly **1 new file**; prepared host-side command stages exactly that 1 path; message bang-free/backtick-free via `ClaudeFolder/_scratch/` for `git commit -F`; expected count stated. If your porcelain shows anything else attributable to you, STOP and say so. Refuse-to-close: name what the M9.3 instruction still needs beyond this dossier (expected: the Doc-18 Lock confirmation + the CC allow-list verification — both outside your lane).
