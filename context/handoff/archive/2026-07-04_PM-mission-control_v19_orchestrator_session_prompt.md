<!--
file: context/handoff/2026-07-04_PM-mission-control_v19_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v19) SUPERSEDING v18 (retired at two full arcs + six beats: the adversarial return audited [one refuted-DEFECT, two pm-lessons] → the §1 batch ruled in ONE turn → M9.4a authored [P1 split; three grounding corrections incl. the ledger-side bridge] → dispatched on the proven allow-list → landed 64f2ce0+748962f with check GREEN 155 PRE-commit → two-layer audited ACCEPT ZERO DEFECTS [six [REVIEW]s ruled; the hero loop GREEN hardware-free — THE THESIS IS A TEST RESULT] → AMD-98 RATIFIED b65ed7d [watermark 98; 174/52] → Nick's three sharpenings + one confirm folded [brightness DERIVATION-side; issuability semantics pinned; explanation-vocabulary in-scope; guard-membership test]). v19's charge: THE CURRENCY PASS (Nick's directive, grounded quote-exact in §2) → M9.4b on the clean substrate → bench → soak → the explainability lane.
audience: PM/Architect (fresh Cowork conversation; nexsys-project-manager skill; Mode-3 Director); Nick (gates, commits, bench, mirror sync).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-04 by the v18 hub at beat 6. Launch on Nick's word.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — lock-free porcelain ×5 + both spine frontmatters BEFORE any spine write). (1) Ground: env-model FIRST (§2 attribution corollary; §4 index-lock + corrupt-index escalation; §5 anchor hygiene — line-anchored heading cuts, the archival-rule text was de-baited at v18 beat 4 after baiting its SECOND splice; §10 exact counts; §11) → the cold-boot trio → truth-hierarchy → the 11-check preflight (expect PASS or STALE-benign; Check 8 expects ACTIVE: 1 — the M9.4a coder note is annotated SERVED-archives-on-commit; archive it below the separator at your first P2 touch). (2) Archive the v18 prompt → context/handoff/archive/. (3) READ IN FULL: pm-handoff v18 beats 1–6 (beat-2 = the §1 rulings verbatim record · beat-4 = the audit + six [REVIEW] rulings · beat-5 = the sharpenings incl. the brightness DERIVATION-side correction with both Doc-08 quotes · beat-6 = the landing reconciliation + this charter's origin) + the backlog M9.4 row (the a-landed record + the FULL M9.4b inherited charter) + `context/pre-verifications/WU-M9.4.md` (P1–P42 — still the pin sheet for M9.4b's shared surfaces).
baseline (RE-DERIVE at preflight): core **748962f** (M9.4a — landed as 64f2ce0 [2 renames] + 748962f [41 files], content-exact split, reconciled beat 6) · docs **b65ed7d** (AMD-98 RATIFIED; **watermark AMD-98; 174/52**) · hivemind **33aae08 + beat-6 pending-or-landed (verify)** · bench **5ceff3b** · skills **ae434ca** (mirrors byte-identical at v18 launch; Check 9 goes STALE mid-pass — see §2E). **OPEN GATE: the ci.yml + install-smoke glance on `748962f` is UNCONFIRMED — verify with Nick FIRST; it is the gate of record and M9.4b does not dispatch until it is green** (§4b spirit: the check ran green pre-commit, so this is the CI-record formality — but a formality that has caught real drift before). **Dependabot: day 7+, 1 critical, re-nagged on BOTH of today's pushes.**
-->

# Session Brief — PM Mission-Control v19 (the currency pass → M9.4b → bench → the explainability lane)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v18. **The thesis ran on 2026-07-04:** motion → `occupancy.occupied` → the real engine → dispatch → real adapter code over the scripted NCP → an honest `state_confirmed` joining command AND report — hardware-free, every suite green on first execution, zero audit defects. What stands between here and the mid-Aug go/no-go: clean substrate → M9.4b → Nick's bench → the 72 h soak → the explainability surface that SELLS the verdicts the loop now renders.

## 1. Where you are (verify, then move)

M9.4a landed the moat: supersession expiry (issuance-supersedes), ledger-side expectation derivation, adoption-installed per-device confirmation, the classifier arms, the ==13 band, the command write path, and the two composition-root gates. **M9.4b's charter is FULLY RULED and lives in the backlog M9.4 row** — headline items: registry UNIFICATION (R4 — mandatory before the real transport binds) · brightness DERIVATION-side fix (Doc 08 §3.5/§392 are the constitutional quotes — beat-5 record; root correction = `StandardCapabilities.brightness()` schema 0–100→0–254, making the bridge rescale generic; + query-time percent derivation before any dashboard render) · identify/effects issuability with the PINNED semantics ("never-tracked and honestly-verdicted are different promises" — immediate rendered verdict, never silent bypass, test-asserted) · the disposition-guard MEMBERSHIP test · ReopenAction composition (the PortWatchdog:58 seam) · NETWORK_UP await (stackStatusHandler — frame id bench-verify) · the restart-honesty E2E · F-4/F-7/F-8/F-11/F-12/F-14/F-15 · N-4/N-5/N-8 · DECIDE-04 phantom-comment fix in Main.java · bench-acceptance staging (the beat-42 seven + DP-F re-pair + Q10 + FE-7; **hashed-TCLK ELECTED with the fallback clause — it lands as the formation-config pin in Nick's bench protocol, not code branching**).

## 2. FIRST SUBSTANTIVE ARC — THE CURRENCY PASS (Nick's directive, v18 beat 6: stale context/tokens cleaned throughout; skills current on everything built and in flight, so future session-prompts "precisely define and instruct all the details necessary for the most optimal coding results")

Execute as ONE bounded pass, sections in order, exact counts on every commit handed to Nick. The inventory is GROUNDED (v18 verified each item at close) — trust but re-verify anchors before cutting.

**A. Spine rotation (env-model §8 token economics; file-tools-only, phantom-safe, the v14 rotation method):** pm-handoff — KEEP v18 beats 1–6 + v17 beats 1–6 + Open Risks; rotate everything older → `archive/pm-handoff-beats-rotated-2026-07-04.md` (chronology-assert before cutting; the file's existing rotation markers show the shape). PROJECT_SNAPSHOT — KEEP the v18 + v17 blocks; rotate v14–v16 blocks → `archive/PROJECT_SNAPSHOT-beats-rotated-2026-07-04.md`. Verify tails via host file tools after every scripted cut (§5).

**B. handoff/ rotation:** `mv` the **19 SERVED prompts/briefs** (everything in `context/handoff/*.md` EXCEPT pm-handoff.md · coder-handoff.md · cross-agent-notes.md · the STANDING v19 prompt) → `archive/`. The v18 prompt is one of them (your first-act step 2 already moved it — count accordingly and state the final number from porcelain, not from this line).

**C. Reference-file currency (quote-exact targets):** START_HERE.md — full read; verify pointer-cleanliness; refresh Nick's copy-paste launch lines to the current pattern (the v19-era launch line). `context/strategic-context-map.md` — Check-10-style pass; add/refresh rows only where the map's own catalog classes demand. `context/process/2026-05-31_two-project-claude-architecture.md` — adjudicate: does the two-Claude-Projects workflow still exist post-Cowork? If superseded, mark SUPERSEDED at the masthead pointing at the current model (never delete history). `context/planning/weeks/` — **author the W28 plan** (jul06–jul12: M9.4b → bench → soak start; the explainability lane; reconstruct W27 close-state from the v17/v18 beat records). Weekly W27 → Status COMPLETE at its close.

**D. project-knowledge/ regenerate-or-retire (the truth-hierarchy §3 rule; the F2 rot class):** four hand-maintained files (`CLAUDE_PROJECT_CORE_custom-instructions.md` · `CLAUDE_PROJECT_DOCS_custom-instructions.md` · `CLAUDE_PROJECT_KNOWLEDGE_SOURCES.md` · `Decisions_Quick_Reference.md`). Audit each for rot; recommend retire (if the Claude-Projects workflow is dead) or regenerate-from-source with a regeneration rule; **Decisions_Quick_Reference has a live consumer** (rationale-index Layer-0) — it either gains a regeneration line or the pointer retargets to the registers. NICK RULES the retirements — present evidence + recommendation, veto-or-default.

**E. THE SKILLS PASS (writable sources; Check 9 goes STALE on commit — Nick's mirror sync BEFORE M9.4b authoring starts):**
- `nexsys-hivemind/project-manager/SKILL.md` masthead: +audit-craft discipline (pm-lessons 2026-07-04 ×2, POINTERS not restatements): *audit-agent prompts QUOTE the instruction verbatim for subtle semantics; agent verdict labels are CLAIMS, agent quotes are EVIDENCE — adjudicate every DEFECT/REFUTED against the primary text; any new-test Files-table row in an untouched module gets a build-file gating check.*
- `references/coding-instruction-format.md`: **+#13 test-home grounding** (the R1 lesson, near-verbatim from pm-lessons: a test that exists but never runs in the gate of record reads as coverage).
- `references/review-and-quality.md`: the audit fan-out method gains the two audit-craft rules above.
- `references/constraint-enforcement.md`: spot-verify the at-a-glance table is still true at AMD-98 (the band change is register/AMD-level; expect no row change — verify, don't assume).
- `nexsys-hivemind/coder/` (SKILL.md + CLAUDE.md + references): full de-stale read — the coder skill was last touched at v17 beat 6; verify zero state claims, and fold the M9.4a-earned coder-lessons pointers if the skill's lesson-index section carries them.
- **`nexsys-skills/orchestrators/nexsys-frontend/` — the BIG fold (this is what "current on the integrations" means for the next lane):** the LIVE verdict vocabulary as domain facts (POINTERS to source): `state_confirmed(commandEventId, reportEventId, attributeKey, expected, actual, matchType)` · `command_result.outcome` ∈ {acknowledged, rejected, superseded, unconfirmed, expired_on_restart} (String field; the ledger + adapter publishers) · `command_confirmation_timed_out` · the hero-question mapping Nick ruled IN-SCOPE (*honest-can't-know vs known-failed vs deliberately-superseded — "its entire vocabulary"*) · `StandardExplanationService`'s UNCONFIRMED/FAILED flattening = the lane's named target · **brightness is 0–254 canonical with percent DERIVED AT QUERY (Doc 08 §3.5) — the dashboard derives %, never re-scales state** · the read-API contract stays FROZEN v1.1 (the lane grounds whether verdict events flow through it or need a contract conversation — flag, don't improvise).
- **MODULE_CONTEXT content-audit:** the three M9.4a updates (automation · zigbee · integration-runtime) were checklist-verified but never CONTENT-audited — spot-audit each against the landed diff (`git show 748962f -- <path>`); fix drift in place.

**F. Deletables + ledger (Nick manual, surface in your first report):** `rmdir skills-initiative` (empty husk) · `rm homesynapse-core-docs/.git/index.lock.cleared-by-cowork-2026-07-03` (inert) · **Dependabot day 7+ (1 critical — re-nagged twice on 2026-07-04)** · the CI glance on `748962f` if still open.

**Pass close:** WUCP-P2-style drift check across everything touched → the commit set(s) handed with exact counts → **Nick's mirror sync → re-run the preflight → expect full PASS → only THEN M9.4b authoring begins.**

## 3. Then the delivery loop (standing cadence to the mid-Aug gate)

M9.4b JIT authoring on the clean substrate (grounding-subagent at the post-pass HEAD → `WU-M9.4b` additions to the pin sheet → the instruction: registry unification FIRST, then the ruled charter per the backlog row; quote the beat-5 record EXACTLY for the brightness shape and the issuability semantics) → dispatch (CC lane; verify settings unchanged) → two-layer audit vs the DIFF → WUCP P2 → **Nick's live-bench acceptance** (hashed-TCLK + fallback clause exercise HERE) → **72 h soak** (NQ-6 restart spike + the W2-VAL synthetic legs DURING) → the explainability-UX lane charter authored on the refreshed frontend skill (it consumes REAL verdicts — never mocked screens). The dossier-content website lane stays launchable anytime (independent). Wave-2 arrival → the unboxing runbook (bench-first; the MG24 stays untouched; a v14-batch unit = F-1's characterization asset).

## 4. Nick's standing ledger (surface each session until closed)

**CI glance `748962f`** (gate of record — blocks M9.4b dispatch) · **Dependabot CORE: 5 (1 critical) — day 7+, re-nagged on both 2026-07-04 pushes; his own words: stop deferring** · mirror sync (mid-pass, Check 9) · project-knowledge retirement rulings (§2D) · `npx astro telemetry disable` · website.yml adopt · LICENSE-flip timing · frontend.yml `cf3733e` glance · Wave-2 arrival watch · Node-20 action-majors · husk `rmdir` + inert lock `rm` (§2F).

## 5. Disciplines (carry — the compact set)

Env-model first (§2 phantom family + attribution corollary; §4 lock hazards; §5 anchor hygiene + tail verification after scripted cuts; §10 audit + exact counts; §11 restart-resume). Truth-hierarchy/pointer-not-copy (re-derive every count/HEAD; the register's regeneration line is the watermark's home — it reads AMD-98 as of b65ed7d). Two-layer audit on EVERYTHING incl. your own subagents AND your own audit prompts (labels-are-claims/quotes-are-evidence; quote primary text for subtle semantics — the twice-proven v18 lesson). Grounding-subagent-before-authoring; test-home build-file grounding on new-test rows (#13). WUCP P2 vs the DIFF, never the report. Quote spine phrases EXACTLY in lane briefs AND audit prompts. Lane routing: backend M-WUs → Claude Code; frontend/research → Cowork lanes; the hub never implements. Never M9.4c-anything while an M9.4b gate is unresolved (§4b). Capture-then-execute: author v20 the moment you near depth. **The north star (carried until the loop closes ON SILICON): one-way-door precision outranks velocity — and its v18 extension: never-false-CONFIRMED's spirit extends to never-false-ALIVE.** The independence charge stands: second-check our codebase AND the field's; an evidence-backed refutation is a reward.

## 6. Success

The currency pass lands with zero stale claims surviving in any hot-path file, the 19 archives rotated, W28 live, project-knowledge ruled, all four skills current-and-synced (Check 9 PASS post-sync), and the three M9.4a MODULE_CONTEXTs content-audited → M9.4b authored from the ruled charter quote-exact, dispatched on the proven allow-list, landed ≤1 gate round, audited two-layer, WUCP P2 complete → Nick's bench acceptance staged (hashed-TCLK exercised with the fallback clause honored) → the 72 h soak RUNNING with the W2-VAL synthetic legs riding it → the explainability-UX lane chartered on real verdict vocabulary → v20 authored before depth — and the mid-Aug go/no-go approached with gate 3 physically on the bench and the moat rendering honest verdicts a user can SEE.
