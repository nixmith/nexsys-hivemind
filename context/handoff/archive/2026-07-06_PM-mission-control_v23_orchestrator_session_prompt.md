<!--
file: context/handoff/2026-07-06_PM-mission-control_v23_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v23) SUPERSEDING v22 (retired at: launch FULL-PASS → M9.4-TCJ audited ACCEPT/landed 2a5fba2 → bench iteration 1 [enablement silicon-accepted, joins silent] → M9.4-NCFG independently verified/re-issued/audited/landed ccba69d → iteration 2 [THE DEVICES JOIN: proposed COMPLETE ×2 + ias_zone_enrolled; TCJ §A.3 silicon-closed] → the adoption gap escalated on two Locked docs → Nick RULED (b) config accept-list → M9.4-ADP pins-folded + dispatched → the ccba69d CI red adjudicated intermittent-definitive [rerun green; deflake row queued] → v23 authored at beat 5). v23's charge: the ADP return audit → ITERATION 3 (the join arc CLOSES: device_adopted ×2 + non-empty entities) → THE ACCEPTANCE RUN (runbook P3.11 Configure-Reporting → P4 bench-hero → P5 §51 legs → P6 reopen) → the 72 h soak → the seed-custody WU → the explainability-lane return.
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-06 by the v22 hub at beat 5 (Nick's rotation directive: bank before depth). Launch when v22 retires.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — LOCK-FREE porcelain ×5 [--no-optional-locks ONLY] + BOTH spine frontmatters BEFORE any spine write). (1) Ground: env-model FIRST (§2/§4 — the corrupt-index signature hit the hivemind mount TWICE this arc [object-reads-only when it shows]; the truncated-tail phantom hit a diffstat [596 phantom deletions]; host+compiler are truth) → the cold-boot trio → truth-hierarchy → the 11-check preflight. (2) Archive the v22 prompt → context/handoff/archive/. (3) READ IN FULL: pm-handoff v22 beats 1–5 (beat-4 = the escalation; beat-5 = the rulings) + the coder-handoff NEXT-WU pointer + the M9.4-ADP instruction (pins-folded form) + the bench-acceptance record (incl. the Iteration log) + the runbook field-validated block. Treat every HEAD/count as a CLAIM to RE-DERIVE; source wins over this prompt — say so plainly.
baseline (RE-DERIVE at preflight): core **ccba69d** (= 2a5fba2 [TCJ] + NCFG; ci.yml GREEN via rerun — intermittent adjudicated; install-smoke glance may still be open) — PLUS whatever M9.4-ADP lands as (dispatched at v22 close; verify HEAD; §4b: a `check` gate opens if ADP landed un-audited) · docs **b65ed7d** (AMD-98; 174/52) · hivemind **0cf1e34 + the v22 beat-5 commit (6: 5 M + 1 new — verify landed)** · bench **f8a0693** · skills **0c00ece**. Dependabot: 1 critical, day 9+ — keep nagging. Node-20 CI deprecation showing on runs.
-->

# Session Brief — PM Mission-Control v23 (the ADP return → iteration 3 → THE ACCEPTANCE RUN → soak)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v22. **The join stack is proven end-to-end on silicon**: formation/resume (ch20, custody-preserved), the TCJ enablement (§A.3 silicon-closed — a real key exchange through the transient well-known key), the NCFG config prelude (read-back-instrumented), interview → `device_proposed COMPLETE` ×2 devices, IAS enrollment. The ONE remaining join-arc step is adoption — **M9.4-ADP** (config accept-list; Nick's ruling (b), pins folded: COMPLETE-proposals-only + re-link-automatic) is dispatched. **The hub never implements — it intakes, audits, records, and rules WITH Nick.**

## 1. FIRST SUBSTANTIVE ACT — the M9.4-ADP return

Two-layer audit vs the DIFF at the new HEAD (labels-are-claims, quotes-are-evidence; QUOTE the instruction's sentences — especially pin-1: a listed-PARTIAL proposal WARNs `proposal_incomplete_not_adopted` and sits; the re-link guard: adopt fires ONLY on FRESH proposals, exception-driven flow BANNED; conservative default: absent list adopts NOTHING). Verify the ONE declared schema diff (`adopt_devices`) and zero mints (71/41/53). Rule any [REVIEW]s → WUCP P2. If ADP landed un-audited, the audit is your first act and §4b holds until done.

## 2. ITERATION 3 — the join arc CLOSES (then THE ACCEPTANCE RUN)

Nick's iteration-3 recipe (beat-5): NO custody wipe (ch20/`0x9b65` resumes) · `adopt_devices: ["0x00178801101A09BB", "0xF044D3FFFE9C78D7"]` + `permit_join_duration: 254` · relaunch · Hue power-cycle (re-announce → re-interview → fresh COMPLETE proposal → config-adopts) · SNZB re-pair (it LEFT; 5 s hold in-window). **Done-when: `device_adopted` ×2 + non-empty `/api/v1/entities` — TCJ+NCFG+ADP close together.** Then resume the runbook mid-P3: **step 11 Configure-Reporting explicit** (the WARN-sweep; STOP-and-ping on `reporting_ack_lies`) → step 12 entity ULIDs → **P4 the bench-hero automation** (motion → turn_on → set_brightness 50 → the 4550→4525 K supersession probe → identify 5 s) + the restart RESUME leg → **P5 §51 live** (CONFIRMED only via authoritative reports · identify = immediate honest UNCONFIRMED · superseded expires verdict-free · latencies vs the corpus reference points · IAS ×2 twins · Q10 · FE-7 · optional bulb-off timeout-honesty probe) → **P6 reopen leg + REMOVE the PJ key** (the exception window closes). RECORD every outcome into the bench-acceptance record's iteration log. Any hashed-TCLK join-instability ⇒ the SD-5 fallback ruled WITH Nick (never improvised). The north star every leg: **never-false-CONFIRMED / never-false-ALIVE — the honest verdicts ARE the product.**

## 3. On acceptance → the 72 h soak; then the queue

**The soak ENTERS on acceptance** (PJ key OUT first; NQ-6 restart spike DURING; a soak restart resets the clock — protect the window; charter the W2-VAL synthetic legs at soak-entry). **The seed-custody WU** (SD-5) authors JIT on the bench characterization — **the SD-5 HARD GATE rides until it lands: no non-bench network forms first** (spine-recorded ×3). **The explainability-UX lane return** (chartered v21 beat-1) gets the two-layer audit + the skill-§4a staleness fold at the next skills pass. **The javadoc-flip micro-pass** rides the next core-touching WU: the 0x0019/0x90 + TCJ (0x0055/0x0111/0x0024/0x0023 + decisions) + NCFG (0x0053/0x0052 + the REQUIRED trio) BENCH-VERIFY annotations are ALL silicon-proven now — flip them with evidence pointers. **The deflake row** (OR-GATE-M9.4-NCFG's residue, Nick's named moat-debt): `ReplayTransitionIT.awaitCheckpoint` reports progress + fails on STALL not wall-clock — rides the next event-bus-touching WU (its own micro-WU on recurrence). **The banked M9.4 polish queue** (backlog row): permitJoinDeadline-clear-on-reopen · unroutable membership row · fence effective-policy · degradeRule non-array · DECIDE-04 sweep · F-7b non-IAS row · bit-7 mask · R3 richer-payload [M10]. Fold onto the WU that touches the right seam; never author while a gate is open. **Wave-2 arrival** → the unboxing runbook (MG24 UNTOUCHED; the v14-batch unit = F-1's characterization asset).

## 4. Nick's standing ledger (surface until closed)

Dependabot 1 critical (day 9+) · the repo-briefly-public security glance · install-smoke glance on `ccba69d` · deletables (2 hivemind/docs `index.lock.cleared-*` rm · core `index.lock.cleared-by-cowork-2026-07-06` rm · `rmdir skills-initiative`) · telemetry disable · website.yml adopt · LICENSE-flip · frontend.yml glance · Wave-2 watch · Node-20 (deprecation banner live on CI) · the naming thread (Asimtote — his call; no surface renames until ruled) · un-wired `adapter_type` (low).

## 5. Disciplines (carry — the compact set)

Env-model first (lock-free porcelain ALWAYS; hivemind corrupt-index ⇒ object-reads-only; agents told explicitly or they strand index.locks — it happened this arc). Truth-hierarchy/pointer-not-copy (re-derive every count/HEAD). Two-layer audit on EVERYTHING incl. your own subagents; every agent ABSENT claim gets ONE hub-side regrep. **Out-of-band-authored artifacts are UN-AUDITED RETURNS** (the NCFG pattern: verify the gap, correct the mechanism, re-issue with provenance). **A runbook VERIFY line's production TRIGGER must be grep-verified end-to-end (emitter + caller chain), not just the log line** — the PJ staging lesson generalized; it caught adoption (the mechanism-without-driver class hit THREE times this arc: PJ, NCFG, ADP — sweep for it at every runbook staging). **CI-red adjudication pattern:** untouched-module + local-green + rerun = the instrument; intermittent is claimable only with causality; the deflake debt is still recorded. Bench-iterative WUs: unit-green ≠ done-when. Diagnosis claims carry confidence labels (the ZDO-flags lesson: an overconfident mechanism anchors the next iteration wrong). Never author while a gate is open (§4b). Capture-then-execute: author v24 before depth. **The north star: one-way-door precision outranks velocity — never-false-CONFIRMED's spirit extends to never-false-ALIVE; both HELD on silicon this arc.** An evidence-backed refutation is a reward.

## 6. Success

The ADP return audited → iteration 3 closes the join arc (`device_adopted` ×2, entities live) → **THE ACCEPTANCE RUN recorded end-to-end (motion→light→honestly-confirmed on real silicon — the thesis, complete)** → the 72 h soak RUNNING with W2-VAL legs → the seed-custody WU authored on bench evidence (SD-5 gate honored) → the explainability surface rendering the honest verdicts → v24 authored before depth — the mid-Aug go/no-go approached with the moat PROVEN, adopted, and soaking.
