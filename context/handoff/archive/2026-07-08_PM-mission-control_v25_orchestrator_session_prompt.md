<!--
file: context/handoff/2026-07-08_PM-mission-control_v25_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v25) SUPERSEDING v24 (retired at: launch FULL-PASS [all 11 — Check 9 mirrors already synced] → THE 0x11 DECODE VERIFIED TRIPLE-SOURCE [TC_REJECTED_APP_KEY_REQUEST; the policy-ID mis-map — our 0x09 was TC_REJOINS_USING_WELL_KNOWN_KEY_POLICY on v13, the REAL EZSP_TC_KEY_REQUEST_POLICY=0x05 never written] → M9.4-KEY authored/dispatched/audited ACCEPT/landed 8800424 CI-GREEN → **ITERATION 5a: THE JOIN ARC CLOSED FOR REAL** [the SNZB joins → key_established TC_REQUESTER_VERIFY_KEY_SUCCESS → adopts → reports 2/3 → STAYS ≥5 min; the moat's full join→key→adopt→report→STAY pipeline silicon-proven end-to-end within a lifetime] → the §B honesty-in-the-instrument finding [0x06 benign progress mis-WARNed] → M9.4-KEYb authored/dispatched/audited ACCEPT/landed e15858d [three-way classifier; the §51 honesty-grep precondition MET] → **DUR RULED (i) EVENT-SOURCE THE REGISTRIES** [Nick's rationale VERBATIM at pm-handoff v24 beat-4 + the rationale-index row; G1 govern-like-confirmation[]; G2 bounded ~2 event types] → the completing survey BOUNDED CONFIRMED → **the registries-projection AMD authored RATIFICATION-PENDING as the v24 capstone** [docs design/amendments/2026-07-08_Registries-Projection_Amendment_PROPOSED.md] → v25 authored at beat 6). v25's charge: intake Nick's AMD RATIFICATION (fold his edits; run the ratification mechanics) → author + dispatch the DUR coding WU on his word → audit → **ITERATION 5b (identities survive restart; pin-2 re-link FINALLY silicon-proven; the view shows exactly the live entities)** → **THE ACCEPTANCE RUN** (P3.11–12 → P4 bench-hero + restart RESUME leg → P5 §51 legs → P6 reopen + PJ key OUT) → the 72 h soak → seed-custody → the explainability-lane return.
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-08 by the v24 hub at beat 6 (rotation banked BEFORE the AMD depth, per Nick's directive). Launch when v24 retires.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — LOCK-FREE porcelain ×5 [--no-optional-locks ONLY] + BOTH spine frontmatters BEFORE any spine write; ONE hub session at a time). (1) Ground: env-model FIRST (§2/§4 — the v24 arc paid ~6 phantom hits: truncated-tail diffstats showing net-DELETIONS on all-insertion changes ×2, read-lag porcelain hiding LANDED files ×4, CRLF-normalization echoes; HOST tools are truth, always) → READ `context/process/bench-troubleshooting-playbook.md` IN FULL → the cold-boot trio (working-with-nick + infrastructure-map + decision-rationale-index) → truth-hierarchy → the 11-check preflight (expect PASS; zero skill edits in the v24 arc). (2) Archive the v24 prompt → context/handoff/archive/. (3) READ IN FULL: pm-handoff v24 beats 1–6 (beat 4 = THE DUR RULING VERBATIM + guardrails — the binding governance record; beat 3 = iteration 5a; beat 6 = the AMD + close) + **THE PROPOSED AMD IN FULL** (docs `design/amendments/2026-07-08_Registries-Projection_Amendment_PROPOSED.md` — the ruling box + §§1–9) + the coder-handoff NEXT-WU pointer + the bench record iterations 1–5a + the runbook field-validated block + amended step-11. Treat every HEAD/count as a CLAIM to RE-DERIVE; source wins over this prompt.
baseline (RE-DERIVE at preflight): core **e15858d** (= 8800424 M9.4-KEY [CI GREEN, gate CLOSED] + M9.4-KEYb [Nick's check GREEN 155 pre-commit; **CI GREEN on e15858d — gate CLOSED**]) · docs **b65ed7d + the AMD-proposal commit (1 new — verify landed)** · hivemind **7c5a1b8 + the v24 beat-6 commit (exactly 3: 2 M + 1 new — verify landed)** · bench **aa45edc** · skills **0c00ece**. Watermark **AMD-98, 174/52** — ADVANCES at the AMD ratification (next number + 1 invariant mint; re-derive from the register's regeneration line, never copy). §4b: CLEAR at authoring (ALL core gates of record GREEN through e15858d).
-->

# Session Brief — PM Mission-Control v25 (AMD ratification → the DUR WU → iteration 5b → THE ACCEPTANCE RUN → soak)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v24. **The moat's full pipeline is silicon-proven end-to-end WITHIN a process lifetime** (iteration 5a: join → TCLK key established → config-adoption → measured reporting → the device STAYS) and **the instrument is honest to DEBUG granularity** (KEYb: a healthy exchange logs exactly one `key_established`, zero false failures). **The ONE remaining architecture gap is identity durability ACROSS restarts — and it is RULED (Nick, verbatim at v24 beat-4): the registries become PROJECTIONS of the event log**, governed by the proposed amendment awaiting his ratification. **The hub never implements — it intakes, audits, records, and rules WITH Nick.**

## 1. FIRST SUBSTANTIVE ACT — the AMD ratification intake

Nick rules on the proposed amendment (the ruling box is at its top; "ratify as recommended" or per-letter overrides). On his word: **run the ratification mechanics** — assign the next AMD number (rename the file to the `AMD-NN_` pattern), advance the watermark, mint the registries-projection invariant into the register (174→175; category per the ruling box; §0.3 + §17 + §18 + the regeneration line all move together — re-derive, never copy), stage the docs commit with exact counts. His EDITS fold before ratification — a contested clause is a conversation, not an improvisation. **G1 holds: NO DUR coding instruction exists until the AMD is ratified.**

## 2. The DUR coding WU (author on the ratified AMD; dispatch on his word)

Full instruction-format pass at the ratified shapes: the two event records + typed payload mirrors (full fidelity per the AMD — a STOP gate verifies every registry-record component is mapped or explicitly ruled out) · the single-apply-path + idempotent-by-identity + write-ahead pins · the registry-projection subscriber on the EXISTING replay machinery (Phase 3 CORE_DOMAIN home; the Phase-3→Phase-6 catch-up ordering invariant at `HomeSynapseCore:731` region is the boundedness proof — if the WU grounding finds no awaitable hook, ONE composition-root await is the sanctioned addition, anything wider = STOP) · `device_removed` tombstone semantics · the ArchUnit rule with production-code reach (`HomeSynapseArchRules`, app test classpath) · **the P2 lockstep set: EventTypes 71→73 · @EventType records 41→43 · codec registrations 53→55 · EventCategoryMapping +2 · the AllEventClasses manifest · SubjectType if touched — every count-pin test moves in the same change** · tests incl. a FULL-PROCESS-RESTART identity IT (construct a new core over the same dbPath → the SAME deviceIds/entityIds resolve) — RestartHonestyIT restarts only the integration; this is the missing leg. §4c Clock reminder. Bench-iterative: unit-green ≠ done-when.

## 3. ITERATION 5b (the DUR WU's own gate — the ratified bench protocol)

**ONE bench events-DB wipe** (the AMD's one-time cleanup — the 3 orphan rows die with it; **custody `data/zigbee/` PRESERVED**, the network resumes ch20/`0x9b65`) → relaunch → re-adopt ×2 under the NEW event flow (announce → propose → adopt → `device_registered` + `entity_registered` in the log) → **RESTART the app** → done-when: **the SAME deviceIds/entityIds resolve post-restart** → a Hue power-cycle re-announce takes the **RE-LINK arm (`onRejoin` fires — pin-2 silicon-proven AT LAST)** → `/api/v1/entities` shows exactly the live entities → **the KEYb piggyback check** (any fresh join: exactly one `key_established`, zero `key_establishment_failed`). Playbook §8 paste-block discipline; ⏺ RECORD into the bench record's iteration log.

## 4. THE ACCEPTANCE RUN (opens on 5b) → the soak → the queue

Resume the runbook: P3 step 11 (amended, positive-evidence-first) + step 12 ULIDs → **P4 the bench-hero automation** (the hub AUTHORS the config — entity IDs are durable now; + the restart RESUME leg) → **P5 §51 live** (CONFIRMED only via authoritative reports · identify = immediate honest UNCONFIRMED · superseded expires verdict-free · latencies vs corpus · IAS ×2 twins · Q10 · FE-7 · the optional bulb-off timeout-honesty probe) → **P6 reopen + REMOVE the PJ key**. ⏺ RECORD every leg. **The soak ENTERS on acceptance** (PJ key OUT first; NQ-6 restart spike DURING = the DUR proof; a soak restart resets the clock; W2-VAL synthetic legs chartered at entry). **Seed-custody WU** JIT (SD-5 HARD GATE rides: no non-bench network first). **The explainability-lane return** (chartered v21 — STILL OUTSTANDING) gets the two-layer audit on arrival. **Queued rows (fold onto the right seams, never author while a gate is open):** R2 SD-3-fence-reads-installed-policy (Nick-ack pending) · R3 designed-posture WARN re-rate + the SNZB degraded-fact-no-WARN covering-capability miss · the javadoc BENCH-VERIFY flips (0x0019/0x90 + TCJ + NCFG + RPT + KEY/KEYb — all silicon-proven post-5a) · ReplayTransitionIT deflake · the M9.4 polish queue · **the backlog M9.4 mega-row compaction at the next currency pass**. Wave-2 arrival → the unboxing runbook (MG24 UNTOUCHED).

## 5. Nick's standing ledger (surface until closed)

**frontend.yml on `199324e`** (he reports green — ONE confirm closes it) · the repo-briefly-public security glance · telemetry disable · website.yml adopt · LICENSE-flip · Node-20 CI deprecation · the naming thread (his call; no surface renames until ruled) · Wave-2 watch · un-wired `adapter_type` (low). ~~Dependabot~~ ~~deletables~~ CLOSED (v23 beat-7 / v24 beat-5).

## 6. Disciplines (carry — the compact set)

Env-model first (lock-free porcelain ALWAYS; host tools are truth — ~6 phantom hits in the v24 arc alone; corrupt-index ⇒ object-reads-only). The playbook governs bench work. Truth-hierarchy/pointer-not-copy (re-derive every count/HEAD/watermark). Two-layer audit on EVERYTHING incl. own subagents; quote-the-instruction-verbatim in audit prompts; labels-are-claims/quotes-are-evidence. Out-of-band artifacts are UN-AUDITED RETURNS. ONE live hub session. Bench-iterative WUs: unit-green ≠ done-when. Diagnosis claims carry confidence labels; evidence-backed refutation is a reward. Verbatim-preserve Nick's rulings (the beat-4 record is the model). Capture-then-execute: author v26 before depth. **The north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE held on silicon through five iterations, and now extend to the instrument itself.**

## 7. Success

The AMD RATIFIED (watermark advanced, the invariant minted, register regenerated) → the DUR WU landed CI-green with the ArchUnit teeth in the gate of record → **iteration 5b: identities SURVIVE restart, pin-2 re-link silicon-proven, the view shows exactly the live entities** → **THE ACCEPTANCE RUN recorded end-to-end (motion → light → honestly-confirmed over MEASURED reporting, on DURABLE identity — the thesis, complete)** → the 72 h soak RUNNING (PJ key out; NQ-6 doubles as the DUR proof) → seed-custody authored (SD-5 honored) → the explainability surface rendering honest verdicts → v26 authored before depth — the mid-Aug go/no-go approached with the moat proven, adopted, reporting, honest, and DURABLE.
