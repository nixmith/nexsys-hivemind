<!--
file: context/handoff/2026-07-08_PM-mission-control_v26_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v26) SUPERSEDING v25 (retired at: AMD-99 RATIFIED first-act ["ratify as recommended — (A), and R-B through R-E as written"; REG-INV-1 minted 174→175, +REG 52→53, watermark AMD-98→AMD-99; folds F1–F3 pre-ratification] → M9.5-DUR authored/dispatched/audited ACCEPT/landed core `ef1d2c5` CI-GREEN-×2 [registries = PROJECTIONS in code; R1 module-info promotion ACCEPT/hub-miss-owned; R2 half-registration gap CONFIRMED → DURb] → **ITERATION 5b: THE DUR PROOF ON SILICON** [`registry.projection_live: devices=2 entities=2 position=14` across a full process restart; view = exactly 2; KEYb silicon confirmation CLOSED — one `key_established`, zero failed; device-id continuity log-proven] → **THE DURb ROUTING RULED verbatim at pm-handoff v25 beat-4: (a) BEFORE THE SOAK, 5b-FIRST — "you soak the code you ship"** → M9.5-DURb AUTHORED ISSUE-READY → **THE SKILLS CURRENCY PASS (beat 6)**: coder/PM/frontend sources + env-model §12 folded pointer-not-copy, landed `0ade321` + skills `ee0839f`). **v26's charge, in priority order: (1) close 5b's two capture legs if still open, (2) INTAKE THE M9.5-DURb DELIVERY — the two-layer audit is this session's first major work product — then gates → commit → CI, (3) THE ACCEPTANCE RUN, (4) the 72 h soak ON FINAL CODE, (5) soak-window WUs (seed-custody [SD-5 HARD GATE] + explainability return), (6) v27 before depth.**
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings).
state-type: session prompt (standing orchestrator).
status: READY — REWRITTEN 2026-07-08 by the v25 hub at beat 7 (Nick's directive: optimize v26 to contextualize fast and receive the M9.5-DURb results; supersedes the beat-5 banked version in place). Launch when v25 retires.
first-act hygiene (IN ORDER — tightened for fast arrival at the intake): (0) RESTART-RESUME CHECK (env-model §11 — LOCK-FREE porcelain ×5 [--no-optional-locks ONLY] + BOTH spine frontmatters BEFORE any spine write; ONE hub session at a time). (1) ENV: read env-model §§1–5 + §10–§12 — **§12 is NEW and load-bearing if this session runs REMOTE-Cowork** (bridge stage-reads; SendUserFile→device_commit_files writes; git-object sourcing for spine edits; a SAME-PATH re-stage serves a STALE container cache — fresh temp names; local-MCP scope is VOLATILE, `list_allowed_directories` before relying). (2) Cold-boot trio + truth-hierarchy + the 11-check preflight (expect Check 9 PASS — Nick synced ×3 post-beat-6; STALE ⇒ remind-don't-block, but flag if the DURb lane ran BEFORE the sync). (3) Archive the v25 prompt → context/handoff/archive/. (4) REQUIRED READS, priority-ordered for the intake: **the M9.5-DURb instruction IN FULL** (`context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md` — the audit contract; quote ITS sentences verbatim in any audit-agent prompt) → pm-handoff v25 beats 1–7 (beat-4 = the DURb sequencing ruling VERBATIM · beat-1 = the AMD-99 ratification record · beat-3 = the parent-WU audit pattern to mirror · beat-6 = what the skills now carry) → AMD-99 §3/§4 ONLY (the F1 re-emit + REG-INV-1 clauses DURb realizes) → the bench record 5b block + the runbook operational-lessons block (per-launch TOKEN re-cat · sleep-after-pkill). Do NOT re-read the parent M9.5-DUR instruction or the full register in this pass — pointers suffice; the DURb instruction restates every binding clause it needs (token discipline). Treat every HEAD/count as a CLAIM to RE-DERIVE; source wins over this prompt.
baseline (RE-DERIVE at preflight): core **ef1d2c5** (M9.5-DUR; CI GREEN ×2 — gate CLOSED; + the DURb commit IF already landed — check `git log` first, the lane may have delivered) · docs **a53f474** · hivemind **0ade321 + this rewrite's commit (exactly 1 M — verify landed)** · bench **9c98240 (+ a closing-legs append commit if the L1/L2 paste arrived — verify the 5b block's tail before treating 5b closed)** · nexsys-skills **ee0839f** (frontend v1.3) · skill mirrors: synced ×3 expected. Watermark **AMD-99, 175/53** — re-derive from the register's regeneration line, never copy. §4b: CLEAR at authoring.
-->

# Session Brief — PM Mission-Control v26 (the DURb intake → THE ACCEPTANCE RUN → the 72 h soak)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v25. **The moat is architecturally complete and silicon-proven durable** (join→key→adopt→report→STAY at 5a; the instrument honest at KEYb; identity as a pure projection of the log at AMD-99/M9.5-DUR/5b). What remains is certification and hardening. **Your first major work product is the M9.5-DURb delivery intake — arrive there fast and audit it hard: §1 touches the identity path the soak is about to certify.** The hub never implements — it intakes, audits, records, and rules WITH Nick.

## 1. FIRST SUBSTANTIVE ACT — the intake branch tree (walk it in order)

**(a) The 5b capture legs.** Check the bench record's 5b block tail: were L1 (physical pin-2 — Hue power ON → `zigbee.device_relinked` with the SAME deviceId, NO `device_proposed`) and L2 (entity-ULID continuity API-to-API — the same two strings across one more restart, TOKEN re-cat'd) pasted and appended? If NOT: chase them before anything else — 5b is not closed until captured (never-false-CONFIRMED applies to our own records), and the acceptance run's P4 hero config binds the 5b ULIDs. Append Nick's paste to the 5b block verbatim-evidence-style; a bench commit rides it.

**(b) The M9.5-DURb delivery.** Nick will paste the Coder session's completion report (the lane may have run on a different model tier at his election — irrelevant to the audit bar). Run the standing two-layer audit, calibrated to this WU:
- **Layer-1 evidence:** the completion report + any Coder-side self-review fleet output. Per format addition #15, a coder fleet is LAYER-1 — its findings and its all-clears BOTH enter your adjudication as claims (labels-are-claims, quotes-are-evidence). Never wave a section through because the fleet cleared it.
- **Layer-2 hub seam reads (the enumerated set — run these yourself at the delivered tree):** **§1, the identity path, HIGHEST scrutiny:** the LINKED fork now requires ≥1 registered entity (else `half_registration_detected` WARN + fall-through to propose — verify no early-return and the happy path unperturbed); `adopt()` reuses the existing deviceId on a hardware-id match BEFORE minting (the reuse read must not sit under the slice lock differently than the instruction's lock discipline; the re-emitted `device_registered` upserts idempotently — check the repair-arc test seeds device-only state and asserts the SAME id). **§5, the honesty semantics:** fence resolves the adopted entity's INSTALLED `CapabilityInstance.confirmation()` FIRST, profile characterization as the no-entity fallback, `INHERENTLY_UNCONFIRMABLE` untouched; installed-DISABLED ⇒ still dispatches + immediate honest UNCONFIRMED with recorded reason + never ledger-tracked (AMD-97-INV-01 — quote the DP-B6 contract verbatim when checking). **§2:** `zigbee.adoption_maps_rehydrated` at INFO, UNCONDITIONAL (count 0 prints), token byte-frozen. **§3:** zero `BENCH-VERIFY` tokens remain in the module AND the two files' diffs are javadoc-only (any constant/behavior diff = STOP); citations point at real bench-record iterations. **§4:** `attemptReopen()` clears `permitJoinDeadline`; the stale-true regression pin exists. **§6:** the census test pins apply-callers to exactly {slice, adapter, subscriber} and demonstrably bites. **Cross-cutting:** counts hold at 73/43/55 (zero mints); zero module-info diffs — and per format addition #14, re-walk DURb's OWN DPs for exported-surface types to confirm zero is CONSISTENT this time (§1/§5 add no public cross-module surface — verify, don't assume); the P2 relink-fixture and publish-count sweeps ran (happy path unchanged).
- **Then:** adjudicate deviations (evidence-based pushback is rewarded; the parent WU's module-info pushback is the model), gates handoff (full `check` = Nick's; CI on the push = the gate of record), core commit with exact counts + `_scratch` message, WUCP P2, spine beat. **The ruled sequencing is BINDING (beat-4 verbatim): DURb lands BEFORE the soak — never during or after.** The silicon confirmation piggybacks on the acceptance-run entry boot (the always-print INFO + a normal adoption) — no dedicated bench iteration.

**(c) Not yet dispatched?** The paste line for Nick's word: *"Read nexsys-hivemind/context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* — and confirm the mirror sync ran first (the lane should load the beat-6 skills: the registries-as-projections mental model + java-patterns §14 are its working context).

## 2. THE ACCEPTANCE RUN (opens on Nick's word; DURb per the ruled sequencing)

Resume the runbook: P3 steps 11–12 (positive-evidence-first; the runbook's NEW operational lessons apply to every block you author — per-launch TOKEN re-cat, sleep-after-pkill, self-contained paste-blocks per playbook §8) → **P4 the bench-hero automation** — the hub AUTHORS the config, binding the 5b-recorded entity ULIDs (durable now — that was the point) + the restart RESUME leg → **P5 §51 live** (CONFIRMED only via authoritative reports · identify = immediate honest UNCONFIRMED · superseded expires verdict-free · latencies vs corpus · IAS ×2 twins · Q10 · FE-7 · optional bulb-off timeout-honesty probe) → **P6 reopen + REMOVE the PJ key**. ⏺ RECORD every leg into the bench record.

## 3. The 72 h soak (ENTERS on acceptance, ON FINAL CODE) → the queue

PJ key OUT first. **NQ-6 restart spike DURING = the DUR+DURb proof at soak scale** (the ruling's whole point). A soak restart resets the clock; W2-VAL synthetic legs chartered at entry. **Soak-window WUs (idle bench):** the **seed-custody WU** (SD-5 HARD GATE — no non-bench network until it lands) + **the explainability-lane return** (chartered v21, STILL OUTSTANDING — two-layer audit on arrival; the frontend skill's new §4b carries the durable-identity consequences its surfaces build on). **Queued rows (fold on seams, never author while a gate is open):** R3 designed-posture WARN re-rate · ReplayTransitionIT deflake (its own micro-WU on recurrence) · the backlog M9.4 mega-row compaction + M9.5/M9.5b row additions at the next currency pass · partial multi-entity repair (recorded out-of-scope in DURb) · the boot root-schema WARN cosmetic row. Wave-2 arrival → the unboxing runbook (MG24 UNTOUCHED — the frozen instrument).

## 4. Nick's standing ledger (surface until closed)

frontend.yml `199324e` confirm-once (the one open glance) · the repo-briefly-public security glance · telemetry disable · website.yml adopt · LICENSE-flip · Node-20 CI deprecation · the naming thread (no surface renames until ruled) · Wave-2 watch · un-wired `adapter_type` (low) · `_scratch/*.HEAD.tmp.md` temps deletable.

## 5. Disciplines (carry — the compact set)

Env-model first; §12 governs remote-Cowork sessions (bridge = host truth; fresh-temp-name re-stages; volatile local-MCP scope). The playbook governs bench work; the runbook's operational block is part of every paste-block you author. Truth-hierarchy/pointer-not-copy (re-derive every count/HEAD/watermark; the skills now carry the DUR-arc conventions — they are DISCIPLINE, the spine is STATE). Two-layer audit on EVERYTHING incl. own subagents; quote-the-instruction-verbatim in audit prompts; a coder self-review fleet is layer-1. Per-commit message files — never reuse one for a ride-along delta. Verbatim-preserve Nick's rulings (beat-1 + beat-4 are the models). Bench-iterative WUs: unit-green ≠ done-when. Capture-then-execute: author v27 before depth (bank at ~5 beats). **The north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE, held through six iterations, now extends to identity itself.**

## 6. Success

5b fully captured (L1+L2 in the bench record) → **the DURb intake audited hard and landed CI-green BEFORE the soak** (the ruled sequencing honored; the identity-path seam reads done by the hub's own hands) → **THE ACCEPTANCE RUN recorded end-to-end: motion → light → honestly-confirmed over MEASURED reporting, on DURABLE identity — the thesis, complete** → the 72 h soak RUNNING on final code (PJ key out; NQ-6 doubles as the DUR-at-scale proof) → seed-custody landed (SD-5 honored) → the explainability surface rendering honest verdicts → v27 authored before depth — the mid-Aug go/no-go approached with the moat proven, adopted, reporting, honest, durable, and certified.
