<!--
file: context/handoff/2026-07-10_PM-mission-control_v27_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v27) SUPERSEDING v26 (retired at: M9.5-DURb INTAKE ACCEPT/landed core `04f5f70` CI-green → THE ACCEPTANCE RUN run overnight 2026-07-09/10 [§51 core legs PASS · the 5b legs L1/L2 CLOSED superior · the ch22 foreign-network incident SURVIVED, writer unattributed · the restore-path (`network_restored_from_parameters`) FIRST silicon rep · identity held across THREE networks · ZERO false CONFIRMs ~25 verdicts · ONE CONFIRMED DEFECT: the reopen-target locator blind to re-enumeration] → **THE 72 h SOAK ENTERED 2026-07-10 07:50:35 Pi-local on `04f5f70`** (boot `bench-2026-07-10-075035.log`, network ch20/0x2d83, permit key OUT, ends ≥ 2026-07-13 ~07:50) → the retrospective + this prompt banked at beat 3). **v27's charge, in priority order: (1) SOAK STEWARDSHIP (watch, NQ-6 ruling+scheduling, exit intake — the certification completes here), (2) THE DEEP RETROSPECTIVE SESSION Nick chartered (intake `context/assessments/2026-07-10_acceptance-arc-retrospective_and_direction.md` §§1–4 as the seed, then go DEEPER: adjudicate every row, take the design rulings to Nick, produce the ruled post-soak priority stack), (3) THE REPO/BENCH HYGIENE SWEEP (the assessment §5 inventory — backlog compaction, handoff rotations, cross-agent archival, _scratch archaeology, log-retention policy), (4) AUTHOR the top ruled WUs issue-ready (reopen-locator micro-WU + availability-ALIVE + the seed-custody WU [SD-5 HARD GATE] + the explainability-return dispatch), (5) v28 before depth.**
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-10 by the v26 hub at beat 3, on Nick's directive (deep retrospective + repo cleanup + strategy in a fresh session). Launch when v26 retires (on this prompt's first act, or Nick's word).
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — lock-free porcelain ×5 [`--no-optional-locks` ONLY] + BOTH spine frontmatters BEFORE any spine write; ONE hub session at a time). (1) ENV: env-model §§1–5 + §10–§12 (§12 governs if remote-Cowork: bridge stage-reads are host truth; SendUserFile→device_commit_files writes; git-object sourcing; SAME-PATH re-stage = stale cache; device_bash file-reads adjudicate NOTHING recent — the beat-1 phantom lesson). (2) Cold-boot trio + truth-hierarchy + the 11-check preflight (expect PASS; Check 9 mirrors were ≡ at v26 launch and no skill-source edits landed since). (3) Archive the v26 prompt → context/handoff/archive/. (4) REQUIRED READS, priority-ordered: **the retrospective assessment IN FULL** (`context/assessments/2026-07-10_acceptance-arc-retrospective_and_direction.md` — the session's seed; the v26 conversation's knowledge is captured THERE and in the bench record's acceptance-run block, nowhere else) → pm-handoff v26 beats 1–3 (beat 2 = the acceptance-run record pointer; beat 1 = the DURb audit pattern) → the bench record's acceptance-run block (bench `a81d678` — the evidence layer; quote IT, not memory) → the runbook's operational-lessons block (now +6, incl. `tools/bench.sh` — ALL bench ops go through it) → the M9.5-DURb instruction (pointer only — landed and closed). Treat every HEAD/count as a CLAIM to re-derive.
baseline (RE-DERIVE at preflight): core **04f5f70** (M9.5-DURb; CI GREEN both workflows — gate CLOSED) · docs **a53f474** · hivemind **54e0a84 + the beat-3 commit (exactly 4: 2 M + 2 new — verify landed)** · bench **a81d678** · nexsys-skills **ee0839f** · mirrors ≡ (re-verify). Watermark **AMD-99, 175/53** — re-derive at the register. §4b: CLEAR (no deferred gates open; CI green on `04f5f70` confirmed pre-bench).
KNOWN LEDGER DELTAS the preflight will hit (pre-adjudicated, do not re-derive as findings): hivemind `312f0dc` swept 7 files against a "stages exactly 6" message (the v26-prompt M rode along; the targeted 1-M commit never ran; its msg file `_scratch/2026-07-08_hivemind_v26-rewrite_commit-msg.txt` is an ORPHAN — delete in the hygiene sweep; reconciliation of record = v26 beat 3) · the two `_scratch/*.HEAD.tmp.md` temps remain deletable · the Pi carries retired custody files + ~15 bench logs (the hygiene sweep's §5 list governs).
-->

# Session Brief — PM Mission-Control v27 (soak stewardship → THE DEEP RETROSPECTIVE → hygiene → the ruled next arc)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v26. **The moat is certified through the acceptance run and is SOAKING on final code** — the 72 h clock runs to ≥ 2026-07-13 ~07:50 Pi-local. Nick has chartered this session for depth over motion: digest what the acceptance arc taught (the retrospective is the seed — go deeper), clean the workshop, and rule the next arc's priorities WITH him. The hub never implements — it intakes, audits, records, rules with Nick, and authors instructions.

## 1. SOAK STEWARDSHIP (continuous; interrupts everything else)

The soak is sacred: **no restarts, no config edits, no joins** (permit key is OUT). Nick pastes `~/bench.sh health`/`status` glances on request — watch for: `network_parameter_mismatch` or ANY foreign-network sign (= the ch22 writer RETURNING — live-threat escalation, STOP-and-rule) · recurrence of the 0xFFFF `key_establishment_failed` timeout (once = dangle; twice = investigation row goes active) · `transport_failed`/reopen events (the locator defect means a bumped dongle STAYS DEAD until a ruled restart — that is a soak-clock ruling, not an improvisation) · verdict-stream health (bench-hero fires on motion organically; spot-check `bench.sh events` for continued zero-false-CONFIRM). **NQ-6 needs ONE ruling from Nick at scheduling time:** the charter says the restart spike runs DURING the soak (the DUR+DURb proof at scale) but the standing rule says a restart resets the clock — surface the tension verbatim, offer options (a: spike at ~T+60 h and accept a reset to a fresh final 72 h · b: spike as the soak's EXIT act · c: separate post-soak spike), REC (b) as the cleanest. At soak exit: the close-out intake (full-log sweep greps, uptime/verdict/twin counts, the exit block into the bench record + commit).

## 2. THE DEEP RETROSPECTIVE (Nick's charter — the session's core work)

Intake `context/assessments/2026-07-10_acceptance-arc-retrospective_and_direction.md` IN FULL, then go deeper than it could: **(a)** adjudicate every §2 pain point into DISPOSITION (row / WU / design-ruling / lesson-only) — with source reads where the assessment flagged them (the command-thread serialization read [`integration-cmd-zigbee-0` head-of-line question] · the availability-tracker ALIVE wiring · the PortIdentity matching root-cause for the reopen defect); **(b)** take the DESIGN RULINGS to Nick, one-turn-rulable each: the no-change verdict vocabulary (already-satisfied commands — vocabulary not semantics, never CONFIRMED without evidence; Doc 08/AMD conversation) · the supersession-payload bench-decrypt question (observability vs at-rest encryption) · the NQ-6 tension (§1) · the `hs-radio` first-party tooling charter (info/leave/custody-restore on OUR EZSP stack) · whether the restore path gains frame-counter custody; **(c)** produce THE RULED PRIORITY STACK for the next arc from the assessment's §4 REC (soak exit → seed-custody [SD-5 HARD GATE — unchanged, before ANY non-bench network] → the explainability/observability push [elevated: tonight proved verdicts-in-SQLite is not a product] → field-reality hardening [reopen locator · hs-radio · frame counters · udev RAMP] → the design rulings → deferred queue) — Nick rules the order, the hub records it verbatim; **(d)** mine the strengths (§3) into positioning notes for the website-content lane (the identity-cannot-be-hijacked claim is now evidence-backed) and into the skills at the next currency pass (pointer-not-copy).

## 3. THE HYGIENE SWEEP (the assessment §5 inventory, executed)

hivemind: backlog compaction (M9.4 mega-row + M9.5/M9.5b + the new rows: reopen-locator · availability-ALIVE · ch22 investigation · 0xFFFF watch · no-change-verdict ruling · hs-radio · frame-counters · on_off direct leg · supersession-payload · bench.sh→hsctl evolution) · cross-agent: mark the DUR + DURb coder notes SERVED → archive · coder-handoff.md rotation (288 KB — the env-model §8 tax rule) · handoff-dir archive sweep · `_scratch` archaeology (served msgs → `_archive-commit-msgs/`; delete the two `.HEAD.tmp.md` temps + the orphan v26-rewrite msg). bench: the log-retention policy (copy-off + prune recipe for the Pi's bench-*.log accumulation) · a `tools/` README stub if warranted. Pi (via Nick): KEEP the retired custody files + the zbtool venv (documented tooling); delete `~/hs-bench/bench-hero.yaml` (staging copy). Every commit per-message-file with exact counts — and state the SWEEP-GUARD in each Nick-order (the `312f0dc` lesson: name what `add -A` will sweep, or use targeted adds).

## 4. AUTHOR THE RULED WUs (issue-ready, dispatch on the word)

On the §2(c) ruling, author in ruled order — expected top of stack: **the seed-custody WU** (SD-5 HARD GATE; unchanged charter) · **M9.6-RO the reopen-locator micro-WU** (grounding read first: PortLocator/PortIdentity matching vs re-enumeration; test = simulated devnum-change re-enumeration; the field scenario IS the test) · **the availability-ALIVE fix** (tracker wiring read first; never-false-ALIVE preserved — reports = evidence) · **the explainability-return dispatch** (the FE lane consumes REAL verdicts; the frontend skill v1.3 §4b carries the durable-identity consequences). Full format discipline: STOP gates grounded at `04f5f70`, MODULE_CONTEXTs + verbatim module-infos, P2 consumer/pin surveys, §4c Clock reminder, tests-before-implementation, done-when with the bench-iterative clause where silicon is owed.

## 5. Nick's standing ledger (surface until closed)

frontend.yml `199324e` confirm-once · the repo-briefly-public security glance · telemetry disable · website.yml adopt · LICENSE-flip · Node-20 CI deprecation · the naming thread · Wave-2 watch · un-wired `adapter_type` (low) · the §3 hygiene items above.

## 6. Disciplines (carry) + Success

Env-model first (§12 if remote; the bridge is host truth — the beat-1 phantom is the standing example). The playbook + runbook govern bench work — **all operator interaction through `tools/bench.sh`; physical acts name their observable success signal in advance; journalctl is a first-class instrument; no mid-run reboots unless ruled.** Two-layer audit on everything including own subagents; labels-are-claims, quotes-are-evidence; instructions quoted verbatim in audit prompts. Truth-hierarchy/pointer-not-copy: re-derive every count/HEAD/watermark. Per-commit message files + sweep-guards. Verbatim-preserve Nick's rulings. Capture-then-execute: **v28 authored before depth** (bank at ~5 beats). **The north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE, held through seven iterations and one hostile night, extends to identity itself.**

**Success:** the soak completes and is CLOSED-OUT on the record (NQ-6 ruled and run; zero false verdicts across 72 h) → the retrospective adjudicated to a RULED priority stack (every pain point dispositioned; the design rulings taken) → the repos and bench are CLEAN (compacted backlog, rotated handoffs, archived notes, a log policy) → the top WUs authored issue-ready and the first dispatched on Nick's word → v28 banked — the mid-Aug go/no-go approached with the moat proven, certified, SOAKED, and the next arc chosen deliberately.
