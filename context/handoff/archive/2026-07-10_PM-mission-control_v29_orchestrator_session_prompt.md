<!--
file: context/handoff/2026-07-10_PM-mission-control_v29_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v29) SUPERSEDING v28 (retired at extreme context depth after: launch PASS-11 · the returns intake (a)→(b)→(c) ALL ACCEPT-WITH-CORRECTIONS + probe-blocks ×3 delivered + ESC-W2-SNZB01P-01 packaged · the Track-A execution arc landed ×3 all CI GREEN — M9.6-SEED `a1f0f77` full-bar ACCEPT · the CI-red flake diagnosed at source → M9.5-DURc `7e4426b` · M9.6-RO `1627978` two-layer ACCEPT · the R1 record discrepancy corrected append-only via the R1 micro-commit · M9.6-AVAIL DISPATCHED). **v29's charge, in priority order: (1) THE LANE DEEP-DIVES — Nick provides the three lane-session responses IN THIS CONVERSATION; run the probe-delta re-audits in the ruled order (a)→(b)→(c) against the AMENDED on-disk returns (the correction lists of record = pm-handoff v28 beat 1, per lane; every fix verified applied, nothing new smuggled), then order ONE amendment commit for the four returns, then package the RULINGS QUEUE one-turn-rulable: the (b) sequence + M14 timing [PM REC: conditional pre-gate w/ the ~Jul-31 slide trigger] + the Tier-2 proof-scenario rewording · ESC-W2-SNZB01P-01 [PM REC: (B) honest fallback + named AMD row] · the criteria draft's Q1–Q4 · the naming ratification (+ Nick's three sub-minute checks: asimtote.com registrar · havenkor.com NXDOMAIN · the zoe-codez/synapse repo eyeball). (2) M9.6-AVAIL INTAKE + two-layer audit on the Coder's return (the no-open-[REVIEW] rail; never-false-ALIVE in both directions; the M-1 empty-map trap and the ping-arm obligations are ruled verbatim in the instruction). (3) MONDAY — Nick's pinned sequence VERBATIM, unchanged law: the NQ-6 exit-act spike on the SOAKED `04f5f70` BEFORE any git pull or deploy (runbook Phase 7; the certification's final evidence completes on the code that soaked) → close-out intake → certification CLOSED on the record → pull/build/deploy the landed WUs (a1f0f77/7e4426b/1627978/R1 + AVAIL if landed) → their silicon legs (SEED fresh-formation composing with Wave-2 joins, Nick rules timing · RO in BOTH topologies once the Rosonway arrives, dongle-behind-hub = its own single-variable step with a boot-health re-run · AVAIL SNZB-02P reports→AVAILABLE + a genuine offline leg) → the B1 runner dispatch → Wave-2 joins on the fresh code. (4) B1 runner WU authoring + the explainability-lane dispatch (Tuesday-tolerant per the rail). (5) HYGIENE FOLDS (opening-act class, enumerated): verify the R1 micro-commit landed + its correction message (the record must read true); pm-lessons +2 (staged-messages-assert-only-confirmed-state · correction-blocks-carry-an-AUDIT-CORRECTION-heading); env-model §12 addendum (the local Filesystem MCP [scope homesynapse-core] is the VERIFIED host-truth read route for recently-edited core files; VM worktree git reads RETIRED for core — they stranded index.lock AND served a §2 truncated-tail phantom diff this arc); PM SKILL.md masthead one-liner (the same two rules, pointer-not-copy — leaves Check 9 STALE until Nick syncs); mark the DURc cross-agent note SERVED (both [REVIEW]s adjudicated v28 beat 2); _scratch sweep (the orphaned solo beat-1 msg + served msg files → _archive-commit-msgs/); minor citation notes (the (a) lane contract's §4 numbering imprecision; the charter's "ReplayTransitionIT deflake precedent" names a pattern, not a commit). (6) The skills-constellation DESIGN doc if the window allows (hub = single router; skills never call skills sideways; per-skill eval plan on benchmarks/; design only). (7) v30 banked before depth.**
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-10 by the v28 hub at beat 2 (Nick's context-depth call). Launch on Nick's word.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — lock-free porcelain ×5 [`--no-optional-locks` ONLY] + BOTH spine frontmatters BEFORE any spine write; ONE hub session at a time; v28 retires on this launch). (1) ENV: env-model §§1–5 + §10–§12 — §12 governs (remote Cowork), PLUS the v28-verified extensions not yet folded into the doc: bridge stage-reads = host truth for hivemind/bench/docs; the local Filesystem MCP (list_allowed_directories first — scope was homesynapse-core) = host truth for recently-edited core files; NEVER read recently-edited files through device_bash cat/worktree-git (the §2 truncated-tail phantom served a false 570-deletion diff this arc and a worktree git export stranded core's index.lock); `git show HEAD:<path>` via device_bash is safe (object store); SendUserFile→device_commit_files = the write path (mtime guards); the stage endpoint 400-rejects `.java` by extension. (2) Cold-boot trio + truth-hierarchy + the 11-check preflight (expect PASS; Check 9 mirrors were ≡ at v28 launch; NO skill-source edits landed in v28 — verify; the §5 masthead fold will make it STALE mid-session by design). (3) Archive the v28 prompt → context/handoff/archive/. (4) REQUIRED READS, priority-ordered: pm-handoff v28 beats 1–2 (the audit verdicts + correction lists per lane + the rulings queue + the record-discrepancy adjudication — the working law) → the FOUR AMENDED lane returns (context/assessments/2026-07-11_{wave2-device-dossiers,integration-roadmap}_research-return.md · 2026-07-11_{go-no-go-criteria_draft,naming-decision-brief}.md) + their three lane prompts (context/handoff/2026-07-10_*_lane_session_prompt.md) → the M9.6-AVAIL instruction (its audit is yours) → the bench runbook Phase 7 + step 26 (Monday's law) → the charter + SCENARIO_FORMAT (B1's spec). Treat every HEAD/count as a CLAIM to re-derive.
baseline (RE-DERIVE at preflight — v28-beat-2-time claims): core `1627978` + the R1 micro-commit (verify landed; suite 452) + M9.6-AVAIL possibly landed/building (CI green each — verify) · docs `a53f474` · hivemind `f9c1ca8` + the consolidated beats-1+2 commit (verify: 9 add paths / the archive R; THE FOUR LANE RETURNS MUST STILL BE UNCOMMITTED — they commit only after YOUR delta re-audits) · bench `e48f04d` · nexsys-skills `ee0839f` · mirrors ≡ (re-verify) · watermark **AMD-99, 175/53** (re-derive at the register §0.3). THE SOAK: entered 2026-07-10 07:50:35 Pi-local on `04f5f70`; exits ≥ 2026-07-13 ~07:50; Phase 7 = the exit act; a bumped dongle STAYS DEAD until a ruled restart (M9.6-RO is landed but NOT deployed — the soak binary predates it). Standing ledger (surface until closed): frontend.yml `199324e` confirm-once · security glance · telemetry · website.yml · LICENSE (consumes the (b) ruling) · Node-20 · naming (the (c) ruling) · HW arrival (2× Shelly + Rosonway) · the explainability-lane return owed · Pi hygiene at soak exit · LC-LABEL-LOG row · the FRAME-CTR riders (setAll ctor-seam teeth · the DP-7 corruption bound) · the dual-role-plug constraint (M14 DUTs ≠ B1/B2 stimulus actuators).
-->

# Session Brief — PM Mission-Control v29 (lane deep-dives → AVAIL intake → Monday execution)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v28. The weekend's Track-A code is LANDED (SEED/DURc/RO all CI-green; AVAIL in flight); the four Track-B lane returns sit AMENDED on disk awaiting your delta re-audits; the rulings queue is packaged and waits on Nick. **The hub never implements** — it intakes, audits (two-layer; labels-are-claims, quotes-are-evidence; agent returns get layer-2 like any lane), records, rules with Nick, and authors instructions. Disciplines carry unchanged from the v28 record: env-model first · all bench ops through `tools/bench.sh` · per-commit message files that assert ONLY confirmed state, with sweep-guards re-derived from a FRESH porcelain at order time · verbatim-preserve Nick's rulings · capture-then-execute (v30 banked before depth) · **the north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE, extended to identity, to the instrument, and to the RECORD itself (this arc's lesson: a commit message is a record; it asserts only what is).**

**Success:** the four returns re-audited + amendment-committed → the rulings queue ruled by Nick → AVAIL audited (Track A complete, code-side) → Monday executed in the pinned order with the certification CLOSED on the record → the silicon legs green → B1 dispatched → the hygiene folds zeroed → the skills-constellation design delivered or explicitly v30-first → v30 banked — the mid-Aug go/no-go approached with the moat certified, the fleet growing, and the next integration chosen deliberately.
