<!--
file: context/handoff/2026-07-12_PM-mission-control_v30_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v30) SUPERSEDING v29 (retired at depth after: launch FULL-PASS all 11 [the PortLocatorTest phantom adjudicated byte-identical via the Filesystem-MCP route] · the lane deep-dives CLOSED — delta re-audits ACCEPT ×4, nothing smuggled → the amendment commit `ada8b46` · M9.6-AVAIL two-layer ACCEPT [the adoption-time-seed [REVIEW] RATIFIED-ACCEPT, hub-owned] → core `1aa809d` CI GREEN — **TRACK A CODE-COMPLETE** (SEED a1f0f77 · DURc 7e4426b · RO 1627978+3590fca · AVAIL 1aa809d, all CI-green; silicon legs = Monday) · R4 ruled IN PART (company Asimtote AFFIRMED; product name OPEN → a dedicated brand session) · the connectivity doctrine + the V1.x feedback & maturity charter row recorded · hygiene folds executed [pm-lessons +2 · env-model §12 addendum · SKILL.md masthead (Check 9 STALE pending Nick's sync) · _scratch sweep ×12 · v28 prompt archived] · backlog rows flipped). **v30's charge, in priority order: (1) B1 RUNNER WU AUTHORING — pre-Monday if the window allows (charter §2 B1 + SCENARIO_FORMAT v0: bench.sh grows `scenario <name>` · `suite <list>` · `bundle <run-id>`; the three seed scenarios boot-health/command-confirm/timeout-honesty-no-change; PLUS the usb-reenumeration scenario — the RULED coupling: B1 is M9.6-RO's regression harness and delivers that scenario BEFORE RO's bench verification; stimulus-independence; API-first assertions; the anti-vacuous rule; soak sanctity — B1 lands on the Pi only AFTER the exit act + close-out). (2) MONDAY EXECUTION — Nick's pinned sequence VERBATIM, unchanged law: soak exits ≥ 2026-07-13 ~07:50 Pi-local; the NQ-6 exit-act spike ×3 on the SOAKED `04f5f70` BEFORE any git pull or deploy (runbook Phase 7 steps 20–26; anti-actions hold; a bumped dongle STAYS DEAD until a ruled restart) → close-out intake → THE CERTIFICATION CLOSED on the record (A1/A2/A3 evidence per the criteria draft; the zero-false-CONFIRM sweep; RSS/log deltas against the A1 bounds) → pull/build/deploy `1aa809d` → the silicon legs (SEED fresh-formation under the generated seed composing with the Wave-2 joins — Nick rules timing; the SD-5 HARD GATE discharges on it · RO in BOTH topologies once the Rosonway arrives — dongle-behind-hub is its own single-variable step with a boot-health re-run first; the R-row closes on B1's usb-reenumeration + both topologies · AVAIL: SNZB-02P reports→AVAILABLE in /api/v1/entities + a genuine offline leg — never-false-ALIVE both directions) → the B1 dispatch → Wave-2 joins on the fresh code (the (a) dossiers are the profile/scenario source; per-line `within:` timeouts minted at scenario authoring; the S31 adopts via exact_model; the 04P ships capture-led 0xFC57 with the §6.3-g conflict adjudicated by OUR interview read). (3) THE EXPLAINABILITY-LANE DISPATCH (Tuesday-tolerant; G1 is the gate's longest pole): refresh `context/handoff/2026-07-06_explainability-ux-lane_session_prompt.md` against current state (the verdict vocabulary is LIVE; AVAIL landed — the FE-7 availability tile's data path exists pending the silicon leg; the FROZEN v1.1 read-API; the no-change ruling: the explanation NEVER upgrades or replaces a verdict) and dispatch on Nick's word. (4) THE BRAND-SESSION PROMPT — author the dedicated brand-identity session's dispatch brief: company = Asimtote FIXED; the PRODUCT name is the deliverable (Nick brings his own candidates as FIRST-CLASS inputs — adversarially screen them with the same evidence bar as the naming brief: fresh domain/USPTO-knockout/collision screens, radio test, thesis fit, enterprise carry, UI/UX complement); inputs = the amended naming brief + 2026-07-05 naming/candidates strategy docs + the positioning notes + the frontend design tokens/W-11 + the connectivity doctrine + the enterprise ambition; nexsys-frontend skill loaded; deliverable ONE-TURN-RULABLE; decide-by tied to the website-content lane (~2–3 weeks); NOTE: attorney clearance on the COMPANY mark + `asimtote` namespace claims proceed NOW, decoupled (Nick's action list). (5) RULINGS INTAKE — R1 (the (b) sequence + M14 conditional-pre-gate w/ the slide trigger [the ruling text becomes the canonical wording — the return says ~Aug 1, the REC said ~Jul 31; reconcile in the ruling] + the Tier-2 proof-scenario rewording) · R2 (ESC-W2-SNZB01P-01 — PM REC (B) honest fallback + the named AMD row) · R3 (criteria Q1–Q4 + ratification + name the A4/H3 carriers; on ratification the hub RE-STATUSES EVERY ROW per the draft's own §5 and freezes J1's process) — record every ruling VERBATIM. (6) HYGIENE FIRST-ACTS (small, enumerated): the cross-agent SERVED restamps ×3 (DURc [adjudicated v28 beat 2] · the M9.6-RO delivery note [intaken v28 beat 2] · the M9.6-AVAIL delivery note [adjudicated v29 beat 1]) + the ACTIVE-count restamp — NOTE the v29 lesson: the VM mount served a 4-hour-stale cross-agent view (139,523 B vs the host's 144,112 B); size-verify any device-side copy against a fresh host `device_list_dir` BEFORE editing, or defer again; verify Nick's mirror sync landed (Check 9 — the PM SKILL.md edit awaits it); the AVAIL WUCP-P2 residuals close with these. (7) The V1.x feedback & maturity charter + the skills-constellation design = named rows, explicitly post-gate/v31-class unless Nick pulls them forward. (8) v31 banked before depth.**
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-12 by the v29 hub at beat 1. Launch on Nick's word.
first-act hygiene (IN ORDER): (0) RESTART-RESUME CHECK (env-model §11 — lock-free porcelain ×5 [`--no-optional-locks` ONLY] + BOTH spine frontmatters BEFORE any spine write; ONE hub session at a time; v29 retires on this launch). (1) ENV: env-model §§1–5 + §10–§12 — §12 governs (remote Cowork) and NOW CARRIES the v29 fold: the local Filesystem MCP (scope homesynapse-core — `list_allowed_directories` first, scope VOLATILE) = the VERIFIED host-truth read route for recently-edited core files; VM worktree git reads RETIRED for core; `git show HEAD:<path>` via device_bash safe (object store); bridge stage-reads = host truth for hivemind/bench/docs BUT a same-path re-stage serves a STALE container cache AND a VM-mount `cp` of a recently-edited file can serve a stale view — SIZE-VERIFY every device-side copy against a fresh host listing before trusting it; SendUserFile→device_commit_files = the write path (mtime guards); the stage endpoint 400-rejects `.java` (export via `git show` to `.txt`). (2) Cold-boot trio + truth-hierarchy + the 11-check preflight (Check 9 may read STALE if Nick's mirror sync hasn't run — expected, names the PM SKILL.md masthead edit; everything else expect PASS). (3) Archive the v29 prompt → context/handoff/archive/. (4) REQUIRED READS, priority-ordered: pm-handoff v29 beat 1 + v28 beats 1–2 (the working law: the audit verdicts · the rulings queue R1–R3 · the R4 partial ruling · the connectivity doctrine) → the bench-automation charter + SCENARIO_FORMAT v0 (B1's spec) → the bench runbook Phase 7 steps 20–26 (Monday's law) → the (a) wave2 dossiers return (Monday's profile/scenario source; committed at `ada8b46`) → the criteria draft (the evidence scoreboard; re-status at ratification) → the M9.6-AVAIL instruction + coder-handoff newest entry (the silicon-leg contracts). Treat every HEAD/count as a CLAIM to re-derive.
baseline (RE-DERIVE at preflight — v29-beat-1-time claims): core `1aa809d` (suite 466; CI GREEN both workflows — verify) · docs `a53f474` · hivemind = the v29 beat-1 commit (verify landed; `ada8b46` its parent) · bench `e48f04d` · skills `ee0839f` · mirrors: Check 9 pending Nick's sync (the SKILL.md masthead edit) · watermark **AMD-99, 175/53** (re-derive at the register §0.3). THE SOAK: entered 2026-07-10 07:50:35 Pi-local on `04f5f70`; exits ≥ 2026-07-13 ~07:50; Phase 7 = the exit act; NOTHING deploys before the certification closes. Standing ledger (surface until closed): frontend.yml `199324e` confirm-once · security glance · telemetry (now doctrine-shaped — the v29 connectivity doctrine) · website.yml · LICENSE (consumes the R1 ruling) · Node-20 · the PRODUCT name (the brand session; company mark actions decoupled and NOW) · HW arrival (2× Shelly + Rosonway) · the explainability-lane return owed (dispatch = v30 charge #3) · Pi hygiene at soak exit · LC-LABEL-LOG row · the FRAME-CTR riders (setAll ctor-seam teeth · the DP-7 corruption bound) · the dual-role-plug constraint (M14 DUTs ≠ B1/B2 stimulus actuators) · the (b) NOTEs (growth-band span gloss; the slide-trigger wording — the ruling text is canonical) · the V1.x feedback & maturity charter row (post-gate).
-->

# Session Brief — PM Mission-Control v30 (B1 authoring → Monday execution → the dispatches → the rulings)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v29. Track A is CODE-COMPLETE (four WUs, all CI-green — the silicon legs are Monday's); the four Track-B returns are audited, amended, committed (`ada8b46`), and the rulings queue R1–R3 waits on Nick (R4 ruled in part: company Asimtote affirmed; the product name goes to a dedicated brand session whose prompt YOU author). **The hub never implements** — it intakes, audits (two-layer; labels-are-claims, quotes-are-evidence; agent returns get layer-2 like any lane), records, rules with Nick, and authors instructions. Disciplines carry unchanged from the v28–v29 record: env-model first · all bench ops through `tools/bench.sh` · per-commit message files asserting ONLY confirmed state, sweep-guards re-derived from a FRESH porcelain at order time, correction blocks HEADED as corrections · verbatim-preserve Nick's rulings · capture-then-execute (v31 banked before depth) · **the north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE, extended to identity, the instrument, the RECORD, and the data flow (the egress doctrine).**

**Success:** B1 authored issue-ready (pre-Monday) → Monday executed in the pinned order with THE CERTIFICATION CLOSED on the record → `1aa809d` deployed → the silicon legs green (SEED formation = the SD-5 discharge · AVAIL both directions · RO pending topology hardware) → the B1 dispatch + Wave-2 joins on fresh code → the explainability dispatch out → the brand-session prompt delivered → R1–R3 ruled and recorded verbatim → the hygiene residuals zeroed → v31 banked — the mid-Aug gate approached with the moat certified at soak scale, the fleet at five classes, and every remaining pole (G1, the N-envelope) on an owned, dated path.
