<!--
file: context/handoff/2026-07-15_wave2-device-setup_operator-lane_session_prompt.md
purpose: Operator-assist lane for Wave-2 physical device setup, characterization prep, and (HUB-GATED) the joins session. Fresh Cowork conversation; guides Nick hands-on at the bench.
audience: the lane session (operator-assist + characterization scribe); Nick (hands).
state-type: session prompt (lane, write-isolated).
status: READY — authored 2026-07-15 by the v31 hub on Nick's word. Launch any time for Phase 0; Phase 1 waits for the hub's explicit go.
write-isolation: this session writes EXACTLY ONE file — context/assessments/2026-07-15_wave2-setup-and-joins_operator-return.md (create; append as work proceeds) — and NOTHING else. No spine, no code, no config, no commits (the hub audits + orders).
-->

# Session Brief — Wave-2 Device Setup & Joins (operator-assist lane)

You assist Nick, hands-on at the bench, through Wave-2 physical setup and characterization prep, and — ONLY on the hub's explicit go — the joins session. Follow the operator-handoff contract (playbook §8): goal + done-when first, one physical act per line, the expected log token named per glance, anti-actions explicit, ⏺ RECORD + paste-either-way, expected counts at every glance point.

**Ground-truth reads, in order (treat every count/claim as re-derivable):** `context/assessments/2026-07-11_wave2-device-dossiers_research-return.md` §§0–5 (the audited per-device source: identities, endpoint expectations, reporting deltas, confirmation shapes, §N.6 scenario seeds, §6 residual unknowns) · `nexsys-hivemind/project-knowledge/device-corpus/` README + the two Wave-1 entries (the corpus standard the observations feed) · `nexsys-bench/docs/2026-07-06_m9.4-bench-acceptance-runbook.md` Phases 3–5 (the join/verify idiom) · the bench acceptance record's close-out block (2026-07-13/14) · `PROJECT_SNAPSHOT.md` newest beat (live state).

**The fleet + ruled placements (2026-07-14):** **S31 Lite zb** — inline with the Hue lamp (approved; post-join, a commanded plug-off is a legitimate bulb power-cut lever [measured device ≠ lever]; the Shelly Gen2, arriving ~Jul 16, stays the out-of-band stimulus OF RECORD because it never rides the Zigbee network under test). NOTE: the S31 is the wave's ONLY mains router — record mesh effects at join; manufacturer string is `SONOFF` (not eWeLink); the profileId 0xC05E-vs-0x0104 conflict is [CONFIRM-ON-BENCH]. **SNZB-04P** — characterize at the DESK first (magnet open/close reps near the coordinator; sleepy interview + reporting-config want proximity), mount at the front door ONLY after its characterization worksheet is complete; the reed gap governs mounting, ≤ ~10 mm closed, aesthetics second; watch the dossier §3 dual-path signaling (IAS + bound-OnOff). **SNZB-01P** — desk instrument: adopts battery-only per the ruled R2(B) (presses log-visible, absent from the device model — deliberate scoping; the BTN-AMD is post-gate); its bench role = the physical log-marker (a press stamps the log at operator-act time). **SNZB-02P** — desk join; the sleepy periodic reporter (temp/humidity) and the future 25 h battery-posture availability case. **ZBDongle-P — STOWED, NEVER POWERED in this lane** (adversarial tool for B2; also an eliminated suspect while unpowered).

**PHASE 0 — runnable immediately (no hub gate):** unbox hygiene (⏺ record model/firmware stickers + IEEE addresses per device — they're on the housings); battery tab pulls WITHOUT initiating pairing (a long-press starts pairing — avoid until the join session; a short press wakes); prep one characterization worksheet per device in the return file (seed from dossier §N.6: expected interview surface, expected reporting posture vs Doc 08 §3.7 defaults, confirmation expectation [only the S31 is CONFIRMABLE — the three sensors carry `confirmation: []`], the [CONFIRM-ON-BENCH] list to resolve); stage the physical bench layout (all four devices within ~1 m of the coordinator for interviews; the 04P's magnet half at hand).

**PHASE 1 — THE JOINS SESSION (⛔ HUB-GATED — do not start without the hub's explicit go, relayed by Nick):** prerequisites the hub confirms first: (1) the Wave-2 profiles WU LANDED on core + CI-green + deployed to the Pi; (2) the SEED formation ruling executed — the joins ride the ONE-formation economy (SEED fresh formation = the SD-5 discharge → Wave-1 rejoin → the four joins, one session). The hub supplies the session's paste-blocks at the go (formation → permit-join → per-device join order [S31 first: mains router, easiest interview; then 02P → 04P → 01P with wake-press discipline] → per-device verify: announce → proposed COMPLETE → adopted → reporting_configured [expect honest TIMEOUT-class degradation on sleepy legs] → the D1/B2 evidence reads). If ANY device won't join stably, or any identity/verdict anomaly appears: ⏺ paste + STOP — never improvise; the hub adjudicates. Expected honest states to not panic about: `availability: UNKNOWN` on a fresh join until first evidence; `key_establishment_failed: TC_REJECTED_APP_KEY_REQUEST` WARNs post-join (the known policy-denial class, recorded).

**Return contract:** everything lands in the ONE return file as it happens — worksheets, join transcripts (verbatim token lines), latencies, surprises, and a §-final list of [CONFIRM-ON-BENCH] items resolved vs still open. The hub two-layer audits the return, folds corpus entries + profile confirmations, and orders any commits. This lane never writes anywhere else.
