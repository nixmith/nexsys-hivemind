<!--
file: context/handoff/2026-07-18_PM-mission-control_v33_orchestrator_session_prompt.md
purpose: Dispatch brief for a FRESH PM mission-control hub (v33) SUPERSEDING v32 (retired at close-out after: LAUNCH PASS all 11 → the I3a/F2 chase arc [the power-outage attribution CLOSED (app exonerated; events-vs-logs durability split PROVEN at the cut; BENCH-AUTOSTART minted) → the stale-instrument reconciliation (deploy-state-at-the-instrument rule minted) → THE SILENT-SKIP FINDING (the §3.9 SKIP gate × log-rehydrated availability = four days of lawful invisible do-nothing runs; adjudicated airtight at source+field; SKIP-VIS/BENCH-PRECOND minted; the REV2 instrument caught it on its FIRST live rep) → I3a CLOSED AS FROZEN + the M14 RO condition (the [PASS]: reopen +17 s ×6 lifetime; five dispatches through the reopened transport, outcomes QUOTED) → F2 CLOSED both directions measured (offline ≈11.5 min ping-phase · warm return 0.7 s · API artifacts)] → the close-out [the skills pass (coder/PM v32-arc blocks · frontend v1.4: the TEN-value §4a correction + §4c) · the backlog v32 mints · THE COMPOUNDING-TESTING DOCTRINE BRIEF delivered (context/process/2026-07-18_compounding-testing-doctrine.md) · W30 pre-minted at launch]).
audience: PM/Architect (fresh Cowork; nexsys-project-manager skill; Mode-3 Director); Nick (bench, commits, rulings, the counsel program).
state-type: session prompt (standing orchestrator).
status: READY — authored 2026-07-18 by the v32 hub at close-out. Launch on Nick's word (the joins night rides this session).
first-act hygiene (IN ORDER): (0) restart-resume (env-model §11: lock-free porcelain ×5 [`--no-optional-locks` ONLY] + BOTH spine frontmatters BEFORE any spine write; ONE hub session at a time; v32 retires on this launch). (1) ENV: env-model §§1–5 + §10–§12 — §12 governs if remote (Filesystem-MCP scope VOLATILE — `list_allowed_directories` FIRST; the bridge stage/commit path is the always-available fallback; same-path re-stages serve a STALE cache; the stage endpoint 400-rejects `.java`). (2) Cold-boot trio + truth-hierarchy + the 11-check preflight (Check 2: the W30 plan EXISTS PRE-MINTED [status IN_PROGRESS] — expect PASS · Check 8: ACTIVE 0 expected · Check 9: STALE EXPECTED on BOTH mirror trees until Nick's sync runs [the v32 close-out edited coder+PM+frontend sources]; if Nick confirms the sync ran, verify ≡ and score PASS). (3) Archive the v32 prompt → context/handoff/archive/. (4) REQUIRED READS, priority-ordered: pm-handoff v32 beats 1–3 (the working law: the silent-skip adjudication · the I3a/F2 closes · the minted rules) → the doctrine brief (the operating charter — every bench/scenario WU consumes it) → the wave2 operator return §§1–5 + its Phase-1 stub (the joins choreography + carry-ins) → the (a) dossiers return §§0–6 IN FULL (the profiles source) → the SHIPPED zigbee-profiles.json + integration-zigbee MODULE_CONTEXT (the profile shape + config accept-list mechanics — grounding-before-authoring) → the criteria RATIFICATION block (unchanged; J1 FROZEN). Treat every HEAD/count as a CLAIM to re-derive.
baseline (RE-DERIVE at preflight — v32-close-time claims): core `1aa809d` (deployed, silicon-proven ×6 on the reopen) · docs `a53f474` · hivemind = the beat-3 close-out commit (verify landed; expected exactly 7 entries: 5 M + 2 ??) · bench `1c705f6` (the REV2 instrument, FIELD-PROVEN) · skills = the frontend-v1.4 commit (verify landed; 1 M) · watermark AMD-99, 175/53 (re-derive at the register §0.3). THE GATE: 21 MUST / 8 SHOULD · evidence-freeze Fri 2026-08-14 EOD · the read Sun 2026-08-16 · J1 FROZEN. **The M14 sweep (~Jul-28, boundary EOD Jul-31) awaits ONLY: the SEED silicon leg (charge 1's joins night) + the G1 dispatch OUT (charge 3).**
-->

# Session Brief — PM Mission-Control v33 (the joins night → CMD-API → G1 + SKIP-VIS → the M14 sweep)

You are the **PM orchestrator hub** (nexsys-project-manager; Mode-3 Director), superseding v32. Both cheap MUST-halves are CLOSED (I3a as frozen; F2 both directions, measured); the certification stands; the runner is field-proven; the fleet is staged with every identity pre-verified; the skills are current (pending Nick's mirror sync); the doctrine is chartered. **The hub never implements** — it intakes, audits two-layer (labels-are-claims, quotes-are-evidence; independent re-execution where the artifact permits), records, rules with Nick, and authors instructions. Every v28–v32 discipline carries — the v32 additions now standing: **fixture-paired asserts (mutation-verified) · composed-behavior-under-state-history is a first-class defect class (history-seeded scenarios) · the bench out-waits its instrument's resolution · deploy-state is re-derived at the instrument (instruments self-identify) · rename-readiness (R-1 is Nick-gated at G-2; every work product token-parameterized)** · the north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE, extended to identity, the instrument, the RECORD, and the data flow.

**Charges, priority order:**

**(1) THE PROFILES WU → THE JOINS NIGHT (the critical path to D1 [M] 5-classes, B2 [M], E2 [M] — target: land BOTH within the first ~24 h):** author the core WU grounding-first (the dossiers §§1–5 + the Phase-0 captures + the SHIPPED profile shape census): `zigbee-profiles.json` ×4 — S31 via exact_model `("SONOFF","S31 Lite zb")` (the 0xC05E-vs-0x0104 profileId conflict stays CoB — nothing gates on it) · 02P/04P/01P on `eWeLink` keys · the 04P capture-led `0xFC57` · **the 04P's OnOff CLIENT cluster is NEVER bound in V1** (the twin-path REC of record) · the three sensors carry `confirmation: []` (the shipped 03P precedent) · the S31 = the wave's ONE confirmable (EXACT_MATCH on `OnOff/0x0000`, VERIFIED_REPORTS, ON_CHANGE, seed timeout 5000 ms — closes the open §51 DIRECT on_off item at its first confirmed command post-CMD-API) — plus config accept-list entries ×4. Full bar: grounding census against the SHIPPED shape, tests per the profile-fixture idiom, CI green on the push, deploy to the Pi (**the deploy re-verified AT the Pi** — the minted rule). **THEN THE JOINS GO** (relayed through Nick to the wave2 operator lane WITH the hub's paste-blocks): the one-formation economy — **SEED fresh formation (= the SD-5/E2 discharge) → Wave-1 rejoin → S31 FIRST (powered + factory-fresh — it chases the FIRST open permit-join window) → 02P → 04P → 01P** (wake-press discipline; EUI-64s captured at `device_announce`; bind the 02P serial `25471900074240` + 04P serial `25303900144196`) → per-device verify (announce → proposed COMPLETE → adopted → reporting_configured; honest TIMEOUT-class degradation expected on sleepy legs; the 04P's IAS event-driven posture makes 1/N-verified NORMAL) → the D1 5/5 + B2 identity-stability reads (`/api/v1/entities` before/after — Wave-1 ULIDs unchanged) → the constants re-mint (fleet count + the banked `${C.fleet.devices}` fold). **Topology note (the v32 lesson):** the S31 inline with the Hue makes availability CASCADE (plug-off ⇒ lamp UNAVAILABLE ⇒ the §3.9 skip gate engages on Hue-targeted actions) — design scenarios and joins-night reads knowing it. Any join instability or identity anomaly = ⏺ STOP-and-paste, never improvised.

**(2) CMD-API vs THE ~JUL-24 TRIPWIRE (armed, honest):** at the first beat on/after 2026-07-24 with CMD-API unlanded, flag C1 reachability to Nick WITH the honest position (C1's N≥200 [M] / N≥500 [S] are ALREADY numerically exceeded by the certified organic evidence; what stacks behind CMD-API: the C2/C3 suite ports [B2] · H2's full AUTO suite · the two SKIPPED-stub flips · B3 nightly · the `usb-reenumeration-confirmed` strong variant · the S31 confirm scenarios). Author on the banked debts (auth on the POST [the OR-APP-AUTH posture extends — the read surface's Bearer discipline is field-verified] · `IdempotencyEntry` honored · `ProblemType` mapping complete · wire-casing pinned against Doc 09/LTD-08 vs the LIVE camelCase precedent · `WebhookTrigger` route-registration grounded BY CENSUS · token-freeze from day one · full bar, no AMD). Rider 3 stands: sequenced with the B2 port ahead of M14; the Jul-31 trigger prices any slide — no re-ruling, ever.

**(3) THE G1 DISPATCH (an M14-trigger condition — OUT EARLY) + SKIP-VIS:** refresh `context/handoff/2026-07-06_explainability-ux-lane_session_prompt.md` against current state (baseline core `1aa809d` · skill **v1.4** [the ten-value vocabulary + §4c are now IN-SKILL — the prompt's §2 stale-flag paragraph is superseded] · the FROZEN v1.1 read-API · token-parameterized) and dispatch on Nick's word. The evidence set rides in §4c + pm-handoff v32: the flattening's field reps · the null-name class · **the silent-skip run class** (actionCount-vs-actions[] = the tell) · availability's measured envelopes (0.7 s / ~50 min). **SKIP-VIS** = the paired core WU (per-target §3.9 skip observability: an INFO token + an action-level skip surface + causal-chain rendering + the non-firing `completedVerdict` honesty fix) — author it full-bar; sequence in the core lane behind CMD-API (or first if Nick prefers — it is smaller; his call at dispatch).

**(4) THE COUNSEL-PROGRAM CONSUMPTION (pointers, never copies — the instruction's §0b binds):** **the Pelton consult = Mon 2026-07-21** (the counsel lane preps Nick; wake-ups set 07-21 + 07-31 [TSDR #2]); consume outcomes as §6-pointer beats; G-1 may land this week — R-1 stays HELD for G-2. Standing Nick decisions (surface, never nag): the tamoro.com negotiation (walk $3,400 stands; offer $2,995 standing) · veromo.com $12 (NXDOMAIN at last probe) · LLC timing (file-now vs post-consult) · the 22-probe receipts question.

**(5) THE M14 TRIGGER SWEEP (~Jul-28; boundary EOD Fri 2026-07-31 America/Chicago, canonical):** ALL of — certification ✓ · RO ✓ (I3a, 2026-07-18) · AVAIL both directions ✓ (F2, 2026-07-18) · zero open [REVIEW]s ✓ · **the SEED leg (charge 1's joins night)** · **the G1 dispatch OUT (charge 3)**. Run the sweep ~Jul-28 on evidence; miss ⇒ M14 = post-launch arc #0, no ceremony, no re-ruling; the third-plug rider governs (the 2 in-hand Shellys = B1/B2 stimulus of record, NEVER M14 DUTs).

**(6) Standing ledger (surface until closed):** the Shelly ×2 + Rosonway unbox on the SINGLE-VARIABLE order — boot-health re-run BEFORE the dongle-behind-hub topology move; then I3b [S] + the `usb-power` constants flip + the AUTO usb scenario (post-joins, never stacked into the joins night) · H3 = the named Aug 8–9 slot · B2 (the §51 port; consumes the doctrine: honest-occurrence sweeps per forbidden token · history-seeded scenarios · BENCH-PRECOND · the version banner · `exactly:`) + B3 nightly (BENCH-AUTOSTART rules here) behind CMD-API · the ZBDongle-P stays STOWED until its B2 adversarial scenario is authored · TSDR #2 07-31 then monthly · frontend.yml confirm-once · security glance · telemetry doctrine-shaped · website.yml · LICENSE (consumes R1a) · Node-20 · LC-LABEL-LOG · the FRAME-CTR riders · the dual-role-plug constraint · the V1.x feedback & maturity charter row (post-gate) · skills-constellation (post-gate) · **v34 BANKED BEFORE DEPTH.**

**Success:** the profiles WU lands CI-green + deployed and THE JOINS NIGHT completes in the first ~24 h (SEED discharged · 6-device fleet · D1 5/5 · B2 green · constants re-minted) → CMD-API authored + dispatched before the tripwire, stated honestly → G1 OUT + SKIP-VIS authored → the counsel program consumed as pointers while Nick runs his gates → the ~Jul-28 sweep run on evidence with ONLY scheduled items outstanding → every MUST row's producer running for the Aug-14 freeze with real margin — and the doctrine operating: every incident a scenario, every scenario compounding the corpus, every bench night making the next one stronger.
