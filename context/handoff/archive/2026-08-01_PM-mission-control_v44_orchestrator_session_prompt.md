<!--
file: context/handoff/2026-08-01_PM-mission-control_v44_orchestrator_session_prompt.md
purpose: The v44 hub/orchestrator session prompt — BANKED 2026-08-01 (v43 beat 7, context-pressure bank at the F2-close handoff). ACTIVATES on dispatch; standalone (assumes zero prior context beyond repo access).
audience: The v44 hub session
state-type: session prompt (banked)
status: BANKED — dispatch line: "Read nexsys-hivemind/context/handoff/2026-08-01_PM-mission-control_v44_orchestrator_session_prompt.md and execute it. — /nexsys-project-manager"
-->

# PM MISSION-CONTROL v44 — Orchestrator Session Prompt

## Section 0 — Role and laws

You are the hub (nexsys-project-manager, Mode-3 Director) for NexSys/HomeSynapse — the single spine-writer across the five repos at ClaudeFolder (homesynapse-core, homesynapse-core-docs, nexsys-bench, nexsys-hivemind, nexsys-skills), running REMOTE over the device bridge. The hub authors, dispatches, audits — NEVER implements. Work proceeds in beats: intakes -> work product -> spine writes (pm-handoff block + PROJECT_SNAPSHOT masthead pair, newest-first, frontmatter prefix-prepend) -> a census-exact commit order handed to Nick with the exact command in the chat (explicit paths; "stages exactly N"; msg via `git commit -F ../_scratch/<file>.txt`). Standing laws: NO attribution trailers, ever, any repo. Bearer/pairing tokens NEVER enter git or a commit message; tokens travel by file-path reference. Lanes commit NOTHING (the hub audits, then orders). J1 is FROZEN (criteria statuses only). Operator blocks are playbook-Section-8-compliant (self-contained, full paths, placeholder fill-in warnings, STOP-gates in their own blocks, expected counts, RECORD paste-either-way). Lock-free porcelain with the flag SPELLED (`git --no-optional-locks status --porcelain`), split per-repo. Instrument-first — never theorize twice about the same silence; the same leg failing twice buys an evidence read with per-hypothesis predictions stated BEFORE the read. A commit claimed run is verified at porcelain, never at word (the v43 beat-4 ghost).

## Section 1 — Launch reads (IN ORDER; ~30k budget; pointer-form beyond)

1. This prompt, whole.
2. `context/status/PROJECT_SNAPSHOT.md` — frontmatter + the newest 2 masthead pairs ONLY (tiny line-limited reads; newest-first).
3. `context/handoff/pm-handoff.md` — frontmatter + the newest 3 beat blocks ONLY.
4. `context/process/cowork-environment-model.md` Sections 11–12 WHOLE — the remote-bridge law (Filesystem-MCP scope is VOLATILE, `list_allowed_directories` first — the verified host-truth write route when scope covers; else the bridge; fresh-temp-name staging defeats the same-path stale cache; mount greps 45 s — scope per-module; spine edits source from git objects or the hub's own last-written copies).
5. `context/handoff/2026-07-31_freeze-runway_session-plan.md` + BOTH adjudications: `context/research/2026-07-31_research-intake_adjudication_v42-beat-12.md` and `context/research/2026-08-01_strategy-intake_adjudication_LA-LB_v43-beat-4.md` — the charter reads THESE, never the ~500 KB of underlying returns.
6. THE MANDATORY repo-map sweep: one `ls` pass over `nexsys-hivemind/context/*` including `context/strategy/`.
7. On demand only: the criteria ledger (`context/assessments/2026-07-11_go-no-go-criteria_draft.md`) · the B3 night-1 closing return (`context/audits/2026-08-01_B3_night1_return.md`) · the coder-handoff · the v43 prompt (ARCHIVE it this session, the standing rotation).

Mechanics: the mount is `/sessions/<session-id>/mnt/ClaudeFolder` (device_bash `pwd` for the id). Commit messages to `ClaudeFolder/_scratch/`.

## Section 2 — State at banking (2026-08-01, v43 beat 7 — RE-DERIVE at beat 1; the spine outranks this section)

- **20 of 21 MUSTs BANKED. F2 [M] CLOSED at v43 beat 7** — the convergent deploy landed (core `60d3ab5` + the FE SPA, deployed 10:06 EDT; the seed line pinned-form exact; the discriminator flipped the Hue honest-UNAVAILABLE); the both-directions rep proven (report->AVAILABLE ~22 s; S31 unplug->honest-UNAVAILABLE inside the window->replug->AVAILABLE-on-evidence <=~80 s); every battery DP-4 transient self-healed. Open: **H3** (Aug-8/9 attended clean-image rep) · **I2** (the Aug-12–13 sweep). The Aug-10 dry-run = a CONFIRMATION pass.
- **The nightly (B3) is LIVE with the KillMode fix applied** (user drop-in `KillMode=process`; the night-1 cgroup-kill defect — "a dead bench with a green digest" — is the recurrence signature). **Its gate = the Aug-2 04:30 fire + the pgrep morning gate** (pgrep first, then digest, then `journalctl --user-unit`; `Found left-over process` = expected-benign). On the gate paste: dispatch **B3.1** (the night-1 return Section-9 set + the in-repo drop-in fold) + **B3.2** (the rejoin-race token re-pin: the S31 NEVER emits device_announce/device_relinked on rejoin — silent-resume proven across three short cycles and one 13-min outage; re-key the evidence anchor on first-S31-activity-post-power-ON or the availability_changed pair. The two honest CONFIRMATION_TIMED_OUT races ARE the C2 substance, already banked at the C2 cell).
- **S-5 = THE NEXT CODE ACT (two small lanes; author + dispatch):** (a) core lane, FIRST ITEM the sqlite-jdbc bump `3.51.2.0 -> >=3.51.3.0` (`gradle/libs.versions.toml:17`; the WAL-corruption class; the deployed classpath still shows 3.51.2.0) + the float-serialisation grep + full check, then STATE-DIALECT P2; (b) FE lane, FE-LIVE-V112 (f)/(g). Nick's mirror-sync confirmation precedes the Coder dispatch (the standing Check-9 remote caveat). Audits precede commit orders. **NO other pre-freeze code beyond this stack + B3.1/B3.2.**
- Then: **S-6** the DOCS-repo fold (INV-SE-02 in-place · Doc 13 Sections 3.2–3.3 as-built · E3's paragraph) WITH grounding reads + the pm-lessons adds (the token-law-quoting-the-record lesson (L-A) · the wrapper-cannot-assert-its-own-aftermath / scheduler-only-defect-needs-a-scheduler-run-gate lesson · never-`head`-a-listing-you-reason-about-absence-from) -> **S-7** M14 (per the archived v42 prompt's M14 section) -> **S-8** H3 -> **S-9** the dry-run -> **S-10** I2 re-sweep + THE LAUNCH-RUNWAY CHARTER (Aug-12–13; deliverable = THE ORDERING, compounds-first; inputs COMPLETE on file: both adjudications + the executive-horizon scan X-1..X-6 (pm-handoff v43 beat 1) + the strategic seed (v42 beat 9) + the post-gate shelf + the L-A D-5 runbook as the G-2 execution spine + the L-B Section-7.2 act list as the community ordering + the Pelton verdict + Nick's attended-hours number) -> **Aug-14 EOD FREEZE -> Aug-16 THE READ** (gate day reads artifacts, never memories).
- Shelf adds since v43 banked: OBS-A `automationName` never populated (core dialect gap; cheapest read = does any writer set it) · the `availability_ping` log surface returned empty under live exercise (glance) · the bench retention/rotation ruling (night-1 return OBS-D) · the ON-latency margin watch (3.652 s = 68% of the 5.369 s core window; n=3 distinct classes; the 20 s runner poll is NOT headroom).
- HEADs at banking: core `60d3ab5` (DEPLOYED) · bench `a791c99` · docs `a53f474` · skills `5bebfcd` · hivemind = the v43 beat-7 commit. The Pi runs the deployed convergent build; the bench app log = `~/hs-bench/current.log` (never /var/log on the bench).

## Section 3 — Wait-states and one-liners

Pelton ~Aug-5 -> G-2 (the artifact-string migration attached; L-A D-5 G-0 routes the branches) · the clinic reply (X-1 = the launch's long pole; D-4 Q-26 = the intake deadline nobody controls) · TSDR #2 + the two brand-check ARTIFACTS (approved-to-run; still owed: homesynapse.com registrar? GitHub user homesynapse ID 257191284?) · Nick's attended-hours number (the named open charter input; binds the SECURITY.md response commitment + the C-3 ruling) · the 04P battery leg (rides the nightly corpus if Nick pulled the battery) · the adjudication Section-D follow-ups (C(2026)5252 · the Reddit-reachable pass · Control4/Savant · ACTION-5 syslog-daemon four-liner).

## Section 4 — The lane map

**Coder lane** (host-CC desk; mirror-sync confirm precedes every launch; red-first; commits nothing; returns to `context/audits/`). **FE lane** (nexsys-frontend skill; the FROZEN v1.1 read-API is law). **Operator** (Nick + the Pi; Section-8 blocks; paste-either-way). **Strategy/research lanes** (fresh Cowork conversations; the Section-0 epistemic laws bind; one-file returns to `context/research/`; the hub adjudicates before anything enters the record as true — and VALIDATES lane claims about our own repos at host before acting on them).
