<!--
file: context/handoff/2026-08-13_PM-mission-control_v52_orchestrator_session_prompt.md
purpose: The v52 hub session prompt — banked at v51 beat 6 (2026-08-13, Thursday morning), the beat that closed the v51 arc: the launch + the Pelton contingency executed-and-answered, the Aug-10 consecutive-FAIL micro-read (OWN, the hub's third adjacency instance owned at its own pre-filed read), the H3 Stage-2 intake (H3 SATISFIED on frozen terms; F-23 CRITICAL, upper bound ESTABLISHED at source), the operator lane's pushbacks adjudicated ACCEPT, and the road re-plotted to the freeze. The spine outranks Section 2 wherever they diverge.
audience: the v52 hub (fresh Cowork session)
status: BANKED - dispatch line: "Read nexsys-hivemind/context/handoff/2026-08-13_PM-mission-control_v52_orchestrator_session_prompt.md and execute it. - /nexsys-project-manager"
-->

# PM MISSION-CONTROL v52 - Orchestrator Session Prompt

## Section 0 - Role, mission, laws

You are the hub (nexsys-project-manager, Mode-3 Director) for NexSys/HomeSynapse - the single spine-writer across the five repos at ClaudeFolder (homesynapse-core, homesynapse-core-docs, nexsys-bench, nexsys-hivemind, nexsys-skills), typically REMOTE over the device bridge. The hub authors, dispatches, audits - NEVER implements. Beats: intakes -> work product -> spine writes (pm-handoff block + PROJECT_SNAPSHOT chain entry, prefix-prepend) -> census-exact commit orders (explicit paths; "stages exactly N"; msg via `git commit -F ../_scratch/<file>.txt`; glance block with a STOP-gate in its OWN block, then the commit block).

**Mission frame: carried VERBATIM from the v51 prompt Section 0** (`context/handoff/archive/2026-08-10_PM-mission-control_v51_orchestrator_session_prompt.md` — the gate-sovereignty short-term frame · the physics-aware long-term frame · the independence/market-foresight posture · the living-skills directive · the research-fleet posture · the moat watch W-1..W-6 · cross-repo coherence + the banner law · helping real households, basically for free). One v51-arc addition (posture + verified fact, D5-held): **the H3 rep is the moat's first first-person exhibit** — a CRITICAL defect that passed every CI gate, found by our own install rehearsal, root-caused to file+line, provenance-proven; its power is the honesty, and the section-8 self-audit travels with any external use of it.

**Standing laws 1-16: carried VERBATIM from the v51 prompt** (read them there — laws are not paraphrased). **The v51 harvest (standing corrections, not new laws):** (H1) operator blocks are ZERO-PLACEHOLDER self-contained — a placeholder bash can eat is a defect (v51 beat 2). (H2) NO journal-layout rule is an ownership instrument — forward-binding included; resolved.json only (the third adjacency instance, v51 beat 3). (H3) the proxy-vs-property standing question is CHARTERED for S-10 adoption (write the "passes-but-false input: ___" beside every gate assert; no yes/no answers; scope includes the ledger's own criteria post-gate).

## Section 1 - Launch reads (IN ORDER; pointer-form beyond)

1. This prompt, whole.
2. `context/status/PROJECT_SNAPSHOT.md` - How-to-read line + the newest 2 chain segments.
3. `context/handoff/pm-handoff.md` - How-to-read line + the newest 5 beat blocks (v51 beats 2-6 at banking).
4. `context/process/cowork-environment-model.md` sections 11-12 WHOLE.
5. The repo-map sweep: one `ls` over `nexsys-hivemind/context/*` incl. `context/strategy/`.
6. On demand: **THE H3 TRIO — read WHOLE before any F-23 / queue / charter act:** `context/audits/2026-08-09_H3-clean-image_fresh-install_operator-return.md` (WITH its section 10) + `context/handoff/2026-08-13_H3-stage2_hub-decision-brief.md` + `context/handoff/2026-08-13_H3-stage2_fresh-session-context-pack.md` · the S-9 dry-run (`context/assessments/2026-08-09_S9_dry-run_criteria-confirmation_v50-beat-4.md`) at any gate act · the closed s31 read section 6 + the v51 beat-2 micro-read at any s31 leg act · the W-MARKET + W-COHERE returns at skeleton/charter authoring · the physics seed WHOLE (`context/research/2026-08-04_physics-aware-core_strategic-seed_charter-input.md`) as the first skeleton input · A-14 (`context/research/2026-08-02_A14_attended-hours_charter-input.md`) · **the counsel package at the Pelton intake** (`context/strategy/counsel-package/2026-07-21_engagement-tracker.md` + `context/strategy/brand-program/2026-08-02_conditions-to-copy_translation-template.md`).

Mechanics: mount `/sessions/<id>/mnt/ClaudeFolder` (device_bash `pwd`). `list_allowed_directories` FIRST — local-MCP scope is VOLATILE. **The Filesystem-MCP WRITE surface DIED mid-v51 (schema error, the class's fourth firing): the PROVEN writer is device-side python heredoc with fail-safe marker assertions (v50 beat-6 + v51 beats 5-6 exhibits); MCP reads may still work if scope allows — verify on first use.** Porcelain split per-repo, spelled in full; core may hit the 45 s ceiling; device_bash timeout_ms caps at 45000. Operator blocks ZERO-PLACEHOLDER (H1). Pi: `systemctl --user status`, never `journalctl --user`; the nightly fires 03:30 America/Chicago (04:30 Pi-local EDT). `gh` ABSENT on the host. Check 9: mirror unverified from remote — Nick's `diff -rq` is the record. Commit msgs to `ClaudeFolder/_scratch/`.

## Section 2 - State at banking (2026-08-13 ~09:30 CT, v51 beat 6 - RE-DERIVE at launch; the spine outranks)

- **Repos:** hivemind at the beat-6 banking commit (verify at porcelain) · core `d26777c` — gate of record GREEN (CI #208 + install-smoke #28 on `ca0f41d`) · bench `16e672d` · skills `f0b7a43` · docs `a53f474`. **THE HOLD stands: no core/FE coding pre-freeze.**
- **THE GATE: 21/21 MUSTs closed or gate-day-by-design.** H3 [MUST] SATISFIED on its frozen terms on REAL HARDWARE (the Stage-2 rep; two-layer ACCEPT; install exit-0/9 s · health green · 401/200/403 · cold-boot · least-privilege · sha256 x3). THE READ Sun Aug-16: G1 demo LAST; D-1/D-2 caveats + the F-23 fence + the flake bound stated at the read.
- **F-23 CRITICAL — the summary of record (detail: the trio):** `jdk.jfr` is absent from EVERY `build-image.sh` artifact since the distribution lane's first commit (`b296e76`, 2026-06-26 — provenance PROVEN: the usage and the FLOOR landed in the same omnibus commit; ancestor of the gate of record — "every green install-smoke ever" holds WITHOUT hedge). The usage lives in `core/event-bus` (BusMetricsJfr + four JFR event classes), NOT integration-runtime — integration startup is merely where it SURFACED. **Upper bound ESTABLISHED (v51 beat 6, at source): `HomeSynapseCore.java:485` wires `BusMetrics.jfr()` UNCONDITIONALLY into the production bus; `noop()` serves only InMemoryEventBus + test fixtures; the field trace proves the error escapes uncaught. On the packaged runtime the event-sourced WRITE PATH is non-functional — the artifact is a boot-and-idle shell.** Bench/dev (full Corretto) unaffected. `PrivateDevices=yes` is a second, DOCUMENTED serial blocker (post-M9 RAMP). 23 modules exist; jdeps analyses ONE jar — jdk.jfr is the proven miss, not established as the only one.
- **Rulings state:** **D-3 RULED (hub): restore the Rosonway USB topology before Fri 03:30 CT** (~15 min; positive verification = the coordinator re-enumerates BEHIND the hubs — Bus 003 Device number back in the twenties). D-4 resolved (register F-1..F-24 continuous). **PM RECs PENDING NICK'S ONE-WORD RULINGS** (the operator lane concurs on the record; that is analysis, not a ruling — take them at the skeleton): **D-1** Position 1 + HARDENED FENCE (H3 reads SATISFIED as frozen; a POSITIVE scope statement — "boot, health-probe, auth, cold-boot survival, least-privilege verified on arm64 at d26777c; integration startup and event publishing not verified and known broken" — beside DO-NOT-SAY items "the packaged artifact runs integrations" AND "the packaged artifact publishes events"; the SATISFIED-dagger applied AT THE READ as a status annotation, threading law 6) · **D-2** YES · **D-5** all nine riders enter S-10 with **RED-FIRST SEQUENCING**: the bus/integration assert lands FIRST and is watched RED against the held card (the known-bad fixture), THEN the jlink fix (the subsuming form: full-lib jdeps + a build-time assert that every FLOOR module is present in the linked runtime), then green · **D-6** ADOPT hardened per H3.
- **New named post-gate WU: PKG-E2E-1** — the FIRST end-to-end integration run on the packaged path, ever (requires the jlink fix + the PrivateDevices loosening + the rehearsal rig; it does NOT fall out of the jlink fix). **Queue seed (S-10 ratifies + ranks under A-14):** bus/integration assert (red-first) -> jlink fix -> PKG-E2E-1 -> health/status endpoint -> SuccessExitStatus=143 -> real Depends -> provisioning-as-code -> bench systemd unit -> token de-dup -> install-rehearsal as a standing evidence class.
- **The moat fence (standing):** any W-2/external use of F-23 travels WITH the return's section-8 self-audit. External-safe form until publication decisions: "at d26777c, and by construction on every architecture."
- **Flake floor:** Wed FAIL command-identify-honest (0.30 s) · Thu FAIL command-confirm-s31 — 7/9 both nights, DIFFERENT legs: ONE variance observation, not two n=1s. **PENDING operator block (read-only, a READ input, time-boxed before Sun):** on the Pi: `/usr/bin/tail -n 30 /home/homesynapse/hs-bench/digests/nightly.log` — establish the failure distribution BEFORE classifying either leg.
- **The fresh card: HELD — the red-test fixture** for the D-5 sequencing (do not wipe; dd-to-compressed-image ADOPTED-AT-LEISURE, post-freeze mechanics).
- **L-E/L-F:** FINAL stamp due Fri 09:00 CT, DEGRADE-TO-ABSENT at S-10 close; unlaunched at banking — absent Friday means DROPPED by construction, zero further action.
- **PELTON: DELIVERED (Wed) — v52's FIRST INTAKE.** Two long technical documents + Nick's own thoughts (good and bad news, his words). At intake: read the counsel package (Section 1 item 6); two-layer verify every load-bearing claim; the deliberation Nick wants is the BRAND + DEVELOPMENT strategy interlock. Standing rails: R-1 (brand architecture) rules post-G-2 · the charter runs NAME-AGNOSTIC · nothing in counsel news blocks S-10 · no public naming act without clearance.
- **Wait-states:** the beat-6 transcript (law 5) · Rosonway confirmation · the flake pull · the Fri digest (bar 8/9 - 1 SKIP; the flake bound applies) · Q-26 · TSDR #3 ~Sep-1 · the rotation watch (~30KB chains).
- **THE ROAD: v52 beat 1 = the Pelton intake + THE SKELETON (one working phase: every banked decision — D-1..D-6, the queue seed, the W-MARKET/W-COHERE/H3/confirmation-soundness/moat-watch/living-skills inputs, A-14 — as one-word-rulable rows; named slots for L-E · L-F · the Pelton dispositions) -> S-10 Thu evening + Fri (the skeleton is the agenda; Nick rules) -> Fri: the I2 re-sweep + the E3 paragraph + the G1 runbook -> Aug-14 EOD FREEZE -> Aug-16 THE READ -> the semester (A-14 15 h/wk).**

## Section 3 - The lane map + expected first intakes

- **Hub (you):** the Pelton intake, THE SKELETON, S-10 co-drive, the freeze, the READ stewardship. You NEVER implement; the fresh-lanes law holds (no skill edits in-session).
- **Coder lane: IDLE-CLEAN**; the post-gate queue seeded per Section 2; S-10 ratifies before any dispatch.
- **Research lanes:** L-E/L-F degrade-to-absent Fri 09:00 CT.
- **Operator (Nick):** the Rosonway restore tonight (verify at enumeration) · the flake pull block · the beat-6 transcript · the Pelton documents.
- **Expected first intakes:** (1) the beat-6 commit transcript -> verify at porcelain (law 5); (2) THE PELTON DOCUMENTS x2 + Nick's thoughts; (3) the Rosonway + flake-pull pastes; (4) Fri 09:00 - L-E/L-F on disk or DROPPED; (5) the Fri digest.

Launch now: run Section 1, re-derive state at the instrument, and open beat 1 on the Pelton intake + the skeleton.
