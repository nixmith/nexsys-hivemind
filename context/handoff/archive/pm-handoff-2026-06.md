<!--
file: context/handoff/archive/pm-handoff-2026-06.md
purpose: Archived historical PM session records evicted from active pm-handoff.md (monthly rotation per archive/README.md §4). Covers the pre-M5-window June records: 2026-06-04 → 2026-06-05.
audience: PM, Coder
update-cadence: frozen
state-type: history
status: ARCHIVED
last-verified: 2026-06-10 — rotated at the M6.1 sha-reconciliation + cleanup session (sandbox VM down; rotation executed via file tools, content preserved verbatim)
-->

# PM Handoff — Archive 2026-06 (pre-M5-window records)

Archived from `context/handoff/pm-handoff.md` on 2026-06-10. Covers PM session records 2026-06-04 → 2026-06-05 (everything older than the M5 window, which begins with the 2026-06-06 post-M4 synthesis + charter session). Content preserved verbatim.

---

## This Session — M4 Retrospective + Next-Piece Recommendation + Partial-Closeout Reconciliation (2026-06-05, latest)

**Documents-only governance session. No code, no coding instructions. HEAD `8ef9e9f` unchanged; M4 COMPLETE.**

**Freshness preflight: STALE → reconciled → PASS.** The M4.C WUCP Phase 2 closeout had landed *partial* — it updated the big-ticket artifacts (snapshot header, backlog, this handoff's session record, W23 Progress) but left four stragglers: (1) PROJECT_SNAPSHOT Recent Session Log had no M4.C-completion row (most-recent row was the C *briefing*, `e76b925`); (2) **coder-handoff's M4.C Deferred Build Gate still read OPEN/DEFERRED** (never flipped to RESOLVED+`8ef9e9f` after Nick ran it GREEN); (3) the W23 plan's "Current state" footer still read `e73e199`/AMD-53/"B Stage 1 DONE"; (4) cross-agent-notes carried an unarchived superseded-pointer trail. Lower-priority: strategic-context-map prose still says "M4.0a NEXT"; the snapshot's "Days remaining" was stale (182→173). This is the 2026-04-11 M2.5 failure mode recurring — the preflight caught it (working as designed); the closeout *discipline* slipped. Full analysis in the retrospective §5.

- **Deliverable 1 — M4 retrospective:** `context/audits/2026-06-05_M4-retrospective.md`. Constructive, critical. Covers: milestone sizing (M4 was an epic wearing a milestone's name — should have been 3 first-class milestones; P1); the instruction-scope-miss pattern (the M4.C 7-site event-type lockstep cluster) + the systemic **consumer/pin (fan-out) survey** fix (P2); the deferred-gate model (keep it; shift-left the inspection-discoverable misses; P5); the partial-closeout finding (P3); amendment-rigor proportionality (lightweight block-amendment track; P4); the debt register (§7); strategic/velocity (Core-rigor is healthy and not the risk — non-Core at zero is; P6); and the keep-doing list (STOP-gates, AMDs-as-truth, the DOCS reviews that caught AMD-55/56, the preflight, research→AMD→code, dual-skill mirrors).
- **Deliverable 2 — next-piece recommendation:** `context/planning/2026-06-05_next-piece-recommendation.md`. **Recommend: M5 (Platform API + test-support) as a small Core lane, paired with a non-preemptable W3 website/docs standup; M6 (Configuration) follows M5; AMD-65 authored in the M5 window.** M5-first because it's dependency-free, small, and its test-support module (TestClock/InMemoryEventStore/SynchronousEventBus) is a force-multiplier for M6/M7/M9. Do NOT make the next piece pure Core — that defers the roadmap's #1 schedule risk (website/docs) a third time, as W23 Goal 4 already was. Awaiting Nick's posture confirmation (recommendation §7).
- **Reconciliation applied (retroactive WUCP Phase 2 → restores PASS):** snapshot masthead restructured (run-on header → 5-line current-state block; days 182→173) + 2 Recent-Session-Log rows added; **coder-handoff M4.C gate flipped OPEN→RESOLVED `8ef9e9f`**; W23 footer advanced to `8ef9e9f`/AMD-64/M4 COMPLETE; cross-agent CURRENT POINTER rotated + superseded trail archived. Dual-skill mirror `diff -rq` clean (Check 9 PASS). Preflight re-run: **PASS**.
- **NEW Open Risk (working-tree hygiene):** three uncommitted, build-breaking edits exist in the homesynapse-core working tree at `8ef9e9f` — see Open Risks. NOT touched (documents-only); flagged for Nick.
- **Debt logged:** AMD-65 (confirmed QUEUED; recommend M5-window authoring); event-naming dual-convention note (Doc 01 / MODULE_CONTEXT); EventCategoryMapping reauth-category refinement (low-priority; a `SECURITY` EventCategory exists — the two `integration.reauth.*` events may warrant it; revisit at M9); PLAN-M4-CONSOLIDATED → mark SUPERSEDED; Phase-2 traceability stubs (fold per-subsystem into M6/M9/M10/M11 rather than a standalone sweep); process proposals P1–P6. (See retrospective §7/§10 + backlog.)
- **Next forward (fresh PM session, on Nick's posture call):** write the W24 weekly plan; author AMD-66–71 (M6) + AMD-65 (lightweight block-track); issue the **M5** coding instruction **with the P2 consumer/pin survey applied**; (posture A) stand up the **W3 website/docs** lane. M9 supervisor remains ~late-Jul/Aug. SPIKE-DC pre-M14 (~Aug).
- **Project-knowledge spine refreshed (for the two Claude Projects):** all five uploaded files brought current to M4 COMPLETE — `HomeSynapse_Current_State.md` (masthead + §1 + impl-order + §4 CURRENT rewritten), `HomeSynapse_Knowledge_Primer.md` (M4 entry + the new `value-model` module + device-model `AttributeValue`-relocation note + integration-api 22→40 + field counts + invariant count), `HomeSynapse_Navigation_Index.md` (amendment tables + value-model chain + watermark AMD-64 + P2-ratified), `Invariants_Quick_Reference.md` (104→**133 invariants / 32 categories** — added §22 AMD-52, §23 AMD-53, §24–§34 AMD-54..64), `Decisions_Quick_Reference.md` (M4-completion block + AMD-51-staging correction). **ACTION (Nick):** re-upload these five to BOTH the CORE and DOCS Claude Projects (per `project-knowledge/README.md` upload protocol), then verify with a known-term search. The two custom-instructions files need no change.
- **Commit pending:** this session's two new docs + the reconciliation edits + the five refreshed `project-knowledge/` files are UNCOMMITTED — Nick host-git commit (no `sed` placeholder this session; real SHA `8ef9e9f` written throughout).

## This Session — M4.C WUCP Phase 2 → APPROVE → M4 COMPLETE (2026-06-05)

**Mode-3 Director → WUCP Phase 2.** Coder executed M4.C (Claude Code); Nick ran the gate. First run **aborted at `core:event-model`** (upstream) on 2 stale `EventTypesTest` pins (count 46, lowercase-underscore regex) — the new dot-namespaced constants. Root-caused as a downstream-lockstep miss: the instruction's Files-to-Modify listed the producer (`EventTypes.java`) but not the artifacts that pin/consume the event-type set. **Gate-fix round (Coder):** the 5 prescribed fixes (EventTypesTest count→53 + dot-aware regex; EventCategoryMapping +7 entries; three `27→34` count-pins) **plus two the Coder's straggler-grep found** — the production composition root `HomeSynapseCore` and `IntegrationTestHarness` both aggregated only `CORE+LIFECYCLE` and missed the new `CAPABILITY_EVENT_CLASSES` (AMD-59 §3) → 3-way `Stream.of(...)`. The second is a genuine latent-M9 bug (capability events unregistered in production → `encode()` throws), not just a count; no test pinned it.

- **Re-run GREEN:** `./scripts/clean.sh && ./gradlew check --continue` — **145 tasks, BUILD SUCCESSFUL.** All suites ran (event-model, integration-api, integration-runtime, persistence, lifecycle); ArchUnit (`NO_JACKSON_IN_DOMAIN_MODEL` + `NO_DIRECT_TIME_ACCESS`) + spotless included.
- **PM WUCP Phase 2 read vs source → APPROVE.** Verified `CapabilityAdded` matches the R6 co-sign (4 components, derived non-component `capabilityId()`, full `CapabilityInstance`); EventCategoryMapping's 7 entries (5 integration.* → `[SYSTEM, DEVICE_HEALTH]`, 2 capability.* → `[DEVICE_STATE]`); `HomeSynapseCore` 3-way aggregation. integration-api 22→40 types. **NO `module-info`/Gradle/`projectionVersion` change; watermark AMD-64.**
- **2 `[REVIEW]` deviations ACCEPTED:** (a) EventCategoryMapping categories (PM-prescribed; derived runtime lookup, freely amendable); (b) composition-root/harness scope expansion (AMD-59 §3 mandate; no `module-info`/Gradle change; fixed a latent gap — the right evidence-based Coder call).
- **AMD-65 (Expectation persisted codec) deferred BLOCKING-for-M9** — command-less `CapabilityAdded` round-trips today; the `@Disabled("AMD-65 pending")` onOff test is the executable acceptance spec; `WithinTolerance(double,double)` needs the AMD-52 float-determinism treatment. Logged to the pre-M9 queue (backlog) alongside the NQ-6 restart spike.
- **Closeout applied:** PROJECT_SNAPSHOT (M4.C + **M4 COMPLETE**, latest `8ef9e9f`, watermark AMD-64), milestone backlog (M4.C DONE, M4 parent ✅ COMPLETE, pre-M9 queue added), W23 progress, this handoff, cross-agent CURRENT POINTER. Coder did Phase 1 (coder-handoff/coder-lessons +2/MODULE_CONTEXTs).
- **Next forward (fresh PM session):** pre-M9 — author **AMD-65** (lightweight DOCS ratification; the `@Disabled` test is its acceptance spec) → Coder WU; the **NQ-6 restart spike**; then the **M9 supervisor** coding instruction (Research 6 implementation: `RestartLedger`/`HealthFsm`/`ReauthDispatcher`/`ConfigUpdateApplier`/`AdapterMigrationRunner`/`CapabilityChangeRouter` + the `CapabilityPublisher`/`CredentialRotator` impls + the entity-registry capability projection). **M9 must not publish command-bearing `CapabilityAdded` until AMD-65 lands.** Non-Core: the **website/docs standup (W3, Goal 4)** is the highest-leverage parallel lane now that M4 is closed. **SPIKE-DC (dual-coordinator)** remains pre-M14 (~Aug).
- **Commit pending:** all M4.C core files + this hivemind closeout are UNCOMMITTED. Nick commits + fills `8ef9e9f` via sed + pushes all three repos.

## This Session — AMD-54..64 Review + Ratification + Mechanics + M4.C Issue (2026-06-05, earlier)

**Review/ratification-mechanics session (documents-only; no code; HEAD stays `e76b925`; watermark raised AMD-53→AMD-64).** Nick dispatched the DOCS-Project review brief into this session with the eleven AMDs + the **Research 6 return INLINE** (received complete, §1–§7 — no INCOMPLETE gates). Full gate run G1–G5 → **block verdict RATIFY-WITH-EDITS, zero blocking, zero constraint violations, zero JPMS drift**; return written to `context/audits/2026-06-05_AMD-54-64_DOCS_Review_Return.md` (per-AMD table, verbatim G1 diffs, §9 arbitration addendum).

- **G1 highlights:** R4 — REC-42's 12-count confirmed but the lists differ in 11/12 entries (two taxonomies, not transcription drift); R5 — `IntegrationReauthCompleted` confirmed in §7.3; name diffs `Requested→Required`, `Migrated→MigrationCompleted` (the return is internally inconsistent — hook `onReauthRequired` vs event `reauth_requested`); R2 — `ConfigChangeSet` substitution verified sound (per-key `oldValue`/`newValue` preserves the research's load-bearing diff requirement), but the void `onReauthRequired()` made non-implementation undetectable; R3 — REC-43 **did** propose `IntegrationAuthException extends RuntimeException` (quoted in AMD-56 §2). **Two genuine defects caught:** AMD-56's route-(a) trigger unimplementable against the shipped type (hard-coded `errorCode()`, no code-bearing ctor); AMD-55's reauth signalling gap. **G2:** R6 sound on all three counts (co-sign formalized); R7 strict widen-trigger unmet but OAuth-pair atomicity load-bearing; R1 confirmed (research requires no options/config partition freeze). **G3:** every brief shape independently re-derived at `e76b925` — all exact, both module-infos character-for-character. **G4/G5:** block coherent; invariants registrable.
- **Nick arbitrations (same-day, folded pre-ratification):** **A1** keep PM `HealthDetail` transition-trigger taxonomy (metrics-aggregation vs OpenHAB self-report; REC-42 verbatim list preserved in AMD-57 §2.1; may resurface as observability display labels); **A2** keep PM permit names (hook-to-event alignment; diffs recorded); **A3** `ReauthOutcome { INITIATED, UNSUPPORTED }`, default `UNSUPPORTED` (no `FAILED` in the return — fidelity-checked); **A4** code-bearing `PermanentIntegrationException` ctor pair, append-only; `IntegrationAuthException` declined (RuntimeException base contradicts the checked `HomeSynapseException` hierarchy) — frozen AMD-56-INV-03; **A5** `rotate(Map<String,String>)` primary + `default` single-key delegating — AMD-60-INV-03 strengthened to atomic-across-entries; **E3** `ConfigUpdateOutcome.REJECTED` added (outcome-enum channel over exception-typing; prior config = defined safe recovery) — frozen AMD-55-INV-04; **E7** migration failure deliberately emits NO event — the FAILED transition is the signal; the REJECTED-asymmetry principle recorded in AMD-58 §2.1 (rejected apply has a valid fallback state; failed migration has none); **E8** `CapabilityRemovalReason` (FIRMWARE_DOWNGRADE|DEVICE_REPLACED|TRANSIENT_LOSS|UNREGISTERED) restored onto `CapabilityRemoved` + `publishRemoved(...)` (Research 12 Aqara field evidence; persisted records freeze at M4.C) — descriptive-only doctrine frozen AMD-59-INV-06; **E1/E10** mechanical/doc-only. **All eleven AMDs flipped RATIFIED 2026-06-05** with populated §Review-Disposition blocks and ticked checklists. Block invariant count: 26→**29**.
- **Mechanics applied:** `Architecture_Invariants_v1.md` — **29 invariants registered as §24–§34** (+§0.3 prefix rows, §17 index **104→133** across **32** categories, §18 traceability ×11 rows). **Watermark AMD-53→AMD-64** (nav-index §Amendments). Nav-index: +11 RATIFIED rows + Doc-05 row note. **Doc-05 body-fold** (masthead → RATIFIED/folded): §3.7 AUTH_FAILED routing row + code-bearing-ctor fold note; §3.8 `SecurityServices`/`DiscoveryServices` rows + context 10→12 + `RequiredService` 3→5; §3.14 per-descriptor timeout + the RESTART_REQUIRED/REJECTED planned-restart triggers; §4.1 the 14-component order + convenience-ctor defaults; §4.2 AMD-62 note (probe-vs-retry distinctness; OTP 1/60s embedded override); §4.3 14-component record incl. `detail` + the HealthDetail fold note; §4.4 +7 event-table rows + the AMD-58/59 fold note; §8.2 +12 type rows; §8.4 `commandHandler()` + the four hook summaries. **Doc 02 Stage-2 fold (queued since B-S2):** masthead → Stage 2 IMPLEMENTED `e76b925`; §3.10 6×3 legality-matrix table (source-verified per-constant); §4.2 `entity_role` in the record JSON + 12-component note + the AMD-59 mutation-path boundary note; §11.2 `old_role`/`new_role` log fields.
- **M4.C instruction ⛔ LIFTED + ISSUED:** issue-gate checklist cleared in-file; CREATE list +`ReauthOutcome.java`/`CapabilityRemovalReason.java`; MODIFY list +`PermanentIntegrationException.java`; error-handling +2 rows; invariants line → 29/§24–§34; tests +`OutcomeEnumsTest`/`PermanentIntegrationExceptionTest`/`CapabilityRemovalReasonTest`/`CredentialRotatorDefaultTest`; +**G13** (PermanentIntegrationException pre-state gate); G12 post-change expectation = 8 declared methods. All STOP-gate anchors re-verified at `e76b925` via the review's G3 pass; HEAD unchanged at issue (session documents-only).
- **No new Open Risks; no deferred build gates (documents-only).** **Uncommitted (Nick host-git):** see the cross-agent CURRENT POINTER for the file inventory.
- **Next forward:** (1) Nick commits hivemind + docs (host git); (2) **Coder executes M4.C** (instruction live — the refuse-to-close lifts); (3) Nick build gate; (4) PM WUCP Phase 2 → **M4 COMPLETE** → M5 planning; (5) W23 Goal 4 (website/docs track) remains open in parallel.

## Prior Session — Workstream C Briefing: AMD-54..64 Block Authored + DOCS Review Prompt + Gated M4.C Instruction (2026-06-05)

**Mode-1 Architect (documents-only; no code; HEAD stays `e76b925`; watermark stays AMD-53 until block ratification).** Freshness preflight at session start: **PASS** — HEAD `e76b925` committed and consistent across `git log`/PROJECT_SNAPSHOT/backlog row/W23; W23 IN_PROGRESS; Open Risks all RESOLVED (< 7 days); coder pointer = "BLOCKED on the C instruction" (valid refuse-to-close); dual-skill `diff -rq` both empty; strategy-map spot-check clean. Two micro-reconciliations folded into this session's edits (allowed under PASS): the pm-handoff `BS2SHA` placeholder → `e76b925` (the sed list at B-S2 closeout omitted this file), and the backlog header re-anchored `e73e199`→`e76b925`. Every source shape Read-tool-verified at `e76b925` (standing rule: in-sandbox `git`/`grep` used only for discovery, never as the source of record).

- **The integration AMD block is AUTHORED — AMD-54..64, eleven files, all PROPOSED** (`homesynapse-core-docs/design/amendments/`). Assigned contiguously from the live watermark (AMD-53) per P2 §4/§6/§8; the REC-41 hooks-vs-schema split resolved as **two full integers** (AMD-54 config-schema versioning, AMD-55 hooks) per P2 §8.1 — dependency-ordered, schema model first. Mapping: 54 REC-41-schema/NQ-2; 55 REC-41-hooks; 56 REC-43 `AUTH_FAILED`; 57 REC-42 `HealthDetail`; 58 REC-44 five lifecycle permits (5→10, dot-namespaced `integration.*`, legacy snake_case five frozen, three-way registration lockstep); 59 REC-47 capability events + `CapabilityPublisher` + `DiscoveryServices` per NQ-3/4; 60 REC-45 `SecurityServices`/`CredentialRotator` per NQ-1; 61 REC-46 `softDependencies`; 62 REC-48 `BackoffParameters` + the NQ-5/6 restart-intensity Javadoc fold; 63 REC-50 `IsolationLevel` (v2-plan correction folded: NEW field, no `isolationHint` rename); 64 REC-51 `plannedRestartTimeout`. REC-49 stays REJECTED (no integer); REC-52 → M9. Aggregate surface: `IntegrationDescriptor` 8→14, `IntegrationContext` 10→12, `RequiredService` 3→5, lifecycle permits 5→10 + 2 capability events — **zero `module-info`/Gradle changes across the block** (every new type lands in already-exported packages; every foreign type rides existing `requires transitive` edges — source-verified).
- **Seven review-flags (R1–R7) mark the honest gaps:** the Research 6 *return* document is not on disk (only the assessment) **and is NOT in the DOCS Project's knowledge either** (its knowledge = the `homesynapse-core-docs` repo connector only; Nick-corrected 2026-06-05) — so **Nick pastes the return INLINE at review dispatch**, and the `HealthDetail` 12 values (R4), the REC-44 permit names/fields (R5), and the exact hook signatures (R2) are **PM-reconstructed, pending verbatim fidelity diffs against that inline copy** (prompt amended: missing/truncated inline return ⇒ the reviewer returns INCOMPLETE on those gates, never reconstructs). **R6 and R7 are PRE-CO-SIGNED by Nick (2026-06-05, this session — deliberation done; formal co-sign at ratification):** **R6** — AMD-59's persisted payload drops the NQ-3 sketch's `Class<? extends Capability>`/`ts` fields (persisted events are pure data — a `Class<?>` token is a replay/serde liability; a payload timestamp would violate envelope-owns-time), adds `EntityId` (forced by the NQ-4 call: `Entity.capabilities` is untargetable from `DeviceId` alone), carries the full `CapabilityInstance` (replay self-sufficiency); the permit-class identity survives only at the `CapabilityPublisher` API level. **R7** — AMD-60 reuses the config `SecretEntry` vocabulary via string-based `rotate(...)`, no `SecureCredentialBundle` (the reuse-existing-types lesson applied). The review's G2 verifies soundness, not preference. R1 (options-vs-config semantics deferred) and R3 (REC-43 exception-type question) remain ordinary review items.
- **DOCS-Project review prompt READY:** `context/instructions/2026-06-05_AMD-54-64_DOCS_Project_Review_Prompt.md` — per-AMD verdicts, G1 fidelity diffs (quote-back rule), G2 PM-refinement confirms, G3 source spot-checks, G4 cross-block coherence, G5 invariant registrability; ratified-NQ decisions fenced as not-reopenable.
- **Gate B discharged at masthead level:** Doc-05 currency note added (block pointer; body-fold of §3.7/§3.8/§3.14/§4.1–§4.4/§8 at ratification mechanics). Gate-status note flipped historical. **Doc 02 §11.2/§3.10/§4.2 Stage-2 currency fold remains queued for ratification mechanics** (not done this session — deliberately bundled with the block fold per the handoff chain).
- **Gated M4.C coding instruction DRAFTED:** `context/coding-instructions/M4.C_Integration_API_Freeze.md` — full house format (12 STOP gates incl. caller-survey + shape-test greps, verbatim module-info embeds, three-way lockstep, §4c arch-rule test-`Clock` reminder, the `CapabilityAdded` serde STOP-and-report, test-first suite list). **⛔ Issue-gated:** do NOT hand to the Coder until AMD-54..64 are RATIFIED, review deltas are folded, and shapes are re-verified vs the then-current HEAD (the ⛔ checklist is at the top of the instruction).
- **Trackers:** backlog M4.C row (PLANNED — GATED) + header re-anchor; W23 progress entry (Goal 3 masthead-discharged); cross-agent CURRENT POINTER rotated; PROJECT_SNAPSHOT updated. **No new Open Risks; no deferred build gates (documents-only).** **State at handoff (Nick-confirmed):** homesynapse-core committed AND pushed at `e76b925` (origin/main current); this session's hivemind + docs edits to be committed by Nick from host git next, then review dispatch.
- **Next forward:** (1) **Nick dispatches the DOCS-Project review** — prompt + the eleven AMD files **+ the Research 6 return pasted INLINE** (the DOCS Project does not hold it; prompt §Inputs amended accordingly); (2) review returns → PM folds deltas (R4/R5/R2 verbatim corrections; INCOMPLETE gates loop back to Nick for the missing inline content) → **Nick ratifies** (R6/R7 pre-co-signs formalized); (3) **ratification mechanics session** — invariants §24+ registration, watermark AMD-53→AMD-64, nav-index rows, Doc-05 body-fold, Doc 02 §11.2/§3.10/§4.2 Stage-2 currency fold, re-verify instruction shapes vs HEAD, lift the ⛔ gate (it holds exactly as authored until then — Nick directive 2026-06-05); (4) **issue M4.C** → Coder → build gate → WUCP Phase 2 → **M4 COMPLETE**; (5) W23 Goal 4 (website/docs track) remains open in parallel.

## This Session — W23 "Mon+Tue" Batch: Reconciliation + M4.B-S2 Issued + NQ-1..6 Resolved (2026-06-04)

**Mode-3 Director.** Freshness preflight at session start: **STALE** (Check 1 — PROJECT_SNAPSHOT session-log table lagged the five 05-31 closeouts and the footer cited `76288af`/2026-05-22; Check 8 — superseded 05-31 cross-agent notes unarchived; backlog header behind `e73e199`) → **reconciled** (session-log rows appended, footer re-anchored `e73e199`/2026-06-04, backlog header updated, superseded notes moved below `## Archived`) → re-run **PASS** (Check 9 dual-skill `diff -rq` both empty; no skill files touched this session). HEAD `homesynapse-core` `e73e199` unchanged all session — documents-only.

- **Goal 1 (PM half) DONE — M4.B-S2 coding instruction AUTHORED + ISSUED:** `context/coding-instructions/M4.B-S2_AMD44_EntityRole.md`. AMD-44 §2.5 Stage 2: `EntityRole` enum; `EntityType` 6×3 legality matrix (`legalRoles` ctor + `allows()` + `legalRoles()` accessor, `Set.copyOf` immutability; **existing constant order preserved** — the AMD code-block order differs from source, content governs); `Entity` 11→12 components + `ProposedEntity` 3→4 (null→PRIMARY coercion **before** the construction-time matrix guard, DP-1 per the AMD-47-INV-04 structural-guard precedent; defensive collection copies per the B-S1 pattern; back-compat 11-arg/3-arg convenience ctors → zero existing-caller breakage). Every shape Read-tool-verified at `e73e199`; G8 caller survey (Entity: EntityTest/TestEntityFactory/StubIntegrationContext; ProposedEntity: none); 8 STOP gates; §4c arch-rule reminder (device-model + platform-api non-whitelisted); `entity_profile_changed` oldRole/newRole confirmed **taxonomy-level only** (no Java record — `EventTypes.java:107` constant untouched; Doc 02 §11.2 fold = PM task at closeout). **TypedIdTest `FloorId` fold-in bundled** (W23 carry-item; closes the B-S1 [INFO] follow-up). NO `module-info`/Gradle/`projectionVersion` change. Build gate defers to Nick (commands in the instruction; track at B-S2 closeout).
- **Goal 2 DONE — Research 6 NQ-1..6 RESOLVED:** Nick adopted **all six PM leans** (NQ-1 `SecurityServices` aggregator; NQ-2 `descriptorSchemaVersion` rename + `configSchemaMajor/Minor`; NQ-3 sealed `Capability` class + `CapabilityInstance` identity; NQ-4 no new SQLite table — project into `Entity.capabilities`; NQ-5 reject REC-49; NQ-6 keep 1/60s + pre-M9 spike). Recorded: Research 6 assessment addendum; **OQ-05-05 → RESOLVED**; gate-status note **Gate A CLEARED** (NQ-3/4 pre-decisions finalize at B-S2 landing). **Workstream C now gates only on B-S2 landing + the Doc-05 pass at C-briefing** (W23 Goal 3 queued there per gate-status §3). AMD block re-bases 54+ at C-briefing.
- **Next forward:** (1) Coder executes M4.B-S2 (Claude Code) → Nick build gate → **PM WUCP Phase 2** → Workstream B COMPLETE; (2) **brief Workstream C** (assign AMDs from 54+, freeze vs post-B model, fold Doc-05); (3) W23 Goal 4 — website/docs track standup (posture A parallel lane). Host commits pending: this session's hivemind edits + the still-untracked W23 plan/roadmap files (outside `395bf74`).

**Last updated:** 2026-06-05 — **M4.B-S2 (EntityRole) COMPLETE: PM WUCP Phase 2 → APPROVE; build GREEN (26+38+143); COMMITTED `e76b925`; WORKSTREAM B COMPLETE.** Same day: Research 7 v2 (A−) + Research 12 (A) assessed; NQ-1..7 RATIFIED (https override); SPIKE-DC scheduled ~Aug. **Next forward = Workstream C briefing (fresh PM Mode-1 session): author the integration AMD block from 54+ (content = Research 6 dispositions + ratified NQ-1..6, frozen against the shipped 12-/4-component Entity/ProposedEntity shapes) → DOCS-Project ratification review → mechanics + Doc-05 fold + Doc 02 §11.2/§3.10/§4.2 Stage-2 currency fold → C coding instruction.** Full record: cross-agent-notes CURRENT POINTER + PROJECT_SNAPSHOT. Prior: 2026-06-04 (W23 Mon+Tue batch). Prior: 2026-05-31 (**M4.B-S1 (AMD-44 Stage 1 — Floor/`FloorId`/Area + `FloorRegistry`/`AreaRegistry` interfaces + `AreaId` Javadoc + `Set<HardwareIdentifier>`) COMPLETE + COMMITTED `e73e199`** — PM WUCP Phase 2 read all 9 created + 8 modified files vs source + AMD-44 → APPROVE; build GREEN (`:platform:platform-api:check` 38 + `:core:device-model:check` 26 + full `./gradlew check` 143); **no `module-info`/Gradle/`projectionVersion` change**; device-model 52→56, platform-api 12→13; Stage-2 fence held; 3 `[INFO]` deviations accepted; one minor follow-up logged (`FloorId`→`TypedIdTest`). **Next forward = cleanup pass + Workstream B Stage 2 (EntityRole, AMD-44 §2.5).** See the M4.B-S1 section below. Prior this session-chain: **M4.0b-5 (AMD-53 timestamp-model unifier) COMPLETE + COMMITTED `c99b425`** — PM WUCP Phase 2 read every changed file vs source → APPROVE; `projectionVersion` 4→5; watermark AMD-53; build GREEN; Workstream A complete — typed end-to-end AND event-time-deterministic. See the M4.0b-5 section below. Prior this session-chain: **Research 11 ASSESSED (A−) + AMD-52 AUTHORED + RATIFIED** — typed `StateChangedEvent` payload. Ratified by Nick + an independent HomeSynapse Core Claude Project review (F1 bit-anchored float identity / `chain_hash` inert; F2 Path B = defined `DegradedEvent`, no decode-path upcaster). PM applied ratification mechanics doc-only (no code; HEAD stays `98f705b`): watermark **AMD-51→AMD-52**; AMD-52-INV-01..07 registered `Architecture_Invariants_v1.md` **§22** (+ §0.3/§17/§18); Doc 01/03/04 masthead currency; nav-index AMD-52 row; state-store MODULE_CONTEXT pointer. **Next forward = the M4.0b-4 coding instruction** (UNBLOCKED — AMD-51 §2.7 String-payload freeze lifted; milestone id M4.0b-4 confirmed). Prior this session-chain: **OQ-05-08 AMD-52 design beat DONE → Research 11 dispatched** — AMD-52 authoring was GATED on Research 11 (serializer + replay-determinism forks) + Nick adjudication; the String `StateChangedEvent` payload stayed frozen until AMD-52 ratified. Docs produced this session: `homesynapse-core-docs/design/2026-05-31_AMD-52_Typed_Payload_Serializer_Replay_Design_Beat.md`, `context/instructions/Research_11_Typed_Event_Payload_Persistence_Brief.md`, `context/planning/2026-05-31_AMD-52_design-track-note.md`; trackers updated (milestone backlog M4.0b-4 PLANNED-GATED, research-agenda Research 11 addendum). Prior: **M4.0b-3 (AMD-51) COMPLETE + COMMITTED `98f705b`** — independently reviewed → APPROVE; `projectionVersion` 3; watermark AMD-51; Workstream A COMPLETE. Earlier this session-chain: AMD-51 AUTHORED + RATIFIED; Research 10 RATIFIED; **M4.B3 COMMITTED `60b4185`**; Research 4 FULLY RESOLVED (v4); P4r AMD-47 RATIFIED; M4.0b-2 COMPLETE `7610296`.)


---

# ROTATED 2026-06-11 (hygiene pass, M6.2-return session) — session records 2026-06-06 → 2026-06-10 (late), from `context/handoff/pm-handoff.md`

## Prior Session — Research 13 Assessment → M6.4 + M6.2 Instructions AUTHORED (2026-06-10, late)

**Mode-3 Director session (Cowork) per the pinned-state session brief (`context/handoff/archive/2026-06-10_research13-return_M6.4-M6.2_session_prompt.md` — consumed + archived this session). Documents-only in the hivemind + ONE file added to the docs repo (the archived return); homesynapse-core untouched. Sandbox VM UP — bash/diff available again.**

**Freshness preflight at session start: PASS** (all 11, spot-verified against the brief's PINNED STATE — one glance per check as designed). **Check 9 ran REAL this session** (`diff -rq` both skill trees: byte-identical — Nick's external sync confirmed). **One pinned-line divergence investigated per the brief's protocol:** the pinned state said the M6.1 instruction "lives ONLY at archive" / the `f9a551c` commit message says "tombstone removed" — but `git show --name-status f9a551c` shows status **M** on the live path: `git add -A` **committed the 9-line tombstone instead of removing it**. Content accurate (points to the archive), so not CONFLICTED — **deleted in-tree this session**; rides the closeout commit. Web-fetch spot-verification of external quotes (ADR-0021) was attempted but the fetch tool timed out — external evidence graded on citation specificity per prior-assessment practice.

- **Research 13 return assessed → A−** (`context/assessments/2026-06-10_Research_13_PM_Assessment.md`; raw return archived per research-agenda §2 Step 0 → docs `research/returns/2026-06-11_Research_13_Config_System_Market_Superiority.md`). Quote-back gate HONORED (module-info character-exact vs HEAD). **Per-REC:** 130/132/133 ACCEPT → M6.4 test obligations; **131 NARROWED** (Locked Doc 06 §3.5 already documents comment loss + mandates the pre-first-write `config.yaml.bak.{ISO8601}` backup mitigation — the obligation is the mitigation, NOT an emitter); **134 RE-BUCKETED → ALREADY-COVERED** (Doc 06 §3.6 pins `additionalProperties:false`@WARNING; shipped `9035110` — the lone table defect); **135 NARROWED** (crash-window test yes; NO new diagnostic event — AMD-70 scope fence); 136 → FUTURE-AMD (reference-integrity is genuinely open; the pass cannot live in `config` — JPMS); 137/139 → M10/M11/Doc 13 parking; **138 → FUTURE-AMD with the now-or-never claim SOFTENED** (per-event `schema_version` + upcast machinery means deferral doesn't foreclose; §4.4 already sketches `trigger`; actor identity awaits auth); 140 ACCEPT → M6.2. REJECT bucket genuinely empty — endorsed. **PM-surfaced tension the return couldn't see:** Locked Doc 06 §12.4 ("payloads never carry configuration values") vs the frozen value-bearing `ConfigChangedEvent` record → resolved by ruling [R4] below; reconciliation queued with REC-138 (one FUTURE-AMD family).
- **M6.4 instruction AUTHORED — ISSUE-READY** (`context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md`): `reload()`/`write()` un-staged on `StandardConfigurationService`; volatile-swap atomicity (DP-1) + ONE ReentrantLock (DP-2); the §3.3 pipeline order with reject-and-keep-prior-good (C5); AMD-66 invocation matrix incl. throwing-listener-rejects + unannotated→PROCESS_RESTART; `ConfigChangeSet` diff + the §4.3 filter methods; §3.5 write protocol with the stale-token rejection + first-write backup; **R2** §3.7 step-7 migration write-back + `.pre-migration-v{M}.{m}` backup with a crash-window test; **R1** per-ERROR `config_error` startup publish (existing `ConfigErrorEvent`, DIAGNOSTIC metadata, `"(none)"` null-default rendering, **§12.4 x-sensitive fence** — `"[REDACTED]"` message); `ConfigSectionReloadedEvent` (AMD-70 §2.1 payload verbatim, type-residency-clean) with the **FULL P2 fan-out** (54→55, 23→24, 35→36, registry/warmup/Javadoc/samples + mandated Coder survey re-run); **CoreSchema-on-re-parse pinned as DP-12** (route through `YamlLoader`). 8 STOP-gates; both module-infos embedded verbatim; NO module-info/Gradle/projectionVersion change anywhere. **Rulings for Nick at dispatch: [R4]** (reload publishes `config.section_reloaded` ONLY; `ConfigChangedEvent` stays registered-but-unpublished) + **[R5]** (DIAGNOSTIC/SYSTEM/null-eventTime metadata, the validation_completed ruling extended). Folds REC-130/131(n)/132/133/135(n).
- **M6.2 instruction AUTHORED — ISSUE-READY** (`context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`): **CARRY 1 E2 bridge embedded VERBATIM** + zero-new-edge re-verified at authoring (persistence ∌ config / config ∌ persistence / app ⊇ both — all three module-infos embedded verbatim; Coder re-verifies at execution); `ScopeKeyManager` public-iface + package-private impl (`.root-key` SecureRandom 0400 lazy, **hand-rolled RFC-5869 HKDF-SHA256** w/ RFC test vectors — NO new catalog dep, `javax.crypto` is `java.base`), wrapped DEKs, `config_secrets` scope (Doc 15 §7.3 shared-root unification); `PayloadCipher` consumer-defined in persistence (`java.base`-only signatures), app-adapter bridge threaded through `HomeSynapseCore`, HELD until M6.3; `SecretStore` 4→5 (`setAll` AMD-68 §2.1 verbatim + §2.2 temp-fsync-rename-fsync-dir) + AMD-16 backup rotation; `!secret`/`!env` stage-3 tag constructors (CoreSchema kept; LTD-15 no-value-in-messages; FATAL-collected, `!env VAR:default` form); Doc 15 §8.2 **freeze-gate encrypt→destroy→unreadable test**; explicit warning off the stale §8.1 "implement PayloadCipher" row. **Ruling for Nick at dispatch: [R6]** — `scope_keys` = config-side `scope_keys.json` per Locked §3.8's ownership sentence; the §4.1 CREATE TABLE placement is structurally unreachable from config in BOTH directions under zero-new-edge (worked through at authoring) → Doc-15 currency nit parked with the §8.1 nit for the next Doc-15-touching amendment. Folds REC-140. Crypto fences hold: NO M6.3 scope, AMD-69 untouched.
- **Sequencing recommendation:** **M6.4 executes FIRST** — both WUs modify `StandardConfigurationService`; M6.2's G7 STOP-gate adapts to whichever state it finds, but the reload/write rewrite is the bigger delta and M6.2's threading is the more rebasable. Nick's call at dispatch.
- **Housekeeping:** M6.1 tombstone deleted (the `f9a551c` `git add -A` miss); consumed session prompt → `context/handoff/archive/`; research-agenda Research-13 addendum → RETURNED + ASSESSED; cross-agent CURRENT POINTER rotated; snapshot/backlog/W24 updated; snapshot Latest-commit bumped to `7e0bce8` per the pinned-state instruction. WUCP drift check: snapshot/backlog/W24/charter-rows/pm-handoff/cross-agent all agree (M6.4 + M6.2 ISSUE-READY; M6.3 triple-gated; no open deferred gates). Check 9 unaffected (no skill files touched).
- **Next forward:** (1) **Nick: commit nexsys-hivemind + homesynapse-core-docs** (messages in the session report; core untouched). (2) **Nick: rule [R4]/[R5]/[R6] (defaults stand unless vetoed) → dispatch M6.4**, then M6.2. (3) PM: WUCP Phase 2 on each as they land. (4) FUTURE-AMD queue when convenient: REC-136; REC-138 + `ConfigChangedEvent`/§12.4 reconciliation. Nick-paced unchanged: Pi-4 microbench (OQ-15-2 — **must not slip past M6.2**), interviews, **M5-C floor (P6 — still not started; Research 13's superiority material is website-grade input)**, B2 C8/C9.

## Prior Session — Sha-Reconciliation + WUCP Hygiene + Research 13 Scoping (2026-06-10, follow-up)

**Cleanup + research-scoping session per Nick's two-part task brief (documents-only across 3 repos; no code, no coding instructions issued). Sandbox VM down all session — every read/edit via the file tools (the authoritative path); git state read directly from `.git` files.**

**Freshness preflight at session start: STALE** (Check 3 — git HEAD `9035110` ["M6.1a: YamlLoader…", on `b7bc65c`] vs the snapshot's `6c6dd33`/"commit pending"; Check 4 — backlog "sha to fill"; all other checks PASS; Check 9 UNVERIFIABLE — VM down, evidence-based PASS, no skill files touched since the 2026-06-09 clean check) → **the allowed retroactive reconciliation = exactly Part 1 of the brief** → re-run **PASS** (recorded above the Part-2 work). Also confirmed from `.git/logs/HEAD`: Nick's hivemind closeout commit `40de3ec` landed ~3.5 min before core `9035110` — which is why the sha couldn't be filled at the EOD closeout.

- **Part 1.1 — Step-0 sha-reconciliation DONE:** `9035110` filled into coder-handoff (Deferred Build Gate status + `### DEFERRED — M6.1a` gate line), pm-handoff Open Risks (**M6.1 build-gates entry CLOSED — no open deferred build gates**), PROJECT_SNAPSHOT (masthead/current-state/next-action/Latest-commit/session-log), backlog (M6.1 row + masthead re-anchored to `9035110`), M6 charter status line, W24 last-verified (Lane-2 stretch goal MET in full), cross-agent CURRENT POINTER (Nick's commit action struck DONE).
- **Part 1.2 — Archival pass DONE:** cross-agent-notes — the `## Archived` separator relocated so ONLY the CURRENT POINTER stays active; the resolved 2026-06-07→06-10 trail (M5-A/M5-D/M6-gate/M6.1) preserved verbatim below it. Consumed M6.1 instruction → `context/coding-instructions/archive/M6.1_Config_Pipeline.md` with an EXECUTION RECORD appended (+ DP-4/DP-5/STOP-gate supersession markers); **a tombstone remains at the live path — the VM being down means file tools cannot delete; Nick: `git rm` the tombstone at commit.** pm-handoff — pre-M5-window session records rotated per the monthly convention: 2026-06-04/05 → **`archive/pm-handoff-2026-06.md`** (new), 2026-05-30/31 → **`archive/pm-handoff-2026-05.md`** ("Rotated 2026-06-10" section); the legacy M3-era sections below are MARKED for the next rotation (left in place — they're entangled with reference sections).
- **Part 1.3 — Docs-lane corrections DONE (homesynapse-core-docs, one commit):** Doc 06 §3.2 dated correction note (`schema/`→**`schemas/`** per ratified AMD-71 §2.1; flags the §9 example's `schema_output_path` too); Doc 06 §3.7 **amendments-in-force banner** (embedded code predates AMD-67: `(major,minor)` pair, 5-method `ConfigMigrator`, 6-component `MigrationPreview`, `MigrationResult` unchanged per E67-1, `ConfigChange`→`MigrationChange` rename, step-7 write-back/backup deferred to M6.4 per R2); Doc 01 §4.4 **+1 mapping row** (`config.validation_completed` → `system`, the 35th EventCategoryMapping entry, dated); `research/homesynapse-core-cloud-scalability-analysis.md` §1.4 **staleness note** (config no longer filesystem-agnostic — designated consumer per AMD-71/Rule-5 ruling 2026-06-10; AMD-71-INV-01 containment as compensating control; cloud argument survives in adjusted form). **Doc 15 untouched** (the §8.1 nit stays parked). Bonus currency fix: config `MODULE_CONTEXT.md` Phase-3 M6.1 status line → gates GREEN/committed (tiny homesynapse-core docs-only commit).
- **Part 1.4 — preflight re-run: PASS** (Checks 1–8/10/11; Check 9 evidence-based — Nick spot-checks mirrors at next sync). Recorded before Part 2 per the brief.
- **Part 2 — Research 13 brief AUTHORED (dispatchable):** `context/instructions/Research_13_Config_System_Market_Superiority_Brief.md` — "Config System Market Superiority: UX Failure Modes + Runtime Robustness." **Two Nick rulings folded:** (1) **numbered Research 13** (the task brief said "Research 12", but Research 12 = the consumed Zigbee de-risk return, REC-120..129, already cited inside ratified AMD-59 — append-only register; REC block re-derived from research-agenda at authoring → **REC-130+**); (2) **dispatch → DOCS Project** (the disposition table's ALREADY-COVERED citations need the connector-resident AMD-66..71/Doc 06/Doc 15), with **web search REQUIRED** for RQ1/RQ2/RQ5 and the **stated fallback**: split evidence-gathering to a generic deep-research run if needed — the disposition pass is never the generic run. Brief mechanics: §0 quote-back gate (R7v2 counter-measure); verbatim config `module-info.java` at `9035110` + the charter/MODULE_CONTEXT ratified-machinery register embedded (Research-6 discipline); RQ1 HA failure catalog / RQ2 cross-ecosystem Mom-Test (Groovy retirement = calibration case) / RQ3 runtime non-breakage taxonomy → gap table vs ratified machinery / RQ4 scale+layout churn (AMD-44/AMD-17) / RQ5 local-cloud-hybrid with now-or-never vs deferrable classification; mandatory REC + disposition table (ALREADY-COVERED / M6.2-or-M6.4 INSTRUCTION OBLIGATION / FUTURE AMD / POST-MVP UI-or-cloud / REJECT); guardrails (AMD-66..71/Doc 06/Doc 15 inviolate; no M6.3 crypto creep; the three pinned M6.4 obligations not duplicated/contradicted); §7 LIGHT. research-agenda.md: Research 13 addendum + numbering note registered.
- **Next forward:** (1) **Nick: commit the three repos** (messages in the session report — hivemind cleanup+brief; docs corrections; core MODULE_CONTEXT line; `git rm` the M6.1 tombstone in the hivemind commit). (2) **Nick: dispatch Research 13** to the DOCS Project. (3) **PM: assess the return** (6-step; disposition-table fold) → **author the M6.4 instruction** (pinned obligations: R1 per-ERROR `config_error` publish; `config.section_reloaded` P2 survey re-run; CoreSchema on the reload re-parse; + any INSTRUCTION-OBLIGATION rows from Research 13) **and the M6.2 instruction** (E2 bridge verbatim; zero-new-edge re-verify; `!secret`/`!env` stage-3; AMD-68 `setAll`). M6.3 stays triple-gated. Nick-paced: Pi-4 microbench (OQ-15-2 — must not slip past M6.2), interviews, **M5-C floor (P6 — still not started)**, B2 C8/C9 ratification.

## Prior Session — M6.1 COMPLETE (whole): M6.1a + Gate-Fix APPROVE (2026-06-10 EOD)

**SESSION DELTA (later 2026-06-10 — M6.1a executed, gate-failed 2×, gate-fixed, GREEN):** Nick issued M6.1a (fresh Claude Code session); first gate run FAILED twice — `YamlLoaderTest.nullValuedKeysDropped` (snakeyaml-engine's DEFAULT scalar schema is the JSON schema, where `~` = the string `"~"` not null; LTD-09 → explicit `CoreSchema` required on BOTH LoadSettings) and `HomeSynapseArchRulesTest.noDirectFilesystemInCore` (Rule 5 was stale vs ratified AMD-71 — written when config was interface-only; PM RULED: drop `com.homesynapse.config..` from Rule 5; AMD-71-INV-01 canonicalization-containment, `ConfigLayoutTest`-pinned, is the compensating control). Both PM-diagnosed from source, ruled, gate-fix round 1 applied by the Coder, **re-run GREEN (27 + 147 tasks) → M6.1 COMPLETE (M6.1b `b7bc65c` + M6.1a commit pending)**. **Phase 2 contract verification:** module-info = the ruling EXACTLY (closes the §7-embed CARRY's verification half); Gradle lockstep ✓; CoreSchema both settings ✓; Rule 4 untouched ✓. **R1/R2/R3 ruled ACCEPT** (R1 per-ERROR `config_error` publish → M6.4 obligation; R2 migration write-back/backup → atomic-write seam; R3 fire-on-FATAL-pass = faithful AMD-70). **Carries:** docs lane — Doc 06 §3.2 `schema/`→`schemas/`, §3.7 pre-AMD-67 embed, cloud-scalability-analysis :56, DP-5 prose; M13/composition root — D4 `SystemId` must exist BEFORE config load (config publishes system-subject events at startup step 1). **PM-lesson logged below:** the M6.1 instruction's watch-outs missed `NO_DIRECT_FILESYSTEM_IN_CORE` — the P2 consumer/pin survey must include the `HomeSynapseArchRules` package lists whenever a module gains a NEW behavioral posture (first filesystem I/O, first Jackson, first time access); and third-party-library *semantic* defaults (engine schema, charset, locale) need the same source-verification discipline as API signatures. **Commit messages handed over (core M6.1a single commit; hivemind closeout).** _The morning record below stands._

## Earlier This Session — M6.1b WUCP Phase 2 → APPROVE + M6.1a Ruling → UNBLOCKED (2026-06-10 morning)

**Mode-3 Director session (Cowork): WUCP Phase 2 review of the Coder's M6.1 execution + the `[BLOCKING]` escalation adjudication. homesynapse-core working tree (M6.1b, layered on `6c6dd33`) reviewed; gate already GREEN (Nick, 2026-06-10: targeted 57 tasks + full `./gradlew check` 147 tasks); commit pending — messages handed to Nick (3 repos).**

**Freshness preflight at session start: PASS** (Checks 1–8/10 PASS; **Check 9 UNVERIFIABLE in-session** — the sandbox VM failed to start so `diff -rq` could not run; treated as evidence-based PASS: last clean check 2026-06-09 and no skill-tree files touched by the Coder session or this one — **Nick: spot-check the mirrors at next sync**; Check 11 = the source-verification below). Sandbox down also means all reads/edits ran through the file tools (the authoritative path per the standing rule); zero sandbox git use.

- **M6.1b review → APPROVE (source-verified vs the ratified AMDs, not the completion report):** config `module-info.java` **byte-identical** to `6c6dd33` (STOP-gate held); `ConfigurationChangeListener` = AMD-66 §2.1 **verbatim** (plain/non-generic/non-sealed, F7); `ConfigModel` 6-component with `major>=1`/`minor>=0` guards + `Map.copyOf` + both halves of the AMD-54↔AMD-67 cross-ref Javadoc (AMD-67-INV-01); `ConfigMigrator` 5-method with the AMD-67-INV-02 major-only-trigger Javadoc; `ConfigValidationCompletedEvent` = the E70-1 **flattened** payload (`Map<String,Integer>` keyed by `Severity.name()` strings — no config type in event-model; sum==issueCount guard; tab-indented per module style); `EventCategoryMapping` +1 `[SYSTEM]` **sibling-consistent** (beside CONFIG_CHANGED/CONFIG_ERROR — PM CONFIRMS the category); `ScopedConfigurationAccess` package-private, snapshot semantics, INV-CE-02 zero-config empty section. P2 pins (53→54, 22→23 ×2, 34→35 ×4) empirically closed by the GREEN gate; `HomeSynapseCore`/`IntegrationTestHarness` `Stream.of` aggregation verified un-pinned/untouched.
- **`[BLOCKING]` escalation UPHELD + RULED (Nick, 2026-06-10 — APPROVE):** the no-new-`requires` mandate was physically uncompilable for M6.1a (snakeyaml-engine 2.9 = explicit JPMS `org.snakeyaml.engine.v2`; json-schema-validator 1.5.6 = explicit `com.networknt.schema` requiring jackson-databind NON-transitively; module-path resolution proven by persistence's precedent). **Ruling:** `requires org.snakeyaml.engine.v2; com.networknt.schema; com.fasterxml.jackson.core; com.fasterxml.jackson.databind; org.slf4j;` — all non-transitive, package-private consumers only — plus `implementation(libs.jackson.databind)` + `implementation(libs.slf4j.api)` (catalog entries to be checked at implementation). The [AMD-71-A] zero-new-HomeSynapse-edge property is untouched. **Docs-lane corrections AUTHORED this session** (homesynapse-core-docs): dated ruling-correction notes above the §7 embeds in AMD-66/67/68/70/71 + the AMD-66 §2.4 `List`-ctor correction.
- **2 `[REVIEW]` ruled:** (1) DP-4 — **ACCEPT `List<ConfigurationChangeListener>` ctor** (Nick confirmed); the AMD-66 §2.4 `Map` param made its own duplicate-rejection contract unenforceable; service builds the map, throws on duplicates; the `ConfigurationChangeListener` Javadoc registration line updates at M6.1a (deliberately NOT touched post-gate — no code edits after a GREEN gate without a re-run). (2) `config.validation_completed` publish metadata — **CONFIRM DIAGNOSTIC priority** (ConfigErrorEvent precedent), `EventOrigin.SYSTEM`, null `eventTime`, `publishRoot`, system subject; exercised at M6.1a. **4 `[INFO]` accepted** (incl. the E67-1 `MigrationResult` no-touch — correct conditional reading; and the sum==issueCount defensive guard — degrades to `DegradedEvent` per codec design).
- **RULING ADDENDUM appended** to `context/coding-instructions/M6.1_Config_Pipeline.md` — supersedes the conflicting Success-Criterion (3)/STOP-gate rows; it is the complete M6.1a issuing delta. **Strategic assessment delivered to Nick** (local-first→hybrid-cloud posture + dual-audience UX): the M6.1 contract set is regret-proof for the cloud-hybrid future (immutable atomically-swapped `ConfigModel`, AMD-67 version/migration machinery, `fileModifiedAt` concurrency token, flattened observability events, secrets kept local-resident + separately encrypted); no rework required; provenance/multi-writer semantics consciously deferred post-MVP.
- **Closeout applied:** coder-handoff gate flipped RESOLVED-GREEN/commit-pending + next-WU → M6.1a UNBLOCKED; PROJECT_SNAPSHOT masthead/current-state/next-action/days(170→168)/session-log row; backlog M6.1 row; W24 Lane 2; M6 charter status; cross-agent CURRENT POINTER rotated; Open Risks below (gate entry + the §7-embed CARRY). Check 9 re-run impossible (VM down) — no skill files touched this session; flagged to Nick.
- **Next forward:** Nick commits the 3 repos (messages in the session report) → **issue M6.1a** (Coder reads the RULING ADDENDUM first). Then M6.4 (P2 survey re-run for `config.section_reloaded` at issue), M6.2 (E2 bridge; zero-new-edge re-verify), M6.3 last (triple-gated). Nick-paced: Pi-4 microbench (OQ-15-2 — must not slip past M6.2), interviews, **M5-C floor (P6 risk — still not started)**, B2 C8/C9 ratification.

## Prior Session — AMD-66..71 Ratification Mechanics → M6 Entry-Gate CLOSED (2026-06-09)

**Mode-1 governance-mechanics session (Nick-authorized ratification, per `context/instructions/2026-06-09_AMD-66-71_Ratification_Prompt.md` — now archived). Documents-only; homesynapse-core HEAD `6c6dd33` untouched; Doc 06 and Doc 15 bodies untouched; watermark unchanged at AMD-87. No code, no coding instructions authored, no verdict re-opened.**

**Freshness preflight at session start: PASS (all 11).** Baselines matched expected: core `6c6dd33`, docs `aedff55` (all 7 review edits pre-folded), hivemind `da9d2f4` (review return on disk). Check 9 dual-skill `diff -rq` clean on both trees (Nick's 2026-06-09 sync confirmed). **Sandbox-truncation hazard CONFIRMED live this session** (the review return served as 0 bytes through the sandbox mount; file-tool read complete) — the §1.1 standing rule held: file tool authoritative, zero sandbox git writes, Nick commits from host. **Untracked `coder.zip` + `project-manager.zip` sit in the hivemind root** (skill-sync residue) — flagged for Nick, not deleted.

- **Ratification executed (Nick, 2026-06-09):** **AMD-66 RATIFIED** (RATIFY-AS-IS; [AMD-66-A] `PROCESS_RESTART` fallback ENDORSED); **AMD-67/68/70/71 RATIFIED** (RATIFY-WITH-EDITS; E67-1/2, E68-1, E70-1/2, E71-1/2 all pre-folded at docs `aedff55` — nothing further folded this session); **AMD-69 DEFERRED — Nick confirmed Option (a)** (Tier-2/OQ-15-3; the number stays reserved; OQ-15-3 stays closed). Authority chain: review return `context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md` (block RATIFY-WITH-EDITS; verified at core `6c6dd33`) → PM edit-fold (docs `aedff55`) → Nick's ratification nod (2026-06-09).
- **Mechanics applied:** the six AMD files (mastheads → RATIFIED/DEFERRED with the return pointer; §9 checklists ticked with dates; §10 dispositions closed on the AMD-54 pattern; AMD-69 §7/§8 closed with the locked confirmation text). `Architecture_Invariants_v1.md`: **§37–§41 registered** (§37 preamble introduces the five-section block + the AMD-69 no-invariant deferral note; all 8 statements verbatim from the AMD §7s; implementing WUs named M6.1/M6.2/M6.4) + §0.3 (+5 `AMD-NN-INV` families) + **§17 (+8 rows)** + §18 (+5 rows mirroring the AMD-54-block style) in the same commit. Nav-index: +6 amendment rows (66/67/68/70/71 RATIFIED 2026-06-09; 69 DEFERRED Tier-2/OQ-15-3), §Amendments preamble re-stated (reserved → filled; **watermark line unchanged AMD-87**), Doc 06 row Notes mirrors the Doc 05 style. Gate lifts: M6.1 instruction ⛔→**READY TO ISSUE** (AMD numbers pinned; Fork-2 issue-whole note embedded); M6 charter entry-gate → CLOSED; AMD-68 half of M6.2's gate satisfied (still gated on M6.1 landing). Trackers: backlog (M6.1 UNBLOCKED/READY TO ISSUE; M6.2 note; M5-B B1 ✅), snapshot, coder-handoff next-WU, cross-agent CURRENT POINTER, W24 progress, KB ledger (Current_State / Knowledge_Primer / Decisions_QR / Invariants_QR +8 / Navigation_Index). Bounded stale-sweep run on the §4.6 grep set only; both consumed instruction prompts archived to `context/instructions/archive/`.
- **⚠ Invariant-count regeneration (Check 11 finding, this session):** the §17 Invariant Index total was a copied-forward running count that had drifted **9 below the table** — stated "135 invariants / 34 categories" vs **152 identifier rows / 41 §0.3 prefix categories** actually on disk pre-/post-this-block (the −9 offset traces back through the "133" and "104" statements). Per the source-round-trip rule the total line is now **regenerated from the table: 152/41** (KB Invariants_Quick_Reference re-based to match). Nick: eyeball-confirm at commit; downstream docs citing "135/34" (historical session-log rows) were left as historical record.
- **Verification pass (§5 of the ratification prompt):** §7 verbatim `module-info.java` embeds in all six AMDs re-confirmed byte-identical to `config/configuration/src/main/java/module-info.java` at `6c6dd33` (single `requires transitive com.homesynapse.event;`, single export); the 8 registered statements diffed verbatim vs the AMD §7 texts; section numbering contiguous §36→§37–§41; nav-index has no "reserved for the M6 config block" residue and the watermark line still reads AMD-87; AMD-70's ratified text retains the E70-1 flattened payloads (`Map<String,Integer>`, `String appliedClassification`) + type-residency rule; no code, no Doc 06/Doc 15 body edits, no watermark change; WUCP drift check — snapshot/backlog/pm-handoff/charter/M6.1 status agree (M6.1 READY TO ISSUE everywhere).
- **What stays parked:** **M6.3 triple gate** (OQ-15-2 microbench + AMD-86 §3 interview signal + OR-M6-NONCE — see Open Risks); **Doc 15 §8.1 `ScopeKeyManager`-row nit** (carry, below); **M6.4 P2-survey obligation** for `config.section_reloaded` (carry, below); **B2 C8/C9** independent review + ratification; **M5-C floor not started** (P6 risk — stand up a defined increment this week); M5-D interviews/microbench/spikes Nick-paced.
- **Next forward:** **issue M6.1** (fresh session or this one on Nick's green light — instruction READY, issue whole per Fork-2). Then M6.2 (E2 bridge — re-verify zero-new-edge at issue), M6.4 (survey re-run), M6.3 last (triple-gated). **Commit:** two host-git messages handed to Nick in the session report (docs ratification commit + hivemind closeout commit).

## Prior Session — M5-D Evidence Lane: three artifacts AUTHORED (2026-06-07)

**Cowork PM session (M5-D evidence lane, in parallel with M5-A Part 2 on Claude Code). Documents-only; HEAD homesynapse-core `8028337` unchanged; Locked Doc 15 untouched.**

**Freshness preflight at session start: PASS (all 11).** Source-verified against the actual repos (Read tool + greps; sandbox git treated as non-authoritative per the standing rule). HEAD `8028337` = PROJECT_SNAPSHOT Latest Commit; Doc 15 **Locked**, watermark **AMD-87**, AMD-86 + AMD-87 **RATIFIED** (all source-confirmed from the docs repo, not the state docs); Check 9 dual-skill `diff -rq` **clean** on both trees; `Expectation.java` = the 4 permits (ExactMatch/WithinTolerance/EnumTransition/AnyChange), `@Disabled("AMD-65 pending")` present at `EventPayloadCodecTest.java:317`; all 21 MODULE_CONTEXTs populated (config at `config/configuration/`, not `core/`). **One micro-reconciliation folded under PASS** (precedent: allowed): the W24 charter "## Current state" line read a stale "watermark AMD-64 (→87 at ratification)" — corrected to **AMD-87** (source = nav-index). Two benign housekeeping advisories logged (not STALE): W23 (its final ISO day) still IN_PROGRESS while W24 is the active charter — due a COMPLETE flip at next closeout; cross-agent superseded 06-05/06-06 pointers due for archival (within 48 h).

- **Three M5-D deliverables AUTHORED (PM produces; Nick/hardware/Claude Code run) — all in `context/assessments/`:**
  - **(1) Pi-4 AES-256-GCM write-path microbench spec + per-category decision rule** (`2026-06-07_M5-D_Pi4_AES-GCM_write-path_microbench_spec_OQ-15-2.md`) — **the priority deliverable; resolves OQ-15-2 and gates the M6 at-rest-encryption WU.** Runnable spec: what to measure (per-event AES-256-GCM encrypt at the Doc 15 §3.2 write-path point, Pi-4 intrinsics-off, warm/cold, across the candidate categories' projected write rates), methodology (JMH per-event cost + fixed-corpus throughput soak; Pi-4-deployment-profile JVM flags; payload sourcing from `TestEventSamples`/AMD-52 typed payload; write-rate derivation), and the **pass/fail decision rule** per category against the §10 budget (~30–60 µs/event, ≥500 ev/s sustained) — encrypt-on-write vs plaintext-at-rest fallback. **Candidate set = `identity` + `presence_personal` only** (Doc 15 §9); **energy/telemetry is plaintext-at-rest at MVP** (Doc 15 §3.4 governs — reconciled against the strategy docs that lumped energy as "sensitive"). Result writes back into the Doc 15 §9 `encrypted_scopes` default + §3.4 list as a **TUNING, not a Doc 15 re-open**; explicit STOP-and-escalate if a result implies a design change.
  - **(2) Energy + erasure interview guide** (`…_M5-D_energy-erasure_interview_guide.md`) — Part A = the **AMD-86 §3 verifiable-erasure re-scope-up trigger** (Mom-Test questions framed so a qualifying "yes" — a launch-window buyer requiring *operational* crypto-shred / provable per-scope erasure with a retained audit log, go/no-go to sign — is unambiguous; ≥1 qualifying YES → escalate to **re-open AMD-86 via the formal pipeline BEFORE M6 freezes the write path**; the interviewer never edits AMD-86/Doc 15). Part B = the **D4 energy-shape questions** (shape now / features later) → the regret-proof C9 energy event shape; decision + threshold per question; grounded in the data-readiness spec §4 + the institution report Part 3, not invented personas.
  - **(3) sd_notify transport decision matrix + recommendation** (`…_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`) — see OR-M13-SDNOTIFY below.
- **Guardrail honored:** Locked Doc 15 stays inviolate — the microbench tunes the encrypted-scope *list*; only the erasure interviews can re-open AMD-86, via the formal pipeline. **No docs-repo files touched this session** (verified). **No code, no coding instructions, no amendments.**
- **Closeout applied:** W24 charter M5-D lane updated + watermark straggler fixed; OR-M13-SDNOTIFY resolution path updated with the matrix + recommendation; backlog M5-D row updated; PROJECT_SNAPSHOT + cross-agent pointer; Check 9 re-run **clean** (no skill-tree files touched this session). **Commit:** one nexsys-hivemind message provided in the session report (Nick host-git).
- **⚠ Working-tree note for Nick (sandbox git is non-authoritative):** the sandbox `git status` shows substantial **pre-existing** modifications on the skill trees (`coder/`, `project-manager/`), several `context/` trackers, and the **docs repo** (Doc 15/AMD-86/AMD-87/nav-index/invariants) that are **NOT from this session** — they are the prior session's host-git state showing as sandbox/host desync (confirmed not pure EOL churn, but content is source-verified-correct and Check 9 is clean). **Reconcile against host-git truth.** This session's *real* additions = the 3 new `context/assessments/` files (untracked) + the surgical tracker edits listed above.
- **M5-A Part 2 WUCP Phase 2 review — DONE → APPROVE (later this session, after Nick pasted the Claude Code output + GREEN build).** HEAD unchanged at review (`8028337`; Part 2 uncommitted) → no full preflight re-run. Source-verified the **7 changed `.java` files vs AMD-87 §2 + the standalone instruction (NOT the completion report):** interface-keyed `{"t":…}` tagged union; exhaustive no-`default` serializer switch over the 4 permits (`ExactMatch`/`AnyChange` delegate `v` via `provider.defaultSerializeValue`/`ctxt.readTreeAsValue` to the registered `AttributeValue` codec; `EnumTransition` String; `WithinTolerance` reuses `AttributeValueSerializer.writeDouble`/`readDouble`); **AMD-87-INV-01** (per-permit round-trip + bit-identity, and the **enabled** `capabilityAdded_onOff_roundTrips` GREEN → decodes to the real event, not `DegradedEvent`); the lone `persistence requires com.homesynapse.device` edge **acyclic** (device requires value/event/platform only); `@Disabled` **deleted not re-numbered** + `TestEventSamples` AMD-65 Javadoc retired; **no `@JsonTypeInfo`/domain annotations** (Rule 10), **no `projectionVersion`/migration/shape change**. **3 `[INFO]` adjudicated, all accepted:** (#1 `requires`-without-direct-Gradle-dep) ACCEPT — transitive path structurally stable (state-store's `requires transitive device` is load-bearing for its own API); optional hardening = add `implementation(project(":core:device-model"))`, documented as a MODULE_CONTEXT gotcha, NOT blocking; (#2 DisplayName AMD-65→AMD-87) ACCEPT; (#3 `WithinTolerance` text-form not literal long) ACCEPT — correct AMD-52 reuse, "<bits>" = the guarantee. **Nick ran the gate GREEN (147 tasks) + COMMITTED `7f44bed`** → coder-handoff gate flipped **RESOLVED `7f44bed`**; **M5-A COMPLETE (Parts 1+2+3); M9 command-bearing-`CapabilityAdded` prerequisite CLEARED.** Check 9 clean. Commit messages handed over (homesynapse-core + nexsys-hivemind). **Next forward = the M5-D evidence Nick runs (critical path to M6); M6 parked until its entry-gate closes.**

## This Session — M5-A Part 1+3 COMPLETE (GREEN) + Gate-Fix + Both M5-Window DOCS Reviews Returned + WUCP Phase 2 (2026-06-07, latest)

**Cowork PM session (review + source-verification + Step 0 closeout). homesynapse-core M5-A Part 1+3 COMMITTED `8028337` (on `8ef9e9f`); working tree clean.** Freshness preflight at session start: **STALE** (the hivemind stopped at the 2026-06-06 "authored, pending commit" state; since then the docs commits landed, M5-A executed + went GREEN + was committed `8028337`, and both M5-window DOCS reviews returned) → this session ran the retroactive WUCP Phase 2 **Step 0 sha-reconciliation** that restores PASS, then source-verified all foldable edits and **STOPPED twice on the missing full review return** (not in message / not on disk) rather than fold from the summary. **Then — same session — Nick placed the verbatim review on disk (`context/audits/2026-06-06_Doc15_AMD-86_DOCS_Review_Return.md`); the full fold + ratification was executed:** Doc 15 E1 (all 3 occurrences + semantic comment) + E2 (composition-root bridge) folded and **Doc 15 LOCKED**; **AMD-86 + AMD-87 RATIFIED**; INV-PD-07 narrowed + INV-PD-03 at-rest posture + AMD-86-INV-01/AMD-87-INV-01 registered (§35/§36; 135/34); nav-index rows + **watermark AMD-64→87**; **M5-A Part 2 UN-GATED** (Claude Code); the m2 counter-nonce hazard carried as **OR-M6-NONCE**. See the 2026-06-07 cross-agent pointer + the ratification detail bullet below.

- **M5-A Part 1+3 COMPLETE — full `./gradlew check` GREEN (147 tasks).** Part 1 = platform-systemd deployment impls (`LinuxSystemPaths`/`LocalPaths` → `PlatformPaths`; `SystemdHealthReporter`/`NoOpHealthReporter` → `HealthReporter`) behind a package-private `NotifyTransport` seam; Part 3 = `FloorId` Jackson registration in `PersistenceJacksonModule` (10→11 typed wrappers) + `FloorIdSerializationTest` — closes the B-S1 FloorId-serde OPEN item. **Part 2 (AMD-87 Expectation codec) correctly NOT done — ⛔ gated on AMD-87 ratification.**
- **Gate-fix round (1 round-trip):** first gate run failed `-Werror [exports]` ×4 — the exported platform-systemd impls expose `PlatformPaths`/`HealthReporter` as supertypes but `module-info` required `com.homesynapse.platform` **non-transitively**. **Root cause = the M5-A instruction embedded a non-transitive `requires` verbatim (PM defect); 3rd occurrence of this exact pattern (M2.9, M3.6e.1, M5-A).** Fix = the house lockstep `requires transitive com.homesynapse.platform` ↔ `api(project(":platform:platform-api"))` (+ SHOULD-FIX `UlidSerializationTest` 10→11 incl. FloorId; + OPTIONAL clearer M13 exception in the real transport). Re-run GREEN; round-trip-safety prediction (moduleGraphAssert allows the edge; slf4j/test-support not graph-checked) held.
- **Both M5-window DOCS reviews RETURNED → RATIFY-WITH-EDITS** (PM assessment: verdicts correct; the crypto foundation + strategic calls held under independent review; all edits foldable — no redesign, no security overstatement):
  - **Doc 15 + AMD-86 (FULL, J1–J7):** **F-A PASS** ("most honestly stated at-rest crypto posture" — no overstated claim); D2 / minimal-AMD-86 / Doc 06 + AMD-60 / m1 / m2 [BLOCKING-for-M6-impl] all PASS. **Edits:** **E1** `encrypted_kek`→`encrypted_dek` (§4.1 schema + comment + §8.2 `ScopeKey`) — a real schema-naming bug the review caught that I had missed; **E2** §3.8 `PayloadCipher` wiring stated precisely (lean: composition-root bridge — cycle-proof both directions; or one-way `config→persistence` edge). **AMD-86 itself clean** (no required edits).
  - **AMD-87 (LIGHTWEIGHT, P4):** P4 classification + 4-permit codec fidelity + edge/scope fences all PASS. **Edits:** **E1** §7 watermark "65"→"87"; **E2** `@Disabled` text → "AMD-65 pending" (matches HEAD); **E3** INV-GA-02 citation nit (I cited INV-GA-02 for AMD-number reuse; it governs `INV-XX-NN` — cite the P2 ledger instead).
- **PM ruling — sd_notify transport (Coder §6 pushback ACCEPTED + correct):** pure OpenJDK 21 has no AF_UNIX `SOCK_DGRAM` (`DatagramChannel.open(UNIX)` throws at runtime; JEP 380 = stream only). The `NotifyTransport` seam is the right call for M5-A (protocol semantics unit-tested; compile + tests GREEN). **Real AF_UNIX-datagram transport DEFERRED to M13** (composition root owns reporter construction + `$NOTIFY_SOCKET`); the mechanism choice (JNR/JNA native binding vs `systemd-notify` subprocess) **folds into M5-D evidence** (entangled with the GraalVM native-image decision). No non-catalog dep / subprocess added. → **NEW OR-M13-SDNOTIFY.**
- **Closeout applied (this session, in progress):** Open Risks — OR-PD-07-AMD updated (Doc 15 + AMD-86 review RETURNED RATIFY-WITH-EDITS; resolution now pending edit-fold + ratification); M5-A deferred gate logged RESOLVED (GREEN, 147 tasks); NEW OR-M13-SDNOTIFY. PROJECT_SNAPSHOT brought current. **Remaining (this session):** coder-handoff gate flip; W24 weekly + backlog lane rows; pm-lessons; the reorg pass (Tier B). Dual-skill mirror: **this PM session touched NO skill-tree files** (only docs + `context/`), yet a closeout `diff -rq` shows the **`nexsys-project-manager` source ahead of its `.claude/skills` mirror on 8 files** (CLAUDE.md, SKILL.md, 6 references) — Check 9 was CLEAN at session start, so an **external project-manager skill-update ran mid-session** (the source now reads `last-verified: 2026-06-07 against 8028337`, value-model-aware, M5-window-aware; the mirror is stale at `25bc23b`/2026-05-20; cf. `context/audits/2026-06-07_skill-update-audit.md`). This was source-ahead / mirror-sync pending (STALE-class, not corruption), unrelated to this ratification. **RESOLVED 2026-06-07:** Nick committed the skill refresh (`04761c9`) + manually synced the mirror; the closeout `diff -rq` is now **clean on BOTH `project-manager` and `coder` trees (Check 9 PASS)**.
- **PM/process lessons:** (1) PM must embed `requires transitive` ↔ `api` in coding-instruction module-infos whenever an exported impl class exposes another module's type as a supertype/return — the `[exports]`/-Werror consequence; **P-class retro proposal:** deferred-gate may defer the FULL check, but the Coder should run a targeted `compileJava` on -Werror-sensitive touched modules before handoff (would have caught all three occurrences in ~20s). (2) The DOCS review earned its keep on the E1 schema catch.
- **Next forward (post-ratification — all of (1)/(2)/(3) DONE this session):** **(1) M5-A Part 2 → Claude Code** — un-gated; scope re-verified vs `8028337` (persistence→device acyclic; 4 permit shapes match; `@Disabled` removal; FloorId already in Part 3 so Part 2 adds only the `Expectation` serializers; retire `TestEventSamples:315` Javadoc). **(2)** the **E2 `com.homesynapse.app` composition-root-bridge** wording + the **m2/OR-M6-NONCE** counter-nonce hazard fold verbatim into the eventual **M6** coding instruction; **AMD-66–71 (M6 config) now UNHELD** (Doc 15 Locked). **In parallel (Nick's pace):** M5-C website/docs floor; M5-D energy/erasure interviews + the Pi-4 AES-GCM write-path microbench (OQ-15-2) + the sd_notify mechanism spike (OR-M13-SDNOTIFY). **Commit:** two messages provided below (homesynapse-core-docs Lock+ratify; nexsys-hivemind closeout) — Nick host-git.
- **Commit status (2026-06-07):** homesynapse-core M5-A Part 1+3 **COMMITTED `8028337`** (`git log` HEAD confirmed; working tree clean). This session's hivemind Step-0 sha-reconciliation is UNCOMMITTED — Nick host-git. The **AMD-87 DOCS review return is correctly filed** at `context/audits/2026-06-06_AMD-87_DOCS_Review_Return.md` (verified present this session; the earlier "misfiled at `context/assessments/`" note is stale — no dup remains there). The **Doc 15 + AMD-86 full review return is STILL not on disk** → must be captured verbatim to `context/audits/` (Nick paste) **before** any fold/ratify — the edit-fold, AMD-86/AMD-87 ratification, Doc 15 Lock, and Part 2 un-gate are all GATED on it.

## This Session — Post-M4 Synthesis + M5-Window Charter (decisions D1–D8) (2026-06-06)

**Documents-only governance session. No code, no coding instructions, no amendments authored. HEAD `8ef9e9f` unchanged; M4 COMPLETE; working tree clean.**

**Freshness preflight: PASS (all 10).** Forward work unblocked.

- **Deliverable 1 — post-M4 synthesis:** `context/planning/2026-06-06_post-M4-plan-and-M5-scoping.md`. Decision-support built on the M4 retro + the two 2026-06-06 assessments (foundation-readiness; language-replatform + its velocity follow-on) + a source-verified deep read of the candidate-next surface. **Key reframing (the headline finding):** the originally-planned M5 (Platform API + test-support) is **thinner than it looked** — `TestClock`/`SynchronousEventBus`/`NoRealIoExtension` (test-support) and `InMemoryEventStore` (event-model testFixtures, 27-method contract-tested) **already shipped in the M1.x wave**, so the test-support force-multiplier the next-piece rec rested on is already banked. The real leverage is the design/decision work that is either **regret-proof** (the schema-irreversibles — `actorRef`/energy-events/payload-typing — safe under both the stay-Java and go-Rust futures) or **gates M6** (the crypto entry-gate). Source-verified this session (file:line): `EventEnvelope.actorRef` bare `Ulid` (`:112`); `EventTypes` zero energy types (energy rides `telemetry_summary :185`); config `SecretStore` interface-only, zero impls, narrow flat namespace (no `rotate`/scoping); INV-PD-07 `:427` MVP-mandate vs the crypto Draft §5.2 Tier-2 phasing (a direct, unreconciled contradiction).
- **Decisions D1–D8 (Nick):** `context/decisions/2026-06-06_post-M4_M5-window_decisions.md`. Adopt **Alternative 3 — the blended, lane-tracked M5 window** (D1), chartered to de-risk M6, sized to Nick's gate, tracked as **four first-class lane pieces M5-A…M5-D, not one milestone** (P1). **D2 diverges from the synthesis default:** operational crypto-shredding → **POST-MVP**; build the key-management infrastructure + scope categories + at-rest encryption at MVP; reconcile via a **minimal INV-PD-07 amendment, full DOCS review (not P4)**. **PM assessment: endorsed D2, did not flip it** (operational shred has no MVP consumer — the immutability-vs-erasure conflict is itself post-MVP; the crypto/transparency differentiator is strategy-scoped as horizon, not a launch claim; opportunity cost on critical-path M6 is real) **+ added the write-path-encryption boundary refinement** (crypto-shredding can only destroy data written encrypted-per-scope; the log is immutable; so the now-or-never call is *from what point sensitive categories are envelope-encrypted-on-write* — the crypto owner-doc must pin this, recommended default = no per-category erasure feature at MVP / whole-install reset / write-path encryption switches on at first off-device flow). D3 non-Core floor ADOPTED; D4 energy shape-now-features-later ADOPTED; D5 language-spikes-ride-the-window, window doesn't wait on the language call ADOPTED; D6 AMD-65 in M5-A ADOPTED; D7 verification-foundation quick-scope-first, don't rebuild doubles ADOPTED; D8 adopt P1/P2/P3/P6 now ADOPTED.
- **Deliverable 2 — W24 charter:** `context/planning/weeks/2026-W24_jun08-jun14.md`. The four lanes with done-whens + gate-capacity sequencing (Nick-gated items leverage-ordered: M5-B/B1 the M6 entry-gate first). M5-A trimmed small; Lane B trimmed to the M6 entry-gate (C6/C7/C12) + regret-proof schema decisions (C8/C9/C10); C11/C13/C18 deferred just-before-consumer.
- **Closeout applied (WUCP-style):** decision register written; backlog "M5 Window" subsection + 4 lane rows added (+ M5/M6 rows updated, masthead re-dated); PROJECT_SNAPSHOT masthead/next-action/open-flags/days(173→172)/session-log updated; this handoff record; OR-M4-WT cleared; NEW Open Risk OR-PD-07-AMD logged. Dual-skill mirror clean. Preflight re-run: **PASS.**
- **Next forward — B1 IN PROGRESS; M5-A authored (advanced 2026-06-06):** **Authored, pending review + commit:** B1 = **Doc 15 Cryptographic Architecture (Draft)** + **AMD-86 (PROPOSED**, INV-PD-07 narrow + INV-PD-03 posture, full DOCS review); **AMD-87 (PROPOSED**, Expectation codec, lightweight block-track — reassigned from the retired AMD-65 per **INV-GA-02**, see ledger note below); **M5-A coding instruction issue-ready** (`context/coding-instructions/M5-A_Platform_Impls_AMD87_FloorId.md`); **two DOCS review prompts** (`context/instructions/`). **Nick's actions:** commit homesynapse-core-docs (Doc 15 + AMD-86 committed; AMD-87 new) + nexsys-hivemind; dispatch the FULL review (Doc 15 + AMD-86) + the LIGHTWEIGHT review (AMD-87); hand M5-A to Claude Code (parallel, no crypto-review dependency); focus M5-C (website/docs floor) + start M5-D (energy/erasure interviews + Pi-4 spikes incl. the AES-GCM write-path microbench). **HELD:** AMD-66–71 (M6 config) until the Doc 15 review settles §7.3. **Full WUCP Phase 2 closeout deferred to ratification** (nav-index rows, invariant text, watermark→87, Doc 15 Locked). M9 supervisor ~late-Jul/Aug; SPIKE-DC pre-M14 (~Aug).
- **Ledger note (INV-GA-02 — AMD numbering):** AMD-64/AMD-65 stay **RETIRED** (Research 5 v2; see the Research-5 ledger entry below). The **Expectation codec is reassigned 65→87** (AMD-86 = INV-PD-07/crypto; AMD-87 = next clean, confirmed free). Historical mentions of "~AMD-65 (Expectation codec)" in the M4.C records and the live `@Disabled("AMD-65 pending")` test annotation are the *retired* number in frozen context — the active amendment is **AMD-87**; M5-A deletes the stale annotation.
- **Commit pending:** Doc 15 + AMD-86 committed by Nick (verified); **AMD-87 (docs repo) + the M5-A instruction + AMD-87-renumber edits + these handoff/snapshot/charter/backlog updates (hivemind) are UNCOMMITTED** — Nick host-git commit (two repos).



---

# ROTATED 2026-06-12 (post-ratification hygiene pass) — sessions below were live-file records through 5d280b2

## Prior Session — R14-A/R14-B/R15 Returns ASSESSED + Merged Disposition + Charter POPULATED (2026-06-12, later)

**Mode-3 Director session (Cowork, same-conversation continuation of the brief-authoring session — Nick dispatched the three briefs and returned all three same-day; shas `776fe2d` hivemind / `e5ea76f` core reconciled at start). Serialized assessment per the architecture doc: fold order R14-B → R14-A → R15, one return per pass.**

- **All three quote-back gates HONORED; zero returns discarded; zero fabricated identifiers across the cycle** (both R14 runs were DOCS-connector-blind — declared per protocol; the verbatim-embed discipline held). All three assessed **A−**.
- **R14-B (engineering, folded first):** 15 RECs → 10 M7-obligations (5 PM-narrowed) + 2 M8 + 2 FUTURE-AMD + 1 REJECT (no engine retry — anti-requirement). The W1–W8 crash-window vocabulary enters the M7 carry-pin namespace. PM source-verified Doc 07 §3.10/§3.11 before ruling — the Locked text is RICHER than the embeds (zombie-Run finalization; per-idempotency-class crash recovery; event-driven confirmation), converting two "OPEN" framings to CLOSED-UNTESTED.
- **R14-A (community/UX, folded second):** 15 RECs → 6 M7 + 1 M8 + 5 ALREADY-COVERED + 1 FUTURE-AMD + 1 POST-MVP + 1 REJECT (no templating DSL). PM verification re-bucketed REC-147 (drop DIAGNOSTICs already at Doc 07 :270; INFO already default) and narrowed REC-141/145 (all four "why didn't it fire?" details already Locked-specified; traces are log events — no ring buffer exists). **The §3.3 ledger-gap dossier (frenck "out of scope… a level UP" + installed verify-retry hacks) is the M5-C flagship of the cycle.** REC-153 split (153a M7 validation ⊕ REC-136; 153b FUTURE-AMD indirection).
- **R15 (strategic, folded last):** register fence HELD (zero code obligations). The researcher ran generic-deep-research (strategy files inaccessible), marked ALL bucketing provisional, and self-flagged the §0.2 re-run mandate — **PM executed the in-Project re-run** (files 1–2 in full + docx exec summaries via python-docx): 13/15 buckets confirmed; **REC-173's licensing verdict FLIPPED — Apache 2.0 (file 2, LOCKED) is CONFIRMED by the fork-pattern evidence** (governance-posture refinement survives); **REC-182 re-bucketed FUTURE-PARKING → M5-C** (the competitor-pain match exists in R14-A's RQ2 evidence + Six-Battlefields B1/B3/B5); REC-180 narrowed to a segment-messaging rule (file 3 explicitly pursues mainstream). One PM-observed tension logged for Nick: file-4 data-value framing vs file-2 "no data monetization, ever" — needs one explicit reconciliation line at the strategy refresh.
- **Merged disposition (THE blueprint authority):** `context/planning/2026-06-12_M7-blueprint_merged-disposition.md` — one table REC-31..40 + 141..185; 7 collisions adjudicated (33⊕143⊕144⊕161 one AMD item; 36⊕158; 153a⊕136; 182←R14-A; 147 re-bucket; 173 flip; anti-requirements register 151/155/162/181). **AMD block = SIX families F1–F6 (the AMD-66..71 sizing class). M7 holds at 3 pieces (P1 passes); event vocabulary distributes per piece — the dedicated-piece fork is CLOSED.** Spikes: deadline calibration (161), storm threshold (157), recovery herd (170/M8). Research high-water **REC-185**.
- **Charter POPULATED** (same file, status flipped): gate row 1 → content enumerated; M8.2/M8.3 populated; §2b of the merged disposition governs per-piece obligations. **M5-C content backlog now evidence-rich** (merged disposition §2e: 6+ website-grade items + 3 copy guardrails).
- **Housekeeping:** raw returns archived → docs `research/returns/2026-06-12_*` (3 files; were untracked in hivemind — clean cross-repo move); consumed briefs → `instructions/archive/`; research-agenda addendum flipped to CYCLE COMPLETE.
- **Next:** (1) **PM (next governance session): DRAFT the M7 entry-gate AMD block** (6 families per merged disposition §2a) + the DOCS review prompt; strategy-refresh drafts (§2f) can ride the same session or follow. (2) **Nick:** C8/C9 ratification (C8 = entry-gate row 2, now carrying R14-A's Hubitat evidence); the M6.3-vs-M7 ordering call at the W25/W26 boundary (Lane-2 evidence gates); M5-C Increment 1 (Lane-4 gate — the §2e backlog is ready); mirror sync (Check 9, unchanged); delete the renamed index.lock file (still present). (3) Coder: unchanged — M6.3 only when triple-gated clear; M7.1 awaits the 5-row entry gate.

## Prior Session — W0 Currency Delta + R14/R15 Briefs AUTHORED + M7/M8 Charter Skeleton (2026-06-12)

**Mode-3 Director session (Cowork). Preflight vs the session-brief PINNED STATE: STALE (Check 9 only — the one pinned mirror file, expected until Nick's sync; Check 10 had one drift, fixed in-session: strategic-context-map cited the rehomed `phase-2-block-backlog.md` at its old live path). All chartered work delivered.**

- **Step-0:** consumed `2026-06-11_M6.2-return_W25-charter_session_prompt.md` → `handoff/archive/`. Rollover (Jun 14/15) not reached — no W24/W25 flip. Hivemind HEAD at session start was `48d66a7` (Nick's commit of the session prompt + reconciliation — expected; pinned `d161ca9` is its parent).
- **W0 Research-4 currency delta COMPLETE:** `context/assessments/2026-06-12_W0_Research_4_Currency_Delta.md`. All Research-4 claims re-verified at `7c73c91` (permits 9/7/8/6, `RunContext` verbatim, `ActivateSceneAction` empty, advancer in tree, `actorRef` bare Ulid). **DQ escalation list is EMPTY** (all four resolved at v4, re-verified clean). **REC-39 re-anchored with 5 obligations** — headline NEW finding: the type-residency rule (`RunId`/`RunStatus`/`PendingStatus` cannot appear in event payloads — JPMS cycle; flatten-or-relocate = an M7 AMD-block decision). C8 folded as a hard M7 entry-gate row.
- **Three briefs AUTHORED + self-reviewed ×3** (each: §0 quote-back gate, verbatim automation module-info at `7c73c91`, inventory summary, Doc 07 anchors scoped per register, minimum read set, REC range + 6-bucket disposition table, guardrails): `context/instructions/Research_14A_Automation_UX_Failure_Modes_Brief.md` (REC 141–155, DOCS Project, Mom-Test register) · `Research_14B_Automation_Runtime_Robustness_Brief.md` (REC 156–170, DOCS Project, engineering register) · `Research_15_Ecosystem_Positioning_Scan_Brief.md` (REC 171–185, generic-deep-research evidence permitted, disposition in-Project, CANNOT mint code obligations). Self-review caught + fixed: a wrong permit count copied from MODULE_CONTEXT ("27" → re-derived **30**; **the MODULE_CONTEXT source defect corrected in core** — docs-only change, see commit note), a dangling Doc-15 §13.4 anchor in R14-B, a quotable-out-of-context "no telemetry" phrase in R15.
- **M7/M8 charter SKELETON:** `context/planning/2026-06-12_M7-M8-charter-skeleton.md` — 3 first-class M7.x rows (trigger/condition path · run/action/dispatch path · ledger) + provisional M8.1–M8.3, the 5-row entry-gate (AMD block · C8 RATIFIED · M6.3-vs-M7 ordering call at W25/W26 · Lane-4 structural gate · MODULE_CONTEXT/§4c currency), the 5 standing carry pins, population markers for synthesis.
- **Environment (lesson-grade, folded into cowork-environment-model.md):** the VM mount served TRUNCATED FILE TAILS this session — `git status` showed phantom modifications in BOTH repos (files cut mid-token, e.g. `pr`, `Nick autho`); host files verified intact via file tools. Treat VM git status/diff as suspect under lag; host file tools are truth. Also: a VM `git status` created `.git/index.lock` in nexsys-hivemind that the VM cannot delete (permission-gated) — **neutralized by rename to `.git/index.lock.cleared-by-cowork-2026-06-12`; Nick: delete that file at convenience.** VM died mid-session (M6.4 precedent); file tools carried the session.
- **Next:** (1) **Nick: veto-or-default the research architecture + dispatch all 3 briefs simultaneously** (R14-A/B → DOCS Project; R15 venue per its masthead). (2) Commit both repos (messages handed over in the session report; core = the MODULE_CONTEXT count fix, docs-only, the `7e0bce8` precedent). (3) Returns → serialized assessments (fold order B→A→15) → populate the charter + draft the M7 AMD block. (4) Standing Nick-paced: Pi-4 microbench (Wed Jun 17 target) + interviews (the M6.3 gates), M5-C Increment 1 (the Lane-4 gate), C8/C9 ratification, mirror sync (Check 9, still the one file).

## Prior Session — M6.2 WUCP Phase 2 → APPROVE + Step-0 + Hygiene Pass + R14/R15 Architecture (2026-06-11)

**M6.2 return processed: gate GREEN (82 targeted + full 147, ZERO gate-fix rounds — an M6 first), PM Phase 2 → APPROVE, R-1/R-2/R-3 ACCEPT, COMMITTED `7c73c91` EOD (hivemind `d161ca9`).** Full verification + rulings record: the M6.2 entry under `

---

# Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; source: git `58794d7`)

## This Session — Return ASSESSED → Edits FOLDED → **AMD-88..93 + C8/C9 RATIFIED** → Mechanics COMPLETE (2026-06-12, the AMD-block conversation continued)

**Mode-2 governance session (Cowork — ran in the ORIGINAL AMD-block conversation at Nick's direction, superseding the parked fold session below; the author-≠-reviewer independence held because the REVIEW ran externally in the DOCS Project).**

- **Return assessed (grade A−):** full coverage — all 10 §A items + every [R*] flag ruled; complete §C disagreement register; §D steelman. Docked: one fabricated enum in a suggested edit (`EventOrigin` quoted as 4 values {DEVICE, USER, AUTOMATION, SYSTEM}; source = 7 {PHYSICAL, USER_COMMAND, AUTOMATION, DEVICE_AUTONOMOUS, INTEGRATION, SYSTEM, UNKNOWN}) and one false conditional (E92-3 — Locked §3.7.1 **:342** DOES enumerate the `cascade_depth_exceeded` payload). Neither affected a verdict; both caught at PM source-verification — the fold-side discipline working in both directions.
- **Folds:** E88-1 (two-surface breaking characterization) · E88-2 (offset sign convention) · E88-3 (the `ReachabilityTransition`→`targetAvailability` deviation documented) · E89-1 (empty-`includedRoles` → §6.1 FAILURE class) · E91-1 (reconstruction-source pin: event log or in-process `RunContext`, NEVER the windowed §4.5 map) · E92-1 (rows 15 AND 16 gain `triggerId` — row-16 = PM ruling per reviewer recommendation) · E92-2 (grep-attestation checklist row) · EC8-1 (Glossary §5.4 + INV-MU-01 check added to C8's edit set) · EC8-2 (origin-consistency rule, source-corrected enum). **E92-3 DECLINED-WITH-CITATION.**
- **Attestations (all three recorded in the artifacts):** (1) module-info embeds byte-checked against disk this session ✓; (2) zero-publish-site grep CLEAN at `e5ea76f` (only the 2 record definitions + the EventTypes roster in main code) ✓; (3) C8 anchors re-derived at `7c73c91`: `EventEnvelope:112` HOLDS; `SqliteEventStore` write site now `:329–332`; hydrate anchor re-stated in the Doc 01 banner ✓. Bonus verification: INV-MU-01 carries NO `user_ref` wording — the EC8-1 check is a no-op there (Glossary only).
- **Nick RATIFIED ALL (AskUserQuestion, recorded).** Mechanics executed: 6 AMD statuses → RATIFIED + §9 ticks + §10 dispositions; **invariants §42–§47 (+11; §0.3 +6 categories; §17 index +11 rows, total REGENERATED → 163/47; §18 +6 matrix rows; 11 full-text entries)**; nav-index preamble + 6 RATIFIED rows; **watermark AMD-87 → 93**; **AMD-04 SUPERSEDED banner** in Design_Review_Amendments_v1 (rate-limiting formally NOT adopted); **Doc 07** amendments-in-force banner + §3.7.1 AMD-91 note + BOTH §6.2 CRITICAL→NORMAL corrections + §8.2 RunStatus 7-value currency note (the §C.8 pre-existing drift, fixed opportunistically); **Doc 01** banner + §4.3 family note + §4.5 supersession label; **Glossary §5.4 superseded-in-part**; C8/C9 record → RATIFIED; charter rows 1–2 → CLOSED; **all 5 spine files flipped** (invariants quick-ref → §42–§47 section + 163/47 masthead/footer).
- **⚠ ENVIRONMENT INCIDENT (contained, repaired, lesson logged):** a VM-side python bulk-edit of the six AMD files hit the truncated-tail mount artifact (read truncated → wrote truncated), clobbering the AMD tails ON THE HOST (§10 dispositions + checklists lost). Detected at the next host Read (AMD-88 ended mid-`module-info`, 94/118 lines). **Repaired by reconstruction from git `77d5e51` + the in-session fold record (every edit string verbatim in-conversation); all six files host-verified end-to-end (line counts match git + adds; one `RATIFIED by Nick` §10 per file).** NEW STANDING RULE (this is the second paid lesson in this artifact class): **bulk edits to governance/AMD files run HOST-SIDE (Edit/Write) or source from GIT OBJECTS — never VM read-modify-write of mounted worktree files.** Environment-model update rides the next hygiene pass.
- **M7.x instruction carries (logged for the M7.1/M7.2 authors):** the C1-interim pin (M7.1 registers but does NOT production-publish `automation_triggered` before M7.2 delivers the completing side — return §A.9); the chain-narrowing authoring note (intentional deep re-fire suppressed deterministically — return §D); the literal-`:`-in-plain-labels note (R89-2); REC-161 calibration spike pre-M7.3 (Nick); the M10 notes (ActorRegistry tolerates unresolvable historical refs; pre-C8 dev-log refs outside the closed set).
- **Entry-gate state: rows 1–2 ✅ CLOSED · row 3 ⛔ (M6.3-vs-M7 ordering — microbench + interviews) · row 4 ⛔ (M5-C Increment 1 — NOTHING blocks starting it now; the principal non-evidence blocker) · row 5 ✅.**
- **SAME-CONVERSATION ADDENDUM — post-ratification HYGIENE PASS + Project-instruction upgrade (Nick-directed):** (1) Shas reconciled: docs **`d7ea212`** / hivemind **`5d280b2`**, both pushed. (2) **Rotations (git-object-sourced per the new §5 rule):** PROJECT_SNAPSHOT 137K→107K (prior chains → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-12.md`); pm-handoff 66K→59K (R14/R15-era records → `archive/pm-handoff-2026-06.md`); cross-agent 65K→18K (below-separator → `archive/cross-agent-notes-2026-Q2.md`). ⚠ One in-pass repair: the first cross-agent cut matched the PROSE mention of "## Archived" instead of the heading — caught at host-verify, repaired from git; the line-anchored-heading corollary added to the environment model §5 alongside the bulk-edit source rule (the "rides the next hygiene pass" promise from the incident bullet above is now DISCHARGED). (3) **CORE + DOCS custom instructions UPGRADED** (research_mode sections: REC/disposition/quote-back/register-fence/Mom-Test-evidence/verbatim-vector/honesty + strategy-assumption-audit disciplines + the smart-home/IoT/AIoT teardown charter, feats-AND-failures; DOCS doc-count corrected 14→15; watermark-93/AMD-04-SUPERSEDED stated). (4) **Advisory (Nick decisions, NOT executed):** `homesynapse-core/spike/` = 1.4 MB consumed spike husk in the production repo — recommend `git rm` (results live in AMD-38/39 + docs research); DOCS Project connector — deselect `research/returns/` + `archive/` (~2 MB; the assessments carry the dispositive content; the biggest capacity lever); `phase-3-milestone-backlog.md` (76K) DONE-row compression queued for the next governance window; **`index.lock.cleared-by-cowork-2026-06-12` STILL PRESENT (fourth flag)**; Check-9 mirror sync pending (one file). (5) **Nick upload checklist: 5 spine files + BOTH custom-instruction files → both Projects.**

## Prior Session (superseded) — Bundled-Review FOLD ABORTED AT STEP-0: return not on disk (2026-06-14 brief; executed Jun 12 UTC)

**Mode-3 governance session (Cowork). THE SESSION'S MANDATORY INPUT IS MISSING: `context/audits/2026-06-DD_AMD-88-93_C8-C9_DOCS_Review_Return.md` is absent — host file tools AND VM mount agree; a full three-repo sweep (any `*return*`/`*review*`/`*C8*C9*` filename, all files newer than Jun 10) found no candidate. Only the dispatched prompt exists. Per the session brief: STOPPED after Step-0; hygiene-only; NOTHING folded, NO mechanics; watermark untouched at AMD-87; all six AMDs stay PROPOSED; C8/C9 record untouched.**

- **Step-0 vs PINNED STATE:** core `e5ea76f` ✓ (GREEN 147, pins 55/24/36, pv 5 — one glance each); hivemind HEAD **`ba1466d`** = Nick's closeout commit ✓ message-verified (sha reconciled via this record); untracked = ONLY the Jun-14 session prompt ✓; consumed Jun-13 prompt already in `handoff/archive/` ✓; rollover conditional: Jun 12 < Jun 15 → NO flip (W24 stays IN_PROGRESS); Check 9 STALE on exactly the pinned one file (`project-manager/references/coding-instruction-format.md`; coder mirror byte-identical) — expected, not abnormal.
- **DIVERGENCE (Nick action): docs HEAD is still `ed5cf91` — the six AMD-88..93 files are UNTRACKED.** The pinned state expected the AMD-block commit. Commit them BEFORE the review dispatch (or before the fold, if the review already ran against the on-disk text) so the reviewer baseline and the fold's source-verification target are one stable sha.
- **`index.lock.cleared-by-cowork-2026-06-12` STILL PRESENT → FOURTH flag** (deletion is Cowork-permission-gated; Nick deletes).
- **Resolution paths:** (a) Nick saves the DOCS-Project return verbatim to `context/audits/` under the prompt's filename convention; or (b) the brief's FALLBACK — a SEPARATE fresh Cowork conversation runs the bundled prompt as its entire instruction (no PM skill, no prior context, re-derive from source) and writes the return, logging the process deviation in its masthead. Never review+fold in one conversation. The fold session then re-runs its brief against the landed return; this Step-0 is banked (only the docs sha needs re-reconciling post-commit).
- **M7 research-vector advisory (Nick's add-on ask; checked against the disposition + R14A/B assessments + the review prompt to exclude duplicates):** (1) **Dry-run/no-side-effect execution seam** — nothing in R13/R14/R15 or the AMD block covers test-before-enable; the feature is M10+ UI territory, but the M7 engine design should RESERVE a command-dispatch seam a future simulator can swap (trace generation without side effects). One design-note/review-question now = regret-proof; retrofit later = expensive. The "test in production" pain is a top HA/Hubitat complaint class and composes with the ledger/explainability flagship. (2) **AMD-91 chain semantics across the command→state-change hop** — does `RunCausalChain` propagate when automation A's action moves device state that TRIGGERS automation B? If yes, cross-automation oscillation ("fighting automations" — a category-of-one diagnostic vs HA/Hubitat) falls out nearly free and deserves explicit statement + a future diagnostic event; if no, state-mediated loops are invisible to chain-membership cycle detection and the limitation should be stated in AMD-91. Neither reading is wrong; the AMD should say which. NOT among the prompt's 10 §A items — worth adding at fold time or as a fold-session check. (3) Unavailable-target runtime posture: ALREADY COVERED (`UnavailablePolicy` at baseline + the in-session confirmation/onUnavailable mirroring fix) — no action. (4) Strong-concepts scan: blueprint-style parameterized definition SHARING (HA's best-loved authoring feature) stays deliberately post-MVP (adjacent to the no-templating-DSL anti-requirement — needs its own carefully-scoped beat if ever); AMD-93's strict-schema/validation/forward-only posture makes definitions machine-generatable (LLM/assist authoring) — a strategy-refresh messaging line, not an M7 item. **Verdict: NO new external research brief needed pre-M7** — coverage is sufficient; misfire/DST (REC-166), rate-limiting (REC-168), and entity-reference indirection (REC-153b) stay parked where the disposition put them.
- **Open Risks: unchanged** — no deferred build gates (documents-only; the session's only writes are this record + the snapshot session-log row).

## Prior Session — M7 Entry-Gate AMD Block DRAFTED + Bundled Review Prompt + Strategy Drafts (2026-06-13 brief; executed Jun 11 local/Jun 12 UTC)

**Mode-2/Mode-3 governance session (Cowork; the W25 weekend-bridge work, run early). Preflight vs the session-brief PINNED STATE: STALE (Check 9 only — the one pinned mirror file, source-ahead direction verified; all other 10 checks PASS at one glance each). Environment: the truncated-tail mount artifact was ACTIVE in BOTH repos (VM git showed phantom EOF-truncation deletions across many files; host tails verified intact via file tools — VM git treated as suspect throughout, `--no-optional-locks` used for all read-only git).**

- **Step-0:** hivemind HEAD observed at `5f30ff0` (one past the pinned `fbf3124` — Nick committed the W25 re-charter + the Jun-13 session prompt post-authoring; benign, message-verified). Shas reconciled into the snapshot reconciled-line / W25 masthead / cross-agent masthead. Consumed `2026-06-12_W0_R14-briefs_session_prompt.md` → `handoff/archive/`. Rollover check: Jun 12 UTC < Jun 14/15 → NO flip (W24 stays IN_PROGRESS, W25 stays RE-CHARTERED — answer-key match). `index.lock.cleared-by-cowork-2026-06-12` STILL PRESENT → re-flagged (third surfacing). `ed5cf91` commit-message nit: logged, append-only, moved on.
- **The M7 entry-gate AMD block — AMD-88..93 PROPOSED** (`homesynapse-core-docs/design/amendments/`, numbers derived from the nav-index at drafting; the block sits ABOVE the AMD-87 watermark — ratification raises it to 93): **AMD-88** triggers (9→12 permits; Calendar/Reachability/Manual new; Webhook promoted w/ fields; PresenceTrigger promotion-DESIGNATION only, fields M8 per DQ-1; triggerId additive identity; promotions = field-additions, no new switch cases — stated). **AMD-89** selectors (SemanticTagSelector on the shipped labels-as-namespaced-tags convention; `includedRoles` BREAKING on Area/Label/Type + sweep mandated; ⚠ binds `EntityRole` — `EntityCategory` never existed in source, deviation flagged R89-1). **AMD-90** actions (ConfirmationPolicy enum + root default-method, named default **BEST_EFFORT** [R90-1]; binds the EXISTING `automation.command_pipeline.default_confirmation_timeout_ms` — mints NO key; REC-161 Pi-4 calibration spike pinned pre-M7.3; RepeatAction w/ hard maxIterations; ActivateSceneAction→InvokeAutomationAction per DQ-2, no parameter passing). **AMD-91** RunCausalChain (single-field RunContext swap; ChainLink-based chain w/ derived depth(); cycle detection = chain-membership, deterministic, replaces the windowed correlation-set; **formal AMD-04 supersession ledger** — rate-limiting clause NOT ADOPTED, every other element carried; Doc 01 §4.5 query-side view stands). **AMD-92** event vocabulary (the full Doc-07 inventory enumerated w/ per-type baseline state: **16 mints + 2 reshapes** (AutomationTriggered/CompletedEvent are M1-era minimal shapes, zero producers — reshape-now is the regret-proof window) **+ 1 record-for-existing-constant** (capability_mismatch); type-residency **FLATTEN** (RunId→Ulid, RunStatus→String — relocation rejected, automation-internal by design); expected pins 55→71 / 24→41 / 36→+17, survey-fixed per slice incl. the publish-count-pin category; BOTH module-infos embedded verbatim; C8 cited PROPOSED-pending at every stamping mention; `automation_invoked` = the one non-Doc-07 mint, flagged R92-1; the §3.7-vs-§6.2 automation_disabled priority tension flagged R92-3). **AMD-93** schema posture ((major,minor) per AMD-67 substrate; forward-only idempotent migration guarantee = REC-151 structural; dangling-ref load validation w/ tombstone-redirect exemption; the W0 §4.2 Doc 07 §3.3 ↔ AMD-66/71/M6.2-R-1 reconciliation stated; REC-136 queue annotation honored). Anti-requirements 151/155/162 land as explicit §6 non-goals + §7 citations in every relevant AMD (the brief's "§7s" realized as the §6+§7 pair — noted in the review prompt so it isn't flagged). 11 invariant candidates (AMD-88-INV-01/02 … AMD-93-INV-01/02). Self-reviewed per review-and-quality §1/§2 (two precision fixes applied in-session: confirmation non-null mirroring onUnavailable; the AMD-70-pattern freeze-boundary sentence).
- **Bundled DOCS review prompt READY TO DISPATCH** (`context/instructions/2026-06-13_AMD-88-93_C8-C9_Bundled_DOCS_Review_Prompt.md`): one dispatch, two halves (§A block w/ 10 adversarial items incl. all [R*] flags; §B C8/C9 ratification review w/ the R14-A REC-146 Hubitat provenance evidence handed to C8 + the C8⊕AMD-92 envelope-only independence claim to verify). Bundling = PM default per the 2026-06-12 sequencing; **Nick veto-or-default — if vetoed, C8/C9 first, block second** (the halves split cleanly).
- **Strategy-refresh drafts D-1..D-7** (`context/planning/2026-06-13_strategy-refresh-drafts_R15.md`): 172 Matter economics+fence (file 2) · 173+183 governance-credibility/license-re-affirmed (file 2) · 178 OHF model (file 3) · 180 segment messaging rule (files 1/3) · 184 Grid/Assure-hypothesis w/ M5-D interviews as the test (files 2/3/4) · 185 unsized-segment risk (files 3/4) · D-7 the file-4↔file-2 data-monetization reconciliation line (R15 Step-F note 2). Insert-ready, per-item vetoable; docx folds ride the M5-C session.
- **Stretch NOT reached:** M5-C Increment-1 skeletons untouched — Lane-4 gate state unchanged (still the entry-gate row-4 blocker).
- **Open Risks: unchanged** — no deferred build gates (documents-only session; core untouched at `e5ea76f`).
- **WUCP drift check:** snapshot (masthead/next-action/session-log row) ✓ pm-handoff (this record + masthead) ✓ cross-agent (new CURRENT POINTER) ✓ W25 (bridge EXECUTED line; last-verified) ✓ charter skeleton (gate row 1 → DRAFTED) ✓ backlog untouched (no milestone state change — correct) ✓ both consumed prompts archived ✓. Check 9 remains the only STALE (Nick's sync).
- **SAME-SESSION ADDENDUM — project-knowledge spine REGENERATED (Nick-directed):** all five spine files (`project-knowledge/`) updated to the 2026-06-13 ground for re-upload to BOTH Claude Projects (CORE + DOCS): **Current_State** (M6 3-of-4 + M7 entry-gate §1 rewrite; governance/build/commit-chain; the "135/34" invariant under-count corrected to 152/41), **Knowledge_Primer** (config module = PRODUCTION M6 surface; automation = 53 types/30 permits w/ the do-not-cite-PROPOSED-as-current fence; CORE_PRODUCTION 22→24 + pins 55/24/36; register-total note rewritten re-derive-don't-propagate), **Navigation_Index** (AMD-52..93 separate-files table added incl. the six PROPOSED rows + the M7 blueprint authority chain + research-returns archive row), **Decisions_Quick_Reference** (NEW sections: M6 execution rulings [module-info ruling, GF-1 ×2, R4/R5/R6, R-1/R-2/R-3] + M7 decided ground [DQ-1/2/3/5] + B2 C8/C9 + the AMD-88..93 PM defaults, all PROPOSED-fenced), **Invariants_Quick_Reference** (stale "133/32" FOOTER corrected — the masthead/footer disagreed; the 11 AMD-88..93 candidates added as an explicit PENDING section, register-at-§42+ on ratification). README invariant-row cell fixed. Custom-instructions ×2 untouched (they delegate currency to the spine by design). Register provenance verified: `Architecture_Invariants_v1.md` untouched since `539ed48` (the 2026-06-09 ratification) → 152/41 stands. **Nick: re-upload the 5 spine files to both Projects (KNOWLEDGE_SOURCES spec unchanged) BEFORE dispatching the bundled review — the DOCS reviewer's spine must carry the PROPOSED-block state.**

_Session records 2026-06-11→06-12 (R14/R15 cycle + W0) rotated → `archive/pm-handoff-2026-06.md` (2026-06-12 hygiene pass)._

## Open Risks` below. New FUTURE-AMD queued: tag-preserving YAML emitter (post-MVP; with REC-136 + REC-138/§12.4). **The Coder's M6.2 cross-agent note was lost to a mount-lag write overlap (its closeout vs the PM rotation, same hour) — RECONSTRUCTED in cross-agent from the completion report; checklist-vs-file verification is now a standing Phase-2 step.** Research architecture for the M7+ blueprint authored (`context/planning/2026-06-11_M7-blueprint_research-architecture.md`, PROPOSED) + research-agenda addendum (R14-A/R14-B/R15, REC ranges 141–185 partitioned). A1/A3/A4 rotations executed post-Phase-2 per Amendment 1/2. W25 charter authored.

**Mode-3 Director session (Cowork). Preflight vs the session-brief PINNED STATE: STALE (Check 9 only — the one pinned mirror file `project-manager/references/coding-instruction-format.md`, expected until Nick's sync; all other checks PASS at one glance each). Allowed-under-STALE activity only until the M6.2 review.**

- **Step-0 sha-reconciliation:** M6.4 = **`62a81e6`** filled across PROJECT_SNAPSHOT (Last-updated / Current-state / HEAD chain / Latest-commit / Next-action), backlog (masthead + M6.4 row + M6.2 row → DISPATCHED), W24 masthead, coder-handoff gate status, cross-agent pointer, this file's masthead. **pm-handoff M6.4 Open-Risks gate entry CLOSED** (its stated resolution condition). Hivemind closeout `82c8068` confirmed.
- **Context-hygiene pass (Nick-approved manifest; before/after evidence for the next retrospective):**
  - **A2 pm-handoff rotation EXECUTED: 144K → 38K (−73%).** Session records 2026-06-06→06-10 → `archive/pm-handoff-2026-06.md` (75K); the legacy M3-era sections + 2026-05-18..22 records → `archive/pm-handoff-2026-05.md` (110K) — **the 2026-06-10 ROTATION MARKER is completed and retired.** Live file = current session record + rotation pointers + Open Risks.
  - **B rehoming EXECUTED (27 files, ~480K of live-path content):** 2 committed M5-A coding instructions → `coding-instructions/archive/`; 6 consumed session prompts → `handoff/archive/`; 11 consumed research briefs (R4–R13, all RETURNED+ASSESSED) → `instructions/archive/`; W21–W23 → `weeks/archive/`; 3 consumed planning one-offs → `planning/archive/`; rust-deliberation relay (76K) → `relay/archive/` (new); skill-update audit (60K) → `audits/archive/`. **Reference-sweep:** live trackers updated (`research-agenda.md` 6 brief pointers, `open-questions.md` R9 pointer, backlog P-row pointer — see below); historical citations inside assessments/returns keep old paths per Nick's ruling (archive is in-tree + findable). **Check 10 re-verified: the strategic-context-map cites none of the moved files — PASS by delta.**
  - **C EXECUTED:** `ClaudeFolder/hs-spike-value/` deleted (verified empty incl. hidden; the M4.0b-4a spike's consumed husk).
  - **A1/A3/A4 HELD per Nick's Amendment 1 (sequencing guard):** coder-handoff (270K), cross-agent-notes (165K), coder-lessons (144K) rotate AFTER the M6.2 handoff content is fully landed + Phase 2 done — in one session, M6.2 records intact at top. **Amendment 2 binds A1:** the refuse-to-close next-WU pointer + the newest gate record survive the rotation; dual rotation mastheads (live→archive pointer, archive states date/source).
- **Mid-session sync-collision audit (lesson-grade):** the M6.2 Coder session's writes (host 23:02) synced into the Cowork mount MID-SESSION — the survey initially read the pre-M6.2 file state. Outcome verified clean: the Coder's M6.2 gate-status demotion and the PM's Step-0 M6.4 closure BOTH survive in `coder-handoff.md` (PM text intact in the `_Prior:_` chain). **No Coder M6.2 cross-agent entry exists in the current file** — if the M6.2 completion report claims one was appended, it was lost to the mount-lag overlap and must be reconstructed from the coder-handoff masthead (PM watch-item at the return). **Process note: at any session start where a Coder session may be in flight, treat handoff-file mtimes vs git-status as a freshness signal before editing shared files.**
- **M6.2 state at writing:** files PRODUCED on baseline `62a81e6` (15 modified + 14 created in core, per `git status`); G1–G8 PASS per the Coder masthead; **build gate NOT yet run — DEFERRED to Nick** (new Open-Risks entry below). Formal WUCP Phase 2 runs when Nick provides the return + gate output (answer key: the 2026-06-11 session brief, Task 1).
- **Next in-session:** M6.2 Phase-2 source pre-verification (gate-independent answer-key items); W25 charter (+ W24 → COMPLETE at rollover); post-M6 deliberation — M7/M8 runway + Research 14/15 brief authoring (PM default: 3-brief parallel split, Nick veto-or-default at dispatch).

## Prior Session — M6.4 WUCP Phase 2 → APPROVE (2026-06-11, earlier)

**Mode-3 Director session (Cowork, continuation of the Research-13/instruction session context): gate-failure diagnosis → GF-1 ruling → Coder applied → full gate GREEN (147 tasks) → Phase 2 APPROVE. Sandbox VM died mid-session (again) — all verification via file tools (the authoritative path).**

- **Gate round 1 (169/1):** `ConfigLayoutTest.composeAfterMerge:264` — PM-diagnosed from source as a **stale M6.1a-era publish-count pin**, NOT an implementation defect: the test pinned `rootDrafts.hasSize(1)` on a one-ERROR load; the ratified R1/DP-10 obligation adds one `config_error` per ERROR beside `validation_completed` → 2 drafts. **Test-first integrity note: the fix updates the test to the RATIFIED contract (PM-specified), never to the implementation** — and the suite worked as designed (an untouched contract test stopped the gate on a behavior change). GF-1 ruled + applied exactly (hasSize(2), order-agnostic type-filter, contract-anchored assertions on both payloads, dated pin-bump comment); the Coder's defensive module-wide `rootDrafts` sweep found no other stale pin (consistent with 169/1).
- **Phase 2 verification (source-level):** DP-1/2 (volatile + one `ReentrantLock`) ✓; DP-7 (no `ConfigChangedEvent` publish site — Javadoc citation only) ✓; DP-9/10 metadata + the §12.4 fence (implemented STRICTER than spec — sensitive paths suppress `appliedDefault` too, the right §12.5-consistent reading) ✓; `ConfigSectionReloadedEvent` = AMD-70 §2.1 verbatim + full guards ✓; helpers package-private ✓; `collectSections` flattens exhaustively → the R-D diff semantics are COMPLETE (every leaf diffed exactly once at its immediate section) ✓; `ConfigChangeSet` filters ✓; new tests are contract-anchored (count-deltas + type-filters; torn-read REC-133, CoreSchema-`~` DP-12, crash-window REC-135, stale-token REC-130, backup-once REC-131, AMD-66 matrix REC-132) ✓; P2 fan-out pins 54→55/23→24/35→36 empirically closed by the GREEN gate.
- **Rulings:** **R-A ACCEPT** (`homesynapse.yaml.bak.{basic-ISO}` — `ROOT_DOCUMENT_NAME` verified `homesynapse.yaml`; DP-6's literal was the Doc-06-era name; Windows colon constraint real). **R-B ACCEPT, condition CONFIRMED at source** (`:888-889`: backup-copy then atomic-write sequential in one try — backup failure skips the rename; original never replaced without its on-disk backup; WARN + continue in-memory is INV-RF-06-consistent). **R-C ACCEPT** (frozen `write()` throws-clause forces the wrap; `IllegalStateException` w/ file-written/model-prior message, test-pinned; M10 REST-mapping note → Doc 09 lane). **R-D ACCEPT** (verified sound, see above; M9 register-at-leaf-section gotcha posted by the Coder). **13 [INFO] accepted** (incl. global-warning-count `issueCount` — defensible AMD-70 reading, MODULE_CONTEXT-documented; reload skips the `schemas/` cache rewrite — registrations are construction-time). **`CONFIG_SECTION_RELOADED → [SYSTEM]` CONFIRMED** (sibling-consistent, observability-only).
- **Process lesson (shared PM+Coder miss, the M6.1a arch-rules lesson one layer up):** the P2 survey enumerated manifest/count pins but not **behavioral publish-count pins in the producing module's sibling tests** — the Coder adapted `StandardConfigurationServiceTest` for the publish change but didn't sweep `ConfigLayoutTest`; a 10-second `grep rootDrafts src/test` would have caught it in-session. **Category added to the writable skill source** (`project-manager/references/coding-instruction-format.md` §Consumer/Pin survey); **Nick: run the mirror sync** — Check 9 reads STALE until then (expected). Coder-side lesson already appended by the Coder.
- **M6.2 needs NO instruction edit:** grep-verified no R1/`config_error` item exists in it (the Coder's "drop R1" referred to the stale pre-M6.4 coder-handoff pointer); its G7 gate was authored adaptive to the post-M6.4 service. Dispatch line: "[R6] CONFIRMED; R1 is DONE in M6.4."
- **Carries:** docs lane (next docs touch, non-blocking): Doc 06 §3.5 `config.yaml` naming predates AMD-71's `homesynapse.yaml` (ties to R-A); §3.6/§4.5 `config_error` text can cite the implemented §12.4 fence semantics. M10/Doc 09: the R-C `IllegalStateException` REST mapping. M9: the R-D register-at-leaf-section listener guidance.
- **Next forward:** (1) Nick commits core (M6.4, message in the session report) + hivemind closeout (incl. `git mv` of the consumed M6.4 instruction → `archive/` — EXECUTION RECORD appended; no tombstone) + mirror sync. (2) Dispatch M6.2. (3) Next PM session: **W25 charter (due Jun 14/15) + the post-M6 deliberation beat** — M7/M8 automation runway + Research-14 scoping (automation-engine market-superiority on the Research-13 pattern; ecosystem/positioning scan feeding M5-C + strategy). (4) **M6.3's triple gate is now the M6 critical path — both evidence gates are Nick-paced** (Pi-4 microbench OQ-15-2; energy/erasure interviews AMD-86 §3). FUTURE-AMD queue unchanged.

_Session records 2026-06-06 → 2026-06-10 rotated to `archive/pm-handoff-2026-06.md` at the 2026-06-11 hygiene pass — see `## Rotated session records` below._

## Rotated session records (2026-06-10)

**Extended 2026-06-11 (hygiene pass):** session records **2026-06-06 → 2026-06-10** → `archive/pm-handoff-2026-06.md`; the **legacy M3-era sections + the 2026-05-18..22 records** (per the 2026-06-10 ROTATION MARKER, now completed) → `archive/pm-handoff-2026-05.md`.

Session records older than the M5 window were rotated to the archive per `archive/README.md` §4 at the 2026-06-10 M6.1 sha-reconciliation + cleanup session: the **2026-06-04 → 2026-06-05** records (W23 Mon+Tue batch; Workstream C briefing; AMD-54..64 review + ratification + M4.C issue; M4.C Phase 2 → M4 COMPLETE; M4 retrospective + next-piece) → **`archive/pm-handoff-2026-06.md`**; the **2026-05-30 → 2026-05-31** records (Research 4/10; AMD-51; M4.0b-3; AMD-52 design beat + Research 11 + AMD-52; M4.0b-4b; M4.0b-5; M4.B-S1) → **`archive/pm-handoff-2026-05.md`** ("Rotated 2026-06-10" section). Content preserved verbatim. **The legacy pre-M4 sections further down this file (Current Task / Phase 3 Work Unit Status / the 2026-05-20..22 M3.6 closeouts and reference sections) are queued for the next monthly rotation — left in place this session, marked.**






> **Rotation executed 2026-06-11** — the legacy M3-era sections and the 2026-05-18..22 records below Open Risks now live in `archive/pm-handoff-2026-05.md` (the 2026-06-10 ROTATION MARKER is completed and retired). Only the current session record, the rotation pointers, and `## Open Risks` remain live in this file.

## Rotated Open-Risks blocks (all RESOLVED/CLOSED)

#### M6.2 build gate — **RESOLVED/CLOSED 2026-06-11: COMMITTED `7c73c91`** (NEW 2026-06-11; CLOSED 2026-06-11 EOD)
- **Milestone:** M6.2 (SecretStore + per-scope key-management infrastructure, AMD-68 + Doc 15 §3.5/§4.2/§7.3 + CARRY 1), working tree layered on **`62a81e6`**. **Owner:** Nick (commit) / PM (logged).
- **Gate:** Nick ran `./scripts/clean.sh` → `./gradlew :config:configuration:check :core:persistence:check :lifecycle:lifecycle:check :app:homesynapse-app:check` (**82 tasks**) + full `./gradlew check` (**147 tasks**) — **GREEN 2026-06-11, zero gate-fix rounds** (first M6 piece to go GREEN in one round; the P5 shift-left + 12-agent adversarial pre-review earns the credit — note for the retrospective). **Deferred Build Gate flag:** honored; gate-level CLOSED.
- **PM WUCP Phase 2 → APPROVE 2026-06-11** (source-verified vs the session-brief answer key, not the report): zero-new-edge BOTH directions + no module-info/Gradle/catalog touches + event manifests untouched (55/24/36 safe) ✓; surfaces exact (3 public, 3 package-private; `PayloadCipher`/`EncryptedPayload` java.base-only, consumed by NOTHING in persistence) ✓; both-path tag resolution via shared `runPipeline()` (load :346 / reload :421), write-path loader deliberately NON-resolving (:591) ✓; LTD-15 clean ✓; lazy key artifacts (INV-CE-02) + `zeroConfigLoadTouchesNoKeyFiles` ✓; AMD-68 §5 five rows + AMD-16 rotation (first-write-no-backup / sixth-prunes-oldest / restorable) + HKDF A.1/A.2 + §8.2 freeze-gate (`destroyedScopeKeyIsUnreadable`) + `crossScopeDecryptFailsAuthentication` + `destroyedVersionIsNeverReissued` + `corruptRootKeyFailsClosed` ✓; DP-6 `[exports]` watch resolved clean (lifecycle already `requires transitive` persistence, LD#10). **Rulings: R-1 ACCEPT** (fail-closed tag-bearing `write()` — resolving on write would bake plaintext secrets into the emitted file, INV-SE-03/LTD-15; the [R4] reasoning class: leakage irreversible, restriction reversible; **NEW FUTURE-AMD queued: tag-preserving YAML emitter, post-MVP**; M10 design-input note: UI/API mutation of tag-bearing configs blocked until then). **R-2 ACCEPT** (`PersistenceFactory` untouched is correct — extending the frozen gateway IS the M6.3 wiring step; cipher held at `HomeSynapseCore` per DP-6). **R-3 ACCEPT** (DP-6 "wire" discharged as package-private `Main.payloadCipher` factory + 5-arg ctor + bridge round-trip test; live bootstrap rides M13). **10 [INFO] accepted** (incl. the three review-driven hardenings: crash-atomic root-key write, fsynced backups, overflow-guarded suffix parse).
- **Resolution condition:** CLOSED when Nick commits and the sha is reconciled into the trackers (the Step-0 pattern). Consumed instruction archived with EXECUTION RECORD this session.
- **RESOLVED 2026-06-11 EOD:** **COMMITTED `7c73c91`** (29 files, +3476/−58, pushed) + hivemind closeout **`d161ca9`** (pushed); shas reconciled same session. **Gate entry CLOSED — no open deferred build gates.** *(Commit-body nit: bash history expansion ate the `!secret` bullet lead line — content intact; future `!`-bearing messages via `git commit -F`.)*

#### M6.4 build gate — **RESOLVED/CLOSED 2026-06-11: COMMITTED `62a81e6`** (NEW 2026-06-11; CLOSED 2026-06-11)
- **Milestone:** M6.4 (hot-reload atomic swap + write path), working tree layered on `7e0bce8`. **Owner:** Nick (commit) / PM (logged).
- **Gate:** round 1 = 169/1 (the GF-1 stale-pin, PM-ruled, test updated to the ratified contract); round 2 = **full `./gradlew check` GREEN, 147 tasks, 2026-06-11** (the halted round-1 never reached `-Werror`/spotless/ArchUnit/integration tails — the round-2 full run covered them). **Deferred Build Gate flag:** honored; gate-level CLOSED.
- **Resolution condition:** CLOSED when Nick commits and the sha is reconciled into the trackers (the M6.1a Step-0 pattern).
- **RESOLVED 2026-06-11:** **COMMITTED `62a81e6`** (`git log` HEAD verified by the PM at the M6.2-return session Step-0; sha reconciled across snapshot / backlog / W24 / coder-handoff / cross-agent). **Gate entry CLOSED — no open deferred build gates.** Owner: Nick (committed) / PM (closed).

#### M6.1 build gates — **RESOLVED/CLOSED 2026-06-10: M6.1b COMMITTED `b7bc65c` + M6.1a COMMITTED `9035110`** (incl. ⟳ gate-fix round 1) (NEW 2026-06-10; CLOSED 2026-06-10)
- **M6.1a:** Nick ran `./gradlew :config:configuration:check` (27 tasks) + full `./gradlew check` (147 tasks) **GREEN 2026-06-10** after gate-fix round 1 (CoreSchema + Rule-5 config exemption). **RESOLVED:** **COMMITTED `9035110`** ("M6.1a: YamlLoader…", on `b7bc65c` — `git log` HEAD verified by the PM 2026-06-10 at sha-reconciliation). **Gate entry CLOSED — no open deferred build gates.** Owner: Nick (committed) / PM (closed).
- _Original M6.1b entry below (gate closed; committed `b7bc65c`)._
- **Milestone:** M6.1b (config access/events half), working tree layered on `6c6dd33`. **Owner:** Nick (commit) / PM (logged).
- **Gate:** Nick ran `./gradlew :config:configuration:check :core:event-model:check :core:persistence:check` (57 tasks) + full `./gradlew check` (147 tasks) — **GREEN 2026-06-10** (commands matched coder-handoff `### DEFERRED — M6.1b`). **Deferred Build Gate flag:** honored and now closed at the gate level.
- **Resolution:** **CLOSED** — M6.1b committed `b7bc65c`; M6.1a committed `9035110` on top (both shas reconciled against `git log` HEAD).

#### CARRY — AMD §7-embed staleness at M6.1a landing (NEW 2026-06-10) — verification half CLOSED 2026-06-10 EOD
- **CLOSED (verification):** the implemented module-info matches the ruling exactly (5 directives, non-transitive, dated comment; PM-verified at Phase 2). **REMAINING (watch-only):** no future doc may re-embed the config module-info without the ruling-correction note.
- _Original entry below._
- The AMD-66/67/68/70/71 §7 ruling-correction notes are authored (docs repo, 2026-06-10, per Nick's module-info ruling). When M6.1a lands, the config `module-info` gains the five third-party non-transitive `requires`; the §7 embeds remain the labeled `6c6dd33` baseline **by design** (the correction notes scope "unchanged" to the HomeSynapse edge set). **Owner:** PM — verify at M6.1a Phase 2 that the implemented module-info matches the ruling exactly (5 directives, non-transitive, nothing more) and that no doc re-embeds the old file without the note.

#### AMD-69 — Passphrase-root KDF (Argon2id/BouncyCastle) DEFERRED to Tier-2 — **RESOLVED 2026-06-09: Nick confirmed Option (a) at the block ratification** (NEW 2026-06-08)
- **RESOLVED 2026-06-09:** Nick confirmed the Tier-2 deferral (Option (a)) at the 2026-06-09 block ratification. AMD-69's masthead carries the confirmation text; its §8 disposition is closed. The number stays reserved for the eventual Tier-2/OQ-15-3 passphrase-root-KDF amendment (BC Argon2id vs zero-dep PBKDF2 — gated on the GraalVM C15 closed-world evidence); OQ-15-3 stays closed-as-open-question, owned by Doc 15. The active M6 config block is AMD-66/67/68/70/71. _Original entry below for the record._
- **Severity:** LOW (governance reconciliation — **no MVP consumer is blocked**; M6.1/M6.2/M6.3 do not depend on the passphrase root).
- **Detail:** Research 5 REC-58 (assessed 2026-05-22) planned AMD-69 = commit the M6 secret store to Argon2id + the BouncyCastle dependency. The later-Locked **Doc 15 governs the encryption domain and reclassifies this**: §2.3 puts the passphrase/Argon2id root at **post-MVP**; §3.5/§7.3 root the MVP secret store on the **shared machine-local key** (zero-config, no passphrase); §3.8 states the **MVP path adds zero new dependencies**, with the Argon2id-vs-PBKDF2 / BouncyCastle-vs-zero-dep choice the **OPEN OQ-15-3**. Committing BouncyCastle now would violate Locked Doc 15 on three counts. Per the W24 guardrail (keep Locked Doc 15 inviolate), the PM authored **AMD-69 as DEFERRED** (`design/amendments/AMD-69_Passphrase_Root_KDF_Deferred_Tier2.md`, §4 escalation), reserving the number for the Tier-2 amendment that resolves OQ-15-3.
- **Review (2026-06-09):** all three Doc 15 conflicts re-verified verbatim; ruling **CONFIRM-DEFERRAL (Option (a))**; do NOT re-open OQ-15-3 now (Tier-2 has no MVP consumer; GraalVM closed-world input not yet in). **Owner:** Nick (confirm the deferral) / PM (authored). **Resolution:** closed when Nick confirms the Tier-2 deferral at ratification; the active M6 config block is then AMD-66/67/68/70/71.

#### AMD-66–71 — M6 config block — **RESOLVED 2026-06-09: AMD-66/67/68/70/71 RATIFIED + AMD-69 deferral confirmed; mechanics executed; M6.1 READY TO ISSUE** (NEW 2026-06-08; UPDATED 2026-06-09; RESOLVED 2026-06-09)
- **RESOLVED 2026-06-09:** Nick ratified AMD-66/67/68/70/71 and confirmed the AMD-69 deferral. Invariants §37–§41 registered (+8); nav-index rows added; watermark unchanged AMD-87; M6.1 gate lifted (READY TO ISSUE, not issued); AMD-68 half of M6.2's gate satisfied. **Two carry-forward items survive this closure (do NOT drop):**
  - **CARRY — Doc 15 §8.1 `ScopeKeyManager`-row currency nit:** the row still reads "implement `PayloadCipher`," stale vs the E2-folded §3.8 (adapter-at-`app`; `ScopeKeyManager` does NOT implement the persistence type). **Fix rides the next Doc-15-touching amendment — do NOT touch the Locked doc now.** Owner: PM (at that amendment).
  - **CARRY — M6.4 P2-survey obligation:** `config.section_reloaded` lands at M6.4 — its coding instruction MUST re-run the P2 consumer/pin survey (only `config.validation_completed` lands at M6.1). Owner: PM (at M6.4 issue).
  _Original entry below for the record._
- **Severity:** MEDIUM (gates M6.1; the M6 config-amendment side of the entry-gate).
- **Detail:** AMD-66 (`ConfigurationChangeListener`, F7-corrected), 67 (config-doc schema `(major,minor)` — REC-41 cleared by ratified AMD-54), 68 (`SecretStore.setAll(Map)` — the Doc 06 currency amendment; REC-57 bundle/`credentialsFor` retired by ratified AMD-60), 69 (DEFERRED, above), 70 (config observability events), 71 (hybrid dir layout). All PROPOSED in `homesynapse-core-docs/design/amendments/` (below the AMD-87 watermark — reserved slots). DOCS-review prompt: `context/instructions/2026-06-08_AMD-66-71_DOCS_Review_Prompt.md`.
- **Review RETURNED 2026-06-09** (`context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md`): **RATIFY-WITH-EDITS, no rejects.** 66 RATIFY-AS-IS; 67/68/70/71 RATIFY-WITH-EDITS; 69 CONFIRM-DEFERRAL. **7 edits FOLDED by PM 2026-06-09** (E67-1/2, E68-1, **E70-1 — the load-bearing JPMS-cycle fix: config-event payloads flattened to event-resident types + a type-residency rule**, E70-2, E71-1/2). Per-amendment frontmatter status lines flipped to the verdicts.
- **Owner:** Nick (ratify) / PM (folded). **Resolution:** closed when Nick **ratifies AMD-66/67/68/70/71** + confirms the AMD-69 deferral → then the ratification mechanics: **register the invariants** (66-INV-01/02, 67-INV-01/02, 68-INV-01, 70-INV-01, 71-INV-01/02 into `Architecture_Invariants_v1.md`), **add nav-index rows** (watermark **unchanged** — all < AMD-87), and **lift the M6.1 ⛔ gate** (AMD-66/67/70/71) + the AMD-68 half of the M6.2 gate.
- **Two carry-forward block-level notes (from the review §4):** (i) **Doc 15 §8.1 `ScopeKeyManager` row** still reads "implement `PayloadCipher`," stale vs the E2-folded §3.8 (adapter-at-`app`; ScopeKeyManager does NOT implement the persistence type) — fix in the **next Doc-15-touching amendment**, NOT now (Doc 15 inviolate). (ii) **M6.4's coding instruction must re-run the P2 consumer/pin survey** for `config.section_reloaded` (only `config.validation_completed` lands at M6.1).

#### B2 schema decisions C8 (`actorRef`) + C9 (energy shape) — PROPOSED; independent review pending (NEW 2026-06-08)
- **Severity:** LOW–MEDIUM (schema-irreversible over the immutable log; cheap now, a migration later).
- **Detail:** `context/decisions/2026-06-08_B2_schema_decisions_C8_C9.md` — C8 fixes `actorRef` identity semantics (keep the bare `Ulid`; closed 4-kind set; Tier-1 API keys → `API_CLIENT`; automations stamp `AutomationId`); C9 freezes the energy event **shape** (the `power_measurement` 4-attribute set + the 6 aggregate fields + the energy consent-scope), no features (D4). C10 consciously deferred. Each passes the contract-freeze-readiness gate.
- **Owner:** Nick (ratify) / PM (authored). **Resolution:** closed on independent review + Nick ratification + the Doc 01 (C8) / Doc 02 (C9) currency notes.

#### OR-PD-07-AMD — INV-PD-07 amendment pending (crypto-shred MVP-scope contradiction) (NEW 2026-06-06)
- **Severity:** MEDIUM (governance integrity — a ratified invariant currently contradicts the de-facto/decided scope; load-bearing for M6 sizing).
- **Detail:** Per decision **D2**, operational crypto-shredding is deferred POST-MVP (to the first cloud/institutional data-sharing product); only the per-scope key-management infrastructure + encryption-scope categories + at-rest encryption are MVP. But ratified **INV-PD-07** (`homesynapse-core-docs/governance/Architecture_Invariants_v1.md:427`) still mandates *"crypto-shredding must be operational for at least one data category"* at MVP, and the unpromoted 2026-03-22 crypto Draft §5.2 files it under "Tier 2 / Post-MVP." Until the amendment lands, the ratified invariant, the Draft, and the decision are in three-way contradiction (the Draft must not silently override the invariant — D2 reconciles it via the formal pipeline).
- **Sub-risk (the regret-proof boundary) — Nick-sharpened 2026-06-06:** crypto-shredding can only ever destroy data written encrypted-per-scope, and the event log is immutable. **Two separable things were conflated; only one is safe to defer.** The **shred *operation*** (key-destruction + erasure triggers) has no MVP consumer → defer freely. But **encrypting sensitive PII categories (identity, person-linked presence) on write is NOT the shred feature** — it is an **INV-PD-03 at-rest obligation in its own right** (plaintext PII on a Pi's removable storage is a live privacy hole) *and* the preservation of future shreddability; since the per-scope key infrastructure is already MVP, it is a small increment. **Decided posture:** envelope-encrypt the sensitive PII categories on write under per-scope DEKs **from MVP**; defer only the operation. The crypto owner-doc must pin **two boundaries:** (i) the event-DB at-rest mechanism (SQLCipher whole-DB vs app-level per-scope payload encryption — likely the latter with vanilla sqlite-jdbc, and the same machinery as future shred), and (ii) from what point each sensitive category is encrypted-on-write — resolved against the **Lane D Pi-4 AES-256-GCM write-path microbench**, falling a category back to whole-install-reset-only only where Pi-4 perf forces it, consciously.
- **Owner:** Nick (ratify) / PM (author). **Resolution condition:** the INV-PD-07 amendment is authored minimally (strike only the "operational for ≥1 category" clause; preserve infra+categories sentence + design intent), **widened one line to also state the INV-PD-03 at-rest posture** (reconcile both privacy invariants in one pass), and **full-DOCS-Project-ratified** (not P4); AND the crypto owner-doc pins **both** boundaries above. This is M5-B / B1 — the first piece to execute. Closed when the amendment is RATIFIED and the owner-doc states both boundaries.
- **RESOLVED 2026-06-07:** **AMD-86 RATIFIED** (full DOCS review RATIFY-WITH-EDITS; **AMD-86 itself reviewed clean** — the two required edits were Doc 15's E1/E2; J1/F-A threat-model PASS, J4 minimality PASS, J5 Doc 06/AMD-60 shared-root PASS) + **Doc 15 LOCKED**. Applied in `Architecture_Invariants_v1.md`: INV-PD-07 narrowed (operational crypto-shred → first cloud/institutional consumer, decision D2; key infra + scope categories + `scope_keys` seam stay MVP) + INV-PD-03 at-rest posture stated (partial-at-MVP; machine-local root; user-owned-key + media-theft resistance = Tier-2); **AMD-86-INV-01 registered (§35)** + §17 index (135/34) + §0.3 prefix + §18 traceability; on-disk watermark AMD-64→**87**; nav-index row added. **Doc 15 E1 folded across THREE occurrences** (§4.1 column **and** semantic comment "scope DEK wrapped by the scope KEK", §4.2 key-hierarchy row, §8.2 `ScopeKey.encryptedDek` — the prior on-disk E1 summaries had under-counted §4.2 L204; the verbatim review flagged the inconsistency but its fix list also omitted §4.2 — PM source-derivation caught the full extent). **E2 folded as the `com.homesynapse.app` composition-root bridge** (zero new module edge; sharper than Doc 15's original `HomeSynapseCore` — `app` already requires both config+persistence, lifecycle does not), stated verbatim-ready for the M6 instruction. The three-way invariant/Draft/decision contradiction is closed. **The [BLOCKING-for-M6-impl] m2 counter-nonce hazard is carried forward as OR-M6-NONCE (below)** so it survives Doc 15's Lock.

#### OR-M4-WT — Uncommitted build-breaking working-tree edits at `8ef9e9f` — **CLEARED 2026-06-06**
- **Status:** **CLEARED.** `git -C homesynapse-core status` is clean this session (nothing to commit, working tree clean) — the three stray build-breaking edits (`EventCategoryMapping.java` + the two test files) were reverted. The committed `8ef9e9f` remains the correct M4-COMPLETE tree (build GREEN, 145 tasks). No action outstanding.

#### Open Item — AMD-51 §2.6 erratum (D-1 spec/code reconciliation) — RESOLVED 2026-05-31
- **Severity:** LOW (governance hygiene — ratified spec vs shipped code).
- **Detail:** M4.0b-3 ships **missing-schema → `StringValue` string-compare fallback** in `AttributeValueReconstructor`, whereas AMD-51 §2.6 did not specify the no-schema case (the coding-instruction error table said "Degraded / no-emit"). PM ACCEPTED the deviation (more correct — Degrade-on-no-schema would permanently freeze unschematized attributes; the fallback preserves M4.0b-2 semantics and the back-compatible no-arg `production()` gateway; `StandardCapabilities` covers all standard production traffic).
- **Resolution:** **RESOLVED** — Nick authorized; PM applied the erratum
