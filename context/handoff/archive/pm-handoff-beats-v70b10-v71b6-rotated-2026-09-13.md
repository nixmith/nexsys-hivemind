<!--
file: context/handoff/archive/pm-handoff-beats-v70b10-v71b6-rotated-2026-09-13.md
purpose: v70 beat 10 and v71 beats 1–6 rotated VERBATIM out of pm-handoff.md at v72 beat 6 (the live cap is 12 blocks). bytes(kept) + bytes(archived body) + 1 = bytes(before), asserted.
status: ARCHIVED 2026-09-13 (v72 beat 6; instrument 2026-09-13T15:34:09Z)
-->

## 2026-09-12 (v71 beat 6 — THE POST-CLOSE INTAKE: R3 corrected; `5f918c7` landed, `check` green, v1.1.4 frozen; the bus-soak job red = sample #15, unread, Sat 2026-09-12 ~18:5x CT (instrument 2026-09-12T23:55:29Z))

**The correction (R3):** the lane applied rows 1–5 (row 5 taken: `matchedAt` through the shared `instantOf`, wire byte-identical); red at 23:38Z on the rewritten T7 (`expected 2026-01-01T00:00:02Z but was null`) and on T7c over 17 fixtures; green at 23:39Z on the gate line verbatim — automation 228 → 229, rest-api 156/156, Spotless clean; census 13 M; the return's §5 appended (14,748 B; past the cap by the §5 alone — accepted). One phrase left for the hub: the rest-api MODULE_CONTEXT's "JSON null when" cell for `settledAt` still carries the pre-correction wording — a Phase-2 delta that rides the next core landing. **Landed by Nick's hands:** core `5f918c7` (13 M, `72efb42..5f918c7`); hivemind `7addbc8` (10). **The gate of record on `5f918c7`: Build & Check GREEN — v1.1.4 is frozen on the wire.** **The `bus-soak` job RED:** `BusSoakIT.soak_kHeroLoops_reportsLatencyAndAnomalies` — `[bus.soak: anomalies] expected: 0L but was: 1L` (`BusSoakIT.java:204`); the census IT passed; 2 tests, 1 failed. This is SAMPLE #15, the first anomaly line since FIX-1a's kinds landed and the first under FIX-2b-ii's instrument; UNREAD — the kind, the subscriber, the position and the `automation.handoff_census` line are in the artifact, which is the one act (`SAMPLE: 5f918c7 at _scratch/v71/ci-5f918c7/`). If the kind is `NOTIFY_SKIPPED_LIVE`, it is the AMD-101 §6 exhibit Nick's ruling reserved, the revert clock stops, and BUS-ORDER-1 dispatches on the clean tree at `BUSORDER: go`; if another kind, it gets its own reading before anything dispatches. The silent streak is 0 pending the read. **v72 opens on:** the sample #15 read → `BUSORDER: go` or the reading → HERO-1c → 114b → `AMD101: ratify` → R-4c's record. ctx: beat 6 · calls ≈195 · read ≈255 KB · wrote ≈150 KB

**Leverage line:** the first anomaly line in fifteen samples arrived on the push that froze v1.1.4 — the instrument was landed one push earlier.
**The alternative shape, rejected:** reading the red from the CI log alone — the log prints the count, the artifact prints the kind.
**2029 test:** the sample is banked as a line, unread and said so; the kind is read at the bytes before anything moves.

## 2026-09-12 (v71 beat 5 — THE v71 CLOSE: EXPLAIN-114a accepted with one correction; sample #14 and REPRO-1 green; the R3 paste handed, Sat 2026-09-12 ~18:1x CT (instrument 2026-09-12T23:13:00Z))

**EXPLAIN-114a:** returned 22:16Z (11,999 B, §0 first, `RETURNED` last); 13 M under `core/automation` and `api/rest-api`; module-info untouched; the FIX-2b-ii lines byte-untouched; red-first in two stages → 228/228 · 156/156 under `-Werror`; DP-5/DP-6 from source. Audited ACCEPT WITH ONE CORRECTION (`…v71-b5_EXPLAIN-114a_intake_two-layer-audit.md`, filed 22:46Z, adopted as the record): R1 (the `asString` dialect) ACCEPT; R2 (an auto-disabled automation never reports DISABLED — a registry fact vs a log fact) → EXPLAIN-114b row DISABLED-2; R3 (`settled: true` beside `settledAt: null` for a superseded DISPATCHED) CORRECT BEFORE THE FREEZE — `…EXPLAIN-114a_AUDIT-CORRECTION_R3-settledAt.md` §3 (four lines, T7 split, T7c pins the invariant; 13 M); R4 the instruction's own bad cite (the clock law is `HomeSynapseArchRules:98`). The core msg is cut (`_scratch/2026-09-12_core_EXPLAIN-114a_commit-msg.txt`); the card follows the lane's §5. The return rode `7ecaffa` before its audit (22 read 23); no harm. **Sample #14** (`72efb42`, run 34717732026, `available_processors=2`): soak 20/20 `anomalies=0`; census `automation_engine delivered=24 checkpoint=24 missed=0`; 20/20/20 lines · 0 died · 0 kind E — the instrument prints on the runner; silent sample 1 of 10. **REPRO-1** (Nick's desk, `vt_parallelism=2`, K=200): 200/200, `missed=0`, kind E 0 — green; the desk still does not reproduce the class. **Landed:** hivemind `7ecaffa` (23), docs `0d62a54` (AMD-101 PROPOSED). **v71 CLOSES here** (five beats; the deliverable on disk and landed; context health). **v72 opens on:** the R3 return → the EXPLAIN core card → CI → `BUSORDER: go` (BUS-ORDER-1) → 114b from the return (DISABLED-2, the cursor helper, the constructor collapse) → HERO-1c on web-ui → `AMD101: ratify` → R-4c's record → the v1.1.4 freeze note. ctx: beat 5 · calls ≈180 · read ≈250 KB · wrote ≈140 KB

**Leverage line:** one additive key with a self-contradicting null caught before the push, not after — the freeze makes the difference.
**The alternative shape, rejected:** landing 114a as returned and fixing R3 in 114b — a frozen wire is corrected before it freezes.
**2029 test:** the revert clock is a count in the OR row, not a feeling; sample #14 is its first tick.

## 2026-09-12 (v71 beat 4 — AMD-101 and BUS-ORDER-1 authored on the ruling; the docs card handed as a queued act, Sat 2026-09-12 ~16:1x CT (instrument 2026-09-12T21:15:53Z))

**The finding that shaped the AMD:** Doc 01 §3.4 (`homesynapse-core-docs/design/01-event-model-and-event-bus.md:170–:247`) already describes the model Nick ruled — step 3 "bus notifies via `LockSupport.unpark()` when matching events exist beyond the subscriber's checkpoint", step 4 "subscriber polls `readFrom(lastCheckpoint, batchSize, filter)`", step 5 "processes each event, then acknowledges by writing checkpoint". The realization drifted: `notifyEvent` hands positions into a per-subscriber queue guarded by the persisted checkpoint and `liveLoop` delivers what it is handed. So AMD-101 is a REALIZATION CORRECTION, not a design change: it states the invariant once (AMD-101 §2 — Nick's acceptance bar, sentence by sentence), names the drift with its exhibits by path (§1, §6), rules the six realization changes (§3) and carries the Doc 01 §3.4/§9 edit that executes at `AMD101: ratify` (§4). PROPOSED in the docs working tree; Nick's card lands the proposal (1 A). **BUS-ORDER-1** (`…BUS-ORDER-1_read-forward-live-delivery_coding-instruction.md`, ISSUE-READY, ⛔ gated on the EXPLAIN-114a landing and `BUSORDER: go`): the cursor in `SubscriberRuntime`; `liveLoop` pages `readFrom(cursor, liveReadBatch)` and delivers in order through the unchanged supervisor; the notify wakes LIVE unconditionally; `TransitionCoordinator` hands the cursor; `EventBusConfig` + two fields; T1–T3 red-first; `DeliveryAnomalyEmissionTest` the only existing test that may change; `ReplayTransitionIT` untouched (a STOP gate); the K=200 two-carrier desk soak in the gate lines; (ii)-B-i folded in; the cap and the gate task names by the b2 checklist. **Next:** HERO-1c's charter (web-ui; b5 or v72); the EXPLAIN intake on `EXPLAIN: RETURNED`; sample #14 on `SAMPLE:`; R-4c today. Order: hivemind 22 = 12 M + 10 A by explicit paths (computed from porcelain inside the splice); docs 1 A. ctx: beat 4 · calls ≈150 · read ≈225 KB · wrote ≈125 KB

**Leverage line:** the design already said it; the amendment says it once, with the exhibits.
**The alternative shape, rejected:** a new §3.4 — the drift was in the code; a rewrite would hide that the doc was right.
**2029 test:** the invariant is one sentence, the corollaries five, each with a named test in the instruction.

## 2026-09-12 (v71 beat 3 — `72efb42` landed, CI green = sample #14; `BUSFIX: b` ruled and filed; EXPLAIN-114a re-baselined and handed, Sat 2026-09-12 ~16:0x CT (instrument 2026-09-12T21:09:02Z))

**Banked:** core `72efb42` (FIX-2b-ii (i), 10 M; `ff1a6e1..72efb42`), porcelain 0, push count 0; CI GREEN on Nick's word (`check` · install-smoke · `bus-soak`) — sample #14, the first carrying `automation.run_handoff` and kind E; its artifact is owed (`SAMPLE: 72efb42 at _scratch/v71/ci-72efb42/`) and is read by the hub, not assumed. **The ruling:** `BUSFIX: b` — store-driven, in-order LIVE delivery; filed verbatim with Nick's reasons and his acceptance bar (`…2026-09-12_v71_BUSFIX-b_ruling-of-record.md` §1–§2): the invariant stated once in the AMD (every matching position to each LIVE subscriber exactly once, in position order, whatever the notify order); the in-memory max-delivered as the read-forward's truth, the persisted checkpoint the durable floor; the notify a wake hint plus a bounded idle tick; two red-first tests (out-of-order notifies; the TRANSITION→LIVE flip); the replay and transition ITs untouched; the desk soak K=200 under two carriers green; then twenty consecutive `bus-soak` samples with kind E at zero (the OR's closure). Sequenced after EXPLAIN-114a; the AMD to Doc 01 §3.4 and BUS-ORDER-1's instruction authored in parallel; the revert to (c) pre-registered (ten samples + REPRO-1, kind E never firing). (ii)-B's B-i folded into BUS-ORDER-1. **EXPLAIN-114a** (`…EXPLAIN-114a_read-API-v1.1.4-additive_coding-instruction.md`): §14 re-cut — the baseline clause lists the six subjects of `eabdbb1..HEAD` exactly and names the core/automation delta the lane must re-pin around; the paste is Nick's one act; the hivemind card (21) follows while the lane runs; R-4c today. **Next:** b4 the AMD + BUS-ORDER-1's instruction; b5 HERO-1c and the close; sample #14 on `SAMPLE:`; the EXPLAIN intake in v72 if long. Order: hivemind 21 = 12 M + 9 A by explicit paths (computed from porcelain inside the splice). ctx: beat 3 · calls ≈135 · read ≈205 KB · wrote ≈95 KB

**Leverage line:** the ruling arrived with its own acceptance bar; the AMD and the instruction transcribe it.
**The alternative shape, rejected:** B-i as its own lane before EXPLAIN — B-i's parts are (b)'s, and the slot is worth more to the differentiator.
**2029 test:** the revert condition and the closure count are written before the first sample is read.

## 2026-09-12 (v71 beat 2 — FIX-2b-ii (i) returned and ACCEPTED; REV-1 finds the census's second face at source; (ii)-B authored; the core card handed, Sat 2026-09-12 ~14:1x CT (instrument 2026-09-12T19:17:27Z))

**The intake:** the return (9,978 B) audited two-layer — the diffs read whole at the eight sites; Nick's `clean` + `check` BUILD SUCCESSFUL (156 tasks); the tagged pair green, `loops=20 ok=20 anomalies=0`, 20/20/20 lines. ACCEPT; the hub spliced the `core/event-bus` and `lifecycle` MODULE_CONTEXTs (kind 6 → 7; the notify-order gotcha; the parent-logger capture; the handoff census); **the core card 10 = 10 M** (`_scratch/2026-09-12_core_FIX-2b-ii-i_commit-msg.txt`). **REV-1** (an agent, read-only; `…REV-1_second-read_return.md`): `NotifyingEventPublisher` notifies on the caller's thread with no lock across append + notify; `liveLoop` checkpoints each delivery in delivery order; `UPSERT_SQL` is `INSERT OR REPLACE` — `[head, head−1]` regresses the checkpoint and the census's `position <= checkpoint` rule prints `missed=1 first_missed=<head>` for a fully delivered subscriber: #11 and #12's tuple byte for byte. The harmful arm — `head−1`'s late notify skipped by the guard — is invisible to that census; kind E names it. The soak's stall stays open, instrumented by (i). **Authored:** FIX-2b-ii (ii)-B (`…FIX-2b-ii-B_notify-order_census-truth_coding-instruction.md`): B-i — `maxDelivered` per runtime, the checkpoint monotonic, the census on the in-memory max, `bus.head:` types on timeout, T1 red-first; B-ii — an H10 (a: LIVE offers unconditionally · b: store-driven in-order delivery · c: wait), rec b. **Lessons** (pm-lessons): the cap arithmetic; exact gate tasks; long heredocs; transcripts under `_scratch/`. **Next:** the core card → `CI:` + `SAMPLE:` (#14) → the hivemind card → B-i → `BUSFIX:` → EXPLAIN-114a → HERO-1c → R-4c today. Order: hivemind 18 = 11 M + 7 A by explicit paths (computed from porcelain inside the splice). ctx: beat 2 · calls ≈120 · read ≈190 KB · wrote ≈80 KB

**Leverage line:** one read of an `INSERT OR REPLACE` turned two identical reds into a measured artefact and one proven drop path.
**The alternative shape, rejected:** waiting for the next red before touching the census — the token itself was wrong.
**2029 test:** the mechanism is cited to the line; the fix is a ruling with three graded options, not a patch inside the instrument.

## 2026-09-12 (v71 beat 1 — THE v71 BOOT: the source reads correct the frame audit on the checkpoint; FIX-2b-ii authored in two parts; the paste handed, Sat 2026-09-12 ~11:2x CT (instrument 2026-09-12T16:26:01Z))

**The boot:** read set 44.4 KB of 45; HEADs core `ff1a6e1` · hivemind `e16eb06` · skills `c630c5c` · bench `4539f13` · docs `73c204b`, clean, pushed; the dispatch filed verbatim. Preflight: Check 9 STALE (27/28); Check 12 STALE — CI-1, FIX-2a, FIX-2b-i, HERO-1b flipped to EXECUTED (status lines only). **The reads (`…v71-b1_boot-and-source-read_intake-audit.md` §2):** the run is handed to a VT `automation-run-<automationId>-<runId>` (`StandardRunManager:570–:575`) started right after `publishTriggered` (`:319–:320`, `:538–:539`); `runBody` catches `RuntimeException` only (`:486`) — an `Error` escapes unlogged; `BusThreadDump` hides a never-mounted run VT (16 of 36 hidden in #11). **The correction:** `PlatformThreadWriteCoordinator.submit` blocks on `future.get()` (`:69–:97`); `writeCheckpoint` is synchronous — the v70 audit's "fire-and-forget" is withdrawn. `automation_engine matched=24 delivered=23 missed=1 first_missed=24 checkpoint=23` is identical in #11 and #12: the engine (`SubscriptionFilter.all()`) did not receive the head; the offer path's one silent branch is `notifyEvent`'s checkpoint guard (`InProcessEventBus:371–:373`). **Authored:** FIX-2b-ii — (i) `automation.run_handoff` (four sites), `run_body_entered`, `run_thread_died` (the VT's uncaught handler), `action_step_started` (`StandardActionExecutor:183`), plus `DeliveryAnomaly.Kind.NOTIFY_SKIPPED_LIVE` at the guard; the hero IT asserts per loop (red-first); 8 M. (ii) a gated table keyed on the line the next red lacks. The paste is Nick's one act; the hivemind card follows on his word. **Next:** b2 REV-1 + REPRO-1; EXPLAIN-114a after (i) lands; HERO-1c; R-4c today; `H8A:` owed. Order: hivemind 11 = 8 M + 3 A by explicit paths (computed from porcelain inside the splice). ctx: beat 1 · calls ≈45 · read ≈110 KB · wrote ≈36 KB

**Leverage line:** one blocking `get()` read at the bytes turned a "token artefact" back into a real miss, in both samples.
**The alternative shape, rejected:** two lines and a caveat as dispatched — the caveat's premise was false at source; two lines cannot tell a never-mounted thread from a dead one.
**2029 test:** every claim carries its line range; the fix table is pre-registered before the red that picks its row.

## 2026-09-12 (v70 beat 10 — THE v70 CLOSE: sample #11's frame read; CI-1 landed `ff1a6e1`, `check` GREEN, the gate restored; the bus-soak job GREEN = sample #13, Sat 2026-09-12 ~10:5x CT (instrument 2026-09-12T15:51:42Z))

**The frame (sample #11, `bd5d35e`, run 34670078359):** `pending=0` on every subscriber in both failures; the engine's virtual thread parked idle in `liveLoop` (`InProcessEventBus.java:536`); every `hs-sub-read-*`, `hs-read-0`, `hs-write-0` idle; `hs-sched-0` idle; no pinned trace; 36 threads dumped, none executing the run whose `automation_triggered` (`StandardRunManager:651`, published on the engine's own thread) is the head of the store. H1 (pinning), H2 (read-executor saturation) and H3-parked are falsified; the bus delivered and went quiet; **the class is the run pipeline's hand-off after `automation_triggered`** — `core/automation`, not the bus. `SqliteCheckpointStore.writeCheckpoint` submits without waiting (`:176–:184`), so the snapshot's checkpoint lags delivery and the census's `missed=1` on an idle bus is a token artefact. Filed: `context/audits/2026-09-12_CI-bd5d35e_frame-read_bus-idle-run-handoff_v70-b10.md` (§2 names the next reads and the two INFO lines that settle the step in one push). **CI-1** (Sat 09:38–09:51 CT): returned (6,036 B), audited ACCEPT (`…CI-1_intake_two-layer-audit_v70-b10.md`; I1 applied by the hub), landed `ff1a6e1` by Nick's hands. **The gate of record on `ff1a6e1`: `check` GREEN (3 m), install-smoke amd64 + arm64 GREEN, the `bus-soak` job GREEN (1 m) — sample #13 of the class, green.** Earlier: `bd5d35e` (FIX-2b-i), `6af76f7` (HERO-1b; `FE:` owed); hivemind through `5e73d37`. **Next (v71):** FIX-2b-ii-i — two INFO lines in `core/automation` (`automation.run_handoff`, `automation.action_step_started`) + the census's checkpoint-lag caveat, first on the Java slot so every later push samples it; then EXPLAIN-114a; HERO-1c on web-ui; R-4c today; `H8A:` owed. Order: hivemind 4 = 4 M + 0 A by explicit paths (the spine; computed from porcelain inside the splice). ctx: beat 10 · calls ≈165 · read ≈420 KB · wrote ≈330 KB

**Leverage line:** thirteen samples and one dump moved the class from "the bus drops" to "the run does not continue".
**The alternative shape, rejected:** a bus rewrite on the checkpoint numbers alone — the dump shows the bus idle and correct.
**2029 test:** the frame audit carries the greps; every landing carries its CI verdict as a line.
