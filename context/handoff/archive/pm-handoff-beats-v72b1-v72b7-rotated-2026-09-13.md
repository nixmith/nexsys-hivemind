<!--
file: context/handoff/archive/pm-handoff-beats-v72b1-v72b7-rotated-2026-09-13.md
purpose: v72 beats 1–7 rotated VERBATIM out of pm-handoff.md at v73 beat 6 (the live cap is 12 blocks). bytes(kept) + bytes(archived body) = bytes(before), asserted inside the beat-6 splice.
status: ARCHIVED 2026-09-13 (v73 beat 6; instrument 2026-09-13T22:57:38Z)
-->

## 2026-09-13 (v72 beat 7 — THE v72 CLOSE: P-1 RETURNED and audited ACCEPT (the gate re-run on the device, 19/0; the fence proven); Erik's line banked; the bench card and v73's dispatch line handed, Sun 2026-09-13 ~13:2x CT (instrument 2026-09-13T18:22:51Z))

**Landed by Nick's hands:** hivemind `256c26b..45f1bf3` (13; beats 5–6). **P-1 returned** (`context/audits/2026-09-13_P-1_return.md`, 12,286 B): 3 A + 1 M on bench `4539f13`, P1–P4 HELD, RED 16/16 → GREEN 19/19, zero live commands, zero network calls under an instrumented chokepoint; five wrong cites in the hub's charter (P4); six real defects in its own first cut found by its self-audit and fixed with regression checks (the worst: a deletable factory-reset guard). **Layer 2 inside the splice:** bench porcelain exactly the four paths; `test_harness.py` 19/0 with the fence check green; `nightly_digest --selftest` 21/0; `constants.yaml` +57/−0. **Rulings on §6:** the rating's label read at H8-a (`S31-LABEL: <A> <W>`); the fold extends the reserved `plug:` key, aligns exit codes and adds a time bound; the P-1 name collision (quiesce 07-31 vs harness 09-11) disambiguated; the first live run gets its own packet through the prior-ledger gate. The bench card (4 files) is the one act. **Erik (Sun 09-13):** "Starting the search on PALOKI now" — the wait-state moves to his opinion; the 09-18 wanted-by date will not hold from a 09-13 start, the fence extends, nothing on the map depends on it. **The window closes on the deliverable met at b3** (BUS-ORDER-1 landed, green, ratified) plus R-4c's C-003 and P-1 landed; **v73 opens** on the v67 prompt unchanged — 114b's instruction, R-5's charter, HERO-1c on `FE:`, H8-a on `H8:`, the strategy fold; the plan file's §6 says what it inherits. Audit: `context/audits/2026-09-13_v72-b7_P-1_intake_two-layer-audit.md`. ctx: beat 7 · calls ≈125 · read ≈300 KB · wrote ≈260 KB · no compaction

**Leverage line:** the harness lane audited itself and found the guard its own tests could not miss — the second lane this window to hand back a stronger artifact than it was asked for.
**The alternative shape, rejected:** authoring 114b in this beat to keep the §4 promise — at ≈125 calls the window is near the v67 measure; a fresh v73 authors it better than a tired v72.
**2029 test:** the close names what is met, what is landed by whose hands, what is not banked, and the one file the next hub reads first.

## 2026-09-13 (v72 beat 6 — R-4c RETURNED (66 min at the rig on `a458a64`'s .deb) and audited against the navigator's own attack list: C-003 MINTED with a bound; the null arm held back; the prior-ledger gate adopted; P-1's paste handed, Sun 2026-09-13 ~10:3x CT (instrument 2026-09-13T15:34:09Z))

**The return:** the record + the source audit + the navigator's adversarial brief; `H8A: not run`. **Layer 2 at the bytes:** run #53 / `a458a64` / both jobs green and the arm64 digest `883c5f8d…` re-read from the public run page; the four custody surfaces identical in the record; the harvest's 11 lines: the exit's two tokens ×1 each, the four failure tokens ×0, the census 1 + 5×0, six in the list; the window 14:41:46.727Z → adopted 14:43:48.783Z (+122.06 s of 254); read 2 ran after 14:50:28Z, so the 399.9 s null window was never sampled; `:1152` is `log.debug`; the adapter: `unknown_sender` and `tc_join` reach the same `admitRejoinCandidate`, and the proposal followed the `tc_join` by 1.04 s, not the resolution 72 s earlier. **Rulings:** C-003 MINTED-BOUNDED (adopted; the ZDO surface's necessity untested — R-5 runs the counterfactual) · F-R4-1b LIVE-VERIFIED as a resolver (516/265 ms) · F-R4c-A the finding MINTED, its explanation PROVISIONAL · O-2 PROVISIONALLY CLOSED · LASTREPORTED-1b: instant arm MINTED, null arm NOT BANKED · the anomaly zero BOUNDED weakly (a rig-load zero, not a sample) · the Hue RECORDED (P-1's case) · F-R4-2 two ULIDs MINTED · the root cause BOUNDED at four of eleven. **§6 ADOPTED:** THE PRIOR-LEDGER GATE (pm-lessons; W-SKILLS-9). **§7:** the design fence lifts with F-R4c-A as a constraint, the code fence stays on the counter; R-5 counts re-seen and adopted separately; the null-arm block in the next rig packet; the ULID audit to the bench lane; P-1 now. The record's §10 written; the packet EXECUTED; v70 b10–v71 b6 rotated (seven; live 6). Audit: `context/audits/2026-09-13_v72-b6_R-4c_intake_two-layer-audit.md`. ctx: beat 6 · calls ≈100 · read ≈255 KB · wrote ≈215 KB

**Leverage line:** one source read turned "adopted by the ZDO surface" into "identified by it, adopted after its own join" — the bound the rehearsals needed.
**The alternative shape, rejected:** minting C-003 as written — the chain is real and the fleet is complete, but the causal sentence is not in the bytes.
**2029 test:** each ruling names its attack and its instrument; what is not banked is listed with the read that would bank it.

## 2026-09-13 (v72 beat 5 — THE PROGRAM RE-CUT on Nick's brief: the git history read, the desk nine days ahead and the rig unmoved since 09-06; R-4c re-pinned to `a458a64`'s .deb and handed as the one act; v73's window shaped, Sun 2026-09-13 ~07:5x CT (instrument 2026-09-13T12:57:59Z))

**Landed by Nick's hands:** docs `0d62a54..2235845` (3 files: AMD-101 RATIFIED; Doc 01; the register) · hivemind `de48f32..256c26b` (7). **The read** (`context/planning/2026-09-13_v72_PROGRAM-RECUT_git-read_objectives_and_v73-window-plan.md`): 246 core commits, 23 since 09-01, W36 and W37 twelve each; six themes since 08-28 (packaging · the honesty line · the Zigbee re-adopt surface · v1.1.3 → v1.1.4 · the hero · the bus class, seven commits ending in a ratified invariant); every landing since 09-03 left an instrument. Against the plan of record: v1.1.4 landed nine days early, the hero's design and first build a week early; R-4c (Sat) and H8-a (Fri) have no record — C-003 unreached, the B-2/B-3 fence up, P-1 → R-5 → the 72-hour rehearsals queued behind the rig. **The ranking:** the rig first (R-4c, then H8-a), P-1's paste after, the desk lanes unattended (HERO-1c on `FE:`, 114b on the Java domain), the dated company items this week (`Activate:` 09-15 · Silabs 09-17 · the opinion and 09-18), the counter passive. **R-4c re-pinned** (eleven `1e26912` mentions → `a458a64`; the audit path kept; the CI aside rewritten; MEASUREMENT-ONLY unchanged) — the rig measures the tree of record. **v73's shape:** two pastes authored by v72 b6, handed at v73 beat 1; intakes as beats; the strategy fold at beat 6 by range; a splice library proposed. **Minted:** THE OUTPUTS-PATH MIRROR (pm-lessons). ctx: beat 5 · calls ≈80 · read ≈200 KB · wrote ≈180 KB

**Leverage line:** the git log priced the fortnight in one screen — a third of the desk went to a bus class nobody planned and came back as an invariant; the rig, which no one else can run, is the only line that did not move.
**The alternative shape, rejected:** a strategy read tonight — the north star and the brand strategy are read by range at v73 beat 6, after R-4c's record exists; reading them now would spend this window's context on a fold with one input missing.
**2029 test:** the re-cut names its inputs (the log, the two plans) and its date; the ranking is per hour of the one non-substitutable resource; the v73 shape is measured against v72's own numbers.

## 2026-09-13 (v72 beat 4 — AMD-101 RATIFIED on Nick's word; five edits executed in the docs tree (three files); the ruling of record filed; the docs card handed, Sun 2026-09-13 ~07:3x CT (instrument 2026-09-13T12:32:17Z))

**The word:** `AMD101: ratify`, with a rationale and six checks (verbatim in `context/planning/2026-09-13_v72_AMD101-ratify_ruling-of-record.md`; his §DONE line: hivemind `de48f32`, 5 files). **The checks at the bytes:** `HOME_DEFAULT` at `a458a64` = 64 / 250 ms — the AMD's values stand; INV-PROJ-04's statement names "a subscriber's persisted checkpoint" — in scope by its own words, one clause added; Doc 01 §11 lists no kinds — no edit; the kinds still emitted from `src/main`: `LIVE_READ_FAILED` (`:574`) and `NOTIFY_NOT_VISIBLE` (`:337`), the five others defined and unemitted; AMD-101 §7 has no revert row — the revert condition lives in the BUSFIX-b ruling and is voided there; the run number: the Actions API refused the hub (403) — `RUN: 5f918c7 <n>` fills it whenever. **Two corrections to his reading:** position 206 was delivered in sample #15 (census 254/254, `missed=0`) — the arm fired, the loss did not; and kind E fired on the second sample under the instrument (#14 silent, #15 fired). **Executed (docs tree, 3 M):** AMD-101 RATIFIED (§4 execution note, §5 checks, §6 note, §7 rows 1–2) · Doc 01 (the head pointer; §3.4 the paragraph after step 6; §9 `live_read_batch: 64`, `live_idle_tick_ms: 250`) · the register (INV-BUS-01 "and in position order" + the cursor clause; INV-PROJ-04's BUS-ORDER-1 clause). **Hivemind:** the ruling of record (new); the BUSFIX-b revert clause VOID; the TR-1 audit's census-definition addendum; the TR-1b row carries the sentence. HERO-1c's charter moves to b5. ctx: beat 4 · calls ≈58 · read ≈150 KB · wrote ≈100 KB

**Leverage line:** six checks Nick named cost seven reads and changed two edits (INV-PROJ-04 gained a clause; §5 gained the emitted-kinds sentence) — the amendment ratifies against the tree, not against its own draft.
**The alternative shape, rejected:** applying his six checks as written — two of his readings (the "drop for good"; "the first sample") were not what the artifact says, and the record carries the bytes, not the recollection.
**2029 test:** the ruling file holds his words and the hub's adjudication side by side, each point tied to a line number or a grep count; the AMD's status line points at it.

## 2026-09-13 (v72 beat 3 — BUS-ORDER-1 LANDED `a458a64`, CI GREEN on both jobs: the window's deliverable is met; the closure counter restarts at 1/20; AMD-101 ready to ratify, Sun 2026-09-13 ~06:5x CT (instrument 2026-09-13T11:54:51Z))

**Landed by Nick's hands** (his transcript at `_scratch/v72/2026-09-13_v72-b3_nick-report_landings.md`): core `5f918c7..a458a64` — `16` · `staged: 16` · `0` · 16 files, +1,497/−530 (verified at the instrument: HEAD `a458a64`, porcelain 0, ahead 0); hivemind `7210fb1..2fabfc7` (8). **The gate of record:** his line — `a458a64` GREEN on every action and check, `check` and `bus-soak` both. That `bus-soak` run is sample #16 and the closure counter's first: 1/20 (green by verdict; its artifact not fetched — `SAMPLES: passive`; the census line's `delivered_max=` is read at the next artifact anyone opens). The Deferred Build Gate on BUS-ORDER-1 RESOLVES: CI green on the landing sha. The instruction flips EXECUTED. **The deliverable named at beat 1 is met:** BUS-ORDER-1 running, audited, landed, green. **AMD-101:** its §7 rows 3 and 4 tick in the docs tree (landed and audited; the counter restarted at `a458a64`); rows 1 and 2 wait on Nick's word — the §4 edits are three (Doc 01 §3.4, §9, INV-BUS-01) plus the §6 exhibit; `AMD101: ratify` is due and asked; the docs card follows in the beat after the word. **The rest of the window:** HERO-1c's charter (b4, authored ahead of `FE: 6af76f7`), 114b's instruction (b5, the Java domain is free), the intakes as the lines come; close ≤ b8. ctx: beat 3 · calls ≈46 · read ≈125 KB · wrote ≈70 KB

**Leverage line:** sample #15's one line at 19:5x Saturday became a landed fix with a green sample at 06:5x Sunday — one hub window, one lane, one operator, no chat storage.
**The alternative shape, rejected:** asking for the `a458a64` artifact to read `delivered_max=` tonight — `SAMPLES: passive` stands and the green is not a grant; the line is read at the next artifact opened for cause.
**2029 test:** the gate is one line in Nick's words beside the sha and the instrument's porcelain; the counter's start is a named sample.

## 2026-09-13 (v72 beat 2 — BUS-ORDER-1 RETURNED and audited ACCEPT: the read-forward loop on disk, 16 files, gates green on the desk; the core card handed, Sun 2026-09-13 ~06:3x CT (instrument 2026-09-13T11:31:49Z))

**The intake.** The lane ran Sat ~20:55 → ~22:3x CT on `5f918c7` and returned `context/audits/2026-09-12_BUS-ORDER-1_return.md` (14,215 B): P1–P4 HELD (T1 forced the skip arm; T2 forced reversed hints and found the drain dropping two positions at the flip — kind E's drain-side twin; T3 timed out at two ticks), P5 owed to CI; 4 [REVIEW] · 8 [INFO] · O1 (a spent unpark permit: p99 553 → 35 ms once the loop parks only on an empty hint queue; T5 pins it). **Layer 2 at the bytes:** core porcelain 16 = 14 M + 2 A, staged 0, `module-info` diff empty, production confined to `core/event-bus`; the five §2 corollaries each hold a test (`ReadForwardDeliveryTest` T1/T2/T2b/T3/T5); the park sits behind the hint-queue check (`InProcessEventBus:604–610`); `TRANSITION_READ_*` survive only in the enum and one javadoc line; `ReplayDriver` gains two `advanceCursor` calls; **the gate re-derived from Nick's own `check` XMLs** (11:18–11:20Z): 240/0 (30 skipped) · 413/0 · 81/0 · 30/0. Not re-executed, said: the K=200 soak line (its XML was overwritten by the later `check`) and the JDK probe behind O1. **Rulings:** R1 · R2 · R3 ACCEPT; R4 → AMD-101 §4 gains INV-BUS-01's clause (docs tree, rides `AMD101: ratify`). Hub-owned: the Files table said 8 M + 3 A, the tree says 14 M + 2 A. **The landing:** the core card, 16 explicit paths; CI is the gate of record and its first `bus-soak` sample the closure counter's first. Audit: `context/audits/2026-09-13_v72-b2_BUS-ORDER-1_intake_two-layer-audit.md`; Nick's transcript at `_scratch/v72/2026-09-13_v72-b2_nick-report_BUS-ORDER-1.md`. ctx: beat 2 · calls ≈38 · read ≈115 KB · wrote ≈60 KB

**Leverage line:** the second red test found a silent two-position drop the ruling had not named, and the soak found a lost-wake mechanism the design had not named — both fixed before the first push.
**The alternative shape, rejected:** landing on the lane's gate table alone — Nick's own `check` left XMLs on the desk, and reading them cost one call.
**2029 test:** every ruling names the line it rests on; the one open slot (the soak line) is named as open and closes on CI's next artifact.

## 2026-09-12 (v72 beat 1 — THE v72 BOOT AND THE SAMPLE #15 READ: kind E; the revert clock stops; BUS-ORDER-1's gate met and its paste handed, Sat 2026-09-12 ~19:5x CT (instrument 2026-09-13T00:53:13Z))

**The window's one deliverable:** BUS-ORDER-1 running on `5f918c7` on the kind-E read, its return audited against AMD-101 §2 if it comes inside the window. **The boot:** the read-set 43.7 KB of 45; the HEADs at the dispatch state (core `5f918c7` · hivemind `3209101` · skills `c630c5c` · bench `4539f13` · docs `0d62a54`; porcelain 0 ×5; ahead 0 ×5; no locks); the preflight 12 lines — Check 9 STALE by the known file (`project-manager/SKILL.md`, 27/28), Check 12 one flip (the R3 paste → EXECUTED), the rest PASS; his dispatch text filed verbatim. **The read at the bytes** (`_scratch/v71/ci-5f918c7/…/TEST-com.homesynapse.lifecycle.BusSoakIT.xml`, 28,929 B; 2026-09-12T23:47:03Z, a 2-vCPU runner): `bus.delivery_anomaly: kind=NOTIFY_SKIPPED_LIVE subscriber=automation_engine position=206 detail=notifyEvent: checkpoint=207 at or past position` at 23:47:05.464, `[Test worker]`, between the 16th and 17th `run_handoff` lines; `bus.soak: loops=20 ok=20 timed_out=0 p50_ms=21 p99_ms=70 max_ms=70 anomalies=1`; the census `automation_engine matched=254 delivered=254 missed=0 checkpoint=254`, `state_projection` 254/254; the census IT green. The skip did not become a miss in this run; the kind is the arm AMD-101 removes. **Under the ruling:** AMD-101 §6's reserved row is filled (the docs tree, 1 M; rides `AMD101: ratify`); the revert clock STOPS at 1/10; BUS-ORDER-1's gate is met at the instrument (`72efb42..HEAD` = one commit; tree clean) — §8 is the one act; `BUSORDER: go` is his line when the lane runs. The run number is the one slot the exhibit leaves open. Filed: `context/audits/2026-09-12_v72-b1_sample-15_5f918c7_kind-E_reading_and_boot-intake.md`. v70 b5–b9 rotated (five blocks; live 8). ctx: beat 1 · calls ≈26 · read ≈80 KB · wrote ≈32 KB

**Leverage line:** one WARN line read at the bytes turns a paused ruling into a running lane; the instrument landed two pushes ago is what made the line exist.
**The alternative shape, rejected:** dispatching on the CI count alone — the count says one anomaly, the kind says which arm; another kind would have sent the lane to the wrong fix.
**2029 test:** the exhibit is quoted verbatim in a filed audit and cited by the AMD; a reader in 2029 re-derives the ruling from the line, not from this block.

