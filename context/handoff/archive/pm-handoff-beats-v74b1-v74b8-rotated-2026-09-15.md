<!--
file: context/handoff/archive/pm-handoff-beats-v74b1-v74b8-rotated-2026-09-15.md
purpose: v74 beats 1–8 rotated VERBATIM out of pm-handoff.md at v75 beat 5 (the live cap is 12 blocks). bytes(kept) + bytes(archived body) = bytes(before), asserted inside the beat-5 splice.
status: ARCHIVED 2026-09-15 (v75 beat 5; instrument 2026-09-15T23:09:46Z)
-->

## 2026-09-14 (v74 beat 8 — THE v74 CLOSE: hivemind `e5fd594` LANDED (10); the five HEADs re-derived clean, push 0 ×5, no locks; Check 9 PASS 28/28 (the SOURCE trees' list md5 = the synced list's); the definition of done met on the disk; Nick's close brief banked verbatim; `PLUG: two` RULED with the criteria and THE ADOPTION FENCE; the plan §11 = v75's window; the ledger re-cut; v75's line handed; no new authoring, Mon 2026-09-14 ~13:0x CT (instrument 2026-09-14T18:02:58Z))

**Landed at diff-tree:** hivemind `e5fd594` (10); the other four HEADs clean. **The deliverable, adjudicated:** H8-a's packet through the gate (b1) — MET; R-5B's packet — NOT cut here (a gate act on the tail of a tired window is how R-4c inherited six deviations) → the NEXT WU, v75 b1; the sitting Sat 09-19 by default, both packets in Nick's hands by Friday. **The definition of done on the disk:** the map's return and audit filed (ACCEPT); IR-15..IR-19 present; D-v74-4 RULED; the plan §10 present; every instruction of the window EXECUTED or LIVE by rule (Check 12: the H8-a packet only); Check 9 28/28 — no discrepancy to name. **Banked from Nick's brief (DR §1 verbatim; §3/§4 the rulings):** `requires: [operator]` for reference-meter readings; THE MEASUREMENT RECORD minted once at ENERGY-READ; the frame rate counted at the ingestion unit (one block in R-5B's packet); `PLUG: two` — STANDARD_ZCL only, 0x0B04 AND 0x0702 as server clusters with multiplier/divisor, the divisor into the fixture first, the Shelly Plug US Gen4 units graded first; THE ADOPTION FENCE — no metering plug joins any card before ENERGY-READ is green; a market/IoT strategy pass recorded, with the `Research Papers` folder (the moat curriculum) Nick connected at the close inventoried in the DR §1. **Measured:** 8 beats · ≈110 calls · three lanes accepted + H8-a re-cut · core +1 green · hivemind +8 · skills +1 · no compaction. **Next WU:** R-5B's packet through THE PRIOR-LEDGER GATE (v75 b1). ctx: beat 8 · calls ≈5 · read ≈8 KB · wrote ≈22 KB

**Leverage line:** a window that names what it did not do hands the next one a first act instead of a doubt.
**The alternative shape, rejected:** cutting R-5B's packet in this beat to meet the deliverable on paper — Nick's own line: no packet on the tail of a tired session.
**2029 test:** which half of the deliverable was met and which was handed; the word Nick gave and the fence; where v75's window lives.

## 2026-09-14 (v74 beat 7 — THE AMBITION MAP INTAKEN, ACCEPT with the hub's critique: returned in one hour (14,313 B; 9 admitted · 13 rejected · 2 measured-at-R-5B); every load-bearing source cite re-executed at `6bd8508` and TRUE, the secondary page re-fetched and TRUE; IR-15..IR-19 minted (IR-15 a DEFECT and a fence); D-v74-4 `PLUG:` packaged; the plan's §10 addendum; hivemind `fe2d32d` LANDED (8); the b7 card handed, Mon 2026-09-14 ~11:5x CT (instrument 2026-09-14T16:54:42Z))

**Landed at diff-tree:** hivemind `fe2d32d` (8); push 0. **The map** (`context/research/2026-09-14_AMBITION-MAP_return.md`): P1 confirmed on rank, refuted on instrument; P4 corrected the charter's own fence (2) — the plug's read side does NOT exist at HEAD: `ClusterHandlers` registers eight handlers, none for 0x0B04/0x0702; `EndpointClassifier` :99–:101 maps 0x0051 to `SWITCH` + `on_off`; the bench plug (S31 Lite ZB) exposes a switch alone. So `HARNESS-PLUG:` promotes a power-cycle harness only; the energy class is a purchase + one Core WU. Also true: `iasCapability()` :175 selects `motion` for every non-CONTACT zone while `IasZoneHandler` :77 emits `active` — a one-way-door DEFECT (IR-15: the micro-WU after 114c; no such device joins until it lands); LQI/RSSI parsed and dropped, `TelemetryWriter` unproduced (IR-16, Seam B); Seam A (IR-17/18). **The critique, RULED:** the metering truth is a reference meter (±10 %), not a label; a reading's verdict is `within|outside` — `confirmed` stays the action leg's word; LINK-READ measures the frame rate first (arc 1); the three `.docx` strategy files were outside the lane's reach (IR-19). The ranking and the thirteen rejections stand; the fence holds — nothing dispatches before R-5B and rehearsal 1. **D-v74-4 `PLUG: two|one|hold`** (rec two of one metering model + a reference meter). **Handed:** the b7 card (9). **Next:** b8 the close — R-5B's packet to v75 b1; `H8:` asked once. ctx: beat 7 · calls ≈6 · read ≈24 KB · wrote ≈24 KB

**Leverage line:** a map that found its own charter's premise false at the source is worth more than one that confirmed it — the first energy datum will now be a measurement, not a label.
**The alternative shape, rejected:** cutting R-5B's packet beside the intake — a gate act gets a fresh beat, not the tail of one.
**2029 test:** which cites were re-run and true; what the critique changed; the one defect that must land before a device is adopted.

## 2026-09-14 (v74 beat 6 — THE SPINE ROTATED (v73 b1–b7 → the archive, verbatim, bytes asserted); core `6bd8508` (HERO-1d) LANDED, CI ALL CHECKS GREEN (the counter 4/20; IR-8 RETIRED at the sha); hivemind `548744f` LANDED (9); THE AMBITION MAP CHARTERED inside Nick's fences and its paste handed; the b6 card handed, Mon 2026-09-14 ~07:0x CT (instrument 2026-09-14T12:07:08Z))

**Landed at diff-tree:** core `6bd8508` (17 rows; Nick's line "passed all checks GREEN" banks as `CI: 6bd8508 all checks green` — a passive `bus-soak` sample: **4/20**); hivemind `548744f` (9); push 0 ×2; both desk slots free. **The rotation:** v73 b1–b7 (7 blocks) → `context/handoff/archive/pm-handoff-beats-v73b1-v73b7-rotated-2026-09-14.md` verbatim; the map row added; bytes(kept) + bytes(archived) = bytes(before), asserted; 6 live after this one. **The ambition map** (`context/instructions/2026-09-14_strategy-lane_AMBITION-MAP_expansion-rows-with-instruments_charter.md`, DISPATCH-READY): §0 carries Nick's three fences in force (half-proven rows "measured at R-5B"; row one the metering-plug class; nothing dispatches before R-5B lands and the first rehearsal is on the calendar) and the inherited laws (the north star; the Substrate Thesis §11; the language law; the four-qualifier law; no Matter before P3; gate sovereignty); §1 the strategy layer whole + the register, the plan, the findings cards, the R-5 charter, P-1's design; §2 the row schema — THE INSTRUMENT is the gate column; §3 AM-1..AM-5 pre-registered + the lane's own; §4 the overreach test; §5 ONE return ≤14 KB, ≤5 H10 rows for October–December; P1–P4. **Handed:** the paste (the act) → the b6 card (7). **Next:** b7 R-5 Part B's packet through THE PRIOR-LEDGER GATE (the deliverable); `H8:` asked — today if the rig is free. ctx: beat 6 · calls ≈8 · read ≈18 KB · wrote ≈26 KB

**Leverage line:** a map whose gate column is the instrument cannot be argued with in chat — a row either names how it would be proven or it is not on the map.
**The alternative shape, rejected:** 114c or FE-114 as the second lane today — both are desk lanes the next window authors better with a clean context; the map is the one lane Nick's brief is waiting on.
**2029 test:** where the seven v73 blocks went and that the bytes balanced; which sample the counter took; what the map may and may not do, in Nick's own fences.

## 2026-09-14 (v74 beat 5 — HERO-1d INTAKEN, ACCEPT: returned Sun ~21:5x CT (11,613 B; 17 = 14 M + 3 A under web-ui; `npm run verify` GREEN, 480 tests; the lint 11 → 0; P2/P5 failed on their counts, disclosed); the hub's re-execution at the bytes; IR-8 RETIRED, IR-12/13/14 minted; the charter EXECUTED; the web-ui card (17) and one hivemind card covering b4 + b5 handed — the b4 card OVERTAKEN unrun; Mon 2026-09-14 ~06:5x CT (instrument 2026-09-14T11:53:23Z))

**Intake at the bytes (core `93390f0`):** porcelain 17, all under `web-ui/dashboard/`, 3 `??`; +350/−39; 0 `expect` lines removed, 43 added; 24 new keys; six byte-identity spot checks 6/6; `colour` 11 residual (the five sites 0); SPEC 24 rows + the amendment line; the lint block's three selectors. Not re-run: vitest/tsc/the build (`node_modules` off the mount) — `frontend.yml` on the push is the gate. **ACCEPT** (`context/audits/2026-09-14_v74-b5_HERO-1d_intake_two-layer-audit.md`). **Findings → the register:** IR-12 the lint pattern's reach (apostrophes, two-word runs, a capital before `${}`, property literals); IR-13 the dialect of record — RULED US (`Cancelled` stays; the residual UK spellings are §7 text changes at FE-114); IR-14 `format.ts` `pendingHint` = the catalog's sentence. The two unconsumed terminal rows → FE-114's charter. IR-8 RETIRED on the landing. **The b4 card was never run** (porcelain said so): OVERTAKEN by this beat's one hivemind card, never a ghost. **Handed:** block 1 the web-ui card (`git add -A web-ui/dashboard`, 17; `_scratch/v74/2026-09-14_core_HERO-1d_commit-msg.txt`) → `CI: <sha> frontend <green|red> check <green|red> bus-soak <green|red>` (a counter sample) · block 2 the hivemind card (9: b4's five + the charter, the return, the audit, the register). **Next:** b6 the spine rotated (12 live); the ambition-map charter; R-5B's packet through the gate. ctx: beat 5 · calls ≈6 · read ≈20 KB · wrote ≈22 KB

**Leverage line:** a lane that returns the same evening and a hub that lands it the next morning is a slot that turned over in twelve hours.
**The alternative shape, rejected:** landing HERO-1d on the lane's own `verify` line — CI on the push is the gate of record, and the lint's AST count is the one number the hub could not re-run.
**2029 test:** what the hub re-ran and what it could not; which count the lane missed and why the rows were right anyway; where the dialect was ruled.

## 2026-09-13 (v74 beat 4 — HERO-1d CHARTERED on the ground at `93390f0`, its paste handed; `SCOPE: lane` RULED — Nick's two sharpenings and one caution banked verbatim (DR §1) as the ambition-map charter's fences (DR §4); `Activate:` the word Tuesday; hivemind `169e6d3` LANDED (7); the b4 card handed, Sun 2026-09-13 ~20:4x CT (instrument 2026-09-14T01:49:45Z))

**Landed at diff-tree:** hivemind `169e6d3` (7); push 0; core `93390f0` clean — the web-ui slot free. **HERO-1d** (`context/instructions/2026-09-13_frontend-lane_HERO-1d_keyless-hero-sentences_literal-lint_charter.md`, DISPATCH-READY): D0 IR-8's literal lint FIRST (an ESLint block over the six hero files — JSX text nodes · template literals · title/lede/label attributes; the hit list at HEAD is the baseline, 0 the gate, inside `npm run verify`); D1 `terminalLine` keyed; D2 the condition line, the cascade link, the do-nothing step + hint, the recovered suffix; D3 the hub page; D4 the why-not and runs pages; D5 the §7 amendment (no sentence changed); D6 `colour` → `color` at five sites (arc 25); D7 MODULE_CONTEXT. The ground cited line by line from the source at `93390f0`; P2: ≥ 12 lint hits at HEAD → 0; P5: `colour` 5 → 0. **D-v74-3 RULED `SCOPE: lane`:** the rig's half-proven rows marked "measured at R-5B"; the expansion rows ranked by what the 72-hour run and the corpus need; row one pre-registered — the metering-plug class (P-1's harness; `state_reported` power/energy; a known load); presence/mmWave needs a bench scenario first; air quality read-only; whole-home energy P4; the fence: the map INFORMS October's P2/P3-seam charters — nothing dispatches from it before R-5B lands and the first rehearsal is on the calendar. The charter at b5. **Handed:** the HERO-1d paste → the b4 card (6). **Next:** b5 the ambition-map charter + 114c; b6 R-5B's packet; `H8:` asked once at the close. ctx: beat 4 · calls ≈9 · read ≈26 KB · wrote ≈24 KB

**Leverage line:** a lint that fails on a literal sentence retires the §6.3 grep and every future "did the lane key everything" audit at once.
**The alternative shape, rejected:** 114c first — the heavier instruction, and no lane waits on the Java slot; the web-ui lane runs overnight from a charter the ground already supports.
**2029 test:** which sentences moved and why the screen did not change; where Nick's ruling lives in his words; what the map may not do before R-5B.

## 2026-09-13 (v74 beat 3 — THE THREE HUB CARDS IN ONE + NICK'S AMBITION BRIEF BANKED: hivemind `1b1eb98` + skills `180375f` LANDED (W-SKILLS-9 closed; Check 9 PASS); the v1.1.4 freeze note written into the freeze doc; C-003 MINTED-BOUNDED in the claim register (narrow); the v74 decision record opened — D-v74-2 (the Activate H10; rec hold) and D-v74-3 (the ambition map; rec a lane); the b3 card handed, Sun 2026-09-13 ~19:5x CT (instrument 2026-09-14T00:51:37Z))

**Landed at diff-tree:** hivemind `1b1eb98` (18) · skills `180375f` (4); push 0. **The freeze note** (`context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md`): the stamp → **v1.1.4** (HERO-0 §3 / `EXPLAIN: three`; 114a `5f918c7` + 114b `fed99e8`; the FE mirror `6af76f7`); the seven keys + `FIRED_CONFIRMED` in the shapes; three inline `[v1.1.4 amendment]` notes (the null rules; `firingValue` now populated; the order pins; DISABLED-2 → AMD-102) — written from `api/rest-api/MODULE_CONTEXT.md` §EXPLAIN-114a. **114c unblocked.** **C-003** (`context/strategy/claim-register.md`): the placeholder → the narrow claim on `a458a64` (one sleepy end device by the ZDO surface; the fence — not the fleet; resolution ≠ adoption; no automation); refutable-by R-5 B2; `C003: wait` reverts. **D-v74-2** (`context/planning/2026-09-13_v74_decision-record.md` §3): Activate — apply · hold · skip; rec hold; the word Tue/Thu. **Nick's brief, mid-beat (DR §1 verbatim):** be more ambitious — more devices, integrations, protocols; more complex, data-rich device designs — without overreaching the long-term strategy. **D-v74-3:** (a) a strategy lane, "the ambition map", every row with an instrument, graded against the strategy layer · (b) the plan's re-cut at the close · (c) a WU now (rejected: arc 1); rec (a); `SCOPE: lane|close|<expansion>`; silence = (a), chartered at b5. **Handed:** the b3 hivemind card (7). **Next:** b4 HERO-1d + 114c authored, one dispatched. ctx: beat 3 · calls ≈14 · read ≈32 KB · wrote ≈30 KB

**Leverage line:** a brief banked verbatim and packaged as three graded options is the difference between a direction and a program.
**The alternative shape, rejected:** answering the ambition brief in chat with a list of protocols — a list without instruments is the overreach the brief warns against.
**2029 test:** where v1.1.4's record lives and what it was written from; what C-003 claims and fences; the two decisions packaged and what Nick said, in his words.

## 2026-09-13 (v74 beat 2 — W-SKILLS-9 INTAKEN, ACCEPT: hivemind `d214fd8` LANDED (6 rows); the return audited two-layer (0 rule names lost; Check 12 identical ×3; 13/13 md5s); Check 9 PASS 28/28 against Nick's sync; IR-11; the charter EXECUTED; two cards handed, Sun 2026-09-13 ~19:3x CT (instrument 2026-09-14T00:36:45Z))

**Landed, verified at diff-tree:** hivemind `d214fd8` (6 rows; push 0). **W-SKILLS-9 (`context/audits/2026-09-13_W-SKILLS-9_return.md`, `RETURNED … 8853`) — the hub's re-execution:** the `**UPPER-NAME**` census over the 14 files, HEAD vs tree: 68 → 73 distinct, **0 lost**, 5 gained (arcs (56)–(57) at `laws-ledger.md:79–80` among them); Check 12's shared section md5 `404da674` / 3,276 B in all three copies; the return's md5 list 13/13; `pm-lessons.md` = nine headings suffixed, zero other lines, 341 lines (P4 iv); no sha, no new beat number in any SKILL.md; the fold rows resolve (CIF #31 :561 · WUCP :310 · R&Q :195 · the environment model :103–105). **Check 9 PASS 28/28** — Nick's account sync equals the post-fold SOURCE trees at every md5; the record catches up on the cards. **ACCEPT** (`context/audits/2026-09-13_v74-b2_W-SKILLS-9_intake_two-layer-audit.md`). P1 refuted on the charter's arithmetic (9/3 typed against a table of 10/4) → **IR-11** (a charter's census is counted from its table). The charter → EXECUTED with this commit. **Correction:** b1's splice left the chain pointer un-advanced (118 / v73 b5; the archive's 119 headings were right) — now 120 / v73 b7. **Handed:** the hivemind card (18 by explicit paths: the lane's 11 + this beat's 7) → the skills card (4). **Next:** b3 the v1.1.4 freeze note + C-003's narrow row + the `Activate:` H10 (Tue or Thu on Nick's line); `H8:` names the sitting; then b4 HERO-1d + 114c · b5 the brand/entity prep · b6 R-5B's packet · b7–b8 the close. ctx: beat 2 · calls ≈14 · read ≈24 KB · wrote ≈20 KB

**Leverage line:** the nine mints now live in the files every lane boots from — the next packet author meets the prior-ledger gate as a numbered law, not as a lesson.
**The alternative shape, rejected:** landing the return on Nick's relay alone — its counts were right; the audit is still the hub's re-execution, because the one time a relay is wrong is the time it lands in the ledger as fact.
**2029 test:** what the fold changed, the numbers re-run, why Check 9 read PASS before the landing, where the miscount went.

## 2026-09-13 (v74 beat 1 — THE v74 BOOT AND THE H8-a RE-CUT: the boot 41,959 B; HEADs = Nick's line, porcelain 0 ×5, push 0 ×5; the preflight 12/12 (Check 9 STALE by one file); W-SKILLS-9 pulled forward from b5 on Nick's word, PASTED and RETURNED inside the beat (`context/audits/2026-09-13_W-SKILLS-9_return.md`, 8,853 B; the intake at b2); the H8-a packet RE-CUT through THE PRIOR-LEDGER GATE and re-pinned to `93390f0`'s run #55, Sun 2026-09-13 ~19:2x CT (instrument 2026-09-14T00:20:13Z))

**The window's ONE deliverable:** H8-a's and R-5 Part B's packets through the gate and the sitting named (`H8:` / `R5:`; Sat 09-19 or Sun 09-20) — or run. **Intake at the bytes:** hivemind `98e2f39` = the b7 card (11 rows); no other `??`; `CI: 93390f0` banked (3/20). The dispatch text verbatim, the preflight and the gate's 18 greps: `context/audits/2026-09-13_v74-b1_boot-intake_and_H8a_prior-ledger-gate.md`. **The re-cut (22 edits; 30,119 → 34,997 B):** the pin — `install-smoke` runs on every `main` push (`install-smoke.yml:2–7`; the `paths:` filter is `pull_request`'s), so HERO-1c's push built run #55 (id 34788417946; both jobs green; the arm64 zip's published digest `78ee52fb…`; the .deb `…git20260913.230020.g93390f0_arm64.deb`). The gate's hits: R-4c D-3/ask #5 → THE ANONYMOUS ORIGIN GATE is §1 (the newest zip by mtime, identity before hash, hash = the published digest); R-4b D-13/R-4c D-11 → B6's grade read split from the halt (O-2's second reading its own block); R-4c D-9 → B2's rows 3 → 4; R-4c §7/§8 → the incumbent `a458a64`. Already fixed on 09-06: D-5, D-6, D-7. Added: `S31-LABEL: <A> <W>` at B0 (read only; P-1's `rating_w`); B0 reads the first `fleet:` line; §N reads R-4c; §H banks O-2 CLOSED and the label. The jobs/artifacts API answered 403 — the run page was the instrument (disclosed). **Next:** b2 W-SKILLS-9's intake and its two cards, then the freeze note + C-003's row + the `Activate:` H10; `H8:` names the sitting. ctx: beat 1 · calls ≈34 · read ≈98 KB · wrote ≈50 KB

**Leverage line:** a packet bound to a published digest never installs a stale download again — on any rig, in every packet after this one.
**The alternative shape, rejected:** cutting H8-a fresh from R-4c's packet — the 09-06 body had folded R-4b's thirteen deviations; the gate found four changes and one addition, not a new packet.
**2029 test:** which run built the .deb, what the gate caught and what it found fixed, why the skills lane moved.

