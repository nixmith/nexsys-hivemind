<!--
file: context/handoff/archive/pm-handoff-beats-v75b1-v75b4-rotated-2026-09-18.md
purpose: v75 beats 1–4 rotated VERBATIM out of pm-handoff.md at v76 beat 5 (the live cap is 12 blocks). bytes(kept) + bytes(archived body) = bytes(before), asserted inside the beat-5 splice.
status: ARCHIVED 2026-09-18 (v76 beat 5; instrument 2026-09-18T20:54:29Z)
-->

## 2026-09-15 (v75 beat 4 — THE VERDICTS AND THE PLAN: core `e56f555` (114c) and `3d40b5f` (FE-114) CI GREEN at the instrument (#249/#250; 6/20; six register rows RETIRED at the shas); the freeze doc v1.1.5, its false mirror line corrected; THE PREMISE GATE minted and applied — MEASURE-1 cut through it (one cite corrected), the plug dossier ACCEPT (the Gen4 units qualify; buy the meter only); HASH-1 a DEFECT over eight records (D-v75-5); the six-week PLAN the plan of record; the card handed, the navigator send scheduled, Mon 2026-09-14 ~21:3x CT (instrument 2026-09-15T02:31:35Z))

**At the bytes:** CI — `ci.yml` #249 `e56f555` success, #250 `3d40b5f` success. The freeze doc 29,642 → 34,475 B (`_scratch/v75/freeze_edit_b4.py`, four exact-once replacements), widened in the splice. MEASURE-1's premise table: every row grepped at `3d40b5f`; `AreaSelector` `Set.copyOf` `:45` → `:47`. The dossier: the Shelly matrix row and the z2m converter (`v25.80.0`) re-fetched TRUE. **Ruled (Nick):** `DUR: measure` (the instrument named first — it exists); `H8: Sat 09-19 10:00`; `Activate: hold` Tue; HASH-1 a defect, not a row; the dead-key table as the b3 audit §3; retire at the shas. **Found (F-b4-4):** `Map.copyOf` in `CommandAction:51` / `EmitEventAction:37` / `EventTrigger:43`, `Set.copyOf` in `WebhookTrigger:55` — a two-parameter command action is predicted per-JVM TODAY; HASH-0 probes it. **Dropped (the plan §7):** TR-1b at the card; the second plug; a FRAME-COUNT micro-WU. **Bridge:** a stray empty `nexsys-hivemind/_scratch/` (the hub's mis-path; one `rmdir` on the card); a zero-byte `index.lock` swept by rename. **Next act:** the b4 card (14 = 10 M + 4 A) → the MEASURE-1 paste → the shopping card. **Next WU:** b5 — the spine rotation FIRST (12 live = the cap), the `/apply` read + the Activate email, the knockout charter, MEASURE-1's intake → DUR-1 + HASH-1. ctx: b4 · calls ≈60 · read ≈75 KB · wrote ≈80 KB

**Leverage line:** a premise grepped before dispatch costs the hub a minute; one refuted by the lane costs the lane an hour and the charter its shape.
**The alternative shape, rejected:** HASH-1 as a canonical serializer — one file, every hash moves once; ordered views fix the salt where it lives and leave every single-element key alone.
**2029 test:** which six rows retired at which sha; why the freeze doc carries a dated CORRECTION; where the eight salted records are; the plan of record's path.

## 2026-09-14 (v75 beat 3 — THE INTAKES: EXPLAIN-114c and FE-114 RETURNED the evening they were pasted, both ACCEPT two-layer — the working-tree patches (10 + 25 files) applied clean to the hub's clone of `6bd8508`, every instrument re-run there, `npm run verify` re-run GREEN in the container (28 / 535; 70.7 KB; v1.1.4-2026-09-13); 114c's R1/O1/R2 banked as the hub's corrections, O4 → IR-23; FE-114's STOP-class proceed and D3 copy ACCEPTED, the charter's two cite errors owned; the coder-handoff entry; the three-block landing card handed, Mon 2026-09-14 ~19:1x CT (instrument 2026-09-15T00:12:08Z))

**At the bytes:** 114c — 10 = 8 M + 2 A; the desk gate green twice (automation 243, rest-api 157); K2 1 window page after (2 / 40 / 100 before); `readByTimeRange(` 1 · `readByType(` 1 · `"fired and confirmed at"` 0 in the applied tree; the condition-key pin is a `containsOnlyKeys` (the coder's honest correction); two assertion flips, named. FE-114 — 25 = 24 M + 1 A (three declared deviations); the lint 6 → 0; five pinned facts changed, each old → new; the fixture md5s unchanged (re-derived); P5 17 → 1. **Ruled:** the fixed-clock seed is a hint HIT (3 / 41 / 101 window pages) and the displaced id is the fallback — the hub's DP-1 was wrong, the return measured it; the marker precedes the run id's instant (the LEAD is the knob); the STOP-class narrowing is SPEC-backed (`SPEC.md:88`); "It ran at {time}, and the record says the device confirmed it." beats the charter's form. **Docketed:** IR-23 (`DefinitionHashes` hashes a set in per-JVM order); the freeze doc's false "mirror at v1.1.4" line; the `v114` mock touch and the dead-key retirements → FE-115. **The bridge:** a zero-byte `index.lock` from the hub's own timed-out `git diff`, swept by rename. **Next act:** two core commits, one push each, then the hivemind card (12 = 9 M + 3 A). **Next WU:** b4 — the CI verdicts, the v1.1.5 stamp, DUR-0 on `DUR: measure`, the plug dossier. ctx: beat 3 · calls ≈30 · read ≈60 KB · wrote ≈45 KB

**Leverage line:** a lane's gate re-run in the hub's own container is the cheapest layer-2 there is.
**The alternative shape, rejected:** one push for both lanes — two domains earn two CI verdicts and the counter counts samples, not batches.
**2029 test:** which corrections were the hub's and which the lanes'; why the fixed-clock seed is a hit; where the definition hash breaks.

## 2026-09-14 (v75 beat 2 — THE TWO DESK LANES: hivemind `cc17ce7` LANDED (9); IR-1 MEASURED on Nick's host (pages 2/40/100 = the by-construction counts); `FRAME: a` RULED with two sharpenings (the v75 DR §1 verbatim); 114c and FE-114 AUTHORED on two in-conversation grounding agents audited two-layer — one claim refuted at the source, K1/K4 re-classified from keys to engine facts (IR-22: a `for_duration` expiry initiates no run at `6bd8508`); R-5B's EXPECTED/§H sharpened, no command changed; IR-21/IR-22; D-v75-2..4 packaged, Mon 2026-09-14 ~15:0x CT (instrument 2026-09-14T20:09:22Z))

**Landed at diff-tree:** hivemind `cc17ce7` (9); core clean after the IR-1 run (the copy removed). **The two lanes:** `2026-09-14_coder-lane_EXPLAIN-114c_…` (K2 the RunId-time hint read with the counted fallback, the IR-1 test as the guard ≤ 2 pages; K3 `conditions[].definition` structured and hash-guarded — Core prose refused by the catalog rule; DP-B2 `FIRED_CONFIRMED` iff ≥ 1 CONFIRMED and no DISPATCHED/UNCONFIRMED/FAILED, the sentence naming both instants; IR-2; IR-3; 10 = 8 M + 2 A) and `2026-09-14_frontend-lane_FE-114_…` (the mirror to v1.1.4 with the tri-state validators; EXPLAIN-8/9; the `FIRED_CONFIRMED` arm "as the record says"; IR-12's widened lint and its six hits keyed; IR-13; IR-14; the two terminal rows; the five twins; the dead-key table) — both DISPATCH-READY, disjoint write-sets, pasted by the b2 packet. **The source findings (the audit §3):** K1 — every run is a root and the chain breaks at the device's `state_reported` root (D-v75-3); K4 — `expire()` publishes and validates, nothing initiates the run (D-v75-4, DUR-0 an hour after 114c). Nick's `:321` cite is the Hue's `clusters=3` (OnOff+Level+Color), not a plug's — the count discriminates for the S31 only; the LIST is IR-20's. **Next act:** the b2 packet (the card 11 = 7 M + 4 A; the two pastes). **Next WU:** b3 the plug dossier micro-lane → the shopping card; the intakes as they land. ctx: beat 2 · calls ≈45 · read ≈120 KB · wrote ≈95 KB

**Leverage line:** two agents grounded two lanes; the hub's re-executions caught the one claim that would have shipped a wrong reason.
**The alternative shape, rejected:** 114c as the docket read it (four keys) — two of the four are engine facts; authoring them as keys would have chartered the wrong module.
**2029 test:** why parentRunId is null, why a for_duration automation may never have fired, and where each decision went.

## 2026-09-14 (v75 beat 1 — THE BOOT (≈45.3 KB; the preflight 12/12; Check 9 PASS 28/28; the five HEADs = Nick's STATE line; the b8 card verified at diff-tree `9795c2c` = 6); the deliverable named (both sitting packets through the gate, the sitting Sat 09-19); IR-1's instrument AUTHORED by an in-conversation agent, the run BLOCKED at the container's egress → Nick's host (the b1 packet's block 2); the H8-a packet RE-PINNED to `6bd8508`'s install-smoke run #56; R-5B's packet CUT through THE PRIOR-LEDGER GATE (28 greps; R-4b's ledger re-grepped) with the S31 re-read and the frame block; IR-20; D-v75-1, Mon 2026-09-14 ~14:0x CT (instrument 2026-09-14T19:01:21Z))

**The deliverable at this beat:** both packets on the disk — H8-a re-pinned (run #56, id 34840877827, digest `11f3a79e…8a75`, version `0.1.0+git20260914.115803.g6bd8508`; the one-sitting clause) and R-5B cut (`context/instructions/2026-09-14_R-5B_…navigator-packet.md`, 39,081 B) — the sitting's hh:mm is the only open word. **Two source findings, filed (the audit §5):** the S31's interview count IS in the corpus (R-4b F-R4b-C: `reporting_configured … clusters=1` on 09-04 — the map's "no corpus entry" was a miss; the packet re-reads it, P-B6); the ingestion unit emits NO per-frame token at `6bd8508` (`ZclIngestionUnit.route` :436–470 → `onFrame`, no log; root INFO, no knob) — Nick's premise for the counting block does not hold on this artifact, so B3 counts the store's rows by subject over the window as a LOWER BOUND and the counter is D-v75-1 (`FRAME: a|b`; rec a; silence = a). **IR-1:** the test (`_scratch/v75/Ir1PageCountMeasurementTest.java`) seeds N = 1k/20k/50k under a `CountingEventStore`; pages = ceil(N/500) by construction; the run's lines land via the b1 packet's block 2, filed at b2 before 114c. **The next act:** the b1 packet — block 1 the hivemind card (9 = 6 M + 3 A), block 2 the IR-1 run. **Next WU:** b2 — IR-1's lines filed; 114c + FE-114 authored and pasted. ctx: beat 1 · calls ≈70 · read ≈150 KB · wrote ≈75 KB

**Leverage line:** a packet cut at the source finds the missing witness before the rig does.
**The alternative shape, rejected:** a FRAME-COUNT micro-WU before Saturday (the counter Nick's brief assumed) — the right instrument in the wrong week; offered as (b), the plan's order as (a).
**2029 test:** what the packets were pinned to and why; that the sitting's frame number is a bound, not a rate; where the S31's count already lived.

