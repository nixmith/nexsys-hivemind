<!--
file: context/handoff/archive/pm-handoff-beats-v75b5-v75b8-rotated-2026-09-19.md
purpose: v75 beats 5–8 rotated VERBATIM out of pm-handoff.md at v77 beat 1 (the live cap is 12 blocks). bytes(kept) + bytes(archived body) = bytes(before) + bytes(the archive-map row), asserted inside the beat-1 splice.
status: ARCHIVED 2026-09-19 (v77 beat 1; instrument 2026-09-19T13:09:00Z)
-->

## 2026-09-17 (v75 beat 8 — THE v75 CLOSE: the DUR-1 + HASH-1 verdict GREEN at the instrument (run #251's `Run check` step, Nick's screenshot; the desk's `check --continue` green twice) — IR-22/IR-23 RETIRED at `be4788a`, the freeze doc's HASH NOTE a fact, FE-115 unblocked; the b7 card re-cut (the cleanup needs the GitHub CLI); the v76 dispatch text written; the plan's §13; the deliverable MET and held; the close packet handed, Thu 2026-09-17 ~16:4x CT (instrument 2026-09-17T21:42:02Z))

**At the bytes:** the screenshot — `Run check` ✓ (BUILD SUCCESSFUL in 3m 14s; 156/156), `Upload test reports` ✗ ("Artifact storage quota has been hit"; 1,451 files would have uploaded); the desk — `./scripts/clean.sh && ./gradlew check --no-daemon --continue` → BUILD SUCCESSFUL ×2; `gh` absent on the host (block 1 re-cut: `winget install --id GitHub.cli -e`, a new terminal, `gh auth login`, then the list and the delete; run #56's artifacts kept). **The window measured:** 8 beats · 8 lanes accepted (114c, FE-114, MEASURE-1, DUR-1 + HASH-1; the dossier, DEVICE-SET, ZIGBEE-GAPS, FOP-1) · core `e56f555` · `3d40b5f` · `be4788a` green at the instrument (+ CI-2 on the card) · hivemind 7 commits + this card · the deliverable (both Saturday packets through the gate, the sitting named) MET at b1–b2 and untouched since · THE PREMISE GATE minted (b4), extended to outward text (b6) · the plan of record re-cut (§0–§9) with §10–§13 · the counter 6/20 · one scheduled navigator. **Hub errors owned:** DP-1 (114c); the `recognise` pin (FE-114); the ±0.2 % meter row and the 0x010A miss (b4); the b5 email (four faults); three cites the gate caught. **Next act:** the close packet — the cleanup (with `gh`) · CI-2's landing → `CORE: CI2` → `CI:` · the hivemind card (16 = 11 M + 5 A) · the email v3 · the v76 dispatch paste. **Next WU (v76 b1):** ENERGY-READ through the gate; Saturday untouched. ctx: b8 · calls ≈25 · read ≈30 KB · wrote ≈35 KB

**Leverage line:** the verdict lived in one step's colour; the screenshot was the instrument, and the register moved on it — not on the run's red badge.
**The alternative shape, rejected:** chartering ENERGY-READ in the close beat — an hour of source reads belongs to a fresh window, not the tail of a full one.
**2029 test:** where `be4788a`'s green is recorded; why the artifact cleanup waited on a CLI install; what v76 opens with; which hub errors this window owned.

## 2026-09-17 (v75 beat 7 — THE CI RED READ AT THE INSTRUMENT: run #251 on `be4788a` failed at both jobs' UPLOAD steps (the artifact-storage quota), no check annotation — the DUR-1 + HASH-1 verdict OPEN until `Run check` is read; CI-2 spliced (uploads never gate; retention cut; the .deb's 7 days kept); the cleanup as Nick's hands (run #56 KEPT); ZIGBEE-GAPS ACCEPT (29.7 %; color/buttons fenced → `GAPS:`); FOP-1 ACCEPT (13/34 FALSE; HotMobile Oct 9 → `HOTMOBILE:`); the email v3; core `be4788a` + hivemind `d02557d` LANDED; the card handed, Thu 2026-09-17 ~16:1x CT (instrument 2026-09-17T21:18:53Z))

**At the bytes:** run #251 (id 35169981477): both jobs FAILURE; annotations ×2 "Failed to CreateArtifact: Artifact storage quota has been hit … recalculated every 6-12 hours"; the step list login-walled; the desk ran the gate line, not `check`, so the verdict is OPEN. CI-2: 4 = 4 M (`ci.yml`; `install-smoke.yml` + its mirror; `architecture.md`); no code. ZIGBEE-GAPS: five cites re-read at HEAD TRUE (`ZigbeeProfileLoader:237` parses `InitializationWrite`, nothing executes it → IR-26). FOP-1: HotMobile's CFP re-fetched — "Friday October 9th, 2026, 11:59pm AoE" TRUE. **Nick's word (the DR §1):** the b6 email reads "so what" — security, policy, auditable tracing, room for what comes next; the shape "what I'm building → who I am → where the ambiguity lies" — v3 on the register's preamble, no "every", no comparative. **Words packaged:** D-v75-7 `GAPS: color|buttons|both|neither` (rec neither before the run); D-v75-8 `HOTMOBILE: submit|skip` (rec skip). **Next act:** the card — the `Run check` glance → `CI: be4788a check-<green|red>` · the cleanup (`gh api`; run #56 kept) · CI-2's landing (4) → `CORE: CI2 <sha>` · the hivemind card (13 = 10 M + 3 A) · the email v3 · `FOP: apply|hold`. **Next WU:** b8 — the verdicts; ENERGY-READ (IR-15, IR-24 widened, IR-25); FE-115; Saturday's intake. ctx: b7 · calls ≈35 · read ≈40 KB · wrote ≈40 KB

**Leverage line:** a red that names its cause in its annotation is read in one fetch; read from the chat alone it would have sent a lane through 250 green tests.
**The alternative shape, rejected:** dropping the image tarball from the artifact — `install.sh` installs from it on boxes without dpkg; retention and gating change no product surface.
**2029 test:** why #251 is red and what stays open; which artifacts were kept; the coverage number and its band; when HotMobile is due.

## 2026-09-16 (v75 beat 6 — TWO INTAKES, ACCEPT: DUR-1 + HASH-1 (red → green; T4 preserved; the loop re-tallied, one hash per definition; R1 ACCEPTED) and DEVICE-SET (`DEVICES: core` rec; P3 REFUTED at the manual; IR-24/IR-25); THE ACTIVATE PLAN RE-CUT on Nick's critique — four hub errors named, the corrected email, skip-by-default with the Oct 9 rule, the pitch row restored; ZIGBEE-GAPS and FOP-1 chartered; hivemind `13e8d64` LANDED (14); the five-block card handed, Wed 2026-09-16 ~20:1x CT (instrument 2026-09-17T01:10:31Z))

**At the bytes:** DUR-1's return 7,074 B; core 14 = 12 M + 2 A, nothing outside `core/automation`; the diff read over the mount (the expired bucket; the evaluator's arm with the `EventTrigger` carve-out; the eight ordered views); the tee'd file 64 lines, 8 pids, A `202ada4d…` ×16, B `ba46bcf1…` ×16. DEVICE-SET's return 13,995 B; the manual: "Active Power 0.5% 2%", "Voltage 0.2% 1%" — the b4 dossier's ±0.2 % was the voltage row; `EndpointClassifier:41` 0x0051 only, the Gen4 is 0x010A — two hub misses at b4. **Nick's critique (the DR §1):** the b5 email said the LLC "is forming" (formed 2025-07-17); two claims the register does not hold; two questions with no useful answer; the yes-rule dateless. Re-cut: skip default; a written yes to BOTH by Fri Oct 9, hours added on top; the FOP's stale rows → FOP-1; the pitch row → the strategy pass. **Rule extended:** outward text through the premise gate — every fact at its source, every product sentence from the register. **Next act:** the core landing (14) → the hivemind card (16 = 11 M + 5 A) → the email → the two research pastes. **Next WU:** b7 — `CI:` → IR-22/23 retired, the HASH NOTE a fact; ENERGY-READ chartered (IR-15, IR-24, IR-25); FE-115; the knockout charter; Saturday. ctx: b6 · calls ≈30 · read ≈45 KB · wrote ≈55 KB

**Leverage line:** the gate that catches a wrong `file:line` is the same gate that catches a wrong company fact — a sentence that leaves the building is grepped at its source or it does not leave.
**The alternative shape, rejected:** dropping Activate outright — a dated watch list costs one line a year; a long-shot application costs the run.
**2029 test:** why R1 stands; what the P4460 actually measures to; why the email says "an LLC formed in July 2025" and nothing about the fleet; when Oct 9 matters.

## 2026-09-15 (v75 beat 5 — MEASURE-1 RETURNED, ACCEPT two-layer: DUR-0 RED and HASH-0 salted on BOTH sources (32 lines re-tallied) — IR-22/IR-23 measured DEFECTS; DUR-1 + HASH-1 CHARTERED through THE PREMISE GATE (three cites corrected, a third expiry site added); the shopping card WITHDRAWN on Nick's word → DEVICE-SET chartered; Activate `/apply` read (closes Oct 30; `hold` stands), the email drafted; THE SPINE ROTATED (v74 → the archive); hivemind `0eef472` LANDED (14); the card handed, Tue 2026-09-15 ~18:0x CT (instrument 2026-09-15T23:09:46Z))

**At the bytes:** MEASURE-1's return 5,107 B; core 2 = 1 M + 1 A, nothing under `src/main`; the tee'd file 32 lines, 8 pids; A `67464952…` ×5 / `202ada4d…` ×3; B `ba46bcf1…` ×5 / `303c1b94…` ×3; the salts flip independently — sets AND maps must be ordered (D-v75-5 (a) over eight records holds). T1's seam: the expired envelope redelivered through `onEvent` in LIVE. **DUR-1:** the registry indexes a for_duration trigger also under `trigger_duration_expired`; the evaluator matches the expired payload to its trigger as an IMMEDIATE match; initiation stays LIVE-only. **HASH-1:** eight ordered views in the same `toString()` format; `DefinitionHashes` untouched; T4 pins single-element hashes at the baseline. **Nick's word (block 3):** no meter ordered; a scientific setup — multiple models, a reference, repeatable as devices and plugins grow → DEVICE-SET (`DEVICES: core|extended|<edits>`). **Activate:** close Oct 30 12 p.m. PT (the run's weekend); bachelor's + 4 post-bacc years; "hardware-based" — the email asks both. **Rotation:** v74 b1–b8 → `archive/pm-handoff-beats-v74b1-v74b8-rotated-2026-09-15.md`; 5 live. **Next act:** the b5 card (14 = 9 M + 5 A) → the DUR-1 + HASH-1 paste on `HASH1: a` → the DEVICE-SET paste → the Activate email. **Next WU:** b6 — the knockout charter; DUR-1 + HASH-1's intake → the landing card; ENERGY-READ on DEVICE-SET's return. ctx: b5 · calls ≈35 · read ≈40 KB · wrote ≈60 KB

**Leverage line:** a measured RED outranks a designed fix — both charters were cut on the lane's numbers, not the hub's reading alone.
**The alternative shape, rejected:** initiating the run inside `expire()` — it bypasses the bus and breaks replay determinism; the redelivered expiry is the event-sourced shape.
**2029 test:** what MEASURE-1 measured; why DUR-1 matches on redelivery; why the card was withdrawn; when Activate closes.
