<!--
file: context/handoff/archive/pm-handoff-beats-v77b1-v77b4-rotated-2026-09-23.md
purpose: v77 beats 1–4 rotated VERBATIM out of pm-handoff.md at v79 beat 1 (the live cap is 12 blocks). bytes(kept) + bytes(archived body) = bytes(before) + bytes(the archive-map row), asserted inside the beat-1 splice.
status: ARCHIVED 2026-09-23 (v79 beat 1; instrument 2026-09-23T05:55:41Z)
-->

## 2026-09-19 (v77 beat 4 — MEASURE-2b AND FE-115 INTAKEN TWO-LAYER, BOTH ACCEPT — THE READ PATH MEASURED AT RUN-SCALE AND ONE DEFECT THE RUN CANNOT CARRY: `2e39b27` verified; MEASURE-2b (13,995 B; core 8 M + 2 A at porcelain): the fixture exists, q1 `explainRun` O(N) 1.8 → 90.5 ms at 500k, every other read < 5 ms, restart 1.6 s, 503 B/row; **F-1: every restart re-mints `AutomationId` → NEVER_TRIGGERED of an automation that fired (IR-39) → AUTO-ID-1 (AMD-93 §2.3) ahead of LINK-READ (`JAVA-NEXT:` rec autoid)**; FE-115 (13,649 B; 22 = 18 M + 4 A): the real bodies pass the frozen validators (0 errors), the v1.1.5 pins, the definition sentence MIRRORED (the H8-b read next); IR-36..43; the box's setup order (the plan §20: nothing joins before Thu; Thu the plugs; the two sensors at rehearsal 1; `PRESENCE:` rec pair); two core cards; Sat 2026-09-19 ~20:5x CT (instrument 2026-09-20T01:50:14Z))

**At the bytes:** the three `MEASURE2B` lines = the XML's `<system-out>` (3); 503.4 B/row re-derived; `HomeSynapseCore.java:677` `new InMemoryAutomationIdentityStore(clock)`; the row-0 diff 27+/6−; zigbee 663/1 skip + app 30 re-summed (lifecycle's 82 overwritten by the later runs — disclosed); `git grep v1.1.5-2026-09-19` = 5 pins + the beat line; the fixtures carry the capture's sha256 (1 each); `git diff --stat` web-ui 18 M +1,021/−57. **Not re-executed:** `npm run verify` (the device VM's `/sessions` at 100 %); the EXPLAIN plan; T4's run. **Filed:** the b4 audit; the two returns staged; the register (8 rows); the record (the lane's M); coder-handoff's stamp; the plan §20; the DR (D-v77-5..7). **Next act (Nick):** the b4 packet — block 1 core card A (10) → `CORE: M2B <sha>` → CI; block 2 core card B (22) → `CORE: FE115 <sha>` → CI; block 3 the hivemind card (12 = 9 M + 3 A) → `HIVE: LANDED <sha>`. Words: `JAVA-NEXT:` · `PRESENCE:` · `HUE-MOTION:` + the standing four. ctx: b4 · calls ≈125 · read ≈260 KB · wrote ≈140 KB

**Leverage line:** the fixture paid on its first day — the restart defect is a one-line assertion now, and AUTO-ID-1's test is `restart()` then the id by slug.
**The alternative shape, rejected:** LINK-READ first as planned — a link-quality read is a feature; a false NEVER_TRIGGERED after rehearsal 1's kill −9 is the run's honesty invariant failing on the rig.
**2029 test:** why the automation identity became durable in September; why the sensors joined at a rehearsal and not with the plugs.

## 2026-09-19 (v77 beat 3 — KO-2 INTAKEN TWO-LAYER, ACCEPT — THE VERDICT KILL ADOPTED AS THE RECOMMENDATION (`NAME2:` silence = accept): `415873e` verified; the return 21,196 B, the canaries re-run (9 PASS; the ASR rig MISSED and named → IR-35); the hub re-fetched ★1 (RDAP: created 2015-07-13, Name.com, expiring 2027) and ★4 (TTAB 91235072: dismissed with prejudice 2018-06-19 on the opposer's withdrawal — the lane's "settled" corrected); S5 fails the program's own exact-`.com` law and S2-EU carries a senior class-9 software mark on the sound; PELTON-READY's fallback VOID → NAMING-B re-armed (dispatched only on a NOT CLEAR opinion); **MEASURE-2b NOT STARTED at the instrument** (core porcelain 0; no fixture; no worktree) — re-handed as block 0; FE-115 CHARTERED through the gate on the H8-a real-wire bodies (P4 = the live-wire bar; the seven condition `type`s grounded); the v77 DR opened (D-v77-1..4); Sat 2026-09-19 ~17:5x CT (instrument 2026-09-19T22:51:57Z))

**At the bytes:** `rdap.verisign.com` → `2015-07-13T22:52:09Z` / `2027-07-13`; TTABVUE → "BD DECISION: DISMISSED W/ PREJ" 06/19/2018; `git grep -n 'v1.1.4-2026-09-13' -- web-ui/dashboard` = 5 code sites + the MODULE_CONTEXT line; `ConditionDefinitionRenderer.java:56–:67` = 7 types; `GetRunCausalChainEndpoint.java:178–:195` `definitionMap`; `git worktree list` = main only. **Not re-executed:** the GBD/EUIPO pages; the tmsearch POSTs. **Filed:** the b3 audit; the FE-115 charter; the v77 DR; IR-35; the KO-2 return staged. **Next act (Nick):** the b3 packet — block 0 the MEASURE-2b paste (if not yet running); block 1 the hivemind card (8 = 4 M + 4 A) → `HIVE: LANDED <sha>`; block 2 the FE-115 paste → `FE115: RETURNED`. Words: `NAME2:` · `BENCH-CORE:` · `REARM:` · `SHAKE:` (silence = the recs). ctx: b3 · calls ≈100 · read ≈200 KB · wrote ≈95 KB

**Leverage line:** the second lane slot goes to FE-115 tonight instead of waiting for Sunday's intake — the real-wire bodies it needs are already in the tree.
**The alternative shape, rejected:** a third candidate's knockout tonight — the fallback matters only if PALOKI's opinion is NOT CLEAR, and the rig (IR-35) is the thing to fix before another name is scored.
**2029 test:** why the candidate died on a domain fact before counsel; why FE-115's first act is parsing the shipped artifact's real bytes.

## 2026-09-19 (v77 beat 2 — THE SITTING READ AT THE BYTES; MEASURE-2b AND KO-2 CHARTERED: H8-a (42,844 B) and R-5B (50,494 B) RETURNED, zero STOPs — K1–K4 re-asserted by the hub on the filed capture, the stop-proof clean twice, the restore MET, the first `fleet:` datum; P-B2/P-B3/P-B6 NOT TESTED (D-3 → `REARM:`); ONE lane claim REFUTED (the usb-reenumeration FAIL is the sshd `PATH`, not an absent binary — the scenario and driver byte-identical across the pull; the 08:32Z timer run passed it) → NO install, Sunday's line pre-registered; `PI: pulled fa01cad` BANKED from R-5B B6-1 — step 0 DONE a day early (the b8 relocation never grepped the packet; owned); step 0b FOUND (the bench card's Core, pre-CG-123, → `d1c2cbc` before the adoption; `BENCH-CORE:` rec sun); MEASURE-2b dispatched 14:2x CT (the Java slot); KO-2 through the gate (the candidate by slot — no docket in the tree; owned); IR-33/IR-34; the plan §19; the close waits on `R5B-2:`; Sat 2026-09-19 ~14:2x CT (instrument 2026-09-19T19:29:22Z))

**At the bytes:** `sha256` × 8 capture files = the records'; K1–K4 by the hub's script (rows 4 · refs 2 · `triggerRef` = the StateChangeTrigger's); `git diff 16e672d fa01cad -- scenarios/usb-reenumeration.yaml tools/runner/drivers.py` 0 lines; `drivers.py:77` the bare name; the service `ExecStart=/usr/bin/bash %h/nexsys-bench/tools/nightly.sh`; H8-a B0-1 `2026-09-19 … 8/9 PASS · 1 SKIP(hue-online)`; R-5B B6-1 `git pull --ff-only` → `fa01cad`, `selftest: 43/0`; `git ls-files | grep -i docket` → R10 only. **Not re-executed:** the journal counts; the Pi's banner; Sunday's line. **Filed:** the b2 audit; the two charters; the two records + captures staged (20 files). **Next act (Nick):** the b2 packet — block 1 the hivemind card (20 files: 8 M + 12 A; porcelain rows 14) → `HIVE: LANDED <sha>`; block 2 the KO-2 paste with its ONE slot → `KO: RETURNED`. MEASURE-2b is RUNNING → `M2B: RETURNED`. ctx: b2 · calls ≈70 · read ≈150 KB · wrote ≈60 KB

**Leverage line:** one refuted claim saved a rig change; step 0b found on Saturday costs 25 min Sunday, not Thursday.
**The alternative shape, rejected:** closing the two records tonight on their own §0s — Sunday's line is the instrument that adjudicates the pre-registration, and a close before it would bank a floor number the record disputes.
**2029 test:** why the Pi pull is dated Saturday; why the bench card's Core moved before the adoption; why the S31 carries a vacuum note.

## 2026-09-19 (v77 beat 1 — THE BOOT AT THE INSTRUMENT: the five HEADs = the dispatch line with its two placeholders filled by the instrument (hivemind `ad273c2` = the b8 order, 8 = 6 M + 2 A; bench `fa01cad` = the 1b table, 4 M; porcelain 0 ×5; ahead 0 ×5); the preflight 12/12 after one reconciliation (Check 9 28/28; Check 12 STALE on three landed charters → marked EXECUTED, bodies untouched); the boot read-set 44,961 B ≤ 45 KB (the plan §2 trimmed to its first 1,700 B); the paste = the filed v77 text at the bytes (the heading and the fence aside; the as-received copy under `_scratch/v77/`; the filed text marked PASTED); the spine rotated (v75 b5–b8 out; nine live); the window's ONE deliverable named — THE THURSDAY ORDER step 0 done on Sunday's two intakes CLOSED; Sat 2026-09-19 ~08:0x CT (instrument 2026-09-19T13:09:00Z))

**At the bytes:** `git show --name-status ad273c2` 2 A + 6 M; `fa01cad` 4 M; the 1b return's last line `RETURNED … 5780`; the splice lib md5 `d6780d13…`; the paste diff `1,2d0` + `17d14` only; `bootAndAdopt` at `BusSoakIT.java:144/:309`; `SCAN_BATCH = 500` at `StandardExplanationService.java:115`; `SOURCE_DEFAULT` at `RetentionPolicy.java:46`; one cite off — `ZigbeeAdoptionSlice.java:377–:378` is the classifier call, the `device=` line is re-derived at b2. **Not re-executed (disclosed):** CI #253's green (banked at v76 b6); the Pi's engine (`PI: pulled` is the instrument). **Filed:** `context/audits/2026-09-19_v77-b1_boot-and-intake_audit.md`; `archive/pm-handoff-beats-v75b5-v75b8-rotated-2026-09-19.md`. **Next act (Nick):** the b1 card (10 = 8 M + 2 A) → `HIVE: LANDED <sha>`; b2 = MEASURE-2b through the gate → the paste before 09:45 CT or after the sitting. ctx: b1 · calls ≈22 · read ≈80 KB · wrote ≈22 KB

**Leverage line:** both cards banked from porcelain (no words asked); the paste diffed identical (no second file); nine live blocks for the weekend.
**The alternative shape, rejected:** MEASURE-2b as the deliverable — Thursday hangs on the Pi and the intakes, not on a charter.
**2029 test:** why the deliverable is the Sunday close; why Check 12's three edits ride a boot beat.

