<!--
file: context/handoff/archive/pm-handoff-beats-v77b5-v77b7-rotated-2026-09-23.md
purpose: v77 beats 5–7 rotated VERBATIM out of pm-handoff.md at v79 beat 5 (the v79 close; the live cap is 12 blocks; 10 live after the b5 block). The multiset of pm-handoff lines is conserved (asserted inside the splice).
status: ARCHIVED 2026-09-23 (v79 beat 5; instrument 2026-09-23T21:51:35Z)
-->

## 2026-09-20 (v77 beat 7 — THE v77 CLOSE: `CORE: AID 13d439f` + `CI: 13d439f green` → the closure counter 11/20; `HIVE: LANDED d21e502`; BENCH-CORE-2 DONE on the outputs file (`c819a02` → `13d439f` at 15:52Z, BUILD 7 s, boot-health 6/6, `relinked=6 adopted=0`, rows 186,353→186,359) with ONE read skipped — `automations.ids.yaml` on the bench card UNVERIFIED → Monday's `IDS:`; `VACUUM: off · CONFIRMATION_TIMED_OUT` read twice on the 09-20 bundle (consistent with the vacuum on the S31, not proven by it); the S31 switched OFF and unloaded ~15:42Z — ruled harmless on `command-confirm-s31.yaml:1–:2, :61–:62` (the leg wants a real ON-edge); Monday pre-registered `8/9`; THE THURSDAY ORDER step 0b DONE TWICE; the knowledge base consolidated (the plan §21 STATE OF THE SYSTEM; the coder-handoff stamped; the DR CLOSED); THE SURVEY IS RUN, NOT ASSIGNED minted; the v78 text filed (`context/handoff/2026-09-20_v78_dispatch-text.md`); the window COMPACTED by the harness before this beat (the live context ≈45–50K tokens; the harness counter is a budget, not the context) — every claim here re-read from disk; v77 CLOSED at seven, Sun 2026-09-20 ~11:1x CT (instrument 2026-09-20T16:14:54Z))
- **The deliverable MET and exceeded:** the intakes CLOSED (b6); MEASURE-2b, FE-115 and AUTO-ID-1 GREEN at CI; the bench card on the newest core; the automation identity durable in code (its file on the card unread until Monday).
- **Layer 2 (the b7 audit §1):** re-executed — the HEADs, the outputs file's stamps / `deployed=` / `BUILD` / boot-health / rows / registry, the bundle's two reads, the scenario's precondition text, the archive count (148 → 149). Not re-executed — CI's XMLs on `13d439f`; the bench card's ids.yaml; the gate XMLs.
- **Hub errors owned (the audit §7):** a say-back that predicted (`ids.yaml present`) before a read; an "untouched" without its reason.
- **Order:** hivemind 10 = 8 M + 2 A by explicit paths (computed from porcelain inside the splice). Then Monday: `R5B-3:` + `IDS:` → the v78 paste into a FRESH conversation.

## 2026-09-20 (v77 beat 6 — H8-a AND R-5B CLOSED ON SUNDAY'S LINE; AUTO-ID-1 ACCEPT; BENCH-CORE-1 DONE — THE DELIVERABLE MET: `R5B-2:` read (`7/9 · FAIL command-confirm-s31 · fleet: 6/6 · re-seen 6`) — the pre-registration CONFIRMED (usb-reenumeration PASSED on the timer, nothing installed) and a new 7/9 on the S31 class, the first night with the vacuum on the S31 → the vacuum off today, Monday's line pre-registered `8/9`; FE-113 VERIFIED (K1–K4; doubled by FE-115's P4), O-2 CLOSED, OR-FAILCHAN re-scoped (§6-B + EXITCODE closed on the wire; the orderly close dead → IR-33); AUTO-ID-1 (12,987 B) ACCEPT — core 8 M + 6 A at porcelain, P4 = 0, P5 on lifecycle only, the arch rules untouched, the XMLs 267/93/30, `hero_id_stable=true`; the [REVIEW]s ruled (IR-44); BENCH-CORE-1 DONE on its outputs file (`e845cd9` → `c819a02`, BUILD 1m 59s, boot-health 6/6, rows 186,270→186,279, `deviceId` ULIDs on every row); the second deploy today (D-v77-8); Sun 2026-09-20 ~10:1x CT (instrument 2026-09-20T15:15:24Z))

**At the bytes:** the outputs file (5,467 B, five sections) vs the card's EXPECTED lines — every line met; the AUTO-ID-1 root diff; `git diff --stat HomeSynapseArchRules.java` = 0; the XML sums; `hero_id_stable=true` in the smoke XML. **Not re-executed:** the nightly bundle's verdict (Nick's one line, block 1); the timer run; the AUTO-ID-1 gate (CI is the gate). **Owned:** the card's `$S` echo; the `INV-GA-02` cite; the P1 table again. **Filed:** the b6 audit; H8-a §10 and R-5B §10 (the closes); the AUTO-ID-1 return staged; IR-44; the two OR status lines. **Next act (Nick):** the b6 packet — block 1 the vacuum off the S31 + tonight's bundle verdict (one line) → `VACUUM: off · <verdict>`; block 2 core card A (14) → `CORE: AID <sha>` → `CI:`; block 3 the hivemind card (9 = 7 M + 2 A) → `HIVE: LANDED <sha>`; block 4 after CI green: BENCH-CORE-1's blocks 1–3 on the new sha → `BENCH-CORE: deployed <sha> · 6/6 · rows … · ids.yaml present`. ctx: b6 · calls ≈175 · read ≈330 KB · wrote ≈200 KB

**Leverage line:** one pre-registration paid twice — no install on a wrong diagnosis, and the S31 class now has a named, testable cause for Monday.
**The alternative shape, rejected:** closing the records on their §0s Saturday — Sunday's line was the instrument, and it carried a finding.
**2029 test:** why the S31 scenario failed the night the vacuum arrived; why the bench card deployed twice in one Sunday.

## 2026-09-20 (v77 beat 5 — THE THREE LANDINGS BANKED; AUTO-ID-1 CHARTERED; BENCH-CORE-1 CUT: core `c819a02` on `a6b56d6` (MEASURE-2b + FE-115 LANDED; `CI: green` ×2 on Nick's line → the counter 10/20), hivemind `99d4187`, porcelain 0 ×3; AUTO-ID-1 through the gate (Doc 07 §4.1's companion file as `FileAutomationIdentityStore`, snakeyaml-engine, atomic write, 30-day retention, fail-closed; wired at `HomeSynapseCore.java:677`; T1 on `RealCoreFixture` reproduces F-1 red → green; `hero_id_stable` the second instrument); BENCH-CORE-1 cut through THE PRIOR-LEDGER GATE with the hub's own premise corrected — the bench card's Core is an `installDist` tree run by `~/bench.sh` (`bench.sh:6–:7`), not a .deb: pull to `c819a02` + `installDist` + `~/bench.sh restart` + `boot-health 6/6`, a `.backup` first; the deliverable's CI half MET, the intakes half on `R5B-2:`; Sun 2026-09-20 ~07:1x CT (instrument 2026-09-20T12:13:10Z))

**At the bytes:** `git log -1` ×3; `AutomationIdentityStore.java` (2 methods) · `InMemoryAutomationIdentityStore.java:44–:49` · `AutomationDefinitionLoader.java:133/:221` · `HomeSynapseCore.java:677–:678` · `config/configuration/module-info.java:23` · Doc 07 `:622` · AMD-93 §2.3 · `RealCoreFixture.java:126/:324/:347`; `bench.sh:6–:7` (`APP=…/build/install/homesynapse-app/bin/homesynapse-app`; `HOMESYNAPSE_HOME=$HOME/hs-bench`); `web-ui/dashboard/build.gradle.kts:34–:35` (installDist needs node + npm). **Not re-executed:** the CI runs; the Pi's clone (block 0). **Filed:** the b5 audit; the charter; the card. **Next act (Nick):** the b5 packet — block 1 the `R5B-2:` read (one ssh line) → paste the digest line; block 2 the AUTO-ID-1 coder paste → `AID: RETURNED`; block 3 the hivemind card (8 = 4 M + 4 A) → `HIVE: LANDED <sha>`; block 4 BENCH-CORE-1 (after block 1) → `BENCH-CORE: deployed c819a02 · boot-health 6/6 · rows …`. ctx: b5 · calls ≈150 · read ≈300 KB · wrote ≈175 KB

**Leverage line:** the same restart the fixture made a method call is now the WU's first test; the bench card's deploy is one pull instead of a .deb custody chain.
**The alternative shape, rejected:** a SQLite-backed identity store — cleaner to persist, but the Locked text names a companion file and a table would cost an AMD before the run.
**2029 test:** why the identity file appeared the weekend before rehearsal 1; why the bench card was never a .deb.

