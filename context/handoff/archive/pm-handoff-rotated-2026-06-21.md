<!--
file: context/handoff/archive/pm-handoff-rotated-2026-06-21.md
purpose: Rotated pm-handoff session records (the three 2026-06-19 sections: AMD-94 authored / AB-3 landed / M7-forward plan), moved out of the hot-path pm-handoff by the 2026-06-21 v3-hub context-hygiene rotation. Live pm-handoff keeps 2026-06-20 (Doc 16 Lock) + 2026-06-21 (v2 launch + M7.2a-1 closeout) + Open Risks. Full history in git.
rotated-by: PM mission-control v3 hub, 2026-06-21
-->

# pm-handoff — rotated session records (2026-06-19 arc)

## 2026-06-19 — AMD-94 AUTHORED PROPOSED (Doc 15 §6 currency amendment) (Cowork PM, fresh governance session)

**Outcome: AMD-94 is on disk PROPOSED + its DOCS-Project review prompt is dispatch-ready. NOT ratified, NOT folded into Locked Doc 15 — Nick's step after the review.** Step-0 preflight **PASS** (core `60d50ce` / docs `32afb3f` / hivemind `bcd7376` reconcile to the brief baseline; watermark AMD-93; Doc 15 LOCKED; M6+M7.1+AB-3 COMPLETE; zero CONFLICTED). Implements **A4** (RULED 2026-06-18) via the formal re-open→DOCS→ratify pipeline — Doc 15 is Locked, no silent edit.

- **AMD-94** (`homesynapse-core-docs/design/amendments/AMD-94_Doc15-sec6_Rotate-DEK-on-Restore_and_Envelope-Version-Discriminator.md`, PROPOSED) binds: **(1) rotate-DEK-on-restore** = install an additive new DEK version + retain priors (no re-encrypt → chain-safe per §5; no replace → no silent data loss; Track-3 F6); carry-high-water-mark → defense-in-depth **cross-check**; the refuse-to-encrypt **boot invariant** (R-α REC-235) — **closes OR-M6-NONCE restore-half on ratification** (R-α Problem 1). **(2) Reserve the 1-byte envelope version discriminator** (v1 = current envelope; distinct from `dek_ref`'s `key_version`; the version **POLICY is R-γ-pending**, slot existence is not). Mints candidate **AMD-94-INV-01/02**. §2.4 stages the §3.4/§4.1/§5/§6/§13.4/§16 edits (NOT folded). Optional **strikeable §8.1 rider** (the `ScopeKeyManager`-row currency nit). Scope-disciplined: no algorithm change, no module/JPMS/`projectionVersion` change; F3/F13 + the cause-discriminated read contract explicitly OUT.
- **Review track: FULL independent DOCS-Project review** (persisted shape + crypto contract + new invariants → not a shared block; constraint-enforcement §6). Prompt `context/handoff/2026-06-19_AMD-94_DOCS-Project_review_prompt.md` (dispatch-ready; adversarial mandate — verify each §2.4 staged-edit anchor exists verbatim in Locked Doc 15, the crypto soundness, scope discipline, INV minting; return RATIFY-AS-IS / WITH-EDITS / REJECT to `context/audits/2026-06-19_AMD-94_DOCS_Review_Return.md`).
- **Spine updated:** AB-4 backlog row (§6 amendment → PROPOSED); app-bootstrap charter entry-gate; snapshot masthead + Open flags (OR-M6-NONCE restore-half → PROPOSED-closes-on-AMD-94-ratify; §8.1 nit → AMD-94 rider). WUCP drift check run (snapshot/backlog/charter/pm-handoff agree on AMD-94-PROPOSED, watermark-still-93, Doc-15-unfolded).
- **Open Risks: unchanged** (OR-M6-NONCE restore-half now carries a PROPOSED closure vehicle = AMD-94; OR-APP-AUTH/C1, OR-RF-DECRYPT/C2, OR-M13-SDNOTIFY unchanged). **No open deferred build gates** — governance-only session, no code shipped.
- **Escalations to Nick:** (1) **Ratification co-sign** after the DOCS review (PM does not self-ratify). (2) **AMD-94-INV-01/02** minted — final identifiers + §17/§18 register + watermark **AMD-93→94 at ratification** (INV-GA-02), NOT now. (3) **R-γ** version-policy refinement is a separate, later input (slot already reserved; non-blocking).
- **Next:** (1) Nick commits the docs AMD-94 + the hivemind spine (messages handed over, bang-free `-F`). (2) Dispatch the AMD-94 DOCS-Project review (own fresh conversation). (3) Nick ratifies → fold §2.4 edits into Locked Doc 15, register the invariants, bump watermark AMD-93→94, flip OR-M6-NONCE restore-half **CLOSED**. (4) Check 9 stays benign-STALE from the prior AB-3 session until Nick's mirror sync (this session made **no** skill-source edits). _The AB-3 closeout record follows._

## 2026-06-19 — AB-3 LANDED GREEN + WUCP Phase 2 (Cowork PM, fresh session)

**Outcome: AB-3 (app-bootstrap lifecycle reconciliation) is gate-verified GREEN (`./gradlew check` 149 tasks, clean re-run) → PM WUCP Phase 2 = APPROVE. The system boots and runs for the first time (`main()` was a one-line stub).** This session authored + issued the AB-3 instruction (both escalations ruled by Nick: **PD-1** HSC implements the Doc-12-Locked `SystemLifecycleManager` [implement-don't-delete]; **PD-2** `InMemory*Registry` in `core:device-model`). The Coder built it, surfaced one blocking fork (HTTP-gate vs the `void start()` ripple → Nick ruled **Option-1 test-only HTTP path**), and caught two latent bugs in my instruction. I reviewed pre-gate (flagged SD-9 + HomeId), the first gate went GREEN, I ruled the two correctness items, the Coder fixed them, the re-gate is GREEN, and I verified both fixes + the flipped test in source.

- **What landed.** HSC implements `SystemLifecycleManager` (interface preserved); openHAB-startlevel phases (00–70) + the §3.10 health loop + watchdog + reporter selection (`SystemdHealthReporter`→`NoOpHealthReporter` fallback on JDK-21 per OR-M13-SDNOTIFY); the pre-migration snapshot hook (config-level, dormant — chain-migration is AB-4); 3 `InMemory*Registry` (device-model, MVP-substrate, **no Floor/Capability — the contained fold held**); a public `ConfigurationServiceFactory` + `AutomationEngineAssembly` seam (keeps `AutomationEngineSubscriber` package-private); the three M7.1 deferred items (config+registries assembled, schema registered in Phase 1, definitions loaded from the `automation` section, `automation_engine` subscribed **after** state-store catch-up); HTTP gated closed via a test-only `exposeHttpSurface()` (C1) + cipher inert; `LifecycleWiringTest` (7 methods) + `HealthLoopTest`. 14 new + 13 modified; green check 149 tasks (two-gate module graph, §4c clock, spotless, moduleGraphAssert all clean).
- **WUCP Phase 2 — two correctness items the green gate could not catch (both fixed + re-verified in source):** **(1) SD-9 FATAL-boot → surface-and-continue.** The Coder flagged it for ruling; I ruled per the loader's ratified contract — a malformed def is rejected + published as `config_error`, valid siblings still load (Doc 12 §4 FATAL governs subsystem-init, not a per-entry config error). HSC now iterates `LoadResult.failures()`, publishes `config_error` (best-effort, AMD-70-INV-01) + logs each, boots with `loaded()`; `malformedDefinitionSurfacedValidSiblingsLoad` asserts boot=RUNNING + only the valid sibling loaded + `config_error` in the store. **(2) Durable HomeId.** `home_id` is stamped on every event (AMD-34); `Main` now read-or-creates `configDir/home_id` (the `.root-key` pattern).
- **The Coder's two latent-bug catches (correct — instruction defects, mine):** the automation core-schema must register in Phase 1 **before** `config.load()` validates; and `loader.load(rawMap())` silently dropped every automation (the loader reads top-level `automations`; config nests under the `automation` section) → `loader.load(rawMap().get("automation"))`. The same nesting bug sits in the `AutomationSchema` + loader Javadocs (doc-currency).
- **5 instruction defects logged as an erratum** on the AB-3 instruction masthead (the FIX-07 precedent): the automation `event.bus` transitive bump; the missing lifecycle→`platform.systemd` edge; the schema-ordering; the loader-nesting; the OR-M13-aware reporter selection. None changed the milestone outcome — the Coder's verification caught them pre/at-gate. **pm-lesson logged** (the consumer/pin survey must include public-method-signature changes as a fan-out category; pin fail-closed SCOPE — per-record vs per-subsystem-init — before issue).
- **Residual / carries (none blocking):** (a) 2 Javadoc errata + 1 MODULE_CONTEXT nit (stale `AutomationConfigBridge` reference) → doc-currency, fold into AB-1's automation touch or a small doc commit; (b) **OR-M13-SDNOTIFY** — the health loop is structurally wired but Tier-1 `sd_notify` stays deferred to M13 (NoOp fallback); (c) [INFO] the `home_id` write isn't crash-atomic (first-boot mid-write could strand a partial id — low risk; harden in the backup/restore WU or AB-4).
- **Open Risks: unchanged** (OR-APP-AUTH/C1, OR-RF-DECRYPT/C2 — both AB-1/AB-2; OR-M6-NONCE R-α; OR-M13-SDNOTIFY). **Deferred build gate: RESOLVED GREEN** (AB-3 diff, 149 tasks). No open deferred gates.
- **Next-WU = AB-1 + AB-2 + AB-4 (the Seam-1 go-live):** auth (`AuthMiddleware`/`RateLimiter`) wires into `exposeHttpSurface()` + the loopback bind posture; the read-contract fail-closed half + `DegradedEvent` seam; cipher activation via the 6-arg HSC ctor (`Main.payloadCipher`). The AB-3 phase gates are the integration points. Author when Nick routes the slot; Doc 16 review→Lock + AMD-94 run in parallel (gate M7.2b, not AB-1/2/4).
- **Next:** (1) Nick commits homesynapse-core (AB-3; message handed over, bang-free `-F`) → SHA reconciled at next Step-0. (2) Nick commits the hivemind closeout (message handed over). (3) Nick re-runs the mirror sync (Check 9 — the Coder touched coder-lessons). (4) Route AB-1+2+4 to a Coder slot.

## 2026-06-19 — M7-FORWARD PLAN + M7.1 SECOND-CHECK (Cowork PM, fresh session)

**Outcome: M7.1 verified sound; the M7-forward plan is recorded; M7.2 is NOT dispatched (entry-gate OPEN); the sequencing call + D2/REC-162 are escalated to Nick.** Step-0 preflight **PASS** — HEADs core `beb4bc3` / docs `32afb3f` (Doc 16 committed-as-DRAFT, **NOT Locked**) / hivemind `ec9905e`; the working-tree "modifications" in all three repos were the **truncated-tail mount artifact** (host file tools authoritative; VM git quarantined per env-model §2/§5), so the trees are clean on the host; Check 9 benign-STALE; zero CONFLICTED.

- **Part A — M7.1 second-check = PASS-WITH-FINDINGS.** (1) Closeout consistent: backlog M7.1=DONE (`beb4bc3`), snapshot, pm-handoff, MODULE_CONTEXT ×2, coder-lessons all agree; **watermark AMD-93 confirmed** (no new AMD landed; AMD-94 is reserved-not-on-disk for app-bootstrap's Doc 15 §6 amendment). (2) The 3 deferrals are homed: composition-root wiring→**AB-3** (decision-record addendum + AB-3 row); the FIX-07 `core→config` ban→coder-lessons 2026-06-19 (the superseding `assertAllowedModuleDependencies` lesson); the schema fragment→config-free `AutomationSchema` (source-confirmed: `AutomationConfigBridge` deleted, no `requires config` in core:automation module-info). (3) The 6 [REVIEW] adjudications hold — the 3 behavioral defaults (multi-entity condition all-of/empty→false; `EventTrigger` event-type-only matching; device-subject `entityRef` flattening) are **tracked M7.2a carry-items, none silently constrains M7.2.** (4) Source round-trip clean: `StandardTriggerEvaluator.evaluate`→`List<AutomationId>` (the matched-IDs decision); the AMD-92 slice rows 1/3/11–16/19 landed; **+7 EventTypes / 24→32 classes / 36→44 category rows are source-accurate** (the +7-vs-+8 = the documented row-1-reshape/row-19-existing-constant gap; an interim "+8 constants" flag was a miscount, corrected against `git diff`); M7.2 owes rows 2/4–10/17/18; the M7.2 pieces are interface-only/absent (`RunCausalChain` absent; `RunContext` still `int cascadeDepth`); no fabrication.
- **Findings (cosmetic, reconciled in-session).** **F1** — the coder-handoff masthead still framed the M7.1 gate as "RE-DEFERRED / Open Risk until GREEN / NOT gate-verified" (the Coder's hand-back posture, committed in `0f3951f`, never flipped to the landed GREEN); reconciled with a single-anchor RESOLVED note on its DEFERRED section citing `beb4bc3`. **F2** — the PROJECT_SNAPSHOT Recent-Session-Log lagged the masthead (top row 2026-06-14); 3 rows appended (this session + the two 2026-06-18 sessions). Neither is a conflict or a material gap; the gate IS green and the primary records were already consistent.
- **Observation O1.** Doc 16 §4 labels `RunCausalChain` "Existing (AMD-91)" and reads it in the explainability surface; in source it is M7.2-pending (the AMD-91 swap). Consistent for a Phase-1 design doc; the plan owns that M7.2 builds it and the Doc-16 explainability/audit surfaces sequence after M7.2.
- **Part B — M7-forward plan recorded** (`context/planning/2026-06-19_M7-forward_plan.md`), baking in the three interlocks (I1 Doc 16 gates the M7.2 action-model freeze; I2 wiring deferred to app-bootstrap; I3 D2/REC-162 live). **M7.2 SPLIT per P1 (≈5 sub-pieces):** **M7.2a** = run lifecycle (`RunManager` FSM) + the AMD-91 `RunContext`→`RunCausalChain` swap + `ActionExecutor` exec mechanics + `CommandDispatchService` + the AMD-92 run-lifecycle/dispatch event slice (rows 2/4–10/17/18) + run-trace — **baseline-buildable-now, Doc-16-independent**, builds against injected deps (I2) with `initiateRun` carrying the Doc-16 computed-param-resolution as a DP-seam; wiring→AB-3. **M7.2b** = the action-model freeze (computed-param resolution semantics + the run-coupled-reliability terminal contract + the D2 disposition) — **Lock-gated on Doc 16 + D2/REC-162**. **M7.3** (`PendingCommandLedger`) UNAFFECTED (Doc 16 §7.2), gated on M7.2a. **Doc 16 is review-ready** (16 substantive sections, §7.2 M7-impact present + consistent, 5 anti-requirements held, OQs all NON-BLOCKING) → dispatch the DOCS second-opinion review as its own parallel session → Lock = the M7.2b prerequisite. **Sequencing recommendation (Nick rules): app-bootstrap AB-3 takes the next Coder slot** — existential runnability (`main()` is a stub), AB-3 is the wiring substrate both M7.1/M7.2 defer into, and app-bootstrap's decision-gates are all RULED; Doc 16 review→Lock runs in parallel; then M7.2a → M7.2b → M7.3.
- **Escalations to Nick:** (1) the **sequencing call** (rec: app-bootstrap AB-3 next); (2) **D2/REC-162** (default = keep REC-162 anti-retry, remediation→M8.2; rule at/before M7.2b scoping; never silent); (3) **contract-reshape watch** (none triggered; any Doc-16/M7.2b contract change → formal AMD with re-sequencing); (4) carry: **AX-7 versioning policy** (before user-authored components) + the **AB-3 lifecycle API-shape** call; (5) **flag:** AB-3 may pull in device-model breadth (Entity/Device/Area registry impls may not exist — AMD-44 Floor/EntityRole, Research-8 REC-23–30).
- **Rulings (Nick, 2026-06-19) — both escalations decided.** **R1 sequencing = app-bootstrap AB-3 next Coder slot** (runnability is the existential gap; AB-3 unblocks M7.1's deferred wiring + gives M7.2 a runtime). **AB-3 scope must resolve the device-model registry dependency up front** (fold minimal Entity/Device/Area production registries, or sequence that device-model slice just ahead — AMD-44/Research-8; never a silent block); **M7.2a = explicit fallback** only if AB-3 prep stalls. **R2 D2/REC-162 = keep REC-162 anti-retry as default; decide the action-model disposition AT M7.2b** (post-Doc-16-Lock; remediation→M8.2). Cheap M7.2-scoping fact-check (not a flip): confirm the verification half ("did the device reach the commanded state?") is already satisfied by `Expectation`+AMD-90 (R-δ judged it likely is). Both keep the irreversible call at maximum context.
- **Open Risks: unchanged** — OR-APP-AUTH (C1), OR-RF-DECRYPT (C2), OR-M6-NONCE restore-half (R-α), OR-M13-SDNOTIFY; **no open deferred build gates** (M7.1's gate RESOLVED GREEN at `beb4bc3`).
- **Next:** (1) Nick commits the hivemind reconcile (message handed over, bang-free `-F`). (2) Nick rules the sequencing + D2/REC-162. (3) Dispatch the Doc 16 DOCS second-opinion review (own fresh session). (4) Per the sequencing ruling, author the next coding instruction (recommended: app-bootstrap AB-3). (5) Nick re-runs the external mirror sync (clears Check 9).

