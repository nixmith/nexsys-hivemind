<!--
file: context/handoff/coder-handoff.md
purpose: Coder session continuity — current task, deferred build gate, next WU, recent closeouts.
audience: Coder, PM
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-19 (Coder, W2-LEARN closeout — the W2-LEARN DELIVERED entry prepended [newest, authoritative for current task + next WU]; core repo, working tree on `9d40ce8`, all in-session gates GREEN incl. full `./gradlew check` [this WU's CC-lane grant] + in-session mutation verification; hub two-layer audit precedes any commit; the D1 silicon leg rides the landing + Pi deploy). Prior: 2026-07-18 (Coder, M9.7-W2 closeout — the M9.7-W2 DELIVERED entry prepended; core repo, working tree on `1aa809d`, all in-session gates GREEN incl. full `./gradlew check` [that WU's CC-lane grant]; hub two-layer audit preceded the commit; landed AS `9d40ce8`). Prior: 2026-07-14 (Coder, B1-REV2 closeout — the B1-REV2 DELIVERED entry prepended; desk half complete: 15 porcelain entries in the bench repo [5 M + 10 new], desk gates GREEN [15-run demo sweep exact exit codes · byte-frozen regression on every other scenario's demos · mutation-verified fixture teeth · 4-lens fleet, 21 findings adjudicated, both DEFECTs fixed + demos re-run green]; hub two-layer audit precedes any commit; Nick's ONE operator rep rides the landing). Prior: 2026-07-12 (Coder, B1-REV1 closeout — the B1-REV1 DELIVERED entry prepended [newest, authoritative for current task + next WU]; desk half complete: 20 files in the bench repo [2 M + 18 new], desk gates GREEN [shellcheck clean · 5 mandated demos + 3 supplementary · 4-lens self-review fleet, 30 findings adjudicated + fixed]; hub two-layer audit precedes any commit; the Pi half rides Nick's post-close-out word). Prior: 2026-07-12 (Coder, B1 STOP-AND-ESCALATE — the B1 ⛔ STOPPED entry [G-B1-2 FAILED at source; the ruling landed: "(c) + (a)", v30 beat 3; SERVED]). Prior: 2026-07-11 (Coder, M9.6-AVAIL closeout — the M9.6-AVAIL entry prepended [newest, authoritative for current task + next WU]; targeted gates GREEN 466 forced-fresh + lifecycle 56 defensive, full check deferred to Nick). Prior: 2026-07-10 (Coder, M9.6-RO closeout — targeted gates GREEN 451 forced-fresh, full check deferred to Nick). Prior: 2026-07-10 (v27 hub, beat 2 — ROTATION: entries older than M9.5-DUR moved to `archive/coder-handoff-rotated-2026-07-10.md` [M9.4-KEYb … AB-1/AB-2, the M7/M9.4 arcs — all WUCP-closed + CI-green + silicon-certified]; kept: M9.5-DURb [newest, incl. the NEXT-WU pointer — its acceptance-run leg is DONE, the soak runs] + M9.5-DUR. The newest entry below remains authoritative for current task + next WU.) Prior: 2026-07-01 (v13 hub — frontmatter currency restamp only; the body's newest entry is the live truth and had run far ahead of this line, which had frozen at 2026-05-27/M3.7). The newest entry below is authoritative for current task + next WU; the spine (`context/status/PROJECT_SNAPSHOT.md` newest beat) is authoritative for fleet state. Prior stamp: 2026-05-27 against M3.7 closeout working tree — `./gradlew check` GREEN (139 tasks).
-->

# Coder Session Handoff

---

## W2-LEARN — ZoneEnrollRequest zoneType Learn (F-7a completion) + the S31-ROUTE Closure (core repo) — DELIVERED (2026-07-19)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (Success Criterion 5). The build gates that RAN in-session, ALL GREEN against the working tree at core `9d40ce8` (clean at dispatch; this WU's Build Discipline granted the CC lane the FULL check):
> ```
> ./gradlew :integration:integration-zigbee:test --rerun-tasks   # BASELINE at HEAD pre-edit: 518/0/0 (G-WL-5 re-derived —
>                                                                #   matches the P14 at-commit record exactly, no drift)
>                                                                # stage-A red: 526 tests, EXACTLY 7 failed for the right
>                                                                #   reasons (empty learn ×3 · "detected"≠"open" · absent
>                                                                #   unknown-DEBUG · absent diagnostic · absent learn-INFO in
>                                                                #   the e2e twin); the 8th new leg (short-payload) GREEN at
>                                                                #   HEAD exactly as the instruction predicted
>                                                                # stage-B green: 526/0/0 forced fresh (518+8); re-run
>                                                                #   post-mutant-restore: GREEN
> mutation verification (in-session)                             # guard-deletion kills EXACTLY the short-payload leg (1/32);
>                                                                #   invalidate-deletion kills BOTH rebuild legs (2/32 —
>                                                                #   attribute AND enroll feeders through the shared core);
>                                                                #   payload-parse-deletion proven by construction (the
>                                                                #   stage-A red IS its kill record); restores cmp-verified
>                                                                #   byte-identical
> ./gradlew :app:homesynapse-app:test :lifecycle:lifecycle:test --rerun-tasks
>                                                                # RAN (defensive, forced fresh): app 19/0/0 · lifecycle 56/0/0
> ./gradlew check                                                # RAN, GREEN (156 tasks)
> ./gradlew :integration:integration-zigbee:spotlessApply        # RAN — clean (ZclIngestionUnit cmp-unchanged post-apply)
> CI on the pushed commit remains the gate of record (push on the hub's order, via Nick)
> ```
> Change: **exactly 4 porcelain entries, all integration-zigbee** — 1 main M (`ZclIngestionUnit`: the enroll-arm guarded LE-uint16 zoneType parse + learn-BEFORE-respond [DP-WL-1/DP-WL-3] · the shared private `learnZoneType(IEEEAddress, long)` core with the attribute path delegating after its existing `Long`-extraction, semantics byte-preserved [DP-WL-2] · the route() non-HA DEBUG `zigbee.ingestion_profile_skipped: nwk=0x{} profile=0x{} cluster=0x{}; non-HA frame skipped` [DP-WL-4]) + 2 test M (`ZclIngestionUnitTest` +7 legs + the module's logback-capture idiom imported; `ZigbeeWave2ContactAdoptionTest` +1 — the enroll-driven end-to-end twin, both existing methods byte-untouched, the existing `containsExactly` learn pin holds) + MODULE_CONTEXT (the W2-LEARN section + the S31 bench watch REWRITTEN as the DP-WL-5 CLOSURE + the DP-WL-6 note). **ZERO:** event mints (73/43/55 hold) · module-info · build files · config schema · public surface (grep: 0 `public` tokens in the main diff) · frozen-token diffs (`ias_zone_type_learned` / `ias_zone_enrolled` / `ias_zone_type_unknown` byte-untouched — the extraction left them as diff-CONTEXT, not rewrites) · locking (LTD-11: none added — the enroll arm shares the attribute arm's cycle thread, verified at source) · `effectiveZoneType` / `learnedZoneType` accessor / classifier / adapter `zoneTypeFor` stub / `ReportingConfigurator` / dedup-scope edits.

**Coder (Claude Code).** Built per `context/instructions/2026-07-19_W2-LEARN_enroll-zonetype-learn_coding-instruction.md` + pre-verification `WU-W2-LEARN.md` (P1–P14 ALL re-verified at source at `9d40ce8` clean; STOP gates G-WL-1..G-WL-5 ALL PASS pre-code — G-WL-5 re-derived forced-fresh, 518/0/0). Duplicate-dispatch check FIRST: newest handoff entry was M9.7-W2, now committed AS `9d40ce8` — W2-LEARN is a fresh dispatch. Red-first staged honest (the M9.6-RO idiom). §1 realized exactly (the joins-night gap is now a regression tooth: `enrollRequestLearnsZoneType` red at HEAD, green with the parse); §2 realized as RECORD-ONLY (the DP-WL-5 closure text landed in MODULE_CONTEXT; zero routing code); §3's diagnostic verified to fire on NO pre-existing test path (swept: no test constructs non-HA non-ZDO frames — only the new leg drives it). P13 verified at read (`HA_PROFILE_ID=0x0104` / `ZDO_PROFILE_ID=0x0000` at `EzspCoordinatorProtocol:117-:119`). The e2e twin exercises the PRODUCTION enroll-response send path (`protocol::sendZclFrame` → FakeNcp SEND_UNICAST ack) for the first time — `ias_zone_enrolled` observed through it.

### Deviations (severity-honest — ZERO [REVIEW]/[BLOCKING], five [INFO])
- **[INFO]** The DP-WL-3 named mutant ("reorder respond-before-learn with a rejecting sender ⇒ leg 3 fails") is UNREALIZABLE as literally stated — `respondZoneEnroll` is `void` and non-throwing on rejection, so a plain reorder keeps the learn unconditional and leg 3 stays green; the leg DOES kill the real threat class (any shape gating the learn on the send result). Lesson banked (coder-lessons 2026-07-19).
- **[INFO]** `ZclIngestionUnitTest` gained the logback `ListAppender` + `setLevel(DEBUG)` try/finally harness (the Wave2/KeyEstablishment idiom — appender attach in `setUp`, new `@AfterEach` detach, 2 private helpers): needed by 4 of the 7 new legs; test-file-only, inside the Files table's MODIFY row.
- **[INFO]** Documentation-only javadoc touches: the `ZclIngestionUnit` class doc's F-7a sentence gains the enroll-payload clause; the learn javadoc split across the two overloads (attribute path vs shared core); one W2-LEARN paragraph added to the Wave2 test's CLASS doc (the two existing test METHODS byte-untouched — P10 holds).
- **[INFO]** The instruction/pre-verification cite the enroll arm at ":399–:405"; at source it spans :400–:405 (cite off-by-one; substance exact — non-STOP).
- **[INFO]** Instruction + pre-verification dated 2026-07-19 vs the session's context date 2026-07-18; the substantive dispatch gates (HEAD `9d40ce8` exact + clean tree) both PASS — noted for the record only.

### NEXT WU (refuse-to-close pointer)
- **The hub's two-layer audit FIRST — no commit until its order** (this entry + the completion report are the audit inputs).
- **Then the D1 closer's silicon leg (Success Criterion 6, UNIT-GREEN ≠ WU-DONE):** deploy re-verified AT the Pi (version banner — the v32 deploy-state convention) → ONE permit window + the 04P long-press re-pair → `zigbee.ias_zone_type_learned … zoneType=CONTACT` on the wire → accept-list → adopt as BINARY_SENSOR/contact → **D1 [M] 5/5**. The hub authors the operator mini-package at the deploy word; BENCH-CONST rides after adoption.
- Carried candidates unchanged from the M9.7-W2 entry (sidecar snapshot-init WU · real-store `setAll` teeth · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · LC-LABEL-LOG); **the S31 route watch is CLOSED** (DP-WL-5 — recorded in MODULE_CONTEXT; the §3 DEBUG line is the standing instrument).

---

## M9.7-W2 — Wave-2 Profiles + Classification/Ingestion Completion (core repo) — DELIVERED (2026-07-18)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (Success Criterion 5). The build gates that RAN in-session, ALL GREEN against the working tree at core `1aa809d` (clean at dispatch; this WU's Build Discipline granted the CC lane the FULL check):
> ```
> ./gradlew :integration:integration-zigbee:test --rerun-tasks   # BASELINE at HEAD pre-edit: 466/0/0 (G-W2-6 GREEN;
>                                                                #   the instruction's "452" parenthetical was stale by
>                                                                #   exactly the M9.6-AVAIL +14 — adjudicated non-STOP)
>                                                                # stage-A red: 513 tests, EXACTLY 30 failed for the right
>                                                                #   reasons (census 2≠6 and 6≠8, absent handler behavior,
>                                                                #   motion≠contact through every layer incl. the ladder)
>                                                                # stage-B green: 513/0/0; post-fleet-fix: 518/0/0 forced fresh
> ./gradlew :app:homesynapse-app:test :lifecycle:lifecycle:test --rerun-tasks
>                                                                # RAN (defensive, forced fresh vs the NEW tree): app 19/0/0
>                                                                #   (RegistryApplyCensusTest + ArchUnit green), lifecycle 56/0/0
> ./gradlew check                                                # RAN, GREEN (×2: post-stage-B and post-fleet-fix)
> ./gradlew :integration:integration-zigbee:spotlessApply        # RAN — clean
> CI on the pushed commit remains the gate of record (push on the hub's order, via Nick)
> ```
> Change: **exactly 18 porcelain entries, all integration-zigbee** — 7 main (5 M: `zigbee-profiles.json` [2→6 profiles + DP-10 provenance], `ClusterHandlers` [6→8], `EndpointClassifier` [§3 arms + 2-arg overload], `ZclIngestionUnit` [+`learnedZoneType` accessor], `ZigbeeAdoptionSlice` [8-arg ctor + 7-arg overload + the §4 consult], `ZigbeeIntegrationAdapter` [the deferred-read wiring]; 2 NEW package-private: `TemperatureMeasurementHandler`, `RelativeHumidityHandler`) + 10 test (6 M + 4 NEW: `Wave2BundledProfilesTest`, `TemperatureMeasurementHandlerTest`, `RelativeHumidityHandlerTest`, `ZigbeeWave2ContactAdoptionTest`) + MODULE_CONTEXT (WUCP). **ZERO:** event mints (73/43/55 hold) · module-info · build files · config schema · public surface (both new types package-private; grep-verified) · frozen-token diffs (zero existing log lines touched) · `ReportingConfigurator`/`Fingerprint.matches()`/adapter-`zoneTypeFor`-stub edits. New tokens: NONE (two F-9 DEBUG diagnostics in the new handlers — the PowerConfigurationHandler precedent shape; flagged [REVIEW] for the LTD-15 strict-reading call).

**Coder (Claude Code).** Built per `context/instructions/2026-07-18_M9.7-W2_wave2-profiles-and-classification_coding-instruction.md` + the pre-verification (P1–P14 re-verified at source; gates G-W2-1..6 all PASS pre-code). Pre-write verification fan-out (5 lenses) + in-session 4-lens adversarial review fleet (the SEED/RO layer-1 precedent): **the fleet's findings all closed in-session** — the 0x0107-arm zoneType pass-through and the 1-arg-overload byte-equivalence both gained mutation-killer tests (the RO-R1 unfalsifiable-branch class, closed pre-commit), DP-2 hardened with `doesNotContain(0xFC37)`, and the temperature handler gained the F-9 band guard `[−27315..32767]` closing the mis-typed-wire hole (a uint16-decoded 0x8000 = 32768 must never publish 327.68 °C). **P9 REFUTED at execution:** `ZigbeeProfileLoaderTest:260` pins the bundled id-set exactly (`containsExactlyInAnyOrder` ×2) — adapted in lockstep per the Files table's P2-sweep clause. Key source facts consumed: the codec SIGN-EXTENDS int16 (the temp sentinel is −32768 post-decode, never 0x8000); `initialize()` constructs the slice BEFORE the ingestion unit (⇒ the deferred-read R4 shape); `adopt()` runs on the ingestion cycle thread (⇒ the lock-free learned-map read is confinement-safe).

### Deviations (severity-honest — TWO [REVIEW], six [INFO]; full detail in the completion report)
- **[REVIEW] R1 — two F-9 DEBUG out-of-band diagnostics** (humidity >10000; temperature outside [−27315..32767]) with band-drop behavior test-pinned but line FORMATS not frozen/pinned: free-prose DEBUG mirroring `PowerConfigurationHandler`'s shipped F-9 line byte-for-shape, arguably outside the `zigbee.*:` token class — flagged for the LTD-15 strict-vs-precedent reading.
- **[REVIEW] R2 — the temperature band guard itself is spec-plus** (the instruction's error table named only the 0x8000 sentinel): DP-9's match-the-siblings idiom + the fleet-demonstrated mis-typed-wire fabrication scenario argue for it; independently revertible (one `if` + 2 constants + 3 tests).
- **[INFO]** `ZigbeeWave2ContactAdoptionTest` is a NEW file outside the Files table (the prose's "adoption ladder test" leg — the §4 chain proven end-to-end incl. the choreography-guard ordering and the real-registry profile match). · **[INFO]** loader-test census adaptation (P9 refuted). · **[INFO]** the G-W2-6/P14 "452" staleness (true baseline 466; realized +52 → 518). · **[INFO]** the §3 "byte-equivalent no-zoneType path" is satisfiable only as scoped to IAS selection — the DP-7/DP-8 deltas ride the 1-arg path by design (cluster-driven). · **[INFO]** the temp ingestion wire-leg runs on the rig's 03P-shaped device (the handler table is per-device universal; the 02P-shaped classification is covered at classifier/slice level). · **[INFO]** the slice's propose arm never classifies — the §4 consult lives only in `adopt()` (the instruction's "propose/adopt arms" wording overcounts).

### NEXT WU (refuse-to-close pointer)
- **The hub's two-layer audit FIRST — no commit until its order** (this entry + the completion report are the audit inputs; the two [REVIEW]s ride it).
- **Then the deploy leg: the joins night gates on this WU landing CI-green AND deploying to the Pi** (P12: adoption is a one-way door — deploy BEFORE any Wave-2 accept-listing; deploy-state re-verified AT the Pi per the v32 convention). The go-package carries the D1 [M] 5-classes · B2 [M] · E2 [M] reads and the 04P choreography guard (`ias_zone_type_learned … CONTACT` observed BEFORE its adopt step — the end-to-end test pins the same ordering desk-side).
- **UNIT-GREEN ≠ WU-DONE:** the silicon leg is the joins night; ONE re-formation can serve the M9.6-SEED hard-gate leg AND the Wave-2 joins together (Monday's pinned order; timing Nick rules).
- Carried candidates unchanged from the AVAIL/B1-REV2 entries (sidecar snapshot-init WU · real-store `setAll` teeth · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · LC-LABEL-LOG · the S31 profileId-conflict bench adjudication + the route()-gates-on-0x0104 watch now recorded in MODULE_CONTEXT).

---

## B1-REV2 — Liveness-Assert Amendment (bench repo, ruled "(A)": triggered-after-reopen) — DELIVERED, desk half (2026-07-14)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (the instruction's completion clause). No build system exists in this lane (bench repo — shell + python; bench.sh untouched, shellcheck N/A). The desk gates that RAN in-session, all GREEN against the working tree at bench `b568024` + core `1aa809d` (both clean at dispatch; **G-REV2-1 PASS** — the scenario file carried the 2026-07-14 90s adjudication header; **G-REV2-2 PASS** — verify-not-redo over the banked B1 record, no census re-derivation; the ONE new wire binding spot-checked at source: `triggeredAt` = `Instant.toString()` ISO-UTC camelCase, `ListRunsEndpoint.java:127` + `RunSummary.java:44`):
> ```
> python3 -B (3.12.4 + PyYAML 6.0.2)     # all modules import; all YAML parses
> 15-run demo sweep, EXACT exit codes    # 5 REV2 legs: PASS(0) · no-new-run FAIL(1) ·
>                                        #   anti-false-PASS FAIL(1) · 2 mutant-killers FAIL(1)/FAIL(1)
>                                        # 8 existing demos: 0/1/1/0/2 + 0/1/0 — demos 01-05 + 08
>                                        #   BYTE-IDENTICAL to the pre-WU baseline; 06/07 (the amended
>                                        #   scenario's own dry-runs) differ at EXACTLY the 2 lawful lines
>                                        # 3 REFUSED constructions (exit 2): log_any-anchor attack ·
>                                        #   mixed-asserts · unordered/unknown anchor
> mutation verification                  # mutants deleting condition (a) or (c) PASS their killer
>                                        #   fixture; the honest engine FAILs both; engine restored
>                                        #   byte-identical (cmp-verified)
> LF endings                             # direct CR byte-count = 0 on every touched file; zero __pycache__
> ```
> Change: **exactly 15 porcelain entries — 5 M + 10 new** (fresh porcelain): M `tools/runner/engine.py` (the `new_run_after` assert + the api-fixture desk mechanism + fleet hardenings) · M `scenarios/usb-reenumeration-manual.yaml` (REV2-2: the ruled-contract header [the 90s adjudication block KEPT], the assert swap, the extended wave note) · M `scenarios/SCENARIO_FORMAT.md` (REV2-4: ONE additive §5 entry citing Nick's 2026-07-14 "(A)" ruling + the amendment file) · M `tools/runner/README.md` (REV2-1's mandated conservative-bound statement + the sibling-fixture convention + the first-act-snapshot/anchor rules) · M `fixtures/runner-demo/README.md` (the fixture catalog + demo commands); NEW `fixtures/runner-demo/synthetic-liveness-{pass,no-new-run,pre-reopen-run,snapshot-member,empty-chain}.{txt,api.yaml}` ×10. **ZERO:** bench.sh/runner.py/drivers.py/bundles.py edits (git-diff-proven) · other-scenario edits · constants edits · CMD-API coupling growth (REV2-5) · product surface (G-B1-4) · Pi contact (G-B1-5) · core-repo edits.

**Coder (Claude Code).** Built per base B1 → B1-REV1 → **B1-REV2 (GOVERNING)**. **What landed (delta by delta):** REV2-1 — `new_run_after: "<log-token>"` with the pinned semantics exactly: lint-REFUSED unless the anchor is a PRECEDING plain `log:` positive (see deviations D2); at the anchor's match the engine records its OWN UTC instant (M_observed, actually-matched tokens only); the runId snapshot stamps at the FIRST act's marker (the first operator ENTER), never re-stamped, first-ATTEMPT-wins; PASS = (a) not in the snapshot AND (b) `triggeredAt` ≥ M_observed (ISO-UTC on the API timestamp — `parse_iso_utc` normalizes Z/offsets, unparseable ⇒ ignored/under-count) AND (c) causal chain shows ≥1 executed action of ANY outcome value, **outcomes QUOTED in the evidence line** (`chain outcomes ['DISPATCHED', 'UNCONFIRMED']` — the rep-3 steady-state class the old conjunction false-FAILed); `new_confirmed_run` stays implemented + documented, byte-level semantics untouched (its eval path unchanged; one defensive isinstance guard, see deviations). REV2-2 — the scenario carries the ruled contract in its header (the STRONG fact stays BANKED on the record, cited; the B2 `usb-reenumeration-confirmed` variant named), `within: 90s` unchanged, the wave act stays with the organic-tolerance note extended. REV2-3 — the three mandated fixture classes + TWO mutant-killer pairs (see deviations D3): every one of the assert's three conditions now has a fixture whose verdict FLIPS if that condition is deleted (mutation-verified in-session, engine restored byte-identical). REV2-4 — one additive §5 entry (REFUSED rule, conservative bound, the first-act-pin supersession clause, the mixed-assert refusal, both sanction cites). REV2-5 — reaffirmed zero.

**The desk api-fixture mechanism (the load-bearing design call):** REV2-3 demands fixture-pinned PASS/FAIL/anti-false-PASS demos of an API assert the desk cannot reach live. Mechanism: a **sibling `<log-fixture>.api.yaml`** (extension REPLACED) scripting responses per path, consumed in poll order — response 1 feeds the first-act snapshot, responses 2..N are the assert's scripted polls, the list's length IS the window's desk analogue (a static fixture's future is known — exhaustion = the deadline, deterministic and instant). Absent sibling ⇒ api asserts PLAN exactly as before (every pre-existing demo byte-identical). Timestamp device: post-reopen rows pin far-FUTURE (2099), pre-reopen far-PAST (2020) — deterministic through the SAME comparison code that runs live. Convention-over-flag keeps runner.py/bench.sh byte-frozen.

**In-session 4-lens adversarial fleet (layer-1 evidence, the B1/SEED precedent): 21 findings (2 DEFECT · 11 REVIEW · 8 INFO); both DEFECTs + every cheap-and-clearly-right hardening FIXED in-session, all demos re-run green.** The DEFECT (found independently by two lenses, demonstrated as a false PASS): a `log_any` line's satisfaction stamped M_observed for EVERY member token, so an anchor that never appeared in the log could evaluate — killed at both ends (lint: plain-`log:`-only anchors; eval: `satisfied_at_utc` stamps only actually-matched tokens; `satisfied_at_index` keeps its ratified B1 all-members semantics for forbidden `after:` scoping). Also fixed from the fleet: first-ATTEMPT-wins snapshot (a failed first read stays None and reports honestly — never re-baselines after the organic run landed); `isinstance(dict)` row guards in `snapshot_runs` + `eval_new_confirmed_run` (fixture-reachable crash → honest degrade); dry anchor-miss folds to FAIL-preserving skip (see D4); mid-run REFUSED verdicts keep recorded detail; dry evidence-start snapshot parity; doc corrections tracking all of it.

### Deviations (severity-honest — FOUR [REVIEW], seven [INFO])
- **[REVIEW] D1 — api asserts EXECUTE desk-side against explicit sibling fixtures.** The base instruction's verification clause said api asserts "cannot execute desk-side and must not be faked"; REV2-3 (GOVERNING) demands fixture-pinned api-assert demos, which requires SOME desk execution surface. Read: the base clause bars faking a LIVE surface / vacuous api-greens; scripted, labeled-SYNTHETIC responses are the identical harness idiom the log fixtures already are (and the demos never write bundles). Scoped: engages ONLY when the sibling file exists; plan-printing otherwise, byte-identical. Ratification requested.
- **[REVIEW] D2 — the anchor is narrowed to PRECEDING plain `log:` positives (a `log_any` member is lint-REFUSED as an anchor).** REV2-1 said "one of the scenario's own log positives"; the fleet DEMONSTRATED a false PASS through the log_any corner (the OR's satisfaction does not prove THE member matched). The narrowing is a strict subset realizing the pin's own never-vacuous intent; the shipped scenario's anchor is a plain `log:` line either way. Ratification requested.
- **[REVIEW] D3 — census: 5 M + 10 fixture adds vs the instruction's "expected: 2 M + 1 M + fixture adds".** The two excess M are `tools/runner/README.md` — REV2-1 ITSELF mandates "conservative bound stated in README" (instruction-internal inconsistency, hub-owned authoring) — and `fixtures/runner-demo/README.md` (the fixture catalog documenting the adds; the repo's standing convention). The fixture adds are 5 pairs, not 3: the two mutant-killer pairs close the fleet-found unfalsifiable-branch gap (conditions (a)/(c) had NO verdict-flipping coverage — the exact M9.6-RO-R1 [REVIEW] class, closed pre-commit per that precedent, mutation-verified). Ratification requested.
- **[REVIEW] D4 — dry-mode anchor-miss = FAIL with a recorded skip, not REFUSED.** When the anchor's own positive already FAILED against a fixture (an honest broken-capture red), the api line records `[--] not evaluated: … its anchor positive did not match` and the FAIL stands — matching live mode's fail-fast, where the assert is never reached. REFUSED (exit 2) remains for mis-AUTHORED scenarios (lint) and as the eval backstop. Reading of the REV2-1 pin's intent (REFUSED = authoring defect; fixture-miss = honest red); ratification requested.
- **[INFO]** combining `new_confirmed_run` + `new_run_after` in one scenario is lint-REFUSED in v0 — three lenses converged: their snapshot semantics differ (first-act pin vs re-stamped marker) and the mix would silently WEAKEN the strong assert (the thing REV2-1 forbids); no consumer exists; B2 defines the mix if one appears. Documented in the §5 entry + README. · **[INFO]** first-ATTEMPT-wins realizes "snapshots at the FIRST operator act's ENTER" exactly (a failed first read reports `no runs snapshot at the first act's marker` — an honest FAIL, never a silent re-baseline that could swallow the organic run). · **[INFO]** `satisfied_at_utc` stamps only ACTUALLY-matched tokens; `satisfied_at_index` (ratified B1 forbidden-scoping) untouched. · **[INFO]** isinstance row-guards in `snapshot_runs`/`eval_new_confirmed_run`: pre-fix a malformed fixture row CRASHED into the runner-internal-error backstop; `new_confirmed_run`'s assert semantics are unweakened (a non-dict row never carried a runId). · **[INFO]** mid-run REFUSED verdicts now carry already-recorded detail (evidence is never discarded). · **[INFO]** runner.py's dry-run banner conditionally understates when a sibling fixture engages; the engine's corrective note prints immediately below, before any evidence (lens-4 adjudicated adequate; banner + `--against` help text banked as a micro-item for whichever WU next unfreezes runner.py). · **[INFO]** the eval-time anchor refusal is now defense-in-depth-unreachable (lint ordering + actual-match stamping + sequential evaluation close every path) — kept deliberately.

### NOTE ledger (rides the record, no action this WU)
Window-internal ordering inversion: a stale in-window `zigbee.reopened` line (post-ENTER, pre-pull — e.g. a prior flap's recovery) can satisfy the reopen positive at a line INDEX before the detection line's; M_observed still stamps at OBSERVATION time (post-detection, sequential evaluation), so the REV2 timestamp bound stays sane — the exposure is the reopen POSITIVE itself, a pre-existing B1 at-least-window property all log positives share; a tightening (monotonic match indices) is a format-semantics change, hub's call. · SCENARIO_FORMAT §5's ratified operator-act bullet ("the run-window marker and runs snapshot re-stamp when the act's window opens") is now superseded FOR new_run_after SCENARIOS by the new entry's first-act pin — stated INSIDE the new entry (the one-additive-entry sanction barred editing the ratified bullet; a one-line qualifier there is a named hub option). · Outcome values are not vocabulary-validated (any truthy outcome counts, incl. SKIPPED — instruction-level: "any outcome vocabulary value"; the QUOTED outcomes keep adjudication honest; B2 hardening candidate: validate against `C.runs-outcomes`). · Nanosecond-precision `triggeredAt` needs python3 ≥ 3.11 (stock Bookworm ships it; README notes it). · Pi deploy reminder: Nick's Windows checkout runs `core.autocrlf=true` — deploy from a fresh LF checkout or `git archive`, never scp this working tree (the standing `.gitattributes` hygiene row; THIS delivery's files are LF-only on disk today).

### NEXT WU (refuse-to-close pointer)
- **The hub's two-layer audit FIRST — no commit until its order** (this entry + the completion-report demo transcripts are the audit inputs; the four [REVIEW]s ride it).
- **Then Nick's ONE ~90-second operator rep (the ARC's done-when, post-landing):** `bench.sh scenario usb-reenumeration-manual` on the Pi → pull/re-seat → ENTER-gated wave (or let the organic run count — the ruled philosophy) → **`[PASS] usb-reenumeration-manual` closes I3a AS FROZEN (rider 4) and the M14 RO condition together** (REV2's done-when verbatim). The AUTO variant stays gated on the Rosonway hub placement (I3b, non-blocking).
- **Then B2 (the §51 port) pending CMD-API** — now ALSO carrying: the `usb-reenumeration-confirmed` STRONG variant (`new_confirmed_run`'s consumer — banked by the ruling, NOT built here); the mixed-assert snapshot semantics IF a scenario ever needs both; the outcome-vocabulary validation candidate; `exactly:` (its standing first consumer).
- Carried candidates unchanged from B1-REV1 (constants flip checklist at CMD-API · the `.gitattributes` hygiene row · the runner.py banner micro-item now added).

---

## B1-REV1 — Scenario Runner v0 (bench repo, REDUCED per the ruled "(c) + (a)") — DELIVERED, desk half (2026-07-12)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (dispatch clause). No build system exists in this lane (bench repo — shell + python). The desk gates that RAN in-session, all GREEN against the working tree at bench `e48f04d` + core `1aa809d` (both clean at dispatch; re-verified exact):
> ```
> shellcheck tools/bench.sh                    # RAN (shellcheck 0.11.0) — CLEAN
> python3 -B <imports + full-YAML parse>       # RAN — all 4 modules + all 7 YAML files parse
> the 5 mandated dry-run demos                 # RAN — PASS(0) · expected-not-seen FAIL(1) ·
>                                              #   forbidden-hit FAIL(1) · SKIPPED [command-api](0) ·
>                                              #   empty-positives lint REFUSAL(2) — transcripts in the report
> +3 supplementary dry-runs                    # RAN — the [REVIEW] scoped-forbidden proven BOTH directions
>                                              #   (healthy re-seat PASSes · post-reopen flap FAILs) + usb SKIP
> ```
> Change: **exactly 20 files — 2 M + 18 new** (fresh porcelain): M `tools/bench.sh` (additive `scenario|suite|bundle` arm + one additive usage line; every existing verb + its output byte-untouched; `python3 -B` keeps the tree bytecode-free) + M `scenarios/SCENARIO_FORMAT.md` (one additive §5); NEW `tools/runner/` ×5 (`runner.py` CLI · `engine.py` · `drivers.py` · `bundles.py` · `README.md`) + `scenarios/` ×6 (`constants.yaml` + the 5 scenarios) + `fixtures/runner-demo/` ×7 (labeled SYNTHETIC desk-demo assets — `.txt` deliberately: the repo gitignores `**/*.log`). **ZERO:** core-repo edits · Pi contact (G-B1-5 honored — desk reads/writes only) · sqlite assertion surface (deliberately absent) · CMD-API implementation of any kind (REV-4: the `command-api` capability flag is the ONLY coupling) · product-surface growth (G-B1-4).

**Coder (Claude Code).** Re-dispatched per the base instruction + **B1-REV1 (GOVERNING)** after the ruled G-B1-2 STOP ("(c) + (a)", pm-handoff v30 beat 3 — G-B1-2 RETIRED, the endpoints-unwired fact SETTLED, not re-derived). The prior session's banked verification consumed as an INPUT OF RECORD (verify-not-redo): **all 12 G-B1-3 tokens spot-checked verbatim at source at `1aa809d`** (every file:line cite held — the currency guard PASSED, no STOP) + the phase vocabulary (`CommandLifecyclePhase.java:36-84`) + the casing caveat + the `ActionOutcome` vocabulary (`RunExplanation.java:227-233`) re-read first-hand. G-B1-1 re-run: PASS (charter + format ratified 2026-07-10, both last touched by `d1d6e3c` — zero later edits; bench.sh verbs + the 90 s loop exact). Read set consumed in full including the two the STOP session deferred (retention §2.6; dossier §5 — the plug driver implements its verb table, accept tokens, `-109` never-retry, idempotent-only retry, and settle-means-confirmed-state semantics exactly).

**What landed (DP by DP, REV deltas governing):** DP-1 bench.sh delegates three subcommands to `tools/runner/runner.py` (python3 + PyYAML only — stock-Pi apt deps). DP-2 `let:` (api pre-read + `other_of`) implemented + documented additively. DP-3 `log:` binds frozen tokens against the current boot log, run-window scoped; `api:` binds the frozen v1.1 read surface; the token re-reads at scenario start and after every `bench:` verb (lazily — a virgin bench boots before its first token read). DP-4 verdict vocabulary PASS/FAIL/SKIPPED + engine-REFUSED (distinct, exit 2); anti-vacuous ENGINE-ENFORCED; SKIPPED reporting carries rider 1's named-reason shape verbatim. DP-5 `constants.yaml` with the re-mint header, source cites per value, and REV-1's `capabilities:` map (`command-api`/`usb-power`/`plug` all false — a flip is a constants re-mint, never a code edit; the PROVISIONAL lifecycle block carries the citation + casing caveat + the status-read WIRE PATHS as constants (`terminal-field`/`phase-field`) so the CMD-API flip stays a constants re-mint). DP-6 bundles always (scenario copy · resolved constants+lets+markers+extracted values [the boot position rides the bundle — the aged-replay stake] · window-scoped log slice · journalctl slice with DEGRADE-HONEST manifest recording · api captures incl. the final at-deadline read on an api-timeout FAIL · one-page verdict · MANIFEST). DP-7 markers stamp at stimulus time; `bench:` verbs reset the window to the new boot log's line 1. **REV-1:** `command-confirm` + `timeout-honesty-no-change` authored IN FULL as SKIPPED-honest stubs on `requires: [command-api]` — suite-reported `SKIPPED: [command-api] — the command plane is unwired (CMD-API pending)`, never absent, never vacuously green; every command-plane path/field marked PROVISIONAL-until-CMD-API (incl. the AttributeValue `{"t","v"}` envelope: state fields bind `…brightness_percent.v`, re-pin at flip). **REV-2:** `usb-reenumeration` (AUTO, `requires: [usb-power]`) = detection (`log_any` transport_failed|port_unhealthy, 30s) + reopen (120s) ONLY; `usb-reenumeration-manual` (OPERATOR) gains the liveness third — after `zigbee.reopened`, the pinned wave act, then `new_confirmed_run` on the frozen runs surface (runId-set diff at the act marker → causal-chain `actions[].outcome == CONFIRMED`, 30s — API-first held, zero sqlite/log-scraping). **REV-3:** `suite all` renders the expected Monday coverage line exactly (`ran 1/5 — 2 SKIPPED: [command-api] · 1 SKIPPED: [usb-power] · 1 OPERATOR-deferred` — desk-verified via the Verdict path). DP-9 uhubctl wrapper (constants-addressed; PLACEHOLDER-guarded). DP-10 honored — no red-run scheduled; the scenario is structurally red pre-fix (the reopened POSITIVE). DP-11 plug driver ships capability-gated. DP-12 suite: lexical, never aborts, honest coverage line.

**In-session 4-lens self-review fleet (layer-1 evidence, the SEED precedent):** four parallel adversarial agents (code-correctness · instruction-compliance · token/constants-verbatim vs core `1aa809d` · frozen-surface/repo-hygiene) returned **30 findings; every defect-class finding FIXED in-session and all demos re-run green**: crash-class faults now decisive verdicts, never tracebacks (subprocess timeouts/missing binaries/`yaml` parse errors/non-numeric coercions → FAIL-with-evidence or REFUSED; plus a last-resort suite backstop — DP-12's never-abort holds absolutely) · api-evidence honesty (a non-200/unreachable read is NAMED in the progress evidence — a stale-token 401 can't masquerade as "no data"; the final at-deadline read rides the bundle) · dry-run of capture-carrying scenarios binds sentinels (plans print at flip time, exit-2 trap removed) · lazy token read (virgin-bench boot order) · `fresh-boot` precondition resets the window to line 1 · api/plug markers snapshot runs (B2-proofing) · per-line lint key enforcement at EVERY level (a misspelled `same_line:` would have silently degraded the pinnedOnly=false glance to vacuous-green — now REFUSED) · `bench.sh log` read from stdout only (stderr noise can't become the "path") · plug HTTPError classified honestly (never retried) + settle re-confirms state after the hold · `python3 -B` (no runtime bytecode in the tree) · multi-cap coverage attribution · citation precision (the brightness_percent fact lives in the RUNBOOK, not the record).

### Deviations (severity-honest — THREE [REVIEW], eight [INFO])
- **[REVIEW] R1 — the `reopen_no_target` forbidden is `after:`-scoped to `zigbee.reopened` in BOTH usb scenarios (the instruction pins it blanket).** The blanket form false-FAILs every healthy run: during the ~10 s off-window the watchdog HONESTLY prints no-target (first attempt immediate — `PortWatchdog:147`; backoff 1→30 s; the field record's attempts 1–4 pre-re-attach; `PortWatchdogTest` pins fail-×4-recover-at-5 as CORRECT). Scoped, a post-reopen no-target is still the pathology (flap/wrong-stick) and the pre-fix defect stays caught by the reopened POSITIVE. Fixture-proven both directions (supplementary demos). Independently revertible: two `after:` lines. Ratification requested.
- **[REVIEW] R2 — the engine mechanics beyond DP-2's one sanctioned `let:`** (`log_any` · `count` · `same_line` · `extract`/`min` · operator act-maps with `after:`/`confirm: enter` · forbidden `after:` · named api asserts incl. `capture` and `new_confirmed_run`): each is load-bearing for the instruction's OWN pinned content (position≥watermark ⇒ extract/min · relinked ×2 ⇒ count · REV-2's OR ⇒ log_any · the command-id handoff ⇒ capture · the liveness leg ⇒ new_confirmed_run · the reopen-then-wave flow ⇒ operator after:). All documented additively in SCENARIO_FORMAT §5 with a RATIFICATION NOTE (post-B1 format changes are STOP-gated — the hub's ratification converts or prunes). Ratification requested.
- **[REVIEW] R3 — bench.sh's `*)` usage arm was split across two source lines** to append the additive second usage line: the FIRST output line and the exit code are byte/behavior-identical (diff quoted in the report); flagged because DP-1's freeze wording covers "verbs and their output" and this arm's output gained a line. Trivially revertible if ruled out.
- **[INFO]** operator acts gate on ENTER (`confirm: enter`) so REV-2's pinned 30 s liveness window opens when hands are ready — the act-2 NOTE carries the SNZB ~59 s occupied-hold physics (waving inside the hold produces no fresh edge; the 30 s anchor kept un-retuned). · **[INFO]** the runner README lives at `tools/runner/README.md` (the instruction offered either home; `harness/README.md` documents the unrelated zigpy capture tool). · **[INFO]** demo fixtures renamed `.txt` in-session — the repo's `**/*.log` gitignore would have silently dropped them from the commit (caught by an in-session census; the fleet's frozen-surface lens confirmed the fix). · **[INFO]** precondition `app: running` unmet ⇒ FAIL with the status output quoted (a silent skip would be dishonest; an auto-start would mutate state implicitly). · **[INFO]** `exactly:` is lint-REFUSED in v0 (first consumer is B2's ias-twin port — stated in README + §5). · **[INFO]** `suite` defers OPERATOR scenarios (`OPERATOR-deferred`, reported — a nightly can't block on hands); run them via `bench.sh scenario <name>`. · **[INFO]** the `device_proposed`/`network_parameter_mismatch` forbiddens bind the instruction's UNPREFIXED grep forms (deliberately broader than the emitted `zigbee.`-prefixed tokens; both spellings noted for the sweep obligation). · **[INFO]** `new_confirmed_run` = runId-set diff vs the act-marker snapshot (organic tolerance: an organic post-ENTER run IS genuine reopened-transport traffic).

### NOTE ledger (rides the record, no action this WU)
`.gitattributes` hygiene row for the hub (core.autocrlf=true + no .gitattributes ⇒ a future Windows checkout rewrites bench.sh CRLF; a working-tree scp deploy would then ship a broken shebang — all delivered files verified LF today) · the AUTO usb variant's post-reopen forbidden window is one final sweep (recorded in its header; the OPERATOR variant's liveness third provides real dwell; a dwell mechanic = unratified format growth, deferred) · `within:` anchors are sequential-start (documented §5; lengthen-only, can never false-FAIL; B4's latency corpus will want the marker-relative form) · the two reseat fixtures dry-run via the OPERATOR variant only (the AUTO variant SKIPs on [usb-power] before reading any fixture — fixtures README states it).

### NEXT WU (refuse-to-close pointer)
- **The hub's two-layer audit FIRST — no commit until its order** (this entry + the cross-agent note are the audit inputs; the five demo transcripts ride the completion report).
- **The Pi half (Nick operates, ONLY on his post-close-out word — soak exit ≥ 2026-07-13 ~07:50, NQ-6 exit act → certification close → `1aa809d` deploy → THEN this):** deploy block + constants re-mint (watermark→P0 from Phase-7 step 20; channel/pan re-verified) + `boot-health` decisive with bundle + `suite all` coverage line + **`usb-reenumeration-manual` GREEN direct-attached (satisfies the ratified I3a AS FROZEN — rider 4; M9.6-RO R-row leg (a))**; the AUTO variant runs when the Rosonway hub is placed (I3b, non-blocking; single-variable order with a boot-health re-run first — R-row leg (b)). Paste-blocks: the 2026-07-12 cross-agent note.
- **Then B2 (the §51 port) pending CMD-API:** the two stubs flip live with CMD-API + B2's port — flip checklist recorded in constants.yaml (capability flip + re-pin `terminal-field`/`phase-field`/`data.commandId`/`…brightness_percent.v` against the live surface; rider 2: token-freeze from day one).
- Carried candidates unchanged (real-store `setAll` teeth row · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · BENCH-VERIFY test-javadoc micro-pass · LC-LABEL-LOG · the sidecar snapshot-init WU).

---

## B1 — Scenario Runner v0 (bench repo) — ⛔ STOPPED AT G-B1-2, NO CODE WRITTEN (2026-07-12)

> **STOP-AND-ESCALATE (the instruction's own load-bearing gate + the dispatch's hazard #2):** G-B1-2's confirmation target does not exist at core `1aa809d` — **the command endpoints are UNWIRED.** Neither `POST /api/v1/entities/{entity_id}/commands` nor `GET /api/v1/commands/{command_id}` is registered anywhere; the wired v1.1 HTTP surface is **READ-ONLY** (nine GET routes, zero write routes in all of src/main). Per the gate's verbatim clause this is the ruled contract conversation (charter §5 rider: *"if a §51-class assertion needs an event the frozen v1.1 surface doesn't expose, that is a contract conversation brought to me — never a raw-SQLite fallback inside a scenario"*). Per the STOP discipline, **zero files were written in either repo** (bench tree clean at `e48f04d`; core clean at `1aa809d`). Full evidence census + ruling options: the 2026-07-12 cross-agent note. The decision is the hub's/Nick's — nothing was improvised around it.

**Coder (Claude Code).** Dispatched per `context/instructions/2026-07-12_B1_scenario-runner-v0_coding-instruction.md`. Duplicate-dispatch check FIRST: newest handoff entry was M9.6-AVAIL — B1 is a fresh dispatch, not a re-paste; bench porcelain clean. Baselines re-verified EXACT at dispatch: bench `e48f04d` · core `1aa809d` (clean trees ⇒ working-tree reads ≡ the pinned git objects). Mandatory read set consumed through the STOP point: charter + SCENARIO_FORMAT + bench.sh + runbook Phases 4/5/7 + the rest-api contract files + rest-api MODULE_CONTEXT + integration-zigbee MODULE_CONTEXT M9.6-RO/M9.6-AVAIL sections (retention §2.6 + dossier §5 deliberately NOT consumed — they govern implementation details the STOP suspends; the re-dispatch session reads them fresh).

**Gate adjudications (all run BEFORE any code, as mandated):**
- **G-B1-1 (spec currency): PASS.** Charter + SCENARIO_FORMAT read as ratified 2026-07-10; bench git history confirms zero later edits (both last touched by `d1d6e3c`, the B0 delivery commit itself). `tools/bench.sh` matches baseline: verbs exactly `start|stop|restart|status|health|log|entities|runs|events|state` (`:4`, `:67-79`), the 90 s decisive-radio loop at `:49-61`; last touched `a81d678`.
- **G-B1-2 (contract grounding): FAIL ⇒ STOP.** The census: (1) the ONLY route-registration site in all of core src/main is `RestFilters` — `app.get` ×9: `/api/v1/entities` `:133` · `/api/v1/entities/{entityId}` `:135` · `/api/v1/entities/{entityId}/state` `:137` · `/internal/dlq` `:211` · `/internal/projection` `:213` · `/api/v1/runs` `:259` · `/api/v1/runs/{runId}/causal-chain` `:261` · `/api/v1/automations` `:304` · `/api/v1/automations/{id}/non-firing` `:306`; **zero `app.post/put/delete` registrations exist anywhere in src/main** (repo-wide census grep). (2) `bringUpHttpSurface` (`HomeSynapseCore:887-947`) installs exactly five groups — auth, readiness, entity-query, admin, run-query, automation-query — no command group exists to install. (3) The five lifecycle types (`CommandRequest`/`CommandAcceptedResponse`/`CommandStatusResponse`/`CommandLifecyclePhase`/`LifecyclePhaseDetail`) are **Phase-2 specification artifacts with ZERO production references outside their own five files**; their javadoc names both routes — which is what made the instruction's read-set framing plausible at authoring. (4) rest-api `MODULE_CONTEXT.md:372` lists "command issuance, command status" under **"Endpoint handler implementations needed"** (Phase-3 outstanding). (5) `RestApiServer.registerRoute` (`:46`) has zero production callers — the Phase-2 interface path was never wired; the real wiring is `RestFilters` over Javalin. (6) websocket-api: zero command/issue matches — no alternative command surface. **The failure is BROADER than the gate's anticipated shape:** not merely the discriminator missing from the status read — the STIMULUS (the POST) doesn't exist either, so the pre-stated OPERATOR-tier fallback *as authored* also fails at source (no REST command stimulus exists at ANY tier; bench commands ride the `bench-hero` automation + a physical wave — runbook steps 13/15 — with verdicts read via raw sqlite, the human-only fallback the charter bars from scenarios).
- **G-B1-3 (frozen-token verbatim): PASS — all 12 resolved at source** (the 11 listed + `zigbee.key_establishment_failed` from boot-health's forbidden list). Table below — carried so the re-dispatch needn't re-derive.
- **G-B1-4 (product-surface STOP): not exercised** — no code written.
- **G-B1-5 (soak sanctity): HONORED** — zero Pi contact; desk reads only.

**The G-B1-3 token table (verbatim formats at `1aa809d`):**
| Token (format-frozen) | Source |
|---|---|
| `registry.projection_live: devices={} entities={} position={}` | `lifecycle/RegistryProjectionSubscriber.java:107` |
| `zigbee.adoption_maps_rehydrated: devices={}` | `ZigbeeIntegrationAdapter.java:971` |
| `zigbee.device_relinked: device={} deviceId={} — re-pairing, …` | `ZigbeeAdoptionSlice.java:513` |
| `zigbee.device_proposed: device={} manufacturer={} model={} …` | `ZigbeeAdoptionSlice.java:218` |
| `zigbee.network_resumed: channel={} panId=0x{}` | `ZigbeeIntegrationAdapter.java:626` |
| `zigbee.port_identity_captured: stableId={} vendorId={} productId={} pinnedOnly={}` (ids lowercase hex; once per BIND — never re-prints at reopen) | `ZigbeeIntegrationAdapter.java:579` |
| `zigbee.transport_failed: {} — the watchdog owns …` | `ZigbeeIntegrationAdapter.java:725` |
| `zigbee.port_unhealthy: cause={}; reopen scheduling started` | `PortWatchdog.java:148` |
| `zigbee.reopened: port={}` | `ZigbeeIntegrationAdapter.java:773` |
| `zigbee.reopen_no_target: the coordinator port did not …` | `ZigbeeIntegrationAdapter.java:757` |
| `zigbee.network_parameter_mismatch: coordinator channel={} …` | `NetworkFormation.java:307/:315` |
| `zigbee.key_establishment_failed: device={} status={}` | `ZclIngestionUnit.java:287` |

**Phase vocabulary pinned for the record (from the Phase-2 types — ready the day the endpoints land):** `CommandLifecyclePhase` = `ACCEPTED | DISPATCHED | ACKNOWLEDGED | CONFIRMED | CONFIRMATION_TIMED_OUT` (`CommandLifecyclePhase.java:36-84`); `CommandStatusResponse` = 8 fields incl. `currentPhase` + `terminal` (the MODULE_CONTEXT:346 count gotcha); terminal classes = CONFIRMED · CONFIRMATION_TIMED_OUT · rejected/timed-out ACKNOWLEDGED (javadoc `:24-27`). **Wire-casing caveat for the contract conversation:** LTD-08 says SNAKE_CASE, but the LIVE v1.1 endpoints hand-build **camelCase** wire keys (`GetRunCausalChainEndpoint` javadoc `:36` + `meta.viewPosition`/`meta.timestamp` `:87-88`) — the command read's casing must be pinned at implementation time, never assumed from the module row.

**What the frozen surface CAN discriminate today (decision-grade for any fallback ruling):** `GET /api/v1/runs/{runId}/causal-chain` exposes `actions[].outcome` ∈ {`DISPATCHED`, `CONFIRMED`, `UNCONFIRMED`, `FAILED`, `SKIPPED`} + `actions[].reason` (`RunExplanation.java:227-233`); the event mapping (`StandardExplanationService.java:644-653`): `state_confirmed` → CONFIRMED · `command_confirmation_timed_out` → UNCONFIRMED with reason `"confirmation timed out"` · command-caused failure → FAILED. **Never-false-CONFIRMED is per-action dischargeable on the frozen surface** — but only for AUTOMATION-initiated commands (physical trigger ⇒ OPERATOR tier; no REST manual-fire route exists either).

**Blast radius over the four scenarios as authored:** `boot-health` — fully implementable (log tokens + `GET /api/v1/entities`), zero impact · `command-confirm` — dead as authored (no stimulus, no verdict read) · `timeout-honesty-no-change` — dead as authored (same; the DP-2 `let:` read `GET /entities/{id}/state` EXISTS but is purposeless without a command stimulus) · `usb-reenumeration` ×2 — detection + reopen legs fully implementable (all tokens live), the post-reopen liveness leg (the command-confirm shape) blocked. **The Monday RO⊗B1 coupling survives in reduced form if so ruled** — the R-row's (a)+(b) evidence is detection→reopen in both topologies; the liveness third is the blocked part.

### NEXT WU (refuse-to-close pointer)
- **BLOCKED on the hub's contract ruling.** Options laid out in the cross-agent note (none pre-empted here): **(a)** a ruled core WU wiring the two command endpoints (the Phase-2 types are complete; needs issuance→`publishRoot(command_issued)` + a status assembler over the EventStore by correlation id + `RestFilters` registration + the casing/envelope decision), then B1 proceeds exactly as authored; **(b)** the instruction's own pre-stated fallback *as amended by source reality*: re-author rows 2–3 + the usb liveness leg onto the frozen runs surface (wave-stimulus OPERATOR tier, causal-chain verdict read); **(c)** split — dispatch B1-reduced now (engine + `boot-health` + usb detection/reopen legs + SKIPPED-honest command scenarios), hold rows 2–3 for (a), keeping Monday's RO regression harness intact.
- Everything verified this session (gate evidence, the token table, the phase vocabulary, the casing caveat) is recorded above to make the re-dispatch cheap. Lesson appended (coder-lessons 2026-07-12: route-census-not-type-read).

---

## M9.6-AVAIL — Availability-ALIVE Wiring (the tracker goes live) — DELIVERED (2026-07-11)

> **DEFERRED BUILD GATE (Nick):** full `./gradlew check` was NOT run in-session. The instruction's CC-lane targeted allow-list RAN in-session and is **GREEN** against the M9.6-AVAIL working tree (built on core `3590fca` — M9.6-RO-R1; the STOP gates G-AV1–G-AV5 re-run at THIS HEAD per the instruction's dispatch clause, all PASS: G-AV1 tracker unwired [grep: zero production constructions/callers] + signatures held; **G-AV2 held with the hub's pre-adjudicated exception** — M9.6-RO's one `initialize()` line [the PortLocator 2-arg construction, `channelOpener==null` branch]; its other adapter hunks sat in imports/fields/ctors/`bindTransport` — `onFrame`/`runCycleOnce` untouched; G-AV3 `ZigbeeAdoptionSlice` diff since `04f5f70` EMPTY + relink emission device-grain confirmed at source; G-AV4 zero exported-surface additions [the one `+public` diff token = the interface-mandated `@Override` inside the PRIVATE nested listener, the `AdapterIngestionListener` idiom]; G-AV5 `AVAILABILITY_CHANGED`/payload record/entity-grain projection apply all pre-exist, zero core edits):
> ```
> ./gradlew :integration:integration-zigbee:compileJava     # RAN early (+compileTestJava, shift-left, -Werror) — GREEN
> ./gradlew :integration:integration-zigbee:test --rerun-tasks
>                                                           # RAN — stage-A red at HEAD: 24 targeted, EXACTLY 12 failed for the right reasons;
>                                                           # stage-B: 466 tests, 0 failures, 0 skipped (452 + 14 new, forced fresh)
> ./gradlew :lifecycle:lifecycle:test --rerun-tasks         # RAN (defensive, beyond the allow-list — the new publishes ride the real bus there) — GREEN: 56/0/0
> ./gradlew :integration:integration-zigbee:spotlessApply   # RAN — clean
> ./gradlew check                                           # Nick's gate — NOT run; CI on the pushed commit is the gate of record
> ```
> Change: **4 files** — 2 main (`ZigbeeIntegrationAdapter`, `ZigbeeAdoptionSlice`) + 1 modified test (`ZigbeeConfigAcceptedAdoptionTest` — the ONE P2-sweep drift, T4 grain-split) + 1 NEW test (`ZigbeeAvailabilityWiringTest`, 14 scenarios) + MODULE_CONTEXT (WUCP). **ZERO:** event mints (73/43/55 hold) · schema · config · module-info · build files · state-store/event-model edits · public surface. **The tracker trio (`StandardAvailabilityTracker`/`AvailabilityTracker`/`AvailabilityReason`) BYTE-UNTOUCHED** (the `transition()` dead-expression smell deliberately left — the instruction's tidy-only-if-byte-proven condition not exercised). Tokens: the DP-8 tracker INFO (`zigbee.availability_changed: device={} available={}`) goes LIVE and is **FROZEN from this WU** (file zero-diff ⇒ byte-frozen by construction; test-pinned verbatim both directions); ONE new defensive WARN `zigbee.availability_publish_conflict` (the relink-precedent conflict swallow — flagged [INFO], see deviations); all pre-existing tokens verified byte-untouched in the diff.

**Coder (Claude Code).** Built per `context/instructions/2026-07-10_M9.6-AVAIL_availability-alive-wiring_coding-instruction.md` (grounding read first: pm-handoff v27 beat-1 R2 + beat-5 AVAIL settlements; the full minimum read set first-hand at HEAD — the tracker trio in full, the three adapter regions, the slice publish/entity regions, the cache sidecar, the payload record, both projection methods, the `zclGlobalExchange`/`readBasic` seams + `awaitIncomingLocked`'s consumption semantics). **Tests before implementation, red-first staged honest:** all 14 tests + the T4 update written FIRST and compiled against UNMODIFIED HEAD (zero new production symbols referenced — the purest red stage); the red run: EXACTLY 12 failing for the right reasons (`Expected size: N but was: 0 in: []` on the publish legs · the DP-8 INFO absent verbatim [`Expecting actual: []`] · the sidecar `Optional empty`), the 3 never-feeds pins + the M-1 silence pin green by design; the implementation turned all 466 green forced-fresh.

**What landed (DP by DP):** DP-1 `initialize()` constructs `StandardAvailabilityTracker(clock, powerSourceLookup, Map.of(), listener)` — **empty map deliberately** (the ruled M-1 trap; sidecar snapshot-init + boot view reconciliation = the NAMED deferred WU, untouched); DP-2 lookup = `cache.device(ieee).map(powerSource).orElse(0)` verbatim. DP-3 `onFrame` adds `availabilityTracker.recordFrame(device, clock.instant())` beside the existing cache/queue calls (pre-dedup; the `cache.recordFrame` name-collision commented at the site). DP-4 `runCycleOnce()` gains `evaluateAvailabilityTimeouts()` after the interview steps (post-drain — this cycle's RX counts first): battery 25 h as-built; each mains candidate gets ONE ZCL Basic read of attr 0x0000 via the EXISTING `zclGlobalExchange` seam (`AVAILABILITY_PING_TIMEOUT_MILLIS=5000`, chosen — the reporting binding's window) → `recordCommandResult(candidate, responded, now)`; empty exchange result (NCP rejection or await-timeout) = the honest `responded=false`; an `EzspCommandTimeoutException` (deaf NCP) propagates to the production loop's existing watchdog arm. DP-5 the private nested `EntityAvailabilityPublisher`: one root event PER ADOPTED ENTITY via the NEW package-private `ZigbeeAdoptionSlice.entitiesFor(IEEEAddress)` (populated by adopt AND relink — post-restart fanout works), entity-grain `SubjectRef`, canonical `"online"`/`"offline"`, CRITICAL→offline / NORMAL→online, `publishRoot` + swallow-conflict; every transition writes `cache.setAvailability`; `previousStatus` = the listener's own last-published state (mirrors the tracker's prior state exactly under DP-1). DP-6 relink emission BYTE-UNTOUCHED (slice diff = exactly the accessor). DP-7 honored: the ping arm is the ONLY `recordCommandResult` caller (census in the report). DP-8 as above.

### Deviations (severity-honest — ONE [REVIEW], five [INFO])
- **`[REVIEW]` the adoption-time view seed (`seedFreshAdoption` + the composed `onAdopted` hook):** on the bench the announce ALWAYS precedes adoption and `handleAnnounce` fires `onFrame` first (`ZclIngestionUnit:322`) — the tracker's online edge fires with ZERO adopted entities (publishes nothing per DP-5) and edge-triggering keeps every later report silent: freshly adopted entities would sit UNKNOWN in the view until the device's first offline/online cycle, **failing the WU's own silicon done-when.** The seed publishes the per-entity `("unknown","online")` edge at adoption IFF the tracker already holds the device AVAILABLE (state-guarded; a tracker-unseen device seeds nothing — its first frame edges normally; the seed feeds the tracker NO evidence, so DP-3/DP-7's input census is undisturbed). No settled decision contradicted — an emergent ordering gap. One independently-revertible hunk-pair; ratification requested.
- **`[INFO]` the DP-4 "responding ping ALSO arrives as RX" parenthetical is refuted at source:** `awaitIncomingLocked` CONSUMES the matched response (only non-matching callbacks feed the drain) — `PING_SUCCESS` via `recordCommandResult(true)` is the sole (still device-originated) success evidence; behavioral outcome identical. The mains-ping-success test asserts reality (lastSeen-refresh pinned via the no-re-ping-inside-a-fresh-window observable).
- **`[INFO]` the ping arm resolves addresses CACHE-ONLY** (record + non-sentinel `networkAddress`; unresolvable ⇒ `responded=false`, self-healing on next RX): the protocol `lookupNetworkAddress` fallback THROWS ISE on unknown devices, which would escape the cycle to the supervisor — and a mains-classified candidate always has an interviewed record (powerSource ⇒ record exists). Ping endpoint = the record's first non-Green-Power endpoint (the interview's EP-selection rule; EP-1 defensive fallback).
- **`[INFO]` the defensive conflict WARN `zigbee.availability_publish_conflict`:** DP-8 said "no other new tokens" but the mandated swallow-conflict idiom needs a log line (the relink precedent's own shape). Error-arm only, unreachable in practice, not scenario vocabulary — flagged rather than silently minted.
- **`[INFO]` `entitiesFor` accessor on the slice** (package-private, returns the stored immutable copy): realizes DP-5's "resolve via the adoption slice's entity map" — no all-entities-per-device accessor existed.
- **`[INFO]` `:lifecycle:lifecycle:test` run beyond the allow-list** — defensive (the new entity-grain publishes ride the real bus + state projection in those ITs); GREEN 56/0/0, zero edits.

### NEXT WU (refuse-to-close pointer)
- **Nick's gate:** full `./gradlew check` pre-commit; **CI on the pushed commit = the gate of record.** Then the hub's M9.6-AVAIL audit (the [REVIEW] seed adjudication rides it) → next dispatch per the ruled priority stack: the explainability push's remaining items (FE-7 consumes THIS WU's data · the no-change explanation ruling (b) · the verdict-detail read grounding) and Monday's pinned silicon order (soak exit ≥ 2026-07-13 ~07:50 Pi-local; the SEED/RO/AVAIL silicon legs compose with Wave-2 per the pinned sequence).
- **UNIT-GREEN ≠ WU-DONE — the silicon leg (post-soak, composes with Wave-2):** the SNZB-02P joins → reports flow → its entities read AVAILABLE in `/api/v1/entities` (the FE-7 tile's data goes live); then a genuine offline leg (a device unpowered past its window / a mains ping-timeout) reads UNAVAILABLE — never-false-ALIVE demonstrated in both directions on real devices.
- **NEW named deferred WU dependency now live:** the sidecar snapshot-init + boot-time view reconciliation WU (DP-1's named deferral) — the sidecar is being written (this WU's write-through) and the relink device-grain oddity awaits the same WU; one coherent WU, never split.
- Carried candidates unchanged from the RO/SEED/DURc entries (real-store `setAll` teeth row · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · BENCH-VERIFY test-javadoc micro-pass · LC-LABEL-LOG).

---

## M9.6-RO — Reopen Identity Capture (fix-the-capture + the pinned-only sentinel) — DELIVERED (2026-07-10)

> **DEFERRED BUILD GATE (Nick):** full `./gradlew check` was NOT run in-session. The instruction's CC-lane targeted allow-list RAN in-session and is **GREEN** against the M9.6-RO working tree (built on core `7e4426b` — M9.6-SEED `a1f0f77` + M9.5-DURc landed above the authoring baseline `04f5f70`; **G-RO2 disjointness re-run at dispatch HEAD and CONFIRMED:** neither commit touched this WU's code files [SEED = NetworkFormation/stores/EzspCoordinatorProtocol §security + 5 tests; DURc = 3 lifecycle ITs]; the only shared path is `MODULE_CONTEXT.md`, the WUCP doc itself — the SEED section is untouched by this WU's append. G-RO1/G-RO3/G-RO4 all verified PASS first-hand pre-code):
> ```
> ./gradlew :integration:integration-zigbee:compileJava     # RAN early (+compileTestJava, shift-left) — GREEN
> ./gradlew :integration:integration-zigbee:test --rerun-tasks
>                                                           # RAN ×2 — stage-A red: 451 tests, EXACTLY 4 failed for the right reasons;
>                                                           # stage-B: 451 tests, 0 failures, 0 skipped (440 + 11 new, forced fresh)
> ./gradlew :integration:integration-zigbee:spotlessApply   # RAN — clean
> ./gradlew check                                           # Nick's gate — NOT run; CI on the pushed commit is the gate of record
> ```
> Change: **7 files + MODULE_CONTEXT** — 3 main (`PortIdentity`, `PortLocator`, `ZigbeeIntegrationAdapter`) + 4 test (`PortIdentityTest`, `PortLocatorTest`, `PortWatchdogTest`, `ZigbeeProductionTransportTest`). **ZERO:** event mints (73/43/55 hold) · schema · config (`serial_port` semantics unchanged — its alias form simply starts working on reopen) · module-info (G-RO4 held: zero `public` additions, diff-grep-verified) · build files. New log token: the DP-5 capture INFO only (`zigbee.port_identity_captured: stableId={} vendorId={} productId={} pinnedOnly={}` — ids lowercase hex); `reopen_no_target` / `zigbee.reopened` / `production_session_started` verified BYTE-FROZEN (zero frozen-token hits in the diff).
>
> **RE-VERIFIED 2026-07-11 (Coder, duplicate dispatch — verify-not-redo):** the M9.6-RO dispatch was pasted into a second fresh session on 2026-07-11, which found this delivery already on the working tree (this entry authoritative) and VERIFIED rather than redid it: grounding reads re-run (pm-handoff v27 beat-1 R1 · the bench correction block) · **G-RO1–G-RO4 re-run first-hand PASS at `7e4426b`** (all four G-RO1 signatures confirmed at HEAD via `git show`; SEED/DURc file-disjointness confirmed from the commits themselves; G-RO3 ctor-pin grep clean; G-RO4 zero added `public` tokens) · the full diff re-audited DP-by-DP (DP-1…DP-6 + all 8 instruction test rows located in the delivered tests) · frozen tokens re-verified byte-untouched in CODE (the only 2 diff hits are MODULE_CONTEXT prose describing them) · the 0/0-construction census re-run (exactly the one DP-1 fallback site, `ZigbeeIntegrationAdapter:505`) · `reopenTarget` caller census re-run (exactly `attemptReopen()`, `:648`) · **targeted gates re-run GREEN: `compileJava`+`compileTestJava` (-Werror) · `test --rerun-tasks` 451 / 0 failures / 0 skipped forced-fresh · `spotlessCheck` clean.** ZERO new edits to the core tree. Nick's full `check` + CI on the push remain the gate of record; the silicon legs stand as recorded below.

**Coder (Claude Code).** Built per `context/instructions/2026-07-10_M9.6-RO_reopen-identity-capture_coding-instruction.md` (grounding read first: pm-handoff v27 beat-1 R1 + the bench record's 2026-07-10 correction block; all five locator-family files + the four adapter regions + the fake-enumerator idiom read first-hand at HEAD). **Tests before implementation, red-first staged honest:** all 11 tests written first; stage A (seams ONLY — `isPinnedOnly`, the `PortLocator` 2-arg ctor + `realPathCanonicalizer`, the adapter 9-arg ctor + wiring; `reopenTarget`/`bindTransport` behavior untouched) let them compile; the red run: EXACTLY 4 failing for the right reasons (the FIELD REPRO + unresolvable-pin + locator-arm legs on the ABSENT DP-5 INFO — `Expecting actual: []` verbatim in the report XML — and the pinned-only match on the absent DP-2 arm), the 7 regression pins green by design (devnum-immunity PIN · two-sticks safety · raw-string semantics · healthy-never-consults-canonicalizer · `realPathCanonicalizer` fallback · the truth table · the watchdog cadence); stage B (`captureIdentity` + the pinned-only arm + the INFO) turned all 451 green forced-fresh. Handed back **gate-targeted-GREEN / full-check-deferred**.

**What landed (DP by DP):** DP-1 `bindTransport()`'s `vendorId() < 0` arm rewritten to private `captureIdentity()`: canonicalize the configured path → re-scan `portEnumerator.enumerate()` for `systemPath()` equal to the canonical path AND `vendorId() >= 0` → `identityFor` (the REAL identity: by-id stableId + real VID:PID — every reopen tier works); unfindable-or-unidentified ⇒ the honest pinned-only sentinel `(0, 0, pinnedPath)`; the happy arm UNTOUCHED. DP-2 `reopenTarget`'s LEADING pinned-only arm: canonicalized-path equality ONLY (both sides canonicalized; null `byIdPath` guarded), Tier 2 SKIPPED, NO class-constant fallback (the two-sticks safety leg test-pinned, both resolvable and unresolvable arms); healthy identities' two tiers BYTE-UNCHANGED (pinned by a throwing-canonicalizer test). DP-3 `PortIdentity.isPinnedOnly()` (0/0; USB VID 0x0000 reserved — the sanctioned sentinel; ctor negative-rejection STAYS). DP-4 `PortLocator` 2-arg ctor + `static realPathCanonicalizer()` (best-effort `toRealPath`, RAW-string fallback on `IOException | InvalidPathException` — total, never throws); the 1-arg ctor delegates on `UnaryOperator.identity()` (raw-string semantics = pre-RO behavior); the adapter's 8-arg ctor binds the production canonicalizer (factory UNTOUCHED); tests inject pure maps. DP-5 ONE INFO at bind time, all arms, format-frozen. DP-6 honored: no re-resolution fallback anywhere; `attemptReopen()`'s post-match sequence BYTE-UNTOUCHED.

### Deviations (severity-honest — ZERO [REVIEW], six [INFO])
- **`[INFO]` the DP-1 re-scan filters `vendorId() >= 0`** — `identityFor` constructs `PortIdentity` whose ctor REJECTS negatives (the DP-3 pin, "no relaxation"), while enumerated candidates lawfully carry jSerialComm's −1/−1 "unknown"; without the guard, a pinned UART-hat-class node (`/dev/ttyAMA0`) literal-matching an unidentified candidate would throw `IllegalArgumentException` out of `bindTransport`. A found-but-unidentified node falls to the DP's own pinned-only fallback (the honest representation). Lesson appended (coder-lessons 2026-07-10).
- **`[INFO]` the adapter gained a 9-arg canonical ctor** (+`UnaryOperator<String> pathCanonicalizer`, package-private, last param; the 8-arg delegates on the production binding): required by the instruction's OWN field-repro test row ("fake canonicalizer mapping alias→real" must reach the adapter's bind-time use) and the P2 survey's "adapter wiring" clause. Zero existing-call-site churn; zero public surface.
- **`[INFO]` the 1-arg `PortLocator` ctor defaults to `UnaryOperator.identity()`** (raw-string semantics), NOT the production binding — the class stays I/O-free unless composition explicitly injects I/O (DP-4's own test clause); the production binding lives at the adapter ctor.
- **`[INFO]` DP-5 `vendorId`/`productId` render lowercase hex** (`Integer.toHexString`) — the instruction pinned the format string, not the argument encoding; hex matches the family's VID:PID rendering (the `transport_unbound` PIE's "10c4"). Test-pinned verbatim on every arm.
- **`[INFO]` the optional watchdog-timeline row realized as ONE field-cadence leg** (fail ×4 on the backoff → recover at attempt 5, accounting asserted) — the row's remainder was already covered by the existing `cappedBackoffSchedule`/`successResetsBackoff` pins.
- **`[INFO]` the pinned-only adapter test extends the instruction's row end-to-end:** after the sentinel capture, a late-resolving alias (mutable map) proves the DP-4 wiring — `attemptReopen()` TRUE via the pinned-only rule.

### NEXT WU (refuse-to-close pointer)
- **Nick's gate:** full `./gradlew check` pre-commit; **CI on the pushed commit = the gate of record.** Then per the no-open-[REVIEW] rail: the hub's M9.6-RO audit → **M9.6-AVAIL dispatch** (authored ISSUE-READY at v27 beat 5 — `context/instructions/2026-07-10_M9.6-AVAIL_availability-alive-wiring_coding-instruction.md`).
- **UNIT-GREEN ≠ WU-DONE — the silicon legs (post-soak, Monday's pinned order):** (a) the B1 runner's `usb-reenumeration` scenario (uhubctl power interruption) GREEN on the fixed build — the ruled regression harness; (b) **BOTH topologies** verified, direct-attached AND hub-attached (the dongle-behind-hub move is its own single-variable step with a boot-health re-run first); (c) the DP-5 INFO reading `pinnedOnly=false` on the alias-pinned bench config. **The R-row closes on (a)+(b).** UDEV-RAMP stays gated on this landing (the alias best practice IS the pre-fix defect window).
- Carried candidates unchanged from the SEED/DURc entries (real-store `setAll` teeth row · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · BENCH-VERIFY test-javadoc micro-pass · LC-LABEL-LOG).

---

## M9.5-DURc — Lifecycle IT Registry-Projection Checkpoint Barrier (TEST-ONLY) — DELIVERED (2026-07-10)

> **DEFERRED BUILD GATE (Nick):** NO build commands ran in-session (this lane's CLAUDE.md: Nick owns the compile gate outright — no targeted runs either). The instruction's done-when commands, against the M9.5-DURc working tree (built on core `a1f0f77`, the two target files untouched by it — baseline exact):
> ```
> ./gradlew :lifecycle:lifecycle:test --rerun-tasks    # ×3 consecutive forced-fresh — the cheap flake shakedown
> ./gradlew check                                      # Nick's gate pre-commit; CI on the pushed commit = the gate of record
>                                                      # — and it simultaneously completes the a1f0f77 (M9.6-SEED) gate record
> ```
> `spotlessApply` also not run — the new code hand-matches the three files' existing style; if spotlessCheck complains, `./gradlew :lifecycle:lifecycle:spotlessApply` is safe (test-tree-only diff).
> Change: **exactly 3 test files, +123 insertions / 0 deletions, ZERO src/main** (`git diff --stat` verified): `HeroLoopHardwareFreeIT`, `ZigbeeReplaySafetyIT`, **`RestartHonestyIT` (the [REVIEW] extension — below)**. **ZERO:** production diffs · log-token changes · build files · assertion changes · await-budget retuning · MODULE_CONTEXT (out of scope per instruction — the hub records the race spine-side).

**Coder (Claude Code).** Built per `context/instructions/2026-07-10_M9.5-DURc_lifecycle-projection-barrier_coding-instruction.md`. Red-first sanctioned-impractical (the CI record on `a1f0f77` is the red); the optional spike repro not run (no build capability this lane). Every seam verified first-hand at source before code: `SubscriberSnapshot.checkpoint()` (last delivered global position), `EventStore.latestPosition()`, `RegistryProjectionSubscriber.SUBSCRIBER_ID`/`subscriptionFilter()`, `InProcessEventBus.notifyEvent`/`liveLoop`, `ZigbeeAdoptionSlice` publish order.

**What landed:** one private `awaitRegistryProjectionCaughtUp()` + `registryProjectionCheckpoint()` helper pair per IT (each file's existing duplicated-helper idiom — no shared util minted), called between the last `adopt()` and the first `label()` (DP-2: HeroLoop `bootAndAdopt()` after the hue adopt; ReplaySafety after `rig.adopt`; RestartHonesty `boot()` after the hue adopt). The barrier reuses each IT's existing `awaitTrue` (DP-3, no new timeouts) with the instructed await description verbatim. The checkpoint reads ride `core.eventBus().subscribers()` filtered on `RegistryProjectionSubscriber.SUBSCRIBER_ID` (same-package symbolic binding, no string literal to drift).

### Deviations (severity-honest)
- **[REVIEW] DP-1's literal mechanism corrected — the await target is the max `globalPosition` over the `device_registered`/`entity_registered` envelopes, NOT the event-store head.** The instructed shape (`checkpoint >= head`, head via `latestPosition()`/max envelope position) **deadlocks deterministically**: the subscriber is type-filtered (`forTypes` over the 3 registration/removal types — `RegistryProjectionSubscriber.subscriptionFilter()`, registration at `HomeSynapseCore` Step 3.1), and the bus advances a runtime subscriber's checkpoint ONLY on MATCHING deliveries (`InProcessEventBus.notifyEvent` never offers a filtered-out position — `:276`; `liveLoop`'s filter miss `continue`s with no checkpoint write — `:492`), while `adopt()` publishes the NON-matching `device_adopted` LAST (`ZigbeeAdoptionSlice:59`, `:382`). So the store head is strictly above the highest reachable checkpoint at barrier time, every run — the barrier would convert a flake into a guaranteed 10 s timeout red. The `ReplayTransitionIT` pattern source is head-safe only because its subscriber is `SubscriptionFilter.all()`. The corrected target is exactly "the subscriber's durable consumption of the adoption events" the DP intends; checkpoint ≥ last matching position ⇒ every adoption self-delivery has applied (checkpoint writes follow successful delivery). The instruction's "resolve the exact seam / state the choice" latitude partially covers this; flagged [REVIEW] because it diverges from the DP's stated formula. `latestPosition()` deliberately NOT used (stated choice: it is the cheaper accessor but the WRONG target for a filtered subscriber).
- **[REVIEW] DP-4's exclusion premise is refuted by its own mandated grep — the barrier is ALSO in `RestartHonestyIT` (independently revertible hunk).** The pin's rationale ("they don't label") is false: `RestartHonestyIT.java:152` labels, `:157` `updateEntity` — the identical adopt→label boot shape, the identical wipe race (its `fireManual("hero-turn-on")` → `awaitEnvelope(command_issued)` would exhaust the same way; its prior CI green is probabilistic per the diagnosis's own doctrine). Leaving a diagnosed same-mechanism race armed in the same suite contradicts the WU's flake-is-a-defect purpose and endangers the very CI run that is this WU's + M9.6-SEED's gate of record. **PM ratification requested**; reverting is one hunk if ruled out of scope. `ProcessRestartIdentityIT` confirmed label-free by grep (zero `label(`/`updateEntity` hits) and is UNTOUCHED — DP-4 holds there.
- **[INFO]** helper decomposition: two private helpers per IT (barrier + checkpoint read) instead of one inline block; `AssertionError` on an empty registration-fact set (a can't-happen guard that fails loud if a future edit moves the barrier above `adopt()`).

### NEXT WU (refuse-to-close pointer)
- **Nick's gate:** the ×3 forced-fresh lifecycle runs + full `./gradlew check`, then push — **CI green on that commit is the gate of record AND completes the a1f0f77 (M9.6-SEED) gate record.** Then per the M9.6-SEED pointer: **M9.6-AVAIL next, then M9.6-RO** (PM instructions expected); the SEED silicon leg rides post-soak in Monday's pinned order.
- **Recorded follow-up (hub backlog row, NOT this WU):** test labeling rides the log (an F1 `entity_registered` re-emit carrying the label) — direct `updateEntity` from tests races REG-INV-1's projection by construction; the row now covers THREE ITs (RestartHonestyIT included).
- Carried candidates unchanged from the M9.6-SEED entry (real-store `setAll` teeth row · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · BENCH-VERIFY test-javadoc micro-pass).

---

## M9.6-SEED — Generated-Seed TCLK Custody (SD-5) — DELIVERED, code half (2026-07-10)

> **DEFERRED BUILD GATE (Nick):** full `./gradlew check` was NOT run in-session. The instruction's CC-lane targeted allow-list RAN in-session and is **GREEN** against the M9.6-SEED working tree (built on core `04f5f70`, baseline EXACT — G-SEED1–G-SEED5 all verified PASS first-hand pre-code):
> ```
> ./gradlew :integration:integration-zigbee:compileJava   # RAN early (shift-left, -Werror) — GREEN
> ./gradlew :integration:integration-zigbee:test --rerun-tasks
>                                                          # RAN — GREEN: 440 tests, 0 failures, 0 skipped (429 + 11 new, forced fresh)
> ./gradlew :lifecycle:lifecycle:test --rerun-tasks        # RAN (defensive, beyond the allow-list — the DURb precedent; the rig ITs form under the new mint) — GREEN: 56
> ./gradlew :integration:integration-zigbee:spotlessApply  # RAN — clean
> ./gradlew check                                          # Nick's gate — NOT run; CI on the pushed commit is the gate of record
> ```
> Change: **9 files + MODULE_CONTEXT** — 4 main (`NetworkParameterStore`, `PersistentNetworkParameterStore`, `NetworkFormation`, `EzspCoordinatorProtocol`) + 5 test (`NetworkFormationTest`, `PersistentNetworkParameterStoreTest`, `EzspProtocolTest`, `ZigbeeTrustCenterJoinTest`, `RecordingNetworkParameterStore`). **ZERO:** event mints (73/43/55 hold) · schema · module-info (pinned, held) · build files · config-module (G3-frozen, untouched) · public surface. New log token: the DP-10 posture INFO only (`zigbee.tc_link_key_posture: posture={generated_seed|well_known} context={formation|restore}`); all frozen tokens byte-untouched (`network_formed`/`network_resumed`/`network_restored_from_parameters`/`network_parameter_mismatch`/`network_key_missing`/`tc_joins_enabled` verified in the diff); `enablePreconfiguredKeyJoins()` BYTE-UNTOUCHED (no diff hunk).

**Coder (Claude Code).** Built per `context/instructions/2026-07-10_M9.6-SEED_generated-seed-custody_coding-instruction.md` on the pre-verification `WU-SEED-CUSTODY.md` (read first; every §1/§2 signature re-verified first-hand at `04f5f70`). **Tests before implementation, red-first staged honest:** all new tests written first; stage A (seam widening + store custody + posture-agnostic pass-through, policy untouched) let them compile; the red run confirmed **9 failing for exactly the right reasons** (fresh-custody mint/atomic-batch/order absent · seed-reuse got well-known · both asymmetric arms absent · restore-with-seed struct carried the ASCII well-known bytes · formation struct carried well-known · posture INFO absent on the restore-without-seed leg · the TCJ DP-8 leg's struct carried well-known) with the regression pins green by design (NVRAM no-frame/no-INFO · restore-without-seed=well-known · pre-seed custody loads empty · store round-trips — the data layer IS stage A · `form_existingKeyReused`); stage B (the `NetworkFormation` policy) turned all 440 green. **In-session 4-lens adversarial audit over the final diff** (DP-compliance · frozen-pins · INV-SE-03 · test-honesty): DP-compliance and frozen-pins lenses CLEAN; three REVIEW-class test-coverage findings on the instruction's INV-SE-03 sweep row — the exception-message surface was uncovered, the log sweep bound only the formation logger, the TCJ leg went unswept — **all three FIXED in-session** (both PIE legs now run with a seed in custody + sweep messages both casings; the EzspProtocolTest appender now captures formation AND protocol loggers; the TCJ leg sweeps its captured ingestion lines). Handed back **gate-targeted-GREEN / full-check-deferred**.

**What landed (DP by DP):** DP-1/DP-4 `form()` resolves key+seed load-or-mint (reuse, never re-mint), both-minted rides ONE `saveNetworkKeyAndTclkSeed` (→ `SecretStore.setAll`, AMD-68 never-torn), asymmetric custody persists only the missing half, every minted secret durable BEFORE `ops.formNetwork`, params after success (cross-object order journal test-pinned: `saveNetworkKeyAndTclkSeed → formNetwork → save`). DP-2 fixed ref `TCLK_SEED_REF="zigbee.tclk_seed"` hex-inside the data-dir SecretStore; `zigbee-network.json` unchanged; PRESENCE is the posture marker. DP-3 seam +3 package-private methods, INV-SE-03 restated, `saveNetworkKey` not overloaded. DP-5 `CoordinatorOps.formNetwork` +`trustCenterLinkKey`; protocol posture-agnostic (`encodeInitialSecurityState` writes the handed key at [2..17], both keys length-validated); `TC_LINK_KEY` private→package-private; bitmask/network-key/struct BYTE-UNTOUCHED (0x1B84 LE byte-asserted). DP-6 fresh formation passes the seed (the only minting arm). DP-7 restore AS-FORMED (seed present ⇒ seed; absent ⇒ well-known; never a mint; NVRAM arm untouched + regression-pinned); the recorded scope limit (seed-only custody loss indistinguishable from well-known custody — FRAME-CTR closes it) in `resume()`/`loadTclkSeed()` javadoc. DP-8 `enablePreconfiguredKeyJoins` byte-untouched; the seed NEVER rides `importTransientKey` (adapter-level pin: seed in custody ⇒ struct carries the seed AND the transient key is the well-known literal). DP-9 zero policy-constant changes (javadoc note: 0x51 send-current serves seed-derived TCLKs, the bellows posture). DP-10 the ONE posture INFO, both arms, no material ever logged. DP-11 no migration code — the bench resumes untouched.

### Deviations (severity-honest)
- **`[INFO]` the DP-4 batch method lives ON the seam** (`saveNetworkKeyAndTclkSeed`, a third method beyond DP-3's two named): `NetworkFormation` never touches `SecretStore`, so seam-level batch persistence is the only way `form()` can batch-persist — the instruction's own test row ("fake/spy store asserts ONE atomic batch") presupposes it. Within the pre-flagged DP-4 mint-block latitude.
- **`[INFO]` `TCLK_SEED_REF` placed in `PersistentNetworkParameterStore`** (beside `KEY_NAME_PREFIX`), not `NetworkFormation`: the fixed-ref store methods mean only the store impl names the secret. Pinned by an independent literal + an independent same-dir `SecretStore.list()/resolve()` read in the round-trip test.
- **`[INFO]` `INITIAL_SECURITY_BITMASK` javadoc trailing note amended** (the ruled v18 beat-2 election quote BYTE-UNTOUCHED): "the preconfigured key stays ZigBeeAlliance09" was true pre-SEED and is now false — rewritten to the SEED posture, resolving the M9.4b [REVIEW] bellows generated-seed re-derivation note (we now match that reference posture).
- **`[INFO]` `form_existingKeyReused` re-scoped to full custody** (key + seed seeded) to preserve its no-fresh-generation intent under the new mint block; the key-present/seed-absent case became its own DP-4 asymmetric test.
- **`[INFO]` real-store `setAll` teeth limit (audit finding, recorded not widened):** the ONE-atomic-batch assertion lives at the seam fake per the instruction's row; the real store's `setAll` usage is source-verified but not test-distinguishable from two `set()` calls without an unsanctioned `SecretStore` injection seam on the store ctor. A regression to torn writes is undetectable by the suite — flagged for a possible future teeth row.
- **`[INFO]` `:lifecycle:lifecycle:test` run beyond the allow-list** — defensive (the rig ITs now form under the seed mint); GREEN, zero edits.

### NEXT WU (refuse-to-close pointer)
- **CI on the pushed commit = the gate of record.** Then per Nick's session note the M9.6 arc continues: **M9.6-AVAIL next, then M9.6-RO** (coding instructions expected from the PM). The **SEED silicon leg** rides post-soak in Monday's pinned order — a FRESH formation under a generated seed, both bench devices joining, the SNZB 0x06→0x34 TCLK arc (exactly one `key_established`, zero failed), posture INFO reading `generated_seed`; ONE re-formation can serve this leg AND the Wave-2 joins together (Nick rules the timing at close-out). **The SD-5 HARD GATE discharges only when that leg passes** — until then, no non-bench network forms.
- Carried candidates unchanged: 0x81/0x82 mains-with-backup ruling, IAS dedup posture, StandardExplanationService verdict flattening, the test-javadoc BENCH-VERIFY vocabulary micro-pass, + NEW: the real-store `setAll` teeth row (optional, see [INFO] above) and FRAME-CTR custody schema (closes the DP-7 recorded limit).

---

## M9.5-DURb — Half-Registration Repair + the Zigbee Seam Pack — DELIVERED (2026-07-09)

> **DEFERRED BUILD GATE (Nick):** full `./gradlew check` was NOT run in-session. The instruction's CC-lane targeted allow-list RAN in-session and is **GREEN** against the M9.5-DURb working tree (built on core `ef1d2c5`, baseline EXACT — G-DB1–G-DB6 all verified PASS first-hand pre-code):
> ```
> ./gradlew :integration:integration-zigbee:test :core:device-model:test :app:homesynapse-app:test --rerun-tasks
>                                                      # RAN — GREEN: 429 + 418 + 19 = 866 tests, 0 failures, 0 skipped (forced fresh)
> ./gradlew :lifecycle:lifecycle:test                  # RAN (defensive, beyond the allow-list — the adapter-sensitive ITs) — GREEN
> ./gradlew :integration:integration-zigbee:spotlessApply :app:homesynapse-app:spotlessApply   # RAN — clean
> ./gradlew check                                      # Nick's gate — NOT run; CI on the pushed commit is the gate of record
> ```
> Change: **10 files** — 5 main (`ZigbeeAdoptionSlice`, `ZigbeeIntegrationAdapter`, `ZigbeeCommandHandler`, `EzspCoordinatorProtocol`, `EzspReportingOps`) + 4 modified test (`ZigbeeAdoptionSliceTest`, `ZigbeeCommandHandlerTest`, `ZigbeeConfigAcceptedAdoptionTest`, `ZigbeePermitJoinTest`) + 1 NEW app test (`RegistryApplyCensusTest`) + the two MODULE_CONTEXT WUCP updates. **ZERO:** event mints (73/43/55 hold) · schema · module-info · build files · public API. New log token: the DP-B1 WARN only. All frozen tokens byte-untouched (`device_relinked`/`device_adopted`/`proposal_accepted`/`reporting_configured`/`key_established` + the `adoption_maps_rehydrated` format string — only its LEVEL moved, per DP-B3).

**Coder (Claude Code).** Built per `context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md` on Nick's routing ruling (a) — land before the soak, after 5b. STOP gates G-DB1–G-DB6 verified first-hand at `ef1d2c5` before any code (G-DB5 interpreted on the operative production census — see [INFO] below). **Tests before implementation, red-first staged honest:** all 10 new behavioral tests written first; the ONE data-only stage (the `ZigbeeCommandHandler` ctor widening, the KEYb compile-stage idiom) let them compile; the red run confirmed 7 failing for exactly the right reasons (LINKED-instead-of-PROPOSED · no-proposal ISE on the repair adopt · silent fence where a verdict belongs · characterization-verdict where installed-kept demands silence · INFO absent at both counts · stale-true deadline) with the 3 regression pins green by design; the implementation turned all 866 green. Handed back **gate-targeted-GREEN / full-check-deferred**.

**What landed (§ by §):**
- **§1 (DP-B1/DP-B2) — the repair:** the LINKED arm requires ≥1 registered entity; the entity-less case logs ONE WARN `zigbee.half_registration_detected: device={} deviceId={} — re-proposing for repair` and falls through to propose (no early return; registry reads outside the slice lock). `adopt()` reads `findByHardwareIdentifier` BEFORE minting and REUSES the durable deviceId — the `device_registered` re-emit is idempotent-by-identity (AMD-99 F1; test-proven upsert, no duplicate device, fresh entity ids, registries whole). The stale `publishRegistration` known-gap javadoc rewritten to the REPAIRED state (scope limit recorded: partial multi-entity still LINKs — Wave-1 single-entity).
- **§2 (DP-B3):** `adoption_maps_rehydrated` DEBUG→INFO, UNCONDITIONAL (count 0 prints; tests pin presence VERBATIM at devices=0 AND devices=2). Token frozen.
- **§4 (DP-B5):** a successful `attemptReopen()` clears `permitJoinDeadline` after `resumeStored()` — `isPermitJoinActive()` can no longer read stale-true past a reopen (regression-pinned via a two-channel reopen leg in `ZigbeePermitJoinTest`); a failed reopen leaves it (backoff domain), per the DP as ruled.
- **§5 (DP-B6):** `ZigbeeCommandHandler` ctor +`EntityRegistry` (package-private; adapter wires the read-only `context.entityRegistry()`); the fence reads the INSTALLED `CapabilityInstance.confirmation()` FIRST — installed-DISABLED ⇒ dispatch + immediate honest UNCONFIRMED + never tracked (the M9.4-RPT posture-DISABLED silent class CLOSED); kept policy ⇒ no immediate verdict on the tuned timeout, EVEN against an UNCONFIRMABLE characterization (installed wins — test-pinned precedence); characterization = the pre-adoption/no-entity fallback (regression-pinned); `INHERENTLY_UNCONFIRMABLE` untouched. Both pre-existing identify fence tests pass BYTE-UNTOUCHED (the reason ladder preserves their verbatim reasons).
- **§3 (DP-B4):** all 18 + 3 BENCH-VERIFY clauses flipped to silicon-verified statements citing the bench record (it-1 · it-2 · it-4 · it-5a · it-5b; citation lines copied, not invented). Constants BYTE-UNTOUCHED — the git diff on the two files is comment/javadoc-only; **grep-assert: `BENCH-VERIFY` count = 0 in both files and 0 across src/main + testFixtures.** The isolated constants-block structure stays.
- **§6 (DP-B7):** `RegistryApplyCensusTest` (app tree, beside `RegistryMutationRuleTest`, same idiom) pins production `RegistryProjection.apply*` callers to EXACTLY the sanctioned three; the named `UnpinnedApplyCallerFixture` + bite leg prove it detects an unpinned caller on every build.

### Deviations (severity-honest)
- **`[INFO]` G-DB5 census scope:** the literal "zero elsewhere in the module" holds for src/main + testFixtures (18+3 exact); the TEST tree carries 6 comment-level references to the BENCH-VERIFY *discipline* (4 files) and MODULE_CONTEXT.md carries historical mentions — not flip targets (test javadoc describes the symbolic-binding model, not unverified claims), left untouched per the instruction's two-file flip scope. If the hub wants the test-javadoc vocabulary refreshed too, it is a comment-only micro-pass.
- **`[INFO]` §A.3 transient-key self-expiry flip is honesty-bounded:** the bench record never directly observed expiry (it-1 recorded it "unobserved"; no later iteration measured it) — the flip states the enablement+join arc evidence and records the direct observation as unmeasured with the clear-on-window-close contingency standing. No citation was invented.
- **`[INFO]` installed-DISABLED reason ladder (coder's choice within DP-B6's "recorded reason"):** UNCONFIRMABLE-characterization note → SD-3 no-surface generic (inherently-unconfirmable commands) → the new installed-policy generic. Keeps both frozen-behavior identify pins verbatim; a §3 posture-downgraded actuator renders the installed-policy generic (the measurement lives in the downgrade WARN + the posture fact). A CONFIRMABLE characterization's note is deliberately NOT used as a DISABLED reason (it describes a confirmable surface — misleading).
- **`[INFO]` `:lifecycle:lifecycle:test` run beyond the allow-list** — defensive regression over the adapter-sensitive ITs (the M9.4-RPT precedent); GREEN, zero edits.

### NEXT WU (refuse-to-close pointer)
- **CI on the pushed commit = the gate of record**, then **the M9.4 ACCEPTANCE RUN** (hub-dispatched operator protocol): DURb's silicon confirmation PIGGYBACKS on the acceptance-run entry boot — the always-print `zigbee.adoption_maps_rehydrated: devices=2` INFO at boot + a normal adoption; no dedicated bench iteration is owed. The 5b closing legs (L1 physical pin-2 re-link · L2 entity-ULID continuity API-to-API) ride the same session, then the **72 h soak** (NQ-6 is this fix's natural test bed, per the ruling).
- Carried candidates unchanged: hashed-TCLK generated-seed custody (SD-5 HARD GATE), 0x81/0x82 mains-with-backup ruling, IAS dedup posture, StandardExplanationService verdict flattening, the test-javadoc BENCH-VERIFY vocabulary micro-pass (new, optional — see [INFO] above).

---

## M9.5-DUR — Registries as Projections of the Event Log (AMD-99) — DELIVERED, code half (2026-07-08)

> **DEFERRED BUILD GATE (Nick):** full `./gradlew check` was NOT run in-session. The instruction's CC-lane targeted allow-list RAN in-session and is **GREEN** against the M9.5-DUR working tree (built on core `e15858d`, baseline EXACT — G-DUR1..G-DUR7 all verified PASS pre-code):
> ```
> ./gradlew :core:event-model:test :core:device-model:test :core:persistence:test :lifecycle:lifecycle:test :integration:integration-zigbee:test :app:homesynapse-app:test --rerun-tasks
>                                                      # RAN — GREEN: 507+418+412+56+419+17 = 1,829 tests, 0 failures (forced fresh)
> ./gradlew <touched-6>:spotlessApply + spotlessCheck  # RAN — clean
> ./gradlew check                                      # Nick's gate — NOT run; CI on the pushed commit is the gate of record
> ```
> **ONE `[REVIEW]`-class module-info diff, flagged loudly (the instruction pinned zero):** `core/device-model/module-info.java` — `requires com.homesynapse.event` PROMOTED to `requires transitive` (one word). Unavoidable under `-Xlint:exports -Werror`: `RegistryProjection`/`RegistryEventMapper` name `DeviceRegisteredEvent`/`EntityRegisteredEvent`/`DeviceRemovedEvent` on PUBLIC methods of the exported package — the exact three-precedent promotion class (M2.9/M3.6e.1/M5-A; the M9.1 `List<IntegrationFactory>` and M9.4b `DeviceRegistry` promotions in lifecycle are the verbatim precedent comments). Gradle was ALREADY `api(":core:event-model")` — the lockstep needed only the JPMS word. The alternatives (package-private + Object-typed gateway; @SuppressWarnings("exports")) were rejected as contract-obscuring; evidence in the module-info comment. **PM ratification requested before this WU is considered closed.**

**Coder (Claude Code).** Built per `context/instructions/2026-07-08_M9.5-DUR_registries-as-projections_coding-instruction.md` against ratified AMD-99 (read in full). STOP gates G-DUR1–G-DUR7 ALL PASS at `e15858d` (10-agent verification fan-out + first-hand reads; G-DUR2 mutator census = exactly the four sites, no G2 sizing signal). Tests authored before implementation per module; red-first caught one real defect in-flight (the `?:` autoboxing mis-canonicalization — coder-lessons 2026-07-08). Handed back **gate-targeted-GREEN / full-check-deferred**.

**What landed (24 files: 15 main + 9 test/fixture, across event-model / device-model / persistence / lifecycle / integration-zigbee / app):**
- **Event-model (+10 public types; 71→73 constants, 41→43 manifest):** `DeviceRegisteredEvent` + `EntityRegisteredEvent` (@EventType, full-fidelity per AMD-99 §3 — `integrationId` as Crockford String per the ratified contract; ids raw Ulid, LTD-04 wrappers reconstruct at apply) + the mirror family (`HardwareIdentifierRef`, `CapabilityInstanceRef`, `AttributeSchemaRef`, `CommandDefinitionRef`, `ParameterSchemaRef`, `ExpectedOutcomeRef`, sealed `ExpectationRef` w/ 4 nested permits, `ConfirmationPolicyRef` — NONE @EventType/DomainEvent, test-pinned by `mirrorsAreNotEvents`) + package-private `PayloadMirrors` (Number canonicalization to the Jackson-native Integer/Long/Double pair + sorted-collection determinism — the Watch-Out #3 resolution, documented in javadoc + MODULE_CONTEXT).
- **Device-model (+2 public):** `RegistryProjection` (THE single apply path: DP-8 upsert-in-log-order, DP-7 tombstone incl. entity cascade + no-op-on-absent + clean resurrection, ReentrantLock/LTD-11, publishes nothing per INV-ES-09, deliberately log-silent — no SLF4J edge in device-model) + `RegistryEventMapper` (toPayload×2/toDevice/toEntity, exhaustive Expectation switches both directions, unknown enum name → loud IAE → bus DLQ path, helper-entity rejection at emission).
- **Persistence:** +2 `EventCategoryMapping` rows (device-family pattern beside DEVICE_ADOPTED), `ExpectationRefSerializer/Deserializer` (the AMD-87 pattern, IDENTICAL wire tags — stable even across mirror renames), registered in `PersistenceJacksonModule`. Pins moved in lockstep: `EventTypesTest` 71→73, `EventTypeAnnotationTest` +2 records & 41→43, `EventTypeRegistryTest` 41→43(×2)+53→55, `JacksonWarmupTest` 53→55 & 41→43, `EventCategoryMappingTest` 53→55 (the two beyond the instruction's list = the R-E "et al."). `AllEventClasses` needed ZERO code edits (aliases the manifests; stale "37 core" javadoc corrected). `RegistrationEventSerdeTest` = deep-fixture round-trip (2 capabilities, all 4 ExpectationRef permits, canonical Integer/Double Numbers, byte-identical re-encode) through the production registry→warmup→codec path.
- **Lifecycle:** `RegistryProjectionSubscriber` ("registry_projection"; `forTypes` filter over the 3 types; tombstone DeviceId from the envelope SubjectRef; DegradedEvent → loud WARN `registry.projection_degraded_event`; the anti-vacuous `registry.projection_live: devices={} entities={} position={}` INFO on `onCaughtUp()` — the Subscriber interface has NO `onLive`; onCaughtUp IS the AMD-42 TRANSITION→LIVE once-per-lifetime callback, an [INFO] naming deviation). HomeSynapseCore Step 3.1: projection construction + **checkpoint reset-to-0 BEFORE subscribeRuntime** + registration (`coalesceExempt=false, atomicCheckpoint=false` per instruction); `awaitRegistryProjectionLive()` beside the existing await BEFORE Step 3.4 (the ONE sanctioned composition-root addition, C3); paired teardown BOTH branches (graceful: unsubscribed LAST in the reverse chain; abandon: bulk `eventBus.abandon()`); PUBLIC `registryProjection()` accessor (deliberately unguarded — resolves mid-start at Phase 6; javadoc'd). **`ProcessRestartIdentityIT`** (beside RestartHonestyIT): 3-boot arc over ONE dbPath — live-apply leg, full process-restart identity leg (same deviceId/entityIds, EQUAL tuned records incl. the DP-a ConfirmationPolicy, findByHardwareIdentifier resolves, the projection_live INFO exactly once with devices=1 entities=2), tombstone leg (device + entities gone post-rebuild). Lifecycle test tree gains `testImplementation(libs.logback.classic)` + `logback-test.xml` (the integration-zigbee F-12 precedent) for the INFO capture.
- **Integration-zigbee:** `adopt()` publish-then-apply per DP-3 (device_registered → apply → entity_registered×N → apply → maps/listener → device_adopted LAST byte-unchanged; the `zigbee.device_adopted` INFO untouched); NEW write-ahead-STRICT `publishRegistration` (a SequenceConflict — impossible on fresh subjects — aborts via ISE into the cycle's `interview_failed` WARN, never an apply of an unpersisted event; the log-and-continue `publishRoot` untouched for the other three event types). `reinstallOverrides` DELETED outright (DP-4) + the relink pin-2 fence javadoc REWRITTEN (fence REALIZED by AMD-99 + DP-6). `applyPostureRouting` re-emits `entity_registered` via the projection (publish-first; conflict → ONE WARN `zigbee.posture_reemit_conflict` + skip-apply; `updateEntity` gone). DP-6 `rehydrateAdoptionMaps()` at `initialize()` over the now-package-private `relink(ieee, device, cache-profile-or-null)` (profile id from `zigbee-devices.json`; DEBUG `zigbee.adoption_maps_rehydrated: devices={}`). Slice/adapter ctors +RegistryProjection; factory PUBLIC ctor now 4-arg with `Supplier<RegistryProjection>` resolved at `create()` (the R4 shape, PIE `zigbee.registry_projection_unbound` on null); `Main` wires via a one-element holder; the rig ctor widened (3 lifecycle IT call sites updated). Tests: +`adoptEmitsRegistrationFactsInCausalOrder` (order + full-fidelity payload≡registry + subject refs), `relinkNeverMutatesTheRegistry` REPLACES the pre-DUR reinstall pin (registry byte-identical across re-link; maps rebuild; idempotent), +DP-6 rehydration leg in ZigbeeConfigAcceptedAdoptionTest (resolves with NO announce; never adopts). Suite 419.
- **App:** `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` (Rule 11) — custom ArchCondition over the 8 mutator names × both registry interfaces (assignability covers the InMemory impls); sole production exclusion `RegistryProjection`; wired into `checkAll` + the @ArchTest sweep (rides `./gradlew check`/CI from day one, R-B). Teeth are PERMANENT, not scratch-and-remove: `RegistryMutationRuleTest` holds a named violating fixture (excluded from the sweep BY NAME, rationale in the rule comment) and asserts the un-excluded condition REJECTS it on every build + ACCEPTS the projection. The codebase-wide sweep passing IS the census proof that zero direct mutators remain.

### Deviations (severity-honest)
- **`[REVIEW]` device-model module-info promotion** (top block) — the one instruction-pinned constraint that could not hold under `-Werror`.
- **`[REVIEW]` public-surface widenings the wiring required:** `ZigbeeIntegrationFactory` public ctor 3→4 arg; `HomeSynapseCore.registryProjection()` public accessor; `ZigbeeHardwareFreeRig` public ctor; lifecycle `build.gradle.kts` +test-scope logback. All realize the DP-3 "follow the R4 injection path" mandate; no frozen surface moved (IntegrationContext untouched, CoordinatorProtocol untouched).
- **`[INFO]` DP-6 rehydration publishes one `availability_changed` per rehydrated device at adapter `initialize()`** — the direct consequence of "invoke the EXISTING relink" (relink carries the availability publish). Bench-visible at 5b as N availability lines at boot; if unwanted, splitting relink into map-rebuild vs publish is a small follow-up — flagging rather than improvising.
- **`[INFO]` the instruction's "onLive hook" is realized as `Subscriber.onCaughtUp()`** (no onLive exists; semantics identical).
- **`[INFO]` Number canonicalization at the mirror boundary** — registry-held Numbers post-apply are the canonical Integer/Long/Double pair; StandardCapabilities-held schemas untouched; no existing assertion compares across that boundary (verified).
- **`[INFO]` `entity_registered` carries an ENTITY SubjectRef** → the state projection's else-branch bumps that entity's stateVersion on delivery (cursor semantics, intentional per existing design — the S-AGG survey item-7 caveat, recorded).

### Post-delivery adversarial review (3-lens fleet + refutation verify, run in-session over the final diff)
Five findings survived verification; three fixed in-session, two routed: (1)+(2) [FIXED] the mandated "re-emit published" half of the posture-routing test was missing (an apply-without-publish regression would have passed the suite and silently reverted measured downgrades on the next restart) — T5 now pins `ENTITY_REGISTERED ×2` AND the re-emit payload rebuilding the routed entity; (3) [FIXED] the mapper round-trip fixtures left every nullable Device/Entity component null (a silent drop or a same-typed adjacent-field swap in `toPayload` would have passed) — both round-trips now populate EVERY nullable with distinct values + pin them individually; (4) [ROUTED = the module-info [REVIEW] above] — the reviewer independently confirmed it needs explicit ratification; (5) **[REVIEW — NEEDS A PM RULING] the durable half-registration gap:** if adoption aborts (or the process dies) between the durable `device_registered` and its entities' publishes, the log permanently carries an entity-less device — every re-announce takes the LINKED arm (no proposal ever re-mints, no re-adopt arm exists, no `device_removed` emitter exists), and boot replay faithfully rebuilds the same state. Pre-DUR this window self-healed on restart (in-memory registry vanished); post-DUR it is permanent. This is a design gap AMD-99 INHERITS from DP-3's non-atomic sequence, not a coding slip — candidate repairs: a LINKED-with-zero-registry-entities + COMPLETE-interview re-propose arm, or a compensating tombstone emit. The falsified pre-DUR recovery javadoc on `publishRegistration` is corrected in-place with the gap recorded; the arm itself is NOT improvised. Suggest ruling before/at 5b (the window is a process-death race — unlikely on the bench, real in the field).

### NEXT WU (refuse-to-close pointer)
- **ITERATION 5b — the real WU-done gate (bench; the operator protocol is dispatched separately at 5b entry):** one events-DB wipe (custody `data/zigbee/` preserved) → re-adopt ×2 under the new flow → RESTART → the SAME deviceIds/entityIds resolve (`registry.projection_live: devices=2 ...` is the boot glance-point) → the Hue re-announce takes the RE-LINK arm (`onRejoin` fires) → `/api/v1/entities` shows exactly the live entities → the KEYb piggyback (exactly one `key_established`, zero `key_establishment_failed` per fresh join). UNIT-GREEN ≠ WU-DONE.
- Carried candidates unchanged: hashed-TCLK generated-seed custody (SD-5 HARD GATE), 0x81/0x82 mains-with-backup ruling, IAS dedup posture, StandardExplanationService verdict flattening, SD-3-fence-reads-installed-policy polish row, BENCH-VERIFY javadoc flip micro-pass.

---

*Entries older than M9.5-DUR were rotated 2026-07-10 (v27 hub, beat 2 — the chartered sweep) to `archive/coder-handoff-rotated-2026-07-10.md`: M9.4-KEYb · M9.4-KEY · M9.4-RPT · M9.4-ADP · M9.4-NCFG · M9.4-TCJ · M9.4-PJ · M9.4b · M9.4a · M9.3 · M9.2 · M9.1 · M7.5c-a · AB-4 · M7.5b · M7.5a · M7.4a–d · M7.3 · M7.2a/b · AB-1/AB-2 (+ the pre-M7 tail). All are WUCP-closed, CI-green, and silicon-certified through the 2026-07-09/10 acceptance run; per-WU detail lives in the archive file + git history.*
