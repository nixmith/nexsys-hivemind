<!--
file: context/handoff/coder-handoff.md
purpose: Coder session continuity — current task, deferred build gate, next WU, recent closeouts.
audience: Coder, PM
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-05-27 against M3.7 closeout working tree — `./gradlew check` GREEN (139 tasks). Pending commit.
-->

# Coder Session Handoff

**Last updated:** 2026-06-11 by Coder (Claude Code) — **M6.2 (SecretStore + per-scope key-management infrastructure — AMD-68 + Doc 15 §3.5/§4.2/§7.3 + CARRY 1) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see `### DEFERRED — M6.2` below).** Baseline `62a81e6` (M6.4 landed first, per the PM's sequencing recommendation). **STOP-gates G1–G8 ALL PASS at execution** — G7 found the post-M6.4 10-arg service ctor with reload/write LIVE, so the DP-10 threading flows through BOTH load and reload via the shared `runPipeline()`; G3/G4/G5 module-infos byte-identical to the instruction embeds; G6 grep clean; G8 untouched. **Delivered — the three charter deliverables + the E2 bridge (10 files created + 11 modified + 4 MODULE_CONTEXT):** (1) **`ScopeKeyManager`** (public iface + `create(Path, Clock)` gateway, DEC-M3-16) / `StandardScopeKeyManager` (package-private): lazy 256-bit `SecureRandom` root key at `.root-key` — written via same-directory temp + ATOMIC_MOVE so a crash mid-write can never leave a short/zero key file in place (self-review-driven hardening); the temp is created `rw-------` → write+fsync → chmod `r--------` 0400 (carried by the rename), best-effort + WARN on non-POSIX (DP-1; you cannot write a file created 0400, hence the two-step); scope KEK = hand-rolled RFC-5869 `Hkdf.derive(root, null, "scope:"+scopeId, 32)` (DP-2 — JDK 21 has no HKDF API; A.1/A.2 vectors pinned in `HkdfTest`, locally re-verified against an independent implementation before pinning), cached per scope, never stored; scope DEKs (256-bit, AES-256-GCM-wrapped, fresh 96-bit wrap IV) in the config-side **`scope_keys.json`** ([R6]/DP-3 — JSON array of the Doc 15 §8.2 `ScopeKey(scopeId, keyVersion, encryptedDek, iv, createdAt, destroyedAt?)` rows, Base64 byte arrays + hand-encoded ISO instants because the module deliberately gains NO jsr310 requires; written via the M6.4 `AtomicYamlWriter` temp-fsync-rename discipline; plaintext-correct — DEKs are wrapped); key versions NEVER reused past destroyed rows; decrypt of absent/destroyed (scope,version) → `IllegalArgumentException` (the §8.2 unreadability property, freeze-gate test GREEN by construction), GCM auth failure → `IllegalStateException`; one `ReentrantLock` over root/caches/store, GCM payload ops outside it, per-call `Cipher` (LTD-11). New public records `ScopeCipherResult(ciphertext, iv, keyVersion)` + `ScopeKey` — NO defensive copies per DP-4, documented. (2) **`StandardSecretStore`**: `secrets.enc` = plaintext JSON envelope `{keyVersion, iv, ciphertext}` over AES-256-GCM of the §4.8 `{"version":1,"entries":[...]}` document under the **`config_secrets` scope DEK** (Doc 15 §7.3 shared root — ONE key system; the legacy `.secret-key` design superseded before first release, no migration); `SecretStore` 4→5 methods — **`setAll(Map)`** with the AMD-68 §2.1 Javadoc verbatim (all-or-nothing + durable-before-return via temp-fsync-rename-fsyncdir, AMD-68-INV-01), `set` delegates to `setAll`, + `create(Path, ScopeKeyManager, Clock)` gateway; AMD-16/DP-9 backups `secrets.enc.bak.{N}` (N monotonic 1-indexed, max 5 retained oldest-pruned, none on first-ever write, backup-copy failure fails the mutation closed); `resolve` missing → `IllegalArgumentException` naming the KEY only (the decision-#7 pin: REUSE IAE, no new type; LTD-15); decrypted store lives per-operation only, plaintext byte[]s zeroed best-effort (§3.4). (3) **Stage-3 tag resolution (DP-10):** `YamlLoader` now has TWO ctors — the resolving `(Path, SecretStore, Function<String,String>)` form used by `runPipeline()` (load AND reload): `!secret <key>` → `resolve`, `!env VAR` / `!env VAR:default` (default = everything after the FIRST colon, may be empty/contain colons), resolution in root AND included documents, missing key/unset var → FATAL naming the key/var only, **collected across the whole parse** (every failure reported in one pass — the faithful "collected, not thrown mid-parse" reading); and the one-arg NON-resolving form kept for `writeLocked()`'s re-parse — **tag-bearing documents are rejected by `write()` fail-closed** (the mutated tree is re-emitted; resolving first would bake plaintext secrets into the file — INV-SE-03/§12.3; [REVIEW] R-1 below). Service ctor 10→12 args (`secretStore`, `envLookup` appended; composition root passes `System::getenv`). **E2 bridge (CARRY 1, zero new edges):** persistence defines `PayloadCipher` + `EncryptedPayload` (java.base-only signatures, DP-5; NO persistence code calls them — M6.3 consumes); `Main.payloadCipher(Path, Clock)` (package-private static — DP-6 "not a new public type") builds the `ScopeKeyManager` and adapts; `HomeSynapseCore` gains the 5-arg ctor + final nullable `payloadCipher` field Javadoc-marked for M6.3 (4-arg ctor delegates null — every existing caller incl. `HomeSynapseE2eHarness` compiles unchanged). **Zero-new-edge re-verified in both directions: NO module-info/`build.gradle.kts`/`libs.versions.toml`/`projectionVersion`/SQLite-schema/event-manifest change in ANY module.** **Tests FIRST (6 new classes + 7 extended/adapted):** `HkdfTest` (RFC A.1/A.2 PRK+OKM separately + scope-info divergence + length cap), `ScopeKeyManagerTest` (round-trip, fresh-IV, cross-scope GCM auth failure, lazy creation INV-CE-02, fresh-manager durability, keyVersion persisted+honored, corrupt root key fail-closed, destroyed/absent unreadability via direct store manipulation — the §8.2 freeze gate, 0400 row `Assumptions`-guarded per the ConfigLayoutTest precedent), `SecretStoreTest` (ALL AMD-68 §5 rows verbatim-named incl. `setAllIsAllOrNothingOnFailure` via the deterministic `secrets.enc.tmp` directory-squat injection + `setAllDurableBeforeReturn` as the REC-140 fresh-store restart; rotation: 6th post-seed mutation creates bak.6 + deletes bak.1; backups restorable by direct decrypt; createdAt-preserved/updatedAt-refreshed; envelope-no-plaintext INV-SE-03), `YamlLoaderSecretEnvTest` (root+included resolution, LTD-15 no-value assertions, multi-failure collection, env default forms, CoreSchema `~` regression pin, nested-include still FATAL, REAL-machinery zero-touch INV-CE-02, write-path-form rejection ×2), `StandardConfigurationServiceSecretsTest` (load resolves into the model + on-disk doc still carries tags; **reload re-resolves** — the M6.4-sequencing obligation; write() on a tag-bearing doc → `ConfigurationValidationException`, file byte-identical + model untouched; missing key fails load naming key only; service-level zero-config no-key-files), `PayloadCipherBridgeTest` (app — the charter done-when: encrypt→`EncryptedPayload`→decrypt round-trip through the REAL adapter, fresh-adapter durability = the (store) leg, scope isolation through the bridge), `HomeSynapseCoreTest` +`constructorAcceptsPayloadCipherSeam` (existing lifecycle tests stay GREEN via the 4-arg delegating ctor). All new tests `Clock.fixed`-injected. The 12 pre-existing `new StandardConfigurationService(` sites across ServiceTest/ReloadTest/WriteTest/ConfigLayoutTest updated with a per-file lazy `noSecrets()` helper + `key -> null` env (real machinery, zero file-touch — ConfigLayoutTest's layout rows stay byte-identical). `YamlLoaderTest.secretTagIsFatalUntilM62` renamed/retargeted to the write-path form. **Deviations: 3×`[REVIEW]`** (R-1 the write-path tag rejection above — Doc 06 §3.5 UI/API mutation of tag-bearing documents is now impossible until a tag-preserving emitter exists; fail-closed is the only INV-SE-03-compatible choice; propose a post-MVP AMD. R-2 DP-6's "passes it to the persistence factory…which HOLDS it" — threading STOPS at `HomeSynapseCore`'s held field: `PersistenceFactory` is not in the Files table and extending its frozen public gateway ctor is exactly the M6.3 wiring step; the file table's own "held/forwarded (DP-6)" wording supports this. R-3 `Main.main()` is still the Phase-2 stub — there is NO live `HomeSynapseCore` construction to wire into, so DP-6's "wire" is discharged as the package-private factory + the 5-arg ctor + the app bridge test; full bootstrap rides the app-bootstrap milestone.) **`[INFO]`×10** (Completion Report; highlights: decrypt exception-type split IAE/ISE; `setAll` null-entry NPEs; backup-prune failure WARNs-and-continues — only the backup COPY fails closed; multi-failure collection beyond the single-issue !include pattern; `StandardConfigurationServiceSecretsTest` as an instruction-unnamed test class; app MODULE_CONTEXT also updated as a 4th; store format version pinned `1` fail-closed; ISO instants hand-encoded; cross-scope-auth test materializes the foreign scope first; ctor-arg helper churn in 4 test files). **Next WU = M6.3 (at-rest write-path encryption) — still TRIPLE-GATED (OQ-15-2 Pi-4 microbench + the Doc-15 co-design/AMD + this M6.2 gate landing); the PM issues the next instruction after Nick's gate (refuse-to-close).** _Prior:_ **⟳ M6.4 GATE-FIX ROUND 1 APPLIED (1 PM-diagnosed-and-ruled defect, 1 file; gate RE-DEFERRED to Nick — FULL `./gradlew check`, expect 147 tasks; see `### DEFERRED — M6.4` below):** Nick's first gate run failed exactly one test — `ConfigLayoutTest.composeAfterMerge` (:264; 169 config tests / 1 failed). PM diagnosis confirmed: NOT an implementation bug — the `rootDrafts.hasSize(1)` pin is M6.1a-era, made stale by the ratified R1/DP-10 obligation (a load with one ERROR now publishes `config_error` + `validation_completed` = 2 drafts). **GF-1 applied exactly:** pin 1→2, order-agnostic type-filtering (exactly one `ConfigErrorEvent` — path `endsWith("channel")`, severity `"ERROR"`, appliedDefault `"15"`; exactly one `ConfigValidationCompletedEvent` — `severityCounts` `containsEntry("ERROR", 1)`), model assertions kept, dated comment added; +2 imports. The implementation is UNCHANGED. Root cause owned: the M6.4 publish-behavior change swept `StandardConfigurationServiceTest` but not the module's other test classes — a module-wide `rootDrafts` grep would have caught it pre-gate (lesson logged). **PM rulings recorded: R-A/R-C/R-D ACCEPTED as implemented (no code change). R-B ACCEPTED — condition CONFIRMED against source:** `writeBackMigratedDocument` runs `copyBackup` then `writeAtomically` as sequential statements in ONE try (`StandardConfigurationService.java:888-889`, catch at :895) — a backup-copy failure skips the rename entirely (WARN + continue in-memory); the original is never replaced without its backup on disk. Defensive sweep run: no other `rootDrafts` count pin exists outside the two already-swept service test classes (grep + the 169/1 gate evidence agree). NOTE for the gate: the failed runs stopped at `:config:configuration:test` — event-model/persistence tests, compile of the new files under `-Werror`, spotless, and ArchUnit have NOT yet executed; the re-run is the FULL check. _Delivery record:_ **M6.4 (hot-reload atomic swap + UI/API write path) COMPLETE — files produced, BUILD GATE RE-DEFERRED to Nick (see `### DEFERRED — M6.4` below).** Baseline `7e0bce8` verified at dispatch; STOP-gates G1–G8 ALL PASS; rulings [R4]/[R5] applied as confirmed; the P2 consumer/pin survey RE-RUN by own grep (matches the instruction baseline + one addition: the `EventTypes` line-15 class-Javadoc count, folded in). **Delivered (7 files created + 14 source modified + 3 MODULE_CONTEXT across config/event-model/persistence/integration-tests):** `StandardConfigurationService` un-stages **`reload()`** (DP-3 §3.3 via a shared `runPipeline()` + caller-mapped `PipelineAbortException` factoring: re-parse incl. AMD-67 migration + default-merge + validation → FATAL-or-ERROR whole-candidate rejection BEFORE listeners are invoked (C5 — validation rejection thus deterministically precedes listener rejection) → `ConfigModelDiff` (DP-5: post-pipeline, key-by-key `Objects.equals`, both-sides-map keys owned by the nested section's own diff, per-key `x-reload` stamped, TreeMap-sorted sections) → DP-4 classification (registered AMD-66 listener first — throwing listener rejects, AMD-66 §4; else most-restrictive per-key `x-reload`, unannotated → PROCESS_RESTART) → DP-1 single volatile swap under the ONE `ReentrantLock` (DP-2; `load()` also serialized — the M6.1a Javadoc deferred exactly this) → one `config.section_reloaded` per changed section (DP-8/DP-9: R5 metadata, publish-failure never fails, AMD-70-INV-01) → `ReloadResult(newModel, changeSet, warnings)`) and **`write()`** (DP-6 §3.5: real-mtime token check (§6.7, unchecked `ConcurrentModificationException`; absent-file zero-config compares the model's synthetic token) → mutate the freshly RE-PARSED sparse on-disk document (INV-CE-01; null = remove key; defaults never baked into the file) → validate-the-mutated-copy (FATAL/ERROR → `ConfigurationValidationException`, file untouched) → REC-131 one-time `homesynapse.yaml.bak.{yyyyMMdd'T'HHmmss'Z'}` backup (absence-keyed, clock-stamped, fail-closed) → `AtomicYamlWriter.writeAtomically` temp-fsync-rename (§6.8 — prior file intact on failure, `UncheckedIOException`) → `reloadLocked()` under the held lock = the ONLY event leg (DP-7, no double-publish)). **DP-10/R1:** per-ERROR `config_error` publication in `load()` (never `reload()`), after each completed validation pass incl. FATAL passes before the throw; §12.4 fence via the new `SchemaAnnotationIndex` — `x-sensitive` paths publish `"[REDACTED]"`/`"(none)"`, null schema default → `"(none)"`. **DP-11/R2:** §3.7 step-7 migration write-back — `homesynapse.yaml.pre-migration-v{M}.{m}` copy FIRST (never auto-deleted), atomic replace with the declared pair stamped, `fileModifiedAt` refreshed from the NEW mtime; failure = WARN + load continues on the in-memory migration (idempotent chain re-runs next boot); zero-config/no-migration loads write nothing. **DP-8 + P2 fan-out:** `ConfigSectionReloadedEvent` (flat `java.base` payload per E70-1; guards sectionPath/classification non-blank, changeCount>=1, issueCount>=0) + `EventTypes.CONFIG_SECTION_RELOADED` + `CORE_PRODUCTION_EVENT_CLASSES` 23→24 + ALL pins: `EventTypesTest` 54→55, `EventTypeAnnotationTest` +record/24/renamed `exactlyTwentyFourAnnotatedRecords`, `EventCategoryMapping` +`[SYSTEM]` entry (sibling-consistent; PM confirms at review) + test 35→36, `EventTypeRegistryTest` 23→24×3+35→36, `JacksonWarmupTest` ×3, `AllEventClasses`/`IntegrationTestHarness`/`EventPayloadCodecTest` Javadoc counts, `TestEventSamples.configSectionReloaded()` + codec round-trip; `HomeSynapseCore` `Stream.of` verify-NO-edit confirmed. `ConfigChangeSet` +`hot()`/`integrationRestart()`/`processRestart()` (DP-5). **Tests FIRST (3 new classes + 2 extended):** `StandardConfigurationServiceReloadTest` (classification ×7 incl. listener-before-publish ordering + previous/candidate args; atomicity ×5 incl. throwing-listener/parse-FATAL/pre-load; publication ×5 incl. DP-7-only-event-type + R5 metadata + warning issueCount; pipeline edges ×5 incl. the DP-12 `~`-CoreSchema pin, zero-config, removed-key→default; the REC-133 torn-read concurrency test — reader thread over volatile `getCurrentModel()` across 50 alternating reloads, no mixed (queue_capacity, max_days) pair), `StandardConfigurationServiceWriteTest` (REC-130 round-trips ×5 incl. token refresh + null-removes-key; §6.7 stale token + file-untouched; validation rejection ×2 (no backup, no events); REC-131 backup ×3 (original content, exact clock-stamped name, once-only, zero-config skip); §6.8 ×2 — no-`.tmp`-residue + the tmp-DIRECTORY-squat failure injection leaving the original intact), `ConfigSectionReloadedEventTest` (DP-8 guards + `@EventType` + 4 components), existing `StandardConfigurationServiceTest` EXTENDED (+`ConfigErrorPublicationTests` ×6, +`MigrationWriteBackTests` ×6 incl. crash-window backup+original recoverable + second-load-no-migration idempotency; the two M6.4 staging tests REMOVED — their `UnsupportedOperationException` assertions are now false; `publishedEvent()` made type-filtering since DP-10 adds `config_error` drafts to ERROR loads). **NO `module-info`/`build.gradle.kts`/`projectionVersion`/`libs.versions.toml` change in ANY module** (the Success-Criterion property; new third-party API surface — snakeyaml-engine `Dump`/`DumpSettings`/`FlowStyle` — javap-verified in the cached 2.9 jar incl. the module-info `exports …v2.common`, found at `META-INF/versions/11/`). **Deviations: 4×`[REVIEW]`** (R-A backup file name `homesynapse.yaml.bak.*` per the AMD-71 root-document name vs DP-6's Doc-06-era literal `config.yaml.bak.{ISO8601}`, + ISO-8601 BASIC stamp — extended-format colons are illegal on Windows; R-B migration write-back failure = WARN + continue on the in-memory migration rather than failing startup — DP-11 pins the file-state invariant (held) but not the load outcome; R-C a post-flush listener rejection inside `write()`'s reload leg surfaces as `IllegalStateException` wrapping the `ConfigurationReloadException` — `write()` cannot throw the checked reload exception; file stays written, model stays prior; R-D the diff attributes nested-leaf changes to the IMMEDIATE containing section only — a listener at an ancestor path is NOT consulted for nested-leaf-only changes; M9 listener-registration guidance needed), **`[INFO]`×13** (Completion Report; highlights: load() under the DP-2 lock; rejection-precedes-listeners reading of C5; sorted event order; `issueCount` = the reload's global warning count on every section event; `x-reload` parse accepts `-`/`_` any-case, unknown → WARN + PROCESS_RESTART; §6.8/backup failures surface as `UncheckedIOException`; reload does not rewrite the `schemas/` cache). _Prior:_ **⟳ M6.1a GATE-FIX ROUND 1 APPLIED (2 PM-diagnosed-and-ruled defects; gate RESOLVED-GREEN `9035110` — see `### DEFERRED — M6.1a` below): (1) `YamlLoader` now sets the YAML 1.2 `CoreSchema` explicitly on BOTH `rootSettings()` AND `includedSettings(...)` — snakeyaml-engine's DEFAULT LoadSettings schema is the JSON schema, under which `~` is the string `"~"` not null (LTD-09; failed `YamlLoaderTest.nullValuedKeysDropped`; `CoreSchema` javap-verified in the cached 2.9 jar incl. the exported `…v2.schema` package; the test untouched — it is correct); (2) arch Rule 5 `NO_DIRECT_FILESYSTEM_IN_CORE` drops `com.homesynapse.config..` from its package list ONLY per the PM ruling on ratified AMD-71/[AMD-71-A] — config is the designated filesystem consumer rooted at the composition-root-injected configDir `Path`, AMD-71-INV-01 canonicalization-containment (`ConfigLayoutTest`-pinned) is the compensating control; ruling recorded in the rule's banner + Javadoc (failed `HomeSynapseArchRulesTest.noDirectFilesystemInCore`; Rule 4 + everything else untouched). Gate-fix files: `YamlLoader.java` + `HomeSynapseArchRules.java` (+ config MODULE_CONTEXT gotcha). Scope-pin grep survey in the DEFERRED section.** _Same-day prior:_ **M6.1a (config load/validate — the second and final M6.1 half) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see `### DEFERRED — M6.1a` below). M6.1 IS NOW DELIVERED WHOLE** (M6.1b committed `b7bc65c` gate-GREEN + M6.1a layered on top of it). The 2026-06-10 RULING ADDENDUM applied exactly: (1) `module-info.java` +5 third-party **non-transitive** `requires` (`org.snakeyaml.engine.v2`, `com.networknt.schema`, `com.fasterxml.jackson.core`, `com.fasterxml.jackson.databind`, `org.slf4j` — HomeSynapse edge set unchanged, [AMD-71-A] zero-new-edge holds) + `build.gradle.kts` `+implementation(libs.jackson.databind)`/`+implementation(libs.slf4j.api)` (both already in the catalog — NO `libs.versions.toml` change); (2) DP-4 corrected — `StandardConfigurationService` ctor takes **`List<ConfigurationChangeListener>`**, builds the internal map, duplicate section path → ctor `IllegalArgumentException` (+ `ConfigurationChangeListener` Javadoc registration line reworded to the list form); (3) publish metadata as ruled — DIAGNOSTIC, `EventOrigin.SYSTEM`, null `eventTime`, `publishRoot`, system subject (`SubjectRef.system(SystemId)`; `SystemId` read via the existing `event→platform` implied-readability chain — NO new edge). **Delivered (9 new files + 3 edits):** `YamlLoader` (safe YAML 1.2; one-level `!include` parsed WITHOUT the include constructor so nesting is a structural unknown-tag FATAL — AMD-71-INV-02; `toRealPath()` containment under `integrations/` fail-closed naming the path — AMD-71-INV-01; null YAML values dropped = unset→default, satisfies the frozen records' null-hostile `Map.copyOf`; unknown tags incl. `!secret`/`!env` FATAL until M6.2), `JsonSchemaCompositeValidator` (allErrors; §3.6 keyword classification `required`→FATAL / `additionalProperties`→WARNING / else ERROR with `appliedDefault` from the schema default; networknt-1.5.6 instanceLocation+property semantics **verified from jar bytecode via javap** — the in-sandbox runtime probe was permission-denied, so static verification stood in), `StandardSchemaRegistry` (`ReentrantLock`; inline fragment embedding — fragments are strings, no `$ref`; DP-1 `schema_version` object property with const-pinned major + declared-pair default; ctor takes the declared `(major,minor)` — **must match the service's pair**, composition-root contract, MODULE_CONTEXT gotcha), `StandardConfigurationService` (10-arg ctor; §3.1 pipeline parse→migrate→default-merge→validate→§3.6 startup model→`ConfigModel`; AMD-67 chain per the `ConfigMigratorChainTest` reference semantics — in-memory application + declared-pair stamp, chain gap or NEWER persisted major = FATAL; DP-2 ERROR→schema-default revert / FATAL→`ConfigurationLoadException`; `config.validation_completed` once per **completed** validation pass — also fired on a FATAL pass before the throw, never on parse-stage aborts; publish failure logged + load continues (AMD-70-INV-01); `schemas/config.schema.json` cache rewritten per load (WARN-on-fail, regenerable); `reload()`/`write()` staged `UnsupportedOperationException` until M6.4; `getCurrentModel()` pre-load → `IllegalStateException`) + tests `YamlLoaderTest`, `ConfigLayoutTest` (all 5 AMD-71 §5 rows; symlink row `Assumptions`-guarded for non-privileged Windows), `JsonSchemaCompositeValidatorTest`, `StandardConfigurationServiceTest` (publish metadata, DP-2 both branches, all AMD-67 trigger/order/gap/newer-major/malformed-version cases, AMD-66 duplicate rejection, M6.4 staging) + package-private `RecordingEventPublisher` test helper (lifecycle `StubPublisher` precedent — no new test-fixture dependency). **Deviations: 3×`[REVIEW]`** (R1 per-ERROR `config_error` startup publication mandated by Doc 06 §3.6 NOT implemented — outside instruction scope, recommend folding into M6.4 or a micro-WU; R2 §3.7 step-7 migration disk write-back + pre-migration backup deferred to the atomic-write milestone — AMD-67 §6 fences execution to the chain contract, no production migrator exists so unreachable; R3 the event also fires on FATAL validation passes — the faithful "once per validation pass" AMD-70 §4 reading, recommend ACCEPT), **`[INFO]`×8** (Completion Report). **Doc-accuracy findings for the PM/docs lane:** Doc 06 §3.2 step-4 path `/etc/homesynapse/schema/` (singular) is stale vs the ratified AMD-71 `schemas/`; Doc 06 §3.7 body still shows pre-AMD-67 shapes (`fromVersion`/`toVersion`, single-int `schema_version`, the unrenamed `ConfigChange`) — consider an amendments-in-force banner; M6.1 DP-5's "stamps loadedAt/fileModifiedAt from an injected Clock" conflicts with the frozen `ConfigModel` Javadoc + §3.5 token semantics for `fileModifiedAt` (implemented: real file mtime when present, clock only for the absent-file zero-config case); `SystemId` "recorded in the configuration store" + config-loads-first (Doc 12) is a chicken-and-egg the M13 composition root must resolve (generate-or-load SystemId BEFORE config load). **Next WU = M6.2 (SecretStore + key management, AMD-68; crypto-gated per the M6 charter) upon Nick's M6.1a gate + PM instruction; M6.4 (hot-reload) trails it and must re-run the P2 survey for `config.section_reloaded` (refuse-to-close pointer below).** _Prior (2026-06-09):_ M6.1 ISSUED (Nick, direct) and SPLIT at the pre-authorized Fork-2 seam: M6.1b (access/events + AMD-66/67/70 contract shapes) COMPLETE — files produced, build gate then-deferred (now RESOLVED-GREEN `b7bc65c`); M6.1a (load/validate) was BLOCKED on a `[BLOCKING]` ESCALATION — the instruction's no-new-`requires` mandate was physically uncompilable for the lib-dependent pipeline classes (escalation upheld; Nick ruled 2026-06-10 — see the RULING ADDENDUM in the instruction). Escalation in one line: snakeyaml-engine 2.9 (explicit JPMS module `org.snakeyaml.engine.v2`) and networknt json-schema-validator 1.5.6 (explicit JPMS module `com.networknt.schema`, requiring jackson-databind NON-transitively) resolve on the module path (precedent: persistence's `requires com.fasterxml.jackson.databind`), so `YamlLoader`/`JsonSchemaCompositeValidator`/`StandardSchemaRegistry`/`StandardConfigurationService` cannot compile inside `com.homesynapse.config` without new third-party `requires` directives — the instruction's STOP gate reserves that to a PM/Nick ruling. The [AMD-71-A] *substance* (NO `config→platform` HomeSynapse edge; the E2-bridge zero-new-edge property) is untouched by third-party directives. Coder recommendation: `requires org.snakeyaml.engine.v2; requires com.networknt.schema; requires com.fasterxml.jackson.core; requires com.fasterxml.jackson.databind; requires org.slf4j;` — all non-transitive, consumed only by package-private classes (no `-Xlint:exports` exposure), + Gradle `implementation(libs.jackson.databind)`/`implementation(libs.slf4j.api)`; the AMD-66/67/70/71 §7 verbatim module-info embeds then need a docs-lane correction. **M6.1b delivered (23 files across config/event-model/persistence + 1 harness Javadoc):** AMD-67 shapes (`ConfigModel` 5→6 components + `major>=1`/`minor>=0` guards + AMD-54 cross-ref Javadoc; `ConfigMigrator` 3→5 methods; `MigrationPreview` 4→6 components; `MigrationResult` deliberately UNCHANGED per E67-1; `TestConfigFactory` ×5 ctor sites + `InMemoryConfigAccessTest` ×1 accessor mechanically updated), AMD-66 `ConfigurationChangeListener` (verbatim §2.1 shape; registration/duplicate-rejection tests ride M6.1a with the service), `ScopedConfigurationAccess` (package-private `ConfigurationAccess` impl — snapshot semantics, INV-CE-02 empty-section, strict `instanceof` typed accessors), AMD-70 `ConfigValidationCompletedEvent` in `com.homesynapse.event` (flattened E70-1 payload `(configSchemaMajor, configSchemaMinor, issueCount, Map<String,Integer> severityCounts)` keyed by `Severity.name()` strings; sum==issueCount ctor guard) + `EventTypes.CONFIG_VALIDATION_COMPLETED` ("config.validation_completed") + `CORE_PRODUCTION_EVENT_CLASSES` 22→23 + the full P2 manifest set: `EventCategoryMapping.TABLE` +1 → `[SYSTEM]` (sibling-consistent; PM confirms at review), count pins 53→54 (`EventTypesTest`), 22→23+rename `exactlyTwentyThreeAnnotatedRecords` (`EventTypeAnnotationTest`), 34→35 (`EventCategoryMappingTest` +1 mapping test), 22→23/34→35 (`EventTypeRegistryTest` ×3, `JacksonWarmupTest` ×2), Javadoc counts (`AllEventClasses`, `IntegrationTestHarness`, `EventPayloadCodecTest`), `TestEventSamples.configValidationCompleted()` + codec round-trip test (Map component round-trips). `HomeSynapseCore` + `IntegrationTestHarness` aggregate via `Stream.of` — no count pins, NO edits (survey-verified). New tests: `ConfigModelTest`, `ConfigMigratorChainTest` (AMD-67-INV-02 trigger/ordering reference semantics for the M6.1a loader), `ScopedConfigurationAccessTest`, `ConfigurationChangeListenerTest`, `ConfigValidationCompletedEventTest`. MODULE_CONTEXT ×3 updated (config 22→24 types; event-model 47→48; persistence EventCategoryMapping row de-staled 27→35 + M6.1b section). **NO `module-info`/`build.gradle.kts` change anywhere in M6.1b** (config module-info verbatim-unchanged — the STOP-gate holds for the delivered half). Deviations: 1×`[BLOCKING]` (M6.1a escalation), 2×`[REVIEW]` (event priority DIAGNOSTIC proposed — publish path M6.1a-held; DP-4 `Map` ctor param vs AMD-66 §2.4 duplicate-rejection contradiction — suggest `List<ConfigurationChangeListener>` ctor, service builds the map), `[INFO]`×4 (Completion Report). **HELD for M6.1a (not written):** `YamlLoader`, `JsonSchemaCompositeValidator`, `StandardSchemaRegistry`, `StandardConfigurationService` + `YamlLoaderTest`/`ConfigLayoutTest`/`JsonSchemaCompositeValidatorTest`/`StandardConfigurationServiceTest` (+AMD-71 layout/include/traversal behavior + the publish path + AMD-66 registration tests). **Next WU = M6.1a upon the PM/Nick module-info ruling (refuse-to-close).** _Prior (PM, 2026-06-09):_ M6.1 READY TO ISSUE — entry-gate closed 2026-06-09: **AMD-66/67/68/70/71 RATIFIED + AMD-69 DEFERRED** (Tier-2/OQ-15-3); instruction `context/coding-instructions/M6.1_Config_Pipeline.md`. Scope preview: YAML 1.2 load (snakeyaml-engine) → JSON-Schema validation (networknt; JSON-text-`String` params only) → AMD-67 `(major,minor)` `ConfigModel` → `ConfigurationAccess`/`ConfigurationService` impls → AMD-66 listener + registration → `config.validation_completed` (AMD-70, flattened event-resident payload) → AMD-71 hybrid layout + canonicalization traversal guard, all on the DP-3 injected config-dir `Path` (NO `config→platform` edge — ratified [AMD-71-A]). Non-negotiables travel with it: §4c `Clock` injection (config is non-whitelisted); the P2 consumer/pin survey across the full manifest set; the E70-1 JPMS/contract-direction check (any new type in a base module references only that module / a leaf / `java.base`). Issue whole (Fork-2); your Phase-2 file-touch inventory is the split checkpoint (seam: M6.1a load/validate / M6.1b access/events). M9 may now publish command-bearing `CapabilityAdded` (AMD-87 serde gate GREEN). _Prior 2026-06-07:_ (**M5-A Part 2 (AMD-87 `Expectation` persisted codec) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see Deferred Build Gate § + the `### DEFERRED — M5-A Part 2` subsection). 7 files: 2 new package-private codec classes (`ExpectationSerializer`/`ExpectationDeserializer` — tagged-union `{"t":"<permit-simple-name>",…}` over the 4 permits, exhaustive no-`default` switch; `ExactMatch`/`AnyChange` delegate `v` to the registered `AttributeValue` codec via `provider.defaultSerializeValue`/`ctxt.readTreeAsValue`, `EnumTransition` plain string, `WithinTolerance` reuses `AttributeValueSerializer.writeDouble`/`AttributeValueDeserializer.readDouble`), persistence `module-info` `+requires com.homesynapse.device` (non-transitive; **NO `build.gradle.kts` change** — device-model already on the compile classpath via `api(":core:state-store") → api(":core:device-model")`; acyclic + `moduleGraphAssert`-clean `:core:.*->:core:.*`), `PersistenceJacksonModule` `+Expectation` pair keyed on the interface (`FloorId` untouched), un-`@Disabled` `EventPayloadCodecTest.capabilityAdded_onOff_roundTrips` (+removed the now-unused `Disabled` import so Spotless stays GREEN; DisplayName AMD-65→AMD-87), retired stale AMD-65 Javadoc in `TestEventSamples.onOffInstance()`, new per-permit `ExpectationSerdeTest`. NO `build.gradle.kts`/`projectionVersion`/event-shape/migration/`@JsonTypeInfo` change. No `[BLOCKING]`/`[REVIEW]` deviations; 1 `[INFO]` (the device `requires` is the lone `requires` in this module-info satisfied transitively at the Gradle level — PM may optionally add `implementation(project(":core:device-model"))` for lockstep-consistency, not needed for GREEN). persistence 50→52 types. Next WU = PM's M6 instruction or next M5 lane (refuse-to-close — Coder BLOCKED on PM). Prior: M5-A (platform-systemd PlatformPaths/HealthReporter impls + FloorId Jackson registration) Part 1 + Part 3 COMPLETE + COMMITTED `8028337`; Part 2 (AMD-87 codec) UN-GATED 2026-06-07 (AMD-87 RATIFIED) — GO for Claude Code. Part 1+3 record: files produced, build gate RESOLVED + COMMITTED `8028337` (see Deferred Build Gate § below). Part 2 (AMD-87 Expectation codec) was NOT done at Part-1+3 time — gated on AMD-87 ratification, now lifted. NEW `platform-systemd` module populated (scaffold→4 impls + `module-info`): `LinuxSystemPaths`/`LocalPaths` (FHS / CWD-rooted; resolve-once+cache 6 paths; create the 5 writable dirs, NOT the read-only `binaryDir`; clear `tempDir`; package-private `fsRoot`/`baseDir` test seam) + `SystemdHealthReporter` (sd_notify `READY`/`WATCHDOG`/`STOPPING`/`STATUS` over AF_UNIX SOCK_DGRAM; `ReentrantLock`-serialised; `READY` once via `AtomicBoolean`; send-and-forget WARN-on-fail; nested `NotifyTransport` test seam) + `NoOpHealthReporter`. `registerTypedWrapper(FloorId.class, FloorId::toString, FloorId::parse)` added to `PersistenceJacksonModule` (10→11 ULID-shaped types) + new `FloorIdSerializationTest` — closes the B-S1 platform-api MODULE_CONTEXT FloorId-serde OPEN item. platform-systemd `module-info` `requires transitive com.homesynapse.platform` (⟳ gate-fix round 1 — was plain `requires`; the 4 public impls expose platform-api supertypes in an exported package → `-Xlint:exports`/`-Werror`) `+requires org.slf4j`, and `build.gradle.kts` `api(platform-api)` `+slf4j.api`/`+test-support`. NO persistence `module-info`/`requires com.homesynapse.device` change (that is gated Part 2). **TECHNICAL PUSHBACK:** pure JDK 21 `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime — compile + unit tests GREEN via the transport seam, but real Tier-1 sd_notify needs a native binding (JNR/JNA, not in catalog) or a `systemd-notify` fallback (M13/composition-root concern). Deviations: 1×`[REVIEW]` (`+requires org.slf4j` beyond the instruction's embedded module-info), `[INFO]`×3 (package-private test-seam ctors; dir-prep duplication across the two Paths impls; stale "all 10 typed wrappers" DisplayName left in `UlidSerializationTest`). Next WU = M5-A Part 2 (AMD-87 codec) — ✅ **GO (AMD-87 RATIFIED 2026-06-07; un-gated; hand to Claude Code)**. See cross-agent-notes 2026-06-07 + the refuse-to-close Next-Work-Unit block below.**) Prior: **M4.C (AMD-54..64 — integration-api interface freeze) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see Deferred Build Gate § below). Contract-only freeze of the adapter-facing surface; supervisor impl = M9. integration-api 22→40 types: +5 enums (`ConfigUpdateOutcome`/`MigrationOutcome`/`ReauthOutcome`/`CapabilityRemovalReason`/`IsolationLevel`), +10 records (5 dot-namespaced lifecycle events; 2 capability events `CapabilityAdded`/`CapabilityRemoved`; 2 aggregators `SecurityServices`/`DiscoveryServices`; `BackoffParameters`), +1 sealed iface `CapabilityEvent`, +2 ifaces `CredentialRotator`/`CapabilityPublisher`. `IntegrationDescriptor` 8→14 (rename `schemaVersion`→`descriptorSchemaVersion` + 6 new; 14-arg canonical + 8-arg convenience ctor + guards incl. `dependsOn ∩ softDependencies = ∅`). `IntegrationContext` 10→12 (`security`/`discovery` nullable; 12-arg + 10-arg convenience). `IntegrationAdapter` 4→8 declared (4 `default` hooks; existing adapters unchanged). `RequiredService` 3→5 (`DISCOVERY`/`SECURITY`). `IntegrationLifecycleEvent` 5→10 permits. `IntegrationEvents` +`CAPABILITY_EVENT_CLASSES`. `PermanentIntegrationException` +append-only code-bearing ctor pair (dotted-lowercase guard). integration-runtime: `ExceptionClassification` 3→4 (+`AUTH_FAILED` last), new `HealthDetail` (12), `IntegrationHealthRecord` 13→14 (+`detail` after `state`, no convenience ctor). event-model `EventTypes` +7 string constants (5 `integration.*`, 2 `capability.*`). Three-way registration lockstep verified. **NO `module-info`/`build.gradle.kts`/`projectionVersion` change** (G9 both module-infos byte-identical). STOP-gates G1–G13 ALL PASS at `e76b925`. **One [BLOCKING-for-M9] serde deferral (Nick-arbitrated Option 2 → ~AMD-65 follow-up): command-bearing `CapabilityAdded` does NOT round-trip — the sealed `Expectation` device type has no persistence codec; M4.C's green codec round-trip uses a command-less `occupancy()`-derived instance, with a `@Disabled("AMD-65 pending")` onOff acceptance test. M9 must not publish command-bearing CapabilityAdded until the Expectation codec lands.** **Next WU (updated 2026-06-06, M5 window chartered) = M5-A** — `PlatformPaths` (LinuxSystemPaths + LocalPaths) + `HealthReporter` (SystemdHealthReporter `sd_notify` + NoOpHealthReporter) + **AMD-87 `Expectation` codec** (the deferred serde above — reassigned from the retired AMD-65 per INV-GA-02; the live `@Disabled("AMD-65 pending")` annotation keeps the retired number, delete it; lightweight block-track) + register `FloorId` in `PersistenceJacksonModule`; apply the P2 consumer/pin survey; do NOT rebuild existing test doubles. See `context/coding-instructions/M5-A_Platform_Impls_AMD87_FloorId.md` + `weeks/2026-W24_jun08-jun14.md` + decisions D1–D8. **M9 supervisor remains ~late-Jul/Aug** (its PM-authored instruction follows M6; M9 must still not publish command-bearing CapabilityAdded until AMD-65 lands). No `[BLOCKING]`/`[REVIEW]` deviations beyond the AMD-65 serde follow-up.**) Prior: **M4.B-S2 (AMD-44 Stage 2 — EntityRole enum + EntityType legality matrix + `Entity`/`ProposedEntity` entityRole) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see Deferred Build Gate § below). New device-model enum `EntityRole` (PRIMARY/DIAGNOSTIC/CONFIG — exactly 3, no SYSTEM). `EntityType` gains a `Set<EntityRole> legalRoles` ctor + `allows(EntityRole)`/`legalRoles()` (6×3 matrix per AMD-44 §2.5.1; `EnumSet.of`→`Set.copyOf` immutable; **source declaration order preserved** — DP-5, pinned by `EntityTypeTest`). `Entity` 11→12 components (`entityRole` between `capabilities` and `createdAt`) and `ProposedEntity` 3→4 (`entityRole` last): compact ctors coerce null→PRIMARY → matrix-guard (`IllegalArgumentException` "entityRole %s is not legal for entityType %s") → defensive-copy; **back-compat convenience ctors** (Entity 11-arg, ProposedEntity 3-arg) default role PRIMARY → all existing callers (`EntityTest`, `TestEntityFactory`, `StubIntegrationContext`) compile unchanged (DP-3; StubIntegrationContext NOT edited). `TestEntityFactory` builder gains `entityRole(...)` (default PRIMARY) + 12-arg build. `TypedIdTest.allTypesDistinct` FloorId fold-in (8→9 wrappers — closes the B-S1 [INFO] follow-up, DP-7). **NO `module-info`/`build.gradle.kts`/`projectionVersion`/event-model change.** device-model 56→57 types (58→59 files). **Workstream B COMPLETE.** Next WU = Workstream C briefing (PM). No `[BLOCKING]`/`[REVIEW]` deviations. Prior: M4.B-S1 (AMD-44 Stage 1 — Floor aggregate + minimal Area + `Set<HardwareIdentifier>` refactor) COMPLETE + COMMITTED `e73e199`. New platform-api type `FloorId` (typed ULID wrapper, mirrors `AreaId`). New device-model types `Floor`/`Area` (records, compact-ctor validation) + `FloorRegistry`/`AreaRegistry` (interfaces only — NO impl, matching `DeviceRegistry`/`EntityRegistry`). `Device`/`ProposedDevice`/`DiscoveryPipeline` `hardwareIdentifiers` `List`→`Set` (AMD-44 §2.6 — duplicate (namespace,value) tuples are meaningless); `Device`/`ProposedDevice` gained NEW defensive-copy compact ctors (`Set.copyOf`/`List.copyOf` — null collection now NPEs where the bare records didn't; no existing caller passed null). `AreaId` first-line Javadoc de-conflated ("or floor" → "or other user-defined spatial grouping"). NO `module-info`/`build.gradle.kts` change (both target packages already exported). EntityRole + everything touching `EntityType`/`Entity`/`ProposedEntity` deliberately UNTOUCHED = Stage 2. device-model 52→56 types; platform-api 12→13. Next WU = M4.B-S2 (AMD-44 Stage 2 — EntityRole).**) Prior: **M4.0b-5 (AMD-53 timestamp-model unifier — event-time activity-timestamp materialization; `projectionVersion` 4→5) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see Deferred Build Gate § below). `StateProjection` now sources all three `EntityState` activity timestamps (`lastChanged`/`lastUpdated`/`lastReported`) from `eventTime ?? ingestTime` in EVERY `applyToState` branch + `initialEntityState` adoption seeding (was the projection wall-clock); `applyToState` no longer reads `clock`; `backfillTimestamp` renamed `eventTimestamp` (the single shared rule); `applyBackfillAttribute` behaviourally unchanged (was already event-time) with the `[REVIEW]` interim note retired; `initialEntityState` gained an `Instant seed` param (both call sites pass the event-time stamp). `HomeSynapseCore` `projectionVersion` literal 4→5 — the 4→5 reconciliation heals legacy wall-clock timestamps via AMD-50's frozen backfill (unchanged, §2.5). `staleAfter`/`stale` stay wall-clock (carve-out, AMD-53-INV-02). NO `module-info`/`build.gradle.kts`/codec/event-shape/attribute-value/`stateVersion` change (AMD-53 §6). Tests extended (not greenfielded): `StateProjectionContractTest` +3 (the GATE — LIVE ≡ replay-from-zero across all three fields with a fixed clock distinct from the corpus event/ingest times so a wall-clock regression fails; carve-out read-time `stale` flips on clock vs `staleAfter`; no-op report keeps `lastChanged`); `ReconciliationTest` +6 (event-time sourcing + `eventTime==null`→`ingestTime` fallback for all three fields + `availability_changed` `lastUpdated`; adoption-seeding determinism §1.5; the 4→5 reconciliation heal — all three event-time, `stateVersion`/attribute values unchanged, AMD-50-INV-01 no double-increment). MODULE_CONTEXT + Constraints (AMD-53-INV-01/02) + Amendments-in-force updated; the M4.0b-2 mixed-`lastChanged` gotcha flipped to RESOLVED. Next WU = Workstream B Stage 1 (M4.B-S1 — AMD-44 Floor/Area/HardwareIdentifier, already authored).** Prior: **M4.0b-4b (AMD-52 typed `StateChangedEvent` payload + `AttributeValue` codec + typed materialization + Path-B gate + `projectionVersion` 3→4) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see Deferred Build Gate § below). Implements AMD-52 §2.1–§2.7 on the M4.0b-4a relocated `com.homesynapse.value` packages. Two new package-private persistence types: `AttributeValueSerializer`/`AttributeValueDeserializer` (hand-rolled `{"t":…,"v":…[,"u":…]}` tagged-union codec, exhaustive no-`default` switch, float bit-identity + `NaN`/`±Inf` sentinels + `−0.0`→`+0.0`), registered in `PersistenceJacksonModule`. `StateChangedEvent.oldValue/newValue` String→`AttributeValue` (oldValue nullable). `ProductionDerivationRule` emits typed at `schema_version=2`. `StateProjection.applyToState`/`applyBackfillAttribute` materialize typed; `shouldPublishDerived` typed-to-typed `equals`. `CheckpointSerializer` typed envelope (`Map<String,AttributeValue>`, ALWAYS null round-trip preserved). `EventPayloadCodec` Path-B gate (`state_changed`+v1→`DegradedEvent`). `HomeSynapseCore` `projectionVersion` 3→4. New ArchUnit rule `NO_JACKSON_IN_DOMAIN_MODEL` (value/event/device/state Jackson-free). New `AttributeValueSerdeTest` + extended `EventPayloadCodecTest`/`CheckpointSerializerTest`/`ProductionDerivationRuleTest`/`StateChangedEventTest`/`ReconciliationTest`/`StateProjectionContractTest`/`TestEventSamples`/`SqliteStateStoreTest`. NO `module-info`/`build.gradle.kts` change (the `requires com.homesynapse.value` edges already existed from 4a — the event-model→device STOP-gate did NOT fire). Next WU = see "Next work unit" below.** Prior: **M4.0b-4a (AttributeValue hierarchy relocated to the new `com.homesynapse.value` leaf module — AMD-52 §11 erratum) COMPLETE — build gate GREEN (`./gradlew check`), promoted from the M4.0b-4a compile-spike worktree. Pure behavior-preserving relocation: 10 types moved device→value, the `requires com.homesynapse.value` edge set wired (transitive on event/device/state, plain on persistence/automation, test-only on rest-api), all imports swept, MODULE_CONTEXTs updated. `com.homesynapse.event` does NOT `requires com.homesynapse.device` — the cycle is broken. NO semantics/`projectionVersion`/`StateChangedEvent` change. Next WU = M4.0b-4b (the AMD-52 typed `StateChangedEvent` payload, rebased on the relocated packages). Prior: M4.0b-3 (AMD-51 typed `AttributeValue` change-detection comparator) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (see Deferred Build Gate § below). Next WU = AMD-52 typed `StateChangedEvent` payload (staged behind OQ-05-08 — do NOT start until that design beat closes).** New state-store types: public `AttributeValueComparator` (+`structural()`), `ComparisonPolicy` (`FP_NOISE_DEFAULT 1e-9`), `AttributeSchemaResolver` (+`of`/`empty`); package-private `StructuralAttributeValueComparator` (exhaustive no-`default` 8-arm switch, IEEE-754 totality) + `AttributeValueReconstructor` (schema-driven both-sides parse). New device-model production type `StandardCapabilities` (DP-K — `all()`/`attributeSchemas()`, lifted from `TestCapabilityFactory` which now delegates). `ProductionDerivationRule` rewritten typed (reconstruct-both-sides → `comparator.changed`, **String payload preserved** — DP-G); `DerivationRule.production(comparator,policy,schemas)` overload added (no-arg kept = string-fallback). `HomeSynapseCore` wires comparator+policy+resolver, bumps `projectionVersion` **2→3**. 4 new test suites (`StructuralAttributeValueComparatorTest`, `AttributeValueReconstructorTest`, `ProductionDerivationRuleTest`, `StandardCapabilitiesTest`) + `ReconciliationTest` +2 typed tests. **NO `module-info`/`build.gradle.kts`/`CheckpointSerializer`/event-store change.** One `[REVIEW]`: missing-schema → `StringValue` fallback (not §2.6 Degraded/no-emit) — see cross-agent-notes 2026-05-30. **Key de-risk: every existing test + integration test uses non-catalog attribute keys (`power`/`v`/`attr`/`level`/`k`/`color`) → string-fallback path → behaviour identical to M4.0b-2; the typed path is exercised only by new tests (`temperature_c`).** Prior: **M4.B3 (AMD-47 AttributeValue expansion + `AttributeValueUpcaster` SPI) COMPLETE + committed `60b4185`, build GREEN; PM WUCP Phase 2 closed.** Build gate RESOLVED — Nick ran `./gradlew :core:state-store:check` + `:lifecycle:lifecycle:check` + full `check` (139 tasks) GREEN. PM Phase 2 read every changed file vs source and confirmed targets A–J; AMD-50-INV-01..04 upheld. Implements **AMD-50** (RATIFIED) for the 1→2 transition on the M4.0b-1 string change-detect rule: `projectionVersion` 1→2 in `HomeSynapseCore`; the `backfillActive` provenance gate in `StateProjection` (set in the reconciliation branch of `initialize()`, cleared in `onCaughtUp()`); a non-emitting one-shot backfill applied on BOTH `onEvent` and `processBatch` via the narrow `applyBackfillAttribute` helper (attributes + event-time `lastChanged` only; no second `stateVersion` increment — INV-01); supersession suppression in `applyToState`'s `state_changed` branch (gate-conditional); `Clock` removed from `DerivationContext` (§2.4). All six AMD-50 §5 tests added/extended across `ReconciliationTest` (+6) and `StateProjectionContractTest` (rebuild-idempotency extended, determinism adjusted, 4 `DerivationContext` sites updated). **Next: M4.B3 then M4.0b-3** (typed comparator/payload, AMD-47/51/52 — reuse this backfill path unchanged for the 2→3 transition). Prior: **M4.0b-1 COMPLETE + committed `cf1a97e`, build GREEN**; M4.0a COMPLETE `a441fdf`.)

**PM gate-state append (2026-06-12, Pi-evidence session — single-anchor, the Coder's record above untouched):** the M6.3 "Next WU" pointer's triple gate is now **1-of-3 CLOSED** — ✅ **OQ-15-2 RESOLVED 2026-06-12**: the microbench ran (guided session; Pi 5 intrinsics-off, NON-AUTHORITATIVE caveat accepted per the recorded R1 conditional) and the **`encrypted_scopes` default is CONFIRMED `[identity, presence_personal]`** — the M6.3 instruction will carry the resolved set verbatim, plus the cold-start finding (first ~100 encrypts cost 80–700 µs after JVM start — a cipher-warmup beat to weigh at M6.3 scoping, JacksonWarmup precedent) and OR-M6-NONCE unchanged. Still open: the AMD-86 §3 interview signal (Nick-paced) + the M6.2 gate landing per your record. Disposition: `context/assessments/2026-06-12_OQ-15-2_disposition_encrypted-scopes-CONFIRMED.md`. M6.3 remains do-NOT-issue until all three gates close.

Canonical Coder handoff file referenced by the nexsys-coder skill (`../context/handoff/coder-handoff.md`). A duplicate at `homesynapse-core/docs/handoff/coder-handoff.md` (created during a Cowork session) was consolidated into this file on 2026-05-15 and removed.

---

> **Rotated 2026-06-11 (hygiene pass A1):** the `## M4 Readiness` section and ALL gate records older than M6.2 (M6.4 → M3.7) now live in `archive/coder-handoff-2026-06.md`. This live file carries: the masthead, the current gate status, and the NEWEST gate record (`### DEFERRED — M6.2`, which holds the refuse-to-close next-WU pointer).

## Deferred Build Gate

**Status:** **RESOLVED-GREEN — M6.2 COMMITTED `7c73c91`** (sha reconciled by the PM 2026-06-11 EOD against `git log`; gate GREEN 82+147, ZERO gate-fix rounds; PM WUCP Phase 2 → APPROVE, R-1/R-2/R-3 ACCEPT). **No open deferred build gates.** Next WU = **M6.3 ONLY when its triple gate clears (do NOT start)**; gate record in `### DEFERRED — M6.2` below. _Prior:_ **OPEN — M6.2 DEFERRED to Nick 2026-06-11.** _Prior:_ **RESOLVED-GREEN — M6.4 gate run by Nick 2026-06-11 (full `./gradlew check`, 147 tasks) GREEN; COMMITTED `62a81e6`** (sha reconciled by the PM 2026-06-11 against `git log` HEAD at the M6.2-return session Step-0). PM WUCP Phase 2 → APPROVE (R-A/R-B/R-C/R-D ruled ACCEPT; GF-1 verified applied exactly as ruled). **No open deferred build gates.** Next WU = **M6.2 — DISPATCHED 2026-06-11** ([R6] CONFIRMED; R1 done in M6.4; its G7 gate adapts to the post-M6.4 service; baseline `62a81e6`). _Prior:_ **OPEN — M6.4 (hot-reload atomic swap + UI/API write path) DEFERRED to Nick 2026-06-11** — see `### DEFERRED — M6.4` below for the exact commands and commit. _Prior:_ **RESOLVED-GREEN — M6.1a (incl. ⟳ gate-fix round 1) gate run by Nick 2026-06-10: `./gradlew :config:configuration:check` (27 tasks) + full `./gradlew check` (147 tasks) GREEN; **COMMITTED `9035110`** (sha reconciled by the PM 2026-06-10 against `git log` HEAD — "M6.1a: YamlLoader…", on `b7bc65c`). PM WUCP Phase 2 → APPROVE (module-info verified = the ruling exactly; R1/R2/R3 ruled ACCEPT). M6.1 COMPLETE (whole: M6.1b `b7bc65c` + M6.1a `9035110`). **No open deferred build gates.** Next WU = M6.2 on the PM instruction (after the PM's config research beat).** _Gate-fix history:_ ⟳ ROUND 1 applied 2026-06-10: Nick's first gate run FAILED 2× (`YamlLoaderTest.nullValuedKeysDropped` at `:config:configuration:test`; `HomeSynapseArchRulesTest.noDirectFilesystemInCore` at `:app:homesynapse-app:test`); both PM-ruled fixes applied (explicit `CoreSchema` on both LoadSettings; Rule 5 config exemption per ratified AMD-71) — gate RE-DEFERRED, same commands. The Coder produced files only 2026-06-10 (CLAUDE.md build discipline; the in-sandbox P5 shift-left compile is unavailable — `javac` probes are permission-denied in this environment); Nick runs the gate. See `### DEFERRED — M6.1a` below for the exact commands.** Prior: **RESOLVED-GREEN — M6.1b build gate run by Nick 2026-06-10: `./gradlew :config:configuration:check :core:event-model:check :core:persistence:check` (57 tasks) + full `./gradlew check` (147 tasks) GREEN; COMMITTED `b7bc65c` (sha reconciled by the Coder 2026-06-10 against `git log` HEAD). PM WUCP Phase 2 → APPROVE (2026-06-10). M6.1a UNBLOCKED — module-info ruling APPROVED (see the M6.1 instruction RULING ADDENDUM).** Prior: **RESOLVED — M5-A Part 2 (AMD-87 `Expectation` persisted codec) build gate RESOLVED `7f44bed`.** Nick ran `./gradlew :core:persistence:check :core:device-model:check` (53 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert — the persistence→device STOP-gate) **GREEN** and **COMMITTED `7f44bed`** (2026-06-07, 8 files, +588/−13); PM WUCP Phase 2 → **APPROVE** (codec source-verified vs AMD-87 §2 + AMD-87-INV-01, not the completion report; 3 `[INFO]` accepted — incl. the optional `implementation(project(":core:device-model"))` lockstep hardening). **M5-A COMPLETE (Parts 1+2+3); M9 command-bearing-`CapabilityAdded` prerequisite CLEARED. No open deferred build gates.** Prior: **RESOLVED — M5-A (platform-systemd PlatformPaths/HealthReporter impls + FloorId Jackson registration, Part 1 + Part 3) build gate RESOLVED** — after the ⟳ gate-fix round 1 (`requires transitive` + Gradle `api` for the `-Xlint:exports`/`-Werror` failure), Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** (2026-06-07); **COMMITTED `8028337`** (Nick host-git; `git log` HEAD confirmed this session); PM WUCP Phase 2 (Step 0 sha-reconciliation) → this closeout. Part 2 (AMD-87 codec) is now **UN-GATED (AMD-87 RATIFIED 2026-06-07) + IMPLEMENTED** — its build gate is the open deferred gate above (see `### DEFERRED — M5-A Part 2`). Prior: **RESOLVED — M4.C (AMD-54..64 integration-api interface freeze) build gate RESOLVED** — Nick ran `./scripts/clean.sh && ./gradlew check --continue` (145 tasks, incl. ArchUnit + spotless) GREEN against the gate-fixed M4.C tree and **COMMITTED `8ef9e9f`**; PM WUCP Phase 2 → APPROVE (2 `[REVIEW]` ACCEPTED — EventCategoryMapping categories + composition-root/harness scope); **WORKSTREAM C + M4 COMPLETE**; **AMD-65 (Expectation codec) deferred BLOCKING-for-M9.** **No open deferred build gates.** _(Flipped OPEN→RESOLVED 2026-06-05 in retroactive reconciliation — the M4.C-complete closeout left this entry at DEFERRED; see `context/audits/2026-06-05_M4-retrospective.md` §5.)_ Prior: **M4.B-S2 (AMD-44 Stage 2 — EntityRole) build gate RESOLVED** — Nick ran `:core:device-model:check` (26 tasks, clean build) + `:platform:platform-api:check` (38) + full `./gradlew check` (143, incl. ArchUnit `NO_DIRECT_TIME_ACCESS` + `NO_JACKSON_IN_DOMAIN_MODEL` + spotless) → **GREEN**; PM WUCP Phase 2 read all four production contract surfaces vs source + AMD-44 §2.5 → **APPROVE**; commit pending (placeholder `e76b925` in trackers — fill at commit). **Workstream B COMPLETE.** Prior: **M4.B-S1 (AMD-44 Stage 1 — Floor/Area/`FloorId` + `Set<HardwareIdentifier>`; NO `projectionVersion`/`module-info` change) build gate RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN; **COMMITTED `e73e199`**; PM WUCP Phase 2 → APPROVE. Prior: **M4.0b-5 (AMD-53 event-time activity-timestamp materialization; `projectionVersion` 4→5) build gate RESOLVED — GREEN, COMMITTED `c99b425`.** Prior: **M4.0b-4b (AMD-52 typed `StateChangedEvent` payload + codec) build gate RESOLVED — GREEN, COMMITTED `72596cb`.** Prior: **M4.0b-4a (AttributeValue relocation → `com.homesynapse.value`) build gate RESOLVED — `./gradlew check` GREEN (2026-05-31).** Prior: **M4.0b-3 (AMD-51) build gate DEFERRED to Nick (see subsection below)** — the full M4.0b-4a `check` runs on a tree layered over the M4.0b-3 changes, so they are exercised GREEN by that run too (PM may formally flip the M4.0b-3 entry). Prior gates all RESOLVED: **M4.B3 (AMD-47)** GREEN and **COMMITTED `60b4185`** (2026-05-30); **M4.0b-2** (AMD-50) GREEN `7610296`; **M4.0b-1** GREEN `cf1a97e`; **M4.0a + D-1** RESOLVED (`a441fdf`); all M3.7 gates RESOLVED 2026-05-27.

### DEFERRED — M6.2: SecretStore + Per-Scope Key-Management Infrastructure (AMD-68 + Doc 15 §3.5/§4.2/§7.3 + CARRY 1; 2026-06-11)

**State:** files produced 2026-06-11 (Claude Code session; CLAUDE.md build discipline — the Coder runs no builds). Working tree layered on **`62a81e6`** (M6.4 GREEN baseline). NO module-info / `build.gradle.kts` / `libs.versions.toml` / `projectionVersion` / SQLite-schema / event-manifest change in any module — the zero-new-edge property (CARRY 1) re-verified by grep in both directions at execution.

**Commands for Nick (in order):**

```bash
./gradlew :config:configuration:check :core:persistence:check :lifecycle:lifecycle:check :app:homesynapse-app:check
./gradlew check          # full — expect 147 tasks (no new Gradle modules)
```

**Touched modules:** config/configuration (6 new types + SecretStore/YamlLoader/StandardConfigurationService edits + 5 new test classes + 5 test classes adapted to the 12-arg ctor), core/persistence (PayloadCipher + EncryptedPayload — seam only, nothing calls them), lifecycle/lifecycle (HomeSynapseCore 5-arg ctor + held nullable cipher field + 1 test), app/homesynapse-app (Main.payloadCipher factory + PayloadCipherBridgeTest — the charter done-when round-trip).

**Risk notes for the gate:** (1) the 0400 permission row and the ConfigLayoutTest symlink row are `Assumptions`-guarded — on Windows they SKIP, on the Pi they run; (2) `SecretStoreTest.setAllIsAllOrNothingOnFailure` injects failure by pre-creating a DIRECTORY at `secrets.enc.tmp` — deterministic on both POSIX and NTFS (open-for-write on a directory fails IOException on both); (3) the M6.2 tests assert exact instants from `Clock.fixed` — no wall-clock reads anywhere (NO_DIRECT_TIME_ACCESS clean by grep); (4) `HkdfTest` pins the RFC 5869 A.1/A.2 vectors — verified against an independent HKDF implementation before pinning. (5) A 12-agent multi-lens adversarial review (compile-correctness / instruction-compliance / arch-constraints / crypto, each finding independently re-verified) ran pre-handoff: ZERO blocker/major findings; all five confirmed minors were fixed in-tree before this handoff — crash-atomic root-key write (temp+ATOMIC_MOVE), fsynced secret-store backups, overflow-guarded backup-number parse, the never-reuse-version pin test, and `Clock.fixed` in the new lifecycle seam test (§4c).

**Resolution (Nick):** on GREEN, commit; on failure, paste the failing task + test output back to the Coder for a gate-fix round.

---
