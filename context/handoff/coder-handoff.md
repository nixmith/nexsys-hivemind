<!--
file: context/handoff/coder-handoff.md
purpose: Coder session continuity — current task, deferred build gate, next WU, recent closeouts.
audience: Coder, PM
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-08-20 (Coder, R1R2 closeout — the R1R2 DELIVERED entry prepended [newest, authoritative for the CORE lane; supersedes the B′ DELIVERED entry below it — the B′ chain CLOSED: ca0f41d landed, CI #208 + install-smoke #28 green, MODULE_CONTEXT folded d26777c]. CORE repo, tree DIRTY at `c091f7c` + EXACTLY 4 M (the instruction's census: `distribution/image/build-image.sh` · `distribution/smoke/run-smoke.sh` · `.github/workflows/install-smoke.yml` · `distribution/ci/install-smoke.yml`) + the known pre-existing `?? _scratch/`. ZERO Java — scripts + workflow YAML only; NO gradle owed BY DESIGN (the instruction's §3). R-1: run-smoke gains check 4 "EVENT WRITE PATH IS LIVE" (two probes: ≥1 `events` row at `${HS_DB_FILE}` via the update-smoke `mode=ro` idiom, printing the observed count on PASS · zero `NoClassDefFoundError|jdk.jfr|BusMetrics` hits over journal+stdout with a zero-source honest-fail guard, printing the grep'd-zero + source count on PASS; the D-6 line shipped verbatim; checks 4–8 renumbered 5–9, digit-only, disclosed). R-2: build-image jdeps goes full-lib (every `${IMAGE}/lib` jar a root, flags kept, union unchanged) + FLOOR gains `jdk.jfr` + the 2b floor-presence assert (`--list-modules` must contain every ADD_MODULES entry; die printing absentees; green path prints the module count; passes-but-false comment verbatim) + the :75–:77 comment now describes the actual invocation. Twins: one identical sqlite3 apt step before run-smoke (duplicate-not-move, disclosed); byte-identical POST-edit (diff-proven; nlink=1 ×4 pre-checked). Desk gates GREEN on the final bytes: bash -n ×2 clean · shellcheck ABSENT on host (flagged; CI static-lint re-covers) · fragment dry-runs (case arms, hit arithmetic, floor loop incl. die-with-names) all exact · census exactly 4 M · nothing committed. P1–P9 ALL verified at c091f7c, zero STOP-grade mismatches; sharpenings filed (persist-precedes-notify at NotifyingEventPublisher :55/:56 ⇒ held-card RED most likely rows-present + ~6 throw hits, the §OP disjunction's second arm; SystemStartedEvent registered-but-unpublished — the boot-row guarantee rides the H3-proven hardware-less integration-lifecycle publishes + update-smoke.sh:8). Return: `context/audits/2026-08-20_R1R2_return.md` (§6 = the §OP ⏺ intake slots). Red-first accounting disclosed: CI green-by-construction; the RED leg = §OP block 1 on the held card (0.1.0+gd26777c) BEFORE the fix reaches it, Sat 2026-08-22. Next: hub audit → Nick commits/pushes (stages EXACTLY the 4 M) → install-smoke CI green on that push = the gate of record → the §OP card sitting → R-3/PKG-E2E-1 (weekend 2). Detail: the entry below.) Prior: ROTATED 2026-08-21 (v54 hub, beat 10) — the pre-R1R2 chain verbatim at the head of archive/coder-handoff-entries-rotated-2026-08-21.md.
-->

# Coder Session Handoff

---

## R-1/R-2 — the packaged write-path assert (red-first) + the jlink fix (CORE repo) — DELIVERED (2026-08-20; NEWEST — authoritative for the CORE lane; supersedes the B′ DELIVERED entry below as lane-newest)

> **DEFERRED GATE (hub, then Nick, then the card):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-08-20_R1R2_return.md` + this entry). **Deferred Build Gate: N/A-BY-DESIGN** — ZERO Java touched (scripts + workflow YAML only); the instruction's §3 orders NO gradle for this lane. The gate of record = **install-smoke CI green on Nick's push** of exactly these 4 M at/atop `c091f7c` (law 16); the RED/GREEN hardware legs are **§OP (Sat 2026-08-22, the held-card sitting — NEVER inside 03:00–04:15 CT)**. Working tree: core **`c091f7c`** + **EXACTLY 4 M** (`distribution/image/build-image.sh` · `distribution/smoke/run-smoke.sh` · `.github/workflows/install-smoke.yml` · `distribution/ci/install-smoke.yml`) + the known pre-existing `?? _scratch/`. Twins byte-identical POST-edit (diff-proven); hardlink pre-check nlink=1 ×4.
> **The delta:** R-1 — run-smoke check 4 "EVENT WRITE PATH IS LIVE" after the health probe (checks 4–8 renumbered 5–9, digit-only): probe 1 asserts ≥1 row in `events` at `${HS_DB_FILE}` (the update-smoke `sqlite3 file:…?mode=ro` idiom; sqlite3-absent = honest `bad`, never a skip; PASS prints the observed count), probe 2 asserts ZERO `grep -icE 'NoClassDefFoundError|jdk.jfr|BusMetrics'` hits over journal + stdout log (the H3-pinned real signature — the unescaped dot nets `jdk/jfr` AND `jdk.jfr` spellings; zero-scannable-sources = honest `bad`; PASS prints the grep'd-zero + source count); the D-6 line shipped VERBATIM in the check; all arms count via `bad` (the `set -uo pipefail` no-early-exit law held). R-2 — build-image: full-lib jdeps (every `${IMAGE}/lib` jar a root via `find|sort -z|xargs -0`, flags + FLOOR-union byte-unchanged) · FLOOR gains `jdk.jfr` · NEW 2b floor-presence assert (`runtime/bin/java --list-modules` must contain EVERY `${ADD_MODULES}` entry, `grep -qxF` per name, `die` printing ALL absentees, green path prints the module count; passes-but-false comment VERBATIM) · the :75–:77 comment now describes the actual invocation (the divergence was PART of F-23). CI twins: one identical "Install smoke prerequisites (sqlite3)" step before run-smoke (duplicate-not-move — update-smoke's own install stays for self-sufficiency; disclosed).
> **Desk gates GREEN on the final bytes:** `bash -n` ×2 clean · shellcheck ABSENT on this host (flagged; CI's static-lint step re-runs bash -n over `distribution/**/*.sh` on the push) · fragment dry-runs exact (probe-1 case arms incl. empty/non-numeric/multiline-unreadable + 0-dead + count-print; probe-2 arithmetic under `${VAR:-0}`; the grep count idiom 0-clean/3-on-H3-sample; the floor loop passes-complete + dies-naming-missing) · census exactly 4 M · **nothing committed** (the lane commits NOTHING).
> **P1–P9: ALL verified at `c091f7c` pre-implementation, ZERO STOP-grade mismatches.** Sharpenings filed in the return §5 (report-and-proceed): persist-precedes-notify (`NotifyingEventPublisher` :55/:56 vs the H3 trace's throw AT :56) ⇒ the held card's RED most likely = rows-PRESENT + ~6 throw hits — the §OP disjunction's second arm, exactly what probe 2 exists for; `SystemStartedEvent` is registered but UNPUBLISHED today — the boot-≥1-row guarantee rides the integration-lifecycle publishes (H3-proven CALLED six-per-boot hardware-less) + `update-smoke.sh:8`'s standing "boot writes lifecycle events"; P2's grep nets 10 files (8 main + module-info + test) vs the row's main-consumer EIGHT — enumeration scope, not drift. Red-first accounting (#18, disclosed): CI is green-by-construction; the ⏺ RED→GREEN pair on the held card (`0.1.0+gd26777c` → the rebuilt artifact) is the WU's red-first evidence of record. #22 N/A-disclosed (no FE surface).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the core commit msg (post-diff) → Nick's commit + main push (stages EXACTLY the 4 M) → **install-smoke CI green on that push = the gate of record** → the §OP Saturday card sitting (block 1 RED on the held card BEFORE the fix reaches it · block 2 rebuild + GREEN · block 3 restore; ⏺ intake slots = return §6; BOTH-CLEAN on block 1 = F-23 refuted at the instrument → STOP, hub adjudicates) → **R-3 / PKG-E2E-1** (weekend 2, MINTED DISTINCT — PrivateDevices loosening · the rehearsal rig on the preserved held card · integration-under-load). The D-1 claim fences stand VERBATIM until R-4 — nothing in this WU lifted any claim; `distribution/README.md:117` untouched. This lane HOLDS until the audit verdict or the next instruction.

---


_Rotation 2026-08-21 (v54 hub, beat 10): the 21 entries below the R-1/R-2 DELIVERED entry (M9.5-DURd-B′ DELIVERED 2026-08-08 and older) + the pre-R1R2 frontmatter chain → `archive/coder-handoff-entries-rotated-2026-08-21.md` (verbatim; order preserved; prior rotations 2026-07-26 / 2026-07-10 / 2026-06-21 + the monthly files). In-entry supersession pointers ("the entry below") resolve there. Zero content loss._
