<!--
file: context/handoff/coder-handoff.md
purpose: Coder session continuity — current task, deferred build gate, next WU, recent closeouts.
audience: Coder, PM
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-09-20 (hub, v77 beat 7 — AUTO-ID-1 LANDED `13d439f` GREEN at CI (the seam: `AutomationIdentityCompanion` + `CompanionAutomationIdentityStore` in `core/automation`; `FileAutomationIdentityCompanion` in lifecycle at `configDir.resolve("automations.ids.yaml")`; T1 on `RealCoreFixture` red → green; `hero_id_stable=true`; deployed to the bench card by installDist 15:52Z, the file there unread until Monday's `IDS:`); MEASURE-2b `a6b56d6` and FE-115 `c819a02` LANDED green (the numbers in the plan §21.4; IR-39 answered in code by AUTO-ID-1, IR-40 open); the closure counter 11/20; the Deferred Build Gate RESOLVED by CI on each landing sha; IR-44 open for the next `app/` touch; NEXT WU **LINK-READ** (D-v76-10; chartered v78 Monday through the gate against `RealCoreFixture` at `13d439f`; the record's definition in the v76 b6 audit / the plan §17 (4); IR-36/37 the rig-blindness rows as its §); the Java slot in series; Sun 2026-09-20 ~11:1x CT (instrument 2026-09-20T16:14:54Z).) Prior: 2026-09-19 (hub, v77 beat 4 — MEASURE-2b DELIVERED and ACCEPT (uncommitted on the desk: core 8 M + 2 A; `RealCoreFixture` exists — the boot shape of three ITs; medians ms at 10k/100k/500k: q1 1.8/10.6/90.5 (O(N), IR-40) · q2 0.4/0.9/4.2 · q3 0.1 · q4 0.2/0.5/3.2 · q5 0.2/0.5/3.4 · q6 0.1–0.2 · restart 161/545/1611; F-1 the identity re-mint IR-39, F-2 IR-40 open; Deferred Build Gate: `./gradlew check` owed to CI on the landing sha) and FE-115 DELIVERED and ACCEPT (web-ui 18 M + 4 A; `frontend.yml` owed to CI); NEXT WU **AUTO-ID-1** (AMD-93 §2.3's identity companion; a restart IT on the fixture), then LINK-READ; Sat 2026-09-19 ~20:5x CT (instrument 2026-09-20T01:50:14Z).) Prior: 2026-09-18 (hub, v76 beat 6 — THE v76 CLOSE: ENERGY-READ LANDED `d1c2cbc`, CI #253 GREEN at the instrument, the gate RESOLVED; NEXT WU MEASURE-2b (hub-authored at v77 — the read path at run-scale on a real store; the shared boot fixture; ENERGY-READ-b as row 0; the rig-blindness sweep), then LINK-READ as a second session). Prior: 2026-09-18 (hub, v76 beat 5 — ENERGY-READ RETURNED and ACCEPT (the b5 audit); the tree 22 = 16 M + 6 A on the desk, the core card in Nick's hands; DEFERRED GATE = CI on the landing; NEXT WU LINK-READ (hub-authored) with ENERGY-READ-b's two rows riding it). Prior: 2026-09-17 (hub, v75 beat 8 — THE v75 CLOSE: the DUR-1 + HASH-1 gate RESOLVED, `be4788a` green at the instrument; IR-22/23 retired; NEXT WU ENERGY-READ, chartered by v76 b1 through the premise gate on DEVICE-SET + ZIGBEE-GAPS rows 3/7; FE-115 at v76 b3). Prior: 2026-09-17 (hub, v75 beat 7 — DUR-1 + HASH-1 LANDED `be4788a`; run #251 red at the upload steps only, the verdict open on the `Run check` glance; CI-2 on the card; NEXT WU ENERGY-READ). Prior: 2026-09-16 (hub, v75 beat 6 — the DUR-1 + HASH-1 DELIVERED entry prepended [newest; ACCEPT; the deferred gate = CI on Nick's landing]; NEXT WU ENERGY-READ on DEVICE-SET's rows, FE-115 at CI green). Prior: 2026-09-15 (hub, v75 beat 5 — the MEASURE-1 DELIVERED entry prepended [newest; both probes measured; the tests uncommitted]; NEXT WU DUR-1 + HASH-1 DISPATCH-READY → ENERGY-READ). Prior: 2026-09-15 (hub, v75 beat 4 — the EXPLAIN-114c DEFERRED GATE RESOLVED at core `e56f555` CI green; NEXT WU MEASURE-1 → DUR-1 + HASH-1 → ENERGY-READ; the plan of record `context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md`). Prior: 2026-09-14 (hub, v75 beat 3 — the EXPLAIN-114c DELIVERED entry prepended [newest, authoritative for the CORE lane; supersedes EXPLAIN-114b, landed at fed99e8]; the hub files the entry per the lane contract; next WU DUR-0 on `DUR: measure`, then ENERGY-READ). Prior: 2026-09-13 (hub, v73 beat 3 — the EXPLAIN-114b DELIVERED entry prepended [newest, authoritative for the CORE lane; supersedes BUS-ORDER-1, landed at a458a64]; the hub files the entry per the lane contract; next WU EXPLAIN-114c). Prior: 2026-08-23 (Coder, R-7b version-scheme closeout).
-->

# Coder Session Handoff

> **CURRENCY STAMP (v59 close, 2026-08-31):** every entry below is HISTORICAL. Core HEAD **`f519f42` CLEAN** — PKG-SEC-1 LANDED `c368909` (CI green, run 33346067195; OR-TOKEN-MODE-644 CLOSED) and FE-HONEST-1 LANDED `f519f42` (frontend.yml 33418520416 green). run-smoke ok-sites = 23 (the 19-PASS / 18/18 counts below are historical, never re-pin). Next core WUs charter at R-10: the CG-1/2/3 additive contract amendment · the F-R4-1 adoption-gap ruling · PKG-SEC-2 (`context/planning/2026-08-30_R10-docket_additions_from-R4.md`).

---

## ENERGY-READ — metering by clusters; the divisors at adoption; scaled reporting; the rate instrument (CORE repo, integration/integration-zigbee; 22 = 16 M + 6 A, UNCOMMITTED on the desk — the b5 card lands it) — DELIVERED (Fri 2026-09-18 ~14:4x CT; NEWEST — authoritative for the CORE lane; supersedes the DUR-1 + HASH-1 DELIVERED entry below)

> **DEFERRED GATE — RESOLVED at v76 beat 6: core `d1c2cbc`, CI #253 GREEN at the instrument (id 35394284061: `Run check` ✓, the uploads ✓, `Run the bus soak` ✓ → the closure counter 8/20); install-smoke #61 both arches ✓ (the b6 audit §0).** The desk's gate line was green in one round (659 zigbee tests, 1 skipped by design; the app's 30 incl. the ArchUnit rules; spotless clean) — the return `context/audits/2026-09-18_ENERGY-READ_return.md` (17,555 B); the hub's audit `context/audits/2026-09-18_v76-b5_ENERGY-READ_intake_two-layer-audit.md`.
> **The delta:** `EndpointClassifier` (IR-15's zone map; `withMeters` by the clusters present; the classification INFO line) · `MeteringFormatting` (A) · `ElectricalMeasurementHandler` (A) · `MeteringHandler` (A) · `ClusterHandlers` (8/9/10) · `ReportingOps.readAttributes` + `EzspReportingOps` (the read; the uint48 change field's encode AND read-back widened to `long` — a HEAD wire defect the lane measured) · `ReportingConfigurator` (the metering rows out of DEFAULTS; the formatting read on the adoption drive, the cache on re-link; 1 W / 5–600 s and 5 Wh / 5–3600 s scaled by the read pair; unreadable or non-kWh → not configured) · `ZigbeeDeviceCache` (the `learnedMeteringFormatting` section) · `ZclIngestionUnit` (the seed/hook; `agreedAcross`; `metering_unscaled` once) · `ZigbeeIntegrationAdapter` (the wiring) · `MODULE_CONTEXT.md` · the rig fixture · 9 test classes. MEASURE-2: `rate=24031.1` events/s in memory (record rows M2-1..3).
> **Deviations ruled (the b5 audit §3):** R-1 the R6 line without `device=` and R-5 `SENSOR` for a metering-only endpoint → **ENERGY-READ-b** (two rows: the call-site log; `ENERGY_METER` when 0x0702 and no other arm) rides the next Java-slot session; R-2 the values ride INFO `zigbee.metering_formatting_read`; R-3 the MEASURE-2 knob is the env var; R-4 the wire defect FIXED; R-6 → IR-32.
> **NEXT WU (refuse-to-close pointer; re-cut v76 b6 — D-v76-10):** **MEASURE-2b** — hub-authored at v77 (the read path at 10k/100k/500k rows on a real SQLite store; the `BusSoakIT.bootAndAdopt` boot shape extracted into one shared fixture; ENERGY-READ-b's two rows as row 0; the rig-blindness sweep as a §), then **LINK-READ** as the second session (IR-20's per-device frame counter at `onFrame`; the sampling rule; `link-quality.yaml`'s asserts). THE ADOPTION FENCE lifts when CI is green on the landing: the first metering adoption is Thu 2026-09-24's CHAR session (record rows G4-1..4, REP-1).

## DUR-1 + HASH-1 — the run on expiry; the ordered views (CORE repo, core/automation; 14 = 12 M + 2 A, UNCOMMITTED on the desk — the b6 card lands it) — DELIVERED (Tue 2026-09-15 ~19:0x CT; NEWEST — authoritative for the CORE lane; supersedes the MEASURE-1 DELIVERED entry below as lane-newest)

> **DEFERRED GATE — RESOLVED at v75 beat 8: core `be4788a`, run #251's `Run check` step GREEN at the instrument (BUILD SUCCESSFUL 3m 14s, 156/156 — Nick's screenshot; the desk's `check --continue` green ×2); only the upload step red (the org's artifact-storage quota — CI-2 on the close card). IR-22/IR-23 RETIRED at the sha; the freeze doc's HASH NOTE a fact; FE-115 unblocked. The counter did NOT sample #251. (Was: hub audited ACCEPT at v75 beat 6 → Nick's card → CI = the gate of record.)** the return `context/audits/2026-09-15_DUR-1_HASH-1_return.md` (7,074 B) · the audit `context/audits/2026-09-17_v75-b6_DUR1-HASH1_DEVICE-SET_intakes_activate-recut_audit.md` §1. The lane's desk gate: the gate line green in ONE round (automation 250 = 243 + 7; rest-api 157, 3 skipped; lifecycle compiled; spotless green); Nick's own clean build (`./scripts/clean.sh` + the gate line, 96 tasks) green; the eight-launch loop one hash per definition, twice (the lane's tee'd file re-tallied by the hub; Nick's second loop read). **Resolved when CI is green on the landing commit** — then IR-22 and IR-23 RETIRE and the freeze doc's [v1.1.5] HASH NOTE is re-cut from a prediction to a fact.
> **The delta:** `StandardAutomationRegistry` (a forDuration trigger also indexed under `trigger_duration_expired`) · `StandardTriggerEvaluator` (the expired-payload IMMEDIATE match keyed on `automationId` + `triggerIndex`, forDuration permits only, `continue` after; the `EventTrigger` carve-out — R1, ACCEPTED by the hub: a definition on that event type is loadable and fired at HEAD; `forDurationOf` package-private) · the eight records (ordered immutable views over `Set.copyOf`/`Map.copyOf`; the same `toString()` format — T4 proves every single-element hash unchanged) · `MODULE_CONTEXT` · `DurationTimerTest` (T1 green; T1b the expired envelope is the run's triggering event, correlation = the timer's; T1c REPLAY initiates nothing) · `DefinitionHashOrderProbeTest` (A) · `DefinitionHashStabilityTest` (A). **Findings banked:** a second redelivery of the same expired envelope reaches the initiator again — dedup is `StandardRunManager`'s C2 key `(automationId, triggeringEventId)` `:222–:225`, pinned not fixed; `Set.copyOf` routes a mutable collection through a `HashSet` of enum identity hashes (per JVM) before `Set12` — the salt's mechanism; hash fixtures mint identities with `Ulid.parse` (the lane's coder-lessons entry).
> **NEXT WU (refuse-to-close pointer — SUPERSEDED at v76 b5 by the ENERGY-READ entry above):** **ENERGY-READ** — hub-authored on DEVICE-SET's rows and ZIGBEE-GAPS row 3 (`context/research/2026-09-17_ZIGBEE-GAPS_return.md` — metering blocks 10.4 % of the corpus; row 7 widens IR-24 to classify-by-clusters) (`context/research/2026-09-15_DEVICE-SET_return.md`): IR-15 first (the IAS zone types); the two handlers 0x0B04/0x0702; **IR-24** metering attached by CLUSTERS present, not device type (the Gen4 is 0x010A); **IR-25** the reportable change scaled by the divisor read at interview; the divisor value read at the first read (no prediction); THE MEASUREMENT RECORD minted; the fixture = the two Gen4 units + the second-vendor plug + the reference meters of `DEVICES: core`. FE-115 at this landing's CI green (the web-ui slot). Then LINK-READ (IR-16, IR-20). The register: `context/planning/improvement-register.md`.

## MEASURE-1 — DUR-0 and HASH-0, two red probes, no production change (CORE repo, core/automation; tests only, UNCOMMITTED on the desk) — DELIVERED (Tue 2026-09-15 ~17:0x CT; NEWEST — authoritative for the CORE lane; supersedes the EXPLAIN-114c DELIVERED entry below as lane-newest — 114c landed at e56f555, CI #249 green)

> **The measurements (hub audited ACCEPT at v75 beat 5 — `context/audits/2026-09-15_v75-b5_MEASURE-1_intake_DUR1-HASH1_DEVICE-SET_activate_audit.md` §1; the return `context/audits/2026-09-15_MEASURE-1_return.md`, 5,107 B):** DUR-0 RED at `3d40b5f` — `Expected size: 1 but was: 0` (zero `initiateRun` calls after the expiry; the expired envelope redelivered through `onEvent` in LIVE) → IR-22 is a DEFECT. HASH-0 — eight fresh JVMs, two hashes per source: the two-role selector (`Set.copyOf`) AND the two-parameter command action (`Map.copyOf`), the salts flipping independently → IR-23 is a DEFECT on eight records. Census 2 = 1 M + 1 A (`DurationTimerTest.java` +T1; `DefinitionHashOrderProbeTest.java`), nothing under `src/main`; **the two files stay on the desk and land with their fixes — never alone (T1 is red by design).**
> **NEXT WU (refuse-to-close pointer — SUPERSEDED at v75 b6 by the DUR-1 + HASH-1 entry above):** **DUR-1 + HASH-1** in ONE coder session (`context/instructions/2026-09-15_coder-lane_DUR-1_and_HASH-1_the-run-on-expiry_and-the-ordered-views_coding-instruction.md`, DISPATCH-READY v75 b5 through THE PREMISE GATE; on Nick's `HASH1: a`, silence = (a)): the registry indexes a for_duration trigger also under `trigger_duration_expired`; the evaluator matches the expired payload to its trigger as an IMMEDIATE match (initiation stays LIVE-only); the eight `Set.copyOf`/`Map.copyOf` records store ordered immutable views in the same `toString()` format; `DefinitionHashes` untouched; T1/T3 red → green, T4 the single-element preservation pins recorded at the baseline; census 14 = 12 M + 2 A. Then **ENERGY-READ** (IR-15 first; the two handlers 0x0B04/0x0702; DEVICE-SET's plugs and reference meter as the fixture; THE MEASUREMENT RECORD minted) — hub-authored on DEVICE-SET's return. FE-115 after HASH-1 is green. The register: `context/planning/improvement-register.md`.

## EXPLAIN-114c — the by-id read (`locateTriggered` through the run id's own timestamp: one bounded `readByTimeRange` window, the type scan as the fallback), `conditions[].definition` (v1.1.5, additive; structured, hash-guarded, present-null), `FIRED_CONFIRMED` only on confirmation evidence, the `listRuns` cursor stop, the typed scan visitor (CORE repo, core/automation + api/rest-api) — DELIVERED (Mon 2026-09-14 ~18:1x CT; NEWEST — authoritative for the CORE lane; supersedes the EXPLAIN-114b DELIVERED entry below as lane-newest — 114b landed at fed99e8)

> **DEFERRED GATE — RESOLVED at v75 beat 4: landed core `e56f555`, CI run #249 GREEN (the counter 6/20); IR-1/2/3 RETIRED at the sha; the freeze doc stamped v1.1.5. (Was: hub audited ACCEPT at v75 beat 3 → Nick's card → CI = the gate of record.)** the return `context/audits/2026-09-14_EXPLAIN-114c_return.md` (14,568 B) · the audit `context/audits/2026-09-15_v75-b3_114c-and-FE-114_intake_two-layer-audit.md`. The lane's desk gate: `./gradlew :core:automation:test :api:rest-api:test :lifecycle:lifecycle:compileJava spotlessCheck --offline` green in one round (automation 243 = 232 + 11 · rest-api 157 = 156 + 1, 3 skipped) and again after two review fixes; the hub could not run Gradle (disclosed). **Resolved when CI is green on the landing commit.**
> **The delta (10 = 8 M + 2 A; the wire byte-identical outside ONE key — `conditions[].definition` appended last; `readByTimeRange(` 0 → 1, `readByType(` stays 1, `"fired and confirmed at"` 2 → 0):** `StandardExplanationService` (K2 · K3 · DP-B2 · IR-2 · IR-3) · `RunExplanation` (`ConditionView` 4 → 5; `ConditionDefinitionView`) · `ConditionDefinitionRenderer` (A) · `GetRunCausalChainEndpoint` (the last key) · the three test classes + `ByIdReadPageCountTest` (A, IR-1's instrument as the guard: 2 / 40 / 100 type pages before, 1 window page after) · the two MODULE_CONTEXTs. **Corrections banked from the return:** the fixed-clock seed is a hint HIT at the old cost, the displaced run id is the fallback (R1); the marker precedes the run id's instant — the 1-s LEAD is the operative knob (O1); one assertion beyond the named flips (R2); `DefinitionHashes` set-order instability → IR-23 (O4).
> **NEXT WU (refuse-to-close pointer — SUPERSEDED at v75 b5 by the MEASURE-1 entry above):** **MEASURE-1** (`context/instructions/2026-09-15_coder-lane_MEASURE-1_DUR-0_and_HASH-0_two-red-probes_coding-instruction.md`, DISPATCH-READY v75 b4 — DUR-0 the RED run-after-expiry test; HASH-0 the eight-launch hash probe over a two-role selector AND a two-parameter command action; no production change), then **DUR-1 + HASH-1** in one coder session (D-v75-5 (a): ordered immutable views in the eight `Set.copyOf`/`Map.copyOf` records; the run initiated on expiry), then **ENERGY-READ** (IR-15 first; the two handlers; the 0x0051 arm; the dossier fixture; THE MEASUREMENT RECORD minted) — hub-authored. K1 (`cascade.parentRunId`) waits on `CASCADE:` (D-v75-3); HASH-1 (IR-23, a DEFECT — D-v75-5) rides the DUR-1 session; FE-115 renders `conditions[].definition` only after HASH-1 is green. The register: `context/planning/improvement-register.md`.

## EXPLAIN-114b — the maintainability landing under the v1.1.4 freeze: ONE type-index scan (`scanType`) for the five walks of `StandardExplanationService`, ONE `NonFiringExplanation` constructor (the rest-api test fixture `NonFiringExplanations`), the two MODULE_CONTEXT phrases (CORE repo, core/automation + api/rest-api test tree) — DELIVERED (Sun 2026-09-13 ~15:1x CT; NEWEST — authoritative for the CORE lane; supersedes the BUS-ORDER-1 DELIVERED entry below as lane-newest — BUS-ORDER-1 landed at a458a64)

> **DEFERRED GATE (hub audited ACCEPT at v73 beat 3 → Nick's card → CI = the gate of record; the `bus-soak` sample after the landing advances the closure counter):** the return `context/audits/2026-09-13_EXPLAIN-114b_return.md` (9,978 B) · the audit `context/audits/2026-09-13_v73-b3_EXPLAIN-114b_intake_two-layer-audit.md`. The lane's desk gate: `./gradlew :core:automation:test :api:rest-api:test :lifecycle:lifecycle:compileJava spotlessCheck --offline` green in one round (automation 232 · rest-api 156, 3 skipped); the hub could not re-run Gradle on the device (disclosed). **Resolved when CI is green on the landing commit.**
> **The delta (7 = 6 M + 1 A; wire byte-identical — `readByType(` 5 → 1, `public NonFiringExplanation(` 3 → 0, zero assertion lines changed in the rest-api tests):** `StandardExplanationService` (`scanType` + `lastMatching`; five visitors; the early stop kept) · `NonFiringExplanation` (three conveniences deleted) · `StandardExplanationServiceTest` (T1–T3: the first page-boundary tests, through a counting `EventStore`) · `NonFiringExplanations` (A, the fixture) · `AutomationEndpointsTest` (8 sites through the fixture) · the two MODULE_CONTEXTs.
> **NEXT WU (refuse-to-close pointer):** **EXPLAIN-114c** — the v1.1.5 additive keys (`parentRunId`, condition text, the by-id read, `forDuration` `firingValue`) + D4's DP-B2 sentence + the register rows IR-2/IR-3 — hub-authored after the freeze note and HERO-1c's needs. DISABLED-2 stays on the formal path (D-v73-1). The register: `context/planning/improvement-register.md`.

## BUS-ORDER-1 — the read-forward LIVE delivery (AMD-101 §2's realization): the cursor on `SubscriberRuntime`, `notifyEvent` a wake hint with no checkpoint guard, `liveLoop` = `readFrom(cursor, liveReadBatch)` + the idle tick, the flip hands the cursor, `EventBusConfig` 2 → 4, `InProcessEventBus.lastDelivered`, the census on the cursor (CORE repo, core/event-bus + lifecycle test) — DELIVERED (Sat 2026-09-12 ~22:2x CT; NEWEST — authoritative for the CORE lane; supersedes the FIX-2b-ii (i) DELIVERED entry below as lane-newest — FIX-2b-ii (i) landed at 72efb42, EXPLAIN-114a at 5f918c7)

> **DEFERRED GATE (hub, then Nick, then CI = the gate of record AND the first `bus-soak` sample after the landing = P5):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-12_BUS-ORDER-1_return.md` §0-first + this entry). **The return carries FOUR [REVIEW], eight [INFO] and one observation:** R1 `ReplayDriver` +2 lines (the cursor's init from the checkpoint + its advance per paged row — outside the Files table; without it the drain re-delivers REPLAY's positions) · R2 `EventBusConfig` gains a 2-arg convenience ctor (row 4's "four sites updated" vs row 7's test fence: three sites are `EventBusConfigTest`) · R3 `TRANSITION_READ_EMPTY`/`_EXHAUSTED` retire with the drain's single-position read, T-R2 re-pinned, the coordinator's emitter parameter removed · R4 INV-BUS-01's `lastReplayedPosition` clause → the cursor (one line for the docs card) · I1–I8 · **O1 the spent unpark permit** — the first K=200 soak read `p99_ms=553` (49/200 loops at one idle tick, 4 at two; a JDK 21.0.4 probe: a permit landing while the VT is parked in its own `Future.get()` is spent there), fixed by parking only when the hint queue is empty, re-measured `p99_ms=35`, pinned by `ReadForwardDeliveryTest` T5, lesson filed. **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 16 atop `5f918c7`.
> **The delta (16 = 14 M + 2 A; `git diff --stat -- '*module-info.java'` EMPTY; nothing under core/automation, api/, web-ui/, HomeSynapseCore, build files):** `SubscriberRuntime` (+`cursor()`/`advanceCursor` CAS-max, `wake(long)`, `drainHints()`) · `InProcessEventBus` (`notifyEvent`/`offerUnfiltered` route without a checkpoint read for active subscribers — kind E unreachable; `liveLoop` = drain hints → `readFrom(cursor, batch)` → `deliverStep` per envelope → park the tick only if no hint arrived; `deliverStep` + `StepOutcome` package-private static shared with the drain; `lastDelivered(String)` public; `readLivePosition` + the FIX-1b constants removed) · `TransitionCoordinator` (hints → `readForwardToHead` → the CAS under the queue lock; ctor `(…, clock, int readBatch)`; `readQueuedPosition` + constants removed) · `ReplayDriver` (+2) · `EventBusConfig` (`liveReadBatch` 64 · `liveIdleTick` 250 ms · the 2-arg form) · `DeliveryAnomaly` (javadoc: five kinds retained, `NOTIFY_NOT_VISIBLE` + `LIVE_READ_FAILED` still emitted) · A `ReadForwardDeliveryTest` (T1 · T2 · T2b · T3 · T5) · A `EventBusConfigLiveFieldsTest` (5) · `DeliveryAnomalyEmissionTest` re-pinned to the page semantics (T1/T2/T4/T5/T6/T-R2; T3/T7/T-R1 unchanged in substance; the scripted store scripts + counts PAGE reads by `afterPosition`) · lifecycle test: `BusPositionCensusIT` (`census(events, snapshots, cursorOf)`, `delivered_max=` beside `checkpoint=`, `concreteBus(core)`, `headAndTail` → `bus.head:`/`bus.tail:` on every timeout) · `BusAwaitDiagnostic` (` cursor=` after ` checkpoint=`, ` pending=` → ` hints=`) + its test · `BusSoakIT` (one call site) · `HeroLoopHardwareFreeIT:700` (the `render` caller) · both MODULE_CONTEXTs.
> **Measured (P1–P4 HELD, `--rerun --offline`):** compile-red 02:23Z (16 + 6 errors: the unbuilt seams) → behavior-red 02:26Z (18 run / 9 failed — T1 `[1L, 3L]` vs `[1L, 2L, 3L]`, T2 the drain dropped P+1 and P, T3 `within 400 ms`, the six DAE re-pins) → green 02:34Z → T5 red 02:59Z → green 03:01Z → gates forced fresh 03:04Z: **event-bus 240**/30 skipped · **persistence 413** · **lifecycle 81** · **app 30**, `spotlessCheck` ×2 green, zero `warning:` → the verbatim soak line (K=200, `-PvtParallelism=2`) 03:04:52Z: `bus.soak: loops=200 ok=200 timed_out=0 p50_ms=26 p99_ms=35 max_ms=35 anomalies=0` · every scored subscriber `missed=0` · `unscored_missed=0` · kind E 0 · `automation.run_handoff` 200; `automation_engine checkpoint=2414 delivered_max=2414`; the type-filtered subscribers' `delivered_max` sits above their checkpoint by the trailing non-matching span (by design, §5).
> **NEXT WU (refuse-to-close pointer):** the hub's intake against AMD-101 §2 sentence by sentence (rules R1–R4) → the msg file + census card (hub-authored) → Nick's commit (stages EXACTLY the 16) + main push → **`check` GREEN = the gate of record; the first `bus-soak` artifact after the landing shows `delivered_max=` on every census line and no kind-E line (P5)** → OR-BUS-SILENT-DROP's closure counter restarts at the landing (twenty consecutive green samples with kind E at zero — the hub's count, not the lane's) → `AMD101: ratify` (the §4 edit + R4's INV-BUS-01 clause, one docs card) → HERO-1c / EXPLAIN-114b per the v72 order. Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING. This lane HOLDS until the audit verdict or the next instruction.

---

## FIX-2b-ii (i) — the run hand-off named: lines A–D in `core/automation` (`automation.run_handoff` · `run_body_entered` · `run_thread_died` · `action_step_started`) + kind E `NOTIFY_SKIPPED_LIVE` at the bus's offer guard + the hero IT asserting once per loop + the `automation.handoff_census:` line on both soak timeouts (CORE repo, core/automation + core/event-bus + lifecycle test) — DELIVERED (Sat 2026-09-12 ~13:3x CT; NEWEST — authoritative for the CORE lane; supersedes the CI-1 DELIVERED entry below as lane-newest — CI-1 landed at ff1a6e1)

> **DEFERRED GATE (hub, then Nick, then CI = the gate of record AND the first `bus-soak` sample after the landing = §7's criterion):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-12_FIX-2b-ii-i_return.md` §0-first + this entry). **The return carries ZERO [REVIEW], seven [INFO] and one observation:** I1 the census values are COUNTS (`2` = a double hand-off), not `<0|1>` · I2 `BusPositionCensusIT.timeoutDiagnostic` +`List<ILoggingEvent>`; `attachLineCapture`/`homesynapseLogger` hoisted there, the soak's two privates removed · I3 the `logHandoff` helper · I4 the hero IT keys on the `automation_triggered` payload runId · I5 one `DeliveryAnomaly` javadoc clause · I6 javadoc at the changed sites · I7 `core/event-bus` + `lifecycle` MODULE_CONTEXTs untouched (the 8-file census; the hub's kind-E delta + the census grammar are theirs) · O1 caller-thread notify (`NotifyingEventPublisher:48/:56`) can reorder two publishers' notifies — a kind-E mechanism that misses BELOW the checkpoint, not sample #11's at-the-head shape. **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 8 atop `ff1a6e1`.
> **The delta (8 = 8 M + 0 A; `git diff --stat -- '*module-info.java'` EMPTY):** `StandardRunManager` (line A after each of the four `publishTriggered`s — for admitted/drained BEFORE `vt.start()`; line B first in `runBody`; line C the builder's `uncaughtExceptionHandler`, observation-only — the `Error` still escapes, `finalizeRun` still does not run) · `StandardActionExecutor` (line D before `publishStarted`) · `DeliveryAnomaly.Kind.NOTIFY_SKIPPED_LIVE` · `InProcessEventBus` (`checkpoint >= globalPosition` AND `runtime.mode() == LIVE` → `emitAnomaly(NOTIFY_SKIPPED_LIVE, <subscriber>, <position>, "notifyEvent: checkpoint=<c> at or past position")`, then the same `continue`) · `HeroLoopHardwareFreeIT` (a `ListAppender` on `LoggerFactory.getLogger("com.homesynapse")` for the confirm test, attached before boot, detached in `@AfterEach`; exactly 1 hand-off `mode=admitted`, exactly 1 body, ≥1 `index=0` step, all on the triggered runId; 0 died; 0 `NOTIFY_SKIPPED_LIVE`) · `BusSoakIT` + `BusPositionCensusIT` (the capture at the `com.homesynapse` parent; `automation.handoff_census: runId= handoff= body= step0= died=` after the FIX-2b-i reading on every timeout path, in the stdout AND the thrown message) · `core/automation/MODULE_CONTEXT.md` (one GOTCHA: the VT name form, A–D and their sites, the `RuntimeException`-only catch, the census grammar).
> **Measured (P1–P2 HELD, `--rerun --offline`):** red at HEAD 18:21Z (`[automation.run_handoff] Expected size: 1 but was: 0`, 5 completed / 1 failed, `compileTestJava` executed) → green 18:22Z 5/5 with the four tokens in `<system-out>` → gates 18:25Z: `:core:automation:test` **211** · `:core:event-bus:test` **230** · `:lifecycle:lifecycle:test` **81**/19 (the gate shape) · `:app:homesynapse-app:test` **30** (the arch rules) · `spotlessCheck` ×3 green → the tagged pair (`-PincludeBusSoak`) 2/2: `bus.soak: loops=20 ok=20 timed_out=0 p50_ms=26 p99_ms=35 max_ms=35 anomalies=0`; `grep -c 'automation.run_handoff'` on the soak XML = **20** = the loop count (§7's criterion, on the desk).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the msg file + census card (hub-authored) → Nick's commit (stages EXACTLY the 8) + main push → **`check` GREEN = the gate of record; the first `bus-soak` artifact after the landing = §7's criterion (`automation.run_handoff` × loops in the soak XML's `<system-out>`)** → part (ii) dispatches ONLY on a red that names its §7 row (the `automation.handoff_census:` shape + `NOTIFY_SKIPPED_LIVE` present/absent) → EXPLAIN-114a after (i). Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING. This lane HOLDS until the audit verdict or the next instruction.

---

## CI-1 — the bus-soak amplifier OFF the gate, kept as a sample source: `@Tag("bus-soak")` on `BusSoakIT` + `BusPositionCensusIT` · the conventions' `useJUnitPlatform { excludeTags("bus-soak") }` unless `-PincludeBusSoak` · the non-gating `bus-soak` CI job (CORE repo, lifecycle test + build-logic + .github/workflows) — DELIVERED (Sat 2026-09-12 ~09:5x CT; NEWEST — authoritative for the CORE lane; supersedes the FIX-2b-i DELIVERED entry below as lane-newest — FIX-2b-i landed at bd5d35e, HERO-1b at 6af76f7)

> **DEFERRED GATE (hub, then Nick, then CI = the gate of record AND the `bus-soak` job's first artifact):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-12_CI-1_return.md` §0-first + this entry). **The return carries ZERO [REVIEW] and three [INFO]:** I1 — `lifecycle/lifecycle/MODULE_CONTEXT.md:245`'s "How to run" line now needs `-PincludeBusSoak` (MEASURED: `--tests '*BusSoakIT*'` without it → `No tests found for given includes`); the 4-file census forbids the fifth file — the one-token doc delta is the hub's at commit or docket · I2 `spotlessCheck` not run (the instrument limit; the Java delta is one import + one annotation per file, header untouched) · I3 the exclusion reaches every module's Test task, harmless (the tag lives in the two lifecycle classes only). **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 4 atop `6af76f7`.
> **The delta (4 = 4 M + 0 A; zero `src/main`; the `check` job byte-untouched):** `@Tag("bus-soak")` directly under each IT's `@DisplayName` (+ the `org.junit.jupiter.api.Tag` import); the conventions' `Test` block `useJUnitPlatform { if (!project.hasProperty("includeBusSoak")) { excludeTags("bus-soak") } }` (a PROJECT property, `-P` not `-D`); `ci.yml` +39: the `bus-soak` job (`Bus soak (non-gating sample)`, `ubuntu-latest`, 15 min, `continue-on-error: true`; checkout / JDK 21 corretto / setup-gradle byte-identical to `check`'s; `run: ./gradlew :lifecycle:lifecycle:test --tests '*BusSoakIT*' --tests '*BusPositionCensusIT*' -PincludeBusSoak --no-daemon`; `if: always()` upload of `lifecycle/lifecycle/build/test-results/` + `reports/tests/` as `bus-soak-${{ github.run_number }}`, 14 days). Triggers unchanged — PR and dispatched runs also produce a sample.
> **Measured (P1–P3 HELD, `--rerun --offline`):** HEAD 83/21 → R1 (no property) **81/19** green, the hero IT's XML present, the two ITs' absent (`:build-logic:compileKotlin` executed) → R2 (`-PincludeBusSoak`) **83/21** green (`bus.soak: loops=20 ok=20 timed_out=0 p50_ms=26 p99_ms=27 max_ms=27 anomalies=0`, `bus.delivery_anomaly` 0); R4 = the job's exact `run:` line → 2/2 green; PyYAML parses `jobs = [check, bus-soak]`, the setup steps list-equal to `check`'s.
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the msg file + census card (hub-authored) → Nick's commit (stages EXACTLY the 4) + main push → **`check` GREEN without the two ITs = the gate of record (the hero IT's own red rate returns); the `bus-soak` job's artifact `bus-soak-<run_number>` = the next sample of the class, read from its `BusSoakIT.xml` / `BusPositionCensusIT.xml` (`pending=` + `bus.thread:` lines on a red), never from the check mark** → FIX-2b-ii authored on the first depth+dump-bearing red → EXPLAIN-114a. Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING. This lane HOLDS until the audit verdict or the next instruction.

---

## FIX-2b-i — the stall gets a queue depth (A: `SubscriberSnapshot.pendingDepth` → `pending=` on the diagnostic and the census) and a thread dump (B: `BusThreadDump` at every timeout path, virtual threads included) + the conventions' two lines (C: `-Djdk.tracePinnedThreads=short` · the `-D` soak passthrough) (CORE repo, core/event-bus + api/rest-api test + lifecycle test + build-logic) — DELIVERED (Fri 2026-09-11 ~21:58 CT; NEWEST — authoritative for the CORE lane; supersedes the FIX-2a DELIVERED entry below as lane-newest — FIX-2a landed at cc05a54)

> **DEFERRED GATE (hub, then Nick, then CI = the gate of record AND the runner's first depth+dump sample):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-11_FIX-2b-i_return.md` §0-first + this entry). **The return carries ONE [REVIEW]:** R1 — row C's passthrough is shipped in the CONDITIONAL form (`providers.systemProperty("homesynapse.soak.loops").orNull?.let { systemProperty(…, it) }` — the line the FIX-2a return named and the FIX-2a audit docketed), not the instruction's `getOrElse("20")`: the literal sets the property on every run and `BusSoakIT` reads the property FIRST, so the env-var fallback the instruction keeps would be dead code; MEASURED all three arms (`-D…=3` → `loops=3`; env `=3` alone → `loops=3`; neither → `loops=20`). **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 13 atop `cc05a54`.
> **The delta (13 = 11 M + 2 A; `src/main` touched at TWO files):** `SubscriberSnapshot` 6 → 7 components (`int pendingDepth` after `dlqDepth`; the javadoc names it + the `resume()` caveat) · `InProcessEventBus.buildSnapshot` passes `runtime.pendingPositions().size()` · `DlqStatusEndpointTest`'s six sites gain `0` (the frozen A5 envelope untouched — the endpoint builds its map by hand) · `BusAwaitDiagnostic.render` prints ` pending=` right after ` dlq=` (pinned by `BusAwaitDiagnosticTest`: `dlq=1 pending=2 behind=2`) · A `BusThreadDump.capture(dir)`: `<java.home>/bin/jcmd(.exe) <pid> Thread.dump_to_file -format=plain <dir>/threads-<ms>.txt` (10 s bound) → the filtered rendering (`hs-*` names + any thread with `com.homesynapse` in its top 12 frames; `bus.thread_dump:` / `bus.thread: name= state= virtual= tid=` / `bus.thread_frame:` ×≤12), else `Thread.getAllStackTraces()` with `bus.thread_dump: source=platform_only …` + `bus.thread_dump_fallback: reason=`; never throws · A `BusThreadDumpTest` (one test, three arms: the live JVM · the render pinned byte-exact on a synthetic JDK 21 dump · the forced fallback) · the three ITs' `timeoutDiagnostic` print `reading + "\n" + dump` and throw the reading alone (the static is now `timeoutDiagnostic(core, dumpDir, what, awaited)`; each IT holds its `@TempDir`) · `BusPositionCensusIT.SubscriberCensus` +`pendingDepth`, the census line ` pending=<n>` after ` dlq=` · row C's two lines · both MODULE_CONTEXTs.
> **Red-first (P1/P2 HELD, staged):** the tests alone → compile-red 02:48:46Z (3 ctor sites + 3 `BusThreadDump` symbols) → the record in → `DlqStatusEndpointTest` compile-red at exactly `:55 :57 :121 :124 :126 :145` (02:50:07Z) → the sites + the inert seam → rest-api 152/0 + two behaviour-reds (`… dlq=1 pending=2 …` expected; `"" to start with "bus.thread_dump: "`) 02:50:47Z → the render + the real dump → GREEN 02:51:19Z. **P3 HELD** (no timeout fired on the desk; the dump on a real stall is the runner's). **P4 HELD both ways.** Final: event-bus 230 · rest-api 152 · lifecycle 83 (+1) · `spotlessCheck` green on every module; `BusThreadDumpTest` on the desk `bus.thread_dump: threads=22 shown=1 file=…` (jcmd attached; `source=platform_only` absent); every census line `pending=0`; `<== monitors:` 0; `bus.delivery_anomaly` 0. Deviations: [REVIEW] R1 · [INFO] I1 `-format=plain` (JDK 21's domain is `plain|json`) · I2 the `hs-` prefix (superset of `hs-sub-`: adds `hs-read-<n>`/`hs-write-0`) · I3 the trailing ` virtual=`/` tid=` keys and `state=unknown` for VTs (JDK 21's dump carries no state) · I4 the static's `dumpDir` parameter · I5 one test, three arms · I6 the FIX-2a K sentence rewritten in place · I7 the wall-clock sweep. `[FORESIGHT-NOTE]` O1: the dump FILE dies with the `@TempDir`; `build/test-results/test/thread-dumps` would ship the whole file in the CI artifact — one line, the hub's call.
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return (rules R1) → the msg file + census card (hub-authored) → Nick's commit (stages EXACTLY the 13) + main push → **`ci` GREEN = the gate of record; a RED there now carries `pending=<n>` per subscriber and the `bus.thread:` frames of every `hs-*` / `com.homesynapse` thread in the failing test's `<system-out>` (plus the JDK's `<== monitors:` lines if a VT pinned)** → **FIX-2b-ii** (the fix) authored on that first depth+dump-bearing red (the audit's §2 table: `pending ≥ 1` + the engine VT's frames → H1/H2/H3-parked; `pending=0` + a parked loop → H3 fan-out; `Thread.sleep` under the action executor → H4) → EXPLAIN-114a (held behind the gate). Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING. This lane HOLDS until the audit verdict or the next instruction.

---

## FIX-2a — OR-BUS-SILENT-DROP, instrument first: the hero-loop await diagnostic (A) · TR-1b's in-process position census (B) · the K-loop bus soak (C) (CORE repo, the lifecycle TEST tree only — zero `src/main`, module-info untouched) — DELIVERED (Fri 2026-09-11 ~20:0x CT; NEWEST — authoritative for the CORE lane; supersedes the HONESTY-1 DELIVERED entry below as lane-newest — HONESTY-1 landed at 94ae99d, HERO-1 at 1e26912)

> **DEFERRED GATE (hub, then Nick, then CI = the gate of record AND the runner's first soak sample):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-11_FIX-2a_return.md` §0-first + this entry). **The return carries ONE [REVIEW]:** R1 — the `-Dhomesynapse.soak.loops=N` override the instruction names (§4-C, §12) cannot reach the forked test JVM: this build's `Test` tasks declare no `systemProperty` passthrough — MEASURED (`-D…=2` on the gradlew line ran `loops=20`); the test reads the property first as specified AND falls back to the environment variable `HOMESYNAPSE_SOAK_LOOPS` (MEASURED: `=3` → `loops=3`); the one-line build passthrough is in the return for the hub's call (a `build.gradle.kts` row is outside the Files table). **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 6 atop `1e26912` (the gate of record). Desk gates GREEN on the final bytes: lifecycle 79 → 82 (`--rerun`, XML 00:42Z → 00:56Z), `compileTestJava` under `-Xlint:all -Werror`, `:lifecycle:lifecycle:spotlessCheck`.
> **The delta (6 = 2 M + 4 A; zero production code):** A `BusAwaitDiagnostic` (pure `render` — `bus.await_timeout:` + one `bus.await_subscriber:` line per snapshot) + A `BusAwaitDiagnosticTest` (the exact string; 1 test) · M `HeroLoopHardwareFreeIT` (65/3: `awaitTrue` becomes an instance method with a `LongSupplier` sibling; on timeout the reading is printed to stdout and thrown as the message; the step-3 On-frame await names the newest `state_reported occupied=true` position; no assertion changed, no retry) · A `BusPositionCensusIT` (one hero loop → TR-1 §1's census in-process, the FROZEN TR-1 §3 tokens + `checkpoint=/dlq=/atomic=true/dlq_unscored=true` appended after the frozen keys; scored = non-atomic AND `dlq=0`; the census settles ≤10 s before it is scored; the manifest/census/tokens/store-reads are package-private statics the soak shares) · A `BusSoakIT` (K loops in one core, `bus.soak_host:` + `bus.soak:` + the census with `runs=<ok>`; the anomaly `ListAppender` attached BEFORE boot; `System.nanoTime()` ONLY for the latency, named in the class javadoc) · M `lifecycle/lifecycle/MODULE_CONTEXT.md` (the `### FIX-2a` entry under Phase 3 Notes: the three instruments, the grammar, the DLQ-depth limit, how to run, the first readings).
> **Red-first (P1 HELD, two-stage):** the test alone at HEAD → compile-red 00:44:23Z (`cannot find symbol: variable BusAwaitDiagnostic` ×2, `compileTestJava FAILED`) → the class with an inert `render` → behaviour-red 00:44:40Z (`expected: "bus.await_timeout: …" but was: ""`) → the real render → GREEN 00:45:14Z. **P2 FAILED — the INVERSE ARM, first-class:** 7 soak samples at K=20 (140 loops; 3 solo runs of record + the three-class run + the `-D` run + ONE pinned runner-shaped run `available_processors=2 vt_parallelism=2` + the full-suite run) + one at K=3: `timed_out=0 anomalies=0` and scored `missed=0` on EVERY run; `grep -c bus.delivery_anomaly` = 0 on every XML. The class does not reproduce in-process on this desk; the next instrument is the card's driver (TR-1b), not a fix. **P3 HELD:** p50 24–26 ms, p99 28–96 ms. **P4 HELD (green arm):** after one loop `missed=0` for the five non-atomic subscribers AND `state_projection … missed=0 … atomic=true` (checkpoint 24 = head 24); the atomic lag shows only in the soak (`unscored_missed` 0–1).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return (rules R1) → the msg file + census card (hub-authored) → Nick's commit (stages EXACTLY the 6) + main push → **`ci` GREEN = the gate of record; that run's `BusSoakIT.xml` is the RUNNER's first soak sample (2 vCPUs) — read its `bus.soak:` line and `grep -c bus.delivery_anomaly` on it** → TR-1b's driver on the card (the census WITH the DLQ table) and/or FIX-2b (checkpoint-driven LIVE delivery; the AMD to Doc 01 §3.4) authored on these readings → EXPLAIN-114a (held behind this lane) dispatches. Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING (the runner sample). This lane HOLDS until the audit verdict or the next instruction.

---

## HONESTY-1 — four value corrections: `lastReported` seeds null (A) · command-event provenance inherited, the primitive stamps `UNKNOWN` (B) · the `commandEventId` javadoc (C) · install-smoke's push `paths:` filter dropped in both copies (D) (CORE repo, core/state-store + core/automation + integration/integration-api + .github/workflows + distribution/ci) — DELIVERED (Thu 2026-09-10 ~18:2x CT; NEWEST — authoritative for the CORE lane; supersedes the F-R4-1b DELIVERED entry below as lane-newest — F-R4-1b landed at 39c8dd3)

> **DEFERRED GATE (hub, then Nick, then CI = the gate of record AND the sample):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-10_HONESTY-1_return.md` §0-first + this entry). **The return carries ONE [REVIEW]:** R1 — `EntityState.java:73`'s `@param lastReported … never {@code null}` is now false; the Files table did not list the file, so it is NOT edited (the table governs) — the one-line replacement is in the return for the hub to apply at commit or docket. **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 13 atop `39c8dd3` (law 16); on that same push `install-smoke` must RUN with no manual dispatch (P4), both jobs green, `distribution-artifacts-arm64` present = R-4c's `.deb`. In-lane gate GREEN on the final bytes (`--offline`, `--rerun` per test task): `:core:state-store:test` 164→165 · `:core:automation:test` 207→211 · `:core:persistence:test` 412→413 · `compileJava` executed on state-store/automation/integration-api under `-Xlint:all -Werror` · `spotlessCheck` executed on the four touched modules · both YAMLs parse (PyYAML) and stay byte-identical.
> **The delta (13 M + 0 A; zero module-info/build/catalog/schema/wire-key change):** (A) `StateProjection.initialEntityState` → `(…, seed, seed, null, null, false)`; three javadoc/comment sites re-worded; no branch of `applyToState`/the backfill touched (each already carries `prior.lastReported()`). (B) `StandardCommandDispatchService`: `onEvent` passes `event.origin()`/`event.actorRef()`, `dispatch` passes `EventOrigin.UNKNOWN`/`null`, threaded `route → publishDispatched/publishResult → publish` as two parameters (the instruction's recommended shape — no `Provenance` record, no new type) onto the `EventDraft`'s `origin`/`actorRef`; `idempotencyKey` stays `null`; `+ import Ulid` (platform-api, already `requires transitive`). (C) `CommandEnvelope.commandEventId` javadoc = the instruction's text. (D) the `push: paths:` line deleted in BOTH copies; `branches`/`pull_request`/`workflow_dispatch` untouched. Tests: `ReconciliationTest` #4b · `CheckpointSerializerTest.nullLastReportedPreserved` · `CommandDispatchSubscriberTest` T-B1/T-B2/T-B4 · `StandardCommandDispatchServiceTest` T-B3 · `AutomationTestSupport.commandIssued` 6-arg overload (the 4-arg delegates `AUTOMATION, null`; every caller unchanged).
> **Red-first:** HEAD + the tests (23:03Z) → state-store 165 / **1 RED** (#4b `expected: null but was: 2025-09-15T08:30:00Z`) · automation 211 / **3 RED** (T-B1/T-B2 `expected: USER_COMMAND but was: AUTOMATION`, T-B3 `expected: UNKNOWN but was: AUTOMATION`) · persistence 413 / 0 (A-ser and T-B4 green-by-construction, DISCLOSED) → Parts A–D in (23:06Z) → 165 / 211 / 413 all green; ZERO pre-existing tests red (P3 HOLDS). Deviations: [REVIEW] R1 · [INFO] I1 census 13 M, not the predicted 12 (the Files table's YAML row is two files) · I2 `EventEnvelopeAssert.hasOrigin` is not on automation's test classpath — plain AssertJ, no build-file edit · I3 G4's literal grep also lists `SqliteEventStore:467/:532` (`draft.origin()` column writes: a pass-through, not a filter — the gate passes on its intent) · I4 the MC entries carry both dates (ruled 09-07, landed 09-10) · I5 `:integration:integration-api:compileJava` added to the desk gate (a touched module). Observations (return §3): O2 a pre-landing checkpoint keeps the adoption-instant `lastReported` on never-reported entities until they report or the next version-transition rebuild (no bump ordered) · O3 the docs precedence condition is already met (`876a395` on the docs tree) · O4 Check 12 lists two non-HONESTY-1 files still carrying a live `status:` (hub hygiene).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return (rules R1) → the msg file + census card (hub-authored) → the core commit (stages EXACTLY the 13) + main push → **`ci` GREEN = the gate of record (the passive sample) AND `install-smoke` RUNS on that push (P4) with both jobs green + `distribution-artifacts-arm64` = R-4c's `.deb` (Act 12 retired)** → **the v1.1.4 EXPLAIN batch** (the assessment §3). Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING (law #22). This lane HOLDS until the audit verdict or the next instruction.

---

## F-R4-1b — the ZDO `IEEE_addr_req` second surface for interview-on-rejoin (the router-parented sleepy device; R-4b's measured miss arm; docket Row 27) (CORE repo, integration-zigbee) — DELIVERED (Sun 2026-09-06 ~17:40 CT; NEWEST — authoritative for the CORE lane; supersedes the CG-123 DELIVERED entry below as lane-newest — CG-123 landed at f25291b, FE-113 at d192d17)

> **DEFERRED GATE (hub, then Nick, then CI, then R-4c):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-06_F-R4-1b_return.md` §0-first + this entry). **The return carries TWO [REVIEW] the hub rules BEFORE the commit:** R1 — T7 (a pre-existing test) re-fixtured: its reopen leg scripted only the 0x0061 miss and now RESOLVED through the air (the harness's default IEEE_addr_rsp is SUCCESS — a device asked for its own EUI64 answers) → `ieeeAddrStatus = 0x81` beside the table miss + the PAIR pinned (the air re-asked after each reopen: 1, then 2); R2 — +1 test beyond the table (589, not 588): T3e pins DP-6's response-nwk-differs arm and the conditional `response_nwk=` key (LTD-15). **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on Nick's push of exactly the 6 atop `d192d17` (law 16). In-lane gate GREEN on the final bytes (`:integration:integration-zigbee:compileJava :integration:integration-zigbee:test --rerun spotlessCheck --offline` → BUILD SUCCESSFUL 6s; 0 warnings under `-Werror`; XML aggregate **589/0/0**, every result file mtime 2026-09-06T22:39:52Z; **zigbee 582 → 589**). Working tree: core **`d192d17`** (clean at launch) + **EXACTLY 6 at porcelain (6 M + 0 A + 0 D — the Files table exact)**: `integration/integration-zigbee/MODULE_CONTEXT.md` · main `ZdoCodec` · `EzspCoordinatorProtocol` · `ZigbeeIntegrationAdapter` · test `ZdoCodecTest` · `ZigbeeInterviewOnRejoinTest`. `module-info.java` byte-unchanged; ZERO build/catalog/schema/event/`CoordinatorProtocol`/`InterviewOps`/rig diffs; ZERO commits, ZERO staged.
> **The delta:** `ZdoCodec` +2 constants (`CLUSTER_IEEE_ADDR_REQ 0x0001` / `CLUSTER_IEEE_ADDR_RSP 0x8001` — re-derived at zigpy `zdo/types.py` AND zigbee-herdsman `zspec/zdo`, both agreeing with DP-5/DP-6) + nested record `IeeeAddressResponse(status, ieeeAddress, networkAddress)` + `encodeIeeeAddressRequest` (`[tsn][nwk LE][0x00][0x00]`) + `parseIeeeAddressResponse` (12 B minimum; the status CARRIED, never judged). `EzspCoordinatorProtocol.requestIeeeAddress(nwk, timeoutMillis)` (package-private, over `zdoUnicastExchange`): INFO `zigbee.ieee_addr_req` · INFO `zigbee.ieee_addr_rsp: nwk= device=` (+ `response_nwk=` only when it differs) · WARN `zigbee.ieee_addr_rsp_failed: nwk= status=` · WARN `zigbee.ieee_addr_req_unanswered: nwk= timeout_ms=`; a < 12 B body ⇒ `EzspFormatException`. Adapter: `IEEE_ADDR_REQ_TIMEOUT_MILLIS = STEP_TIMEOUT_MILLIS` (10 s); the H-ii arm's (3) after a CLEAN (2) miss only, the ONE set bounding the pair; `reason=zdo_miss` replaces `lookup_miss` (retired); admission on the RESPONSE's pair, the two calls byte-unchanged. MODULE_CONTEXT: the F-R4-1b section (9 gotchas incl. the herdsman SUCCESS-only-read BENCH-VERIFY residual, the R-4c glance grammar, the tokens) + two in-place pointers.
> **Red-first (staged):** HEAD + the tests → compile-red 12 (6 codec-test symbols + 6 harness `CLUSTER_IEEE_ADDR_REQ`) → codec in → 589 run / **4 RED** for the right reasons (T3/T3b/T3e "no IEEE_addr_req captured", T3c "the clock advanced 0 ms") + C1–C3 green + T3d green-by-construction (disclosed) → protocol + adapter in → 589 / 1 RED (T7, R1) → re-fixtured → 589/589. Observations (return §3): O1 the 2-byte failure-reply residual (BENCH-VERIFY) · O2 `timeout_ms=` on the rejection arm is disambiguated by the preceding `aps_unicast_rejected` (the split needs a typed seam — the hub rules) · O3 `recordAnnounce` takes any nwk (no DP-6 conflict) · O5 the Files table's "+1 INFO token" has no DP-7 counterpart (none invented).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return (rules R1/R2) → the msg file (hub-authored; the census + the rulings are its inputs) → Nick's commit + main push (stages EXACTLY the 6) → **CI green on that push = the gate of record** → **R-4c on the held card (⟨R4C: Sat 09-12 | Sun 09-13⟩ — the C-003 slot: the six-device fleet re-adopted, routers by rejoin, the SNZB-02P by this surface; the glance: `lookup_eui64_failed nwk=0x15ac status=0x1` → `ieee_addr_req` → `ieee_addr_rsp … device=0xF044D3FFFED2A201` → `rejoin_candidate … source=unknown_sender` → `device_adopted`; an `ieee_addr_req_unanswered` on the sleepy device is a first-class datum re-opening DP-4)** → P-1's charter. Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING. This lane HOLDS until the audit verdict or the next instruction.

---

## FE-113 — the read-API v1.1.3 mirror in the dashboard (deviceId · lastReported · triggerRef · components[].ref), rendered honestly (CORE repo, web-ui/dashboard/ only) — DELIVERED (Sun 2026-09-06 ~11:12 CT; the FE lane; the CORE lane's newest entry remains CG-123 below — this entry is the DASHBOARD lane's newest)
FE-113 delivered 2026-09-06 CT: v1.1.3 mirrored (14 M + 2 A under web-ui/dashboard/, two [REVIEW]s ruled ACCEPT by the hub: contract-check.mjs's EXPECTED_VERSION pin (the hub's stale fence, owned); AutomationsView.ref.test.tsx as an A (no such test at HEAD)). Gate: the lane's container `npm run verify` GREEN 274/274, tsc 0, lint 0, bundle 64.9/100 KB; **Deferred Build Gate: YES — `frontend.yml` on Nick's push (`cd web-ui/dashboard && npm ci && npm run verify`) is the gate of record.** Fixtures READ ONLY, md5 unchanged. Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING (law #22). Audit: `context/audits/2026-09-06_FE-113_intake_two-layer-audit_v66-b5.md`.
> **NEXT WU (refuse-to-close pointer):** Nick's commit of exactly the 16 → the push → `frontend.yml` green = the gate (and `ci.yml`'s run = passive sample 4/20) → **F-R4-1b dispatches on the core tree** (its instruction on disk) → H8-a's B2 reads `deviceId` / `lastReported` / `triggerRef` / `components[].ref` on the card = FE-113 VERIFIED; the v1.1.3 capture → **FE-113b** (the recorded fixture + its stability test) → the explainability-hero charter (after HERO-0's null census).
> **Lesson (folded to coder-lessons):** a fence that names a constant is grepped at source across every file type before dispatch — `EXPECTED_VERSION` existed (`scripts/contract-check.mjs`) and gated the very `verify` the lane was told to keep green.

## CG-123 — the read-API v1.1.3 additive keys (`triggerRef` · `components[].ref` · `deviceId` · `lastReported`), Row 30 adjudicated at source (docket Row 14 RULED (a)) (CORE repo, core/automation + api/rest-api + lifecycle) — DELIVERED (Sat 2026-09-05 ~22:15 CT; NEWEST — authoritative for the CORE lane; supersedes the FAILCHAN-FIX-1 DELIVERED entry below as lane-newest — FIX-1a landed at 5ed9178, FIX-1b at e5fa035)

> **DEFERRED GATE (hub, then Nick, then CI, then H8):** the hub's audit precedes ANY commit (inputs: the return `context/audits/2026-09-05_CG-123_return.md` §0-first + this entry). **The return carries ONE [REVIEW] the hub rules BEFORE the commit:** R1 — `NumericCondition` is selector-bearing at source (`NumericCondition.java:27`) but absent from §DP-2's enumerated permit list; the rule as written ("iff exactly ONE entity by identity") is applied, so `NumericCondition(DirectRef E)` yields `("entity", E)` on `components[].ref` (pinned in T4; the flip to `null` is one `case` line + one assertion). **Deferred Build Gate: YES** — `./gradlew check` (the ArchUnit sweep in `:app`) NOT run on this desk; owed against Nick's commit of exactly the 17 atop `e5fa035`; the gate of record = **CI on the push** (law 16). In-lane gates GREEN (the instruction's allow-list, `--offline`, `--rerun` on both test tasks): `:core:automation:test` 203 → **207/207** · `:api:rest-api:test` 147 → **152/152** (3 skipped, unchanged) · `:lifecycle:lifecycle:compileJava` · `spotlessCheck` clean on every module; 0 warnings under `-Werror`; `RunEndpointsTest` untouched, green. Working tree: core **`e5fa035`** (clean at launch) + **EXACTLY 17 at porcelain (17 M + 0 A + 0 D — the Files table exact)**; NOT touched: any `module-info.java` / `build.gradle.kts` / `web-ui/**` / the freeze doc / the ITs. Words honoured: `CG2-SCOPE: A1-only` · `ROW30: as-ruled`.
> **The delta:** (CG-1) `NonFiringExplanation` 9→10 (+ nullable `SubjectRefView triggerRef`; the 8-arg + a new 9-arg convenience ctor delegate null) · `AutomationSummary.ComponentView` 2→3 (+ nullable `ref`; 2-arg kept) · `StandardExplanationService`: `triggerRef = refOf(triggers.get(0))` derived ONCE beside `triggerSummary`, threaded through `deriveNonFiring`/`completedVerdict` into all 8 constructions (canonical form; 0 convenience forms left in main); `componentsOf` passes `refOf(x)`; FOUR exhaustive sealed switches, no `default` (`refOf` ×3 over Trigger/Condition/Action + `selectorRef`): only `DirectRefSelector` and `CalendarTrigger.calendarEntityId` yield a ref; every group selector, `ReachabilityTrigger` (a DEVICE — stated limitation), the subject-less permits, and the compounds (never descended) yield `null`. (rest-api) `EndpointResponses.subjectRefMap` hoisted verbatim from `GetRunCausalChainEndpoint` (both call sites moved, private copy gone — wire byte-identical); `GetNonFiringEndpoint` appends `triggerRef` after `noCommandsIssued` (10 keys); `ListAutomationsEndpoint` appends `ref` after `summary`; `ListEntitiesEndpoint` ctor gains `EntityRegistry` (2nd), `summarise` becomes an instance method appending `deviceId` (`findEntity → Entity::deviceId → toString`, null when absent/helper; no swallow) then `lastReported` (`Instant.toString()` or null) → 5 keys; `RestFilters.installEntityQueryEndpoints` is 5-param (registry to the list endpoint ONLY; A2/A3 code-unchanged, `@see` only). (lifecycle) `HomeSynapseCore:1029` passes `entityRegistry` — ONE line. **DP-3 default HELD:** `EntityRegistry` on the exported gateway compiled clean under `-Xlint:exports` (device reaches consumers via `requires transitive state → device`); the module-info/build fallback did NOT fire. Both MODULE_CONTEXTs carry the CG-123 sections (the `refOf` rule verbatim as the projection's contract; the Row 30 GOTCHA: `outcome: CONFIRMED` + `resultOutcome: null` is the happy path — `ZigbeeCommandHandler.publishResult` fires only for unroutable/rejected/unconfirmed; `firingValue: null` is V1-SUBSET by design → FIRING-VALUE, hub-docketed). Registry double in tests: the module's existing `FakeEntityRegistry`.
> **Red-first (staged):** HEAD + the 11 tests → compile-red 11 + 11 (the absent `triggerRef()`/`ComponentView::ref`/10-arg ctor/3-arg `ComponentView`/4-arg `ListEntitiesEndpoint`) → stage A (seams declared, hooks inert) → automation 207 run / **3 RED** (T1 · T3 Calendar arm · T4; T2 green-by-construction, disclosed) + rest-api 152 run / **7 RED** (T5–T11, every one on the absent key) → stage B → 207/207 · 152/152. Deviations: [REVIEW] R1 · [INFO] I1 rest-api +5 tests not "+4" · I2 the `componentsOf` cite (`:440`, not ~:770) · I3 `:1029` unshifted · I4 FQN `@see` · I5 arch Rules 4/5 name `device..` as subject scope only — not a fifth reaching rule · I6 `CompoundSelector([DirectRef E])` observation (rule kept as written).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return (rule R1) → the msg file (hub-authored; the census + R1's ruling are its inputs) → Nick's commit + main push (stages EXACTLY the 17) → **CI green on that push = the gate of record** → **the FE fast-follow lane** (the v1.1.3 mirror: `CONTRACT_VERSION`/`EXPECTED_VERSION` → `v1.1.3-<landing CT date>`, `contract.ts`/`shapes.ts`/mocks/the honesty tests; the FE contract-check FAILS until then — expected) → the hub's freeze-doc amendment note → **H8's real-wire read of `deviceId` · `lastReported` · `triggerRef` · `components[].ref` on the card = LIVE-VERIFIED** (law #22). Follow-ons charter as their own rows: FIRING-VALUE (§DP-5 (i)) · A2/A3 `deviceId` with Row 8's serializer rebuild · a device-typed ref for `ReachabilityTrigger` (a later bump) · the `CompoundSelector`-of-one ruling. This lane HOLDS until the audit verdict or the next instruction.

---

## FAILCHAN-FIX-1 — the delivery-drop class made visible (FIX-1a, the instruments) and fixed on its measurement (FIX-1b, the mechanism) (CORE repo, core/event-bus + lifecycle + .github + build-logic) — DELIVERED (Sat 2026-09-05 ~16:55 CT CT; NEWEST — authoritative for the CORE lane; supersedes the FAILCHAN DELIVERED entry below as lane-newest — FAILCHAN landed at 7af2d6c, PR #5 at dc3328b)

> **DEFERRED GATE (hub, then Nick ×2, then CI ×2, then the veto-only samples):** the hub's two-layer audit precedes ANY commit (inputs: the return `context/audits/2026-09-05_FIX-1_return.md` §0-first + the corpus `context/audits/2026-09-05_FIX-1_loops/` + the patch `context/audits/2026-09-05_FIX-1b.patch` + this entry). **The return carries THREE [REVIEW] the hub rules BEFORE the commits:** R1 FIX-1b is delivered as a `git apply -p1` patch beside the return (two commits from one uncommitted tree need separable bytes; the tree holds exactly FIX-1a's 9) · R2 ReplayTransitionIT's red is a **TRANSITION checkpoint gap in PHASE 1** (`drainAndPromote` delivered without checkpointing; reproduced 3/56 pinned WITHOUT load, 0/20 WITH load; pinned deterministically by T-R1), not the hub's `:302`/phase-2 theory — Nick's Act 3 (the `dc3328b` HTML) adjudicates the phase · R3 a second HeroLoop mechanism found at source and NOT implemented (the (c) row forbids other changes): `notifyEvent` fan-out REORDERING across concurrent publishers (the router can see `command_dispatched` before `command_issued` → `route_join_miss` with zero drops) — a FIX-2 charter candidate (instrument first: `LIVE_OUT_OF_ORDER` + a monotonic checkpoint guard). **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed to CI on BOTH pushes; in-lane gates GREEN in one round each: `:core:event-bus:compileJava :test` · `:lifecycle:lifecycle:compileJava :test` · `spotlessCheck --offline` → FIX-1a BUILD SUCCESSFUL 1m 5s (**event-bus 221 → 225 · lifecycle 79/79**, XML 18:10Z), FIX-1b BUILD SUCCESSFUL 36s (**event-bus 225 → 230 · lifecycle 79/79**, XML 19:29Z); `-Werror` clean; LF everywhere. Working tree: core **`dc3328b`** (clean at launch 17:51Z) + **EXACTLY 10 at porcelain (8 M + 2 A) = FIX-1a** (the Files table's 9 + `core/event-bus/src/test/java/com/homesynapse/event/bus/ReplayTransitionIT.java` — the hub's ONE stamp `(resting checkpoint N)` in `awaitCheckpoint`, v64 b1 note, [INFO] I8): M `.github/workflows/ci.yml` · `build-logic/src/main/kotlin/homesynapse.java-conventions.gradle.kts` · `core/event-bus/MODULE_CONTEXT.md` · `core/event-bus/src/main/java/com/homesynapse/event/bus/InProcessEventBus.java` · `…/TransitionCoordinator.java` · `lifecycle/lifecycle/MODULE_CONTEXT.md` · `lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java`; A `…/event/bus/DeliveryAnomaly.java` · `core/event-bus/src/test/java/com/homesynapse/event/bus/DeliveryAnomalyEmissionTest.java`. FIX-1b = 6 M + 0 A in the patch (the two bus classes · `DeliveryAnomaly` · the test class · both MODULE_CONTEXTs; neither IT). ZERO module-info / build.gradle.kts / catalog / event / schema diffs; both ITs' bounds byte-unchanged; `HeroLoopHardwareFreeIT` untouched; ZERO commits by the lane; the `ef02d13` worktree added and removed per §A.4.
> **The delta:** (1a) `DeliveryAnomaly(subscriberId, globalPosition, Kind, detail, timestamp)` over a ctor-injected `Consumer` — the `HealthSignal` shape; the bus stays SLF4J-free and adds no eighth metric; emits at the four silent drop points BEFORE their `return`/`continue` (`notifyEvent` empty page `"*"`, `liveLoop` empty/threw, `drainAndPromote` empty), the emitter swallowed on `RuntimeException`; the composition root routes ONE WARN token `bus.delivery_anomaly: kind= subscriber= position= detail= at=`; CI: `--continue`, `workflow_dispatch`, XML per test case + FULL exceptions, `test-reports-<run>` uploaded `always()` with `test-results/`, the desk-only `vtParallelism` knob. (1b) DP-2: `readLivePosition` — 5 reads, parks 1→2→4→8 ms, `LIVE_READ_EXHAUSTED` + SUSPEND; `offerUnfiltered` on an invisible notification; the drain's `readQueuedPosition` (same shape, `TRANSITION_READ_EXHAUSTED`); **the drain checkpoints every SUCCESS delivery (AMD-45-gated) — the fourth bus-side writer**.
> **The measurement (§A.5, 215 runs + 1 smoke, Windows-native pin `start /affinity 3` ⇒ 2 carriers, ±2 busy loops, `--rerun --no-daemon --offline` per run):** HeroLoop **0/90** (fix1a pinned+load 0/20 · `ef02d13` 0/20 · CPU-shape 0/10 · pinned no-load 0/20) → **(c)**; ReplayIT **0/40 with load, 3/56 without** (smoke 1/1 · no-load 1/20 · my-stamp 0/15 · hub-stamp 1/20 → `(resting checkpoint 28)`), all PHASE 1, zero anomaly tokens → **(a)**; T8 FIX-1b ReplayIT no-load **20/20 GREEN**, T9 HeroLoop pinned+load **20/20 GREEN**, ReplayIT pinned+load **10/10 GREEN**; the hub-stamp re-run on the FIX-1a bytes (v64 b1): pinned+load **0/20**, pinned no-load **1/20** — the RED (run-07, 21:48Z) read **`(resting checkpoint 28)`**: the REPLAY tail; positions 29..1000 went through the TRANSITION drain, which delivered them without a checkpoint (T-R1 proves the delivery half; a 'late, not lost' reading would have been ≥ 1000). Corpus: `ReplayIT/fix1a-hubstamp-noload/run-07/`. Red-first: HEAD compile-red → stage A 4/4 behaviour-red → green; FIX-1b 5/5 red on the FIX-1a bytes → 9/9 green.
> **NEXT WU (refuse-to-close pointer):** the hub's audit (rules R1–R3) → the FIX-1a msg file → **Nick commits exactly the 10 and pushes = SAMPLE #4** (Build & Check now `--continue`) → `git apply -p1 context/audits/2026-09-05_FIX-1b.patch` → the FIX-1b msg file → **Nick's second push = THE GATE OF RECORD** → three `workflow_dispatch` samples at the landed sha, veto-only (§B.4) → CG-1/2/3 dispatches on the green; FIX-2 (the fan-out reorder, R3) charters from the return §4-2; Act 3 (the `dc3328b` HTML) adjudicates R2's phase. This lane HOLDS until the audit verdict or the next instruction.

---

## FAILCHAN — the boot-honesty sweep: 143-is-clean + `Restart=always` (Part A), the `ExitCode` contract WIRED behind the exit seam (Part B), the orderly-path emission sweep (Part C) (R-10 Row 6 (a) + EXITCODE (a)) (CORE repo, distribution + lifecycle + app + integration-zigbee) — DELIVERED (2026-09-03 ~22:20 CT; NEWEST — authoritative for the CORE lane; supersedes the PKG-SEC-2 DELIVERED entry below as lane-newest — PKG-SEC-2 landed at ef02d13)

> **DEFERRED GATE (hub, then Nick, then CI, then R-4b's first stop):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-09-04_FAILCHAN_return.md` + this entry) — the return carries **TWO [REVIEW] the hub rules before the commit (return §0):** R1 the configuration recommendation says `homesynapse.yaml` (the real root document, `YamlLoader.ROOT_DOCUMENT_NAME`) instead of the instruction's `config.yaml`; R2 the census is **19 = 14 M + 5 A**, the instruction's 18 **+1 M** `ZclIngestionUnit.java` (comment-only: the §10-M KEEP-WITH-REASON code comment the ruling row (ii) prescribes) — drop it and the census is exactly 18. **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed against Nick's commit of exactly the 19 atop `ef02d13`; the gate of record = **CI on the push: Build & Check + install-smoke BOTH legs** (law 16). In-lane gates GREEN in one round (the instruction's allow-list): `:app:homesynapse-app` / `:lifecycle:lifecycle` / `:integration:integration-zigbee` `compileJava :test` + `spotlessCheck --offline` → BUILD SUCCESSFUL in 23s, zero `-Werror` warnings, **app 27 → 30 · lifecycle 75 → 79 · zigbee 581 → 582**, every test task executed fresh (XML mtimes 03:06:44Z/03:06:59Z/03:07:01Z); T9 7/7 + three lint mutants (FAIL 1 each) + HEAD's unit FAIL 2; `bash -n` ×10 · `sh -n` · WSL `dash -n` + `systemd-analyze` 255 verify 0 Unknown-key on the new unit; twins IDENTICAL (sha `5e7544419154418b8…`); PyYAML 14 steps, verify<lint<echo; the check-8 fragment 4 arms under a stub `systemctl`; CR=0 ×19. Working tree: core **`ef02d13`** (clean at launch) + **EXACTLY 19 at porcelain (14 M + 5 A)** — M the unit · `run-smoke.sh` · both `install-smoke.yml` · `boot-contract-map.md` · `SystemLifecycleManager` · `HomeSynapseCore` · `Main` · `ZigbeeIntegrationAdapter` · `ZclIngestionUnit` · `ZigbeeProductionTransportTest` · 3 MODULE_CONTEXT; A `distribution/smoke/unit-directives-test.sh` · `StartupFailureReport` · `HomeSynapseCoreStartupFailureTest` · `ExitCodes` · `ExitCodesTest`. ZERO module-info / build / catalog / event / schema diffs; ZERO commits by the lane.
> **The delta:** (A) `SuccessExitStatus=143` + `Restart=always` (+`RestartSec=10`, `RestartPreventExitStatus=10` kept) with the ruled comment; the DANGER block untouched; the fixture-paired lint (seven active directives, each exactly once) wired in both twins after `systemd-analyze verify`; run-smoke check 8 asserts `Result=success` ∧ `ActiveState=inactive` after `systemctl stop` (ok-sites 23 → 24); the contract map's two sections rewritten. (B) public record `StartupFailureReport(phase, subsystem, recommendation)`; `SystemLifecycleManager.lastStartupFailure()` (`default` empty; no test double exists — grep); `HomeSynapseCore`: the `volatile String initializing` marker before each fatal-set init (`configuration` at the top of the Phase-1 block · `persistence`/`event-bus` before each Phase-2 build — the marker is what tells 11 from 12 · device-model · state-store · automation · observability · rest-api · integration), the catch builds the report BEFORE teardown and emits **`lifecycle.startup_failed: phase= subsystem= recommendation="…" — tearing down initialized subsystems`** (throwable last), `throw fatal;` byte-unchanged; `ExitCodes.forStartupFailure` (empty → 99; configuration 10 · persistence 11 · event-bus 12 · else 99); `Main`: `sigtermReceived` set FIRST in the hook, ONLY `start()` wrapped — SIGTERM seen ⇒ `return` (the hook owns the 143), else one stderr line + `System.exit(code)` in `main`, never the hook. (C) §10-O: both `productionLoop()` catches consult `closeRequested()` (`stopSignal.getCount()==0`) FIRST ⇒ INFO **`zigbee.transport_closed_orderly`** + `break`, watchdog never fed; `productionLoop()` package-private (drive seam); §10-M: KEEP-WITH-REASON (ii) — the all-ones partner is the null EUI64, 0x32 the TC Verify-Key timeout, cause OPEN per R-3a §8, no spec cite ⇒ the WARN stays; §10-I routed, untouched.
> **Red-first (staged):** HEAD → compile-red ×3 (the absent symbols; `productionLoop()` private) + T9 shell RED (2 of 7) → stage A (seams declared, hooks inert) → app 3 run / 2 RED (T1/T2; T3 green-by-construction), lifecycle 4 run / 2 RED (T4/T5 empty report; T6/T7 green-by-construction), zigbee 10 run / 1 RED (T8: no orderly INFO) → stage B green. T10 red-by-measurement (O-2); CI shows the green. Deviations: [REVIEW] R1 (`homesynapse.yaml`) · [REVIEW] R2 (census +1 comment-only) · [INFO] I1–I6 (marker placement at the Phase-1 top · the drive-seam visibility · the shared predicate · the 13-no-producer note in `ExitCodes` not `ExitCode.java` · no test double exists · the lint's prefixed verdict line).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return (rules R1/R2) → the msg file (hub-authored; the census + the ruled deviations are its inputs) → Nick's commit + main push (stages EXACTLY the 19, or 18 if R2 is dropped) → **Build & Check + install-smoke BOTH legs green on that push = the gate of record** (Static lint: `PASSED ✓ (7 directives)` + `unit directives verified`; run-smoke 24 PASS per leg incl. `clean stop grades success (Result=success ExecMainStatus=143)`) → **R-4b's first `systemctl stop` on the card reads `inactive`/`success`** (the free rider iff CI is green by Sat 08:00 CT) → **Row 7 (`OR-JOURNALD-PRIO`, the `<N>` journald prefix)** next on this desk → the docs correction block gains Doc 12 §8.4 :663 + §6.4/§6.6 + LTD-13 `Restart=on-failure` (return §3 O4). This lane HOLDS until the audit verdict or the next instruction.

---

## PKG-SEC-2 — the zigbee schema admission at Phase-1 validation (R-10 Row 13 RULED (a′); R-4 C-1) (CORE repo, lifecycle + app + integration-zigbee + the config D-f line) — DELIVERED (2026-09-03; NEWEST — authoritative for the CORE lane; supersedes the F-R4-1 DELIVERED entry below as lane-newest — F-R4-1 landed at a1c6966)

> **DEFERRED GATE (hub, then Nick, then CI, then R-4b):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-09-03_PKG-SEC-2_return.md` + this entry) — and the return carries **ONE [REVIEW] the hub must rule BEFORE the commit (return §0.2):** the zigbee fragment's `permit_join_duration` `default: 120` was REMOVED, because a fragment composed at Phase 1 has every `default` merged into the model on EVERY boot (Doc 06 §3.1 stage 4), which would have opened a 120 s join window at every unconfigured start against the M9.4-PJ law ("an ABSENT key opens NOTHING" — `ZigbeeIntegrationAdapter:87–:93`, R-4 §6-iv measured); mutation-proven (restored ⇒ exactly 4 FAIL); the revert is one line. **Deferred Build Gate: YES** — `./gradlew check` NOT run on this desk; owed against Nick's commit of exactly the 10 paths atop `a1c6966`; the gate of record = **CI on the push** (law 16). In-lane gates GREEN (the allow-list + the touched third module): `:lifecycle:lifecycle` / `:app:homesynapse-app` / `:integration:integration-zigbee` `compileJava :test :spotlessCheck --offline` → BUILD SUCCESSFUL in 24s, zero `-Werror` warnings, **lifecycle 62 → 75 · app 24 → 27 · zigbee 581/581**, every touched task executed fresh (XML mtimes 12:07Z), LF/clock/`synchronized` checks clean. Working tree: core **`a1c6966`** (clean at launch) + **EXACTLY 10 at porcelain (8 M + 2 A)** — M `lifecycle/lifecycle/MODULE_CONTEXT.md` · `lifecycle/.../HomeSynapseCore.java` · `app/homesynapse-app/MODULE_CONTEXT.md` · `app/.../Main.java` · `config/configuration/MODULE_CONTEXT.md` (the D-f `:173` correction only) · `integration/integration-zigbee/MODULE_CONTEXT.md` · `integration/.../ZigbeeIntegrationFactory.java` (Javadoc) · `integration/.../resources/schema/zigbee-config-schema.json`; A `lifecycle/.../HomeSynapseCoreSchemaAdmissionTest.java` · `app/.../MainSchemaFragmentsTest.java`. ZERO module-info / build / catalog / event diffs; ZERO commits by the lane.
> **The delta:** `HomeSynapseCore.registerIntegrationSchema` no longer throws pre-start — it QUEUES (`pendingIntegrationSchemas`, registration order, last-per-type wins); the new private `installSchemaRegistry` drains the queue in Phase 1 AFTER `registerCoreSchema(automation)` and BEFORE `configurationService.load()`, then publishes the registry (queue + publication under the new `schemaLock`, `ReentrantLock`, never across I/O); a post-publication call registers directly (in-memory recompose; the on-disk `schemas/config.schema.json` is rewritten by the service's next `load()`/`reload()`/`write()`, NOT by the registration — read at `StandardSchemaRegistry.register`, preserved, test-pinned). Tokens: INFO `lifecycle.integration_schema_registered: type= stage=pre-load` (one per drained fragment) · `… stage=direct`. Guards NPE/IAE; a malformed pre-start fragment fails the boot in Phase 1 (STOPPED). `Main.integrationSchemaFragments()` (package-private; zigbee → `configSchemaJson()`) iterates into `core.registerIntegrationSchema` right before `manager.start()`; the post-start W10 call REMOVED. `config/configuration/MODULE_CONTEXT.md:173` corrected to the measured root (`/var/lib/homesynapse/config/` + `integrations/zigbee.yaml` via `!include`); every other `/etc/homesynapse` hit classified KEEP (the table in the return). **The Doc 12 conflict check (return §0.1):** the instruction's premise is wrong — Doc 12 §3.3 :133 DOES carry the "core-only … recompose after Phase 6" sentence; read as descriptive of a never-built mechanism on a retired premise, not prohibitive; Doc 06 §3.2 + C7 govern; PROCEEDED under (a′) — the hub adjudicates; a Doc 12 :133 correction note is owed by the hub.
> **Red-first (staged):** HEAD main + the new class → 13 run / 13 RED (all `IllegalStateException` — the guard IS the defect); stage A (queue seam live, Main supplies, drain inert) → 13 run / **11 RED for the right reasons** (the R-4 exhibit line verbatim in T1/T1b/T3/T7; T2 size 3 not 2; T4 the empty node; T5/T6b/T8 no composition/INFO; T9b no throw; T7b NPE) + app 27 run / 1 RED (the default pin); stage B → 75/75 · 27/27 · zigbee 581/581. Deviations filed (return §1): [REVIEW] the default removal · [REVIEW] the Doc 12 premise · [REVIEW] the remaining defaults become operative (no adapter reads them today) · [INFO] the queue shape, `stage=direct`, the supply seam + app pin, the hero IT untouched.
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return (rules §0.2 — keep the default OFF, or restore it and consciously accept a 120 s join window per boot — and §0.1) → the commit msg (`_scratch/2026-09-03_core_PKG-SEC-2_commit-msg.txt`, hub-authored; the return's census + §0.2 wording are its inputs) → Nick's commit + main push (stages EXACTLY the 10) → **Build & Check + install-smoke green on that push = the gate of record** → **R-4b's rig check at the next power-on:** `journalctl -u homesynapse.service -b --no-pager | grep -c 'Configuration issue'` → **0** and exactly one `lifecycle.integration_schema_registered: type=zigbee stage=pre-load` per boot (with `permit_join_duration` UNSET: NO `permit_join_opened` line — the M9.4-PJ law surviving composition, measured) → the follow-ons charter as their own rows (the Doc 12 :133 + Doc 08 :871 correction notes · the hero IT re-timed pre-start · the defaulted-key audit rule, gotcha #10). This lane HOLDS until the audit verdict or the next instruction.

---

## F-R4-1 — interview-on-rejoin, the silent-rejoiner adoption gap (R-10 Row 10 RULED (a)) (CORE repo, integration-zigbee) — DELIVERED (2026-09-02; NEWEST — authoritative for the CORE lane; supersedes the R-7b DELIVERED entry below as lane-newest — R-7b landed at dec35be)

> **DEFERRED GATE (hub, then Nick, then CI, then R-4b):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-09-02_F-R4-1_return.md` + this entry). **Deferred Build Gate: YES** — `./gradlew check` (ArchUnit `NO_DIRECT_TIME_ACCESS` / `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` / the app + lifecycle suites) was NOT run on this desk; owed against Nick's commit of exactly the 13 paths atop `f519f42`; the gate of record = **CI on the push** (law 16). In-lane gates GREEN (the instruction's targeted allow-list): `:integration:integration-zigbee:compileJava :test :spotlessCheck --offline` → BUILD SUCCESSFUL, zero `-Werror` warnings, **581/581** (560 → 581, +21), Spotless clean, clock/`synchronized` greps clean. Working tree: core **`f519f42`** (clean at launch) + **EXACTLY 13 at porcelain (12 M + 1 A)** — M `integration/integration-zigbee/MODULE_CONTEXT.md` · main `CoordinatorProtocol` · `EzspCoordinatorProtocol` · `PendingInterviewQueue` · `ZclIngestionUnit` · `ZigbeeAdoptionSlice` · `ZigbeeIntegrationAdapter` · test `EzspProtocolTest` · `FixtureReplayTest` · `PendingInterviewQueueTest` · `ZclIngestionUnitTest` · `ZigbeeTrustCenterJoinTest`; A `src/test/…/ZigbeeInterviewOnRejoinTest.java`. Nothing committed, nothing staged.
> **The delta (detector / policy):** `ZclIngestionUnit.IngestionListener` +2 abstract methods — `onRejoinCandidate(int nwk, int cluster)` raised on EVERY unknown-sender HA frame BEFORE the standing `ingestion_unknown_sender` WARN (H-ii), and `onRejoinCandidate(IEEEAddress, int)` raised from `handleTrustCenterJoin` when the new `TrustCenterJoin.rejoined()` holds (accepted SECURED/UNSECURED_REJOIN; a fresh UNSECURED_JOIN, a denied join, a leave, a malformed callback never raise — H-i). `ZigbeeIntegrationAdapter` decides in cost order: `isPermitJoinActive()` FIRST (closed ⇒ INFO `zigbee.rejoin_ignored_window_closed: nwk= cluster=` ONCE per nwk per window epoch) → the cache's NWK→IEEE view → `rejoinLookupAttempted` (ONE coordinator ask per nwk per epoch, hit or miss) → `protocol.lookupIeee(nwk)` (NEW on `CoordinatorProtocol`, +1 public method, instruction-sanctioned; EZSP `lookupEui64ByNodeId` 0x0061 — bellows-verified at source 2026-09-02, NOT silicon-verified: R-4b criterion 0; miss ⇒ empty + WARN `zigbee.lookup_eui64_failed: nwk= status=`) → adopted ⇒ DEBUG `rejoin_candidate_ignored … reason=already_adopted` and nothing else; else EXACTLY the announce path's two calls (`cache.recordAnnounce` + `interviewQueue.schedule(…, Source.REJOIN)`) + INFO `zigbee.rejoin_candidate: device= nwk= source=unknown_sender|tc_join`; a miss ⇒ WARN `zigbee.rejoin_candidate_unresolved: nwk= cluster= reason=`. Both once-per-epoch sets clear inside `openPermitJoinWindow()`. `PendingInterviewQueue.Source {ANNOUNCE, REJOIN}` (nested enum) rides `Pending` into `ZigbeeAdoptionSlice.onDeviceDiscovered(…, source)` → the `zigbee.device_proposed` line gains a trailing `source=`; event payloads byte-unchanged; `relink` untouched. The M9.4-TCJ §A.2 PIN AMENDED for exactly the accepted-rejoin-during-window case — source Javadoc, MODULE_CONTEXT in-place pointer + a `## F-R4-1 Implementation` block (deltas, gotchas, the R-4b log grammar, tokens), and the `ZigbeeTrustCenterJoinTest` re-pin (wording only, assertions byte-unchanged; verbatim in the return §0).
> **Red-first (staged):** HEAD main code → compile-red on EXACTLY the four seams; stage A (seams declared, hooks inert, `lookupIeee` stub-empty) → 581 run / **15 RED** for the right reasons (T8 ×3, detection ×2, adapter scenarios ×10; T6/T6b + the never-raise pins + the queue plumbing GREEN-by-construction, disclosed); stage B → 581/581. Pushback/observations filed (return §3): the adopted-but-cache-unindexed corner (T5b, spec §5 honored literally; the re-index follow-on proposed) · no fake `CoordinatorProtocol` exists in test-support · `InterviewAttempt` has no reason slot · two DEBUG/WARN tokens beyond the instruction's three.
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the commit msg (`_scratch/2026-09-02_core_F-R4-1_commit-msg.txt`, hub-authored; the return §5 carries the proposed paragraph) → Nick's commit + main push (stages EXACTLY the 13) → **Build & Check + install-smoke green on that push = the gate of record** → **R-4b** (the four-of-four re-rep; criterion 0 = the 0x0061 hop's first ⏺ — the intake slots in the return §4; the log grammar in MODULE_CONTEXT §F-R4-1) → the follow-ons charter as their own rows (the T5b cache re-index · the additive `source` payload key · the ZDO IEEE_addr_req second surface if 0x0061 misses · silent-rejoin dedup hygiene for adopted devices · the rig's 0x0061 script). This lane HOLDS until the audit verdict or the next instruction.

---

## R-7b — the version scheme `0.1.0+git<YYYYMMDD.HHMMSS>.g<sha>` (R-V) + the skeleton fence + HS_DIST_DIR/git-first order + the node/upload majors + riders i/j/k + E1 (CORE repo, distribution-only) — DELIVERED (2026-08-23; NEWEST — authoritative for the CORE lane; supersedes the R-9 DELIVERED entry below as lane-newest — R-9 landed at e845cd9)

> **DEFERRED GATE (hub, then Nick, then CI):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-08-23_R7b_version-scheme_return.md` + this entry — the return carries TWO [REVIEW] pushbacks the hub must rule: **§3.1** `upload-artifact@v5` declares `runs.using: node20` (v5.0.0's action.yml at tag `330a01c4…`; the v6.0.0 release note names v5 as still-node20), so H-4's "zero Node-20 annotations" holds only at `@v6` — the RULED `@v5` is implemented as written and a one-token ×3-files flip is staged; **§3.2** rider (k)'s literal `! grep -q 'Unknown key name'` fragment cannot fail a `set -e` step (bash exempts `!`-inverted commands from -e; proven exit 0 on a match) — shipped as `if grep … exit 1` + a `command -v systemd-analyze` hard guard, mutation-proven RED on HEAD's unit at `:31` and GREEN on the fixed unit). **Deferred Build Gate: N/A-BY-DESIGN** — ZERO Java (shell + workflow YAML + the unit token + three docs); the instruction orders no gradle for this lane. The gate of record = **CI on Nick's push: Build & Check + install-smoke BOTH legs** (law 16). Working tree: core **`e845cd9`** (the R-9 landing commit — the instruction's alternate baseline; `common.sh` line numbers +1 vs the instruction, gated content identical, report-and-proceed) + **EXACTLY 11 M at porcelain** (0 A, 0 D — the census exact: `.github/workflows/ci.yml` · `.github/workflows/install-smoke.yml` · `distribution/ci/install-smoke.yml` · `distribution/common.sh` · `distribution/deb/build-deb.sh` · `distribution/docs/architecture.md` · `distribution/docs/boot-contract-map.md` · `distribution/docs/escalations.md` · `distribution/image/build-image.sh` · `distribution/smoke/version-grammar-test.sh` · `distribution/systemd/homesynapse.service`). Nothing committed, nothing staged.
> **The delta:** `common.sh` — the non-tag arm becomes `0.1.0+git<committer-date UTC>.g<sha>` (`TZ=UTC git log -1 --format=%cd --date=format-local:%Y%m%d.%H%M%S`, POSIX 8.6-digit case pattern; `-dirty` rides through; missing/malformed date → `''` FAIL-CLOSED — never `0.1.0+g<sha>`, which post-R-7b sorts BELOW every `+git` build; `|| true` on the capture so a FAILING git log reaches the same `''` → build-image's die, not a bare abort) + `_dist_dir` honours `HS_DIST_DIR` first + resolution order now **env → git → VERSION → ../VERSION → skeleton** (the cwd accident closed at the function). Both build-script captures pass `HS_DIST_DIR="${DIST}"`; build-image's passes-but-false comment now names the SKELETON fallback (the tag is the hypothetical) and its die tail reads the new scheme. Twins (cmp-identical, sha `cd83543a…`): `LTD-10`→`LTD-01` comment · `setup-node@v5` + `upload-artifact@v5` (majors verified via ls-remote FIRST: `a0853c24…` / `330a01c4…`) · echo step `V="$(HS_DIST_DIR=distribution bash -c …)"` + the skeleton fence AFTER the grammar assert + the green line extended with the scheme name and `sha256 <hex>  <deb>` (the R-3/R-4 origin hash) · Static lint gains the systemd-analyze verify block. `ci.yml` `upload-artifact@v5` (R-7b-2). The rig 23 → **34 checks** (10 arm incl. the empty-cdate fail-closed row, the VERSION-bypass row — git outranks a planted skeleton file — and the FAKE_NOGIT tarball-carrier row; 9 accept; 3+3 reject; **§2b: 4 dpkg --compare-versions ordering rows** — `+git gt +g` ×2, time-decides-not-sha, bare-id-gt = the documented one-time downgrade — or ONE explicit SKIP line without dpkg; 5 carriers incl. the fence literal per twin; verdict prints the count and asserts ran==expected). Unit `:31` `LogDirectory=`→`LogsDirectory=` (one token; under ProtectSystem=strict /var/log/homesynapse was never service-writable). `boot-contract-map.md` §Health rewritten to the shipped R-9 posture (loopback /health, E3 CLOSED, the M13 block a DANGER note not a staged flip). `architecture.md` version-string section rewritten (git-first order, the scheme + ordering argument, retire "the clean fix is a tag" → tags begin at the first release; no claim language). `escalations.md` E1 += "(c) taken at R-7 … 3m01s (install-smoke #33)".
> **Desk gates GREEN on the final bytes** (Windows 11; Git Bash 5.2.26 + msys dash; WSL Ubuntu-24.04: dash, dpkg 1.22.6, systemd 255): red-first at launch `7 of 30 FAILED` (the 5 arm/order rows + both fence rows) → post-implementation **34/34 (WSL) · 30+SKIP (Git Bash)** → mutation `COMMON_SH=<HEAD's common.sh>` **FAILED exactly 5, row-for-row the hub's derivation**; `bash -n` over every distribution .sh (the CI loop verbatim) + `sh -n`/`dash -n` on common.sh; `hs_version` = `0.1.0+git20260823.164515.ge845cd9-dirty` from bash+sh+dash × {repo root, distribution/ cwd} × {with, without HS_DIST_DIR} (the `-dirty` proof rode the WU's own dirty tree); `+up gt` and `-dirty gt` base at dpkg (update-smoke's V2 stays ordered); systemd-analyze: fixed unit zero Unknown-key ("Command … not executable" lines present, non-fatal as designed), HEAD's unit RED at `:31`; PyYAML structural asserts 25/25 across the three workflows; the echo-step body (PyYAML-extracted, never retyped) 8/8 arms under stub dpkg-deb; the assert fragment ×6 inputs; CR=0 + trailing LF + `i/lf w/lf` ×11.
> **NEXT WU (refuse-to-close pointer):** hub audit on the return (rules §3.1 `@v5`-stands vs the one-token `@v6`, and blesses/reverts the §3.2 `if`-form) → the core commit msg (post-diff) → Nick's commit + main push (stages EXACTLY the 11 M) → **Build & Check + install-smoke BOTH legs green on that push = the gate of record** (Static lint: 34 PASS + `PASSED ✓ (34 checks)` + `unit directives verified`; echo: `hs_version=0.1.0+git<cd>.g<sha>` + the sha256-bearing green line; 19 `[smoke] PASS` lines/leg; update-smoke `V2=<ver>+up`; artifacts `distribution-artifacts-{amd64,arm64}`; Node-20 annotations: ONE remains unless `@v6` is taken) → **R-3b** rides `distribution-artifacts-arm64` (hash-verifiable from the run log; the one-time `--allow-downgrades` on the two `7c9e4fa` cards; first block = the R-9 artifact-absent restart proof). This lane HOLDS until the audit verdict or the next instruction.

---

## R-9 / E3-HEALTH — the unauthenticated loopback `/health` (R-H1 LOOPBACK-ONLY) + R-H2 (the last-full-access-token guard) + riders R-9-a/R-9-b (CORE repo) — DELIVERED (2026-08-22; authoritative for the CORE lane until R-7b above; supersedes the R-7 DELIVERED entry below as lane-newest — R-7 landed at 89a912e)

> **DEFERRED GATE (hub, then Nick, then CI, then the bench):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-08-22_R9_E3-HEALTH_return.md` + this entry). **Deferred Build Gate: NO on this desk** — the targeted gates (`:api:rest-api:compileJava :api:rest-api:compileTestJava :api:rest-api:test :lifecycle:lifecycle:compileJava :lifecycle:lifecycle:test`, `BUILD SUCCESSFUL in 19s`, zero lint warnings) + Spotless (`spotlessCheck` exit 0, nothing to apply) + the FULL `./gradlew check` (`BUILD SUCCESSFUL`, 156 tasks: 7 executed incl. the app suite fresh — 24/24, `HomeSynapseArchRulesTest` 11/11 — and 149 up-to-date from the targeted run on the identical final bytes) ran GREEN here on Windows/JDK 21.0.4/Gradle 8.8. Test counts by test-results XML (the CI count): **rest-api 130 → 147** (+17: `HealthEndpointTest` A 5 · `RestFiltersAuthTest` 6 → 12 · `OpaqueTokenStoreTest` 21 → 26 · `TokenAdminEndpointsTest` 9 → 10; 3 POSIX-gated skips on this desk run on the Linux runner), **lifecycle 60 → 62** (both new e2e arms RAN — the off-loopback arm found a site-local interface on this desk), app 24 unchanged. `testing/integration-tests` is excluded from `check` by its own build file (on-device Pi profile) — compiled, not run. The gate OF RECORD stays **CI on Nick's push** (law 16): Build & Check + install-smoke BOTH legs with **19** `[smoke] PASS` lines each (check 3b: `unauthenticated loopback /health green (HTTP 200 — the unit's probe path, E3)`) + update-smoke through `--health-path /health`; **the WU CLOSES at §OP-H on the bench** (H8). Working tree: core **`89a912e`** (= the instruction's "62dbca3 + the R-7 commit"; clean at launch) + **EXACTLY 24 at porcelain** (22 M + 2 A — the 23-row R-H2-IN census + `api/rest-api/src/test/java/com/homesynapse/api/rest/StandardAuthMiddlewareTest.java`, a `revoke()` caller the instruction's P2 survey missed whose single-full-access-token fixture the R-H2 guard would otherwise refuse → test red; reported under the instruction's +1-and-say-so clause). Nothing committed, nothing staged.
> **The delta:** `HealthEndpoint` (A, package-private `Handler`): `GET`/`HEAD /health` → 200 iff `ReadinessSource.mode()==LIVE`, else 503 + `X-HomeSynapse-Projection-State` + `Retry-After: 5` (the gate's own package-private constants); body EXACTLY `{"status":"<mode>"}` in both arms, no envelope; `Cache-Control: no-store`; DEBUG only. `RestFilters.installHealthEndpoint(Object, ReadinessSource)` registers `app.get` AND `app.head` with ONE handler — **Javalin 6.7.0's automatic HEAD-for-GET answers a bare 200 WITHOUT running the handler (bytecode: `DefaultTasks`' HTTP fallback arm), a passes-but-false "ready" during REPLAY — so HEAD is registered explicitly (return §5 item 1, [REVIEW])**. `authorize()` gains the SECOND exemption as its own early-return after the shell's (`isPathSafe` still FIRST): `isHealthProbeRequest(method, path, ctx.ip())` = GET/HEAD ∧ exactly `/health` ∧ `isLoopbackLiteral(addr)`; `ctx.ip()` = the default context resolver = `HttpServletRequest.getRemoteAddr()` (bytecode); `isLoopbackLiteral` strips Jetty's IPv6 brackets + a `%zone`, parses the IPv4 arm locally (strict dotted quad, 127/8 — `InetAddress` untouched), charset-guards the IPv6 arm and only then calls `getByName` (for a colon-bearing literal JDK 21 parses or THROWS — `InetAddress.java:1635–:1663` — never resolves; the instruction's looser charset rule would have resolved hex-only hostnames like `cafe.babe`, return §5 item 3). `HomeSynapseCore.bringUpHttpSurface` +1 line between `app.get("/", …)` and `installReadinessGate`. R-H2: `OpaqueTokenStore.RevokeOutcome {REVOKED, NOT_FOUND, REFUSED_LAST_FULL_ACCESS}`; `revoke()` refuses the ONLY active (non-revoked, non-expired) `*` row — nothing persisted, one WARN with the key id; `rotate()` untouched and never refused; the request-file arm → `skipped "line N: revoke <keyId>: refused — the last active full-access token (use rotate)"`; `ProblemType.TOKEN_REVOKE_REFUSED` (409, after its 409 neighbour; no count pin exists — survey re-run); `TokenAdminEndpoints.revoke` → a three-arm switch (409 problem+json, no audit). Distribution: the unit `ExecStartPost … --health-path /health` + the probe comment + the header's "two-line edit" claim reworded + the M13 block → the R-9-b DANGER note (the bricking mechanism, the precondition, the R10-IN-L §3 f1 pointer, the shape kept commented); `common.sh` `HS_HEALTH_PATH` default `/health` (one hunk; the R-7 arm untouched); `health-probe.sh` header only (8 lines → 8 lines; :16–EOF byte-identical; default path unchanged by design); `run-smoke.sh` check 3 banner + NEW check 3b (the same binary, no token, `--timeout 30`, the passes-but-false line; 4–9 unrenumbered); `install.sh`/`update.sh` ×2/`update-smoke.sh` ×2 → `--health-path "${HS_HEALTH_PATH}"`; the helper: usage +2 lines (R-H2), CAUTION → NOTE, the `rm -f` block REMOVED whole, no-arg output byte-identical (cmp, present + absent); `token-rotation.md` caveat → a History note + :156 + the `revoke` row + the DELETE API row (409); `escalations.md` E3 → CLOSED at R-9. MODULE_CONTEXT ×2 (rest-api §E3-HEALTH 8 rows + 2 gotchas + the `ProblemType` cell 13 → 14 + the R-6/R-8 probe gotcha marked retired; lifecycle blockquote).
> **Red-first (HEAD 89a912e, once):** lifecycle `16 tests completed, 2 failed` — `healthServesHeaderlessOnLoopbackWhileDataRoutesStayGuarded` `expected: 200 but was: 401` (`HomeSynapseCoreTest.java:396`), `healthStaysGuardedOffLoopback` `expected: 200 but was: 404` (`:449`, the authed off-loopback GET; its 401 half passed at HEAD); rest-api `compileTestJava` 31 `cannot find symbol` (the four missing symbols) — disclosed as red-by-absence.
> **Desk gates GREEN on the final bytes:** `bash -n` ×7 + the CI lint loop over all `distribution/**/*.sh` · `dash -n`/`sh -n` on the three POSIX files · the helper's validation arms exit 2 ×6 · check 3b's fragment under a stub probe (green arm PASS/`FAILS=0`, red arm `bad`+diag/`FAILS=1`) · `ok` sites 20 → 21 · all banners 79 columns · `systemd-analyze --man=no verify` (WSL, systemd 255) parses the unit, the only non-comment delta = the `ExecStartPost` line · LF + trailing newline ×10 (zero CR bytes; `git ls-files --eol` = `i/lf w/lf`) · twins byte-identical + porcelain-clean · `shellcheck` ABSENT (flagged; CI re-covers).
> **P1–P15: ALL verified at `89a912e` pre-implementation, zero STOP-grade mismatches** (P10's `install.sh` cite is :89 at this checkout, same statement; P15's baselines re-derived from the test-results XML: 130/24/60). Rulings honored: **R-H1 LOOPBACK-ONLY** (the classifier + the site-local e2e arm, which `HomeSynapseConfig.bindHost` admits with no production code — P13) · **R-H2 IN** · **R-9-a** (SUSPENDED in the matrix + a `SubscriberMode` set pin) · **R-9-b** (the DANGER block). **Pushback/findings filed (return §5):** the HEAD third answer (item 1) · the DNS-safe literal tightening (item 3) · the unit header's duplicate "two-line edit" claim (item 4) · OUTSIDE the census, not edited: **`homesynapse.service:31` `LogDirectory=` is not a systemd directive (`LogsDirectory=` is — `systemd-analyze`: "Unknown key name … ignoring"; `/var/log/homesynapse` is therefore NOT implicitly writable under `ProtectSystem=strict`; harmless until `LinuxSystemPaths.logDir()` is wired)** · `boot-contract-map.md:70–:82` stale (no-unauthenticated-endpoint + the staged flip) · Doc 09 §15 Q1 fold owed (docs repo) · the banners' "then delete the token file" are TRUE again (no edit).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the core commit msg (post-diff) → Nick's commit + main push (stages EXACTLY the 24; `HealthEndpoint.java` + `HealthEndpointTest.java` are the 2 A) → **Build & Check + install-smoke BOTH legs green on that push = the gate of record** (rest-api 147 · lifecycle 62 · app 24; 19 PASS lines per leg — quote check 3b's line from the amd64 log; update-smoke green) → **§OP-H on the bench** (≤10 min: the probe line `ready (200) at http://127.0.0.1:7070/health`, the 401 fence, the exact `{"status":"LIVE"}` body — the bench's probe is the source-tree script, the `installDist` bench has no `/opt` libexec; rebuild the bench from the landed commit first) = the WU's H8 close; the interim operator law (never revoke the artifact's key / never delete the artifact) RETIRES at that beat → **R-7b** (the version scheme + the skeleton fence + `HS_DIST_DIR` + the node/upload majors + E1; the hub's beat-6 audit §4 design) → **R-3b** carries the packaged-unit artifact-absent restart proof as its first block → the §7 pointers as their own WUs (`LogsDirectory=` + a `systemd-analyze verify` lint step · the `boot-contract-map.md` fold · the Doc 09 §15 Q1 fold · the F-V2 residue). This lane HOLDS until the audit verdict or the next instruction.

---

## R-7 / W2-1 — the arm64 artifact channel (+ F-V1 + the action-deprecation rider) (CORE repo) — DELIVERED (2026-08-22; authoritative for the CORE lane until R-9 above; supersedes the R-6/R-8 DELIVERED entry below as lane-newest)

> **DEFERRED GATE (hub, then Nick, then CI):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-08-22_R7_arm64-artifact-channel_return.md` + this entry). **Deferred Build Gate: N/A-BY-DESIGN** — ZERO Java touched (scripts + workflow YAML + one doc); the instruction orders no gradle for this lane. The gate of record = **install-smoke on Nick's push, BOTH matrix legs green (`ubuntu-latest`/amd64 + `ubuntu-24.04-arm`/arm64) + Build & Check green** (law 16; the three R-E bumps are proven by CI — a red from a bump = revert that ONE bump, disclose, never retune blind; an arm64 leg that never schedules = STOP, the hub rules the QEMU fallback). Working tree: core **`62dbca3`** (the instruction said `33861ad`; the census trees byte-identical between them — return §0) + **EXACTLY 7 at porcelain** (6 M: `.github/workflows/ci.yml` · `.github/workflows/install-smoke.yml` · `distribution/ci/install-smoke.yml` · `distribution/common.sh` · `distribution/docs/architecture.md` · `distribution/image/build-image.sh` + 1 A: `distribution/smoke/version-grammar-test.sh`). Twins byte-identical POST-edit (sha256 `85039248…` ×2; the WIRING-SEAM header verbatim); hardlink pre/post-check nlink=1 ×7. Nothing committed.
> **The delta:** the install-smoke job is a MATRIX (`fail-fast: false`; `include: [{runner: ubuntu-latest, arch: amd64}, {runner: ubuntu-24.04-arm, arch: arm64}]`; `runs-on: ${{ matrix.runner }}`; the job name carries `${{ matrix.arch }}`; `on.push.paths` gains the workflow file; artifact `distribution-artifacts-${{ matrix.arch }}` — the amd64 name CHANGED from `distribution-artifacts`, consumer-visible, nothing consumes it yet) + two NEW steps after "Assemble .deb": the ARCH-TRUTH assert (`dpkg-deb --field <deb> Architecture` == the leg's arch; `::error::` + exit 1 naming both strings) and the VERSION-GRAMMAR echo (the live `hs_version` AND the `.deb`'s stamped `Version` must match `^[0-9]+\.[0-9]+\.[0-9]+`; stamped == the image `VERSION` file hard; live ≠ stamped only a `::warning::`). R-D/F-V1: `hs_version`'s `case` → `*.*)` pass-through / `*)` wrap as `0.1.0+g<id>` (the dot discriminator; the old `[0-9]*)` arm shipped `7c9e4fa` BARE on the 2026-08-22 Block-0 build while `d26777c` wrapped) + the `build-image.sh` grammar assert (charset `case` + `grep -Eq` on the regex, strings verbatim, placed right after `die()` since `die` is undefined at `:37`, outside any `$( )`, before the first use at `:58`) + `smoke/version-grammar-test.sh` (23 checks: 7 arm fixtures through a stub `git` on PATH from a hermetic cwd with `HS_VERSION=` — both bypasses disabled by construction — + the regex accept/reject sets + the charset mirror + the literal pinned in its three carriers; `COMMON_SH=` drives a mutant) wired into Static lint via `bash …`. R-E: `checkout@v5` · `setup-java@v5` · `setup-gradle@v5` in all three workflow files (every major verified at the marketplace by `git ls-remote --tags`; `setup-node`/`upload-artifact` left at v4 — outside the ruling). Docs: `architecture.md` "The artifact channel" (the matrix, the two asserts, the artifact table + 7-day retention + the `if: always()` caveat, the desktop→Pi fetch idiom with `sha256sum` on every hop, the version-string rule, the one-time `--allow-downgrades`).
> **Desk gates GREEN on the final bytes:** `bash -n` ×9 · `sh -n`/`dash -n` on `common.sh` · the test 23/23 · **Mutant 1 (HEAD's arm) FAILED 2** (not 3 — `d26777c` is a-f-leading and wrapped even at HEAD; the test header corrected at the instrument) · **Mutant 2 (retyped regex in a scratch carrier) FAILED 1** (a first `sed` attempt silently did not apply — caught, redone via Python) · the assert fragment under `set -euo pipefail` ×9 inputs exact (bare id / `v1.2.3` die "not tag-shaped"; empty / space / colon die "not Debian-safe"; wrapped, tag, `-dirty`, `0.1.0-skeleton` pass) · PyYAML parse + structural asserts on all three workflows (14 steps in order, matrix ×2, `runs-on` a string, majors as ruled) · the two step bodies ×6 arms under `bash -eo pipefail` with a stub `dpkg-deb` (green; arch mismatch exit 1; bare stamped exit 1; stamped≠image exit 1; live≠stamped warning exit 0; bad live exit 1) · LF + trailing newline ×7 · `shellcheck`/`yamllint`/`actionlint`/`gh`/`dpkg` ABSENT on this host (flagged; CI re-covers).
> **P1–P8: ALL verified at `62dbca3` pre-implementation, zero STOP-grade mismatches** (P5 line numbers off by one: `VERSION=` `:35`, `ARCH=` `:36`). **Pushback filed (return §5):** item 8 — `_dist_dir()` resolves from the CALLER's `$0`, i.e. the cwd under the `bash -c` idiom, so the tracked `distribution/VERSION` (`0.1.0-skeleton`) wins only from `distribution/` or `distribution/image/` as cwd — the version of record is cwd-dependent (untouched; fenced); item 9 — `+g<sha>` builds do not order monotonically between themselves (`g7c9e4fa` < `gd26777c` by dpkg's segment compare), so "`--allow-downgrades` exactly once" holds for the `7c9e4fa` → `0.x` move only; `0.1.0+git<rev-list --count>.g<sha>` or a first tag fixes it — implemented AS SPECIFIED, the hub rules.
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the core commit msg (post-diff) → Nick's commit + main push (stages EXACTLY the 7; `git add --chmod=+x distribution/smoke/version-grammar-test.sh` optional — the workflow invokes it via `bash`) → **install-smoke BOTH legs + Build & Check green on that push = the gate of record** (quote the arm64 leg's six `[build-image]` lines — `version=0.1.0+g<sha> arch=arm64 jdk=21` · `bundled 55 jars` · the 16-module `--add-modules` line · `jlinked runtime → 61M` · `floor-presence assert green: all 16 …` · `MANIFEST.sha256 (147 entries)` — plus the two green assert lines and the leg's wall-clock) → **R-3b rides the new channel** (the CI-built `homesynapse_0.1.0+g<sha>_arm64.deb` from `distribution-artifacts-arm64` replaces the manual bench build; the R-3/R-4 packets carry the one-time `--allow-downgrades` for the two cards on `7c9e4fa`) → the §7 pointers as their own WUs (`escalations.md` E1 → (c) taken · `distribution/VERSION` vs `_dist_dir` · the version scheme's monotonicity · `setup-node`/`upload-artifact` majors · an optional twins `cmp` in Static lint). This lane HOLDS until the audit verdict or the next instruction.

---

## R-6/R-8 TOKEN-OPS — the token-rotation operator path (CORE repo) — DELIVERED (2026-08-22; NEWEST — authoritative for the CORE lane; supersedes the R-1/R-2 DELIVERED entry below as lane-newest)

> **DEFERRED GATE (hub, then Nick, then CI):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-08-22_R6R8_token-ops_return.md` + this entry). **Deferred Build Gate: NO on this desk** — the targeted gates (`:api:rest-api:compileJava :api:rest-api:compileTestJava :api:rest-api:test :lifecycle:lifecycle:compileJava :app:homesynapse-app:compileJava :app:homesynapse-app:test`, 63 tasks / 11 executed) + Spotless + the FULL `./gradlew check` (156 tasks, exit 0; `:lifecycle:lifecycle:test` 60/60 executed fresh; rest-api/app tests up-to-date from the targeted run on identical inputs, mtimes in the return §3) ran GREEN here on Windows/JDK 21.0.4/Gradle 8.8. The gate OF RECORD stays **CI on Nick's push** (law 16): Build & Check + install-smoke. Working tree: core **`33861ad`** (the instruction said `7c9e4fa`; the WU's trees are byte-identical between the two — disclosed §0) + **EXACTLY 16 at porcelain** (11 M + 5 A — the instruction's 14 + 3 minus `RecordingEndpointContext.java`, which already records every header; return §4/§5). Nothing committed.
> **The delta:** `OpaqueTokenStore` — `TokenSummary`/`OperatorRequestReport` (public nested records) · `summaries()` · `rotate(displayName)` (mint-then-revoke-rest, ONE `persist()`, artifact rewritten) · `processOperatorRequests()` (`config/token_ops.request`: read → **delete FIRST** → execute; unreadable → WARN + zero report + file LEFT; undeletable → WARN + zero report + NOTHING executes; one summary WARN with counts + the artifact PATH) · `storePath()` · `artifactCarriesDeadToken()` + the `revoke()` WARN (the readiness-probe finding) · `persist()`/`writeArtifact()` now temp+`ATOMIC_MOVE`, `rw-------` on POSIX (the javadoc's "atomically" made TRUE). `TokenAdminEndpoints` (A) + `RestFilters.installTokenAdminEndpoints` — `GET`/`POST /internal/tokens`, `DELETE /internal/tokens/{keyId}`; two-layer auth (filter + `fullAccess()`), identity PASSED IN from the Javalin layer (no seam widening), audit line `token admin: actor= verb= target=` key-ids-only via an injectable sink, `no-store` everywhere. `EndpointResponses.problem` sets `application/problem+json` AFTER `json(...)` (R-C — Javalin's `json()` sets `application/json` itself, verified at the 6.7.0 bytecode; RED captured at `GetEntityEndpointTest:73` before the fix). `HomeSynapseCore.bringUpHttpSurface` +2 lines (after `ensureInitialToken()`; after `installAdminEndpoints`). `Main` dispatches `token …` to `TokenCli` (A) BEFORE any directory write — read-only `token status` table + trailer, exit 0/2, injected clock. Helper `homesynapse-token`: `status | rotate | revoke <keyId> | mint <name>` (request file via `install … -m 0600 /dev/null` + append; `systemctl restart` blocks on ExecStartPost); no-arg output byte-identical under dash. Doc `distribution/docs/token-rotation.md` (A) incl. the packaged readiness-probe caveat. MODULE_CONTEXT ×3 delta rows.
> **Desk gates:** `dash -n`/`sh -n` clean (shellcheck ABSENT on this host — flagged; CI re-covers); the helper's functional arms run under dash (every validation arm exit 2; the no-arg `cmp` byte-identical present+absent); **one defect caught here before handoff** — the `$(printf '
')` newline check stripped to `""` and rejected every `mint` name; fixed to a literal-newline variable, planted-control re-proven. Tests: rest-api 102 → 125 `@Test` (files 21 → 22; `OpaqueTokenStoreTest` 7 → 21, 3 POSIX/non-root-gated SKIP on Windows and run on the Linux runner; `TokenAdminEndpointsTest` 9), app 8 → 13 (`TokenCliTest` 5), lifecycle 60 → 60. **Review fleet (ultracode, 6 lenses + 2 refuters/finding) adoptions before handoff:** `skipped` entries never echo operator text (a pasted raw token would reach the journal) · `mint()` rejects a scope containing `,` (the CSV would reload it as a silent `*` grant) + 400 at the endpoint · `rotate` WARNs on a failed artifact write after a durable persist · `no-store` on problem responses too · `token status` on an unreadable store = one line + exit 1 · helper asserts consumption after the restart (`Request NOT consumed` → exit 1) + re-asserts owner/mode · the `NO_DIRECT_TIME_ACCESS` wording (the rule EXCLUDES `com.homesynapse.app..`, tests included) · the doc's bench paths written out (`~/hs-bench`). Dispositions table: return §3b. **The fleet's one CONFIRMED-2/2 defect (F1):** the packaged `Type=exec` `ExecStartPost` probe caches the pre-rotation artifact token at exec time (`health-probe.sh:87`), so a `rotate` restart would 403 → unit failed → helper exits non-zero though the rotation persisted — CLOSED in-census (the helper removes the artifact before a `rotate` restart; the probe then waits for the NEW one); root cause (the probe's cache) = §7 item 1b, outside the census. Also adopted: `runuser` for an already-root `status` (sudo is not a Depends) · a symlink/non-regular-file refusal at the request path. Fixed clocks everywhere; zero `synchronized`; zero build-file/module-info edits.
> **P1–P11: ALL verified at `33861ad` pre-implementation, zero STOP-grade mismatches**; P11 ⇒ R-C IN. Rulings honored: R-A IN (+ the audit rider) · R-B TZ-HOLD (PI-TZ untouched) · R-C IN (order corrected — return §5 item 2). **Pushback filed (return §5):** the R-C order (bytecode evidence) · the readiness probe authenticates with `initial_api_token` (`health-probe.sh:25/:85–:99`), so a bare `revoke` of that key or a deleted artifact FAILS the next packaged restart — doc caveat + store WARN + helper usage text; the `postinst`/`install.sh` "then delete the token file" banner now contradicts the unit (§7, outside the census).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the core commit msg (post-diff) → Nick's commit + main push (stages EXACTLY the 16) → **Build & Check + install-smoke CI green on that push = the gate of record** → **§OP-A on the bench** (the `rotate` request through the NEW path; expect the summary WARN once, `token_ops.request` gone, both files `-rw-------`, re-pair; refinements in the return's §OP-A) → the §7 pointers as their own WUs (E3 the probe's artifact dependence · the F-V2 residue in `IssueCommandEndpoint.problemWithFields`/`ReadinessFilter` · the lifecycle e2e pins · R-6's remaining riders: health/status endpoint · `SuccessExitStatus=143` · real `Depends`). §OP-B (PI-TZ) stays TZ-HOLD until R-5. This lane HOLDS until the audit verdict or the next instruction.

---

## R-1/R-2 — the packaged write-path assert (red-first) + the jlink fix (CORE repo) — DELIVERED (2026-08-20; NEWEST — authoritative for the CORE lane; supersedes the B′ DELIVERED entry below as lane-newest)

> **DEFERRED GATE (hub, then Nick, then the card):** the hub's audit precedes ANY commit (audit inputs: the return at `context/audits/2026-08-20_R1R2_return.md` + this entry). **Deferred Build Gate: N/A-BY-DESIGN** — ZERO Java touched (scripts + workflow YAML only); the instruction's §3 orders NO gradle for this lane. The gate of record = **install-smoke CI green on Nick's push** of exactly these 4 M at/atop `c091f7c` (law 16); the RED/GREEN hardware legs are **§OP (Sat 2026-08-22, the held-card sitting — NEVER inside 03:00–04:15 CT)**. Working tree: core **`c091f7c`** + **EXACTLY 4 M** (`distribution/image/build-image.sh` · `distribution/smoke/run-smoke.sh` · `.github/workflows/install-smoke.yml` · `distribution/ci/install-smoke.yml`) + the known pre-existing `?? _scratch/`. Twins byte-identical POST-edit (diff-proven); hardlink pre-check nlink=1 ×4.
> **The delta:** R-1 — run-smoke check 4 "EVENT WRITE PATH IS LIVE" after the health probe (checks 4–8 renumbered 5–9, digit-only): probe 1 asserts ≥1 row in `events` at `${HS_DB_FILE}` (the update-smoke `sqlite3 file:…?mode=ro` idiom; sqlite3-absent = honest `bad`, never a skip; PASS prints the observed count), probe 2 asserts ZERO `grep -icE 'NoClassDefFoundError|jdk.jfr|BusMetrics'` hits over journal + stdout log (the H3-pinned real signature — the unescaped dot nets `jdk/jfr` AND `jdk.jfr` spellings; zero-scannable-sources = honest `bad`; PASS prints the grep'd-zero + source count); the D-6 line shipped VERBATIM in the check; all arms count via `bad` (the `set -uo pipefail` no-early-exit law held). R-2 — build-image: full-lib jdeps (every `${IMAGE}/lib` jar a root via `find|sort -z|xargs -0`, flags + FLOOR-union byte-unchanged) · FLOOR gains `jdk.jfr` · NEW 2b floor-presence assert (`runtime/bin/java --list-modules` must contain EVERY `${ADD_MODULES}` entry, `grep -qxF` per name, `die` printing ALL absentees, green path prints the module count; passes-but-false comment VERBATIM) · the :75–:77 comment now describes the actual invocation (the divergence was PART of F-23). CI twins: one identical "Install smoke prerequisites (sqlite3)" step before run-smoke (duplicate-not-move — update-smoke's own install stays for self-sufficiency; disclosed).
> **Desk gates GREEN on the final bytes:** `bash -n` ×2 clean · shellcheck ABSENT on this host (flagged; CI's static-lint step re-runs bash -n over `distribution/**/*.sh` on the push) · fragment dry-runs exact (probe-1 case arms incl. empty/non-numeric/multiline-unreadable + 0-dead + count-print; probe-2 arithmetic under `${VAR:-0}`; the grep count idiom 0-clean/3-on-H3-sample; the floor loop passes-complete + dies-naming-missing) · census exactly 4 M · **nothing committed** (the lane commits NOTHING).
> **P1–P9: ALL verified at `c091f7c` pre-implementation, ZERO STOP-grade mismatches.** Sharpenings filed in the return §5 (report-and-proceed): persist-precedes-notify (`NotifyingEventPublisher` :55/:56 vs the H3 trace's throw AT :56) ⇒ the held card's RED most likely = rows-PRESENT + ~6 throw hits — the §OP disjunction's second arm, exactly what probe 2 exists for; `SystemStartedEvent` is registered but UNPUBLISHED today — the boot-≥1-row guarantee rides the integration-lifecycle publishes (H3-proven CALLED six-per-boot hardware-less) + `update-smoke.sh:8`'s standing "boot writes lifecycle events"; P2's grep nets 10 files (8 main + module-info + test) vs the row's main-consumer EIGHT — enumeration scope, not drift. Red-first accounting (#18, disclosed): CI is green-by-construction; the ⏺ RED→GREEN pair on the held card (`0.1.0+gd26777c` → the rebuilt artifact) is the WU's red-first evidence of record. #22 N/A-disclosed (no FE surface).
> **NEXT WU (refuse-to-close pointer):** the hub's audit on the return → the core commit msg (post-diff) → Nick's commit + main push (stages EXACTLY the 4 M) → **install-smoke CI green on that push = the gate of record** → the §OP Saturday card sitting (block 1 RED on the held card BEFORE the fix reaches it · block 2 rebuild + GREEN · block 3 restore; ⏺ intake slots = return §6; BOTH-CLEAN on block 1 = F-23 refuted at the instrument → STOP, hub adjudicates) → **R-3 / PKG-E2E-1** (weekend 2, MINTED DISTINCT — PrivateDevices loosening · the rehearsal rig on the preserved held card · integration-under-load). The D-1 claim fences stand VERBATIM until R-4 — nothing in this WU lifted any claim; `distribution/README.md:117` untouched. This lane HOLDS until the audit verdict or the next instruction.

---


_Rotation 2026-08-21 (v54 hub, beat 10): the 21 entries below the R-1/R-2 DELIVERED entry (M9.5-DURd-B′ DELIVERED 2026-08-08 and older) + the pre-R1R2 frontmatter chain → `archive/coder-handoff-entries-rotated-2026-08-21.md` (verbatim; order preserved; prior rotations 2026-07-26 / 2026-07-10 / 2026-06-21 + the monthly files). In-entry supersession pointers ("the entry below") resolve there. Zero content loss._
