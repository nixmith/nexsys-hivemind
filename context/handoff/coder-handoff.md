<!--
file: context/handoff/coder-handoff.md
purpose: Coder session continuity — current task, deferred build gate, next WU, recent closeouts.
audience: Coder, PM
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-09-23 (hub, v78 beat 5 — IR-40 + IR-44 RETURNED (11,998 B) and ACCEPT: `INDEXED BY idx_events_event_time` at `SqliteEventStore.java:237` with the plan pinned (the planner flips on the bound `LIMIT ?` value at ≥ 500 — production's `SCAN_BATCH`); the `automation` arm at `HomeSynapseCore.java:1648`; q1 flat in N (1.2 / 1.0 / 2.0 ms at 10k / 100k / 243k vs the walk's 1.8 / 10.6 / 42.3); the core card 6 = 5 M + 1 A → CI (13/20); IR-48/49/50 opened; THIS FILE ROTATED to a lane-sized record — the entries older than ENERGY-READ verbatim at `archive/coder-handoff-entries-rotated-2026-09-23.md` (bytes asserted); NEXT WU: none dispatched — the hub names it at v79 (LINK-READ-2 = IR-45 after Thu 09-24, or MC-ROTATE-1); Wed 2026-09-23 ~00:2x CT (instrument 2026-09-23T05:20:53Z).) Prior: 2026-09-21 (hub, v78 beat 4 — LINK-READ LANDED `d2cddb1` GREEN at CI (12/20); IR-40 + IR-44 DISPATCH-READY: `context/instructions/2026-09-21_coder-lane_IR40-IR44_time-range-plan-on-the-index_startup-report-automation-arm_coding-instruction.md` (`INDEXED BY idx_events_event_time` + the plan test; the `automation` arm at `HomeSynapseCore.java:1638`); this file is rotated to ≤ 12 KB at the v78 close; Mon 2026-09-21 ~14:0x CT (instrument 2026-09-21T19:05:44Z).) Prior: 2026-09-21 (hub, v78 beat 3 — LINK-READ RETURNED (13,678 B) and ACCEPT: the two forks (the frozen DP-8 line; the RSSI bound) ruled as AUDIT CORRECTIONS 1–2 in the instruction's §15–§16; D-2 KEEP; the core card 12 = 9 M + 3 A in Nick's hands → CI (12/20); IR-45/46/47; NEXT WU: IR-40 + IR-44 (D-v78-1; chartered v78 b4); Mon 2026-09-21 ~13:3x CT (instrument 2026-09-21T18:36:51Z).) Prior: the four older segments (v78 b2 → the oldest) verbatim at archive/coder-handoff-entries-rotated-2026-09-23.md §0 (rotated v78 b5, 2026-09-23)
-->

# Coder Session Handoff

> **CURRENCY STAMP (v59 close, 2026-08-31):** every entry below is HISTORICAL. Core HEAD **`f519f42` CLEAN** — PKG-SEC-1 LANDED `c368909` (CI green, run 33346067195; OR-TOKEN-MODE-644 CLOSED) and FE-HONEST-1 LANDED `f519f42` (frontend.yml 33418520416 green). run-smoke ok-sites = 23 (the 19-PASS / 18/18 counts below are historical, never re-pin). Next core WUs charter at R-10: the CG-1/2/3 additive contract amendment · the F-R4-1 adoption-gap ruling · PKG-SEC-2 (`context/planning/2026-08-30_R10-docket_additions_from-R4.md`).

---

## ENERGY-READ — metering by clusters; the divisors at adoption; scaled reporting; the rate instrument (CORE repo, integration/integration-zigbee; 22 = 16 M + 6 A, UNCOMMITTED on the desk — the b5 card lands it) — DELIVERED (Fri 2026-09-18 ~14:4x CT; NEWEST — authoritative for the CORE lane; supersedes the DUR-1 + HASH-1 DELIVERED entry below)

> **DEFERRED GATE — RESOLVED at v76 beat 6: core `d1c2cbc`, CI #253 GREEN at the instrument (id 35394284061: `Run check` ✓, the uploads ✓, `Run the bus soak` ✓ → the closure counter 8/20); install-smoke #61 both arches ✓ (the b6 audit §0).** The desk's gate line was green in one round (659 zigbee tests, 1 skipped by design; the app's 30 incl. the ArchUnit rules; spotless clean) — the return `context/audits/2026-09-18_ENERGY-READ_return.md` (17,555 B); the hub's audit `context/audits/2026-09-18_v76-b5_ENERGY-READ_intake_two-layer-audit.md`.
> **The delta:** `EndpointClassifier` (IR-15's zone map; `withMeters` by the clusters present; the classification INFO line) · `MeteringFormatting` (A) · `ElectricalMeasurementHandler` (A) · `MeteringHandler` (A) · `ClusterHandlers` (8/9/10) · `ReportingOps.readAttributes` + `EzspReportingOps` (the read; the uint48 change field's encode AND read-back widened to `long` — a HEAD wire defect the lane measured) · `ReportingConfigurator` (the metering rows out of DEFAULTS; the formatting read on the adoption drive, the cache on re-link; 1 W / 5–600 s and 5 Wh / 5–3600 s scaled by the read pair; unreadable or non-kWh → not configured) · `ZigbeeDeviceCache` (the `learnedMeteringFormatting` section) · `ZclIngestionUnit` (the seed/hook; `agreedAcross`; `metering_unscaled` once) · `ZigbeeIntegrationAdapter` (the wiring) · `MODULE_CONTEXT.md` · the rig fixture · 9 test classes. MEASURE-2: `rate=24031.1` events/s in memory (record rows M2-1..3).
> **Deviations ruled (the b5 audit §3):** R-1 the R6 line without `device=` and R-5 `SENSOR` for a metering-only endpoint → **ENERGY-READ-b** (two rows: the call-site log; `ENERGY_METER` when 0x0702 and no other arm) rides the next Java-slot session; R-2 the values ride INFO `zigbee.metering_formatting_read`; R-3 the MEASURE-2 knob is the env var; R-4 the wire defect FIXED; R-6 → IR-32.
> **NEXT WU (refuse-to-close pointer; re-cut v78 b5, THE CLOSE):** none dispatched — the hub names the Java slot's next at v79's first beat: **LINK-READ-2** (IR-45: `AvailabilityChangedEvent` v2 with its upcaster; the DIAGNOSTIC entity per device with `rssi_dbm`/`lqi`; `link-quality.yaml`'s pin flipped by a constants re-mint) after Thu 09-24's adoption, or **MC-ROTATE-1** (the six MODULE_CONTEXT histories rotated, bytes asserted) between units. IR-40 + IR-44 DELIVERED (the entry below; the core card in Nick's hands; EXECUTED when CI is green on its sha). LINK-READ LANDED `d2cddb1` GREEN. THE ADOPTION FENCE lifts at Thu 2026-09-24's CHAR session; no device joins before it; LINK-READ deploys to the bench card only after THE THURSDAY ORDER.

> **LINK-READ — the per-device frame counter; the last link reading on every transition; the sampling rule (CORE, integration-zigbee + one lifecycle IT; 12 = 9 M + 3 A, UNCOMMITTED) — DELIVERED (Mon 2026-09-21 ~13:2x CT).** `check` green on the desk; CI on the landing sha is the gate of record. The delta: `LinkReading` (wire domain + total `fromWire`) · `onFrame`/`recordFrame` carry `Optional<LinkReading>` · the counter in `route()` · `zigbee.link_summary` and `zigbee.availability_link` on the adapter · `zigbee.link_reading_suspect` once per device · `zigbee.availability_changed` FROZEN, untouched. AC1 + AC2 the hub's; D-2 awaits a ruling. NEXT WU: IR-40 + IR-44 (D-v78-1); LINK-READ-2 = IR-45. LINK-READ deploys to the bench card only after THE THURSDAY ORDER.

> **IR-40 + IR-44 DELIVERED (2026-09-21, uncommitted on `d2cddb1`): 5 M + 1 A.** `SELECT_BY_TIME_RANGE_SQL` gains `INDEXED BY idx_events_event_time` (package-private); `SqliteEventStoreTimeRangePlanTest` T1 (the plan at bound maxCount 1/7/400/500/1000/unbound) · T2 (semantics vs a data-computed list) · T3 (index dropped → `no such index`); `recommendationFor` gains the `automation` arm, `HomeSynapseCoreStartupFailureTest` T10 pins it verbatim. `check` GREEN in-lane (persistence 416 · lifecycle 95 · app 30). Finding: the planner flips on the BOUND `LIMIT` value (index ≤ 400, walk ≥ 500) — `SCAN_BATCH = 500` walked. MEASURE-2b re-read: §7. NEXT WU: the hub's (IR-45 per the v78 plan); the Coder stays blocked on the next instruction.

> **The older entries (DUR-1 + HASH-1 → the beginning; 190 lines) were rotated VERBATIM to `archive/coder-handoff-entries-rotated-2026-09-23.md` at v78 beat 5 (2026-09-23); a lane reads this file whole (now lane-sized) and the archive never.**
