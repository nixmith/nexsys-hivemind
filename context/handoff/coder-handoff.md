<!--
file: context/handoff/coder-handoff.md
purpose: Coder session continuity — current task, deferred build gate, next WU, recent closeouts.
audience: Coder, PM
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-08-02 (Coder, B3.1+B3.2 closeout — the B3.1+B3.2 DELIVERED entry prepended [newest, authoritative for current task + next WU; supersedes the DISPATCH entry below it]. BENCH repo, tree UNCOMMITTED on `a791c99` + EXACTLY 8 porcelain entries (all M, zero ??). ALL desk gates GREEN in-session against the FINAL tree: nightly stub harness 50/50 (baseline 10 RED / 40 GREEN under the corrected instrument) · runner fixture harness 27/27 (baseline 19 RED / 8 GREEN; the night-2 app-log-slice absence reproduced RED first) · digest selftest 21/21 byte-exact (A-7 lockstep not needed) · bash -n + py_compile ×5 + suite auto --list 9-lawful with the NEW banner `runner B3.1-2026-08-02-postwindow @ a791c99` + rejoin --list lawful + poisoned-injection REFUSED exit 2 + CR-scan clean with planted control proven. Return filed to context/audits/2026-08-02_B3.1-B3.2_return.md; ZERO [REVIEW] — ten [INFO] + four OBS ride it. The hub two-layer audit precedes ANY commit; ⚠ deploy embargo holds until the Aug-3 morning-gate intake.) Prior: 2026-08-02 (v44 hub, beat 2 — the B3.1+B3.2 DISPATCH entry prepended [newest, authoritative for current task + next WU; it consumes the WU-AVAIL-SEED DELIVERED entry's next-WU clauses — the audit COMPLETED ACCEPT zero-defects (v42 beat 10), the commit landed `60d3ab5`, the convergent deploy ran 2026-08-01, and the attended F2 rep is BANKED (F2 [M] closed, v43 beat 7)]. BENCH repo, baseline `a791c99` CLEAN (hub-verified at authoring). Launch gate: Nick's mirror-sync confirmation precedes the session. Instruction: context/instructions/2026-08-02_B3.1-B3.2_nightly-hardening-and-rejoin-repin_coding-instruction.md; return: context/audits/2026-08-02_B3.1-B3.2_return.md. ⚠ DEPLOY EMBARGO: nothing from this WU reaches the Pi before the Aug-3 morning-gate intake — the bench is an armed natural experiment tonight. Next WU after B3.1/B3.2 = the S-5 small-fix stack (core lane: the sqlite-jdbc bump FIRST + STATE-DIALECT P2 · FE lane: FE-LIVE-V112 (f)/(g)), queued behind the B3.1/B3.2 audit.) Prior: 2026-07-31 (Coder, WU-AVAIL-SEED closeout — the WU-AVAIL-SEED DELIVERED entry prepended [newest, authoritative for current task + next WU]; CORE repo, tree on `4288a9d` + EXACTLY 13 porcelain entries (12 M + 1 ?? — the §4 census; 11 in-module incl. testFixtures + the two §7-sanctioned lifecycle TEST-tree touches); ALL in-session gates GREEN: red-first staged (12 zigbee + 1 lifecycle behavioral reds XML-quoted at `4288a9d` incl. the mandated T-1/T-2 quotes + the stage-A2 compile-red on the new seams, disclosed) → green forced-fresh ×2 executed-state-proven (zigbee 557/0/0 · lifecycle 60/0/0) → spotless zero reformats → full `./gradlew check` GREEN 156 actionable (this WU's CC-lane grant); DP-3 executed Branch STOP per the dispatch-line ruling; return filed to `context/audits/2026-07-31_WU-AVAIL-SEED_return.md`; hub two-layer audit precedes ANY commit; ZERO [REVIEW] — eight [INFO] + three OBS ride the return; the attended F2 rep on the fixed build [pre-Aug-10] is the post-deploy half.) Prior: 2026-07-31 (v42 hub, beat 9 — the WU-AVAIL-SEED DISPATCH entry prepended [newest, authoritative for current task + next WU; it consumes the B3 DELIVERED entry's next-WU clauses (1)–(2), which are DONE — the P-block pasted, the pins minted at bench `7c8efbb`, the desk half audited ACCEPT and committed at `94864c7`; B3's REMAINING halves stay Nick-owned (the §9 install + the first digest + the §10 rejoin rep) and do NOT block this dispatch]. CORE repo, baseline `4288a9d` CLEAN (hub-verified at dispatch). Gate-critical: the F2 [M] producer. Launch gates: Nick's mirror-sync confirmation + the DP-3 ruling (DP-B) in the dispatch line — absent ⇒ G-4 skips DP-3 lawfully. Instruction: context/instructions/2026-07-31_WU-AVAIL-SEED_availability-seed-and-boot-truth_coding-instruction.md; return: context/audits/2026-07-31_WU-AVAIL-SEED_return.md.) Prior: 2026-07-31 (Coder, B3 DESK-HALF closeout — the B3 DESK-HALF DELIVERED entry prepended [newest, authoritative for current task + next WU]; bench repo, tree on `8bce651` clean + EXACTLY 9 porcelain entries (4 M + 5 ?? = 11 file-level, the return §7 census); ALL desk gates GREEN in-session (digest selftest 21/21 incl. the DP-4 line byte-exact · `suite auto --list` with the mechanical order assert · OPERATOR-refusal in BOTH modes, zero legs executed · the 5-run nightly.sh stub harness ALL 25 CHECKS GREEN incl. the TERM-mid-suite trap-restore proof · the R6 CR-scan control pair); ⛔PIN ×4 UNMINTED — the P-block is the return's §2 and Nick's NEXT ACT; return filed to `context/audits/2026-07-31_B3_return.md`; hub two-layer audit precedes ANY commit; ZERO [REVIEW] — twelve [INFO] + four OBS ride the return; the Pi install half (§9) + the rejoin-race rep (§10) PENDING behind the pins.) Prior: 2026-07-28 (Coder, B2 DESK-HALF closeout — the B2 DESK-HALF DELIVERED entry prepended [newest, authoritative for current task + next WU]; bench repo, working tree on `9f2b5ad` (clean at dispatch) + EXACTLY 14 porcelain entries (8 M + 6 ??) = the instruction's §8 census + the anticipated .txt sibling + TWO [REVIEW] census deltas (empty-chain fixture M · timeout-honesty-no-change M — both load-bearing, reasoned in the return); rider #1 red-first + demo-proven ×2 + final-tree ×2; all three §8 desk gates GREEN in-session; return filed to context/audits/2026-07-28_B2_suite-port_return.md; hub two-layer audit precedes any commit; THREE [REVIEW]s + nine [INFO]s ride the return; Pi half PENDING (operator). Next WU = B3 nightly — its rider-#1 gate is satisfied by the desk evidence.) Prior: 2026-07-28 (v40 hub, beat 1 — the WU-B2 DESK-HALF DISPATCH entry prepended [it consumes the DASH-SERVE entry's next-WU=B2 pointer]. Launch preconditions: Nick's skills-mirror sync + the beat-1 hivemind push. Bench baseline `9f2b5ad` clean; the desk gate of record = the runner-demo fixture suite + lint, no gradle. Next WU after B2 = B3 nightly [gates on rider #1].) Prior: 2026-07-27 (Coder, DASH-SERVE closeout — the DASH-SERVE DELIVERED entry prepended [newest, authoritative for current task + next WU]; core repo, working tree on `2040a66` (clean at dispatch, P0 exact), census EXACTLY 13 (12 M + 1 new fixture), all in-session gates GREEN incl. full `./gradlew check` [this WU's CC-lane grant: the targeted loop + one full check + the mandated Node-free grep, quoted] + in-session mutation verification M1–M3 with named kills + cmp-proven restores; hub two-layer audit precedes any commit; ZERO [REVIEW] — seven [INFO]s ride the entry; next WU = B2 [bench lane, hub-authored; core QUIET until the M14 WUs].) Prior: 2026-07-27 (v39 hub, beat 1 — the WU-DASH-SERVE DISPATCH entry prepended [newest, authoritative for current task + next WU; it supersedes the SKIP-VIS entry's next-WU=B2 pointer per the v38 beat-8 ratified sequencing: DASH-SERVE first (core lane, the idle Coder), B2 second (bench lane, hub-authored)]. Launch preconditions: Nick's skills-mirror sync (Check 9 mismatch recorded at the v39 launch) + the beat-1 hivemind push. Working tree baseline core `2040a66` clean.) Prior: 2026-07-26 (Coder, SKIP-VIS closeout — the SKIP-VIS DELIVERED entry prepended [newest, authoritative for current task + next WU]; core repo, working tree on `4bc1258` (clean at dispatch, P0 exact), all in-session gates GREEN incl. full `./gradlew check` ×2 [this WU's CC-lane grant: the targeted loop + one full check; the second check is the gate of record against the final tree] + in-session mutation verification M1–M3 with cmp-proven restores + a 4-lens adversarial review fleet (zero code defects); hub two-layer audit precedes any commit; ZERO [REVIEW] — seven [INFO]s ride the entry). Prior: 2026-07-22 (Coder, CMD-API closeout — the CMD-API DELIVERED entry prepended [newest, authoritative for current task + next WU]; core repo, working tree on `355a711` (clean at dispatch), all in-session gates GREEN incl. full `./gradlew check` [this WU's CC-lane grant] + in-session mutation verification M1–M3 with cmp-proven restores; hub two-layer audit precedes any commit; ONE [REVIEW] rides it — the app-tree ArchUnit Rule 9 scoping). Prior: 2026-07-22 CMD-API (kept below). The LEARN-PERSIST→M9.5-DUR-era entries + chain ROTATED 2026-07-26 (v38 hub, beat 5) → archive/coder-handoff-entries-rotated-2026-07-26.md; git history.
-->

# Coder Session Handoff

---

## B3.1 + B3.2 — nightly hardening + rejoin-race re-pin (BENCH repo, desk half) — DELIVERED (2026-08-02; NEWEST — authoritative for current task + next WU; supersedes the DISPATCH entry below)

> **DEFERRED GATE (hub, then Nick/operator):** the hub's two-layer audit precedes ANY commit order (the return at `context/audits/2026-08-02_B3.1-B3.2_return.md` + this entry are the audit inputs). Deferred Build Gate: **N/A-gradle** (bash/python/yaml repo); the desk gates of record RAN in-session against the FINAL tree, ALL GREEN: (1) the rebuilt-and-EXTENDED nightly.sh stub harness **50/50** across 7 runs (quiesced-PASS · FAIL-floor+evidence-copy · TERM-mid-suite with the NON-VACUOUS mid-suite swap assert · branch-B CONFIG-DRIFT carrier-byte-untouched · pin-refusal exit-2 zero-side-effects · restore-failed exit-3 · INT-mid-suite), red-first at `a791c99`: **10 RED / 40 GREEN** under the SAME corrected instrument — the reds exactly A-3 (stamps ×3) + A-4 (closing line ×6) + A-5 (the [OK] drift line); (2) the A-6/A-9 runner fixture harness **27/27** (baseline **19 RED / 8 GREEN**; the night-2 `app-log-slice ABSENT` class reproduced RED first; the one-read law, deadline path, rejoin field_equals shape, non-terminal boundary, and dry-run no-op all pinned); (3) digest selftest **21/21** byte-exact (A-7 docstring-only, no lockstep delta); (4) `bash -n` + `py_compile` ×5 + `suite auto --list` (9 lawful, park LAST, banner **`runner B3.1-2026-08-02-postwindow @ a791c99`** — ENGINE_VERSION bumped per the engine-touch law) + `suite rejoin-race-operator --list` lawful + the poisoned-injection probe (OPERATOR name in a SCRATCH constants copy → REFUSED, exit 2) + the CR-scan pair (zero hits; planted control FOUND then re-cleaned).
> **Delivered (per-item):** A-1 the INSTALL-6 repair RE-HOMED into the runner README red procedure (`--user-unit`, log-file-first, "the journal may have rotated; the log file has not") — the repo-wide sweep found **ZERO** in-repo `journalctl --user -u ` occurrences (the defective dialect lived only in the superseded hub-owned B3 return; full enumeration in return §5) · A-2 the §8-compliant week-1 morning gate (pgrep survival gate + full-path digest + systemctl show + journalctl --user-unit; both expected-benign notes) · A-3 the wrapper stream timestamped `[YYYY-MM-DD HH:MM:SS]` + line-buffered via a bash read-loop stamper (bash+date only — no tee/stdbuf, tee's stdio buffering WAS G-1; per-line writes to journal AND log; finish()-side drain; digest grammar untouched by construction) · A-4 the closing line as finish()'s last act (`exit path normal|trap-INT|trap-TERM|restore-failed|pre-suite-crash, exit code N`; exit-code contract byte-unchanged; header+README legibility text) · A-5 `[OK] drift check: live == live-basis` on the cmp-pass arm (boundary pair harness-proven) · A-6 `journal-slice.txt` DROPPED (one MANIFEST line; journalctl call + subprocess import removed) + the app-log capture TIERED (window cache → marker-offset re-read at bundle time [the night-2 fail-fast race] → time-window scan over the current boot log [measured HH:MM:SS.mmm stamp, 400-line cap DISCLOSED]; ABSENT only when every tier read zero, tiers named) · A-7 the docstring 0.11s labeled SYNTHETIC-EXAMPLE with the real night-1 3.65s named · A-8 `KillMode=process` folded into [Service] with the night-1 mechanism + the Aug-2 banked proof line quoted; header gains the daemon-reload refresh law + the harmless-drop-in note · A-9 `engine.capture_post_window_state` — ONE GET of the command target's `/state` on a command-class terminal-read FAIL (both fail shapes, live + fixture modes; classifier = phase_terminal OR field_equals-on-terminal-field; entity from the scenario's own resolved command stimulus; first-FAIL-wins, no retries, degrade-with-note never disturbs the verdict; dry-run safe) → bundle `post-window-state.json` + MANIFEST line, body VERBATIM untruncated; **wire pin from the FILED corpus checked before any source read** (WCAP capture-5 :27/:76/§3-F3 + B2 return :276 F-6 + constants :109–:114; citations in return §6). **B3.2:** DP-B1 the relink positive DELETED, header rewritten around the silent-resume finding with provenance (×4 incl. 13-min outage, v43 beat 7; boot-only relink ×6 Aug-2 04:31:43); forbidden identity pair STAYS · DP-B2 the hub-side premise adjudication stated as the PRIMARY contract (machine asserts = the disposition-agnostic terminal read + the A-9 rider; operator note now orders NOTE-THE-CLOCK — the timestamps ARE the premise evidence) · DP-B3 the long-cycle variant documented header-only with its filed anchors named (availability pair, ~80 s replug bound, IEEE hand-grep) + the explicit v0-cannot-express statement; zero engine growth for it.
> **Census EXACTLY 8, all M** (return §7 sweep-guard): scenarios/constants.yaml [s31-ieee COMMENT consumer-line amendment only — pin values byte-identical] · scenarios/rejoin-race-operator.yaml · tools/nightly.sh · tools/runner/README.md · tools/runner/bundles.py · tools/runner/engine.py · tools/runner/nightly_digest.py · tools/scheduler/nexsys-bench-nightly.service. bench.sh, runner.py, drivers.py, the timer, command-confirm-s31.yaml, command-s31-settle.yaml BYTE-UNTOUCHED (the armed experiment untouched; no retuning). Deviations: **ZERO [REVIEW]** — ten [INFO] (I-1 zero-occurrence sweep + README re-homing · I-2 the stamper-not-stdbuf mechanism · I-3 pre-suite-crash classified-not-simulated · I-4 the three-tier honest-absence realization + disclosed cap · I-5 the field_equals classifier arm + untruncated body · I-6 ENGINE_VERSION bump · I-7 README bundle listing rewritten; SCENARIO_FORMAT.md:43 NOT touched → OBS-1 · I-8 the constants comment amendment · I-9 the three harness-instrument corrections with baselines RE-RUN under corrected instruments against git-extracted `a791c99` · I-10 verdict block kept + note-the-clock) + four OBS (format-doc journalctl mention · PASS-bundle absence-by-design · the git-grep --untracked flag-order mint [lessons entry appended 2026-08-02] · the live A-9 seam first exercises on the first post-deploy FAIL night).
> **NEXT WU (refuse-to-close pointer):** the hub's two-layer audit FIRST — no commit until its order (staging = EXPLICIT paths, exactly the 8-file census). ⚠ **DEPLOY EMBARGO HOLDS:** nothing reaches the Pi before the Aug-3 morning-gate intake (the armed s31 park experiment adjudicates FIRST, instrument-first, predictions pre-stated in `context/audits/2026-08-02_B3_night2_command-confirm-s31_evidence-read.md`); the Pi pull + unit re-copy + `systemctl --user daemon-reload` ride a hub-authored post-gate operator block. After the audit → the S-5 small-fix stack (core lane: sqlite-jdbc `3.51.2.0 → >=3.51.3.0` at `gradle/libs.versions.toml:17` FIRST + the float-serialisation grep + full check, then STATE-DIALECT P2 · FE lane: FE-LIVE-V112 (f)/(g)). Nothing else dispatches from this lane while the audit is open.

---

## B3.1 + B3.2 — nightly hardening + rejoin-race re-pin (BENCH repo) — DISPATCH READY (2026-08-02, v44 hub beat 2; superseded by the DELIVERED entry above)

- **Instruction:** `context/instructions/2026-08-02_B3.1-B3.2_nightly-hardening-and-rejoin-repin_coding-instruction.md` (ISSUE-READY; bench baseline `a791c99` clean at authoring — re-verify porcelain at launch; returns to `context/audits/2026-08-02_B3.1-B3.2_return.md`). Licensed by the BANKED Aug-2 KillMode gate (pgrep 69055 alive post-`Finished`; systemd's own `remains running after unit stopped` line).
- **Scope:** B3.1 = the night-1 return §9 amendment set (items 1–8; item 9 done hub-side) + two night-2 additions (A-9 post-window state read on terminal-assert FAILs — wire pin from the FILED measurement corpus per format #20; A-6 widened FAIL-bundle app-log capture + journal-slice DROP). B3.2 = the rejoin-race evidence-anchor re-pin (the S31 silent-resume finding; DP-B1..B3 pre-ruled in-instruction).
- **Launch gates:** ⚠ Nick's mirror-sync confirmation precedes the session (standing lane-map law) · desk gates only (stub harness · runner fixture gates · digest selftest · bash -n / scenario lint) — no Gradle, no Pi. **The lane commits NOTHING.**
- **⚠ DEPLOY EMBARGO:** nothing reaches the Pi before the Aug-3 morning-gate intake (the armed s31 park experiment — `context/audits/2026-08-02_B3_night2_command-confirm-s31_evidence-read.md`). The Pi pull + unit re-copy + `systemctl --user daemon-reload` ride a hub-authored post-gate operator block.
- **NEXT WU (refuse-to-close pointer):** the hub's two-layer audit precedes any commit; after the audit → the S-5 small-fix stack (core lane: sqlite-jdbc `3.51.2.0 → >=3.51.3.0` at `gradle/libs.versions.toml:17` FIRST + the float-serialisation grep + full check, then STATE-DIALECT P2 · FE lane: FE-LIVE-V112 (f)/(g)).

---

## WU-AVAIL-SEED — the availability seed + boot truth (CORE repo) — DELIVERED (2026-07-31; NEWEST — authoritative for current task + next WU; supersedes the DISPATCH entry below)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (the return at `context/audits/2026-07-31_WU-AVAIL-SEED_return.md` + this entry are the audit inputs). Deferred Build Gate: **NO** — the full `./gradlew check` RAN in-session, GREEN (156 actionable tasks, 87 executed — the CC-lane grant: the targeted loop + ONE full check + spotless, all consumed). Working tree: core **`4288a9d`** (clean at dispatch, G-5 all pins exact) + **EXACTLY 13 porcelain entries** (12 M + 1 ??): integration-zigbee main ×4 (`StandardAvailabilityTracker` [the nested `Seed(Boolean, Instant)` record + persisted-instant seeding, NEVER boot time; evaluate widened to everything-but-UNAVAILABLE with null-lastSeen = infinitely stale; the `evidenced` mark + `isEvidencedAvailable`] · `ZigbeeDeviceCache` [the additive `lastEvidenceAt` file field + `recordEvidence`/`lastEvidenceAt`/`lastEvidenceSnapshot`; absent-field silent, malformed-value ONE-WARN degrade] · `ZigbeeIntegrationAdapter` [the sidecar seed replaces `Map.of()` + the DP-5(a) `zigbee.availability_seeded` INFO; onFrame single-instant evidence write; the ping arm's DP-5(b) `zigbee.availability_ping` DEBUG (ok|timeout|error) + evidence-on-ok; `seedFreshAdoption` gated on evidence] · `ZigbeeAdoptionSlice` [DP-3 **Branch STOP**: the relink publish DELETED, the log line preserved as the sole observable]) + module tests ×5 + testFixtures rig (`silence(long)` — the dead-device toggle) + MODULE_CONTEXT (WU section + the M-1 trap gotcha SUPERSEDED markers) + lifecycle TEST tree ×2 (§7-sanctioned, named in the return: `RestartHonestyIT` re-link barrier → the relink LOG line [the old await pinned the removed emission]; NEW `AvailabilityBootTruthIT` = T-7, the DP-2 floor SERVED-truth leg on the real composition root via `stateQueryService()`).
> **Gates:** baseline `4288a9d` porcelain-clean → stage-A1 behavioral reds: zigbee 544 tests EXACTLY 12 failed + lifecycle 60 tests EXACTLY 1 failed, every red XML-quoted for its designed reason (T-1 `expected: 1L but was: 0L` [Map.of() ⇒ never tracked/pinged] · T-2 `Expected size: 1 but was: 0` [no verdict without the seed] · T-5 `expected: 0L but was: 3L` [the boot burst visible] · T-7 `timed out awaiting the DP-2 floor` [F-14 reproduced under test]) → stage-A2 compile-red disclosed (`class Seed` ×3 + `isEvidencedAvailable` ×4 quoted from javac) → GREEN forced-fresh ×2 with executed-state proof (per-task `--rerun`, task lines + result mtimes verified — the v39 lesson applied): **zigbee 557/0/0 · lifecycle 60/0/0** → spotless zero reformats (diffstat byte-identical) → **full check GREEN 156 actionable**. Every PASS fixture-paired with its false-verdict boundary (fresh-recency quiet legs · window-exact 24 h→26 h pair · old-format + malformed-value pair · the T-7 in-test pre-convergence AVAILABLE assert · seeded-UNKNOWN first-evidence still edges).
> **Deviations: ZERO [REVIEW] — eight [INFO] + three OBS** (D-1 the two named lifecycle test entries · D-2 the RestartHonesty barrier rework [baseline-compatible, near-vacuous old await] · D-3 the superseded first-frame-edges test deleted [its trap gotcha rewritten] · D-4 the evidence guard [DP-1's grow-minimally sanction; closes the seed→adoption-seed manufacture hole] · D-5 malformed-value per-device degrade [the learnedZoneTypes posture] · D-6 the T-2-boundary leg red-at-baseline via the L1 burst [tooth = regression] · D-7 the ok|timeout|error enum realization · D-8 the rig silence toggle; OBS-1 the DP-2 stretch = G-3 floor-only with the stale-field derivation cited at `MaterializedStateQueryService:221-236` [read-time, staleAfter-fed, lawfully unreachable by adapter-private seeded-ness — design sketch in the return §7] · OBS-2 the registry-loss corner [test-only; production adoption is announce-preceded] · OBS-3 previousStatus="unknown" on seed-originated verdicts [listener memory, cosmetic, pinned]). New grep-stable tokens documented per WUCP: `zigbee.availability_seeded` · `zigbee.availability_ping` · `zigbee.evidence_recency_malformed`; `zigbee.availability_changed` verified firing identically on seed-originated verdicts; `availability_publish_conflict` + `device_relinked` untouched.
> **NEXT WU (refuse-to-close pointer):** the hub's two-layer audit FIRST — no commit until its order (staging = EXPLICIT paths, exactly the 13-file census). **Post-deploy: the attended F2 rep proves BOTH directions on the FIXED build before Aug-10** and gives the mains ping arm its first live exercise (DP-5(b) makes every ping grep-visible for it), pairing with the B3 §10 rejoin-race rep. Nothing else dispatches from this lane while the audit is open; the small-fix stack (STATE-DIALECT core P2 + FE-LIVE-V112 (f)/(g)) queues behind it; the FE redeploy (charge 4) stays hub/operator-owned in parallel; the DOCS-repo folds stay hub-owned.

---

## WU-AVAIL-SEED — the availability seed + boot truth (CORE repo) — DISPATCH READY (2026-07-31, v42 hub beat 9; superseded by the DELIVERED entry above; B3's Nick-owned install/digest/rejoin halves proceed in parallel and do not block)

- **Instruction:** `context/instructions/2026-07-31_WU-AVAIL-SEED_availability-seed-and-boot-truth_coding-instruction.md` (ISSUE-READY; CORE baseline `4288a9d` clean at dispatch; returns to `context/audits/2026-07-31_WU-AVAIL-SEED_return.md`). **Gate-critical: the F2 [M] producer** — the last open non-scheduled MUST; the attended F2 rep runs on the FIXED build before Aug-10.
- **Launch gates:** ⚠ Nick's mirror-sync confirmation precedes the host-CC session (standing lane-map law) · **the DP-3 ruling (DP-B) rides the dispatch line** (hub REC: stop manufacturing the relink "available"); absent ⇒ instruction G-4 skips DP-3 lawfully.
- **Shape (settled DPs — not open questions):** DP-1 the sidecar seed replaces `Map.of()` (`ZigbeeIntegrationAdapter:318`-era; seed ENTERS tracking, NEVER counts as evidence, publishes NOTHING) · DP-2 boot truth (FLOOR: a dead seeded mains device is SERVED UNAVAILABLE within one window post-boot; event-flood mechanisms REJECTED; arch breaches ⇒ floor + OBS, never rule edits; STRETCH: honest `stale:true` for the pre-convergence window if lawful) · DP-3 the relink emission per the ruling (both branches pre-ruled in-instruction; `ZigbeeAdoptionSlice:553`-era) · DP-4 `lastEvidenceAt` additive sidecar persistence (transition + coarse periodic + shutdown floor; false-UNAVAILABLE-brief is the lawful failure direction — never-false-ALIVE is the law; old-format sidecars load, absent-field tolerated) · DP-5 the one-grep observability surface (the boot seed line · per-ping DEBUG · `availability_changed` on seed-originated verdicts · the conflict WARN untouched).
- **Red-first mandated:** T-1 (silent seeded mains → UNAVAILABLE) and T-2 (stale persisted battery clock fires) quoted RED at `4288a9d` before the fix; every PASS pairs with its false-verdict boundary. Build grant: targeted loop + ONE full `./gradlew check` + spotless. The lane commits NOTHING.
- **NEXT WU (refuse-to-close pointer):** nothing else dispatches from this lane while AVAIL-SEED is in flight; the FE redeploy (charge 4) is HUB/OPERATOR-owned in parallel; the small-fix stack (STATE-DIALECT core P2 + FE-LIVE-V112 (f)/(g)) queues behind the AVAIL-SEED audit; the DOCS-repo folds stay hub-owned.

---

## WU-B3 — BENCH-AUTOSTART nightly (bench repo, DESK half) — DELIVERED (2026-07-31; NEWEST — authoritative for current task + next WU; supersedes the 'newest' tag on the DISPATCH entry below)

> **DEFERRED GATE (hub, then Nick/operator):** the hub's two-layer audit precedes ANY commit order (the return at `context/audits/2026-07-31_B3_return.md` + this entry are the audit inputs). Deferred Build Gate: **N/A-gradle** (bash/python/yaml repo); the desk gates of record RAN in-session against the FINAL tree, ALL GREEN: (1) red-first ×6 quoted at baseline `8bce651` (suite auto absent · --list absent · digest tool absent · digest/api_token verbs undispatched · nightly.sh absent); (2) `nightly_digest.py --selftest` **21/21** incl. the DP-4 example line BYTE-EXACT + the SKIP-honest first-night floor + the `n/a(<verdict>)` never-fabricate arms + the config-env placeholder refusal; (3) `bench.sh suite auto --list` — 9 legs load lawfully + the MECHANICAL order assert (resolved == `auto-suite:` key order; park LAST); (4) OPERATOR-injection REFUSED in both modes (scratch constants; run mode `ran 0/1 — 1 REFUSED` exit 2, ZERO legs executed — C-1 averted structurally); (5) the 5-run nightly.sh stub harness **ALL 25 CHECKS GREEN** (scratch http-server through the REAL curl+grep instrument): R1 quiesced/ABSENT-asserted + trap restore + `RESTORE-FAILED ⛔` honest-detection + latency 0.11s from a REAL api-captures fixture · R2 assert-failed honesty + `RESTORED ✓` + FAIL-floor-with-bundle + evidence-copy into the FAIL bundle only · R3 TERM-mid-suite with a MID-RUN swapped-out assert (restore proof non-vacuous) + a digest line ALWAYS lands · R4 branch-B CONFIG-DRIFT carrier-byte-untouched + `PRESENT ✓ (never swapped)` · R5 pin-refusal exit 2 zero side effects; (6) the R6 CR-scan anti-vacuous pair (`git grep -I --untracked` zero hits · planted control FOUND · re-closed) + `bash -n`/`py_compile` on final bytes.
> **Delivered (per-DP):** DP-1 `auto-suite:` key (9 legs, park LAST, consumers named) + `suite auto` key-order resolution + PRE-FLIGHT OPERATOR refusal + `--list` · DP-2 `tools/nightly.sh` (config-gate → quiesce → verify → `timeout 45m` suite → trap-guaranteed restore → verify → digest; suite BACKGROUNDED under `wait` — bash defers traps past foreground children, the signal-safety lesson) · DP-3 branches A+B behind ⛔PIN-1 with the cmp drift-guard (drift ⇒ honest UNQUIESCED run, never overwrite) + positive-evidence ABSENT/PRESENT reads to `<date>-quiesce-evidence.txt`, copied into failure bundles · DP-4 the one-line digest (all forms selftest-pinned; `bench.sh digest [N]`; missing-line=red stated in README) · DP-5 `tools/scheduler/` systemd-user pair (03:30 America/Chicago, Persistent, bash-prefixed ExecStart, stop-timeout sized for the trap) + cron fallback · DP-6 `nightly_digest.py latency` (lifecycle `.at` source-verified at GetCommandStatusEndpoint.java:242; PASS-only mining, `n/a(<verdict>)` else) + `on-latency.log` · DP-7 `rejoin-race-operator.yaml` (device-scoped relink premise via the NEW `command.s31-ieee` mint + disposition-agnostic terminal; loadable-for-scenario/REFUSED-for-auto both desk-proven) · R4 README nightly section (re-run trap by pointer) · R5 `api_token` verb · R6 `.gitattributes`. **Census EXACTLY 11 file-level** (4 M: constants.yaml, bench.sh, runner.py, runner/README.md + 7 new: nightly_digest.py, nightly.sh, scheduler ×3, rejoin-race-operator.yaml, .gitattributes) — return §7 sweep-guard; no engine.py churn (ENGINE_VERSION lawfully unmoved); existing bench.sh verb lines byte-untouched.
> **⛔PIN ×4 UNMINTED — the wrapper structurally refuses to run until they mint** (config-gate desk-proven). **The P-block (return §2) is Nick's NEXT ACT** (~5 min read-only: PIN-1 carrier grep · PIN-2 automations-list measure · PIN-3 scheduler discriminate · PIN-4 boot-slice capture). Deviations: **ZERO [REVIEW]** — twelve [INFO] (the honest digest-grammar extensions I-1 · `--list` I-2 · pre-flight-refusal shape I-3 · wrapper overrides I-4 · the CRLF/path byte-stability pair I-5/I-6 · no-lock I-7 · PASS-only latency I-8 · labeled desk fixtures I-9 · exec-bit posture I-10 · pycache I-11 · green-by-construction scan disclosure I-12) + four OBS for the hub (retention-policy copy-off NOT ordered/built · ENGINE_VERSION law · the boot-demo RED-arm era-staleness REC · P2 survey results).
> **Next WU (refuse-to-close pointer):** B3's own remaining halves — (1) Nick runs the P-block (return §2), pastes back; (2) the hub mints the pins into `constants.yaml` `quiesce:` + rules OBS-3 (the fixture re-mint scope) + audits/commits per the §7 census; (3) the Pi install half (return §9, INSTALL-1..6 incl. the NIGHT-1 park bootstrap + timer enable); (4) the first digest closes H2's cadence half; (5) the rejoin-race rep (return §10) at an attended session before Aug-10. Nothing else dispatches while B3 is in flight; the small-fix stack (STATE-DIALECT core P2 + FE-LIVE-V112 (f)/(g)) queues behind the hub's criteria re-status (v42 charge order).

---

## WU-B3 — BENCH-AUTOSTART nightly (bench repo) — DISPATCH READY (2026-07-31, v42 hub beat 2; superseded by the DELIVERED entry above)

- **Instruction:** `context/instructions/2026-07-31_B3_bench-autostart-nightly_bench-instruction.md` (ISSUE-READY; baseline bench `8bce651`; ZERO core changes; returns to `context/audits/2026-07-31_B3_return.md`).
- **Launch gate:** ⚠ Nick's mirror-sync confirmation precedes the host-CC desk session (standing lane-map law; the mirror is UNVERIFIED from the remote hub). No Gradle in this repo — desk gates are runner dry-runs (`--against` + the REV2 sibling-fixture mechanism).
- **Shape (settled DPs — not open questions):** DP-1 `auto-suite:` constants key + `bench.sh suite auto` (park LAST; OPERATOR-tier refusal ASSERTED — lexical `suite all` is UNLAWFUL for the nightly) · DP-2 `tools/nightly.sh` with restore under `trap` on EVERY exit path · DP-3 quiescence by config-swap (branches A/B both pre-ruled; ⛔PIN-1 discriminates; CONFIG-DRIFT → run honest-UNQUIESCED, never overwrite Nick's edits) + the ABSENT/PRESENT anti-vacuous evidence pair · DP-4 the one-line digest (`RESTORE-FAILED ⛔` = red; missing digest = red) + the `digest` verb · DP-5 scheduler pre-ruled (systemd-user REC / cron fallback; ⛔PIN-3; 03:30 America/Chicago REC) · DP-6 the ON-report-latency digest line (C4 + CONFIRM-SOUNDNESS feed; n/a-never-fabricate) · DP-7 `rejoin-race-operator.yaml` (C2's named third class; OPERATOR tier, never nightly) · R5 the `api_token` verb (F-1) · R6 `.gitattributes` + the CR scan w/ positive control.
- **MEASURE-THEN-PIN:** ⛔PIN ×4 (config carrier · automations-list route · scheduler branch · the I5 fixture slice) behind Nick's read-only P-block — the desk AUTHORS the P-block first (§8-compliant), pins from the pastes; never invents routes or paths.
- **NEXT WU (refuse-to-close pointer):** nothing else dispatches while B3 is in flight; the small-fix stack (STATE-DIALECT core P2 + FE-LIVE-V112 (f)/(g)) queues behind the hub's criteria re-status per the v42 charge order.

---

## WU-B2 — the §5.1 suite port + THE ENGINE REBIND (bench repo, DESK half) — DELIVERED (2026-07-28; newest, authoritative for current task + next WU)

> **DEFERRED GATE (hub, then Nick/operator):** the hub's two-layer audit precedes ANY commit order (the return at `context/audits/2026-07-28_B2_suite-port_return.md` + this entry are the audit inputs). Deferred Build Gate: **N/A-gradle** (Python/YAML repo); the desk gates of record RAN in-session, ALL GREEN: (1) the runner-demo fixture suite — the §2.4 table EXACTLY: the red quoted pre-rebind (missing-trigger-view [PASS]-on-triggeredAt where FAIL is honest), the predicted verdicts ×2 post-rebind + ×2 again on the FINAL tree; (2) lint — ZERO REFUSED across every new/modified scenario; (3) the no-tty suite dry parse ×2 — all 8 AUTO-list scenarios LOAD, `[SKIP] command-confirm — [hue-online]` + `[SKIP] usb-reenumeration — [usb-power]` with reasons, coverage `ran 6/8`, banner `runner B2-2026-07-28-rebind @ 9f2b5ad` before the first verdict line. **The §2.5 ordering law COMPLIED: the rebind landed and demo-proved BEFORE any Pi AUTO leg (none has run); B3's gate on rider #1 is satisfied by this evidence.** The PI HALF is PENDING (operator, instruction §4.2/§7: P-1..P-4 prep + S-1..S-3 measure-then-pin probes + the suite runs + bundles; the return's §10 carries the evidence slots).
> **Delivered:** rider #1 the REBIND (`eval_new_run_after` binds `data.trigger.matchedAt`, chain fetched FIRST, ignored-with-reason ×4 arms, NEVER a triggeredAt fallback; ok-payload `{runId, matchedAt, triggeredAt, mObserved, anchor, outcomes, agree}`; divergence = its own [INFO] line, never a fail; :930–:938 + lint :228–:251 + eval_new_confirmed_run + parse_iso_utc byte-preserved — HEAD-slice-proven ×7) · rider #2 (`pending_positive_tokens` per-token/per-line — DONE-WHEN lists only outstanding conditions; three-state harness evidence in the return) · rider #3 (the `:74` goal string topology-neutral, one line) · rider #4 (ENGINE_VERSION + once-per-process banner at the engine's own entry points — zero runner.py surface) · the §4.1 desk re-mint (command-api TRUE with citation · hue-online minted false · s31-entity minted · command-lifecycle re-pinned at c09c61c source, values HELD, casing caveat RESOLVED · usb.* placeholder untouched) · the §5 set (command-confirm gated [command-api, hue-online]; NEW command-confirm-s31 / command-timeout-absent / command-supersession / command-identify-honest — the latter two measure-then-pin with source-derived predictions; usb-reenumeration untouched) · the §7.3 AUTO suite of record documented in constants.
> **Census EXACTLY 14** (8 M + 6 ??): the instruction's enumerated 11 + `synthetic-liveness-missing-trigger-view.txt` (the anticipated sibling) + TWO [REVIEW] deltas — `M synthetic-liveness-empty-chain.api.yaml` (R1: fetch-first would kill the condition-(c) mutant-killer; +trigger 2099 preserves verdict, reason, AND teeth) and `M timeout-honesty-no-change.yaml` (R2: `capability` is a REQUIRED live-wire body field, IssueCommandEndpoint.java:263-277 — un-fixed, its suite leg 400s; command-confirm got the same fix in-census). **[REVIEW] R3:** command-confirm-s31 realized as deterministic turn_off-settle + turn_on-assert — the instructed let→opposite-command-NAME is inexpressible in v0 mechanics (other_of picks values not names; turn_on/turn_off parameterless; toggle has zero ExpectedOutcomes = unconfirmable). Nine [INFO]s incl. I1 (identify's honest terminal = ACKNOWLEDGED/result-unconfirmed — TIMED_OUT structurally impossible, ledger never tracks identify) · I2 (the recorded reason does NOT surface on the command status read — details={result} only; contract conversation if wanted) · I5 (PRE-EXISTING boot-demo fixture staleness: devices=2 era vs the 6/6@25065 re-mint — fails dry at HEAD before B2; hub-owned re-mint candidate). NO commit made; staging at the hub's order = the explicit 14 paths.
> **Next WU: B3 nightly (bench lane, hub-authored)** — gates on rider #1: SATISFIED by the desk evidence; the Pi half's first-consumer rep + suite green are the remaining preconditions. Core stays QUIET until the M14 WUs.

---

## WU-B2 — the §5.1 suite port + THE ENGINE REBIND (bench repo, DESK half) — DISPATCH (2026-07-28, v40 hub beat 1)

> **The instruction:** `context/instructions/2026-07-28_B2_suite-port-and-rebind_bench-instruction.md` — execute the DESK half (§2 the rebind FIRST · §3 riders · §4.1 the desk-stage re-mint · §5 the scenario set · §8 desk gates). The Pi half is Nick's (operator blocks in-instruction). **Launch preconditions: Nick's skills-mirror sync (Check 9) + the v40 beat-1 hivemind push.** Working tree baseline: bench **`9f2b5ad`** clean. This is a PYTHON/YAML repo — no gradle; the gate of record for the desk half = the runner-demo fixture suite (the §2.4 table exactly: reds quoted pre-rebind, the predicted verdicts ×2 post) + lint + the no-tty parse. Ordering law: **the rebind lands and demo-proves BEFORE any AUTO leg runs on the Pi; B3 gates on it.** Re-pin obligations at source (quote file:line in the return): the command wire paths at core `c09c61c` rest-api (`data.commandId` · `data.terminal` · `data.currentPhase` · the phase vocabulary) · the S31 command names + state field path. Census expectation: §8's enumerated set (4 M + 5 ?? + the fixture-sibling adds, enumerated exactly at commit time); lock-free porcelain = `git --no-optional-locks status --porcelain` (SPELL THE FLAG — a bare `git status` through the bridge strands `index.lock` at Nick's keyboard). Returns file to `context/audits/`. Welcome pushback: if the engine's structure makes any arm's stated shape wrong at implementation depth, flag it — the anti-false-PASS and never-vacuous arms are contract, the rest is yours.

---

## WU-DASH-SERVE — the dashboard serve path (package · mount · the ruled (A) auth exemption; core repo) — DELIVERED (2026-07-27)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (this entry + the completion report + the cross-agent note are the audit inputs). Deferred Build Gate: **NO** — the full `./gradlew check` RAN in-session, GREEN. Working tree: core **`2040a66`** (clean at dispatch, P0 exact) + EXACTLY 13 porcelain entries = the instruction's census (12 M + 1 new fixture `lifecycle/lifecycle/src/test/resources/dashboard/index.html`). Pre-verification: ALL pins P0–P15 verified against the working tree pre-code (P14 verified against the actual Javalin 6.7.0 jar via javap: `cfg.staticFiles.add(Consumer<StaticFileConfig>)` with public `hostedPath`/`directory`/`location` fields · `cfg.spaRoot.addFile(String,String,Location)` · `Location.CLASSPATH` · `ctx.method()` → `HandlerType`; zero mismatches, zero pushback needed).
> ```
> ./gradlew :api:rest-api:test :lifecycle:lifecycle:test :app:homesynapse-app:test --rerun
>                                        # BASELINE at HEAD pre-edit (forced-fresh, 68/68 tasks
>                                        #   executed): rest-api 104/0/0 · lifecycle 56/0/0 ·
>                                        #   app 19/0/0 (the SKIP-VIS "104" record matched — no drift)
> red-first (tests written FIRST)        # rest-api compileTestJava FAILED: 7 missing-symbol sites on
>                                        #   isPublicShellRequest = all 3 new unit legs red-by-compile
>                                        #   (helper absent — disclosed as such); lifecycle 14 tests:
>                                        #   rootRedirects RED (401 where 302 expected) ·
>                                        #   shellServes RED (401 where 200 expected) ·
>                                        #   writeAndDataRoutesStayGuarded GREEN-BY-CONSTRUCTION
>                                        #   (disclosed [INFO]: it is the preservation fixture — its
>                                        #   three asserts ARE the HEAD-preserved behaviors, incl. the
>                                        #   encoded traversal already 400 at HEAD, which also proved
>                                        #   the %2e%2e probe reaches isPathSafe on the wire)
> green ×2 (per-task --rerun, executed-state verified)
>                                        # rest-api 107/0/0 (6 @Test in RestFiltersAuthTest) ·
>                                        #   lifecycle 59/0/0 (14 @Test in HomeSynapseCoreTest) ·
>                                        #   app 19/0/0 — two fresh JVMs; Javalin's redirect observed
>                                        #   302 (the smoke pin's code), SPA fallback + HEAD 200 live
> mutation verification (in-session)     # M1 (+POST to the exemption): KILLED by the NAMED
>                                        #   dashboardWriteAndDataRoutesStayGuarded (POST leg 404≠401);
>                                        #   M2 (startsWith "/dash"): KILLED by the NAMED
>                                        #   isPublicShellRequest_rejectsEverythingElse
>                                        #   (the /dashboardevil row); M3 (exemption ABOVE isPathSafe):
>                                        #   KILLED by the NAMED EXACT-400 traversal row — the probe
>                                        #   SERVED under the mutant (200, expected 400): the order
>                                        #   contract is the security property. Restores cmp-verified
>                                        #   byte-identical ×3.
> ./gradlew check                        # FULL GATE GREEN — 157 actionable tasks, 91 executed
>                                        #   (run 1, the final tree). Node-free grep (steady state,
>                                        #   run 3 after clearing a git-invisible empty scaffold dir —
>                                        #   see [INFO]-6): EVERY :web-ui:dashboard: task
>                                        #   NO-SOURCE/UP-TO-DATE (compileJava NO-SOURCE ·
>                                        #   processResources NO-SOURCE · test NO-SOURCE · check
>                                        #   UP-TO-DATE = the module WAS in the graph); grep for
>                                        #   npmInstall|npmBuild|stageDashboard|jar → ZERO lines.
> ```
> **Per-DP:** DP-1 `:web-ui:dashboard` = bare-`java` resources jar, staging at `build/staged-dashboard`, npm attaches to the jar task ONLY, doLast self-assert on `dashboard/index.html`, legacy `src/main/resources/dashboard` swept by clean. DP-2 app `testRuntimeClasspath` `exclude(module = "dashboard")` (production `runtimeClasspath`/installDist untouched). DP-3 `staticFiles`+`spaRoot` CLASSPATH mount in the create lambda + `app.get("/", redirect /dashboard/)` after installAuth; install sequence otherwise untouched. DP-4 `isPublicShellRequest` classifier + the authorize() early-return BETWEEN isPathSafe and authenticate; installAuth Javadoc amended. DP-5 both install-smoke copies + setup-node@v4 '22' (byte-identical, diff-verified) + run-smoke.sh serve asserts (302 / 200, sections renumbered 6→7→8). **Deviations: ZERO [REVIEW]/[BLOCKING], seven [INFO]** (null-path guard the tests mandate; ZipFile import — Kotlin DSL shadows `java` as the extension accessor; clean sweeps the legacy staging dir; red-leg-6 green-by-construction; smoke section renumber; local removal of the git-invisible empty `src/main/resources/static` scaffold dir so the local gate matches fresh-checkout NO-SOURCE; the order-proof test also pins the hazard side — isPublicShellRequest(traversal)==true). **Next WU: B2 (bench lane — the hub authors it; core goes QUIET until the M14 WUs).** Commit staging at the hub's order: EXPLICIT paths, exactly the 13-file census, core-repo only.

---

## WU-DASH-SERVE — the dashboard serve path (package · mount · the ruled (A) auth exemption; core repo) — DISPATCH READY (2026-07-27, v39 hub beat 1)

> **Current task (the next Coder session executes this):** read `context/pre-verifications/WU-DASH-SERVE.md` (P0–P15 — verify every pin, STOP on mismatch), then execute `context/instructions/2026-07-27_DASH-SERVE_dashboard-serve-path_coding-instruction.md`. Baseline: core **`2040a66`** clean (the deployed build of record). **CC-lane grant:** `./gradlew :api:rest-api:compileJava :api:rest-api:test :lifecycle:lifecycle:compileJava :lifecycle:lifecycle:test :app:homesynapse-app:test` + **ONE full `check`** — plus the mandated **Node-free grep assert** on the check output (quoted in the completion report, presence AND absence lines). **NO `assemble`/`installDist`/`:web-ui:dashboard:jar` in-session** (npm is not in the grant; the packaging leg's gate of record is `install-smoke.yml` on the pushed commit). Tests FIRST (6 new: 3 unit + 3 e2e; red-first with recorded counts; M1–M3 mutation legs with named kills + cmp-proven restores). **Census EXACTLY 13** (12 M + 1 new test fixture) — anything else in porcelain is a STOP. The hub's two-layer audit precedes ANY commit; staging will be EXPLICIT paths. **Next WU after this one: B2 (bench lane — the hub authors it; core goes QUIET until the M14 WUs).**

---

## SKIP-VIS — Explanation Honesty (raw outcome carry · silent-skip verdict honesty · the triggeredAt fix; DP-4 GO; core repo) — DELIVERED (2026-07-26)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (this entry + the completion report are the audit inputs). The build gates that RAN in-session, ALL GREEN against the working tree at core `4bc1258` (clean at dispatch — P0 exact; this WU's Build Discipline granted the CC lane the targeted loop + ONE full check):
> ```
> ./gradlew :core:automation:test :api:rest-api:test --rerun-tasks
>                                        # BASELINE at HEAD pre-edit (re-derived forced-fresh, never
>                                        #   carried): automation 186/0/0 · rest-api 101/0/0 (the
>                                        #   CMD-API "101" record matched exactly — no drift)
>                                        # stage-A red: automation 203 tests EXACTLY 13 failed ·
>                                        #   rest-api 104 tests EXACTLY 5 failed = 18 legs red for
>                                        #   XML-verified behavioral reasons (superseded→FAILED [the
>                                        #   CORE-P1 defect itself] · commandless→NEVER_TRIGGERED [the
>                                        #   false clean-success] · triggeredAt/evaluatedAt off by
>                                        #   exactly durationMs · resultOutcome null at stub · settled
>                                        #   false where true expected · the new wire keys absent from
>                                        #   the containsOnlyKeys maps); 4 of the 20 new legs GREEN at
>                                        #   stage-A BY CONSTRUCTION (disclosed per-leg, the W2-LEARN
>                                        #   8th-leg class: both ingest-fallback legs compute the
>                                        #   identical pre/post value; both DP-2 boundary legs pin
>                                        #   unchanged behavior + a null marker) — their tooth is
>                                        #   regression
>                                        # stage-B green: automation 203/0/0 · rest-api 104/0/0
>                                        #   forced-fresh; re-proven in a SECOND fresh JVM
>                                        #   post-mutant-restores (the determinism proof)
> mutation verification (in-session)     # M1 isFailure any-non-acknowledged restore kills EXACTLY
>                                        #   4/203 (the NAMED superseded ×2 + honest-unconfirmed +
>                                        #   the five-modes tuple test); M2 unconditional-subtraction
>                                        #   restore in the shared derivedTriggerInstant (= BOTH DP-3
>                                        #   sites at once — both sites derive through the one helper)
>                                        #   kills EXACTLY 3/203 (the THREE NAMED:
>                                        #   listRuns_triggeredAtEqualsEventTime ·
>                                        #   triggeredAt_equalsMatchedAt_regression ·
>                                        #   evaluatedAt_equalsEventTime [site B's kill]); M3
>                                        #   marker-branch deletion (completedVerdict ignores the
>                                        #   payload counts = the pre-WU body) kills EXACTLY 1/203
>                                        #   (the NAMED commandless test — a zero-noise record);
>                                        #   restores cmp-verified byte-identical ×3
> ./gradlew :app:homesynapse-app:test :lifecycle:lifecycle:test --rerun-tasks
>                                        # RAN (defensive, forced fresh): app 19/0/0 (every arch rule
>                                        #   GREEN incl. NO_DIRECT_TIME_ACCESS + the scoped Rule 9) ·
>                                        #   lifecycle 56/0/0
> ./gradlew :core:automation:spotlessApply :api:rest-api:spotlessApply
>                                        # RAN — zero reformats (cmp-verified against the pre-apply file)
> ./gradlew check                        # RAN GREEN ×2 (156 tasks; the SECOND run is the gate of
>                                        #   record — against the FINAL tree, after the one
>                                        #   javadoc-provenance touch on RunSummary)
> CI on the pushed commit remains the gate of record (push on the hub's order, via Nick)
> ```
> Change: **exactly 12 porcelain entries, ALL M — exactly the instruction's P2-audited census, sweep-guard clean** — automation main ×4 (`StandardExplanationService` [DP-1: the 5-branch precedence law + the last-causation-matched `resultOutcome` fact-carry + the private `Outcome` record grown to 3 components with the Q1b `settled()` derivation method · DP-2: the payload-arithmetic commandless branch FIRST in `completedVerdict`, the frozen "issued no device commands" sentence, `Boolean.TRUE`-never-false, the clean-success text unreachable on that path · DP-3: BOTH sites re-derive through the new shared `derivedTriggerInstant(EventEnvelope, long)` — eventTime-present ⇒ the eventTime itself, NO arithmetic; null ⇒ `ingestTime − durationMs` clamped; the now-unused `terminalInstant` helper removed · class-javadoc derivation bullet restated to the v1.1.2 precedence] · `RunExplanation` [ActionView 6→8: +`resultOutcome` nullable String 7th (GAP-1 javadoc verbatim), +`settled` primitive boolean 8th (Q1b); compact-ctor null-checks unchanged] · `NonFiringExplanation` [8→9: +`noCommandsIssued` nullable Boolean 9th (CORE-P2 javadoc) + the 8-arg delegating convenience ctor — validation lives ONLY in the canonical ctor] · `RunSummary` [javadoc only: the mandated corrected parenthetical + one v1.1.2 provenance sentence]) + automation test ×2 (`StandardExplanationServiceTest` 13→25 [+7 DP-1 single-mode legs + `explainRun_fiveFailureModesDistinct` (row-exact + pairwise-distinct on `(outcome, resultOutcome, reason)`) + the DP-4 settled pair-in-one + 3 DP-3 legs; `projection_writesNothing` extended to read a DP-1 chain (instruction-sanctioned); new helpers `seedRunWithResult`/`seedCompletedWithDuration`/`seedCompletedNullEventTime` (the null-eventTime `EventDraft` leg, never faked by pre-subtraction)] · `NonFiringExplanationServiceTest` 11→16 [the DP-2 triple + the DP-3b pair; `seedCompleted` gained a counts/duration overload]) + rest-api main ×2 (`GetRunCausalChainEndpoint` [`actionsList` +`resultOutcome` +`settled` appended after `reason`, size hint 8; one-sentence v1.1.2 javadoc amendment] · `GetNonFiringEndpoint` [`toWire` +`noCommandsIssued` after `lastEvaluation`, size hint 9; javadoc likewise]) + rest-api test ×2 (`RunEndpointsTest` 8→10 [ONLY the `:201` actions pin + the `:278` 8-arg construction updated + `causalChain_resultOutcomeOnWire` (populated + null) + `causalChain_settledOnWire` (true + false); the `:79` runs-entry 6-key pin and the outcome-block pin UNTOUCHED and passing — SD-5's proof] · `AutomationEndpointsTest` 9→10 [the `:79–:80` pin gains `noCommandsIssued` + `nonFiring_noCommandsIssuedOnWire` (true + absent-null); the three 8-arg `NonFiringExplanation` constructions BYTE-UNTOUCHED via the convenience ctor]) + both MODULE_CONTEXTs (WUCP: the SKIP-VIS banner + the branch-split gotcha in automation; the v1.1.2 additive-key section in rest-api). **ZERO:** event mints · module-info edits BOTH modules (P13 held — every new component is `String`/`Boolean`/`boolean` = `java.base`) · build files · new public top-level types · emitter edits (`StandardRunManager`/`StandardActionExecutor`/event records/ledger untouched — SD-3) · `ListRunsEndpoint` edits (SD-5; VALUE corrected at the derivation, SHAPE frozen) · enum growth (`ActionOutcome` 5 / `NonFiringVerdict` 4 — SD-2) · new clock reads / locks / logging / EXEC-DETERMINISM touches (SD-6 held).

**Coder (Claude Code).** Built per `context/instructions/2026-07-26_SKIP-VIS_explanation-honesty_coding-instruction.md` + pre-verification `WU-SKIP-VIS.md` (P0–P14 ALL re-verified at source at `4bc1258` clean; STOP gates G-1..G-10 ALL PASS pre-code — every line anchor exact, incl. the 724-line count, the `:466`/`:228–:229` subtraction sites, the G-3 three-construction-site census re-proven by repo-wide grep, and the G-10 §3.9 silent-skip emission truth). Duplicate-dispatch check FIRST: newest handoff entry was CMD-API, committed AS `5b4797e` under HEAD `4bc1258` — SKIP-VIS a fresh dispatch (its own CMD-API next-WU pointer named SKIP-VIS as separate). **DP-4 executed GO** (the dispatch turn's ruling). **The two mandated findings:** (1) `InMemoryEventStore` stamps `ingestTime = clock.instant()` at append (`InMemoryEventStore.java:345`) — under the suite's `FIXED_CLOCK` every ingestTime is `FIXED_INSTANT`, making both ingest-fallback pins exact (`FIXED_INSTANT − 34.204s`); (2) `readByCorrelation` returns stable insertion/log order (forward iteration over the append list), so DP-1's last-wins premise HOLDS at source — the instruction's anticipated order-instability STOP-flag does not fire. In-session 4-lens adversarial review fleet (instruction-compliance / correctness / wire-contract / constraints, findings adversarially verified): ZERO code defects; 2 raw findings — 1 refuted (the RunSummary provenance nit; closed spec-plus anyway, see I6), 1 confirmed-INFO (the fleet's own dispatch census said "10 java files" while the two MODULE_CONTEXT writes landed mid-review; the settled tree is exactly the instruction's 12 — a process note, not a defect).

### Deviations (severity-honest — ZERO [REVIEW]/[BLOCKING], seven [INFO])
- **[INFO] I1** — 4 of the 20 new legs are GREEN at stage-A by construction, disclosed per-leg (the W2-LEARN 8th-leg class): `listRuns_triggeredAt_ingestFallback` + `evaluatedAt_ingestFallback` (the fallback arm computes the IDENTICAL value pre/post fix — old code subtracted from ingestTime when eventTime was null too), and `completedWithConfirmedCommands_cleanPathUnchanged` + `completedWithUnconfirmed_unchanged` (DP-2 boundary legs pinning unchanged behavior + the null marker). Their tooth is regression (the fallback pair guards the branch-split gotcha; the boundary pair kills a DP-2 over-fire).
- **[INFO] I2** — M2 realized as ONE mutant edit: both DP-3 sites derive through the shared `derivedTriggerInstant` helper (sharing the instruction itself anticipated), so restoring the unconditional subtraction in the helper restores it at BOTH sites simultaneously; the three named kills span both sites' consumers (2 SET legs + the NFT leg).
- **[INFO] I3** — M3 realized as deletion of the whole commandless marker branch (the pre-WU body): the faithful "completedVerdict ignores commandCount" mutant whose single kill is exactly the named commandless test. (The alternative — dropping only the `commandCount == 0` clause — fires the marker for EVERY completed run and kills the boundary legs instead; the instruction's arrow names the commandless test, so branch-deletion is the specified shape.)
- **[INFO] I4** — an acknowledged-only `DISPATCHED` derives `settled=false` (provisional): the Q1b formula's letter — `resultOutcome` `"acknowledged"` is a non-settling record (an ack is not a settlement; confirm/timeout can still arrive). The DP-4 prose "each other mode ⇒ true" enumerates the five-modes table, which the acked variant is not a row of; the formula governs and the settled test pins acked=false alongside bare=false and the four settled shapes.
- **[INFO] I5** — the convenience-ctor option taken for `NonFiringExplanation` (the instruction's offered choice): the three `AutomationEndpointsTest` 8-arg construction sites and all seven pre-existing production sites compile byte-untouched; only the DP-2 marker branch uses the canonical 9-arg form.
- **[INFO] I6** — `RunSummary`'s record javadoc gains ONE spec-plus v1.1.2 provenance sentence beyond the mandated verbatim parenthetical (the instruction's Code Quality Standards line asks provenance on every changed public record; the review fleet raised-then-refuted the gap — closed affirmatively; javadoc-only, wire-invisible).
- **[INFO] I7** — the new `seedRunWithResult` test chains omit the `automation_condition_evaluated` row (irrelevant to action-outcome derivation; keeps the DP-1 fixtures minimal); `projection_writesNothing` extended with a DP-1 (superseded) chain read per the instruction's "if trivial" sanction.

### NEXT WU (refuse-to-close pointer)
- **The hub's two-layer audit FIRST — no commit until its order** (this entry + the completion report are the audit inputs; zero [REVIEW]s — the seven [INFO]s and the SD-7 stated residue [`expired_on_restart`/`invalid` stay failure-class; one line from Nick re-classes either later, `resultOutcome` already carries both distinctly] ride it). The read-API freeze doc's v1.1.2 amendment note is HUB-OWNED at WUCP Phase 2 (SD-1 — not written from this lane).
- **Then B2 (the §51 bench port, bench lane) per the hub's sequencing** — unchanged from the CMD-API pointer (the Aug-14 keystone; its `constants.yaml` re-pins the CMD-API-frozen tokens; flip `capabilities.command-api.available` with B2). **DP-3's landing removes the bench `new_run_after` instrument's duration-wide false-FAIL dead zone (Rosonway §4); the engine's own matchedAt rebind stays B2 rider #1** (separate WU, per the instruction's Dependencies note).
- Carried candidates unchanged from the CMD-API entry (the 04P persist-verified two-window silicon leg · sidecar snapshot-init WU · real-store `setAll` teeth · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · LC-LABEL-LOG · **EXEC-DETERMINISM re-homed to the first post-gate automation/executor-touching WU, candidate pairing CMD-API-ACTOR (SD-6 — deliberately NOT touched here)**) + noted-not-built (SD-3): an emitter-side per-target skip event (first-class chain skip visibility with reasons) remains a separate, unruled candidate for the hub.

---

## CMD-API — The Command Write Surface (POST issue + GET status; core repo) — DELIVERED (2026-07-22)

> **DEFERRED GATE (hub, then Nick):** the hub's two-layer audit precedes ANY commit order (Success Criterion 6; ONE [REVIEW] rides it). The build gates that RAN in-session, ALL GREEN against the working tree at core `355a711` (clean at dispatch; this WU's Build Discipline granted the CC lane the targeted loop + ONE full check):
> ```
> ./gradlew :api:rest-api:test :lifecycle:lifecycle:test --rerun-tasks
>                                        # BASELINE at HEAD pre-edit (G-1 RE-DERIVED forced-fresh,
>                                        #   never carried): rest-api 64/0/0 · lifecycle 56/0/0
>                                        # stage-A red: 101 tests, EXACTLY 37 failed = ALL 37 new
>                                        #   legs, every one for a behavioral reason (inert-handler
>                                        #   status/body nulls · zero-publish counts · stub cache
>                                        #   MISS-always / empty-fingerprint); 64 pre-existing green
>                                        # stage-B green: 101/0/0 forced-fresh ×2 (TWO fresh JVMs —
>                                        #   the determinism proof, see I5) · lifecycle 56/0/0 ·
>                                        #   app 19/0/0 (defensive, forced-fresh — the adapted Rule 9
>                                        #   + every other arch rule GREEN)
> mutation verification (in-session)     # M1 terminal-guard deletion kills EXACTLY 2/101 (the NAMED
>                                        #   superseded-disposition leg + rejected-terminal); M2
>                                        #   CONFIRMED-from-acknowledged kills EXACTLY 3/101 (the
>                                        #   NAMED never-false-CONFIRMED lock + acknowledged-non-
>                                        #   terminal + the full-chain key pin); M3 fingerprint-
>                                        #   comparison deletion RE-RUN on the FINAL tree kills
>                                        #   EXACTLY 2/101 (the NAMED 409 endpoint leg + the cache
>                                        #   CONFLICT leg); restores cmp-verified byte-identical ×4
>                                        #   (M1, M2, M3 first run, M3 re-run)
> ./gradlew check                        # RAN, GREEN (156 tasks)
> ./gradlew :api:rest-api:spotlessApply :lifecycle:lifecycle:spotlessApply :app:homesynapse-app:spotlessApply
>                                        # RAN — zero reformats
> CI on the pushed commit remains the gate of record (push on the hub's order, via Nick)
> ```
> Change: **exactly 17 porcelain entries** — 7 M (rest-api `RestFilters` [the pinned 7-arg `installCommandEndpoints` gateway — THE ONLY PUBLIC DELTA, grep-verified] · `EndpointContext`/`JavalinEndpointContext` [the seam grows `body()` + `requestHeader()` for the first body-consuming handler — the documented Responder→EndpointContext growth pattern, package-private, I1] · test `RecordingEndpointContext` [`withBody`/`withRequestHeader`] · lifecycle `HomeSynapseCore` [ONE install call after `installAutomationQueryEndpoints`, passing the composition-root `eventPublisher`/`entityRegistry`/`persistenceFactory.eventStore()` + `(int) PendingCommandLedgerAssembly.DEFAULT_CONFIRMATION_TIMEOUT_MS` — the SAME value the executor receives] · app `HomeSynapseArchRules` [THE [REVIEW]: Rule 9 scoped with two named exemptions] · rest-api `MODULE_CONTEXT.md` [WUCP]) + 10 new (3 main package-private: `IssueCommandEndpoint`, `GetCommandStatusEndpoint`, `IdempotencyCache`; 7 test: the 3 mandated test classes + fakes `RecordingEventPublisher`/`FakeEntityRegistry`/`FakeEventStore` + `MutableTestClock`). **ZERO:** event mints (73/43/55 hold) · module-info edits BOTH modules (P13/P14 held — `event`/`device` readability rides automation's transitive closure exactly as pinned) · build-file edits · config-schema edits · new public types · dispatch/ledger/confirmation edits · auth/token edits (DP-8; `before(*)` covers the new routes).

**Coder (Claude Code).** Built per `context/instructions/2026-07-22_CMD-API_command-write-surface_coding-instruction.md` + pre-verification `WU-CMD-API.md` (P0–P16 ALL re-verified at source at `355a711` clean; STOP gates G-1..G-6 ALL PASS pre-code — G-2's zero-`app.post` re-confirmed repo-wide; the 26-char route census now 9 GET + 1 POST + 1 new GET). Duplicate-dispatch check FIRST: newest handoff entry was LEARN-PERSIST, committed AS HEAD `355a711` — CMD-API a fresh dispatch. Red-first staged honest (stage-A = tests + inert stubs; 37/37 right-reason reds, XML-verified per-leg). **DP-2 realized with the value split the Phase-2 records demand:** 202 `data.viewPosition` = the PERSISTED log position (`envelope.globalPosition()` — the record javadoc's "position at which the command was persisted", what `IdempotencyEntry` stores and replays stably) while `meta.viewPosition` = the projection cursor (the live idiom); the replay test pins the distinction (I4). **DP-5's linkage mirrors the M7.5a ExplanationService:** dispatched/result by causation, confirmed/timed-out by payload `commandEventId` (the ledger's causation is the REPORT event — verified at `StandardPendingCommandLedger:886-:902`), plus a foreign-confirmation guard leg (a `state_confirmed` naming a DIFFERENT command never renders CONFIRMED — never-false-CONFIRMED hardened beyond the mandated lock, I6). The ledger's disposition publishes were source-verified to thread the expired command's OWN correlation, so a superseded API command's chain renders ACKNOWLEDGED-terminal exactly as DP-5 specifies.

### Deviations (severity-honest — ONE [REVIEW], eleven [INFO])
- **[REVIEW] R1 — `app/homesynapse-app/.../HomeSynapseArchRules.java` modified (outside the Files table): Rule 9 `REST_ENDPOINTS_NO_EVENT_PUBLISHING` scoped from package-wide to all-but-two.** At `355a711` the rule forbade ALL of `com.homesynapse.api.rest..` from accessing `EventPublisher` (`:294`) — the pinned gateway signature ("EventPublisher, cast internally") + DP-3 (the endpoint publishes) + Success Criterion 2 (full check GREEN) are jointly unsatisfiable with it. The rule's own M3.6e.2 comment block anticipated exactly this surface ("Write operations have their own surface (command issuance, M5+) and route through the proper command-validator pipeline"); the adaptation names `IssueCommandEndpoint` + `RestFilters` as the ONLY exemptions, keeps every query/status handler pinned, and makes a future writer need a deliberate edit (the apply-caller-census pattern). Test classes are outside the scan (main-classpath import — verified via app build.gradle). Ratification requested; independently revertible with the WU.
- **[INFO] I1** — `EndpointContext` + `JavalinEndpointContext` + `RecordingEndpointContext` widened (3 files outside the Files table): `body()`/`requestHeader(String)` — no POST handler can exist without request-body access; the M3.6e.1→e.2 seam-growth precedent; package-private, zero exported surface.
- **[INFO] I2** — envelope `actorRef` = null: Doc 09's "actor = API identity" is unrealizable at `355a711` (`ApiKeyIdentity.keyId` is a String, no Ulid-typed actor for tokens; `EndpointContext` deliberately carries no identity per DP-8) — recorded WITH the DP-6 V1-subset class in MODULE_CONTEXT; the instruction's own DP-3 field list names origin only.
- **[INFO] I3** — `EventDraft.idempotencyKey` = null: the AMD-35 store-uniqueness channel deliberately unused — store-enforced per-home uniqueness would contradict DP-4's documented lost-on-restart semantics (a post-restart same-key retry must issue a NEW command, not bounce off the unique index).
- **[INFO] I4** — the DP-2 "viewPosition duplication (data + meta)" realized as KEY duplication with distinct values (persisted position vs projection cursor) per the Phase-2 record javadocs + the INV-TO-03 staleness contract; test-pinned both ways (replayed `data.viewPosition` stays the original persisted position while `meta` moves).
- **[INFO] I5 — a REAL latent defect caught and fixed in-session:** payload `parameters` bytes were per-JVM nondeterministic (`Map.copyOf` iteration order is SALTED per JVM; the M3 mutant run's fresh JVM flipped the salt and failed the exact-string pin). Fixed with `ORDER_MAP_ENTRIES_BY_KEYS` (key-sorted at every depth) + the test now sends REVERSED client key order and pins sorted bytes. **Observation for the PM (not changed — out of scope): the executor's live path has the same latent class** — `StandardActionExecutor` serializes `CommandAction.parameters()` (a `Map.copyOf` map) via `persistenceFactory.commandParameterSerializer()`; if that mapper doesn't sort, `command_issued` bytes from automations differ across restarts for identical actions.
- **[INFO] I6** — phase-event linkage filters + the foreign-confirmation guard leg (spec-plus never-false-CONFIRMED hardening; the M7.5a linkage precedent).
- **[INFO] I7** — `GetCommandStatusEndpoint` takes `EntityRegistry` for best-effort `capability` derivation (the frozen response shape demands the field; the payload carries only `commandType`); JSON null when unresolvable, test-pinned.
- **[INFO] I8** — `parameters` is a REQUIRED body field (P1: all three `CommandRequest` components non-null; `{}` for parameterless) — missing/non-object → 400 FieldError, test-pinned.
- **[INFO] I9** — DP-6's parenthetical "(the live entity-endpoint precedent)" is inaccurate about the bad-ULID half (the live entity GETs return 400 `invalid-parameters` for malformed ULIDs); the PINNED CMD-API behavior (404 for bad-ULID AND absent) is implemented and test-pinned as instructed — cite nuance only, non-STOP.
- **[INFO] I10** — `MutableTestClock` added test-local (rest-api's test tree has no `testing/test-support` dependency; mirrors `MutableClock`'s `advance()` surface at minimal size; NO_DIRECT_TIME_ACCESS-clean).
- **[INFO] I11** — M1/M2 kill records predate the I5 fix; their target file (`GetCommandStatusEndpoint`) is byte-identical across it, so the records stand; M3 was RE-RUN on the final tree for a zero-noise record.
- **P2-survey results (reported per the instruction):** `RestFilters` shape/method-count tests: NONE exist (grep-verified). Lifecycle `command_issued` count pins: all trigger-driven awaits (`HeroLoopHardwareFreeIT`/`RunPipelineWiringTest`/`IntegrationSpineWiringTest`/`RestartHonestyIT`) — a dormant new route cannot affect them; lifecycle 56/0/0 forced-fresh corroborates. Arch-rule count pins: none. Observed emitter priority: NORMAL (reported per DP-3).

### NEXT WU (refuse-to-close pointer)
- **The hub's two-layer audit FIRST — no commit until its order** (this entry + the completion report are the audit inputs; the R1 [REVIEW] rides it).
- **Then the keystone unblocks (v36 charge 1, the Aug-14 gate):** B2 (the §51 bench port, bench lane) — its `constants.yaml` PROVISIONAL blocks (P16) re-pin the DP-2 tokens THIS WU froze (`data.terminal` / `data.currentPhase` / UPPERCASE phases now live truth); flip `capabilities.command-api.available: false → true` with B2. C2/C3/H2 producers + B3 + M14 stack behind it. SKIP-VIS stays a separate WU.
- Carried candidates unchanged from the LEARN-PERSIST entry (the 04P persist-verified two-window silicon leg [rides that landing — now core `355a711` — + Pi deploy] · sidecar snapshot-init WU · real-store `setAll` teeth · FRAME-CTR custody schema · 0x81/0x82 ruling · IAS dedup posture · StandardExplanationService flattening · LC-LABEL-LOG) **+ new candidate: the executor-path parameters-byte-determinism observation (I5's second half — PM ruling on sorting `commandParameterSerializer`)**.

---
