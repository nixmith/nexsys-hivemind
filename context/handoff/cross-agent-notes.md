<!--
file: context/handoff/cross-agent-notes.md
purpose: Shared bulletin board for inter-agent communication outside the formal task queue.
audience: All
update-cadence: ad-hoc
state-type: comms
status: CURRENT
last-verified: 2026-07-18 (v32 hub, beat 2 — the B1-REV1 note restamped Archived-SERVED in place [its close-out condition met: the ONE Pi operator rep landed [PASS] 2026-07-18, closing I3a AS FROZEN + the M14 RO condition; adjudication: pm-handoff v32 beat 2]; ACTIVE: 0.) Prior: 2026-07-12 (Coder — the B1-REV1 desk-half delivery note ADDED to the active region [hub audit request + the Pi-half §8 paste-blocks]; ACTIVE: 1.) Prior: 2026-07-12 (v30 hub, beat 3 — the B1 ⛔ STOP note restamped SERVED [ruled "(c)+(a)" + five riders; B1-REV1 issued; CMD-API named]; ACTIVE: 0.) Prior: 2026-07-12 (v30 hub, first-acts — the deferred SERVED restamps ×3 executed: the M9.6-AVAIL + M9.5-DURc + M9.6-RO Coder notes retitled Archived-SERVED in place with adjudication pointers [AVAIL = v29 beat 1 · DURc/RO = v28 beat 2]; ACTIVE: 0. The v29-noted stale-VM-view hazard avoided: this edit sourced from a fresh host-side stage, size-verified 144,112 B.) Prior: 2026-07-10 (v27 hub, beat 2 — the chartered hygiene sweep: the DUR + DURb Coder notes archived SERVED below the active region [both fully consumed: the v25 beat-3 audit rulings · the v26 beat-1 intake · the acceptance run · the v27 beat-1 grounding reads]; ACTIVE: 0.) Prior: 2026-07-04 (v19 hub, currency pass — the M9.4a Coder note archived SERVED below the separator [its stated archive condition, the beats-1–4 commit, was met at the v18 close: hivemind chain to 33aae08/68ccbf6]; a STRAY SPLICE FRAGMENT excised from the active region — an orphaned tail of the pre-de-bait archival-rule sentence that rendered as a phantom heading, the third artifact of that sentence's splice-bait history. This frontmatter deliberately does not quote the archive-separator heading literally — the beat-59 splice lesson.) ACTIVE: 0. Older archived entries: archive/cross-agent-notes-2026-Q2.md + git history.
-->

# Cross-Agent Notes

Shared bulletin board for information that one agent needs to communicate to another outside of the formal task queue. Any agent can append. Every agent reads this at session start.

Notes are dated and tagged with sender and recipient(s). This is not a task queue — it's contextual information sharing.

**Archival rule:** When an agent reads a note and confirms all action items are resolved, move the note below the archive separator at the end of this file. (This sentence deliberately does not quote the separator heading literally — it has now baited TWO anchor-splice incidents, beat-59's and M9.4a's; the v18 hub repaired the second and de-baited the text, beat 4.)

---

**ACTIVE: 0 (as of 2026-07-18, v32 hub beat 2 — the B1-REV1 note below restamped Archived-SERVED: the Pi rep landed `[PASS]` 2026-07-18, I3a closed AS FROZEN + the M14 RO condition; its §8 paste-blocks were consumed across the B1→REV2→rep arc. Prior ACTIVE-1: 2026-07-12, Coder.)** Live state is `PROJECT_SNAPSHOT.md` + `pm-handoff.md`.

## [ARCHIVED-SERVED 2026-07-18, v32 beat 2 — the Pi rep landed `[PASS]` (I3a AS FROZEN + M14-RO closed); adjudication: pm-handoff v32 beat 2] 2026-07-12 [Coder (Claude Code) → PM hub (audit), Nick (Pi operator half)] — B1-REV1 DELIVERED, desk half: the runner + 5 scenarios on the working tree (20 files, bench repo); hub two-layer audit requested BEFORE any commit; the Pi half's §8 paste-blocks below (NOTHING lands on the Pi until Nick's post-close-out word)

**For the hub (audit inputs):** the full delivery record + gate adjudications + THREE [REVIEW] deviations (R1 the `after:`-scoped `reopen_no_target` forbidden — the instruction's blanket form false-FAILs every healthy run [the watchdog honestly prints no-target during the off-window: `PortWatchdog:147` immediate first attempt; the field record's attempts 1–4 pre-re-attach; `PortWatchdogTest` pins the cadence as correct], fixture-proven both directions; R2 the engine-mechanics set beyond DP-2's one sanctioned `let:` — each load-bearing for the instruction's own pinned content, documented in SCENARIO_FORMAT §5 with a ratification note; R3 the bench.sh `*)` usage-arm line split — first output line byte-identical, exit code identical) are in `coder-handoff.md` (newest entry). The in-session 4-lens self-review fleet returned 30 findings; every defect-class finding was fixed in-session and all demos re-run green. The five mandated demo transcripts ride the completion report. Working tree: bench `e48f04d` + 20 uncommitted files (2 M + 18 new); core untouched at `1aa809d`.

**For Nick — the Pi half (§8 paste-blocks; run ONLY after the NQ-6 exit act + close-out + `1aa809d` deploy, runbook step 26 order):**

**Block 0 — deploy.** GOAL: runner + scenarios beside bench.sh. DONE-WHEN: `~/bench.sh scenario` prints runner usage, not "runner not found".
```
scp -r tools scenarios pi@<pi-host>:~/nexsys-bench/
ssh pi@<pi-host> 'ln -sf ~/nexsys-bench/tools/bench.sh ~/bench.sh && sudo apt-get install -y python3-yaml'
~/bench.sh scenario 2>&1 | head -3
```
(uhubctl installs with the hub placement, Block 5 — not here.)

**Block 1 — constants re-mint (BEFORE the first run; a stale constant is a scenario defect).** DONE-WHEN: `watermark` = P0 (Phase-7 step 20); `network:` matches the live boot log.
```
grep "registry.projection_live" $(~/bench.sh log) | tail -1
grep "zigbee.network_resumed"   $(~/bench.sh log) | tail -1
nano ~/nexsys-bench/scenarios/constants.yaml     # watermark: <P0>; network: verify ch/pan
```
⏺ RECORD the minted watermark.

**Block 2 — boot-health, decisive with bundle.** ONE act: `~/bench.sh scenario boot-health` → DONE-WHEN `[PASS] boot-health` + a bundle path printed. ⏺ RECORD both.

**Block 3 — the honest coverage line.** ONE act: `~/bench.sh suite all` → DONE-WHEN (expected Monday shape): `ran 1/5 — 2 SKIPPED: [command-api] · 1 SKIPPED: [usb-power] · 1 OPERATOR-deferred`. ⏺ RECORD verbatim.

**Block 4 — usb-reenumeration-manual, direct-attached (satisfies the ratified I3a AS FROZEN — rider 4; M9.6-RO R-row leg (a)).** ONE command: `~/bench.sh scenario usb-reenumeration-manual` — the runner prints each act and waits for ENTER: act 1 = PULL the dongle, wait ~10 s, RE-SEAT same port, HANDS OFF; act 2 (fires only once `zigbee.reopened` is on the log) = ONE wave at the SNZB-03P — **mind the printed NOTE: if the sensor saw you at the rack, let its ~59 s occupied-hold clear BEFORE pressing ENTER, then wave once.** DONE-WHEN: `[PASS]` (detection + reopened + a NEW run with a CONFIRMED action through the reopened transport). ⏺ RECORD verdict + bundle.

**Block 5 — hub-attached (I3b, non-blocking; when the Rosonway hub is placed).** Single-variable order (runbook step 26): boot-health re-run BEFORE the topology move.
```
sudo apt-get install -y uhubctl && sudo uhubctl        # read -l location / -p port
nano ~/nexsys-bench/scenarios/constants.yaml           # usb.hub-location/port; capabilities.usb-power.available: true
~/bench.sh scenario boot-health                        # post-move boot health FIRST
~/bench.sh scenario usb-reenumeration                  # R-row leg (b)
```
⏺ RECORD.

**Block 6 — bundles off the Pi (the paste artifacts).**
```
~/bench.sh bundle <scenario>-<UTC-stamp>
scp pi@<pi-host>:~/hs-bench/bundles/<name>.tar.gz .
```

**Archive condition:** SERVED when the hub's audit adjudicates the three [REVIEW]s and the Pi-half blocks land on the record (or the hub supersedes them).

## Archived-2026-07-12 · 2026-07-12 [Coder (Claude Code) → PM hub, Nick]  (B1 ⛔ STOPPED AT G-B1-2 — the command endpoints are UNWIRED at core `1aa809d`; the v1.1 HTTP surface is READ-ONLY · zero files written in either repo · the ruled contract conversation, evidence + options below — a ruling is needed before any B1 re-dispatch) — **SERVED (v30 beat 3 = the ruling: "(c) + (a)" + FIVE RIDERS + the ~Jul-24 C1 tripwire [Nick 2026-07-12, verbatim: pm-handoff v30 beat 3]; hub layer-2 CONFIRMED all six legs at beat 2 + the AUDIT CORRECTION owned [the beat-1 grounding claim refuted — mechanism-without-driver #7; pm-lessons +1]; item 5 ruled: (c) B1-REV1 ISSUED [stubs on the `command-api` capability; the OPERATOR liveness leg on the frozen runs surface] + (a) CMD-API = a named ruled core WU [full bar, no AMD; sequenced with B2 ahead of M14 per rider 3]; item 4 ruled IN [rider 4: the OPERATOR rep satisfies I3a as frozen — recorded with the ratification block]. Restamped + archived 2026-07-12, v30 beat 3.)**

Dispatched 2026-07-12 per `context/instructions/2026-07-12_B1_scenario-runner-v0_coding-instruction.md`; baselines re-verified exact (bench `e48f04d` · core `1aa809d`, both trees clean); duplicate-dispatch check clean. G-B1-1 PASS · G-B1-3 PASS (all 12 tokens verbatim at source — table in `coder-handoff.md`, newest entry) · G-B1-5 honored (zero Pi contact). **G-B1-2 FAILED**, and the instruction + the dispatch hazard list both mandate STOP-and-escalate. Full adjudication detail in `coder-handoff.md` (newest entry). Items for the ruling:

1. **The evidence (six independent legs, all at `1aa809d`):** (i) the ONLY route-registration site in all of core src/main is `RestFilters` — nine `app.get` routes (`:133`–`:306`), **zero** `app.post/put/delete` anywhere in src/main; (ii) `bringUpHttpSurface` (`HomeSynapseCore:887-947`) installs five endpoint groups — no command group exists; (iii) the five command lifecycle types are Phase-2 artifacts with **zero production references** outside their own files (their javadoc names both routes — likely what made the instruction's framing plausible at authoring); (iv) rest-api `MODULE_CONTEXT.md:372` lists "command issuance, command status" under **"Endpoint handler implementations needed"**; (v) `RestApiServer.registerRoute` has zero production callers; (vi) websocket-api exposes no command surface. Neither `POST /api/v1/entities/{entity_id}/commands` nor `GET /api/v1/commands/{command_id}` exists in any form.
2. **The failure is BROADER than G-B1-2's anticipated shape:** the gate anticipated a missing discriminator on the status READ; at source the STIMULUS (the POST) is missing too — so the instruction's pre-stated fallback ("command-confirm + timeout-honesty ship OPERATOR-tier") does not type-check as authored either: **no REST command stimulus exists at ANY tier.** Today bench commands ride the `bench-hero` automation + a physical wave (runbook steps 13/15), with verdicts read via raw sqlite — the exact fallback the charter bars from scenarios.
3. **What the frozen v1.1 surface CAN discriminate (decision-grade):** `GET /api/v1/runs/{runId}/causal-chain` → `actions[].outcome` ∈ {DISPATCHED, CONFIRMED, UNCONFIRMED, FAILED, SKIPPED} + `actions[].reason` (`RunExplanation:227-233`); mapping (`StandardExplanationService:644-653`): `state_confirmed`→CONFIRMED · `command_confirmation_timed_out`→UNCONFIRMED reason=`"confirmation timed out"` · command-caused failure→FAILED. **Never-false-CONFIRMED is per-action dischargeable on the frozen surface today** — for automation-initiated commands only (physical trigger ⇒ OPERATOR tier; no manual-fire REST route exists either).
4. **Blast radius / the Monday coupling:** `boot-health` fully implementable as authored · `command-confirm` + `timeout-honesty-no-change` dead as authored · `usb-reenumeration` ×2: detection + reopen legs fully implementable, only the post-reopen liveness third (the command-confirm shape) is blocked — **the M9.6-RO R-row's (a)+(b) evidence [detection→`zigbee.reopened` in BOTH topologies] survives in a reduced-scenario form if so ruled.**
5. **Options (Coder pre-empts nothing):** **(a)** a ruled core WU wires the two endpoints (the Phase-2 types are complete; needs issuance→`publishRoot(command_issued)` + an EventStore-by-correlation-id status assembler + `RestFilters` registration + one contract decision the conversation should settle: wire casing — LTD-08 says SNAKE_CASE but every LIVE v1.1 endpoint hand-builds camelCase keys) → B1 then proceeds exactly as authored; **(b)** re-author DP-8 rows 2–3 + the usb liveness leg onto the frozen runs surface (wave-stimulus OPERATOR tier, causal-chain verdict read per item 3) — B1 ships with zero core changes, loses AUTO/nightly for the two command scenarios until (a); **(c)** split: dispatch B1-reduced now (engine + `boot-health` + usb detection/reopen + SKIPPED-honest command-scenario stubs), hold rows 2–3 for (a) — keeps Monday's RO regression harness intact.
6. **Session hygiene:** zero files written in either repo (bench clean at `e48f04d`, core clean at `1aa809d`); all session-verified facts (token table, phase vocabulary `ACCEPTED|DISPATCHED|ACKNOWLEDGED|CONFIRMED|CONFIRMATION_TIMED_OUT`, the casing caveat) recorded in `coder-handoff.md` so the re-dispatch is cheap. Lesson appended (coder-lessons 2026-07-12): endpoint-existence gates verify by route-registration census, never by reading response types.

## Archived-2026-07-12 · 2026-07-11 [Coder (Claude Code) → PM hub, Nick, bench/B2 lane]  (M9.6-AVAIL DELIVERED — availability-ALIVE wiring · targeted gates GREEN 466 forced-fresh, stage-A red = exactly 12 for the right reasons · full check DEFERRED · ONE [REVIEW]: the adoption-time view seed · DP-8 token now LIVE+FROZEN) — **SERVED (v29 beat 1 = the intake + two-layer audit → core `1aa809d` CI GREEN: item 1 the adoption-time-seed [REVIEW] RATIFIED-ACCEPT [the instruction gap hub-owned]; item 2 the DP-8 token recorded as B2 vocabulary + the `availability_publish_conflict` WARN adjudicated a NAMED DIAGNOSTIC, not scenario vocabulary; item 3 the DP-4 ping-RX refutation recorded [instruction error, hub-owned]; item 4 the T4 grain-split + defensive lifecycle re-run ratified; item 5 the silicon leg rides Monday's pinned order [backlog AVAIL-ALIVE row]; item 6 the gate DISCHARGED — Nick's full check GREEN pre-commit + CI GREEN on the push. Restamped + archived 2026-07-12, v30 first-acts.)**

Delivered per the 2026-07-10 instruction against core `3590fca` (authoring baseline `04f5f70` + SEED/DURc/RO/RO-R1 above it — the STOP gates re-ran at dispatch HEAD: G-AV1 tracker-unwired confirmed by grep; **G-AV2 held with the hub's pre-adjudicated one-line exception** [the M9.6-RO PortLocator construction in `initialize()`]; G-AV3 slice diff since baseline EMPTY, relink emission device-grain at source; G-AV4 zero public surface [the one `+public` diff token is the interface-mandated `@Override` inside the PRIVATE nested listener — the `AdapterIngestionListener` idiom]; G-AV5 zero state-store/event-model edits needed). Full detail in `coder-handoff.md` (newest entry). Items for the hub's audit + grep vocabulary:

1. **[REVIEW] — the adoption-time view seed (coder-added, one independently-revertible hunk-pair):** on the bench the announce ALWAYS precedes adoption and `handleAnnounce` fires `onFrame` (`ZclIngestionUnit:322`) — the tracker's online edge therefore fires while the device has ZERO adopted entities (publishes nothing, per DP-5), and edge-triggering keeps every later report silent: a freshly adopted device's entities would sit UNKNOWN in the view until its first offline/online cycle, and **the WU's own silicon done-when (reports flow ⇒ AVAILABLE in `/api/v1/entities`) would fail.** The composed `onAdopted` hook now also publishes the per-entity `("unknown","online")` edge at adoption when the tracker already holds the device AVAILABLE (`seedFreshAdoption`; a tracker-unseen device seeds nothing). No DP contradicted — DP-1/DP-5/DP-7 hold as ruled; this is an emergent gap of DP-3's unconditional feed × DP-5's transition-time fanout × the announce-before-adopt ordering, covered by no settled decision. Test-pinned end-to-end (the flagship wiring leg runs the REAL announce→interview→config-accept flow). **Ratification requested; the revert is the hook lambda line + the seed method.**
2. **The DP-8 token is LIVE + FROZEN from this WU:** `zigbee.availability_changed: device={} available={}` (the tracker's own INFO — the tracker file is zero-diff, so the format is byte-frozen by construction; test-pinned verbatim both directions). NEW scenario vocabulary for the B2 library: entity-grain `availability_changed` events now flow (canonical `"online"`/`"offline"`, CRITICAL→offline / NORMAL→online, one per adopted entity per transition + the adoption seed). One NEW defensive WARN exists (`zigbee.availability_publish_conflict` — the relink-precedent conflict swallow, unreachable in practice): flagged because DP-8 said "no other new tokens"; it is an error-arm log line, not scenario vocabulary — hub adjudicates.
3. **The instruction's DP-4 parenthetical "a responding ping ALSO arrives as RX (onFrame fires)" is refuted at source** (`awaitIncomingLocked` CONSUMES the matched response — `EzspCoordinatorProtocol:1621-1653`; only non-matching callbacks feed the drain): `PING_SUCCESS` via `recordCommandResult(true)` is the ping's sole (still device-originated) evidence. Behavioral outcome identical (edge-triggered either way); the mains-ping-success test asserts reality. [INFO]-class; recorded so the next reader doesn't re-derive it.
4. **P2 publish-count sweep outcome:** ONE in-module drift — `ZigbeeConfigAcceptedAdoptionTest` T4's `AVAILABILITY_CHANGED count==1` → 2 (the entity-grain seed + the untouched device-grain relink emission; updated with grain-split assertions). Everything else immune (type-filtered `ofType` counts, or legs whose frames never reach `onFrame`). The TCJ/KEY `published().isEmpty()` pins hold structurally (join/child/key handlers never call `onFrame` — re-asserted by the new never-feeds tests). Lifecycle ITs defensively re-run forced-fresh: 56/0/0 GREEN.
5. **Silicon leg (post-soak, composes with Wave-2):** SNZB-02P joins → reports flow → entities AVAILABLE in `/api/v1/entities` (FE-7's data goes live); then a genuine offline leg (unpowered past the window / mains ping-timeout) reads UNAVAILABLE. The mains ping arm's wire behavior (a Basic read of 0x0000 answered by real devices) is bench-verify-class — every constant rides existing proven seams.
6. **Nick's gate:** full `./gradlew check` pre-commit; CI on the pushed commit = the gate of record.

## Archived-2026-07-12 · 2026-07-10 [Coder (Claude Code) → PM hub, Nick]  (M9.5-DURc DELIVERED — the lifecycle projection barrier, TEST-ONLY ×3 files · NO builds run in-session, the full gate is Nick's · TWO [REVIEW]s need adjudication: the DP-1 mechanism corrected + the DP-4 premise refuted-and-extended) — **SERVED (v28 beat 2 = the adjudication: BOTH [REVIEW]s RATIFIED-ACCEPT at source, both instruction errors HUB-OWNED [item 1 the checkpoint-target deadlock — the corrected max-matching-registration-fact target IS the DP's intent; item 2 the refuted RestartHonestyIT premise — the barrier extension ratified as one revertible hunk] → landed `7e4426b` CI GREEN, completing `a1f0f77`'s gate record; item 3 the LC-LABEL-LOG follow-up row NAMED (three ITs); item 4 the gate DISCHARGED. Restamped + archived 2026-07-12, v30 first-acts.)**

Delivered per the 2026-07-10 instruction against core `a1f0f77` (both named target files untouched by it — baseline exact). Diff: exactly 3 lifecycle test files, +123/-0, ZERO src/main (git-verified). Full detail in `coder-handoff.md` (newest entry). Items needing hub/Nick attention:

1. **[REVIEW] DP-1's literal barrier (`checkpoint >= event-store head`) deadlocks deterministically — implemented as `checkpoint >= max(globalPosition) over the device_registered/entity_registered envelopes` instead.** Verified at source: the bus advances a runtime subscriber's checkpoint ONLY on MATCHING deliveries (`InProcessEventBus.notifyEvent:276` never offers filtered-out positions; `liveLoop:492` filter-miss `continue`s with no checkpoint write); `registry_projection` is `forTypes`-filtered over the 3 registration/removal types; `adopt()` publishes the non-matching `device_adopted` LAST (`ZigbeeAdoptionSlice:59/:382`) — so the store head is unreachable by that checkpoint at barrier time, every run. The `ReplayTransitionIT` pattern source is head-safe only because its subscriber is `SubscriptionFilter.all()`. The corrected target IS the DP's intent (durable consumption of the adoption events). Lesson appended (coder-lessons 2026-07-10).
2. **[REVIEW] DP-4's exclusion premise ("they don't label") is REFUTED by its own mandated grep for `RestartHonestyIT`** (`label` at `:152`, `updateEntity` at `:157` — the identical adopt→label boot shape, the identical wipe race, prior green probabilistic per the diagnosis's own doctrine). The barrier was extended there as an independently-revertible hunk — leaving a diagnosed same-mechanism race armed in the same suite would endanger the very CI run that is this WU's + M9.6-SEED's gate of record. **Ratification requested; one-hunk revert if ruled out of scope.** `ProcessRestartIdentityIT` confirmed label-free by grep (zero hits) and untouched — DP-4 holds there.
3. **The instruction's recorded follow-up row (test labeling rides the log — an F1 `entity_registered` re-emit carrying the label) now covers THREE ITs**, RestartHonestyIT included, when the hub names it.
4. **Nick's gate:** `:lifecycle:lifecycle:test --rerun-tasks` ×3 + full `./gradlew check`, then push — CI green on that commit is the gate of record and simultaneously completes the `a1f0f77` (M9.6-SEED) gate record. `spotlessApply` was not run in-session (no build capability this lane); the diff is test-tree-only, so a spotless touch-up is safe if needed.

## Archived-2026-07-12 · 2026-07-10 [Coder (Claude Code) → PM hub, Nick, bench/B2 lane]  (M9.6-RO DELIVERED — reopen identity capture · targeted gates GREEN 451 forced-fresh, stage-A red = exactly 4 for the right reasons · full check DEFERRED · ZERO [REVIEW] deviations · silicon legs owed post-soak) — **SERVED (v28 beat 2 = the intake + two-layer ACCEPT → `1627978` CI GREEN [item 1 the DP-5 token banked for the B2 library; item 2 the six [INFO]s ratified incl. the vendorId≥0 guard; item 3 the MODULE_CONTEXT corrections verified]; the audit's one [REVIEW] closed by the R1 by-id-pin test `3590fca` WITH the append-only record correction [the OBS-2 precedent — pm-handoff v28 beat 2]; item 4 the silicon legs = Monday's pinned order: B1's usb-reenumeration scenario + BOTH topologies + the pinnedOnly=false glance [backlog M9.6-RO row]; item 5 the gate DISCHARGED. Restamped + archived 2026-07-12, v30 first-acts.)**

Delivered per the 2026-07-10 instruction against core `7e4426b` (dispatch HEAD; authoring baseline `04f5f70` + SEED + DURc above it — **G-RO2 re-run at dispatch: code-file disjointness HELD exactly**; the only shared path is `MODULE_CONTEXT.md`, the WUCP doc, SEED's section untouched by my append). Full detail in `coder-handoff.md` (newest entry). Items for the hub's intake + grep vocabulary:

1. **NEW frozen-format INFO token (DP-5) — a B2-library note, no scenario binds it yet:** `zigbee.port_identity_captured: stableId={} vendorId={} productId={} pinnedOnly={}` (ids lowercase hex, e.g. `10c4`/`ea60`; the pinned sentinel renders `0`/`0`). Post-fix the alias-pinned bench config must read `pinnedOnly=false` — the both-topologies verification glance-point. `reopen_no_target` / `zigbee.reopened` / `production_session_started` byte-frozen (diff-verified, zero hits).
2. **Zero [REVIEW] deviations — six [INFO]s in coder-handoff.** The load-bearing one: the DP-1 re-scan guards `vendorId() >= 0` because `identityFor` + the `PortIdentity` ctor's negative-rejection pin would crash `bindTransport` on jSerialComm's −1/−1 "unknown" candidates (the UART-hat class) — the reject routes to the DP's own pinned-only fallback. Lesson appended (coder-lessons 2026-07-10); gotcha recorded (MODULE_CONTEXT M9.6-RO).
3. **The M9.4b MODULE_CONTEXT gotcha "reopen then matches by stable path only" was corrected in place** (dated, the SEED 0x1B04-row precedent): it described intent the mechanism never realized — the field-confirmed capture bug. The M9.4 multi-identical-dongle limitation row gained a DP-2 note (pinned-only identities now immune by construction; the healthy-identity Tier-2 two-stick exposure stands, unchanged this WU).
4. **Silicon legs (the R-row's close, Monday's pinned order):** (a) the B1 runner's `usb-reenumeration` scenario green on the fixed build; (b) BOTH topologies (direct-attached AND hub-attached — the dongle-behind-hub move is its own single-variable step); (c) the `pinnedOnly=false` INFO glance on the alias-pinned config. UDEV-RAMP stays gated on this landing.
5. **Nick's gate:** full `./gradlew check` pre-commit; CI on the pushed commit = the gate of record. Per the no-open-[REVIEW] rail, the M9.6-AVAIL dispatch rides this WU's audit clearing.

## Archived-2026-07-10 · 2026-07-09 [Coder (Claude Code) -> PM hub, Nick]  (M9.5-DURb DELIVERED — half-registration repair + zigbee seam pack · targeted gates GREEN 866 tests forced-fresh + lifecycle defensive-green · full check DEFERRED · silicon confirmation piggybacks on the acceptance-run entry boot) — **SERVED (v26 beat 1 = the intake + two-layer audit [zero code defects; the four INFOs adjudicated ACCEPT]; the grep-vocabulary items rode the acceptance run’s §51 greps; the DP-B3 supersession honored; item 3’s R2/TCJ-R2 closures recorded in the audit. Archived 2026-07-10, v27 beat 2 — the chartered sweep.)**

M9.5-DURb delivered per the 2026-07-08 instruction against core `ef1d2c5` (baseline exact; G-DB1–G-DB6 all PASS first-hand). Full detail in `coder-handoff.md` (newest entry). Zero mints / schema / module-info / build-file / public-API diffs — the instruction's pins all held this time. Items for the hub's grep vocabulary + intake:

1. **NEW frozen WARN token (DP-B1):** `zigbee.half_registration_detected: device={} deviceId={} — re-proposing for repair` — fires only on the durable half-registration state; the repair shape in a log is WARN → `device_proposed` → `proposal_accepted` → `device_adopted` with the SAME deviceId as the original adoption. NO `availability_changed` on the repair path (it never rides relink).
2. **`zigbee.adoption_maps_rehydrated: devices={}` is now INFO and UNCONDITIONAL (DP-B3)** — the acceptance-run entry boot should show `devices=2` at adapter init (the DURb piggyback glance-point, with a normal adoption alongside). The prior DUR note's item-4 "at DEBUG" clause is superseded.
3. **The M9.4-RPT R2 row is CLOSED (DP-B6):** the command fence now reads the INSTALLED policy first — a §3 posture-DISABLED capability's command renders the immediate honest UNCONFIRMED verdict (never silent). The M9.4-TCJ R2 stale-window limitation is CLOSED (DP-B5: successful reopen clears the deadline). The DUR note's item-5 half-registration ruling is EXECUTED (this WU).
4. **[INFO] G-DB5 residual:** the BENCH-VERIFY token is zero in src/main + testFixtures (the DONE-when grep); the TEST tree keeps 6 comment-level references to the *discipline* (not flip targets, outside the instruction's two-file scope) — a comment-only micro-pass if the hub wants the vocabulary gone entirely.
5. **[INFO] the §A.3 transient-key self-expiry flip is honesty-bounded:** the record never directly observed expiry (it-1: "unobserved"; never measured since) — flipped to enablement-arc evidence + "direct observation unmeasured; clear-on-window-close contingency stands", not a fabricated citation.

---

## Archived-2026-07-10 · 2026-07-08 [Coder (Claude Code) -> PM hub, Nick]  (M9.5-DUR DELIVERED, code half — registries-as-projections per ratified AMD-99 · targeted gates GREEN 1,829 tests / full check DEFERRED · ONE [REVIEW]-class module-info diff needs a PM ruling · WU-done gate = bench ITERATION 5b) — **SERVED (v25 beat 3 = the rulings record: item 1/R1 module-info promotion RATIFIED-ACCEPT [hub authoring miss owned]; item 2 widenings ACCEPT; item 3 DP-6 availability_changed noted + observed at 5b; item 4 glance-points served iterations 5b + the acceptance run; item 5 half-registration → M9.5-DURb LANDED `04f5f70` + silicon-confirmed. Archived 2026-07-10, v27 beat 2.)**

M9.5-DUR delivered per the 2026-07-08 instruction against core `e15858d` (baseline exact; G-DUR1-G-DUR7 all PASS pre-code; no G2 sizing signal — the mutator census was exactly the four sites). Full detail in `coder-handoff.md` (newest entry). Items needing hub/Nick attention:

1. **[REVIEW] `core/device-model/module-info.java`: `requires com.homesynapse.event` -> `requires transitive` (one word).** The instruction pinned ZERO module-info diffs, but DP-2's public `RegistryProjection`/`RegistryEventMapper` name event payloads on the exported surface — under `-Xlint:exports -Werror` the plain requires cannot compile (the M2.9/M3.6e.1/M5-A three-precedent class; lifecycle's M9.1/M9.4b promotions carry the identical rationale comments). Gradle was already `api` — the lockstep needed only the JPMS word. Requesting ratification; the register's device-model JPMS row should fold it.
2. **[REVIEW] public-surface widenings the DP-3 R4-shape wiring required:** ZigbeeIntegrationFactory public ctor 3->4 arg (`Supplier<RegistryProjection>`), public unguarded `HomeSynapseCore.registryProjection()` accessor, rig ctor widening, lifecycle test-scope logback (the zigbee F-12 precedent applied to lifecycle for the projection_live INFO capture).
3. **[INFO for the 5b runbook]: DP-6 rehydration publishes one `availability_changed` per registry-carried device at adapter initialize()** (the direct consequence of reusing the EXISTING relink verbatim, as instructed). At 5b expect N availability lines at boot BEFORE any announce; if the hub wants map-rebuild-without-publish instead, it is a small ruled follow-up, not an improvisation.
4. **5b glance-points now live:** `registry.projection_live: devices={} entities={} position={}` at every boot (the anti-vacuous rebuild evidence); `zigbee.adoption_maps_rehydrated: devices={}` at DEBUG.
5. **[REVIEW — ruling wanted before/at 5b] the durable half-registration gap** (found by the in-session adversarial review, confirmed): a process death or entity-publish failure BETWEEN the durable `device_registered` and its entities' publishes leaves a permanently entity-less device — re-announces take the LINKED arm forever (no proposal re-mints; no `device_removed` emitter exists), and replay faithfully rebuilds the hole. AMD-99 inherits this from DP-3's non-atomic emission; candidate repairs in the coder-handoff entry (re-propose arm on LINKED-with-zero-entities + COMPLETE interview, or a compensating tombstone). Javadoc corrected in-place; no arm improvised.

---

## Archived-2026-07-07 · 2026-07-07 [Coder (Claude Code) → PM hub, Nick]  (M9.4-RPT DELIVERED — reporting binding + drive + posture routing + §B; three [REVIEW]s, one runbook-vocabulary action before iteration 4) — **SERVED (v23 hub): runbook step-11 AMENDED beat 5 (item 1); R2 = named polish row + R3 = re-rate row on iteration-4 data (beat 5, Nick-ack pending on R2); the §3 selector adjudicated in the beat-5 audit; the BENCH-VERIFY blocks proved out in iteration 4 (0x11 measured; the sleepy 5 s window held). Archived beat 7.**

M9.4-RPT delivered per the 2026-07-07 instruction against core `43c2880` (baseline exact; G-RPT1–G-RPT6 all PASS; §B included). Full detail in `coder-handoff.md` (newest entry) + the session completion report. Deferred build gate: targeted `:integration:integration-zigbee:test` + `:app:homesynapse-app:test`, then full `./gradlew check` — Nick's environment.

**PM-relevant items:**
1. **Runbook step-11 vocabulary ([REVIEW], pre-iteration-4 action):** the frozen M9.3 configurator has NO WARN on the 0x86/0x8C/timeout degrade rungs (only `bind_failed`/`reporting_ack_lies`/`ias_enroll_incomplete` exist at source). Loudness for those rungs rides the NEW `zigbee.confirmation_downgraded` WARN + the `zigbee.reporting_configured` INFO's `degraded=` count. Step-11's grep list should add `confirmation_downgraded` and treat INFO-present + WARN-absent as the healthy shape.
2. **Posture-DISABLED commands render no immediate verdict ([REVIEW]):** the `ZigbeeCommandHandler` SD-3 fence reads the PROFILE characterization, never the INSTALLED policy — a §3 posture-DISABLED capability's command dispatches optimistic-silent (never-false-CONFIRMED still structural). Closing the instruction's "still renders its immediate honest verdict" for posture-driven downgrades = teaching the fence to also read installed policy; needs a ruling.
3. **Recurring WARN on designed postures ([REVIEW]-lite):** Xiaomi-skip and sleepy-TIMEOUT facts are VERIFIED_REPORTS-with-note ⇒ strict-VERIFIED rates them best_effort ⇒ one `confirmation_downgraded` WARN per drive on healthy devices of those classes. By-the-book per Doc 08 §3.6 line 291; consider re-rating that arm to INFO in a future WU.
4. **The §3 gap-3 selector (FLAGGED coder's choice, adversarially endorsed):** VERIFIED = read-back-matched (`VERIFIED_REPORTS && note==null`); NONE ⇒ DISABLED-with-recorded-reason; all other non-verified ⇒ BEST_EFFORT with tracking KEPT. The counterfactual is named in coder-handoff: DISABLED-for-READBACK_ONLY breaks RestartHonestyIT's CT-15000 pin.
5. **Bench-watch (iteration 4):** both new BENCH-VERIFY blocks (`EzspReportingOps` ZDO/ZCL ids + the 0x009B layout/EmberKeyStatus vocabulary + getEui64 0x0026) are one-constant corrections; the sleepy-exchange timeout is 5 s (chosen) — verify against the SNZB awake window.

## Archived

_(All entries below resolved/superseded — kept inline for trail; bulk-move to `archive/cross-agent-notes-2026-Q2.md` at the next hygiene pass. The M9.4a note directly below was archived 2026-07-04 by the v19 hub at the currency pass — its "archives on the beats-1–4 commit" condition was met when the v18 beats-1–6 commits all landed (hivemind `33aae08`/`68ccbf6`). The four 2026-07-02/03 delivery notes — FE-1b · Doc-18 · M9.1 · M7.5c-a — were archived 2026-07-03 by the v15 hub at the launch quiescent window; the six 2026-06-27/28 notes were archived 2026-07-02 by the v14 hub. Separator relocation, content untouched. The M9.2 note below was EXTRACTED from the frontmatter splice at beat 59 — one canonical copy, served.)_

## 2026-07-04 [Coder (Claude Code) -> PM hub, Nick]  (M9.4a DELIVERED — the hero loop is GREEN hardware-free; six [REVIEW] items need rulings before/at M9.4b authoring) — **SERVED (v18 hub, beat 4): all six ruled ACCEPT (pm-handoff v18 beat-4 = the rulings record); the queued items folded into the backlog M9.4b row; the gate was discharged pre-commit (check 155 GREEN). Archived 2026-07-04 (v19 currency pass) — the beats-1–4 commit condition was met at the v18 close.**

M9.4a delivered per `context/instructions/2026-07-04_M9.4a_moat-wiring-and-hardware-free-hero-loop_coding-instruction.md` — full detail in `coder-handoff.md` (newest entry) + the session completion report. The done-when is green in-session: `HeroLoopHardwareFreeIT` (motion → `occupancy.occupied` → trigger → dispatch → real adapter → byte-asserted frame → honest CONFIRMED with the causal chain; the CT derivation with the tuned 15 s window visible on `command_issued`; the F-2 supersession pair; the identify honest verdict; timeout honesty) + `ZigbeeReplaySafetyIT` (zero NCP frames in replay, the LIVE control fires). Full `./gradlew check` = Nick's deferred gate.

**Rulings queued for the PM/Nick (detail under [REVIEW] in coder-handoff):**
1. IT home = lifecycle test tree (testing/integration-tests is Pi-profile-gated out of default `check`) — accept or relocate.
2. `ZigbeeClusterHandler.buildCommand` base-`final` removal (P27's per-impl-throws shorthand was structurally inaccurate).
3. **identify is not issuable through Tier-1 dispatch validation** (no core capability defines it; the profile characterization has nothing to install onto) — needs an identify capability or a validation carve-out; the E2E drives router+handler directly meanwhile.
4. **R4 registry duality:** Main's factory-injected `InMemoryDeviceRegistry` is NOT the core's internal one — MUST unify at M9.4b when the real transport binds (dispatch resolution reads the core's).
5. The `onCommandResult` disposition guard (the F-2 loop-back class + the pre-existing `expired_on_restart` sibling) — a Doc 07 §3.11.2 currency note.
6. Two forced `HomeSynapseCore` surface additions for W10 (`registerIntegrationSchema` public gateway, java.base-only signature; package-private `schemaRegistry()`).

**Also flagged ([INFO], M9.4b candidates):** the brightness capability-domain mismatch (level 0–254 at ingestion vs percent 0–100 in the derivation domain — set_brightness confirmations would false-timeout); `StandardExplanationService` renders `superseded`/`unconfirmed` as FAILED; the trigger-map `type:` selector-key collision in the automation loader (inline type-selectors on triggers are unreachable).

## 2026-07-03 [Website lane (Cowork) → PM hub, Nick]  (Website lane DELIVERED: SSG ruled [Astro @ docs-repo website/site/] + 6-page skeleton green [zero-JS enforced, shared tokens cross-repo] + the R-3 developer page [truthful posture, 13-row claim→truth table] · dist-gitignore rider REFUTED with evidence — commit 1 intentionally absent, ZERO core writes · docs commit stages EXACTLY 26 · gate 4 NOT declared closed · SERVED beat 62: residuals landed docs `78e0c5c`, zero-JS gate green on host)

_(Hub annotation, beat 62: every item below is served — the audit ACCEPT + refutation fold landed beat 59; Nick's residuals landed 2026-07-03: native gate GREEN (6/6 pages + "zero-JS gate OK" on the Windows-fixed script) → the 26-path content landed inside docs `78e0c5c` (merged with the Doc-18 DRAFT into one `add -A` sweep — 27 files exact, arithmetic reconciled, provenance note pm-handoff beat 62); Commit B stayed correctly un-run (preempted beat 58); the index.lock never obstructed. website.yml adopt-now + LICENSE-flip remain on Nick's OPEN ledger — carried in the spine, not by this note. Original note text follows verbatim.)_

**Topic:** The 2026-07-03 website-lane brief is closed per its §4 contract; full detail in `context/audits/2026-07-03_website-lane_return.md`. Ruling on record (Nick, in-session): Astro (default confirmed; ^5.18 pinned for Vite-6 sameness with the dashboard) at `homesynapse-core-docs/website/site/`. Built: the landing built out inside the D-4 IA (segment fork on one shared landing; W-4 embargo held over the brief's plug-and-play phrasing), three stubs as SHORT honest public pages (planning stubs preserved in-file), config-superiority rendered (edits = 3 name-token lines), and `pages/developers.md` — "SDK maturing — the adapter contract is frozen and documented," L-12 voice, DP-18-A/C stated per the beat-57 rulings, no dates/device-counts/sandbox claims. Build: one command, green in-lane (esbuild native SIGSEGVs in this sandbox → gate ran on a md5-proven /tmp copy with a build-copy-only wasm override; committed tree is native; host re-run = gate of record, commands in the return §7). Name-light throughout ({{productName}}/{{companyName}} build-time substitution; company name tokenized too — W-7's footer line survives the W-11 rename).
**Action needed:**
- **PM hub:** (1) two-layer audit + fold the return. (2) **Fold the §6 refutation: `web-ui/dashboard/dist/` has been DOUBLY gitignored since `b296e76`** (module-local `.gitignore:3` + root unanchored `dist/`; check-ignore + ls-files evidence in the return) — the FE-1b finding routes to CLOSED-REFUTED; the brief's commit 1 is intentionally absent. (3) Note the three prompt-vs-source corrections (Apache-2.0 publish-gated, not repo-true today; DP-18-A already RATIFIED at beat-57; W-4-over-D-4 phrasing) — the license flip must land before the developer page publishes. (4) Rule/route the §8 `website.yml` proposal (docs-repo CI, build-only, dual sparse checkout — recommend adopt-now). (5) Next-lane ranking: content build-out of the three dossiers (W-5/PG-1, the binding gate-4 content gap) > website CI > FE T2.
- **Nick:** (1) **`rm homesynapse-core-docs/.git/index.lock`** (stale 0-byte, VM-created this session — blocks the commit). (2) Run the deferred gate host-side: `cd homesynapse-core-docs/website/site && npm ci && npm run build` (expect 6 pages + zero-JS gate OK). (3) Commit A (docs, stages exactly 26) + Commit B (hivemind, stages exactly 2) per the return §10; messages staged in `_scratch/`. (4) No core action — this lane wrote zero core files.

## 2026-07-03 [Coder (Claude Code) → PM, Nick]  (M9.2 — Zigbee transport/EZSP layer DONE, NOT gate-verified · Gradle DENIED again despite the intended allow-list · SERVED: audited beats 58–59, rounds 1+2 hub-fixed, R1–R7 ruled ACCEPT, landed core `6ea6912`)

_(Hub annotation, beat 59: every PM item below is served — the seven `[REVIEW]`s ruled at beat 58; the gate-environment flag escalated [CC allow-list fix before M9.3]; the doc-currency notes queued on the Doc-18 docs pass; the refuse-to-close line's "DP-B needs Nick's confirm" was STALE at authoring — DP-B was CONFIRMED at beat 57. Original note text follows verbatim.)_

- **Delivered** per the 2026-07-03 instruction against core HEAD `cf3733e` (baseline exact; all STOP gates G1–G9 re-verified). The transport substrate is live in code: byte seam → ASH codec/session → EZSP transport/codec → `EzspCoordinatorProtocol` → probe/port-identity/watchdog → §3.13 formation over the `NetworkParameterStore` seam. 39 files (4 modified + 35 created), all new production types package-private, zero mint (71/41/53 untouched), zero DDL, zero composition-root change. **Deferred build gate (Nick):** spotlessApply → targeted compileJava → targeted test → full `check` — exact commands + change-set arithmetic in coder-handoff §DEFERRED. The 6-dimension adversarial workflow ran as the prescribed fallback: 2 HIGH found+fixed in-session (an AshCodecTest double-strip that would have failed the test gate; `SL_STATUS_NOT_JOINED` mis-pin 0x0B→0x17), 2 MEDIUM fixed (`resetSession()` relaunch gap; security bitmask → bellows baseline 0x1B04), 6 LOW hardening items fixed; zero compile-level findings (signatures verified against the actual cached jars).
- **GATE-ENVIRONMENT FLAG for Nick:** the session's permission gate denied `./gradlew :integration:integration-zigbee:compileJava` in its EXACT allow-listed form (plus spotlessApply, plus a sandbox-disabled retry). The allow-list either did not land in settings or the harness policy overrides it — worth fixing before M9.3, or the P5 shift-left keeps degrading to the LLM fallback.
- **Six `[REVIEW]`s for PM/Nick ruling (full detail in coder-handoff):** (1) forced `requires org.slf4j` + `implementation(libs.slf4j.api)` beyond the pinned one-directive diff (LTD-15 logging edge — no transitive slf4j exists; M9.1 precedent pair); (2) `testImplementation(:testing:test-support)` for TestClock (M9.1 precedent); (3) **formation security bitmask 0x1B04** (bellows baseline: +REQUIRE_ENCRYPTED_KEY +NO_FRAME_COUNTER_RESET; plaintext ZigBeeAlliance09 kept per §3.13; hashed-TCLK deferred to M9.4 — Nick security election); (4) negotiated >14 → PermanentIntegrationException (unspecified tier — ratify as AMD-96 band-edge policy); (5) resume-mismatch → PIE `zigbee.network_parameter_mismatch` (§3.13 has no explicit mismatch rule — ratify + Doc 08 currency note); (6) D-M92-6 tripwire crossed at 3/7 stubs (~43%) with the stub set exactly matching the instruction's anticipated classes — retroactive ACCEPT requested. Plus `resetSession()` (new package-private method, adversarial-review-driven, M9.1 relaunch discipline).
- **Doc-currency notes for the hub:** Doc 08 §3.3's “5 consecutive timeouts” matches neither reference (bellows 4 / Silabs+herdsman 6) — keep-as-policy recorded; W6's automatic-module premise is stale (jSerialComm 2.11.x ships a real module-info.class); §3.13 needs the mismatch rule + (later) the NETWORK_UP-await hardening note for M9.4.
- **Next WU (refuse-to-close):** M9.3 (interview/ingestion/profile registry) — JIT-authored after WUCP Phase 2; **Doc 18 must Lock first and DP-B needs Nick's confirm**. M9.3 inherits `drainPendingCallbacks()` as the ingestion feed + the seam inventory + the `integrations.zigbee.adapter_type` key binding + the pendingCallbacks-retention ruling.

## 2026-07-03 [Frontend lane (Cowork) → PM hub, Nick]  (FE-1b — the v1.1.1 fold DELIVERED + live smoke PASS vs core ec2e3b4 · lane gate green 55/55, bundle 57.1/100 KB · 11 files, all web-ui/dashboard/** · 4 ops findings routed · next-lane pointer: the WEBSITE build)
**Topic:** FE-1b is closed per the 2026-07-02 brief; full detail in `context/audits/2026-07-03_frontend-dev-lane_FE-1b_return.md`. **DRIFT-2 folded:** `ApiProblem.slug` keys every type detection on the URI slug suffix (`PROBLEM_TYPE_URI_PREFIX` + `problemSlug()` pinned in contract.ts, mirroring Doc 09 §3.8 / ProblemType.java:160); the mock emits URI-form types so mock === wire; contract-check now trips on version AND prefix drift. **DRIFT-1 folded as contract CURRENCY** — source-verified that no bare-body shim ever existed (the client's strict envelope check is what SURFACED DRIFT-1); A4 gains the ruled additive `entityCount`/`ready`; A5 `parkedSubscribers` CORRECTED to the ratified `string[]` (the mirror's pre-ratification object guess would have rendered blank ids on live parked data — caught, fixed in HealthView, rejection-pinned); additive `subscribers[]` typed + rendered. `CONTRACT_VERSION = v1.1.1-2026-07-02`; tests 45→55. **Live smoke vs `ec2e3b4`** (M9.1 delta verified disjoint from web-ui/ + api/rest-api/): poll loop LIVE (the FE-1 "views never refresh" failure is CLOSED), both Health cards render from the enveloped A4/A5, URI-form types confirmed on the live wire, Activity renders the honest "not served yet" state, zero error banners, console clean.
**Action needed:**
- **PM hub:** (1) two-layer audit + fold the return. (2) Route the four §4 findings: **(a)** operator token-lockout trap — `api_tokens` store vs `initial_api_token` artifact divergence 403-locks the operator with no recovery hint; cwd-relative `.homesynapse/` leaves stale siblings; a documented re-pair story is DP-F/A4.5-adjacent · **(b)** root-DEBUG logging echoes raw `Authorization: Bearer` headers (Jetty layer; freeze §0's "never logged" holds for HS code only) — suggest INFO default for `:run` · **(c)** the FE-1 return's "fresh DB after scripts/clean.sh" is CORRECTED (clean.sh is build-only; it never touches `.homesynapse/`) · **(d)** ExplainHub "Your automations" renders blank at zero automations (T2 polish, deferred per scope-OUT). Also one-line follow-up: `web-ui/dashboard/dist/` lacks a gitignore rule. (3) Dispatch the **website lane** (gate 4's open half — the brief's own next-lane pointer; beat-53's plan).
- **Nick:** commit commands + §10 audits staged — core stages EXACTLY 11 via explicit file list (host `npm run verify` creates an un-ignored `dist/`; do not directory-sweep), message `_scratch/2026-07-02_webui_fe1b_commit-msg.txt`; hivemind stages EXACTLY 2 (this note + the return), message `_scratch/2026-07-03_hivemind_fe1b-return_commit-msg.txt`. Post-bench token hygiene per the return §6 (delete the artifact once paired; the old token is dead).

## 2026-07-02 [Doc-18 research lane (Cowork) → PM hub, Nick]  (Doc-18 research + charter DELIVERED: plugin-ecosystem-wars dossier (L-1..L-33) + requirements charter · both FINAL, not DRAFT · write-isolated · routes to hub two-layer audit)
**Topic:** Both §5 deliverables of the 2026-07-02 dispatch are filed final. (1) `context/assessments/2026-07-02_plugin-ecosystem-wars_research-dossier.md` — nine ecosystems (HA · Z2M · SmartThings Groovy→Edge · Homebridge · openHAB · Hubitat/ESPHome/Node-RED + Matter-bridge note · VS Code · Chrome MV2→MV3 · Obsidian wildcard), the cross-cutting answers to all seven §3 questions, and the lessons ledger **L-1..L-33** the charter cites by number. (2) `context/planning/2026-07-02_Doc-18_requirements-charter.md` — Doc-18 section map in the Doc-17 mold (every requirement traces to an L-n or an assessment-§2 gap); **EXT-INV-1/2** mint candidates; the seven-seam reservation list finalized; **DP-18-A** (AX-7: semver + declared compat range + forward-only migration + deprecation floor recommended; strict immutability appears nowhere at ecosystem scale), **DP-18-B** (isolation line: IN_JVM curated wave-1; RESERVED_SUBPROCESS pinned as the marketplace-time default rung; honesty rule on sandbox claims), **DP-18-C** (R-C: no surveyed vendor paywalls community content — evidence framed, Nick rules); an explicit what-Doc-18-should-NOT-attempt; sequenced to Lock before M9.3.
**Verification posture:** 8 parallel research subagents under the anti-fabrication discipline (agent ids + tool-call counts in dossier §4); the 6 most load-bearing sources re-fetched lane-direct and confirmed verbatim (✓✓ in the tables); every load-bearing claim tagged VERIFIED/INFERRED/UNVERIFIED; honest gaps + corrections-to-the-brief listed in dossier §4.
**One flag for the audit:** dossier raw `wc -w` = 9,735 vs the ≤~8K envelope — ~2.4K of that is the per-claim source tables (the audit surface the discipline demands); prose ≈7.3K. Hub may trim tables at fold if the envelope is binding.
**Commit prep (env-model §10 audit):** this lane created exactly **2 new files** (both .md, both §5 deliverables) + this note; message at `_scratch/2026-07-02_hivemind_doc18-research-charter_commit-msg.txt`. The working tree ALSO carries 8 pre-existing modified files + 1 untracked (the dispatch prompt) from the v14 hub's beat-53 session — NOT this lane's. The prepared command therefore stages ONLY the 2 new deliverables (**stages exactly 2 files**); this cross-agent-notes.md append rides the hub's next hivemind sweep (the file already carried pending hub modifications — staging it under this lane's commit would sweep the hub's work in). No runtime state, build output, or secrets in the change set; no server run, no token minted. Command for Nick (host-side): `cd /c/Users/Nick/Desktop/Code/ClaudeFolder/nexsys-hivemind && git add context/assessments/2026-07-02_plugin-ecosystem-wars_research-dossier.md context/planning/2026-07-02_Doc-18_requirements-charter.md && git commit -F /c/Users/Nick/Desktop/Code/ClaudeFolder/_scratch/2026-07-02_hivemind_doc18-research-charter_commit-msg.txt`
**Action needed:** **PM hub:** two-layer audit both deliverables (source-check via the per-platform tables), then fold into Doc 18 authoring (Mode 1); queue DP-18-A/B/C for Nick at review; note the charter's M9.3 coupling (namespace convention must land before the profile-registry freeze — assessment §3 item 1). **Nick:** nothing until the hub routes. No resume point — the lane completed its arc.

## 2026-07-02 [Coder (Claude Code) → PM, Nick]  (M9.1 — the integration spine DELIVERED: first IntegrationSupervisor impl + command_dispatched→CommandHandler router + Phase-6 wiring, fake-proven + INV-ES-09-pinned · NOT gate-verified (in-session Gradle DENIED — the ENTIRE gate incl. targeted compile is Nick's) · zero-mint 71/41/53 · 25 files · 3 sanctioned-deviation seams for PM ruling)
**Topic:** M9.1 is built and handed back **fix-clean / NOT gate-verified**. The spine: `command_issued` → `command_dispatched` → **`CommandRoutingSubscriber` causation-join → `CommandHandler.handle(CommandEnvelope)` on a per-adapter single-threaded VT executor** — E2E-proven on the real core with a recording fake (`IntegrationSpineWiringTest` T18–T20) and **INV-ES-09-pinned** (`RunPipelineReplaySafetyTest` extended per its own M9 forward note: recorder ZERO across the replay window with a registered, ROUTABLE fake — the seeded `command_dispatched` is now causation-chained + carries the fake's derived id — and recorder FIRES in the LIVE positive control). `HomeSynapseCore` gains the canonical 7-arg ctor (5/6-arg delegate; `Main` unchanged → Phase 6 skip-if-empty, byte-identical production behavior until factories are injected). An in-session 6-dimension adversarial verification workflow (agents cross-checking every signature against real sources, in lieu of the denied compiler) found ZERO compile-level defects + 4 confirmed supervisor concurrency/behavioral bugs — ALL FIXED in-session (executor-recreation on restart; zombie-loop identity guards; claim-to-close double-close fix; spurious integration_stopped fix) + 1 regression test. Full closeout + deferred-gate commands + the complete `[Design point]`/`[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block); 4 MODULE_CONTEXTs updated (integration-runtime, lifecycle, config, persistence); 1 lesson appended.
**Cross-role facts that changed (new seams other lanes consume):**
- **`ConfigurationAccess.scoped(integrationType, model)`** (config, public static interface factory) — the B7 exposure seam is LIVE; the config MODULE_CONTEXT "deferred to M9" gotcha is closed.
- **`PersistenceFactory.commandParameterDecoder()`** (persistence, `Function<String, Map<String,Object>>`) — the M7.4b serializer's opposite direction; never throws on the bus thread (null/blank/"{}"/malformed → `Map.of()`).
- **`IntegrationSupervisorAssembly`** (integration-runtime, public): `SUBSCRIBER_ID="integration_supervisor"` (the 5th runtime subscriber when factories are present), `subscriptionFilter()` = {command_issued, command_dispatched}/DIAGNOSTIC/ENTITY, and `abandon(supervisor)` — the W4 fast-stop gateway (`[REVIEW]`, the InProcessEventBus.abandon precedent).
- **Lifecycle module-info:** `requires com.homesynapse.integration` PROMOTED to transitive (+ Gradle api flip) — the 7-arg public ctor exposes `List<IntegrationFactory>`; `+ requires com.homesynapse.integration.runtime` (plain).
- The router publishes **failure-only** `command_result`s (`integration_unavailable`/`unsupported`/`handler_error`, CRITICAL, origin SYSTEM) — the ledger consumes them for free; a normal `handle()` return publishes NOTHING (the adapter owns the eventual result, M9.4).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout; (2) rule the THREE `[REVIEW]`s (assembly `Function<String,ConfigurationAccess>` param · the persistence decoder pair outside the Files table · the `abandon` gateway) — all argued in the handoff top block, all recommend ACCEPT; (3) queue **DP-B (durable integration identity)** for Nick BEFORE M9.2 device adoption; (4) author M9.2 (transport/EZSP) JIT per the split charter.
- **Nick:** ~~run the deferred gate~~ **GATE PARTIALLY RUN (2026-07-02, same session):** targeted compile GREEN + all four targeted test suites GREEN (T1–T21 pass); full `check` failed at `:assertMaxHeight` (the ruled `lifecycle → integration-runtime` edge deepens the graph 7 → 8 — the direction allow-list was fine; the HEIGHT ceiling was the unpinned knob). **Fixed same-session: root `build.gradle.kts` `maxHeight = 8`** (documented; `[REVIEW]` in the handoff — change set now 26 files). **Remaining: re-run `./gradlew check`** — count pins 71/41/53; `RunPipelineConfirmWiringTest`/`HomeSynapseCoreTest` unmodified-green; the arch-rule suite + `:testing:integration-tests` had not yet run when the height assert tripped. **The 11 modified `web-ui/dashboard` files in the working tree are the FE lane's, not M9.1's.** (5) Rule DP-B when convenient — it gates M9.2.

## 2026-07-02 [Coder (Claude Code) → PM, Nick, frontend lane]  (M7.5c-a — the two `/internal/*` reads now carry the frozen v1.1.1 `{data, meta}` envelope · DELIVERED + unit-tested · NOT gate-verified · zero-mint 71/41/53 · BOTH module-infos + every build.gradle.kts UNCHANGED · unblocks the FE-1b live poll cursor)
**Topic:** M7.5c-a is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; the sandbox cannot run Gradle — attempted, failed at `build-logic` on the mount + JDK 11 vs 21). The DRIFT-1 close: A4 `GET /internal/projection` now returns `{data:{mode, viewPosition, lagEvents, projectionVersion, entityCount, ready}, meta:{viewPosition, timestamp}}` (frozen 4 + the ruled additive 2) and A5 `GET /internal/dlq` returns `{data:{depth, parkedSubscribers, subscribers[]}, meta:{...}}` (frozen 2 + the ruled additive per-subscriber detail). Both set the conforming weak ETag `W/"{viewPosition}"` + `X-HomeSynapse-View-Position`. The envelope replicates the conforming M7.5a/b inline idiom verbatim — pre-verify found NO shared helper exists (`ResponseMeta` is an unused Phase-2 record); no second mechanism invented. `lagEvents` = `max(0, eventStore.latestPosition() − cursor)`, wired as a second `java.base` `LongSupplier` through a widened `RestFilters.installAdminEndpoints` (5→8 args; sole caller `bringUpHttpSurface` updated); `projectionVersion` = the new `HomeSynapseCore.PROJECTION_VERSION = 5` constant (hoisted from the inline `StateProjection.create` literal — single source, M4.0b-5/AMD-53). **Zero event mint (71/41/53); BOTH module-infos + every `build.gradle.kts` UNCHANGED; zero DDL; NO new module edge.** Shape tests rewritten field-for-field (RunEndpointsTest pattern, `containsOnlyKeys`, `Clock.fixed`) + the instruction's explicit meta-presence pin on both + the `EndpointE2eIT` DLQ consumer updated to the enveloped paths. Full closeout + deferred-gate commands + `[INFO]`/`[REVIEW]` list in `coder-handoff.md` (top block); rest-api MODULE_CONTEXT §M7.5c-a + a lifecycle MODULE_CONTEXT seam-change gotcha added; 1 lesson appended.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout. (2) Rule the ONE `[REVIEW]`: `parkedSubscribers` = ids with `dlqDepth > 0` (DLQ-entry vocabulary; `SubscriberMode` has no PARKED value) — a SUSPENDED-but-empty-ring subscriber is NOT listed (visible via the additive `subscribers[].mode`); recommend ACCEPT. (3) Notify the FE lane: A4/A5 are now envelope-conformant at the frozen shapes — FE-1b can drop any bare-body tolerance. (4) Advance the next-WU pointer to **M9.1** (instruction issue-ready).
- **Nick:** run the deferred build gate against the working tree on core HEAD `2786526` — `./gradlew :api:rest-api:compileJava :lifecycle:lifecycle:compileJava`, then `:api:rest-api:test :lifecycle:lifecycle:test`, then `./gradlew check` (+ watch `:testing:integration-tests` — `EndpointE2eIT`'s DLQ paths moved under `$.data`). **Count pins stay 71/41/53; both module-infos + every `build.gradle.kts` UNCHANGED; change set = 10 files (7 source/test + 3 MODULE_CONTEXT).**

## 2026-06-28 [Coder (Claude Code) → PM, Nick]  (AB-4 — at-rest cipher ACTIVATED + F1 envelope version byte + F3 nonce-construction fence + F13b fail-closed nonce-counter fsync DELIVERED + unit-tested · NOT gate-verified · zero-mint 71/41/53 · all four module-infos UNCHANGED · A4.5 backup-key seam recorded below · one Doc 15 edit FLAGGED FOR NICK)
**Topic:** AB-4 is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). It is the trust-hygiene hard-order step that must land **before M9** writes the first real device data — **encrypt-from-genesis** (the immutable log is encrypted from the first sensitive write or never). The five obligations: **A4.1** `Main` activates the cipher via the **6-arg** `HomeSynapseCore` ctor (flips `SqlitePersistenceLifecycle`'s cipher-presence gate → encryption LIVE for `[identity, presence_personal]`); **A4.2 F1** a 1-byte envelope version discriminator (`v1`) framed in the persistence codec (`SqliteEventStore` prepend/strict-parse — unknown byte → new `FailureKind.UNKNOWN_ENVELOPE_VERSION`, never implicit-v1) AND bound as GCM **AAD** (downgrade-resistant) via a **widened `PayloadCipher` seam** (`byte[] aad`, `java.base`-only → zero new edge); **A4.3 F3** a programmatic one-nonce-construction-per-scope fence in `StandardScopeKeyManager`; **A4.4 F13b** a fail-closed nonce-counter directory fsync (`AtomicYamlWriter.writeAtomicallyDurable`, scoped to not break Windows). **Zero event mint (71/41/53 unchanged); ALL FOUR `module-info`s (app/persistence/config/lifecycle) + every `build.gradle.kts` UNCHANGED; zero-DDL** (the F1 byte rides the existing `payload` BLOB). Ran a **4-dimension adversarial review** (crypto / compile / constraints / tests) → **ZERO confirmed defects**. LLM pass, NOT the gate. Full closeout + deferred-gate commands + the full `[Design point]`/`[INFO]`/`[REVIEW]` list in `coder-handoff.md` (top block); both MODULE_CONTEXTs updated; 1 lesson appended.
**📌 A4.5 — the backup-key portability seam + rotate-DEK-on-restore boot contract (recorded here per the instruction — "the hub records the seam in the spine"):**
- **Backup-key portability seam (the exportable recovery artifact).** The machine-local `.root-key` (Doc 15 §3.5) is install-local — a backup restored on a *different* machine cannot re-derive the scope KEKs/DEKs from it. A restorable backup therefore needs an **exportable recovery artifact** that carries (or re-wraps) the key material so a restored install can re-install the DEKs. AB-4 reserves this as a **seam only** — the export/import mechanics are the backup/restore WU. The natural shape: an operator-held recovery secret (or a passphrase-wrapped export of the root key / per-scope DEKs) produced at backup time and consumed at restore time, kept OUT of the event log.
- **Rotate-DEK-on-restore boot contract (DP-3 / AMD-94 §2.1, R-α REC-235).** The engine **refuses to encrypt in a scope** until EITHER a fresh DEK version is installed (the restore path — rotate = an additive new `scope_keys` row, `key_version` = prior max + 1; **retain all priors, never re-encrypt or overwrite**, F6) OR the persisted nonce counter is proven ≥ all prior nonces under the active DEK version (the crash-recovery path). This is what makes a restore safe: rather than resume a possibly-replayed counter under the old DEK (a `(key, nonce)` reuse), the restore rotates the DEK so the reused nonce bytes pair with a fresh key. AB-4 **carries this as the seam contract**; the restore mechanics (and the refuse-to-encrypt enforcement point) are the backup/restore WU. F13b (the durable nonce-counter fsync) is the crash-recovery half's foundation: the high-water mark is durable-ahead-of-return, so crash recovery resumes strictly above all used nonces.
**`[Design point]` resolutions (the instruction invited pushback):**
- **F1 siting = B2 (persistence frames the byte; AAD threaded to the config GCM).** Forced by three joint requirements: the typed `UNKNOWN_ENVELOPE_VERSION` must live in persistence (where the exception type is), the AAD must reach the config GCM, and decrypt-on-restart must be correct (a config-side AAD inference would be split-source-of-truth or a restart bug — a fresh manager's in-memory construction map is empty). Hence the widened `PayloadCipher`/`ScopeKeyManager` seam — an `[INFO]` deviation from the Files-to-Modify table (which did not list them), `java.base`-only so zero new edge.
- **F13b scoping = nonce-counter-only, open-vs-force distinction.** Fail-closed applies only to the nonce-counter write; a directory channel that cannot be OPENED (Windows/non-POSIX) is tolerated, only a genuine POSIX `force()` failure is fatal. `scope_keys`/`secrets`/config keep the best-effort `writeAtomically`.
- **Cipher warmup (OQ-15-2) = DEFERRED** (latency nicety, not correctness; encrypted scopes have ~zero live producers pre-M9 so the cold-start cost is not paid until M9).
- **F13a = write-time guard CONFIRMED; ctor assertion deliberately NOT added** (source-verified: the AB-2 fail-closed-READ tests construct the store with a null cipher + non-empty encrypted scopes; a ctor assertion would break that).
**`[REVIEW]` behavioral test change:** `ScopeKeyManagerPayloadNonceTest.fence_randomIvPathUnchanged` was rewritten — it used to mix `encrypt`+`encryptPayload` on ONE scope, which F3 now FORBIDS. Production is unaffected (secrets vs event scopes are disjoint).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 1 lesson + both MODULE_CONTEXTs). (2) Ratify the F1-siting `[INFO]` deviation (the widened seam) + the F3 test rewrite. (3) Advance the next-WU pointer to **M9** (or re-sequence **M7.5c** back ahead, Nick's call — AB-4 leapfrogged it 2026-06-28).
- **Nick:** (1) run the deferred build gate against the working tree on core HEAD `eed477e` — `./gradlew :app:homesynapse-app:compileJava :core:persistence:compileJava :config:configuration:compileJava :lifecycle:lifecycle:compileJava` then `./gradlew check`. **All four module-infos + every `build.gradle.kts` UNCHANGED; count pins stay 71/41/53.** (2) **Land the A4.5 Doc 15 §3.4/§6 note** (the backup-key seam + rotate-on-restore contract above — seam only, a docs-repo edit AB-4 flagged but did not make).

## 2026-06-28 [Coder (Claude Code) → PM, Nick]  (M7.5b — the non-firing read `GET /api/v1/automations/{id}/non-firing` + the automation list `GET /api/v1/automations` DELIVERED + unit-tested · NOT gate-verified · zero-mint 71/41/53 · BOTH module-infos UNCHANGED · DP-B2 clean-success edge ROUTED TO NICK · completes the frozen §B3 differentiator read surface)
**Topic:** M7.5b is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). It completes the differentiator's read surface as **pure projections** over the immutable log + the in-memory `AutomationRegistry` (Doc 16 §3.3/§4 LOCKED; INV-SA-03/SP2): two ADDITIVE methods on the M7.5a `ExplanationService` (`explainNonFiring(AutomationId, long expectedSincePosition)` + `listAutomations()`), the new records `NonFiringExplanation` (+ `LastEvaluationView` + the 4-value `NonFiringVerdict`) and `AutomationSummary` (+ `ComponentView`), impl in the existing `StandardExplanationService`, and two package-private rest-api handlers behind `RestFilters.installAutomationQueryEndpoints(...)` (Object-erased gateway). **Zero event mint (71/41/53 unchanged); BOTH `core/automation` + `api/rest-api` module-infos UNCHANGED; `build.gradle.kts` UNCHANGED** (reuses M7.5a's plain `requires com.homesynapse.automation`; no new edge). Ran a **two-stage in-session verification** (a 7-agent source-verification fan-out BEFORE writing — which CORRECTED several instruction inaccuracies, see below — and a 4-dimension adversarial review AFTER that found exactly one defect, a runtime test-assertion mismatch, now fixed; otherwise zero). Both LLM passes, NOT the gate. Full closeout + deferred-gate commands + the full `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block); both MODULE_CONTEXTs updated; 1 lesson appended.
**⚠ The decision the PM/Nick MUST rule (public-API semantics):**
- **DP-B2 — the clean-confirmed-success edge.** The frozen 4-value `verdict` has no "fired fine" value. When the most-recent in-window run is a clean confirmed success, I implemented the instruction's **recommended default**: `verdict=NEVER_TRIGGERED` with a **non-null** `lastRelevantRunId` + a "last fired and confirmed at <time>" explanation (so the UI distinguishes "ran fine" from "never ran"). The alternative is an additive post-V1 5th verdict `FIRED_CONFIRMED` (the `name`/`inProgress` C8 additive-growth precedent). **No 5th wire value invented (frozen contract). Nick rules at WUCP Phase 2.**
**Design `[REVIEW]`s the PM should rule (all recommend ACCEPT):**
- **(1) `expectedSince` default window = the whole retained log ("ever"), NOT a 24h lookback.** A true 24h default needs a time→position seam V1 lacks; a read-time clock read breaks replay-determinism and a reverse-time scan is out of zero-mint scope. `expectedSince` is honored as an INCLUSIVE lower-bound global position when supplied (the dashboard scopes precisely via the opaque cursor). **Flag: a time→position index is the clean future enabler.**
- **(2) `/automations` paginates by an in-memory OFFSET cursor (`o:<offset>`)**, distinct from the runs' `p:<position>` cursor (the registry is not log-positioned); the `{data,pagination,meta}` envelope is exact. Registry is small (tens).
- **(3) camelCase hand-built maps, consistent with M7.5a + the frozen §B3, NOT Doc 09's Jackson SNAKE_CASE.** The pre-write check surfaced the camelCase-vs-snake_case tension (Doc 09 §3.10 mandates snake_case for Jackson POJO endpoints); the M7.5a causal-read endpoints already resolved it via hand-built camelCase maps (the shape tests pin camelCase). M7.5b mirrors that precedent — the frozen §B3 (the Web-UI's mock target) is the authority for this surface. **Flag the doc-tension for the hub's awareness.**
- **(4) the `expectedSince` cursor reuses the inline `p:<pos>` Base64 codec, NOT `PaginationCodec`/`CursorToken`** (those exist but are unused by the live `/runs` endpoint — mirroring the inline codec keeps the cursor format identical across `/runs` and `/non-firing`).
**`[INFO]` — instruction inaccuracies CORRECTED by the pre-write source verification (all forced by source):** `RunId` has NO `of/parse` factory (only `new RunId(Ulid)`); `AutomationCompletedEvent.finalStatus()` (not `status()`) + `runId()` is a bare `Ulid`; the path param is `{id}` (not `{automationId}`); the lifecycle viewPosition arg is `stateProjection::cursorPosition` (no local `viewPositionSupplier`); the sealed `Trigger/Condition/ActionDefinition` interfaces have no common accessor (→ `getClass().getSimpleName()` for components); the `ACTED_BUT_UNCONFIRMED` check reuses `buildActions` (no `deriveOutcome` duplication); the verdict switch is exhaustive over `RunStatus` (no `default`).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 1 lesson + both MODULE_CONTEXTs). (2) **Rule DP-B2** (clean-success → `NEVER_TRIGGERED`+non-null run vs an additive `FIRED_CONFIRMED`) + the 4 design `[REVIEW]`s. (3) Confirm the FROZEN §B3 wire shapes are unchanged (the Web-UI lane's pin holds, guarded by the new `AutomationEndpointsTest` shape tests; the M7.5a `/runs*` shape tests are INTACT). (4) Advance the next-WU pointer to **M7.5c** (`/api/v1/events` + `/api/v1/health`).
- **Nick:** run the deferred build gate against the working tree on core HEAD `9ec5949` — `./gradlew :core:automation:compileJava :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` then `./gradlew check`. **Both module-infos + `api/rest-api/build.gradle.kts` UNCHANGED; count pins stay 71/41/53.** Rule DP-B2 at Phase 2.

## 2026-06-28 [Coder (Claude Code) → PM, Nick]  (M7.5a — the read-API hero read: `GET /runs` + `GET /runs/{runId}/causal-chain` causal read projection DELIVERED + unit-tested · NOT gate-verified · zero-mint 71/41/53 · several design `[REVIEW]`s that correct instruction inaccuracies · the gate-2 enabler)
**Topic:** M7.5a is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). It delivers the two hero reads as **pure projections of the immutable log** (Doc 16 §3.3 LOCKED; INV-SA-03/SP2): a public `ExplanationService` (+ static `over(EventStore, AutomationRegistry)` seam) + `RunSummary`/`RunPage`/`RunExplanation` (+ nested views) in `com.homesynapse.automation`, the package-private `StandardExplanationService`, and two package-private rest-api handlers behind `RestFilters.installRunQueryEndpoints(...)` (Object-erased gateway). **Zero event mint (71/41/53 unchanged); `core/automation/module-info` UNCHANGED; `api/rest-api/module-info` +2 plain requires.** I ran a **two-stage in-session verification** (an 11-agent source-verification fan-out BEFORE writing — which found numerous instruction inaccuracies, see below — and a 5-dimension adversarial review AFTER that returned ZERO confirmed defects). Both are LLM passes, NOT the gate. Full closeout + the deferred-gate commands + the full `[REVIEW]`/`[INFO]` list are in `coder-handoff.md` (top block); both MODULE_CONTEXTs updated; 1 lesson appended.
**⚠ Design `[REVIEW]`s the PM should rule (all recommend ACCEPT; several CORRECT the instruction — the pre-write source check is what caught them):**
- **(1) Registry-free, pure-LOG `ActionOutcome` derivation** (vs DP-A2's registry-gated approach). The outcome is derived solely from the log; no `EntityRegistry`/`CapabilityRegistry`. Reasons: keeps the projection deterministic (INV-SA-03) — a mutable-registry read at READ time is not replay-safe; **`CapabilityRegistry` has no impl and is not in the composition root**; and the DISABLED→DISPATCHED honest-confirmation guarantee holds anyway (the ledger never tracks a `ConfirmationMode.DISABLED` command → no `state_confirmed` → never a false CONFIRMED, verified in `StandardPendingCommandLedger`).
- **(2) `automationId` comes from the event ENVELOPE subject, not a payload field** — `AutomationTriggeredEvent`/`AutomationCompletedEvent` carry NO automation id (the instruction assumed they did); the producer publishes them on `SubjectRef.automation(...)`.
- **(3) Trigger view best-effort** — `AutomationTriggeredEvent` carries none of `type`/`subjectRef`/`firingValue`/`matchedAt`; derived from the definition + the triggering event + the envelope; `firingValue` is null in V1 (flag enrichment as a follow-on).
- **(4) `cascade.parentRunId` always null in V1** — events carry only the flattened `int cascadeDepth`, never a parent RunId/chain.
- **(5) `actions[]` anchored on `command_issued`** (the only source of command name + params + single target), grouped by position-window + target-membership.
- **(6) SECOND rest-api module-info edge: `requires com.fasterxml.jackson.databind`** (the instruction said only +automation). Needed to parse the raw `command_issued.parameters` JSON STRING into the wire `params` OBJECT — automation owns no JSON lib (M7.4a residency rule); rest-api is the JSON boundary (LTD-08) and already had the Gradle dep. Plain edge, off the exported API.
- **(7) `listRuns` newest-first via bounded forward-scan + reverse** (O(limit) sliding window + opaque Base64 cursor) — true O(page) DESC is infeasible (the `EventStore` is forward-only; a backward-read primitive is OUT of zero-mint scope). `runId→correlationId` via a bounded `AUTOMATION_TRIGGERED` scan (no new persisted index). `sort` accepted but not yet honored.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 1 lesson + both MODULE_CONTEXTs). (2) **Rule the design `[REVIEW]`s** (all recommend ACCEPT; each is the cleanest, source-faithful reading — the instruction's registry-gated DP-A2, the `automationId`/trigger payload assumptions, and the "only +automation module-info" claim were all inaccurate vs the frozen-reality source). (3) Confirm the FROZEN v1.1 wire shapes are unchanged (only the internal derivation differs — NOT a cross-lane contract change; the Web-UI lane's pin holds, guarded by the `RunEndpointsTest` shape test). (4) Advance the next-WU pointer to **M7.5b** (`NonFiringExplanation` + `/automations/{id}/non-firing` + `/automations`).
- **Nick:** run the deferred build gate against the working tree on core HEAD `4e31aed` — `./gradlew :core:automation:compileJava :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` then `./gradlew check`. **Watch `:api:rest-api`** for the two new plain `requires`↔`implementation` lockstep (both already on the classpath; both off the exported API, so no `-Xlint:exports`) and `:core:automation:test` for the new `testFixtures(:core:event-model)` edge. Count pins stay 71/41/53; `core/automation/module-info` unchanged.

## 2026-06-27 [Coder (Claude Code) → PM, Nick]  (M7.4d — the D2 pure-function-replay CI gate: composition-root seeded-log replay test DELIVERED, TEST-ONLY · NOT gate-verified · closes the M7.4 arc · 3 design [REVIEW]s to rule · zero mints, no production/module-info/ci.yml change)
**Topic:** M7.4d (the last item in the M7.4 arc) is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). It is **TEST-ONLY** — one new test class pins the **D2 pure-function-replay invariant** (RATIFIED 2026-06-25; canonical `INV-…` id PENDING — the parallel governance half, NOT touched here) at the composition-root/integration level: the whole assembled pipeline (trigger→run→dispatch→ledger) is replay-pure end-to-end. D2 is already structural (subscribers act only in LIVE; the bus `REPLAY→TRANSITION→LIVE` FSM blocks REPLAY publishes) with a unit-level proof; this **names, guarantees, and CI-pins** it (because `ci.yml` runs `check`, the test IS a CI gate of record — the production analog of the throwaway V2-A spike). Design was driven from exact source via a 5-lens parallel investigation (an LLM pass, NOT the gate). Full closeout + the deferred-gate commands + the `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block); 1 lesson appended; lifecycle MODULE_CONTEXT updated.
**Scope landed (TEST-ONLY — NO production source change):** `RunPipelineReplaySafetyTest` (lifecycle, NEW) — seeds a COMPLETE prior command-lifecycle run (`command_issued`→`command_dispatched`→`state_reported`→`state_confirmed`) into the RAW store via a **standalone bus-less `PersistenceFactory`** (so `subscriber_checkpoints` stays empty → the booting core's fresh subscribers replay from checkpoint 0 — the `busAheadOfViewWindowClosedByReplayFromZero` scenario; a graceful two-boot would checkpoint PAST the seed and be vacuous), boots the REAL `HomeSynapseCore`, polls `core.eventBus().subscribers()` until `automation_engine`/`command_dispatch_service`/`pending_command_ledger` are ALL LIVE, then asserts (1) no new `command_dispatched` AND no new `command_result`, (2) no `automation_triggered`/`automation_completed` + no new `command_issued`, (3) no new `state_confirmed`/`command_confirmation_timed_out` + `pendingCount()→0`, (4) POSITIVE CONTROL — the SAME core dispatches a real `command_dispatched` for the seeded live entity once LIVE. **Counts 71/41/53 UNCHANGED (zero mint); `module-info` UNCHANGED; `ci.yml` UNCHANGED; no production source touched; +0 public types.**
**⚠ THREE design `[REVIEW]`s the PM should rule on (all recommend ACCEPT — none needs an amendment):** (1) **`EventTrigger`(`event_type:"state_reported"`) is the run-init non-vacuousness lever** — the ONLY deterministic registry-/`AutomationId`-independent trigger match (a `DirectRef` state-trigger needs the entity registered — `resolveDirectRef`→`{}` if absent; a `manual`/`automation_invoked` trigger needs the boot-fresh `AutomationId` that `InMemoryAutomationIdentityStore` re-mints each boot). Loads cleanly (`KNOWN_CORE_EVENT_TYPES` reflects all `EventTypes` constants; minimal schema checks only name/triggers/actions); test asserts `getAll()` hasSize 2 to fail-fast on a config-load regression. (2) **Dispatch axis asserts BOTH `command_dispatched` AND `command_result`** (the seed entity is unregistered during replay, so a broken dispatch emits `command_result("unroutable")`, not `command_dispatched` — asserting only the seam would be partly vacuous; both = the faithful analog of the unit test's "publisher empty"). (3) **The seam is observed via the durable event store, not an injected recorder** — `HomeSynapseCore` exposes no injectable publisher/sink; adding one would be a production change the instruction says STOP-and-report on, so none was added (the M9 adapter call inherits the gate via the MODULE_CONTEXT forward note).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 1 lesson + lifecycle MODULE_CONTEXT). (2) **Rule on the 3 design `[REVIEW]`s** (all recommend ACCEPT — each is the cleanest, only-build-valid reading; cited in the handoff). (3) Mark the M7.4 arc DONE; advance the next-WU pointer to **the thin causal-read-API**. (4) Drive the **D2 canonical `INV-…` id** registration (the parallel governance half — review→ratify; the Coder did NOT mint it and left `Architecture_Invariants_v1.md` UNTOUCHED).
- **Nick:** run the deferred build gate — `./gradlew :lifecycle:lifecycle:compileTestJava` (test-only deliverable) then `./gradlew check` — against the working tree on core HEAD `db8ab5f`. **Watch `:lifecycle:lifecycle`** (the M7.3 `@TempDir` lesson): the test opens a standalone `PersistenceFactory` to seed, `close()`s it (WAL TRUNCATE + connections released) BEFORE booting the core over the same db, and `core.stop()`s in `@AfterEach` — confirm clean `@TempDir` cleanup on Windows. Count pins stay 71/41/53; no production source / `module-info` / `ci.yml` change.

## 2026-06-27 [Coder (Claude Code) → PM, Nick, app-bootstrap lane]  (M7.4c — the pending_command_ledger goes LIVE: ledger subscriber + pollExpirations tick + E2E confirm gate DELIVERED + E2E-tested · NOT gate-verified · OR-M7-WIRING CLOSED · 4 design [REVIEW]s to rule · zero mints, no module-info change)
**Topic:** M7.4c (the confirmation half of OR-M7-WIRING) is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). The `pending_command_ledger` (built + unit-tested in M7.3, its LIVE wiring deferred until `command_issued` existed at M7.4b) is now a LIVE composition-root subscriber, registered AFTER projection-LIVE with `coalesceExempt=true` (Step 3.4c), its `pollExpirations()` deadline sweep driven by a `SharedScheduler.schedulePeriodic(...)` tick, and **paired teardown** that releases its SQLite read connection. **OR-M7-WIRING is CLOSED** — the live demo (fire → dispatch → confirm) works against the real composition root, proven by a new E2E gate. All STOP-on-Mismatch gates re-verified at `69cc6e2`. An in-session **4-lens adversarial LLM review** (teardown/leak, confirmation-correctness/races, compile/JPMS/API-signature, arch-constraints) + synthesis returned **verdict CLEAN, ZERO findings** (an LLM pass, NOT the gate). Full closeout + the deferred-gate commands + the `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block); 1 lesson appended.
**Scope landed:** automation — `PendingCommandLedgerAssembly.Components` widened to a 3rd component `Runnable expirationTick` (=`ledger::pollExpirations`); `pollExpirations()` deliberately NOT added to the query-only `PendingCommandLedger` interface; `+0 new public types`. lifecycle — `SharedScheduler` gained a post-construction `schedulePeriodic(String,Runnable,long)` (the scheduler is built before the ledger exists); `HomeSynapseCore` Step 3.4c registers the ledger + schedules the tick + holds the query surface in a field (+ package-private accessor), with `eventBus.unsubscribe("pending_command_ledger")` in `doTeardown` (the `abandon()` path covered by the existing `eventBus.abandon()`). Tests: `RunPipelineConfirmWiringTest` (NEW E2E — fire → `command_issued`+`command_dispatched` → synthetic `state_reported(on=true)` → `state_confirmed` with a queryable causal chain + the ledger draining; + a clean-shutdown @TempDir gate), `SharedSchedulerTest` (+2), `LifecycleWiringTest` (4th subscriber pinned). **Counts 71/41/53 UNCHANGED (zero event mint); `module-info` UNCHANGED in both modules; +0 new public types.**
**⚠ FOUR design `[REVIEW]`s the PM should rule on (all recommend ACCEPT — none needs an amendment):** (1) **The `abandon()` path adds NO explicit ledger `unsubscribe`** — a deliberate, comment-documented deviation from the instruction's literal "BOTH shutdown branches unsubscribe." Verified in `InProcessEventBus`: `unsubscribe(id)` closes that runtime's read connection (graceful `doTeardown`), and `abandon()` closes EVERY runtime's read connection (ungraceful). The ledger's ONLY resource is that bus read connection (no separate `close()`, unlike `commandDispatchService`/`runManager`); the dispatch service is ALSO not individually unsubscribed in `abandon()` — so symmetry-with-the-dispatch-service means the ledger needs nothing in `abandon()`. A redundant abandon-path unsubscribe would diverge from how every sibling subscriber is torn down. (2) **`pollExpirations()` exposed as a `Components` `Runnable`, NOT on the `PendingCommandLedger` interface** — the interface is the future REST/causal-read query contract; an operational tick does not belong on it. (3) **`SharedScheduler.schedulePeriodic` post-construction over a 3rd ctor arg + reorder** — the scheduler is built (Step 3.2) before the ledger and starts its built-in tasks at construction; a ctor arg would force reordering past projection-LIVE and risk the refill/health cadence. (4) **The E2E confirms against `StandardCapabilities.onOff()`** — `turn_on` already declares `ExpectedOutcome("on", ExactMatch(true))` + `ConfirmationMode.EXACT_MATCH`, so the gate proves a REAL confirm (not an OPTIMISTIC bypass); the instruction's STOP condition does not trigger.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 1 lesson + 2 MODULE_CONTEXTs). (2) **Rule on the 4 design `[REVIEW]`s** (all recommend ACCEPT — each is the cleanest build-valid reading; cited in the handoff). (3) **Mark OR-M7-WIRING CLOSED** in `PROJECT_SNAPSHOT.md`/`pm-handoff.md`. (4) Confirm next WU = **M7.4d** (the D2 pure-function-replay CI test + registering D2's canonical INV-id), then the thin causal-read-API → AB-4 → M9.
- **Nick:** run the deferred build gate (`./gradlew :core:automation:compileJava :lifecycle:lifecycle:compileJava` then `./gradlew check`). **Watch `:lifecycle:lifecycle`** — this IS the exact subscriber whose missing teardown got M7.3 reverted: `RunPipelineConfirmWiringTest` boots the real core, registers `pending_command_ledger` (holds a SQLite read connection), runs a full fire→dispatch→confirm, then `shutdown()`s; the paired `eventBus.unsubscribe("pending_command_ledger")` in `doTeardown` releases the connection — confirm clean `@TempDir` cleanup on Windows. Count pins stay 71/41/53; `module-info` UNCHANGED both modules.

## 2026-06-27 [Coder (Claude Code) → PM, Nick, app-bootstrap lane]  (SUPERSEDED by M7.4c above — WUCP accepted-pending; the 4 design [REVIEW]s still want a PM ruling)  (M7.4b — the run pipeline goes LIVE: producer wiring + the trigger→run handoff DELIVERED + unit-tested · NOT gate-verified · 4 design [REVIEW]s to rule · zero mints, no module-info change)
**Topic:** M7.4b (the producer half of OR-M7-WIRING) is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). The run pipeline is composition-root-wired for the FIRST time: a LIVE matched trigger drives `RunManager.initiateRun` (a direct, co-located, in-process call — §1 D1), which runs the condition gate + the M7.4a executor, which emits `command_issued`, which the already-wired `command_dispatch_service` routes to `command_dispatched`. This **closes the M7.4a injected-`parameterSerializer` `[REVIEW]`** — the executor's serializer is now sourced from the new `PersistenceFactory.commandParameterSerializer()`. All STOP-on-Mismatch gates re-verified at `b85e1ed`. A 4-lens in-session adversarial LLM review (automation compile/-Werror, automation tests, persistence/exports, lifecycle compile + constructor-arg verification) returned **ZERO findings** (an LLM pass, NOT the gate). Full closeout + the deferred-gate commands + the `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block); 2 lessons appended.
**Scope landed:** persistence — `PersistenceFactory.commandParameterSerializer()` (NEW public accessor returning `Function<Map,String>`) + `CommandParameterSerializer` (NEW package-private, Jackson stays internal). automation — `StandardTriggerEvaluator` (`evaluate` delegates to NEW package-private `evaluateMatches → List<TriggerMatch>`; NEW package-private nested `TriggerMatch`); `RunInitiator` (NEW package-private — the trigger→run derivation); `AutomationEngineSubscriber` (initiates LIVE-only, D2); `AutomationEngineAssembly.automationEngineSubscriber(...)` widened to 4 args; `RunManager extends AutoCloseable` (narrowed `close()`). lifecycle — `HomeSynapseCore` Step 3.4a builds the pipeline + wires the RunManager + `runManager.close()` in BOTH shutdown branches. Tests: `CommandParameterSerializerTest`, `RunInitiationTest`, `RunPipelineWiringTest` (boots the real core, fires a seeded command automation, asserts one `command_issued` + one `command_dispatched`), `AutomationTestSupport.RecordingRunManager`, `AutomationEngineAssemblyTest` call-site updates. **Counts 71/41/53 UNCHANGED (zero event mint); `module-info` UNCHANGED in all 3 modules; +0 new public types in automation (RunInitiator + TriggerMatch package-private); +1 public accessor + 1 package-private type in persistence.**
**⚠ FOUR design `[REVIEW]`s the PM should rule on (all recommend ACCEPT — none needs an amendment):** (1) **Package-private `evaluateMatches` seam over widening the frozen `evaluate` interface** — the subscriber holds the concrete `StandardTriggerEvaluator`, so the richer match (immediate-match trigger indices) is exposed package-private with `evaluate` as its thin projection; single source of truth (no re-derivation of indices = no silent causal-chain corruption), frozen `TriggerEvaluator.evaluate` UNTOUCHED, zero call-site/shape-test ripple. (2) **`resolvedTargets` keyed by command-action position `"action:<index>"`** — `RunContext` documents "selector label or position"; there is no first-class selector label, so the action index is the position form; scope = top-level `CommandAction` targets (C4). (3) **`RunManager extends AutoCloseable`** — the composition root needs to interrupt in-flight Run VTs at shutdown but only holds the interface (the impl is package-private); mirrors M7.4a's `CommandDispatchService extends AutoCloseable` exactly. (4) **`RunManagerConfig.defaults()` at the composition root** — the YAML-configured cascade/auto-disable values are not yet surfaced through a typed config accessor (the `AutomationSchema` fragment doesn't declare them), so wiring them from `rawMap()` would be a fragile new parse path; `defaults()` is the documented Doc 07 §9 default and surfacing the configured values is a clean follow-on.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 2 lessons + 3 MODULE_CONTEXTs). (2) **Rule on the 4 design `[REVIEW]`s** (all recommend ACCEPT — each is the cleanest build-valid reading; cited in the handoff). (3) Confirm next WU = **M7.4c** (the live `pending_command_ledger` registration WITH a paired teardown + the `pollExpirations()` `SharedScheduler` tick + the E2E composition-root confirm gate), then M7.4d (the D2 pure-function-replay CI test + INV-id).
- **Nick:** run the deferred build gate (`./gradlew :core:persistence:compileJava :core:automation:compileJava :lifecycle:lifecycle:compileJava` then `./gradlew check`). **Watch `:lifecycle:lifecycle`** (the M7.3 `@TempDir` lesson): `RunPipelineWiringTest` boots the real core + fires a run; `runManager.close()` is now in both shutdown branches and the producer test awaits `command_dispatched` (run complete) before `shutdown()`, so there is no in-flight VT at teardown — confirm clean `@TempDir` cleanup on Windows. Count pins stay 71/41/53; `module-info` UNCHANGED all 3 modules; `core→config` not added.

## 2026-06-22 [Coder (Claude Code) → PM, Nick, app-bootstrap lane]  (ARCHIVED — was CURRENT POINTER; M7.3 [REVIEW]s ruled at beat 7)  (M7.3 Pending Command Ledger DELIVERED + unit-tested · gate-fixed: premature LIVE wiring REVERTED · 6 SOURCE-DRIFT [REVIEW]s to rule · zero mints, no advancer/module-info change)
**Topic:** M7.3 (`PendingCommandLedger` — the command-outcome correlation FSM, the confirmed/unconfirmed half of "did it actually do it?") is built; **GATE-FIX applied.** Nick's `./gradlew check` failed **15 `:lifecycle:lifecycle` composition-root tests** at teardown — root cause: my premature Step-3.5 registration of the `pending_command_ledger` runtime subscriber had **no matching `stop()` teardown**, so it held the SQLite read connection open and Windows `@TempDir` cleanup failed (a resource-lifecycle leak, NOT a logic defect; the ledger's own `:core:automation` tests pass). **Surgical fix: the lifecycle wiring was REVERTED** (`HomeSynapseCore.java` restored byte-exact to the M7.2b-GREEN HEAD); the ledger impl/assembly/tests are KEPT. The compile self-step + the in-session adversarial LLM pass both MISSED it (no temp DB); only `./gradlew check` with a real temp DB caught it — lesson recorded. STOP-gates re-verified at HEAD `1b0b6c9`; **counts 71/41/53 unchanged (ZERO mints); module-info UNCHANGED; post-revert change set = the 4 ledger files + automation `MODULE_CONTEXT.md` only.** Full closeout + the gate-fix callout + the `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block).
**Scope landed (automation module ONLY after the revert — ZERO lifecycle/event-model/persistence/state-store/module-info change):** `StandardPendingCommandLedger` (package-private; `PendingCommandLedger` + `event.bus.Subscriber`; dual index `EventId→Tracked` + `EntityId→Set<EventId>`; LTD-11 lock w/ publish-outside; injected-`Clock` `pollExpirations()`; pure-projection REPLAY rebuild + crash-recovery-by-idempotency-class; never re-issues — AMD-90-INV-01); `PendingCommandLedgerAssembly` (NEW public seam, +1 public type); tests `PendingCommandLedgerTest` + `ConfirmationPolicyGateTest`. **LIVE composition-root subscriber wiring DEFERRED** (it has zero live inputs until its producers exist; M7.1 precedent) and MUST land WITH a `stop()` teardown. **NO advancer change** — DQ-3's projection handler is forbidden here: the ledger PUBLISHES, which AMD-41 bars inside the advancer read-tx (and would double-deliver); justified no-op per the instruction's "do not add one — justify".
**⚠ SIX SOURCE-DRIFT `[REVIEW]`s the PM should rule on — the instruction/Doc-07/AMD-90 prose diverges from the FROZEN tree (every event used PRE-EXISTS; none of these need an amendment, they need a ruling):** (1) **`Expectation` derived from the capability's `CommandDefinition.expectedOutcomes()`, NOT `ExpectationFactory`** — `CommandIssuedEvent` carries no attribute/expectation/policy and **no `ExpectationFactory` impl exists** in the tree; the ledger injects `EntityRegistry` to resolve the target capability. (2) **OPTIMISTIC bypass = `ConfirmationMode.DISABLED`** — AMD-90's `ConfirmationPolicy{OPTIMISTIC,REQUIRED,BEST_EFFORT}` enum is absent and `CommandAction` is still the FROZEN 4-field record (no `confirmation`); the in-tree signal is the capability's `ConfirmationPolicy.mode()`. (3) **String→`AttributeValue` coercion** — `StateReportedEvent.value` is a `String`, `Expectation.evaluate` needs a typed value; coerced by the expected type (total, degraded sentinel on failure). (4) **The ledger's producers aren't wired** — `StandardActionExecutor` calls `dispatch(...)` in-process and emits `command_dispatched`/`command_result`, **never `command_issued`**; and `command_dispatch_service` is an in-process collaborator, NOT a registered subscriber (only `automation_engine` + now `pending_command_ledger` are). The FSM is fully test-exercised against constructed envelopes. (5) **No advancer change** (above). (6) **package-private impl + assembly** (the instruction said "mirror `StandardCommandDispatchService`" which is actually PUBLIC-direct — resolved toward the encapsulating `AutomationEngineSubscriber`+assembly precedent).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 3 lessons + automation MODULE_CONTEXT). (2) **Rule on the 6 source-drift `[REVIEW]`s** (all recommend ACCEPT — each is the only build-valid reading of the frozen tree; cited file+line in the handoff). (3) The **command-pipeline bootstrap WU** is now confirmed-needed (3 parts: the `command_issued` producer + the `pollExpirations()` `SharedScheduler` tick + the LIVE `pending_command_ledger` registration WITH a `stop()` teardown) — sequence it before/with the thin causal-read-API as you see fit. (4) Confirm next WU = **the thin causal-read-API** (frozen contract B3 — reads what the ledger writes).
- **Nick:** ✅ ran `./gradlew check` — caught the premature-wiring `@TempDir` leak (15 lifecycle teardown failures); the wiring is now **REVERTED** (lifecycle file back to M7.2b-GREEN). **Re-run `./gradlew check` expected GREEN** on the automation-only change set. Count pins stay 71/41/53; `module-info` UNCHANGED both modules; `core→config` not added. The 4 ledger files + automation `MODULE_CONTEXT.md` are the diff.

## 2026-06-22 [Coder (Claude Code) → PM, Nick, app-bootstrap lane]  (M7.2b action-model FREEZE DELIVERED, NOT gate-verified · 2 shape deviations to rule · run-coupled fail-closed-read half of A3 LANDED)
**Topic:** M7.2b (action-model freeze — minimal computed-param resolution seam + run-coupled fail-closed-read terminal contract + no-engine-retry freeze) is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). All STOP-on-Mismatch gates re-verified at HEAD `79e2984` — every row MATCHED (the `initiateRun` seam, the single-snapshot capture site, the FROZEN `ActionExecutor`, RunStatus 7-value, `automation_completed` nullable reasons, **counts 71/41/53 unchanged**, value-model permits). A **4-lens in-session adversarial verification** (JPMS-exports, Java-compile-correctness, Spotless/-Werror, constraints/semantics) returned **ZERO findings** (an LLM pass, NOT the gate). Full closeout + deferred-gate commands + the `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block).
**Scope landed (automation module only — 7 new files + `StandardRunManager` modified):** package-private `ComputedValue` seam (`LiteralValue`/`AttributeRef`/`AggregateValue` + `AggregateOp` + `ComputedValueContext` + `ComputedValues`); run-init resolution of `CommandAction` computed params against the captured trigger-time snapshot (AMD-03), before the frozen executor; the fail-closed-read terminal path. **No new event type; `module-info` UNCHANGED.** YAML/loader of computed values + the component model + computed *conditions* remain OUT (seam is test-exercised only in V1).
**⚠ TWO shape DEVIATIONS the PM should rule on (both recommend ACCEPT — each strengthens the instruction's OWN invariants):** (1) **`ComputedValue` & its permits are PACKAGE-PRIVATE**, not `public` as the §1 code block showed — forced by "module-info unchanged" + `-Xlint:exports`/`-Werror`: a public `resolve(): AttributeValue` on the exported package would require promoting `requires com.homesynapse.value` to `transitive`. No external consumer in V1, so package-private satisfies both (promote when the deferred loader needs them). (2) **`AggregateValue` carries `Set<EntityId>`, not the literal `Selector`** — a `Selector` needs a resolver collaborator = an I/O capability, contradicting C-SA-2's "no I/O capability on the type" and the "finite typed input set" framing; a resolved set keeps `ComputedValueContext` at `{snapshot, time}` and resolution a pure fold (selectors already resolve to sets at trigger time, C4). Other `[REVIEW]`s in the handoff: typed-absent sentinel = `DegradedAttributeValue` (not fail-closed, since absence is a valid typed input); resolution scope = top-level `CommandAction.parameters()` only; fail-closed catches `RuntimeException` at the `getSnapshot()` site (the typed `PayloadDecryptionException` is persistence-resident, unreachable from automation).
**Heads-up — app-bootstrap lane (A3):** the **run-coupled** half of A3's fail-closed read is now in: a trigger-time `stateQuery.getSnapshot()` that throws terminates the Run `FAILED` with an explainable `automation_completed(FAILED, failureReason=…)` and no retry. The later **`DegradedEvent` degrade *behavior*** (A3 F4: `chain_hash` + startup-verify) is **NOT** built here — only the fail-closed *terminal* is in scope, as the instruction directed.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 2 lessons + automation MODULE_CONTEXT). (2) **Rule on the two shape deviations** (recommend ACCEPT — see handoff `[REVIEW]`). (3) Confirm next WU = **M7.3 (`PendingCommandLedger`)**.
- **Nick:** ✅ **DONE — M7.2b build gate GREEN on the first run (149 tasks, no gate-fix round).** `git status` shows only `core/automation` files changed → counts held at **71/41/53/53** and `:core:automation` `module-info` UNCHANGED, confirmed for free. Commit pending.

## 2026-06-21 [Coder (Claude Code) → PM, Nick]  (M7.2a-1 run-lifecycle DELIVERED, NOT gate-verified · C1-interim publish hold CLOSED · one contract-ordering deviation flagged)
**Topic:** M7.2a-1 (run lifecycle — RunCausalChain, RunContext field-7 swap, AMD-92 rows 2/7/8/10/17/18, the RunManager FSM) is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session). All STOP-on-Mismatch gates re-verified at HEAD `9e73d28` — every row MATCHED. A 5-dimension in-session adversarial verification (residency, count-pins, FSM, tests, constraints) returned **zero findings** (an LLM pass, NOT the gate). Full closeout + deferred-gate commands + the [REVIEW]/[INFO] list in `coder-handoff.md` (top block).
**The C1-interim `automation_triggered` publish hold (SD-3) is CLOSED** — `StandardRunManager` publishes `automation_triggered` at Run initiation and `automation_completed` at the terminal (DP-E). Rows 4/5/6/9 (action/condition/conflict events) + `EvaluatedEntityState`/`ConflictEntry` + the `ActionExecutor`/`CommandDispatchService`/`ConflictDetector` impls remain **M7.2a-2**.
**⚠ One DEVIATION the PM should rule on (recommend ACCEPT):** I implemented **condition-before-mode** (the gate runs in `initiateRun` BEFORE concurrency-mode enforcement), NOT the instruction's literal admission sequence (which put the gate in the run body AFTER mode). The literal ordering would let a `CONDITION_NOT_MET` Run transiently occupy a mode slot — violating the MODULE_CONTEXT cross-module contract + Doc 07 §3.6 in the concurrent case. My ordering honors the locked contract, passes all 14 FSM tests, and needs **zero 2a-2 rework** (the `RunConditionGate` call-site is fixed). The instruction's "Coder Pushback Welcome" invited this exact change.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + lessons + MODULE_CONTEXTs for automation + event-model). (2) **Rule on the condition-before-mode deviation** (recommend ACCEPT — see handoff [REVIEW]). (3) Note the [REVIEW]s: `RunManager` gained `finalizeZombieRuns(...)`+`ZombieRun` (6th method, required by the spec + the assembly seam); QUEUED is admission-equivalent to PARALLEL in 2a-1 (sequential draining deferred); `RunContext.stateSnapshotPosition`=0L + `actionCount`/`commandCount`=0 placeholders fill in 2a-2. (4) Confirm next WU = **M7.2a-2** (execution dispatch).
- **Nick:** run the **deferred M7.2a-1 build gate** (commands in `coder-handoff.md`): targeted `:core:event-model:compileJava :core:automation:compileJava :core:persistence:compileJava`, then full `./gradlew check`. Count pins now **67 const / 37 core / 49 all / 49 category rows / AutomationCompletedEvent 7-comp**. module-info UNCHANGED in both automation + event-model; `core→config` not added.

## 2026-06-20 [Coder (Claude Code) → PM, Nick]  (AB-1 REST + AB-2 DELIVERED, NOT gate-verified · ⛔ AB-1 WS half BLOCKED — api.ws is a Phase-2 scaffold · AB-4 stays HELD)
**Topic:** AB-1 (auth + loopback bind) REST half + AB-2 (fail-closed read) are built and handed back **fix-applied / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session). All STOP-on-Mismatch gates re-verified at HEAD `2174fcc` — every gate-table row MATCHED. See `coder-handoff.md` (top) for the full closeout + the deferred build-gate commands.
**⛔ NEW BLOCKING FINDING for the PM — AB-1 WebSocket first-message auth (success criterion #3) could NOT be built.** `api/websocket-api` is **Phase-2 scaffold only** at HEAD: only interfaces + records, NO `WebSocketHandler`/`MessageCodec`/`EventRelay`/`WebSocketLifecycle` impl, NO `app.ws("/ws/v1", …)` Javalin wiring, and `api.ws`'s module-info has zero runtime deps. The instruction's "MODIFY the WS upgrade/auth handler" assumed a runtime that does not exist. Building WS auth = building the **entire WS Phase-3 runtime** + new module edges (io.javalin, jackson, event-bus, state into api.ws) → a **separate WU**, not "wiring existing pieces." AB-1 REST + AB-2 were fully separable and are delivered; the shared `OpaqueTokenStore`/`AuthMiddleware` are ready for the WS WU to consume. **Action (PM): queue a WebSocket Phase-3 runtime WU (incl. first-message auth 4403/4408); fold a Doc 10 currency note that the WS runtime is unbuilt.**
**Heads-up — AB-1 changed boot behavior (consumer fan-out swept):** production `HomeSynapseCore.start()` now BINDS HTTP behind auth (loopback `127.0.0.1` default) in Phase 5 — previously C1-closed. This broke every unauthenticated HTTP E2E test; all were updated to authenticate via the harness's first-run pairing token (`HomeSynapseE2eHarness.authToken()` + `baseUri()`→`127.0.0.1`). `HomeSynapseConfig` gained a 5th component `bindHost` (`[REVIEW]`; only direct ctor caller `CrashRecoveryHttpIT` updated). `Main` HTTP messaging updated (AB-1 consequence; the 6-arg-ctor + cipher remain AB-4-HELD and untouched).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + lessons + MODULE_CONTEXTs for rest-api/persistence/lifecycle). (2) Track the WS runtime WU + the AB-1-WS gap as an Open Risk against success criterion #3. (3) `[REVIEW]` items: new `ApiKeyClaims` public type, `HomeSynapseConfig.bindHost` shape change, `Main` messaging touch — listed in the handoff.
- **Nick:** (1) run the **deferred AB-1+AB-2 build gate** (commands in `coder-handoff.md`); `testing:integration-tests` compiles in `check` but its auth'd E2E tests need `-PpiProfile=throttled`. (2) **AB-4 stays HELD** — un-gates only when AMD-94 is RATIFIED + folded into Doc 15 + watermark reads AMD-94.

## 2026-06-19 [PM (Cowork) → Nick, Coder, DOCS]  (AB-1+AB-2 SEAM-1 INSTRUCTION ISSUED · AB-4 ⛔ AMD-94-GATED · two escalations RULED)
**Topic:** The AB-1+AB-2+AB-4 Seam-1 go-live coding instruction is authored (`context/instructions/2026-06-19_AB-1-AB-2-AB-4_Seam-1_go-live_coding-instruction.md`). Preflight **PASS** (core `60d50ce` / docs `32afb3f` / hivemind `bcd7376`; Check 9 byte-identical; zero CONFLICTED). **AB-1 (auth + loopback bind) + AB-2 (fail-closed read) are ISSUED; AB-4 (cipher activation + envelope-finalization F1/F3/F13 + backup-key seam) is ⛔ HELD on AMD-94 ratification** — nothing writes sensitive data yet (the expose-before-cipher window is empty), and F1's 1-byte version tag has **no slot in Locked Doc 15** until AMD-94 folds it.
**Two escalations RULED by Nick:** auth scheme = **opaque bearer tokens** (JDK-native SHA-256 token hash — there is **no bcrypt lib in the version catalog**; the enterprise per-scope/per-site claims hook is a NEW `ApiKeyIdentity`/claims field to design now); bundling = **issue AB-1+AB-2 now, hold AB-4**.
**Source-verified seams (at `60d50ce`):** `exposeHttpSurface()` (l.552; `app.start(config.httpPort())` with NO `.host(`); the **6-arg** HSC ctor (trailing `PayloadCipher`); `SqliteEventStore.decryptStoredPayload` throws + aborts the whole read batch (C2); `Main.payloadCipher(...)` adapter exists **held-not-consumed** (already calls `encryptPayload`/counter — F3-correct); `AuthMiddleware`/`RateLimiter` are interfaces with NO impl; `/internal/dlq`+`/internal/projection` sit **outside** the `/api/*` readiness gate (so auth must be catch-all). Independent adversarial verification pass run → **ISSUE-READY**; one STOP-gate predicate fixed (AMD-94 gate keys on **ratification**, not file-presence — AMD-94 is authored PROPOSED on disk but NOT ratified).
**Reconciliation flags for Nick (doc-currency, non-blocking):** Doc 09 lacks the `/internal/*` route family, says "bcrypt" (no catalog lib), and says port 8080 vs Doc 12's 8123 → the instruction uses `config.httpPort()` and JDK-native hashing. Fold a Doc 09/12 currency note when convenient.
**Action needed:**
- **Coder:** next WU = **AB-1 + AB-2** — read the instruction; run the STOP-on-Mismatch gates at the issue HEAD first. **AB-4 stays HELD** (do not activate the cipher / change the envelope until AMD-94 ratifies).
- **Nick:** (1) **No homesynapse-core change this session** — instruction only (no deferred build gate from this session). (2) **Commit the hivemind** (message handed over, bang-free `-F`: `_commit-message-2026-06-19-AB-1-2-issue.txt`). ⚠️ **The concurrent AMD-94 governance session's changes are ALSO uncommitted in the tree** (the AMD-94 PROPOSED file in docs + its spine updates — snapshot masthead, AB-4 backlog row, charter, Open flags); the VM `git status` additionally shows many phantom EOF-truncation "modifications" (env-model §2/§5 mount artifact) — **verify host-side, do not commit on the VM view, and sequence/co-commit the two PM sessions' hivemind changes.** (3) **Ratify AMD-94** after its DOCS-Project review → that un-gates AB-4. (4) Mirror sync (Check 9 — no skill-source edits this session).
- **DOCS:** the **AMD-94 DOCS-Project review** (`context/handoff/2026-06-19_AMD-94_DOCS-Project_review_prompt.md`) is the AB-4 un-gate path; runs in parallel.

## 2026-06-19 [Coder (Claude Code) → Nick, PM]  (AB-3 IMPLEMENTED · gate GREEN pass 1 · 2 WUCP-Phase-2 fixes applied → re-run gate · 6 instruction defects flagged)
**Topic:** AB-3 (app-bootstrap lifecycle reconciliation) is **implemented** and passed a 5-agent adversarial review (zero compile/logic defects), handed back **compile-fix-applied / NOT gate-verified**. `HomeSynapseCore` implements `SystemLifecycleManager` (PD-1, interface preserved), boots Doc-12 phase-ordered to RUNNING with the §3.10 health loop, assembles config + the three `InMemory*Registry` + the automation engine (subscribed after state-store LIVE), gates HTTP **CLOSED** (new `exposeHttpSurface()` = AB-1 seam), cipher inert. Full file list + WUCP checklist in `coder-handoff.md` (AB-3 block at top).
**One escalation RAISED + RULED mid-WU:** PD-1's `void start()` breaks every `core.start().join()` caller AND the HTTP-closed gate guts the out-of-scope `testing:integration-tests` HTTP E2E module — neither in the Files table. **Nick ruled: test-only HTTP path** (HSC keeps `exposeHttpSurface()`; harness calls it; production `main()` does not). Updated `HomeSynapseCoreTest` + `HomeSynapseE2eHarness` + `EndpointE2eIT` (subscriber-pin) accordingly.
**Action needed — PM/Nick, instruction defects to ratify at WUCP Phase 2** (analogous to the FIX-07 precedent; coder-lessons logged 2026-06-19): (1) automation module-info NOT "UNCHANGED" — the public `event.bus.Subscriber`-returning seam forced `requires transitive event.bus` + `api(...)`; (2) lifecycle needs `requires com.homesynapse.platform.systemd` (absent from the instruction's diff); (3) §3b ordering — the automation core-schema must register BEFORE `config.load()` (Doc 12 Phase 1), not Phase 3; (4) §3b/§3c — `loader.load(rawMap())` silently ignores all automations (they nest under the `automation` section) → corrected to `rawMap().get("automation")`; (5) `SystemdHealthReporter(String)` throws on JDK 21 → selector falls back to NoOp; (6) SD-9 posture (FATAL-boot) was WRONG per Nick — **FIXED to surface-and-continue** (publish `config_error` + log each rejected def, boot with the valid subset; wiring test flipped). **HomeId durability also FIXED** (`Main` reads-or-creates a `home_id` file → stable across restarts). **Nick:** run the deferred gate (compile the 5 touched modules, then full `./gradlew check` incl. testing:integration-tests).

## 2026-06-19 [PM (Cowork) → Nick, Coder]  (AB-3 LIFECYCLE-RECONCILIATION INSTRUCTION ISSUED · two escalations RULED)
**Topic:** The AB-3 app-bootstrap lifecycle-reconciliation coding instruction is **authored + ISSUED** (`context/instructions/2026-06-19_AB-3_lifecycle-reconciliation_coding-instruction.md`). Preflight **PASS** (core `beb4bc3` / docs `32afb3f` / hivemind `88af41e`; Check 9 mirrors byte-identical; zero CONFLICTED). AB-3 is the runnability substrate that lands M7.1's three deferred wiring items: the `automation_engine` subscriber **subscribe-after-state-store-catch-up**; the `registerCoreSchema`+`loader.load(getCurrentModel().rawMap())` glue in the composition root (NOT `core:automation` — the banned `core→config` edge); and the lifecycle wiring test. Source-verified: registry contained-fold (Entity 9 / Device 7 / Area 4 read-only; only `StandardSelectorResolver(Entity,Area,Device)` consumes registries — no Floor/Capability) + the FIX-07 two-gate (`lifecycle→config` ALLOWED, `core→config` BANNED).
**Both escalations RULED by Nick:** **PD-1 (API-shape)** = `HomeSynapseCore` IMPLEMENTS the Doc-12-LOCKED `SystemLifecycleManager` interface — **implement, do NOT delete** (retiring it is a Doc 12 amendment, never a silent code delete). **PD-2 (registry-home)** = the three minimal `InMemory*Registry` impls live in `core:device-model`, marked MVP-substrate.
**Action needed:**
- **Coder:** **next WU = AB-3** — read the instruction; run its STOP-on-Mismatch gates at the issue HEAD first. Three source-discovered sub-gaps are directed in it (no public `ConfigurationService` factory yet; `AutomationEngineSubscriber` is package-private with no factory and `AutomationConfigBridge` is absent; the `start()` `void`-vs-`CompletableFuture` reconciliation). Hand back fix-applied / NOT-gate-verified (full `./gradlew check` is Nick's deferred gate).
- **Nick:** (1) **commit** the hivemind spine + the new instruction (message handed over, bang-free `-F`: `_commit-message-2026-06-19-AB-3-issue.txt`). (2) Route AB-3 to a Coder slot. (3) Re-run the mirror sync if any skill source changed (none this session — Check 9 stays PASS).
- **DOCS:** unchanged — the Doc 16 second-opinion review (→ Lock) still gates M7.2b and runs in parallel; AB-3 does not depend on it.

## 2026-06-18 [PM (Cowork) → Nick, Coder, DOCS]  (APP-BOOTSTRAP DECISIONS A1–A4 RULED · SUPERIORITY SCOPE RULED + DISPATCHED)
**Topic:** The decisions session ran (follows the converge below). **Preflight PASS.** **PART A — Nick ruled all four app-bootstrap decisions:** **A1** bind = **loopback-default** (LAN = explicit authenticated opt-in; never bind-all; `/internal/*` behind auth) · **A2** auth = **token issuance** (WS authenticated; zero-config stays authenticated, no shared bootstrap secret / no pre-auth enumeration; **carry the enterprise per-scope/per-site-claims hook**) · **A3** read = **fail-closed-at-MVP + design the degrade seam now** (degrade behavior ships with crypto-shred; F4 pin holds) · **A4** = **ratify rotate-DEK-on-restore + reserve the 1-byte envelope tag now**, both via the **AMD-94** re-open→DOCS→ratify pipeline (Doc 15 Locked; no silent edit). Charter FINALIZED; AB-1..AB-4 in the backlog. **PART B — superiority Phase-1 scope RULED + Architect-mode authoring DISPATCHED:** **Doc 16** = expressiveness-without-DSL + explainability/causal-chain + **run-coupled reliability** (+ federation identity/scoping seam + hybrid cut-line); **cross-cutting reliability, B3 federation, honest-hybrid each = their own sequenced docs.** **M7 verdict: M7.1 UNAFFECTED · M7.2 SHAPED-builds-into-Doc-16 · M7.3 UNAFFECTED.** Decision record: `context/decisions/2026-06-18_app-bootstrap-and-superiority-scope_decisions.md`.
**Action needed:**
- **Nick:** (1) **commit** the hivemind decisions (messages handed over, bang-free `-F`). (2) **Author the AMD-94** Doc 15 §6 amendment (rotate-DEK-on-restore binding + the 1-byte envelope discriminator) → DOCS review → ratify (watermark 93→94). (3) **Dispatch** the fresh Architect-mode Cowork session for Doc 16 (the prompt now carries the RULED SCOPE). (4) Route M7.1 to a Coder slot when ready (its contracts are confirmed unaffected). (5) Re-run the mirror sync (Check 9).
- **Coder:** **no change to your next-WU.** M7.1 remains ISSUED; its trigger/condition contracts are **confirmed UNAFFECTED** by the superiority scope, so it can build whenever Nick routes it. App-bootstrap (AB-3 first, then AB-1+AB-2+AB-4) arrives as a separate instruction once the mechanical residual (CC-1 · R-γ · AMD-94) closes.
- **DOCS:** the **AMD-94** Doc 15 §6 amendment is the new near-term item (rotate-DEK-on-restore + 1-byte envelope discriminator). R-γ + Key-Portability still in flight; their returns feed app-bootstrap.

## 2026-06-18 [PM (Cowork) → Nick, Coder, DOCS]  (CONVERGE / W25-rollover: evidence gate RETIRED · core-review folded · direction set)
**Topic:** The overdue converge session ran. **Preflight STALE → PASS** (currency lag only). **The energy/erasure interview gate is RETIRED** — the 2026-06-15 M5-D desk-research assessment (grade A−, primary-verified) shows M6 already froze the write path in the regret-proof encrypt-on-write posture, so the interviews are no longer decision-grade; **the app-bootstrap lane is UN-PARKED.** The **core-review (A/B/CONVERGE, 2026-06-15)** is folded: core is sound-where-built but **not yet runnable (`main()` is a stub)**; two HIGH findings (**C1** unauth composition-root HTTP / INV-SE-02; **C2** read-path decrypt aborts the whole replay) are now Open Risks OR-APP-AUTH / OR-RF-DECRYPT, both detonating at app-bootstrap. **W24→COMPLETE, W25→IN_PROGRESS, W26 scoped.**
**Direction (Nick delegated the call):** prioritize **app-bootstrap (carry C1+C2+C9)** ∥ **superior-automation Phase-1 design** (federation / honest-hybrid / expressiveness-without-DSL — the differentiator; likely a NEW Locked design doc) ∥ **agent currency pass**. **M7.1 stays ISSUED + sound but is a ride-along, not the focus** — it builds after the superiority design confirms its trigger/condition contracts.
**Action needed:**
- **Nick:** (1) **commit** the hivemind reconcile (message handed over). (2) **Decide Phase-1 scope** for the superiority design doc (greenlit for design; the Lock is yours) → dispatch that Cowork session from the brief. (3) **Two HIGH decisions** for app-bootstrap: C1 bind posture (loopback-default vs all-interfaces; feeds CC-1); C2 confidentiality-vs-availability contract. (4) **Re-run the external mirror sync** after the agent pass (clears Check 9). (5) Route M7.1 to a Coder slot when ready.
- **Coder:** **no change to your next-WU yet** — M7.1 remains your issued instruction, but it is now sequenced *behind* the superiority design (build after it confirms the trigger/condition contracts). Do not start until Nick routes it. App-bootstrap + the two HIGHs will arrive as a separate instruction.
- **DOCS:** R-γ (crypto-agility) + Key-Portability briefs still in flight; their returns feed app-bootstrap, not M7.

## 2026-06-14 [PM (Cowork) → Nick, Coder, DOCS]  (Track-4 forward execution: M7.1 ISSUED · C3 landed · 3 dispatches fired · entry-gate row 4 CLOSED)
**Topic:** The interview-independent forward lane executed. **M6.3 is COMMITTED `1eddd9a` (M6 COMPLETE 4-of-4).** **Entry-gate row 4 CLOSED** — the M5-C config-superiority veto was delegated to the PM and ruled **APPROVE** (Increment-1 acceptance met; 3 register guardrails + anti-reqs verified; INV-CE-01 blockquote verbatim-confirmed; competitor-receipts + README items deferred as PUBLISH gates). The Lane-4 no-new-Core-instruction condition is lifted. **M7.1 (automation trigger/condition path) is ISSUED to the Coder** — interview-INDEPENDENT (row 3 / interviews gate app-bootstrap + the M6.3 restore-half R-α, not M7.1). **C3 (cascade-governance doc-fix) is LANDED** (PM direct host edit — `RunContext.java` Javadoc + `core/automation/MODULE_CONTEXT.md` now cite AMD-91; record unchanged; `{@code}` not `{@link}` for the future type). **Three parallel dispatches fired** (none competes for the Coder or the critical path).
**Action needed:**
- **Nick:** (1) **commit** — hivemind (spine reconcile + M7.1 finalize + C3 docs + the new key-portability brief + this note), core repo (the C3 `RunContext.java` Javadoc + `MODULE_CONTEXT.md` edits — doc-only, no behavior change), docs repo (the `config-superiority.md` APPROVE/currency edits). Commit messages handed over as `-F` files (bang-free, quote-free). (2) **Upload to the DOCS Project:** `R-γ` (crypto-agility envelope-versioning CHALLENGE) + the new `Key-Portability` brief (R-δ §5 #1, HIGH). (3) **Route to the Coder/CORE:** `CC-1` bind-address spike. (4) **Deferred build gates** (run + confirm): M7.1 full `check` (after the Coder lands it); C3 `-Xdoclint`/spotless on `:core:automation`.
- **Coder:** your **next WU is M7.1** (`context/instructions/2026-06-15_M7.1_trigger-condition-path_coding-instruction.md` — ISSUED; re-run the pre-verification + STOP-gates at the then-current HEAD). Carry-pins incl. red-team F7/C4 wire-commitment + **R-δ AX-5 fail-closed-at-load (SD-9)**. Separately, **CC-1** is a throwaway CORE spike (bind posture; outside the production tree) — parallel, non-critical-path.
- **DOCS:** two briefs incoming — R-γ (adversarial challenge) and Key-Portability (design study); both READY TO DISPATCH; serialized PM assessment on return.
- **PM (next):** the W24→COMPLETE rollover + W26 scoping remain the **converge session's** job (NOT done here); the **app-bootstrap charter finalization** is post-interview (F2 scope-width + milestone placement + R-γ/CC-1 returns). Standing escalations to Nick carried (retry-vs-REC-162 as an M7.2 question; AX-7 deprecation policy; AB-1 auth model).

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (R16 ASSESSED — zero discards; OUTPUT_CONVENTIONS.md DRAFTED in core)
**Topic:** **The R16 cycle is COMPLETE: 16-B grade A / 16-A grade A−; merged disposition `context/planning/2026-06-12_R16_output-contract_merged-disposition.md` (REC-186–215, high-water 215); the gate-free `scripts/dev/OUTPUT_CONVENTIONS.md` DRAFTED in the core repo** (9 rules; pi-health = reference implementation). One confirmed in-house defect ships with it: **REC-204** — zero `>&2` in pi-health.sh, pi4-validation.sh, AND the OQ-15-2 bench driver; the convention routes diagnostics→stderr, machine lines (`KEY:value` + verdict)→stdout. The retrofit is small and can ride the already-queued F1/F3 pi4-validation fix commit.
**Action needed:**
- **Nick:** (1) **hivemind commit + SEPARATE core commit** (messages in the session report; both `!`-free, plain `-m` safe). (2) **Docs repo:** the 2 rehomed R16 returns are new files there — and your working tree already carried staged deletions + same-path untracked re-adds for 3 files (the R15 return + both sqlite-wal spike files); PM left that state untouched — reconcile when committing. (3) M5-C Increment 1 now also carries the R16 M5-C-COPY bucket (A-193/194/195 — the WTH porch-light dossier is the flagship).
- **Coder:** nothing blocks; future M12 instructions will carry the disposition §2b obligations (correlation_id pin; **MDC propagation = explicit copy on Java 21 per LTD-01**, + spike; naming convention; noise-budget test; config_error message register). New scripts follow OUTPUT_CONVENTIONS.md once committed.
- **PM (next):** the Sunday converge session (W25 rollover/W26 scoping) now synthesizes with the full R16 result; M12 scoping consumes disposition §2b.

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (THE PI-EVIDENCE SESSION: OQ-15-2 RESOLVED — encrypted set CONFIRMED; M6.3 gate 1-of-3 CLOSED; superseded as pointer by the R16-assessment entry above)
**Topic:** **OQ-15-2 is RESOLVED (2026-06-12): both candidates ENCRYPT-ON-WRITE; the Doc 15 §9 `encrypted_scopes` default is CONFIRMED `[identity, presence_personal]`** — no fallback, no STOP-escalate, **Doc 15 untouched** (expected-case rule). Margins: G1 13–37× at the real payload distribution; G2 49× headroom; two reproducing runs ±0.5%. Run on the Pi 5 intrinsics-off — NON-AUTHORITATIVE per spec §3.D, accepted per the recorded R1 conditional (optional Pi-4 confirmation queued advisory). Also: **pi4-validation.sh first exercise PASS** (42 m, zero failures, manual tar-pipe equivalent); readiness zero-drift; **the index.lock flag is DELETED (saga closed)**.
**Detail:** §8 filled in the spec (status → RESOLVED); disposition `context/assessments/2026-06-12_OQ-15-2_disposition_encrypted-scopes-CONFIRMED.md`; raw evidence + harness `context/assessments/oq-15-2-harness/`; full session record in pm-handoff (top). C9 §3.4 energy staging item RECONCILED/CLOSED. Findings F1/F3 (pi4-validation.sh unrunnable as written from Git Bash — fix advisory) + F4 (diagnostic-flag JVM refusal — driver hardened).
**Action needed:**
- **Nick:** (1) hivemind commit (message in the session report; no `!` — plain `-m` safe). (2) **Delete `CorpusDumpOQ152Test.java` from the core working tree before any core commit** (+ the untracked `pi4-validation-*` artifacts, keep-or-delete). (3) Re-upload **Current_State** (changed tonight) with the next spine upload. (4) **The energy/erasure interviews are now the SOLE open evidence gate** for M6.3 AND row 3 — schedule them. (5) The M5-C Increment-1 session prompt is ready to run as a parallel content conversation (D-1..D-7 vetoes at its start).
- **PM (next sessions):** M5-C Increment 1 (prompt on disk); the M6.3 instruction once interviews + the ordering call land (carries the resolved set verbatim + the cold-start cipher-warmup beat + OR-M6-NONCE).
- **Coder:** M6.3 still do-NOT-issue (2 gates remain: interviews + the ordering call; your M6.2 gate note stands).

## 2026-06-12 [PM (Cowork) → Nick]  (R16 briefs READY TO DISPATCH — ADDITIVE entry; superseded as pointer by the Pi-evidence entry above)
**Topic:** The R16 research cycle (logging/observability output UX — one output contract for end-users, operators, and LLM agents, Core logging + Pi scripts) is AUTHORED: **R16-A** (REC-186–200, UX/market) + **R16-B** (REC-201–215, engineering), both in `context/instructions/`, self-reviewed ×2; research-agenda addendum appended. First full exercise of the upgraded `research_mode` charters. Authored PARALLEL to the Pi-evidence session — this entry is a single-anchor append by design; no shared-file restructuring (the Pi session's closeout owns rewrite rights).
**Action needed:** **Nick:** (1) dispatch both briefs to the **DOCS Project in SEPARATE fresh conversations** (veto-or-default); (2) save returns to `context/instructions/` per the standing flow; (3) `index.lock.cleared-by-cowork-2026-06-12` STILL PRESENT (fifth flag — delete when convenient); (4) hivemind commit message in the session report.

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (prior pointer — AMD-88..93 + B2 C8/C9 RATIFIED — watermark 93; entry-gate rows 1–2 CLOSED)
**Topic:** **The M7 entry-gate AMD block AND B2 C8/C9 are RATIFIED (2026-06-12).** Bundled review returned same-day → PM assess/source-verify/fold (E92-3 declined-with-citation; one reviewer enum-misquote corrected) → Nick ratified ALL → mechanics complete: invariants §42–§47 (+11, index **163/47**), **watermark AMD-87→93**, AMD-04 SUPERSEDED banner, Doc 07/Doc 01 amendments-in-force banners + the §6.2 priority corrections, Glossary §5.4 superseded-in-part, charter rows 1–2 CLOSED, all 5 spine files flipped. Attestations recorded (module-info ✓; zero-publish-site grep CLEAN ✓; C8 anchors re-derived ✓).
**Detail:** Full record: pm-handoff "This Session" (2026-06-12, ratification). Return: `context/audits/2026-06-12_AMD-88-93_C8-C9_DOCS_Review_Return.md`. ⚠ Environment incident (contained + repaired): a VM-side bulk edit hit the truncated-tail mount artifact and clobbered the six AMD tails on the host; reconstructed from git `77d5e51` + the in-session fold record; all six host-verified end-to-end. NEW RULE: bulk edits to governance files go host-side (Edit/Write) or git-object-sourced — never VM read-modify-write of worktree files.
**Action needed:**
- **Nick:** (1) **Commit both repos** (messages in the session report). (2) **Re-upload the 5 spine files to BOTH Projects** (now RATIFIED/163-47/watermark-93). (3) **The M7 path is now rows 3+4:** Pi-4 microbench (Jun 17) + interviews (row 3 + M6.3); **M5-C Increment 1 (row 4 — nothing blocks starting it NOW)**. (4) Strategy-draft vetoes D-1..D-7. (5) index.lock.cleared deletion (if present) + mirror sync (Check 9).
- **PM (next session):** M5-C Increment 1 content session (unblocked); then the M7.1 instruction when rows 3–4 close (carries: shared construction-site sweep, the C1-interim pin, AMD-93 schema+reference pass, M7.1 event slice + survey, FIX-07 wiring, the M7.2 authoring-guidance notes [chain narrowing; literal-colon labels]).
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 not yet issued (rows 3–4). The ratified AMD-88..93 are now the binding contract shapes for all M7.x work.

## 2026-06-13 [PM (Cowork) → Nick, Coder]  (M7 AMD block DRAFTED — superseded by the RATIFIED pointer above)
**Topic:** **The M7 entry-gate AMD block is on disk — AMD-88..93 PROPOSED (six families per the merged disposition §2a; nav-index-derived numbers; ratification raises the watermark 87→93). The bundled DOCS review prompt (block + B2 C8/C9 in ONE dispatch) is READY. Strategy-refresh drafts D-1..D-7 await per-item veto. Documents-only — core untouched at `e5ea76f`.**
**Detail:** Full record: pm-handoff "This Session" (2026-06-13). Artifacts: `homesynapse-core-docs/design/amendments/AMD-88..93_*.md` (six files); `context/instructions/2026-06-13_AMD-88-93_C8-C9_Bundled_DOCS_Review_Prompt.md`; `context/planning/2026-06-13_strategy-refresh-drafts_R15.md`. Headlines: F5 type-residency ruled FLATTEN (PM default, review confirms); the two M1-era automation event records get RESHAPED (zero producers = the regret-proof window); AMD-91 carries a formal element-by-element AMD-04 supersession ledger (rate limiting NOT adopted — REC-168 owns that space); AMD-89 binds the shipped `EntityRole` (the disposition's `includedCategories` label named a type that never existed — flagged R89-1 for the review); C8 is cited PROPOSED-pending at every stamping mention — the bundle resolves block+C8 together by design.
**Action needed:**
- **Nick:** (0) **RE-UPLOAD the 5 regenerated `project-knowledge/` spine files to BOTH Claude Projects (CORE + DOCS) FIRST** — they now carry the M6-3-of-4 + AMD-88..93-PROPOSED ground the bundled review verifies against (same-session addendum, pm-handoff). (1) **Veto-or-default the BUNDLING, then dispatch the review** (`context/instructions/2026-06-13_AMD-88-93_C8-C9_Bundled_DOCS_Review_Prompt.md` → DOCS Project; if vetoed: C8/C9 first, block second). (2) **Per-item veto on D-1..D-7** (strategy drafts — strike freely, the rest folds at M5-C). (3) Commit both repos (messages in the session report). (4) **Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` — STILL present, third flag.** (5) Mirror sync (Check 9, the one file, unchanged). (6) Standing: Pi-4 microbench (Wed Jun 17) + energy/erasure interviews (the M6.3 gates AND the row-3 ordering input — a third slip makes the W25/W26 call undecidable); M5-C Increment 1 (Lane-4 gate, still not started).
- **PM (next session):** assess the review return → fold edits → ratification mechanics (invariants §42+, nav-index rows, watermark 87→93, AMD-04 SUPERSEDED banner, Doc 07/Doc 01 currency edits) → M7.1 instruction once entry-gate rows 1–5 are green.
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 awaits the 5-row entry gate. Nothing dispatched this session.

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (R14/R15 cycle COMPLETE — merged disposition + populated charter; superseded by the pointer above)
**Topic:** **All three research returns ASSESSED same-day (R14-B/R14-A/R15, serialized, each A−, quote-backs honored, zero discards) → MERGED DISPOSITION on disk (`context/planning/2026-06-12_M7-blueprint_merged-disposition.md` — THE M7 blueprint authority: AMD block = 6 families F1–F6; M7 = 3 pieces confirmed; W1–W8 crash-window pins; high-water REC-185) → charter POPULATED.**
**Detail:** Assessments: `context/assessments/2026-06-12_Research_14B/14A/15_PM_Assessment.md`. Headlines: ledger-gap dossier = M5-C flagship (frenck "out of scope" + installed verify-retry hacks); Apache 2.0 CONFIRMED by fork-pattern evidence (governance posture refined); Doc 07's Locked text richer than assumed (zombie-Run finalization, per-idempotency crash recovery — several "gaps" were CLOSED-UNTESTED); anti-requirements registered (no engine retry, no templating DSL, no forced migration, never lead with commodity encryption). Raw returns → docs `research/returns/`; consumed briefs → archive.
**Action needed:**
- **Nick:** (1) **C8/C9 ratification** — C8 is M7 entry-gate row 2 and now carries R14-A's external evidence (Hubitat "what turned on my light?" = the top provenance complaint). (2) **The M6.3-vs-M7 ordering call** at the W25/W26 boundary (Lane-2 evidence gates: microbench Jun 17 + interviews). (3) **M5-C Increment 1** — the content backlog is now evidence-rich (merged disposition §2e). (4) Commit both repos (messages in the session report). (5) Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` (still present). (6) Mirror sync (Check 9, the one file, unchanged).
- **PM (next session):** **draft the M7 entry-gate AMD block** (6 families, merged disposition §2a) + DOCS review prompt; strategy-refresh drafts (§2f) same session or following.
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 awaits the 5-row entry gate (charter).

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (W0 + R14/R15 briefs + M7/M8 skeleton — superseded by the pointer above)
**Topic:** **W0 Research-4 currency delta COMPLETE (zero open DQs; REC-39 re-anchored — NEW type-residency finding: `RunId`/`RunStatus`/`PendingStatus` must not appear in M7 event payloads, flatten-or-relocate = AMD-block decision) · 3 research briefs AUTHORED + self-reviewed ×3, READY TO DISPATCH · M7/M8 charter SKELETON on disk.**
**Detail:** Full record: pm-handoff "This Session" (2026-06-12). Artifacts: `context/assessments/2026-06-12_W0_Research_4_Currency_Delta.md`; `context/instructions/Research_14A_*.md` / `Research_14B_*.md` / `Research_15_*.md`; `context/planning/2026-06-12_M7-M8-charter-skeleton.md`. The MODULE_CONTEXT permit-count defect ("27"→30) corrected in core (docs-only).
**Action needed:**
- **Nick:** (1) **Veto-or-default `context/planning/2026-06-11_M7-blueprint_research-architecture.md` + dispatch all 3 briefs simultaneously** (R14-A/R14-B → DOCS Project, web search required; R15 → generic deep research permitted for evidence, disposition in-Project). (2) Commit both repos — messages in the session report. (3) Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` (a VM-created lock the sandbox cannot delete; renamed harmless). (4) Standing: Pi-4 microbench (Jun 17) + interviews; M5-C Increment 1; C8/C9 ratification (C8 = M7 entry-gate row 2); mirror sync (Check 9, one file).
- **Coder:** no change — next WU = M6.3 ONLY when its triple gate clears; M7.1 is research-gated + entry-gated (see the charter skeleton). Nothing dispatched this session.
- **PM (next session):** assessments as returns land — serialized, fold order R14-B → R14-A → R15; then populate the charter + draft the M7 AMD block.

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.2 delivery — gate DEFERRED to Nick)
**Topic:** **M6.2 (SecretStore + ScopeKeyManager + PayloadCipher seam) COMPLETE — files produced; BUILD GATE DEFERRED (commands in coder-handoff `### DEFERRED — M6.2`). Three [REVIEW] deviations need PM rulings; one queued post-MVP AMD candidate.**
**Detail:** Full record in coder-handoff (2026-06-11 M6.2 entry) + the Completion Report returned to Nick. Zero-new-edge property re-verified in both directions; NO module-info/Gradle/catalog/projectionVersion/schema/event-manifest change anywhere. The Doc 15 §8.1 stale row ("ScopeKeyManager … implement PayloadCipher") was honored as stale per CARRY 1/§3.8 — reconfirming the parked currency nit for the next Doc-15-touching amendment, alongside the [R6] `scope_keys` placement nit.
**Action needed:**
- **Nick:** run the M6.2 gate (two commands in the handoff DEFERRED section); commit on GREEN.
- **PM (WUCP Phase 2):** rule on **R-1** (the §3.5 write path REJECTS `!secret`/`!env`-bearing documents fail-closed — resolving in the write path would re-emit plaintext secrets into the file, INV-SE-03; consequence: UI/API mutation of tag-bearing configs is impossible until a tag-preserving emitter exists → propose queueing a post-MVP AMD in the REC-138 family), **R-2** (PayloadCipher threading deliberately STOPS at `HomeSynapseCore`'s held nullable field — `PersistenceFactory` is untouched; forwarding is the M6.3 wiring step), **R-3** (`Main.main()` is still the Phase-2 stub, so DP-6's "wire" is discharged as the package-private `Main.payloadCipher(Path, Clock)` factory + the app-level bridge round-trip test; full bootstrap rides the app-bootstrap milestone).
- **PM (M6.3 instruction):** the M6.2 surface M6.3 builds on: `ScopeKeyManager.encrypt/decrypt` + `Main.payloadCipher` + `HomeSynapseCore` 5-arg ctor; remember the random-IV-here / counter-nonce-there fence (OR-M6-NONCE) and that key versions are never reused past destroyed rows (`dek_ref` stability).

## 2026-06-11 [PM (Cowork) → Nick, Coder]  (M6.2 WUCP Phase 2 — superseded by the 2026-06-12 pointer above)
**Topic:** **M6.2 COMPLETE — gate GREEN (82 targeted + full check 147, ZERO gate-fix rounds — an M6 first) → PM WUCP Phase 2 APPROVE. R-1/R-2/R-3 ruled ACCEPT; 10 [INFO] accepted; zero-new-edge + manifest-zero-change source-verified. COMMITTED **`7c73c91`** (hivemind `d161ca9`; reconciled EOD). M6 = M6.1 ✓ M6.4 ✓ M6.2 ✓ — only M6.3 (triple-gated).**
**Detail:** Full verification + rulings record: pm-handoff `## Open Risks` M6.2 entry (2026-06-11). R-1 fail-closed tag-bearing `write()` ACCEPT (resolving on write bakes plaintext secrets into the emitted file — the [R4] class: leakage irreversible, restriction reversible; **NEW FUTURE-AMD: tag-preserving YAML emitter, post-MVP**; M10 design-input note). R-2 ACCEPT (`PersistenceFactory` untouched — extending the frozen gateway IS M6.3's wiring step). R-3 ACCEPT (DP-6 discharged: `Main.payloadCipher` factory + 5-arg ctor + bridge round-trip test; live bootstrap rides M13). ⚠ The Coder's M6.2 cross-agent note was lost to a mount-lag write overlap (Coder closeout vs PM rotation, same hour) — RECONSTRUCTED below; checklist-vs-file verification is now a standing Phase-2 step.
**Action needed:**
- **Nick:** (1) **COMMIT homesynapse-core (M6.2)** — message in the session report. (2) **COMMIT nexsys-hivemind** (closeout + hygiene pass; all moves are in-tree renames — plain `git add -A`; the consumed M6.2 instruction already sits in `coding-instructions/archive/` with its EXECUTION RECORD). (3) Skill-mirror sync still pending (Check 9). (4) **M6.3's evidence gates = the M6 critical path** (Pi-4 microbench OQ-15-2 + AMD-86 §3 interviews — both yours). (5) **Research 14/15: veto-or-default** `context/planning/2026-06-11_M7-blueprint_research-architecture.md`. (6) B2 C8/C9 — C8 `actorRef` is now on the M7 critical path.
- **Coder:** next WU = **M6.3 ONLY when its triple gate clears — do NOT start.** Nothing else is open.
- **PM (next session):** ~~Step-0 (M6.2 commit)~~ DONE EOD; W0 Research-4 currency delta; author the 3 R14/R15 briefs; M7/M8 charter skeleton on the returns. Session prompt: `context/handoff/2026-06-12_W0_R14-briefs_session_prompt.md`.
- **Status (2026-06-11 EOD):** Nick items (1)/(2) DONE — core `7c73c91` + hivemind `d161ca9`, pushed, reconciled; (3) mirror sync STILL pending (Check 9 — one file, now incl. the 2026-06-11 format additions); (4)–(6) standing. **Coder feedback folded:** 4 format-skill additions (internal-consumer survey; table-governs-over-prose; minimum read set; verbatim external-standard vectors), the NO_DIRECT_TIME_ACCESS-reach OQ, pm-lessons entry. **Request to Coder:** write future handoff entries as short PARAGRAPHS, not single mega-lines — 270KB single-line entries defeat line-based pagination (the A1 rotation fixed the backlog; entry style fixes the source).

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.2 delivery — RECONSTRUCTED by the PM from the completion report after the mount-lag loss; resolved same-day by the pointer above)
**Topic:** **M6.2 COMPLETE — files produced (14 created + 15 modified incl. 4 MODULE_CONTEXTs), BUILD GATE DEFERRED to Nick (commands in coder-handoff §DEFERRED — M6.2; baseline `62a81e6`). Three [REVIEW]s need PM ruling: R-1 `write()` fail-closed on tag-bearing docs; R-2 cipher threading stops at `HomeSynapseCore`; R-3 `Main.main()` stays the Phase-2 stub. 10 [INFO]. 12-agent adversarial review: 0 blockers/majors; 5 minors fixed in-tree.**
**Action needed:** Nick gate run → PM Phase 2 rulings → commit. *(All discharged same day — see the pointer above.)*

## 2026-06-11 [PM (Cowork) → Nick, Coder]  (M6.4 WUCP Phase 2 — superseded same-day by the M6.2 pointer above)
**Topic:** **M6.4 COMPLETE — gate GREEN (full check, 147 tasks) → PM WUCP Phase 2 APPROVE. R-A/R-B/R-C/R-D ruled ACCEPT (R-B condition source-confirmed); `CONFIG_SECTION_RELOADED → [SYSTEM]` confirmed; GF-1 verified applied exactly as ruled. COMMITTED `62a81e6` (sha reconciled 2026-06-11 at Step-0); M6.2 DISPATCHED (return in flight).**
**Detail:** Full record in pm-handoff "This Session" (2026-06-11). Survey-lesson logged: P2 survey + behavioral publish-count pins category — **skill source updated** (`project-manager/references/coding-instruction-format.md`), mirror sync pending (Check 9 STALE until Nick syncs — expected). M6.2 needs NO instruction edit (G7 adaptive; no R1 item exists in it — grep-verified). EXECUTION RECORD appended to the M6.4 instruction; Nick `git mv`s it to `coding-instructions/archive/` at commit (no tombstone this time).
**Action needed:**
- **Nick:** (1) **Commit homesynapse-core (M6.4)** — message in the session report. (2) **Commit nexsys-hivemind closeout** — include `git mv context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md context/coding-instructions/archive/`. (3) **Run the skill mirror sync** (one changed file). (4) **Dispatch M6.2**: instruction `context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`; dispatch line: "Ruling [R6] CONFIRMED; R1 is DONE in M6.4"; baseline = the new M6.4 sha (its G7 gate adapts). (5) **Microbench + interviews are now the M6 critical path** (M6.3's gates — both Nick-paced).
- **Coder:** next WU = **M6.2** on Nick's dispatch (refuse-to-close pointer in coder-handoff stands).
- **PM (next session):** sha-reconciliation for the M6.4 commit (Step-0); W25 charter (due Jun 14/15); the post-M6 deliberation beat (M7/M8 automation runway + Research-14 scoping); rotate this file's resolved 06-10/06-11 trail below `## Archived`.
- **Status (2026-06-11, M6.2-return session Step-0):** Nick items (1)/(2)/(4) DONE — core `62a81e6`, hivemind `82c8068`, M6.2 dispatched; item (3) skill-mirror sync still PENDING (Check 9 STALE — expected); item (5) standing. PM Step-0 sha-reconciliation + this file's rotation DONE; W25 charter + deliberation beat run this session.

### 2026-06-21 | Coder → PM/Nick | M7.2a-2 (execution/dispatch) DONE, NOT gate-verified
- **Delivered** per the v3-hub instruction against HEAD `1541446` (all STOP gates re-verified; re-derived 67/37/49 at HEAD → 71/41/53 after). The differentiator engine now ACTS: 5 Tier-1 actions execute with row 5/6 + §6.2 fail-fast + real tally, command dispatch validates + emits `command_dispatched`/`command_result`, the real condition gate emits row 4, conflict detection emits row 9, and **all four DP-A carries** landed (QUEUED single-flight drain, row-4 causal chain, dedup-before-gate, real snapshot + tallies). module-info UNCHANGED in both modules. Deferred build gate (Nick): `./gradlew check`, pins **71/41/53** — see coder-handoff for the targeted commands + the Spotless re-run note.
- **For PM ACCEPT review — the `[REVIEW]` deviations (all flagged, all instruction-invited):** (1) **`RunConditionGate` widened to 4 params** + (2) **`ActionExecutor.execute` widened** (envelope param + `ActionExecutionResult` 3rd `failureReason` field) — both because a seam that publishes a causally-correct event needs the triggering `EventEnvelope` (the frozen `RunContext` lacks correlationId/eventTime); no JPMS edge added. (3) **`DeviceRegistry.getIntegrationForEntity()` named in the instruction does not exist** — resolved via the two-hop EntityRegistry→DeviceRegistry. (4) **`EmittedDomainEvent`** (EmitEventAction payload) is unregistered → degrades on replay; full custom-event substrate is future work (recommend a tracked follow-up). (5) `EvaluatedEntityState.lastChangedByEventId`/`.value` nullable (substrate gap — `EntityState` doesn't track the last-changing event id). (6) executor re-resolves `CommandAction.target` via `SelectorResolver` (C4 read-back deferred to subscriber wiring). **Recommend ACCEPT on all** — each honors a Locked contract (AMD-92 §2.4 causal stamping / AMD-03 single snapshot) the literal spec couldn't satisfy, and each is in a "Coder Pushback Welcome" seam.
- **Next WU (refuse-to-close):** M7.2b OR M7.3 per the PM Core sequence — Nick/PM to dispatch. The composition-root wiring of the executor/dispatch subscribers + the zombie-set log reconstruction ride app-bootstrap (the `RunManagerAssembly` seam — now `runManager(publisher, executor, gate, stateQuery, clock, config)` — is the handoff).

### 2026-06-27 | Coder → PM/Nick | M7.4a (command_issued producer + co-located dispatch subscriber) DONE, NOT gate-verified
- **Delivered** per the 2026-06-26 M7.4a instruction against core HEAD `b296e76` (Doc 07 §3.11.1 AMD-95-reconciled; §1 D1/D2/D3). The substrate-native command hop is live in code: `StandardActionExecutor` **emits `command_issued`** (frozen 5-comp; capability→config timeout precedence; device→event idempotency map) instead of in-process dispatch, and `StandardCommandDispatchService` is now a **co-located `Subscriber`** that dispatches **in LIVE only (D2)** with a paired `close()` teardown, registered in `HomeSynapseCore` (third runtime subscriber, `coalesceExempt=true`, after projection LIVE, torn down in both shutdown branches). +1 public type (`CommandDispatchAssembly`); `CommandDispatchService extends AutoCloseable`. **counts stay 71/41/53; module-info UNCHANGED** in both modules. Deferred build gate (Nick): `./gradlew check` — commands + pins in coder-handoff §DEFERRED. A 4-lens in-session adversarial review (compile/-Werror, contract, tests, regressions) returned **0 findings** (LLM pass, NOT the gate).
- **For PM ACCEPT review — the `[REVIEW]` design choices (all flagged, all instruction-invited "Coder Pushback Welcome"):** (1) **injected `parameterSerializer` (`Function<Map,String>`)** on the executor — automation has no JSON library + no-new-edge pin, and resolved params can hold value-model `AttributeValue`, so the composition root supplies the persistence-mapper-backed serializer (vs. a hand-rolled writer that would drift from the canonical serialization the M9 adapter deserializes with); (2) **`onEvent` passes `Map.of()` to `validate()`** — the Tier-1 validator is value-agnostic and params ride durably on `command_issued` for M9, so the subscriber doesn't deserialize (no JSON parser in automation); (3) **`coalesceExempt=true`** for the dispatch subscriber (a coalesced command = a command that never dispatched); (4) **`CommandDispatchService extends AutoCloseable`** (narrowed `void close()`) gives the composition root a typed teardown handle mirroring `triggerEvaluator.close()`. **Recommend ACCEPT on all** — each preserves a Locked contract the literal spec couldn't, with no new module edge.
- **OR-M7-WIRING status nuance (for milestone tracking):** AMD-95 §3.11.1's "current-vs-target currency note" is now **HALF live** — the SUBSCRIBER side is wired (`command_dispatch_service` registered + torn down) and the PRODUCER code is complete + unit-tested, but the **executor's live composition-root wiring (the RunManager pipeline that fires the producer in production, incl. supplying the Jackson serializer) remains DEFERRED** per the M7.2a-2 handoff. So the registered subscriber currently has **no live producer** (idle, harmless). The remaining producer wiring + the ledger's live registration + `pollExpirations()` + the E2E gate + the D2 replay CI test are **M7.4b** (the refuse-to-close pointer).
- **Next WU (refuse-to-close):** M7.4b, then the thin causal-read-API → AB-4 → M9.

_(Rotation marker, 2026-06-12 hygiene pass: entries earlier than this point were rotated → `archive/cross-agent-notes-2026-Q2.md`. This marker was formerly a second archive-separator HEADING with a caption claiming entries above it were active — both false since the beat-57 separator relocation, and a live anchor-bait of the beat-59 splice class. De-baited to plain text at v16 beat 62; the file's SINGLE live separator is near the top.)_


---

## 2026-07-04 | Coder → PM | M9.3 delivered — §1 realized; four [REVIEW] items + the WUCP Phase 2 fold pointer

_(Hub annotation, v17 beat 3 — SERVED same-day: two-layer audit ACCEPT (3 verification agents + hub direct checks; zero defects; the fixture arithmetic and corpus transcription independently recomputed). All five [REVIEW]s ruled ACCEPT — rulings in pm-handoff v17 beat-3. Item 1's Doc 08 §3.6 currency note LANDED in this beat's docs commit; item 4's richer payload QUEUED as an event-model AMD candidate (M9.4/M10 window); item 7's wire pins carried into the backlog M9.4 row. **Location note:** this note was appended at EOF BELOW the `## Archived` separator (append-at-EOF miss — new notes belong ABOVE it); served this same beat, it stays here as archived record and frontmatter ACTIVE: 0 remains accurate. Original text follows verbatim.)_

M9.3 is delivered (see coder-handoff for the gate state + arithmetic). PM-relevant items:

1. **§1 landed as its own coherent unit** (AMD-97 realization + DP-2 MatchCriteria + §C namespace javadoc). The Doc 08 §3.6 currency-note fold (WUCP Phase 2) is yours: `DeviceProfile` is 10 components with `confirmation` LAST; `confirmationMode` is TYPED as device-model's `ConfirmationMode` (the §1.1 STOP-gate found the core type — no String, no mint); `degradeRule` is `Set<DegradeRule>`.
2. **[REVIEW] Interview retry architecture:** the frozen `interview()` runs ONE attempt; the Doc 08 §3.4 3-retry/5-15-30 s ladder + sleepy park/resume are clock-scheduled in `PendingInterviewQueue`, driven by the ingestion cycle — no thread sleeps. Read as an architectural realization of §3.4, not a behavior change; flag if you read §3.4 as requiring in-call retries.
3. **[REVIEW] `InterviewStatus` has no FAILED constant** (COMPLETE/PARTIAL/PENDING — A9 verified): persistent failure renders PARTIAL-with-gathered-data; an attempt with ZERO endpoints has no constructible `InterviewResult` (frozen non-empty pin) and the frozen surface throws `IllegalStateException` (the `resumeNetwork()` precedent). The internal pipeline never hits that path.
4. **[REVIEW] The frozen `DeviceDiscoveredEvent` is 4 fields** (integrationId, protocolAddress, manufacturer, model — non-blank); Doc 08 §4.4 sketches a richer payload (endpoints, power_source, interview_status, matched_profile_id) that event-model never got. M9.3 publishes the frozen shape with an `"unknown"` sentinel for PARTIAL identity. If the richer payload is wanted, that is an event-model AMD (zero-mint held this WU).
5. **[REVIEW] `DeviceRegistry` is constructor-injected into `ZigbeeAdoptionSlice`** — it is not an `IntegrationContext` component; the Doc 02 §3.12 dedup surface used is `findByHardwareIdentifier("zigbee", ieeeHex)`. M9.4 wiring decides the instance (AB-3 in-memory substrate at the composition root). If adoption should instead ride a context aggregator, that is an integration-api decision.
6. **Pre-existing test updated (declared):** `EzspProtocolTest` "D-M92-6 stubs name their completing milestone" no longer asserts `interview()` throws — the seam is filled; sendZclFrame/topologyScan assertions retained.
7. **Wire pins for the M9.4 bench list:** `sendUnicast 0x0034` / `incomingMessageHandler 0x0045` / `lookupNodeIdByEui64 0x0060` (bellows-re-derived, v4 lineage through v13); the v13 lookup reply is a bare nodeId — verify the v14 dialect on silicon.

---

## 2026-07-04 — Coder → PM: M9.4b delivered (real transport + R4 unification + the honest-verdict fences) — **SERVED (v20 hub, beat 2): all six items covered by the beat-2 rulings record (pm-handoff v20 beat-2 — R1–R8 + the SD-5 frame to Nick + the bench-watch items staged into the acceptance checklist). LOCATION MISS ANNOTATED: this note was appended BELOW the `## Archived` separator (the M9.3 append-at-EOF class, repeated despite the coder-CLAUDE.md rule) — annotated in place per the M9.3 precedent; coder-skill wording strengthening queued for the next skills pass. ACTIVE count unaffected (served same-day).**

M9.4b is delivered (see coder-handoff for the gate state + arithmetic; HeroLoop 5/5, RestartHonestyIT new, both replay suites untouched-green, 71/41/53 green). PM-relevant items:

1. **SD-5 derivation outcome ([REVIEW], Nick's eyes):** bellows' `use_hashed_tclk` supplies a GENERATED random seed as `preconfiguredKey` — it diverges from keep-well-known. The RULED one-constant realization (0x1B84, ZigBeeAlliance09 kept) shipped; with the well-known key as the hashed root, per-device TCLKs are publicly derivable, so the election as realized characterizes join behavior WITHOUT adding key secrecy. Recommend ruling the generated-seed custody follow-up (persisted via the §5.5 store + a CoordinatorOps seam widening) before any real user network pairs.
2. **The SD-4 vocabulary question:** `"unroutable"` (dispatch service + zigbee handler) is in NEITHER the guarded nor the terminal SD-4 set — analyzed terminal-class (it reports the tracked command's routing fate); needs an explicit ruling + a membership-test row when ruled.
3. **Bench-watch items for the M9.4 acceptance pass:** `stackStatusHandler 0x0019` / `EMBER_NETWORK_UP 0x90` are BENCH-VERIFY (bellows-derived, synthetic-tested); IAS cluster-specific notifications no longer pass dedup (the verbatim "unsolicited 0x0A only" ruling) — watch for ×2 notification twins on real silicon; hashed-TCLK join stability per SD-5's fallback clause.
4. **Two rulings queued from fold analysis:** ZCL PowerSource 0x81/0x82 (mains-with-battery-backup) currently classify battery-conservative (the literal {0x01,0x02} mains set — safe direction); a present-but-non-array `degradeRule` field still silently ignores (the same fail-open class F-15 closed for unknown values).
5. **Doc currency for WUCP Phase 2:** Doc 03 §8.1's "filtered queries at the API layer" vs the ruled Doc 08 §3.5 query-time `brightness_percent` decoration (read-contract note: a NEW data key inside the existing attributes map — the frozen v1.1 read-API SHAPE untouched; the explainability lane consumes it from your brief). `CommandResultEvent`'s outcome javadoc is now the live 10-value vocabulary. "DECIDE-04" still survives at HomeSynapseCore ctor javadoc / HomeSynapseArchRules / several MODULE_CONTEXTs (only the mandated Main.java excision shipped — hygiene sweep is yours).
6. **Test-infrastructure [REVIEW]:** integration-zigbee gained `testImplementation(libs.logback.classic)` + `logback-test.xml` — the F-12 one-WARN pin needed the repo's first unit-test log-capture idiom (test-scope only, outside the graph-assert). Accept or direct a behavioral re-pin.
