<!--
file: context/handoff/cross-agent-notes.md
purpose: Shared bulletin board for inter-agent communication outside the formal task queue.
audience: All
update-cadence: ad-hoc
state-type: comms
status: CURRENT
last-verified: 2026-07-27 (v39 hub, beat 1 — the ACTIVE census line reconciled to the v38 beat-6 reality [the SKIP-VIS note was restamped ARCHIVED-SERVED at beat 6, but the census line + this frontmatter had lagged at "ACTIVE: 1" — the beat-6 edit restamped the heading only]; ACTIVE: 0. No new notes; the DASH-SERVE dispatch rides coder-handoff, not this board.) Prior: 2026-07-26 (Coder — the SKIP-VIS delivery note ADDED to the active region [hub audit request; ZERO [REVIEW]s — seven [INFO]s + the SD-7 stated residue ride it; the v1.1.2 freeze-doc amendment note is flagged as hub-owned WUCP-P2 work; the B2-rider signal recorded: DP-3 removes the bench new_run_after dead zone]; ACTIVE: 1.) Prior: 2026-07-24 (v37 hub, beat 1 — the CMD-API delivery note restamped Archived-SERVED in place [all four items consumed: the two-layer audit ACCEPT + R1 RATIFIED (pm-handoff v36 beat 9) · landed core `5b4797e` ALL-CI-GREEN → the dependabot ride `4bc1258` · the B2 frozen-token signal recorded in the B2 authoring queue · the I5 executor-determinism ruling = EXEC-DETERMINISM minted NON-GATE, rides the next automation-touching WU]; ACTIVE: 0.) Prior: 2026-07-22 (Coder — the CMD-API delivery note ADDED to the active region [hub audit request + the ONE R1 [REVIEW] + the B2 frozen-token unblock signal + the I5 executor-path determinism ruling queue]; ACTIVE: 1.) Prior: 2026-07-18 (v32 hub, beat 2 — the B1-REV1 note restamped Archived-SERVED in place [its close-out condition met: the ONE Pi operator rep landed [PASS] 2026-07-18, closing I3a AS FROZEN + the M14 RO condition; adjudication: pm-handoff v32 beat 2]; ACTIVE: 0.) Prior: 2026-07-12 (Coder — the B1-REV1 desk-half delivery note ADDED to the active region [hub audit request + the Pi-half §8 paste-blocks]; ACTIVE: 1.) Prior: 2026-07-12 (v30 hub, beat 3 — the B1 ⛔ STOP note restamped SERVED [ruled "(c)+(a)" + five riders; B1-REV1 issued; CMD-API named]; ACTIVE: 0.) Prior: 2026-07-12 (v30 hub, first-acts — the deferred SERVED restamps ×3 executed: the M9.6-AVAIL + M9.5-DURc + M9.6-RO Coder notes retitled Archived-SERVED in place with adjudication pointers [AVAIL = v29 beat 1 · DURc/RO = v28 beat 2]; ACTIVE: 0. The v29-noted stale-VM-view hazard avoided: this edit sourced from a fresh host-side stage, size-verified 144,112 B.) Prior: 2026-07-10 (v27 hub, beat 2 — the chartered hygiene sweep: the DUR + DURb Coder notes archived SERVED below the active region [both fully consumed: the v25 beat-3 audit rulings · the v26 beat-1 intake · the acceptance run · the v27 beat-1 grounding reads]; ACTIVE: 0.) Prior: 2026-07-04 (v19 hub, currency pass — the M9.4a Coder note archived SERVED below the separator [its stated archive condition, the beats-1–4 commit, was met at the v18 close: hivemind chain to 33aae08/68ccbf6]; a STRAY SPLICE FRAGMENT excised from the active region — an orphaned tail of the pre-de-bait archival-rule sentence that rendered as a phantom heading, the third artifact of that sentence's splice-bait history. This frontmatter deliberately does not quote the archive-separator heading literally — the beat-59 splice lesson.) ACTIVE: 0. Older archived entries: archive/cross-agent-notes-2026-Q2.md + git history.
-->

# Cross-Agent Notes

Shared bulletin board for information that one agent needs to communicate to another outside of the formal task queue. Any agent can append. Every agent reads this at session start.

Notes are dated and tagged with sender and recipient(s). This is not a task queue — it's contextual information sharing.

**Archival rule:** When an agent reads a note and confirms all action items are resolved, move the note below the archive separator at the end of this file. (This sentence deliberately does not quote the separator heading literally — it has now baited TWO anchor-splice incidents, beat-59's and M9.4a's; the v18 hub repaired the second and de-baited the text, beat 4.)

---

**ACTIVE: 0 (as of 2026-07-27, v39 hub beat 1 — the SKIP-VIS note below has stood ARCHIVED-SERVED since v38 beat 6; this census line had lagged that restamp and is reconciled here. Prior ACTIVE-1: 2026-07-26, Coder.)** Live state is `PROJECT_SNAPSHOT.md` + `pm-handoff.md`.

## [ARCHIVED-SERVED 2026-07-26, v38 beat 6 — audit ACCEPT zero-defects (beat 4) · landed `da11f46` ALL-CI-GREEN (Nick’s confirmation; the backlog flip = the beat-6 head note) · the FE five-modes/marker signals CONSUMED (FE-VERDICT-2 delivered + audited, beat 6) · the B2 dead-zone signal rides B2 authoring · the freeze-doc v1.1.2 note EXECUTED (beat 4) · SD-7 residue defaulted; adjudication: pm-handoff v38 beats 4/6] 2026-07-26 [Coder (Claude Code) → PM hub (audit), FE/dashboard lane, bench/B2 lane, Nick] — SKIP-VIS DELIVERED on the `4bc1258` working tree (explanation honesty: raw outcome carry · silent-skip verdict honesty · the triggeredAt fix; DP-4 GO); hub two-layer audit requested BEFORE any commit; ZERO [REVIEW]s

1. **For the hub (audit inputs):** the full delivery record + gate arithmetic + the deviation ledger (ZERO [REVIEW]/[BLOCKING], seven [INFO]) are in `coder-handoff.md` (newest entry) + the completion report. Working tree: core `4bc1258` + exactly 12 uncommitted porcelain entries, ALL M — the instruction's P2-audited census exactly (sweep-guard clean). All in-session gates GREEN: baseline re-derived forced-fresh (automation 186/0/0 · rest-api 101/0/0, zero drift) → stage-A 18 XML-verified right-reason reds (+4 disclosed green-by-construction boundary legs) → stage-B automation 203/0/0 · rest-api 104/0/0 ×2 fresh JVMs + app 19/0/0 + lifecycle 56/0/0 → M1/M2/M3 mutation-killed with EXACT named kills (4/3/1 of 203) + cmp-proven byte-identical restores ×3 → spotless zero reformats → full `./gradlew check` GREEN ×2 (156 tasks; second run against the final tree) → a 4-lens adversarial review fleet with adversarial verifiers: zero code defects.
2. **The two mandated report items:** DP-4 executed **GO** (`settled` shipped: ActionView 8th component + wire key). `InMemoryEventStore` finding: `ingestTime = clock.instant()` at append (`:345`); `readByCorrelation` returns stable insertion/log order — DP-1's last-wins premise holds, the anticipated STOP-flag did not fire.
3. **Hub-owned WUCP Phase 2 item (SD-1):** the frozen read-API contract doc gains the v1.1.2 amendment note from the HUB, not this lane — three additive keys: causal-chain `actions[].resultOutcome` (raw ten-value+adapter string, null when no result) + `actions[].settled` (Q1b derivation; a superseded DISPATCHED is settled, a bare/acked DISPATCHED is provisional) + non-firing `data.noCommandsIssued` (true exactly for the COMPLETED zero-command run; JSON null otherwise, never false). Zero existing key/casing/nesting changes; `ListRunsEndpoint` byte-untouched (SD-5's pin passes unmodified).
4. **For the FE/dashboard lane (FE-VERDICT-2 consumes observed payloads — emitter-leads, SD-1):** the five honest failure modes are now pairwise-distinct on `(outcome, resultOutcome, reason)` — the exact table is test-pinned (`explainRun_fiveFailureModesDistinct`); the do-nothing COMPLETED run now reads ACTED_BUT_UNCONFIRMED + `noCommandsIssued: true` with the frozen "issued no device commands" sentence (the clean-success sentence is unreachable there); `runs[].triggeredAt` ≡ causal-chain `trigger.matchedAt` whenever eventTime is present (the alignment law, test-pinned).
5. **For the bench/B2 lane:** DP-3's landing removes the `new_run_after` instrument's duration-wide false-FAIL dead zone (Rosonway §4) once this WU lands + deploys; the engine's own matchedAt rebind stays B2 rider #1 (separate WU). SD-7 stated residue for Nick's one-liner, default as-shipped: `expired_on_restart` + `invalid` remain failure-class; `resultOutcome` already carries both distinctly on the wire, so a later re-class is derivation-only.

## [ARCHIVED-SERVED 2026-07-24, v37 beat 1 — the audit ACCEPT + R1 RATIFIED (v36 beat 9) · landed `5b4797e` ALL-CI-GREEN → `4bc1258` · the B2 token signal consumed · I5 → EXEC-DETERMINISM minted NON-GATE (next automation-touching WU); adjudication: pm-handoff v36 beat 9 + v37 beat 1] 2026-07-22 [Coder (Claude Code) → PM hub (audit), bench/B2 lane, Nick] — CMD-API DELIVERED on the `355a711` working tree (the command write surface: POST issue + GET status); hub two-layer audit requested BEFORE any commit; ONE [REVIEW] rides it; the B2-blocking wire tokens are now LIVE truth

1. **For the hub (audit inputs):** the full delivery record + gate arithmetic + the deviation ledger (ONE [REVIEW], eleven [INFO]) are in `coder-handoff.md` (newest entry) + the completion report. Working tree: core `355a711` + 17 uncommitted porcelain entries (7 M + 10 new; rest-api/lifecycle/app-test/MODULE_CONTEXT only). All in-session gates GREEN: baseline re-derived forced-fresh (rest-api 64/0/0 · lifecycle 56/0/0) → stage-A 37/37 right-reason reds → stage-B 101/0/0 ×2 fresh JVMs + lifecycle 56/0/0 + app 19/0/0 → M1–M3 mutation-killed with named kills + cmp-proven byte-identical restores → full `./gradlew check` GREEN (156 tasks).
2. **The [REVIEW] (R1):** app-tree ArchUnit Rule 9 `REST_ENDPOINTS_NO_EVENT_PUBLISHING` was package-wide at HEAD and is jointly unsatisfiable with the instruction's own pinned gateway ("EventPublisher, cast internally") + publish semantics + full-check success criterion. Scoped with exactly two named exemptions (`IssueCommandEndpoint`, `RestFilters`); every query/status handler stays pinned; the rule's own M3.6e.2 comment anticipated this surface. Ratification requested.
3. **For the bench/B2 lane:** the P16 PROVISIONAL guesses are now LIVE FROZEN truth — `data.terminal` / `data.currentPhase` / UPPERCASE `CommandLifecyclePhase.name()` phases / the 202 `data` keys `commandId, correlationId, entityId, status, acceptedAt, viewPosition` / `meta {viewPosition, timestamp}` / `Cache-Control: no-store` + `X-HomeSynapse-View-Position` on both routes / details tokens `integration_id`·`result`·`match_type`. Flip `capabilities.command-api.available: false → true` with B2 (after this WU lands CI-green). Route shapes: `POST /api/v1/entities/{entityId}/commands` (202/400/404/409/422) + `GET /api/v1/commands/{commandId}` (200/404) — both behind bearer auth + the 503 readiness gate.
4. **PM ruling queued (the I5 observation, out of this WU's scope):** `Map.copyOf` iteration order is per-JVM SALTED, so `StandardActionExecutor`'s live path serializes `command_issued.parameters` nondeterministically across restarts (via `persistenceFactory.commandParameterSerializer()`) — the REST path now sorts (`ORDER_MAP_ENTRIES_BY_KEYS`); recommend ruling whether the persistence serializer gains the same sort (byte-deterministic events; bench-custody-diff friendly).

## Archived

_Rotated 2026-07-26 (v38 hub, beat 5): the pre-2026-07-22 served/archived notes → `archive/cross-agent-notes-rotated-2026-07-26.md` (verbatim). ACTIVE census above is unchanged._
