<!--
file: context/handoff/cross-agent-notes.md
purpose: Shared bulletin board for inter-agent communication outside the formal task queue.
audience: All
update-cadence: ad-hoc
state-type: comms
status: CURRENT
last-verified: 2026-06-12 (2026-06-13-brief session Step-0) — R14/R15 cycle closed; core HEAD `e5ea76f` (substantive `7c73c91`) / docs `ed5cf91` / hivemind `5f30ff0` (fbf3124 closeout + W25 re-charter/prompt commit); only the 2026-06-12 CURRENT POINTER active.
-->

# Cross-Agent Notes

Shared bulletin board for information that one agent needs to communicate to another outside of the formal task queue. Any agent can append. Every agent reads this at session start.

Notes are dated and tagged with sender and recipient(s). This is not a task queue — it's contextual information sharing.

**Archival rule:** When an agent reads a note and confirms all action items are resolved, move the note below the `## Archived` separator at the bottom of this file. Only notes above the separator are active. This keeps the file append-only while managing signal-to-noise as block count grows.

---

## 2026-06-13 [PM (Cowork) → Nick, Coder]  ← CURRENT POINTER  (M7 AMD block DRAFTED — AMD-88..93 PROPOSED; bundled review READY TO DISPATCH)
**Topic:** **The M7 entry-gate AMD block is on disk — AMD-88..93 PROPOSED (six families per the merged disposition §2a; nav-index-derived numbers; ratification raises the watermark 87→93). The bundled DOCS review prompt (block + B2 C8/C9 in ONE dispatch) is READY. Strategy-refresh drafts D-1..D-7 await per-item veto. Documents-only — core untouched at `e5ea76f`.**
**Detail:** Full record: pm-handoff "This Session" (2026-06-13). Artifacts: `homesynapse-core-docs/design/amendments/AMD-88..93_*.md` (six files); `context/instructions/2026-06-13_AMD-88-93_C8-C9_Bundled_DOCS_Review_Prompt.md`; `context/planning/2026-06-13_strategy-refresh-drafts_R15.md`. Headlines: F5 type-residency ruled FLATTEN (PM default, review confirms); the two M1-era automation event records get RESHAPED (zero producers = the regret-proof window); AMD-91 carries a formal element-by-element AMD-04 supersession ledger (rate limiting NOT adopted — REC-168 owns that space); AMD-89 binds the shipped `EntityRole` (the disposition's `includedCategories` label named a type that never existed — flagged R89-1 for the review); C8 is cited PROPOSED-pending at every stamping mention — the bundle resolves block+C8 together by design.
**Action needed:**
- **Nick:** (0) **RE-UPLOAD the 5 regenerated `project-knowledge/` spine files to BOTH Claude Projects (CORE + DOCS) FIRST** — they now carry the M6-3-of-4 + AMD-88..93-PROPOSED ground the bundled review verifies against (same-session addendum, pm-handoff). (1) **Veto-or-default the BUNDLING, then dispatch the review** (`context/instructions/2026-06-13_AMD-88-93_C8-C9_Bundled_DOCS_Review_Prompt.md` → DOCS Project; if vetoed: C8/C9 first, block second). (2) **Per-item veto on D-1..D-7** (strategy drafts — strike freely, the rest folds at M5-C). (3) Commit both repos (messages in the session report). (4) **Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` — STILL present, third flag.** (5) Mirror sync (Check 9, the one file, unchanged). (6) Standing: Pi-4 microbench (Wed Jun 17) + energy/erasure interviews (the M6.3 gates AND the row-3 ordering input — a third slip makes the W25/W26 call undecidable); M5-C Increment 1 (Lane-4 gate, still not started).
- **PM (next session):** assess the review return → fold edits → ratification mechanics (invariants §42+, nav-index rows, watermark 87→93, AMD-04 SUPERSEDED banner, Doc 07/Doc 01 currency edits) → M7.1 instruction once entry-gate rows 1–5 are green.
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 awaits the 5-row entry gate. Nothing dispatched this session.

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (R14/R15 cycle COMPLETE — merged disposition + populated charter; superseded by the pointer above)
**Topic:** **All three research returns ASSESSED same-day (R14-B/R14-A/R15, serialized, each A−, quote-backs honored, zero discards) → MERGED DISPOSITION on disk (`context/planning/2026-06-12_M7-blueprint_merged-disposition.md` — THE M7 blueprint authority: AMD block = 6 families F1–F6; M7 = 3 pieces confirmed; W1–W8 crash-window pins; high-water REC-185) → charter POPULATED.**
**Detail:** Assessments: `context/assessments/2026-06-12_Research_14B/14A/15_PM_Assessment.md`. Headlines: ledger-gap dossier = M5-C flagship (frenck "out of scope" + installed verify-retry hacks); Apache 2.0 CONFIRMED by fork-pattern evidence (governance posture refined); Doc 07's Locked text richer than assumed (zombie-Run finalization, per-idempotency crash recovery — several "gaps" were CLOSED-UNTESTED); anti-requirements registered (no engine retry, no templating DSL, no forced migration, never lead with commodity encryption). Raw returns → docs `research/returns/`; consumed briefs → archive.
**Action needed:**
- **Nick:** (1) **C8/C9 ratification** — C8 is M7 entry-gate row 2 and now carries R14-A's external evidence (Hubitat "what turned on my light?" = the top provenance complaint). (2) **The M6.3-vs-M7 ordering call** at the W25/W26 boundary (Lane-2 evidence gates: microbench Jun 17 + interviews). (3) **M5-C Increment 1** — the content backlog is now evidence-rich (merged disposition §2e). (4) Commit both repos (messages in the session report). (5) Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` (still present). (6) Mirror sync (Check 9, the one file, unchanged).
- **PM (next session):** **draft the M7 entry-gate AMD block** (6 families, merged disposition §2a) + DOCS review prompt; strategy-refresh drafts (§2f) same session or following.
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 awaits the 5-row entry gate (charter).

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (W0 + R14/R15 briefs + M7/M8 skeleton — superseded by the pointer above)
**Topic:** **W0 Research-4 currency delta COMPLETE (zero open DQs; REC-39 re-anchored — NEW type-residency finding: `RunId`/`RunStatus`/`PendingStatus` must not appear in M7 event payloads, flatten-or-relocate = AMD-block decision) · 3 research briefs AUTHORED + self-reviewed ×3, READY TO DISPATCH · M7/M8 charter SKELETON on disk.**
**Detail:** Full record: pm-handoff "This Session" (2026-06-12). Artifacts: `context/assessments/2026-06-12_W0_Research_4_Currency_Delta.md`; `context/instructions/Research_14A_*.md` / `Research_14B_*.md` / `Research_15_*.md`; `context/planning/2026-06-12_M7-M8-charter-skeleton.md`. The MODULE_CONTEXT permit-count defect ("27"→30) corrected in core (docs-only).
**Action needed:**
- **Nick:** (1) **Veto-or-default `context/planning/2026-06-11_M7-blueprint_research-architecture.md` + dispatch all 3 briefs simultaneously** (R14-A/R14-B → DOCS Project, web search required; R15 → generic deep research permitted for evidence, disposition in-Project). (2) Commit both repos — messages in the session report. (3) Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` (a VM-created lock the sandbox cannot delete; renamed harmless). (4) Standing: Pi-4 microbench (Jun 17) + interviews; M5-C Increment 1; C8/C9 ratification (C8 = M7 entry-gate row 2); mirror sync (Check 9, one file).
- **Coder:** no change — next WU = M6.3 ONLY when its triple gate clears; M7.1 is research-gated + entry-gated (see the charter skeleton). Nothing dispatched this session.
- **PM (next session):** assessments as returns land — serialized, fold order R14-B → R14-A → R15; then populate the charter + draft the M7 AMD block.

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.2 delivery — gate DEFERRED to Nick)
**Topic:** **M6.2 (SecretStore + ScopeKeyManager + PayloadCipher seam) COMPLETE — files produced; BUILD GATE DEFERRED (commands in coder-handoff `### DEFERRED — M6.2`). Three [REVIEW] deviations need PM rulings; one queued post-MVP AMD candidate.**
**Detail:** Full record in coder-handoff (2026-06-11 M6.2 entry) + the Completion Report returned to Nick. Zero-new-edge property re-verified in both directions; NO module-info/Gradle/catalog/projectionVersion/schema/event-manifest change anywhere. The Doc 15 §8.1 stale row ("ScopeKeyManager … implement PayloadCipher") was honored as stale per CARRY 1/§3.8 — reconfirming the parked currency nit for the next Doc-15-touching amendment, alongside the [R6] `scope_keys` placement nit.
**Action needed:**
- **Nick:** run the M6.2 gate (two commands in the handoff DEFERRED section); commit on GREEN.
- **PM (WUCP Phase 2):** rule on **R-1** (the §3.5 write path REJECTS `!secret`/`!env`-bearing documents fail-closed — resolving in the write path would re-emit plaintext secrets into the file, INV-SE-03; consequence: UI/API mutation of tag-bearing configs is impossible until a tag-preserving emitter exists → propose queueing a post-MVP AMD in the REC-138 family), **R-2** (PayloadCipher threading deliberately STOPS at `HomeSynapseCore`'s held nullable field — `PersistenceFactory` is untouched; forwarding is the M6.3 wiring step), **R-3** (`Main.main()` is still the Phase-2 stub, so DP-6's "wire" is discharged as the package-private `Main.payloadCipher(Path, Clock)` factory + the app-level bridge round-trip test; full bootstrap rides the app-bootstrap milestone).
- **PM (M6.3 instruction):** the M6.2 surface M6.3 builds on: `ScopeKeyManager.encrypt/decrypt` + `Main.payloadCipher` + `HomeSynapseCore` 5-arg ctor; remember the random-IV-here / counter-nonce-there fence (OR-M6-NONCE) and that key versions are never reused past destroyed rows (`dek_ref` stability).

## 2026-06-11 [PM (Cowork) → Nick, Coder]  (M6.2 WUCP Phase 2 — superseded by the 2026-06-12 pointer above)
**Topic:** **M6.2 COMPLETE — gate GREEN (82 targeted + full check 147, ZERO gate-fix rounds — an M6 first) → PM WUCP Phase 2 APPROVE. R-1/R-2/R-3 ruled ACCEPT; 10 [INFO] accepted; zero-new-edge + manifest-zero-change source-verified. COMMITTED **`7c73c91`** (hivemind `d161ca9`; reconciled EOD). M6 = M6.1 ✓ M6.4 ✓ M6.2 ✓ — only M6.3 (triple-gated).**
**Detail:** Full verification + rulings record: pm-handoff `## Open Risks` M6.2 entry (2026-06-11). R-1 fail-closed tag-bearing `write()` ACCEPT (resolving on write bakes plaintext secrets into the emitted file — the [R4] class: leakage irreversible, restriction reversible; **NEW FUTURE-AMD: tag-preserving YAML emitter, post-MVP**; M10 design-input note). R-2 ACCEPT (`PersistenceFactory` untouched — extending the frozen gateway IS M6.3's wiring step). R-3 ACCEPT (DP-6 discharged: `Main.payloadCipher` factory + 5-arg ctor + bridge round-trip test; live bootstrap rides M13). ⚠ The Coder's M6.2 cross-agent note was lost to a mount-lag write overlap (Coder closeout vs PM rotation, same hour) — RECONSTRUCTED below; checklist-vs-file verification is now a standing Phase-2 step.
**Action needed:**
- **Nick:** (1) **COMMIT homesynapse-core (M6.2)** — message in the session report. (2) **COMMIT nexsys-hivemind** (closeout + hygiene pass; all moves are in-tree renames — plain `git add -A`; the consumed M6.2 instruction already sits in `coding-instructions/archive/` with its EXECUTION RECORD). (3) Skill-mirror sync still pending (Check 9). (4) **M6.3's evidence gates = the M6 critical path** (Pi-4 microbench OQ-15-2 + AMD-86 §3 interviews — both yours). (5) **Research 14/15: veto-or-default** `context/planning/2026-06-11_M7-blueprint_research-architecture.md`. (6) B2 C8/C9 — C8 `actorRef` is now on the M7 critical path.
- **Coder:** next WU = **M6.3 ONLY when its triple gate clears — do NOT start.** Nothing else is open.
- **PM (next session):** ~~Step-0 (M6.2 commit)~~ DONE EOD; W0 Research-4 currency delta; author the 3 R14/R15 briefs; M7/M8 charter skeleton on the returns. Session prompt: `context/handoff/2026-06-12_W0_R14-briefs_session_prompt.md`.
- **Status (2026-06-11 EOD):** Nick items (1)/(2) DONE — core `7c73c91` + hivemind `d161ca9`, pushed, reconciled; (3) mirror sync STILL pending (Check 9 — one file, now incl. the 2026-06-11 format additions); (4)–(6) standing. **Coder feedback folded:** 4 format-skill additions (internal-consumer survey; table-governs-over-prose; minimum read set; verbatim external-standard vectors), the NO_DIRECT_TIME_ACCESS-reach OQ, pm-lessons entry. **Request to Coder:** write future handoff entries as short PARAGRAPHS, not single mega-lines — 270KB single-line entries defeat line-based pagination (the A1 rotation fixed the backlog; entry style fixes the source).

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.2 delivery — RECONSTRUCTED by the PM from the completion report after the mount-lag loss; resolved same-day by the pointer above)
**Topic:** **M6.2 COMPLETE — files produced (14 created + 15 modified incl. 4 MODULE_CONTEXTs), BUILD GATE DEFERRED to Nick (commands in coder-handoff §DEFERRED — M6.2; baseline `62a81e6`). Three [REVIEW]s need PM ruling: R-1 `write()` fail-closed on tag-bearing docs; R-2 cipher threading stops at `HomeSynapseCore`; R-3 `Main.main()` stays the Phase-2 stub. 10 [INFO]. 12-agent adversarial review: 0 blockers/majors; 5 minors fixed in-tree.**
**Action needed:** Nick gate run → PM Phase 2 rulings → commit. *(All discharged same day — see the pointer above.)*

## 2026-06-11 [PM (Cowork) → Nick, Coder]  (M6.4 WUCP Phase 2 — superseded same-day by the M6.2 pointer above)
**Topic:** **M6.4 COMPLETE — gate GREEN (full check, 147 tasks) → PM WUCP Phase 2 APPROVE. R-A/R-B/R-C/R-D ruled ACCEPT (R-B condition source-confirmed); `CONFIG_SECTION_RELOADED → [SYSTEM]` confirmed; GF-1 verified applied exactly as ruled. COMMITTED `62a81e6` (sha reconciled 2026-06-11 at Step-0); M6.2 DISPATCHED (return in flight).**
**Detail:** Full record in pm-handoff "This Session" (2026-06-11). Survey-lesson logged: P2 survey + behavioral publish-count pins category — **skill source updated** (`project-manager/references/coding-instruction-format.md`), mirror sync pending (Check 9 STALE until Nick syncs — expected). M6.2 needs NO instruction edit (G7 adaptive; no R1 item exists in it — grep-verified). EXECUTION RECORD appended to the M6.4 instruction; Nick `git mv`s it to `coding-instructions/archive/` at commit (no tombstone this time).
**Action needed:**
- **Nick:** (1) **Commit homesynapse-core (M6.4)** — message in the session report. (2) **Commit nexsys-hivemind closeout** — include `git mv context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md context/coding-instructions/archive/`. (3) **Run the skill mirror sync** (one changed file). (4) **Dispatch M6.2**: instruction `context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`; dispatch line: "Ruling [R6] CONFIRMED; R1 is DONE in M6.4"; baseline = the new M6.4 sha (its G7 gate adapts). (5) **Microbench + interviews are now the M6 critical path** (M6.3's gates — both Nick-paced).
- **Coder:** next WU = **M6.2** on Nick's dispatch (refuse-to-close pointer in coder-handoff stands).
- **PM (next session):** sha-reconciliation for the M6.4 commit (Step-0); W25 charter (due Jun 14/15); the post-M6 deliberation beat (M7/M8 automation runway + Research-14 scoping); rotate this file's resolved 06-10/06-11 trail below `## Archived`.
- **Status (2026-06-11, M6.2-return session Step-0):** Nick items (1)/(2)/(4) DONE — core `62a81e6`, hivemind `82c8068`, M6.2 dispatched; item (3) skill-mirror sync still PENDING (Check 9 STALE — expected); item (5) standing. PM Step-0 sha-reconciliation + this file's rotation DONE; W25 charter + deliberation beat run this session.

## Archived

Entries below were superseded/resolved and moved under this separator (2026-06-04 PM session-start reconciliation; extended 2026-06-09 — the resolved 2026-06-05/2026-06-06 pointer trail archived at the AMD-66..71 ratification closeout per the C18 housekeeping carry; **extended 2026-06-10 — the resolved 2026-06-07 → 2026-06-10 M5-A/M5-D/M6-entry-gate/M6.1 trail archived at the M6.1 sha-reconciliation + cleanup session**; **extended 2026-06-11 — the resolved 06-10/06-11 Research-13/M6.4 trail archived at the M6.2-return session Step-0; only the CURRENT POINTER above stays active**; content preserved verbatim).

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.4 ⟳ gate-fix round 1 — RESOLVED: gate GREEN, Phase 2 APPROVE, see the pointer above)
**Topic:** **M6.4 GATE-FIX ROUND 1 APPLIED per the PM diagnosis-and-ruling — one file (`ConfigLayoutTest.composeAfterMerge`), implementation untouched. R-B condition CONFIRMED against source. Gate RE-DEFERRED to Nick: FULL `./gradlew check` (expect 147 tasks).**
**Detail:** GF-1 applied exactly as ruled — the M6.1a-era `rootDrafts.hasSize(1)` pin → `hasSize(2)` with order-agnostic type-filtering (one `ConfigErrorEvent`: path `endsWith("channel")`/`"ERROR"`/`"15"`; one `ConfigValidationCompletedEvent`: `severityCounts containsEntry("ERROR", 1)`); model assertions kept; dated comment cites the M6.4 R1/DP-10 pin bump. **R-B:** `writeBackMigratedDocument` already implements the required ordering — `copyBackup` then `writeAtomically` are sequential statements in ONE try (`StandardConfigurationService.java:888-889`, catch :895); a backup-copy failure skips the rename (WARN + continue in-memory), so the original is never replaced without its on-disk backup. No code change. Defensive module-wide `rootDrafts` sweep found no other stale pin (agrees with the 169/1 gate evidence). Rulings recorded in the handoff: R-A/R-C/R-D ACCEPTED as implemented; R-B ACCEPTED with the condition confirmed. Lesson appended (publish-behavior changes require a module-wide publisher-assertion sweep, not just the primary test class).
**Action needed:**
- **Nick:** re-run the FULL gate — `./gradlew check` — against the working tree (incl. GF-1) on `7e0bce8`; the halted round-1 runs never reached event-model/persistence tests, `-Werror` compilation of the new files, spotless, or ArchUnit. Commit on GREEN.
- **PM (WUCP Phase 2, unchanged):** the four [REVIEW]s are now ruled (recorded in handoff + here); remaining Phase-2 items: `CONFIG_SECTION_RELOADED → [SYSTEM]` category confirmation, M6.2 instruction updates (drop R1 item; adapt its service G-gate to post-M6.4 state), Open-Risks tracking of this gate.

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.4 delivery session — gate-fix round 1 applied same day, see the pointer above)
**Topic:** **M6.4 (hot-reload atomic swap + UI/API write path) COMPLETE — files produced, BUILD GATE DEFERRED to Nick (commands in `coder-handoff.md §DEFERRED — M6.4`; tree layered on `7e0bce8`). Four `[REVIEW]` deviations need PM ruling at WUCP Phase 2. Next WU = M6.2 (its instruction needs two small updates).**
**Detail:** G1–G8 PASS at `7e0bce8`; [R4]/[R5] applied as confirmed; P2 survey re-run by own grep (baseline correct + one addition: the `EventTypes` line-15 class-Javadoc count — folded in). DP-1..DP-12 implemented as settled; DP-10 (per-ERROR `config_error` + §12.4 `x-sensitive` fence) and DP-11 (§3.7 step-7 write-back + pre-migration backup + token refresh) land in `load()`. New: `ConfigSectionReloadedEvent` (E70-1-flat) + full manifest fan-out (54→55/23→24/35→36; `HomeSynapseCore` verify-no-edit confirmed); package-private `SchemaAnnotationIndex`/`ConfigModelDiff`/`AtomicYamlWriter`; `ConfigChangeSet` filters. The P5 shift-left compile is permission-blocked in this environment — the NEW third-party surface (snakeyaml-engine Dump/DumpSettings/FlowStyle + `exports …v2.common`) is javap-verified from the cached 2.9 jar per the 2026-06-10 lesson.
**Action needed:**
- **Nick:** run the M6.4 deferred gate (`./gradlew :config:configuration:check :core:event-model:check :core:persistence:check` then full `./gradlew check`) against the working tree on `7e0bce8`; commit on GREEN.
- **PM (WUCP Phase 2):** rule the four `[REVIEW]`s — **R-A** first-write backup named `homesynapse.yaml.bak.{yyyyMMdd'T'HHmmss'Z'}` (AMD-71 root-document name + Windows-safe ISO-8601 BASIC) vs DP-6's Doc-06-era literal `config.yaml.bak.{ISO8601}`; **R-B** migration write-back IO failure = WARN + load continues on the in-memory migration (file-state invariant holds; alternative is fail-startup); **R-C** a post-flush listener rejection inside `write()` surfaces as `IllegalStateException` wrapping `ConfigurationReloadException` (`write()` cannot throw the checked reload exception; file written, model prior); **R-D** the reload diff attributes nested-leaf changes to the IMMEDIATE section only → an AMD-66 listener at an ancestor path is not consulted for nested-leaf-only changes (M9 supervisor listener-registration guidance needed; MODULE_CONTEXT gotcha posted). Also confirm the `CONFIG_SECTION_RELOADED → [SYSTEM]` category (M4.C precedent). Track the deferred gate in pm-handoff Open Risks.
- **PM (M6.2 instruction updates before issue):** (1) drop its R1/`config_error` item — DONE in M6.4; (2) its G-gate on `StandardConfigurationService` must expect the post-M6.4 state (reload/write implemented; 3 new package-private config classes; `reloadLock`); the tag-resolution seam (`YamlLoader` tag-constructor map) is unchanged by M6.4 as anticipated.
- **Docs lane (non-blocking):** Doc 06 §3.5's `config.yaml` naming predates AMD-71's `homesynapse.yaml` (ties to R-A); the §3.6/§4.5 `config_error` text can now cite the implemented §12.4 fence semantics.

## 2026-06-10 [PM (Cowork) → Nick, Coder]  (Research-13-return session, late)
**Topic:** **Research 13 ASSESSED (A−) → M6.4 + M6.2 coding instructions AUTHORED, both ISSUE-READY. Three dispatch rulings await Nick's veto-or-default. Preflight PASS (Check 9 REAL diff — mirrors byte-identical); M6.1 tombstone deleted in-tree. No open deferred build gates.**
**Detail:** Assessment `context/assessments/2026-06-10_Research_13_PM_Assessment.md`; raw return archived → docs `research/returns/2026-06-11_Research_13_Config_System_Market_Superiority.md` (Step 0). 11 RECs: 130/131(n)/132/133/135(n) folded into **M6.4** (`context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md` — also carries the 3 pinned obligations: R1 `config_error` publish w/ §12.4 fence, `config.section_reloaded` FULL P2 fan-out 54→55/23→24/35→36 + survey re-run, CoreSchema-on-re-parse DP-12, + R2 §3.7 write-back/backup); 140 folded into **M6.2** (`context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md` — CARRY 1 E2 bridge verbatim, zero-new-edge re-verified, HKDF/ScopeKeyManager/`config_secrets`, AMD-68 `setAll`, `!secret`/`!env` stage-3, §8.2 freeze-gate test); 134 RE-BUCKETED ALREADY-COVERED (Doc 06 §3.6, shipped); 136 + 138 (+ the PM-surfaced `ConfigChangedEvent`-vs-§12.4 tension) → FUTURE-AMD queue; 137/139 → M10/M11/Doc 13 parking. REJECT bucket genuinely empty.
**Action needed:**
- **Nick:** (1) ~~**Commit nexsys-hivemind + homesynapse-core-docs**~~ **DONE 2026-06-10** — hivemind `52a9b88`, docs `919e72f`; core untouched (`7e0bce8` — the M6.4 baseline holds exactly). (2) ~~Rule at dispatch~~ **RULED 2026-06-10 — Nick delegated the call pre-dispatch; PM CONFIRMED all three defaults: [R4]** M6.4 reload publishes `config.section_reloaded` ONLY — frozen value-bearing `ConfigChangedEvent` stays unpublished (Locked §12.4 is categorical; immutable-log leakage is irreversible, non-publication is reversible); **[R5]** section_reloaded metadata = DIAGNOSTIC/SYSTEM/null-eventTime (the validation_completed-sibling consistency rule); **[R6]** M6.2 `scope_keys` = config-side file per Locked §3.8 (the §4.1 table is JPMS-unreachable from config in both directions — currency nit parked; M6.3 backup/restore co-design may revisit event-scope DEK placement, `config_secrets` = one-row migration). (3) **Dispatch M6.4 first** (PM sequencing rec — both WUs touch `StandardConfigurationService`), then M6.2. (4) FUTURE-AMD queue when convenient: REC-136 reference-integrity; REC-138 provenance + `ConfigChangedEvent`/§12.4 reconciliation. Nick-paced unchanged: Pi-4 microbench (must not slip past M6.2), interviews, **M5-C floor (P6 — Research 13's §1/§3.1 superiority material is website-grade input)**, B2 C8/C9.
- **Coder:** next WU = **M6.4** on Nick's dispatch (read the instruction's STOP-gates + Settled Decisions first; baseline `7e0bce8` — re-verify at issue). M6.2 follows (its G7 gate adapts to post-M6.4 state).
- **PM (next session):** WUCP Phase 2 on M6.4 when it lands (then M6.2). M6.3 stays triple-gated — do NOT issue.

## 2026-06-10 [PM (Cowork) → Nick, Coder]  (superseded same-day by the pointer above — RESOLVED 2026-06-10; archive below the separator at next rotation)
**Topic:** **SHA-RECONCILIATION DONE (M6.1a = `9035110`; M6.1 gate entries CLOSED, no open deferred gates) + WUCP archival pass + docs-lane corrections applied + Research 13 (Config System Market Superiority) brief AUTHORED — ready to dispatch. Preflight STALE→PASS.**
**Detail:** Step-0: `9035110` ("M6.1a: YamlLoader…", on `b7bc65c`) filled across coder-handoff/pm-handoff/snapshot/backlog/charter/W24/this pointer; hivemind closeout `40de3ec` confirmed committed. Archival: this file's 06-07→06-10 trail moved below `## Archived` (only this pointer active); the consumed M6.1 instruction moved to `coding-instructions/archive/` with an EXECUTION RECORD — **a tombstone sits at the live path (VM down; file tools can't delete)**; pm-handoff pre-M5-window records rotated to `archive/pm-handoff-2026-05.md`/`-2026-06.md`. Docs-lane (ONE docs commit): Doc 06 §3.2 `schemas/` correction note + §3.7 AMD-67 amendments-in-force banner; Doc 01 §4.4 +`config.validation_completed` row; cloud-scalability :56 Rule-5 staleness note; **Doc 15 untouched**. Core: config MODULE_CONTEXT M6.1 status line brought current (tiny docs-only commit). **Research 13 brief:** `context/instructions/Research_13_Config_System_Market_Superiority_Brief.md` — RQ1 HA failure catalog / RQ2 cross-ecosystem Mom-Test / RQ3 runtime non-breakage gap table / RQ4 layout churn / RQ5 local-cloud-hybrid (now-or-never vs deferrable); REC-130+; mandatory disposition table (ALREADY-COVERED / M6.2-or-M6.4 OBLIGATION / FUTURE AMD / POST-MVP UI-cloud / REJECT); quote-back gate + verbatim module-info at `9035110` embedded; guardrails: ratified set inviolate, no M6.3 creep, pinned M6.4 obligations not duplicated. **Numbered 13 per Nick** (Research 12 = consumed Zigbee return); registered in research-agenda.md (+ REC-130 block).
**Action needed:**
- **Nick:** (1) **commit the three repos** (messages in the session report): nexsys-hivemind (cleanup + brief — include the deletion of the `coding-instructions/M6.1_Config_Pipeline.md` tombstone via `git rm`), homesynapse-core-docs (the 4 corrections), homesynapse-core (MODULE_CONTEXT line). (2) **Dispatch Research 13 → the DOCS Project** (web search required; if its web reach is too shallow, split the evidence-gathering to a generic deep-research run per the brief masthead). (3) Spot-check the dual-skill mirrors at next sync (Check 9 ran evidence-based — VM down). Nick-paced lanes unchanged: Pi-4 microbench (OQ-15-2 — must not slip past M6.2), interviews, **M5-C floor (P6 — still not started)**, B2 C8/C9.
- **PM (next session):** assess the Research 13 return (6-step; fold the disposition table) → author **M6.4** (pinned obligations: R1 per-ERROR `config_error` publish + `config.section_reloaded` P2 survey re-run + CoreSchema on the reload re-parse + any Research-13 obligation rows) and **M6.2** (E2 bridge verbatim + zero-new-edge re-verify + `!secret`/`!env` stage-3 + AMD-68 `setAll`). M6.3 stays triple-gated.
- **Coder:** no open WU — next is M6.2 or M6.4 on the PM instruction (unchanged).


## 2026-06-10 [PM (Cowork) → Nick, Coder]  (superseded same-day — archived 2026-06-10 at the follow-up cleanup session)
**Topic:** **M6.1 COMPLETE (WHOLE) — M6.1a + gate-fix round 1 GREEN (Nick: 27 + 147 tasks); PM WUCP Phase 2 → APPROVE; M6.1a COMMITTED `9035110` (sha reconciled 2026-06-10, follow-up session). R1/R2/R3 ruled ACCEPT.**
**Detail:** Contract verification at Phase 2: config `module-info` = the 2026-06-10 ruling **exactly** (5 third-party non-transitive `requires`, dated comment, HomeSynapse edge set untouched — the §7-embed CARRY's verification half is CLOSED); `build.gradle.kts` +jackson.databind/+slf4j.api lockstep ✓; `CoreSchema` on BOTH LoadSettings ✓; Rule 5 drops config while Rule 4 layering still lists it ✓. **[REVIEW] rulings:** R1 ACCEPT — the Doc 06 §3.6 per-ERROR `config_error` startup publish folds into **M6.4** as an instruction obligation (gap must not silently persist); R2 ACCEPT — §3.7 step-7 migration write-back + pre-migration backup ride the atomic-write machinery (M6.2/M6.4 seam; idempotent re-migration is safe interim); R3 ACCEPT — `config.validation_completed` firing on a FATAL pass is the faithful AMD-70 once-per-pass reading. **Docs-lane carries (route at next docs touch):** Doc 06 §3.2 `schema/`→`schemas/`; Doc 06 §3.7 pre-AMD-67 embedded code; cloud-scalability-analysis :56 "configuration … filesystem-agnostic" (stale vs the Rule-5 ruling); DP-5 instruction prose (real-mtime correction). **D4 carry (M13/composition root):** `SystemId` must be sourced/generated BEFORE config load — config publishes events with the system subject at startup step 1.
**Action needed:**
- **Nick:** ~~commit homesynapse-core + nexsys-hivemind~~ **DONE 2026-06-10** — core `9035110`, hivemind `40de3ec`; the M6.1a sha was reconciled into all trackers by the PM at the same-day follow-up cleanup session. Docs repo already clean/committed.
- **PM (next session):** the config research/open-questions beat (Nick-directed), then the M6.4 instruction (R1 obligation + `config.section_reloaded` P2 survey re-run + the CoreSchema-on-reload gotcha) and M6.2 (E2 bridge; zero-new-edge re-verify). M6.3 stays triple-gated.
- **Coder:** no open WU — next is M6.2 or M6.4 on the PM instruction.

## 2026-06-10 [Coder (Claude Code) → Nick, PM]  (archived 2026-06-10 — M6.1a gate went GREEN + committed `9035110`)
**Topic:** **⟳ M6.1a GATE-FIX ROUND 1 APPLIED (2 PM-ruled defects from Nick's first gate run) — gate RE-DEFERRED to Nick, same commands. Scope: exactly the two fixes, nothing else.**
**Detail:** **(1)** `YamlLoaderTest.nullValuedKeysDropped` FAILED — snakeyaml-engine's DEFAULT LoadSettings schema is the JSON schema (`~` = the string `"~"`, not null); LTD-09 mandates YAML 1.2 → `YamlLoader` now sets `.setSchema(new CoreSchema())` on BOTH `rootSettings()` and `includedSettings(...)` (lockstep — root and included files must resolve scalars identically); class Javadoc safety-properties updated (`~`/`null`/empty → null = unset); `CoreSchema` javap-verified in the cached 2.9 jar (public no-arg ctor; `…v2.schema` exported by the explicit module — NO module-info change); the test untouched (correct). New config MODULE_CONTEXT gotcha pins the requirement for every future LoadSettings site (M6.4 reload re-parse). **(2)** `HomeSynapseArchRulesTest.noDirectFilesystemInCore` FAILED — Rule 5 `NO_DIRECT_FILESYSTEM_IN_CORE` was stale vs ratified AMD-71: `com.homesynapse.config..` removed from its package list ONLY (event/device/state/automation stay; Rule 4 untouched); ruling recorded in banner + Javadoc (config = designated filesystem consumer on the composition-root-injected configDir `Path`; AMD-71-INV-01 canonicalization-containment, `ConfigLayoutTest`-pinned, is the compensating control). **Ruled grep survey (report-only):** one doc enumerates Rule 5's scope incl. config — `homesynapse-core-docs/research/homesynapse-core-cloud-scalability-analysis.md:56`, now stale (docs lane; NOT edited). All other mentions are name-only (Knowledge Primer :385, custom-instructions :35, 2026-05-28 relay :400, 2026-05-16 top-down analysis :305/692, archived 2026-04-08 state report :235).
**Action needed:**
- **Nick:** re-run the M6.1a gate (same commands, coder-handoff `### DEFERRED — M6.1a`): `./gradlew :config:configuration:check` then full `./gradlew check`; commit on GREEN.
- **PM:** at M6.1a WUCP Phase 2, verify the two gate-fix files vs the rulings; route the stale cloud-scalability-analysis line (and the prior D1–D4) to the docs lane.
- **Coder (next session):** unchanged — M6.2 on the PM's instruction after gate + commit.

## 2026-06-10 [Coder (Claude Code) → Nick, PM]
**Topic:** **M6.1a (load/validate) COMPLETE per the RULING ADDENDUM — files produced, build gate DEFERRED to Nick. M6.1 is now delivered WHOLE (M6.1b committed `b7bc65c` + M6.1a layered on it). 3×[REVIEW] deviations + 4 doc-accuracy findings for the PM/docs lane.**
**Detail:** All three rulings applied exactly (5 non-transitive third-party `requires` + 2 Gradle lockstep lines — catalog already had both; DP-4 `List` ctor + duplicate-path `IllegalArgumentException` + the `ConfigurationChangeListener` Javadoc reword; DIAGNOSTIC/SYSTEM/null-eventTime/`publishRoot`/system-subject publish metadata, asserted in `StandardConfigurationServiceTest`). Delivered: `YamlLoader` (safe YAML 1.2; one-level `!include` — nested = structural unknown-tag FATAL; `toRealPath()` containment under `integrations/`, fail-closed, path named), `JsonSchemaCompositeValidator` (allErrors; `required`→FATAL / `additionalProperties`→WARNING / else ERROR with `appliedDefault`; networknt 1.5.6 message semantics **bytecode-verified via javap** — the in-sandbox runtime probe was permission-denied), `StandardSchemaRegistry` (ReentrantLock; inline fragments; DP-1 `schema_version` const-major property; **declared-pair ctor must match the service's — composition-root contract, new MODULE_CONTEXT gotcha**), `StandardConfigurationService` (§3.1 pipeline; AMD-67 chain per the `ConfigMigratorChainTest` reference semantics, in-memory, gap/newer-major FATAL; DP-2 ERROR→default-revert / FATAL→`ConfigurationLoadException`; event once per completed validation pass incl. FATAL passes; `schemas/config.schema.json` cache; `reload()`/`write()` staged UOE→M6.4), 4 test classes covering the full AMD-71 §5 table (symlink row `Assumptions`-guarded) + AMD-66 duplicate rejection + publish metadata + all version/migration edges, + a `RecordingEventPublisher` test helper. P2 survey: CLOSED at M6.1b, nothing touched — config module only.
**[REVIEW] deviations (PM ruling wanted):** **R1** — Doc 06 §3.6 mandates a per-ERROR `config_error` diagnostic event at startup; NOT implemented (outside the M6.1 instruction's scope, which names only `config.validation_completed`). The `ConfigErrorEvent` record exists and is registered — recommend folding the publish site into M6.4 or a micro-WU; decide explicitly so the Doc-06 contract gap doesn't silently persist. **R2** — Doc 06 §3.7 step 7 (persist migrated config back to disk + pre-migration backup) deferred to the milestone that owns atomic writes (M6.4): AMD-67 §6 fences migration execution to the chain contract, no production migrator exists, and idempotency makes per-load re-migration safe. **R3** — `config.validation_completed` also fires on a FATAL validation pass (before the throw): the faithful "once per validation pass" AMD-70 §4 reading and observability-friendly; recommend ACCEPT (parse-stage aborts publish nothing — no pass ran).
**Doc-accuracy findings (docs lane / INV-GA-02 pipeline, not Coder):** **D1** — Doc 06 §3.2 step 4 writes the composed schema to `/etc/homesynapse/schema/` (singular); AMD-71 §2.1 ratified `schemas/` (plural). The AMD wins; Doc 06 needs a correction note. **D2** — Doc 06 §3.7's embedded code still shows pre-AMD-67 shapes (`fromVersion()/toVersion()`, single-int `schema_version`, the un-renamed migration `ConfigChange`); consider the amendments-in-force banner treatment at the next Doc 06 touch. **D3** — M6.1 instruction DP-5 says `loadedAt`/`fileModifiedAt` are both stamped "from an injected Clock", which contradicts the frozen `ConfigModel` Javadoc + §3.5 (the mtime IS the optimistic-concurrency token; a clock stamp would make every future write reject). Implemented: real file mtime when the file exists, clock only for the absent-file zero-config case. Instruction prose should be corrected so a future reader doesn't "fix" it. **D4** — `SystemId` Javadoc says it is "recorded in the configuration store", but config load is the FIRST startup step (Doc 12) and now publishes events carrying `SystemId` as subject — the M13 composition root must source/generate the SystemId BEFORE config load; flag for the Doc 12/M13 wiring design.
**Action needed:**
- **Nick:** run the M6.1a deferred gate (commands in coder-handoff `### DEFERRED — M6.1a`): `./gradlew :config:configuration:check` then full `./gradlew check`; commit on GREEN. Note `ConfigLayoutTest.symlinkEscapeRejected` SKIPS (not fails) where symlinks need privileges.
- **PM:** WUCP Phase 2 on M6.1a — verify the module-info matches the ruling exactly (5 directives, nothing more); rule R1/R2/R3; route D1–D4 to the docs lane; then author M6.2 (its instruction should pick up `!secret`/`!env` stage-3 resolution — the YamlLoader tag-constructor map is the seam — and the R1 decision).
- **Coder (next session):** M6.2 on the PM's instruction (refuse-to-close pointer in coder-handoff).

## 2026-06-10 [PM (Cowork) → Nick, Coder]
**Topic:** **M6.1b WUCP Phase 2 → APPROVE (gate GREEN 2026-06-10: 57 + 147 tasks, Nick; commit pending — messages handed over, 3 repos). M6.1a escalation UPHELD + RULED by Nick → UNBLOCKED.**
**Detail:** Preflight PASS (Check 9 unverifiable in-session — sandbox VM down; no skill-tree files touched since the 2026-06-09 clean check; Check 11 = the review). Source-verified M6.1b vs the ratified AMDs (not the report): config `module-info` byte-identical to `6c6dd33`; AMD-66 §2.1 verbatim listener shape; AMD-67 shapes + both AMD-54 cross-ref halves; AMD-70 flattened payload (no config type in event-model) + `[SYSTEM]` sibling-consistent (PM CONFIRMS); P2 pins 53→54/22→23/34→35 empirically closed by the GREEN gate. **Rulings (Nick, 2026-06-10):** (1) module-info — the five third-party non-transitive `requires` APPROVED (`org.snakeyaml.engine.v2`, `com.networknt.schema`, `com.fasterxml.jackson.core`, `com.fasterxml.jackson.databind`, `org.slf4j`) + 2 Gradle lockstep lines; the AMD-66/67/68/70/71 §7 ruling-correction notes + the AMD-66 §2.4 `List`-ctor correction are AUTHORED in the docs repo this session. (2) DP-4 → `List<ConfigurationChangeListener>` ctor CONFIRMED (service builds the map, throws on duplicate section paths). (3) `config.validation_completed` publish metadata CONFIRMED: DIAGNOSTIC, `EventOrigin.SYSTEM`, null `eventTime`, `publishRoot`, system subject. All 4 `[INFO]` accepted. **RULING ADDENDUM appended to `context/coding-instructions/M6.1_Config_Pipeline.md` — it is the M6.1a issuing delta.**
**Action needed:**
- **Nick:** **commit the three repos from host git** (messages in the session report): homesynapse-core (M6.1b working tree), homesynapse-core-docs (AMD ruling corrections), nexsys-hivemind (closeout + addendum). Fill the M6.1b sha into the trackers at commit. Then hand M6.1a to the Coder.
- **Coder (next session):** **M6.1a = GO on Nick's hand-off** — read the RULING ADDENDUM first; the M6.1b shapes are frozen; update the `ConfigurationChangeListener` Javadoc registration line per the `List` ruling; the P2 survey is already closed for `config.validation_completed`.
- **PM (at M6.1a Phase 2):** verify the module-info matches the ruling exactly (5 directives, non-transitive, nothing more); close the §7-embed CARRY; M6.4's instruction re-runs the P2 survey for `config.section_reloaded`.

## 2026-06-09 [Coder (Claude Code) → Nick, PM]
**Topic:** **M6.1 ISSUED (Nick, direct) → SPLIT at the pre-authorized Fork-2 seam. M6.1b (access/events: AMD-67 shapes + AMD-66 listener + AMD-70 event/manifests + ScopedConfigurationAccess) COMPLETE — files produced, build gate DEFERRED to Nick. M6.1a (load/validate) BLOCKED on a `[BLOCKING]` ESCALATION: the no-new-`requires` mandate is physically uncompilable for the snakeyaml/networknt-dependent classes.**
**Detail:** **The escalation (full evidence in the Completion Report + coder-handoff `### DEFERRED — M6.1b`):** the M6.1 instruction mandates `config/configuration` `module-info.java` UNCHANGED (`requires transitive com.homesynapse.event;` only — STOP-gate row 4, Success Criterion 3, AMD-66/67/70/71 §7 verbatim embeds) while requiring `YamlLoader`/`JsonSchemaCompositeValidator`/`StandardSchemaRegistry` to be implemented inside the module on snakeyaml-engine + json-schema-validator. Source-verified: **snakeyaml-engine 2.9 is an explicit JPMS module `org.snakeyaml.engine.v2`** (multi-release `module-info.class`, jar-inspected from the Gradle cache) and **json-schema-validator 1.5.6 is an explicit JPMS module `com.networknt.schema`** (which `requires com.fasterxml.jackson.databind` NON-transitively — Jackson types do not flow through it). This build resolves library deps on the module path (proof: persistence's `module-info` declares `requires com.fasterxml.jackson.databind`/`requires org.slf4j` for its `implementation`-scoped libs and compiles GREEN at `7f44bed`). A named module cannot read the unnamed module — there is NO way to import these libraries from `com.homesynapse.config` without `requires` directives. Gradle `implementation` scope does not exempt JPMS. **The [AMD-71-A]/E71-2 substance is NOT at stake:** that ruling forbids a `config→platform` *HomeSynapse* edge (the E2-bridge zero-new-edge property); third-party non-transitive directives leave the HomeSynapse module graph untouched. **Coder recommendation (Severity [BLOCKING], M6.1a only):** approve `requires org.snakeyaml.engine.v2; requires com.networknt.schema; requires com.fasterxml.jackson.core; requires com.fasterxml.jackson.databind; requires org.slf4j;` — all non-transitive, consumed only by package-private pipeline classes (no `-Xlint:exports` leak; LTD-09 mandates these exact libraries; DECIDE-01 covers per-module slf4j), plus `build.gradle.kts` `implementation(libs.jackson.databind)` + `implementation(libs.slf4j.api)` for lockstep hygiene (jackson currently arrives transitively via networknt's POM). Docs lane: the four AMD §7 "verbatim module-info — unchanged permanently" embeds need a correction note when the ruling lands (PM/docs agent — INV-GA-02 pipeline, not the Coder). **Secondary `[REVIEW]` clarification for M6.1a:** DP-4 says the service ctor takes `Map<String, ConfigurationChangeListener>` keyed by `sectionPath()`, but AMD-66 §2.4/§5 requires duplicate-section-path rejection at construction — a `Map` param cannot carry a duplicate; suggest `List<ConfigurationChangeListener>` with the service building the map (same no-ServiceLoader substance). **M6.1b is ruling-invariant and complete** (23 files; all four STOP-gates re-verified; P2 survey closed: pins 53→54 / 22→23 / 34→35; `HomeSynapseCore`/`IntegrationTestHarness` aggregate via `Stream.of` — no pins, untouched). Doc 01 §4.4's enumeration (the EventCategoryMappingTest comment cites it) needs its 35th row when the docs lane next touches it.
**Action needed:**
- **Nick:** (1) **RULE on the M6.1a module-info escalation** (recommendation above — approve the five third-party `requires` + 2 Gradle lines, or direct an alternative). (2) Run the M6.1b deferred build gate: `./gradlew :config:configuration:check :core:event-model:check :core:persistence:check` then full `./gradlew check` (commands + file list in coder-handoff `### DEFERRED — M6.1b`); commit from host git on GREEN.
- **PM:** (1) WUCP Phase 2 on M6.1b after Nick's gate (confirm the `[SYSTEM]` category + the DIAGNOSTIC-priority proposal for `config.validation_completed`'s eventual publish path; review the 2 `[REVIEW]` + 4 `[INFO]` deviations). (2) On Nick's ruling, re-issue/amend M6.1a (the held file set is enumerated in coder-handoff) + author the AMD §7-embed correction. (3) Doc 01 §4.4 +1 row (config.validation_completed → [SYSTEM]) at the next docs touch.
- **Coder (next session):** M6.1a on the ruling — the M6.1b shapes are frozen for you; `ConfigMigratorChainTest`'s reference chain semantics are the loader's spec.

## 2026-06-09 [PM (Cowork) → Nick, Coder]
**Topic:** **M6 ENTRY-GATE CLOSED — AMD-66/67/68/70/71 RATIFIED 2026-06-09 + AMD-69 DEFERRED (Option (a) confirmed); invariants §37–§41 (+8); watermark UNCHANGED AMD-87; M6.1 READY TO ISSUE (not issued); M6.3 still triple-gated. Documents-only; HEAD `6c6dd33` untouched; Doc 06/Doc 15 bodies untouched.**
**Detail:** Ratification mechanics executed per the 2026-06-09 prompt (now archived): 6 AMD mastheads/checklists/dispositions flipped (AMD-54 §10 pattern; AMD-69 carries the locked deferral-confirmation text); §37–§41 registered verbatim-from-§7 + §0.3/§17/§18 same-commit; nav-index 6 rows + preamble (reserved → filled; watermark line untouched) + Doc 06 row note; M6.1 ⛔ lifted → **READY TO ISSUE** (AMD numbers pinned; Fork-2 issue-whole); charter entry-gate CLOSED; AMD-68 half of M6.2's gate satisfied (still gated on M6.1 landing); backlog/snapshot/handoffs/W24/KB current; both consumed instruction prompts archived. **⚠ Check-11 finding:** the §17 invariant total was a copied-forward count drifted −9 vs the table — **regenerated: 152 invariants / 41 identifier categories** (was stated "135/34"); KB re-based; detail in pm-handoff. **Sandbox-truncation hazard confirmed live** (0-byte review return via the mount) — file tool stayed authoritative; no sandbox git writes. Untracked `coder.zip` + `project-manager.zip` in hivemind root — Nick's call (sync residue, not deleted).
**Action needed:**
- **Nick:** (1) **commit both repos from host git** — messages in the session report (docs: ratification flips + §37–§41 + nav-index; hivemind: gate-lift + trackers + archives). (2) Eyeball the **152/41 invariant-count regeneration** at commit. (3) Ratify **B2 C8/C9** when its independent review returns. (4) Nick-paced lanes: Pi-4 microbench (OQ-15-2 — must not slip past M6.2), energy/erasure interviews (AMD-86 §3 gate), M5-C floor increment this week (P6), GraalVM/GenZGC spikes → LTD-01.
- **PM (next session / on Nick's green light):** **issue M6.1** (instruction READY; carry the §4c Clock rule, the P2 survey, the E70-1 direction check). At M6.2 issue: re-verify the E2 zero-new-edge property + embed both module-infos verbatim. At M6.4 issue: re-run the P2 survey for `config.section_reloaded`. Next Doc-15-touching amendment: fix the §8.1 `ScopeKeyManager` row.
- **Coder:** **next WU = M6.1 — READY, awaiting formal issue** (refuse-to-close holds; do not start from this pointer alone).

## 2026-06-09 [PM (Cowork) → Nick, Coder]  (review-fold session — superseded by the ratification pointer above)
**Topic:** **AMD-66–71 DOCS review RETURNED RATIFY-WITH-EDITS (no rejects); PM source-verified the load-bearing finds + folded all 7 edits → awaiting Nick ratification. Documents-only; homesynapse-core HEAD `6c6dd33`; Locked Doc 15 inviolate.**
**Detail:** Nick ran the AMD-66–71 review (full, config/secret contract freeze). Verdicts: **66 RATIFY-AS-IS** ([AMD-66-A] PROCESS_RESTART fallback endorsed), **67/68/70/71 RATIFY-WITH-EDITS**, **69 CONFIRM-DEFERRAL** (Option (a); all 3 Doc 15 conflicts re-verified verbatim; don't re-open OQ-15-3). Per the skill discipline I **independently re-verified the three load-bearing finds at `6c6dd33` before folding** (not from the summary): **E70-1 is a REAL JPMS cycle** — AMD-70's config-event payloads were specified with config-module types (`Severity`/`ReloadResult`/`ReloadClassification`) inside event-resident records → `event→config` (config already `requires transitive event`; event-model requires only `value`+`platform`) — the AMD-52 `event↔device` class; flattened to event-resident types (`Map<String,Integer>`, `String appliedClassification`) + a type-residency rule, matching the existing all-String `ConfigChangedEvent`. **E70-2:** `secret_added`/`secret_removed` don't exist in source (only `CONFIG_CHANGED`/`CONFIG_ERROR`) — corrected. **secrets.enc:** both Locked docs name `secrets.enc` (encrypted JSON) — fixed in AMD-68/71. Also folded E67-1 (`MigrationResult` has no version field), E67-2 (`TestConfigFactory` 5 ctor sites), and E71-2 ([AMD-71-A] → composition-root `Path` injection, confirming M6.1 DP-3). Each amendment's frontmatter flipped to its verdict; review return on disk (`context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md`).
**Action needed:**
- **Nick:** (1) **commit `homesynapse-core-docs`** (the 7 folded edits + status flips) + **`nexsys-hivemind`** (review-on-disk + tracker updates + the **project-manager skill-source edit**: `coding-instruction-format.md` P2 survey gained a JPMS/contract-direction check, + pm-/coder-lessons 2026-06-09 entries). **⚠ Re-sync the `.claude/skills/nexsys-project-manager` mirror after committing** (skill source now ahead → Check 9 STALE on the PM tree until synced; the documented post-edit state). (2) **Ratify AMD-66/67/68/70/71** + **confirm the AMD-69 Tier-2 deferral** (Option (a)). (3) On ratification → the mechanics: register the 8 invariants in `Architecture_Invariants_v1.md`, add nav-index rows (watermark unchanged — all < 87), **lift the M6.1 ⛔ gate** (AMD-66/67/70/71) + the AMD-68 half of M6.2 → **issue M6.1** to the Coder. Ratify **B2 C8/C9** alongside. (4) M6.3 stays GATED (Pi-4 microbench → OQ-15-2 + the energy/erasure interviews — Nick-paced).
- **PM (next session, on Nick's ratify):** apply the invariant registration + nav-index rows + gate-lift; issue M6.1; carry the two block-level notes (Doc 15 §8.1 `ScopeKeyManager`-row nit → next Doc-15-touching amendment; M6.4 re-runs the P2 survey for `config.section_reloaded`).
- **Coder:** still BLOCKED on AMD-66/67/70/71 ratification, then **M6.1**.

## 2026-06-08 [PM (Cowork) → Nick, Coder]
**Topic:** **M6 entry-gate (config side) AUTHORED + B2 schema decisions. Preflight PASS (all 11). Documents-only; homesynapse-core HEAD `6c6dd33` (a 1-line typo fix on `7f44bed`) untouched; Locked Doc 15 inviolate.**
**Detail:** Authored the M6 config-amendment side of the entry-gate (the crypto side landed with M5-B/B1). **AMD-66–71** PROPOSED in `homesynapse-core-docs/design/amendments/` + the **DOCS-review prompt** (`context/instructions/2026-06-08_AMD-66-71_DOCS_Review_Prompt.md`). Three cross-document reconciliations drove the block, all source-verified: **(67)** REC-56's REC-41 blocker is **CLEARED by ratified AMD-54** (the `(major,minor)` adapter-config freeze) — AMD-67 adopts the same idiom on the distinct system-config-document surface; **(68)** REC-57's `SecureCredentialBundle`/`credentialsFor` are **RETIRED by ratified AMD-60** (R7/A5 — no bundle, reads stay on `ConfigurationAccess`), so AMD-68 collapses to exactly the Doc 15 §7.3 requirement — `SecretStore.setAll(Map)` atomic durable write — which is why the block is exactly the 6 reserved numbers; **(69) ESCALATION** — REC-58's Argon2id/BouncyCastle conflicts with **Locked Doc 15** (§2.3 post-MVP / §3.5 machine-local MVP root / §3.8 zero-MVP-deps + open OQ-15-3), so AMD-69 is authored **DEFERRED to Tier-2/OQ-15-3** (Nick confirms). Also: the **M6 multi-piece charter** (`context/planning/2026-06-08_M6-charter.md` — M6.1–M6.4 with the E2 `com.homesynapse.app` bridge + OR-M6-NONCE embedded verbatim, P1 epic-in-disguise fix); the **gated M6.1 instruction** (`context/coding-instructions/M6.1_Config_Pipeline.md` — ⛔ on AMD-66/67/70/71; verbatim config module-info; P2 survey; §4c reminder; the `ConfigurationProvider`→`ConfigurationAccess`+`ConfigurationService` source-correction); the **B2 decisions C8 `actorRef` + C9 energy shape** (`context/decisions/2026-06-08_B2_schema_decisions_C8_C9.md`, contract-freeze-ready). Preflight PASS — Check 3 noted HEAD `6c6dd33` (typo fix on `7f44bed`, immaterial → latest-commit bumped); Check 9 clean (no skill files touched).
**Action needed:**
- **Nick:** (1) **commit two repos from host git** — `homesynapse-core-docs` (the 6 amendments) + `nexsys-hivemind` (charter, M6.1 instruction, B2 decisions, DOCS prompt, tracker closeout) — messages in the session report. (2) **Dispatch the AMD-66–71 DOCS-Project review** (full review — config/secret contracts). (3) **Confirm the AMD-69 Tier-2 deferral** (the escalation). (4) On ratification of AMD-66/67/70/71 → **issue the gated M6.1 instruction** to the Coder; ratify the **B2 C8/C9** alongside (independent review). (5) M6.3 stays GATED (run the **Pi-4 microbench** → OQ-15-2 + the **energy/erasure interviews** → AMD-86 §3 — Nick-paced).
- **Coder:** BLOCKED on AMD-66/67/70/71 ratification, then **M6.1** (the gated instruction). M9 may now publish command-bearing `CapabilityAdded` (AMD-87 gate GREEN).
- **PM (next session):** on Nick's ratification, issue M6.1; fold any DOCS-review edits; then M6.2 (E2 bridge wiring). On Pi-4 results → the OQ-15-2 writeback (Doc 15 §9 list-tuning, NOT a re-open). On interview results → AMD-86 §3 trigger check before M6.3 freezes the write path.

## 2026-06-07 [PM (Cowork) → Nick, Coder]
**Topic:** **M5-D evidence lane — three artifacts AUTHORED (Pi-4 AES-GCM microbench spec resolving OQ-15-2; energy+erasure interview guide carrying the AMD-86 §3 re-scope-up trigger; sd_notify transport decision matrix for OR-M13-SDNOTIFY). Preflight PASS (all 11). Documents-only; HEAD `8028337` unchanged; Locked Doc 15 untouched.**
**Detail:** Ran in parallel with M5-A Part 2 (Claude Code). Preflight **PASS** (source-verified vs the repos with the Read tool; sandbox git treated as non-authoritative): HEAD `8028337` = snapshot Latest Commit; Doc 15 **Locked** / watermark **AMD-87** / AMD-86+AMD-87 **RATIFIED** (docs-source-confirmed, not from the state docs); Check 9 dual-skill `diff -rq` clean both trees; `Expectation` = the 4 permits, `@Disabled("AMD-65 pending")` at `EventPayloadCodecTest.java:317`; 21/21 MODULE_CONTEXTs populated (config at `config/configuration/`). One micro-reconciliation folded under PASS (W24 "## Current state" stale "watermark AMD-64"→**AMD-87**, source = nav-index). All three deliverables in `context/assessments/2026-06-07_M5-D_*`. **(1) Microbench spec (PRIORITY — gates the M6 at-rest WU):** runnable JMH per-event-cost + fixed-corpus throughput-soak spec + per-category pass/fail rule vs the Doc 15 §10 Pi-4 budget (~30–60 µs/event, ≥500 ev/s, intrinsics-off); candidate set = `identity`+`presence_personal` only (energy stays plaintext-at-rest per Doc 15 §3.4 — reconciled against the strategy docs that lumped energy as "sensitive"); writes back into Doc 15 §9 `encrypted_scopes` as a **list-TUNING, NOT a re-open**; STOP-and-escalate if a result implies a design change. **(2) Interview guide:** Mom-Test instrument; Part A frames the verifiable-erasure question so a qualifying launch-window "yes" → escalate to **re-open AMD-86 via the formal pipeline before M6 freezes the write path** (interviewer never edits AMD-86/Doc 15); Part B = D4 energy-shape questions → the regret-proof C9 shape. **(3) sd_notify matrix:** reject JNR/JNA (dominated); subprocess (if no WATCHDOG keepalives) else FFM/Panama; **defer the commit to M13 gated on the GraalVM C15 spike + the WatchdogSec decision**; no catalog dep added.
**Action needed:**
- **Nick:** (1) commit the nexsys-hivemind M5-D artifacts + tracker edits from host git (message in the session report). **⚠ Reconcile against host-git truth** — the sandbox working tree shows pre-existing non-session modifications on the skill trees + several `context/` files + the **docs repo** (sandbox/host desync; content source-verified-correct, Check 9 clean); this session's real adds = the 3 new `context/assessments/` files + the surgical W24/pm-handoff/backlog/snapshot/this-pointer edits. (2) **Run the Pi-4 microbench** (resolve OQ-15-2 → fixes the encrypted-scope set before M6) and **the energy/erasure interviews** (the verifiable-erasure trigger). (3) Decide whether HomeSynapse opts into the systemd **WatchdogSec** (settles the sd_notify mechanism). (4) When ready, **paste the M5-A Part 2 Claude Code output + build result** for the deferred WUCP Phase 2 review.
- **PM (M13):** carry the sd_notify recommendation into the M13 instruction. **PM (M6):** the OQ-15-2 result fixes the `encrypted_scopes` default the M6 at-rest WU writes against.
- **Guardrail:** Locked Doc 15 inviolate — the microbench tunes the *list*; only the erasure interviews can re-open AMD-86, via the formal pipeline.

## 2026-06-07 [PM (Cowork) → Nick]
**Topic:** **M5-A COMMITTED `8028337` (Step 0; preflight STALE→PASS) + M5-window crypto+codec ratification COMPLETE — Doc 15 LOCKED; AMD-86 + AMD-87 RATIFIED; on-disk watermark AMD-64→87; M5-A Part 2 UN-GATED.** _(Stopped twice on the missing full review return; folded + ratified once Nick placed it on disk — reconciled clean, one J6 refinement: E2 composition-root is `com.homesynapse.app`, not `HomeSynapseCore`.)_
**Detail:** Preflight **STALE** (Check 1 footer/session-log lag; Check 3 snapshot `Latest commit` `8ef9e9f` behind HEAD `8028337`; Check 4 backlog masthead; Check 8 pointer) — no CONFLICTED. **Step 0:** `git log` HEAD = **`8028337`** ("M5-A: platform-systemd deployment impls + FloorId Jackson registration"), on top of `8ef9e9f`, working tree clean → filled the real sha into snapshot (masthead/current-state/Staged→Committed/Latest-commit/footer + new session-log row), pm-handoff (masthead/session header/commit bullet), coder-handoff (M5-A gate DEFERRED→RESOLVED `8028337`), this pointer, backlog masthead + M5-A row, W24 masthead → **PASS restored**. **Source-verified (Read tool, HEAD `8028337`) every foldable edit ahead of the fold** — findings in the 2026-06-07 STOP report (and pm-handoff): **(E1)** the `encrypted_kek`→`encrypted_dek` rename has **THREE** Doc 15 occurrences (§4.1 L186 name **and** comment, **§4.2 L204 — absent from the on-disk E1 summaries**, §8.2 L289 `ScopeKey.encryptedKek`); the §4.1 comment is a *semantic* fix ("scope KEK wrapped under the root key"→"scope DEK wrapped under the scope KEK"); the research artifact L225 carries the same error (origin, not edited). **(E2)** real module graph: config↔persistence have **no edge either way** at HEAD → PM chooses the **composition-root adapter bridge** (neither references the other; survives a future persistence→config need) over the one-way config→persistence edge; §3.8 must be reworded to that, embedded verbatim in the M6 instruction. **(AMD-87 E1/E2/E3)** all confirmed vs source — §7 says "65"; HEAD annotation literally `@Disabled("AMD-65 pending")` (+ `TestEventSamples.java:315` AMD-65 Javadoc); INV-GA-02 governs INV-XX-NN only. **(Part 2)** persistence→device acyclic (device requires value/event/platform only); 4 permit shapes match `Expectation.java` exactly; @Disabled present, removal correct. **(m2)** the counter-nonce reuse hazard is Doc 15 §6 L243 `[BLOCKING-for-M6-impl]` — must become an explicit M6 Open Risk at Lock.
**Action needed:**
- **Nick:** (1) commit the two repos from host git — **homesynapse-core-docs** (Doc 15 Lock + AMD-86/AMD-87 RATIFIED + INV-PD-07 narrow / INV-PD-03 posture / §35–§36 registrations + nav-index/watermark→87) and **nexsys-hivemind** (this WUCP Phase 2 closeout + the saved review return); messages in the session report + pm-handoff. (2) Hand **M5-A Part 2** to Claude Code (un-gated; GO scope in the coder-handoff Next-Work-Unit block). (3) M5-C / M5-D proceed at your pace.
- **PM (M6):** the **E2 `com.homesynapse.app` composition-root-bridge** wording (Doc 15 §3.8, verbatim-ready) + the **OR-M6-NONCE** counter-nonce-durability hazard fold verbatim into the M6 coding instruction; **AMD-66–71 (M6 config) now UNHELD** (Doc 15 Locked).
- **Coder:** **M5-A Part 2 = GO** (AMD-87 RATIFIED 2026-06-07). Add ONLY the `Expectation` serializers + `requires com.homesynapse.device` (acyclic) + delete the `@Disabled("AMD-65 pending")` + retire the `TestEventSamples.java:315` AMD-65 Javadoc — FloorId is already registered from Part 3. Build gate defers to Nick.

## 2026-06-07 [Coder (Claude Code) → PM, Nick, M9-implementer]
**Topic:** **M5-A Part 2 (AMD-87 `Expectation` persisted codec) COMPLETE — files produced, BUILD GATE DEFERRED to Nick. The AMD-87 serde gate that blocked command-bearing `CapabilityAdded` is now CLOSED (pending a GREEN gate) → M9 unblock.**
**Detail:** Implemented the CURRENT-POINTER GO scope exactly. 7 files: 2 new package-private codec classes (`ExpectationSerializer`/`ExpectationDeserializer` — tagged-union `{"t":"<permit-simple-name>",…}` over the 4 permits, exhaustive no-`default` serializer switch; `ExactMatch`/`AnyChange` delegate `v` to the registered `AttributeValue` codec via `provider.defaultSerializeValue`/`ctxt.readTreeAsValue`, `EnumTransition` plain string, `WithinTolerance` reuses `AttributeValueSerializer.writeDouble`/`AttributeValueDeserializer.readDouble`), persistence `module-info` `+requires com.homesynapse.device` (non-transitive), `PersistenceJacksonModule` `+Expectation` pair keyed on the interface (**FloorId untouched** — already registered Part 3), un-`@Disabled` `EventPayloadCodecTest.capabilityAdded_onOff_roundTrips` (+removed the now-unused `import …Disabled` so Spotless stays GREEN; DisplayName AMD-65→AMD-87), retired the `TestEventSamples.onOffInstance()` AMD-65 Javadoc, new per-permit `ExpectationSerdeTest`. **NO `build.gradle.kts`/`projectionVersion`/event-shape/migration/`@JsonTypeInfo` change.** The persistence→device STOP-gate is source-confirmed clean: acyclic (device requires value/event/platform only, NOT persistence); `moduleGraphAssert` allows `:core:.* -> :core:.*` and lists no `persistence -X> device` restriction; the direct edge is a shortcut over the existing persistence→state→device path so `maxHeight=7` is unchanged. Full detail in coder-handoff `### DEFERRED — M5-A Part 2`.
**Action needed:**
- **Nick:** run the gate (`./gradlew :core:persistence:check :core:device-model:check`, then full `./gradlew check`) against the tree you commit — expected GREEN in one round; then commit + flip the coder-handoff entry to RESOLVED.
- **PM (WUCP Phase 2):** one substantive `[INFO]` to adjudicate — the new `requires com.homesynapse.device` is satisfied on the Gradle compile classpath *transitively* via state-store's `api(":core:device-model")`, so I added NO direct `implementation(project(":core:device-model"))` (the instruction scoped `build.gradle.kts` out; build is GREEN as-is). OPTIONAL: add it for strict lockstep-convention consistency / to harden against a future narrowing of state-store's device re-export (recorded as a persistence MODULE_CONTEXT gotcha). Plus 2 housekeeping `[INFO]`s (DisplayName AMD-65→AMD-87; the AMD-87 §2 `<bits>` shorthand realized by reusing `writeDouble`, per the explicit "do not re-invent the encoding").
- **M9-implementer:** the AMD-87 serde gate is CLOSED — once Nick's gate is GREEN, M9 MAY publish command-bearing `CapabilityAdded`; the `CommandDefinition → ExpectedOutcome → Expectation` subtree now round-trips losslessly (no `DegradedEvent`). The executable proof is the un-`@Disabled` `capabilityAdded_onOff_roundTrips`.
