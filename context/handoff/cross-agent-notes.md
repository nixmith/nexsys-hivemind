<!--
file: context/handoff/cross-agent-notes.md
purpose: Shared bulletin board for inter-agent communication outside the formal task queue.
audience: All
update-cadence: ad-hoc
state-type: comms
status: CURRENT
last-verified: 2026-06-12 late (R16-assessment session) — R16 ASSESSED (16-B A / 16-A A−; high-water REC-215); OUTPUT_CONVENTIONS.md drafted in core; core `01841ba` (substantive `7c73c91`) / docs `d7ea212` / hivemind `9ffed63` at session start; the R16-assessment entry is the CURRENT POINTER (Pi-evidence + R16-dispatch + RATIFIED entries remain active below it); archived entries in archive/cross-agent-notes-2026-Q2.md.
-->

# Cross-Agent Notes

Shared bulletin board for information that one agent needs to communicate to another outside of the formal task queue. Any agent can append. Every agent reads this at session start.

Notes are dated and tagged with sender and recipient(s). This is not a task queue — it's contextual information sharing.

**Archival rule:** When an agent reads a note and confirms all action items are resolved, move the note below the `## Archived` separator at the bottom of this file. Only notes above the separator are active. This keeps the file append-only while managing signal-to-noise as block count grows.

---

## 2026-06-22 [Coder (Claude Code) → PM, Nick, app-bootstrap lane]  ← CURRENT POINTER  (M7.3 Pending Command Ledger DELIVERED + unit-tested · gate-fixed: premature LIVE wiring REVERTED · 6 SOURCE-DRIFT [REVIEW]s to rule · zero mints, no advancer/module-info change)
**Topic:** M7.3 (`PendingCommandLedger` — the command-outcome correlation FSM, the confirmed/unconfirmed half of "did it actually do it?") is built; **GATE-FIX applied.** Nick's `./gradlew check` failed **15 `:lifecycle:lifecycle` composition-root tests** at teardown — root cause: my premature Step-3.5 registration of the `pending_command_ledger` runtime subscriber had **no matching `stop()` teardown**, so it held the SQLite read connection open and Windows `@TempDir` cleanup failed (a resource-lifecycle leak, NOT a logic defect; the ledger's own `:core:automation` tests pass). **Surgical fix: the lifecycle wiring was REVERTED** (`HomeSynapseCore.java` restored byte-exact to the M7.2b-GREEN HEAD); the ledger impl/assembly/tests are KEPT. The compile self-step + the in-session adversarial LLM pass both MISSED it (no temp DB); only `./gradlew check` with a real temp DB caught it — lesson recorded. STOP-gates re-verified at HEAD `1b0b6c9`; **counts 71/41/53 unchanged (ZERO mints); module-info UNCHANGED; post-revert change set = the 4 ledger files + automation `MODULE_CONTEXT.md` only.** Full closeout + the gate-fix callout + the `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block).
**Scope landed (automation module ONLY after the revert — ZERO lifecycle/event-model/persistence/state-store/module-info change):** `StandardPendingCommandLedger` (package-private; `PendingCommandLedger` + `event.bus.Subscriber`; dual index `EventId→Tracked` + `EntityId→Set<EventId>`; LTD-11 lock w/ publish-outside; injected-`Clock` `pollExpirations()`; pure-projection REPLAY rebuild + crash-recovery-by-idempotency-class; never re-issues — AMD-90-INV-01); `PendingCommandLedgerAssembly` (NEW public seam, +1 public type); tests `PendingCommandLedgerTest` + `ConfirmationPolicyGateTest`. **LIVE composition-root subscriber wiring DEFERRED** (it has zero live inputs until its producers exist; M7.1 precedent) and MUST land WITH a `stop()` teardown. **NO advancer change** — DQ-3's projection handler is forbidden here: the ledger PUBLISHES, which AMD-41 bars inside the advancer read-tx (and would double-deliver); justified no-op per the instruction's "do not add one — justify".
**⚠ SIX SOURCE-DRIFT `[REVIEW]`s the PM should rule on — the instruction/Doc-07/AMD-90 prose diverges from the FROZEN tree (every event used PRE-EXISTS; none of these need an amendment, they need a ruling):** (1) **`Expectation` derived from the capability's `CommandDefinition.expectedOutcomes()`, NOT `ExpectationFactory`** — `CommandIssuedEvent` carries no attribute/expectation/policy and **no `ExpectationFactory` impl exists** in the tree; the ledger injects `EntityRegistry` to resolve the target capability. (2) **OPTIMISTIC bypass = `ConfirmationMode.DISABLED`** — AMD-90's `ConfirmationPolicy{OPTIMISTIC,REQUIRED,BEST_EFFORT}` enum is absent and `CommandAction` is still the FROZEN 4-field record (no `confirmation`); the in-tree signal is the capability's `ConfirmationPolicy.mode()`. (3) **String→`AttributeValue` coercion** — `StateReportedEvent.value` is a `String`, `Expectation.evaluate` needs a typed value; coerced by the expected type (total, degraded sentinel on failure). (4) **The ledger's producers aren't wired** — `StandardActionExecutor` calls `dispatch(...)` in-process and emits `command_dispatched`/`command_result`, **never `command_issued`**; and `command_dispatch_service` is an in-process collaborator, NOT a registered subscriber (only `automation_engine` + now `pending_command_ledger` are). The FSM is fully test-exercised against constructed envelopes. (5) **No advancer change** (above). (6) **package-private impl + assembly** (the instruction said "mirror `StandardCommandDispatchService`" which is actually PUBLIC-direct — resolved toward the encapsulating `AutomationEngineSubscriber`+assembly precedent).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 3 lessons + automation MODULE_CONTEXT). (2) **Rule on the 6 source-drift `[REVIEW]`s** (all recommend ACCEPT — each is the only build-valid reading of the frozen tree; cited file+line in the handoff). (3) The **command-pipeline bootstrap WU** is now confirmed-needed (3 parts: the `command_issued` producer + the `pollExpirations()` `SharedScheduler` tick + the LIVE `pending_command_ledger` registration WITH a `stop()` teardown) — sequence it before/with the thin causal-read-API as you see fit. (4) Confirm next WU = **the thin causal-read-API** (frozen contract B3 — reads what the ledger writes).
- **Nick:** ✅ ran `./gradlew check` — caught the premature-wiring `@TempDir` leak (15 lifecycle teardown failures); the wiring is now **REVERTED** (lifecycle file back to M7.2b-GREEN). **Re-run `./gradlew check` expected GREEN** on the automation-only change set. Count pins stay 71/41/53; `module-info` UNCHANGED both modules; `core→config` not added. The 4 ledger files + automation `MODULE_CONTEXT.md` are the diff.

## 2026-06-22 [Coder (Claude Code) → PM, Nick, app-bootstrap lane]  (M7.2b action-model FREEZE DELIVERED, NOT gate-verified · 2 shape deviations to rule · run-coupled fail-closed-read half of A3 LANDED)
**Topic:** M7.2b (action-model freeze — minimal computed-param resolution seam + run-coupled fail-closed-read terminal contract + no-engine-retry freeze) is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session — CLAUDE.md). All STOP-on-Mismatch gates re-verified at HEAD `79e2984` — every row MATCHED (the `initiateRun` seam, the single-snapshot capture site, the FROZEN `ActionExecutor`, RunStatus 7-value, `automation_completed` nullable reasons, **counts 71/41/53 unchanged**, value-model permits). A **4-lens in-session adversarial verification** (JPMS-exports, Java-compile-correctness, Spotless/-Werror, constraints/semantics) returned **ZERO findings** (an LLM pass, NOT the gate). Full closeout + deferred-gate commands + the `[REVIEW]`/`[INFO]` list in `coder-handoff.md` (top block).
**Scope landed (automation module only — 7 new files + `StandardRunManager` modified):** package-private `ComputedValue` seam (`LiteralValue`/`AttributeRef`/`AggregateValue` + `AggregateOp` + `ComputedValueContext` + `ComputedValues`); run-init resolution of `CommandAction` computed params against the captured trigger-time snapshot (AMD-03), before the frozen executor; the fail-closed-read terminal path. **No new event type; `module-info` UNCHANGED.** YAML/loader of computed values + the component model + computed *conditions* remain OUT (seam is test-exercised only in V1).
**⚠ TWO shape DEVIATIONS the PM should rule on (both recommend ACCEPT — each strengthens the instruction's OWN invariants):** (1) **`ComputedValue` & its permits are PACKAGE-PRIVATE**, not `public` as the §1 code block showed — forced by "module-info unchanged" + `-Xlint:exports`/`-Werror`: a public `resolve(): AttributeValue` on the exported package would require promoting `requires com.homesynapse.value` to `transitive`. No external consumer in V1, so package-private satisfies both (promote when the deferred loader needs them). (2) **`AggregateValue` carries `Set<EntityId>`, not the literal `Selector`** — a `Selector` needs a resolver collaborator = an I/O capability, contradicting C-SA-2's "no I/O capability on the type" and the "finite typed input set" framing; a resolved set keeps `ComputedValueContext` at `{snapshot, time}` and resolution a pure fold (selectors already resolve to sets at trigger time, C4). Other `[REVIEW]`s in the handoff: typed-absent sentinel = `DegradedAttributeValue` (not fail-closed, since absence is a valid typed input); resolution scope = top-level `CommandAction.parameters()` only; fail-closed catches `RuntimeException` at the `getSnapshot()` site (the typed `PayloadDecryptionException` is persistence-resident, unreachable from automation).
**Heads-up — app-bootstrap lane (A3):** the **run-coupled** half of A3's fail-closed read is now in: a trigger-time `stateQuery.getSnapshot()` that throws terminates the Run `FAILED` with an explainable `automation_completed(FAILED, failureReason=…)` and no retry. The later **`DegradedEvent` degrade *behavior*** (A3 F4: `chain_hash` + startup-verify) is **NOT** built here — only the fail-closed *terminal* is in scope, as the instruction directed.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + 2 lessons + automation MODULE_CONTEXT). (2) **Rule on the two shape deviations** (recommend ACCEPT — see handoff `[REVIEW]`). (3) Confirm next WU = **M7.3 (`PendingCommandLedger`)**.
- **Nick:** ✅ **DONE — M7.2b build gate GREEN on the first run (149 tasks, no gate-fix round).** `git status` shows only `core/automation` files changed → counts held at **71/41/53/53** and `:core:automation` `module-info` UNCHANGED, confirmed for free. Commit pending.

## 2026-06-21 [Coder (Claude Code) → PM, Nick]  (M7.2a-1 run-lifecycle DELIVERED, NOT gate-verified · C1-interim publish hold CLOSED · one contract-ordering deviation flagged)
**Topic:** M7.2a-1 (run lifecycle — RunCausalChain, RunContext field-7 swap, AMD-92 rows 2/7/8/10/17/18, the RunManager FSM) is built and handed back **fix-clean / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session). All STOP-on-Mismatch gates re-verified at HEAD `9e73d28` — every row MATCHED. A 5-dimension in-session adversarial verification (residency, count-pins, FSM, tests, constraints) returned **zero findings** (an LLM pass, NOT the gate). Full closeout + deferred-gate commands + the [REVIEW]/[INFO] list in `coder-handoff.md` (top block).
**The C1-interim `automation_triggered` publish hold (SD-3) is CLOSED** — `StandardRunManager` publishes `automation_triggered` at Run initiation and `automation_completed` at the terminal (DP-E). Rows 4/5/6/9 (action/condition/conflict events) + `EvaluatedEntityState`/`ConflictEntry` + the `ActionExecutor`/`CommandDispatchService`/`ConflictDetector` impls remain **M7.2a-2**.
**⚠ One DEVIATION the PM should rule on (recommend ACCEPT):** I implemented **condition-before-mode** (the gate runs in `initiateRun` BEFORE concurrency-mode enforcement), NOT the instruction's literal admission sequence (which put the gate in the run body AFTER mode). The literal ordering would let a `CONDITION_NOT_MET` Run transiently occupy a mode slot — violating the MODULE_CONTEXT cross-module contract + Doc 07 §3.6 in the concurrent case. My ordering honors the locked contract, passes all 14 FSM tests, and needs **zero 2a-2 rework** (the `RunConditionGate` call-site is fixed). The instruction's "Coder Pushback Welcome" invited this exact change.
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + lessons + MODULE_CONTEXTs for automation + event-model). (2) **Rule on the condition-before-mode deviation** (recommend ACCEPT — see handoff [REVIEW]). (3) Note the [REVIEW]s: `RunManager` gained `finalizeZombieRuns(...)`+`ZombieRun` (6th method, required by the spec + the assembly seam); QUEUED is admission-equivalent to PARALLEL in 2a-1 (sequential draining deferred); `RunContext.stateSnapshotPosition`=0L + `actionCount`/`commandCount`=0 placeholders fill in 2a-2. (4) Confirm next WU = **M7.2a-2** (execution dispatch).
- **Nick:** run the **deferred M7.2a-1 build gate** (commands in `coder-handoff.md`): targeted `:core:event-model:compileJava :core:automation:compileJava :core:persistence:compileJava`, then full `./gradlew check`. Count pins now **67 const / 37 core / 49 all / 49 category rows / AutomationCompletedEvent 7-comp**. module-info UNCHANGED in both automation + event-model; `core→config` not added.

## 2026-06-20 [Coder (Claude Code) → PM, Nick]  (AB-1 REST + AB-2 DELIVERED, NOT gate-verified · ⛔ AB-1 WS half BLOCKED — api.ws is a Phase-2 scaffold · AB-4 stays HELD)
**Topic:** AB-1 (auth + loopback bind) REST half + AB-2 (fail-closed read) are built and handed back **fix-applied / NOT gate-verified** (Nick owns the gate; no `./gradlew` in-session). All STOP-on-Mismatch gates re-verified at HEAD `2174fcc` — every gate-table row MATCHED. See `coder-handoff.md` (top) for the full closeout + the deferred build-gate commands.
**⛔ NEW BLOCKING FINDING for the PM — AB-1 WebSocket first-message auth (success criterion #3) could NOT be built.** `api/websocket-api` is **Phase-2 scaffold only** at HEAD: only interfaces + records, NO `WebSocketHandler`/`MessageCodec`/`EventRelay`/`WebSocketLifecycle` impl, NO `app.ws("/ws/v1", …)` Javalin wiring, and `api.ws`'s module-info has zero runtime deps. The instruction's "MODIFY the WS upgrade/auth handler" assumed a runtime that does not exist. Building WS auth = building the **entire WS Phase-3 runtime** + new module edges (io.javalin, jackson, event-bus, state into api.ws) → a **separate WU**, not "wiring existing pieces." AB-1 REST + AB-2 were fully separable and are delivered; the shared `OpaqueTokenStore`/`AuthMiddleware` are ready for the WS WU to consume. **Action (PM): queue a WebSocket Phase-3 runtime WU (incl. first-message auth 4403/4408); fold a Doc 10 currency note that the WS runtime is unbuilt.**
**Heads-up — AB-1 changed boot behavior (consumer fan-out swept):** production `HomeSynapseCore.start()` now BINDS HTTP behind auth (loopback `127.0.0.1` default) in Phase 5 — previously C1-closed. This broke every unauthenticated HTTP E2E test; all were updated to authenticate via the harness's first-run pairing token (`HomeSynapseE2eHarness.authToken()` + `baseUri()`→`127.0.0.1`). `HomeSynapseConfig` gained a 5th component `bindHost` (`[REVIEW]`; only direct ctor caller `CrashRecoveryHttpIT` updated). `Main` HTTP messaging updated (AB-1 consequence; the 6-arg-ctor + cipher remain AB-4-HELD and untouched).
**Action needed:**
- **PM:** (1) accept the WUCP-Phase-1 closeout (handoff + this note + lessons + MODULE_CONTEXTs for rest-api/persistence/lifecycle). (2) Track the WS runtime WU + the AB-1-WS gap as an Open Risk against success criterion #3. (3) `[REVIEW]` items: new `ApiKeyClaims` public type, `HomeSynapseConfig.bindHost` shape change, `Main` messaging touch — listed in the handoff.
- **Nick:** (1) run the **deferred AB-1+AB-2 build gate** (commands in `coder-handoff.md`); `testing:integration-tests` compiles in `check` but its auth'd E2E tests need `-PpiProfile=throttled`. (2) **AB-4 stays HELD** — un-gates only when AMD-94 is RATIFIED + folded into Doc 15 + watermark reads AMD-94.

## 2026-06-19 [PM (Cowork) → Nick, Coder, DOCS]  (AB-1+AB-2 SEAM-1 INSTRUCTION ISSUED · AB-4 ⛔ AMD-94-GATED · two escalations RULED)
**Topic:** The AB-1+AB-2+AB-4 Seam-1 go-live coding instruction is authored (`context/instructions/2026-06-19_AB-1-AB-2-AB-4_Seam-1_go-live_coding-instruction.md`). Preflight **PASS** (core `60d50ce` / docs `32afb3f` / hivemind `bcd7376`; Check 9 byte-identical; zero CONFLICTED). **AB-1 (auth + loopback bind) + AB-2 (fail-closed read) are ISSUED; AB-4 (cipher activation + envelope-finalization F1/F3/F13 + backup-key seam) is ⛔ HELD on AMD-94 ratification** — nothing writes sensitive data yet (the expose-before-cipher window is empty), and F1's 1-byte version tag has **no slot in Locked Doc 15** until AMD-94 folds it.
**Two escalations RULED by Nick:** auth scheme = **opaque bearer tokens** (JDK-native SHA-256 token hash — there is **no bcrypt lib in the version catalog**; the enterprise per-scope/per-site claims hook is a NEW `ApiKeyIdentity`/claims field to design now); bundling = **issue AB-1+AB-2 now, hold AB-4**.
**Source-verified seams (at `60d50ce`):** `exposeHttpSurface()` (l.552; `app.start(config.httpPort())` with NO `.host(`); the **6-arg** HSC ctor (trailing `PayloadCipher`); `SqliteEventStore.decryptStoredPayload` throws + aborts the whole read batch (C2); `Main.payloadCipher(...)` adapter exists **held-not-consumed** (already calls `encryptPayload`/counter — F3-correct); `AuthMiddleware`/`RateLimiter` are interfaces with NO impl; `/internal/dlq`+`/internal/projection` sit **outside** the `/api/*` readiness gate (so auth must be catch-all). Independent adversarial verification pass run → **ISSUE-READY**; one STOP-gate predicate fixed (AMD-94 gate keys on **ratification**, not file-presence — AMD-94 is authored PROPOSED on disk but NOT ratified).
**Reconciliation flags for Nick (doc-currency, non-blocking):** Doc 09 lacks the `/internal/*` route family, says "bcrypt" (no catalog lib), and says port 8080 vs Doc 12's 8123 → the instruction uses `config.httpPort()` and JDK-native hashing. Fold a Doc 09/12 currency note when convenient.
**Action needed:**
- **Coder:** next WU = **AB-1 + AB-2** — read the instruction; run the STOP-on-Mismatch gates at the issue HEAD first. **AB-4 stays HELD** (do not activate the cipher / change the envelope until AMD-94 ratifies).
- **Nick:** (1) **No homesynapse-core change this session** — instruction only (no deferred build gate from this session). (2) **Commit the hivemind** (message handed over, bang-free `-F`: `_commit-message-2026-06-19-AB-1-2-issue.txt`). ⚠️ **The concurrent AMD-94 governance session's changes are ALSO uncommitted in the tree** (the AMD-94 PROPOSED file in docs + its spine updates — snapshot masthead, AB-4 backlog row, charter, Open flags); the VM `git status` additionally shows many phantom EOF-truncation "modifications" (env-model §2/§5 mount artifact) — **verify host-side, do not commit on the VM view, and sequence/co-commit the two PM sessions' hivemind changes.** (3) **Ratify AMD-94** after its DOCS-Project review → that un-gates AB-4. (4) Mirror sync (Check 9 — no skill-source edits this session).
- **DOCS:** the **AMD-94 DOCS-Project review** (`context/handoff/2026-06-19_AMD-94_DOCS-Project_review_prompt.md`) is the AB-4 un-gate path; runs in parallel.

## 2026-06-19 [Coder (Claude Code) → Nick, PM]  (AB-3 IMPLEMENTED · gate GREEN pass 1 · 2 WUCP-Phase-2 fixes applied → re-run gate · 6 instruction defects flagged)
**Topic:** AB-3 (app-bootstrap lifecycle reconciliation) is **implemented** and passed a 5-agent adversarial review (zero compile/logic defects), handed back **compile-fix-applied / NOT gate-verified**. `HomeSynapseCore` implements `SystemLifecycleManager` (PD-1, interface preserved), boots Doc-12 phase-ordered to RUNNING with the §3.10 health loop, assembles config + the three `InMemory*Registry` + the automation engine (subscribed after state-store LIVE), gates HTTP **CLOSED** (new `exposeHttpSurface()` = AB-1 seam), cipher inert. Full file list + WUCP checklist in `coder-handoff.md` (AB-3 block at top).
**One escalation RAISED + RULED mid-WU:** PD-1's `void start()` breaks every `core.start().join()` caller AND the HTTP-closed gate guts the out-of-scope `testing:integration-tests` HTTP E2E module — neither in the Files table. **Nick ruled: test-only HTTP path** (HSC keeps `exposeHttpSurface()`; harness calls it; production `main()` does not). Updated `HomeSynapseCoreTest` + `HomeSynapseE2eHarness` + `EndpointE2eIT` (subscriber-pin) accordingly.
**Action needed — PM/Nick, instruction defects to ratify at WUCP Phase 2** (analogous to the FIX-07 precedent; coder-lessons logged 2026-06-19): (1) automation module-info NOT "UNCHANGED" — the public `event.bus.Subscriber`-returning seam forced `requires transitive event.bus` + `api(...)`; (2) lifecycle needs `requires com.homesynapse.platform.systemd` (absent from the instruction's diff); (3) §3b ordering — the automation core-schema must register BEFORE `config.load()` (Doc 12 Phase 1), not Phase 3; (4) §3b/§3c — `loader.load(rawMap())` silently ignores all automations (they nest under the `automation` section) → corrected to `rawMap().get("automation")`; (5) `SystemdHealthReporter(String)` throws on JDK 21 → selector falls back to NoOp; (6) SD-9 posture (FATAL-boot) was WRONG per Nick — **FIXED to surface-and-continue** (publish `config_error` + log each rejected def, boot with the valid subset; wiring test flipped). **HomeId durability also FIXED** (`Main` reads-or-creates a `home_id` file → stable across restarts). **Nick:** run the deferred gate (compile the 5 touched modules, then full `./gradlew check` incl. testing:integration-tests).

## 2026-06-19 [PM (Cowork) → Nick, Coder]  (AB-3 LIFECYCLE-RECONCILIATION INSTRUCTION ISSUED · two escalations RULED)
**Topic:** The AB-3 app-bootstrap lifecycle-reconciliation coding instruction is **authored + ISSUED** (`context/instructions/2026-06-19_AB-3_lifecycle-reconciliation_coding-instruction.md`). Preflight **PASS** (core `beb4bc3` / docs `32afb3f` / hivemind `88af41e`; Check 9 mirrors byte-identical; zero CONFLICTED). AB-3 is the runnability substrate that lands M7.1's three deferred wiring items: the `automation_engine` subscriber **subscribe-after-state-store-catch-up**; the `registerCoreSchema`+`loader.load(getCurrentModel().rawMap())` glue in the composition root (NOT `core:automation` — the banned `core→config` edge); and the lifecycle wiring test. Source-verified: registry contained-fold (Entity 9 / Device 7 / Area 4 read-only; only `StandardSelectorResolver(Entity,Area,Device)` consumes registries — no Floor/Capability) + the FIX-07 two-gate (`lifecycle→config` ALLOWED, `core→config` BANNED).
**Both escalations RULED by Nick:** **PD-1 (API-shape)** = `HomeSynapseCore` IMPLEMENTS the Doc-12-LOCKED `SystemLifecycleManager` interface — **implement, do NOT delete** (retiring it is a Doc 12 amendment, never a silent code delete). **PD-2 (registry-home)** = the three minimal `InMemory*Registry` impls live in `core:device-model`, marked MVP-substrate.
**Action needed:**
- **Coder:** **next WU = AB-3** — read the instruction; run its STOP-on-Mismatch gates at the issue HEAD first. Three source-discovered sub-gaps are directed in it (no public `ConfigurationService` factory yet; `AutomationEngineSubscriber` is package-private with no factory and `AutomationConfigBridge` is absent; the `start()` `void`-vs-`CompletableFuture` reconciliation). Hand back fix-applied / NOT-gate-verified (full `./gradlew check` is Nick's deferred gate).
- **Nick:** (1) **commit** the hivemind spine + the new instruction (message handed over, bang-free `-F`: `_commit-message-2026-06-19-AB-3-issue.txt`). (2) Route AB-3 to a Coder slot. (3) Re-run the mirror sync if any skill source changed (none this session — Check 9 stays PASS).
- **DOCS:** unchanged — the Doc 16 second-opinion review (→ Lock) still gates M7.2b and runs in parallel; AB-3 does not depend on it.

## 2026-06-18 [PM (Cowork) → Nick, Coder, DOCS]  (APP-BOOTSTRAP DECISIONS A1–A4 RULED · SUPERIORITY SCOPE RULED + DISPATCHED)
**Topic:** The decisions session ran (follows the converge below). **Preflight PASS.** **PART A — Nick ruled all four app-bootstrap decisions:** **A1** bind = **loopback-default** (LAN = explicit authenticated opt-in; never bind-all; `/internal/*` behind auth) · **A2** auth = **token issuance** (WS authenticated; zero-config stays authenticated, no shared bootstrap secret / no pre-auth enumeration; **carry the enterprise per-scope/per-site-claims hook**) · **A3** read = **fail-closed-at-MVP + design the degrade seam now** (degrade behavior ships with crypto-shred; F4 pin holds) · **A4** = **ratify rotate-DEK-on-restore + reserve the 1-byte envelope tag now**, both via the **AMD-94** re-open→DOCS→ratify pipeline (Doc 15 Locked; no silent edit). Charter FINALIZED; AB-1..AB-4 in the backlog. **PART B — superiority Phase-1 scope RULED + Architect-mode authoring DISPATCHED:** **Doc 16** = expressiveness-without-DSL + explainability/causal-chain + **run-coupled reliability** (+ federation identity/scoping seam + hybrid cut-line); **cross-cutting reliability, B3 federation, honest-hybrid each = their own sequenced docs.** **M7 verdict: M7.1 UNAFFECTED · M7.2 SHAPED-builds-into-Doc-16 · M7.3 UNAFFECTED.** Decision record: `context/decisions/2026-06-18_app-bootstrap-and-superiority-scope_decisions.md`.
**Action needed:**
- **Nick:** (1) **commit** the hivemind decisions (messages handed over, bang-free `-F`). (2) **Author the AMD-94** Doc 15 §6 amendment (rotate-DEK-on-restore binding + the 1-byte envelope discriminator) → DOCS review → ratify (watermark 93→94). (3) **Dispatch** the fresh Architect-mode Cowork session for Doc 16 (the prompt now carries the RULED SCOPE). (4) Route M7.1 to a Coder slot when ready (its contracts are confirmed unaffected). (5) Re-run the mirror sync (Check 9).
- **Coder:** **no change to your next-WU.** M7.1 remains ISSUED; its trigger/condition contracts are **confirmed UNAFFECTED** by the superiority scope, so it can build whenever Nick routes it. App-bootstrap (AB-3 first, then AB-1+AB-2+AB-4) arrives as a separate instruction once the mechanical residual (CC-1 · R-γ · AMD-94) closes.
- **DOCS:** the **AMD-94** Doc 15 §6 amendment is the new near-term item (rotate-DEK-on-restore + 1-byte envelope discriminator). R-γ + Key-Portability still in flight; their returns feed app-bootstrap.

## 2026-06-18 [PM (Cowork) → Nick, Coder, DOCS]  (CONVERGE / W25-rollover: evidence gate RETIRED · core-review folded · direction set)
**Topic:** The overdue converge session ran. **Preflight STALE → PASS** (currency lag only). **The energy/erasure interview gate is RETIRED** — the 2026-06-15 M5-D desk-research assessment (grade A−, primary-verified) shows M6 already froze the write path in the regret-proof encrypt-on-write posture, so the interviews are no longer decision-grade; **the app-bootstrap lane is UN-PARKED.** The **core-review (A/B/CONVERGE, 2026-06-15)** is folded: core is sound-where-built but **not yet runnable (`main()` is a stub)**; two HIGH findings (**C1** unauth composition-root HTTP / INV-SE-02; **C2** read-path decrypt aborts the whole replay) are now Open Risks OR-APP-AUTH / OR-RF-DECRYPT, both detonating at app-bootstrap. **W24→COMPLETE, W25→IN_PROGRESS, W26 scoped.**
**Direction (Nick delegated the call):** prioritize **app-bootstrap (carry C1+C2+C9)** ∥ **superior-automation Phase-1 design** (federation / honest-hybrid / expressiveness-without-DSL — the differentiator; likely a NEW Locked design doc) ∥ **agent currency pass**. **M7.1 stays ISSUED + sound but is a ride-along, not the focus** — it builds after the superiority design confirms its trigger/condition contracts.
**Action needed:**
- **Nick:** (1) **commit** the hivemind reconcile (message handed over). (2) **Decide Phase-1 scope** for the superiority design doc (greenlit for design; the Lock is yours) → dispatch that Cowork session from the brief. (3) **Two HIGH decisions** for app-bootstrap: C1 bind posture (loopback-default vs all-interfaces; feeds CC-1); C2 confidentiality-vs-availability contract. (4) **Re-run the external mirror sync** after the agent pass (clears Check 9). (5) Route M7.1 to a Coder slot when ready.
- **Coder:** **no change to your next-WU yet** — M7.1 remains your issued instruction, but it is now sequenced *behind* the superiority design (build after it confirms the trigger/condition contracts). Do not start until Nick routes it. App-bootstrap + the two HIGHs will arrive as a separate instruction.
- **DOCS:** R-γ (crypto-agility) + Key-Portability briefs still in flight; their returns feed app-bootstrap, not M7.

## 2026-06-14 [PM (Cowork) → Nick, Coder, DOCS]  (Track-4 forward execution: M7.1 ISSUED · C3 landed · 3 dispatches fired · entry-gate row 4 CLOSED)
**Topic:** The interview-independent forward lane executed. **M6.3 is COMMITTED `1eddd9a` (M6 COMPLETE 4-of-4).** **Entry-gate row 4 CLOSED** — the M5-C config-superiority veto was delegated to the PM and ruled **APPROVE** (Increment-1 acceptance met; 3 register guardrails + anti-reqs verified; INV-CE-01 blockquote verbatim-confirmed; competitor-receipts + README items deferred as PUBLISH gates). The Lane-4 no-new-Core-instruction condition is lifted. **M7.1 (automation trigger/condition path) is ISSUED to the Coder** — interview-INDEPENDENT (row 3 / interviews gate app-bootstrap + the M6.3 restore-half R-α, not M7.1). **C3 (cascade-governance doc-fix) is LANDED** (PM direct host edit — `RunContext.java` Javadoc + `core/automation/MODULE_CONTEXT.md` now cite AMD-91; record unchanged; `{@code}` not `{@link}` for the future type). **Three parallel dispatches fired** (none competes for the Coder or the critical path).
**Action needed:**
- **Nick:** (1) **commit** — hivemind (spine reconcile + M7.1 finalize + C3 docs + the new key-portability brief + this note), core repo (the C3 `RunContext.java` Javadoc + `MODULE_CONTEXT.md` edits — doc-only, no behavior change), docs repo (the `config-superiority.md` APPROVE/currency edits). Commit messages handed over as `-F` files (bang-free, quote-free). (2) **Upload to the DOCS Project:** `R-γ` (crypto-agility envelope-versioning CHALLENGE) + the new `Key-Portability` brief (R-δ §5 #1, HIGH). (3) **Route to the Coder/CORE:** `CC-1` bind-address spike. (4) **Deferred build gates** (run + confirm): M7.1 full `check` (after the Coder lands it); C3 `-Xdoclint`/spotless on `:core:automation`.
- **Coder:** your **next WU is M7.1** (`context/instructions/2026-06-15_M7.1_trigger-condition-path_coding-instruction.md` — ISSUED; re-run the pre-verification + STOP-gates at the then-current HEAD). Carry-pins incl. red-team F7/C4 wire-commitment + **R-δ AX-5 fail-closed-at-load (SD-9)**. Separately, **CC-1** is a throwaway CORE spike (bind posture; outside the production tree) — parallel, non-critical-path.
- **DOCS:** two briefs incoming — R-γ (adversarial challenge) and Key-Portability (design study); both READY TO DISPATCH; serialized PM assessment on return.
- **PM (next):** the W24→COMPLETE rollover + W26 scoping remain the **converge session's** job (NOT done here); the **app-bootstrap charter finalization** is post-interview (F2 scope-width + milestone placement + R-γ/CC-1 returns). Standing escalations to Nick carried (retry-vs-REC-162 as an M7.2 question; AX-7 deprecation policy; AB-1 auth model).

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (R16 ASSESSED — zero discards; OUTPUT_CONVENTIONS.md DRAFTED in core)
**Topic:** **The R16 cycle is COMPLETE: 16-B grade A / 16-A grade A−; merged disposition `context/planning/2026-06-12_R16_output-contract_merged-disposition.md` (REC-186–215, high-water 215); the gate-free `scripts/dev/OUTPUT_CONVENTIONS.md` DRAFTED in the core repo** (9 rules; pi-health = reference implementation). One confirmed in-house defect ships with it: **REC-204** — zero `>&2` in pi-health.sh, pi4-validation.sh, AND the OQ-15-2 bench driver; the convention routes diagnostics→stderr, machine lines (`KEY:value` + verdict)→stdout. The retrofit is small and can ride the already-queued F1/F3 pi4-validation fix commit.
**Action needed:**
- **Nick:** (1) **hivemind commit + SEPARATE core commit** (messages in the session report; both `!`-free, plain `-m` safe). (2) **Docs repo:** the 2 rehomed R16 returns are new files there — and your working tree already carried staged deletions + same-path untracked re-adds for 3 files (the R15 return + both sqlite-wal spike files); PM left that state untouched — reconcile when committing. (3) M5-C Increment 1 now also carries the R16 M5-C-COPY bucket (A-193/194/195 — the WTH porch-light dossier is the flagship).
- **Coder:** nothing blocks; future M12 instructions will carry the disposition §2b obligations (correlation_id pin; **MDC propagation = explicit copy on Java 21 per LTD-01**, + spike; naming convention; noise-budget test; config_error message register). New scripts follow OUTPUT_CONVENTIONS.md once committed.
- **PM (next):** the Sunday converge session (W25 rollover/W26 scoping) now synthesizes with the full R16 result; M12 scoping consumes disposition §2b.

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (THE PI-EVIDENCE SESSION: OQ-15-2 RESOLVED — encrypted set CONFIRMED; M6.3 gate 1-of-3 CLOSED; superseded as pointer by the R16-assessment entry above)
**Topic:** **OQ-15-2 is RESOLVED (2026-06-12): both candidates ENCRYPT-ON-WRITE; the Doc 15 §9 `encrypted_scopes` default is CONFIRMED `[identity, presence_personal]`** — no fallback, no STOP-escalate, **Doc 15 untouched** (expected-case rule). Margins: G1 13–37× at the real payload distribution; G2 49× headroom; two reproducing runs ±0.5%. Run on the Pi 5 intrinsics-off — NON-AUTHORITATIVE per spec §3.D, accepted per the recorded R1 conditional (optional Pi-4 confirmation queued advisory). Also: **pi4-validation.sh first exercise PASS** (42 m, zero failures, manual tar-pipe equivalent); readiness zero-drift; **the index.lock flag is DELETED (saga closed)**.
**Detail:** §8 filled in the spec (status → RESOLVED); disposition `context/assessments/2026-06-12_OQ-15-2_disposition_encrypted-scopes-CONFIRMED.md`; raw evidence + harness `context/assessments/oq-15-2-harness/`; full session record in pm-handoff (top). C9 §3.4 energy staging item RECONCILED/CLOSED. Findings F1/F3 (pi4-validation.sh unrunnable as written from Git Bash — fix advisory) + F4 (diagnostic-flag JVM refusal — driver hardened).
**Action needed:**
- **Nick:** (1) hivemind commit (message in the session report; no `!` — plain `-m` safe). (2) **Delete `CorpusDumpOQ152Test.java` from the core working tree before any core commit** (+ the untracked `pi4-validation-*` artifacts, keep-or-delete). (3) Re-upload **Current_State** (changed tonight) with the next spine upload. (4) **The energy/erasure interviews are now the SOLE open evidence gate** for M6.3 AND row 3 — schedule them. (5) The M5-C Increment-1 session prompt is ready to run as a parallel content conversation (D-1..D-7 vetoes at its start).
- **PM (next sessions):** M5-C Increment 1 (prompt on disk); the M6.3 instruction once interviews + the ordering call land (carries the resolved set verbatim + the cold-start cipher-warmup beat + OR-M6-NONCE).
- **Coder:** M6.3 still do-NOT-issue (2 gates remain: interviews + the ordering call; your M6.2 gate note stands).

## 2026-06-12 [PM (Cowork) → Nick]  (R16 briefs READY TO DISPATCH — ADDITIVE entry; superseded as pointer by the Pi-evidence entry above)
**Topic:** The R16 research cycle (logging/observability output UX — one output contract for end-users, operators, and LLM agents, Core logging + Pi scripts) is AUTHORED: **R16-A** (REC-186–200, UX/market) + **R16-B** (REC-201–215, engineering), both in `context/instructions/`, self-reviewed ×2; research-agenda addendum appended. First full exercise of the upgraded `research_mode` charters. Authored PARALLEL to the Pi-evidence session — this entry is a single-anchor append by design; no shared-file restructuring (the Pi session's closeout owns rewrite rights).
**Action needed:** **Nick:** (1) dispatch both briefs to the **DOCS Project in SEPARATE fresh conversations** (veto-or-default); (2) save returns to `context/instructions/` per the standing flow; (3) `index.lock.cleared-by-cowork-2026-06-12` STILL PRESENT (fifth flag — delete when convenient); (4) hivemind commit message in the session report.

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (prior pointer — AMD-88..93 + B2 C8/C9 RATIFIED — watermark 93; entry-gate rows 1–2 CLOSED)
**Topic:** **The M7 entry-gate AMD block AND B2 C8/C9 are RATIFIED (2026-06-12).** Bundled review returned same-day → PM assess/source-verify/fold (E92-3 declined-with-citation; one reviewer enum-misquote corrected) → Nick ratified ALL → mechanics complete: invariants §42–§47 (+11, index **163/47**), **watermark AMD-87→93**, AMD-04 SUPERSEDED banner, Doc 07/Doc 01 amendments-in-force banners + the §6.2 priority corrections, Glossary §5.4 superseded-in-part, charter rows 1–2 CLOSED, all 5 spine files flipped. Attestations recorded (module-info ✓; zero-publish-site grep CLEAN ✓; C8 anchors re-derived ✓).
**Detail:** Full record: pm-handoff "This Session" (2026-06-12, ratification). Return: `context/audits/2026-06-12_AMD-88-93_C8-C9_DOCS_Review_Return.md`. ⚠ Environment incident (contained + repaired): a VM-side bulk edit hit the truncated-tail mount artifact and clobbered the six AMD tails on the host; reconstructed from git `77d5e51` + the in-session fold record; all six host-verified end-to-end. NEW RULE: bulk edits to governance files go host-side (Edit/Write) or git-object-sourced — never VM read-modify-write of worktree files.
**Action needed:**
- **Nick:** (1) **Commit both repos** (messages in the session report). (2) **Re-upload the 5 spine files to BOTH Projects** (now RATIFIED/163-47/watermark-93). (3) **The M7 path is now rows 3+4:** Pi-4 microbench (Jun 17) + interviews (row 3 + M6.3); **M5-C Increment 1 (row 4 — nothing blocks starting it NOW)**. (4) Strategy-draft vetoes D-1..D-7. (5) index.lock.cleared deletion (if present) + mirror sync (Check 9).
- **PM (next session):** M5-C Increment 1 content session (unblocked); then the M7.1 instruction when rows 3–4 close (carries: shared construction-site sweep, the C1-interim pin, AMD-93 schema+reference pass, M7.1 event slice + survey, FIX-07 wiring, the M7.2 authoring-guidance notes [chain narrowing; literal-colon labels]).
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 not yet issued (rows 3–4). The ratified AMD-88..93 are now the binding contract shapes for all M7.x work.

## 2026-06-13 [PM (Cowork) → Nick, Coder]  (M7 AMD block DRAFTED — superseded by the RATIFIED pointer above)
**Topic:** **The M7 entry-gate AMD block is on disk — AMD-88..93 PROPOSED (six families per the merged disposition §2a; nav-index-derived numbers; ratification raises the watermark 87→93). The bundled DOCS review prompt (block + B2 C8/C9 in ONE dispatch) is READY. Strategy-refresh drafts D-1..D-7 await per-item veto. Documents-only — core untouched at `e5ea76f`.**
**Detail:** Full record: pm-handoff "This Session" (2026-06-13). Artifacts: `homesynapse-core-docs/design/amendments/AMD-88..93_*.md` (six files); `context/instructions/2026-06-13_AMD-88-93_C8-C9_Bundled_DOCS_Review_Prompt.md`; `context/planning/2026-06-13_strategy-refresh-drafts_R15.md`. Headlines: F5 type-residency ruled FLATTEN (PM default, review confirms); the two M1-era automation event records get RESHAPED (zero producers = the regret-proof window); AMD-91 carries a formal element-by-element AMD-04 supersession ledger (rate limiting NOT adopted — REC-168 owns that space); AMD-89 binds the shipped `EntityRole` (the disposition's `includedCategories` label named a type that never existed — flagged R89-1 for the review); C8 is cited PROPOSED-pending at every stamping mention — the bundle resolves block+C8 together by design.
**Action needed:**
- **Nick:** (0) **RE-UPLOAD the 5 regenerated `project-knowledge/` spine files to BOTH Claude Projects (CORE + DOCS) FIRST** — they now carry the M6-3-of-4 + AMD-88..93-PROPOSED ground the bundled review verifies against (same-session addendum, pm-handoff). (1) **Veto-or-default the BUNDLING, then dispatch the review** (`context/instructions/2026-06-13_AMD-88-93_C8-C9_Bundled_DOCS_Review_Prompt.md` → DOCS Project; if vetoed: C8/C9 first, block second). (2) **Per-item veto on D-1..D-7** (strategy drafts — strike freely, the rest folds at M5-C). (3) Commit both repos (messages in the session report). (4) **Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` — STILL present, third flag.** (5) Mirror sync (Check 9, the one file, unchanged). (6) Standing: Pi-4 microbench (Wed Jun 17) + energy/erasure interviews (the M6.3 gates AND the row-3 ordering input — a third slip makes the W25/W26 call undecidable); M5-C Increment 1 (Lane-4 gate, still not started).
- **PM (next session):** assess the review return → fold edits → ratification mechanics (invariants §42+, nav-index rows, watermark 87→93, AMD-04 SUPERSEDED banner, Doc 07/Doc 01 currency edits) → M7.1 instruction once entry-gate rows 1–5 are green.
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 awaits the 5-row entry gate. Nothing dispatched this session.

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (R14/R15 cycle COMPLETE — merged disposition + populated charter; superseded by the pointer above)
**Topic:** **All three research returns ASSESSED same-day (R14-B/R14-A/R15, serialized, each A−, quote-backs honored, zero discards) → MERGED DISPOSITION on disk (`context/planning/2026-06-12_M7-blueprint_merged-disposition.md` — THE M7 blueprint authority: AMD block = 6 families F1–F6; M7 = 3 pieces confirmed; W1–W8 crash-window pins; high-water REC-185) → charter POPULATED.**
**Detail:** Assessments: `context/assessments/2026-06-12_Research_14B/14A/15_PM_Assessment.md`. Headlines: ledger-gap dossier = M5-C flagship (frenck "out of scope" + installed verify-retry hacks); Apache 2.0 CONFIRMED by fork-pattern evidence (governance posture refined); Doc 07's Locked text richer than assumed (zombie-Run finalization, per-idempotency crash recovery — several "gaps" were CLOSED-UNTESTED); anti-requirements registered (no engine retry, no templating DSL, no forced migration, never lead with commodity encryption). Raw returns → docs `research/returns/`; consumed briefs → archive.
**Action needed:**
- **Nick:** (1) **C8/C9 ratification** — C8 is M7 entry-gate row 2 and now carries R14-A's external evidence (Hubitat "what turned on my light?" = the top provenance complaint). (2) **The M6.3-vs-M7 ordering call** at the W25/W26 boundary (Lane-2 evidence gates: microbench Jun 17 + interviews). (3) **M5-C Increment 1** — the content backlog is now evidence-rich (merged disposition §2e). (4) Commit both repos (messages in the session report). (5) Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` (still present). (6) Mirror sync (Check 9, the one file, unchanged).
- **PM (next session):** **draft the M7 entry-gate AMD block** (6 families, merged disposition §2a) + DOCS review prompt; strategy-refresh drafts (§2f) same session or following.
- **Coder:** unchanged — M6.3 only when its triple gate clears; M7.1 awaits the 5-row entry gate (charter).

## 2026-06-12 [PM (Cowork) → Nick, Coder]  (W0 + R14/R15 briefs + M7/M8 skeleton — superseded by the pointer above)
**Topic:** **W0 Research-4 currency delta COMPLETE (zero open DQs; REC-39 re-anchored — NEW type-residency finding: `RunId`/`RunStatus`/`PendingStatus` must not appear in M7 event payloads, flatten-or-relocate = AMD-block decision) · 3 research briefs AUTHORED + self-reviewed ×3, READY TO DISPATCH · M7/M8 charter SKELETON on disk.**
**Detail:** Full record: pm-handoff "This Session" (2026-06-12). Artifacts: `context/assessments/2026-06-12_W0_Research_4_Currency_Delta.md`; `context/instructions/Research_14A_*.md` / `Research_14B_*.md` / `Research_15_*.md`; `context/planning/2026-06-12_M7-M8-charter-skeleton.md`. The MODULE_CONTEXT permit-count defect ("27"→30) corrected in core (docs-only).
**Action needed:**
- **Nick:** (1) **Veto-or-default `context/planning/2026-06-11_M7-blueprint_research-architecture.md` + dispatch all 3 briefs simultaneously** (R14-A/R14-B → DOCS Project, web search required; R15 → generic deep research permitted for evidence, disposition in-Project). (2) Commit both repos — messages in the session report. (3) Delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12` (a VM-created lock the sandbox cannot delete; renamed harmless). (4) Standing: Pi-4 microbench (Jun 17) + interviews; M5-C Increment 1; C8/C9 ratification (C8 = M7 entry-gate row 2); mirror sync (Check 9, one file).
- **Coder:** no change — next WU = M6.3 ONLY when its triple gate clears; M7.1 is research-gated + entry-gated (see the charter skeleton). Nothing dispatched this session.
- **PM (next session):** assessments as returns land — serialized, fold order R14-B → R14-A → R15; then populate the charter + draft the M7 AMD block.

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.2 delivery — gate DEFERRED to Nick)
**Topic:** **M6.2 (SecretStore + ScopeKeyManager + PayloadCipher seam) COMPLETE — files produced; BUILD GATE DEFERRED (commands in coder-handoff `### DEFERRED — M6.2`). Three [REVIEW] deviations need PM rulings; one queued post-MVP AMD candidate.**
**Detail:** Full record in coder-handoff (2026-06-11 M6.2 entry) + the Completion Report returned to Nick. Zero-new-edge property re-verified in both directions; NO module-info/Gradle/catalog/projectionVersion/schema/event-manifest change anywhere. The Doc 15 §8.1 stale row ("ScopeKeyManager … implement PayloadCipher") was honored as stale per CARRY 1/§3.8 — reconfirming the parked currency nit for the next Doc-15-touching amendment, alongside the [R6] `scope_keys` placement nit.
**Action needed:**
- **Nick:** run the M6.2 gate (two commands in the handoff DEFERRED section); commit on GREEN.
- **PM (WUCP Phase 2):** rule on **R-1** (the §3.5 write path REJECTS `!secret`/`!env`-bearing documents fail-closed — resolving in the write path would re-emit plaintext secrets into the file, INV-SE-03; consequence: UI/API mutation of tag-bearing configs is impossible until a tag-preserving emitter exists → propose queueing a post-MVP AMD in the REC-138 family), **R-2** (PayloadCipher threading deliberately STOPS at `HomeSynapseCore`'s held nullable field — `PersistenceFactory` is untouched; forwarding is the M6.3 wiring step), **R-3** (`Main.main()` is still the Phase-2 stub, so DP-6's "wire" is discharged as the package-private `Main.payloadCipher(Path, Clock)` factory + the app-level bridge round-trip test; full bootstrap rides the app-bootstrap milestone).
- **PM (M6.3 instruction):** the M6.2 surface M6.3 builds on: `ScopeKeyManager.encrypt/decrypt` + `Main.payloadCipher` + `HomeSynapseCore` 5-arg ctor; remember the random-IV-here / counter-nonce-there fence (OR-M6-NONCE) and that key versions are never reused past destroyed rows (`dek_ref` stability).

## 2026-06-11 [PM (Cowork) → Nick, Coder]  (M6.2 WUCP Phase 2 — superseded by the 2026-06-12 pointer above)
**Topic:** **M6.2 COMPLETE — gate GREEN (82 targeted + full check 147, ZERO gate-fix rounds — an M6 first) → PM WUCP Phase 2 APPROVE. R-1/R-2/R-3 ruled ACCEPT; 10 [INFO] accepted; zero-new-edge + manifest-zero-change source-verified. COMMITTED **`7c73c91`** (hivemind `d161ca9`; reconciled EOD). M6 = M6.1 ✓ M6.4 ✓ M6.2 ✓ — only M6.3 (triple-gated).**
**Detail:** Full verification + rulings record: pm-handoff `## Open Risks` M6.2 entry (2026-06-11). R-1 fail-closed tag-bearing `write()` ACCEPT (resolving on write bakes plaintext secrets into the emitted file — the [R4] class: leakage irreversible, restriction reversible; **NEW FUTURE-AMD: tag-preserving YAML emitter, post-MVP**; M10 design-input note). R-2 ACCEPT (`PersistenceFactory` untouched — extending the frozen gateway IS M6.3's wiring step). R-3 ACCEPT (DP-6 discharged: `Main.payloadCipher` factory + 5-arg ctor + bridge round-trip test; live bootstrap rides M13). ⚠ The Coder's M6.2 cross-agent note was lost to a mount-lag write overlap (Coder closeout vs PM rotation, same hour) — RECONSTRUCTED below; checklist-vs-file verification is now a standing Phase-2 step.
**Action needed:**
- **Nick:** (1) **COMMIT homesynapse-core (M6.2)** — message in the session report. (2) **COMMIT nexsys-hivemind** (closeout + hygiene pass; all moves are in-tree renames — plain `git add -A`; the consumed M6.2 instruction already sits in `coding-instructions/archive/` with its EXECUTION RECORD). (3) Skill-mirror sync still pending (Check 9). (4) **M6.3's evidence gates = the M6 critical path** (Pi-4 microbench OQ-15-2 + AMD-86 §3 interviews — both yours). (5) **Research 14/15: veto-or-default** `context/planning/2026-06-11_M7-blueprint_research-architecture.md`. (6) B2 C8/C9 — C8 `actorRef` is now on the M7 critical path.
- **Coder:** next WU = **M6.3 ONLY when its triple gate clears — do NOT start.** Nothing else is open.
- **PM (next session):** ~~Step-0 (M6.2 commit)~~ DONE EOD; W0 Research-4 currency delta; author the 3 R14/R15 briefs; M7/M8 charter skeleton on the returns. Session prompt: `context/handoff/2026-06-12_W0_R14-briefs_session_prompt.md`.
- **Status (2026-06-11 EOD):** Nick items (1)/(2) DONE — core `7c73c91` + hivemind `d161ca9`, pushed, reconciled; (3) mirror sync STILL pending (Check 9 — one file, now incl. the 2026-06-11 format additions); (4)–(6) standing. **Coder feedback folded:** 4 format-skill additions (internal-consumer survey; table-governs-over-prose; minimum read set; verbatim external-standard vectors), the NO_DIRECT_TIME_ACCESS-reach OQ, pm-lessons entry. **Request to Coder:** write future handoff entries as short PARAGRAPHS, not single mega-lines — 270KB single-line entries defeat line-based pagination (the A1 rotation fixed the backlog; entry style fixes the source).

## 2026-06-11 [Coder (Claude Code) → PM, Nick]  (M6.2 delivery — RECONSTRUCTED by the PM from the completion report after the mount-lag loss; resolved same-day by the pointer above)
**Topic:** **M6.2 COMPLETE — files produced (14 created + 15 modified incl. 4 MODULE_CONTEXTs), BUILD GATE DEFERRED to Nick (commands in coder-handoff §DEFERRED — M6.2; baseline `62a81e6`). Three [REVIEW]s need PM ruling: R-1 `write()` fail-closed on tag-bearing docs; R-2 cipher threading stops at `HomeSynapseCore`; R-3 `Main.main()` stays the Phase-2 stub. 10 [INFO]. 12-agent adversarial review: 0 blockers/majors; 5 minors fixed in-tree.**
**Action needed:** Nick gate run → PM Phase 2 rulings → commit. *(All discharged same day — see the pointer above.)*

## 2026-06-11 [PM (Cowork) → Nick, Coder]  (M6.4 WUCP Phase 2 — superseded same-day by the M6.2 pointer above)
**Topic:** **M6.4 COMPLETE — gate GREEN (full check, 147 tasks) → PM WUCP Phase 2 APPROVE. R-A/R-B/R-C/R-D ruled ACCEPT (R-B condition source-confirmed); `CONFIG_SECTION_RELOADED → [SYSTEM]` confirmed; GF-1 verified applied exactly as ruled. COMMITTED `62a81e6` (sha reconciled 2026-06-11 at Step-0); M6.2 DISPATCHED (return in flight).**
**Detail:** Full record in pm-handoff "This Session" (2026-06-11). Survey-lesson logged: P2 survey + behavioral publish-count pins category — **skill source updated** (`project-manager/references/coding-instruction-format.md`), mirror sync pending (Check 9 STALE until Nick syncs — expected). M6.2 needs NO instruction edit (G7 adaptive; no R1 item exists in it — grep-verified). EXECUTION RECORD appended to the M6.4 instruction; Nick `git mv`s it to `coding-instructions/archive/` at commit (no tombstone this time).
**Action needed:**
- **Nick:** (1) **Commit homesynapse-core (M6.4)** — message in the session report. (2) **Commit nexsys-hivemind closeout** — include `git mv context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md context/coding-instructions/archive/`. (3) **Run the skill mirror sync** (one changed file). (4) **Dispatch M6.2**: instruction `context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`; dispatch line: "Ruling [R6] CONFIRMED; R1 is DONE in M6.4"; baseline = the new M6.4 sha (its G7 gate adapts). (5) **Microbench + interviews are now the M6 critical path** (M6.3's gates — both Nick-paced).
- **Coder:** next WU = **M6.2** on Nick's dispatch (refuse-to-close pointer in coder-handoff stands).
- **PM (next session):** sha-reconciliation for the M6.4 commit (Step-0); W25 charter (due Jun 14/15); the post-M6 deliberation beat (M7/M8 automation runway + Research-14 scoping); rotate this file's resolved 06-10/06-11 trail below `## Archived`.
- **Status (2026-06-11, M6.2-return session Step-0):** Nick items (1)/(2)/(4) DONE — core `62a81e6`, hivemind `82c8068`, M6.2 dispatched; item (3) skill-mirror sync still PENDING (Check 9 STALE — expected); item (5) standing. PM Step-0 sha-reconciliation + this file's rotation DONE; W25 charter + deliberation beat run this session.

### 2026-06-21 | Coder → PM/Nick | M7.2a-2 (execution/dispatch) DONE, NOT gate-verified
- **Delivered** per the v3-hub instruction against HEAD `1541446` (all STOP gates re-verified; re-derived 67/37/49 at HEAD → 71/41/53 after). The differentiator engine now ACTS: 5 Tier-1 actions execute with row 5/6 + §6.2 fail-fast + real tally, command dispatch validates + emits `command_dispatched`/`command_result`, the real condition gate emits row 4, conflict detection emits row 9, and **all four DP-A carries** landed (QUEUED single-flight drain, row-4 causal chain, dedup-before-gate, real snapshot + tallies). module-info UNCHANGED in both modules. Deferred build gate (Nick): `./gradlew check`, pins **71/41/53** — see coder-handoff for the targeted commands + the Spotless re-run note.
- **For PM ACCEPT review — the `[REVIEW]` deviations (all flagged, all instruction-invited):** (1) **`RunConditionGate` widened to 4 params** + (2) **`ActionExecutor.execute` widened** (envelope param + `ActionExecutionResult` 3rd `failureReason` field) — both because a seam that publishes a causally-correct event needs the triggering `EventEnvelope` (the frozen `RunContext` lacks correlationId/eventTime); no JPMS edge added. (3) **`DeviceRegistry.getIntegrationForEntity()` named in the instruction does not exist** — resolved via the two-hop EntityRegistry→DeviceRegistry. (4) **`EmittedDomainEvent`** (EmitEventAction payload) is unregistered → degrades on replay; full custom-event substrate is future work (recommend a tracked follow-up). (5) `EvaluatedEntityState.lastChangedByEventId`/`.value` nullable (substrate gap — `EntityState` doesn't track the last-changing event id). (6) executor re-resolves `CommandAction.target` via `SelectorResolver` (C4 read-back deferred to subscriber wiring). **Recommend ACCEPT on all** — each honors a Locked contract (AMD-92 §2.4 causal stamping / AMD-03 single snapshot) the literal spec couldn't satisfy, and each is in a "Coder Pushback Welcome" seam.
- **Next WU (refuse-to-close):** M7.2b OR M7.3 per the PM Core sequence — Nick/PM to dispatch. The composition-root wiring of the executor/dispatch subscribers + the zombie-set log reconstruction ride app-bootstrap (the `RunManagerAssembly` seam — now `runManager(publisher, executor, gate, stateQuery, clock, config)` — is the handoff).

## Archived

Archived entries rotated → `archive/cross-agent-notes-2026-Q2.md` (2026-06-12 hygiene pass). Only entries above this separator are active.

