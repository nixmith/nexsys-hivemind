<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
**[2026-06-19 — M7-FORWARD PLAN + M7.1 SECOND-CHECK (Cowork PM)]:** Preflight **PASS** (core `beb4bc3` / docs `32afb3f` Doc-16-DRAFT-not-Locked / hivemind `ec9905e`; truncated-tail mount artifact host-cleared per env-model §2/§5; Check 9 benign-STALE; zero CONFLICTED). **Part A — M7.1 second-check = PASS-WITH-FINDINGS:** closeout sound (all primary records agree; watermark AMD-93, no new AMD — AMD-94 reserved-not-on-disk); 3 deferrals homed (wiring→AB-3, FIX-07 `core→config` ban→coder-lessons, config-free `AutomationSchema`); 6 [REVIEW] adjudications hold (the 3 behavioral defaults = tracked M7.2a carry-items, none silent); source round-trip clean (`StandardTriggerEvaluator.evaluate`→`List<AutomationId>`; AMD-92 slice rows 1/3/11–16/19 landed; **+7 EventTypes / 24→32 classes / 36→44 category rows source-accurate**; M7.2 owes rows 2/4–10/17/18; no fabrication). **Findings reconciled this session:** F1 (coder-handoff masthead lagged the landed GREEN — single-anchor RESOLVED note added) + F2 (snapshot Recent-Session-Log lagged the masthead — rows appended). **Part B — M7-forward plan recorded** (`context/planning/2026-06-19_M7-forward_plan.md`): **M7.2 SPLIT (P1)** → M7.2a (baseline-buildable-now; builds against injected deps, wiring→AB-3) + M7.2b (action-model freeze, **Lock-gated on Doc 16 + D2/REC-162**); **M7.3** UNAFFECTED (Doc 16 §7.2), gated on M7.2a; **Doc 16 review-ready** → DOCS second-opinion review (own parallel session) → Lock = M7.2b prereq; **sequencing rec = app-bootstrap AB-3 next Coder slot** (Nick rules); M7.2 NOT dispatched (entry-gate OPEN). Substrate gaps routed (slug-tombstone→identity/device; `Area` slug→AMD-44 Stage-2; `Availability`→integration AX-8). **RULED by Nick 2026-06-19:** sequencing = **app-bootstrap AB-3 next Coder slot** (AB-3 scope resolves the device-model registry dependency up front — fold minimal registries or sequence the device-model slice; **M7.2a = fallback** if prep stalls); D2/REC-162 = **keep REC-162 anti-retry, decide at M7.2b** (cheap M7.2-scoping fact-check: verification half likely covered by `Expectation`+AMD-90). Open Risks unchanged; no open deferred build gates. _The M7.1-landing record follows._
**[2026-06-18 — M7.1 LANDED GREEN + WUCP Phase 2 (Cowork PM)]:** **M7.1 (automation trigger/condition path) is gate-verified GREEN** — full `./gradlew check` PASS (149 tasks): compile + spotless + ArchUnit (LTD-11 / NO_DIRECT_TIME_ACCESS) + `assertAllowedModuleDependencies` + all count pins + serde round-trips. **First automation code landed.** **Gate-fix round 1 (2 failures, fixed in one pass):** (1) compile — `StandardTriggerEvaluator:307` sourced `automationId` from the removed `ActiveTimer` field → corrected to `entry.getKey().automationId()` (the `TimerKey` is the canonical source; field stays removed); (2) **module-graph — `:core:automation -> :config:configuration` is a forbidden edge** (caught by `assertAllowedModuleDependencies`; the doc'd layering is config→core, never core→config). Relocated: `AutomationConfigBridge` deleted, `requires/implementation config` dropped, the schema fragment moved to a config-free `AutomationSchema` holder; the config-loading + schema-registration wiring **rides app-bootstrap (composition root)** alongside the already-deferred `automation_engine` subscriber wiring. (`requires event.bus` stays — legal core→core.) Plus the pre-gate review-found `EventTypesTest` 55→62. **Deferred build gate: RESOLVED (GREEN) — none lingering.** **INSTRUCTION DEFECT logged + corrected:** the M7.1 instruction's FIX-07 directive to re-add `requires config` was wrong (banned at any scope; the §authoring exported-API check is necessary-not-sufficient vs the module-graph allow-list); instruction banner added, coder-lesson captured. **[REVIEW] deviations adjudicated — all ACCEPT:** 3 substrate gaps routed forward (slug-tombstone → future identity/device WU; first-class `Area` slug → device-model AMD-44 Stage-2; `Availability` asleep-vs-dead granularity → M7/integration per R-δ AX-8); 3 behavioral defaults accepted (multi-entity condition = all-of/empty→false — fold a Doc 07 §3.8 clarification when next touched at M7.2; `EventTrigger` payload-field matching → M7.2; device-subject duration `entityRef` flattening → M7.2 cleanup). **Wiring deferral VALIDATED** — the green build confirms the M7.1 core builds cleanly against injected deps; the subscriber + config wiring + the lifecycle wiring test ride app-bootstrap AB-3 (inbound dependency recorded in the decision record). **Next-WU = M7.2** (run/action/dispatch; C3 already landed `03f16dc`); per the Part-B ruling M7.2 builds **into** Doc 16 — do not freeze its action contract before Doc 16 Locks. **Commit:** the M7.1 code is its own homesynapse-core commit (green, self-contained); this closeout is the hivemind M7.1-WUCP batch.
**[2026-06-18 RECONCILED — Cowork PM, the CONVERGE/W25-rollover session (run late)]:** Spine HEADs now **core `03f16dc` / docs `f2e064d` / hivemind `b7a893e`** (all clean). **Preflight STALE → PASS** (currency lag only; zero CONFLICTED). Propagated this session: (1) **the energy/erasure interview gate is RETIRED** per the 2026-06-15 M5-D desk-research assessment (Track B energy→parked to the future energy milestone; Track A erasure trigger MOOT — M6 froze the write path correctly, shred is a recoverable post-MVP WU) → **the app-bootstrap lane is UN-PARKED**; (2) **core-review A/B/CONVERGE (2026-06-15)** logged — core sound-where-built but **not runnable (`main()` stub)**; two HIGH → Open Risks **OR-APP-AUTH (C1)** + **OR-RF-DECRYPT (C2)**, both detonating at app-bootstrap; (3) **W24→COMPLETE, W25→IN_PROGRESS, W26 scoped**; (4) **direction set by Nick (delegated):** app-bootstrap (C1+C2+C9) ∥ superior-automation Phase-1 design ∥ agent currency pass; M7.1 = ISSUED/awaiting-build, held as a ride-along (substantive HEAD still `1eddd9a`). The Track-4 line below stands for its own session; this supersedes it where they differ. Check 9 → benign-STALE after the agent pass until Nick's mirror sync.
**[2026-06-14 RECONCILED — Cowork PM, Track-4 forward-execution]:** Spine HEADs now **core `1eddd9a` / docs `c309123` / hivemind `7ea7992`** (all committed/pushed; core working tree clean). **M6.3 COMMITTED `1eddd9a` → M6 COMPLETE 4-of-4.** **Entry-gate row 4 CLOSED** — `homesynapse-core-docs/website/pages/config-superiority.md` APPROVED 2026-06-14 (PM quality gate, veto delegated by Nick; Increment-1 acceptance + guardrails verified; INV-CE-01 blockquote verbatim-confirmed; competitor-receipts + README items deferred as PUBLISH gates). The Lane-4 no-new-Core-instruction condition is **lifted**; **M7.1 cleared to issue** (interview-independent — row 3 gates only the parked app-bootstrap lane + the M6.3 restore-half R-α; the M6.3-vs-M7 ordering call is resolved by M6.3 landing first). The 2026-06-15 artifacts (Track-3 red-team audit, R-γ/R-δ briefs, R-δ/R-α assessments, app-bootstrap charter, M7.1 + C3 instructions, CC-1 spec, two-part core-review + CONVERGE synthesis) are landed + now registered in snapshot/backlog/coder-pointer. Crypto/app-bootstrap lane PARKED behind Nick's energy/erasure interviews; W24→COMPLETE rollover + W26 scoping still owned by the converge session. The stale spine HEADs in the 2026-06-13 line below are superseded. _This session's full record is appended under the session-records section below._
**[2026-06-13 RECONCILED — Cowork PM, parallel lane]:** The 2026-06-12 ratification IS complete and folded — **AMD-88..93 + B2 C8/C9 RATIFIED**, watermark **93**, invariants **163/47**, AMD-04 superseded by AMD-91 (docs `8002b7e`; the AMD files' own status lines + `Architecture_Invariants_v1.md` line 1066 confirm). **The "FOLD ABORTED … watermark stays AMD-87 … six AMDs stay PROPOSED" narration below is SUPERSEDED** — it captured the pre-ratification moment when the bundled review return was not yet on disk; the review subsequently landed and Nick ratified the block. Current spine HEADs: core `824d6ba` / docs `8002b7e` / hivemind `5e4943a`. W24→COMPLETE rollover NOT advanced here (calendar-bound; the 2026-06-14 W25-rollover converge session owns it). Open M6.3 evidence gate unchanged: the energy/erasure interviews.
last-verified: 2026-06-12 Pi-evidence session (evening) — **OQ-15-2 RESOLVED: encrypted set CONFIRMED `[identity, presence_personal]`; M6.3 gate 1-of-3 CLOSED; pi4-validation first exercise PASS (42 m, zero failures); index.lock flag DELETED by Nick (saga closed); M5-C Increment-1 prompt authored. Doc 15 untouched (expected-case rule). Sole open M6.3 evidence gate: the energy/erasure interviews.** _Prior:_ 2026-06-14-brief session (executed Jun 12 UTC) — **FOLD ABORTED AT STEP-0: the bundled AMD-88..93 + C8/C9 review return is NOT on disk** (`context/audits/` — absent both namespaces, three-repo sweep negative; only the dispatched prompt exists) **→ STOP per brief, hygiene-only: nothing folded, no mechanics, watermark stays AMD-87, six AMDs stay PROPOSED. Docs divergence: AMD-88..93 still UNTRACKED at `ed5cf91` (Nick: commit before dispatch/fold). index.lock.cleared FOURTH flag. Hivemind `ba1466d` reconciled. Rollover not passed. Check 9 STALE (expected). M7 research-vector advisory delivered (dry-run seam + AMD-91 state-hop question; no new research brief needed). Next: Nick lands the return (or runs the Cowork-fallback review in a FRESH separate conversation) → re-run the fold brief.** _Older masthead chain (the 06-13 AMD-block entry and earlier) → git ≤`58794d7` + `archive/pm-handoff-2026-06.md`._
-->

# PM Session Handoff

## 2026-06-21 — PM MISSION-CONTROL v3 LAUNCHED: hygiene rotation + #0 device brief + read-API freeze + M7.2a-2 ISSUED + CI confirmed + 2 lane prompts (Cowork PM standing hub; supersedes v2)

**Outcome: the v3 hub is live as the single spine-writer; the §9 first-action sequence is executed through §9.5. Preflight PASS (all 11); zero forward-work blockers.** The truncated-tail mount phantom is ACTIVE across all three repos — host-verified intact (Doc 16 L488, RunManager L169, snapshot whole); VM `git status`/`diff` quarantined (env-model §2/§5); **blind `git add -A` is unsafe — selective-stage host-side.**

- **Preflight PASS.** core `1541446` (M7.2a-1 GREEN, reconciles the `9e73d28`+1 baseline) / docs `e47f01e` (Doc 16 LOCKED, watermark AMD-94, 169/49) / hivemind `23a6cf7` (the M7.2a-1 closeout — it already committed the v3 prompt, a benign update vs the masthead's "untracked"). Source round-trip clean: EventTypes 67 / roster 37 / category 49 re-derived from source. Check 9 PASS (no skill-source edits this session).
- **§9.1 Hygiene rotation (host-side, git-object-sourced per env-model §5).** Hot-path 298KB→176KB (−41%): PROJECT_SNAPSHOT 90.8→76.5KB (six 2026-06-19 mastheads → `status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-21.md`); pm-handoff 54.5→39.1KB (three 2026-06-19 sections → `handoff/archive/pm-handoff-rotated-2026-06-21.md`); cross-agent 53.8→44.7KB (below-`## Archived` entries → appended to `archive/cross-agent-notes-2026-Q2.md`); coder-handoff 99→16.2KB (AB-3 + the stale `## Deferred Build Gate` M6.2/M6.3 records → `handoff/archive/coder-handoff-rotated-2026-06-21.md`). 5 superseded prompts (the 2026-06-19 hub, the earlier 2026-06-20 hub, the v2 prompt, the AMD-94 review prompt, the Doc-16 review prompt) → `handoff/archive/`. Governance docs + lessons logs UNTOUCHED. All host-verified intact after write.
- **§9.3 — The #0 device-acquisition + test strategy brief** (`context/planning/2026-06-21_device-acquisition-and-test-strategy_brief.md`). Web-verified June-2026 prices. Order: ZBDongle-P (CC2652P/ZNP, ~$30) + the **EFR32MG24** stick (Sonoff Dongle Plus MG24 / SMLIGHT SLZB-06MG24, EZSP, ~$30–35) which **doubles as a Thread border router** — Matter/Thread future-proofing for ~$0 in the coordinator choice (upgraded from the v2 spec's MG21 ZBDongle-E). Curated 6-archetype set (Sonoff SNZB + Philips Hue White, off IKEA), ~$87–95; recommended bundle ~$140–175. 72h validation protocol → the Oct 12–25 window; Matter/MQTT/Thread research (defer endpoints to their own post-V1 milestones; V1 stays Zigbee-only). **✅ RESOLVED 2026-06-21 (Nick): A1 (order the ~$140–175 bundle this week) + B1 (Thread-via-MG24 now, Matter/MQTT endpoints deferred). The silicon-vs-software integration-sequencing principle recorded in the brief §6 (radio = front-load the silicon; IP-based = software-on-demand, schedule-flexible).**
- **§9.4b — Dashboard read-API contract FROZEN v1** (`context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md`). Source-verified at `1541446`: EXISTING shapes (entities list/get/state; `{data,[pagination],meta:{viewPosition,timestamp}}`; RFC 9457 incl. `state-store-replaying` 503; `Authorization: Bearer`; cursor pagination) + FROZEN-UNBUILT shapes the frontend mocks (events B1, health B2, the causal-chain hero B3 + run list + automations). The hard prerequisite for the Web-UI lane; any A/B-class change is a cross-lane event routed through the hub.
- **§9.4a — M7.2a-2 ISSUED** (`context/instructions/2026-06-21_M7.2a-2_execution-dispatch_coding-instruction.md`). Source-verified end-to-end: the 4 interfaces (`ActionExecutor` void→tally per DP-D; `CommandDispatchService`; `ConflictDetector`; `RunConditionGate`), `CommandValidator` (new), the 5 Tier-1 permits (Tier-2 reserved → throw), the AMD-92 rows 4/5/6/9 exact component lists (+ nested `EvaluatedEntityState`/`ConflictEntry`), both module-infos verbatim + **UNCHANGED** (residency clean), counts re-derived 67→71 / 37→41 / 49→53. The four 2a-1 carries are settled DPs (QUEUED true sequential drain; row-4 CausalContext; dedup-before-gate; real `stateSnapshotPosition` + tallies). command_* events PRODUCED not minted (DP-E). Full P2 survey + SequenceConflictException/spotless/§4c traps.
- **§9.4c — CI confirmed live** (`context/process/ci-as-gate-of-record.md`). core `ci.yml` runs `./gradlew check --no-daemon` on push+PR (JDK 21). Adopt push→gate→adjudicate; add the Coder spotless self-step; extension plan for the new lanes (lint/typecheck/build + contract-check; install-smoke).
- **§9.5 — Both lane prompts authored** (`context/handoff/2026-06-21_frontend-dev-lane_session_prompt.md` + `..._distribution-skeleton-lane_session_prompt.md`). Write-isolated (frontend → `web-ui/dashboard/`; Distribution → a new `distribution/` workspace), build against the frozen contract / the current artifact, CI-gated. Launch after Nick's go.
- **Open Risks: unchanged** — only OR-M13-SDNOTIFY open (OR-APP-AUTH/C1 RESOLVED-for-REST; OR-RF-DECRYPT/C2 resolved-at-MVP; OR-M6-NONCE CLOSED). No open deferred build gates. The WS Phase-3 runtime WU stays backlog-tracked. **NEW standing item: hardware APPROVED 2026-06-21 (Nick) — the #0 schedule-critical pole; tracked open until the order is placed + the parts are in hand + paired (the mid-August go/no-go's hardware evidence).**
- **Commit handed to Nick (hivemind only — no core/docs change this session):** the rotation (4 trimmed hot-path files + 4 new archive files + the Q2-archive append + the 5 prompt moves) + the 5 new artifacts (#0 brief, read-API freeze, M7.2a-2 instruction, CI note, 2 lane prompts) + this spine batch (snapshot, this pm-handoff, the backlog currency note). Selective-stage host-side (phantom makes blind `git add -A` unsafe); bang-free `git commit -F`. Heredoc in the session report.
- **Next (the loop): Nick** — (1) approve the hardware spend + the Matter/MQTT call; (2) route M7.2a-2 to the Coder; (3) launch the frontend + Distribution lanes; (4) commit (selective-stage). **Hub** — reconcile each lane's returns as the single spine-writer; sequence M7.2b (entry-gate already cleared) → the thin causal-read-API → AB-4 → M9 → validation; keep the mid-August go/no-go on the calendar.

## 2026-06-21 — M7.2a-1 (run lifecycle) LANDED GREEN + WUCP Phase 2 → APPROVE (Cowork PM hub)

**Outcome: M7.2a-1 is gate-verified GREEN (`./gradlew check`, 149 tasks) → PM WUCP Phase 2 = APPROVE.** The Coder built the run-lifecycle slice from the 2a-1 instruction; one gate-fix round (a spotless unused-import in `StandardRunManagerTest`, fixed by `spotlessApply`). The differentiator engine now has a working Run FSM: `RunCausalChain` (AMD-91) + the `RunContext`/`RunManager` swaps, the run-lifecycle event sub-slice (rows 2/7/8/10/17/18 → EventTypes 62→67, roster 32→37, category 44→49), and `StandardRunManager` driving `EVALUATING→RUNNING→terminal` with C2 dedup, cascade depth+chain-cycle suppression, concurrency modes, auto-disable, and REPLAY-zombie INTERRUPTED. The M7.1 C1-interim `automation_triggered` publish hold is CLOSED.

- **FSM verified in source** (the contracts the green gate cannot catch): cascade suppression is a pure function of `parentChain` (no windowed state, AMD-91-INV-01); `finalizeZombieRuns` re-derives terminal completions from caller-supplied log descriptors without re-executing (E91-1); `ReentrantLock` only with publishes outside the lock (LTD-11); clock-injected throughout (§4c); all payloads flattened (residency — the GREEN compile proves no `event→automation` cycle).
- **Deviation ACCEPTED — condition-before-mode (the Coder's catch, my instruction defect).** My 2a-1 admission sequence placed the condition gate in the run body *after* mode admission, contradicting the locked MODULE_CONTEXT/Doc-07 §3.6 contract — which the same instruction cited in its watch-out ("CONDITION_NOT_MET must not consume a mode slot"). The Coder honored the locked contract (gate before mode; a CONDITION_NOT_MET Run never enters `activeRuns`). Correct. Logged as a pm-lesson; the corrected ordering carries into the 2a-2 instruction.
- **Other deviations ACCEPTED:** `RunManager` gained `finalizeZombieRuns(List<ZombieRun>)` + the nested `ZombieRun` (anticipated); `stateSnapshotPosition`/`actionCount`/`commandCount` are 0 placeholders (2a-2 fills them); the codec/harness touches were P2-survey-directed.
- **CARRIED to M7.2a-2 (tracked, not silent):** (1) **QUEUED is currently admission-equivalent to PARALLEL** — 2a-2 must implement QUEUED's true sequential single-flight drain (unobservable against a stub executor, so deferred — but it must NOT ship as PARALLEL); (2) the **`RunConditionGate` needs the triggering `CausalContext`** to publish row 4 (`automation_condition_evaluated`) in the correct chain — 2a-2 widens the seam or publishes row 4 from the FSM; (3) **dedup-claim should precede the gate** in 2a-2 so a raced duplicate doesn't double-publish row 4.
- **Open Risks: unchanged.** No open deferred build gates (this one resolved GREEN).
- **Commits handed to Nick:** core = the M7.2a-1 implementation (28 files, GREEN); hivemind = the issue + this WUCP Phase 2 closeout batch (incl. the Coder's WUCP Phase 1 writes). The next-WU pointer is **M7.2a-2**.

## 2026-06-21 — PM MISSION-CONTROL v2 (PARALLEL LANES) LAUNCHED + D-OPEN-1/2/3 RESOLVED + WAVE SET (Cowork PM standing hub)

**Outcome: the v2 parallel-lanes mission-control hub is live as the single spine-writer (supersedes both prior hubs); the three V1 open items are RESOLVED by Nick; the build-wave sequence is SET.** Step-0 preflight clean — baseline confirmed exactly (core `9e73d28` / docs `e47f01e` Doc-16-LOCKED / hivemind `b8fb613` V1-scope); skill mirrors byte-identical (Check 9 PASS); **zero CONFLICTED**. The **truncated-tail mount phantom is ACTIVE across all three repos** (host-verified intact; VM `git status`/`diff` quarantined per env-model §2/§5) — the only genuinely untracked file is the v2 orchestrator prompt.

- **Doc 16 Lock + V1 record RECONCILED, not re-applied.** INV-SA-01..04 present once each at §49; count host-re-derived to **169/49** (218 section-rows = 169 §17 index + 49 §18 matrix — counted, not propagated); watermark stays **AMD-94**. No double-mint. V1 launch-scope record internalized as the strategic authority.
- **One benign currency-lag (not a blocker):** the Coder-owned `coder-handoff.md` masthead still reads "AB-1+AB-2 DONE, not gate-verified" and points at AB-4 — it predates the committed GREEN (`9e73d28`) and the Doc-16-Lock determination that M7.2a is next. Cosmetic (the AB-1+AB-2 WUCP Phase 2 is already closed in the spine); not rewritten (Coder-owned, not spine).
- **D-OPEN-1/2/3 RESOLVED (Nick) — recorded in the V1 scope record.** **Hardware:** spec hub-verified against Doc 08 §3.2–3.3 (the two-layer coordinator architecture is real) + the integration-zigbee scaffold — buy one stick per transport path: **CC2652P** (ZNP) + **EFR32MG21/MG24** (EZSP), auto-detected at startup; **order now if not in hand** (longest non-compressible pole). **Device set:** Sonoff SNZB line + Philips Hue White (moved **off IKEA** — TRÅDFRI wound down 2026); all six archetypes map to ZCL clusters the device model expresses (Doc 08 §3.5 + Doc 02 §3.6, hub-verified, no gaps). **WS:** deferred for V1 (poll 1–2s) — the "no WebSocket runtime in V1" anti-requirement is now firm.
- **Wave SET (Nick):** **M7.2a (Core) + (read-API contract freeze & CI-as-gate-of-record) in the same beat → frontend-dev + Distribution-skeleton next** (after the contract is frozen). Rejected all-four-now (frontend-before-frozen-contract churn) and CI-first-alone (idles the Core pole).
- **M7.2a source landscape mapped** for the coding instruction: `RunCausalChain` is the one must-create (AMD-91 §2.1); `RunContext` currently carries `int cascadeDepth` (the AMD-91 field swap is M7.2a); `RunManager`/`ActionExecutor`/`CommandDispatchService` are interface-only stubs; AMD-92 owes event rows 2/4–10/17/18; event-count pins (EventTypes 62→71, classes 32→41, category 44→53) — **re-derive from source at issue, do not propagate these**. Module `com.homesynapse.automation` module-info UNCHANGED for M7.2a.
- **Open Risks: unchanged** (OR-APP-AUTH/C1 RESOLVED-for-REST; OR-RF-DECRYPT/C2 resolved-at-MVP; OR-M6-NONCE CLOSED; OR-M13-SDNOTIFY open; the WS Phase-3 runtime WU is backlog-tracked). **No open deferred build gates.**
- **Next (hub):** ✅ **M7.2a SPLIT (P1) + M7.2a-1 ISSUED** (`context/instructions/2026-06-21_M7.2a-1_run-lifecycle_coding-instruction.md` — source-verified end-to-end: AMD-91/AMD-92, the full P2 pin survey with file:line, module-info verbatim-UNCHANGED, the SequenceConflictException + JPMS-residency + §4c-clock traps baked in). Remaining same-beat: **freeze the dashboard read-API contracts** + **confirm CI live and adopt as gate-of-record**; then **M7.2a-2** (execution/dispatch) + the **frontend-dev** + **Distribution-skeleton** lane prompts.
- **Commit handed to Nick (selective-stage; bang-free `git commit -F`):** hivemind = the V1 scope-record resolution update + this pm-handoff entry (+ the untracked v2 orchestrator prompt). ⚠ The all-three-repos truncated-tail phantom makes a blind `git add -A` unsafe on docs/hivemind — stage these two files explicitly, verify host-side.

## 2026-06-20 — DOC 16 (Superior Automation Layer) LOCKED + Q1/Q2/Q3 RULED + AB-1/AB-2 SHA RECONCILE (Cowork PM mission-control hub, standing)

**Outcome: Doc 16 is LOCKED; the M7.2b entry-gate is cleared; the next Coder slot is M7.2a; AB-1+AB-2's commit SHA is reconciled.** Standing-hub session succeeding the 2026-06-19 hub. Step-0 preflight **STALE (currency-lag only; zero CONFLICTED)** — live HEADs core `9e73d28` / docs `f6c0c64` / hivemind `413930e` reconcile to the dispatch baseline exactly; **the truncated-tail mount phantom is ACTIVE across ALL THREE repos** (broader than the brief's docs-only flag), host-verified intact via the file tools (`HomeSynapseCore.watchdogPeriod` at L991; snapshot whole; both phantom diffs end `\ No newline at end of file`) → VM `git status`/`diff` quarantined; Check 9 PASS (skill mirrors byte-identical).

- **Doc 16 Lock applied (single-writer fold).** The independent context-isolated DOCS review (`context/audits/2026-06-20_Doc16_independent_DOCS_Review_Return.md`) returned **SCOPE = RIGHT** + **RATIFY-WITH-EDITS** (E1/E2/E4/E5/E6/E7 + S1/S2, all NON-BLOCKING; §7.2 cross-check re-run at `60d50ce` → M7.1-UNAFFECTED). Edits were already folded host-side (verified §5.2 E4 citations + the §4.7 `AutomationComponent` Glossary entry with the deprecated-"Component" disambiguation); I flipped `16-superior-automation.md` **DRAFT→Locked** and registered the invariants.
- **The diligence catch (why this was not a rubber-stamp).** The dispatch brief's masthead said "mint 2 → 167/49"; the **folded Doc 16 §5.3 leaned register-all-4.** I did not take the brief's number — surfaced the fork to Nick (the Locked doc text and the register had to agree). **Q1 RULED: mint 4 → 169/49.** INV-SA-01 (no-DSL) + INV-SA-02 (federation) novel/first-class; INV-SA-03 (explanation-projection / no parallel trace store) + INV-SA-04 (deterministic terminal + recorded reason) citing compositions — each adds a constraint its parents do not impose. Provisional-tag mapping recorded in §5.3: [SA-INV-1]→01, [SA-INV-4]→02, [SA-INV-2]→03, [SA-INV-3]→04.
- **Q2 RULED: INV-SA category, watermark stays AMD-94** (new-design-doc Lock, not an amendment — registers at its own Lock like the foundational docs / §19 subsystem categories). Registered `Architecture_Invariants_v1.md` §49 (new section) + §0.3 (the `SA` row) + §17 index (4 rows) + §18 matrix (1 row); **total host-re-derived 169/49** (169 index rows, 49 category rows — counted, not propagated). **M7.2b entry-gate CLEARED.**
- **Q3 RULED: next Coder slot = M7.2a** (run/dispatch differentiator, Doc-16-independent, buildable now; AB-4 has runway — nothing writes a sensitive scope until M9). **AB-4 dispatch authored concurrently** to capture the fresh folded-Doc-15 crypto context (F1 `v1` slot / §3.4/§5), slotted anywhere pre-M9.
- **SHA reconcile.** AB-1+AB-2 committed `9e73d28` (the prior "core SHA reconciles next step-0"); snapshot masthead/footer/Current-WU/Design-Docs + the backlog currency note updated to match.
- **Open Risks: unchanged** (OR-APP-AUTH/C1 RESOLVED-for-REST; OR-RF-DECRYPT/C2 resolved-at-MVP; OR-M6-NONCE CLOSED; OR-M13-SDNOTIFY open). **No open deferred build gates.**
- **Commits handed to Nick (selective-stage — the all-three-repos phantom makes a blind `git add -A` unsafe on docs/hivemind).** docs = ONLY `design/16-superior-automation.md` + `foundations/HomeSynapse_Core_v1_Glossary.md` + `governance/Architecture_Invariants_v1.md` (NOT the VM-phantom `15-cryptographic-architecture.md` / the AMD-94 file / the invariants-phantom — host git won't show them). hivemind = snapshot + backlog + this pm-handoff + the untracked orchestrator prompt. Bang-free `git commit -F`.
- **Next:** author the **M7.2a coding instruction** + the **AB-4 dispatch** (per `references/coding-instruction-format.md`); then Nick routes M7.2a to a Coder slot. The WS Phase-3 runtime WU + the Doc 09/12 currency amendment remain parallel/downstream lanes.



**Outcome: AB-1 (authenticated HTTP surface) + AB-2 (fail-closed read contract) are gate-verified GREEN (`./gradlew check` 149 tasks) after one gate-fix round → PM WUCP Phase 2 = APPROVE.** The composition-root HTTP server is no longer unauthenticated — core-review **C1** closes for REST. Built by Claude Code from the Seam-1 instruction; **AB-4 stayed ⛔ HELD throughout** (cipher/envelope untouched).

- **AB-1 (REST auth + bind).** `StandardAuthMiddleware`/`StandardRateLimiter`/`OpaqueTokenStore` (opaque bearer tokens, JDK-native SHA-256 token hash, **zero new catalog dep / zero new module edge** — the two-gate check is clean, independently confirmed by the GREEN `moduleGraphAssert`); a catch-all `RestFilters.installAuth` over `/api/*`+`/internal/*` with **auth-before-path-canonicalization** (RFC 9457 problem responses); **loopback-default bind** via a new `HomeSynapseConfig.bindHost` (5th record component; LAN = explicit opt-in); a first-run **pairing token → `config/initial_api_token`** (+WARN) = the operator bootstrap credential; `opensHttpOnlyBehindAuth`+`httpSurfaceBindsLoopbackOnly` in the default gate. `ApiKeyClaims` = the A2 enterprise per-scope/per-site claims hook (server-side, off `ApiKeyIdentity`).
- **AB-2 (fail-closed read).** Typed `PayloadDecryptionException` in `SqliteEventStore`'s read path, classifying **CASE-a** (key absent/destroyed) vs **CASE-b** (GCM-auth fail); **per-read-batch scope**, whole-batch abort retained (the A3-ruled MVP fail-closed posture); degrade seam (`DegradedEvent`, `(scope,key_version)`) **designed-not-wired** per F4. 3 fail-closed tests.
- **Gate-fix round 1 (only round).** The new AB-2 test didn't declare the checked `SequenceConflictException` on its `publishRoot` sites — the **recurring M6.3 trap** (coder-lessons 2026-04-10). Declarations-only fix (sibling-test `throws` pattern); the Coder swept the other touched test files (none else). No assertion/logic changed. **Deferred build gate: RESOLVED GREEN** (149 tasks).
- **Scope finding — AB-1 WS first-message auth could NOT build (ACCEPTED → new WU).** `api/websocket-api` is **Phase-2 scaffold** (26 files, interfaces/records only; no handler/relay/codec/lifecycle impl, no `app.ws("/ws/v1",…)` wiring, zero runtime module edges) — I verified host-side. The Seam-1 instruction over-assumed a live `/ws/v1`. Disposition: a new **WS Phase-3 runtime + auth WU** (backlog row added; sequence near M11 or sooner). **No security gap today** (no WS surface exists); INV-SE-02-for-WS is the WU's acceptance gate; the shared `OpaqueTokenStore`/`AuthMiddleware` are already built+tested for it. Instruction-assumption finding (logged), **not** a Coder defect — the Coder's ship-REST/defer-WS disposition is correct.
- **[REVIEW] deviations — all ACCEPT:** `ApiKeyClaims` (ruled A2 hook, zero fan-out); `bindHost` (A1 loopback, only direct ctor caller `CrashRecoveryHttpIT` updated); `Main` HTTP-messaging (an AB-1 consequence on an AB-4 file — ctor/cipher stayed HELD). [INFO]: the A1.5 `.host(` static check can't match (the bind is `app.start(config.bindHost(), config.httpPort())` per the Javalin-6 host API — proven by the two new lifecycle tests); stale "bcrypt" Javadoc left on the frozen `AuthMiddleware`/`ApiKeyIdentity`.
- **Open Risks:** **OR-APP-AUTH/C1 → CLOSED for the REST surface** (WS-auth requirement carried onto the WS WU). **OR-RF-DECRYPT/C2 → resolved-at-MVP** (fail-closed posture shipped per A3; graceful-degrade *behavior* → crypto-shred WU, gated on `chain_hash` liveness, F4). OR-M13-SDNOTIFY unchanged. **No open deferred build gates.**
- **Doc-currency carried (non-blocking):** production `start()` now binds loopback HTTP behind auth + mints the first-run pairing token — a real boot-behavior change → Doc 09 (auth/`/internal/*` family, port, drop "bcrypt") + Doc 12 + the deployment/operator docs need a currency note.
- **Residual:** the **Pi-profile E2E auth run** (`:testing:integration-tests:test -PpiProfile=throttled`) is a confidence check on the auth'd E2E path — Pi-gated, NOT in `./gradlew check` — pending Nick's result; the auth logic itself is covered by the rest-api unit tests + the lifecycle HTTP-auth tests in the GREEN gate.
- **Next-WU = AB-4** (the open Coder slot; un-gated by AMD-94). Its rows are already in the Seam-1 instruction; the hub authors/confirms the AB-4 dispatch next. The Doc 16 fold→Lock (hub-owned) + the WS-runtime WU run in parallel/downstream.
- **Commit:** the AB-1+AB-2 code is its own homesynapse-core commit (GREEN, self-contained; message handed to Nick, core SHA reconciles next step-0); this closeout + the Coder's coder-handoff/cross-agent-notes/coder-lessons = the hivemind AB-1+AB-2 batch.

## Rotated session records

**Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; git-object-sourced from `58794d7`):** the ratification record (Return ASSESSED → RATIFIED → mechanics), the superseded fold-abort record, the M7-AMD-block record, the M6.4-WUCP record + the M6.2-era fragment, and the RESOLVED/CLOSED build-gate blocks from Open Risks → `archive/pm-handoff-2026-06.md`. Earlier rotations per the markers preserved there. Full history in git.

## Open Risks


#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
