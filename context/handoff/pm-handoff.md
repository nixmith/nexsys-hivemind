<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-27 (v39 hub, beat 8 — **THE WCAP-2 INTAKE + THE v39 CLOSE-OUT: v39 ENDS on the beat-8 landing.** **The measured answer: fresh-run chains hydrate UNANIMOUSLY** (3/3 populated, 1693–1717 B vs the 507 B skeleton; capture C = a POST-deploy-stamped run with `automationName: "bench-hero"` + `trigger.type: "StateChangeTrigger"` resolving — **the G1 demo's wire is PROVEN**; the live five-modes vocabulary observed raw). **The three-era gradient separates TWO mechanisms:** action hydration follows run ERA (the Jul-19↔Jul-25/26 correlation-stamping boundary — a HISTORY artifact, P3) while name/trigger-type follow IDENTITY CURRENCY (the rotation inference upgraded — the join works, the nulls are rotation fallout; WCAP-1's `commandCount: 0` retro-explained as the same non-hydration). **The G1 watch RE-NARROWED to ONE dev-tools glance** (does the SPA render the proven-populated payload — the redux blanks on recent runs remain the honest residual); the ledger re-stamped. NEW small row: `firingValue: null` in ALL eras (the hero's opening line; rule at STATE-DIALECT authoring). **v40 BANKED** (`context/handoff/2026-07-27_PM-mission-control_v40_orchestrator_session_prompt.md` — **B2 = its FIRST act, SOLE, full-bar**: v39 hands it to a fresh context deliberately rather than compressing it at depth; the chain-glance intake, the small-fix stack, the docs fold + skills pass, and the gate calendar all charged). Detail: the beat below.) Prior: 2026-07-27 (v39 hub, beat 7 — **THE NORTH-STAR INTAKE + DELIBERATIONS D1–D6** (Nick's charge: long-term research aims at the agent-substrate thesis — the Technical Overview FILED at `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md`): D1 filed-as-north-star (feeds the fusion synthesis + the launch-runway charter + flag-plant content) · **D2 the seams claim VERIFIED FALSE-TODAY at source** (no agent SubjectRef type, no proposal/adjudication event types; the governance half exists — AIOT-INV-1 §50) → **AGENT-SEAMS minted as a post-gate/launch-runway candidate** (make the claim TRUE; additive event-model WU); external use softens the sentence meanwhile · D3 **the AGENT-SUBSTRATE RESEARCH LANE authored** (fresh Cowork, zero gate cost, lawful now — verifies every literature claim at primary source, EU-AI-Act Art-12 at the statute, the functional-safety/RV/capability prior art, the competitive scan, the post-gate program shaping, + the document honesty table) · D4 AUTO-IDENT gains strategic weight (attribution-as-graph-traversal requires durable identity; still REC post-gate) · D5 gate sovereignty re-affirmed (zero pre-freeze code from the document) · **D6 the capacity-honest re-sequencing: CHAIN-HYDRATE's authoring GATES on WCAP-2** (the beat-6/7 desk checks EXONERATED the read path by diff [`buildActions` byte-stable across `4bc1258..c09c61c`] and re-opened the recent-run hop — the Jul-25 Rosonway field evidence proves fresh chains hydrated on `355a711`; WCAP-1 measured only an 8-day-old run; **WCAP-2 authored: 3 captures on recent runs with the interpretation table in-file**; L1 — never author on an unmeasured hop) **· B2 = the NEXT hub beat's SOLE act at the full bar.** Detail: the beat below.) Prior: 2026-07-27 (v39 hub, beat 6 — **THE WCAP INTAKE: THE CLUSTER SPLITS THREE WAYS, ALL ADJUDICATED** (+ two hub desk checks at source). **F4 = SERVER-EMPTY, CONFIRMED + LOCALIZED:** the chain hydrates from `readByCorrelation` (`StandardExplanationService:176` — pure log, NO registry dependency), so the self-contradicting skeleton (`actionCount: 9` · `actions: []` · `commandCount: 0` · `trigger.type: null` while `matchedAt` populates) means the run-family events are found but the COMMAND events are not — a correlation-join seam between the executor's emission stamping and the read; **WU-CHAIN-HYDRATE named (core, P1, THE G1 gate-day blocker — outranks M14; next-authored WITH B2)**; fixture-vs-live is the hazard class (the fixtures write command events under the run's correlation and pass — the live executor may stamp differently; composed-behavior-under-state-history). **F3 = STATE-DIALECT (DRIFT-2-class, core-side + an FE consumption half):** the freeze doc's A3 specifies the AMD-52 typed-value envelope `{"t":…,"v":…}` — the wire serves `{"value":…}` + fractional epoch-second instants + a raw `{msb,lsb}` entityId (Jackson-default leakage), and the SPA reads flat values: THREE dialects, NOBODY matches the contract; the fix conforms BOTH sides TO the contract per the DRIFT-1 precedent (core hand-builds like its siblings; FE-LIVE-V112 +(h) consumes A3-conformant); the seconds-as-ms "9:52 AM" mechanism arithmetically proven in the return. **F2 DISSOLVED → THE REAL FINDING: AUTOMATION IDENTITY ROTATES EVERY BOOT** (bench-hero's ULID differs across restarts; orphans ALL run history from names/lastRunId — explains four symptoms at once); the why-not endpoint is HEALTHY (404 in 6 ms; the browser "hang" = the id rotating into an FE spinner-without-error posture); **AUTO-IDENT chartered as a design question — REC post-gate** (identity/event semantics in the freeze window = wrong risk; the G1 demo scripts a FRESH run and is rotation-compatible); Nick's one-liner asked. G1's ledger WATCH NARROWED to CHAIN-HYDRATE alone. FE-LIVE-V112 +(f) error postures +(g) timestamp qualification +(h) A3 conformance. The WCAP return FILED at audits/. Residuals kept honest: the valid-id why-not capture + the rotation-confirm read ride the next bench touch. Detail: the beat below.) Prior: 2026-07-27 (v39 hub, beat 5 — **THE DEPLOY-EVENING INTAKE: COMPLETE — THE PI RUNS `c09c61c`; G2 ✅ CLOSED** (the redux's Glance-1 tile verbatim = the strongest evidence; both ledger cells flipped; the serve path proven live: AuthGate → token accepted → ● Live; three `[PASS]` floors; the first npm-bearing build **49 s** — the envelope surprised FAST; the dashboard jar on the running classpath) · **THE FOUR FINDINGS ADJUDICATED: F1 = BY-DESIGN** (the `/api/v1/events` 404 is the M7.5c-b frozen-but-unbuilt slice, source-verified at all three carriers; graceful degrade working as designed; **EVENTS-API candidate row minted** so the L4 owner-row law is satisfied) **· F2/F3/F4 = THE DETAIL-READ CLUSTER** (every list surface populates, every detail surface empty/hanging — server-vs-client UNDECIDED; the bench's own successful chain fetches prove the wire is not dead) → **WCAP wire-capture block AUTHORED** (5 token-safe curls, ~10 min, instrument-first — measurement before theory) · **⚠ THE G1 EXPOSURE NAMED:** the gate-day demo legs (a)/(c) are BLOCKED until the cluster resolves — the WATCH rides both G1 ledger cells; the fix WU outranks M14 absolutely · FE-LIVE-V112 charter +(f) uniform honest error/empty postures +(g) timestamp qualification + the npm-audit advisory watch · the return FILED at audits/. Next: WCAP pastes → adjudication → the fix WU ∥ B2 authoring + the docs fold. Detail: the beat below.) Prior: 2026-07-27 (v39 hub, beat 4 — **THE THREE ANSWERS INTAKEN + THE DEPLOY BRIEF AUTHORED.** (1) **Check 9 CLEARS on Nick's word** ("we are and have been synced") — with the refinement RECORDED: the beat-1 STALE evidence was the REMOTE session's skill cache (a THIRD location, account-sync-lagged), NOT Nick's local mirror; host-CC lanes read the CURRENT mirror (the Coder's clean run corroborates); future remote-hub Check-9 adjudications must distinguish the two. (2) **THE SKILLS-PASS CHARGE intaken** (Nick authorizes a careful full optimization + re-sync) → SCHEDULED post-deploy-evening (one re-ground pass on deployed truth, not two; + the arc's lesson folds + the remote-cache refinement + §8 token economics). (3) **Deploy shape (a) SPLIT-EARLY RULED** → **the fresh operator brief AUTHORED** (`context/handoff/2026-07-27_dash-serve-deploy-evening_operator-brief.md` — Node 22 one-time op-order with disk check · the reset first-build envelope [5–20 min lawful; the jar self-assert = the loud failure] · the redux with the L3 token protocol · abort ladder to `2040a66`; L2 copy-paste-complete; navigator-optional per the v38 template). The evening runs whenever Nick is ready; **G2 closes on its three ⏺s.** The beat-3 landing `c9fd7b6` census-exact (4 M). Next hub acts: B2 AUTHORED + the docs fold. Detail: the beat below.) Prior: 2026-07-27 (v39 hub, beat 3 — **THE LANDING INTAKE: core `c09c61c` ALL-ACTIONS-GREEN (Nick's own verification — ci.yml + install-smoke's FIRST serve-assert run + frontend.yml) → DASH-SERVE ✅ DONE** (the backlog flip + the cross-agent note restamp executed this beat; the seam is CI-proven end-to-end and asserted on every future push) · **THE REDUX-UNBLOCK FINDING:** the served build defaults REAL-transport at `window.location.origin` and the paste-once token UX ALREADY EXISTS complete (`AuthGate.tsx` · `realTransport.ts` · 401/403 handling — verified at `c09c61c` source, every hop) → **the browser-block redux needs NO FE landing; G2 is gated only on the deploy evening itself**; FE-LIVE-V112 off G2's critical path (verification halves; launch still gated on the ⚠ mirror sync — STILL UNCONFIRMED, fourth ask) · B2's suite runs lawfully against the deployed `2040a66` (nothing wire-side changed) · the deploy-evening shape REC queued: (a) split-early [REC] vs (b) combined with B2's first evening · hivemind beat-2 landing `48385da` census-exact (6 M). Next: the docs fold + B2 authoring + the fresh deploy brief. Detail: the beat below.) Prior: 2026-07-27 (v39 hub, beat 2 — **THE DASH-SERVE TWO-LAYER AUDIT: ACCEPT, ZERO DEFECTS** (layer-2 host-truth reads of ALL 13 files + fresh porcelains; I1–I7 ratified; the "ALL THREE e2e red" instruction imprecision hub-owned — the preservation fixture cannot red by construction; the 157-vs-156 check-task delta reconciled to the INFO-6 empty-dir removal; **the M3 gate-reorder mutant SERVED the traversal probe (200)** — the EXACT-400 pin is the order's standing instrument; Nick's independent post-full-clean `check` GREEN 156/153 in 1m21s = the strongest Node-free proof) · the beat-1 landing `6669d9f` CENSUS-EXACT (9) · **THE CORE COMMIT ORDERED** (13 explicit paths → push → THREE glances: ci.yml + **install-smoke.yml's FIRST serve-assert run** + frontend.yml) + the hivemind beat-2 order (6 M) · the backlog flip + note restamp gate on the CI-green landing intake (the CMD-API precedent) · ⚠ the mirror-sync confirmation still owed (one word). Detail: the beat below.) Prior: 2026-07-27 (v39 hub, beat 1 — **LAUNCH + WU-DASH-SERVE AUTHORED ISSUE-READY.** Preflight: 10/11 PASS + **Check-9 MIRROR MISMATCH** (all three role-skill mirrors pre-date the v38 beat-3 pass; FE mirror reads v1.4 vs source v1.6) → **⚠ NICK'S MIRROR SYNC IS OWED BEFORE ANY LANE LAUNCH** (the DASH-SERVE Coder session included; authoring-at-desk proceeded lawfully — hub reads ride SOURCE). The beat-8 landing `026f6b3` CENSUS-EXACT (11) + the skills landing `844e561` (1 M) verified; the v38 prompt archived; the uploaded v39 prompt md5-≡ the banked copy. **THE AUTHORING:** instruction `context/instructions/2026-07-27_DASH-SERVE_dashboard-serve-path_coding-instruction.md` + pre-verification `WU-DASH-SERVE.md` (P0–P15) — the L1 re-verify re-confirmed all three blockers at `2040a66` source; **the authoring-caught hazard: the beat-7 "stageDashboard → processResources" sketch would have DRAGGED NPM INTO `check`** (java-plugin resources sweep + app testRuntimeClasspath) — realized mechanism: build-dir staging + npm-on-the-JAR-TASK-ONLY with a doLast packaging self-assert + the app-side testRuntimeClasspath exclude + a Node-free grep gate assert; the fourth hop verified ALREADY SOUND (vite base `/dashboard/`, hash routing); DP-5 CI rider (install-smoke setup-node ×2 copies + run-smoke serve asserts — the standing seam instrument); 6 new tests incl. the EXACT-400 traversal order pin + M1–M3; census EXACTLY 13; **CMD-API-ACTOR adjudicated OUT** (freeze-window single-concern; parked for the first post-gate rest-api touch); hub-owned at landing: the INV-SE-02 in-place amendment + the Doc 13 §3.2–§3.3 as-built note. Cross-agent ACTIVE census line reconciled (lagged the beat-6 restamp; ACTIVE 0); the coder-handoff dispatch entry written this beat is newest-authoritative (next Coder WU = DASH-SERVE, then core QUIET until M14). Detail: the beat below.) Prior: 2026-07-26 (v38 hub, beat 8 — **THE CLOSE-OUT: v38 ENDS on the beat-8 landing.** The beat-7 landing `8ca133c` CENSUS-EXACT + the Pi one-liner RAN (the stray store + token burned) · **RULINGS BY DELEGATION: posture (A) static-shell exemption RULED** (GET/HEAD `/dashboard/**` + the root redirect ONLY; every data route stays guarded) **+ the sequencing ratified** (DASH-SERVE authored first · B2 second · the next deploy evening carries DASH-SERVE → the browser-block redux → G2 closes) · **THE STALE-CONTEXT SWEEP:** two live carriers fixed (the playbook §8 interim dead-zone rule → ⛔ EXPIRED-stamped; the frontend skill → v1.6 with §4c/§4d re-grounded to deployed truth + THE SERVE-PATH REALITY bullet) + 3 consumed one-offs archived (the FE lane prompt · the deploy brief · the navigator prompt) + every other hit adjudicated dated-record-stands · **pm-lessons +4** (L1 every-hop · L2 practiced-X ban · L3 token-paste poison · L4 design-intent-needs-an-owner-row) · **v39 BANKED** (`context/handoff/2026-07-26_PM-mission-control_v39_orchestrator_session_prompt.md` — DASH-SERVE first, all rulings baked, Check-9 mirror expectation pre-declared). Detail: the beat below.) Prior: 2026-07-26 (v38 hub, beat 7 — **THE DEPLOY-EVENING INTAKE: COMPLETE — THE PI RUNS `2040a66`** (six lawful boots; soak clean; floor + deploy + verdict all [PASS]) · **A4 ✅ + H4 ✅ BANKED** (kill −9: position 25065 preserved, six ULIDs identical; the restart rep lawful) · **Block 4b UNOBSERVED — THE DASHBOARD HAS NO SERVE PATH: three stacked blockers verified at source** (B-1 no `staticFiles` mount anywhere in lifecycle/app · B-2 no web-ui artifact — `runtimeOnly(project(":web-ui:dashboard"))` resolves EMPTY, npm wired to `assemble` only, so installDist neither builds nor packages the SPA [the 23s build = the arithmetic proof] · B-3 AB-1's catch-all `before(*)` auth [INV-SE-02] 401s any headerless browser navigation) → **WU-DASH-SERVE named P1** (core; gates G2 + FE-LIVE-V112; the auth-posture ruling asked — REC (A) static-shell exemption) · the beat-6 "installDist builds the dashboard — verified at source" grounds claim CORRECTED and OWNED (a transitive claim verified at one hop of three; the deploy lost nothing; (b) still right in hindsight — B-1/B-3 predate the FE landing, the glances were impossible under either shape) · FLAGS 1–3 retire WIRE-side, observation legs → FE-LIVE-V112 (BLOCKED on DASH-SERVE) · the §8 interim dead-zone rule EXPIRED (DP-3 live) · the 3.1 stray-store residue adjudicated nil-harm (navigator error; the leaked token = the stray store's only, never live-valid; `rm -rf ~/homesynapse-core/.homesynapse` ordered = the structural burn) · the return filed at context/audits/. Detail: the beat below.) Prior: 2026-07-26 (v38 hub, beat 6 — **`da11f46` ALL-CI-GREEN (Nick’s confirmation) → SKIP-VIS ✅ DONE** (the backlog beat-6 head note = the flip of record; the Coder’s note ARCHIVED-SERVED) · the beat-5 landing `3909a1d` CENSUS-EXACT (89 = 9 M + 7 new + 73 R, pushed) · **THE FE-VERDICT-2 TWO-LAYER AUDIT: ACCEPT, ZERO DEFECTS** (census porcelain-exact 20+1, lockfiles untouched; the Q1b formula core-identical field-first; the never-false validator; the four AA token corrections computed-in-place; D-1..D-6 RATIFIED [D-1 = the correct R-1-HELD reading — the dispatch’s brand-token phrasing owned as hub imprecision]; G2 → DELIVERED-pending-the-live-glance; **the dependabot deps watch CLOSES**; FE-LIVE-V112 = the named next FE WU) · **the deploy-shape one-liner queued: REC (b) — land the FE commit now, tonight deploys the FE landing** (installDist builds the dashboard on the Pi — verified at source; one evening banks the write surface + the honesty fixes + G2-live + FE-LIVE (a)–(c)). Detail: the beat below.) Prior: 2026-07-26 (v38 hub, beat 5 — THE LANDINGS INTAKEN: core da11f46 CENSUS-EXACT (12 files, 672+/58−, PUSHED — SKIP-VIS ON MAIN) + hivemind 6393554 (6 M, pushed); both ran as add-A and matched only because the trees held exactly the ordered sets — EXPLICIT-PATHS stays ABSOLUTE; **ci.yml on da11f46 = the ONE owed glance** (the backlog SKIP-VIS flip + the cross-agent note archival gate on it → beat 6) · THE FULL CLEANUP+ROTATION (Nick’s charge): pm-handoff/snapshot/coder-handoff/cross-agent/coder-lessons/pm-lessons/backlog rotated (~1.0 MB → ~0.35 MB hot-path); 7 rotation archives minted; weeks W25–W29 archived; _scratch 69 → consumed/; 12 served handoff one-offs + 46 instructions + 10 pre-verifications archived (citation-grepped: 0 standing refs; the one rosonway ref lived in the deploy brief and is amended) · THE DEPLOY BRIEF AMENDED: target **da11f46** + a ci.yml-GREEN-verified precondition (Block 1); Block-5 Rosonway line updated (DONE); the FE brief baseline updated (landed-not-deployed) · the launch/dispatch instruction set issued. Detail: the beat below.) last-verified: 2026-07-26 (v38 hub, beat 4 — THE SKIP-VIS TWO-LAYER AUDIT: **ACCEPT, ZERO DEFECTS** (layer-2 host-truth reads of all 12 files; every DP realized exactly — the 5-branch derivation + fact-carry, the payload-first commandless branch, the shared derivedTriggerInstant at both DP-3 sites, DP-4 GO with the Q1b formula; wire keys appended-last camelCase, existing keys byte-same, ListRunsEndpoint untouched, SD-5 pins verbatim; counts 25/16/10/9 cohere with the XML totals; ONE INFO adjudicated: the report’s "(9→10)" prose slip for AET, truth 8→9; I1–I7 all RATIFIED; WUCP P1 5/5, next-WU=B2, the splice seam verified intact) · the freeze-doc v1.1.2 amendment EXECUTED (hub-owned: masthead stamp + three inline notes per the v1.1.1 precedent) · THE CORE COMMIT ORDERED (12 explicit → push → ci.yml = the gate of record) + the beat-4 hivemind order (6 M) · the backlog flip waits for the CI-green landing intake · **Nick’s deploy-target one-liner queued: tonight = the SKIP-VIS landing HEAD (REC) or 4bc1258 as-briefed**. Detail: the beat below.) Prior: 2026-07-26 (v38 hub, beat 3 — THE SKILLS CURRENCY PASS at Nick’s pre-launch charge: all three role skills swept full-read + grep-classes, 8 drift classes CONFIRMED-and-FIXED at 10 files across 2 repos — headline: the PM constraint-enforcement licensing principle RE-GROUNDED from "proprietary preserved / one-way door optionality" to the LOCKED Apache-2.0-forever law (the ambition-critical fix) · the asimtote-leading brand drift resolved at both carriers to the RULED Architecture C (TAMORO forward / ASIMTOTE quiet parent; R-1 HELD until G-2) · the NO_DIRECT_TIME_ACCESS test-reach claim corrected at its three coder carriers · the coder §3.9 until-clause aligned to the authored SKIP-VIS truth · PM doc-counts + Build-Verification reconciled · format additions #16/#17 (ARCH-RULE-REACH · SEAM-I/O) folded at the operative checklist · FE +§4d (the v1.1.2 ruling · the five-modes law · the triggeredAt interim caveat). The beats-1–2 commit LANDED 62007ce CENSUS-EXACT mid-beat (8 entries incl. the archive pair; pushed; its msg CONSUMED; the drafted beats-1–3 msg = SUPERSEDED PRE-ISSUE, never run — the orphan-sweep set grows by one); beat 3 commits ALONE (exactly 9 M) + the skills-repo order (3 M); ⚠ MIRROR SYNC BEFORE ANY LANE LAUNCH. Detail: the beat below.) Prior: 2026-07-26 (v38 hub, beat 2 — LAUNCH PASS all 11 (beat 1; the beat-4 landing 8fc7dfb census-exact; the v37 prompt archived; the uploaded prompt md5-≡ the banked copy) · SKIP-VIS AUTHORED ISSUE-READY (context/instructions/2026-07-26_SKIP-VIS_explanation-honesty_coding-instruction.md + pre-verification WU-SKIP-VIS P0–P14; three DPs + ⛔DP-4/Q1b one-line-rulable; 12-M census; the two-layer audit caught 2 authoring DEFECTs pre-issue [the AutomationEndpointsTest non-firing pin · the §3.9-emits-nothing correction] — both folded) · FE-VERDICT-2 lane brief AUTHORED (five-modes law + §3a CVD + G2 live-half + the deps watch) · G2 → IN-FLIGHT on the ledger · EXEC-DETERMINISM adjudicated OUT/re-homed post-gate · the deploy stays Nick’s next physical act (tonight lawful; Block 0a first; five pastes → intake here). Detail: the beats below.) Prior: v38 beats 3/2/1 (below). The v37→v32-era Prior-chain + beat sections ROTATED 2026-07-26 → archive/pm-handoff-beats-v32-v37-rotated-2026-07-26.md (§A the chain verbatim · §B the sections verbatim); earlier: archive/pm-handoff-beats-rotated-2026-07-19.md + git history.
-->

# PM Session Handoff

## 2026-07-27 (v39 beat 8 — **THE WCAP-2 INTAKE (the hop is measured: fresh chains HYDRATE — the demo wire proven; the gradient separates the two mechanisms)** · the G1 watch re-narrowed to ONE glance · **v40 BANKED — v39 closes on this landing**)

**The intake (the WCAP-2 return, status COMPLETE — filed at `context/audits/2026-07-27_WCAP2_recent-run-chain_return.md`, its self-path already audits-correct per the block's stated rule).** The beat-7 commit **`5ef42bf`** landed CENSUS-EXACT (6, pushed). **The one unmeasured hop is measured, unanimously:** captures A/B (Jul-26 runs, pre-deploy-stamped on `2040a66`, rotated identities) and C (the freshest run, 23:47Z Jul-27, POST-deploy-stamped on `c09c61c`, CURRENT identity) all serve **populated chains** — 5 CommandActions each, the live five-modes vocabulary raw on the wire (`UNCONFIRMED`/"confirmation timed out" · `DISPATCHED`/`resultOutcome: "superseded"` · the verbatim "DefaultResponse SUCCESS +90 ms, then no report, ever" · `settled: true`), `actionCount: 9` ⊃ `actions[5]` (the delay actions uncounted in the array — the FE expects array ⊂ count). Capture C additionally resolves `automationName` + `trigger.type` — **the identity join WORKS for current identities; the nulls elsewhere are rotation fallout** (WCAP-1's inference upgraded to near-proven; one restart-double-read still rides B2 prep). WCAP-1's `commandCount: 0` retro-explained: the same era non-hydration zeroes the derived tally. **The adjudication: interpretation-table branch 1 fires — CHAIN-HYDRATE RE-SCOPES:** (i) the era-boundary bounded core look (Jul-19 ↔ Jul-25/26 correlation-stamping history) = **P3, non-gate** (history is append-only; the affected runs pre-date the boundary forever); (ii) FE-LIVE-V112 gains **(i) the honest "no detail recorded for this run" state**; (iii) **the ONE remaining G1 residual = the SPA's render of populated payloads** — the redux's recent-run blanks are honestly unexplained (the wire NOW serves populated; whether it did at browser time is unmeasurable, but the read path is deploy-stable) → **Nick's dev-tools glance decides it** (F12 → Network → open the freshest run in the dashboard → ⏺ the causal-chain response status/size + what renders; renders = the G1 watch CLEARS [the redux blanks eras-confounded]; blanks-on-populated = a small FE render fix, FIRST item of FE-LIVE-V112). The ledger's G1 cells re-stamped to exactly that. NEW small row: **`firingValue: null` in ALL eras incl. C** — "what set it off" never carries a value (stamp-side or serialize-side, one look; the hero's opening line; priority ruled at STATE-DIALECT authoring). The C-slip 400 (`RUNID` placeholder → honest ULID-validation problem+json) = a positive corpus note + an L2 template addendum (placeholder lines get a fill-in warning).

**THE v39 CLOSE-OUT.** **v40 BANKED:** `context/handoff/2026-07-27_PM-mission-control_v40_orchestrator_session_prompt.md`. The deliberate hand-off: **B2 is v40's FIRST act, sole, at the full bar** — this session reached its arc boundary (launch → DASH-SERVE authored/audited/landed/deployed → **G2 CLOSED** → the cluster measured-and-split → the north star intaken and programmed) and hands B2 to a fresh context rather than compressing the week's concentration risk at depth (the v39-prompt's own "v40 BANKED BEFORE DEPTH" law). v40's charges: B2 first (rebind → §51 port → riders → the three-flip re-mint; B3 behind it; the Jul-31 trigger named) → the chain-glance intake + the small-fix stack (STATE-DIALECT · FE-LIVE-V112 (a)–(i) · the era look · firingValue) → the docs fold + THE SKILLS PASS (+ Nick's re-sync) → the research-lane return intake → M14 margin-gated → H3 (the no-Node-on-target pin) → the Aug-10 dry-run → Aug-12–13 I2 + close-out + v41 + THE LAUNCH-RUNWAY CHARTER (AGENT-SEAMS · AUTO-IDENT · posture-(B) · EVENTS-API · the research rows) → **Aug-14 EOD FREEZE → Aug-16 THE READ.** The standing carries: the pending AUTO-IDENT one-liner (REC post-gate) · TSDR #2 Thu · Pelton ~Aug-5 → G-2 two acts → BRAND-G2-EXEC.

**Nick, in order:** **(1) THE BEAT-8 COMMIT** (hivemind) — EXPLICIT paths, exactly 6 porcelain entries (4 M + 2 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/assessments/2026-07-11_go-no-go-criteria_draft.md context/audits/2026-07-27_WCAP2_recent-run-chain_return.md context/handoff/2026-07-27_PM-mission-control_v40_orchestrator_session_prompt.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-8_commit-msg.txt` → push. **(2) THE DEV-TOOLS GLANCE (~5 min, anytime):** dashboard open (tunnel + token as at the redux) → F12 → Network tab → click the freshest run → **⏺ the `causal-chain` response's status + size + whether the card renders the actions** → paste to the next hub. **(3) LAUNCH v40 whenever ready:** `Read nexsys-hivemind/context/handoff/2026-07-27_PM-mission-control_v40_orchestrator_session_prompt.md and execute it.` **(4) the still-pending one-liner: AUTO-IDENT post-gate (REC).** **(5) your ledger:** TSDR #2 **07-31 (Thu)** · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12 · the research lane at leisure.

**v39 CLOSES on the beat-8 landing.** Eight beats: authored and audited a WU to a zero-defect landing, put the dashboard live behind a ruled exemption with the whole seam CI-instrumented, **closed G2 on a verbatim-perfect live tile**, split a four-symptom failure cluster into measured mechanisms with two ten-minute instrument blocks, intook the company's north star with its one false claim caught at source, and banked a successor loaded for the evidence engine. The hub stands relieved on the landing paste.

## 2026-07-27 (v39 beat 7 — **THE NORTH-STAR INTAKE (deliberations D1–D6 by delegation)** · the CHAIN-HYDRATE evidence re-opened honestly → **WCAP-2 gates its authoring** · the research lane authored · **B2 = the next beat, sole, full-bar**)

**The intake.** The beat-6 commit **`68570e5`** landed CENSUS-EXACT (5, pushed). Nick's charge: assume the long-term research aims at the uploaded **HomeSynapse Technical Overview** (the agent-substrate thesis: *the harness enforces, the model only proposes*; deterministic runtime enforcement at the actuation boundary; the event log as state + audit record + dataset; agents as first-class principals; shadow mode; counterfactual replay; the EU-AI-Act Art-12 tamper-evidence angle) — and make the deliberations/decisions to author the next sessions. **FILED:** `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` (verbatim under a filing frontmatter).

**THE DELIBERATIONS (D1–D6, hub by delegation).** **D1 — the document IS the north star:** it is the crystallization of the standing moat thesis (never-false-CONFIRMED · the explainability hero · the receipts corpus) lifted to the agent-substrate frame; it FEEDS (not replaces) the standing programs — the fusion ~mid-Oct synthesis consumes it, THE LAUNCH-RUNWAY CHARTER takes its candidates, the flag-plant content class gains a publishable seed ("Open research questions" is an essay waiting). **D2 — the load-bearing claim VERIFIED, and it is FALSE-TODAY:** "the event model reserves the seams it requires … agents as a first-class subject type" — greps at `c09c61c`: SubjectRef has NO agent type; EventTypes has NO proposal/adjudication types. The DESIGNED-FOR half is real (Doc 17 Locked; **AIOT-INV-1 "AI is never an autonomous actuator" registered §50**) — but reservation-in-code does not exist. Ruling: **make it true, post-gate** — **AGENT-SEAMS** minted (additive event-model WU: the proposal/adjudication/execution event types + the agent subject type + AIOT-INV-1's enforcement hook; a launch-runway-charter candidate beside AUTO-IDENT/posture-(B)/EVENTS-API); any external use of the document softens that sentence until it lands (the research lane's Charge 6 builds the full claim-by-claim honesty table: TRUE-TODAY / DESIGNED-FOR / ASPIRATIONAL — incl. tense-checking "hash-chained" and "rides Matter and Thread"). **D3 — the lawful-now move is research:** `context/handoff/2026-07-27_agent-substrate-research_lane_session_prompt.md` AUTHORED (fresh Cowork; read-only; ONE return file; zero gate-lane contact): verify EMBGuard/HomeGuard/RoboGuard/AgentSpec/IS-Bench at primary source (refute-welcome) · Art-12 at the statute · **the prior art the document does not cite** (IEC-61508-class functional safety, runtime verification, capability security — the defensible novelty claim is likely THE COMPOSITION, and the lane names it honestly) · the competitive scan · the post-gate program shaped into 3–5 effort-classed rows mapped onto the research agenda. **D4 — AUTO-IDENT elevates:** "attribution is a graph traversal" and long-horizon retrieval REQUIRE durable identity — AUTO-IDENT becomes a NAMED PREREQUISITE of the agent layer (timing ruling still Nick's pending one-liner; REC post-gate unchanged). **D5 — gate sovereignty re-affirmed:** nothing in the document touches pre-freeze code; every build-out charters post-gate. **D6 — the capacity-honest re-sequencing (the week's execution truth):** the beat-6/7 desk checks moved CHAIN-HYDRATE's evidence — the diff **exonerates the read path** (`buildActions` byte-stable `4bc1258..c09c61c`; SKIP-VIS touched only Javadoc references to it), `AutomationActionStartedEvent` has existed since 2026-06-22, `commandCount: 0` traces to a pre-SKIP-VIS terminal payload (Jackson-defaulting int), and the Jul-25 Rosonway field record (§5.4: a live chain with 5 action entries) PROVES fresh chains hydrated on `355a711`. WCAP-1 measured only an 8-day-old run; the browser's "all runs empty" composes possibly-multiple causes. **The one unmeasured hop: does a RECENT run's chain hydrate on `c09c61c`? WCAP-2 AUTHORED** (`context/handoff/2026-07-27_WCAP2_recent-run-chain_operator-block.md` — 3 captures with the interpretation table in-file; returns file to audits/ per the standing rule, now stated in the block itself). **CHAIN-HYDRATE's instruction authors ON its pastes, never before** (L1). **B2 = the next hub beat's SOLE act at the full bar** (the Jul-31 suites-running trigger governs; B2's grounding reads are bench-repo, untouched by any of this).

**Nick, in order:** **(1) the beat-7 hivemind commit** — EXPLICIT, exactly 6 porcelain entries (3 M + 3 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md context/handoff/2026-07-27_WCAP2_recent-run-chain_operator-block.md context/handoff/2026-07-27_agent-substrate-research_lane_session_prompt.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-7_commit-msg.txt` → push. **(2) WCAP-2 — ~5 min on the Pi, anytime** (it gates CHAIN-HYDRATE's authoring). **(3) LAUNCH the research lane at leisure** (fresh Cowork): `Read nexsys-hivemind/context/handoff/2026-07-27_agent-substrate-research_lane_session_prompt.md and execute it.` **(4) the pending one-liner: AUTO-IDENT post-gate (REC) — still open.** **(5) your ledger:** TSDR #2 **07-31** (Thu) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v39):** **B2 AUTHORED (the next beat, sole, full-bar)** → the WCAP-2 intake → **CHAIN-HYDRATE AUTHORED on its evidence** → the Coder runs it → STATE-DIALECT → the docs fold + THE SKILLS PASS (+ re-sync) → B3 nightly → FE-LIVE-V112 (f)(g)(h)(i: the honest no-detail-recorded state) → M14 (margin-gated) → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER (AGENT-SEAMS · AUTO-IDENT · posture-(B) · EVENTS-API · the research lane's rows) → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-27 (v39 beat 6 — **THE WCAP INTAKE: the cluster splits and every piece lands somewhere named** — WU-CHAIN-HYDRATE (the one G1 blocker) · STATE-DIALECT (DRIFT-2) · AUTO-IDENT (the design discovery; Nick's one-liner) )

**The intake (the WCAP return, status COMPLETE — filed at `context/audits/2026-07-27_WCAP_detail-read-wire-capture_return.md`, the handoff/ self-path corrected at filing).** All five captures landed token-free against the untouched pid 42770 on `c09c61c`; the beat-5 landing verified at the next porcelain. The instrument-first bet paid: the cluster is NOT one bug and NOT one side.

**F4 — ADJUDICATED SERVER-EMPTY, and the hub's desk check LOCALIZES it.** Capture 2 is the conviction: HTTP 200, 507 bytes, `outcome.actionCount: 9` + `durationMs: 34026` against `actions: []` + `conditions: []` + `trigger.type: null` + `commandCount: 0` — while `matchedAt` populates. The hub then read the assembly at source: `StandardExplanationService:176` — `chain = eventStore.readByCorrelation(correlationId)` → `buildConditions/buildActions(chain, runId)`. **Pure log-derived, zero registry dependency** — so identity rotation does NOT explain F4, and the split evidence (run-family events found: matchedAt, outcome; command events absent: actions, commandCount 0) points at ONE seam: **the correlation join between what the live executor stamps on command events and what `readByCorrelation` reads.** The named hazard class for authoring: the automation test fixtures write command events under the run's correlationId and pass 203-green — if the LIVE executor stamps children differently (own correlation per command, causation-linked), fixtures are structurally blind to it: composed-behavior-under-state-history, the §51 class. **WU-CHAIN-HYDRATE minted: core, P1, THE G1 gate-day blocker, outranks M14 absolutely, next-authored alongside B2.** Its shape at authoring: read the executor's emission stamping + `readByCorrelation` semantics + `buildActions` filters AT SOURCE; the fix hydrates the chain from the events the executor actually emits; a live-verification leg rides the next deploy (a FRESH run's chain must serve populated rows) + the `commandCount` symptom adjudicated in the same WU; a history-seeded fixture reproducing the live emptiness BEFORE the fix (the red that matches the field).

**F3 — ADJUDICATED: STATE-DIALECT (DRIFT-2-class; both sides conform TO the contract).** The wire is fully populated (`battery_pct: 100`, `open: false`, AVAILABLE, stateVersion 195) — nothing empty server-side. But the hub's contract check settles the routing three ways at once: the freeze doc A3 specifies the **AMD-52 typed-value envelope `{"t":<AttributeType>,"v":…}`** for attributes — the wire serves `{"value":…}` (neither flat nor typed-envelope), instants as fractional epoch-seconds (vs the family's ISO-8601 everywhere else), and `entityId` as raw `{msb,lsb}` (Jackson-default internal leakage no consumer could lawfully use). The SPA reads flat values. **THREE dialects; NOBODY matches the frozen contract.** Per the DRIFT-1 precedent (M7.5c-a): core conforms `/state` TO the contract (hand-built maps like every sibling endpoint: typed envelope + ISO-8601 + string ULID), and the FE consumes the contract shape — **FE-LIVE-V112 grows (h)**. The return's arithmetic proof of the "9:52 AM" (epoch-seconds parsed as ms → Jan-21-1970 15:52 UTC → 9:52 AM CST, all six devices in one boot window) is corpus-grade. Priority: P2 — behind CHAIN-HYDRATE (no demo leg reads the drawer; G2 closed without it), ahead of M14.

**F2 — DISSOLVED, and the residue is the week's biggest design discovery: AUTOMATION IDENTITY ROTATES EVERY BOOT.** The "hung" why-not answers in 5.7 ms with an honest problem+json — the id simply no longer exists: bench-hero re-registered as `01KYGZS4Q655C4FE6RWEW4F9C1` (vs the browser-era `01KYGXTT…`), `lastRunId: null`. One mechanism explains four symptoms (the stale why-not link · `lastRunId: null`/"No runs yet" on a bench that demonstrably ran · `automationName: null` on every historical run · the universal italic "An earlier automation"). **AUTO-IDENT chartered as a design question with product weight** (every restart orphans the entire run history from its automations — the explainability story's continuity). **REC: post-gate** — durable automation identity touches identity/event semantics, exactly the wrong class for the freeze window; the G1 gate-day demo scripts a FRESH run (current id, name resolves, why-not works) and is rotation-compatible; the launch-runway charter takes AUTO-IDENT as a named candidate beside the B-3 pairing UX. **Nick's one-liner below.** The browser-time spinner: the endpoint's 6 ms 404 rendering as eternal "Loading…" is the FE error-posture note — **FE-LIVE-V112 (f)** — and the two honest residuals stand exactly as the return stated (the valid-id why-not capture + the rotation-confirm double-read; both ride the next bench touch or B2 session prep).

**Dispositions.** G1's ledger WATCH **narrowed to WU-CHAIN-HYDRATE alone** (both cells re-stamped this beat). The corpus ratchets (+the WCAP return — the seconds-as-ms proof and the 507-byte self-contradicting payload are exhibit-grade). The error-posture inventory (one honest surface, two silent, three behaviors) rides FE-LIVE-V112 (f). EVENTS-API unchanged (post-gate candidate). **The forward order restated: CHAIN-HYDRATE authored next beat ∥ B2 authored next beat (both instructions ready before the next Coder/bench sessions; the Coder is idle — CHAIN-HYDRATE is his next WU) → the docs fold rides the same beat → THE SKILLS PASS after (one-pass deployed truth, now inclusive of the WCAP truths) → B3 → FE-LIVE-V112 (f)(g)(h) → M14 strictly behind the cluster fixes.**

**Nick, in order:** **(1) the beat-6 hivemind commit** — EXPLICIT, exactly 5 porcelain entries (4 M + 1 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/assessments/2026-07-11_go-no-go-criteria_draft.md context/audits/2026-07-27_WCAP_detail-read-wire-capture_return.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-6_commit-msg.txt` → push. **(2) ONE LINE — AUTO-IDENT timing: post-gate (REC) or pull it pre-freeze?** (REC grounds: identity/event semantics in the freeze window is the wrong risk class; the demo is rotation-compatible; it charters into the launch runway either way.) **(3) your ledger:** TSDR #2 **07-31** (Thu) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v39):** **WU-CHAIN-HYDRATE AUTHORED + B2 AUTHORED** (the same beat; instructions for both lanes ready together) + the docs fold → the Coder runs CHAIN-HYDRATE → its landing rides the next deploy + a fresh-run chain capture (the live-verification leg) → THE SKILLS PASS (+ re-sync) → B3 nightly → FE-LIVE-V112 (f)(g)(h) → M14 (behind the cluster) → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER (AUTO-IDENT + posture-(B) + EVENTS-API as named candidates) → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-27 (v39 beat 5 — **THE DEPLOY-EVENING INTAKE: COMPLETE — the Pi runs `c09c61c`; G2 ✅ CLOSED** · the four findings adjudicated (F1 by-design · **the detail-read cluster** → WCAP ordered) · **the G1 gate-day exposure named**)

**The intake (the return, status COMPLETE — filed verbatim at `context/audits/2026-07-27_dash-serve-deploy-evening_return.md`; the self-declared handoff/ path corrected to audits/ at filing per precedent).** Blocks 0–5 all `[PASS]`/COMPLETE: three `[PASS]` boot-healths bracketing the change (bundles 043050Z/043758Z/051201Z); Node `v22.23.1`/npm `10.9.8` installed one-time (root Avail 100G); fast-forward `2040a66..c09c61c` with direct adjacency rev-parse-proven at close; **the first npm-bearing build: `BUILD SUCCESSFUL in 49s`** (330 packages in 9 s; Vite 947 ms; the 5–20 min envelope surprised on the FAST side — the standing envelope for warm builds is now seconds-class) — the jar self-assert implicit in the success and corroborated by `dashboard-0.1.0-SNAPSHOT.jar` on the running app's classpath (pid 42770); the lawful boot signature in full; the fleet 6/6 @ 25065, ch20/0x774c throughout; the write surface DORMANT; L3 held (the token in no paste, no screenshot, no document). **THE REDUX: Glance 0 — the AuthGate rendered at `/dashboard/`, the token accepted, every surface authenticated with ● Live. Glance 1 — G2's tile VERBATIM-perfect** (`Available 6 of 6` · `Offline none` · `Not determined yet none` · `Stale readings none` + the not-a-live-test disclosure). **G2 [S] → ✅ CLOSED — both criteria-ledger cells flipped this beat.** Glances 2–3 → findings (below). **The DASH-SERVE arc is fully banked: authored → landed → all-gates-green → deployed → serving → authenticated — every hop proven at its own instrument.**

**THE FINDINGS ADJUDICATION (F1–F4; the return reports, the hub adjudicates).** **F1 (`GET /api/v1/events` → honest 404 card): BY-DESIGN — adjudicated at source in one pass:** the freeze doc defines B1 as a "frozen-but-unbuilt … NEW small Core read-slice"; rest-api MODULE_CONTEXT: "`/api/v1/events` + `/api/v1/health` remain **M7.5c-b (unscheduled)**"; the SPA's `EventsView` comment documents the graceful degrade. The error card IS the design working. **L4 applied — it gets an owner row: EVENTS-API minted as a backlog candidate** (non-gate; post-gate default; pairs `/api/v1/health`; the "unscheduled" state was the L4 nobody's-WU class, now owned). **F2/F3/F4 = ONE CLUSTER (the return's pattern ratified):** every list/summary surface populates (Overview, Devices table, runs list, Automations, Health — ULIDs matching the boot-health asserts exactly) while every detail surface fails (run causal chain: shell renders, card EMPTY, dozens of runs all ages · why-not: eternal spinner · drawer: values and age all em-dash while `Last reported` populates). **Server-vs-client is UNDECIDED and will not be theorized:** the bench's own instruments fetch causal chains successfully (the §51/C2/C3 evidence), so the wire is not dead — the SPA may be rejecting/mis-unwrapping live payloads (validator/envelope class), or the endpoints may return empty/erroring payloads for the SPA's query pattern. **WCAP AUTHORED** (`context/handoff/2026-07-27_WCAP_detail-read-wire-capture_operator-block.md`): five token-safe captures (the runs list · the EXACT empty-rendered chain `01KXVWR03HW5MQ6WSW9XRTAWTX` · the automations list · the EXACT hung why-not `01KYGXTTAESH0QWPEV2FR01HPT` with a 20 s cap so a hang prints as its own finding · the 04P's `/state`) — the fix WU routes core-side or FE-side on the pastes. **⚠ THE G1 EXPOSURE:** G1's gate-day evidence is the live ≤10-min demo, and legs (a) why-fired + (c) verdict-vocabulary render through exactly the surfaces F4/F2 broke — **the WATCH now rides both G1 ledger cells; the cluster's fix WU outranks M14 absolutely and sits beside B2 on the critical path.** Secondary adjudications: the italic "An earlier automation" on every historical row = EXPECTED-pending-confirm (automation identity registers per-boot from config; devices/entities got durable identity, automations deliberately didn't — confirm at the cluster diagnosis); the bare `9:52 AM` timestamp + the error-posture inconsistency (honest card / eternal spinner / silent empty) + the npm-audit `1 high` advisory → **FE-LIVE-V112 charter grows (f) uniform honest error/empty postures, (g) timestamp qualification, + the advisory rides the next deps touch**; the H3 note: the clean-image rep needs NO Node on the target (the .deb ships built artifacts; CI proves it) — pin rides the H3 brief.

**Nick, in order:** **(1) the beat-5 hivemind commit** — EXPLICIT, exactly 6 porcelain entries (4 M + 2 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/assessments/2026-07-11_go-no-go-criteria_draft.md context/audits/2026-07-27_dash-serve-deploy-evening_return.md context/handoff/2026-07-27_WCAP_detail-read-wire-capture_operator-block.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-5_commit-msg.txt` → push. **(2) WCAP — ~10 minutes on the Pi, anytime** (read-only curls; the pastes route back here). **(3) your ledger:** TSDR #2 **07-31** (Thu) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v39):** the WCAP adjudication → **the detail-read fix WU** (core-side or FE-side on the evidence; gate-critical via G1) ∥ **B2 AUTHORED** (unchanged: rider #1 THE REBIND first · the §51 port · §5.5/§5.6 riders · uhubctl prep · the three-flip re-mint · OBS-CONFIRM + C4 + RUNNER-VERSION-BANNER at authoring · L3) + the docs fold → **THE SKILLS PASS** (post-cluster-adjudication for one-pass deployed truth; then Nick re-syncs) → B3 nightly → FE-LIVE-V112 (+(f)/(g)) → M14 (margin-gated, now explicitly behind the cluster fix) → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-27 (v39 beat 4 — the three answers intaken (**Check 9 CLEAR** · **the skills-pass charge scheduled** · **shape (a) RULED**) · **THE DASH-SERVE DEPLOY-EVENING BRIEF AUTHORED** — the evening is Nick's to run; G2 closes on its ⏺s)

**The intakes.** The beat-3 commit **`c9fd7b6`** landed CENSUS-EXACT (4 M, pushed `48385da..c9fd7b6`). **(1) THE MIRROR SYNC: CONFIRMED** — Nick: "we are and have been synced." **Check 9 CLEARS at substance**, and the refinement is RECORDED for every future remote hub: the beat-1 STALE-MISMATCH evidence was the **remote session's own skill cache** — a THIRD location (account-synced into cloud Cowork sessions, lag-prone), distinct from Nick's local `.claude/skills` mirror (the thing Check 9 actually governs, and the thing host-CC lanes read — the Coder's zero-defect run under the current coder skill corroborates). Consequence: a remote hub observing stale in-session skill copies must adjudicate WHICH location is stale before flagging; Nick's `diff -rq` remains the instrument of record for the mirror. This refinement folds into the preflight reference + env-model §12 at the scheduled skills pass. **(2) THE SKILLS-PASS CHARGE** — Nick authorizes a careful, full optimization of the skills with a re-sync after. INTAKEN and **scheduled for immediately after the deploy evening**, deliberately: the FE skill's §4d serve-path bullet remains literally true until `c09c61c` is ON the Pi (the deployed app today still does not serve the dashboard); doing the pass post-evening re-grounds everything to deployed truth in ONE pass. Scope staged for it: the FE §4c/§4d re-ground · the arc's PM/coder lesson folds (the npm-into-check mechanism class · `--rerun` binding · preservation-fixture red predictions · the remote-cache/mirror distinction) · §8 token-economics trims where found. **(3) DEPLOY SHAPE (a) SPLIT-EARLY — RULED.**

**THE BRIEF AUTHORED:** `context/handoff/2026-07-27_dash-serve-deploy-evening_operator-brief.md` — Blocks 0–5: the floor-before → **Node 22 one-time install** (NodeSource system-wide — Gradle's `Exec` needs `npm` on the system PATH; disk-headroom check FIRST, ⏺ the Avail figure) → **pull + the first npm-bearing build with the envelope RESET** (5–20 min lawful; ~330 packages + Vite; the jar's doLast self-assert is the loud failure mode; the 23-second era is over for this run) → the deploy restart + floor-after → **THE BROWSER-BLOCK REDUX** (glance 0: the AuthGate renders at `/dashboard/` — the serve path itself; **the L3 token protocol: `cat ~/hs-bench/config/initial_api_token` on the Pi terminal, into the BROWSER only, excluded from every ⏺**; glances 1–3: the honest availability rows + not-a-live-test line · evidence-with-age · the five modes from the FIELD on a historical Rosonway chain with no recovery note) → close + the return-package spec (the v38 §5 shape). Scope law restated throughout: a broken glance is a FINDING, never a debug session, never retro-failing Blocks 0–3. The abort ladder reverts to `2040a66` with its own ⏺. Navigator-optional: the archived v38 navigator template's §2 interaction protocol + §5 return spec apply verbatim if Nick prefers a side-conversation walkthrough. **G2 CLOSES on the three glance ⏺s.**

**Nick, in order:** **(1) the beat-4 hivemind commit** — EXPLICIT, exactly 3 porcelain entries (2 M + 1 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/handoff/2026-07-27_dash-serve-deploy-evening_operator-brief.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-4_commit-msg.txt` → push. **(2) RUN THE EVENING whenever ready** (tonight lawful; ~40 min end-to-end) — the return routes back here and intakes as the next beat. **(3) your ledger:** TSDR #2 **07-31** (Thu) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v39):** **B2 AUTHORED** (the next hub beat — rider #1 THE REBIND first · the §51 port · §5.5/§5.6 riders · uhubctl session prep · the three-flip constants re-mint · OBS-CONFIRM + C4 + RUNNER-VERSION-BANNER at authoring · the `api_token()` AUTH note, L3) + **the docs fold** (INV-SE-02 in-place + Doc 13 §3.2–§3.3 as-built + the doc-drift ×3 — one docs-repo commit order) → the evening's return intake (**G2 → CLOSED** on the ⏺s) → **THE SKILLS PASS** (the charge above; then Nick re-syncs) → B3 nightly → FE-LIVE-V112 → M14 (margin-gated) → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-27 (v39 beat 3 — **THE LANDING INTAKE: `c09c61c` ALL-ACTIONS-GREEN → DASH-SERVE ✅ DONE** · **THE REDUX-UNBLOCK FINDING** (the token UX already exists — G2 gated only on the evening) · the forward plan re-derived)

**The intakes.** Core **`c09c61c`** landed (13 files, 265+/28−, `2040a66..c09c61c`) and **ALL GitHub Actions ran GREEN — Nick's own verification**: ci.yml (the check gate, Node-free by mechanism) + **install-smoke.yml's FIRST run with the serve asserts** (the load-bearing one: npm-on-runner → the jar's doLast self-assert → image → .deb → boot → headerless `/`→302 → `/dashboard/`→200 — **the whole seam is CI-proven end-to-end and now asserted on every push**) + frontend.yml. Hivemind **`48385da`** landed census-exact (6 M). **Executed this beat per the CMD-API/SKIP-VIS precedent:** the backlog row → ✅ DONE (the beat-3 head note) · the Coder's cross-agent note → ARCHIVED-SERVED (ACTIVE 0).

**THE REDUX-UNBLOCK FINDING (the L1 verification the beat-2 plan demanded, executed at `c09c61c` source).** The risk was a beat-6-class repeat: DASH-SERVE serves the SHELL, but the glances need DATA, and data needs `Authorization: Bearer`. Verified hop-by-hop: **the token UX already exists, complete** — `AuthGate.tsx` (paste-once, in-memory only per Doc 13 §12, honest first-run copy), `realTransport.ts` (Bearer on EVERY request), `client.ts`/`index.ts` (401 → prompt; 403 → drop-token + honest message), and **the served build defaults to the REAL transport** (`resolveMode()`: prod ⇒ real; `baseUrl = window.location.origin` — which IS Core when Javalin serves the bundle; the scenario switcher is mock-builds-only). **Consequence: the browser-block redux is unblocked AS-BUILT.** G2's close is gated ONLY on the deploy evening (Node op-order + deploy + the three glances + the pasted token). FE-LIVE-V112 leaves G2's critical path — it remains the named FE WU (live-verification halves (a)–(e) + UX refinements against real payloads), launch-gated on the mirror sync. **L3 note for the evening brief:** the live token is read on the Pi terminal (Nick's screen) and pasted into the BROWSER only — it never enters a conversation, a ⏺ paste, or a file; the brief's paste-blocks will be authored token-excluding.

**The forward plan re-derived (the critical path is now pure execution):** **(1) B2 decoupled** — its suite consumes wire truth that is ENTIRELY in the deployed `2040a66` (DASH-SERVE added nothing wire-side), so B2 evenings run against the Pi as it stands; when `c09c61c` deploys, the suite re-runs in one command (H2's deployed-build green preserved). **(2) The deploy-evening shape (Nick's one-liner):** **(a) REC — split early:** run the deploy evening on the fresh brief as soon as it's authored (Node install + `c09c61c` + first-build envelope + the redux → **G2 closes early**, de-risking the Node/build variance before B2's evenings) with B2's first silicon evening following separately; **(b)** keep them combined (the v38 REC's shape — one evening, more stacked). **(3) C1 arithmetic:** ~25/200 banked; ~12–13/day needed; B3 nightly by ~Aug-1 makes it comfortable; B2 not running suites by Fri Jul-31 = the named escalation (scope moves, never the gate). **(4) The docs fold** (hub-owned, next beat): INV-SE-02 in-place amendment + Doc 13 §3.2–§3.3 as-built + the doc-drift ×3 (Doc 18 ×2 · Doc 12 ×2) — one batched docs-repo commit. **(5) M14** only behind green-with-margin (yields-always). Calendar unmoved: H3 Aug-8/9 · dry-run ~Aug-10 · I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER Aug-12–13 · **Aug-14 EOD FREEZE · Aug-16 THE READ**.

**Nick, in order:** **(1) ⚠ THE MIRROR SYNC — the fourth ask: one word** (it gates only the FE lane now, but Check 9 stays STALE until it runs). **(2) the beat-3 hivemind commit** — EXPLICIT, exactly 4 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/handoff/cross-agent-notes.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-3_commit-msg.txt` → push. **(3) ONE LINE — the deploy-evening shape: (a) split-early [REC] or (b) combined.** **(4) your ledger:** TSDR #2 **07-31** (Thu) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v39):** the docs fold AUTHORED (one commit order) → **B2 AUTHORED** (rider #1 THE REBIND first · the §51 port · §5.5/§5.6 riders · uhubctl prep · the three-flip re-mint · OBS-CONFIRM + C4 + RUNNER-VERSION-BANNER at authoring · L3) → **the fresh deploy-evening brief** (Node 22 ARM64 op-order + envelope reset + the redux + the token-excluding paste-blocks; **G2 closes on the ⏺**) → B3 nightly → FE-LIVE-V112 (post-sync) → M14 (margin-gated) → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-27 (v39 beat 2 — **THE DASH-SERVE TWO-LAYER AUDIT: ACCEPT — ZERO DEFECTS** · the beat-1 landing `6669d9f` census-exact · **THE CORE COMMIT ORDERED** (13 explicit; the three-glance landing) · the flip + restamp gate on the landing intake)

**The intakes.** The beat-1 commit **`6669d9f`** landed CENSUS-EXACT (9 = 5 M + 2 A + 1 R100; chain `026f6b3` → `6669d9f` coherent). Nick launched the Coder on WU-DASH-SERVE and it ran to completion; the delivery arrived as the coder-handoff DELIVERED entry + the completion report + the cross-agent note (ACTIVE: 1). Fresh porcelains at audit time: core = EXACTLY the 13-census (12 M + the fixture dir ??), core HEAD untouched at `2040a66` (audit-before-commit HELD); hivemind = exactly the Coder's 3 closeout files (coder-handoff · cross-agent-notes · coder-lessons +2). **⚠ Open one-liner: whether the mirror sync ran pre-launch was not stated — confirm (it MUST be clear before the FE lane; the zero-defect return means no harm materialized here either way).**

**THE AUDIT (layer 1 = the Coder's record; layer 2 = hub host-truth reads of ALL 13 files + the landing verifications).** Layer-2 confirmations, per DP: **DP-1** realized exactly — bare `java` with the stated rationale; `staticOut = layout.buildDirectory.dir("staged-dashboard")`; the jar is the ONLY npm seam (`from(stageDashboard) { into("dashboard") }` + the doLast `ZipFile` self-assert with the L1 comment); the redundant assemble wiring gone; the DECOUPLING NOTE v2 states BOTH halves + the proof obligations; `clean` sweeps `dist` + the legacy `src/main/resources/dashboard`. **DP-2** — the one-line `testRuntimeClasspath` `exclude(module = "dashboard")` with the mandated comment (placed inside the `dependencies` block adjacent to the dep it governs — valid Kotlin, proven by both check runs). **DP-3** — the mount + spaRoot sit inside the existing `Javalin.create` lambda with the Doc-13 comment; `Location` imported (`:91`); the root redirect registers directly after `installAuth` (`:925`); install order + bind posture byte-unchanged; module-info untouched. **DP-4** — `authorize()` order verified at source: `isPathSafe` FIRST → the exemption early-return (with the load-bearing-order comment) → authenticate → rate-limit; the classifier is string-typed, package-private, exact-allowlist (`"/"` · `"/dashboard"` · `startsWith("/dashboard/")`), null-guarded both parameters; the installAuth Javadoc carries the exemption's invariant. **DP-5** — the two install-smoke copies diff-IDENTICAL post-edit; setup-node@v4 Node 22 after Setup Gradle; run-smoke section 6 pins EXACT 302 + 200 with named B-1/B-2/B-3-class failure messages; sections 7–8 renumbered. **Tests at source:** the 3 unit rows incl. the BOTH-HALVES order pin (I7 — the prefix rule alone WOULD admit the probe, `isPathSafe` must run first: the pair that makes an exemption-first refactor loudly red); the 3 e2e on the real-boot harness — **`Clock.fixed` in the NEW tests verified at source with the §4c citation in-line** (the file's `:368/:380` `systemUTC` sites are PRE-EXISTING documented-harness tests below the insertion — adjudicated non-findings); the HttpClient NEVER-follow redirect policy documented as load-bearing; the `%2e%2e` probe pinned EXACTLY 400. **MODULE_CONTEXT beats ×3 model-grade** (the lifecycle beat's `Clock.fixed` claim source-verified TRUE; the web-ui beat carries the two-halves law + the packaging model + the no-FE-change record).

**Adjudications.** **I1–I7 ALL RATIFIED** (null guard = the spec's own test row; the `ZipFile` import = Kotlin-DSL `java`-extension shadowing, lesson minted; the clean legacy sweep; I4 — see next; renumbering; the empty-dir removal — benign, disclosed, and it RECONCILES the check-task counts: the Coder's run-1 showed 157 actionable because the git-invisible empty `static/` dir made `processResources` execute, Nick's post-full-clean run shows 156 with 153 executed in 1m21s — no npm time signature, zero npm tasks: **the strongest Node-free proof, on a fresh-clean tree**; I7 strengthens the order proof). **I4 = a hub instruction imprecision, OWNED:** "expect ALL THREE e2e red" was unreachable for `dashboardWriteAndDataRoutesStayGuarded` by construction — my own spec named it the preservation fixture; the Coder's honest green-by-construction disclosure is the lawful reading (the prediction sentence should have carved it out). **The M3 mutant finding recorded:** with the exemption moved above the traversal gate, the encoded probe **SERVED (200)** — not merely misrouted; the order IS the security property and the EXACT-400 e2e pin is its standing instrument (the authoring-time exact-status tightening earned its keep). Gate arithmetic coheres end-to-end: baselines 104/56/19 ≡ the SKIP-VIS records; green ×2 (TRUE per-task `--rerun` after the Coder CAUGHT its own vacuous first ×2 — the binding lesson minted) 107/59/19 = +3/+3; M1/M2/M3 named kills, cmp-proven restores. The Coder's two coder-lessons entries (java-shadowing; `--rerun` binding + empty-dir NO-SOURCE evidence discipline) are folded — both are the evidence-discipline class this program pays for. **WUCP P1 5/5; Deferred Build Gate: NO** (the full check ran in-session — the first WU under the expanded grant to do so).

**Dispositions.** DASH-SERVE → **DELIVERED + AUDIT-ACCEPT; the commit is ORDERED below.** The backlog row flips ✅ DONE at the CI-green landing intake, and the cross-agent DASH-SERVE note restamps ARCHIVED-SERVED there too (the CMD-API/SKIP-VIS precedent). **The push's three glances:** ci.yml (the check gate of record — Node-free by mechanism), **install-smoke.yml — its FIRST run with the serve asserts, now the standing CI instrument for the whole artifact→packaging→serving→auth seam** (⏺ the verdict either way; a RED here is a FINDING the hub adjudicates before any deploy), frontend.yml (the web-ui paths trigger it; FE source unchanged → trivial green expected). Queued at the landing intake (hub-owned): the INV-SE-02 in-place amendment + the Doc 13 §3.2–§3.3 as-built note (docs repo, folded with the doc-drift ×3 batch). FE-LIVE-V112's serve-path block lifts at the landing + deploy; the Pi op-order (one-time Node install; the first `installDist` gains npm-ci + Vite minutes — 23s is no longer the lawful envelope) rides the NEXT deploy brief.

**Nick, in order:** **(1) ⚠ ONE WORD: did the mirror sync run before the Coder launch?** (Check 9 must be clear before any FE-lane launch regardless.) **(2) THE CORE COMMIT** — from homesynapse-core, EXPLICIT paths, exactly 13: `git add .github/workflows/install-smoke.yml api/rest-api/MODULE_CONTEXT.md api/rest-api/src/main/java/com/homesynapse/api/rest/RestFilters.java api/rest-api/src/test/java/com/homesynapse/api/rest/RestFiltersAuthTest.java app/homesynapse-app/build.gradle.kts distribution/ci/install-smoke.yml distribution/smoke/run-smoke.sh lifecycle/lifecycle/MODULE_CONTEXT.md lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/HomeSynapseCoreTest.java web-ui/dashboard/MODULE_CONTEXT.md web-ui/dashboard/build.gradle.kts lifecycle/lifecycle/src/test/resources/dashboard/index.html` → `git commit -F ../_scratch/2026-07-27_core_dash-serve_commit-msg.txt` → push → **⏺ THE THREE GLANCES: ci.yml + install-smoke.yml (the first serve-assert run — paste the verdict) + frontend.yml, and RECORD the SHA.** **(3) the hivemind beat-2 commit** — EXPLICIT, exactly 6 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/handoff/coder-handoff.md context/handoff/cross-agent-notes.md context/lessons/coder-lessons.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-2_commit-msg.txt` → push. **(4) your ledger:** TSDR #2 **07-31** · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v39):** the landing intake (beat 3: the three-glance verdicts ⏺ · the backlog flip · the note restamp · the INV-SE-02 + Doc 13 docs-repo fold order) → **B2 AUTHORED** (rider #1 THE REBIND first · the §51 suite port · §5.5/§5.6 riders · uhubctl session prep · the three-flip constants re-mint · OBS-CONFIRM + C4 + RUNNER-VERSION-BANNER at authoring · the AUTH NOTE/L3) → B3 nightly → FE-LIVE-V112 → the next deploy evening (fresh brief; the Pi Node op-order; **the browser-block redux; G2 CLOSES**) → M14 → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-27 (v39 beat 1 — **LAUNCH (10/11 + the Check-9 mirror mismatch → ⚠ SYNC OWED)** · beat-8 + skills landings census-exact · the v38 prompt archived · **WU-DASH-SERVE AUTHORED ISSUE-READY** (the (A) ruling realized; the npm-into-check hazard caught at authoring; CMD-API-ACTOR OUT))

**The launch record.** Porcelain ×5 clean (lock-free). HEADs re-derived at source: core **`2040a66`** (deployed, the Pi's build of record) · docs `a53f474` · hivemind **`026f6b3`** — the v38 beat-8 landing, CENSUS-EXACT (11 porcelain entries = 4 M + 1 A + the 3 archive renames R100; chain `1b3b117` → `673bf0a` → `8ca133c` → `026f6b3` coherent) · skills **`844e561`** (exactly 1 M — the FE v1.6 currency fix) · bench `9f2b5ad`. The uploaded v39 prompt md5-≡ the banked copy (`4b74ff57…`). Watermark AMD-99, **175/53** re-derived at the register's regeneration line. W31 governs (pre-minted per precedent; W32 pre-mints at the Aug-3 boundary). The v38 prompt ARCHIVED → `context/handoff/archive/` (rides this beat's commit as the rename pair). **Preflight: 10/11 PASS + Check 9 STALE-MISMATCH** — the remote-session mirrors of ALL THREE role skills differ from source (the FE mirror reads **v1.4** — two versions behind v1.6; the PM/coder mirrors pre-date the beat-3 currency pass: the loaded PM masthead is v35-era, missing the Apache-2.0-forever re-grounding + format #16/#17). Per the v39 prompt's pre-declared adjudication: **⚠ the mirror sync (PM + coder + frontend) must run BEFORE any lane launch** — including the DASH-SERVE Coder session (its skill mirror carries the licensing + format corrections). Authoring-at-desk proceeded lawfully: hub reads ride SOURCE (the format reference was read at hivemind HEAD, not the stale mirror), and the banked v39 charge-2 order + the beat-8 ratified sequencing govern over the generic STALE restriction — adjudication on the record. **Drift reconciled this beat:** the cross-agent ACTIVE census line still read "ACTIVE: 1" from the Coder's stamp although the SKIP-VIS note was ARCHIVED-SERVED at v38 beat 6 — fixed (ACTIVE 0); the coder-handoff next-WU pointer (B2, from the SKIP-VIS closeout) is superseded by the ratified sequencing — the DASH-SERVE dispatch entry written this beat is newest-authoritative.

**WU-DASH-SERVE AUTHORED (the first code act, per the ratified sequencing; the Coder is idle).** Issued: `context/instructions/2026-07-27_DASH-SERVE_dashboard-serve-path_coding-instruction.md` + `context/pre-verifications/WU-DASH-SERVE.md` (P0–P15, every pin an observed signature read at `2040a66` this session). The L1 every-hop re-verify EXECUTED: B-2 (`plugins { base }` only; npm wired to `assemble` only; the DECOUPLING NOTE verbatim), B-1 (zero `staticFiles`/`spaRoot` anywhere in lifecycle/app main; the `Javalin.create` lambda at `:903`), B-3 (the catch-all `before(*)` + throw-halts-pipeline at source; the live 401-on-`/` proof corr `ae7990f0`) — all three re-confirmed. **The authoring-caught hazard (the audit-before-issue dividend):** the beat-7 sketch's "wire `stageDashboard` → `processResources`" would have **dragged npm into `./gradlew check`** by two roads (the java-plugin's implicit `src/main/resources` sweep; the app's `testRuntimeClasspath` extending `runtimeClasspath` and resolving the dashboard jar) — violating the WU's own stated law. The realized mechanism instead: staging re-pointed to `build/staged-dashboard`; **npm attaches to the JAR TASK ONLY** (`jar { from(stageDashboard) into "dashboard" }`) with a **doLast packaging self-assert** (a jar without `dashboard/index.html` fails its own build — L1's corroborating instrument); the app-side one-line `testRuntimeClasspath exclude(module = "dashboard")`; and a **Node-free grep gate assert** on the full `check` output (anti-vacuous: the report quotes both the absence of npm tasks AND the presence of the module's SKIPPED/NO-SOURCE lines). **The fourth hop was verified ALREADY SOUND:** `vite.config.ts` carries `base: '/dashboard/'` + hash routing ("Served builds need no proxy") — NO FE-source change rides the WU. **DP-5 CI rider:** install-smoke gains a pinned `setup-node` 22 in BOTH copies (`installDist` now builds the SPA; the active workflow triggers on this WU's own paths) + `run-smoke.sh` gains the serve asserts (headerless `/` → 302, `/dashboard/` → 200) — the gate of record becomes the seam's standing instrument (L4's live-glance class, automated). Tests: 6 new (3 unit exemption-exactness rows on the new string-typed `isPublicShellRequest` helper; 3 e2e on the `HomeSynapseCoreTest` boot harness — redirect+API-preservation pair · shell/SPA-fallback/HEAD · write-guard + the **EXACT-400 encoded-traversal order pin**) + M1–M3 mutation legs (method-widen · prefix-widen · exemption-above-the-gate) with named kills. Census EXACTLY **13** (12 M + 1 lifecycle test fixture). ARCH-RULE-REACH: all ten rules enumerated with line cites — ZERO collisions (P12). SEAM-I/O: the artifact→packaging→serving→auth contract stated hop-by-hop with each hop's own proof. **CMD-API-ACTOR adjudicated OUT** with grounds (a write-path `command_issued` payload change inside a freeze-window serve-path WU — single-concern; parked for the first post-gate rest-api touch; the standing-ledger pairing question closed). **Hub-owned at landing:** the INV-SE-02 in-place amendment + the Doc 13 §3.2–§3.3 as-built note (docs repo, folded with the doc-drift ×3 batch).

**Nick, in order:** **(1) ⚠ THE MIRROR SYNC** (PM + coder + frontend → `.claude/skills/`) — Check 9's gate; nothing launches before it. **(2) THE BEAT-1 COMMIT** (hivemind) — EXPLICIT paths, exactly 9 porcelain entries (5 M + 2 ?? + the archive rename pair, recording as R): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/handoff/coder-handoff.md context/handoff/cross-agent-notes.md context/instructions/2026-07-27_DASH-SERVE_dashboard-serve-path_coding-instruction.md context/pre-verifications/WU-DASH-SERVE.md context/handoff/2026-07-25_PM-mission-control_v38_orchestrator_session_prompt.md context/handoff/archive/2026-07-25_PM-mission-control_v38_orchestrator_session_prompt.md` → `git commit -F ../_scratch/2026-07-27_hivemind_v39-beat-1_commit-msg.txt` → push. **(3) LAUNCH THE CODER** (host-CC, homesynapse-core, after (1)+(2)): `Read nexsys-hivemind/context/pre-verifications/WU-DASH-SERVE.md and nexsys-hivemind/context/instructions/2026-07-27_DASH-SERVE_dashboard-serve-path_coding-instruction.md, then execute — /nexsys-coder` (allow-list: `./gradlew :api:rest-api:compileJava :api:rest-api:test :lifecycle:lifecycle:compileJava :lifecycle:lifecycle:test :app:homesynapse-app:test` + ONE full `check`; NO assemble/installDist — npm is not in the lane's grant). **(4) your ledger:** TSDR #2 **07-31** (4 days) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com glance · tamoro.dev $12.

**Hub next (v39):** the DASH-SERVE return two-layer audit → **B2 AUTHORED** (rider #1 THE REBIND first · the §51 suite port · §5.5/§5.6 riders · uhubctl session prep · the three-flip constants re-mint · OBS-CONFIRM + C4's latency arm + RUNNER-VERSION-BANNER at authoring · the AUTH NOTE: every AUTO command leg rides `api_token()`, L3) → B3 nightly (gated on the rebind) → FE-LIVE-V112 (the token UX; live halves post-DASH-SERVE-deploy) → the next deploy evening (a FRESH brief: the DASH-SERVE landing + the Pi Node one-time op-order + the first-build envelope reset + THE BROWSER-BLOCK REDUX → **G2 CLOSES**) → the M14 WUs → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-26 (v38 beat 8 — **THE CLOSE-OUT** · rulings by delegation recorded: **posture (A) RULED** + the sequencing REC ratified · THE STALE-CONTEXT SWEEP executed · pm-lessons +4 · **v39 BANKED** · v38 CLOSES on the beat-8 landing)

**The intakes.** The beat-7 commit **`8ca133c`** landed CENSUS-EXACT (6 = 5 M + the return ??, 240+/9−, pushed `673bf0a..8ca133c`; the chain `1b3b117` [beat 6] → `673bf0a` [beat 6b, the deploy package] → `8ca133c` coheres). **The Pi one-liner RAN** — the stray `~/homesynapse-core/.homesynapse` tree is gone; the leaked token is structurally burned. **RULINGS BY DELEGATION (Nick: "my rulings are whatever you suggest"):** **(1) the dashboard auth posture = (A), the static-shell exemption** — the catch-all filter will allowlist EXACTLY GET/HEAD `/dashboard/**` + the root redirect; every `/api/*` + `/internal/*` request stays token-guarded; INV-SE-02 amends in place with the exemption's own invariant (no DATA route ever unauthenticated). Posture (B) cookie-session pairing = the named post-gate design successor (launch-runway-charter candidate); (C) rejected. **(2) the sequencing REC RATIFIED:** WU-DASH-SERVE authored FIRST (the idle Coder runs it), B2 second (before the next bench evening), the next deploy evening carries DASH-SERVE → the browser-block redux → G2 closes there.

**THE STALE-CONTEXT SWEEP (Nick's close-out charge: no future session operates off stale context).** Grep-classes swept over the non-archive hot path (`355a711` · dead-zone · the installDist claim · in-flight/not-deployed · the map/canonical docs): every `355a711` + installDist hit outside the spine = a DATED point-in-time record (the state-of-the-company close line, the 04P report's observed-at, the filed returns — historical, standing lawfully). **TWO live carriers were stale and are FIXED this beat:** **(1) the playbook §8 interim dead-zone rule → ⛔ EXPIRED-stamped** (the second terminator fired: DP-3 deployed inside `2040a66`; the explicit-wait mitigation no longer required; the rebind still lands as authored; the historical text preserved under the stamp) — `context/process/bench-troubleshooting-playbook.md`. **(2) the frontend skill → v1.6** (nexsys-skills `orchestrators/nexsys-frontend/SKILL.md`): §4d's "fix is IN FLIGHT"/"until it LANDS" → LANDED-AND-DEPLOYED; the triggeredAt INTERIM CAVEAT → EXPIRED; §4c's silent-skip interim guidance → RETIRED (the marker is live wire); **+ THE SERVE-PATH REALITY bullet** (no dashboard serving until DASH-SERVE; `/dashboard/` base; Bearer on every live fetch; the token UX; L3). **Consumed one-offs ARCHIVED (3 renames → `context/handoff/archive/`):** the FE-VERDICT-2 lane prompt (served; its baseline line is stale by design) · the deploy brief (EXECUTED-stamped) · the navigator prompt (self-declared CONSUMED). **Deliberately untouched:** coder-handoff (its next entry is the DASH-SERVE dispatch — the authoring beat writes it; no Coder session launches before then), the criteria ledger beyond the beat-7 flips (the Aug-10 dry-run IS the designed full-ledger currency pass), every dated report/return (history is append-only). **pm-lessons +4 (the close-out fold):** **L1** every-hop-or-it-is-not-verified (the transitive-claim autopsy) · **L2** "your practiced X" BANNED from operator handoffs (the 3.1 incident; the practiced-motion-of-record table folds into future briefs) · **L3** token-paste poison (credentials travel by file-path reference, never by value; filed returns redact-and-document) · **L4** design-doc intent needs an OWNER ROW (Doc 13 §3.2–§3.3 = the month-long nobody's-WU; the live-glance class = the standing seam-detector).

**v39 BANKED:** `context/handoff/2026-07-26_PM-mission-control_v39_orchestrator_session_prompt.md` — charges in order: WU-DASH-SERVE authoring FIRST (posture (A) baked in; the L1 re-verify-at-source discipline; the Pi Node op-order rides the NEXT deploy brief) → B2 (rebind first; the AUTH NOTE: every AUTO command leg rides `api_token()`; L3) → FE-LIVE-V112 expanded (the token UX; blocked on DASH-SERVE) → the next deploy evening (fresh brief; the browser-block redux; **G2 closes**) → M14 → H3 Aug-8/9 → the Aug-10 dry-run → Aug-12–13 I2 + close-out + v40 + THE LAUNCH-RUNWAY CHARTER (+ the B-3 pairing UX as a charter candidate) → **Aug-14 EOD FREEZE → Aug-16 THE READ**. Check-9 expectation pre-declared: ONE skill source changed (FE v1.6) — the mirror must sync before any FE-lane launch.

**Nick, in order:** **(1) THE BEAT-8 COMMIT** (hivemind) — EXPLICIT paths, exactly 11 porcelain entries (4 M + 1 ?? + the 3 archive rename pairs, recording as R): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/lessons/pm-lessons.md context/process/bench-troubleshooting-playbook.md context/handoff/2026-07-26_PM-mission-control_v39_orchestrator_session_prompt.md context/handoff/2026-07-26_FE-VERDICT-2_lane_session_prompt.md context/handoff/archive/2026-07-26_FE-VERDICT-2_lane_session_prompt.md context/handoff/2026-07-24_cmd-api-deploy-evening_operator-brief.md context/handoff/archive/2026-07-24_cmd-api-deploy-evening_operator-brief.md context/handoff/2026-07-26_deploy-evening_navigator_session_prompt.md context/handoff/archive/2026-07-26_deploy-evening_navigator_session_prompt.md` → `git commit -F ../_scratch/2026-07-26_hivemind_v38-beat-8_commit-msg.txt` → push. **(2) THE SKILLS COMMIT** — from nexsys-skills, EXPLICIT, exactly 1 M: `git add orchestrators/nexsys-frontend/SKILL.md` → `git commit -F ../_scratch/2026-07-26_skills_frontend-v1.6_commit-msg.txt` → push. **(3) ⚠ RUN YOUR MIRROR SYNC** (nexsys-frontend → `.claude/skills/`) before any FE-lane launch. **(4) LAUNCH v39 whenever ready:** `Read nexsys-hivemind/context/handoff/2026-07-26_PM-mission-control_v39_orchestrator_session_prompt.md and execute it.` **(5) your ledger:** TSDR #2 **07-31** (5 days) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**v38 CLOSES on the beat-8 landing.** Eight beats: authored + audited two WUs to zero-defect landings, deployed the fleet build of record, banked A4/H4 + the soak, caught the program's biggest hidden seam with a five-minute browser block, and left the next session a single-ruling-free runway. The hub stands relieved on the landing paste.

## 2026-07-26 (v38 beat 7 — **THE DEPLOY-EVENING INTAKE: COMPLETE — THE PI RUNS `2040a66`; A4 ✅ + H4 ✅ BANKED** · **Block 4b UNOBSERVED: THE DASHBOARD HAS NO SERVE PATH** (three stacked blockers, each verified at source this beat) · **WU-DASH-SERVE named** · the beat-6 grounds claim corrected and OWNED)

**The intake (the navigator return, status COMPLETE — filed verbatim at `context/audits/2026-07-26_deploy-evening_return.md`).** Block 0a: the soak is CLEAN — the newest transport token (13:25:06.748, `reopen_no_target`) sits inside the lawful 13:24–13:25 rep window and predates the 13:57 session close; **the Rosonway topology's first soak evidence BANKS FREE.** Block 0: floor `[PASS]` on the pre-deploy build. Block 1: fast-forward **`355a711` → `2040a66`** (50 files, 4057+/377− — the pull spans CMD-API + the dependabot merges + SKIP-VIS + FE-VERDICT-2 in one hop); HEAD chain verified `2040a66` / `da11f46` / `4bc1258`; `BUILD SUCCESSFUL in 23s` (**the tell — see the finding**). Block 2: the deploy restart is LAWFUL (relinked ×6 · zero proposes · `projection_live devices=6 entities=6 position=25065` ≥ P-pre · ch20/0x774c) + boot-health `[PASS]` — **THE DEPLOY IS PROVEN; the Pi runs `2040a66`.** Block 3: **H4 [S] BANKS** (rep boot 39794 lawful; `[PASS]`, bundle 211424Z). Block 4: **A4 [S] BANKS** (`kill -9` 39911 with "nothing was running" confirming SIGKILL took it; rehydration to position **25065 ≥ P-kill at equality** [the store took no writes tonight — the assert satisfied at equality, noted]; the six entity ULIDs IDENTICAL; `[PASS]`, bundle 211954Z — MVP §8.1 write-ahead durability exercised as SIGKILL). Six lawful boots on `2040a66` total (each verdict run restarts on its own — MORE restart evidence than briefed, 3.3); anti-actions honored; the write surface stayed DORMANT. **The playbook §8 interim dead-zone rule is EXPIRED** (DP-3 is live wire; `runs[].triggeredAt ≡ matchedAt` where eventTime is present, henceforth trustworthy to the bench instruments).

**THE FINDING — Block 4b's three glances were STRUCTURALLY IMPOSSIBLE (`GET /` → 401, RFC-9457 `authentication-required`, corr `ae7990f0`): the dashboard has NO serve path. Three independent blockers, EACH sufficient, ALL verified at source this beat:** **(B-1 SERVE)** grep of `lifecycle/` + `app/` Java: **ZERO `staticFiles` mounts** — the composition root never mounts the SPA; the web-ui build file's own comment ("served by Javalin at /dashboard/ — Doc 13 §3.2–§3.3") is design intent that was NEVER composed. Even authenticated, `/dashboard/` would 404. **(B-2 PACKAGE)** `web-ui/dashboard/build.gradle.kts` applies only the `base` plugin and registers NO artifact in any configuration → the app's `runtimeOnly(project(":web-ui:dashboard"))` resolves EMPTY (nothing on the classpath, no task dependency); the npm pipeline (`npmInstall`/`npmBuild`/`stageDashboard`) is wired into `assemble` ONLY → **`installDist` neither runs npm nor packages one byte of the SPA** — the Pi's 23s / 11-executed build is the arithmetic proof (the navigator's 3.6 suspicion CONFIRMED). **(B-3 AUTH)** AB-1 (the 2026-06-19 Seam-1 go-live WU) installed the catch-all `before(*)` filter — INV-SE-02, "an unauthenticated request can never reach a handler", NO exemptions, `StandardAuthMiddleware` strict RFC 6750 — and a browser's top-level navigation CANNOT attach a bearer header, so even a served + packaged SPA shell is unreachable from any browser as built. (Why boot-health's API assert still passed: `bench.sh` AUTHENTICATES — `api_token()` reads `~/hs-bench/config/initial_api_token` and every curl sends the Bearer header; the bench credential path works end-to-end on the Pi, proven twice tonight.) **The seam verdict: this is a SEAM NOBODY OWNED** (format-addition-#17's exact class) — the core lane stopped at the REST API, the FE lane stopped at the Vite build + mocks (every FE session ever ran `npm run dev`; the D-2 environment class masked the gap), and Doc 13 §3.2–§3.3's serving section was never scheduled as anyone's WU.

**HUB DEFECT OWNED (beat 6):** my grounds line "installDist builds the dashboard ON the Pi — verified at source" was **FALSE** — I read the app-side dependency + the dashboard's `assemble` wiring and inferred the transitive chain without verifying the middle (no artifact) or the far end (no serve). Layer-2 discipline failed at a seam I was asserting about. Consequences priced honestly: the deploy itself lost NOTHING (the FE files rode to the Pi inert; every backend proof banked), and shape (b) remains correct in hindsight (under (a) the same B-1/B-3 blockers — which PREDATE the FE landing — would have 401'd the glances identically); but the promised (b) prize "G2-live + FE-LIVE (a)–(c) tonight" was **never achievable under either shape**, and FLAG-4's "two-minute browser action" premise (plus my Block-4b "usual address" phrasing) assumed a dashboard Nick has in fact NEVER opened — no one ever has, from the app. Lessons minted (fold to pm-lessons at close-out): **(L1)** a transitive claim is verified at EVERY hop or it is not "verified at source" — artifact wiring, packaging, serving are three hops, not one; **(L2)** "your practiced X" is BANNED from operator command handoffs — the navigator's one §2.2 violation (a bare launcher path + "practiced start" prose, seeded by the brief's own language) caused the evening's only incident (3.1); name the `bench.sh` verb or spell the full command; **(L3)** token-paste poison — the mint WARN line must never ride a paste; glance greps stay token-scoped (tonight's were).

**The 3.1 incident + residue (adjudicated: NAVIGATOR ERROR, operator blameless; zero harm — each no-harm claim evidenced in the return):** the raw launcher run from `~/homesynapse-core` died at the port bind (the running app held 7070), but not before creating a STRAY empty event store + config under `~/homesynapse-core/.homesynapse/` and MINTING that stray store's initial token — which then rode two transcripts. **Severity: NIL-by-construction** — the leaked token belongs to the stray empty store ONLY; it was never valid against the live app for a single second (the live store at `~/.homesynapse/` minted its own token tonight, which is NOT in any transcript and MUST never be — L3). `.homesynapse/` is gitignored (verified — line 8 of core `.gitignore`), so the stray tree never touched porcelain. **The cleanup order (one line, on the Pi, anytime): `rm -rf ~/homesynapse-core/.homesynapse` — removes the stray store and structurally burns the leaked token.**

**Dispositions:** the deploy → **DONE** (criteria ledger: the deploy row + **A4 → ✅** + **H4 → ✅**, evidence = tonight's bundles 205848Z/211206Z/211424Z/211954Z). **G2 stays OPEN** — the tile is delivered + audited (beat 6 stands); the live glance is now BLOCKED on WU-DASH-SERVE and closes at the NEXT deploy evening's browser-block redux (the real URL will be `/dashboard/`, and the SPA will need its token UX — see the WU plan). **FLAGS 1–3 RETIRE wire-side** (the deployed read surface IS v1.1.2; the mock-only caveat is obsolete) with their live-OBSERVATION legs folded into **FE-LIVE-V112, which is BLOCKED on WU-DASH-SERVE**. The corpus ratchets (+the return, +four bundles, +the six-boot restart evidence).

**THE WU PLAN (the "from here"):** **(1) WU-DASH-SERVE — named, P1, core lane, the Coder is IDLE now** (authoring next beat, gated only on Nick's posture ruling below): **(i)** B-2 fix — make `:web-ui:dashboard` a real artifact: apply the `java` plugin (resources-only module), wire `stageDashboard` → `processResources` so the jar packages the staged SPA under `/dashboard`, and the existing `runtimeOnly` project dependency then carries it onto the app classpath AND pulls the npm build into `installDist`'s graph (keep `check` Node-free exactly as the DECOUPLING NOTE demands); **(ii)** B-1 fix — the composition root mounts Javalin `staticFiles` (classpath `/dashboard`, hostedPath `/dashboard`, SPA fallback to `index.html`) + a `/` → `/dashboard/` redirect; **(iii)** B-3 fix per the RULING; **(iv)** riders: filter tests (the exemption is EXACTLY GET/HEAD on the static surface; 401 preserved everywhere else incl. every `/api/*` + `/internal/*`), a packaging test (the dist contains `dashboard/index.html`), the Pi op-order (Node one-time install; the first `installDist` gains npm-ci + vite minutes — the build-time envelope resets, stated in the next brief). **(2) FE-LIVE-V112 EXPANDS** (FE lane, parallel-safe on mocks): the token UX — a paste-once token entry, `Authorization: Bearer` on every fetch, an honest 401/403 screen, the `/dashboard/` base path; then its original (a)–(e). **(3) B2 UNCHANGED** (bench lane; rider #1 THE REBIND first; authored before Nick's next bench evening). Sequencing REC: **DASH-SERVE authored FIRST** (the idle Coder runs it while B2 authoring proceeds), B2 second, both this week; **the NEXT deploy evening (B2's first silicon evening) carries the DASH-SERVE landing → the browser-block REDUX → G2 closes there.** Calendar: NO slip — freeze Aug-14, dry-run ~Aug-10, and the glance needs only one pre-freeze evening.

**THE RULING ASK (dashboard auth posture — B-3 is a security-invariant change, never hub-silent):** **(A) REC — the static-shell exemption:** the catch-all filter allowlists EXACTLY GET/HEAD on `/dashboard/**` + the root redirect; every `/api/*` and `/internal/*` request stays token-guarded. INV-SE-02's substance holds — no DATA ever moves unauthenticated; the shell is inert public bytes (same class as any installed app's binary) — and the SPA supplies the token for data via FE-LIVE-V112's token UX. Smallest change, freeze-compatible, reversible in one filter line. **(B) cookie-session pairing** (`GET /pair?token=…` once → HttpOnly SameSite=Strict cookie; CSRF posture work on the write surface; the nicer long-term UX) — NAMED as the post-gate pairing-UX design (a launch-runway-charter candidate), not the freeze-window move. **(C) loopback-read exemption — REC AGAINST** (it guts INV-SE-02 for every consumer on the box and buys nothing (A) doesn't).

**Nick, in order:** **(1) the Pi one-liner (anytime):** `rm -rf ~/homesynapse-core/.homesynapse` (the stray store + token burn). **(2) THE POSTURE RULING — one line: (A)/(B)/(C). REC (A).** **(3) the hivemind beat-7 commit** — EXPLICIT paths, exactly 6 (5 M + 1 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/assessments/2026-07-11_go-no-go-criteria_draft.md context/handoff/2026-07-24_cmd-api-deploy-evening_operator-brief.md context/audits/2026-07-26_deploy-evening_return.md` → `git commit -F ../_scratch/2026-07-26_hivemind_v38-beat-7_commit-msg.txt` → push. **(4) on your (A):** I author WU-DASH-SERVE next beat (issue-ready with census/gates/pre-verification), then B2. **(5) your ledger:** TSDR #2 **07-31** · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v38):** WU-DASH-SERVE AUTHORED (on the ruling) → **B2 AUTHORED** (rider #1 THE REBIND first · the §51 port · §5.5/§5.6 riders · uhubctl session prep · the three-flip constants re-mint · OBS-CONFIRM + C4 + RUNNER-VERSION-BANNER at authoring) → the Coder runs DASH-SERVE ∥ Nick's B2 evenings → B3 nightly (gated on the rebind) → the M14 WUs → Aug-8/9 H3 → **the next deploy evening carries DASH-SERVE → the browser-block redux → G2 CLOSES** → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v39 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-26 (v38 beat 6 — **`da11f46` ALL-CI-GREEN → SKIP-VIS ✅ DONE** · `3909a1d` CENSUS-EXACT · **THE FE-VERDICT-2 AUDIT: ACCEPT — ZERO DEFECTS** (G2 delivered; the deps watch closes) · **the deploy-shape one-liner: REC (b)**)

**The intakes.** **ci.yml GREEN on `da11f46`** (Nick’s confirmation of record) — SKIP-VIS’s gate of record is CLOSED; the backlog flips ✅ DONE via this beat’s head currency note; the Coder’s cross-agent note restamps ARCHIVED-SERVED. The beat-5 cleanup commit **`3909a1d`** landed CENSUS-EXACT (89 files = 9 M + 7 creates + 73 renames, the stat corroborating the ordered census 1:1) and pushed. The playbook §8 interim dead-zone rule now expires at TONIGHT’s deploy.

**THE FE-VERDICT-2 TWO-LAYER AUDIT (ACCEPT).** Layer-1 = the return’s record: `npm run verify` GREEN ×2 (the closing run on a pristine `npm ci`; 116/116; bundle 61.6/100 KB), the deps-watch evidence (§4: baseline tree GREEN on the bumped lockfile 86/86; lockfiles md5-untouched before/after), 8 dual-theme screenshots delivered in-conversation, the preflight run with the expected Check-2 STALE-as-the-WU. Layer-2 = hub host-truth reads: the census is porcelain-exact (20 M under `web-ui/dashboard/` + this return; `package.json`/lockfile ABSENT from the set ✓) · `CONTRACT_VERSION v1.1.2-2026-07-26` + the optional additive keys with the interim `triggeredAt` annotation ✓ · the `noCommandsIssued` validator FAILS a wire `false` (the never-false idiom pinned) ✓ · **`isActionSettled` = the core Q1b formula VERBATIM, field-first** ✓ · `actionVerdict` = field-first, recovery only on absence, with recovered-provenance disclosed ✓ · the four mode-paired AA corrections sit in `tokens.dtcg.json` with computed before/after ratios in their $descriptions (light ok/warn/info + dark textMuted; `tokens.css` regenerated, never hand-edited) ✓ · the `five-modes` scenario is matchedAt-exact ✓ · the G2 tile carries “Not determined yet” + the not-a-live-test line ✓ · the MODULE_CONTEXT beat conforms ✓. **Adjudications:** **D-1 RATIFIED** — the lane correctly REFUSED to import the brand-exploration palette under HELD R-1 and instead applied the mode-paired computed-AA DISCIPLINE to the dashboard’s own shipped ramps; the dispatch’s “on the brand lane’s corrected mode-paired tokens” phrasing was loose — hub imprecision OWNED (the lane’s reading is the lawful one). **D-2..D-6 RATIFIED** (D-2’s superseded-outranks-timeout rendering = CORE-P1(b)’s letter; D-6’s glyph set = the existing shape-carries-state mechanism, no escalation owed — Nick’s eyes on the 8 screenshots stand as the brand veto surface). **FLAG-1..3** = law (c) working as designed (they retire at the deploy with ZERO code changes). **FLAG-4 ADOPTED** — the G2 live glance rides tonight’s browser block. The dark ok/warn CVD-luminance observation ROUTES to the post-G-2 token-authoring checklist (correctly flagged, not improvised). The 35.1-vs-60 KB bundle-accounting reconciliation is RECORDED (the old figure was the JS+CSS subtotal; no accounting change). GAP-3/GAP-5 stay open candidates (GAP-5 = G2’s cheapest upgrade; post-gate ruling). **FE-LIVE-V112** = the named next FE WU; halves (a)–(c) ride tonight’s browser block, (d)–(e) wait for fleet-uniform v1.1.2. **ACCEPT — zero defects, second lane running.**

**THE DEPLOY SHAPE (Nick’s one-liner; REC (b)).** **(a)** Tonight deploys `da11f46` exactly as the amended brief stands; the FE commit lands after the evening; the new dashboard reaches the Pi at a LATER deploy (G2’s live glance + FE-LIVE (a)–(c) wait for it). **(b) REC:** run the core FE commit NOW → push → **both glances green** (ci.yml [zero-Java, trivial] + frontend.yml [the FE gate of record]) → **tonight deploys THE FE LANDING SHA. Amendment-of-record (in-conversation, per precedent; the file-fold rides beat 7): the brief’s Block-1 expectation becomes the FE landing** (the commit whose message begins `core: FE-VERDICT-2`, directly above `da11f46`; ⏺ RECORD the SHA; BOTH workflows green on it verified pre-evening; every other block unchanged). Grounds: `installDist` builds the dashboard ON the Pi (`:app` `runtimeOnly(project(":web-ui:dashboard"))` + the npm tasks wired into `assemble` — verified at source; prior Pi deploys prove npm-on-Pi works); the npm-ci-against-the-bumped-lockfile delta is IDENTICAL under (a) and (b) — tonight is the Pi’s first build past `4bc1258` either way, and the return’s §4 baseline-green evidence de-risks exactly that; (b)’s only extra delta = the lane’s 20 files, verify-GREEN ×2; the floor instruments (boot/transport/H4/A4) see IDENTICAL Java bytes under both. The prize: ONE evening banks the write-surface proof + the honesty fixes live + **G2’s live glance** + **FE-LIVE-V112 (a)–(c)** — and because the explanation surface is log-DERIVED, the Rosonway night’s historical chains render the five modes retroactively, live, tonight. **The browser block (~5 min, after Block 4, before Block 5):** open the dashboard against the Pi → (1) Overview → the availability tile (the three honest rows + the not-a-live-test line) ⏺ · (2) the pre-flight run’s causal chain (`/explain/run/01KYD3D4VN6B5M7PBT41FA1F3D`-class) → the modes render DISTINCT and the “Recorded outcome” disclosure shows the raw field with NO recovery note ⏺ · (3) any device drawer → evidence-with-age ⏺.

**Nick, in order:** **(1) THE CORE FE COMMIT** — from homesynapse-core, EXPLICIT paths, exactly 20 M: `git add web-ui/dashboard/MODULE_CONTEXT.md web-ui/dashboard/scripts/contract-check.mjs web-ui/dashboard/src/a11y.test.tsx web-ui/dashboard/src/components/CausalChain.tsx web-ui/dashboard/src/components/StatusPill.module.css web-ui/dashboard/src/components/StatusPill.tsx web-ui/dashboard/src/lib/api/contract.test.ts web-ui/dashboard/src/lib/api/contract.ts web-ui/dashboard/src/lib/api/mock/mockData.ts web-ui/dashboard/src/lib/api/mock/scenarios.ts web-ui/dashboard/src/lib/api/shapes.ts web-ui/dashboard/src/lib/format.test.ts web-ui/dashboard/src/lib/format.ts web-ui/dashboard/src/lib/verdicts.test.ts web-ui/dashboard/src/lib/verdicts.ts web-ui/dashboard/src/styles/tokens.css web-ui/dashboard/src/styles/tokens/tokens.dtcg.json web-ui/dashboard/src/views/DevicesView.tsx web-ui/dashboard/src/views/OverviewView.tsx web-ui/dashboard/src/views/WhyNotView.tsx` → `git commit -F ../_scratch/2026-07-26_core_fe-verdict-2_commit-msg.txt` → push → **⏺ BOTH glances: frontend.yml (the FE gate of record) + ci.yml, and RECORD the SHA.** **(2) the hivemind beat-6 commit** — EXPLICIT paths, exactly 6 entries (5 M + 1 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/assessments/2026-07-11_go-no-go-criteria_draft.md context/handoff/cross-agent-notes.md context/audits/2026-07-26_FE-VERDICT-2_lane_return.md` → `git commit -F ../_scratch/2026-07-26_hivemind_v38-beat-6_commit-msg.txt` → push. **(3) ONE LINE: deploy shape (a) or (b)** — REC **(b)**. **(4) TONIGHT: THE DEPLOY EVENING** — interactive `ssh pi`; Block 0a FIRST; on (b) Block-1 expects the FE SHA per the amendment-of-record; + the ~5-min browser block after Block 4; **⏺ everything → beat 7.** **(5) your ledger:** TSDR #2 **07-31** · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12.

**Hub next (v38):** the deploy-evening intake (beat 7: A4 → ✅ · H4 → ✅ · G2 CLOSES on the glance ⏺ · the criteria ledger re-status · the amendment file-fold · the corpus ratchet · FLAGS 1–3 retire) → **B2 AUTHORED** (rider #1 THE REBIND first · the §51 port · §5.5/§5.6 riders · uhubctl session prep · the three-flip constants re-mint · OBS-CONFIRM + C4 + RUNNER-VERSION-BANNER at authoring) → B3 (gated on the rebind) → the M14 WUs → Aug-8/9 H3 → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v39 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-26 (v38 beat 5 — **THE LANDINGS INTAKEN (core `da11f46` on main — SKIP-VIS LANDED; ci.yml glance = the one owed ⏺)** · **THE FULL CLEANUP + ROTATION executed (Nick’s charge)** · the deploy brief re-targeted to `da11f46`-on-CI-green · THE LAUNCH INSTRUCTIONS issued)

**The landings.** Core **`da11f46`** = exactly the ordered 12 (672+/58−), PUSHED (`4bc1258..da11f46`) — **SKIP-VIS is on main; the explanation surface’s honesty fixes + the v1.1.2 additive keys await only CI confirmation + the deploy.** Hivemind `6393554` = exactly the ordered 6 M, pushed. Both commits ran as `git add -A` and matched ONLY because the trees held exactly the ordered sets — the explicit-paths rule is restated ABSOLUTE (the v36 beat-5 precedent). **The one owed act on the landing: the ci.yml Actions glance on `da11f46` (the gate of record) → ⏺ the verdict.** On GREEN: the backlog SKIP-VIS row flips ✅ DONE and the Coder’s cross-agent note restamps Archived-SERVED (both at beat 6, per the CMD-API precedent). On RED: STOP — the hub adjudicates before ANYTHING deploys.

**THE CLEANUP + ROTATION (this beat’s work product — token economics for every next lane; env-model §8).** Hot-path rotations (kept-set → archive, tails verified, frontmatter Prior-chains trimmed with pointers): **pm-handoff** 264 KB → the v38 beats + Open Risks (the v37→v32 beat sections + chain → `archive/pm-handoff-beats-v32-v37-rotated-2026-07-26.md`) · **PROJECT_SNAPSHOT** 76 KB → the v38 masthead + the standing body (the v37→older blocks → `context/status/archive/PROJECT_SNAPSHOT-beats-v33-v37-rotated-2026-07-26.md`) · **coder-handoff** 149 KB → frontmatter + SKIP-VIS + CMD-API entries (LEARN-PERSIST→M9.5-DUR → `archive/coder-handoff-entries-rotated-2026-07-26.md`) · **cross-agent-notes** 164 KB → the ACTIVE SKIP-VIS note + the served CMD-API stamp (all else → `archive/cross-agent-notes-rotated-2026-07-26.md`) · **coder-lessons** 207 KB → the 2026-07 entries (pre-July → `context/lessons/archive/coder-lessons-pre-2026-07-rotated-2026-07-26.md`) · **pm-lessons** 73 KB → the 2026-07 entries (pre-July → `context/lessons/archive/pm-lessons-pre-2026-07-rotated-2026-07-26.md`) · **the backlog** 93 KB → the newest 5 currency notes + the rows + Notes (the 14 older notes → `context/planning/archive/phase-3-backlog-currency-notes-rotated-2026-07-26.md`). Directory hygiene: **weeks** W25–W29 → `weeks/archive/` (W30 current through tonight; W31 governs from tomorrow) · **_scratch** 69 consumed/orphaned msg files → `_scratch/consumed-2026-07/` (untracked; the orphan-sweep set incl. the beats-1-2/beats-1-3 pair) · **12 served handoff one-offs** (the 07-10×3 / 07-15×2 lane prompts · the joins-night go-package · the 04P mini-packages ×2 · the explainability-ux, brand-program, fusion-deliberation prompts · the rosonway operator package) → `context/handoff/archive/` — every move citation-grepped against the STANDING docs (map · canonical-paths · open-questions · program status · the v38 prompt · both live briefs · all three skill trees): ZERO standing references except the rosonway line in the deploy brief, amended this beat · **46 consumed instructions** (everything except SKIP-VIS) → `context/instructions/archive/` · **10 consumed pre-verifications** (everything except WU-SKIP-VIS + README) → `context/pre-verifications/archive/`. KEPT in place: every report/finding (the receipts corpus), the v38 prompt, the FE brief, the deploy brief, the Matter charter (standing program pointer), the SKIP-VIS instruction pair (standing citations: the FE brief + the freeze doc + B2).

**THE DEPLOY BRIEF AMENDED** (`2026-07-24_cmd-api-deploy-evening_operator-brief.md`): the target is now **`da11f46`** (the SKIP-VIS landing — with `da11f46` on main, a plain `git pull` fetches it; deploying `4bc1258` would now require an unnatural detached checkout) with a NEW hard precondition: **ci.yml GREEN on `da11f46`, verified BEFORE the evening starts** (⏺ the verdict; RED ⇒ the evening is OFF, the hub adjudicates). Grounds: one evening proves the write surface AND kills the wire lies; the delta beyond `4bc1258` is read-side derivation only (zero emitter/boot/transport-path changes, CI-gated); the playbook §8 interim dead-zone rule expires at this deploy. Block-5’s Rosonway line updated (that evening is DONE; the package archived). **If you want the old two-evening shape instead, say so — one line re-amends.** The FE brief’s baseline line updated: SKIP-VIS is LANDED (`da11f46`), not yet deployed — law (c)’s mock discipline holds until the deploy, exactly as written.

**Nick, in order (the full launch/dispatch set):** **(1) ONE Actions glance: ci.yml on `da11f46`** → ⏺ the verdict here (GREEN expected; it gates everything below). **(2) THE CLEANUP COMMIT** (hivemind) — the tree state is 100% this beat’s audited product; fresh porcelain verified census-exact immediately before this order: `git add -A` → `git commit -F ../_scratch/2026-07-26_hivemind_v38-beat-5_commit-msg.txt` → push (the message states the exact census: 8 M + 7 new + 73 renames; the §10 audit rode this beat). **(3) LAUNCH FE-VERDICT-2** (fresh Cowork, parallel-safe, anytime): `Read nexsys-hivemind/context/handoff/2026-07-26_FE-VERDICT-2_lane_session_prompt.md and execute it.` — its return routes here for the two-layer audit. **(4) TONIGHT — THE DEPLOY EVENING on the amended brief:** interactive `ssh pi` (never `ssh pi '<cmd>'` — the TTY rule) → **Block 0a FIRST** (the soak grep, before anything, because boot-health restarts the app) → Block 0 floor `[PASS]` → Block 1 pull → **HEAD-verify `da11f46`** → installDist → Block 2 deploy restart + boot glance (relinked ×6 lawful · zero proposes · position ≥ P-pre) + boot-health `[PASS]` → Block 3 H4 rep → Block 4 A4 kill−9 rep (P-kill recorded FIRST) → **⏺ ALL FIVE pastes here, either way — they intake as beat 6** (A4 → ✅ · H4 → ✅ · the criteria ledger re-status · the backlog flip on your CI paste · the corpus ratchet). **(5) your ledger:** TSDR #2 **07-31** (5 days) · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com glance · tamoro.dev $12 · discovery sourcing at leisure. **Nothing else is owed — no mirror sync (zero skill edits this beat), no core act beyond the CI glance, no ruling outstanding.**

**Hub next (v38):** the CI + five-paste intake (beat 6: the backlog flip · A4/H4 → ✅ · note archival · the dead-zone rule retires) → **B2 AUTHORED** (rider #1 THE REBIND first · the §51 port · §5.5/§5.6 riders · uhubctl session prep · the three-flip constants re-mint · OBS-CONFIRM + C4 latency + RUNNER-VERSION-BANNER at authoring) → B3 nightly (gated on the rebind) → the M14 WUs (post-B2, rider 3) → Aug 8–9 H3 → **~Aug-10 THE DRY-RUN** (29 rows vs artifacts) → Aug-12–13 I2 + close-out + **v39 BANKED** + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.** Beside: FE-VERDICT-2’s return audit (G2 closes on its landed demo) · TSDR #2 → Pelton ~Aug-5 → G-2 = two acts → BRAND-G2-EXEC · fusion sovereignty absolute.

## 2026-07-26 (v38 beat 4 — **THE SKIP-VIS TWO-LAYER AUDIT: ACCEPT — ZERO DEFECTS** · the freeze-doc v1.1.2 amendment EXECUTED (hub-owned) · **THE CORE COMMIT ORDERED (12 explicit; ci.yml = the gate of record)** · the deploy-target one-liner queued · the forward plan restated across all four lanes)

**The audit (basis stated honestly).** Layer-1 = the Coder’s record: baseline re-derived forced-fresh (automation 186/0/0 · rest-api 101/0/0 — the rest-api count matching the CMD-API record exactly, no drift) · stage-A exactly 18 XML-verified behavioral reds (+4 disclosed green-by-construction regression legs, the W2-LEARN class) · stage-B automation 203/0/0 + rest-api 104/0/0 forced-fresh ×2 JVMs · M1 kills exactly 4 / M2 exactly 3 (both DP-3 sites) / M3 exactly 1, all restores cmp-proven · full `check` GREEN ×2 (156) · spotless zero · the Coder’s own 4-lens fleet returned zero code defects (layer-1 evidence, never a substitute). Layer-2 = hub host-truth reads of ALL 12 files: **DP-1** — the 5-branch precedence realized exactly (`NON_FAILURE_OUTCOMES = {acknowledged, superseded, unconfirmed}`; the recorded-reason-verbatim branch ABOVE the timeout branch; `resultOutcome` fact-carried in every branch; “last = readByCorrelation log order” documented) · **DP-2** — the commandless branch FIRST on the terminal payload (no chain fetch on that path), the exact mandated sentence, `Boolean.TRUE` marker, clean-success text unreachable; the two legacy branches ride the 8-arg convenience ctor · **DP-3** — the shared `derivedTriggerInstant` helper at BOTH sites with the branch law + the don’t-simplify javadoc; `RunSummary`’s parenthetical corrected · **DP-4 GO** — `ActionView` 6→8 (`resultOutcome` nullable · `settled` primitive via the exact Q1b formula on the `Outcome` record; nonDispatched views settled=true with reasoning) · wire deltas appended LAST, camelCase, existing keys byte-identical; `ListRunsEndpoint` absent from the census (SD-5’s proof) and the `:79` six-key pin verbatim · test counts 25/16/10/9 — the suite arithmetic coheres (+17 automation / +3 rest-api vs the XML totals) · both MODULE_CONTEXT banners are model-grade currency (the SD-7 residue even recorded) · WUCP P1 5/5 (coder-handoff prepended, **next WU = B2** ✓; the cross-agent audit-request note ACTIVE at the correct seam — the archived CMD-API heading intact after the Coder’s in-session splice repair; coder-lessons +1 [the splice-hygiene lesson — its third incident class, new mechanism]). **Adjudications:** ONE [INFO] — the completion report’s “AutomationEndpointsTest (9→10)” is a prose slip (the file is 8→9 `@Test`; the XML totals and census are the ground truth and are exact). **I1–I7 all RATIFIED** (I2’s shared-helper M2 realization is instruction-anticipated; I4’s acked-only `settled=false` is the formula’s letter, correctly). **ACCEPT.**

**The freeze-doc v1.1.2 amendment EXECUTED (SD-1(d), hub-owned — the write this lane correctly refused):** `context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md` — the masthead state-type line stamps **v1.1.2** + three inline notes per the v1.1.1 precedent (the runs-list `triggeredAt` VALUE-correction note · the causal-chain `actions[]` shape gains `resultOutcome`/`settled` with the amendment record · the non-firing shape gains `noCommandsIssued` with the detection law). The v1.1 base stays byte-stable; consumers key on the additive keys.

**Nick, in order:** **(1) THE CORE COMMIT** — from homesynapse-core, EXPLICIT paths, exactly 12 M: `git add core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java core/automation/src/main/java/com/homesynapse/automation/RunExplanation.java core/automation/src/main/java/com/homesynapse/automation/NonFiringExplanation.java core/automation/src/main/java/com/homesynapse/automation/RunSummary.java core/automation/src/test/java/com/homesynapse/automation/StandardExplanationServiceTest.java core/automation/src/test/java/com/homesynapse/automation/NonFiringExplanationServiceTest.java core/automation/MODULE_CONTEXT.md api/rest-api/src/main/java/com/homesynapse/api/rest/GetRunCausalChainEndpoint.java api/rest-api/src/main/java/com/homesynapse/api/rest/GetNonFiringEndpoint.java api/rest-api/src/test/java/com/homesynapse/api/rest/RunEndpointsTest.java api/rest-api/src/test/java/com/homesynapse/api/rest/AutomationEndpointsTest.java api/rest-api/MODULE_CONTEXT.md` → `git commit -F ../_scratch/2026-07-26_core_skip-vis_commit-msg.txt` → push → **ONE Actions glance: ci.yml on the pushed commit (the gate of record) → ⏺ the verdict + the SHA.** **(2) the hivemind beat-4 commit** — EXPLICIT paths, exactly 6 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md context/handoff/coder-handoff.md context/handoff/cross-agent-notes.md context/lessons/coder-lessons.md` → `git commit -F ../_scratch/2026-07-26_hivemind_v38-beat-4_commit-msg.txt` → push. **(3) ONE LINE — tonight’s deploy target:** **(a) the SKIP-VIS landing HEAD on CI green (REC** — one evening proves the write surface AND kills the wire lies; the delta is read-side derivation only, CI-green; the playbook §8 interim dead-zone rule expires TONIGHT; on your “(a)” I hand the brief’s amended Block-1 expected-HEAD line with the SHA) or **(b) `4bc1258` as-briefed** (SKIP-VIS deploys with B2’s first silicon evening; the interim rule persists until then). **(4) LAUNCH FE-VERDICT-2** if not yet running (parallel-safe; unchanged launch line). **(5) TONIGHT: THE DEPLOY EVENING** — Block 0a FIRST; ⏺ all five pastes → they intake as beat 5 (A4 → ✅ · H4 → ✅ · the criteria ledger re-status · the corpus ratchet). **(6) your ledger:** TSDR #2 **07-31** · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com glance · tamoro.dev $12 · discovery sourcing at leisure.

**Hub next (v38) — the forward plan across the four lanes, restated as the standing sequence:** **Coding (core):** after the SKIP-VIS landing the core lane goes QUIET until **the M14 WUs** (~4–6, integration-shelly; authored AFTER B2 lands per rider 3; ident gate `S4PL-00116US`, capture-verbatim at unbox, DUT-of-record + spare role ruled at authoring; M14 yields to gate rows on ANY contention); EXEC-DETERMINISM + CMD-API-ACTOR stay post-gate. **Testing/benching:** tonight’s deploy intake (beat 5) → **B2 AUTHORED** (my next major desk product: rider #1 THE REBIND first [fetch-first, bind `trigger.matchedAt`, anti-false-PASS preserved, fixture pairs, both fields in the ok-payload] · the §51 suite port [C2/C3 [M] + S31 confirm legs + the AUTO usb scenario + H2’s AUTO suite] · OBS-CONFIRM + C4’s latency arm + the RUNNER-VERSION-BANNER rule at authoring · riders §5.5 `pending_positive_tokens` + §5.6 goal-string · uhubctl install = session prep · **the three-flip constants re-mint** [`command-api` → true on the wire truth · `usb-power` → true with `usb.*` minted from uhubctl’s OWN read · fleet re-verified]) → **B3 nightly** (the C1-N accumulation engine, gated on the rebind) → **H3 [M] = the named Aug 8–9 slot** (clean-image fresh install) → **~Aug-10 THE DRY-RUN** (all 29 rows vs ARTIFACTS). **Research/design:** gate sovereignty absolute through Aug-16 — no new design work; discovery sourcing stays Nick’s leisure lane; Matter Doc-19 Phase C rides its program hub; the §5.2 availability≠reachability divergence is PARKED as a named post-gate design conversation; post-gate week 1 = REC-GRANT-SCAN-EARLY, then the provenance-prior-art + phase1-commercial-feature-set lanes launch, the launch-runway charter lands at the Aug-12–13 close-out (REC-LAUNCH-GATE), and ~Oct runs trial-as-gate + watch run #1 + the mid-Oct discovery synthesis that rules the beachhead and re-cuts FQ-5. **Frontend/brand:** FE-VERDICT-2 runs now (its return → my audit; **G2 closes on its landed demo**; the mock→live swap is a flag flip once SKIP-VIS deploys); brand wait-states tick — TSDR #2 **07-31** → Pelton ~**Aug-5** → the results consult (approved agenda) → **G-2 = exactly TWO acts** → BRAND-G2-EXEC (pre-authorized). Then: Aug-12–13 I2 + close-out + **v39 BANKED** + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-26 (v38 beat 3 — **THE SKILLS CURRENCY PASS (Nick’s pre-launch charge): 8 drift classes CONFIRMED-and-FIXED across all three role skills** (10 files, 2 repos) · **the beats-1–2 commit LANDED `62007ce` MID-BEAT — CENSUS-EXACT, pushed** · beat 3 commits ALONE (9 M) · ⚠ **MIRROR SYNC BEFORE ANY LANE LAUNCH**)

**The charge + method.** Nick, pre-Coder-launch: ensure the skills carry nothing outdated or contradictory vs the next sessions, the next Coder sessions, and the long-term ambitions. Full reads of all three SKILL.md + CLAUDE.md; targeted reference sweeps by grep class (volatile state · brand/naming · ambition claims · mechanism claims); every hit adjudicated at the spine sources per truth-hierarchy. Mirrors were 3/3 md5-≡ at launch, so container-cache reads were lawful source proxies; every fix applied to the WRITABLE sources (hivemind `project-manager/`+`coder/`; skills-repo `orchestrators/nexsys-frontend/`).

**The 8 confirmed-and-fixed classes:** **(1) AMBITION-CRITICAL — the licensing principle** (`project-manager/references/constraint-enforcement.md` taught “Proprietary licensing preserved … one-way door … preserves optionality” — the OPPOSITE of the LOCKED strategy [Apache 2.0 core forever, never a license rug — `Revenue_Model_and_Licensing_Strategy.md` + the FQ-rulings standing law; the LICENSE flip = a launch-runway producer]; RE-GROUNDED: destination Apache-2.0, headers proprietary only until the flip, dependency rule Apache-2.0-clean [GPL/AGPL never — operative effect unchanged], the one-way-door language retired). **(2) The asimtote-leading brand drift** (FE SKILL.md §ruled-decisions + `references/brand-and-design-system.md` §6.1 — the item QUEUED since v36 beat 8, resolved at BOTH carriers): → the RULED Architecture C (TAMORO forward · ASIMTOTE quiet parent · TAMODO fallback-of-last-resort; R-1 HELD until G-2; design-validate against TAMORO; W-A lead · D-A+D-B prototype · red retired; name-light discipline unchanged) + the FE freshness-preflight Check-6 brand-authority list updated. **(3) NO_DIRECT_TIME_ACCESS reach** (coder SKILL.md §Phase-3 + `java-patterns.md` §11 + `testing-standards.md` §8 all asserted the rule scans test code in every non-whitelisted module — refuted 2026-06-13 [M6.2], previously corrected only at the PM carriers): all three coder carriers now state convention-everywhere / enforcement-from-app-test-classpath / production-all-modules-caught. **(4) The coder §3.9 until-clause** (“zero log surface until the SKIP-VIS WU lands” implied SKIP-VIS emits skip events): aligned to the authored truth — READ-side honesty; skip events stay unemitted; the terminal payload’s counts are the ONLY disclosure (the beat-2 audit DEFECT-2 ground truth, propagated). **(5)** PM SKILL.md Mode-1 doc count 14 → 18-with-re-derive (+ the Doc-16/17/18 Locks named). **(6)** PM CLAUDE.md doc count 15 → 01–18-re-derive **+ Build Verification reconciled** (the prior text mandated an unconditional in-session full `check`, contradicting the 2026-07-04 lane discipline and WU-SKIP-VIS’s own build section). **(7)** coder SKILL.md “the only remaining stub is web-ui/dashboard” — refuted at source (13,664 B, populated through FE-1b); corrected. **(8)** format additions **#16 ARCH-RULE-REACH + #17 SEAM-I/O** folded into `coding-instruction-format.md` (the v36 beat-9 owed carrier-fold — the lessons lived in pm-lessons; the operative checklist now carries them). **Verified-current, no edit:** LTD count 19 ✓ · 22 Gradle modules ✓ · the terminal-materialization line (consistent with DP-G/DP-3) ✓ · mental-model registries-as-projections ✓ · review-and-quality Apache-2.0 line ✓ · FE §4a ten-value vocabulary ✓ · FE §4c interim guidance ✓ (still pre-landing). **FE +§4d added** (the v1.1.2 ruling · the five-modes-distinct law · the SKIP-VIS-in-flight pointer · **the `runs[].triggeredAt`-understated-by-`durationMs` interim caveat** · provisional-outcome + availability-honesty pointers) — the FE lane’s constitution now carries today’s laws, matching the FE-VERDICT-2 brief.

**MID-BEAT LANDING, reconciled at the fresh porcelain (the standing discipline — the `d20f75d` precedent exactly):** while the skills pass was in flight, **Nick ran the beats-1–2 order — LANDED `62007ce`, CENSUS-EXACT** (7-row stat = the ordered 8 porcelain entries with the archive D/?? pair recording as the rename row; PUSHED — `main ≡ origin/main` verified). **The beats-1–2 msg is CONSUMED, not an orphan; the drafted consolidated beats-1–3 msg (`_scratch/2026-07-26_hivemind_v38-beats-1-3_commit-msg.txt`) was SUPERSEDED PRE-ISSUE — never handed, never run, joins the orphan-sweep set.** The fresh porcelain after the landing + the skills writes = EXACTLY 9 M (the 7 skill sources + the 2 spine files re-modified by this beat) — the beat-3 order below stages exactly those.

**Nick, in order:** **(1) THE BEAT-3 COMMIT** (hivemind) — EXPLICIT paths, exactly 9 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md project-manager/SKILL.md project-manager/CLAUDE.md project-manager/references/constraint-enforcement.md project-manager/references/coding-instruction-format.md coder/SKILL.md coder/references/java-patterns.md coder/references/testing-standards.md` → `git commit -F ../_scratch/2026-07-26_hivemind_v38-beat-3_commit-msg.txt` → push. **(2) THE SKILLS-REPO COMMIT** — from nexsys-skills, EXPLICIT paths, exactly 3 M: `git add orchestrators/nexsys-frontend/SKILL.md orchestrators/nexsys-frontend/references/brand-and-design-system.md orchestrators/nexsys-frontend/references/freshness-preflight.md` → `git commit -F ../_scratch/2026-07-26_skills_frontend-v1.5_commit-msg.txt` → push. **(3) ⚠ RUN YOUR MIRROR SYNC** (hivemind `project-manager/` + `coder/` and skills-repo `nexsys-frontend/` → `.claude/skills/`) **BEFORE launching either lane** — both lanes load the mirrors; Check 9 stays STALE until the sync (the expected post-edit state). **(4) the three one-liners** (Q1b/DP-4 GO-or-OUT [REC GO] · DP-3 Site-B confirm [REC IN] · SD-7 residue [REC keep]). **(5) LAUNCH the Coder** (with the DP-4 line) **+ FE-VERDICT-2** — launch lines exactly as beat 2 stated. **(6) TONIGHT: THE DEPLOY** — Block 0a FIRST; ⏺ five pastes → intake as beat 4. **(7) ledger:** TSDR #2 **07-31** · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com · tamoro.dev $12 · discovery sourcing.

**Hub next (v38):** the deploy intake (beat 4) → the SKIP-VIS + FE-VERDICT-2 return audits → B2 (rider #1 the rebind FIRST; three-flip re-mint) → B3 (gated on the rebind) → the M14 WUs → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v39 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## 2026-07-26 (v38 beat 1 — **LAUNCH: PASS all 11** (every expected adjudication confirmed) · **the beat-4 landing `8fc7dfb` CENSUS-EXACT** (4 = 2 M + 2 new; the v37 chain complete and pushed) · the v37 prompt ARCHIVED · the deploy = the next physical act (tonight lawful))

**Launch (remote §12).** Filesystem-MCP scope re-verified at first act: FULL ClaudeFolder. §11 clean — lock-free porcelain ×5 ALL CLEAN; both spine frontmatters read pre-write (last writer = v37 beat 4, committed — no ghost). The uploaded v38 prompt **md5-≡ the repo copy** (`0a915cc7…`) — no v37-close refresh occurred; booted on the copy of record. Baselines re-derived at source 5/5: core **`4bc1258`** (**the Pi still runs `355a711` — THE DEPLOY IS OWED**, ≥ 07-26 satisfied TODAY) · docs `a53f474` · hivemind **`8fc7dfb`** (chain `d20f75d`→`de143d9`→`8fc7dfb` verified; stat = exactly 2 M + 2 new) · bench `9f2b5ad` · skills `dccc85f`. Mirrors 3/3 composite-md5-≡ (zero v37-arc skill edits, as expected). Watermark **AMD-99, 175/53** verbatim at the register regeneration line; fleet **25065, ch20/0x774c** at `scenarios/constants.yaml`. Preflight adjudications: Check 2 — W30 IN_PROGRESS current through 07-26 + W31 PRE-MINTED for the 07-27 boundary (W32 correctly NOT owed — the Aug-3 boundary is a week out). Check 4 — backlog currency = v37 beats 1–3, CURRENT (the M14 legacy-row redescription stays queued for the M14-adjacent touch). Check 5 — Open Risks all resolved. Check 8 — ACTIVE 0. Check 9 — PASS-per-proxy. Check 11 — spot-checks verbatim + **the authoring-grade re-enumeration executed on the SKIP-VIS target** (see beat 2; it found the SECOND `minusMillis` site). The v37 prompt → `context/handoff/archive/` (the D/?? pair rides this commit as R). W32 pre-mint: not owed. Drift check: clean.

## 2026-07-26 (v38 beat 2 — **SKIP-VIS AUTHORED ISSUE-READY** (three DPs + ⛔DP-4 one-line-rulable; 12-M census; two-layer audited — **2 authoring DEFECTs caught pre-issue**) · **FE-VERDICT-2 LANE BRIEF AUTHORED** (parallel-safe) · **G2 → IN-FLIGHT** · EXEC-DETERMINISM OUT/re-homed · Nick’s one-liners queued: Q1b GO/OUT · Site-B confirm · SD-7 residue)

**THE INSTRUCTION** — `context/instructions/2026-07-26_SKIP-VIS_explanation-honesty_coding-instruction.md` + `context/pre-verifications/WU-SKIP-VIS.md` (P0–P14), authored against `4bc1258` with every line anchor re-enumerated at source (the v38 charge’s beat-2-era cites confirmed: toSummary decl `:453`, arithmetic `:466`; completedVerdict `:284–:307`; deriveOutcome `:616`; isFailure `:656`). **DP-1 (CORE-P1):** the 5-branch derivation contract — failure-class = outcome ∉ {acknowledged, superseded, unconfirmed}; honest-`unconfirmed` → UNCONFIRMED with the recorded reason verbatim (above the timeout branch, so the richer record wins); superseded NEVER selects FAILED (falls to DISPATCHED; distinctness rides the additive key); `resultOutcome` = the last causation-matched `command_result`’s raw outcome in EVERY branch (fact-carry). **The five-modes law made testable:** the instruction embeds the signature table — five pairwise-distinct `(outcome, resultOutcome, reason)` tuples — and mandates `explainRun_fiveFailureModesDistinct`. **DP-2 (CORE-P2):** payload-arithmetic detection (`actionCount>0 && commandCount==0`) → ACTED_BUT_UNCONFIRMED (frozen 4 — no enum growth) + additive nullable `noCommandsIssued` marker + the honest explanation; the clean-success sentence becomes unreachable on that path. **DP-3 (RUNS-TRIGGEREDAT):** eventTime-present ⇒ `triggeredAt = eventTime` (DP-G: it IS the trigger instant); `−durationMs` survives ONLY on the ingestTime fallback; **+ Site B** — the SAME defect at `explainNonFiring`’s `evaluatedAt` (`:228–:229`), found by the mandated re-enumeration, same branch law, wire-shape-unchanged; `triggeredAt ≡ matchedAt` regression + ingestTime-fallback legs + M2 mutant kills at BOTH sites; `RunSummary` javadoc parenthetical corrected. **⛔DP-4 (Q1b, one-line-rulable):** additive per-action `settled` boolean (pure derivation: provisional ⇔ DISPATCHED with resultOutcome null/acknowledged) — gated on Nick’s GO/OUT in the dispatch; never half-implemented. Wire law: SD-1 = ruling 1’s four constraints verbatim-class; the v1.1.2 amendment note on the contract-freeze doc is **HUB-owned at WUCP P2** (write-isolation — the lane’s only sanctioned hivemind writes are the three WUCP-P1 handoff files). Census: **exactly 12 M, 0 new**; targeted gates + one full `check`; mutation M1–M3 with cmp restores.

**THE TWO-LAYER AUDIT (2 adversarial agents, distinct lenses; labels-are-claims/quotes-are-evidence; layer-2 adjudicated every finding):** **DEFECT-1 (source-conformance agent, CONFIRMED + folded):** the non-firing wire pin lives at `AutomationEndpointsTest.java:79–:80` (`nonFiring_v11ShapeTest`; + 3 × 8-arg `NonFiringExplanation` constructions `:57/:102/:150`) — an unenumerated consumer that DP-2’s wire delta hard-fails; the census grew 11→12 M, G-7b + the files-table row added. The exact P2-survey class, caught by the audit layer working as designed. **DEFECT-2 (CONFIRMED + folded):** the claimed “§3.9 skip emits `action_completed "skipped"” was a misattribution — `StandardActionExecutor:192`’s `"skipped"` publish is the InterruptedException/ABORT path; the real §3.9 skip is `case SKIP -> continue` (`:243–:245`), which emits NOTHING, and an all-skipped action completes `"success"` (`:199`) — so the terminal payload’s counts are the ONLY log-visible disclosure of the silent-skip class (this STRENGTHENS DP-2’s payload-detection design; G-10/P9 rewritten to the truth; an emitter-side per-target skip event noted as a separate unruled candidate). Also folded: the consistency agent’s test-count arithmetic fix (13→24 / 11→16 / 8→9 +AET), the five-modes seed pinning (row-exact variants), M2’s both-site extension, the WUCP artifact homes, the casing-pointer tightening. **Adjudicated-and-surfaced (Nick’s one-liners below):** the SD-7 residue — `expired_on_restart` + `invalid` (two of the four DISPOSITIONS) remain failure-class under the operative CORE-P1 letter · DP-3 Site B is authored IN as a PM-level same-file/same-law extension.

**FE-VERDICT-2 AUTHORED** — `context/handoff/2026-07-26_FE-VERDICT-2_lane_session_prompt.md` (fresh Cowork lane, nexsys-frontend skill, write-isolated to `web-ui/dashboard/` + one return file): consumes the ruled v1.1.2 shape (law (c): mock-to-the-ruled-shape + FLAG until SKIP-VIS lands — stated as the lane’s constitution); **the five-modes-distinct law + the §3a ALL-USERS/CVD mandate** (distinction never rides hue alone; both themes AA on the brand lane’s corrected mode-paired tokens — pointer to the return + identity-system files, values read at source); §5.9 provisional-vs-settled rendering (client-side derivation identical to DP-4’s rule — a one-line swap if GO); §5.3 availability honesty (AVAILABLE ≠ live-contact; surface `lastReported` age; `staleAfter: null`); **G2 [S] live-half** (the availability tile renders the honest states against the DEPLOYED read surface — no SKIP-VIS dependency); the dashboard-deps watch (npm ci + build + suite on the dependabot-bumped lockfile — the standing watch closes on its evidence line); D-4 retirement via first-class `resultOutcome` with graceful pre-v1.1.2 degrade. **G2 ledger row → IN-FLIGHT** (F2 ✓ v32 · G1 ✓ v35 — the stale PENDING cleared; closes on the lane’s landing). **EXEC-DETERMINISM adjudicated OUT** with grounds (a write-path executor/serializer concern — `CommandParameterSerializer` lives in core/persistence — inside a read-side honesty WU; non-gate, cosmetic) **and re-homed explicitly: the first post-gate automation/executor-touching WU, candidate pairing CMD-API-ACTOR.** §5.4 adjudicated by-design (SD-4; counts disclose, no delay-action surfacing).

**Nick, in order:** **(1) THE COMMIT** — EXPLICIT paths, exactly 8 porcelain entries (3 M + 3 ?? + the archive D/?? pair [records as R]): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/assessments/2026-07-11_go-no-go-criteria_draft.md context/instructions/2026-07-26_SKIP-VIS_explanation-honesty_coding-instruction.md context/pre-verifications/WU-SKIP-VIS.md context/handoff/2026-07-26_FE-VERDICT-2_lane_session_prompt.md context/handoff/2026-07-24_PM-mission-control_v37_orchestrator_session_prompt.md context/handoff/archive/2026-07-24_PM-mission-control_v37_orchestrator_session_prompt.md` → `git commit -F ../_scratch/2026-07-26_hivemind_v38-beats-1-2_commit-msg.txt` → push. **(2) THREE ONE-LINERS** (any order): **(a) Q1b/DP-4** — the additive per-action `settled` marker: GO or OUT? (REC: **GO** — pure derivation, additive-lawful, FE consumes it directly.) **(b) DP-3 Site B** (the `evaluatedAt` sibling — same file, same law, wire-shape-unchanged, authored IN): confirm, or one line strikes it. (REC: **IN**.) **(c) SD-7 residue** — `expired_on_restart` + `invalid` stay failure-class for now (REC: **keep**; `resultOutcome` carries both distinctly on the wire from this WU on, so a later re-class is additive-cheap.) **(3) LAUNCH THE CODER** (host-CC, homesynapse-core, AFTER the push, WITH your (a)/(b) lines in the launch message): `Read nexsys-hivemind/context/pre-verifications/WU-SKIP-VIS.md and nexsys-hivemind/context/instructions/2026-07-26_SKIP-VIS_explanation-honesty_coding-instruction.md, then execute — DP-4 is GO/OUT [your ruling] — /nexsys-coder` (allow-list: `./gradlew :core:automation:compileJava :core:automation:test :api:rest-api:compileJava :api:rest-api:test` + one full `check`). **(4) LAUNCH FE-VERDICT-2** (fresh Cowork, parallel-safe, after the push): `Read nexsys-hivemind/context/handoff/2026-07-26_FE-VERDICT-2_lane_session_prompt.md and execute it.` **(5) TONIGHT: THE DEPLOY EVENING** on the amended brief — **Block 0a FIRST**; ⏺ all five pastes → they intake here as beat 3. **(6) your ledger:** TSDR #2 **07-31** · unbox capture-verbatim (`S4PL-00116US`) · tamorro.com glance · tamoro.dev $12 · discovery sourcing at leisure.

**Hub next (v38):** the deploy intake (A4/H4 → ✅; criteria ledger re-status; the corpus ratchet) → the SKIP-VIS return two-layer audit → the FE-VERDICT-2 return audit → **B2 authoring** (rider #1 THE REBIND first; §5.5/§5.6 riders; uhubctl prep; the three-flip constants re-mint) → B3 (gated on the rebind) → the M14 WUs → ~Aug-10 THE DRY-RUN → Aug-12–13 I2 + close-out + v39 + THE LAUNCH-RUNWAY CHARTER → **Aug-14 EOD FREEZE → Aug-16 THE READ.**

## Open Risks

#### OR-GATE-M9.4-NCFG — ci.yml RED on pushed `ccba69d` (`ReplayTransitionIT` timeout) — [✅ RESOLVED 2026-07-06, v22 beat 5 — rerun GREEN; closed as INTERMITTENT-definitive] (NEW 2026-07-06, v22 beat 4+)
- **RESOLVED (beat 5):** Nick re-ran the failed jobs on `ccba69d` → GREEN. Closed as intermittent with Nick's reinforcing causality on the record: *NCFG touched integration-zigbee only and cannot causally reach event-bus replay — runner variance, not a regression; "intermittent" is definitive, not probable.* §4b UNBLOCKED; the M9.4-ADP dispatch hold lifted. **The deflake row stays QUEUED regardless (Nick: "latent moat-debt"):** `awaitCheckpoint` must report checkpoint PROGRESS and fail on STALL, not wall-clock — rides the next event-bus-touching WU (its own micro-WU on recurrence).
- **Milestone:** M9.4-NCFG (the gate of record on its push). **Commit:** `ccba69d`. **Failure:** `:core:event-bus:test` → `ReplayTransitionIT` "resumes from persisted checkpoint and catches up to LIVE without gaps under continuous publish" — `AssertionError` at `:212` = the `awaitCheckpoint` 30 s timeout (called `:167`); 221 tests, 1 failed.
- **Adjudication frame (hub, at intake):** event-bus is UNTOUCHED for 65 commits (last touch `a441fdf`, M4.0a); CI ran green on identical event-bus bytes many times incl. `e363320` same-day; Nick's local full `check` GREEN ×2 today on trees carrying this exact code; the failing await is wall-clock-bounded real-thread concurrency (1500-event continuous publish) — the slow-runner flake profile. NOT waved off: the same signature would present for a REAL subscriber-stall race, and the error omits the last-seen checkpoint — undecidable from the log alone.
- **Owner:** Nick (re-run) / hub (adjudication + the deflake row). **Commands (Nick):** GitHub → the failed run on `ccba69d` → **Re-run failed jobs**. Optional local hunt: `./gradlew :core:event-bus:test --tests "*ReplayTransitionIT*"` repeated ×5.
- **Resolution condition:** rerun GREEN ⇒ gate CLEARS as intermittent (this OR closes; the **deflake/instrumentation row is QUEUED regardless** — `awaitCheckpoint` must report the last-seen checkpoint and fail on STALL rather than pure wall-clock; rides the next event-bus-touching WU, its own micro-WU if it recurs). Rerun RED ⇒ escalates to a REAL investigation WU (a reproducible-on-runner liveness failure in the delivery path outranks the bench — never-false-* extends to "without gaps").
- **§4b consequence: the M9.4-ADP dispatch HOLDS until this gate clears** (the ruling can land meanwhile; "go" fires on the green).

#### OR-GATE-M7.5c-a — deferred build gate on the M7.5c-a working tree — [✅ RESOLVED 2026-07-02, beat 50] (NEW 2026-07-02, beat 49)
- **RESOLVED (beat 50):** round 2 GREEN — 53 targeted + 149 full-`check` tasks on Nick's terminal → core committed **`e3d7296`** (10 paths exact) → pushed → **ci.yml ✅ + install-smoke ✅** on the push. M9.1 routing UNBLOCKED (§4b clear). Total: ONE fix round (2 test-compile errors, hub-fixed — within the P5 one-round target for compile-class defects).
- **Milestone:** M7.5c-a (internal-read envelope conformance). **Commit:** pending — working tree on core `2786526`; change set = exactly 10 files (7 source/test + 3 MODULE_CONTEXT).
- **Commands (Nick):** `./gradlew :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` → `./gradlew :api:rest-api:test :lifecycle:lifecycle:test` → `./gradlew check` (count pins stay **71/41/53**; both module-infos + every `build.gradle.kts` unchanged) → `./gradlew :testing:integration-tests:check` (watch `EndpointE2eIT` — DLQ paths moved under `$.data`). `-Werror` rides the convention plugin — do NOT pass it as a CLI flag.
- **Round 1 (2026-07-02):** targeted `compileJava` GREEN + `:testing:integration-tests:check` GREEN + host porcelain exactly 10 ✓; `compileTestJava` FAILED — 2 AssertJ varargs-capture errors (`DlqStatusEndpointTest` L75/L135: `containsExactly` on a `List<?>` actual). **Hub-applied 2-line fix** (SendMessage-to-lane unavailable; mechanical class): the `@SuppressWarnings("unchecked")` typed-local idiom per conforming `ListEntitiesEndpointTest` L83–88. Lesson appended to coder-lessons. **Re-run pending:** `:api:rest-api:test :lifecycle:lifecycle:test` → `check`.
- **Owner:** Nick. **Deferred Build Gate:** YES — the lane's sandbox attempt failed at `build-logic` (mount delete + VM JDK 11 vs 21); evidenced in coder-handoff, never silently skipped.
- **Resolution condition:** `check` green → core commit (staged message, exactly 10 paths) → push → `ci.yml` green on the pushed commit (the gate of record). On resolution: M9.1 routes to the next Coder lane; FE-1b launches on the landing.

#### OR-M7-WIRING — Command-pipeline live wiring — [✅ RESOLVED 2026-06-27 — live-wired at M7.4b/c] (formalized 2026-06-25; NEW in beat 7, 2026-06-22)
- **RESOLVED 2026-06-27 (M7.4b producer + M7.4c confirmation/ledger; v8 beats 17/19; reconciled by the v9 hub).** The command pipeline is LIVE on the real composition root: trigger → run → `command_issued` → co-located dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`, with the live `pending_command_ledger` + `pollExpirations()` tick and paired teardown. The resolution condition's **E2E composition-root confirm gate is met** — `RunPipelineConfirmWiringTest` (host-verified present in `lifecycle/lifecycle/src/test`).
- **Residual → M7.4d (the only remaining M7.4-arc item):** the resolution condition's *second* clause — the **D2 pure-function-replay CI test wired as a gate** (+ registering D2's canonical INV-id) — is NOT yet landed. The *wiring* is resolved; the *replay-safety CI gate* is the M7.4d hardening.
- **Severity:** RESOLVED (was MEDIUM). **Owner:** PM/Coder — done. _See OR-GATE-M7.4 below for the gate-of-record confirm on `db8ab5f`._

#### OR-GATE-M7.4 — gate-of-record (CI) on the pushed `4e31aed` (M7.4d HEAD) — [✅ RESOLVED 2026-07-01 — ci.yml VERIFIED GREEN on core 52824e9 (beat 43)] (NEW 2026-06-27, v9 hub; updated v10 beat-25)
- **RESOLVED 2026-07-01 (v13 hub, beat 43; reconciled into this ledger by the v14 hub):** `ci.yml` GREEN on pushed core `52824e9` — screenshot-verified (Build & Check 3m5s, Gradle 8.8, `check` ✅). Subsumes the per-commit CI-confirm for the whole ancestor chain (`4e31aed`/`9ec5949`/`eed477e`/AB-4/M7.5a/b). The `frontend.yml` first-trigger glance on `52824e9`+`2786526` is a standing Nick item (hub tracks), not an OR.
- **Status:** NARROWED. The **local `./gradlew check` is CONFIRMED GREEN** on the M7.4c working tree that became `db8ab5f` (Nick's terminal 2026-06-27: BUILD SUCCESSFUL, 149 tasks; he then committed + pushed core `69cc6e2..db8ab5f`). So the M7.4b/c/**d** deferred build gates are all **resolved locally** (Nick's terminals: `./gradlew check` 149 tasks green on the M7.4c tree → `db8ab5f`, and again on the M7.4d tree). The only residual is the **gate-of-record** formality: the `ci.yml` GitHub Actions run green on the pushed HEAD **`4e31aed`** (the M7.4d commit has now landed; `db8ab5f`→`4e31aed`). **De-risked (v10 beat-25):** `git ls-tree 4e31aed gradlew` = `100755` (executable) → the prior exit-126 failure mode is structurally cleared, and `ci.yml` runs `./gradlew check --no-daemon`; what remains is purely Nick confirming the Actions run is green on `4e31aed` — the standing b85e1ed/M7.4a pattern, "confirm CI green on the pushed commit, not just locally."
- **Severity:** LOW (the substantive gate — the full local `check` — is green; this is the CI-record formality).
- **Owner:** Nick (glance at the Actions tab for `db8ab5f`). **Resolution condition:** Nick confirms the `ci.yml` run is green on `db8ab5f` → record M7.4b/c CI-gated → close. M7.4d may be authored/routed now (the local gate clears §4b — done, ISSUE-READY beat 22).

#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
