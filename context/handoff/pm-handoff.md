<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-09-25 (v80 beat 4 — THE v80 CLOSE at four, on Nick's word ("too many steps"): T4 `a45686f` · T5 · T6 PASS 9/9 + the `power_w` wire pin · T4b `df4a2d7` (the load at one 40 W lamp); the CHAR NOT RUN (one aborted run; G4-2 UNAVAILABLE after boot-health's restart — IR-56's class); `CHAR: sat-am`; THE SIMPLER WAY ruled (D-v80-17; two pm-lessons); the plan §27; the v81 text (`context/handoff/2026-09-26_v81_dispatch-text.md`); Lane E (rehearsal 1's inputs; IR-18 first); the Pi's clock erratum (UTC−4) corrected; Fri 2026-09-25 ~22:4x CT (instrument 2026-09-26T03:40:57Z). Order: ONE card for b2 + b3 + b4 (hivemind 53 = 9 M + 44 A by explicit paths, computed at -uall inside the splice))) Prior: 2026-09-25 (v80 beat 3 — THE RE-MINT BEAT: T2b intaken at the bytes (both Gen4 adopted by "Start pairing" from their web pages; T3b 9/9, Bearer 0; the mechanism corrected — a lost membership, not a silent resume; IR-56's instrument stands); T4 the constants re-mint written to the bench working tree from the capture's own reads (fleet 9/9; the three plug entities; `metering-plug: true`; provenance untouched); Half 2 (T4 → T8) DISPATCH-READY; v78 b1–b4 rotated; Fri 2026-09-25 ~21:0x CT (instrument 2026-09-26T02:04:19Z). Order: ONE card for b2 + b3 (hivemind 46 = 7 M + 39 A by explicit paths, computed at -uall inside the splice); the bench's T4 commit is Nick's own card)) Prior: EVERY SEGMENT v55 b3 → v80 b2 ROTATED VERBATIM into archive/chains-rotated-2026-08-27.md (one heading per rotation = the COUNTED record: 163 rotations through v80 b4; the v80 b3 segment is the one Prior above); THE FULL PRE-2026-08-23 CHAIN (v55 b2 → v54 b10 segments + everything earlier) verbatim at archive/chains-pre-region-cap-2026-08-23.md; the pre-v55 chain at archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md.
-->

# PM Session Handoff

> **How to read this file:** beats are NEWEST-FIRST below; a fresh session reads the frontmatter `last-verified:` chain (one line, newest segment first) + the newest ONE beat block (THE CONTEXT-BUDGET LAW, the v67 prompt §1/§1b: chain ≤3,000 B · ≤12 live blocks · Open Risks OPEN-only); older blocks only when the newest sends you there. Older beats live in `archive/pm-handoff-beats-*-rotated-*.md` (the archive map at the tail). The `## Open Risks` section at the tail is standing. Only the hub writes here (prefix-prepend); the newest beat outranks everything below it.

## 2026-09-25 (v80 beat 4 — THE v80 CLOSE at four, on Nick's word: the night at the bytes (T4 `a45686f` · T5 · T6 boot-health PASS 9/9, CMP-IDENTICAL, the `power_w` wire path CONFIRMED · `CHAR: tonight-40w` → T4b `df4a2d7`); the CHAR NOT RUN (one aborted run; G4-2 UNAVAILABLE after boot-health's restart — IR-56's class, twice observed); `CHAR: sat-am` ruled on Nick's state; THE SIMPLER WAY ruled on his word (D-v80-17: five acts and one line per rig card; the prompts read and the affordances asked before the card; three readings on a steady load; no re-plan at the rig; the availability read first; no rig hours after 21:00 CT); the v81 text cut around it; Fri 2026-09-25 ~22:4x CT (instrument 2026-09-26T03:40:57Z))
- **The files:** `context/audits/2026-09-25_v80-b4_CLOSE_audit.md` (the close: the facts; the hub's own accounting §2; the ruling); `context/handoff/2026-09-26_v81_dispatch-text.md` (LIVE); `context/audits/2026-09-25_v80-b4_laneE_rehearsal-1_packet-inputs_digest.md` (rehearsal 1's inputs; IR-18 first in the Java slot — the presence sensor's adoption is one-way); `context/audits/2026-09-25_THURSDAY-ORDER_capture/run2/T6.txt` + the T4–T6 transcript; `context/lessons/pm-lessons.md` (+2); the DR §3d (CLOSED; D-v80-16..17); the plan §27; the packet, T2b and Half-2 cards' statuses (EXECUTED · EXECUTED · PARTIAL); the brief.
- **Layer 2:** re-executed — T6's output at the bytes (the PASS line; CMP-IDENTICAL; the TR3's state JSON; the API's UTC stamp beside the log's local stamp → the clock erratum ruled and the record's stamps corrected before this card); T4b's parse before and after; the four record files' wrong `Z` stamps replaced; the runner banner `@ df4a2d7` and "The load is 40 W" on Nick's screenshot. Not re-executed — the aborted run's bundle (v81 lists it); the tmux session's state; Nick's meter reads.
- **Order:** ONE card, b2 + b3 + b4 (the b2 and b2b3 cards SUPERSEDED, un-run). Nothing at the rig tonight. Saturday: v81's text pasted; `SAT-NIGHTLY:`; the CHAR on METER-2's re-cut, five-act cards.
- ctx: beat 4 · calls ≈16 · read ≈30 KB · wrote ≈40 KB · v80 CLOSED at four (the deliverable — rehearsal 1's packet — carries to v81 with its inputs digested)

## 2026-09-25 (v80 beat 3 — THE RE-MINT BEAT: T2b landed (both Shelly Gen4 adopted by "Start pairing" from their own web pages inside one re-opened window; T3b devices=9 entities=9, proposals 0, Bearer 0); the mechanism corrected (the Gen4's page read `steering` — a lost membership after the T2 restart); T4 the constants re-mint from the capture's own reads; Half 2 (T4 → T8, the CHAR) cut; v78 b1–b4 rotated; Fri 2026-09-25 ~21:0x CT (instrument 2026-09-26T02:04:19Z))
- **The files:** `context/audits/2026-09-25_v80-b3_T2b-intake_and_T4-remint_audit.md`; the capture `context/audits/2026-09-25_THURSDAY-ORDER_capture/run2/` (six boot logs, the lines, the entities, the labels, `zigbee.yaml` after, T1b/T3b.txt, the T2b transcript); `context/instructions/2026-09-25_THURSDAY-ORDER_Half-2_T4-T8_operator-card.md` (DISPATCH-READY); the bench working tree `scenarios/constants.yaml` (M, uncommitted — Nick's T4 card; `_scratch/v80/v80b3_constants_remint_a.py`, md5 after `d9eb0efd5f96`); the DR §3c (D-v80-14..15); the brief; `context/handoff/archive/pm-handoff-beats-v78b1-v78b4-rotated-2026-09-25.md` (A).
- **Layer 2:** re-executed — `device_adopted` ×3 across the capture (one deviceId each); the entity map from `entities-post.json` (9/9); the labels; the old six remembered ULIDs = the other six entities; the divisors read (Gen4 0x0B04 div 1, 0x0702 div 10⁶; TR3 div 10 / 1000); the re-mint's parse before and after with every other key asserted equal; provenance untouched. Not re-executed — the Gen4 web page's states (Nick's screenshots and word); the bench's runner against the new constants (T6 is that instrument; the wire pin for `power_w` at T6 gates T7).
- **Order:** ONE card, b2 + b3 (the b2 card SUPERSEDED, un-run); the bench's T4 card (1 M) separately; T5 · T6 tonight if Nick's hands allow (Saturday's nightly then reads 9/9); T7's day is Nick's.
- ctx: beat 3 · calls ≈14 · read ≈40 KB · wrote ≈30 KB · the window closes by b5 (D-v80-13)

## 2026-09-25 (v80 beat 2 — THE RIG DAY, FRIDAY: the complete operator packet; Half 1 intaken at the bytes — 1 of 3 adopted (the TR3), the two Shelly Gen4 resumed silently → IR-56, the recovery card T2b; `R5B-6:` 8/9 PASS as pre-registered; SHAKE B1 44.4 W vs B2 41.7 W; 7a ACCEPT-WITH-NOTES; VERIFY-72H's inputs; the window re-cut; Fri 2026-09-25 ~20:2x CT (instrument 2026-09-26T01:29:59Z))
- **The files:** `context/audits/2026-09-25_v80-b2_THURSDAY-ORDER_Half-1_intake_audit.md` (the Half-1 intake: Nick's report vs the capture, the source read, the ruling); the capture home `context/audits/2026-09-25_THURSDAY-ORDER_capture/run1/` (the three boot logs, the adoption and closed-boot lines, the entities, the labels, `zigbee.yaml` after, T0–T3.txt, the console transcript; Bearer 0); `context/instructions/2026-09-25_THURSDAY-ORDER_T2b_shelly-recovery_operator-card.md` + `context/instructions/2026-09-24_THURSDAY-ORDER_scripts/T1b.sh` (self-tested WRITTEN · KEPT · STOP); `context/audits/2026-09-25_v80-b2_C-S0-7a_intake_audit.md` + the Lane C/D digests; the register +3 (IR-54..56); the plan §26; the DR §1b–§3b (D-v80-9..13); the brief; the complete operator packet in `_scratch/v80/` (outside git; commands verbatim from the packet of record).
- **Layer 2:** re-executed — the three boot logs grepped for both Shelly IEEEs and their T1 short addresses (T2/T3 boots: 0 lines); the T1 log's ten Shelly lines; `zigbee.yaml.after-T3` parsed (9; the three present; no key); the adoption path at source (`ZigbeeIntegrationAdapter.java:1309` · :1437 · :1093; `ZigbeeAdoptionSlice.java:222`; the cache load seeds availability only, :392); `T1b.sh` run three ways on the after-T3 file; the six reused command strings of the complete packet counted 1/1 in the packet of record; Lane C's and Lane D's cites (the two audits §2). Not re-executed — the Gen4 button semantics (UNVERIFIED; T2b asks); the meters' displays; 7a's fetched pages.
- **Order:** T2b is the one act (tonight or Saturday first); the b2 card 28 = 7 M + 21 A by explicit paths — the lib's `census()` (plain porcelain) saw the capture dir as one `??`; re-computed at `-uall` right after: 28 = 7 + 21 (a `splice_lib_v2` item). The Saturday 03:30 CT nightly pre-registered RED on boot-health (devices=7 vs the constant 6; D-v80-11).
- ctx: beat 2 · calls ≈32 · read ≈120 KB · wrote ≈75 KB · two read-only lanes (≈180K tokens outside the hub) · the window closes by b5 (D-v80-13)

## 2026-09-23 (v80 beat 1 — THE BOOT AT THE INSTRUMENT; THE INTAKE; THE WINDOW VALIDATED ROW BY ROW (Lane A); THE HORIZON RE-CUT'S INPUTS DIGESTED (Lane B); THE PREMISE CORRECTED — the name is CLEAR and the application commissioned, not filed or registered (D-v80-3); the plan §25 corrected on Nick's word (SHAKE = B1 + B2; the P4460 boxed until CHAR — D-v80-2) and on the record (the K-refresh → Sunday, D-v80-5); the v80 DR opened (D-v80-1..8); ONE act for Thursday morning; Wed 2026-09-23 ~23:2x CT (instrument 2026-09-24T04:29:21Z))
- **The files:** the DR `context/planning/2026-09-23_v80_decision-record.md`; the audit `context/audits/2026-09-23_v80-b1_boot-and-intake_audit.md` and the two lane digests beside it (`…_laneA_window-table-validation_digest.md` · `…_laneB_horizon-recut-inputs_digest.md`); the plan §25 (three cells + a correction note); `coder-handoff.md` :26 (IR-40 + IR-44 LANDED `d22a8a4`); the brief; the v80 dispatch text → EXECUTED.
- **Layer 2:** re-executed — the boot read-set 45,871 B (over §1's 45 KB by 871 B: the dispatch's own list; disclosed); the five HEADs (ahead 0 ×5; porcelain 1 = the 7a return); the preflight 12/12 (Check 9 28/28 at the bytes); the 7a return at 10,239 B with its `RETURNED` line; FIN-1's page present (words, no numbers); the paste = the dispatch file's block at 15 anchors; Lane A's plan :182, the packet :40, plan :115 (3)(7), coder-handoff :26 re-read; Lane B's G2 :22, SoR :59, install-smoke.yml :32, MRP :325 re-read. Not re-executed — 7a's §1–§9 (beat 2); the lanes' other cites (named in the audit §4–§5).
- **The window (validated):** Thu the rig (`R5B-6:` → SHAKE → T-A → T3 → the re-mint beat → T4 → T8) · Fri the digest, the BENCH-CORE-3 card before the nightly (D-v80-7), VERIFY-72H's charter + the bench CODE lane (IR-52), the strategy pass, 7a intaken · Sat rehearsal 1's packet through the gates, the Java unit, THE HORIZON RE-CUT (`POSTURE:` — Nick's word) · Sun the S31 read, the B-1 charter, the attorney search, the K-refresh · Mon `OUTREACH:`, C-S0-1, v81's text.
- **Order:** none tonight (Nick's word). The b1 card folds onto the b2 card after the rig (one card, b1 + b2; the 7a return stays `??` until its audit).
- ctx: beat 1 · calls ≈26 · read ≈95 KB · wrote ≈45 KB · two read-only lanes (≈280K tokens outside the hub)

## 2026-09-23 (v79 beat 5 — THE v79 CLOSE: the application COMMISSIONED and the `.com` OWNED (Nick's two facts; FIN-1's page for the payment, `FIN1:` REC contribution); Nick's two steers ruled — the record cleaned before the next window; the window chosen off the short- and long-term plans; THE LONG-TERM READ: the plan of record ends at the run and the long-term plan's next gate (P6, the install flow end-to-end; P7, launch prep → Nov 25) has no rows → OR-HORIZON-UNPLANNED + THE HORIZON RE-CUT (D-v79-11: v80, Saturday, desk-only); the record cleaned (the master release plan, the milestone backlog and the research agenda bannered STALE; 32 older Open-Risk status bullets rotated verbatim, ≈26 KB off the spine; v77 b5–b7 rotated); the plan §25 = the one screen v80 boots on; THE v80 DISPATCH TEXT cut (the one deliverable: rehearsal 1's packet on the metered fleet); Wed 2026-09-23 ~16:5x CT (instrument 2026-09-23T21:51:35Z))
- **The files:** the plan §25; the DR §1e + D-v79-10..13 (the DR CLOSED); `context/handoff/2026-09-23_v80_dispatch-text.md` (LIVE); the two rotation archives; the audit `context/audits/2026-09-23_v79-b5_CLOSE_long-term-read_record-hygiene_v80-text_audit.md`; FIN-1's page outside git (`ClaudeFolder/legal/NexSys-LLC/finance/`).
- **Layer 2:** re-executed — the long-term files read by two read-only lanes with file:line cites; the hub re-read the master release plan's P4–P7 and packaging/install/launch rows (:57–63, :232–234, :322–323), the backlog's currency notes (:13–15), the strategy of record's launch-moment row (:59) and BRAND-G2-EXEC's (:22); every rotation conserved (the multiset of lines: before − moved + added = after; each moved line verbatim in its archive). Not re-executed — the root Technical Roadmap (a curriculum, not a product plan); the Founder Operating Plan's claims (FOP-1, the plan §11–§12).
- **Order:** hivemind 26 = 13 M + 13 A by explicit paths (computed from porcelain -uall inside the splice) — b3 + b4 + b5 in one card; the core block: four merged local branches, `git branch -d`. Then tonight SHAKE + 7a + FIN-1's page; v80 pasted Thursday before the rig.
- ctx: beat 5 · the session compacted once (the trademark day) · two read-only lanes (≈420K tokens outside the hub) · the hub read ≈120 KB · wrote ≈55 KB

## 2026-09-23 (v79 beat 4 — COUNSEL'S ANSWERS INTAKEN; THE FILING-TO-PUBLIC PROTOCOL CUT: counsel's reply filed verbatim — OKI and PALO "unlikely to present any significant risk", PLOKI and PALIKEA "different enough", LOKI not addressed (the identification's exclusion is the mitigation; the draft read checks it survives) → the PELTON ruling's refutable-by NOT triggered, **CLEAR stands**; the Legal Services Agreement read before signing (its prepayment line reads $350.00 for a $3,500.00 sum; the Client line runs Nick and NEXSYS LLC together; the §1(b) statement-of-use scope asked) → an email of two corrections and one question; the `.com` moved ahead of FILED (D-v79-9, REC now); **BRAND-G2-EXEC re-cut** (`context/strategy/2026-09-23_BRAND-G2-EXEC_re-cut_the-filing-to-public-protocol.md`: the draft → FILED (the `.com`, the handles dormant, the evidence log) → the receipt (the spoken side) → B-1 identity → W2 → the launch moment; the two company instruments to a business attorney); **the draft read pre-registered** (the ruling §8, ten items); two words on the desk — `OUTREACH:` REC mon (no buyer script exists; Thursday is the rig) · `HANDLES:` REC secure; the b3 card was not run → folded, one card for b3 + b4; Wed 2026-09-23 ~11:0x CT (instrument 2026-09-23T16:05:37Z))
- **The files:** the protocol; the ruling §7–§8; the DR §1c + D-v79-7/8; the audit `context/audits/2026-09-23_v79-b4_counsel-answers_G2-protocol_the-draft-read_audit.md`.
- **Layer 2:** re-executed — counsel's reply against the questions as sent; the strategy of record §2 P1–P2 and §4 (the counsel wave, B-1, the launch moment). Not re-executed — the fee agreement's text (checked against the draft's fees at the read); the handles (the sweep is FILED day's).
- **Order:** hivemind 19 = 10 M + 9 A by explicit paths (computed from porcelain -uall inside the splice) — b3 + b4 in one card. Then tonight SHAKE + 7a; Thursday the packet.
- ctx: beat 4 · calls ≈185 · read ≈370 KB · wrote ≈190 KB

## 2026-09-23 (v79 beat 3 — THE THURSDAY PACKET CUT, THE WINDOW'S ONE DELIVERABLE: `HIVE: LANDED ae3f2a1` verified; `CARRIER: READ-END · settle 60` read at the bytes — the carrier `!include`s `integrations/zigbee.yaml` (line 2), so the adoption edits that file and **the hero-less variant + live-basis need no regeneration** (both carry the include; the carrier's 1,208 B do not move — the b7 order's last clause retired); **tmux ABSENT** → installed at T-A; the Gen4s' firmware and Shelly's Zigbee switch at T-A (DEVICE-SET note 4); `~/bench.sh` → `tools/bench.sh`; the packet `context/instructions/2026-09-24_THURSDAY-ORDER_the-three-plugs_operator-packet.md` DISPATCH-READY — Half 1 whole (T0–T3 scripts: each plug's IEEE from its own `device_proposed` line, one device at a time, every write guarded, self-tested on a mock rig incl. the STOP paths), Half 2 at the re-mint beat from T3's capture; THE PRIOR-LEDGER GATE's greps in the audit §2; the settle bundles: the park runs LAST, it CONFIRMED on the three relay-ON nights → the b2 claim withdrawn, IR-51 re-cut to the confirm leg's missing precondition; the plan §24; Wed 2026-09-23 ~07:2x CT (instrument 2026-09-23T12:21:57Z))
- **The files:** the packet + `context/instructions/2026-09-24_THURSDAY-ORDER_scripts/T0.sh · T1.sh · T2.sh · T3.sh`; the audit `context/audits/2026-09-23_v79-b3_THURSDAY-PACKET_cut_carrier-read_settle-read_audit.md`; the read `…_v79-b3_carrier-read_the-Pi-at-12-08Z.txt`.
- **Layer 2:** re-executed — the carrier, the variant diff, the live-basis cmp, the symlink, the tools, the entities (6/6); the 60 parks; the suite order `constants.yaml:345–:346`; the adoption path at source; the scripts on a mock rig. Not re-executed — anything on the Pi after 12:08:46Z.
- **Order:** hivemind 14 = 7 M + 7 A by explicit paths (computed from porcelain inside the splice). Then tonight SHAKE + 7a; Thursday T-A → T3, the re-mint beat, T4 → T8.
- ctx: beat 3 · calls ≈160 · read ≈330 KB · wrote ≈150 KB

## 2026-09-23 (v79 beat 2 — THE INTAKE, THE FOUR STEERS, THE S31 READ, THE PLAN: `ERIK: SENT` (as sent: item 3, the register check before filing, removed — the hub reads the register on the draft day); `HIVE: LANDED e989681` verified (12 = 6 A + 6 M); `R5B-5:` `8/9 PASS … ON-latency 0.08s` — CONFIRMED; `READ: saved 59` → every `command-confirm-s31` bundle on the Pi read at the bytes: **22 of 59 FAIL since 07-30 (37 %)**, all `CONFIRMATION_TIMED_OUT` against the 5,000 ms on_off window, PASS latencies split (27 ≤ 0.66 s · 10 at 1.70–3.65 s), 13 no-edge A-9 reads + 3 relay-already-ON nights (the settle leg's claim refuted) — the record's count was partial; OR-S31-INTERMITTENT corrected; IR-51/52/53; the steers: 1 ACCEPT (the receipt unlocks the spoken side only; BRAND-G2-EXEC re-cut before the filing), 2 ACCEPT on the corrected premise (`HARNESS-PLUG:` open until a cause), 3 ACCEPT (the packet cut with the P4460 on the bench), 4 AGREE (the `.com` on FILED; one payment; NEXSYS LLC); the ruling's acts 2–4 amended; the box: B2 + the P4460 in, all OFF; SHAKE and 7a tonight; the plan dated to the filing; Wed 2026-09-23 ~07:0x CT (instrument 2026-09-23T12:04:18Z))
- **The files:** the S31 read `context/audits/2026-09-23_S31-command-confirm_59-bundles_first-read.md`; the audit `context/audits/2026-09-23_v79-b2_intake_steers_S31-read_the-plan_audit.md` (§3 the plan).
- **Layer 2:** re-executed — the 59 bundles' verdicts, lifecycles and A-9 reads (uncapped); on_off's window at `StandardCapabilities.java:144–:153`; the settle leg's assert; the adoption path at `ZigbeeAdoptionSlice.java:224–:279` + `ZigbeeIntegrationAdapter.java:1094–:1110`; `bench.sh:91` (`scenario` delegates to the runner). Not re-executed — the settle bundles (Block 2 copies them).
- **Order:** hivemind 10 = 8 M + 2 A by explicit paths (computed from porcelain inside the splice). Then b3: THE THURSDAY PACKET on the carrier read.
- ctx: beat 2 · calls ≈125 · read ≈270 KB · wrote ≈110 KB

## 2026-09-23 (v79 beat 1 — THE BOOT; THE INTAKE; THE PELTON RULING — CLEAR: the HEADs at the record plus the IR-40 landing (core `d22a8a4`; `CI: d22a8a4 green` on Nick's note → **the counter 13/20**; hivemind `1c81051` = v78 b5); porcelain 0 ×5; the preflight 12/12 PASS; `R5B-4:` `7/9 · FAIL command-confirm-s31` on the second night with the S31 unloaded — the pre-registration REFUTED, the v77 b7 pre-registration FIRED (the fourth FAIL since 09-12) → **OR-S31-INTERMITTENT** with its instrument (the bundle → `api-captures.json` against the journal → LINK-READ after Thursday); `R5B-5:` at 03:30 CT (the morning read); counsel's letter read at the bytes and its enclosure row by row → **CLEAR** on the letter's own sentence (D-v79-1); the filing scope Classes 9 + 42 on Nick's own words of 09-09 (D-v79-2 REC); three silences asked in the go-word email (OKI · LOKI · PALO; two pending Class 9 filings; the identifications); RS-13's pre-registration HELD; NAMING-B stood down; the letter filed verbatim; the v79 DR opened; v77 b1–b4 rotated; Wed 2026-09-23 ~00:5x CT (instrument 2026-09-23T05:55:41Z))
- **The ruling:** `context/strategy/2026-09-23_PELTON-RULING_{{NAME}}_CLEAR.md`; the act: `context/strategy/brand-program/2026-09-23_ERIK-EMAIL_{{NAME}}_filing-go-word.txt`; the audit `context/audits/2026-09-23_v79-b1_boot_intake_PELTON-ruling_audit.md`. The fence holds until the filing receipt.
- **Layer 2:** re-executed — the letter's text layer and page images; the report's 30 tables (USPTO 1 + 111, WIPO 2 + 44 rows, uncapped); its search strings; the `.com` at RDAP and at the marketplace ($2,495, unchanged). Not re-executed — CI on `d22a8a4` (Nick's word); the 09-22 bundle; the register after 09-16 (counsel, email item 3).
- **Order:** hivemind 12 = 6 M + 6 A by explicit paths (computed from porcelain inside the splice). Then b2: THE THURSDAY PACKET.
- ctx: beat 1 · calls ≈62 · read ≈150 KB · wrote ≈65 KB

## 2026-09-23 (v78 beat 5 — THE v78 CLOSE: IR-40 + IR-44 RETURNED AND ACCEPT (the core card 6 = 5 M + 1 A → 13/20 at CI); THE RECORD MADE LANE-SIZED: `IR40: RETURNED` (11,998 B) — `INDEXED BY idx_events_event_time` at `SqliteEventStore.java:237` with the plan pinned by a sweep of the bound `maxCount` (sqlite-jdbc 3.51.3 plans a bound `LIMIT ?` by its VALUE: the index at ≤ 400, the rowid walk at ≥ 500 — production's `SCAN_BATCH`; F-2's premise held exactly), semantics and the loud failure pinned; the `automation` arm at `HomeSynapseCore.java:1648`, T10; q1 flat in N (1.2 / 1.0 / 2.0 ms at 10k / 100k / 243k against the walk's 1.8 / 10.6 / 42.3; the pre-registration HELD at the IT's seeding cap; true-500k unmeasured, F-2's 90.5 ms stands); layer 2 at the bytes (the porcelain, the SQL, the tests' XML, the arm, the HINT constants 1 s / 60 s); all deviations [INFO], accepted; IR-48/49/50 opened; `coder-handoff.md` ROTATED 119,426 B → lane-sized (the entries older than ENERGY-READ and four chain segments verbatim to `archive/coder-handoff-entries-rotated-2026-09-23.md`; a lane reads the live file whole, the archive never); the two pm-lessons mints (a frozen token is a pin; a count from an uncapped listing) + the coder-lessons entry (a plan test binds the caller's values); the plan §23 STATE OF THE SYSTEM (the boot read for v79); the v79 text filed; THE DELIVERABLE NOT MET BY THIS WINDOW — Tuesday passed without a beat; THE THURSDAY PACKET is v79's, cut Wednesday for Thursday; the opinion arrived Tuesday (the class word opens v79); v78 CLOSED at five, Wed 2026-09-23 ~00:2x CT (instrument 2026-09-23T05:20:53Z))
- **Layer 2 (§1 of the audit):** re-executed — the core porcelain, the SQL and its visibility, the plan test's XML, T10's pin, the arm, the HINT constants, the return's byte count. Not re-executed — `check` (its lifecycle XML overwritten by the measurement run; CI is the gate), the measurement (its XML read as quoted).
- **Hub errors owned:** the window's deliverable missed — named at b4 as the risk and now the fact; the close is the remedy. IR-44's path was the register's, corrected b4.
- **Order:** core 6 = 5 M + 1 A (Nick's card); hivemind 15 = 11 M + 4 A by explicit paths (computed from porcelain inside the splice). Then v79 (Wed, FRESH): THE THURSDAY PACKET.
- ctx: beat 5 · calls ≈95 · read ≈330 KB · wrote ≈215 KB

## Open Risks

#### OR-S31-INTERMITTENT — `command-confirm-s31` fails intermittently with no load on the plug — [OPEN 2026-09-23, v79 beat 1; source: Nick's paste of the digest tail (`R5B-4:`); the v77 b7 pre-registration fired]
- **The risk (corrected v79 b2 from the 59 bundles on the Pi — `context/audits/2026-09-23_S31-command-confirm_59-bundles_first-read.md`):** 22 of 59 runs FAIL since 07-30 (37 %; September 8 of 24), every one `CONFIRMATION_TIMED_OUT` against on_off's 5,000 ms window (`StandardCapabilities.java:144–:153`); the load is refuted as a cause (FAILs all August with nothing on the plug); 13 A-9 reads see no edge within ~6 s; 3 see the relay already ON before the command, after a confirmed park the night before — ON between nights, outside the suite (IR-51, re-cut v79 b3). No mechanism is on record, and none before the reads (arc 28). It holds `HARNESS-PLUG:` open until a cause and keeps rehearsal 1 off the S31's ON-edge; it gates nothing Thursday.
- **The instrument, in order:** (1) the settle bundles, READ v79 b3 (53 of 60 parks CONFIRMED; the park runs LAST, after the confirm leg); (2) two more post-window reads at +30 s and +120 s (IR-52); (3) LINK-READ's reading on the S31 after its bench deploy (Fri, BENCH-CORE-3); then the window's policy (IR-53).
- **Owner:** the hub (the reads) · Nick (the rig). **Closes:** the mechanism named at the bytes, or seven consecutive 8/9 nights with LINK-READ deployed. **Milestone:** the R-5B nightly on `13d439f`. Deferred Build Gate: no.

#### OR-HORIZON-UNPLANNED — the plan of record ends at the run; the launch path after it has no rows — [OPEN 2026-09-23, v79 beat 5; source: the long-term read, the plan §25 and the v79 b5 close audit §2]
- **The risk:** the long-term plan's gate after the 72 h — P6, the install flow end-to-end (the signed package channel, a fresh Raspberry Pi OS to a live dashboard through the install command, the docs and launch content; `master-release-plan.md` rows 33–35, P6 Oct 26 – Nov 15) — and P7's launch prep (→ Nov 25) are scheduled nowhere, while the run ends Nov 2 and the strategy of record's P3 exit is the Nov-25 runway; the in-tree rename waits for the run's evidence (BRAND-G2-EXEC §1) while its fallback date is 10-31 (the strategy of record §4). What exists toward P6: the CI-built arm64 `.deb` (install-smoke) and the held card's fresh-install stop-proof.
- **Owner:** hub (THE HORIZON RE-CUT, v80 Saturday, D-v79-11) / Nick (one word: the launch posture). **Closes when:** the re-cut is on disk — every post-run row with its instrument and its date — and Nick's word is given.

#### OR-NIGHTLY-0902-S31 — the bench floor breached: the 2026-09-02 nightly read 7/9 with FAIL command-confirm-s31 after four 8/9 nights — [OPEN 2026-09-03, v61 beat 10; source: E2, Nick's paste of `~/hs-bench/digests/nightly.log`]
- **The risk:** the floor fence (8/9) is the standing evidence that the certified bench build confirms commands on real hardware; a breach is either a repeat of the 08-02 intermittent (adjudicated once: CONFIRMATION_TIMED_OUT at ~5.1 s, the app alive, the device back a minute later) or a new mechanism. No theory is on record and none is allowed before the bundle read (arc-discipline 28).
- **The ordered act (read-only; the s31/nightly HANDS-OFF fence stands until R-5):** the evidence-read session `context/instructions/2026-09-03_evidence-read_s31-nightly-0902_session_prompt.md` — five hypotheses pre-filed (H-A device silence · H-B never dispatched · H-C late confirmation · H-D infrastructure collateral · H-E the 08-02 class repeats); returns at `context/audits/2026-09-03_s31-nightly-0902_evidence-read_return.md`; the hub audits two-layer at intake.
- **Rider:** the 2026-09-03 nightly MISSED (the held card in the Pi overnight; no bundle, no digest line) — a recorded miss, not a suite defect; the bench card is back (~06:10 CT 09-03) and tonight's run fires normally; its floor line banks at the next beat.
- **Owner:** Nick (launch) / hub (audit; a row for the R-5 charter if the class repeats, its own row if a new mechanism). **Closes when:** the return is audited and the class adjudicated, and the next nightly reads 8/9.

- **Status 2026-09-20 (v77 beat 7) — THE 09-20 BUNDLE READ; THE S31 OFF AND UNLOADED.** `verdict.txt`: `FAIL — terminal phase mismatch: expected CONFIRMED, read CONFIRMATION_TIMED_OUT` (commandId `01M2YZ1RQQBTTBQTK765169TZR`) — consistent with the vacuum's trips, not proof (the bundle cannot tell a tripped relay from a radio miss). The vacuum is off the S31 and the S31's relay OFF since ~15:42Z (harmless: `nexsys-bench/scenarios/command-confirm-s31.yaml:1–:2, :61–:62` wants ONE real ON-edge). Monday's `R5B-3:` is the instrument: pre-registered `8/9 PASS · 1 SKIP(hue-online)`; a fourth FAIL → its own row with the bundle's `api-captures.json` against the journal.
- **Status 2026-09-23 (v79 beat 1) — THE FOURTH FAIL SINCE 09-12, WITH THE S31 UNLOADED → OR-S31-INTERMITTENT carries the class** (the v77 b7 pre-registration fired); this row closes with it.
- **Status 2026-09-21 (v78 beat 1) — THE 09-21 LINE READ: THE PRE-REGISTRATION CONFIRMED.** `8/9 PASS · 1 SKIP(hue-online) · fleet: 6/6 · re-seen 6 · ON-latency 0.16s` on the first night with the vacuum off and the S31 unloaded — consistent with the vacuum's overcurrent trips as the FAIL class's cause (09-12, 09-18, 09-20), not proof of it; the suite's last leg leaves the relay OFF (`command-confirm-s31.yaml:4`), so the card's state is unchanged. The instrument continues: `R5B-4:` (Tue) and `R5B-5:` (Wed) on the same state; a FAIL on the unloaded S31 refutes the vacuum cause → its own row and instrument (the bundle's `api-captures.json` against the journal). The S31 is neither pressed nor loaded before rehearsal 1's packet says so.
- **Older status (2 bullets, verbatim, in file order):** `context/handoff/archive/open-risks-status-history-rotated-2026-09-23.md` § OR-NIGHTLY-0902-S31 (rotated v79 b5).
#### OR-FAILCHAN — healthy outcomes reported through failure channels (a trust-erosion class) — [OPEN 2026-08-30, v58 beat 6; source: the R-3a hub-return §2.3]
- **The class — four instances in one day, plus the fixed KEYb sibling:** a graceful operator-requested stop exits 143 → systemd marks the unit `failed` (§6-B; root-correct fix = exit 0 on the caught-SIGTERM shutdown path — the record §6 mechanism note; a unit-side `SuccessExitStatus=143` would mask a genuine kill) · an orderly serial close emits `transport_failed` carrying F-S16’s "physical port loss" signature (§10-O) · `key_establishment_failed` for `0xFFFFFFFFFFFFFFFF` (§10-M — CAUSE OPEN; the emission is a faithful 0x009B relay, source-read v58 b6) · a device with no recorded report time renders `Current` (§10-I; read-path). **Never-false-ALIVE guards against claiming health that is not there; nothing guards against claiming failure that is not there — the second error costs operator trust as fast.**
- **Owner:** hub (the sweep WU charters at the September plan; §6-B is the first candidate) / Coder. **Closes when:** the sweep lands and each instance is re-classified or fixed, verified on a card.
- **Status 2026-09-05 (v64 beat 8) — LANDED: FIX-1a `5ed9178` (10 files, +703/−21; sample #4 — its CI verdict OWED as one line) · FIX-1b `e5fa035` (6 files, +483/−103) — Nick's word: "the `e5fa035` commit for `homesynapse-core` has all checks pass" → THE GATE IS CLEARED on FIX-1b's push run (law 16; the clearance of record). The three veto-only `workflow_dispatch` samples: not yet taken (Nick's pin: only after a push run is green — it is). `main` is GREEN for a reason at `e5fa035`; CG-1/2/3 may dispatch. **Closes when** the §6-B/EXITCODE hardware proof rides the next card session on `e5fa035`'s CI-built artifact (H8) AND the samples are not red. A later HeroLoop red re-opens the gate as FIX-2 with its mechanism named — a red on a gate run is a red, never "the instrument working" (Nick's pin).
- **Status 2026-09-06 (v65 beat 6) — CG-123 LANDED `f25291b` ON TOP OF THE FIX; ITS PUSH RUN GREEN = SAMPLE #5 ON THE FIX-1b BYTES (Nick: "All GitHub checks passed").** Sample #4 (`5ed9178`) and the three veto-only samples still owed. The §6-B/EXITCODE stop-proof rides **H8-a** (this week, `f25291b`'s CI-built artifact; the word `H8:` asked) — the risk closes when that proof is read on the card.

- **Status 2026-09-20 (v77 beat 6) — RE-SCOPED AT THE H8-a CLOSE.** Instance 1 (§6-B) and instance 5 (EXITCODE) CLOSED on the wire: two clean `success · 143 · inactive` grades on `6bd8508` (H8-a B3, R-5B B4) beside R-4c's on `a458a64`; the `SuccessExitStatus=143` form was ruled at v62 b3 with the mask caveat as a constraint. Instance 2 (the orderly serial close) is MEASURED STILL OPEN: `transport_closed_orderly` 0 on 2/2 stops, `transport_failed` 3–6 ms into the hook, the watchdog re-opening under a dying JVM — §10-O's path never runs (IR-33). The OR stays open on instance 2 only.
- **Older status (10 bullets, verbatim, in file order):** `context/handoff/archive/open-risks-status-history-rotated-2026-09-23.md` § OR-FAILCHAN (rotated v79 b5).
#### OR-BUS-SILENT-DROP — the event bus drops a delivery without a signal on four LIVE/TRANSITION paths — [OPEN 2026-09-04, v63 beat 1; source: the hub's source read, the grounding audit §4-S4]
- **The class:** `InProcessEventBus.notifyEvent` `:250` (the notified position not yet readable → `return`, offered to no one) · `liveLoop` `:480` (empty page → `continue`) · `:487–:488` ("Transient read failure — skip this position; retain mode") · `TransitionCoordinator.drainAndPromote` `:100` (empty page → `continue`); the LIVE loop processes offered positions only and never pages forward from its checkpoint, so a dropped position is lost to that subscriber permanently and the next delivery writes a checkpoint past it (`:508`). No log, no metric (the bus is SLF4J-free by design; AMD-43's seven metrics are locked). A dropped `command_issued` renders exactly as `integration.route_join_miss` — "the ledger's timeout owns the outcome" — i.e. a real automation's command silently never leaves the engine. FAILCHAN's mirror image (a failure through a silent channel) in the module the D5 floor stands on.
- **Owner:** hub (charter: FAILCHAN-FIX-1, Part A tokens + Part B DP-2) / Coder (the lane). **Commit of record:** `dc3328b` (the bytes read). **Deferred Build Gate:** YES — FIX-1a's and FIX-1b's `./gradlew check` are CI on Nick's pushes. **Closes when** FIX-1b lands with T5–T7 green (retry-then-SUSPEND; a checkpoint never advances past an undelivered matching position) and CI is green on that push.
- **Status 2026-09-13 (v72 beat 3) — BUS-ORDER-1 LANDED `a458a64`; CI GREEN (`check` and `bus-soak`); THE DEFERRED BUILD GATE RESOLVED; THE CLOSURE COUNTER RESTARTED: 1/20.** Nick's line: `a458a64` green on every action and check. Sample #16 = the `bus-soak` job on `a458a64`, green by verdict (artifact not fetched, `SAMPLES: passive`). Closes at twenty consecutive green samples from `a458a64` with kind E at zero; any red sample is read at its artifact before the count moves. The kind-E arm and the drain's two-position drop are gone from the tree; the checkpoint is a floor; the census reads the cursor.
- **Status 2026-09-13 (v72 beat 4) — AMD-101 RATIFIED; the docs card handed.** Doc 01 §3.4 now states the delivery invariant in one paragraph; INV-BUS-01 reads "exactly once and in position order" with the cursor as the drain's dedup gate; INV-PROJ-04 names BUS-ORDER-1 as the realization that makes checkpoint monotonicity hold on the LIVE path. The kinds this risk's samples can still show: `LIVE_READ_FAILED` and `NOTIFY_NOT_VISIBLE`; the closure counter 1/20 from `a458a64`; the run-hand-off face (FIX-2b-ii's tokens) stays this risk's second open question, untouched by the amendment.
- **Status 2026-09-13 (v72 beat 6) — R-4c's rig-load zero.** `bus.delivery_anomaly` 0 across the whole R-4c invocation on `a458a64` (a window, two ZDO exchanges, an adoption, four reporting devices) at 4.21 rows/min — a rig at rest with one adoption, three orders under the soak's rate. Recorded as a rig-load zero; NOT counted toward the closure counter (still 1/20). The record's `lookups=` counter counts misses only and `zdo_rsp` counts failures too (F-2, F-3); the census grammar of record since BUS-ORDER-1 is in the TR-1 audit's addendum.
- **Now (v79 b5):** the passive count is the closure counter — 13/20 at `d22a8a4` (the operator brief §HELD).
- **Older status (20 bullets, verbatim, in file order):** `context/handoff/archive/open-risks-status-history-rotated-2026-09-23.md` § OR-BUS-SILENT-DROP (rotated v79 b5).

#### OR-ADOPT-AWAKE — a device identified on the air is not thereby onboarded (F-R4c-A, R-4c 2026-09-13)
- **Opened 2026-09-13 (v72 beat 6).** The SNZB-01P was resolved over the air (265 ms), admitted as a rejoin candidate and never proposed; the SNZB-02P was proposed 1.04 s after its own `tc_join`, not after its ZDO resolution 72 s earlier. At source both admissions are the same two calls (`ZigbeeIntegrationAdapter.admitRejoinCandidate`), so the difference is downstream of admission. Explanation PROVISIONAL (the interview needs an awake device; the provocation control was contaminated). **Owner:** the hub (R-5's charter carries the counterfactual and the battery-pull test). **Closes** when R-5's first window either adopts a passively-resolved device without a join (refutes) or fails to (confirms) and the design constraint is written into the R-5/rehearsal charters: count re-seen and adopted as two numbers; never assume resolution ⇒ onboarding. Related: the null-arm block (one rig block, next packet) and the ULID audit (persisted ids outside the minting card) ride the same charter. Detail: `context/audits/2026-09-13_v72-b6_R-4c_intake_two-layer-audit.md` §3.3, §5.

#### OR-METERING-VOLUME — the run's largest event input is unmeasured and set by a constant never read — [OPEN 2026-09-17, v76 beat 2; source: Nick's v76 b2 brief §4, attacked in the b2 audit §4]
- **The class:** `ReportingConfigurator.java:71–:72` configures 0x0B04 (change 10 raw) and 0x0702 (change 5 raw) at a 5-s minimum for every ADOPTED endpoint listing them (`ZigbeeIntegrationAdapter.adoptIfAccepted` :979–:996 → `driveReporting` :1009). 0x0702 is a monotonic accumulator: at any seMetering divisor ≥ ~45,000 it reports at the 5-s floor at 80 W — 720 frames/h/device; the TR3's divisor is 3,600,000 (PLUG-DOSSIER row 29), the Gen4's unread. 0x0B04 adds 720/h only at divisor 100. Three plugs × 72 h ≈ 155k (floor) – 311k (ceiling) metering events. Today no adopted plug exists (THE ADOPTION FENCE) and nothing floods; the day ENERGY-READ adopts one, this row is live. Nothing at `3af6213` measures sustained event rate, store growth per hour or heap over hours (`BusSoakIT` is K=20 latency loops; the bench has no rate scenario).
- **Owner:** hub (ENERGY-READ's charter: IR-25 as a SIZED decision — engineering-unit thresholds chosen with the predicted frames/h per divisor stated, the max interval included; MEASURE-2 / IR-28 the rate instrument on `ZigbeeHardwareFreeRig`). **Commit of record:** `3af6213` (the bytes read). **Deferred Build Gate:** no. **Closes when** MEASURE-2's number is filed (events/s sustained for M minutes; store growth/h; heap over time) and IR-25's thresholds put the three plugs' 72-h volume inside it with the margin stated.

- **Status 2026-09-18 (v76 beat 5) — THE WIRE FACT CORRECTED; THE RATE LEG CLOSED WITH MARGIN; THE STORAGE LEG → THE SOAK.** ENERGY-READ's return R-4 (verified by the hub's own emulation, the b5 audit §0): at `3af6213` the six-byte reportable-change field was encoded with an `int` shift whose distance Java takes mod 32, so HEAD's 0x0702 row never sent 5 raw — it sent `05 00 00 00 05 00` (2.1 × 10¹⁰), a threshold no accumulator crosses, and the read-back parse folded the same way, verifying what was never sent. The flood arithmetic above stands as the INTENDED configuration; on the wire an adopted plug would have reported the 0x0702 leg only at the 3600-s maximum. Fixed in the lane (both sides widened to `long`); the uint48 width meets silicon at the first metering adoption (THE MEASUREMENT RECORD row G4-4). MEASURE-2 (IR-28): the adapter path ingests ~24,000 metering reports per second on the desk in memory (`rate=24031.1`, JIT-cold) against the run's worst case of ~1.2 per second — the rate leg CLOSES with margin. The store's growth per hour is NOT yet measured (the zigbee module cannot reach SQLite; M2-2 is a 30 B/event payload floor) — the storage leg moves to THE SOAK (wk 10-12), with a lifecycle-module MEASURE-2b (row M2-2b) if the number is wanted earlier. **Closes when** the soak's store growth is filed and IR-25's thresholds (now on the wire: 1 W / 5–600 s; 5 Wh / 5–3600 s, scaled by the read divisor) are read back verified at the first adoption.

#### OR-JOURNALD-PRIO — journald priority is blind to every application warning — [OPEN 2026-08-30, v58 beat 6; source: the R-3a hub-return §10-P; MECHANISM VERIFIED at source v58 b6]
- **The mechanism (verified):** `homesynapse.service:99–100` routes stdout/stderr → journal; no `sd_journal`/`SyslogLevelPrefix`/priority-prefix emission exists anywhere in app/core/config/distribution — every application line lands at journald’s default INFO priority. Measured: 158–201 app WARN/ERROR lines in one R-3a day; `journalctl -p warning` reported **2**, both systemd’s own. Any priority-filtering monitor sees a healthy service while warnings accumulate.
- **Fix candidates:** `<N>`-prefix the console handler (systemd’s `SyslogLevelPrefix` default honors it) or journal-native logging. Charter at the September plan / the R-10 docket. **Closes when:** app WARN+ lines carry journal priority ≥ warning, proven by a `journalctl -p warning` count on a card.
- **Status 2026-09-04 (v62 beat 2) — RULED Row 7 (a) (ADOPT-ALL-RECS, 09-03):** the `<N>`-prefix on the console handler (systemd's `SyslogLevelPrefix` default honors it). Charter queued behind FAILCHAN (same files' neighbourhood; one lane on the core tree). Closes when `journalctl -p warning` on a card counts the app's WARN+ lines.
- **Owner:** hub (the charter; Row 7's instruction authors behind CG on the one-lane rule) / Coder (the WU). **Deferred Build Gate:** none yet (no WU has run). *(Field made explicit at v64 beat 1 — preflight Check 5; the ruling and status lines above are unchanged.)*

#### OR-REHOMED-OQ — the two live items re-homed from the CLOSED open-questions channel — [OPEN 2026-08-18, v54 beat 2; created by the R-11 closure ruling (its §5.5), executed at intake]
- **(a) OQ-05-07 — Research-9 registry residuals** (deferrable; revisit at an operator-rebuild window). **(b) The 2026-06-11 NO_DIRECT_TIME_ACCESS enforcement-reach decision** — whether the arch-rule gate extends to non-app test classpaths; until ruled, §4c's posture stands (gate-enforced for production code, self-enforced convention for non-app test code — PM review is the only enforcement it has). Rides the next app-module-touching WU.
- **Owner:** hub (carry + surface in status updates). **Resolution:** (a) closes at its named rebuild window; (b) closes at the ruling inside that WU's instruction. Source of record: `context/open-questions.md` head (CLOSED-channel note) + this row.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Status 2026-08-22 (v56 beat 2) — OPEN; charter-bound.** The R10-IN-L return (`context/audits/2026-08-22_R10-IN-L_liveness-and-notify-transport_return.md` §3/§5) re-priced the M5-D matrix: FFM is final in JDK 22 but LTD-01 forbids preview features, so T-2 is an LTD-01 amendment, not a transport choice; recommendation **T-0 (stay `Type=exec` + the probe; defer M13 indefinitely; the unit's staged flip block is a live trap — rewritten as a DANGER note by R-9 rider R-9-b)**. Resolves at the R-10 charter's R-L2 word (T-0 closes this OR as DEFERRED-BY-DESIGN; any transport word re-opens it as a WU).
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.
- **Status 2026-09-04 (v62 beat 2) — RULED Row 5 (b) HOLD (ADOPT-ALL-RECS, 09-03):** the seam holds; T-0 is NOT spoken this month; the JDK-trajectory question (the Pelton-week orphan row, ORPHANS (a)) rides the R-5 charter as a carry-forward row. Status unchanged otherwise (DEFERRED-BY-DESIGN pending that row).

**Resolved/closed risk entries live at `context/handoff/archive/open-risks-resolved-2026-08-23.md` (eleven blocks, verbatim; rotated v56 b4 + v58 b6 + v62 b1 under W-HIVE-1 P1: this section carries OPEN entries only).**

## The archive map (pointer trail; maintained at each rotation — zero dangling addresses)

Any pointer of the form “pm-handoff §<date> (vN beat K)” resolves here. LIVE above: the newest ≤12 blocks (v77 b5 → the newest, at 2026-09-23) + Open Risks (OPEN entries only since v56 b4). ROTATED, verbatim:

- the resolved/closed Open-Risks ledger (nine blocks) → `archive/open-risks-resolved-2026-08-23.md` · both pre-region-cap `last-verified:` chains → `archive/chains-pre-region-cap-2026-08-23.md` (rotated 2026-08-23, v56 b4)
- v54 b7–b1 · v53 b9–b1 · v52 b7–b1 · v51 b6–b1 · v50 b7–b1 · v49 b5–b1 + the pre-rotation LV chain → `archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md`
- v48–v45 → `archive/pm-handoff-beats-v45-v48-rotated-2026-08-10.md` · v44–v41 → `archive/pm-handoff-beats-v41-v44-rotated-2026-08-06.md` · v40–v38 → `archive/pm-handoff-beats-v38-v40-rotated-2026-07-31.md` · v37–v32 → `archive/pm-handoff-beats-v32-v37-rotated-2026-07-26.md`
- earlier eras → `archive/pm-handoff-beats-rotated-2026-07-19.md` · `-2026-07-07` · `-2026-07-04` · `-2026-07-02` · `archive/pm-handoff-2026-06.md` · `archive/pm-handoff-2026-05.md` · `archive/pm-handoff-rotated-2026-06-22.md` · `-2026-06-21.md`
- sibling rotations this date: the snapshot chain/mastheads → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-08-21.md` · coder-handoff entries → `context/handoff/archive/coder-handoff-entries-rotated-2026-08-21.md` · coder-lessons 2026-07 → `context/lessons/archive/coder-lessons-2026-07-rotated-2026-08-21.md`
- sibling rotation 2026-08-26 (v57 b1, P6): the ten oldest coder-lessons entries (2026-08-02 … the second 2026-08-22, BY COUNT) → `context/lessons/archive/coder-lessons-2026-08-part1-rotated-2026-08-26.md` (verbatim; the live file holds the newest five)
- the `:8` chain's v55 b3–b6 segments → `archive/chains-rotated-2026-08-27.md` (verbatim; rotated 2026-08-27, v57 b2, under the ≤3,000 B chain cap)
- v65 b5 → v54 b8 (95 blocks; 2026-09-05 → 2026-08-21) → `archive/pm-handoff-beats-v54b8-v65b5-rotated-2026-09-07.md` (verbatim; rotated 2026-09-07, v66 b10, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v66 b6 → v65 b6 (8 blocks; 2026-09-06) → `archive/pm-handoff-beats-v65b6-v66b6-rotated-2026-09-07.md` (verbatim; rotated 2026-09-07, v67 b1, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v67 b3 → v66 b7 (7 blocks; 2026-09-07 → 2026-09-06) → `archive/pm-handoff-beats-v66b7-v67b3-rotated-2026-09-10.md` (verbatim; rotated 2026-09-10, v68 b2, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v67 b7 → v67 b4 (4 blocks; 2026-09-09 → 2026-09-07) → `archive/pm-handoff-beats-v67b4-v67b7-rotated-2026-09-11.md` (verbatim; rotated 2026-09-11, v69 b3, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v68 b6 → v68 b1 (6 blocks; 2026-09-11 → 2026-09-10) → `archive/pm-handoff-beats-v68b1-v68b6-rotated-2026-09-11.md` (verbatim; rotated 2026-09-11, v70 b3, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v69 b4 → v69 b1 (4 blocks; 2026-09-11) → `archive/pm-handoff-beats-v69b1-v69b4-rotated-2026-09-11.md` (verbatim; rotated 2026-09-11, v70 b9, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b2 → v70 b1 (2 blocks; 2026-09-11) → `archive/pm-handoff-beats-v70b1-v70b2-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v71 b3, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b3 (1 block; 2026-09-11) → `archive/pm-handoff-beats-v70b3-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v71 b5, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b4 (1 block; 2026-09-11) → `archive/pm-handoff-beats-v70b4-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v71 b6, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b9 → v70 b5 (5 blocks; 2026-09-11) → `archive/pm-handoff-beats-v70b5-v70b9-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v72 b1, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted: kept + archived body + 1 = before)
- v73 b7 → v73 b1 (7 blocks; 2026-09-13) → `archive/pm-handoff-beats-v73b1-v73b7-rotated-2026-09-14.md` (verbatim; rotated 2026-09-14, v74 b6, under the ≤12-live-blocks rule; bytes asserted)
- v72 b7 → v72 b1 (7 blocks; 2026-09-12/13) → `archive/pm-handoff-beats-v72b1-v72b7-rotated-2026-09-13.md` (verbatim; rotated 2026-09-13, v73 b6, under the ≤12-live-blocks rule; bytes asserted)
- v71 b6 → v70 b10 (7 blocks; 2026-09-12) → `archive/pm-handoff-beats-v70b10-v71b6-rotated-2026-09-13.md` (verbatim; rotated 2026-09-13, v72 b6, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted: kept + archived body + 1 = before)
- v74 b8 → v74 b1 (8 blocks; 2026-09-13/14) → `archive/pm-handoff-beats-v74b1-v74b8-rotated-2026-09-15.md` (verbatim; rotated 2026-09-15, v75 b5, under the ≤12-live-blocks rule; bytes asserted)
- v75 b4 → v75 b1 (4 blocks; 2026-09-14/15) → `archive/pm-handoff-beats-v75b1-v75b4-rotated-2026-09-18.md` (verbatim; rotated 2026-09-18, v76 b5, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before)
- v75 b8 → v75 b5 (4 blocks; 2026-09-15/16/17) → `archive/pm-handoff-beats-v75b5-v75b8-rotated-2026-09-19.md` (verbatim; rotated 2026-09-19, v77 b1, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before + the map row)
- v76 b4 → v76 b1 (4 blocks; 2026-09-17/18) → `archive/pm-handoff-beats-v76b1-v76b4-rotated-2026-09-20.md` (verbatim; rotated 2026-09-20, v77 b5, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before + the map row)
- v76 b8 → v76 b5 (4 blocks; 2026-09-18/19) → `archive/pm-handoff-beats-v76b5-v76b8-rotated-2026-09-21.md` (verbatim; rotated 2026-09-21, v78 b2, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before + the map row)
- v77 b4 → v77 b1 (4 blocks; 2026-09-19) → `archive/pm-handoff-beats-v77b1-v77b4-rotated-2026-09-23.md` (verbatim; rotated 2026-09-23, v79 b1, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before + the map row)
- v78 b4 → v78 b1 (4 blocks; 2026-09-21) → `archive/pm-handoff-beats-v78b1-v78b4-rotated-2026-09-25.md` (verbatim; rotated 2026-09-25, v80 b3, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before + the map row)
- v77 b7 → v77 b5 (3 blocks; 2026-09-20) → `archive/pm-handoff-beats-v77b5-v77b7-rotated-2026-09-23.md` (verbatim; rotated 2026-09-23, v79 b5 — the close's clean record — under the ≤12-live-blocks rule; the multiset of lines asserted: before − moved + added = after)
- The Open Risks' older status bullets (32: OR-NIGHTLY-0902-S31 2 · OR-FAILCHAN 10 · OR-BUS-SILENT-DROP 20) → `archive/open-risks-status-history-rotated-2026-09-23.md` (verbatim, in file order; rotated 2026-09-23, v79 b5; each live entry keeps its class, owner and three newest status bullets and points there)
