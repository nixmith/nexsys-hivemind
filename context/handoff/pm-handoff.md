<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-03 (v16 hub, beat 63 — Doc-18 review returned RATIFY-WITH-EDITS, audited, FOLDED (co-sign-ready); two reviewer observations refuted as index-phantoms; profile-registry research lane authored; fleet: core 6ea6912 / docs 78e0c5c+fold-uncommitted / hivemind c54a058+beat-63 / bench 5ceff3b / skills ae434ca). Prior stamp chain preserved in archive/pm-handoff-beats-rotated-2026-07-02.md frontmatter.
-->

# PM Session Handoff

## 2026-07-03 (beat 63 — v16 hub) — the Doc-18 review return: audited ACCEPT, two observations REFUTED, B-1/N-1..N-4 FOLDED; the profile-registry research lane authored; co-sign package open

**The return** (`context/audits/2026-07-03_Doc18_independent_DOCS_Review_Return.md`, on disk, untracked — the reviewer's write-isolation held: exactly 1 file): verdict **RATIFY-WITH-EDITS**. Its substance is excellent — both charter-citation corrections adjudicated RIGHT from primary source (incl. resolving DECIDE-04 further than the hub had: it lives as an `IntegrationFactory.java` javadoc tag, lines 19–23, not a governance id); the full citation table (18 governance ids + 7 core-source claims) all VERIFIED with zero MIS-CITED/ABSENT; rulings-fidelity PASS on all four beat-57 decisions; all seven §4 rows non-precluding; RESTATES-tag hygiene clean; all six anti-requirements held — the reviewer specifically confirmed the hub's softening of the charter's "guaranteed safer" superlative. 18 L-n citations spot-followed into the dossier, all faithful.

**Layer-2 audit (the hub's own source checks on the return's finds):** (1) **INV-CE-05** (§8 line 528) and **INV-SE-04** (§10 line 605) both re-verified in the register — the quoted text is exact, and both distinctness arguments hold: CE-05 binds the extension→core stability relationship (isolation/quotas/version-pinning/graceful-degradation) with NO clause requiring core to boot with the extension set empty; SE-04 binds privilege-minimality, not declaration-completeness. **B-1 ACCEPTED — both mints survive, strengthened.** (2) **TWO baseline observations REFUTED** (the FE-1b false-observation class — this time caught at the audit): (i) the return's "pending website/-only Commit A is still dirty" is FALSE — `git show 78e0c5c` enumerates **27 paths, 26 under website/** (the Commit-A content landed in the beat-62 merged sweep; the hub verified in the git OBJECT, not any porcelain); (ii) "4 pre-existing hub-modified spine files, deliberately not staged" is FALSE as a live fact — those four landed in `c54a058` (beat 62; Nick's host count held silently). Both observations trace to the **truncated-`.git/index` read phantom** (bench precedent, beat-43-confirmed class): the docs VM porcelain this session even shows `RD …package-lock.json -> …package-` — a rename target cut MID-FILENAME, the unambiguous signature. Neither false observation contaminated the review's verdicts (the doc content reviewed ≡ committed content); both are corrected HERE so they cannot re-enter the spine. Env-model note: the §2 phantom family now spans worktree-file tails AND `.git/index` reads on BOTH repos in one session — lanes must never trust VM git status (the new research-lane prompt says exactly this).

**Folds executed (docs working tree, 7 edits — HELD UNCOMMITTED for the Lock commit, the Doc-17 "fold commits at Lock" precedent):** B-1 into EXT-INV-1 (INV-CE-05 as fourth parent + the reverse-direction novelty clause) and EXT-INV-2 (INV-SE-04 as fourth parent + the least-privilege⊥declaration-completeness orthogonality) + both ids into the header Dependencies line; N-1 (AMD-55-INV-01 re-glossed as default-method backward-compat, AMD-55 §2.4, truthful-absence tie-in); N-2 (the enforcement-scoping co-sign note inline at EXT-INV-2, AIOT-INV-1-precedent framed); N-3 (INV-CS-03 at §4 row 6's config-schema clause; INV-CE-04 at §3.5(d)); N-4 (the template-exclusion footer sentence); status line → **DRAFT — REVIEW FOLDED** with the full review-chain record. **F-1** (IntegrationContext javadoc documents scoping/write-only as enforced while enforcement is the unbuilt rung — Doc 18's "unscoped/full" claim is the accurate one) → routed to the **R-4 backlog rows** at the next backlog edit. **Queued skill touch-up (next skills pass, non-urgent):** `project-manager/references/constraint-enforcement.md`'s at-a-glance LTD table is a lossy summary (its LTD-16 row) — the reviewer correctly bypassed it for the register; fix the row when the skills next open (Check 9 consequence noted).

**Profile-registry research lane AUTHORED** (`context/handoff/2026-07-03_device-profile-registry_research_session_prompt.md`): the hub CONCURS with the research-first recommendation — M9.3 freezes the registry (one-way), Doc 18 §3.5 Locked the constraints but the design space (matching model, interview FSM, reporting robustness, format versioning) is un-researched to project standard; a freeze on incomplete competitive knowledge is the expensive mistake class. The lane extends the six survey questions to **ten** (hub vectors: **Q7** corpus update-cadence/distribution at thousands-of-devices scale — profile updates must not require core releases, inside LTD-16 + seam-6; **Q8** contributed-profile proof discipline — L-22 staging-area concretized, bench-fixture-replay as the acceptance gate; **Q9** sleepy/battery interview robustness — the BEST_EFFORT confirmability leg, Wave-2's sleepy sensor as validation vehicle; **Q10** identity/rejoin/re-interview stability — OTA fingerprint drift, pin-vs-rematch, DP-B/DP-F adjacency). Internal grounding mandated and ordered BEFORE the external survey: bench corpus (= the M9.3 acceptance spec) · Doc 08 §3.3–3.7 as AMD-96/97-folded · Doc 02 §3.12 adoption-boundary mapping · the frozen Phase-2 `DeviceProfile`/`DeviceProfileRegistry` shapes (any deviation = a NAMED AMD need) · Doc 18 §3.5 validated-never-relitigated · never-false-CONFIRMED inviolable in the Q3 degrade ladder. Read-only; single-artifact return; the current phantom hazards stated. **Launchable NOW, parallel with the dossier-content lane** (write-disjoint; both read-only vs each other).

**The co-sign package (Nick — two decisions, one turn):** (1) **OQ-1, the DP-18-A deprecation floor:** proposed **N = 6 release cycles with a Repairs-class automated-migration surface** (L-11 = HA ADR-0021 verbatim ≥6-cycles+Repairs; never below LTD-16's one-major floor; the reviewer evidence-checked the citation ✓). (2) **N-2, EXT-INV-2 enforcement scoping:** hub recommends **mint UNSCOPED with the enforcement-arrival note as folded** — the AIOT-INV-1 precedent is exact (its E3 actor-axis CI test also lands post-MVP; minting with a named enforcement point beats scoping-down a constitutional statement); rung 2 is that named point and already lives in the R-4 rows. On the word: the hub executes **LOCK + the consolidated docs pass in ONE watermark advance** — Doc 18 status→Locked + EXT-INV-1/2 registration (new `INV-EXT` category, §50 new-doc-Lock precedent, watermark-neutral) + the queued batch (DP-B/DP-C/DP-D one-liners · A4.5 Doc-15 §3.4/§6 note · strategic-context-map refresh incl. the four wipe-test files · the Connect reword [DP-18-C(i)] · R4 >v14-band + R5 §3.13-mismatch Doc-08 currency notes · Doc-08 W6 jSerialComm touch-up · D-4 R15 Six_Battlefields fold gap · the FE-1b-finding correction note) — docs + strategy + hivemind commits staged THEN with exact counts.

**Nick, in order:** (1) hivemind commit 1 — the review return: `git add context/audits/2026-07-03_Doc18_independent_DOCS_Review_Return.md` → `git commit -F ../_scratch/2026-07-03_Doc18-review-return_commit-msg.txt` (**stages exactly 1**; the reviewer's own staged message). (2) hivemind commit 2 — beat-63: **stages exactly 3** (`context/status/PROJECT_SNAPSHOT.md` · `context/handoff/pm-handoff.md` · `context/handoff/2026-07-03_device-profile-registry_research_session_prompt.md` new), message `_scratch/2026-07-03_hivemind_beat-63_commit-msg.txt` → push. **Ignore any extra VM-phantom flags; your HOST porcelain is truth — if IT shows more than these sets, STOP.** (3) **Launch the profile-registry research lane** (fresh Cowork on its prompt). (4) Dossier-content lane at leisure (gate met since beat 62). (5) **The two co-sign decisions** (OQ-1 N · N-2 scoping) — on your word the Lock executes here. (6) NO docs commit yet (the fold rides the Lock commit). (7) Standing: Dependabot 5 (1 critical, overdue) · ci.yml + install-smoke glance `6ea6912` · telemetry disable · CC allow-list before M9.3 · website.yml · LICENSE-flip · hashed-TCLK · frontend.yml `cf3733e` glance · Wave-2 watch · Node-20.

**Hub next:** on co-sign → Lock + docs pass (the beat-64 arc; staged commits with exact counts) → audit the two research-lane returns as they land (layer-2 incl. observations) → **M9.3 JIT authoring** on Lock ✓ + profile-dossier ✓ + allow-list ✓ (grounding-subagent; both graph knobs pinned; the coder-handoff NEXT-WU inheritances + zigbee MODULE_CONTEXT gotchas + the Q1–Q10 dossier constraints; queue the classifier cause-walk + hashed-TCLK for Nick in the M9.3/M9.4 window) → M9.4 → bench acceptance (+FE-7, DP-F re-pair rehearsal) → 72 h soak (NQ-6 during).

## 2026-07-03 (beat 62 — v16 hub) — fleet landed (docs `78e0c5c` merged sweep + hivemind `8acd72a`); zero-JS gate PROVEN; website note archived; review lane launching

**Nick's transcript, read as primary evidence:** (1) The docs `index.lock` never obstructed (no rm needed — either cleared earlier or expired; not asserted further). (2) Host gate: `npm ci` 277 pkgs (audit: **2 vulns, 1 low + 1 HIGH — unchanged, BUILD tree only**; ledger item stands) → prebuild "shared design sources OK" → Astro **6/6 pages** in 1.26 s → **postbuild "zero-JS gate OK (no script bundles emitted)"** — the beat-60 Windows `fileURLToPath` fix is PROVEN on the host; the gate of record ran BEFORE any commit. (3) `npx astro telemetry disable` did NOT appear in the transcript — stays open (one-time hygiene).

**The commit deviation, recorded (truth-hierarchy: record the actual, not the plan):** instead of the staged two-commit split (Commit A = 26 explicit paths, then Doc-18 DRAFT = 1), Nick ran **one `git add -A` sweep committed under the Doc-18-DRAFT message** → docs **`78e0c5c`, 27 files changed, 6515 insertions** — the create-list in his transcript enumerates design/18 + developers.md + the 19-file site tree; 6 M + 21 create = **27 = 26 + 1, arithmetic exact**. Content assessment: NOTHING unintended landed (pre-commit porcelain was exactly the audited set; `website/site/.gitignore` keeps dist/node_modules out; no dist/ or junk in the create list). The only cost is provenance: `78e0c5c`'s message says "stages exactly 1" while carrying 27, and the website set's explicit-list commit message (staged `_scratch/2026-07-03_docs_website-lane_commit-msg.txt`, now DEAD — do not reuse) never entered history. The authoritative content record for the website set remains the website-lane return §10 + this note. No corrective action recommended (a history rewrite would cost more than the mismatch); future archaeology: treat `78e0c5c` as "website skeleton + Doc-18 DRAFT." Pushed `1509b34..78e0c5c`. Hivemind `8acd72a` landed exactly as staged (6 entries, rename 100%, 109 insertions — consistent with single-line paragraphs).

**Executed this beat:** Check-8 archival — the website-lane note moved below the separator with a beat-62 SERVED annotation (heading suffix + hub annotation + body verbatim — the beat-59 M9.2 pattern); ACTIVE: 0; frontmatter restamped (still never quoting the separator string). Snapshot + this file restamped to beat 62.

**Verification-pass findings (both closed in-beat):** (1) **Backlog VM-phantom, diagnosed + dismissed:** post-write VM porcelain showed a 4th modified path (`phase-3-milestone-backlog.md`, untouched this beat). VM diff signature = the env-model §2 truncated-tail phantom exactly — last surviving line cut MID-PHRASE ("…The Master Release Plan", no trailing newline) + the whole `## Notes` block "deleted." **Host tail read via file tools: INTACT** (line 127 complete, Notes block complete, 137 lines ≡ HEAD). Read-side lag only; nothing staged from the VM view; Nick's host porcelain should show exactly the 3 beat-62 paths — the STOP rule stands if it doesn't. (2) **Legacy second `## Archived` heading DE-BAITED (cross-agent line ~315):** a 2026-06-12 rotation relic — a second separator heading whose caption falsely claimed "only entries above this separator are active" (untrue since the beat-57 relocation) and which made every `## Archived` anchor two-hit (the beat-59 splice class, latent). Rewritten to a plain-text italic rotation marker (host-side, line-anchored on its unique caption); the file now has exactly ONE separator heading. Also noted: the first verification awk false-alarmed on CRLF (`$`-anchored pattern vs `\r\n` line endings) — line-number comparison is the CRLF-safe order check; worth remembering, not worth a lessons entry.

**Live consequences:** (a) The **dossier-content lane's HARD START GATE is MET** — the three stubs are tracked + clean at `78e0c5c`; the lane is launchable NOW and is write-disjoint from the review lane (3 docs pages vs 1 hivemind audit file) — Nick launches both in parallel whenever he likes. (b) The **DOCS-review lane now reads Doc 18 from a pinned committed baseline** (`78e0c5c`), strictly better than the working-tree read its prompt tolerated; its Commit-A baseline-shift clause is moot. (c) Ledger after this beat: ci.yml + install-smoke glance on `6ea6912` · **Dependabot 5 (1 critical, overdue)** · telemetry disable · website.yml adopt-now · LICENSE-flip · hashed-TCLK · OQ-1 N (co-sign) · CC allow-list before M9.3 · frontend.yml `cf3733e` glance · Wave-2 watch · Node-20 majors. Mirror sync CLOSED (Check 9 PASS, beat 61).

**Beat-62 hivemind commit (host-side):** stages exactly **3 paths** — `context/status/PROJECT_SNAPSHOT.md` · `context/handoff/pm-handoff.md` · `context/handoff/cross-agent-notes.md`. Message: `git commit -F /c/Users/Nick/Desktop/Code/ClaudeFolder/_scratch/2026-07-03_hivemind_beat-62_commit-msg.txt`. §10 audit: all three trace to this beat (beat entries + restamps + the served archival); no hazard classes; no server, no tokens. Host porcelain is truth — anything beyond these 3, STOP and ping the hub.

**Hub next:** the review return arrives → two-layer audit (deliverable claims AND observations that route actions — including its adjudication of the two charter corrections, re-verified against LTD/AMD source myself) → fold edits → Nick co-signs (rules OQ-1 N, proposed 6) → **LOCK + the consolidated docs pass in ONE watermark advance** (EXT-INV registration · DP-B/C/D one-liners · A4.5 · map refresh incl. the four wipe-test files · Connect reword · R4/R5 Doc-08 notes · W6 touch-up · D-4 R15 fold · FE-1b-correction note) → **M9.3 JIT authoring** (CC lane; allow-list verified FIRST; grounding-subagent; both graph knobs pinned; the coder-handoff NEXT-WU inheritances + zigbee MODULE_CONTEXT gotchas; queue the classifier cause-walk candidate + hashed-TCLK for Nick in that window).

## 2026-07-03 (beat 61 — v16 hub LAUNCH) — hygiene + preflight reconciled; DOC 18 AUTHORED (DRAFT); the two lane prompts authored

**First-act hygiene (all four, in order):** (0) restart-resume clean — porcelain matches the v16 masthead exactly (hivemind beat-60 LANDED as `c0648b0`, 7 paths; docs dirty ≡ the 26-path Commit-A set, HEAD still `1509b34` → **Commit A PENDING**; core/bench/skills clean at baseline); no ghost writes, no failed anchors (one Edit bounced on the READ-TRACKER, not on content — re-read confirmed anchors byte-identical, applied clean). (1) Ground (env-model, the cold-boot trio, truth-hierarchy) + **11-check preflight: STALE-benign, zero CONFLICTED** — Check 4 STALE (backlog lagged M9.2-DONE) → **reconciled this session** (v16-launch currency note + M9.2 row flip to ✅ DONE `6ea6912`); Check 10 STALE-benign (map refresh rides the Doc-18 docs pass, standing ruling); **Check 9 PASS — both mirrors byte-identical AND the beat-58 additions (seam-map et al.) verified present IN the mirror — Nick's sync ran; that ledger item CLOSES.** Check 6 verified directly (coder-handoff refuse-to-close names M9.3, Doc-18-Lock-gated, DP-B line already corrected). (2) v15 prompt → `archive/`. (3) Check-8: website-lane note stays ACTIVE (correctly — its residuals are Nick's Commit A); archive it at the beat that confirms Commit A landed.

**DOC 18 AUTHORED (Mode 1, the session's product):** `homesynapse-core-docs/design/18-extension-and-plugin-architecture.md` — DRAFT on disk, **uncommitted by design** (its commit is staged for Nick to run AFTER Commit A: stages exactly 1, message `_scratch/2026-07-03_docs_doc18-draft_commit-msg.txt` — the Doc-17 precedent of landing the DRAFT pre-review so the review lane reads a pinned baseline). Shape: Doc-17 mold §§0–7 (charter §1 map); seven seams as §4 rows with per-row non-preclusion arguments; §3.5 namespace/identity governance carrying the four M9.3-binding constraints (publisher-scoped two-level identity + reserved bare namespace; collision ruling = HA-shape shadow-with-consent + UI flag + core-wins-brand; fingerprint-priority + rebrand mapping; Zigbee-scoped-with-general-seam-note registry posture); EXT-INV-1/2 through the minting test with CI-testability stated (recommend MINT at Lock as a new INV-EXT category, §50 watermark-neutral precedent); three principles kept unminted with the INV-GA-02/AIOT-INV-2/3 justifications; the honesty rule as doc text (§2 P3, §3.3, §4 rows 4/7); DP-18-C stated as a standing CONSTRAINT (§2); §4 row 6 cross-cites INV-PD-08 (signing restated, Doc-17-owned) — a self-review catch. **Authoring corrections vs the charter (both source-verified, both flagged in-doc for the review to adjudicate):** (1) EXT-INV-1's static-composition parent is **LTD-17** — the charter's "DECIDE-04/LTD-16" was wrong on both halves (LTD-16 = semver/API; DECIDE-04 greps to nothing in `governance/`); (2) the charter's "AMD-56 error-code registry" parent for EXT-INV-2 is wrong — **AMD-56 = Exception Classification (Auth Failed)** (verified at `design/amendments/`); re-grounded on the `IntegrationDescriptor` freeze + AMD-55-INV-01. **OQ-1 (BLOCKING for Lock): deprecation-floor N — PROPOSED 6 release cycles + Repairs-class surface** (L-11/HA ADR-0021; never below LTD-16's one-major floor); Nick quantifies at co-sign. OQ-2 (LTD-17 amendment shape — proposed, not assumed) + OQ-3 (marketplace-floor ops) NON-BLOCKING.

**Review lane AUTHORED:** `context/handoff/2026-07-03_Doc18_independent-DOCS-review_session_prompt.md` — fresh Cowork, PM skill Mode-1 review discipline, **review-ONLY** (single-artifact return at `context/audits/2026-07-03_Doc18_independent_DOCS_Review_Return.md`; zero docs-repo writes; the dirty-tree + pre-authorized-Commit-A-baseline-shift hazards stated). Five-part charge: charter fidelity + the two corrections adjudicated FROM SOURCE (not from either party's claim) · EXT-INV minting-test verdicts · full citation table (every id + every core-source claim VERIFIED/MIS-CITED/ABSENT + §17 scan for uncited relevant invariants) · rulings-fidelity of the four SETTLED decisions vs the beat-57 record · per-row non-preclusion + §3.5/M9.3 coherence + template compliance on the Doc-17 precedent. Verdict scale RATIFY-TO-LOCK / RATIFY-WITH-EDITS / REVISE with an individually-actionable edit list.

**Dossier-content lane AUTHORED:** `context/handoff/2026-07-03_dossier-content-lane_session_prompt.md` — nexsys-frontend skill; the three stubs → config-superiority reviewer grade (the W-5 publish gate's content residue); the in-file Increment-2 planning blocks are the per-page briefs, their receipt obligations discharged with LIVE-FETCHED dated citations (HA stored_traces baseline re-verified; Insteon 2022 with CEO-estimate attribution; the REC-142 "to our knowledge" receipts); claim→truth register per page (the website-return §4 shape); publish gates held (no LICENSE claim, W-4 embargo, D-FE-6 hero-showcase gate, W-11 tokens, Doc 17 §7-6 boundary); scaffolding removed after fold. **HARD START GATE (hub catch): docs Commit A must LAND before the lane edits — the three stub pages are themselves inside Commit A's 26-path set**; "parallel-safe vs everything" held at lane level but NOT at file level. Write surface: exactly 3 docs paths + 1 hivemind return. Micro-finding recorded: "PG-1" (the v16 brief's tag) doesn't grep-resolve in the master plan or amendment — W-5 is the real gate id; the prompt pointer-forms it.

**Nick, in order:** (1) `rm homesynapse-core-docs/.git/index.lock` if still present → native website gate (`cd website/site && npm ci && npm run build`; expect 6 pages + "zero-JS gate OK" on the fixed script). (2) **Docs Commit A** — explicit list, EXACTLY 26 paths (unchanged; the untracked `design/18-*.md` + the archived-prompt rename are NOT in it). (3) **Docs Doc-18-DRAFT commit** — stages exactly **1** path (`design/18-extension-and-plugin-architecture.md`), message `_scratch/2026-07-03_docs_doc18-draft_commit-msg.txt`. (4) **Hivemind beat-61 commit** — stages exactly **6 entries** (3 modified: PROJECT_SNAPSHOT · pm-handoff · phase-3-milestone-backlog; 2 new: the two lane prompts; 1 rename: v15 prompt → archive/), message `_scratch/2026-07-03_hivemind_beat-61_commit-msg.txt`. §10 audit run: every path traces to this beat; no runtime state/build output/secrets; no server ran, no token minted; nothing stages from `_scratch/`. **Host porcelain is truth — if it shows anything beyond these sets, STOP and ping the hub.** (5) **Launch the DOCS-review lane** (fresh Cowork on its prompt — the critical-path item; M9.3 waits on the Lock). (6) Dossier lane AFTER Commit A (its own start gate enforces this). (7) ci.yml + install-smoke glance on core `6ea6912` (residual). (8) **Dependabot 5 (1 critical) — overdue since 06-27.** (9) Rulings at leisure: website.yml adopt-now (recommended) · LICENSE-flip timing · hashed-TCLK (M9.4 window) · **OQ-1 deprecation-floor N at co-sign (proposed 6)**. (10) CC allow-list fix stands verified-needed BEFORE M9.3 dispatch (`"Bash(./gradlew:*)"` in core `.claude/settings.json`, restart the CC session).

**Hub next:** two-layer audit the review return on arrival (fidelity of its source checks INCLUDED — audit observations that route actions) → fold edits → Nick co-sign (OQ-1) → **LOCK + the consolidated docs pass in ONE watermark advance** (EXT-INV registration [§50-precedent, new INV-EXT category] · DP-B/DP-C/DP-D one-liners · A4.5 Doc-15 §3.4/§6 backup-key note · strategic-context-map refresh incl. the four wipe-test files · Connect reword [DP-18-C(i)] · R4 >v14-band + R5 §3.13-mismatch Doc-08 currency notes · Doc-08 W6 jSerialComm touch-up · D-4 R15 Six_Battlefields fold gap · the FE-1b-finding correction note) → **M9.3 JIT** (grounding-subagent first; pre-verification pins BOTH graph knobs; inheritances per the coder-handoff NEXT-WU pointer + zigbee MODULE_CONTEXT; queue for Nick in that window: the classifier cause-walk-for-PIE-only candidate + hashed-TCLK). Then M9.4 → bench acceptance (+FE-7) → 72 h soak. Wave-2 unboxing runbook rides arrival (bench-first; MG24 untouched).

## 2026-07-03 (beat 60 — v15 hub) — the v15 CLOSE: v16 AUTHORED; the zero-JS gate's Windows path bug fixed pre-Commit-A

**Beat-59 landed `79659f6`** (5 paths exact). **Nick's host website-gate run:** `npm ci` (277 pkgs; audit: 2 vulns, 1 low + 1 HIGH — BUILD-tree only, the shipped site has zero runtime JS; leisure bump or fold into the dossier lane's window) → prebuild preflight OK → **Astro built 6/6 pages** → **postbuild zero-JS gate FAILED with "dist/ not found"** immediately after the build wrote dist/. Root cause (hub-verified in source): `check-zero-js.mjs:10` used `new URL(...).pathname`, which yields `/C:/Users/…` on Windows — `readdirSync` can't open it; the gate failed CLOSED (correct posture) with a misleading message; the lane's in-lane Linux run masked the platform bug. **Fixed:** `fileURLToPath(new URL('../dist/', import.meta.url))` + a provenance comment. The script is ALREADY one of the 26 Commit-A paths — **count unchanged**. Nick: re-run `npm run build` (expect "zero-JS gate OK — no script bundles emitted") → then Commit A. Also queued: `npx astro telemetry disable` (one-time; build-time telemetry, keeps the no-tracking posture coherent).

**v16 AUTHORED:** `context/handoff/2026-07-03_PM-mission-control_v16_orchestrator_session_prompt.md`. First-act 0 = restart-resume; hygiene includes archiving THIS (v15) prompt + serving the website-lane note once Commit A is confirmed. Charge: **Doc 18 (Mode 1) → DOCS review → co-sign → LOCK before M9.3** with the consolidated docs pass · the dossier-content lane prompt (W-5/PG-1) · M9.3 JIT on Lock (CC lane — **verify the allow-list fix BEFORE dispatch**; inheritances enumerated in the prompt §2.4) · M9.4 → bench acceptance (+FE-7) → 72 h soak. Nick's ledger carried in v16 §4 (CI glance `6ea6912` · Commit A · mirror sync · Dependabot 5 · site npm audit · telemetry · allow-list · website.yml · LICENSE-flip · hashed-TCLK · Wave-2 · Node-20 · frontend.yml residual).

**Why v15 closes here (capture-then-execute):** the arc since launch — hygiene/preflight, M9.2 authoring, four rulings, the M9.2 double-gate arc with the checked-PIE sweep and the hardening pass, the skills folds, the website audit with the dist refutation, the splice repair, the Windows gate fix — is deep; Doc 18 deserves a fresh context. Everything is banked: 4 hivemind commits (`e81a22a`→`79659f6`+this beat), core `6ea6912` pushed, zero unresolved defects, zero open deferred gates, all seven M9.2 [REVIEW]s + all four strategic rulings on record.

**The wipe-test migration set (Nick's directive — make a memory-less Claude boot at full speed from `ClaudeFolder/` + skills alone):** FOUR new versioned files + one unversioned pointer. `nexsys-hivemind/START_HERE.md` (the cold-boot front door: role → skill → newest orchestrator prompt → preflight → the operating-context reads; Nick's copy-paste launch lines for all three roles) + `context/process/working-with-nick.md` (the operator contract: environment, interaction patterns [exact counts, evidence+recommendation+default, veto-or-default, full-transcript evidence, second-checking both directions], cadence, risk posture, anti-patterns) + `context/process/infrastructure-map.md` (remotes incl. the nexsys-io/nixmith account split, CI inventory, Dependabot state, toolchain, bench hardware, permission surfaces) + `context/process/decision-rationale-index.md` (every load-bearing ruling: one-line WHY + pointer; the beat-57 verbatim-rationale set indexed; PENDING items fenced). Plus `ClaudeFolder/START_HERE.md` (root pointer — UNVERSIONED, outside all repos; survives the folder-zip, hivemind copy is canonical). v16's ground step now names the trio. The strategic-context-map refresh (riding Doc 18's docs pass) should add all four.

**Beat-60 hivemind commit (host-side):** stages exactly **7 paths** — `context/status/PROJECT_SNAPSHOT.md` · `context/handoff/pm-handoff.md` · `context/handoff/2026-07-03_PM-mission-control_v16_orchestrator_session_prompt.md` (new) · `START_HERE.md` (new, repo root) · `context/process/working-with-nick.md` (new) · `context/process/infrastructure-map.md` (new) · `context/process/decision-rationale-index.md` (new). Message: `git commit -F /c/Users/Nick/Desktop/Code/ClaudeFolder/_scratch/2026-07-03_hivemind_beat-60_commit-msg.txt`. Host porcelain is truth. (The ClaudeFolder-root pointer file is deliberately outside every repo — it will show nowhere in porcelain.)

## 2026-07-03 (beat 59 — v15 hub) — M9.2 LANDED `6ea6912`; website-lane return AUDITED ACCEPT (dist refutation verified; license publish-gate real); the cross-agent splice repaired

**M9.2 closed on main:** clean-tree `check` GREEN ×2 (152 tasks) → `6ea6912` (39 files/6463 insertions — the audited arithmetic exact) → pushed. Beat-58 hivemind landed `e96fe83` with ONE deviation: the `-A` sweep took 9 paths, the 9th being the website-lane return (untracked, pre-audit). Reconciled: the return is a legitimate artifact, now audited (below); the lane's staged hivemind Commit B is WHOLLY PREEMPTED (both its paths rode `e96fe83`) — Nick must not run it. **Dependabot escalated: the `6ea6912` push nag enumerates 5 (1 critical, 1 high, 3 moderate)** — M9.2 added no npm surface; candidates unchanged (web-ui package-lock → frontend micro-WU; or actions).

**Website-lane audit (ACCEPT — layer-2 source verifications):** (1) **§6 dist-refutation CONFIRMED** — `web-ui/dashboard/.gitignore:3` (`dist/`) since `b296e76` whose own commit message says "git add respects the .gitignores"; root `.gitignore:118` unanchored `dist/`; zero tracked files under dashboard dist. **FE-1b's "un-ignored dist/" finding → CLOSED-REFUTED** — a false lane observation that survived the beat-54 audit because the hub source-verified deliverable claims but not supporting OBSERVATIONS that ROUTE actions (audit-culture lesson; the website lane's second-check caught it). The rider I authored into the website brief §2.5 carried the false premise. (2) **LICENSE = proprietary placeholder verified** → the developer page's Apache-2.0 claim is correctly PUBLISH-GATED; **the LICENSE flip is a NEW Nick standing item (strategic timing; blocks developer-page publish, nothing else).** (3) Write isolation EXACT (docs porcelain 8 lines ≡ 26 paths, 0 outside `website/`; ZERO core writes; site tree 19 files exact). (4) Content: developers.md = Register A, L-12 spine, 13 provenance rows all consistent with the spine record, guardrail self-audit clean; index.md = D-4-exact (reliability lead, W-4 plug-and-play embargo held, segment fork as one section, publish-blocked mechanics TODO-fenced). (5) The lane's 3 prompt-corrections ACCEPTED — incl. that my brief's DP-18-A line was stale against the same-day beat-57 rulings (authored pre-rulings; the lane's currency check was right). (6) Astro ^5.18/Vite-6-sameness pin + no-preact-yet + defaults §9 (1)–(9): ACCEPT as recorded/revisable. (7) website.yml (docs repo, build-only, ~20 lines): hub recommends ADOPT-NOW; Nick rules. (8) Next-lane: hub CONCURS with (1) dossier content build-out (W-5/PG-1 = the binding gate-4 content gap; parallel-safe vs M9.x). Env-model gained the esbuild-SIGSEGV quirk + the /tmp-md5-copy + wasm-shim-on-copy pattern (the lane's green ≠ gate of record; Nick's native run is).

**Cross-agent splice (found via a failed beat-59 edit anchor — the §11 tripwire working):** the CC lane's M9.2 note was inserted at the FIRST occurrence of the archive-separator heading string — a PROSE MENTION inside the file's frontmatter comment (env-model §5: match headings line-anchored; prose mentions match first). It rode `e96fe83` spliced (the note was invisible — inside an HTML comment). Repaired at beat 59: clean frontmatter that no longer quotes the separator literally (structural de-baiting), one canonical copy of the note below the separator with a served annotation (its DP-B line was stale — confirmed beat 57), ACTIVE=1 (website note; Nick residuals). Third anchor-class incident today (hub replace_all; reviewer claims; now the lane's insert) — the existing pm-lessons entry covers the class; no new entry, the structural fix is the fold.

**Nick, in order:** (1) `rm C:\Users\Nick\Desktop\Code\ClaudeFolder\homesynapse-core-docs\.git\index.lock` (verified present, 0-byte, VM-created). (2) Native website gate: `cd homesynapse-core-docs/website/site && npm ci && npm run build` (expect: preflight OK → 6 pages → zero-JS gate OK). (3) Docs **Commit A** per the return §10 (explicit list, EXACTLY 26 paths; message staged `_scratch/2026-07-03_docs_website-lane_commit-msg.txt`). **Skip Commit B.** (4) Hivemind beat-59 commit (staged below, 5 paths). (5) ci.yml + install-smoke glance on core `6ea6912`. (6) **Skill mirror sync** (Check 9 STALE: project-manager + coder — from beat 58). (7) **Dependabot triage** (5 enumerated — overdue). (8) Rulings at leisure: website.yml adopt-now · LICENSE-flip timing · hashed-TCLK (M9.4 window). (9) CC allow-list fix before M9.3 (`"Bash(./gradlew:*)"` in core `.claude/settings.json`).

**Hub next (capture-then-execute):** v15 has carried launch + M9.2's full double-arc + the hardening review + the website audit — it is at depth. **Author v16 NOW; Doc 18 (Mode 1) is v16's first substantive act** (all DP-18 rulings on record; charter = authority; Lock BEFORE M9.3; the docs pass carries: DP-B/DP-C/DP-D one-liners · A4.5 Doc-15 note · strategic-map refresh · the Connect reword (DP-18-C(i)) · R4 >v14-band + R5 §3.13-mismatch Doc-08 currency notes · the D-4 R15-fold gap · the FE-1b-finding correction note). Then: the dossier-content lane prompt · M9.3 JIT on Doc-18 Lock · Wave-2 runbook on arrival.

**Beat-59 hivemind commit (host-side):** stages exactly **5 paths** — `context/status/PROJECT_SNAPSHOT.md` · `context/handoff/pm-handoff.md` · `context/handoff/cross-agent-notes.md` (splice repair + ACTIVE currency) · `context/process/cowork-environment-model.md` (+1 quirk) · `context/planning/weeks/2026-W27_jun29-jul05.md` (close-out line). Message: `git commit -F /c/Users/Nick/Desktop/Code/ClaudeFolder/_scratch/2026-07-03_hivemind_beat-59_commit-msg.txt`. Host porcelain is truth.

## 2026-07-03 (beat 58 — v15 hub) — M9.2 DELIVERED + audited ACCEPT-pending-gate; round 1 bounced at compileJava, HUB-FIXED (the checked-PIE sweep); all seven [REVIEW]s ruled

**Two-layer audit (source-verified against the diff, NOT the report):** change-set arithmetic EXACT — porcelain 4 M + 21 named prod files + test tree (13 files counted) + logback.xml = **39** ✓; module-info diff = exactly 2 plain requires with corrected W6 comment ✓ (jSerialComm ships REAL module-info.class — the hub instruction's "automatic module" claim was wrong, lane's correction verified); catalog = exactly 2 entries + the Apache-2.0 election comment verbatim ✓; logback.xml matches the R1 spec incl. the security comment ✓; **package-private claim VERIFIED by grep — every `public` type in the module is pre-existing Phase-2 scaffold, zero new public types** ✓; zero event-model surface in the diff ✓. The lane's derivation fleet earned its keep pre-gate: SL_STATUS_NOT_JOINED 0x0B→0x17 (a v14-restore-breaking mis-pin caught against live-fetched sl_status.h) + the AshCodecTest Flag double-strip (would have failed every gate run). Lane report quality: the M9.1/FE-1b bar held (re-derivable counts, staged explicit-list command, honest gate-denial evidence). One stale line in its chat report: "DP-B needs your confirm" — DP-B was CONFIRMED at beat 57; the handoff file itself is clean.

**Gate round 1 (bounced → hub-fixed same session):** Nick ran full `check`; `:integration:integration-zigbee:compileJava` FAILED — 2 errors, undeclared checked `PermanentIntegrationException` from `startSession()` (Javadoc documented `@throws`, signature didn't declare — the class of defect LLM review can't see). **Hub sweep found THREE files carrying it** (`NetworkFormation.resume()` 211/234; both `TransportProbe.detect()` overloads → 159); javac's error-stop had reported only the first — fixing only the reported sites would have bounced round 2 blind. **The ruled fix — the checked-PIE seam split:** checked PIE lives ONLY on the package-private adapter seam (`startSession()`, NEW `resumeStored()`, both `detect()` overloads — all now declare it; the M9.4 adapter propagates from its lifecycle methods per Doc 05 §3.7 → FAILED-no-retry); the frozen no-throws `CoordinatorProtocol` surface delivers it UNCHECKED cause-chained (`resumeNetwork()` → `IllegalStateException`, pinned by the new wrapper test). Tests: `startSessionOrFail()` helper (21 sites, replace verified 24-occurrence arithmetic), mismatch test split checked-path/wrapper-pin (net +1), `throws Exception` on 7 test methods (2 NetworkFormationTest + 5 TransportProbeTest). 7 files touched, ALL within the 39 — count unchanged. MODULE_CONTEXT +1 gotcha (seam split + provenance). pm-lessons +1 (checked-flow blindness + sweep-the-class-on-bounce + the instruction-internal-consistency case).

**Rulings (WUCP Phase 2, all seven ACCEPT):** R1 `+requires org.slf4j` — the instruction mandated structured logging (LTD-15/§Vectors) while pinning a 1-directive diff; the pin was the hub's own inconsistency; M9.1 precedent pair exact. R2 `+testImplementation(test-support)` — first test tree needs TestClock; M9.1 precedent; test-scope outside the graph-assert. R3 security bitmask **0x1B04** — bellows formation baseline, strictly-hardening flags added, plaintext ZigBeeAlliance09 per §3.13; **hashed-TCLK (0x0084) = Nick's security-posture election AT M9.4** (breaks some legacy joiners — bench evidence should inform it). R4 **negotiated >14 → PIE RATIFIED as band-edge policy** (unknown dialect = the <8 class; best-effort risks misdecode) — AMD-96 one-line currency note QUEUED on the Doc-18 docs pass. R5 **resume-mismatch → PIE** — never silently re-form (orphans every paired device); §3.13 mismatch-rule currency note QUEUED same pass. R6 D-M92-6 43% — ACCEPT retroactive (stub set = the instruction's own anticipated classes; refine future tripwires to exclude instruction-anticipated stubs from the arithmetic). R7 `resetSession()` — ACCEPT (the M9.1 relaunch discipline applied to the codec pin; ReopenAction contract documented).

**Gate round 2 (the hub's fix bounced — the hub's own defect, owned):** `compileTestJava` → "PIE never thrown in body of corresponding try" at the hub's helper. Root cause: round 1's `replace_all` is SUBSTRING matching — the 8-space pattern matched inside the helper's 12-space `protocol.startSession();` line and rewrote the helper into calling ITSELF; the hub's verification grep (24 occurrences) was arithmetically consistent with both the healthy and corrupted file and was read optimistically. Repair: one line; verified by DIRECT READ of the region; full-tree sweep confirms exactly 1 PIE catch (the helper, now legal) + 1 legal InterruptedException catch — the checked-catch class is exhausted. pm-lessons +1 (substring-anchor hygiene; a verification must distinguish the worlds it's meant to distinguish). Round-2 cost: one gate run; production untouched.

**Forward-risk register (the audit's answer to Nick's cascading-risk question; source-verified):** (1) **THE CLASSIFIER-WRAP TRAP — the one real cascading hazard.** `ExceptionClassifier.classify()` = bare `instanceof`, NO cause unwrapping; its Javadoc declares wrapped→TRANSIENT deliberate (the HA anti-pattern guard). A permanent condition arriving wrapped (e.g. the interface surface's ISE) = infinite restart-with-backoff on an unrecoverable state. Rule now in the zigbee MODULE_CONTEXT (M9.4-binding, Matter-general): adapter lifecycle methods propagate PIE BARE via checked seams. **Candidate hardening for Nick at M9.3/M9.4: classifier walks cause chains FOR PIE ONLY** (recognizes wrapped permanents; cannot reintroduce unknown→PERMANENT). (2) `pendingCallbacks` is an unbounded ArrayDeque — harmless until live wiring; M9.3 MUST establish drain cadence + bound (MODULE_CONTEXT note added). (3) >v14→PIE band policy: correct for misparse safety; operator story present in the message; AMD-96 one-liner queued. (4) v14 codec seam synthetic-only by policy (bench baseline outside the #30891 cluster). (5) Bitmask 0x1B04/plaintext-TCLK: bench-validates at M9.4; hashed-TCLK = Nick election there. (6) **Matter containment argument holds:** M9.2 added ZERO public API, zero core edges, zero jSerialComm leakage — nothing a future Matter module could couple to; the reusable assets are PATTERNS (byte-seam testability, checked-seam split, VID:PID identity, signals-only health), not shared code. The cross-integration contract remains the frozen 2-type SPI.

**Post-green hardening pass (Nick's directive — two independent adversarial reviewers over all three integration modules, full detail in the zigbee MODULE_CONTEXT hardening-review record):** implementation verdict SOUND (threading seams, mod arithmetic, adaptive-timeout formula, key custody incl. crash-ordering idempotence, width-seam offsets — all source-verified); test-net verdict SOLID-with-3-gaps. **Hardening batch H1–H5 applied by the hub (5 files, all within the 39 — count unchanged):** H1 `AshFrameAccumulator` 512-byte storm bound + direct test (heap-safety on a babbling NCP — also closed the substituted-path accumulation the reviewer missed); H2 ASH frmNum 7→0 wrap test; H3 EZSP 300-command 0xFF→0x00 wrap test; H4 v14 wide-status END-TO-END resume test (the helpers were already width-aware — verified `defaultHandler` emits genuine 4-byte shapes at connect(14)); H5 both-casings key-leak assertions. **Three reviewer claims REJECTED on evidence** (recorded in MODULE_CONTEXT so they don't resurface): the startSession already-active guard (breaks pinned idempotence), isHeldByCurrentThread-redundancy (reentrant tryLock SUCCEEDS for the owner — the guard is load-bearing), stale-pendingCallbacks-across-reset (already cleared, line-verified). PortLocator multi-identical-dongle limitation documented as an M9.4 inheritance (probe-fingerprint disambiguation before any two-stick topology — Wave-2 ZBDongle-P makes it real).

**Skills system folded (Check 9 STALE — Nick's mirror sync covers project-manager + coder):** `project-manager/references/coding-instruction-format.md` +3 additions (7: checked-exception SEAM MAP mandatory; 8: stub-tripwire arithmetic excludes instruction-anticipated stubs; 9: module-info pins validated against every mandated behavior); `coder/references/deviation-and-quality.md` +2 sections (the wire-constant DOUBLE-DERIVATION discipline — pre-write derive + post-write live-source residual check, the 0x17 catch as origin; checked-exception-flow-is-LLM-blind rules incl. the classifier-wrap warning). pm-lessons carries the full narratives (2 entries this session); coder-lessons carries the lane's 2. nexsys-skills repo: NO change (frontend skill current at `ae434ca`; next fold rides the website-lane return audit). The shift-left keeps dying at the permission gate; this round it cost a full gate bounce that in-session targeted compile would have caught in 20 s. **Fix before M9.3:** in `homesynapse-core/.claude/settings.json` (project scope — the repo CC opens), `"permissions": { "allow": [ "Bash(./gradlew:*)" ] }` (prefix rule; exact-string rules don't match trailing args/output redirection), then restart the CC session so settings load.

**Next:** Nick — (1) re-run the gate: `./gradlew :integration:integration-zigbee:spotlessApply` → `:integration:integration-zigbee:compileJava` → `:integration:integration-zigbee:test` → full `check` (71/41/53; replay/lifecycle tests unmodified; graph knobs untouched); (2) on GREEN: core commit — staged `_scratch/2026-07-03_core_m92_commit-msg.txt`, explicit-list staging per coder-handoff step 5, **exactly 39 files**; (3) push → ci.yml + install-smoke glance; (4) the CC settings fix; (5) hivemind beat-58 commit (staged below). Hub — **Doc 18 authoring is the next work product** (all DP-18 rulings on record; Lock before M9.3; the docs pass carries DP-B/C/D one-liners + A4.5 + map refresh + Connect reword + the R4/R5 Doc-08 currency notes now queued). Website-lane return: audit on arrival.

**Beat-58 hivemind commit (host-side):** stages exactly **8 paths** — `context/handoff/coder-handoff.md` (lane top block + hub gate updates + DP-B currency) · `context/handoff/cross-agent-notes.md` (the lane's M9.2 note) · `context/lessons/coder-lessons.md` (the lane's 2 appends) · `context/lessons/pm-lessons.md` (hub +2) · `context/status/PROJECT_SNAPSHOT.md` · `context/handoff/pm-handoff.md` · `coder/references/deviation-and-quality.md` · `project-manager/references/coding-instruction-format.md`. Message: `git commit -F /c/Users/Nick/Desktop/Code/ClaudeFolder/_scratch/2026-07-03_hivemind_beat-58_commit-msg.txt`. Host porcelain is truth; if it shows other paths, ping the hub. **After this commit: run the skill mirror sync (Check 9 STALE — project-manager + coder).**

## 2026-07-03 (beat 57 — v15 hub LAUNCH) — hygiene clean; M9.2 + website lane AUTHORED; DP-B + DP-18-A/B/C ALL RULED (Doc 18 unblocked)

**First-act hygiene (all four, in order):** (0) restart-resume check — porcelain showed 2 modified spine files atop `35b8936`, BOTH the known truncated-tail mount artifact (env-model §2: mid-token EOF cut, no trailing newline; host tails verified INTACT via file tools against the HEAD content) — read-side lag, NOT a prior-writer collision; no VM-side "fix" attempted. (1) Ground + 11-check preflight = **STALE-benign, zero CONFLICTED** (Checks 4/8 reconciled this session; Check 10 = the queued map refresh, rides Doc 18's docs pass; **Check 9 PASS — Nick's mirror sync + the skills commit `ae434ca` landed before launch**, clearing the prompt's predicted STALE). Fleet re-derived: core `cf3733e` / docs `1509b34` / hivemind `35b8936` / bench `5ceff3b` / skills `ae434ca` — the two commits the beat-56 stamp pre-announced are ON main; snapshot masthead was honest. (2) v14 prompt → `archive/`. (3) Check-8 sweep: the four served notes (FE-1b · Doc-18 · M9.1 · M7.5c-a) moved below `## Archived` (separator relocation, content untouched; ACTIVE: 0); backlog gained the beat-57 currency note (M9.1/FE-1b DONE facts; M9.2 NEXT).

**M9.2 AUTHORED ISSUE-READY** (grounding-subagent-before-authoring: two read-only surveys — core seams/catalog/graph-knobs/logging + docs/bench/charters): `context/instructions/2026-07-03_M9.2_zigbee-transport-ezsp_coding-instruction.md` + `context/pre-verifications/WU-M9.2.md`. Authoring decisions D-M92-1..8 settled per the charter's authoring notes — highlights: **D-M92-1** `CoordinatorTransport.open(Object)` STAYS (zero frozen-interface change; jSerialComm interior-only → plain `requires com.fazecast.jSerialComm` + `implementation(libs.jserialcomm)`, no transitive edge, lockstep-verified); **D-M92-3** byte-channel seam makes zero-real-serial-I/O structural; **D-M92-4** v14 codec seam synthetic-only (**#30891 re-checked lane-direct: CLOSED, 8.0.2/v14-pinned, no 7.4.5 reports** — the frozen bench baseline stays outside the cluster); **D-M92-6** frozen-interface stubs = UOE + `[INFO]` inventory (AMD-63 precedent, >⅓-stubbed = STOP); **D-M92-7** formation storage seam (INV-SE-03 custody contract; real SecretStore binding at M9.3/M9.4); **D-M92-8** DP-E backup seam reserved ZERO-code. Pre-verification pins **BOTH graph knobs** (allow-list edge pre-exists; maxHeight 8 untouched — neither moves in M9.2). Rider R1: `app/homesynapse-app/src/main/resources/logback.xml` root INFO (FE-1b (b) — survey grounded: NO logback config exists anywhere; Logback zero-config DEBUG fallback was the Bearer-echo path). UG100/AN706 constants embedded verbatim with the re-derive rule; ZNP probe-encode only (DP-C). CC-lane gate note: Nick allow-lists targeted `:integration:integration-zigbee:compileJava/:test` (+ spotlessApply) — the M9.1 denial lesson.

**Website lane AUTHORED:** `context/handoff/2026-07-03_website-lane_ssg-and-developer-page_session_prompt.md` — dispatch-craft compliant (ordered reading list, veto-or-default SSG gate [default Astro per the master plan's research; Nick rules at lane start], hazards mined from FE-1b [explicit-list staging, secrets, mount lag], claim→shipped-truth table as a HARD gate [R-3], D-4 segment IA, the L-12 voice on the developer page, dist-gitignore rider as commit 1, CI proposed-not-implemented, write-isolation surfaces named, return contract with re-derivable counts).

**Rulings (Nick, this beat — full rationale preserved):**
- **DP-B CONFIRMED — deterministic identity is PERMANENT policy.** Rationale: the derivation is restart- AND reinstall-stable by construction (pure function of the type string; no identity file for backup/restore or crypto-shred to corrupt/lose — generate-once-persist is the WORSE shape for this architecture). The rename hazard is real but is a NAMING-layer problem: Doc-18 §3.5 namespace governance (publisher-scoped, immutable-by-convention type strings) defuses it BEFORE M9.3 makes anything one-way — the sequencing already protects. M9.3 device adoption is UNGATED on identity.
- **DP-18-A RATIFIED as recommended** (semver on `(componentId, version)` + declared compat range + forward-only non-destructive migration + a NAMED deprecation floor; strict immutability rejected). Nick's added strategic weight: this single ruling unlocks BOTH futures — community-shared components AND AI-as-author (Doc 17 sequences AX-7 ahead of any AI-authoring milestone); forward-only migration is AMD-93-aligned (composes with existing governance, no new mechanism); the named deprecation floor is the anti-SmartThings pledge expressed as MECHANISM, not sentiment.
- **DP-18-B RATIFIED as recommended** (wave-1 curated IN_JVM behind the quality gate; `RESERVED_SUBPROCESS` pinned as the intended default rung for non-curated third-party code at marketplace time; the no-sandbox-claims honesty rule as doc text). Nick: IN_JVM matches what's built + the HA-core analog; RESERVED_SUBPROCESS is exactly what AMD-63 bought the enum slot for; Homebridge proved the retrofit is cheap when the contract stays narrow — ours is a frozen 2-type SPI. The honesty rule weighs MOST: for a trust company, one over-claimed sandbox costs more than every marketing sentence it earned.
- **DP-18-C RULED (a) — reword Connect to convenience framing.** Nick's reasoning (the record the pricing page will need): the community is the growth engine the strategy itself depends on ("fifty rock-solid + the first community wave," months 12–24) — paywalling donated volunteer work poisons contributor motivation and hands HA's advocates the adoption-killing talking point in the exact segment that supplies contributors/evangelists/future Trust-Mark validators. The asymmetry seals it: the paywall guards an EMPTY room today (no marketplace until 12–24 mo) while the reputational cost lands the day a pricing page publishes; rewording now is a one-line edit in an unpublished document. Revenue re-homes to honest ground: Connect sells the hosted/verified/one-click CHANNEL (curated update streams, version-pinned channels, breaking-change assurance — the paid channel funds the per-version scanning + kill-switch infrastructure that protects everyone); the repository stays free + sideloadable. "Community content free forever; we monetize convenience, certification, and cloud" is the procurement-grade, HA-proven shape: **monetize trust, not access.** Refinements ruled in: (i) the reworded Connect line lands in the SAME docs pass as Doc 18 (strategy + governance coherent in one commit); (ii) option (c) skipped — the PRINCIPLE (never paywall community content) is ruled TODAY, so Doc 18 states it as a CONSTRAINT; exact Connect wording polishes at pricing time.

**Next:** hub authors **Doc 18 — Extension & Plugin Architecture (Mode 1)** against the charter (Doc-17 mold; builds nothing; EXT-INV-1/2 through the register minting test; the seven seams as §4 rows; DP-18 rulings folded as settled; the DP-B/DP-C/DP-D one-liners + A4.5 + map refresh + Connect reword ride the Lock's docs pass) → DOCS second-opinion review → Nick co-sign → **Lock BEFORE M9.3**. Nick meanwhile: **commit beat-57** (staged, see below) · **dispatch M9.2** (open Claude Code on the instruction; allow-list the targeted gradlew) · **launch the website lane** (fresh Cowork on the §prompt) · **Dependabot 1-critical — COMPOUNDING since 06-27, the 60-second Security-tab triage** (if it lives in `web-ui/dashboard/package-lock.json` → a frontend micro-WU rides the website-lane window) · frontend.yml glance on `cf3733e` (residual) · Wave-2 arrival → the §3 unboxing runbook triggers.

**Beat-57 commit (hivemind, host-side):** stages exactly **9 entries** — 5 modified (`context/status/PROJECT_SNAPSHOT.md`, `context/handoff/pm-handoff.md`, `context/handoff/cross-agent-notes.md`, `context/planning/phase-3-milestone-backlog.md`, `context/planning/weeks/2026-W27_jun29-jul05.md`) + 3 new (`context/pre-verifications/WU-M9.2.md`, `context/instructions/2026-07-03_M9.2_zigbee-transport-ezsp_coding-instruction.md`, `context/handoff/2026-07-03_website-lane_ssg-and-developer-page_session_prompt.md`) + 1 rename (the v14 prompt `context/handoff/2026-07-02_PM-mission-control_v14_orchestrator_session_prompt.md` → `context/handoff/archive/`). §10 audit: every path traces to this beat's deliverables; no runtime state/build output/secrets; no server ran, no token minted. Message: `git commit -F /c/Users/Nick/Desktop/Code/ClaudeFolder/_scratch/2026-07-03_hivemind_beat-57_commit-msg.txt`. **Your HOST porcelain is truth** — the VM view may still phantom-truncate; if host status shows anything beyond these 9, STOP and ping the hub.

## 2026-07-03 (beat 56 — v14 hub) — the orchestration retrospective FOLDED into the skills system (the true v14 close)

**Nick's directive:** reflect critically on the two external lanes' dispatch/orchestration quality + this hub's own arc — inputs, outputs, context/tokenization — then harden the skills system (nexsys-hivemind AND nexsys-skills) with what was learned.

**Folded:** env-model gains **§11 client-restart semantics** (the twice-paid resume protocol + its four signatures, incl. the Task-not-found rollback tell) and the **§5 gate-copy recipe** (FE-1b's mount-outrun pattern); pm-lessons **+2** (restart-resume; lane-dispatch craft — the dispatch prompt IS the quality gate: ordered ground-first reading lists, evidence-artifact/decision-artifact separation, anti-fabrication tags, prose-vs-tables size caps, known-hazards mined from prior returns, mid-lane baseline-shift protocol, re-derivable counts in returns, defer-shared-file-appends-on-foreign-writes); **PM SKILL.md masthead** gains the settled lane-routing matrix + restart-resume + selective-staging + dispatch-craft pointer clauses (pointer-not-copy — detail lives in env-model/lessons); **nexsys-frontend `references/build-and-ci-discipline.md`** gains the baseline-shift protocol + explicit-list staging + gate-copy pointer. v15 first-act (3) adjusted (lesson queue discharged; sweep = Check-8 only). **Check 9 goes STALE for BOTH skills — Nick's mirror sync covers project-manager + nexsys-frontend.**

**Retrospective verdicts (compact):** the Doc-18 lane's near-perfect auditability was PROMPT-CAUSED, not luck — the demanded structures (tracing, tagging, two artifacts, done-when) are what returned. FE-1b set the return-quality bar (baseline-shift disjointness proof; explicit-list staging catching `dist/`; attempt-1 failure banked as evidence, not hidden). The hub's two real misses: the un-warned token-lockout trap (prior-return hazard mining is now doctrine) and the beat-54 collision (now env-model §11). The in-conversation micro-lane (M7.5c-a, ~300 K lane tokens) priced that pattern honestly and drove the CC-for-backend routing — empiricism over doctrine. Hub token economy validated: the light spine (beat-48 rotation) is what let one conversation carry an 8-beat arc spanning two milestones, two lane folds, an assessment, and a charter.

## 2026-07-03 (beat 55 — v14 hub) — v15 AUTHORED (the v14 close)

**v15 hub prompt AUTHORED:** `context/handoff/2026-07-03_PM-mission-control_v15_orchestrator_session_prompt.md`. First-act 0 = the NEW restart-resume protocol (porcelain + frontmatter freshness before any spine write; failed-anchor = halt — twice-paid); then ground → archive v14 → Check-8 sweep + the queued pm-lesson append. First substantive acts: M9.2 authoring (CC lane; charter notes + both-graph-knobs + DP-E seam + the Bearer-echo rider; DP-B confirm gates device adoption) → website-lane prompt → Doc 18 on Nick's DP-18 rulings (Lock BEFORE M9.3; the docs pass carries DP-B/C/D one-liners + A4.5 + map refresh). §3 carries the Wave-2 unboxing-runbook charge (bench-first characterization; produced from the bench guide + corpus on arrival). Launch as a fresh Cowork conversation on Nick's word; v14 closes at this beat.

## 2026-07-03 (beat 54 — v14 hub) — BOTH returns audited ACCEPT: FE-1b CLOSED; the Doc-18 dossier + charter FOLDED as the authoring authority

**FE-1b audit (ACCEPT):** spot-verified against source — `PROBLEM_TYPE_URI_PREFIX` + slug derivation live in `contract.ts` (L58–66); A5 `parkedSubscribers` at the RATIFIED `string[]` with the pre-ratification-object rejection test pinned (`contract.test.ts:154` — the freeze discipline catching a mirror guess before live data could render blank); commits exact (core `cf3733e` 11 web-ui files explicit-list-staged; hivemind `74b6df6` 2 paths); host `npm run verify` matched the lane number-for-number (55/55, bundle 57.1/100, contract-check 11 @ v1.1.1-2026-07-02); live smoke PASS on a real Core (poll loop LIVE — the FE-1 load-once bug closed; screenshots on the hub record). Residual: **`frontend.yml` glance on `cf3733e`** (Nick). Baseline-carryover discipline (e3d7296→ec2e3b4 mid-lane, delta verified disjoint) was exemplary.

**FE-1b findings ROUTED:** (a) operator token-lockout trap (api_tokens vs initial_api_token divergence; cwd-relative `.homesynapse/` stale siblings) → folds into the **A4.5/DP-F re-pair story** (docs + first-run UX; backlog row at next edit); (b) root-DEBUG echoes raw `Authorization: Bearer` headers (Jetty layer) → **core hardening micro-WU candidate** (INFO default for `:run`), queue post-M9.2; (c) clean.sh-is-build-only correction recorded; (d) ExplainHub blank at zero automations → T2 polish backlog; dist/-gitignore 1-liner → ride the website lane's first commit or Nick's leisure; **Dependabot critical STILL unclassified and compounding** — if it lives in `web-ui/dashboard/package-lock.json` the fix is a frontend micro-WU (bump + verify).

**Doc-18 lane audit (ACCEPT):** dossier = 9 ecosystems, L-1..L-33, anti-fabrication enforced (per-claim source tables, six ✓✓ lane-direct re-fetches, honest-gaps + corrections-to-priors sections — the strongest research return on record). Charter = section-map + EXT-INV-1/2 candidates + the seven-seam reservation list + DP-18-A/B/C, every requirement tracing to L-n or an assessment gap; constraints honored (builds-nothing; proposes-not-assumes the LTD-16/17 amendment shape; R-C evidenced-not-ruled). **FOLDED: the charter is the Doc-18 authoring authority.** Hub concurrences: DP-18-A recommendation (semver + declared compat range + forward-only migration + a named deprecation floor; reject strict immutability) — concur; DP-18-B (wave-1 curated stays IN_JVM behind the quality gate; RESERVED_SUBPROCESS pinned as the intended non-curated rung; the no-sandbox-claims honesty rule as doc text) — concur; DP-18-C — Nick's alone (evidence unambiguous: L-24, nobody paywalls community content; option (a) reword-to-convenience recommended by the lane).

**Next:** the hub authors **v15** (capture-then-execute — v14 is at saturation depth) → v15 runs: M9.2 JIT authoring (CC lane) → Doc 18 authored against the charter + the DP-B/C/D one-liners docs pass (Lock before M9.3) → website lane launch (developer-page IA + the L-12 "breaking changes are a contract" positioning voice). Nick meanwhile: `frontend.yml` glance on `cf3733e` · DP-B one-line confirm (STILL OPEN) · DP-18-A/B/C rulings · the Dependabot 60-second triage · Wave-2 order.

**Restart-collision record (this beat):** a client restart rolled the hub's visible context back AFTER this beat's writes landed; the resumed hub re-audited both returns blind and re-inserted a duplicate beat-54 — the failed-frontmatter-anchor tripwire caught it mid-batch, and the duplicate is merged out (this block is canonical). The two passes CONVERGED verdict-for-verdict — an accidental self-cross-validation. The resumed pass ADDED: commit-chain verification (7d65b35→9bda395→74b6df6 ✓), dossier counts exact (33 lessons / 6 UNVERIFIED ✓), the contract.test.ts:154 rejection-pin re-verified in source ✓, and one routing enrichment — finding (a)'s re-pair story also folds into **M9.4 bench-acceptance prep** (it rehearses exactly there). Check-8: the M9.1 + FE-1b cross-agent notes are served — archival rides v15's first-act sweep. **Lesson QUEUED for pm-lessons (v15 sweep):** after ANY client restart, porcelain + frontmatter freshness-check BEFORE any spine write; a failed Edit anchor mid-batch is the prior-writer signature — halt the batch (paid for twice: the v13→v14 launch and this beat).

## 2026-07-02 (beat 53 — v14 hub) — R-A RULED (research-first Doc-18); the Doc-18 lane authored; the three-lane forward plan set

**Nick's ruling on R-A:** Doc 18 proceeds RESEARCH-FIRST — a dedicated independent Cowork lane deep-dives the ecosystem wars (successes/failures/brilliant ideas of other smart-home + best-in-class plugin platforms), then articulates the Doc-18 requirements charter. Prompt AUTHORED: `context/handoff/2026-07-02_Doc18-plugin-ecosystem_research-and-charter_session_prompt.md` (two deliverables: the plugin-ecosystem-wars research dossier [anti-fabrication tagged] + the Doc-18 requirements charter [section map, invariant candidates, reservation list, AX-7/R-C/isolation-line decision points framed with market evidence for Nick]). The lane runs PARALLEL to M9.2; the hub authors Doc 18 (Mode 1) against the audited charter, targeted to Lock BEFORE M9.3 issues.

**The three-lane forward plan:** Lane 1 Core (CC, serial): M9.2 authoring (hub JIT, next work product) → M9.3 (carries the assessment's profile-registry non-preclusion constraints) → M9.4 → live-bench acceptance → 72 h soak (NQ-6 during) = gate 3. Lane 2 Frontend (Cowork): FE-1b (in flight) → website lane (developer-page IA obligation) = gate 4's website half (install-smoke half already continuously green). Lane 3 Research: the Doc-18 lane → charter → Doc 18 → DOCS review → Lock. Nick's open ledger: DP-B one-line confirm · R-B AX-7 (now rides the charter's framed decision point) · R-C (same vehicle) · Dependabot critical · Wave-2 order EARLY · A4.5 + queued one-liners next docs pass · Node-20.

**Hub context note (capture-then-execute):** v14 is deep; the next heavy arc (M9.2 authoring or the FE-1b/Doc-18 return audits) may warrant authoring v15 first — the hub flags the call at the next turn rather than risking a mid-arc saturation.

## 2026-07-02 (beat 52 — v14 hub) — M9.1 LANDED (`ec2e3b4`, CI + install-smoke green); the extensibility & plugin-ecosystem assessment FILED

**M9.1 closed:** selective stage held exactly (26 staged / 11 web-ui unstaged, Nick's porcelain screenshot-consistent) → core `ec2e3b4` pushed → **ci.yml ✅ + install-smoke ✅** (3 m each) → hivemind beat-51 `a770091` (5 paths exact). The integration spine is on main. DP-B one-line confirm from Nick still pending (asked at beat 51).

**Assessment (Nick's strategic question — is the build fit for breadth/AIoT/plugins/dominance?):** `context/assessments/2026-07-02_extensibility-and-plugin-ecosystem_PM-assessment.md`, synthesized from two read-only survey agents (code seams over `ec2e3b4`; strategy/governance alignment). **Verdicts:** D1 integration breadth YES-by-architecture (2-type frozen SPI, protocol-agnostic capability/confirmation models, breadth-by-data via D5+bench) · D2 AI/AIoT THE-SUBSTRATE-IS-THE-MOAT (replayable provenance log + causal explanations + the honest confirmation ledger + AIOT-INV-1 safety frame — built and CI-enforced) · D3 plugin ecosystem NOT-YET (static composition triple-enforced; the high-value surfaces sealed or reserved-empty; trust boundary paper-only; SDK/marketplace promised in strategy with zero governance footprint — the ONE seam class the non-precluding discipline never formally reserved) · D4 longevity YES-with-named-dependencies (AX-7, SPIKE-DC, GraalVM/FFM). **Bottom line: the foundation supports the ambition; what's missing is a governed reservation of the third-party seam class + the Wave-1→"fifty rock-solid + community" bridge — cheap now, expensive after M9.3/M14 freeze more surfaces.**

**Escalations to Nick (from the assessment):** **R-A** authorize Doc 18 — Extension & Plugin Architecture (Doc-17-style seam reservation; PM authors post-M9.2, BEFORE M9.3 freezes the profile registry) · **R-B** resolve AX-7 (one ruling gates both community components and AI-as-author) · **R-C** the Connect "community integration repository access" paywall line vs the free-tier non-negotiable (flag only). PM-side obligations folded: M9.3 authoring carries the profile-registry non-preclusion constraints; the website-lane prompt carries the developer-page stub; the DP-B/DP-C/DP-D one-liners + strategic-context-map refresh ride the next docs pass; R-4 FUTURE backlog rows to name at the next backlog edit.

**Next:** Nick: DP-B one-line confirm · R-A/R-B/R-C rulings (any order; none blocks M9.2) · FE-1b results when ready. Hub: **M9.2 JIT authoring** on Nick's word (grounding-subagent; charter notes + both-graph-knobs pre-verification + the DP-E coordinator-backup seam + CC-permissions note). Carried: Dependabot 1-critical (re-nagged again on the M9.1 push) · Wave-2 order EARLY · A4.5 + queued one-liners · Node-20 bumps.

## 2026-07-02 (beat 51 — v14 hub) — M9.1 DELIVERED (Claude Code) + audited ACCEPT; gate GREEN round 2; the integration spine is LIVE

**Hub layer-2 audit** (on the lane's 6-dimension source-verifying fleet + Nick's green gate): change-set arithmetic porcelain-exact — 12 M + 13 new + root `build.gradle.kts` = **26 core paths**; the 11 `web-ui/dashboard` files in the tree are the FE-1b lane's, diff-content-verified by the lane itself and EXCLUDED from this commit. Spot-verified in source: lifecycle `module-info` (integration → transitive with the `-Xlint:exports` rationale on the exported 7-arg ctor; `integration.runtime` correctly plain per the AB-3 pattern); the maxHeight raise (comment documents the 8-hop ruled path + do-not-raise rule; direction rules untouched); coder-handoff top block + cross-agent note + 2 lessons present and complete. Gate evidence: targeted compile 45 tasks + 4 suites 70 tasks GREEN (T1–T21), full `check` **GREEN 149** round 2.

**Rulings (WUCP P2, all ACCEPT):** (1) assembly `Function<String, ConfigurationAccess>` — IS the hub-audit-corrected B7/DP-12 (per-integration scoping now; explicit assembly-composition freedom); (2) the persistence decoder pair outside the Files table — DP-3's own mandate (the M7.4a serializer mirrored; JPMS forbids the lifecycle alternative; zero module-info change); (3) `IntegrationSupervisorAssembly.abandon(...)` — W4-forced, `InProcessEventBus.abandon()`-precedented, additive public API (public-API shape = Nick's call; his commit co-signs); (4) root `maxHeight` 7→8 — the ruled architecture's depth, every hop an intended layer, guard stays honest. The lane's fleet found + fixed **4 supervisor lifecycle bugs in-session** (+1 regression test) — the adversarial layer earning its keep.

**DP-B reconciled:** the lane carries DP-B (durable integration identity) as open for Nick; **beat-46 already RULED it** — deterministic identity derivation = permanent policy (governance one-liner still queued for the next docs pass). `IntegrationIds.deriveStable` IS that policy, not an interim. Nick: one-line confirm (or name the durable-story refinement) before M9.2's device adoption binds it.

**Process findings:** (a) the CC session's in-session Gradle was PERMISSION-DENIED — the shift-left loop never ran, so the maxHeight bounce reached Nick's gate; for M9.2, allow-list targeted `./gradlew :*:compileJava :*:test` in CC (keep deny on commit/push). (b) Pre-verification lesson: pin BOTH module-graph knobs (allow-list AND height ceiling) — A33 pinned only the former.

**Commit mechanics (env-model §10):** core = SELECTIVE stage `git add -A -- ':(exclude)web-ui'` → staged exactly **26** / unstaged 11 web-ui → `commit -F _scratch/2026-07-02_core_m91_commit-msg.txt` → push → ci.yml + install-smoke glance. Hivemind beat-51 = exactly **5** paths; if FE-1b lane files appear in the porcelain, HOLD them and tell the hub.

**Next:** core commit+push+CI → hub authors **M9.2 JIT** (grounding-subagent first; charter notes: UG100/AN706 constants verbatim · jSerialComm + Apache-2.0 pin at the catalog line · z2m #30891 recheck for the 7.4.5 line · both-graph-knobs pre-verification) → **FE-1b return audit** when it arrives (its 11 files commit only after that audit). Carried: Dependabot 1-critical · Wave-2 order EARLY · A4.5 + the DP-B/C/D one-liners ride the next docs pass · Node-20 bumps.

## 2026-07-02 (beat 50 — v14 hub) — M7.5c-a LANDED CI-GREEN (`e3d7296`); M9.1 → Claude Code; FE-1b dispatched; install-smoke discovery

**Gate:** round 2 GREEN — targeted tests (53 tasks) + full `check` (149) on Nick's terminal → core committed `e3d7296` (exactly the audited 10 paths; one transient push connection-reset, clean re-push) → **ci.yml ✅ AND install-smoke ✅ on the push (3 m each, screenshot-verified)**. Hivemind beat-49 committed `8a34a7f` (exactly 5 paths). OR-GATE-M7.5c-a → RESOLVED (ledger below); **M9.1 routing UNBLOCKED (§4b clear)**.

**Discovery (spine correction):** `.github/workflows/install-smoke.yml` is ACTIVE on main (activated at `652f9b3` — "frontend + install-smoke workflows, mid-August gate 4") and runs on every push — the carried "install-smoke UNSCHEDULED" item was stale at the v14 brief's authoring. **Gate 4's install-smoke half is CONTINUOUSLY VERIFIED**; the residual gate-4 surface is the WEBSITE build (content exists `homesynapse-core-docs/website/`; build unstarted; lane launches after FE-1b).

**Routing (Nick's decisions this session):** M9.x → **Claude Code** (host-side compile loop — the round-1 bounce class dies in-session; the instruction files are already CC-shaped; the hub stays single-spine-writer + auditor; CC returns ride coder-handoff/cross-agent per the standing protocol). FE-1b → a **separate Cowork conversation** (prompt: `context/handoff/2026-07-02_FE-1b_v111-fold-and-live-smoke_session_prompt.md`) — keeps this hub's context for orchestration + audits.

**Executed this beat:** the beat-46-ruled backlog M9-row replacement (the M9 epic row → M9.1 NEXT-ROUTED + M9.2–M9.4 JIT + M9-deferred, all charter-pointed to the return §3) + the beat-50 currency note; the W27 midweek-progress block; the M9.1 dispatch annotation (HEAD `52824e9`→`e3d7296`; the M7.5c-a delta [rest-api envelopes + lifecycle PROJECTION_VERSION/installAdminEndpoints] is disjoint from the M9.1 read-set; the instruction's own RE-VERIFY-at-issue clause governs — CC re-runs the STOP gates before coding).

**Next:** Nick launches FE-1b (fresh Cowork conversation on the new prompt) + opens Claude Code on `context/instructions/2026-07-02_M9.1_integration-spine_supervisor-and-command-routing_coding-instruction.md` (kickoff text in the hub conversation). Hub: audit both returns as they land (two-layer, source-checked); author M9.2 JIT after M9.1's WUCP close (grounding-subagent-before-authoring); author the website-lane prompt after FE-1b closes. Carried: Dependabot 1-critical (re-nagged on THIS push — the ~60 s Security-tab triage) · Wave-2 order EARLY · A4.5 Doc 15 note + the DP-B/C/D one-liners ride the next docs pass · Node-20 action bumps.

## 2026-07-02 (beat 49 — v14 hub) — M7.5c-a DELIVERED + audited ACCEPT (first in-conversation Coder lane); Check-8 archival executed; gate open

**Lane mechanics (NEW pattern, Nick's directive):** M7.5c-a ran as a write-isolated in-conversation agent under the hub (vs the historical separate-conversation lanes) — ~300 K lane tokens, 86 tool calls, clean single-message return, hub context preserved for the audit. The two-layer audit held: every load-bearing claim source-verified (P1–P6 outcomes, the 10-file change set porcelain-exact, all 11 tests present by name, zero direct time access in rest-api tests, `PROJECTION_VERSION` single-sourced at HomeSynapseCore feeding both `StateProjection.create` and the gateway, freeze-vocabulary match on A4/A5). One suspected compile-breaker (`\`-for-`//` comment markers at the wiring site) chased and EXONERATED as a Grep-tool render artifact — host-side Read shows clean `//`; recorded so the next auditor recognizes the signature.

**Rulings this beat:** (1) `[REVIEW]` `parkedSubscribers` = ids with `dlqDepth > 0` — **ACCEPT** (faithful to §A5's DLQ framing; `SubscriberMode` has no PARKED value; a SUSPENDED-but-empty-ring subscriber stays visible via the additive `subscribers[].mode`; a 2-line "wedged" widening remains available if operator feedback wants it). (2) P6 note-and-defer **ACCEPT** — server-side `If-None-Match`→304 is an API-wide non-feature; A4/A5 now have full caching parity with the conforming reads. (3) P2 observation **LOGGED**: the conforming envelope is an inline idiom, not a shared helper — a nine-handler helper-extraction refactor WU is a post-M9 candidate; do NOT fold it into M9.x.

**Check-8 archival EXECUTED** (the beat-48 queue, at its named quiescent window): the `## Archived` separator relocated above the six ruled 06-27/28 notes — zero content moves; the M7.5c-a delivery note is the sole active entry; ACTIVE line rewritten accurately.

**Deferred gate → OR-GATE-M7.5c-a** (Open Risks below — blocks M9.1 routing per §4b). Core commit staged: `_scratch/2026-07-02_core_m75ca_commit-msg.txt` — **stages exactly 10 paths**, all under `api/rest-api` + `lifecycle` + `testing/integration-tests`, zero hazard-class, no tokens minted (env-model §10 audit run by the hub).

**Next:** Nick runs the gate → core commit + push → `ci.yml` glance (gate of record; `frontend.yml` won't trigger — no `web-ui/` paths) → hub routes **M9.1** (serial Coder lane) ∥ launches **FE-1b** (frontend lane; A4/A5 envelope-conformant — the lane drops any bare-body tolerance). The W27-plan progress line + the backlog M9-row replacement ride the M9.1 issuance edit (beat-46 ruling). Carried Nick items unchanged from beat 48.

**Gate round 1 (post-write addendum):** test-compile bounce — 2 AssertJ capture errors in `DlqStatusEndpointTest` (the ONLY new test class with list-content asserts; A4's fields are scalars). Hub-fixed to the conforming typed-local idiom (2 lines, detail in OR-GATE-M7.5c-a); production compile + integration-tests check were already GREEN. The defect class is exactly what a host-side in-session compile loop kills — reinforces the M9.x→Claude-Code routing recommendation recorded this beat. Staged core commit message unchanged (still exactly 10 paths).

## 2026-07-02 (beat 48 — v14 hub) — LAUNCHED; the deferred spine rotation EXECUTED; preflight PASS on a light spine

Launch preflight = STALE-benign, zero CONFLICTED (the predicted Latest-commits v11-era field + stamp/backlog/Check-8 trails); fleet verified clean at core `2786526` · docs `1509b34` · hivemind `b9349e8` (beat-47 committed) · bench `5ceff3b` · skills `90f8258`; both skill mirrors byte-identical (Check 9 PASS — the sync landed).

**The rotation (v14 §2, phantom-safe file-tools-only — executed via three write-disjoint agents under hub two-layer audit):** snapshot masthead beats 6–41 (27 entries, chronology-asserted) → `archive/PROJECT_SNAPSHOT-beats-06-41-rotated-2026-07-02.md` (69.3 KB); second pass: the 2026-06-19/21-era Current-Work-Unit blocks (5) + the pre-beat-era Recent-Session-Log rows (30) → `archive/PROJECT_SNAPSHOT-work-unit-blocks-and-session-log-rotated-2026-07-02.md` (44.2 KB); pm-handoff beats 5–43 (33 sections; beat 18 never existed in this file) → `archive/pm-handoff-beats-rotated-2026-07-02.md` (115.9 KB). Retained beats converted to short-paragraph blocks (env-model §8). The v11-era **Latest-commits / Last-updated / Current-state** fields RETIRED to pointers — they carried drifted AMD-95/170/50-era claims vs the ratified AMD-97/172/51. Live spine: snapshot ~38 KB (was 154.8) · pm-handoff ~25 KB (was 138.9). Tail-fingerprints verified on both files; every count re-derived by the hub against the actual files (never folded from the agent reports alone).

**Also this beat:** OR-GATE-M7.4 ledger-reconciled ✅ RESOLVED (the beat-43 ci.yml-green-on-`52824e9` subsumption — the ledger text had lagged it); cross-agent frontmatter restamped ACCURATELY (the six 06-27/28 Coder notes above `## Archived` are ALL RULED at beats 14–34 but stay in place — archival QUEUED at the M7.5c-a WUCP quiescent window per env-model §2b Coder-in-flight caution); pm-lessons +3 (two-grep absence rule · `add --dry-run` takes the lock · keep-both-audit-layers); v13 prompt → `archive/`. Re-run preflight = **PASS (all 11)**; forward work unblocked. Audit trail: one agent transcription defect self-caught pre-write (duplicate session-log row), one hub post-return correction (an agent's "cleared of ruled entries" frontmatter overclaim) — the two-layer model earned its keep again.

**Nick, host-side (env-model §10 audit stated):** hivemind commit — `git add -A` stages exactly **9 paths** on your HOST porcelain (4 modified: snapshot · pm-handoff · cross-agent-notes · pm-lessons; 1 archived-prompt rename pair: v13 prompt D + archive ??; 3 new archive files). The VM view shows 8 (pm-lessons.md edit inside the >30-min mount lag — expected, host is authoritative). All .md, zero hazard-class files, no tokens minted this session; nothing stages from `_scratch/` (sibling dir). Command: `cd /c/Users/Nick/Desktop/Code/ClaudeFolder/nexsys-hivemind && git add -A && git commit -F ../_scratch/2026-07-02_hivemind_beat-48_commit-msg.txt && git push`. Other four repos: untouched this session, porcelains clean at launch.

**Next:** M7.5c-a (route to Coder if not already in flight) → FE-1b lane on its landing → M9.1 on its WUCP close; the backlog M9-row replacement rides the M9.1 issuance edit (beat-46 ruling, on record). Carried Nick items: Dependabot 1-critical (beat-33 tree, ~60 s) · A4.5 Doc 15 §3.4/§6 note · Wave-2 device order (order EARLY — procurement lead time; BEST_EFFORT-class Xiaomi/Aqara sensor first, energy plug second) · Node-20 action-major bumps · DP-B/DP-C/DP-D one-line governance notes ride the next docs-touching amendment · install-smoke (gate 4) still UNSCHEDULED — slot after M9.1 or fold into the soak window.

## 2026-07-02 (beat 47 — v13 hub) — pre-commit audit made standing; env lessons folded; v14 authored (the v13 close)

Fleet post-beat-46: core `2786526` (FE-1 committed — the pre-commit audit found exactly 16 files, zero junk, .gitignore airtight; `frontend.yml` green-glance pending) · hivemind `41a105d`.

**Adopted standing (Nick's directive):** the **pre-commit change-set audit** = env-model **§10** (inventory via lock-free porcelain, every-file-maps-to-a-deliverable, ignore/secrets sweep, expected-count stated in every command handoff; skills point at it) + the frontend CLAUDE.md lane-return pointer.

**Env lessons folded (both paid-for today):** `git add --dry-run` TAKES the index lock through the mount (one stranded + cleared 2026-07-02) — preview change sets with `--no-optional-locks status --porcelain` only; the mount read-lag correction (>30 min observed for edited files; new files sync promptly).

**v14 hub prompt AUTHORED** (`context/handoff/2026-07-02_PM-mission-control_v14_orchestrator_session_prompt.md`): first-act = the deferred spine rotation with a **phantom-safe FILE-TOOLS-ONLY method** (do-not-re-defer) + Check-8 archival + the snapshot Latest-commits retirement + three pm-lessons candidates; then the delivery loop (M7.5c-a → M9.1 → M9.2-4 JIT ∥ FE-1b → website lane) to the mid-Aug gate; Nick's standing items incl. the THRICE-nagged Dependabot critical + the Wave-2 device order (procurement lead time).

**M7.5c-a routes to the Coder NOW — it does not wait for v14.** CLAUDE.md-untracked noted as deliberate (Nick's call, no action).

Next: Nick commits beat-47 → routes M7.5c-a → launches v14 as this conversation's successor.

## 2026-07-02 (beat 46 — v13 hub) — both lane returns AUDITED + ADJUDICATED; four rulings landed; M7.5c-a issued; M9.1 issue-ready (corrected)

**M9-authoring return: AUDIT PASS-WITH-ONE-CORRECTION.** Ten-row independent spot-audit against core `52824e9`: nine PASS verbatim (incl. the M7.4d javadoc M9-mandate at L127-128, both module-info embeds, ctor arity, the WithinTolerance stub, count-pin tests, module-graph allow-list).

ONE REFUTED: WU-M9.1 §A31/B7 claimed "NO ConfigurationAccess implementation exists anywhere" — false: `ScopedConfigurationAccess` (production, config module, package-private, `(String integrationType, ConfigModel)`) + `InMemoryConfigAccess` (testFixtures) exist.

**Correction FOLDED** into WU-M9.1 (A31+B7 rows) + the instruction (Files-table row + DP-12): preferred path = expose the existing scoped impl via a small public config-module factory (per-integration scoping arrives free; zero JPMS change), fallback = the original lifecycle adapter, Coder pushback invited.

The lane's zero-fabrication bar held on signatures; the absence-proof failed — noted for pm-lessons (grep-absence claims need a second grep form).

**FE-1 return: AUDIT PASS; both drifts adjudicated on the truth hierarchy — OPPOSITE rulings, each grounded:** DRIFT-1 (bare `/internal/*`): no Locked doc backs the bare shape; it breaks the freeze's own §0 poll anchor → **Core conforms** (Nick: micro-WU FIRST).

DRIFT-2 (URI problem-type): Locked Doc 09 §3.8 + `ProblemType.java` AGREE on the URI form → **the freeze mis-transcribed → v1.1 → v1.1.1** (folded into the freeze record: URI form + clients-key-on-slug-suffix + the A4/A5 conformance ruling note).

Also verified: `npm run verify` 45/45 green claim, bundle 56.9/100, write-isolation held, nothing patched client-side against drifted shapes (exemplary freeze discipline).

FE lane's env-note (mount lag >30 min) queued for the env-model at the next process pass.

**Nick's four rulings (2026-07-02):** DP-A ratify-all (split M9.1→M9.4 + DP-C/D/E/F accepted; backlog M9 row replacement rides the next backlog edit) · DP-B deterministic identity derivation = permanent policy (one-line governance note queued for the next docs-touching pass, with the DP-C/DP-D V1-subset-of-Doc-08 recording) · DRIFT-1 conform-Core-micro-WU-first · DRIFT-2 amend-freeze-to-URI (done).

**Issued/next:** **M7.5c-a** (internal-read envelope conformance — `context/instructions/2026-07-02_M7.5c-a_*.md`, ISSUE-READY, sequenced BEFORE M9.1) → **M9.1** (corrected instruction, issue on M7.5c-a's WUCP close) → M9.2–M9.4 authored just-in-time from the return's §3 charter.

**FE-1b** (drift fold: slug-suffix matching + smoke re-run) launches AFTER M7.5c-a lands.

**Nick, host-side:** (1) commit core `web-ui/dashboard/**` + delete the pairing-token artifact per Core's log instruction (the FE lane's step); (2) commit hivemind (this beat + lane files + corrections + freeze v1.1.1 + the M7.5c-a instruction); (3) route M7.5c-a to the Coder.

Carried: Dependabot triage · A4.5 Doc 15 note · Node-20 bumps · phantom-gated rotation · the DP-B/DP-C/DP-D governance notes at the next docs pass · lane drift-findings #1/2/5/6 (MODULE_CONTEXT staleness + Doc 02 §3.9 residency) fold at M9.x WUCPs.

## 2026-07-01 (beat 45 — v13 hub) — nexsys-frontend skill currency + dual-surface pass (nexsys-skills repo)

The frontend skill (authored 06-27, pre-build-arc) brought current: the live-vs-mocked endpoint partition → re-derive-at-preflight rule (M7.5a/b hero endpoints are REAL; /events+/health await M7.5c); the HA-2026.7 watch RESOLVED (verified 2026-07-01: not-triggered traces still absent from stable — both moat halves hold; watch continues); NEW explainability §2b = the four measured AMD-97 rendering behaviors (values pointed at Doc 08 §3.6 + corpus, never hardcoded); Doc 08 added to the SKILL.md truth: block; CLAUDE.md gains a "Runtime surfaces" section (Cowork deferred-gate norm vs Claude Code run-verify-yourself).

Verified zero stale phrases remain; the skill was born pointer-not-copy (aged far better than coder/PM).

**Nick:** nexsys-skills commit (`_scratch/2026-07-01_skills_frontend-currency_commit-msg.txt`) + one more mirror sync (this skill's mirror is now behind) — then launch the lanes.

The FE-1 lane benefits directly (it loads this skill).

## 2026-07-01 (beat 44 — v13 hub) — skill-source precision pass; Check 9 STALE pending mirror sync

Pointer-not-copy applied to both skill mastheads + the PM Mode-3 state narrative; preflight 10/11 strays fixed; coder-handoff frontmatter restamped; the survey agent's "CLAUDE.md module-count wrong" claim verified FALSE (19 is correct — no edit). Full record: snapshot beat-44 (pointer, not copy). **Nick:** beat-44 hivemind commit (`_scratch/2026-07-01_hivemind_beat-44_commit-msg.txt`) → **mirror sync (Check 9)** → launch the two lanes. Deferred: mega-line overhaul rides the phantom-gated rotation; coder/references content refresh post-M9.

## Rotated session records

**Rotated 2026-07-02 (v14 hub first-act — the deferred beat-block rotation, phantom-safe file-tools-only method):** beat sections 5→43 (33 sections; beat 18 was never written to this file) -> `archive/pm-handoff-beats-rotated-2026-07-02.md`. Kept: beats 44–47 + Open Risks (reconciled). Full history in git.

**Rotated 2026-06-22 (v5-hub first-act hygiene; git-object-sourced from HEAD `04c0a54`):** the stale HTML-masthead reconciliation prose (2026-06-13 / -14 / -18 / -18 / -19) + the beats older than beat-5 (beat-4 v4-launch, beat-3 v4-handoff, beat-2 explainability, plus the v3-launch / M7.2a-1-landing / v2-parallel-lanes / Doc-16-Lock / AB-1+AB-2 entries) -> `archive/pm-handoff-rotated-2026-06-22.md`. Kept: beat-6 + beat-5 + Open Risks. Full history in git.

**Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; git-object-sourced from `58794d7`):** the ratification record (Return ASSESSED → RATIFIED → mechanics), the superseded fold-abort record, the M7-AMD-block record, the M6.4-WUCP record + the M6.2-era fragment, and the RESOLVED/CLOSED build-gate blocks from Open Risks → `archive/pm-handoff-2026-06.md`. Earlier rotations per the markers preserved there. Full history in git.

## Open Risks

#### OR-GATE-M7.5c-a — deferred build gate on the M7.5c-a working tree — [✅ RESOLVED 2026-07-02, beat 50] (NEW 2026-07-02, beat 49)
- **RESOLVED (beat 50):** round 2 GREEN — 53 targeted + 149 full-`check` tasks on Nick's terminal → core committed **`e3d7296`** (10 paths exact) → pushed → **ci.yml ✅ + install-smoke ✅** on the push. M9.1 routing UNBLOCKED (§4b clear). Total: ONE fix round (2 test-compile errors, hub-fixed — within the P5 one-round target for compile-class defects).
- **Milestone:** M7.5c-a (internal-read envelope conformance). **Commit:** pending — working tree on core `2786526`; change set = exactly 10 files (7 source/test + 3 MODULE_CONTEXT).
- **Commands (Nick):** `./gradlew :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` → `./gradlew :api:rest-api:test :lifecycle:lifecycle:test` → `./gradlew check` (count pins stay **71/41/53**; both module-infos + every `build.gradle.kts` unchanged) → `./gradlew :testing:integration-tests:check` (watch `EndpointE2eIT` — DLQ paths moved under `$.data`). `-Werror` rides the convention plugin — do NOT pass it as a CLI flag.
- **Round 1 (2026-07-02):** targeted `compileJava` GREEN + `:testing:integration-tests:check` GREEN + host porcelain exactly 10 ✓; `compileTestJava` FAILED — 2 AssertJ varargs-capture errors (`DlqStatusEndpointTest` L75/L135: `containsExactly` on a `List<?>` actual). **Hub-applied 2-line fix** (SendMessage-to-lane unavailable; mechanical class): the `@SuppressWarnings("unchecked")` typed-local idiom per conforming `ListEntitiesEndpointTest` L83–88. Lesson appended to coder-lessons. **Re-run pending:** `:api:rest-api:test :lifecycle:lifecycle:test` → `check`.
- **Owner:** Nick. **Deferred Build Gate:** YES — the lane's sandbox attempt failed at `build-logic` (mount delete + VM JDK 11 vs 21); evidenced in coder-handoff, never silently skipped.
- **Resolution condition:** `check` green → core commit (staged message, exactly 10 paths) → push → `ci.yml` green on the pushed commit (the gate of record). On resolution: M9.1 routes to the next Coder lane; FE-1b launches on the landing.

#### OR-M7-WIRING — Command-pipeline live wiring — [✅ RESOLVED 2026-06-27 — live-wired at M7.4b/c] (formalized 2026-06-25; NEW in beat 7, 2026-06-22)
- **RESOLVED 2026-06-27 (M7.4b producer + M7.4c confirmation/ledger; v8 beats 17/19; reconciled by the v9 hub).** The command pipeline is LIVE on the real composition root: trigger → run → `command_issued` → co-located dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`, with the live `pending_command_ledger` + `pollExpirations()` tick and paired teardown. The resolution condition's **E2E composition-root confirm gate is met** — `RunPipelineConfirmWiringTest` (host-verified present in `lifecycle/lifecycle/src/test`).
- **Residual → M7.4d (the only remaining M7.4-arc item):** the resolution condition's *second* clause — the **D2 pure-function-replay CI test wired as a gate** (+ registering D2's canonical INV-id) — is NOT yet landed. The *wiring* is resolved; the *replay-safety CI gate* is the M7.4d hardening.
- **Severity:** RESOLVED (was MEDIUM). **Owner:** PM/Coder — done. _See OR-GATE-M7.4 below for the gate-of-record confirm on `db8ab5f`._

#### OR-GATE-M7.4 — gate-of-record (CI) on the pushed `4e31aed` (M7.4d HEAD) — [✅ RESOLVED 2026-07-01 — ci.yml VERIFIED GREEN on core 52824e9 (beat 43)] (NEW 2026-06-27, v9 hub; updated v10 beat-25)
- **RESOLVED 2026-07-01 (v13 hub, beat 43; reconciled into this ledger by the v14 hub):** `ci.yml` GREEN on pushed core `52824e9` — screenshot-verified (Build & Check 3m5s, Gradle 8.8, `check` ✅). Subsumes the per-commit CI-confirm for the whole ancestor chain (`4e31aed`/`9ec5949`/`eed477e`/AB-4/M7.5a/b). The `frontend.yml` first-trigger glance on `52824e9`+`2786526` is a standing Nick item (hub tracks), not an OR.
- **Status:** NARROWED. The **local `./gradlew check` is CONFIRMED GREEN** on the M7.4c working tree that became `db8ab5f` (Nick's terminal 2026-06-27: BUILD SUCCESSFUL, 149 tasks; he then committed + pushed core `69cc6e2..db8ab5f`). So the M7.4b/c/**d** deferred build gates are all **resolved locally** (Nick's terminals: `./gradlew check` 149 tasks green on the M7.4c tree → `db8ab5f`, and again on the M7.4d tree). The only residual is the **gate-of-record** formality: the `ci.yml` GitHub Actions run green on the pushed HEAD **`4e31aed`** (the M7.4d commit has now landed; `db8ab5f`→`4e31aed`). **De-risked (v10 beat-25):** `git ls-tree 4e31aed gradlew` = `100755` (executable) → the prior exit-126 failure mode is structurally cleared, and `ci.yml` runs `./gradlew check --no-daemon`; what remains is purely Nick confirming the Actions run is green on `4e31aed` — the standing b85e1ed/M7.4a pattern, "confirm CI green on the pushed commit, not just locally."
- **Severity:** LOW (the substantive gate — the full local `check` — is green; this is the CI-record formality).
- **Owner:** Nick (glance at the Actions tab for `db8ab5f`). **Resolution condition:** Nick confirms the `ci.yml` run is green on `db8ab5f` → record M7.4b/c CI-gated → close. M7.4d may be authored/routed now (the local gate clears §4b — done, ISSUE-READY beat 22).

#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
