<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-09-11 (v70 beat 2 — the `1e26912` red read at the correct artifact (run 34650129654): no `DeliveryAnomaly` token anywhere in the reports or the log; the motion edge is in the store, the journal is silent for ten seconds, no dispatch; P2 holds, P1 fails, P3 holds; FIX-2 opens with the instrument (the IT's timeout diagnostic · TR-1b's position-census ITs · the bench `bus-soak` verb), the structural fix dispatches on its reading; filed at `context/audits/2026-09-11_CI-1e26912_red-read_HeroLoop-lifecycle_v70-b2.md`; beat 1 landed `89de5b3` (Nick's hands); next: b3 R-4c's packet, Fri 2026-09-11 ~18:0x CT (instrument 2026-09-11T23:02:50Z). Order: hivemind 5 = 4 M + 1 A (computed from porcelain inside the splice).) Prior: 2026-09-11 (v70 beat 1 — the v70 boot: the dispatch filed verbatim (`_scratch/v70/beat1_dispatch_verbatim.txt`); five HEADs clean, ahead 0; preflight 10 PASS + Check 9 STALE (`project-manager/SKILL.md`; the mirror sync is Nick's, no lane waits) + Check 12 flipped (the HERO-1 charter → EXECUTED); Act 0 verified at the bytes: the `1e26912` run's log + test reports under `_scratch/v70/ci-1e26912/` (sha `1e269127…`, 2026-09-11T21:35Z, `:614`/`:115`); the H8-a send re-armed on v70 (`trig_01UFmAs97CvSBzVXB6apVWLd`, 23:45Z) and v69's deleted; the paste file on disk (4,759 B); the deliverable: R-4c's packet; next: b2 the red read at the artifact by grep, Fri 2026-09-11 ~17:5x CT (instrument 2026-09-11T22:57:32Z). Order: hivemind 5 = 5 M + 0 A (computed from porcelain inside the splice).) Prior: EVERY SEGMENT v55 b3 → v69 b4 ROTATED VERBATIM into archive/chains-rotated-2026-08-27.md (one heading per rotation = the COUNTED record: 90 rotations through v70 b2; the v70 b1 segment is the one Prior above); THE FULL PRE-2026-08-23 CHAIN (v55 b2 → v54 b10 segments + everything earlier) verbatim at archive/chains-pre-region-cap-2026-08-23.md; the pre-v55 chain at archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md.
-->

# PM Session Handoff

> **How to read this file:** beats are NEWEST-FIRST below; a fresh session reads the frontmatter `last-verified:` chain (one line, newest segment first) + the newest ONE beat block (THE CONTEXT-BUDGET LAW, the v67 prompt §1/§1b: chain ≤3,000 B · ≤12 live blocks · Open Risks OPEN-only); older blocks only when the newest sends you there. Older beats live in `archive/pm-handoff-beats-*-rotated-*.md` (the archive map at the tail). The `## Open Risks` section at the tail is standing. Only the hub writes here (prefix-prepend); the newest beat outranks everything below it.

## 2026-09-11 (v70 beat 2 — the red read at the bytes: P2 holds; FIX-2 opens with the instrument; beat 1 landed `89de5b3`, Fri 2026-09-11 ~18:0x CT (instrument 2026-09-11T23:02:50Z))

**Landed (Nick's hands):** hivemind `89de5b3` (beat 1), pushed, porcelain 0. His line: `ARTIFACT: 1e26912 · run 34650129654 · sha verified · reports: yes`. **The read (grep only; the audit is the record):** `context/audits/2026-09-11_CI-1e26912_red-read_HeroLoop-lifecycle_v70-b2.md` (4.5 KB). Three greps, three zeros: no `DeliveryAnomaly` or `bus.delivery_anomaly` in the HeroLoop test's XML (51,699 B, md5 `78db6356…`), in all 42 lifecycle report files, or in the 25,759-byte Run check log; WARN lines from other loggers are present, so the silence is the detector's. The test passed both baseline awaits (`occupied=false`, then `occupied=true` read back from the store, `:98–:103`); then nothing for ten seconds after `zigbee.device_adopted` at 21:38:40.031 until `stopped` at 21:38:50.114 — no `zigbee.command_result`, no frame at the NCP; the await at `:115` timed out. The emitter's kinds are six (`NOTIFY_NOT_VISIBLE` … `TRANSITION_READ_EXHAUSTED`); none fired. **Adjudicated against the pre-registration:** P1 FAILS, P2 HOLDS, P3 HOLDS. **The branch:** FIX-2 opens with the instrument — (I1) the hero-loop IT's timeout diagnostic (per subscriber: last delivered position + checkpoint, beside the store head); (I2) TR-1b's position-census ITs as red-first tests; (I3) the bench `bus-soak` verb (N hero loops under load; latency p50/p99; anomaly count). The structural fix (checkpoint-driven LIVE delivery; an AMD to Doc 01 §3.4) is the designed candidate and dispatches on the instrument's reading. EXPLAIN-114a stays behind FIX-2. **Disclosed:** no green hero-loop artifact is on disk to set beside the silence; the engine has no firing log line in source. **Handed:** this beat's card (4 M + 1 A). **Next:** b3 R-4c's packet (the deliverable) · the H8-a paste at 18:45 by the send · b4 FIX-2's instruction. Order: hivemind 5 = 4 M + 1 A (computed from porcelain inside the splice). ctx: beat 2 · calls ≈24 · read ≈100 KB · wrote ≈20 KB

**Leverage line:** the read cost four greps and settles FIX-2's first move without a re-run.
**The alternative shape, rejected:** authoring the structural fix tonight on the strength of the silence; the silence is what the instrument exists to explain.
**2029 test:** the audit carries the re-runnable greps, the md5 and the line numbers.

## 2026-09-11 (v70 beat 1 — the v70 boot: intake at the bytes; the H8-a send moved to v70; the deliverable named, Fri 2026-09-11 ~17:5x CT (instrument 2026-09-11T22:57:32Z))

**Boot:** the dispatch filed verbatim at `_scratch/v70/beat1_dispatch_verbatim.txt` (5,164 B); the read-set 38.8 KB of 45. **Instrument:** core `1e26912` · hivemind `ea96351` · skills `c630c5c` · bench `4539f13` · docs `876a395`, all clean, ahead 0. **Preflight:** 1–8, 10, 11 PASS; Check 9 STALE on 1 of 28 files (`project-manager/SKILL.md`, `bda340b1` vs synced `76bf4ce6`; Nick's sync, no lane waits); Check 12 STALE on one file, flipped here (the HERO-1 charter → EXECUTED). **Act 0 verified:** `_scratch/v70/ci-1e26912/` holds the `1e26912` run (checkout sha `1e269127152157…`; dated 2026-09-11T21:35:40Z; `5_Run check.txt` 25,759 B, the failure at `HeroLoopHardwareFreeIT.java:614` ← `:115`; 42 lifecycle report files incl. the HeroLoop TEST xml). The August log under `_scratch/v69/` is retired as evidence. **The deliverable:** R-4c's packet (Saturday). **Time-gated:** the H8-a paste goes at 18:45 CT by `trig_01UFmAs97CvSBzVXB6apVWLd` (23:45Z) from `_scratch/v70/2026-09-11_H8a_paste_18-45CT.md`; v69's `trig_01AHRS3Q29MtP6q39yT24ogs` deleted. Tonight's artifact stays the packet's `f25291b`; R-4c's .deb carries HONESTY-1's Java, where `lastReported` is read. **Pre-registered for b2 (grep only):** P1 the HeroLoop stdout carries `DeliveryAnomaly` ⇒ FIX-2 opens with the structural fix (checkpoint-driven LIVE delivery + an AMD to Doc 01 §3.4); P2 no token ⇒ the `bus-soak` instrument first; P3 `SUSPEND` and `*_READ_EXHAUSTED` absent either way. **Handed:** this beat's card (5 M). **Next:** b2 the red read · b3 R-4c's packet · b4 FIX-2 · b5 HERO-1b · b6 P-1 · the H8-a intake on `H8A:`. Order: hivemind 5 = 5 M + 0 A (computed from porcelain inside the splice). ctx: beat 1 · calls ≈17 · read ≈75 KB · wrote ≈13 KB

**Leverage line:** the boot verified the artifact's sha, so b2 reads bytes proven to be today's run.
**The alternative shape, rejected:** re-pointing tonight's §0 guard at `1e26912`'s .deb forty minutes before the rig, an unmeasured change to the block asserts; Saturday reads the new Java on its own packet.
**2029 test:** every claim names a file, a sha or a trigger id.

## 2026-09-11 (v69 beat 4 — EXPLAIN-114a authored: the deliverable, from the source; the dispatch handed for after H8-a, Fri 2026-09-11 ~16:4x CT (instrument 2026-09-11T21:44:57Z))

**Landed (Nick's hands):** core `1e26912` (`docs(dashboard): HERO-1`, three design files) and hivemind `d0bcc1e` (beats 2–3). **Then the gate of record spoke: the `ci` run on `1e26912` FAILED at `:lifecycle:lifecycle:test`**. A commit with no source change cannot alter lifecycle behaviour, so this is a veto sample against the FIX-1b bytes (OR-BUS-SILENT-DROP stood at 8/20 green) — named by Nick's second paste: `HeroLoopHardwareFreeIT.heroLoop_motionToHonestConfirmed` timed out awaiting the On frame (`awaitTrue :614`, 10 s), the CI-613 class; sample #9 on the FIX-1b bytes is RED. The artifact's `DeliveryAnomaly` tokens decide the branch; no re-run; EXPLAIN-114a HELD; FIX-2 is the candidate next Java WU. Nick's words: a fix and a long-term solution for the class; **`HERO1:` (a) on all seven rows; `EXPLAIN: three`** — banked. **Grounded at `eabdbb1`:** the log already holds five of HERO-0's keys (`triggeringEventId`, `definitionHash`, the classifying events' instants, `automation_disabled`, the DP-B2 growth path); `EventStore` has no by-id read; the rest are 114b/114c. **Authored:** `context/instructions/2026-09-11_coder-lane_EXPLAIN-114a_…coding-instruction.md` (30259 B): 13 M + 0 A expected; four predictions; DP-1..DP-7 settled with cites; the dispatch line for a host-side Claude Code session. **Handed:** the artifact save; the HERO1 and EXPLAIN words; this beat's card (4 M + 1 A). **Next:** the H8-a paste at 18:45 by the send · the red read at the artifact · R-4c's packet (b5 or v70) · HERO-1b after `HERO1:` · P-1. Order: hivemind 5 = 4 M + 1 A (computed from porcelain inside the splice). ctx: beat 4 · calls ≈80 · read ≈420 KB · wrote ≈165 KB

**Leverage line:** the instruction was cut to what the log already proves, so nothing in it waits on a design; the red is held as evidence, not re-run away.
**The alternative shape, rejected:** dispatching EXPLAIN tonight over the red — the rule against issuing over an unresolved gate exists for exactly this evening.
**2029 test:** every claim in the instruction carries a file and a line at a named HEAD; the deferred keys are named with the reason each waits.

## 2026-09-11 (v69 beat 3 — HERO-1 returned and audited ACCEPT; the landing cards, the ruling batch and the EXPLAIN word handed; four v67 blocks rotated, Fri 2026-09-11 ~16:2x CT (instrument 2026-09-11T21:28:56Z))

**State:** hivemind `19dd310` with beat 2 uncommitted (the charter, the lane's return); core `eabdbb1` with `?? web-ui/dashboard/design/`. The lane opened 20:48Z and returned within the hour. H8-a is at 19:00 CT tonight.

**HERO-1 (`context/audits/2026-09-11_HERO-1_return.md`, 7,294 B) — ACCEPT** (`context/audits/2026-09-11_HERO-1_intake_two-layer-audit_v69-b3.md`). At the bytes: the three files' sizes and md5s match; the name grep finds only `{{NAME}}`; the DAS banned list finds nothing in the 140-string copy table; the three moved line cites re-read at `eabdbb1`; `states.html` rendered in headless Chromium — the five modes distinct by label and glyph in both themes, 0 scripts, 94 cards. Two observations for the build lane and Q7 for Nick (ratify "the hub" as the UI term; five strings at HEAD use it, the Glossary has no entry). Register: DESIGN-COMPLETE, BUILD PENDING.

**Handed:** card 1 lands the design on core (`git add -A web-ui/dashboard/design`, 0 M + 3 A); card 2 lands beats 2–3 on hivemind (4 M + 4 A); then `HERO1:` Q1–Q7 (the hub recommends (a) on each) and `EXPLAIN: three | one` — the ten keys read as three compile-and-commit units (114a the keys the emitter already holds · 114b parent run id, condition text, the stable definition key · 114c per-permit trigger refs), one bump each; three is the default. **Rotated:** v67 b7 → b4 (4 blocks, 9729 B) to `archive/pm-handoff-beats-v67b4-v67b7-rotated-2026-09-11.md`; 9 live blocks after this beat. **Next:** b4 the EXPLAIN-114a instruction · the H8-a paste at 18:45 by the send · R-4c's packet · P-1's charter to v70 if the budget closes first. Order: hivemind 8 = 4 M + 4 A (computed from porcelain inside the splice). ctx: beat 3 · calls ≈64 · read ≈345 KB · wrote ≈125 KB

**Leverage line:** one paste from Nick bought a design of record; the build lane inherits 140 strings and 30 sentences it will not have to invent.
**The alternative shape, rejected:** landing the design without the render — the modes' distinctness is the one claim only a picture can check.
**2029 test:** the audit names every check and its result; the spec sits beside the code it governs.

## 2026-09-11 (v69 beat 2 — beat 1 landed by Nick's hands; PROTECT scoped to core + docs; HERO-1, the hero design lane, chartered and handed, Thu 2026-09-10 ~22:0x CT (instrument 2026-09-11T03:05:28Z))

**Landed:** hivemind `695cc07..19dd310` by Nick's card (M=11 A=1 at porcelain; the message grepped clean of trailers; porcelain 0 after). **Nick's word on PROTECT:** the ruleset covers homesynapse-core and homesynapse-core-docs only — the two repositories that will be public (the docs feed the site); the six clicks for both were handed in chat; `PROTECT: done` is owed, not gating. **HERO-1 chartered** (`context/instructions/2026-09-10_design-lane_HERO-1_explainability-hero_design-charter.md`, 16345 B): a frontend lane in design mode, run beside EXPLAIN in the web-ui slot; it writes only `web-ui/dashboard/design/hero-v1/` (SPEC.md, states.html, README.md) and nothing under `src/`. The spec starts from HERO-0's null census and four empty states, owns the L1 headline grammar per RunStatus × ActionOutcome and per NonFiringVerdict (FE-NULL-1's O1), renders the five confirmation modes distinct, and shows each EXPLAIN-1..9 surface twice — today's honest sentence and the sentence once the v1.1.4 key lands. `{{NAME}}` is the only name; the frontend skill's brand reference §6.1 (an older candidate) is named as superseded by the spine. Four predictions pre-registered; seven audit rules pre-filed; Nick rules on SPEC §12 in one batch after the audit. **Nick's one act:** the hivemind card, then the HERO-1 paste. **Next:** b3 the EXPLAIN v1.1.4 instruction (the deliverable) · b4 R-4c's packet · b5 P-1's charter. Order: hivemind 5 = 4 M + 1 A (computed from porcelain inside the splice). ctx: beat 2 · calls ≈42 · read ≈220 KB · wrote ≈80 KB

**Leverage line:** the design lane costs Nick one paste and runs in the slot EXPLAIN does not use; when v1.1.4 lands, the hero already knows what to say with each key.
**The alternative shape, rejected:** authoring EXPLAIN first and the design after — the design has no dependency on the code and the slot was idle; ordering it second would have idled a lane for a day.
**2029 test:** the charter names its baseline, its write-set, its predictions and its audit rules; the spec it produces will sit beside the code it governs.

## 2026-09-11 (v69 beat 1 — the v69 boot: intake at the bytes; the H8-a send re-armed; the map re-cut on Nick's three edits; Nick's two words taken — no trailers, and the hub never commits, Thu 2026-09-10 ~21:0x CT (boot 2026-09-11T01:00:18Z; closed 2026-09-11T02:04:02Z))

**The window's one deliverable:** the EXPLAIN v1.1.4 coding instruction (slot 1; dispatches Fri). **Intake:** dispatch + paste filed verbatim in `context/audits/2026-09-10_v69_beat-1_intake_dispatch-verbatim_HEADs_and_preflight.md`. Five HEADs at the instrument: core `eabdbb1` · docs `876a395` · hivemind `695cc07` · skills `c630c5c` · bench `4539f13`; porcelain 0 and ahead 0 in all five; no locks; both slots free. **Preflight 12/12** (read-set 40.3 KB; Check 9 28/28; Check 12: HONESTY-1 and FE-NULL-1 still ISSUE-READY after landing — set to EXECUTED; the H8-a packet's status names its Friday slot, body unchanged). **The H8-a send:** `trig_01AHRS3Q29MtP6q39yT24ogs` fires 2026-09-11T23:45Z (18:45 CT). **The map** re-cut: HONESTY-1 and FE-NULL-1 landed; R-4c measurement-only; P-1 directly after R-4c; H8-a Fri 19:00 CT.

**Nick's two words (~20:4x, ~21:0x CT):** (1) no attribution trailers — the first beat-1 message carried them; withdrawn before it ran. Arc 7 has forbidden them since 2026-07-19; a 2026-09-03 sentence in SKILL.md §1 and the prompt §1.5 had treated the harness's reminder as a rule. Sweep: 62 hivemind, 2 skills, 4 core commits carry trailers; history stays. Fixed here: SKILL.md §1/§8, the prompt §0/§1.5, two lessons (the mechanism; the register of record — DAS §2 applies to the hivemind's prose). (2) the hub never commits or pushes, in any repo: the commit-boundary law re-cut in SKILL.md and the prompt; every landing is a card for his hands. Check 9 STALE until the mirror sync. Detail: audit §6. **Nick's one act:** land this beat, then the PROTECT card. **Next:** b2 the hero charter · b3 EXPLAIN · b4 R-4c · b5 P-1. Order: hivemind 12 = 11 M + 1 A (computed from porcelain inside the splice). ctx: beat 1 · calls ≈26 · read ≈155 KB · wrote ≈45 KB

**Leverage line:** a rule broken 68 times and a practice that drifted past the operator's contract, both closed at the source before the first authoring act.
**The alternative shape, rejected:** commit first, fix the skill later — that commit would have been the 69th instance.
**2029 test:** the trigger id, the flips, the sweep counts and the corrected sentences are in files.

## 2026-09-11 (v68 beat 6 — THE v68 CLOSE ON CONTEXT HEALTH: FE-NULL-1 LANDED `eabdbb1` (frontend GREEN); P4 MET; OR-HONESTY-1-GATE CLOSED; ACT 12 RETIRED; NICK'S WORDS BANKED; v69 DISPATCHED, Thu ~19:5x CT (instrument 2026-09-11T00:52:21Z) — **BANKED: core `3f3f5cc..eabdbb1` (8 files, web-ui only); hivemind `8717fe1..062080d` pushed; four checks green on `eabdbb1`; install-smoke RAN BY ITSELF on `94ae99d` + `eabdbb1` → P4 MET; the gate CLOSED + rotated; Act 12 RETIRED · **NICK'S WORDS:** `DESIGN: start` (the hero design lane, slot 2, `{{NAME}}`, first row = the L1 headline per outcome — BESIDE EXPLAIN) · `PROTECT: no-force` (block force-pushes + deletions on `main`; no PR rule; his act, owed) · `SAMPLES: passive` (veto samples + stale owed lines RETIRED) · V69 edits: R-4c MEASUREMENT-ONLY (EXPLAIN dispatches Fri) · P-1 directly after R-4c · the H8-a fallback (Nick opens the packet at 19:00 if v69 is not booted by 18:45) · v68's send_later CANCELLED · BUS-SILENT-DROP 8/20 (`94ae99d` · `3f3f5cc` · `eabdbb1`) · one lesson minted (the same-path re-write) · **THE WINDOW MEASURED:** boot 37.6 KB · 6 beats · ≈115 calls · ≈270 KB read · ≈125 KB written · NO compaction · the deliverable shipped at b1 + two lanes LANDED · **DoD:** [x] returns on disk, audited, filed [x] landings census-exact, pushed, CI banked [x] MODULE_CONTEXT by the lanes [x] the gate opened → closed → rotated [x] caps held; 7 blocks rotated at b2 [x] commits census-exact, porcelain 0 [x] Check 9 28/28 [x] the next WU named: **v69 = the EXPLAIN v1.1.4 instruction** · NICK'S ONE ACT: push the close + paste the v69 line · Order: hivemind 6 = 6 M + 0 A (computed from porcelain inside the splice). · ctx: beat 6 · calls ≈115 · read ≈270 KB · wrote ≈125 KB (the session's whole)**)

**Leverage line:** one operator, one evening, under twenty minutes hands-on — two lanes landed, four gates green, the brand sent, and the next window's list written by the operator's own words.
**The alternative shape, rejected:** running v68 to beat 8 to author EXPLAIN here — the biggest WU of the month deserves a fresh budget; the law closes on the deliverable, not on a cliff.
**2029 test:** Nick's words are recorded with their consequences; the retired samples, the closed gate and the rotated risk each leave a pointer, not a gap.

## 2026-09-11 (v68 beat 5 — HONESTY-1 LANDED `94ae99d` ci GREEN; FE-NULL-1 RETURNED AND AUDITED ACCEPT; THE DEPENDABOT BUMP `3f3f5cc` BANKED; THE FE LANDING CARD HANDED; THE WINDOW'S CLOSE NAMED, Thu ~19:2x CT (instrument 2026-09-11T00:25:32Z) — **BANKED: HONESTY-1 `39c8dd3..94ae99d` PUSHED, `ci` GREEN — OR-HONESTY-1-GATE half-closed; **P4 (`install-smoke` started by itself?) still Nick's one line** · a stranded `index.lock` blocked his first commit — the FE lane's `git status` without `--no-optional-locks` (the hub's line omitted the flag; owned) · Dependabot #15/#16 (`@vitest/mocker` path traversal, moderate, dev-only) MERGED `3f3f5cc` (vitest `^4.1.11`), CI green, pulled — core HEAD; no act · **FE-NULL-1 RETURNED 00:0xZ (11,218 B) · ACCEPT** (`context/audits/2026-09-10_FE-NULL-1_intake_two-layer-audit_v68-b5.md`): Layer 2 at the bytes — 8 M all under `web-ui/dashboard/` · the four types `| null` (`contract.ts` :322/:333/:340/:343) · the validators (`shapes.ts` :259/:278/:290/:291) · the mock's five null arms · the HERO-0 sentences in `format.ts` · fixtures md5 unchanged · no version bump · not re-executed (disclosed): `npm run verify` (307 tests, the lane's log) · RULINGS: S3 `[DELTA-headline]` ACCEPTED (the honesty law reaches the headline) · O1 → the hero charter's FIRST ROW (the L1 sentence per outcome; SKIPPED headlines "acted" until then) · O3 recorded for W-SKILLS-9 · the vitest bump beneath the work is re-verified by `frontend.yml` · NICK'S ONE ACT: the FE-NULL-1 landing (`git add -A web-ui/dashboard` on `3f3f5cc`; the msg file on disk) + the hivemind push; one line back with the P4 word · THE WINDOW: beat 5 of ≤8; the close is beat 6 — v69 = the EXPLAIN v1.1.4 instruction · Order: hivemind 6 = 4 M + 2 A (computed from porcelain inside the splice). · ctx: beat 5 · calls ≈100 · read ≈250 KB · wrote ≈115 KB**)

**Leverage line:** two lanes opened at 18:00 and both returned by 19:00 — one landed, one on the card — while the operator's total hands-on time was under fifteen minutes.
**The alternative shape, rejected:** fixing O1 with the offered one-line interim before landing — it would pin a headline sentence the hero charter is about to own; the null arms are strictly more honest than HEAD and land now.
**2029 test:** the Dependabot pair, the stranded lock and the vitest bump beneath an open lane are each one recorded fact with its cause; none lives only in chat.

## 2026-09-10 (v68 beat 4 — HONESTY-1 RETURNED AND AUDITED ACCEPT; R1 APPLIED; THE LANDING CARD HANDED; FE-NULL-1 RUNNING, Thu ~18:4x CT (instrument 2026-09-10T23:39:37Z) — **HONESTY-1 RETURNED 23:16Z (9,816 B; the RETURNED line in the file AND printed) — the audit `context/audits/2026-09-10_HONESTY-1_intake_two-layer-audit_v68-b4.md`: Layer 2 VERIFIED at the bytes — 13 M exactly (`web-ui/` 0) · A `initialEntityState` seeds `seed, seed, null, null, false` · B `publish(…, origin, actorRef)` replaces the hard-coded `AUTOMATION, null`; the primitive stamps `UNKNOWN` · C the `commandEventId` javadoc · D the push `paths:` gone, the pair identical · XMLs 23:06Z: 165/211/413, 0 failures; red-first on a VALUE · not re-executed (disclosed): the Gradle run, `-Werror` freshness, spotless on R1 · RULINGS: **R1 APPLIED by the hub's guarded splice** (`EntityState.java` :73–:74; core now **14 M**) · **O2 → LASTREPORTED-1b DOCKETED, instrument-first (arc 28):** R-4c reads `lastReported` on one never-reported entity after the `.deb`; a `projectionVersion` bump only if the stamp persists · O5 STANDS (two params, no record) · I3 recorded (G4 = readers) · P4 banks on the push; Act 12 retires on it · the deferred gate → Open Risks `OR-HONESTY-1-GATE` · FE-NULL-1 LAUNCHED (Thu ~18:2x CT; web-ui porcelain 0 at 23:33Z) · NICK'S ONE ACT: the HONESTY-1 landing card (the 14 paths by name; `_scratch/2026-09-10_core_HONESTY-1_commit-msg.txt`, 782 B, no trailers); one line back `HONESTY-1: <sha> · ci <verdict> · install-smoke <ran|did not run>` · THE HUB'S NEXT: EXPLAIN v1.1.4's instruction + R-4c's · Order: hivemind 7 = 5 M + 2 A (computed from porcelain inside the splice; coder-handoff.md is the lane's write, staged with its audit). · ctx: beat 4 · calls ≈88 · read ≈205 KB · wrote ≈95 KB**)

**Leverage line:** the honesty batch closed in one evening because the instruction pre-registered its reds and the audit re-read them at the XML — the record, not the transcript, got read.
**The alternative shape, rejected:** ordering the `projectionVersion` bump now — a theory about the fleet's checkpoint before the wire has been read; R-4c reads it for free on Saturday.
**2029 test:** every value this WU corrected is named with the source line that lied and the test that now guards it; the one javadoc that would have kept lying was fixed in the same commit.

## 2026-09-10 (v68 beat 3 — THE CLOSE CARD BANKED AT PORCELAIN; HONESTY-1 RUNNING; FE-NULL-1 AUTHORED FOR THE SECOND SLOT, Thu ~18:1x CT (instrument 2026-09-10T23:04:06Z) — **BANKED (Nick's transcript, verified at the instrument 22:55Z): the stale core `index.lock` swept · hivemind `d5bce6e..8717fe1` PUSHED (ahead 0) · docs `876a395` (AMD-53 §1.5) PUSHED `8262a3c..876a395` (ahead 0) · HONESTY-1's Claude Code session LAUNCHED by Nick (Thu ~17:5x CT; core porcelain 0 at 22:55Z; verified only at its return file) · NICK'S WORD: available all week and weekend; aligned with the map → H8-a defaults Fri 09-11 19:00 CT (`H8:` to move); R-4c Sat · THE SECOND SLOT OPENED: `context/instructions/2026-09-10_frontend-lane_FE-NULL-1_chain-null-arms_fast-follow_instruction.md` (ISSUE-READY; 17,083 B) — HERO-0's F2 four chain keys (`trigger.subjectRef` · `observedState[].value` · `actions[].command` · `actions[].targetRef`) REQUIRED-NULLABLE in the mirror + F4's fifth row (depth > 0, null parent); every emitter line re-read at `39c8dd3` (`StandardExplanationService` ~:644–:649 · ~:771 · ~:776; `RunExplanation` :137 · :213–:219); the mirror's lie located (`shapes.ts` :256/:272 REJECT lawful null; `targetRef` never validated); no version bump; P1 = 8 M + 0 A; D4 made operational (porcelain `-- web-ui/dashboard`; never `core/**`; never `coder-handoff.md`; landing = `git add -A web-ui/dashboard`) · NICK'S ONE ACT: the FE-NULL-1 paste into a FRESH Cowork conversation (5 min); one line back `FE-NULL-1: dispatched <hh:mm>`; HONESTY-1's `RETURNED` line whenever it comes · THE HUB'S NEXT: R-4c's instruction (Sat) while both lanes run; HONESTY-1's audit + landing card on its return · Order: hivemind 5 = 4 M + 1 A (computed from porcelain inside the splice). · ctx: beat 3 · calls ≈75 · read ≈150 KB · wrote ≈82 KB**)

**Leverage line:** the second slot filled the same evening as the first — two lanes on one tree, disjoint by path, each with its own gate; throughput doubled without a second operator.
**The alternative shape, rejected:** waiting for HONESTY-1 to land before opening the FE lane — D4 exists so that the web-ui domain never waits on Java; the only shared file (`coder-handoff.md`) is fenced by the instruction.
**2029 test:** the mirror's four lies are recorded with the emitter lines that prove them and the sentences that replace them; the lane cannot land a mock that hides a null again.

## 2026-09-10 (v68 beat 2 — NICK'S WORDS BANKED: THE EAR TEST PASSED, THE PACKET SENT; THE MOMENTUM MAP; THE CLOSE CARD HANDED WHOLE, Thu ~16:5x CT (instrument 2026-09-10T21:54:15Z) — **BANKED (Nick, Thu): `EAR-PALOKI: pass` ("good enough to execute on; momentum and execution over more hours and roadblocks") · the Erik email SENT Thu ~12:00 CT with both attachments → the brand is a WAIT-STATE (his sight-read on OKI/LOKI → his date → the opinion by 09-18, or his own expedite); `SEARCH-NAME: PALOKI` FINAL · THE HUB'S HONEST READ, banked: agree — the record supports executing (clean where it matters; costs known and managed; the ear passed); the name is made by the product; the one constraint (no public use before the opinion) costs nothing · THE INSTRUMENT, 21:48Z: hivemind `380c1fb` 8 ahead · docs `8262a3c` + 1 M · core `39c8dd3` porcelain 0 with the same 0-B `index.lock` → the close card STILL NOT RUN; HONESTY-1 NOT dispatched · THE MAP RE-CUT (ritual 1) at the brand landing: `context/planning/2026-09-10_v68_MOMENTUM-MAP_two-weeks_critical-path-and-Nicks-hours.md` — one critical path (HONESTY-1 → R-4c Sat → EXPLAIN v1.1.4 → P-1 → R-5 → the 72-h run → 11-25), two lanes on disjoint domains, Nick's hours by day, the compounding per block · SPINE ROTATION: 7 blocks (v67 b3 → v66 b7) VERBATIM → `archive/pm-handoff-beats-v66b7-v67b3-rotated-2026-09-10.md`, bytes asserted equal; 6 live · NICK'S ONE ACT (the whole paste in §NEXT): the close card — sweep the stale core lock · push hivemind · the docs AMD-53 commit · HONESTY-1's paste into a host-side Claude Code session; one line back `close: hivemind <sha> · docs <sha> · HONESTY-1 dispatched <hh:mm>` · THE HUB'S NEXT while HONESTY-1 runs: R-4c's instruction (Sat) + FE-NULL-1's (web-ui; the second slot) · Order: hivemind 6 = 4 M + 2 A (computed from porcelain inside the splice). · ctx: beat 2 · calls ≈58 · read ≈120 KB · wrote ≈62 KB**)

**Leverage line:** two lanes on disjoint domains double the desk's throughput at zero coordination cost — D4 was written for this evening.
**The alternative shape, rejected:** EXPLAIN v1.1.4 first because it is the differentiator — the map §6: it would sit on un-landed honesty keys and a tree the `.deb` job does not yet build from.
**2029 test:** the brand pivot closed on Nick's own ear and his words, both filed; the program resumed on a written map with its compounding stated per block, not on a mood.

## 2026-09-10 (v68 beat 1 — THE v68 BOOT + THE PALOKI PACKET AUTHORED AND HANDED, Wed ~19:0x CT (instrument 2026-09-10T00:24:42Z) — **THE INTAKE (the record wins over the dispatch line's blank slots): hivemind `f7ec0f1` 7 AHEAD (NOT pushed) · docs `8262a3c` + 1 ` M` (AMD-53 §1.5 NOT committed) · core `39c8dd3` porcelain 0 + a 0-B `.git/index.lock` at 23:55Z (HONESTY-1's dispatch UNVERIFIED) · skills `c630c5c` · bench `4539f13` · `EAR-PALOKI:` NOT GIVEN → the close card's steps 1–3 re-queued as Act 2 · PREFLIGHT 12/12 after one reconciliation (C9 28/28; C12.1: the HIVE-CLEAN-1 + W-SKILLS-8 dispatch files, LANDED, → EXECUTED) · boot read 37.6 KB · NICK'S MID-TURN NOTE = Erik's 09-05 reply verbatim ($1,200 · 5 business days · expedite "a few hundred") — the quote the v67 b6 ruling stands on; nothing new in fact; four days unanswered · THE WINDOW'S ONE DELIVERABLE AUTHORED: the PALOKI commissioning packet `context/strategy/brand-program/PALOKI_Search_Packet_2026-09-09.{pdf,docx,md}` (8 pp: §1 the engagement · §2 the embedded strings on sight — OKI to price, the LOKI wording + the TALLOKI goods caveat, PALO · §3 the read order · A1–A5 from RS-13 §A, the DuPont walk, ★1–11, the unreached axes as asks) + the ONE paste `2026-09-09_ERIK-EMAIL_PALOKI_short-body_with-attachments.txt` · EXPEDITE RULED IN THE BODY (DELEGATE; `REVERT EXPEDITE`): standard turnaround, the opinion wanted by Fri 09-18, Erik takes the expedite himself only if his queue misses it — a date rule, not a second round-trip · the intake filed: `context/audits/2026-09-10_v68_beat-1_intake_dispatch-verbatim_HEADs_preflight_and_EXPEDITE-ruling.md` (§6 = the H10) · NICK'S ONE ACT: the ear test → on pass, the send; one line back `EAR-PALOKI: pass <n> · SENT <hh:mm>` · Order: hivemind 11 = 6 M + 5 A (computed from porcelain inside the splice). · ctx: beat 1 · calls ≈45 · read ≈100 KB · wrote ≈50 KB**)

**Leverage line:** the record answered Nick's question before he asked it — the ruling, the quote and the shape were on disk; this beat built the file and folded the ear test into the send.
**The alternative shape, rejected:** a holding reply to Erik tonight and the packet tomorrow — two emails and a name sent before its gate; the ear test is five minutes; the packet is done.
**2029 test:** the change of candidate is one sentence in the file Erik receives; the expedite decision is a written rule with its refuter, not a mood.

> **Rotated 2026-09-07 (v66 beat 10):** the 95 blocks v65 beat 5 → v54 beat 8 (2026-09-05 → 2026-08-21) live VERBATIM in `archive/pm-handoff-beats-v54b8-v65b5-rotated-2026-09-07.md`; the archive map at the tail carries every earlier rotation.
> **Rotated 2026-09-07 (v67 beat 1):** the 8 blocks v66 beat 6 → v65 beat 6 (2026-09-06) live VERBATIM in `archive/pm-handoff-beats-v65b6-v66b6-rotated-2026-09-07.md`; bytes asserted equal by the splice.
> **Rotated 2026-09-10 (v68 beat 2):** the 7 blocks v67 beat 3 → v66 beat 7 (2026-09-07 → 2026-09-06) live VERBATIM in `archive/pm-handoff-beats-v66b7-v67b3-rotated-2026-09-10.md`; bytes asserted equal by the splice.
> **Rotated 2026-09-11 (v69 beat 3):** the 4 blocks v67 b7 → v67 b4 (2026-09-09 → 2026-09-07) → `archive/pm-handoff-beats-v67b4-v67b7-rotated-2026-09-11.md`, verbatim, bytes asserted equal.


## Open Risks

#### OR-NIGHTLY-0902-S31 — the bench floor breached: the 2026-09-02 nightly read 7/9 with FAIL command-confirm-s31 after four 8/9 nights — [OPEN 2026-09-03, v61 beat 10; source: E2, Nick's paste of `~/hs-bench/digests/nightly.log`]
- **The risk:** the floor fence (8/9) is the standing evidence that the certified bench build confirms commands on real hardware; a breach is either a repeat of the 08-02 intermittent (adjudicated once: CONFIRMATION_TIMED_OUT at ~5.1 s, the app alive, the device back a minute later) or a new mechanism. No theory is on record and none is allowed before the bundle read (arc-discipline 28).
- **The ordered act (read-only; the s31/nightly HANDS-OFF fence stands until R-5):** the evidence-read session `context/instructions/2026-09-03_evidence-read_s31-nightly-0902_session_prompt.md` — five hypotheses pre-filed (H-A device silence · H-B never dispatched · H-C late confirmation · H-D infrastructure collateral · H-E the 08-02 class repeats); returns at `context/audits/2026-09-03_s31-nightly-0902_evidence-read_return.md`; the hub audits two-layer at intake.
- **Rider:** the 2026-09-03 nightly MISSED (the held card in the Pi overnight; no bundle, no digest line) — a recorded miss, not a suite defect; the bench card is back (~06:10 CT 09-03) and tonight's run fires normally; its floor line banks at the next beat.
- **Owner:** Nick (launch) / hub (audit; a row for the R-5 charter if the class repeats, its own row if a new mechanism). **Closes when:** the return is audited and the class adjudicated, and the next nightly reads 8/9.
- **Status 2026-09-03 (v61 beat 11) — ADJUDICATED, rows pending.** The evidence read returned and audited ACCEPT (`context/audits/2026-09-03_intake_two-layer-audit_v61-beat-11_s31-read+PKG-SEC-2.md` Part A): H-D — transport collateral from the suite's own `usb-reenumeration` leg (port kill → NCP reset → reopen 6.4 s before the s31 command); the hub verified at source that `attemptReopen()` (:841–:874) calls `resumeStored()` without `awaitNetworkUp()` — the loop resumes before NETWORK_UP and nothing logs the unicast accept/reject. **The rider above is CORRECTED: the 09-03 nightly was NOT missed — it fired late (~11:12Z, a catch-up on the card's return) and PASSED 8/9; the floor is back.** Candidate rows for Nick's word (H10, v62): REOPEN-NETUP (Core) · S31-SETTLE-AFTER-REENUM (bench, at R-5). **Closes when** both rows are worded and the class re-reads 8/9 across the following nights.

#### OR-FAILCHAN — healthy outcomes reported through failure channels (a trust-erosion class) — [OPEN 2026-08-30, v58 beat 6; source: the R-3a hub-return §2.3]
- **The class — four instances in one day, plus the fixed KEYb sibling:** a graceful operator-requested stop exits 143 → systemd marks the unit `failed` (§6-B; root-correct fix = exit 0 on the caught-SIGTERM shutdown path — the record §6 mechanism note; a unit-side `SuccessExitStatus=143` would mask a genuine kill) · an orderly serial close emits `transport_failed` carrying F-S16’s "physical port loss" signature (§10-O) · `key_establishment_failed` for `0xFFFFFFFFFFFFFFFF` (§10-M — CAUSE OPEN; the emission is a faithful 0x009B relay, source-read v58 b6) · a device with no recorded report time renders `Current` (§10-I; read-path). **Never-false-ALIVE guards against claiming health that is not there; nothing guards against claiming failure that is not there — the second error costs operator trust as fast.**
- **Owner:** hub (the sweep WU charters at the September plan; §6-B is the first candidate) / Coder. **Closes when:** the sweep lands and each instance is re-classified or fixed, verified on a card.
- **Status 2026-09-03 (v62 beat 1) — a FIFTH instance, source-verified (the mirror of §6-B): EXITCODE-UNWIRED.** `app/homesynapse-app/src/main/java/com/homesynapse/app/ExitCode.java:25` defines `CONFIGURATION_FAILURE(10)` (+11 PERSISTENCE · 12 EVENT_BUS · 13 SUBSYSTEM_INIT_TIMEOUT · 99 UNEXPECTED) and nothing in main sources references it; `Main.main` is `throws Exception` (:63) with `manager.start()` unwrapped (:148); `HomeSynapseCore.start()` :441–:448 logs the fatal, tears down and re-throws → the JVM exits 1. The unit's `RestartPreventExitStatus=10` (`distribution/systemd/homesynapse.service:66`) therefore never matches and a deterministic bad config restart-loops (`RestartSec=10`) to `StartLimitBurst=5`/300 s — the loop the unit's own comment (:61–:63) says it prevents. Doc 12 C12-04 (:457) is half-built: the structured log line exists, the exit code does not. **Disposition:** rides the FAILCHAN charter (v62 Block 2) as its own classification row; H10 in the v62 brief §G-1 (rec (a): catch at `main`, map FATAL subsystem → `ExitCode`, `System.exit(code)` after teardown). Evidence: `context/audits/2026-09-03_v62-b1_boot-grounding_docs-and-strategy_validation.md` §1.1.
- **Status 2026-09-04 (v62 beat 2) — CHARTERED.** Row 6 RULED (a) by ADOPT-ALL-RECS (09-03) and EXITCODE RULED (a) the same evening; the FAILCHAN coding instruction authors at v62 beat 3 with the H10 reconciliation inside (the §6 note's app-side exit 0 vs the unit-line rec; `Restart=` ruling) and Nick's caveat as a constraint (`System.exit(code)` in `main` after `start()` threw and teardown ran — never in a shutdown hook; the exit behind a thin seam so the FATAL-subsystem → code mapping is unit-tested and the process exit is not). Dispatch: Nick's Coder lane tonight (one lane on the core tree). **Closes when** the sweep lands, CI green, each instance re-classified or fixed, and §6-B + EXITCODE verified on a card (R-4b's free rider if CI is green by Sat 08:00 CT, else the next card session).
- **Status 2026-09-04 (v62 beat 4) — THE SWEEP RETURNED + AUDITED ACCEPT-WITH-RULINGS (`context/audits/2026-09-04_FAILCHAN_intake_two-layer-audit_v62-beat-4.md`); LANDING PENDING Nick's commit + CI.** §6-B FIXED (`SuccessExitStatus=143` + `Restart=always` + the lint + run-smoke check 8) · instance 5 EXITCODE-UNWIRED FIXED (the report beside the throw; the exit in `main` behind the SIGTERM guard) · §10-O FIXED (both catches consult `stopSignal` before the watchdog; INFO `zigbee.transport_closed_orderly`) · §10-M KEEP-WITH-REASON (the status is `0x32 TC_REQUESTER_VERIFY_KEY_TIMEOUT`, the all-ones partner the null EUI64; the cause stays OPEN — the §6 line above's "0x009B" was the frame id) · §10-I ROUTED to §10-J. **Instance 6 (NEW, LOW, residual):** a `close()` landing between the loop check and the next `pumpInbound` reaches `requireOpen()`/`requireConnected()` → an `IllegalStateException` at `run()`'s boundary at stop time (not yet evidenced on a card; recorded in the zigbee MODULE_CONTEXT) — rides Row 7's WU as a rider. **Closes when** CI is green on the FAILCHAN commit AND a card's first `systemctl stop` reads `inactive`/`success` (R-4b today if guard 1 selects the artifact; else the next card session) AND instance 6 is classified in Row 7's WU.
- **Status 2026-09-04 (v62 beat 6) — LANDED ON MAIN `7af2d6c` (07:54 CT; 19 files exact); CI #225 RED** — `:lifecycle:lifecycle:test` 79/1 failed: `HeroLoopHardwareFreeIT` "timed out awaiting the dispatched On frame" (`:614`, the 10 s `awaitTrue` poll); local full-clean re-run GREEN; prior reds on `main` in the Actions history (CI #169/#183/#206). The FAILCHAN sweep itself is on `main`; **the deferred gate is OPEN on `7af2d6c`** (the skill §5 rule: no other core lane before the fix WU). Mechanism un-adjudicated at this beat — candidates: the loop parked unhealthy on the runner vs the dispatch never reaching the adapter; the next instrument is the class stdout's token census (the brief §A2); **FAILCHAN-FIX-1 authors on it.** The hardware proof of §6-B waits for the fixed commit's artifact (R-4b runs on `ef02d13`'s).
- **Status 2026-09-04 (v62 beat 7) — §6-B CONFIRMED ON A SECOND ARTIFACT:** R-4b's §9 clean stop on `ef02d13` read `Result=exit-code · ExecMainStatus=143 · ActiveState=failed` (20:25:45Z; the R-4b record §9) — the lie is artifact-independent, as the mechanism says. The FAILCHAN proof on hardware waits on FAILCHAN-FIX-1's green CI and the next card session.
- **Status 2026-09-04 (v62 beat 8, the close) — THE §A2 CENSUS READ; FAILCHAN-FIX-1 RULED INSTRUMENT-FIRST.** Nick's census of CI #225's class stdout: no `<N>` prefix (H-prefix refuted) · boot tokens ×5 = one boot per `@Test` (five methods: `:90 :165 :224 :269 :318`) · adoption tokens ×10 (two rig devices × five boots) · `zigbee.command_result` ×1 · `integration.route_join_miss` ×1 · the tail a clean boot then Javalin stopping ~120 ms after RUNNING. Four of the five methods await a sent frame and PASSED; only `timeoutHonesty_noReportNeverConfirms` (`:318`) lost its On frame (`:324`) after `command_issued(turn_on)` was found — H-dispatch ("never left the engine in four of five") is UNVERIFIED and per-class counts cannot decide it. **The fix WU: Part A = I-1 (the prior reds' identity, #169/#183/#206) · I-2 (the per-method XML split) · I-3 (20× under CPU pressure on `7af2d6c`) · I-4 (the §10-O hunk reverted in a scratch worktree); Part B on the branch — (a) pre-existing determinism defect → fix the wait's mechanism · (b) the guard → fix the guard, keep §10-O's intent · (c) CI-only → the fix branch's CI instrumented (`outputPerTestCase`).** Authors at v63 beat 1 on the census FILED to `context/audits/2026-09-04_CI-225_HeroLoopHardwareFreeIT_stdout-census.txt`. The gate stays OPEN; CG behind the green.
- **Status 2026-09-04 (v63 beat 1) — `main` RED A SECOND TIME ON IDENTICAL JAVA BYTES; FAILCHAN-FIX-1 RE-CUT TO THE CLASS AND AUTHORED.** `dc3328b`'s run (Nick's PR #5 merge; Java bytes = `7af2d6c`) failed `ReplayTransitionIT:212` in `core:event-bus` — a module FAILCHAN never touched — and never reached lifecycle; PR #5's run on the same bytes was GREEN (`8e6e0e1`'s parent is `7af2d6c`: I-0 answered). The §10-O hunk is unreachable in the rig's driven mode (`productionLoop()` never runs) — the guard-regression branch (b) is refuted at source for the adapter; the census token `route_join_miss ×1` is a skipped `command_issued` delivery on `integration_supervisor` in LIVE mode (`CommandRoutingSubscriber:181–:198`). The mechanism candidates and the instruments: the grounding audit §4 + the instruction (`context/instructions/2026-09-04_coder-lane_FAILCHAN-FIX-1_CI-nondeterminism_coding-instruction.md`; two commits, one lane; FIX-1b's push run clears). **The hardware proof of §6-B + EXITCODE rides the next card session on FIX-1b's artifact.**
- **Status 2026-09-05 (v63 beat 2) — I-1 READ (Nick, verbatim in the grounding audit §7-A): BRANCH (a) CONFIRMED FOR `HeroLoopHardwareFreeIT` BEFORE THE LOOPS.** #206 failed the hero-loop method (`:613`), #183 failed brightness AND color-temperature in ONE run (`:562` ×2), #169 was a deterministic testFixtures compile miss (not the class) — four different methods across three pre-FAILCHAN runs plus #225, every one at an await helper's throw: pre-existing, method-agnostic, delivery-path. FAILCHAN's lifecycle hunks are not the cause; the lane's `ef02d13` loop is now a rate baseline only. The census file committed (two ephemeral CI test-token values redacted per `TOKLEN-OK`; the `route_join_miss` WARN line lies outside its 150-line tail). Nick's words: `FIX1: class` (with the guard: the lane narrows from the §A.5 table if the two tests fail for unrelated reasons) · `SAMPLES: veto-only`. **The FIX-1 lane DISPATCHED 09-05 ~12:xx CT; the wait-state: the return at `context/audits/2026-09-05_FIX-1_return.md`.** Act 3 (the `dc3328b` ReplayTransitionIT message from the `test-reports` artifact) still owed.
- **Status 2026-09-05 (v64 beat 1) — THE LANE RUNNING; A STAMP DEFECT IN THE INSTRUCTION FOUND AT SOURCE, THE CORRECTION HANDED.** At 18:25Z the lane's tree read exactly FIX-1a's census (9 = 7 M + 2 A); the loops next. The hub read `ReplayTransitionIT.awaitCheckpoint` (`:200–:214`): its throw prints target + timeout, never the resting checkpoint — the §A.5 ReplayIT diagnostic is unmeasurable as instrumented. Act A (the v64 brief) hands the lane a one-stamp correction (`+ " (resting checkpoint " + store.readCheckpoint(subscriberId) + ")"`; FIX-1a's census → 10 = 8 M + 2 A, declared `[INFO]`). The Block-1 adjudication rules R1–R6 are pre-filed (`context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §4): two mechanisms expected; branch (c) clears the gate but not OR-BUS-SILENT-DROP; the corpus ≥110 runs; redacted before staged; mismatches first. The wait-state stands: the return at `context/audits/2026-09-05_FIX-1_return.md` (band ≈18:30–20:30 CT).
- **Status 2026-09-05 (v64 beat 5) — THE RETURN AUDITED: FIX-1a ACCEPT · FIX-1b ACCEPT-WITH-RULINGS (`context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md`); LANDING PENDING Nick's two pushes.** FIX-1a 9 = 7 M + 2 A exact at porcelain (the instruments: `--continue` · XML `if: always()` · `workflow_dispatch` · per-test XML + FULL messages · the typed `DeliveryAnomaly` emitter at four sites · one WARN route); FIX-1b 7 M as `context/audits/2026-09-05_FIX-1b.patch` (DP-2 retry-then-SUSPEND; the TRANSITION drain checkpoints each SUCCESS — ReplayIT's mechanism at source; two IT stamps). The corpus: 176 runs on a 2-carrier pin, 2 RED (both ReplayIT phase 1, unloaded), zero tokens. Act 7 = sample #4 (a red on a flake is the instrument working); Act 8 = THE GATE; three veto-only samples. **Closes when** FIX-1b's push run is GREEN (samples not red) AND the §6-B/EXITCODE hardware proof rides the next card session on FIX-1b's artifact. A post-landing HeroLoop red with `route_join_miss` and zero anomaly tokens re-opens it as FIX-2 (the fan-out reorder), mechanism named.
- **Status 2026-09-05 (v64 beat 8) — LANDED: FIX-1a `5ed9178` (10 files, +703/−21; sample #4 — its CI verdict OWED as one line) · FIX-1b `e5fa035` (6 files, +483/−103) — Nick's word: "the `e5fa035` commit for `homesynapse-core` has all checks pass" → THE GATE IS CLEARED on FIX-1b's push run (law 16; the clearance of record). The three veto-only `workflow_dispatch` samples: not yet taken (Nick's pin: only after a push run is green — it is). `main` is GREEN for a reason at `e5fa035`; CG-1/2/3 may dispatch. **Closes when** the §6-B/EXITCODE hardware proof rides the next card session on `e5fa035`'s CI-built artifact (H8) AND the samples are not red. A later HeroLoop red re-opens the gate as FIX-2 with its mechanism named — a red on a gate run is a red, never "the instrument working" (Nick's pin).
- **Status 2026-09-06 (v65 beat 6) — CG-123 LANDED `f25291b` ON TOP OF THE FIX; ITS PUSH RUN GREEN = SAMPLE #5 ON THE FIX-1b BYTES (Nick: "All GitHub checks passed").** Sample #4 (`5ed9178`) and the three veto-only samples still owed. The §6-B/EXITCODE stop-proof rides **H8-a** (this week, `f25291b`'s CI-built artifact; the word `H8:` asked) — the risk closes when that proof is read on the card.

#### OR-BUS-SILENT-DROP — the event bus drops a delivery without a signal on four LIVE/TRANSITION paths — [OPEN 2026-09-04, v63 beat 1; source: the hub's source read, the grounding audit §4-S4]
- **The class:** `InProcessEventBus.notifyEvent` `:250` (the notified position not yet readable → `return`, offered to no one) · `liveLoop` `:480` (empty page → `continue`) · `:487–:488` ("Transient read failure — skip this position; retain mode") · `TransitionCoordinator.drainAndPromote` `:100` (empty page → `continue`); the LIVE loop processes offered positions only and never pages forward from its checkpoint, so a dropped position is lost to that subscriber permanently and the next delivery writes a checkpoint past it (`:508`). No log, no metric (the bus is SLF4J-free by design; AMD-43's seven metrics are locked). A dropped `command_issued` renders exactly as `integration.route_join_miss` — "the ledger's timeout owns the outcome" — i.e. a real automation's command silently never leaves the engine. FAILCHAN's mirror image (a failure through a silent channel) in the module the D5 floor stands on.
- **Owner:** hub (charter: FAILCHAN-FIX-1, Part A tokens + Part B DP-2) / Coder (the lane). **Commit of record:** `dc3328b` (the bytes read). **Deferred Build Gate:** YES — FIX-1a's and FIX-1b's `./gradlew check` are CI on Nick's pushes. **Closes when** FIX-1b lands with T5–T7 green (retry-then-SUSPEND; a checkpoint never advances past an undelivered matching position) and CI is green on that push.
- **Status 2026-09-05 (v64 beat 5) — THE CLASS WIDENED AND PARTLY FIXED; THE CLOSURE RE-CUT TO THE PASSIVE INSTRUMENT (rule R2).** FIX-1a tokenizes the four drop points; FIX-1b retries-then-SUSPENDs them (`LIVE_READ_EXHAUSTED` · `TRANSITION_READ_EXHAUSTED`) and fixes a FIFTH silent path found at source by the lane — `TransitionCoordinator.drainAndPromote` delivered the drained burst but never checkpointed it (a checkpoint that lied low; on a restart the burst would redeliver under INV-ES-05; ReplayIT's phase-1 stall). A SIXTH is named at source, unmeasured: `notifyEvent` fan-out REORDERING across subscribers (a `command_dispatched` overtaking its `command_issued` at the router → `route_join_miss` with zero drops — HeroLoop's likely mechanism; FIX-2, instrument-first: a `LIVE_OUT_OF_ORDER` token + a monotonic checkpoint guard, then ordered offers or the AMD-42 §3.4.2 pull-from-cursor loop). **Closes when** the `bus.delivery_anomaly` token is ABSENT and both ITs GREEN across the next 20 ordinary `main` runs after FIX-1b lands (never a re-run); a HeroLoop red with a miss and zero tokens re-opens it as FIX-2.
- **Status 2026-09-05 (v64 beat 8) — THE PASSIVE INSTRUMENT'S CLOCK STARTS AT `e5fa035`; THE REGISTER FENCE WRITTEN.** FIX-1a + FIX-1b landed (`5ed9178` · `e5fa035`, the gate cleared). Nick's word `FENCE-BUS: add` → the claim register carries the fence (no delivery-completeness sentence until this OR closes). Run 1 of the 20 ordinary `main` runs is `e5fa035`'s own push run (green); the count continues with every push. **Closes when** the `bus.delivery_anomaly` token is ABSENT and both ITs GREEN across 20 ordinary `main` runs from `e5fa035` inclusive; a HeroLoop red with `route_join_miss` and zero tokens re-opens it as FIX-2.
- **Status 2026-09-06 (v65 beat 6) — THE PASSIVE COUNT: 2 of 20** (`e5fa035` · `f25291b`, both ordinary `main` push runs, both green, no anomaly token reported). Every push a sample; no re-runs; no batched pushes (Nick's law, 09-06). **TR-1 chartered** (the position census as a bench verb — the same instrument closes this risk on EVIDENCE rather than on twenty quiet runs, is the MVP's event-loss audit, and mints B-1/bus); the closure rule R2 stands until TR-1's design returns and is ruled.
- **Status 2026-09-06 (v65 beat 7) — THE PASSIVE COUNT: 3 of 20** (`e5fa035` · `f25291b` · `093d5b4` — the nanoid lock-only push ran Build & Check too; `ci.yml` carries no path filters). The three veto-only `workflow_dispatch` samples FIRING (Nick, 09-06 morning; verdicts this evening) — a red VETOES, read against the pre-registered HeroLoop prediction first.
- **Status 2026-09-06 (v66 beat 6) — THE PASSIVE COUNT 4/20:** FE-113's push `d192d17` (web-ui only; `ci.yml` has no path filter) ran Build & Check GREEN on the FIX-1b bytes — `e5fa035` · `f25291b` · `093d5b4` · `d192d17`. The three veto-only `workflow_dispatch` samples still firing (Nick's `samples: k/3` line owed). TR-1 (the position census as a bench verb; the "complete" criterion pre-registered before any run) is the evidence-closure instrument; its return this evening.
- **Status 2026-09-06 (v66 beat 9) — THE PASSIVE COUNT 5/20:** F-R4-1b's push `39c8dd3` ran Build & Check GREEN on the FIX-1b bytes — `e5fa035` · `f25291b` · `093d5b4` · `d192d17` · `39c8dd3`. The three veto-only samples still owed (`samples: k/3`). TR-1 not yet dispatched (Act after the hygiene pair).
- **Status 2026-09-08 (v67 beat 5) — THE CLOSURE INSTRUMENT DESIGNED (TR-1, ACCEPT):** the position census = post-drain high-water-mark comparison MINUS the dead-letter rows, the State Projection's atomic checkpoint special-cased (a naive `checkpoint ≥ P` over-reports — `InProcessEventBus:557,:565-566`; `ReplayDriver:191`); computed IN-PROCESS by the two ITs and printed as ONE frozen token; run by a `fix1-loop.sh`-lineage driver (`TR1-B2 = DRIVER`, REVERT-able) until R-5. Evidence closure = TR-1b's first measured run (after HONESTY-1 lands). The passive count stays 5/20.
- **Status 2026-09-11 (v68 beat 6) — THE PASSIVE COUNT 8/20** (`94ae99d` · `3f3f5cc` · `eabdbb1`, `ci` Build & Check green on each — Nick's word + the checks screenshot for `eabdbb1`); **`SAMPLES: passive` (Nick):** the three manual veto samples and the stale owed lines (`sample4` · `act3` · `nightly 09-05/09-06`) RETIRED — ordinary pushes plus the now-per-push install-smoke are the instrument; the count is re-derived at the Actions list by each hub, never typed forward.
- **Status 2026-09-11 (v69 beat 4) — SAMPLE #9 RED: `1e26912` (three design files, no source) failed `ci` at `:lifecycle:lifecycle:test` — `HeroLoopHardwareFreeIT.heroLoop_motionToHonestConfirmed`, "timed out awaiting the On frame reaching the scripted NCP" (`awaitTrue :614`, 10 s; the CI-613 class).** The passive instrument has done its job: the class is not closed by FIX-1b. Next: the `test-reports-<run_number>` artifact read for `DeliveryAnomaly` / `LIVE_READ_EXHAUSTED` / `SUSPEND` tokens in the test's stdout — tokens present ⇒ the drop was seen and the retry budget lost to it (FIX-2 = the structural fix: checkpoint-driven LIVE delivery with an idle tick, notification as a hint; an AMD to Doc 01 §3.4); tokens absent ⇒ a path FIX-1a did not tokenize or a scheduling stall (FIX-2 opens with the instrument: a bench `bus-soak` verb, N hero loops under load, latency percentiles + anomaly count). No `main` re-run; the next core push is the fix. Nick's word (Fri ~17:0x CT): a fix AND a long-term solution for the class.

#### OR-JOURNALD-PRIO — journald priority is blind to every application warning — [OPEN 2026-08-30, v58 beat 6; source: the R-3a hub-return §10-P; MECHANISM VERIFIED at source v58 b6]
- **The mechanism (verified):** `homesynapse.service:99–100` routes stdout/stderr → journal; no `sd_journal`/`SyslogLevelPrefix`/priority-prefix emission exists anywhere in app/core/config/distribution — every application line lands at journald’s default INFO priority. Measured: 158–201 app WARN/ERROR lines in one R-3a day; `journalctl -p warning` reported **2**, both systemd’s own. Any priority-filtering monitor sees a healthy service while warnings accumulate.
- **Fix candidates:** `<N>`-prefix the console handler (systemd’s `SyslogLevelPrefix` default honors it) or journal-native logging. Charter at the September plan / the R-10 docket. **Closes when:** app WARN+ lines carry journal priority ≥ warning, proven by a `journalctl -p warning` count on a card.
- **Status 2026-09-04 (v62 beat 2) — RULED Row 7 (a) (ADOPT-ALL-RECS, 09-03):** the `<N>`-prefix on the console handler (systemd's `SyslogLevelPrefix` default honors it). Charter queued behind FAILCHAN (same files' neighbourhood; one lane on the core tree). Closes when `journalctl -p warning` on a card counts the app's WARN+ lines.
- **Owner:** hub (the charter; Row 7's instruction authors behind CG on the one-lane rule) / Coder (the WU). **Deferred Build Gate:** none yet (no WU has run). *(Field made explicit at v64 beat 1 — preflight Check 5; the ruling and status lines above are unchanged.)*

#### OR-REHOMED-OQ — the two live items re-homed from the CLOSED open-questions channel — [OPEN 2026-08-18, v54 beat 2; created by the R-11 closure ruling (its §5.5), executed at intake]
- **(a) OQ-05-07 — Research-9 registry residuals** (deferrable; revisit at an operator-rebuild window). **(b) The 2026-06-11 NO_DIRECT_TIME_ACCESS enforcement-reach decision** — whether the arch-rule gate extends to non-app test classpaths; until ruled, §4c's posture stands (gate-enforced for production code, self-enforced convention for non-app test code — PM review is the only enforcement it has). Rides the next app-module-touching WU.
- **Owner:** hub (carry + surface in status updates). **Resolution:** (a) closes at its named rebuild window; (b) closes at the ruling inside that WU's instruction. Source of record: `context/open-questions.md` head (CLOSED-channel note) + this row.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Status 2026-08-22 (v56 beat 2) — OPEN; charter-bound.** The R10-IN-L return (`context/audits/2026-08-22_R10-IN-L_liveness-and-notify-transport_return.md` §3/§5) re-priced the M5-D matrix: FFM is final in JDK 22 but LTD-01 forbids preview features, so T-2 is an LTD-01 amendment, not a transport choice; recommendation **T-0 (stay `Type=exec` + the probe; defer M13 indefinitely; the unit's staged flip block is a live trap — rewritten as a DANGER note by R-9 rider R-9-b)**. Resolves at the R-10 charter's R-L2 word (T-0 closes this OR as DEFERRED-BY-DESIGN; any transport word re-opens it as a WU).
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.
- **Status 2026-09-04 (v62 beat 2) — RULED Row 5 (b) HOLD (ADOPT-ALL-RECS, 09-03):** the seam holds; T-0 is NOT spoken this month; the JDK-trajectory question (the Pelton-week orphan row, ORPHANS (a)) rides the R-5 charter as a carry-forward row. Status unchanged otherwise (DEFERRED-BY-DESIGN pending that row).

**Resolved/closed risk entries live at `context/handoff/archive/open-risks-resolved-2026-08-23.md` (eleven blocks, verbatim; rotated v56 b4 + v58 b6 + v62 b1 under W-HIVE-1 P1: this section carries OPEN entries only).**

## The archive map (pointer trail; maintained at each rotation — zero dangling addresses)

Any pointer of the form “pm-handoff §<date> (vN beat K)” resolves here. LIVE above: the newest ≤12 blocks (v68 b1 → the newest, at 2026-09-11) + Open Risks (OPEN entries only since v56 b4). ROTATED, verbatim:

- the resolved/closed Open-Risks ledger (nine blocks) → `archive/open-risks-resolved-2026-08-23.md` · both pre-region-cap `last-verified:` chains → `archive/chains-pre-region-cap-2026-08-23.md` (rotated 2026-08-23, v56 b4)
- v54 b7–b1 · v53 b9–b1 · v52 b7–b1 · v51 b6–b1 · v50 b7–b1 · v49 b5–b1 + the pre-rotation LV chain → `archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md`
- v48–v45 → `archive/pm-handoff-beats-v45-v48-rotated-2026-08-10.md` · v44–v41 → `archive/pm-handoff-beats-v41-v44-rotated-2026-08-06.md` · v40–v38 → `archive/pm-handoff-beats-v38-v40-rotated-2026-07-31.md` · v37–v32 → `archive/pm-handoff-beats-v32-v37-rotated-2026-07-26.md`
- earlier eras → `archive/pm-handoff-beats-rotated-2026-07-19.md` · `-2026-07-07` · `-2026-07-04` · `-2026-07-02` · `archive/pm-handoff-2026-06.md` · `archive/pm-handoff-2026-05.md` · `archive/pm-handoff-rotated-2026-06-22.md` · `-2026-06-21.md`
- sibling rotations this date: the snapshot chain/mastheads → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-08-21.md` · coder-handoff entries → `context/handoff/archive/coder-handoff-entries-rotated-2026-08-21.md` · coder-lessons 2026-07 → `context/lessons/archive/coder-lessons-2026-07-rotated-2026-08-21.md`
- sibling rotation 2026-08-26 (v57 b1, P6): the ten oldest coder-lessons entries (2026-08-02 … the second 2026-08-22, BY COUNT) → `context/lessons/archive/coder-lessons-2026-08-part1-rotated-2026-08-26.md` (verbatim; the live file holds the newest five)
- the `:8` chain's v55 b3–b6 segments → `archive/chains-rotated-2026-08-27.md` (verbatim; rotated 2026-08-27, v57 b2, under the ≤3,000 B chain cap)
- v65 b5 → v54 b8 (95 blocks; 2026-09-05 → 2026-08-21) → `archive/pm-handoff-beats-v54b8-v65b5-rotated-2026-09-07.md` (verbatim; rotated 2026-09-07, v66 b10, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v66 b6 → v65 b6 (8 blocks; 2026-09-06) → `archive/pm-handoff-beats-v65b6-v66b6-rotated-2026-09-07.md` (verbatim; rotated 2026-09-07, v67 b1, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v67 b3 → v66 b7 (7 blocks; 2026-09-07 → 2026-09-06) → `archive/pm-handoff-beats-v66b7-v67b3-rotated-2026-09-10.md` (verbatim; rotated 2026-09-10, v68 b2, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v67 b7 → v67 b4 (4 blocks; 2026-09-09 → 2026-09-07) → `archive/pm-handoff-beats-v67b4-v67b7-rotated-2026-09-11.md` (verbatim; rotated 2026-09-11, v69 b3, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
