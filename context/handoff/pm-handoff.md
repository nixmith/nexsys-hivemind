<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-08-21 (v55 hub, beat 2 — THE WEEKEND PROGRAM AUTHORED: six dispatch-ready products on disk — the card-sitting packet (LAYER-2-AUDITED: two root-cause defects caught at the scripts BEFORE any hardware act — build-deb.sh skips build-image.sh when a stale image tree exists [:36–:39], and hs_version prints a 0–9-leading id BARE [common.sh :62–:65] so the version of record is `7c9e4fa`, not `0.1.0+g7c9e4fa`; F-V1 minted → R-6: the a–f-only wrapper is a latent apt-downgrade trap; the L3 mask step added — the initial mint WARN-logs the raw token into the journal) · the R-3/PKG-E2E-1 SKELETON (the rig-shape ruling CLONE rec / SPARE; the custody STOP-gate replaces SD-5 for R-3 because resumeOrForm() FORMS on empty custody) · the RS-4 CRA-readiness brief · the R-6/R-8 token-ops instruction + WU-R6R8 (the operator-request-file mechanism; the /internal/tokens endpoints R-A IN rec; PI-TZ R-B TZ-HOLD rec) · the FE fixture brief (the 08-20 body = the SECOND capture of the null arm, filed as dialect-stability evidence, not a "third arm"); audits: packet 6 DEFECT/17 NOTE/18 CONFIRMED → corrected; R6R8/skeleton pins 3 DEFECT/18 NOTE/62 CONFIRMED → corrected; one auditor claim refuted at the git object (distribution/VERSION exists). THE COMBINED ORDER (beats 1+2, one msg file): stages exactly 8 = 2 M + 6 A. Detail: the beat-2 block below.) Prior: 2026-08-21 (v55 hub, beat 1 — THE LAUNCH: zero-drift verified at the instrument — hivemind 04629b1 (the beat-10 rotation commit, 10-exact = 4 M + 5 A + 1 R, porcelain EMPTY, HEAD == origin/main) · core 7c9e4fa (porcelain EMPTY, HEAD == origin/main); the 11-check preflight aggregates PASS (Check 9: the hub’s in-session PM skill cache == SOURCE byte-exact 7/7; the mirror unverified from here, no skill edit since the R-11/R-12 landings); the v55 dispatch text (Nick, 2026-08-21 — the OPERATING POSTURE 1–5 + the first acts) recorded VERBATIM in the beat-1 block per its own instruction; the on-demand reads banked (S-10 close · the R1R2 instruction §OP · WU-R1R2 P1–P9 · the R1R2 return · the sitting record §6); INTAKE: the Pi bench-card pull c091f7c..7c9e4fa (ff, 4 files — lawful, zero bench impact, the corrected Block 0’s first act); THE §OP BLOCK-2 ADJUDICATION — OVERTAKEN at the artifacts before any hardware act (the held card is the clean image and cannot build; a same-boot GREEN is a FALSE RED on probe 2’s `journalctl -b`; check 9 removes the package) → the corrected self-contained sitting packet `context/handoff/2026-08-21_card-sitting_operator-packet.md` (A; Block 0 build-on-bench tonight-eligible on Nick’s word · Block 1 RED + journal dump · Block 2 upgrade → reboot → discriminator 0 → run-smoke 9/9 → re-install the rig · Block 3 restore + THE TOKEN ROTATION; predictions P-0..P-3 filed, H12/H13-derived); THE WEEKEND AUTHORING PROGRAM OPENED — the R-3/PKG-E2E-1 skeleton (finalized at the card ⏺s) · the RS-4 brief · the R-6/R-8 instruction · the FE fixture brief · stretch: the brand build-out sprint brief — products land at beat 2 under ONE combined order (no msg file minted at beat 1; nothing to retire). Detail: the beat-1 block below.) Prior: 2026-08-21 (v54 hub, beat 10 — THE ROTATION + THE V55 BANKING, the session closes on the banked line: beats-8+9 LANDED 21b7e4c (2-exact, pushed); core stands 7c9e4fa BOTH CHECKS GREEN; THE ROTATION executed per the standing convention with the pointer-trail law, zero content loss, every rotated byte verbatim from 21b7e4c: pm-handoff beats v49→v54b7 (41 blocks) + the pre-rotation chain → archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md (live keeps beats 10/9/8 + Open Risks + the archive map; ~302 KB → ~45 KB) · PROJECT_SNAPSHOT priors chain + masthead pairs → archive/PROJECT_SNAPSHOT-priors-rotated-2026-08-21.md, body refreshed to post-GATE truth (~81 KB → ~3 KB) · coder-handoff: 21 pre-R1R2 entries + the pre-R1R2 chain → archive/coder-handoff-entries-rotated-2026-08-21.md, the R1R2 entry lane-authored untouched (~121 KB → ~8 KB) · coder-lessons 2026-07 block → archive/coder-lessons-2026-07-rotated-2026-08-21.md, the 2026-08 entries live (~107 KB → ~19 KB) — hub-session boot ~395 KB → ~62 KB, coder-lane boot ~228 KB → ~28 KB; THE V55 PROMPT BANKED at context/handoff/2026-08-21_PM-mission-control_v55_orchestrator_session_prompt.md (laws by pointer; H12 capture-hygiene/artifact-derived-expectations + H13 one-instrument-two-rigs minted; Nick’s weekend directive verbatim in §0; the v54 prompt → archive/, 1 R); THE WEEKEND PROGRAM RULED: Sat the card sitting per §OP (Block 1 RED-expected on the held card; Block 2 GREEN-expected, un-gated at 7c9e4fa; Block 3 restore + THE TOKEN ROTATION — the exposed token VALID until it runs) → the ⏺ intake → Sat-night/Sun R-3/PKG-E2E-1 authoring (pulled forward on Nick’s directive) + ranked parallel lanes (RS-4 CRA memo · the brand build-out board B-1..B-8 · R-6/R-8 token-ops · the FE fixture WU) → Wed/Thu the Pelton branch word. Stages exactly 10: 4 M + 5 A + 1 R. Detail: the beat-10 block below.) Prior: ROTATED 2026-08-21 — the complete prior chain (v54 beat 9 → v49 era) verbatim at the head of archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md; the beat-9/beat-8 segments also readable as their blocks below.
-->

# PM Session Handoff

> **How to read this file:** beats are NEWEST-FIRST below; a fresh session reads the frontmatter `last-verified:` chain (one line, newest segment first) + the newest 5 beat blocks. Older beats live in `archive/pm-handoff-beats-*-rotated-*.md`. The `## Open Risks` section at the tail is standing. Only the hub writes here (prefix-prepend); the newest beat outranks everything below it.

## 2026-08-21 (v55 beat 2 — **THE WEEKEND PROGRAM AUTHORED: six dispatch-ready products · the packet layer-2-audited — two root-cause defects caught before hardware · ONE combined order**)

**The products (six files, all A; each self-contained under arc-35; each read at source per D3 with the pins in-file):** (1) `context/handoff/2026-08-21_card-sitting_operator-packet.md` — the corrected Saturday sitting (Block 0 build-on-bench → Block 1 RED → Block 2 upgrade → reboot → discriminator → run-smoke → re-install → Block 3 restore + the rotation); (2) `context/instructions/2026-08-21_R3_PKG-E2E-1_packaged-integration-run_coding-instruction_SKELETON.md` — status SKELETON, finalized after the ⏺s + the rig word; (3) `context/instructions/2026-08-21_research-lane_RS4-cra-readiness-memo_brief.md` — fresh Cowork, due Mon 08-24; (4) `context/instructions/2026-08-21_R6-R8_token-rotation-operator-path_coding-instruction.md` + (5) `context/pre-verifications/WU-R6R8.md` — host-CC, ISSUE-READY, post-anchor rank; (6) `context/instructions/2026-08-21_FE-lane_never-triggered-fixture_and_F-S3_brief.md` — fresh Cowork under /nexsys-frontend, due Tue 08-25.

**THE LAYER-2 AUDIT OF THE HUB'S OWN PRODUCTS (D17 — two independent agents over the staged scripts + git-object exports; every verdict adjudicated at the PRIMARY text, D18).** *The packet:* 6 DEFECT / 17 NOTE / 18 CONFIRMED → **two root causes, both corrected before any hardware act:** (a) `build-deb.sh` :36–:39 runs `build-image.sh` ONLY when `distribution/image/build/opt/homesynapse` is ABSENT — the bench's stale `d26777c` image tree would have been repackaged (old runtime, NO `jdk.jfr`) under the new version, Block 2's discriminator would have printed ≥1, and the fix would have been charged with a FALSE RED (the same false-red class §0 had just caught on the journal side). Block 0 now moves the old tree aside (delete-nothing) and runs `build-image.sh` explicitly, with "no `[build-image]` lines = STOP" and `cat /opt/homesynapse/VERSION` on the held card as the cheap post-install discriminator. (b) `hs_version` (`common.sh` :62–:65) wraps a describe id as `0.1.0+g<id>` ONLY when it begins a–f; the `[0-9]*` arm prints `7c9e4fa` BARE (auditor-proven empirically; the `0.1.0+gd26777c` precedent began with `d`). Every expected string corrected; dpkg orders `7c9e4fa` > `0.1.0+gd26777c`, so the upgrade is lawful. **F-V1 MINTED → the R-6 packaging batch:** the arm was meant for tag-shaped versions; a future a–f-leading id sorts BELOW `7c9e4fa` and `apt install` refuses it as a downgrade — fix = wrap every bare id. *Also folded into the packet:* the L3 hazard — the initial mint WARN-logs the RAW token into the journal (`OpaqueTokenStore` :214–:216) and `dump_logs` tails the journal into the smoke output → a `sed` MASK of `Token: ` lines in both evidence files before anything leaves the card · the 18-PASS-line census with the `[smoke] ` prefix (nine checks) · the `grep -c` LINE-count prediction (≥6; 12–24 is the expected shape, not a deviation) · `apt-get update` before the two additive installs · the `activating (start-post)` wait · the `set -e` inheritance (a verdict-less output = a fail presentation) · the remove→install pre-checks (`id`/`is-enabled`, unmask if masked) · the fifth host-bearing line spelled · the known_hosts collision on the IP fallback. **One auditor claim REFUTED at the primary object (D18):** "no `distribution/VERSION` exists in the tree" — it exists (blob `18bbf25`, content `0.1.0-skeleton`; the auditor's upload set lacked it); the cwd rule stands. *R-6/R-8 + WU-R6R8 + the skeleton's pins:* 3 DEFECT (two citation ranges; the helper is 23 lines not 26; the files-table arithmetic — 14 rows / 11 with R-A OUT) + 18 NOTE / 62 CONFIRMED / 9 UNVERIFIABLE-HERE — all corrected; two design strengtheners folded: the request-file READ-failure arm (never bricks startup) and the CLI dispatch placed between `:57` and `:59`, before the first `Files.createDirectories` write.

**The R-3 design finding (banked at source, load-bearing for weekend 2):** `ZigbeeIntegrationAdapter.resumeOrForm()` (:690–:700) FORMS a new network whenever `parameterStore.load()` is empty — a fresh packaged data dir + the bench coordinator would RE-FORM the bench network (six devices orphaned). The skeleton therefore rules out "plug and see", replaces the H3-era SD-5 fence with a custody STOP-gate (`zigbee-network.json` present + parsing + the bench's ch20/PAN before the coordinator is visible to the packaged service), and offers two lawful rig shapes: **CLONE** (the bench's self-contained `data/zigbee/` custody + the yaml cloned onto the held card; the bench fleet reports into the packaged path; zero new hardware; the Nov-25 migration rehearsed) vs **SPARE** (a second EZSP dongle + a fresh network + a spare device). The zigbee custody is an INDEPENDENT SecretStore rooted at `<dataDir>/zigbee/` (its own `.root-key`/`scope_keys.json`/`secrets.enc`) — no collision with the app's config-dir keys, which is what makes CLONE cheap.

**THE RULINGS PACKAGED (H10 — one word each; unre-worded rows stand as RECOMMENDED at dispatch):** **R-0** Block 0 TONIGHT on the bench card — GO (rec) / HOLD · **R-3** the rig shape — CLONE (rec) / SPARE / BOTH · **R-A** the `/internal/tokens` endpoints — IN (rec) / OUT · **R-B** PI-TZ — TZ-HOLD (rec; the s31/nightly fence, and a timezone change would shift a local-time nightly by an hour) / TZ-NOW (with the cron compensation, ⏺'d before and after). **One enrichment ask (once; declining closes it, arc-36):** the bench config + zigbee-custody listing (`ssh pi 'ls -la ~/hs-bench/config ~/hs-bench/config/integrations ~/hs-bench/data/zigbee; grep -nE "!secret|!env|serial_port|channel|permit_join|adopt_devices" ~/hs-bench/config/homesynapse.yaml ~/hs-bench/config/integrations/*.yaml'`) — grounds the CLONE blocks.

**Dispatch readiness (Nick's execution queue, full):** RS-4 and the FE brief are fresh-Cowork lanes — dispatchable NOW, in parallel, by their dispatch lines; R-6/R-8 is host-CC — dispatchable NOW (it never displaces the sitting; R-A/R-B default to the recommendations if unre-worded); R-3 finalizes in one hub pass after the ⏺s + the rig word. Saturday's hardware act = the packet, whole.

**THE COMBINED ORDER (beats 1+2 — one msg file, env-model §11 corollary; no prior msg file exists to retire).** Stages exactly 8: 2 M (`context/handoff/pm-handoff.md`, `context/status/PROJECT_SNAPSHOT.md`) + 6 A (the six product paths above). Msg: `../_scratch/2026-08-21_hivemind_v55-beats-1-2_commit-msg.txt`. The order text rides the beat-2 dispatch message (glance block with its own STOP-gate, then the commit block).

**Wait-states.** The card ⏺s (Sat; outrank everything) · the four words (R-0/R-3/R-A/R-B) · the enrichment read · lane returns as dispatched · Pelton (~Wed/Thu) · the beats-1+2 landing transcript (law 5 — verified at porcelain) · (P) standing asks unchanged.

## 2026-08-21 (v55 beat 1 — **THE LAUNCH: zero-drift verified at the instrument · the v55 dispatch recorded verbatim · the weekend authoring program opened**)

**The launch verification (law 5; the 11-check preflight; 17:51 CDT).** hivemind `04629b1` = the beat-10 rotation commit — **10-exact** at `git show --stat` (4 M: pm-handoff · coder-handoff · coder-lessons · PROJECT_SNAPSHOT; 5 A: the v55 prompt + the four verbatim archives; 1 R: the v54 prompt → `archive/`), porcelain EMPTY (`git --no-optional-locks status --porcelain`), `HEAD == origin/main` (pushed). Core `7c9e4fa`, porcelain EMPTY, `HEAD == origin/main`. **Zero drift on the boot reads** — the v55 prompt §2, the beat-10 block, and the snapshot chain agree at every HEAD and every wait-state. Preflight rows: 1 PASS (chain stamp = today, = the newest beat) · 2 PASS-by-cadence (the weeks series ends at W31; `planning/weeks/README.md` documents the milestone-cadence shift; S-10 + the v55 prompt §2 are the schedule of record) · 3 PASS (`7c9e4fa` at the snapshot and at `git log`) · 4 pointer (the S-10 ranked program is the semester backlog; the M-backlog is pre-gate history — not re-read, law 11) · 5 PASS (one OPEN row, OR-REHOMED-OQ — owner + resolution named) · 6 PASS (coder-handoff's R1R2 entry names R-3/PKG-E2E-1 §7) · 7 PASS-by-carry (zero Java since the last MODULE_CONTEXT fold) · 8 N/A (the channel retired 08-18, pointer stub verified) · 9 **the hub's in-session PM skill cache == SOURCE byte-exact (md5 7/7: SKILL.md + six references)** — Nick's `.claude/skills` MIRROR is outside the connected folder and unverified from here; no skill edit has landed since the R-11/R-12 landings (Nick-committed 08-18) ⇒ no new drift introduced; authoring lawful (hub reads ride SOURCE) · 10/11 PASS-by-carry (R-11 closed 08-18; zero Java). **Aggregate PASS — forward work unblocked.** The beat-10 transcript wait-state CLOSES at the instrument (the porcelain is the record; no paste needed).

**THE DISPATCH TEXT (Nick, 2026-08-21 — recorded verbatim per its own instruction; it sets the v55 tempo and supersedes nothing in the banked prompt):**

> You are the v55 PM MISSION-CONTROL hub. Boot from `nexsys-hivemind/context/handoff/2026-08-21_PM-mission-control_v55_orchestrator_session_prompt.md` — execute its Section 1 reads exactly (they are lean by design: the 2026-08-21 rotation cut the boot to ~62 KB; stay inside it — pointer-form beyond the listed reads, bounded reads on large files, archives only when a specific act demands a specific address via the archive map), re-derive state at the instrument, then open beat 1.
>
> OPERATING POSTURE FOR v55 — sets the tempo; supersedes nothing in the banked prompt:
>
> 1. VELOCITY WITH ZERO DISCIPLINE RELAXATION. We are racing the clock (Sat: the card sitting · ~Aug-27/28: Pelton → the branch word · Aug-29/30: R-3 → R-4 → THE FENCE LIFT · ~Sept-11: Art-14 · early-Sep: the filing · Nov-25: the runway). v54 proved the discipline IS the velocity: the 9f99368 red was caught by the gate we built — before any hardware act — and turned green inside one beat-arc. The laws, STOP-gates, census-exact orders, D2, law 16, and the two-layer audits are never traded for speed. "No mistakes" means: predictions filed before runs, expectations derived from artifacts (H12), adjudicate-first on any deviation, never improvise past a mismatch.
>
> 2. AGGRESSIVE THROUGHPUT. Bias every beat toward MORE dispatch-ready product: author parallel lane briefs ahead of need, keep Nick's execution queue full so operator time is never the bottleneck, and pull ratified work forward whenever its gates are already satisfied (the R-3 pull-forward is the precedent). When you see a defensible acceleration, propose it with the risk named and a one-word ruling packaged — never sit on it, and never execute past a fence without the word. The fences are absolute: D-1 until R-4 · README:117 until W2-3 · no public brand use before G-2 · s31 HANDS OFF until R-5 · the hub never implements.
>
> 3. TOKEN ECONOMY AS LAW. Banked verdicts are final (law 16) — never re-derive what the spine already carries. Read once, bank on disk (law 11), point rather than copy. Scope every device call. One combined order per census. A read not required by the current act does not happen.
>
> 4. FABLE 5 EXTRA EVERYWHERE. Hub, coder, research, and FE lanes all run on Fable 5 Extra: write DENSER briefs — more scope per lane where the census stays clean — and expect lane strengtheners beyond the brief. Capability never substitutes for verification: the two-layer audit and H12/H13 stand at full strength.
>
> 5. DECISIONS TO NICK IN RULING FORM (H10). Every escalation arrives as branches + a recommendation + a one-word ask, G-2-brief style. Open-ended questions are a defect.
>
> First acts: the launch verification (zero-drift on the boot reads; porcelains — hivemind `04629b1`, core `7c9e4fa`) → then author forward until the card ⏺s arrive: the R-3/PKG-E2E-1 skeleton (finalized only after the ⏺s ground it), the RS-4 brief, the R-6/R-8 instruction, the FE fixture brief — the whole weekend program dispatch-ready before Saturday night. The card ⏺s, when they arrive, outrank everything; the token rotation block rides Block 3 and the exposed token remains valid until it runs. Record this dispatch text in the beat-1 spine write.
>
> Execute all tasks, problems, and/or deeper assessments thoroughly and pay attention to details. I (Nick) am fully aligned with your judgment and intuitions for this conversation. Take all time necessary for any deep work.

**The on-demand reads banked this beat (§1.6 — each BEFORE its act; pointer-form hereafter):** the S-10 close (R-3/R-6/R-8/RS-4 definitions + the §3b I-rows incl. the CRA readiness rail) · the R1R2 instruction §OP + WU-R1R2 P1–P9 + the R1R2 return (the card predictions of record: Block 1 rows-PRESENT + ≈6 hits most likely, the red on probe 2; both-clean = the F-23-refutation STOP; the held card = R-3/R-4's rig, preserved by Block 3) · the sitting record (F-S1..F-S5; §6 = the rotation block — `mv config/api_tokens` aside → `~/bench.sh restart` → read `initial_api_token` on the Pi terminal ONLY → re-pair; the `wire-2026-08-20-never-triggered` body verbatim at §6 = the FE fixture's source; the F-S3 fetch-once question).

**THE WEEKEND AUTHORING PROGRAM (opened; products land at beat 2).** In dispatch order, each a self-contained file under the fresh-context law (arc-35): **(1) the R-3/PKG-E2E-1 SKELETON** — `context/instructions/2026-08-21_R3_PKG-E2E-1_packaged-integration-run_coding-instruction_SKELETON.md`, status SKELETON — every section authored that the ⏺s cannot change (the objective, the fences, the PrivateDevices ground at source, the rehearsal-rig contract, the red-first/D-5 prediction discipline, the files table shape); the ⏺-dependent rows carry explicit `[⏺-GROUNDED-AT-INTAKE]` slots; finalization = one hub pass after the Saturday intake, never before. **(2) the RS-4 brief** — `context/instructions/2026-08-21_research-lane_RS4-cra-readiness-memo_brief.md` (fresh Cowork; findings-with-citations; the two-layer audit at intake). **(3) the R-6/R-8 instruction** — `context/instructions/2026-08-21_R6-R8_token-rotation-operator-path_coding-instruction.md` + `context/pre-verifications/WU-R6R8.md` (the token-rotation operator path: the `--rotate`-class verb + the documented procedure + PI-TZ; grounded at source THIS beat-arc, module-info verbatim). **(4) the FE fixture brief** — `context/instructions/2026-08-21_FE-lane_never-triggered-fixture_and_F-S3_brief.md` (the third provenance-grade arm + the F-S3 confirm; H8 the bar). **(5) stretch:** the brand build-out research-sprint brief over board B-1..B-8 (reads the G-2 readiness brief first). **Order: DEFERRED** — this beat's 2-M spine delta rides the beat-2 combined census (one msg file per order, env-model §11 corollary); no msg file is minted at beat 1, so nothing is retired.

**INTAKE (Nick, mid-beat, pasted): the Pi bench-card checkout pulled `c091f7c..7c9e4fa` (fast-forward, 4 files, +97/−14 = exactly the R-1/R-2 + capture-fix census; `hs-dev-1`, user `homesynapse`).** Adjudicated: LAWFUL and ZERO bench impact — zero Java/TS in the delta, no rebuild ran, `bench.sh` and the nightly's inputs untouched; the running app is unaffected. It is the correct FIRST act of the corrected Block 0 below (the build runs on the bench card), and it proved the Pi's remote reach on the day before the sitting.

**THE §OP BLOCK-2 ADJUDICATION (the hub's own authored block, found defective at the artifacts BEFORE any hardware act — the v54 posture's exhibit).** Two premises fail: (1) **the held card cannot build** — `build-image.sh` §0 dies without `JAVA_HOME`→JDK 21 (`jlink`/`jdeps`) and runs `./gradlew --no-daemon :app:homesynapse-app:installDist` in the repo; the held card is the H3 Stage-2 CLEAN image (`hs-fresh`, user `nick`, `ssh -i ~/.ssh/id_ed25519_pi nick@hs-fresh.local` per F-18; the artifact was *transferred* and `apt install`ed; no toolchain, no checkout). (2) **a same-boot GREEN would be a FALSE RED** — check 4 probe 2 greps `journalctl -u homesynapse.service -b` (THIS boot); Block 1's ≈6 RED lines from the old artifact's start stay in the journal and `postinst` restarts the service in the same boot on upgrade ⇒ the fixed artifact would count the old artifact's throws. Also derived for the operator: check 1 self-installs (no-op on the same version), check 8 stops, **check 9 REMOVES the package** (data preserved) — so the rig is re-installed as Block 2's last act; `hs_version` resolves `git describe` only from the repo ROOT (cwd-relative `VERSION`; `distribution/VERSION` = `0.1.0-skeleton`). **RULED: §OP Block 2 OVERTAKEN; Blocks 1/3 carried.** The corrected sitting is the self-contained packet `context/handoff/2026-08-21_card-sitting_operator-packet.md` (A): Block 0 = BUILD on the bench card (tonight-eligible; `installDist` UP-TO-DATE by construction on a zero-Java delta — the midweek-deploy precedent; the old `.deb` copied aside, delete-nothing) → `git archive` the LF-clean `distribution/` tree on the Pi → sha256 on every hop → Block 1 RED verbatim + the journal dumped to a file (the Lite journal is volatile; the red evidence survives the reboot as a file) + the `config/` listing → Block 2 = upgrade → (conditional store reset if `initial_api_token` is absent — checks 3/5 read it) → `sudo reboot` → the byte-identical discriminator grep (predict 0; H13) → run-smoke 9/9 → re-install for R-3's rig → Block 3 restore + THE TOKEN ROTATION BLOCK verbatim. Predictions P-0..P-3 filed in the packet §6, every expected line derived from the scripts at `7c9e4fa` (H12). **Acceleration proposed to Nick (ruling form):** run Block 0 TONIGHT on the bench card — risk named (Gradle on the live install tree; UP-TO-DATE by construction), word asked.

**Wait-states.** The card ⏺s (Sat; they outrank everything on arrival) · Nick's word on the Block-0-tonight acceleration · Pelton (~Wed/Thu) · the beat-2 combined order · (P) standing asks unchanged: owner-rows batch word · mirror-host one-liner · mp3 archive drop · the optional `7c9e4fa` CI-log three-line paste · archive-README grooming.

## 2026-08-21 (v54 beat 10 — **THE ROTATION + THE V55 BANKING: four files lean, archives verbatim · the weekend program ruled · the session closes on the banked line**)

**The landing (law 5).** Beats 8+9 LANDED: hivemind `21b7e4c` (2-exact, pushed). Core stands at `7c9e4fa` — Build & Check GREEN (2m) + install-smoke GREEN (2m), banked at beat 9.

**THE ROTATION (the standing convention + the pointer-trail law; zero content loss — every rotated byte verbatim from `21b7e4c` / the staged coder pair).** (1) **pm-handoff**: beats v54b7→v49b1 (41 blocks) + the complete pre-rotation LV chain → `archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md`; live keeps beats 10/9/8 + `## Open Risks` (operative, untouched) + the archive map below (~302 KB → ~45 KB). (2) **PROJECT_SNAPSHOT**: the priors chain + the two v48-era masthead pairs → `archive/PROJECT_SNAPSHOT-priors-rotated-2026-08-21.md`; the body sections refreshed to post-GATE truth, the old body rotated verbatim beside the chain (~81 KB → ~3 KB). (3) **coder-handoff**: the 21 entries below the R-1/R-2 DELIVERED entry + the pre-R1R2 LV chain (19.7 KB) → `archive/coder-handoff-entries-rotated-2026-08-21.md`; live keeps the front-matter, the R1R2 entry (lane-authored, untouched), the rotation notice (~121 KB → ~8 KB). (4) **coder-lessons**: the 2026-07 block → `archive/coder-lessons-2026-07-rotated-2026-08-21.md`; the 2026-08 entries live; notices chained (~107 KB → ~19 KB). **The hub-session boot read drops ~395 KB → ~62 KB; the coder-lane boot ~228 KB → ~28 KB.** Archive-README grooming = (P) at v55’s leisure.

**THE V55 PROMPT BANKED** at `context/handoff/2026-08-21_PM-mission-control_v55_orchestrator_session_prompt.md` — laws 1–16 by pointer (v51, archive) · H1–H6 (v52/v53) · H7–H11 (v54 prompt → archived this order, 1 R) · **NEW HARVESTS MINTED: H12 CAPTURE HYGIENE + ARTIFACT-DERIVED EXPECTATIONS** (one lesson, two halves — the 9f99368 red and the glance-line own: any $( ) capture feeding a parser is whitelist-filtered to its expected grammar at the capture site, and every expected-output value in an order derives from the ARTIFACT, never the intent) · **H13 ONE INSTRUMENT, TWO RIGS** (a probe pattern shared between CI and an operator block is byte-identical in both — THROW_RE the exhibit) · the L3-extends-to-screenshots carry. Nick’s weekend directive rides §0 verbatim. **Dispatch line:** `Read nexsys-hivemind/context/handoff/2026-08-21_PM-mission-control_v55_orchestrator_session_prompt.md and execute it. - /nexsys-project-manager`

**THE WEEKEND PROGRAM (ruled on Nick’s directive — deep research + development, comprehensive, detail-oriented).** **SAT:** the card sitting per §OP (the R1R2 instruction; 03:00–04:15 CT guard) — Block 1 RED-expected on the held card `0.1.0+gd26777c` (rows PRESENT + ≈6 throw hits; the red rides probe 2; both-clean = the F-23-refutation STOP, hub adjudicates before any further act) · Block 2 GREEN-expected (the unpinned pull fetches `7c9e4fa`; predicted: the clean 16-module jlink line, §2b “all 16”, checks 1–9 green with a printed events count) · Block 3 restore + **THE TOKEN ROTATION BLOCK** (sitting record §6; the exposed token VALID until it runs; slip ⇒ run it alone) → the ⏺ intake at the hub. **SAT-NIGHT/SUN:** R-3/PKG-E2E-1 authoring (the weekend-2 anchor pulled forward per the directive; PrivateDevices loosening + the rehearsal rig; grounded at the card ⏺s; dispatch when Nick is ready). **SUN PARALLEL (as time permits, ranked):** (1) the RS-4 CRA readiness memo research lane (Art-14 Sept-11 + Dec-2027; the audit-trail-as-compliance-asset thesis) · (2) the brand build-out research sprint over board B-1..B-8 · (3) the R-6/R-8 token-ops WU instruction (authorable now; post-anchor rank) · (4) the FE fixture WU (the wire-2026-08-20-never-triggered fixture + F-S3 confirm; H8 the bar). **WED/THU:** Pelton → the ONE branch word (A/B/C per the G-2 readiness brief; on A also the R-1 word) → same-day execution (reply scaffold; runbook arming on A).

**Next.** The beat-10 landing transcript (law 5) → **v54 CLOSES; v55 launches on the banked line** · the card ⏺s · the branch word · (P) standing asks unchanged: owner-rows batch word · mirror-host one-liner · mp3 archive drop · the optional 7c9e4fa CI-log three-line paste · archive-README grooming.

## 2026-08-21 (v54 beat 9 — **THE GATE WENT GREEN: 7c9e4fa · R-2 CI-PROVEN, R-1's FIRST CLEAN-MACHINE GREEN · Saturday fully un-gated · THE STRATEGIC REVIEW**)

**The landing (law 5).** `7c9e4fa` landed: 1-exact (`distribution/image/build-image.sh`, +4/−1), porcelain clean at the pasted transcript, pushed `9f99368..7c9e4fa`. ORDER A ran whole, including step A0 — the v49-era stray moved to the sibling; core porcelain was EMPTY before the apply.

**The mid-landing adjudication (the glance-line own).** `grep -c "grep -Ex"` returned **2** against the order's "expect: 1" — Nick STOPPED at the gate exactly per discipline. Adjudicated at the artifact (hub ran the differentiator on the live tree): `:83` = the teaching COMMENT carries the literal `grep -Ex`; `:90` = the CODE line; the sharper gate `grep -c "2>/dev/null | grep -Ex"` = 1. **The 2 is the correct count of the correct artifact; the order's expectation was the defect** — authored from intent ("one grep stage") instead of derived from the artifact. RESUMED; the own banks. ADDENDUM minted onto the beat-8 capture-hygiene harvest row: expected-output values in orders are derived from the ARTIFACT, never from the intent.

**The verdicts (law 16 — banked at the checks surface).** On `7c9e4fa`: **CI/Build & Check GREEN (2m)** and **install-smoke GREEN (2m)** ("Build image + .deb, install-smoke on a clean machine"). Meaning: jlink linked the fixed module set, §2b passed, and ALL NINE smoke checks passed on a clean runner — including check 4, **the first-ever green of the packaged write-path assert: the artifact wrote ≥1 committed event row with zero throw signatures on a clean machine.** R-2 is CI-proven; R-1's runner half is proven; the hardware half (red-first on the held card, then fixed-green) rides tomorrow. Honesty line: the beat-8 predicted LOG lines (the 16-module `--add-modules`, §2b "all 16", the check-4 count) were not pasted — the green banks at the check level; (P) optional at leisure: paste those three lines from the install-smoke run log; otherwise Block 2 produces the same surface on hardware tomorrow.

**Saturday now (fully un-gated).** Block 1 unchanged: expected RED on the held card (rows PRESENT + ≈6 throw hits; the red rides probe 2; both-clean = the F-23-refutation STOP, adjudicated at the hub before any further act). Block 2 UN-GATED: expected GREEN — the §OP's unpinned `git pull` fetches `7c9e4fa`; predicted surface: clean jlink line, §2b "all 16", checks 1–9 green with a printed events count. Block 3: restore + THE TOKEN ROTATION BLOCK — **the exposed token remains valid until it runs**; if the sitting slips, run the rotation block alone.

**ORDER-B OVERTAKEN (§11, the beats-4+5 precedent).** The beat-8 spine order never ran (hub re-verified at porcelain: the 2 M lines uncommitted, HEAD `3be8aef`); the beat-9 intake widened the spine CONTENT with the census UNCHANGED (the same two files). The beat-8 msg is retired IN PLACE — its file now carries the retirement stamp and cannot produce a valid commit; the order of record is the COMBINED beats-8+9 msg (`../_scratch/2026-08-21_hivemind_v54-beats-8-9_commit-msg.txt`, stages exactly 2 M).

**THE STRATEGIC REVIEW (the step-back, banked).**
- **The semester anchor (F-23 → R-1/R-2)** is code-complete, hub-audited ACCEPT ×2, and now CI-PROVEN on `7c9e4fa`. The ONLY open piece is the hardware evidence leg — tomorrow's card sitting (red-first on the old artifact, green on the fixed build, restore). After it, in order: R-3/PKG-E2E-1 authoring (weekend 2: PrivateDevices loosening + the rehearsal rig) → R-4 re-rep → THE FENCE LIFT (the D-1 language stands verbatim until then; `README.md:117` stays fenced until W2-3) → R-5 (the s31 legs/nightly stay HANDS-OFF until R-5).
- **Currency/skills**: R-11 + R-12 closed at beat 2; harvest rows accumulated for the next W-SKILLS touch: probe-patterns-shared-verbatim (beat 7), capture hygiene + the derive-from-artifact addendum (beats 8–9), H11 one-artifact-per-navigator.
- **The brand program** stands fully staged for Pelton (~Aug-27/28): ZENDOMO primary field-confirmed ×6 markets, VERDOMO hedge REC (NL-settled), the G-2 readiness brief (branches A/B/C one-word-rulable, per-branch Pelton reply scaffolds, the money table with the $8k walk, LLC = the branch-independent acceleration, board B-1..B-8), the 48-h swap runbook armed-inert on gates G+R. The waiting posture is CORRECT — no act before the branch word.
- **FE/bench**: the browser phase closed-full (beat 6); the H8 flip banked (NEW-2/NEW-3 VERIFIED-LIVE); F-S1..F-S5 filed with named consumers. The ONE live security pend: the token rotation (Block 3 or standalone).
- **Hygiene**: pm-handoff ~289KB — rotation at the first quiet beat (Sunday post-card fits). (P) standing asks unchanged: owner-rows batch word · mirror-host one-liner · mp3 archive drop · the wire-2026-08-20-never-triggered fixture at the FE lane's next touch · F-S3 FE confirm.
- **THE RANKED NEXT**: (1) tonight — the ONE combined spine order; (2) TOMORROW — the card sitting exactly per §OP, the highest-value next work in the program: it closes the anchor's evidence leg AND retires the token exposure in the same sitting; (3) Sunday/quiet — hub authors R-3/PKG-E2E-1 from the card ⏺s + the pm-handoff rotation; (4) Wed/Thu — the branch word → same-day execution. Nothing jumps this queue.

**Next.** The card ⏺ intake → R-3/PKG-E2E-1 authoring → R-4 → THE FENCE LIFT · the branch word ~Aug-27/28 · RS-4 post-anchor · R-6/R-8 (F-S5) post-anchor · rotation items as above.

## 2026-08-21 (v54 beat 8 — **THE INSTALL-SMOKE RED: ADJUDICATED AT THE LOG · THE CAPTURE-WHITELIST FIX ORDERED · Block 1 free, Block 2 gated on the green**)

**The landing + the verdicts (law 5, law 16).** Beat 7 LANDED: core `9f99368` (4-exact, pushed) + hivemind `3be8aef` (5-exact, pushed) — both at the pasted porcelains. The gate of record then spoke on `9f99368`: **CI/Build & Check GREEN (2m)** — banks; **install-smoke RED** — banks, permanent, never re-run (law 16). Hub porcelains re-taken fresh this beat (lock-free): hivemind CLEAN at `3be8aef`; core carries exactly one stray line — the stray ruling below.

**The mechanism (adjudicated at the pasted step log — D2; the log is the exhibit).** Gradle+npm SUCCESS (55 jars bundled); the jlink step died. The all-roots jdeps invocation ALSO lists every jar on `--class-path "${IMAGE}/lib/*"`, so each package is seen at the SAME jar path twice — once as a root, once as a class-path member — and jdeps emits a per-jar `Warning: split package: <pkg> <path> <same path>` line **on STDOUT**. The `2>/dev/null` silences stderr only. The `$( )` capture swallowed warnings + module list whole into `JDEPS_MODS`; the `tr ',' '\n' | sed | sort -u | paste -sd,` normalizer folded each warning LINE in as a “module” token (capital `W` collates ahead of every lowercase module name); jlink received garbage tokens in `--add-modules` and died before linking: `java.lang.module.FindException: Module Warning: split package: kotlin.collections.unsigned … not found` — exit 1. **§2b never ran — correctly**: it guards a PRODUCED runtime; jlink's own resolver is an earlier gate of the same family, and it fired first.

**The proof inside the red.** The polluted value's tail carried the REAL union list: `java.base,java.desktop,java.instrument,java.logging,java.management,java.naming,java.net.http,java.security.jgss,java.sql,java.xml,jdk.crypto.cryptoki,jdk.crypto.ec,jdk.jfr,jdk.management,jdk.unsupported,jdk.zipfs` (16 modules) — **`jdk.jfr` PRESENT** (the F-23 payload made it through discovery) plus TWO genuine full-set finds the old single-root invocation never saw (`java.desktop`, `java.instrument`). The R-2 approach is proven; only the capture was dirty.

**Why no earlier gate caught it — and the own.** The desk gate was `bash -n`: syntax only, structurally blind to a tool's runtime stdout behavior. The first live execution of the new invocation was — by design AND by the beat-7 pre-ruling (“commit+push tonight, CI before the card”) — the CI runner: the gate of record fired exactly as sequenced, BEFORE any hardware act. THE HUB OWNS the Layer-2 miss: the audit reasoned over the diff and never asked *“what else does jdeps print on stdout under the new shape?”* Capture hygiene was not in the audit question set. It is now — the harvest row below.

**The fix (ruled: minimal-diff; hub-authored patch artifact per the OR-GATE-M7.5c-a form; Nick applies; census exactly 1 M; NEW commit per law 16).** ONE pipeline stage inserted at the capture: `2>/dev/null | grep -Ex '[a-zA-Z0-9._]+(,[a-zA-Z0-9._]+)*' || true` — a WHITELIST at the expected grammar: only the pure comma-joined module-deps line can enter `JDEPS_MODS`; every warning shape — present or future, self-split or genuine cross-jar — is excluded by grammar, not enumerated; an empty capture degrades to the pre-existing `${JDEPS_MODS:-java.base}` floor-only fallback, whose breakage the downstream smoke asserts catch honestly. `--class-path` deliberately KEPT: minimal diff on a red, and genuine cross-jar splits (the kotlin-stdlib-shim class) would pollute stdout regardless — the filter, not root-set surgery, is the load-bearing fix. Three comment lines teach the mechanism at the site (comment/code unity — the R-2 brief's own discipline). The patch travels as a FILE, not chat bytes (law 11): `_scratch/2026-08-21_build-image_capture-whitelist.patch`, applied `git apply --check` then `git apply` — atomic: applies whole or rejects loud. Hub-side pre-verification: TEST-APPLIED GREEN in-container against a byte-exact stub of the region (git apply + `bash -n` + anchor greps — old line gone, new line single, diffstat +4/−1).

**The gate + the prediction (filed pre-run, D-5 style).** install-smoke on the fix commit = the gate of record. Expected green log: `jlink --add-modules java.base,java.desktop,java.instrument,…,jdk.zipfs` (the 16-module union, warnings absent), §2b `floor-presence assert green: all 16 requested modules present in the runtime`, then checks 1–9 green with check 4 printing an observed events count ≥1 (probe 1) and zero throw-signature hits (probe 2). Green ⇒ bank next beat; Saturday proceeds whole. Red ⇒ paste-back, adjudicate at the hub, never re-run — a runner probe-1 red remains the pre-ruled P6-refutation STOP.

**Saturday impact (the sitting is TOMORROW, Sat Aug 22).** Block 1 UNAFFECTED — the held card runs the OLD artifact (`0.1.0+gd26777c`); the RED evidence leg needs no new build. Block 2 GATED on the fix commit's install-smoke GREEN — and the §OP self-heals: its Block-2 first act is `git pull && git log --oneline -1` (unpinned; the fix rides in). Pi bench deploys unaffected (installDist path — `build-image.sh` is not in it). The token rotation block still rides Block 3 (restore) or the first quiet beat.

**The stray ruling (D14 sweep-guard).** The fresh core porcelain carries `?? _scratch/` — a repo-INSIDE `_scratch/` holding two v49-era commit-msg files (mtimes Aug-8: `2026-08-08_core_lifecycle-mc-bprime-fold_commit-msg.txt`, `commit-msg-m9.5-durd-bprime.txt`) — a pre-session stray from the M9.5-DURd-B′ era, written inside the repo instead of the sibling `_scratch/`. Content benign (msg text already public in git history). RULED: mv-to-sibling (delete-nothing) — ORDER A step 0 moves both files to `../_scratch/` and removes the emptied dir; core porcelain returns to clean; census staging stayed explicit-path throughout, so the stray never threatened an order.

**Harvest row minted (for the next skills/lessons touch, alongside beat-7's).** CAPTURE HYGIENE: any command-substitution capture that feeds a parser (jlink, config, census) is whitelist-filtered to its expected grammar AT the capture site. The audit question is “what else can this tool print on THIS stream under THIS invocation?” Stream redirects and blacklists are not filters of record.

**Next.** The fix landing + the CI verdict paste → bank · Saturday: the card ⏺ intake → then R-3/PKG-E2E-1 authoring (weekend 2) → R-4 re-rep → THE FENCE LIFT · ~Aug-27/28: the branch word (readiness brief standing; on A also the R-1 word) · RS-4 post-anchor · the token rotation block (Saturday Block 3 or the first quiet beat) · pm-handoff rotation at the first quiet beat (~282KB) · (P) standing asks unchanged.

## Open Risks

#### OR-REHOMED-OQ — the two live items re-homed from the CLOSED open-questions channel — [OPEN 2026-08-18, v54 beat 2; created by the R-11 closure ruling (its §5.5), executed at intake]
- **(a) OQ-05-07 — Research-9 registry residuals** (deferrable; revisit at an operator-rebuild window). **(b) The 2026-06-11 NO_DIRECT_TIME_ACCESS enforcement-reach decision** — whether the arch-rule gate extends to non-app test classpaths; until ruled, §4c's posture stands (gate-enforced for production code, self-enforced convention for non-app test code — PM review is the only enforcement it has). Rides the next app-module-touching WU.
- **Owner:** hub (carry + surface in status updates). **Resolution:** (a) closes at its named rebuild window; (b) closes at the ruling inside that WU's instruction. Source of record: `context/open-questions.md` head (CLOSED-channel note) + this row.

#### OR-GATE-M9.4-NCFG — ci.yml RED on pushed `ccba69d` (`ReplayTransitionIT` timeout) — [✅ RESOLVED 2026-07-06, v22 beat 5 — rerun GREEN; closed as INTERMITTENT-definitive] (NEW 2026-07-06, v22 beat 4+)
- **RESOLVED (beat 5):** Nick re-ran the failed jobs on `ccba69d` → GREEN. Closed as intermittent with Nick's reinforcing causality on the record: *NCFG touched integration-zigbee only and cannot causally reach event-bus replay — runner variance, not a regression; "intermittent" is definitive, not probable.* §4b UNBLOCKED; the M9.4-ADP dispatch hold lifted. **The deflake row stays QUEUED regardless (Nick: "latent moat-debt"):** `awaitCheckpoint` must report checkpoint PROGRESS and fail on STALL, not wall-clock — rides the next event-bus-touching WU (its own micro-WU on recurrence).
- **Milestone:** M9.4-NCFG (the gate of record on its push). **Commit:** `ccba69d`. **Failure:** `:core:event-bus:test` → `ReplayTransitionIT` "resumes from persisted checkpoint and catches up to LIVE without gaps under continuous publish" — `AssertionError` at `:212` = the `awaitCheckpoint` 30 s timeout (called `:167`); 221 tests, 1 failed.
- **Adjudication frame (hub, at intake):** event-bus is UNTOUCHED for 65 commits (last touch `a441fdf`, M4.0a); CI ran green on identical event-bus bytes many times incl. `e363320` same-day; Nick's local full `check` GREEN ×2 today on trees carrying this exact code; the failing await is wall-clock-bounded real-thread concurrency (1500-event continuous publish) — the slow-runner flake profile. NOT waved off: the same signature would present for a REAL subscriber-stall race, and the error omits the last-seen checkpoint — undecidable from the log alone.
- **Owner:** Nick (re-run) / hub (adjudication + the deflake row). **Commands (Nick):** GitHub → the failed run on `ccba69d` → **Re-run failed jobs**. Optional local hunt: `./gradlew :core:event-bus:test --tests "*ReplayTransitionIT*"` repeated ×5.
- **Resolution condition:** rerun GREEN ⇒ gate CLEARS as intermittent (this OR closes; the **deflake/instrumentation row is QUEUED regardless** — `awaitCheckpoint` must report the last-seen checkpoint and fail on STALL rather than pure wall-clock; rides the next event-bus-touching WU, its own micro-WU if it recurs). Rerun RED ⇒ escalates to a REAL investigation WU (a reproducible-on-runner liveness failure in the delivery path outranks the bench — never-false-* extends to "without gaps").
- **§4b consequence: the M9.4-ADP dispatch HOLDS until this gate clears** (the ruling can land meanwhile; "go" fires on the green).

#### OR-GATE-M7.5c-a — deferred build gate on the M7.5c-a working tree — [✅ RESOLVED 2026-07-02, beat 50] (NEW 2026-07-02, beat 49)
- **RESOLVED (beat 50):** round 2 GREEN — 53 targeted + 149 full-`check` tasks on Nick's terminal → core committed **`e3d7296`** (10 paths exact) → pushed → **ci.yml ✅ + install-smoke ✅** on the push. M9.1 routing UNBLOCKED (§4b clear). Total: ONE fix round (2 test-compile errors, hub-fixed — within the P5 one-round target for compile-class defects).
- **Milestone:** M7.5c-a (internal-read envelope conformance). **Commit:** pending — working tree on core `2786526`; change set = exactly 10 files (7 source/test + 3 MODULE_CONTEXT).
- **Commands (Nick):** `./gradlew :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` → `./gradlew :api:rest-api:test :lifecycle:lifecycle:test` → `./gradlew check` (count pins stay **71/41/53**; both module-infos + every `build.gradle.kts` unchanged) → `./gradlew :testing:integration-tests:check` (watch `EndpointE2eIT` — DLQ paths moved under `$.data`). `-Werror` rides the convention plugin — do NOT pass it as a CLI flag.
- **Round 1 (2026-07-02):** targeted `compileJava` GREEN + `:testing:integration-tests:check` GREEN + host porcelain exactly 10 ✓; `compileTestJava` FAILED — 2 AssertJ varargs-capture errors (`DlqStatusEndpointTest` L75/L135: `containsExactly` on a `List<?>` actual). **Hub-applied 2-line fix** (SendMessage-to-lane unavailable; mechanical class): the `@SuppressWarnings("unchecked")` typed-local idiom per conforming `ListEntitiesEndpointTest` L83–88. Lesson appended to coder-lessons. **Re-run pending:** `:api:rest-api:test :lifecycle:lifecycle:test` → `check`.
- **Owner:** Nick. **Deferred Build Gate:** YES — the lane's sandbox attempt failed at `build-logic` (mount delete + VM JDK 11 vs 21); evidenced in coder-handoff, never silently skipped.
- **Resolution condition:** `check` green → core commit (staged message, exactly 10 paths) → push → `ci.yml` green on the pushed commit (the gate of record). On resolution: M9.1 routes to the next Coder lane; FE-1b launches on the landing.

#### OR-M7-WIRING — Command-pipeline live wiring — [✅ RESOLVED 2026-06-27 — live-wired at M7.4b/c] (formalized 2026-06-25; NEW in beat 7, 2026-06-22)
- **RESOLVED 2026-06-27 (M7.4b producer + M7.4c confirmation/ledger; v8 beats 17/19; reconciled by the v9 hub).** The command pipeline is LIVE on the real composition root: trigger → run → `command_issued` → co-located dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`, with the live `pending_command_ledger` + `pollExpirations()` tick and paired teardown. The resolution condition's **E2E composition-root confirm gate is met** — `RunPipelineConfirmWiringTest` (host-verified present in `lifecycle/lifecycle/src/test`).
- **Residual → M7.4d (the only remaining M7.4-arc item):** the resolution condition's *second* clause — the **D2 pure-function-replay CI test wired as a gate** (+ registering D2's canonical INV-id) — is NOT yet landed. The *wiring* is resolved; the *replay-safety CI gate* is the M7.4d hardening.
- **Severity:** RESOLVED (was MEDIUM). **Owner:** PM/Coder — done. _See OR-GATE-M7.4 below for the gate-of-record confirm on `db8ab5f`._

#### OR-GATE-M7.4 — gate-of-record (CI) on the pushed `4e31aed` (M7.4d HEAD) — [✅ RESOLVED 2026-07-01 — ci.yml VERIFIED GREEN on core 52824e9 (beat 43)] (NEW 2026-06-27, v9 hub; updated v10 beat-25)
- **RESOLVED 2026-07-01 (v13 hub, beat 43; reconciled into this ledger by the v14 hub):** `ci.yml` GREEN on pushed core `52824e9` — screenshot-verified (Build & Check 3m5s, Gradle 8.8, `check` ✅). Subsumes the per-commit CI-confirm for the whole ancestor chain (`4e31aed`/`9ec5949`/`eed477e`/AB-4/M7.5a/b). The `frontend.yml` first-trigger glance on `52824e9`+`2786526` is a standing Nick item (hub tracks), not an OR.
- **Status:** NARROWED. The **local `./gradlew check` is CONFIRMED GREEN** on the M7.4c working tree that became `db8ab5f` (Nick's terminal 2026-06-27: BUILD SUCCESSFUL, 149 tasks; he then committed + pushed core `69cc6e2..db8ab5f`). So the M7.4b/c/**d** deferred build gates are all **resolved locally** (Nick's terminals: `./gradlew check` 149 tasks green on the M7.4c tree → `db8ab5f`, and again on the M7.4d tree). The only residual is the **gate-of-record** formality: the `ci.yml` GitHub Actions run green on the pushed HEAD **`4e31aed`** (the M7.4d commit has now landed; `db8ab5f`→`4e31aed`). **De-risked (v10 beat-25):** `git ls-tree 4e31aed gradlew` = `100755` (executable) → the prior exit-126 failure mode is structurally cleared, and `ci.yml` runs `./gradlew check --no-daemon`; what remains is purely Nick confirming the Actions run is green on `4e31aed` — the standing b85e1ed/M7.4a pattern, "confirm CI green on the pushed commit, not just locally."
- **Severity:** LOW (the substantive gate — the full local `check` — is green; this is the CI-record formality).
- **Owner:** Nick (glance at the Actions tab for `db8ab5f`). **Resolution condition:** Nick confirms the `ci.yml` run is green on `db8ab5f` → record M7.4b/c CI-gated → close. M7.4d may be authored/routed now (the local gate clears §4b — done, ISSUE-READY beat 22).

#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**

## The archive map (pointer trail; maintained at each rotation — zero dangling addresses)

Any pointer of the form “pm-handoff §<date> (vN beat K)” resolves here. LIVE above: beats 10 / 9 / 8 + Open Risks. ROTATED, verbatim:

- v54 b7–b1 · v53 b9–b1 · v52 b7–b1 · v51 b6–b1 · v50 b7–b1 · v49 b5–b1 + the pre-rotation LV chain → `archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md`
- v48–v45 → `archive/pm-handoff-beats-v45-v48-rotated-2026-08-10.md` · v44–v41 → `archive/pm-handoff-beats-v41-v44-rotated-2026-08-06.md` · v40–v38 → `archive/pm-handoff-beats-v38-v40-rotated-2026-07-31.md` · v37–v32 → `archive/pm-handoff-beats-v32-v37-rotated-2026-07-26.md`
- earlier eras → `archive/pm-handoff-beats-rotated-2026-07-19.md` · `-2026-07-07` · `-2026-07-04` · `-2026-07-02` · `archive/pm-handoff-2026-06.md` · `archive/pm-handoff-2026-05.md` · `archive/pm-handoff-rotated-2026-06-22.md` · `-2026-06-21.md`
- sibling rotations this date: the snapshot chain/mastheads → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-08-21.md` · coder-handoff entries → `context/handoff/archive/coder-handoff-entries-rotated-2026-08-21.md` · coder-lessons 2026-07 → `context/lessons/archive/coder-lessons-2026-07-rotated-2026-08-21.md`
