<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-08 (v25 hub, beat 6 — **THE SKILLS CURRENCY PASS** (Nick's pre-dispatch directive): coder + project-manager + frontend skill SOURCES folded with the arc's durable content (dur-arc conventions · mental-model §3 registries-as-projections · java-patterns §14 `?:`-promotion · format additions #14–15 · PM per-commit-msg + remote-bridge rules · frontend v1.3 §4b IDENTITY IS DURABLE) + env-model §12 (the remote-Cowork bridge model) — ALL pointer-not-copy, zero volatile state entered any skill; **Check 9 STALE ×3 until Nick's mirror sync**; beat-5 landed (bench `9c98240` 2-exact · hivemind `306793c` 4-exact). Pending: hivemind beat-6 (8 M) · nexsys-skills (1 M) · the two 5b closing legs · the DURb word. Detail: the beat-6 section. Prior stamp [beat 5 — **ITERATION 5b: THE DUR PROOF ON SILICON** (`registry.projection_live: devices=2 entities=2 position=14` across a full process restart, `bench-5b-r2` log; view = exactly 2; **the M9.4-KEYb silicon confirmation CLOSED** — one `key_established`, zero failed; device-id continuity log-proven; two closing legs at close: L1 physical pin-2 [Hue power ON → `device_relinked`, no `device_proposed`] + L2 entity-ULID API-to-API [baseline `01KX1PA4HSJ581GASYB7DHE40F` / `01KX1PB9AAB4VB3E10BD477TV3`]) · **M9.5-DURb AUTHORED ISSUE-READY** (`context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md` — the repair + always-print rehydration INFO + BENCH-VERIFY flips 18+3 + permitJoinDeadline clear + fence-reads-installed-policy + apply-caller pin; G-DB1–6 grounded at `ef1d2c5`; dispatch on Nick's word) · **v26 BANKED** (`context/handoff/2026-07-08_PM-mission-control_v26_orchestrator_session_prompt.md`) · bench record 5b block + runbook token/pkill lessons WRITTEN (bench commit = exactly 2 M) · the `6fd6ddc` reused-message staleness reconciled on the record. Fleet at stamp: core ef1d2c5 / docs a53f474 / hivemind 6fd6ddc + beat-5-pending (4: 2 M + 2 new) / bench aa45edc + 5b-pending (2 M) / skills 0c00ece. Watermark **AMD-99, 175/53**.) Prior stamp chain: git history (v25 beats 1–4 + v24 beat-6 stamps) + archive/pm-handoff-beats-rotated-2026-07-07.md frontmatter.
-->

# PM Session Handoff

## 2026-07-08 (v25 beat 6 — **THE SKILLS CURRENCY PASS** (Nick's directive, pre-DURb-dispatch) · three skills + env-model folded, ALL pointer-not-copy · **Check 9 STALE ×3 until the mirror sync** · beat-5 LANDED (bench `9c98240` 2-exact · hivemind `306793c` 4-exact))

**The folds (durable disciplines + permanent contracts ONLY — every volatile fact stayed a pointer; all sources hash-verified ≡ mirrors pre-edit):** **coder** — SKILL.md masthead +dur-arc conventions ×4 (registries-are-projections rules · mirrors-track-schema · pin-ACTUAL-types in Number serde · push-back-on-self-inconsistent-pins); mental-model §3 +the registries-as-projections block (single apply path, write-ahead publish-then-apply, idempotent re-emit as the ONLY update path, replay-from-zero, the mirror-family conventions — REG-INV-1/register-§53 cited as the permanent contract) + §4 +the registry-mutators CANNOT bullet; java-patterns +§14 (the `?:` binary-numeric-promotion re-box, JLS §15.25, pin-actual-types tripwire). **project-manager** — SKILL.md masthead +the per-commit-message-files rule (the `6fd6ddc` lesson) + the remote-bridge pointer (env-model §12); coding-instruction-format +addition #14 (surface-export check against the instruction's OWN DPs before ANY module-info pin — the addition-#9 class, 4th occurrence) + #15 (crash-window rulings for non-atomic multi-event emission contracts; coder-self-review-fleet = LAYER-1 evidence, never a layer-2 substitute). **frontend** — v1.3, +§4b IDENTITY IS DURABLE (entity IDs stable across restarts — deep-links/favorites may bind durably · removal NOT wired, no UI presuming remove · availability-UNKNOWN-post-restart is an HONEST state, render calmly · device provenance now in the log · FROZEN v1.1 unchanged, grounding-question discipline). **env-model §12** — the remote-Cowork bridge model (same-path re-stage = stale container cache · SendUserFile→commit-writes · git-object sourcing · VOLATILE local-MCP scope — every rule paid for this arc). The mental-model's one state-bearing tail line was inspected and left: it self-flags as point-in-time with a re-derive instruction (compliant form).

**Nick, in order:** (1) **hivemind beat-6 commit — stages exactly 8 M** (pm-handoff · PROJECT_SNAPSHOT · coder/SKILL.md · coder/references/homesynapse-mental-model.md · coder/references/java-patterns.md · project-manager/SKILL.md · project-manager/references/coding-instruction-format.md · context/process/cowork-environment-model.md), msg `_scratch/2026-07-08_hivemind_v25-beat-6_commit-msg.txt` → push. (2) **nexsys-skills commit — stages exactly 1 M** (`orchestrators/nexsys-frontend/SKILL.md`), msg `_scratch/2026-07-08_skills_frontend-v13_commit-msg.txt` → push. (3) **MIRROR SYNC ×3** (coder · project-manager · nexsys-frontend) — Check 9 is STALE until done; **sync BEFORE dispatching DURb** so the Coder lane loads the updated skill. (4) The two 5b closing legs (L1 Hue power ON → `device_relinked`, no `device_proposed`; L2 restart → re-`cat` TOKEN → the same two ULIDs) → ⏺ paste. (5) **THE WORD on M9.5-DURb** (paste line: beat-5).

**Hub next:** 5b closing-legs intake (append to the bench record's 5b block) → DURb dispatch on the word → two-layer audit → gates → **THE ACCEPTANCE RUN** → the 72 h soak ON FINAL CODE → v25 retires on the DURb landing — **LAUNCH v26** (banked beat-5).

## 2026-07-08 (v25 beat 5 — **ITERATION 5b: THE DUR PROOF ON SILICON** (`registry.projection_live: devices=2 entities=2 position=14` across a full process restart; view = EXACTLY 2; **the M9.4-KEYb silicon confirmation CLOSES**; device-id continuity log-proven; two closing legs run at close — physical pin-2 + entity-ULID API-to-API) · **M9.5-DURb AUTHORED ISSUE-READY** · **v26 BANKED** (Nick's session-length flag — rotation before the dispatch depth) · the commit trail reconciled · bench record + runbook updated)

**5b evidence read (Nick's precise read CONFIRMED, one correction folded):** the DUR proof line lives in `bench-5b-r2-*.log` (boot 3, 16:33) — the earlier `bench-5b-restart` log is boot 2 (post-wipe `devices=0 position=0`, consumed by the pkill→relaunch port race; its devices=0 is NOT a regression). r2: `devices=2 entities=2 position=14` + DP-6 `device_relinked` ×2 at init (deviceIds match boot-1's adoption lines — device-id continuity proven) + `/api/v1/entities` exactly 2 (`01KX1PA4HSJ581GASYB7DHE40F`, `01KX1PB9AAB4VB3E10BD477TV3` = the L2 baseline). The r2 relinks are DP-6 map-rehydration, NOT physical pin-2 (the Hue was powered off) — hence closing leg L1 (Hue power ON → `device_relinked` on a REAL re-announce, NO `device_proposed`) and L2 (one more restart → the SAME two ULID strings, token re-cat'd). The 403 was the per-launch token rotation (operator-procedure fact; runbook lesson added). `adoption_maps_rehydrated` confirmed logging at DEBUG — silent even at devices=2 → the always-print fix rides DURb (G-DB3). **The 5b block is WRITTEN into the bench record's iteration log (closing-legs results append on Nick's paste — the block says so explicitly); the runbook gains the token-rotation + sleep-after-pkill lessons.**

**M9.5-DURb** (`context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md`, ISSUE-READY, baseline `ef1d2c5`, KEYb-sized): §1 the half-registration repair (DP-B1 LINKED requires ≥1 registered entity, else `half_registration_detected` WARN + fall-through to propose; DP-B2 adopt() REUSES the existing deviceId on hardware-id match — identity continuity, F1-clean; partial multi-entity recorded out-of-scope) · §2 rehydration INFO always-print (DEBUG→INFO, count 0 included; token frozen) · §3 the BENCH-VERIFY javadoc flips (18+3 sites, citations from the bench record, constants byte-untouched) · §4 `attemptReopen()` clears `permitJoinDeadline` (the TCJ R2 close) · §5 the fence reads INSTALLED policy (registry `CapabilityInstance.confirmation()` first, profile fallback; installed-DISABLED ⇒ dispatch + immediate honest UNCONFIRMED, never tracked — the RPT R2 row realized) · §6 the apply-caller census pin (app test, the RegistryMutationRuleTest idiom). STOP gates G-DB1–6 all grounded at `ef1d2c5` this beat. ZERO mints/schema/module-info/surface. Silicon confirmation piggybacks on the acceptance-run entry boot.

**Commit-trail reconciliation (the record corrected):** hivemind `90f0b69` = the beat-3 commit, 5 M exact (its message accurate) · **`6fd6ddc` = 1 M only (pm-handoff, the beat-4 ruling append) but REUSED the beat-3 message file — its "stages exactly 5 M" clause is STALE against its own diff; this note is the correction of record** (the message content is otherwise accurate; the 5 M landed in `90f0b69`). Lesson absorbed: per-commit message files, even for ride-along spine deltas.

**Nick, in order:** (1) **the two 5b closing legs** (L1 Hue power ON → `device_relinked`, no `device_proposed`; L2 restart [sleep-after-pkill] → re-`cat` TOKEN → curl → the SAME two ULIDs) — ⏺ paste either way; I append to the 5b block and it closes. (2) **bench commit — stages exactly 2 M** (the record + the runbook), msg `_scratch/2026-07-08_bench_iteration-5b_commit-msg.txt` → push. (3) **hivemind beat-5 commit — stages exactly 4** (2 M: pm-handoff · PROJECT_SNAPSHOT; 2 new: the DURb instruction + the v26 prompt), msg `_scratch/2026-07-08_hivemind_v25-beat-5_commit-msg.txt` → push. (4) **THE WORD on M9.5-DURb** — paste: *"Read nexsys-hivemind/context/instructions/2026-07-08_M9.5-DURb_half-registration-repair-and-seam-pack_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (5) Standing ledger unchanged (+ the two `_scratch/*.HEAD.tmp.md` temps, deletable).

**Hub next:** DURb intake → two-layer audit → gates → commit/push/CI → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero [hub authors the config on the 5b ULIDs] + RESUME leg → P5 §51 → P6 + PJ key OUT) → **the 72 h soak ON FINAL CODE** (the beat-4 ruled sequencing; NQ-6 = the DUR+DURb proof at scale) → seed-custody + explainability in the soak window → **v25 RETIRES on the DURb landing or sooner at Nick's word — LAUNCH v26** (banked this beat: `context/handoff/2026-07-08_PM-mission-control_v26_orchestrator_session_prompt.md`).

## 2026-07-08 (v25 beat 4 — **M9.5-DUR LANDED `ef1d2c5` + CI GREEN ×2 (gate of record CLOSED)** · **DURb RULED (a) BEFORE-THE-SOAK, 5b-FIRST** (verbatim below) · **5b MID-FLIGHT: leg-1 + KEYb piggyback PROVEN; restart legs pend the log read** · two handoff gaps OWNED)

**THE DURb ROUTING RULING (Nick, 2026-07-08 — PRESERVE VERBATIM):** *"(a) — author now, land before the soak. One firm sequencing constraint: after iteration 5b. The decisive reason is a principle, not a preference: **you soak the code you ship.** The 72 h soak with NQ-6 is the durability certification — so the durability foundation must be final before it runs. Landing DURb after the soak means shipping a change to the identity path that the certification never exercised; you'd owe a re-soak or ship un-soaked durability code. That's exactly the asterisk you don't want on the one guarantee AMD-99 exists to make. And the flip side is the upside: NQ-6 is DURb's natural test bed — a restart spike is precisely what stresses the re-registration/re-link path, so landing before the soak means the soak validates the fix instead of running around it. The cost of doing it now is near-zero, which removes the only reason to defer: it's F1-clean — no new event type, no AMD, no governance latency — and it clears accumulated debt in one pass (the long-deferred BENCH-VERIFY javadoc flips, the permitJoinDeadline one-liner, the SD-3 fence polish, the apply-caller pin). Several of those matter during certification: the permitJoinDeadline clear and the reopen path get exercised in the acceptance P6 reopen leg and across soak restarts. So landing them before the soak means the whole certification chain runs on tidied code. The schedule pays for it — it's weeks to the go/no-go, and DURb is a KEYb-sized micro-WU. The one hard line: 5b first. 5b proves the DUR foundation clean on silicon (same IDs across restart, pin-2 re-link fires); don't stack the edge-fix on an unvalidated foundation, or a 5b surprise makes you debug DUR + DURb together. Preferred path: 5b → DURb → acceptance run → 72 h soak (both the acceptance evidence and the soak then certify final code). If you'd rather bank the acceptance-run milestone first, 5b → acceptance → DURb → soak is acceptable — the non-negotiable is simply that DURb lands before the soak, not during or after it."*

**5b evidence so far (Terminal-1 log, 16:21–16:25):** the wipe was surgical (`homesynapse-events.db` gone, `zigbee/` intact); fresh DB, 5 migrations; **`EventTypeRegistry initialized with 55 event types` + `JacksonWarmup pre-populated 55` — the mints live on silicon**; **`registry.projection_live: devices=0 entities=0 position=0`** (Phase-3-before-Phase-6 ordering visible: projection LIVE at :31.465, adapter init :31.915); resume exact ch20/`0x9b65`. **Hue:** announce → proposed COMPLETE → `proposal_accepted source=config` → **`device_adopted deviceId=01KX1PA4GRZHY2GD37B5CFVQHY entities=1`** → `reporting_configured 3/3`. **SNZB:** the hold = leave (16:22:35) → rejoin :41 → proposed → adopted **`deviceId=01KX1PB9A5931A8G0F0X03QXT2 entities=1`** → 2/3 (the known sleepy posture) → **`key_established: TC_REQUESTER_VERIFY_KEY_SUCCESS`, ZERO `key_establishment_failed`, then STAYED to shutdown — THE KEYb SILICON CONFIRMATION IS CLOSED** → `ias_zone_enrolled`. **UNPROVEN (not failed):** the restart legs — the pkill→instant-relaunch race left the old instance dying while the new one grabbed the port (the old watchdog's `transport_failed`/`reopen_no_target` at 16:25:05 is shutdown noise, not a defect); the grep raced the boot; `$TOKEN` was unset in the fresh ssh session so the 401 is an auth artifact. **Both handoff gaps are the hub's (playbook §8):** the block should have mandated a sleep-after-pkill and re-established TOKEN per block. Instrument note: `adoption_maps_rehydrated` did not print at count 0 (first boot, empty registries) — verify it prints with 2 in the restart log; if it is count-gated, that is a vacuous-silence seam for DURb to fix (one INFO, always). Also noted: pre-restart entity-ULID baseline wasn't captured (TOKEN) — device-id continuity is provable against this log's adoption lines; entity-id continuity gets an API-to-API leg across ONE more restart.

**Nick, remaining 5b legs (corrected block in the beat-4 chat handoff):** (1) read the restart-instance log — expect `registry.projection_live: devices=2 entities=2` + `adoption_maps_rehydrated` 2 + resume; transport errors ⇒ the stale-NCP remedy (dongle power-cycle, relaunch). (2) TOKEN + `/api/v1/entities` — exactly 2 rows, ⏺ RECORD both ULIDs. (3) **Hue wall power ON** (it is currently off — that IS the re-announce): expect **`zigbee.device_relinked: … deviceId=01KX1PA4GRZHY2GD37B5CFVQHY` and NO `device_proposed` — pin-2 across restart PROVEN**. (4) ONE more restart (pkill → **sleep 5** → relaunch) → TOKEN + curl → **THE SAME two ULIDs** = entity-id continuity API-to-API. ⏺ RECORD + paste either way. Then: **hivemind beats-3+4 commit — stages exactly 5 M**, msg `_scratch/2026-07-08_hivemind_v25-beat-3_commit-msg.txt` (covers both beats; the beat-4 delta is this file + snapshot ride-along).

**Hub next:** 5b close intake (⏺ RECORDs → the bench record's iteration log + the bench commit) → **author M9.5-DURb** per the ruling (the repair + the packed seam rows; KEYb-sized; the rehydration-INFO always-print fix joins the pack) → dispatch on the word → audit → gates → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero + RESUME leg → P5 §51 → P6 + PJ key OUT) → **the 72 h soak on FINAL code** (the ruled sequencing) → seed-custody + explainability in the soak window → v26 before depth (4 beats deep — v26 banks next beat).

## 2026-07-08 (v25 beat 3 — **M9.5-DUR DELIVERED + TWO-LAYER AUDIT ACCEPT (zero code defects at layer-2)** · Nick's full `check` GREEN 156 PRE-COMMIT (deferred gate DISCHARGED) · R1 ACCEPT (authoring miss OWNED) · **R2 half-registration gap CONFIRMED → M9.5-DURb queued, ROUTING = NICK'S RULING** · R3 ACCEPT + polish row · **ITERATION 5b IS THE LIVE GATE — bench ONLY after the CI glance is green**)

**The intake (61 working-tree changes: 39 M + 22 new; the Coder ran its own 3-lens adversarial review fleet in-session and fixed its three fixable findings; hub layer-2 direct seam reads at the working tree):** adopt() emission order EXACT — `device_registered` publish `:275` → apply `:284` → per-entity `entity_registered` `:319`→`:328` → the RETAINED `device_adopted` `:357` (payload + `zigbee.device_adopted` token byte-unchanged); `reinstallOverrides` GONE (0 hits repo-wide); posture routing publish-first-then-apply (`:740`→`:751`, the write-ahead javadoc in place); checkpoint reset `:525` + `awaitRegistryProjectionLive()` `:589` gating Step 3.4; EventTypes = 73; **all SIX pin sites at new values (73/43/55)** — incl. the two the instruction didn't enumerate (`JacksonWarmupTest:112` + `EventCategoryMappingTest`, coder-found — the R-E "et al." honored); category rows pattern-exact (`DEVICE_STATE, DEVICE_HEALTH`); the rule at `HomeSynapseArchRules:393`, checked `:428`, **teeth PERMANENT** (`RegistryMutationRuleTest` — the un-excluded condition rejects a violator on every build, not scratch-and-remove); **apply-caller census = exactly 6 call sites in 3 sanctioned classes** (slice ×2 · adapter ×1 · subscriber ×3). Suite: 1,829 targeted forced-fresh green + **Nick's full `./gradlew check` GREEN (156) — the deferred gate is DISCHARGED; residual = core commit + push + CI glance (the gate of record; bench ONLY after green).** `ProcessRestartIdentityIT` re-run green in isolation on Nick's terminal.

**Adjudications:** **R1** (device-model `requires com.homesynapse.event` → `requires transitive`, one word + rationale comment) = **ACCEPT — the hub's authoring miss, OWNED**: the instruction's zero-module-info pin contradicted its own DP-2 (public `RegistryProjection`/`RegistryEventMapper` naming event payloads on an EXPORTED surface) — the format doc's addition-#9 class (module-info pins validated against every mandated behavior), recurred at M9.5-DUR; the Coder's promotion is the three-precedent M2.9/M3.6e.1/M5-A resolution, Gradle already `api`, direction acyclic (event never requires device). **R2** (the durable half-registration gap — the Coder's review fleet CONFIRMED it and correctly improvised NO arm) = **REAL, inherited from AMD-99 §3's non-atomic emission**: death between the durable `device_registered` and its entities' publishes leaves an entity-less device the LINKED arm re-links forever (no re-propose; no removal emitter; replay faithfully rebuilds the hole). Millisecond window — NOT a 5b/bench blocker; it IS the one open durability asterisk, and the north star says close it before freeze. **R3** (factory ctor 3→4 · public unguarded `HomeSynapseCore.registryProjection()` · rig ctor · lifecycle test-scope logback) = **ACCEPT** with the census recorded and a NAMED polish row: pin the apply-callers structurally (an unpublished `apply()` would be registry state not derived from the log — latent-only, zero rogue callers today). **INFOs ×4 ACCEPT:** DP-6 rehydration publishes one `availability_changed` per registry-carried device at `initialize()` (EXPECTED ×2 at the 5b restart — in the glance list) · `onCaughtUp()` realizes the instruction's "onLive" hook (authoring imprecision, fine) · Number canonicalization to Integer/Long/Double at the mirror boundary, red-first-proven (the `?:` autoboxing catch → coder-lessons) · `entity_registered` bumps the entity's view stateVersion (intentional cursor semantics, recorded).

**M9.5-DURb (queued micro-WU — authors on Nick's routing ruling):** the half-registration repair. REC shape: the LINKED fork re-proposes when `listEntitiesByDevice` is EMPTY, and adopt() REUSES the existing deviceId on a hardware-id match (identity continuity; the F1 idempotent re-emit makes the repair log-clean — no new event type, no AMD). PACKS the queued zigbee-seam rows while the module is open: the BENCH-VERIFY javadoc flips (0x0019/0x90 · TCJ · NCFG · RPT · KEY/KEYb — all silicon-proven) · the R2-SD-3 fence-reads-installed-policy polish · the `permitJoinDeadline`-clear-in-`attemptReopen()` one-liner · the R3 apply-caller pin. **Routing options: (a) author now, land BEFORE the soak [REC — the soak's NQ-6 restart spike is its natural test bed] · (b) post-soak · (c) fold into the acceptance-run window.** One letter dispatches.

**ITERATION 5b (the WU's own gate — run AFTER the CI glance is green; full paste-block in the beat-3 chat handoff):** stop app → **ONE-TIME events-DB wipe** (everything under `~/hs-bench/data/` EXCEPT `data/zigbee/` — custody preserved; the 3 orphan rows die here) → relaunch → glance `registry.projection_live: devices=0 entities=0` (the projection's first positive-evidence read) + resume ch20/`0x9b65` → **re-adopt ×2** (Hue ONE wall power-cycle → propose → `proposal_accepted source=config` → adopted; SNZB ONE ~5 s hold in-window → **the KEYb piggyback: exactly one `key_established`, ZERO `key_establishment_failed`**) → ⏺ RECORD both deviceIds + both entity ULIDs (`/api/v1/entities` = exactly 2 rows) → **RESTART the app** → done-when: `registry.projection_live: devices=2 entities=2` + `adoption_maps_rehydrated` count 2 + **THE SAME two entity ULIDs** + ~2 expected `availability_changed` (the DP-6 INFO, benign) → Hue ONE wall power-cycle → **`zigbee.device_relinked: … deviceId=<SAME>` with NO `device_proposed` — PIN-2 SILICON-PROVEN** → view still exactly 2. What-ifs (≤3): `device_proposed` on the re-link leg ⇒ STOP-and-ping · `projection_live` ≠ 2/2 post-restart ⇒ paste the boot log · any `key_establishment_failed` ⇒ STOP-and-ping. Config UNCHANGED (accept-list + `permit_join_duration: 254` stay — pairing phase; the PJ key comes OUT at soak entry).

**Nick, in order:** (1) **core commit — `git add -A` stages exactly 61** (39 M + 22 new), msg `_scratch/2026-07-08_core_m95-dur_commit-msg.txt` → push → **CI glance (ci.yml + install-smoke — the gate of record; bench ONLY after green)**. (2) **hivemind beat-3 commit — stages exactly 5 M** (pm-handoff · PROJECT_SNAPSHOT · coder-handoff · coder-lessons · cross-agent-notes), msg `_scratch/2026-07-08_hivemind_v25-beat-3_commit-msg.txt` → push. (3) **THE DURb ROUTING** — (a) before-soak [REC] / (b) post-soak / (c) acceptance-window. (4) **ITERATION 5b** per the paste-block — ⏺ RECORD + paste either way. (5) Standing: frontend.yml `199324e` confirm-once · security glance · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2. (Housekeeping: two `_scratch/*.HEAD.tmp.md` staging temps are deletable at leisure; the local Filesystem MCP re-registered scoped to core mid-day — hivemind writes ride the bridge, no action needed.)

**Hub next:** 5b intake at the ⏺ RECORDs (the bench record's iteration log gets the 5b block) → **THE ACCEPTANCE RUN** (P3.11–12 → **P4 the bench-hero automation** [the hub AUTHORS the config — entity IDs are durable now] + the restart RESUME leg → **P5 §51 live** [CONFIRMED only via authoritative reports · identify = immediate honest UNCONFIRMED · superseded expires verdict-free · latencies vs corpus · IAS ×2 twins · Q10 · FE-7] → **P6 reopen + REMOVE the PJ key**) → **the 72 h soak ENTERS on acceptance** (PJ key OUT first; **NQ-6 restart DURING = the DUR proof at soak scale**; W2-VAL synthetic legs chartered at entry) → soak-window WUs (idle-bench packing): **seed-custody [the SD-5 HARD GATE — lands before ANY non-bench network]** + M9.5-DURb on the ruling + the explainability-lane return audit → Wave-2 unboxing on arrival → the backlog M9.4 mega-row compaction at the next currency pass → **v26 authored before depth** (3 beats deep; bank at ~5).

## 2026-07-08 (v25 beat 2 — beat-1 LANDED (docs `a53f474` 5-exact · hivemind `1d95288` 4-exact) · CI `e15858d` RE-CONFIRMED GREEN · **M9.5-DUR AUTHORED ISSUE-READY against the RATIFIED AMD-99 + carry-list C1–C4** · **DISPATCH ON NICK'S WORD**)

**The grounding survey (hub-direct at `e15858d`):** **C1 = EXACTLY FOUR production mutator call sites** — `ZigbeeAdoptionSlice:238` (`createDevice`, adopt) · `:272` (`createEntity`, adopt) · `:426` (`updateEntity` in `reinstallOverrides` — the pin-2 override re-install; **REMOVED per Nick's predicted resolution**: post-DUR the adoption-time tuning persists via replay, and a profile-file change silently mutating the registries is exactly what REG-INV-1 bans) · `ZigbeeIntegrationAdapter:704` (`updateEntity` in `applyPostureRouting` — the M9.4-RPT posture routing, a GENUINE capability change; **becomes the F1 idempotent re-emit of `entity_registered`**). Zero production `remove*/enable*/disable*` callers — the tombstone apply is their first caller, via the projection. **C3: the awaitable hook EXISTS** — `awaitProjectionLive()` (`:1184`, poll on `SubscriberMode.LIVE` via the bus's subscriber snapshot) already gates Step 3.4 (automation binding) at `:567`; the ONE sanctioned addition is a sibling `awaitRegistryProjectionLive()`, same shape. **Write-ahead verified at source:** `EventPublisher` — *"durable in SQLite WAL before this method returns; subscriber notification begins asynchronously after return"* — publish-then-apply-inline realizes REG-INV-1's clause exactly, adoption stays synchronous. **The from-zero subtlety resolved:** in-memory registries must replay from 0 EVERY boot ⇒ `CheckpointStore.writeCheckpoint(id, 0L)` pre-subscribe (contract: 0 ⇒ begin from the start of the log); the AMD §5 direct-read fallback stays named coder latitude (flag before switching). **C2 forest inventoried to the leaf:** Device(14) / Entity(12) / CapabilityInstance(7) / AttributeSchema(11) / CommandDefinition(6) / ParameterSchema(7) / ExpectedOutcome(3) / Expectation(sealed ×4: ExactMatch/WithinTolerance/EnumTransition/AnyChange — AttributeValue-bearing) / ConfirmationPolicy(4); `AttributeType` + `AttributeValue` stay TYPED (value-model leaf — event-model `requires transitive` ✓); other enums flatten to names; `Number` rides the AMD-52/AMD-87 lossless discipline; Set→sorted-List determinism mandated. **ZERO module-info changes** (every home fits the existing graph — pinned in-instruction; mirrors in `com.homesynapse.event`, `RegistryProjection` in `com.homesynapse.device` [device requires event ✓], subscriber wrapper in lifecycle, rule in app tests). **DP-6 realized:** adapter maps rehydrate FROM the registries at startup via the EXISTING `relink` — the `:159` `findByHardwareIdentifier` fork then takes LINKED naturally, so a post-restart re-announce fires `onRejoin` (pin-2's silicon leg). Pins mapped: `EventTypesTest:46`=71 · `EventTypeRegistryTest:33/:131`=41 + `:46`=53 · `JacksonWarmupTest:43`=53 · `EventCategoryMapping` +2 rows matching `DEVICE_ADOPTED`'s (`DEVICE_STATE, DEVICE_HEALTH`) · `AllEventClasses` (persistence TEST aggregator) +2.

**THE INSTRUCTION** (`context/instructions/2026-07-08_M9.5-DUR_registries-as-projections_coding-instruction.md`, ISSUE-READY, baseline `e15858d`): STOP gates **G-DUR1–7** (G-DUR2 fifth-mutator STOP = the C1/G2 sizing signal; G-DUR6 mirror-drift STOP = the C2 gate; G-DUR7 pins the tombstone's SubjectRef-carries-identity shape) · settled **DP-1–8** (payload (A) · apply-home device-model `RegistryProjection` = the rule's sole subject, apply consumes ONLY payloads so live-apply ≡ replay-apply by construction · emission device→entities→`device_adopted`-RETAINED · F1 re-emit + `reinstallOverrides` deletion · checkpoint-reset + ONE await · DP-6 rehydration · R-D tombstone · upsert-in-log-order idempotency) · verbatim module-info embeds ×2 · the full-fidelity mapping table · P2 survey incl. the **behavioral publish-count sweep** (adopt() gains 1+N publishes — the M6.4 stale-pin lesson named, whole-suite grep mandated) · tests-first: `ProcessRestartIdentityIT` in the lifecycle test tree (the M9.4a R1 test-home precedent; `RestartHonestyIT` structurally cannot cover process restart) + the C4 tombstone leg + deep codec round-trips + rule-bites-red-first · the anti-vacuous **`registry.projection_live: devices= entities= position=` INFO at onLive = iteration 5b's boot glance-point** (playbook §1) · §4c Clock reminder · **done-when = ITERATION 5b; unit-green ≠ done**.

**Ledger:** CI `e15858d` re-confirmed GREEN by Nick (GitHub) — the KEYb gate of record stays CLOSED; §4b CLEAR. Standing: frontend.yml `199324e` confirm-once · security glance · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2.

**Nick, in order:** (1) **hivemind beat-2 commit — stages exactly 3** (2 M: pm-handoff · PROJECT_SNAPSHOT; 1 new: the M9.5-DUR instruction), msg `_scratch/2026-07-08_hivemind_v25-beat-2_commit-msg.txt` → push. (2) **THE WORD on M9.5-DUR** — dispatch paste (CC lane): *"Read nexsys-hivemind/context/instructions/2026-07-08_M9.5-DUR_registries-as-projections_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (3) Standing ledger above.

**Hub next:** DUR delivery intake → two-layer audit (quote-the-instruction-verbatim prompts; labels-are-claims, quotes-are-evidence) → gates → commit/push/CI → **ITERATION 5b** (the ratified bench protocol — the operator paste-block authors at 5b entry per playbook §8: ONE events-DB wipe with custody `data/zigbee/` PRESERVED → re-adopt ×2 → RESTART → SAME ids resolve + `registry.projection_live` at boot → the Hue re-announce takes the RE-LINK arm → view = exactly the live entities → the KEYb piggyback check) → **THE ACCEPTANCE RUN** → the 72 h soak → seed-custody → explainability → **v26 authored before depth** (2 beats deep; bank at ~5).

## 2026-07-08 (v25 beat 1 — LAUNCH FULL-PASS (all 11; §11 CLEAN — every porcelain flag hashed ≡ HEAD, all phantom) · **THE AMD IS RATIFIED: AMD-99 (Registries-Projection)** — *"ratify as recommended — (A), and R-B through R-E as written"* · mechanics EXECUTED (rename · REG-INV-1 minted 174→175 · +`REG` 52→53 · watermark AMD-98→AMD-99 · Docs 01/02/12 pointer folds) · folds F1–F3 applied pre-ratification · **G1 DISCHARGED — the DUR coding WU authors next; DISPATCH ON NICK'S WORD**)

**Launch:** §11 CLEAN — lock-free porcelain ×5; every M/D/RD/?? flag across core/hivemind/bench hashed IDENTICAL to its HEAD object (CR-normalized md5, 6 spine files) — ALL PHANTOM (read-lag + fabricated-index classes; the `RD` rename-target cut mid-filename was the named env-model §2 signature); zero real uncommitted edits, zero ghost writes. Baselines re-derived: core `e15858d` · docs `2a90e7f` (the AMD-proposal commit LANDED) · hivemind `880a5a6` (beat-6 3-exact LANDED) · bench `aa45edc` · skills `0c00ece`. The uploaded AMD ≡ the on-disk file ≡ the HEAD object (md5) — the text Nick ruled on is the text of record. Preflight PASS all 11 — notable: Check 9 all 15 mirror files byte-size-identical to source (zero v24-arc skill edits); W28 IN_PROGRESS; cross-agent ACTIVE: 0; Open Risks §4b CLEAR; **174/52 + watermark AMD-98 re-derived at the register BEFORE the mint** (§17 rows counted = 174; §0.3 rows counted = 52); backlog M9.4 mega-row compaction stays QUEUED (the v25 prompt already carries it — not a staleness finding). v24 prompt archived (R, this commit). Required reads IN FULL: v24 beats 1–6 · the AMD · coder-handoff NEXT-WU · bench record iterations 1–5a · runbook field-validated + amended step-11 · the playbook · env-model · the cold-boot trio · truth-hierarchy.

**THE RATIFICATION (Nick, 2026-07-08 — PRESERVED VERBATIM; the DP-18-C-class record):**

```markdown
Verdict: ratify as recommended — (A), and R‑B through R‑E as written.
This is the strongest work product the arc has produced, and it earns ratification cleanly. It doesn't just answer the five things I asked for at the checkpoint — it exceeds several of them, and it caught things I didn't ask for. I'll walk why it's sound, reframe what it actually buys us, then give the DUR coding WU its carry-list (none of which blocks ratification).

Why it's sound
R‑A is decided on exactly the right criterion, and the rejection of (B) is airtight. (B) fails because its rebuild depends on zigbee-devices.json + profile files — non-log artifacts that can drift, lag, or be deleted — so the projection wouldn't be a pure function of replay. That's precisely the disqualifier. And the denormalization note (§2) is the sharpest line in the document: storing capability schemas in the event isn't waste, it's recording what was installed at adoption, including the DP‑a confirmation tuning — the fact a trust product must be able to prove later, independent of what a profile file says today. That's not a workaround; it's event-sourcing done correctly.
R‑B ships the invariant with teeth and reasons correctly about the reach subtlety. REG‑INV‑1 + the REGISTRY_MUTATION_ONLY_VIA_PROJECTION ArchUnit rule landing in the DUR WU, no N‑2 deferral — exactly what I demanded. And §4's handling of the 2026‑06‑13 enforcement-reach caveat is correct: that caveat is about non‑app test code; this rule targets production code, which app's test classpath catches across every module. Whoever wrote this understood the trap and stepped around it.

R‑C is the P1 boundedness proof I wanted — a registry-projection subscriber on the existing bus replay machinery, Phase‑3 CORE_DOMAIN, riding the existing Phase‑3‑before‑Phase‑6 catch-up invariant, with exactly ONE sanctioned addition (a composition-root await) and a STOP on anything wider. The fallback (direct ordered store read) is named as coder latitude. Bounded, existing-seam, flagged.

The correctness detail that shows real care: the write-ahead + idempotent-by-identity apply (REG‑INV‑1) means adoption still populates the registry synchronously — the event is durable, then applied inline, and the later bus delivery is a no-op. So there's no async gap at adoption; entities stay immediately usable. That's the subtle thing a weaker version would have broken.
R‑D (tombstone vs one-time bench wipe, cleanly separated, additive/no-migration) and R‑E (mints explicit — and it caught the EventCategoryMapping +2 easy-miss and the AllEventClasses aggregators, plus the register deltas 174→175 / 52→53) are both exactly right.

What this actually buys us — reframe
This isn't a bug fix with a governance cost. Ratifying (A) extends the moat itself: the event log now carries full device provenance — what was adopted, as what, with which capabilities and confirmation tuning, and when — replayable and immutable. For a product whose entire pitch is the honest, single, replayable log, closing "the one place everything-is-derived-from-the-log had a hole" (§1) makes the moat more complete, not just patched. That's why doing (i) instead of the snapshot was the right call, and the AMD proves it was affordable.

Carry-list for the DUR coding WU (not ratification blockers)

The refactor scope is ALL registry mutators, not just adopt()'s — §8's phrasing ("the single-apply-path refactor of adopt()'s registry writes") slightly undersells it. The whole point of the ArchUnit rule is that nothing outside the projection mutates the registries. The grounding survey must enumerate every current mutator before the refactor — including the M9.4a pin‑2 relink path's updateEntity and any availability/update writes — and route or retire each. Likely resolution: pin‑2's re-install of confirmation overrides becomes redundant post‑DUR (the tuning now persists via replay) and is removed; a genuine capability change (Q10 re-interview) rides an idempotent re-emit of entity_registered, not a third event type. The rule will catch any missed mutator, which is good — but if the survey finds many, that's the G2 sizing signal to flag before dispatch.

The capability-mirror layer is the WU's largest chunk and where G2 risk concentrates. Mirroring the full attribute/command/confirmation schema forest into event-model-local records, both directions (emit + apply), under the "full-fidelity, no silent drops" STOP gate. This is bounded but it's the part most likely to cascade — watch it specifically, and add a MODULE_CONTEXT gotcha that the mirrors must track the device-model schema (a new schema field needs a mirror field or an explicit exclusion ruling).

The intra‑Phase‑3 ordering is the load-bearing seam — the registry projection must be caught up to log head before automation definitions bind entity refs, not just before Phase 6. §5's "one flagged await" is the right sanctioned fix; the survey just has to prove the exact hook exists or add the single await.
Test device_removed even though nothing emits it yet — feed the projection the tombstone and assert the device+entities drop, so the mechanism is proven complete though unexercised in production.

Two optional tightenings (fold before ratifying, or leave to the WU — your call):

1. One line that registration updates ride idempotent re-emit of the existing two types — no third event type — so the +2 mint is closed and can't be silently reopened later. (REG‑INV‑1's idempotency clause implies it; making it explicit locks the count.)
2. A half-line precision on §6/§8: durable IDs fix the restart-orphan class specifically; the removal→state‑view cleanup rides the future removal-emission milestone (correctly out of scope now — just don't let "fixes orphan accumulation" read as fixing the removal class too).
```

**Mechanics executed (AMD §9, all four steps):** (1) number assigned + file renamed `design/amendments/AMD-99_Registries-Projection.md` (the dated PROPOSED name retired; ruling box + header restamped RATIFIED). (2) Register: **REG-INV-1 minted** — §0.3 table row + prose sentence, §17 index row, §18 traceability row, the NEW §53 section (invariant text verbatim from AMD §4; cited parents INV-ES-02/04/05/09 — every title source-verified against the register BEFORE citing), Total **174→175 / 52→53**, the regeneration line updated, **watermark AMD-98→AMD-99** — all in the SAME change. REG is a NAMED category (not `AMD-99-INV`-scoped) per the ratified §4 text; the departure from the §20 convention is recorded in the register's own §0.3/§53 prose. (3) Pointer notes folded: **Doc 01** (masthead blockquote + §4.3 device-family inline note), **Doc 02** (masthead amendment-status fold — now ends "No further amendments pending"), **Doc 12** (masthead blockquote + §3.5 Step-3.1 inline — the step's stated "load from the domain event store" intent gains its ratified realization). (4) **G1 DISCHARGED.** **Folds applied pre-ratification (his two offered tightenings + the §8 scope precision his carry-list stated, folded per edits-fold-verbatim):** **F1** = §3 update-semantics line (idempotent re-emit of the SAME two types, no third — locks R-E's +2 closed); **F2** = §6 scope-precision + §8 out-list echo (restart-orphan class vs the removal class); **F3** = §8 in-list rescope (**ALL registry mutators**, not just `adopt()`'s writes — enumeration is the plan, the ArchUnit rule is the backstop). Rationale-index DUR row updated with the ratification pointer (this commit).

**THE CARRY-LIST (binding on the DUR WU authoring — none blocked ratification):** **(C1)** the refactor scope is ALL registry mutators — the grounding survey enumerates EVERY current production mutator BEFORE the refactor (incl. the M9.4a pin-2 relink path's `updateEntity` + any availability/update writes), each routed through the projection-apply or retired; likely resolutions per Nick: pin-2's re-install of confirmation overrides becomes REDUNDANT post-DUR (the tuning persists via replay) and is removed; a Q10 re-interview capability change rides an idempotent re-emit of `entity_registered`; a MANY-mutator survey result = the G2 flag-before-dispatch signal. **(C2)** the capability-mirror layer is the G2 concentration point (the WU's largest chunk — the full attribute/command/confirmation schema forest mirrored BOTH directions, emit + apply, under the full-fidelity/no-silent-drops STOP gate) — watch it specifically; ADD the MODULE_CONTEXT gotcha: the mirrors must track the device-model schema (a new schema field needs a mirror field or an explicit exclusion ruling). **(C3)** the intra-Phase-3 ordering is the load-bearing seam — the registry projection caught up to log head BEFORE automation definitions bind entity refs, not just before Phase 6; the survey proves the exact awaitable hook or adds the ONE sanctioned composition-root await. **(C4)** test `device_removed` though nothing emits it — feed the projection the tombstone, assert the device + its entities drop (the mechanism proven complete though unexercised in production).

**Nick, in order:** (1) **docs commit — pre-stage porcelain shows 6 lines (4 M + 1 D + 1 ??); `git add -A` stages exactly 5 entries** (4 M: `governance/Architecture_Invariants_v1.md` · `design/01-event-model-and-event-bus.md` · `design/02-device-model-and-capability-system.md` · `design/12-startup-lifecycle-shutdown.md`; 1 R: the AMD → `design/amendments/AMD-99_Registries-Projection.md`), msg `_scratch/2026-07-08_docs_amd-99-ratification_commit-msg.txt` → push. (2) **hivemind beat-1 commit — pre-stage porcelain shows 5 lines (3 M + 1 D + 1 ??); `git add -A` stages exactly 4 entries** (3 M: pm-handoff · PROJECT_SNAPSHOT · decision-rationale-index; 1 R: the v24 prompt → `archive/`), msg `_scratch/2026-07-08_hivemind_v25-beat-1_commit-msg.txt` → push. STOP if host porcelain shows different counts. (3) **No further word needed for the WU authoring** (G1 is discharged; authoring proceeds) — **the DISPATCH paste line comes with the finished instruction.** (4) Standing: frontend.yml `199324e` confirm-once (the one open glance) · security glance · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2.

**Hub next:** **author the DUR coding WU against the RATIFIED AMD-99 text + carry-list C1–C4** (grounding survey FIRST: the all-mutators enumeration · the Phase-3 catch-up hook proof · the mirror-forest inventory · the count-pin test map; MODULE_CONTEXTs + verbatim module-infos embedded; the full-process-restart identity IT; §4c Clock reminder; bench-iterative done-when = ITERATION 5b, unit-green ≠ done) → **DISPATCH ON NICK'S WORD** → two-layer audit → gates → **iteration 5b** (ONE bench events-DB wipe, custody `data/zigbee/` PRESERVED → re-adopt ×2 under the new flow → RESTART → the SAME deviceIds/entityIds resolve → the Hue re-announce takes the RE-LINK arm, pin-2 silicon-proven → `/api/v1/entities` = exactly the live entities → the KEYb piggyback check) → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero + restart RESUME leg → P5 §51 → P6 + PJ key OUT) → the 72 h soak (NQ-6 = the DUR proof) → seed-custody JIT → the explainability-lane return → **v26 authored before depth**.

## 2026-07-08 (v24 beat 6 — THE CLOSE: **v25 BANKED** (rotation before depth, per Nick's directive) · **THE REGISTRIES-PROJECTION AMD AUTHORED — RATIFICATION PENDING (the v24 capstone)** · CI GREEN on `e15858d` (the KEYb gate of record CLOSED — **all core gates green through HEAD**) · **LAUNCH v25 ON NICK'S WORD**)

**The AMD** (`homesynapse-core-docs/design/amendments/2026-07-08_Registries-Projection_Amendment_PROPOSED.md` — neutral-numbered per the in-flight convention; renamed to `AMD-NN_` at ratification): the §0 RULING BOX makes it one-turn-rulable — **R-A** payload shape = **(A) full-fidelity records** decided on Nick's criterion (log-alone reconstruction, pure function of replay; (B) REJECTED: its rebuild depends on `zigbee-devices.json` + profile files — non-log artifacts, impure replay), with the denormalization stated honestly (the event records what was INSTALLED at adoption incl. the DP-a tuning — the fact a trust product must prove later); **R-B** REG-INV-1 minted WITH teeth — the `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` ArchUnit rule ships IN the DUR WU (production-code reach from the app rules file covers every module; no N-2 deferral needed) + the single-apply-path / idempotent-by-identity / write-ahead pins; **R-C** the boot rebuild = a registry-projection subscriber on the EXISTING replay machinery, Phase 3 CORE_DOMAIN (registries+state-store+automation already live there; integrations at Phase 6; the `:731` catch-up ordering invariant is the boundedness proof) — the ONE flagged addition: a composition-root await if no hook exists, anything wider = STOP; **R-D** `device_removed` = first-class tombstone (exists, zero emitters — the projection honors it from day one) + orphans separated: design mechanism vs the ONE-TIME bench events-DB wipe at 5b entry (custody preserved; NOT a migration; new types additive); **R-E** mints explicit — EventTypes 71→73 · @EventType 41→43 · codecs 53→55 · EventCategoryMapping +2 (the P2 easy-miss, named) · count-pins lockstep in the SAME change · register 174→175 + new REG category 52→53 + watermark +1 at ratification. §8 = the G2 sizing statement (in/out lists; cascade ⇒ flag-before-dispatch).

**v25 BANKED** (`context/handoff/2026-07-08_PM-mission-control_v25_orchestrator_session_prompt.md`): carries the ratification intake → the DUR WU (authored ONLY against the ratified text — G1) → iteration 5b (the ratified bench protocol: one events-DB wipe, custody preserved → re-adopt ×2 under the new flow → RESTART → the SAME ids resolve → the Hue re-announce takes the RE-LINK arm, pin-2 silicon-proven → view = exactly the live entities → the KEYb piggyback check) → THE ACCEPTANCE RUN → the 72 h soak → seed-custody → explainability → v26 before depth.

**Nick, in order:** (1) **docs commit — stages exactly 1 new** (the AMD proposal), msg `_scratch/2026-07-08_docs_dur-amd-proposal_commit-msg.txt` → push. (2) **hivemind beat-6 commit — stages exactly 3** (2 M: pm-handoff · PROJECT_SNAPSHOT; 1 new: the v25 prompt), msg `_scratch/2026-07-08_hivemind_v24-beat-6_commit-msg.txt` → push. (3) **READ THE AMD → RULE IT** ("ratify as recommended" or per-letter overrides — R-A..R-E; your edits fold verbatim before ratification). (4) **LAUNCH v25** (it intakes the ratification, runs the mechanics, authors the DUR WU on your word). (5) Standing: frontend.yml `199324e` confirm-once · security glance · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2.

**v24 arc, closed (6 beats · 2 dispatches · 2 WUs landed CI-green · 1 bench iteration · 2 rulings recorded verbatim):** the 0x11 decode (triple-source) → M9.4-KEY (one constant) → **the join arc CLOSED on silicon (iteration 5a: the SNZB joins, keys, adopts, reports, STAYS)** → the §B honesty finding self-caught → M9.4-KEYb (the instrument honest to DEBUG granularity; the §51 precondition MET) → DUR ruled (i) with Nick's rationale preserved → the AMD authored ratification-pending. The moat is proven, adopted, reporting, honest — durability is ruled, governed, and one ratification from its WU.

## 2026-07-08 (v24 beat 5 — **M9.4-KEYb DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects)** · Nick's `check` GREEN 155 PRE-COMMIT (gate DISCHARGED) · **CI GREEN on `8800424` — the M9.4-KEY gate of record CLOSED** · **the §51 honesty-grep precondition is MET: the instrument is honest** · next: THE DUR AMD)

**The intake (4 files, all integration-zigbee, all insertions — the instruction's exact declared set; hub layer-2 seam reads at the shipped bytes):** §1 `progress()` = EXACTLY the ruled set {0x06, 0x07, 0x0C} (`:1165-1169` — no widening); §2 the middle arm exact (between the INFO and WARN arms `:282-286`; the ONE new DEBUG token `zigbee.key_establishment_progress`; return-after); `established()` BYTE-UNTOUCHED ({0x01,0x03,0x34,0x65}); both frozen tokens untouched; THE PIN re-asserted on all four new tests. **Test honesty CONFIRMED at source** — the DEBUG-capture vacuous-VERIFY trap handled exactly per the instruction: logger level save/set/restore in try/finally (`:323-332`), PRESENCE-based `containsExactly` on the DEBUG line (`:336-341` — a filtered-out line fails loudly); the staged red failed ON the presence assertion (0x06 fell to WARN pre-arm — the right reason, not a capture artifact). Suite 417 + 14, zero failures; spotless clean; **Nick's full `./gradlew check` GREEN (155) pre-commit — the deferred gate is DISCHARGED; residual = core commit + push + CI glance.** INFO adjudications ×3 ALL ACCEPT (level-idiom per instruction · T-KB2 one-session delivery sound · T-KB4 dual raw+symbolic pinning = the id-family lesson applied). **A healthy TCLK exchange's INFO stream now shows exactly one `key_established` and zero `key_establishment_failed` — the §51 greps can honestly bind failure to mean failure.** Silicon confirmation piggybacks on the next fresh join.

**Environment (4 more phantom hits, host ruled):** the VM diffstat showed net-DELETIONS on the all-insertions change (truncated-tail, 2nd hit this arc — quarantined; host truth 187 insertions + Nick's green check); the read-lag variant hid the KEYb coder-lessons entry AND the MODULE_CONTEXT section from VM reads — both HOST-VERIFIED landed.

**Ledger:** **CI `8800424` GREEN (Nick: "all GREEN") — the M9.4-KEY gate of record CLOSED.** **Deletables CLOSED** — Nick ran the hivemind cleared-lock rm at session start (pre-beat-1); beats 1–4 carried a stale ×1 — corrected, owned. **frontend.yml on `199324e` = the ONE open glance** (path-filtered web-ui/**; no KEY-arc commit touches it, so no newer run exists — one confirm closes it). Standing: security glance · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2.

**Nick, in order:** (1) **core commit — `git add -A` stages exactly 4** (MODULE_CONTEXT · EzspCoordinatorProtocol · ZclIngestionUnit · ZigbeeKeyEstablishmentTest), msg `_scratch/2026-07-08_core_m94-keyb_commit-msg.txt` → push → CI glance. (2) **hivemind beat-5 commit — `git add -A` stages exactly 4 M** (pm-handoff · PROJECT_SNAPSHOT · coder-handoff · coder-lessons), msg `_scratch/2026-07-08_hivemind_v24-beat-5_commit-msg.txt` → push. (3) frontend.yml `199324e` confirm-once when convenient. No bench action this beat — the next physical act is iteration 5b, post-DUR.

**Hub next: THE DUR DESIGN/AMD ARTIFACT (G1)** — the record shapes (the A/B payload fork presented with evidence), the registries-are-projections invariant text, the boot-rebuild seam (Phase 3.1→Phase 6), orphan tombstoning + the bench DB's 3-orphan disposition, the mint arithmetic (71→73 / 41→43 / 53→55) — authored for Nick's ratification per the confirmation[] model → on ratification: the DUR coding WU → **iteration 5b** (same ids across restart; pin-2 silicon-proven; view = live entities; +the KEYb piggyback check: one `key_established`, zero failures on any fresh join) → **THE ACCEPTANCE RUN** (P4 hero → P5 §51 → P6 + PJ key OUT) → the 72 h soak → seed-custody → explainability → **v25 authored before the AMD depth if the beat count climbs** (5 beats + 2 dispatches now).

## 2026-07-08 (v24 beat 4 — **THE TWO RULINGS BEAT: DUR = (i) EVENT-SOURCE THE REGISTRIES (ruled, verbatim below, with two guardrails) · §B GO → M9.4-KEYb AUTHORED ISSUE-READY — DISPATCH AT WILL** · the completing survey: **BOUNDED CONFIRMED** (~2 event types hold; one payload fork rides the AMD) · beat-3 landed: bench `aa45edc` 1-exact · hivemind `2408995` 2-exact)

**THE DUR RULING (Nick, 2026-07-08 — PRESERVE VERBATIM; the DP-18-C-class record):** *"DUR — (i). Event-source the registries."* His four reasons: (1) *"The moat is the single event-sourced log. Imperative in-memory registries that aren't derived from it are the one place the 'everything is provably derived from the log' claim has a hole — and for a trust-in-the-log product, that hole is exactly what the north star says to close before freeze. (ii)'s snapshot doesn't close it; it bolts a second, drift-prone source of truth onto the moat — the precise asterisk we don't want to ship."* (2) *"It's a one-way door being frozen at V1. The event model is the hardest thing to change post-launch, and `confirmation[]` is the precedent: we amend the model for load-bearing moat pieces. Cheaper now than after freeze."* (3) *"The schedule pays for the correctness. The join arc closed weeks early; it's ~5–6 weeks to the go/no-go. So (i)'s cost over (ii) is essentially zero in schedule — which is the only thing (ii) was buying."* (4) *"(i) fixes four things root-cause: durable identity, the orphan rows, a real `device_removed` tombstone, and pin-2 across restart. (ii) fixes only the first and adds a new drift surface."* **TWO GUARDRAILS (binding on the hub):** (G1) *"Govern it like `confirmation[]`. Ratify the record shapes and the invariant that the device/entity registries are projections of the log (so it can never silently regress) in the design/AMD layer before the coding WU."* (G2) *"Bound it — watch the P1 sizing smell. Minimal surface: the event(s) carrying the full device + entity records, a boot projection rebuild, orphan tombstoning — not a general registry rearchitecture. If the completing survey shows materially more than ~2 event types or a cascade of amendments, flag it before you commit."* **And his sequencing insight, on the record:** *"automations load only at boot, and boot re-mints identity, so an entity-bound automation can never survive the very restart that loads it. Durable identity is a hard prerequisite for the hero loop, not just the soak's restart leg."*

**The completing survey (read-only, at `8800424`) — GUARDRAIL-2 VERDICT: BOUNDED CONFIRMED.** Two event types hold (device-registration + entity-registration class); `device_removed` ALREADY EXISTS as an event type with a codec/category mapping and ZERO production emitters — tombstoning gives it its first real semantics, no new type. Record shapes at source: `Device` = 14 components (identity wrappers + Strings + collections + Instant — payload-friendly); `Entity` = 12 components INCLUDING `List<CapabilityInstance>`, and `CapabilityInstance` is a DEEP structure (attribute/command schema maps + `ConfirmationPolicy`). **The one design fork the AMD must rule (JPMS-forced):** an event record in `com.homesynapse.event` can NEVER reference device-model types (the payload-residency rule — device requires event, so event→device is the AMD-52 cycle class). So: **(A) flattened-full-record payloads** (the all-String/leaf-value idiom over the whole Device+Entity forest incl. capabilities — pure log projection, heavy schema) vs **(B) identity-binding payloads** (deviceId/entityIds-by-endpoint/IEEE/integrationId/matchedProfileId/slugs — flat, light) **+ deterministic capability re-derivation at boot** through the SAME classification+installOverrides path adoption uses, sourced from the already-persisted `zigbee-devices.json` cache (survey-confirmed: it persists NodeDescriptor + endpoints + manufacturer/model + interviewStatus + matchedProfileId — everything but the minted IDs, which is exactly the gap). Hub lean: (B) — lighter, JPMS-clean, and the invariant wording then reads "registry IDENTITY is a pure projection of the log; characterization re-derives deterministically" — but (A) is the purer reading of G1's invariant; **the AMD presents both, Nick ratifies.** Also found: the adoption slice already has a device-restore seam shape (`:380` region puts into `devicesByIeee` from an existing `Device`) — the rebuild has a natural landing. Boot seam: registries install at Phase 3.1; integrations launch at Phase 6 — the rebuild sits between. Existing stored `device_adopted` events are untouched (codecs are per-type; no migration).

**§B → M9.4-KEYb AUTHORED ISSUE-READY** (`context/instructions/2026-07-08_M9.4-KEYb_key-establishment-progress-reclassification_coding-instruction.md`): `KeyEstablishment.progress()` = exactly {0x06,0x07,0x0C} (the ruled set) → the middle DEBUG arm `zigbee.key_establishment_progress` in `handleKeyEstablishment`; `established()` + `statusName()` + both frozen tokens BYTE-UNTOUCHED; THE PIN re-asserted in every new test; T-KB1 (the healthy 0x06→0x34 sequence → exactly one INFO, zero WARN) / T-KB2 (each progress byte → DEBUG) / T-KB3 (0x11 + 0x33 still WARN — the regression pin) / T-KB4 (the truth table); DEBUG-capture watch-out named (vacuous-VERIFY class); 4 files, zero mints/surface; G-KB1–5. Silicon confirmation piggybacks on the next fresh join — no dedicated iteration.

**Nick, in order:** (1) **hivemind beat-4 commit — `git add -A` stages exactly 4** (3 M: pm-handoff · PROJECT_SNAPSHOT · decision-rationale-index; 1 new: the KEYb instruction), msg `_scratch/2026-07-08_hivemind_v24-beat-4_commit-msg.txt` → push. (2) **DISPATCH M9.4-KEYb** (CC lane) — paste: *"Read nexsys-hivemind/context/instructions/2026-07-08_M9.4-KEYb_key-establishment-progress-reclassification_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (3) **CI glance `8800424`** if not yet confirmed (the gate of record). (4) Standing: frontend.yml `199324e` confirm · security glance · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2.

**Hub next:** **the DUR design/AMD artifact (G1 — the governance step BEFORE any coding WU):** record shapes (the A/B fork presented with evidence), the registries-are-projections invariant text, the boot-rebuild seam, orphan tombstoning + the bench DB's 3-orphan disposition, the mint arithmetic (71→73 / 41→43 / 53→55) — authored for Nick's ratification (the confirmation[]/DP-1 model: freeze as governance, never ride in on implementation churn) → on ratification: the DUR coding WU → **iteration 5b** (restart → SAME ids resolve → re-announce takes the re-link arm, `onRejoin` fires — pin-2 silicon-proven → the view shows exactly the live entities) → KEYb intake/audit on its delivery (parallel) → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero → P5 §51 → P6 + PJ key OUT) → the 72 h soak (NQ-6 doubles as the DUR proof) → seed-custody → explainability return → v25 before depth.

## 2026-07-08 (v24 beat 3 — **ITERATION 5a: THE JOIN ARC CLOSES FOR REAL** (core `8800424`) — the SNZB joins → completes its TCLK update (`key_established: TC_REQUESTER_VERIFY_KEY_SUCCESS`) → adopts → configures reporting → **STAYS ≥5 min hands-off (NO `device_left`)** · the one-constant policy-ID fix silicon-confirmed · **the moat's full pipeline is silicon-proven end-to-end within a lifetime** · TWO items to the acceptance run + **the DUR ruling SHARPENED by the grounding survey**)

**The evidence (log 21:04–21:07, core `8800424`):** resume exact (`network_resumed ch20/0x9b65` + `ncp_configured` + `tc_joins_enabled` + `permit_join_opened 254s`). SNZB-03P (`0xF044D3FFFE9C78D7`), ONE ~5 s hold: `device_join UNSECURED_JOIN/USE_PRECONFIGURED_KEY` → `child_join SLEEPY_END_DEVICE` → `device_announce` (21:07:23) → **the TCLK exchange flipped: `TC_RESPONDED_TO_KEY_REQUEST` (0x06, the TC SENT the key) → `key_established: TC_REQUESTER_VERIFY_KEY_SUCCESS` (0x34, the device VERIFIED it)** — vs iteration 4's `TC_REJECTED_APP_KEY_REQUEST` (0x11) → leave. → `device_proposed COMPLETE` → `proposal_accepted source=config` → `device_adopted deviceId=01KWZM7VTZ… entities=1` → `reporting_configured clusters=3 verified=2 degraded=1` → **≥5 min hands-off: NO `device_left`. THE SNZB STAYED.** The `POLICY_TC_KEY_REQUEST 0x09→0x05` correction is the whole fix, confirmed on silicon. **The moat's full pipeline is silicon-proven end-to-end within a process lifetime** (formation/resume/custody · NCP config · TC-join · **TCLK key establishment [NEW]** · interview→proposal · config-adoption · reporting+posture · **the device STAYS [NEW]**).

**NEW §B finding (honesty-in-the-instrument — hub-owned):** `TC_RESPONDED_TO_KEY_REQUEST` (0x06) is a BENIGN PROGRESS step, but the binary `established()`/else classifier logs it as `key_establishment_failed` (WARN) — a false-failure verdict on a healthy exchange, 0.26 s before the real success. The M9.4-KEY binary split was designed off iteration-4's failure-only evidence; silicon revealed the in-flight progress callbacks {0x06, 0x07, 0x0C} it didn't anticipate. **M9.4-KEYb queued** (reclassify progress out of the failure bucket; a healthy exchange then logs exactly one `key_established`, zero `key_establishment_failed`) — land it BEFORE the acceptance run's §51 honesty greps rely on `key_establishment_failed` meaning an actual failure.

**DUR ruling — SHARPENED by the grounding survey (this is why we verify before authoring):** the registries are **imperative in-memory, NOT event-sourced** — `DeviceAdoptedEvent` carries ONLY `entityId` (no device record, no capabilities, no bindings); there is NO `entity_registered`/`device_registered` event type; `adopt()` populates `deviceRegistry.createDevice` + `entityRegistry.createEntity` + the IEEE→id/binding maps imperatively from live interview data that is never persisted as an event. So **option (i) "event-replay rehydration at boot" means first making the registries PROJECTIONS of the log — minting proper registration events (device_registered + entity_registered carrying the full records) + a boot rebuild** — the architecturally-correct, event-sourced-native answer (and the root fix: the registries SHOULD be projections), but a **governance-class WU** (new event types → schema → codec → the 71/41/53 mint pins → the adoption path emits them). **Option (ii) persisted registry snapshot** avoids the event-schema change but bolts on a PARALLEL truth artifact (drift-prone, against the event-sourced grain) — faster to the soak. **(iii) cache-side re-link** stays insufficient alone. **Hub REC: (i)** — the registries-as-projections is the design, this is a one-way-door foundation worth getting right, and the window (~Aug 16) has room for a bounded governance WU; **(ii) if velocity-to-soak outranks architecture-purity here** (your call — it's a genuine fork, both now grounded). DUR gates P4 (automations bind entity IDs) + the soak's NQ-6 + pin-2 (still unexercised).

**Nick, in order:** (1) **bench commit — stages exactly 1 M** (the record's iteration-5a block), msg `_scratch/2026-07-08_bench_iteration-5a_commit-msg.txt` → push. (2) **hivemind beat-3 commit — stages exactly 2 M** (pm-handoff · PROJECT_SNAPSHOT), msg `_scratch/2026-07-08_hivemind_v24-beat-3_commit-msg.txt` → push. (3) **THE DUR RULING** — (i) event-source the registries [hub REC] / (ii) snapshot / (iii) cache-side — one letter and I run the completing survey (event-payload shape for the mints + boot-order seam + orphan disposition) → author. (4) **The §B micro-WU** — "go" and I author M9.4-KEYb (fast, parallel to DUR — independent module). (5) **CI glance on `8800424`** (the gate of record; you pushed it before the bench). (6) Standing: frontend.yml `199324e` confirm · security glance · deletable ×1 · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2.

**Hub next:** on the DUR ruling → the DUR WU authored (grounding-survey-first) → **iteration 5b** (restart → the SAME deviceIds/entityIds resolve → a re-announce takes the re-link arm, `onRejoin` fires — pin-2 finally silicon-proven → the view shows exactly the live entities) → **THE ACCEPTANCE RUN** (P3.11–12 → P4 the bench-hero automation [the hub authors the config] + the restart RESUME leg → P5 §51 live [CONFIRMED via authoritative reports · identify honest-UNCONFIRMED · superseded verdict-free · latencies · IAS ×2 · Q10 · FE-7] → P6 reopen + REMOVE the PJ key) → the 72 h soak (W2-VAL at entry; NQ-6 doubles as the DUR proof) → seed-custody JIT (SD-5 gate rides) → the explainability-lane return → v25 before depth. §B rides in before P5.

## 2026-07-07 (v24 beat 2 — **M9.4-KEY DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects)** · Nick's `./gradlew check` GREEN 155 PRE-COMMIT (deferred gate DISCHARGED) · **ITERATION 5a IS THE LIVE GATE — unit-green ≠ done-when: the SNZB must JOIN AND STAY**)

**The intake (3 files, all integration-zigbee — the instruction's exact declared set; hub layer-2 direct seam reads at the `199324e` working tree, the proportionate audit for a one-constant WU):** §1 CONFIRMED at source — `POLICY_TC_KEY_REQUEST = 0x05` (`:176`); `enablePreconfiguredKeyJoins()` writes it with `DECISION_ALLOW_TC_KEY_REQUESTS` (0x51) — exactly the bellows `EZSP_POLICIES_SHARED` reference (0x05←0x51), the trust-center-policy + transient-key exchanges byte-intact. §2 CONFIRMED bellows-verbatim — the 17 folded `KEY_STATUS_*` names (0x00/0x02/0x06–0x13/0x1E) match `types/named.py` exactly incl. `0x11 = TC_REJECTED_APP_KEY_REQUEST`; `statusName()` hex fallback RETAINED; **`established()` byte-untouched (success set exactly {0x01,0x03,0x34,0x65} — a rejected/left device can never render `key_established`; never-false-ALIVE holds in the instrument itself)**. Tests: T-K1 pins 0x11 BY VALUE + all 17 by name + unmapped→hex; T-K2 the WARN verbatim `status=TC_REJECTED_APP_KEY_REQUEST` + `assertPin` (the §B log-only pin holds); T-K4 established() byte-untouched — 7 @Test (4 §B + 3 new); 413 + 14 GREEN. **Zero collateral CONFIRMED (VM porcelain + Nick's host status AGREE: exactly 3 files):** module-info · build.gradle.kts · toml · schema · the 71/41/53 count-pin tests · the TCJ test all byte-untouched (T-K3 — the TCJ test's symbolic binding at `:140-141` followed the constant with zero edits); the frozen M9.3 trio untouched. **Nick's `./gradlew check` GREEN (155) pre-commit — the deferred gate is DISCHARGED; residual = core commit + push + CI glance (the gate of record; bench ONLY after green).**

**INFO adjudications (all ACCEPT):** `KEY_STATUS_NONE` (0x00) renders `"NONE"` sans-prefix — internally consistent with every other `statusName()` arm; the 0x00 sentinel never appears in a real establishment log (the Coder's comment records the bellows-name quirk). T-K1 covers all 17 names (floor was "representative") — more coverage, the 16 non-measured stay symbolic so silicon corrections remain one-constant. Targeted Gradle ran in-session per the CC allow-list (a first for this lane); full `check` correctly deferred → Nick's GREEN.

**Environment (paid this beat):** the mount served the §2 truncated-tail phantom on the just-committed spine (`becc43a`) — pm-handoff + PROJECT_SNAPSHOT showed porcelain-M with a pure-deletion-at-EOF diff (the committed tail cut in the VM view; host intact, git-object-verified) AND the read-lag variant HID `coder-lessons.md` from porcelain while the host file carried the landed KEY lesson (lag in both directions, one beat). Host tools ruled throughout; no VM-view commit. **coder-lessons VERIFIED landed on host** (the id-family verification lesson — verify id-family constants by position in the version-pinned header AND against the reference stack's write set).

**Nick, in order:** (1) **core commit — `git add -A` stages exactly 3** (MODULE_CONTEXT · EzspCoordinatorProtocol · ZigbeeKeyEstablishmentTest), msg `_scratch/2026-07-07_core_m94-key_commit-msg.txt` → push → **CI glance (ci.yml + install-smoke — the gate of record; bench ONLY after green)**. (2) **hivemind beat-2 commit — `git add -A` stages exactly 4 M** (pm-handoff · PROJECT_SNAPSHOT · coder-handoff · coder-lessons), msg `_scratch/2026-07-07_hivemind_v24-beat-2_commit-msg.txt` → push. (3) **BENCH ITERATION 5a on the CI-green commit** — the in-chat paste-block: NO wipe, config UNCHANGED, PJ key stays; Pi pull + installDist + relaunch (resume ch20/`0x9b65`); **SNZB ONE ~5 s hold then HANDS OFF ≥5 min**; done-when: `key_established` (success name) + ZERO `key_establishment_failed` + ZERO `device_left` = THE JOIN ARC CLOSES FOR REAL. (4) **THE DUR RULING** whenever ready ((i) event-replay rehydration [REC] / (ii) snapshot / (iii) cache-side) — it gates 5b + the acceptance run, NOT 5a. (5) Standing: frontend.yml `199324e` confirm-once · security glance · deletable ×1 · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2.

**Hub next:** iteration-5a intake — the SNZB STAYS ⇒ the join arc closes for real → the DUR WU authors on the ruling → iteration 5b (identities survive restart; pin-2 silicon-proven; the view shows exactly the live entities) → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero [the hub authors the automation config at that beat] → P5 §51 → P6 + PJ key OUT) → the 72 h soak (W2-VAL charter at entry; NQ-6 doubles as the DUR proof) → seed-custody JIT (SD-5 HARD GATE rides) → the explainability-lane return → v25 authored before depth. A 5a `status=TC_REJECTED_APP_KEY_REQUEST` recurrence ⇒ the 0x05 write isn't reaching the NCP (a `getPolicy` read-back is the next rung, ruled WITH Nick — instrument-first, one instrument at a time).

## 2026-07-07 (v24 beat 1 — LAUNCH FULL-PASS (all 11 — **Check 9 PASS, mirrors already synced**) · **THE 0x11 DECODE VERIFIED TRIPLE-SOURCE: the policy-ID mis-map** (our "TC key request" write lands on policy 0x09 = TC_REJOINS_USING_WELL_KNOWN_KEY_POLICY; the REAL EZSP_TC_KEY_REQUEST_POLICY = **0x05** was never written → firmware-default deny → `0x11 = TC_REJECTED_APP_KEY_REQUEST` ×3 → BDB leave) · **M9.4-KEY AUTHORED ISSUE-READY — DISPATCH ON NICK'S WORD** · the DUR options staged for his ruling)

**Launch:** §11 CLEAN (lock-free porcelain ×5 all clean; both spine frontmatters read pre-write; beat-7 `1240ec6` verified landed — 9 paths exact, parent `91f8cda`; the uploaded v24 prompt sha256-identical to the repo copy). Preflight **PASS all 11** — notable: **Check 9 PASS** (both mirror trees byte-identical, sha256 over all 15 files — Nick ran the sync before launch; the expected beat-7 STALE never presented). v23 prompt archived (R, this commit). Deletables re-derived from source: **the `skills-initiative` husk is ALREADY GONE** (find across ClaudeFolder = a design-doc filename hit only) — the ledger corrects ×2 → **×1** (the hivemind `index.lock.cleared-by-cowork-2026-07-04` rm remains). Required reads done IN FULL (pm-handoff beats 1–7 + Open Risks [§4b CLEAR — zero open gates] · coder-handoff NEXT-WU · bench record iterations 1–4 · runbook field-validated + AMENDED step-11 · the playbook · env-model · truth-hierarchy · the cold-boot trio). 174/52 re-derived from the register's own regeneration line at AMD-98.

**THE DECODE (was MEDIUM, now HIGH — verify-before-anything-moves honored):** `0x11` = **`TC_REJECTED_APP_KEY_REQUEST`** — bellows `types/named.py` VERBATIM (the pre-decode candidate CONFIRMED) + the classic `ezsp-enum.h` + **GSDK 4.4.5 `ezsp-enum.h` (= EmberZNet 7.4.5, THE frozen stack)**. The v13 policy map: **0x05 = `EZSP_TC_KEY_REQUEST_POLICY`** (TCLK-request handling) · **0x09 = `EZSP_TC_REJOINS_USING_WELL_KNOWN_KEY_POLICY`** (a different policy). At `199324e`: `POLICY_TC_KEY_REQUEST = 0x09` (`:165`), written with decision `0x51` at the enablement (`:937`) — **the decision VALUE is exactly what bellows sets for the REAL policy** (`EZSP_POLICIES_SHARED`: `TC_KEY_REQUEST_POLICY → ALLOW_TC_KEY_REQUESTS_AND_SEND_CURRENT_KEY`; bellows does NOT write policy 0x09 at all) — so the fix is the pure one-constant correction **0x09 → 0x05**, and the TCJ javadoc's own "BENCH-VERIFY whether the frozen v13 stack needs it" clause resolves: needed, at the right id. Fits every silicon fact: 3 rejects at ~5 s → leave +18.8 s (SNZB, strict BDB) · the Hue stays (Signify tolerance, playbook §6) · ZHA keeps both (it writes 0x05). Ladder rung 3, reference-grounded; the SD-5 fallback untouched (last resort, Nick's lever).

**M9.4-KEY** (`context/instructions/2026-07-07_M9.4-KEY_tc-key-request-policy-correction_coding-instruction.md`): §1 the one-constant correction + verified-source javadocs (incl. resolving the DECISION_ALLOW_TC_KEY_REQUESTS "droppable" clause — silicon proved it load-bearing, mis-aimed); §2 the vocabulary fold (0x00/0x02/0x06–0x13/0x1E bellows-verbatim → `statusName()` self-decoding logs; `established()` success set BYTE-UNTOUCHED {0x01,0x03,0x34,0x65}; the log TOKENS frozen — runbook greps unaffected); exactly 3 files, all integration-zigbee; the TCJ test follows SYMBOLICALLY (binds the production constant at `:140-141` — G-KEY3 sweeps for raw-byte fixtures); zero mints/schema/module-info/public-surface; STOP gates G-KEY1–5; tests-before-impl T-K1–T-K4; **the instruction's own gate = ITERATION 5a: SNZB ONE ~5 s hold → HANDS OFF ≥5 min → `key_established` + ZERO `device_left` = THE JOIN ARC CLOSES FOR REAL.**

**DUR (staged for Nick's ruling — grounding survey run at source `199324e`):** the 7-arg `HomeSynapseCore` ctor delegates with `new InMemoryDeviceRegistry()` (`:347`) BUT the canonical 8-arg form (M9.4b R4) already ACCEPTS the registry — "the composition root owns device-registry identity"; `entityRegistry` is ALWAYS fresh (`:510`, no injection seam). `adopt()` mints ULIDs → `deviceRegistry.createDevice` → registers classified entities → publishes `device_adopted` (SOLE production emitter repo-wide; `DeviceAdoptedEvent` persisted + codec-registered — replayable). `RestartHonestyIT` restarts the INTEGRATION only (core + registries survive it) — process-restart identity durability is untested by construction, exactly the bench-observed gap. **Options for the ruling:** **(i) event-replay rehydration at boot [hub REC]** — lifecycle replays device/entity registration events into both registries before Phase 6 launches integrations; event-sourced-native (no second truth artifact), integration-agnostic; scope = the boot-order seam + `device_removed` tombstones + idempotency + the orphan-row disposition (3 rows / 2 live devices — clean-or-rule) + the pin-2 re-link leg becomes testable across restarts; **(ii)** persisted registry snapshot — faster boot, but a PARALLEL truth artifact that can drift from the log (the class this architecture exists to avoid); **(iii)** cache-side re-link only — zigbee-local, does NOT restore core deviceId/entityId identity (not viable alone). Sits BEFORE P4 (automations bind entity IDs) and gates the soak's NQ-6 leg. The DUR WU authors ONLY on the ruling, after the completing grounding survey (event-payload completeness + view reconciliation) — never under bench pressure.

**Nick, in order:** (1) **hivemind beat-1 commit — pre-stage porcelain shows 5 lines (2 M + 2 ?? + 1 D); `git add -A` stages exactly 4 entries** (2 M: pm-handoff · PROJECT_SNAPSHOT; 1 new: the M9.4-KEY instruction; 1 R: the v23 prompt → archive/), msg `_scratch/2026-07-07_hivemind_v24-beat-1_commit-msg.txt` → push. (2) **THE WORD on M9.4-KEY** — dispatch paste: *"Read nexsys-hivemind/context/instructions/2026-07-07_M9.4-KEY_tc-key-request-policy-correction_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (3) **THE DUR RULING** — (i) event-replay rehydration [REC] · (ii) snapshot · (iii) cache-side only — plus WHERE it sits (REC: before P4). One letter dispatches the sequence. (4) **frontend.yml on `199324e`** — you reported green; one confirm closes the ledger row. (5) Standing: security glance · deletable ×1 (the hivemind cleared-lock rm) · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2 watch.

**Hub next:** KEY dispatched on the word → two-layer audit → gates → commit/push/CI → **ITERATION 5a** (the SNZB joins AND STAYS) → the DUR WU on the ruling → **ITERATION 5b** (identities survive restart; pin-2 silicon-proven; the view shows exactly the live entities) → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero [the hub authors the automation config at that beat] → P5 §51 → P6 + PJ key OUT) → the 72 h soak (W2-VAL charter at entry; NQ-6 doubles as the DUR proof) → seed-custody JIT (SD-5 HARD GATE rides) → the explainability-lane return → v25 authored before depth.

## 2026-07-07 (v23 beat 7 — THE CLOSE: step-11 formally NON-VACUOUS · Dependabot CLOSED · spine ROTATED · THE SKILLS FOLD + the bench-troubleshooting playbook · **v24 AUTHORED — launch on Nick's word**)

**Closures:** step-11 CONFIRMED non-vacuous (positive evidence ×2: Hue `clusters=3 verified=3 degraded=0` · SNZB `clusters=3 verified=2 degraded=1`; failure sweep CLEAN — and NOTE, banked as R3-row data: the SNZB's degraded fact raised NO `confirmation_downgraded` WARN, i.e. no covering capability on the entity; identify it at the next currency pass, not tonight). **Dependabot CLOSED** — Nick patched the dashboard devDeps (`npm audit fix --force`, vitest 4.1.10; `npm run verify` fully green incl. bundle + contract checks; core `199324e`, web-ui only — frontend.yml triggered on the push, Nick reports GitHub green; the remote's 5-vuln nag in the push output predates the scan). Core HEAD for v24 = `199324e`.

**Hygiene:** pm-handoff v21–v17 beats → `archive/pm-handoff-beats-rotated-2026-07-07.md`; PROJECT_SNAPSHOT v21–v18 blocks → `context/status/archive/PROJECT_SNAPSHOT-beats-rotated-2026-07-07.md`; both frontmatter Prior-chains compacted to archive pointers (185K/73K chars → the per-session read tax cut). The coder's RPT cross-agent note archived SERVED (runbook action executed beat 5; R2 ruled polish-row beat 5). `_scratch` landed-message files swept to `_scratch/served/`.

**THE SKILLS FOLD (Check 9 goes STALE until Nick's mirror sync — deliberate):** NEW `context/process/bench-troubleshooting-playbook.md` — the M9.4 bench arc's integration-troubleshooting doctrine distilled (instrument-first; the mechanism-without-driver sweep THROUGH the gate; vacuous-VERIFY pairing; timeline-consistent closures + the discriminator-experiment pattern; operator-action logging; the escalation ladder; the device behavior bank) — **the seed of the future dedicated troubleshooting/integrations skill**, pointer-not-copy from both skill mastheads. PM SKILL.md + coder SKILL.md masthead pointers added (each ~4 lines, durable-discipline form).

**Nick, in order:** (1) **hivemind beat-7 commit — stages exactly 9** (5 M: pm-handoff · PROJECT_SNAPSHOT · cross-agent-notes · project-manager/SKILL.md · coder/SKILL.md; 4 new: the 2 rotation archives · the playbook · the v24 prompt), msg `_scratch/2026-07-07_hivemind_v23-beat-7_commit-msg.txt` → push. (2) **MIRROR SYNC ×2 (PM + coder — Check 9 clears).** (3) **LAUNCH v24** (`context/handoff/2026-07-07_PM-mission-control_v24_orchestrator_session_prompt.md`). (4) Standing (post-Dependabot): security glance · deletables ×2 (hivemind cleared-lock rm · `skills-initiative` rmdir) · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2 watch.

**Hub next (v24's charge):** the 0x11 decode (v13 vocabulary, bellows reference) → **M9.4-KEY** authored + dispatched on the word → **the DUR ruling** (options staged; REC event-replay rehydration at boot) + the DUR WU → **iteration 5: the SNZB joins AND STAYS + pin-2 re-link exercisable** → **THE ACCEPTANCE RUN** (P4 hero → P5 §51 → P6 + PJ key OUT) → the 72 h soak (W2-VAL at entry) → seed-custody (SD-5 HARD GATE rides) → explainability return.

## 2026-07-07 (v23 beat 6 — **ITERATION 4 (core `7c0561d`, CI GREEN): THE REPORTING BINDING IS SILICON-PROVEN** (drive ×2; Hue 3/3 VERIFIED; **the Hue entity row MATERIALIZED on its first configured report** — the iteration-3 gap CLOSED) · **§B ADJUDICATES OBS-2: DEVICE-INITIATED, MEASURED** (`key_establishment_failed status=0x11` ×3 → leave +18.8 s — the TCLK-exchange-failure class confirmed) · **ONE NEW ARCHITECTURE GAP: identity durability across restart** (ESCALATED — Nick's ruling) · two WUs stand between here and the acceptance run)

**The evidence (log 17:32–17:33, detail in the bench record):** RPT's §1+§2+anti-vacuous INFO all proven live — Hue `clusters=3 verified=3 degraded=0` → first report → row `01KWZ7YKTM…` in the view; SNZB `clusters=3 verified=2 degraded=1` (degraded-fact identity pends the corrected grep — Terminal-2's sweep ran on an EMPTY `$LOG` and is VOID). §B: three `key_establishment_failed status=0x11` at ~5 s cadence, then `device_left` +18.8 s post-join, hands off — **OBS-2 CLOSED as device-initiated; it-2/it-3 leaves re-attributed to the class.** 0x11 is UNNAMED in our map (the 0x06–0x11 TC key-request range is unmapped; bellows-derived vocabulary covers 0x01–0x05/0x32–0x34/0x64–0x65); legacy-enum candidate `TC_REJECTED_APP_KEY_REQUEST` [MEDIUM — VERIFY against the v13 vocabulary first]. Fix class = the TCJ policy family (0x09=0x51 flagged "droppable if unneeded" at TCJ authoring — silicon shows it LOAD-BEARING on the TCLK-update leg); hashed-TCLK interaction still candidate; SD-5 fallback = last resort. **The durability gap, source-grounded:** `HomeSynapseCore:347` = `new InMemoryDeviceRegistry()` every boot, zero rehydration in lifecycle — the Hue re-proposed FRESH and re-adopted under NEW ids; the view accumulates orphans (3 rows, 2 live devices); **pin-2 re-link is unreachable across restarts** (the hub's iteration-4 onRejoin recipe premise was WRONG — owned; the ADP instruction's restart⇒re-propose text was right); entity-ID-bound automations break on restart — **gates P4 stability and the soak's NQ-6 leg.**

**Nick, in order:** (1) **the corrected greps** (fresh ssh session killed `$LOG`): `LOG=~/hs-bench/bench-2026-07-07-173213.log` then the step-11 pair + the key/left tail — paste (closes the SNZB degraded-fact identity + the formal non-vacuous step-11 record). (2) **hivemind beat-6 commit — stages exactly 3 M** (pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog), msg `_scratch/2026-07-07_hivemind_v23-beat-6_commit-msg.txt` → push. (3) **bench commit — stages exactly 1 M** (the record's iteration-4 block), msg `_scratch/2026-07-07_bench_iteration-4_commit-msg.txt` → push. (4) **TWO RULINGS (tomorrow is fine):** (a) **TCLK fix** — the hub decodes 0x11 against the v13 vocabulary + verifies the policy semantics at the bellows reference, then presents the one-constant candidate(s); your word dispatches the micro-WU (M9.4-KEY). (b) **Durability routing** — options: (i) event-replay rehydration at boot (event-sourced-native; hub REC), (ii) persisted registry snapshot, (iii) cache-side re-link only (doesn't fix identity stability — not recommended alone); your call on shape + where it sits (it must land BEFORE soak entry; my read: before P4 too — automations bind entity IDs). (5) **No more device theater tonight** — the SNZB will leave on every join until the TCLK fix; the Hue is up, configured, reporting. (6) Standing: **Dependabot 1 critical — the push output nagged AGAIN (1 critical / 1 high / 3 moderate), day 10** · security glance · deletables ×2 · Node-20.

**Hub next:** the 0x11 decode + policy verification (bellows reference, read-only) → M9.4-KEY authored on the evidence → the durability escalation formalized (options + impact table) for Nick's ruling → on both landing: iteration 5 (SNZB joins AND STAYS = the join arc's last leg; pin-2 re-link finally exercisable post-DUR) → THE ACCEPTANCE RUN (P4 hero → P5 §51 → P6 + PJ key OUT) → soak → seed-custody (SD-5 gate rides) → explainability return. **v24 authors NEXT BEAT regardless (rotation directive: this hub is 6 beats deep + 2 dispatches + a merge — bank before depth).**

## 2026-07-07 (v23 beat 5 — **M9.4-RPT DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects; 10/10 checks)** · gates GREEN PRE-COMMIT (62 + 155 — deferred gate DISCHARGED) · rulings R1/R2/R3 + the C5 tension adjudicated · runbook step-11 AMENDED non-vacuous · **ITERATION 4 IS THE LIVE GATE**)

**The intake (9 paths: 6 M + 3 new, all integration-zigbee + testFixtures):** layer-1 = one focused compliance agent (C1–C10, all PASS — single-agent deviation from the 2-agent norm recorded deliberately: the coder's own 4-lens fleet + Nick's green gates + CI + heavier hub layer-2 carried the balance at session depth) + hub direct seam reads (`adoptIfAccepted:634-652` both arms · `driveReporting:664-683` w/ the dedicated `reporting_drive_failed` backstop that never leaks into `interview_failed` · `applyPostureRouting:692-712` update-if-differs). Verified at source: **the frozen M9.3 trio blob-identical ×3** (rev-parse = hash-object); the `zdoExchange` HOIST semantics-identical against baseline (token-level compare; the interview-specific timeout WARN retained at the delegating inner); **§B log-only pin test-enforced** (`assertPin`: zero publishes/devices/interviews/registry changes, frame-id-scoped); the anti-vacuous INFO unconditional incl. all-degraded (T4 asserts it by name); module-info/build.gradle.kts/toml/schema/CoordinatorProtocol all hash-untouched; zero mints (71/41/53 counting tests untouched); FakeNcp delta = exactly the declared getEui64 built-in; Clock-clean, default-gated, verbatim log bindings; agent refutation banked: **all three runbook WARN vocabularies EXIST at source** (`:155/:204-216/:237`) — [REVIEW]-1's premise was half-wrong, only the degrade rungs are quiet, and they now speak through `confirmation_downgraded` + the INFO counts.

**Rulings:** **R1** (step-11 vocabulary) = ACCEPT; **runbook step-11 AMENDED** (positive-evidence grep FIRST — ≥1 `reporting_configured` per device; failure sweep gains `confirmation_downgraded|reporting_drive_failed`; REPORTING-CLEAN without the positive line = VACUOUS) — the coder's cross-agent action SERVED. **R2** (posture-DISABLED renders no immediate verdict — the SD-3 fence reads profile characterization, never installed policy) = **NAMED polish row: extend the SD-3 adapter fence to consult installed policy — rides the next zigbee-touching WU**; ledger-side never-false-CONFIRMED unaffected (DISABLED ⇒ never tracked ⇒ never CONFIRMED); grazes Nick's AMD-97-literal pin ("never-tracked and honestly-verdicted are different promises") so **his ack is requested, non-blocking** — not tonight's path (Hue/SNZB expected VERIFIED). **R3** (recurring WARN on designed postures) = ship as-is tonight (2 devices, trivial noise); **re-rate row queued** — decide with iteration-4 data on which postures actually fire. **C5 tension** (BEST_EFFORT-class postures keep tracking, WARN-only) = ADJUDICATED ACCEPT: the ratified vocabulary is explicit at baseline (`tune:141-142` — best-effort-ness is measured timeout + recorded posture, NOT a mode) and CONFIRMED structurally requires a real arriving report; the instruction's own ratified-mapping clause governs over its stricter north-star sentence — hub authoring imprecision owned.

**Nick, in order:** (1) **core commit — `git add -A` stages exactly 9** (6 M + 3 new), msg `_scratch/2026-07-07_core_m94-rpt_commit-msg.txt` → push → **CI glance (the gate of record — bench ONLY after green)**. (2) **hivemind beat-5 commit — stages exactly 6 M** (coder-handoff · cross-agent-notes · coder-lessons · pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog), msg `_scratch/2026-07-07_hivemind_v23-beat-5_commit-msg.txt` → push. (3) **bench commit — stages exactly 1 M** (the runbook amendment), msg `_scratch/2026-07-07_bench_runbook-step-11_commit-msg.txt` → push. (4) **BENCH ITERATION 4** — the chat instructions (Phase C): NO wipe, config unchanged; Pi pull + installDist + relaunch; **Hue = ONE wall power-cycle**; **SNZB = ONE ~5 s hold then HANDS OFF 60+ s**; the amended step-11 greps; done-when: `reporting_configured` ×2 + TWO entity rows + non-vacuous step-11 + pin-2 re-link + the OBS-2 discriminator outcome (§B's `key_established`/`key_establishment_failed` adjudicates a leave). (5) Standing: **Dependabot 1 critical (day 10)** · security glance · deletables ×2 · Node-20.

**Hub next:** iteration-4 intake at the ⏺ RECORDs → OBS-2 ruled on the discriminator + §B measurement → THE ACCEPTANCE RUN (P3.12 ULIDs → P4 hero [automation seeding staged at that beat] → P5 §51 → P6 + PJ key OUT) → 72 h soak (W2-VAL charter at entry) → seed-custody JIT (SD-5 HARD GATE rides) → explainability return → **v24 authored before depth — this hub is at 5 beats + 2 dispatches + a parallel-writer merge; approaching rotation.**

## 2026-07-07 (v23 beat 4 — **M9.4-RPT AUTHORED ISSUE-READY** (`context/instructions/2026-07-07_M9.4-RPT_reporting-binding-and-posture-routing_coding-instruction.md`) · §B (0x009B) rides SEVERABLE under the hub's ruling-at-authoring + Nick's standing veto · **DISPATCH ON HIS WORD** · beats 2–3 landed `59bbe7a` (4 exact) · bench iteration-3 record landed `624a935` (1 exact, the v2 message — the contested-OBS-2 framing is the committed record))

**The authoring (grounded this session):** `ReportingOps` read in full (4 typed methods, no checked exceptions — the frozen M9.3 seam; its own javadoc names this WU: "The EZSP binding lands with the M9.4 ZCL write path"); write seams verified (`sendZclFrame:1054` + nwk overload `:1079` + `lookupNetworkAddress`); the ZDO-Bind_req-expressibility unknown pinned as **G-RPT2** (sanctioned fallback: a package-private ZDO-send mirroring the locked pattern; anything wider = STOP); the posture→confirmability routing pinned as CONTRACT (VERIFIED-only stays report-confirmable; non-VERIFIED downgrades per the AMD-95/97 vocabulary with ONE WARN; never-false-CONFIRMED enforced at install time) with mechanism = coder's choice + a STOP valve on any surface widening; reporting NEVER gates adoption/re-link (Doc 02 staging); the anti-vacuous INFO `zigbee.reporting_configured` mandated on EVERY drive completion; 7 test scenarios (incl. ack-lies, the degrade ladder, the §3 fence, the §B log-only pin); FakeNcp minimal widening sanctioned (NCFG precedent); ZERO schema/mints/module-info/build/toml/public-surface + the frozen M9.3 trio untouched; STOP gates G-RPT1–6.

**Nick, in order:** (1) **hivemind beat-4 commit — stages exactly 4** (3 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog; 1 new: the RPT instruction), msg `_scratch/2026-07-07_hivemind_v23-beat-4_commit-msg.txt` → push. (2) **DISPATCH M9.4-RPT** (CC lane, proven allow-list) — paste: *"Read nexsys-hivemind/context/instructions/2026-07-07_M9.4-RPT_reporting-binding-and-posture-routing_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* — §B veto: strike the section before dispatch or say "no §B" and the hub re-stages. (3) On the delivery: paste the completion report to the hub → audit → gates (`:integration:integration-zigbee:test :app:homesynapse-app:test` → `check`) → commit (msg staged at intake) → push → CI glance. (4) **BENCH ITERATION 4** per the instruction's gate block (NO wipe; config UNCHANGED): Pi pull + installDist → relaunch → **Hue: ONE wall-switch power-cycle (never the 6× reset)** → **SNZB: ONE ~5 s hold then HANDS OFF 60+ s (the OBS-2 discriminator)** → the verify greps → paste. Done-when: `reporting_configured` per device + the Hue row materializes + step-11 NON-VACUOUS + pin-2 re-link + the discriminator outcome. (5) Standing: **Dependabot 1 critical (day 10)** · security glance · deletables ×2 · Node-20.

**Hub next:** RPT return two-layer audit → iteration-4 intake (done-when above; a §B log line adjudicates OBS-2 if the SNZB leaves; SD-5 fallback stays last-resort, ruled together) → THE ACCEPTANCE RUN opens for real (P3.11 re-run non-vacuous → P4 bench-hero [automation seeding staged at that beat] → P5 §51 legs → P6 reopen + PJ key OUT) → the 72 h soak (W2-VAL charter at entry; a restart resets the clock) → seed-custody WU JIT (SD-5 HARD GATE rides) → explainability-lane return → v24 authored before depth.

## 2026-07-06/07 (v23 beat 3, MERGED — instrument returns: **OBS-1 ROOT-CAUSED: the FOURTH mechanism-without-driver — `ReportingConfigurator` has NO production caller AND `ReportingOps` has NO production implementation** · **OBS-2: closure CONTESTED by the timeline — the iteration-4 re-pair is the free discriminator** · CI `43c2880` GREEN · **M9.4-RPT is the next WU, on the acceptance-run critical path — authoring on Nick's word**)

**MERGE NOTE (§11 protocol):** a parallel hub instance wrote this beat's first draft + edited the staged bench message; treated as an un-audited return — its OBS-1 claims independently SOURCE-VERIFIED by this hub (all hold, quotes in the bench record); its OBS-2 closure corrected below against Nick's own timeline testimony; staging arithmetic reconciled (+pm-lessons, which this hub WROTE — not queued). **Single-spine-writer rule: retire one of the two live hub sessions.**

**OBS-2 (closure CONTESTED — Nick rules):** the parallel draft recorded "operator-induced both iterations; TCLK retired." Nick's testimony in-thread: **exactly ONE press — the ~5 s pairing hold, which ENDED before the join (:37) — "the only time I pressed that SNZB button during the run."** The leave came at :56, **18.3 s AFTER a clean join+enroll, hands off**, no self-rejoin in 21 min. A pre-join press cannot explain a post-join leave ⇒ CLOSED-BENIGN does not rest on tonight's record; device-initiated is the live alternative (TCLK-update class: ~15 s device timer fits; 0x51-policy/hashed-TCLK candidates; ZHA-stays = the A/B). It-2's two-press account stands for it-2 only. **The discriminator is FREE and already scheduled: iteration 4's SNZB re-pair (hands-off after the hold) — STAYS ⇒ benign closes + pin-2 re-link leg banked; LEAVES ~15–30 s ⇒ device-initiated confirmed → the §B instrument reads it out.** If Nick has a fact the thread lacks (a touch after :37), one word closes this benign and §B drops. **OBS-1 (source facts at `43c2880`):** entities 1-of-2 at +22 min, `viewPosition` frozen 27, zero DLQ lines, zero reporting activity — because `ReportingConfigurator` (M9.3, logic proven vs fakes) was never wired: **no production caller; `ReportingOps` never got its EZSP binding** (the M9.3 Phase-3 note deferred it to "the M9.4 ZCL write path" — M9.4b shipped the write path, not the binding; `ReportingPostureFact` has zero downstream consumers either). The state view consumes NO adoption events — rows materialize from entity-scoped events; the SNZB row came from the device-initiated IAS-enroll arm (the ONE wired arm); the Hue is adopted+registry-real but state-invisible until reporting exists. **REPORTING-CLEAN = VACUOUS pre-RPT** (runbook step 11 amended-in-spirit: the parity evidence is WARN-absence PLUS reports flowing). **The P4/P5 CONFIRMED legs are structurally unreachable pre-RPT** — the hero confirmation rides authoritative reports. **pm-lessons WRITTEN (+1, 2026-07-07):** verifying the gate ≠ verifying the mechanism behind the gate (class-sweeps walk every gated-on edge THROUGH the gate to a production call site) + the vacuous-VERIFY corollary (every no-output-is-healthy VERIFY line gets a paired positive-evidence line — RPT's per-device success INFO exists for exactly this). Boot-WARN classified PRE-EXISTING (all 10 logs) → cosmetic polish row (root-schema/integration-schema registration order). **Deferred ruling row (NON-blocking, rec: UI milestone with the REST accept surface):** should `/api/v1/entities` render adopted-but-never-reported entities (honest UNKNOWN) or is state-view-materializes-on-events as-designed? RPT closes the practical gap within seconds of first report; don't touch projection semantics under bench pressure.

**M9.4-RPT scope sketch (JIT authoring next, grounded this session):** (1) EZSP-backed `ReportingOps` impl — ZDO Bind 0x0021 + ZCL ConfigureReporting 0x06 + ReadReportingConfiguration 0x08 over the existing M9.4b write path, BENCH-VERIFY constants block (the 0x0019/0x90 model); (2) the adoption-path drive — `configureDevice(...)` post-adopt on the ingestion thread (fresh-adopt + the `onRejoin(:145)` arm at re-link), same-thread synchronous (the interview I/O precedent); (3) posture-fact routing to the AMD-97 confirmability consumption (realizing the recorded M9.3/AMD-97 intent — the measured overlay on the profile characterization; the one design decision to pin at authoring); (4) ONE per-device success INFO (`zigbee.reporting_configured: device= clusters= postures=`) — the read-back doctrine, the next bench log is measurement; (5) tests: configurator logic already proven — new coverage = the ops binding + the adoption drive + fact routing. **(6) §B (severable, pending Nick's OBS-2 ruling):** log-only consumption of EZSP 0x009B `zigbeeKeyEstablishmentHandler` — the TCLK-outcome instrument (the TCJ §A/§B disjoint-hunk model; ~one handler arm + verbatim-INFO test); with §B aboard, an iteration-4 SNZB leave is MEASUREMENT, not another theory round. **Done-when (bench iteration 4): the Hue's entity row materializes on its first configured report + step-11 sweep non-vacuous + the SNZB re-pair re-links (pin-2) + the OBS-2 discriminator outcome recorded.**

**Nick, in order:** (1) **the word on M9.4-RPT** — "go, with §B" or "go, without §B" (or the OBS-2 fact that closes it benign) — and I author ISSUE-READY for the CC lane. (2) hivemind commit if not yet run: `_scratch/2026-07-06_hivemind_v23-beats-1-3_commit-msg.txt` (**stages exactly 7: 6 M + 1 R** — pm-handoff · PROJECT_SNAPSHOT · backlog · coder-handoff · coder-lessons · pm-lessons + the v22 prompt R) — or if beats already partially landed, `_scratch/2026-07-06_hivemind_v23-beats-2-3-delta_commit-msg.txt` (**stages exactly 3 M**: pm-handoff · PROJECT_SNAPSHOT · pm-lessons). Your porcelain count picks the file. (3) bench commit — stages exactly 1 M; **msg REC: `_scratch/2026-07-06_bench_iteration-3_commit-msg_v2.txt`** (identical to your edit except the OBS-2 clause now matches your own testimony + the record file); your original stands if you rule OBS-2 benign with the missing fact. (4) Standing: **Dependabot 1 critical (day 9+)** · security glance · deletables ×2 · Node-20 · the rest. App on the Pi can stay up or come down — nothing further to observe tonight; the PJ key stays for the next pairing session.

**Hub next:** author M9.4-RPT on the word (full instruction-format pass: consumer/pin survey at `43c2880`, STOP gates incl. re-grep of both absences, §4c reminder, test-home check) → dispatch → two-layer audit → bench iteration 4 (RPT done-when + pin-2 leg) → THEN the acceptance run opens for real: P3.11 re-run non-vacuous → P4 hero → P5 §51 → P6 + PJ key OUT → soak → seed-custody (SD-5 gate rides) → explainability return. v24 authors before depth if this session approaches limits.

## 2026-07-06/07 (v23 beat 2 — **ITERATION 3 (core `43c2880`): ADOPTION PROVEN ON SILICON — the config accept-list fired ×2, `device_adopted` ×2** · the G-NCFG6 resume arm silicon-proven · **done-when SPLIT: two observations OPEN hold the acceptance-run entry** · instruments dispatched)

**The evidence (log 21:38–21:40):** resume exact (`network_resumed ch20/0x9b65`; `ncp_configured` on a RESUME boot — G-NCFG6's re-configure arm now proven live) → **Hue: power-cycle → re-announce → COMPLETE proposal → `proposal_accepted source=config` → `device_adopted … entities=1`** (+ the anticipated `characterization_unmatched [effect]` tolerate-unknown WARN) → **SNZB: re-pair → join/child_join → COMPLETE proposal → `proposal_accepted` → `device_adopted … entities=1` → `ias_zone_enrolled`**. TCJ+NCFG+ADP: every join-arc mechanism is silicon-proven. Pin-2 untested tonight (the Hue was a FRESH proposal — correct; no re-link occurred pre-leave).

**OBS-1 (OPEN, MEDIUM-defect-suspicion): `/api/v1/entities` = 1-of-2 at +47 s.** ULID-decoded: the one row IS the SNZB entity, minted at adoption-time (01:39:38.536Z) — so the view materializes at adoption and **the Hue's entity is ABSENT** (its ULID window would be ~01:39:18.7). One visible asymmetry: the Hue path carried the `[effect]` characterization skip. Hypotheses: projection drop/DLQ vs late-appearing benign semantics. Instruments: re-query now · DLQ/projection-error grep · ReportingConfigurator activity grep.

**OBS-2 (OPEN, attribution pending Nick): SNZB `device_left` 18.3 s post-join — SECOND occurrence.** It-2's leave was attributed to the operator's long-press; tonight's needs Nick's answer (did he touch it post-pairing?). If untouched ⇒ device-initiated; leading candidate (MEDIUM, unproven): the Z3.0 BDB TCLK-update leg (device-side ~15 s timer fits 18.3 s; policy-0x51/hashed-TCLK interaction candidates; Hue-stays is consistent given Signify tolerance; ZHA-stays-on-this-stick = the standing A/B — mechanism known-good, difference is our TC config). Adoption SURVIVES the leave (registry-persisted; re-pair re-links per pin-2). Candidate instrument (authored only on evidence + Nick's word): log-only micro-WU consuming EZSP 0x009B `zigbeeKeyEstablishmentHandler` — makes the TCLK outcome measurement, the NCFG-read-back doctrine. SD-5 fallback: ruled-WITH-Nick, last resort. The it-2 attribution is FLAGGED in the bench record pending the answer. Also queued-to-classify: the boot `integrations.zigbee not defined in the schema` WARN (pre-existing-class suspected; old-log grep dispatched).

**Nick, in order:** (1) the button question + the 6-command instrument batch (chat; app is LIVE — do NOT restart, the live view is evidence). (2) CI glance `43c2880` (ci.yml + install-smoke — the gate of record; also closes the ccba69d install-smoke glance). (3) hivemind commit: if beat-1 was NOT yet committed use `_scratch/2026-07-06_hivemind_v23-beats-1-2_commit-msg.txt` (stages exactly 6: 5 M + 1 R); if it WAS, use `_scratch/2026-07-06_hivemind_v23-beat-2-delta_commit-msg.txt` (stages exactly 2 M). (4) bench commit — stages exactly 1 M (the record's iteration-3 block), msg `_scratch/2026-07-06_bench_iteration-3_commit-msg.txt`. (5) Standing: Dependabot 1 critical day 9+ · the rest.

**Hub next:** instrument-return intake → OBS-1 ruled (benign ⇒ record + proceed; drop ⇒ hub source-verify the projection path, then a JIT fix WU) · OBS-2 ruled WITH Nick (operator ⇒ record + proceed; device-initiated ⇒ the 0x009B observability micro-WU first — instrument-before-theory) → only on BOTH resolved: the acceptance run opens at P3.11 (the sweep is in the batch; STOP-and-ping on `reporting_ack_lies`) → P4–P6 → PJ key OUT → soak.

## 2026-07-06/07 (v23 beat 1 — LAUNCH FULL-PASS (all 11) · **M9.4-ADP DELIVERED + TWO-LAYER AUDIT ACCEPT (zero code defects)** · gates GREEN PRE-COMMIT (62 + 155 — deferred gate DISCHARGED) · commits staged (core 4 · hivemind 6) · **ITERATION 3 IS THE LIVE GATE — the join arc closes on `device_adopted` ×2 + non-empty entities**)

**Launch:** restart-resume §11 CLEAN (porcelain ×5 lock-free; both spine frontmatters consistent; beat-5 commit `142b499` verified landed, 6 exact) · preflight PASS all 11 (mirrors byte-identical ×3; Open Risks all resolved — §4b CLEAR; 174/52 re-derived from the register's own regeneration line at AMD-98; `adopt(:207-208)` zero-production-callers re-verified pre-return) · v22 prompt archived (rename, rides this commit) · required reads done in full (beats 1–5, ADP instruction pins-folded, bench record + iteration log, runbook field-validated block) · **deletables ledger corrected from source: hivemind cleared-lock ×1 + `skills-initiative` husk — the docs/core cleared-locks are already gone** (the v23 prompt's ×3 claim was stale; source wins).

**The intake (M9.4-ADP, working tree on `ccba69d` — porcelain exactly 3 M + 1 new):** two-layer audit vs the DIFF — 2 fan-out agents (instruction-compliance A1–A10 · test-quality/regression B1–B9, prompts quoting pin-1/re-link-guard/conservative-default verbatim) + hub layer-2 seam reads + regreps. **ACCEPT, zero code defects; 19/19 checks PASS.** The coder's headline discovery VERIFIED TRUE at baseline: the sanctioned slice widening was ZERO-DIFF — `onDeviceDiscovered` has returned package-private `DiscoveryOutcome{LINKED, PROPOSED}` since M9.3 (`git log -S` → `c856eab`; LINKED = re-link-no-proposal, PROPOSED = fresh-mint — exactly the §2 indicator), the adapter was discarding it at `:565`; this WU consumes it (`:594-596`), and the slice is blob-identical both sides (`ca18490f`). Letter-verified at source: **pin-1** — `!= COMPLETE` ⇒ verbatim WARN `zigbee.proposal_incomplete_not_adopted: device={} status={}` + return, listed-only, proposal sits (`:622-626`; T7 pins the WARN + the sit + the later COMPLETE re-proposal adopting); **the re-link guard is outcome-driven** — zero catch in the new code (the 6 adapter catches are all pre-existing; the banned exception-flow does not exist); **conservative default** — absent/empty/non-list ⇒ `Set.of()` ⇒ the gate short-circuits (`:642-644`, `:618-621`); **the ONE schema diff** — `adopt_devices` `:33-37` is the only semantic schema change (full baseline compare); **zero mints** — 71/41/53 derivation LOCATED (EventTypesTest `:44-46` = 71 · EventTypeRegistryTest `:29-33` = 41 · `:42-46` = 53 over AllEventClasses) and all three pinned tests rode Nick's GREEN gates; exactly ONE production `.adopt(` caller repo-wide (the gate, `:629`); module-info/build.gradle.kts byte-identical; new members private/package-private only. **INFO adjudications ×5, all ACCEPT:** zero-diff widening (smaller than sanctioned shape, flagged as instructed) · bare-16-hex additive-only (matches `IEEEAddress.fromHexString`'s documented contract; strict length keeps truncated entries LOUD) · schema deliberately pattern-free (a pattern would strip the whole defaultless key on one typo — the per-entry WARN/skip mandate REQUIRES malformed entries to reach the adapter) · non-list-⇒-absent (schema `type: array` + `ConfigurationReloadException` upstream is the loud layer; adapter fallback is defensive depth, javadoc'd) · gate private-not-package-private (all 7 tests drive the FULL REAL chain through `runCycleOnce()` — zero seam use, stronger than the PJ precedent). Tests bind production log strings VERBATIM via ListAppenders on both loggers; Clock-disciplined (§4c clean); default-gated (only `testing/integration-tests` is piProfile-gated — the R1 class checked). **WUCP P1 artifacts verified against actual files:** coder-handoff entry at TOP single-copy w/ deferred-gate flag + NEXT-WU pointer; coder-lessons +1 (riders-before-response, line 335 — HOST-verified; the VM porcelain hadn't synced it, the claimed-but-absent signature was LAG); MODULE_CONTEXT §M9.4-ADP insertion-only, 4 spot-claims true. **Mount-phantom note (4th hit this arc):** the VM diffstat showed net-DELETIONS on the freshly-written adapter and a VM grep missed `.adopt(:629)` entirely — host tools ruled throughout; never adjudicate this WU's content from the VM view.

**Nick's gates (pre-commit, his terminal):** targeted `:integration:integration-zigbee:test :app:homesynapse-app:test` GREEN (62 tasks) + full `./gradlew check` GREEN (155) — **the deferred build gate is DISCHARGED; residual = core commit + push + the ci.yml/install-smoke glance (the gate of record).**

**Nick, in order:** (1) **core commit — `git add -A` stages exactly 4** (3 M: adapter · schema · MODULE_CONTEXT; 1 new: `ZigbeeConfigAcceptedAdoptionTest`), msg `_scratch/2026-07-06_core_m94-adp_commit-msg.txt` (`git commit -F`) → push → **CI glance (ci.yml on the new HEAD; install-smoke covers the still-open `ccba69d` glance)**. (2) **hivemind commit — `git add -A` stages exactly 6** (5 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · coder-handoff · coder-lessons; 1 R: the v22 prompt → archive/), msg `_scratch/2026-07-06_hivemind_v23-beat-1_commit-msg.txt` → push. (3) **BENCH ITERATION 3** (custody PRESERVED — NO wipe): add to `~/hs-bench/config/integrations/zigbee.yaml` → `adopt_devices: ["0x00178801101A09BB", "0xF044D3FFFE9C78D7"]` (keep `permit_join_duration: 254`) → Pi `git pull` + `installDist` → relaunch → expect `network_resumed: channel=20 panId=0x9b65` → **power-cycle the Hue** (re-announce → re-interview → fresh COMPLETE proposal → config-adopts) → **re-pair the SNZB** (it LEFT — button hold ~5 s in-window) → **⏺ RECORD: `zigbee.proposal_accepted` ×2 + `device_adopted` ×2 + `curl /api/v1/entities` non-empty — done-when; paste the log either way.** A listed-PARTIAL stall shows as the loud `proposal_incomplete_not_adopted` WARN (re-announce re-proposes; expected iteration, not defect). (4) On the join-arc close, the ACCEPTANCE RUN opens mid-P3: **step 11 Configure-Reporting sweep** (`grep -E "zigbee\.(bind_failed|reporting_ack_lies|ias_enroll_incomplete)" ~/hs-bench/bench-*.log || echo REPORTING-CLEAN` — ⏺ RECORD per device; **STOP-and-ping on any `reporting_ack_lies`**) → step 12 entity ULIDs → P4 bench-hero → P5 §51 legs → P6 reopen + REMOVE the PJ key. (5) Standing: **Dependabot 1 critical (day 9+)** · security glance · deletables ×2 (corrected) · telemetry · website.yml · LICENSE · frontend.yml · Wave-2 · Node-20 · naming.

**Hub next:** iteration-3 intake at the ⏺ RECORD points (the join arc closes TCJ+NCFG+ADP together) → acceptance-run intake P3.11→P6 (any hashed-TCLK join-instability ⇒ SD-5 fallback ruled WITH Nick, never improvised) → 72 h soak ENTERS on acceptance (PJ key OUT first; NQ-6 DURING; W2-VAL charter at entry; a restart resets the clock) → seed-custody WU JIT on bench evidence (SD-5 HARD GATE rides until it lands) → explainability-lane return audit → the javadoc-flip micro-pass (0x0019/0x90 + TCJ + NCFG BENCH-VERIFY ids, all silicon-proven — ADP added no new wire ids) + the deflake row + the M9.4 polish queue ride their named seams — never authored while a gate is open.

## 2026-07-06 (v22 beat 5 — CI gate CLEARED (intermittent-definitive) · **ADOPTION RULED (b)** + pins folded · TCJ §A.3 SILICON-CLOSED (Nick's confirm) · M9.4-ADP ISSUE-READY → DISPATCHING · **v23 AUTHORED**)

**The rulings (Nick, verbatim-grade — preserve):** (1) **CI:** rerun on `ccba69d` GREEN → OR-GATE-M9.4-NCFG closed **intermittent-definitive** — his causality: NCFG touched integration-zigbee only and *cannot causally reach event-bus replay*; runner variance, not regression. Deflake row queued as his named **"latent moat-debt"** (progress-based await, fail-on-stall). §4b unblocked. (2) **Adoption = (b), config accept-list.** His rationale: *(b) honors both Locked docs as-shipped — a user hand-listing a device's IEEE is deliberate per-device acceptance (Doc 02 §3.12 Stage 3), and it's config, not REST (Doc 09 intact). The decisive point: (b) is ADDITIVE, not a one-way door — the REST/UI accept surface (a) coexists later; defer the Doc 09 amendment to the UI milestone where it belongs, never amend a Locked boundary under bench pressure.* His two pins FOLDED into the instruction pre-dispatch (the hub's §2 "completeness not re-checked" text was superseded — a material divergence caught at the ruling): **pin-1** adopt only on a COMPLETED proposal for a listed IEEE (a listed-PARTIAL logs ONE WARN `proposal_incomplete_not_adopted` and sits; +test scenario 7); **pin-2** re-links stay automatic — accept-list is first-adoption-only consent. (3) **TCJ §A.3 SILICON-CLOSED** (his upgrade of the record): `UNSECURED_JOIN → USE_PRECONFIGURED_KEY → Device_annce` proves the transient well-known key completed a REAL key exchange — transient-key USE is validated, not merely accepted (window-expiry behavior remains the one unobserved §A.3 leg — noted, low-stakes, observable at any post-window join attempt).

**Landings reconciled:** hivemind beat-4 = `0cf1e34` (4 exact) · bench = `f8a0693` (1 exact; the record's iteration-2 block). **TCJ + NCFG are both FULLY VALIDATED on silicon (Nick's words: on the record).** M9.4-ADP flipped ISSUE-READY (rationale-index row added); **dispatch is GO** on the folded instruction.

**Nick, in order:** (1) **hivemind beat-5 commit — stages exactly 6** (5 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · decision-rationale-index · the ADP instruction [pin fold]; 1 new: the v23 prompt), msg `_scratch/2026-07-06_hivemind_v22-beat-5_commit-msg.txt` → push. (2) **DISPATCH M9.4-ADP** — paste: *"Read nexsys-hivemind/context/instructions/2026-07-06_M9.4-ADP_config-accepted-adoption_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (3) On the landing: gates → commit → push → CI → **iteration 3** (NO wipe — resume ch20/0x9b65; `adopt_devices: ["0x00178801101A09BB", "0xF044D3FFFE9C78D7"]` + keep `permit_join_duration: 254`; relaunch; Hue power-cycle re-proposes; SNZB re-pairs [5 s hold, in-window]; **done-when: `device_adopted` ×2 + non-empty `/api/v1/entities`**) → then the ACCEPTANCE RUN opens (runbook P3 step 11: Configure-Reporting sweep → P4 bench-hero → P5 §51 legs → P6). (4) install-smoke on `ccba69d` — one glance (the rerun covered Build & Check). (5) Standing: **Dependabot 1 critical (day 9+)** · security glance · deletables ×3 · Node-20 (the CI deprecation banner showed again tonight) · the rest.

**Hub next:** ADP return two-layer audit → iteration-3 intake (**the join arc closes: TCJ+NCFG+ADP**) → acceptance-run intake at the ⏺ RECORD points → 72 h soak ENTERS (PJ key OUT at entry; NQ-6 DURING; W2-VAL charter) → seed-custody WU JIT (SD-5 HARD GATE rides) → explainability-lane return audit → **v22 retires; v23 launches on Nick's word** (authored this beat — `context/handoff/2026-07-06_PM-mission-control_v23_orchestrator_session_prompt.md`).

## 2026-07-06 (v22 beat 4 — **ITERATION 2: THE DEVICES JOIN ON SILICON** · the last un-driven mechanism found (adoption has no production caller) · **ESCALATION: the Tier-1 acceptance surface** (two Locked docs at the fork; rec (b) config accept-list; M9.4-ADP pre-authored PENDING-RULING))

**Iteration 2 (core `ccba69d` — NCFG landed; log 19:05):** read-back CLEAN (`zigbee.ncp_configured: zdo_flags=0x3 stack_profile=2 security_level=5`; skips id=0x1/0x6 = the expected firmware-managed tail) → fresh formation ch20 PAN `0x9b65` → **Hue LCA017 JOINS at +11 s** (`device_join … status=UNSECURED_JOIN decision=USE_PRECONFIGURED_KEY` → announce → `device_proposed … profile=philips_hue_white_color_a19 status=COMPLETE`) → **SNZB-03P JOINS at +46 s** (join → `child_join type=SLEEPY_END_DEVICE` → announce → proposed COMPLETE → **`ias_zone_enrolled`** — F-7a on silicon). The NCFG association-layer diagnosis was CORRECT; TCJ's 0x0024/0x0023 layouts parsed REAL payloads (both records silicon-proven); the transient-key join path works within the window (§A.3 expiry semantics still unobserved). SNZB `device_left` 19:06:15 = the operator's second long-press (leave+re-pair mode — honest observability, no defect); the Hue "never blinked" because it joined DIRECTLY on power-up (already factory-fresh — the blink marks the 6×-reset completing, which it never needed); the 19:05:55 re-announce = his continued power-cycling. **The stall: `device_proposed` → `device_adopted` NEVER fires — zero entities.** Hub-verified: `ZigbeeAdoptionSlice.adopt(:207)` has ZERO production callers (its own javadoc: "callable (tests/M9.4 API)"); the E2Es pre-adopted via the package-private seam — the M9.4-PJ class a THIRD time (mechanism complete, driver absent). **Class-sweep run:** adoption was the LAST runbook-expected trigger without a production driver (automation/dispatch/confirmation/REST verified driven; reporting-config is adoption-gated and unblocks with it). pm-lessons fold queued: a runbook VERIFY line's production TRIGGER must be grep-verified end-to-end (emitter + caller chain), not just the log line's existence — the PJ lesson generalized.

**ESCALATION TO NICK — the Tier-1 adoption-acceptance surface.** Issue: Doc 02 §3.12 Stage 3 (LOCKED) — *"The user accepts a proposed device"* — gates adoption on user consent; Doc 09 :65 (LOCKED) — adoption is *"not [an] API operation in Tier 1."* No acceptance surface exists. Options: **(a)** AMD Doc 09 now — Tier-1 REST proposals/adopt endpoints (the eventual UI path; a real Locked-boundary amendment tonight); **(b)** config-file acceptance — `integrations.zigbee.adopt_devices: ["<IEEE>", …]`, per-device consent in the Tier-1 idiom (the config file IS the headless-first user surface; Doc 02's text honored — consent per device by hardware identity; Doc 09 untouched; PJ-shaped micro-WU, +1 schema key); **(c)** another surface you prefer. **Hub rec: (b) now, (a) rides the UI milestone** (where the Tier boundary gets a deliberate pass, not an emergency amendment). Blocking: the bench pairing legs only — commits/CI proceed regardless. **M9.4-ADP is PRE-AUTHORED for (b)** (`context/instructions/2026-07-06_M9.4-ADP_config-accepted-adoption_coding-instruction.md`, status PENDING-RULING — the re-link guard [adopt only on FRESH proposals; exception-driven flow banned], conservative default [absent list = nothing adopts], 6 test scenarios, iteration-3 bench step [custody PRESERVED — no wipe; power-cycle the Hue to re-propose; re-pair the SNZB which left]). One line rules it.

**Nick, in order:** (1) **THE RULING** — (a)/(b)/(c); "(b), go" dispatches immediately. (2) **hivemind beat-4 commit — stages exactly 4** (3 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog; 1 new: the ADP instruction), msg `_scratch/2026-07-06_hivemind_v22-beat-4_commit-msg.txt` → push. (3) **bench commit — stages exactly 1 M** (the record, iteration-2 block), msg `_scratch/2026-07-06_bench_iteration-2_commit-msg.txt` → push. (4) **THE `ccba69d` CI RED (arrived at beat-4 close): re-run the failed job** — see OR-GATE-M9.4-NCFG below (event-bus `ReplayTransitionIT` 30 s-await timeout; module untouched 65 commits; local green ×2 — flake-leaning, NOT waved off; rerun green clears it, rerun red escalates). (5) On (b) **AND the gate clearing**: **DISPATCH M9.4-ADP** — paste: *"Read nexsys-hivemind/context/instructions/2026-07-06_M9.4-ADP_config-accepted-adoption_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (6) **CI glance `2a5fba2`** (still unconfirmed) + install-smoke on `ccba69d`. (6) Standing: **Dependabot 1 critical — day 9+, re-nagged on tonight's push output** · security glance · deletables ×3 · the rest.

**Hub next:** ADP return audit → iteration 3 (accept-list in config → Hue power-cycle re-proposes → **`device_adopted` ×2 + non-empty `/api/v1/entities` = the join arc CLOSES** [TCJ+NCFG+ADP together]) → runbook Phase 3 step 11 onward (Configure-Reporting sweep → bench-hero automation → §51 live → latencies → IAS ×2/Q10/FE-7) → the bench-verify javadoc flip queue (0x0019/0x90 + TCJ + NCFG ids — now ALL silicon-proven) rides the next core WU → 72 h soak (PJ key OUT; W2-VAL charter) → seed-custody WU (SD-5 gate rides) → explainability return → **v23 authored before depth (this hub is at 4 beats + 3 dispatches — nearing it).**

## 2026-07-06 (v22 beat 3 — **M9.4-NCFG DELIVERED + AUDITED ACCEPT** · gates GREEN PRE-COMMIT (62 + 155) · commits staged (core 5 · hivemind 5) · **bench iteration 2 is the live gate**)

**The intake:** NCFG delivered per the re-issued instruction (all six STOP gates passed at `2a5fba2`; tests-first; the coder's own 4-lens adversarial fleet zero-defects). Hub layer-2 seam adjudication: `configureNcp()` at `:419` INSIDE the guarded post-negotiation block (`negotiatedVersion` commits `:414`) — the exact G-NCFG6 shape; `resetSession()` clears negotiation state (`:583`) so the watchdog-reopen ladder re-negotiates AND re-configures; **the coder's "every real path is safe" claim on the retry edge VERIFIED at the supervisor** — a relaunch constructs a FRESH adapter (`StandardIntegrationSupervisor:705-708`), so no in-place `startSession()` retry exists to skip config (MODULE_CONTEXT gotcha recorded); the SUPPORTING arm logs `ncp_config_skipped` per instruction (REQUIRED = `ncp_config_rejected` + honest failure BEFORE any formation frame — never a half-open window); the new `ZigbeeNcpConfigurationTest` binds production constants (`containsExactly` REQUIRED-first ordering; the `zigbee.ncp_configured` read-back INFO asserted verbatim; a 6th reset-then-reconfigure scenario). The FakeNcp built-in config model realizes the sanctioned responder-widening at the SHARED fixture — every per-class responder byte-untouched, the regression matrix green structurally. Zero-diff pins hold (5 files, one module; no module-info/build/toml/schema/mints/public surface). Nick's gates pre-commit: 62 targeted + full `check` 155 GREEN — deferred gate DISCHARGED; residual = commit + push + CI glance.

**Nick, in order:** (1) **core commit — stages exactly 5** (4 M + 1 new), msg `_scratch/2026-07-06_core_m94-ncfg_commit-msg.txt` → push → CI glance. (2) **hivemind commit — stages exactly 5 M**, msg `_scratch/2026-07-06_hivemind_v22-beat-3_commit-msg.txt` → push. (3) **BENCH ITERATION 2** — Pi pull + `installDist` → wipe `~/hs-bench/data/zigbee` → relaunch → **⏺ RECORD `zigbee.ncp_configured` VERBATIM** (healthy: `zdo_flags=0x3 stack_profile=2 security_level=5`; `ncp_config_skipped` WARNs on the SUPPORTING tail are EXPECTED, not failures) → Hue reset first (6×-cycle in-range), then SNZB (5 s hold) → paste the log either way. (4) Standing ledger unchanged (Dependabot 1 critical · security glance · deletables ×3 · the rest).

**Hub next:** iteration-2 intake — a join closes TCJ+NCFG together (then §A.3 transient-expiry observed; runbook P3–P6 resume; the bench-verify javadoc flip queue [0x0019/0x90 + TCJ ids + NCFG ids] rides the next core-touching WU) · `ncp_config_rejected`/`readback_mismatch` ⇒ one-constant correction fed to CC · clean read-back + still-silent ⇒ the association-layer instrument (ZHA/bellows sniff of the reset, or a brief ZHA re-pair proof that the devices still attempt) — **ruled WITH Nick, never improvised** · then soak (PJ key OUT at entry; W2-VAL charter) → seed-custody WU (SD-5 HARD GATE rides) → explainability-lane return → v23 before depth.

## 2026-07-06 (v22 beat 2 — **BENCH ITERATION 1: enablement SILICON-ACCEPTED, joins TOTALLY SILENT** · the NCP-never-configured gap INDEPENDENTLY VERIFIED · **M9.4-NCFG RE-ISSUED under the hub** (4 corrections) · dispatch on Nick's word)

**The bench evidence (core `2a5fba2` — TCJ + beat-1 both LANDED; hivemind beat-1 = `5662034`):** §B validated on silicon — `channel_pinned: channel=20` → `network_formed: channel=20 panId=0x3fe6` (fresh custody after the wipe), and the resume arm re-proven on the second boot (`network_resumed` + `network_up (buffered)`). TCJ §A enablement **ACCEPTED by the frozen v13 stack** — `zigbee.tc_joins_enabled` logged, all three `requireSuccess` passed, then `permit_join_opened: duration=254s`. **Then total silence:** both devices factory-reset in-range across two 254 s windows — zero `device_join` (0x0024), zero `child_join`, zero announce, entities empty. The honesty posture held: nothing false-rendered; the system simply cannot see a join.

**The diagnosis intake (an out-of-band Cowork session authored M9.4-NCFG; the hub treated it as an un-audited return):** VERIFIED at source — hub regreps ×9 at the `2a5fba2` object tree: ZERO `setConfigurationValue`/`EZSP_CONFIG`/`STACK_PROFILE`/`SECURITY_LEVEL`/`ZDO_FLAGS` anywhere in integration-zigbee main; `startSession()` (`:311`) is version-negotiation-only; the NCP resets to firmware defaults every launch (`resetCode=0xb`); bellows/ZHA (which pairs these devices on this stick) writes a config batch before stack-up. The config gap is REAL and must land regardless. **CORRECTED — the "ZDO_FLAGS hides Device_annce = the whole story" claim does not fit the evidence shape:** `trustCenterJoinHandler (0x0024)` is a STACK callback firing on association events (including DENIED joins) and is NOT ZDO-gated; if ZDO-flags were the only gap we'd have seen `device_join` lines with adoption stalling later. Zero 0x0024 places the failure at/below ASSOCIATION — **`STACK_PROFILE` (default 0) beacon-incompatibility is the leading total-silence candidate** (Z3.0 devices ignore a non-PRO beacon → never attempt); ZDO_FLAGS is the announce-leg candidate; SECURITY_LEVEL=5 likely already default (pinned as hygiene). The re-issued instruction records causality honestly so a still-silent iteration 3 looks at the association layer, not a spent theory.

**The re-issue (`context/instructions/2026-07-06_M9.4-NCFG_ncp-session-configuration_coding-instruction.md`, provenance line in-file):** hub corrections = (1) **§1.2 read-back instrument** — `getConfigurationValue` the three REQUIRED ids post-write, ONE INFO `zigbee.ncp_configured: zdo_flags/stack_profile/security_level` with the NCP-REPORTED values (closes accepted-but-not-applied; the next bench log becomes measurement); mismatch ⇒ WARN + honest failure. (2) **G-NCFG6** — `startSession()`'s `negotiatedVersion > 0` early-return vs the reopen path: `resetSession()` must clear negotiation state so a watchdog-reopened (defaults-reset) NCP re-configures; if it doesn't, that's a live defect to surface, not absorb. (3) Mechanism-honest wording throughout. (4) The bench step pinned in the gate block (one more custody wipe → fresh formation under corrected config; Hue first). Batch unchanged from the draft: REQUIRED {STACK_PROFILE=2 · SECURITY_LEVEL=5 · ZDO_FLAGS=0x0003} fail-honest (never-false-ALIVE: no half-open window over a rejected config) + SUPPORTING ×7 best-effort WARN-and-continue (7.4.x self-manages some — e.g. packet buffers). Zero public surface, zero mints; tests-first ×5 incl. the FakeNcp 0x0053/0x0052 responder widening.

**Nick, in order:** (1) **hivemind beat-2 commit — `git add -A` stages exactly 4** (3 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog; 1 new: the M9.4-NCFG instruction), msg `_scratch/2026-07-06_hivemind_v22-beat-2_commit-msg.txt` → push. (Supersedes the out-of-band session's `_scratch/2026-07-06_hivemind_ncfg-instruction_commit-msg.txt` — its "stages exactly 1" predates the spine writes; use mine.) (2) **bench commit — stages exactly 1 M** (the acceptance record, iteration-1 block), msg `_scratch/2026-07-06_bench_iteration-1_commit-msg.txt` → push. (3) **DISPATCH M9.4-NCFG** (CC lane, proven allow-list) — paste: *"Read nexsys-hivemind/context/instructions/2026-07-06_M9.4-NCFG_ncp-session-configuration_coding-instruction.md, run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (4) **CI glance on `2a5fba2`** (ci.yml + install-smoke) if not already confirmed — the TCJ gate of record. (5) On the NCFG landing: gates → commit → push → CI → Pi pull + `installDist` → `rm -rf ~/hs-bench/data/zigbee` → relaunch → **⏺ RECORD `zigbee.ncp_configured` verbatim** → Hue reset first, then SNZB → paste the log either way. (6) Standing: **Dependabot 1 critical day 9+** · security glance · deletables ×3 · telemetry · website.yml · LICENSE · frontend.yml · Wave-2 · Node-20 · naming.

**Hub next:** NCFG return two-layer audit → iteration-2 intake (the `ncp_configured` read-back is the measurement; a join closes TCJ+NCFG together — then §A.3 transient-expiry observed, the bench-verify javadoc flips queue on the next core WU) → if STILL silent with a clean read-back: the association-layer instrument (ZHA/bellows sniff or a brief ZHA re-pair proof) ruled WITH Nick, never improvised → on a real join: runbook P3–P6 → 72 h soak (remove the PJ key at entry; W2-VAL charter) → seed-custody WU JIT (SD-5 HARD GATE rides) → explainability-lane return audit → v23 before depth.

## 2026-07-06 (v22 beat 1 — **M9.4-TCJ DELIVERED + TWO-LAYER AUDIT ACCEPT (zero code defects)** · Nick's gates GREEN PRE-COMMIT (targeted 62 + full `check` 155 — the deferred gate DISCHARGED) · R1/R2 RULED · commits STAGED (core 10 · hivemind 6) · **the §A bench iteration is OPEN — unit-green ≠ done-when**)

**The intake:** M9.4-TCJ delivered (CC lane; §B authored first, tests-before-impl; STOP gates G-TCJ1–5 re-verified at `e363320` ALL PASS — the G-TCJ5 sweep proved the announce→interview→adopt chain single-caller). Two-layer audit vs the working tree at `e363320`: 2 fan-out agents (instruction-compliance §A ×8 checks · honesty/§B/tests/regression ×10 checks) + hub layer-2 direct seam reads + the hub regrep of the one-key-literal ABSENT-claim — **ACCEPT, zero code defects; 18/18 checks PASS.** Layer-2 verified at source: adapter `:452-469` (absent-key early-return → clamp → `enablePreconfiguredKeyJoins()` → `permitJoin` → deadline-AFTER — a NAK propagates with the deadline UNSET, never a half-open door), the 3× `execute`+`requireSuccess` enablement `:707-728` (policy 0x0055 ×2 → wildcard all-0xFF transient 0x0111 carrying the REUSED `TC_LINK_KEY` — regrep: ONE literal in src/main `:242`), the `ZclIngestionUnit` log-only join arms (never a device/interview/event — adoption stays Device_annce-gated), atomic-per-window (`attemptReopen` never re-enables). §B verified: form-path-only read; pinned ⇒ zero energy scan; resume never consults the key (stored channel outranks config, test-pinned); custody path identical to scan-formation. Zero mint re-verified: module-info + build.gradle.kts BYTE-IDENTICAL to baseline; 71/41/53 hold; public surface = exactly the one `CoordinatorProtocol` method; both new records pass the STOP-check. **Nick's gates pre-commit: targeted 62 tasks + full `./gradlew check` 155 GREEN — the deferred gate is DISCHARGED; residual = core commit + push + the ci.yml/install-smoke glance (the gate of record).**

**Rulings:** **R1** (`CoordinatorProtocol` +1 method vs the MODULE_CONTEXT frozen-surface note) = **ACCEPT — instruction-worked-as-designed**: the budget was deliberate and twice-stated in the instruction; the seam is coordinator-neutral (INV-CE-04 analyzed — ZNP has the same join-policy concept); sole implementor repo-wide verified; MODULE_CONTEXT records the sanctioned exception. **R2** (reopen-vs-window: `isPermitJoinActive()` can read true post-reopen for a window the reset NCP no longer holds) = **ACCEPT as the recorded pre-existing M9.4-PJ limitation** — no new dishonesty class (a stale-window join attempt fails honestly and synthesizes nothing; device-level never-false-ALIVE untouched); **polish row queued: clear `permitJoinDeadline` in `attemptReopen()` (one line) — rides the next zigbee-touching WU.** INFOs accepted: §B out-of-range = **WARN + IGNORE, never clamp** (a clamped channel is a materially different radio election, unlike a clamped duration — "an invalid pin is ignored loudly, never obeyed approximately", operator semantic on record) · `device_join_failed` +`decision=` field (diagnostic superset) · sl_Status v13 low-byte read (BENCH-VERIFY rides §A anyway) · the instruction's "checked exception" wording in §A.4.6 was hub authoring imprecision, owned — the mandate (propagation + TRANSIENT classification + window-not-open) holds exactly; `EzspCommandException` is Doc-05-classified TRANSIENT.

**WUCP P2:** coder P1 artifacts verified against actual files (the mount-lag rule): coder-handoff entry at TOP, single copy — **the append-below-separator class did NOT repeat**; coder-lessons +1 (riders-not-drained / ping-then-cycle, line 327 — the VM view lagged, host Grep is truth); MODULE_CONTEXT M9.4-TCJ section source-audited claim-by-claim (agent B check-10: no contradiction). Backlog M9.4 row updated: **UNIT-GREEN ≠ WU-DONE — bench-iterative, the done-when is a real device joining on silicon.** Check 9 unaffected (zero skill edits this WU).

**Environment (paid this beat):** the mount served the §2 phantom on the TCJ working tree (diffstat claimed 596 deletions on a mostly-additive set — quarantined; all audit reads were host file tools + git-object baselines) · an audit agent's plain `git status` stranded a core `index.lock` → **mv'd to `homesynapse-core/.git/index.lock.cleared-by-cowork-2026-07-06`** (delete at leisure; the commit is unblocked) · the hivemind mount serves the corrupt-index signature again (object-reads-only all session; host porcelain is truth for the counts below).

**Nick, in order:** (1) **core commit — `git add -A` stages exactly 10** (8 M + 2 new, all `integration/integration-zigbee`), msg `_scratch/2026-07-06_core_m94-tcj_commit-msg.txt` → push → **ci.yml + install-smoke glance (the gate of record)**. STOP if host porcelain ≠ 10. (2) **hivemind commit — stages exactly 6 entries** (5 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · coder-handoff · coder-lessons; 1 R: the v21 prompt → `archive/` [pre-stage: 1 D + 1 ??]), msg `_scratch/2026-07-06_hivemind_v22-beat-1_commit-msg.txt` → push. (3) **THE BENCH §A ITERATION** — rec (veto-or-default): pin ch20 (`channel: 20` in `integrations/zigbee.yaml`) + wipe `~/hs-bench/data/zigbee` ONCE (nothing adopted yet — the wipe costs zero; ZHA proved both devices join on ch20, so the join iteration runs one-variable) → Pi `git pull` + `installDist` → launch → runbook Phase 3 (bulb 6×-cycle / sensor 5 s hold, device in-range immediately — the window is 254 s per boot) → paste the log from launch through the join attempt. (4) Deletables: + the NEW core `index.lock.cleared-by-cowork-2026-07-06`. (5) Standing: **Dependabot 1 critical day 9+** · the repo-briefly-public security glance · telemetry · website.yml · LICENSE-flip · frontend.yml · Wave-2 · Node-20 · naming.

**Hub next:** bench-log intake at the ⏺ RECORD points (a NAK on enablement or a stalled join ⇒ ONE-constant/one-layout correction fed to CC — the BENCH-VERIFY blocks are isolated for exactly this; §A.3 transient-key expiry observed on silicon while pairing) → on a real join: resume runbook P3–P6 (Configure-Reporting explicit → bench-hero → §51 live → latencies → IAS ×2 / Q10 / FE-7) + flip the 0x0019/0x90 + TCJ bench-verify javadocs at the next core-touching WU → **72 h soak ENTERS on acceptance** (remove the PJ key first; NQ-6 DURING; W2-VAL charter at entry) → the seed-custody WU JIT (the SD-5 HARD GATE rides until it lands) → the explainability-lane return audit → v23 authored before depth.

**Rotated 2026-07-07 (v23 hub, beat 7 — the close-hygiene rotation; git-object-sourced from hivemind HEAD `91f8cda`):** beat sections v21-beat-3 → v17-beat-1 (v21 ×3 · v20 ×3 · v19 ×1 · v18 ×6 · v17 ×6) → `archive/pm-handoff-beats-rotated-2026-07-07.md`. Kept: v23 beats 1–7 + v22 beats 1–5 + Open Risks. Full history in git.

**Rotated 2026-07-04 (v19 hub, the currency pass §2A; git-object-sourced from hivemind HEAD `68ccbf6`):** beat sections v16-beat-65 -> v13-beat-44 (22 sections: v16 61-65 · v15 57-60 · v14 48-56 · v13 44-47) -> `archive/pm-handoff-beats-rotated-2026-07-04.md`. Kept: v18 beats 1-6 + v17 beats 1-6 + Open Risks. Full history in git.

**Rotated 2026-07-02 (v14 hub first-act — the deferred beat-block rotation, phantom-safe file-tools-only method):** beat sections 5→43 (33 sections; beat 18 was never written to this file) -> `archive/pm-handoff-beats-rotated-2026-07-02.md`. Kept: beats 44–47 + Open Risks (reconciled). Full history in git.

**Rotated 2026-06-22 (v5-hub first-act hygiene; git-object-sourced from HEAD `04c0a54`):** the stale HTML-masthead reconciliation prose (2026-06-13 / -14 / -18 / -18 / -19) + the beats older than beat-5 (beat-4 v4-launch, beat-3 v4-handoff, beat-2 explainability, plus the v3-launch / M7.2a-1-landing / v2-parallel-lanes / Doc-16-Lock / AB-1+AB-2 entries) -> `archive/pm-handoff-rotated-2026-06-22.md`. Kept: beat-6 + beat-5 + Open Risks. Full history in git.

**Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; git-object-sourced from `58794d7`):** the ratification record (Return ASSESSED → RATIFIED → mechanics), the superseded fold-abort record, the M7-AMD-block record, the M6.4-WUCP record + the M6.2-era fragment, and the RESOLVED/CLOSED build-gate blocks from Open Risks → `archive/pm-handoff-2026-06.md`. Earlier rotations per the markers preserved there. Full history in git.

## Open Risks

#### OR-GATE-M9.4-NCFG — ci.yml RED on pushed `ccba69d` (`ReplayTransitionIT` timeout) — [✅ RESOLVED 2026-07-06, v22 beat 5 — rerun GREEN; closed as INTERMITTENT-definitive] (NEW 2026-07-06, v22 beat 4+)
- **RESOLVED (beat 5):** Nick re-ran the failed jobs on `ccba69d` → GREEN. Closed as intermittent with Nick's reinforcing causality on the record: *NCFG touched integration-zigbee only and cannot causally reach event-bus replay — runner variance, not a regression; "intermittent" is definitive, not probable.* §4b UNBLOCKED; the M9.4-ADP dispatch hold lifted. **The deflake row stays QUEUED regardless (Nick: "latent moat-debt"):** `awaitCheckpoint` must report checkpoint PROGRESS and fail on STALL, not wall-clock — rides the next event-bus-touching WU (its own micro-WU on recurrence).
- **Milestone:** M9.4-NCFG (the gate of record on its push). **Commit:** `ccba69d`. **Failure:** `:core:event-bus:test` → `ReplayTransitionIT` "resumes from persisted checkpoint and catches up to LIVE without gaps under continuous publish" — `AssertionError` at `:212` = the `awaitCheckpoint` 30 s timeout (called `:167`); 221 tests, 1 failed.
- **Adjudication frame (hub, at intake):** event-bus is UNTOUCHED for 65 commits (last touch `a441fdf`, M4.0a); CI ran green on identical event-bus bytes many times incl. `e363320` same-day; Nick's local full `check` GREEN ×2 today on trees carrying this exact code; the failing await is wall-clock-bounded real-thread concurrency (1500-event continuous publish) — the slow-runner flake profile. NOT waved off: the same signature would present for a REAL subscriber-stall race, and the error omits the last-seen checkpoint — undecidable from the log alone.
- **Owner:** Nick (re-run) / hub (adjudication + the deflake row). **Commands (Nick):** GitHub → the failed run on `ccba69d` → **Re-run failed jobs**. Optional local hunt: `./gradlew :core:event-bus:test --tests "*ReplayTransitionIT*"` repeated ×5.
- **Resolution condition:** rerun GREEN ⇒ gate CLEARS as intermittent (this OR closes; the **deflake/instrumentation row is QUEUED regardless** — `awaitCheckpoint` must report the last-seen checkpoint and fail on STALL rather than pure wall-clock; rides the next event-bus-touching WU, its own micro-WU if it recurs). Rerun RED ⇒ escalates to a REAL investigation WU (a reproducible-on-runner liveness failure in the delivery path outranks the bench — never-false-* extends to "without gaps").
- **§4b consequence: the M9.4-ADP dispatch HOLDS until this gate clears** (the ruling can land meanwhile; "go" fires on the green).

#### OR-GATE-M7.5c-a — deferred build gate on the M7.5c-a working tree — [✅ RESOLVED 2026-07-02, beat 50] (NEW 2026-07-02, beat 49)
- **RESOLVED (beat 50):** round 2 GREEN — 53 targeted + 149 full-`check` tasks on Nick's terminal → core committed **`e3d7296`** (10 paths exact) → pushed → **ci.yml ✅ + install-smoke ✅** on the push. M9.1 routing UNBLOCKED (§4b clear). Total: ONE fix round (2 test-compile errors, hub-fixed — within the P5 one-round target for compile-class defects).
- **Milestone:** M7.5c-a (internal-read envelope conformance). **Commit:** pending — working tree on core `2786526`; change set = exactly 10 files (7 source/test + 3 MODULE_CONTEXT).
- **Commands (Nick):** `./gradlew :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` → `./gradlew :api:rest-api:test :lifecycle:lifecycle:test` → `./gradlew check` (count pins stay **71/41/53**; both module-infos + every `build.gradle.kts` unchanged) → `./gradlew :testing:integration-tests:check` (watch `EndpointE2eIT` — DLQ paths moved under `$.data`). `-Werror` rides the convention plugin — do NOT pass it as a CLI flag.
- **Round 1 (2026-07-02):** targeted `compileJava` GREEN + `:testing:integration-tests:check` GREEN + host porcelain exactly 10 ✓; `compileTestJava` FAILED — 2 AssertJ varargs-capture errors (`DlqStatusEndpointTest` L75/L135: `containsExactly` on a `List<?>` actual). **Hub-applied 2-line fix** (SendMessage-to-lane unavailable; mechanical class): the `@SuppressWarnings("unchecked")` typed-local idiom per conforming `ListEntitiesEndpointTest` L83–88. Lesson appended to coder-lessons. **Re-run pending:** `:api:rest-api:test :lifecycle:lifecycle:test` → `check`.
- **Owner:** Nick. **Deferred Build Gate:** YES — the lane's sandbox attempt failed at `build-logic` (mount delete + VM JDK 11 vs 21); evidenced in coder-handoff, never silently skipped.
- **Resolution condition:** `check` green → core commit (staged message, exactly 10 paths) → push → `ci.yml` green on the pushed commit (the gate of record). On resolution: M9.1 routes to the next Coder lane; FE-1b launches on the landing.

#### OR-M7-WIRING — Command-pipeline live wiring — [✅ RESOLVED 2026-06-27 — live-wired at M7.4b/c] (formalized 2026-06-25; NEW in beat 7, 2026-06-22)
- **RESOLVED 2026-06-27 (M7.4b producer + M7.4c confirmation/ledger; v8 beats 17/19; reconciled by the v9 hub).** The command pipeline is LIVE on the real composition root: trigger → run → `command_issued` → co-located dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`, with the live `pending_command_ledger` + `pollExpirations()` tick and paired teardown. The resolution condition's **E2E composition-root confirm gate is met** — `RunPipelineConfirmWiringTest` (host-verified present in `lifecycle/lifecycle/src/test`).
- **Residual → M7.4d (the only remaining M7.4-arc item):** the resolution condition's *second* clause — the **D2 pure-function-replay CI test wired as a gate** (+ registering D2's canonical INV-id) — is NOT yet landed. The *wiring* is resolved; the *replay-safety CI gate* is the M7.4d hardening.
- **Severity:** RESOLVED (was MEDIUM). **Owner:** PM/Coder — done. _See OR-GATE-M7.4 below for the gate-of-record confirm on `db8ab5f`._

#### OR-GATE-M7.4 — gate-of-record (CI) on the pushed `4e31aed` (M7.4d HEAD) — [✅ RESOLVED 2026-07-01 — ci.yml VERIFIED GREEN on core 52824e9 (beat 43)] (NEW 2026-06-27, v9 hub; updated v10 beat-25)
- **RESOLVED 2026-07-01 (v13 hub, beat 43; reconciled into this ledger by the v14 hub):** `ci.yml` GREEN on pushed core `52824e9` — screenshot-verified (Build & Check 3m5s, Gradle 8.8, `check` ✅). Subsumes the per-commit CI-confirm for the whole ancestor chain (`4e31aed`/`9ec5949`/`eed477e`/AB-4/M7.5a/b). The `frontend.yml` first-trigger glance on `52824e9`+`2786526` is a standing Nick item (hub tracks), not an OR.
- **Status:** NARROWED. The **local `./gradlew check` is CONFIRMED GREEN** on the M7.4c working tree that became `db8ab5f` (Nick's terminal 2026-06-27: BUILD SUCCESSFUL, 149 tasks; he then committed + pushed core `69cc6e2..db8ab5f`). So the M7.4b/c/**d** deferred build gates are all **resolved locally** (Nick's terminals: `./gradlew check` 149 tasks green on the M7.4c tree → `db8ab5f`, and again on the M7.4d tree). The only residual is the **gate-of-record** formality: the `ci.yml` GitHub Actions run green on the pushed HEAD **`4e31aed`** (the M7.4d commit has now landed; `db8ab5f`→`4e31aed`). **De-risked (v10 beat-25):** `git ls-tree 4e31aed gradlew` = `100755` (executable) → the prior exit-126 failure mode is structurally cleared, and `ci.yml` runs `./gradlew check --no-daemon`; what remains is purely Nick confirming the Actions run is green on `4e31aed` — the standing b85e1ed/M7.4a pattern, "confirm CI green on the pushed commit, not just locally."
- **Severity:** LOW (the substantive gate — the full local `check` — is green; this is the CI-record formality).
- **Owner:** Nick (glance at the Actions tab for `db8ab5f`). **Resolution condition:** Nick confirms the `ci.yml` run is green on `db8ab5f` → record M7.4b/c CI-gated → close. M7.4d may be authored/routed now (the local gate clears §4b — done, ISSUE-READY beat 22).

#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
