<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
**[2026-06-18 — M7.1 LANDED GREEN + WUCP Phase 2 (Cowork PM)]:** **M7.1 (automation trigger/condition path) is gate-verified GREEN** — full `./gradlew check` PASS (149 tasks): compile + spotless + ArchUnit (LTD-11 / NO_DIRECT_TIME_ACCESS) + `assertAllowedModuleDependencies` + all count pins + serde round-trips. **First automation code landed.** **Gate-fix round 1 (2 failures, fixed in one pass):** (1) compile — `StandardTriggerEvaluator:307` sourced `automationId` from the removed `ActiveTimer` field → corrected to `entry.getKey().automationId()` (the `TimerKey` is the canonical source; field stays removed); (2) **module-graph — `:core:automation -> :config:configuration` is a forbidden edge** (caught by `assertAllowedModuleDependencies`; the doc'd layering is config→core, never core→config). Relocated: `AutomationConfigBridge` deleted, `requires/implementation config` dropped, the schema fragment moved to a config-free `AutomationSchema` holder; the config-loading + schema-registration wiring **rides app-bootstrap (composition root)** alongside the already-deferred `automation_engine` subscriber wiring. (`requires event.bus` stays — legal core→core.) Plus the pre-gate review-found `EventTypesTest` 55→62. **Deferred build gate: RESOLVED (GREEN) — none lingering.** **INSTRUCTION DEFECT logged + corrected:** the M7.1 instruction's FIX-07 directive to re-add `requires config` was wrong (banned at any scope; the §authoring exported-API check is necessary-not-sufficient vs the module-graph allow-list); instruction banner added, coder-lesson captured. **[REVIEW] deviations adjudicated — all ACCEPT:** 3 substrate gaps routed forward (slug-tombstone → future identity/device WU; first-class `Area` slug → device-model AMD-44 Stage-2; `Availability` asleep-vs-dead granularity → M7/integration per R-δ AX-8); 3 behavioral defaults accepted (multi-entity condition = all-of/empty→false — fold a Doc 07 §3.8 clarification when next touched at M7.2; `EventTrigger` payload-field matching → M7.2; device-subject duration `entityRef` flattening → M7.2 cleanup). **Wiring deferral VALIDATED** — the green build confirms the M7.1 core builds cleanly against injected deps; the subscriber + config wiring + the lifecycle wiring test ride app-bootstrap AB-3 (inbound dependency recorded in the decision record). **Next-WU = M7.2** (run/action/dispatch; C3 already landed `03f16dc`); per the Part-B ruling M7.2 builds **into** Doc 16 — do not freeze its action contract before Doc 16 Locks. **Commit:** the M7.1 code is its own homesynapse-core commit (green, self-contained); this closeout is the hivemind M7.1-WUCP batch.
**[2026-06-18 RECONCILED — Cowork PM, the CONVERGE/W25-rollover session (run late)]:** Spine HEADs now **core `03f16dc` / docs `f2e064d` / hivemind `b7a893e`** (all clean). **Preflight STALE → PASS** (currency lag only; zero CONFLICTED). Propagated this session: (1) **the energy/erasure interview gate is RETIRED** per the 2026-06-15 M5-D desk-research assessment (Track B energy→parked to the future energy milestone; Track A erasure trigger MOOT — M6 froze the write path correctly, shred is a recoverable post-MVP WU) → **the app-bootstrap lane is UN-PARKED**; (2) **core-review A/B/CONVERGE (2026-06-15)** logged — core sound-where-built but **not runnable (`main()` stub)**; two HIGH → Open Risks **OR-APP-AUTH (C1)** + **OR-RF-DECRYPT (C2)**, both detonating at app-bootstrap; (3) **W24→COMPLETE, W25→IN_PROGRESS, W26 scoped**; (4) **direction set by Nick (delegated):** app-bootstrap (C1+C2+C9) ∥ superior-automation Phase-1 design ∥ agent currency pass; M7.1 = ISSUED/awaiting-build, held as a ride-along (substantive HEAD still `1eddd9a`). The Track-4 line below stands for its own session; this supersedes it where they differ. Check 9 → benign-STALE after the agent pass until Nick's mirror sync.
**[2026-06-14 RECONCILED — Cowork PM, Track-4 forward-execution]:** Spine HEADs now **core `1eddd9a` / docs `c309123` / hivemind `7ea7992`** (all committed/pushed; core working tree clean). **M6.3 COMMITTED `1eddd9a` → M6 COMPLETE 4-of-4.** **Entry-gate row 4 CLOSED** — `homesynapse-core-docs/website/pages/config-superiority.md` APPROVED 2026-06-14 (PM quality gate, veto delegated by Nick; Increment-1 acceptance + guardrails verified; INV-CE-01 blockquote verbatim-confirmed; competitor-receipts + README items deferred as PUBLISH gates). The Lane-4 no-new-Core-instruction condition is **lifted**; **M7.1 cleared to issue** (interview-independent — row 3 gates only the parked app-bootstrap lane + the M6.3 restore-half R-α; the M6.3-vs-M7 ordering call is resolved by M6.3 landing first). The 2026-06-15 artifacts (Track-3 red-team audit, R-γ/R-δ briefs, R-δ/R-α assessments, app-bootstrap charter, M7.1 + C3 instructions, CC-1 spec, two-part core-review + CONVERGE synthesis) are landed + now registered in snapshot/backlog/coder-pointer. Crypto/app-bootstrap lane PARKED behind Nick's energy/erasure interviews; W24→COMPLETE rollover + W26 scoping still owned by the converge session. The stale spine HEADs in the 2026-06-13 line below are superseded. _This session's full record is appended under the session-records section below._
**[2026-06-13 RECONCILED — Cowork PM, parallel lane]:** The 2026-06-12 ratification IS complete and folded — **AMD-88..93 + B2 C8/C9 RATIFIED**, watermark **93**, invariants **163/47**, AMD-04 superseded by AMD-91 (docs `8002b7e`; the AMD files' own status lines + `Architecture_Invariants_v1.md` line 1066 confirm). **The "FOLD ABORTED … watermark stays AMD-87 … six AMDs stay PROPOSED" narration below is SUPERSEDED** — it captured the pre-ratification moment when the bundled review return was not yet on disk; the review subsequently landed and Nick ratified the block. Current spine HEADs: core `824d6ba` / docs `8002b7e` / hivemind `5e4943a`. W24→COMPLETE rollover NOT advanced here (calendar-bound; the 2026-06-14 W25-rollover converge session owns it). Open M6.3 evidence gate unchanged: the energy/erasure interviews.
last-verified: 2026-06-12 Pi-evidence session (evening) — **OQ-15-2 RESOLVED: encrypted set CONFIRMED `[identity, presence_personal]`; M6.3 gate 1-of-3 CLOSED; pi4-validation first exercise PASS (42 m, zero failures); index.lock flag DELETED by Nick (saga closed); M5-C Increment-1 prompt authored. Doc 15 untouched (expected-case rule). Sole open M6.3 evidence gate: the energy/erasure interviews.** _Prior:_ 2026-06-14-brief session (executed Jun 12 UTC) — **FOLD ABORTED AT STEP-0: the bundled AMD-88..93 + C8/C9 review return is NOT on disk** (`context/audits/` — absent both namespaces, three-repo sweep negative; only the dispatched prompt exists) **→ STOP per brief, hygiene-only: nothing folded, no mechanics, watermark stays AMD-87, six AMDs stay PROPOSED. Docs divergence: AMD-88..93 still UNTRACKED at `ed5cf91` (Nick: commit before dispatch/fold). index.lock.cleared FOURTH flag. Hivemind `ba1466d` reconciled. Rollover not passed. Check 9 STALE (expected). M7 research-vector advisory delivered (dry-run seam + AMD-91 state-hop question; no new research brief needed). Next: Nick lands the return (or runs the Cowork-fallback review in a FRESH separate conversation) → re-run the fold brief.** _Older masthead chain (the 06-13 AMD-block entry and earlier) → git ≤`58794d7` + `archive/pm-handoff-2026-06.md`._
-->

# PM Session Handoff

## 2026-06-18 — THE CONVERGE / W25-ROLLOVER SESSION (run late; reconcile + direction-set)

**Outcome: the overdue converge ran end to end; the spine is current and the next phase is set.** Step-0 preflight came back **STALE** (currency lag only — the converge session had never run; the 2026-06-15 M5-D assessment + core-review CONVERGE landed after the Track-4 note; hivemind 3 commits ahead at `b7a893e`) → **reconciled to PASS** in-session (snapshot masthead + Current-WU + Days-remaining; this handoff masthead + record; W24→COMPLETE / W25→IN_PROGRESS / W26 created; backlog currency note; Open Risks C1/C2). Trees verified clean at core `03f16dc` / docs `f2e064d` / hivemind `b7a893e`.

- **THE EVIDENCE GATE IS RETIRED (the headline).** The 2026-06-15 M5-D desk-research assessment (grade A−; EDPB 2025 CEF, NIST SP 800-88 r2, the 20-state privacy wave, FERC Order 2222 timelines all primary-verified; the clause-language follow-up confirms "verifiable crypto-erasure + tamper-evident log" is RARE as contracted language) stands the energy/erasure interviews **down as decision-grade**. The three-week bottleneck is gone — not by running the interviews, but by the analysis showing M6 already froze the write path in the regret-proof posture. **The app-bootstrap lane is UN-PARKED** (only R-γ + CC-1 remain as inputs).
- **CORE-REVIEW (A/B/CONVERGE) FOLDED IN.** Verdict: sound where built, **not yet runnable (`main()` stub)**. Backlog C1–C15; the two HIGH (**C1** unauth composition-root HTTP / INV-SE-02 unenforced; **C2** read-path decrypt-failure aborts the whole replay) both detonate at app-bootstrap and are logged under Open Risks. C3 fix-first already landed (`03f16dc`); C4 settles at M7.1/F5 scoping.
- **DIRECTION SET (Nick delegated the strategic call this session).** Prioritize **app-bootstrap (carry C1+C2+C9)** ∥ **superior-automation Phase-1 design** (federation/B3, honest-hybrid deploy, expressiveness-without-DSL — the differentiator; likely a NEW Locked design doc, his Phase-1 scope, greenlit for design) ∥ **agent currency pass**. M7.1 stays ISSUED + sound but is a **ride-along, not the focus**; M7.2/M7.3 build into the superiority design. Rationale recorded in the snapshot 2026-06-18 masthead note.
- **Authored this session (dispatch-ready):** app-bootstrap milestone brief + the automation-superiority Phase-1 design-session brief (`context/planning/` + `context/handoff/`); the coder + project-manager skill mastheads currency-passed.
- **Next:** (1) Nick commits the hivemind reconcile (message handed over). (2) Decide Phase-1 scope for the superiority design doc (greenlit for *design*; the Lock is his call) and dispatch that Cowork session. (3) Stand up the app-bootstrap milestone (the two HIGHs are Nick-decisions: bind posture for C1; the confidentiality-vs-availability contract for C2). (4) Re-run Nick's external mirror sync after the agent pass (clears Check 9). (5) M7.1 build whenever a Coder slot opens — after the superiority design confirms its trigger/condition contracts.

## 2026-06-14 — TRACK-4 FORWARD EXECUTION (the interview-independent lane: issue M7.1 · land C3 · fire dispatches)

**Outcome: the interview-independent forward lane executed end to end.** Step-0 preflight came back **STALE** (currency lag only — the spine HEADs, M6.3-commit-status, and the five 2026-06-15 artifact commits were unregistered; NOT conflicted) → **reconciled in-session to PASS** (snapshot masthead + Current-WU + session-log; backlog M6.3→DONE `1eddd9a` / M7.1→NEXT; coder next-unit→M7.1; this pm-handoff masthead; the 2026-06-15 artifacts registered). Tree verified current: core `1eddd9a` / docs `c309123` / hivemind `7ea7992`; all 8 charter artifacts present.

- **Row-4 veto — DELEGATED by Nick ("you decide the veto, then proceed") → PM ruled APPROVE.** Reviewed `config-superiority.md` against the governing criteria: Increment-1 acceptance met (reviewer-readable end-to-end); all three register guardrails hold (Matter absent; encryption non-leading/table-stakes; segment-appropriate); anti-requirements claimed only as absences; **INV-CE-01 blockquote verbatim-confirmed against `Architecture_Invariants_v1.md` §8**; the HA split-brain claim is grounded in our own §8 "Addressed failure mode" text; encryption metrics match the OQ-15-2 disposition exactly; the M6.3 tense-truth gate is now CLEARED (M6.3 shipped); 4 self-review defects already fixed. **Residual items are PUBLISH gates, not row-4 gates** (competitor-receipt verification — ADR-0010/issue numbers; the README open items — framework/works-with/install) → recorded on the page + tracked, NOT blocking. **Entry-gate row 4 CLOSED**; Lane-4 no-new-Core-instruction condition lifted.
- **M7.1 ISSUED** (`context/instructions/2026-06-15_M7.1_trigger-condition-path_coding-instruction.md`). At-issue verification (subagent-assisted): P2 manifest counts **+7 EventTypes / +8 records / +8 EventCategoryMapping** independently re-derived against AMD-92 rows 1/3/11–16/19 and confirmed EXACT (the +7-vs-+8 gap = row 1 reshape [0/0/0] + row 19 record-against-existing-constant); construction-site sweep EMPTY; reshape regret-window holds. Finalized: Issue Gate flipped to ISSUED (row 4 closed; **row 3/interviews reframed as NOT gating M7.1** per the charter's interview-independence; the M6.3-vs-M7 ordering call is resolved by M6.3 landing first); added **SD-9 = R-δ AX-5 fail-closed-at-load** (first-class loading contract + `DefinitionLoadFailClosedTest`); carried AMD-52 codec explicit + the AX-8 reachability watch-out. All standing pins present (type-residency/FLATTEN, C1-interim no-publish, §4c Clock, LTD-11, red-team F7/C4).
- **C3 LANDED** (PM direct host edit per the instruction's authorization — doc/Javadoc only): `RunContext.java` Cascade-Governance Javadoc + `@param cascadeDepth` + `core/automation/MODULE_CONTEXT.md` (2 lines) now cite **AMD-91 supersedes AMD-04** (chain-membership cycle detection, `cascade_loop_detected`, field-swap flagged M7.2). Record UNCHANGED (Read-verified: 8 fields, `int cascadeDepth` at pos 7); `{@code}` not `{@link}` for the future `RunCausalChain` (the build-breaking trap avoided). **NEW deferred build gate** → Open Risks.
- **3 dispatches fired** (none competes for the Coder/critical path): **CC-1** bind-address spike → CORE/Coder; **R-γ** crypto-agility CHALLENGE → DOCS; **Key-Portability** brief (R-δ §5 #1, HIGH) — **AUTHORED this session** (`context/instructions/2026-06-15_Research_Key-Portability_Backup-Recovery_DOCS_Brief.md`) → DOCS. Registered in research-agenda (Track-3/4 addendum) + cross-agent note.
- **Nick-requested deliverable (mid-session): automation-engine superiority + safe-scaling research-vector proposal** authored at `context/planning/2026-06-14_automation-engine-superiority_research-vector_proposal.md` — 10 candidate vectors (correctness/trust, scale incl. B3 multi-site/enterprise federation, deployment-model incl. C1 honest-hybrid, expressiveness-without-DSL, observability), each mapped to a lock point + priority + sequencing (dispatch after M7.1/M7.2 land). Menu for Nick to approve/sequence; nothing dispatched without his go.

**PARKED (tracked, NOT advanced — per the charter):**
- **App-bootstrap charter FINALIZATION** (`context/planning/2026-06-15_app-bootstrap_charter.md`): authored-and-pending; its F2 (encrypted-scope width) + the milestone-placement call are resolved by the energy/erasure interviews + R-γ + CC-1. Post-interview follow-on session finalizes it.
- **M7.2-vs-app-bootstrap sequencing** (the ordering call) — post-interview; do not pre-empt.
- **W24→COMPLETE rollover + W26 scoping** — the converge session owns it (NOT done here; only Track-4-lane currency was touched in W25).

**Standing escalations to Nick (carry; do not decide):** (1) the **retry-vs-REC-162 tension** (R-δ AX-5 — re-open as an M7.2 action-model design question; do NOT flip REC-162 silently); (2) the **AX-7 versioning/deprecation-policy** commitment (before users author definitions); (3) the **app-bootstrap AB-1 auth model**; (4) the **milestone-placement** call (interviews). Plus the **automation-superiority vectors** (esp. B3 federation / C1 hybrid likely imply a new Locked design doc — Nick's Phase-1 scope call).

**Open Risks delta:** **C3 deferred build gate — RESOLVED 2026-06-14** (`./gradlew :core:automation:compileJava :core:automation:spotlessCheck` GREEN, 27 tasks, against commit `03f16dc`; the `{@code}`-not-`{@link}` Javadoc is doclint-clean). M7.1's full `./gradlew check` becomes a deferred gate only once the Coder lands the code (not yet). No other open deferred gates (M6.1/M6.2/M6.3/M6.4/M5-A all RESOLVED).

**Next:** (1) Nick commits the three repos (messages handed over as `-F` files); uploads R-γ + Key-Portability to DOCS; routes CC-1 to Code; runs the C3 doclint gate. (2) Coder executes M7.1 (re-running the pre-verification + STOP-gates at the then-current HEAD). (3) The converge session does the W24→COMPLETE rollover + W26 scoping. (4) Post-interview: finalize the app-bootstrap charter + the ordering call. (5) On the M7.1 return: PM executes WUCP Phase 2 (review, MODULE_CONTEXT updates for automation+event-model, drift check, dual-skill sync, deferred-gate log; next-WU = M7.2 — only after C3, which is now landed).

## 2026-06-12 — THE M5-C INCREMENT-1 SESSION (the 2026-06-13 brief; CONTENT register; ran parallel to the R16-assessment session per the diverge architecture)

**M5-C Increment 1 DONE (drafted-awaiting-veto) — the W25 Lane-4 structural-gate deliverable is on disk: website skeleton + a reviewer-readable config-superiority page draft at `homesynapse-core-docs/website/`** (venue RULED by Nick in-session: the PM default, no site repo). Files: `README.md` (register rules — the three copy guardrails + anti-requirements + the D-5 Grid/Assure usage note — page map, open items incl. the framework decision), `index.md` (landing, mainstream register per D-4), **`pages/config-superiority.md` (THE deliverable — INV-CE-01 verbatim spine + the R13-assessment dossier + the OQ-15-2 trust point in guardrail-(b)-compliant non-headline placement; full review-only provenance appendix incl. publish gates)**, and Increment-2 stubs `pages/ledger-gap-dossier.md` / `pages/no-cloud-account.md` / `pages/explainability.md`.

- **Step -1 RULED: D-1..D-7 ALL ACCEPTED as drafted** (Nick, in-session, with recorded rationale — every item a constraint/hedge/reconciliation, none expands a claim). The Matter fence + segment rule bound this session's copy as RATIFIED, not provisional. **Fold mechanics into strategy files 1–4 deliberately NOT taken** (out of the Increment-1 brief's scope; Lane-6 work — files 1/2 markdown direct, files 3/4 via the docx skill; queued Next).
- **R16 M5-C-COPY bucket CONSUMED mid-session** (the brief's lands-first branch): the parallel session's merged disposition landed while drafting → REC-193/194/195 folded at stub/citation level. Correction made at the fold: **REC-194 (porch-light/B3 dossier) routes to the EXPLAINABILITY page**, not ledger-gap; REC-193 receipts (HA `stored_traces` default 5, Hubitat ~1 MB) cited; REC-195 carries its register-check note. The read-out's **"rendering, not data"** honest negative is registered as a binding copy guardrail in `website/README.md`.
- **Self-review (content register, claim-by-claim):** 4 defects found + fixed in own draft — a forward-state "dashboard" claim (Doc 13 UI unbuilt), an untraceable "lost years of configuration" line, "0.12%" understated as "a tenth of one percent", and a missing M6.3 tense-truth publish gate on the encryption-at-rest sentence (at-rest payload encryption is ratified design, implementation is M6.3 — publish-gate flags added). Guardrail audit clean: Matter absent from all copy (fence trivially satisfied); encryption non-headline with exact figures only; segment rule held both registers; anti-reqs clean; Grid/Assure absent.
- **Entry-gate row 4 → drafted-awaiting-veto.** The DONE-when is met on disk + the review pointer is delivered (`homesynapse-core-docs/website/pages/config-superiority.md`); **Nick's APPROVE flips row 4 — and the Lane-4 no-new-Core-instruction condition lifts at that flip** (with rows 1/2/5 closed, M7.1 then waits only on row 3's ordering call).
- **Step-0 record:** HEADs pinned-exact at start (hivemind `9ffed63` / core `01841ba` / docs `d7ea212`); preflight STALE (Check 9 only — the known one-file state); truncated-tail artifact ACTIVE (host file tools authoritative for every write; VM git quarantined; prompt archive via plain `mv`). Consumed session prompt → `handoff/archive/`. Shared-file footprint kept to single-anchor appends — **the Sunday converge session owns the three-lane synthesis.**
- **Next:** (1) Nick reads the page draft → verdict (APPROVE flips row 4); (2) hivemind + docs commits (messages in the session report; sequence after/with the R16-assessment session's handed-over commits); (3) **Increment 2 queued:** ledger-gap + explainability full drafts (R16 receipts now on disk), no-cloud-account, REC-174/175/176 pages, the works-with/landing decisions, and the D-1..D-7 fold mechanics (ruled ACCEPTED, fold pending).

## 2026-06-12 — THE R16-ASSESSMENT SESSION (the 2026-06-14 brief, input gate satisfied early; fresh conversation per discipline)

**R16 cycle COMPLETE — both returns assessed (serialized, B FIRST per the fold order), zero discards: 16-B grade A (`context/assessments/2026-06-12_Research_16B_PM_Assessment.md`), 16-A grade A− (`…16A_PM_Assessment.md`); merged disposition `context/planning/2026-06-12_R16_output-contract_merged-disposition.md` (authoritative bucket table, REC-186–215, high-water now 215); the gate-free deliverable `scripts/dev/OUTPUT_CONVENTIONS.md` DRAFTED in core (9 rules; pi-health = reference implementation; REC-204 retrofit list embedded).** Verification ledger: every §0 quote-back verbatim in both returns and every embed re-verified to ground (scripts byte-exact at `01841ba` via git objects; Doc 07 §11.2's 14 names exact; `CausalContext` javadoc + `com.homesynapse.event` module-info exact at `7c73c91`; Register C exact vs DAS §1.1; LTD-15 MDC fields/rotation/no-OTel verbatim — and LTD-15 says "JSON lines" *explicitly*, strengthening B's case; INV-TO-04 verbatim; Doc 06 `config_error` machinery confirmed). REC-204 (diagnostics→stdout) re-confirmed TRUE: zero `>&2` in pi-health.sh AND pi4-validation.sh AND the OQ-15-2 bench driver. REC-214 verified TRUE and understated (Doc 11 §3.5: 10 rows, ≥3 prefix styles). External spot-checks 100%: JEP 506 final-in-JDK-25 incl. the `ScopedValue.orElse` no-null change; OTel semconv v1.41.0/v1.41.1 exact; Anthropic 25k-token quote verbatim; HA WTH 219488 exact; `stored_traces` default 5 exact; Hubitat ~1 MB exact. The seeded `automation.cascade.depth_exceeded` hazard did NOT propagate (grep-verified; the §4 adjudication rule was moot). **PM adjudications (the disagreement register):** B-211 corrected against LTD-01 — Java 21 is locked, so the M12 MDC mechanism is explicit copy (TaskDecorator-style), Scoped Values recorded as JDK-trajectory-conditional only; B-209's auto-emit-on-non-TTY clause dropped (contradicts B's own REC-202 porcelain doctrine); A-196 re-routed DOC-11-CURRENCY→M12 folded into A-190 (Doc 11 has no Elm text to annotate); A-195 single-bucketed (M5-C only). **Disposition prominence: 16-A's honest negative — the B3 risk is data-without-register** (rendering register thinner than data machinery; routed via A-190/A-191/A-194). Mechanics: raw returns REHOMED to docs `research/returns/2026-06-12_Research_16{A,B}_*.md` (md5-identical to the committed hivemind copies → hivemind working tree now shows their deletion, intended); both briefs → `instructions/archive/`; consumed session prompt → `handoff/archive/`; agenda addendum heading → ASSESSED; snapshot masthead + session-log row appended. **M5-C-COPY bucket (A-193/194/195) routes to M5-C Increment 1** — its prompt is still unarchived (= not yet run), and the refreshed prompt consumes the bucket; if it turns out to have run, the bucket waits for Increment 2 (NO retro-injection). **Step-0 record:** hivemind `9ffed63` reconciled (exactly the one expected intake commit); core `01841ba` / docs `d7ea212` unchanged; no index.lock anywhere; preflight STALE (Check 9 only — the known one-file state); truncated-tail artifact ACTIVE again (host tails verified intact; VM git quarantined; all script verification via git objects per environment-model §5). **NEW FLAG → Nick: the docs repo working tree carries staged deletions + same-path untracked re-adds for 3 files** (the R15 raw return + both sqlite-wal spike files) — a host-side mid-operation signature (partially matches the standing spike-dir `git rm` item, but the R15 return's inclusion is unexplained); PM did not touch it; reconcile before/with the docs commit that picks up the 2 new R16 return files. **Next:** (1) Nick: hivemind commit + core commit (separate; messages handed over in the session report — both `!`-free, plain `-m` safe) + docs additions ride his next docs commit; (2) M5-C Increment 1 (now also carries the R16 M5-C bucket); (3) the Sunday converge session (W25 rollover/W26 scoping) now has the full R16 result to synthesize; (4) M12, whenever scoped, picks up its 8 obligations from the disposition §2b; (5) the REC-204 stderr retrofit of pi-health/pi4-validation/driver is a small follow-up Nick can fold into the F1/F3 pi4-validation fix commit already queued advisory.

## 2026-06-12 — R16 RETURNS INTAKE (same conversation as the Pi-evidence session, late; intake-grade validation — the FULL assessment stays routed to the fresh session per discipline)

**Both R16 returns are ON DISK, renamed to convention, intake-VALIDATED:** `Research_16A_..._RETURN.md` (REC-186–200; HA/Hubitat/SmartThings/Homey pain corpus; the "Month of WTH" porch-light evidence = the B3 dossier spine; 3 REJECTs incl. emoji-as-semantics) + `Research_16B_..._RETURN.md` (REC-201–215; the 10-rule agent-consumer contract; ONE real defect found — diagnostics→stdout, REC-204, **confirmed TRUE at intake against the shipped helpers**; MDC-on-virtual-threads M12 pin + spike, REC-211). Intake findings: §0 quote-backs VERBATIM (byte-checked against the in-session script reads at `01841ba`); both connector-blind-declared with honest gap lists; **the seeded `automation.cascade.depth_exceeded` ground-precision hazard did NOT propagate** (both returns used the real 14); cross-return coherence clean (A's SCRIPT-STANDARD pair ⊂ B's REC-201–209; REJECT sets complementary; register fences held). Intake observations embedded in the assessment prompt — the assessor verifies independently. **The going-forward architecture (Nick-directed, diverge→converge):** DIVERGE = two parallel fresh conversations, prompts on disk (`2026-06-14_R16-returns_assessment_session_prompt.md` [input gate now SATISFIED] + `2026-06-13_M5-C_Increment-1_session_prompt.md` [pinned-state refreshed; consumes the R16 M5-C-COPY bucket if the assessment lands first]); CONVERGE = `2026-06-14_W25-rollover_W26-scoping_synthesis_session_prompt.md` (NEW — calendar-bound Sun Jun 14: W24→COMPLETE scorecard, the M6.3-vs-M7 ordering-call decision package for Nick, W26 scoping, the designated governance window [backlog compression + advisory rulings], three-lane state synthesis). **Next:** Nick commits the intake (message handed over) → dispatches the two diverge sessions in any order → the converge session Sunday.

## 2026-06-12 — THE PI-EVIDENCE SESSION: OQ-15-2 RESOLVED — encrypted set CONFIRMED; M6.3 gate 1-of-3 CLOSED (2026-06-13 Pi-evidence brief; executed Jun 12 UTC, evening; the guided evidence run — PM guided, Nick executed every on-device command)

**THE M6-CRITICAL-PATH UNLOCK, three weeks slipped, now DONE: OQ-15-2 RESOLVED 2026-06-12 — both candidates ENCRYPT-ON-WRITE; `encrypted_scopes` default CONFIRMED `[identity, presence_personal]`; no fallback; no STOP-escalate.** §8 filled verbatim in the spec; disposition `context/assessments/2026-06-12_OQ-15-2_disposition_encrypted-scopes-CONFIRMED.md`; raw pastes + the zero-dep harness archived at `context/assessments/oq-15-2-harness/`. Gate numbers: G1 at the REAL payload distribution (44/82 B — below the §10 band, reported per §4) p50 2.82–4.46 µs / p99 3.28–4.61 µs → 13–37× margins, holds through the 1 KB outlier; G2 floor PASS by composition (D1-spike ceiling 24,473 ev/s = 49× the floor; encryption tax ≤0.12% of one core at derived R_C; the 42-min Pi IT suite GREEN same evening at current HEAD); G3 no flag. Two reproducing runs (pre/post-reboot) agree ±0.5% on every p50 — 6 warm forks + 2 cold forks total.

- **Step-0:** preflight STALE (Check 9 only — the known one-file state); SHAs reconciled into the snapshot masthead (hivemind chain `988ea4d`→`36bf324` [straggler CONFIRMED COMMITTED]→`7d23829`→`723e0d9` R16; core `01841ba` scripts-only pair, substantive HEAD stays `7c73c91`; docs `d7ea212` exact). Truncated-tail artifact ACTIVE again (host tails verified intact; VM git quarantined). **`index.lock.cleared-by-cowork-2026-06-12` DELETED BY NICK this session — the six-flag saga is CLOSED.**
- **Readiness:** pi-health.sh READINESS:PASS fully adjudicated (full output pasted; throttle 0x0 incl. since-boot, NTP ✓, noatime ✓, swap 0, Corretto 21.0.10, disk 1%); sole WARN = no on-Pi repo at `~/homesynapse-core` (intentional, rsync model). Zero environment drift found after weeks idle.
- **pi4-validation.sh FIRST EXERCISE: PASS** — manual-equivalent (tar-pipe deploy + remote gradle; BUILD SUCCESSFUL 42 m 5 s, 56 tasks, ZERO failures — BurstLoad/HeapBudget/Pi4Sustained 10-min/Pi4D1Spike/CrashRecovery, the suite's first-ever on-device run).
- **Methodology rulings (veto-or-default, defaults applied, all recorded in §8 + the disposition):** R1 hardware authority (Pi-5 NON-AUTHORITATIVE per spec §3.D → conditional-accept at ≥10× margins, met with 13×+; 3× A72-derating analysis green; NOTE: no evidence in the lane has EVER touched Pi-4 silicon — the D1 spike was also this Pi 5) · R2 JMH→zero-dep standalone harness (no JMH in the catalog; SampleTime-equivalent protocol) · R3 §3.B rig unbuildable pre-M6.3 (the E2 bridge is held-not-consumed — the spec's circularity) → G2 = measured baseline + analytic composition at derived R_C (identity ≈0, no event type in source; presence 60 ev/s burst ceiling, Doc 08 transition-driven basis). PM re-order ruling: bench BEFORE validation (thermal hygiene) — vindicated.
- **Findings (advisory queue):** **F1+F3 — pi4-validation.sh cannot run as written from Nick's host** (default `PI_HOST=hs-dev-1` ≠ the working `pi` alias; reachability probe lacks `BatchMode=yes`; **Git Bash has no rsync** — Step 3 hard-fails); one script-fix commit recommended (alias default + BatchMode + tar-pipe fallback). **F4 — intrinsic toggles are DIAGNOSTIC VM options** (JDK 11+): `-XX:+UnlockDiagnosticVMOptions` must precede them or the JVM refuses to start; driver v1 died silently (suppressed stderr + pipefail), v2 hardened (JVM preflight + diagnostics-never-kill-payload) — reproduced and fix-verified in-sandbox before re-issue. **Cold-start finding** (M6.3 scoping input): first ~100 encrypts after JVM start cost 80–700 µs (first op ~2–3 ms) — C2 compiling the software-AES path; consider a JacksonWarmup-style cipher warmup. **Optional Pi-4 confirmation run** queued (advisory; re-runs the spec §6 rule on authoritative silicon if Nick ever wants it). **Corpus-dumper reminder: Nick deletes `CorpusDumpOQ152Test.java` from the core working tree BEFORE committing** (plus untracked `pi4-validation-*` artifacts, keep-or-delete).
- **Consequence propagation (all applied):** spec status → RESOLVED + §8 filled · disposition note authored · **Doc 15: NO EDIT (the expected-case rule — default confirmed; the optional one-line §9/§15.2 currency note is Nick's hygiene call, the only sanctioned touch)** · C9 §3.4 energy staging item → RECONCILED/CLOSED (perf would not have forced energy out; MVP membership stays plaintext on Doc 15 §3.4 PII grounds; §14 post-MVP extension now has its cost input) · M6.3 gate → 1-of-3 in snapshot/Current_State/W25/charter/coder-handoff pointer · M7 entry-gate row 3 → microbench half DELIVERED (interviews remain, Nick-paced) · W25 Lane-2(a) → DONE 5 days early.
- **Stretch NOT taken** (the separate 60-min soak — the 42-min suite already soaked the device tonight; the never-run soak item stays queued). **M5-C Increment 1 session prompt AUTHORED** (row 4 — `context/handoff/2026-06-13_M5-C_Increment-1_session_prompt.md`; runnable as a parallel content-register Cowork conversation; D-1..D-7 vetoes ruled at its start). Consumed Pi-evidence prompt archived. **Open Risks: unchanged** — no deferred build gates (core untouched; the only core-tree artifact is the throwaway corpus dumper Nick deletes).
- **Next:** (1) Nick: hivemind commit (message handed over) + spine re-upload (Current_State changed) + the corpus-dumper deletion; (2) the energy/erasure interviews — now the SOLE open evidence gate for M6.3 AND row 3; (3) M5-C Increment 1 (the prompt is ready — the principal non-evidence blocker); (4) at W25/W26 boundary: the M6.3-vs-M7 ordering call once interviews land.
- **SAME-EVENING ADDENDUM (post-`58794d7` push):** hot-path hygiene rotation executed — snapshot 107K→~59K (M3.7/M4-era WU blocks + pre-June session-log rows + the ratification masthead-prior → `status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-12.md`) + pm-handoff 70K→17K (pre-R16 session records + RESOLVED gate blocks → `archive/pm-handoff-2026-06.md`); git-object-sourced from `58794d7` per environment-model §5, all four tails host-verified (the mount was actively serving pm-handoff 11K short during the pass — the artifact's third confirmed sighting this session; one pre-existing truncated session-log row [M3.5b "19 files"] verified original-to-git, copied faithfully). `58794d7` reconciled into the masthead + the Pi-evidence session-log row added. **R16-returns assessment session prompt AUTHORED** (`2026-06-14_R16-returns_assessment_session_prompt.md` — mandatory-input gated, B-first serialized assessment, carries the `cascade_depth_exceeded` ground-precision adjudication rule + the gate-free `OUTPUT_CONVENTIONS.md` deliverable); M5-C prompt pinned-state refreshed. Backlog DONE-row compression deliberately NOT taken (stays queued for a governance window). Runway: M5-C (now, parallel) → R16 assessment (when returns land) → Sun rollover/W26 scoping → interviews (the bottleneck) → M6.3-or-M7.1 per the ordering call.

## 2026-06-12 — R16 brief-authoring session (2026-06-13 brief; executed Jun 12 UTC; ran PARALLEL to the Pi-evidence session — minimal-footprint closeout per the guardrail; appended above the ratification record without touching it)

**R16 cycle AUTHORED, READY TO DISPATCH (Nick veto-or-default):** two briefs on the R13–R15 pattern — **R16-A Logging & Observability UX: Market and Human Factors** (REC-186–200, UX/market register) + **R16-B LLM-Legible Output Engineering** (REC-201–215, engineering register), both → the DOCS Project in SEPARATE fresh conversations, web search required; quote-back set = the pi-health idiom block + the Doc 07 §11.2 event-name list (all 14) + the DAS Register-C paragraph (16-B adds `CausalContext` + the `com.homesynapse.event` module-info verbatim); cycle buckets SCRIPT-STANDARD / M12-INPUT / M10-M13-INPUT / DOC-11-CURRENCY / M5-C-COPY / FUTURE / REJECT; both self-reviewed ×2 (caught + fixed: inline glosses adulterating the verbatim READINESS embed; missing freshness-horizon citation); research-agenda R16 addendum appended (single-anchor, above the spike tracker). Step-0: shas matched PINNED STATE exactly (core `01841ba` / docs `d7ea212` / hivemind `7d23829`, no Pi-session advances observed); preflight **STALE (Check 9 only — the pinned one-file state, answer-key match)**; `index.lock.cleared-by-cowork-2026-06-12` STILL PRESENT → **FIFTH flag** (Nick deletes); consumed R16 session prompt archived → `handoff/archive/`; the Pi-evidence session had written nothing to the shared files at every read this session (guardrail honored: single-anchor appends only, no reflow, no masthead/heading rewrites — the Pi session's closeout owns rewrite rights while both are open). Ground-precision note for the assessment pass: the R16 session prompt's `automation.cascade.depth_exceeded` exemplar is not a Doc 07 §11.2 table name — the briefs embed the real 14 (`automation.run.started` class); the cascade diagnostic is the *event* `cascade_depth_exceeded` (§3.7.1/AMD-92 ground). Files: `context/instructions/Research_16A_Logging_Observability_UX_Market_Human_Factors_Brief.md` + `Research_16B_LLM_Legible_Output_Engineering_Brief.md`. **Next:** Nick dispatches both → returns saved to `context/instructions/` → archived to docs `research/returns/` → PM serialized assessment (fold B first: engineering constrains, human evidence prioritizes) → merged disposition → the `scripts/dev/OUTPUT_CONVENTIONS.md` draft (gate-free) + M12 evidence base + M10/M13 parking + Doc-11 currency queue + M5-C copy. Hivemind commit message handed over in the session report. Documents-only: core and docs untouched.

## Rotated session records

**Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; git-object-sourced from `58794d7`):** the ratification record (Return ASSESSED → RATIFIED → mechanics), the superseded fold-abort record, the M7-AMD-block record, the M6.4-WUCP record + the M6.2-era fragment, and the RESOLVED/CLOSED build-gate blocks from Open Risks → `archive/pm-handoff-2026-06.md`. Earlier rotations per the markers preserved there. Full history in git.

## Open Risks


#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [HIGH; latent until `main()` wired] (NEW 2026-06-18)
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [HIGH; gates crypto-shred] (NEW 2026-06-18)
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [write-path RESOLVED 2026-06-13; restore-half OPEN → R-α] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
