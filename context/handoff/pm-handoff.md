<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-01 (v13 hub, beat 41 — launched; the v12 arc independently re-verified [holds, with amendments]; preflight STALE → reconciled; core **dbb0109** / docs **75d0345** / hivemind **89da546**+beat-41 / bench **5ceff3b** / skills **5bc78bc**+carry). PRIOR STAMP: 2026-06-28 (v11 hub, beat 29 — LAUNCHED; preflight STALE-benign → reconciled to committed reality) -- core `9ec5949` (M7.5a COMMITTED; the M7.4 arc COMPLETE) / docs `75d0345` (AMD-95 RATIFIED; Doc 17 LOCKED; w/m 95; 170/50) / hivemind `c3a6f8b` (the beat-28 crystallization COMMITTED — bench decision-record + research dispatch + v11 prompt + v10 archive) / bench `4c4496d` (`nexsys-bench`, the 5th repo — inited + scaffolded + pushed). Beats 13→27 retained above. Open Risks reconciled (OR-M7-WIRING RESOLVED; OR-GATE-M7.4 narrowed to the CI-confirm on the pushed `4e31aed`). Prior stamp: 2026-06-22 (v5-hub) core `1b0b6c9` / docs `e47f01e` / hivemind `04c0a54`; older beats + stale HTML-masthead prose rotated -> archive/pm-handoff-rotated-2026-06-22.md.
-->

# PM Session Handoff

## 2026-07-02 (beat 46 — v13 hub) — both lane returns AUDITED + ADJUDICATED; four rulings landed; M7.5c-a issued; M9.1 issue-ready (corrected)

**M9-authoring return: AUDIT PASS-WITH-ONE-CORRECTION.** Ten-row independent spot-audit against core `52824e9`: nine PASS verbatim (incl. the M7.4d javadoc M9-mandate at L127-128, both module-info embeds, ctor arity, the WithinTolerance stub, count-pin tests, module-graph allow-list). ONE REFUTED: WU-M9.1 §A31/B7 claimed "NO ConfigurationAccess implementation exists anywhere" — false: `ScopedConfigurationAccess` (production, config module, package-private, `(String integrationType, ConfigModel)`) + `InMemoryConfigAccess` (testFixtures) exist. **Correction FOLDED** into WU-M9.1 (A31+B7 rows) + the instruction (Files-table row + DP-12): preferred path = expose the existing scoped impl via a small public config-module factory (per-integration scoping arrives free; zero JPMS change), fallback = the original lifecycle adapter, Coder pushback invited. The lane's zero-fabrication bar held on signatures; the absence-proof failed — noted for pm-lessons (grep-absence claims need a second grep form).

**FE-1 return: AUDIT PASS; both drifts adjudicated on the truth hierarchy — OPPOSITE rulings, each grounded:** DRIFT-1 (bare `/internal/*`): no Locked doc backs the bare shape; it breaks the freeze's own §0 poll anchor → **Core conforms** (Nick: micro-WU FIRST). DRIFT-2 (URI problem-type): Locked Doc 09 §3.8 + `ProblemType.java` AGREE on the URI form → **the freeze mis-transcribed → v1.1 → v1.1.1** (folded into the freeze record: URI form + clients-key-on-slug-suffix + the A4/A5 conformance ruling note). Also verified: `npm run verify` 45/45 green claim, bundle 56.9/100, write-isolation held, nothing patched client-side against drifted shapes (exemplary freeze discipline). FE lane's env-note (mount lag >30 min) queued for the env-model at the next process pass.

**Nick's four rulings (2026-07-02):** DP-A ratify-all (split M9.1→M9.4 + DP-C/D/E/F accepted; backlog M9 row replacement rides the next backlog edit) · DP-B deterministic identity derivation = permanent policy (one-line governance note queued for the next docs-touching pass, with the DP-C/DP-D V1-subset-of-Doc-08 recording) · DRIFT-1 conform-Core-micro-WU-first · DRIFT-2 amend-freeze-to-URI (done).

**Issued/next:** **M7.5c-a** (internal-read envelope conformance — `context/instructions/2026-07-02_M7.5c-a_*.md`, ISSUE-READY, sequenced BEFORE M9.1) → **M9.1** (corrected instruction, issue on M7.5c-a's WUCP close) → M9.2–M9.4 authored just-in-time from the return's §3 charter. **FE-1b** (drift fold: slug-suffix matching + smoke re-run) launches AFTER M7.5c-a lands. **Nick, host-side:** (1) commit core `web-ui/dashboard/**` + delete the pairing-token artifact per Core's log instruction (the FE lane's step); (2) commit hivemind (this beat + lane files + corrections + freeze v1.1.1 + the M7.5c-a instruction); (3) route M7.5c-a to the Coder. Carried: Dependabot triage · A4.5 Doc 15 note · Node-20 bumps · phantom-gated rotation · the DP-B/DP-C/DP-D governance notes at the next docs pass · lane drift-findings #1/2/5/6 (MODULE_CONTEXT staleness + Doc 02 §3.9 residency) fold at M9.x WUCPs.

## 2026-07-01 (beat 45 — v13 hub) — nexsys-frontend skill currency + dual-surface pass (nexsys-skills repo)

The frontend skill (authored 06-27, pre-build-arc) brought current: the live-vs-mocked endpoint partition → re-derive-at-preflight rule (M7.5a/b hero endpoints are REAL; /events+/health await M7.5c); the HA-2026.7 watch RESOLVED (verified 2026-07-01: not-triggered traces still absent from stable — both moat halves hold; watch continues); NEW explainability §2b = the four measured AMD-97 rendering behaviors (values pointed at Doc 08 §3.6 + corpus, never hardcoded); Doc 08 added to the SKILL.md truth: block; CLAUDE.md gains a "Runtime surfaces" section (Cowork deferred-gate norm vs Claude Code run-verify-yourself). Verified zero stale phrases remain; the skill was born pointer-not-copy (aged far better than coder/PM). **Nick:** nexsys-skills commit (`_scratch/2026-07-01_skills_frontend-currency_commit-msg.txt`) + one more mirror sync (this skill's mirror is now behind) — then launch the lanes. The FE-1 lane benefits directly (it loads this skill).

## 2026-07-01 (beat 44 — v13 hub) — skill-source precision pass; Check 9 STALE pending mirror sync

Pointer-not-copy applied to both skill mastheads + the PM Mode-3 state narrative; preflight 10/11 strays fixed; coder-handoff frontmatter restamped; the survey agent's "CLAUDE.md module-count wrong" claim verified FALSE (19 is correct — no edit). Full record: snapshot beat-44 (pointer, not copy). **Nick:** beat-44 hivemind commit (`_scratch/2026-07-01_hivemind_beat-44_commit-msg.txt`) → **mirror sync (Check 9)** → launch the two lanes. Deferred: mega-line overhaul rides the phantom-gated rotation; coder/references content refresh post-M9.

## 2026-07-01 (beat 43 — v13 hub) — fleet committed+pushed (5 repos); ci.yml green on 52824e9; two lanes authored + launch-ready

Fleet: docs `1509b34` / hivemind `cea7ae1` (beats 41+42; message says 41 — cosmetic) / core `52824e9` / skills `846fe56` / bench `5ceff3b`. Bench clean in Nick's terminal → phantom diagnosis confirmed. `ci.yml` GREEN on `52824e9` (verified via screenshot — subsumes the OR-GATE ancestor chain). **Open items:** (1) `frontend.yml` run on `52824e9` — Nick's Actions glance (path-filtered to `web-ui/dashboard/**`, so 52824e9 is its FIRST trigger; dbb0109 couldn't fire it); (2) Dependabot 5-vuln triage (1 crit/1 high/3 mod — shipped-vs-dev per beat-33 tree); (3) NEW residual: Node-20 deprecation on checkout/setup-java/setup-gradle@v4 — bump action majors at a convenient window; (4) the A4.5 Doc 15 §3.4/§6 note (Nick, still); (5) phantom-gated: spine rotation + Check-8 archival. **Lanes authored:** M9-authoring (`context/handoff/2026-07-01_M9-authoring-lane_session_prompt.md`) + FE-1 live-integration (`context/handoff/2026-07-01_frontend-live-integration_FE-1_session_prompt.md`) — both write-isolated, both return to this hub. **Next:** beat-43 commit → launch both → hub audits the M9 instruction return → Nick issues to Coder → 72 h soak → install-smoke → mid-Aug go/no-go.

## 2026-07-01 (beat 42 — v13 hub) — governance queue RATIFIED + FOLDED (AMD-96, AMD-97, INV-ES-09); watermark AMD-97; invariants 172/51; M9 UNBLOCKED

Nick's four rulings (v13 decision pass) executed same-session, host-side, every edit grep-verified, the invariant total re-derived from the §17 table (=172 exactly). Details in the snapshot beat-42 entry (pointer, not copy). Docs repo now carries the uncommitted fold set: Doc 02 (§3.6/§3.8/§3.10), Doc 08 (§3.3/§3.5/§3.6/§3.7), Doc 16 (§4 note), the register (INV-ES-09 §2 + §51 + §17 rows + §0.3 + total/watermark), nav-index (currency incl. its stale-at-AMD-87 watermark line), AMD-96 status→RATIFIED, NEW AMD-97 file. Hivemind: the three governance instruction files closed (RATIFIED/APPLIED). **Nick, host-side (updated command file `_scratch/2026-07-01_host-side_commands.md`):** the docs commit (message file staged) + the beat-41/42 hivemind commit + the core frontend.yml sync + the skills carry + the bench `git status` sanity check + the Actions/Dependabot glances. **Next:** (a) **author M9** — fresh-context session against the measured corpus (`nexsys-bench` 5ceff3b) + ratified AMD-97 + the beat-41 acceptance additions (Configure-Reporting explicit step; reporting-config parity/re-measure; latency reference point; occupancy binding; dedup; tolerate-not-require IAS; firmware-version-aware); (b) **launch FE-1 live-integration lane** (M9-independent) routing the E5 rendering semantics (CT ~7–8 s legit-slow pending→confirmed; idempotent may never visually confirm without readback; effect/identify immediate honest UNCONFIRMED; superseded expire); (c) then 72 h soak → install-smoke → the mid-Aug go/no-go. Deferred (phantom-gated): the spine beat-block rotation + Check-8 archival.

## 2026-07-01 (beat 41 — v13 hub) — v13 launched; v12 arc re-verified (holds, with amendments); spine reconciled; governance queue consolidation-ready

**Preflight = STALE, zero CONFLICTED** (the v12 arc never updated snapshot/pm-handoff; no W27 plan) → this beat is the retroactive reconciliation, written AFTER the §2 independent re-verification so it records verified fact. Full verdicts + evidence in the snapshot beat-41 masthead entry (pointer, not copy). Highlights: the moat/corpus/fixtures HOLD (timestamp-level traceability fixture↔corpus↔raw-diagnostics; genuine captures); D5 "M9 small" HOLDS for Wave-1 with named residues (reference-stack-conditioned latencies; Doc 08 §3.7 lacks an OccupancySensing default-reporting row; category-B/A untested → Wave-2 target); E1 verified against ratified D-OPEN-1 (v13+/7.4+); all AMD-CAND-1 + D2 review edits re-derived WARRANTED from the Locked text; frontend go-live claims confirmed in source.

**The consolidated governance queue (Nick rules; hub folds host-side, git-object-sourced):**
1. **AMD-96 ratify** — use the AMD-CAND-2/3 verbatim diffs (`context/instructions/2026-06-28_AMD-CAND-2-3_doc02-08-color-currency_staged-fold.md`) as its §2.A/§2.B edit text; fold E1 (baseline = measured 7.4.5.0/v13; v14 = batch contingency + reflash-re-anchor), E2 (auto-detect = VID:PID `10c4:ea60` + probe, never descriptor strings), E6 (read version at stack-init, registry unreliable) into §2.C. NEW: add the Doc 08 §3.7 OccupancySensing default-reporting row (the hero trigger's cluster is missing from the table).
2. **AMD-CAND-1 ratifies as AMD-97** (the AMD-96 number is taken) — fold the B1 review edits (enum `degradeRule`; state the field count [dispatch says "seven," lists 8]; NonFiringVerdict = distinct-vocabulary wording; numbering) + the measured values (5 Hue blocks + SNZB empty, from `nexsys-bench` corpus) + the E5 taxonomy split (by-report-UNCONFIRMABLE vs readback-BEST_EFFORT) + the never-false-CONFIRMED invariant (register as AMD-97-INV-01; no existing invariant covers it). **Unblocks M9.**
3. **D2 → INV-ES-09**, watermark-neutral (decision-record L44 authorization; SA/AC precedent), 170→171 re-derived, AIOT-INV-1 citation fix (drop the stale "pending registration at M7.4b"), text edit crediting the FSM + the per-subscriber guard.
4. Carried: the A4.5 Doc 15 §3.4/§6 note (Nick); OR-GATE CI-green Actions glances (now incl. `frontend.yml` on `dbb0109`); Dependabot 1-critical triage.

**M9 acceptance additions (fold into the M9 instruction when authored):** Configure-Reporting as an explicit acceptance step (the corpus's ON_CHANGE posture was ZHA-configured; fixture replay cannot catch its omission); reporting-config parity or re-measure under Doc 08 §3.7 defaults (Level min 5 s vs ZHA ~1 s; CT min 5 s vs ~10 s — the measured timeout bands are config-relative); pin the latency reference point (corpus on_off values are ACK→report; brightness/CT are command→report); occupancy-not-motion hero binding; ingestion dedup (SNZB ×2 reports); tolerate-not-require IAS; firmware-version-aware (v13+ band, version at stack-init).

**Environment/Open Risks:** the truncated-tail phantom is ACTIVE (VM read-side; confirmed on bench `iac/99-zigbee-coordinator.rules` AND on bench's binary `.git/index` — the VM "mass staged deletions + rename-to-`c` + `./` entries" status is a truncated index read, NOT real corruption; worktree verified byte-identical to HEAD host-side, all files). **Nick, host-side:** (a) verify `git status` in nexsys-bench is clean in your terminal (expected); (b) run the beat-41 hivemind commit (command file in `_scratch/`); (c) commit core's `web-ui/dashboard/ci/frontend.yml` (byte-identical sync of the activated root workflow — a missed v12 sync commit); (d) the `nexsys-skills` frontend-skill carry commit. §2 beat-block rotation + Check-8 cross-agent archival stay HELD until phantom quiescence. **Next Core slot: M9** (post-AMD-97 fold). FE-1 live-integration is M9-independent — can launch parallel.

## 2026-06-28 (beat 35 — v11 hub) — v12 hub prompt AUTHORED + the v11→v12 handoff

**The v11 conversation (the full arc: launch → M7.5a/b read-API core → Stream-B research → the mid-Aug go/no-go review → AB-4 cipher activation → the v11-arc cleanup) is handing off to a fresh-context v12 hub.**

- **v12 prompt:** `context/handoff/2026-06-28_PM-mission-control_v12_orchestrator_session_prompt.md` (supersedes v11). It carries the bench-reframed three-stream plan with **the bench as the critical path** + the **march to M9** (gated on the bench corpus + AMD-CAND-1; M7.5c the deferrable filler). Baseline: core `89f775d` (AB-4) / docs `75d0345` / hivemind `8f8beb1`+beat-35 / bench `2c0a33c`. DONE: the read-API differentiator core (M7.5a/b) + AB-4 (Seam-1 closed; the log encrypted-from-genesis).
- **The v11 prompt is archived** → `context/handoff/archive/`.
- **v12 first-act hygiene:** archive-v11 (done) + the **DEFERRED spine beat-block rotation** (the v8→v11 masthead beats → the snapshot/pm-handoff archives — HELD across the v11 arc by the truncated-tail phantom, which is ACTIVE again this beat; the v12 hub runs it once quiescent host-side). The §2 hygiene was correctly held all arc; it's a clean session-start task.
- **Carried for Nick:** the beat-35 commit (core `89f775d` already pushed); route AMD-CAND-1 + D2 to review (parallel with the bench); rule AMD-CAND-2/3 (commit-on-word); the A4.5 Doc 15 note; the OR-GATE + AB-4 CI-green Actions glance; the Dependabot 1-critical Security-tab glance; the `nexsys-frontend` skill commit + the **Check-9 mirror sync** (the v11-arc skill-currency fold); **bench first-light = the highest-leverage next block.**
- **Next:** launch v12 as a fresh Cowork conversation; its first act is grounding + the first-act hygiene.

## 2026-06-28 (beat 34 — v11 hub) — AB-4 DONE + GREEN + WUCP Phase 2 ACCEPT — the at-rest cipher is LIVE + the v11-arc cleanup

**Nick built + gate-greened AB-4 (`./gradlew check` 149 tasks). The hub ran WUCP Phase 2 = ACCEPT (source-verified against the diff) + the v11-arc cleanup.**

- **AB-4 source-verified ACCEPT:** the change set is **16 files + 1 new test, NO module-info / build.gradle / migration / event-model** → zero-mint (71/41/53, green ArchUnit pins), zero-edge, zero-DDL. A4.1 cipher activation (Main 6-arg ctor → M6.3 at-rest encryption LIVE for `[identity, presence_personal]`); A4.2 F1 (`v1` byte, single assemble-site, prepended + GCM-AAD-bound, unknown-byte → `UNKNOWN_ENVELOPE_VERSION` fail-closed — verified in `SqliteEventStore`); A4.3 F3 (`NonceConstruction{RANDOM_IV,COUNTER}` + `bindConstructionLocked` rejects cross-construction — verified in `StandardScopeKeyManager`); A4.4 F13b (`writeAtomicallyDurable`, Windows-scoped); A4.5 seam recorded. Phase-1 artifacts landed host-side (mount-lag check PASS; phantom quiescent this beat).
- **Deviations all ACCEPT:** the seam-widening `[INFO]` (PayloadCipher/ScopeKeyManager +`byte[] aad`, java.base-only, zero-edge — the "widen-the-seam-don't-infer" call); the F3 test rewrite `[REVIEW]` (the old test asserted now-forbidden cross-construction; production scopes disjoint); the declined F13a ctor-assertion `[Design point]` (would break AB-2's null-cipher fail-closed-read construction — write-time guard is correct); the deferred cipher-warmup `[Design point]`. **`[FLAG-NICK]`: the A4.5 Doc 15 §3.4/§6 note (backup-key seam + rotate-on-restore) is Nick's docs-repo landing** — the seam is recorded in cross-agent-notes.
- **The v11-arc cleanup (Nick's ask):** (1) **Skill currency** — folded the v11 PM lessons into `context/lessons/pm-lessons.md` (parallelize-independent-inputs; source-verify-against-the-diff WUCP; grounding-subagent-before-authoring; read-the-actual-src not just the top-level glob; staged-commit-on-word amendments; the read-API-subset-vs-Locked-doc recording pattern; the `_scratch` + `git commit -F` protocol); brought the `project-manager` + `coder` SKILL.md mastheads current to the v11 / M7.5b / AB-4 point. **Check-9 → STALE** (skill-source edits) — clears on Nick's external mirror sync. (2) **Spine rotation** — the v8/v9/v10-arc beat blocks rotated from the snapshot/pm-handoff hot path to their archives (token-economy; the phantom is quiescent → host-side rotation safe). (3) **Stale `_scratch`** — ~20 consumed `_commit-*.txt` files flagged for Nick to clear (deletion permission-gated).
- **Next Core slot = M9** (real Zigbee; hero on real silicon vs the bench corpus) — or M7.5c if Nick re-sequences. **The 3 pre-M9 inputs:** AB-4 ✅ · bench first-light (pending — Nick's hands + hub orchestration) · AMD-CAND-1 + D2 (route-ready, parallel). **Commit:** core AB-4 + hivemind beat-34 — messages in `_scratch/` (`git commit -F`). **Residuals:** OR-GATE-M7.4 + AB-4 CI-green; AMD-CAND-1/2/3 + D2 governance; Dependabot (the 1-critical shipped-vs-dev glance); the A4.5 Doc 15 note.

## 2026-06-28 (beat 33 — v11 hub) — AB-4 ISSUE-READY (cipher activation) + Nick's 3 sharpenings applied

**Nick committed M7.5b (core `eed477e`) + beat-32 (hivemind `77876ec`). The hub applied his 3 sharpenings and authored AB-4 (the ruled next Core slot, leapfrogging M7.5c).**

- **AB-4 ISSUE-READY** — `context/instructions/2026-06-28_AB-4_cipher-activation_coding-instruction.md`, authored from a source-verified grounding pass against `eed477e`. **Much smaller than the 2026-06-19 spec** (AB-1/AB-2/M6.3/the F13a write guard all landed since): A4.1 cipher activation (flip `Main` to the 6-arg `HomeSynapseCore` ctor → M6.3 at-rest encryption goes live for `[identity, presence_personal]` via the cipher-presence gate) + A4.2 F1 (the `v1` 1-byte envelope version discriminator, prefix-in-envelope + GCM AAD, zero-DDL) + A4.3 F3 (programmatic one-nonce-construction-per-scope — the GCM nonce-reuse guard, doc-only today) + A4.4 F13b (the nonce-counter dir-fsync fail-closed, **scoped past the deliberate Windows swallow**) + A4.5 the backup-key portability seam (document only; carry rotate-DEK-on-restore). **Zero-mint (71/41/53), zero module-edge, zero-DDL.** 3 `[Design point]`s: F1 siting, F13b scoping, the cipher cold-start warmup (OQ-15-2 — add vs defer). Honesty caveat baked in: F1's chain-coverage tamper-evidence is inert until `chain_hash` is live (the AAD downgrade-resistance is real now).
- **Sharpening 1 — AMD-CAND-1 routes NOW in parallel (the serialize slip fixed).** The dispatch gained a "Routing timing" section: it is a SCHEMA review; the research return's worked examples ground sufficiency; first-light POPULATES the slot, doesn't validate its SHAPE; the measured values fold at the RATIFY step (a ratify condition, never a reason to delay the review). Routes concurrently with the bench + D2.
- **Sharpening 2 — the frontend is SUBSTANTIALLY BUILT, not scaffold (material go/no-go correction).** The prior "scaffold" read globbed only top-level `src/` and was wrong. Source-verified: all hero views built (`WhyNotView` renders the M7.5b `NonFiringExplanation` exactly; `CausalChain` the M7.5a hero; `RunsView`/`AutomationsView`); the full API layer to the frozen `v1.1-2026-06-21` contract (`contract.ts`/`endpoints.ts`/`shapes.ts`); a **real transport already wired** (`index.ts` flips mock→real on `VITE_USE_MOCKS`, default real-in-prod; `realTransport` = fetch-over-loopback + AB-1 bearer + ETag). **Gate-2's render half is largely BUILT** (mock-backed, contract-conformant, real-transport-ready), at `b296e76` (no web-ui commits since — the Track-A first beat). **Residual:** the live-integration pass (point realTransport at the live M7.5a/b backend + verify the hero renders real log-derived data) + the lane's current staffing/velocity (the one human input still needed). Materially de-risks gate-2 vs the readiness review's pessimistic read.
- **Sharpening 3 — Dependabot 1-critical shipped-vs-dev triage.** Catalog deps are current-pinned (jackson 2.18.6 / javalin 6.7.0 / logback 1.5.32 / slf4j 2.0.17 / sqlite-jdbc 3.51.2 / snakeyaml-**engine** 2.9 [not classic SnakeYAML → the constructor-RCE class does NOT apply] / json-schema-validator 1.5.6). **Shipped-runtime:** jackson, javalin (+ Jetty transitives), logback, snakeyaml-engine, json-schema-validator, sqlite-jdbc. **Dev-only:** junit, assertj, archunit, awaitility, spotless, modules-graph. The 1 critical is most plausibly a **transitive of a shipped dep (Jetty via Javalin the prime suspect)** since the directs are current-pinned → a 30-sec Security-tab glance against this decision tree resolves it (dev list → deferred stands; shipped path → assess + bump). (The hub can't read the GitHub alert; the scope map makes the glance instant.)
- **Next (Nick):** route AB-4 → Coder; route AMD-CAND-1 + D2 to independent review (parallel with the bench); rule AMD-CAND-2/3 (fold on word); OR-GATE Actions check on `eed477e`+`4e31aed`+`9ec5949`; the Dependabot glance; bench first-light (say the word, Phase 0 command-by-command). Commit: hivemind beat-33 (message prepared in `_scratch/`). **Residuals unchanged.**

## 2026-06-28 (beat 32 — v11 hub) — M7.5b DONE + GREEN + WUCP Phase 2 ACCEPT — the read-API differentiator core is COMPLETE

**Nick ran the full gate (`./gradlew check` GREEN, 149 tasks) + the targeted compile/test, verified actually-green. The hub ran WUCP Phase 2 = ACCEPT, source-verifying against the git diff + host-side reads (code > report).**

- **Source-verified (the diff + host reads, not the completion report):** the change set is exactly **7 modified + 6 new, with NO module-info / NO event-model / NO build.gradle.kts** → zero-mint, **counts hold 71/41/53** (the green ArchUnit count-pins prove it; the coder-handoff independently confirms both module-infos + build.gradle.kts unchanged — M7.5b didn't even need the jackson edge). `ExplanationService` gains 2 additive methods; M7.5a's `listRuns`/`explainRun`/`over()` are **byte-intact** (read host-side — the large `StandardExplanationService` churn is the instructed shared-helper factoring + reorder, behavior-preserving + the unchanged M7.5a tests pass green). The `FakeExplanationService` fan-out landed. The new records + the `AutomationEndpointsTest` shape tests pin the frozen v1.1 §B3 shapes **field-for-field (camelCase)**. The verdict derivation is correct + exhaustive over `RunStatus` (no `default`) + honest on the EVALUATING/RUNNING producer-anomaly.
- **Phase-1 artifacts all landed host-side (mount-lag check PASS despite the active phantom):** both MODULE_CONTEXTs, coder-handoff (M7.5b block + deferred-gate + next-WU=M7.5c), cross-agent-notes (M7.5b + DP-B2-routed), coder-lessons.
- **The `[REVIEW]`s — all ACCEPT; 1 corrects the instruction (the M7.5a registry-free-correction class):** the `expectedSince` default window = the **whole retained log**, not a 24h lookback — a wall-clock 24h default would breach replay-determinism (INV-SA-03); a position-bound-only window is strictly more correct (the frontend scopes the window by passing `expectedSince`). Others ACCEPT: `/automations` in-memory offset cursor; the camelCase hand-built maps (M7.5a precedent — governance-thread item below); the inline `p:<pos>` cursor codec; the source-forced `[INFO]` corrections (`RunId` no of/parse; `finalStatus()`; `{id}`; sealed-no-accessor → `getSimpleName`; exhaustive switch; `ACTED_BUT_UNCONFIRMED` reuses `buildActions`); the optional new `AutomationEndpointsTest.java`. **No Coder pushback was wrong** — the `expectedSince` determinism correction is a legitimate evidence-based improvement, accepted.
- **DP-B2 — Nick's ruling. The Coder shipped the recommended default:** clean confirmed success → `NEVER_TRIGGERED` + non-null `lastRelevantRunId` + "last fired and confirmed at <time>" (the UI tells "ran fine" from "never ran" by the non-null id), **no frozen-contract change. Hub recommendation: ACCEPT the default; defer `FIRED_CONFIRMED` as a post-V1 additive value if real usage shows the overload is confusing** (adding it now reopens the frozen 4-value contract — a cross-lane event, friction for a non-differentiating edge; reversible-by-addition later).
- **Governance-thread item (fold into the AMD-CAND-1 pass):** the dashboard read-API endpoints ship **camelCase** (the frozen v1.1 contract) while Doc 09 §3.10 mandates Jackson snake_case — a known, M7.5a-precedented divergence (the frozen dashboard contract governs these hand-built-map endpoints; Doc 09's snake_case governs the Jackson POJO endpoints). Record it alongside the read-API-subset-vs-Doc-16-§4 recording when AMD-CAND-1 is processed.
- **Milestone:** the **read-API differentiator core is COMPLETE** — both hero halves on the live log (M7.5a + M7.5b) = **gate-2's read-API half DONE.** **Next Core slot = Nick's ruling (hub rec: leapfrog M7.5c with AB-4)** — AB-4 (cipher, the ready un-gated M9-prerequisite; AMD-87 cleared) ahead of M7.5c (events feed + health, deferrable, rides the frontend's need) → M9. **Do not author either until Nick rules** (deferred-gate discipline satisfied — M7.5b P2 now closed). **Critical path = the bench (first-light) + the Core march to M9.**
- **Commit pending (Nick):** core (the M7.5b working tree — SHA reconciles here) + hivemind (beat-32). Commit messages in the hub report. **Residuals:** OR-GATE-M7.4 CI-green (now + the M7.5b commit); D2 (route-ready); AMD-CAND-1 (route-ready); AMD-CAND-2/3 (staged, Nick's word); Dependabot; §2 hygiene.

## 2026-06-28 (beat 31 — v11 hub) — M7.5b coding-wait: the mid-Aug go/no-go readiness review + quick-wins packaged

**The Coder is building M7.5b (fleet committed: hivemind `9f12f80`, bench `2c0a33c`). The hub spent the wait on the highest-value autonomous work: a candid go/no-go readiness review + staging the quick-wins one-decision-away. No Core authoring (guardrail: M7.5b WUCP Phase 2 not closed); no mass-rewrites (phantom active).**

- **Mid-August go/no-go readiness review** — `context/planning/2026-06-28_mid-august-go-no-go_readiness-review.md` (candid critical-path read, evidence-based). **Gates:** 1 engine-LIVE ✅ DONE; 2 hero-on-real-data 🟡 PARTIAL (read-API nearly there — M7.5a done, M7.5b building — but the **frontend hero views are unbuilt**: `web-ui/dashboard` is a buildable scaffold + doctrine + contract-check + CI, src is still app/main only, last web-ui commit = the Track-A first beat; the `nexsys-frontend` skill was just stood up to staff it; **real data needs bench first-light**); 3 hardware-validating 🔴 NOT STARTED (the longest sequential pole: bench → AB-4 [authored, un-gated, ready] → M9 → 72h-stable; **AMD-87 the prior pre-M9 codec blocker is already cleared `7f44bed`**); 4 install-proven 🟡 (the `distribution/` harness + `install-smoke.yml` exist; needs green on HEAD).
- **Critical-path finding (confirms the standing belief):** **the bench is the critical path** — upstream of gate-2's real-data half AND all of gate-3 (it is M9's acceptance spec, sizes M9 via the open D5 re-open trigger, and supplies the AMD-CAND-1 measured confirmation values). It is also the one thing the hub can't advance autonomously (Nick's hands). **First-light pays off three ways in one session:** the moat measurement (real CONFIRMED / honest UNCONFIRMED), the D5 "modest map?" trigger, and the AMD-CAND-1 measured anchor.
- **~7-week verdict:** 49 days to ~Aug 16 = tight but credible for a **conditional go**, contingent on (a) bench first-light soon and (b) an honest frontend-progress read this week. **Pacing risks:** the frontend long-pole's unproven velocity (contingency: scope the hero to the 2 differentiator reads on the real endpoints, defer breadth/polish); M9's size gated on the bench (contingency: scope M9 to the curated Wave-1 set per the D5 fallback, defer device breadth post-go/no-go).
- **Quick-wins staged one-decision-away:** (1) **AMD-CAND-1** dispatch ready to route to independent DOCS review (`context/instructions/2026-06-28_AMD-CAND-1_…governance-review-dispatch.md`); (2) **AMD-CAND-2/3** Doc 02/08 color-currency fold **pre-drafted with verbatim before→after diffs, commit-on-Nick's-word** (`context/instructions/2026-06-28_AMD-CAND-2-3_doc02-08-color-currency_staged-fold.md` — white/CT scope for V1 + Kelvin-canonical-at-ingestion; co-sign directly or bundle into the AMD-CAND-1 pass); (3) the **D2 INV-id registration proposal** currency-confirmed route-ready (`context/instructions/2026-06-27_D2-replay-invariant_registration-proposal_review-dispatch.md` — the enforcing test is landed + green; route review→ratify); (4) **OR-GATE-M7.4** = Nick's GitHub Actions-tab confirm green on `4e31aed`+`9ec5949`.
- **Bench-readiness:** the Phase-0 runbook is ready; the hub will orchestrate it command-by-command the moment the physical session is set up — order held: **Ethernet verified before Wi-Fi/2.4 GHz off**; **MG24 firmware reflash before any measurement**.
- **Commit pending (Nick):** hivemind beat-31 (this section + the snapshot beat-31 + the go/no-go review + the AMD-CAND-2/3 staged fold + the D2 currency touch). Commit message in the hub report. **Residuals unchanged:** OR-GATE-M7.4 CI-green; D2 (now route-ready); Dependabot; §2 hygiene.

## 2026-06-28 (beat 30 — v11 hub) — M7.5b ISSUE-READY (audit-verified) + Stream B research RETURNED & reconciled

**Stream C (Core) + Stream B (research) both advanced this beat. M7.5b is authored + audit-verified; the device-model research returned and is fully reconciled (its session is done).**

- **M7.5b ISSUE-READY (audit-verified).** The coding instruction (`context/instructions/2026-06-28_M7.5b_non-firing-read-and-automation-list_coding-instruction.md`) delivers `GET /api/v1/automations/{id}/non-firing` (the co-equal "why didn't it fire?" — the 4-value frozen `verdict`) + `GET /api/v1/automations` (component summaries), extending M7.5a's `ExplanationService`. **Zero-mint (71/41/53 hold); ZERO module-info change** (M7.5a already added rest-api's `requires automation`). An independent adversarial subagent audit verified all 12 structural claims against source = **ISSUE-READY, zero fabrications**; the one compile-breaking consumer (`FakeExplanationService` in `RunEndpointsTest`) was caught by the P2 survey and the note flipped may→confirmed. Settled: **DP-B1** (ship the frozen 4-value verdict; defer the Doc-16-§4 7-value `SuppressionReason` depth — the M7.5a map-at-boundary precedent). Flagged for Nick: **DP-B2** (the clean-confirmed-success `/non-firing` verdict edge — a public-API-semantics call; recommended default implemented, Nick rules at WUCP Phase 2). Awaiting Nick's commit → route → gate → WUCP Phase 2.
- **Stream B research RETURNED + reconciled (DONE).** `context/assessments/2026-06-28_device-model-and-corpus_research-return.md` (HIGH confidence; disciplined sourcing). Realizes D5: a HomeSynapse-owned declarative JSON corpus (IR schema-v2) borrowing Z2M `exposes` (capability layer) + deCONZ generic-items inheritance (scale-without-rot) + Matter conformance-rigor (internal type system), **rejecting eval-in-the-data-path** (forced out by INV-PROJ-01/AMD-50-INV-03 → named HomeSynapse codecs via `manufacturerCodec`). New contribution: the **per-device confirmation-characterization block** (`confirmability` CONFIRMABLE|BEST_EFFORT|UNCONFIRMABLE / `reportsAuthoritative` / `reportingPosture`) — the schema realization of the `confirmed|unconfirmed|failed` moat; the slot the bench fills.
- **Routing — AMD-CAND-1 (substantive, pre-M9) → governance review→ratify.** The confirmation block → Doc 08 §3.6 `DeviceProfile` + a Doc 02 §3.8 per-device confirmability note. Dispatch authored: `context/instructions/2026-06-28_AMD-CAND-1_device-confirmation-characterization_governance-review-dispatch.md` (PM does not self-ratify). Clean amendment — extends a reserved seam, no invariant conflict (Nick's read). **M9 confirmation acceptance depends on it.** Bundles the standing governance fold (below).
- **Routing — AMD-CAND-2/3 → Nick's scope call (Doc 02/08 now-fixes, not pipeline-blocking).** AMD-CAND-2: ESC-W1-HUE-01 full-color GAP (Doc 08 §3.5 has only ColorControl(CT)→color_temperature; Doc 02 §3.10 lists color_hs/xy as light-optional → invited-but-unrealizable) — **decide white/CT-only (A, hub rec) vs pull-color-forward (B).** AMD-CAND-3: color-temp canonical-unit drift (Doc 08 §3.5 stores mireds/convert-at-query vs Doc 02 §3.6/§3.7 Kelvin-canonical/convert-at-ingestion) — **pick one canonical unit (hub rec: conform Doc 08 to Doc 02's Kelvin-at-ingestion moat rule).** Folds together. AMD-CAND-4/5 housekeeping (corpus IR governance; the Doc 02 §3.5→Doc 08 §3.5 pointer fix).
- **Stream A steer (the one concrete change to the bench).** The Phase-0/1 runbook now directs Phase-1 capture to record TOWARD the confirmation block (`confirmability`/`reportsAuthoritative`/`reportingPosture`/timeout/degradeRule), not just the raw interview — one first-light pass answers both the moat measurement AND the open D5 re-open trigger (is the `exposes`→capability map "modest" on real silicon?).
- **Phase-2 seed.** The IR schema + the two-path (upstream-ingest + bench-capture) onboarding pipeline → `nexsys-bench/docs/2026-06-28_phase-2_corpus-model_IR-schema-and-onboarding-pipeline.md` (the durable corpus form; built in Phase 2 after Phase 0/1).
- **Standing governance thread (recorded; fold into the AMD-CAND-1 pass).** The V1 read-API has shipped a frozen-v1.1 subset of Locked Doc 16 §4 twice (`RunExplanation` M7.5a, `NonFiringExplanation` M7.5b). Coherent (frozen contract = V1 wire; Doc 16 §4 = eventual model; boundary maps), but wants a short explicit recording so shipped code isn't read as contradicting a Locked doc — bundled into AMD-CAND-1's review pass.
- **Commits pending (Nick):** hivemind (beat-29+30 + the M7.5b instruction + the research return + the AMD-CAND-1 dispatch) and nexsys-bench (the runbook steer + the Phase-2 model). Commit messages provided in the hub report. **Residuals unchanged:** OR-GATE-M7.4 CI-green; D2 INV-id registration; Dependabot; §2 hygiene.

## 2026-06-28 (beat 29 — v11 hub) — LAUNCHED; superseded v10; preflight STALE-benign → reconciled to committed reality

**The v11 mission-control hub (the bench-reframed three-stream plan) launched, grounded, ran the 11-check preflight, and reconciled the latest beat.** Grounded env-model → truth-hierarchy/pointer-not-copy → V1 launch-scope record + the read-API breakdown + the dashboard contract-freeze + **the bench test-and-truth-engine decision-record** → the 11-check preflight.

- **Preflight = STALE-benign, zero CONFLICTED** — currency-lag only: the beat-28 masthead anticipated "bench init pending / hivemind `a9689c3`+crystallization (lands next commit)"; Nick has since committed both. **Re-derived from source (not propagated):** core **`9ec5949`** (M7.5a; clean, pushed) · docs **`75d0345`** (AMD-95; Doc 16+17 LOCKED; **170/50**) · hivemind **`c3a6f8b`** (the beat-28 crystallization — bench decision-record + research dispatch + v11 prompt + v10 archive — COMMITTED) · **bench `4c4496d`** (`nexsys-bench`, the 5th repo — inited + scaffolded + pushed). **Counts hold 71/41/53**; `projectionVersion` 5. **Check 9 PASS** (both skill mirrors byte-identical). M7.5a source-confirmed (`ExplanationService`/`RunExplanation`/`StandardExplanationService` resolve in `core:automation`).
- **⚠ Truncated-tail phantom ACTIVE** — VM git shows the 3 spine files (snapshot/backlog/pm-handoff) "modified" as single-hunk EOF truncations; host file tools confirm them intact (the "Counter-nonce" content the VM reads as the truncation point lives at L291 host-side). Per the env-model: host tools authoritative, VM git suspect; §2 body-rotation hygiene HELD; targeted host-side Edits only this session.
- **Carry — `nexsys-skills` has uncommitted work (NOT the spine, write-isolated lane):** `MANIFEST.yaml` (mod) + `design/2026-06-27_nexsys-frontend-skill_creation_return.md` + `orchestrators/nexsys-frontend/` — the nexsys-frontend skill-creation return, awaiting Nick's commit. Flagged, not touched (single-spine-writer discipline).
- **First-act hygiene:** the v10 prompt is already archived → `context/handoff/archive/2026-06-27_PM-mission-control_v10_orchestrator_session_prompt.md` (CONFIRMED — absent from the live handoff dir). No duplicate.
- **Reconciled this session (safe/reversible host-side Edits):** PROJECT_SNAPSHOT (the beat-29 block + Last-updated/Current-state/Latest-commits stamped to `c3a6f8b`/`4c4496d`); this pm-handoff (this beat + masthead); the milestone backlog (a v11 currency note).
- **Residuals (unchanged):** OR-GATE-M7.4 CI-green on `4e31aed`+`9ec5949`; the D2 canonical INV-id registration (review→ratify; proposal staged); Dependabot 5-vuln (1 critical; deferred); the §2 hygiene remainder (deferred while the phantom is active).
- **Next:** author **M7.5b** (the non-firing read `GET /api/v1/automations/{id}/non-firing` + `GET /api/v1/automations`) against committed `9ec5949`, per the breakdown + the frozen v1.1 §B3, building on M7.5a's `ExplanationService` plumbing → Nick → Coder → WUCP Phase 2. Orchestrate bench Phase 0/1 (Stream A) with Nick; ensure the device-model research is launched (Stream B); reconcile returns. core `9ec5949` · docs `75d0345` · hivemind `c3a6f8b` · bench `4c4496d`.

## 2026-06-28 (beat 28 — v10 hub) — M7.5a COMMITTED (`9ec5949`); the bench test-and-truth-engine reframe crystallized; nexsys-bench (5th repo) + v11 authored

**M7.5a is committed + pushed** (core `4e31aed`→`9ec5949`; hivemind `828cdb6`→`a9689c3`). The v10 hub then crystallized the bench reframe before saturation (Nick's explicit capture-then-execute instruction).

- **The reframe (RATIFIED, co-signed):** the hardware bench is the **test-and-truth engine** — *a captured real device event stream is a seeded event log*, so real-world interactions become permanent, fast, hardware-free regression tests (extending M7.4d), and the bench is where the `confirmed|unconfirmed|failed` moat becomes measured fact. Decision-record + the five rulings (replay-as-regression + raw/transform split; `nexsys-bench` 5th repo; Ethernet/radio-off + dongle firmware reflash; ZHA-first + our harness; the research scope + 3 sharpenings): `context/decisions/2026-06-28_bench-test-and-truth-engine_decision-record.md`.
- **The three streams:** **A** — the bench (`nexsys-bench`, scaffolded: README/IaC/Phase-0 runbook/harness+fixtures discipline; Nick's hands + hub orchestration); **B** — the device-model research (`context/handoff/2026-06-28_device-model-and-corpus_research_session_dispatch.md`, READY — realizes D5 + first-classes confirmation + returns the onboarding pipeline); **C** — Core (M7.5b — DEFERRED to the v11 hub for clean-context authoring; the one defensible cut, since M7.5b is fully reconstructable from durable sources and authors better fresh).
- **5-repo discipline:** core / docs / hivemind / skills / **bench** (commit+push all touched before any launch).
- **v11 authored** (`context/handoff/2026-06-28_PM-mission-control_v11_orchestrator_session_prompt.md`, supersedes v10) — carries the three-stream plan; tasks the v11 hub to author M7.5b + orchestrate the streams. The v10 prompt archives.
- **Next (Nick):** init+push `nexsys-bench`; commit the hivemind crystallization (decision-record + research dispatch + v11 + the spine beats + the v10 archive); launch v11 + the research session; run bench Phase 0 on `hs-dev-1` (Ethernet + radio-off → Pi prep → dongle firmware reflash → ZHA → the moat measurement).
- **Residuals unchanged:** OR-GATE-M7.4 CI-green on `4e31aed`+`9ec5949`; the D2 INV-id registration (review→ratify); Dependabot (deferred); §2 hygiene (deferred).

## 2026-06-28 (beat 27 — v10 hub) — M7.5a GATE-GREEN + WUCP Phase 2 ACCEPT (the read-API hero read lands)

**M7.5a built (Claude Code), gate GREEN — verified actually-green on Nick's terminal: the targeted `:core:automation:compileJava :api:rest-api:compileJava :lifecycle:lifecycle:compileJava :core:automation:test :api:rest-api:test` (51 tasks) + `./gradlew check` (149 tasks). WUCP Phase 2 = ACCEPT.** The hero reads (`GET /api/v1/runs` + `/runs/{runId}/causal-chain`) are live as pure log-derived projections (INV-SA-03; zero mint).

- **Source-verified (git status + the green gate, not the report):** `core/automation/module-info` UNCHANGED + zero event-model change → **counts 71/41/53 hold** (the green ArchUnit count-pins prove it); `api/rest-api/module-info` +2 plain requires (`com.homesynapse.automation`, `com.fasterxml.jackson.databind`); `core/automation/build.gradle.kts` +`testFixtures(:core:event-model)`. The Coder's WUCP Phase 1 artifacts all landed (both MODULE_CONTEXTs, coder-handoff, cross-agent-notes, coder-lessons) — mount-lag check PASS.
- **8 `[REVIEW]`s — all ACCEPT; 2 correct the M7.5a instruction:** (1) **registry-free pure-LOG outcome derivation** — DP-A2's registry gate would inject a mutable, non-deterministic read into a pure projection (breaks INV-SA-03 determinism); the Coder's log-only derivation is strictly more correct (a `DISABLED` command is never ledger-tracked → no `state_confirmed` → never a false `CONFIRMED`); (2) **`automationId` from the envelope subject** (`SubjectRef.automation(...)`; the run-lifecycle payloads carry none — the hub's pre-issue audit flagged the same). Others ACCEPT: trigger-view best-effort (`firingValue` null V1); `cascade.parentRunId` null V1; `actions[]` anchored on `command_issued`; `listRuns` newest-first via bounded forward-scan+reverse (the `EventStore` is forward-only); `+jackson.databind` (the params-object parse at the JSON boundary — rest-api owns the JSON boundary; the dep pre-existed in build.gradle).
- **Forward notes (product, not fixes — carry to M7.5b / a polish pass):** derive `firingValue` from the triggering event when the UI needs it; the `DISABLED`-vs-in-flight `DISPATCHED` collapse self-resolves for confirming commands once they confirm/timeout (so it is honest for terminal runs); a backward-read `EventStore` primitive is the proper O(page) DESC fix (post-V1, OUT of the thin slice).
- **No cross-lane change:** the frozen v1.1 wire shapes are UNCHANGED (the `RunEndpointsTest` shape test guards them); the status mapping + log-derivation are internal. The Web-UI lane swaps its B3 mock for the real endpoints with no re-sync (a non-breaking FYI per the contract §D).
- **Deferred gate / commit:** M7.5a local gate GREEN confirmed; the code is UNCOMMITTED → **SHA reconciles at Nick's commit**; CI-on-pushed-commit is the residual (the standing gate-of-record loop, same as OR-GATE-M7.4).
- **Next:** Nick commits + pushes (core M7.5a + the hivemind spine) → CI confirm → **M7.5b** (the non-firing read `GET /automations/{id}/non-firing` + `GET /automations`). Parallel lanes continue (frontend; bench; D2 review→ratify; positioning/currency reviews).

## 2026-06-27 (beat 26 — v10 hub) — M7.5a ISSUE-READY (the read-API hero read; authored + independently verified)

**The v10 hub's first substantive act: the M7.5a coding instruction is authored, source-verified, and ISSUE-READY** (`context/instructions/2026-06-27_M7.5a_read-API-hero-read_runs-and-causal-chain_coding-instruction.md`), against committed `4e31aed`. It delivers `GET /api/v1/runs` + `GET /api/v1/runs/{runId}/causal-chain` as pure log-derived projections (INV-SA-03; ZERO mint — 71/41/53 hold) to the exact frozen v1.1 shapes.

- **Grounding caught 4 source-vs-contract findings (folded in):** (1) `RunCausalChain` is the cascade ancestry only — it feeds the wire `cascade` field alone; the trigger/conditions/actions/outcome tree projects from the AMD-92 run-lifecycle EVENTS (Doc 16 §3.3 `RunExplanation`), NOT from `RunCausalChain`. (2) No historical-run query exists (`RunManager` is active-runs-only) → a NEW automation read-side `ExplanationService` over `EventStore.readByCorrelation`/`readByType`. (3) `RunStatus` ≠ the wire `status` vocabulary. (4) M7.4c made confirmation live → the FULL outcome enum ships now (not the contract's deferral fallback).
- **Nick's 3 co-signed rulings (encoded as Settled Decisions §A):** DP-A1 map `RunStatus`→wire at the read boundary (`CONDITION_NOT_MET→SKIPPED`, `ABORTED→CANCELLED`; precise reason rides `terminalReason`); DP-A2 ship the FULL `actions[].outcome` enum (with the `DISABLED`→`DISPATCHED` honesty nuance — never a false `CONFIRMED`); DP-A3 projection in automation read-side per Locked Doc 16 §3.3.
- **JPMS (verified):** the ONLY module-info change is rest-api `+requires com.homesynapse.automation` (PLAIN — via an `Object`-erased gateway, the `installAdminEndpoints` precedent; `build.gradle.kts` already carries the `implementation` edge). **automation module-info UNCHANGED.**
- **Independent adversarial audit (subagent) = PASS:** every cited type/signature/enum/module-info/SPI/factory resolves in source; both frozen v1.1 wire shapes match field-for-field; DP-A3 grounded in Locked Doc 16 §3.3/§4/§8. Two hardening fixes folded (the `conditions[].expression`/`evaluated` derive-don't-read gap — the event carries `conditionType`, not an expression; envelope-level correlation/timestamps).
- **Cross-lane FYI (the Web-UI lane / the still-open v9 advisor):** the wire `status` vocabulary is UNCHANGED — only the internal `RunStatus`→wire mapping is new, so the Web-UI mocks stay valid. Non-breaking; recorded per the contract §D (no cross-lane re-sync needed).
- **Next:** Nick gates the instruction → routes to the Coder → build → WUCP Phase 2 (the hub reconciles the return + rules the 3 `[Design point]`s: run-list paging/order; `runId→correlationId` resolution; `cascade.parentRunId` recoverability). Then M7.5b. Parallel lanes (frontend in flight; bench front-load on hardware; D2 review→ratify; OR-GATE-M7.4 CI confirm; positioning/currency reviews) continue, write-isolated.

## 2026-06-27 (beat 25 — v10 hub) — LAUNCHED; preflight STALE-benign → reconciled to committed M7.4d (`4e31aed`)

**The v10 mission-control hub superseded v9, ran the 11-check preflight (STALE-benign, zero CONFLICTED — currency-lag only), and reconciled the spine to the committed M7.4d reality.** Grounded env-model → truth-hierarchy → V1/v6/§1 → preflight. The v9 prompt is archived → `context/handoff/archive/`; the v9 conversation stays open as the write-isolated nexsys-frontend advisor (does NOT write the spine).

- **Verified from source (not propagated).** core **`4e31aed`** = M7.4d committed: `git show --stat` = ONLY `RunPipelineReplaySafetyTest.java` (+504, new) + `lifecycle/lifecycle/MODULE_CONTEXT.md` (+2) — **test-only, no production change, `module-info` unchanged → counts hold 71/41/53** (re-derived from source AND the gate assertions: `EventTypesTest`=71, `EventTypeAnnotationTest`=41, `EventCategoryMappingTest`=53). docs **`75d0345`** — watermark **AMD-95**, **170/50** (re-derived from the §17 table footer). hivemind advanced **`f613c36`** (v9 arc, beats 20–24) → **`828cdb6`** (the v10 prompt commit). **Check 9 PASS** (both skill mirrors byte-identical).
- **Preflight = STALE-benign (Checks 1/3/4 = currency-lag; zero CONFLICTED).** The snapshot/backlog HEAD pointers cited `db8ab5f` and the M7.4d commit was unrecorded — both stamped to `4e31aed` here. Checks 2/5/6/7/8/9/10/11 = PASS.
- **⚠ Truncated-tail phantom ACTIVE.** VM `git status` shows every spine file "modified"; each diff is a single-hunk EOF truncation (`\ No newline at end of file`); host file tools confirm the files intact (verified the lifecycle MODULE_CONTEXT tail — full M7.4a/b/c/d notes present). Per the env-model: host tools authoritative, VM git suspect, nothing "fixed"/committed off the VM view; the §2 body-rotation hygiene (mass-rewrites) is HELD — targeted host-side Edits only this session.
- **Reconciled this session (safe/reversible host-side Edits):** PROJECT_SNAPSHOT (beat-25 + Last-updated/Current-state/Latest-commits stamped to `4e31aed`/`828cdb6`); the milestone backlog (a v10 currency note + the M7.4d commit `4e31aed` recorded in the M7.4 row); this pm-handoff (this beat + the masthead + OR-GATE-M7.4 → `4e31aed`).
- **Carried residuals (unchanged):** OR-GATE-M7.4 — confirm the `ci.yml` Actions run is GREEN on the pushed `4e31aed` (`gradlew` is `100755`/executable in the commit → the exit-126 mode is cleared; the Actions-green confirm is Nick's). The D2 canonical INV-id registration (review→ratify; proposal staged; reconciles AIOT-INV-1's dangling D2 citation). The Dependabot 5-vuln (1 critical) CI-hardening item (deferred). The §2 hygiene remainder (deferred while the phantom is active).
- **Next:** author the **M7.5a** coding instruction (the read-API hero read) against `4e31aed`, per the breakdown + the frozen v1.1 contract → Nick → Coder. The parallel lanes (frontend in flight; bench front-load when hardware lands; D2 review→ratify; positioning + currency reviews) continue, write-isolated.

## 2026-06-27 (beat 24 — v9 hub) — read-API scope-freeze + breakdown SET + the nexsys-frontend skill-creation dispatch authored (READY)

**Per Nick's delegation to drive Core forward, the next Core direction is set and the Web-UI staffing mechanism is dispatched.**

- **Causal-read-API — scope-frozen + right-sized (the strategic Core call).** Grounded against the FROZEN v1.1 contract: the read-API needs **no new machinery** — the hard "why-didn't-it-fire" half is answerable from existing run records / absence / config (deep per-non-match trigger recording is post-V1, OUT). It's a projection + REST read surface over data the now-live M7.4 pipeline already produces. Six reads → over the P1 sizing line → **split into M7.5a/b/c:** **M7.5a** = the hero `GET /runs` + `GET /runs/{id}/causal-chain` (the "why did it fire? / did it confirm?" core; gate-2; the read-projection pattern the others reuse) → **M7.5b** = the non-firing read + the automation list → **M7.5c** = the events feed (B1) + health consolidation (B2). Scope-guarded vs M12 (no query language, no trace store, no per-non-match recording, no WS). Authority: `context/planning/2026-06-27_causal-read-API_scope-freeze-and-milestone-breakdown.md`.
- **Next Core deliverable = the M7.5a coding instruction** — authored against the **committed** post-M7.4d HEAD (the discipline that held M7.4d authoring until M7.4c committed), to the exact frozen v1.1 shapes, with a shape test + the cross-cutting contract (AB-1 auth, `state-store-replaying`(503), poll-meta).
- **`nexsys-frontend` skill-creation dispatch authored + READY** (`nexsys-skills/design/2026-06-27_nexsys-frontend-skill_creation_session_dispatch.md`) — the structural fix for the idle Web-UI lane (#1 schedule risk). A role skill (the nexsys-coder/PM family): frontend + web-design + UI/UX + **brand identity**; hivemind-integrated (preflight, write-isolation, single-spine-writer preserved, the v1.1 contract as a consumed hard input); created via `skill-creator` + conformed to design-v1; **mandated to ask Nick the executive brand/design questions** before writing the brand sections. Off the critical path.
- **Next:** Nick commits the v9 arc (core M7.4d test+MODULE_CONTEXT; the hivemind), launches the frontend session, then routes M7.5a (which the hub authors next). The D2 INV-id co-sign + the CI-on-commit confirm remain the open M7.4 residuals.

## 2026-06-27 (beat 23 — v9 hub) — M7.4d GATE-GREEN + WUCP Phase 2 ACCEPT → the M7.4 arc is COMPLETE

**M7.4d built, `./gradlew check` GREEN (149 tasks, first pass; shift-left `:lifecycle:compileTestJava` also clean), WUCP Phase 2 = ACCEPT.** The D2 pure-function-replay invariant is now CI-pinned at the composition-root level (`RunPipelineReplaySafetyTest`). **Source-verified against the diff, not the report:** core tree = ONLY the new test + `lifecycle MODULE_CONTEXT` (no production change; `module-info` unchanged; counts 71/41/53); docs tree CLEAN → **`Architecture_Invariants_v1.md` UNTOUCHED — the Coder did not self-mint D2's id** (correct; that's the review→ratify half).

- **Non-vacuousness — empirically proven.** The LIVE positive control (`afterLive_newTrigger_doesDispatch`) passed in the same green run: the harness sees a real `command_dispatched` in LIVE while seeing zero across the boot REPLAY. The dispatch axis asserts both `command_dispatched`+`command_result` (the unregistered-seed-entity gap); the run-init axis pins `automationRegistry().getAll().hasSize(2)` to fail-fast on a config-load regression; the seed is written via a **bus-less `PersistenceFactory`** so the booting core replays from checkpoint 0 (the real crash-recovery case, not a vacuous resume-past-the-seed).
- **The 3 `[REVIEW]`s — all ACCEPT.** (1) the `EventTrigger`(event_type:state_reported) lever — the only deterministic, registry-/`AutomationId`-independent trigger match; (2) asserting both dispatch event types — strictly more correct than `command_dispatched`-only; (3) observe via the durable event store / no production seam — with the **M9-seam forward note** (M9 must extend the gate to assert the real adapter-call recorder is zero across replay).
- **Residuals.** (a) **OR-GATE-M7.4** — Nick's local `./gradlew check` is green for M7.4d; the CI-on-pushed-commit confirm covers the forthcoming M7.4d commit (on `db8ab5f`). (b) **The D2 canonical INV-id registration** — the parallel governance half; the proposal is staged (`context/instructions/2026-06-27_D2-replay-invariant_registration-proposal_review-dispatch.md`), routes review→ratify; AIOT-INV-1's dangling citation reconciles at the fold.
- **Next Core slot = the thin causal-read-API** — pending Nick's scope-freeze ruling (the IN/OUT line proposed beat-22-era). Then AB-4 → M9.
- **Commit note.** The hivemind working tree now carries the full v9 arc (beats 21–23 + the two M7.4d instruction files + the v8-prompt archive) **plus** the Coder's M7.4d Phase-1 artifacts (coder-handoff, cross-agent-notes) — review `git diff` and commit; core has the M7.4d test + MODULE_CONTEXT to commit separately.

## 2026-06-27 (beat 22 — v9 hub) — gate-of-record CONFIRMED (local) + M7.4d ISSUE-READY (both halves authored)

**Nick ran `./gradlew check` GREEN (149 tasks) on the working tree that became `db8ab5f`, then committed + pushed both repos** (core `69cc6e2..db8ab5f`; hivemind `ce84b0e..a142d9b`). The M7.4c **local** deferred-gate is confirmed → **OR-GATE-M7.4 narrowed** to the CI-on-pushed-commit confirmation only; M7.4d's §4b prerequisite is **clear**. The push re-surfaced the **Dependabot 5-vuln (1 critical)** alert — already tracked (ci-as-gate-of-record.md CI-hardening backlog item 5; deferred per Nick post-go/no-go).

- **M7.4d authored + ISSUE-READY (the M7.4 arc's last item).** (a) Coding instruction `context/instructions/2026-06-27_M7.4d_d2-pure-function-replay-CI-gate_coding-instruction.md` — `RunPipelineReplaySafetyTest`: a composition-root seeded-log replay asserting ZERO dispatch/run/confirm side-effects + a LIVE positive control; the V2-A spike (`spike/dispatch-latency-and-log-growth/`, confirmed present) is the tested template; `ci.yml` auto-gates it (no ci.yml change; **no production change — D2 is already structural**, this names + CI-pins it). (b) Governance half `context/instructions/2026-06-27_D2-replay-invariant_registration-proposal_review-dispatch.md` — PROPOSES D2 as `INV-ES-NN` (watermark-neutral doc-currency fold; reconciles AIOT-INV-1's dangling "decision-record D2" citation), routed review→ratify; **the PM does NOT self-mint**.
- **Next:** Nick routes M7.4d → Coder → gate → WUCP Phase 2; co-signs/dispatches the D2 registration review; confirms CI green on `db8ab5f` (closes OR-GATE-M7.4). Then the thin causal-read-API. The §2 hygiene remainder (the body-rotations, the project-knowledge regenerate/retire, the beat-20 skills folds) is still pending — gated by Nick (the VM truncation-tail phantom was active this session, so mass-rewrites were correctly held).

## 2026-06-27 (beat 21 — v9 hub) — LAUNCHED; preflight STALE-benign → reconciled; spine re-stamped to committed M7.4c (`db8ab5f`)

**The v9 mission-control hub superseded v8, ran the 11-check preflight (STALE-benign, zero CONFLICTED — currency-lag only), and reconciled the spine to the committed M7.4c reality.** Grounded env-model → truth-hierarchy → V1/v6/§1 → preflight. **Verified from source (not propagated):** core `db8ab5f` (M7.4c; parent `69cc6e2` M7.4b, ancestor-confirmed) · docs `75d0345` · hivemind `a142d9b` + uncommitted beats 20–21; counts **71/41**/53 re-derived (71 = `EventTypes.java` constants; 41 = `CORE_PRODUCTION_EVENT_CLASSES`); watermark **AMD-95** / **170/50** (`AC` §50; **AIOT-INV-1** sole Doc-17 mint, **D2 INV-id pending → M7.4d**); M7.4c artifacts present (`RunPipelineConfirmWiringTest`). **Check 9 PASS.**

- **Reconciled this session (safe/reversible host-side Edits):** PROJECT_SNAPSHOT beat-21 + the stale masthead fields (Current-state/Latest-commits had drifted to the M7.2b-era `5363347`/`b296e76`/AMD-94/169-49) → `db8ab5f` reality; **OR-M7-WIRING → RESOLVED**; **NEW OR-GATE-M7.4** deferred-gate risk; the milestone-backlog M7.4 row → a/b/c DONE + M7.4d next.
- **OR-M7-WIRING RESOLVED:** the live-wiring + E2E confirm gate landed (M7.4b/c). The resolution condition's **D2 replay-CI clause is carried to M7.4d** (the remaining M7.4-arc hardening).
- **Deferred gate (OPEN — OR-GATE-M7.4):** M7.4b/c handed back NOT gate-verified (sandbox runs no Gradle); confirm `./gradlew check` + CI green on the pushed `db8ab5f` before recording CI-gated + before M7.4d (§4b + gate-of-record).
- **Hygiene remainder (gated by Nick):** the body-rotations; project-knowledge regenerate/retire (file-delete permission-gated); the beat-20 skills folds (rot-tally + the SD-5 fix). **Working tree:** the v9 reconcile layers on the uncommitted v8 beat-20 + the v9 prompt — review the diff; commit the v8-close + v9-launch reconcile together.
- **Next:** restore preflight PASS → **M7.4d** (D2 replay CI gate + register D2's INV-id via review→ratify) → the thin causal-read-API.

## 2026-06-27 (beat 20 — v8 hub) — skills triggers/recursion deliberation returned + reconciled (off critical path)

**The fresh write-isolated deliberation returned** (`nexsys-skills/design/2026-06-27_skills-build-triggers-and-recursive-stewardship_deliberation_return.md` + the parked `orchestrators/skill-architect_SPEC_v0.md`). High-quality, source-verified (it ran its own fact-checker), critical, willing to say no. Off the M7.4 critical path; non-blocking.

- **Triggers reframed gate-vs-value.** A build starts iff ≥1 value-trigger fired AND the gates are open — G1 (critical-path clear; Rung-0 harness exempt) + G2 (spare capacity ≥5 days); T1 same-rot-class≥3 · T2 rolling rot-rate · T3 same-model re-derived ≥3× · T4 fresh-session rate · T5 new-model = RE-EVAL, not build. "Smallest-rung-that-addresses-it." **Instrument:** a one-file append-only rot-incident tally (senses the spine, where rot is) + a one-line trigger-check on the WUCP Phase-2 closeout.
- **Recursion safety sharpened by change-class.** SK-META-INV-A (detection = pure read-only projection) · B (steward = structural proposer, no apply path except via the gate) · C (content = eval OR human; **self-modification = eval AND human AND keystone AND reversible**) · D (auto-apply only for semantically-null idempotent self-verifying ops). Safe first increment = DETECT+REPORT (the parked daily-check). The Doc 17 dogfooding parallel made rigorous: "the steward is never an autonomous editor," two-axis CI-testable.
- **Bootstrap = a calibrated NO.** Build no live skill pre-trigger (no harness → no eval gate; write-isolation forbids the live load path). Author the `skill-architect` SPEC (parked, non-loadable); ratify curator=skill-creator (adopt, don't build); stand up the tally; instantiate skill-architect as the first live skill at the first build-trigger, eval-gated.
- **A real rot caught (verify + fix at hygiene; tally's first entry).** `nexsys-skills/README.md` + the topology proposal cite "description-as-API = SD-5"; design v1 §16 SD-5 is the *harness* decision — description-as-contract is §1 Principle 5 / §13.2. A decision-number drifted under two pointers — exactly the STALE-CACHE class the tally exists to surface.
- **Routing.** #1–#3 (frame + tally + closeout line) fold into the standing re-eval; #4–#5 (SK-META-INV-A..D + the safe-first-increment) are design intent → the harness spec at Rung-0; #6–#7 (curator=skill-creator + the skill-architect spec) route through the design-v1-Lock/eval — **parked, not instantiated.** The **v9 hub executes the folding + the tally + the SD-5 fix in clean context.**

## 2026-06-27 (beat 19 — v8 hub) — M7.4c GATE-GREEN + WUCP Phase 2 ACCEPT — OR-M7-WIRING CLOSED (the pipeline confirms end-to-end)

**M7.4c built, gate GREEN on the first run, WUCP Phase 2 ACCEPT — the confirmation half is live and OR-M7-WIRING is closed.** The full command pipeline now runs end-to-end on the real composition root: trigger → run → `command_issued` → dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`. The "did it actually confirm?" hero renders live.

- **Gate.** `./gradlew check` GREEN first run, **no gate-fix** — the Coder applied the M7.4b P2 lesson and swept every consumer (the `Components` arity, `SharedScheduler`, the 4th-subscriber pin in `LifecycleWiringTest`).
- **Phase 2 ACCEPT (verified).** git status → module-info unchanged both modules + zero event-model change → **71/41/53**. The green lifecycle/`@TempDir` tests confirm the ledger's read connection is released — **the M7.3 leak class is closed for the exact subscriber that caused it**. The E2E gate (`RunPipelineConfirmWiringTest`) proves `command_issued → command_dispatched → state_confirmed` via a synthetic `state_reported`, against `onOff()`'s real `ExpectedOutcome("on", ExactMatch(true))` + `EXACT_MATCH` — a true confirm, not an optimistic bypass.
- **The 4 `[REVIEW]`s — ACCEPT.** (1) the **asymmetric teardown** — hub-verified against `InProcessEventBus`: `abandon()` (L217-231) closes every runtime's read connection, `unsubscribe()` (L176-184) closes the one; the graceful path gets the explicit `unsubscribe`, the abandon path is covered by `eventBus.abandon()` — both leak paths closed, and the deviation from "both branches" is the more correct read. (2) `pollExpirations()` as a `Components` Runnable; (3) `schedulePeriodic` post-construction; (4) the `onOff()` E2E. All per spec / sound.
- **OR-M7-WIRING: CLOSED** (was the standing Open Risk — mark it resolved on the next Open-Risks pass; beat-19 is current truth). Only **M7.4d** remains in the M7.4 arc — the D2 pure-function-replay CI gate + registering D2's canonical INV-id (safety/governance, not new functionality).
- **Next:** Nick commits (M7.4c Core + the hivemind spine) → M7.4d. Parallel/off-critical-path: the positioning re-weight; the AMD-96 currency review; the bench ASH posture; the skills design-v1 review + the new triggers/recursion deliberation.

## 2026-06-27 (beat 17 — v8 hub) — M7.4b GATE-GREEN + WUCP Phase 2 ACCEPT (the run pipeline is LIVE)

**The Coder built M7.4b; the hub gate-fixed a P2 miss and ran it to GREEN, then WUCP Phase 2 = ACCEPT.** The run pipeline is wired live into the composition root for the first time — a LIVE matched trigger drives `RunManager.initiateRun` → condition gate → executor → `command_issued` → the M7.4a `command_dispatch_service` → `command_dispatched`.

- **Gate.** First `./gradlew check` RED on a P2 fan-out miss: `DurationTimerTest:205` constructed the now-2-arg `AutomationEngineSubscriber` directly (the Coder swept the assembly test, missed the direct-ctor consumer). Hub surgical gate-fix (inert recording doubles; the REPLAY test never initiates a run) → re-run GREEN. The deferred-gate discipline working: the 4-lens LLM review returned 0 findings (cannot compile); the gate caught it in 9s.
- **Phase 2 ACCEPT (verified).** `git status` → module-info unchanged ×3 + zero event-model change → **71/41/53** holds. Green gate → the lifecycle/`@TempDir` tests pass (paired `runManager.close()` teardown clean — M7.3 class closed), the D2 replay-safety test, and the producer-pipeline test (a seeded trigger emits `command_issued`→`command_dispatched`). Read `RunInitiator.java` directly.
- **The 4 `[REVIEW]`s — ACCEPT.** (1) the package-private `evaluateMatches` seam — **better than the interface-widening the instruction floated** (single source of truth, no re-derivation, frozen `evaluate` untouched); (2) `resolvedTargets` keyed `"action:<index>"` (align with a selector-label convention later); (3) `RunManager extends AutoCloseable` (M7.4a precedent); (4) `RunManagerConfig.defaults()` (surface cascade/auto-disable via typed config when operator-configurable). The M7.4a serializer review is **closed in production** — `PersistenceFactory.commandParameterSerializer()` returns `Function<Map,String>` (not `ObjectMapper`), backed by the `AttributeValue`-capable mapper, with a real round-trip test.
- **Lesson.** On a ctor signature change, the P2 sweep must `grep "new <Class>("` across the ENTIRE test tree, not just seam/assembly tests.
- **OR-M7-WIRING: half-closed** (producer live). **Next:** Nick commits (M7.4b Core + the separate distribution fix + this hivemind spine) → M7.4c (live `pending_command_ledger` + `pollExpirations()` tick + the E2E confirm gate). Open Risks unchanged (OR-M7-WIRING closes at M7.4c).

## 2026-06-27 (beat 16 — v8 hub) — ecosystem-currency research returned + reconciled (off critical path)

**The write-isolated research session returned `context/assessments/2026-06-27_smart-home-ecosystem-currency_research-return.md`** (web-backed; 71 VERIFIED / 16 honestly-flagged UNVERIFIED; the SafeGate fabrication sought + excluded). The hub read it in full — source discipline is strong, the 6 decisions hold. **No V1-scope change; the Core critical path (M7.4b building) is untouched.** Two clusters confirm-reinforced, one moved.

- **MOVED — the explainability moat (Cluster 2; TIME-SENSITIVE; Nick's strategic call).** HA merged the FRAMEWORK for durable never-evicted "why-didn't-it-fire" traces to `dev` 2026-06-22 (core #174116 + frontend #52708) — **emitter reverted, so NOT shipped**, but the why-not half is no longer "unshipped everywhere." Re-weight the mid-Aug differentiator onto (a) the durable `confirmed|unconfirmed|failed` device-confirmation projection (unaddressed on every platform) + (b) never-evicted-as-a-structural-property (HA's is capped/bucketed/bolt-on). **Baseline CORRECTION:** HA traces persist to disk (not "in-memory/evicted"); real limits = cap(5) + no-trace-on-never-match — fix before any copy ships. Go/no-go watch on the HA 2026.7-line.
- **AMEND AMD-96 + elevate M9 (Cluster 1A).** Direction holds; widen the band to **v14→v18 / EmberZNet 8.0.2→9.0** (v14 is the dongle floor; SDK current 9.0/v18). The **`ASH_ERROR_TIMEOUT` is a factory-8.0.2 MG24 cluster** (reflash remedy) → promote to a first-class **M9 + hero-bench reliability item** (the hero runs on this exact silicon); V1 posture is a hardening call for Nick + the bench. Fold into the AMD-96 currency-review scope + M9 scoping + the wave-1 device-model.
- **NARROW the Thread seam (Cluster 1C).** Single-chip Zigbee+Thread on one MG24 (RCP Multi-PAN) is deprecated upstream → reserve as "add a dedicated Thread radio later," not "MG24 dual-stack." A non-precluding currency refinement to the wave-1 reserved-seam posture.
- **CONFIRMs (reinforced — no action):** Matter V1 deferral (1.6 focused/no new categories; still fragmented); D5 converter-DB licensing (unchanged; zigpy→Open Home Foundation de-risks; GPL app separate); the AIoT planner→verifier→executor consensus (5/5 baseline papers verified).
- **SCOPE the Q1 patent follow-up + a correction (Cluster 3B → Doc 17 §7).** No blocking patent found (directional, moderate confidence); run the under-covered surfaces (Samsung/SmartThings, WO2025024326A2, Apple US20250077786A1, non-US) before any superlative. **CORRECTION:** US10367652B2 family = Nant Holdings IP, not Nest/Google. Defensible claim language holds; superlatives stay counsel-gated.
- **Hub reconciliation:** the three AMENDs route to their channels (the AMD-96 currency review; Doc 17 §7 Q1; the positioning = Nick's strategic call); the hub does not self-edit Locked/proposed bodies mid-flight. CONFIRMs + the 2 baseline corrections recorded. **Next:** Nick rules the positioning re-weight (time-sensitive) + the hero-bench ASH posture; the AMD-96 review (when dispatched) folds the band-widen + the ASH cluster. Open Risks unchanged.

## 2026-06-27 (beat 15 — v8 hub) — M7.4 split 3-way (Nick co-signed); M7.4b ISSUE-READY (the run pipeline goes live)

**Grounding the M7.4b wiring surfaced that M7.4b-as-framed was an epic, and the v8 hub split it + authored M7.4b.** The decisive find: `RunManager` is **not wired into `HomeSynapseCore` at all** — `AutomationEngineSubscriber.onEvent` calls `evaluator.evaluate(event)` and ignores the returned matches (comment: "Run initiation is M7.2"). So the whole run lifecycle (RunManager → condition gate → M7.4a executor → `command_issued`) is built + unit-tested but never assembled live; the trigger→run handoff has been deferred since M7.1. The M7.4a `command_dispatch_service` is wired with no live producer.

- **M7.4 split 3-way (P1 — size visible at scoping):** **M7.4b** = the run pipeline goes live (producer: construct the public selector/condition/executor[+serializer]/gate/RunManager into the composition root + the trigger→RunManager handoff as a direct co-located `initiateRun` call; a composition-root test proving a seeded trigger emits `command_issued`→`command_dispatched`). **M7.4c** = confirmation goes live (live `pending_command_ledger` + the `pollExpirations()` tick [expose the package-private method + extend `SharedScheduler`] + the E2E composition-root confirm gate with a synthetic `state_reported`). **M7.4d** = the D2 pure-function-replay CI test (V2-A template) + register D2's canonical INV-id (the §1-reserved governance fold). Each is reviewable + gate-able; closes OR-M7-WIRING at M7.4c, hardens it at M7.4d.
- **M7.4b instruction ISSUE-READY:** `context/instructions/2026-06-27_M7.4b_run-pipeline-go-live_producer-and-trigger-to-run-wiring_coding-instruction.md`. Authored against `b85e1ed` with full STOP-on-Mismatch gates (the exact signatures it depends on), the contracts fixed + Coder-pushback opened on the two genuinely under-determined points.
- **Two findings baked into the instruction (the kind that compile green today and bite later):** (1) **trigger→run derivation** — `evaluate()` returns only `List<AutomationId>` but `initiateRun` needs `matchedTriggers`+`resolvedTargets`; the contract is fixed (one root Run per matched automation, LIVE-only), and whether to re-derive vs. widen `evaluate`'s return is the Coder's call (pushback invited) — a wrong derivation silently corrupts the causal chain. (2) **`PersistenceObjectMapper` is package-private**; `PersistenceFactory` has no serializer accessor → M7.4b adds a public `Function<Map,String>` param-serializer seam (returns `Function`, not `ObjectMapper`, to keep Jackson off persistence's exported API). This wires the **real** persistence mapper (which already round-trips `AttributeValue`) into the executor, closing the M7.4a forward risk in production — with a round-trip test on a real `AttributeValue` param, not a stub.
- **Also carried:** the RunManager in-flight-Run-VT teardown is flagged as a verify-or-justify item (the M7.3 teardown class generalized); the `-Werror` self-check guidance is corrected (`-Werror` is a compiler arg applied by the conventions plugin, not a Gradle CLI flag).
- **State:** core `b85e1ed` (M7.4a committed + pushed; confirm the ci.yml run on it is green to close the gate-of-record loop) · docs `75d0345` · hivemind `278c9e6` + this beat. **Next:** Nick routes M7.4b to the Coder → build → WUCP Phase 2 → M7.4c. Parallel/off-critical-path: the scoped smart-home/ecosystem research session (feeds back to this hub for steering); the skills design-v1 review; the physical bench; AMD-96/Doc-13 currency reviews. Open Risks unchanged (OR-M7-WIRING OPEN, closes at M7.4c).

## 2026-06-27 (beat 14 — v8 hub) — M7.4a RETURNED + GATE-GREEN + WUCP Phase 2 ACCEPT; M7.4b scope sharpened (incl. the producer live-wiring)

**The Claude Code M7.4a build landed and the v8 hub ran WUCP Phase 2, verified against source (not the Completion Report).** Local gate **`./gradlew check` GREEN — 149 tasks** (host-authoritative). The `-Werror` targeted self-check failed only on a malformed command (`-Werror` is a compiler arg, not a Gradle CLI flag — `check` exercises it and passed); guidance defect, not a code defect. The work is **uncommitted** (9 modified + 2 new on `606d410`).

- **Source-verified mechanical claims (truth-hierarchy: code > report).** module-info unchanged both modules + zero event-model file in the diff → **71/41/53 holds structurally**; the paired `close()` teardown is in **BOTH** `HomeSynapseCore` shutdown branches (graceful L766-767 + ungraceful L819-820, null-guarded) — the reverted-M7.3 `@TempDir` trap is closed (and the lifecycle composition-root tests pass GREEN, the real proof); the dispatch subscriber registers Step-3.4b **after projection LIVE** with `coalesceExempt=true`; the D2 LIVE-only guard (`volatile replayMode`, default-false) is the gate-proven M7.3 ledger pattern, sound given the bus's setMode-before-replay-delivery contract; the 5 required subscriber tests + producer tests assert the right things (the §3.11.2 causal chain, the REPLAY no-dispatch proof, the idempotent teardown smoke test).
- **The 5 `[REVIEW]` design choices — all ACCEPT (instruction-invited):** (1) injected `parameterSerializer Function<Map,String>` — sound dependency-inversion, keeps automation JSON-free + no module edge; (2) `onEvent` → `validate(Map.of())` — Tier-1 validator is value-agnostic, params ride durably on `command_issued`; (3) `coalesceExempt=true` — correctness-critical (a coalesced command never dispatches); (4) `CommandDispatchService extends AutoCloseable` — additive, Phase-3-internal interface evolution (consistent with how it was touched in M7.2a-2/M7.3), not a frozen-§8 contract change; (5) executor ctor widened (dropped the dispatch collaborator; added `EntityRegistry`+timeout+serializer) — impl-class ctor, all call sites green.
- **Two tracked FORWARD items (carry, do not lose).** (1) **M7.4b:** the executor is NOT live-wired yet, so the GREEN gate uses a STUB serializer — when the real persistence-`ObjectMapper` serializer is wired, **verify it round-trips the resolved param value types (esp. value-model `AttributeValue`)** or the producer emits broken/empty params and the hero "did it confirm?" renders garbage. (2) **M9:** the subscriber deliberately does not deserialize `command_issued.parameters`, and `command_dispatched` carries none — the M9 adapter must read params off the durable `command_issued` by causation; Tier-2 param-schema validation is future. (Plus a low-priority hardening note: the D2 guard's safety rests on the bus setMode-before-replay invariant shared by both subscribers; a fail-safe default-`true` lifted to the `Subscriber` contract would localize the single most catastrophic failure mode — a real command on replay. Not a blocker.)
- **Milestone truth — OR-M7-WIRING stays OPEN.** M7.4a wired the SUBSCRIBER side + built the producer CODE (unit-tested), but the executor/RunManager pipeline is **not assembled into `HomeSynapseCore`** (deferred since M7.2a-2). So the registered `command_dispatch_service` currently has **no live producer** (idle, harmless), and the pipeline is not live end-to-end. **M7.4b scope therefore = the QUARTET:** (a) wire the RunManager + executor producer into the composition root **with the real serializer**, (b) the live `pending_command_ledger` subscriber + `pollExpirations()` tick **with paired teardown**, (c) the E2E composition-root gate (boot real core, fire a seeded automation, assert issue→confirmation + a queryable causal chain), (d) the D2 pure-function-replay CI test (V2-A harness template). That quartet closes OR-M7-WIRING.
- **Residual (gate-of-record loop, per the 2026-06-27 CI lesson).** M7.4a is uncommitted → no CI run on it yet. On Nick's commit+push, **confirm the ci.yml run is green on the M7.4a commit** (the first real milestone use of CI as the gate of record) before recording M7.4a as CI-gated. Local `check` is green; CI-on-commit is the loop to close.
- **Coder WUCP Phase 1 reconciled (not rewritten — Coder-owned, write-isolated):** coder-handoff M7.4a section (+ Deferred Build Gate flag + next-WU=M7.4b), 2 coder-lessons (inject-the-serializer for a JSON-free producing module; widen-to-AutoCloseable + pin-the-LIVE-guard-with-a-replay-test), and the cross-agent note (the 4 `[REVIEW]`s + the OR-M7-WIRING half-live nuance) are present and coherent.
- **Next.** Nick commits/pushes M7.4a + confirms CI green → the hub grounds (the §1 D2 record + the V2-A replay-harness template + the RunManagerAssembly/composition-root wiring shape) and authors M7.4b. Parallel/off-critical-path: skills git-init + design-v1 review; the physical Wave-1 bench capture; AMD-96 + Doc-13 currency reviews. Mid-Aug go/no-go on the calendar.

## 2026-06-27 (beat 13 — v8 hub launch) — preflight STALE→reconciled; governance-close COMMITTED; two returns reconciled (skills Phase-0 LANDED; M7.4a awaited)

**The v8 mission-control hub launched, superseded v7 (v7 prompt archived → `context/handoff/archive/`), grounded (env-model → truth-hierarchy/pointer-not-copy → V1 record + v6 forward plan + §1 record → 11-check preflight), and executed the mandated STALE→reconcile.** Aggregate **STALE-benign, zero CONFLICTED** — Checks 1/3 = snapshot currency-lag (it still cited core `b296e76` / docs `f54d0e0` / "folds pending commit"); Check 6 = coder-handoff next-unit at pre-M7.4a granularity; Check 9 PASS (mirrors byte-identical). No open deferred build gate.

- **Baselines confirmed at COMMITTED reality (re-derived from source, not propagated forward).** core **`606d410`** (`b2529cc` M7.3+Track-A+gradlew/scripts-+x → `652f9b3`/`604440b` frontend+install-smoke activation → `606d410` build-deb broken-pipe fix; **tree clean** — the 5 hivemind "M" + the `build-deb.sh` "M" are the documented mount-truncation phantoms: host file tails verified intact via the file tools, VM git view suspect, nothing "fixed" off it) · docs **`75d0345`** (AMD-95 RATIFIED w/m 95; Doc 17 LOCKED + AIOT-INV-1; AMD-96 PROPOSED; **170/50**, re-derived from the §17 table) · hivemind **`17f26d9`** (v7 governance `442eb8d` + this v8 prompt). CI genuinely GREEN (149 tasks; ArchUnit guards enforced). Counts **71/41/53** (M7.4a zero-mint).
- **Return B — skills Phase-0 VERIFIED + ACCEPTED → DONE (parallel, off the product critical path).** Nick independently re-checked the return against the filesystem (not on claim) — clean: every tier dir incl. all four `domain/` subdirs carries a `.gitkeep`+README (no empty dir vanishes on `git add`), zero `SKILL.md` anywhere, role skills + the mirror-sync/`.claude` invoke path untouched (purely additive). `nexsys-skills/` stood up: 4-tier skeleton (`disciplines`/`domain`/`harness`/`orchestrators`/`stewardship`/`sync`) + README + provenance `MANIFEST.yaml` (phase-0-skeleton STUB, `skills:` empty until the catalog build, deferred post-V1) + `design/` (the loose `skills-initiative/` docs versioned in, incl. design-v1 the Lock-candidate + `2026-06-27_skills-architecture-design-v1_independent-review_dispatch.md`); `skills-initiative/` de-cluttered (empty); harness daily-check parked. Builds no skill content; does NOT move role-skill sources out of hivemind (that's Phase 1). **NOT git-initialized.** **Residual = Nick:** `git init` nexsys-skills + the prepared `_scratch/_commit-2026-06-27-nexsys-skills-phase0-init.txt`; then **launch the design-v1 independent review** (the dispatch is authored + READY) → co-sign → Lock (ratifies the design, not a build commitment; conform-not-rebuild per §15.5). **Phase 1** (conform the two role skills into `nexsys-skills/orchestrators/` + rewire the mirror-sync & freshness-preflight Check-9 paths) is deferred to its own future session — not started. The now-empty `skills-initiative/` is Nick's to delete (deletion permission-gated; the hub only moved). One-line spine note recorded; the skills track does not clog the hub.
- **Return A — M7.4a NOT yet in the repo (verified against source, not assumed).** No command-pipeline producer/subscriber change on `main`: `StandardCommandDispatchService` still at its M7.2a-2 commit `79e2984` (not yet `implements Subscriber`); no `CommandDispatchAssembly.java`; core tree clean; the only `claude/*` ref is the dead 2026-03-27 `confident-williamson` test-infra branch (0 commits ahead of main). The Claude Code M7.4a build is still in flight / not yet relayed-synced by Nick. **M7.4a is NOT marked done; M7.4b is NOT authored (gated on M7.4a landing GREEN).** This is exactly the "verify the gate is actually green / verify against source before trusting a claimed state" discipline — the masthead's "M7.4a building" was a point-in-time assumption; source says it has not arrived.
- **Next.** Nick relays/syncs the M7.4a Coder return → run `./gradlew check` AND confirm the ci.yml run is green on the M7.4a commit (first real use of CI as the milestone gate) → **GREEN:** WUCP Phase 2 (source-verify 71/41/53; module-info unchanged; the paired `stop()` teardown present in BOTH `HomeSynapseCore` shutdown branches — the reverted-M7.3 trap; the LIVE-only-dispatch + replay-safety + teardown tests; rule any `[REVIEW]`s) → author **M7.4b** (live `pending_command_ledger` subscriber + `pollExpirations()` tick + the E2E composition-root gate + the D2 pure-function-replay CI test, V2-A harness as template; closes OR-M7-WIRING). **RED:** diagnose + issue a gate-fix (the M7.2a-2 / M7.3 pattern). Parallel/off-critical-path: skills git-init + design-v1 review; the physical Wave-1 bench capture; the AMD-96 + Doc-13 currency reviews. Mid-Aug go/no-go (≈ wk 8; engine LIVE / hero on real data / hardware validating / install proven) stays on the calendar.
- **Open Risks unchanged.** OR-M7-WIRING OPEN (closes at M7.4b GREEN); OR-M13-SDNOTIFY standing; no open deferred build gates.

## 2026-06-26 (beat 12 — v7 hub) — §3 consolidated decision pass RULED; governance CLOSED; M7.4a ISSUED

**The v7 mission-control hub launched, grounded (env-model → V1 record → v6 forward plan → §1 record → 11-check preflight = STALE-benign, zero CONFLICTED; reconciled the snapshot/cross-agent-notes to beat-11; Check 9 PASS), ran the §3 consolidated decision pass with Nick, and folded the co-signed outcomes.** Baselines confirmed (core `b296e76` / docs `f54d0e0` / hivemind `cd8b046`). The harmless 0-byte `.git/index.lock.cleared-by-cowork-2026-06-26` leftover flagged to Nick (no active lock; non-blocking).

- **§3 decision pass — Nick ruled all four groups as recommended** (the assembled 12 decisions, each source-verified against the two review returns + the Wave-1 bench report + the two lane returns; the hub assembled + recommended, did not decide unilaterally).
- **A1 — AMD-95 RATIFIED (folded; watermark AMD-94 → AMD-95).** Review = RATIFY-WITH-EDITS, all 8 frozen-signature claims VERIFIED EXACT at `5363347`. Applied: Doc 07 §3.11.1 (affirm event-driven + co-located-for-MVP + current-vs-target note; the two-hop `EntityRegistry→DeviceRegistry` resolution; `handle(CommandEnvelope)`), §3.11.2 (capability-sourced `CommandDefinition.expectedOutcomes()`; `ConfirmationMode.DISABLED` gating; `String`→`AttributeValue` coercion), §4.3 record comment, §7 interaction table, §16 D16, revision history; AMD-90 §2.1 inline marker + §11 supersede-in-part; nav-index watermark + AMD-94/95 rows + Doc 07/15/16/17 docs-table rows. ZERO event/module-info/source change. **Unblocks M7.4a.**
- **A2 — D-07-A deferred to M8** (the 9th action permit: `RepeatAction` unbuilt, `ActivateSceneAction` never renamed; 8 not 9). Forward/unbuilt currency note in AMD-90 §11; re-home to M8. Off the M7.4 critical path.
- **A3 — Doc 17 LOCKED + AIOT-INV-1 minted.** Review = new-doc cut RIGHT (conditional on S1) / RATIFY-WITH-EDITS. Folded S1 (the §0.1 owns/restates/coordinates boundary) + S2 + E1 (INV-LF-05/INV-PD-08/INV-CS-05/INV-ES-02 citations) + E2–E5 + E3 (the structural proposer-only port). Minted **AIOT-INV-1** (new `INV-AC` category — register §0.3/§17/§18/§50; invariants 169→170, categories 49→50; cites INV-SA-04 + AMD-90-INV-01 + D2-pending-registration-at-M7.4b); kept AIOT-INV-2/3 as principles; sequenced AX-7 before AI-authoring; adopted the honest claim language; commissioned the Q1 patent search. Watermark stays AMD-95 (a new-doc Lock; the AMD-95 amendment set it).
- **B (Wave-1 device-model reconciliation) — recorded + AMD-96 PROPOSED.** Decision record `context/decisions/2026-06-26_wave1-device-model-reconciliation_decision-record.md` (B4 White/CT + Kelvin-canonical; B5 EZSP-v14 currency + an M9 v14/ASH test item; B6 hero binds to `occupancy.occupied`; B7 keep the `[REF]` scaffold). The Doc 02/08 Locked-body edits ride **AMD-96 (PROPOSED)** through a source-verifying currency review (the hub does not self-ratify Locked-doc edits). B6 is propagation only (no AMD).
- **C (lane-integration) — note + the v1.1 contract field.** `context/decisions/2026-06-26_lane-integration-decisions_note.md`. C8 additive `name` field **folded into the v1.1 contract** (optional; Core returns when set, omits when unset; fills with config/M9, not M7.4). C9 Doc 13 WS→poll currency addendum (next currency-amendment item). C10 the two CI file drops = **Nick** (`.github/workflows/`; activates gate #4). C11 E2 bind-host/port = a small **Core WU ahead of the M9 wizard** (+E1 arch matrix, E3 loopback /health, E6 token 0600; E4@M13, E5/E7 no-action). C12 FRONTEND_DOCTRINE.md → shared home.
- **M7.4a AUTHORED (ISSUE-READY).** `context/instructions/2026-06-26_M7.4a_command-issued-producer-and-co-located-dispatch-subscriber_coding-instruction.md` — the `command_issued` producer (off `StandardActionExecutor`'s dispatch) + the co-located `command_dispatch_service` subscriber (replacing the in-process call) + a **paired `stop()` teardown** (the reverted-M7.3 lesson). D2 (LIVE-only dispatch; unit-level replay proof) + D3 (additive versioning) as constraints; the **E2E composition-root gate + the D2 replay CI test are fenced to M7.4b**. Authored against the AMD-95-reconciled §3.11.
- **Commit:** the v7 §3 folds are ON DISK, uncommitted — the commit-message file is at the ClaudeFolder root (`_commit-2026-06-26g-v7-governance-close.txt`; bang-free/backtick-free, `git commit -F`). Nick commits (docs = the AMD-95 fold + Doc 17 Lock + AMD-96 + the register/nav-index; hivemind = the decision records + the lane note + M7.4a + this spine reconcile), drops the two CI files, mirror-syncs if any skill source changed (none this session), and routes M7.4a to the Coder.
- **Next:** Nick commits + routes M7.4a → Coder builds M7.4a → WUCP Phase 2 → M7.4b (live ledger + tick + E2E gate + D2 replay CI test). Parallel: the Doc 13 + AMD-96 currency reviews; the bench physical capture; the E2 Core hook ahead of M9. The mid-August go/no-go stays on the calendar.

## 2026-06-26 (beat 11) — Track-A fleet first returns (frontend + distribution + bench Session C); decisions consolidated; v7 handoff authored

**The development fleet's first beat returned.** All three write-isolated (frontend/distribution to their trees, bench to `project-knowledge/device-corpus/`); none touched the spine or design docs.

- **Frontend-dev (first beat DONE; `web-ui/dashboard/`, 65 files).** A buildable, type-clean Preact+TS SPA: shell + AB-1 auth + the token design system + device/health/event views + **both hero halves** (why-did / why-didn't / sent-vs-confirmed), all over the **frozen v1.1 contract** with a one-switch mock/real seam, 1–2s polling (no WS). Verified GREEN: tsc(strict)/eslint/contract-check + 32 executed logic assertions; Vite build + Vitest run in CI (esbuild segfaults in-sandbox — env limit). Bundle est. ~22–26 KB vs the 100 KB budget. **Hub items:** (1) Doc 13 WS→poll currency note (§1/§3.5/§3.8/§8.2 + parts of §4/§6/§11); (2) CI wiring — drop `web-ui/dashboard/ci/frontend.yml` into `.github/workflows/`; (3) contract friction — entity reads carry no display `name` (recommend an additive field; Nick's public-API call); (4) `FRONTEND_DOCTRINE.md` promotion candidate (shared home + a future `nexsys-frontend` skill). A visual preview (`dashboard-visual-preview.html`) awaits Nick's taste pass on the hero.
- **Distribution (skeleton DONE; `homesynapse-core/distribution/`, 23 source files + `.gitignore`'d build artifacts).** The full one-command-install path around the *current* artifact: Gradle→jlink image → least-privilege systemd unit (loopback, `Restart` per the ExitCode contract, `ExecStartPost` HTTP health probe; `Type=notify`/`WatchdogSec` staged-commented for M13) → `.deb` + dpkg-free `install.sh` (service user + state dirs + prints the pairing-token path) → non-destructive `update` → container **install-smoke = mid-Aug gate #4**. Source-verified (`docs/boot-contract-map.md`); 2 real probe bugs caught+fixed in-sandbox; jlink build is CI's job. **Escalations E1–E8** (`distribution/docs/escalations.md`): **E1** target arch (rec CI matrix arm64+amd64); **E2** bind-host/port operator-configurable in Core (the headless-Pi + wizard prerequisite — rec prioritize ahead of the M9 wizard); E3 loopback `/health`; E4 wire `LinuxSystemPaths`@M13; E5 jlink-in-Gradle; E6 token `0600`; E7 repo-home (decided: in-repo); **E8** CI wiring — drop `install-smoke.yml` into `.github/workflows/`. Note: don't commit `distribution/deb/build/` + `image/build/` (mock artifacts; `.gitignore`'d).
- **Bench Session C (knowledge layer + verdicts DONE; PHYSICAL CAPTURE PENDING).** An automated session can't run physical silicon, so it delivered the full knowledge layer (every field `[REF]` documented or `[CONFIRM-ON-BENCH]` live-only) + the **capture-independent verdicts/fingerprint/escalations**; the **real silicon capture (the M9 acceptance ground-truth + the M7.4 E2E motion fixture) remains Nick's physical bench step** (a fast confirm/correct). Coordinator fingerprint: MG24/EZSP, `0x10C4:0xEA60`, **EmberZNet 8.0.2 = EZSP v14**, INV-CE-04 auto-detect recorded. **Escalations:** **ESC-W1-COORD-01** (Doc 08 §3.3 describes EZSP v13/EmberZNet 7.4 + names only MG21; real silicon is v14/8.x — a currency amendment + an M9 v14-negotiation/ASH-timeout test item; medium, not hero-blocking); **ESC-W1-HUE-01** (full color is unrepresentable in the MVP model — no Doc 08 §3.5 full-color row; Doc 02 §3.6 reserves `color_hs`/`color_xy` post-MVP while §3.10 advertises them = internal inconsistency; + a mireds↔Kelvin canonical-unit drift; **decision: scope V1 to White/CT vs pull color forward** — hero needs On/Off only); **ESC-W1-SNZB03P-01** (advisory — the SNZB-03P is **Occupancy `0x0406` → `occupancy`, NOT IAS/`motion`**; the hero rule + M9 + the frontend hero must bind to `occupancy.occupied`; profile-match `(eWeLink, SNZB-03P)`, not "Sonoff motion → IAS"). The `[REF]`-scaffold-vs-blank corpus choice is Nick's (rec keep).
- **Cross-session coherence:** the bench's `occupancy` binding aligns with V2-B's converter-DB hero mapping (SNZB-03P = `occupancy`+`battery` `binary_sensor`) and must align with the frontend hero mock + M9 — one binding, three lanes. The full-color scope decision is cross-cutting (Doc 02/08 + the converter-DB Hue mapping + the frontend device view + M9).
- **Consolidation: the v6 hub is context-heavy; a v7 handoff is authored** (`context/handoff/2026-06-26_PM-mission-control_v7_orchestrator_session_prompt.md`) carrying the clean spine + the consolidated decision set + the forward work (the governance close-out, M7.4a, the bench/device-model amendments, the lane next-beats, the physical bench run, the mid-Aug go/no-go). The v5→v6 pattern, repeated at saturation. **The full decision set Nick rules is the v7 prompt §3 + the consolidation plan below.**

## 2026-06-26 (beat 10) — fan-out v2 returned (V2-A/B/C); all fold cleanly; four decisions pending; index.lock cleared

**Nick ran the fan-out v2 research wave; the v6 hub reconciles it** (the meta-session's reconciliation memo: `context/assessments/2026-06-26_fanout-v2_hub-reconciliation_PM-memo.md`). All three write-isolated sessions returned and **fold cleanly — none changes V1 scope; the mid-Aug go/no-go + the Core critical path are untouched.** The verification gate PASSED: write-isolation mtime-proven (the only fan-out artifacts are the 3 reports + the `spike/` tree; the spine `M` markers are the known post-commit mount phantom), V2-A numbers internally sane, V2-C citations re-verified (Blueprint-First arXiv 2508.02721 confirmed; the bad SafeGate citation isolated).

- **V2-A (Pi spike) — D1 CONFIRMED, D2 PASS, D4 sized.** Co-located dispatch hop **≈ 1.9 ns/op** (0.19 % of a 1 µs dispatch; 3–5 orders below actuation) — the latency objection is empirically closed. **1e6-event replay → zero side-effects** (the M7.4b D2 CI-gate template). D4 curve: ~488 B/event, knee 1e6–1e7, snapshot to keep the replay tail ≤ ~1e6; per-scope+priority retention. **Storage figures carry a sandbox-FUSE caveat → FREEZE D4 thresholds on a Session D Pi re-run** (the harness ships Pi-ready). Empirical-validation addendum folded into the §1 record.
- **V2-B (converter-DB pipeline) — M9-prep delivered.** Offline, human-gated, version-pinned: fetch(pinned commit)→parse/classify→IR→map→[HUMAN GATE]→emit, targeting Doc 08 §3.6's `DeviceProfile` registry + Doc 02 §3.6 rows (attributed data, never upstream code/dependency). D5 cleavage at Stage-2: standard-ZCL majority (~60 %) = pure data via a rule table; code-shaped tail (~40 %: `fromZigbee`/`toZigbee`, Tuya `0xEF00`, Xiaomi) = re-expressed via the curated/community path. Hero mappings (Hue=`light`, SNZB-03P=`binary_sensor`) **bench-confirm PENDING Session C**. Provenance = consolidated NOTICE from the ingest manifest + pinned-commit/semver/license-at-ingest. **M9-gating:** pipeline shape + standard-majority rule table + Session C HERO confirm = before scoping; legal items 1–3 = parallel soft-gate; Tuya/Xiaomi codec + community + re-ingest = deferrable.
- **V2-C (AIoT safety frame) — researched; the moat sharpened.** Planner→verifier→deterministic-executor is the 2026 consensus (Blueprint-First "deterministic engine owns the path; the LLM never decides it"). **The moat (independent find): AI-as-author emits `AutomationComponent`s that expand into the sealed permits (INV-SA-01), so the `AutomationLinter` statically rejects malformed AI output at load — before it runs; every surveyed system verifies plan *behavior*, none type-checks the authored *form* against a sealed schema, because none has one.** **E3 structural proposer-only port** specified (composition-root, actuation structurally absent; mock-AI-can't-reach-dispatch CI test) → upgrades AIOT-INV-1 from asserted → **CI-testable on both axes** (D2 temporal + E3 actor). **AX-7 confirmed a hard prerequisite before the AI-authoring milestone** (answers Doc 17 Q4). Honest claim language set: defensible = "AI is structurally a proposer, statically verified, durably auditable"; **needs counsel + the Q1 patent search** = "safest AIoT" / "can never misfire" / "formally verified" (we do static type-checking, not model-checking).
- **V1-non-preclusion (H-1..H-5): all PASS** — V1 builds no AI path; every seam is additive over the sealed model + the generic inbound definition/command path. **Tracked forward dependency: H-3** (if authoring-provenance must ever be event-resident → a formal AMD; no V1 action). **H-4** (semantic-policy constraints, e.g. "AI may never propose unlocking an exterior door without confirmation") = candidate to NAME as a reserved sub-seam in the Doc 17 fold.
- **Source-integrity (§6):** the 2026-06-23 prior-art study cited "SafeGate" (arXiv 2604.05427) as FACT-tier; it is unverifiable. Already pre-hedged by the prior-art PM assessment; V2-C resolved it (4 of 5 verified, SafeGate isolated). Action: quarantine/strike the SafeGate citation at the next prior-art-study touch. Integrity posture is now *better* (the guardrail fired).
- **Environment:** a stranded `nexsys-hivemind/.git/index.lock` (FUSE blocked unlink) **was renamed aside by the hub** (`index.lock.cleared-by-cowork-2026-06-26` — Nick deletes when convenient; git unblocked). The V2-A `spike/` tree is the throwaway (standing `git rm` after the Pi re-run; keep `src`+`run.sh`+`README`+`lib` until then; the `dbtmp/*.db*` + `.class` leftovers are FUSE-blocked-unlink junk the disposal absorbs).
- **Four decisions pending Nick's co-sign** (see the reconciliation memo §10): (1) D1/D4 fold + schedule the **Session D Pi re-run** to freeze D4; (2) **M9-scoping unblocked** + commission the LOW-risk legal spot-check (V2-B's 4-question list) + confirm Session C = the HERO-mapping gate; (3) route the **Doc 17 fold** with V2-C substantiation — mint AIOT-INV-1 with the E3 port, sequence AX-7 before AI-authoring, adopt the honest claim language, commission the Q1 patent search before any superlative; (4) approve the **SafeGate quarantine**.

## 2026-06-26 (beat 9) — fan-out return reconciled (Sessions A/B done; C/D reframed); D5 RATIFIED; AMD-95 + doc-currency Cowork review authored

**Nick relayed the 2026-06-23 parallel-fan-out return (run as a write-isolated PM session); the v6 hub reconciles it.** The fan-out session ran the mandatory preflight (**AGGREGATE PASS** — it independently re-derived the same state; two cosmetic masthead `last-verified` re-stamps flagged, applied here), validated **every** factual assertion in the dispatch brief against primary sources + source (**zero false assertions**), and — correctly catching that **§1 ratified 2026-06-25, after the 2026-06-23 brief** — executed only what stayed live: **Session A in full** (the one session still gating D5), **Session B as a focused delta**, and **escalated C + reframed D** to the bench rather than fabricate hardware data. Three additive, write-isolated returns landed in `context/assessments/` (Session A, Session B, the reconciliation memo `2026-06-26_v6-fanout-validation-and-dispatch-reconciliation_PM-memo.md`); no spine file was touched by that session.

- **Session A (converter-DB license/feasibility) — DONE; D5 READY TO RATIFY (pending Nick's co-sign).** Licenses verified at the file level: `zigbee-herdsman-converters` **MIT**, `zigbee-herdsman` **MIT**, `zha-device-handlers` (`zha-quirks`) **Apache-2.0** — all permissive, attribution/NOTICE only, **no copyleft / no source-disclosure / no keep-separable**. The GPL-3.0 is the **Z2M application** (a different package — NOT consumed). Recommendation (CONFIDENCE: HIGH license / MED-HIGH technical fit): **adapt-the-data** (embed the transformed, attributed `zigbeeModel`/`fingerprint` + `exposes` capability data — maps onto our ZCL model) **+ curated-subset/community fallback** for the `fromZigbee`/`toZigbee` transform long tail (Tuya `0xEF00`, Xiaomi); **reject runtime-interop (B)** (Node co-process; GPL-entanglement + Pi/trust-brand) **and rebuild (D)** (multi-year, no payoff). A **LOW-risk legal spot-check** (Apache-2.0 NOTICE/patent mechanics at enterprise-sublicensing scale; TS→Java derivative-work form) gates the **embed mechanics**, not the decision. **→ D5 RATIFIED 2026-06-26 (Nick co-signed): adapt-the-data + curated-subset fallback; reject runtime-interop/rebuild; legal spot-check commissioned; bench validates the `exposes`→capability mapping this week. M9 scoping unblocks.**
- **Session B (why-not differentiator/moat) — DONE (delta).** Platform sweep was ~80% pre-covered (06-21 + 06-23 research); the genuine delta = the patent/IP scan (no shipping product or patent covers **durable** "why-not" explainability; but command-confirmation **as a mechanic** is well-trodden prior art) → **claim the durable plain-language projection, not confirmation itself**; counsel before any "unique/only" language.
- **Session C (bench) — premises VALIDATED, escalated to Nick's bench (Nick-driven).** De-risk notes folded into the bench brief: **lead with ZHA/bellows over Zigbee2MQTT for the EZSP/MG24 path** (Z2M Ember support is the younger driver); **record the EmberZNet/EZSP version FIRST** (the version-mismatch hard-failure class); MG24 (EFR32MG24) ≠ the older ZBDongle-E (EFR32MG21); Hue factory-reset/Touchlink if it won't join. No corpus fabricated.
- **Session D (Pi dispatch-latency + log-growth) — REFRAMED → the bench Pi-performance spike.** Latency half is now **confirmatory** (D1 ratified); log-growth half still **sizes D4**; both need Pi-class hardware + a Coder-built harness → run as the single bench Pi-performance spike, non-blocking (M7.4a proceeds on the ratified co-location default).
- **AMD-95 + documentation-currency Cowork review authored (Nick's redirect — Cowork, not DOCS; all-documentation):** `context/handoff/2026-06-26_AMD-95-and-documentation-currency-review_Cowork_session_prompt.md`. Tier 1 = AMD-95 to ratify-readiness (source-verify the frozen signatures at `5363347`). Tier 2 = a **doc-vs-source currency sweep** across the docs surface (design 00-17 + amendments + governance), prioritized by what M7.4+ author against, to catch the next §3.11-class drift before it bites. Amendments-scope call made explicit: IN scope as currency-cross-check targets (propose notes/new AMDs; never edit ratified bodies).
- **Cosmetic re-stamps applied (Check 1/4):** the snapshot + backlog masthead `last-verified` lines refreshed to 06-25/06-26 v6 (they trailed at 06-22/v5). No state change.

## 2026-06-25 (beat 8 — v6 hub launch) — preflight reconciled; §1 deeper-M7 architecture beat RATIFIED; the Doc 17 AIoT/Cloud readiness beat DRAFTED

**The v6 mission-control hub launched (supersedes the token-saturated v5) and ran its first substantive product: the §1 deeper-M7 automation-architecture deliberation → Nick co-signed the rulings.** Ground: env-model → V1 record → 11-check preflight = **STALE-benign, zero CONFLICTED**. Core HEAD reconciled to **`5363347`** (M7.3 DELIVERED GREEN; live wiring deferred → M7.4 — the SHA the v6 masthead said reconciles here), docs `e47f01e` (Doc 16 LOCKED, AMD-94, 169/49), hivemind advanced `04c0a54`→`837e743` (v6 handoff → research/fan-out → Wave-1 received). **Check 9 CLEAR** (both skill mirrors byte-identical — v5 sync landed). Source round-trip clean (M7.3 types resolve at HEAD; 71/41/53 zero-mint; no fabrication). Snapshot commit-pointer fields stamped to the true HEADs.

- **§1 decision record RATIFIED (Nick co-signed 2026-06-25):** `context/decisions/2026-06-25_deeper-M7-automation-architecture_decision-record.md`. **D1 — command-pipeline = logical event-driven, physically co-located** (the executor emits `command_issued`; `command_dispatch_service` becomes a real co-located subscriber on it — supersedes the v5 "emit + in-process" hybrid lean; pure-in-process-no-`command_issued` stays rejected). Reasoning recorded: the hybrid splits the source of truth (log records / side-channel performs → can diverge); full event-driven makes the log the single source of truth for dispatch. Three deciders: (1) replay-safety becomes structural (a subscriber acts only in LIVE → cannot dispatch on replay → D2 automatic; the hybrid needs the error-prone hand-guard that bit M7.3); (2) scales additively across process/host/cloud/AI; (3) AMD-31 ordering free from global position. Latency (hybrid's only edge) is neutralized by co-location; **Session D** is the empirical Pi confirmation. **D2 — pure-function-replay invariant** (no external/device side-effect on replay; first-class + CI test; formal id-registration is a forward governance action). **D3 — additive event versioning** (`version` field + upcaster + full-history rebuild test). **D4 — log retention/compaction/snapshot discipline** (per-scope; sized by Session D's Pi log-growth numbers). **D5 — converter-DB leverage direction = HELD PENDING Session A** (license/feasibility; do NOT ratify ahead of the data; gates M9). Q2 (event log = universal substrate) + Q5 (local↔cloud seam) RATIFIED as confirmations.
- **Doc 17 "AIoT + Cloud Readiness" beat DRAFTED (NOT self-ratified):** `homesynapse-core-docs/design/17-aiot-and-cloud-readiness.md` — review-candidate, runs the formal DOCS-review→Nick-co-sign→Lock pipeline (the Doc 16 path; watermark would stay AMD-94 at Lock). Scoping call stated at top (lean **new doc** vs Doc-16 extension — Nick rules at review). Names the AIoT direction + the four AI seams; states **"AI proposes, the deterministic engine disposes"** as a first-class safety principle (the prior-art emerging consensus — potential "safest AIoT" moat); pins **cloud as strictly additive** (the log replicates outward, never a local dependency); ties the reserved seams — federation INV-SA-02, audit Doc 16 §3.3, component-authoring AX-7, crypto-shred INV-PD-07 — **plus a new cloud-replication seam + a CRA/PSTI SBOM-update seam** — to the vision as explicitly non-precluding. Three PROPOSED `[AIOT-INV-n]` candidates for the review (not minted).
- **Forward sequence (serial, hub-owned):** §8.C — the **Doc 07 §3.11 / AMD-90 doc-vs-source reconciliation**, now unblocked and shaped by D1 (adopt the event-driven shape; reconcile against the frozen source the M7.3 [REVIEW]s mapped) → then **M7.4a** (`command_issued` producer + co-located dispatch subscriber, paired `stop()` teardown) → **M7.4b** (live ledger subscriber + `pollExpirations` tick + E2E composition-root gate + the D2 replay CI test). D3/D4 ride in as constraints. **Bench:** bundle Session D (Pi dispatch-latency + log-growth) onto the live Wave-1 bench. **Session A** (converter-DB license) lands before M9 scoping → resolves D5.
- **§8.C authored (same session, after the §1 co-sign):** **AMD-95 (PROPOSED)** — `homesynapse-core-docs/design/amendments/AMD-95_Command-Pipeline_Currency_Event-Driven-Co-Located.md` — the Doc 07 §3.11 / AMD-90 command-pipeline currency reconciliation. Affirms §3.11.1's event-driven shape (D1) + adds co-located-for-MVP + the current-vs-target note (in-process until M7.4 wires it); corrects the three drifts vs frozen source at `5363347` (ExpectationFactory → capability `CommandDefinition.expectedOutcomes()` via EntityRegistry; `CommandAction.confirmation` 5th component → capability `ConfirmationPolicy.mode()`/`ConfirmationMode.DISABLED`, superseding AMD-90 §2.1 in part; `command_issued` carries no expectation; `String`→`AttributeValue` coercion). ZERO source/event/module-info change; affirms AMD-90-INV-01 (no-retry). **NOT self-ratified** — runs DOCS review→Nick co-sign; watermark AMD-94→95 at ratification. Authors BEFORE M7.4a.
- **Doc 17 review-dispatch prompt authored:** `context/handoff/2026-06-25_Doc17_independent-DOCS-review_session_prompt.md` — context-isolated independent DOCS review (the Doc 16 path): rule the §0 scoping call (new Doc 17 vs Doc-16 extension), assess the `[AIOT-INV-1..3]` candidates (mint-at-Lock vs principles), source-verify the cited invariant ids (INV-LF-01/02, INV-RF-01, INV-ES-06 esp.). Nick launches it in parallel (off the M7.4 critical path).
- **Commits (two, clean — §1 ratification lands FIRST per the env-model):** **Commit 1 (the §1 ratification):** hivemind = the §1 decision record + spine reconcile [snapshot SHA stamp + the beat-8 entry above + backlog M7.4 row + OR-M7-WIRING] ; docs = the Doc 17 DRAFT. **Commit 2 (§8.C + the review prompt):** docs = AMD-95 PROPOSED ; hivemind = the Doc 17 review-dispatch prompt + this addendum. No core change this beat. Commit message: `_commit-message-2026-06-25-v6-S1-ratify.txt` (both commits laid out).
- **Next:** the AMD-95 DOCS-review prompt (the §8.C companion) authors next if Nick wants it bundled, then **M7.4a** authoring (the `command_issued` producer + co-located dispatch subscriber) once §8.C ratifies — or M7.4a authors in parallel against the reconciled design at Nick's call. Bench: Session A + Session C dispatch now (Nick); Session B + D queue (D → a dedicated Coder slot, non-blocking).

## 2026-06-22 (beat 7) — M7.3 ledger DELIVERED GREEN + gate-fix (live wiring deferred) + WUCP Phase 2 APPROVE; "live spine first" ruled

**The PendingCommandLedger landed as a unit; its live wiring was premature and is deferred.** The Coder built `StandardPendingCommandLedger` + `PendingCommandLedgerAssembly` + `PendingCommandLedgerTest` + `ConfirmationPolicyGateTest` from the M7.3 instruction — the correlation FSM (5 `PendingStatus` states), dual index, REPLAY rebuild, crash-recovery by idempotency class, Clock-injected `pollExpirations`, publish-outside-lock. Per AMD-92 it minted ZERO event types (counts stay 71/41/53), no module-info change. The first `./gradlew check` came back RED: the instruction (mine) over-reached — it told the Coder to wire the live `pending_command_ledger` subscriber into the running composition root, but its producers (`command_issued`) and the `pollExpirations` tick are not built. That subscriber registered with no `stop()` teardown → its SQLite read connection held the temp DB open → JUnit `@TempDir` cleanup failed on Windows → all 15 composition-root tests failed at teardown (the ledger's own `core:automation` tests passed). Diagnosed from the test-results XML. **Fix: defer the live wiring** — reverted the 19-line composition-root block (`HomeSynapseCore` byte-identical to M7.2b); re-run **GREEN (149 tasks)**.

- **WUCP Phase 2 → APPROVE.** Counts confirmed 71/41/53 (no mint); no module-info change. **6 source-drift [REVIEW]s ACCEPT** — the Coder correctly built against the frozen tree, not the prose: (1) Expectation from the capability's `CommandDefinition.expectedOutcomes()` (no `ExpectationFactory` impl exists; `command_issued` carries no expectation); (2) OPTIMISTIC bypass = the capability's `ConfirmationPolicy.mode()==DISABLED` (AMD-90's `CommandAction` enum doesn't exist; `CommandAction` is still the frozen 4-field record); (3) String→`AttributeValue` coercion; (4) producers not yet wired; (5) no advancer change (the ledger publishes — AMD-41 forbids that inside a read-tx handler — DQ-3's escape hatch); (6) package-private impl + public assembly. None need an amendment; all six are evidence of the doc-vs-source drift below.
- **DECISION (Nick): "live spine first."** M7.3 delivered GREEN-but-inert; the engine is built breadth-first while the live end-to-end path lags. **Re-sequenced: M7.4 — command-pipeline live integration (the `command_issued` producer + the dispatch/ledger live subscribers WITH teardown + the `pollExpirations` `SharedScheduler` tick + an E2E composition-root gate test) is the NEXT Core slot, BEFORE the thin causal-read-API** — so the read-API + hero render on a real pipeline, de-risking the mid-Aug go/no-go early.
- **Open Risks: NEW OR-M7-WIRING — the command-pipeline live wiring is deferred** (M7.1's wiring rode AB-3; M7.3's rides M7.4). Owner: M7.4. Resolution condition: a GREEN E2E test that boots the real composition root and proves command issue→confirmation + a queryable causal chain. Until M7.4, the ledger + dispatch are unit-tested but NOT live in the running system. OR-M13-SDNOTIFY unchanged. No open deferred build gates (M7.3 GREEN).
- **NEW governance item — Doc 07 §3.11 / AMD-90 doc-vs-source currency:** the docs describe a command pipeline (an `ExpectationFactory`, `ConfirmationPolicy` on `CommandAction`, `command_issued` carrying the expectation) the code never fully built — the source of the 6 [REVIEW]s and the recurring wiring surprises. Reconcile via a currency amendment, paired with M7.4 so it is authored against reality.
- **Commits:** core = the M7.3 milestone (ledger + tests + MODULE_CONTEXT; `HomeSynapseCore` unchanged). hivemind = this closeout (the Coder's coder-handoff/coder-lessons/cross-agent-notes + this spine reconciliation + the two pm-lessons + the backlog M7.4 re-sequence). Core SHA reconciles at Nick's commit.

## 2026-06-22 (beat 6) — M7.2b GREEN (first pass) + WUCP Phase 2 APPROVE — the action model is FROZEN

**M7.2b landed GREEN on the first gate run** (`./gradlew check` 149 tasks, zero gate-fix rounds) — the inline spotless self-step + the record-collision STOP-check baked into the instruction worked. Delivered: a minimal computed-param resolution seam (`ComputedValue` sealed, 3 permits, resolved at run-init against the captured AMD-03 snapshot before the frozen `ActionExecutor`); the run-coupled fail-closed-read terminal (snapshot read throws → `FAILED` + recorded reason; both `automation_triggered`+`automation_completed` published, C1 preserved); no-engine-retry frozen (AMD-90-INV-01). **No new event type — counts 71/41/53; module-info UNCHANGED** (only automation files touched).

- **WUCP Phase 2 = APPROVE.** Spot-verified: the 3 deviations, the package-private decision (AttributeValue stays off the exported API → module-info unchanged), the test contracts (computed-param-before-executor, resolves-against-captured-snapshot, fail-closed→FAILED+reason, ComputedValue determinism/typed-absent/bounded-fold).
- **[REVIEW] deviations → all ACCEPT (3 caught real instruction tensions — good evidence-based pushback):** (1) package-private types (vs the illustrative `public`) to honor the module-info-unchanged pin; (2) `AggregateValue(Set<EntityId>)` not `Selector` (Selector resolution = I/O, contradicts C-SA-2); (3) absent → `DegradedAttributeValue` typed-absent (valid input must not throw). [INFO]: `ComputedValueContext.resolutionTime` carried but unconsumed by the 3 V1 permits (the {time} seam for future time-based values).
- **PM lesson (fold at the hygiene pass):** my instruction carried two internal tensions the Coder correctly resolved — an illustrative `public` signature vs the binding module-info pin, and `AggregateValue(Selector)` vs the stated no-I/O C-SA-2. Instruction internal-consistency: illustrative signatures must not contradict the binding constraints.
- **Forward pointer:** when the component model + YAML loader land (post-V1), `ComputedValue` may need to go public + `requires transitive value` — a deliberate future change; package-private is correct for V1 (no external consumer).
- **Sequence:** **M7.3 (PendingCommandLedger) is the next Core slot** — the confirmed/unconfirmed hero half (Option A); its instruction authors next.
- **Commits:** core = the M7.2b milestone (GREEN); hivemind = this closeout + the Coder's coder-handoff/cross-agent-notes (+ coder-lessons — confirm it shows the M7.2b append host-side; the VM view lagged it). Open Risks unchanged.

## 2026-06-21 (beat 5) — M7.2a-2 GREEN + WUCP Phase 2 APPROVE (gate-fix round 1)

**M7.2a-2 (execution/dispatch) landed GREEN and is closed out — the Core differentiator engine now ACTS.** The first deferred `./gradlew check` came back RED: **8 compile errors, two Java record component/static-factory name collisions** — `ValidationResult(boolean valid)` + a `static valid()` factory (new code), and `ModeDecision(admit/restartCancel/enqueue)` + same-named static factories (a **regression** — 2a-2 needlessly refactored 2a-1's working canonical-constructor form). The Coder's sandbox can't build, so the deferred gate caught it as designed; the in-session LLM "adversarial verification: clean" did NOT (it reads plausibility, not `javac`'s record rules). The hub did a **full host-side pre-review of all 2a-2 main source** and confirmed those two collisions were the *only* main-compile blockers (every event-construction site, the widened seams, the 6-arg assembly wiring, the 4 new event records all type-checked). Gate-fix round 1 (`context/instructions/2026-06-21_M7.2a-2_gate-fix-round-1_coding-instruction.md`) fixed exactly those two, minimal-diff → **`./gradlew check` GREEN (149 tasks), no round 2.**

- **WUCP Phase 2 = APPROVE.** Counts **re-derived from source 71/41/53** (EventTypes / `CORE_PRODUCTION_EVENT_CLASSES` / `EventCategoryMapping.TABLE`; pins green). **module-infos UNCHANGED** both modules. The **four DP-A carries** are present + contract-tested: `queued_sequentialSingleFlightDrain` (one-at-a-time, distinct from the PARALLEL `queue_full` test), `dedupClaimedBeforeGate`, real snapshot `viewPosition` (not 0), real action/command tally. 5 Tier-1 actions + AMD-92 rows 4/5/6/9 + the 4 explainability event records shipped.
- **[REVIEW] deviations → all ACCEPT:** widened `ActionExecutor`/`RunConditionGate` (need the triggering `EventEnvelope` for causal stamping; no JPMS edge); two-hop entity→device→integration resolution (no `getIntegrationForEntity`); command-causality threading best-effort → M7.3.
- **Tracked (non-blocking):** [INFO] `AllEventClasses.java:19` stale Javadoc ("37" core records → should be 41; comment-only, inconsistent with line 37 of the same file — route a one-line fix to the Coder); **EmittedDomainEvent→DegradedEvent** replay limitation (write-side correct; custom-event replay-as-self = future WU); **EvaluatedEntityState.lastChangedByEventId** substrate gap (state-store doesn't track the last-changing event id yet).
- **Lessons (skill-source fold PENDING — sets Check 9 STALE until Nick's mirror sync):** record component/static-factory STOP-check (coder-lessons — Coder recorded it; elevate to the §5 trap list); "LLM in-session verification is not a gate — CI/compile is the source of truth" (pm-lesson, reinforces CI-as-gate-of-record); "don't refactor working code without cause" (the ModeDecision regression); plus the v3-deferred folds (spotless self-step + the reconcile-before-launch pm-lesson; both SKILL.md mastheads).
- **Sequence unblocked:** M7.2a-2 GREEN + co-signed → **M7.2b is UNGATED**; its coding instruction authors next; **M7.3 queued after M7.2b** (Option A). Open Risks unchanged.
- **Commits:** core = the M7.2a-2 milestone (homesynapse-core; SHA reconciles into the snapshot next). hivemind = this beat (commit plan handed to Nick). The Coder wrote `coder-handoff`/`coder-lessons`/`cross-agent-notes` (write-isolated); the hub wrote the spine + decisions + the gate-fix instruction.

## Rotated session records

**Rotated 2026-06-22 (v5-hub first-act hygiene; git-object-sourced from HEAD `04c0a54`):** the stale HTML-masthead reconciliation prose (2026-06-13 / -14 / -18 / -18 / -19) + the beats older than beat-5 (beat-4 v4-launch, beat-3 v4-handoff, beat-2 explainability, plus the v3-launch / M7.2a-1-landing / v2-parallel-lanes / Doc-16-Lock / AB-1+AB-2 entries) -> `archive/pm-handoff-rotated-2026-06-22.md`. Kept: beat-6 + beat-5 + Open Risks. Full history in git.

**Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; git-object-sourced from `58794d7`):** the ratification record (Return ASSESSED → RATIFIED → mechanics), the superseded fold-abort record, the M7-AMD-block record, the M6.4-WUCP record + the M6.2-era fragment, and the RESOLVED/CLOSED build-gate blocks from Open Risks → `archive/pm-handoff-2026-06.md`. Earlier rotations per the markers preserved there. Full history in git.

## Open Risks

#### OR-M7-WIRING — Command-pipeline live wiring — [✅ RESOLVED 2026-06-27 — live-wired at M7.4b/c] (formalized 2026-06-25; NEW in beat 7, 2026-06-22)
- **RESOLVED 2026-06-27 (M7.4b producer + M7.4c confirmation/ledger; v8 beats 17/19; reconciled by the v9 hub).** The command pipeline is LIVE on the real composition root: trigger → run → `command_issued` → co-located dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`, with the live `pending_command_ledger` + `pollExpirations()` tick and paired teardown. The resolution condition's **E2E composition-root confirm gate is met** — `RunPipelineConfirmWiringTest` (host-verified present in `lifecycle/lifecycle/src/test`).
- **Residual → M7.4d (the only remaining M7.4-arc item):** the resolution condition's *second* clause — the **D2 pure-function-replay CI test wired as a gate** (+ registering D2's canonical INV-id) — is NOT yet landed. The *wiring* is resolved; the *replay-safety CI gate* is the M7.4d hardening.
- **Severity:** RESOLVED (was MEDIUM). **Owner:** PM/Coder — done. _See OR-GATE-M7.4 below for the gate-of-record confirm on `db8ab5f`._

#### OR-GATE-M7.4 — gate-of-record (CI) on the pushed `4e31aed` (M7.4d HEAD) — [local gate CONFIRMED 2026-06-27; CI-confirm OPEN] (NEW 2026-06-27, v9 hub; updated v10 beat-25)
- **Status:** NARROWED. The **local `./gradlew check` is CONFIRMED GREEN** on the M7.4c working tree that became `db8ab5f` (Nick's terminal 2026-06-27: BUILD SUCCESSFUL, 149 tasks; he then committed + pushed core `69cc6e2..db8ab5f`). So the M7.4b/c/**d** deferred build gates are all **resolved locally** (Nick's terminals: `./gradlew check` 149 tasks green on the M7.4c tree → `db8ab5f`, and again on the M7.4d tree). The only residual is the **gate-of-record** formality: the `ci.yml` GitHub Actions run green on the pushed HEAD **`4e31aed`** (the M7.4d commit has now landed; `db8ab5f`→`4e31aed`). **De-risked (v10 beat-25):** `git ls-tree 4e31aed gradlew` = `100755` (executable) → the prior exit-126 failure mode is structurally cleared, and `ci.yml` runs `./gradlew check --no-daemon`; what remains is purely Nick confirming the Actions run is green on `4e31aed` — the standing b85e1ed/M7.4a pattern, "confirm CI green on the pushed commit, not just locally."
- **Severity:** LOW (the substantive gate — the full local `check` — is green; this is the CI-record formality).
- **Owner:** Nick (glance at the Actions tab for `db8ab5f`). **Resolution condition:** Nick confirms the `ci.yml` run is green on `db8ab5f` → record M7.4b/c CI-gated → close. M7.4d may be authored/routed now (the local gate clears §4b — done, ISSUE-READY beat 22).

#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
