<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-22 (v36 hub, beat 5 — the beat-4 landing `65fd50a` census-exact (ran as add-A; matched only because the tree held exactly the 2 M — the explicit-paths rule re-stated ABSOLUTE) · PELTON SIGNED+PAID (the ~10-day clock RUNNING; results ~Aug-5) · the Gerben foreign-quote email SENT · title guidance: Founder (individual client; the OWNER-AT-FILING question routed to the results consult — LLC-before-filing) · **THE GATE-WINDOW EXECUTION PLAN OF RECORD minted** (the wait IS the gate window; CMD-API blocks 3 of the 5 remaining MUST producers; five lanes; the Aug-10 dry-run; M14 yields to gate rows on any contention). CMD-API authors NEXT TURN. Detail: the beat below.) Prior: 2026-07-22 (v36 hub, beat 4 — the beat-3 landing `42c5b62` census-exact · NICK'S RULINGS INTAKEN (Pelton SIGN+PAY, no rider email — both email items withdrawn; **VEROMO DECLINED-CLOSED**; the brand criterion of record minted [tomorrow-evocation load-bearing] → TAMODO DEMOTED to legal-fallback-of-last-resort; the adverse branch re-instrumented [graduated: knockout+file, not a 2nd comprehensive]; never-LTO NARROWED to never-LTO-as-acquisition [LTO-as-short-dated-lock legitimate with two term-checks]) · THE C&D FRAME CORRECTED (the OpenAI conflation ×3; funding asymmetry outranks merits; never-receive-the-letter is the defense) · CMD-API authors next turn BY DEFAULT. Detail: the beat below.) Prior: 2026-07-22 (v36 hub, beat 3 — the beat-2 landing `1870069` census-exact · THE GERBEN-BUNDLE QUESTION ADJUDICATED (REC stay-unbundled; the $3,000 bundle = the phase-2 benchmark ceiling) · the TOMORO probe (SN 79411529 Classes 9+42, status-630-unassigned per the lagged aggregator snapshot; the suspension-clock frame; TSDR #2 07-31 = the instrument) · the STANDARD-CHARACTERS filing REC + the logo intake · Pelton email v2 (trimmed; the §5 line retained) · the tamodo.com buy-trigger sharpened · veromo 9th flag · CMD-API tripwire reached, authors next turn. Detail: the beat below.) Prior: 2026-07-22 (v36 hub, beat 2 — the beat-1 landing `64a53fe` census-exact · THE COUNSEL/DOMAIN INTAKE (Pelton scope 1/3 [US-only confirmed]; the Gerben filed-application misread + the clarifying reply; tamoro.co/.tech purchased; tamodo.com PRICED $2,195 buy-now; the tamoro.com owner still silent) · hub RECs: sequential search TAMORO-first · no tamodo.com buy pre-G-2 · never LTO · veromo.com 8th flag · R-1 HELD. CMD-API unmoved — authors next turn. Detail: the beat below.) Prior: 2026-07-22 (v36 hub, beat 1 — LAUNCH (PASS all 11): baselines 5/5 verified at source; the close-out commit census-exact; the v35 prompt archived; the CMD-API tripwire flagged — CMD-API authors at the next hub turn. Detail: the beat below.) Prior: 2026-07-22 (v35 hub, beats 1–10 — CLOSE-OUT: boot-health [PASS] OF RECORD (6/6 positive · 0 forbidden · min=25065 met · ALL SIX remembered ULIDs matched on the assert's first run; bundle boot-health-20260722T005123Z) — the instrument-staleness era ends; the Rosonway/I3b move now ELIGIBLE · v36 REFRESHED at close (the beat-1 banked copy superseded in place; CMD-API-first; M14-OPEN state; the counsel/TAMODO pointers) · v35 RETIRES on the v36 launch — the ten-beat arc: the v34-tail reconciliation + THE ROTATION + G1 closed end-to-end + the BENCH-STOP + LEARN-PERSIST landed-and-proven-under-adversity + D1 5/5 + THE M14 TRIGGER SWEEP PASS 7/7 + BENCH-CONST + the counsel intake + the TAMODO screen + the no-trailer directive + the currency sweeps. CMD-API authors at v36 beat 1. Detail: the beats below.) Prior: the full Prior-chain (v25→v34 era) rotated 2026-07-19 → archive/pm-handoff-beats-rotated-2026-07-19.md §A; per-beat detail in the kept/archived sections + git history.
-->

# PM Session Handoff

## 2026-07-22 (v36 beat 5 — the beat-4 landing `65fd50a` CENSUS-EXACT · **PELTON SIGNED + PAID (the clock runs; ~Aug-5)** · the Gerben email SENT · **THE GATE-WINDOW EXECUTION PLAN OF RECORD** (the "wait" IS the gate window) · CMD-API authors next turn)

**The landing + intake.** Hivemind `65fd50a` = exactly 2 files, census-exact, pushed — but Nick ran it as `git add -A`; it matched only because the tree held exactly the 2 M. The explicit-paths rule re-stated ABSOLUTE (the ride-along class is at 5 occurrences; `-A` matching clean is luck, never license). **PELTON: SIGNED + PAID — the ~10-business-day search clock RUNS; results ~Aug-5 → G-2.** The Gerben foreign-quote clarifying email SENT (quote awaited). Signature-block guidance delivered: **Title = Founder** (individual client; no entity exists); the load-bearing flag routed forward: **the §1(b) applicant must be the true owner at filing** (ITU assignment restrictions pre-use) — **the LLC-before-or-after-filing question goes to the Pelton results consult**, deadline-attached (the "LLC timing" open item now has its slot).

**THE GATE-WINDOW EXECUTION PLAN (the executive read, minted as the plan of record; W31 pre-mints from this block at the week boundary).** The framing fact: **the Pelton "wait" and the gate runway are the SAME window** — evidence-freeze Fri 2026-08-14 EOD, the read Sun 2026-08-16; there is no idle time to fill. Re-derived MUST ledger: 16 of 21 rows are DONE/continuous; the remaining producers are **C2 + C3 (suite ports, ride B2) · H2 (the AUTO suite on the deployed build, rides CMD-API→B2) · H3 (clean-image rep, named slot Aug 8–9) · I2 (close-out key-hygiene sweep)** — i.e. **CMD-API blocks THREE of the five remaining MUST producers.** It is not a feature; it is the keystone: its landing converts the bench from manual evidence to automated N-accumulation (B3 nightly), and M14's WUs queue behind it by pre-ruled rider 3.

**The five lanes:** **(1) CORE (serialized):** CMD-API (authors next turn; grounding-first on the banked debts; host-CC lane; two-layer audit; deploy) → SKIP-VIS (+CORE-P1/P2, folds on the GAP-1 ruling) → the M14 WUs (~4–6, integration-shelly; grounding pre-staged in gaps so authoring is same-day on B2-green). **(2) BENCH:** B2 suite port (flips the SKIPPED stubs; C2/C3 + S31 confirm legs + the AUTO usb scenario; OBS-CONFIRM + C4's assertion arm + RUNNER-VERSION-BANNER rule AT authoring; the PROVISIONAL command-lifecycle constants re-pin at flip) → B3 nightly (BENCH-AUTOSTART rules there) → every night after compounds the verdict corpus. **(3) FRONTEND (zero contention, Nick-ruling-gated):** FE-VERDICT-2 dispatches the moment GAP-1/GAP-2 are ruled (+the dependabot branch rides; G2 [S] re-statuses at authoring — F1/F2/G1 all done, it may be nearly free). **(4) OPERATOR (Nick's hands, ~1 session each):** the Rosonway dongle-behind-hub move + I3b [S] — ELIGIBLE NOW, single-variable order (boot-health re-run → move → usb-power flip → the hub-attached usb-reenumeration leg) · A4 [S] + H4 [S] fold into the CMD-API deploy bench session · H3 Aug 8–9 · the third-plug ORDER (M14 DUT; lead time bites within days). **(5) PROGRAMS/DESK (gaps):** Matter Doc-19 draft rides its program hub in-window · E3 [S] public statement (writable now) · the doc-drift ×3 docs commit · corpus entries (joins-night + 04P arcs, the ratchet rule) · TSDR #2 07-31 + the Pelton/Gerben intakes ride as beats.

**The calendar:** Jul 22 plan ratified + the GAP rulings asked → Jul 23–24 CMD-API lane runs/lands/deploys (+A4/H4 riders) → Jul 24–26 SKIP-VIS (if ruled) + B2 authored → Jul 25–28 B2 green on the deployed build (C2/C3/H2 producers RUNNING) + B3 enabled + FE-VERDICT-2 out + the Rosonway session → Jul 28–Aug 5 M14 WUs + Matter draft + desk items → ~Aug 5 Pelton results (G-2) → Aug 8–9 H3 → **~Aug 10 THE GATE DRY-RUN (all 29 rows re-statused against artifacts — zero surprises at the read)** → Aug 12–13 I2 sweep + close-out + **v37 banked** → Aug 14 EOD FREEZE → Aug 16 the read.

**Priority guard, stated:** M14 is OPEN and exciting and **not a gate row** — on any contention, **M14 yields to gate producers, never the reverse.** Deliberately PARKED until post-gate: the V1.x charter row, the Matter spike, ADOPT-REPROPOSE, BTN-AMD/D2 [S] (presses not log-visible; consciously deprioritized), the `awaitCheckpoint` deflake (no event-bus WU in-window). **Nick's unlock list, priority order: (1) the GAP-1/GAP-2 rulings — minutes of ruling that unlock TWO lanes (SKIP-VIS fold + FE-VERDICT-2); (2) the third-plug order; (3) schedule the Rosonway session; (4) launch lanes as authored; (5) HUE-RESET at leisure.**

**Nick, in order:** **(1) the beat-5 commit** — EXPLICIT paths, exactly 2 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v36-beat-5_commit-msg.txt` → push. **(2) the GAP-1/GAP-2 rulings** (the highest-leverage minutes available). **(3) the third-plug order. (4)** the Rosonway session when convenient this week. **(5)** 07-31 TSDR #2.

**Hub next (v36): CMD-API AUTHORING — NOW.** The counsel program is in wait-state; the deck is clear; the next hub turn is the grounding pass + pre-verification + the coding instruction → SKIP-VIS → B2 → FE-VERDICT-2 → the M14 WUs → the Aug-10 dry-run → the Aug-14 freeze.

## 2026-07-22 (v36 beat 4 — the beat-3 landing `42c5b62` CENSUS-EXACT · **NICK'S RULINGS INTAKEN ×5** (Pelton SIGN+PAY no-rider · **VEROMO DECLINED-CLOSED** · the brand criterion of record → **TAMODO DEMOTED** · the adverse branch RE-INSTRUMENTED · never-LTO NARROWED) · **THE C&D FRAME CORRECTED on the record** · CMD-API authors next turn BY DEFAULT)

**The landing.** Hivemind `42c5b62` = exactly the ordered 2 M, pushed; HEAD + clean porcelain re-derived at source this beat.

**NICK'S RULINGS, INTAKEN + ADJUDICATED:** **(1) Pelton: SIGN + PAY, no rider email** — both v2 items WITHDRAWN on Nick's consult evidence (Pelton holds the TAMORO memo and referenced the target mark in-consult; the TOMORO-specific confirm is satisfied-by-conduct) — adjudicated-ACCEPT; the four analysis asks (services-ID overlap · TOMORO prosecution posture/decay · suspension-vs-refusal likelihood · per-branch cost/timeline) MOVE to the G-2 read of the delivered analysis, no pre-payment traffic. Payment = the act that starts the ~10-business-day clock; results ~Aug-5 → G-2. **(2) VEROMO: DECLINED-CLOSED** (Nick, verbatim-class: does not care for it; the 9-flag veromo.com row RESOLVES; the fallback bench is TAMODO only). **(3) THE BRAND CRITERION OF RECORD, minted:** tomorrow-evocation + catchy/brandable is LOAD-BEARING for Nick — a replacement candidate must be BOTH clearable/domain-available AND equally brandable; unprompted third-party enthusiasm for name + logo noted as field evidence. **Adjudicated consequence: TAMODO fails Nick's own brand criterion** (it breaks the tomorrow echo the funnel selected for) → **DEMOTED from second-candidate to legal-fallback-of-last-resort**; P(TAMODO becomes the mark) is LOW even in the adverse branch (the funnel re-runs on the criterion before TAMODO wins by default) → minimal pre-spend on TAMODO artifacts is CORRECT. **(4) The adverse branch RE-INSTRUMENTED per Nick's lean:** on an adverse TAMORO read the default instrument is GRADUATED — a knockout-level screen (~$300–600 class) + file, NOT a second $1,200 comprehensive; caveat RECORDED: the desk screen's own stated limit (register phonetic equivalents unmapped) means knockout-then-file carries real residual risk — decide AT the branch with counsel's input. **(5) never-LTO NARROWED on Nick's valid pushback:** the rule was aimed at LTO-as-ACQUISITION (financing premium + deferred title) and stands there; **LTO-as-short-dated-LOCK is a legitimate instrument** (month-1 ≈ a ~$101 option that takes the listing off the market) IFF two term-checks pass before use: cancellation forfeits only payments made (no residual obligation) · early buyout available at ~buy-now-equivalent. Per ruling (3) the hedge value DROPPED — REC: zero tamodo.com spend now; the free weekly status-watch stands offered; revisit at the branch.

**THE C&D FRAME, CORRECTED ON THE RECORD (the OpenAI conflation's 3rd occurrence — worth a durable line):** any TOMORO enforcement would come from **Tomoro AI Ltd** (UK AI consultancy), never OpenAI — the partner halo does not put OpenAI's enforcement machine behind the mark (this cuts in Nick's FAVOR: consultancy-scale appetite for US enforcement against a smart-home OS is low). The "I could win" frame is corrected: at bootstrapped scale **the merits are the SECOND question — the funding asymmetry is the first** (a C&D costs ~$1–3k to send; a lawyered response $5–15k; a TTAB opposition ~$50–150k; federal litigation $200k+; winning a fight you cannot fund is losing, and the fork arrives at maximum-traction, maximum-cost timing). The working defense at this scale is **never-receive-the-letter**: clearance + distinct identifications + the honest moderate read already being purchased. The "they're dropping TOMORO / moving into the OpenAI brand" theory is UNVERIFIED speculation — a live register entry suspends/blocks regardless of the owner's branding energy; §66(a) extensions register without US use; **the decay clocks + TSDR #2 (07-31) are the instrument, never vibes.** "Pivot harder to hardware" helps the merits marginally but cannot pivot away the Class-9 software identifications the product actually needs. Not-a-lawyer caveat standing.

**Nick, in order:** **(1) the beat-4 commit** — EXPLICIT paths, exactly 2 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v36-beat-4_commit-msg.txt` → push. **(2) SIGN + PAY Pelton** (starts the clock; results ~Aug-5 → G-2). **(3) the Gerben foreign-quote reply** (unchanged, still owed). **(4)** GAP-1/GAP-2 when convenient. **(5)** 07-31 TSDR #2.

**Hub next (v36): CMD-API AUTHORING — thrice-deferred by counsel traffic, authors NEXT TURN BY DEFAULT** (counsel outcomes keep riding as intake beats) → SKIP-VIS → B2 → FE-VERDICT-2 → the M14 WUs → the Aug-14 freeze producers.

## 2026-07-22 (v36 beat 3 — the beat-2 landing `1870069` CENSUS-EXACT · **THE GERBEN-BUNDLE QUESTION ADJUDICATED: STAY UNBUNDLED** · **THE TOMORO PROBE + the suspension-clock frame** · the STANDARD-CHARACTERS filing REC + the logo intake · Pelton email v2 · CMD-API tripwire REACHED)

**The landing.** Hivemind `1870069` = exactly the ordered 2 M, pushed; HEAD + clean porcelain re-derived at source this beat.

**THE GERBEN-BUNDLE QUESTION (Nick, this turn: worth switching the US lane to Gerben's $3,000 + $350/class flat package?) — ADJUDICATED: REC STAY-UNBUNDLED (Pelton US).** The clean-branch money is roughly a WASH (Pelton $1,200 search + a phase-2 opinion/filing quote plausibly ~$1,500–2,000 ≈ the bundle); the option structure is NOT: (a) the adverse branch exits at $1,200 unbundled vs a consumed $3,000 package on a mark never filed; (b) **the bundle EXCLUDES substantive Office Actions — and a §2(d) event off SN 79411529 is precisely the most probable expensive event in this filing**; the flat fee is not the expected cost; (c) this is a CONTESTED clearance, not a commodity filing — the purchased asset is the specialist TOMORO-specific read, which a productized opinion letter is weakest at; (d) the report-portability scope term enables a later brain/hands split (Pelton search+opinion, cheapest-competent filer drafts/files — possibly Gerben) if phase-2 pricing disappoints. **The $3,000 bundle stands ON THE RECORD as the phase-2 benchmark ceiling** — if Pelton's opinion+filing quote pushes the stack materially past bundle-equivalent, negotiate or split. The intake misread on Gerben's first reply is noted as a mild signal, not a disqualifier (sales-intake, not the attorney).

**THE TOMORO PROBE (supplementary instrument; aggregator-LAGGED — TSDR #2 on 07-31 remains the status instrument of record).** Justia snapshot of SN 79411529: §66(a) Madrid extension · filed 2024-05-07 · owner **Tomoro AI Ltd** · **Classes 9 + 42** (AI/ML/autonomous-agent/NLP software; SaaS; tech consulting; R&D) · status **630 New Application — NOT ASSIGNED TO EXAMINER** as of the 2024-12-05 event · pseudo-mark not displayed on the snapshot (the program's TSDR receipt for the TOMORROW pseudo-mark OUTRANKS the aggregator; discrepancy noted, not adopted). Consequences delivered in-conversation as the risk frame: (i) both marks would live in 9/42 — **the armor is the specific-goods/channel gap, never a class-level gap**; (ii) if TOMORO is still pre-examination, TAMORO's likely §2(d) event is **SUSPENSION behind a pending application, not a fast refusal — the operative cost is the CLOCK (registration limbo riding TOMORO's own prosecution), not the verdict**; (iii) use-vs-registration split stated: a suspension delays the certificate, not the launch — the kill criterion is confusion exposure IN USE, which is what the search's freedom-to-use read prices; (iv) the four concrete asks to extract from the Pelton analysis: services-ID overlap vs our intended 9/42 IDs · TOMORO's prosecution posture + decay probability · suspension-vs-refusal likelihood · expected cost/timeline per branch. Not-a-lawyer caveat standing.

**FILING-FORM REC + the logo intake:** file the word mark in **STANDARD CHARACTERS** (broadest scope; not tied to font/color/design); the draft logo does NOT ride the first application — a design mark is a separate, later filing if ever wanted; **real logo investment stays HELD until R-1 resolves at G-2** (the rename-readiness rule extends naturally: a logo is the maximally name-coupled artifact). The draft intaken with in-conversation notes (concept endorsed: strong wordmark + single bold device; execution notes: the scribble/gradient/grid treatment reads dated, the red-circle device field is crowded, the rising-sun tomorrow-echo is on-brand but nudges commercial-impression convergence with the senior mark's pseudo-mark — a minor factor, noted for the record).

**Pelton email v2 (trimmed per Nick's ruling — the all-in line DROPPED as settled):** the TOMORO-specific-analysis confirmation RETAINED (one line protecting the purchase's core value, Nick's own stated priority) + **the §5 TAMODO line RETAINED on adjudication** — its value is the PRICED, fire-ready adverse branch (the anti-dawdle instrument), not counsel's musings — + a payment-imminent close. **The tamodo.com buy-trigger SHARPENED: buy outright (never LTO) the DAY the TAMODO search is commissioned** (the adverse-TAMORO branch — before its 2-week search window opens, not after); secondary trigger = credible listing movement. The Gerben reply stands UNCHANGED (no bundle-signal rider — the foreign quote stays clean). **veromo.com $12 — 9th flag.**

**Nick, in order:** **(1) the beat-3 commit** — EXPLICIT paths, exactly 2 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v36-beat-3_commit-msg.txt` → push. **(2) the Pelton email v2 → pay on the reply** (results ~Aug-5 → G-2). **(3) the Gerben clarifying reply** (as drafted, unchanged). **(4) veromo.com $12.** **(5)** GAP-1/GAP-2 when convenient. **(6)** 07-31 TSDR #2.

**Hub next (v36): CMD-API AUTHORING — the tripwire date is REACHED; the instruction authors on the next hub turn** (counsel intake rides as beats without displacing it) → SKIP-VIS → B2 → FE-VERDICT-2 → the M14 WUs → the Aug-14 freeze producers.

## 2026-07-22 (v36 beat 2 — the beat-1 landing `64a53fe` CENSUS-EXACT · **THE COUNSEL/DOMAIN INTAKE** (Pelton scope 1/3 confirmed · the Gerben misread + the clarifying reply · tamoro.co/.tech purchased · **tamodo.com PRICED $2,195 buy-now**) · the strategy read delivered in-conversation · CMD-API unmoved — authors next turn)

**The landing.** Hivemind `64a53fe` = exactly the ordered census (2 M + 1 R; the archive move recorded as a 100% rename; explicit paths honored — no ride-along), pushed; HEAD + clean porcelain re-derived at source this beat. The beats-8–10 and beat-1 orders are both fully consumed; their `_scratch/` msg files join the standing orphan-sweep set.

**THE COUNSEL INTAKE (§6-pointers; Nick's direct brief served this turn — his brief outranks the queue; the CMD-API charge is unmoved).** **(1) Pelton:** the $1,200 search confirmed **US-only** — scope answer 1 of 3. The pre-payment remainder: the all-in confirmation · the TOMORO-SN-79411529-specific §2(d)-analysis confirmation · **the §5 TAMODO second-candidate line** — ONE combined email drafted in-conversation; **pay on good answers**; the ~10-business-day clock puts results ~Aug-5 → G-2. **(2) Gerben REPLIED with an intake misread** — presumes a FILED application (asked for a serial number; asked same-application-or-different-mark); no foreign quote yet; neither original question answered. The clarifying reply drafted in-conversation: nothing filed; pre-filing clearance with separate US counsel; single mark; direct-national JP/AU/UK/EU claiming Paris priority off the forthcoming US §1(b) within the six-month window; per-country all-in quote + local-search inclusion + local-refusal rates. **(3) DOMAIN MOVES:** Nick purchased **tamoro.co + tamoro.tech** (defensive TLDs; claims-inventory rows OWED at the next counsel-package touch — the hub defers the lane-file edit per §2 write-isolation); **tamodo.com is PRICED: GoDaddy premium buy-now $2,195** (or $101/mo lease-to-own — REC of record: **never LTO**; title transfers only at term-end); other TLDs report open on both names; **atom.com: the tamoro.com owner still SILENT** — the $2,995 offer stands · walk $3,400 · silence doctrine unchanged, and the walk is now CHEAPER (usable TAMORO TLDs in hand + a fully-priced substitute anchor).

**Hub RECs of record (strategy + process; the legal read stays counsel's — standing not-a-lawyer caveat):** **R-A SEQUENTIAL SEARCH** — TAMORO first (the funnel's selection; the researched position); TAMODO priced-but-BENCHED; its search fires only on an adverse TAMORO read (a second ~$1,200 search + opinion + filing would pressure the $2,500–4,500 Phase-1 guardrail; the adverse-branch cost of sequencing ≈ 2 weeks; Pelton's §5 answer may re-weight — Nick's call). **R-B NO tamodo.com BUY pre-G-2** — the mark is UNSEARCHED (the register layer is exactly what the paid search reads); premium listings are sticky at the ~weeks horizon (tail risk stated honestly: it can sell or re-price); **sunk cost never votes at G-2**; the $12 veromo.com hedge PRECEDES any $2,195 hedge (**8th flag**). **R-C the tamoro.com doctrine UNCHANGED** — no re-anchor, no chase; an acceptance arriving pre-G-2 (~Aug-5) is the one branch where $2,995 would land on an unsearched mark — decide AT that branch, with normal closing mechanics as the buffer. **R-D** R-1 stays HELD for G-2; nothing this intake moves the name decision — the one genuinely new G-2 balance-sheet line: TAMODO's total acquisition cost is now BOUNDED and lower (~$2,195 + hand-reg TLDs vs $2,995–3,400 + the owned .co/.tech).

**Nick, in order:** **(1) the beat-2 commit** — EXPLICIT paths, exactly 2 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v36-beat-2_commit-msg.txt` → push. **(2) the Pelton close-out email** (in-conversation: the two remaining confirmations + the §5 line verbatim) → **pay on good answers**. **(3) the Gerben clarifying reply** (in-conversation). **(4) veromo.com $12.** **(5)** GAP-1/GAP-2 when convenient (GAP-1 before SKIP-VIS). **(6)** 07-31 TSDR #2.

**Hub next (v36): CMD-API AUTHORING — the single next act, unmoved** → SKIP-VIS (+CORE-P1/P2 on the GAP-1 ruling) → the B2 port → FE-VERDICT-2 (+dependabot) → the M14 WUs → the Aug-14 freeze producers.

## 2026-07-22 (v36 beat 1 — **LAUNCH (PASS all 11)** · baselines 5/5 verified at source · the close-out census-exact · the v35 prompt ARCHIVED · **THE CMD-API TRIPWIRE FLAGGED (~Jul-24 = two days out)** · CMD-API authors next turn)

**Launch (remote §12).** Filesystem-MCP scope re-verified at first act: **FULL ClaudeFolder** (widened from the v34–v35 core-only era; the volatile-scope rule held). §11 clean — lock-free porcelain ×5 ALL CLEAN; both spine frontmatters read pre-write (last writer = v35 beat 10, committed — no ghost). The uploaded launch prompt verified **md5-≡ the repo's refreshed copy** (`3bd8f1d3…`) — the hub boots on the copy of record. Baselines verified at source: core **`355a711`** (HEAD; CI-green; deployed; clean) · docs `a53f474` · hivemind **`1e455b2`** (the beats-8–10 close-out LANDED **census-exact: 3 M + 1 A — no ride-along**) · bench `9f2b5ad` (BENCH-CONST landed; boot-health `[PASS]` of record, bundle `boot-health-20260722T005123Z`) · skills `dccc85f`. **Mirrors 3/3 composite-md5-≡** (Nick's sync ran post-beat-6 fold — Check 9 PASS-per-proxy). Watermark **AMD-99, 175/53** re-derived at the register regeneration line. Fleet of record: **6/6 @ position 25065, ch20/0x774c**; the 04P at deviceId `01KY12MQVQ204M1VP39F1ZDM33` / entityId `01KY12MQW954E4XYNKH0Y5H8VX`, `binary_sensor`/contact off the persisted learn.

**Preflight PASS all 11 — adjudications of record:** Check 2 — W30 current; the beat-6/7 currency notes govern over the pre-mint. Check 4 — one drift NOTE, scored within PASS: the backlog's legacy M14 **table row** still carries the pre-program "Zigbee Integration / PLANNED / ZCL+Tuya+Xiaomi" description vs the operative currency-note layer (M14 = integration-shelly, **OPEN** at the beat-7 sweep); the head note supersedes-where-differs by its own charter; the row redescription rides the next backlog touch (M14 WU authoring or its WUCP P2). Check 5 — Open Risks clear (all entries resolved; the `awaitCheckpoint` deflake row stays QUEUED, rides the next event-bus-touching WU). Check 8 — ACTIVE 0. Check 10 — the strategy map is stamp-stale (2026-07-04) but pointer-safe; 13/13 existence spot-checks OK. Check 11 spots at HEAD objects: `ZoneType.java:26` CONTACT(0x0015)=21 verbatim ✓ · `learned_zonetypes_rehydrated` live at `ZigbeeIntegrationAdapter.java:339` + the `count=1` test pins ✓ · the ten-value `CommandResultEvent` vocabulary ✓ · AMD-99/175/53 ✓. Required reads executed (v35 beats 1–10 · the G1 return + the GAP-1..5/CORE-P1..P4 set · the 04P reports ×2 · the RATIFIED criteria [21 M / 8 S; J1 FROZEN; freeze Fri 08-14 EOD · read Sun 08-16] · the doctrine brief · the counsel tracker + TAMODO screen · Matter §-beats). The v35 prompt ARCHIVED → `context/handoff/archive/` (the D/?? pair records as R).

**THE CMD-API TRIPWIRE (~Jul-24), flagged with the honest posture:** C1 [M]'s N ≥ 200 is already organically exceeded (~1,728 recorded verdicts, zero false CONFIRM) — the tripwire's real bite is the DEPENDENCY STACK behind the unlanded write surface, not C1 arithmetic: the B2 suite port (flips the SKIPPED stubs; the C2/C3 suite legs) · B3 nightly (the N-accumulation engine) · H2's AUTO suite · the S31 confirm legs · the AUTO usb scenario · the M14 WUs (rider 3: CMD-API + B2 precede them). **CMD-API AUTHORS AT THE NEXT HUB TURN — nothing queues ahead** (grounding-first on the banked debts: auth-on-POST per the OR-APP-AUTH `installAuth` posture + token-freeze day one · `IdempotencyEntry` honored · `ProblemType` complete · wire-casing pinned at the endpoint vs the LIVE camelCase precedent · the WebhookTrigger 4-grep route census · the B1-reduced command-lifecycle assertion surface; pre-verification ≥3 source-state assumptions enumerated AT SOURCE; host-CC Coder lane; two-layer audit; the bench constants' PROVISIONAL command-lifecycle block re-pins at flip as a constants re-mint).

**Nick, in order:** **(1) the beat-1 commit** — EXPLICIT paths, exactly 4 porcelain entries (2 M + 1 D + 1 ??; records as 2 M + 1 R): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/handoff/2026-07-19_PM-mission-control_v35_orchestrator_session_prompt.md context/handoff/archive/2026-07-19_PM-mission-control_v35_orchestrator_session_prompt.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v36-beat-1_commit-msg.txt` → push. **(2) Your program (all standing, none blocking the hub):** the Pelton scope answers + the TAMODO §5 line → pay on good answers · tamoro.com (offer $2,995 standing · walk $3,400 · silence) · **veromo.com $12** (still the cheapest unfinished item) · the third-plug order (M14-DUT-only, never stimulus) · the GAP-1/GAP-2 rulings (GAP-1 before SKIP-VIS folds CORE-P1/P2 into one wave) · 07-31 TSDR #2.

**Hub next (v36): CMD-API AUTHORING — the single next act** → SKIP-VIS (+CORE-P1/P2 on the GAP-1 ruling) → the B2 port → FE-VERDICT-2 on the contract ruling (+ the dependabot dashboard-deps branch rides it) → the M14 WUs → every Aug-14 freeze producer running.

## 2026-07-22 (v35 beat 10 — **BOOT-HEALTH `[PASS]` OF RECORD — the instrument-staleness era ENDS** · **THE v35 CLOSE-OUT: v36 REFRESHED at close** · v35 retires on the v36 launch)

**Boot-health GREEN of record.** Nick pulled `9f2b5ad` at the Pi (fast-forward, the constants diff exact 35+/17−) and ran the boot-health SCENARIO on the runner: **`[PASS] boot-health — 6/6 positive · 0 forbidden`** (boot `bench-2026-07-21-205111.log`; bundle `boot-health-20260722T005123Z`). Every assert satisfied against the re-minted constants: `projection_live devices=6 entities=6` with `min=25065` MET · `adoption_maps_rehydrated: devices=6` · `device_relinked` ≥2 (observed ×6 incl. the 04P) · `network_resumed channel=20 panId=0x774c` · `port_identity_captured … pinnedOnly=false` · **the API entities assert: rows=6 with ALL SIX remembered ULIDs matched** — the grown ULID list has teeth on its first run. The lawful-[FAIL] era that began at the joins night is formally CLOSED; the **Rosonway dongle-behind-hub move + I3b [S] is now ELIGIBLE** (the single-variable order stands: boot-health re-run first).

**THE v35 CLOSE-OUT.** The arc, ten beats: LAUNCH STALE-adjudicated + the v34-tail reconciliation → THE ROTATION (the hot-path tax paid: pm-handoff 393 KB → ~100 KB-class · snapshot 115 → ~30 KB-class · both masthead chains archived) → the G1 return TWO-LAYER ACCEPT → landed `f3bfd5c` frontend.yml-GREEN (**G1 CLOSED end-to-end**; GAP-1..5 + CORE-P1..P4 intaken) → the 04P BENCH-STOP (the cached-interview race caught PRE-door — the two-window guard's first falsification dividend) → **LEARN-PERSIST** authored → ACCEPT zero-[REVIEW] → landed `355a711` CI-GREEN → **proven UNDER ADVERSITY at the bench** (the learn survived a leave + three reboots; contact adoption off the persisted learn, zero re-learns) → **D1 [M] 5/5 CLOSED** → **THE M14 TRIGGER SWEEP PASS 7/7 — M14 OPEN**, 10 days ahead of the boundary → BENCH-CONST re-minted (6/6 @ 25065) → **boot-health [PASS]** (this beat) → the counsel arc intaken (G-1 unbundled ACCEPT; the TAMODO field screen ask-with-intent) → THE NO-TRAILER DIRECTIVE minted durable → the currency sweeps (W30 · backlog · playbook §7/§8 · env-model · the PM skill · pm-lessons — every fresh-session boot surface current). Durable rules minted this arc: no-attribution-trailers (env-model §9) · enumerate-file-shapes-at-source · instrument-semantics glance-points (playbook §8) · the explicit-paths rule re-proven ×2 more (the ride-along class at 5 occurrences). Candidate rows minted: ADOPT-REPROPOSE · OBS-CONFIRM. **v36 REFRESHED at close** (the 2026-07-19 banked copy superseded IN PLACE — same path, 1 M in this commit): current baselines, M14-OPEN state, the CMD-API-first charge, the counsel/TAMODO pointers, the eligible Rosonway move. **v35 retires on the v36 launch. CMD-API authors at v36 beat 1 with a fresh context window — nothing queues ahead of it.**

**Nick, in order (the close-out set — the last acts of this conversation):** **(1) the CONSOLIDATED beats-8–10 commit** (the beats-8–9 msg `_scratch/2026-07-22_hivemind_v35-beats-8-9_commit-msg.txt` is a **NAMED ORPHAN — never run it**): EXPLICIT paths, exactly 4 entries (3 M + 1 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/handoff/2026-07-19_PM-mission-control_v36_orchestrator_session_prompt.md context/strategy/counsel-package/2026-07-22_tamodo-candidate-field-screen.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v35-beats-8-10_commit-msg.txt` → push. **(2) LAUNCH v36** — a fresh Cowork conversation: attach/point it at `nexsys-hivemind/context/handoff/2026-07-19_PM-mission-control_v36_orchestrator_session_prompt.md` (the refreshed copy) — v35 retires on that launch. **(3) Your program (all carried in the v36 prompt):** the Pelton scope answers + the TAMODO §5 line → pay on good answers · tamoro.com (walk $3,400 stands) · veromo.com $12 · the third-plug order at your discretion · GAP-1/GAP-2 rulings (GAP-1 before SKIP-VIS) · 07-31 TSDR #2.

**Nothing else is owed to close this hub.** Every return is audited, every landing reconciled, every instrument green, every lesson banked, and the successor prompt carries the whole state — the fresh window boots on nothing stale.

## 2026-07-22 (v35 beat 9 — **THE TAMODO FIELD SCREEN landed** (counsel-package; the conflict-axis read: ~a tier lighter than TOMORO-vs-TAMORO, with named counterweights) · **boot-health: values CORROBORATED at the health glance; the SCENARIO `[PASS]` still OWED** (the bench-pull prerequisite caught) · the dependabot-branch watch · beats 8–9 CONSOLIDATE)

**Boot-health adjudication.** Nick's Pi paste = `~/bench.sh health` — the token GLANCE, not the boot-health SCENARIO. The glance CORROBORATES the re-mint exactly, read AT the instrument: `projection_live devices=6 entities=6 position=25065` · `device_relinked` ×6 incl. the 04P at its LEARN-PERSIST deviceId (`01KY12MQVQ…`) · `adoption_maps_rehydrated: devices=6` · `network_resumed channel=20 panId=0x774c` · zero failure tokens. But the `[PASS]` of record is the RUNNER's fixture-paired verdict against the re-minted constants — and the paste shows NO `nexsys-bench` pull, so `9f2b5ad`'s constants are likely not on the Pi yet (the health verb is constants-independent; the glance was valid either way). **The close is two commands:** `cd ~/nexsys-bench && git pull` → the boot-health scenario run → ⏺ paste `[PASS]` (the instrument-staleness era formally ends on that paste). **Watch item minted:** origin gained `dependabot/npm_and_yarn/web-ui/dashboard/multi-…` (dashboard devDeps) — the 2026-07-07 precedent class (`npm run verify` green → merge on a convenience pass); routes naturally with FE-VERDICT-2 or the next frontend touch; non-blocking.

**THE TAMODO FIELD SCREEN (Nick's directed deepening — conflicts, not domains):** landed at `context/strategy/counsel-package/2026-07-22_tamodo-candidate-field-screen.md` (receipts dated 2026-07-22; the §5 paste-ready Pelton line included). Findings of record: **(1)** the identical string has HISTORY — a defunct-looking "Tamodo" affiliate-marketing network (~2019–22; tamodo.com now parked; scam-vs-legit review debris persists) — LOW as a legal conflict (likely-abandoned, far field) but a real brand-hygiene cost TAMORO does not have; **(2)** **KOMODO** = the nearest ACTIVE neighbor (Komodo Health's expanding word-mark portfolio, filings 2020→Apr-2026) — low-to-low-moderate: the -OMODO spine is close but the onset consonant differs and healthcare-services field-gap does real work; **(3)** **COMODO** (the security-software family — the closest SEMANTIC field) **rebranded to Xcitium in 2022** — legacy registrations persist but the owner is migrating off the mark; **(4)** MOVADO far-field. **The comparative read:** TAMODO's worst realistic neighbor prices **about a tier lighter** than the TOMORO-vs-TAMORO priced-MODERATE, and none of TAMODO's neighbors share a commercial impression with it (vs the pseudo-mark-TOMORROW anchor TOMORO carries). Counterweights stated: desk-screen-not-search (phonetic equivalents live on the register) · the hygiene debris · the evocation forfeit · the researched-position forfeit. **The OpenAI conflation corrected on the record: the TOMORO mark's owner is Tomoro Ltd, an OpenAI PARTNER consultancy — the partner halo does not put OpenAI's enforcement behind the mark.** REC SHARPENED (from beat 8's bench-don't-swap): **TAMODO graduates to ASK-WITH-INTENT** — the §5 line goes on the Pelton scope thread PRE-payment; candidates still resolve at G-2 with search results; R-1 stays HELD; the tamoro.com coupling stands (offer standing · walk $3,400 · silence · resolve conviction before any seller acceptance).

**Nick, in order:** **(1) the CONSOLIDATED beats-8–9 commit** (beat 8's msg `_scratch/2026-07-22_hivemind_v35-beat-8_commit-msg.txt` is a **NAMED ORPHAN — never run it**; its stages-exactly-2 clause went stale when beat 9 landed): EXPLICIT paths, exactly 3 entries (2 M + 1 ??): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/strategy/counsel-package/2026-07-22_tamodo-candidate-field-screen.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v35-beats-8-9_commit-msg.txt` → push. **(2) the Pi close (two commands):** `cd ~/nexsys-bench && git pull` → the boot-health scenario run → ⏺ paste. **(3) the Pelton line** (§5 of the field-screen note, paste-ready) — then pay on satisfactory answers. **(4) veromo.com $12** (7th probe; still the cheapest insurance in the program). **(5)** GAP-1/GAP-2 when convenient.

**Hub next (v35): CMD-API AUTHORING — the next turn, no further intake ahead of it** (the tripwire is ~Jul-24; the honest posture stands; the instruction authors grounding-first on the banked debts with its pre-verification) → SKIP-VIS (+CORE-P1/P2 if ruled) → the B2 port → FE-VERDICT-2 (+ the dependabot merge rides it) → M14 WUs → the counsel §6-pointers (the scope answers + the TAMODO pricing answer · the search verdict → G-2 · 07-31 TSDR #2).

## 2026-07-22 (v35 beat 8 — the `9f2b5ad`/`750141e` landing (**the add-A ride-along, 5th occurrence, RECONCILED** — the counsel lane's 3 files rode) · **THE COUNSEL ARC INTAKEN** (G-1 unbundled ADJUDICATED-ACCEPT; the Pelton $1,200 search pending scope answers) · **THE TAMODO QUESTION screened + adjudicated** · boot-health `[PASS]` paste OWED)

**The landing + reconciliation (append-only, of record).** Bench `9f2b5ad` = exactly the ordered 1 M ✓ (the CRLF warning is the known benign class). Hivemind `750141e` landed **9 files against a stages-exactly-6 message** — Nick's `add -A` swept the counsel lane's three lawful files in (M `context/assessments/2026-07-15_counsel-navigator_return.md` [Addendum 4, the 07-21 consult log] · M `context/strategy/counsel-package/2026-07-15_domain-handle-claims-inventory.md` [freshness pass] · ?? `context/strategy/counsel-package/2026-07-21_engagement-tracker.md` [the new engagement source-of-truth board]) — **the ride-along class, 5th occurrence**; content legitimate on both sides (the counsel lane's scheduled 07-21 wake-up session wrote them; they were destined for a hub-ordered commit regardless); census verified from Nick's own pasted porcelain + commit summary; nothing foreign rode; THIS beat is the append-only correcting record. The rule stands ABSOLUTE — and the silver lining: the counsel-package tree the lane flagged as "audit + commit" is now committed. **Boot-health: the `[PASS]` paste did NOT arrive with the commits — OWED** (⏺ paste either way; the GREEN close of the instrument-staleness era records on the paste, not before).

**The counsel arc intaken (pointers — `context/strategy/counsel-package/2026-07-21_engagement-tracker.md` is the source of truth; Addendum 4 of the counsel-navigator return is the narrative):** the Pelton consult HELD 07-21 (memo-aligned; declined specifics without engagement — professionally correct, and the filing-mill negative-signal is absent). **G-1 EVOLVED — unbundled by jurisdiction: Pelton = US ($1,200 standalone comprehensive search FIRST → opinion/filing on separate agreements; VEROMO rider + Asimtote companion ride here) · Gerben = FOREIGN candidate (JP/AU/UK/EU, own local searches) · Oliner dormant.** Hub adjudication: **ACCEPT** — a coherent, logged deviation from the instruction's single-firm Phase-1 (zero search duplication; the wasteful Pelton-search + Gerben-bundled-US combo explicitly avoided; the $2,500–4,500 Phase-1 guardrail intact). The scope-confirmation email (3 Qs: phonetic/design coverage · all-in pricing · does the written analysis cover the TOMORO §2(d) read) is out; **payment correctly HELD until answered** — the hub endorses paying on satisfactory answers (the strategic read delivered in-conversation 2026-07-22). Fee reality banked (~$550/class realistic US gov fees; substantive-OA rates asked of every filer). Standing: tamoro.com negotiation UNRESOLVED (offer $2,995 standing · walk $3,400 · silence doctrine) · TSDR audit #2 = 07-31 · R-1 HELD for G-2 · LLC timing open.

**THE TAMODO QUESTION (Nick, post-consult: would TAMODO be stronger/easier than TAMORO?) — screened + adjudicated.** House-method screen, receipts 2026-07-22 (dns.google + web): **tamodo.com REGISTERED, afternic-parked** (another aftermarket seller, price unknown — the anchor .com is NOT free either way) · tamodo.ai NXDOMAIN · tamodo.io NXDOMAIN · organic TAMODO presence ≈ zero (generic-only results) · the free register layer INCONCLUSIVE (Justia errored on both queries; no TESS read — register truth is exactly what the paid search instruments) · **veromo.com STILL NXDOMAIN** (7th consecutive probe; the $12 buy remains the cheapest unfinished item in the program). **The adjudication (recorded; full reasoning in-conversation):** TAMODO is plausibly MORE DISTANT from TOMORO (a 2-letter delta vs TAMORO's 1; it breaks the "tomorrow" phonetic echo — load-bearing because TOMORO carries pseudo-mark "TOMORROW" ON FILE) but **NOT presumptively "way easier"**: it restarts clearance on an unscreened landscape with its own neighbor class (the KOMODO-family tech marks share the -OMODO spine; Komodo Health is a major Class-9/42 software mark), forfeits the RESEARCHED position (TOMORO's decay clocks mapped · the goods-gap armor built · the moderate tier priced), loses the tomorrow-evocation the 22-name funnel selected FOR, and its .com is also aftermarket. **RECs:** (1) never swap on vibes — TAMODO enters the same funnel every candidate did; (2) the cheap high-leverage move: **one added line on the ALREADY-OPEN Pelton scope thread, BEFORE payment** — second-candidate search pricing + would-counsel-advise-candidate-prioritization (a comprehensive search prices PER MARK; the question converts the idea into a priced option); (3) **the timing coupling flagged as the live decision:** the $2,995 tamoro.com offer is STANDING and seller-acceptable at any moment — if conviction in TAMORO has actually dropped, resolve that BEFORE acceptance (silence is already the doctrine; no new broker signal either way); if TAMODO is insurance, bench it as a fallback candidate beside VEROMO and change nothing; (4) R-1 stays HELD for G-2 — the name resolves with search results in hand, exactly as chartered. Not-a-lawyer standing caveat: the §2(d) legal read is Pelton's to give; this is strategy + process.

**Nick, in order:** (1) **the beat-8 commit** — EXPLICIT paths, exactly 2 M: `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md` → `git commit -F ../_scratch/2026-07-22_hivemind_v35-beat-8_commit-msg.txt` → push. (2) **⏺ the boot-health paste** (owed — either way). (3) **The TAMODO call:** add the second-candidate line to the Pelton scope thread before paying (draft in-conversation), or drop it and pay on the scope answers as planned. (4) **veromo.com $12** (still open; still the cheapest insurance in the program). (5) The GAP-1/GAP-2 rulings when convenient (GAP-1 before SKIP-VIS folds CORE-P1/P2 into one wave).

**Hub next (v35): CMD-API AUTHORING** — the pole; the ~Jul-24 tripwire is TWO days out with the honest posture standing (C1's N already organically exceeded); the instruction authors on the next hub turn (grounding-first on the banked debts; pre-verification; host-CC lane) → SKIP-VIS (+CORE-P1/P2 if ruled) → the B2 port → FE-VERDICT-2 on the contract ruling → M14 WUs behind CMD-API+B2 → the counsel §6-pointers as they land (the scope answers · the search verdict → G-2 · 07-31 TSDR #2).

## 2026-07-21 (v35 beat 7 — **THE 04P BENCH REPORT: TWO-LAYER ADJUDICATION ACCEPT — D1 [M] 5/5 CLOSED** · **THE M14 TRIGGER SWEEP: PASS 7/7 — M14 OPENS** · BENCH-CONST RE-MINTED (6/6 @ 25065) · WUCP P2 ×2 — both rows flip DONE · the incident + Q1–Q5 adjudicated · two candidate rows minted)

**The adjudication (basis stated honestly).** Layer-1 = the bench session's report (landed verbatim: `context/handoff/2026-07-21_04p-adoption_bench-session-report_for-hub-adjudication.md` — §7-checklist-shaped, refutation-welcoming, the right form) + Nick's operator record. Layer-2 (hub, HEAD-object reads at `355a711`): **the §7 checklist 1–5 ALL CONFIRM** — the five instruments at their cited sites (`device_left` :293 is `log.info` + `return`, ZERO state mutation; `ias_zone_type_learned` :621; `learned_zonetypes_rehydrated` :339; `permit_join_opened` :721; the JSON section per the beat-5 full-file read) · the flush-on-shutdown premise holds (`cache.flush()` at adapter `:401`; `flush()` `:335` writes immediately and ignores the backoff — **Gate 1's on-disk check is valid by construction**) · `ZoneType.java:26` **`CONTACT(0x0015, "contact")`** — 21 verbatim · `learnedZoneTypes.clear()` has exactly ONE site (`:575`, inside `load()`'s corrupt-catch — no leave path exists; the learn-survived-the-leave claim is structurally true) · the classify fork stands as audited at beat 5. Item 6: CI-green on `355a711` = Nick's direct confirmation of record (beat 6). The §1 DONE-WHEN table verifies against its own evidence; **the door-test-via-`viewPosition` inference is ACCEPTED as the correct instrument given D-1** (bracketed deltas +4/+10/+7 against a measured ~2/min idle). **ACCEPT. D1 [M] 5/5 IS CLOSED** — per the ruled accounting (light = banked-historical, HUE-RESET stands as a ledger row and was never a D1 blocker), with the contact class LIVE off the persisted learn, zero re-learns at adoption. The §8 assessment is endorsed: proven under adversity beats proven in the happy path — the persistence was exercised at exactly the failure it exists to tolerate.

**The incident, ruled.** Window arithmetic verified exact (open 20:24:35 + 254 s = 20:28:49; `device_left` at 20:28:52 = **T+3 s past close**). **H-B proximate, H-A structural, unified:** the long-press initiated the standard SNZB leave-then-fresh-join re-pair; the leave landed seconds after the window shut, so the fresh join had no open window and the device sat off-network — lawfully silent — until the recovery window's press drove the textbook `UNSECURED_JOIN → announce → propose → proposal_accepted source=config → device_adopted` arc. H-C unnecessary (indistinguishable, moot). The one-way-door asset was never at risk (§5's CONFIRM held at source). **Q1 RULED at source: NO hands-off re-propose path exists at `355a711`** — the adapter's only propose reference is the `:851` adopt-failure javadoc; proposals are announce-driven, in-memory, `PROPOSAL_MAX_AGE`-bounded, never rehydrated → **ADOPT-REPROPOSE minted** (candidate row: hands-off propose of a known-but-unadopted cached-interview device newly present in `adopt_devices` at boot; kills the Window-#2 press-hazard class; post-M14-open, non-gate). **Q2 → OBS-CONFIRM minted** (the D-1 gap is real and ironic — the product's hero question is "did it actually confirm?" and the bench cannot show one without inference; instrument-first; rule at B2 authoring). **Q3 accepted-as-documented** (degraded=1 = the sleepy power-cluster arm, the `EzspReportingOps` class; corpus note rides B2). **Q4 discharged by the class rule** (playbook §8, this beat — glance-points name their instrument's SEMANTICS: log-token vs event-type · boot-once vs live · terminal-materialization). **Q5 RULED: the BENCH-CONST counter is the `projection_live` replay-head** (the certification-era instrument correction of record — never the viewPosition) → **25065**. **D-1/D-2 are HUB-OWNED package defects, owned** (the Block-5 grep asserted an event type as a log token; Block-6 read a boot-once instrument at a non-boot glance); the v3 erratum is DECLINED as moot (the package is consumed; the class rule generalizes instead; the Window-#2 press-early choreography also folded into §8).

**BENCH-CONST RE-MINTED this beat** (the constants file's own charter authorizes the operator/hub re-mint — "a stale constant is a scenario defect"): `scenarios/constants.yaml` — fleet 2→**6/6** · `remembered-ulids` grown to ALL SIX (max teeth; durable by REG-INV-1, now re-proven through the leave/rejoin + three reboots) · watermark 14→**25065** (the aged-replay stake goes real) · panId `0x2d83`→**`0x774c`** (channel 20 holds) · the Hue's §3.9-skip standing note added at `command.entity` · the PROVISIONAL CMD-API blocks untouched. **Verify: ONE boot-health run on the Pi post-pull → [PASS] expected → boot-health GREEN ends the lawful-[FAIL] era.**

**THE M14 TRIGGER SWEEP — RUN ON EVIDENCE, THIS BEAT: PASS 7/7.** Certification ✓ (closed 2026-07-14; ~1,728 recorded verdicts, zero false CONFIRM) · RO ✓ (I3a `[PASS]` 07-18) · AVAIL ✓ (F2 both directions, measured, 07-18) · SEED ✓ (E2 discharged, the joins night) · zero open [REVIEW]s ✓ · G1 OUT ✓ (closed end-to-end: `f3bfd5c` + `frontend.yml` green) · **D1 5/5 ✓ (this beat)**. **M14 (integration-shelly) OPENS — 7 days ahead of the ~Jul-28 estimate, 10 ahead of the Jul-31 boundary.** Consequences: **the third-plug rider FIRES** (the ~$25–35 plug, just-in-time — Nick's purchase call) · the sequencing stands PRE-RULED (rider 3: CMD-API + the B2 port precede M14 WUs — no re-ruling) · **J1 stays FROZEN** — the mid-Aug gate (freeze Fri 08-14 EOD · the read Sun 08-16) is untouched by the trigger.

**WUCP P2 ×2 COMPLETE:** **W2-LEARN → DONE** (code `554e18c`, learn silicon-verified, gate-of-record subsumed-green, the adoption done-when met via LEARN-PERSIST) · **LEARN-PERSIST → DONE** (code `355a711` CI-green, the silicon leg complete under adversity). Backlog flipped + the beat-7 currency block; the W30 trigger-watch line updated; drift check clean (spine/backlog/snapshot/W30 consistent at this commit); NO skill edits (Check 9 stays PASS post-sync); the playbook §8 fold is the lessons vehicle (pm-lessons already carries the enumerate-at-source entry; the D-1/D-2 class lives in the playbook where operator-package authors read it).

**Nick, in order:** **(1) the bench commit** — from nexsys-bench, EXPLICIT path (exactly 1 M): `git add scenarios/constants.yaml` → `git commit -F ../_scratch/2026-07-21_bench_bench-const-remint_commit-msg.txt` → push → **Pi: `cd ~/nexsys-bench && git pull` → ONE boot-health scenario run → `[PASS]` expected → ⏺ paste** (boot-health GREEN closes the last instrument-staleness item). **(2) the hivemind beat-7 commit** — EXPLICIT paths, exactly 6 entries (5 M: pm-handoff · PROJECT_SNAPSHOT · the backlog · the playbook · the W30 plan + 1 ??: the bench report): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/planning/phase-3-milestone-backlog.md context/process/bench-troubleshooting-playbook.md context/planning/weeks/2026-W30_jul20-jul26.md context/handoff/2026-07-21_04p-adoption_bench-session-report_for-hub-adjudication.md` → `git commit -F ../_scratch/2026-07-21_hivemind_v35-beat-7_commit-msg.txt` → push. **(3) the third-plug call** — the rider fired; order at your discretion (~$25–35; it becomes an M14 DUT, never B1/B2 stimulus — the dual-role constraint stands). **(4) PELTON TODAY:** carry `context/programs/matter-design/memos/2026-07-19_C1-C4_counsel-handoff_pelton.md` (the four Matter/certification questions — wording stays counsel's; nothing gates on the answers) alongside your standing TAMORO posture (TSDR #1 reclassified RESPONSE-EQUIVALENT · moderate §2(d) tier · the goods-gap armor · R-1 HELD for G-2 · the tamoro.com brokerage, walk $3,400 · LLC timing = a good consult question · TSDR audit #2 = 07-31). Outcomes intake here as §6-pointer beats — paste or summarize at leisure. **(5)** The rulings queue stands (GAP-1+CORE-P1 · GAP-2 v1.1.2 · GAP-3/GAP-5 — GAP-1 before SKIP-VIS folds CORE-P1/P2 into one wave).

**Hub next (v35): CMD-API AUTHORING IS THE SINGLE NEXT ACT** — the week's pole, ~Jul-24 tripwire with real margin, grounding-first on the banked debts (auth-on-POST per the OR-APP-AUTH `installAuth` posture + token-freeze day one · `IdempotencyEntry` honored · `ProblemType` complete · wire-casing pinned at the endpoint vs the live camelCase precedent · the WebhookTrigger 4-grep route census · the B1-reduced command-lifecycle assertion surface; pre-verification ≥3 assumptions; host-CC lane; two-layer audit; the constants re-pin at flip rides the B2 port) → SKIP-VIS (+CORE-P1/P2 if ruled) → the B2 port (flips the SKIPPED stubs; the re-mint precedent set this beat) → FE-VERDICT-2 on the contract ruling → M14 WUs behind CMD-API+B2 → every Aug-14 freeze producer running.

## 2026-07-19 (v35 beat 6 — **BOTH COMMITS LANDED census-EXACT · `355a711` CI GREEN — THE LEARN-PERSIST GATE OF RECORD CLOSED · THE DEPLOY GATE IS OPEN** · **THE NO-TRAILER DIRECTIVE MINTED** · **THE CURRENCY SWEEP EXECUTED** (W30 · backlog · env-model · PM skill · pm-lessons))

**The landing.** Core `355a711` (exactly the 7 ordered paths, 714+/17−) pushed, **CI GREEN — the LEARN-PERSIST gate of record is CLOSED**; hivemind `fb4395d` (exactly the 6 ordered entries) pushed. Both censuses matched the staged messages exactly — no ride-along this time (note honestly: `add -A` matched only because no parallel writer had landed files between order and run; the explicit-paths rule stands ABSOLUTE, unchanged). **The deploy gate is OPEN: the v2 persist-verified 04P package is GO** on the Pi deploy of `355a711`.

**THE NO-TRAILER DIRECTIVE (Nick, 2026-07-19, standing):** commit messages NEVER carry `Co-Authored-By:` / AI-attribution / session-link trailer lines — any message, any repo, from now on. MINTED durably: env-model §9 (the rule's home) + the PM skill masthead's v35-arc disciplines line. The four landed messages that carry the trailer (`f3bfd5c` / `26c8637` / `355a711` / `fb4395d`) are immutable history — never rewritten, never repeated. Every message this hub stages from beat 6 forward is trailer-free (this beat's own msg file complies).

**THE CURRENCY SWEEP (Nick's directive: close out stale context before any future fresh session boots on it).** Findings + fixes, all in this beat's commit: **(1) the W30 plan** (Check 2's Monday read) was the largest trap — pre-minted Thursday, its entry gates and lanes 1/3/5 read as pending though the weekend completed them; a dated supersedes-where-they-differ currency note now heads it (the live remainder: D1 via the v2 package · CMD-API · B2/B3 · Pelton; every M14 condition ✓ except D1). **(2) the backlog** gains the v35 block: G1 CLOSED end-to-end · W2-LEARN re-scoped (row flips at D1 5/5) · the LEARN-PERSIST row minted code-DONE @ `355a711` · BENCH-CONST pinned to the Block-6 instrument values · SKIP-VIS+CORE-P1/P2 fold on the GAP-1 ruling · **MATTER-INT's stale "one-word ruling pending" SUPERSEDED** (resolved-to-program; R-0 + B1–B5 ruled; Phase C open). **(3) env-model §9** gains the no-trailer rule. **(4) the PM skill masthead** gains the v35-arc disciplines line (no-trailers · enumerate-file-shapes-at-source · subsumption-names-its-delta-class) — **Check 9 goes STALE on exactly ONE file (`project-manager/SKILL.md`) pending Nick's mirror sync.** **(5) pm-lessons** gains the enumerate-at-source entry (the LEARN-PERSIST [INFO] pair, discharged from beat 5's promise). Verified CURRENT with no edit needed: the playbook (beat-5 rewrite holds) · MODULE_CONTEXT (rode `355a711`) · coder-handoff/coder-lessons (the Coder's closeout entries) · cross-agent (ACTIVE 0) · the v36 banked prompt (its RE-DERIVE hedging absorbs everything this arc moved) · the coder + frontend skill sources (untouched; mirrors ≡).

**Nick, in order:** **(1) the hivemind beat-6 commit — EXPLICIT paths, exactly 7 M:** `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/process/cowork-environment-model.md context/lessons/pm-lessons.md context/planning/phase-3-milestone-backlog.md context/planning/weeks/2026-W30_jul20-jul26.md project-manager/SKILL.md` → `git commit -F ../_scratch/2026-07-19_hivemind_v35-beat-6_commit-msg.txt` → push. **(2) your skills mirror sync** (the one-file delta: `project-manager/SKILL.md`) → Check 9 returns PASS for every future session. **(3) THE SILICON LEG** — Pi: `cd ~/homesynapse-core && git pull` (expect `355a711`) → `./gradlew :app:homesynapse-app:installDist` → **the v2 package, Blocks 0–7** (`context/handoff/2026-07-19_04p-adoption_operator-mini-package_v2-persist-verified.md`) — the two ⛔ gates are the whole safety story: Window #1 closes on the ON-DISK persist verify (`"0x449FDAFFFE688F57" : 21`) BEFORE the 04P is listed; Window #2's boot must show `learned_zonetypes_rehydrated: count=1` before you press anything → ⏺ the pastes (Block 6 = the BENCH-CONST 6/6 inputs). **(4)** The rulings queue at leisure (GAP-1+CORE-P1 · GAP-2 v1.1.2 · GAP-3/GAP-5 — ruling GAP-1 before SKIP-VIS folds CORE-P1/P2 into one core wave). **(5)** Pelton Mon 07-21 (C-1..C-4 in hand).

**Hub next (v35):** the silicon pastes intake → **D1 [M] 5/5** (the sweep's last trigger condition) → BENCH-CONST authored from the Block-6 values → boot-health GREEN → WUCP P2 ×2 (the W2-LEARN + LEARN-PERSIST backlog flips + drift check) → **CMD-API authoring vs the ~Jul-24 tripwire** (the immediate desk work) → SKIP-VIS (+CORE-P1/P2 if ruled) → FE-VERDICT-2 on the contract ruling → **the ~Jul-28 M14 sweep on evidence** → the Matter Phase-C §-pointers.

## 2026-07-19 (v35 beat 5 — **LEARN-PERSIST INTAKE: TWO-LAYER AUDIT ACCEPT (zero [REVIEW]; nine [INFO] ratified — two hub-owned)** · the core commit ORDERED (7 M explicit) · **the 04P v2 persist-verified package AUTHORED** · the playbook §7/§8 currency folds)

**The audit (basis stated honestly).** Layer-1 = the Coder's record: G-LP-4 baseline RE-DERIVED forced-fresh 526/0/0 at `f3bfd5c` pre-edit (matches P14, no drift) · stage-A red 536/9 with EVERY red XML-verified for the right reason (the 10th leg — absent-section tolerance — green-at-stub by construction, the W2-LEARN 8th-leg class, stated not hidden) · stage-B **536/0/0 forced-fresh** · **mutation verification M1/M2/M3 all killed** (5/3/4 failures incl. each NAMED kill; M3's kill XML-confirmed as the DP-LP-4 INFO-absence pin firing on the construction-time INFO) with cmp-proven byte-identical restores + post-restore re-green · app 19/0/0 + lifecycle 56/0/0 forced-fresh · full `./gradlew check` GREEN · spotless clean · the DP-LP-8 zero-sweeps (0 added `public` in main diffs; the 2 frozen-token diff lines are added COMMENTARY, emitted strings untouched; 0 new locks; 0 time-access) — **PLUS Nick's independent re-runs** (clean.sh → forced-fresh module suite → full `check`, both GREEN — his CLI of record). Layer-2 (hub, Filesystem-MCP host-truth; mount currency proven by new-token presence-greps): **`ZigbeeDeviceCache` read IN FULL** — DP-LP-1/2/3 conform exactly (the top-level section written only-when-non-empty with unsigned-sorted keys · `recordLearnedZoneType` = lock + dirty ONLY, the debounce/F-14/shutdown-flush machinery untouched and carrying the I/O · `loadLearnedZoneTypes`: absent ⇒ silent, non-object ⇒ ONE WARN, per-entry skip + ONE WARN with parseable siblings applying · the corrupt-catch clears the new map · zone-type VALIDITY correctly deferred to the ingestion candidate-scan — the one tolerance path) · **`ZclIngestionUnit` ctor/seed read line-level** — the 9-arg overload with the 7-arg delegating (`Map.of()` + no-op sink; every pre-existing construction site byte-equivalent, proven by the 526 legacy greens) · the seed = a plain put through the shared `zoneTypeForZclId` scan, NO INFO / NO invalidation / NO sink at seed (DP-LP-4 verbatim) · **the sink at `:610-:616` AFTER the map put, both cases** (DP-LP-3) · `learnedZoneTypeCount()` = the observability seam · adapter wiring verified `:333-:339` (seed + `cache::recordLearnedZoneType` + the unconditional rehydrated INFO) · the race leg present with the `count=1` boot-glance pin ×2 and BOTH existing wire-learn `containsExactly` pins byte-intact (G-LP-5 held) · diff-stat corroborates 7 files / 714+ / 17−. **ACCEPT.**

**[INFO] adjudications ×9 — ALL RATIFIED.** Notables: **#6/#7 — the two minted token formats are RATIFIED AS FROZEN from here** (`zigbee.learned_zonetypes_malformed` · `zigbee.ias_zone_type_seed_unknown`, joining `zigbee.learned_zonetypes_rehydrated: count={}`) · **#8** unconditional count-0 INFO ratified (the `adoption_maps_rehydrated` anti-vacuous precedent — stronger than the instruction's letter) · **#5** unsigned-sorted serialization adopted as convention (custody-diff-friendly) · #1/#2/#4 ratified (honest stub-green; byte-preservation over DRY; harness field promotion with bodies byte-identical). **#3 + #9 are HUB-OWNED imprecisions, owned on the record:** the instruction's "two existing methods" against the file's three (carried from the W2-LEARN close-out prose, not enumerated at source) and P8's "Adapter ctor" against the actual `initialize()` sites (inferred from line numbers, not read). The Coder read both conservatively — correct. Lesson class rides pm-lessons at WUCP P2: file-shape counts and construction-site locations are ENUMERATED AT SOURCE at authoring, never carried from prose.

**The playbook currency folds (this beat — the WU's spine obligations + Nick's currency directive):** §7 REWRITTEN-CURRENT — the pre-DUR "registries rebuild empty / adopted devices re-propose fresh" paragraph was STALE against the live relink behavior and is marked HISTORICAL; the current semantics recorded (projections + relink-at-boot · **persisted learns + the `learned_zonetypes_rehydrated: count=N` boot glance as the standing instrument** · accept-list initialize-time · **the cached-interview race rule: enroll-before-propose holds ONLY for fresh interviews**). §8 gains the grep-guard rule (anchor ACTIVE lines; close-downs DELETE keys, never comment them). Skills: NO edits this arc — the mirrors stay 3/3 md5-≡; Check 9 stays PASS for every fresh session. MODULE_CONTEXT: current via the Coder's diff (rides the core commit). coder-handoff/coder-lessons: the Coder's WUCP-P1 writes verified present + well-formed (the restart-simulation harness lesson is a genuine asset — two production-adapter sessions over ONE data directory, the NOT_JOINED interceptor closing the resume gap).

**THE 04P v2 PACKAGE AUTHORED** (`context/handoff/2026-07-19_04p-adoption_operator-mini-package_v2-persist-verified.md` — SUPERSEDES the v1 package): the persist-verified two-window shape per the beat-3 adjudication — Window #1 closes with `~/bench.sh stop` → **the JSON persist verify** (`"0x449FDAFFFE688F57": 21` in `learnedZoneTypes` — the shutdown flush guarantees it regardless of the 30 s debounce) BEFORE the 04P is ever listed; Window #2's boot glance **`learned_zonetypes_rehydrated: count=1` is the one-way-door STOP gate** (count=0 ⇒ STOP + paste, never press); `position` is EXPECTED to advance past 18946 at adoption (record the final value — Block 6 captures the BENCH-CONST 6/6 inputs AT the instrument). ⛔ gated on: landed + CI green on the pushed SHA + deploy re-verified AT the Pi.

**Nick, in order:** **(1) the core LEARN-PERSIST commit** — from homesynapse-core, EXPLICIT paths (exactly 7 M): `git add integration/integration-zigbee/MODULE_CONTEXT.md integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/ZclIngestionUnit.java integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/ZigbeeDeviceCache.java integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/ZigbeeIntegrationAdapter.java integration/integration-zigbee/src/test/java/com/homesynapse/integration/zigbee/ZclIngestionUnitTest.java integration/integration-zigbee/src/test/java/com/homesynapse/integration/zigbee/ZigbeeDeviceCacheTest.java integration/integration-zigbee/src/test/java/com/homesynapse/integration/zigbee/ZigbeeWave2ContactAdoptionTest.java` → `git commit -F ../_scratch/2026-07-19_core_learn-persist_commit-msg.txt` → push → **the CI glance on the pushed SHA (the gate of record)**. **(2) the hivemind beats-4–5 commit** — EXPLICIT paths, exactly 6 entries (5 M: pm-handoff · PROJECT_SNAPSHOT · coder-handoff · coder-lessons · the playbook + 1 ??: the v2 package): `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/handoff/coder-handoff.md context/lessons/coder-lessons.md context/process/bench-troubleshooting-playbook.md context/handoff/2026-07-19_04p-adoption_operator-mini-package_v2-persist-verified.md` → `git commit -F ../_scratch/2026-07-19_hivemind_v35-beats-4-5_commit-msg.txt` → push. **(3) the Pi deploy** (pull → `installDist` → restart) → **the v2 package, Blocks 0–7** → ⏺ the pastes (Block 6 = the BENCH-CONST inputs). **(4)** The rulings queue stands (GAP-1+CORE-P1 · GAP-2 v1.1.2 · GAP-3/GAP-5) — **ruling GAP-1 before SKIP-VIS authoring folds CORE-P1/P2 into one core wave.** **(5)** Pelton tomorrow (C-1..C-4 in hand).

**Hub next (v35):** the silicon pastes intake → **D1 [M] 5/5** → the BENCH-CONST 6/6 re-mint authored from the Block-6 instrument values → boot-health GREEN → **WUCP P2 ×2** (W2-LEARN + LEARN-PERSIST: backlog flips · the pm-lessons imprecision-class entry · the drift check) → **CMD-API authoring vs the ~Jul-24 tripwire** (the week's core pole; grounding-first on the banked debts) → SKIP-VIS (+CORE-P1/P2 if ruled) → FE-VERDICT-2 on the contract ruling (parallel Cowork lane) → **the ~Jul-28 M14 sweep** (D1 the last trigger condition) → the Matter Phase-C §-pointers as they land.

## 2026-07-19 (v35 beat 4 — **THE LANDING RECORD:** hivemind `26c8637` (17-file — **the add-A ride-along, 4th occurrence, RECONCILED**) · core `f3bfd5c` ALL CHECKS GREEN — **THE G1 GATE OF RECORD IS CLOSED** · W2-LEARN's `554e18c` gate **SUBSUMED-GREEN** · LEARN-PERSIST dispatch HEAD = `f3bfd5c`, **GO given**)

**The hivemind landing + reconciliation (append-only, of record).** Nick's `git add -A` on the beats-1–3 order swept the Matter program's lawful 4-path tree (00_PROGRAM_STATUS M · the Phase-C design charter · the B4-r1 quote drafts · the C-1..C-4 counsel handoff) into the commit — **`26c8637` = the ordered 15-entry census + the program's beat-3 artifacts (17 files, the 2 archive pairs recording as clean 100% renames)**. Census verified from Nick's own pasted porcelain + commit summary: every path traces to one of the two claimed deliverable sets; nothing foreign rode; both pushes fast-forwarded (`a5aa9ba..26c8637` · `554e18c..f3bfd5c`). The commit message's "stages exactly 15" clause is STALE against the 17-file reality — **this beat is the append-only correcting record** (the 92ec713 / 3cc423b / 2b3d7bf class, 4th occurrence; the rule stands ABSOLUTE: explicit paths, never `-A`, while two-plus writers are live — the paste-ready `git add <paths>` block ships in every order for exactly this reason). **Consequence for the Matter program hub:** its beat-3 artifacts are now committed by `26c8637`; any banked program-side commit order for them is **CONSUMED — a named orphan, never run**; the program's next §-beat should pointer this reconciliation.

**The core landing.** `f3bfd5c` = exactly the ordered 14 (census exact, message true). Nick reports **ALL GitHub checks GREEN on the push** ⇒ **the G1 gate of record is CLOSED** (`frontend.yml` green on the pushed SHA — the lane's in-session green is now superseded by the real gate). Per the recorded subsumption precedent (OR-GATE-M7.4: a green run on a descendant subsumes the per-commit CI-confirm for the ancestor chain), ci.yml green on `f3bfd5c` — a **ZERO-Java delta** from `554e18c` (web-ui only) — **closes W2-LEARN's outstanding `554e18c` gate-of-record confirm as well** (the bench return's §1 ask: discharged).

**Status effects.** **G1 [M]: dispatched → returned → audited ACCEPT → landed → CI-GREEN. CLOSED end-to-end.** The M14 sweep's open trigger set = **D1 4/5 ONLY** (LEARN-PERSIST is its predecessor, dispatching now). FE-VERDICT-2 stays queued on the GAP-1/GAP-2 rulings. **LEARN-PERSIST dispatch note (the baseline-shift line):** core HEAD at dispatch = **`f3bfd5c`**, not the instruction's authoring HEAD `554e18c` — an EXPECTED, web-ui-only shift; every P1–P14 file:line signature lives in Java untouched by the delta and re-verifies unchanged; G-LP-4's 526/0/0 baseline re-derives forced-fresh AT `f3bfd5c` (expected identical; any drift ⇒ STOP per the gate). **The dispatch word is GIVEN** — the Coder launches on the standing line.

**Nick:** nothing owed right now. This beat's 2-M spine delta rides the NEXT natural hivemind commit (the LEARN-PERSIST intake beat) together with the Coder's WUCP-P1 writes — the order will census them all explicitly. Your program stands: Pelton Mon 07-21 · HUE-RESET · TSDR #2 07-31 · the beat-2 rulings queue at leisure.

**Hub next (v35):** the LEARN-PERSIST return → two-layer audit → commit order (explicit paths; fresh porcelain census incl. THIS beat's 2 M + the Coder's handoff/lessons writes) → CI → deploy word + **the re-authored persist-verified 04P operator package** → D1 [M] 5/5 → BENCH-CONST 6/6 → boot-health GREEN → W2-LEARN + LEARN-PERSIST WUCP P2 → **CMD-API authoring vs ~Jul-24** (desk work proceeds while the lane runs) → SKIP-VIS (+CORE-P1/P2 if ruled) → the ~Jul-28 M14 sweep → the Matter Phase-C §-pointers.

## 2026-07-19 (v35 beat 3 — **THE 04P BENCH-STOP INTAKE: TWO-LAYER AUDIT ACCEPT-WITH-ADJUDICATIONS ×2** · W2-LEARN's learn SILICON-CONFIRMED · the two-window guard CAUGHT the cached-interview race pre-door · **LEARN-PERSIST AUTHORED ISSUE-READY** (the D1 predecessor) · D1 stays 4/5)

**The intake.** Nick ran the 04P mini-package with a bench-hub conversation; the return (`context/handoff/2026-07-19_04p-adoption_BENCH-STOP_learn-persist-finding.md` — delivered in-conversation, LANDED on the spine this beat verbatim with two MARKED hub-adjudication annotations, italic + attributed, never silent) reports a deliberate ⛔ STOP at Window #1: the learn fired on the wire but AFTER the proposal. **The STOP was correct** — asymmetric irreversibility (a MOTION mis-adoption is durable; undo = full re-formation nuking all 5 identities) — and Window #1 did exactly what the two-window shape exists for: falsify the ordering on live silicon BEFORE the one-way door. The operator-package hazard-gate discipline paid its first falsification dividend.

**The audit (basis stated honestly).** Layer-1 = the return's own log evidence + Nick's pasted raw operator CLI (both boots' health tokens, the full Window-#1 arc, the close-down reads — corroborating, not derivative). Layer-2 (hub, §12 host-truth at `554e18c` objects) — **the mechanism chain 5/5 CONFIRM at source:** (1) `learnedZoneTypes` = in-memory `HashMap` (`ZclIngestionUnit:142`) with NO removal site anywhere — device-leave does NOT wipe it, only restart does (F-8's `invalidateHandlers` clears the HANDLER table only, `:560-:563`); (2) the accept list is initialize-time (`readAdoptAcceptList` in the ctor `:289`; `ADOPT_DEVICES_KEY` `:128`) ⇒ listing forces the wiping restart; (3) `adopt()`'s classify is an in-memory learned-ONLY read (slice `:348-:358`, "never blocking, never I/O", `orElse(null)`) with `EndpointClassifier.iasCapability` CONTACT-else-MOTION (`:175-:177`); (4) `adoptIfAccepted` fires at the propose outcome (`:815`); (5) `DeviceRemovedEvent` has NO production emitter (tombstone apply only) ⇒ durable. **Timeline arithmetic re-verified exact:** propose t0+0.773 s · TCLK t0+3.508 s · learn t0+9.979 s ⇒ the propose beat the learn by 9.206 s. **Close-down corroborated in the raw CLI:** `position=18946` byte-identical both boots (zero registry appends) · 5/5 AVAILABLE `viewPosition=21117` · `permit_join_opened` count 0 · config restored (the close-down `sed` removed even the commented key — cleaner than joins night's). The W2-LEARN silicon confirmation is genuine: `ias_zone_type_learned … zoneType=CONTACT (was MOTION)` on real silicon from a real ZoneEnrollRequest — the exact line the WU exists to produce.

**Adjudications (×2, both recorded as marked annotations in the landed return):** **(A-1)** the §6 "single window" payoff is OVERSTATED for the FIRST adoption — tonight's learn was in-memory and is ALREADY LOST; listing the 04P before a learn has PERSISTED recreates the exact race (empty rehydrated map at the 0.8 s propose). The first LEARN-PERSIST-era adoption keeps the TWO-window shape with the STOP re-pointed: window #1 verifies the learn fired AND PERSISTED (the new rehydrate glance) BEFORE the 04P is ever listed; single-window collapses only for SUBSEQUENT re-adoptions. The re-authored operator package must encode this. **(A-2)** §9's "G1 dispatch OUT still the other open condition" is STALE against beat 2 — G1 is dispatched + returned + audited ACCEPT; its residual is the `frontend.yml` gate-of-record glance on Nick's push. **D1 4/5 is the sweep's ONLY open trigger condition.** Carried ask: CI-green on `554e18c` was never explicitly confirmed (behaviorally live at the Pi; one Actions glance closes W2-LEARN's gate-of-record record).

**LEARN-PERSIST AUTHORED ISSUE-READY — with a design sharpening the hub's grounding found:** `zigbee-devices.json` ALREADY carries an additive per-device sidecar — `lastKnownAvailability`, "tolerated-additively by this loader," frozen `ZigbeeDeviceRecord` untouched — with the 30 s debounce, the F-14 write-failure backoff, the shutdown flush, and the LTD-11 lock discipline built in. The WU therefore RIDES `ZigbeeDeviceCache` (an additive top-level `learnedZoneTypes` section, IEEE-hex → wire `zclId`) instead of minting a new sidecar file: write-through from the shared learn core (BOTH feeders, change-case AND same-as-effective case — a first learn matching the resolver default is still a learned fact), a ctor-seed of `ZclIngestionUnit` from `cache.learnedZoneTypeIds()` (the cache at `:279` precedes the unit at `:323` ⇒ **rehydrate-before-joins holds BY CONSTRUCTION**), the silent-seed rule (NO `ias_zone_type_learned` INFO at seed — the e2e twin's `containsExactly` pin and operator glances depend on that token meaning exactly one thing; the one new anti-vacuous INFO is `zigbee.learned_zonetypes_rehydrated: count=N`), LEARNED-ONLY preserved (only persisted LEARNS seed; the unlearned/MOTION arm stays reachable), unknown-id tolerance via the same candidate-scan, corrupt/absent ⇒ fail-safe-to-current. ZERO: mints/module-info/build/config-schema/public-surface/classifier/adopt-trigger. Named mutants all REALIZABLE (the v34 lesson): seed-loop deletion · sink deletion · INFO-firing-seed. Tests-first incl. **THE RACE LEG** — a hardware-free restart-simulation in the e2e twin: session A learns via the production enroll path → new cache+unit from the same file → session B classifies CONTACT with ZERO frames processed (RED at HEAD; the standing regression of tonight's finding). Artifacts: `context/instructions/2026-07-19_LEARN-PERSIST_persist-learned-zonetypes_coding-instruction.md` + `context/pre-verifications/WU-LEARN-PERSIST.md` (P1–P14, same-day signatures at `554e18c`). **DISPATCH ON NICK'S WORD** (host-CC lane, homesynapse-core).

**Milestone/spine effects.** **W2-LEARN re-scoped on the record:** code-complete + learn-SILICON-VERIFIED; the adoption done-when TRANSFERS to LEARN-PERSIST's silicon leg; the backlog flip stays HELD (rides LEARN-PERSIST's WUCP). **The 04P mini-package: SUPERSEDED** (re-authors post-landing to the persist-verified two-window shape). **LEARN-PERSIST: candidate → REQUIRED predecessor** of D1 5/5. **BENCH-CONST:** correctly NOT re-minted at 5/5 (the volatile-baselines law held under pressure); waits for 6/6. **S31-ROUTE silicon confirmation:** still open, rides a future window (tonight's anti-action honored — only the 04P was touched). **New durable lessons ride the WU close-out:** coder-lessons (the cached-interview fast-propose race — enroll-before-propose holds ONLY for fresh interviews) · playbook §7 (restart wipes in-memory learns) + §8 (grep-guards anchor ACTIVE lines, never comments — the Block-1 near-miss) · the MODULE_CONTEXT gotcha (rides the WU diff). **CMD-API:** authoring is the hub's next work product, in parallel with the LEARN-PERSIST lane (disjoint modules; commits sequential); the ~Jul-24 tripwire stands at 5 days with the honest posture unchanged (C1's N already organically exceeded).

**Nick, in order:** **(1) the CONSOLIDATED hivemind commit (beats 1–3)** — the beats-1–2 msg file is a **NAMED ORPHAN, never run it** (`_scratch/2026-07-19_hivemind_v35-beats-1-2_commit-msg.txt`; its stages-exactly-12 clause went stale when beat 3 landed). EXPLICIT paths: the beat-1 list PLUS `context/handoff/2026-07-19_04p-adoption_BENCH-STOP_learn-persist-finding.md context/instructions/2026-07-19_LEARN-PERSIST_persist-learned-zonetypes_coding-instruction.md context/pre-verifications/WU-LEARN-PERSIST.md` → `git commit -F ../_scratch/2026-07-19_hivemind_v35-beats-1-3_commit-msg.txt` → push. Stages exactly **15 porcelain entries (2 M + 11 ?? + 2 D; the archive D/?? pairs record as 2 R)**; the Matter program's lawful tree (1 M + 3 ??) NEVER rides. **(2) the core G1 commit** (unchanged — explicit 14 paths, msg `_scratch/2026-07-19_core_g1-verdict-surface_commit-msg.txt`) → push → the `frontend.yml` glance (the gate of record). **(3) ONE Actions glance: CI green on `554e18c`** (closes W2-LEARN's gate-of-record record). **(4) THE WORD on LEARN-PERSIST** → launch the Coder (Claude Code, in homesynapse-core): `Read nexsys-hivemind/context/instructions/2026-07-19_LEARN-PERSIST_persist-learned-zonetypes_coding-instruction.md and its pre-verification, then execute — /nexsys-coder`. **(5)** The beat-2 rulings queue stands (GAP-1+CORE-P1 · GAP-2 v1.1.2 · GAP-3/GAP-5). **(6)** Your program: Pelton Mon 07-21 (C-1..C-4 in hand) · HUE-RESET · TSDR #2 07-31.

**Hub next (v35):** the LEARN-PERSIST two-layer audit on return → commit order → deploy word + **the re-authored 04P package** (persist-verified two-window; the rehydrate-INFO glance is the new window-#1 STOP condition) → D1 [M] 5/5 → BENCH-CONST 6/6 → boot-health GREEN → W2-LEARN + LEARN-PERSIST WUCP P2 (backlog flips + the lessons/playbook/gotcha folds) → **CMD-API authoring vs the ~Jul-24 tripwire** (already queued at desk; grounding-first on the banked debts) → SKIP-VIS (+CORE-P1/P2 if ruled) → the ~Jul-28 M14 sweep (D1 the last trigger condition) → the Matter Phase-C §-pointers.

## 2026-07-19 (v35 beat 2 — **G1 EXPLAINABILITY-UX RETURN: TWO-LAYER AUDIT ACCEPT** · the verdict surface delivered · GAP-1..5 + CORE-P1..P4 INTAKEN · the core commit ORDERED (14 explicit) · FE-VERDICT-2 queued)

**The audit (basis stated honestly).** Layer-1 = the lane's own record: its 8-check preflight (Check-5 B-class mock-vs-real re-derived at the rest-api source; Check-7 STALE the expected class — since CLEARED, see beat 1) + the in-session gate (`npm ci && npm run verify` GREEN in its container: 86/86 across 6 files incl. the axe suite · lint/typecheck clean · 35.1 KB gz vs the 100 KB budget · contract-check 11 endpoints @ v1.1.1-2026-07-02) — evidence, NOT the gate of record. Layer-2 (hub, §12 host-truth): **the §10 census EXACT** — a fresh lock-free porcelain shows core `554e18c` + exactly the claimed 14 (12 M + 2 ??, all under `web-ui/dashboard/`; `tokens.css` correctly absent) and the return file as hivemind's ?? — 15/15, nothing unexplained. **Java evidence 8/8 CONFIRM at HEAD `554e18c` objects:** the ten-value `CommandResultEvent` javadoc verbatim (incl. "the last four (including invalid) are DISPOSITIONS" + adapters-may-publish-more) · `deriveOutcome`/`isFailure` flatten exactly as cited (`StandardExplanationService.java:617-:656`; FAILED minted with `firstNonBlank(failureReason, outcome)`; UNCONFIRMED only from the timeout event) · the DP-B2 `completedVerdict` javadoc verbatim ("the UI tells them apart by the non-null run id", `:275-:311`) · BOTH ledger disposition strings byte-match the classifier constants (`StandardPendingCommandLedger.java:912-916` prefix-matched [the superseding id varies], `:922-924` exact-matched) · `brightness_percent` = the derived-at-query key (`MaterializedStateQueryService:113/:240`) · zigbee honest-unconfirmed reasons are profile-note-driven/VARIABLE (D-4's no-pattern-matching justified at source) · EventTypes `downgrad` zero-hit + `confirmation_downgraded` lives ONLY as `ConfirmationOverrideInstaller` WARN vocabulary (GAP-3 confirmed) · the rest-api HEAD census carries NO events/health endpoint class (B1/B2 stay mocked ✓). **Host-truth reads (Filesystem-MCP route):** `verdicts.ts` + `verdicts.test.ts` read IN FULL — ten distinct labels test-locked (set-size assert), superseded never error/warn + "not a failure" locked, the recovery classifier fires on deterministic wire truth ONLY (variable-reason negative fixtures present), posture-is-not-a-verdict locked, the silent-skip truth table exact; renderings presence-verified in every claimed surface (WhyNotView `ranFine = NEVER_TRIGGERED && lastRelevantRunId !== null` → "It did run" + the run-page bridge link · CausalChain do-nothing terminal "Finished …, but nothing was changed" + `classifyRecordedReason` applied to FAILED actions only + null `trigger.type` → "recorded before the current automations" + the Recorded-reason disclosure · RunsView `<em title={NULL_NAME_NOTE}>{runName(…)}</em>` — the 'An earlier automation' literal lives in `format.ts`, the return's wording verified accurate · DevicesView `role="status"` evidence-with-age line + derived-percent-only `brightnessDisplay` · the three scenarios registered wire-faithful · the mockData hardcode tokenized to `BRAND.productName`). **ACCEPT.**

**Adjudications (the lane's D-1..D-6):** **D-1 RATIFIED at hub level** — `timed_out` classed honest-can't-know conforms to the brief's §3 (its known-failed list deliberately omits `timed_out`; a send with no reply proves nothing) — noted to Nick for awareness, not a ruling ask. **D-2 CARRIED** — the live-fleet scenario's payload shapes are SOURCE-DERIVED (the Pi API is unreachable from the lane's container and from this hub); re-verify at the first live run, FE1_GO_LIVE §0 style. **D-3 = the one policy fold, ACCEPTED-PENDING-RATIFICATION:** exactly-null tolerance on `RunSummary.automationName`/`CausalChain.automationName`/`trigger.type` ahead of formal contract ratification — the engineering rationale holds (a validator that hard-fails the live wire on every prior-instance run would break the dev-runtime drift check on real data), the tolerance is pinned to exactly-null by tests (absence/wrong-type still FAIL), and the fold is RECORDED here as an adjudication, not a silent divergence. **D-4/D-5 ACCEPTED** (honesty over cleverness; the variable-reason class keeps the FAILED pill + verbatim reason until CORE-P1; the identify-class §1 pin is realized where the wire is deterministic, complete realization rides CORE-P1). **D-6 VERIFIED** (no new brand element/IA/hero class; scenario additions follow the registry pattern).

**The rulings queue (Nick, one-turn-rulable):** **(1) GAP-1 + CORE-P1 — PM REC: APPROVE.** Additive optional `resultOutcome: string` on the causal-chain action (v1.1 → v1.1.2, additive-only, backward-compatible) + the core fix at `StandardExplanationService.java:617-:656` ((a) carry the raw outcome; (b) `superseded` must not select the FAILED branch — an intent change is not a failure, your beat-5 pin; (c) zigbee's honest-`unconfirmed` command_result derives UNCONFIRMED with its recorded reason, not FAILED). **Fold into the SKIP-VIS WU** — CORE-P2's `completedVerdict` fix (`:288-:309`) is the same file, one instruction, one test wave. **(2) GAP-2 — PM REC: ratify the nullability annotation as a v1.1.2 CLARIFICATION** (annotation-only, no shape change), with **CORE-P3** (durable automation identity across YAML reloads — kills the null-name class at the root) standing as the named backlog candidate it already is (skill v1.4 §4c). **(3) GAP-3 (posture read surface, from the AMD-99 durable provenance — CORE-P4) + GAP-5 (`lastReported` on the A1 summary) — PM REC: rule as additive v1.1.2 candidates in the same conversation, or defer post-M14 at zero cost** (the lane renders honestly without them). **(4) SKIP-VIS** gains the lane's consuming-side endorsement + CORE-P2's exact lines — already minted, authoring queued behind CMD-API per charge order (or first, your call at dispatch). **FE-VERDICT-2** = the named next FE WU (swap the recovery to first-class `resultOutcome` endpoint-by-endpoint, render skip tokens, retire D-4's limitation, + the dark/light AA sweep of the three new scenarios) — dispatchable on the contract ruling, zero bench dependency.

**The core commit ORDERED (Nick, explicit paths — the beat-1 Next block carries the exact command).** Push → **`frontend.yml` on the pushed SHA is the gate of record**; the lane's in-session green is evidence only. WUCP P1 artifacts: none owed beyond the change-set (frontend lane; `MODULE_CONTEXT.md` rode it ✓).

## 2026-07-19 (v35 beat 1 — **LAUNCH (STALE — adjudicated + reconciled)** · **THE v34-TAIL RECONCILIATION** (beat-3 was never written) · **THE ROTATION EXECUTED** (393→99 KB · 115→20 KB) · the v34 prompt + the 2026-07-06 G1 brief archived · **v36 BANKED**)

**Launch (remote §12).** Filesystem-MCP scope verified at first act = **CORE-ONLY** (3rd occurrence; all hivemind traffic rode the bridge). §11: lock-free porcelain ×5 — docs `a53f474` · skills `dccc85f` · bench `1c705f6` all CLEAN; core CLEAN at `554e18c` except exactly the G1 lane's claimed 14 (adjudicated at beat 2 as the audited change-set); hivemind at `a5aa9ba` carrying the Matter program's own lawful tree (1 M + 3 ?? under `context/programs/`) + 3 hub-class ?? (the v35 + G1-lane prompts, the G1 return). Both spine frontmatters read pre-write. Mirrors: **3/3 composite-md5-IDENTICAL** (hivemind PM + coder trees; nexsys-skills frontend v1.4) — Nick's sync RAN; the G1 return's Check-7 STALE is CLEARED. Watermark **AMD-99, 175/53** re-derived at the register regeneration line. Check 2: W29 current through today; W30 pre-minted, flips at the Mon 07-20 boundary. Check 8: ACTIVE 0. Check 5: no open deferred gates (§4b clear). Check 6: the coder-handoff W2-LEARN entry carries its NEXT-WU pointer. Check 10: strategy-file spot-checks resolve.

**THE ADJUDICATION OF RECORD — the v34 beat-3 close-out was never written and never landed.** Hivemind HEAD = `a5aa9ba` (v34 beat 2); no beat-3 spine delta exists on disk (pm-handoff/SNAPSHOT stamps end at beat 2; the v35 prompt sat untracked). The v34 arc's tail — the 04P mini-package outcome · BENCH-CONST · the G1 refresh + dispatch · the v35 banking — had NO spine record until this beat; the v35 prompt's masthead was its only witness (honestly hedged RE-DERIVE in-prompt). Consequences adjudicated: **(a)** W2-LEARN's WUCP P2 never ran — the backlog flip is **HELD** on the silicon-leg answer (its done-when includes D1 [M] 5/5 via the 04P); **(b)** **BENCH-CONST NOT LANDED** (bench clean at `1c705f6`) — boot-health presumed still lawfully-[FAIL]-stale; **(c)** **fleet state is UNKNOWN to the spine** (last recorded 5/5 @ position 18946; the 04P adoption / D1 5/5 / 6-6 expectation is unverifiable from this container — the Pi API is unreachable) — **Nick's answer below is charge 0's gate**; **(d)** the preflight scored honestly — 10 PASS + Check 4 STALE (the backlog lags `554e18c`) + this reconciliation class ⇒ **aggregate STALE** ⇒ this session ran reconciliation-only (beats 1–2 + the rotation + the intake); **CMD-API authoring is HELD** until the spine reconciles (this commit) + the 04P answer lands. The ~Jul-24 tripwire is 5 days out — flagged, not breached; the W30 plan already carries CMD-API as the week's core pole.

**THE ROTATION (first-act 4, charge 1 — executed as its own beat, one commit order with the intake).** pm-handoff `393,258 B → 101,407 B`: the pre-v32 beat sections (43 sections, v31-beat-3 → v22-beat-1) + the masthead Prior-chain → `archive/pm-handoff-beats-rotated-2026-07-19.md` (§A the chain verbatim; §B the sections verbatim). PROJECT_SNAPSHOT `115,253 B → 20,853 B`: the masthead Prior-chain + the pre-v32 body blocks (37 blocks, same span) → `context/status/archive/PROJECT_SNAPSHOT-beats-v22-v31-rotated-2026-07-19.md`. Method: staged-copy-sourced (this hub provably the only spine writer since stage; mtime-guarded bridge commit-writes), assert-all-before-write, tails + sizes verified at the host after commit. Kept: the v32/v33/v34 beats + the prior rotation markers + **Open Risks untouched** (outside the charged scope). Full history in git.

**Archived:** the v34 prompt → `archive/` (first-act 3) · the SUPERSEDED 2026-07-06 explainability brief → `archive/` (its 2026-07-19 refresh governed the G1 lane). **v36 BANKED** (before depth, per the standing ledger): `context/handoff/2026-07-19_PM-mission-control_v36_orchestrator_session_prompt.md` — launch on Nick's word AFTER this beat's commit lands; states re-derive.

**Nick, in order:** **(1) the hivemind commit — EXPLICIT PATHS (the absolute rule):** `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/handoff/archive/pm-handoff-beats-rotated-2026-07-19.md context/status/archive/PROJECT_SNAPSHOT-beats-v22-v31-rotated-2026-07-19.md context/audits/2026-07-19_explainability-ux-lane_return.md context/handoff/2026-07-19_PM-mission-control_v35_orchestrator_session_prompt.md context/handoff/2026-07-19_explainability-ux-lane_session_prompt.md context/handoff/2026-07-19_PM-mission-control_v36_orchestrator_session_prompt.md context/handoff/2026-07-19_PM-mission-control_v34_orchestrator_session_prompt.md context/handoff/archive/2026-07-19_PM-mission-control_v34_orchestrator_session_prompt.md context/handoff/2026-07-06_explainability-ux-lane_session_prompt.md context/handoff/archive/2026-07-06_explainability-ux-lane_session_prompt.md` → `git commit -F ../_scratch/2026-07-19_hivemind_v35-beats-1-2_commit-msg.txt` → push. Stages exactly 12 porcelain entries (2 M + 8 ?? + 2 D; the archive D/?? pairs record as 2 R). Your porcelain will ALSO show the Matter program's lawful tree (1 M + 3 ??) — it NEVER rides this commit. **(2) the core G1 commit** — from homesynapse-core: `git add web-ui/dashboard/MODULE_CONTEXT.md web-ui/dashboard/src/components/CausalChain.tsx web-ui/dashboard/src/lib/api/contract.test.ts web-ui/dashboard/src/lib/api/contract.ts web-ui/dashboard/src/lib/api/mock/mockData.ts web-ui/dashboard/src/lib/api/mock/scenarios.ts web-ui/dashboard/src/lib/api/shapes.ts web-ui/dashboard/src/lib/format.test.ts web-ui/dashboard/src/lib/format.ts web-ui/dashboard/src/views/DevicesView.tsx web-ui/dashboard/src/views/RunsView.tsx web-ui/dashboard/src/views/WhyNotView.tsx web-ui/dashboard/src/lib/verdicts.ts web-ui/dashboard/src/lib/verdicts.test.ts` → `git commit -F ../_scratch/2026-07-19_core_g1-verdict-surface_commit-msg.txt` → push → **the `frontend.yml` glance on the pushed SHA (the gate of record)**. Stages exactly 14. **(3) ANSWER charge 0:** did the 04P mini-package run — window #1 wire-learn (`ias_zone_type_learned … CONTACT`) → accept-list → window #2 adopt (`device_adopted entities=1` BINARY_SENSOR/contact) → **D1 5/5** — and if yes, paste the BENCH-CONST capture values (final `registry.projection_live` counts + position + panId read AT the instrument)? If it did NOT run, it is charge 0 of the next beat and the mini-package stands ready. **(4) the rulings queue** (beat 2: GAP-1+CORE-P1 · GAP-2 v1.1.2 · GAP-3/GAP-5 · D-1 noted). **(5) your program:** Pelton Mon 07-21 (C-1..C-4 in hand) · HUE-RESET (dimmer ~$25 vs TOUCHLINK-RESET) · TSDR #2 07-31 · the B3 purchases at your discretion.

**Hub next (v35):** on the answers — BENCH-CONST authoring from the pasted instrument values → boot-health GREEN → W2-LEARN WUCP P2 close (backlog flip) → preflight re-scores PASS → **CMD-API authoring vs the ~Jul-24 tripwire** (grounding-first on the banked debts: auth-on-POST per OR-APP-AUTH's `installAuth` posture + token-freeze day one · IdempotencyEntry honored · ProblemType complete · wire-casing pinned at the endpoint vs the live camelCase precedent · the WebhookTrigger 4-grep route census · the B1-reduced command-lifecycle assertion surface; pre-verification ≥3 source-state assumptions; host-CC lane; two-layer audit; CI-green with real margin) → SKIP-VIS (+CORE-P1/P2 if ruled) → the G1 gate-of-record confirm → the ~Jul-28 M14 sweep on evidence → the Matter Phase-C §-pointers as they land.

## 2026-07-19 (v34 beat 2 — **W2-LEARN TWO-LAYER AUDIT: ACCEPT (zero [REVIEW])** · the core commit ORDERED · **THE MATTER B1–B5 RULINGS LANDED** (pointers) · the 04P mini-package AUTHORED · the 2b3d7bf ride-along reconciled)

**The audit.** Layer-1: the Coder's preflight re-verified every gate at source (G-WL-1..5 PASS; the 518/0/0 baseline RE-DERIVED forced-fresh at `9d40ce8` per P14, never carried; P13 verified at read: HA `0x0104` / ZDO `0x0000`); stage-A red HONEST (7/8 for the right reasons, failure messages captured; the 8th GREEN-at-HEAD exactly as the instruction predicted — stated, not hidden); stage-B **526/0/0 forced-fresh** (+8 = 7 unit legs + the enroll-driven end-to-end twin); **in-session mutation verification with cmp-proven byte-identical restores** (guard-deletion kills exactly the short-payload leg 1/32 · invalidate-deletion kills BOTH rebuild legs 2/32 — the shared core carries F-8 for the attribute AND enroll feeders · the payload-parse mutant proven by construction: the stage-A tree IS it); app 19/0/0 + lifecycle 56/0/0 forced-fresh; full `./gradlew check` GREEN in-session + Nick's independent module `--rerun-tasks` + `check` GREEN. Layer-2 (hub, §12 host-truth): the FULL main diff audited line-level against DP-WL-1..6 (the guarded LE parse · learn-BEFORE-respond, send-independent · the shared `learnZoneType(IEEEAddress, long)` core with semantics byte-preserved — the frozen `ias_zone_type_learned` INFO survives as diff-CONTEXT, never rewritten · the `zigbee.ingestion_profile_skipped` DEBUG with the Green-Power comment) + the MODULE_CONTEXT S31-ROUTE→CLOSURE rewrite verbatim-faithful to DP-WL-5 + both test diffs read at source (the twin: CONTACT truth via ZoneEnrollRequest ONLY, the learn-INFO pinned verbatim PRE-adopt, `ias_zone_enrolled` through the PRODUCTION send path — its first end-to-end exercise; the existing two methods byte-untouched, the `containsExactly` pin holds) + the mount-lag adjudicator (worktree diff-stat 26/49/196/63 EXACT vs the Coder's recorded census). **[INFO] ×5 ratified** — the notable one: the DP-WL-3 named mutant is UNREALIZABLE as stated (`respondZoneEnroll` is void/non-throwing — a plain reorder cannot gate the learn); hub-owned instruction imprecision, correctly classified by the Coder, the realizability lesson banked in coder-lessons 2026-07-19 with its instruction-author impact line — the hub ADOPTS it (name mutants against control-flow reality). The new MODULE_CONTEXT gotcha (enroll fixtures now LEARN as a side effect) is a valuable forward-guard. **ACCEPT → the core commit ORDERED** (below) → CI glance → deploy.

**The 2b3d7bf reconciliation (the ride-along class, 2nd occurrence):** the program's beat-2 commit carried the Coder's coder-handoff (+45) and coder-lessons (+8) WUCP-P1 writes alongside its claimed set. Content legitimate on both sides (both files were destined for the next hivemind commit regardless); recorded append-only, census verified at source. **The explicit-paths rule is now ABSOLUTE: every hivemind commit, every ordering agent, `git add <paths>` from the stated census — never `-A` — while two-plus writers are live.** The program's beat-3 artifacts (1 M + 3 ?? under `context/programs/`) sit uncommitted awaiting ITS own order; they never ride this beat's commit.

**THE MATTER RULINGS (consumed as pointers; the verbatim ledger is the program status file's):** **B1** (a) matterjs-server sidecar + the four riders · **B2** the scope cut as REC'd · **B3** phase-1 core APPROVED + MSS315 IN (droppable at order time without re-ruling) + ZBT-2 front-load + isolation Option C · **B4** (a)→(c)@Hub-trigger + the $0 lab-quote rider + the Adopter-join reserve · **B5** O1 reaffirmed + the spike RATIFIED (post-B1 ✓ + post-gate-read). **Phase C OPEN** (design charter on disk; the hub's two sharpenings folded as design rows — the Pi memory budget BLOCKING with the spike carrying the RSS obligation, Node 24.x pinned at packaging). Standing main-hub intakes: the doc-drift ×3 (Doc 12 §3.8 ServiceLoader + its watchdog/sd_notify text · Doc 18 "built, unwired" ×2 places [the hub's catch — §2/59 AND §4-row-6/94]) batched onto a convenient docs commit · **Q-A4-1** (confirm-before-ack — a Doc-01-class contract question) pre-logged for the Phase-C escalation path · C-1..C-4 ride Monday's Pelton queue (the handoff file on disk).

**The 04P mini-package AUTHORED (⛔ gated on landed + CI GREEN + deploy re-verified AT the Pi + the hub's GO):** `context/handoff/2026-07-19_04p-adoption_operator-mini-package.md` — the TWO-WINDOW shape honoring the human gate before the one-way door: window #1 long-press re-pair → **the enroll-payload learn fires ON THE WIRE (`ias_zone_type_learned … CONTACT` — the W2-LEARN silicon proof) verified BEFORE the 04P is ever accept-listed** → list the announced EUI → window #2 second long-press → the fresh enroll re-learns in the ADOPT session → `device_adopted entities=1` BINARY_SENSOR/contact → **D1 [M] 5/5** → close-down (permit key OUT) + the BENCH-CONST value capture (final projection_live counts + position + panId read AT the instrument — the re-mint authors from THOSE 6/6-era values, never the banked 5/5@18946; volatile-baselines law). KNOWN-HAZARD carried honestly: the learned map is in-memory and adoption must consume a learn from its OWN session — all silicon evidence (3/3 enrollments pre-propose) + the end-to-end pin support the ordering; a misordered adopt would be durable, so the STOP condition is explicit, and **LEARN-PERSIST** (persist/re-read learned zoneTypes across restarts) is minted as a named future-WU candidate for the backlog at WUCP P2.

**Nick, in order:** (1) **the core commit** — from homesynapse-core, EXPLICIT paths: `git add integration/integration-zigbee/MODULE_CONTEXT.md integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/ZclIngestionUnit.java integration/integration-zigbee/src/test/java/com/homesynapse/integration/zigbee/ZclIngestionUnitTest.java integration/integration-zigbee/src/test/java/com/homesynapse/integration/zigbee/ZigbeeWave2ContactAdoptionTest.java` → `git commit -F ../_scratch/2026-07-19_core_w2-learn_commit-msg.txt` → push → **CI glance on the pushed SHA** (both workflows). (2) **the hivemind beat-2 commit** — EXPLICIT paths, exactly 3 (2 M: pm-handoff · PROJECT_SNAPSHOT + 1 ??: the mini-package; msg `_scratch/2026-07-19_hivemind_v34-beat-2_commit-msg.txt`) → push. (3) **the Pi deploy** (the practiced pull → build → restart order) → ⏺ paste the mini-package Block 0 → **the GO fires on that paste**. (4) The program's beat-3 commit rides ITS order whenever you like. (5) Your program: Pelton Mon 07-21 (C-1..C-4 in hand) · HUE-RESET · the B3 purchase at your discretion.

**Hub next (v34):** the Block-0 paste → GO → intake the 04P pastes (**D1 5/5** + the BENCH-CONST capture) → author + order BENCH-CONST → boot-health GREEN → WUCP P2 for W2-LEARN (backlog flip · the LEARN-PERSIST row · the drift check) → **CMD-API authoring vs the ~Jul-24 tripwire** → G1 refresh + OUT (the sweep's last condition) → the ~Jul-28 M14 sweep. Rotation hygiene still queued for a dedicated window.

## 2026-07-19 (v34 beat 1 — **LAUNCH (PASS all 11)** · **THE 3cc423b SWEEP RECONCILED** (program-era staging rule minted) · **W2-LEARN AUTHORED ISSUE-READY** (S31-ROUTE CLOSED at desk) · the v33 prompt archived)

**Launch (remote §12):** Filesystem-MCP scope verified at first act — **CORE-ONLY** (as the v34 prompt predicted; hivemind traffic rode the bridge throughout). §11: lock-free porcelain ×5 — core/docs/bench/skills CLEAN at the claimed HEADs; hivemind carried exactly the beat-5 staged-on-word delta (2 M + 1 ??) — adjudicated PENDING-ORDERED-COMMIT, not a ghost; both spine frontmatters read pre-write. Baselines verified at source: core `9d40ce8` · docs `a53f474` · bench `1c705f6` · skills `dccc85f` · hivemind `162b651` (beat-4 landed) → `3cc423b` mid-session (below). Watermark **AMD-99, 175/53** re-derived at the register regeneration line. Preflight **PASS all 11**; adjudications: **Check 2** — W29 (IN_PROGRESS) current through 07-19; W30 pre-minted, flips at the Mon 07-20 boundary. **Check 8** — ACTIVE: 0. **Check 9** — **15/15 files md5-IDENTICAL** source-vs-container-mirror on BOTH trees (stronger than the size proxy; PASS-per-proxy, the remote class). **Check 11** spot: the six W2 types resolve at HEAD; MODULE_CONTEXT §M9.7-W2 present; the uploaded v34 prompt md5-≡ the banked copy. Required reads done in full (v33 beats 1–5 · the M9.7-W2 instruction + MODULE_CONTEXT §M9.7-W2 · the joins go-package + ADDENDUM · the criteria RATIFICATION block [21 M / 8 S; J1 FROZEN; freeze 08-14 → read 08-16] · the doctrine brief · the Matter program prompt).

**THE 3cc423b RECONCILIATION (of record; benign — the 92ec713 class):** Nick's `git add -A` on the beat-5 order swept the Matter program hub's freshly-written 6-file tree (00_PROGRAM_STATUS + the A1–A5 dispatches) into the beat-5 commit — **`3cc423b` = v33 beat 5 (3 paths) + the program's beat 1 (6 paths), combined**; its message's "stages exactly 3" clause is stale against the 9-file reality and THIS beat is the append-only correcting record. Census verified at source: all 9 files trace to the two claimed deliverable sets; nothing foreign rode; tree clean after. The program's own commit-msg `_scratch/2026-07-19_hivemind_matter-program-beat1_commit-msg.txt` is a **NAMED ORPHAN — never run it** (archive/delete at the next `_scratch` sweep). Root cause: the beat-5 sweep-guard predated the program hub's writes (the v27-beat-5 fresh-porcelain-at-commit-time rule, hit again under TWO live writers). **RULE MINTED (program-era commit hygiene): while the Matter program runs, every hivemind commit order stages EXPLICIT PATHS (`git add <path>…`), never `-A`/directory sweeps — lane returns may land on disk at any moment.** This beat's own order complies. Program-hub launch return otherwise verified conforming (write-isolation held — census-proven; price restated in every artifact; R-0 recorded; the live-fleet STOP fence present); its freshness rider is now formally backed by this hub's full 11-check PASS.

**W2-LEARN AUTHORED ISSUE-READY (charge 1 — the D1 closer):** grounding ran at source (HEAD object reads): **(a)** the enroll-parse gap CONFIRMED — `learnZoneType` has exactly ONE caller (the attribute arm, ZclIngestionUnit:376); the ZoneEnrollRequest arm (:399–:405) answers via `respondZoneEnroll` without reading the payload, yet ZCL8 §8.2.2.3 puts zoneType (uint16 LE) exactly there — the silicon truth (3 enrollments / 0 learns; the 04P HELD on the door) follows. **(b) S31-ROUTE verify-first CLOSED AT DESK:** `route()` gates on **the FRAME's APS `profileId()`** (:299, bare-return skip), never the cached descriptor; the night's S31 ingest (`on:true` @ stateVersion 7 with descriptor 0xC05E) proves its frames carry 0x0104 ⇒ **widening DECLINED**; the silent skip gains a DEBUG diagnostic as the standing instrument. **(c)** the S31 `best_effort` posture upgrade stays operational-not-code (the sanctioned re-emit on a post-deploy re-drive). AUTHORED: `context/instructions/2026-07-19_W2-LEARN_enroll-zonetype-learn_coding-instruction.md` (DP-WL-1..6 settled; G-WL-1..5 STOP gates; tests-first with the :315 enroll fixture as the red-first regression tooth + the enroll-driven end-to-end twin; zero mints/module-info/surface; module-info embedded verbatim) + `context/pre-verifications/WU-W2-LEARN.md` (P1–P14, same-day signatures; P14 states the 518/0/0 baseline AS-AT-`9d40ce8` per the volatile-baselines lesson). Silicon leg after landing+CI+deploy-re-verified-AT-the-Pi: ONE permit window + the 04P long-press re-pair → `ias_zone_type_learned … CONTACT` → accept-list → adopt contact → **D1 [M] 5/5** → **BENCH-CONST** (5/5 · position 18946 · panId 0x774c · the `${C.fleet.devices}` fold) → boot-health GREEN (the operator mini-package authors at the deploy word).

**Nick, in order:** (1) **the beat-1 commit — EXPLICIT PATHS (the minted rule):** `git add context/handoff/pm-handoff.md context/status/PROJECT_SNAPSHOT.md context/instructions/2026-07-19_W2-LEARN_enroll-zonetype-learn_coding-instruction.md context/pre-verifications/WU-W2-LEARN.md context/handoff/2026-07-18_PM-mission-control_v33_orchestrator_session_prompt.md context/handoff/archive/2026-07-18_PM-mission-control_v33_orchestrator_session_prompt.md` → `git commit -F ../_scratch/2026-07-19_hivemind_v34-beat-1_commit-msg.txt` → push. Stages exactly 6 paths (2 M + 2 ?? + the archive pair, records as 1 R). In-flight Matter lane returns under `context/programs/matter-design/returns/` may LAWFULLY show as extra ?? in your porcelain — they NEVER ride this commit. (2) **Launch the Coder on W2-LEARN** (Claude Code, in homesynapse-core): `Read nexsys-hivemind/context/instructions/2026-07-19_W2-LEARN_enroll-zonetype-learn_coding-instruction.md and its pre-verification, then execute — /nexsys-coder`. (3) The five Matter lanes: launch at will (the lines in 00_PROGRAM_STATUS.md; returns route to the PROGRAM hub for audit, not here). (4) Your program: Pelton Mon 07-21 · the HUE-RESET call · tamoro/veromo/LLC.

**Hub next (v34):** the W2-LEARN two-layer audit on return → commit order → deploy word + the 04P operator mini-package → D1 5/5 + BENCH-CONST + boot-health green → **CMD-API authoring vs the ~Jul-24 tripwire** → G1 refresh + OUT + SKIP-VIS → the ~Jul-28 M14 sweep (G1 is the last condition). Hot-path rotation (pm-handoff ~377 KB) queued for the next dedicated hygiene window — never mid-WU.

## 2026-07-19 (v33 beat 5 — **THE MATTER RULING** · the design-program hub CHARTERED · v33's final beat)

**The ruling (Nick, 2026-07-19, verbatim intent):** no hasty Matter code — but commit real capacity NOW to "*all* necessary research, deliberations/debate, full & comprehensive designs, and an implementation plan/procedure," coordinated by a dedicated mission-control conversation orchestrating the research/deliberation Cowork sessions, so everything is ready for diving in; timed against his week (fast bench passes; counsel/domain work pending). The hub's REC (design-first, no code pre-Lock/pre-sweep) is ACCEPTED and EXTENDED into a full program. **The price stands unchanged:** the Gen4 plugs remain stimulus-only radios-off; J1 is FROZEN; no core writes from the program ever; code sequencing stays the MAIN hub's post-sweep call.

**CHARTERED:** `context/handoff/2026-07-19_matter-design-program_hub_session_prompt.md` — the standing program-hub brief (write-isolated to `context/programs/matter-design/`; phases A [research ×5: stack · transport/DUTs · architecture-fit · the moat-translation question · #2/certification] → B [one-turn-rulable memos: stack/scope/DUT-budget/certification/sequencing] → C [Doc-19 draft + independent review → the main-hub Lock flow] → D [implementation plan + the Matter bench charter]). Launch line: `Follow all instructions in nexsys-hivemind/context/handoff/2026-07-19_matter-design-program_hub_session_prompt.md — /nexsys-project-manager`. Runs PARALLEL to v34; the main hub consumes §-pointer beats (the counsel-program §0b discipline).

**Nick, in order:** (1) **the beat-5 commit** — msg `_scratch/2026-07-19_hivemind_v33-beat-5_commit-msg.txt`; expected porcelain: **exactly 3 entries = 2 M (pm-handoff · PROJECT_SNAPSHOT) + 1 ?? (the program prompt)** → push. (2) Launch **v34** (the gate path: W2-LEARN → D1 5/5 → BENCH-CONST → CMD-API → G1) and, whenever you like today, **the Matter program hub** (fresh conversation, the launch line above) — two parallel conversations, both built to run on rulings-only attention. (3) Your program: Pelton Mon 07-21 · tamoro/veromo/LLC · the HUE-RESET call · the program's Phase-B rulings as they arrive.

**v33 CLOSES HERE — five beats:** launch + the corruption repair → the scope finding + M9.7-W2 authored → the audit + commit ordered → THE JOINS NIGHT (E2 ✓ · B2 ✓ · H4 ✓ · D1 4/5) + v34 banked → the Matter program chartered. The hub retires on the v34 launch.

## 2026-07-19 (v33 beat 4 — **THE JOINS NIGHT: CLOSED** · E2 ✓ · B2 ✓ · D1 4/5 live · H4 ✓ · WUCP P2 complete · **v34 BANKED**)

**The night's evidence chain (all ⏺ pastes in-conversation, adjudicated live):** deploy `9d40ce8` re-verified AT the Pi (git HEAD + fresh installDist + RADIO UP 11 s) with **H4 [S] scored** on the deploy boot's honest-restart glance (projection_live 2/2@14 · relinked ×2 same ids · zero proposed · resume ch20/0x2d83). B2 baseline banked (viewPosition 18669). Custody reset exact (4 files → dated backup; `zigbee-devices.json` preserved; the `.retired` artifact left in place). **Boot #1 = THE SEED FORMATION: `zigbee.tc_link_key_posture: posture=generated_seed context=formation` · `channel_pinned: 20` · `network_formed: channel=20 panId=0x774c` — E2's line on the record**; the S31 gate-crashed window #1 in 3 s exactly as choreographed and **matched its new profile on the wire** (`profile=sonoff_s31_lite_zb`, COMPLETE). The 03P re-paired with `key_established: TC_REQUESTER_VERIFY_KEY_SUCCESS` — the TCLK arc under the GENERATED seed — and re-enrolled. The 02P/04P/01P joined in ~90 s total, each COMPLETE, each profile-matched, each with its own `key_established` (**five clean TCLK arcs, zero failures, night total**). Adoptions: 02P `reporting_configured 3/3/0` (a PERFECT sleepy drive) · 01P `1/1/0` (the R2(B) battery-only sensor) · S31 adopted with `confirmation_downgraded: on_off … posture=VERIFIED_REPORTS/SLEEPY outcome=best_effort` (below). **Final: 5 devices / 5 entities, projection_live 5/5@18946, Wave-1 ULIDs byte-identical baseline→final — B2 [M] PROVEN.** The 02P's live state: **temperature_c 26.1 · humidity_pct 51.7 · battery_pct 100 — the M9.7-W2 handlers proven on silicon.** Permit key OUT (grep count 0; the SD-5 exception window is closed).

**Adjudications of record:** (1) **The 04P CONTACT guard fired BY ABSENCE — the hold held.** Three enrollments tonight produced ZERO `ias_zone_type_learned`: on real silicon nothing elicits the zoneType attribute onto the wire (edges carry zoneStatus; the enroll-request parse evidently does not feed the learn). The 04P ends the night joined/enrolled/proposed/HELD, mounted on the door (gap <10 mm; RF from the mount proven — its join frames reached the coordinator), one long-press-in-a-window from adoption once **W2-LEARN** lands. Its mid-night `device_left` → join-limbo arc (the door move triggered a fresh-join retry AFTER the window's transient key expired) is a corpus-worthy class: sleepy fresh-joins need an open window; secured rejoins do not. (2) **The S31 surprise — reports INGEST.** C-05 closed at the cache: `profileId: 49246` = 0xC05E (the machine capture right, SmartThings wrong). Yet its state reads `on: true` at stateVersion 7 — inbound reports ARE landing despite the route() 0x0104 gate ⇒ working hypothesis: ZLL-descriptor devices EMIT frames carrying 0x0104 (interop posture). **S31-ROUTE demotes from required-fix to a verify-first code-read folded into W2-LEARN.** The `confirmation_downgraded` WARN = the configure-verify TIMEOUT class (the z2m #20618 family) — AMD-97 honestly downgraded CONFIRMABLE→best_effort rather than trust the seed; NOT stranded: `applyPostureRouting`'s sanctioned re-emit upgrades it on any post-fix re-drive. The button-press probe was inconclusive (no observed toggle); the S31's confirm legs ride CMD-API properly. (3) **The Hue is stranded off-network, recorded honestly:** its factory reset needs touchlink or a Hue dimmer (neither in-house; it joined factory-new originally — an operator constraint, not an error); its API `AVAILABLE` is rehydrated history = **the live AVAIL-RECONCILE exhibit**; Hue-targeted bench-hero actions lawfully §3.9-skip meanwhile (the adjudicated class). Decision row **HUE-RESET** (dimmer ~$25 vs a TOUCHLINK-RESET WU) rides Nick's ledger. D1's light class stands on the banked certified corpus. (4) **boot-health [FAIL] is LAWFUL instrument staleness** — the scenario pins `devices=2 entities=2` against a 5-device fleet: the pre-named **BENCH-CONST** re-mint (5/5 · position 18946 · panId 0x774c · `${C.fleet.devices}`) is priority-quick in v34; until it lands the regression floor reads red for exactly this known reason. (5) **D2 honest note:** the 01P adopted; presses produced NOTHING observable unbound (the dossier §6.4-e class) — "presses log-visible" is NOT yet confirmed; BTN-AMD-era work owns it. Its battery attributes populate on the 1–2 h cadence. (6) SNZB battery covers don't open — re-announce paths are long-press re-pairs in a window, never battery pulls (operator constraint of record). (7) Filesystem-MCP scope flipped to core-only mid-session after the host-side Coder session (§12 volatility, second observed occurrence; every hivemind write rode the bridge path unaffected).

**WUCP Phase 2 for M9.7-W2: COMPLETE this beat** — backlog flipped DONE + the beat-4 currency block minted (W2-LEARN · BENCH-CONST · HUE-RESET · MATTER-INT rows); pm-lessons ×2 appended (mechanism-sweeps + volatile-baselines); MODULE_CONTEXT current from Phase 1; no skill edits (Check 9 unaffected); drift check rides the v34 launch preflight.

**THE M14 SWEEP POSITION (~Jul-28, boundary EOD Jul-31):** certification ✓ · RO ✓ · AVAIL ✓ · zero open [REVIEW]s ✓ · **SEED ✓ (TONIGHT)** · **remaining: the G1 dispatch OUT — ONE condition.**

**v34 BANKED:** `context/handoff/2026-07-19_PM-mission-control_v34_orchestrator_session_prompt.md` — v33 retires on its launch (the deepest-context hub arc yet; the rotation hygiene it flags is real). **Nick, in order:** (1) **the hivemind beat-4 commit** — msg `_scratch/2026-07-19_hivemind_v33-beat-4_commit-msg.txt`; expected porcelain: **exactly 5 entries = 4 M (pm-handoff · PROJECT_SNAPSHOT · the backlog · context/lessons/pm-lessons.md) + 1 ?? (the v34 prompt)** → push. (2) **LAUNCH v34 in the morning** (fresh conversation; the read-and-follow line) — its first acts: W2-LEARN grounding-first → the 04P adoption → D1 5/5 → BENCH-CONST → then CMD-API vs the ~Jul-24 tripwire. (3) Sleep — the night earned it. (4) Your program: the Pelton consult Mon 07-21 · the Matter one-word ruling · the HUE-RESET call · tamoro/veromo/LLC.

**Hub next: v33 RETIRES on the v34 launch.** The conversation's yield: M9.7-W2 authored→landed→deployed→silicon-proven in one arc · the joins night executed on the one-formation economy with every identity stable · two MUST rows discharged (E2 · B2) + H4 · D1 to the brink · the doctrine compounding visibly (the guard that held the 04P, the fleet that caught the sentinel, the WARN that downgraded honestly).

## 2026-07-18 (v33 beat 3 — **M9.7-W2 AUDIT: ACCEPT** · R1/R2 ratified · the S31-ROUTE watch · the core commit ordered · the Matter REC)

**The intake.** The Coder delivered M9.7-W2 on the working tree at `1aa809d` (18 porcelain entries, all integration-zigbee + MODULE_CONTEXT), full-bar: tests-first red-first staged honest (stage-A exactly 30 red, every one for the right reason), stage-B + fleet fixes **518/0/0 forced-fresh** (466 baseline + 52 new), full `./gradlew check` GREEN ×3 in-session, app **19/0/0** + lifecycle **56/0/0** forced-fresh (`RegistryApplyCensusTest` + arch rules hold), and Nick independently re-ran `clean.sh` + the module suite `--rerun-tasks` + full `check` — all GREEN. Zero mints (73/43/55), zero module-info/build/schema/public-surface diffs, frozen tokens untouched, `ReportingConfigurator` untouched.

**TWO-LAYER AUDIT: ACCEPT.** Layer-1: the Coder ran a 5-lens pre-write source-verification fleet — which caught, BEFORE any code existed, that `ZclCodec` sign-extends int16 (the temperature invalid sentinel is **−32768 post-decode**, never wire 0x8000; a test written from the instruction's framing would have enshrined the wrong constant and left a fabrication hole where a mis-typed uint16 0x8000 publishes 327.68 °C) — plus a 4-lens adversarial diff review; every finding closed in-session (two classifier mutation survivors gained killer tests; DP-2 gained `doesNotContain(0xFC37)`; the band guard closed the demonstrated hole). Layer-2 (hub, direct host-truth reads via the §12 route): the §1 JSON **verbatim-exact** against the instruction block (all four entries, field-for-field, notes included; DP-1 S31 exact_model-only; DP-10 provenance honest); both handlers (canonical schema keys, post-decode sentinels, band guards, package-private final, the F-9 idiom); the FULL classifier (DP-6 `iasCapability` CONTACT-else-motion; occupancy-outranks unconditional; DP-7 battery-only as the FINAL arm before empty; DP-8 exact; the 1-arg delegate); the adapter tail (the `CacheDeviceResolver` MOTION stub byte-untouched).

**Adjudications of record:** **[REVIEW] R1 ACCEPT** — the two DEBUG out-of-band skip lines mirror `PowerConfigurationHandler`'s shipped F-9 shape; DEBUG-level non-`zigbee.*` diagnostics need no format-freeze (the LTD-15 freeze clause covers scenario-vocabulary tokens); drop behavior is test-pinned. **[REVIEW] R2 ACCEPT** — the temperature band guard is spec-plus in exactly the doctrine's direction (never a fabricated reading; the fleet demonstrated the failure path; independently revertible: one if, two constants, three tests). **[INFO] ×6 ratified**, incl. `ZigbeeWave2ContactAdoptionTest` (a new ladder class realizing the instruction's own mandated §4 end-to-end leg with the learn-before-adopt choreography guard pinned — the Files-table [INFO] protocol followed correctly) and the `ZigbeeProfileLoaderTest` 2→6 census adaptation (sanctioned by the P2-sweep clause). **HUB-OWNED MISSES, owned on the record:** (1) pre-verification **P9 REFUTED** — the hub's count-pin sweep grepped resource-name strings + `allProfiles()` and missed the `loadBundled()`/`extracting(profileId)` census pin; the durable lesson: **sweep count-pins by MECHANISM (every caller of the loading entry point), never by name-string grep alone.** (2) The instruction's G-W2-6 "452" baseline was stale by exactly one WU (M9.6-AVAIL +14 → 466); lesson: **state suite baselines as at-commit-X or re-derive at authoring, never carry a count forward.** Both fold into pm-lessons at the WU close-out.

**NEW WATCH MINTED — S31-ROUTE.** The Coder's MODULE_CONTEXT note: `ZclIngestionUnit.route()` gates ZCL dispatch on APS profile `0x0104`. Composed with the S31's open 0xC05E-vs-0x0104 CoB (dossier §6.1-a): a 0xC05E interview would drop the S31's REPORTS at routing — adoption/classification UNAFFECTED (exact_model carries), confirmations degrade to honest `command_confirmation_timed_out`, **never a false CONFIRM** (the moat holds either direction). The joins go-package now carries an ADDENDUM: the Block-8 S31 press read doubles as the probe; ⏺ the S31's interview profile value either way (closes CoB C-05); a dropped-report finding = STOP on that leg + the pre-named **S31-ROUTE** routing-widening micro-WU. 

**WUCP state:** Phase 1 complete (MODULE_CONTEXT M9.7-W2 section verified present; coder-handoff entry + coder-lessons appended — both ride THIS hivemind commit). Phase 2 completes at beat 4 after the core commit lands + CI green (backlog row flip, drift check, the constants re-mint order post-joins).

**THE MATTER REC (Nick's question; pending his ruling).** No Matter code before the gate. Grounds: (1) the Gen4 plugs are RULED stimulus-only — bench LAN RPC, Matter/Zigbee radios never provisioned (stimulus independence + the third-plug rider); their bench contract is already fully served by the dossier §5 HTTP driver + the Gen4 ident-gate constants item. (2) Zero of the 21 frozen MUST rows touch Matter; J1 is FROZEN. (3) A Matter integration is a NEW SUBSYSTEM (design doc → Phase 2 interfaces → Phase 3 + a Java Matter-stack dependency decision) — weeks-scale, squarely the M14+/V1.x integration program the roadmap return already frames. REC: mint a named post-gate backlog row (**MATTER-INT**: design-doc-first, seeded by the integration-roadmap return's Matter section + the Gen4 `Shelly.GetDeviceInfo` captures at provisioning), and OPTIONALLY charter a write-isolated **design-research lane** (no core writes) to draft the Matter design-doc groundwork in parallel once CMD-API is dispatched — parallelism without touching the critical path. The joins → CMD-API → G1/SKIP-VIS → M14-sweep window stays undiluted.

**Nick, in order:** (1) **The core commit** — msg `_scratch/2026-07-18_core_m97-w2_commit-msg.txt`; a fresh porcelain must show **exactly 18 entries (12 M + 6 ??), all integration/integration-zigbee** → push → **CI glance on the pushed SHA** (both workflows). (2) **The hivemind beat-3 commit** — msg `_scratch/2026-07-18_hivemind_v33-beat-3_commit-msg.txt`; expected porcelain: **exactly 5 M (pm-handoff · PROJECT_SNAPSHOT · coder-handoff · context/lessons/coder-lessons.md · the joins go-package [the S31-ROUTE addendum])** → push. (3) **The Pi deploy** (the practiced runbook order: pull → build → restart) → run **go-package Block 0** (the sha + banner re-verify AT the instrument) → ⏺ paste it HERE. (4) On the hub's **GO** (issued on that paste): THE JOINS NIGHT, blocks in order, pastes either way. (5) Your program: the 07-21 Pelton consult · tamoro (walk $3,400) · veromo.com · LLC · the Matter ruling (one word: row-only / row+design-lane / write-code-now — REC is row+design-lane at CMD-API dispatch).

**Hub next (v33):** intake the Block-0 paste → THE GO → intake the joins-night pastes (D1 5/5 · B2 · E2 flips; the S31 C-05 read; constants re-mint order; corpus entries; beat 4 = WUCP P2 close incl. backlog + pm-lessons ×2) → CMD-API authoring vs the ~Jul-24 tripwire → G1 refresh + OUT + SKIP-VIS authoring → the ~Jul-28 M14 sweep.

## 2026-07-18 (v33 beat 2 — **THE SCOPE FINDING** · Nick's one-WU ruling · **M9.7-W2 AUTHORED ISSUE-READY** · the joins-night go-package)

**The finding (grounding-before-authoring, source-verified at core `1aa809d`).** The v33 charge framed the profiles WU as data-only. Three legs refuted at source: **(1)** `ClusterHandlers.forDevice` ships EXACTLY six handlers — no TemperatureMeasurement/RelativeHumidity handler exists, and the classifier's `0x0302` arm installs `temperatureMeasurement()` only ⇒ a 02P adopted tonight would carry a temp-only capability set forever and its readings would drop at `ingestion_unhandled_cluster`. **(2)** IAS classification installs `motion()` unconditionally (device type `0x0402` is not in the classifier's table; the fallback's IAS arm never consults zoneType) — the zoneType-aware machinery already exists DOWNSTREAM (`IasZoneHandler` maps CONTACT→`open`; `ZclIngestionUnit` wire-learns at :497–:514 and prefers learned at :545; the adapter's `zoneTypeFor` at :1026 is the documented MOTION stub) ⇒ the 04P would adopt as a motion-typed binary_sensor. **(3)** the battery-only remainder returns `Optional.empty()` ⇒ an accept-listed 01P would register an ENTITY-LESS device and then trip the M9.5-DURb half-registration repair arm into a lawful re-propose/re-adopt loop on every announce — R2(B)'s "adopts battery-only (sensor/battery)" is not realizable at HEAD. **The clincher: adoption is a ONE-WAY DOOR at V1** (no `device_removed` emitter; adopted entities never re-classify) — a wrong adoption tonight is durable for gate day. `StandardCapabilities` already carries `humidityMeasurement()`/`contact()`/`battery()` — every fix is integration-zigbee-internal, zero public surface, zero mints.

**The ruling (Nick, 2026-07-18, presented one-tap with REC):** **ONE WU — M9.7-W2, profiles + code**; the joins night gates on it. (Alternatives offered: two WUs; a reduced S31-only night; challenge-the-finding.)

**AUTHORED ISSUE-READY:** `context/instructions/2026-07-18_M9.7-W2_wave2-profiles-and-classification_coding-instruction.md` — §1 the four profile entries VERBATIM (S31 exact_model-only per the 0xC05E-vs-0x0104 CoB [dossier §6.1-a — "the profile field is CONFIRM-ON-BENCH before any fingerprint ships"]; 02P/04P/01P exact_model + INERT machine-capture fingerprints [the 04P `0xFC57` capture-led per §6.3-g; the 01P has no `0xFC11`]; the S31 `confirmation[]` = the wave's ONE confirmable — EXACT_MATCH `OnOff/0x0000` · VERIFIED_REPORTS · ON_CHANGE · CONFIRMABLE · seed 5000 ms stated-as-seed · the three degrade rules; sensors `confirmation: []` per the 03P precedent; provenance note goes honest [DOSSIER-SOURCED pre-silicon; loader never parses provenance — verified]); §2 the two ingestion handlers (canonical schema keys, 0.01-unit scaling, invalid sentinels 0x8000/0xFFFF emit NOTHING, the −20.00 °C sign-extension vector embedded); §3 classifier completion (0x0302 arm +humidity+battery; zoneType-aware IAS — CONTACT⇒contact(), unlearned⇒motion() byte-equivalent, occupancy-outranks-IAS pinned; the battery-only⇒SENSOR+battery arm realizing R2(B)); §4 the learned-zoneType plumbing (expose the ingestion unit's learned state to the slice's classify path; the adapter's :1026 MOTION stub deliberately UNTOUCHED — it is the handler-table fallback and :545 already prefers learned; NO live ZCL read — DP-6 records the choreography guard instead). 11 settled DPs · 6 STOP-on-mismatch gates · P2 fan-out survey (handler-table pin; classification-outcome pins; publish-count pins; ctor arity; zero-module-info self-consistency checked against the DPs) · tests-first red-first with fixture-paired false-verdict bounds (the WB01/-04PR2 predecessor traps become regression teeth) · zero mints 73/43/55 · zero module-info/build/schema/public-surface. Pre-verification: `context/pre-verifications/WU-M9.7-W2.md` (P1–P14, all same-day signatures, incl. the refuted-assumption record).

**THE JOINS-NIGHT GO-PACKAGE AUTHORED (⛔ gated):** `context/handoff/2026-07-18_joins-night_operator-go-package.md` — gates: M9.7-W2 landed CI-green + deployed RE-VERIFIED AT THE PI + the hub's GO. Blocks 0–9: deploy re-verify → **the B2 baseline read FIRST** → config preflight (permit_join_duration 254; adopt_devices untouched till Block 6) → custody reset (mv zigbee-network.json + secrets.enc + .root-key to a dated backup; **zigbee-devices.json PRESERVED**) → boot #1 = THE SEED FORMATION (`tc_link_key_posture: posture=generated_seed context=formation` = the E2 line; rehydrated devices=2; permit window) → Wave-1 rejoin (relink ×2, ZERO proposes; **the S31 gate-crash caveat recorded — powered factory-fresh, it chases the first window; lawful, EUI recorded**) → the four joins in order with wake-press discipline + EUI capture (serials `25471900074240`/`25303900144196` bind at announce) + **the 04P CONTACT-learn guard: `ias_zone_type_learned … CONTACT` MUST print before Block 6 or STOP** → accept-list ×4 (the log's own `0x`+16-hex rendering) → boot #2 adoption ×4 (`proposal_accepted source=config` → `device_adopted entities=1` ×4; `half_registration_detected` ×0 or STOP; the restore posture line = a free AS-FORMED read) → the D1/D2/B2 reads (6 devices; classes light·occupancy·switch·temp/hum/battery·contact·battery-only; Wave-1 ULIDs byte-identical vs baseline) + per-class live evidence → close-down (REMOVE permit_join_duration; boot-health `[PASS]`). Anti-actions: ZBDongle-P stowed · Shelly Gen4 radios never join anything · no topology moves · Hue on direct wall power · no long-press on joined devices.

**Nick, in order:** (1) **The hivemind beat-2 commit** — msg `_scratch/2026-07-18_hivemind_v33-beat-2_commit-msg.txt`; expected porcelain: **exactly 5 entries = 2 M (pm-handoff · PROJECT_SNAPSHOT) + 3 ?? (the pre-verification · the M9.7-W2 instruction · the go-package)** → push. (2) **Launch the Coder** (Claude Code, in homesynapse-core): `Read nexsys-hivemind/context/instructions/2026-07-18_M9.7-W2_wave2-profiles-and-classification_coding-instruction.md and its pre-verification, then execute — /nexsys-coder`. (3) The Coder's completion report → back to the hub for the TWO-LAYER AUDIT; commit only on the hub's order; CI green on the push; deploy; the go-package unlocks on the hub's GO. (4) Your program: the 07-21 Pelton consult (lane-prepped) · tamoro (walk $3,400 stands) · veromo.com · LLC.

**Hub next (v33):** the M9.7-W2 two-layer audit on return → commit order → deploy word → **THE JOINS GO** → intake the night's pastes (D1 5/5 · B2 · E2 flips on the spine; constants re-mint; corpus entries) → CMD-API authoring vs the ~Jul-24 tripwire → G1 refresh + OUT + SKIP-VIS authoring → the ~Jul-28 M14 sweep.

## 2026-07-18 (v33 beat 1 — **LAUNCH (PASS all 11)** · baselines 5/5 · **the worktree-corruption repair** · the Gen4-Shelly intake · required reads done)

**Launch (remote §12):** Filesystem-MCP scope re-verified at first act — **FULL ClaudeFolder this session** (recovered from v32's core-only; the volatile-scope rule held). §11 clean — lock-free porcelain ×5: docs/hivemind/bench/skills CLEAN; core showed ONE M (adjudicated below). Both spine frontmatters read pre-write (last writer = v32 beat 3, committed — no ghost). Baselines verified at source: core `1aa809d` (HEAD; deployed) · docs `a53f474` · hivemind **`d059af6`** (the beat-3 close-out commit LANDED — Nick ran both ordered commits) · bench `1c705f6` · skills **`dccc85f`** (frontend-v1.4 LANDED). Watermark **AMD-99, 175/53** re-derived at the register regeneration line. Preflight **PASS all 11**; adjudications of record: **Check 2** — W29 (IN_PROGRESS) current through 07-19; W30 pre-minted (as the v33 prompt predicted). **Check 8** — ACTIVE: 0. **Check 9** — **Nick's mirror sync RAN** (the container-loaded mirror carries the v32-arc skill edits; 15/15 files size-exact source-vs-mirror on BOTH trees; content spot-confirmed on both SKILL.md mastheads) — scored PASS-per-proxy (the governing local-mirror compare is unreachable from remote; the v32-beat-1 proxy class). Check 5 — Open Risks §4b clear (no coder WU since REV2, which landed + field-proved). Check 11 spot-checks: the ten-value `command_result.outcome` vocabulary verified at `CommandResultEvent.java` ✓; the shipped zigbee-profiles.json shape verified ✓.

**THE LAUNCH ANOMALY — worktree corruption found, adjudicated byte-exact, repaired.** Core porcelain showed `M StandardAvailabilityTracker.java`; host mtime ≈1 h pre-launch; host size 10,183 B vs HEAD 10,179 B — a REAL host-side edit, not a mount phantom. Adjudication (the §12 route: Filesystem-MCP host read vs HEAD object): the ENTIRE diff is one 4-byte insertion — stray keystrokes `clea` typed before `log.info` at the transition-emit site, rendering `clealog.info(...)` — compile-breaking, unambiguously accidental (an editor window received keyboard input ~1 h before launch). PROOF: HEAD (md5 `95f44ea669dcdb236a72c0d5000ee00e`) + exactly that insertion reproduces the host md5 (`ab49d26deb6f08deea19f817b335e76a`) byte-for-byte. REPAIRED: the worktree file restored to exact HEAD content via the bridge commit path (SendUserFile → device_commit_files, mtime-guarded); host size re-verified 10,179 B. Core worktree clean again; NOTHING to commit on core. The Pi's deployed `1aa809d` was never affected. (If the edit was somehow intentional: it was not compile-valid, and the corrupt text is fully recoverable from this beat's evidence.)

**New-hardware intake (Nick, at launch): the Shelly order arrived GEN4.** 2× **Shelly Plug US Gen4 — `S4PL-00116US`** (Matter `0523-312-4791` · FCC `28DC6-PLUGUS-GEN4` · IC `33486-PLUGUSGEN4`) + 1× **Rosonway `RSH-A107C`** 7-port USB hub — unboxed, NOT installed/powered. The dossiers §6.5-h anticipated exactly this generation drift ("a fulfilled order could ship Gen4 … same RPC contract"). Consequences recorded: (1) the §5 driver contract HOLDS (Gen2+ RPC is generation-generic) but the §5.4 `ident` gate must expect **`S4PL-00116US`**, not the Gen2 `SNPL-00116US` — a driver-constants item for the B-lane WU that lands the `plug:` verb (constants.yaml carries expected model per plug; capture `gen`/`model`/`app`/`fw_id` verbatim at §5.3 step 1); (2) **the Gen4 carries Matter + Zigbee radios that STAY OFF/unprovisioned** — stimulus independence (the plugs ride the bench LAN only, NEVER the Zigbee network under test, never Matter-onboarded) is now an explicit provisioning rule, not just a default; (3) the third-plug rider STANDS — these two are B1/B2 **stimulus of record, NEVER M14 DUTs** (the printed Matter number changes nothing). Sequencing per the standing ledger: staged-unboxed only tonight; the Rosonway dongle-behind-hub topology move is post-joins, single-variable, with a boot-health re-run BEFORE it (then I3b [S] + the `usb-power` constants flip + the AUTO usb scenario); Shelly §5.3 provisioning (WiFi-side only) is out-of-band and never stacks into the joins night.

**Required reads done in full:** pm-handoff v32 beats 1–3 · the compounding-testing doctrine brief (the operating charter) · the wave2 operator return §§1–5 + the Phase-1 stub + carry-ins (the S31 powered-early + factory-fresh — it chases the FIRST open permit-join window; serials 02P `25471900074240` / 04P `25303900144196` bind to EUI-64s at `device_announce`; NO housing prints an EUI-64) · the dossiers return §§0–7 IN FULL (the four profile anchors; the §6.3-g 0xFC57-vs-0xFC37 capture-led conflict; the §6.1-a 0xC05E-vs-0x0104 CoB; the §5 Shelly contract + §6.5 unknowns) · the SHIPPED zigbee-profiles.json (schema 1.0 + provenance + the Hue/03P instances = the shape census) · integration-zigbee MODULE_CONTEXT targeted (DeviceProfile 10-component shape + the 6-nullable-collections gotcha; MatchCriteria SEALED-FOREVER — Wave-2 populates Fingerprint.matches() BEHAVIOR on the existing permit, today it throws UOE and registry arms treat it as no-match; StandardDeviceProfileRegistry total order [criteria tier → source rank → priority desc → profileId asc; fingerprint tier 0 reserved]; ZigbeeProfileLoader eager-index/lazy-body, unknown-major fail-closed, F-15/F-12 semantics; the M9.4-ADP `adopt_devices` accept-list mechanics — initialize-time read-once, 16-hex normalized, malformed-entry WARN-and-skip, conservative default is LAW) · the criteria RATIFICATION block (unchanged: 21 M / 8 S; J1 FROZEN; the rider-4 I3a adjudication recorded).

**The v32 prompt archived** (the R in this commit). **Fleet position at launch:** both cheap MUST-halves CLOSED (I3a frozen · F2 both directions measured) · certification ✓ · zero open [REVIEW]s · the runner FIELD-PROVEN · the fleet staged with every identity pre-verified · **the M14 sweep (~Jul-28) awaits ONLY the SEED leg (the joins night) + the G1 dispatch OUT.**

**Nick, in order:** (1) **The hivemind beat-1 commit** — msg `_scratch/2026-07-18_hivemind_v33-beat-1_commit-msg.txt`; expected porcelain: **exactly 4 entries = 2 M (pm-handoff · PROJECT_SNAPSHOT) + the v32-prompt archive D/?? pair (records as 1 R)** → push. Sweep-guard: safe IFF a fresh porcelain shows exactly those 4; the `_scratch` siblings never appear. (2) Nothing else is owed tonight — the profiles WU authoring is the hub's next act in THIS session; THE JOINS NIGHT rides its landing + deploy. (3) Your program: the 07-21 Pelton consult (lane-prepped; wake-ups set 07-21 + 07-31) · tamoro (walk $3,400 stands; offer $2,995 standing) · veromo.com $12 · LLC timing.

**Hub next (v33):** the profiles WU authored grounding-first (dossiers §§1–5 + the Phase-0 captures + the shipped shape census; ×4 profiles + ×4 accept-list entries; the 04P OnOff CLIENT cluster NEVER bound in V1 [the twin-path REC of record]; the three sensors `confirmation: []` [the 03P precedent]; the S31 = the wave's ONE confirmable — EXACT_MATCH on `OnOff/0x0000`, VERIFIED_REPORTS, ON_CHANGE, seed timeout 5000 ms; the 0xC05E CoB stays non-gating — exact_model carries) → full bar, tests per the profile-fixture idiom, CI green on the push, deploy re-verified AT the Pi → **THE JOINS GO** (SEED fresh formation = the SD-5/E2 discharge → Wave-1 rejoin → S31 FIRST → 02P → 04P → 01P; wake-press discipline; EUI-64s at `device_announce`; the hub's paste-blocks through Nick) → the D1 5/5 + B2 identity-stability reads → the constants re-mint → CMD-API (the ~Jul-24 tripwire, honest posture) → G1 refresh + OUT + SKIP-VIS authored → the ~Jul-28 M14 sweep on evidence.

## 2026-07-18 (v32 beat 3 — **CLOSE-OUT** · F2 CLOSED · the skills pass · the doctrine brief · the backlog mints · **v33 BANKED**)

**F2 [M] — CLOSED, both directions measured on the API + the log.** The retried clean cycle (the first attempt's lesson recorded: a ~60 s cut sits inside the ping interval — **the bench out-waits its instrument's resolution**, now a standing discipline): cut ≈21:44Z → honest AVAILABLE reads through the ping cadence → `availability_changed: available=false` **21:55:36Z** (≈11.5 min post-cut, ping-phase-dependent; Jul-14's reps measured 47 s and ~7 min — the window's resolution spans the ping interval by design) → the API artifact `"availability": "UNAVAILABLE", "stale": false` (21:59:39Z) → restore 22:02:00 local-act → `available=true` **22:02:00.701Z — 0.7 s, the warm-rejoin envelope** → the API AVAILABLE artifact (22:02:24Z). The frozen row's evidence set is complete: the Jul-14 both-transition log block + the multi-day rehydrated-offline persistence + today's API before/after pair + both log lines. The SNZB held AVAILABLE throughout (the control entity). J1 untouched — the row closes as frozen. **Both cheap MUST-halves are now DONE (I3a beat 2 · F2 this beat).**

**THE SKILLS CURRENCY PASS (charge 6 — executed).** Writable sources edited, all pointer-not-copy, zero volatile state: **coder/SKILL.md** — the v32-arc conventions line (fixture-paired asserts STANDING + mutation-verified fixture teeth · OR-group observation anchors [the REV2 D2 class] · the §3.9 silent-skip composition gotcha + history-seeded testing · deploy-state at the instrument + version banners · terminal-materialization + the type-filtered-checkpoint rule). **project-manager/SKILL.md** — the v32-arc disciplines line (the same standing rules at hub altitude + composed-behavior-under-state-history as a first-class defect class + out-wait-the-instrument + deploy hygiene + operator-choreography-vs-sensor-physics + **rename-readiness**: R-1 is Nick-gated at G-2, every work product stays token-parameterized). **nexsys-frontend/SKILL.md v1.4** (nexsys-skills repo) — **the §4a vocabulary CORRECTED to the live TEN values** (the five-value list was the pre-M9.4b subset; the 2026-07-06 lane prompt's flagged discrepancy resolved at its named carrier — this pass) + **new §4c THE EXPLANATION-SURFACE FIELD EVIDENCE** (the flattening's field reps · the null-name class · the silent-skip run class with the actionCount-vs-actions[] tell · availability's measured envelopes 0.7 s/~50 min · rename-readiness). **Check 9 goes STALE on both mirror trees pending Nick's sync** (expected; the launch-noted v29-line container drift squares in the same sync).

**The backlog v32 currency + row mints** landed as one block (SKIP-VIS · BENCH-PRECOND · BENCH-AUTOSTART · RUNNER-VERSION-BANNER · POWER-RESTORE · CMD-API · BTN-AMD · TAMORO/R-1 · the BENCH-AUTO arc note · the I3a/F2 flips). **THE COMPOUNDING-TESTING DOCTRINE BRIEF DELIVERED** — `context/process/2026-07-18_compounding-testing-doctrine.md`: Nick's charge (v31 beat 2, verbatim-anchored) discharged as the operating charter — verdicts-as-monotone-asset · the ratchet rule · instrument semantics (fixture pairs, evidence-quoting, self-identification, resolution-awareness) · the composition tier (history-seeded scenarios) · metrics-that-only-go-up · the Data Value Engine coupling (B4/B5 ride only this substrate inside AIOT-INV-1) · the executive read (the doctrine IS the trust brand's manufacturing process).

**v33 BANKED:** `context/handoff/2026-07-18_PM-mission-control_v33_orchestrator_session_prompt.md` — v32 retires on its launch. v33's charges: the profiles WU → **THE JOINS NIGHT** (the critical path; Sunday) → CMD-API (the honest ~Jul-24 tripwire) → the G1 dispatch refresh + OUT + SKIP-VIS authoring → the counsel pointers (consult 07-21 · TSDR #2 07-31) → the ~Jul-28 M14 sweep (ONLY SEED + G1 remain) → v34 banked before depth.

**Nick, in order:** (1) **the hivemind beat-3 commit** — msg `_scratch/2026-07-18_hivemind_v32-beat-3_commit-msg.txt`; expected porcelain: **exactly 7 entries = 5 M (pm-handoff · PROJECT_SNAPSHOT · coder/SKILL.md · project-manager/SKILL.md · the backlog) + 2 ?? (the doctrine brief · the v33 prompt)** → push. (2) **The nexsys-skills commit** — msg `_scratch/2026-07-18_skills_frontend-v14_commit-msg.txt`; expected porcelain: **exactly 1 M (orchestrators/nexsys-frontend/SKILL.md)** → push. (3) **Run your skills mirror sync** (Check 9 clears — both trees). (4) **LAUNCH v33 tomorrow** (fresh conversation; attach the v33 prompt; the read-and-follow line) — the joins night rides it. (5) Your program: the 07-21 Pelton consult (lane-prepped) · tamoro (walk $3,400 stands) · veromo.com · LLC.

**Hub next: v32 RETIRES on the v33 launch.** The conversation's yield stands recorded: two MUST-halves closed · the silent-skip class found, adjudicated, and rowed · the doctrine chartered · the skills current · the fleet staged for the joins.

## 2026-07-18 (v32 beat 2 — **I3a CLOSED AS FROZEN + the M14 RO condition** · the outage attribution · **THE SILENT-SKIP FINDING** · the stale-instrument reconciliation · F2's cycle queued)

**THE CLOSE.** `[PASS] usb-reenumeration-manual — 3/3 positive · 0 forbidden` (bundle `usb-reenumeration-manual-20260718T212347Z`, the REV2 instrument verified current on the Pi at `1c705f6`). Detection instant (`zigbee.transport_failed` 17:21:09.878); **autonomous reopen +17 s** (17:21:26.631 — the SIXTH consecutive silicon rep inside the 16–17 s envelope; the reopen fix now has a reproducible signature). The liveness third under the ruled (A) semantics: new run `01KXVHS8365P4EM9Q7WPKC5GWH`, `triggeredAt` 2026-07-18T21:22:38.462Z ≥ M_observed 21:21:26.819Z (anchor `zigbee.reopened`), **chain outcomes QUOTED `['UNCONFIRMED','UNCONFIRMED','FAILED','UNCONFIRMED','DISPATCHED']`** — five real dispatches through the reopened transport, the honest steady-state vector, zero false CONFIRMs; the `FAILED` = the superseded→FAILED flattening rendering itself into a bench evidence line (a same-day field rep for G1). Rider 4's frozen adjudication satisfied verbatim (a green OPERATOR rep, direct-attached). **I3a [M] CLOSED · the M14 RO condition CLOSED.** The Coder's B1-REV1 cross-agent note restamped Archived-SERVED this beat (ACTIVE: 0).

**The outage attribution (CLOSED, operator-confirmed).** Power outage early AM 2026-07-16 (Nick-confirmed; Pi boot 07:21:05; `throttled=0x0`; no `hs_err`; no SIGTERM shutdown shape). The app is EXONERATED — it ran healthy ~30 h and was alive executing runs at 00:27 Jul-16 (run `01KXMPAQ…` on the events surface) while its final log lines died unflushed in the page cache at the cut: **the events-vs-logs durability split proven by accident** (fsync'd events queryable days later; best-effort logs lost their tail — by design). The ~2.3-day evidence outage = the M13 no-autostart gap, field-priced → **BENCH-AUTOSTART row minted (rule at B3 authoring, where a self-starting bench becomes load-bearing for nightly N-accumulation).** The first two rep attempts ran a STALE instrument — the Pi had never pulled REV2 (the v31 beat-3 pull step unexecuted; the hub compounded it by asserting the pull had run — owned). Durable rule minted: **deploy-state is re-derived at the instrument, never assumed from an ordered sequence**; RUNNER-VERSION-BANNER candidate banked (bench.sh prints its repo HEAD; B2). The stale reps still banked detection+reopen ×2 and reproduced the rep-3 false-FAIL class live — the (A) ruling field-confirmed a third time.

**THE SILENT-SKIP FINDING (the day's first-class yield).** The REV2 assert refused a COMPLETED run with an empty causal chain → adjudicated at source + field, airtight: `StandardActionExecutor.issueCommands` applies the Doc 07 §3.9 `UnavailablePolicy` per target (bench-hero config-verified: five direct `entity_ref` CommandActions on the Hue, no `on_unavailable` key → default SKIP); the Hue's availability **REHYDRATED-UNAVAILABLE from the 2026-07-14 20:41:54 cut event by log replay** (no fresh declaration on any later boot — none needed; hence zero log evidence); every bench-hero run from 20:41 Jul-14 through 18:50 Jul-18 lawfully SKIPPED all five commands and completed `"success"` — referee: causal-chain `actionCount=9 / commandCount=0` on `01KXV82W…` (today) AND `01KXMPAQ…` (00:27 Jul-16 — also dating the outage onset ≈00:28); the known-good Jul-13 chain `01KXF5Z29V…` renders perfectly (read path healthy). The Hue returned ORGANICALLY at 18:50:20Z (~50 min powered-into-dead-air rejoin backoff — corpus-worthy: device recovery timing depends on power-up ORDER, network-first vs device-first). **Verdict: every component behaved correctly per its ruled design; the COMPOSITION was invisible** — no log token, no `skipped` marker (per-target skip ≠ action skip), an empty chain, and (source-verified) the non-firing `completedVerdict` renders such a run "fired and confirmed." NEW ROWS: **SKIP-VIS** (core WU candidate — per-target skip evidence: an INFO token + an action-level skip surface + causal-chain rendering + the `completedVerdict` honesty fix; routes with EXPLAIN-PUSH/G1) · **BENCH-PRECOND** (liveness-class scenarios gain a target-availability precondition so an offline target reads as itself, not as a window FAIL) · **the doctrine-brief EXHIBIT** (the fixture-paired assert caught the invisible-lawful-behavior class on its first live rep and forced the diagnosis). Also banked: run-lifecycle events publish at terminal (`triggered.eventTime == completed.eventTime` — the terminal-materialization mechanics confirmed at the event layer) · prior-instance runs render `automationName`/`trigger.type` null (G1) · ash CRC ×1 absorbed (line-noise class, corpus note) · `TC_REJECTED_APP_KEY_REQUEST` continuing (the named watch, consistent — and NOT tripped at either boot-health PASS today).

**F2 [M] — the cycle queued.** The composed evidence is banked (the Jul-14 log block: cut→offline ≤47 s [20:16:20] · restore→online + the relink/reporting-reverify cascade [20:31:42] · second cut→offline [20:41:54]; the multi-day rehydrated-offline persistence across boots = never-false-ALIVE at its strongest; both-AVAILABLE API reads). The clean 12-minute cycle (available → cut → API-offline read → restore → API-available read) is Nick's queued act — **F2 closes on his paste**; REC recorded: run it, the row becomes unarguable.

**Nick, in order:** (1) the F2 cycle → ⏺ paste. (2) **The beat-2 commit** — msg `_scratch/2026-07-18_hivemind_v32-beat-2_commit-msg.txt`; expected porcelain: **exactly 3 M (pm-handoff · PROJECT_SNAPSHOT · cross-agent-notes)** → push. (3) Launch the Coder on the Wave-2 profiles WU when the hub hands it issue-ready. (4) Your program: the 07-21 Pelton consult (lane-prepped) · tamoro ($3,400 walk stands) · veromo.com · LLC.

**Hub next (v32):** the profiles WU authoring (grounding-first: dossiers §§1–5 + the shipped profile-shape census) → the joins-night paste-block package (Sunday: SEED formation = the SD-5/E2 discharge → Wave-1 rejoin → S31 → 02P → 04P → 01P → per-device verify → D1 5/5 + B2 reads → constants re-mint) → CMD-API authoring (Monday; the ~Jul-24 tripwire, honest posture) → the G1 dispatch refresh + OUT (now carrying the flattening ×2, the null-name class, AND the silent-skip exhibit) + the SKIP-VIS WU authoring → the skills pass + backlog folds + the doctrine brief → **the ~Jul-28 M14 sweep** (position after this weekend: certification ✓ · RO ✓ · F2 [tonight] · SEED [Sunday] · G1 [Monday] · zero open [REVIEW]s).

## 2026-07-18 (v32 beat 1 — **LAUNCH (PASS all 11)** · baselines verified 5/5 · the engine.py residual DISCHARGED · W30 pre-minted · the I3a + F2 chases OPEN)

**Launch (remote §12):** Filesystem-MCP scope re-verified CORE-ONLY at first act (`list_allowed_directories` — matches the v32 prompt's warning; every hivemind/bench read/write this arc rides the bridge stage/commit path). §11 clean — lock-free porcelain ×5 ALL CLEAN; both spine frontmatters read pre-write (last writer = v31 beat 3, committed — no ghost). Baselines verified at source: core `1aa809d` (HEAD, deployed) · docs `a53f474` · hivemind `c43587e` (the beat-3 commit LANDED exactly as ordered — 18 files = 4 M + 14 new incl. the counsel-package tree; parent `9f7e493`) · bench `1c705f6` (B1-REV2 LANDED — 15 entries = 5 M + 10 new, census exact vs the staged message; parent `b568024`) · skills `ee0839f`. Watermark **AMD-99, 175/53** re-derived at the register regeneration line (§0.3/§17). Preflight **PASS all 11**, two adjudications of record: **Check 2** — W29 (IN_PROGRESS) is current through 2026-07-19; the W30 plan is PRE-MINTED this beat (`context/planning/weeks/2026-W30_jul20-jul26.md`) so the Monday boundary never presents stale. **Check 9** — the governing comparison (source vs Nick's LOCAL mirror) is unreachable this session (no `.claude` mount in the VM); the proxy ran instead: source vs the container-loaded mirror = 14/15 md5-≡, the ONE differ being EXACTLY the known v29 masthead line (source-side, dated 2026-07-11; mtime confirms zero source edits since; v31 beat 1 verified Nick's local mirror 15/15 ≡ on 07-14, AFTER that edit landed) — scored **PASS-per-prior-verification**; a one-glance re-confirm is offered to Nick; the row goes honestly STALE at the charge-6 skills pass regardless. Cross-agent ACTIVE: 1 — the Coder's B1-REV1 note (EXPECTED by design; restamps SERVED at the I3a-rep close-out). Open Risks §4b CLEAR (no open deferred gates). The v31 prompt archived (the R in this commit).

**The engine.py launch residual — DISCHARGED (the REV2-1 line-read):** `eval_new_run_after` + its lint arm verified point-for-point against the REV2-1 spec: the anchor is lint-REFUSED unless a PRECEDING plain `log:` positive (log_any members deliberately excluded — the D2/DEFECT fix in force at both ends); M_observed reads from `satisfied_at_utc` (actually-matched tokens only); eval REFUSES when the anchor never matched (never vacuous); the runId snapshot binds to the FIRST act's marker, first-ATTEMPT-wins, never re-stamped (a failed first read stays None and reports honestly); PASS = not-in-snapshot AND `triggeredAt` ≥ M_observed (`parse_iso_utc` on the API timestamp, never log-time parsing; unparseable ⇒ ignored/under-count — the documented conservative bound) AND causal chain ≥ 1 executed action with the observed outcomes QUOTED in the evidence line; the anti-false-PASS arm ignores predates-M_observed rows with quoted per-run evidence (the rep-2/pre-pull classes named in-code). `new_confirmed_run` retained. ONE conservative addition beyond the spec letter, consistent with the D1–D4 ratifications: a v0 lint refusal when both runs-asserts share one scenario (their snapshot semantics differ; B2 defines the mix — already carried on the Coder's own NEXT-WU candidate list). No defect found — B1-REV2's audit basis is complete at line level.

**Required reads done in full:** v31 beats 1–3 · the bench acceptance record close-out (2026-07-13/14) · B1-REV2 + the Coder's REV2 coder-handoff entry · the wave2 operator return §§1–5 (Phase-0 captures, the S31-powered-early carry-in, the joins stub + CoB ledger) · the counsel-navigator return + ADDENDA 1–3 (the TSDR RECLASSIFICATION arc, the brokerage state, the 07-21 consult booking) · the criteria RATIFICATION block (21 M / 8 S; J1 FROZEN; the rider-4 I3a adjudication verbatim) · the (a) dossiers return §0 + the per-device profile anchors (the FULL §§1–5 re-read rides the profiles-WU authoring — grounding-before-authoring).

**Chases OPEN (charge 1 — nothing else outranks):** (a) **the I3a operator rep** — post-REV2-commit + Pi `git pull`; ONE `usb-reenumeration-manual` run (the practiced craft: pull/re-seat, hands off; at act 2 let the ~59 s occupancy hold clear, ENTER + one wave — the operator's own post-reseat movement now lawfully counts); `[PASS]` closes **I3a AS FROZEN (rider 4) + the M14 RO condition**; a FAIL pastes for adjudication, never retried blind. (b) **the F2 offline leg** — the oldest open [M]-half (owed since 07-14): lamp off at the wall → entities reads every ~3 min to the honest offline transition (the ~10 min mains-ping window) → lamp on → AVAILABLE on evidence; ⏺ both deltas. Both asked of Nick at launch.

**Nick, in order:** (1) the I3a rep paste (either way). (2) The F2 offline paste (both deltas). (3) **The hivemind beat-1 commit** — msg `_scratch/2026-07-18_hivemind_v32-beat-1_commit-msg.txt`; expected porcelain: **exactly 5 entries = 2 M (pm-handoff · PROJECT_SNAPSHOT) + 1 ?? (the W30 plan) + the v31-prompt archive D/?? pair (records as 1 R)** → push. (4) Your program: the tamoro.com negotiation (walk $3,400 stands) · veromo.com $12 (still NXDOMAIN) · LLC timing · the 07-21 Pelton consult (the counsel lane preps you; wake-ups already set 07-21 + 07-31).

**Hub next (v32):** the Wave-2 profiles WU authoring (charge 2 — from the audited (a) dossiers + the Phase-0 captures; full bar; then **THE JOINS GO** relayed through Nick WITH the hub's paste-blocks: SEED fresh formation = the SD-5/E2 discharge → Wave-1 rejoin → S31 first [powered + factory-fresh — it chases the FIRST open window] → 02P → 04P → 01P → per-device verify → the D1 5/5 + B2 identity-stability reads → the constants re-mint) → CMD-API authoring vs the honest ~Jul-24 tripwire (N≥200/N≥500 already numerically exceeded by the certified organic evidence — the flag states this) → the explainability dispatch refresh (G1) → the counsel folds (the §1a supersession + TSDR reclassification are already spine-recorded at v31 beat 3; future gates consumed as §6-pointer beats) → the skills pass + backlog folds + the compounding-testing doctrine brief (charge 6) → the ~Jul-28 M14 sweep on evidence → v33 banked before depth.

**Rotated 2026-07-19 (v35 hub, beat 1 — THE ROTATION, staged-copy-sourced + mtime-guarded bridge writes; assert-all-before-write):** the masthead Prior-chain (§A) + beat sections v31-beat-3 → v22-beat-1 (43 sections, §B) -> `archive/pm-handoff-beats-rotated-2026-07-19.md`. Kept: v34 beats 1–2 + v33 beats 1–5 + v32 beats 1–3 + Open Risks (untouched). Full history in git.

**Rotated 2026-07-07 (v23 hub, beat 7 — the close-hygiene rotation; git-object-sourced from hivemind HEAD `91f8cda`):** beat sections v21-beat-3 → v17-beat-1 (v21 ×3 · v20 ×3 · v19 ×1 · v18 ×6 · v17 ×6) → `archive/pm-handoff-beats-rotated-2026-07-07.md`. Kept: v23 beats 1–7 + v22 beats 1–5 + Open Risks. Full history in git.

**Rotated 2026-07-04 (v19 hub, the currency pass §2A; git-object-sourced from hivemind HEAD `68ccbf6`):** beat sections v16-beat-65 -> v13-beat-44 (22 sections: v16 61-65 · v15 57-60 · v14 48-56 · v13 44-47) -> `archive/pm-handoff-beats-rotated-2026-07-04.md`. Kept: v18 beats 1-6 + v17 beats 1-6 + Open Risks. Full history in git.

**Rotated 2026-07-02 (v14 hub first-act — the deferred beat-block rotation, phantom-safe file-tools-only method):** beat sections 5→43 (33 sections; beat 18 was never written to this file) -> `archive/pm-handoff-beats-rotated-2026-07-02.md`. Kept: beats 44–47 + Open Risks (reconciled). Full history in git.

**Rotated 2026-06-22 (v5-hub first-act hygiene; git-object-sourced from HEAD `04c0a54`):** the stale HTML-masthead reconciliation prose (2026-06-13 / -14 / -18 / -18 / -19) + the beats older than beat-5 (beat-4 v4-launch, beat-3 v4-handoff, beat-2 explainability, plus the v3-launch / M7.2a-1-landing / v2-parallel-lanes / Doc-16-Lock / AB-1+AB-2 entries) -> `archive/pm-handoff-rotated-2026-06-22.md`. Kept: beat-6 + beat-5 + Open Risks. Full history in git.

**Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; git-object-sourced from `58794d7`):** the ratification record (Return ASSESSED → RATIFIED → mechanics), the superseded fold-abort record, the M7-AMD-block record, the M6.4-WUCP record + the M6.2-era fragment, and the RESOLVED/CLOSED build-gate blocks from Open Risks → `archive/pm-handoff-2026-06.md`. Earlier rotations per the markers preserved there. Full history in git.

## Open Risks

#### OR-GATE-M9.4-NCFG — ci.yml RED on pushed `ccba69d` (`ReplayTransitionIT` timeout) — [✅ RESOLVED 2026-07-06, v22 beat 5 — rerun GREEN; closed as INTERMITTENT-definitive] (NEW 2026-07-06, v22 beat 4+)
- **RESOLVED (beat 5):** Nick re-ran the failed jobs on `ccba69d` → GREEN. Closed as intermittent with Nick's reinforcing causality on the record: *NCFG touched integration-zigbee only and cannot causally reach event-bus replay — runner variance, not a regression; "intermittent" is definitive, not probable.* §4b UNBLOCKED; the M9.4-ADP dispatch hold lifted. **The deflake row stays QUEUED regardless (Nick: "latent moat-debt"):** `awaitCheckpoint` must report checkpoint PROGRESS and fail on STALL, not wall-clock — rides the next event-bus-touching WU (its own micro-WU on recurrence).
- **Milestone:** M9.4-NCFG (the gate of record on its push). **Commit:** `ccba69d`. **Failure:** `:core:event-bus:test` → `ReplayTransitionIT` "resumes from persisted checkpoint and catches up to LIVE without gaps under continuous publish" — `AssertionError` at `:212` = the `awaitCheckpoint` 30 s timeout (called `:167`); 221 tests, 1 failed.
- **Adjudication frame (hub, at intake):** event-bus is UNTOUCHED for 65 commits (last touch `a441fdf`, M4.0a); CI ran green on identical event-bus bytes many times incl. `e363320` same-day; Nick's local full `check` GREEN ×2 today on trees carrying this exact code; the failing await is wall-clock-bounded real-thread concurrency (1500-event continuous publish) — the slow-runner flake profile. NOT waved off: the same signature would present for a REAL subscriber-stall race, and the error omits the last-seen checkpoint — undecidable from the log alone.
- **Owner:** Nick (re-run) / hub (adjudication + the deflake row). **Commands (Nick):** GitHub → the failed run on `ccba69d` → **Re-run failed jobs**. Optional local hunt: `./gradlew :core:event-bus:test --tests "*ReplayTransitionIT*"` repeated ×5.
- **Resolution condition:** rerun GREEN ⇒ gate CLEARS as intermittent (this OR closes; the **deflake/instrumentation row is QUEUED regardless** — `awaitCheckpoint` must report the last-seen checkpoint and fail on STALL rather than pure wall-clock; rides the next event-bus-touching WU, its own micro-WU if it recurs). Rerun RED ⇒ escalates to a REAL investigation WU (a reproducible-on-runner liveness failure in the delivery path outranks the bench — never-false-* extends to "without gaps").
- **§4b consequence: the M9.4-ADP dispatch HOLDS until this gate clears** (the ruling can land meanwhile; "go" fires on the green).

#### OR-GATE-M7.5c-a — deferred build gate on the M7.5c-a working tree — [✅ RESOLVED 2026-07-02, beat 50] (NEW 2026-07-02, beat 49)
- **RESOLVED (beat 50):** round 2 GREEN — 53 targeted + 149 full-`check` tasks on Nick's terminal → core committed **`e3d7296`** (10 paths exact) → pushed → **ci.yml ✅ + install-smoke ✅** on the push. M9.1 routing UNBLOCKED (§4b clear). Total: ONE fix round (2 test-compile errors, hub-fixed — within the P5 one-round target for compile-class defects).
- **Milestone:** M7.5c-a (internal-read envelope conformance). **Commit:** pending — working tree on core `2786526`; change set = exactly 10 files (7 source/test + 3 MODULE_CONTEXT).
- **Commands (Nick):** `./gradlew :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` → `./gradlew :api:rest-api:test :lifecycle:lifecycle:test` → `./gradlew check` (count pins stay **71/41/53**; both module-infos + every `build.gradle.kts` unchanged) → `./gradlew :testing:integration-tests:check` (watch `EndpointE2eIT` — DLQ paths moved under `$.data`). `-Werror` rides the convention plugin — do NOT pass it as a CLI flag.
- **Round 1 (2026-07-02):** targeted `compileJava` GREEN + `:testing:integration-tests:check` GREEN + host porcelain exactly 10 ✓; `compileTestJava` FAILED — 2 AssertJ varargs-capture errors (`DlqStatusEndpointTest` L75/L135: `containsExactly` on a `List<?>` actual). **Hub-applied 2-line fix** (SendMessage-to-lane unavailable; mechanical class): the `@SuppressWarnings("unchecked")` typed-local idiom per conforming `ListEntitiesEndpointTest` L83–88. Lesson appended to coder-lessons. **Re-run pending:** `:api:rest-api:test :lifecycle:lifecycle:test` → `check`.
- **Owner:** Nick. **Deferred Build Gate:** YES — the lane's sandbox attempt failed at `build-logic` (mount delete + VM JDK 11 vs 21); evidenced in coder-handoff, never silently skipped.
- **Resolution condition:** `check` green → core commit (staged message, exactly 10 paths) → push → `ci.yml` green on the pushed commit (the gate of record). On resolution: M9.1 routes to the next Coder lane; FE-1b launches on the landing.

#### OR-M7-WIRING — Command-pipeline live wiring — [✅ RESOLVED 2026-06-27 — live-wired at M7.4b/c] (formalized 2026-06-25; NEW in beat 7, 2026-06-22)
- **RESOLVED 2026-06-27 (M7.4b producer + M7.4c confirmation/ledger; v8 beats 17/19; reconciled by the v9 hub).** The command pipeline is LIVE on the real composition root: trigger → run → `command_issued` → co-located dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`, with the live `pending_command_ledger` + `pollExpirations()` tick and paired teardown. The resolution condition's **E2E composition-root confirm gate is met** — `RunPipelineConfirmWiringTest` (host-verified present in `lifecycle/lifecycle/src/test`).
- **Residual → M7.4d (the only remaining M7.4-arc item):** the resolution condition's *second* clause — the **D2 pure-function-replay CI test wired as a gate** (+ registering D2's canonical INV-id) — is NOT yet landed. The *wiring* is resolved; the *replay-safety CI gate* is the M7.4d hardening.
- **Severity:** RESOLVED (was MEDIUM). **Owner:** PM/Coder — done. _See OR-GATE-M7.4 below for the gate-of-record confirm on `db8ab5f`._

#### OR-GATE-M7.4 — gate-of-record (CI) on the pushed `4e31aed` (M7.4d HEAD) — [✅ RESOLVED 2026-07-01 — ci.yml VERIFIED GREEN on core 52824e9 (beat 43)] (NEW 2026-06-27, v9 hub; updated v10 beat-25)
- **RESOLVED 2026-07-01 (v13 hub, beat 43; reconciled into this ledger by the v14 hub):** `ci.yml` GREEN on pushed core `52824e9` — screenshot-verified (Build & Check 3m5s, Gradle 8.8, `check` ✅). Subsumes the per-commit CI-confirm for the whole ancestor chain (`4e31aed`/`9ec5949`/`eed477e`/AB-4/M7.5a/b). The `frontend.yml` first-trigger glance on `52824e9`+`2786526` is a standing Nick item (hub tracks), not an OR.
- **Status:** NARROWED. The **local `./gradlew check` is CONFIRMED GREEN** on the M7.4c working tree that became `db8ab5f` (Nick's terminal 2026-06-27: BUILD SUCCESSFUL, 149 tasks; he then committed + pushed core `69cc6e2..db8ab5f`). So the M7.4b/c/**d** deferred build gates are all **resolved locally** (Nick's terminals: `./gradlew check` 149 tasks green on the M7.4c tree → `db8ab5f`, and again on the M7.4d tree). The only residual is the **gate-of-record** formality: the `ci.yml` GitHub Actions run green on the pushed HEAD **`4e31aed`** (the M7.4d commit has now landed; `db8ab5f`→`4e31aed`). **De-risked (v10 beat-25):** `git ls-tree 4e31aed gradlew` = `100755` (executable) → the prior exit-126 failure mode is structurally cleared, and `ci.yml` runs `./gradlew check --no-daemon`; what remains is purely Nick confirming the Actions run is green on `4e31aed` — the standing b85e1ed/M7.4a pattern, "confirm CI green on the pushed commit, not just locally."
- **Severity:** LOW (the substantive gate — the full local `check` — is green; this is the CI-record formality).
- **Owner:** Nick (glance at the Actions tab for `db8ab5f`). **Resolution condition:** Nick confirms the `ci.yml` run is green on `db8ab5f` → record M7.4b/c CI-gated → close. M7.4d may be authored/routed now (the local gate clears §4b — done, ISSUE-READY beat 22).

#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
