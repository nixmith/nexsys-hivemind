<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-06 (v21 hub, beat 1 — SD-5 ROUTED (a) under Nick's delegated authority (HARD GATE recorded: no non-bench network until the seed-custody WU lands); explainability-UX lane CHARTERED; naming artifacts → context/strategy/; W28 IN_PROGRESS; v22 AUTHORED. Fleet: core 959f925 / docs b65ed7d / hivemind a28441c / bench 5ceff3b / skills 0c00ece. Watermark AMD-98, 174/52.) Prior: 2026-07-04 (v20 hub, beat 3 — THE CLOSE: M9.4b LANDED core 959f925 (64 exact) + CI GREEN both workflows (gate of record CLEARED); hivemind 8c0ba20→9bdf7a0 (7 exact); M9.4 CODE-COMPLETE — remaining = bench (STAGED, beat-2) + soak; SD-5 ruling OPEN (rec (a)); v21 AUTHORED.) Prior stamp chain preserved in archive/pm-handoff-beats-rotated-2026-07-04.md frontmatter.
-->

# PM Session Handoff

## 2026-07-06 (v21 beat 2 — beat-1 LANDED `66a026f` (10 exact, arithmetic reconciles file-for-file) · **NICK'S BENCH-BLOCKER CATCH CONFIRMED AT SOURCE: permit-join has ZERO production callers** — the v20-staged checklist's step 2 was unexecutable as-shipped · **M9.4-PJ micro-WU AUTHORED ISSUE-READY** (`context/instructions/2026-07-06_M9.4-PJ_permit-join-config-wiring_coding-instruction.md`) · **the executable bench runbook STAGED** (`nexsys-bench/docs/2026-07-06_m9.4-bench-acceptance-runbook.md`) — Phases 0–2 runnable NOW, Phase 3 gated on PJ landing)

**The adjudication (two survey agents + hub layer-2 on every load-bearing claim — the ABSENT-claims rule applied, and this one VERIFIED):** `CoordinatorProtocol.permitJoin(int)` (:55) is implemented (`EzspCoordinatorProtocol:600-608`, frame 0x0022) but has NO main-source caller (hub regrep: interface + impl + javadoc only); no trust-center policy opens joins at formation; the schema's `permit_join_duration` (shipped M9.3, default 120) is read by NOTHING; `ZigbeeIntegrationAdapter.isPermitJoinActive()` is a `return false` stub whose own comment defers the surface to a post-M9.4a REST path never built. **Consequence: devices cannot join the bench network on `959f925`. Hub miss owned:** the v20 bench-checklist staging never source-verified an open-join invocation existed — checklist steps that assume an invocation need pin-grade grounding exactly like WU Files-table rows (lesson for the next pm-lessons fold). Also confirmed for the record: **the bench network IS the sanctioned SD-5 exception** (the gate reads NON-bench; this network is the election's purpose); the fallback stays a joint ruling.

**M9.4-PJ (the fix, deliberately micro):** wire the already-schema'd `integrations.zigbee.permit_join_duration` → after `resumeOrForm()+awaitNetworkUp()` in the PRODUCTION `run()` path only (:195-199 seam), key present ⇒ ONE `permitJoin(clamp 1..254)` + `zigbee.permit_join_opened` INFO + a clock-based window behind `isPermitJoinActive()` (never-false-ALIVE: the window state must be TRUE); **key ABSENT ⇒ never opens — the conservative default is law** (the schema's default:120 is documentation-side); driven/test mode untouched; REST remains the future UI surface. Shape: 1 main file + tests, zero module-info/deps/mint/public-surface. Grounded against WU-M9.4 §G pins P43/P53/P55 + fresh hub reads at `959f925`; STOP gates G-PJ1–4 pinned. Dispatch on Nick's word (CC lane, proven allow-list; targeted zigbee tests in-session; full `check` + CI = gates of record).

**The runbook (bench repo — every command source-grounded, file:line inline):** P0 free-the-instrument (ZHA leave BOTH devices while HA runs, THEN stop HA + autostart; port-free check; dialout) → P1 provision (Pi `installDist` — jars + Pi's own JDK 21, no cross-arch jlink; `HOMESYNAPSE_HOME=~/hs-bench`; `homesynapse.yaml` + `integrations/zigbee.yaml` with `serial_port: /dev/zigbee` + the PJ key marked PAIRING-ONLY) → P2 FORMATION leg (verify: `EZSP session negotiated: protocolVersion=13` · `zigbee.network_formed` · **`zigbee.network_up` = the 0x0019/0x90 bench-verify, formation arm** · custody files walk) → P3 [PJ-GATED] pairing (bulb 6×-cycle + sensor 5 s hold per corpus notes; adoption logs; **Configure-Reporting explicit** = the bind_failed/ack_lies/ias_enroll WARN sweep, absence + flowing reports = parity; entity ULIDs via the bearer-auth REST) → P4 bench-hero automation (source-exact YAML: `state_change`/`occupied` trigger → turn_on · set_brightness 50 · the 4550→4525 K supersession pair (25 K < ±50 K tolerance — the §E direction-2 probe) · identify 5 s; ISO-8601 delays) + restart = **RESUME leg (0x0019/0x90 second arm)** → P5 acceptance runs (ONE wave drives all legs; state_confirmed is event-only — the sqlite `events` type+ingest_time query is the confirm+latency read; `brightness_percent=50` at query; superseded verdict-free; identify = immediate honest `unconfirmed`, zero state_confirmed; IAS ×2 twins watch; optional bulb-off timeout-honesty probe) → P6 REOPEN leg (pull/re-seat → `zigbee.reopened`, resume-never-reform) + **remove the PJ key** (the exception window closes) → soak entry. STOP conditions = joint-ruling only.

**Nick, in order:** (1) **DISPATCH M9.4-PJ** — Claude Code in `homesynapse-core`, paste: *"Read nexsys-hivemind/context/instructions/2026-07-06_M9.4-PJ_permit-join-config-wiring_coding-instruction.md and nexsys-hivemind/context/pre-verifications/WU-M9.4.md section G (pins P43/P53/P55), run the STOP gates, then execute. Tests before implementation. /nexsys-coder"* (2) Meanwhile: **runbook Phases 0–2 run TODAY on `959f925`** if you want the evening — formation/custody legs need no joins; on the PJ landing, `git pull` + re-run `installDist` on the Pi, restart (that restart double-serves as an extra resume rep), then Phase 3+. Or wait and run it all on the landed build — one baseline, simpler record; your pacing. (3) **hivemind beat-2 commit — stages exactly 3** (2 M: pm-handoff · PROJECT_SNAPSHOT; 1 new: the PJ instruction), msg `_scratch/2026-07-06_hivemind_v21-beat-2_commit-msg.txt` → push. (4) **bench commit — stages exactly 1** (the runbook), msg `_scratch/2026-07-06_bench_runbook_commit-msg.txt` → push. (5) Standing: Dependabot day 9+ · deletables · the ledger.

**Hub next:** PJ return two-layer audit vs the DIFF → bench-outcome intake against the runbook's ⏺ RECORD points → the fallback ruled jointly if invoked → soak entry → seed-custody WU JIT (the SD-5 gate rides) → explainability-lane return audit → v22 at retire.

## 2026-07-06 (v21 beat 1 — LAUNCH FULL-PASS (all 11; Check 2 reconciled in-launch: W28 PLANNED→IN_PROGRESS per its own flip note; Check 9 byte-identical ×3, exactly as the v21 charter predicted) · **SD-5 ROUTED (a) — RULED BY THE HUB UNDER NICK'S EXPLICIT DELEGATION** · the explainability-UX lane CHARTERED on source-verified live vocabulary · the two Jul-5 naming artifacts CAPTURED to `context/strategy/` · v22 AUTHORED — `context/handoff/2026-07-06_PM-mission-control_v22_orchestrator_session_prompt.md`)

**Launch + grounding (source outranked the prompt where they met):** restart-resume clean (lock-free porcelain ×5; both spine frontmatters read BEFORE any spine write). Fleet re-derived: core `959f925` · docs `b65ed7d` (**AMD-98, 174/52** re-derived from the register regeneration line) · hivemind **`a28441c`** (the v20 beat-3 commit VERIFIED LANDED — the prompt's "verify landed" resolves ✓) · bench `5ceff3b` · skills `0c00ece`; mirrors ×3 byte-identical; zero live index locks; §4b CLEAR (zero open deferred gates). v20 prompt → `archive/`. **Source-vs-spine deltas found at grounding:** (1) the frontend skill §4a vocabulary is a STALE 5-value subset — source carries 10 (`CommandResultEvent.java:22-27`, the M9.4b §4.3 currency); carried as a lane-brief flag; the skill fix rides the NEXT skills pass (never mid-arc). (2) NEW state postdating the v21 prompt: two 2026-07-05 naming/brand artifacts (Asimtote parent — domains OWNED; consumer product-name shortlist) found UNVERSIONED at the ClaudeFolder root → captured to `context/strategy/2026-07-05_{naming-and-domain-strategy,product-name-candidates}.md` beside their `asymptote-name-recommendation.md` predecessor (mv only, reversible; veto standing). Naming is Nick's open strategy thread — no project-surface renames follow from it today (W-11 name-light already prices a rename at 2 tokens in 1 file).

**SD-5 — ROUTED (a) (the hub's ruling, on Nick's delegation — his words verbatim: *"I want you to think carefully and strategically about this then decide/execute for yourself how we route it."*):** the bench proceeds AS-SHIPPED (0x1B84 + the well-known ZigBeeAlliance09 root); the generated-seed custody WU (§5.5 `PersistentNetworkParameterStore` custody + a `CoordinatorOps` seam widening) authors JIT POST-BENCH — grounding-subagent first, the M9.4b instruction + WU-M9.4 §F–§G as the shape templates. **Rationale on the record:** (1) the election's own text (v18 beat-2 verbatim) is evidence-first / one-variable / "characterized before any user network exists" — the bench exists to characterize the SHIPPED shape; (2) option (b) INVERTS evidence-first — a pre-bench seed WU authors against a shape the bench's fallback clause may yet revert, and stacks governance latency in front of the critical-path head (the W28 risk register's named hazard); (3) option (c) would overturn the RULED election at the routing layer — the fallback clause is the bench's to invoke, not the router's; (4) the desk-bench exposure window is negligible (2 devices; the industry-default well-known root) AND the boundary is now STRUCTURAL: **HARD GATE — no non-bench network forms until the seed-custody WU lands** (recorded here + the backlog M9.4 row + v22 §2, so it survives hub rotation). The ELECTION itself (hashed, v18 beat-2) stands untouched — this ruling routes only its consequence. Nick reviews outcomes.

**Chartered — the explainability-UX lane** (`context/handoff/2026-07-06_explainability-ux-lane_session_prompt.md`; fresh Cowork; nexsys-frontend v1.2; write-isolated to `web-ui/dashboard/**` + its return file; launchable NOW, zero bench dependency): consumes the LIVE vocabulary quote-exact with file:line — the 10-value `command_result.outcome` (source-quoted verbatim in the brief; the last four are ledger-guard-skipped DISPOSITIONS), `StateConfirmedEvent`'s six components, `command_confirmation_timed_out` (EventTypes:65), `brightness_percent` derived-at-query (`MaterializedStateQueryService:78/:113/:209` — a SHAPE-compatible NEW data key inside the FROZEN v1.1 attributes map; flag-don't-improvise stands), the NAMED target `StandardExplanationService` flattening (:56 vocabulary note; :251-265 ACTED_BUT_UNCONFIRMED collapse), and Nick's beat-5 scope ruling VERBATIM as the charter boundary. The skill-§4a staleness is flagged IN-BRIEF (source outranks; pointer-not-copy working as designed). **W2-VAL synthetic-legs charter deliberately NOT authored this beat** — it rides the soak window and nothing is lost pre-bench; spec'd as v22's first parallel act (capture-then-execute spent on v22 instead).

**Nick, in order:** (1) **hivemind commit — `git add -A` stages exactly 10 entries** (5 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · decision-rationale-index · weeks/2026-W28; 4 new: the explainability lane prompt · the v22 prompt · the 2 naming captures; 1 rename: v20 prompt → archive/ [pre-stage porcelain: 5 M + 1 D + 5 ??]), message `_scratch/2026-07-06_hivemind_v21-beat-1_commit-msg.txt` → push. Host porcelain is truth — anything beyond these, STOP and ping the hub. (2) **THE BENCH** per the v20 beat-2 STAGED checklist (quoted verbatim in the session close message) — on acceptance the **72 h soak ENTERS** (NQ-6 restart spike DURING; a soak restart resets the clock — protect the window). (3) **Launch the explainability lane** (launch line in the close message) — fully parallel with the bench. (4) **Dependabot — day 9+, 1 critical: your own words, stop deferring.** (5) Deletables + standing (cleared-lock rm · husk rmdir · telemetry · website.yml · LICENSE-flip · frontend.yml glance · Wave-2 watch · Node-20).

**Hub next:** bench-outcome intake (RECORD everything incl. the 0x0019/0x90 verification → flip the bench-verify javadocs at the next core-touching WU; any fallback invocation ruled WITH Nick, never improvised) → the seed-custody WU JIT post-bench (the SD-5 HARD GATE rides until it lands) → the W2-VAL synthetic-legs charter at soak-entry → the explainability-lane return two-layer audited (labels-are-claims; quotes-are-evidence) → v22 launches when this hub retires.

## 2026-07-04 (v20 beat 3 — THE V20 CLOSE: M9.4b LANDED core `959f925` + **CI GREEN BOTH WORKFLOWS on the push (the gate of record CLEARED)**; hivemind beats 1–2 landed `8c0ba20`→`9bdf7a0`; **M9.4 IS CODE-COMPLETE** — remaining = bench + soak; SD-5 ruling OPEN (re-surfaced); v21 AUTHORED — `context/handoff/2026-07-04_PM-mission-control_v21_orchestrator_session_prompt.md`)

**Landings (all exact):** core **`959f925`** (64 files: 52 M + 12 new; 4739+/316− — the create-mode list matches the staged message file-for-file) + **ci.yml ✅ + install-smoke ✅ on the push, ~3 m each — M9.4b is CLEARED end-to-end** (in-session targeted greens → Nick's full `check` 155 pre-commit → CI on the pushed commit). Hivemind beat-1 = `8c0ba20`, beat-2 = `9bdf7a0` (7 exact). Docs stranded lock file DELETED (one deletable closed). **Dependabot re-nagged on the core push — day 7+, 1 critical, now nagged on BOTH of today's pushes.** Zero count deviations anywhere this arc.

**State after the arc:** the moat is CODE-COMPLETE on a real-transport-capable adapter — every honesty fence test-asserted, every gate green, the thesis one bench session away from silicon. The v20 arc, banked in ONE day: full-PASS launch → M9.4b grounded (five agent false-absences refuted; one live defect found — the un-guarded `invalid`) → authored quote-exact → dispatched → delivered → audited ACCEPT zero-defects → landed CI-green. **OPEN for Nick: (1) the SD-5 ruling** (a = bench-as-shipped + seed-custody WU before any non-bench network [REC] · b = seed WU first, bench waits · c = revert to plain) — one line closes it; **(2) the bench acceptance** per the beat-2 staged checklist; **(3) Dependabot.**

**Hub next = v21's:** SD-5 intake → Nick's bench (outcomes recorded; fallback ruled WITH him if join instability shows) → 72 h soak ENTERS (NQ-6 + the W2-VAL synthetic legs chartered DURING) → the explainability-UX lane (v1.2 frontend skill; real verdicts + `brightness_percent` live; `StandardExplanationService` flattening = the named target) → JIT the next coder WU from bench outcomes + the banked queue (seed-custody leading if (a); unroutable membership row · fence effective-policy · degradeRule non-array · DECIDE-04 sweep · F-7b non-IAS row · bit-7 mask [bench-gated] · Doc 03 §8.1 currency note at the next docs pass). This hub retires at three beats + one full same-day arc.

## 2026-07-04 (v20 beat 2 — M9.4b ✅ DELIVERED + TWO-LAYER AUDIT ACCEPT (ZERO code defects); Nick's full `./gradlew check` GREEN (155) PRE-COMMIT — the deferred gate is DISCHARGED; all EIGHT [REVIEW]s ruled; the SD-5 security consequence framed for Nick's ruling; BENCH ACCEPTANCE STAGED)

**The lane (Claude Code, proven allow-list; §1-first marker held; tests-before-implementation per section):** baseline exact `dc1fb49`; G1–G15 + P43–P70 re-verified at HEAD (11-agent fan-out, 144 findings, 3 benign shorthand divergences, zero STOPs); every suite green in-session — `HeroLoopHardwareFreeIT` **5/5** (the 4 M9.4a legs + the NEW brightness leg: byte-asserted level 127 → report 127 → honest `state_confirmed` → query renders `brightness_percent`=50) · `RestartHonestyIT` 1/1 (no double-actuation across `restartIntegration`; honest timeout; **pin-2 re-install PROVEN at the E2E level** — post-restart CT `command_issued.confirmationTimeoutMs`=15000) · `ZigbeeProductionTransportTest` (reopen order · NETWORK_UP both arms · second-boot RESUMES with zero form frames + INV-SE-03) · both replay suites untouched-green (INV-ES-09) · the M9.3 §J floors EXACTLY intact (SNZB 18-edge; Kelvin CT stream) · 71/41/53 green. **Nick ran the FULL `./gradlew check` GREEN (155) on the working tree — gate DISCHARGED pre-commit (the M9.3/M9.4a pattern); residual = core commit + push + the ci.yml/install-smoke glance (the gate of record).**

**Two-layer audit (2 fan-out agents, evidence-quoted per check w/ verbatim instruction embeds, + hub layer-2 adjudication): ACCEPT — zero code defects.** Layer-2 highlights: §1 same-instance proven at Main (ONE variable to BOTH paths) + the module-info/api lockstep exactly-one; the rescale formula byte-matches (Math.round double math; identity on null/equal bounds; tolerance untouched; 50→127 independently recomputed); percent decoration on ALL THREE read paths, never-throws/never-stored/never-an-event; guard set exactly {superseded, expired_on_restart, unconfirmed, invalid} w/ both-directions membership test + the cross-constant pin; the SD-3 fence never-silent branch verbatim (generic Register-C reason) firing only post-dispatch; 0x1B84 + the RULED-election javadoc carrying SD-5 verbatim + the one-constant fallback line; custody-corruption honesty (names the file, never the key); reopen composes the P24 order ending `resumeStored()` — never re-form; every §6 fold quote-verified with its named test. **Hub refinement NOTEs (recorded, NOT gate-worthy):** (1) fence exotic edge — characterization-present-but-non-UNCONFIRMABLE on a DISABLED-root capability publishes nothing (corpus-unreachable; needs a self-contradictory profile) → polish row: derive the verdict from effective-policy; (2) the membership test's terminal half lacks `"unroutable"` → ruled below, one-line row queued.

**Rulings (all eight [REVIEW]s):** **R1 SD-5 derivation** — the instruction WORKED AS DESIGNED (the mandated pre-implementation bellows re-derivation ran and flagged): bellows' `use_hashed_tclk` supplies a GENERATED random seed as `preconfiguredKey` (persisted in Open-Coordinator-Backup `stack_specific.ezsp.hashed_tclk`); the ruled one-constant shape shipped (0x1B84 + ZigBeeAlliance09 root) — **consequence: per-device TCLKs derivable from the public root; join semantics exercised, no key secrecy added. The follow-on is NICK'S security election — framed below.** · **R2** permits 16→17 + census deltas ACCEPT (sealed-hierarchy-forced; declared) · **R3** test-scope `logback-classic` + `logback-test.xml` ACCEPT (catalog alias pre-existing — zero catalog diff; the M9.2 test-scope precedent; the literal "one build-file" pin superseded by the behavioral shape: production graph untouched) · **R4** `StateQueryService.materialized` 5-arg overload ACCEPT (the necessary public seam; shape test 6→7 declared; the synthetic-lambda-on-interface gotcha banked in coder-lessons) · **R5 `"unroutable"` RULED TERMINAL** (it reports the tracked command's routing fate — killing the entry is correct; the terminal-membership test row rides the NEXT automation-touching WU rather than re-opening a discharged gate) · **R6** IAS-notification dedup scope ACCEPT (verbatim-faithful to the ruled "unsolicited 0x0A only"; ×2-twin bench-watch staged) · **R7** PowerSource 0x81/0x82 battery-side ACCEPT (conservative + instruction-literal; bit-7 mains-backup mask = bench-gated polish) · **R8** staged package-private boot decomposition ACCEPT (the sanctioned HOW latitude; composed order audit-verified). **Cross-agent M9.4b note SERVED** (all six items covered) — **the append-below-separator miss REPEATED (M9.3 class, despite the coder-CLAUDE.md rule):** annotated in place; skill-wording strengthening queued for the next skills pass. Polish queue banked on the backlog row: fence effective-policy hardening · unroutable membership row · degradeRule non-array fail-open · DECIDE-04 identifier sweep (ArchRules display name + HomeSynapseCore ctor javadoc + MCs + Main's stale "six-argument" javadoc) · F-7b non-IAS read-back-unavailable row · bit-7 mask.

**SD-5 — NICK'S RULING (security election consequence; evidence + options + rec):** the hashed election as realized characterizes JOIN behavior without key secrecy (public root ⇒ derivable per-device TCLKs). **(a) BENCH AS-SHIPPED, then a generated-seed custody WU BEFORE any non-bench network** — the election's stated purpose ("characterized before any user network exists") is preserved; the bench fleet is 2 devices and DP-F re-pair is already rehearsed; the §5.5 store already owns key custody (the WU = generated seed + a `CoordinatorOps` seam widening; the leading post-bench candidate). **(b)** hold the bench until the seed WU lands — delays the mid-Aug longest pole for secrecy the bench doesn't need. **(c)** revert to plain 0x1B04 now — abandons the characterization the election exists to buy. **Hub recommendation: (a).**

**BENCH ACCEPTANCE — STAGED (Nick, on silicon; sources: charter return §3 done-when [the beat-42 seven all covered] + SD-5 + the M9.4b bench-verify constants):** (1) **fresh formation under HomeSynapse's OWN network** — hashed-TCLK 0x1B84 exercised HERE (DP-F: the stick's ZHA channel-20 network is the reference capture's, not ours); **fallback per SD-5 verbatim** — join instability attributable to hashed ⇒ the one-constant revert to 0x1B04, reason recorded, W2 row. (2) re-pair BOTH devices (Hue LCA017 + SNZB-03P) on the frozen MG24 7.4.5.0/v13 instrument; the ==13 band with the reflash-re-anchor contingency (AMD-96/98). (3) **BENCH-VERIFY the synthetic constants:** `stackStatusHandler 0x0019` / `EMBER_NETWORK_UP 0x90` on formation AND resume; real-port locate (VID:PID `10c4:ea60`, never descriptor strings) / probe / **reopen leg** (pull + re-seat exercises the watchdog: close→reopenTarget→reopen→resetSession→startSession→**resume**). (4) **Configure-Reporting as an EXPLICIT acceptance step** (fixture replay structurally cannot catch its omission); reporting-config parity recorded OR re-measured under the §3.7 posture before trusting timeout bands. (5) latency reference points pinned: on_off = ACK→report; brightness/CT = command→report (corpus baselines). (6) the §51 behaviors live: on_off/brightness/CT CONFIRMED only via authoritative reports; identify = immediate honest UNCONFIRMED, never transitions; superseded expectations expire verdict-free. (7) **bench-watch:** IAS ×2 notification twins (dedup is 0x0A-only now) · Q10 re-characterize + FE-7 (standing) → on acceptance, the **72 h soak ENTERS** (NQ-6 restart spike + the W2-VAL synthetic legs DURING).

**Nick, in order:** (1) **core commit — `git add -A` stages exactly 64 paths (52 M + 12 new)**, message `_scratch/2026-07-04_core_m94b_commit-msg.txt` → push → **ci.yml + install-smoke glance (the gate of record)**. §10 audit: every path traces to the coder-handoff change-set arithmetic; no runtime state, no secrets (key custody writes only in test tempdirs), test-scope-only build edits beyond the declared lifecycle lockstep. STOP if porcelain ≠ 64. (2) **hivemind beat-2 commit — stages exactly 7 M** (pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · coder-handoff · coder-lessons · cross-agent-notes · pm-lessons), message `_scratch/2026-07-04_hivemind_v20-beat-2_commit-msg.txt` → push. (3) **The SD-5 ruling** (a/b/c above — rec (a)). (4) **BENCH ACCEPTANCE** per the staged checklist → the 72 h soak. (5) **Dependabot — day 7+, 1 critical.** (6) Deletables standing (2 cleared lock files · husk rmdir · telemetry · website.yml · LICENSE-flip · frontend.yml glance · Wave-2 watch · Node-20).

**Hub next:** on the landing + bench outcomes → JIT-author the next coder WU (leading candidates by Nick's rulings: the SD-5 generated-seed custody WU · the queued one-liners riding whichever WU touches automation next) → **charter the explainability-UX lane in parallel** (the v1.2 frontend skill; it consumes REAL verdicts — the 10-value `command_result` vocabulary + `brightness_percent` are now live; `StandardExplanationService` flattening = the named target; FROZEN v1.1 read-API — the new `brightness_percent` data key is SHAPE-compatible, flagged in the lane brief) → 72 h soak riding → **v21 authored the moment this hub nears depth (it is at two beats + one full arc and NEARING it).**

## 2026-07-04 (v20 beat 1 — LAUNCH FULL-PASS (all 11) + **M9.4b AUTHORED ISSUE-READY**: `context/instructions/2026-07-04_M9.4b_real-transport-registry-unification-and-honest-verdicts_coding-instruction.md` + the WU-M9.4 §F–§G pin extension (P43–P70) — DISPATCH READY on Nick's commit + veto window)

**Launch + preflight:** restart-resume clean (lock-free porcelain ×5; both spine frontmatters at v19 beat-1; all three v19-pending commits verified LANDED: hivemind `c7549eb` · skills `0c00ece` · core `dc1fb49`). **Preflight FULL PASS, all 11 — the mirror sync ×3 ran (Check 9 diffs empty ×3), exactly as the v20 charter predicted.** Fleet: core `dc1fb49` (source ≡ `748962f`) · docs `b65ed7d` · hivemind `c7549eb` · bench `5ceff3b` · skills `0c00ece`; watermark **AMD-98, 174/52** re-derived from the register regeneration line; §4b CLEAR (zero open deferred gates; CI green on `748962f` per v19 beat-1); cross-agent ACTIVE 0. v19 prompt → `archive/`. Environment: the §2 truncated-tail phantom hit core/docs/skills mount views (all host-verified intact, attribution corollary applied); hivemind mount served the CORRUPT-index signature → object-reads-only all session (env-model §4); one stranded `index.lock` (created by this hub's first porcelain — plain `git status` before the env-model read, owned) mv'd to `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-07-04` — Nick deletes at leisure. Check-4 note: the backlog M9.4 row still carried "CI glance OPEN" (superseded by v19 beat-1) — reconciled in-row this beat.

**Grounding (2 read-only survey agents, BOTH layer-2 audited + hub direct reads on every load-bearing surface):** the two-layer-on-own-subagents rule paid **×5** — refuted agent claims: `HomeSynapseCore` accessors "absent" (registerIntegrationSchema :1223 / schemaRegistry() :1234 / **deviceRegistry() :1249** all EXIST), `ReportingConfigurator` "Wave-2 placeholder" (EXISTS :44), `DegradeRule` "absent" (public enum :30), `zigbee.transport_unbound` "absent" (adapter :82-87), `SecretStore` "absent" (config module, public create — agent searched the wrong dir). **ONE NEW DEFECT found at grounding: `StandardCommandDispatchService:189` publishes `command_result(outcome="invalid")` and the ledger guard does NOT contain it** — the live F-2 loop-back sibling; instruction §4-mandatory with the SD-4 membership test. All pins P43–P70 into `WU-M9.4.md` §F–§G (masthead notes the A–D/F/G/E letter order).

**Authoring (quote-exact from the ruled charter; the M9.4a shape template):** seven sections in the charter's headline order — §1 R4 registry unification FIRST · §2 brightness derivation-side (schema 0–100→0–254 root fix + the GENERIC schema-driven param→attribute rescale w/ worked example 50%→127±2 + `brightness_percent` derived-at-read) · §3 identify issuability (the beat-5 semantics QUOTED VERBATIM; adapter fence never-silent, test-asserted) · §4 disposition-guard membership (both directions + the `invalid` fix + CommandResultEvent javadoc currency) · §5 real transport (locator VID:PID → channel → session → resume-or-form → NETWORK_UP await [0x0019/0x90 bench-verify-annotated] → watchdog ReopenAction resume-never-reform → `PersistentNetworkParameterStore` w/ SecretStore-over-data-dir custody + custody-corruption = PERMANENT-not-reform → hashed-TCLK 0x1B04→**0x1B84** one-constant, election verbatim, fallback = bench-protocol revert) · §6 the folds (F-4 10 s window + unsolicited-only · F-7a/b enroll-response + posture derives-not-asserts · F-8 invalidation ×3 triggers · F-11 atomic claim · F-12/F-15 fail-closed/graceful profiles · F-14 I/O-outside-lock + backoff · N-4/N-5 [unknown⇒battery-conservative]/N-8 24 h age · DECIDE-04 comment excision) · §7 E2Es (brightness honest-confirm leg · identify-through-REAL-dispatch [R3 interim retires] · RestartHonestyIT w/ pin-2 re-install proven · reopen-order · NETWORK_UP both arms · second-boot resume w/ zero form frames · both replay suites untouched-green). Pinned change-shape: exactly 1 module-info diff (lifecycle device→transitive, M9.1-precedent lockstep) + 1 gradle scope change + zero deps + zero mint + public surface = Identify record + the 8-arg ctor. Disciplines carried: #13 test-home grounding stated per new-test row (zigbee+lifecycle src/test default-gated) · §4c verbatim · G15 record STOP-check · checked-PIE seam map · P2 fan-out survey w/ behavioral publish-count greps · self-review caught + fixed one authoring defect pre-issue (missing-key custody: PERMANENT-fail, never silent re-form — the fleet-orphan class).

**Hub rulings-at-authoring (the charter's "rule at authoring" latitude — VETO WINDOW at dispatch, silence = proceed, the (b)-correction pattern):** (1) **R4 mechanism = ctor INJECTION** (8-arg `HomeSynapseCore` overload, 7-arg delegates; Main passes ONE instance to factory + core) over the read-surface alternative — removes temporal coupling; the ITs already unify via `deviceRegistry()`. (2) **Identify issuability = a core `identify` capability** (Doc-02-native; zero validator change; `CAPABILITY_BY_COMMAND` + the installer's tolerate-unknown WARN literally name identify as the anticipated case; DISABLED-at-root policy ⇒ never-tracked structural; the adapter fence renders the verdict even characterization-absent — SD-3 realized verbatim). (3) **`color_loop`/effects stays NON-issuable Wave-1** — adding a core effects vocabulary is strategy-adjacent; deferred with the R3 richer-payload window, recorded. (4) Percent home = `MaterializedStateQueryService` decoration (all read consumers consistent; FROZEN v1.1 SHAPE untouched — new data key flagged for the lane brief).

**Nick, in order:** (1) **hivemind commit — stages exactly 6 entries** (4 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · pre-verifications/WU-M9.4.md; 1 new: the M9.4b instruction; 1 rename: the v19 prompt → `archive/` [displays pre-stage as 1 D + 1 ??]), message `_scratch/2026-07-04_hivemind_v20-beat-1_commit-msg.txt` → push. Host porcelain is truth — anything beyond these 6, STOP and ping the hub. (2) **The veto window** on the four rulings-at-authoring above — silence = proceed. (3) **DISPATCH M9.4b** — open Claude Code in `homesynapse-core` (allow-list re-verified INTACT this session: `"Bash(./gradlew:*)"` allowed, no gradlew deny — P39 ✓, no smoke needed per the M9.3/M9.4a proof) and paste: *"Read nexsys-hivemind/context/instructions/2026-07-04_M9.4b_real-transport-registry-unification-and-honest-verdicts_coding-instruction.md and nexsys-hivemind/context/pre-verifications/WU-M9.4.md (sections F and G first, then A-E), run the STOP gates, then execute the instruction. Tests before implementation, per section. /nexsys-coder"* (4) **Dependabot — day 7+, 1 critical: your own words, stop deferring.** (5) Deletables at leisure: `rm nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-07-04` · `rm homesynapse-core-docs/.git/index.lock.cleared-by-cowork-2026-07-03` · `rmdir skills-initiative`. (6) Standing: telemetry disable · website.yml adopt · LICENSE-flip · frontend.yml `cf3733e` glance · Wave-2 arrival watch · Node-20.

**Hub next:** on the M9.4b return → two-layer audit vs the DIFF (fan-out w/ embedded expected values; QUOTE the instruction verbatim in audit prompts; labels-are-claims) → WUCP P2 (MODULE_CONTEXT ×4 verification + the backlog flip + **bench-acceptance staging into the bench protocol: the beat-42 seven + DP-F re-pair + Q10 + FE-7 + the hashed-TCLK pin w/ Nick's fallback clause verbatim**) → Nick's bench acceptance → **72 h soak RUNNING** (NQ-6 restart spike + the W2-VAL synthetic legs DURING) → the explainability-UX lane chartered on the v1.2 frontend skill (real verdicts only; `StandardExplanationService` flattening = the named target; FROZEN v1.1 — flag, don't improvise). **v21 authored the moment this hub nears depth (capture-then-execute).**

## 2026-07-04 (v19 beat 1 — THE CURRENCY PASS EXECUTED end-to-end (§2A–§2F); **CI on `748962f` CONFIRMED GREEN by Nick — the gate of record is CLEARED, M9.4b dispatch unblocks at close**; project-knowledge RETIRED on Nick's two §2D rulings; v20 AUTHORED — `context/handoff/2026-07-04_PM-mission-control_v20_orchestrator_session_prompt.md`)

**Launch + preflight:** restart-resume clean (lock-free porcelain ×5; both spine frontmatters at v18 beat 6; no ghost-instance signature). Preflight **STALE-benign exactly as the charter anticipated** — Check 4 (the backlog M9.1 row had sat at "ROUTED" since landing `ec2e3b4`; git-verified, flipped in-pass) + Check 8 housekeeping (the SERVED M9.4a note + a REAL defect: a stray splice fragment at cross-agent line 33 rendering as a phantom heading — the third artifact of the archival-sentence's splice-bait history; excised). The truncated-tail phantom (env-model §2) hit the mount view of 10 recently-committed files across docs+hivemind at launch — host verified intact on every one, attribution corollary applied, VM porcelain quarantined for content all session. Fleet re-derived: core `748962f` · docs `b65ed7d` · hivemind `68ccbf6` (beat-6 landed exactly as staged, 6 ✓) · bench `5ceff3b` · skills `ae434ca`; **watermark AMD-98, 174/52** from the regeneration line; mirrors byte-identical ×3.

**The pass, by section.** **§2A** spine rotation (git-object-sourced from `68ccbf6`, assert-all-before-write, host tails verified): pm-handoff kept v18+v17 beats + Open Risks (180K→84K chars; 22 sections → `archive/pm-handoff-beats-rotated-2026-07-04.md`); PROJECT_SNAPSHOT kept v18+v17 blocks (23 blocks → `archive/PROJECT_SNAPSHOT-beats-42-65-rotated-2026-07-04.md`) **PLUS the standing-section surgery the rotation exposed** — Code State / Active Work / Blocking Issues / snapshot-Open-Risks / Tracked Gaps / Schedule Position / Next-on-Critical-Path / Recent Session Log were ALL carrying 2026-05/06-era copied state ("All 14 design documents", "Next: M3.7 scoping", M3-era risks); every one converted to pointer-form (originals preserved in the archive §B), Docs 17/18 added to the design-doc section, Tracked-Gap #1 CLOSED with source evidence (un-disabled at M3.6d-a per the event-bus MODULE_CONTEXT), the P3 status table capped as a frozen historical record (109K→38K chars total). **§2B** 19 SERVED prompts → `archive/` (+the v18 prompt at first-act = 20 total; `handoff/` now holds exactly the 3 spine files + the standing prompt). **§2C** START_HERE held CLEAN (deliberately state-free — restamp only; the PM launch line matched Nick's actual v19 launch verbatim); the strategic-context-map de-staled (§1 + the product-table Core row had SURVIVED the 2026-07-03 refresh carrying "14 docs Locked / 724 files / M4.0a NEXT" — both pointer-formed; **the module table rebuilt from `settings.gradle.kts`, which exposed a missing `core:value-model` row and the hand-maintained Status column retired as the F2 class**; the claude.ai-Projects mechanism claims reconciled; §8 update-rules aligned to the single-spine-writer model; §9 cited paths existence-verified); `2026-05-31_two-project-claude-architecture.md` adjudicated **SUPERSEDED-in-practice** at the masthead (no Project round-trip in any v13–v18 beat; the lane model replaced it; history preserved); **W27 → COMPLETE** with the close-state block ("the week the thesis ran"); **W28 AUTHORED** (`weeks/2026-W28_jul06-jul12.md` — the moat on silicon: M9.4b → bench → soak → explainability lane; entry gates + the P6 floor). **§2D** Nick RULED both (in-session, recorded): **RETIRE** — 8 files → `project-knowledge/archive/` (the 3 workflow files + 5 curated copies), tombstone README rewritten with the ruling + evidence, `device-corpus/` + the Method Constitution stay live; **Decisions_Quick_Reference retarget+archive** — rationale-index Layer-0 now points solely at the registers. **§2E THE SKILLS PASS:** PM SKILL.md masthead +the three audit-craft pointers (quote-the-instruction-verbatim in audit prompts · labels-are-claims/quotes-are-evidence · new-test-row build-file gating check); coding-instruction-format **+#13 test-home grounding** (the R1 lesson near-verbatim); review-and-quality +the two audit-craft rules at the P5-audit paragraph; constraint-enforcement **spot-verified NO row change at AMD-98** (the band change is register/AMD-level; restamped with the verification); coder skill de-staled (SKILL.md masthead clean + **+1 durable discipline: the M9.4a self-consumption disposition guard**; CLAUDE.md: "15 Locked docs"→18 re-derive form, **the Build Verification section reconciled to the standing lane discipline** [it had mandated the full in-session `check` the discipline defers], session-start baseline clause; mental-model's "Status (current, HEAD 8028337)" de-staled to point-in-time form); **nexsys-frontend → v1.2, the BIG fold: §4a THE VERDICT VOCABULARY IS LIVE** — every fact source-verified same day (`StateConfirmedEvent(commandEventId, reportEventId, attributeKey, expectedValue, actualValue, matchType)` · `command_result.outcome` String ∈ {acknowledged, rejected, superseded, unconfirmed, expired_on_restart} with file:line pointers · `command_confirmation_timed_out` · the hero-question mapping per Nick's beat-5 scope ruling · `StandardExplanationService.isFailure` flattening = the lane's NAMED TARGET · brightness 0–254-canonical/percent-at-query · the FROZEN-v1.1 grounding rule [flag, don't improvise]); **MODULE_CONTEXT content-audit ×3 vs `git show 748962f`:** automation ✓ ACCURATE (ctor-5/deriveOutcome/guard-triple/isFailure-note all source-confirmed) · integration-runtime ✓ ACCURATE (classifier row = F-5 + pinned tests) · **integration-zigbee ✓ with 2 drift fixes applied in place** (both gotchas still framed brightness + identify as "ruling queued"; reconciled to the beat-5 rulings: brightness = DERIVATION-side with the 0–100→0–254 schema root-fix; identify = mechanism M9.4b + the PINNED issuability semantics). Env-model §9 +2 verified quirks (host-Glob false-absence; VM `wc -l` phantom on recently-committed files).

**Nick's rulings this session (recorded):** project-knowledge **RETIRE** (recommended default, elected explicitly) · **DQR retarget+archive** · **CI glance `748962f` = GREEN, both workflows** (ci.yml + install-smoke — the gate of record; §4b clear for M9.4b).

**Nick, in order:** (1) **hivemind commit — `git add -A` stages 19 M + 4 new + 28 renames** (the 18 M: pm-handoff · PROJECT_SNAPSHOT · cross-agent-notes · backlog · strategic-context-map · two-project-doc · START_HERE · W27 · decision-rationale-index · project-knowledge/README · PM SKILL.md · instruction-format · review-and-quality · constraint-enforcement · coder SKILL.md · coder CLAUDE.md · coder mental-model · env-model; the 4 NEW: the 2 rotation archives · W28 · the v20 prompt; the 28 renames: v18 prompt + 19 SERVED prompts → handoff/archive/ + 8 project-knowledge → archive/ [pre-stage these display as D + ?? pairs]), message `_scratch/2026-07-04_hivemind_v19-beat-1_commit-msg.txt` → push. **Host porcelain is truth — the VM view is phantom-contaminated today; if your counts differ materially from 18 M + 4 new + 28 R, STOP and ping the hub.** (2) **nexsys-skills commit — stages exactly 1** (`orchestrators/nexsys-frontend/SKILL.md`), message `_scratch/2026-07-04_skills_frontend-v12_commit-msg.txt` → push. (3) **core commit — stages exactly 1** (`integration/integration-zigbee/MODULE_CONTEXT.md`), message `_scratch/2026-07-04_core_zigbee-mc-reconcile_commit-msg.txt` → push (doc-only; CI will run — expected green). (4) **MIRROR SYNC ×3** (PM + coder → `.claude/skills/nexsys-{project-manager,coder}/`; frontend → `.claude/skills/nexsys-frontend/`) — Check 9 clears. (5) **LAUNCH v20** (fresh Cowork on its prompt — M9.4b JIT authoring is its first substantive act; this hub retires at one deep beat). (6) **Dependabot — day 7+, 1 critical: the 60-second triage.** (7) Deletables at leisure: `rmdir skills-initiative` · `rm homesynapse-core-docs/.git/index.lock.cleared-by-cowork-2026-07-03`. Standing: telemetry · website.yml · LICENSE-flip · frontend.yml `cf3733e` · Wave-2 watch · Node-20.

**Re-preflight (run at close, against the written state):** Checks 1–8, 10, 11 PASS by construction (restamps + fixes verified); **Check 9 STALE by design** (three skill sources edited — clears on Nick's sync). Expected v20 launch state: **full PASS.**

**Hub next = v20's:** M9.4b JIT authoring on the clean substrate (grounding-subagent at the post-pass HEAD → WU-M9.4b pin-sheet additions → the instruction: registry unification FIRST, then the ruled charter per the backlog M9.4 row — quote the beat-5 record EXACTLY for the brightness shape and the issuability semantics) → dispatch (CC lane; P39 allow-list re-verify) → two-layer audit vs the DIFF → WUCP P2 → Nick's bench (hashed-TCLK + fallback) → 72 h soak (NQ-6 + W2-VAL synthetic legs) → the explainability-UX lane charter on the refreshed v1.2 frontend skill.

## 2026-07-04 (v18 beat 6 — THE V18 CLOSE: all three repos LANDED (one provenance split, reconciled); Nick's CURRENCY-PASS directive received and GROUNDED into the v19 charter; v19 AUTHORED — `context/handoff/2026-07-04_PM-mission-control_v19_orchestrator_session_prompt.md`)

**Landings (reconciled):** core = **`64f2ce0`** (the 2 pre-staged testFixtures renames — the `-F` ran before `add -A`) **+ `748962f`** (the remaining 41 files, 4299 insertions), a content-exact two-commit split (the beat-62 provenance class; 2+41=43 ✓ reconciles the staged message; both pushed). Docs = **`b65ed7d`** (5 exact) — **AMD-98 IS RATIFIED; the watermark is AMD-98; 174/52 unchanged.** Hivemind = the incremental chain `a94ae74` → `291a71a` → `33aae08` (content-complete; Nick committed as beats landed — each message's arithmetic reflects its own moment). **OPEN: the ci.yml + install-smoke glance on `748962f` — the gate of record; verify before M9.4b dispatches. Dependabot re-nagged on BOTH core pushes: day 7+, 1 critical.**

**Nick's directive (beat 6, the v19 charge):** clean up stale context/tokens throughout ClaudeFolder/ + nexsys-hivemind, with skills/agents fully optimized and current on everything built and in flight — so future Cowork/CC session authoring "precisely define[s] and instruct[s] all the details necessary for the most optimal coding results." **Hub grounded the inventory THIS beat while the arc's staleness map was in-head (quote-exact targets embedded in the v19 prompt §2):** spine rotation (pm-handoff/snapshot keep v17+v18 beats, rotate older — the §8 token-economics play) · 19 SERVED lane prompts → handoff/archive/ · W28 weekly plan (W27 closes Jul 5) · START_HERE + strategic-context-map + two-project-architecture-doc currency verify · project-knowledge/ regenerate-or-retire ruling (4 hand-maintained files, the F2 rot class; Decisions_Quick_Reference has a live consumer) · THE SKILLS PASS (PM masthead +audit-craft; instruction-format +#13 test-home grounding; review-and-quality +labels-are-claims/quotes-are-evidence; coder skill de-stale read; **nexsys-frontend gains the LIVE verdict vocabulary + brightness percent-at-query + the explainability charter inputs** — Nick's beat-5 scope ruling) · MODULE_CONTEXT content-audit of the three M9.4a updates (checklist-verified only, never content-audited) · deletables surfaced (husk rmdir · inert lock rm). **Two one-liner stale claims fixed IN THIS BEAT rather than chartered** (rationale-index Layer-0 "172/51 at AMD-97"→pointer-form [it went stale twice — the copied-count ban now stated inline]; infrastructure-map "docs 01–17 (+18 in authoring)"→01–18 all Locked + the docs remote observed from today's push).

**Nick, in order:** (1) **hivemind beat-6 commit — stages exactly 6 paths** (5 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · decision-rationale-index · infrastructure-map; 1 new: the v19 prompt), message `_scratch/2026-07-04_hivemind_v18-beat-6_commit-msg.txt` → push. (2) **The CI glance on `748962f`** (ci.yml + install-smoke — the gate of record). (3) **LAUNCH v19** (fresh Cowork on its prompt; this hub retires at two full arcs + six beats). (4) **Dependabot — day 7+, re-nagged twice today: the 60-second triage.** (5) Standing: telemetry · website.yml · LICENSE-flip · frontend.yml `cf3733e` · Wave-2 watch · Node-20 · husk/lock-file rm (now chartered in v19's pass too).

**Hub next = v19's:** the currency pass (§2 inventory, then Check-9 mirror sync + re-preflight) → M9.4b JIT on the clean substrate (registry unification first · brightness derivation-side w/ the 0–100→0–254 schema root correction · identify issuability w/ the pinned AMD-97-literal semantics · guard-membership test · ReopenAction/NETWORK_UP/restart-E2E · the seven hardening folds · bench staging w/ hashed-TCLK + fallback) → dispatch → audit → **Nick's bench** → 72 h soak → the explainability-UX lane charter on the refreshed frontend skill. The v18 arc, banked: the adversarial return audited (one refuted-DEFECT lesson) · the §1 batch ruled in one turn · M9.4a authored/dispatched/landed/audited zero-defect · AMD-98 ratified · **the thesis is a test result.**

## 2026-07-04 (v18 beat 5 — Nick's THREE SHARPENINGS + one confirm on the forward register; ONE corrects a hub fix direction; ACCEPT stands, commits proceeding)

**(1) Brightness — DERIVATION-side, not ingestion-side (fix-direction ruling; the hub's beat-4 direction superseded and struck at source):** hub source-verified BOTH legs — `LevelControlHandler` javadoc: *"the 0–254 level is canonical; percentage derives at query time (Doc 08 §3.5)"*; Doc 08 §3.5 table :206: *"`brightness` (IntValue, 0–254; percentage derived at query time)"*; and **Doc 08 §392's worked example ALREADY SPECIFIES the fix** — *"`brightness.set_brightness(128)` registers a `WithinTolerance` expectation for `brightness` within ±2 of 128… the tolerance values account for rounding differences between the 0–254 ZCL range"* (target AND tolerance in level units). The M9.4b shape: the F-3 bridge performs the same percent→level conversion `buildCommand` does; ROOT CAUSE = `StandardCapabilities.brightness()` attribute schema (0–100) contradicts the Locked canonical → correct to 0–254, which makes the bridge conversion GENERIC (param-domain → attribute-domain linear rescale when bounds differ — schema-driven, protocol-agnostic, no ZCL knowledge in core); plus query-time percent derivation lands before any dashboard brightness renders. An ingestion-side "canonicalize to percent" would have fought the Locked design AND the M9.3 fixture pins. §392 note banked for authoring: the Locked text also names ADAPTER-side expectation registration via `ExpectationFactory` as a design seam — the ledger-side derivation is the generic fallback, coexisting; protocol-precision cases may later register adapter-side per §392 (no action now).

**(2) UNCONFIRMABLE issuability semantics PINNED for the M9.4b ruling (verbatim):** *"an immediate rendered `UNCONFIRMED` verdict with recorded reason, not the silent DISABLED bypass; never-tracked and honestly-verdicted are different promises."* When identify/effects become issuable, the adapter verdict path is the enforcing seam — the regression fence: an issuable command whose policy is DISABLED but whose characterization is absent must NOT silently bypass; test-asserted at M9.4b.

**(3) Explanation-vocabulary → IN-SCOPE for the explainability-UX lane charter (scope ruling):** *"honest-can't-know vs known-failed vs deliberately-superseded are its entire vocabulary"* — `StandardExplanationService`'s UNCONFIRMED/FAILED flattening erases exactly what the hero surface sells; the lane charter carries verdict-vocabulary rendering as in-scope, not follow-up.

**(4) CONFIRMED — disposition-guard membership test (M9.4b instruction-mandatory):** a test pinning every self-published outcome string ∈ the `onCommandResult` guard set, so a future disposition constant cannot silently reintroduce the loop-back class.

All four folded into the backlog M9.4b row this beat; the beat-4 brightness line struck-and-pointered at source. Counts UNCHANGED (same 11-entry hivemind set; message restaged to cover beats 1–5). *"Clean arc — the R1 and R5 rulings in particular are the culture working as designed"* (Nick, banked).

## 2026-07-04 (v18 beat 4 — M9.4a DELIVERED + TWO-LAYER AUDIT ACCEPT, ZERO code defects: THE THESIS RAN — the hero loop GREEN hardware-free; the deferred gate DISCHARGED pre-commit (`check` 155); all six [REVIEW]s ACCEPT; AMD-98 staged)

**The lane (Claude Code, proven allow-list; every suite green on FIRST execution):** §1.1 marker held (§1 landed first); tests-before-implementation per section; `HeroLoopHardwareFreeIT` 4/4 (motion → `occupancy.occupied` ASSERTED-never-"motion" → real engine → dispatch → real handler → byte-asserted 0x0006/0x01 at the scripted NCP on the CORRECT nwk → Hue confirm report through real ingestion → honest `state_confirmed` joining BOTH commandEventId and reportEventId with correlation intact · CT derivation with the tuned 15 s window visible on `command_issued` + the F-2 supersession pair E2E · identify immediate honest verdict, NEVER state_confirmed [AMD-97-INV-01 loop-level] · timeout honesty) + `ZigbeeReplaySafetyIT` 1/1 (ZERO NCP frames in replay; LIVE control fires; `RunPipelineReplaySafetyTest` untouched-green). **Nick's full `./gradlew check` GREEN (155) on the working tree — the deferred gate is DISCHARGED pre-commit (the M9.3 pattern); residual = core commit + push + ci.yml/install-smoke glance (the gate of record).**

**Two-layer audit (2 fan-out agents, both layer-2'd, + hub direct reads on every load-bearing surface): ACCEPT — zero code defects.** Layer-2 highlights: supersession flow direct-read (unresolvable→nothing; DISABLED-with-resolvable-attribute EXPIRES per issuance-supersedes — **the one agent DEFECT claim REFUTED: it judged against the HUB'S OWN prompt compression ("DISABLED → no expiry"), not the instruction; the code follows the instruction and ratified caveat 3** — the dispatch-craft lesson recurs at audit-prompt level, pm-lessons entry this beat); `reindexLive` handles the empty-registry restart edge HONESTLY-BY-DESIGN (every unresolvable path mirrors LIVE non-tracking — "optimistic in effect, never a false expired_on_restart"; the residual is the FENCED registry-rebuild gap, not a ledger defect); buildCommand ×3 byte-verified incl. kelvin↔mireds round-trip; sendZclFrame rides the bench-proven unicast (v13 layout test-pinned); handler causation chained on every publication, zero publishRoot; installer maps from BUNDLED profile values; W10 verified end-to-end (Main:110 → HomeSynapseCore:1223 → registry); FakeNcp fakes at the ASH BYTE layer (the hero claim is real — production AshCodec parses the test's frames); `ReplayInFlight` +parameters extension clean; hygiene sweeps clean (zero synchronized/Instant.now/@EventType; census exactly ONE new public type — `ZigbeeIntegrationAdapter` verified package-private).

**Rulings — all six [REVIEW]s ACCEPT:** **R1** IT-home=lifecycle (integration-tests is `piProfile`-gated out of default `check` — grep-verified; parking THE done-when there would hide it from every gate; **hub authoring miss OWNED: the Files-table home was pinned ungrounded**) · **R2** base-`final` removal (public `ClusterHandler` untouched — the frozen seam holds) · **R3** identify-not-issuable interim ACCEPT (the E2E synthesizes only the executor front-end, the REAL router+handler run; **product fact: no UNCONFIRMABLE command is production-issuable until M9.4b rules identify/effects issuability**) · **R4** registry-duality ACCEPT-as-inert (Main's comment self-documents: production zigbee = PIE-at-initialize → FAILED-no-retry → boot continues, INV-RF-01; **unification MANDATORY at M9.4b before the real transport binds**) · **R5** disposition guard ACCEPT (beyond-the-text catch: the looped-back `superseded` would have killed the NEWER command; also closes the pre-existing `expired_on_restart` loop-back; filters superseded/expired_on_restart/unconfirmed, real outcomes still terminal-match, unit-pinned) · **R6** W10 gateway ACCEPT (java.base-only public method, the M3.6e.1 pattern; rejected-alternative documented). **[INFO] routing:** brightness canonicalization → M9.4b NAMED item (grep-confirmed: LevelControl publishes RAW 0–254 vs the 0–100 schema — set_brightness confirmation would FALSE-FAIL once exercised; ~~percent-at-ingestion~~ **direction CORRECTED by Nick at beat 5: DERIVATION-side — see the beat-5 record; the ingestion-side direction recorded here was wrong and is struck so it cannot propagate**) · explanation-vocabulary (`superseded`/`unconfirmed` render as FAILED today) → the explainability-UX lane · DECIDE-04 PHANTOM-ID in a Main comment (the beat-61 excised identifier resurfaced — comment-only, M9.4b one-liner) · degradeRule-enum-in-reason-text + sentinel-reload-silence → polish list · scale register (no action): pollExpirations O(n)/tick, single-in-flight EZSP queue (D-M92-5 — the ASH-window lever is Wave-2+), reindexLive re-derivation at boot. Backlog M9.4b row carries the full inherited set.

**P2 executed this beat:** **AMD-98 authored + staged (docs, 6 paths): the consolidated amendment** — §1 the EZSP band correction (supersedes AMD-96/E1 v14-acceptance in part; un-narrow condition = Wave-2 characterization) + §2 the four realizations of ratified text + §3 zero mints (174/52 unchanged; watermark 97→98 in the register regeneration line) + Doc 08 §3.6 / Doc 02 §3.8 / Doc 05 §3.7 realization-currency notes. Backlog M9.4a row → ✅ DELIVERED+AUDITED; M9.4b row + inherited set. Cross-agent M9.4a note → SERVED (ACTIVE: 0). pm-lessons +1 (audit-prompt compression). rationale-index +1 (the M9.4a ruling set). Check 9 untouched (no skill edits).

**Nick, three commits, in order:** (1) **core — `git add -A` stages exactly 43 paths** (29 M + 12 new + 2 renames [already-staged testFixtures moves]), message `_scratch/2026-07-04_core_m94a_commit-msg.txt` → push → **ci.yml + install-smoke glance (the gate of record)**. §10 audit: every path traces to the completion report's Files section; no runtime state, no secrets, test-scope-only build edits (2 files, enumerated). STOP if porcelain ≠ 43. (2) **docs — stages exactly 5 paths** (AMD-98 NEW · Architecture_Invariants_v1.md · design/02 · design/05 · design/08 — STOP beyond 5), message `_scratch/2026-07-04_docs_amd98_commit-msg.txt` → push. **Your push IS the AMD-98 ratification act (every constituent ruled at beat 2).** (3) **hivemind beats-1–4 — stages exactly 11 entries** (8 M: pm-handoff · PROJECT_SNAPSHOT · backlog · rationale-index · pm-lessons · cross-agent-notes · coder-handoff · coder-lessons; 2 new: WU-M9.4.md · the M9.4a instruction; 1 rename: v17 prompt → archive/), message `_scratch/2026-07-04_hivemind_v18-beat-1_commit-msg.txt` (rewritten to cover beats 1–4) → push. (4) **Dependabot — day 7+.** (5) Standing unchanged.

**Hub next:** on the three landings → **M9.4b JIT authoring** (grounding at the NEW core HEAD; the ruled scope + the inherited set — registry unification FIRST [blocks real transport] · identify issuability ruling · brightness canonicalization · ReopenAction/NETWORK_UP/restart-E2E · F-4/F-7/F-8/F-11/F-12/F-14/F-15 · bench staging w/ hashed-TCLK pin + fallback) → dispatch → audit → **Nick's live-bench acceptance** (beat-42 seven + DP-F re-pair + Q10 + FE-7; the TCLK election exercises HERE) → 72 h soak (NQ-6 + W2-VAL synthetic legs). Explainability-UX lane NOW UNBLOCKED (real verdicts exist — `state_confirmed`/`superseded`/`unconfirmed` are live vocabulary; it consumes the M9.4a loop). **v19 authored the moment this hub nears depth (capture-then-execute — this hub is at four beats + two full arcs and NEARING it).**

## 2026-07-04 (v18 beat 3 — M9.4 AUTHORED: the P1 SPLIT ruled at authoring (M9.4a/M9.4b); M9.4a ISSUE-READY on the proven allow-list; THREE grounding corrections banked — one amends an un-vetoed hub default)

**Grounding (two read-only survey agents, both layer-2 spot-checked + hub direct reads; HEAD verified `c856eab` clean, no baseline shift):** everything pinned in `context/pre-verifications/WU-M9.4.md` (P1–P42 + §C verbatim module-infos + §D fold-map + §E the F-2 arithmetic both directions). **Three corrections where SOURCE outranked the spine shorthand (⚠A1–A3):** (1) **`ConfirmationInstance` does not exist** — ruling (a) realizes as a TUNED `ConfirmationPolicy` + rebuilt `CommandDefinition.defaultTimeout` on the per-entity `CapabilityInstance` at adoption (zero new device-model types; the executor's :302 precedence and the ledger's :294/:302 read points untouched — Nick's "read path works unchanged," realized purer than the shorthand). (2) **The (b) bridge CANNOT be executor-side** — `CommandIssuedEvent` is FROZEN at 5 components (its own javadoc: AMD-95 §2.B/§2.C); an executor-side shape forces an event-model AMD contradicting ruling (a)'s zero-new-event-surface virtue; the derivation lands in the ledger's `onCommandIssued` (which already resolves the CapabilityInstance and holds `parameters`), decoder injected as `Function<String,Map<String,Object>>` from the EXISTING `PersistenceFactory.commandParameterDecoder()` (no new module edge). **This corrects the un-vetoed (b) default — same capability-domain semantics, different insertion point; flagged to Nick for veto at dispatch.** (3) **`PortWatchdog` EXISTS with the injected `ReopenAction` seam** (:58, :81) — the composition is M9.4b's; the agent's "not built" claim refuted at source. Also verified: `turn_on` HAS its declared `ExpectedOutcome` (P41 — the hero confirm rides the existing `chooseOutcome`); the agents' two other label-noise items resolved by direct reads (the standing two-layer-on-own-subagents pattern).

**The P1 split (ruled at authoring — the fold grew the scope to five coherent chunks):** **M9.4a — the moat wiring + the hardware-free hero loop** (instruction: `context/instructions/2026-07-04_M9.4a_moat-wiring-and-hardware-free-hero-loop_coding-instruction.md`): §1 governance-freeze-first unit (supersession expiry with ISSUANCE-supersedes semantics + the derivation bridge + F-10 deadline-in-future + classifier UOE-arm-and-PIE-cause-walk with the three pinned tests + MAX 14→13 with the reflash-contingency PIE text) → §2 adoption-installed overrides incl. pin-2's shared installer (adopt + relink from cached `matchedProfileId`) → §3 minimal command path (buildCommand OnOff/Level/ColorCT byte-exact, sendZclFrame over the bench-proven unicast layout, `ZigbeeCommandHandler` with the F-6 hint-join + N-6 causation + the immediate honest UNCONFIRMED verdict realizing Doc 02 §3.8's "reason recorded") → §4 minimal composition (`ZigbeeIntegrationFactory` public [the ONE new public type] @ `Main.java:71` + W10 schema registration + R4 registry instance) → §5 `HeroLoopHardwareFreeIT` (the binary done-when: motion → occupancy.occupied asserted → real engine → dispatch → FakeNcp byte-asserted → Report_Attributes → honest CONFIRMED with causal joins; + the supersession pair E2E + UNCONFIRMABLE identify + timeout honesty) + `ZigbeeReplaySafetyIT` (INV-ES-09 with the REAL handler; `RunPipelineReplaySafetyTest` untouched-green). Pins: zero mint (71/41/53), zero production deps, zero module-info diffs, ONE new public type; FakeNcp promotes to zigbee testFixtures (the three enumerated TEST-scope build edits are the only permitted build-file changes). **M9.4b (JIT after a's gate; §4b block honored):** ReopenAction composition · NETWORK_UP await · restart-honesty E2E · F-4/F-7/F-8/F-11/F-12/F-14/F-15 · N-4/N-5/N-8 · bench staging (hashed-TCLK formation pin + the fallback clause live in the BENCH protocol, not code).

**Nick, in order:** (1) **hivemind beats-1–3 commit — stages exactly 8 entries** (5 M: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · decision-rationale-index · the annotated audit return; 2 new: `WU-M9.4.md` · the M9.4a instruction; 1 rename: v17 prompt → archive/ [displays as 1 D + 1 ??]), message `_scratch/2026-07-04_hivemind_v18-beat-1_commit-msg.txt` → push. Host porcelain shows 9 lines pre-stage; anything beyond, STOP. (2) **Veto window on the (b) insertion-point correction** (ledger-side derivation) — silence = proceed; it preserves your (a) rationale exactly. (3) **DISPATCH M9.4a**: open Claude Code in `homesynapse-core` (allow-list verified intact this session — no smoke needed per the M9.3 proof) and paste: *"Read nexsys-hivemind/context/instructions/2026-07-04_M9.4a_moat-wiring-and-hardware-free-hero-loop_coding-instruction.md and nexsys-hivemind/context/pre-verifications/WU-M9.4.md, run the STOP gates, then execute the instruction. Tests before implementation, per section. /nexsys-coder"* (4) **Dependabot — day 7+.** (5) Standing unchanged (telemetry · website.yml · LICENSE-flip · frontend.yml glance · Wave-2 watch · Node-20 · docs lock-file rm).

**Hub next:** on the M9.4a return → two-layer audit vs the DIFF (§J-style independent recomputation on the hero-loop assertions + the byte-exact frames) → WUCP P2 (incl. the docs pass staging: the consolidated-amendment realization notes on Doc 08 §3.6/Doc 02 §3.8/Doc 05 §3.7 + the AMD-96 band correction) → M9.4b JIT authoring → bench acceptance staging → 72 h soak. **v19 authored the moment this hub nears depth (capture-then-execute).**

## 2026-07-04 (v18 beat 2 — the M9.4 §1 DECISION BATCH RULED, one turn, all six items; THIS IS THE CANONICAL RULINGS RECORD — M9.4 authors against it)

**M9.4-DP-a — RULED (i): adoption installs per-device-tuned `ConfirmationInstance` overrides (verbatim, decisive):** *"The ledger already reads the capability-instance level (`instance.confirmation().mode()` at line 294) and the executor already resolves the timeout into `command_issued` — so (i) means the adapter maps `confirmation[]` → core-typed `ConfirmationInstance` overrides once, at adoption, and the entire existing read path works unchanged and protocol-agnostically. It's the Doc-02-§3.8-native shape, which makes the AMD a realization of a Locked concept rather than a new mechanism — the same virtue the beat-57 rulings prized in DP-18-A. Option (ii) puts per-device facts on the wire per-command (denormalized), demands a frozen-event AMD now, and obligates every future adapter at every dispatch instead of once at adoption. The observability benefit (ii) claims is recoverable later — the explanation service can join verdicts to the installed characterization, and the R3 richer-payload AMD stays available as a pure observability enhancement when the REST discovery surface wants it."* **TWO BINDING PINS (Nick's, instruction-mandatory): (1) the mapping lives ADAPTER-side (INV-CE-04 — core never learns Zigbee vocabulary); (2) the re-link path RE-INSTALLS overrides from the cached `matchedProfileId` (the in-memory registries start empty on restart)** — pin 2 extends the restart-honesty prove-seam.

**M9.4-DP-d — RULED: narrow acceptance to ==13; v14 → PIE-until-dialect-complete (verbatim):** *"Your own doctrine decides this: freeze the schema, defer the behavior — and the return's own scope note says the v14 surface was never systematically characterized, so 'version the codecs now' means writing wire code against silicon we own zero units of. Half-right v14 support is worse than none: partially deaf is a three-hour sniffer session, PIE is a one-line diagnosis. Narrowing is the R4 band-edge principle applied inside the band, the PIE message carries the negotiated version + the AMD-96 reflash contingency (Register C voice), and the arriving v14-batch Wave-2 unit becomes the characterization asset that earns the dialect properly. A deaf radio that looks paired is a lying system — **never-false-CONFIRMED's spirit extends to never-false-ALIVE**."* Execution: MAX_SUPPORTED 14→13 (one constant + test); PIE text carries negotiated version + the AMD-96 reflash-contingency reference.

**Hashed-TCLK — ELECTED (Nick's security election): hashed TCLK at the M9.4 formation, WITH THE EXPLICIT FALLBACK CLAUSE (verbatim):** *"Elect hashed TCLK for the M9.4 formation, and if the bench shows join instability attributable to it, drop to plain for Wave-1 with the reason recorded and a W2 row — evidence-first, one variable at a time, characterized before any user network exists rather than after."* Rationale banked: posture flips on real user networks mean re-pairing fleets (the SmartThings-class betrayal); conservative-defaults/encrypt-from-genesis is the standing posture; DP-F's fresh-network re-pair rehearsal is already in the M9.4 bench script.

**Classifier — RULED: ADOPT BOTH (cause-walk-for-PIE-only + the F-5 UOE→PERMANENT arm) (verbatim):** *"The cause-walk-for-PIE-only is surgical: it preserves the bare-instanceof HA-guard for everything else (wrapped random garbage still classifies TRANSIENT), and walking only for PIE can't misfire — finding a PIE anywhere in a cause chain means someone deliberately threw permanent intent, and suppressing that intent is the bug, not the walk. The UOE arm is… a Doc 05 §3.7 taxonomy-currency fix: an UnsupportedOperation is definitionally non-transient — retry cannot help — and classifying it PERMANENT converts a stub slip… from a silent infinite restart loop into a loud SUSPENDED with a reason."* **THREE PINNED TESTS (instruction-mandatory): wrapped-PIE → PERMANENT · UOE → PERMANENT · wrapped-anything-else → TRANSIENT preserved.** Rides M9.4's integration-runtime touch; reconcile the enum javadoc same pass.

**(b)/(c) hub defaults STAND un-vetoed:** (b) the parameters→Expectation bridge is EXECUTOR-side, capability-domain (commanded value parsed at issue; `WithinTolerance(target, policy.tolerance)`/`ExactMatch` from `ConfirmationPolicy` + the AMD-97 per-device override; realizes the javadoc-anticipated `ExpectationFactory` seam; the ledger stays a pure evaluator). (c) supersession-expiry per ratified caveat 3 verbatim (newer LIVE `command_issued` on same (entity, attribute) expires older in-flight expectations, recorded superseded disposition, no new event type; acceptance asserts the 161/216/221 triple + a synthetic within-tolerance pair). **All six items land inside ONE consolidated Mode-2 AMD at M9.4 §1 (number assigned at milestone per the P2 rule).**

**Consolidation note:** beats 1–2 ride ONE hivemind commit — the file set is identical (the 6 entries), the staged message now covers both beats. **Hub next: M9.4 authoring** (grounding-subagent at issue-time HEAD → `WU-M9.4.md` → the instruction: the consolidated §1 AMD at TOP realizing all six rulings + the five-seam prove-list + the 14 ruled folds; done-when = the hero loop GREEN hardware-free over FakeNcp incl. BOTH F-2 directions + immediate honest UNCONFIRMED for identify/color_loop + pin-2's re-link re-install case; M9.4a/b smell-test at authoring) → proven-allow-list dispatch → two-layer audit → WUCP P2 → bench (now incl. the hashed-TCLK leg + fallback clause) → 72 h soak.

## 2026-07-04 (v18 beat 1 — LAUNCH + the adversarial return AUDITED two-layer: ACCEPT, 15/15 findings verified, ONE arithmetic correction (F-2 units); rulings 14 FOLD-M9.4 / 1 DEFER-W2 / 0 FIX-NOW / 0 REFUTE; the M9.4 §1 decision batch to Nick)

**Launch verification:** restart-resume clean (lock-free porcelain ×5 repos; both spine frontmatters at v17 beat 6; no prior-writer signature). Preflight **FULL PASS, all 11** — first full-PASS launch of the arc (Check 9 PASS: both mirrors byte-identical, Nick's sync ran; hivemind beat-6 landed `6bf6929` exactly as staged). Baseline re-derived: core `c856eab` · docs `01602fa` · hivemind `6bf6929` · bench `5ceff3b` · skills `ae434ca`; watermark AMD-97, invariants 174/52 (register regeneration line re-read); zero unresolved deferred gates (M9.3's was discharged pre-commit, beat 3); coder-handoff NEXT-WU pointer = M9.4 ✓. v17 prompt → `archive/`. One self-inflicted near-miss owned: a pointless cross-device `mv` probe of the v18 prompt to `/tmp` — env-model §4 saved it (copy succeeded, gated unlink failed, original untouched; tail verified host-side; junk cleared). Lesson: never `mv` mounted files off-mount, even "temporarily."

**The audit (layer 2 on every load-bearing claim; method = hub direct reads + one 24-claim read-only verification agent, itself layer-2'd):** **F-1 CONFIRMED byte-for-byte** — bellows v13/v14 `commands.py` RE-FETCHED by the hub (v14 overrides 0x0034 `message_tag`→u16 and 0x0045 gains `timestamp` u32 with nwk/eui64 reorder → length [18]→[30], offset arithmetic independently recomputed 1+11+2+8+1+1+1+1+4=30; v13 carries NO override — and the v13 shape is bench-PROVEN by the green fixtures); in-repo: `sendUnicastLocked` writes tag u8 at [14] + length [15], `EzspIncomingMessage.parse` reads sender [14..15] + length [18], negotiation accepts ≤14 (PREFERRED/TARGET=13, MIN=8, MAX=14) — **the narrow fix is ONE constant (MAX 14→13) and the existing >MAX PIE message is already the honest R4-spirit text.** Hub sweep-extension: `messageSentHandler` 0x003F (also v14-widened) is UNCONSUMED in main — no additional F-1 surface; M9.4 must not newly consume it pre-dialect-decision. **F-3 CONFIRMED on all four legs** + sharpened: `ConfirmationPolicy` ALREADY carries mode+attribute+tolerance (±2 brightness / ±50 K CT, 5000 ms) — the only missing ingredient is the commanded VALUE; `ExpectationFactory` is javadoc-anticipated-and-absent (ledger javadoc names it). **F-2 UPHELD with a UNITS CORRECTION (the audit's one substantive catch, annotated at the source finding per the FE-1b precedent):** the review's |221−216|=5≤50 computes a MIREDS delta against the KELVIN tolerance — in capability domain the triple is 6211/4630/4525 K vs the 4525 K report, so the fixture yields a false TIMEOUT (false-FAIL, which the fixture's own verdict line names), not a false CONFIRMED; false-CONFIRM needs a ≤50 K command pair (the UI dimmer-drag coalescing class). Caveat 3 bans BOTH directions — severity HIGH and fix direction unchanged; M9.4 acceptance asserts both cases. **F-10 verified + Doc-07 compatibility established** (§3.11.2 defines crash recovery over commands "in-flight at crash time"; a post-restart issue is definitionally outside it — the deadline-in-future fix needs NO amendment and preserves conservative no-re-fire). **F-7 adjudicated NOT-refute** (the M9.3 instruction's own §3.12 line mandated the enroll-response flow ATTEMPTED; the send half needs M9.4's write path; the posture-fact over-claim is real — VERIFIED_REPORTS returned in BOTH arms, direct-read). F-2/F-7's refute-adjacency flags therefore resolve to ENFORCEMENT of ratified/instructed text, not refutation — nothing re-litigates a ruling. Agent fan-out: 24 claims, all evidence-quoted; its two REFUTED labels (F-5b, F-10) were self-contradicted by their own quoted evidence and resolved by hub direct reads (the two-layer-on-own-subagents rule paid again — label noise, zero substance). Clean sweeps spot-verified (replay-purity greps, zero `synchronized`, zero-mint, Kelvin arithmetic recomputed: 447→2237, 221→4525, 154→6494, 153→6536, 196→5102 ✓). Write-isolation: ONE file, verified.

**Rulings (indexed; folded into the backlog M9.4 row verbatim):** F-1/F-2/F-3 → FOLD-M9.4 feeding §1 decisions (d)/(c)/(b) · F-4/F-5/F-6/F-7/F-8/F-9/F-10/F-11/F-12/F-14/F-15 → FOLD-M9.4 (hardening set; F-5 rides the cause-walk ruling) · F-13 → DEFER-W2 (+MODULE_CONTEXT divergence note at M9.4's P2) · N-6 ELEVATED BINDING (dispatch always chains causation; oldest-deadline deterministic fallback) · N-4/N-5/N-8 ride the M9.4 hardening list; N-7 reinforces the queued R3 AMD candidate; N-1/N-2/N-3/N-9/N-10/N-11 recorded, no action now. **ZERO FIX-NOW** (nothing bites pre-M9.4: the bench instrument is v13-frozen so F-1 cannot fire before Wave-2; the moat path is unwired until M9.4 by design). **P1: the fold CONFIRMS the M9.4a/b split likelihood** — smell-test at authoring. The adversarial lane's price/value: 3 HIGH + 8 MEDIUM caught at desk price after every automated gate ran green — the post-green pattern is now twice-proven.

**Nick, in order:** (1) **The M9.4 §1 decision batch — THIS TURN** (sub-decisions a–d + hashed-TCLK + classifier cause-walk; evidence + recommendation each; (b)/(c) presented veto-or-default, (a)/(d)/TCLK/cause-walk as elections). (2) **hivemind beat-1 commit — stages exactly 6 entries** (5 modified: pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · decision-rationale-index · the audit return [+1 hub annotation at F-2]; 1 rename: the v17 prompt → `archive/`), message `_scratch/2026-07-04_hivemind_v18-beat-1_commit-msg.txt` → push. Host porcelain is truth — anything beyond these 6, STOP. (3) **Dependabot — day 7+: the 60-second Security-tab triage; your own words: stop deferring.** (4) Standing: telemetry disable · website.yml adopt · LICENSE-flip timing · frontend.yml `cf3733e` glance · Wave-2 arrival watch (a v14-batch unit = F-1's characterization asset) · Node-20 · the inert docs lock-file rm · constraint-enforcement LTD touch-up (rides next skills pass).

**Hub next:** on the rulings → M9.4 authoring (grounding-subagent at issue-time HEAD → `WU-M9.4.md` → the instruction: §1 AMD at TOP realizing the ruled a–d + the five-seam prove-list + the ruled folds; done-when = the hero loop GREEN hardware-free over FakeNcp incl. the F-2 both-direction cases + immediate honest UNCONFIRMED for identify/color_loop; smell-test the a/b split) → dispatch on the proven allow-list (verify settings unchanged) → two-layer audit → WUCP P2 vs the DIFF → bench acceptance → 72 h soak (W2-VAL synthetic legs + NQ-6 during). Explainability-UX lane sequences just behind the moat wiring; dossier-content lane launchable anytime. v19 authored the moment this hub nears depth.

## 2026-07-04 (v17 beat 6 — the ACTUAL v17 close) — the bounded SKILLS PASS executed (durable arc-lessons folded; the queued LTD-table fix done — two rows were factually wrong); the skills-initiative corpus found MIGRATED + versioned; full-system activation DEFERRAL re-affirmed with evidence

**Beat-5 landed `49574c7` (4 exact).** Nick's pre-v18 directive: assess whether to update/optimize the skills + the skill system for the arc's lessons. **Verdict executed: YES to a bounded skills pass NOW (this was the ledger's queued "next skills pass" moment); NO to activating the full skills-initiative build (the 2026-06-27 deferral re-affirmed WITH evidence: five beats, two CC lanes, four Cowork lanes, ZERO skill-staleness incidents, Check 9 PASS throughout — the cheap discipline is winning; the critical path is the thesis run, not skill infrastructure).**

**Two discoveries en route (both resolved):** (1) **`skills-initiative/` is an EMPTY HUSK** — the design corpus MIGRATED into the versioned `nexsys-skills/design/` (14 tracked files verified via lock-free ls-files); the truth-hierarchy + infrastructure-map pointers were stale → retargeted this beat; the husk directory is deletable at leisure. (2) **The constraint-enforcement LTD table had two factually wrong rows** — row 16 read "REST (Javalin) + WebSocket + JSON Schema" while the register's LTD-16 is Semantic Versioning + the one-major-version deprecation window (the exact floor DP-18-A builds on!), and row 17 cited the phantom "DECIDE-04" (proven nonexistent at beat 61). Both fixed FROM THE REGISTER + the table now carries the lossy-summary warning ("the register is authoritative; a reviewer who bypasses this table is doing it right"). The rot class = hand-maintained copy drift — exactly what the truth hierarchy predicts; the fix honors it.

**The pass (9 paths, all hivemind):** `project-manager/SKILL.md` masthead +2 durable disciplines (post-green ADVERSARIAL-REVIEW lanes at arc boundaries [the M9-arc precedent: 3 HIGH after full green]; the two-layer audit extends to YOUR OWN subagents) · `references/coding-instruction-format.md` +3 additions (#10 governance-freeze-first sections · #11 cross-repo fixture provenance + fixture-clock · #12 declare anticipated seam-fills in stub tests) · `references/review-and-quality.md` +2 methods (audit fan-out with EMBEDDED EXPECTED VALUES + independent recomputation; the post-green adversarial-review pattern) · `references/constraint-enforcement.md` (the LTD-table de-lossing) · `coder/CLAUDE.md` (cross-agent notes append ABOVE the `## Archived` separator — the M9.3 miss made rule) · `context/process/truth-hierarchy…md` + `infrastructure-map.md` (corpus pointers retargeted; the CC deny-beats-allow root cause + PROVEN fix recorded; the husk noted). **nexsys-frontend untouched deliberately** (no frontend work this arc; its next fold rides the explainability-UX lane). **Check 9 goes STALE on commit until Nick's mirror sync — sync BEFORE launching v18 and v18 boots with Check 9 PASS.**

**Nick, in order:** (1) **hivemind beat-6 commit — stages exactly 9 paths** (4 under `project-manager/` · 1 `coder/CLAUDE.md` · 2 `context/process/` · pm-handoff · PROJECT_SNAPSHOT), message `_scratch/2026-07-04_hivemind_v17-beat-6_commit-msg.txt` → push. (2) **Mirror sync** (`nexsys-hivemind/{project-manager,coder}/` → `.claude/skills/nexsys-{project-manager,coder}/` — your manual act; clears Check 9). (3) Optional hygiene: `rmdir skills-initiative` (empty husk) + the inert docs lock file. (4) **LAUNCH v18** (fresh Cowork on its prompt — unchanged; it reads beats 5–6 at boot). (5) **Dependabot — day 7+.** (6) Standing unchanged otherwise.

**Skills-system disposition (the assessment of record):** the 4-tier design stays deferred-but-ratifiable at `nexsys-skills/design/2026-06-26_Skills-Architecture_design_v1.md` (review → co-sign → Lock whenever Nick chooses — ratifying the design, not a build commitment). Revisit trigger: the post-V1 window per the standing ruling, or earlier only if skill count or rot rate actually grows (this arc: zero rot incidents; the one defect found was in a hand-maintained TABLE, which is the anti-pattern the discipline already bans). The battle-testing Nick asked about is happening exactly where it should: in the spine records, the reference-file addition sections, and the lessons files — the skills stay thin, current, and pointer-clean.

## 2026-07-04 (v17 beat 5) — the adversarial return LANDED (3H/8M/4L/11N, no CRITICAL; hub fast-checks corroborate all three HIGHs); v18 AUTHORED — `context/handoff/2026-07-04_PM-mission-control_v18_orchestrator_session_prompt.md`

**Beat-4 landed `e55378b` (4 exact).** Nick launched the adversarial-review lane; its return is on disk (`context/audits/2026-07-04_M9-arc_adversarial-review_return.md`, 38.5 KB, write-isolation held: ONE file; baseline pins match `c856eab`/`01602fa`/`5ceff3b`). **Headline: no CRITICAL as wired; 3 HIGH all landing on M9.4's named seams** — F-1 v14-accepted-but-v13-only wire dialect (0x0034/0x0045; bellows v13/v14 fetched live by the lane — deaf-radio class on v14 ship-batch silicon) · F-2 no superseded-expectation expiry in the ledger (the Hue 216→221 coalescing case ⇒ false CONFIRMED once tracking wires; AMD-97 caveat 3 + the §51 test spec mandate it) · F-3 TOLERANCE structurally unreachable (empty `expectedOutcomes` on set_brightness/set_color_temperature; static `ExpectedOutcome`; zero production `WithinTolerance` construction — the parameters→Expectation bridge is missing). **Hub fast-checks THIS beat corroborated the in-repo halves of all three** (StandardCapabilities.java:174/200 `List.of()` ✓; only the AMD-87 deserializer constructs WithinTolerance ✓; zero supersession logic in the ledger ✓). Notable MEDIUMs: dedup-no-time-window · UOE→TRANSIENT vs Doc 05 §3.7 · NWK-reuse stale dispatch addresses · IAS enroll-drop + posture over-claim · the ledger TRANSITION-window ghost class. Clean sweeps (two-grep-verified by the lane): replay purity, ASH/EZSP framing core, codecs, retry ladder, Kelvin, §3.7 table, the §1 freeze layer. **The FULL two-layer audit of the return is v18's FIRST SUBSTANTIVE ACT — findings are corroborated, not yet ruled; nothing folds into the backlog until the audit rules them.**

**v18 AUTHORED (capture-then-execute — this hub closed at depth after four arcs: launch-verification + THE LOCK [fb91c23, 174/52] · M9.3 authored+dispatched [the CC deny-list root cause fixed and PROVEN] · landed c856eab CI-green + audited ACCEPT zero-defects [five [REVIEW]s ruled] · the strategy beat + the adversarial lane full cycle).** v18's order: hygiene → audit the return (layer-2 on every HIGH; rule FOLD-M9.4/FIX-NOW/DEFER-W2/REFUTE) → **the M9.4 §1 decision batch to Nick** (moat-wiring sub-decisions a–d incl. the F-1 v14 posture [hub rec: narrow-to-13 + PIE, the R4 band-edge spirit] + hashed-TCLK + classifier cause-walk in ONE turn) → M9.4 authored (five-seam prove-list; done-when = the hero loop GREEN hardware-free over FakeNcp incl. the F-2 supersession case; **P1 split M9.4a/b now LIKELY** — smell-test at authoring) → dispatch → bench → soak.

**Nick, in order:** (1) **hivemind beat-5 commit — stages exactly 4 paths** (2 modified: `context/handoff/pm-handoff.md` · `context/status/PROJECT_SNAPSHOT.md`; 2 new: the v18 prompt · the adversarial return), message `_scratch/2026-07-04_hivemind_v17-beat-5_commit-msg.txt` → push. Host porcelain is truth — anything beyond these 4, STOP. (2) **LAUNCH v18** (fresh Cowork on its prompt — the standing hub; this conversation retires). (3) **Dependabot — day 7+, re-nagged on your own push: the 60-second triage.** (4) The §1 decision batch arrives from v18 in one turn — the four moat-wiring sub-decisions + hashed-TCLK + classifier cause-walk; rule before silicon. (5) Standing: telemetry disable · website.yml · LICENSE-flip · frontend.yml `cf3733e` · Wave-2 watch · Node-20 · the inert docs lock-file rm.

**Hub next = v18's (this hub's final act is this record).** The v17 arc, banked: 5 beats · 3 repos advanced (core `6ea6912`→`c856eab`; docs `78e0c5c`→`01602fa` incl. the Doc-18 LOCK; hivemind `036e2aa`→`e55378b`+this) · Doc 18 LOCKED + §52 minted (174/52, watermark-neutral) · M9.3 delivered/audited/landed CI-green with ZERO audit defects · the moat schema realized in source · two environment root-causes found and fixed (the corrupt-index escalation; the CC deny-beats-allow) · the adversarial culture proven profitable (F-1/F-2/F-3 caught pre-wiring, at desk price, not bench price).

## 2026-07-04 (v17 beat 4) — M9.3 LANDED `c856eab` + CI GREEN (gate of record); the M9.4 charter direction SET (the thesis run); the M9-arc adversarial-review lane AUTHORED; v18 recommended for M9.4 authoring

**Beat-3 landed everywhere exactly as staged:** core **`c856eab`** (75 files, 10811 insertions) + **CI GREEN on the push in 3 m** · docs `01602fa` (1) · hivemind `090ac16` (6). M9.3 backlog row → ✅ DONE. Zero deviations across all three counts. **Dependabot re-nagged on the push: 1 critical/1 high/3 moderate — day 7.**

**The strategic deliberation (Nick, beat 4 — the frame is his; the verification + additions the hub's):** M9.4 = the first time the THESIS runs, not just another milestone. **Nick's load-bearing find CONFIRMED in source and SHARPENED:** `Confirmability`/`ConfirmationCharacterization` referenced NOWHERE outside integration-zigbee (grep-clean) — the moat schema is source-realized but engine-unwired — BUT the wiring channel partially exists: `command_issued.confirmationTimeoutMs` (executor resolves `CommandDefinition.defaultTimeout` → config fallback) + the ledger's `ConfirmationMode.DISABLED` OPTIMISTIC bypass (never-tracked ⇒ never-CONFIRMED — the §51 read-path half, already structural). The gap = the per-DEVICE tuning path (Doc 02 §3.8's override concept, never wired) + the UNCONFIRMABLE/BEST_EFFORT semantics. **The M9.4 §1-equivalent (freeze deliberately at TOP, the M9.3 pattern):** candidate (i, leading) — adoption installs per-device-tuned `ConfirmationInstance` overrides from the profile's `confirmation[]` (zero new event surface; protocol-agnostic by construction; UNCONFIRMABLE → DISABLED-with-recorded-reason per the AMD-95 slot; BEST_EFFORT → `recommendedTimeoutMs`); candidate (ii) — the R3 richer-payload directive event. Ground at authoring; it is the next one-way door. **The prove-list grew from Nick's three seams to FIVE** (full detail: backlog M9.4 row): + **replay purity with the real CommandHandler registered** (ghost-commands-on-restart is the catastrophic class) + **restart/lifecycle honesty** (ReopenAction ordering · NETWORK_UP await · classifier-wrap trap) — with the identity join (entity→IEEE→nwk at dispatch; cache `matchedProfileId` lookup) named inside the round-trip seam. **Sequencing ruled by Nick, hub concurs fully: prove the loop hardware-free FIRST** (composition-root replay: SNZB fixture → real ingestion → real automation engine → dispatch → real adapter code over the FakeNcp channel → Hue fixture window → honest CONFIRMED; `occupancy.occupied` binding ASSERTED), **then the radio** — a mismatch must be a failing assertion, never a Pi-and-sniffer session. P1 sizing watch: M9.4a/M9.4b split decided at authoring.

**The adversarial-review lane AUTHORED (Nick's call — hub strongly concurs; the M9.2 post-green hardening precedent):** `context/handoff/2026-07-04_M9-arc_independent-adversarial-review_session_prompt.md` — fresh Cowork, READ-ONLY, baseline `c856eab`; hunts the classes green gates cannot see (logic/concurrency/protocol-semantics/spec-divergence/scalability traps), evidence-required (concrete failing scenario or it's a NOTE), settled ground fenced (refutation-with-evidence welcome), ONE return file. **Launchable NOW, parallel with everything** — findings fold before/into M9.4, which touches the same seams.

**Nick, in order:** (1) hivemind beat-4 commit — **stages exactly 4 paths** (pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · the adversarial-lane prompt NEW), message `_scratch/2026-07-04_hivemind_v17-beat-4_commit-msg.txt` → push. (2) **Launch the adversarial-review lane** (fresh Cowork on its prompt) — anytime. (3) **Dependabot — day 7, re-nagged on your own push: the 60-second Security-tab triage.** Hub concurs with your own framing: for a trust product this is a credibility liability, the cheapest trust-preserving action on the board — stop deferring it. (4) **The two in-window rulings M9.4 consumes: hashed-TCLK election + classifier cause-walk-for-PIE-only** — rule BEFORE the silicon run (the classifier-wrap trap is exactly what a mismatched network exercises; don't discover it in a backoff loop). (5) **Launch v18 on your word** — this hub is at depth after three full arcs (launch+Lock · M9.3 author+dispatch · audit+P2+strategy); M9.4 authoring is the heaviest remaining class and deserves fresh context. v18's charge: the M9.4 §1 grounding + instruction (per the backlog-row charter direction) → CC dispatch → audit → bench → soak; the explainability-UX frontend lane sequences JUST BEHIND the moat wiring (it consumes real verdicts — mocked screens before the engine renders them is waste); W2-VAL synthetic legs ride the soak window. (6) Standing: telemetry disable · website.yml · LICENSE-flip · frontend.yml `cf3733e` · Wave-2 arrival · Node-20.

**Hub next (v18):** M9.4 §1 grounding-subagent (Doc 02 §3.8 + the ledger/executor confirmation path + `ConfirmationInstance` shape + adoption write-surface) → the moat-wiring AMD decision framed for Nick (or ruled if delegation covers it) → WU-M9.4 pre-verification → the instruction (the five-seam prove-list; hardware-free hero loop = the done-when; bench legs = Nick's gate) → dispatch on the still-proven CC allow-list → two-layer audit → bench acceptance → 72 h soak. The dossier-content website lane remains launchable (independent).

## 2026-07-04 (v17 beat 3) — M9.3 DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects); all five [REVIEW]s ruled; WUCP Phase 2 executed; Nick's full `check` GREEN pre-commit

**The lane (Claude Code, ~90 min, allow-list smoke held):** tests-first throughout; §1 landed as its own unit BEFORE the pipeline consumed it; every STOP gate re-verified at HEAD (two fired with consequences, both handled: `ConfirmationMode` exists in core → CONSUMED typed [no String, no mint, A2 stays +1]; A10's stub located at `WithinTolerance.evaluate()` itself, consumer `StandardPendingCommandLedger:365`). **Nick ran the FULL `./gradlew check` GREEN (153 tasks) on the working tree — the deferred gate is DISCHARGED pre-commit**; residual = core commit + push + ci.yml/install-smoke glance.

**Two-layer audit (3 read-only verification agents + hub direct checks): ACCEPT, ZERO defects.** Layer 1 highlights: the eight AMD-97 fields byte-match the amendment; all four enum constant-sets exact; the corpus transcription (zigbee-profiles.json + MeasuredCorpusValues) FIELD-BY-FIELD exact vs `nexsys-bench@5ceff3b`; the §J fixture assertions INDEPENDENTLY RECOMPUTED (18 SNZB edges; Kelvin 2237/4525/6494/6494/6536 from the fixture mireds — the ±1-mired drift rows pin nearest-rounding); precedence implementation = tier → source-rank (USER>RUNTIME>BUNDLED) → priority desc → profileId asc (deterministic total order, ReentrantLock); schemaVersion fail-closed-major/tolerant-minor; §G bound = 1024 drop-oldest; zero `Instant.now()` family anywhere new; zero-mint held (4 pre-existing constants consumed; no `@EventType` in zigbee); knobs untouched; visibility census 10 public §1 + 31 package-private exactly as instructed. Layer 2: the coder's baseline observation (hivemind moved past the pre-verification pin; core delta zero) TRUE and correctly handled; the completion-report cross-agent-note claim TRUE but the note was appended BELOW the `## Archived` separator (append-at-EOF miss — annotated in place, served this beat, ACTIVE: 0 stays accurate); the three coder-lessons entries confirmed present.

**Rulings (all five [REVIEW]s ACCEPT):** **R1 queue-scheduled retry** — §3.4 mandates the retry POLICY, not its threading; the clock-scheduled `PendingInterviewQueue` realization is superior (no sleeping threads, deterministic tests) and the lesson generalizes (coder-lessons 2026-07-04 #3). **R2 endpointless → ISE on the frozen surface** — `InterviewStatus` has no FAILED (A9) and `InterviewResult` pins non-empty endpoints, so the result is unconstructible; unchecked ISE follows the `resumeNetwork()` precedent, classifies TRANSIENT → supervisor backoff (the safe direction for a possibly-sleepy device); internal pipeline never hits it. If real silicon surfaces a genuinely-permanent endpointless class, route via a checked seam THEN (Wave-2 watch). **R3 4-field `DeviceDiscoveredEvent` + `"unknown"` sentinel** — zero-mint was the ruled expectation and held; the **richer discovery payload (endpoints/interview_status/matched_profile_id) is QUEUED as an event-model AMD candidate** for the M9.4/M10 window (when the REST discovery surface consumes it). **R4 `DeviceRegistry` ctor-injection** — not an `IntegrationContext` component (12-field record verified); constructor seam is clean; **M9.4 wires the instance** (AB-3 in-memory substrate at the composition root) — inheritance noted on the M9.4 row. **R5 `EzspProtocolTest` stub-assertion update** — the seam fill is the instruction's own Files-table item; sendZclFrame/topologyScan M9.4 stub assertions verified RETAINED. **All five [INFO]s accepted** (Files-table-governs on `properties` · in-tree attribute vocabulary · the 2.0–3.0 V battery band as documented chosen constant · dedup payload-scope = measured-truth-over-brief, the twins differ in TSN byte · full-stream Hue assertions). **Hub maintainability check (the one item no agent flagged): the TWO handler seams** — frozen public `ClusterHandler` (= the M9.4 `buildCommand` command-path seam, implementor-less by design, NOT dead) vs package-private `ZigbeeClusterHandler` (ingestion-side richer internal, the M7.4b pattern) — ruled SOUND; a MODULE_CONTEXT gotcha line added this beat so M9.4 never duplicates normalization or silently retires the public seam.

**P2 executed this beat:** Doc 08 §3.6 realization-currency note (docs, 1 path) · zigbee MODULE_CONTEXT +1 handler-duality gotcha (rides the core commit) · cross-agent M9.3 note annotated SERVED (location miss noted) · backlog M9.3 row → ✅ DELIVERED+AUDITED · beat records + restamps · three commit messages staged. Check-9 PASS stands (no skill edits); Check-8 ACTIVE: 0 accurate; drift check clean (snapshot/backlog/handoffs all restamped this beat).

**Nick, three commits, in order:** (1) **core — `git add -A` stages exactly 75 paths** (9 M + 66 new; arithmetic in the message), message `_scratch/2026-07-04_core_m93_commit-msg.txt` → push → **ci.yml + install-smoke glance on the pushed commit** (the gate of record). §10 audit: every path traces to the M9.3 Files table; no runtime state (`zigbee-devices.json` is NOT in the tree), no secrets (fixtures/profiles are bench captures), nothing from `_scratch/`. STOP if porcelain shows ≠75. (2) **docs — stages exactly 1 path** (`design/08-zigbee-adapter.md`), message `_scratch/2026-07-04_docs_doc08-m93-currency_commit-msg.txt` → push. (3) **hivemind beat-3 — stages exactly 6 paths** (pm-handoff · PROJECT_SNAPSHOT · phase-3-milestone-backlog · coder-handoff · coder-lessons · cross-agent-notes — the last three are the lane's closeout writes riding this beat), message `_scratch/2026-07-04_hivemind_v17-beat-3_commit-msg.txt` → push. (4) Standing: **Dependabot 5 (1 critical — day 7)** · **hashed-TCLK election + classifier cause-walk-for-PIE-only are NOW IN WINDOW** (M9.4 authoring consumes both — rule at leisure before or during) · telemetry disable · website.yml · LICENSE-flip · frontend.yml `cf3733e` · Wave-2 arrival watch · Node-20.

**Hub next:** on the core landing → **M9.4 JIT authoring** (adapter factory + composition-root wiring [R4's instance decision + W10 `registerIntegrationSchema` + config-key binding] + command dispatch over `ClusterHandler.buildCommand` + ReopenAction ordering [close→reopen→resetSession→startSession] + formation NETWORK_UP await + the confirmation-acceptance engine on the §51 fixture suite + hero E2E; inheritances: the beat-3 R2/R3/R4 notes + the wire-pin list [`0x34/0x45/0x60`; v14 lookup dialect verify-on-silicon] + checked-PIE seam rule + classifier-wrap trap) → Nick's live-bench acceptance (the beat-42 seven + DP-F re-pair + Q10 re-characterize + FE-7) → 72 h soak (NQ-6 during). W2-VAL synthetic-fixture de-risk slots into the soak window. **v18 authored the moment this hub nears depth.**

## 2026-07-03 (v17 beat 2) — M9.3 AUTHORED ISSUE-READY (instruction + WU-M9.3 pre-verification); dispatch gated on the CC smoke

**Beat-1 landed both repos exactly as staged:** docs **`fb91c23`** (6 paths — Doc 18 LOCKED, §52 minted, 174/52, watermark AMD-97) + hivemind **`f6315ee`** (10 entries, rename 100%), both pushed. Nick's host porcelains matched the staged counts precisely; the deviation count this beat is ZERO.

**M9.3 authored (grounding-subagent-before-authoring — two read-only survey agents over core/docs/bench + hub direct reads):** `context/instructions/2026-07-03_M9.3_interview-ingestion-profile-registry_coding-instruction.md` + `context/pre-verifications/WU-M9.3.md` (A1–A15), both at core `6ea6912` / docs `fb91c23` / bench `5ceff3b`. Shape: the **north star + doctrine in the preamble** (verbatim) → **§1 = the discrete Mode-2 AMD-correction at the TOP** (DP-1: `confirmation[]` 9→10 realizing RATIFIED AMD-97, the eight fields embedded VERBATIM, characterization tests from the measured Hue/SNZB corpus values; DP-2: sealed `MatchCriteria` — ALL THREE variants defined [`Fingerprint(mfr, model, EndpointSignature[])` shaped from the corpus IR's `fingerprint[]`: profileId/deviceType/in+out clusters], matching implemented for two, §3.5(d) precedence; §C namespace convention on `profileId`) → dossier **§A–§K as settled constraints** → the Doc-08 §3.4–§3.7 pipeline contracts with every charter-measured truth (EP-11 Basic read · GP-242/unknown-cluster graceful skip · TSN+payload dedup per (device,endpoint,cluster) cleared on rejoin · Kelvin-at-ingestion raw-retained · IAS tolerate-not-require · ACK-lies read-back downgrade · re-apply-on-rejoin · sleepy queue 24 h) → adoption slice identity-UNGATED (DP-B; Doc 02 §3.12; A13 fences the core-side) → **checked-exception seam map** (interview failures = `InterviewStatus`, never new checked throws on the frozen surface; classifier-wrap trap carried) → P2 survey (ctor fan-out = ZERO existing sites, verified) → zero-mint pinned 71/41/53 → module-info diff = exactly ONE plain `requires com.fasterxml.jackson.databind` (interior-only, D-M92-1 pattern; validated against every mandated behavior) → **done-when = both bench fixtures replay green through real ingestion rendering recorded confirmability (§J)**. W2-VAL fence explicit. Cross-module touch: `WithinTolerance` evaluation (A10 STOP-gate; AMD-87 codec round-trip untouched). Backlog M9.3 row → ISSUE-READY.

**Layer-2 note (agent-survey correction, banked):** one grounding agent claimed "only 4 public types" in integration-zigbee — REFUTED against the MODULE_CONTEXT header (38 public Phase-2 + 21 package-private M9.2) + a direct `DeviceProfile.java` read; the pre-verification pins the corrected fact (A1). The attribution/verify-before-trust culture applied to our own subagents.

**Nick, in order:** (1) **hivemind beat-2 commit — stages exactly 5 paths** (3 modified: `context/handoff/pm-handoff.md` · `context/status/PROJECT_SNAPSHOT.md` · `context/planning/phase-3-milestone-backlog.md`; 2 new: the instruction + `WU-M9.3.md`), message `_scratch/2026-07-03_hivemind_v17-beat-2_commit-msg.txt` → push. Host porcelain is truth — anything beyond these 5, STOP. (2) **The CC gate, in this exact order:** edit `homesynapse-core/.claude/settings.json` — REMOVE `"Bash(./gradlew *)"` from deny (keep the git-commit/push/merge/reset/rebase denials; removing `"Bash(gradle *)"`/`"Bash(javac *)"` optional — gradlew is the only needed lane) and ADD `"Bash(./gradlew:*)"` to allow → RESTART the Claude Code session → **SMOKE: `./gradlew :integration:integration-zigbee:compileJava` runs GREEN inside CC** → only then paste the M9.3 launch line. (3) Standing: **Dependabot 5 (1 critical — day 6)** · ci/install-smoke glance `6ea6912` · telemetry disable · website.yml · LICENSE-flip · hashed-TCLK + classifier cause-walk (queued for the M9.3/M9.4 window — surface when the lane is in flight) · frontend.yml `cf3733e` · Wave-2 watch · Node-20 · `rm` the neutralized docs lock file at leisure.

**Hub next:** audit the M9.3 return on landing (two-layer vs the DIFF; R1-class pushback expected and welcome) → WUCP P2 → **M9.4 JIT** (adapter wiring + ReopenAction ordering + NETWORK_UP await + confirmation acceptance + hero E2E; §51 acceptance suite) → Nick's live-bench acceptance (the beat-42 seven + DP-F re-pair + Q10 re-characterize + FE-7) → 72 h soak (NQ-6 during). Dossier-content website lane still launchable anytime (independent). **v18 authored the moment this hub nears depth (capture-then-execute).**

## 2026-07-03 (v17 beat 1 — LAUNCH + THE LOCK) — Doc 18 LOCKED; EXT-INV-1/2 minted §52 (174/52, watermark stays AMD-97); the consolidated docs pass executed; Check 10 clears; CC allow-list ROOT CAUSE found

**Launch verification (Nick's directive: validate everything before dispatching):** restart-resume clean (no prior-writer signature; beat-65 = `036e2aa` landed + pushed). 11-check preflight = **STALE-benign on Check 10 ONLY** (cleared this beat), zero CONFLICTED. Baseline re-derived and CONFIRMED: core `6ea6912` clean · docs `78e0c5c` + the 7 fold edits held on disk (status line read host-side: DRAFT—REVIEW-FOLDED) · bench `5ceff3b` · skills `ae434ca` (mirrors byte-identical ×2). **172/51 mechanically re-counted from the §17 table** (171 pattern-rows + the AIOT row); watermark AMD-97 triple-sourced. `DeviceProfile` = 9 components + `findProfile(String, String)` **verified in source**. Beat-65 rulings read verbatim, inherited SETTLED. v16 prompt → `archive/`. Cross-agent ACTIVE 0 ✓.

**Environment findings (all banked):** (1) **CC allow-list ROOT CAUSE — core `.claude/settings.json` carries `"Bash(./gradlew *)"` (+ `gradle`/`javac`) in the DENY list, and deny beats allow in Claude Code** — this explains both prior failures even when the exact allow form was attempted. The fix (Nick, BEFORE M9.3 dispatch): **remove the gradlew deny entry** (keep the git-commit/push denials) **+ add `"Bash(./gradlew:*)"` to allow → restart the CC session → prove with the trivial targeted-compile SMOKE** (e.g. `./gradlew :integration:integration-zigbee:compileJava`). (2) Docs `index.lock` stranded by a **corrupt VM index view** (`error: cache entry has null sha1` during a `--no-optional-locks` diff) → mv'd aside per env-model §4 (`index.lock.cleared-by-cowork-2026-07-03`; git unblocked; Nick deletes at leisure); env-model §4 gains the corrupt-index escalation rule (this commit). (3) Mount staleness severe this session — hivemind worktree views matched NO recent commit; host file tools used throughout; attribution corollary applied (zero VM-porcelain claims trusted).

**THE LOCK (executed on Nick's in-session GO after the readiness report):** Doc 18 status → **LOCKED 2026-07-03** — OQ-1 ruled-N + N-2 folded into §6/§7 (OQ-1 marked RULED; review-gates line flipped to EXECUTED; the stale *"formal AMD lands with this pass"* line reconciled: the direction's formal home is the Locked doc, the mechanism AMD rides Doc 16 OQ2 — the OQ-2 class, watermark-neutral). Register: **§52 `EXT` minted — EXT-INV-1/2** with the B-1-completed parent sets (INV-CE-05/INV-SE-04 fourth parents), **the N-2 enforcement-arrival deferral INLINE in the EXT-INV-2 entry** ("second half becomes CI-testable at ladder rung 2" — named + scheduled, pointing at the EXT-LADDER rows), §0.3 row + prose, §17 +2 rows, §18 +1 row, regeneration line → **174/52; the amendment watermark stays AMD-97**. Nav-index +Doc-18 row. **The docs pass, same commits:** Doc 05 §4.1 DP-B one-liner · Doc 08 §3.2 W6 + §3.3 R4 + §3.3 DP-C + §3.8 DP-D + §3.13 R5 · Doc 15 §3.4 A4.5 seam note · Connect reword (repository free forever; Connect sells the verified CHANNEL — *monetize trust, not access*) · D-4/REC-180 → Six_Battlefields battlefield 2 · FE-1b dist-finding CLOSED-REFUTED annotation at the source return · **EXT-LADDER (assessment R-4) backlog row created, carrying F-1** · rationale-index +Lock row, AX-7 fence CLOSED · **map REFRESHED → Check 10 clears; the preflight is full-PASS once this lands.**

**Nick, two commits (host-side, in order):** (1) **docs — stages exactly 6 paths** (`design/18-*` · `governance/Architecture_Invariants_v1.md` · `design/00-navigation-index.md` · `design/05-*` · `design/08-*` · `design/15-*`), message `_scratch/2026-07-03_docs_doc18-lock_commit-msg.txt` → push. **STOP if host porcelain shows anything beyond these 6** (any `website/` flag is the known phantom class — host porcelain is truth). (2) **hivemind — stages exactly 10 entries** (9 modified: pm-handoff · PROJECT_SNAPSHOT · decision-rationale-index · cowork-environment-model · phase-3-milestone-backlog · strategic-context-map · strategy/Revenue_Model · strategy/Six_Battlefields · audits/…FE-1b_return.md; +1 rename: the v16 prompt → `archive/` — may display pre-stage as a delete + untracked pair), message `_scratch/2026-07-03_hivemind_v17-beat-1_commit-msg.txt` → push. (3) `rm homesynapse-core-docs/.git/index.lock.cleared-by-cowork-2026-07-03` at leisure. (4) **Standing: Dependabot 5 (1 critical — day 6)** · ci/install-smoke glance `6ea6912` · **the CC settings fix per the root cause above + restart + SMOKE** · telemetry disable · website.yml · LICENSE-flip · hashed-TCLK (M9.4) · classifier cause-walk (M9.3/M9.4) · frontend.yml `cf3733e` · Wave-2 watch · Node-20.

**Hub next:** on the Lock landing → **M9.3 JIT authoring** (grounding-subagent at issue-time HEAD → `context/pre-verifications/WU-M9.3.md` [BOTH graph knobs; integration-zigbee `module-info.java` VERBATIM; the 9-component ctor + every §A/§B touched type + the `new DeviceProfile(` fan-out list] → the instruction: the discrete AMD-97-realization Mode-2 AMD at the TOP [eight fields verbatim; tests from measured Hue/SNZB values] + dossier §A–§K numbered + DP-1/2/3 + the doctrine + the north star in the preamble + inheritances [checked-PIE seam rule · `WithinTolerance` · `drainPendingCallbacks()` + §G bound-and-drain · `adapter_type` binding · §3.5 namespace · §4c Clock reminder] + done-when = Wave-1 fixtures replay green) → dispatch ONLY after the CC smoke proves the allow-list → M9.4 → bench acceptance → 72 h soak. Audit the dossier-content website lane return when it lands (two-layer, attribution corollary).

## Rotated session records

**Rotated 2026-07-04 (v19 hub, the currency pass §2A; git-object-sourced from hivemind HEAD `68ccbf6`):** beat sections v16-beat-65 -> v13-beat-44 (22 sections: v16 61-65 · v15 57-60 · v14 48-56 · v13 44-47) -> `archive/pm-handoff-beats-rotated-2026-07-04.md`. Kept: v18 beats 1-6 + v17 beats 1-6 + Open Risks. Full history in git.

**Rotated 2026-07-02 (v14 hub first-act — the deferred beat-block rotation, phantom-safe file-tools-only method):** beat sections 5→43 (33 sections; beat 18 was never written to this file) -> `archive/pm-handoff-beats-rotated-2026-07-02.md`. Kept: beats 44–47 + Open Risks (reconciled). Full history in git.

**Rotated 2026-06-22 (v5-hub first-act hygiene; git-object-sourced from HEAD `04c0a54`):** the stale HTML-masthead reconciliation prose (2026-06-13 / -14 / -18 / -18 / -19) + the beats older than beat-5 (beat-4 v4-launch, beat-3 v4-handoff, beat-2 explainability, plus the v3-launch / M7.2a-1-landing / v2-parallel-lanes / Doc-16-Lock / AB-1+AB-2 entries) -> `archive/pm-handoff-rotated-2026-06-22.md`. Kept: beat-6 + beat-5 + Open Risks. Full history in git.

**Rotated 2026-06-12 evening (Pi-evidence closeout hygiene; git-object-sourced from `58794d7`):** the ratification record (Return ASSESSED → RATIFIED → mechanics), the superseded fold-abort record, the M7-AMD-block record, the M6.4-WUCP record + the M6.2-era fragment, and the RESOLVED/CLOSED build-gate blocks from Open Risks → `archive/pm-handoff-2026-06.md`. Earlier rotations per the markers preserved there. Full history in git.

## Open Risks

#### OR-GATE-M7.5c-a — deferred build gate on the M7.5c-a working tree — [✅ RESOLVED 2026-07-02, beat 50] (NEW 2026-07-02, beat 49)
- **RESOLVED (beat 50):** round 2 GREEN — 53 targeted + 149 full-`check` tasks on Nick's terminal → core committed **`e3d7296`** (10 paths exact) → pushed → **ci.yml ✅ + install-smoke ✅** on the push. M9.1 routing UNBLOCKED (§4b clear). Total: ONE fix round (2 test-compile errors, hub-fixed — within the P5 one-round target for compile-class defects).
- **Milestone:** M7.5c-a (internal-read envelope conformance). **Commit:** pending — working tree on core `2786526`; change set = exactly 10 files (7 source/test + 3 MODULE_CONTEXT).
- **Commands (Nick):** `./gradlew :api:rest-api:compileJava :lifecycle:lifecycle:compileJava` → `./gradlew :api:rest-api:test :lifecycle:lifecycle:test` → `./gradlew check` (count pins stay **71/41/53**; both module-infos + every `build.gradle.kts` unchanged) → `./gradlew :testing:integration-tests:check` (watch `EndpointE2eIT` — DLQ paths moved under `$.data`). `-Werror` rides the convention plugin — do NOT pass it as a CLI flag.
- **Round 1 (2026-07-02):** targeted `compileJava` GREEN + `:testing:integration-tests:check` GREEN + host porcelain exactly 10 ✓; `compileTestJava` FAILED — 2 AssertJ varargs-capture errors (`DlqStatusEndpointTest` L75/L135: `containsExactly` on a `List<?>` actual). **Hub-applied 2-line fix** (SendMessage-to-lane unavailable; mechanical class): the `@SuppressWarnings("unchecked")` typed-local idiom per conforming `ListEntitiesEndpointTest` L83–88. Lesson appended to coder-lessons. **Re-run pending:** `:api:rest-api:test :lifecycle:lifecycle:test` → `check`.
- **Owner:** Nick. **Deferred Build Gate:** YES — the lane's sandbox attempt failed at `build-logic` (mount delete + VM JDK 11 vs 21); evidenced in coder-handoff, never silently skipped.
- **Resolution condition:** `check` green → core commit (staged message, exactly 10 paths) → push → `ci.yml` green on the pushed commit (the gate of record). On resolution: M9.1 routes to the next Coder lane; FE-1b launches on the landing.

#### OR-M7-WIRING — Command-pipeline live wiring — [✅ RESOLVED 2026-06-27 — live-wired at M7.4b/c] (formalized 2026-06-25; NEW in beat 7, 2026-06-22)
- **RESOLVED 2026-06-27 (M7.4b producer + M7.4c confirmation/ledger; v8 beats 17/19; reconciled by the v9 hub).** The command pipeline is LIVE on the real composition root: trigger → run → `command_issued` → co-located dispatch → `command_dispatched` → (synthetic) `state_reported` → `state_confirmed`, with the live `pending_command_ledger` + `pollExpirations()` tick and paired teardown. The resolution condition's **E2E composition-root confirm gate is met** — `RunPipelineConfirmWiringTest` (host-verified present in `lifecycle/lifecycle/src/test`).
- **Residual → M7.4d (the only remaining M7.4-arc item):** the resolution condition's *second* clause — the **D2 pure-function-replay CI test wired as a gate** (+ registering D2's canonical INV-id) — is NOT yet landed. The *wiring* is resolved; the *replay-safety CI gate* is the M7.4d hardening.
- **Severity:** RESOLVED (was MEDIUM). **Owner:** PM/Coder — done. _See OR-GATE-M7.4 below for the gate-of-record confirm on `db8ab5f`._

#### OR-GATE-M7.4 — gate-of-record (CI) on the pushed `4e31aed` (M7.4d HEAD) — [✅ RESOLVED 2026-07-01 — ci.yml VERIFIED GREEN on core 52824e9 (beat 43)] (NEW 2026-06-27, v9 hub; updated v10 beat-25)
- **RESOLVED 2026-07-01 (v13 hub, beat 43; reconciled into this ledger by the v14 hub):** `ci.yml` GREEN on pushed core `52824e9` — screenshot-verified (Build & Check 3m5s, Gradle 8.8, `check` ✅). Subsumes the per-commit CI-confirm for the whole ancestor chain (`4e31aed`/`9ec5949`/`eed477e`/AB-4/M7.5a/b). The `frontend.yml` first-trigger glance on `52824e9`+`2786526` is a standing Nick item (hub tracks), not an OR.
- **Status:** NARROWED. The **local `./gradlew check` is CONFIRMED GREEN** on the M7.4c working tree that became `db8ab5f` (Nick's terminal 2026-06-27: BUILD SUCCESSFUL, 149 tasks; he then committed + pushed core `69cc6e2..db8ab5f`). So the M7.4b/c/**d** deferred build gates are all **resolved locally** (Nick's terminals: `./gradlew check` 149 tasks green on the M7.4c tree → `db8ab5f`, and again on the M7.4d tree). The only residual is the **gate-of-record** formality: the `ci.yml` GitHub Actions run green on the pushed HEAD **`4e31aed`** (the M7.4d commit has now landed; `db8ab5f`→`4e31aed`). **De-risked (v10 beat-25):** `git ls-tree 4e31aed gradlew` = `100755` (executable) → the prior exit-126 failure mode is structurally cleared, and `ci.yml` runs `./gradlew check --no-daemon`; what remains is purely Nick confirming the Actions run is green on `4e31aed` — the standing b85e1ed/M7.4a pattern, "confirm CI green on the pushed commit, not just locally."
- **Severity:** LOW (the substantive gate — the full local `check` — is green; this is the CI-record formality).
- **Owner:** Nick (glance at the Actions tab for `db8ab5f`). **Resolution condition:** Nick confirms the `ci.yml` run is green on `db8ab5f` → record M7.4b/c CI-gated → close. M7.4d may be authored/routed now (the local gate clears §4b — done, ISSUE-READY beat 22).

#### OR-APP-AUTH — Unauthenticated composition-root HTTP surface (core-review C1 / B-H1) — [✅ RESOLVED-for-REST 2026-06-20] (NEW 2026-06-18)
- **RESOLVED 2026-06-20 (AB-1; `./gradlew check` GREEN 149; PM WUCP Phase 2 APPROVE):** the REST surface is authenticated — `StandardAuthMiddleware`/`StandardRateLimiter` wired via a catch-all `installAuth` over `/api/*`+`/internal/*` (auth-before-canonicalize, RFC 9457), loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`. INV-SE-02 now has enforcing code + tests for the REST interface. **Carried forward:** WS first-message auth is an acceptance gate on the **new WS Phase-3 runtime + auth WU** (`api.ws` was Phase-2 scaffold — no live WS surface today; the shared `OpaqueTokenStore`/`AuthMiddleware` are built for it).
- **Status (2026-06-18):** OPEN — surfaced by the 2026-06-15 core-review (source-verified). The composition root (`lifecycle/.../HomeSynapseCore.java:408-435`) installs only `RestFilters.installReadinessGate` and calls `app.start(config.httpPort())` with **no `.host()`, no auth, no rate-limit**; `/internal/*` admin endpoints sit outside even the readiness gate. **INV-SE-02 ("auth mandatory on every external interface; no local-trust exception") has no enforcing implementation** — `AuthMiddleware`/`RateLimiter` are zero-impl. Latent only because `main()` is a stub.
- **Severity:** HIGH (security). The moment app-bootstrap wires `main()`, this is a live unauthenticated surface.
- **Owner:** Nick (bind-posture decision: loopback-default vs all-interfaces — feeds the CC-1 spike) / PM (coding instruction) / Coder (implement at app-bootstrap). **Resolution condition:** the app-bootstrap milestone ships `AuthMiddleware before("/api/*")` + `RateLimiter` + a decided bind posture, with auth-before-network-exposure tracked as a gate. Closed when INV-SE-02 has enforcing code + tests and app-bootstrap lands GREEN.

#### OR-RF-DECRYPT — Read-path decrypt failure aborts the whole replay (core-review C2 / F-A1) — [✅ RESOLVED-at-MVP 2026-06-20] (NEW 2026-06-18)
- **RESOLVED-at-MVP 2026-06-20 (AB-2; GREEN; PM WUCP Phase 2 APPROVE):** the read path now fails **closed** with a distinct typed `PayloadDecryptionException` (per-read-batch scope, CASE-a key-absent / CASE-b GCM-auth) — the A3-ruled MVP posture; no more raw untyped whole-replay abort. **Carried forward:** the graceful per-row degrade *behavior* (`DegradedEvent`) is designed-not-wired (F4) and ships with the **crypto-shred WU** (gated on `chain_hash` liveness, ZERO today).
- **Status (2026-06-18):** OPEN — source-verified at the core-review (`core/persistence/.../SqliteEventStore.java:804-810` + `:896-913`). `fromRow` routes any `dek_ref`-bearing row through `decryptStoredPayload`, which **throws** on missing cipher, malformed `dek_ref`, GCM auth-fail, or a destroyed key. **No per-row catch → one bad row fails the whole `readRows` batch** — deliberately unlike codec parse failures, which degrade to `DegradedEvent` so the read continues. Consequences: a lost/corrupt `.root-key` → the **entire store is unreplayable** (vs INV-RF-04); when crypto-shred lands, replay across a shredded event throws (vs INV-PD-07). The missing-cipher-on-read and shredded-key-on-read paths have **no test**.
- **Severity:** HIGH (correctness/security, forward). Inert until `main()` activates `payloadCipher`; then it becomes a confidentiality-vs-availability contract gap that also blocks the crypto-shred differentiator.
- **Owner:** Nick (the confidentiality-vs-availability design tradeoff) / PM (coding instruction) / Coder (crypto-shred WU). **Resolution condition:** decide the contract **before** crypto-shred is built — most likely catch decrypt failure in `fromRow` and emit `DegradedEvent` for the shredded/undecryptable case, keeping a hard fail only for "cipher entirely unwired but encrypted rows exist"; add tests for both read-side failure modes. Interlocks with OR-M6-NONCE restore-half (R-α) + the key-portability brief. Carry under the app-bootstrap lane.

#### OR-M6-NONCE — Counter-nonce crash/restore durability — [CLOSED 2026-06-19 — crash-half M6.3, restore-half AMD-94] (NEW 2026-06-07; carried from Doc 15 §6 at Lock)
- **Status (2026-06-19 — CLOSED):** both halves discharged. **Crash-half** = M6.3 durable, fsync-ahead-of-return per-scope counter (GREEN `1eddd9a`). **Restore-half** = **AMD-94 RATIFIED + folded into Locked Doc 15 2026-06-19** (rotate-DEK-on-restore = additive new DEK version, retain priors; carry-high-water-mark demoted to a defense-in-depth cross-check; the fail-closed boot invariant REC-235). Per the ratified DOCS-review edit **E-1**, the boot invariant is discharged after a restore **only** by installing a fresh DEK version — rotation is the restore-completion gate; the counter-proven-≥-all-priors branch is the crash-recovery path (sound because the M6.3 counter is durable), and must not clear a restore (a restore can roll the counter back below a used nonce and erase the true-high-water evidence). AMD-94-INV-01 (`Architecture_Invariants_v1.md` §48) discharges the AMD-86-INV-01 (§35) restore-half corollary. The §6 `[BLOCKING-for-M6-impl]` marker is resolved. Mechanics ship at the future backup/restore WU; the boot invariant rides app-bootstrap AB-2. _Prior status history follows._
- **Status (2026-06-13 — M6.3 landed GREEN, PM WUCP Phase 2 APPROVE):** the write-path half (a) is **DISCHARGED** — M6.3 ships the config-side durable per-scope nonce counter (`scope_nonce_counters.json`, fsync-ahead-of-return inside the `ScopeKeyManager` lock, re-init from the persisted max on boot — never from memory), and the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test (`AtRestEncryptionWritePathTest.killMidEncryptThenRestart_nonceNeverRepeats`) is GREEN in the full `./gradlew check` (147 tasks). The restore half (b) **remains OPEN**, re-homed to side-research **R-α** (crash-safe backup/restore for an event-sourced + per-scope-encrypted store): M6.3 only "does not preclude" DEK-rotation-on-restore (instruction DP-D); the full backup/restore co-design (foundation-readiness F3) carries the remaining obligation.
- **Severity:** HIGH for the M6 at-rest-encryption implementation (a cryptographic-correctness gate — catastrophic if missed). NOT a blocker for Doc 15's Lock: the review (J6/J7) correctly classified it as blocking the M6 *implementation*, not the *design*. Logged here so it is not lost now that Doc 15 is Locked (the explicit M6 carry the brief required).
- **Detail:** Doc 15 §3.4 mandates counter-based deterministic 96-bit GCM nonces per scope (never random — to avoid the GCM birthday bound). If the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK, then (key, nonce) reuse **breaks AES-256-GCM confidentiality AND authentication** for that scope (the catastrophic GCM nonce-reuse failure). Documented in Doc 15 §6 (Failure Modes — "Counter-nonce reuse across crash/restore"), §13.4 (Testing Strategy), and the §16 summary; it is the AMD-86-INV-01 corollary registered in `Architecture_Invariants_v1.md` §35.
- **Owner:** PM (embed verbatim in the M6 coding instruction) / Coder (implement at M6). **Resolution condition:** the M6 at-rest-encryption WU (a) persists the per-scope counter high-water mark atomically with (or ahead of) the encrypted write and re-inits from the persisted max on boot — never from memory; and (b) co-designs the deferred backup/restore feature (foundation-readiness F3) so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). Closed when the M6 impl ships with the §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.

#### Deferred build gates — ALL RESOLVED (2026-05-31)
- **M4.B-S1 (AMD-44 Stage 1)** deferred gate **RESOLVED** — Nick ran `:platform:platform-api:check` (38 tasks) + `:core:device-model:check` (26) + full `./gradlew check` (143, incl. ArchUnit + spotless) GREEN against the M4.B-S1 working tree and committed `e73e199`. PM WUCP Phase 2 → APPROVE.
- **M4.0b-5 (AMD-53)** deferred gate **RESOLVED** — GREEN (58 + 143 tasks), committed `c99b425` (the coder-handoff entry was lagging at "DEFERRED"; flipped to RESOLVED this session).
- **M4.0b-4b (AMD-52)** deferred gate **RESOLVED** — Nick ran `./gradlew check` (143 tasks, incl. ArchUnit + spotless) + the targeted set (`:core:event-model` `:core:value-model` `:core:device-model` `:core:state-store` `:core:persistence` `:lifecycle:lifecycle` `:app:homesynapse-app`, 92 tasks) GREEN against `72596cb` and committed it.
- **M4.0b-4a (relocation)** deferred gate **RESOLVED** — `./gradlew check` GREEN (143 tasks), committed `971cfa1`.
- **M4.0b-3 (AMD-51)** deferred gate **RESOLVED** — Nick ran `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` + full `./gradlew check` (139 tasks) GREEN and committed `98f705b`. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion (the 30-min `Pi4D1SpikeIT` soak is a long-running test, not a gate failure; it is excluded from the default `check`).
- **M5-A (platform-systemd impls + FloorId reg)** deferred gate **RESOLVED 2026-06-07** — after the `-Werror [exports]` gate-fix round, Nick ran `./scripts/clean.sh && ./gradlew :platform:platform-systemd:check :core:persistence:check :platform:platform-api:check` (56 tasks) + full `./gradlew check` (147 tasks, incl. ArchUnit + spotless + moduleGraphAssert) **GREEN** against the M5-A working tree. Commit pending (Nick host-git). PM WUCP Phase 2 → this closeout. **No open deferred build gates.**

#### Resolved gates ledger — rotated
- All M5-A/M6.1/M6.1a/M6.2/M6.4 deferred-gate records RESOLVED/CLOSED (see the 'Deferred build gates — ALL RESOLVED' block above for the M5-era ledger); the per-gate detail blocks live in `archive/pm-handoff-2026-06.md` + git ≤`58794d7`. **No open deferred build gates as of 2026-06-12.**
