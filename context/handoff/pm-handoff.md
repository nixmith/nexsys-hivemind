<!--
file: context/handoff/pm-handoff.md
purpose: PM session continuity — current task, work-unit status, open risks, outstanding instructions.
audience: PM, Coder
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-09-19 (v77 beat 1 — THE BOOT AT THE INSTRUMENT: the five HEADs = the dispatch line, its two placeholders filled by the instrument (hivemind `ad273c2` = the b8 order, 8 = 6 M + 2 A; bench `fa01cad` = the 1b table, 4 M); porcelain 0 ×5, ahead 0 ×5; the preflight 12/12 after Check 12's reconciliation (three landed charters marked EXECUTED); the boot read-set 44,961 B; the paste = the filed v77 text at the bytes (marked PASTED); the spine rotated (v75 b5–b8 out); the window's ONE deliverable = THE THURSDAY ORDER step 0 on Sunday's two intakes CLOSED; Sat 2026-09-19 ~08:0x CT (instrument 2026-09-19T13:09:00Z). Order: hivemind 10 = 8 M + 2 A by explicit paths (computed from porcelain inside the splice).) Prior: 2026-09-19 (v76 beat 8 — BENCH-METER-1b RETURNED AND INTAKEN TWO-LAYER, ACCEPT; THE WEEKEND DISPATCH: `764e537` + `90e3348` verified; the bench tree = the table (4 M + 0 A); the instruments re-run (36/0; 43/0; 13 legs; the banner `BENCH-METER-1b-2026-09-19-subtract-record @ 764e537`); the engine and Thursday's script read whole; the real file's walk a committed test; one lane claim corrected (the README carries no `field_within`); THE THURSDAY ORDER step 0 → Sunday after `R5B-2:`; the v77 text re-cut for a Saturday start (THE WEEKEND ORDER; PELTON-READY); v76 CLOSES at eight; Sat 2026-09-19 ~07:4x CT (instrument 2026-09-19T12:49:06Z). Order: bench 4 = 4 M by Nick's card; hivemind 8 = 6 M + 2 A by explicit paths (computed from porcelain inside the splice).) Prior: EVERY SEGMENT v55 b3 → v76 b7 ROTATED VERBATIM into archive/chains-rotated-2026-08-27.md (one heading per rotation = the COUNTED record: 143 rotations through v77 b1; the v76 b8 segment is the one Prior above); THE FULL PRE-2026-08-23 CHAIN (v55 b2 → v54 b10 segments + everything earlier) verbatim at archive/chains-pre-region-cap-2026-08-23.md; the pre-v55 chain at archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md.
-->

# PM Session Handoff

> **How to read this file:** beats are NEWEST-FIRST below; a fresh session reads the frontmatter `last-verified:` chain (one line, newest segment first) + the newest ONE beat block (THE CONTEXT-BUDGET LAW, the v67 prompt §1/§1b: chain ≤3,000 B · ≤12 live blocks · Open Risks OPEN-only); older blocks only when the newest sends you there. Older beats live in `archive/pm-handoff-beats-*-rotated-*.md` (the archive map at the tail). The `## Open Risks` section at the tail is standing. Only the hub writes here (prefix-prepend); the newest beat outranks everything below it.

## 2026-09-19 (v77 beat 1 — THE BOOT AT THE INSTRUMENT: the five HEADs = the dispatch line with its two placeholders filled by the instrument (hivemind `ad273c2` = the b8 order, 8 = 6 M + 2 A; bench `fa01cad` = the 1b table, 4 M; porcelain 0 ×5; ahead 0 ×5); the preflight 12/12 after one reconciliation (Check 9 28/28; Check 12 STALE on three landed charters → marked EXECUTED, bodies untouched); the boot read-set 44,961 B ≤ 45 KB (the plan §2 trimmed to its first 1,700 B); the paste = the filed v77 text at the bytes (the heading and the fence aside; the as-received copy under `_scratch/v77/`; the filed text marked PASTED); the spine rotated (v75 b5–b8 out; nine live); the window's ONE deliverable named — THE THURSDAY ORDER step 0 done on Sunday's two intakes CLOSED; Sat 2026-09-19 ~08:0x CT (instrument 2026-09-19T13:09:00Z))

**At the bytes:** `git show --name-status ad273c2` 2 A + 6 M; `fa01cad` 4 M; the 1b return's last line `RETURNED … 5780`; the splice lib md5 `d6780d13…`; the paste diff `1,2d0` + `17d14` only; `bootAndAdopt` at `BusSoakIT.java:144/:309`; `SCAN_BATCH = 500` at `StandardExplanationService.java:115`; `SOURCE_DEFAULT` at `RetentionPolicy.java:46`; one cite off — `ZigbeeAdoptionSlice.java:377–:378` is the classifier call, the `device=` line is re-derived at b2. **Not re-executed (disclosed):** CI #253's green (banked at v76 b6); the Pi's engine (`PI: pulled` is the instrument). **Filed:** `context/audits/2026-09-19_v77-b1_boot-and-intake_audit.md`; `archive/pm-handoff-beats-v75b5-v75b8-rotated-2026-09-19.md`. **Next act (Nick):** the b1 card (10 = 8 M + 2 A) → `HIVE: LANDED <sha>`; b2 = MEASURE-2b through the gate → the paste before 09:45 CT or after the sitting. ctx: b1 · calls ≈22 · read ≈80 KB · wrote ≈22 KB

**Leverage line:** both cards banked from porcelain (no words asked); the paste diffed identical (no second file); nine live blocks for the weekend.
**The alternative shape, rejected:** MEASURE-2b as the deliverable — Thursday hangs on the Pi and the intakes, not on a charter.
**2029 test:** why the deliverable is the Sunday close; why Check 12's three edits ride a boot beat.

## 2026-09-19 (v76 beat 8 — BENCH-METER-1b RETURNED AND INTAKEN TWO-LAYER, ACCEPT; THE WEEKEND DISPATCH: `764e537` + `90e3348` verified; the bench tree = the table (4 M + 0 A; constants and provenance untouched); the instruments RE-RUN (36/0; 43/0; 13 legs; banner `BENCH-METER-1b-2026-09-19-subtract-record @ 764e537`); the engine read whole (Decimal per operand; VOID before the value; `record` OPERATOR-only; `min:`; the witness recorded never asserted); the scenario read whole (22 entries; a LOAD STEP in all nine rep prompts; `operator` gone) — Thursday's script as the record wrote it; the real file's walk a committed test (8 WITHIN · 1 OUTSIDE · TR3 r1 3.755 %); one lane claim corrected (the README has no `field_within` at all); THE THURSDAY ORDER step 0 → SUNDAY after `R5B-2:`; the v77 text re-cut for a SATURDAY start (THE WEEKEND ORDER; PELTON-READY); the bench card cut; v76 CLOSES at eight, Sat 2026-09-19 ~07:5x CT (instrument 2026-09-19T12:49:06Z))

**At the bytes:** `engine.py:93/:106/:107/:116/:327/:366/:1084/:1616/:1710/:1744/:1755/:2213/:2250/:2253`; `metering-known-load.yaml:80 requires`, 22 `- name:`, 9 `THE LOAD STEP`, 9 `on_outside: record`; `test_engine.py:995/:1013/:1133/:1215/:1307/:1398/:1450`; `SCENARIO_FORMAT.md:120–:123`; `tools/runner/README.md` 0 matches. **Owned:** the charter's `requires:` cite off by three (`:75`); P2's wording. **Filed:** `context/audits/2026-09-19_v76-b8_BENCH-METER-1b_intake_two-layer-audit_and-the-weekend-dispatch.md`; the return staged; the v77 text; the plan §18; the brief. **Next act (Nick):** the bench card (4) → `BENCH: BM1B <sha>`; the b8 card (8) → `HIVE: LANDED <sha>`; the v77 paste before the sitting; then the rig. ctx: b8 · calls ≈18 · read ≈45 KB · wrote ≈32 KB

**Leverage line:** the lane's walk now ships as a test, so Thursday's script cannot drift from the record without a red selftest; the Pi pull moved to Sunday morning buys a second watched nightly without touching R-5B's night.
**The alternative shape, rejected:** opening v77 on Sunday — MEASURE-2b loses Saturday's coder hours and the knockout lane its night; the sitting costs the hub nothing to run beside.
**2029 test:** why the Pi pull is Sunday, not Saturday night; why the README row is a docs card, not a Thursday blocker; where PELTON-READY's three forms come from.

## 2026-09-18 (v76 beat 7 — BENCH-METER-1 INTAKEN TWO-LAYER, ACCEPT — THE HUB'S FIRST CUT CORRECTED ON NICK'S CRITIQUE BEFORE EITHER CARD LANDED (b7c): `11a684b` verified; the bench tree = the table (4 M + 2 A); the instruments RE-RUN (31/0; 43/0; 13 legs); "simultaneity by construction" WITHDRAWN (the engine orders the read; freshness is a load step per rep in the operator's hands); F-1 re-ruled (drop `operator`), F-2 split (`field_subtract` OFFSET · `reference_subtract` TARE; exact-decimal; VOID never REFUSED), F-3 kept with VOID, F-4 WITHDRAWN (`provenance:` is global); THE THURSDAY ORDER stated once (the Pi pulled Mon/Tue + one nightly → adoption → the whole re-mint, fleet counts and ULIDs, the variant/basis regenerated → the scenario in tmux, CHAR-BEFORE is the CHAR); BENCH-METER-1b re-chartered wholesale (a half-day); both cards re-issued, the trailer grep a GATE; the re-close, Fri 2026-09-18 ~20:5x CT (instrument 2026-09-19T01:56:18Z))

**At the bytes:** `engine.py:944` orders the read; nothing reads freshness (`EntityState.java:87` entity-level stamps; the TR3 lags ~1 min, DEVICE-SET `:25`); `:2036–:2040` a mid-run refusal returns BEFORE the bundle write (`:2050`); `:1419` `ulids:` is set equality; `:729` provenance is global; `test_engine.py:993/:998` T3 reads the LIVE flags; `:691` the walk was synthetic; `nightly.sh:172–:176` CONFIG-DRIFT on the carrier that `adopt_devices` edits (`ZigbeeIntegrationAdapter.java:146`); the Pi pulls (B2 return `:160`). **Owned:** a mechanism claimed unread; two rulings wrong, one withdrawn. **Filed:** the b7 audit, the 1b charter, the v77 text (all re-cut), REP-1's cell, the plan §17, pm-lessons. **Next act (Nick):** the bench card → `BENCH: BM1 <sha>`; the b7 card (11) → `HIVE: LANDED <sha>`; the 1b paste; Sunday the v77 paste. ctx: b7+b7c · calls ≈28 · read ≈60 KB · wrote ≈45 KB

**Leverage line:** Nick read the audit against the engine and the record before committing; the cards were re-issued with nothing landed — one evening, against a hardware day.
**The alternative shape, rejected:** landing b7 as cut and patching in v77 — a false mechanism claim in the record, a charter whose refusal loses the typed session, a Friday nightly red on the fleet counts.
**2029 test:** why freshness is a load step, not an engine property; why two subtractions with the side in the name; why `provenance:` never holds a plug ULID.

## 2026-09-18 (v76 beat 6 — THE v76 CLOSE: ENERGY-READ LANDED `d1c2cbc`, CI #253 GREEN at the instrument (every step ✓ → the counter 8/20; install-smoke #61 ✓) — THE ADOPTION FENCE LIFTS; `cfecdd2` verified; Phase 2 done; Nick's re-cut ADOPTED with one edit (MEASURE-2b a read-path measurement on a real store with the shared boot fixture; LINK-READ the second session; ENERGY-READ-b and the rig-blindness sweep riding) + the retention fact (7/90/365 days); BENCH-METER-1 chartered, DISPATCH-READY; the v77 dispatch text written; the deliverable's second half EXCEEDED, Sunday's intakes handed to v77; the close packet handed, Fri 2026-09-18 ~16:2x CT (instrument 2026-09-18T21:22:09Z))

**At the bytes:** core `d1c2cbc` = 22 at diff-tree (16 M + 6 A); hivemind `cfecdd2` = 13; both clean. The API (21:11Z): CI #253 (id 35394284061) success — every step ✓, `bus-soak-253` stored; install-smoke #61 both arches ✓. The bench's premise rows at `f3631cb` (the audit §1). `RetentionPolicy.SOURCE_DEFAULT = (7, 90, 365)`. **Ruled:** the rate leg closed; disk-fill retired; the read path at run-scale is the exposure — MEASURE-2b re-scoped; LINK-READ after it; the DIAGNOSTIC class purges at 7 days → VERIFY-72H exports at the run's end. **Filed:** `context/audits/2026-09-18_v76-b6_close_ENERGY-READ-green_bench-charter_audit.md`; the charter; the plan §17; the DR; `context/handoff/2026-09-18_v77_dispatch-text.md`. **The window:** six beats · ENERGY-READ chartered → GREEN in one day, a wire defect found on the way · two briefs attacked · THE MEASUREMENT RECORD minted · the counter 7 → 8. **Next act (Nick):** block 1 the bench paste; block 2 the b6 card → `HIVE: LANDED <sha>`; Sunday the v77 paste with `R5B-2:`. **Next WU (v77):** the intakes; MEASURE-2b. ctx: b6 · calls ≈16 · read ≈45 KB · wrote ≈35 KB

**Leverage line:** the slot came free five days early and the plan has no float — the read path at scale is the one question that otherwise costs a 72-hour run to answer, so it goes first.
**The alternative shape, rejected:** one lane for MEASURE-2b and LINK-READ — a charter against a fixture the same lane is building pins nothing; the second session costs one paste.
**2029 test:** where `d1c2cbc`'s green is recorded; why MEASURE-2b measures reads, not disk; what the retention default means for the export; why v76 closed at six.

## 2026-09-18 (v76 beat 5 — ENERGY-READ RETURNED AND INTAKEN TWO-LAYER, ACCEPT: the tree = the Files table (22 = 16 M + 6 A); the gate's 80 JUnit XMLs summed at the bytes (659 · 1 skipped · 0 · 0); every production diff read; a HEAD WIRE DEFECT the lane measured (the uint48 change field's `int` shift, mod 32) verified by the hub's own emulation — HEAD's 0x0702 row sent 2.1 × 10¹⁰, not 5, and the read-back folded the same way; MEASURE-2 ≈ 24,000 reports/s in memory (the rate leg closed; storage → the soak); six deviations ruled (R-5 → `ENERGY_METER`; ENERGY-READ-b rides LINK-READ; IR-32); five charter misses owned; `PROTECT: done` banked; the spine rotated (v75 b1–b4); the core card cut, Fri 2026-09-18 ~15:5x CT (instrument 2026-09-18T20:54:29Z))

**At the bytes:** `6384e64` = 10 at diff-tree; core porcelain 16 ` M` + 6 `??` under `integration/integration-zigbee/`; the main diff 8 M + 3 new (869+/55−); the XMLs' newest stamp 19:39:03Z; the `MEASURE2` tee line verbatim; the return 17,555 B, `RETURNED` last; the letters 114,222 B. The emulation: 5 → `05 00 00 00 05 00` at HEAD. The diffs (the audit §2): the read on the adoption drive, the cache on re-link, the sanitizing record, 8/9/10, the change capped at 0x7FFF, the DP-6 pin re-pinned in the open, T4's frame order as bytes. **Owned (the audit §4):** P1's arithmetic; the table without `ZigbeeAdoptionSlice.java`; the note-vs-`verifiedFact` collision; the `-D` line; two cites off by two. **Filed:** the audit `context/audits/2026-09-18_v76-b5_ENERGY-READ_intake_two-layer-audit.md`; the return staged; the letters under `context/research/`; the record's G4 cells, G4-4, M2-2b; IR-32; the coder-handoff entry; the lesson. **Next act (Nick):** block 1 the core card → `CORE: ER <sha>`; block 2 the b5 card → `HIVE: LANDED <sha>`; then `CI: <sha> green|red`. **Next (hub, b6):** the bench lane's charter. ctx: b5 · calls ≈20 · read ≈75 KB · wrote ≈40 KB

**Leverage line:** the lane's frame-order test caught a defect every read-back had verified — a read-back that decodes with the encoder's error verifies nothing; the assertion was on the bytes.
**The alternative shape, rejected:** landing R-1 and R-5 in this tree before the card — two files and a re-run for a log field and an entity type no fleet device exercises.
**2029 test:** where the uint48 defect is recorded and what it means for the flood arithmetic; why M2-2 is a floor; what ENERGY-READ-b carries.

## 2026-09-18 (v76 beat 4 — ENERGY-READ THROUGH THE PREMISE GATE, DISPATCH-READY: `7605dfc` verified; the charter (23 rows) — IR-15, IR-24 by clusters, the formatting READ at adoption and never predicted, handlers silent until the scale is known, IR-25 SIZED (1 W / 5–600 s; 5 Wh / 5–3600 s; < 10k run events against 155k–311k), MEASURE-2 — every premise row grepped at `3af6213`; THE MEASUREMENT RECORD minted; Nick's eight additions dated (§16: the watts go/no-go Sun 10-11 20:00 CT; VERIFY-72H's gates and invariants; the run's log indexed; the outreach from Fri 09-25); the packet handed, Fri 2026-09-18 ~13:2x CT (instrument 2026-09-18T18:28:07Z))

**At the bytes:** `7605dfc` = 10 at diff-tree (7 M + 3 A). Twenty-two premise rows grepped (the audit §1: `iasCapability` :175–:178; eight handlers, no metering; `forDevice`'s one caller `ZclIngestionUnit.java:731`; the classifier's one call site `ZigbeeAdoptionSlice.java:377`; `ReportingOps` without an attribute read; `readBasic`'s unicast + await; the codec's uint48 and no int24; the two raw rows :71–:72; the adoption drive :979–:1027; the cache's learned pattern; one fake, no shape test, no arch rule). The arithmetic checked. **Design:** the read rides the adoption drive, not the interview — an unadopted device is never asked; unknown formatting → nothing configured, nothing emitted; the raw beside every scaled value. **Filed:** the instruction; `context/audits/2026-09-18_v76-b4_ENERGY-READ_charter_premise-gate_audit.md`; `context/planning/measurement-record.md`; the plan §16. **Next act (Nick):** the packet — block 1 the dispatch paste; block 2 the b4 card → `HIVE: LANDED <sha>`; `ER: RETURNED <path> <bytes>` when the lane returns. **Next (hub, b5):** the bench lane's charter (CHAR; the two chains; T1 re-written; `field_within`; `link-quality.yaml`). ctx: b4 · calls ≈24 · read ≈70 KB · wrote ≈60 KB

**Leverage line:** the divisor is the one number the whole corroboration leg scales by, and the charter refuses to guess it — a handler that does not exist until the device has answered cannot publish a wrong watt.
**The alternative shape, rejected:** reading the formatting in the interview — it asks an unadopted device for numbers and touches the M9.3 freeze types; the adoption drive owns the reporting posture.
**2029 test:** why the thresholds are 1 W and 5 Wh; why a metering handler is absent rather than unscaled; where the Gen4's divisor is written when read.

## 2026-09-18 (v76 beat 3 — EXT-REVIEW-1 ADJUDICATED AGAINST THE RECORD: `c0158a1` verified; the 95-row packet read whole — the reviewer converged on RS-3 §6.1-1 as a wedge and left one artifact the record lacked (a pre-registered falsifiable comparison with a kill test in front); at `3af6213` E-1 EXISTS, E-2 ANSWERED, E-5's gap measured; X-6 verified at the HA docs; the seven rows ruled — the November pilot, the run as THE SOAK + THE RECORD'S HONESTY (VERIFY-72H), the source-cited why-not (IR-31), the vocabulary defined whole (VERDICT-VOCAB-1), the spoken-pitch clause, `HOTMOBILE: skip` stands; 09-24 is a Thursday; the v76 DR opened; the b3 card handed, Fri 2026-09-18 ~13:0x CT (instrument 2026-09-18T18:02:05Z))

**At the bytes:** `c0158a1` = 9 at diff-tree (7 M + 2 A), clean. The packet filed under `context/research/`. Greps at `3af6213`: `CausalContext(correlationId, causationId)` :15–:47; `StandardExplanationService` :47–:74 "purely by reading the immutable EventStore … replay-safe" (INV-SA-03); `NonFiringVerdict {CONDITION_NOT_MET, NEVER_TRIGGERED, ACTED_BUT_UNCONFIRMED, DISABLED, FIRED_CONFIRMED}`; `ConfirmationResult {CONFIRMED, NOT_YET, FAILED}` — a failed expectation ages out (:427–:445). The HA docs today: "The last 5 traces are recorded for all automations." **Refused:** the enterprise wedge; the comparative clause outward; the pilot before the run; HotMobile; product work beyond three register rows and one design doc. **Filed:** the audit `context/audits/2026-09-18_v76-b3_EXT-REVIEW-1_intake_adjudication_and_the-window-plan.md`; the DR `context/planning/2026-09-18_v76_decision-record.md`; the plan §15; the lesson. **Next act (Nick):** the b3 card → `HIVE: LANDED <sha>`. **Next (hub, b4):** ENERGY-READ through the gate, dispatched tonight; b5 the bench charter. ctx: b3 · calls ≈22 · read ≈95 KB · wrote ≈45 KB

**Leverage line:** the reviewer's deepest row (no explanation of a non-event without a declared expectation) was answered from the record — the platform declares one per device already, in the availability edge.
**The alternative shape, rejected:** letting the study apparatus into the window — seven rounds produced a method and no code; the method is kept verbatim, after the run.
**2029 test:** what EXT-REVIEW-1 changed (the pilot, the verifier, three register rows, the spoken clause) and what it did not (the window, HotMobile); where E-2's answer is.

## 2026-09-17 (v76 beat 2 — NICK'S BRIEF ATTACKED AT THE BYTES: `5bbbd14` verified; the words banked, D-v75-2/3/6/7/8 RULED; the calendar = the window's shape (Wed 09-24 operative); the P4460 band corrected to the guaranteed figure (3.03 %); CHAR accepted with four additions, three meters for CHAR only; the reporting-volume row — arithmetic holds, three mechanisms corrected, both folds accepted (IR-25 sized; IR-28), OR-METERING-VOLUME opened; the Monday read REFUSED → `GEN4-READ: rehearsal-2`; the b2 card handed, Thu 2026-09-17 ~21:1x CT (instrument 2026-09-18T02:15:55Z))

**At the bytes:** `5bbbd14` = 7 at diff-tree (5 M + 2 A), clean. Nick's brief read whole and filed beside the audit. Greps at `3af6213`: `ReportingConfigurator.java:71–:72` the two rows as read; `ClusterHandlers.forDevice` eight handlers, no metering; `adoptIfAccepted` :979–:996 — an unlisted IEEE is interviewed, proposed, never adopted, never configured; `driveReporting` :1009/:1015 only on adoption or re-link; the unhandled-cluster branch `ZclIngestionUnit.java:540`; `InterviewStateMachine.Step` :52–:57 — no metering read; `BusSoakIT` K=20, no rate dimension; no bench rate scenario. The dossier row 29: the TR3's seMetering divisor 3,600,000 (z2m) → the 0x0702 flood certain for one plug of three; the Gen4's unread. **Corrections to Nick:** the flood is post-adoption; the frames die at the no-handler branch; 0.5 % typical is not Class 0.5; a HEAD interview reads no metering attribute. **Filed:** the audit `context/audits/2026-09-17_v76-b2_nick-brief_hardware-calendar_attack_audit.md`; the brief `context/handoff/2026-09-17_v76-b2_nick-brief_hardware-calendar-and-window.md`; the plan §14; IR-25 re-cut, IR-28 added; OR-METERING-VOLUME. **Next act (Nick):** the b2 card → `HIVE: LANDED <sha>`; `PROTECT: done` chased. **Next (hub, b3):** ENERGY-READ through the gate, MEASURE-2 riding; the bench lane's charter. ctx: b2 · calls ≈24 · read ≈70 KB · wrote ≈30 KB

**Leverage line:** the premise Nick flagged held; the one he did not — what an interview reads — is what decided §5. Every claim was grepped, the certain ones included.
**The alternative shape, rejected:** absorbing the CHAR edit and the P4460 band as written — a verdict claimed at a typical figure is a claim the maker does not make.
**2029 test:** why the band is 3.03 %; why the Monday read was refused; what MEASURE-2 measures and why it rides the Java slot.

## 2026-09-17 (v76 beat 1 — THE BOOT AT THE INSTRUMENT: the five HEADs = the dispatch line; the preflight 12/12 (Check 9 28/28); the b8 packet's verdicts intaken — CI #252 on `3af6213` GREEN, every upload THROUGH (CI-2's never-gate clause unexercised; it stands by construction), `Run the bus soak` ✓ → the counter 7/20; run #56's artifacts through the weekend; the dispatch text filed verbatim; the deliverable named; ENERGY-READ moved to b2; the b1 card handed, Thu 2026-09-17 ~18:1x CT (instrument 2026-09-17T23:16:25Z))

**The deliverable (plan §13):** the sitting's two records intaken two-layer by Sunday night and ENERGY-READ DISPATCH-READY through THE PREMISE GATE. **At the bytes:** `date -u` 2026-09-17T23:02:39Z (~18:02 CT); the read budget ≈40.7 KB of 45 (the DR §3 at its headings); core `3af6213` · hivemind `08b8474` · skills `180375f` · bench `f3631cb` · docs `7221ddc`, porcelain 0, push 0, no lock; `08b8474` = 16 at diff-tree (11 M + 5 A), `3af6213` = 4 M — both cards as ordered, no trailer; the API from the device: CI #252 success — `Run check` ✓, `Upload test reports` ✓, `Run the bus soak` ✓ (`bus-soak-252` stored, 28,916 B); install-smoke #60 both arches ✓; run #56's two artifacts unexpired, to 09-21 ~12:00Z. **One correction to the dispatch line:** "CI green with the quota still hit" — the uploads succeeded at 21:54Z, so #252 proves the tree, not the never-gate clause; that clause is proven the first time an upload fails under it. **Not re-executed:** the sample's XML, run #56's sha256 (a token). **Filed:** `context/handoff/2026-09-17_v76_dispatch-text_verbatim.md` · `context/audits/2026-09-17_v76-b1_boot-and-intake_audit.md`. **Next act (Nick):** the b1 card → `HIVE: LANDED <sha>`. **Next WU (b2):** ENERGY-READ through the gate — IR-15 first; the six sources grepped at `3af6213`. ctx: b1 · calls ≈34 · read ≈80 KB · wrote ≈22 KB

**Leverage line:** the instrument was one unauthenticated API call from the device — the counter and the quota moved on a step's conclusion and an artifact record, not on a sentence in the dispatch.
**The alternative shape, rejected:** chartering ENERGY-READ inside b1 as the plan bundles it — the card would have waited behind an hour of source reads; a slot-releasing landing outranks a read-only dispatch (arc 52 ii).
**2029 test:** where #252's green and the seventh sample are recorded; why CI-2 is "by construction", not "proven"; why ENERGY-READ opened at b2.

## Open Risks

#### OR-NIGHTLY-0902-S31 — the bench floor breached: the 2026-09-02 nightly read 7/9 with FAIL command-confirm-s31 after four 8/9 nights — [OPEN 2026-09-03, v61 beat 10; source: E2, Nick's paste of `~/hs-bench/digests/nightly.log`]
- **The risk:** the floor fence (8/9) is the standing evidence that the certified bench build confirms commands on real hardware; a breach is either a repeat of the 08-02 intermittent (adjudicated once: CONFIRMATION_TIMED_OUT at ~5.1 s, the app alive, the device back a minute later) or a new mechanism. No theory is on record and none is allowed before the bundle read (arc-discipline 28).
- **The ordered act (read-only; the s31/nightly HANDS-OFF fence stands until R-5):** the evidence-read session `context/instructions/2026-09-03_evidence-read_s31-nightly-0902_session_prompt.md` — five hypotheses pre-filed (H-A device silence · H-B never dispatched · H-C late confirmation · H-D infrastructure collateral · H-E the 08-02 class repeats); returns at `context/audits/2026-09-03_s31-nightly-0902_evidence-read_return.md`; the hub audits two-layer at intake.
- **Rider:** the 2026-09-03 nightly MISSED (the held card in the Pi overnight; no bundle, no digest line) — a recorded miss, not a suite defect; the bench card is back (~06:10 CT 09-03) and tonight's run fires normally; its floor line banks at the next beat.
- **Owner:** Nick (launch) / hub (audit; a row for the R-5 charter if the class repeats, its own row if a new mechanism). **Closes when:** the return is audited and the class adjudicated, and the next nightly reads 8/9.
- **Status 2026-09-03 (v61 beat 11) — ADJUDICATED, rows pending.** The evidence read returned and audited ACCEPT (`context/audits/2026-09-03_intake_two-layer-audit_v61-beat-11_s31-read+PKG-SEC-2.md` Part A): H-D — transport collateral from the suite's own `usb-reenumeration` leg (port kill → NCP reset → reopen 6.4 s before the s31 command); the hub verified at source that `attemptReopen()` (:841–:874) calls `resumeStored()` without `awaitNetworkUp()` — the loop resumes before NETWORK_UP and nothing logs the unicast accept/reject. **The rider above is CORRECTED: the 09-03 nightly was NOT missed — it fired late (~11:12Z, a catch-up on the card's return) and PASSED 8/9; the floor is back.** Candidate rows for Nick's word (H10, v62): REOPEN-NETUP (Core) · S31-SETTLE-AFTER-REENUM (bench, at R-5). **Closes when** both rows are worded and the class re-reads 8/9 across the following nights.

#### OR-FAILCHAN — healthy outcomes reported through failure channels (a trust-erosion class) — [OPEN 2026-08-30, v58 beat 6; source: the R-3a hub-return §2.3]
- **The class — four instances in one day, plus the fixed KEYb sibling:** a graceful operator-requested stop exits 143 → systemd marks the unit `failed` (§6-B; root-correct fix = exit 0 on the caught-SIGTERM shutdown path — the record §6 mechanism note; a unit-side `SuccessExitStatus=143` would mask a genuine kill) · an orderly serial close emits `transport_failed` carrying F-S16’s "physical port loss" signature (§10-O) · `key_establishment_failed` for `0xFFFFFFFFFFFFFFFF` (§10-M — CAUSE OPEN; the emission is a faithful 0x009B relay, source-read v58 b6) · a device with no recorded report time renders `Current` (§10-I; read-path). **Never-false-ALIVE guards against claiming health that is not there; nothing guards against claiming failure that is not there — the second error costs operator trust as fast.**
- **Owner:** hub (the sweep WU charters at the September plan; §6-B is the first candidate) / Coder. **Closes when:** the sweep lands and each instance is re-classified or fixed, verified on a card.
- **Status 2026-09-03 (v62 beat 1) — a FIFTH instance, source-verified (the mirror of §6-B): EXITCODE-UNWIRED.** `app/homesynapse-app/src/main/java/com/homesynapse/app/ExitCode.java:25` defines `CONFIGURATION_FAILURE(10)` (+11 PERSISTENCE · 12 EVENT_BUS · 13 SUBSYSTEM_INIT_TIMEOUT · 99 UNEXPECTED) and nothing in main sources references it; `Main.main` is `throws Exception` (:63) with `manager.start()` unwrapped (:148); `HomeSynapseCore.start()` :441–:448 logs the fatal, tears down and re-throws → the JVM exits 1. The unit's `RestartPreventExitStatus=10` (`distribution/systemd/homesynapse.service:66`) therefore never matches and a deterministic bad config restart-loops (`RestartSec=10`) to `StartLimitBurst=5`/300 s — the loop the unit's own comment (:61–:63) says it prevents. Doc 12 C12-04 (:457) is half-built: the structured log line exists, the exit code does not. **Disposition:** rides the FAILCHAN charter (v62 Block 2) as its own classification row; H10 in the v62 brief §G-1 (rec (a): catch at `main`, map FATAL subsystem → `ExitCode`, `System.exit(code)` after teardown). Evidence: `context/audits/2026-09-03_v62-b1_boot-grounding_docs-and-strategy_validation.md` §1.1.
- **Status 2026-09-04 (v62 beat 2) — CHARTERED.** Row 6 RULED (a) by ADOPT-ALL-RECS (09-03) and EXITCODE RULED (a) the same evening; the FAILCHAN coding instruction authors at v62 beat 3 with the H10 reconciliation inside (the §6 note's app-side exit 0 vs the unit-line rec; `Restart=` ruling) and Nick's caveat as a constraint (`System.exit(code)` in `main` after `start()` threw and teardown ran — never in a shutdown hook; the exit behind a thin seam so the FATAL-subsystem → code mapping is unit-tested and the process exit is not). Dispatch: Nick's Coder lane tonight (one lane on the core tree). **Closes when** the sweep lands, CI green, each instance re-classified or fixed, and §6-B + EXITCODE verified on a card (R-4b's free rider if CI is green by Sat 08:00 CT, else the next card session).
- **Status 2026-09-04 (v62 beat 4) — THE SWEEP RETURNED + AUDITED ACCEPT-WITH-RULINGS (`context/audits/2026-09-04_FAILCHAN_intake_two-layer-audit_v62-beat-4.md`); LANDING PENDING Nick's commit + CI.** §6-B FIXED (`SuccessExitStatus=143` + `Restart=always` + the lint + run-smoke check 8) · instance 5 EXITCODE-UNWIRED FIXED (the report beside the throw; the exit in `main` behind the SIGTERM guard) · §10-O FIXED (both catches consult `stopSignal` before the watchdog; INFO `zigbee.transport_closed_orderly`) · §10-M KEEP-WITH-REASON (the status is `0x32 TC_REQUESTER_VERIFY_KEY_TIMEOUT`, the all-ones partner the null EUI64; the cause stays OPEN — the §6 line above's "0x009B" was the frame id) · §10-I ROUTED to §10-J. **Instance 6 (NEW, LOW, residual):** a `close()` landing between the loop check and the next `pumpInbound` reaches `requireOpen()`/`requireConnected()` → an `IllegalStateException` at `run()`'s boundary at stop time (not yet evidenced on a card; recorded in the zigbee MODULE_CONTEXT) — rides Row 7's WU as a rider. **Closes when** CI is green on the FAILCHAN commit AND a card's first `systemctl stop` reads `inactive`/`success` (R-4b today if guard 1 selects the artifact; else the next card session) AND instance 6 is classified in Row 7's WU.
- **Status 2026-09-04 (v62 beat 6) — LANDED ON MAIN `7af2d6c` (07:54 CT; 19 files exact); CI #225 RED** — `:lifecycle:lifecycle:test` 79/1 failed: `HeroLoopHardwareFreeIT` "timed out awaiting the dispatched On frame" (`:614`, the 10 s `awaitTrue` poll); local full-clean re-run GREEN; prior reds on `main` in the Actions history (CI #169/#183/#206). The FAILCHAN sweep itself is on `main`; **the deferred gate is OPEN on `7af2d6c`** (the skill §5 rule: no other core lane before the fix WU). Mechanism un-adjudicated at this beat — candidates: the loop parked unhealthy on the runner vs the dispatch never reaching the adapter; the next instrument is the class stdout's token census (the brief §A2); **FAILCHAN-FIX-1 authors on it.** The hardware proof of §6-B waits for the fixed commit's artifact (R-4b runs on `ef02d13`'s).
- **Status 2026-09-04 (v62 beat 7) — §6-B CONFIRMED ON A SECOND ARTIFACT:** R-4b's §9 clean stop on `ef02d13` read `Result=exit-code · ExecMainStatus=143 · ActiveState=failed` (20:25:45Z; the R-4b record §9) — the lie is artifact-independent, as the mechanism says. The FAILCHAN proof on hardware waits on FAILCHAN-FIX-1's green CI and the next card session.
- **Status 2026-09-04 (v62 beat 8, the close) — THE §A2 CENSUS READ; FAILCHAN-FIX-1 RULED INSTRUMENT-FIRST.** Nick's census of CI #225's class stdout: no `<N>` prefix (H-prefix refuted) · boot tokens ×5 = one boot per `@Test` (five methods: `:90 :165 :224 :269 :318`) · adoption tokens ×10 (two rig devices × five boots) · `zigbee.command_result` ×1 · `integration.route_join_miss` ×1 · the tail a clean boot then Javalin stopping ~120 ms after RUNNING. Four of the five methods await a sent frame and PASSED; only `timeoutHonesty_noReportNeverConfirms` (`:318`) lost its On frame (`:324`) after `command_issued(turn_on)` was found — H-dispatch ("never left the engine in four of five") is UNVERIFIED and per-class counts cannot decide it. **The fix WU: Part A = I-1 (the prior reds' identity, #169/#183/#206) · I-2 (the per-method XML split) · I-3 (20× under CPU pressure on `7af2d6c`) · I-4 (the §10-O hunk reverted in a scratch worktree); Part B on the branch — (a) pre-existing determinism defect → fix the wait's mechanism · (b) the guard → fix the guard, keep §10-O's intent · (c) CI-only → the fix branch's CI instrumented (`outputPerTestCase`).** Authors at v63 beat 1 on the census FILED to `context/audits/2026-09-04_CI-225_HeroLoopHardwareFreeIT_stdout-census.txt`. The gate stays OPEN; CG behind the green.
- **Status 2026-09-04 (v63 beat 1) — `main` RED A SECOND TIME ON IDENTICAL JAVA BYTES; FAILCHAN-FIX-1 RE-CUT TO THE CLASS AND AUTHORED.** `dc3328b`'s run (Nick's PR #5 merge; Java bytes = `7af2d6c`) failed `ReplayTransitionIT:212` in `core:event-bus` — a module FAILCHAN never touched — and never reached lifecycle; PR #5's run on the same bytes was GREEN (`8e6e0e1`'s parent is `7af2d6c`: I-0 answered). The §10-O hunk is unreachable in the rig's driven mode (`productionLoop()` never runs) — the guard-regression branch (b) is refuted at source for the adapter; the census token `route_join_miss ×1` is a skipped `command_issued` delivery on `integration_supervisor` in LIVE mode (`CommandRoutingSubscriber:181–:198`). The mechanism candidates and the instruments: the grounding audit §4 + the instruction (`context/instructions/2026-09-04_coder-lane_FAILCHAN-FIX-1_CI-nondeterminism_coding-instruction.md`; two commits, one lane; FIX-1b's push run clears). **The hardware proof of §6-B + EXITCODE rides the next card session on FIX-1b's artifact.**
- **Status 2026-09-05 (v63 beat 2) — I-1 READ (Nick, verbatim in the grounding audit §7-A): BRANCH (a) CONFIRMED FOR `HeroLoopHardwareFreeIT` BEFORE THE LOOPS.** #206 failed the hero-loop method (`:613`), #183 failed brightness AND color-temperature in ONE run (`:562` ×2), #169 was a deterministic testFixtures compile miss (not the class) — four different methods across three pre-FAILCHAN runs plus #225, every one at an await helper's throw: pre-existing, method-agnostic, delivery-path. FAILCHAN's lifecycle hunks are not the cause; the lane's `ef02d13` loop is now a rate baseline only. The census file committed (two ephemeral CI test-token values redacted per `TOKLEN-OK`; the `route_join_miss` WARN line lies outside its 150-line tail). Nick's words: `FIX1: class` (with the guard: the lane narrows from the §A.5 table if the two tests fail for unrelated reasons) · `SAMPLES: veto-only`. **The FIX-1 lane DISPATCHED 09-05 ~12:xx CT; the wait-state: the return at `context/audits/2026-09-05_FIX-1_return.md`.** Act 3 (the `dc3328b` ReplayTransitionIT message from the `test-reports` artifact) still owed.
- **Status 2026-09-05 (v64 beat 1) — THE LANE RUNNING; A STAMP DEFECT IN THE INSTRUCTION FOUND AT SOURCE, THE CORRECTION HANDED.** At 18:25Z the lane's tree read exactly FIX-1a's census (9 = 7 M + 2 A); the loops next. The hub read `ReplayTransitionIT.awaitCheckpoint` (`:200–:214`): its throw prints target + timeout, never the resting checkpoint — the §A.5 ReplayIT diagnostic is unmeasurable as instrumented. Act A (the v64 brief) hands the lane a one-stamp correction (`+ " (resting checkpoint " + store.readCheckpoint(subscriberId) + ")"`; FIX-1a's census → 10 = 8 M + 2 A, declared `[INFO]`). The Block-1 adjudication rules R1–R6 are pre-filed (`context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §4): two mechanisms expected; branch (c) clears the gate but not OR-BUS-SILENT-DROP; the corpus ≥110 runs; redacted before staged; mismatches first. The wait-state stands: the return at `context/audits/2026-09-05_FIX-1_return.md` (band ≈18:30–20:30 CT).
- **Status 2026-09-05 (v64 beat 5) — THE RETURN AUDITED: FIX-1a ACCEPT · FIX-1b ACCEPT-WITH-RULINGS (`context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md`); LANDING PENDING Nick's two pushes.** FIX-1a 9 = 7 M + 2 A exact at porcelain (the instruments: `--continue` · XML `if: always()` · `workflow_dispatch` · per-test XML + FULL messages · the typed `DeliveryAnomaly` emitter at four sites · one WARN route); FIX-1b 7 M as `context/audits/2026-09-05_FIX-1b.patch` (DP-2 retry-then-SUSPEND; the TRANSITION drain checkpoints each SUCCESS — ReplayIT's mechanism at source; two IT stamps). The corpus: 176 runs on a 2-carrier pin, 2 RED (both ReplayIT phase 1, unloaded), zero tokens. Act 7 = sample #4 (a red on a flake is the instrument working); Act 8 = THE GATE; three veto-only samples. **Closes when** FIX-1b's push run is GREEN (samples not red) AND the §6-B/EXITCODE hardware proof rides the next card session on FIX-1b's artifact. A post-landing HeroLoop red with `route_join_miss` and zero anomaly tokens re-opens it as FIX-2 (the fan-out reorder), mechanism named.
- **Status 2026-09-05 (v64 beat 8) — LANDED: FIX-1a `5ed9178` (10 files, +703/−21; sample #4 — its CI verdict OWED as one line) · FIX-1b `e5fa035` (6 files, +483/−103) — Nick's word: "the `e5fa035` commit for `homesynapse-core` has all checks pass" → THE GATE IS CLEARED on FIX-1b's push run (law 16; the clearance of record). The three veto-only `workflow_dispatch` samples: not yet taken (Nick's pin: only after a push run is green — it is). `main` is GREEN for a reason at `e5fa035`; CG-1/2/3 may dispatch. **Closes when** the §6-B/EXITCODE hardware proof rides the next card session on `e5fa035`'s CI-built artifact (H8) AND the samples are not red. A later HeroLoop red re-opens the gate as FIX-2 with its mechanism named — a red on a gate run is a red, never "the instrument working" (Nick's pin).
- **Status 2026-09-06 (v65 beat 6) — CG-123 LANDED `f25291b` ON TOP OF THE FIX; ITS PUSH RUN GREEN = SAMPLE #5 ON THE FIX-1b BYTES (Nick: "All GitHub checks passed").** Sample #4 (`5ed9178`) and the three veto-only samples still owed. The §6-B/EXITCODE stop-proof rides **H8-a** (this week, `f25291b`'s CI-built artifact; the word `H8:` asked) — the risk closes when that proof is read on the card.

#### OR-BUS-SILENT-DROP — the event bus drops a delivery without a signal on four LIVE/TRANSITION paths — [OPEN 2026-09-04, v63 beat 1; source: the hub's source read, the grounding audit §4-S4]
- **The class:** `InProcessEventBus.notifyEvent` `:250` (the notified position not yet readable → `return`, offered to no one) · `liveLoop` `:480` (empty page → `continue`) · `:487–:488` ("Transient read failure — skip this position; retain mode") · `TransitionCoordinator.drainAndPromote` `:100` (empty page → `continue`); the LIVE loop processes offered positions only and never pages forward from its checkpoint, so a dropped position is lost to that subscriber permanently and the next delivery writes a checkpoint past it (`:508`). No log, no metric (the bus is SLF4J-free by design; AMD-43's seven metrics are locked). A dropped `command_issued` renders exactly as `integration.route_join_miss` — "the ledger's timeout owns the outcome" — i.e. a real automation's command silently never leaves the engine. FAILCHAN's mirror image (a failure through a silent channel) in the module the D5 floor stands on.
- **Owner:** hub (charter: FAILCHAN-FIX-1, Part A tokens + Part B DP-2) / Coder (the lane). **Commit of record:** `dc3328b` (the bytes read). **Deferred Build Gate:** YES — FIX-1a's and FIX-1b's `./gradlew check` are CI on Nick's pushes. **Closes when** FIX-1b lands with T5–T7 green (retry-then-SUSPEND; a checkpoint never advances past an undelivered matching position) and CI is green on that push.
- **Status 2026-09-05 (v64 beat 5) — THE CLASS WIDENED AND PARTLY FIXED; THE CLOSURE RE-CUT TO THE PASSIVE INSTRUMENT (rule R2).** FIX-1a tokenizes the four drop points; FIX-1b retries-then-SUSPENDs them (`LIVE_READ_EXHAUSTED` · `TRANSITION_READ_EXHAUSTED`) and fixes a FIFTH silent path found at source by the lane — `TransitionCoordinator.drainAndPromote` delivered the drained burst but never checkpointed it (a checkpoint that lied low; on a restart the burst would redeliver under INV-ES-05; ReplayIT's phase-1 stall). A SIXTH is named at source, unmeasured: `notifyEvent` fan-out REORDERING across subscribers (a `command_dispatched` overtaking its `command_issued` at the router → `route_join_miss` with zero drops — HeroLoop's likely mechanism; FIX-2, instrument-first: a `LIVE_OUT_OF_ORDER` token + a monotonic checkpoint guard, then ordered offers or the AMD-42 §3.4.2 pull-from-cursor loop). **Closes when** the `bus.delivery_anomaly` token is ABSENT and both ITs GREEN across the next 20 ordinary `main` runs after FIX-1b lands (never a re-run); a HeroLoop red with a miss and zero tokens re-opens it as FIX-2.
- **Status 2026-09-05 (v64 beat 8) — THE PASSIVE INSTRUMENT'S CLOCK STARTS AT `e5fa035`; THE REGISTER FENCE WRITTEN.** FIX-1a + FIX-1b landed (`5ed9178` · `e5fa035`, the gate cleared). Nick's word `FENCE-BUS: add` → the claim register carries the fence (no delivery-completeness sentence until this OR closes). Run 1 of the 20 ordinary `main` runs is `e5fa035`'s own push run (green); the count continues with every push. **Closes when** the `bus.delivery_anomaly` token is ABSENT and both ITs GREEN across 20 ordinary `main` runs from `e5fa035` inclusive; a HeroLoop red with `route_join_miss` and zero tokens re-opens it as FIX-2.
- **Status 2026-09-06 (v65 beat 6) — THE PASSIVE COUNT: 2 of 20** (`e5fa035` · `f25291b`, both ordinary `main` push runs, both green, no anomaly token reported). Every push a sample; no re-runs; no batched pushes (Nick's law, 09-06). **TR-1 chartered** (the position census as a bench verb — the same instrument closes this risk on EVIDENCE rather than on twenty quiet runs, is the MVP's event-loss audit, and mints B-1/bus); the closure rule R2 stands until TR-1's design returns and is ruled.
- **Status 2026-09-06 (v65 beat 7) — THE PASSIVE COUNT: 3 of 20** (`e5fa035` · `f25291b` · `093d5b4` — the nanoid lock-only push ran Build & Check too; `ci.yml` carries no path filters). The three veto-only `workflow_dispatch` samples FIRING (Nick, 09-06 morning; verdicts this evening) — a red VETOES, read against the pre-registered HeroLoop prediction first.
- **Status 2026-09-06 (v66 beat 6) — THE PASSIVE COUNT 4/20:** FE-113's push `d192d17` (web-ui only; `ci.yml` has no path filter) ran Build & Check GREEN on the FIX-1b bytes — `e5fa035` · `f25291b` · `093d5b4` · `d192d17`. The three veto-only `workflow_dispatch` samples still firing (Nick's `samples: k/3` line owed). TR-1 (the position census as a bench verb; the "complete" criterion pre-registered before any run) is the evidence-closure instrument; its return this evening.
- **Status 2026-09-06 (v66 beat 9) — THE PASSIVE COUNT 5/20:** F-R4-1b's push `39c8dd3` ran Build & Check GREEN on the FIX-1b bytes — `e5fa035` · `f25291b` · `093d5b4` · `d192d17` · `39c8dd3`. The three veto-only samples still owed (`samples: k/3`). TR-1 not yet dispatched (Act after the hygiene pair).
- **Status 2026-09-08 (v67 beat 5) — THE CLOSURE INSTRUMENT DESIGNED (TR-1, ACCEPT):** the position census = post-drain high-water-mark comparison MINUS the dead-letter rows, the State Projection's atomic checkpoint special-cased (a naive `checkpoint ≥ P` over-reports — `InProcessEventBus:557,:565-566`; `ReplayDriver:191`); computed IN-PROCESS by the two ITs and printed as ONE frozen token; run by a `fix1-loop.sh`-lineage driver (`TR1-B2 = DRIVER`, REVERT-able) until R-5. Evidence closure = TR-1b's first measured run (after HONESTY-1 lands). The passive count stays 5/20.
- **Status 2026-09-11 (v68 beat 6) — THE PASSIVE COUNT 8/20** (`94ae99d` · `3f3f5cc` · `eabdbb1`, `ci` Build & Check green on each — Nick's word + the checks screenshot for `eabdbb1`); **`SAMPLES: passive` (Nick):** the three manual veto samples and the stale owed lines (`sample4` · `act3` · `nightly 09-05/09-06`) RETIRED — ordinary pushes plus the now-per-push install-smoke are the instrument; the count is re-derived at the Actions list by each hub, never typed forward.
- **Status 2026-09-11 (v69 beat 4) — SAMPLE #9 RED: `1e26912` (three design files, no source) failed `ci` at `:lifecycle:lifecycle:test` — `HeroLoopHardwareFreeIT.heroLoop_motionToHonestConfirmed`, "timed out awaiting the On frame reaching the scripted NCP" (`awaitTrue :614`, 10 s; the CI-613 class).** The passive instrument has done its job: the class is not closed by FIX-1b. Next: the `test-reports-<run_number>` artifact read for `DeliveryAnomaly` / `LIVE_READ_EXHAUSTED` / `SUSPEND` tokens in the test's stdout — tokens present ⇒ the drop was seen and the retry budget lost to it (FIX-2 = the structural fix: checkpoint-driven LIVE delivery with an idle tick, notification as a hint; an AMD to Doc 01 §3.4); tokens absent ⇒ a path FIX-1a did not tokenize or a scheduling stall (FIX-2 opens with the instrument: a bench `bus-soak` verb, N hero loops under load, latency percentiles + anomaly count). No `main` re-run; the next core push is the fix. Nick's word (Fri ~17:0x CT): a fix AND a long-term solution for the class.
- **Status 2026-09-11 (v70 beat 6) — SAMPLE #9 READ; FIX-2a AUTHORED (instrument first).** The `1e26912` run (34650129654) read at the correct artifact: no `DeliveryAnomaly`/`bus.delivery_anomaly` token in the HeroLoop XML, in all 42 lifecycle report files or in the Run check log; the motion edge in the store; ten silent seconds after `device_adopted`; no dispatch (`context/audits/2026-09-11_CI-1e26912_red-read_HeroLoop-lifecycle_v70-b2.md`). The detector's six kinds do not name this drop. FIX-2a (`context/instructions/2026-09-11_coder-lane_FIX-2a_bus-silent-drop_instrument-first_coding-instruction.md`, test-only) gives the desk the await diagnostic, TR-1b's census IT and a K=20 soak; FIX-2b (checkpoint-driven LIVE delivery; an AMD to Doc 01 §3.4) is authored on its distribution. **Owner:** hub / Coder (FIX-2a on Nick's paste). **Closes when:** the soak reproduces and FIX-2b's bytes take the count to 20/20 green with the census at `missed=0`.
- **Status 2026-09-11 (v70 beat 7) — FIX-2a RETURNED, ACCEPT; THE DESK DOES NOT REPRODUCE THE CLASS.** 143 in-process hero loops on two topologies (24 cores; 2 carriers pinned), zero timeouts, zero anomaly lines, zero scored misses (`context/audits/2026-09-11_FIX-2a_return.md` §7; the hub's audit `…_FIX-2a_intake_two-layer-audit_v70-b7.md` §2). The instruments land on Nick's card: the await diagnostic (every subscriber's mode/checkpoint/DLQ beside the store head in the failure message), `BusPositionCensusIT` (TR-1's tokens, trailing keys), `BusSoakIT` (K=20 per CI push). **The sample source is now the runner itself**; the count restarts at the landing sha with the soak in the suite. **Owner:** hub (FIX-2b's charter on the first diagnostic-bearing red or on TR-1b's card reading; the `-D` passthrough docketed). **Closes when:** FIX-2b's bytes take 20 consecutive push samples green with `bus.soak … timed_out=0 anomalies=0` and the census at `missed=0`, or the class is shown to be a runner artefact by the diagnostic.
- **Status 2026-09-11 (v70 beat 8) — SAMPLE #10 RED WITH A READING; THE CLASS IS REAL ON 2-vCPU HOSTS.** `ci` on `cc05a54` (FIX-2a's landing): 82 run, 3 failed — the hero loop, the census loop, the soak at loop 18 of 20 — all one shape: the motion report, then `automation_triggered`, then ten silent seconds; every subscriber LIVE; anomaly lines 0; `available_processors=2`. The stall sits between the run's initiation and its first action (`context/audits/2026-09-11_CI-cc05a54_red-read_first-diagnostic_v70-b8.md`, H1–H4). **Next:** FIX-2b-i (`pendingDepth` on the snapshot · a virtual-thread dump at the timeout · the runner's pinned-thread trace) on Nick's paste; FIX-2b-ii on the first dump-bearing red. **Owner:** hub / Coder. **Closes when:** FIX-2b-ii's bytes take 20 consecutive push samples green with the soak at `timed_out=0` and the census at `missed=0`.
- **Status 2026-09-11 (v70 beat 9) — FIX-2b-i RETURNED, ACCEPT; THE DISCRIMINATORS LAND ON NICK'S CARD.** `pendingDepth` on every snapshot; a jcmd thread dump (virtual threads included) at every await timeout; `-Djdk.tracePinnedThreads=short` on every test JVM. Sample #11 is the landing push; a lifecycle red there carries `pending=<n>` per subscriber and the `bus.thread:` frames. FIX-2b-ii is authored on that reading against H1–H4; if #11 is green or the frame is ambiguous, v71 charters REV-1 (independent adversarial read) and REPRO-1 (a desk reproduction under two carriers + full-check load). O2's caveat: the pinned-thread trace prints from the pinned thread — a hang at a pin means drop the flag.
- **Status 2026-09-12 (v70 beat 10) — SAMPLE #11 READ WITH THE DUMP: THE BUS IS IDLE; THE CLASS IS THE RUN HAND-OFF.** `bd5d35e` (run 34670078359): `pending=0` on every subscriber; the engine's virtual thread parked idle at `InProcessEventBus.liveLoop:536`; every `hs-sub-read-*`, `hs-read-0`, `hs-write-0` idle; `hs-sched-0` idle; no pinned trace; no thread executing the run whose `automation_triggered` is the head of the store (`context/audits/2026-09-12_CI-bd5d35e_frame-read_bus-idle-run-handoff_v70-b10.md`). H1, H2 and H3-parked FALSIFIED. `writeCheckpoint` is fire-and-forget (`SqliteCheckpointStore:176–:184`): the snapshot's checkpoint lags delivery; the census's `missed=1` is a token artefact. **Next:** v71 reads `StandardRunManager:600–:720` (the hand-off after `:651`) and `SubscriberSupervisor.deliver`; FIX-2b-ii = two INFO lines (`automation.run_handoff`, `automation.action_step_started`) as the red-first instrument in `core/automation`, then the fix on the step they name. CI-1 (landing) keeps the sample stream beside the gate. **Owner:** hub / Coder. **Closes when:** the named step is fixed and 20 consecutive `bus-soak` job samples read `timed_out=0`.
- **Status 2026-09-12 (v70 close) — THE GATE RESTORED; SAMPLE #13 GREEN.** CI-1 landed `ff1a6e1`: `check` GREEN without the two soak ITs; the `bus-soak` job (non-gating, every push, XML uploaded as `bus-soak-<run>`) GREEN on its first run = sample #13. The count of record from here is the `bus-soak` job's: 1 green since `ff1a6e1`.
- **Status 2026-09-12 (v71 beat 3) — SAMPLES #11–#13 READ; THE SECOND FACE FOUND AT SOURCE; THE FIX RULED.** Sample #11's dump (`bd5d35e`): the bus idle, `pending=0` everywhere, the run not continuing after `automation_triggered` — the hand-off is a per-run VT (`StandardRunManager.startRun`); FIX-2b-ii (i) landed `72efb42` (four INFO/ERROR tokens on the run path; `NOTIFY_SKIPPED_LIVE` at the notify guard; the hero IT asserts per loop) — sample #14 is the first with it. REV-1 (`…REV-1_second-read_return.md` F3/F4): notify order ≠ append order (`NotifyingEventPublisher`, no lock across append + notify) and the checkpoint upsert is `INSERT OR REPLACE`, so `[head, head−1]` regresses the checkpoint and TR-1's census prints `missed=1 first_missed=<head>` for a delivered subscriber (the #11/#12 tuple) while a late notify skipped by the guard is a real drop the census cannot see. **Nick ruled `BUSFIX: b`** (store-driven in-order delivery; `…2026-09-12_v71_BUSFIX-b_ruling-of-record.md`): BUS-ORDER-1 after EXPLAIN-114a; **closes when** twenty consecutive `bus-soak` samples after BUS-ORDER-1 are green with kind E at zero; reverts to (c) if kind E never fires in ten samples and REPRO-1 (K=200, two carriers) is green. The passive count restarts at BUS-ORDER-1's landing.
- **Status 2026-09-12 (v71 beat 5) — SAMPLE #14 GREEN WITH THE INSTRUMENT; REPRO-1 GREEN; THE REVERT CLOCK 1/10.** `72efb42` (run 34717732026, a 2-vCPU runner): soak 20/20 `anomalies=0`, census `missed=0` for every scored subscriber, 20/20/20 hand-off lines, kind E 0 — the first sample carrying FIX-2b-ii (i), and the first without the census tuple. REPRO-1 on Nick's desk (`vt_parallelism=2`, K=200, no load): 200/200, `missed=0`, kind E 0. Nick's revert rule (`…BUSFIX-b_ruling-of-record.md` §1): ten consecutive silent samples AND a green REPRO-1 → `BUSFIX: c` and BUS-ORDER-1 waits for the R-5 floor. Silent samples: 1 (`72efb42`). BUS-ORDER-1 stays ISSUE-READY behind EXPLAIN-114a's landing; the hub counts at every push.
- **Status 2026-09-12 (v71 beat 6) — SAMPLE #15 RED: THE FIRST ANOMALY LINE UNDER THE INSTRUMENT, UNREAD.** `5f918c7` (EXPLAIN-114a's landing; `check` GREEN): the `bus-soak` job failed `BusSoakIT` on `[bus.soak: anomalies] expected: 0L but was: 1L` (`:204`); the census IT green. The kind is in the artifact (`bus.delivery_anomaly: kind=… subscriber=… position=…`) and is read before anything dispatches: `NOTIFY_SKIPPED_LIVE` → AMD-101 §6's reserved exhibit, the revert clock STOPS, BUS-ORDER-1 dispatches on `BUSORDER: go`; any other kind → its own reading. The silent-sample count resets to 0 pending the read (it was 1: `72efb42`).
- **Status 2026-09-12 (v72 beat 1) — SAMPLE #15 READ: KIND E; THE REVERT CLOCK STOPS; BUS-ORDER-1'S GATE IS MET.** The artifact (`_scratch/v71/ci-5f918c7/test-results/test/TEST-com.homesynapse.lifecycle.BusSoakIT.xml`): `bus.delivery_anomaly: kind=NOTIFY_SKIPPED_LIVE subscriber=automation_engine position=206 detail=notifyEvent: checkpoint=207 at or past position` (23:47:05.464, the `[Test worker]` thread, between the 16th and 17th hand-off lines); `bus.soak` 20/20 `anomalies=1`; the census `missed=0` for every scored subscriber (`automation_engine` 254/254, checkpoint 254). The skip did not become a miss in this run; it is the arm AMD-101 removes and the exhibit its §6 reserved (filled in the docs tree; rides `AMD101: ratify`). Nick's revert rule no longer applies (the clock stopped at 1/10). BUS-ORDER-1 dispatches on his paste (`BUSORDER: go`); the closure counter restarts at its landing: twenty green samples with kind E at zero. Reading: `context/audits/2026-09-12_v72-b1_sample-15_5f918c7_kind-E_reading_and_boot-intake.md`.
- **Status 2026-09-13 (v72 beat 2) — BUS-ORDER-1 RETURNED, AUDITED ACCEPT; THE CORE CARD HANDED.** The read-forward LIVE loop is on disk at `5f918c7` (16 = 14 M + 2 A; `module-info` untouched): the cursor is the source of truth, the checkpoint the floor, the notification a wake hint with a bounded tick; the drain reads forward too (T2 found its gap detection dropping two positions at the flip, kind E's drain-side twin). Deferred Build Gate: `./gradlew check` run by Nick's hands Sun ~06:1x CT — GREEN (the test XMLs at 11:18–11:20Z re-derive 240/413/81/30, 0 failed); the gate of record is CI on the landing sha. The closure counter restarts at that landing: twenty green `bus-soak` samples with kind E at zero and `delivered_max=` on every census line. Audit: `context/audits/2026-09-13_v72-b2_BUS-ORDER-1_intake_two-layer-audit.md`.
- **Status 2026-09-13 (v72 beat 3) — BUS-ORDER-1 LANDED `a458a64`; CI GREEN (`check` and `bus-soak`); THE DEFERRED BUILD GATE RESOLVED; THE CLOSURE COUNTER RESTARTED: 1/20.** Nick's line: `a458a64` green on every action and check. Sample #16 = the `bus-soak` job on `a458a64`, green by verdict (artifact not fetched, `SAMPLES: passive`). Closes at twenty consecutive green samples from `a458a64` with kind E at zero; any red sample is read at its artifact before the count moves. The kind-E arm and the drain's two-position drop are gone from the tree; the checkpoint is a floor; the census reads the cursor.
- **Status 2026-09-13 (v72 beat 4) — AMD-101 RATIFIED; the docs card handed.** Doc 01 §3.4 now states the delivery invariant in one paragraph; INV-BUS-01 reads "exactly once and in position order" with the cursor as the drain's dedup gate; INV-PROJ-04 names BUS-ORDER-1 as the realization that makes checkpoint monotonicity hold on the LIVE path. The kinds this risk's samples can still show: `LIVE_READ_FAILED` and `NOTIFY_NOT_VISIBLE`; the closure counter 1/20 from `a458a64`; the run-hand-off face (FIX-2b-ii's tokens) stays this risk's second open question, untouched by the amendment.
- **Status 2026-09-13 (v72 beat 6) — R-4c's rig-load zero.** `bus.delivery_anomaly` 0 across the whole R-4c invocation on `a458a64` (a window, two ZDO exchanges, an adoption, four reporting devices) at 4.21 rows/min — a rig at rest with one adoption, three orders under the soak's rate. Recorded as a rig-load zero; NOT counted toward the closure counter (still 1/20). The record's `lookups=` counter counts misses only and `zdo_rsp` counts failures too (F-2, F-3); the census grammar of record since BUS-ORDER-1 is in the TR-1 audit's addendum.

#### OR-ADOPT-AWAKE — a device identified on the air is not thereby onboarded (F-R4c-A, R-4c 2026-09-13)
- **Opened 2026-09-13 (v72 beat 6).** The SNZB-01P was resolved over the air (265 ms), admitted as a rejoin candidate and never proposed; the SNZB-02P was proposed 1.04 s after its own `tc_join`, not after its ZDO resolution 72 s earlier. At source both admissions are the same two calls (`ZigbeeIntegrationAdapter.admitRejoinCandidate`), so the difference is downstream of admission. Explanation PROVISIONAL (the interview needs an awake device; the provocation control was contaminated). **Owner:** the hub (R-5's charter carries the counterfactual and the battery-pull test). **Closes** when R-5's first window either adopts a passively-resolved device without a join (refutes) or fails to (confirms) and the design constraint is written into the R-5/rehearsal charters: count re-seen and adopted as two numbers; never assume resolution ⇒ onboarding. Related: the null-arm block (one rig block, next packet) and the ULID audit (persisted ids outside the minting card) ride the same charter. Detail: `context/audits/2026-09-13_v72-b6_R-4c_intake_two-layer-audit.md` §3.3, §5.

#### OR-METERING-VOLUME — the run's largest event input is unmeasured and set by a constant never read — [OPEN 2026-09-17, v76 beat 2; source: Nick's v76 b2 brief §4, attacked in the b2 audit §4]
- **The class:** `ReportingConfigurator.java:71–:72` configures 0x0B04 (change 10 raw) and 0x0702 (change 5 raw) at a 5-s minimum for every ADOPTED endpoint listing them (`ZigbeeIntegrationAdapter.adoptIfAccepted` :979–:996 → `driveReporting` :1009). 0x0702 is a monotonic accumulator: at any seMetering divisor ≥ ~45,000 it reports at the 5-s floor at 80 W — 720 frames/h/device; the TR3's divisor is 3,600,000 (PLUG-DOSSIER row 29), the Gen4's unread. 0x0B04 adds 720/h only at divisor 100. Three plugs × 72 h ≈ 155k (floor) – 311k (ceiling) metering events. Today no adopted plug exists (THE ADOPTION FENCE) and nothing floods; the day ENERGY-READ adopts one, this row is live. Nothing at `3af6213` measures sustained event rate, store growth per hour or heap over hours (`BusSoakIT` is K=20 latency loops; the bench has no rate scenario).
- **Owner:** hub (ENERGY-READ's charter: IR-25 as a SIZED decision — engineering-unit thresholds chosen with the predicted frames/h per divisor stated, the max interval included; MEASURE-2 / IR-28 the rate instrument on `ZigbeeHardwareFreeRig`). **Commit of record:** `3af6213` (the bytes read). **Deferred Build Gate:** no. **Closes when** MEASURE-2's number is filed (events/s sustained for M minutes; store growth/h; heap over time) and IR-25's thresholds put the three plugs' 72-h volume inside it with the margin stated.

- **Status 2026-09-18 (v76 beat 5) — THE WIRE FACT CORRECTED; THE RATE LEG CLOSED WITH MARGIN; THE STORAGE LEG → THE SOAK.** ENERGY-READ's return R-4 (verified by the hub's own emulation, the b5 audit §0): at `3af6213` the six-byte reportable-change field was encoded with an `int` shift whose distance Java takes mod 32, so HEAD's 0x0702 row never sent 5 raw — it sent `05 00 00 00 05 00` (2.1 × 10¹⁰), a threshold no accumulator crosses, and the read-back parse folded the same way, verifying what was never sent. The flood arithmetic above stands as the INTENDED configuration; on the wire an adopted plug would have reported the 0x0702 leg only at the 3600-s maximum. Fixed in the lane (both sides widened to `long`); the uint48 width meets silicon at the first metering adoption (THE MEASUREMENT RECORD row G4-4). MEASURE-2 (IR-28): the adapter path ingests ~24,000 metering reports per second on the desk in memory (`rate=24031.1`, JIT-cold) against the run's worst case of ~1.2 per second — the rate leg CLOSES with margin. The store's growth per hour is NOT yet measured (the zigbee module cannot reach SQLite; M2-2 is a 30 B/event payload floor) — the storage leg moves to THE SOAK (wk 10-12), with a lifecycle-module MEASURE-2b (row M2-2b) if the number is wanted earlier. **Closes when** the soak's store growth is filed and IR-25's thresholds (now on the wire: 1 W / 5–600 s; 5 Wh / 5–3600 s, scaled by the read divisor) are read back verified at the first adoption.

#### OR-JOURNALD-PRIO — journald priority is blind to every application warning — [OPEN 2026-08-30, v58 beat 6; source: the R-3a hub-return §10-P; MECHANISM VERIFIED at source v58 b6]
- **The mechanism (verified):** `homesynapse.service:99–100` routes stdout/stderr → journal; no `sd_journal`/`SyslogLevelPrefix`/priority-prefix emission exists anywhere in app/core/config/distribution — every application line lands at journald’s default INFO priority. Measured: 158–201 app WARN/ERROR lines in one R-3a day; `journalctl -p warning` reported **2**, both systemd’s own. Any priority-filtering monitor sees a healthy service while warnings accumulate.
- **Fix candidates:** `<N>`-prefix the console handler (systemd’s `SyslogLevelPrefix` default honors it) or journal-native logging. Charter at the September plan / the R-10 docket. **Closes when:** app WARN+ lines carry journal priority ≥ warning, proven by a `journalctl -p warning` count on a card.
- **Status 2026-09-04 (v62 beat 2) — RULED Row 7 (a) (ADOPT-ALL-RECS, 09-03):** the `<N>`-prefix on the console handler (systemd's `SyslogLevelPrefix` default honors it). Charter queued behind FAILCHAN (same files' neighbourhood; one lane on the core tree). Closes when `journalctl -p warning` on a card counts the app's WARN+ lines.
- **Owner:** hub (the charter; Row 7's instruction authors behind CG on the one-lane rule) / Coder (the WU). **Deferred Build Gate:** none yet (no WU has run). *(Field made explicit at v64 beat 1 — preflight Check 5; the ruling and status lines above are unchanged.)*

#### OR-REHOMED-OQ — the two live items re-homed from the CLOSED open-questions channel — [OPEN 2026-08-18, v54 beat 2; created by the R-11 closure ruling (its §5.5), executed at intake]
- **(a) OQ-05-07 — Research-9 registry residuals** (deferrable; revisit at an operator-rebuild window). **(b) The 2026-06-11 NO_DIRECT_TIME_ACCESS enforcement-reach decision** — whether the arch-rule gate extends to non-app test classpaths; until ruled, §4c's posture stands (gate-enforced for production code, self-enforced convention for non-app test code — PM review is the only enforcement it has). Rides the next app-module-touching WU.
- **Owner:** hub (carry + surface in status updates). **Resolution:** (a) closes at its named rebuild window; (b) closes at the ruling inside that WU's instruction. Source of record: `context/open-questions.md` head (CLOSED-channel note) + this row.

#### OR-M13-SDNOTIFY — sd_notify real AF_UNIX-datagram transport deferred to M13 (NEW 2026-06-07)
- **Status 2026-08-22 (v56 beat 2) — OPEN; charter-bound.** The R10-IN-L return (`context/audits/2026-08-22_R10-IN-L_liveness-and-notify-transport_return.md` §3/§5) re-priced the M5-D matrix: FFM is final in JDK 22 but LTD-01 forbids preview features, so T-2 is an LTD-01 amendment, not a transport choice; recommendation **T-0 (stay `Type=exec` + the probe; defer M13 indefinitely; the unit's staged flip block is a live trap — rewritten as a DANGER note by R-9 rider R-9-b)**. Resolves at the R-10 charter's R-L2 word (T-0 closes this OR as DEFERRED-BY-DESIGN; any transport word re-opens it as a WU).
- **Severity:** LOW–MEDIUM (a *functional* Tier-1 systemd health reporter needs it; M5-A ships the contract + protocol semantics correctly behind a seam — compile + unit tests GREEN; no MVP consumer is blocked until on-device Tier-1 deployment).
- **Detail:** Pure OpenJDK 21 does not support AF_UNIX `SOCK_DGRAM` — `DatagramChannel.open(StandardProtocolFamily.UNIX)` throws `UnsupportedOperationException` at runtime (JEP 380 delivered Unix-domain *stream* sockets only). `sd_notify` is a datagram protocol, so `SystemdHealthReporter`'s real transport is non-functional on a stock JDK. M5-A isolates this behind the package-private `NotifyTransport` seam (the real transport is never constructed in tests; its default ctor now throws a clear "deferred to M13" `IllegalStateException`).
- **Owner:** PM (decide mechanism) / Coder (implement at M13 composition root). **Resolution condition:** pick the transport mechanism — **JNR/JNA native binding** (needs a version-catalog entry + approval; interacts with the GraalVM native-image decision) **vs a `systemd-notify` subprocess fallback** (watchdog-PID caveats; process-per-heartbeat is poor on a Pi) — **resolved with the M5-D evidence** (the choice is entangled with the native-image spike), then implemented at M13 where the reporter is constructed and `$NOTIFY_SOCKET` resolved. Closed when a working transport ships + a Tier-1 sd_notify integration test passes on-device.
- **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`): compares JNR/JNA vs the `systemd-notify` subprocess (+ the FFM/Panama JDK-native option due-diligence adds, + the M5-A "keep the seam, defer" baseline) with the GraalVM native-image entanglement flagged (cites the language-replatform assessment §1.2/§3.1/§4.1). **PM recommendation:** **defer the final commit to M13, gated on the M5-D GraalVM spike + the WatchdogSec decision + the JDK-version trajectory** — **reject JNR/JNA** (dominated: new dep + worst native-image fit for a 3-syscall job); **subprocess if no WATCHDOG keepalives** (zero-dep, closed-world-clean), else **FFM/Panama** (zero-dep, in-process, native-image-friendly; needs JDK 22+ / `--enable-preview`). **No catalog dependency added now.** Two items surfaced for Nick: (i) the GraalVM C15 spike should report whether JNR/JNA-class reflection bindings survive its closed-world config; (ii) **decide whether HomeSynapse opts into the systemd watchdog (WatchdogSec)** — a Doc 12 lifecycle choice that largely settles FFM-vs-subprocess.
- **Status 2026-09-04 (v62 beat 2) — RULED Row 5 (b) HOLD (ADOPT-ALL-RECS, 09-03):** the seam holds; T-0 is NOT spoken this month; the JDK-trajectory question (the Pelton-week orphan row, ORPHANS (a)) rides the R-5 charter as a carry-forward row. Status unchanged otherwise (DEFERRED-BY-DESIGN pending that row).

**Resolved/closed risk entries live at `context/handoff/archive/open-risks-resolved-2026-08-23.md` (eleven blocks, verbatim; rotated v56 b4 + v58 b6 + v62 b1 under W-HIVE-1 P1: this section carries OPEN entries only).**

## The archive map (pointer trail; maintained at each rotation — zero dangling addresses)

Any pointer of the form “pm-handoff §<date> (vN beat K)” resolves here. LIVE above: the newest ≤12 blocks (v75 b1 → the newest, at 2026-09-15) + Open Risks (OPEN entries only since v56 b4). ROTATED, verbatim:

- the resolved/closed Open-Risks ledger (nine blocks) → `archive/open-risks-resolved-2026-08-23.md` · both pre-region-cap `last-verified:` chains → `archive/chains-pre-region-cap-2026-08-23.md` (rotated 2026-08-23, v56 b4)
- v54 b7–b1 · v53 b9–b1 · v52 b7–b1 · v51 b6–b1 · v50 b7–b1 · v49 b5–b1 + the pre-rotation LV chain → `archive/pm-handoff-beats-v49-v54b7-rotated-2026-08-21.md`
- v48–v45 → `archive/pm-handoff-beats-v45-v48-rotated-2026-08-10.md` · v44–v41 → `archive/pm-handoff-beats-v41-v44-rotated-2026-08-06.md` · v40–v38 → `archive/pm-handoff-beats-v38-v40-rotated-2026-07-31.md` · v37–v32 → `archive/pm-handoff-beats-v32-v37-rotated-2026-07-26.md`
- earlier eras → `archive/pm-handoff-beats-rotated-2026-07-19.md` · `-2026-07-07` · `-2026-07-04` · `-2026-07-02` · `archive/pm-handoff-2026-06.md` · `archive/pm-handoff-2026-05.md` · `archive/pm-handoff-rotated-2026-06-22.md` · `-2026-06-21.md`
- sibling rotations this date: the snapshot chain/mastheads → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-08-21.md` · coder-handoff entries → `context/handoff/archive/coder-handoff-entries-rotated-2026-08-21.md` · coder-lessons 2026-07 → `context/lessons/archive/coder-lessons-2026-07-rotated-2026-08-21.md`
- sibling rotation 2026-08-26 (v57 b1, P6): the ten oldest coder-lessons entries (2026-08-02 … the second 2026-08-22, BY COUNT) → `context/lessons/archive/coder-lessons-2026-08-part1-rotated-2026-08-26.md` (verbatim; the live file holds the newest five)
- the `:8` chain's v55 b3–b6 segments → `archive/chains-rotated-2026-08-27.md` (verbatim; rotated 2026-08-27, v57 b2, under the ≤3,000 B chain cap)
- v65 b5 → v54 b8 (95 blocks; 2026-09-05 → 2026-08-21) → `archive/pm-handoff-beats-v54b8-v65b5-rotated-2026-09-07.md` (verbatim; rotated 2026-09-07, v66 b10, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v66 b6 → v65 b6 (8 blocks; 2026-09-06) → `archive/pm-handoff-beats-v65b6-v66b6-rotated-2026-09-07.md` (verbatim; rotated 2026-09-07, v67 b1, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v67 b3 → v66 b7 (7 blocks; 2026-09-07 → 2026-09-06) → `archive/pm-handoff-beats-v66b7-v67b3-rotated-2026-09-10.md` (verbatim; rotated 2026-09-10, v68 b2, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v67 b7 → v67 b4 (4 blocks; 2026-09-09 → 2026-09-07) → `archive/pm-handoff-beats-v67b4-v67b7-rotated-2026-09-11.md` (verbatim; rotated 2026-09-11, v69 b3, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v68 b6 → v68 b1 (6 blocks; 2026-09-11 → 2026-09-10) → `archive/pm-handoff-beats-v68b1-v68b6-rotated-2026-09-11.md` (verbatim; rotated 2026-09-11, v70 b3, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v69 b4 → v69 b1 (4 blocks; 2026-09-11) → `archive/pm-handoff-beats-v69b1-v69b4-rotated-2026-09-11.md` (verbatim; rotated 2026-09-11, v70 b9, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b2 → v70 b1 (2 blocks; 2026-09-11) → `archive/pm-handoff-beats-v70b1-v70b2-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v71 b3, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b3 (1 block; 2026-09-11) → `archive/pm-handoff-beats-v70b3-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v71 b5, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b4 (1 block; 2026-09-11) → `archive/pm-handoff-beats-v70b4-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v71 b6, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted equal)
- v70 b9 → v70 b5 (5 blocks; 2026-09-11) → `archive/pm-handoff-beats-v70b5-v70b9-rotated-2026-09-12.md` (verbatim; rotated 2026-09-12, v72 b1, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted: kept + archived body + 1 = before)
- v73 b7 → v73 b1 (7 blocks; 2026-09-13) → `archive/pm-handoff-beats-v73b1-v73b7-rotated-2026-09-14.md` (verbatim; rotated 2026-09-14, v74 b6, under the ≤12-live-blocks rule; bytes asserted)
- v72 b7 → v72 b1 (7 blocks; 2026-09-12/13) → `archive/pm-handoff-beats-v72b1-v72b7-rotated-2026-09-13.md` (verbatim; rotated 2026-09-13, v73 b6, under the ≤12-live-blocks rule; bytes asserted)
- v71 b6 → v70 b10 (7 blocks; 2026-09-12) → `archive/pm-handoff-beats-v70b10-v71b6-rotated-2026-09-13.md` (verbatim; rotated 2026-09-13, v72 b6, under the ≤12-live-blocks rule — the v67 prompt §1b.10; bytes asserted: kept + archived body + 1 = before)
- v74 b8 → v74 b1 (8 blocks; 2026-09-13/14) → `archive/pm-handoff-beats-v74b1-v74b8-rotated-2026-09-15.md` (verbatim; rotated 2026-09-15, v75 b5, under the ≤12-live-blocks rule; bytes asserted)
- v75 b4 → v75 b1 (4 blocks; 2026-09-14/15) → `archive/pm-handoff-beats-v75b1-v75b4-rotated-2026-09-18.md` (verbatim; rotated 2026-09-18, v76 b5, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before)
- v75 b8 → v75 b5 (4 blocks; 2026-09-15/16/17) → `archive/pm-handoff-beats-v75b5-v75b8-rotated-2026-09-19.md` (verbatim; rotated 2026-09-19, v77 b1, under the ≤12-live-blocks rule; bytes asserted: kept + archived body = before + the map row)
