<!--
file: context/instructions/2026-07-02_M7.5c-a_internal-read-envelope-conformance_coding-instruction.md
purpose: Micro-WU M7.5c-a — conform the two /internal/* A-class reads (A4 projection, A5 dlq) to the frozen v1.1 envelope, per the ruled DRIFT-1 adjudication (2026-07-02, Nick co-signed: "conform Core, micro-WU first"). Unblocks the live dashboard's poll cursor (the FE-1 smoke found views load once and never refresh). The first slice of M7.5c; /events + /health remain M7.5c-b (unscheduled).
audience: Coder. Issued by the v13 hub after the FE-1 return adjudication.
status: ISSUE-READY 2026-07-02. Sequencing: BEFORE M9.1 on the serial Coder lane (Nick's ruling).
baseline: core 52824e9 (+ the uncommitted web-ui FE-1 work, which touches NOTHING under api/ — disjoint). docs 1509b34. The freeze record is now v1.1.1 (same file, two [v1.1.1] blocks — read it fresh).
-->

# M7.5c-a — Envelope the internal reads (A4 `/internal/projection`, A5 `/internal/dlq`)

**What/why in one line:** the dashboard polls A4 for `meta.viewPosition` (the freeze §0 anchor: EVERY read carries `meta`); both `/internal/*` endpoints ship pre-freeze bare bodies, so the live poll loop dies — wrap them in the same envelope the conforming endpoints already use, emitting the frozen A4/A5 `data` fields.

## Files to read FIRST
`context/pre-verifications/` has NO WU file for this micro-WU — the pre-verification is INLINED as STOP-gated obligations below (P1–P6). Read: the freeze record (v1.1.1) §0 + §A4/§A5 · `api/rest-api` MODULE_CONTEXT + `module-info.java` (verbatim) · `ProjectionStatusEndpoint.java` + `DlqStatusEndpoint.java` · whichever envelope/`ResponseMeta`/`ApiResponse` machinery the M7.5a/b endpoints use (find it from `RunEndpoints`/`EntityEndpoints` — the conforming pattern is the LAW here) · `RunEndpointsTest` (the shape-test pattern to mirror).

## Pre-verify (STOP-on-mismatch — report, don't improvise)
- **P1:** A4's current bare fields are `{mode, viewPosition, entityCount, ready}` (javadoc + handler); A5's are `{subscribers:[{subscriberId, mode, …}]}`. If different, STOP.
- **P2:** the conforming endpoints build `{data, meta}` via an existing helper + `ResponseMeta(viewPosition, timestamp)`-equivalent. Reuse it verbatim — if the helper is not reusable from these endpoints' package, STOP and report (do NOT duplicate envelope logic).
- **P3:** frozen A4 `data` = `{mode, viewPosition, lagEvents, projectionVersion}`. `projectionVersion` = the existing constant (5 — locate its authoritative source; do not hardcode a literal without citing it). `lagEvents` = log head − projection viewPosition: pre-verify whether a log-head supplier is already reachable at the rest-api wiring seam (the endpoints already receive a `LongSupplier` for viewPosition, wired from the composition root). If a head supplier must be added, wire it the SAME way (a `LongSupplier` from the composition root — NO new module edge; if a new module edge appears unavoidable, STOP).
- **P4:** frozen A5 `data` = `{depth, parkedSubscribers[]}` — derive both from the existing DLQ/subscriber state the current endpoint reads; keep the existing per-subscriber detail as an ADDITIVE `subscribers` field if it's cheap, else drop (frozen fields are the contract; extras are optional).
- **P5:** keep `entityCount` + `ready` as ADDITIVE fields inside A4 `data` (the FE tolerates + the ruling retains them).
- **P6:** ETag: give both endpoints the same `ETag`/`If-None-Match` behavior the conforming reads have (freeze §0 caching line) IF the shared machinery makes it free; if it's not free, note-and-defer in the handoff (NOT a silent skip).

## Scope
- **IN:** the two endpoint classes + their wiring call sites + tests (shape tests field-for-field against frozen A4/A5, camelCase, the RunEndpointsTest pattern; a meta-presence test asserting `meta.viewPosition` on both). Update `api/rest-api` MODULE_CONTEXT.
- **OUT:** `/events` + `/health` (M7.5c-b, unscheduled) · any problem-type change (DRIFT-2 resolved record-side — Core emits URIs correctly already) · anything under `web-ui/` (the FE lane owns it; its uncommitted work is disjoint).
- **Mints/pins:** ZERO event types (71/41/53 untouched); no module-info change expected (STOP if P3 forces one); no DDL.

## Watch out
Tests must inject `Clock` — no `Instant.now()`/`System.nanoTime()` in test code (rest-api is NOT whitelisted; the ArchUnit reach nuance means non-app test code is self-enforced convention — see coder-lessons 2026-04-10). The record component/static-factory STOP-check on any new record. Targeted `./gradlew :api:rest-api:compileJava` + tests before handoff; full `check` deferred to Nick per protocol; count pins must stay green.

## Done-when
Both endpoints return `{data, meta}` with the frozen fields + ruled extras; shape tests green; `meta.viewPosition` asserted present on both; existing A4/A5 consumers in tests updated; MODULE_CONTEXT updated; handoff names the next WU (M9.1 — instruction already issued-ready).
