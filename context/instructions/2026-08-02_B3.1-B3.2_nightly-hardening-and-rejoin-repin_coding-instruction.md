<!--
file: context/instructions/2026-08-02_B3.1-B3.2_nightly-hardening-and-rejoin-repin_coding-instruction.md
purpose: The B3.1 (nightly hardening — the night-1 return §9 set + the night-2 additions) + B3.2 (rejoin-race evidence-anchor re-pin) coding instruction. BENCH repo, desk half only.
audience: Coder (host-CC bench lane)
status: ISSUE-READY
baseline: nexsys-bench `a791c99` (hub-verified clean at authoring, 2026-08-02; re-verify porcelain at launch)
launch-gates: Nick's skills mirror-sync confirmation (Check-9) precedes the session. The lane COMMITS NOTHING; the hub audits, then orders.
return: context/audits/2026-08-02_B3.1-B3.2_return.md (one file)
deploy-embargo: NOTHING from this WU reaches the Pi before the Aug-3 morning-gate intake — the bench is an ARMED natural experiment tonight (the s31 park discriminator; see context/audits/2026-08-02_B3_night2_command-confirm-s31_evidence-read.md). The Pi pull + unit re-copy + daemon-reload ride a hub-authored post-gate operator block.
-->

# Coding Task: B3.1 Nightly Hardening + B3.2 Rejoin-Race Evidence-Anchor Re-Pin

**Subsystem:** nexsys-bench (test-and-truth engine) — tools/nightly.sh, tools/runner/, tools/scheduler/, scenarios/
**Phase:** 3-Implementation (bench lane; no Gradle — the desk gates are the fixture harness + selftest + lint)
**Task Brief Reference:** pm-handoff v43 beats 6–7 (B3.1/B3.2 minted) + v44 beats 1–2 (the night-2 adjudication; dispatch on the banked KillMode gate)

## What This Implements

The nightly (B3) survived its KillMode gate on the Aug-2 fire (systemd's own line: `Unit process 69055 (java) remains running after unit stopped` — the same instrument that proved the night-1 kill). Two work packets are now licensed: **B3.1** folds the night-1 closing return's §9 amendment set in-repo (evidence-surface hardening, the KillMode fix itself, morning-gate legibility) plus two night-2 additions (the post-window state read; FAIL-bundle app-log capture). **B3.2** repairs the rejoin-race operator scenario whose mid-rejoin-premise anchor is structurally unsatisfiable on the S31 (silent-resume proven ×4 — the device never emits `device_relinked`/`device_announce` on a mid-session rejoin; relink lines fire only at BOOT rehydration, ×6 in the Aug-2 journal).

## Files to Read Before Starting (minimum read set — the floor, not the ceiling)

| File | Why |
|---|---|
| `nexsys-hivemind/context/audits/2026-08-01_B3_night1_return.md` §§5–9, 11 | The §9 amendment set THIS WU implements, as drafted by the night-1 lane; the KillMode mechanism; the journal-horizon findings |
| `nexsys-hivemind/context/audits/2026-08-02_B3_night2_command-confirm-s31_evidence-read.md` | The night-2 adjudication: the broken-park mechanism, the post-window-state-read mint (item A-9 here), the armed experiment (the deploy embargo's reason) |
| `tools/nightly.sh` (whole — 276 lines at baseline) | The wrapper this WU hardens; the trap/finish() structure governs A-3/A-4/A-5 |
| `tools/bench.sh` (whole — 104 lines) | The delegation surface; the playbook law (all bench operations go through it) |
| `tools/runner/nightly_digest.py` — the docstring examples (~lines 18–19), `compose` (~line 130), the selftest (~lines 431–480) | A-7's target; the byte-exact selftest you must keep green |
| `tools/runner/bundles.py` + `tools/runner/engine.py` — the bundle-write and evidence-capture regions | A-6/A-9's target; locate the journal-slice writer, the app-log-slice marker logic, and the api-assert failure path |
| `tools/runner/README.md` — the nightly section | A-1/A-2's documentation home |
| `tools/scheduler/nexsys-bench-nightly.service` + `.timer` | A-8's target (no KillMode line exists at baseline) |
| `scenarios/rejoin-race-operator.yaml` (whole) | B3.2's target; read the header contract before touching the anchors |
| `scenarios/boot-health.yaml` header + `scenarios/constants.yaml` | The standing grep-token forms and pin conventions the re-pin must match |
| `nexsys-hivemind/context/process/bench-troubleshooting-playbook.md` §8 | Operator-block law for any block you draft into README/docs |

## STOP-on-Mismatch Gates

Read and confirm BEFORE writing; if diverged, STOP and report — never proceed on stale assumptions.

| File | Expected state |
|---|---|
| `tools/nightly.sh` | 276 lines; `exec > >(tee -a "$NIGHTLY_LOG") 2>&1` at ~line 72; `finish()` trap on EXIT/INT/TERM; exit-code contract documented in the header (0/1/2/3/124/137) |
| `tools/runner/nightly_digest.py` | Docstring example lines carry `ON-latency 0.11s`; selftest checks the DP-4 example byte-exact (~lines 431–480) |
| `tools/scheduler/nexsys-bench-nightly.service` | [Service] has Type=oneshot, TimeoutStartSec=3900, TimeoutStopSec=300, and **NO KillMode line** |
| `scenarios/rejoin-race-operator.yaml` | The first positive is `log: "zigbee.device_relinked"` with `same_line: ["${C.command.s31-ieee}"]`, `within: 120s` (~lines 73–76) |

## Part A — B3.1 (the §9 set + night-2 additions). Settled decisions — implement as stated.

- **A-1 (§9 item 1 + the dialect-sweep obligation, format #21):** Repair the INSTALL-6 red procedure: `journalctl --user -u nexsys-bench-nightly.service` → `journalctl --user-unit nexsys-bench-nightly.service`, and name `~/hs-bench/nightly-logs/nightly-<date>.log` as the FIRST fallback, with the sentence "the journal may have rotated; the log file has not." **Sweep the whole repo** (docs/, tools/, README) for the `journalctl --user -u ` pattern and fix every occurrence; enumerate the occurrences in the return.
- **A-2 (§9 item 2):** Document the standing week-1 morning gate in `tools/runner/README.md` (nightly section), §8-compliant, full paths: (1) `pgrep -af 'java|homesynapse'` — **pgrep is the survival gate** (F-1: never `status`-based); (2) `/home/homesynapse/nexsys-bench/tools/bench.sh digest`; (3) `systemctl --user show nexsys-bench-nightly.service -p Result,ExecMainStatus,KillMode,Type`; (4) `journalctl --user-unit nexsys-bench-nightly.service --since <fill in the date before running>`. Two expected-benign notes, verbatim in spirit: `Unit process <pid> (java) remains running after unit stopped` = the KillMode fix WORKING (the Aug-2 exhibit); `Result=exit-code` + `Failed to start` on a FAIL night = the wrapper's DOCUMENTED exit contract (exit 1 = suite FAIL) — the digest is the verdict surface, never the unit status.
- **A-3 (§9 item 3):** Timestamp and line-buffer the wrapper's own output at the tee seam. Constraint: bash + coreutils ONLY (no `ts`/moreutils, no gawk-`strftime` assumption — neither is a proven-present tool on the Pi). Mechanism is yours; it must survive all trap paths and not reorder lines.
- **A-4 (§9 item 4):** A wrapper closing line, emitted as the LAST act of `finish()`: names the exit path (`normal` | `trap-INT` | `trap-TERM` | `restore-failed` | `pre-suite-crash`) and the exit code. Also add one header/README sentence making the exit contract legible to a morning reader (the night-2 journal is the exhibit of why).
- **A-5 (§9 item 5):** The branch-B drift check emits a positive INFO on pass: `[OK] drift check: live == live-basis` (the vacuous-verify pairing rule — success must speak).
- **A-6 (§9 item 6 + G-4, pre-ruled):** **DROP the bundle `journal-slice.txt`** — two nights, two noise-only exhibits (night-1 D-6; night-2's two tailscaled lines); the MANIFEST records the drop and the reason in one line. **Widen the app-log capture so a FAIL leg still lands an `app-log-slice`:** the night-2 FAIL bundle shipped `app-log-slice.log ABSENT` exactly when it was most needed; if the marker-based window read cannot run, degrade to a time-window read over the current boot log — absence is never the designed outcome for a written bundle.
- **A-7 (§9 item 7):** Label the synthetic `0.11s` in the digest docstring examples as SYNTHETIC-EXAMPLE (it invites a false 33× regression reading against real data). The selftest stays 21/21 GREEN; if a byte-exact fixture must change to stay consistent, change it lockstep and name the delta in the return.
- **A-8 (§9 item 8 — licensed by the banked gate):** Fold `KillMode=process` INTO `tools/scheduler/nexsys-bench-nightly.service` [Service], with a comment citing the night-1 cgroup-kill defect and the Aug-2 proof line. Update the unit-header install notes: a refresh re-copy requires `systemctl --user daemon-reload`; Nick's live user drop-in (`~/.config/systemd/user/nexsys-bench-nightly.service.d/killmode.conf`) remains a harmless duplicate until removed at a future trip.
- **A-9 (NEW — the night-2 mint; wire-pin law, format #20):** On a command-class API positive assert failing at a terminal read (the CONFIRMATION_TIMED_OUT class), the runner captures ONE additional GET of the target entity's state into the bundle as `post-window-state.json` (+ a MANIFEST line) — the late-report-vs-no-edge discriminator the night-2 bundle lacked. **The state route + payload dialect pin comes from the FILED wire-measurement corpus, checked BEFORE any source read** (the WCAP detail-read capture + the B2 return §12/F-6 corpus — `/state`'s live nesting is measured on file); cite the file + line you pinned from in the return. Keep the mechanism minimal: one read, failure-path only, no retries, no new config.
- **(§9 item 9 is DONE hub-side** — the old B3 return was SUPERSEDED-stamped at v43 beat 6; do not touch it.)

## Part B — B3.2 (rejoin-race re-pin). Settled decisions.

**The finding (filed; provenance goes into the scenario header):** the S31 NEVER emits `device_announce`/`device_relinked` on a mid-session rejoin — silent-resume proven ×4 including a 13-min outage (pm-handoff v43 beat 7, the F2-close record); relink lines fire only at boot-time adoption-map rehydration (the Aug-2 04:31:43 journal, ×6). The current first positive (S31-scoped `zigbee.device_relinked` within 120s) is therefore structurally unsatisfiable and produced the two [FAIL]s while the SUBSTANCE held (two honest CONFIRMATION_TIMED_OUT under genuine races — banked at C2).

- **DP-B1:** DELETE the `zigbee.device_relinked` positive. Rewrite the header's mid-rejoin-premise and token-sources paragraphs to record the silent-resume finding with its provenance; the identity-law `forbidden` pair (`device_proposed`, `network_parameter_mismatch`) STAYS.
- **DP-B2 (the honest machine floor for the short race cycle):** no log token is PROVEN to fire inside a ~5-s power cycle on this device — the availability flip takes minutes-scale (filed: honest-UNAVAILABLE in-window; replug→AVAILABLE ≤~80 s — the F2 rep, v43 beat 7) and the `zigbee.availability_ping` surface returned EMPTY under a live glance (shelved, v43 beat 7) — so do NOT pin any of them as the premise. The machine asserts remain the disposition-agnostic terminal read (already present) **plus the A-9 post-window state read on the S31 entity**; the power-cycle premise is adjudicated hub-side from the operator act timestamps + bundle markers. State this honestly in the header as the DESIGN (the current "degrade path" language becomes the primary contract).
- **DP-B3:** The long-cycle variant (≥10 min off, where the `zigbee.availability_changed` pair IS filed-proven) is documented in the header as an operator option with its anchors named — but do NOT grow the runner engine to support alternative positive sets. If the engine cannot express it in one scenario file, the note stays documentation-only and says so.

## What to Watch Out For

- **Deploy hygiene rides every bench touch:** exec bits preserved (bench `a791c99` carries `tools/bench.sh` 100755 in-repo), `.gitattributes` LF-only law — never introduce CRLF; the runner consumes paths CRLF-proofed (the R6 class) — keep it that way.
- **The token must never reach a log (L3):** `nightly.sh` deliberately has no `set -x` and rides `api_token` through command substitution only. A-3's timestamping and A-4's closing line must not change that.
- **The trap structure is the invariant:** restore-on-every-exit-path is B3's core law. Any edit inside `finish()` keeps the ordering: stop suite → restore → verify-restored → evidence-copy → digest → (new) closing line → exit code. The closing line is observability, never control flow.
- **Byte-exact selftest:** `bench.sh digest selftest` checks lines byte-exact; A-7 must not break 21/21 (extend lockstep if needed — say so).
- **`suite auto` refuses OPERATOR names structurally** — B3.2 cannot affect the nightly; keep it that way (no tier change).
- **No retuning of `command-confirm-s31`:** the Aug-3 fire adjudicates the armed experiment first (instrument-first on a repeat failure — arc-discipline 28). That scenario is OUT OF SCOPE entirely.

## Test Requirements (desk gates — no Gradle, no Pi)

1. **The nightly.sh stub harness** (the B3 desk pattern — 5-run, TERM-mid-suite trap-restore proof): extend to cover A-3 (timestamps present on every line, order preserved), A-4 (closing line names path + code on normal, TERM, and restore-failed runs), A-5 (the drift [OK] line on pass AND the CONFIG-DRIFT line on the planted-drift fixture — the false-verdict boundary). All existing checks stay green.
2. **Runner fixture gates** for A-6/A-9: a synthetic FAIL bundle proves `post-window-state.json` + its MANIFEST line land, `journal-slice.txt` is absent with its MANIFEST drop-line present, and `app-log-slice` is present on the FAIL path (the night-2 absence reproduced RED at baseline first).
3. **`bench.sh digest selftest`** — 21/21 (or N/N with the lockstep delta named).
4. **Lint:** `bash -n tools/nightly.sh tools/bench.sh`; the scenario passes the runner's own load/lint path (`suite --list` or equivalent — name what you ran).
5. **Red-first prediction, sorted per format #18:** the harness/fixture checks for A-3/A-4/A-5/A-6/A-9 are RED at baseline (the tokens/mechanisms don't exist); the A-7 selftest and the B3.2 yaml edit are GREEN-BY-CONSTRUCTION (preservation + structural), disclosed here.

## Out of Scope

- Any change to `scenarios/command-confirm-s31.yaml` or `command-s31-settle.yaml` (the armed experiment adjudicates first).
- Runner-engine growth beyond A-9's minimal failure-path hook; no alternative-positive-set engine feature (DP-B3).
- constants.yaml ⛔PIN values; core-repo anything; C-3 publishing pre-reqs (post-gate, charter-owned).
- The Pi install/refresh itself (operator, post-embargo, hub-authored block).

## Coder Pushback Welcome

If any pin here contradicts what you read at source (the bundles.py structure, the selftest arithmetic, the engine's assert-failure path), flag it with the evidence rather than silently adapting — the hub owns its authoring misses and rules fast.

## Success Criterion

DONE when: every A-item and DP-B is implemented or explicitly pushback-flagged; all four desk gates GREEN with the red-first evidence quoted; the census (exact file list, M/?? split) + the A-1 sweep enumeration + the A-9 pin-source citation are in the return at `context/audits/2026-08-02_B3.1-B3.2_return.md`; the tree is left UNCOMMITTED on `a791c99` for the hub's two-layer audit.
