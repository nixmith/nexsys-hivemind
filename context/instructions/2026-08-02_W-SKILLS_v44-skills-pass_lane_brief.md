<!--
file: context/instructions/2026-08-02_W-SKILLS_v44-skills-pass_lane_brief.md
purpose: The W-SKILLS v44 pass - the skills currency + optimization lane brief (the v42-v44 law harvest fold + the context-engineering-return-guided optimization of the three live orchestrator skills). Dispatch-ready; runs as a DEDICATED FRESH LANE per the full-bar-at-fresh-context law (the context-deep hub does not run skills passes in-session).
audience: The W-SKILLS lane session (fresh Cowork or desk conversation)
status: DISPATCH-READY (authored v44 beat 4, 2026-08-02)
dispatch-line: "Read nexsys-hivemind/context/instructions/2026-08-02_W-SKILLS_v44-skills-pass_lane_brief.md and execute it."
return: context/audits/2026-08-0X_W-SKILLS_v44-pass_return.md (ONE file; fill XX with the actual date)
timing: the Aug-3-4 Pelton lull. Effects arrive only in NEW sessions after the hub orders the commits AND Nick re-syncs the .claude/skills mirror (Check-9).
-->

# W-SKILLS v44 - The Skills Currency + Optimization Pass (lane brief)

## Section 0 - Laws (bind absolutely)

1. **THE LANE COMMITS NOTHING.** Edit the WRITABLE SOURCE trees only: `nexsys-hivemind/coder/`, `nexsys-hivemind/project-manager/`, `nexsys-skills/orchestrators/nexsys-frontend/`. Never touch `.claude/skills/` (read-only mirror) or your own in-session skill copies (a lag-prone third location - Check-9 law). The hub audits the return, then orders census-exact commits.
2. **DELTA-EDIT WITH RULE CENSUS - NEVER REWRITE.** For every edited file: enumerate every named rule/discipline BEFORE (the in-census), enumerate AFTER (the out-census), and prove equal-or-justified - every prior rule survives by name or its retirement is argued in the return. This is the project's practiced mechanic and it is now independently validated (the context-engineering return's ACE/ICLR-2026 citation: LLM rewrite passes silently destroy accumulated laws). A compression that loses one uncensused rule is a FAIL.
3. **POINTER-NOT-COPY + THE STATE-POINTER LAW.** No project state (HEADs, counts, milestone status, watermarks) enters any skill file; volatile facts live in the spine only. Compression = pointers to detail carriers, never deletion of the only carrier.
4. **TWO-LAYER ON THE RESEARCH INPUT.** The context-engineering return is DELIVERED-UNADJUDICATED - verify every claim you act on at the primary artifact first: MEASURE the actual token counts of the three SKILL.md trees (report before/after numbers); READ the frontend skill's Section-4a-4e against SK-INV-01's own text before calling it a breach; note the return's disclosed gap (the hivemind was NOT staged for it - its description of the harvest came from Nick's words, so THIS brief's Section 2 list is authoritative for harvest content, not the return). Adjudicate the return's claims in your return: VERIFIED / REFUTED / NOT-ACTED-ON, each with evidence.
5. **OUT OF SCOPE (hard):** W-EXEC (authors POST-CHARTER - the standing ruling, now evidence-validated by the return's own vendor-guidance citations; its design question set is a charter input, not lane work). Per-module skills (the return's own ruling adopted: one at a time on T3 demand-evidence, never a batch - zero module skills this pass). Any project-state edits, any spine writes, any bench/core/docs-repo touches.

## Section 1 - Inputs (read in this order)

1. This brief, whole.
2. `nexsys-skills/design/2026-08-02_context-engineering-and-skills-expansion_deep-research_return.md` - THE CANONICAL COPY (the hivemind `context/research/` copy is a recorded duplicate). Its Section-3 audit is your optimization worklist CANDIDATE set (verify per law 4); its Section-5 structural moves apply where they survive law 2.
3. The three live skill trees at SOURCE (the exact files you will edit): `nexsys-hivemind/project-manager/SKILL.md` + `references/` · `nexsys-hivemind/coder/SKILL.md` + `references/` · `nexsys-skills/orchestrators/nexsys-frontend/SKILL.md`.
4. `nexsys-hivemind/context/handoff/pm-handoff.md` - the newest 4 beat blocks ONLY (the v44 arc: the gate intakes, the adjudication) + the v43 beat-11 block (the harvest mint).
5. `nexsys-hivemind/context/process/cowork-environment-model.md` Sections 11-12 (the carriers several harvest items point at).

## Section 2 - The law harvest to fold (authoritative list; each item = operative sentence + pointer at its correct carrier)

Fold into the PM skill (and coder/frontend siblings where the rule touches them), as pointer-form arc-disciplines or reference-file additions - NOT as prose essays:

- **H-1 The Section-11 ghost-commit class, 2 new exhibits + the OVERTAKEN-ORDER form** (v43 beats 4/9; v44 beats 1-2): a commit claimed run is verified at porcelain, never at word; when the hub's own next beat supersedes an un-run order, the order is RETIRED (msg file overwritten with a retirement stamp) and a COMBINED order issues - no ghost, recorded as overtaken. Carrier: env-model Section 11 + the pm-handoff exhibits.
- **H-2 CHAT IS NOT A STORAGE TIER** (v43 beat 9, standing law): anything a fresh session needs lives in a FILE or the dispatch line; verdicts/analysis delivered in-chat are FILED before they are banked (the v44 beat-1 evidence-read filing is the worked exhibit).
- **H-3 THE OPERATOR-PACKET CLASS** (v43 beat 9): Nick's helper sessions get SELF-CONTAINED packet files exactly like lanes - the fresh-context law extends to helper sessions.
- **H-4 The bridge/env-model addenda pointers** (v42 arc): volatile Filesystem-MCP scope, fresh-temp-name staging, spell-the-flag porcelain - verify the skill's pointers at the current env-model text, add none that duplicate it.
- **H-5 ENRICHMENT ASKS STOP AT THE OPERATOR'S FIRST NO** (v44, Nick's 04P ruling): when the operator rules an evidence-enrichment act closed (hardware risk, cost, judgment), the hub records CLOSED and never re-asks; banked-evidence sufficiency is the operator's call. pm-lessons + PM-skill conduct line.
- **H-6 The instrument-first exhibits updated** (v44 beats 1-2): the KillMode gate (survival proven at pgrep + systemd's own accounting) and the s31 broken-park adjudication (pre-stated predictions filed BEFORE the discriminating read) as pointer-form exhibits on the existing arc-disciplines 20/28 - no new rules, updated pointers only if the carrier lines cite exhibits.

## Section 3 - The optimization pass (the return's Section-3 worklist, law-4-verified first)

- Measure and report each skill's body + masthead token counts (before). Target: reduce always-loaded mass WITHOUT losing one censused rule - compression moves: hoist repeated pointers, collapse superseded-exhibit prose to one-line pointer form, move detail to references/ files where a reference already exists. Report after-counts.
- The frontend Section-4a-4e / SK-INV-01 question: verify at the skill's own invariant text; if confirmed, fix per the invariant (likely: move the stale-able content to the spine/carrier it points at); if refuted, record REFUTED with the quote.
- The masthead arc-disciplines list: KEEP the indexed one-list form (rule-census-verified at both prior passes); fold Section-2 items as new indexed entries or exhibit-pointer updates; do NOT convert to prose.
- Frontmatter last-verified: prefix-prepend the pass record with the census arithmetic (N-in/N-out per file), per the prior-pass convention.

## Section 4 - Deliverables + gates

ONE return file (path in the masthead): the per-file rule census (every name, in/out), token counts before/after, the research-return claim adjudication (VERIFIED/REFUTED/NOT-ACTED-ON with evidence), the exact porcelain census of edited files (lock-free porcelain, flag spelled, per repo), and any pushback on this brief (welcome - flag, never silently deviate). Leave both trees UNCOMMITTED. The hub two-layer audits, then orders the commits; effects arrive after Nick's mirror re-sync.
