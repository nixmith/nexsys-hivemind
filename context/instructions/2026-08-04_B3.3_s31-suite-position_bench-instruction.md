<!--
file: context/instructions/2026-08-04_B3.3_s31-suite-position_bench-instruction.md
purpose: B3.3 — the s31 suite-position amendment (Nick's 2026-08-04 fix ruling on the hub REC). Moves command-confirm-s31 from position 3 to position 8 of the AUTO suite so the asserted CONFIRMED-class leg fires in the MEASURED-healthy +40–55 s post-resume band instead of the measured-failing 0.772 s cold-mesh window. ONE data file changes; zero scenario/engine/window changes — this is a precondition amendment in the F1 lineage, not a retune.
audience: the Coder (bench lane, host-CC desk)
status: ISSUE-READY. baseline: bench `41a7a3c` CLEAN (hub-verified 2026-08-04). Return → `context/audits/2026-08-04_B3.3_return.md`. The lane commits NOTHING.
-->

# Coding Task: B3.3 — the s31 suite-position amendment (constants-only)

**Subsystem:** nexsys-bench (scenario suite data)
**Governing record:** Nick's 2026-08-04 fix ruling; the F1 park ruling lineage; the three filed audits (below)
**Phase:** bench maintenance (data amendment + verification gates)

## What This Implements

Three consecutive park-verified nightly FAILs of `command-confirm-s31` (Aug-2/3/4 bundles `…-2026080{2,3,4}T083057Z`) were adjudicated instrument-first: the edges PHYSICALLY OCCURRED (settle off-edge confirms at 143 ms / 3.59 s; coincidence ≈10⁻⁵ against the measured ~1-report/5-min cadence) and the mechanism is **margin exhaustion at the boot-adjacent window** — the leg fires **0.772 s after EMBER_NETWORK_UP** (`network_resumed` 04:30:51.004 → ACCEPTED 04:30:51.776, the Aug-3 suite-boot log) on a coordinator cut mid-close seconds earlier, while the SAME device confirms in 0.14–3.6 s at +40–55 s post-resume. The fix: run the leg at the position whose physics are measured healthy. `command-confirm-s31` moves from 3rd to 8th — immediately before the park (`command-s31-settle`, which REMAINS LAST per the F1 ruling). The suite list is DATA (`constants.yaml auto-suite:`, resolved in KEY ORDER by `runner.py resolve_auto_suite` — never sorted), so this is a one-file amendment plus its rationale block.

## Files to Read Before Starting (minimum read set — the floor is declared; read more at will)

| File | Why |
|---|---|
| `scenarios/constants.yaml` — the `auto-suite:` key (:224) + THE AUTO SUITE OF RECORD block (:157–:223) | The two edit sites; the existing rationale + the quoted `bench.sh suite …` example line that must not drift |
| `tools/runner/runner.py` — `resolve_auto_suite` (:83–:100) + `auto_preflight_refusals` (:102–:125) | Confirms KEY-ORDER resolution (no sort) and the OPERATOR-refusal preflight this change must preserve |
| `tools/runner/nightly_digest.py` — :24–:50 (`LATENCY_LEG` :42) + `parse` region (:88–:113) | Confirms the digest is leg-NAME-keyed, order-agnostic — zero lockstep expected |
| `nexsys-hivemind/context/audits/2026-08-04_B3_night3_blockR-adjudication_v45-beat-3.md` + `…_R8-R9-addendum_and_night4-baseline.md` + `2026-08-03_B3_night3_command-confirm-s31_clean-park-FAIL_evidence-read.md` | The measured basis this amendment cites (the premise is MEASURED, not theorized — format #19/#20 satisfied by these filings) |

## STOP-on-Mismatch Gates (verify, then proceed; divergence ⇒ STOP and report)

| Check | Expected |
|---|---|
| `constants.yaml` `auto-suite:` list | Exactly: boot-health, command-confirm, command-confirm-s31, command-timeout-absent, command-supersession, command-identify-honest, usb-reenumeration, timeout-honesty-no-change, command-s31-settle |
| `runner.py:83–:85` | `resolve_auto_suite` doc states KEY ORDER / never sorted / park-LAST rides the order |
| `nightly_digest.py:42` | `LATENCY_LEG = "command-confirm-s31"` (name-keyed — no positional coupling) |
| bench porcelain | CLEAN at `41a7a3c` before any edit |

## Files to Create or Modify

| Action | File | Description |
|---|---|---|
| MODIFY | `scenarios/constants.yaml` | (1) `auto-suite:` reorder; (2) the AUTO SUITE OF RECORD block gains the dated re-amendment paragraph; (3) the in-comment `bench.sh suite …` example line updated to the new order |

**The new `auto-suite:` order (exact):** boot-health · command-confirm · command-timeout-absent · command-supersession · command-identify-honest · usb-reenumeration · timeout-honesty-no-change · **command-confirm-s31** · **command-s31-settle** (LAST, unchanged — the park).

**The rationale paragraph (author in the block's established voice, dated `RE-AMENDED 2026-08-04 (Nick's suite-position ruling)`), which MUST carry:** the measured mechanism (dispatch at 0.772 s post-`network_resumed` TIMED_OUT ×3 park-verified nights, vs settle confirms 143 ms / 3.59 s at +40–55 s post-resume — 10 s after the usb leg's deliberate dongle power-cycle recovery on two of those nights, so position-8 physics are proven under the harshest in-suite condition); the three-boot choreography note (the leg previously fired 3rd, ~12 s into the suite boot); the audit pointers BY PATH (the three files in Files-to-Read); the explicit statement that the WINDOW, the SCENARIOS, and the park-LAST law are all UNCHANGED; and that the MARGIN WATCH + pre-ruled HUE-RESET contingency paragraphs REMAIN in force (the Hue re-power is an H3 physical act, complementary to this amendment, not superseded by it).

## Settled Decisions (NOT open questions)

- **DP-1:** Position 8 exactly (immediately before the park). Rationale above; the settle-adjacency is lawful — if the confirm's ON lands, the settle's off-edge may itself be clock-held and its TIMED_OUT terminal still PASSes the disposition-agnostic assert while the relay physically parks (the settle header's own contract).
- **DP-2:** ZERO changes to any `.yaml` scenario file, any window, any engine/runner/digest code. A reorder that appears to need code is a STOP, not an improvisation.
- **DP-3:** The archive/history text in the block (the 2026-07-30/31 rulings) is NEVER rewritten — the new paragraph prepends the block's amendment chain in its established newest-first style.

## Verification Gates (desk; the lane runs no Gradle, no Pi)

1. `python3 -B tools/runner/runner.py suite auto --list --bench-sh tools/bench.sh` → **9 legs, the NEW order exactly (mechanical order assert: diff the printed list against DP-1's line), park-LAST, "all load lawfully."**
2. The poisoned-injection preservation probe (the B3.1 shape: OPERATOR name prepended to a SCRATCH constants copy) → `[REFUSED] … UNLAWFUL in the auto suite`, exit 2.
3. `python3 -B tools/runner/nightly_digest.py --selftest` → **21/21 byte-exact** (order-agnostic by construction — a selftest delta means you touched something out of scope: STOP).
4. The CR/LF scan on the touched file (`git grep -I -l --untracked $'\r' -- scenarios/constants.yaml` → zero).
5. Red-first disclosure per format #18: this is a DATA amendment with no red-able fixture — the gates above are the mechanical evidence; predict and disclose green-by-construction.

## Census (the sweep-guard)

**Exactly 1 porcelain entry: ` M scenarios/constants.yaml`.** Everything else byte-untouched — explicitly including `command-confirm-s31.yaml`, `command-s31-settle.yaml`, all runner/digest/wrapper code, both units. The armed-experiment discipline continues: no retune anywhere.

## Post-landing rider (hub-owned; recorded so the return can point at it)

Hub audit → commit order → the Pi pull block → **tonight's 04:30 fire IS the verification gate** (G-E form: one full NIGHTLY, never a manual run). **Pre-stated prediction: digest `8/9 PASS · 1 SKIP(hue-online) · ON-latency present` with the latency value entering the distribution. A FAIL at position 8 is a REAL finding — it escalates to the pre-ruled HUE-RESET contingency ruling, never a retry.**

## Coder Pushback Welcome

If the constants block's structure, the runner's resolution, or anything else contradicts this instruction's pins, STOP and report with the evidence — the pins were verified at `41a7a3c` on 2026-08-04, but your read at launch is the fresher instrument.

## Out of Scope

Scenario YAML edits · window/timeout changes · engine/runner/digest code · the HUE-RESET act itself (H3, Nick's hands) · core repo anything · the Pi (the hub's post-audit block owns deploy).

## WUCP Phase 1

Standard: coder-handoff DELIVERED entry (Deferred Build Gate: N/A-gradle — the desk gates of record are §Verification above, run in-session) + lessons append if any + the return to `context/audits/2026-08-04_B3.3_return.md` with the census, the gate outputs quoted, and deviations severity-honest.
