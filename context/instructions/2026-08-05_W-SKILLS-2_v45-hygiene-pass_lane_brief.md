<!--
file: context/instructions/2026-08-05_W-SKILLS-2_v45-hygiene-pass_lane_brief.md
purpose: W-SKILLS-2 — the chartered v45 hygiene pass over the skills + governance layer: fold the v45 arc's harvested laws and lessons into the role skills, discharge the R-2 queued touches BY NAME, sweep stale/overtaken references, and fold Nick's mission/market posture into the strategy-facing skill sections D5-lawfully. Successor to skills-pass-2 (return: context/audits/2026-07-30_skills-pass-2_return.md); this brief IS the chartered lane that pass's §3E finding asked for.
audience: a FRESH review/edit lane (host-CC desk, dispatched by Nick)
status: ISSUE-READY. Baseline: nexsys-skills + nexsys-hivemind at the HEADs you derive at launch (state them in the return). Due: 2026-08-10 EOD (before the Aug-12–13 charter; no earlier gate depends on it). Return → `context/audits/2026-08-05_W-SKILLS-2_return.md`. The lane commits NOTHING.
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-05_W-SKILLS-2_v45-hygiene-pass_lane_brief.md and execute it."
-->

# Lane Brief: W-SKILLS-2 — the v45 hygiene pass (skills + governance currency)

## §0 — Laws (bind this lane)

The lane COMMITS NOTHING — worktree edits only; the hub audits and orders the commits. NO attribution trailers. Tokens travel by file-path reference, never in files or messages. Lock-free porcelain with the flag SPELLED (`git --no-optional-locks status --porcelain`), split per-repo, in the return's census. Delta-edits only — no rewrites, no reorganizations, no new Tier-3 artifacts (skills-pass-2 §3D precedent: a new artifact wants its own chartered lane). Where an instruction here conflicts with what you find at the bytes, FLAG it in the return's pushback section — evidence over instruction, never silent deviation. The D5 language law binds every sentence you write: state posture and verified fact; never write capability or verification claims the record doesn't hold.

## §1 — Writable trees (EXACT; everything else read-only)

1. `nexsys-skills/` — the same three writable trees as skills-pass-2 (the PM, Coder, and shared-conventions trees), **PLUS, this pass only, `nexsys-skills/README.md`** — the skills-pass-2 §3E SD-5 mis-citation gets its chartered touch here (fix the citation; nothing else in the README). `MANIFEST.yaml` remains OUT.
2. `nexsys-hivemind/context/process/*` and `nexsys-hivemind/context/lessons/*` — only where a §2 harvest item lands.

NOT writable, no exceptions: the spine (`pm-handoff.md`, `PROJECT_SNAPSHOT.md`), the criteria ledger (J1 FROZEN), `context/audits/*`, `context/instructions/*`, `context/handoff/*`, `context/research/*`, `context/strategy/*`, and the core/bench/docs repos (read-only for citation).

## §2 — The harvest (fold each item where it lives; cite file:line for every edit)

1. **THE NEW LAW (v45 beat 8, the stall):** *a dispatched lane is verified at its RETURN ON DISK, never at word* — fold into the PM skill's dispatch/audit discipline beside the existing commit-claimed-run-verified-at-porcelain law (they are siblings: one for commits, one for lanes), and into the Coder skill's WUCP section (a WU is not DELIVERED until the return file exists at its named path).
2. **Bundle-stamp-as-deploy-state (v45 beat 8, night-5):** a nightly-gate read states WHICH suite order/code actually ran — derived from the bundle stamp — BEFORE adjudicating any leg as a fix test. Fold into the gate-reading guidance wherever the skills/process layer carries it (locate; likely the Coder skill's bench section or the process doc that owns gate reads).
3. **The delivery-phase gap (REV-1 S-1, CONFIRMED at source):** DISPATCHED ≠ delivered — the radio's send-status ack is requested but discarded (`ZclIngestionUnit` :266–268 class). Verify the DO-NOT-SAY list already carries the no-delivery-proof-claims entry (added v45 beat 7); add a one-line concept pointer (to `context/audits/2026-08-04_REV-1_audit_v45-beat-7.md`) wherever confirmation language is taught. Do NOT restate the analysis — pointer, not copy.
4. **The A-9 view-lag class (v45, B3.1 verification):** verify it is present in `context/lessons/coder-lessons.md`; if absent, add the one-paragraph lesson with its audit pointer; if present, cite and move on — no duplicate.
5. **R-2's queued touches, discharged BY NAME (queued "for the NEXT skills touch per R-2's own wording" — this is that touch):** (a) the PM-skill **item-33 carrier note** — the env-model §11 fold landed at v45 beat 1, so retire the stale carrier note; (b) the **FE-mirror Check-9 third pair** — adopt the third format-enforcement pair exactly as R-2 recorded it.
6. **R-5 presence check:** the standing rule *re-invoke the role skill after any auto-compaction* — verify present in BOTH the PM and Coder skills; add the one-liner where absent.
7. **Mission/market posture (Nick's directive, v45 close), folded D5-LAWFULLY into the PM skill's strategy posture:** the hub thinks independently and critically, stays constantly market-aware, and is building both the company and the flagship product — we are helping real households, and doing so basically for free. Write it as POSTURE (how the hub orients), never as capability claims; the physics-aware direction cites its seed (`context/research/2026-08-04_physics-aware-core_strategic-seed_charter-input.md`) as a charter INPUT, not as adopted strategy — the Aug-12–13 charter adopts.
8. **Stale sweep (grep-driven, delta-edit):** across the writable trees only — references to the s31 leg's OLD third position or the pre-B3.3 suite order (the ruled order is: `command-confirm-s31` 8th, `command-s31-settle` LAST); orders/instructions marked OVERTAKEN that a skill still cites as live; dates or SHAs presented as current that the spine has since moved past. Fix by delta; list every hit (fixed or lawfully-left) in the return.

## §3 — STOP-on-mismatch gates

| Check | Expected |
|---|---|
| `context/audits/2026-07-30_skills-pass-2_return.md` exists | yes — read its §1–§5 before editing (the writable-tree fence and the queued touches are defined against it) |
| The R-2 record of Check-9's third pair | located and quoted in the return before adoption (if you cannot find R-2's wording, STOP that item and flag — do not invent the pair) |
| Porcelain of both repos at launch | CLEAN at the derived HEADs (state them); anything dirty ⇒ STOP and report |

## §4 — Return shape (the skills-pass-2 shape, kept)

§1 What was done (per harvest item: file:line, before→after) · §2 Rule census (re-derive counts adversarially against the pre-edit originals) · §3 Token counts, measured (per touched skill file, before/after) · §4 Adjudication of each harvest item (DONE / ALREADY-PRESENT / STOPPED-flagged) · §5 Any finding that reframes the pass · §6 Pushback and recommendations (flagged, never silently deviated) · §7 Porcelain census (lock-free, flag spelled, per repo) · §8 State at close.

## §5 — Out of scope

W-EXEC (post-charter, untouched) · the 5k tiering redesign (a NAMED PROJECT awaiting a Nick+hub ruling) · per-module skills · `MANIFEST.yaml` · all spine files and J1 · anything in the core/bench/docs repos · new skills or new Tier-3 artifacts · the pm-handoff rotation (the v46 hub's dedicated beat, not yours).
