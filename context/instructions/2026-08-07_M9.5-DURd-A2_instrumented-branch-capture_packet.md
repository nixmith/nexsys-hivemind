<!--
file: context/instructions/2026-08-07_M9.5-DURd-A2_instrumented-branch-capture_packet.md
purpose: DURd-A2 — the H-E discriminator: an instrumented DIAGNOSTIC-BRANCH CI capture (the Coder's recommended instrument, hub-ratified v48 beat 7). Two halves: the Coder applies the documented diagnostics as real test-only instrumentation; Nick commits it to a THROWAWAY branch + PR so CI runs it on the red environment. The branch NEVER merges.
audience: the M9.5-DURd coder lane (resumes; Part 1) + Nick (Part 2)
status: ISSUE-READY. baseline: core `8955e23`, clean tree (the lane's Phase-A scratches were reverted).
return: the lane appends §A2 to context/audits/2026-08-07_M9.5-DURd_return.md; the CAPTURE itself is the CI run's test-reports artifact, read by the hub from disk as before.
-->

# DURd-A2 — the instrumented-branch capture (the H-E discriminator)

**GOAL:** one red CI run that carries the generalized fork capture, so the failure names its own mechanism. H-E predicts: at the `:115` timeout, `state_projection` is NOT LIVE (or its checkpoint trails the motion report), ZERO `state_changed` events exist in the store, both labels are INTACT, and zero `command_issued` exist. The residual-label arm predicts labels wiped. An engine stall predicts `state_changed` present + labels intact + no `command_issued`.

**PREMISE-PROVENANCE (§8 (5)):** ALREADY: Phase A went 11/11 green at desk with instrument fidelity proven; label-wipe is source-closed at `8955e23` per the return §5 (hub layer-2: the four-id `awaitRuntimeSubscribersLive` set and StateProjection's LIVE-only publish verified verbatim at source, v48 beat 7). THIS RUN ADDS: the same capture the desk could not exercise, executed on the environment that actually reds — a diagnostic branch, never the main gate.

## Part 1 — the Coder lane (resumes; test-only instrumentation)

1. Re-verify G-1 (baseline `8955e23`, clean porcelain).
2. Apply the diagnostics documented in the return (the stood-ready scratch), generalized per the escalation: instrument `HeroLoopHardwareFreeIT` so that ANY of the :98/:102/:115 awaits timing out prints, BEFORE the throw, a clearly delimited block (`=== DURD-A2 FORK CAPTURE ===`) to stdout carrying: (a) `state_projection` subscriber mode + checkpoint (and the four gated ids' modes for contrast); (b) count of `STATE_CHANGED` events in the store; (c) count of `STATE_REPORTED` for the motion entity; (d) BOTH entities' current registry records' labels; (e) count of `COMMAND_ISSUED`; (f) the store's max position. Stdout lands in the test report — the same artifact route the hub already reads.
3. §4c holds (no new time APIs); assertions/timeouts byte-unchanged — the instrumentation OBSERVES, it never alters the race (no added awaits, no ordering changes on the pass path).
4. Write the branch commit message to `../_scratch/2026-08-07_core_durd-a2-diag_commit-msg.txt` AFTER the diff exists: first line `DIAGNOSTIC (do not merge): DURd-A2 fork-capture instrumentation - HeroLoopHardwareFreeIT stdout capture at await timeouts; test-only; reverts with the branch.` plus a `Stages exactly N:` census line listing the exact paths.
5. Append §A2 to the return (what was instrumented, the census, the predictions restated). Leave the tree dirty with exactly that census. Commit NOTHING (unchanged law — Nick commits).

## Part 2 — Nick (the branch + PR; ~5 minutes)

**Block N-1 — verify the census (own block):**
```
# desktop repo (MINGW64) — homesynapse-core
cd ~/Desktop/Code/ClaudeFolder/homesynapse-core
git --no-optional-locks status --porcelain
```
Expected: EXACTLY the paths the lane's commit-message census names (test files only). ⏺ RECORD.
```
# STOP-GATE: any path outside the lane's stated census — STOP, paste to the hub, commit nothing.
```

**Block N-2 — the diagnostic branch (never merges):**
```
# desktop repo (MINGW64) — homesynapse-core
cd ~/Desktop/Code/ClaudeFolder/homesynapse-core
git checkout -b diag/durd-a2-instrumented-capture
git add lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/HeroLoopHardwareFreeIT.java
git commit -F ../_scratch/2026-08-07_core_durd-a2-diag_commit-msg.txt
git push -u origin diag/durd-a2-instrumented-capture
git checkout main
```
(If the lane's census names more files than the one above, `git add` exactly those instead — the census line in the msg file is the authority.)

**Block N-3 — the PR + the capture:** open a PR from `diag/durd-a2-instrumented-capture` titled **"DIAGNOSTIC — DO NOT MERGE: DURd-A2 fork capture"** (the PR trigger is proven — CI #203 ran on the dependabot PR). Let CI run. **Expected: RED at the hero-loop test — that is the instrument working.** Download the run's `test-reports` artifact and extract it exactly as before; the hub reads the `=== DURD-A2 FORK CAPTURE ===` block from disk. If the branch run goes GREEN: re-run up to twice (lawful HERE — a diagnostic branch is an instrument, not the gate of record); three greens ⇒ paste that fact, it is itself a datum (environment variance returned).

**Anti-actions:** never merge the PR; no re-runs on MAIN; nothing touches the S31 or the bench.

**After adjudication (hub's word):** close the PR unmerged; `git push origin --delete diag/durd-a2-instrumented-capture` at leisure.

## The pre-ruled fork (so the next beat is one word)

- **H-E CONFIRMED** (projection not-LIVE/trailing + zero `state_changed` + labels intact) ⇒ **Phase B′ authors immediately:** extend the IT harness's liveness gate to include `state_projection` (the DURc class — test-only, an await added to the existing helper family; the red = this capture, the green = the fixed run + Phase-C gates), main push, **CI green closes the evidence read.**
- **Labels WIPED** ⇒ the label arm revives with the capture as its instrument; LC-LABEL-LOG (Phase B as originally authored) re-arms.
- **`state_changed` PRESENT + labels intact + no `command_issued`** ⇒ an engine-side stall — a new evidence read, per-hypothesis predictions filed before any further move.
