<!--
file: context/instructions/2026-08-07_M9.5-DURd-Bprime_state-projection-liveness-window-closure_coding-instruction.md
purpose: M9.5-DURd-B′ — the ruled WINDOW-CLOSURE WU (Nick's v49 beat-1 GO on the three-green A2 outcome): gate `state_projection` LIVE in the four IT liveness-helper copies. Test-only; closes the source-proven liveness-gate gap; NOT "the fix for #206" (mechanism UNCONFIRMED-PROBABLE-H-E — the caveat travels).
audience: the M9.5-DURd coder lane (host-CC per D12; the SAME lane resumes)
status: ISSUE-READY (disposal language corrected pre-issue at the beat-1 ask — the order was un-run; law 10). baseline: core `8955e23` (MAIN, clean tree — the `diag/durd-a2-instrumented-capture` branch is DISPOSED at dispatch: PR closed unmerged + branch deleted, Nick's v49 beat-1 ruling; never recreate it)
return: append §B′ to `context/audits/2026-08-07_M9.5-DURd_return.md`
-->

# Coding Task: M9.5-DURd-B′ — gate `state_projection` LIVE in the IT liveness helpers (the window-closure)

## What This Implements

The authority chain, so the framing is exact: your return §7 recommended this closure → the A2 packet pre-ruled it as Phase B′ → the v48 beat-9 extended pre-ruling framed the three-green disposition → **Nick ruled GO at v49 beat 1** (PR #4 GREEN ×3 on `af82c16`; the environment-variance datum; re-runs exhausted; no capture — pre-priced).

**The binding reframe:** this WU **closes the source-proven liveness-gate gap** — `awaitRuntimeSubscribersLive()` gates four subscriber ids and not `state_projection` (your §7; hub layer-2 verified verbatim, v48 beat 7), so nothing orders the projection LIVE before the first report pump, and a pre-LIVE projection permanently swallows a derived `state_changed` edge (`StateProjection` LIVE-only publishing, :57/:399–:400/:475–:478). It is **not** "the fix for #206": that red's mechanism remains **UNCONFIRMED-PROBABLE-H-E**, and the evidence read closes carrying that caveat verbatim. A hole-closure, not a paper-over: no assertion, timeout, or message byte changes anywhere.

## Files to Read Before Starting (minimum read set — the floor is declared; read more freely)

- This instruction, whole.
- `nexsys-hivemind/context/audits/2026-08-07_M9.5-DURd_return.md` §§6–7 + §A2 (your own filings — the mechanism and the id set).
- `nexsys-hivemind/context/audits/2026-08-07_CI-red_HeroLoop-613_evidence-read_v48-beat-4.md` §§7–10 (the thread of record incl. the three-green adjudication and the close condition).
- `lifecycle/lifecycle/MODULE_CONTEXT.md` — Gotchas + Phase 3 Notes sections.
- The four helper regions + their call sites (table below), and `HomeSynapseCore` :183 (`PROJECTION_SUBSCRIBER_ID = "state_projection"`).

## Files to Create or Modify (the Files table governs — addition #2; census: exactly 4 M)

| File | Delta |
|---|---|
| `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/HeroLoopHardwareFreeIT.java` | `awaitRuntimeSubscribersLive()` (:481) gains the fifth conjunct `&& subscriberMode("state_projection") == SubscriberMode.LIVE` |
| `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/RestartHonestyIT.java` | same delta in its identical copy (:223) |
| `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/ZigbeeReplaySafetyIT.java` | same delta in its identical copy (:225) |
| `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/RunPipelineReplaySafetyTest.java` | its variant copy (:420) gains the fifth id per ITS OWN idiom: a `SubscriberMode projection = subscriberMode("state_projection");` local + `failOnSuspended("state_projection", projection);` + `&& projection == SubscriberMode.LIVE` in the conjunction |

Line numbers were verified at source at authoring (2026-08-07, `8955e23`); re-verify at execution — they are anchors, not pins. The three identical copies and the one variant were each read at authoring; if any copy's shape differs from the description at your read, that is a `[REVIEW]`, not a silent adaptation.

## Technical Specification

- The subscriber id literal is `"state_projection"` (`HomeSynapseCore` :183, `PROJECTION_SUBSCRIBER_ID`). Use the string literal exactly as the existing four ids do — do not import or reference the private constant.
- Preserve each helper's structure byte-for-byte outside the delta: the poll bound (250), `sleepBriefly()`, the throw, and its message `"runtime subscribers did not reach LIVE within ~5s"` all stay unchanged. The only delta per file is the fifth id's participation in the readiness conjunction (plus, in RunPipelineReplaySafetyTest, its idiom-matching local and `failOnSuspended` line).
- Expected semantics shift, stated for the record: on an environment slow enough that the projection lags boot (the H-E window), the suite now reds AT THIS GATE with the honest readiness message instead of silently at a downstream `awaitTrue` — that conversion is the point of the WU.

## Phase A — per-file conflict census (BEFORE any edit)

For each of the four files, verify at source and record in §B′:
1. The `awaitRuntimeSubscribersLive()` call site precedes the first report pump / any dependence on a derived `state_changed` in every test method that reaches it.
2. No test in the file deliberately exercises PRE-LIVE projection semantics downstream of the call such that the strengthened gate would mask or alter what the test asserts. The two replay-safety files are the careful reads — their REPLAY/TRANSITION assertions must all sit on paths where the projection lawfully reaches LIVE by the await point.

**Any conflict ⇒ STOP for that file: report it in §B′, edit nothing in it.** Expected: zero conflicts, four edits.

## Phase B — the edits

The four deltas exactly as the Files table specifies. Nothing else.

## Phase C — gates (in-session; the CC-lane full-`check` grant carries over from DURd)

1. `:lifecycle:lifecycle:test` forced-fresh — expect 60 completed / 0 failed; run ×2 (the second `--rerun`, mtime-proven against the final bytes).
2. The module's Spotless check — zero reformats.
3. Full `./gradlew check` — GREEN; note the actionable-task count against the 156 record (drift is a `[INFO]`, not a blocker).
4. §4c grep over the four touched files — zero new time APIs (the edit adds none by construction; prove it anyway).
5. Census: `git --no-optional-locks status --porcelain` = exactly the 4 M paths above. **Commit NOTHING** (standing law — the hub audits first; the core commit message authors post-diff; Nick commits and pushes).

## Red-first disclosure (addition #18)

All four edits are **green-by-construction at desk, disclosed**: a strengthened readiness gate cannot red where the desk always reaches LIVE promptly, so there is no honest desk red leg for this class. The red leg of record is the CI history (the 3/3 Aug-7 morning reds — DP-3, addition #18's DURc precedent). **Do not manufacture a red.**

## P2 Consumer/Pin (Fan-Out) Survey (result)

No enum, registry, event-type set, sealed hierarchy, or counted-or-pinned set is touched. Zero count pins live in the four helpers. No event publish site is added, removed, or relocated (⇒ no behavioral publish-count sweep owed). No module-info/JPMS delta (test-source-only). ARCH-RULE-REACH: no module/layer boundary is crossed and no new API is used; no ArchUnit rule reaches these helper bodies — **zero collisions** with the full-`check` success criterion.

## What to Watch Out For

- **The `diag/durd-a2-instrumented-capture` branch is GONE by the time you run** (PR closed unmerged + branch deleted — Nick's v49 beat-1 disposal ruling). Do NOT recreate it or cherry-pick from it; the A2 instrumentation is preserved verbatim in the return §A2 if a future red ever needs it again. All work happens on `main` at `8955e23`.
- `awaitRegistryProjectionCaughtUp()` is the DIFFERENT helper (the DURc barrier) — untouched.
- `RunPipelineReplaySafetyTest`'s variant idiom (per-id locals + `failOnSuspended`) — match it exactly; do NOT normalize the four copies into a shared helper (out of scope; the duplication is the harness's existing idiom).
- The helper's ~5 s ceiling stays as-is. If the fifth conjunct ever exhausts it on a slow environment, the resulting red carries the honest readiness message — that is designed behavior, not a regression to soften.
> **Tests must inject `Clock`.** Do NOT use `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, or `System.currentTimeMillis()` in this module's test code. Use `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` injected via constructor/`@BeforeEach`. **Enforcement reach:** `NO_DIRECT_TIME_ACCESS` runs from `com.homesynapse.app`'s test classpath, so it mechanically catches PRODUCTION code in every non-whitelisted module (plus app's own tests) — it does **not** scan this module's test source set. Clock-injection here is a self-enforced project convention that PM review, not `./gradlew check`, enforces.

## Coder Pushback Welcome

Flag concerns rather than silently implementing — especially: a Phase-A conflict in either replay-safety file; any helper copy whose shape differs from this instruction's description at your read; any reason the fifth-conjunct placement is wrong at source. Evidence-based pushback is signal, never insubordination.

## Out of Scope

Production code of any kind; LC-LABEL-LOG (the named post-gate hygiene shelf); any assertion/timeout/message change; helper consolidation; the A2 instrumentation (it lives on the diag branch only and dies with it); `ci.yml` and every workflow file; the bench and the S31.

## Work Unit Completion (WUCP Phase 1)

Append **§B′** to `context/audits/2026-08-07_M9.5-DURd_return.md`: the Phase-A census result per file, the four deltas summarized, the gate records (counts, forced-fresh mtime proof, the full-`check` actionable count), the exact census, and any `[INFO]`/`[REVIEW]` items. Leave the tree dirty at exactly 4 M. The hub's two-layer audit precedes any commit; then the core msg (post-diff), Nick's commit + main push, and **CI green on that push = the gate of record — it closes the CI-red evidence read with the UNCONFIRMED-PROBABLE-H-E caveat traveling.**

## Success Criterion (binary)

1. Census exactly 4 M (the table's paths, nothing else).
2. `:lifecycle:lifecycle:test` 60/0/0 forced-fresh ×2 on the final bytes.
3. Full `./gradlew check` GREEN.
4. §B′ appended to the return with the Phase-A census recorded per file.
5. Zero production/assertion/timeout/message deltas (the §4c grep and the diff both prove it).

Then, operator-side: main push → the CI verdict banks at the next hub beat (law 16); green closes the evidence read.
