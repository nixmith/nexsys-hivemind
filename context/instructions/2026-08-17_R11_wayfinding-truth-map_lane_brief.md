<!--
file: context/instructions/2026-08-17_R11_wayfinding-truth-map_lane_brief.md
purpose: R-11 — the ratified wayfinding rebuild (v53 beat 4, adopted with THE ONE-HOP BAR): build the role-indexed truth-map/index + the edit-procedure register (OWNER column per the maintenance-loop law), then disposition the seven ⚠-quarantined wayfinding files (rebuild-or-archive, DELETE NOTHING), and land the named v53/v54 wayfinding rows (hardlink twins · return-filename convention · the backlog head-note). The W-COHERE navigation audit is the charge document; this brief points, never restates.
audience: a FRESH lane (fresh Cowork over the bridge, or host-CC desk; dispatched by Nick). NEVER the hub in-session (the fresh-lanes law).
status: ISSUE-READY. Baseline: nexsys-hivemind at the HEAD you derive at launch (state it in the return). Due: Fri 2026-08-21 EOD for the map + register + dispositions (weekday-parallel under the schedule fences — R-1/R-2 eat first on any tightening); the per-file REBUILDS may stage beyond Friday if the return says which and why.
return: context/audits/<filing-date>_R11_wayfinding_return.md — dated by the operator-day you FILE it (America/Chicago), never by the due date. The lane commits NOTHING.
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-17_R11_wayfinding-truth-map_lane_brief.md and execute it."
-->

# Lane Brief: R-11 — the wayfinding truth-map + edit-procedure register, under THE ONE-HOP BAR

## §0 — Laws (bind this lane)

The lane COMMITS NOTHING — worktree edits/creations only; the hub audits and orders the commits. **DELETE NOTHING — rebuild-or-archive only** (in-tree `mv` to `archive/` is the only removal form; Nick's `git add -A` commits it as a rename). NO attribution trailers. Lock-free porcelain with the flag SPELLED (`git --no-optional-locks status --porcelain`), in the return's census. Pointer-not-copy governs every artifact you write (`context/process/truth-hierarchy-and-pointer-not-copy-discipline.md` is the constitution here — read it before authoring). Where an instruction conflicts with what you find at the bytes, FLAG it — evidence over instruction. After ANY auto-compaction, re-invoke your role skill before the next act (law 13).

## §1 — THE BAR (falsifiable; the return must DEMONSTRATE it, not assert it)

**A fresh session handed ONLY the truth-map reaches the correct, CURRENT artifact in ONE hop** — for every row of the W-COHERE top-10 gap list and for every role × task-class the map indexes. The return demonstrates the bar row by row: quote the map line, name the artifact it lands on, cite that artifact's currency stamp. Any lookup needing a second hop (map → stale file → banner → real file) means the MAP is wrong — fix the map or record exactly why the two-hop form is load-bearing. "The map exists" does not pass this bar; the demonstrated hops do.

## §2 — The charge

**(a) The role-indexed truth-map/index** — one file (placement PROPOSED by you; REC `context/truth-map.md`; the hub ratifies placement at intake). For each role (executive/strategy · PM/hub · coder · frontend · bench-operator · navigator/operator-session) × task-class: where truth lives (repo + path + which SECTION), and what outranks what (the spine > prompts > skills-mastheads rule stated once). Volatile state is POINTED AT, never copied in.

**(b) The edit-procedure register** — one file (REC `context/process/edit-procedure-register.md`): per file-class (spine · ledger/J1 · skills sources · MODULE_CONTEXT · instructions/briefs · audits/returns · operator packets · strategy · decisions records), WHO writes it, HOW (the write route + verification), and an **OWNER column** per the maintenance-loop law (the W-COHERE finding: every wayfinding file outside a pass discipline drifted — a file with no owner and no cadence is pre-drifted).

**(c) The seven ⚠-quarantined files** (bannered v50 beat 6; each banner cites its W-COHERE gap row — quote the banner in the return before touching the file): `context/governance/project-instructions.md` · `context/canonical-paths.md` · `context/strategy/README.md` · `context/process/decision-rationale-index.md` · the WUCP partially-stale form (`context/protocols/work-unit-completion-protocol.md`) · `context/open-questions.md` · `context/handoff/cross-agent-notes.md`. For EACH: rule REBUILD (delta or fresh content at the same path, banner lifted with the rebuild recorded) or ARCHIVE (mv to the sibling `archive/` with a pointer stub ONLY if the map now carries its whole function). REC on the record: cross-agent-notes → ARCHIVE-WITH-POINTER (the beat spine absorbed the channel — census ACTIVE 0 since 2026-07-27); the rest are yours to rule with argument.

**(d) The named v53/v54 rows (new; each lands as ONE precise touch):**
1. **HARDLINK twin-location enumeration** — two exhibits on record: `nexsys-skills/README.md` (nlink=2, W-SKILLS-2) and the FE lane's seven-file list in core (`ci/frontend.yml`, `scripts/build-tokens.mjs`, `src/components/{ThemeToggle,DevPanel}.tsx`, `src/lib/theme.ts`, `src/styles/fonts.css`, `FRONTEND_DOCTRINE.md`, `src/styles/fonts/README.md`, all under `web-ui/dashboard/`; FE return §7 dev-9). Enumerate the twins host-side (`find <root> -samefile <file>`-class, read-only), record every pair, and PROPOSE the standing rule (break-links-on-next-edit vs document-the-pairs); the hub ratifies. No link surgery in this lane.
2. **The return-filename convention row** — returns dated by FILING day, never due day (minted v54 beat 1). The two grandfathered exhibits `context/audits/2026-08-20_RS1_…` and `2026-08-21_RS2_…` are COMMITTED history — do NOT rename them; the map carries the two aliases with their true filing date (2026-08-17).
3. **The backlog head-note** — `context/planning/phase-3-milestone-backlog.md` gains ONE line in its head currency note: the operative post-gate program is the S-10 close (`context/assessments/2026-08-14_S10_close_ranked-program.md`, R-1..R-15 as ratified 2026-08-16); milestone rows below remain the historical record.
4. **Coder-handoff supersession NOTE (map-side only — do NOT edit coder-handoff, it is the Coder's file):** the map's coder row states that the newest DISPATCH entry outranks any prior entry's yield-queue (the B′ entry's queue predates the ratified program; the R-1/R-2 dispatch supersedes it by name).

## §3 — Inputs BY NAME (read before authoring; point, never restate)

`context/audits/2026-08-09_WCOHERE_navigation-audit_return.md` (the top-10 gap list + the maintenance-loop law — THE charge document) · the seven banners themselves (§2c) · `context/process/truth-hierarchy-and-pointer-not-copy-discipline.md` · pm-handoff v50 beat 6 (the rotation/quarantine record) + v53 beat 7 (the hardlink mint) · the v54 prompt §1 (what a launch actually reads — the map must agree with the real launch route) · `context/handoff/archive/README.md` if present (archive conventions).

## §4 — STOP-on-mismatch gates

| Check | Expected |
|---|---|
| The W-COHERE return exists at its §3 path | yes — read WHOLE before any edit; absent ⇒ STOP the lane |
| Every §2c file carries its ⚠ banner | quote each banner in the return BEFORE touching the file; a missing banner ⇒ STOP that file and flag |
| Porcelain at launch | CLEAN at the derived HEAD (state it); dirty ⇒ STOP and report |
| Any act that would DELETE a file or edit the spine/ledger/skills trees/core/bench/docs | STOP — out of scope by law |

## §5 — Return shape

§1 The map + register (paths, with the placement argument) · §2 THE BAR DEMONSTRATED (per gap-row + per role: the quoted hop) · §3 Per-file dispositions for the seven (REBUILT / ARCHIVED-WITH-POINTER / DEFERRED-with-reason; banner text quoted; zero deletions affirmed) · §4 The §2d rows (the hardlink pair table + proposed rule; the convention row; the head-note text) · §5 Pushback and recommendations · §6 Porcelain census (lock-free, flag spelled) · §7 State at close.

## §6 — Out of scope

The skills trees (R-12's ground, running in parallel — coordinate by NOT touching `context/process/cowork-environment-model.md` §11/§12 or `context/lessons/*`, which R-12 owns this week) · the spine (`pm-handoff.md`, `PROJECT_SNAPSHOT.md`) and the criteria ledger (J1) · the rotation (hub-owned) · core/bench/docs edits (the hardlink enumeration is read-only) · link surgery · renaming committed files.
