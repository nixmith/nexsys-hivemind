<!--
file: context/instructions/2026-08-17_W-SKILLS-3_R12_currency-pass_lane_brief.md
purpose: R-12 / W-SKILLS-3 — the ratified living-skills currency pass (v53 beat 4, adopted AS FENCED): fold the v51–v53 harvest (H7–H11 + the mechanics mints) into the role skills where each item lives, discharge the queued env-model fold, FINALLY adjudicate the 2026-08-02 context-engineering deep-research return, and run an Anthropic-guidance refresh — all under THE THREE FENCES below. Successor to W-SKILLS-2 (return: context/audits/2026-08-05_W-SKILLS-2_return.md; audit: context/audits/2026-08-06_W-SKILLS-2_audit_v46-beat-3.md); the shape of that pass is the shape of this one.
audience: a FRESH review/edit lane (host-CC desk or fresh Cowork over the bridge; dispatched by Nick). NEVER the hub in-session (the fresh-lanes law).
status: ISSUE-READY. Baseline: nexsys-skills + nexsys-hivemind at the HEADs you derive at launch (state them in the return; at authoring: skills f0b7a43 · hivemind at the v54 beat-1 commit). Due: Thu 2026-08-20 EOD (weekday-parallel under the schedule fences — R-1/R-2 eat first on any tightening; this lane never costs a bench weekend).
return: context/audits/<filing-date>_W-SKILLS-3_return.md — dated by the operator-day you FILE it (America/Chicago), NEVER by the due date (the RS-1/RS-2 filename lesson, v54 beat 1). The lane commits NOTHING.
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-17_W-SKILLS-3_R12_currency-pass_lane_brief.md and execute it."
-->

# Lane Brief: W-SKILLS-3 (R-12) — the living-skills currency pass, THREE-FENCED

## §0 — Laws (bind this lane)

The lane COMMITS NOTHING — worktree edits only; the hub audits and orders the commits. NO attribution trailers. Tokens travel by file-path reference, never in files or messages. Lock-free porcelain with the flag SPELLED (`git --no-optional-locks status --porcelain`), split per-repo, in the return's census. Delta-edits only — no rewrites, no reorganizations, no new Tier-3 artifacts. Where an instruction here conflicts with what you find at the bytes, FLAG it in the return's pushback section — evidence over instruction, never silent deviation. The D5 language law binds every sentence you write (posture and verified fact; the layered enforcement form — THE FLOOR IS MISSING, NOT INFERIOR — never deterministic-beats-model). After ANY auto-compaction, re-invoke your role skill before the next act (law 13).

## §1 — THE THREE FENCES (the ratification's own terms; violating any one is a STOP, not a deviation)

1. **THE MISSION-POINTER LAW:** every launch surface (SKILL.md mastheads, orchestrator prompts) carries at most **ONE pointer line** to the mission/strategy layer; the CARRIERS stay the strategy files (`context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` · `context/strategy/Substrate_Thesis_v0.md` · the posture rows already in the PM skill's strategy-layer block). No mission prose is duplicated into skills; existing duplicated prose found in a writable tree is REDUCED to the pointer (delta-edit, retirement argued by name in the census).
2. **MEASURED, THEN GREEN — the required return fields:** (a) per touched skill file, **token counts measured before/after** (state the instrument used); (b) **rule census N-in / N-out with ZERO ORPHANS** — every prior rule name survives, merges-with-argument, or retires-with-argument, BY NAME, adversarially re-derived against the pre-edit originals (the W-SKILLS-2 form). A return missing either field is INCOMPLETE by its own terms.
3. **Delta-edit-never-rewrite, writable trees only (§2).** No new skills, no MANIFEST.yaml touch, no reorganization of reference files.

## §2 — Writable trees (EXACT; everything else read-only)

1. `nexsys-hivemind/project-manager/` and `nexsys-hivemind/coder/` (the writable skill SOURCES; the `.claude/skills/` mirrors are Nick's sync, never yours).
2. `nexsys-skills/orchestrators/nexsys-frontend/` (the FE skill tree; H8's carriers land here).
3. `nexsys-hivemind/context/process/*` and `nexsys-hivemind/context/lessons/*` — ONLY where a §3 harvest item names its landing.
4. `nexsys-skills/README.md` / `nexsys-skills/design/*` — read-only EXCEPT the §3 item-14 adjudication stamp on the design return's own frontmatter (one status line: ADJUDICATED at <return path>).

NOT writable, no exceptions: the spine (`pm-handoff.md`, `PROJECT_SNAPSHOT.md`), the criteria ledger (J1 FROZEN), `context/audits/*` (except CREATING your return), `context/instructions/*`, `context/handoff/*`, `context/research/*`, `context/strategy/*`, `context/decisions/*`, and the core/bench/docs repos (read-only for citation).

## §3 — The harvest (fold each item where it lives; cite file:line before→after for every edit; adjudicate each DONE / ALREADY-PRESENT / STOPPED-flagged)

1. **H7 — THE MEASURED-THEN-GREEN FRAME** (the semester's governing lens; v53 beat 4): "done" = measured, then green; no surface outruns its instrument. Fold as the definition-of-done line where the PM skill teaches acceptance/review; pointer to the v54 prompt §0.
2. **H8 — THE LIVE-WIRE VERIFICATION RULE (NEW-8b, STANDING; carriers ASSIGNED AT THIS PASS per the ratification):** every FE surface earns "verified" by ONE recorded real-wire exercise; green-CI-on-a-surface-never-touched-by-a-real-payload is a NAMED false-confidence class; a mock that always populates a nullable field MANUFACTURES the false type that causes the defect (exhibit: the why-not incident; the 2026-08-16 wire fixture `web-ui/dashboard/src/lib/api/fixtures/wire-2026-08-16-nonfiring.ts`). Carriers: the FE skill's DoD/acceptance section AND the PM skill's review discipline (one line each + pointer to `context/audits/2026-08-17_FE-lane_NEW23_return.md` §5/§8).
3. **H9 — THE FENCE-LIFT PROTOCOL:** a fenced claim lifts ONLY on the lifting WU's return on disk + audited (law 37), and lifts INTO the positive-scope register ("verified on real hardware at commit X"), never marketing air. Fold beside the PM skill's claim-fencing/D-1 guidance.
4. **H10 — RULING FORMS ARE NAMED UP FRONT** in any gate/deliberation protocol (per-row words · batch adoption · delegation-with-record), so a predictable form needs no deviation paragraph. Fold where the PM skill teaches gate/deliberation authoring.
5. **H11 — the navigator pattern ×5 + THE ONE-ARTIFACT RULE** (a navigator files ONE return; a desk pre-flight and its execution record are one artifact filed once). Verify/fold beside the operator-packet class (arc-35).
6. **PI-TZ + the UTC-banner law:** the Pi's displayed clock runs +1 h vs CT (Z-stamped bundles govern; `timedatectl` rides R-8); session date banners are UTC — re-derive the operator's America/Chicago day before ANY calendar-gated act. Fold where Pi/bench reads and scheduling are taught (playbook pointer lawful).
7. **The index.lock remove-and-rerun line (v53 beat 8, second core stranding) — THE QUEUED ENV-MODEL FOLD, discharged here BY NAME:** into `context/process/cowork-environment-model.md` §11: *on an index.lock failure during an order — confirm no lane session is active on that repo, remove the lock, re-run the order verbatim; the census re-verifies itself at the commit output.* One paragraph, exhibit-pointed (pm-handoff v53 beat 8).
8. **The HARDLINK class (two exhibits: the skills README (W-SKILLS-2) + the FE lane's seven-file list, v53 beat 7):** host files can be filesystem-hardlinked; the device bridge refuses link-alias reads; edits risk writing through to an unknown twin — break-with-byte-identical-copy before editing, flag the twin. Fold into env-model §12; the twin-location ENUMERATION is R-11's charge, not yours — pointer only.
9. **resolved.json-only ownership + command_id-in-verdict** (v51 H2; the R-5 rider): NO journal-layout rule is an ownership instrument; ownership adjudicates at the per-leg resolved.json until the redesigned instrument prints command_id inside the verdict line. Presence-check where gate reads are taught; fold if absent.
10. **Zero-placeholder operator blocks (v51 H1)** — presence-check against the playbook §8 addenda; fold the one-liner if absent.
11. **H4 rig-state-at-the-instrument + H6 lsusb-t adoption** (v52/v53): rig-state claims verify at dmesg/`lsusb -t`/driver-registration evidence, never inferred from schedule state. Presence-check in bench-read guidance.
12. **Pi environment facts** — `journalctl --user` is DEAD on the bench Pi (use `systemctl --user status`); `gh` is ABSENT on the host (browser click-path for GitHub); the nightly fires ~03:30 CT / ~08:32Z. Presence-check where operator blocks are taught.
13. **Law 16 (CI-verdict banking)** — presence-check in the PM skill; fold the one-liner if absent.
14. **THE 2026-08-02 CONTEXT-ENGINEERING RETURN — FINALLY ADJUDICATED (the ratification's named input):** TWO on-disk copies exist — `nexsys-skills/design/2026-08-02_context-engineering-and-skills-expansion_deep-research_return.md` (banked DELIVERED-UNADJUDICATED at skills e4646a9 — treat as the copy of record) and `nexsys-hivemind/context/research/2026-08-02_skills-context-engineering_deep-research_return.md` (verify whether twin or distinct at content; flag the finding to R-11 either way). Adjudicate ONCE, item by item: ADOPT (fold now, censused) / DECLINE-WITH-ARGUMENT / DEFER-NAMED. The adjudication table is a REQUIRED return section.
15. **The Anthropic-guidance refresh (the ratification's second named input):** re-read the current official Claude skills/context-engineering guidance (docs.claude.com and the published skills references), date-stamp what you read, and fold deltas as PROPOSALS with citations — nothing external is adopted without passing the fence-2 census like any other edit.
16. **The return-filename convention (minted v54 beat 1):** returns are dated by FILING day, never due day. Fold one line into the PM skill's dispatch/return conventions (your own return demonstrates it).

## §4 — STOP-on-mismatch gates

| Check | Expected |
|---|---|
| `context/audits/2026-08-05_W-SKILLS-2_return.md` + `context/audits/2026-08-06_W-SKILLS-2_audit_v46-beat-3.md` exist | yes — read both before editing (the census baseline and the audit's correction pattern are defined against them) |
| The ratification wording for the three fences | located at pm-handoff v53 beat 4 (hivemind) and QUOTED in the return before any fence-governed edit; cannot-locate ⇒ STOP the pass |
| The two context-engineering copies (§3.14) | both located before adjudication; a missing copy is a FINDING, not a silent skip |
| Porcelain of both writable repos at launch | CLEAN at the derived HEADs (state them); anything dirty ⇒ STOP and report |
| Any edit that would remove a strategy-file carrier or add a second mission line to a launch surface | STOP — fence-1 violation by construction |

## §5 — Return shape (the W-SKILLS-2 shape, kept, plus the fence fields)

§1 What was done (per harvest item: file:line, before→after) · §2 Rule census N-in/N-out, zero orphans, adversarially re-derived · §3 Token counts measured before/after per touched file (fence 2) · §4 Adjudication per harvest item (DONE / ALREADY-PRESENT / STOPPED-flagged) · §5 The context-engineering adjudication table (§3.14) · §6 The Anthropic-refresh findings with citations (§3.15) · §7 Pushback and recommendations · §8 Porcelain census (lock-free, flag spelled, per repo) · §9 State at close + the mirror-sync note (Check 9: your edits leave the mirrors STALE until Nick syncs — say so).

## §6 — Out of scope

The pm-handoff rotation (hub-owned, first quiet beat) · R-11's ground (the wayfinding files, truth-map, quarantined seven) · the spine and J1 · new skills or Tier-3 artifacts · `MANIFEST.yaml` · anything in core/bench/docs · the R-1/R-2 arc (the coder lane's ground) · skill-mirror syncing (Nick's act).
