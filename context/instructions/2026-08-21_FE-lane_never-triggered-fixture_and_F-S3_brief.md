<!--
file: context/instructions/2026-08-21_FE-lane_never-triggered-fixture_and_F-S3_brief.md
purpose: THE FE LANE'S NEXT TOUCH (weekend program rank (4), v55 prompt §2): (1) file the 2026-08-20 live non-firing body as a provenance-grade fixture and make it EARN its place with a cross-deployment dialect-stability test (the H8 tier-1 discipline — a second real capture of the same shape is evidence of STABILITY, not a new arm; the brief says so instead of rounding up) · (2) answer F-S3 at source (the run-detail "Updated" stamp: what mechanism drives it; whether the copy is honest) and PROPOSE the copy ruling · (3) OPTIONAL, census-permitting: the NEW-7 (a) teaching state for the unbuilt `/events` endpoint, keyed on the observed wire discriminator (path ∧ 404 ∧ `application/json`-not-problem+json), carried as REPO-COMPLETE until the next sitting verifies it live (H8 tier 2).
audience: a FRESH Cowork FE-lane session under /nexsys-frontend on Fable 5 Extra (NOT the hub; write-isolated; returns to the hub). Dense brief, dense return expected; strengtheners beyond the brief are welcome and LABELED.
state-type: lane brief / dispatch (Phase-3 FE work; frontend.yml is the CI gate).
dispatch-line: "Read nexsys-hivemind/context/instructions/2026-08-21_FE-lane_never-triggered-fixture_and_F-S3_brief.md and execute it. - /nexsys-frontend"
baseline: homesynapse-core `7c9e4fa` (main; HEAD==origin/main at authoring; the dashboard tree is untouched since `c091f7c`'s MODULE_CONTEXT fold and `beb664e`'s NEW-2/3 landing). All work in `web-ui/dashboard/`. ZERO core-Java changes — any apparent need is a STOP + escalate finding.
due: Tue 2026-08-25 EOD (graceful degrade Wed EOD; a partial return with an honest census beats silence).
return: `context/audits/<filing-date>_FE-lane_never-triggered-fixture_F-S3_return.md` — filing-day dated (America/Chicago), ONE file, uncommitted; the hub stages it. Returns file to context/audits/ — state this in your frontmatter.
laws: technical pushback WELCOME · the v1.1.2 read-API contract is FROZEN — consumer-side only · the FE skill's honesty-copy laws + the five-modes verdict vocabulary govern · L3 — no token material enters the session (the capture record below carries none) · a11y + design tokens per the FE skill · H8 — every touched surface carries its two-tier live-wire plan and is reported REPO-COMPLETE, LIVE-VERIFICATION PENDING THE DEPLOY (never "verified") · deviations declared, never silent · SOURCE WINS over this brief; flag contradictions.
-->

# FE Lane — The 2026-08-20 Never-Triggered Fixture + F-S3 (+ the optional NEW-7 (a) rider)

## §0 Evidence base (READ FIRST, in order)

1. This brief, whole.
2. `context/audits/2026-08-20_midweek-FE-deploy_sitting-record.md` — §2 row 2 + §6 (the live body VERBATIM, captured seconds before the 19:00 CT wave-trigger on the deployed `c091f7c` bundle `index-C95CAnmp.js`; the 404 discriminator for `/events`; F-S3 as observed). §3 F-S1 tells you why header screenshots are not in this record.
3. The FE skill + its references (you are under /nexsys-frontend).
4. Source, at your checkout: `web-ui/dashboard/src/lib/api/fixtures/wire-2026-08-16-nonfiring.ts` (the provenance-header FORM you mirror — read its comment block; it is the law of the fixture class) · `src/lib/api/contract.ts` (`NonFiringExplanation`, `Envelope`, `lastEvaluation: … | null` at ~:361) · `src/views/WhyNotView.tsx` + `src/views/WhyNotView.nullability.test.tsx` (the test you extend) · `src/lib/poll.tsx` (`PollProvider` :36 — `intervalMs = 1500`; `useApi` :129–:151 — **it re-fetches on every `viewPosition` change, and ONLY then**; `usePollCursor` :109) · `src/views/RunChainView.tsx` (`Resource` + `useApi`) · `src/views/RunsView.tsx` · `src/components/Resource.tsx` + `src/lib/api/client.ts` (`ApiProblem`: `status`, `problem`, `slug`, the `isOffline`/`isAuthRequired` predicates) · `src/lib/format.ts` :79–:85 (the relative-stamp renderer) · `web-ui/dashboard/MODULE_CONTEXT.md` (the three NEW-2/3 gotcha rows folded at `c091f7c`).

## §1 Charge 1 — the fixture, and what it is honestly evidence OF

**The capture (sitting record §6, verbatim on record):** `GET /api/v1/automations/01M0GPZFVANYA5TZMZSXRCV063/non-firing` → 200 · body `{ data: { automationId: "01M0GPZFVANYA5TZMZSXRCV063", automationName: "bench-hero", enabled: true, verdict: "NEVER_TRIGGERED", lastRelevantRunId: null, explanation: "Automation 'bench-hero' has not been triggered; it fires on state change.", triggerSummary: "state change", lastEvaluation: null, noCommandsIssued: null }, meta: { viewPosition: 104220, timestamp: "2026-08-20T23:59:59.684724880Z" } }`.

**The adjudication you inherit (H7 — nothing rounded up):** the 2026-08-16 fixture ALREADY carries `automationName: 'bench-hero'`, `verdict: 'NEVER_TRIGGERED'`, `lastEvaluation: null`, `noCommandsIssued: null`. The 2026-08-20 body is therefore NOT a "third arm" of the `lastEvaluation` tri-state (absent / null / value) — it is the SECOND real-wire capture of the null arm, from a DIFFERENT deployment (post-NEW-2/3 bundle, the re-provisioned automation id, a later view position, a nanosecond-precision timestamp string). Its value is **cross-deployment dialect stability**: the non-firing surface's wire shape did not drift between the gate-day build and the NEW-2/3 build — exactly the class of silent drift the STATE-DIALECT finding (F-S2) proves is possible on the `/state` surface. File it as that, and test it as that.

**The work:** (a) `src/lib/api/fixtures/wire-2026-08-20-never-triggered.ts` — `WIRE_20260820_NEVER_TRIGGERED_BENCH_HERO: Envelope<NonFiringExplanation>`, the body VERBATIM (key order and values as captured; envelope included), the provenance header in the 2026-08-16 file's exact form (Captured · Request · Response (200 OK · 396 B · the ETag/View-Position values the record carries — if a value is NOT on record, write `not captured` rather than inventing it) · Record · Build `index-C95CAnmp.js` at `c091f7c` · WHY IT EXISTS = the stability paragraph above). (b) **The stability test** (in `WhyNotView.nullability.test.tsx` or a sibling `fixtures.stability.test.ts` — your call, justified): the two captures have IDENTICAL key sets (recursive, envelope included) and IDENTICAL null-ness per key; the differing VALUES are exactly the enumerated set {`automationId`, `meta.viewPosition`, `meta.timestamp`} — anything else differing FAILS the test (that is the drift detector). (c) The render test: `WhyNotView` renders the 08-20 body to the same honest never-triggered surface as the 08-16 body (the (b)-arm "why didn't it?" copy), no `lastEvaluation` row, no spinner, no throw. **Red-first (#18):** (b) and (c) are green-by-construction on a stable wire — disclosed; make (b) prove its teeth by a deliberately mutated copy in the test (`{ ...body, data: { ...body.data, extra: 1 } }` must FAIL the key-set assertion) so the detector's false-verdict boundary is fixture-paired (arc-discipline 10).

## §2 Charge 2 — F-S3 answered at source, the copy PROPOSED (not changed unilaterally)

**Observed (sitting record §2 row 6 / §3 F-S3):** list views' `Updated` stamps ticked at ~30 s; the run-DETAIL view's stamp read "3 min ago" after ~3 min on the page. The v54 hub's hypothesis was fetch-once-by-design. **The source says something more precise** (pinned at authoring, re-verify): `useApi` (:129–:151) re-fetches exactly when `viewPosition` changes; a quiet projection (no new events) means NO refetch on ANY view, and the stamp — whatever it renders — ages. So the mechanism question is: **what does the stamp render** — the time of the last successful fetch, the response `meta.timestamp`, or something else — and why did list views tick while the detail did not (different fetch cadence? the footer `● Live` vs a per-view stamp? a separate poll for lists?). Find the renderer (grep the `Updated` copy / the `ago` formatter call sites; `src/lib/format.ts:79–:85`), pin the mechanism at file:line, and answer in the return: (i) is the detail view fetch-once, viewPosition-driven, or interval-driven? (ii) is the current label honest under the FE skill's copy laws for THAT mechanism? (iii) PROPOSE exactly one of: keep · relabel (`Loaded <stamp>` if fetch-once; `As of <time> · live` if position-driven) · add the mechanism to the tooltip/aria-description — with the one-line rationale. **Do not change the copy or the mechanism in this lane** (DX-22's open question + the named post-gate discriminator stand); the hub rules on your proposal.

## §3 Charge 3 — OPTIONAL rider, census-permitting: NEW-7 (a), the teaching state for the unbuilt endpoint

Only if Charges 1–2 are complete and your census stays small: the Activity nav's `GET /api/v1/events?sort=DESC&limit=50` returns a router-level 404 — `Content-Type: application/json` (NOT `application/problem+json`), 159 B — adjudicated CLOSED-SUFFICIENT with the teaching-state upgrade implementable client-side on the discriminator (path `/api/v1/events` ∧ status 404 ∧ content-type `application/json`-not-problem). The work: in the client/`Resource` error path, detect that discriminator and render the honest teaching card ("The activity feed endpoint is not part of this release yet" — the FE skill's register; the existing calm 404 card + Try again stays the fallback for every OTHER 404). Fixture: the 404 RESPONSE BODY was NOT captured — build the fixture from the recorded headers + an empty body and key ONLY on the discriminator (never on body text); label the fixture `HEADERS-ON-RECORD, BODY NOT CAPTURED`. Tests: the discriminator fires only for that path+status+content-type; a problem+json 404 still renders the generic card. **H8 accounting:** REPO-COMPLETE, LIVE-VERIFICATION PENDING — the next sitting's plan (§4) carries the one-line check.

## §4 The acceptance bar — H8 (two tiers, both REQUIRED in the return)

1. **Real-payload fixtures NOW:** the 08-20 body verbatim (Charge 1); the 404 headers-only fixture if Charge 3 runs.
2. **The live re-exercise plan:** for Nick's next warm deploy (the midweek-deploy packet idiom): open `#/why-not/<automation>` for bench-hero before any trigger → the never-triggered surface; the run-detail stamp observed across 3 min with the mechanism now KNOWN (what it should read, by source); if Charge 3 ran — Activity nav → the teaching card (falsifier: the generic 404 card). This lane's scope is repo-only — no deploy; conclude REPO-COMPLETE, LIVE-VERIFICATION PENDING THE DEPLOY in those words.

## §5 Gates + return contract

`npm test` (vitest; state the test-method count before/after — 144 `it(`/`test(` sites across 12 files at authoring, derived from a grep, re-derive yours) · `npm run lint`/typecheck per the FE skill · `frontend.yml` GREEN on the push = the CI gate of record. The return (ONE file at the named path): (1) the changed-file census, exact (expected: 1 A fixture + 1 M/A test for Charge 1; zero for Charge 2; +2–3 for Charge 3 if run); (2) the fixture provenance block as filed; (3) the stability test's mutated-copy red proof; (4) the F-S3 source answer (file:line) + the ONE proposal; (5) the live re-exercise plan; (6) deviations; (7) the route-back line: "Intakes at the hub for two-layer audit; the deploy + live re-exercise ride the next Pi trip." Refutation welcome in both directions — if the 08-16 and 08-20 bodies differ in a key the brief did not enumerate, that IS the finding: file it first.
