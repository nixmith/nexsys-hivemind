<!--
file: context/instructions/2026-08-22_R7b_version-scheme_distribution-micro-WU_coding-instruction.md
purpose: R-7b — the distribution-only micro-WU the R-7 intake audit ruled (`context/audits/2026-08-22_R7_intake_two-layer-audit_v55-beat-6.md` §2 H-1/H-2/H-4, §4 R-V + R-7b — READ §2 AND §4 FIRST; this instruction does not re-narrate them). It lands the R-V version scheme `0.1.0+git<YYYYMMDD.HHMMSS>.g<sha>` so CI-built artifacts ORDER monotonically along `main` (the `+g<sha>` suffixes do not — "`--allow-downgrades` exactly once" breaks on the second install otherwise), fences the `0.1.0-skeleton` passes-but-false value out of CI, makes the VERSION-file lookup deterministic, and clears the last Node-20 annotation. FENCE (absolute): R-7b lands BEFORE R-3b installs any CI-built artifact on a card.
audience: the R-7b Coder lane (host-side Claude Code per D12; shell only — zero Java, zero Gradle; the lane runs `bash distribution/smoke/version-grammar-test.sh` + the mutation run + `bash -n` over every touched script) + Nick (the commit; the CI read).
status: ISSUE-READY (v56 beat 1). Baseline: core `89a912e` (R-7 landed; CI GREEN on all three verdicts, the arm64 leg 3m01s on `ubuntu-24.04-arm`) — OR the R-9 landing commit if R-9 has landed first (hub-recommended order: R-9 → R-7b; never both on the checkout at once). R-9 touches `distribution/common.sh` at the health-path block (`:40–:44`) and `distribution/docs/escalations.md` (E3) — disjoint from every hunk below; the STOP gates are written by CONTENT, not line number, so they hold under either order. Report-and-proceed if line numbers have shifted; STOP if any gated CONTENT differs.
return: nexsys-hivemind/context/audits/<filing-date>_R7b_version-scheme_return.md (filing-day dated, America/Chicago). The lane commits NOTHING — the hub audits at the bytes, Nick commits; CI on the push (Build & Check + install-smoke BOTH legs) is the gate of record (law 16). Done-when = the three verdicts GREEN with the predictions in §6 met.
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-22_R7b_version-scheme_distribution-micro-WU_coding-instruction.md and execute it. - /nexsys-coder"
rulings (H10): R-V stands (RULED). **R-7b-1 the resolution reorder — STANDS (RULED by Nick 2026-08-22)** · **R-7b-2 `ci.yml` `upload-artifact@v5` — IN (RULED)**. Hub-ruled engineering details inside R-V's scope, disclosed: (i) the resolution ORDER moves git ahead of the VERSION file (§1.b — the banked ruling's item (c) as written would have made the tracked `0.1.0-skeleton` win every CI build once `_dist_dir` resolved correctly: walk the arms — `common.sh:52` consults the file BEFORE git); (ii) `ci.yml`'s `upload-artifact` rides the v5 bump (one line; IN unless Nick says OUT).
-->

# Coding Task: R-7b — the version scheme `0.1.0+git<date>.g<sha>` (distribution-only)

**Subsystem:** distribution (`distribution/**` + the two workflow twins + one `ci.yml` line). No Java. **Phase:** 3. **Task brief:** the R-7 audit §4 (R-V · R-7b). **Budget:** ≤ 1 h host-CC.

## 0. Minimum read set (#3)

The R-7 audit §2 + §4 (above) · `distribution/common.sh` whole (98 lines; the version block `:46–:75`) · `distribution/image/build-image.sh` `:27–:60` (the `bash -c` capture at `:35`, the grammar assert `:46–:60`) · `distribution/deb/build-deb.sh` `:19–:30` (`:24` evaluates `hs_version` a second time) · `distribution/smoke/version-grammar-test.sh` whole (122 lines — the rig you extend) · `.github/workflows/install-smoke.yml` `:49–:136` (the twin at `distribution/ci/install-smoke.yml` is byte-identical — edit one, copy to the other, `cmp` them) · `distribution/docs/architecture.md` `:59–:80` + `:119–:137` · `distribution/docs/escalations.md` `:9–:19` (E1).

## STOP-on-mismatch gates (by content)

| File | Expected at launch | STOP if |
|---|---|---|
| `distribution/common.sh` | `hs_version` resolves env → `${_d}/VERSION` → `${_d}/../VERSION` → `git describe --tags --always --dirty` with the `*.*) … ;; *) printf '0.1.0+g%s'` case → `printf '0.1.0-skeleton'`; `_dist_dir` uses `$0` only | any arm differs, or a `HS_DIST_DIR` reference already exists |
| `distribution/VERSION` | exactly `0.1.0-skeleton\n` (15 B, tracked) | content differs |
| `build-image.sh` `:35` / `build-deb.sh` `:24` | both capture `VERSION="$(… bash -c '. "<DIST>/common.sh"; hs_version')"` | either passes `HS_DIST_DIR` already |
| the twins | byte-identical (`cmp` exit 0); `setup-node@v4`, `upload-artifact@v4`; the echo step has no `skeleton` fence | not identical, or already bumped |
| `version-grammar-test.sh` | 23 checks: 7 arm + 7 accept + 3 grammar-reject + 3 charset-reject + 3 carriers; the stub `git` answers only `rev-parse` + `describe` | count ≠ 23 |

## 1. Files to modify (census **9 M, 0 A** — the Files table governs, #2)

**a. `distribution/common.sh` — the arm (R-V).** In the non-tag-shaped arm, replace `printf '0.1.0+g%s' "${_v}"` with: capture `_cd="$(TZ=UTC git -C "${_d}" log -1 --format=%cd --date=format-local:%Y%m%d.%H%M%S 2>/dev/null)"` — `TZ=UTC` ON the git invocation (never the operator's zone; `format-local` honours `TZ`) — then `case "${_cd}" in [0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].[0-9][0-9][0-9][0-9][0-9][0-9]) printf '0.1.0+git%s.g%s' "${_cd}" "${_v}" ;; *) printf '' ;; esac` (the pattern is POSIX — the hub ran it under `dash` and `bash`: the well-formed date wraps; empty, short, non-dot, and trailing-space inputs all yield `''`). The hub also ran every §1.f ordering row through `dpkg --compare-versions` (dpkg 1.22): all four TRUE, plus `…+up gt …` and `…-dirty gt …`. The EMPTY output on a missing/malformed date is deliberate and fail-closed: `build-image.sh:57` dies on `''` and `dpkg-deb` rejects an empty `Version` — never fall back to `0.1.0+g<sha>` (post-R-7b that is the lawful-but-wrong value, H15: it sorts BELOW every `+git` build and re-opens the downgrade trap). `-dirty` rides through unchanged (`…g<sha>-dirty`). Rewrite the comment block `:57–:66` to state the scheme and WHY it orders (`+git…` > `+g…` because `g` is a proper prefix; monotone by committer time UTC; depth-free — `rev-list --count` is a constant 1 on a shallow checkout; reproducible — commit date, never build date); cite the audit path.
**b. `distribution/common.sh` — the order + `_dist_dir`.** `_dist_dir() { if [ -n "${HS_DIST_DIR:-}" ]; then printf '%s' "${HS_DIST_DIR}"; return; fi; <today's body>; }`. Resolution order becomes **env → git describe → `${_d}/VERSION` → `${_d}/../VERSION` → `0.1.0-skeleton`**: in any git tree the commit decides, regardless of cwd; the tracked `distribution/VERSION` stays (unchanged bytes) as the git-less (tarball-export) carrier, reached ONLY when git is absent or the tree is not a repo. Update the `:47` comment to the new order.
**c. `build-image.sh:35` + `build-deb.sh:24`.** Pass `HS_DIST_DIR="${DIST}"` into the `bash -c` capture exactly as `HS_VERSION` is passed (inline env, POSIX-portable; no `export`). Rewrite `build-image.sh:53–:55` ("passes-but-false input"): the real one is the `0.1.0-skeleton` fallback, fenced in CI by the twins' echo step; a tag is a hypothetical.
**d. The twins (edit `distribution/ci/install-smoke.yml`, copy to `.github/workflows/install-smoke.yml`, `cmp`).** (0) the `setup-java` step's comment `# matches ci.yml / LTD-10 pinning` → `LTD-01 pinning` (the JDK pin is LTD-01 Java 21 LTS; LTD-10 is Gradle — a hub mis-attribution found by the R10-IN-L lane; comment-only). (1) `actions/setup-node@v4` → `@v5`; `actions/upload-artifact@v4` → `@v5` — verify both majors exist FIRST: `git ls-remote --tags https://github.com/actions/setup-node v5` / `…/upload-artifact v5` (report the two sha lines). (2) The echo step: `V="$(HS_DIST_DIR=distribution bash -c '. "distribution/common.sh"; hs_version')"` (one instrument with the build scripts), and after the grammar assert add the skeleton fence: `[ "${V}" != "0.1.0-skeleton" ] || { echo "::error::version of record is the git-less fallback '0.1.0-skeleton' — never lawful in CI (actions/checkout leaves .git)"; exit 1; }`; extend the green line to print the scheme name AND the `.deb`'s `sha256sum` (`sha256sum "${DEB}" | cut -c1-64`) — the origin hash the R-3/R-4 packets compare their desktop and card hashes against (the artifact channel becomes hash-verifiable from the run log). Keep `timeout-minutes: 25` (the arm64 wall-clock is 3m01s on file; no change ruled).
**e. `.github/workflows/ci.yml`** — `actions/upload-artifact@v4` → `@v5` (the `Upload test reports` step; one line).
**f. `distribution/smoke/version-grammar-test.sh` — the rig.** Stub `git` gains `log) printf '%s' "${FAKE_CDATE-}"; exit 0 ;;` and a `FAKE_NOGIT=1` mode (`rev-parse` → `exit 1`; `describe` prints nothing). `run_hs_version` takes `(describe, cdate)` and the rig sets `HS_DIST_DIR="${WORK}/tree/cwd"` — the hermetic cwd ITSELF, so old and new `_dist_dir` resolve to the same dir and the mutation run isolates the ORDER change, not the dir change (rows that plant a `VERSION` file put it at `${WORK}/tree/cwd/VERSION` and remove it after). Rows — **§1 arm (10):** `7c9e4fa` + `20260822.143100` → `0.1.0+git20260822.143100.g7c9e4fa` · `d26777c` + same date → `…gd26777c` · `7c9e4fa-dirty` → `…g7c9e4fa-dirty` · the three tag rows unchanged · `''` → `0.1.0-skeleton` · **NEW** `7c9e4fa` + empty cdate → `''` (fail-closed) · **NEW** `7c9e4fa` + date WITH `cwd/VERSION` = `0.1.0-skeleton` planted → the `+git` shape (git outranks the file — the H-1 bypass closed at the function; on the pre-R-7b `common.sh` this row FAILS because the file wins) · **NEW** `FAKE_NOGIT=1` + `cwd/VERSION` = `1.2.3` planted → `1.2.3` (the tarball carrier still works; PASSES on both old and new). **§2 accept:** add the two `+git` shapes (clean + `-dirty`); reject rows unchanged. **§2b ordering (NEW, the proof of R-V):** if `command -v dpkg`, assert `dpkg --compare-versions A gt B` for: `0.1.0+git20260822.143100.g7c9e4fa gt 0.1.0+g7c9e4fa` · `… gt 0.1.0+gd26777c` · `0.1.0+git20260822.143100.g1111111 gt 0.1.0+git20260822.143059.gffffffff` (time decides, not sha) · `7c9e4fa gt 0.1.0+git20260822.143100.g7c9e4fa` (the documented one-time downgrade — asserted so the doc's claim is pinned); else print ONE explicit `[version-grammar-test] SKIP  ordering rows (no dpkg on this host)` line (arc-19 — a silent skip is vacuous). **§3 carriers** unchanged + two rows (one per twin) asserting the skeleton-fence literal `!= "0.1.0-skeleton"` is present. Update the header comment (what it proves) and the expected-count line. Mode stays `100755`.
**g. `distribution/docs/architecture.md`** — rewrite "The version string" (`:119–:137`): the new order, the scheme and its ordering argument, the one-time `--allow-downgrades` on the two `7c9e4fa` cards (unchanged), and RETIRE `:135–:137` ("the clean fix is a tag") → tags begin at the first release (a tag at HEAD would sort BELOW every `0.1.0+…`); no claim language (the D-1 fence; the artifact channel is "proven in CI", never "verified on hardware").
**h. `distribution/docs/escalations.md`** — E1: append a resolution line "**(c) taken at R-7 (2026-08-22):** `ubuntu-24.04-arm`, GitHub-hosted, free for public repos; first arm64 leg 3m01s wall-clock (install-smoke #33)". Leave E3 untouched (R-9's).

## 2. What to watch out for

POSIX `sh` in `common.sh` — no bashisms, no `[[`, no `local` assumptions beyond what the file already uses; test it under `dash` if present (`dash -n`, and `dash -c '. distribution/common.sh; hs_version'` from the repo root) as well as `bash`. · The `bash -c` capture makes `$0` = `bash`, so without `HS_DIST_DIR` `_dist_dir` = the cwd — that is WHY (b)/(c) exist; do not "fix" it by changing the capture form (the rig pins that form). · `git describe` on a shallow `actions/checkout@v5` prints the short sha via `--always`; `git log -1` works at depth 1 — do not add `fetch-depth: 0`. · `%cd` is the COMMITTER date; a rebase re-stamps it — correct for ordering along `main`. · Never copy-retype the grammar regex; the §3 carrier rows will tell you. · The twins must stay byte-identical (`cmp` in your return). · Mutation run (arc-10): `git show HEAD:distribution/common.sh > /tmp/common.pre-R7b.sh && COMMON_SH=/tmp/common.pre-R7b.sh bash distribution/smoke/version-grammar-test.sh` (a regular file — the rig's `-f` check rejects a process substitution) MUST fail exactly the rows the old arm/order cannot satisfy — the hub's derivation is **5** (three `+git` arm rows · the empty-date row · the VERSION-bypass row); report your exact count and the row names.

## 3. Out of scope

Tags (none minted), the R-3/R-4 packets' `--allow-downgrades` line (hub-owned), `README.md:117` (fenced until W2-3), E3/R-9's files, `timeout-minutes`, any Java.

## 4. Verification (the lane's gates)

`bash -n` on every touched `.sh`; `bash distribution/smoke/version-grammar-test.sh` → `VERSION-GRAMMAR-TEST PASSED ✓` with the NEW check count stated (derive it; the hub's: 10 arm + 9 accept + 3 grammar-reject + 3 charset-reject + 4 ordering (or one SKIP line) + 5 carriers = **34 with dpkg / 30 + SKIP without**); the mutation run's FAIL count; from the repo root `HS_DIST_DIR=distribution bash -c '. distribution/common.sh; hs_version'` prints `0.1.0+git<YYYYMMDD.HHMMSS>.g<sha>` matching `TZ=UTC git log -1 --format=%cd --date=format-local:%Y%m%d.%H%M%S`; from `distribution/` as cwd the SAME value (the cwd accident is closed); `cmp` the twins; the two `ls-remote` lines.

## 5. Return shape (≤ 2 pages)

Census (9 M, zero A, zero D) · per-file what/where · the test output whole · the mutation run output · the `hs_version` proofs (root cwd + `distribution/` cwd + a `-dirty` tree if you can make one cheaply and restore it) · the dpkg ordering rows' output · pushback (welcome — the reorder (b) is the hub's own correction of its ruling; if you find an arm it breaks, say so with the row).

## 6. CI predictions (H12 — filed here before the push; law 16 banks what lands)

Build & Check GREEN ~3 min, zero Java delta (test counts unchanged). install-smoke BOTH legs GREEN (amd64 ~2m45s · arm64 ~3m00s); Static lint prints the new PASS count + `PASSED ✓`; the echo step prints `hs_version=0.1.0+git<YYYYMMDD.HHMMSS>.g<sha>` and the `.deb` is `homesynapse_0.1.0+git<date>.g<sha>_<arch>.deb`; 18 `[smoke] PASS` lines unchanged (19 if R-9 landed first); update-smoke GREEN (`V2 = <ver>+up`); **zero Node-20 annotations on the run** (H-4 clears — a verifiable prediction); artifacts `distribution-artifacts-{amd64,arm64}`. Plausible REDs: an `upload-artifact@v5` input rename (adjudicate at the job log, revert that bump only) · a `dash`-incompatibility in the new case pattern (the static-lint step runs `bash -n` only — test under dash locally, per §2).
