<!--
file: context/instructions/2026-08-22_research-lane_BRAND-SPRINT-1_build-out-board_brief.md
purpose: BRAND-SPRINT-1 — the Pelton-week fodder. The G-2 readiness brief §5 board (B-1..B-8) rides the branch word (~Wed/Thu Aug-27/28); this lane does the RESEARCH half of the rows that can be studied BEFORE the word without touching any fence, so that on the word the build-out is a choice among studied options, not a blank page. Rows in scope: B-1 (identity directions, {{NAME}}-parameterized), B-7 (the JP/katakana variant study), B-2 (the voice/tone §-fold draft from the field data), B-3 (the website integration map — pointers only), B-5 (the CLAIM-FENCE REGISTER only — no story-kit prose before R-4). Rows OUT: B-4 (exists), B-6 (post-charter), B-8 (its own lanes).
audience: a fresh Cowork research lane (Fable 5 Extra; web-backed; the device bridge for the inputs below) + the hub (intake audit) + Nick (reads it the day the word lands).
status: DISPATCH-READY (v56 beat 1). Due: on disk by Tue 2026-08-25 EOD CT (the Pelton results land ~Wed/Thu).
return: nexsys-hivemind/context/audits/<filing-date>_BRAND-SPRINT-1_return.md (filing-day dated; ONE artifact — H11). Read-only on every repo; no commits; no accounts, no outreach, no domain checks that create a record, no posting — NOTHING PUBLIC (the tracker law: no public use of any candidate before G-2).
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-22_research-lane_BRAND-SPRINT-1_build-out-board_brief.md and execute it. Read-only; one return file; nothing public."
decision it feeds: the same-day execution on the branch word (the G-2 brief §1 branches A/B/C): on A, B-1/B-7 go from "charter" to "choose among these"; on B, the trims are argued from the same options; on C, the study transfers to the hedge (VERDOMO — NL-settled, the results bank) at near-zero cost because every artifact is token-parameterized (arc-17).
audit bar (two-layer): every field-data claim cites the results bank by line (`native-speaker-results/2026-08-19_results-bank_and_adjudication.md`) or the JP paste (`…/2026-08-19_JP+operator-paste_all-six.txt`); every katakana reading is shown with its romaji + the checker's own rendering; every identity direction is an OPTION with a named trade-off (the lane recommends; Nick rules); every external source a primary (a foundry, a standard, a registry) with the fetch date; the claim-fence register's every row cites the fence's carrier (D-1 list · `README.md:117` · the north star's DESIGNED-FOR flag). The hub spot-checks ≥ 5 citations at the source.
-->

# BRAND-SPRINT-1 — the build-out board, researched before the word

## 0. Read first (in this order; pointer-form — do not re-narrate them in the return)

`context/strategy/brand-program/2026-08-20_G2-readiness_decision-brief.md` (§1 the branches · §5 the board — your charge list) · `2026-08-19_G2-swap-runbook.md` (§1 IN/OUT, §3 the fence) · `2026-08-13_naming-program-v2_decision-package.md` (the legal roadmap §6; the filing wave; the field-screen method) · `2026-08-13_zendomo_candidate-field-screen.md` · `native-speaker-results/2026-08-19_results-bank_and_adjudication.md` (the field bank — ZENDOMO clean ×6; VERDOMO the hedge) + the six result files beside it (the `.docx` via the docx skill; the JP `.txt` directly) · `2026-07-22_identity-system_exploration.md` (B-1's prior pass — re-run, do not repeat) · `2026-07-22_voice-tone-messaging_platform.md` (B-2's base) · `2026-07-23_domain-handle-claims-refresh.md` (B-4 — read for the handle constraints B-1 inherits) · `context/strategy/Substrate_Thesis_v0.md` §5/§9 + the D5 language law (the enforcement position is restated ONLY in the layered form: the deterministic floor is MISSING from the field, not SUPERIOR) · `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` frontmatter (the DESIGNED-FOR flag — the one claim never quoted externally without it) · the FE skill's brand/design-token section (`nexsys-skills/orchestrators/nexsys-frontend/SKILL.md` — the surfaces B-3 touches; read, do not edit).

## 1. The charges (five; the return carries five numbered sections + §0 + §6)

**B-1 · Identity directions v1, {{NAME}}-parameterized (the largest section).** Re-run the 07-22 exploration against what the field taught: the F-N4 Z→S drift (readers softening the onset) argues a mark that visually ANCHORS the ZEN- onset; the six markets' readings and the calm register are evidence now, not taste. Deliver **three directions** (not one): each with a typographic stance (a named open-licence face or two — verify the licence at the foundry page, cite it; no paid faces assumed), the onset-anchoring device (letterform, weight contrast, a mark element — described, and sketched as inline SVG if you can do it in ≤ 40 lines, otherwise described precisely), a colour stance derived from the voice platform (calm, trust, local-first — NOT a "smart home blue"), the dashboard-first constraint (the wordmark must survive at 16 px in a browser tab and at 0.5 cm on a device label), the dark/light pair, and the accessibility floor (WCAG AA contrast for every text pairing, numbers shown). State for each: what it costs to execute (hours; any paid asset), what it forecloses, and how it transfers to VERDOMO (the hedge) — a direction that only works for one name is a weaker direction. Recommend one; keep the other two as branch-B/C fallbacks.
**B-7 · The JP/katakana variant study (B-1's rider; the filing-wave gift).** From the JP paste: the checker's katakana renderings for the set; for ZENDOMO and VERDOMO give the candidate katakana forms (long-vowel marks, the ゼ/ゾ/ド choices), the romaji, the readings a Japanese reader would default to, any unfortunate homophone/near-word (cite a dictionary source), and the wordmark consequence (does the katakana set at the same visual weight as the Latin mark in each B-1 direction?). This feeds the JP-FIRST foreign-filing wave (naming pkg §6) — say what the filing needs (a katakana mark as a separate application or a stylized composite) and stop there: the legal answer is counsel's.
**B-2 · The voice/tone §-fold draft.** Against the voice-tone platform: the field data shows the calm register reads as intended across six markets — draft the ≤ 1-page fold (what the evidence confirms, what it corrects, two microcopy exhibits), as text Nick can paste into the platform doc at the word. D5 binds every sentence; every capability sentence stays claim-fenced (B-5's register tells you which).
**B-3 · The website brand-integration map (pointers, no edits).** Using the runbook's census instruments, spelled (`git -C homesynapse-core-docs grep -li "homesynapse" -- "website"`; the dashboard's `BRAND.productName` at `web-ui/dashboard/src/lib/i18n.ts:18`), enumerate the site surfaces that carry the working name (copy · config · manifest · og/meta · favicon/app icons · the 404 · footer/legal) with the file:line for each, classify each IN (renames) / OUT (a lawful technical identifier that NEVER renames: the package `homesynapse`, the unit `homesynapse.service`, paths, the JPMS `com.homesynapse`), and note which ones B-1's direction would ALSO restyle (icons, og image). The FE lane's SWAP-CENSUS brief does the dashboard half; you do the website half — do not overlap.
**B-5 · The claim-fence register (NO prose).** A table: every capability sentence the external story will want to say · its fence today (D-1 DO-NOT-SAY item ⇒ "until R-4 lifts, into the positive-scope register"; `README.md:117` ⇒ until W2-3; the DESIGNED-FOR claim ⇒ never without the flag; "proven in CI" vs "verified on real hardware at commit X" — the H9 register) · what lifts it · the honest sentence available NOW (the layered form: what is MISSING from the field). This is the instrument the post-R-4 story kit is written against; it is not the kit.

## 2. Fences (violating one voids the return)

Nothing public; no candidate name typed into any external form that creates a record (no WHOIS lookups on registrars that log, no social-handle "availability" probes, no trademark-office searches — counsel is running the search; the domain map already exists); no edits to any repo; no contact with any vendor or foundry; every font/licence claim from the foundry's own page; the D-1 fence survives every sentence you write; the working name stays everywhere it is today.

## 3. Return shape (≤ 8 pages; B-1 may carry inline SVG)

§0 one-paragraph verdict + the recommendation per row · §1 B-1 (three directions + the recommendation + the transfer-to-hedge note) · §2 B-7 · §3 B-2 (the paste-ready fold) · §4 B-3 (the table) · §5 B-5 (the register) · §6 sources (every URL with its fetch date) + ≤ 5 harvest lines + what you could not do read-only (e.g. "the 16-px legibility test needs a rendered build — the FE lane's job at the word").

## 4. Pushback welcome

If the field data argues AGAINST anchoring the onset (e.g. the drift is a non-issue in five of six markets), say so with the lines; if a direction needs a paid asset to be honest, name the price rather than pretending a free face does the job.
