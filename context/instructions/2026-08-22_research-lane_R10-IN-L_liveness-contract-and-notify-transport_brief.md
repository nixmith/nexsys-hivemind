<!--
file: context/instructions/2026-08-22_research-lane_R10-IN-L_liveness-contract-and-notify-transport_brief.md
purpose: R10-IN-L — an R-10 charter INPUT with two halves that share one seam: (L1) THE LIVENESS CONTRACT — now that `/health` exists (R-9: `GET /health` 200 ⇔ the projection is LIVE, 503 otherwise, body exactly `{"status":"<mode>"}`, loopback-only, the ONLY unauthenticated non-shell path), what does "live" MEAN for this system beyond the projection mode — the lifecycle phase, the integration adapters' availability, the event bus's delivery progress (the `awaitCheckpoint` stall class), the FE's "availability honest" surface — and what a `/livez` vs `/readyz` split would need; (L2) THE NOTIFY TRANSPORT (OR-M13-SDNOTIFY, E3's successor): `Type=notify`/`WatchdogSec` needs `sd_notify`, which is an AF_UNIX SOCK_DGRAM protocol; OpenJDK 21 (JEP 380) is STREAM-only, so the transport is real engineering research under the JDK pin — the 2026-06-07 M5-D decision matrix (JNR/JNA vs `systemd-notify` subprocess vs FFM/Panama) must be re-priced against today's facts: `/health` exists, the JDK-LTS trajectory (25), and the fourth option M5-D did not have — STAY on `Type=exec` + the probe and add a distribution-only systemd timer watchdog against `/health`. Decision-shaped: options one-word-adjudicable; the charter decides; this lane designs nothing into code.
audience: a fresh Cowork research lane (Fable 5 Extra; the device bridge for the repos; the web for the primary sources — systemd's `sd_notify(3)`/`systemd.service(5)`, JEP 380, the JDK FFM JEPs, Corretto release notes) + the hub (intake) + the R-10 charter session.
status: DISPATCH-READY (v56 beat 1). Due: on disk by Fri 2026-08-28 EOD CT (parallel, read-only).
return: nexsys-hivemind/context/audits/<filing-date>_R10-IN-L_liveness-and-notify-transport_return.md (filing-day dated; ONE artifact). Read-only on all repos; no commits; no hardware.
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-22_research-lane_R10-IN-L_liveness-contract-and-notify-transport_brief.md and execute it. Read-only; one return file."
decision it feeds: the R-10 charter's (i) liveness-contract ruling (what `/health` promises and what a second endpoint would promise — a Doc 12/Doc 09 AMD-class decision, so the lane delivers the DECISION SHAPE, not the AMD) and (ii) the M13 transport word (OR-M13-SDNOTIFY's resolution condition: "pick the transport mechanism") — plus whether the unit's staged M13 flip block (`homesynapse.service:100–:106`) stays as written.
audit bar (two-layer): every claim about OUR code cites file:line at the HEAD you read (state the SHA); every claim about systemd/JDK behaviour cites the primary (man page section / JEP number / release note URL, fetch-dated) — a summariser's paraphrase is not a source; every option carries a cost row (code · deps · CI-provability in install-smoke · Pi-class runtime cost) and its failure mode; the hub re-verifies ≥ 4 primaries and ≥ 4 code cites.
-->

# R10-IN-L — the liveness contract + the notify transport

## 0. Read first (pointer-form; banked verdicts final — law 16)

**Ours:** the R-9 instruction `context/instructions/2026-08-22_R9_E3-HEALTH_unauthenticated-loopback-health_coding-instruction.md` (§"Technical specification — contracts" — the `/health` contract as designed; the E3 realization note: "lifecycle phase" is realized as the projection mode because that is what today's probe measures) + `context/pre-verifications/WU-R9.md` · the unit `distribution/systemd/homesynapse.service` whole (`:8–:13` the readiness-today note; `:100–:106` the staged M13 flip: `NotifyAccess=main`, `WatchdogSec=60`, `Restart=on-watchdog`, "health loop pets WATCHDOG=1 at 30s") · `distribution/smoke/health-probe.sh` whole · `distribution/docs/boot-contract-map.md` + `distribution/docs/escalations.md` E3 · `platform/platform-systemd/src/main/java/com/homesynapse/platform/systemd/{SystemdHealthReporter,NoOpHealthReporter}.java` + that module's `module-info.java` (quote it verbatim in the return; never paraphrase a module name) · `lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/{HealthLoop,SystemHealthSnapshot}.java` + `lifecycle/lifecycle/MODULE_CONTEXT.md` · the M5-D matrix `context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md` (the prior adjudication: reject JNR/JNA; subprocess if no WATCHDOG keepalives, else FFM; "decide WatchdogSec" is Nick's) · `pm-handoff.md` `## Open Risks` → OR-M13-SDNOTIFY + OR-GATE-M9.4-NCFG's deflake row (the `awaitCheckpoint` STALL-vs-wall-clock class — a liveness signal the contract should name) · `homesynapse-core-docs/design/12-startup-lifecycle-shutdown.md` (the phases; C12-03 liveness) + `09-rest-api.md` (the auth posture the `/health` exemption sits inside) · the FE "availability honest" surface (`context/audits/2026-08-21_FE-lane_never-triggered-fixture_F-S3_return.md` §5/§8 — what the dashboard can honestly show about liveness today). **Primaries (web):** `sd_notify(3)` + `systemd.service(5)` (`Type=notify`, `NotifyAccess=`, `WatchdogSec=`, `ExecStartPost=` semantics, the `NOTIFY_SOCKET` datagram protocol, `systemd-notify(1)` incl. `--pid=` and its privilege requirement, `sd_notify_barrier`) · JEP 380 (Unix-domain STREAM channels, JDK 16) · the FFM JEPs (preview in 21 — `--enable-preview` is not a production flag; final in JDK 22 — cite the JEP number) · the JDK LTS cadence + Corretto 25 availability (the pin is LTD-10 Corretto 21; the trajectory is a charter input, not your decision) · the `LinuxSystemPaths`/`$NOTIFY_SOCKET` resolution if our code already reads it (grep `NOTIFY_SOCKET` in `platform/`).

## 1. The charges (the return's §1–§6)

**§1 What "live" means today, measured at the code.** A table of every liveness/readiness signal the system has: the projection mode behind `/health` (where it is set, what flips it to non-LIVE, what does NOT — an adapter down? the bus stalled? disk full?), the `HealthLoop`/`SystemHealthSnapshot` fields and who reads them, the readiness gate's two header constants, the ExitCode contract (`common.sh:94–:98`), the `Restart=` policy, the FE's availability signal. For each: the file:line, the semantics, and the gap between what it measures and what an operator would assume "healthy" means.
**§2 The contract options (decision-shaped; the charter rules).** (C-1) `/health` = the projection mode, as shipped — document the promise precisely and stop; (C-2) `/health` widens to a composite (projection AND bus-progress AND N-of-M adapters) — name exactly which signals and the failure semantics (a down adapter ⇒ 503 would make the packaged unit restart-loop on a missing dongle — walk that consequence); (C-3) the split: `/health` stays liveness (the process can serve), `/readyz` becomes the composite for operators/dashboards, both loopback-only, both one enum word, no envelope; for each: the Doc 09/12 sections it touches (AMD-class?), the probe/unit consequence, the FE consequence, the CI proof in install-smoke (+N PASS lines). The bus-progress signal: specify what "progress" would be measured as (the last-seen checkpoint advancing — the OR-GATE-M9.4-NCFG deflake row's own language) and whether it belongs in the contract or only in the snapshot.
**§3 The transport options, re-priced with today's facts.** (T-0) STAY: `Type=exec` + `ExecStartPost` probe (READY) + a **distribution-only watchdog**: a `homesynapse-watchdog.timer`/`.service` pair that probes `/health` every N s and `systemctl restart homesynapse.service` on K consecutive non-200s — zero JVM code, zero deps, provable in install-smoke (kill the app's liveness and watch the timer restart it), the Pi cost of one `curl` per N s; name its blind spots (a wedged JVM that still answers 200; the restart storm interaction with `StartLimitBurst`); (T-1) `systemd-notify --ready` as a subprocess for READY=1 only — with the `NotifyAccess=` value it REQUIRES (the child's PID ≠ the main PID; does `NotifyAccess=main` accept it? cite `systemd.service(5)`; `--pid=` needs what privilege?), no watchdog; (T-2) FFM/Panama `sendto(2)` on an AF_UNIX SOCK_DGRAM from inside the JVM — final API in JDK 22+, so it is gated on the JDK pin moving (cite the JEP + the Corretto 25 availability; price the LTD-10 change as a charter item, not yours); (T-3) JNR/JNA — M5-D rejected it; confirm the rejection still holds or say why not; (T-4) a tiny native helper (`/opt/homesynapse/libexec/notify`) — a C binary shipped in the image that the JVM execs ONCE per heartbeat, or a persistent sidecar that relays over a STREAM socket the JVM CAN open (JEP 380) and forwards as datagrams — price the build (cross-compile in the arm64 CI leg R-7 just made real), the trust surface, and the sidecar's own liveness. For each: code · deps · install-smoke provability · Pi runtime cost · failure modes · whether the staged M13 flip block stays as written.
**§4 The interaction with R-9's `/health`.** `/health` is the seam every option uses; say what each option needs `/health` to promise (T-0 needs it to be LIVENESS, not readiness — the C-1/C-3 choice and the T-choice are coupled; make the coupling explicit as a 3×5 table).
**§5 The recommendation + the one-word asks.** Your recommendation per half with the reasoning in ≤ 10 lines each, then the H10 form: **R-L1 (contract): C-1 / C-2 / C-3 (your rec)** · **R-L2 (transport): T-0 / T-1 / T-2 / T-4 (your rec)** · **R-L3 (watchdog): opt-in WatchdogSec YES / NO (your rec; Nick's per OR-M13)**.
**§6 What a spike would have to prove** (if the charter adopts T-2/T-4 or C-3): the ≤ 1-day throwaway spike's question, its instrument, its pass/fail — spikes live outside the production tree (the PM skill's rule).

## 2. Fences

Research-only: no code, no AMD text, no Doc edits; the `/health` contract as shipped by R-9 is NOT re-litigated here (it may be EXTENDED by C-2/C-3 later) — law 16; no `--enable-preview` proposals for production; the JDK pin is a charter input you price, never change; the INTERIM OPERATOR LAW and E3's closure record are untouched.

## 3. Return shape (≤ 7 pages)

§0 the two recommendations in one paragraph · §1–§6 · §7 sources (primaries with fetch dates; code cites with the SHA) · ≤ 5 harvest lines.

## 4. Pushback welcome

If T-0 makes the M13 flip unnecessary for the curated-fleet v1, say so plainly — "defer M13 indefinitely, delete the staged block" is a lawful recommendation if the evidence carries it. If `/health` as shipped already IS the right liveness contract and the composite belongs in the snapshot only, say that.
