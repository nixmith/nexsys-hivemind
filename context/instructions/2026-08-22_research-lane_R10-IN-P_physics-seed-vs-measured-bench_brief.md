<!--
file: context/instructions/2026-08-22_research-lane_R10-IN-P_physics-seed-vs-measured-bench_brief.md
purpose: R10-IN-P — an R-10 charter INPUT: the physics-aware direction (the 2026-08-04 seed → the L-E deep-research return → RS-2's three one-word options) adjudicated against what the bench has ACTUALLY MEASURED (arc-1: the charter is written after the hardware leg so it is grounded in measured behaviour, not theory). RS-2 delivered the evidence base and the options; what the charter still lacks is the MEASURED-FEASIBILITY column — per option, what the existing corpus, fleet, scenarios, and instruments can already support, what each would need built, and the attended-hours cost against the A-14 floor. This lane fills that column from the FILED measurements. It adopts nothing (RS-2's law: research-only, zero design authority; the charter decides).
audience: a fresh Cowork research lane (Fable 5 Extra; the device bridge for the repos; web only to re-verify a cited primary) + the hub (intake) + the R-10 charter session (reads this beside RS-2 §3 and the seed).
status: DISPATCH-READY (v56 beat 1). Due: before the R-10 charter weekend (on disk by Fri 2026-08-28 EOD CT; earlier is better — it is read-only and parallel).
return: nexsys-hivemind/context/audits/<filing-date>_R10-IN-P_physics-seed-vs-measured-bench_return.md (filing-day dated; ONE artifact). Read-only on all five repos; no commits; NO hardware (the bench is HANDS OFF until R-5 — s31 legs/nightly untouched; you cannot reach the Pi and must not try; the filed measurements ARE the corpus).
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-22_research-lane_R10-IN-P_physics-seed-vs-measured-bench_brief.md and execute it. Read-only; one return file; no hardware."
decision it feeds: the R-10 charter's adopt/decline word on the physics-aware direction (RS-2 §3: A ADOPT-NOW-SCOPED / B / C) — specifically whether option A's bench-and-corpus pilot is feasible on the fleet and corpus we HAVE, and what it costs in attended hours.
audit bar (two-layer): every measured figure cites the filed return/bundle/record by path + line (never the Pi, never memory); every "the corpus carries X" claim names the scenario/fixture/bundle file; every cost figure shows its arithmetic against A-14's floor (`context/research/2026-08-02_A14_attended-hours_charter-input.md`); every gap is stated as "absent from the filed corpus" (not "does not exist") when you could not find it. The hub re-reads ≥ 5 cites at the source.
-->

# R10-IN-P — the physics-aware seed vs the measured bench

## 0. Read first (pointer-form; banked verdicts are final — law 16 — never re-derive them)

The seed: `context/research/2026-08-04_physics-aware-core_strategic-seed_charter-input.md` · L-E: `context/research/2026-08-15_LE_physics-aware-deep-research_return.md` (POST-GATE READING by its own stamp; §1.1's P1-A envelope-corpus method is the adopted class) · RS-2: `context/audits/2026-08-21_RS2_physics-world-model_charter-evidence_return.md` (§2 the Substrate mapping; §3 the three options — your adjudication frame) · the Substrate Thesis `context/strategy/Substrate_Thesis_v0.md` §5/§9 (the evidence discipline) + D5 · the S-10 close `context/assessments/2026-08-14_S10_close_ranked-program.md` (R-10's slot and docket) · A-14 (the 15 h/wk weekend-anchored floor). **The measured corpus:** `nexsys-bench/` — `README.md`, `scenarios/*.yaml` (12 files incl. `SCENARIO_FORMAT.md` + `constants.yaml`), `docs/2026-07-06_m9.4-bench-acceptance-record.md`, `docs/2026-07-10_bench-automation-charter.md`, `docs/bench-log-retention-policy.md`, `corpus/`, `fixtures/`, `harness/`, `bench-logs/` (measure sizes with `git cat-file -s`; sample, never read whole) · the filed reads in `nexsys-hivemind/context/audits/`: `2026-07-28_B2_suite-port_return.md` §§12–13 · `2026-08-02_B3_night2_command-confirm-s31_evidence-read.md` · `2026-08-16_THE-READ_gate-record_and_verdict.md` (the 21-MUST walk: what the gate accepted as measured) · `2026-08-20_midweek-FE-deploy_sitting-record.md` · `2026-08-20_R1R2_return.md` · the compounding-testing doctrine `context/process/2026-07-18_compounding-testing-doctrine.md` + the playbook `context/process/bench-troubleshooting-playbook.md` (the instruments and their resolutions, arc-12) · the dashboard fixture work (`context/audits/2026-08-21_FE-lane_never-triggered-fixture_F-S3_return.md` — the explainability surface's measured dialect).

## 1. The charges (the return's §1–§5)

**§1 The fleet + the channels, as filed.** What devices the bench fleet carries (the six-device fleet — classes, the capabilities/attributes each reports, the numeric vs boolean split), from the filed records (the acceptance record, the scenarios' device references, the R1R2 return, the sitting record). Which channels are NUMERIC and continuous (a temperature/humidity/illuminance/power reading) vs event/boolean (motion, contact). This decides whether RS-2 option A's "ONE numeric environmental channel" already exists or must be added. State it as a table with cites.
**§2 The corpus, as filed.** What the event log and bench corpus already carry that a physics-characterization track would consume: event cadence (events/day, from filed row counts and bundle stamps), the measured `confirmed|unconfirmed` moat figures (the command-confirm suites), the s31 settle measurements, the history-seeded scenario class (arc-11), the nightly bundle's shape and retention, capture fixtures under `fixtures/` and `corpus/` (counts, sizes, date span). Against L-E's P1-A envelope-corpus method: which of its inputs are present today, which absent.
**§3 Per-option measured feasibility (RS-2 §3 A/B/C).** For each option: (a) what it needs that EXISTS (cite); (b) what it needs that is ABSENT (name the instrument/hardware/scenario that would provide it and size it — a sensor class, a scenario file, a harness arm); (c) the first honest measurable milestone on our rig (the "honest null" included); (d) attended hours per week against A-14's floor, shown as arithmetic beside the R-3/R-4/R-5 weekend anchors already claiming those hours; (e) the Substrate-Thesis layer it lands in (L0–L3) and the D5-lawful sentence for it.
**§4 The conflicts the charter must rule.** Where option A competes with SP-4 (L-E's senior pilot) for the same hours or the same bench instrument; where the R-5 settle-redesign (s31/nightly HANDS OFF until then) blocks any new bench scenario; where a numeric channel would change the corpus-model IR (`docs/2026-06-28_phase-2_corpus-model_IR-schema-and-onboarding-pipeline.md`) — name the schema fields that would move.
**§5 The proposed instrument blocks (PROPOSED, never run).** If a measurement the charter needs is missing and is cheap (≤ 15 min on the bench, no scenario change), write it as a playbook-§8-shaped operator block the hub can dispatch AFTER R-5 — self-contained, goal + done-when first, expected tokens, ⏺ lines, STOP-gate in its own block. Mark every one "after R-5; s31/nightly untouched".

## 2. Fences

No hardware, no Pi, no scenario edits, no proposals that touch the s31 legs or the nightly before R-5 (they may be SCHEDULED after it). No design authority: options and costs, never a decision. The D5 language law on every restatement of the thesis. Archives and bundles are read by sample (`head -c`), never whole.

## 3. Return shape (≤ 6 pages; tables carry the weight)

§0 the one-paragraph verdict ("option A is feasible on the fleet as filed / needs X / costs Y h/wk") · §1–§5 as above · §6 sources (path:line) + ≤ 5 harvest lines + the list of things you could not find in the filed corpus (each a candidate instrument, not a claim of absence).

## 4. Pushback welcome

If RS-2's option A is unmeasurable on this rig without a purchase, say so with the price; if the corpus already supports a narrower honest pilot RS-2 did not name, lead with it.
