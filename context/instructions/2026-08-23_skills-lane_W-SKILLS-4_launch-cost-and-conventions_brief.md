<!--
file: context/instructions/2026-08-23_skills-lane_W-SKILLS-4_launch-cost-and-conventions_brief.md
purpose: W-SKILLS-4 — the skills currency + launch-cost pass minted by the W-HIVE-1 return §4b (v56 beat 2 ratification): after the spine rotation, the three role skills are the DOMINANT launch cost (PM `SKILL.md` 53,020 B — 46.4 % of it one masthead comment; coder 35,667; FE 41,636; hub skill-side share now ≈ 70 % of the spine-side read). This pass trims PROVENANCE (never rules), executes the two W-HIVE-1 items routed here (P7, P8's convention half), lands the lesson-size convention, and folds the R-9 lesson candidates — all under the W-SKILLS census discipline: every rule name in = every rule name out, retirements only by name with the hub's ratification.
audience: a dedicated fresh skills lane (the W-SKILLS-3 precedent: the lane EDITS the writable sources, commits nothing; the hub audits; Nick commits and then copies the mirrors) + the hub + Nick (the mirror copy + Check 9).
status: DISPATCH-READY (v56 beat 4). Dispatch Mon/Tue Aug 24–25. Baseline: hivemind at the v56 beat-4 commit; skills `5105abc`.
return: nexsys-hivemind/context/audits/<filing-date>_W-SKILLS-4_return.md (ONE artifact; the RULE CENSUS in/out table is its spine). The lane edits ONLY: nexsys-hivemind/{project-manager,coder}/** and nexsys-skills/orchestrators/nexsys-frontend/** — census-exact, enumerated per file; NOTHING under context/** except the return.
dispatch: "Read nexsys-hivemind/context/instructions/2026-08-23_skills-lane_W-SKILLS-4_launch-cost-and-conventions_brief.md and execute it. Edit only the three skill trees; one return file; no commits."
audit bar: the census table (rules-in / rules-out per file, every prior name surviving or retired BY NAME with the reason); before/after byte counts per file measured at the objects; every moved rule's new carrier quoted; the hub re-counts two files' censuses and diffs three moved rules at the bytes.
decision it feeds: the hub's ratification beat → Nick's commit + mirror copy (`diff -rq` all three pairs — Check 9) → every future hub/coder/FE launch pays the reduced read.
-->

# W-SKILLS-4 — the launch-cost + conventions pass

## 0. Read first

W-HIVE-1 `context/audits/2026-08-22_W-HIVE-1_hivemind-token-economy_return.md` §4b (the measurements; the two observations) + §3 P7/P8 · the five-lane intake audit §1 (the routing rulings) · the W-SKILLS-3 masthead of `project-manager/SKILL.md` (the census form you must reproduce) · `coder/CLAUDE.md` whole · `coder/references/freshness-preflight.md` · the R-9 return §WUCP (the three lesson candidates) · `context/lessons/coder-lessons.md` (the size problem P6 rotates; your job is the CONVENTION).

## 1. The charges (census-exact per file; ≤ 6 files touched)

**(a) `project-manager/SKILL.md` — provenance trim, ZERO rule loss.** The masthead's `last-verified:` narrative (2,950 B) and `pass-history:` (1,066 B) are PROVENANCE — `pass-history` says so of itself. Compress `last-verified:` to ≤ 400 B (the census verdict + the return pointer — the W-SKILLS-3 narrative moves VERBATIM to that pass's return-file's own head as an addendum, or to a new `project-manager/references/pass-history.md` carrying both lines whole); `pass-history:` moves with it, leaving a one-line pointer. **The four rule lists (arc-disciplines 37 · durable D1–D18 · strategy-layer 4 · state-pointer 1) are UNTOUCHED — 60-in / 60-out, every name surviving.** Target: 53,020 → ≤ 49,000 B.
**(b) `coder/CLAUDE.md` — P7 executes.** The Tier-1 row "read the current week's plan in `../context/planning/weeks/`" (`:23`, `:57`, `:89`) RETIRES — the newest file there is 2026-W31, three weeks stale; the plan of record = the newest `coder-handoff.md` entry + the active instruction + (new) `context/planning/`'s newest `*plan-of-record.md` BY POINTER when one exists. −6,925 B per coder launch.
**(c) `coder/CLAUDE.md` + `coder/SKILL.md` — P8's convention half.** The closeout convention changes: the coder-handoff frontmatter `last-verified:` chain is NO LONGER PREPENDED at closeout — the newest entry is authoritative by position; the masthead keeps `status/update-cadence` only. (The hub retires the existing chain at its next rotation, folding the five segment-only facts into their entries first — the hub's act, not yours; you change what future closeouts WRITE.)
**(d) The lesson convention.** Wherever the coder skill tree defines the lessons format: each lesson ≤ 1,200 B, `Discovery` + `Impact` only, `Detail` = a one-line pointer to the return that minted it; when the file exceeds 24,000 B the HUB rotates the oldest lessons (by count) to the archive. State it as the rule; do not rewrite existing lessons.
**(e) The three R-9 lesson candidates → the coder tree's carrier for framework/test patterns** (java-patterns or testing-standards, ≤ 3 lines each, pointer to the R-9 return): the framework's silent HEAD-for-GET fallback is pinned from bytecode · a refusal guard on a mutator re-fixtures every single-token test (grep the method name, assert the non-refused outcome) · a DNS-safe address-literal classifier never hands a hostname to the resolver.
**(f) `nexsys-frontend/SKILL.md` — MEASURE ONLY this pass** (41,636 B; report its masthead/provenance split for a future FE-half pass); edit nothing unless a provenance line is as mechanically separable as (a)'s — if so, same treatment, disclosed.

## 2. Fences

No rule retires without its NAME in the census table and the hub's ratification · state-pointer discipline holds (no project state enters any skill file) · the mirrors are Nick's copy AFTER ratification — the lane never touches `.claude/skills/**` · the hivemind `context/**` is out of scope except the return · every byte moved is byte-identical at its new carrier (`cmp` the extracted spans).

## 3. Return shape (≤ 4 pages)

§0 the census verdict (N-in / N-out, survivals, moves, zero retirements — or each retirement by name) · §1 per-file before/after bytes + the per-launch saving (hub/coder/FE) · §2 the moved-provenance carriers, quoted heads · §3 the convention diffs verbatim · §4 pushback (if a "provenance" line turns out to carry a rule, STOP on that line and say so) · ≤ 5 harvest lines.
