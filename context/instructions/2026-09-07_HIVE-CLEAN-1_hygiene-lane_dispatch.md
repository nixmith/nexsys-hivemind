<!--
file: context/instructions/2026-09-07_HIVE-CLEAN-1_hygiene-lane_dispatch.md
purpose: THE DISPATCH for HIVE-CLEAN-1 — the hivemind/ClaudeFolder hygiene pass chartered by the STATE-OF-THE-PROGRAM assessment §4 (Nick's directive 2026-09-06: "clean up the nexsys-hivemind repo for any potentially outdated or stale information (as well as throughout the rest of ClaudeFolder/) that will mislead our future Claude Cowork and Claude Code sessions"; D4 `CLEAN: go`). Self-contained: the lane reads THIS file whole, then only what §1 names. Every step is a script; the lane executes, records, and invents nothing. Nothing is deleted; evidence bodies are never edited; the spine and the skill trees are never touched.
audience: the HIVE-CLEAN-1 lane (a fresh Cowork window) · the hub (audits under §6; commits) · Nick (the one paste at §7; the `_scratch` script is his hands)
state-type: lane dispatch
status: DISPATCH-READY (v66 beat 9, Sun 2026-09-06 ~20:0x CT; instrument 2026-09-07T01:0xZ). Baseline: nexsys-hivemind at the v66 close (`68e183b` or newer; verify at launch); the hivemind porcelain must be EMPTY at launch except lane `??` files the hub has not yet staged (say which). Clock ≤3 h; a partial return with an honest census outranks a complete one later.
-->

# HIVE-CLEAN-1 — the hygiene pass (dispatch)

## §0 The contract (every line binds)
- `date -u` FIRST; state your instrument limit (device_bash on the mount; each call a fresh shell, ~45–120 s; `git ls-files`, never a wide `find`); CT = UTC−5 once.
- **The law of this pass:** a file's `status:` line is a claim about NOW and is audited like any claim. **Nothing is deleted.** What is over is (a) MARKED over in its own frontmatter by a status-line PREFIX and (b), for the executed OPERATIONAL classes only (prompts · packets · briefs · dispatch texts · instructions · lane briefs), MOVED by `git mv` under an `archive/` sibling of its directory so listings and greps show only what is current. **The evidence trees (`context/audits/**`, `context/assessments/**`, `context/research/**`) keep their bodies AND their locations** — their FILED/COMPLETE/RETURNED statuses are correct and permanent; a moved audit breaks the spine's citations. Executed instructions DO move (their citations resolve by the rule §5 writes into `canonical-paths.md`: a cited path not found is looked up in that directory's `archive/`).
- **Never touch:** `context/handoff/pm-handoff.md` · `context/status/PROJECT_SNAPSHOT.md` · `context/handoff/coder-handoff.md` · `context/lessons/*` · `project-manager/**` · `coder/**` · `nexsys-skills/**` · `homesynapse-core/**` · `homesynapse-core-docs/**` · `nexsys-bench/**` · `ClaudeFolder/_archive/**` · `ClaudeFolder/legal/**` · the brand PDFs/DOCX/XLSX at the ClaudeFolder root · any file's BODY below its frontmatter (status lines only, plus the two wayfinding files in §1 step 7 and the two banners in step 8).
- **KEEP LIVE (do not move, do not re-status):** `context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` · the newest `*_OPERATOR-BRIEF_for-Nick.md` (v66) · `context/handoff/2026-09-06_v66_dispatch-text_verbatim.md` · `context/handoff/2026-09-06_BLOCK6_docs-correction_operator-card.md` (not yet executed) · `context/instructions/2026-09-06_H8a_real-wire_v113-keys_and_failchan-proof_navigator-packet.md` (not yet executed) · `context/audits/2026-09-06_H8a_real-wire_operator-record.md` (the scaffold) · this file and `…_W-SKILLS-8_…_dispatch.md` · `context/instructions/2026-08-22_research-lane_RS3-WMARKET-2_market-currency-refresh_brief.md` (on cadence) · `context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_…md` · `…_SESSION-SYNTHESIS_and_decision-record.md` · `context/planning/2026-09-06_v65-b6_post-landing_program_and_four-charters.md` · `context/planning/2026-09-05_v65_four-lane-map_14-days.md` · `master-release-plan.md` · `phase-3-milestone-backlog.md` · `research-agenda.md` · everything under `context/process/` and `context/protocols/` · `context/strategy/claim-register.md` · the strategy files with a LIVE status (`Revenue_Model…` · `Six_Battlefields…` · `Substrate_Thesis_v0.md` · the standing directive · the brand-program plan-of-record · the counsel-package draft) · `context/decisions/*` (status lines only, no moves).
- **The instrument of the census (run it FIRST, file its output as your Appendix A before any edit):** for every tracked `context/**/*.md` not under `archive/`, print `path | bytes | the status: line's first 90 chars | last-verified: first 40 chars`. Work from that list; never from a directory walk of file bodies.
- **Return:** `nexsys-hivemind/context/audits/<CT-date>_HIVE-CLEAN-1_return.md` — ONE file, **≤8 KB** (a ceiling): §0 the counts by directory (moved · re-statused · untouched) + the three zero-greps (§4) + every EXCEPTION you took and why · §1 the deviations by tag · §2 the WUCP note (none — this lane writes no handoff entry; the hub records the pass). **The full move/re-status table goes to a SECOND file** `nexsys-hivemind/context/audits/<CT-date>_HIVE-CLEAN-1_census.tsv` (one row per file: `old_path \t new_path \t old_status_head \t new_status_head`). Your last line: `RETURNED <path> <bytes>` (the .md). Stage nothing; commit nothing; the hub audits and commits.

## §1 The recipe (execute in this order; each step = one device_bash script you write, run, and quote the counts from)
**The status prefix forms (exact):** an executed instruction/brief/packet/prompt → `status: EXECUTED <date> — return: <path> · audit: <path> · was: <old status first 60 chars>`; a superseded prompt/brief/plan → `status: SUPERSEDED <date> — by: <path> · was: …`; a paused program → `status: PAUSED 2026-09-07 — no beat since 2026-07-19; resumes at the Matter horizon (Strategy v1.2) · was: …`; a consumed pre-verification → `status: CONSUMED by <WU> · was: …`; a file with no status line → insert `status: REFERENCE — <file's own date> — superseded-by: <path>|none` as the last frontmatter line before `-->`. Apply with `sed -i` on the `^status:` line ONLY (assert exactly one match per file before editing; a file with two `status:` lines is an EXCEPTION, reported, untouched).
1. **`context/handoff/`** — (a) every `*_PM-mission-control_v*_orchestrator_session_prompt.md` except v67 → `git mv` to `context/handoff/archive/prompts/` with the SUPERSEDED prefix (by: the next version's path; v66's status already says RETIRED — prefix nothing more, just move); (b) every operator packet · navigator packet/prompt · dispatch text · dispatch prompt · operator brief · context pack · decision brief · session plan whose session has closed (everything dated ≤ 2026-09-05 in the directory, plus `2026-09-04_R-4b_*` and `2026-08-30_R4_*`) → `git mv` to `context/handoff/archive/<yyyy-mm>/` with the EXECUTED prefix (the return/record path from the same-date file under `context/audits/` — if none exists, `return: none on disk`); (c) the v65 brief and the v61 brief → SUPERSEDED (by the v66 brief) + moved; (d) `2026-07-19_04p-adoption_BENCH-STOP_learn-persist-finding.md` → EXECUTED (LEARN-PERSIST landed 2026-07-19) + moved. KEEP LIVE: the §0 list.
2. **`context/instructions/`** — every file whose return exists under `context/audits/` or `context/research/` (match by the WU token in the filename: `SKIP-VIS` · `DASH-SERVE` · `B3` · `R1-R2` · `R6-R8` · `R7` · `R7b` · `R9` · `S-5a` · `S-5c` · `M9.5-DURd*` · `FAILCHAN*` · `CG-123` · `F-R4-1` (the 09-02 one) · `F-R4-1b` (landed `39c8dd3`) · `FE-113` · `FE-HONEST-1` · `SWAP-*` · `W-SKILLS-2/3/4/5` (NOT 7 — see below) · `REV-1` · `LE` · `LF` · `WCOHERE` · `WMARKET` · `R11` · `RS-4` · `R10-IN-L` · `R10-IN-P` · `BRAND-SPRINT-1` · `W-HIVE-1` · `LA/LB/LC/LD` · `R1/R2/R3` research briefs · `evidence-read_s31-nightly-0902` · `PKG-SEC-1` · `R3b_PKG-E2E-1` · `owed-items_operator-packet`) → EXECUTED prefix + `git mv` to `context/instructions/archive/`. A WU token with NO return on disk → `status: DORMANT 2026-09-07 — no return on disk · was: …`, moved. KEEP LIVE: the H8-a packet · `RS3-WMARKET-2` · the two dispatch files of 2026-09-07. **The W-SKILLS-7 dispatch:** `status: SUPERSEDED 2026-09-07 — by: context/instructions/2026-09-07_W-SKILLS-8_skills-pass_lane-dispatch.md (which reads this file whole) · was: …` — NOT moved (W-SKILLS-8 reads it at its current path).
3. **`context/planning/`** — `2026-05-31_release-runway-roadmap.md` · `2026-06-06_post-M4-plan-and-M5-scoping.md` · `2026-06-08_M6-charter.md` · `2026-06-13_side-research-candidates_CORE-DOCS.md` · `2026-06-13_strategy-refresh-drafts_R15.md` · `2026-06-14_automation-engine-superiority_research-vector_proposal.md` · `2026-06-26_v6-forward-plan_execution-refresh.md` · `2026-06-27_causal-read-API_scope-freeze-and-milestone-breakdown.md` · `2026-06-29_frontend-master-plan.md` · `2026-06-30_frontend-pre-hardware-sprint.md` · `2026-09-03_next-sessions_PROGRAM_v62-onward.md` → `status: HISTORICAL 2026-09-07 — superseded by: context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_assessment_trajectory_and_hygiene-program.md §3 · was: …` (status only; NOT moved — other files cite them). `weeks/**` → `git mv` to `context/planning/archive/weeks/` (retired by Nick 2026-08-09; `canonical-paths.md` already says so).
4. **`context/programs/matter-design/`** — every file with READY/ACTIVE/CURRENT → the PAUSED prefix (status only; not moved).
5. **`context/strategy/`** — every file WITHOUT a `status:` line gets the REFERENCE line (§1 form; `superseded-by:` = the newest file on the same subject if one is obvious from the filename, else `none`); files with a status line untouched. **`context/decisions/`:** same rule for the no-status files; nothing moved. **`context/pre-verifications/`:** `WU-DASH-SERVE.md` · `WU-SKIP-VIS.md` → CONSUMED (by DASH-SERVE / SKIP-VIS). **`context/open-questions.md`** (retired) → `git mv` to `context/archive/open-questions.md` (create the directory) with a SUPERSEDED prefix pointing at the brief's §HELD as the wait-state ledger.
6. **The strategic map:** `context/strategic-context-map.md` — for every path this pass moved, update the citation to the new path (`sed` on the exact old string; count the replacements). Same for `context/truth-map.md` and `context/canonical-paths.md`.
7. **Wayfinding (bodies allowed, minimal):** `nexsys-hivemind/START_HERE.md` — re-verify every pointer line against the current shape (the stable prompt · the brief's §HELD as the operator's entry · the archive convention) and bump `last-verified:`; `canonical-paths.md` — add the section "The archive convention (HIVE-CLEAN-1, 2026-09-07)": (i) an executed operational file lives in its directory's `archive/` with an EXECUTED/SUPERSEDED prefix; (ii) a cited path not found is looked up in `<dir>/archive/`; (iii) a `status:` line is an audited claim — LIVE/READY/ISSUE-READY are asserted only of work not yet done; (iv) the three zero-greps of §4 are the invariant (Check 12). `truth-map.md` — one row: "what is current in `context/instructions/`? — the directory listing minus `archive/`".
8. **`ClaudeFolder/` outside the repos (bodies allowed for the two banners only):** `Founder_Operating_Plan_v1.md` and `Technical_Roadmap_v1.md` — insert as line 1 a two-line banner: `> CURRENCY (2026-09-07): this document is a June 2026 baseline. The plan of record is nexsys-hivemind/context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_assessment_trajectory_and_hygiene-program.md §3; the strategy of record is nexsys-hivemind/context/strategy/ (Strategy v1.2, ratified 2026-09-03). Read those first.` `_scratch/` — you cannot delete on the bridge: write `ClaudeFolder/_scratch/CLEANUP_2026-09-07.sh` that `rm -rf`s every `_scratch/v56`…`v65` directory and every top-level `_scratch/*` file older than 7 days (list them by name in the script; `v66/` and the `2026-09-06_*` msg files STAY), plus a `_scratch/README.md` (four lines: per-session directories; deleted at the next session's boot by Nick's hands; msg files kept 14 days; nothing here is evidence). Nick runs the script — his act, not yours.
9. **The census + the return** (§0's two files). Order of priority if the clock runs out: steps 1 and 2 FIRST (the misleading classes), then 7, then the rest.

## §2 Predictions (adjudicate first in your §0)
P1 ≥70 files moved or re-statused · P2 zero evidence-tree files touched · P3 the three greps of §4 read zero after the pass · P4 the strategic map needs ≥10 citation updates · P5 a fresh Claude Code lane asked "read `context/instructions/` and tell me what is current" would answer: the H8-a packet, RS3-WMARKET-2, the two 2026-09-07 dispatches (the hub runs this test after the pass).

## §3 Fences
No deletion (ever) · no body edit except §1 steps 7–8 · no move in the evidence trees · no edit to the spine, the lessons, the skill trees, any other repo · `git mv` only (history follows) · no rewrite of any status line's ORIGINAL text — a prefix + `· was: …` · stage nothing, commit nothing · no candidate brand name typed anywhere · if any step's assert fails (two status lines; a path cited by the spine you are unsure about), SKIP it, record the EXCEPTION, continue.

## §4 The acceptance invariant (Check 12 from here on; the hub re-runs these)
```
cd nexsys-hivemind
grep -l '^status: \(ISSUE-READY\|DISPATCH-READY\|LIVE\|READY\)' context/instructions/*.md | grep -v 'H8a\|RS3-WMARKET-2\|2026-09-07_' | wc -l     # expected 0
grep -l '^status: LIVE' context/handoff/*orchestrator_session_prompt.md | grep -v v67 | wc -l                                              # expected 0
git ls-files 'context/planning/weeks/*' | wc -l                                                                                            # expected 0
```

## §5 Clock
≤3 h. A living census from the first hour; a partial return with honest counts outranks a complete one later.

## §6 The hub's audit rules (pre-filed)
R1 the census TSV rows = the porcelain's renames + modifications exactly · R2 zero evidence-tree paths in the diff · R3 every new status prefix keeps `· was:` with the original head · R4 the three greps read zero · R5 every `superseded-by:`/`return:` path RESOLVES (`git ls-files` or the archive) · R6 START_HERE's pointers each resolve · R7 the `_scratch` script lists only paths that exist and never `v66/`.

## §7 THE DISPATCH LINE (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected)
```
date -u first. You are the HIVE-CLEAN-1 hygiene lane for NexSys/HomeSynapse. Read nexsys-hivemind/context/instructions/2026-09-07_HIVE-CLEAN-1_hygiene-lane_dispatch.md WHOLE and execute it exactly: §0's contract and never-touch/keep-live lists, then the census instrument first (Appendix A), then §1 steps 1–9 in order as scripts (git mv + status-line prefixes only; never a deletion, never an evidence-file body, never the spine, lessons or skill trees), then §4's three greps. Adjudicate §2's predictions first in your §0. Return TWO files: nexsys-hivemind/context/audits/<today's CT date>_HIVE-CLEAN-1_return.md (≤8 KB, a ceiling; §0 counts + the three greps + exceptions first) and …_HIVE-CLEAN-1_census.tsv (one row per file touched). Stage nothing; commit nothing; the hub audits and commits. ≤3 h. Your last line: RETURNED <path> <bytes>. You report to the hub, not to me.
```
