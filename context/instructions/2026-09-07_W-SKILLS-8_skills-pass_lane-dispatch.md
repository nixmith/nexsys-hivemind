<!--
file: context/instructions/2026-09-07_W-SKILLS-8_skills-pass_lane-dispatch.md
purpose: THE DISPATCH for W-SKILLS-8 — the skills pass W-SKILLS-7 never ran (no card ever reached disk), plus the laws minted since it was written (v65 b7 → the v66 close). THIS FILE IS AN ADDENDUM: the W-SKILLS-7 dispatch's §0 contract, §1 read-set, §2 deltas, §3 predictions, §4 fences and §6 audit rules STAND VERBATIM and are executed as written; §2b below adds the new folds with their exhibits, §3b the added predictions. The asset this pass protects: the token shape (every SKILL.md ≤ ~4k tokens, byte-unchanged; laws live in the ledgers, read when cited). "Contextually optimized" = current ledgers, state-free skill files, a preflight that keeps the tree honest.
audience: the W-SKILLS-8 lane (a fresh Cowork window) · the hub (audits; commits hivemind + skills) · Nick (the one paste at §7; ONE account re-sync after the hub's commits)
state-type: lane dispatch (addendum to context/instructions/2026-09-05_W-SKILLS-7_skills-pass_lane-dispatch.md)
status: DISPATCH-READY (v66 beat 9, instrument 2026-09-07T01:xxZ). Baseline: nexsys-hivemind at the v66 close or newer; nexsys-skills `f9c0bf4`; Check 9 28/28 at the bytes at v66 boot. May run BESIDE HIVE-CLEAN-1 (disjoint write-sets: this lane writes `project-manager/references/`, `coder/references/`, `nexsys-skills/orchestrators/nexsys-frontend/references/` and the three `pass-history.md`; HIVE-CLEAN-1 writes `context/**`). Clock 2–3 h.
-->

# W-SKILLS-8 — the skills pass (dispatch; addendum to W-SKILLS-7)

## §0 The contract
Read `context/instructions/2026-09-05_W-SKILLS-7_skills-pass_lane-dispatch.md` WHOLE first — its §0–§6 are your contract, read-set, deltas, predictions, fences and audit rules, unchanged. Then read this file's §1b–§3b. `date -u` first; CT = UTC−5 once. **Return:** `nexsys-hivemind/context/audits/<CT-date>_W-SKILLS-8_return.md` (the W-SKILLS-7 return shape: §0 card ≤3 KB first — the census exact in BOTH repos · the mint→arc mapping table (all 22 W-SKILLS-7 candidates + the §2b items) · the three SKILL.md md5s before/after; whole ≤12 KB — a ceiling, not a target). Last line `RETURNED <path> <bytes>`. Stage nothing; commit nothing.

## §1b Additional read-set (after the W-SKILLS-7 §1 list; nothing else)
8. `context/lessons/pm-lessons.md` — the section "THE OPERATOR-LOAD LAW" (2026-09-06) whole.
9. `context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_assessment_trajectory_and_hygiene-program.md` §2.2–§2.5 and §5 (the rules and their exhibits).
10. `context/planning/2026-09-06_v66_SESSION-SYNTHESIS_and_decision-record.md` §2 and §5 (the three mints; D4's per-domain lane law; D5's stable prompt).
11. `context/lessons/coder-lessons.md` — the 2026-09-06 entries (FE-113: the fence/constant lesson; F-R4-1b: the miss-script sweep).
12. `context/audits/2026-09-06_FE-113_intake_two-layer-audit_v66-b5.md` §3 · `context/audits/2026-09-06_F-R4-1b_intake_two-layer-audit_v66-b8.md` §1 · `context/audits/2026-09-06_HERO-0_intake_two-layer-audit_v66-b6.md` §0–§1 (the exhibits).
13. `context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` §0 (the laws as the stable prompt states them — your ledger text and this list must agree; where they differ, the exhibit decides and you file the difference as `[REVIEW]`).

## §2b The additional deltas (each: the operative sentence + mechanism + exhibit path; placement named)
- **PM `laws-ledger.md`** (continue the numbering after the W-SKILLS-7 folds): **(a) THE OPERATOR-LOAD LAW** as ONE arc — the six clauses verbatim + the mint + the three v66 refinements: a TIME-GATED act is handed at its time by a scheduled send, never behind the report chain (exhibit: the decision record §2(b) — the rig missed); a slot-releasing landing outranks a read-only dispatch — ask "dispatched | not yet" and re-order (exhibit: the v66 b5 beat); a paste never asks the operator to remember — slots are filled from the record or defaulted (exhibit: the decision record §2(d)). **(b) The instrument-beside-the-claim rule:** every source-state claim in an instruction carries the command that made it, so the lane re-runs the command, not the conclusion (exhibits: the FE-113 audit §3 R1 — `EXPECTED_VERSION` grepped `.ts` only; the H8-a packet's guard-1 correction, v66 b2). **(c) The census is COMPUTED, never typed:** the splice derives `N = a M + b A` from porcelain and refuses a typed census that disagrees (exhibit: the v66 b6 amend). **(d) A cap is a ceiling, not a target** (exhibit: five returns at their caps on 2026-09-06 — the assessment §2.4). **(e) The stable prompt:** the orchestrator prompt carries no state; the brief's §HELD + the newest beat + the decision record are the state (exhibit: D5; the v67 prompt). **(f) The per-domain lane law** (D4, REVERT-able; effective at the next landing): one lane per PATH-DOMAIN on the core tree (Java · web-ui), each landing staged by its domain path — recorded AS PROVISIONAL with its REVERT word. **(g) The guarded-splice-into-Nick's-tree pattern for docs** (BLOCK 6: the hub writes the working tree by an asserted splice, stages nothing, hands a one-command card; exhibit: the BLOCK 6 card).
- **PM `coding-instruction-format.md`:** addition **#27 the instrument-beside-the-claim law** (the wording of (b)); **#28 the miss-script sweep** — when a WU appends a resolver/step after a miss, the P2 survey sweeps the HARNESS for the prior surface's miss SCRIPT (not the log token): every scenario that scripts that miss changes meaning (exhibit: the F-R4-1b audit §1 R1); **#29 the cap sentence** in every §0 lane contract ("≤ N KB is a ceiling; the §0 card is what the hub reads first"). (W-SKILLS-7's #23–#26 keep their numbers.)
- **PM `review-and-quality.md`:** the operator-brief SHAPE (§NEXT exactly one act · §HELD-BY-THE-HUB re-printed every beat · §DONE · §WHAT YOU DO NOT DO) beside the H10 form, with the DELEGATE ruling form as v66 used it (decisions taken and each REVERT-able by name; exhibit: the decision record §3).
- **PM `freshness-preflight.md`:** **Check 12 — the archive convention** (the three greps of the HIVE-CLEAN-1 dispatch §4; PASS = all zero; STALE = any nonzero; never CONFLICTED) — added to the PM file AND, as the coder-relevant subset, to `coder/references/freshness-preflight.md` and `nexsys-skills/orchestrators/nexsys-frontend/references/freshness-preflight.md` (the shared-protocol rule those files state).
- **Coder `laws-ledger.md` / `testing-standards.md`:** the miss-script sweep (as a coder-side rule: "a resolver added after a miss changes every scenario that scripts that miss — grep the harness for the prior surface's miss script, then strengthen the hit test to pin the new surface's once-per-epoch bound"; exhibit: coder-lessons 2026-09-06 + the F-R4-1b return §0 R1) · "a fence that names a constant is grepped at source across every file type before dispatch" (exhibit: coder-lessons 2026-09-06, FE-113) · the sleepy-device physics note (a ZDO request to a router-parented end device is answered only inside the parent's indirect window; the desk pins the frame shape, the wire decides; exhibit: the F-R4-1b instruction DP-4 + return §3 O4) · the cap sentence.
- **FE `contract-consumer.md` / `field-evidence-and-rulings.md`:** the manufactured-type class as HERO-0 measured it — validators that REJECT lawful nulls (`actions[].command`, `trigger.subjectRef`) while the mocks populate them; the rule: every nullable emitter arm has a `| null` type, a `strOrNull`/`refOrNull` validator and a null-arm mock (exhibit: the HERO-0 audit §1 F2; FE-NULL-1) · the tri-state honesty idiom as FE-113 shipped it (ABSENT ≠ NULL ≠ PRESENT; two titles, three renders; exhibit: the FE-113 audit §2) · the null census as the hero's design ground (the four empty states; exhibit: the HERO-0 return §2) · FE-DESK-1: the container pre-gate on `git archive HEAD web-ui/dashboard` with md5 round-trips when the desk cannot run the toolchain (exhibit: the FE-113 return §0 I4) · the cap sentence.
- **The three `pass-history.md`:** one `### Moved/Added 2026-09-07 by W-SKILLS-8` section each, pointing at your return and listing every ledger section touched. **No SKILL.md line changes — guard 2 is absolute** (the W-SKILLS-7 text "one pointer line" was already superseded by Nick's word: the pointer lives in `pass-history.md`).

## §3b Added predictions (adjudicate with W-SKILLS-7's §3, first in your §0)
P5 the W-SKILLS-7 candidates + §2b yield ≈20–26 new arcs and ≥8 addenda after mechanism-dedupe · P6 the three SKILL.md md5s are byte-identical before/after · P7 the PM ledger grows by ≤12 KB and no ledger exceeds 40 KB (if one would, split by section under the same filename convention and say so) · P8 at least one §2b item duplicates a W-SKILLS-7 candidate's mechanism (fold as an addendum, not a new arc — say which).

## §4b Fences (in addition to W-SKILLS-7 §4)
The write-set is EXACTLY: `nexsys-hivemind/project-manager/references/*.md` · `nexsys-hivemind/coder/references/*.md` · `nexsys-skills/orchestrators/nexsys-frontend/references/*.md` (each tree's `pass-history.md` included). Nothing under `context/` (HIVE-CLEAN-1 owns it this day); no SKILL.md, no CLAUDE.md; no project state in any skill file (an exhibit path is a pointer, not state); no candidate brand name.

## §5 Clock
2–3 h. A partial return with an honest §0 outranks a complete one later.

## §6b The hub's audit (W-SKILLS-7 §6 R1–R6 stand) + R7 every §2b item is placed or explicitly deduped in the mapping table · R8 Check 12's text is identical across the three preflight files (the shared-protocol rule).

## §7 THE DISPATCH LINE (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected; may run beside HIVE-CLEAN-1)
```
date -u first. You are the W-SKILLS-8 skills lane for NexSys/HomeSynapse. Read nexsys-hivemind/context/instructions/2026-09-05_W-SKILLS-7_skills-pass_lane-dispatch.md WHOLE (its §0–§6 stand verbatim), then nexsys-hivemind/context/instructions/2026-09-07_W-SKILLS-8_skills-pass_lane-dispatch.md WHOLE (the addendum: §1b read-set, §2b deltas, §3b predictions, §4b fences), then the two read-sets in order and nothing older. Execute W-SKILLS-7 §2 plus §2b under the fences: every SKILL.md byte-unchanged (md5 before/after in your §0); the write-set is the three references/ trees only; stage nothing, commit nothing. Adjudicate the predictions (W-SKILLS-7 §3 + §3b) first in your §0. Return ONE file at nexsys-hivemind/context/audits/<today's CT date>_W-SKILLS-8_return.md (§0 card ≤3 KB first: the census exact in both repos · the mint→arc mapping table · the SKILL.md md5s; whole ≤12 KB — a ceiling, not a target). 2–3 h. Your last line: RETURNED <path> <bytes>. You report to the hub, not to me.
```
