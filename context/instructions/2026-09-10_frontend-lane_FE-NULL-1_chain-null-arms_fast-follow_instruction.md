<!--
file: context/instructions/2026-09-10_frontend-lane_FE-NULL-1_chain-null-arms_fast-follow_instruction.md
purpose: The FE-NULL-1 frontend-lane instruction — the HERO-0 F2/F4 fast-follow on the v1.1.3 mirror: four causal-chain keys the emitter serves as null are typed `| null`, validated as null-or-typed, carried by the mock on a null arm each, and rendered by the honest sentence HERO-0 wrote; plus the F4 row (cascade depth > 0 with a null parent). No wire change, no version bump. Runs in the web-ui path-domain beside HONESTY-1 (Java) under D4.
audience: the FE lane (a fresh Cowork conversation booted as nexsys-frontend) · the hub (audits the return) · Nick (the dispatch line §9; the landing card comes from the hub after the audit)
state-type: coding instruction (frontend lane)
status: ISSUE-READY (v68 beat 3, Thu 2026-09-10 ~18:0x CT; instrument 2026-09-10T22:55Z). Baseline: core `39c8dd3`; `web-ui/dashboard/` last changed at `d192d17` (FE-113). Source of the F2/F4 rows: context/research/2026-09-06_HERO-0_null-census_v1.1.3_return.md + context/audits/2026-09-06_HERO-0_intake_two-layer-audit_v66-b6.md (F2, F4).
-->

# FE-NULL-1 — the causal chain's null arms (a fast-follow on FE-113)

## §0 The lane contract (read first; every line binds)
- **`date -u` FIRST**; state your instrument limit; CT = UTC−5 once.
- **THE ONE DELIVERABLE:** `nexsys-hivemind/context/audits/<CT-filing-date>_FE-NULL-1_return.md` — ONE file, **§0 card first (≤3 KB: the census `N = a M + b A` with exact paths · the red-first table · deviations by tag · the `npm run verify` line with counts) · §1 per-file deltas · §2 the gates · §3 pushback/observations · §4 the WUCP Phase-1 checklist. Cap ≤12 KB.** Your last line, IN THE FILE and printed: `RETURNED <path> <bytes>`.
- **Baseline and the two-lane rule (D4):** `homesynapse-core` HEAD must be **`39c8dd3`**. A Java lane (HONESTY-1) may be editing `core/**` on this same tree at the same time — that is lawful and expected. Your check is **scoped**: `git status --porcelain -- web-ui/dashboard` must be EMPTY (STOP and report if not). **Never touch, stage, stash, revert or read-for-edit anything outside `web-ui/dashboard/`; never run `git add`, `git stash`, `git checkout`, `git commit`.** Work ONLY under `web-ui/dashboard/`. The hub audits; Nick commits with a `git add -A web-ui/dashboard` card.
- **Do not write `coder-handoff.md`** (the Java lane may be appending to it); your §4 carries the WUCP Phase-1 checklist and the hub files the handoff entry at landing.
- **Tests first:** every new assertion RED at HEAD before the edit; then GREEN. The validator tests go red by construction (a null `command` or `subjectRef` THROWS at HEAD; a null `targetRef` or observed `value` PASSES the validator at HEAD but the TYPE forbids it — say which). **The gate of record is `frontend.yml` on Nick's push (`npm run verify`)**; your desk `npm run verify` is the pre-gate, reported with counts.
- **The four-constraint law governs (v1.1.2 ratified, Nick 2026-07-22):** additive-only · per-endpoint camelCase · **the emitter leads** (Core's bytes are the truth; §2 cites them at the line; you mirror, never invent) · version discipline — **NO bump here**: the wire did not change; the mirror's TYPES were wrong about it. `CONTRACT_VERSION` stays `v1.1.3-2026-09-06` and `contract.test.ts`'s pin is untouched.
- **The honesty law (FE-HONEST-1):** a value the wire did not carry is never rendered as if it had; NULL renders as the honest sentence HERO-0 wrote (§2), never as a blank, never as "null", never as an invented name or verb. ABSENCE of a v1.1.2+ key stays the separate fact it already is.
- **Predictions (pre-registered; adjudicate first in your §0):** P1 the census is **8 M + 0 A** exactly as §3 · P2 the three recorded fixtures under `src/lib/api/fixtures/` are byte-identical after the lane (md5 `896f861d…` nonfiring · `6f502c7b…` events-404 · `05b02fbc…` never-triggered) and validate unchanged · P3 `npm run verify` is green in one round after the edits · P4 you find ≥1 place this instruction's read of the source is stale (a line number moved, a helper named differently) and file it with a line cite.

## §1 The read-set, in order (nothing older)
1. `context/process/deep-work-window_protocol.md` §2 + §6 (your contract's shape). 2. **HERO-0's return** `context/research/2026-09-06_HERO-0_null-census_v1.1.3_return.md` — the chain rows of its table (the meaning at source + the honest sentence per key) and its §2; **the sentences in §2 below come from there**. 3. `web-ui/dashboard/MODULE_CONTEXT.md` (Gotchas: the tri-state idiom absent ≠ null; the FE-113 banner) · `FRONTEND_DOCTRINE.md`. 4. `src/lib/api/contract.ts` (`CausalTrigger` :317–:322 · `CausalCondition` :324–:329 · `CausalAction` :331–:353 · `CausalChain` :358–:373 with `cascade` :372) · `src/lib/api/shapes.ts` (`strOrNull` :68 · `refOrNull` :79 · `subjectRef` :102 · `'B3:causalChain'` :246–:282: `:256` the trigger's `subjectRef(...)`, `:265` observedState array-only, `:272` `isStr(req(a,'command'…))`, and NOTE: `targetRef` is not validated at all today) · `src/lib/api/v113-additive.test.ts` (the tri-state idiom to copy) · `src/lib/api/fixtures/*` (READ ONLY — recorded measurements) · `src/lib/api/mock/mockData.ts` (the chains at ~:236–:340; the SKIPPED run's action at ~:329–:332 carries `command: 'turn_on'` today — the H8 false-type class) · `src/lib/api/mock/scenarios.ts` (any chain it builds) · `src/components/CausalChain.tsx` (:82–:99 the trigger step · :109–:126 the condition step and its "At the time" detail · :147–:186 the action step · :233–:236 the cascade link) · `src/components/CausalChain.hardening.test.tsx` + `CausalChain.dangling.test.tsx` (the render-test idiom) · `src/lib/format.ts` (`NOT_RECORDED` :28 · `refLabel` :77 already `string | null | undefined` · `danglingTriggerLine` :85 · `danglingTargetLine` :90 · `triggerVerbFromValue` :507) · `actionPhrase` is LOCAL to `CausalChain.tsx` (:287) and already accepts `string | null | undefined` — read what it returns for null before you decide the action line. 5. The `nexsys-frontend` skill's `references/contract-consumer.md` (the additive procedure) and `references/explainability-and-accessibility.md` (the honest-sentence rules).

## §2 The wire, as the emitter serves it (source-cited by the hub at `39c8dd3`; re-read, never trust)
| Key (B3 `/runs/{id}/causal-chain`) | Emitter truth | Mirror at `d192d17` | The honest sentence (HERO-0) |
|---|---|---|---|
| `trigger.subjectRef` | **null** when the triggering event is outside the run's correlation — `StandardExplanationService.java` ~`:644–:649` (the triggering event not found in the chain → `.orElse(null)`); WHEN is undocumented (F3, a docs line, not yours) | `SubjectRef` (non-null); validator `:256` **REJECTS** null | "Something set it off at {time} — what isn't recorded." (the trigger step keeps `matchedAt` and the type/firingValue detail) |
| `conditions[].observedState[].value` | **null** when the entity had no value for the attribute at evaluation — `RunExplanation.java` :137 (`ObservedStateEntry.value`, "or null if unreported") | `string`; validator `:265` checks only that `observedState` is an array (a null value passes unvalidated; the type lies) | "{attr} had no reading yet." (in the "At the time" detail, in place of `= {value}`) |
| `actions[].command` | **null** for a SKIPPED/FAILED action that never issued a command — `StandardExplanationService.java` ~`:776` (`new ActionView(sp.actionType(), targetRef, null, "{}", outcome, cp.errorDetail(), null, true)`) | `string`; validator `:272` **REJECTS** null | "Skipped before any command was sent." (the action line; the `Command` detail already renders `NOT_RECORDED` at :186 — keep it) |
| `actions[].targetRef` | **null** for a non-dispatched action with no target refs — ~`:771` (`sp.targetRefs().isEmpty() ? null : …`) | `SubjectRef` (non-null); **not validated** | "…a device the run didn't name." (the action line names no target and accuses no registry) |
| `cascade.parentRunId` with `cascade.depth > 0` (F4) | `parentRunId` is **always null in V1** — `RunExplanation.java` :213–:219 ("the events carry only the flattened depth") | `string \| null` already; the render (:233) shows a parent link only when truthy, so depth > 0 renders NOTHING | "Started by another run — which one isn't recorded." (rendered when `depth > 0 && parentRunId == null`; depth 0 renders nothing, as today) |

Absence rules are unchanged: these five keys are REQUIRED on the v1.1 wire (the emitter always writes them, null or not); the mirror keeps them required and makes their VALUE nullable. No key here is optional.

## §3 Files to Create or Modify (the Files table governs)
| # | File | Change |
|---|---|---|
| 1 | M `src/lib/api/contract.ts` | `CausalTrigger.subjectRef: SubjectRef \| null` · `CausalCondition.observedState[].value: string \| null` · `CausalAction.targetRef: SubjectRef \| null` · `CausalAction.command: string \| null` — each with a one-line comment in the file's existing style citing the emitter line (§2) and the HERO-0 date. `cascade` unchanged. **No `CONTRACT_VERSION` change.** |
| 2 | M `src/lib/api/shapes.ts` | `'B3:causalChain'`: `:256` → `refOrNull(req(trigger,'subjectRef',…), …)` (required key, null-or-`{type,id}`) · each `observedState[]` entry validated: `entityId` string · `attribute` string · `value` `strOrNull` (a missing key → `ContractError`; today nothing inside the array is checked) · `:272` → `strOrNull(req(a,'command',p), …)` · ADD `refOrNull(req(a,'targetRef',p), …)` (required key; null lawful). Nothing else in the validator changes. |
| 3 | M `src/lib/api/v113-additive.test.ts` | a new `describe('FE-NULL-1 — the chain's null arms')`: for each of the four keys — PRESENT-typed passes · PRESENT-null passes · **MISSING throws** (they are required) · PRESENT-wrong-type throws (`subjectRef: "ent_x"` · `value: 42` · `command: 7` · `targetRef: {type:"entity"}` without `id`). State per test whether it is RED at HEAD by throwing (command, subjectRef) or RED because the type forbids the fixture (targetRef, value — use `as unknown as` only inside the test, with a comment). |
| 4 | M `src/lib/api/mock/mockData.ts` | the v1.1 truth carried by the mock: the SKIPPED run's action gets **`command: null` and `targetRef: null`** (the emitter's `:776` shape; `resultOutcome: null`, `settled: true`); ONE condition somewhere carries an `observedState` entry with **`value: null`**; ONE chain carries **`trigger.subjectRef: null`** (a run whose trigger is outside its correlation — pick the never-triggered/oldest-run shape and say which); ONE chain carries **`cascade: { parentRunId: null, depth: 1 }`**. Every other row unchanged. If `scenarios.ts` builds a chain, the same four arms exist there or you declare why not. |
| 5 | M `src/components/CausalChain.tsx` | the trigger step: `subjectRef` null → the line is the HERO-0 sentence with `clockTimeWithDate(matchedAt)`; no `refLabel`, no dangling pill (nothing is accused) · the "At the time" detail: `value` null → `${refLabel} ${attribute} had no reading yet.` (no `= null`) · the action step: `command` null → the line is "Skipped before any command was sent." (not `actionPhrase(null)`'s fallback, whatever it is); `targetRef` null → the phrase ends with "a device the run didn't name" and no dangling pill · the cascade: `depth > 0 && !parentRunId` → a `<p class={styles.cascade}>` with the F4 sentence (no link). Copy strings go in `src/lib/format.ts` beside `NOT_RECORDED` (row 6), never inline. |
| 6 | M `src/lib/format.ts` (+ `format.test.ts` if it pins the exported copy) | the four exported sentences/fragments from §2 (name-light; no brand token) |
| 7 | M `src/components/CausalChain.hardening.test.tsx` | render tests, one per arm + the F4 row: the sentence is in the DOM; `null` never appears as text; no dangling pill/marker on a null ref; depth 0 still renders no cascade line; a non-null run renders exactly as before (a snapshot-free assertion on the existing mock) — each RED at HEAD |
| 8 | M `web-ui/dashboard/MODULE_CONTEXT.md` | a `*2026-09-1x beat (FE-NULL-1)*` banner: the four keys are REQUIRED-NULLABLE (vs the v1.1.2/.3 OPTIONAL-NULLABLE additive keys — two idioms, one table); `targetRef` had never been validated; the F4 sentence; the emitter lines of record |
| — | — | `src/lib/api/fixtures/*` READ ONLY (P2) · `contract.test.ts` untouched · no new endpoint · no CSS beyond the existing `styles.cascade` / `styles.hint` classes · `scenarios.ts` only if row 4 says so (then the census is 9 M — declare it) |
**Census expected: 8 M + 0 A.** A file outside this table is a `[REVIEW]` with the reason, never a silent add.

## §4 What to watch out for
- **The H8 false-type class:** a mock that populates every key hides every null; row 4 exists so the render tests exercise the real shape. Do not "fix" the mock's SKIPPED action by keeping a command on it.
- **Two lanes, one tree:** `git status` will show Java files changing under `core/**` — not yours, not a STOP. Only `-- web-ui/dashboard` is your porcelain. If a file under `web-ui/dashboard/` is modified before you start, STOP and report.
- **`refLabel` already takes `null | undefined`** (:77) — the null arms must not route through it for the trigger/target lines, because its output is a label, not the honest sentence.
- **Required vs optional:** `command`/`subjectRef`/`targetRef`/`value` MISSING is a contract error (the v1.1 freeze); do not copy the `'key' in obj` optional idiom from FE-113 for these.
- **Do not touch `firingValue`** — its null handling is already the ruled behaviour (:99) and EXPLAIN-1 will carry it in v1.1.4.

## §5 Out of scope
Any Core/Java change (HONESTY-1's lane) · `CONTRACT_VERSION` · the freeze-doc rows (the hub's write in the docs repo) · EXPLAIN-1..10 (v1.1.4) · the hero design (`DESIGN:`) · `coder-handoff.md`.

## §6 Success criterion (binary)
DONE when: (1) the census is exactly §3 or the deviation is declared in §0; (2) every new test RED at HEAD (the table says how) then GREEN; (3) `npm run verify` green on your desk with counts, and **`frontend.yml` GREEN on Nick's push = the gate of record**; (4) the three fixtures byte-identical (P2); (5) `git status --porcelain -- web-ui/dashboard` lists exactly the §3 files; (6) MODULE_CONTEXT updated; (7) the return within cap. **Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING — the surface earns "verified" when a real-wire SKIPPED run renders the sentence (H8-a/R-4c's capture).**

## §7 WUCP Phase 1
Your §4 checklist: Deferred Build Gate YES (`frontend.yml` on Nick's push) · NEXT WU pointer: the hub's audit → the `git add -A web-ui/dashboard` card → CI → FE-113b (the H8-a capture as a fixture) → the hero charter. No `coder-handoff.md` write (§0).

**The alternative shape this instruction did not take:** make the four keys OPTIONAL-nullable like the FE-113 additive keys — rejected: they are on every v1.1 payload by the freeze; "optional" would let a missing key pass silently, which is the opposite of what a required-nullable mirror is for.

## §8 The hub's audit rules (pre-filed)
R1 census at `git status --porcelain -- web-ui/dashboard` = §0's declaration and nothing outside the domain touched · R2 every RED-at-HEAD claim re-executed by reading the test's assertions against HEAD's types/validators · R3 the three fixtures' md5 unchanged · R4 the mock carries all four null arms + the depth-1 chain (row 4) · R5 the five render tests exist and assert the sentences verbatim from §2 · R6 `npm run verify` not re-run by the hub (disclosed); CI is the gate · R7 mismatches first; a `[REVIEW]` with line cites is first-class.

## §9 THE DISPATCH LINE (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected; HONESTY-1 may be running — that is the design)
```
date -u first. You are the FE-NULL-1 frontend lane for NexSys/HomeSynapse — boot as the nexsys-frontend skill. Baseline: homesynapse-core HEAD must be 39c8dd3 and `git status --porcelain -- web-ui/dashboard` must be EMPTY (other paths may be dirty — a Java lane is running; never touch them; STOP and report only if web-ui/dashboard is dirty). Execute nexsys-hivemind/context/instructions/2026-09-10_frontend-lane_FE-NULL-1_chain-null-arms_fast-follow_instruction.md exactly: read its §0 contract and §1 read-set first; adjudicate P1–P4 in your §0. Tests first (RED at HEAD as §3 states), then the eight files of §3, then MODULE_CONTEXT. Work only under web-ui/dashboard/; run `npm run verify` there; stage nothing, commit nothing, no git add/stash/checkout; do not write coder-handoff.md. Write nexsys-hivemind/context/audits/<today's CT date>_FE-NULL-1_return.md (≤12 KB, §0 card first). Your last line, in the file and printed: RETURNED <path> <bytes>. You report to the hub.
```
