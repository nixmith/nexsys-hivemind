<!--
file: context/instructions/2026-09-11_frontend-lane_HERO-1b_hero-build_B1-B7_charter.md
purpose: HERO-1b — the BUILD lane that lands the explainability hero of record (HERO-1's SPEC.md, core 1e26912) into web-ui/dashboard/src: SPEC §11 rows B1–B7 in order, each with its test first, plus B9 (both §9 token requests, ruled (a)); B8 (the EXPLAIN key swaps) lands only as each v1.1.4 key appears on the wire. The Glossary UI-term row for "the hub" travels the amendment path in the hub's hands (AMD-100 in homesynapse-core-docs), not this lane's.
audience: the HERO-1b lane (a fresh Cowork conversation with ClaudeFolder connected; the nexsys-frontend skill governs) · Nick (dispatches with §9's line; lands with a `git add -A web-ui/dashboard` card after the hub's audit) · the hub (audits by §8)
state-type: lane charter (a coding instruction for the web-ui path-domain)
status: ISSUE-READY (authored v70 beat 5, Fri 2026-09-11 ~18:2x CT; instrument 2026-09-11T23:23:24Z). Dispatches on Nick's paste; the web-ui slot is free (D4: one lane per path-domain; FIX-2a may be editing lifecycle/** on the same tree — lawful; never touched).
baseline: core `1e26912` (the SPEC is on this commit at web-ui/dashboard/design/hero-v1/SPEC.md, 57,427 B; the wire contract is the FROZEN v1.1; v1.1.4's keys are NOT on the wire until EXPLAIN-114a lands). Re-verify at issue: `git -C homesynapse-core log -1 --oneline` prints `1e26912` or a later main with the same design files; `git status --porcelain -- web-ui/dashboard` is EMPTY.
rulings: Nick, Fri 2026-09-11 ~17:1x CT — `HERO1:` (a) on Q1–Q7: Q1 the N3 inference wording in the info register until FIRED_CONFIRMED lands · Q2 "was asked to" for sent-but-unconfirmed · Q3 the permanence footer without self-reference (name-free) · Q4 the automation-led frame for non-completed runs · Q5 both §9 token requests (T1, T2) · Q6 the "no detail recorded" tell now, a Core marker as a v1.1.5 ask · Q7 "the hub" as the UI term (a Glossary row via the amendment path).
-->

# HERO-1b — build the hero of record: SPEC §11 B1–B7 (+ B9), tests first

## §0 The lane contract (read first; every line binds)
- `date -u` first. State your instrument limit: what your shell can run (`node`, `npm`, `npx vitest`) and what it cannot. CT = UTC−5, derived once. FE-NULL-1 O3 stands: if the toolchain is not runnable from your shell, every gate you cannot run is DEFERRED to Nick's desk by its exact command in your §0 card, never claimed.
- **The one deliverable:** `nexsys-hivemind/context/audits/<your CT date>_HERO-1b_return.md`, one file, §0 card first (≤3 KB: the census `N = a M + b A` with exact paths under `web-ui/dashboard/src/` · the red-first table (row → RED-at-HEAD how → GREEN) · the predictions adjudicated · the gates run with counts, or deferred by command · deviations by tag), then §1 per-file deltas, §2 the gates, §3 pushback and observations, §4 the WUCP Phase-1 checklist. **Cap ≤12 KB is a CEILING, not a target.** Your last line, in the file and printed to Nick: `RETURNED <path> <bytes>`.
- **Write-set:** `homesynapse-core/web-ui/dashboard/src/**` only (the files §3 names, plus the tests beside them). You do not edit `web-ui/dashboard/design/**` (the design of record; a defect in it is filed in §3 of your return with the line, and the hub amends), nothing under `core/**`, `lifecycle/**`, `api/**`, and nothing outside `web-ui/dashboard/`. `git status --porcelain -- web-ui/dashboard` must be EMPTY when you start; other paths may be dirty (a Java lane). Never run `git add`, `git stash`, `git checkout`, `git commit`. Nick commits with a `git add -A web-ui/dashboard` card after the hub's audit.
- **Tests first, every row.** Each B-row's new assertions are RED at HEAD before the edit (the table in §3 says how each goes red), then GREEN. A row whose assertions HEAD already satisfies cannot be red-first; say so in the row (law 18: a preservation fixture is named, not claimed red).
- **The honesty law binds every rendered sentence:** a value the wire did not carry is never shown as if it had; a null renders as its keyed sentence (§3 slot null arms), never as a blank, "null", an invented name or an invented verb. `DISPATCHED` is not delivered; an acknowledgement is not confirmation; `UNCONFIRMED` is calm.
- **Name-light.** The product name is in a trademark search. Every string uses the literal token `{{NAME}}` where the product name would appear, and the permanence footer carries no product name and no "we" (Q3 ruled). B2's test asserts it.
- **The contract is FROZEN v1.1.** No new key is read from the wire in this lane. B8's swaps (the EXPLAIN-1..9 placeholders → the real values) are ⛔ GATED: each lands only when its v1.1.4 key is on the wire behind the tri-state validator (EXPLAIN-114a's landing), one row per key, in a later dispatch. Until then the placeholders render the honest "not recorded" arms of §6.
- **Predictions, pre-registered; adjudicate them first in your §0 card:** P1 — the census is exactly §3's table (`9 M + 1 A` — the new `hero-states` scenario file if scenarios are one-per-file at HEAD, else `10 M + 0 A`; ground the shape at `git ls-files web-ui/dashboard/src/lib/api/mock/` before you pin it). P2 — `format.test.ts` gains ≥30 headline assertions (one per §3 table row) plus the mode overrides and every slot null arm, and at least 24 of them are RED at HEAD (the rows HEAD's single `causalSentence` cannot produce). P3 — `grep -rn` over `web-ui/dashboard/src/` for `BRAND.productName` inside any hero string, and for the word "we " in `i18n.ts`'s hero keys, returns nothing after B2. P4 — `npm run verify` (or its parts, where runnable) is GREEN on the desk with counts, and `frontend.yml` is GREEN on Nick's push — the gate of record. P5 — you find at least one line cite in this charter or in SPEC §11 that has moved or is wrong at your HEAD, and you file it with the corrected line.

## §1 The read-set, in order (nothing older)
1. This charter. 2. `homesynapse-core/web-ui/dashboard/design/hero-v1/SPEC.md` WHOLE (57 KB — it is the design of record: §3 the headline grammar and its 30-row table, §4–§6 the states and placeholders, §7 the copy table (140 keyed rows), §8 accessibility, §9 tokens, §10 the acceptance script, §11 the build rows), then `README.md` and `states.html` beside it (open `states.html` in a browser for the shapes). 3. `nexsys-hivemind/context/audits/2026-09-11_HERO-1_intake_two-layer-audit_v69-b3.md` §0 (what the hub verified; the seven rulings). 4. The nexsys-frontend skill's `references/contract-consumer.md` and `build-and-ci-discipline.md` (the gate, the fixtures, the validator). 5. `nexsys-hivemind/context/instructions/2026-09-10_frontend-lane_FE-NULL-1_chain-null-arms_fast-follow_instruction.md` §0–§3 (the last build lane on this surface: its census shape, its red-first table form, O1–O3). 6. At HEAD, the files you will edit (§3), whole.

## §2 The ground (source-cited by the hub at `1e26912`; re-read, never trust)
- The files exist at these paths (`git ls-files web-ui/dashboard | grep`): `src/lib/format.ts` · `src/lib/format.test.ts` · `src/lib/i18n.ts` · `src/views/WhyNotView.tsx` · `src/components/CausalChain.tsx` · `src/components/StatusPill.tsx` · `src/components/CausalChain.module.css` · `src/lib/api/mock/mockData.ts` · `src/lib/api/mock/scenarios.ts` · `src/a11y.test.tsx` · `src/styles/tokens/tokens.dtcg.json`.
- `package.json` scripts (`:10–:21`): `tokens` (`node scripts/build-tokens.mjs`), `typecheck`, `lint`, `test` (`vitest run`), `verify` (= `tokens:check && lint && typecheck && test && build && …`). The gate of record is `.github/workflows/frontend.yml` on Nick's push.
- SPEC §11 is the row list (`SPEC.md:293–:296`); §3's table has 30 rows (`:25–:81`); §7 has 140 keyed rows (`:130–:276`); §9's requests are T1 (a light-theme `--hs-text-muted` that clears 4.5:1) and T2 (a `--hs-status-on-bg` foreground pair, or darkened light `--hs-ok-500`/`--hs-error-500`) (`:283–:288`); §10 is the six-sentence acceptance script (`:289–:292`).
- The v1.1.4 keys (`firingValue` populated, `settledAt`/`confirmedAt`, `definitionKey`, `disabledAt`/`disabledReason`, `FIRED_CONFIRMED`) are specified in `nexsys-hivemind/context/instructions/2026-09-11_coder-lane_EXPLAIN-114a_read-API-v1.1.4-additive_coding-instruction.md` and are NOT on the wire at this baseline — B8 waits.

## §3 The rows (in order; each small; the test first)
| Row | Files | What lands | RED at HEAD, how |
|---|---|---|---|
| **B1** the headline grammar | `src/lib/format.ts` (M) · `src/lib/format.test.ts` (M) | replace the single `causalSentence` with SPEC §3: the COMPLETED clause per leading outcome; the frame + tail for the other statuses; the mode override via `actionVerdict()` (superseded · expired-restart); the slot null arms as named constants; the "no action" arm | the test pins one sentence per cell of the 30-row table plus the overrides and every null arm; HEAD's `causalSentence` produces only the happy path, so ≥24 assertions fail on text |
| **B2** the strings | `src/lib/i18n.ts` (M) · a test beside it (M or A) | the hero strings behind `t()` under the §7 keys, the permanence footer without the product name (Q3) | the test asserts every §7 key resolves and no hero string contains `BRAND.productName` or "we"; the keys do not exist at HEAD |
| **B3** why didn't it | `src/views/WhyNotView.tsx` (M) + its test | the N1–N7 L1 sentence with the no-time arm; N3 in the `info` register with body `whyNot.body.ranFine` (Q1); the DISABLED body with the "isn't recorded" sentence | the rendered text per verdict is pinned; HEAD renders the old sentences |
| **B4** the chain's empties | `src/components/CausalChain.tsx` (M) + its test | "genuinely empty" split into the era-boundary card (`explain.chain.noDetail.*`) and the current-instance `…completed.none` headline; the trigger step's reading-not-recorded arm as the line (Q6) | two fixtures (era skeleton · completed-none) render distinct text; HEAD renders one |
| **B5** a11y | `CausalChain.tsx` · `src/components/StatusPill.tsx` (M) · `src/a11y.test.tsx` (M) | the visually-hidden `explain.a11y.step` per step; `explain.a11y.provisional`; one `role="status"` announcement on a settle | the a11y test queries the hidden text and the status role; absent at HEAD |
| **B6** the L2 register + motion | `src/components/CausalChain.module.css` (M) | L2 body text to `--hs-text-secondary` where it is not already; a `prefers-reduced-motion` rule for the pill swap | a preservation-class row: no runtime assertion; the diff is the receipt (say so) |
| **B7** the scenario | `src/lib/api/mock/mockData.ts` (M) · `src/lib/api/mock/scenarios.ts` (M or the scenario file, A) | a `hero-states` scenario: one run per §3 row today's emitter can produce (COMPLETED × each outcome · the silent skip · the era skeleton · one each of SKIPPED/FAILED/CANCELLED/INTERRUPTED) with the wire's null arms (`firingValue` null everywhere; `resultOutcome` null beside CONFIRMED) | the scenario passes the tri-state validator and a test asserts its row count; absent at HEAD |
| **B9** the tokens (Q5: both) | `src/styles/tokens/tokens.dtcg.json` (M) + `npm run tokens` output | T1 the light-theme `--hs-text-muted` at ≥4.5:1; T2 the `--hs-status-on-bg` pair (or the darkened light `--hs-ok-500`/`--hs-error-500`) at ≥4.5:1 on the two light pills | `tokens:check` is the gate; a contrast assertion in the tokens test if one exists at HEAD (ground it), else the computed ratio in your return |
| ⛔ **B8** the EXPLAIN swaps | — | one row per v1.1.4 key, in a later dispatch, as each key lands on the wire | not this lane |

Order is B1 → B2 → B3 → B4 → B5 → B6 → B7 → B9. Each row's test goes RED, then the row lands, then GREEN, then the next row. The census you declare in §0 is what `git status --porcelain -- web-ui/dashboard` shows at your close, exactly.

## §4 What to watch out for
- `actionVerdict()` already classifies superseded / expired-restart (FE-NULL-1 O1 named the defect `commandVerb(:519)` fixes nothing about); B1 uses it, never re-derives it.
- The wire's nulls are real today: `firingValue` is null on a matched trigger and `resultOutcome` is null beside `outcome: CONFIRMED` (the R-4b record F-R4b-H); B7's fixtures carry them as they are, and B1's arms render them honestly. Do not fabricate a firing value in a fixture.
- `{{NAME}}` is the token; no candidate name of any kind in any string, test or comment.
- The acceptance script (SPEC §10) is your own last check: with the `hero-states` scenario loaded, read the six sentences off the screen; put the six rendered sentences in your return's §3 verbatim.
- The toolchain: FE-NULL-1 O3 — if `npm` is not runnable from your shell, run what is (`npx vitest run src/lib/format.test.ts` under `node` if present), and defer the rest to Nick's desk by exact command; never write "verify green" for a gate you did not run.
- Do not touch `design/**`; do not bump `package.json`; do not add dependencies.

## §5 Out of scope
B8 (gated on v1.1.4 keys) · any change to the read-API client, the validator or the fixtures under `src/lib/api/fixtures/` (their md5s stay) · the marketing site · the Glossary row (the hub's AMD-100, docs repo, Nick ratifies) · any Core marker for the "no detail recorded" tell (a v1.1.5 ask, docketed).

## §6 Success criterion (binary)
DONE when: every row B1–B7 and B9 is landed with its test RED-then-GREEN as §3 says (or the preservation row named); the census matches §0's declaration; `npm run verify` is green on a desk that can run it (yours or Nick's, by command), and `frontend.yml` is GREEN on Nick's push; the six §10 sentences render as written; no product name and no "we" in the hero strings; the return is filed with its `RETURNED` line.

## §7 Escalation
STOP and write it in §3 of your return (do not guess) when: a §7 key is missing for a cell you must render; the validator rejects a §3 null arm the SPEC requires; a row cannot go red at HEAD and is not a preservation row; a token change would alter a pill outside the two §9 named; anything requires a file outside the write-set.

## §8 The hub's audit rules (pre-filed)
R1 the census at `git status --porcelain -- web-ui/dashboard` = §0's declaration, nothing outside the domain · R2 every RED-at-HEAD claim re-executed by reading the test's assertions against HEAD's `format.ts` · R3 the fixtures' md5s unchanged (`896f861d…` · `6f502c7b…` · the third as FE-NULL-1 pinned them) · R4 `grep -rn 'BRAND.productName\|{{NAME}}'` over the hero strings (the token present where a name would be; the brand accessor absent) · R5 the six §10 sentences quoted in the return match SPEC §10 word for word · R6 the gates' counts, or the deferral commands, present · R7 the `RETURNED` line is the last line.

## §9 The dispatch line (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected)
```
date -u first. You are the HERO-1b frontend lane for NexSys/HomeSynapse — boot as the nexsys-frontend skill. Baseline: homesynapse-core main at 1e26912 (or a later main carrying web-ui/dashboard/design/hero-v1/SPEC.md unchanged) and `git status --porcelain -- web-ui/dashboard` EMPTY (other paths may be dirty — a Java lane may be editing lifecycle/**; never touch them). Read nexsys-hivemind/context/instructions/2026-09-11_frontend-lane_HERO-1b_hero-build_B1-B7_charter.md WHOLE, then its §1 read-set in order. Rows B1 → B2 → B3 → B4 → B5 → B6 → B7 → B9, each test RED at HEAD before its edit, then GREEN; B8 is gated, not yours. Write only under web-ui/dashboard/src/. Run npm run verify where your shell can; defer by exact command where it cannot. Never git add/commit/stash/checkout. Write the return to nexsys-hivemind/context/audits/<today CT>_HERO-1b_return.md (≤12 KB; §0 card first; predictions P1–P5 adjudicated first; the last line is `RETURNED <path> <bytes>`) and say that one line to Nick.
```
