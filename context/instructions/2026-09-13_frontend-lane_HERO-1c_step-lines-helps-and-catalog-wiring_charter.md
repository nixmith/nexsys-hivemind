<!--
file: context/instructions/2026-09-13_frontend-lane_HERO-1c_step-lines-helps-and-catalog-wiring_charter.md
purpose: The HERO-1c frontend charter — the action step LINES and helps rendered from the §7 catalog per confirmation mode (D6), `outcomeMeta`'s "We sent it" retired, the catalog keys whose surfaces lie outside HERO-1b's rows wired (D7: the hub page, the four state cards, the never-checked cell), and the hub's SPEC §7 amendment (D2/D3/D5 — four rows) applied verbatim as row C0. Tests first, every row; the wire untouched (v1.1.4 FROZEN).
audience: the frontend lane (a fresh Cowork conversation; the nexsys-frontend skill) · the hub (the intake; §8) · Nick (§9 is his paste, on `FE: 6af76f7 frontend green`)
state-type: lane charter (Director mode; the §3 rows govern)
status: DISPATCH-READY on `FE: 6af76f7 frontend green` — authored v73 beat 3 (Sun 2026-09-13 ~14:2x CT; instrument 2026-09-13T19:25:42Z) from the HERO-1b return (§3 D2–D7) and its audit (§3); every cite read at core `a458a64`, `web-ui/dashboard` porcelain empty. A red `FE:` line makes the red read the first act instead.
-->

# HERO-1c — the step lines, the helps, the catalog wired to its last surfaces

## §0 The lane contract (read first; every line binds)
- `date -u` first. State your instrument limit: what your shell can run (`node`, `npm`, `npx vitest`) and what it cannot; with `node_modules` installed outside the mount the Linux shell can run the toolchain (HERO-1b's O3 correction). CT = UTC−5, derived once. Every gate you cannot run is DEFERRED to Nick's desk by its exact command in your §0 card, never claimed.
- **The one deliverable:** `nexsys-hivemind/context/audits/<your CT date>_HERO-1c_return.md`, one file, §0 card first (≤3 KB: the census `N = a M + b A` with exact paths · the red-first table (row → RED-at-HEAD how → GREEN; the preservation rows named) · P1–P5 adjudicated · the gates with counts or deferred by command · deviations by tag), then §1 per-file deltas, §2 the gates, §3 pushback and observations, §4 the WUCP Phase-1 checklist. **Cap ≤12 KB is a CEILING, not a target** (3 KB + ~1 KB per §3 row); the §0 card is what the hub reads first. Your last line, in the file and printed to Nick: `RETURNED <path> <bytes>`.
- **Write-set:** `homesynapse-core/web-ui/dashboard/src/**` (the files §3 names plus the tests beside them), `web-ui/dashboard/design/hero-v1/SPEC.md` for row C0 ONLY (the hub's four rows, verbatim — the design of record changes by the hub's text, not yours; any other SPEC defect is filed in your §3 with the line), and `web-ui/dashboard/MODULE_CONTEXT.md` for row C7 (the hub's spec). Nothing under `core/**`, `api/**`, `lifecycle/**`; nothing outside `web-ui/dashboard/`. `git status --porcelain -- web-ui/dashboard` must be EMPTY when you start; other paths may be dirty (EXPLAIN-114b, a Java lane, is editing `core/automation/**` and `api/rest-api/**` — never touch them). Never run `git add`, `git stash`, `git checkout`, `git commit`. Nick lands the domain with a `git add -A web-ui/dashboard` card after the hub's audit.
- **Tests first, every row.** Each row's new assertions are RED at HEAD before the edit (the table says how), then GREEN; a row whose assertions HEAD already satisfies is named a preservation fixture (law 18), never claimed red. A flipped existing assertion is named in the row with its old and new text.
- **The honesty law binds every rendered sentence:** a value the wire did not carry is never shown as if it had; `DISPATCHED` is not delivered; an acknowledgement is not confirmation; `UNCONFIRMED` is calm; a null renders its keyed sentence. **Name-light:** `{{NAME}}` where the product name would appear; no "we" in any hero string (Register C — `outcomeMeta`'s "We sent it" is the last one and this lane retires it).
- **The wire is FROZEN v1.1.4.** No new key is read; no shape changes. Every string the hero shows comes from the `i18n.ts` catalog through `t()`/`heroCopy()` — after this lane, no hero surface carries a literal sentence (§6's instrument).
- **Predictions, pre-registered; adjudicate them first in your §0 card:** P1 — the census is §3's files exactly, with the test homes you ground at HEAD declared (a new test file is `A`; a test added to an existing file is inside its `M`). P2 — the step-line test gains one assertion per `ActionMode` (nine: `verdicts.ts:285`–`:295`) and at least eight are RED at HEAD (HEAD renders `actionPhrase(command) + target` for every mode — `CausalChain.tsx:215`–`:220`; only the two already-keyed lines, `skipped` and `not-recorded`, can be green-by-construction — sort them and say). P3 — after C3, `grep -rn "We sent it" web-ui/dashboard/src` and `grep -rn outcomeMeta web-ui/dashboard/src` both return nothing. P4 — `npm run verify` (or its runnable parts) is GREEN on the desk with counts, and `frontend.yml` is GREEN on Nick's push — the gate of record. P5 — at least one line cite in this charter has moved or is wrong at your HEAD; you file the corrected line.

## §1 The read-set, in order (nothing older)
1. `homesynapse-core/web-ui/dashboard/design/hero-v1/SPEC.md` §5 (`:94`–`:111`, the mode table with the step-line keys and helps) · §7 (`:130`–`:275`, the catalog of record: the `explain.mode.*` rows `:179`–`:207` region, the `whyNot.kv.neverChecked` and `explain.hub.*` rows, the `explain.error/offline/replaying/loading` rows at the table's end `:266`–`:275`) · §8 (accessibility) · §10 (the acceptance script).
2. `nexsys-hivemind/context/audits/2026-09-11_HERO-1b_return.md` §3 D2–D7 and `…/2026-09-11_HERO-1b_intake_two-layer-audit_v70-b9.md` §3 (the rulings this charter executes).
3. `web-ui/dashboard/src/lib/i18n.ts` whole (224 lines: the catalog; `t()` at `:222`; `MessageKey` `:217`) · `src/lib/format.ts` `:487`–`:520` (`hs`, `fillSlots`, `heroCopy`, `commandVerbs`) and `:197`–`:216` (`outcomeMeta`) · `src/lib/verdicts.ts` `:285`–`:490` (`ActionMode`, `MODE_GLYPHS`, `actionVerdict`) · `src/components/CausalChain.tsx` `:100`–`:245` (the `StepSpec[]` build; the action step at `:205`–`:240`) and `:355`–`:375` (`actionPhrase`, `terminalLine`) · `src/views/ExplainHubView.tsx` whole (75 lines) · `src/components/feedback.tsx` whole · `src/views/WhyNotView.tsx` `:190`–`:215`.
4. The tests beside them: `CausalChain.hardening.test.tsx` (`:265` pins the skipped line), `format.test.ts` `:40`–`:50`, `format.wire-nullability.test.ts` `:140`–`:150`, `WhyNotView.nullability.test.tsx` (the `lastEvaluation: null` block), `i18n.test.ts`, `verdicts.test.ts`.
5. `web-ui/dashboard/MODULE_CONTEXT.md` — the hero section as HERO-1b left it (row C7 rewrites it per §7 below).

## §2 The ground (source-cited by the hub at `a458a64`; re-read, never trust)
- The catalog already CARRIES every key this lane wires (`grep -c` in `i18n.ts`: `explain.mode.*` `.label/.line/.help` for all nine modes at `:98`–`:127`; `whyNot.kv.neverChecked` `:162`; `explain.hub.*` `:163`–`:175`; `explain.error/offline/replaying/loading` `:202`–`:213`). What is missing is the WIRING: `grep -rn "explain\.mode\." src --include=*.tsx` = 0 hits; `format.ts:595` is the only `.line` consumer (`notRecorded`, inside the headline builder).
- The action step line is built at `CausalChain.tsx:215`–`:220` from `actionPhrase(a.command)` (`:355`) + the target label — the same sentence for every outcome (D6). The mode is available beside it: `actionVerdict(a)` (`verdicts.ts:340`) returns `{ mode, label, tone, glyph, help }`; its `help` strings are literals (`verdicts.ts` `help: '…'` ×22; `help: hs(` ×0).
- `outcomeMeta` (`format.ts:197`–`:216`) carries "We sent it, but the device did not confirm it acted." (`:209`); no view calls it (`git grep -l outcomeMeta -- 'web-ui/dashboard/src/**/*.tsx'` = 0); its callers are two tests (`format.test.ts:47`–`:49`, `format.wire-nullability.test.ts:144`–`:149`).
- `ExplainHubView.tsx` renders literals: `title="Ask your home why"` and the lede (`:20`), the kickers (`:23`, `:33`), the go-lines (`:29`, `:39`); `explain.hub.lede`'s catalog text differs from the literal (the SPEC's "See what your home did — and what it didn't." wins).
- The state cards live in `feedback.tsx` (`Loading` `:13`, `ErrorState` `:31`, `ReplayingBanner` `:49`, `OfflineState` `:62`) and are mounted by `Resource.tsx` (`:17`–`:24`); `AppShell.tsx:23`/`:26` carries the shell's status-pill labels — NOT hero copy, out of scope.
- `WhyNotView.tsx:204`–`:210` renders `lastEvaluation` present; the null case renders ABSENCE (its comment `:197`); SPEC §7 gives that cell `whyNot.kv.neverChecked` "Never checked yet."
- SPEC §7's last row is `explain.loading` at `:275`; `## §8 Accessibility` at `:277` — row C0 appends after `:275`.

## §3 The rows (in order; each small; the test first)
| Row | Edit | RED at HEAD how | Files |
|---|---|---|---|
| **C0 — the SPEC §7 amendment (the hub's; verbatim)** | Append the four rows below after `:275`, unchanged to the byte; add the four keys to `i18n.ts` with the same strings | `i18n.test.ts`: four `t()` reads of keys HEAD's `MessageKey` does not have — red by construction (compile/undefined) | `design/hero-v1/SPEC.md` M · `i18n.ts` M · `i18n.test.ts` M |
| **C1 — the step lines per mode (D6)** | `CausalChain.tsx:215`–`:220`: the line = `heroCopy('explain.mode.<key>.line', slots)` keyed on `actionVerdict(a).mode` (mode→key: confirmed→`confirmed` · held-dispatched→`heldDispatched` · timed-out→`timedOut` · superseded→`superseded` · acked-silent→`ackedSilent` · settled-failed→`settledFailed` · expired-restart→`expiredRestart` · skipped→`skipped`/`skipped.lineNamed` · not-recorded→`notRecorded`); slots `Target` (the resolved label, capitalised as the key's brace shows), `target`, `verb`/`verbPast` from `commandVerbs(a.command)` (`format.ts:504`), `reasonClause` = `" — <reason>"` when a recorded reason exists else `""`; the dangling-target and unnamed-target arms keep their sentences | one `it` per mode in a new `CausalChain.modes.test.tsx` (or inside `hardening` — ground and declare): nine assertions; ≥8 RED (HEAD says "Turned on Hallway Light." for all); `skipped` pinned at `hardening:265` stays green (named) | `CausalChain.tsx` M · the test A or M |
| **C2 — the helps from the catalog** | `verdicts.ts` `actionVerdict`: every mode's `help` = `hs('explain.mode.<key>.help')`; the effect/identify class on acked-silent → `.unconfirmable`; settled-failed keeps its reason sub-label in `label` and takes `settledFailed.help`; the `resultOutcomeMeta` reason strings (`:74`–`:104`) are NOT mode helps — leave them | `verdicts.test.ts`: for each mode, `help` equals the catalog string; sort each after reading HEAD's literal (identical text → preservation, named; differing → red) | `verdicts.ts` M · `verdicts.test.ts` M |
| **C3 — retire `outcomeMeta`** | delete `format.ts:197`–`:216` and its two test blocks; if the "unrecognized outcome → honest can't-know" pin (`wire-nullability:144`) is not already covered by `actionVerdict`'s not-recorded arm, move it there | the moved pin (if any) red at HEAD only if it asserts new text — else named preservation; P3's two greps are the instrument | `format.ts` M · `format.test.ts` M · `format.wire-nullability.test.ts` M |
| **C4 — the hub page (D7)** | `ExplainHubView.tsx`: title, lede, both kickers, both card titles, both texts, both go-lines from `t('explain.hub.*')`; `:62`/`:66` link texts only if §7 carries their keys (check `explain.hub.autos.*`; else leave, say so) | a view test pinning the six rendered strings to the catalog; RED on the lede at least (the literal differs) | `ExplainHubView.tsx` M · the test A or M |
| **C5 — the state cards (D7)** | `feedback.tsx`: `Loading` default label = `explain.loading`; `ErrorState` title/body/button = `explain.error.title/.body/.retry`; `OfflineState` = `explain.offline.title/.body` (+ `.retry`); `ReplayingBanner` = `explain.replaying.title/.body`; `AppShell` untouched | a feedback test pinning each card's text to the catalog; RED where the literal differs (read each; sort) | `feedback.tsx` M · the test A or M |
| **C6 — never checked (D7)** | `WhyNotView.tsx:204`–`:210`: `lastEvaluation` null renders `t('whyNot.kv.neverChecked')` in the value cell | `WhyNotView.nullability.test.tsx`: the ABSENCE assertion for `lastEvaluation: null` FLIPS to the keyed sentence — name it with old and new text | `WhyNotView.tsx` M · the test M |
| **C7 — MODULE_CONTEXT (the hub's spec)** | `web-ui/dashboard/MODULE_CONTEXT.md`, the hero section: (1) the single-source rule — every hero sentence is a catalog key behind `t()`/`heroCopy()`, the instrument in §6; (2) the mode→key table of C1 with the slot names; (3) the four C0 keys and where each renders; (4) the tests that pin each surface (by file); (5) "wire FROZEN v1.1.4; no key read that 114a did not freeze" | — (documentation; named as such) | `MODULE_CONTEXT.md` M |

**The four C0 rows (append after SPEC `:275`, verbatim; Level and Words/sentence are hub-computed by the §7 formula at the sample slots — correct them in your §3 if your read of the formula differs):**
```
| `explain.terminal.noSteps` | Done, recorded no steps. | terminal step line, a completed run with `actionCount` 0 (SPEC §4's sentence, given its key) | — | 0.7 | 4 |
| `whyNot.headline.actedButUnconfirmed.noTime` | It ran, but the device never confirmed it acted. | why-not L1, N4 with `lastEvaluation` null (§3 N4's no-time arm, given its key) | — | 3.7 | 9 |
| `whyNot.headline.sentNothing.noTime` | It ran, but sent nothing — every step was skipped. | why-not L1, N6 with `lastEvaluation` null (§3 N6's no-time arm, given its key) | — | 3.7 | 9 |
| `explain.headline.completed.notRecorded` | {Target} was asked to {verb} because {because}; what happened isn't recorded. | L1 headline, the headline action's outcome null or a string this build does not know (D5; today rendered through `explain.mode.notRecorded.line`) | Target·verb·because | 6.8 | 15 |
```

## §4 What to watch out for
- `heroCopy` capitalises nothing for you: `{Target}` at a sentence start is the label as resolved (`refLabel`), `{target}` mid-sentence is the same label — do not lower-case a device name. A null target keeps HERO-1b's unnamed-target sentence.
- The skipped pair: `skipped.line` when `targetRef` is null, `skipped.lineNamed` when present (SPEC §7 `:200`–`:201` region) — `hardening:265` already pins the first; do not regress it.
- Held-DISPATCHED stays provisional: `StatusPill` dashed, `explain.a11y.provisional` `.sr-only`, the colour-class slow-confirm hint (`pendingHint`, `format.ts:248`) still appended to the help, not the line.
- `verdicts.ts` must not import from `format.ts` if that creates a cycle — `hs` is in `format.ts`; if `verdicts.ts` cannot import it, read the catalog through `t()` from `i18n.ts` directly (say which).
- Register C in every string you touch: no "we", no blame, no celebration; the DAS §2.2 banned list.
- A11y: the step line change must keep each `<li>`'s `explain.a11y.step` label and the `role="status"` region intact (SPEC §8); run the axe check `npm run verify` carries.

## §5 Out of scope
Any wire key (v1.1.5 is 114c's); `AppShell.tsx`'s status pills; the L1 headline grammar (HERO-1b's, unchanged except the new D5 key is ADDED to the catalog by C0 and NOT yet consumed — consuming it is a later row after the hub's headline audit); FE-114 (the placeholders → real values; waits on 114b's landing); the marketing site; anything under `core/**`.

## §6 Success criterion (binary) — the instruments
1. `git status --porcelain -- web-ui/dashboard` = §3's files exactly (or the declared deviation); nothing outside the domain.
2. `grep -rn "We sent it" web-ui/dashboard/src` = 0 · `grep -rn outcomeMeta web-ui/dashboard/src` = 0.
3. **No literal hero sentence:** `grep -rn -E "[A-Z][a-z]+ [a-z]+ [a-z]+.*[.…]\"|'[A-Z][a-z]+ [a-z]+ [a-z]+.*[.…]'" web-ui/dashboard/src/views/ExplainHubView.tsx web-ui/dashboard/src/components/feedback.tsx web-ui/dashboard/src/components/CausalChain.tsx` = 0 (report the command's exact output; a hit is a sentence still outside the catalog — fix or file).
4. `npm run verify` GREEN on the desk with counts (or its parts, deferred by command); `frontend.yml` GREEN on Nick's push.
5. SPEC `:276`–`:279` are the four C0 rows byte-identical to this charter's block; `## §8 Accessibility` follows.
Done when all five read as stated and the return is on disk with its `RETURNED` line.

## §7 Escalation
Stop and file (do not guess) if: a §7 key's string cannot be produced from the wire's facts without inventing a value; the mode→key table cannot cover a mode the wire produces; `npm run verify` is red for a reason outside your rows.

## §8 The hub's audit rules (pre-filed)
R1 the census at `git status --porcelain -- web-ui/dashboard` = §0's declaration · R2 every RED-at-HEAD claim re-executed by reading the test's assertions against HEAD's `CausalChain.tsx:215`–`:220` and the literals named in §2 · R3 the C0 bytes: SPEC `:276`–`:279` `diff` against this charter's block = empty; the four catalog strings equal the SPEC strings · R4 `grep -rn 'BRAND.productName\|"we '` over the touched files = 0 · R5 the §6 instruments' outputs quoted in the return re-run on the device · R6 the gates' counts or the deferral commands present · R7 the `RETURNED` line is the last line.

## §9 The dispatch line (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected — only after `FE: 6af76f7 frontend green`)
```
date -u first. You are the HERO-1c frontend lane for NexSys/HomeSynapse — boot as the nexsys-frontend skill. Baseline: homesynapse-core main at a458a64 or later, and `git status --porcelain -- web-ui/dashboard` EMPTY (core/automation and api/rest-api may be dirty — EXPLAIN-114b, a Java lane; never touch them). Read nexsys-hivemind/context/instructions/2026-09-13_frontend-lane_HERO-1c_step-lines-helps-and-catalog-wiring_charter.md WHOLE, then its §1 read-set in order. Rows C0 → C1 → C2 → C3 → C4 → C5 → C6 → C7, each test RED at HEAD before its edit (preservation rows named), then GREEN; C0's four SPEC rows are applied byte-for-byte from the charter's block. Write only under web-ui/dashboard/src/, design/hero-v1/SPEC.md (C0 only) and web-ui/dashboard/MODULE_CONTEXT.md (C7). Run npm run verify where your shell can; defer by exact command where it cannot; read the five §6 instruments and quote their outputs. Never git add/commit/stash/checkout. Write the return to nexsys-hivemind/context/audits/<today CT>_HERO-1c_return.md (≤12 KB; §0 card first; P1–P5 adjudicated first; the last line is `RETURNED <path> <bytes>`) and say that one line to Nick.
```
