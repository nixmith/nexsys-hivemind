<!--
file: context/instructions/2026-09-13_frontend-lane_HERO-1d_keyless-hero-sentences_literal-lint_charter.md
purpose: HERO-1d — the frontend lane that finishes what HERO-1c's D5 named: every hero sentence still living in a JSX text node, a template literal or a JSX attribute moves into the i18n catalog through t()/heroCopy(), byte-identical on screen; IR-8's literal lint becomes the instrument that keeps it so (an ESLint block over the hero files, inside `npm run verify`); one SPEC §7 amendment carries the new keys (the hub's rows, verbatim); the "colour" dialect swept to the catalog's own spelling (arc 25). Authored by the v74 hub at beat 4 on the ground at `93390f0` (HERO-1c landed, CI all green) from HERO-1c's return D5, the v73 b5 audit and IR-8.
audience: the HERO-1d frontend lane (a fresh Cowork conversation; §9 is its dispatch line) · the hub (the intake; §8) · Nick (pastes §9; lands the card)
state-type: lane charter (frontend; the web-ui path-domain — D4)
status: EXECUTED — pasted by Nick Sun 2026-09-13 ~20:5x CT; RETURNED the same evening (`context/audits/2026-09-13_HERO-1d_return.md`, 11,613 B; 17 = 14 M + 3 A under web-ui/dashboard; `npm run verify` GREEN on the desk, 480 tests; the lint 11 → 0; P2/P5 failed on their counts, disclosed); audited ACCEPT at v74 beat 5 (`context/audits/2026-09-14_v74-b5_HERO-1d_intake_two-layer-audit.md`); lands by the b5 web-ui card (`git add -A web-ui/dashboard`, 17) — frontend CI the gate. Was: DISPATCH-READY — authored v74 beat 4 (Sun 2026-09-13 ~20:4x CT; instrument 2026-09-14T01:49:45Z).
-->

# HERO-1d — the keyless hero sentences, the literal lint, the §7 amendment, the colour sweep

## §0 The lane contract (read first; every line binds)
- `date -u` first. State your instrument limit: what your shell can run (`node`, `npm`, `npx vitest`, `npx eslint`) and what it cannot; with `node_modules` installed outside the mount the Linux shell runs the toolchain (HERO-1b's O3). CT = UTC−5, derived once. Every gate you cannot run is DEFERRED to Nick's desk by its exact command in your §0 card, never claimed.
- **The one deliverable:** `nexsys-hivemind/context/audits/<your CT date>_HERO-1d_return.md`, one file, §0 card first (≤3 KB: the census `N = a M + b A` with exact paths · the red-first table (row → RED-at-HEAD how → GREEN; preservation rows named) · P1–P5 adjudicated · the gates with counts or deferred by command · deviations by tag), then §1 per-file deltas, §2 the gates, §3 pushback and observations, §4 the WUCP Phase-1 checklist. Cap ≤12 KB is a CEILING. Your last line, in the file and printed to Nick: `RETURNED <path> <bytes>`.
- **Write-set:** `homesynapse-core/web-ui/dashboard/src/**` (the files §3 names plus the tests beside them), `web-ui/dashboard/eslint.config.js` (row D0 only), `web-ui/dashboard/design/hero-v1/SPEC.md` (row D5's §7 rows — the hub's text, verbatim — and row D6's spelling sweep; nothing else in the SPEC), `web-ui/dashboard/MODULE_CONTEXT.md` (row D7). Nothing under `core/**`, `api/**`, `lifecycle/**`; nothing outside `web-ui/dashboard/`. `git status --porcelain -- web-ui/dashboard` must be EMPTY when you start (core is at `93390f0`, clean); other paths may become dirty while you work (EXPLAIN-114c, a Java lane, may be editing `core/automation/**` and `api/rest-api/**` — never touch them). Never run `git add`, `git commit` or `git push`; Nick lands with one card.
- **Tests first, every row.** Each row's new assertions are RED at HEAD before the edit (the table says how), then GREEN; a row HEAD already satisfies is a preservation fixture (law 18), never claimed red. A flipped existing assertion is named with its old and new text.
- **Byte-identical on screen.** This lane changes WHERE a sentence lives, not what it says: every keyed sentence's catalog text is the literal it replaces, character for character (slots for the interpolated parts). The one text change in the whole lane is row D6's spelling. The honesty law still binds: a null renders its keyed sentence; nothing the wire did not carry is shown.
- **The wire is FROZEN v1.1.4** (the freeze doc's amendment record, 2026-09-13). No new key is read; no shape changes.
- **Predictions, pre-registered; adjudicate them first in your §0 card:** P1 — the census is §3's files exactly, test homes declared (a new test file is `A`). P2 — row D0's lint, run at HEAD BEFORE any other row, reports ≥ 12 hits across the six hero files (the D5 list below is the hub's count from the source at `93390f0`: `CausalChain.tsx` :161 · :251 · :269 + its hint · :401 · :413 ×2 · :420–:423 · :425 ×2 · :427; `ExplainHubView.tsx` :41 · :60 · :64 + the On/Off pills; `WhyNotView.tsx` :60 ×2 · :155 · :157; `RunsView.tsx` :19 ×2 · :27) and 0 after D1–D4 — report the exact hit list at HEAD, then the exact output after. P3 — `npm run verify` GREEN (the lint is inside it after D0). P4 — no existing test assertion changes its expected text (`git diff -U0 -- '*.test.ts*' | grep -E '^[-+]\s*expect' ` shows only ADDED lines); the screen text is byte-identical by construction. P5 — `grep -rni colour web-ui/dashboard/src web-ui/dashboard/design | wc -l` reads 5 at HEAD (SPEC :33 · :70 · :178; `i18n.ts` :108; `CausalChain.modes.test.tsx` :100) and 0 after D6.

## §1 The read-set, in order (nothing older)
1. This charter whole. 2. `web-ui/dashboard/MODULE_CONTEXT.md` (the HERO-1c paragraph; the catalog's shape). 3. `web-ui/dashboard/src/lib/i18n.ts` whole (the catalog: the `explain.terminal.*` keys :143–:152, `explain.condition.line` :204, `explain.cascade.parent` :199, `explain.hub.*` :183–:195, `ui.*` :46–:49). 4. `web-ui/dashboard/design/hero-v1/SPEC.md` §7 (the copy table :130–:282) and §11. 5. HERO-1c's return `nexsys-hivemind/context/audits/2026-09-13_HERO-1c_return.md` §3 D4–D5 (the keys added-not-consumed; the blind spot). 6. `nexsys-hivemind/context/planning/improvement-register.md` row IR-8 (this lane's instrument). 7. The six hero files as you touch them: `src/components/CausalChain.tsx`, `src/views/ExplainHubView.tsx`, `src/views/WhyNotView.tsx`, `src/views/RunsView.tsx`, `src/views/RunChainView.tsx`, `src/components/Resource.tsx` (+ `feedback.tsx` read-only — HERO-1c keyed it).

## §2 The ground (source-cited by the hub at `93390f0`; re-read, never trust)
- `CausalChain.tsx:161` `line: \`The rule "${c.expression ?? NOT_RECORDED}" ${verdict}.\`` — the key exists: `explain.condition.line` "The rule \"{condition}\" {verdict}." (`i18n.ts:204`). `:251` the recovered-reason suffix `' (recovered from the recorded reason — this record predates the current hub software)'` — no key. `:269` `Nothing was changed: ${… 'the planned step' | \`all ${n} planned steps\`} ended without sending a command.` + the hint paragraph beneath (`:271`–) — no keys. `:307` `← See what triggered this run` — the key exists: `explain.cascade.parent` (`i18n.ts:199`). `:399`–`:427` `terminalLine`: `Outcome ${NOT_RECORDED}.` · `Finished in ${secs}s, but nothing was changed.` / `Finished, but nothing was changed.` · `Done${inSecs} — one outcome has not settled yet.` / `… — ${open} outcomes …` · `Done in ${secs}s.` / `Done.` · `Skipped — ${reason}.` / `Skipped.` and the FAILED/CANCELLED/INTERRUPTED arms — the keys `explain.terminal.completed` · `.completed.open` · `.completed.nothing` · `.skipped` · `.failed` · `.cancelled` · `.interrupted` · `.noSteps` exist (`i18n.ts:144`–`:152`; the return's D4 says `.noTime` was added too — confirm at HEAD); the singular-open and no-seconds arms have NO key.
- `ExplainHubView.tsx:41` `<h2>Your automations</h2>`; `:60` `Why did it fire?`; `:64` `Why didn&rsquo;t it?`; the On/Off pills in the same list — no keys (`explain.hub.*` :183–:195 carries the page's other copy).
- `WhyNotView.tsx:60` `title="Why didn't it happen?" lede="Choose the automation you expected to run."`; `:155` `title="Why this didn't happen"`; `:157` `← Pick another automation` — no keys. `RunsView.tsx:19` `title="Why did something happen?" lede="Pick a run to see exactly why it fired, step by step."`; `:27` `emptyLabel="No automation runs yet."` — no keys.
- The §6.3 grep of HERO-1c reads 1 on the five hero files at HEAD; the JSX-text and attribute forms above are invisible to it (D5) — that is IR-8, and row D0 replaces the grep.
- `eslint.config.js` exists (flat config; `eslint ^10`); `npm run verify` = tokens:check → lint → typecheck → test → build → check:bundle → … (`package.json:21`). 441 tests at `93390f0` (the v73 b6 audit).
- `colour` at HEAD: SPEC `:33` and `:70` ("colour role"), `:178` (the `explain.action.pending.color` sentence "Colour changes confirm slowly on some bulbs — this can take several seconds."), `i18n.ts:108` (the same sentence), `CausalChain.modes.test.tsx:100` (a test title). The catalog's own spelling is `color` (the key name); the product's dialect of record is the key's.

## §3 The rows (in order; each small; the test first)
| Row | Files | What |
|---|---|---|
| **D0 — the literal lint (IR-8's instrument), FIRST** | `eslint.config.js` (M) | One added config block scoped to the six hero source files (never the tests) using `no-restricted-syntax` with three selectors: a `JSXText` whose value matches `/[A-Z][a-z]+ [a-z]+ [a-z]+/` (a capitalised ≥3-word run); a `TemplateLiteral > TemplateElement` whose `value.raw` matches the same; a `JSXAttribute[name.name=/^(title|lede|label|emptyLabel|placeholder)$/] > Literal` whose value matches the same. Message text: "hero copy lives in i18n.ts (HERO-1d)". Run it at HEAD before any other row and paste the hit list into your §0 (P2's baseline); it must be RED there. It is GREEN when D1–D4 are done; it stays in `npm run verify` through `npm run lint`. If esquery's regex attribute form is not honoured by your ESLint version, the fallback is a 30-line script `web-ui/dashboard/scripts/hero-literals.mjs` over the same six files with the same three patterns, wired as `check:hero-copy` into `verify` — a T1 instrument choice; say which you took. |
| **D1 — `terminalLine` keyed** | `CausalChain.tsx` (M) · `CausalChain.modes.test.tsx` or a new `CausalChain.terminal.test.tsx` (A) · `i18n.ts` (M) | Every arm returns `t(key, slots)`: the existing keys where they match byte-for-byte; the arms with no key get the D5 rows (`explain.terminal.unrecorded` · `.completed.noTime` if absent · `.completed.nothing.noTime` · `.completed.open.one` · `.completed.open.noTime` · `.completed.open.one.noTime` · `.skipped.noReason`, and the FAILED/CANCELLED/INTERRUPTED arms as HEAD renders them). RED at HEAD: a test that asserts `terminalLine`'s output for each arm equals `t(key, …)` fails on the keyless arms; the byte-identical assertion (old literal === new render) is the preservation row. `terminalLine` keeps its signature. |
| **D2 — the condition line, the cascade link, the do-nothing step, the recovered suffix** | `CausalChain.tsx` (M) · its tests (M) · `i18n.ts` (M) | `:161` → `t('explain.condition.line', {condition, verdict})`; `:307` → `t('explain.cascade.parent')`; `:269` → `t('explain.step.nothing.one')` / `t('explain.step.nothing.many', {count})` and the hint → `t('explain.step.nothing.hint')` (its existing text, byte-preserved); `:251` → `t('explain.action.detail.outcome.recovered')`. RED at HEAD: one assertion per key that the rendered text equals `t(...)` — green-by-construction for none, since none is keyed at HEAD. |
| **D3 — the hub page** | `ExplainHubView.tsx` (M) · `ExplainHubView.test.tsx` (M) · `i18n.ts` (M) | `explain.hub.autos.title` "Your automations" · `explain.hub.autos.whyFire` "Why did it fire?" · `explain.hub.autos.whyNot` "Why didn't it?" (the `&rsquo;` entity becomes the catalog's `’`; assert the rendered text) · `ui.on` "On" · `ui.off` "Off". |
| **D4 — the why-not and runs pages** | `WhyNotView.tsx` (M) · `RunsView.tsx` (M) · their tests (M) · `i18n.ts` (M) | `explain.whyNot.pick.title` "Why didn't it happen?" · `explain.whyNot.pick.lede` "Choose the automation you expected to run." · `explain.whyNot.title` "Why this didn't happen" · `explain.whyNot.back` "← Pick another automation" · `explain.runs.title` "Why did something happen?" · `explain.runs.lede` "Pick a run to see exactly why it fired, step by step." · `explain.runs.empty` "No automation runs yet." `RunChainView.tsx` and `Resource.tsx`: key whatever D0 flags there; if D0 flags nothing, say so (a preservation row). |
| **D5 — the §7 amendment (the hub's rows, verbatim)** | `design/hero-v1/SPEC.md` (M) | Append to §7's copy table, after its last row, one row per key D1–D4 ADDED (not the pre-existing keys), in this exact form: `| \`<key>\` | <the sentence, byte-identical to the literal it replaced, with {slots}> | <surface> | HERO-1d |`. The sentences are the existing literals — this amendment records where copy lives, it changes no copy. Under the table, one line: `*§7 amendment, HERO-1d (2026-09-13): the keys above moved the last literal hero sentences into the catalog; the lint of `eslint.config.js` (IR-8) is the instrument. No sentence changed.*` |
| **D6 — the spelling sweep (arc 25: a dialect fix sweeps the corpus)** | `SPEC.md` (M) · `i18n.ts` (M) · `CausalChain.modes.test.tsx` (M) | `colour` → `color` at the five sites of §2 (the sentence at `i18n.ts:108` and SPEC `:178` become "Color changes confirm slowly on some bulbs — this can take several seconds."; the two "colour role" headers; the test title). The catalog key `explain.action.pending.color` already says so. Report, do NOT change, any other UK/US variant you meet (`Cancelled`, `-ise`, `-our`) as a count in your §3 — the hub decides the register. |
| **D7 — MODULE_CONTEXT** | `web-ui/dashboard/MODULE_CONTEXT.md` (M) | One paragraph under the HERO-1c note headed **HERO-1d (the last literal hero sentences keyed; the literal lint)**: the rule (no hero sentence outside `i18n.ts`; the lint's three selectors and where it lives), the keys added, the test homes, the spelling of record. |

## §4 What to watch out for
- The ESLint block must not fire on the TEST files or on `i18n.ts` itself (the catalog is where sentences live). Scope by `files:` in the flat config.
- `NOT_RECORDED` is a constant; its text stays a constant — key the SENTENCE around it (`explain.terminal.unrecorded` "Outcome {notRecorded}."), never the constant.
- `terminalLine`'s `secs` guard (a missing duration is OMITTED, never "0.0s") is a HERO-1b honesty row — the keyed arms preserve it (the `.noTime` arms exist for exactly this).
- `&rsquo;` in JSX becomes `’` in the catalog; assert the rendered string, not the source.
- The hint under the do-nothing step is a paragraph; key it whole as one string (slots if it interpolates), byte-preserved.
- Nothing in this lane touches `verdicts.ts`, `format.ts`, the wire client or any test's expected text; if a row seems to need it, stop and file the row in §3 with the line.

## §5 Out of scope
New copy; any rendering, verdict or mode change; `feedback.tsx`/`Resource.tsx`'s `ui.*` pair (HERO-1c's); the `Cancelled`-class spellings (reported, not changed); FE-114 (the v1.1.4 keys' rendering — its own lane); anything under `core/**` or `api/**`.

## §6 Success criterion (binary) — the instruments
1. `git status --porcelain -- web-ui/dashboard` = §3's files exactly (or the declared deviation); nothing outside the domain.
2. The D0 lint: the hit list at HEAD pasted in §0 (≥ 12, P2); `npx eslint <the six files>` = 0 after D4; `npm run lint` green.
3. `grep -rni colour web-ui/dashboard/src web-ui/dashboard/design | wc -l` = 0.
4. `npm run verify` GREEN on the desk with counts (or its parts, deferred by command); `frontend.yml` GREEN on Nick's push.
5. SPEC §7 carries the D5 rows in the charter's exact form and the amendment line; no other SPEC line changed but D6's three.
6. `git diff -U0 -- '*.test.ts*' | grep -E '^-\s*expect'` = 0 (no existing expectation removed or reworded).
Done when all six read as stated and the return is on disk with its `RETURNED` line.

## §7 Escalation
STOP and file (do not guess) when: a literal cannot be keyed byte-identically (an interpolation the catalog's slot grammar cannot express); the ESLint regex selector is not honoured AND the script fallback would need more than the three patterns; a test's expected text would have to change; D0 flags a sentence in a file outside §3's list. Say what you saw and the line.

## §8 The hub's audit rules (pre-filed)
The hub re-runs D0's lint on the returned tree, re-greps `colour`, re-runs `npm run test` counts if the desk allows, diffs the SPEC rows against this charter's D5 form, and reads `git diff -U0 -- '*.test.ts*'` for removed expectations. A keyed sentence whose catalog text differs from its former literal by one byte is a FINDING; two are a correction before the landing (the correction-before-freeze rule). The lint's baseline count and its after-count are banked as one spine line.

## §9 The dispatch line (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected)
```
date -u first. You are the HERO-1d frontend lane for NexSys/HomeSynapse — boot as the nexsys-frontend skill. Baseline: homesynapse-core main at 93390f0 or later, and `git status --porcelain -- web-ui/dashboard` EMPTY (core/automation and api/rest-api may be dirty — a Java lane; never touch them). Read nexsys-hivemind/context/instructions/2026-09-13_frontend-lane_HERO-1d_keyless-hero-sentences_literal-lint_charter.md WHOLE, then its §1 read-set in order. Rows D0 → D1 → D2 → D3 → D4 → D5 → D6 → D7: D0's lint runs at HEAD first and its hit list is your P2 baseline; every other row's test is RED at HEAD before its edit, then GREEN; every keyed sentence is byte-identical to the literal it replaces; the only text change is D6's spelling. Write only under web-ui/dashboard/. Never git add/commit/push. Write the return to nexsys-hivemind/context/audits/<today CT>_HERO-1d_return.md (≤12 KB; §0 card first; P1–P5 adjudicated; the lint's before and after lists; the last line `RETURNED <path> <bytes>`) and say that line to Nick.
```
