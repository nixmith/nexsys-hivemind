<!--
file: context/instructions/2026-09-13_skills-lane_W-SKILLS-9_the-fold-of-nine-mints_charter.md
purpose: The W-SKILLS-9 charter — the fold of the nine pm-lessons mints since 2026-09-10 (and the v73 window's structural adds) into the three SOURCE skill trees, in the W-SKILLS-6 token-shaped form: every rule name survives; each mint becomes one operative sentence + its mechanism + its exhibit pointer in the file that the next reader of that rule opens; nothing is re-told. A skills lane (a fresh Cowork conversation); the hub audits at the bytes; Nick's sync clears Check 9.
audience: the skills lane · the hub (the intake; Check 9) · Nick (§4 is his paste)
state-type: lane charter (the fold table governs)
status: DISPATCH-READY — authored v73 beat 7 (Sun 2026-09-13 ~18:1x CT; instrument 2026-09-13T23:18:34Z); dispatches mid-week when a desk slot is free (the plan of record §8 row 5).
-->

# W-SKILLS-9 — the fold of nine mints

## §0 The lane contract
- `date -u` first; CT = UTC−5, once. Baseline: `nexsys-hivemind` and `nexsys-skills` at their `main` HEADs with empty porcelain (the boot names them); write only the §2 files. Never `git add`/`commit`; Nick lands with two cards (hivemind, skills) and then syncs the account skills (Check 9 clears on his sync — a lag is STALE, never CONFLICTED).
- **The deliverable:** `nexsys-hivemind/context/audits/<your CT date>_W-SKILLS-9_return.md` (≤10 KB; §0 card first: the census per tree, the fold table's rows each with its target line number, the byte deltas, the rule-name census — every name that existed before exists after — and the `md5sum` list of the touched files; the last line `RETURNED <path> <bytes>`).
- **The form (W-SKILLS-6):** token-shaped — a rule is one sentence a reader can hold, its mechanism in a clause, its exhibit as a path; the ledgers carry the detail; a SKILL.md carries no project state (a HEAD, a count, a beat number in a skill file is a defect). Register C.
- **Predictions:** P1 — the census is §2's table exactly. P2 — the rule-name census: zero names lost (a grep of every `**THE …**` / arc name before and after). P3 — the three `freshness-preflight.md` copies stay identical in their shared-protocol sections (Check 12's text). P4 — at least one cite in this charter is wrong at your HEAD; file the corrected line.

## §1 The mints (read `context/lessons/pm-lessons.md` lines 316–342; each is one `## 2026-09-1x — NAME` entry)
THE SAME-PATH RE-WRITE (09-11) · THE TRAILER REGRESSION (09-10) · THE REGISTER OF RECORD (09-10) · THE HANDS RULE (09-10) · THE CODER-SESSION PROMPT CHECKLIST (09-12) · THE CORRECTION-BEFORE-FREEZE RULE (09-12) · THE OUTPUTS-PATH MIRROR (09-13) · THE PRIOR-LEDGER GATE (09-13) · THE PROBE-EVERY-CAP RULE (09-13). Plus the v73 structural adds, exhibited in `context/audits/2026-09-13_v73-b1_boot-and-intake_audit.md` (the splice library; Check 9 as run), `…v73-b3_EXPLAIN-114b_intake_two-layer-audit.md` §4 (the improvement register), `…v73-b5_R-5A_and_HERO-1c_intake_two-layer-audit.md` (the combined intake; the correction-before-landing ruling), `…v73-b6_…` (the rotation arithmetic).

## §2 The fold table (the target file · the section · the operative sentence; the exhibit is the mint's `**Detail:**` pointer)
| # | Mint / add | Target | Where and what |
|---|---|---|---|
| 1 | THE HANDS RULE · THE TRAILER REGRESSION · THE REGISTER OF RECORD | `nexsys-hivemind/project-manager/references/laws-ledger.md` | arc 7's exhibit list (the trailer regression); the commit-boundary arc's text = the v69 b1 SKILL §1 sentence (every repo Nick's hands); a new arc: the register of record (DAS §2 on the hivemind's own prose) |
| 2 | THE CODER-SESSION PROMPT CHECKLIST | `project-manager/references/coding-instruction-format.md` (a numbered law after #29) + `nexsys-hivemind/coder/SKILL.md` (the lane's own checklist: the cap arithmetic; the gate lines verbatim; scripts over ~15 lines by the Write tool; a moved capture point says what the old one could not see) | one sentence each side |
| 3 | THE CORRECTION-BEFORE-FREEZE RULE + the correction-before-landing pattern (HERO-1c D3) | `project-manager/references/review-and-quality.md` §3 | one paragraph: an additive wire key with a self-contradicting null, or a user-visible wrong sentence, is corrected BEFORE the landing by an AUDIT CORRECTION paste to a fresh lane ("do not re-run the WU"), never after; the exhibits (114a R3; HERO-1c D3) |
| 4 | THE PRIOR-LEDGER GATE | `coding-instruction-format.md` (a numbered law) + the WUCP Phase-2 checklist for rig WUs (`../context/protocols/work-unit-completion-protocol.md`, one line) + `project-manager/SKILL.md` §3 (one line) | the sentence as minted: before a packet cut from a prior record goes LIVE, its cutter greps that record's deviations ledger and asks for every reused command and witness; the greps listed in the cutting beat's audit |
| 5 | THE PROBE-EVERY-CAP RULE + the splice library | `project-manager/SKILL.md` §3 law 5 (the guarded-splice law gains its mechanism: every capped region probed before PHASE 1; the library `context/process/splice_lib_v1.py`, md5-asserted, versioned by file name) | one sentence + the path |
| 6 | THE SAME-PATH RE-WRITE · THE OUTPUTS-PATH MIRROR · the `.git/index.lock`-through-the-bridge hazard (R-5A) | `nexsys-hivemind/context/process/cowork-environment-model.md` §§9–12 | one bullet each (the mechanism; the fix: heredoc → a python write on the device; `--no-optional-locks` on every git call through the bridge) |
| 7 | THE IMPROVEMENT REGISTER | `project-manager/SKILL.md` §2 (step 7: the intake's findings land as register rows; step 9: WUCP Phase 2 reads the register for the surface) + §6 (one table row: `context/planning/improvement-register.md` — read by grep when authoring on a surface) | two sentences |
| 8 | Check 9 as run at v73 b1 (the per-file md5 SOURCE vs the session's synced copies) · Check 12's exclusion form | `project-manager/references/freshness-preflight.md` Check 9 (the exhibit line → the v73-b1 audit) and Check 12 (the exclusion list is STATE, re-derived: the lanes the newest beat records as dispatch-ready + today's date + the newest prompt version) — and the SAME text in `coder/references/freshness-preflight.md` and `nexsys-skills/orchestrators/nexsys-frontend/references/freshness-preflight.md` (P3) | the shared-protocol rule: amend all three or none |
| 9 | The frontend skill's O3 correction (HERO-1b) · the `ui.*` vs `explain.*` label rule (HERO-1c D3) · the hero-literal lint (IR-8) | `nexsys-skills/orchestrators/nexsys-frontend/SKILL.md` + `references/build-and-ci-discipline.md` / `references/explainability-and-accessibility.md` | O3: "with `node_modules` installed outside the mount, the Linux shell runs the toolchain"; the state primitives default to the app pair, hero views pass the hero rows; every hero sentence is a catalog key — the lint is the instrument |
**Census claim:** hivemind 7 M (`project-manager/SKILL.md` · `laws-ledger.md` · `coding-instruction-format.md` · `review-and-quality.md` · `freshness-preflight.md` · `coder/SKILL.md` · `coder/references/freshness-preflight.md`) + `context/process/cowork-environment-model.md` + `context/protocols/work-unit-completion-protocol.md` = 9 M; skills 3 M (`nexsys-frontend/SKILL.md` + two references). Deviations declared with the row.

## §3 What to watch out for
Every SKILL.md stays state-free (no HEADs, counts, beat numbers). The ledgers grow by one sentence per rule, not by a paragraph. Do not renumber existing laws; append. `pm-lessons.md` is NOT edited (the mints stay as the record; a folded mint gets `— FOLDED W-SKILLS-9` appended to its heading line only). The three preflight copies are byte-compared in your §0.

## §4 The dispatch line (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected)
```
date -u first. You are the W-SKILLS-9 skills lane for NexSys/HomeSynapse — boot as the nexsys-project-manager skill's reader (read, do not act as the hub). Baseline: nexsys-hivemind and nexsys-skills at their main HEADs, porcelain empty. Read nexsys-hivemind/context/instructions/2026-09-13_skills-lane_W-SKILLS-9_the-fold-of-nine-mints_charter.md WHOLE, then context/lessons/pm-lessons.md lines 316–342, then each target file's section before you touch it. Fold row by row in the token-shaped form; never lose a rule name; keep the three freshness-preflight copies identical in their shared sections; edit no SKILL.md into carrying state. Never git add/commit. Write the return to nexsys-hivemind/context/audits/<today CT>_W-SKILLS-9_return.md (≤10 KB; §0 card first; P1–P4 adjudicated; the md5 list; the last line `RETURNED <path> <bytes>`) and say that line to Nick.
```
