<!--
file: context/instructions/2026-09-14_coder-lane_EXPLAIN-114c_by-id-read_condition-definition_DP-B2_coding-instruction.md
purpose: EXPLAIN-114c — the v1.1.5 ADDITIVE read-API step on the causal-chain payload and three corrections inside it: (K2) the by-id read — `locateTriggered` finds a run's `automation_triggered` by the RunId's own timestamp instead of walking the whole type index (IR-1 measured 100 pages at 50k retained markers for the NEWEST run; after this WU ≤ 2); (K3) `conditions[].definition` — the condition rendered as STRUCTURED data from the registry's definition, hash-guarded, present-null; (DP-B2) `FIRED_CONFIRMED` only on confirmation evidence (the HERO-1b audit's Core item); (IR-2) the `listRuns` cursor stop; (IR-3) one named visitor verdict. K1 (`cascade.parentRunId`) and K4 (`forDuration` `firingValue`) are OUT with the reasons in §11 — both are engine facts, not read-side keys (the v75 b2 audit §3). Authored by the v75 hub at beat 2 (Mon 2026-09-14; instrument 2026-09-14T19:28:03Z) on the source at `6bd8508` (an in-conversation grounding agent's return, the hub's re-executions beside every claim) and on IR-1's measured lines (`_scratch/v75/2026-09-14_IR-1_measurement.txt`).
audience: the Coder (host-side Claude Code, nexsys-coder) · the hub (intake) · Nick (the landing card)
state-type: coding instruction
status: EXECUTED — returned 2026-09-14T23:12Z (`context/audits/2026-09-14_EXPLAIN-114c_return.md`, 14,568 B); audited ACCEPT v75 beat 3 (`context/audits/2026-09-15_v75-b3_114c-and-FE-114_intake_two-layer-audit.md`); the landing on Nick's card (`_scratch/v75/2026-09-15_v75-b3_operator-packet.md` block 1); CI on the push is the gate of record; DP-1's fallback mechanism corrected by the return (R1 — the fixed-clock seed is a hint hit; the displaced id is the fallback). Was: DISPATCH-READY — v75 beat 2 (Mon 2026-09-14).
-->

# EXPLAIN-114c — the by-id read, the condition definition on the wire, and FIRED_CONFIRMED on evidence (v1.1.5, additive)

## §0 The lane contract (read first; every line binds)
- `date -u` first; state your instrument limit; CT = UTC−5, derived once.
- **Return path:** `nexsys-hivemind/context/audits/<CT-filing-date>_EXPLAIN-114c_return.md`, one file. §0 card first (≤3 KB: the census `N = a M + b A` with exact paths · P1–P6 adjudicated · the four §12 instruments with their readings · DP-1/DP-2 as taken) · §1 what changed per file · §2 the gates with counts · §3 pushback and observations · §4 the WUCP Phase-1 checklist. **Cap ≤13 KB** (3 KB + 1 KB × the 10 Files-table rows) — a ceiling, not a target; the §0 card is what the hub reads first; a shorter return with the same receipts is a better return. Your last line, in the file and printed: `RETURNED <path> <bytes>`.
- **Baseline:** `git log -1 --format=%h` is `6bd8508` and `git status --porcelain` shows NOTHING under `core/**`, `api/**`, `lifecycle/**` (a frontend lane, FE-114, may be dirtying `web-ui/dashboard/**` beside you — never touch it; anything else dirty: stop and report). Every line number below was read at `6bd8508` with the command beside it; a cite that moved is re-derived at your HEAD and filed (P6).
- **Write-set:** the ten paths of the §3 Files table only. Never `module-info.java` (§4 pins zero change and says why), never `build.gradle.kts`, never `core/event-model/**` (the `EventStore` shape test pins 6 methods — `core/event-model/src/test/java/com/homesynapse/event/EventStoreTest.java:28–:31`; this WU adds no method), never `core/persistence/**`, never `web-ui/**`, never any event payload, never `context/decisions/*`. You commit nothing and stage nothing; Nick lands the tree with a card after the hub's audit.
- **Build discipline:** the gate line, verbatim (task paths from `settings.gradle.kts:19,:25,:42,:58`): `./gradlew :core:automation:test :api:rest-api:test :lifecycle:lifecycle:compileJava spotlessCheck --offline` — green in one round is the target; `./gradlew check` on Nick's push is the gate of record. Scripts over ~15 lines are written with the Write tool and run by path — no long heredocs.
- **The freeze governs the shape:** v1.1.4 is FROZEN; this WU is the v1.1.5 ADDITIVE step (the freeze doc's change discipline §D): ONE new key, `conditions[].definition`, appended LAST inside each condition object (after `observedState`), PRESENT in every v1.1.5 payload, JSON `null` when the service has nothing it can vouch for; every other key, its order, casing and nesting byte-unchanged (§12 instrument 1). One TEXT correction inside a frozen string (DP-B2's `explanation` sentence) and one VERDICT-mapping correction (DP-B2) are declared corrections of a v1.1.4 defect (the correction-before-freeze rule), not shape changes — say so in your §0.
- **Tests first, honestly sorted (format law #18):** rows T1–T4 are RED at HEAD by construction (the table says how); T5–T7 are preservation fixtures, green at HEAD, disclosed as such; T8 is the guard that turns IR-1's measurement into an assertion.
- **Test clock:** *Tests must inject `Clock`.* Do NOT use `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, or `System.currentTimeMillis()` in this module's test code. Use `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` injected via constructor/`@BeforeEach`. **Enforcement reach:** `NO_DIRECT_TIME_ACCESS` runs from `com.homesynapse.app`'s test classpath, so it mechanically catches PRODUCTION code in every non-whitelisted module (plus app's own tests) — it does **not** scan this module's test source set. Clock-injection here is a self-enforced project convention that PM review, not `./gradlew check`, enforces. **The IR-1 instrument you carry (T8) therefore drops its `System.nanoTime` wall-time lines** — the page and envelope counts are the measurement; a ticking test clock (DP-1) replaces the fixed one where the hint needs distinct instants.
- **Predictions, pre-registered; adjudicate them first in your §0:** P1 — the census is the Files table (§3) exactly, or the deviation is declared with its cause. P2 — T8 at HEAD (before K2) prints/asserts pages = 2 / 40 / 100 for the newest run at N = 1k / 20k / 50k (IR-1's measured lines, byte-for-byte the counts) and ≤ 2 after K2 at every N; the fallback case (DP-1's fixed-clock seed) still finds the run at the old page count. P3 — the gate line is green in one round; automation tests 232 + the new rows (232 = `grep -c '@Test'` over the tracked test classes at `6bd8508` — `_scratch/v75/Ir1PageCountMeasurementTest.java` is NOT tracked; re-derive), rest-api 156 (3 skip) + the new rows. P4 — `RunEndpointsTest.causalChain_v114KeysLastInOrder_iso8601OrNull` (`api/rest-api/src/test/java/com/homesynapse/api/rest/RunEndpointsTest.java:262–:294`) passes UNCHANGED except the condition-object key list, which gains `"definition"` last. P5 — `grep -c 'eventStore.readByType(' StandardExplanationService.java` still reads `1` after K2 (K2 adds ONE `readByTimeRange` site: `grep -c 'eventStore.readByTimeRange(' = 1`, `0` at HEAD). P6 — at least one line cite in this instruction has moved at your HEAD, or you state that none did.
- **Two scope decisions you take and declare:** **DP-1 — the ticking clock for T8.** The hint (K2) needs the seeded markers to carry DISTINCT instants; a `Clock.fixed` seed gives every marker the same instant and the time-window read returns all N (the hint is defeated by construction, not by a defect). Default: T8 seeds through a test clock that advances 1 s per published marker (a small mutable `Clock` in the test — check `AutomationTestSupport` for an existing one first: `grep -n 'class .*Clock' core/automation/src/test/java/com/homesynapse/automation/AutomationTestSupport.java`) and runs the FALLBACK case with `Clock.fixed` (all markers at one instant → the hint window holds them all → the counted fallback path — the old page count; both paths asserted). If you find a better shape, declare it. **DP-2 — the renderer's home.** Default: a package-private final class `ConditionDefinitionRenderer` in `com.homesynapse.automation` (row 3), one `static` entry, an exhaustive `switch` over the sealed `ConditionDefinition` and the sealed `Selector`. If you fold it into `StandardExplanationService` instead, say why (the class is already ~1,050 lines — the default keeps it out).

## §1 What this implements
Four things, all inside `core/automation` and `api/rest-api`, on the frozen v1.1.4 contract's additive path:
- **K2 — the by-id read.** `explainRun` (`StandardExplanationService.java:183–:225`; `sed -n '183,225p'`) starts at `locateTriggered(runId)` (`:1007–:1017`), which walks the `automation_triggered` type index from position 0 through `scanType` (`:964–:978`, `SCAN_BATCH = 500` at `:102`) and returns at the first payload whose `runId` matches (`:1010–:1012`). The newest run — the common ask — pays for every retained marker: IR-1's instrument read **pages=2 / 40 / 100, envelopes=1,000 / 20,000 / 50,000** at N = 1k / 20k / 50k (`_scratch/v75/2026-09-14_IR-1_measurement.txt`, run on Nick's host 2026-09-14T19:07Z). The fix needs NO store change: a `RunId` is a time-ordered ULID minted from the engine's clock (`StandardRunManager.java:257 RunId runId = new RunId(UlidFactory.generate(clock));`; `Ulid.extractTimestamp()` at `platform/platform-api/src/main/java/com/homesynapse/platform/identity/Ulid.java:164`), and both stores serve `readByTimeRange(from, to, afterPosition, maxCount)` over `COALESCE(event_time, ingest_time)` as `[from, to)` (`SqliteEventStore.java:221–:226` `SELECT_BY_TIME_RANGE_SQL`; `InMemoryEventStore.java:225–:252`). `locateTriggered` first pages `readByTimeRange(t − 1 s, t + 60 s, 0, SCAN_BATCH)` with `t = runId.value().extractTimestamp()`, matching the payload exactly as today; only on a miss does it fall back to the full ascending scan (the current code, kept verbatim as the fallback), and the fallback is visible: the counting decorator sees one `readByTimeRange` page (or a few) for the hit and the old `readByType` count only on the miss.
- **K3 — the condition definition on the wire.** `buildConditions` (`:715–:737`) renders `ConditionView.expression` from the event's `conditionType` simple name because "the YAML text is not on the event" (`:731`); `AutomationConditionEvaluatedEvent` carries `(Ulid runId, int conditionIndex, String conditionType, boolean result, List<EvaluatedEntityState> evaluatedState)` (`core/event-model/src/main/java/com/homesynapse/event/AutomationConditionEvaluatedEvent.java:46–:53`) and no operands. The definition IS in hand at read time: the service holds the registry (`:132 private final AutomationRegistry automationRegistry;`), `AutomationDefinition.conditions()` is the index-aligned list (`AutomationDefinition.java:67`), and the run's `definitionHash` (on the `automation_triggered` payload, `AutomationTriggeredEvent.java:51–:59`) equals `DefinitionHashes.forDefinition(current)` (`DefinitionHashes.java:29`) exactly when the registry still holds the definition the run ran under. So: `conditions[].definition` = the structured rendering of `automation.conditions().get(conditionIndex)` when the hashes match and the index is in range; `null` otherwise (the definition changed, the automation is gone, or the index is out of range — never a guess). STRUCTURED, not prose: the dashboard's copy lives in its catalog (`web-ui/dashboard/MODULE_CONTEXT.md:51` — a hero sentence is a SPEC §7 row, never a literal from Core), and Core's own prose sentences are the wart the dashboard already refuses to render (`WhyNotView.tsx:173–:176`). The shape (§4).
- **DP-B2 — `FIRED_CONFIRMED` on evidence.** `completedVerdict` (`:356–:395`) returns `FIRED_CONFIRMED` with the sentence "last fired and confirmed at <evaluatedAt>" whenever no action is `UNCONFIRMED`/`FAILED` (`:376–:378`) and the zero-command marker branch did not fire (`:363 commandCount() == 0 && actionCount() > 0`). Two runs slip through with "confirmed" and no confirmation: a run whose commands are still in flight (`buildActions` → `deriveOutcome` yields `DISPATCHED` at `:921` when no `state_confirmed` or `command_result` classifies it) and a run with `actionCount() == 0`. And `evaluatedAt` is the TRIGGER instant (`derivedTriggerInstant`, `:286`, `:482–:486`), not a confirmation instant. The enum's own contract is "every device action confirmed" (`NonFiringExplanation.java:159–:165`). Rule after this WU: `FIRED_CONFIRMED` iff the run has ≥ 1 action whose outcome is `CONFIRMED` and NO action `UNCONFIRMED`, `FAILED` or `DISPATCHED`; its sentence names both instants: `"Automation '<name>' last fired at <evaluatedAt> and its device confirmed at <the latest ActionView.confirmedAt>; no non-firing was detected in the requested window."`; a `DISPATCHED` action present → `ACTED_BUT_UNCONFIRMED` with the existing `:380–:387` sentence ("fired, but a device did not confirm the requested change."); `commandCount() == 0` with any `actionCount` → the existing marker branch (`:363–:371`, `noCommandsIssued = TRUE`) — widen its guard to `commandCount() == 0` alone so a zero-action run also lands there (its sentence already says "no device actions defined").
- **IR-2 + IR-3.** `listRuns`'s visitor (`:148–:160`) returns `true` at `:149–:151` once `e.globalPosition() >= before` — positions ascend, so `false` ends the walk at the cursor (one line; T3). The five type-index readers (`:148`, `:422`, `:451`, `:464`, `:1009` — `sed -n '961p'` names them) share one `Predicate<EventEnvelope>` with two meanings: `scanType`'s `visitor` means continue/stop (`:964`, `:969`), `lastMatching`'s `match` means matches (`:986–:993`). After this WU `scanType` takes a named `Visitor` whose `visit(EventEnvelope)` returns `Verdict.CONTINUE | STOP` (a nested functional interface + enum in the service, no new file), and `lastMatching` keeps a `Predicate` named `matches` — the two meanings carry two types.

## §2 Files to read before starting (minimum read set — mandatory)
- `core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java` — the class javadoc `:70–:130`; `listRuns` `:140–:180`; `explainRun` `:183–:225` (the `CascadeView(null, depth)` at `:219–:220` stays); `completedVerdict` `:340–:395` whole (the javadoc explains v1.1.2 CORE-P2 and v1.1.4 DP-B2 — read it before changing a line); `buildTrigger` `:668–:691`; `firingValueOf` `:702–:711`; `buildConditions` `:715–:737`; `deriveOutcome` `:857–:922` (the five outcomes); `scanType` `:964–:978`; `lastMatching` `:986–:995`; `locateTriggered` `:1007–:1017`; `automationOf` `:1042`.
- `core/automation/src/main/java/com/homesynapse/automation/RunExplanation.java` — `ConditionView` `:140–:141`, `ObservedStateEntry` `:165`, `CascadeView` `:264` and its javadoc `:257–:259`.
- `core/automation/src/main/java/com/homesynapse/automation/NonFiringExplanation.java` — the record `:89–:102`; the verdict enum `:141–:167` (its FIRED_CONFIRMED contract is the rule you implement).
- `core/automation/src/main/java/com/homesynapse/automation/{ConditionDefinition,StateCondition,NumericCondition,TimeCondition,AndCondition,OrCondition,NotCondition,ZoneCondition,Selector}.java` — the sealed hierarchies (`ConditionDefinition.java:37–:39` permits seven; `Selector.java:39–:41` permits `DirectRefSelector(EntityId)`, `SlugSelector(String)`, `AreaSelector`, `LabelSelector`, `TypeSelector`, `SemanticTagSelector`, `CompoundSelector(List<Selector>)` — read each record's components; the renderer is exhaustive over both).
- `core/automation/src/main/java/com/homesynapse/automation/DefinitionHashes.java:29` (`static String forDefinition(AutomationDefinition)`) and `AutomationDefinition.java:56–:69`.
- `core/event-model/src/main/java/com/homesynapse/event/EventStore.java:141` (`EventPage readByTimeRange(Instant from, Instant to, long afterPosition, int maxCount)`), `EventPage.java:34–:42`; `core/event-model/src/testFixtures/java/com/homesynapse/event/test/InMemoryEventStore.java:225–:252` (the `[from, to)` rule; `validatePagination`); `core/persistence/src/main/java/com/homesynapse/persistence/SqliteEventStore.java:221–:226` (READ ONLY — the same rule in SQL).
- `platform/platform-api/src/main/java/com/homesynapse/platform/identity/Ulid.java:164–:167` (`extractTimestamp()`); `UlidFactory.java:79–:83`.
- `api/rest-api/src/main/java/com/homesynapse/api/rest/GetRunCausalChainEndpoint.java` — `toWire` `:114–:127` (`:124` "appended LAST — the LinkedHashMap order is the wire order"), `conditionsList` `:140–:160`, `cascadeMap` `:192–:197`.
- `core/automation/src/test/java/com/homesynapse/automation/StandardExplanationServiceTest.java` — the FIXED clock `:39–:60`; `seedRun`/`seedTriggeredMarker`; `explainRun_stopsAtFirstTriggeredMatch` `:744` (T3 of 114b — it asserts ONE `readByType` call for a first-page hit; after K2 that run's marker must be found through the hint or the fallback — read the test and decide which arm it now proves; a flipped assertion is named old → new); `CountingEventStore` `:1094–:1142` (extend it to count `readByTimeRange` too — T8 needs both counts). `NonFiringExplanationServiceTest.java` — the CONFIRMED fixture `:167–:180` (`ConfirmKind`); `RunEndpointsTest.java:262–:294` and `sampleExplanation` `:340–:356`.
- `_scratch/v75/Ir1PageCountMeasurementTest.java` (untracked; NOT in your tree — Nick's copy at `~/Desktop/Code/ClaudeFolder/_scratch/v75/`) — the seeding shape T8 inherits (the oldest run + N−2 bare markers + the newest run; the counting decorator); drop its wall-time lines and its file output.
- `core/automation/MODULE_CONTEXT.md` §Gotchas `:306–:353` (`:334` event-payload residency; `:353` the v1.1.4 firingValue rule) and `api/rest-api/MODULE_CONTEXT.md` §EXPLAIN-114a `:313–:332` (the table you extend).

## §3 Files to create or modify (the Files table governs)
| # | Path | A/M | What |
|---|---|---|---|
| 1 | `core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java` | M | K2 `locateTriggered` (the hint read + the counted fallback); K3 `buildConditions` gains the definition (through row 3); DP-B2 `completedVerdict`'s rule and sentence; IR-2 the cursor stop; IR-3 the `Visitor`/`Verdict` types and the five call sites; the class javadoc's scan sentence |
| 2 | `core/automation/src/main/java/com/homesynapse/automation/RunExplanation.java` | M | `ConditionView` gains a fifth component `ConditionDefinitionView definition` (nullable) + a 4-arg convenience ctor delegating `null` (the 114a pattern — keeps `:733` and the rest-api test's 4-arg sites compiling); the new nested record `ConditionDefinitionView` (§4) |
| 3 | `core/automation/src/main/java/com/homesynapse/automation/ConditionDefinitionRenderer.java` | A | package-private; `static ConditionDefinitionView render(ConditionDefinition)`; exhaustive over the two sealed hierarchies; no I/O, no clock (DP-2) |
| 4 | `api/rest-api/src/main/java/com/homesynapse/api/rest/GetRunCausalChainEndpoint.java` | M | `conditionsList` appends `"definition"` LAST (after `observedState`) — a nested `LinkedHashMap` in the §4 key order, or JSON `null`; map capacity 5 |
| 5 | `core/automation/src/test/java/com/homesynapse/automation/StandardExplanationServiceTest.java` | M | T1, T3, T4, T5–T7 (§7); `CountingEventStore` counts `readByTimeRange` beside `readByType` |
| 6 | `core/automation/src/test/java/com/homesynapse/automation/NonFiringExplanationServiceTest.java` | M | T2 (the DP-B2 rows) |
| 7 | `core/automation/src/test/java/com/homesynapse/automation/ByIdReadPageCountTest.java` | A | T8 — IR-1's instrument as the guard (DP-1) |
| 8 | `api/rest-api/src/test/java/com/homesynapse/api/rest/RunEndpointsTest.java` | M | the key-order test's condition list gains `"definition"` last; one new row asserting the nested key order and the present-null form (T9) |
| 9 | `core/automation/MODULE_CONTEXT.md` | M | §8 (a) |
| 10 | `api/rest-api/MODULE_CONTEXT.md` | M | §8 (b) |
**Census claim: 10 = 8 M + 2 A.** Folding row 3 into row 1 (DP-2 declared) is 9 = 8 M + 1 A, a declared deviation, not a fault.

## §4 Technical specification
### Settled decisions (not open questions)
- **SD-1 — K2's hint window and fallback.** `t = Instant.ofEpochMilli(runId.value().extractTimestamp().toEpochMilli())` (use the accessor's `Instant` directly); `from = t.minusSeconds(1)`, `to = t.plusSeconds(60)`. Page `eventStore.readByTimeRange(from, to, after, SCAN_BATCH)` ascending exactly as `scanType` pages the type index (`after = page.nextPosition()` while `hasMore`), testing `e.eventType().equals(EventTypes.AUTOMATION_TRIGGERED) && payload instanceof AutomationTriggeredEvent p && p.runId().equals(runId.value())`. On a miss: the existing full scan, unchanged. Why 60 s: the run id is minted at `StandardRunManager:257` before `publishTriggered` (`:684–:695`) on the same clock; the marker's instant is at or after `t`; a minute absorbs any publish latency the bus can carry; a miss beyond it is served correctly by the fallback. Why not a store change: `EventStore` is pinned at 6 methods (`EventStoreTest:28–:31`), `event_id` has no index and the run id is not an event id anyway (`V001__initial_event_store_schema.sql:57–:64`; V003 drops `idx_events_subject`); the correlation id is not the run id (`StandardRunManager:218` vs `:257`). The grounding table for the alternatives is the v75 b2 audit §3; none is this WU's.
- **SD-2 — K3's shape.** A new nested record in `RunExplanation`: `public record ConditionDefinitionView(String type, String selector, String attribute, String value, Double above, Double below, String after, String before, List<ConditionDefinitionView> children)` — `type` = the definition's simple class name (`"StateCondition"` … `"ZoneCondition"`, the same vocabulary `conditionType` already carries); `selector` = the selector rendered to ONE string — `DirectRefSelector` → the entity ULID text; `SlugSelector` → the slug; `AreaSelector`/`LabelSelector`/`TypeSelector`/`SemanticTagSelector` → `"<kind>:<value>"` with `kind` ∈ `area|label|type|tag` and `value` the record's one component (read the records — if one carries more than one component, join them with `/` and say so); `CompoundSelector` → its parts joined by `+`; `attribute`/`value` from `StateCondition`; `attribute`/`above`/`below` from `NumericCondition`; `after`/`before` from `TimeCondition`; `children` = the rendered list for `AndCondition`/`OrCondition`, a one-element list for `NotCondition`, an EMPTY list otherwise (never null — `List.of()`); `ZoneCondition` → `type` only. Components that do not apply are `null`. **On the wire** (row 4): `"definition": {"type","selector","attribute","value","above","below","after","before","children":[…]}` in that order, nested recursively, or `"definition": null`. Present in every condition object.
- **SD-3 — K3's guard.** In `buildConditions`, resolve once per run: `Optional<AutomationDefinition> def = automationRegistry.<the existing lookup automationOf/…>`; `boolean vouched = def.isPresent() && DefinitionHashes.forDefinition(def.get()).equals(triggerPayload.definitionHash())`. Per condition: `definition = vouched && p.conditionIndex() >= 0 && p.conditionIndex() < def.get().conditions().size() ? render(def.get().conditions().get(p.conditionIndex())) : null`. `buildConditions`'s signature grows by the triggered payload (or the two values) — a private method, your call, declared. `DefinitionHashes` is package-private static already (`:29`).
- **SD-4 — DP-B2's rule** (§1, the third bullet) — the marker branch's guard becomes `payload.commandCount() == 0` (the `actionCount() > 0` conjunct dropped; the sentence unchanged); the FIRED_CONFIRMED branch requires `actions` non-empty, every action's outcome ∈ {`CONFIRMED`, `SKIPPED`} and at least one `CONFIRMED`; `DISPATCHED` present ⇒ the ACTED_BUT_UNCONFIRMED branch at `:380–:387` (extend its `anyMatch` with `DISPATCHED`); the FIRED_CONFIRMED sentence names the two instants (the latest non-null `ActionView.confirmedAt` among the CONFIRMED actions, `Instant.toString()`), the rest of the sentence verbatim. Update the `completedVerdict` javadoc's last sentences to the new rule — a comment, not a claim.
- **SD-5 — IR-3's types.** Inside `StandardExplanationService`: `@FunctionalInterface private interface Visitor { Verdict visit(EventEnvelope e); }` and `private enum Verdict { CONTINUE, STOP }`; `scanType(String eventType, Visitor visitor)`; `lastMatching(String eventType, Predicate<EventEnvelope> matches)` keeps the predicate and wraps it in a visitor that always returns `CONTINUE`; the five sites read as verdicts. No behaviour change beyond IR-2's `STOP` at the cursor (T3 names the count that flips).
- **SD-6 — zero `module-info` change** (`grep -c 'requires\|exports' core/automation/src/main/java/module-info.java` unchanged; `api/rest-api` likewise): the renderer is package-private in an exported package's module, the view is a nested public record of an already-exported type, the endpoint already `requires com.homesynapse.automation`; no Jackson enters `core/automation` (the endpoint maps records into `LinkedHashMap`s as today). `module-info.java` verbatim, for the record:
```
// core/automation/src/main/java/module-info.java:16–:53 (comments elided; `sed -n '16,53p'`)
module com.homesynapse.automation {
    requires transitive com.homesynapse.platform;
    requires transitive com.homesynapse.event;
    requires transitive com.homesynapse.device;
    requires transitive com.homesynapse.state;
    requires com.homesynapse.value;
    requires transitive com.homesynapse.event.bus;
    requires org.slf4j;
    exports com.homesynapse.automation;
}
// api/rest-api/src/main/java/module-info.java:18–:43
module com.homesynapse.api.rest {
    requires transitive com.homesynapse.state;
    requires com.homesynapse.event.bus;
    requires com.homesynapse.automation;
    requires com.fasterxml.jackson.databind;
    requires io.javalin;
    requires org.slf4j;
    exports com.homesynapse.api.rest;
}
```
### Wire shapes after this WU (v1.1.5 — additive; everything not named is byte-identical to v1.1.4)
`GET /api/v1/runs/{runId}/causal-chain` (`RestFilters.java:366–:369`): each element of `conditions[]` = `{"expression","evaluated","result","observedState":[…],"definition": <SD-2 object> | null}`. `GET /api/v1/automations/{id}/non-firing`: no key change; the `explanation` string of the FIRED_CONFIRMED case reads as SD-4; a DISPATCHED-in-flight run now reports `ACTED_BUT_UNCONFIRMED` where v1.1.4 reported `FIRED_CONFIRMED` (the declared correction). `GET /api/v1/runs`: byte-unchanged (no per-row correlation read — DP-5 stands).
### Event types produced or consumed — none produced; consumed reads: `automation_triggered` (by time range, then by type), `automation_condition_evaluated`, `automation_completed`, the correlation read. **Configuration parameters — none. Error handling —** a renderer that meets an unknown sealed case cannot compile (exhaustive switch); a registry miss or hash mismatch yields `null`, never a throw; the hint read's `IllegalArgumentException` on `from >= to` cannot occur (`from < t < to` by construction — assert it in T1 anyway).

## §5 Locked decisions and invariants that apply
The read-API contract v1.1 (FROZEN base; additive amendments only — the freeze doc §D); keys appended LAST, present-null, `Instant.toString()` (rest-api MODULE_CONTEXT `:313–:332`); INV: the explanation service is read-only (`QUERY_SERVICE_READ_ONLY`, `REST_ENDPOINTS_NO_EVENT_PUBLISHING` — `app/homesynapse-app/src/test/java/com/homesynapse/app/HomeSynapseArchRules.java:266–:271`, `:306–:316`); `NO_DIRECT_TIME_ACCESS` (`:98–:110`) — production code takes its instants from the envelope or the injected clock, never `Instant.now()`; `NO_JACKSON_IN_DOMAIN_MODEL` (`:337–:348`) is not reached (no Jackson in automation). D4's path-domain rule: this lane owns `core/**` + `api/**`; FE-114 owns `web-ui/**`.

## §6 P2 consumer/pin survey (done at authoring; re-run the greps)
- `new RunExplanation.ConditionView(` — 2 sites: `StandardExplanationService.java:733` (production) and `RunEndpointsTest.java:345` (test) (`grep -rn 'new RunExplanation.ConditionView(' --include=*.java core api`); the 4-arg convenience ctor keeps both compiling; row 1 moves to the 5-arg form.
- `eventStore.readByType(` — 1 site (`:967`); `eventStore.readByTimeRange(` — 0 sites at HEAD in this class; `readByTimeRange` production consumers elsewhere: re-derive with `grep -rn 'readByTimeRange(' --include=*.java core api --exclude-dir=test`.
- `FIRED_CONFIRMED` consumers: `StandardExplanationService.java:389`; tests `NonFiringExplanationServiceTest` (`:167–:180` the CONFIRMED fixture) and `AutomationEndpointsTest` (through the `NonFiringExplanations` fixture); the dashboard maps the value to its unknown arm today (`WhyNotView.tsx:143`) — FE-114 adds the arm on its own lane.
- `EventStore` implementors (7) are untouched — no method is added (`EventStoreTest:28–:31` stays green by construction).
- ARCH-RULE-REACH: no module boundary crossed; no publishing added.

## §7 Test requirements (written first; sorted per format law #18)
| T | Test (class) | Sorting | What it pins |
|---|---|---|---|
| T1 | `locateTriggered_findsNewestRunByIdHint` (`StandardExplanationServiceTest`) | RED at HEAD (the counting store sees `readByTimeRange` 0 and `readByType` = ceil(N/500)); GREEN after | a ticking-clock seed of 2,001 markers (4+ pages) and the newest run; `explainRun(newest)` → `readByTimeRange` calls ≥ 1 and `readByType` calls = 0 |
| T2 | `completedVerdict_firedConfirmedRequiresConfirmation` (+ `_dispatchedIsActedButUnconfirmed`, `_zeroActionsIsNoCommands`) (`NonFiringExplanationServiceTest`) | RED at HEAD (HEAD returns FIRED_CONFIRMED for the DISPATCHED-only and the zero-action fixtures) | DISPATCHED-only → `ACTED_BUT_UNCONFIRMED`; zero actions → `ACTED_BUT_UNCONFIRMED` + `noCommandsIssued == TRUE`; the CONFIRMED fixture → `FIRED_CONFIRMED` with a sentence containing both `last fired at` and `confirmed at` and NOT `fired and confirmed at` |
| T3 | `listRuns_stopsAtCursor` (`StandardExplanationServiceTest`) | RED at HEAD (`reads(AUTOMATION_COMPLETED) == 2` on a 501-marker index with a cursor inside page 1); GREEN (`== 1`) | IR-2's early stop |
| T4 | `buildConditions_definitionVouchedByHash` (+ `_nullWhenDefinitionChanged`, `_nullWhenIndexOutOfRange`) | RED at HEAD (`ConditionView.definition()` does not exist / is null) | K3: a `StateCondition` renders `type/selector/attribute/value`; an `AndCondition` renders `children`; a registry definition whose hash differs → `null`; an index past the list → `null` |
| T5 | the existing `explainRun_stopsAtFirstTriggeredMatch` (`:744`) | preservation (may flip its counted arm — name old → new) | the hint path or the fallback finds a first-page run with the count you now assert |
| T6 | `RunEndpointsTest.causalChain_v114KeysLastInOrder_iso8601OrNull` (`:262–:294`) | preservation with ONE added key in the condition list's `containsExactly` | the root, trigger, action and cascade key lists UNCHANGED |
| T7 | the existing 232 automation + 156 rest-api tests | preservation | no assertion line edited except T5/T6's named flips |
| T8 | `ByIdReadPageCountTest` (row 7) | the IR-1 instrument as a guard: at HEAD prints/asserts pages = 2/40/100 (the fallback path under `Clock.fixed`), after K2 asserts ≤ 2 per N on the ticking-clock seed AND = 2/40/100 on the fixed-clock seed (the fallback still correct) | the number Nick measured stays a number |
| T9 | `RunEndpointsTest.conditionDefinition_v115NestedOrderAndPresentNull` | RED at HEAD | the nested key order of SD-2; a `null` definition serialises as JSON `null`, never absent |
Existing counts: automation 232 (`grep -c '@Test'` over the tracked classes at `6bd8508`; re-derive at your HEAD — `_scratch/v75/Ir1PageCountMeasurementTest.java` is not in your tree) and rest-api 156 (3 skip).

## §8 MODULE_CONTEXT.md update
(a) `core/automation/MODULE_CONTEXT.md` — a §EXPLAIN-114c paragraph after §EXPLAIN-114b: the hint read (SD-1: the window, the fallback, the counting proof), `ConditionDefinitionView` and the hash guard (SD-3), the DP-B2 rule (SD-4), the `Visitor`/`Verdict` types (SD-5); one GOTCHA line: "a `Clock.fixed` test seed defeats the by-id hint by construction — seed with a ticking clock to exercise it; the fallback is the fixed-clock case". (b) `api/rest-api/MODULE_CONTEXT.md` — the §EXPLAIN-114a table gains a v1.1.5 row: `conditions[].definition` · present-null · nested order · "JSON null when the registry no longer holds the run's definition (hash), the automation is gone, or the index is out of range".

## §9 What to watch out for
- The FIXED clock in `StandardExplanationServiceTest` (`:39–:60`) gives every seeded envelope ONE instant; T1/T8's hint case needs distinct instants (DP-1) — do not "fix" the hint by widening the window; the window is a design constant.
- `readByTimeRange` returns EVERY event type in the window: match on the type AND the payload class AND the run id (SD-1); a run's own chain sits in the same seconds.
- `AutomationTriggeredEvent.runId()` is a bare `Ulid` (the JPMS residency gotcha, MODULE_CONTEXT `:334`); compare `p.runId().equals(runId.value())` as `:1010` does.
- The DP-B2 marker branch's sentence already says "no device actions defined" — do not write a fourth sentence; widen the guard.
- `DefinitionHashes.forDefinition` hashes the CURRENT registry definition; the run's hash is on the triggered payload — compare strings, never re-hash the run.
- `ConditionDefinitionView.children` is `List.of()` when empty — the wire's `"children": []` is the honest "no children", and the dashboard's tri-state idiom (absent ≠ null ≠ value) reads an empty array as a value.
- Spotless: `spotlessCheck` is in the gate line; run `spotlessApply` if it reddens, and say so.
- The test-clock reminder in §0 binds T8: no `System.nanoTime` in `core/automation` tests (the `_scratch` instrument used it under the measurement exemption; the guard does not).

## §10 Coder pushback welcome
If the 60-s window or the `[from, to)` semantics are wrong at your HEAD (a store whose `event_time` is set differently for the triggered marker), say so with the line and take the smallest correct window. If `AutomationRegistry` has no lookup by `AutomationId` that `explainRun` can reach (check `automationOf` and the registry interface), name the accessor you use. If `ConditionDefinitionView`'s nine components read as too many for a record, a nested `Map` is NOT the alternative (typed views are the contract's habit) — say what you would do and do the record.

## §11 Out of scope
**K1 — `cascade.parentRunId`.** OUT. The engine never links runs: every run starts at `RunCausalChain.root()` (`RunInitiator.java:96–:101`; `RunCausalChain.extend` has zero production callers — `grep -rn '\.extend(' --include=*.java core/automation/src/main` = the `RunManager.java:66` javadoc only), and the causal chain breaks at the device: a `state_reported` is a ROOT (`ZclIngestionUnit.java:611 publisher.publishRoot(draft)`), the projection's `state_changed` chains to it, so a run triggered by a device's report after another run's command starts a NEW correlation with nothing naming the first run. A read-time bridge exists only for the confirmed case (`state_confirmed.reportEventId` → the ledger's correlation) and needs a type scan. This is an engine design decision (where cascade parentage is minted) — the hub's H10 D-v75-3 to Nick; `CascadeView(null, depth)` stays. **K4 — `forDuration` `firingValue`.** OUT, and re-classified: at `6bd8508` a `for_duration` trigger's expiry publishes `trigger_duration_expired` and validates (`StandardTriggerEvaluator.java:415–:455`) but NO run is initiated for it — `evaluateMatches` (`:166–:197`) starts the timer instead of matching (`:184–:190`), `triggerMatchesEvent` (`:482–:500`) has no expiry arm, `consumedEventType` (`StandardAutomationRegistry.java:174–:188`) maps no trigger to `trigger_duration_expired`, and the subscriber's only `TriggerDurationExpiredEvent` arm is the REPLAY timer bookkeeping (`AutomationEngineSubscriber.java:103`); no test asserts a run after expiry (`grep -rn 'expir' --include=*Test.java core/automation/src/test | grep -i 'run\|trigger'` = 0). The javadoc's "the trigger still fires" (`:441–:445`) is not implemented. That is a DEFECT candidate (register IR-22, MEASURE FIRST: one RED test in `DurationTimerTest` that advances the clock past expiry and asserts an `automation_triggered`/`initiateRun`), owned by its own WU (DUR-1) after Nick's word — not by a read-side lane. Also out: DISABLED-2 (the formal path, D-v73-1); IR-6 (untouched — no component lands on `NonFiringExplanation`); any `EventStore` change; any dashboard change (FE-114); the freeze doc's v1.1.5 stamp (the hub writes it at the landing).

## §12 Success criterion (binary) — four instruments, read and filed in §0
1. **The wire, byte-for-byte outside the one key:** `./gradlew :api:rest-api:test --offline` green; `git diff -U0 -- api/rest-api/src/test/java/com/homesynapse/api/rest/RunEndpointsTest.java | grep -E '^[-+].*containsExactly' ` shows exactly ONE changed `containsExactly` (the condition list) and T9's additions.
2. **The by-id read:** T8's after-counts (≤ 2 pages per N on the ticking seed) pasted in §0 beside IR-1's before-counts (2 / 40 / 100); `grep -c 'eventStore.readByTimeRange(' StandardExplanationService.java` = 1; `grep -c 'eventStore.readByType(' …` = 1.
3. **DP-B2:** `grep -c "fired and confirmed at" core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java` = 0; T2's three rows green.
4. **No assertion line changed** outside T5/T6's named flips: `git diff -U0 -- '*Test.java' | grep -E '^-\s*(assert|then|verify)' | wc -l` reported with the lines listed.
Done when: the census is the Files table (or the declared deviation); T1–T4 and T9 were RED at HEAD as the table says; T5–T7 disclosed; the gate line green in one round; the four instruments read as above; `git diff --stat` shows nothing outside the ten paths; the return is on disk at the named path with its `RETURNED` line.

## §13 Work unit completion (WUCP Phase 1)
Your §4 checklist: files touched with counts; the gates and their counts; the two MODULE_CONTEXT edits; the four instruments' readings; DP-1/DP-2 as taken; deviations by tag; the next WU named (ENERGY-READ — IR-15 first — after the hub's intake; DUR-1 on Nick's word).

## §14 The dispatch line (Nick pastes into a host-side Claude Code session in `~/Desktop/Code/ClaudeFolder/homesynapse-core`)
```
date -u first. Boot as the nexsys-coder skill. Baseline: `git log -1 --format=%h` is 6bd8508 and `git status --porcelain` shows nothing under core/, api/ or lifecycle/ (web-ui/dashboard may be dirty — a frontend lane; never touch it); stop and report otherwise. Execute nexsys-hivemind/context/instructions/2026-09-14_coder-lane_EXPLAIN-114c_by-id-read_condition-definition_DP-B2_coding-instruction.md exactly: read its §0 contract and §2 read-set first; adjudicate P1–P6 in your §0 card; take DP-1 (the ticking test clock) and DP-2 (the renderer's home) from the source and declare them. Order: T8 at HEAD first (the before-counts 2/40/100, pasted), then T1/T3/T4/T2/T9 red, then K2 (the hint read + the counted fallback), IR-2, IR-3, K3 (the view, the renderer, the endpoint's last key), DP-B2 (the rule, the sentence, the widened marker guard), then the two MODULE_CONTEXT edits. Write only the ten paths of its §3 Files table; never module-info, build files, event-model, persistence, web-ui or any payload; no System.nanoTime in tests; run `./gradlew :core:automation:test :api:rest-api:test :lifecycle:lifecycle:compileJava spotlessCheck --offline`; read the four §12 instruments and file their readings; stage nothing, commit nothing. Scripts over ~15 lines go through the Write tool and run by path. Write nexsys-hivemind/context/audits/<today's CT date>_EXPLAIN-114c_return.md (≤13 KB, §0 card first) and end it, in the file and printed, with: RETURNED <path> <bytes>.
```
