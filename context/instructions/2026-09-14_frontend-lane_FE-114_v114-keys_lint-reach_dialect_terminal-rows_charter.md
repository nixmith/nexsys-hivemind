<!--
file: context/instructions/2026-09-14_frontend-lane_FE-114_v114-keys_lint-reach_dialect_terminal-rows_charter.md
purpose: FE-114 — the dashboard folds the v1.1.4 read-API keys (the mirror is still v1.1.3 at `6bd8508`: `contract.ts:14`; none of `settledAt` · `confirmedAt` · `definitionKey` · `disabledAt` · `disabledReason` · `FIRED_CONFIRMED` is typed or rendered) and closes four register rows on the same surface: IR-12 (the literal lint's reach), IR-13 (the dialect sweep — US is the dialect of record), IR-14 (`pendingHint` reads the catalog), and the two unconsumed terminal rows (`explain.terminal.interrupted`, `.noSteps`) plus the five literal twins in `format.ts`. Authored by the v75 hub at beat 2 (Mon 2026-09-14; instrument 2026-09-14T19:28:03Z) on the source at `6bd8508` (an in-conversation grounding agent ran `npm ci` + `npm run verify` GREEN there — 27 files, 480 tests, bundle 70.3/100 KB, contract v1.1.3 — and the hub re-executed the cites beside every claim). Runs BESIDE EXPLAIN-114c (a Java lane on `core/**` + `api/**`); this lane owns `web-ui/dashboard/**` only (D4).
audience: the frontend lane (a FRESH Cowork conversation, nexsys-frontend) · the hub (intake) · Nick (the landing card)
state-type: lane charter
status: EXECUTED — returned 2026-09-14T23:47Z (`context/audits/2026-09-14_FE-114_return.md`, 12,705 B); audited ACCEPT v75 beat 3 (`context/audits/2026-09-15_v75-b3_114c-and-FE-114_intake_two-layer-audit.md` — the STOP-class proceed and the D3 copy accepted; the charter's `recognise` pin did not exist and a third version pin did, both the hub's); the landing on Nick's card (block 2); `frontend.yml` on the push is the gate of record. Was: DISPATCH-READY — v75 beat 2 (Mon 2026-09-14).
-->

# FE-114 — the v1.1.4 keys rendered, the lint's reach, the dialect, the terminal rows

## §0 The lane contract (read first; every line binds)
- `date -u` first. State your instrument limit: what your shell can run (`node`, `npm`, `npx vitest`, `npx eslint`) and what it cannot; with `node_modules` installed outside the mount the Linux shell runs the toolchain (HERO-1b's O3; the hub's agent ran `npm ci` cleanly at `6bd8508`: 330 packages, 0 vulnerabilities). CT = UTC−5, derived once. Every gate you cannot run is DEFERRED to Nick's desk by its exact command in your §0 card, never claimed.
- **The one deliverable:** `nexsys-hivemind/context/audits/<your CT date>_FE-114_return.md`, one file, §0 card first (≤3 KB: the census `N = a M + b A` with exact paths · the red-first table (row → RED-at-HEAD how → GREEN; preservation rows named) · P1–P6 adjudicated · the gates with counts or deferred by command · deviations by tag), then §1 per-file deltas, §2 the gates, §3 pushback and observations (the dead-key table lives here), §4 the WUCP Phase-1 checklist. **Cap ≤14 KB is a CEILING, not a target;** the §0 card is what the hub reads first; a shorter return with the same receipts is a better return. Your last line, in the file and printed to Nick: `RETURNED <path> <bytes>`.
- **Write-set:** `homesynapse-core/web-ui/dashboard/src/**` (the files §3 names plus the tests beside them), `web-ui/dashboard/scripts/contract-check.mjs` (row D0 only), `web-ui/dashboard/eslint.config.js` (row D4 only), `web-ui/dashboard/design/hero-v1/SPEC.md` (§7 rows in the hub's form; the §6 rows the keys retire; D5's spelling lines; nothing else), `web-ui/dashboard/design/hero-v1/states.html` (D5's spelling only), `web-ui/dashboard/MODULE_CONTEXT.md` (D9). Nothing under `core/**`, `api/**`, `lifecycle/**`; nothing outside `web-ui/dashboard/`. `git status --porcelain -- web-ui/dashboard` must be EMPTY when you start (core at `6bd8508` or later); `core/automation/**` and `api/rest-api/**` may be dirty while you work (EXPLAIN-114c) — never touch them. Never run `git add`, `git commit` or `git push`; Nick lands with one card.
- **Tests first, every row.** Each row's new assertions are RED at HEAD before the edit (the table says how), then GREEN; a row HEAD already satisfies is a preservation fixture (law 18), never claimed red. A flipped existing assertion is named with its old and new text — this lane has exactly FOUR named flips (D0's version pins ×2, D7's INTERRUPTED pin, D5's `recognise` pin); a fifth is a STOP (§7).
- **The honesty law and the tri-state idiom bind every new key** (`MODULE_CONTEXT.md:69–:70`): absent ≠ null ≠ value; a v1.1.4 hub serves every new key PRESENT (JSON `null` when it has nothing on record); a pre-v1.1.4 hub omits it; the two recorded fixtures carry none of these keys (`contract.ts:421`) and their bytes do not change (P6). A null renders its keyed sentence; nothing the wire did not carry is shown; "shown as recorded" is the register for anything Core asserts.
- **The wire is FROZEN v1.1.4** (the freeze doc; the emitters `api/rest-api/.../GetRunCausalChainEndpoint.java:125,:175,:176`, `GetNonFiringEndpoint.java:139–:141`, `ListAutomationsEndpoint.java:132`; the table `api/rest-api/MODULE_CONTEXT.md:313–:328`). v1.1.5 (`conditions[].definition`, EXPLAIN-114c) is NOT read by this lane — it lands after 114c's intake as FE-115.
- **Byte-identical on screen except where a row names a text change:** D1/D2/D3/D7 ADD sentences (new §7 rows); D5 changes ONE rendered string's spelling; D8 moves five sentences into the catalog byte-for-byte; nothing else on screen changes.
- **Predictions, pre-registered; adjudicate them first in your §0 card:** P1 — the census is §3's files exactly, test homes declared (a new test file is `A`). P2 — D4's widened lint, run at HEAD BEFORE any other row over the six hero files, reports ≥ 6 hits (the hub's list from the source at `6bd8508`: `CausalChain.tsx:79` `'Completed, nothing changed'` (Property) · `:366` `` `Ran ${command} on` `` / `'Ran an unrecorded command on'` · `Resource.tsx:31` `"Signing in…"` · `RunChainView.tsx:24` `← All runs` · `WhyNotView.tsx:101` `'Ran, but sent nothing'` · `:118` `'It did run'`) and 0 after D4 — report the exact hit list at HEAD, then the exact output after; the SAME rule over all non-test `src/` reports hundreds (`mock/scenarios.ts`, `mockData.ts`, `format.ts` lead) — the scope stays the six files, say the number. P3 — `npm run verify` GREEN (27 test files / 480 tests at HEAD + your rows; the bundle stays under 100 KB; `check:contract` reports `v1.1.4-…`). P4 — `git diff -U0 -- '*.test.ts*' | grep -E '^-\s*expect'` shows ONLY the four named flips. P5 — `grep -rniE 'recognise|behaviour|neighbour|colour|licence' web-ui/dashboard/src web-ui/dashboard/design web-ui/dashboard/*.html web-ui/dashboard/MODULE_CONTEXT.md | wc -l` at HEAD = the count you read (the hub counted ≈ 20 sites: `i18n.ts:151`; `DevicesView.tsx:97`; `DevicesView.honesty.test.tsx:74`; `v113-additive.test.ts:16`; `SPEC.md:92,:96,:110,:207,:311,:313,:323`; `states.html:10,:142,:509,:528,:543`; `MODULE_CONTEXT.md:25`) and after D5 = the quotation lines only (`MODULE_CONTEXT.md:25` quotes the HERO-1d return verbatim — a quotation keeps its bytes; name any other survivor and why). P6 — `md5sum src/lib/api/fixtures/*.ts` unchanged: `wire-2026-08-16-nonfiring.ts 896f861d…`, `wire-2026-08-20-events-404-headers.ts 6f502c7b…`, `wire-2026-08-20-never-triggered.ts 05b02fbc…`, `fixtures.stability.test.ts 8acb204f…` (`md5sum` before and after, pasted).

## §1 The read-set, in order (nothing older)
1. This charter whole. 2. `web-ui/dashboard/MODULE_CONTEXT.md` whole (81 lines; the gotchas `:47–:71`, the tri-state idiom `:69`, the catalog rule `:51`). 3. `src/lib/api/contract.ts` — `CONTRACT_VERSION` `:14`; `RunSummary` `:290–:304`; the trigger `:317–:325`; the action `:336–:362`; the chain root `:367–:385`; the non-firing shape `:388–:424` (`:421` the fixtures' lawful absence); `src/lib/api/shapes.ts` (the validators — the `if ('key' in o)` law); `src/lib/api/v113-additive.test.ts` (the shape your D0 test copies); `scripts/contract-check.mjs:15`; `contract.test.ts:35`. 4. `api/rest-api/MODULE_CONTEXT.md:313–:332` (READ ONLY — the seven keys, their null rules, `Instant.toString()`); the emitters named in §0 (READ ONLY). 5. `src/lib/i18n.ts` whole (186 keys; `:159` `.interrupted`, `:161` `.noSteps`, `:117` `.pending.color`, `:151` the `recognise` string, `:190` `whyNot.body.disabled`); `design/hero-v1/SPEC.md` §6 (`:116–:127` — rows EXPLAIN-5 `:121`, EXPLAIN-8 `:125`, EXPLAIN-9 `:126`) and §7 (`:130–:282`; the HERO-1d four-column rows from `:282`; `:276` the no-steps row). 6. `src/components/CausalChain.tsx` (`:79`, `:119–:144` the trigger line, `:201` the pending hint, `:237`, `:248–:251` the L2 outcome detail, `:301–:306` the cascade, `:366`, `terminalLine` `:402–:436`); `src/views/WhyNotView.tsx` (`:97` the `.replace(' at {time}', '')` twin hack, `:101`, `:107` the verdict switch, `:118`, `:143` the unknown arm, `:173–:176` the refused sentence); `src/lib/format.ts` (`:28` `NOT_RECORDED`, `:40–:43` `noReadingLine`, `:49` `CASCADE_PARENT_UNRECORDED`, `:215–:220` `pendingHint`, `:225–:230` `unconfirmableHint` — the template, `:387`, `:392–:393`); `src/lib/verdicts.ts` (`:357`, `:385–:393`, `:408`); `src/components/Resource.tsx:31`; `src/views/RunChainView.tsx:24`; `eslint.config.js:31–:48`. 7. `nexsys-hivemind/context/planning/improvement-register.md` rows IR-12, IR-13, IR-14 (verbatim; this lane retires them) and `nexsys-hivemind/context/audits/2026-09-13_HERO-1d_return.md` §3 (the blind-spot list; the two unconsumed rows).

## §2 The ground (source-cited by the hub at `6bd8508`; re-read, never trust)
- **The mirror is v1.1.3:** `contract.ts:14 export const CONTRACT_VERSION = 'v1.1.3-2026-09-06' as const;` pinned by `scripts/contract-check.mjs:15` and `contract.test.ts:35`; `grep -rn 'settledAt\|confirmedAt\|definitionKey\|disabledAt\|FIRED_CONFIRMED' web-ui/dashboard/src` = the `WhyNotView.tsx:84` comment only (`git grep -n … -- 'web-ui/dashboard/src'`).
- **What the v1.1.4 wire carries** (rest-api MODULE_CONTEXT `:313–:328`): actions `settledAt` (the classifying envelope's instant or `null`) and `confirmedAt` (the `state_confirmed` instant or `null`); the chain root's `definitionKey` (the run's definition hash); the non-firing read's `disabledAt`/`disabledReason` (the auto-disable marker's instant and reason, or `null`) and `definitionKey`; the automations list's `definitionKey`; the verdict value `FIRED_CONFIRMED` (a clean confirmed success — `NonFiringExplanation.java:159–:165`; 114c tightens its evidence rule on the Core side, not yours). All PRESENT-null on a v1.1.4 hub.
- **What renders today:** `subjectRef`, `matchedAt`, `firingValue`, `type` on the trigger line (`CausalChain.tsx:119–:144`); `resultOutcome` in the L2 detail (`:248–:251`); `settled` feeds `provisional` (`verdicts.ts:357`); `cascade.parentRunId` / `depth` (`:301–:306`); `outcome.*` in `terminalLine`. NOT typed, NOT rendered: `settledAt`, `confirmedAt`, `definitionKey`, `disabledAt`, `disabledReason`; `FIRED_CONFIRMED` falls to the unknown arm (`WhyNotView.tsx:143` "Recorded as … — a verdict this dashboard can't explain yet").
- **The action has no `issuedAt`** on the wire (the v1.1.4 action keys: `type, targetRef, command, params, outcome, reason, resultOutcome, settled, settledAt, confirmedAt` — `GetRunCausalChainEndpoint.java:162–:180`); the only earlier instant on the chain is `trigger.matchedAt`. EXPLAIN-9's "0.4 s after the command" is therefore rendered as the delta from the trigger ("after it fired"), never from a command instant the wire does not carry.
- **The literal lint** (`eslint.config.js:42–:45`) matches three bare words after a capital (`/[A-Z][a-z]+ [a-z]+ [a-z]+/`) on `JSXText`, template quasis and five attributes — blind to apostrophes, two-word runs, a capital beside `${`, and object-property literals (IR-12; the six hits in P2).
- **Dialect:** the one RENDERED UK string is `i18n.ts:151` (`recognise`; the US twin already exists at `format.ts:281`); comments at `DevicesView.tsx:97`, `DevicesView.honesty.test.tsx:74`, `v113-additive.test.ts:16` (`licence`); docs prose in `SPEC.md` and `states.html` (P5's list); `Cancelled` stays everywhere (the enum's mirror; accepted US usage).
- **`pendingHint`** (`format.ts:215–:220`) returns a literal byte-identical to `i18n.ts:117` `explain.action.pending.color` (md5 `8e77a106…` both); the consumer is `CausalChain.tsx:201`; the pins `i18n.test.ts:315`, `e5-semantics.test.ts:53`, `CausalChain.modes.test.tsx:103` (they pin the STRING and stay green by construction).
- **The terminal rows:** `i18n.ts:159` `"explain.terminal.interrupted": "Cut off before it finished."` is referenced only in a comment (`CausalChain.tsx:400`); INTERRUPTED renders `explain.terminal.status` with the label ("Interrupted.") at `:433–:435`, pinned by `CausalChain.terminal.test.tsx:114–:118` (`expect(line).toBe('Interrupted.'); expect(line).not.toBe(k('explain.terminal.interrupted'))`). `:161` `"explain.terminal.noSteps": "Done, recorded no steps."` has NO arm — a COMPLETED run with `actionCount` 0 renders `.completed`/`.completed.noTime` (`:428`); the SPEC row is `:276`.
- **The twins** (two homes for one sentence — IR-14's class): `format.ts:28` `NOT_RECORDED` = `i18n.ts:254` `explain.detail.notRecorded`; `:40–:43` `noReadingLine` = `:246` `explain.condition.noReading`; `:49` `CASCADE_PARENT_UNRECORDED` = `:237` `explain.cascade.parentUnrecorded`; `:387` `'An earlier automation'` = `:92` `explain.slot.automation.earlier`; `:392–:393` `NULL_NAME_NOTE` = `:235` `explain.nullName.note`; and `WhyNotView.tsx:97`'s `.replace(' at {time}', '')` = the `.noTime` twins at `:182`/`:185`. Nineteen catalog keys have zero non-test consumers (the hub's grep; the list is your §3 table).

## §3 The rows (in order; each small; the test first)
| Row | What | RED at HEAD how | Files |
|---|---|---|---|
| **D0** | The mirror to v1.1.4: `CONTRACT_VERSION = 'v1.1.4-2026-09-13'` (the freeze doc's amendment date) at `contract.ts:14`, `contract-check.mjs:15`, `contract.test.ts:35` (two named flips); the seven keys typed OPTIONAL with `null` allowed (`settledAt?`/`confirmedAt?` on the action; `definitionKey?` on the chain root, the automation summary and the non-firing shape; `disabledAt?`/`disabledReason?` on the non-firing shape); `'FIRED_CONFIRMED'` added to the verdict union; `shapes.ts` validators in the tri-state form (`if ('key' in o)` typed-or-null; absent passes; a present wrong type is `ContractError`) | a new `src/lib/api/v114-additive.test.ts` (copy `v113-additive.test.ts`'s shape): present-null passes, present-value passes, absent passes, present-wrong-type throws — RED at HEAD (the keys are unknown to the validators / the version pin fails) | `contract.ts`, `shapes.ts`, `contract.test.ts`, `v114-additive.test.ts` (A), `scripts/contract-check.mjs` |
| **D1** | EXPLAIN-9 (`SPEC.md:126`): the confirmed action's L2 detail gains the instant — §7 row `explain.action.detail.confirmedAt` = `Confirmed at {time}, {delta} after it fired.` and `explain.action.detail.confirmedAt.noDelta` = `Confirmed at {time}.`; `{time}` = `clockTimeWithDate(confirmedAt)`, `{delta}` = `confirmedAt − trigger.matchedAt` in the existing seconds format (the `secs` helper `terminalLine` uses); rendered only when `confirmedAt` is a value; `null`/absent → today's `explain.mode.confirmed.help` unchanged | `CausalChain.modes.test.tsx`: a confirmed action with `confirmedAt` renders the new sentence — RED (no key, no arm) | `i18n.ts`, `CausalChain.tsx`, `verdicts.ts` (the help slot), the tests, `SPEC.md` §7 |
| **D2** | EXPLAIN-8 (`SPEC.md:125`): the disabled body reads the wire — §7 row `whyNot.body.disabled.at` = `Turned off {when}{reason}. Turn it on in your automation settings to let it run.` with `{when}` = `timeAgo(disabledAt)` and `{reason}` = `` — {disabledReason}`` when present, else empty; `disabledAt` null/absent → today's `whyNot.body.disabled` (`:190`) unchanged | `WhyNotView.hero.test.tsx`: a DISABLED payload with `disabledAt` renders the new sentence; without it the old — RED | `i18n.ts`, `WhyNotView.tsx`, the test, `SPEC.md` §7 |
| **D3** | The `FIRED_CONFIRMED` arm (`WhyNotView.tsx:107` switch), placed AFTER the `noCommandsIssued === true` check so a sent-nothing run keeps its headline: §7 rows `whyNot.headline.firedConfirmed` = `It ran, and the record says the device confirmed it{time}.` / `.noTime` twin; `{time}` = `` at {time}`` from `lastEvaluation.at` when present; the run link from `lastRelevantRunId` as the other arms do; the pill `{ label: t('whyNot.pill.firedConfirmed'), tone: 'ok' }` = `Ran and confirmed` (a §7 row). "the record says" is the register: the claim is Core's, shown as recorded (the Q1 ruling) | `WhyNotView.hero.test.tsx`: a `FIRED_CONFIRMED` payload renders the new headline, not the unknown arm — RED | `i18n.ts`, `WhyNotView.tsx`, the test, `SPEC.md` §7 |
| **D4** | IR-12 — the lint's reach: the pattern widened to ≥ 2 words with apostrophes (`'` and `’`) and a capital adjacent to `${` (`TemplateElement` quasis: `…[A-Z][a-z’']+ ?$` / `^ ?[A-Z][a-z’']+…`), punctuation-tolerant (`[,;:—-]?` between words), plus a `Property > Literal` selector for object-property literals; the SAME six-file scope (`eslint.config.js:31–:38`); the six P2 hits keyed through §7 rows (`explain.terminal.completed.nothing.pill`, `explain.action.ran.on` / `.ran.unrecorded.on`, `ui.signingIn`, `ui.allRuns`, `whyNot.pill.sentNothing`, `whyNot.pill.didRun` — your key names may differ; the strings are byte-identical) | the lint at HEAD with the widened rule reports the hit list (P2) — the rule is the test; 0 after | `eslint.config.js`, `i18n.ts`, the six hero files as hit, `SPEC.md` §7 |
| **D5** | IR-13 — the dialect: `i18n.ts:151` `recognise` → `recognize` (a §7 TEXT-CHANGE row; the `i18n.test.ts` pin flips — the third named flip; `SPEC.md:207` and `MODULE_CONTEXT.md`'s catalog mirror follow); the three comments (`DevicesView.tsx:97`, `DevicesView.honesty.test.tsx:74`, `v113-additive.test.ts:16`); the docs prose lines in `SPEC.md` (`:92,:96,:110,:311,:313,:323`) and `states.html` (`:10,:142,:509,:528,:543`) — `colour` → `color`, `neighbours` → `neighbors`, `coloured` → `colored`; `Cancelled` untouched; `MODULE_CONTEXT.md:25` untouched (a quotation) | P5's grep before/after | the files named |
| **D6** | IR-14 — `pendingHint` returns `t('explain.action.pending.color')` (the `unconfirmableHint` template at `format.ts:225–:230`); the three string pins stay green by construction — say so | preservation (the bytes are equal; the test is the pins) | `format.ts` |
| **D7** | The terminal rows: INTERRUPTED → `heroCopy('explain.terminal.interrupted')` (the `:433–:435` arm keeps the open-vocabulary `.status` fallback for UNKNOWN statuses only); the fourth named flip: `CausalChain.terminal.test.tsx:114–:118` old `toBe('Interrupted.')` + `not.toBe(k(...))` → new `toBe(k('explain.terminal.interrupted'))`; COMPLETED with `actionCount === 0` and `outcome` present → `heroCopy('explain.terminal.noSteps')` (SPEC `:276`), placed before the `.completed` arms; the `genuinelyEmpty` note (`:69–:70`, `:282`) stays | `terminal.test`: the no-steps arm RED at HEAD (renders `.completed.noTime` today); the INTERRUPTED flip named | `CausalChain.tsx`, the test, `SPEC.md` §6/§7 amendment line |
| **D8** | The twins: `format.ts:28`, `:40–:43`, `:49`, `:387`, `:392–:393` read the catalog (`t(...)`) byte-for-byte; `WhyNotView.tsx:97`'s `.replace(' at {time}', '')` replaced by the `.noTime` keys (`:182`, `:185`) — byte-identical output; then the DEAD-KEY TABLE in your §3: every remaining zero-consumer key (the hub's list: `boot.startingBody`, `explain.headline.completed.notRecorded` (ruling-gated — keep), `whyNot.neverTriggered.title`, `explain.action.unconfirmed.title/.body`, `explain.action.pending.title/.body`, `explain.hub.autos.noRunsSinceLoad`, and any you find) with one line each: consume (where, this lane, if a row above already renders that surface) or RETIRE-PROPOSED (the hub rules; no deletion this lane) | preservation for the twins (the screen bytes are the pins); the table is filed, not tested | `format.ts`, `WhyNotView.tsx`, `format.test.ts` if a pin names a literal |
| **D9** | `MODULE_CONTEXT.md`: the FE-114 beat paragraph (the mirror at v1.1.4; the tri-state validators; the lint's widened reach and its scope sentence; the dialect sweep closed; the terminal arms; the twins folded); `SPEC.md` §7: every new key as a four-column row (`| key | string | where | FE-114 |`) + the amendment line; §6: rows EXPLAIN-8/EXPLAIN-9 marked LANDED (the key it waited for, now consumed) | — | `MODULE_CONTEXT.md`, `SPEC.md` |

## §4 What to watch out for
- A capture of a live v1.1.4 body does NOT exist in the corpus (H8-a reads the v1.1.3 keys on the wire Saturday); your D0 validators are tested on hand-built objects — say so; the hub's H8 live-wire rule (format law #22) banks the surface as MIRRORED, not VERIFIED, until a capture lands.
- `settled: true` beside `confirmedAt: null` is lawful (a FAILED or UNCONFIRMED action settles without a confirmation) — D1 renders nothing extra for it.
- `timeAgo`/`clockTimeWithDate` take ISO-8601 strings; the wire's instants are `Instant.toString()` (nanosecond-capable) — the existing helpers already parse them (the v1.1.3 keys); do not add a parser.
- The lint's `Property > Literal` selector will hit the pill objects' `tone: 'warn'` values only if your regex admits one-word literals — it must not (≥ 2 words); assert your own false-positive count at HEAD in P2.
- The `.noTime` twins: `WhyNotView.tsx:97` renders the SAME bytes today by string surgery — D8 keys it without changing a byte; the pin is the screen text.
- Keep `CONTRACT_VERSION`'s date the freeze doc's (`2026-09-13`); `check:contract` prints it — P3 reads it.
- Never widen the lint's file scope: the whole-`src/` count (hundreds, mock data) is reported, not fixed.

## §5 Out of scope
Any Core or API change (114c owns `core/**`, `api/**`); v1.1.5's `conditions[].definition` (FE-115, after 114c's intake); the marketing site; the design system; fixture captures (a capture is a rig act); deleting catalog keys (the dead-key table proposes; the hub rules); `Cancelled` → `Canceled`; `AppShell.tsx`'s status pills; any change to `src/lib/api/fixtures/**` (P6).

## §6 Success criterion (binary) — the instruments
1. `git status --porcelain -- web-ui/dashboard` = §3's files exactly (or the declared deviation); nothing outside the domain.
2. D4's widened lint: the hit list at HEAD pasted in §0 (≥ 6, P2); `npx eslint <the six files>` = 0 after; `npm run lint` green; the whole-`src/` count reported.
3. `npm run verify` GREEN with counts (or its parts deferred by command); `check:contract` prints `v1.1.4-2026-09-13`; `frontend.yml` GREEN on Nick's push.
4. P4: exactly the four named flips in `git diff -U0 -- '*.test.ts*' | grep -E '^-\s*expect'`.
5. P5's grep after D5 = the quotation survivors you name; P6's md5s unchanged.
6. `grep -c "recognise" web-ui/dashboard/src/lib/i18n.ts` = 0; `grep -c "'An earlier automation'" web-ui/dashboard/src/lib/format.ts` = 0; `grep -c "replace(' at {time}'" web-ui/dashboard/src/views/WhyNotView.tsx` = 0.
Done when all six read as stated and the return is on disk with its `RETURNED` line.

## §7 Escalation
STOP and file (do not guess) when: a fifth existing expectation would have to change; a v1.1.4 key's null rule as the rest-api table states it contradicts what `shapes.ts`'s idiom can express; the widened lint flags a sentence in a file outside the six; a §7 row's string cannot be byte-identical to the literal it replaces (D4, D8); `npm ci` cannot run and no deferred command can stand in. Say what you saw and the line.

## §8 The hub's audit rules (pre-filed)
The hub re-runs the widened lint on the returned tree, re-runs P5's grep, re-derives P6's md5s, reads `git diff -U0 -- '*.test.ts*'` for removed expectations (four, named), diffs the SPEC §7 rows against this charter's forms, and reads the D0 validators against the rest-api table's null rules line by line. A keyed sentence whose catalog text differs from its former literal by one byte is a FINDING; two are a correction before the landing. The dead-key table lands as register rows, not as chat.

## §9 The dispatch line (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected)
```
date -u first. You are the FE-114 frontend lane for NexSys/HomeSynapse — boot as the nexsys-frontend skill. Baseline: homesynapse-core main at 6bd8508 or later, and `git status --porcelain -- web-ui/dashboard` EMPTY (core/automation and api/rest-api may be dirty — a Java lane; never touch them). Read nexsys-hivemind/context/instructions/2026-09-14_frontend-lane_FE-114_v114-keys_lint-reach_dialect_terminal-rows_charter.md WHOLE, then its §1 read-set in order. Rows D0 → D1 → D2 → D3 → D4 → D5 → D6 → D7 → D8 → D9: D4's widened lint runs at HEAD FIRST and its hit list is your P2 baseline; every other row's test is RED at HEAD before its edit, then GREEN; exactly four named flips; every keyed sentence byte-identical to the literal it replaces; the tri-state idiom on every new key; nothing under src/lib/api/fixtures/ changes. Write only under web-ui/dashboard/. Never git add/commit/push. Write the return to nexsys-hivemind/context/audits/<today CT>_FE-114_return.md (≤14 KB; §0 card first; P1–P6 adjudicated; the lint's before and after lists; the dead-key table in §3; the last line `RETURNED <path> <bytes>`) and say that line to Nick.
```
