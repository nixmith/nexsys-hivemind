<!--
file: context/instructions/2026-09-14_strategy-lane_AMBITION-MAP_expansion-rows-with-instruments_charter.md
purpose: THE AMBITION MAP — a strategy/research lane that turns Nick's ambition brief (the v74 decision record §1: more devices, integrations and protocols; more complex, data-rich system and device designs; without overreaching the long-term strategy and the research to date) into ROWS the program can grade, dispatch and refute: every candidate expansion with what it buys the household and the company, what it asks of the substrate, its cost and risk, and THE INSTRUMENT THAT WOULD PROVE IT — a row with no instrument is not a candidate. Chartered by the v74 hub at beat 6 on D-v74-3 (`SCOPE: lane`, Nick's ruling of 2026-09-13 ~20:3x CT — its two sharpenings and one caution are this charter's §0 fences).
audience: the ambition-map lane (a fresh Cowork conversation; §7 is its dispatch line) · the hub (the intake; the October charters it informs) · Nick (pastes §7; rules the map's H10 rows)
state-type: lane charter (strategy/research; the read-only domain — no repo write but the return)
status: DISPATCH-READY — authored v74 beat 6 (Mon 2026-09-14 ~07:0x CT; instrument 2026-09-14T12:07:08Z); pastes today on the strategy slot (the web-ui slot freed at `6bd8508`; the cap of two holds).
-->

# THE AMBITION MAP — the expansion rows, each with its instrument

## §0 The lane contract (read first; every line binds)
- `date -u` first; CT = UTC−5, once. State your instrument limit (what you can read; you run no code and no device). **You write ONE file:** `nexsys-hivemind/context/research/<your CT date>_AMBITION-MAP_return.md` (≤14 KB; §0 card first; the last line `RETURNED <path> <bytes>`). Nothing else in any repo. Never `git add`/`commit`/`push`.
- **The fences (Nick's ruling, D-v74-3 — verbatim in `context/planning/2026-09-13_v74_decision-record.md` §1; binding):** (1) Do not re-derive what the rig has half-proven: the sleepy-device adopt path and the rejoin-after-power-cycle path are already instrumented — R-5 Part B's counterfactual window and the re-seen/adopted split — so those rows are marked **"measured at R-5B"** with their instrument named and no further bytes spent. Your work is the EXPANSION rows, **ranked by what the 72-hour run and the corpus need next, not by what is most interesting.** (2) **Candidate row one is pre-registered: the metering-plug class** — P-1's harness already drives a plug; the read side is the plug's `state_reported` power and energy attributes; the bench can verify a reading against a known load; a measured energy class enters the corpus with `confirmed|unconfirmed` verdicts as the register's field-language law demands. Presence/mmWave has its instrument named in the strategy (the four-metric benchmark) but needs a bench scenario before any adapter; air quality is read-only and low risk; whole-home energy asks the most of the substrate and reads as P4. (3) **The map INFORMS October's P2/P3-seam charters (Tier-2, the curated fleet, B-6's timing); NOTHING dispatches from it before R-5 Part B lands and the first rehearsal is on the calendar.** Your return recommends; it never orders.
- **The laws you inherit:** the north star — the harness enforces, the model only proposes; the Substrate Thesis's §11 precedence (source, governance artifacts and direct system knowledge win over the thesis); the language law — the deterministic floor is MISSING from the field, never "superior" (L2/L3 without L1 unsound; L1 without L2 insufficient); the sensing four-qualifier law (every sensing sentence carries hardware · environment count · obstruction definition · training regime, or it does not leave the register); no Matter before P3 (the plan of record §8 row 7); gate sovereignty (nothing moves pre-freeze code; every build-out charters post-gate); no external claim, no brand name (`{{NAME}}` is the token; no candidate is graded); no "we" in any sentence meant for outside.
- **Predictions, pre-registered; adjudicate them first in your §0:** P1 — the metering-plug row ranks first on the 72-hour-run criterion with a bench instrument that exists today (P-1's harness in `--dry-run`, the `plug:` grammar of R-5 Part A). P2 — at least three candidate rows FAIL the fence test and are listed as rejected with the clause that rejects them (a map with no rejections did not test). P3 — every admitted row's instrument names a file or a scenario that exists at HEAD, or a bench scenario the row itself specifies in one line. P4 — at least one cite in this charter is wrong at your HEAD; file the corrected line.

## §1 The read-set, in order (by range where the file is long; the strategy layer WHOLE)
1. This charter whole. 2. `context/planning/2026-09-13_v74_decision-record.md` §1 (the brief and the ruling, verbatim). 3. `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` whole. 4. `context/strategy/Substrate_Thesis_v0.md` whole (its §11 precedence law governs). 5. `context/strategy/Six_Battlefields_MVP_Strategy.md`. 6. `context/strategy/Revenue_Model_and_Licensing_Strategy.md`. 7. `context/strategy/2026-08-27_company-and-brand-build_strategy-of-record.md`. 8. `context/strategy/claim-register.md` §0 (the field-language preamble) and §1 (C-001..C-003 — what is verified on hardware today). 9. `context/planning/2026-09-13_v73_PROGRAM-PLAN_after-114b_two-weeks-and-October.md` §2–§5 and §8 (the gate; the October–November calendar; the fences). 10. `context/planning/improvement-register.md` (the rows on the code that exist). 11. The findings cards: `context/audits/2026-09-12_R-4c_measurement-only_operator-record.md` items 1–14 (lines 872–937) and `context/audits/2026-09-04_R-4b_re-rep_operator-record.md` §9. 12. `context/instructions/2026-09-13_R-5_fleet-floor-rebaseline_P-1-fold_and-the-four-open-measurements_charter.md` whole (Part B is the "measured at R-5B" instrument). 13. `nexsys-bench/docs/2026-09-13_P-1_power-harness_design.md` (the harness; the metering plug's home). 14. `homesynapse-core-docs/design/08-zigbee-adapter.md` §1–§3 and `design/02-device-model-and-capability-system.md` §1–§3 (what the substrate's device model already admits); `homesynapse-core/integration/integration-zigbee/MODULE_CONTEXT.md` and `homesynapse-core/core/device-model/MODULE_CONTEXT.md` (the gotchas). 15. `context/research/2026-08-04_physics-aware-core_strategic-seed_charter-input.md` (an INPUT, not adopted strategy). Nothing older unless a file sends you there.

## §2 The row schema (one row per candidate; the table is the deliverable)
`| id | the expansion (one clause) | class | buys the household | buys the company | asks of the substrate | cost | risk | THE INSTRUMENT | the gate it waits on | the fence check | rank |` — class ∈ {protocol depth, protocol breadth, integration, device class, system/device design}; "asks of the substrate" names the event log, the projection, the confirmation leg, the device model or the harness, and whether a new event type or capability is needed; cost in lane-days (desk) and rig sittings; risk as the one thing most likely to make the row false; THE INSTRUMENT is a bench scenario (name it in one line if new), a rig packet block, or a corpus datum — a row without one is written under **§ Rejected — no instrument**, never in the table; the gate is R-5B / the first rehearsal / the 72-hour run / the opinion / P3; the fence check names the clause of §0 the row passed or failed; rank is by what the 72-hour run and the corpus need next.

## §3 The pre-registered rows (fill them; do not re-argue them)
- **AM-1 — the metering-plug class (row one).** Read side: the plug's `state_reported` power (W) and energy (Wh/kWh) attributes on the substrate's existing state path; the instrument: a bench scenario that drives P-1's harness plug against a known load and asserts the reported power inside a tolerance band (`confirmed`) or outside it (`unconfirmed`) — the same verdict vocabulary as the claim register; the gate: `HARNESS-PLUG:` (the S31 label read at H8-a) and R-5B. Cost, risk, what it asks of the substrate — yours to fill from the device model and the harness design.
- **AM-2 — "measured at R-5B":** the sleepy-device adopt path (the ZDO second surface, C-003) and rejoin-after-power-cycle — their instruments are R-5 Part B's counterfactual window and the re-seen/adopted split. One line each, marked; no further bytes.
- **AM-3 — presence/mmWave:** the instrument is the strategy's four-metric benchmark; the row's gate is a bench scenario BEFORE any adapter; the sensing four-qualifier law binds every sentence.
- **AM-4 — air quality (read-only sensors):** low risk; the instrument is a corpus datum (a reported value against a reference); what it buys is the question.
- **AM-5 — whole-home energy:** asks the most of the substrate; P4; say what it would need (a new event type? a meter integration?) and why it waits.
- Then YOUR rows: protocol depth (what the Zigbee adapter still lacks for the fleet the rig has — the sleepy/router/battery classes, groups, binding, OTA); protocol breadth (the READ side of Thread/Matter after P3, Z-Wave, BLE, local-API Wi-Fi devices, the sub-GHz classes); integrations (the local-first ones the north star admits); device classes that capture more data (contact/vibration, water/leak, door/lock state, light-level, temperature/humidity arrays); system/device designs (the data-rich device of Nick's brief — say what it would sense, what event it would emit, and the instrument that proves the data is worth the log). Each admitted with an instrument or rejected under the clause.

## §4 The overreach test (run it row by row; list the failures)
A row fails when: it needs a claim the register cannot yet carry (C-001..C-003 are the whole verified surface); it restates the enforcement position as "superior" or "no one else confirms"; it is a sensing sentence without the four qualifiers; it moves pre-gate code; it needs Matter before P3; it needs a rig sitting the calendar does not have before the 72-hour run; it has no instrument; it is interesting rather than needed by the run or the corpus. A failed row is kept, with its clause — the rejections are half the map's value.

## §5 The deliverable (≤14 KB; §0 first)
§0 card (≤3 KB): the census of rows (admitted / rejected / measured-at-R-5B); the top five ranked with their instruments and gates in one line each; P1–P4 adjudicated; what you could not read. §1 the table (§2's schema). §2 the rejected rows with clauses. §3 **the recommended October–December sequence as H10 rows** — each: the row · cost · risk · what it buys · the recommendation · refutable-by · blocking yes/no — no more than five; the first must be executable the week after R-5B lands. §4 what the substrate must grow first (the one or two seams the admitted rows share). §5 pushback on this charter (P4; anything the strategy layer contradicts). Last line: `RETURNED <path> <bytes>`.

## §6 What to watch out for
Chat is not the deliverable — the file is. No code, no config, no adapter sketch beyond one line per row. No brand name; no external sentence. The Substrate Thesis loses to source and to the governance artifacts where they differ — say so when it happens. The physics-aware seed is an input; the map may decline it. Do not rank by interest. Do not schedule anything before R-5B and the first rehearsal; the map informs, the hub charters, Nick rules.

## §7 The dispatch line (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected)
```
date -u first. You are the AMBITION-MAP strategy lane for NexSys/HomeSynapse — boot as the nexsys-project-manager skill's reader (read; you are not the hub and you dispatch nothing). Read nexsys-hivemind/context/instructions/2026-09-14_strategy-lane_AMBITION-MAP_expansion-rows-with-instruments_charter.md WHOLE, then its §1 read-set in order — the strategy layer whole, the rest by range. Fill the §3 rows and add your own under the §2 schema; run the §4 overreach test on every row and keep the failures with their clause; a row with no instrument never enters the table. Rank by what the 72-hour run and the corpus need next. Write ONE file, nexsys-hivemind/context/research/<today CT>_AMBITION-MAP_return.md (≤14 KB; §0 card first; P1–P4 adjudicated; the table; the rejections; the October–December sequence as at most five H10 rows; the last line `RETURNED <path> <bytes>`). Never git add/commit/push. Say the RETURNED line to Nick.
```
