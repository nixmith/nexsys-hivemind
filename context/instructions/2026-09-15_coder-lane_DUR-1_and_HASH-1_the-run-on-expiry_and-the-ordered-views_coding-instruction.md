<!--
file: context/instructions/2026-09-15_coder-lane_DUR-1_and_HASH-1_the-run-on-expiry_and-the-ordered-views_coding-instruction.md
purpose: DUR-1 + HASH-1 in ONE coder session, on MEASURE-1's numbers: (DUR-1) a `for_duration` trigger's expiry initiates the run — the redelivered `trigger_duration_expired` envelope matches the trigger that armed it (IR-22, a DEFECT: today nothing after `expire()` reaches an initiator and the expired event matches no trigger — T1 RED at `3d40b5f`); (HASH-1) the eight definition records that store a salted `Set.copyOf`/`Map.copyOf` store an ORDERED immutable view instead, so `DefinitionHashes` is stable across JVM launches (IR-23, a DEFECT on the frozen `definitionKey` — HASH-0 measured two hashes per source across eight launches; D-v75-5 shape (a)); every single-element hash UNCHANGED, proven. Tests first: MEASURE-1's two red probes become the green fixtures, plus a preservation probe.
audience: the Coder (host-side Claude Code, nexsys-coder) · the hub (intake) · Nick (the landing card)
state-type: coding instruction
status: EXECUTED — returned 2026-09-16T00:06Z (`context/audits/2026-09-15_DUR-1_HASH-1_return.md`, 7,074 B); audited ACCEPT v75 beat 6 (`context/audits/2026-09-17_v75-b6_DUR1-HASH1_DEVICE-SET_intakes_activate-recut_audit.md` §1): T1/T1b/T1c and T3 red → green, T4 preserved, the loop one hash per definition (re-tallied; Nick's clean build read); [REVIEW] R1 the `EventTrigger` carve-out ACCEPTED; the landing on the b6 card (14 files, one push); CI on the push is the gate of record. Was: DISPATCH-READY — v75 beat 5, through the premise gate; dispatched on `HASH1: a` by silence.
-->

# DUR-1 + HASH-1 — the run on expiry; the ordered views

## §0 The lane contract (read first; every line binds)
- **Baseline:** core `3d40b5f` with MEASURE-1's uncommitted tree on top — `git status --porcelain` reads EXACTLY ` M core/automation/src/test/java/com/homesynapse/automation/DurationTimerTest.java` and `?? core/automation/src/test/java/com/homesynapse/automation/DefinitionHashOrderProbeTest.java`; anything else → stop and report. `date -u` first; CT = UTC−5.
- **Return path:** `nexsys-hivemind/context/audits/<CT-date>_DUR-1_HASH-1_return.md`, one file, ≤7 KB, §0 card first (≤2.5 KB): the census · the three instruments (§12) as measured values · P1–P5 adjudicated · deviations by tag · `RETURNED <path> <bytes>` as the last line, printed too.
- **Write-set (the Files table §3 governs; nothing else):** `StandardTriggerEvaluator.java` · `StandardAutomationRegistry.java` · the eight records · `core/automation/MODULE_CONTEXT.md` · the four test classes named. NO rest-api change, NO event-model change, NO new event type, NO `module-info` change, NO wire change (the read side is untouched — `firingValue` for a forDuration run stays null by K1; K4 is a later docket row).
- **Build discipline:** the gate line, verbatim, green in ONE round before the return: `./gradlew :core:automation:test :api:rest-api:test :lifecycle:lifecycle:compileJava spotlessCheck --offline`. HASH-1's own loop (§7 T3) runs by path: `for i in 1 2 3 4 5 6 7 8; do ./gradlew :core:automation:cleanTest :core:automation:test --tests '*DefinitionHashOrderProbeTest*' --offline -i 2>&1 | grep -E '^HASH0 |    HASH0 |^HASH1 |    HASH1 '; done | tee ../_scratch/v75/2026-09-15_HASH-1_eight-jvms.txt`. Scripts over ~15 lines are written with the Write tool and run by path.
- **Tests first, honestly sorted (format law #18):** T1 (`forDurationExpiry_initiatesARun`, MEASURE-1's) is RED at the baseline and goes GREEN — the DUR-1 fixture. T2 (the probe) stays a probe; T3 (`DefinitionHashStabilityTest`, new) is RED-at-baseline by construction (a golden-hash equality across two insertion orders that the salt breaks) and goes GREEN. T4 (`DefinitionHashStabilityTest.singleElementHashes_preserved`, in the same new class as T3) is a PRESERVATION fixture: it pins the hashes of single-element definitions to values you record at the BASELINE before touching a record (§7). Say which is which in §0.
- **Predictions, pre-registered; adjudicate them first in your §0:** P1 — the census is the Files table exactly (14 = 12 M + 2 A: the two MEASURE-1 test files count as M from the baseline's view of `3d40b5f` — state the porcelain as it reads). P2 — T1 goes GREEN with the DUR-1 change of §4 row 1 alone (the registry index arm + the evaluator's expired-payload arm); `initiateRun` is called exactly once, its `triggeringEvent().eventType()` is `trigger_duration_expired`, its `matchedTriggers` is `[0]`. P3 — T3 goes GREEN with the eight one-line constructor changes and NO change to `DefinitionHashes`; HASH-1's loop prints ONE hash per definition across eight launches. P4 — T4 holds: every single-element hash recorded at the baseline is byte-identical after the change (the ordered views render in the same `toString()` format). P5 — no cite in the table below has moved (HEAD is the pinned sha).
- **THE PREMISE TABLE (the gate, applied; every row grepped at `3d40b5f` by the hub; a mis-filed cite is corrected in your §0, never silently):** `StandardTriggerEvaluator.evaluateMatches` `:166–:197` (`processCancellations` `:170`; the candidate loop `:174–:190`; `triggerMatchesEvent(trigger, event, automation.automationId())` `:181`; `forDurationOf` `:184`; `maybeStartDurationTimer(automation, index, trigger, forDuration, event)` `:186`; the immediate-match add `:188–:189`) · `expire(TimerKey)` `:415–:431` (removes the timer, interrupts the VT, `publishExpired` `:427`, `validateAtExpiry` `:429`) · `pollExpirations()` `:256` (package-private; the test driver — its due timers reach `expire`) · `rebuildTimer` `:278–:300`'s downtime arm `:295–:296` (the THIRD publish+validate site — a timer that expired during downtime fires at the REPLAY→LIVE boundary; the same redelivered envelope path serves it, so DUR-1 needs no change there — say so after reading it) · `publishExpired` `:433–:439` (payload `new TriggerDurationExpiredEvent(automationId, triggerIndex, triggerId, startingEventId)`; correlation = the timer's; causation = `startingEventId`) · `validateAtExpiry` `:446–:455` · `triggerMatchesEvent` `:482–:500` — a `switch` over the 12 trigger permits, NO arm for an expired payload · `forDurationOf` `:583–:587` (5 permits carry it) · `record TriggerMatch(AutomationId, List<Integer>)` `:645` · `StandardAutomationRegistry.Snapshot.build` `:122–:157`: the per-trigger index `String eventType = consumedEventType(trigger)` `:131`, null → skipped `:132–:134`, `index.computeIfAbsent(eventType, …).add(definition)` `:135–:136`; `candidatesForEventType` `:85–:86`; `consumedEventType` `:174–:188` (StateChange/State/NumericThreshold → `STATE_CHANGED`; Availability/Reachability → `AVAILABILITY_CHANGED`; EventTrigger → its type; Manual → `AUTOMATION_INVOKED`; Calendar/Webhook/Time/Sun/Presence → null) — **in the registry class, not the evaluator (MEASURE-1's P4 correction)** · `AutomationEngineSubscriber.onEvent` `:57–:72` (`accumulateReplayTimer` in REPLAY `:59–:61`; `evaluator.evaluateMatches(event)` `:65` in both modes; `if (!replay) runInitiator.initiateRuns(matches, event)` `:69–:71`) · the REPLAY expired arm `:103–:105` (bookkeeping only) · `RunInitiator.initiateRuns(matches, triggeringEvent)` `:81–:101` (`runManager.initiateRun(automation, triggeringEvent, matchedTriggers, resolvedTargets, RunCausalChain.root())` `:96–:101`) · `EventTypes.TRIGGER_DURATION_EXPIRED = "trigger_duration_expired"` (`core/event-model/…/EventTypes.java:152`) · `record TriggerDurationExpiredEvent(AutomationId automationId, int triggerIndex, String triggerId, EventId startingEventId)` `:29–:34` · `DefinitionHashes.forDefinition` `:29–:31` = `sha256Hex(definition.toString())`, `forTrigger` `:35` · the eight salted records: `AreaSelector.java:47` · `LabelSelector.java:46` · `TypeSelector.java:48` · `SemanticTagSelector.java:57` (each `includedRoles = Set.copyOf(includedRoles)`) · `CommandAction.java:51` `parameters = Map.copyOf(parameters)` (record `:34–:39`: `Selector target, String commandName, Map<String, Object> parameters, UnavailablePolicy onUnavailable`) · `EmitEventAction.java:37` `payload = Map.copyOf(payload)` · `EventTrigger.java:43` `payloadFilters = Map.copyOf(payloadFilters)` · `WebhookTrigger.java:55` `allowedMethods = Set.copyOf(allowedMethods)` · `AutomationDefinition.java:85–:87` `List.copyOf` ×3 (ordered — NOT a source) · `AggregateValue.java:59` `entities = Set.copyOf(entities)` — a runtime value, not a definition component (confirm: no path from `AutomationDefinition.toString()` reaches it; say so in §0) · MEASURE-1's measured facts: T1 `Expected size: 1 but was: 0`; HASH-0 A `[DIAGNOSTIC, PRIMARY]` ×5 / `[PRIMARY, DIAGNOSTIC]` ×3 (2 hashes), B `{brightness=50, transition=2}` ×5 / `{transition=2, brightness=50}` ×3 (2 hashes), the two salts flipping independently (passes 4 and 6).

## §1 What this implements
Two defects on the engine and the frozen contract, each with a measured RED and a fixture that turns GREEN. **DUR-1:** a `for_duration` trigger fires — the run it was always documented to start (`MODULE_CONTEXT.md:29` "the trigger still fires") is initiated when the engine's own `trigger_duration_expired` event comes back through the subscriber in LIVE, with that envelope as the run's triggering event (replay-safe: `evaluateMatches` matches in both modes, initiation stays LIVE-only). **HASH-1:** `DefinitionHashes` becomes stable across launches because the collections a definition's `toString()` renders are ordered at the record — the salt is removed where it lives; `DefinitionHashes` itself is untouched, and every hash of a definition whose sets and maps hold at most one element is byte-identical before and after (the frozen `definitionKey` of every run in every log today is preserved).

## §2 Files to read before starting (minimum read set — mandatory)
- `core/automation/MODULE_CONTEXT.md` — `:15` (M7.4b — the trigger→run seam; "a for_duration trigger starts a timer and is NOT an immediate match"), `:29` (114a — the forDuration run's triggering event), `:33` (114c — the hash guard), `:137–:138` (the trigger permits; `forDuration` on 5).
- `StandardTriggerEvaluator.java` `:160–:200`, `:405–:455`, `:478–:505`, `:583–:590`, `:645` · `StandardAutomationRegistry.java` `:80–:90`, `:118–:190` · `AutomationEngineSubscriber.java` `:25–:110` · `RunInitiator.java` `:75–:105`.
- The eight records, whole (each < 70 lines) · `DefinitionHashes.java` whole · `AutomationDefinition.java` `:56–:90`.
- `DurationTimerTest.java` whole (7 `@Test`; T1 is yours to turn green — do not weaken it) · `DefinitionHashOrderProbeTest.java` whole (MEASURE-1's; the fixture builders you reuse) · `AutomationTestSupport.java` `:290–:330` (`RecordingRunManager`, `InitiateCall`).
- `nexsys-hivemind/context/audits/2026-09-15_MEASURE-1_return.md` (the numbers; O1 and O2).

## §3 Files to create or modify (the Files table governs)
| # | Path | M/A | What |
|---|---|---|---|
| 1 | `core/automation/src/main/java/com/homesynapse/automation/StandardAutomationRegistry.java` | M | DUR-1: a for_duration trigger is ALSO indexed under `TRIGGER_DURATION_EXPIRED` (§4 row 1a) |
| 2 | `core/automation/src/main/java/com/homesynapse/automation/StandardTriggerEvaluator.java` | M | DUR-1: the expired-payload arm in `evaluateMatches` (§4 row 1b) |
| 3–6 | `AreaSelector.java` · `LabelSelector.java` · `TypeSelector.java` · `SemanticTagSelector.java` | M | HASH-1: `includedRoles` → an ordered immutable `EnumSet` view (§4 row 2) |
| 7–9 | `CommandAction.java` · `EmitEventAction.java` · `EventTrigger.java` | M | HASH-1: the map → an ordered immutable `TreeMap` view (§4 row 2) |
| 10 | `WebhookTrigger.java` | M | HASH-1: `allowedMethods` → an ordered immutable `TreeSet` view (§4 row 2) |
| 11 | `core/automation/MODULE_CONTEXT.md` | M | the DUR-1 and HASH-1 status paragraphs (§8) |
| 12 | `core/automation/src/test/java/com/homesynapse/automation/DurationTimerTest.java` | M (from `3d40b5f`; MEASURE-1's T1 already on the tree) | T1 unchanged in its assertion; + T1b the triggering-event pins (§7) |
| 13 | `core/automation/src/test/java/com/homesynapse/automation/DefinitionHashOrderProbeTest.java` | M (MEASURE-1's A, on the tree) | the probe stays (prints `HASH1` beside `HASH0`), the System.out lines retained ONLY under the `-i` grep contract — or fold into T3 and delete the probe: say which, the hub accepts either (I2 of MEASURE-1) |
| 14 | `core/automation/src/test/java/com/homesynapse/automation/DefinitionHashStabilityTest.java` | A | T3 the golden-hash equality across insertion orders, both sources; T4 the preservation pins (§7) |
Census: **14 = 12 M + 2 A** against `3d40b5f` (the probe and the stability test are the two A; 13 = 12 M + 1 A if you delete the probe — declare it).

## §4 Technical specification
- **Row 1a — the registry index (DUR-1).** In `Snapshot.build` `:129–:137`, after the `consumedEventType` bucket is added, add the definition ALSO to the `EventTypes.TRIGGER_DURATION_EXPIRED` bucket when the trigger carries a `forDuration` (the same five permits `forDurationOf` knows: `StateChangeTrigger`/`StateTrigger`/`NumericThresholdTrigger`/`AvailabilityTrigger`/`ReachabilityTrigger` with a non-null `forDuration()`); `dedup` `:159` already collapses a definition indexed twice into one bucket entry. No public API change; `candidatesForEventType("trigger_duration_expired")` now returns the for_duration automations in execution order.
- **Row 1b — the evaluator (DUR-1).** In `evaluateMatches`' candidate loop, BEFORE `triggerMatchesEvent` `:181`: if `event.payload() instanceof TriggerDurationExpiredEvent expired`, then the trigger at `index` matches iff `expired.automationId().equals(automation.automationId()) && expired.triggerIndex() == index && forDurationOf(trigger) != null` — record it as an IMMEDIATE match (`matched.computeIfAbsent(...).add(index)`) and `continue`; an expired payload never reaches `maybeStartDurationTimer` and never starts a timer. `processCancellations` `:170` is unaffected (it keys on state events). REPLAY: the arm runs in both modes (matching has no side effect); `initiateRuns` stays behind `if (!replay)` `:69` — a replayed expiry initiates nothing (D2 holds; the run itself is in the log). The run's triggering event is the expired ENVELOPE (correlation = the timer's; causation = `startingEventId`), so the run's `automation_triggered` inherits the timer's correlation — the chain from the state event through `trigger_duration_started` → `expired` → the run is one correlation (state it in MODULE_CONTEXT). `validateAtExpiry`'s divergence event is unchanged and still precedes nothing that blocks the run ("the trigger still fires" — `:444`).
- **Row 2 — the ordered views (HASH-1, shape (a) — D-v75-5).** Each compact constructor keeps its null checks and replaces the salted copy with an ORDERED immutable view that renders in the SAME `toString()` format (`AbstractCollection` `[a, b]` / `AbstractMap` `{k=v, k2=v2}`): sets of `EntityRole` → `Collections.unmodifiableSet(roles.isEmpty() ? EnumSet.noneOf(EntityRole.class) : EnumSet.copyOf(roles))` (ordinal order); `Set<String>` → `Collections.unmodifiableSet(new TreeSet<>(Set.copyOf(s)))`; `Map<String, Object>` → `Collections.unmodifiableMap(new TreeMap<>(Map.copyOf(m)))` — the inner `Set.copyOf`/`Map.copyOf` keeps today's null-key/null-value rejection, the outer view fixes the order. Record `equals`/`hashCode` are content-based and unchanged. `DefinitionHashes` is NOT touched (the hash is still `sha256Hex(toString())`; the input became deterministic). If a record's javadoc states "an unmodifiable copy", amend it to "an unmodifiable, deterministically ordered copy". If `EntityRole`'s ordinal order differs from name order, prefer ordinal (`EnumSet`) — `ConditionDefinitionRenderer` sorts by NAME for the wire and is independent of this.
- **Not in scope (named):** K4 (a forDuration run's `firingValue` via `startingEventId` — read side); `AggregateValue.entities` (runtime, not hashed — confirm); any change to `DefinitionHashes`' algorithm (shape (b) — refused by D-v75-5 unless HASH-0 had shown a source (a) cannot order; it did not).

## §5 Locked decisions and invariants that apply
D2 (no run initiation on replay) · the honesty law (a value the log does not carry is null — `firingValue` stays null for a forDuration run) · SD-3 (emitters untouched — no new event type; `trigger_duration_expired`'s payload is unchanged) · the frozen v1.1.5 wire (byte-unchanged; the hash NOTE in the freeze doc becomes a fact at your landing — the hub re-cuts it) · THE PRIOR-LEDGER GATE does not apply (no rig); THE PREMISE GATE applied (§0).

## §6 P2 consumer/pin survey
`candidatesForEventType` has one caller (`evaluateMatches` `:174`); grep `consumedEventType` (1 site) and `TRIGGER_DURATION_EXPIRED` (the publisher `:434`, the subscriber `:103`, tests) before editing; list every hit in §0. Any test that pins the registry index's bucket count or `triggerIndex` keys (grep `candidatesForEventType\|triggerIndex` under `src/test`) is a pin you name and flip with the reason.

## §7 Test requirements (sorted per format law #18)
| Id | Test | Kind | Proves |
|---|---|---|---|
| T1 | `DurationTimerTest.forDurationExpiry_initiatesARun` (MEASURE-1's; assertion UNCHANGED) | RED → GREEN | the run initiates on the redelivered expiry |
| T1b | `DurationTimerTest.forDurationExpiry_runCarriesTheExpiredEnvelope` | RED → GREEN | the one `InitiateCall`: `triggeringEvent().eventType()` = `trigger_duration_expired`; `matchedTriggers` = `[0]`; the envelope's correlation equals the started event's; a SECOND redelivery of the same expired envelope initiates NO second run only if the engine dedups it today — measure and state (the RunManager owns dedup; if two calls are recorded, say so as a finding, do not fix) |
| T1c | `DurationTimerTest.forDurationExpiry_inReplay_initiatesNothing` | preservation | in REPLAY the same sequence records zero `initiateRun` calls (D2) |
| T2 | `DefinitionHashOrderProbeTest.printsHashAndRendering` | probe | the eight-launch loop's instrument (`HASH0`/`HASH1` lines) |
| T3 | `DefinitionHashStabilityTest.twoRoleSelector_hashesEqualAcrossInsertionOrders` and `twoParameterAction_hashesEqualAcrossInsertionOrders` | RED → GREEN | definition A built with `[DIAGNOSTIC, PRIMARY]` and `[PRIMARY, DIAGNOSTIC]` → one hash; definition B with `{transition, brightness}` and `{brightness, transition}` → one hash; and the rendered `toString()` shows ordinal / key order |
| T4 | `DefinitionHashStabilityTest.singleElementHashes_preserved` | PRESERVATION | at the BASELINE (before any record is edited) run the probe on definition C (a single-role `AreaSelector` + a one-parameter `CommandAction` + an `EventTrigger` with one filter) and record its hash; pin it as a literal; after the change it is equal |
Run order: T4's baseline hash FIRST (the tree untouched), then the eight-launch loop after the change (P3), then the whole gate line.

## §8 MODULE_CONTEXT.md — two paragraphs (the M7.4b paragraph's "NOT an immediate match" sentence gains "… until its expiry: the redelivered `trigger_duration_expired` IS the immediate match (DUR-1)"); the HASH-1 paragraph names the eight records, the format equivalence, the preserved single-element hashes and the golden fixture.

## §9 What to watch out for
- The registry's `dedup` `:159` is O(n²) `contains` — fine at fleet size; do not "improve" it here.
- `EnumSet.copyOf(Collection)` throws on an EMPTY non-EnumSet collection — the `isEmpty()` branch above is not optional. The loader rejects empty roles (E89-1) but a record may be built directly in tests.
- `Set.copyOf`/`Map.copyOf` de-duplicate and reject nulls; keep them as the inner validation so behaviour on bad input is unchanged (a `NullPointerException` today stays a `NullPointerException`).
- The probe's `System.out` lines are test code under the `-i` grep contract — if they stay, say so; LTD-15 binds production only.
- Gradle worker reuse: `cleanTest` forks a fresh worker (MEASURE-1 measured 8 distinct pids); if the pids repeat, add `--no-daemon` and say so.

## §10 Coder pushback welcome
If the expired-payload arm belongs in `triggerMatchesEvent` (with the index threaded through) rather than the loop, say so and do it — the loop form is the hub's minimal reading, not a law. If `EnumSet` ordering changes a rendered role order the wire depends on, stop: the wire renders through `ConditionDefinitionRenderer` (name-sorted), so it should not — prove it with a grep and say so.

## §11 Out of scope
K4 · any read-side change · `AggregateValue` · the rest-api · the freeze doc (the hub's) · coder-lessons (unless a pattern is discovered — then one entry, named in §0).

## §12 Success criterion (binary) — three instruments, read and filed in §0
1. The gate line green in one round; `git status --porcelain` = the Files table; `git diff --stat -- '*/src/main/*'` = rows 1–10 only.
2. T1 GREEN with the `InitiateCall` facts (T1b) pasted; T1c green.
3. The eight-launch loop: ONE hash for A, ONE for B (the 32/64 lines tee'd); T4's baseline literal equals the after-hash (pasted both).

## §13 Work unit completion (WUCP Phase 1)
Your §4 checklist: files touched; the commands and tails; the three instruments; deviations by tag; `MODULE_CONTEXT` updated; coder-handoff NOT written (the hub files it); the next WU named (ENERGY-READ, hub-authored).

## §14 The dispatch line (Nick pastes into a host-side Claude Code session in `~/Desktop/Code/ClaudeFolder/homesynapse-core`, AFTER his word `HASH1: a` or silence)
```
date -u first. Boot as the nexsys-coder skill. Baseline: `git log -1 --format=%h` is 3d40b5f and `git status --porcelain` reads exactly the two MEASURE-1 test files ( M DurationTimerTest.java, ?? DefinitionHashOrderProbeTest.java); stop and report otherwise. Execute nexsys-hivemind/context/instructions/2026-09-15_coder-lane_DUR-1_and_HASH-1_the-run-on-expiry_and-the-ordered-views_coding-instruction.md exactly: read its §0 contract, its premise table and §2 read-set first; adjudicate P1–P5 in your §0 card; record T4's baseline hash BEFORE editing any record. Tests first: T1 (already red) and T3 (write it red) go green through §4 rows 1 and 2 only — the registry's expired bucket, the evaluator's expired-payload arm, the eight ordered views; DefinitionHashes untouched; no new event type, no wire change. Run the gate line verbatim, then the eight-launch loop (tee to ../_scratch/v75/2026-09-15_HASH-1_eight-jvms.txt). Stage nothing, commit nothing. Write nexsys-hivemind/context/audits/<today's CT date>_DUR-1_HASH-1_return.md (≤7 KB, §0 card first, the three instruments as measured values) and end it, in the file and printed, with: RETURNED <path> <bytes>.
```
