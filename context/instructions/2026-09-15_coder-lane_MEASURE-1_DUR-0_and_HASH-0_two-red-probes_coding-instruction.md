<!--
file: context/instructions/2026-09-15_coder-lane_MEASURE-1_DUR-0_and_HASH-0_two-red-probes_coding-instruction.md
purpose: MEASURE-1 — two measurements, tests first, no production change: (DUR-0) does a `for_duration` trigger's expiry initiate a run at `3d40b5f`? — one JUnit test in `DurationTimerTest` that advances the injected clock past expiry and asserts the run is initiated (RED by the hub's reading of the source; the lane measures); (HASH-0) is `DefinitionHashes.forDefinition` stable across JVM launches for a definition whose selector names two roles? — one probe test that prints the hash and the selector's rendering, run in EIGHT fresh test JVMs by the lane, the eight values filed (≥ 2 distinct = the defect proven). Both answer a DEFECT candidate (IR-22, IR-23) before its fix is chartered (DUR-1, HASH-1) — the unmeasured-hop law, and Nick's word `DUR: measure` (D-v75-4) and `HASH1:` (D-v75-5). Authored by the v75 hub at beat 4 (Mon 2026-09-14 ~20:xx CT; instrument 2026-09-15T01:45:26Z) through THE PREMISE GATE (pm-lessons 2026-09-15): every source claim below was grepped at `3d40b5f` before this went DISPATCH-READY — the table is §0's last block and the b4 audit §3.
audience: the Coder (host-side Claude Code, nexsys-coder) · the hub (intake) · Nick (the landing card — the tests land with the fixes, not alone)
state-type: coding instruction (measurement-only)
status: EXECUTED — returned 2026-09-15T22:06Z (`context/audits/2026-09-15_MEASURE-1_return.md`, 5,107 B); audited ACCEPT v75 beat 5 (`context/audits/2026-09-15_v75-b5_MEASURE-1_intake_DUR1-HASH1_DEVICE-SET_activate_audit.md` §1): DUR-0 RED, HASH-0 salted on both sources (the 32 lines re-tallied by the hub); P4's mis-filed cite (`consumedEventType` is the registry's) accepted as the hub's; the two tests stay UNCOMMITTED and land with DUR-1 + HASH-1. Was: DISPATCH-READY — v75 beat 4. Dispatch on Nick's paste (§14).
-->

# MEASURE-1 — DUR-0 and HASH-0: two red probes, no production change

## §0 The lane contract (read first; every line binds)
- `date -u` first; state your instrument limit; CT = UTC−5, derived once.
- **Return path:** `nexsys-hivemind/context/audits/<CT-filing-date>_MEASURE-1_return.md`, one file. §0 card first (≤2 KB: the census `N = a M + b A` · the two verdicts as MEASURED values — DUR-0: RED or GREEN at HEAD with the assertion's actual vs expected; HASH-0: the eight hashes, the count of distinct values, the eight selector renderings · P1–P4 adjudicated) · §1 what changed per file · §2 the commands with their tails · §3 pushback and observations · §4 the WUCP Phase-1 checklist. **Cap ≤5 KB** (3 KB + 1 KB × the 2 Files-table rows) — a ceiling. Your last line, in the file and printed: `RETURNED <path> <bytes>`.
- **Baseline:** `git log -1 --format=%h` is `3d40b5f` and `git status --porcelain` is empty; anything else: stop and report. Every line number below was read at `3d40b5f` with the command beside it (the premise table at the end of this section); a moved cite is re-derived and filed (P4).
- **Write-set:** the two paths of the §3 Files table only — test source, both. **No production file changes** (`git diff --stat -- '*/src/main/*'` = empty is §12 instrument 1). Never `module-info.java`, never build files, never `web-ui/**`. You commit nothing and stage nothing; the tests land with their fixes (DUR-1 / HASH-1) on Nick's card, not alone — a RED test in the tree is not a landable tree.
- **Build discipline:** the gate line for DUR-0, verbatim: `./gradlew :core:automation:test --tests '*DurationTimerTest*' --offline` (the class's existing six stay green; the new one is the measurement). For HASH-0, the loop you run yourself, verbatim: `for i in 1 2 3 4 5 6 7 8; do ./gradlew :core:automation:cleanTest :core:automation:test --tests '*DefinitionHashOrderProbeTest*' --offline -i 2>&1 | grep -E '^HASH0 |    HASH0 '; done` — `cleanTest` forces a fresh test-worker JVM each pass (the salt the probe measures is per JVM launch); if Gradle reuses a worker across passes (the eight `HASH0 jvm=` lines print the same `ProcessHandle` pid), say so and add `--no-daemon`. Scripts over ~15 lines are written with the Write tool and run by path.
- **Tests first, honestly sorted (format law #18):** DUR-0 is a RED-at-HEAD prediction (P2); HASH-0 is a PROBE — it asserts nothing about stability (it prints), and it asserts only that the hash is a 64-hex string (green by construction, disclosed). Neither is a preservation fixture.
- **Test clock:** *Tests must inject `Clock`.* Do NOT use `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, or `System.currentTimeMillis()` in this module's test code. Use `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` injected via constructor/`@BeforeEach`, or `AutomationTestSupport.MutableClock` (`core/automation/src/test/java/com/homesynapse/automation/AutomationTestSupport.java`, `grep -c 'class MutableClock'` = 1). **Enforcement reach:** `NO_DIRECT_TIME_ACCESS` runs from `com.homesynapse.app`'s test classpath, so it mechanically catches PRODUCTION code in every non-whitelisted module (plus app's own tests) — it does **not** scan this module's test source set. Clock-injection here is a self-enforced project convention that PM review, not `./gradlew check`, enforces.
- **Predictions, pre-registered; adjudicate them first in your §0:** P1 — the census is the Files table (2 = 1 M + 1 A) exactly. P2 — DUR-0 is RED at HEAD: after the clock passes `expiresAt`, `trigger_duration_expired` is published (the existing behaviour, `StandardTriggerEvaluator.java:433–:438`) and the recording run manager records ZERO `initiateRun` calls (the hub's reading: `expire()` `:415–:431` publishes and validates and never reaches an initiator; `evaluateMatches` `:184–:190` starts the timer instead of matching; `triggerMatchesEvent` `:482–:500` has no expiry arm; `consumedEventType` `:174–:188` maps no trigger to the expired type; the subscriber's only `TriggerDurationExpiredEvent` arm is REPLAY bookkeeping, `AutomationEngineSubscriber.java:103`). If the test is GREEN — a run IS initiated — say where (the call path), and IR-22 closes as the hub's misread. P3 — HASH-0's eight hashes for the two-role definition contain ≥ 2 distinct values (the hub's reading: `AreaSelector`'s compact constructor stores `Set.copyOf(includedRoles)` — `AreaSelector.java:44–:48`, the `Set.copyOf` at `:47`; `DefinitionHashes.forDefinition` hashes `definition.toString()` — `DefinitionHashes.java:29–:31`; an immutable `Set.copyOf` of ≥ 2 elements iterates in a per-JVM-launch salted order); the eight renderings show the two roles in BOTH orders; the same for definition B's two-parameter action (`CommandAction.java:51` `Map.copyOf`) — ≥ 2 distinct hashes, the two keys rendered in BOTH orders. If all eight are equal for a definition, run eight more for it; sixteen equal → that source closes as the hub's misread (say so; the salt then is not what the hub read). P4 — at least one cite has moved at your HEAD, or none did.
- **THE PREMISE TABLE (the gate, applied; every row grepped at `3d40b5f` by the hub):** `expire()` `:415` · `publishExpired` `:433` · `validateAtExpiry` `:446` · the javadoc "the trigger still fires" `:444` · `maybeStartDurationTimer(automation, index, …)` `:186` · `triggerMatchesEvent` `:482` with 0 `TriggerDurationExpired` arms · `consumedEventType` `:174` with 0 `TRIGGER_DURATION_EXPIRED` cases · the subscriber's arm `:103` inside `accumulateReplayTimer` (1) and `runInitiator.initiateRuns` at `:70` · `DurationTimerTest.java` exists, 6 `@Test`, `loadDurationAutomation` at `:86`, uses `RecordingRunManager` (1) · `AutomationTestSupport` has `MutableClock` (1) and `RecordingRunManager` (1) · tests asserting a run after expiry: 0 · `DefinitionHashes.forDefinition` `:29–:31` hashes `toString()` · `AreaSelector` `Set.copyOf` `:47` (`LabelSelector` `:46`, `TypeSelector` `:48`, `SemanticTagSelector` `:57` — the same idiom) · the loader's default `Set.of(EntityRole.PRIMARY)` `AutomationDefinitionLoader.java:339`, `included_roles` parsed into a `LinkedHashSet` `:342–:344` · no tracked YAML carries `included_roles` (`grep -rln included_roles --include=*.yaml .` = 0) · `CommandAction` record `(Selector target, String commandName, Map<String, Object> parameters, UnavailablePolicy onUnavailable)` `:34–:39`, `parameters = Map.copyOf(parameters)` `:51` · `EmitEventAction` `Map.copyOf` `:37` · `EventTrigger` `Map.copyOf` `:43` · `WebhookTrigger` `Set.copyOf` `:55` · `AutomationDefinition` `List.copyOf` ×3 `:85–:87` (ordered — not a source) · a test building a command action: `NonFiringExplanationServiceTest.java:413` `new CommandAction(new DirectRefSelector(e3), "turn_on", Map.of(), …`.

## §1 What this implements
Nothing in production. Two tests that turn two register rows into measured facts, so their fixes are chartered on numbers: **DUR-0** proves or refutes IR-22 (a `for_duration` trigger's expiry initiates no run); **HASH-0** proves or refutes IR-23 (the definition hash — and with it v1.1.4's `definitionKey` and 114c's `conditions[].definition` guard — is per-JVM for a selector with ≥ 2 roles — AND, the hub's b4 grep found the same idiom on the action side: `CommandAction.java:51` stores `parameters = Map.copyOf(parameters)`, `EmitEventAction.java:37` `payload = Map.copyOf(payload)`, `EventTrigger.java:43` `payloadFilters = Map.copyOf(payloadFilters)`, `WebhookTrigger.java:55` `allowedMethods = Set.copyOf(allowedMethods)`; `Map.copyOf` of ≥ 2 entries iterates in the same per-launch salted order, so a command action with TWO parameters — `brightness` + `transition`, the common case — is predicted to hash per-JVM today, which makes the defect LIVE, not latent. HASH-0 therefore probes BOTH sources: definition A (a two-role selector) and definition B (a two-parameter command action).

## §2 Files to read before starting (minimum read set — mandatory)
- `core/automation/src/test/java/com/homesynapse/automation/DurationTimerTest.java` whole (the fixture `:51–:92`: the `MutableClock` field `:51`, `setUp` `:58`, `newEvaluator` `:73`, `loadDurationAutomation(Duration)` `:86`; the boundary test `firesAtBoundary` `:94–:111` — `clock.advance(…)` then `evaluator.pollExpirations()` is HOW expiry is driven, no wall clock; the W2 replay test `:201–:230` — the subscriber + `RecordingRunManager` shape to copy for a LIVE-mode test).
- `core/automation/src/main/java/com/homesynapse/automation/StandardTriggerEvaluator.java` — `evaluateMatches` `:166–:197`; `maybeStartDurationTimer` `:352–:400`; the expiry poller and `expire` `:405–:431`; `publishExpired` `:433–:438`; `validateAtExpiry` `:441–:455`.
- `core/automation/src/main/java/com/homesynapse/automation/AutomationEngineSubscriber.java` `:60–:110` (LIVE: `evaluateMatches` then `initiateRuns`; REPLAY: the timer bookkeeping).
- `core/automation/src/test/java/com/homesynapse/automation/AutomationTestSupport.java` — `MutableClock`, `RecordingRunManager` (its recorded calls accessor), `FakeSelectorResolver`.
- `core/automation/src/main/java/com/homesynapse/automation/{DefinitionHashes,AreaSelector,LabelSelector,TypeSelector,SemanticTagSelector,AutomationDefinition}.java` and `AutomationDefinitionLoader.java:330–:350` (how `included_roles` is parsed).
- `core/automation/src/test/java/com/homesynapse/automation/StandardExplanationServiceTest.java` — how a definition with a selector is built in tests (T4e `conditionDefinitionRenderer_selectorForms`, 114c) — copy its construction of an `AreaSelector` with two roles.

## §3 Files to create or modify (the Files table governs)
| # | Path | A/M | What |
|---|---|---|---|
| 1 | `core/automation/src/test/java/com/homesynapse/automation/DurationTimerTest.java` | M | DUR-0: one `@Test` `forDurationExpiry_initiatesARun` (§4) |
| 2 | `core/automation/src/test/java/com/homesynapse/automation/DefinitionHashOrderProbeTest.java` | A | HASH-0: the probe (§4) |
**Census claim: 2 = 1 M + 1 A.**

## §4 Technical specification
- **DUR-0 (row 1).** LIVE mode (`subscriber.setMode(LIVE)` — the W2 test's REPLAY line inverted), a `MutableClock`, an automation with ONE `for_duration` trigger (`loadDurationAutomation(Duration.ofMinutes(30))`), a `RecordingRunManager` behind the `RunInitiator`. Publish the state event that satisfies the trigger's predicate (the W2 fixture's `light` entity turning on — copy the existing tests' event), assert `activeDurationTimerCount() == 1` and one `trigger_duration_started` (the existing behaviour, a preservation line, disclosed). Advance the clock past `expiresAt` (`clock.advance(Duration.ofMinutes(30).plusSeconds(1))`) and call `evaluator.pollExpirations()` (package-private, `StandardTriggerEvaluator.java:258`; the class's own tests drive expiry this way — `firesAtBoundary` `:103–:108`); assert one `trigger_duration_expired` was published (preservation). The expired event must then reach the subscriber the way the bus would deliver it — publish it through the test's publisher into `subscriber.onEvent(envelope)` in LIVE mode (the W2 test shows the subscriber's construction) — so the measurement covers BOTH paths the run could take (inside `expire()`, or on the redelivered event). **THE MEASUREMENT:** `assertThat(recorder.calls()).hasSize(1)` (`AutomationTestSupport.RecordingRunManager.calls()` `:316`, a `List<InitiateCall>` — `InitiateCall(automation, triggeringEvent, matchedTriggers, resolvedTargets, …)` `:297`) and, if a call exists, which envelope its `triggeringEvent` is (the expired one or the starting one — record it). The hub predicts `hasSize(0)` at HEAD (RED). Do not fix anything; file the actual.
- **HASH-0 (row 2).** Build TWO `AutomationDefinition`s. **A:** its single condition is a `StateCondition` over `new AreaSelector("kitchen", roles)` with the two roles `EntityRole.PRIMARY` and `EntityRole.DIAGNOSTIC` (`core/device-model/src/main/java/com/homesynapse/device/EntityRole.java:46,:49`); construct `roles` as `new LinkedHashSet<>(List.of(EntityRole.DIAGNOSTIC, EntityRole.PRIMARY))` (the loader's shape) so the record's `Set.copyOf` is what renders. its one action a `CommandAction` with `Map.of()` parameters (the `:413` shape). **B:** a single-role selector (`Set.of(EntityRole.PRIMARY)`) and ONE `CommandAction` whose parameters are built as `new LinkedHashMap<>()` with `transition` put FIRST and `brightness` second (reverse alphabetical, so a sorted rendering is distinguishable from an insertion-ordered one) — the record's `Map.copyOf` is what renders. Print, on stdout, exactly four lines: `HASH0 jvm=<ProcessHandle.current().pid()> def=A hash=<DefinitionHashes.forDefinition(a)>` · `HASH0 def=A selector=<a.conditions().get(0).toString()>` · `HASH0 jvm=<pid> def=B hash=<DefinitionHashes.forDefinition(b)>` · `HASH0 def=B action=<b.actions().get(0).toString()>`. Assert only that each hash is 64 lowercase hex chars. `DefinitionHashes` is package-private (`static`), the test sits in the same package. Run the eight-pass loop (§0), file the thirty-two lines and the distinct-hash count PER definition in §0; the loop's raw output goes to `_scratch/v75/2026-09-15_HASH-0_eight-jvms.txt` (the `tee` in your command).
- **Event types produced or consumed — none new. Configuration — none. Error handling —** n/a.

## §5 Locked decisions and invariants that apply
The unmeasured-hop law (a defect is chartered on a measurement); the test-clock convention (§0); no production change in a measurement lane; D4 — this lane owns `core/automation/src/test/**` only.

## §6 P2 consumer/pin survey — none (test-only).

## §7 Test requirements (sorted per format law #18)
| T | Test | Sorting | What it pins |
|---|---|---|---|
| T1 | `DurationTimerTest.forDurationExpiry_initiatesARun` | RED-at-HEAD PREDICTION (P2) | the expiry initiates exactly one run |
| T2 | `DefinitionHashOrderProbeTest.printsHashAndRendering` | a PROBE (green by construction: the 64-hex assert) | the eight-JVM measurement's instrument |
Existing: `DurationTimerTest`'s six stay green; automation 243 stays green (`./gradlew :core:automation:test --offline` once at the end, with T1's expected red excluded by `--tests` or reported as the one red — say which).

## §8 MODULE_CONTEXT.md — no edit (a measurement; the fixes' lanes write the paragraphs).

## §9 What to watch out for
- Expiry is driven deterministically: `clock.advance(…)` then `evaluator.pollExpirations()` (the virtual-thread sleeper at `:405–:412` is the production clock's path; the tests never wait on it — `DurationTimerTest:103–:108`). No `Thread.sleep` in the new test.
- A `RecordingRunManager` that records `initiateRun` calls is the instrument; do not substitute a mock that returns a run — the count is the measurement.
- HASH-0's `LinkedHashSet` insertion order (A) and `LinkedHashMap` insertion order (B) must be the REVERSE of the ordinal / alphabetical order, so a rendering that follows ordinal order is distinguishable from one that follows insertion order (both are stable; only the salted order is not).
- Gradle's test worker reuse: `cleanTest` re-runs the task; the worker JVM is forked per task run unless `forkEvery`/daemon reuse keeps it — the printed pid tells you; eight distinct pids are required for the measurement to mean anything.

## §10 Coder pushback welcome
If the subscriber cannot be handed the expired envelope in LIVE mode without a bus (the W2 test constructs the subscriber directly — `onEvent` is the seam), name the seam you used; if `RecordingRunManager.calls()` is not the right recorder for `initiateRun`, name the one you used. If HASH-0 prints ONE value eight times, run eight more and file sixteen — do not "help" the measurement by constructing the set differently.

## §11 Out of scope
Any fix (DUR-1 — initiating the run on expiry; HASH-1 — the canonical rendering); any production line; any docs beyond the return; MODULE_CONTEXT.

## §12 Success criterion (binary) — three instruments, read and filed in §0
1. `git diff --stat -- '*/src/main/*'` = empty; `git status --porcelain` = the two test paths.
2. DUR-0's actual count (0 or 1) with the assertion's message pasted; the class's other six green.
3. HASH-0's thirty-two lines, eight distinct pids, the distinct-hash count for A and for B.
Done when the census is 2 = 1 M + 1 A, the three instruments read as above, and the return is on disk at the named path with its `RETURNED` line.

## §13 Work unit completion (WUCP Phase 1)
Your §4 checklist: files touched; the commands and tails; the two measured values; deviations by tag; the next WU named (DUR-1 + HASH-1 on the hub's charter, then ENERGY-READ).

## §14 The dispatch line (Nick pastes into a host-side Claude Code session in `~/Desktop/Code/ClaudeFolder/homesynapse-core`)
```
date -u first. Boot as the nexsys-coder skill. Baseline: `git log -1 --format=%h` is 3d40b5f and `git status --porcelain` is empty; stop and report otherwise. Execute nexsys-hivemind/context/instructions/2026-09-15_coder-lane_MEASURE-1_DUR-0_and_HASH-0_two-red-probes_coding-instruction.md exactly: read its §0 contract, its premise table and §2 read-set first; adjudicate P1–P4 in your §0 card. This lane changes NO production line: write T1 (DUR-0, the run-after-expiry assertion — the hub predicts RED) and T2 (HASH-0, the probe that prints two definitions' hashes and renderings — A the two-role selector, B the two-parameter command action), run T1 once and T2 in eight fresh test JVMs with the exact loop in §0 (tee its output to ../_scratch/v75/2026-09-15_HASH-0_eight-jvms.txt), and file the measured values — never fix what you measure. Write only the two test paths of its §3 Files table; no System.nanoTime; stage nothing, commit nothing (the tests land with their fixes). Write nexsys-hivemind/context/audits/<today's CT date>_MEASURE-1_return.md (≤5 KB, §0 card first, the two verdicts as measured values) and end it, in the file and printed, with: RETURNED <path> <bytes>.
```
