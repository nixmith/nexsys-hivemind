<!--
file: context/instructions/2026-09-18_bench-lane_BENCH-METER-1_metering-known-load_field-within_link-quality-skeleton_charter.md
purpose: BENCH-METER-1 — the bench lane's three owed files before Thu 2026-09-24's hardware: (A) `scenarios/metering-known-load.yaml` — the metering datum's scenario in the record's method (CHAR in place of XCHK; the two chains; T1 re-written; the band at the guaranteed 2 %; `requires: [operator, …]`); (B) the engine's two additive mechanics it needs — an operator-entered `let:` binding and a `field_within` api assert — pre-ruled by the hub as the STOP-gated format change the lane may make; (C) `scenarios/link-quality.yaml`, a SKELETON behind a false capability flag for LINK-READ. Dry-run and selftests only; no hardware; nothing touches the rig, s31 or the nightly. Every premise row grepped at bench `f3631cb` by the hub (the b6 audit).
audience: the bench lane (a fresh Cowork conversation with ClaudeFolder; nexsys-bench) · the hub (intake) · Nick (the dispatch line §9; Thu 09-24's operator blocks)
state-type: lane charter (bench)
baseline: bench `f3631cb` (porcelain 0); core `d1c2cbc` (ENERGY-READ landed, CI #253 green) for the wire fields
status: EXECUTED — LANDED bench `764e537` (Nick's card, Fri 09-18), ACCEPT at v76 b7 (`context/audits/2026-09-18_v76-b7_BENCH-METER-1_intake_two-layer-audit_and-the-re-close.md`; the rulings re-cut at b7c); marked at v77 b1 (Check 12). Was: DISPATCH-READY v76 beat 6 (Fri 2026-09-18) — dispatched on Nick's paste of §9. Returns to `context/audits/<CT-date>_BENCH-METER-1_return.md`.
-->

# BENCH-METER-1 — `metering-known-load.yaml`, `field_within`, the `link-quality.yaml` skeleton

## §0 The lane contract (read first; every line binds)
- **Baseline:** `nexsys-bench` at `f3631cb`, porcelain empty; `date -u` first; CT = UTC−5. **Write-set (the Files table §3 governs):** `scenarios/metering-known-load.yaml` (A) · `scenarios/link-quality.yaml` (A) · `scenarios/constants.yaml` (M — one new block and two capability flags, both `false`) · `tools/runner/engine.py` (M — the two additive mechanics) · `tools/runner/test_engine.py` (M) · `scenarios/SCENARIO_FORMAT.md` (M — §5 gains the two mechanics, ADDITIVE). Nothing else; no `tools/nightly.sh`, no `harness/`, no scheduler file.
- **Return path:** `nexsys-hivemind/context/audits/<CT-date>_BENCH-METER-1_return.md`, ≤ 9 KB (3 KB + ~1 KB per row, six rows — a ceiling, not a target), §0 card first (≤ 2 KB): the census · the three instruments (§7) as measured output · P1–P4 adjudicated · deviations by tag · `RETURNED <path> <bytes>` last, printed too.
- **Build discipline:** the lint and the selftests, verbatim, green before the return: `python3 -B tools/runner/runner.py suite all --list` (the load-only listing: resolve + lint + tier, run nothing — `runner.py:247`, `:366–:375`; `all` is lexical and lawful for a LISTING, unlawful for the nightly), the engine selftests (`tools/runner/test_engine.py` — run it the way its own header says: `python3 -B tools/runner/test_engine.py` if it carries a `__main__`, else `python3 -m pytest tools/runner/test_engine.py -q`; say which), and `python3 -B tools/runner/nightly_digest.py --selftest` (the desk gate, `tools/runner/README.md:247`). The desk DRY RUN: `python3 -B tools/runner/runner.py scenario metering-known-load --against <any bundle log>` evaluates the LOG asserts against a log (`runner.py:360–:361`); an api-only scenario has none, so the lint is its instrument — say so in §0. No live run: `requires: [operator, metering-plug, command-api]` with `metering-plug: false` in constants keeps it SKIPPED by the nightly by construction.
- **Tests first, honestly sorted:** T1 (`field_within` — inside/outside/at-the-edge; a missing reference refuses) RED → GREEN; T2 (the operator-entered `let:` binding parses, refuses a non-number, substitutes as `${let.<name>}`) RED → GREEN; T3 (the two new scenarios lint under `--list`; every existing scenario still lints) — T3's second half GREEN-BY-CONSTRUCTION, disclosed.
- **Predictions, pre-registered:** P1 — the census is 4 M + 2 A. P2 — `test_engine.py` gains ≥ 6 tests and the suite is green in one round. P3 — `suite --list` prints the two new scenarios with `requires` naming a `false` flag, so a nightly run would SKIP both (quote the listing's tier/requires line). P4 — no existing scenario's YAML is touched (`git diff --stat -- scenarios/*.yaml` names only the two new files and `constants.yaml`).
- **THE PREMISE TABLE (grepped at `f3631cb` by the hub):** `tools/runner/engine.py:48` the api assert kinds `{…, "phase_terminal", "field_equals"}` (no `field_within`) · `:194–:197` `parse_within(raw, where)` — `within:` is a DURATION `'<N>s'` (WITHIN_RE), NOT a tolerance: the new assert must not reuse the word for a percentage → name the key `tolerance_pct` · `:265–:269` the positive-line key sets (`api` + `within`; `log` + `same_line/count/extract/min/within`) · `:350–:360` the operator act map keys `{act, goal, note, confirm, after}` · `:1092–:1101` `field_equals` bound to a dotted field · `scenarios/SCENARIO_FORMAT.md:93` "§5 B1 additive mechanics (… additive-only; further format changes are STOP-gated)" — **the hub's ruling in this charter IS the gate: the two mechanics below are additive and pre-approved; anything beyond them is a STOP** · `SCENARIO_FORMAT.md` §5 `let:` — bindings from `api: {path, field}` and `other_of:`; no operator-entered binding exists · `scenarios/constants.yaml:144` `capabilities:` (the flag block; a flip is a constants re-mint, never a code edit — B1-REV1) · `scenarios/command-confirm.yaml:87–:93` the `api:` read of `/api/v1/entities/${C.command.entity}/state` asserting `field_equals: {field: "data.attributes.brightness_percent.value", …}` — the read surface and the dotted-path idiom for the plug's `power_w` (`data.attributes.power_w.value`; confirm the exact key at the wire on Thursday — the first read is the instrument, per format law #20; the scenario carries a `# WIRE PIN PENDING` comment until then) · `rejoin-race-operator.yaml:88` the operator-act precedent.
- **The record this scenario encodes (the b2 audit §3; the plan §14; DEVICE-SET §2 `:49–:60`):** A = the P4460 (0.5 % typical / **2 % max** on power); B1, B2 = two no-spec generics of different brands; the load = two 40 W appliance lamps (80 W, PF ≈ 1, 0.67 A); the plugs G4-1 · TR3 · G4-2. **CHAR** (replaces XCHK): A + B1 + B2 in series on the load, ~20 simultaneous readings after thermal settle (≥ 5 min on), each B's OFFSET from A and SPREAD recorded; the dispute threshold for B is |offset| + 2 × spread (k = 2, refutable at CHAR); whether B1's and B2's offsets are the SAME number is recorded (the shared-OEM tell); CHAR brackets each rep session (once before, once after). **The rep chain** is wall → A → PLUG → LAMP (ONE tare — the plug's own draw, read with the lamp unplugged: T1); B enters a rep only to attribute a dispute. **BAND** at the guaranteed figure: `|r − 1| ≤ 2.0 % + (0.5 × mult/div W) / (A_W − TARE) + 1.6 × ΔV/A_V + maker (TR3 1 %; Shelly 0)` — at 80 W and a divisor-1 plug that is 3.03 %; the typical-figure band (1.53 %) is RECORDED beside it, never the verdict's. The offsets are recorded and NEVER corrected for.
- **Stage nothing, commit nothing** — the hub cuts the card from your return.

## §1 What this implements
(A) A scenario Nick can run on Thu 09-24 with the P4460 in hand: an OPERATOR-tier scenario that walks him through CHAR, T1 and the reps, reads the platform's `power_w` for the plug under test at the instant he reads A, takes A's watts from his keyboard, and asserts the datum `within` the band from constants — the metering datum of THE MEASUREMENT RECORD (row REP-1), the corroboration leg's first `within`. (B) The two additive mechanics the scenario needs: `let: - name: <n>; operator: {prompt: "<text>", type: number}` (a typed value captured at ENTER, substituted as `${let.<n>}`) and the api assert `field_within: {field: <dotted>, reference: "${let.<n>}", tolerance_pct: "${C.metering.band_pct}"}` — `|field/reference − 1| × 100 ≤ tolerance_pct`; a non-numeric field or reference is a FAIL with both values quoted (never a pass by absence). (C) `scenarios/link-quality.yaml` — a skeleton for LINK-READ: `requires: [link-read]` (`link-read: false` in constants), one `api:` positive asserting the per-device link fields exist (`# WIRE PIN PENDING — LINK-READ names the keys`), no stimulus; it lints and is skipped.

## §2 Files to read before starting
`tools/runner/README.md` · `scenarios/SCENARIO_FORMAT.md` whole (15 KB) · `tools/runner/engine.py` `:40–:60`, `:190–:200`, `:250–:300`, `:340–:420`, `:1040–:1110` (the lint, the acts, the asserts) · `tools/runner/test_engine.py` whole (the test idiom) · `scenarios/constants.yaml` `:1–:40` and the `capabilities:` block (`:144–`) · `scenarios/command-confirm.yaml` whole (the api read/assert idiom) · `scenarios/rejoin-race-operator.yaml` `:60–:110` (the operator act idiom) · the record: `nexsys-hivemind/context/research/2026-09-15_DEVICE-SET_return.md` §2 (`:46–:60`), `nexsys-hivemind/context/audits/2026-09-17_v76-b2_nick-brief_hardware-calendar_attack_audit.md` §3, `nexsys-hivemind/context/planning/measurement-record.md` (rows CHAR-1..3, REP-1 — the scenario's operator blocks must let Nick fill them).

## §3 Files to create or modify
| # | File | A/M | What |
|---|---|---|---|
| 1 | `scenarios/metering-known-load.yaml` | A | `scenario: metering-known-load`; `tier: OPERATOR`; `requires: [operator, metering-plug, command-api]`; a header comment carrying the method verbatim (§0's record paragraph) and `# WIRE PIN PENDING` on the `power_w` key; `let:` — `plug_entity` from `${C.metering.plug-entity}`, `a_watts` (operator prompt "A's display now, in W"), `tare_watts` (operator prompt, T1); phases: CHAR-BEFORE (operator: the three meters in series; the CHAR sheet rows CHAR-1..3; `confirm: enter`) → T1 (operator: the lamp unplugged; `tare_watts`) → REP ×3 per plug (operator: "read A at the instant you press ENTER" → `a_watts`; then the `api:` read of `/api/v1/entities/${let.plug_entity}/state` with `field_within: {field: "data.attributes.power_w.value", reference: "${let.a_watts}", tolerance_pct: "${C.metering.band_pct}"}` `within: 10s`) → CHAR-AFTER. The per-plug loop is written out (three plugs × three reps), not templated — the format has no loops. |
| 2 | `scenarios/link-quality.yaml` | A | the skeleton (§1 C) |
| 3 | `scenarios/constants.yaml` | M | a `metering:` block — `plug-entity: "<UNSET — minted at the first adoption, Thu 09-24>"` (a string the lint accepts; the scenario's `let` binds it; a run before the mint fails at the api read, honestly), `band_pct: 3.03` with the derivation comment (2.0 + 0.63 + 0.40 at 80 W, divisor 1; the TR3 adds 1.0 → a second constant `band_pct_tr3: 4.03`), `load_w: 80`, `k_spread: 2`; `capabilities:` gains `metering-plug: false` and `link-read: false` with source comments |
| 4 | `tools/runner/engine.py` | M | the two mechanics; the lint accepts `field_within` beside `field_equals`; the operator `let:` binding; `tolerance_pct` and `reference` substitute `${C.*}`/`${let.*}` |
| 5 | `tools/runner/test_engine.py` | M | T1, T2, T3 |
| 6 | `scenarios/SCENARIO_FORMAT.md` | M | §5 gains the two mechanics (additive; the STOP-gate satisfied by this charter, cited by path) |

## §4 What to watch out for
- `within:` is a duration everywhere in the format; the tolerance is `tolerance_pct`. Do not overload the word.
- The operator prompt must print the §8 block the format already uses for `operator:` acts (goal/note); the typed value is echoed back and recorded in the run bundle (`bundles.py` — read how extracted values are persisted; the reference and the platform value must both land in the bundle, or the datum has no receipt).
- `power_w`'s wire key is a PIN PENDING until Thursday's first read (format law #20); write the dotted path from the capability schema (`power_w`, `StandardCapabilities.powerMeter()` in core) and mark it; the first rep re-pins it.
- The band constant is the GUARANTEED figure; the comment carries the typical figure beside it and says which is the verdict's.
- No loops, no templating beyond `${C.*}`/`${let.*}`; nine rep blocks written out is the honest shape.
- Nothing here runs against hardware; `metering-plug: false` keeps the nightly's skip; the flip is the hub's constants re-mint after the first adoption.

## §5 Coder pushback welcome
The operator-binding syntax (a `let:` entry vs a stimulus-level capture); whether `field_within` should also accept an absolute `tolerance_w`; the bundle's receipt shape.

## §6 Out of scope
`link-quality.yaml`'s real asserts (LINK-READ's charter names the keys) · `metering-known-load.yaml`'s live run (Thu 09-24, Nick) · the harness plug · the nightly · `field_within` on log lines.

## §7 Success criterion (binary) — three instruments
1. `suite --list` output pasted: every scenario lints, the two new ones present with their `requires`. 2. The selftest run's summary line (N passed, 0 failed). 3. `git status --porcelain` = the Files table (4 M + 2 A); `git diff --stat -- scenarios/*.yaml` names only rows 1–3.

## §8 Work unit completion
The return's §0 card; deviations by tag; the next WU named (LINK-READ's `link-quality.yaml` asserts; VERIFY-72H, chartered by the hub Thu 09-24).

## §9 The dispatch line (Nick pastes into a FRESH Cowork conversation with `ClaudeFolder` connected)
```
date -u first. You are the bench lane for NexSys / HomeSynapse (nexsys-bench, the test-and-truth engine). Baseline: `git -C nexsys-bench --no-optional-locks log -1 --format=%h` is f3631cb and its porcelain is empty; stop and report otherwise. Execute nexsys-hivemind/context/instructions/2026-09-18_bench-lane_BENCH-METER-1_metering-known-load_field-within_link-quality-skeleton_charter.md exactly: read its §0 contract, its premise table and §2 read-set first; adjudicate P1–P4 in your §0 card. Tests first (T1, T2 red, then green); the two additive mechanics only; the three files of §3; dry-run and selftests only — no hardware, nothing touches the rig, s31 or the nightly. Run the lint, the selftests and the dry run verbatim. Stage nothing, commit nothing. Write nexsys-hivemind/context/audits/<today's CT date>_BENCH-METER-1_return.md (≤9 KB, §0 card first, the three instruments as measured output) and end it, in the file and printed, with: RETURNED <path> <bytes>.
```
