<!--
file: context/instructions/2026-09-18_bench-lane_BENCH-METER-1b_subtract_on-outside_charter.md
purpose: BENCH-METER-1b — the metering scenario made TRUE to THE MEASUREMENT RECORD's method before Thursday's hardware day, on the tree BENCH-METER-1 landed: (1) `field_within` gains the record's TWO subtractions with the side in the name — `field_subtract:` (the plug's own meter with no load, DEVICE-SET's OFFSET) and `reference_subtract:` (the plug's own draw as A sees it, the TARE) — exact-decimal, a non-positive effective reference a per-rep VOID never a REFUSED; (2) `on_outside: record|fail` — an OUTSIDE or VOID rep is a RESULT (printed as read, recorded, the run continues, the close FAILs); (3) the operator binding gains `min:`; (4) the receipt records the entity's timestamp as a freshness WITNESS; (5) T3 re-cut against test constants so Thursday's flip cannot redden the selftests; (6) the real file walked live-path as a committed test; the per-plug wiring pinned; (7) the scenario's header, prompts and asserts rewritten — a LOAD STEP per rep in the operator's hands (the engine orders the read; it cannot know the value is fresh), tmux, the two offsets named apart, `operator` dropped from `requires:`. Four files, 4 M + 0 A, a half-day.
audience: the bench lane (a fresh Cowork conversation with ClaudeFolder) · the hub (intake)
state-type: lane charter (bench)
baseline: bench at BENCH-METER-1's landing sha (Nick's `BENCH: BM1 <sha>`; `git log -1 --oneline` names `bench: BENCH-METER-1`); porcelain empty
status: EXECUTED — LANDED bench `fa01cad` (Nick's card, Sat 09-19), ACCEPT at v76 b8 (`context/audits/2026-09-19_v76-b8_BENCH-METER-1b_intake_two-layer-audit_and-the-weekend-dispatch.md`); marked at v77 b1 (Check 12). Was: DISPATCH-READY v76 beat 7, RE-CHARTERED WHOLESALE at b7c (Fri 2026-09-18 evening) on Nick's critique of the first cut (the b7 audit §0). Returns to `context/audits/<CT-date>_BENCH-METER-1b_return.md`.
-->

# BENCH-METER-1b — the two subtractions, `on_outside`, VOID, the load step; the scenario true to the record

## §0 The lane contract
- **Baseline:** `nexsys-bench` at the sha Nick reports as `BENCH: BM1 <sha>` — `git -C nexsys-bench --no-optional-locks log -1 --oneline` names `bench: BENCH-METER-1`; porcelain empty; `ENGINE_VERSION == "BENCH-METER-1-2026-09-18-field-within"` (`engine.py:91`). Stop and report otherwise. `date -u` first; CT = UTC−5.
- **Write-set (the table governs — a site not here is a site you may not touch):**

| File | A/M | What |
|---|---|---|
| `tools/runner/engine.py` | M | R1–R6 |
| `tools/runner/test_engine.py` | M | T1–T6 |
| `scenarios/metering-known-load.yaml` | M | S1–S4 |
| `scenarios/SCENARIO_FORMAT.md` | M | §5: the new keys and verdicts, additive; this charter cited as the STOP gate's satisfaction |

`constants.yaml` is NOT in the table (Thursday's re-mint is the hub's); `provenance:` is never touched; no hardware; no Pi. **Return:** ≤ 6 KB, §0 card first (P1–P6 adjudicated); the three instruments re-run verbatim (`python3 -B tools/runner/test_engine.py`; `python3 -B tools/runner/nightly_digest.py --selftest`; `python3 -B tools/runner/runner.py suite all --list`); the walk's printed rep lines quoted; `RETURNED <path> <bytes>` last, printed too. Stage nothing, commit nothing.
- **The premise rows (grepped by the hub at `f3631cb` + the lane's tree; re-grep at your baseline before the first edit):** `engine.py:81 FIELD_WITHIN_KEYS = {"field", "reference", "tolerance_pct"}` · `:262 lint_field_within` · `:944 ensure_operator_lets` (the lazy capture, before the line's `within:` clock) · `:984 capture_operator_let` (a non-number REFUSED and asked again; the receipt keeps `typed`, `refused`, `when`) · `:1480–:1486` OUTSIDE returns `"fail"` (fail-fast per positive) · `:1490 eval_field_within` (the receipt's `verdict: state.upper()` at `:1511`, `read_at`) · `:1927 as_decimal` (`Decimal(repr(float))`) · `:1942 field_within_check` · `:1971 parse_operator_number` (`:1978 value = float(raw)`) · `:2036–:2040` a mid-run `LintRefusal` returns BEFORE the bundle write at `:2050` · `test_engine.py:691 OPERATOR_SCENARIO` (synthetic — one tare, one rep) · `:992–:1008 t_bm1_new_scenarios_skip` reads the LIVE constants and asserts `available is False` · `metering-known-load.yaml:78 requires: [operator, metering-plug, command-api]`, the header `:44–:53` and `:63–:68`, nine rep prompts "Read A AT THE INSTANT you press ENTER", nine `field_within` asserts with `reference: "${let.a_watts_<plug>_r<n>}"` · `runner.py:104 auto_preflight_refusals` (OPERATOR tier never in the auto suite).

## §1 The engine rows (R) — tests first, red → green
- **R1 the two subtractions.** `field_within` gains `reference_subtract:` and `field_subtract:` — each OPTIONAL (absent = 0); a number or ONE whole `${C.*}`/`${let.*}` reference (the lint checks the shape, never the value — a typed value does not exist at lint). Evaluation: `ref_eff = as_decimal(reference) − as_decimal(reference_subtract)`, `val_eff = as_decimal(value) − as_decimal(field_subtract)` — Decimal on EACH operand, never a float subtraction; `r = val_eff / ref_eff`. `ref_eff ≤ 0` → the line's verdict **VOID** with all four operands quoted (a result — never a `LintRefusal`, never a division). The receipt carries `reference`, `reference_subtract`, `reference_effective`, `value`, `field_subtract`, `value_effective`, `ratio`, `deviation_pct`, `tolerance_pct`, `verdict`, `read_at`.
- **R2 `on_outside: record|fail`.** Default `fail` (BENCH-METER-1's behaviour, untouched for every existing scenario); `record` is lawful on `tier: OPERATOR` only (the lint refuses it elsewhere). Under `record`: an OUTSIDE or VOID line is recorded and the run CONTINUES to the next positive; a line whose field is still not a number when its `within:` expires is **VOID** (under `fail` it stays the deadline FAIL it is today); at the close the scenario's verdict is FAIL if any line is OUTSIDE or VOID — each listed with its let name and receipt — else PASS; `bundle: always` writes either way.
- **R3 the per-rep print.** Every `field_within` line prints ONE line as it is decided: `REP <reference's let name> — power_w=<value> − <field_subtract> = <value_effective> vs A=<reference> − <reference_subtract> = <reference_effective> → r=<ratio> |r−1|=<deviation> % vs <tolerance> % → WITHIN|OUTSIDE|VOID`. The operator reads the verdict at the keyboard, not from the bundle.
- **R4 `min:` on the operator binding.** Optional number; a typed value below it is REFUSED at capture and asked again, exactly as a non-number is; the refused entries ride the receipt. (The tares and the no-load readings carry `min: 0`.)
- **R5 the freshness witness.** The receipt records the entity's timestamp from the `/state` body beside `read_at` — the key the live dialect carries for last-reported/last-updated (WIRE PIN PENDING: name it from the fixture you have; `null` when absent) — RECORDED, NEVER ASSERTED. The engine cannot know freshness: the core's `EntityState` stamps are entity-level (an energy report refreshes them too), and `ActivePower` reports on a ≥ 1 W change or a 5–600 s window. Freshness is the operator's load step (S3).
- **R6** `ENGINE_VERSION = "BENCH-METER-1b-<CT date>-subtract-record"`.

## §2 The tests (T) — in `test_engine.py`; LIVE-PATH means a `ScenarioRun` with scripted `/state` reads and a scripted keyboard (the `:776` idiom), never a dry run
- **T1 the subtractions.** (a) A = 79.6, `reference_subtract` = 0.7, field = 81.29067, tolerance 3.03 → `reference_effective` = 78.9, r = 1.0303 EXACTLY, deviation 3.0300 → **WITHIN** (the edge inclusive); the test asserts WITHIN and that `reference_effective` prints as `78.9` — a float subtraction gives 78.89999999999999 and OUTSIDE by rounding alone, which is the defect this test pins. (b) `field_subtract` = 0.3 on value 80.3 → `value_effective` 80.0. (c) `reference_subtract` ≥ reference → VOID, all four operands in the receipt, no exception. (d) absent keys = 0 and BENCH-METER-1's receipts unchanged.
- **T2 the modes, LIVE-PATH.** One scripted scenario with three `field_within` lines, the second OUTSIDE: under `fail` the run stops at line 2 (line 3 never read); under `record` line 3 is read, the close is FAIL naming line 2, the bundle receipts hold three verdicts. (b) a line whose scripted `/state` never carries a numeric field → VOID under `record` at the deadline, FAIL under `fail`. (c) the lint refuses `on_outside: record` on `tier: AUTO`.
- **T3 the REAL file walked LIVE-PATH, committed with its inputs** (`scenarios/metering-known-load.yaml`, the flags and ids overridden in memory, drivers tripwired — the return's scratch walk, now a test). The keyboard, in `let:` order per plug: G4-1 — tare 0.5, no-load 0.0, volts 120.1 / 120.0, A 80.9 / 80.8 / 80.9; TR3 — tare 0.7, no-load 0.0, volts 119.9 / 120.0, A 80.6 / 80.7 / 80.6; G4-2 — tare 0.5, no-load 0.0, volts 120.0 / 120.0, A 81.0 / 80.9 / 81.0; CHAR-AFTER 20. The scripted `power_w` reads, in line order: G4-1 80.1 / 79.4 / 80.3; TR3 76.9 / 80.0 / 80.0; G4-2 76.9 / 80.0 / 80.3. Expected: eight WITHIN, ONE OUTSIDE (G4-2 rep 1: 76.9 vs 80.5 → 4.472 % > 3.03), the run continues through CHAR-AFTER, the close FAIL names `a_watts_g4_2_r1`; TR3 rep 1 is the tare's proof — 76.9 vs 80.6 alone is 4.591 % (OUTSIDE at 4.03; the test computes and prints it for the record) and 76.9 vs 79.9 is **3.755 %, WITHIN at 4.03**. Assert the nine verdicts, the two TR3 figures, the 22 typed receipts and the nine read receipts in the bundle captures.
- **T4 the per-plug wiring.** Load the real file; for each of the nine asserts: the `path` names plug P (`plug-entity.P`), `reference` names `a_watts_P_rN`, `reference_subtract` names `tare_watts_P`, `field_subtract` names `plug_offset_w_P`, `on_outside` is `record`, and `tolerance_pct` names `band_pct_tr3` iff P is tr3 (else `band_pct`).
- **T5 T3c re-cut.** `t_bm1_new_scenarios_skip` takes a DEEP COPY of the loaded constants with `capabilities.metering-plug.available` and `link-read.available` FORCED false — it never asserts the live file's values (Thursday's flip must not redden the selftests); a second check forces `metering-plug` TRUE and asserts `unmet_requirements()` is empty for `metering-known-load` (the gate opens on the flag alone — `operator` gone).
- **T6 `min:`.** A typed −0.3 is REFUSED, 0.7 then accepted; the receipt lists `refused: ["-0.3"]`.

## §3 The scenario rows (S) — `scenarios/metering-known-load.yaml`
- **S1** `requires: [metering-plug, command-api]` — `operator` DROPPED: an OPERATOR-tier leg never enters the nightly by the tier (`runner.py:104`, `:119`); the flag was a phantom gate. Delete the header's REQUIRES paragraph (`:63–:68`).
- **S2 the header, rewritten** (the METHOD block from the charter stays verbatim; the "HOW THE FORMAT CARRIES IT" block is replaced): (a) FRESHNESS IS THE OPERATOR'S — the engine reads within 10 s of ENTER and cannot know the value is fresh (the plug reports on a ≥ 1 W change or a 5–600 s window; the entity's timestamp is entity-level; the TR3's power lags up to a minute), so every rep begins with a LOAD STEP and a WAIT for the dashboard; (b) ONE DATUM PER REP under `on_outside: record` — an OUTSIDE or VOID rep is recorded, the run continues, the close FAILs, B attributes AFTER CHAR-AFTER (never mid-run); (c) THE RATIO IS THE RECORD'S DIV ROW — `r = (power_w − OFFSET) / (A_W − TARE)`: OFFSET = the plug's own meter with no load (`field_subtract`), TARE = the plug's own draw as A sees it (`reference_subtract`), B's CHAR offsets a third thing never subtracted; (d) THE TARE BY DIFFERENCE — the plug's draw sits below A's 0.2 A spec floor, so it is measured at the lit load: W2 − W1; (e) RUN IT IN tmux at an attended TTY (`tmux new -s metering`, then `~/nexsys-bench/tools/bench.sh scenario metering-known-load`) — an SSH drop must not kill a typed session; (f) THE RECEIPT — the fields of R1 and R5; (g) WIRE PIN PENDING as today.
- **S3 the `let:` list, per plug P in the order G4-1 · TR3 · G4-2**, each entry `type: number`; the goal/prompt/note in the §8 shape: (1) `tare_watts_P` (`min: 0`) — "Chain wall → A → LAMP (no plug). After 30 s read A's watts → W1, on the sheet. Re-chain wall → A → P → LAMP, P's relay ON, wait ≥ 30 s, read A → W2. Type W2 − W1 (P's own draw as A sees it; below A's 0.2 A floor read direct — hence the difference). If negative, type 0 and note the pair on the sheet." (2) `plug_offset_w_P` (`min: 0`) — "LAMP OUT of P, relay ON; wait until the dashboard's `power_w` for P settles (the TR3 up to a minute); type it (DEVICE-SET's OFFSET — expect 0.0). Then LAMP back into P." (3) `a_volts_P_1`, (4) `a_volts_P_2` — "A's volts now" / "A's volts again, 10 s later" (the band's ΔV term; recorded). (5–7) `a_watts_P_r1..r3` — "THE LOAD STEP: on the dashboard toggle P's relay OFF, wait ≥ 15 s, ON (or unplug the LAMP from P for ≥ 15 s and back). WAIT until the dashboard's `power_w` for P shows the lit value again (the TR3 up to a minute), then ≥ 5 s more. Read A's watts, type, ENTER — the platform's read follows within 10 s." Note: "REP n/3 on P — the datum. WITHIN/OUTSIDE/VOID prints at once; an OUTSIDE or VOID rep is recorded and the run continues — attribute with B after CHAR-AFTER." Then `char_after_readings` as today. (22 typed entries in all.)
- **S4 the nine asserts** each gain `reference_subtract: "${let.tare_watts_P}"`, `field_subtract: "${let.plug_offset_w_P}"`, `on_outside: record`; `within: 10s` and the tolerances unchanged.

## §4 Predictions (adjudicate each in your §0 card)
P1 the census 4 M + 0 A. P2 the selftests gain ≥ 10 checks; red first (T1a, T2, T3 red on the landed engine), then green. P3 the listing unchanged at 13 legs; `metering-known-load tier=OPERATOR requires=metering-plug,command-api`. P4 the walk prints TR3 rep 1 at `4.591 %` (A alone) and `3.755 %` (A − 0.7) → WITHIN; G4-2 rep 1 `4.472 %` → OUTSIDE; the close FAIL names one line. P5 T1a prints `reference_effective 78.9` and WITHIN. P6 the digest selftest unchanged (43/0).

## §5 What to watch out for
The subtractions are the record's, one per side — never a single `subtract:`; never subtract B's offsets. A REFUSED mid-run writes no bundle, so nothing typed may be judged by a `LintRefusal` — VOID is a result. `record` must weaken nothing outside OPERATOR tier. The T3 walk is a committed test with its inputs in the file, not a scratch run. The engine version is bumped (the Pi's banner is how Thursday knows what runs). Estimate: a half-day — say so in the return if it ran longer.

## §6 The dispatch line (Nick pastes into a FRESH Cowork conversation with ClaudeFolder connected, after `BENCH: BM1 <sha>`)
```
date -u first. You are the bench lane for NexSys / HomeSynapse (nexsys-bench). Baseline: nexsys-bench at BENCH-METER-1's landing sha (`git -C nexsys-bench --no-optional-locks log -1 --oneline` names `bench: BENCH-METER-1`) with an empty porcelain; stop and report otherwise. Execute nexsys-hivemind/context/instructions/2026-09-18_bench-lane_BENCH-METER-1b_subtract_on-outside_charter.md exactly: read its §0 first and re-grep its premise rows at your baseline; adjudicate P1–P6 in your §0 card. Tests first (red, then green): the engine rows R1–R6, the tests T1–T6 (T2 and T3 LIVE-PATH; T3 is the real file walked with the charter's inputs, committed), the scenario rows S1–S4, the format's §5. The four files of the table only; constants.yaml and provenance untouched; no hardware. Re-run the three instruments verbatim and quote the walk's nine REP lines. Stage nothing, commit nothing. Write nexsys-hivemind/context/audits/<today's CT date>_BENCH-METER-1b_return.md (≤6 KB, §0 card first) and end it, in the file and printed, with: RETURNED <path> <bytes>.
```
