<!--
file: context/instructions/2026-09-18_coder-lane_ENERGY-READ_metering-by-clusters_divisors-at-adoption_scaled-reporting_coding-instruction.md
purpose: ENERGY-READ in ONE coder session on the Java slot — the corroboration leg's code before the plugs are in hand: (R1) IR-15, a DEFECT — the classifier attaches `motion` to every non-CONTACT IAS zone while the handler emits `active` for WATER_LEAK/SMOKE/VIBRATION; (R2) IR-24, classify by the CLUSTERS present — a metering plug of ANY device type (the owned Gen4 is 0x010A, not the 0x0051 the plug arm keys on) gains `powerMeter`/`energyMeter`; (R3) the formatting read — the 0x0B04 and 0x0702 multiplier/divisor attributes READ from the device at adoption and persisted, never predicted; (R4) the two handlers, scaling by the read formatting and emitting NOTHING until it is known; (R5) IR-25 as a SIZED decision — the reportable change configured in engineering units scaled by the read divisor, the predicted frames/h per divisor stated; (R6) one INFO line at classification printing the device type and the clusters (DEVICE-SET note 1); (R7) MEASURE-2 / IR-28 — the sustained-rate instrument on the hardware-free rig, its numbers filed in THE MEASUREMENT RECORD. Every premise row grepped at core `3af6213` by the hub (the b4 audit §2).
audience: the Coder (host-side Claude Code, nexsys-coder) · the hub (intake) · Nick (the dispatch line §14; the landing card)
state-type: coding instruction
baseline: core `3af6213` (CI #252 green at the instrument; porcelain 0) — re-verify at issue
status: DISPATCH-READY v76 beat 4 (Fri 2026-09-18) — through THE PREMISE GATE; dispatched on Nick's paste of §14. THE ADOPTION FENCE stands until this lane is green: no metering plug joins any network before then.
-->

# ENERGY-READ — metering by clusters; the divisors at adoption; scaled reporting; the rate instrument

## §0 The lane contract (read first; every line binds)
- **Baseline:** core `3af6213`; `git status --porcelain` EMPTY; anything else → stop and report. `date -u` first; CT = UTC−5; every stamp you write derives from that reading.
- **Return path:** `nexsys-hivemind/context/audits/<CT-date>_ENERGY-READ_return.md`, one file, **≤ 26 KB** (3 KB + ~1 KB per Files-table row, 23 rows, rounded up — a CEILING, not a target; the §0 card is what the hub reads first; a shorter return with the same receipts is a better return), §0 card first (≤ 3 KB): the census · the instruments of §12 as measured values · P1–P8 adjudicated · deviations by tag · `RETURNED <path> <bytes>` as the last line, printed too.
- **Write-set (the Files table §3 governs; nothing else):** `integration/integration-zigbee` main + test + testFixtures, its `MODULE_CONTEXT.md`, and `context/planning/measurement-record.md` (the hivemind; MEASURE-2's rows). NO `module-info` change (every type you add is package-private or internal; `com.homesynapse.device` is already reachable through `requires transitive com.homesynapse.integration` — `integration/integration-zigbee/src/main/java/module-info.java` verbatim in §4). NO event-model change, NO new event type, NO rest-api or web-ui change (the attribute keys are the device model's own: `power_w`, `voltage_v`, `current_a`, `energy_wh` — `StandardCapabilities.java:312/:451/:481`), NO device-model change, NO bench change.
- **Build discipline:** the gate line, verbatim, green in ONE round before the return: `./gradlew :integration:integration-zigbee:test :lifecycle:lifecycle:compileJava :app:homesynapse-app:test spotlessCheck --offline` (the app test task carries `HomeSynapseArchRules` — `NO_DIRECT_TIME_ACCESS` scans this module's PRODUCTION code from there). MEASURE-2 (§4 R7) is NOT in the gate: it runs by path, once, on the desk: `./gradlew :integration:integration-zigbee:test --tests '*MeteringRateMeasurementTest*' -Dhomesynapse.measure2=true --offline -i 2>&1 | grep -E '^MEASURE2 |    MEASURE2 ' | tee ../_scratch/v76/2026-09-18_MEASURE-2_rate.txt`. Scripts over ~15 lines are written with the Write tool and run by path — no long heredocs.
- **Tests first, honestly sorted (format law #18):** RED at the baseline and GREEN after — T1 (IR-15), T2 (IR-24, the Gen4 signature), T4 (the formatting read), T5 (the two handlers), T6 (the scaled change), T7 (no formatting → no emission). GREEN-BY-CONSTRUCTION, disclosed — T3 (every existing classifier row: SNZB-02P, the Hue light, the occupancy sensor — preserved) and T9 (every existing handler, configurator, cache and drive test). A MEASUREMENT, neither red nor green — T8 (MEASURE-2). Say which is which in your §0.
- **Predictions, pre-registered; adjudicate them first in your §0:** P1 — the census is the Files table exactly (23 = 13 M + 10 A). P2 — T1 goes GREEN by one arm in `iasCapability` (WATER_LEAK/SMOKE/VIBRATION → `StandardCapabilities.binaryState()`) and NO handler change. P3 — T2 goes GREEN with the Gen4's recorded signature (EP1, device type 0x010A, input clusters 0x0000 0x0003 0x0004 0x0005 0x0006 0x0702 0x0B04 0xFC21): entity type SWITCH, capabilities `on_off` + `power_meter` + `energy_meter`, and the same signature with device type 0x0051 classifies IDENTICALLY (the type is a hint, never the key). P4 — the formatting read sends ONE ReadAttributes per metering cluster per endpoint (0x0B04: 0x0604, 0x0605; 0x0702: 0x0300, 0x0301, 0x0302, 0x0303) BEFORE the cluster's Configure Reporting, and the configure frame's reportable change equals `round(engineering × divisor / multiplier)`; with the FakeNcp scripted to answer divisor 100 / multiplier 1 on 0x0B04, the change field reads 100 (1 W); with 0x0702 divisor 1,000,000 / multiplier 1 (kWh), the change field reads 5,000 (5 Wh). P5 — with no formatting learned, a 0x0B04 or 0x0702 report produces NO `state_reported`, one DEBUG `zigbee.metering_unscaled` per (device, cluster) and nothing else (honest silence over a guessed scale). P6 — the eight existing handlers are constructed unchanged; `ClusterHandlers.forDevice` returns TEN entries for a device with formatting for both clusters and EIGHT for a device with none (the metering handlers are attached only when their formatting is known). P7 — the cache round-trips the formatting across a save/load (`ZigbeeDeviceCacheTest`), and a rejoin does NOT re-read it (the cached value is used; `onRejoin` re-applies the scaled configure with it). P8 — no cite in the premise table has moved (HEAD is the pinned sha).
- **THE PREMISE TABLE (the gate, applied; every row grepped at `3af6213` by the hub; a mis-filed cite is corrected in your §0, never silently):**
  - `EndpointClassifier.java:41` `private static final int DEVICE_TYPE_SMART_PLUG = 0x0051;` · `:91–:104` the `switch (descriptor.deviceTypeId())` — `:99–:101` the SMART_PLUG arm → `EntityType.SWITCH, capabilities(StandardCapabilities.onOff())`; `:102–:103` `default -> fallback(hasOnOff, hasLevel, hasColor, hasOccupancy, hasIasZone, hasBattery, learnedZoneType)` · `:150–:165` `binarySensor(...)` (Occupancy outranks IAS) · `:175–:178` `iasCapability(ZoneType)` → `CONTACT ? contact() : motion()` — **the IR-15 defect** · `:180–:186` `fallback(...)`: `hasOnOff && hasLevel → light`; `hasOnOff → …` (the arm the Gen4 lands in today). No arm in the file consults 0x0B04 or 0x0702 (`grep -n '0x0B04\|0x0702\|0x0b04' EndpointClassifier.java` = 0 hits).
  - `IasZoneHandler.java:73–:77` `attributeKey()`: `MOTION -> "detected"`, `CONTACT -> "open"`, `WATER_LEAK, SMOKE, VIBRATION -> "active"` · `ZoneType.java:23–:35` the five values (`MOTION 0x000D`, `CONTACT 0x0015`, `WATER_LEAK 0x002A`, `SMOKE 0x0028`, `VIBRATION 0x002D`).
  - `StandardCapabilities.java:312` `powerMeasurement()` (`power_w` only) · `:332` `binaryState()` (schema `active`, boolean) · `:350` `contact()` · `:368` `motion()` · `:451` `energyMeter()` (`energy_wh` float Wh, `direction` enum, `cumulative` boolean; a `reset_meter` command; an EXACT_MATCH policy on `energy_wh`) · `:481` `powerMeter()` (`power_w`, nullable `voltage_v`, nullable `current_a`). The keys exist; this lane declares none.
  - `ClusterHandlers.java:35–:52` `static Map<Integer, ZigbeeClusterHandler> forDevice(IEEEAddress device, Clock clock, ZoneType zoneType)` → `Map.of(...)` with EIGHT entries (0x0006, 0x0008, 0x0300, 0x0406, 0x0001, 0x0402, 0x0405, 0x0500) — no 0x0B04, no 0x0702 (`grep -n 'ElectricalMeasurement\|Metering' ClusterHandlers.java` = 0 hits).
  - `ZigbeeClusterHandler.java` (abstract): `abstract List<NormalizedAttribute> normalize(int endpoint, int clusterId, Map<Integer, Object> attributes)`; `handleAttributeReport` builds `AttributeReport(entityRef, attributeKey, value, clock.instant())` · `NormalizedAttribute(attributeKey, value, unit, rawProtocolValue, rawProtocolUnit)` · `TemperatureMeasurementHandler.java` whole — THE TEMPLATE (`CLUSTER_ID`, the attribute constant, the invalid sentinel, the band check, `raw / 100.0`, the F-9 debug on out-of-band).
  - `ZclCodec.java:32` `COMMAND_READ_ATTRIBUTES = 0x00` · `:165–:174` `encodeReadAttributes(int tsn, int[] attributeIds)` · the decoder: `:195` map8/uint8/enum8 (0x18/0x20/0x30) · `:197` map16/uint16/enum16 · `:199` uint24 (0x22) · `:201` uint48 (0x25) · `:203` int16 (0x29) · int24 (0x2A) is NOT decoded — `InstantaneousDemand` (0x0702 attr 0x0400) is therefore out of scope; ActivePower on 0x0B04 is the power source.
  - `ReportingConfigurator.java:57` `SKIP_CONFIGURE_REPORTING = "configure_reporting"` · `:63–:72` `DEFAULTS` — `:71` `0x0B04 → DefaultRow(0x050B, 0x29, 5, 3600, 10)`, `:72` `0x0702 → DefaultRow(0x0000, 0x25, 5, 3600, 5)` — RAW changes · `:97–:128` `configureDevice(device, endpoints, profile)`: `:102–:104` the profile skip; `:110–:113` `DEFAULTS.get(clusterId)`, `continue` when null; `:125–:126` `configureCluster(device, endpoint, clusterId, effectiveRow(row, clusterId, profile))` · `:131–:136` `onRejoin` → `configureDevice` · `configureCluster`: `ops.bind` → `ops.configureReporting(device, endpoint, clusterId, attributeId, dataType, min, max, change)` → `verifyReadback`.
  - `ReportingOps.java:20` the interface: `:54` `bind`, `configureReporting`, `:83` `readReportingConfiguration`, `:94` `writeCieAddress` — NO attribute read on this seam (`grep -n 'readAttributes' ReportingOps.java` = 0 hits) · `EzspReportingOps.java:145–:162` the Configure Reporting payload (`analogChangeWidth(dataType)`; the change written little-endian at width) · `EzspCoordinatorProtocol.java:1993–:2010` `readBasic`: `encodeReadAttributes(tsn, BASIC_IDENTITY_ATTRIBUTES)` → `sendUnicastLocked(networkAddress, HA_PROFILE_ID, 0x0000, 1, endpoint, tsn, zcl)` → `awaitIncomingLocked(HA_PROFILE_ID, 0x0000, …)` — THE PATTERN for the formatting read (a synchronous unicast + await, on the protocol's lock).
  - `ZigbeeIntegrationAdapter.java:380–:392` the ingestion unit constructed with `cache.learnedZoneTypeIds()` and `cache::recordLearnedZoneType` (LEARN-PERSIST) · `:406–:409` `adoption.onAdopted(ieee -> { ingestion.invalidateHandlers(ieee); availabilityPublisher.seedFreshAdoption(ieee); })` · `:979–:996` `adoptIfAccepted` (LINKED → `driveReporting(…, true)`; unlisted → return; listed COMPLETE → `adoption.adopt` `:995` → `driveReporting(…, false)`) · `:1009–:1027` `driveReporting`: `reporting.onRejoin(...)` or `reporting.configureDevice(ieee, interview.endpoints(), profile)`, the `zigbee.reporting_configured` INFO, `applyPostureRouting`.
  - `ZclIngestionUnit.java:521–:545` the attribute-report / read-response parse (`parseAttributeReports` / `parseReadAttributesResponse`), `learnZoneType` on IAS, `handlersFor(device).get(clusterId)`, `:540` `zigbee.ingestion_unhandled_cluster` · `:586–:593` `publishStateReported`'s unadopted guard.
  - `ZigbeeDeviceCache.java:84` `learnedZoneTypes` · `:291–:296` `recordLearnedZoneType(ieee, zclId)` · `:308–:311` `learnedZoneTypeIds()` · `:364` `maybeFlush` · `:392` `flush` — THE PATTERN for `learnedMeteringFormatting`.
  - `InterviewStateMachine.java:52–:57` `enum Step { NODE_DESCRIPTOR, ACTIVE_ENDPOINTS, SIMPLE_DESCRIPTOR, BASIC_ATTRIBUTES, WHOLE_INTERVIEW_TIMEOUT }` · `:160–:167` `readBasic` → manufacturer/model. The interview is NOT touched by this lane (the formatting read rides the adoption drive, after `adopt()`, not the interview — an unadopted device is never asked; THE ADOPTION FENCE keeps its meaning).
  - The test harness: `ZigbeeReportingDriveTest.java:162` `FakeNcp` (scripted EZSP replies; `zdoMessages(ncp, …)`, `zclMessages(ncp, EzspReportingOps.COMMAND_CONFIGURE_REPORTING)` — read the class's own helpers) · `ZigbeeHardwareFreeRig.java` (testFixtures): `:121` `announce(long ieee)`, `:137–:156` `reportOccupied/reportOnOff/reportColorTemperatureMireds/reportBrightnessLevel`, `:169` `silence`, `:178` `deliverAndCycle`, `:189` `adopt(long ieee)`, `:194` `sentZclFrames()`.
  - The record: the Gen4's signature — PLUG-DOSSIER row 16 (Shelly's feature-matrix row "Shelly Plug US": "On/Off Plug-in Unit (0x010A)", On/Off 0x0006, Metering 0x0702, Electrical Measurement 0x0B04 as Input on Endpoint 1) and DEVICE-SET §1 (`EP1 dev 0x010A: 0006 0702 0B04 FC21`); the ZHA corroboration (row 18: `in_clusters 0x0000,0x0003,0x0004,0x0005,0x0006,0x0702,0x0b04`). The TR3 (dossier row 29): z2m OVERRIDES its divisors (`seMetering divisor 3600000`, `acPowerDivisor 10`) — a sign the device's own formatting attributes may be absent or wrong on that unit; this lane reads what the device says and records it (R3's posture fact), and the TR3's quirk is October's row, not this one. The Gen4's divisor is UNREAD (DEVICE-SET note 3 predicts {1, 100}; the fixture replays 1, 10, 100); the first real value is written into THE MEASUREMENT RECORD at the first adoption, never before.
  - The external-standard constants (format law #4 — embedded here with the source; re-derive them independently before pinning): **Electrical Measurement cluster 0x0B04** — `ActivePower` 0x050B (int16, W × ACPowerMultiplier / ACPowerDivisor), `RMSVoltage` 0x0505 (uint16, V × ACVoltageMultiplier 0x0600 / ACVoltageDivisor 0x0601), `RMSCurrent` 0x0508 (uint16, A × ACCurrentMultiplier 0x0602 / ACCurrentDivisor 0x0603), `ACPowerMultiplier` 0x0604 (uint16), `ACPowerDivisor` 0x0605 (uint16); the invalid sentinels: int16 0x8000, uint16 0xFFFF. **Simple Metering cluster 0x0702** — `CurrentSummationDelivered` 0x0000 (uint48; × Multiplier / Divisor in UnitOfMeasure), `UnitOfMeasure` 0x0300 (enum8; 0x00 = kWh), `Multiplier` 0x0301 (uint24), `Divisor` 0x0302 (uint24), `SummationFormatting` 0x0303 (map8). Source: the Zigbee Cluster Library Specification (document 07-5123), the Simple Metering and Electrical Measurement chapters; corroborated by PLUG-DOSSIER row 28 (Innr staff: "'Multiplier' and 'Divisor' attributes 0x0600 to 0x0605 of cluster 0x0b04") and zigbee-herdsman-converters' `electricityMeter` (which reads the divisor first and configures `change = engineering × divisor / multiplier` — DEVICE-SET note 2 [z]). Multiplier 0 or divisor 0 read from a device is INVALID formatting (treat as unknown — P5's silence).
- **The instrument-limit and CT-rederivation lines:** your desk's `date -u` is the instrument for every stamp; CT = UTC−5 re-derived once from it; a wall-clock in a test is a defect (§9's paste-block).
- **Stage nothing, commit nothing.** The tree is Nick's hands; the hub cuts the card from your return.

## §1 What this implements
A metering plug adopted by HEAD today classifies as a plain switch, is asked (after adoption) to report 0x0B04 and 0x0702 at RAW thresholds nobody scaled, and every metering frame dies at `zigbee.ingestion_unhandled_cluster` (`ZclIngestionUnit.java:540`). After this lane: (R1) a water-leak, smoke or vibration zone classifies to `binary_state` (schema `active`), matching what `IasZoneHandler` already emits — the one-way door of adoption no longer locks the wrong capability for life; (R2) any endpoint listing 0x0B04 and/or 0x0702 gains `power_meter` and/or `energy_meter` beside its other capabilities, whatever its device type; (R3) at adoption — and only then — the adapter READS each metering cluster's formatting attributes from the device, records them in the device cache (persisted, LEARN-PERSIST's pattern) and in a posture fact; (R4) two handlers scale reports by the recorded formatting into `power_w` / `voltage_v` / `current_a` and `energy_wh`, and a device whose formatting is unknown produces no scaled value and no guess; (R5) the reportable change is configured in ENGINEERING units — 1 W on `ActivePower`, 5 Wh on `CurrentSummationDelivered` — scaled by the read formatting, with `ActivePower`'s maximum interval 600 s; (R6) one INFO line at classification prints the device type, the input clusters and the chosen entity type and capabilities; (R7) MEASURE-2 measures what the ingestion path sustains: N synthetic metering reports per second for M minutes on the hardware-free rig — events/s, store growth, heap — the first rows of THE MEASUREMENT RECORD.

**The sized decision (IR-25, Nick's v76 b2 brief §4 fold 1) — the predicted frames/hour/device at an 80 W resistive load, BEFORE and AFTER, per candidate divisor:**
| Cluster · attribute | HEAD's raw change | divisor 1 | divisor 10 | divisor 100 | divisor 1,000 (kWh: 1 raw = 1 Wh) | divisor 1,000,000 (1 raw = 1 mWh) | divisor 3,600,000 (the TR3 per z2m; 1 raw = 1 Ws) |
|---|---|---|---|---|---|---|---|
| 0x0B04 ActivePower, BEFORE | 10 raw | 10 W: edges only + the hourly heartbeat | 1 W: edges + rare noise | 0.1 W: line noise (≈0.32 W at 80 W) crosses continuously → the 5-s floor → **720/h** | — | — | — |
| 0x0B04 ActivePower, AFTER (1 W, max 600 s) | `round(1 × divisor / multiplier)` | 1 raw | 10 raw | 100 raw | — | — | — |
| → predicted | | edges + ≤ 6/h heartbeat at every divisor (an 80 W lamp's on/off edge is 80× the threshold; the noise band stays under it) | | | | | |
| 0x0702 CurrentSummationDelivered, BEFORE | 5 raw | — | — | — | 5 Wh → every 225 s → **16/h** | 5 mWh → 0.225 s → the 5-s floor → **720/h** | 5 Ws → 0.06 s → the 5-s floor → **720/h** |
| 0x0702 AFTER (5 Wh, max 3600 s) | `round(5 Wh × divisor / multiplier)` in the unit's kWh base | — | — | — | 5 raw → **16/h** | 5,000 raw → **16/h** | 18,000 raw → **16/h** |
Three plugs over 72 h: BEFORE 155k (floor) – 311k (ceiling) metering events; AFTER ≈ 3,500 from 0x0702 + the edge-driven 0x0B04 leg (tens per hour worst case) — under 10k. The 5-s minimum interval stays as HEAD's floor (a 3.6 kW load reaches it on 0x0702; nothing in the fleet does). These numbers are PREDICTIONS; MEASURE-2 and the first rep are the instruments (THE MEASUREMENT RECORD rows M2-1..M2-3 and the Gen4's divisor row). Refutable-by: a first-rep reading showing the 1 W threshold under the plug's own noise floor at 80 W — then the threshold moves to 2 W by a one-line change and the record says why.

## §2 Files to read before starting (minimum read set — mandatory)
`integration/integration-zigbee/MODULE_CONTEXT.md` §Purpose, §Gotchas, the M9.3 §1 Freeze Types and the M9.4-RPT / LEARN-PERSIST paragraphs (grep them; the file is 206 KB — read by section, never whole) · `module-info.java` (verbatim in §4) · `EndpointClassifier.java` whole · `ClusterHandlers.java` whole · `ZigbeeClusterHandler.java` whole · `TemperatureMeasurementHandler.java` whole (the template) and its test · `IasZoneHandler.java:60–:80` · `ReportingConfigurator.java` whole · `ReportingOps.java` whole · `EzspReportingOps.java` whole (the encode/decode pairs and the await pattern) · `EzspCoordinatorProtocol.java:1993–:2040` (`readBasic`) · `ZclCodec.java:150–:215` · `ZclIngestionUnit.java:205–:270` (the two constructors and the learned-zone seed), `:515–:600` (the parse and dispatch) and `:650–:740` (`learnZoneType`, `handlersFor`, `invalidateHandlers`) · `ZigbeeIntegrationAdapter.java:370–:420` and `:970–:1030` · `ZigbeeDeviceCache.java:40–:120` and `:280–:320` and `:360–:410` (the JSON section names — the cache's on-disk schema is versioned; read how `learnedZoneTypes` is written and tolerated when absent) · `StandardCapabilities.java:300–:500` (the four capability factories) · `ZigbeeReportingDriveTest.java` whole (the FakeNcp idiom) · `ZigbeeHardwareFreeRig.java` whole (testFixtures) · `EndpointClassifierTest.java`, `ClusterHandlersTest.java`, `ReportingConfiguratorTest.java`, `ZigbeeDeviceCacheTest.java` (the shapes you extend).

## §3 Files to create or modify (the Files table governs)
| # | File | A/M | What |
|---|---|---|---|
| 1 | `integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/EndpointClassifier.java` | M | R1 `iasCapability` arm; R2 the cluster-first metering attach on EVERY arm (a helper `withMeters(Classification, EndpointDescriptor)` applied after the switch and after `fallback`); R6 the INFO line |
| 2 | `.../MeteringFormatting.java` | A | a package-private record: `(int powerMultiplier, int powerDivisor, int voltageMultiplier, int voltageDivisor, int currentMultiplier, int currentDivisor, Integer summationMultiplier, Integer summationDivisor, Integer unitOfMeasure)` with `Optional`-style presence per cluster (`hasElectrical()`, `hasMetering()`); zero multiplier/divisor ⇒ absent |
| 3 | `.../ElectricalMeasurementHandler.java` | A | R4: `CLUSTER_ID = 0x0B04`; `ActivePower` 0x050B → `power_w` = raw × mult / div (int16; 0x8000 invalid → skipped, F-9 debug); `RMSVoltage` 0x0505 → `voltage_v`; `RMSCurrent` 0x0508 → `current_a` (uint16; 0xFFFF invalid); constructed WITH a `MeteringFormatting` — never without |
| 4 | `.../MeteringHandler.java` | A | R4: `CLUSTER_ID = 0x0702`; `CurrentSummationDelivered` 0x0000 (uint48) → `energy_wh` = raw × mult / div × 1000 when `UnitOfMeasure` = 0x00 (kWh); any other unit → skipped with a debug naming the unit (no guess); constructed WITH a `MeteringFormatting` |
| 5 | `.../ClusterHandlers.java` | M | `forDevice(device, clock, zoneType, MeteringFormatting formatting)` — `formatting == null` or absent-per-cluster ⇒ that metering handler is NOT in the map (P6) |
| 6 | `.../ReportingOps.java` | M | ONE new seam method: `Optional<Map<Integer, Object>> readAttributes(IEEEAddress device, int endpoint, int clusterId, int[] attributeIds)` (the decoded map, as `ZclCodec.parseReadAttributesResponse` returns it) |
| 7 | `.../EzspReportingOps.java` | M | the implementation on the `readBasic` pattern (`encodeReadAttributes` + unicast + await on the protocol's lock; the interview's timeout constant or a named `READ_ATTRIBUTES_TIMEOUT_MILLIS`) |
| 8 | `.../ReportingConfigurator.java` | M | R3 + R5: for 0x0B04 / 0x0702 the row is NOT `DEFAULTS`' raw row — `configureDevice` first calls `readFormatting(device, endpoint)` (the two reads), records a `ReportingPostureFact` per cluster naming the read values (or `formatting_unreadable`), then configures `ActivePower` with change `round(1.0 × div / mult)`, min 5, max 600 and `CurrentSummationDelivered` with change `round(5 Wh in the unit's base × div / mult)`, min 5, max 3600; a cluster whose formatting is unreadable is NOT configured (a NONE-class posture, "formatting unreadable; reporting not configured") — no raw-threshold flood; the read formatting is handed back to the adapter (a return value or a listener) for the cache; `onRejoin` uses the CACHED formatting when present and reads only when absent |
| 9 | `.../ZigbeeDeviceCache.java` | M | `learnedMeteringFormatting` section (per IEEE → per endpoint → the record), `recordLearnedMeteringFormatting(ieee, endpoint, MeteringFormatting)`, `learnedMeteringFormatting()`; the JSON section tolerated when absent (an old cache loads clean — the LEARN-PERSIST tolerance) |
| 10 | `.../ZclIngestionUnit.java` | M | `handlersFor(device)` passes the device's learned formatting into `forDevice`; the seed at construction (a `Map<Long, Map<Integer, MeteringFormatting>>` from the cache) and a `recordLearnedMeteringFormatting` hook, mirroring the zone-type pair; `zigbee.metering_unscaled` DEBUG once per (device, cluster) when a metering report arrives with no formatting |
| 11 | `.../ZigbeeIntegrationAdapter.java` | M | `driveReporting` records the returned formatting into the cache and the ingestion seed BEFORE `invalidateHandlers` runs (the `onAdopted` listener already invalidates — order it so the formatting is present when the table is rebuilt); the constructor wires the seed/hook |
| 12 | `integration/integration-zigbee/MODULE_CONTEXT.md` | M | §8's two paragraphs |
| 13 | `.../src/testFixtures/.../ZigbeeHardwareFreeRig.java` | M | `reportAttributes(long ieee, int endpoint, int clusterId, Map<Integer, Object> attributes)` (a generic report-frame injector beside `reportOnOff`), `GEN4_IEEE/NWK/ENDPOINT` constants and a `announceGen4()` that scripts the Gen4 signature (EP1, 0x010A, the eight input clusters) |
| 14 | `.../src/test/.../EndpointClassifierTest.java` | M | T1 (three zone types → `binary_state`), T2 (the Gen4 signature → SWITCH + `on_off` + `power_meter` + `energy_meter`; the same with 0x0051; an endpoint with 0x0702 only → `energy_meter` only), T3 preserved rows untouched |
| 15 | `.../ElectricalMeasurementHandlerTest.java` | A | T5: divisor 1/10/100 × multiplier 1 on `ActivePower` 8000 raw; the invalid sentinel; voltage/current with their own formatting; a report of an attribute the handler does not map → empty |
| 16 | `.../MeteringHandlerTest.java` | A | T5: uint48 summation at divisor 1,000 / 1,000,000 / 3,600,000 → `energy_wh`; unit ≠ kWh → empty + debug |
| 17 | `.../ClusterHandlersTest.java` | M | P6: ten entries with formatting for both, nine with one, eight with none; the eight existing handlers unchanged |
| 18 | `.../ReportingConfiguratorTest.java` | M | T6 (the change field from scripted formatting, both clusters), T7's configurator half (unreadable formatting → not configured, the posture fact), the existing rows preserved |
| 19 | `.../ZigbeeReportingDriveTest.java` | M | T4: the FakeNcp scripted for the two reads BEFORE the configures (frame order asserted); the rejoin arm uses the cache (no read frame on rejoin) |
| 20 | `.../ZigbeeDeviceCacheTest.java` | M | P7: the round-trip; an old cache without the section loads |
| 21 | `.../ZclIngestionUnitTest.java` | M | T7's ingestion half: a 0x0B04 report for a device with no formatting → no `state_reported`, one `zigbee.metering_unscaled`; with formatting → `state_reported` with `power_w` |
| 22 | `.../MeteringRateMeasurementTest.java` | A | T8 / MEASURE-2 (§4 R7): gated by `-Dhomesynapse.measure2=true` (skipped otherwise, with a message); N and M from system properties with defaults 20 frames/s and 3 min; prints `MEASURE2 rate=<events/s> frames=<n> events=<n> dropped=<n> store_rows_before=<n> store_rows_after=<n> store_bytes_before=<n> store_bytes_after=<n> heap_before_mb=<n> heap_after_mb=<n> wall_s=<n>` |
| 23 | `nexsys-hivemind/context/planning/measurement-record.md` | M | the MEASURE-2 rows M2-1..M2-3 filled with the desk's numbers (the file exists with the rows empty; fill the values and the instrument column only) |

## §4 Technical specification
`integration/integration-zigbee/src/main/java/module-info.java`, verbatim at `3af6213` — **proposed diff: NONE**:
```
module com.homesynapse.integration.zigbee {
    requires transitive com.homesynapse.integration;
    requires com.fazecast.jSerialComm; // explicit JPMS module (ships module-info.class); interior-only per D-M92-1
    requires org.slf4j; // plain (implementation-only): Doc 08 §3.3 mandates structured log entries (LTD-15)
    requires com.fasterxml.jackson.databind; // plain (implementation-only): the M9.3 JSON profile loader + device cache; no Jackson type on any exported signature (the D-M92-1 pattern)

    exports com.homesynapse.integration.zigbee;
}
```
Every type this lane adds is package-private in `com.homesynapse.integration.zigbee`; `com.homesynapse.device.StandardCapabilities` is reachable through the transitive chain the module's own javadoc names; Jackson stays interior (the cache's record is serialized through the cache's existing writer, never on an exported signature).

**R1 (IR-15).** `iasCapability(ZoneType)`: `CONTACT → contact()`, `MOTION → motion()`, `WATER_LEAK, SMOKE, VIBRATION → binaryState()`. Nothing else in `binarySensor` changes; the handler's `active` key now has a schema that admits it.
**R2 (IR-24).** After the device-type switch and after `fallback`, apply `withMeters(classification, descriptor)`: if the endpoint's input clusters contain 0x0B04 add `powerMeter()`; if 0x0702 add `energyMeter()`; the entity type is unchanged by the meters (a metering switch stays SWITCH; a metering light stays LIGHT; an endpoint with a meter and no other actuator/sensor arm classifies as SENSOR-class with the meter capabilities only — `EntityType.SENSOR`, the type the module uses for measurement-only endpoints — `EndpointClassifier.java:146` (`temperatureSensor`) and `:199` (the fallback's sensor arm)). The device type remains a hint for the switch arms; no cluster set is inferred from a type.
**R3 (the formatting read).** In `ReportingConfigurator.configureDevice`, before iterating a metering cluster: `ops.readAttributes(device, ep, 0x0B04, {0x0604, 0x0605, 0x0600, 0x0601, 0x0602, 0x0603})` and `ops.readAttributes(device, ep, 0x0702, {0x0300, 0x0301, 0x0302, 0x0303})`, each ONE frame; build a `MeteringFormatting` (absent per cluster on a missing response, an UNSUPPORTED_ATTRIBUTE status on the multiplier or divisor, or a zero); record ONE posture fact per cluster whose `note` names the read values verbatim (`"formatting read: mult=1 div=100"`) or `"formatting unreadable; reporting not configured"`. The read happens on the adoption path and the rejoin path only when the cache holds nothing for that (device, endpoint).
**R4 (the handlers).** As the Files table rows 3–4 describe; both extend `ZigbeeClusterHandler`; both are constructed only with a present formatting for their cluster; `NormalizedAttribute.rawProtocolValue` carries the raw integer as a string and `rawProtocolUnit` the formatting (`"mult=1 div=100"`), so the record shows the observation beside the derived value (the provenance law: a derived value is never presented as an observation without its raw beside it).
**R5 (IR-25, sized).** `ActivePower`: change `round(1.0 × div / mult)` (never below 1), min 5, max 600. `CurrentSummationDelivered`: with `UnitOfMeasure` = kWh, change `round(0.005 × div / mult)` (5 Wh in the kWh base; never below 1), min 5, max 3600; with any other unit → not configured (the posture says why). The readback verification stays as HEAD's (`verifyReadback` compares the effective row). A profile override for these clusters (`effectiveRow`) is applied to min/max only; the change is always the scaled engineering value — say so in the MODULE_CONTEXT paragraph.
**R6.** One INFO at classification: `zigbee.endpoint_classified: device={} endpoint={} deviceType=0x{} inputClusters=[…] entityType={} capabilities=[…]` — emitted at the one call site, `ZigbeeAdoptionSlice.java:377–:378`.
**R7 (MEASURE-2 / IR-28).** On `ZigbeeHardwareFreeRig`: announce and adopt the Gen4 fixture with formatting scripted (div 100 / mult 1; seMetering div 1,000,000 / mult 1 / kWh); then for M minutes deliver N `ActivePower` reports per second (values alternating 8,000 / 8,010 raw so every report crosses the 1 W threshold) through `reportAttributes` + `deliverAndCycle`, on the rig's TestClock advanced 1/N s per frame (no wall clock); count `state_reported` published (the recording publisher), store rows and bytes before/after (the rig's store — name which store it is in the return: SQLite in a temp dir if the factory allows, else in-memory, disclosed), heap before/after (`Runtime` after a `System.gc()` hint — a proxy, disclosed), and wall time. Print the one `MEASURE2 …` line. This is a measurement: it passes when it prints.

## §5 Locked decisions and invariants that apply
LTD-17 (the module depends only on integration-api; no new `requires`) · LTD-15 / Doc 08 §3.3 (structured log lines, the `zigbee.` prefix) · Doc 02's 16 standard capabilities (no new capability; `power_meter` and `energy_meter` as declared) · Doc 08 §3.5 (the smart plug maps to `switch`) · INV-SA-03's spirit on the read side (the raw value beside the derived) · LEARN-PERSIST DP-LP-1/3/4 (the cache's learned section, debounced persistence, rehydrate-before-joins) · M9.4-RPT §2 (reporting outcomes never gate adoption) · THE ADOPTION FENCE (no plug joins any network before this lane is green; the lane runs entirely on the desk).

## §6 P2 consumer/pin survey
- `ClusterHandlers.forDevice` — ONE production caller: `ZclIngestionUnit.java:731` inside `handlersFor` (`grep -rn 'forDevice(' main` = the declaration + this hit; the tests are rows 17/21). The signature change is package-private.
- `EndpointClassifier.classify` — ONE production call site: `ZigbeeAdoptionSlice.java:377–:378` (the adoption-time classification, Stage 3; `grep -rn 'EndpointClassifier\.' main = 2 lines, one call); the R6 INFO goes there.
- `ReportingOps` — ONE implementation (`EzspReportingOps`) + ONE test fake (`ReportingConfiguratorTest`; `grep -rln 'implements ReportingOps\|new ReportingOps()' src` = those two) — the fake gains the new method (a `default` returning `Optional.empty()` on the interface keeps it compiling; T6 scripts it explicitly).
- `ReportingConfigurator.configureDevice`/`onRejoin` — the adapter's `driveReporting` only.
- The cache's JSON schema — `ZigbeeDeviceCacheTest` pins the sections; an old file without `learnedMeteringFormatting` must load (tolerance test, row 20).
- ArchUnit: `grep -n 'zigbee' app/homesynapse-app/src/test/java/com/homesynapse/app/HomeSynapseArchRules.java` = 0 hits — no rule names this package; `NO_DIRECT_TIME_ACCESS` reaches its production code (hence the app test task in the gate). Zero collisions.
- Shape tests: `grep -rl 'getDeclaredMethods().length' integration/integration-zigbee/src/test` = 0 files — no shape test pins the seam.
- The bench: untouched by this lane; `metering-known-load.yaml` (the bench lane) consumes `power_w` at the first rep.

## §7 Test requirements (sorted per format law #18)
| T | Class · test | Sorted | Asserts |
|---|---|---|---|
| T1 | `EndpointClassifierTest.iasWaterLeakSmokeVibration_classifyToBinaryState` | RED → GREEN | each of 0x002A/0x0028/0x002D → `binary_state` whose schema has `active`; CONTACT → `contact`, MOTION → `motion` unchanged |
| T2 | `EndpointClassifierTest.gen4Signature_switchWithMeters` (+ `_sameWithDeviceType0x0051`, `_meteringOnlyEndpoint`) | RED → GREEN | P3 |
| T3 | the existing classifier rows | GREEN-BY-CONSTRUCTION, disclosed | untouched |
| T4 | `ZigbeeReportingDriveTest.freshAdoption_readsFormattingBeforeConfigure` (+ `relink_usesCachedFormatting_noRead`) | RED → GREEN | frame order: two ReadAttributes (0x0B04, 0x0702) before any Configure Reporting on those clusters; the change fields per P4; on rejoin zero read frames |
| T5 | rows 15–16 | RED → GREEN (new classes; assert first, then implement) | the scaled values; the sentinels; the unit guard |
| T6 | `ReportingConfiguratorTest.meteringChange_scaledByReadFormatting` | RED → GREEN | change = 100 (0x0B04 at div 100) and 5,000 (0x0702 at div 1e6, kWh); at div 1 / mult 1: 1 and 1 (never below 1) |
| T7 | `ReportingConfiguratorTest.formattingUnreadable_notConfigured` + `ZclIngestionUnitTest.meteringReportWithoutFormatting_isSilent` | RED → GREEN | P5 |
| T8 | `MeteringRateMeasurementTest` | MEASUREMENT | the `MEASURE2` line; skipped without the property |
| T9 | every existing test in the module | GREEN-BY-CONSTRUCTION | the gate line |

## §8 MODULE_CONTEXT.md — two paragraphs
(1) **Metering by clusters (ENERGY-READ).** The classifier attaches `power_meter` (0x0B04) and `energy_meter` (0x0702) by the clusters present, the device type a hint; IR-15's zone map; the R6 log line. (2) **The formatting read and the scaled reporting.** At adoption the configurator reads the two clusters' multiplier/divisor attributes (one frame each), records them (cache section `learnedMeteringFormatting`; a posture fact per cluster), configures `ActivePower` at 1 W / 5–600 s and `CurrentSummationDelivered` at 5 Wh / 5–3600 s scaled by the read values, and the two handlers scale reports by them; unknown formatting ⇒ no configure, no scaled report, one debug — never a predicted divisor. The predicted frames/h table (§1) and its instrument (MEASURE-2; THE MEASUREMENT RECORD).

## §9 What to watch out for
> **Tests must inject `Clock`.** Do NOT use `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, or `System.currentTimeMillis()` in this module's test code. Use `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` injected via constructor/`@BeforeEach`. **Enforcement reach:** `NO_DIRECT_TIME_ACCESS` runs from `com.homesynapse.app`'s test classpath, so it mechanically catches PRODUCTION code in every non-whitelisted module (plus app's own tests) — it does **not** scan this module's test source set. Clock-injection here is a self-enforced project convention that PM review, not `./gradlew check`, enforces.

- MEASURE-2's wall time is measured with `System.nanoTime()` ONLY in the test's own timing lines, clearly labelled as the instrument (the convention above concerns production-path clocks; the rig's TestClock drives every frame timestamp).
- `Map.of` in `forDevice` has a ten-entry limit — ten entries fit exactly; if you need more, build a `LinkedHashMap` and `Map.copyOf` it.
- The uint48 summation is a `Long` from the decoder; scale in `double` only at the last step; `energy_wh` is a float attribute (`floatAttr`).
- The Configure Reporting change field is written at `analogChangeWidth(dataType)` bytes; a 0x0702 change of 18,000 fits; assert the little-endian bytes in T6, not only the integer.
- The formatting read happens INSIDE `configureDevice`, on the ingestion thread, synchronously (as the configures do) — two more round-trips per metering endpoint at adoption; name the timeout you use and its source.
- `energyMeter()` declares a `reset_meter` command and an EXACT_MATCH confirmation policy on `energy_wh`; the handler's `buildCommand` throws `UnsupportedOperationException` for it (the template's default) — say so in the MODULE_CONTEXT paragraph; a command path is out of scope.
- The TR3's z2m override (dossier row 29) suggests some units answer formatting the converter distrusts; this lane records what the device says and never overrides — the override, if ever, is a profile row in October.
- The dashboard mirror does not label `power_w`/`energy_wh` yet; the read API carries them as ordinary attributes (the keys are the device model's) — FE-116 is the mirror's row, not yours.

## §10 Coder pushback welcome
The seam for the formatting read (a method on `ReportingOps` vs a new `MeteringOps` interface); whether the formatting is better read in the interview (state your reason — the hub's is that an unadopted device must not be asked); the exact `EntityType` for a metering-only endpoint; the MEASURE-2 defaults (N = 20, M = 3 min) if the rig cannot sustain them on the desk. Evidence-based pushback is accepted; vague pushback is probed.

## §11 Out of scope
IR-26 (`initializationWrites`) · LINK-READ (IR-20's counter) · the TR3 quirk cluster 0xFF03 · `InstantaneousDemand` (int24 undecoded) · `reset_meter` · the dashboard labels (FE-116) · the bench scenario (`metering-known-load.yaml`, the bench lane) · any hardware act (THE ADOPTION FENCE) · IR-30/IR-31 (November).

## §12 Success criterion (binary) — three instruments, read and filed in §0
1. The gate line green in one round; `git status --porcelain` = the Files table; `git diff --stat -- '*/src/main/*'` = rows 1–11 only.
2. T1, T2, T4, T5, T6, T7 red at the baseline (each named with its red assertion pasted) and green after; T3/T9 green; `forDevice` sizes 8/9/10 (T17's three asserts pasted).
3. The `MEASURE2` line pasted verbatim, and rows M2-1..M2-3 of `context/planning/measurement-record.md` filled from it.

## §13 Work unit completion (WUCP Phase 1)
Your §4 checklist: files touched; the commands and tails; the three instruments; deviations by tag; `MODULE_CONTEXT` updated; coder-handoff NOT written (the hub files it); the next WU named (LINK-READ, hub-authored).

## §14 The dispatch line (Nick pastes into a host-side Claude Code session in `~/Desktop/Code/ClaudeFolder/homesynapse-core`)
```
date -u first. Boot as the nexsys-coder skill. Baseline: `git log -1 --format=%h` is 3af6213 and `git status --porcelain` is empty; stop and report otherwise. Execute nexsys-hivemind/context/instructions/2026-09-18_coder-lane_ENERGY-READ_metering-by-clusters_divisors-at-adoption_scaled-reporting_coding-instruction.md exactly: read its §0 contract, its premise table and §2 read-set first; adjudicate P1–P8 in your §0 card. Tests first, sorted as §0 says: T1/T2/T4/T5/T6/T7 written red, then green through §4 R1–R5 only; R6 the one INFO line; the eight existing handlers untouched; no new event type, no wire change, no module-info change, no device-model change; the formatting is READ at adoption and never predicted — an unknown formatting configures nothing and emits nothing. Run the gate line verbatim; then MEASURE-2 by path with -Dhomesynapse.measure2=true (tee to ../_scratch/v76/2026-09-18_MEASURE-2_rate.txt) and fill rows M2-1..M2-3 of nexsys-hivemind/context/planning/measurement-record.md. Stage nothing, commit nothing. Write nexsys-hivemind/context/audits/<today's CT date>_ENERGY-READ_return.md (≤26 KB, §0 card first, the three instruments as measured values) and end it, in the file and printed, with: RETURNED <path> <bytes>.
```
