<!--
file: context/instructions/2026-09-19_coder-lane_MEASURE-2b_read-path-at-run-scale_shared-real-core-fixture_coding-instruction.md
purpose: MEASURE-2b — the READ PATH at run-scale row counts on a REAL SQLite store, with the `BusSoakIT`/`HeroLoopHardwareFreeIT` boot shape extracted into ONE shared fixture; ENERGY-READ-b's two rows as row 0; the rig-blindness sweep as a § of the return. D-v76-10; the plan §17 (2)–(5); THE MEASUREMENT RECORD rows M2-2b + M2b-1..M2b-6.
audience: the Coder (a host-side Claude Code session in homesynapse-core; the Java slot) · the hub (the intake) · Nick (§14 only)
state-type: coding instruction (through THE PREMISE GATE — every source claim carries its instrument, grepped at `d1c2cbc` on Sat 2026-09-19)
status: EXECUTED — ACCEPT v77 b4; LANDED `a6b56d6` GREEN (v77 b5) (v78 beat 1 hygiene, Check 12; Mon 2026-09-21 ~07:5x CT; instrument 2026-09-21T12:51:15Z). Was: DISPATCH-READY v77 beat 2 (Sat 2026-09-19 ~14:2x CT; instrument 2026-09-19T19:16Z) — dispatched on Nick's paste of §14. Returns to `context/audits/<CT-date>_MEASURE-2b_return.md`. The Java slot is EXCLUSIVE to this lane until `M2B: RETURNED`; LINK-READ is chartered against the fixture at HEAD after this lands (D-v76-10).
baseline: core `d1c2cbc` (ENERGY-READ; CI #253 green). Re-verify at issue: `git --no-optional-locks log -1 --oneline` reads `d1c2cbc`; porcelain EMPTY.
-->

# MEASURE-2b — the read path at run-scale on a real store; ONE shared real-core fixture; ENERGY-READ-b row 0; the rig-blindness sweep

## §0 The lane contract (read first; every line binds)
- **Baseline:** core `d1c2cbc`; `git --no-optional-locks status --porcelain` EMPTY; anything else → stop and report. `date -u` FIRST; CT = UTC−5; every stamp you write derives from that reading.
- **Return path:** `nexsys-hivemind/context/audits/<CT-date>_MEASURE-2b_return.md`, one file, **≤ 14 KB** (3 KB + ~1 KB per Files-table row, 11 rows, rounded up — a CEILING, not a target; the §0 card is what the hub reads first; a shorter return with the same receipts is a better return). §0 card first (≤ 3 KB): the census · the measurement table (§12) as VALUES with the artifact each was read from · P1–P7 adjudicated · the deviations by tag · the rig-blindness §'s count · `RETURNED <path> <bytes>` as the LAST line, printed too.
- **Write-set (the Files table §3 governs; nothing else):** `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/` (the fixture, the two ITs, the new IT), `lifecycle/lifecycle/MODULE_CONTEXT.md`, `integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/{ZigbeeAdoptionSlice,EndpointClassifier}.java` + `EndpointClassifierTest.java` + that module's `MODULE_CONTEXT.md`, and `nexsys-hivemind/context/planning/measurement-record.md`. **NO production change outside row 0** (no persistence, automation, state-store, rest-api, event-model, module-info, build-logic or bench change). A read-path DEFECT you find is a FINDING in your return (a row of §12's table + a §11-style register row), never a fix in this lane.
- **Build discipline — the gate line, verbatim, green in ONE round before the return:** `./gradlew :lifecycle:lifecycle:test :integration:integration-zigbee:test :app:homesynapse-app:test spotlessCheck --offline` (the lifecycle ITs run in the default `test` task — `lifecycle/lifecycle/build.gradle.kts:93–:99`; the app test task carries `HomeSynapseArchRules`). In the gate the measurement IT runs at its DEFAULT (rows `2000`, §4 R4) — a smoke of the harness, seconds, not the measurement. **The measurement runs by path, once, on the desk, the knob an ENVIRONMENT VARIABLE (pm-lessons 2026-09-18 THE ENV-KNOB LINE; the `BusSoakIT` idiom `BusSoakIT.java:109/:224–:226` — the property, else the env; build-logic forwards ONE property, `homesynapse.soak.loops`, `build-logic/src/main/kotlin/homesynapse.java-conventions.gradle.kts:75–:76`, and no other):** `HOMESYNAPSE_MEASURE2B_ROWS=10000,100000,500000 ./gradlew :lifecycle:lifecycle:test --tests '*MeasureReadPathIT*' --rerun --offline -i 2>&1 | grep -E '^MEASURE2B |    MEASURE2B ' | tee ../_scratch/v77/2026-09-19_MEASURE-2b_readpath.txt`. A `-D` on the gradlew line reaches the Gradle JVM and never the forked worker. Scripts over ~15 lines are written with the Write tool and run by path — no long heredocs.
- **Tests first, honestly sorted (format law #18):** RED at the baseline and GREEN after — T1 (row 0: the call-site log carries `device=`), T2 (row 0: `withMeters` → `ENERGY_METER` for a 0x0702-only endpoint), T3 (row 0: `SENSOR` for a 0x0B04-only endpoint). GREEN-BY-CONSTRUCTION, disclosed — T4 (every `BusSoakIT` assertion, unchanged, on the fixture), T5 (every `HeroLoopHardwareFreeIT` assertion, unchanged, on the fixture), T6 (every existing `EndpointClassifierTest` row). A MEASUREMENT, neither red nor green — T7 (`MeasureReadPathIT`: it FAILS only if the harness cannot seed, time or restart; a slow query is a NUMBER, never a failure). Say which is which in your §0.
- **Predictions, pre-registered; adjudicate them first in your §0:** P1 — the census is the Files table exactly (11 = 9 M + 2 A; computed from the table's A/M column, never typed). P2 — after the extraction, `BusSoakIT.java` and `HeroLoopHardwareFreeIT.java` contain NO `new HomeSynapseCore(` and NO `new ZigbeeHardwareFreeRig(` (grep count 0 each); `RealCoreFixture.java` contains exactly one of each. P3 — T1–T3 are RED at `d1c2cbc` (T1 on the absent `device=` in the classify-site line; T2/T3 on `withMeters`' current result for the two synthetic signatures) and GREEN after. P4 — the seeding rate on SQLite is BELOW MEASURE-2's in-memory `24,031.1` events/s (M2-1) — the store's fsync/WAL path is the difference; the value is the measurement. P5 — `explainRun` (`readByCorrelation`, indexed `idx_events_correlation`) does NOT grow with N (its median at 500k within 3× its median at 10k); `explainNonFiring` and `listAutomations` DO grow with the count of `automation_completed` events (they walk `lastMatching` → `scanType` ascending over `idx_events_type`, `StandardExplanationService.java:1060–:1090`), not with N as such — the shape is a linear growth in the RUN count, flat in the report count. P6 — the store's bytes per row are between 300 and 900 B including the WAL's share (the b6 estimate "300–600 B" is the prior; the measurement decides). P7 — a restart of the core at 500k rows reaches `awaitRuntimeSubscribersLive` in under 30 s (the state projection resumes from `view_checkpoints`, `AtomicCheckpointSink.java:28–:66`; it does not replay from zero). State each as MET / MISSED with the number.
- **Report-back:** the last line of your return, printed too: `RETURNED nexsys-hivemind/context/audits/<CT-date>_MEASURE-2b_return.md <bytes>`. Nick pastes `M2B: RETURNED <path> <bytes>` to the hub. Your transcript, if the hub wants it, goes to `_scratch/v77/MEASURE-2b_transcript.md` — never into chat.
- **The cap sentence:** ≤ 14 KB is a CEILING, not a target.

## §1 What this implements
Nick's re-cut of the Java slot (Fri 09-18; D-v76-10 ADOPTED with one edit; the plan §17 (2)–(5)): the run completing and the run being readable are different outcomes and only the first is instrumented. No test runs the system at 100k–500k events on the real store; the explainability projections walk the log (`StandardExplanationService.java:97–:115` — `SCAN_BATCH = 500`, one paging helper `scanType`). This lane (1) extracts the boot shape that `BusSoakIT.java:309–:342` copied from `HeroLoopHardwareFreeIT.java:383–` ("Harness — the HeroLoopHardwareFreeIT boot shape, copied", `BusSoakIT.java:305–:307`) into ONE class the three ITs call, so every later scale question is a method call; (2) seeds a real core on a temp-dir SQLite store to N events through the scripted rig's `reportAttributes` (`ZigbeeHardwareFreeRig.java:228`) and TIMES the hero's four reads at 10k / 100k / 500k rows; (3) records bytes on disk, the seeding rate on SQLite, and the restart time at N as by-products; (4) lands ENERGY-READ-b's two rows (the b5 audit §5) as row 0; (5) sweeps the rig for blindness (§11) and files the findings as register rows in the return.

## §2 Files to read before starting (the minimum read set — mandatory)
`lifecycle/lifecycle/MODULE_CONTEXT.md` (§Gotchas whole; the M4.0a wiring note) · `integration/integration-zigbee/MODULE_CONTEXT.md` (the ENERGY-READ paragraphs; the classifier's contract) · `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/BusSoakIT.java` WHOLE (419 lines: the env/property knob `:109/:224–:226`; the `bus.soak:` tee `:249–:254`; `bootAndAdopt` `:309`; `awaitRuntimeSubscribersLive` `:355`; `label` `:345`) · `HeroLoopHardwareFreeIT.java` WHOLE (768 lines; `bootAndAdopt` `:383`; the five call sites `:111/:211/:269/:315/:363`) · `integration/integration-zigbee/src/testFixtures/java/com/homesynapse/integration/zigbee/ZigbeeHardwareFreeRig.java` (the constructor `:130`; `factory()` `:150`; `announce` `:161`; `reportAttributes` `:228–:240`; `deliverAndCycle` `:259`; `adopt` `:270`; the constants `HUE_IEEE :53`, `SNZB_IEEE :56`, `GEN4_IEEE :68`, `GEN4_ENDPOINT :70`) · `core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java` (`listRuns :167`, `explainRun :211` → `readByCorrelation :221`, `explainNonFiring :259`, `listAutomations :456`, `lastMatching :1083`, `scanType :1060`, `readByTimeRange :1146`) · `core/persistence/src/main/java/com/homesynapse/persistence/SqliteEventStore.java` (the read methods `:579/:600/:632/:647/:672/:710`) · `core/persistence/src/main/resources/db/migration/events/V001__initial_event_store_schema.sql` (`:57–:62` the indexes) · `core/persistence/.../RetentionPolicy.java:46` (`SOURCE_DEFAULT`) · `api/rest-api/src/main/java/com/homesynapse/api/rest/ListEntitiesEndpoint.java` and `GetEntityStateEndpoint.java` (what the dashboard's state read calls — time THOSE service calls, not HTTP) · `integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/ZigbeeAdoptionSlice.java:370–:385` and `EndpointClassifier.java:110–:200` (row 0) · `integration/integration-zigbee/src/test/java/.../EndpointClassifierTest.java` (the test home for T2/T3; law #13) · `nexsys-hivemind/context/planning/measurement-record.md` (§1 rows M2-1..M2-3, M2-2b; the column header `:14`) · `nexsys-hivemind/context/audits/2026-09-18_v76-b5_ENERGY-READ_intake_two-layer-audit.md` §5 (row 0's two rows, verbatim).

## §3 Files to create or modify (the Files table governs; every write site is a row)
| # | File | A/M | What |
|---|---|---|---|
| 1 | `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/RealCoreFixture.java` | A | THE ONE SHARED FIXTURE (package-private, final): `static RealCoreFixture boot(Path tempDir, String configYaml)` — the exact sequence of `BusSoakIT.bootAndAdopt` `:309–:342` (TestClock · config dir + yaml · the rig · `new HomeSynapseCore(...)` with `HomeSynapseConfig.testing()` · `start()` · `registerIntegrationSchema` · `awaitRuntimeSubscribersLive` · `awaitTrue(rig::sessionStarted)` · announce/deliver/adopt SNZB + HUE · `awaitRegistryProjectionCaughtUp` · the two labels); accessors `core()`, `rig()`, `clock()`, `snzbEntity()`, `hueEntity()`, `tempDir()`; `Map<Integer, EntityId> adoptGen4()` (announce + deliver + adopt `GEN4_IEEE`, the ENERGY-READ path); `long seedPowerReports(int count, int watts0)` (count `reportAttributes(GEN4_IEEE, GEN4_ENDPOINT, 0x0B04, Map.of(0x050B, w))` with w alternating `watts0`/`watts0+1`, `deliverAndCycle()` every 100 reports, then await `latestPosition()` ≥ the expected count; returns the elapsed nanos of the seeding — the rate is count / elapsed); `void pulseMotion()` (one SNZB motion report that fires the hero — the run maker); `long restart()` (`core.stop()`; boot again on the SAME db path and config; returns the nanos from the constructor to `awaitRuntimeSubscribersLive`); `long storeBytes()` (the db file + `-wal` + `-shm` sizes summed) and `long dbBytes()` (the db file alone); `long rows()` (`eventStore().latestPosition()`); `void close()`. The awaits and `label` move here from the two ITs. Javadoc: "the three ITs' boot shape, once". |
| 2 | `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/BusSoakIT.java` | M | `bootAndAdopt`, `label`, `awaitRuntimeSubscribersLive`, `awaitRegistryProjectionCaughtUp` and the `subscriberMode` helper DELETED here and replaced by `fixture = RealCoreFixture.boot(tempDir, heroMotionConfigYaml())`; every assertion unchanged (T4). |
| 3 | `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/HeroLoopHardwareFreeIT.java` | M | the same extraction at `:383`; the five call sites; every assertion unchanged (T5). |
| 4 | `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/MeasureReadPathIT.java` | A | THE MEASUREMENT (T7; §4 R4–R8): for each N in the knob, boot on a fresh temp dir, adopt the Gen4, seed to N with one `pulseMotion()` per 1,000 reports (runs ≈ N/1,000), then time the six reads (§4 R5) k=5 each; print ONE `MEASURE2B` line per N (§4 R7); restart and time it; close. The default knob `2000` (the gate's smoke). |
| 5 | `integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/ZigbeeAdoptionSlice.java` | M | row 0 / row 1: the call-site log at the `EndpointClassifier.classify` call (`:377–:378`) — an INFO line `zigbee.endpoint_classified: device={} endpoint={} entityType={} capabilities={}` with `device=` (the IEEE as the adjacent `zigbee.device_proposed: device=` prints it, `:274`); the classifier's own R6 line kept or demoted to DEBUG (say which). |
| 6 | `integration/integration-zigbee/src/main/java/com/homesynapse/integration/zigbee/EndpointClassifier.java` | M | row 0 / row 2: in `withMeters` (`:172`), an otherwise-empty classification whose endpoint lists 0x0702 → `EntityType.ENERGY_METER` (`EntityType.java:106`); 0x0B04 only → `SENSOR`; one line each; the ruling is the b5 audit R-5. |
| 7 | `integration/integration-zigbee/src/test/java/com/homesynapse/integration/zigbee/EndpointClassifierTest.java` | M | T2 + T3 (two synthetic signatures: `{0x0000, 0x0702}` → ENERGY_METER; `{0x0000, 0x0B04}` → SENSOR); T1 lives in `ZigbeeAdoptionSliceTest.java` ONLY if that test already captures the slice's logger — grep it first; if it does not, T1 is a `ListAppender` on `ZigbeeAdoptionSlice`'s logger added to `EndpointClassifierTest`'s sibling of your choice and the row says so (law #30 (d): the capture point). |
| 8 | `integration/integration-zigbee/MODULE_CONTEXT.md` | M | two lines under the ENERGY-READ paragraph: the classify-site line's shape; `withMeters`' two new arms. |
| 9 | `lifecycle/lifecycle/MODULE_CONTEXT.md` | M | one paragraph: `RealCoreFixture` — the three ITs' boot shape, its methods, the knob, the `MEASURE2B` line's fields; a gotcha: the fixture is test-tree-only (not a testFixtures source set) until a second module needs it. |
| 10 | `nexsys-hivemind/context/planning/measurement-record.md` | M | M2-2b FILLED (bytes/rows at 500k; the growth per hour computed as bytes_per_row × the run's rate — state the rate you assume, from the plan §14: three plugs at the scaled thresholds, and the b6 line "under 10k events for the run"); NEW rows M2b-1 (the seeding rate on SQLite, events/s), M2b-2 (`explainRun` ms at 10k/100k/500k), M2b-3 (`explainNonFiring`), M2b-4 (`listRuns` page of 20), M2b-5 (`listAutomations` + the dashboard's state read — the `ListEntitiesEndpoint`'s service calls), M2b-6 (the restart to subscribers-live at N). Every row: value · instrument (`MEASURE2B` line · field) · date (CT) · sha. |
| 11 | `nexsys-hivemind/context/audits/<CT-date>_MEASURE-2b_return.md` | A | the return (§0 card; the measurement table; P1–P7; the rig-blindness § with its register rows; deviations). |

## §4 Technical specification
- **R1 (the extraction is byte-faithful).** The fixture's `boot` performs the SAME calls in the SAME order as `BusSoakIT.bootAndAdopt` at `d1c2cbc`; the `HomeSynapseCore` constructor arguments are `(tempDir.resolve("homesynapse-events.db"), configDir, HomeSynapseConfig.testing(), clock, TEST_HOME_ID, null, List.of(rig.factory()))` (`BusSoakIT.java:318–:325`). If the two ITs' copies DIFFER anywhere (a diff of the two `bootAndAdopt` bodies is your first act — file it in the return), the fixture takes `HeroLoopHardwareFreeIT`'s form and the difference is a deviation row.
- **R2 (the seed is the run's mix).** Reports are `0x0B04` `ActivePower` (attribute `0x050B`) from the rig's Gen4 (`GEN4_IEEE`, endpoint `GEN4_ENDPOINT`), the ENERGY-READ handler's path, producing `state_reported` on the plug's entity (`power_w`). The hero (motion → light) is NOT triggered by them; `pulseMotion()` every 1,000 reports makes the runs. Verify the first seeded report lands as `state_reported` with `power_w` (read the store) before seeding to N.
- **R3 (the store is real).** SQLite in the temp dir; WAL as production configures it; nothing in-memory. Bytes: the db file, `-wal`, `-shm`, each named in the line.
- **R4 (the knob).** `HOMESYNAPSE_MEASURE2B_ROWS` — a comma list of N; absent → `2000` (the gate's smoke). Read once via `System.getenv`; log the value in the first `MEASURE2B` line.
- **R5 (the six reads, each k=5, min/median/max ms, `System.nanoTime()` around the call — the arch reminder §9 applies to `Clock`, not to a measurement's stopwatch; say so in a comment):** (q1) `explainRun(lastRunId)` — the last run's id from `listRuns(Optional.empty(), Long.MAX_VALUE, 1)`; (q2) `explainNonFiring(heroAutomationId, 0L)`; (q3) the causal chain — `eventStore().readByCorrelation(lastRun's correlationId)` (the chain `explainRun` reads at `:221`); (q4) `listRuns(Optional.empty(), Long.MAX_VALUE, 20)`; (q5) `listAutomations()`; (q6) the dashboard's state read — `StateQueryService.getSnapshot()` + the registry list, as `ListEntitiesEndpoint` reads them (the snapshot is in-memory: P5's flat shape is EXPECTED here; the number says whether the join grows with the entity count, which is 3–4 in this fixture). Obtain the services the way the composition root does: the IT builds `ExplanationService.over(core.eventStore(), core.automationRegistry())` exactly as `HomeSynapseCore.java:1051–:1052` does (`ExplanationService.java:103`; `eventStore()` `:1127`; `automationRegistry()` as `HeroLoopHardwareFreeIT.java:515` calls it); q6 = `core.stateQueryService().getSnapshot()` (`HomeSynapseCore.java:1145`; `ListEntitiesEndpoint.java:74` — the lock-free materialized read) joined with `core.entityRegistry()`'s list the way the endpoint joins them (read `ListEntitiesEndpoint.java:100–:140`).
- **R6 (the by-products).** After seeding: `rows=` (`latestPosition()`), `db_bytes=`, `wal_bytes=`, `shm_bytes=`, `bytes_per_row=` (total/rows), `seed_events_per_s=`. Then `restart_ms=` (R1's `restart()`), and the six reads AGAIN after the restart (the cold read: `q1_cold_ms=` … `q6_cold_ms=`, k=1) — the warm/cold pair is the number the run's operator will feel.
- **R7 (the line).** Exactly one line per N, greppable: `MEASURE2B rows=<n> runs=<r> seed_events_per_s=<f> db_bytes=<b> wal_bytes=<b> shm_bytes=<b> bytes_per_row=<f> q1_ms=<min/med/max> q2_ms=… q3_ms=… q4_ms=… q5_ms=… q6_ms=… restart_ms=<n> q1_cold_ms=<n> … q6_cold_ms=<n>`; the first line of the run: `MEASURE2B knob=<the env value or default> sha=d1c2cbc`. Printed with `System.out.println` (the `bus.soak:` idiom); the JUnit XML's `<system-out>` carries it (law #24: the artifact is the XML under `lifecycle/lifecycle/build/test-results/test/` AND the tee'd file under `_scratch/v77/`).
- **R8 (no fix).** A read that is slow, or grows with N where P5 says flat, is a FINDING with the number; the fix is a later WU the hub charters on the number. A read that THROWS at N is a defect row and the line still prints for the other reads.
- **R9 (row 0).** Rows 5–7 of the table exactly as the b5 audit §5 states them; the R6 line's `device=` uses the same formatting as `zigbee.device_proposed`'s (`:274`).
- **Module-info:** NONE changes. `lifecycle`'s test tree already reaches the zigbee testFixtures (`build.gradle.kts:98–:99`) and `com.homesynapse.persistence`/`automation` are `requires` of `com.homesynapse.lifecycle` (its `module-info.java` verbatim: `requires transitive com.homesynapse.persistence; … requires com.homesynapse.automation;`). If a service accessor you need is NOT exposed by `HomeSynapseCore`, STOP: that is a design question for the hub (pushback §10), not a new getter.

## §5 Locked decisions and invariants that apply
D-v76-10 (the fixture; LINK-READ second); LTD-17 (the zigbee module cannot reach SQLite — the lifecycle module is the instrument, M2-2b's own row); INV-SUB-ISO-02 / AMD-26/27 (the subscriber read executor: reads through the store's own connection discipline — call the services, never open a second connection); the arch rule `NO_DIRECT_TIME_ACCESS` (production code only; §9); the freeze v1.1.5 (no response shape changes — none are touched). AMD-44 (composition validation) stays deferred; row 2 is one classifier arm, not validation.

## §6 P2 consumer/pin survey
- `bootAndAdopt` consumers: `BusSoakIT.java:144` and `HeroLoopHardwareFreeIT.java:111/:211/:269/:315/:363` — all become fixture calls. No other caller (`grep -rn bootAndAdopt --include=*.java` at `d1c2cbc` → these six lines).
- `EndpointClassifier.withMeters` callers: `EndpointClassifier.java:122` only (grep). `classify` callers: `ZigbeeAdoptionSlice.java:378` only.
- ARCH-RULE-REACH: no module boundary is crossed; the fixture is lifecycle-test-internal.
- The miss-script sweep: row 2 adds arms AFTER the existing metering classification; the existing rows (SNZB-02P, the Hue light, the occupancy sensor, the Gen4 SWITCH+meters) must classify identically — T6 is the preservation fixture (law #18 carve-out).

## §7 Test requirements (sorted per format law #18)
RED→GREEN: T1, T2, T3. GREEN-BY-CONSTRUCTION (disclosed): T4, T5, T6. MEASUREMENT: T7. T7's assertions are HARNESS assertions only: the first seeded report is a `state_reported` with `power_w`; `rows()` ≥ N after seeding; each read returns non-empty at N ≥ 2,000; the restart reaches subscribers-live. No timing assertion.

## §8 MODULE_CONTEXT.md — two edits
As rows 8 and 9 state. Nothing else.

## §9 What to watch out for
- **Tests must inject `Clock`.** Do NOT use `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, or `System.currentTimeMillis()` in this module's test code. Use `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` injected via constructor/`@BeforeEach`. **Enforcement reach:** `NO_DIRECT_TIME_ACCESS` runs from `com.homesynapse.app`'s test classpath, so it mechanically catches PRODUCTION code in every non-whitelisted module (plus app's own tests) — it does **not** scan this module's test source set. Clock-injection here is a self-enforced project convention that PM review, not `./gradlew check`, enforces. **The one carve-out this instruction grants:** `MeasureReadPathIT`'s stopwatch is `System.nanoTime()` around each timed call, in ONE private helper named `stopwatchNanos`, with the comment `// MEASURE-2b stopwatch: wall time of a read; not domain time (the domain clock is the fixture's TestClock)`. Nowhere else.
- The fixture's `TestClock.createDefault()` is the domain clock the core and the rig share (`BusSoakIT.java:311`); seeding 500k reports advances nothing unless the rig's `deliverAndCycle` does — read `:259` and say in the return what the clock read at the end of seeding.
- Seeding speed: 500k `reportAttributes` calls through `queuedCallbacks` — deliver in batches of 100 (`deliverAndCycle()` per batch) and await the store's position, never the bus's counters; if 500k does not complete in 10 minutes, cap at the N reached, print the line for it, and file the cap as a deviation with the rate.
- `explainNonFiring`'s second parameter is `expectedSincePosition` (`:259–:260`); 0L means "since the beginning" — the widest walk; say so in the return.
- The gate's smoke (2,000 rows) must finish in under 60 s or the default N drops to 500 (a deviation row).
- The lifecycle MODULE_CONTEXT is 99 KB — edit by exact-once replace at the ENERGY-READ/M9.4a paragraphs, never re-write it.

## §10 Coder pushback welcome
A service accessor missing on `HomeSynapseCore`; a read whose k=5 at 500k exceeds 60 s (cap it, file it); the `ListEntitiesEndpoint`'s calls being HTTP-layer-only (then q6 = `entityRegistry().listEntities()` + the state read per entity, and say so); a `bootAndAdopt` difference between the two ITs.

## §11 Out of scope — and the rig-blindness sweep (a § of the RETURN, ≤ 2 KB, no code)
Out of scope: any read-path fix; retention; LINK-READ; the bench; the dashboard. **The sweep:** grep the rig and `FakeNcp` (`integration/integration-zigbee/src/testFixtures/`) for every place they ENCODE or DECODE a frame with a helper production also uses (`wireType`, `wireValue`, `attributeRecord`, `report`, the EZSP/ZCL codecs) — the class of the uint48 defect (the b5 audit: the rig encoded the reportable-change field with the same helper HEAD used, so the rig could not see the defect). For each shared helper: the rig-side call site, the production-side call site, and whether a defect in it would be invisible to the rig (yes/no + why). Findings as improvement-register rows in your return (`IR-<next> | <surface> | <finding> | <the instrument that would catch it>`); the hub lands them.

## §12 Success criterion (binary) — the measurement table, read and filed in §0
| Row | Value | Artifact · field |
|---|---|---|
| the gate line green in one round | y/n | the gradle summary line |
| P2 grep counts (0 · 0 · 1 · 1) | the four counts | `grep -c` output |
| `MEASURE2B` lines at 10k / 100k / 500k | three lines verbatim | the tee'd file + the JUnit XML `<system-out>` |
| q1..q6 median ms at each N (warm) and the cold read after restart | 18 numbers | the lines |
| `bytes_per_row`, `seed_events_per_s`, `restart_ms` at 500k | three numbers | the lines |
| the rig-blindness § | the count of shared helpers and of blind ones | your return §11 |

## §13 Work unit completion (WUCP Phase 1)
The return; `coder-handoff.md` entry (the fixture's existence; the six reads' numbers in one line); the measurement record's rows; the two MODULE_CONTEXT edits. Commit NOTHING — the tree is Nick's hands; your last act is the `RETURNED` line.

## §14 The dispatch line (Nick pastes into a host-side Claude Code session in `~/Desktop/Code/ClaudeFolder/homesynapse-core`)
```
date -u first. You are the Coder for MEASURE-2b (the nexsys-coder skill governs). Read ../nexsys-hivemind/context/instructions/2026-09-19_coder-lane_MEASURE-2b_read-path-at-run-scale_shared-real-core-fixture_coding-instruction.md WHOLE, then its §2 read set. Baseline d1c2cbc, porcelain empty, or stop. Tests first, sorted as §0 says; the gate line verbatim, green in one round; the measurement by path with the ENV knob (§0), tee'd to ../_scratch/v77/2026-09-19_MEASURE-2b_readpath.txt; the rig-blindness sweep as §11 of your return; commit nothing. Return to ../nexsys-hivemind/context/audits/<CT-date>_MEASURE-2b_return.md, ≤ 14 KB, §0 card first, P1–P7 adjudicated, the last line `RETURNED <path> <bytes>` printed too.
```
