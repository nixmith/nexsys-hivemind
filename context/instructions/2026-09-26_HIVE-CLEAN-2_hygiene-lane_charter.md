<!--
file: context/instructions/2026-09-26_HIVE-CLEAN-2_hygiene-lane_charter.md
purpose: HIVE-CLEAN-2 — the record's hygiene pass Nick asked for at the v80 close ("very meticulously and surgically clean up all context throughout nexsys-hivemind … and the rest of ClaudeFolder"): a fresh, read-and-move lane that re-statuses every file whose status is stale or missing, moves the executed and the one-off out of the live directories into the archive subtrees, tidies the ClaudeFolder root and _scratch, and returns a census-exact card. The lane DELETES NOTHING and EDITS NO BODY: a status line added or replaced (its prior value kept after `Was:`), a `git mv`, a `mv -n` outside git. The rows below were measured by the hub at 2026-09-26T03:5xZ (the instruments in §1); the lane re-measures each before it acts.
audience: the hygiene lane (a fresh Cowork conversation with ClaudeFolder connected) · Nick (one paste to dispatch; one card to land) · the v81 hub (boots after the landing)
state-type: lane charter (hygiene; read-and-move)
status: DISPATCH-READY — cut at the v80 close (Fri 2026-09-25 ~22:5x CT; instrument 2026-09-26T03:5xZ). Return: `context/audits/2026-09-26_HIVE-CLEAN-2_return.md` (≤ 8 KB, §0 first). The card: ONE hivemind commit (renames show as R in `--name-status`) + the ClaudeFolder-root moves listed for Nick's eye.
-->

# HIVE-CLEAN-2 — the hygiene lane

## §0 The lane contract
- **You are a hygiene lane, not the hub.** You never write the spine (`pm-handoff.md`, `PROJECT_SNAPSHOT.md`, `OPERATOR-BRIEF_for-Nick.md`, `coder-handoff.md`), the stable prompt, the newest dispatch text (`2026-09-26_v81_dispatch-text.md`), the newest decision record (`2026-09-23_v80_decision-record.md`), the plan of record (`2026-09-15_v75_PROGRAM-PLAN_…`), `improvement-register.md`, `pm-lessons.md`, or any skill tree (`project-manager/`, `coder/`, `nexsys-skills/`). You never delete. You never edit a body. You never touch the other four repos.
- **Two verbs only:** (a) a status line — `status: <TERMINAL> — HIVE-CLEAN-2 (2026-09-26). Was: <the old line verbatim>` replacing the first `status:` line, or inserted as the last frontmatter line when the file has a frontmatter and no status, or as one HTML comment line at the very top when it has no frontmatter; (b) a move — `git mv` inside the hivemind; `mv -n` (never plain `mv`) outside git, verified by `ls` after.
- **The archive convention is the invariant** (the PM preflight Check 12): a live directory shows only what is current; everything executed, superseded, returned or historical carries a terminal status or lives under `archive/`. Terminal statuses: `EXECUTED · SUPERSEDED · ARCHIVED · FILED · RETURNED · HISTORICAL · CLOSED · RETIRED`.
- **Every act is re-measured first** (`grep -m1 '^status:'`, `git ls-files`, `ls`), then done, then asserted (`git status --porcelain -uall`, `wc -l`); the return lists every file touched with `from → to` or `Was: → now`.
- **Read by range; never a file whole.** Your reads are `grep -n` and `sed -n 'a,bp'` on the first 12 lines of a file (its frontmatter); nothing below the frontmatter is your business.
- **THE PRIOR-LEDGER GATE:** HIVE-CLEAN-1 (`context/instructions/2026-09-07_HIVE-CLEAN-1_hygiene-lane_dispatch.md`, §4 the exclusion lists as run) is the prior record — read its §0 and §4 first; carry its exclusion rule: the lanes the newest spine beat records as RUNNING or DISPATCH-READY and today's-date files are never re-statused.
- **The census card is yours to prepare, Nick's to run:** `git add -A` is refused; the card stages by explicit paths (renames: both sides), checks `git diff --cached --name-status | wc -l` against your N, greps the message for `Co-Authored\|Claude-Session` (must be 0), commits with `-F ../_scratch/hygiene/2026-09-26_hivemind_HIVE-CLEAN-2_commit-msg.txt`, pushes. Nick says back `HIVE: LANDED <sha>`.

## §1 The census as the hub measured it (2026-09-26T03:5xZ; re-measure before acting)
- Tracked files by live directory: `context/audits` 319 (5.0 MB) · `context/strategy` 105 (7.2 MB; 22 non-md — 11 docx, 6 pdf, 5 txt — the counsel package's exhibits and the search packets: EVIDENCE, keep) · `context/assessments` 68 · `context/planning` 51 (+26 archived; 8 decision records live) · `context/instructions` 53 (+135 archived; 40 EXECUTED · 2 RETURNED · 1 SUPERSEDED · 1 RULED · 1 PARTIAL · 2 "PART…" · 1 FILED · this charter) · `context/research` 46 · `context/handoff` 24 (+245 archived) · `context/programs` 15 · `context/decisions` 15 · `context/process` 14 · `context/pre-verifications` 8.
- Status classes on live `context/**/*.md`: FILED 177 · RETURNED 56 · EXECUTED 44 · **`<none>` 41** · DELIVERED 39 · **CURRENT 33** · COMPLETE 30 · REFERENCE 27 · FILED. 14 · HISTORICAL 11 · CLOSED 11 · **PAUSED 10 · LIVE 10 · CHARTERED 10 · AUTHORED 10** · RATIFIED 7 · **v1 5** · RULED 4 · **DRAFT 4** · **ARMED 2** (bold = the classes this pass re-statuses or confirms).
- The handoff root (24 live): the stable prompt · `pm-handoff.md` · `OPERATOR-BRIEF_for-Nick.md` · `coder-handoff.md` · `cross-agent-notes.md` (RETIRED CHANNEL, 1 KB) · the v81 text (LIVE) · nine EXECUTED dispatch texts (v66 · v67 · v71 · v75 · v76 ×2 · v77 · v78 · v79 · v80) · seven one-offs (2026-07-19 matter prompt · 07-21 and 07-25 bench session reports · 08-28 NFCU return · 08-28 pelton card (ARMED) · 09-06 BLOCK6 card · 09-13 R-4c prompt · 09-17 v76-b2 Nick brief).
- The ClaudeFolder root (outside git): `2026-08-31 ZENDOMO Search Summary.pdf` · `PALOKI search opinion 20260922.pdf` · `TAMORO_Trademark_Strategy_Memo.docx` · `TM Search_PALOKI_(Word Search)_20260918.docx` · `TM Search_ZENDOMO_(Word Search)_20260821.docx` · `.xlsx` · `Founder_Operating_Plan_v1.md` and `Technical_Roadmap_v1.md` (both banner themselves unmaintained; the plan of record outranks them — the v79 b5 audit §2) · `START_HERE.md` · `Claude outputs/` · `_archive/` · `_scratch/` (h3 100 MB · _transfer 41 MB · v66 20 MB · v70/v71 19 MB each · a `.tgz` 7 MB · v76–v80 small).
- Skills: the three SOURCE trees matched their synced copies 28/28 at the v80 boot; `pm-lessons.md` carries **11 OPEN mints** since the ledger's last fold → W-SKILLS-10 is owed (NOT this lane — §3 R11).

## §2 The rows (each: the instrument → the act → the assert)
- **R1 — the 41 `<none>` files** (returns, operator records, pre-verifications, `context/status/READ-ME-FIRST.md`, one research return, the PALOKI search packet .md): `grep -L '^status:'` over live `context/**/*.md` → add `status: FILED — historical record (HIVE-CLEAN-2, 2026-09-26)` (a return/record) or `status: REFERENCE — (HIVE-CLEAN-2, 2026-09-26)` (READ-ME-FIRST, if its text is still true; else FILED with a note) → the count of `<none>` is 0 after.
- **R2 — the live-sounding statuses on finished work:** for each file in the classes ARMED (2) · AUTHORED (10) · CHARTERED (10) · CURRENT (33) · PAUSED (10) · DRAFT (4) · v1 (5): read its date and its first 12 lines; if the spine or an audit records its act as done (a charter with a `_return.md` beside it; a card with an EXECUTED beat; an assessment older than the plan of record, 2026-09-15), set `EXECUTED` / `HISTORICAL` / `SUPERSEDED` with `Was:`; if the file is still the current form of something (a PAUSED program the plan names; a reference the map cites), set nothing and list it in the return as CONFIRMED-LIVE with the line that proves it. The hub's expectation: ≥ 60 of the 74 re-statused; every CONFIRMED-LIVE named.
- **R3 — the handoff root:** `git mv` the nine EXECUTED dispatch texts → `context/handoff/archive/dispatch-texts/`; the seven one-offs → `context/handoff/archive/one-off/` (the pelton card's `ARMED` → `EXECUTED — Was: ARMED …` first; the R-4c prompt and the v76-b2 brief FILED). After: the root holds the stable prompt, the four spine files, the RETIRED channel file, and the v81 text — seven files. `cross-agent-notes.md` stays (Check 8 reads it).
- **R4 — `context/instructions/`:** every file whose status is EXECUTED / SUPERSEDED / RETURNED / FILED / RULED / PART… and whose date is before 2026-09-24 → `git mv` to `context/instructions/archive/` (the 135 there already; keep the flat layout). The THURSDAY ORDER files (2026-09-24/25, EXECUTED / PARTIAL), the scripts directory, and this charter stay live. After: ≤ 10 live files.
- **R5 — `context/planning/`:** the closed decision records v75 → v79 → `context/planning/archive/decision-records/` (`git mv`; the v80 DR stays — v81 boots on it); the two "PART…" statuses corrected to a spelled terminal word; the three STALE BASELINE files (`master-release-plan.md`, `phase-3-milestone-backlog.md`, `research-agenda.md`) NOT moved — THE HORIZON RE-CUT rules them (D-v79-11).
- **R6 — `context/assessments/` (68) and `context/strategy/` (105):** statuses only (R1/R2); no moves — the strategic-context-map and the strategy of record cite these paths; a move here needs the map re-cut (v81's desk, not this lane).
- **R7 — the ClaudeFolder root (outside git; `mv -n`, then `ls` to prove it):** the six trademark files → `legal/NexSys-LLC/trademark/` (create it); `Founder_Operating_Plan_v1.md` and `Technical_Roadmap_v1.md` → `_archive/root-plans/`; `Claude outputs/` → `_archive/Claude outputs/`; `_scratch/h3`, `_scratch/_transfer`, `_scratch/v66`, `_scratch/v70`, `_scratch/v71`, the `.tgz` → `_archive/scratch/` (v76–v80 and `thu0924` stay); `START_HERE.md` — read it whole (it is small): if it names anything other than the stable prompt, the brief and the v81 text as the way in, write ONE replacement line at its top (`status:`-style) pointing there and list the stale sentences in the return; do not rewrite it.
- **R8 — the Open Risks and the spine:** not this lane. The return's §3 lists what the v81 hub should retire at its boot (OR-NIGHTLY-0902-S31 subsumed by OR-S31-INTERMITTENT; the Saturday-RED pre-registration lifted by T4 + T5), with the line numbers.
- **R9 — dangling references:** `context/strategic-context-map.md` :195–:196 name the RETIRED `weeks/` and `months/` patterns — change nothing; list them for v81's map re-cut. Grep every moved file's OLD path across `context/` (`grep -rln '<basename>'`) and list every citing file in the return — a moved dispatch text is cited by the beat that filed it; those cites become `archive/…` pointers in v81's next spine beat, not by this lane.
- **R10 — the other repos:** one line each in the return: `homesynapse-core-docs` status classes (`grep -m1 '^status:'` over its tracked .md, counted); `nexsys-bench` untracked dirs (`git status --porcelain -uall | grep '^??' | wc -l` and the top-level names); nothing changed there.
- **R11 — the skills (NOT this lane):** W-SKILLS-10 folds the 11 OPEN mints (THE PREMISE GATE's clauses; THE PRIOR-LEDGER GATE's survey text; THE MECHANISM CLAIM; THE MOVED STEP; THE SURVEY IS RUN; A FROZEN LOG TOKEN; A COUNT FROM AN UNCAPPED LISTING; THE RIG CARD IS FIVE ACTS; THE ADOPTED-DEVICE STATE) into `SKILL.md` §3, `references/laws-ledger.md`, `references/coding-instruction-format.md` and the FE/coder mirrors, then Nick syncs and Check 9 re-runs. v81 charters it as its idle beat. This lane only counts the mints and names the target sections in its return.

## §3 The return (`context/audits/2026-09-26_HIVE-CLEAN-2_return.md`, ≤ 8 KB)
§0 the census before/after (the status-class table; files per live directory; the handoff root's file count; `context/instructions` live count) · §1 every act as a row (`from → to` or `Was: → now`), grouped R1–R7, with the count per row and the assert that proved it · §2 the CONFIRMED-LIVE list (R2) with the proving line each · §3 for v81: the Open Risks to retire (R8), the moved files' citing files (R9), the map rows (R9), the other repos' one-liners (R10), the skills' mint count and targets (R11) · §4 the card (paths explicit; N = the `-uall` porcelain count; renames counted as one line each in `--name-status`) · §5 deviations: anything the charter asked that you could not re-measure or did differently, one line each. The last line: `RETURNED context/audits/2026-09-26_HIVE-CLEAN-2_return.md <bytes> bytes`.

## §4 The dispatch line (Nick pastes into a FRESH Cowork conversation with ClaudeFolder connected)
```
You are HIVE-CLEAN-2, a hygiene lane for the NexSys hivemind. Read `nexsys-hivemind/context/instructions/2026-09-26_HIVE-CLEAN-2_hygiene-lane_charter.md` whole and execute it exactly: §0 is your contract (two verbs — a status line or a move; no deletions; no body edits; never the spine, the newest DR, the plan of record, the register, the lessons or any skill tree), §1 the census to re-measure, §2 the rows R1–R11, §3 the return's shape and path, and the card in §0's form for my hands. Every git call `--no-optional-locks`; every move `git mv` inside the repo and `mv -n` outside it; `date -u` first; no attribution trailers on the commit message. When the return exists at its path, say back exactly one line: `RETURNED <path> <bytes>`, then the card.
```
