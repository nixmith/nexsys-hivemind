<!--
file: context/instructions/2026-09-05_coder-lane_CG-123_read-API-v1.1.3-additive_coding-instruction.md
purpose: The CG-1/2/3 coding instruction — the contract-gap batch on the FROZEN v1.1 dashboard read-API, ONE additive bump (v1.1.2 → v1.1.3) in the SKIP-VIS pattern (additive-only · per-endpoint camelCase · the emitter leads · version discipline). Docket Row 14 RULED (a) 2026-09-03 (ADOPT-ALL-RECS): all three fields, one bump, Core WU then the FE fast-follow. Row 30 (the R-4b F-R4b-H "shape gaps") adjudicated INSIDE at source: zero Core change (§DP-5). Authored AHEAD at v64 beat 2 on source at dc3328b; dispatches ONLY on FAILCHAN-FIX-1b's push run GREEN.
audience: the Coder lane (host-side Claude Code in homesynapse-core) · the hub (audit) · Nick (dispatches; commits; two words inside — §0)
state-type: coding instruction
status: EXECUTED 2026-09-05 — return: context/audits/2026-09-05_CG-123_return.md · audit: context/audits/2026-09-05_CG-123_intake_two-layer-audit_v65-b5.md · was: ⛔ GATED on FIX-1b's push run GREEN (the deferred gate on `ma
-->

# Coding Task: CG-1/2/3 — the read-API v1.1.3 additive keys (triggerRef · components[].ref · deviceId · lastReported), Row 30 adjudicated

**Subsystem:** REST API (`api/rest-api`) + the automation read projection (`core/automation`) + one composition-root line (`lifecycle/lifecycle`)
**Design Doc:** Doc 09 REST API (LOCKED) · Doc 16 §3.3/§4 the explain reads · the FROZEN v1.1 dashboard read-API contract (`nexsys-hivemind/context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md`; v1.1.1 DRIFT-1 2026-07-02; v1.1.2 SKIP-VIS 2026-07-26 — the pattern this WU repeats)
**Phase:** 3-Implementation
**Task Brief Reference:** docket Row 14 ★ (`context/planning/2026-09-02_R10-docket_ruling-cards_v61-b3.md` :85–:87, RULED (a) 2026-09-03) · the FE-HONEST-1 return §4 (`context/audits/2026-08-30_FE-HONEST-1_return.md` :50–:54) · Row 30 (the R-4b record :583, F-R4b-H)

## §0 The lane contract (read first)
- **`date -u` FIRST**; state your instrument limit; re-derive CT as UTC−5 once.
- **Return path:** `nexsys-hivemind/context/audits/<CT-filing-date>_CG-123_return.md` — ONE file. §0 card FIRST (≤3 KB: the census `N = a M + b A` with exact paths · the red-first table ≤2 KB · deviations by tag · the two DP choices you took where this instruction offers a fallback) · §1 what changed per file · §2 the gates run + counts (`:core:automation:test`, `:api:rest-api:test`, `:lifecycle:lifecycle:compileJava`, `spotlessCheck`) · §3 pushback/observations · §4 the WUCP Phase-1 checklist. **Cap ≤12 KB.**
- **Baseline:** `main` HEAD with FIX-1b landed (verify `git status --porcelain` empty and `git log -1` = the sha Nick names in the dispatch line; STOP if not). Cites below are from `dc3328b`.
- **Build discipline:** targeted Gradle on your desk is allowed (`./gradlew :core:automation:test :api:rest-api:test :lifecycle:lifecycle:compileJava spotlessCheck --offline`) — GREEN in one round is the target; the full `./gradlew check` is CI on Nick's push = the gate of record. **One commit; you commit nothing; stage nothing.**
- **Tests first** (red-first): every new test RUNS RED at HEAD before the production edit (or is declared green-by-construction, disclosed); the shape tests that gain a key go RED at HEAD by construction (the key is absent) — say so.
- **Two words Nick may give in the dispatch line (defaults apply if absent):** `CG2-SCOPE: A1-only | all-three` (default **A1-only**, §DP-3) · `ROW30: as-ruled | include-fv` (default **as-ruled**, §DP-5). The dispatch line carries them.

## What This Implements
The dashboard's explain surface (WhyNot · Automations · the entity list) cannot today verify what it names: `GET /api/v1/automations/{id}/non-firing` says "it fires on state change" with no reference to WHICH entity, so a dangling trigger ref renders as a confident sentence (the R-4 concealment the FE-HONEST-1 lane had to stop on); `GET /api/v1/entities` rows carry no `deviceId`, so a row cannot be correlated to the `device_adopted` line that created it; and the same rows carry no report time, so the list claims nothing about freshness (the em-dash shipped at FE-HONEST-1). This WU adds FOUR additive wire keys across THREE reads — `nonFiring.triggerRef` · `automations[].components[].ref` (CG-1) · `entities[].deviceId` (CG-2) · `entities[].lastReported` (CG-3) — as **v1.1.3**, in the SKIP-VIS shape: existing keys, casing, nesting and rendering byte-unchanged; new keys APPENDED; every new key present in every v1.1.3 payload (JSON `null` when unknown — never absent, never a fabricated value). The emitter leads: this WU lands Core-side; the FE fast-follow (`CONTRACT_VERSION`/`EXPECTED_VERSION` → `v1.1.3-<date>`, `contract.ts`/`shapes.ts`/mocks/the honesty tests) follows as its own lane; the freeze-doc amendment note is hub-owned. **Row 30's two "gaps" are adjudicated at source in §DP-5 and cost this WU zero lines.**

## Files to Read Before Starting (minimum read set — MANDATORY)
| File | Why |
|---|---|
| `api/rest-api/MODULE_CONTEXT.md` — §SKIP-VIS (the v1.1.2 precedent: how keys were appended and pinned) + the run-query/automation-query endpoint sections | The pattern of record; the shape-test names it cites |
| `api/rest-api/src/main/java/module-info.java` (verbatim below) | The JPMS edges this WU rides (§DP-3) |
| `core/automation/MODULE_CONTEXT.md` — the SKIP-VIS banner + the `Selector` permits section | `NonFiringExplanation` 8→9 precedent; the seven selector permits |
| `core/automation/src/main/java/module-info.java` · `core/device-model/src/main/java/module-info.java` · `core/state-store/src/main/java/module-info.java` | Module names verbatim; the `requires transitive` chain §DP-3 rests on |
| `core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java` — regions: `explainNonFiring` (:223–:251) · `deriveNonFiring`/`completedVerdict` (:260–:342) · `componentsOf`/`componentView` (~:770–:790) · `triggerSummary`/`humanize` · `subjectRefView` (:793) · `deriveOutcome` (:665–:727, READ ONLY — §DP-5) | Where the refs are derived and threaded |
| `core/automation/…/NonFiringExplanation.java` (:52–:61 the record; :83 the 8-arg ctor) · `AutomationSummary.java` (:33–:38; `ComponentView` :60) · `RunExplanation.java` (:98 `SubjectRefView`) · `Selector.java` (:39–:42 permits) · `DirectRefSelector.java` · every `TriggerDefinition`/`ConditionDefinition`/`ActionDefinition` permit record's components (list them in your return) | The types you extend and the rule §DP-2 switches over |
| `api/rest-api/…/GetNonFiringEndpoint.java` (`toWire` :112–:121) · `ListAutomationsEndpoint.java` (:106–:117) · `ListEntitiesEndpoint.java` (`summarise` :158–:163; ctor :105–:112) · `GetEntityEndpoint.java` (:63 `@see`; :125 the record-direct `data`) · `GetEntityStateEndpoint.java` (:53 `@see`) · `RestFilters.java` (`installEntityQueryEndpoints` :170–:185) · `GetRunCausalChainEndpoint.java` (`subjectRefMap` :128–:136) · `EndpointResponses.java` | The serializers and the gateway |
| `api/rest-api/src/test/…/AutomationEndpointsTest.java` (`nonFiring_v11ShapeTest` :56; `automations_v11ShapeTest` :204) · `ListEntitiesEndpointTest.java` · `core/automation/src/test/…/NonFiringExplanationServiceTest.java` | The pins you extend |
| `core/device-model/…/EntityRegistry.java` (:49 `findEntity`) · `Entity.java` (:57–:69; `deviceId` nullable for helpers) · `core/state-store/…/EntityState.java` (:86–:96) | CG-2/CG-3's sources |
| `lifecycle/lifecycle/…/HomeSynapseCore.java` `:1029` (the `installEntityQueryEndpoints` call; `entityRegistry` field :296, built :590) | The one composition-root line |
| `web-ui/dashboard/src/lib/api/contract.ts` (:139–:190 · :281–:378) — READ ONLY, do not edit | The frozen mirror the FE fast-follow will extend; the key names must match it |

## STOP-on-Mismatch Gates (read, then confirm before any edit)
| File | Expected state | What to check |
|---|---|---|
| `NonFiringExplanation.java` | record of **9** components ending `LastEvaluationView lastEvaluation, Boolean noCommandsIssued`; an 8-arg convenience ctor at :83 | count components; the ctor exists |
| `AutomationSummary.java` | `ComponentView(String type, String summary)` — 2 components | count |
| `GetNonFiringEndpoint.toWire` | 9 `data.put` keys in order `automationId, automationName, enabled, verdict, lastRelevantRunId, explanation, triggerSummary, lastEvaluation, noCommandsIssued` | order + count |
| `ListEntitiesEndpoint.summarise` | 3 keys `entityId, availability, stale`; `LinkedHashMap<>(3)` | order + count (NOTE: no `name` is put — the C8 optional key is not populated by Core today; do NOT add it here — out of scope) |
| `ListAutomationsEndpoint` | component map = `type, summary` | order |
| `RestFilters.installEntityQueryEndpoints` | 4 params `(Object, StateQueryService, LongSupplier, Clock)`; three `app.get` registrations | signature |
| `Selector.java` | `permits DirectRefSelector, SlugSelector, AreaSelector, LabelSelector, TypeSelector, SemanticTagSelector, CompoundSelector` (7) | count |
| `api/rest-api/build.gradle.kts` | `implementation(project(":core:device-model"))` at :9; `api(project(":core:state-store"))` at :10 | present |
| `app/homesynapse-app/src/test/java/com/homesynapse/app/HomeSynapseArchRules.java` | the four rules that reach this WU, read by the hub at `dc3328b`: Rule 8 `QUERY_SERVICE_READ_ONLY` (:265–:271 — `api.rest..` must not access `persistence..`; `EntityRegistry` is `device..`, not persistence) · Rule 9 `REST_ENDPOINTS_NO_EVENT_PUBLISHING` (:305–:316 — no `EventPublisher`; untouched) · Rule 10 AMD-52-INV-02 (:338–:347 — `value/event/device/state` Jackson-free; rest-api is not in the list, so §DP-3 (b)'s view is lawful there) · Rule 11 `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` (:372–:420 — the MUTATOR set `createEntity … disableEntity`; `findEntity` is a read) — **zero collisions** | confirm the four rules read as stated; a fifth rule naming `api.rest` or `device` → STOP |

## Files to Create or Modify (the Files table governs — addition #2)
| # | File | M/A | Delta |
|---|---|---|---|
| 1 | `core/automation/src/main/java/com/homesynapse/automation/NonFiringExplanation.java` | M | 10th component `RunExplanation.SubjectRefView triggerRef` (nullable; NOT null-checked in the compact ctor — the `noCommandsIssued` precedent). The existing 8-arg and 9-arg ctors are KEPT, delegating `triggerRef = null` (javadoc: "test-convenience; production constructs the canonical form"). |
| 2 | `core/automation/src/main/java/com/homesynapse/automation/AutomationSummary.java` | M | `ComponentView(String type, String summary, RunExplanation.SubjectRefView ref)` — 3 components, `ref` nullable; a 2-arg convenience ctor kept (`ref = null`). |
| 3 | `core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java` | M | §DP-2: `private static RunExplanation.SubjectRefView refOf(TriggerDefinition)` / `refOf(ConditionDefinition)` / `refOf(ActionDefinition)` (exhaustive sealed `switch`es, no `default`) + `private static RunExplanation.SubjectRefView selectorRef(Selector)`; `explainNonFiring` derives `triggerRef = definition.triggers().isEmpty() ? null : refOf(definition.triggers().get(0))` ONCE beside `triggerSummary` (:223) and threads it through `deriveNonFiring`/`completedVerdict` into every production `new NonFiringExplanation(…)` (8 sites at `dc3328b`: :228 :238 :263 :271 :284 :315 :331 :338 — re-count at HEAD); `componentsOf` (~:776) passes `refOf(x)` into each `ComponentView`. |
| 4 | `core/automation/src/test/java/com/homesynapse/automation/NonFiringExplanationServiceTest.java` | M | T1–T4 (§Tests) |
| 5 | `core/automation/MODULE_CONTEXT.md` | M | the SKIP-VIS banner gains a v1.1.3 line: `NonFiringExplanation` 9→10, `ComponentView` 2→3; the `refOf` rule stated (§DP-2) as the projection's contract |
| 6 | `api/rest-api/src/main/java/com/homesynapse/api/rest/EndpointResponses.java` | M | hoist `subjectRefMap(RunExplanation.SubjectRefView)` here as a package-private static (the exact 6-line body from `GetRunCausalChainEndpoint:128–:136`: null → null; `LinkedHashMap<>(2)` `type`, `id`) |
| 7 | `api/rest-api/src/main/java/com/homesynapse/api/rest/GetRunCausalChainEndpoint.java` | M | calls the hoisted helper; its own private copy removed; **wire byte-identical** (RunEndpointsTest untouched proves it) |
| 8 | `api/rest-api/src/main/java/com/homesynapse/api/rest/GetNonFiringEndpoint.java` | M | `toWire`: `data.put("triggerRef", EndpointResponses.subjectRefMap(e.triggerRef()))` appended AFTER `noCommandsIssued` → 10 entries (`LinkedHashMap` capacity hint updated) |
| 9 | `api/rest-api/src/main/java/com/homesynapse/api/rest/ListAutomationsEndpoint.java` | M | each component map gains `comp.put("ref", EndpointResponses.subjectRefMap(component.ref()))` appended AFTER `summary` |
| 10 | `api/rest-api/src/main/java/com/homesynapse/api/rest/ListEntitiesEndpoint.java` | M | ctor gains `EntityRegistry entityRegistry` (2nd param; `requireNonNull`); `summarise` becomes an instance method: after `stale`, `summary.put("deviceId", entityRegistry.findEntity(state.entityId()).map(Entity::deviceId).map(DeviceId::toString).orElse(null))` then `summary.put("lastReported", state.lastReported() == null ? null : state.lastReported().toString())` → 5 keys; `LinkedHashMap<>(5)`; class javadoc + `@see` updated to the 5-param gateway |
| 11 | `api/rest-api/src/main/java/com/homesynapse/api/rest/RestFilters.java` | M | `installEntityQueryEndpoints(Object javalinApp, StateQueryService queryService, EntityRegistry entityRegistry, LongSupplier viewPositionSupplier, Clock clock)` — `requireNonNull` the registry; pass it to `ListEntitiesEndpoint` only (§DP-3 default). Javadoc: the registry is the SAME instance the registry projection writes (the composition root guarantees it). |
| 12 | `api/rest-api/src/main/java/com/homesynapse/api/rest/GetEntityEndpoint.java` | M | javadoc `@see` → the 5-param signature ONLY (`:63`); **no code change** (§DP-3 default) |
| 13 | `api/rest-api/src/main/java/com/homesynapse/api/rest/GetEntityStateEndpoint.java` | M | javadoc `@see` → the 5-param signature ONLY (`:53`); **no code change** |
| 14 | `api/rest-api/src/test/java/com/homesynapse/api/rest/AutomationEndpointsTest.java` | M | T5–T8 (§Tests); the two `containsOnlyKeys` pins gain `triggerRef` / `ref` |
| 15 | `api/rest-api/src/test/java/com/homesynapse/api/rest/ListEntitiesEndpointTest.java` | M | every `new ListEntitiesEndpoint(…)` gains a registry (an `InMemoryEntityRegistry` from `core/device-model` main, or a minimal fake — declare which); T9–T12 |
| 16 | `api/rest-api/MODULE_CONTEXT.md` | M | a `### CG-123 — v1.1.3 additive keys (2026-09-0x)` section in the SKIP-VIS shape: the four keys, their positions, their sources, the null semantics, the pins; the gateway's new param; `subjectRefMap` hoisted; the §DP-5 reading of Row 30 recorded as a GOTCHA ("`outcome: CONFIRMED` with `resultOutcome: null` is the happy path: the zigbee handler publishes `command_result` only for unroutable/rejected/unconfirmed") |
| 17 | `lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java` | M | `:1029` (shifted by FIX-1a's hunks — `[INFO]`): pass `entityRegistry` as the new 3rd argument. ONE line. |
| — | `module-info.java` (any) · `build.gradle.kts` (any) · `web-ui/**` · `context/decisions/*freeze*.md` · `HeroLoopHardwareFreeIT` / `ReplayTransitionIT` | — | **NOT touched** (§DP-3's fallback is the ONE exception, declared) |

**Expected census: 17 M + 0 A.** If `CG2-SCOPE: all-three` → +0 files but rows 12–13 gain code (§DP-3 (b)) — declare the census in §0 either way.

## Technical Specification

### Settled Decisions (from the docket ruling + this authoring; NOT open questions)
- **DP-1 — the version.** This WU is **v1.1.3** of the FROZEN read-API, additive-only: zero existing key/casing/nesting/rendering changes anywhere; new keys appended at the END of their object; each new key is PRESENT in every response (JSON `null` when unknown), so a consumer distinguishes "v1.1.3 says unknown" (null) from "pre-v1.1.3 payload" (absent) — the `noCommandsIssued` precedent. The Core tree carries no version string for this contract (the FE's `CONTRACT_VERSION` does; the fast-follow bumps it to `v1.1.3-<landing CT date>`); the freeze-doc amendment note is the hub's at the landing beat.
- **DP-2 — the ref rule (CG-1).** `RunExplanation.SubjectRefView("entity", <ULID string>)` — the SAME view type and the SAME `{type, id}` wire map the causal chain already serves (per-endpoint consistency; the FE census check compares `id` to `entities[].entityId`). A definition component contributes a ref **iff it addresses exactly ONE entity by identity**: a `Selector` that is a `DirectRefSelector(EntityId)` → that id; `CalendarTrigger.calendarEntityId` → that id; **every other case → `null`** — `SlugSelector`, `AreaSelector`, `LabelSelector`, `TypeSelector`, `SemanticTagSelector`, `CompoundSelector` (group-resolving: the automation names a SET, not an entity — a ref would be a fabrication), and every component with no `Selector`/`EntityId` at all (`EventTrigger`, `TimeTrigger`, `SunTrigger`, `ManualTrigger`, `WebhookTrigger`, the Tier-2 reserved permits, `DelayAction`, `EmitEventAction`, the compound conditions — for `AndCondition`/`OrCondition`/`NotCondition`/`ConditionBranchAction` do NOT descend: `null`). **`ReachabilityTrigger(DeviceId deviceId, …)` → `null`**: it addresses a DEVICE, the frozen API has no device read for the FE to census against, and a `{type:"device"}` ref would fail the entity census falsely — record it as a limitation in the javadoc (a device-typed ref is a candidate for a later bump, not this one). The `Selector`-bearing permits the hub read: `StateChangeTrigger` · `StateTrigger` · `NumericThresholdTrigger` · `AvailabilityTrigger` · `StateCondition(Selector, attribute, value)` · `CommandAction(Selector target, …)` — each takes the selector rule; list every permit and its yield in your return. `nonFiring.triggerRef` = `refOf(triggers.get(0))` (definition order; `null` if no trigger or the first has no single-entity ref — say so in the javadoc: "the first trigger's ref; multi-trigger automations expose each in `components[].ref`"). `components[].ref` = `refOf(component)` for every trigger, condition and action in `componentsOf`'s existing order. Implement as **exhaustive sealed `switch`es with no `default`** so a new permit is a compile error, not a silent null (Row 19's silent-path class).
- **DP-3 — CG-2's endpoint scope (a hub ruling; Nick's word `CG2-SCOPE`, default A1-only).** **(a) A1-only (default):** `deviceId` lands on `GET /api/v1/entities` rows, which are map-built (`summarise`) and are the surface the correlation renders on (FE-HONEST-1 §10-H: the DevicesView row → `device_adopted`). A2/A3 (`GET /api/v1/entities/{id}` · `…/state`) serialize the `EntityState` RECORD directly (`body.put("data", maybeState.get())`, `GetEntityEndpoint:125`) — the same path that carries **F-S8's dialect defect** (instants as epoch seconds, `entityId` as `{msb,lsb}` — the card-sitting return 08-23; docket Row 8 RULED (b): ULID + ISO siblings, the Core half of Row 15, its own WU). Rebuilding that path here to append one key would touch Row 8's serializer twice and force this WU to pin the defective dialect as "expected"; so A2/A3's `deviceId` rides Row 8's rebuild (recorded there by the hub). **(b) all-three:** ONLY on Nick's word — A2/A3 wrap the record in a view that preserves every existing key's CURRENT rendering byte-for-byte and appends `deviceId` (e.g. a `@JsonUnwrapped EntityState` + `String deviceId` record — `com.fasterxml.jackson.annotation` is `requires transitive` from `databind`, no module-info change); a golden test asserts the view's JSON minus `deviceId` equals the record's JSON at HEAD. **JPMS (both branches):** `com.homesynapse.device` is INDIRECTLY exported to rest-api's consumers — `com.homesynapse.api.rest requires transitive com.homesynapse.state` and `com.homesynapse.state requires transitive com.homesynapse.device` — and Gradle already has `implementation(project(":core:device-model"))`; so `EntityRegistry` on the public gateway signature is lawful with **zero module-info change** (expected). **Fallback, declared, if `:api:rest-api:compileJava` fails on `[exports]` under `-Werror`:** add `requires transitive com.homesynapse.device;` after the `state` line in rest-api's `module-info.java` AND promote `implementation(project(":core:device-model"))` → `api(project(":core:device-model"))` in `api/rest-api/build.gradle.kts` (the `requires transitive` ↔ `api` lockstep, law #14); census +2 M, tagged `[INFO]` with the javac line quoted.
- **DP-4 — CG-3's rendering.** `lastReported` on A1 rows is `Instant.toString()` (ISO-8601 UTC, the same rendering as `meta.timestamp` and `lastEvaluation.at`) or `null` — the freeze's `string | null`. **Disclosed coupling:** A3's `lastReported` (record-direct) renders in the F-S8 dialect until Row 8 lands — two renderings of one field across two endpoints; A1's is the contract's; the FE fast-follow reads A1's. No change to A3 here. The value is the state projection's `EntityState.lastReported()` — when the projection has none (§10-G's rig evidence: a device with `state_reported` on the log but an empty A3 `lastReported`), A1 serves `null` and the FE keeps its no-claim em-dash: honest by construction, and Row 15's read-path work is where that gap closes.
- **DP-5 — Row 30 adjudicated at source: ZERO Core change (Nick's word `ROW30`, default as-ruled).** (i) **`trigger.firingValue: null` on a matched trigger** — `StandardExplanationService:535–:537`: *"firingValue is not captured on the lifecycle events in V1 (no payload-type-specific extraction); the field is present and nullable per the frozen shape."* By design (a V1-SUBSET record, the DP-6 class), not a gap against v1.1; populating it needs an extraction rule (which attribute of which triggering payload; how a `TypedValue` renders as the `string` the freeze declares) — a design question, its own row (**FIRING-VALUE**, hub-docketed), not a mechanical additive key. **Out of scope** unless `ROW30: include-fv`, in which case STOP and ask the hub for the extraction rule before writing it — it is not in this document. (ii) **`actions[].resultOutcome: null` beside `outcome: CONFIRMED`** — `deriveOutcome:665–:727`: `state_confirmed ⇒ CONFIRMED` regardless of any `command_result`, and `resultOutcome` is "a pure fact-carry" of the LAST `command_result` whose `causationId` equals the `command_issued` id (:698); `ZigbeeCommandHandler.publishResult` (:333–:356) chains exactly that causation (N-6 BINDING) and is called ONLY for `unroutable` (×3), `rejected`, `unconfirmed` (×3) — **never on the happy path**. So a command the device confirmed by its own state report lawfully reads `outcome: CONFIRMED` + `resultOutcome: null`: "confirmed by the device's report; the adapter published no verdict." **Not a defect; not a join miss.** The reading is recorded in `api/rest-api/MODULE_CONTEXT.md` (row 16), in the hub's freeze-doc note, and the FE fast-follow renders the sentence. Zero lines here.

### JPMS — the current `module-info.java` of the target module, verbatim (`api/rest-api/src/main/java/module-info.java` at `dc3328b`; comments elided, directives exact). Expected diff: NONE (§DP-3). The fallback diff: `+    requires transitive com.homesynapse.device;` after the `state` line.
```java
module com.homesynapse.api.rest {
    requires transitive com.homesynapse.state;
    requires com.homesynapse.event.bus;
    requires com.homesynapse.automation;
    requires com.fasterxml.jackson.databind;
    requires io.javalin;
    requires org.slf4j;
    exports com.homesynapse.api.rest;
}
```
`com.homesynapse.state` (`core/state-store`): `requires transitive com.homesynapse.platform; requires transitive com.homesynapse.value; requires transitive com.homesynapse.device; requires transitive com.homesynapse.event; requires transitive com.homesynapse.event.bus; requires org.slf4j; exports com.homesynapse.state;` · `com.homesynapse.device` (`core/device-model`): `requires transitive com.homesynapse.value; requires transitive com.homesynapse.event; requires transitive com.homesynapse.platform; exports com.homesynapse.device;` · `com.homesynapse.automation`: unchanged by this WU (new components are its own `RunExplanation.SubjectRefView` — no new edge).

### Wire shapes after this WU (the FE mirror's future; keys in order; ★ = new)
- `GET /api/v1/entities` row: `entityId, availability, stale, ★deviceId (string|null), ★lastReported (ISO string|null)`
- `GET /api/v1/automations/{id}/non-firing` data: `automationId, automationName, enabled, verdict, lastRelevantRunId, explanation, triggerSummary, lastEvaluation, noCommandsIssued, ★triggerRef ({type:"entity", id}|null)`
- `GET /api/v1/automations` `data[].components[]`: `type, summary, ★ref ({type:"entity", id}|null)`
- Everything else byte-identical (`RunEndpointsTest` unchanged is the proof for the causal chain after the helper hoist).

### Event Types Produced or Consumed — none added; none consumed differently (read projections only).
### Configuration Parameters — none.
### Error Handling — unchanged: the entity list's registry lookup never throws for a missing entity (`findEntity` → `Optional.empty()` → `null` on the wire); a registry exception is NOT caught here (it would be a projection defect and must surface as today's 500 path — do not add a swallow).

## Locked Decisions That Apply
- **LTD-04 (ULID at boundaries):** `deviceId`, `triggerRef.id`, `ref.id` are Crockford strings via `toString()` on the typed ids — never the `{msb,lsb}` record (F-S8's class).
- **LTD-08 (rest-api is the JSON boundary):** serialization stays in the endpoint maps; `core/automation` emits typed views only.
- **LTD-09 / `NO_DIRECT_TIME_ACCESS`:** no clock read is added; `lastReported` is a projection value; `meta.timestamp` stays on the injected `Clock`.
- **The freeze's four-constraint law (v1.1.2, Nick ruling 1, 2026-07-22):** additive-only · per-endpoint camelCase · the emitter leads · version discipline — this WU's whole shape.

## Invariants That Must Hold
- **The v1.1 base is byte-stable:** every pre-existing key renders exactly as at HEAD — the shape tests' `containsEntry` lines for existing keys are unchanged and still pass; `RunEndpointsTest` untouched and green.
- **No fabricated ref (D5):** a ref is emitted only from a `DirectRefSelector`/`calendarEntityId`; a group selector never resolves to "some entity" — the exhaustive switch + T2/T3 pin it.
- **INV-SE-02 unchanged:** the three reads stay behind `installAuth` and the readiness gate exactly as today; no new route.

## P2 Consumer/Pin (Fan-Out) Survey (done at authoring; re-run the greps)
- `new NonFiringExplanation(` — **8 main sites** (`StandardExplanationService` — all move to the canonical 10-arg form carrying the derived ref) · **5 test sites** (`AutomationEndpointsTest`, the 8-arg convenience form — compile unchanged; T6 adds a canonical-form construction).
- `new AutomationSummary.ComponentView(` — 1 main (`componentView`, gains `ref`) · 2 test (`AutomationEndpointsTest`, 2-arg form kept).
- `containsOnlyKeys` pins: `nonFiring_v11ShapeTest` (9 keys → +`triggerRef`) · `automations_v11ShapeTest` (component `type, summary` → +`ref`) — both RED at HEAD once edited, GREEN after. No `hasSize(N)` pins the entity-row key count (`ListEntitiesEndpointTest:109/:133` count ROWS, unaffected).
- `installEntityQueryEndpoints(` — 1 call (`HomeSynapseCore:1029`) + 3 javadoc `@see` (rows 10, 12, 13).
- **JPMS direction:** `SubjectRefView` (automation) on `NonFiringExplanation`/`ComponentView` — same module, no edge. `EntityRegistry`/`Entity`/`DeviceId` (device) in rest-api — readable via `state`'s `requires transitive` (§DP-3); Gradle `implementation(device-model)` present.
- **ARCH-RULE-REACH (law #16), verified by the hub at `dc3328b`:** Rule 8 `QUERY_SERVICE_READ_ONLY` (:265) · Rule 9 `REST_ENDPOINTS_NO_EVENT_PUBLISHING` (:305) · Rule 10 AMD-52-INV-02 Jackson isolation (:338) · Rule 11 `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` (:372; mutators only) — **zero collisions** with a read-only `EntityRegistry.findEntity` call from `api.rest` and with `SubjectRefView` on two automation records. Re-run the grep at HEAD and state the line in §2.
- `subjectRefMap(` — exactly 2 call sites in `GetRunCausalChainEndpoint` (:122 the trigger's `subjectRef`; :165 the action's `targetRef`) + the definition at :128 — all three move to the hoisted helper; `RunEndpointsTest` unchanged proves byte-identity.
- `new ListEntitiesEndpoint(` — 6 sites in `ListEntitiesEndpointTest` (each gains the registry arg) + 1 in `RestFilters:180`.
- **Behavioral publish-count pins:** none (no publish site changes).

## Test Requirements (tests first; red at HEAD unless marked)
| # | Test (class · method) | Red at HEAD? | Asserts |
|---|---|---|---|
| T1 | `NonFiringExplanationServiceTest.explainNonFiring_triggerRef_directRefSelector` | RED (no component) | a definition whose first trigger is `StateChangeTrigger(DirectRefSelector(E), …)` → `triggerRef() == SubjectRefView("entity", E.toString())` |
| T2 | `…explainNonFiring_triggerRef_nullForGroupSelector` | RED | first trigger `StateChangeTrigger(LabelSelector(…))` → `triggerRef() == null` (never a fabricated id) |
| T3 | `…explainNonFiring_triggerRef_nullForSubjectlessTrigger` | RED | first trigger `TimeTrigger`/`EventTrigger` → `null`; `CalendarTrigger(calendarEntityId=E)` → `("entity", E)` |
| T4 | `…listAutomations_componentRef_perComponent` | RED | a definition with `[StateChangeTrigger(DirectRef E1), StateCondition(DirectRef E2, attr, value), CommandAction(target=DirectRef E3), DelayAction]` → `components[].ref` = `[E1, E2, E3, null]` in `componentsOf` order (triggers, then conditions, then actions); a second case with `ReachabilityTrigger(DeviceId D)` first → `[null, …]` |
| T5 | `AutomationEndpointsTest.nonFiring_v11ShapeTest` (M) | RED once `triggerRef` is added to `containsOnlyKeys` | 10 keys; the 8-arg fixture → `triggerRef: null` on the wire (JSON null, present) |
| T6 | `…nonFiring_triggerRefOnWire` | RED | canonical 10-arg fixture with a ref → `data.triggerRef == {type:"entity", id:<ULID>}`, keys `type, id` only |
| T7 | `…automations_v11ShapeTest` (M) | RED once `ref` is added | component keys `type, summary, ref`; the 2-arg fixture → `ref: null` present |
| T8 | `…automations_componentRefOnWire` | RED | 3-arg `ComponentView` with a ref → `{type, id}` |
| T9 | `ListEntitiesEndpointTest.rows_v113Keys_inOrder` | RED | row keys exactly `entityId, availability, stale, deviceId, lastReported` in order |
| T10 | `…deviceId_fromRegistry_andNullWhenUnregistered` | RED | an entity present in the registry with `deviceId = D` → `"deviceId": D.toString()`; an entity absent from the registry (or a helper entity with null deviceId) → `null` present |
| T11 | `…lastReported_iso8601_orNull` | RED | `EntityState.lastReported = Instant.parse("2026-01-01T00:00:00Z")` → `"lastReported": "2026-01-01T00:00:00Z"` (a STRING — the dialect pin refuting epoch seconds); `null` → JSON null present |
| T12 | every existing `ListEntitiesEndpointTest` method | green-by-construction after the ctor gains the registry (disclosed) | unchanged assertions pass |
| — | `RunEndpointsTest` | untouched, GREEN | the causal chain's wire byte-identical after the helper hoist (the no-change proof) |
Existing suites: state the `:core:automation:test` and `:api:rest-api:test` counts at HEAD and after in §2 (+4 · +4 new tests expected; if `CG2-SCOPE: all-three`, + the golden test).

## MODULE_CONTEXT.md Update
`core/automation/MODULE_CONTEXT.md`: the SKIP-VIS banner gains the v1.1.3 line (`NonFiringExplanation` 9→10 · `ComponentView` 2→3 · the `refOf` rule verbatim from §DP-2). `api/rest-api/MODULE_CONTEXT.md`: the `### CG-123` section (row 16) + the Row 30 GOTCHA + the gateway's 5th param. Header type counts unchanged (no new types) — say so.

## What to Watch Out For
- **Tests must inject `Clock`.** Do NOT use `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, or `System.currentTimeMillis()` in this module's test code. Use `Clock.fixed(Instant.parse("2026-01-01T00:00:00Z"), ZoneOffset.UTC)` injected via constructor/`@BeforeEach`. **Enforcement reach:** `NO_DIRECT_TIME_ACCESS` runs from `com.homesynapse.app`'s test classpath, so it mechanically catches PRODUCTION code in every non-whitelisted module (plus app's own tests) — it does **not** scan this module's test source set. Clock-injection here is a self-enforced project convention that PM review, not `./gradlew check`, enforces.
- **Record component / static-factory name collision:** `ref`, `triggerRef` become accessor names — grep each record for a method or nested type of the same name before adding (the M7.2a-2 class).
- **The 8-arg convenience ctor must stay 8-arg** (5 test sites); if you consolidate, the census grows and the return says so.
- **`LinkedHashMap` order IS the wire order** — append; never reorder; the capacity hints are cosmetic but update them.
- **Do not "fix" A1's missing `name`** (C8 optional; Core does not populate it; out of scope) and do not touch A2/A3's serialization under the default scope — the F-S8 dialect is Row 8's.
- **`findEntity` is a projection read on the LIVE registry** — the same instance the `RegistryProjection` writes (`HomeSynapseCore:590/:601`); pass that field, never a new `InMemoryEntityRegistry()`.
- **Spotless** runs on every touched Java file; a javadoc `@see` with a wrong signature is a doclint failure under `-Werror` where enabled — keep the three `@see` lines exact.
- **The FE fast-follow is NOT yours** (`web-ui/**` untouched); its contract-check will FAIL until it bumps `EXPECTED_VERSION` — expected, and `frontend.yml` runs only on `web-ui/**` paths, so this commit does not trigger it.

## Coder Pushback Welcome
If a `Selector` permit or a definition record has a shape this rule handles wrongly (a `CompoundSelector` of exactly one `DirectRefSelector`, say), raise it with the source line — the rule stands as written unless you show a case where `null` conceals a single-entity ref; if the `[exports]` fallback fires, quote javac; if `subjectRefMap`'s hoist collides with anything in `EndpointResponses`, keep it in `GetRunCausalChainEndpoint` as package-private static and say so (`[INFO]`).

## Out of Scope
- `firingValue` population (FIRING-VALUE, its own row — §DP-5 (i)) · A2/A3 `deviceId` under the default scope (Row 8's rebuild) · A1 `name` · the F-S8 dialect (Row 8) · the FE fast-follow (`contract.ts`, `shapes.ts`, mocks, `EXPECTED_VERSION`) · the freeze-doc amendment note (hub) · any new endpoint · `resultOutcome` semantics (§DP-5 (ii): documented, not changed).

## Success Criterion (binary)
DONE when: (1) the census is exactly the Files table (17 M + 0 A) or the deviation is declared in §0 with its cause; (2) T1–T11 ran RED at HEAD (T12 green-by-construction, disclosed) and GREEN after; `RunEndpointsTest` untouched and green; `spotlessCheck` clean; `:core:automation:test` + `:api:rest-api:test` + `:lifecycle:lifecycle:compileJava` GREEN on your desk with counts; (3) the four keys render per §Wire shapes (T5–T11) with JSON null when unknown; (4) both MODULE_CONTEXTs updated; (5) the WUCP Phase-1 checklist; (6) the return within cap. **The gate of record is CI on Nick's push. Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING — the surface is "verified" only when H8's real-wire exercise reads `deviceId`, `lastReported` and `triggerRef` on the card (law #22).**

## Work Unit Completion (WUCP Phase 1)
After your desk gates pass: update the two MODULE_CONTEXT.md files; append the `coder-handoff.md` entry (Deferred Build Gate: YES — the full `./gradlew check` is CI on Nick's push; NEXT WU pointer: the hub's audit → the msg file → Nick's commit + push → CI green = the gate → the FE fast-follow lane (v1.1.3 mirror) → H8 real-wire reads the new keys); a `coder-lessons.md` note only if you learned one; the checklist in the return.

### DISPATCH LINE (Nick pastes into a host-side Claude Code session in `~/Desktop/Code/ClaudeFolder/homesynapse-core` — ONLY after FIX-1b's push run is GREEN; fill the sha)
```
date -u first. Boot as the nexsys-coder skill. Baseline: this tree must be at <FIX-1b's sha> and clean — verify with `git log -1 --format=%h` and `git status --porcelain` (STOP and report if not). Execute nexsys-hivemind/context/instructions/2026-09-05_coder-lane_CG-123_read-API-v1.1.3-additive_coding-instruction.md exactly: read its §0 contract and the minimum read set first; confirm every STOP gate. Words: CG2-SCOPE: A1-only. ROW30: as-ruled. Tests first (T1–T11 red at HEAD), then the production edits, 17 M + 0 A. Return ONE file at nexsys-hivemind/context/audits/<today's CT date>_CG-123_return.md, §0 card first, ≤12 KB. Stage nothing; commit nothing; the hub audits and Nick commits.
```
