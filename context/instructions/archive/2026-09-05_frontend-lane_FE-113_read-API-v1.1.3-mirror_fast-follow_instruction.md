<!--
file: context/instructions/2026-09-05_frontend-lane_FE-113_read-API-v1.1.3-mirror_fast-follow_instruction.md
purpose: Block 4 of the v65 agenda — the FE FAST-FOLLOW on the read-API v1.1.3 additive bump (CG-1/2/3 lands the emitter; this lane mirrors it in the dashboard: `CONTRACT_VERSION` → v1.1.3, `contract.ts`/`shapes.ts` optional-nullable, mocks that do NOT always populate, the honesty tests, the three views rendering presence/null/absence honestly). Authored AHEAD at v65 beat 3 on source at `e5fa035` (the dashboard's tree) + the CG instruction's §Wire shapes; the ⟨slots⟩ fill from the CG return's AUDIT; it DISPATCHES only after CG has LANDED (Nick's commit; CI green) — the lane shares the core working tree (the plan of record §0-ter guard (ii): the fast-follow never runs on a tree with an unlanded Core change).
audience: the FE lane (a fresh Cowork window under `nexsys-frontend`; reads this whole, then §1's read-set) · the hub (the audit rules §8) · Nick (the dispatch line §9)
state-type: coding instruction (ruling-slotted; the slots are wire literals the CG audit fixes)
status: EXECUTED 2026-09-06 — return: context/audits/2026-09-06_FE-113_return.md · audit: context/audits/2026-09-06_FE-113_intake_two-layer-audit_v66-b5.md · was: AUTHORED v65 beat 3; §2 SLOTS FILLED at v65 beat 5 from the 
-->

# FE-113 — the v1.1.3 mirror: four additive keys, rendered honestly

## §0 The lane contract (read first; every line binds)
- **`date -u` FIRST**; state your instrument limit; CT = UTC−5 once.
- **THE ONE DELIVERABLE:** `nexsys-hivemind/context/audits/<CT-filing-date>_FE-113_return.md` — ONE file, **§0 card first (≤3 KB: the census `N = a M + b A` with exact paths · the red-first table · deviations by tag · the `npm run verify` line with counts) · §1 per-file deltas · §2 the gates · §3 pushback/observations · §4 the WUCP Phase-1 checklist. Cap ≤12 KB.** Your last line: `RETURNED <path> <bytes>`.
- **Baseline:** `homesynapse-core` at **⟨CG's landed sha⟩** with `git status --porcelain` EMPTY (STOP and report if not — an unlanded Core change on the tree is the plan's guard (ii)). Work ONLY under `web-ui/dashboard/`. **Stage nothing; commit nothing** — the hub audits; Nick commits.
- **Tests first:** every new assertion RED at HEAD before the edit (the version pin and the "renders when present" tests go red by construction — say so); then GREEN. **The gate of record is `frontend.yml` on Nick's push (`npm run verify`)**; your desk `npm run verify` is the pre-gate, reported with counts.
- **The four-constraint law (v1.1.2 ratified, Nick 2026-07-22) governs:** additive-only · per-endpoint camelCase · the emitter leads (Core's bytes are the truth; you mirror, never invent) · version discipline (one bump: `v1.1.2-2026-07-26` → `v1.1.3-⟨CG's landing CT date⟩`).
- **The honesty law (FE-HONEST-1, §10-H/§10-I):** a value the wire did not carry is never rendered as if it had; ABSENCE (a v1.1.2 hub) and NULL (a v1.1.3 hub with nothing on record) are two different facts and render as two different sentences; a dangling ref renders LOUD through the registry census, never as a confident name.
- **Predictions (H12; adjudicate first in your §0):** P1 the census is 11 M + 1 A exactly as §3's table · P2 the two recorded fixtures (`wire-2026-08-16-nonfiring.ts` · `wire-2026-08-20-never-triggered.ts`) validate UNCHANGED under the new validators (absence lawful) · P3 `npm run verify` is green in one round after the edits · P4 you find ≥1 place this instruction's read of the views is stale at source and file it with a line cite.

## §1 The read-set, in order (nothing older)
1. `context/process/deep-work-window_protocol.md` §2 + §6 (your contract's shape). 2. The CG instruction `context/instructions/2026-09-05_coder-lane_CG-123_read-API-v1.1.3-additive_coding-instruction.md` §Wire shapes (the keys, in order, ★ = new) + §DP-5 (the `resultOutcome` sentence). 3. The CG return + its audit (paths in the dispatch line) — **the literals in §2 come from there, never from this file's memory.** 4. `web-ui/dashboard/MODULE_CONTEXT.md` (Gotchas; the SKIP-VIS/v1.1.2 precedent) · `FRONTEND_DOCTRINE.md`. 5. `src/lib/api/contract.ts` (:14 `CONTRACT_VERSION`; `SubjectRef` :139–:142; `EntitySummary` :158–:163; `NonFiringExplanation` :340–:369; `AutomationSummary` :372–:378) · `src/lib/api/shapes.ts` (`strOrNull` :68 · `optStr` :74 · `A1:entities` :101–:114 · `B3:nonFiring` :267–:298 (the `noCommandsIssued` idiom :288–:296) · `B3:automations` :299–:309) · `src/lib/api/contract.test.ts` (:34 the version pin) · `src/lib/api/fixtures/*` (READ ONLY — recorded measurements) · `src/lib/api/mock/mockData.ts` (:36–:41 the entity rows; :365–:403 the nonFiring map; the automations' `components`) · `src/lib/format.ts` (:156–:167 `lastReportedCell` + `LIST_FRESHNESS_NO_CLAIM_TITLE`) · `src/views/DevicesView.tsx` (:18–:19 the imports; :46–:90 the honesty comments; :85 the em-dash title; :180 the drawer's `lastReportedCell`) · `src/views/WhyNotView.tsx` (:63–:95) · `src/views/AutomationsView.tsx` · the FE-HONEST-1 registry-census helper (`src/lib/registry.ts` — `RefResolution` :36, `RefResolver` :42, `makeRefResolver` :50) and the LOUD-render component it feeds (find it by the `RefResolution` consumer; name it in your return).
6. The `nexsys-frontend` skill's `references/contract-consumer.md` (the additive procedure — W-SKILLS-7 may have just written it; if the section is absent, this file is the procedure).

## §2 The wire, as CG landed it (⟨slots⟩ filled by the hub from the CG audit before dispatch — do not guess)
- `GET /api/v1/entities` row: `entityId, [name], availability, stale, ★deviceId: string|null, ★lastReported: string | null` — FIXED: `Instant.toString()`, ISO-8601 UTC with nanos when present (e.g. `"2026-09-06T02:45:29.123456Z"`), the `meta.timestamp` rendering; never epoch seconds (the lane's test pins it).
- `GET /api/v1/automations/{id}/non-firing` data: `…, noCommandsIssued, ★triggerRef: {type: "entity", id: string} | null` — FIXED: lowercase `entity`, the causal chain's OWN literal since M7.5a (`RunExplanation.java:95`); the ONE rendering is `EndpointResponses.subjectRefMap`.
- `GET /api/v1/automations` `data[].components[]`: `type, summary, ★ref: {type: "entity", id} | null` — the same literal; a `NumericCondition` on a direct ref yields its entity (the hub's R1 ruling).
- **Every new key is PRESENT in every v1.1.3 payload, JSON `null` when unknown (never absent).** A v1.1.2 hub omits them entirely — lawful; rendered as ABSENCE. **The dashboard's `contract.ts:140` comment ("e.g. ENTITY") is STALE — the wire has served lowercase `entity` since M7.5a; fix that comment as an `[INFO]` doc edit inside the additive law; do NOT normalize case anywhere; pin the literal `entity` in the honesty tests.** One more fact to render as-is (the freeze note carries it, not you): `deviceId` is read from the LIVE registry at request time while the row's other fields come from the state snapshot at `viewPosition` — two instants; a null is honest, never a defect.

## §3 Files to Create or Modify (the Files table governs)
| # | File | Change |
|---|---|---|
| 1 | M `src/lib/api/contract.ts` | `CONTRACT_VERSION = 'v1.1.3-⟨date⟩'`; a v1.1.3 comment block in the :15–:30 style (the four keys, their nullability, the H8 false-type warning); `EntitySummary` += `deviceId?: string \| null; lastReported?: string \| null;` · `NonFiringExplanation` += `triggerRef?: SubjectRef \| null;` · `AutomationSummary.components[]` += `ref?: SubjectRef \| null` (declare a named `ComponentSummary` interface if you must — additive, no rename of the existing shape) |
| 2 | M `src/lib/api/shapes.ts` | `A1:entities`: when `'deviceId' in e` → `strOrNull`; when `'lastReported' in e` → `strOrNull` (a non-string non-null → `ContractError`); `B3:nonFiring`: when `'triggerRef' in d` → null or `{type: string, id: string}` (a missing `id` → error); `B3:automations`: each component, when `'ref' in c` → the same; a shared `refOrNull(v, path)` helper beside `strOrNull` |
| 3 | M `src/lib/api/contract.test.ts` | :34 pin → `'v1.1.3-⟨date⟩'` (RED by construction at HEAD) |
| 4 | A `src/lib/api/v113-additive.test.ts` | the tri-state honesty tests per key: PRESENT-typed passes · PRESENT-null passes · ABSENT passes (the two recorded fixtures validate unchanged — P2) · PRESENT-wrong-type throws (`deviceId: 42` · `lastReported: 1725580000` · `triggerRef: "ent_x"` · `ref: {type:"entity"}` without `id`) — each RED at HEAD (the validators ignore unknown keys today, so "throws" fails at HEAD) |
| 5 | M `src/lib/api/mock/mockData.ts` | the v1.1.3 hub shape: every entity row carries BOTH keys, ≥2 rows with real values, ≥1 with `deviceId: null`, ≥1 with `lastReported: null` (the H8 false-type class: a mock that always populates hides a null); the nonFiring map: `triggerRef` present on every entry — an object on the CONDITION_NOT_MET/DISABLED entries, `null` on NEVER_TRIGGERED (mirroring the live shape's honesty); `components[].ref` present on every component, ≥1 `null`; **one dangling ref** (an id absent from the mock registry) so the LOUD render is exercised |
| 6 | M `src/lib/format.ts` (+ `format.test.ts`) | `LIST_FRESHNESS_NO_CLAIM_TITLE`'s comment re-scoped to "a hub that does not serve `lastReported` on the list (pre-v1.1.3)"; ADD `LIST_FRESHNESS_NULL_TITLE` = "This hub has no report time on record for this entity." — two facts, two sentences |
| 7 | M `src/views/DevicesView.tsx` (+ `DevicesView.honesty.test.tsx`) | the freshness cell: key ABSENT → em-dash + `LIST_FRESHNESS_NO_CLAIM_TITLE` (unchanged behaviour) · PRESENT-null → em-dash + `LIST_FRESHNESS_NULL_TITLE` · PRESENT-string → `lastReportedCell(value)`; `deviceId`: PRESENT-string → a muted secondary line "Device ⟨id⟩" (i18n-keyed; name-light) · null/absent → nothing rendered (absence renders absence) — tests for all five states, RED at HEAD |
| 8 | M `src/views/WhyNotView.tsx` (+ `WhyNotView.nullability.test.tsx`) | beside `triggerSummary`: `triggerRef` PRESENT-object → the entity rendered through the FE-HONEST-1 registry census (resolved → the display name as a link; dangling → LOUD, exactly the FE-HONEST-1 component) · null/absent → the sentence alone, no claim; tests: resolved · dangling · null · absent |
| 9 | M `src/views/AutomationsView.tsx` (+ its test) | `components[].ref` the same three-way render per component |
| 10 | M `web-ui/dashboard/MODULE_CONTEXT.md` | a `*2026-09-0x beat (FE-113)*` banner: v1.1.3 mirrored; the tri-state idiom (absent ≠ null) as a Gotcha; the `resultOutcome: null` beside `outcome: CONFIRMED` sentence (§DP-5) recorded as the honest happy path — a MODULE_CONTEXT gotcha, not a code change |
| 11 | M `src/lib/i18n.ts` (+ its test if keys are counted) | the new copy keys (the two titles; "Device") — name-light; the brand-token pin test (`i18n.test.ts`) must stay green |
| 12 | — | `src/lib/api/fixtures/*` READ ONLY · `realTransport.ts`/`client.ts` untouched (the validators are the seam) · no new endpoint · no CSS beyond the muted line's existing token class |
**Census expected: 11 M + 1 A.** A file outside this table is a `[REVIEW]` with the reason, never a silent add.

## §4 What to watch out for
The `name` key's precedent (C8) is your idiom for ABSENCE; `noCommandsIssued`'s validator (:292–:296) is your idiom for "present must be typed" · the recorded fixtures are measurements — if a new validator rejects one, the validator is wrong, not the fixture · `SubjectRef.type` case (§2) · the a11y test (`a11y.test.tsx`) runs in `verify` — the new muted line needs no new landmark · the bundle check (`check:bundle`) — no new dependency · `check:contract` (`scripts/contract-check.mjs`) — read what it checks before you change `CONTRACT_VERSION` · the dark theme is the recorded default; the LOUD render already has both themes.

## §5 Out of scope
Any Core file · `EXPECTED_VERSION` (no such constant exists in the tree — the spine's phrase names `CONTRACT_VERSION` and its test pin only; do not create one) · the explainability HERO composition (the next FE lane; charter separate) · FE-STATE-DIALECT (Row 15) · any rendering of `firingValue` (V1-by-design, §DP-5) · the freeze-doc note (hub) · the marketing site.

## §6 Success criterion (binary)
DONE when: (1) the census is exactly §3 or the deviation is declared in §0; (2) every new test RED at HEAD (the table names which are red-by-construction) then GREEN; (3) `npm run verify` green on your desk with counts, and **`frontend.yml` GREEN on Nick's push = the gate of record**; (4) the two recorded fixtures validate unchanged; (5) MODULE_CONTEXT updated; (6) the return within cap. **Completion register: REPO-COMPLETE, LIVE-VERIFICATION PENDING — the surface earns "verified" only when H8's real-wire exercise reads `deviceId`, `lastReported` and `triggerRef` rendered on the card (law #22).**

## §7 WUCP Phase 1
`coder-handoff.md` entry (Deferred Build Gate: YES — `frontend.yml` on Nick's push; NEXT WU pointer: the hub's audit → the msg file → Nick's commit + push → CI green → H8 real-wire reads the keys → the explainability hero charter); a `coder-lessons.md` note only if you learned one.

**The alternative shape this instruction did not take:** make the four keys REQUIRED in the validators (a strict v1.1.3 contract, no optional marks) — rejected: the two recorded v1.1.2 fixtures and any hub not yet at CG's sha would fail validation and the dashboard would refuse an honest older payload; optional-nullable with ABSENCE rendered as absence is the C8 and v1.1.2 precedent, and the honesty tests (§3 row 4) make "optional" impossible to abuse — a present key must be typed.

## §8 The hub's audit rules (pre-filed)
R1 census at porcelain = §0's declaration · R2 the version pin and every RED-at-HEAD claim re-executed by reading the test file's assertions against HEAD's types · R3 the two fixtures' md5 unchanged · R4 the mock carries ≥1 null per new key and one dangling ref (the H8 class) · R5 the three-way render tests exist for each view · R6 `npm run verify` not re-run by the hub (disclosed); CI is the gate · R7 mismatches first; a `[REVIEW]` with line cites is first-class.

## §9 THE DISPATCH LINE (Nick pastes into a FRESH Cowork window with `ClaudeFolder` connected — ONLY after CG has landed and its CI is green; the hub fills the slots from the CG audit)
```
date -u first. You are the FE-113 frontend lane for NexSys/HomeSynapse — boot as the nexsys-frontend skill. Baseline: homesynapse-core must be at ⟨CG's landed sha⟩ with `git status --porcelain` empty (STOP and report if not); work only under web-ui/dashboard/. Execute nexsys-hivemind/context/instructions/2026-09-05_frontend-lane_FE-113_read-API-v1.1.3-mirror_fast-follow_instruction.md exactly: §0 first, then §1's read-set in order; the wire literals are §2's as the hub filled them from the CG audit at nexsys-hivemind/context/audits/2026-09-05_CG-123_intake_two-layer-audit_v65-b5.md. Tests first (red at HEAD), then the edits, 11 M + 1 A. Return ONE file at nexsys-hivemind/context/audits/<today's CT date>_FE-113_return.md, §0 card first, ≤12 KB. Stage nothing; commit nothing; the hub audits and Nick commits. Your last line: RETURNED <path> <bytes>.
```
