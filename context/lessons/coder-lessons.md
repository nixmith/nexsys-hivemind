<!--
file: context/lessons/coder-lessons.md
purpose: Append-only record of patterns, pitfalls, anti-patterns, and discoveries from coding sessions.
audience: Coder
update-cadence: append-only
state-type: history
status: CURRENT
last-verified: 2026-05-20 against commit 25bc23b
-->

# Coder Lessons Log

Append-only record of patterns, pitfalls, anti-patterns, and discoveries from coding sessions. Read this at session start. Append new entries at the end when discoveries are made.

Periodically, the most significant entries are promoted into the skill reference files (java-patterns.md, testing-standards.md, homesynapse-mental-model.md). Entries are never deleted from this file — it is the complete historical record.

## Entry Format

```
## YYYY-MM-DD | Category: [concurrency|persistence|serialization|testing|module-boundaries|build|anti-pattern|other] | Source: [what task]
**Discovery:** [one sentence summary]
**Detail:** [the specific issue and the correct approach]
**Impact:** [what this means for future work]
```

---

> **Rotated 2026-06-11 (hygiene pass A4):** lessons dated 2026-05-31 and earlier (the M1–M4 era) → `archive/coder-lessons-2026-05-m3-m4.md`. June (M5/M6-era) lessons retained below.

## 2026-06-05 | Category: other | Source: M4.B-S2 (AMD-44 Stage 2) — adding a record component + a back-compat convenience ctor of the OLD arity is the zero-breakage way to evolve a widely-constructed record
**Discovery:** When a record gains a component (`Entity` 11→12, `ProposedEntity` 3→4) but is constructed at many sites you must not (or cannot) touch in this WU, add a **convenience constructor whose parameter list is the pre-change canonical signature**, delegating to the new canonical ctor with the new component defaulted. Every existing `new Entity(... 11 args ...)` call silently re-binds to the convenience ctor — `EntityTest`, `TestEntityFactory`, and the cross-module `StubIntegrationContext` (integration-api testFixtures) all compiled unchanged, no edit needed. There is **no overload ambiguity** because the two ctors differ in arity (11 vs 12 / 3 vs 4); ambiguity would only arise if the new component's type matched a trailing param's type AND the arities collided, which they don't. The one shape test that pins component count (`EntityTest.exactlyElevenFields` → `exactlyTwelveFields`) is the only existing test that MUST change — grep `getRecordComponents()` repo-wide first (here only `EntityTest:82` hit for these two records; `ProposedEntity` had no shape test, so a new `ProposedEntityTest` was created).
**Detail:** Guard order in the compact ctor is **coerce → validate → copy**: `if (role == null) role = DEFAULT;` BEFORE `if (!type.allows(role)) throw IAE;` BEFORE `field = List.copyOf(field);`. This ordering is load-bearing — a `null` role on a type that permits only the default (PLUG → {PRIMARY}) must coerce and pass, not NPE inside `allows(...)`. And because the default (PRIMARY) is permitted by EVERY enum constant, the back-compat ctor (which passes the default explicitly) can never construct an illegal pair, so the new construction-time guard adds ZERO breakage to the existing corpus. Deliberately NOT adding a guard for a field that was never guarded before (here `entityType`) keeps the diff minimal (it NPEs via the `allows(...)` call — documented in a code comment, not "fixed"). The compile-time legality matrix is encoded on the partner enum: each `EntityType` constant declares `EnumSet.of(...)` through an `EntityType(Set<EntityRole>)` ctor that stores `Set.copyOf(...)` — `EnumSet.of` is MUTABLE, so storing it raw would make `legalRoles()` mutable and fail the immutability test; `Set.copyOf` is mandatory. Adding a new enum constant then forces the author to declare its legal-role set (the compiler requires the ctor arg) — the constraint can't be forgotten. Note the AMD's illustrative enum-constant ORDER differed from the source order; the matrix CONTENT (the role sets) is authoritative, the ORDER stays as-is in source (pinned by a `containsExactly` test) — take data from a spec, not incidental ordering.
**Impact:** (1) To add a component to a record with many construction sites, ship a convenience ctor of the OLD arity defaulting the new field — existing callers (even cross-module testFixtures you're told not to edit) keep compiling; only the component-count shape test changes. (2) Grep `getRecordComponents()` repo-wide before editing — it's the one assertion that breaks the instant a component lands (Interface Evolution Checklist). (3) Compact-ctor order is coerce → validate → copy; a null that coerces to a universally-legal default must pass the validate step, which is why "every type permits the default" is the property that makes the change zero-breakage. (4) Encode a compile-time legality matrix on the enum via a ctor arg (forces declaration on every new constant) and store it `Set.copyOf(EnumSet.of(...))` — never retain the mutable `EnumSet`. (5) When a spec's code block lists constants in a different order than the source, the content governs, not the ordering — reordering source constants is a contract change.

## 2026-06-05 | Category: build | Source: M4.C (AMD-54..64) — the actual Spotless config enforces only header + removeUnusedImports + trailing-ws + eof, NOT import-order or indentation
**Discovery:** `build-logic/src/main/kotlin/homesynapse.java-conventions.gradle.kts` declares `spotless { java { licenseHeader(...); removeUnusedImports(); trimTrailingWhitespace(); endWithNewline() } }` — and nothing else. There is NO `googleJavaFormat()`/`palantirJavaFormat()`/`importOrder()`. So Spotless does NOT reformat indentation and does NOT sort imports — but `removeUnusedImports()` fails `check` on ANY unused import.
**Detail:** Two files in the touched set (`EventTypes.java`, `IntegrationEvents.java`) are TAB-indented while the rest of the codebase is 4-space; `TestEventSamples.java` has `com.homesynapse.value.StringValue` interleaved mid-`com.homesynapse.event.*` (not alphabetical). Both pass `check` — direct proof Spotless isn't enforcing format/import-order. Consequences for an Edit-tool WU: (a) when editing a tab-indented file you must match tabs byte-for-byte — a one-shot `^\t` grep (count tab-led lines vs space-led) settles tabs-vs-spaces cheaply before the first Edit; (b) added imports can go in any position, but every one must be USED, and Javadoc `{@link Type}` counts as usage (google-java-format's `RemoveUnusedImports` respects Javadoc refs — that's why `IntegrationEvents` keeps `import …EventType` whose only use is a `{@link}`); (c) the copyright header must be byte-exact (`/*\n * HomeSynapse Core\n * Copyright (c) $YEAR NexSys. All rights reserved.\n */`).
**Impact:** Don't spend effort hand-sorting imports for Spotless in this repo — it doesn't sort. DO audit every added import for actual use (incl. Javadoc-only `{@link}`) before declaring done, and DO preserve the file's existing indentation char (tabs in `EventTypes`/`IntegrationEvents`, spaces elsewhere). Read the spotless block in the convention plugin once for an unfamiliar repo rather than assuming google-java-format — the enforcement set changes what "format-clean" means.

## 2026-06-05 | Category: serialization | Source: M4.C (AMD-59) — a persisted record that embeds an unregistered sealed-type field degrades SILENTLY; prove it from source, escape via a command-less instance
**Discovery:** A persisted event payload round-trips through `EventPayloadCodec` only if every sealed/abstract-typed field in its subtree has a (de)serializer in `PersistenceJacksonModule`. Jackson cannot construct a sealed-interface field with no codec — `decode()` catches the failure and returns a `DegradedEvent` (a silent lossy round-trip, NOT a thrown error). `CapabilityAdded` carries a full `CapabilityInstance` whose `commands → CommandDefinition → ExpectedOutcome.expectation` field is the sealed `Expectation` (device-model), which has no codec (only the 10 ULID wrappers + `AttributeValue` are registered).
**Detail:** Verified the defect WITHOUT running the build (Coder produces files only): (1) read `PersistenceJacksonModule` — `addSerializer/addDeserializer` cover ULIDs + `AttributeValue` only; (2) confirmed `ExpectedOutcome.expectation` is typed as the sealed `Expectation` (`permits ExactMatch, WithinTolerance, EnumTransition, AnyChange`); (3) ruled out a false alarm by grepping event-model for any existing event embedding `Capability`/`Expectation`/`CommandDefinition` — none (`DeviceAdoptedEvent` carries a single `Ulid`), so the codec has never been exercised on this subtree. The instruction flagged this exact case as STOP-and-report: do NOT add a Jackson annotation to the domain type (fails `NO_JACKSON_IN_DOMAIN_MODEL`) and do NOT silently write a custom serializer (a PM design call). The escape that keeps a milestone contract-only AND green: a **command-LESS** capability (`StandardCapabilities.occupancy()` — empty commands map) round-trips losslessly (its `AttributeSchema`/`ConfirmationPolicy` subtree is plain-Jackson; the abstract `Number` min/max fields are null and omitted by `NON_NULL`), so it backs the green codec test; the command-bearing case (`onOff()`) becomes a `@Disabled("AMD-65 pending …")` acceptance test naming the follow-up amendment.
**Impact:** (1) Before asserting a new persisted event round-trips, trace its payload for ANY sealed/abstract-typed field and check `PersistenceJacksonModule` registers it — an unregistered one degrades silently to `DegradedEvent`, so a naive `isEqualTo` assertion fails with a confusing "expected X but was DegradedEvent". (2) A serde defect is provable from source alone: registered-codec list + field types + "has any existing event ever serialized this subtree?" (a clean prior-usage grep rules out a false alarm). (3) When STOP-and-report fires on serde, the contract-only escape is a minimal/command-less instance for the green test + a `@Disabled` acceptance test naming the follow-up — never an annotation, never a silent custom serializer.

## 2026-06-05 | Category: build | Source: M4.C gate fix — adding event-type string constants OR event-record classes ripples to SEVEN downstream artifacts that the producer-only Files-to-Modify list omitted
**Discovery:** Adding 7 constants to `EventTypes` (46→53) and 7 records to the `IntegrationEvents` manifests (lifecycle 5→10 + a new `CAPABILITY_EVENT_CLASSES`, so `ALL_EVENTS` 27→34) compiled cleanly but failed `./gradlew check` at `core:event-model` — and because event-model is upstream of everything, the build aborted there and the integration/persistence suites never ran (their failures were hidden behind the first one). The M4.C instruction's "Files to Modify" listed the PRODUCER (`EventTypes.java`) but none of the consumers. The event-type set has **seven** lockstep sites:
1. `EventTypesTest` — hardcoded constant COUNT (`hasSize(46)`→53) AND a naming REGEX (`[a-z][a-z0-9_]*`) that rejects dot-namespaced values; widen to `[a-z][a-z0-9_]*(\.[a-z0-9_]+)*` for `integration.config.updated`/`capability.added` (AMD-58/59).
2. `AllEventClasses.ALL_EVENTS` (persistence test) — must concat the new manifest(s).
3. `EventCategoryMapping.TABLE` (persistence MAIN, not test) — a per-type category map sized to `ALL_EVENTS`; every new registered event needs an entry, or `explicitMappingCount()` mismatches. This is a real MAIN-source gap, not a count: the AMDs ratify event types but NOT their `EventCategory`, so the Coder picks sibling-consistent categories and flags `[REVIEW]` (categories are a derived runtime lookup, freely amendable).
4. `EventCategoryMappingTest` count (27→34).
5. `EventTypeRegistryTest` count (27→34).
6. `JacksonWarmupTest` count (27→34).
7. The two production AGGREGATION sites that build the real `EventTypeRegistry` from the per-module manifests: `HomeSynapseCore` (lifecycle, the composition root) and `IntegrationTestHarness` (integration-tests) — both 2-arg `Stream.concat(CORE, LIFECYCLE)`; a new manifest list (`CAPABILITY_EVENT_CLASSES`) must be added or the new events are unregistered in PRODUCTION (encode throws "unregistered event class"). AMD-59 §3 mandated this; nothing pinned its size, so it was a silent functional gap, not a test failure.
Plus the annotation tests (`*EventTypeAnnotationTest.EXPECTED_SUBTYPES`) which were in the instruction.
**Detail:** The discovery survey must, in the same pass that greps `getDeclaredMethods()` for interface-shape pins, ALSO grep the event-type set: `grep -rnE "isEqualTo\((N|M)\)|hasSize\((N|M)\)"` for the old counts, AND `grep -rn "LIFECYCLE_EVENT_CLASSES|CORE_PRODUCTION_EVENT_CLASSES|<new manifest>"` to find every aggregation site (test harnesses AND the production composition root). The category map is the easy-miss MAIN-source one — it's keyed by event-type string and sized to the registered set, so it's coupled even though it lives in a different module than the constants. An upstream-module failure ABORTS the whole gate (no `--continue`), so a single missed pin in `core:event-model` masks every downstream failure — fix the upstream-most one first, then re-run with `--continue`.
**Impact:** (1) Any instruction that adds an event-type constant or event-record class MUST enumerate all seven sites; if it doesn't, the Coder's discovery survey adds them by grepping the counts + the manifest names. (2) `core:event-model` is upstream of everything — a failing pin there hides all downstream failures; always re-run with `--continue` after fixing event-model. (3) The production composition root (`HomeSynapseCore`) and any test harness that builds a registry from the manifests are part of the lockstep — a new manifest list (not just more entries in an existing one) must be wired into every aggregation site or the events are silently unregistered at runtime. (4) `EventCategoryMapping.TABLE` (persistence main) is the easy-miss coupling — it's a per-registered-type map in a different module from the constants.

## 2026-06-06 | Category: module-boundaries | Source: M5-A SystemdHealthReporter — pure JDK 21 has no AF_UNIX datagram socket; isolate the unsupported transport behind a seam to keep the gate GREEN
**Discovery:** sd_notify uses an `AF_UNIX` `SOCK_DGRAM` socket, but OpenJDK ≤ 21 supports Unix-domain *stream* sockets only (JEP 380 delivered `SocketChannel`/`ServerSocketChannel`, never `DatagramChannel`). `DatagramChannel.open(StandardProtocolFamily.UNIX)` COMPILES but throws `UnsupportedOperationException` at runtime — and there is no pure-JDK way to bind such a socket for *receiving* either, so neither a working production transport nor a faithful real-socket unit test is possible without a native binding (JNR/JNA, not in the version catalog) or a `systemd-notify` subprocess.
**Detail:** The reporter's message protocol (`READY=1`/`WATCHDOG=1`/`STOPPING=1`/`STATUS=…`), READY-once (`AtomicBoolean` CAS), `ReentrantLock` thread-safety, and send-and-forget error handling are all fully testable; only the datagram send is not. Pattern: extract a package-private `NotifyTransport` seam (`void send(byte[])`), put the `DatagramChannel`-based `UnixDatagramTransport` behind it (correct API shape, runtime-UOE on stock JDK), and inject a *capturing* transport in tests. `./gradlew check` (compile + unit tests) stays GREEN because the real transport is never constructed in tests — the runtime gap is isolated to the composition root (M13), which is the only place that actually instantiates the reporter (with a real `$NOTIFY_SOCKET`). This is the correct STOP-and-report move: do NOT add a non-catalog dependency or a subprocess silently — flag it as a TECHNICAL PUSHBACK and let the PM choose the transport.
**Impact:** Any pure-JVM sd_notify / AF_UNIX-datagram work needs an explicit transport decision (native binding vs `systemd-notify` subprocess) BEFORE it can run on a real host — treat it as a design question, not an inline IO choice. The seam-isolation pattern (correct-shaped-but-runtime-unsupported prod impl behind a package-private interface + a capturing test double) keeps a build-deferred WU GREEN while honestly surfacing a runtime gap; reuse it whenever a JDK API compiles but isn't supported at runtime on the target platform. Because the failure is runtime-only, a "looks-done" impl can hide a non-functional deployment path — say so loudly in the handoff + a cross-agent note.

## 2026-06-06 | Category: build | Source: M5-A path-impl tests — `(Type) null` cast is redundant (and -Werror-fatal) unless it disambiguates overloads
**Discovery:** Under `-Xlint:all -Werror`, casting `null` to select a constructor/method only avoids a `[cast]` warning when there is a genuine overload ambiguity. `new LinuxSystemPaths((Path) null)` has just one single-arg ctor → the cast is redundant → `-Xlint:cast` flags it → `-Werror` fails the build. But `new SystemdHealthReporter((String) null)` has two reference-type ctors (`String` and the package-private `NotifyTransport`) → bare `null` is ambiguous → the cast is REQUIRED and is NOT flagged.
**Detail:** Decide the cast per overload-set: drop it for a single applicable overload (`new X(null)`); keep it only where two reference-typed overloads make a bare `null` ambiguous. The AssertJ `assertThatThrownBy(() -> new X(null))` form still type-checks even when the ctor declares `throws` (the checked exception is absorbed by `ThrowingCallable.call() throws Throwable`).
**Impact:** When writing null-rejection tests in this `-Werror` codebase, choose the cast by overload-set, not by habit — an unnecessary `(Type) null` is a build failure, not a style nit. This sits alongside the M2.9 `-Xlint:exports` and M4.C spotless lessons as a "the gate enforces more than it looks" build note.

## 2026-06-06 | Category: module-boundaries | Source: M5-A gate fix — `-Xlint:exports` -Werror-fatal when an exported public class exposes a supertype from a non-transitively-required module (3rd occurrence: M2.9, M3.6e.1, M5-A)
**Discovery:** `platform-systemd`'s new `module-info` did `requires com.homesynapse.platform;` (plain) while its four exported public classes (`LinuxSystemPaths`/`LocalPaths`/`SystemdHealthReporter`/`NoOpHealthReporter`) `implement` `PlatformPaths`/`HealthReporter` from that module. javac `-Xlint:exports` emits `interface … is not indirectly exported using 'requires transitive'` once per class; `-Werror` makes it fatal. Green unit tests never surface it — it is a pure main-source `compileJava` warning.
**Detail:** Rule — if a public type in an EXPORTED package exposes (as a supertype, return type, parameter, or field) a type from another module, that module must be `requires transitive`, and the Gradle dependency must be `api(...)` in lockstep (api ↔ requires transitive; implementation ↔ plain requires). Fix here: `requires com.homesynapse.platform` → `requires transitive`, and `implementation(project(":platform:platform-api"))` → `api(...)`. `org.slf4j` stayed plain/`implementation` — it appears only in method bodies, never on the public API, so it does NOT trip `-Xlint:exports`. This is the THIRD occurrence (M2.9 `SqlitePersistenceLifecycle`; M3.6e.1 the package-private-impl + public-gateway variant; now M5-A). It recurs specifically when a coding instruction hands a *verbatim* `module-info` for a NEW module that will export impls of a dependency's types and that embed is non-transitive — honoring it blindly reproduces the defect.
**Impact:** (1) When an instruction supplies a verbatim `module-info` for a new module whose exported public classes implement/return another module's types, treat a plain `requires` as suspect and promote to `requires transitive` + `api` (do NOT honor the non-transitive embed blindly — flag it as a `[REVIEW]` correction). (2) Pre-handoff self-check for every `-Werror`-sensitive module touched: for each public exported class, does its signature name a foreign-module type? If yes, that module needs `requires transitive`. A targeted `compileJava` on the touched module is the cheapest way to surface `-Xlint:exports` before handing back. (3) `-Xlint:exports` is main-source-only — passing tests do not clear it.

## 2026-06-07 | Category: module-boundaries | Source: M5-A Part 2 (AMD-87) — a non-transitive `requires X` compiles GREEN with NO direct Gradle dep when X is already on the compile classpath via another dependency's `api` (the lockstep's transitive exception)
**Discovery:** persistence's `module-info` gained `requires com.homesynapse.device` (non-transitive) with **no** matching `implementation(project(":core:device-model"))` in `build.gradle.kts` — and it compiles GREEN. device-model is already on persistence's compile classpath transitively: persistence `api(":core:state-store")` and state-store `api(":core:device-model")`, so Gradle's `api` propagation puts device-model's module on persistence's compile module path. The §13 lockstep ("plain `requires X` ⇔ `implementation(project)`") is really about ensuring X is *reachable*; when a transitive `api` chain already makes it reachable, the direct dep is redundant for a GREEN build (every OTHER `requires` in this module-info happens to be directly paired — this is the lone exception).
**Detail:** Verified from source before relying on it (Source Trust Hierarchy — actual `build.gradle.kts`/`module-info`, not summaries): state-store `module-info` `requires transitive com.homesynapse.device` (JPMS readability) + state-store `build.gradle.kts` `api(project(":core:device-model"))` (Gradle compile classpath). The instruction explicitly scoped `build.gradle.kts` out ("only the module-info changes"), and the PM's own note said "persistence already reads device transitively via `requires transitive state → transitive device`" — leaning on transitive availability was intended, not an oversight, so I did NOT add the direct dep; flagged `[INFO]` with the fragility: if a future refactor narrows state-store's `api(":core:device-model")` to `implementation`, persistence's compile breaks ("module com.homesynapse.device not found") and the direct dep must be added. SEPARATELY the persistence→device **moduleGraphAssert** STOP-gate is clean: root `build.gradle.kts` `allowed` lists `:core:.* -> :core:.*` (any core→core edge) and `restricted` has no `persistence -X> device`; the edge is acyclic (device requires value/event/platform only) and a *shortcut* over the existing persistence→state→device path so `maxHeight=7` is unchanged.
**Impact:** (1) A non-transitive `requires X` does NOT always need a direct `implementation(project)` — check whether X is already on the compile classpath via a transitive `api` chain before adding one (or before flagging the instruction as incomplete). (2) When you DO lean on transitive availability, record it (handoff `[INFO]` + a MODULE_CONTEXT gotcha) so a future narrowing of the intermediary's re-export prompts adding the direct dep. (3) Before claiming a new cross-layer JPMS edge is gate-safe, read the root `moduleGraphAssert` `allowed`/`restricted` arrays — core→core is explicitly allowed here, so the edge passes; a cycle is not the only failure mode (an edge-allowlist would be the other).

## 2026-06-07 | Category: serialization | Source: M5-A Part 2 (AMD-87) — a hand-rolled sealed-type codec delegates nested registered codecs via `provider.defaultSerializeValue` / `ctxt.readTreeAsValue`; closes the M4.C DegradedEvent gap
**Discovery:** The AMD-87 `Expectation` codec is the exact twin of the AMD-52 `AttributeValue` codec: keyed on the sealed **interface** in `PersistenceJacksonModule` (Jackson's `SimpleSerializers` walks a concrete permit's superclasses/interfaces, so all permits resolve through the one pair — never register per-permit), the serializer is an exhaustive `switch` with NO `default` (a 5th permit breaks compilation), the deserializer dispatches on a `String` tag with a rejecting `default` (open discriminator, unlike the serializer's total sealed switch). The two permits that wrap an `AttributeValue` (`ExactMatch`/`AnyChange`) REUSE the already-registered AttributeValue codec instead of re-encoding: serialize with `provider.defaultSerializeValue(value, gen)`, decode with `ctxt.readTreeAsValue(valueNode, AttributeValue.class)` (Jackson ≥2.13). The numeric permit (`WithinTolerance`) reuses the package-private `AttributeValueSerializer.writeDouble`/`AttributeValueDeserializer.readDouble` statics (same package) for AMD-52 bit-anchored-float + sentinel determinism — never re-implement `doubleToLongBits`/sentinels.
**Detail:** This resolves the M4.C `@Disabled("AMD-65 pending")` DegradedEvent gap (see the 2026-06-05 AMD-59 lesson): a command-bearing `CapabilityAdded`'s `CommandDefinition → ExpectedOutcome → Expectation(ExactMatch) → AttributeValue(BooleanValue)` subtree now round-trips because the one missing leaf (`Expectation`) got a codec that delegates its wrapped value back to the existing AttributeValue codec. Wire form: `{"t":"ExactMatch","v":{"t":"BOOLEAN","v":true}}`; `WithinTolerance` → `{"t":"WithinTolerance","target":<num-or-sentinel>,"tolerance":<num-or-sentinel>}` (the AMD-87 §2 `<bits>` notation is the bit-identity *guarantee*, realized by Schubfach round-trippable text + `"NaN"`/`"+Inf"`/`"-Inf"` sentinels — NOT a literal long; the instruction says reuse the helper, "do not re-invent the encoding"). Test edge: a record's auto-`equals` for `double` uses Double semantics (NaN==NaN true; -0.0≠+0.0), and the codec canonicalizes -0.0→+0.0, so assert `WithinTolerance` -0.0 round-trip by `Double.doubleToLongBits(decoded.target()) == doubleToLongBits(0.0)`, NOT `isEqualTo`. **Un-disable gotcha:** deleting the only `@Disabled` in a test file leaves `import org.junit.jupiter.api.Disabled;` unused → Spotless `removeUnusedImports` fails `check`; remove the import in the same edit (see the M4.C spotless lesson).
**Impact:** (1) To add a hand-rolled codec for a sealed type that mirrors an existing one, copy the precedent exactly — interface-keying, no-`default` serializer switch, `String`-tag deserializer WITH a `default`. (2) Delegate nested registered codecs, don't re-encode: `provider.defaultSerializeValue(value, gen)` (write) and `ctxt.readTreeAsValue(node, Target.class)` (read) route through whatever is registered for the runtime/target type in the SAME mapper. (3) Reuse package-private `static` float helpers across same-package codecs for determinism; never duplicate `doubleToLongBits`/sentinel logic. (4) When a `double`-bearing record round-trips through a -0.0→+0.0 canonicalizing codec, assert bit-identity via `doubleToLongBits`, not record `equals`. (5) Un-disabling a test = delete `@Disabled` AND its now-unused import.

## 2026-06-09 | Category: JPMS / contracts | Source: AMD-66–71 DOCS review E70-1 (PM-side design miss; Coder guard for M6.1/M6.4)
**Discovery:** A type that lives in a **base/shared module** (`event-model`, `value-model`, `platform-api`) may reference, in its fields/components, **only** types from that module, a leaf module, or `java.base`. Referencing a **higher-layer domain module's** type (`config`/`device`/`state`/`automation`/`integration`) inverts the JPMS edge and creates a cycle — `config requires transitive event`, so a config type inside a `com.homesynapse.event` record forces `event → config` (the AMD-52 `event↔device` class).
**Detail:** This bit AMD-70's config observability events at the design stage — caught by independent review and flattened before any code, so M6.1's `config.validation_completed` is `(int configSchemaMajor, int configSchemaMinor, int issueCount, Map<String,Integer> severityCounts)` and M6.4's `config.section_reloaded` is `(String sectionPath, int changeCount, int issueCount, String appliedClassification)` — all `java.base`/`String`, mirroring the existing all-`String` `ConfigChangedEvent`. The no-`default` interface-keyed codec pattern (AMD-52/87) works precisely because those payload types live in `value`/`event`, never above.
**Impact:** (1) Before writing **any record in a base module**, read its `module-info` and confirm every component type's module is that module, a leaf, or `java.base`. (2) If a coding instruction's event record names a higher-layer domain type in its payload, **STOP and flag it `[BLOCKING]`** — do not implement around it; the fix is flatten-to-`java.base`/`String` or route typed data through a leaf (`com.homesynapse.value`, e.g. `QuantityValue`). (3) Nick's standing directive (2026-06-09): always be mindful about contracts and JPMS.

## 2026-06-09 | Category: module-boundaries / build | Source: M6.1 — third-party libraries on the JPMS module path require `requires` directives regardless of Gradle scope
**Discovery:** Gradle `implementation(...)` scope does NOT exempt a library from JPMS: in this build every dependency that ships a `module-info.class` or an `Automatic-Module-Name` resolves on the **module path**, so a named HomeSynapse module cannot `import` it without a matching `requires` directive — "it's only an implementation dependency" is the `requires transitive`↔`api` lockstep's blind spot in reverse.
**Detail:** The M6.1 instruction mandated `config/configuration/module-info.java` unchanged ("the pipeline is package-private; the libraries are already on the `implementation` classpath") while requiring YAML/JSON-Schema implementation classes inside `com.homesynapse.config`. Jar inspection from the Gradle cache (`jar --file <jar> --describe-module --release N`) showed snakeyaml-engine 2.9 is the explicit module `org.snakeyaml.engine.v2` and json-schema-validator 1.5.6 is the explicit module `com.networknt.schema` (whose own `module-info` requires jackson-databind NON-transitively, so Jackson types don't flow through it). Precedent proving the module-path resolution: persistence's `module-info` declares `requires com.fasterxml.jackson.databind`/`requires org.slf4j` for its `implementation`-scoped libraries and is GREEN at `7f44bed`. A named module cannot read the unnamed module, so there is no compile path that honors both halves of the instruction — STOP-and-report fired (the instruction pre-authorized exactly this), M6.1 split at the Fork-2 seam, and the lib-free half (M6.1b) shipped while M6.1a awaits the ruling.
**Impact:** (1) When an instruction says "library X is available on the implementation classpath" for a JPMS module, verify the module name from the jar (`jar --describe-module`, with `--release` for multi-release jars) and check whether the embedded module-info permits the needed `requires` — BEFORE writing code against the library. (2) The minimal-directive recipe for implementation-only libraries: plain (non-transitive) `requires`, consumers package-private, which keeps `-Xlint:exports` silent and the public API clean. (3) An automatic/explicit module's own `requires` list determines what flows through it — check it (networknt does not re-export Jackson). (4) "No new requires" guarantees in AMD/instruction embeds should be scoped to HomeSynapse-module edges explicitly, or they falsify the moment an in-module implementation needs its first library.

## 2026-06-10 | Category: verification / build | Source: M6.1a — verify third-party API behavior from jar bytecode (`javap -c`) when the sandbox cannot execute probes
**Discovery:** The M6.1a validator mapping depended on an undocumented networknt-1.5.6 behavior: for `required`/`additionalProperties` violations, is `ValidationMessage.getInstanceLocation()` the containing object's path or does it already include the offending property? Getting it wrong double-appends or truncates every `ConfigIssue.path`. A throwaway runtime probe (`javac`+`java` in /tmp against the Gradle-cache jars) was permission-denied — the no-build discipline is enforced at the environment level, not just by convention. The answer was still obtainable statically: `javap -c -p` on `AdditionalPropertiesValidator`/`RequiredValidator` shows the message builder receiving `instanceLocation` from the **method parameter** (`aload 4`) while `property(...)` receives the iterated key (`aload 7`/`aload 11`) — so the message's instanceLocation is the containing object and the property must be appended by the caller. Signature-level checks (`javap` without `-c`) had already pinned the builder APIs, exception hierarchies (`DuplicateKeyException extends ConstructorException extends MarkedYamlEngineException`), and `LoadSettingsBuilder` methods.
**Detail:** Escalation ladder for pre-gate API verification in this no-execution sandbox: (1) `jar --describe-module --release N` for module names; (2) `javap -cp <jar>` for signatures/ctors/hierarchies (catches wrong method names, missing overloads, checked-vs-unchecked — most `-Werror` API risk); (3) `javap -c -p` dataflow reading for behavioral questions (which value flows into which call); (4) only then fall back to defensive both-ways coding (e.g. also reading `getArguments()[0]` when `getProperty()` is null) with the uncertainty documented. Also useful: grepping the disassembly for `invokevirtual` on a specific method name answers "does this class ever call X" without source access.
**Impact:** (1) Do not hand Nick a gate run to discover an API-shape error — `javap` against the Gradle-cache jar answers signature questions in seconds and `-c` answers many behavioral ones; cite "bytecode-verified" in the handoff so the PM knows the claim's basis. (2) A denied probe is a signal, not a dead end — the no-`javac` rule includes /tmp scratch probes; static inspection is the sanctioned alternative. (3) Keep the defensive fallback ONLY when static evidence is genuinely ambiguous; prefer deleting dead defensiveness once bytecode settles the question (the M6.1a `getArguments()` fallback was kept deliberately as cheap insurance against minor-version drift — documented in the validator).

## 2026-06-11 | Category: testing / filesystem | Source: M6.4 — portable atomic-write failure injection via a directory squatting on the temp-file name; multi-release module-info location
**Discovery:** The §6.8 "disk full / write failure leaves the prior file intact" contract and the REC-135 migration crash-window both need a failure injected INSIDE the atomic-write window without seams or mocks. Pre-creating a DIRECTORY at the exact temp-file name (`homesynapse.yaml.tmp`) makes `FileChannel.open(tmp, CREATE, WRITE, TRUNCATE_EXISTING)` throw on both NTFS (`AccessDeniedException`) and POSIX (`IsADirectoryException`) — a one-line, platform-portable injection that fails the flush AFTER any backup-copy step, exactly where the crash-window test wants it. No test seam enters production code; the production `catch` path (delete-tmp best-effort, prior file untouched, rethrow/WARN) is exercised for real.
**Detail:** Companion findings from the same WU: (1) `Files.move(tmp, target, ATOMIC_MOVE)` replaces an existing target on BOTH platforms (POSIX `rename(2)`; Windows `MoveFileEx(MOVEFILE_REPLACE_EXISTING)`) — no `REPLACE_EXISTING` needed, but if an exotic filesystem ever surfaces `AtomicMoveNotSupportedException` at the gate, adding the flag is the one-line fallback (documented in the handoff). (2) Directory fsync after rename is POSIX-only — `FileChannel.open(dir, READ)` throws on Windows; treat it as best-effort/DEBUG, never load-bearing. (3) Backup-file stamps in file NAMES must use ISO-8601 BASIC format (`yyyyMMdd'T'HHmmss'Z'`) — the extended form's colons are illegal in Windows filenames. (4) `jar --describe-module` can return EMPTY silently on a multi-release jar in this shell; the authoritative fallback is `unzip -l <jar> | grep module` then `javap` on the extracted `META-INF/versions/<N>/module-info.class` (snakeyaml-engine 2.9 keeps its module-info at `versions/11/`, which also definitively lists `exports` — how `…v2.common`/`FlowStyle` reachability was confirmed for the M6.4 Dump-side emission).
**Impact:** (1) For any future atomic-file-write test (M6.2 `secrets.enc` is next), use the tmp-directory-squat injection — no seams, portable, exercises the real failure path. (2) Windows-compatibility checklist for file artifacts: no colons in names, directory-fsync optional, mtime granularity coarse (force distinct mtimes with `Files.setLastModifiedTime`, never sleep). (3) When `--describe-module` is silent, go straight to the versioned module-info — it answers both the module-name AND the exports question in one javap.

## 2026-06-11 | Category: testing / change-impact | Source: M6.4 gate-fix round 1 — a publish-behavior change must sweep EVERY test class asserting publisher interaction counts, not just the class being edited
**Discovery:** M6.4's DP-10 made `load()` publish one `config_error` per ERROR issue alongside `config.validation_completed`. The delivery swept `StandardConfigurationServiceTest` (its `publishedEvent()` helper was made type-filtering and its draft-count assertions checked) — but `ConfigLayoutTest.composeAfterMerge`, an AMD-71 layout test whose fixture happens to carry one ERROR issue, also pinned `rootDrafts.hasSize(1)` and failed Nick's gate (169/1). The blast radius of "this operation now publishes more events" is every test that drives the operation through a recording publisher, regardless of what the test is nominally about.
**Detail:** The 30-second pre-handoff check that would have caught it: `grep -rn "rootDrafts\|chainedDrafts" <module>/src/test` and audit every count-bearing assertion (`hasSize`, positional `get(0).payload()` casts) against the new publish behavior. Positional casts are the stealthier twin — `(SomeEvent) rootDrafts.get(0).payload()` breaks by ClassCastException or silently grabs the wrong draft when event order or count changes; the fix idiom is type-filter-then-assert (select by payload type, order-agnostic, then assert exactly-one + fields), which also survives future additions with no ordering contract. Fix pattern when a pin goes stale by ruling: bump the pin WITH a dated comment naming the ruling (here: 1→2 at M6.4 per R1/DP-10) so the next reader knows it was deliberate, and keep the test's original subject assertions untouched.
**Impact:** (1) Any change that adds/removes/reorders published events: module-wide grep for the recording double's fields BEFORE handoff; audit counts AND positional accesses. (2) Prefer type-filtered exactly-one assertions over positional casts in new tests (the M6.4 suites already do this — only the older test broke). (3) A fixture chosen to exercise feature A (compose-after-merge) can silently also exercise feature B (ERROR publication) — "what does this fixture's data trigger?" is part of the sweep, not just "what does this test claim to test?"

## 2026-06-13 | Category: module-boundaries | Source: M6.3 (at-rest payload encryption) — consuming a consumer-defined seam across a no-edge boundary: mirror the small mapping as a java.base value, gate on injection-presence

When module A (persistence) defines a seam interface and module B (config) supplies the impl, but neither may `requires` the other (the Doc 15 §3.8 / AMD-52 cycle class), the *data* that crosses the seam must be `java.base`-only. M6.3 needed an event→encryption-scope mapping whose canonical owner is config's `EncryptionScope` enum — but persistence resolves the scope on the write path and cannot import it. Resolution: persistence mirrors the **one** MVP edge (`PRESENCE → "presence_personal"`) as a `private static final String` + a `Set<String>` enabled set, documented as a deliberate mirror and pinned by a test — exactly the discipline that already keeps `EncryptedPayload` distinct from config's shape-identical `ScopeCipherResult`. **Also:** when a config flag (here `at_rest_enabled`) isn't wired yet because live `ConfigModel` plumbing is a later WU, model the switch as *injection-presence* — the factory enables the default scope set iff a non-null cipher was injected. This keeps the no-crypto path (null cipher ⇒ empty set ⇒ plaintext-for-all) the M6.2 production state, makes existing tests pass unchanged, and confines the fail-closed throw to a defensive store-level guard you can test directly.

## 2026-06-13 | Category: testing / filesystem | Source: M6.3 — a foreign JDBC connection to a LIVE SQLite-WAL file blocks on Windows; raw-row assertions must read after the executor is shut down

To assert at-rest stored bytes (ciphertext `payload`, non-null `payload_iv`/`dek_ref`) you need raw column access, not the store's decrypting read path. But opening a second `DriverManager` connection to the same db file while the `DatabaseExecutor`'s connections are alive blocks / SQLITE_BUSYs on Windows (the `PersistenceFactoryAbandonTest` precedent: it reopens the file only *after* abandon). Discipline: do decrypting round-trips through the live store, then `dbExecutor.shutdown()`, then open the foreign read connection (`PRAGMA busy_timeout` set) — SQLite auto-recovers the WAL on the fresh open and sees all committed rows. **Also:** when the test module can't depend on the module owning the production cipher (persistence has no `config` test dep, and adding one would violate the "no new dependency" gate), model the production contract with a faithful file-backed test double — a `CountingPayloadCipher` with a durable per-(scope,version) counter (persist-before-return + re-init-on-reconstruction) reproduces the OR-M6-NONCE write-path property end-to-end, while the *real* manager's durability is proven by a unit test in its own module.

## 2026-06-13 | Category: anti-pattern / build | Source: M6.3 gate-fix round 1 — an LLM "compile-review" pass is NOT javac; never report compile-clean off a reasoning pass

A multi-agent adversarial review explicitly tasked with compile-correctness reported the persistence test sources "CLEAN", but `./gradlew check` then failed `compileTestJava` with 11 `unreported exception SequenceConflictException` errors. The seam: `EventPublisher.publishRoot`/`publish` declare the **checked** `SequenceConflictException` (`extends Exception`), and the new test methods called them directly without declaring/catching it. The review (and I) had verified the ONE call inside an `assertThatThrownBy(() -> ...)` lambda (absorbed by `ThrowingCallable.call() throws Throwable`) and generalized from it — missing that the *other* call sites propagate. **Lesson:** an LLM pass catches API-shape mismatches but is unreliable for the mechanical checks `javac` does instantly — checked-exception propagation, overload resolution, generic inference, definite assignment. When CLAUDE.md forbids running the build, hand work back as **compile-fix-applied / NOT gate-verified**, never "compile-clean", and audit EVERY call that crosses a checked-exception boundary (`publishRoot`/`publish`, anything `throws SQLException`/`IOException`, file I/O) by reading the callee's `throws` clause — don't extrapolate from one verified site. Also: `compileTestJava` aborts at the first failing module, so a green-looking run of *other* modules proves nothing about them — audit them too.

## 2026-06-13 | Category: persistence / build | Source: M6.3 gate-fix round 2 — a SQL migration's trailing inline comment becomes a comment-only "statement" that aborts the migration

`MigrationRunner.splitSqlStatements` splits a migration file on `;` (ignoring `;` inside `--` lines) and executes EVERY non-empty trimmed fragment, including the trailing `tail` after the last `;`. So a trailing inline comment — `ALTER TABLE ... ;   -- note` — leaves `-- note` as a standalone comment-only fragment after the final `;`, which sqlite-jdbc compiles to no statement and rejects with `The prepared statement has been finalized`, aborting the whole migration on every DB open. **Idiom (V001-V004, now V005):** keep ALL commentary in the file's header `--` block; statements stay clean; no trailing inline comments; ideally no `;` anywhere but the statement terminators. **Process meta-lesson reinforced:** this is the second M6.3 defect the deferred build gate caught that an LLM reasoning pass declared clean (round 1: a checked-exception propagation; round 2: this). When CLAUDE.md forbids running the build, hand back as fix-applied / NOT gate-verified — never "clean" — and remember a green compile says NOTHING about runtime (migrations, assertions, the crypto/counter logic only execute under `test`).

## 2026-06-19 | Category: planning / composition-root | Source: M7.1 — verify the runtime actually assembles a dependency before committing to a wiring deliverable

A detailed coding instruction can over-assume the runtime's assembled state. M7.1's instruction listed "wire the `automation_engine` subscriber into `HomeSynapseCore.start()`" + a mandated lifecycle wiring test — but a composition-root probe (does `start()` construct a `ConfigurationService`? an `EntityRegistry`/`DeviceRegistry`/`AreaRegistry`?) found it constructs NONE of them, and those registries have NO production implementations anywhere (Phase-2 interface-only). The wiring was physically blocked on app-bootstrap. **Lesson:** before accepting any composition-root/wiring deliverable, grep the composition root for the `new`/factory construction of EVERY dependency the new component needs; if a dependency isn't assembled in the runtime, the wiring is blocked — surface it as a scope finding (and to the user) rather than producing non-compiling stub wiring. The implementable core (impls + tests against injected/stubbed deps) is unaffected and ships; only the runtime `new` is blocked.

## 2026-06-19 | Category: JPMS / API hygiene | Source: M7.1 — package-private seams honor a FIX-07 `requires` re-add WITHOUT leaking the dependency onto the exported API

FIX-07 required re-adding `requires com.homesynapse.config` + `requires com.homesynapse.event.bus` (plain/`implementation`, NOT `requires transitive`/`api`) — but those edges are only legitimate if NO config/bus type appears on the exported package's public signatures (the `requires transitive`↔`api` authoring check). The clean resolution: put all config/bus references inside **package-private** classes (`AutomationConfigBridge`, `AutomationEngineSubscriber`). A package-private class's signatures are NOT part of the exported API even though the package is `exports`-ed, so the plain `requires` stays correct AND the `requires` is genuinely used (not dead). Bonus: these package-private classes ARE the composition-root wiring seams, so the FIX-07 re-add and the wiring seam are the same artifact. **Lesson:** when a base module must `requires` a dependency it uses only internally, confine the usage to package-private types to keep it off the exported API and preserve the plain-`requires`/`implementation` scope.

## 2026-06-19 | Category: build / -Werror | Source: M7.1 self-audit — redundant cast `(FloorId) null` and `-Xlint:serial` on a nested exception

Two mechanical `-Xlint:all -Werror` traps caught in self-review (the kind the deferred gate catches that an LLM pass misses): (1) `new Area(id, name, (FloorId) null, ...)` — casting `null` to the exact parameter type is a REDUNDANT cast → `-Xlint:cast` error; just pass `null`. (2) a `private static final class DefinitionException extends RuntimeException` needs `private static final long serialVersionUID = 1L` or `-Xlint:serial` fails the build (RuntimeException is Serializable). **Lesson:** before handoff, scan new code for (a) casts of `null`/literals to a declared param type, (b) any `extends` of a Serializable type (Exception/Throwable subclasses) without a `serialVersionUID`, (c) unused imports (Spotless `removeUnusedImports` FAILS `check`). Hand work back as compile-fix-applied / NOT gate-verified.

## 2026-06-19 | Category: JPMS / build gate | Source: M7.1 gate-fix — the §authoring exported-API check is necessary but NOT sufficient; `assertAllowedModuleDependencies` bans a forbidden LAYER edge at ANY scope (SUPERSEDES the same-day "package-private seams" lesson for the config case)

The M7.1 instruction's FIX-07 directive told the Coder to re-add `requires com.homesynapse.config` to `core:automation`, with the rationale "config types stay off the exported API → plain `requires`/`implementation` is fine." Both the Coder and a 4-agent adversarial review verified the §authoring check (no config type on any exported signature — TRUE, config was used only inside the package-private `AutomationConfigBridge`) and passed it. **The build gate then failed:** `assertAllowedModuleDependencies` (root `build.gradle.kts`) bans the `core:automation -> config:configuration` edge at EVERY scope — `api` AND `implementation` — because the allowed module graph encodes a LAYER rule ("Core depends only on platform + other core"; config→core is sanctioned, core→config is not). Package-private confinement does NOT help: the task operates on the Gradle dependency graph, not on exported-API surface. **Lesson:** before adding ANY `requires`/dependency to a base module, check TWO independent gates — (1) the `requires transitive`↔`api` exported-API §authoring check (does the type appear on an exported signature?), AND (2) `assertAllowedModuleDependencies` (is this `from->to` LAYER edge in the allowed list at all?). A `core->config` (or any backward/cross-layer) edge fails (2) regardless of (1). **Correction to the same-day "package-private seams" lesson:** that technique legitimately keeps a type off the exported API (so a plain `requires`/`implementation` is correct for an ALLOWED edge — e.g. the `event.bus` `core->core` seam, which stayed), but it CANNOT rescue a forbidden layer edge — the config seam had to MOVE OUT of `core:automation` entirely. The architecturally-correct home for composition-root glue (read `ConfigurationService.getCurrentModel().rawMap()` → `loader.load(...)`; `registerCoreSchema(...)`) is `lifecycle`/`app` (allowed `->core` and `->config`); keep any config-free constants (a JSON schema `String`) near the owning module. **Process meta-lesson reinforced (3rd time):** the deferred gate caught what an LLM pass declared clean — hand back as fix-applied / NOT gate-verified, and when reviewing a base-module dependency, the review brief MUST include the module-graph allow-list, not only the exported-API check.

## 2026-06-19 | Category: instruction-defect / spec-vs-source | Source: AB-3 — a coding instruction's example glue can be subtly wrong against the real loader/schema contract; verify the data path end-to-end

AB-3's §3b gave the glue verbatim as `schemaRegistry.registerCoreSchema(SECTION, JSON); LoadResult r = loader.load(getCurrentModel().rawMap());`. Two latent defects were caught only by reading the actual source end-to-end (both would have shipped a silently-broken automation path that no smoke test on an empty config would surface): (1) **Schema-registration ordering.** The automation schema is a CORE schema, and `ConfigurationService.load()` validates every section against the composed schema. Registering it AFTER `load()` (as the §3b ordering reads) means an `automation:` section fails validation (root `additionalProperties:false`) → FATAL boot. Doc 12 Phase 1 is explicitly "core-only schema composition" → register core schemas BEFORE `load()`; only integration schemas defer (to after Phase 6). (2) **Section nesting.** `AutomationSchema.SCHEMA_SECTION = "automation"` registers a config SECTION; `getCurrentModel().rawMap()` is the whole document keyed by section (`{automation: {automations: [...]}}`), but `AutomationDefinitionLoader.load(doc)` reads `doc.get("automations")`/`doc.get("schema_version")` at the TOP level. So `loader.load(rawMap())` finds NOTHING — every automation is silently ignored. The correct call passes the section content: `loader.load((Map) rawMap().get("automation"))`. **Lesson:** when an instruction hands you composition glue between two modules, trace the data shape through BOTH ends against source (what key does the consumer read? at what nesting does the producer place it? when does validation run relative to registration?) — a verbatim two-line snippet can be contract-wrong in a way that compiles, passes an empty-config boot, and silently drops the feature. Verify with a populated fixture, not just the zero-config path.

## 2026-06-19 | Category: instruction-defect / JPMS | Source: AB-3 — a public assembly seam returning a dependency's interface forces requires-transitive even when the concrete impl stays package-private

AB-3's instruction asserted the `core:automation` module-info was "UNCHANGED" and that a public seam "returns the Subscriber (keeps event.bus-coupled internals off the exported API)." Both halves are wrong together: a `public static Subscriber automationEngineSubscriber(...)` returning `com.homesynapse.event.bus.Subscriber` puts that INTERFACE on automation's exported API, so `-Xlint:exports -Werror` demands `requires transitive com.homesynapse.event.bus` (+ Gradle `api(...)`) — even though the concrete `AutomationEngineSubscriber` class stays package-private. Keeping the concrete class package-private hides the CLASS, not the INTERFACE return type. (Precedent: lifecycle's `HomeSynapseCore` public accessors return event/event.bus/state types, and lifecycle correctly uses `requires transitive` for those.) **Lesson:** "package-private concrete class" ≠ "dependency off the exported API." Any public method whose signature (return type OR param) names a type from module M forces `requires transitive M`. Before trusting an instruction's "module-info UNCHANGED" claim for a new public seam, check whether the seam's signature surfaces a foreign type — if so, the transitive bump (and the Gradle api↔requires-transitive lockstep) is mandatory.

## 2026-06-19 | Category: spec-discipline / fail-closed-semantics | Source: AB-3 WUCP Phase 2 (Nick) — "fail-closed per entry" ≠ "fail the whole boot"; a green test encoding the wrong contract is worse than red

AB-3 read SD-9 "fail-closed-at-load" + "Doc 12 §4 Automation = FATAL" and implemented it as **throw → fail the whole boot** on any malformed automation definition, with a wiring test asserting `start()` throws. The gate went GREEN — but the test encoded the WRONG contract. The `AutomationDefinitionLoader`'s ratified Javadoc is explicit: a bad definition is "rejected and surfaced (a LoadFailure the caller publishes as config_error); valid sibling definitions still load… never loaded into a silently-inert state." So "fail-closed" is **per-definition** (the bad rule fails SAFE = does not run), not "fail the process." And "Doc 12 §4 Automation = FATAL" governs subsystem-INIT failure (the engine can't start at all), NOT a per-entry config error. FATAL-boot-on-one-typo reintroduces the exact "one bad entry bricks the box" anti-pattern the loader was designed to avoid. **Fix:** the caller iterates `LoadResult.failures()`, publishes a `config_error` event + logs each, and boots with `LoadResult.loaded()` (the valid subset). **Lesson:** when a spec gives you a fail-closed/FATAL directive, pin DOWN the SCOPE before coding — per-record vs per-section vs per-subsystem-init are different failure domains. Re-read the producing component's ratified contract (the loader literally said "valid sibling definitions still load" — I'd read it and still over-applied it). And a passing test is not validation if it asserts the wrong behavior: a green test that locks in a contract violation is worse than a red one, because downstream WUs build on the wrong posture. The deferred gate (`assertAllowedModuleDependencies`, clock-injection, compile) is structurally blind to "correct-but-wrong-contract" — that is exactly what human WUCP Phase 2 review is for.

## 2026-06-20 | Category: anti-pattern | Source: AB-1 Seam-1 go-live (WS first-message auth)
**Discovery:** A "MODIFY the X handler" instruction can name a runtime that does not exist yet — verify the target is real at HEAD before treating it as wiring, not building.
**Detail:** AB-1 told the Coder to "MODIFY the WS upgrade/auth handler (first-message authenticate, 4403/4408)." But `api/websocket-api` is a Phase-2 scaffold: only interfaces + records, no `WebSocketHandler`/`MessageCodec`/`EventRelay`/`WebSocketLifecycle` impl, no `app.ws("/ws/v1", …)` wiring, and `api.ws`'s module-info has zero runtime deps. There was nothing to modify. The check is cheap: `grep -rn "app.ws(\|implements WebSocketHandler" api` + read the module-info (`requires transitive api.rest` only) + the MODULE_CONTEXT ("Scaffold"/"Consumers: None"). Building it would have meant the whole WS Phase-3 runtime + new module edges (io.javalin, jackson, event-bus, state into api.ws) — a separate WU, not "wiring existing pieces."
**Impact:** When an instruction says MODIFY a named runtime component, confirm it exists (grep for the impl + an `app.X(` registration + check the module-info has the runtime deps) during STOP-on-Mismatch — even when the gate table doesn't list it. If absent, STOP-and-report it as a blocking scope finding and build the separable parts (here AB-1 REST + AB-2 shipped; only the WS half was blocked). Mirrors the AB-3 "compiling ≠ correct — verify against the consumer's real contract" lesson, one level up: verify the consumer EXISTS.

## 2026-06-20 | Category: module-boundaries | Source: AB-1 Seam-1 go-live (start() now binds HTTP)
**Discovery:** Making a widely-called lifecycle method newly acquire a resource (here: `HomeSynapseCore.start()` binding the HTTP port behind auth, previously C1-closed) is a consumer/pin fan-out just like a signature change — every caller's downstream assumptions shift.
**Detail:** Flipping `start()` to open HTTP-behind-auth broke EVERY existing unauthenticated HTTP E2E test (`EndpointE2eIT` ×5, `CrashRecoveryHttpIT` ×2, `InFlightRequestShutdownIT`) — they now get 401. The fix threaded the first-run pairing token through `HomeSynapseE2eHarness.authToken()` (reads the `initial_api_token` artifact, stable across abandon→restart on the same config dir) and added `Authorization: Bearer` to every call site. Two more fan-out traps: (a) binding IPv4 loopback `127.0.0.1` means clients must connect to `127.0.0.1`, NOT `localhost` (which can resolve to IPv6 `::1` and miss an IPv4-only bind) — fixed `baseUri()`. (b) Fixed-port (7070) binding in many lifecycle unit tests is flaky; moved the smoke tests to the ephemeral `HomeSynapseConfig.testing()` port and kept only the dedicated auth test asserting the production default via a no-bind constant check.
**Impact:** When `start()`/a lifecycle method begins binding/owning a NEW resource, grep every caller (prod + test, all modules) and sweep for: (1) callers that assumed the resource was absent, (2) loopback IPv4-vs-`localhost`-IPv6 mismatches, (3) fixed-port multi-bind flakiness across tests. Also: throwing from a Javalin `before(*)` handler (not merely setting a status) is what halts the pipeline so an unauthenticated request never reaches the endpoint — pair it with an `app.exception(...)` handler that serializes the RFC 9457 body.

## 2026-06-21 | Category: anti-pattern | Source: M7.2a-1 (run lifecycle) — when a coding instruction's step-ordering contradicts a Locked MODULE_CONTEXT/design-doc contract, the LOCKED contract wins; deviate-with-pushback, don't follow the instruction literally
**Discovery:** The M7.2a-1 instruction's "Admission sequence (order matters)" put concurrency-mode enforcement at step 5 and the condition gate in the run body (step 6+, on the VT). But the automation MODULE_CONTEXT cross-module contract (and Doc 07 §3.6) state verbatim: *"Condition evaluation occurs at trigger time, **before mode enforcement** … a CONDITION_NOT_MET Run does NOT consume a concurrency mode slot."* The instruction's literal ordering admits the Run (consuming a slot) before conditions are evaluated, so a CONDITION_NOT_MET Run transiently holds a slot (released only on terminal) — which violates the contract in the concurrent case (a SINGLE automation whose conditions keep failing can still wrongly drop a concurrent trigger). The two cannot both hold: synchronous mode-rejection (initiateRun returns empty + run_skipped with NO automation_triggered) requires conditions to be evaluated synchronously BEFORE mode, so EVALUATING must run in `initiateRun`, not on the VT.
**Detail:** Resolution = honor the locked contract: evaluate the condition gate synchronously in `initiateRun` (it is a pure decision; the gate is a stub in this slice) BEFORE the mode-admission block; a false gate publishes triggered+completed(CONDITION_NOT_MET) and returns the RunId WITHOUT ever entering the active-run map (no slot, no VT). The VT runs only the RUNNING (action) phase. This passed all FSM tests including the explicit `conditionsFalse…noModeSlotConsumed` case AND is strictly more correct than the instruction (no transient slot even concurrently). The skill makes MODULE_CONTEXT the ground truth ("the behavioral promises method signatures don't express"); Phase-2/3 say "Locked behavioral contracts are authoritative." The instruction itself cited the contract and had a "Coder Pushback Welcome" section naming this exact seam — so this is deviate-and-flag, not silent. Flagged as `[REVIEW]` in handoff + a cross-agent note recommending ACCEPT.
**Impact:** Before implementing a step-ordered admission/lifecycle sequence from an instruction, cross-check it against the target module's MODULE_CONTEXT cross-module contracts AND the Locked design doc. If they conflict, implement the contract, prove it passes the instruction's own tests, and raise a `[REVIEW]`/TECHNICAL PUSHBACK — do not encode a known contract violation because "the instruction said so." A signature/param the instruction defines (here the `RunConditionGate(automation, context)` seam) usually does NOT constrain WHERE it is called, so honoring the contract is free of downstream rework.

## 2026-06-21 | Category: testing | Source: M7.2a-1 (run lifecycle) — deterministically observing a VT-per-Run FSM's async terminal events without banned time access, including RESTART-cancelled victims
**Discovery:** An FSM that runs each unit of work on its own virtual thread (LTD-01) and publishes its terminal event from that VT cannot be asserted right after the synchronous `initiate` call returns — the terminal publish is async. The clean, non-flaky await is `Thread.join(long)` on the live VTs (NOT a `Clock`/`Instant.now` deadline — those are banned by NO_DIRECT_TIME_ACCESS in non-whitelisted modules; `Thread.join`/`Thread.sleep` are NOT banned). Expose a package-private `awaitQuiescence(long)` test seam that joins them.
**Detail:** The trap: a RESTART-cancelled victim is removed from the active-run map synchronously (to free its slot) but its ABORTED terminal publish still happens later on its own VT. If `awaitQuiescence` only joined the *active* map, it would return before the victim published → flaky assertion. Fix: keep a separate `Set<Thread> liveRunThreads` (a legitimate production shutdown structure too — `close()` interrupts it) that a Run is added to at admission and removes itself from in a `finally` ONLY AFTER its terminal publish; `awaitQuiescence` joins that set. So `join()` returning guarantees the completed/aborted/cancelled/disabled events are observable. Pair it with a fake executor whose BLOCK mode `latch.await()`s and, on `InterruptedException`, restores the interrupt flag (`Thread.currentThread().interrupt()`) so the FSM's post-execute `isInterrupted()` check finalizes ABORTED — exactly the production cancellation contract. With a `Clock.fixed`, durations are 0 (fine; `durationMs >= 0` holds) and same-millisecond `UlidFactory.generate(clock)` still yields distinct monotonic RunIds.
**Impact:** For VT-per-task FSM tests: (1) await via `Thread.join`, never a clock deadline (NO_DIRECT_TIME_ACCESS); (2) track ALL live task threads (incl. those removed from the "active" accounting map but still finalizing) and remove-after-terminal-publish so join is a true barrier; (3) model cancellation in the fake by restoring the interrupt flag, not by throwing; (4) the synchronous admission-side outcomes (empty + run_skipped/cancelled, slot accounting) are assertable immediately — only the on-VT terminal needs the await.

## 2026-06-21 | Category: architecture | Source: M7.2a-2 (execution/dispatch) — a seam that publishes a causally-correct event needs the triggering envelope, not just the RunContext; widen the seam, don't fatten the fixed record
**Discovery:** AMD-92 §2.4 requires every run/diagnostic event to publish on the *triggering event's* `CausalContext` (`chain(correlationId, causationId)`) with `actorRef = AutomationId` and inherited/null `eventTime`. The M7.2a-2 seams that emit those events — the `RunConditionGate` (row 4) and `ActionExecutor` (rows 5/6) — were specified taking only `(…, RunContext)`. But `RunContext` carries `triggeringEventId` (→ causationId) and `automationId` (→ actorRef) and NOT the chain's `correlationId` or `eventTime`. So a seam given only the RunContext **cannot** construct the correct CausalContext. `RunContext`'s shape is frozen (a STOP-gate), so you can't add fields. Two clean exits: widen the seam to also receive the triggering `EventEnvelope`, or have the FSM (which has the envelope) do the publish. The instruction's own test ("the GATE emits row 4 with the correct CausalContext") forced the gate-publishes design → widen the gate.
**Detail:** Result: `RunConditionGate.conditionsHold` → `(AutomationDefinition, RunContext, EventEnvelope, StateSnapshot)` and `ActionExecutor.execute` → `ActionExecutionResult execute(List, RunContext, EventEnvelope)`. The `EventEnvelope`/`StateSnapshot` params introduce NO JPMS edge (both already `requires transitive` on the automation API; `ConditionEvaluator`/`RunManager` already expose them), so module-info stays UNCHANGED and the public seams don't leak a hidden type. Bonus: passing the `StateSnapshot` lets the FSM capture ONE trigger-time snapshot (AMD-03), record its `viewPosition` into `RunContext`, AND hand the same snapshot to the gate — a single source of truth instead of two `getSnapshot()` calls that could straddle a projection tick. The "the call site is fixed" STOP-gate note constrained the ORDERING (condition-before-mode), not the signature — widening params/returns is fair game when the contract (correct causal stamping / a real tally) demands it; flag it `[REVIEW]` (the instruction's "Coder Pushback Welcome" named these seams).
**Impact:** When a seam must emit an event that has to land in a causal chain, audit what the seam can actually SEE before accepting its signature: identity (actorRef), causation (the causing event id), correlation (chain root), and eventTime each have a distinct source, and a flattened context record (RunContext) usually has only the first two. Either route the publish through the component that holds the causing `EventEnvelope`, or pass the envelope into the seam — never reconstruct a half-correct CausalContext from a record that lacks the root. Cross-check the seam's params against the publish helper's needs at design time, not after the gate fails.

## 2026-06-21 | Category: concurrency | Source: M7.2a-2 (execution/dispatch) — QUEUED single-flight drain on a VT-per-Run FSM: enqueue-then-drain-on-finalize, and a join-one-at-a-time quiescence seam
**Discovery:** M7.2a-1 shipped QUEUED admission-equivalent to PARALLEL (drop `queue_full` at `maxConcurrent`) because a stub executor couldn't observe sequencing. The real contract is a *sequential single-flight drain*: at `maxConcurrent` a QUEUED Run **enqueues** (per-automation FIFO) and drains one-at-a-time as active Runs free slots. The clean implementation on a `ReentrantLock`-guarded, VT-per-Run FSM: split QUEUED from PARALLEL in `decide()` (QUEUED → an `enqueue` decision, not `queue_full`); on enqueue, build the `ActiveRun` (VT unstarted, status EVALUATING-pending, dedup already claimed, row 4 already published at trigger time) and park it in `Map<AutomationId, Deque<ActiveRun>>`; in `finalizeRun` (inside the lock, after freeing the completing Run's slot) call `drainNext(automationId)` which, if a slot is now free, dequeues one and registers its VT — then publish its `automation_triggered` + `vt.start()` OUTSIDE the lock. Conditions are evaluated at trigger time (AMD-03) before enqueue, so a queued Run is already condition-passed; only triggered+execution defer to drain.
**Detail:** Test trap: the existing `awaitQuiescence` snapshotted the live-VT set ONCE and joined it — but a completing Run drains a NEW VT during its own finalize, so the snapshot misses the drained Runs and the await returns early. Fix: `awaitQuiescence` joins ONE live thread at a time in a loop (`for (vt = anyLive(); vt != null; vt = anyLive())`), re-reading the live set after each join, and breaks if a join times out (the VT is still live) to avoid spinning. Because `finalizeRun` registers + starts the drained VT BEFORE the completing VT's `finally` removes itself from the live set, the drained VT is observable the moment the join returns. The sequencing assertion: a recording executor appends `context.runId()` on entry; with `maxConcurrent=1` the order is deterministic FIFO `[R1,R2,R3]` and `activeRunCount==1` throughout — proving single-flight, not just "they all ran." Per-automation FIFO is the right granularity (C3 priority/automationId ordering applies to a same-event BATCH at the subscriber, not to one automation's time-spread queue).
**Impact:** For a per-key single-flight queue on a VT-per-task FSM: (1) make the queue admission a first-class `decide()` outcome distinct from drop; (2) drain inside the terminal-transition critical section but start the drained task's thread + publish outside the lock (publish-outside-lock discipline); (3) the quiescence/await seam must tolerate the live set GROWING mid-drain — join iteratively, not over a stale snapshot; (4) assert ORDER + concurrent-count, not just completion, or the test can't tell single-flight from parallel.

## 2026-06-21 | Category: java | Source: M7.2a-2 gate-fix Round 1 — a record component named X forbids ANY method named X() in that record (static factory collision); the compile gate caught what the LLM read-pass missed
**Discovery:** A Java `record` auto-generates a `public` accessor `X()` for each component `X`. Declaring any other method named `X()` in the same record — most insidiously a `static` factory — is a **compile error** ("invalid accessor method in record … must be public" on the factory; "must match the type of record component" when the return type differs). Worse, at call sites the unqualified `instance.X()` can bind to the wrong member: `record ValidationResult(boolean valid, …)` with a `static ValidationResult valid()` made `if (!validation.valid())` resolve `valid()` to the *factory* (returns ValidationResult) → "bad operand type ValidationResult for unary operator '!'". Two collisions slipped in this WU: a NEW `ValidationResult.valid()` factory vs the `valid` component, and a REGRESSION where the 2a-2 rewire added `ModeDecision.admit()/restartCancel()/enqueue()` factories to a record that already had `admit`/`restartCancel`/`enqueue` boolean components (2a-1 had built it via the canonical constructor — the factory "tidy-up" introduced the bug). **A 3-reviewer in-session adversarial LLM pass did NOT catch this; `:core:automation:compileJava` did (8 errors).**
**Detail:** Fixes (minimal): rename the no-arg factory to a non-component name (`valid()` → `accepted()`) and update the factory call sites — instance-accessor *reads* (`result.valid()`) stay and re-bind correctly once the factory is gone; or drop the colliding factories entirely and build via `new Record(...)` at the call sites (the ModeDecision fix — reverts the gratuitous refactor). Factories whose name does NOT match a component are fine: `invalid(String)`, `skip(String)`, `succeeded(int,int)`, and even an instance no-arg `failed()` PLUS a static `failed(int,int,String)` (legal overloading) are all legal precisely because no component is named `invalid`/`skip`/`succeeded`/`failed`.
**Impact:** New STOP-check before handoff: for EVERY new/edited record, list its component names and confirm NO method (static factory or otherwise) shares a name with a component. LLM read-passes are blind to this binding rule — it must be a mechanical scan (or just the compile gate). Prefer the canonical constructor or a distinct verb (`accepted`/`of`/`from`) over a factory that shadows a boolean/`is`-style component. This is why the deferred build gate exists: `compileJava` is the source of truth, the adversarial LLM pass is a complement, never a substitute.

## 2026-06-22 | Category: module-boundaries | Source: M7.2b (action-model freeze) — "module-info UNCHANGED" + a new sealed type that returns another module's non-transitive type ⇒ the type must be PACKAGE-PRIVATE (not public), if it has no external consumer
**Discovery:** The instruction's §1 code block showed `public sealed interface ComputedValue { AttributeValue resolve(...); }` AND demanded "success criterion 4: `module-info` unchanged." These are mutually exclusive under `-Xlint:exports`/`-Werror`: `AttributeValue` is from value-model, which automation declares as **plain** `requires com.homesynapse.value` (non-transitive). A `public` type in the EXPORTED package `com.homesynapse.automation` that names `AttributeValue` on its public API (a return type, a record component → its accessor, a param) trips `[exports]` → with `-Werror`, a hard compile failure — clearable ONLY by promoting the edge to `requires transitive` + Gradle `api(...)` (a module-info change). The escape that satisfies BOTH: make the whole `ComputedValue` family **package-private**. The `[exports]` rule only governs PUBLIC types in exported packages, so a package-private type may freely name a plain-`requires` module's types on its (public) methods/accessors. Legal because the feature had no external consumer in V1 — loading is OUT, the frozen executor never sees it, resolution is internal — and same-package tests retain full access.
**Detail:** Verified the whole family is package-private (no `public` on `ComputedValue`/`LiteralValue`/`AttributeRef`/`AggregateValue`/`AggregateOp`/`ComputedValueContext`/`ComputedValues`); the public-type count and `module-info` both stay unchanged; the value-model types appear only on package-private surfaces (matching the pre-existing same-package `AttributeValues` helper, which already imports value types under the plain `requires`). A 4-lens adversarial pass confirmed: an implicit record accessor returning `AttributeValue` is NOT subject to the exported-API check when the record itself is package-private. Note the inverse of the AB-3 lesson (where a public assembly seam returning `event.bus.Subscriber` CORRECTLY forced `requires transitive`): there the type WAS on the exported API and had an external consumer (the composition root); here it is not and does not, so package-private is right.
**Impact:** When an instruction's illustrative code shows a `public` type but also pins "module-info unchanged," check whether that type's API names a non-transitively-required (or package-private) module type. If yes and the type has NO external consumer in this WU, make it package-private — that satisfies both, keeps the edge non-transitive, and is reversible (promote to `public` + `requires transitive` + `api(...)` only when a real external consumer arrives, e.g. a later loader). Flag the `public`→package-private choice `[REVIEW]` since it deviates from the shown modifier, but it is the only build-valid reading. The illustrative `public` in a spec is a sketch, not a binding contract — the exports lockstep + the module-info pin are.

## 2026-06-22 | Category: anti-pattern | Source: M7.2b — a "total, no-I/O-capability" derived-value type must carry a RESOLVED finite set, not a query expression (a resolver collaborator IS an I/O capability)
**Discovery:** The instruction specified `AggregateValue(Selector selector, String attribute, AggregateOp op)` AND the invariant "every permit is total over a typed finite input set … No I/O capability on the type (C-SA-2)" AND `ComputedValueContext = {snapshot, resolutionTime}` (no resolver). A `Selector` is NOT a finite input set — it is a query that needs a `SelectorResolver` to resolve, and a resolver reads the entity registry. Threading a resolver into the resolution path (whether on the type or via the context) gives resolution an **I/O capability**, directly contradicting C-SA-2's "no I/O capability on the type" and the "finite typed input set" framing, and making determinism a function of `{snapshot, time, resolver}` instead of `{snapshot, time}`. The clean resolution: carry the already-resolved `Set<EntityId>` (selectors resolve to entity sets once at trigger time anyway — C4 `resolvedTargets`), so `AggregateValue.resolve` is a pure fold over the single snapshot with zero collaborators.
**Detail:** This keeps `ComputedValueContext` at exactly the specified `{snapshot, resolutionTime}` (no third field), makes INV-TO-02 ("same snapshot+time ⇒ same value") airtight, and makes "no I/O capability on the type" literally true (the type holds only data). The deferred YAML loader/component model resolves `selector → Set<EntityId>` at load/run-init (the same place C4 resolves targets), so the `Set` carrier paints no future corner — same behavioral contract ("a bounded fold over a resolved finite entity set's numeric snapshot attributes"), cleaner type. Flagged `[REVIEW]`/TECHNICAL PUSHBACK (deviates from the literal `Selector` signature) — accepted because it strengthens the instruction's OWN invariants. Companion choice: the absent/empty rule returns a typed-absent sentinel (`DegradedAttributeValue`), NOT fail-closed — an absent attribute is a *valid typed input*, and C-SA-2 forbids throwing on valid inputs; fail-closed is reserved for the snapshot READ failing (a genuinely degraded substrate), which is a different condition.
**Impact:** When a spec gives a type signature AND a purity/totality invariant that the signature would violate, the invariant wins — prefer the carrier that makes the invariant structurally true (a resolved finite set over a live query) and flag the signature deviation. "No I/O capability on the type" is a test you can apply mechanically: does resolving the value require ANY collaborator beyond the immutable context data? If yes, the operand is wrong. And distinguish "input absent" (valid → typed-absent sentinel, never throw) from "substrate failed" (degraded → fail-closed/FAILED) — they are different error classes with different correct responses.

## 2026-06-22 | Category: module-boundaries | Source: M7.3 (Pending Command Ledger) — when the instruction's data source doesn't exist in the FROZEN tree, derive the contract from what IS there; verify the actual record/enum shapes before designing, not the prose
**Discovery:** The M7.3 instruction (and Doc 07 §3.11.2) said the ledger records a `PendingCommand` from a `command_issued` event "with the expected_outcome from the event payload (computed by `ExpectationFactory`)" and bypasses tracking for "OPTIMISTIC" commands per AMD-90's `ConfirmationPolicy`. **All three premises were false against the frozen baseline `1b0b6c9`** and I only found it by reading the actual source: (a) `CommandIssuedEvent` is a 5-component FROZEN record (`Ulid targetEntityRef, String commandType, String parameters, int confirmationTimeoutMs, CommandIdempotency idempotencyClass`) — it carries NO attribute, NO expected outcome, NO confirmation policy; (b) there is **no `ExpectationFactory` implementation** in the tree (interface only); (c) AMD-90's `ConfirmationPolicy{OPTIMISTIC,REQUIRED,BEST_EFFORT}` enum does NOT exist and `CommandAction` is still the 4-field FROZEN record (no `confirmation` component) — the only in-tree confirmation-policy signal is the device-model `ConfirmationPolicy` record on the capability (`CapabilityInstance.confirmation()`), whose `ConfirmationMode.DISABLED` ≡ "confirmation off / optimistic". So the buildable design derives the `Expectation` + target attribute from the capability's `CommandDefinition.expectedOutcomes()` (each `ExpectedOutcome` = attribute + `Expectation` + timeout), resolved via `EntityRegistry.findEntity → Entity.capabilities → the CapabilityInstance whose commands() has the type`, and gates OPTIMISTIC on `mode()==DISABLED`. A FOURTH drift: the §3.11.1 prose ("when a `command_issued` is appended, the dispatch service routes it") describes an event-driven dispatcher, but `StandardActionExecutor` actually calls `CommandDispatchService.dispatch(...)` **in-process** and emits `command_dispatched`/`command_result`, never `command_issued` — so the ledger's producers aren't wired yet and the FSM is test-exercised against constructed envelopes.
**Detail:** The verification that surfaced this: before designing, I read the FROZEN record/enum shapes (`CommandIssuedEvent`, `CommandAction`, `ConfirmationPolicy`, `ConfirmationMode`, `CommandDefinition`, `ExpectedOutcome`, `CapabilityInstance`) and grepped for an `ExpectationFactory` impl (none). A fifth bridge fell out of the same discipline: `StateReportedEvent.value` is a `String` while `Expectation.evaluate` takes a typed `AttributeValue`, so the ledger coerces String→`AttributeValue` directed by the expectation's expected `AttributeType` (total; uncoercible → `DegradedAttributeValue`, never throws — the M7.2b typed-absent precedent). Every deviation was recorded as a `[REVIEW]` with the file+line evidence (the instruction's "Coder Pushback Welcome" + "cite the file + line" explicitly invited it); none required a new event type (the STOP-on-mint gate held — counts stayed 71/41/53).
**Impact:** For a Phase-3 impl of an "already-specified seam," the design contract is the FROZEN code, not the design-doc/amendment prose — and the two routinely diverge as the substrate freezes. Read the actual record arities, enum constants, and whether the named factory/producer even has an implementation BEFORE committing to a data-flow; a spec that says "from the event payload / via FactoryX / per PolicyEnumY" is a hypothesis to verify, not a given. When the named source is absent, find the nearest in-tree source that carries the same information (here: the capability's `ExpectedOutcome` and `ConfirmationMode`) and flag the substitution with citations. The mechanical tell: if implementing the prose literally would require a type/field/impl you can't find with grep, stop and re-derive from source.

## 2026-06-22 | Category: architecture | Source: M7.3 (Pending Command Ledger) — a bus subscriber that PUBLISHES cannot be a projection-advancer handler (AMD-41); and wrap the frozen record in an internal carrier for metadata it can't hold
**Discovery:** AMD-92's DQ-3 said "the ledger's projection handlers register on the existing `DispatchingProjectionAdvancer` (separate `Map.of` entries, no new advancer)." But the ledger PUBLISHES (`state_confirmed`/`command_confirmation_timed_out`), and `EnvelopeHandler`s run inside the advancer's read-transaction where **AMD-41 forbids `EventPublisher.publish()` / opening writes**. A publishing component therefore CANNOT be an advancer handler — and it doesn't need to be: it consumes its inputs via its own `event.bus` subscription (the `AutomationEngineSubscriber` precedent — also a bus subscriber, also touches the advancer not at all), so registering an advancer handler would only DOUBLE-DELIVER. Correct call: NO advancer change; the advancer serves only the `StateProjection`'s derivation loop. The instruction explicitly permitted "if the ledger does not need a projection handler … do not add one — justify either way" — so the answer was a justified no-op, citing AMD-41 + the subscriber architecture + the double-delivery hazard, NOT a mechanical DQ-3 registration.
**Detail:** Second pattern: the `PendingCommandLedger` interface's `PendingCommand` record (8 frozen components) cannot carry the run `correlationId` (needed to thread `state_confirmed` onto the causal chain — AMD-91) or the ack `command_result` event id (the nullable `resultEventId` on a later `command_confirmation_timed_out`). Don't fight the frozen record — wrap it: an internal `private record Tracked(PendingCommand command, Ulid correlationId, EventId resultEventId)` is the dual-index value; `getCommand`/`getPendingForEntity` return `tracked.command()`, while the FSM threads the extra metadata. The dual index itself (the instruction told me to "carry both" because Doc 07 §3.11.2 keys `EntityRef→List` while the old MODULE_CONTEXT note said `EventId→PendingCommand`) is primary `EventId→Tracked` + secondary `EntityId→Set<EventId>` — both lookups, one source of truth, updated together under one `ReentrantLock` with publications collected inside the lock and flushed after release (LTD-11 publish-outside-lock). REPLAY rebuild mirrors `AutomationEngineSubscriber.replayTimers`: accumulate lightweight in-flight records during REPLAY (no publish — Doc 01 §3.7), classify at `onCaughtUp`; the replay carrier (`ReplayInFlight`) is deliberately NOT a `PendingCommand` (no `Expectation` is in the log) — re-offer/expire/adapter dispositions are resolved by idempotency class, never by re-matching state.
**Impact:** (1) Before registering a component as a read-tx projection handler, check whether it publishes or writes — if so, it's a bus subscriber, not a handler (AMD-41), and a "register a handler" instruction is satisfied by a JUSTIFIED no-op (cite the read-tx constraint + the double-delivery hazard). (2) When a frozen interface record can't carry the metadata your FSM needs (correlation/causation ids, ack ids), wrap it in a private carrier the index stores and the accessors unwrap — don't lobby to widen the frozen record. (3) For a "rebuilt-from-the-log" projection, model the replay accumulator as a SEPARATE lightweight record (only the fields reconstructable from the log), not the live entry — the live entry's extra fields (here the `Expectation`) often aren't in the log and aren't needed for boundary classification.

## 2026-06-22 | Category: testing | Source: M7.3 gate-fix — a runtime bus subscriber registered in the composition root with NO matching stop() teardown holds its SQLite connection open and fails Windows @TempDir cleanup (15 lifecycle tests RED at teardown); the compile self-step + LLM pass can't see it, only the full gate with a real temp DB can
**Discovery:** M7.3 registered the `pending_command_ledger` runtime subscriber in `HomeSynapseCore.bootstrap()` via `eventBus.subscribeRuntime(...)` mirroring `automation_engine` — but mirrored only the SETUP, not the TEARDOWN. `subscribeRuntime` creates a per-subscriber virtual thread + a SQLite read connection (the bus opens one per runtime subscriber). With no corresponding close on `stop()`/`shutdown(...)`, that connection stays open on `homesynapse-events.db`/`-wal`/`-shm`. On Windows, an open file handle blocks deletion, so JUnit's `@TempDir` cleanup throws `IOException` during teardown — failing EVERY composition-root test that boots the runtime (15 in `:lifecycle:lifecycle`: `HomeSynapseCoreTest` + `LifecycleWiringTest`), all with the same `JUnitException … Caused by: IOException at ForEachOps.java` signature (the recursive temp-dir delete). The ledger's OWN `:core:automation` unit tests passed (they construct the ledger directly, no bus, no DB, no temp dir). **The in-session compile self-step compiled it fine; the 3-lens adversarial LLM pass returned zero findings — neither boots a real runtime against a real temp DB, so neither could see a resource-lifecycle leak.** Only `./gradlew check` (the real lifecycle tests) caught it.
**Detail:** Two correct exits: (A) **defer the live wiring** until the subscriber's producers exist (the M7.1 precedent — `automation_engine`/dispatch subscriber wiring was deferred to app-bootstrap; a subscriber with zero live inputs is premature anyway), OR (B) wire it WITH a matching teardown that closes the read connection on `stop()`, mirroring how the bus closes `automation_engine`'s runtime subscriber (mirror the FULL lifecycle, not just the register call). M7.3 took (A): surgical `git checkout HEAD -- HomeSynapseCore.java` reverted the +19-line block + import byte-exact to the M7.2b-GREEN file, with ZERO change to the ledger deliverable (impl + assembly + 2 test classes stay). The tell at design time: a `subscribeRuntime` (or any `open`/`connect`/`new Thread`/`register`) in `bootstrap()` MUST have a paired close in the reverse-order `shutdown(...)` — grep the teardown path for the symmetric call before adding the setup one.
**Impact:** (1) When you register a runtime resource (bus subscriber, DB connection, VT, file handle) in a composition root, add its teardown in the SAME diff — "mirror the existing subscriber" means mirror BOTH `subscribeRuntime` AND its `stop()` close, not just the register. (2) On Windows, any leaked file handle surfaces as a `@TempDir`-cleanup `IOException` at test teardown, failing tests that look unrelated to your change (they fail in teardown, not the body) — a wall of same-signature teardown failures across one module's runtime-booting tests is the fingerprint of a leaked handle, not N logic bugs. (3) Neither a compile nor an LLM read/adversarial pass can catch a resource leak — only running the real integration tests against a real (temp) DB can; this is exactly why the full `./gradlew check` is the gate of record and an in-session "verification: clean" is a complement, never a substitute (and why premature subscriber wiring is deferred until its surrounding pipeline — producers + teardown — exists).

## 2026-06-27 | Category: build | Source: CI activation — Windows-committed scripts (gradlew + *.sh) tracked 100644 → exit 126 on the Linux runner; and a "gate of record" that was never actually green
**Discovery:** Activating the `install-smoke` + `frontend` workflows surfaced that **`gradlew` had been tracked `100644` (non-executable) since the init commit** — so `ci.yml`'s `./gradlew check` returned **exit 126 (permission denied)** on every CI run since the repo began. `ci.yml` had therefore *never passed on CI*, despite the spine recording "CI-as-gate-of-record CONFIRMED live" on 2026-06-21. The real gate was always Nick's **local** `./gradlew check` (Windows ignores the +x bit — bash runs the script regardless), masking the defect. The same Windows-commit class hit all 8 `distribution/**/*.sh` and the `scripts/**/*.sh` (also `100644`) — `install-smoke` invokes them directly (`run: distribution/image/build-image.sh`), so they 126'd too. NOT a CRLF issue (the files were clean POSIX); purely the missing exec bit (Windows/MINGW64 `git add` records `100644` for shell scripts).
**Detail:** Fix is `git update-index --chmod=+x <files>` (operates on the git index, not the filesystem — works from Windows) then commit/push; the mode flips `100644 → 100755` and the scripts check out executable on the runner *and* install executable from the `.deb`. Diagnose via `git ls-files --stage '*.sh' gradlew | awk '$1=="100644"'`. After the fix `ci.yml` ran the full check **GREEN, 149 actionable tasks** (commit `b2529cc`) — matching the local gate and finally making CI a real gate (ArchUnit module-graph/dependency guards now enforced on every push). `gradlew.bat` correctly stays `100644` (Windows-only, never `./`-invoked on Linux).
**Impact:** (1) Any shell script or `gradlew` committed from Windows needs an explicit `git update-index --chmod=+x` — and a CI guard that fails on a `100644` `*.sh`/`gradlew` makes it structurally impossible to recur (tracked in `ci-as-gate-of-record.md` CI-hardening backlog). (2) **A *claimed* gate must be verified *actually-green* before it is trusted/recorded as live** — "adopted" ≠ "passing." This is the M2.5 arch-debt-retrospective class (a gate assumed-working that wasn't), one level up. Generalized into `context/process/truth-hierarchy-and-pointer-not-copy-discipline.md` §2.4.

## 2026-06-27 | Category: module-boundaries | Source: M7.4a (command_issued producer) — an event payload field that is a serialized String, produced in a module with NO JSON library and a "no new module edge" pin ⇒ INJECT the serializer from the composition root, don't hand-roll it
**Discovery:** `CommandIssuedEvent` carries `String parameters` (a JSON object string, non-blank required), and the producer is `StandardActionExecutor` in `com.homesynapse.automation` — which has **no Jackson** (a repo-wide grep puts `writeValueAsString`/`ObjectMapper` only in persistence, config, and the api/* modules) and the instruction pins "no new module edge / module-info UNCHANGED." So the producer cannot serialize `CommandAction.parameters()` (a `Map<String,Object>`) with the canonical mapper directly, and a hand-rolled JSON writer would have to reproduce the value-model `AttributeValue` serializers (the M7.2b computed-param path can leave **`AttributeValue` instances** in the resolved parameter map) — guaranteed to diverge from persistence's serialization, which is exactly what the future M9 integration adapter will deserialize with. Resolution: inject a `Function<Map<String,Object>,String> parameterSerializer` into the executor; the composition root (which owns the persistence `ObjectMapper`) supplies it. Jackson stays out of automation, the edge stays non-transitive, and parameters round-trip faithfully with the one serialization of record.
**Detail:** The codebase already accepts functional-collaborator injection at the composition root (`DerivedPublishGate publishGate = rateLimit::acquire`, `Consumer<HealthSignal>`), so a raw `Function` seam is idiomatic (no new public type — `java.util.function` is `java.base`). The producer defends the frozen-record contract itself (`parameters` non-blank): `serializeParameters` falls back to `"{}"` if the injected function returns null/blank. A companion finding on the CONSUMER side: the co-located dispatch subscriber's Tier-1 `CommandValidator` is value-agnostic (checks command/capability existence + feature bits, never reads param values), and `command_dispatched`/`command_result` carry no params — so `onEvent` passes `Map.of()` to `validate()` and never deserializes (no JSON parser needed in automation); the full parameters ride the durable `command_issued` for the M9 adapter. Both choices flagged `[REVIEW]`.
**Impact:** When a payload field is a serialized blob but the producing module lacks the serializer library (and a module edge is forbidden), inject the codec from the composition root rather than (a) adding the dependency, or (b) hand-rolling a serializer that will silently diverge from the canonical one the eventual consumer uses. The tell: a `String`-typed JSON/serialized field on an event whose producing module's `module-info` has no `requires` for a serialization library. Check whether the resolved value type can be a foreign sealed type (here `AttributeValue`) — if so, hand-rolling is doubly wrong (it duplicates and will drift from that type's registered serializers). And distinguish producer-needs-serialize from consumer-needs-deserialize: a value-agnostic Tier-1 consumer can skip deserialization entirely as long as the blob stays durable on the event for the real consumer downstream.

## 2026-06-27 | Category: architecture | Source: M7.4a (co-located dispatch subscriber) — give an assembly-exposed runtime subscriber a TYPED teardown handle by widening its service interface to AutoCloseable (the paired-teardown pattern the M7.3 @TempDir leak demanded), and pin replay-safety at the unit level via the LIVE-mode guard
**Discovery:** M7.3's gate-fix established that a runtime bus subscriber registered in the composition root MUST have a paired `stop()`/teardown (an un-closed subscriber leaks its SQLite read connection → Windows `@TempDir` cleanup fails). M7.4a had to ESTABLISH that pattern for the new `command_dispatch_service` (so M7.4b can mirror it for the ledger). The subscriber is built via a public assembly (`CommandDispatchAssembly.commandDispatchSubscriber(...) → Components(CommandDispatchService, Subscriber)`) that returns only **interface views** — so the composition root has no concrete handle to call teardown on. Clean resolution: widen the service interface itself — `CommandDispatchService extends AutoCloseable` with a **narrowed `void close()`** (an override may drop the `throws Exception`, so callers need no try/catch — matching the existing `triggerEvaluator.close()` call style). The composition root holds the `CommandDispatchService` view and calls `close()` in BOTH shutdown branches (`doTeardown` + `abandon`), no cast, no third Components field. `StandardCommandDispatchService.close()` is a no-op-today (the dispatch service is stateless — no DB connection — so it can't reproduce the M7.3 leak), but the pattern is in place.
**Detail:** Two more M7.4a calls worth carrying: (1) **D2 (pure-function-replay) at the unit level** — the subscriber guards dispatch on `mode == LIVE` (`replayMode` tracked via `setMode`, mirroring the ledger), and a unit test delivers a `command_issued` in REPLAY and asserts the publisher recorded ZERO events. The bus FSM already withholds runtime delivery until LIVE, but the explicit guard + test pins it so a refactor can't silently regress (the full seeded-log replay CI gate is M7.4b). (2) **`coalesceExempt=true`** for the dispatch subscriber — a coalesced `command_issued` would be a command that never dispatched (correctness-critical, like the ledger); the assembly returns the `SubscriptionFilter`, but `coalesceExempt` is a `SubscriberInfo` flag set at registration, so the composition root owns that decision. (3) Causality split: the production bus path (`onEvent`) threads the full Doc 07 §3.11.2 chain (corr = the Run's, caus = the `command_issued` id — which is how the ledger's `findPending` matches), while the retained in-process `dispatch(EventId,…)` primitive keeps its pre-existing thin-router `chain(commandEventId, commandEventId)` so its existing test stays green; both call one shared `route(...)` that takes the `CausalContext`.
**Impact:** When a co-located subscriber is exposed only through interface views from an assembly and needs a composition-root teardown, widen its service interface to `AutoCloseable` (narrowed `close()`) rather than casting or adding a teardown field — it gives a typed, no-throw handle that mirrors the module's existing `.close()` teardown idiom, and it's the natural place for the paired teardown the M3/M7.3 `@TempDir` lessons require. Add the teardown in the SAME diff as the `subscribeRuntime` (grep the reverse-order shutdown path for the symmetric call). For any replay-relevant subscriber, pin the LIVE-only guard with a REPLAY unit test now even when the bus FSM makes it structural — the structural guarantee is one refactor away from being lost, and the unit proof is cheap.

## 2026-06-27 | Category: module-boundaries | Source: M7.4b (trigger→run handoff) — when a frozen interface method returns LESS than a downstream consumer needs, add a richer PACKAGE-PRIVATE method on the concrete impl (the consumer holds the concrete type) rather than widening the frozen interface
**Discovery:** The run-initiation handoff needs, per matched automation, the matched **trigger indices** (for `RunContext.matchedTriggers` / `automation_triggered`), but the frozen Phase-2 `TriggerEvaluator.evaluate(EventEnvelope)` returns only `List<AutomationId>`. The instruction floated widening `evaluate`'s return type to a richer `TriggerMatch`. Re-deriving the indices OUTSIDE the evaluator was rejected outright: `triggerMatchesEvent`/`forDurationOf` are private, so re-derivation would DUPLICATE the matching logic AND get it wrong (it can't distinguish an immediate match from a `for_duration` timer-start without re-implementing the timer logic too) — a silent `matchedTriggers`/causal-chain corruption no compile catches. But widening the interface return type was ALSO avoidable: the only consumer (`AutomationEngineSubscriber`) holds the **concrete `StandardTriggerEvaluator`**, not the `TriggerEvaluator` interface. So the fix is a package-private `evaluateMatches(EventEnvelope) → List<TriggerMatch>` on the concrete class (the single source of truth — it owns the matching loop and already computes the indices), with `evaluate()` refactored to its thin projection (`matches.map(TriggerMatch::automationId)`). The frozen interface and every `evaluate(...)` call site / shape test are UNTOUCHED.
**Detail:** Behavior parity between `evaluate` and `evaluateMatches` must be EXACT (the subscriber calls `evaluateMatches` ONCE — calling both would double-run the side-effecting timer logic). The refactor preserves it by making `evaluate` literally call `evaluateMatches` and project: the old `LinkedHashSet<AutomationId>` (insertion-ordered, deduped) becomes a `LinkedHashMap<AutomationId, List<Integer>>` (same key order, accumulating indices), and the `for_duration` arm still starts a timer + is excluded from the result (a sustained trigger fires later via timer expiry, NOT immediate run-init). Removing the now-dead `Set`/`LinkedHashSet` imports is mandatory (`-Werror` + Spotless `removeUnusedImports`) — grep the file for the old type after the refactor. The new `TriggerMatch` record (and the initiator's private `Matched` record) pass the M7.2a-2 component/static-factory name-collision STOP-check (`automationId()`/`matchedTriggerIndices()`/`definition()`/`matchedIndices()` collide with nothing). Net: `+0 public types` (both new types package-private), frozen interface intact, zero call-site ripple — strictly better than the interface-widening alternative the instruction offered.
**Impact:** Before widening a frozen/Phase-2 interface to feed a new in-module consumer, check what STATIC type that consumer holds — if it holds the concrete impl (common for tightly-coupled same-package collaborators wired via an assembly), add the richer method package-private on the impl and make the interface method its projection. You keep the single-source-of-truth (no re-derivation, no divergence risk) AND the frozen contract + its call-site/shape-test blast radius. The general rule: re-deriving a value the producer already computed internally is a silent-corruption smell; expose the producer's own computation through the narrowest seam the consumer can reach (package-private beats public-interface-widening when the consumer is in-package).

## 2026-06-27 | Category: module-boundaries | Source: M7.4c (ledger goes LIVE) — expose a package-private operational tick to the composition root via a Components Runnable (NOT the query interface), and recognize that a subscriber whose ONLY resource is the bus read connection has an ASYMMETRIC teardown (no close())
**Discovery:** The `pending_command_ledger`'s `pollExpirations()` deadline sweep must be driven by the composition root, but it is an OPERATIONAL tick, not a query — putting it on the `PendingCommandLedger` interface (query-only: `trackCommand`/`getCommand`/`getPendingForEntity`/`pendingCount`) would pollute a read contract the future REST/causal-read consumers compile against. `pollExpirations()` is already package-private on `StandardPendingCommandLedger`. Clean exposure: widen the assembly's `Components` record with a 3rd component `Runnable expirationTick` constructed as `ledger::pollExpirations` (the assembly is same-package, so the package-private method ref binds), and have the composition root schedule `Components.expirationTick()`. `+0 public types` (a widened existing record), interface untouched, `Runnable` is `java.base` (no module edge). Same shape as the `Components(service, subscriber)` precedent — just a third view of the one instance.
**Detail:** Two teardown nuances that matter for the M7.3-reverted `@TempDir` leak. (1) **A subscriber whose ONLY resource is the bus's per-subscriber SQLite read connection needs NO separate `close()`** — unlike `command_dispatch_service`/`RunManager` (which own their own resources and are `close()`d in BOTH shutdown branches), the ledger's connection is released by `eventBus.unsubscribe(id)` in the graceful path (`InProcessEventBus.unsubscribe` → `runtime.close()`) AND by `eventBus.abandon()` in the ungraceful path (`abandon()` closes EVERY active runtime). So the graceful `doTeardown` gets an explicit `eventBus.unsubscribe(PendingCommandLedgerAssembly.SUBSCRIBER_ID)` (reverse registration order: ledger→dispatch→automation→projection), but `abandon()` needs NOTHING added — a deliberate, comment-documented asymmetry. This contradicts a literal reading of "unsubscribe in BOTH branches" (the instruction's words): the abandon branch never individually unsubscribes anything (not even the dispatch service — it relies on `eventBus.abandon()`), so true symmetry-with-the-dispatch-service means adding nothing to abandon for the ledger. VERIFY the bus's `unsubscribe`/`abandon` both close the read connection before relying on this. (2) **`SharedScheduler` is constructed (Step 3.2) BEFORE the ledger exists (Step 3.4c, after projection-LIVE)** and starts its built-in tasks at construction — so the 3rd periodic task can't be a constructor arg without reordering the scheduler after the ledger. The clean fix is a post-construction `void schedulePeriodic(String name, Runnable task, long periodMillis)` (wrapped in the same `safelyInvoke` guard; cancelled by the existing `shutdown()`/`shutdownNow()`; throws `IllegalStateException` if called post-shutdown). Cadence ~1000 ms so a 30 000 ms deadline fires within ~1 s of expiry.
**Impact:** (1) To hand the composition root an operational tick on an object whose public contract is query-only, expose `obj::tick` as a `Runnable` component on the existing assembly `Components` record — never add the tick to the query interface. (2) Before mirroring a "paired teardown in BOTH branches" instruction, check what the subscriber actually OWNS: if its only resource is the bus read connection, the graceful `unsubscribe` + the ungraceful `eventBus.abandon()` cover it and no `close()` exists — adding an explicit abandon-path unsubscribe would diverge from how sibling subscribers are torn down. Document the asymmetry in a code comment so a future reader doesn't "fix" the missing line. (3) When a periodic task's collaborator is built after the shared scheduler, add a post-construction `schedulePeriodic` rather than reordering construction — reordering risks breaking the rate-limiter/health-check cadence that must start at boot. (4) E2E confirmation gate: a real CONFIRM (not an OPTIMISTIC bypass) requires the seeded capability to declare an `ExpectedOutcome` AND a non-DISABLED `ConfirmationMode` — `StandardCapabilities.onOff()` already does (`turn_on` → `ExpectedOutcome("on", ExactMatch(true))`, `EXACT_MATCH`), so the gate seeds `onOff` and injects a synthetic `state_reported(on=true)`; the FIXED clock never advances past the deadline so the `pollExpirations` tick can't time the command out before the report confirms it, and per-subscriber FIFO guarantees the ledger tracks `command_issued` before evaluating the report (an `awaitLedgerTracked()` poll on `pendingCount()>=1` makes the boot REPLAY→LIVE timing deterministic).

## 2026-06-27 | Category: testing | Source: M7.4d (D2 pure-function-replay CI gate) — to test "subscribers don't act on REPLAY" at the composition-root level, seed the raw store through a BUS-LESS PersistenceFactory (so checkpoints stay empty → replay-from-zero); a two-boot would checkpoint PAST the seed and be vacuous
**Discovery:** A composition-root replay-safety test must make fresh subscribers actually REPLAY a seeded command-lifecycle log. The intuitive "boot a core, run the pipeline, stop, re-boot over the same db" is VACUOUS: the bus's LIVE loop writes a per-event checkpoint after each successful non-atomic delivery (`InProcessEventBus.liveLoop`, gated only on `!atomicCheckpoint`), so the first boot's `command_dispatch_service`/`automation_engine`/`pending_command_ledger` checkpoint PAST the `command_issued`; the second boot reads those checkpoints (`SqliteCheckpointStore.readCheckpoint`) and resumes AFTER the seed — it never replays it, so "zero side-effects on replay" is trivially true and pins nothing. The fix: seed through a **standalone `PersistenceFactory.start(dbPath, config.persistence(), clock, homeId, eventClasses, null)`** — which builds NO event bus and registers NO subscribers — `eventPublisher().publishRoot(...)` the log, `close()`. No subscriber ever runs, so `subscriber_checkpoints` stays empty; when `HomeSynapseCore` then boots over the same db, every fresh subscriber reads checkpoint `0` and replays the WHOLE log in REPLAY. This is exactly the `CrashRecoveryHttpIT.busAheadOfViewWindowClosedByReplayFromZero` invariant. `PersistenceFactory.eventPublisher()` and `eventStore()` return the SAME `SqliteEventStore`, and `close()` does `PRAGMA wal_checkpoint(TRUNCATE)` + releases all connections, so the boot opens the same file cleanly and `@TempDir` cleanup succeeds on Windows (use try-with-resources; use `close()` NOT `abandon()` — abandon leaves `-wal`/`-shm`). Replicate `HomeSynapseCore`'s exact `eventClasses` manifest (`Stream.of(EventTypes.CORE_PRODUCTION_EVENT_CLASSES, IntegrationEvents.LIFECYCLE_EVENT_CLASSES, IntegrationEvents.CAPABILITY_EVENT_CLASSES).flatMap(List::stream).toList()`) so seeded payloads round-trip.
**Detail:** Three things make such a gate NON-VACUOUS and DETERMINISTIC, each forced by exact source behavior. (1) **The deterministic replay-completion boundary.** `HomeSynapseCore.start()` gates only on the PROJECTION reaching LIVE (`awaitProjectionLive`), then registers the three runtime subscribers AFTER — so on return they may still be replaying on their own VTs. Poll `core.eventBus().subscribers()` (→ `List<SubscriberSnapshot>`, `.subscriberId()`/`.mode()`) until `automation_engine`/`command_dispatch_service`/`pending_command_ledger` are ALL `LIVE` before snapshotting the store. This is a RACE-FREE upper bound: the publisher persists-before-notify (`NotifyingEventPublisher`) and each subscriber delivers sequentially on a single VT, so any replay-phase publish is durably in the store before that subscriber CASes to LIVE. Treat `SUSPENDED` as a fail-fast (a replayed `onEvent` that threw trips the breaker — itself a regression — and would otherwise spin the poll to timeout). Use literal subscriber-id strings (`"automation_engine"`/`"state_projection"` are `private` constants on `HomeSynapseCore`; only the two assembly `SUBSCRIBER_ID`s are public). (2) **The trigger-match lever for the run-init axis.** Only an **`EventTrigger`** (matched by `event.eventType().equals(t.eventType())` ALONE — no selector resolve, no `StateQueryService`, no `AutomationId`) deterministically matches a seeded event on replay. A `DirectRef`/state trigger is OUT — `StandardSelectorResolver.resolveDirectRef` returns `{}` for an entity not in the registry (and the seed entity is deliberately unregistered during replay). A `manual`/`automation_invoked` trigger is OUT — `InMemoryAutomationIdentityStore` re-mints the `AutomationId` each boot, so a seeded `automation_invoked` subject never matches the next boot's automation. The `EventTrigger`'s `event_type` is validated against `KNOWN_CORE_EVENT_TYPES` (reflected from ALL `EventTypes` String constants → `state_reported` is accepted) and the minimal `AutomationSchema` only checks name/triggers/actions presence, so it loads; assert `automationRegistry().getAll()` hasSize N to fail-fast if a config-load regression silently re-vacuates the axis. (3) **The dispatch axis needs BOTH `command_dispatched` AND `command_result` pinned.** Because the seed entity is unregistered during replay, a broken `command_dispatch_service` would emit `command_result("unroutable")` (via `route()`), NOT `command_dispatched` — so asserting only the "seam" (`command_dispatched`) is partly vacuous; assert both stay at the seeded count (the faithful integration analog of the unit test's "publisher empty"). The seam is observed via `core.eventStore().readFrom(0,N)` — `HomeSynapseCore` injects no recording publisher/sink (constructors take only `dbPath/configDir/config/clock/homeId[/payloadCipher]`), so do NOT add a production seam (the instruction says STOP-and-report rather than add one silently); the persisted `command_dispatched` IS the seam's observable output today, and a forward note carries the gate to the M9 adapter recorder. The ledger axis: seed a COMPLETE prior run (`command_issued`→`state_reported`→`state_confirmed`) — the seeded `state_confirmed` folds the command out of `StandardPendingCommandLedger.accumulateReplay`'s `replayInFlight` (it keys removal on `confirmed.commandEventId()` == the `command_issued` envelope's `eventId()`), so nothing is in-flight at `onCaughtUp` for `classifyRestart` to legitimately re-publish (`pendingCount()→0`). A POSITIVE CONTROL (fire one new trigger after LIVE, assert a real dispatch) is mandatory — "zero on replay" is only meaningful if the SAME harness sees non-zero in LIVE.
**Impact:** (1) For ANY "subscribers must not act on REPLAY/recovery" composition-root test, seed via a bus-less `PersistenceFactory` (try-with-resources `close()`) so checkpoints stay at 0 and a single boot replays the seed — never seed by running-then-rebooting a full core (its LIVE checkpoints make the replay vacuous). (2) Gate the post-boot assertion snapshot on polling `core.eventBus().subscribers()` until all runtime subscribers are LIVE (SUSPENDED = fail fast); persist-before-notify + single-VT delivery makes that a race-free "all replay side-effects now visible" boundary — do NOT sleep-and-hope. (3) For replay non-vacuousness keyed on a trigger, `EventTrigger`(type-only) is the ONLY registry-/`AutomationId`-independent match; pin automation-load count to catch a silent config-load regression. (4) Observe a side-effect "seam" with no injectable production hook via the durable event store, and pin EVERY event type a broken impl could emit (here both `command_dispatched` and `command_result`), not just the headline one. (5) Always include a LIVE positive control in a "zero on X" gate. (6) The state projection no-ops (creates an initial `EntityState`, does not throw) on a `state_reported` for an unregistered entity, so seeding state events for unregistered entities does not hang `awaitProjectionLive()`.

## 2026-06-28 | Category: architecture / spec-vs-source | Source: M7.5a (the read-API causal read projection) — a log-derived read projection derives correctness-bearing fields PURELY from the log (never a mutable registry); identity absent from a payload lives on the ENVELOPE; verify event SHAPES before designing
**Discovery:** A coding instruction can confidently mis-state event payload shapes, and following it literally bakes in bugs/debt. M7.5a's instruction assumed `automation_triggered`/`automation_completed` carry `automationId` (+ `triggerType`/`subjectRef`/`firingValue`/`matchedAt`) on the PAYLOAD and that per-action confirmation outcome should be RESOLVED via an injected `EntityRegistry`/`CapabilityRegistry`. Source said otherwise on every count: the run-lifecycle payloads carry NONE of those fields — `automationId` is recovered from the event **envelope** subject (`StandardRunManager` publishes on `SubjectRef.automation(automationId)`), and `AutomationTriggeredEvent` is `(runId, triggeringEventId, matchedTriggers, resolvedTargets, definitionHash, cascadeDepth)`. A pre-write **source-verification fan-out** (one agent per event-record cluster + the rest-api templates + lifecycle + the frozen contract) caught all of it before a line was written; a post-write 5-dimension adversarial review then found zero defects. The cheap up-front verification is what made the expensive write correct.
**Detail:** Three reusable design rules emerged. (1) **A pure log projection (INV-SA-03) must derive correctness-bearing fields from the log alone — reach for a registry only for explicitly-best-effort display labels.** The instruction's registry-gated `ActionOutcome` would have (a) made the read NON-deterministic (the `EntityRegistry` is mutable/hot-reloaded, so the same run could explain differently over time) and (b) depended on a `CapabilityRegistry` that **has no impl and is never constructed in the composition root**. The log already encodes the verdict: `state_confirmed`/`command_confirmation_timed_out` reference the command by payload `commandEventId` (an `EventId`), `command_result` by `causationId == command_issued.eventId` (outcome `"acknowledged"` is a protocol ack, NOT a failure). And the honest-confirmation guarantee (a `ConfirmationMode.DISABLED` command must never render CONFIRMED) is FREE: the ledger never tracks a DISABLED command, so it emits no `state_confirmed`, so a pure-log derivation can only ever render it DISPATCHED. `AutomationRegistry` is still consulted — but ONLY for the best-effort `automationName`/`trigger.type` labels the contract already declares nullable. (2) **When an event payload lacks an id you need, check the ENVELOPE (subjectRef/actorRef) before concluding it's unrecoverable** — read the producer's `publish(...)`/`EventDraft` call, don't assume the payload carries it. (3) **The forward-only `EventStore` (ascending `afterPosition` only; no descending/latest-N read) makes a newest-first list a bounded forward-scan + in-memory reverse with an O(limit) sliding window** (over the type-indexed stream → O(retained-of-type), not O(whole-log)); true O(page) DESC needs a future backward-read primitive. (4) **A JSON-STRING payload field that must surface as a wire OBJECT is the read-side mirror of the M7.4a inject-the-serializer lesson:** carry the raw String through the JSON-free core module (`com.homesynapse.automation`) and parse it at the rest-api JSON boundary (the module that already has Jackson, LTD-08) — which here required a second PLAIN `requires com.fasterxml.jackson.databind` (off the exported API, build dep already present), a deviation from the instruction's "only +automation module-info" but the minimal correct one. (5) **Serialize a frozen camelCase wire contract by hand-building `LinkedHashMap`/`List` with explicit keys, NOT by serializing the projection records** — a `Map` key bypasses any Jackson naming strategy (the live endpoints emit camelCase precisely because they hand-build maps), and it keeps the internal `RunStatus`/records (and the DP-A1 status mapping) off the wire with exact nullability control. Mirror the existing `ListEntitiesEndpoint`.
**Impact:** (1) Before designing any projection/consumer, verify the ACTUAL event-record component lists + the producer's envelope (subject/actor/causal) wiring — an instruction's payload-shape prose is a hypothesis, the `.java` record + the `publish(...)` call are the fact (a parallel source-verification fan-out is cheap insurance and caught ~6 instruction inaccuracies here). (2) Keep a log-projection's correctness-bearing fields a pure function of the log (determinism/replay-safety, INV-SA-03); a mutable-registry read at read time is a latent non-determinism bug even when it "works" — registries are for best-effort, contract-nullable display labels only. (3) The honest-negative property ("never a false CONFIRMED") can be a free consequence of the producer's behavior (no-track → no-confirm-event) rather than something a read-time gate must enforce — verify the producer to claim it. (4) Recover missing identity from the envelope subject/actor. (5) Forward-only stores ⇒ newest-first = bounded-scan-and-reverse (O(limit) window); flag the O(page) gap as a future backward-read store primitive. (6) Hand-build frozen-shape wire bodies as maps with explicit camelCase keys; never round-trip internal records through the shared mapper.

## 2026-06-28 | Category: architecture / spec-vs-source | Source: M7.5b (the non-firing read + automation list) — EXTEND a query-service interface additively (fan-out = the implementors + their test fakes), REUSE an existing projection helper to derive a new verdict (no duplicate outcome logic), render a no-common-accessor sealed type via getClass().getSimpleName(), and reconcile a frozen enum that lacks a value by routing the call to the human (never invent a wire value)
**Discovery:** Adding the co-equal "why did this NOT fire?" read on top of M7.5a was best done by EXTENDING the existing `ExplanationService` (two additive methods + impl in the existing `StandardExplanationService`) rather than minting a new service — same collaborators (`EventStore`+`AutomationRegistry`), same `over(...)` factory unchanged, zero new module edge. The two genuinely new mechanics: (a) the verdict for a COMPLETED run REUSES the M7.5a `buildActions(chain, runId)` (a private instance method, callable directly) — a run is `ACTED_BUT_UNCONFIRMED` iff any returned `ActionView.outcome() ∈ {UNCONFIRMED, FAILED}` — so the honest-confirmation derivation lives in exactly one place; and (b) the new frozen 4-value `verdict` enum has NO value for "fired fine," so the clean-success case (DP-B2) returns `NEVER_TRIGGERED` with a NON-NULL `lastRelevantRunId` (the nullable sibling field distinguishes "ran fine" from "never ran") and the call is ROUTED to Nick rather than inventing a 5th wire value.
**Detail:** Reusable rules. (1) **Extending a frozen-ish service interface: the entire compile fan-out is its implementors — the production impl AND every test fake/stub.** Here the only two were `StandardExplanationService` and `RunEndpointsTest.FakeExplanationService` (a nested `private static final class`); grep the whole test tree for `implements <Interface>` before declaring the fan-out closed, and grep for `getDeclaredMethods`/`hasSize` to confirm whether a reflective method-count shape pin exists (none did on `ExplanationService` — so the COMPILER is the only enforcement; a stale fake silently fails to compile, never a clean error). (2) **Reuse, don't re-derive:** a new read that needs a sub-fact another projection already computes should call that projection's helper (here `buildActions`), not copy its derivation — a second divergent copy of confirmation-outcome logic is a latent inconsistency bug. The COMPLETED branch reads the run's correlation (`completed.causalContext().correlationId()` → `readByCorrelation`) and folds the existing `ActionView` outcomes; `DISPATCHED`/`CONFIRMED` count as clean (matching the instruction's "{UNCONFIRMED,FAILED} only"). (3) **Render a human label from a sealed interface with an EMPTY body (no common `.type()`/`.summary()` accessor) via `getClass().getSimpleName()` + a deterministic humanizer** — robust across all 12/7/8 permits AND any future one, with NO fragile per-permit switch. A richer per-record rendering ("front-door opened") is not even derivable in a pure projection: the concrete records carry entity REFS (ULIDs), not names, and resolving names needs a determinism-breaking registry read. The simple-name rendering is the honest V1 ceiling; flag it. (4) **A frozen wire enum that lacks a value for a real state is a public-API-semantics call: implement the documented recommended default, distinguish the collision via a nullable sibling field, ROUTE the decision to the human owner, and NEVER invent a new wire value** (the frozen contract is co-signed; an additive 5th value is a future contract change, not a coder choice). (5) **A default that cannot be made replay-deterministic (a 24h-time window with no time→position index) degrades to a deterministic position bound (the whole log = "ever") + a flagged seam** — reading a clock at read time to compute "now−24h" would break replay-determinism; the opaque cursor lets the client scope precisely in the meantime. (6) **An exhaustive `switch` expression over the status enum with NO `default`** makes a future enum constant a compile error (not a silent miswire) — handle even the "can't happen" non-terminal values (here `EVALUATING`/`RUNNING` on a terminal marker → log WARN + the safe honest verdict, never a fabricated success).
**Impact:** (1) To add a read to an existing query service, extend the interface additively and grep the test tree for fakes — the compiler-only fan-out (no method-count pin) means an un-updated stub is a build break, so find them all before handoff. (2) Derive a new verdict by reusing the existing projection helper (single source of truth), not by re-deriving the same sub-fact. (3) For a sealed type with no common accessor, `getClass().getSimpleName()`+humanize is the deterministic, permit-robust rendering — don't write a per-permit switch and don't reach for a registry to "enrich" a pure projection. (4) When a frozen enum can't express a real state, ship the recommended default + a nullable distinguisher and route the public-API call to the human; never widen a frozen wire vocabulary unilaterally. (5) Render any read-time default as a deterministic log-position bound, never a wall-clock-derived window, to stay replay-safe; flag the missing index as the future enabler. (6) Keep status→verdict switches exhaustive with no `default`.

## 2026-06-28 | Category: module-boundaries / crypto | Source: AB-4 (at-rest cipher activation — F1 envelope version byte + GCM AAD, F3 nonce-construction fence, F13b fail-closed fsync) — when a typed failure lives in module A but the AEAD primitive lives in module B, thread the auth-context through the seam (don't infer it); widen vs overload by whether the old form has a legit caller; the compile fan-out of a widened interface method includes every test double in every module
**Discovery:** AB-4's F1 wanted three things at once that, taken together, dictate the design: (a) a TYPED `UNKNOWN_ENVELOPE_VERSION` fail-closed on an unknown at-rest envelope version byte — and that exception type lives in `persistence`; (b) the version byte bound as GCM **AAD** for downgrade-resistance — and the AES-GCM `Cipher` lives in `config` (`StandardScopeKeyManager`); (c) correct decrypt **on restart**. The naive "let the config manager own the version byte + bind a fixed AAD" fails (c): a fresh manager's in-memory state is empty, so on a decrypt-FIRST (reading old rows before writing any) it would not know to bind the AAD → GCM auth fails. The correct siting (B2): **`persistence` frames the byte** (one prepend site on write, one strict-parse-and-strip site on read, throwing its own typed exception) AND **threads the byte to the config GCM as AAD through a widened cipher seam** — so the auth context travels WITH the data on every call, never inferred from mutable in-memory state. A 4-dimension adversarial review (crypto/compile/constraints/tests) returned zero defects.
**Detail:** Reusable rules. (1) **When a typed failure must be raised in module A but the primitive that detects/uses the related context is in module B, and B cannot name A's exception type (acyclic edge), keep the PARSE+throw in A and pass the context (here the version byte as `byte[] aad`) DOWN to B through the existing seam** — do not push the typed-failure responsibility into B (it can only throw a `java.base` exception A would mis-classify) and do not have B infer the context from its own state (non-deterministic / wrong on a cold path). The seam parameter stays `java.base`-only (`byte[]`), so **zero new module edge** — a widened signature is NOT an edge change. (2) **Widen an interface method vs add an overload by whether the OLD arity has a legitimate continuing caller.** `PayloadCipher` (exclusively the event path, 1 prod impl + 3 test doubles) was WIDENED (`encrypt`/`decrypt` gain `byte[] aad`) — no legit no-AAD use, so no dead method left behind. `ScopeKeyManager.encryptPayload`/`decrypt` got ADDITIVE overloads instead (the 2-arg/4-arg forms stay) because the **secrets path** (`StandardSecretStore`, `SecretStoreTest`, `ScopeKeyManagerTest`) legitimately keeps calling them; the old forms delegate to the new with an empty `NO_AAD`. (3) **GCM `updateAAD` only when `aad.length > 0`** so empty-AAD callers (secrets + DEK wrap/unwrap) are byte-identical to the pre-change cipher — no migration of any already-written ciphertext, and the encrypt/decrypt sides agree as long as both pass the same (possibly-empty) array. (4) **The compile fan-out of a widened interface method = EVERY implementor in EVERY module, production AND test, PLUS every direct call on a concrete impl.** Here: the `Main` adapter (prod), `CountingPayloadCipher` (persistence test, must actually bind the AAD to stay faithful), the `decryptThrows` stub (persistence test), the anon stub in `HomeSynapseCoreTest` (LIFECYCLE module — easy to miss), AND a direct 2-arg `cipher1.encrypt(...)` call inside `AtRestEncryptionWritePathTest` that a `new PayloadCipher()`/`implements PayloadCipher` grep does NOT surface — grep BOTH `implements/new <Interface>` and `\.<method>\(` repo-wide. (5) **A pre-existing test can encode the OLD (documentation-only) contract that your new PROGRAMMATIC guard now forbids.** `ScopeKeyManagerPayloadNonceTest.fence_randomIvPathUnchanged` deliberately mixed `encrypt`+`encryptPayload` on ONE scope to prove the two IV/counter sequences stayed independent — exactly what the new F3 fence (`bindConstructionLocked`, first-use-wins) now rejects. Making a doc-only fence programmatic REQUIRES rewriting that test (to disjoint scopes) and adding the rejection test; first PROVE production never exercises the now-forbidden path (here secrets=`config_secrets` vs events=`identity`/`presence_personal` are disjoint by construction). (6) **A "fail-closed on fsync failure, but don't break Windows" requirement resolves on the OPEN-vs-FORCE distinction, and the fatal branch is made testable by injecting the force step.** Windows/non-POSIX cannot OPEN a directory as a channel (→ tolerate, durability rides the journal); a POSIX channel that opens but whose `force()` THROWS is a genuine durability failure (→ fatal `UncheckedIOException`). The real `force()` can't be made to fail on a healthy dir, so expose a package-private `DirectoryForce` SAM overload and inject a throwing one in the test (the production overload delegates to the real strategy, exercised separately as "doesn't break on this platform"). Scope the fatal write to the ONE durability-critical caller (the nonce counter), leaving every other writer on the best-effort path. (7) **A ctor-time "fail-closed early" assertion can be WRONG even when it looks safer** — a `null` cipher + non-empty encrypted-scope set is exactly the construction the fail-closed-READ tests need (to open a store and prove an encrypted row fails closed when no cipher is available); the write-time guard is the correct enforcement point. Verify the existing test corpus before "strengthening" an invariant at construction.
**Impact:** (1) Cross-module crypto/format: parse+throw the typed failure in the module that owns the type; thread the auth/format context down to the primitive's module as a `java.base` param (zero new edge); never infer per-call context from a primitive's mutable in-memory state (breaks the cold/restart path). (2) Widen a seam when the old arity has no legitimate caller (no dead method); add an overload when it does (here the secrets path). (3) `updateAAD` only on non-empty AAD → empty-AAD paths are bit-identical, no ciphertext migration. (4) For a widened interface method, grep BOTH `implements/new <Interface>` AND `\.method(` across ALL modules incl. tests — a direct call on a concrete double won't show in an implementor grep. (5) Making a doc-only rule programmatic usually breaks the test that documented the old leniency — rewrite it, and prove no production caller hits the newly-forbidden path first. (6) "Fail-closed fsync but Windows-safe" = tolerate dir-channel-OPEN failure, fatal on dir-channel-FORCE failure; inject the force step to test the fatal branch; scope fail-closed to the one durability-critical writer. (7) Don't add a ctor-time invariant assertion without checking the existing corpus — the "misconfiguration" you'd reject may be a legitimate fail-closed-read construction.

## 2026-07-02 | Category: module-boundaries | Source: M7.5c-a (internal-read envelope conformance) — "reuse the envelope machinery" can mean an inline IDIOM, not a helper; and a frozen contract's field vocabulary may not exist in the tree — derive from the closest in-tree vocabulary and document the mapping
**Discovery:** Two pre-verify surprises. (1) The instruction assumed the conforming endpoints build `{data, meta}` "via an existing helper + `ResponseMeta`-equivalent" — but NO shared envelope helper exists in `api/rest-api`: all seven conforming handlers hand-build the same `LinkedHashMap` meta block (`viewPosition` + `clock.instant().toString()`) + `X-HomeSynapse-View-Position` + weak `W/"{viewPosition}"` ETag INLINE, and the `ResponseMeta` record is a Phase-2 type no live handler touches. "Reuse verbatim" therefore means replicate the idiom exactly (the M7.5b-mirrored-M7.5a precedent — same as its inline `p:<pos>` cursor codec finding), NOT invent a helper mid-WU: extracting one only for the two new endpoints would CREATE the second envelope mechanism the instruction forbids, and migrating all nine handlers is an unauthorized refactor. (2) The frozen A5 field `parkedSubscribers` names a vocabulary the tree lacks — `SubscriberMode` is `{COLD, REPLAY, TRANSITION, LIVE, SUSPENDED}`, no PARKED. In-tree, "parked" is DLQ-ENTRY vocabulary (`SubscriberDlq`, `parkedAt`, `oldestParkedAt`), so the faithful derivation is "subscribers with parked entries" (`dlqDepth > 0`), NOT "mode == some parked-like mode"; a SUSPENDED subscriber stays visible via the additive per-subscriber `mode` field.
**Detail:** The STOP-condition in such a pre-verify gate is usually scoped ("STOP if the helper is not reusable from these endpoints' package") — when the observed reality differs from the gate's ASSUMPTION but not its CONCERN (here: everything lives in one package, so nothing forces duplication of a second mechanism), the right move is proceed-on-the-observed-pattern + report the observation verbatim, not STOP and not improvise a new abstraction. For the vocabulary gap: map the frozen field to the nearest in-tree semantic, state the derivation rule in the handler Javadoc AND the MODULE_CONTEXT row (so the next reader doesn't "fix" it to mode-based), and flag the plausible alternative (include SUSPENDED?) for the PM ruling rather than silently choosing the wider set. Also worth pinning: when a frozen data field needs a value the endpoint's module must not import (A4's `lagEvents` needs the event-log head, but rest-api must not require event-model), the established shape is a `java.base` supplier threaded from the composition root (`LongSupplier logHead = eventStore::latestPosition`) — same pattern as the existing `viewPositionSupplier`; and when the authoritative constant is an inline literal at the composition root (the projection version `5` at `StateProjection.create`), hoist it to a named constant there and pass it through, so the wire field and the projection can never diverge — a cited-but-duplicated literal is the drift the "locate its authoritative source" clause exists to prevent.
**Impact:** (1) Pre-verify the FORM of "existing machinery" before assuming a callable helper — in this repo the envelope, the cursor codec, and the ETag are all inline idioms; conforming means byte-level idiom replication, and any helper extraction is a separate, all-call-sites refactor WU. (2) A frozen-contract field name is not evidence the vocabulary exists in the tree — derive from the closest in-tree concept, document the derivation at both the handler and MODULE_CONTEXT, and route genuinely ambiguous inclusions to the PM. (3) Cross-module values enter rest-api as `java.base` suppliers/params from the composition root, never as new module edges. (4) Hoist an inline authoritative literal to a named constant at its owning site when a second consumer appears; never mint a second cited copy.

## 2026-07-02 | Category: test-code / compile | Source: M7.5c-a gate round 1 (hub-applied 2-line fix) — AssertJ `containsExactly` on a wildcard-typed actual is a varargs-capture compile error; the codebase idiom is the `@SuppressWarnings("unchecked")` typed-local cast
**Discovery:** `assertThat((List<?>) map.get(k)).containsExactly("x")` fails `compileTestJava`: the actual's element type captures as `CAP#1`, and `containsExactly(CAP#1...)` cannot accept `String` (varargs mismatch). Production compile was GREEN — the class is test-only and compile-time, and the lane's sandbox could not run the targeted compile (build-logic bootstrap fails on the mount + VM JDK 11 vs 21), so it survived to Nick's gate despite the instruction's compile-before-handoff step.
**Impact:** (1) Never assert element CONTENTS through a wildcard-typed actual — use the conforming idiom (`ListEntitiesEndpointTest` L83–88): `@SuppressWarnings("unchecked") List<String> xs = (List<String>) map.get(k);` then assert on the typed local. `(List<?>)` casts are fine for `hasSize`/`get(i)` only. (2) Environment corollary: when the lane cannot compile in-session, every compile-only defect lands on the host gate — weight self-review toward compile-sensitive spots (generics/varargs/capture), or route compile-heavy WUs to a host-side Coder (Claude Code) where the targeted loop runs in-session.

## 2026-07-02 | Category: concurrency/design | Source: M9.1 (integration spine) — a supervisor that restarts hosted workers needs THREE ownership disciplines, and an adversarial-review fleet found all three missing on the first cut

**Context:** `StandardIntegrationSupervisor` hosts adapters on per-adapter threads with restart cycles (`restartIntegration`, TRANSIENT backoff relaunch) and a grace-bounded `stop()`. The first implementation compiled clean in review (zero signature errors across a 6-dimension source-verifying fleet) but carried FOUR confirmed lifecycle bugs, all instances of missing ownership discipline.

**The three disciplines (bake into any supervisor-shaped WU):**
1. **Irreversible resources must be re-created on relaunch.** `ExecutorService.shutdownNow()` is permanent; a `final` per-worker executor + a stop-then-start path = a "restarted" worker that silently rejects all work forever while reporting HEALTHY. Any resource killed by the stop half must be recreated by the start half (`if (executor.isShutdown()) executor = newExecutor(...)` under the state lock) — and the field's javadoc must say WHICH lifecycle events kill vs. recreate it.
2. **Loop threads need generation identity, not just a shutdown flag.** A boolean `shuttingDown` that the restart path RESETS resurrects any thread that survived its interrupt past the join grace (wedged JNI read — the exact SERIAL case Doc 05 anticipates): the zombie re-enters the FSM and un-hosts the NEW generation's worker. The guard is identity, not state: the loop only acts while `runtime.runThread == Thread.currentThread()` (checked at loop top, after run() exit, and before hosting a relaunched adapter), and clears the field in a `finally` ONLY if it still owns it.
3. **Exactly one party closes — claim, then close.** When both a supervise loop (spontaneous crash path) and `stop()` can tear down the same worker, close-the-local-reference races into concurrent double-close (the `IntegrationAdapter.close()` contract is sequential-idempotent only). The fix is an atomic claim: compare-and-null the hosted reference under the lock and close ONLY what you claimed; the party that lost the claim also skips the lifecycle-event publish (else duplicate `integration_stopped`).

**Also:** a `join(grace)` interrupted by the CALLER's interrupt is not the closee overrunning its grace — restore the flag and stop waiting, but do NOT interrupt a compliant closer (that punishes close() early and violates the grace contract).

**Impact:** for any future supervisor/host/pool WU (M9.2 transport threads, the post-hero breadth unit): (a) write the executor/thread/resource lifecycle table BEFORE the FSM (created-by / killed-by / recreated-by per lifecycle verb); (b) unit-test the restart verb explicitly — the M9.1 T-table covered crash-restart but not `restartIntegration`, which is exactly where the executor bug hid; (c) when the compile gate is unavailable, a source-verifying adversarial fleet (every call checked against the real declaration + per-finding refutation) is a strong compile substitute — it returned zero false compile findings AND surfaced the four real lifecycle bugs the type system cannot see.

## 2026-07-02 | Category: jpms/api-design | Source: M9.1 — two escape hatches for frozen contracts: the same-package assembly gateway (fast-stop past a frozen interface) and the static interface factory (constructing a package-private impl cross-module)

**Context:** (1) W4 demanded a fast supervisor stop on the abandon path, but `IntegrationSupervisor` is a frozen 9-method Phase-2 interface and the impl is package-private — lifecycle cannot reach a concrete-class method (the `InProcessEventBus.abandon()` precedent assumed the holder sees the concrete type). (2) B7 needed lifecycle to construct the package-private `ScopedConfigurationAccess` living in the config module.

**Lesson:** both are solved without touching the frozen contract or the impl's visibility: (1) a public static gateway on the same-package PUBLIC assembly (`IntegrationSupervisorAssembly.abandon(IntegrationSupervisor)` — downcasts in-package to reach the package-private fast path, falls back to `stop()` for foreign impls); (2) a public static factory ON the interface itself (`ConfigurationAccess.scoped(type, model)` — the `StateQueryService.materialized(...)` pattern; same package ⇒ can construct the package-private class; the exported-package check means ZERO module-info change). Both are `[REVIEW]`-class deviations (new public API) — argue them from the precedent + the frozen-contract constraint, don't silently widen.


## 2026-07-03 | Category: verification | Source: M9.2 (Zigbee transport/EZSP) — split external-standard re-derivation verdicts into WIRE constants (the standard wins; STOP on mismatch) vs POLICY parameters (the LOCKED doc wins; note the divergence); the derivation fleet caught a mis-pinned enum value the instruction never contained
**Discovery:** The pre-write derivation fan-out (one agent per protocol layer, deriving from bellows/herdsman/AN706 knowledge BEFORE comparing to the instruction's §Vectors) surfaced three divergences of two fundamentally different kinds. WIRE-class: the ASH duplicate-frame rule was over-generalized in the instruction (only the duplicate of the last-delivered frame re-ACKs; a genuinely out-of-sequence frame enters the reject condition and NAKs) — the standard wins, the implementation follows bellows/AN706. POLICY-class: Doc 08's "5 consecutive ACK timeouts → FAILED" matches NEITHER reference (bellows uses 4, the Silabs C host and herdsman use 6) — the references disagree with EACH OTHER, the parameter is interop-neutral, so the LOCKED doc's 5 is kept and the divergence recorded for doc currency instead of STOPping. A third case dissolved on inspection: "≥ 2 s RSTACK wait" is satisfiable by the standard's own T_RSTACK_MAX = 3.2 s — pick the value that satisfies both. Separately, the POST-write residual-constant pass (live-fetching bellows sources for the constants my own memory had flagged as moderate-confidence) caught SL_STATUS_NOT_JOINED mis-pinned as 0x0B (= SL_STATUS_IS_WAITING; correct value 0x0017) — a value the instruction never mentioned, introduced by coder memory, that would have broken v14 resume with no test failing (the fake NCP scripts whatever constant production defines).
**Impact:** (1) Classify every derivation mismatch before reacting: a WIRE mismatch (bit layouts, CRC parameters, enum values, struct offsets) is a STOP/fix — garbage on the wire; a POLICY mismatch (retry budgets, timeout counts) where references also disagree among themselves follows the LOCKED doc and gets a currency note. (2) Any constant you pin from MEMORY at moderate confidence must go on an explicit residual-verification list resolved against fetched sources before handoff — self-consistent fake-driven tests CANNOT catch a mis-pinned protocol constant (the fake speaks whatever dialect production defines); flag the ones that stay memory-only as bench-verify items. (3) The two-pass shape (derive-before-write, verify-residuals-after-write with live source fetches) is cheap relative to one wrong enum value surviving to silicon.

## 2026-07-03 | Category: testing | Source: M9.2 — deterministic serial-protocol testing: the fake channel ADVANCES the injected TestClock by the full requested wait on empty reads, and a reactive fake NCP built on the production codec gives full-stack protocol tests with zero real waits
**Discovery:** Every ASH/EZSP timeout path (adaptive ACK band, 5-timeout FAILED, per-command timeouts, probe step budgets, watchdog idle) became instantly and exactly testable with one fake-channel convention: `read(buffer, maxWaitMillis)` returns queued bytes when present, else advances the shared TestClock by maxWaitMillis and returns 0. Production deadline loops (`remaining = deadline - clock.instant()`) then terminate deterministically with EXACT elapsed arithmetic assertable (e.g. 1600 + 4×3200 for the five-timeout path; 8200 ms for the silent probe). Layered on top, a FakeNcp write-handler parses the host's frames with the SAME codec production uses (whose correctness is separately pinned by independent byte vectors: the AN706 example frame, the LFSR keystream, two CRC check values), auto-RSTACKs, auto-ACKs, and answers EZSP commands from a pluggable function — full-stack protocol tests (negotiation tiers, single-in-flight under concurrent VTs, formation byte-structs) with zero real serial I/O and zero Thread.sleep.
**Detail:** Two traps worth carrying. (1) If production FLUSHES input before a handshake (connect/probe), pre-enqueued fake responses are wiped — script reset/probe responses REACTIVELY via the write-handler, never by pre-enqueueing. (2) The codec-based fake is only non-circular because the codec itself is pinned by INDEPENDENT external vectors in a separate test class; without those, fake-vs-production round-trips would happily agree on a wrong wire format. Related review catch: a test helper that strips a framing byte must receive un-stripped input — a double-strip produced a deterministic CRC-reject that an LLM read-pass missed and the adversarial refuter confirmed bit-exactly (the compile/test gate class, caught only because the fallback review re-simulated the codec).
**Impact:** For any timeout-rich byte-protocol unit: (a) make the fake channel's empty-read advance the injected clock by the requested wait — every timeout path becomes exact-arithmetic-assertable with no wall time; (b) build the reactive peer simulator ON the production codec and pin that codec separately with independent external vectors (the involution/keystream/check-value trio); (c) respond reactively to writes when production flushes; (d) when a helper both strips framing AND parses, audit every call site for double-stripping — it fails deterministically but compiles cleanly.

## 2026-07-04 | Category: testing / value-model | Source: M9.3 (WithinTolerance A10) — `QuantityValue` normalizes to CANONICAL units in its compact constructor; a test fixture that hands it a protocol unit gets a silently converted magnitude

`new QuantityValue(126.0, "K")` is temperature-Kelvin and becomes −147.15 °C before `evaluate()` ever sees it — the ±band test then fails mysteriously on a correct implementation. The catalogue conversion is a FEATURE of the value model (canonical-at-construction), not a bug; the lesson is fixture discipline: when a test needs a raw magnitude through a `QuantityValue`, use an identity-conversion unit ("%", "W"); and color-temp Kelvin is an `IntValue` (`color_temp_kelvin`), never `QuantityValue("K")`. Generalizes: before building fixtures on a value type, read its compact constructor — validation is visible in the signature, NORMALIZATION is not.

## 2026-07-04 | Category: protocol / dedup | Source: M9.3 (measured-contract ingestion dedup) — scope the dedup comparison to the COMMAND PAYLOAD, not the frame: the field you dedup on (TSN) is embedded inside the bytes you compare

The measured duplicate pattern is "consecutive TSN + identical payload" — but the twins are only identical AFTER the ZCL header, because the TSN itself differs and lives in the header. Comparing whole-frame bytes makes the dedup structurally never fire, and every unit test that hands the deduplicator a pre-built identical array stays green while the integration path double-publishes. Two rules: (1) when a dedup contract says "identical payload," pin down which byte range "payload" means at the layer doing the comparing — exclude the fields that legitimately vary; (2) write at least one test that feeds the dedup through the REAL parse path with twins that differ exactly as the wire differs (here: the fixture-replay gate caught it same-session).

## 2026-07-04 | Category: architecture / frozen-surfaces | Source: M9.3 (interview retry + frozen DTOs) — when a frozen no-throws surface implies blocking retry semantics, move the schedule into a clock-scheduled queue and make the surface single-attempt; when a frozen record can't carry a failure state, keep a richer internal result type and render the frozen type only where constructible

Doc 08 §3.4's "retry 3× at 5/15/30 s" cannot live inside a frozen `interview()` without sleeping a thread through backoffs (NO Thread.sleep). The working shape: `interview()` = ONE attempt; retries are TIMESTAMPS in a pending queue the ingestion cycle drives (`due()`/`recordFailure()`/`onFrameReceived()`) — the sleepy park/resume machine falls out of the same mechanism for free. Similarly `InterviewResult` (non-empty endpoints, non-null identity) cannot represent a total failure: the internal `InterviewAttempt` carries gathered-data + failedStep, and the frozen record renders only when constructible (PARTIAL uses empty-string identity; the event layer maps that to its sentinel because the frozen event payload rejects blanks). Pattern name: richer-internal-currency, frozen-surface-rendered-at-the-edge (the M7.4b lesson's sibling for results instead of methods).

## 2026-07-04 | Category: event-sourcing / self-consumption | Source: M9.4a (F-2 supersession) — a subscriber that PRODUCES an event type it also CONSUMES must classify its own dispositions before terminal-matching; the bus loops every publication back

The ledger consumes `command_result` (the acknowledged/rejected correlation) and M9.4a made it PRODUCE `command_result(superseded)`. The loop-back is not hypothetical: the bus redelivers the ledger's own publication, `onCommandResult`'s (entity, command) fallback matches the NEWER in-flight command of the SAME name, and the non-acknowledged else-branch removes it and emits a spurious timeout — the hero E2E dies while every pre-existing unit test stays green (unit publishers don't redeliver). The same latent class existed for `expired_on_restart` since M7.3 (post-restart redelivery vs a re-issued command). Fix shape: a disposition guard — outcomes that are terminal REPORTS about an already-concluded command (`superseded`/`expired_on_restart`/`unconfirmed`) are skipped before the terminal-match; adapter REJECTIONS (`rejected`/`timed_out`/`unroutable`/...) still match. Two rules: (1) whenever a change makes a subscriber emit an event type in its own subscription filter, enumerate the loop-back path explicitly and write the redelivery unit test (a RecordingPublisher can't catch it — feed the published payload back through onEvent); (2) grep the consumer surfaces for the outcome vocabulary BEFORE adding values — the S4-style survey found this in minutes; the E2E would have found it in hours.

## 2026-07-04 | Category: harness / composition-root | Source: M9.4a (hero loop) — the load-validated vs trigger-time-resolved selector split decides what a boot-time config can reference; labels are the only selector that joins runtime-minted entities to boot-loaded automations

Adoption mints entity ULIDs at RUNTIME, but automation configs load at Phase 1 — `entity_ref` (unknown ULID) and `entity:` slug (load-time registry-validated, entities don't exist yet) are both unusable, and inline trigger selectors can't use `type:` (the key collides with the trigger's own `type:` — parseSelector reads the same map). `label:` is the seam: NOT validated at load, resolved at trigger time, and the test can re-register an adopted entity with a label via `updateEntity`. Same trick for action targets. Generalizes to any boot-config-references-runtime-identity test: find the selector that defers resolution to fire time, and inject the join at runtime through the registry's update surface. (Also caught: the trigger-map `type:` collision means an inline type-selector on ANY trigger is unreachable — a loader currency item for the PM.)

## 2026-07-04 | Category: instruction-verification | Source: M9.4a (identify leg + buildCommand structure) — behavioral pins can be structurally wrong two ways: where the behavior LIVES (base-final vs per-impl) and whether a mandated flow is REACHABLE through frozen gates (dispatch validation)

Two same-session instances. (1) P27 pinned "six impls ALL throw on buildCommand" — behaviorally exact, structurally wrong: ONE `public final` throw on the abstract base; filling ×3 required removing `final` (an unlisted base-class edit, declared). (2) The mandated hero leg "issue identify → dispatched frame" is unreachable through the REAL path: Tier-1 dispatch validation requires a capability defining the command, and no core capability defines identify — the profile's identify characterization WARN-skips at install (nothing to install ONTO), and the §2 test line asserting a DISABLED policy for it was unrealizable. The E2E drives the router+handler halves directly and the gap is a flagged ruling. Rule: for every mandated end-to-end behavior, trace REACHABILITY through each frozen gate (validators, filters, guards) at read time — a pin sheet verifies signatures and line numbers, but only a flow trace verifies that the mandated path can execute; raise unreachable mandates as findings BEFORE building the workaround, and keep the workaround at the narrowest bypass (skip only the gate that cannot pass, keep every downstream real).

## 2026-07-04 | Category: protocol / await-primitives | Source: M9.4b (§5.3 NETWORK_UP await) — an await built over a single-in-flight pipeline must check the shared callback BUFFER before reading the transport; the signal may have been consumed by an earlier command's own response loop

`awaitNetworkUp()` read the transport for `stackStatusHandler(EMBER_NETWORK_UP)` — green on the FORMATION path (the callback rode the formNetwork response and stayed in the channel) and a guaranteed false timeout on the RESUME path: the callback arrived during the `networkInit` exchange, the `getNetworkParameters` command that followed READ it first, and its response loop dutifully enqueued it into `pendingCallbacks` — the await then re-read an already-answered radio until the window closed. The red §7.6 second-boot test caught it (the two paths differ only in how many commands follow the signal's arrival). Rule: in a single-in-flight pipeline where every command's read loop preserves unrelated frames into a shared buffer, ANY await-for-frame primitive must scan-and-remove from that buffer FIRST, then fall through to transport reads — and the test matrix must include a path where at least one command runs between the signal's cause and the await.

## 2026-07-04 | Category: JPMS / reflective-shape-locks | Source: M9.4b (§2.3 StateQueryService factory overload) — a lambda inside an interface's static method compiles to a SYNTHETIC interface method that reflective shape locks count; park the lambda on the impl class

Adding a 5-arg `materialized(...)` overload should have taken `StateQueryService.getDeclaredMethods()` from 6 to 7 — the shape test failed at 8 because the 4-arg overload's delegation body contained `() -> null`, which javac lowers to a synthetic `private static` method ON THE INTERFACE. Fix: hoist the constant to the package-private impl class (`MaterializedStateQueryService.NO_REGISTRY`) and reference it — the synthetic lands on the impl, whose shape nobody locks. Rule: modules with interface shape tests (`getDeclaredMethods().hasSize(N)`) must keep interface bodies lambda-free (method refs to EXISTING methods are safe; lambdas and capturing expressions are not), or the shape test should filter `isSynthetic()` — prefer the lambda-free body so the lock stays a strict literal count.

## 2026-07-06 | Category: test-harness / callback-delivery | Source: M9.4-TCJ (join-handler tests) — a callback riding a command's response is NOT in the ingestion drain until the protocol next READS; adapter-level tests must pump (ping) before cycling

The FakeNcp idiom for unsolicited callbacks is "ride them on a command's response list" — but the single-in-flight pipeline reads ONLY until its matched response arrives, so riders appended after the response sit unread in the channel. `runCycleOnce()` alone then drains an EMPTY callback queue and the test asserts against silence (the first draft of the M9.4-TCJ join tests had exactly this defect — caught in self-review by tracing the read path, before the adversarial fleet ran). The delivery step is a subsequent read: the rig's `deliverAndCycle()` idiom (`coordinatorProtocol().ping()` — the nop's response loop reads the riders, classifies them `isCallback`, and parks them in `pendingCallbacks` — then `runCycleOnce()` drains). Rule: any adapter-level test that scripts an unsolicited callback must account for WHO performs the read that moves it from the channel into the drain — in production it is `pumpInbound()`/the next command; in tests it is an explicit `ping()`. Corollary for boot-path awaits: the same rider mechanics are why `awaitNetworkUp` checks the buffered queue first (the 2026-07-04 await-primitives lesson's test-side mirror).

## 2026-07-06 | Category: test-harness / shared-fixture-modeling | Source: M9.4-NCFG (the config prelude) — a batch added to SESSION START changes frame arithmetic for EVERY scripted responder; model the NCP-side state in the shared fake (handler-precedence-preserving built-in), never by widening N per-class responders

Adding 13 exchanges (10 `setConfigurationValue` + 3 `getConfigurationValue`) inside `startSession()` put the batch on EVERY test path that negotiates a session — seven independent responder functions across six test classes plus the rig would each have needed the same ~15-line echo arm AND a per-instance value store. Instead the echo model went INTO `FakeNcp` itself as a precedence-preserving built-in: it answers ONLY when the scripted handler returned an EMPTY list (unscripted frame), so an explicit scripted NAK always wins, `null` still means silence (the timeout idiom), and RST clears the store (matching the real NCP's reset-to-defaults — which is the very premise of the WU). Result: zero per-class responder edits; the NAK/mismatch tests script their overrides exactly as before. Two rules: (1) when production gains a session-start (or any always-runs) exchange, put its happy-path model in the SHARED fake behind handler precedence — the fake is simulating the DEVICE, and device state (config memory, reset semantics) belongs there, not copy-pasted into every script; (2) never assert `receivedEzspCommands()` TOTALS or "command at index N" (except index 0 = the version frame, which negotiation-first guarantees) — count per-frame-id or filter legacy frames, because session-start arithmetic is allowed to grow. The four EzspProtocolTest count assertions that broke were exactly the ones violating rule 2; their intent-preserving rewrites (one-legacy-version-frame, second-start-adds-nothing) are STRONGER pins than the totals they replaced — "second start adds nothing" now also proves config-not-rewritten for free.

## 2026-07-06 | Category: test-harness / callback-delivery | Source: M9.4-ADP (config-accepted adoption tests) — within ONE scripted response list, riders must precede the matching response; the receive loop returns at the match and everything behind it strands

The second failure mode of the rider mechanics (the 2026-07-06 TCJ lesson covered the first): the M9.4-ADP tests DID pump correctly (`ping()` before `runCycleOnce()` — the deliverAndCycle idiom), but the handler's nop branch returned `[nop-response, rider]`. `executeLocked`'s receive loop returns AT its matched response, so the rider behind it stayed unread in the channel and every announce processed one protocol-read late — 6 of 7 tests failing, with T6 passing by coincidence of the lag (three announces happened to yield the same totals; a passing test proved nothing). The rig's `handleCommand` has the order right: queued callbacks FIRST, the command's own response LAST — that order makes the pump's read park the riders as callbacks BEFORE the loop terminates on the match. Two rules: (1) in any FakeNcp handler, a callback frame goes BEFORE the response of the command carrying it, never after; (2) a multi-round test whose totals survive an off-by-one-cycle lag is not evidence the pipeline is synchronous — assert something the lag breaks (a count after ONE round). Caught by the adversarial test-refuter lens tracing FakeNcp.apply → AshSession ack-await → executeLocked's return — the harness idiom looked byte-identical to the proven TCJ test and passed visual review; only the read-path trace exposed it.

## 2026-07-07 | Category: design-disambiguation / blast-radius | Source: M9.4-RPT (the §3 downgrade mapping) — when an instruction's vocabulary is ambiguous between two defensible semantics, enumerate which EXISTING end-to-end pins each candidate breaks; the reading that holds the invariant AND the frozen suites green is the ratified one

The §3 contract said non-VERIFIED postures downgrade to "DISABLED-with-recorded-reason or BEST_EFFORT as the ratified mapping dictates" — and the ratified mapping existed nowhere as a table (the docs agent assembled it from Doc 08 §3.6 prose, a worked example, Doc 02 §3.8, and Register §51, with the DISABLED-vs-BEST_EFFORT selector an explicit gap). The disambiguator was not more doc reading — it was blast-radius enumeration: DISABLED-for-READBACK_ONLY would flip the Hue's on_off/brightness/color_temperature policies whenever a drive degrades, and RestartHonestyIT's re-link (whose rig scripts no Bind_rsp → every bind honestly fails → READBACK_ONLY facts) would break its CT-15000 confirmationTimeoutMs pin and its state_confirmed leg — while keep-policy-BEST_EFFORT holds both the invariant (never-false-CONFIRMED is structural: CONFIRMED only renders on genuine report evidence) and every downstream pin. The instruction-level ambiguity was thus resolvable by tracing which candidate semantic the ALREADY-RATIFIED test suite contradicts. Two rules: (1) before implementing an under-specified downgrade/degrade semantic, run the candidate through every frozen E2E that will newly traverse the code path (an always-runs addition like a post-adoption drive puts NEW code under OLD suites — the suites become the tiebreaker); (2) record the chosen selector as a FLAGGED coder's-choice with the counterfactual named ("the other reading breaks pin X"), so the PM ratifies a decision, not a description.

## 2026-07-07 | Category: verification / wire-constants | Source: M9.4-KEY (the TC key-request policy-ID mis-map) — within a FAMILY of similarly-named enum members, name-recall is the mis-pin vector symbolic binding cannot catch; verify id-family constants by POSITION in the version-pinned header AND against the reference stack's own WRITE SET

**Discovery:** M9.4-TCJ pinned `POLICY_TC_KEY_REQUEST = 0x09` — the NAME was exactly right, the VALUE belonged to `TC_REJOINS_USING_WELL_KNOWN_KEY_POLICY` (a sibling member with a near-identical name-shape in the same EzspPolicyId family). Every unit test passed by construction: the module's own BENCH-VERIFY discipline (tests bind the production constant symbolically so silicon corrections stay one-constant edits) makes wrong-but-consistent invisible — that is its designed trade, not a flaw. Silicon caught it in one iteration BECAUSE the §B 0x009B instrument had landed one WU earlier: `key_establishment_failed: status=0x11` ×3 → BDB leave decoded (bellows verbatim) to TC_REJECTED_APP_KEY_REQUEST, which named the mis-aimed policy directly. The three-reference verification that closed it: (1) the classic + GSDK-version-pinned `ezsp-enum.h` for member POSITIONS (GSDK 4.4.5 = EmberZNet 7.4.5, the frozen bench stack); (2) bellows `types/named.py` for the status vocabulary; (3) bellows `EZSP_POLICIES_SHARED` for the reference stack's own WRITE SET — which showed 0x05 written with 0x51 and 0x09 NOT WRITTEN AT ALL, so the fix (re-aim, don't add) fell out of the A/B.
**Impact:** (1) The 2026-07-03 double-derivation discipline extends beyond frame ids/status codes to EVERY id-family enum (EzspPolicyId, EzspDecisionId, EzspConfigId): a name that matches intent can still carry a sibling's value, and the failure mode is specifically FAMILIES of similarly-named members — derive by position in the version-pinned header, and cite that header in the constant's javadoc so the next reader can re-verify without re-research. (2) When a reference implementation exists, verify not just VALUES but the SET OF WRITES it performs — "which policies does bellows write at all" is as load-bearing as "what value does it write"; matching the write set IS the A/B posture. (3) Instrument-first pays compound interest: the observability arm shipped speculative in one WU became the exact decoder of the next WU's defect — when a hardware outcome can fail silently, land the named-vocabulary log arm BEFORE the mechanism that needs it is suspected.

## 2026-07-08 | Category: test-harness / log-capture | Source: M9.4-KEYb (the DEBUG progress-line tests) — a logback ListAppender is level-gated by the LOGGER's effective level, not its own; DEBUG-line assertions must set-and-restore the logger level AND assert the line's PRESENCE, never a higher-level absence

The module's proven ListAppender idiom (attach to the class logger, filter captured events by level+prefix) silently captures NOTHING below the logger's effective level — the test tree's `logback-test.xml` roots at INFO, and logback consults the effective level BEFORE `callAppenders`, so an appender "attached at DEBUG" is a fiction: appenders have no level; the logger gates. A test asserting only "zero WARN" around a DEBUG-classified arm therefore passes VACUOUSLY whether or not the arm exists (the vacuous-VERIFY class — the M9.4-KEYb instruction flagged it pre-emptively). The working idiom: capture the logger's OWN level (`getLevel()` — null when inherited), `setLevel(Level.DEBUG)` scoped to the delivery window in try/finally, `setLevel(previous)` restores (null restores inheritance on a non-root logger), and the assertion is `containsExactly(<the DEBUG line verbatim>)` — presence-based, so a filtered-out line fails loudly. Red-first proved the pair honest: with the level idiom in place but no production arm, the tests failed ON the presence assertion (the byte fell through to WARN), not on a capture artifact. Rule: for every log-line contract at DEBUG (or any level below the test-tree root), the test must (1) explicitly manage the logger level with restoration, and (2) assert the line's presence verbatim — absence-only assertions at suppressed levels are not evidence.
## 2026-07-08 | Category: java-semantics / autoboxing | Source: M9.5-DUR (the PayloadMirrors Number canonicalization) — a conditional expression with Integer and Long branches applies binary numeric promotion and re-boxes the Integer branch to Long; canonicalization helpers must use statement branches (or switch-arm yields), never `cond ? Integer : Long`

The mirror-boundary canonicalizer pinned every payload `Number` to the Jackson-native pair (integral -> Integer/Long, decimal -> Double) so a decoded registration payload is STRUCTURALLY equal to the emitted one (the DP-8 equal-state short-circuit depends on record equality). The first draft's Long arm read `inIntRange ? Integer.valueOf(l.intValue()) : l` — and JLS §15.25 classifies that as a NUMERIC conditional: both operands unbox, promote to long, and the result re-boxes to Long, so the canonical-Integer branch silently produced Long anyway. Every VALUE was right; only the box type was wrong — exactly the class of defect the canonicalizer exists to kill, invisible to value-equality assertions and caught red-first only because the test pinned `isExactlyInstanceOf(Integer.class)`. Two rules: (1) any helper whose CONTRACT is about boxed types must never route mixed-box branches through `?:` — use an if/yield block, or cast both operands to the common reference supertype; (2) the tests for such a helper must assert `isExactlyInstanceOf(...)`, not value equality — a Double-vs-Float slip needs the instanceof pin to surface at all.

## 2026-07-09 | Category: other | Source: M9.5-DURb — flipping "verify later" annotations to "verified" statements is an evidence-transcription job, not a rewording job
**Discovery:** When an instruction mandates flipping every BENCH-VERIFY clause to a silicon-verified statement "citing the bench record iteration that proved it," each clause needs its OWN evidence trace against the record — and at least one clause (the §A.3 transient-key self-expiry) had NO direct proof anywhere in iterations 1–5b (it-1 explicitly recorded it "unobserved"; no later iteration measured it). Mechanically rewording that clause to "verified" would have fabricated a citation the record does not contain.
**Detail:** The honest flip states what the record DOES prove (the enablement accepted at it-1; joins completed through the key at it-2/5a/5b; repeated windows across the arc with no lingering-key misbehavior) and records the direct observation as unmeasured with the original contingency (clear-on-window-close) still standing — then flags the residual as an [INFO] deviation instead of silently absorbing it. The grep-bound DONE-when (zero tokens) is still satisfied: token removal and claim honesty are independent axes. Corollary from the same pass: the flip diff must be provably comment/javadoc-only (`git diff -U0 | grep` filtered to non-comment lines returning empty) — "constants BYTE-UNTOUCHED" is a checkable property, not a promise.
**Impact:** Treat every annotation flip as a claim requiring its own citation lookup ("copy citation lines, do not invent" applies per-clause, not per-file). A clause the evidence base never proved gets an honest partial statement + a deviation flag — never a blanket upgrade. Verify comment-only diffs mechanically before reporting them.
