<!--
file: context/lessons/coder-lessons.md
purpose: Append-only record of patterns, pitfalls, anti-patterns, and discoveries from coding sessions.
audience: Coder
update-cadence: append-only
state-type: history
status: CURRENT
last-verified: 2026-05-20 against commit 25bc23b
-->

# Coder Lessons Log

Append-only record of patterns, pitfalls, anti-patterns, and discoveries from coding sessions. Read this at session start. Append new entries at the end when discoveries are made.

Periodically, the most significant entries are promoted into the skill reference files (java-patterns.md, testing-standards.md, homesynapse-mental-model.md). Entries are never deleted from this file — it is the complete historical record.

## Entry Format

```
## YYYY-MM-DD | Category: [concurrency|persistence|serialization|testing|module-boundaries|build|anti-pattern|other] | Source: [what task]
**Discovery:** [one sentence summary]
**Detail:** [the specific issue and the correct approach]
**Impact:** [what this means for future work]
```

---

> **Rotated 2026-06-11 (hygiene pass A4):** lessons dated 2026-05-31 and earlier (the M1–M4 era) → `archive/coder-lessons-2026-05-m3-m4.md`. June (M5/M6-era) lessons retained below.

## 2026-06-05 | Category: other | Source: M4.B-S2 (AMD-44 Stage 2) — adding a record component + a back-compat convenience ctor of the OLD arity is the zero-breakage way to evolve a widely-constructed record
**Discovery:** When a record gains a component (`Entity` 11→12, `ProposedEntity` 3→4) but is constructed at many sites you must not (or cannot) touch in this WU, add a **convenience constructor whose parameter list is the pre-change canonical signature**, delegating to the new canonical ctor with the new component defaulted. Every existing `new Entity(... 11 args ...)` call silently re-binds to the convenience ctor — `EntityTest`, `TestEntityFactory`, and the cross-module `StubIntegrationContext` (integration-api testFixtures) all compiled unchanged, no edit needed. There is **no overload ambiguity** because the two ctors differ in arity (11 vs 12 / 3 vs 4); ambiguity would only arise if the new component's type matched a trailing param's type AND the arities collided, which they don't. The one shape test that pins component count (`EntityTest.exactlyElevenFields` → `exactlyTwelveFields`) is the only existing test that MUST change — grep `getRecordComponents()` repo-wide first (here only `EntityTest:82` hit for these two records; `ProposedEntity` had no shape test, so a new `ProposedEntityTest` was created).
**Detail:** Guard order in the compact ctor is **coerce → validate → copy**: `if (role == null) role = DEFAULT;` BEFORE `if (!type.allows(role)) throw IAE;` BEFORE `field = List.copyOf(field);`. This ordering is load-bearing — a `null` role on a type that permits only the default (PLUG → {PRIMARY}) must coerce and pass, not NPE inside `allows(...)`. And because the default (PRIMARY) is permitted by EVERY enum constant, the back-compat ctor (which passes the default explicitly) can never construct an illegal pair, so the new construction-time guard adds ZERO breakage to the existing corpus. Deliberately NOT adding a guard for a field that was never guarded before (here `entityType`) keeps the diff minimal (it NPEs via the `allows(...)` call — documented in a code comment, not "fixed"). The compile-time legality matrix is encoded on the partner enum: each `EntityType` constant declares `EnumSet.of(...)` through an `EntityType(Set<EntityRole>)` ctor that stores `Set.copyOf(...)` — `EnumSet.of` is MUTABLE, so storing it raw would make `legalRoles()` mutable and fail the immutability test; `Set.copyOf` is mandatory. Adding a new enum constant then forces the author to declare its legal-role set (the compiler requires the ctor arg) — the constraint can't be forgotten. Note the AMD's illustrative enum-constant ORDER differed from the source order; the matrix CONTENT (the role sets) is authoritative, the ORDER stays as-is in source (pinned by a `containsExactly` test) — take data from a spec, not incidental ordering.
**Impact:** (1) To add a component to a record with many construction sites, ship a convenience ctor of the OLD arity defaulting the new field — existing callers (even cross-module testFixtures you're told not to edit) keep compiling; only the component-count shape test changes. (2) Grep `getRecordComponents()` repo-wide before editing — it's the one assertion that breaks the instant a component lands (Interface Evolution Checklist). (3) Compact-ctor order is coerce → validate → copy; a null that coerces to a universally-legal default must pass the validate step, which is why "every type permits the default" is the property that makes the change zero-breakage. (4) Encode a compile-time legality matrix on the enum via a ctor arg (forces declaration on every new constant) and store it `Set.copyOf(EnumSet.of(...))` — never retain the mutable `EnumSet`. (5) When a spec's code block lists constants in a different order than the source, the content governs, not the ordering — reordering source constants is a contract change.

## 2026-06-05 | Category: build | Source: M4.C (AMD-54..64) — the actual Spotless config enforces only header + removeUnusedImports + trailing-ws + eof, NOT import-order or indentation
**Discovery:** `build-logic/src/main/kotlin/homesynapse.java-conventions.gradle.kts` declares `spotless { java { licenseHeader(...); removeUnusedImports(); trimTrailingWhitespace(); endWithNewline() } }` — and nothing else. There is NO `googleJavaFormat()`/`palantirJavaFormat()`/`importOrder()`. So Spotless does NOT reformat indentation and does NOT sort imports — but `removeUnusedImports()` fails `check` on ANY unused import.
**Detail:** Two files in the touched set (`EventTypes.java`, `IntegrationEvents.java`) are TAB-indented while the rest of the codebase is 4-space; `TestEventSamples.java` has `com.homesynapse.value.StringValue` interleaved mid-`com.homesynapse.event.*` (not alphabetical). Both pass `check` — direct proof Spotless isn't enforcing format/import-order. Consequences for an Edit-tool WU: (a) when editing a tab-indented file you must match tabs byte-for-byte — a one-shot `^\t` grep (count tab-led lines vs space-led) settles tabs-vs-spaces cheaply before the first Edit; (b) added imports can go in any position, but every one must be USED, and Javadoc `{@link Type}` counts as usage (google-java-format's `RemoveUnusedImports` respects Javadoc refs — that's why `IntegrationEvents` keeps `import …EventType` whose only use is a `{@link}`); (c) the copyright header must be byte-exact (`/*\n * HomeSynapse Core\n * Copyright (c) $YEAR NexSys. All rights reserved.\n */`).
**Impact:** Don't spend effort hand-sorting imports for Spotless in this repo — it doesn't sort. DO audit every added import for actual use (incl. Javadoc-only `{@link}`) before declaring done, and DO preserve the file's existing indentation char (tabs in `EventTypes`/`IntegrationEvents`, spaces elsewhere). Read the spotless block in the convention plugin once for an unfamiliar repo rather than assuming google-java-format — the enforcement set changes what "format-clean" means.

## 2026-06-05 | Category: serialization | Source: M4.C (AMD-59) — a persisted record that embeds an unregistered sealed-type field degrades SILENTLY; prove it from source, escape via a command-less instance
**Discovery:** A persisted event payload round-trips through `EventPayloadCodec` only if every sealed/abstract-typed field in its subtree has a (de)serializer in `PersistenceJacksonModule`. Jackson cannot construct a sealed-interface field with no codec — `decode()` catches the failure and returns a `DegradedEvent` (a silent lossy round-trip, NOT a thrown error). `CapabilityAdded` carries a full `CapabilityInstance` whose `commands → CommandDefinition → ExpectedOutcome.expectation` field is the sealed `Expectation` (device-model), which has no codec (only the 10 ULID wrappers + `AttributeValue` are registered).
**Detail:** Verified the defect WITHOUT running the build (Coder produces files only): (1) read `PersistenceJacksonModule` — `addSerializer/addDeserializer` cover ULIDs + `AttributeValue` only; (2) confirmed `ExpectedOutcome.expectation` is typed as the sealed `Expectation` (`permits ExactMatch, WithinTolerance, EnumTransition, AnyChange`); (3) ruled out a false alarm by grepping event-model for any existing event embedding `Capability`/`Expectation`/`CommandDefinition` — none (`DeviceAdoptedEvent` carries a single `Ulid`), so the codec has never been exercised on this subtree. The instruction flagged this exact case as STOP-and-report: do NOT add a Jackson annotation to the domain type (fails `NO_JACKSON_IN_DOMAIN_MODEL`) and do NOT silently write a custom serializer (a PM design call). The escape that keeps a milestone contract-only AND green: a **command-LESS** capability (`StandardCapabilities.occupancy()` — empty commands map) round-trips losslessly (its `AttributeSchema`/`ConfirmationPolicy` subtree is plain-Jackson; the abstract `Number` min/max fields are null and omitted by `NON_NULL`), so it backs the green codec test; the command-bearing case (`onOff()`) becomes a `@Disabled("AMD-65 pending …")` acceptance test naming the follow-up amendment.
**Impact:** (1) Before asserting a new persisted event round-trips, trace its payload for ANY sealed/abstract-typed field and check `PersistenceJacksonModule` registers it — an unregistered one degrades silently to `DegradedEvent`, so a naive `isEqualTo` assertion fails with a confusing "expected X but was DegradedEvent". (2) A serde defect is provable from source alone: registered-codec list + field types + "has any existing event ever serialized this subtree?" (a clean prior-usage grep rules out a false alarm). (3) When STOP-and-report fires on serde, the contract-only escape is a minimal/command-less instance for the green test + a `@Disabled` acceptance test naming the follow-up — never an annotation, never a silent custom serializer.

## 2026-06-05 | Category: build | Source: M4.C gate fix — adding event-type string constants OR event-record classes ripples to SEVEN downstream artifacts that the producer-only Files-to-Modify list omitted
**Discovery:** Adding 7 constants to `EventTypes` (46→53) and 7 records to the `IntegrationEvents` manifests (lifecycle 5→10 + a new `CAPABILITY_EVENT_CLASSES`, so `ALL_EVENTS` 27→34) compiled cleanly but failed `./gradlew check` at `core:event-model` — and because event-model is upstream of everything, the build aborted there and the integration/persistence suites never ran (their failures were hidden behind the first one). The M4.C instruction's "Files to Modify" listed the PRODUCER (`EventTypes.java`) but none of the consumers. The event-type set has **seven** lockstep sites:
1. `EventTypesTest` — hardcoded constant COUNT (`hasSize(46)`→53) AND a naming REGEX (`[a-z][a-z0-9_]*`) that rejects dot-namespaced values; widen to `[a-z][a-z0-9_]*(\.[a-z0-9_]+)*` for `integration.config.updated`/`capability.added` (AMD-58/59).
2. `AllEventClasses.ALL_EVENTS` (persistence test) — must concat the new manifest(s).
3. `EventCategoryMapping.TABLE` (persistence MAIN, not test) — a per-type category map sized to `ALL_EVENTS`; every new registered event needs an entry, or `explicitMappingCount()` mismatches. This is a real MAIN-source gap, not a count: the AMDs ratify event types but NOT their `EventCategory`, so the Coder picks sibling-consistent categories and flags `[REVIEW]` (categories are a derived runtime lookup, freely amendable).
4. `EventCategoryMappingTest` count (27→34).
5. `EventTypeRegistryTest` count (27→34).
6. `JacksonWarmupTest` count (27→34).
7. The two production AGGREGATION sites that build the real `EventTypeRegistry` from the per-module manifests: `HomeSynapseCore` (lifecycle, the composition root) and `IntegrationTestHarness` (integration-tests) — both 2-arg `Stream.concat(CORE, LIFECYCLE)`; a new manifest list (`CAPABILITY_EVENT_CLASSES`) must be added or the new events are unregistered in PRODUCTION (encode throws "unregistered event class"). AMD-59 §3 mandated this; nothing pinned its size, so it was a silent functional gap, not a test failure.
Plus the annotation tests (`*EventTypeAnnotationTest.EXPECTED_SUBTYPES`) which were in the instruction.
**Detail:** The discovery survey must, in the same pass that greps `getDeclaredMethods()` for interface-shape pins, ALSO grep the event-type set: `grep -rnE "isEqualTo\((N|M)\)|hasSize\((N|M)\)"` for the old counts, AND `grep -rn "LIFECYCLE_EVENT_CLASSES|CORE_PRODUCTION_EVENT_CLASSES|<new manifest>"` to find every aggregation site (test harnesses AND the production composition root). The category map is the easy-miss MAIN-source one — it's keyed by event-type string and sized to the registered set, so it's coupled even though it lives in a different module than the constants. An upstream-module failure ABORTS the whole gate (no `--continue`), so a single missed pin in `core:event-model` masks every downstream failure — fix the upstream-most one first, then re-run with `--continue`.
**Impact:** (1) Any instruction that adds an event-type constant or event-record class MUST enumerate all seven sites; if it doesn't, the Coder's discovery survey adds them by grepping the counts + the manifest names. (2) `core:event-model` is upstream of everything — a failing pin there hides all downstream failures; always re-run with `--continue` after fixing event-model. (3) The production composition root (`HomeSynapseCore`) and any test harness that builds a registry from the manifests are part of the lockstep — a new manifest list (not just more entries in an existing one) must be wired into every aggregation site or the events are silently unregistered at runtime. (4) `EventCategoryMapping.TABLE` (persistence main) is the easy-miss coupling — it's a per-registered-type map in a different module from the constants.

## 2026-06-06 | Category: module-boundaries | Source: M5-A SystemdHealthReporter — pure JDK 21 has no AF_UNIX datagram socket; isolate the unsupported transport behind a seam to keep the gate GREEN
**Discovery:** sd_notify uses an `AF_UNIX` `SOCK_DGRAM` socket, but OpenJDK ≤ 21 supports Unix-domain *stream* sockets only (JEP 380 delivered `SocketChannel`/`ServerSocketChannel`, never `DatagramChannel`). `DatagramChannel.open(StandardProtocolFamily.UNIX)` COMPILES but throws `UnsupportedOperationException` at runtime — and there is no pure-JDK way to bind such a socket for *receiving* either, so neither a working production transport nor a faithful real-socket unit test is possible without a native binding (JNR/JNA, not in the version catalog) or a `systemd-notify` subprocess.
**Detail:** The reporter's message protocol (`READY=1`/`WATCHDOG=1`/`STOPPING=1`/`STATUS=…`), READY-once (`AtomicBoolean` CAS), `ReentrantLock` thread-safety, and send-and-forget error handling are all fully testable; only the datagram send is not. Pattern: extract a package-private `NotifyTransport` seam (`void send(byte[])`), put the `DatagramChannel`-based `UnixDatagramTransport` behind it (correct API shape, runtime-UOE on stock JDK), and inject a *capturing* transport in tests. `./gradlew check` (compile + unit tests) stays GREEN because the real transport is never constructed in tests — the runtime gap is isolated to the composition root (M13), which is the only place that actually instantiates the reporter (with a real `$NOTIFY_SOCKET`). This is the correct STOP-and-report move: do NOT add a non-catalog dependency or a subprocess silently — flag it as a TECHNICAL PUSHBACK and let the PM choose the transport.
**Impact:** Any pure-JVM sd_notify / AF_UNIX-datagram work needs an explicit transport decision (native binding vs `systemd-notify` subprocess) BEFORE it can run on a real host — treat it as a design question, not an inline IO choice. The seam-isolation pattern (correct-shaped-but-runtime-unsupported prod impl behind a package-private interface + a capturing test double) keeps a build-deferred WU GREEN while honestly surfacing a runtime gap; reuse it whenever a JDK API compiles but isn't supported at runtime on the target platform. Because the failure is runtime-only, a "looks-done" impl can hide a non-functional deployment path — say so loudly in the handoff + a cross-agent note.

## 2026-06-06 | Category: build | Source: M5-A path-impl tests — `(Type) null` cast is redundant (and -Werror-fatal) unless it disambiguates overloads
**Discovery:** Under `-Xlint:all -Werror`, casting `null` to select a constructor/method only avoids a `[cast]` warning when there is a genuine overload ambiguity. `new LinuxSystemPaths((Path) null)` has just one single-arg ctor → the cast is redundant → `-Xlint:cast` flags it → `-Werror` fails the build. But `new SystemdHealthReporter((String) null)` has two reference-type ctors (`String` and the package-private `NotifyTransport`) → bare `null` is ambiguous → the cast is REQUIRED and is NOT flagged.
**Detail:** Decide the cast per overload-set: drop it for a single applicable overload (`new X(null)`); keep it only where two reference-typed overloads make a bare `null` ambiguous. The AssertJ `assertThatThrownBy(() -> new X(null))` form still type-checks even when the ctor declares `throws` (the checked exception is absorbed by `ThrowingCallable.call() throws Throwable`).
**Impact:** When writing null-rejection tests in this `-Werror` codebase, choose the cast by overload-set, not by habit — an unnecessary `(Type) null` is a build failure, not a style nit. This sits alongside the M2.9 `-Xlint:exports` and M4.C spotless lessons as a "the gate enforces more than it looks" build note.

## 2026-06-06 | Category: module-boundaries | Source: M5-A gate fix — `-Xlint:exports` -Werror-fatal when an exported public class exposes a supertype from a non-transitively-required module (3rd occurrence: M2.9, M3.6e.1, M5-A)
**Discovery:** `platform-systemd`'s new `module-info` did `requires com.homesynapse.platform;` (plain) while its four exported public classes (`LinuxSystemPaths`/`LocalPaths`/`SystemdHealthReporter`/`NoOpHealthReporter`) `implement` `PlatformPaths`/`HealthReporter` from that module. javac `-Xlint:exports` emits `interface … is not indirectly exported using 'requires transitive'` once per class; `-Werror` makes it fatal. Green unit tests never surface it — it is a pure main-source `compileJava` warning.
**Detail:** Rule — if a public type in an EXPORTED package exposes (as a supertype, return type, parameter, or field) a type from another module, that module must be `requires transitive`, and the Gradle dependency must be `api(...)` in lockstep (api ↔ requires transitive; implementation ↔ plain requires). Fix here: `requires com.homesynapse.platform` → `requires transitive`, and `implementation(project(":platform:platform-api"))` → `api(...)`. `org.slf4j` stayed plain/`implementation` — it appears only in method bodies, never on the public API, so it does NOT trip `-Xlint:exports`. This is the THIRD occurrence (M2.9 `SqlitePersistenceLifecycle`; M3.6e.1 the package-private-impl + public-gateway variant; now M5-A). It recurs specifically when a coding instruction hands a *verbatim* `module-info` for a NEW module that will export impls of a dependency's types and that embed is non-transitive — honoring it blindly reproduces the defect.
**Impact:** (1) When an instruction supplies a verbatim `module-info` for a new module whose exported public classes implement/return another module's types, treat a plain `requires` as suspect and promote to `requires transitive` + `api` (do NOT honor the non-transitive embed blindly — flag it as a `[REVIEW]` correction). (2) Pre-handoff self-check for every `-Werror`-sensitive module touched: for each public exported class, does its signature name a foreign-module type? If yes, that module needs `requires transitive`. A targeted `compileJava` on the touched module is the cheapest way to surface `-Xlint:exports` before handing back. (3) `-Xlint:exports` is main-source-only — passing tests do not clear it.

## 2026-06-07 | Category: module-boundaries | Source: M5-A Part 2 (AMD-87) — a non-transitive `requires X` compiles GREEN with NO direct Gradle dep when X is already on the compile classpath via another dependency's `api` (the lockstep's transitive exception)
**Discovery:** persistence's `module-info` gained `requires com.homesynapse.device` (non-transitive) with **no** matching `implementation(project(":core:device-model"))` in `build.gradle.kts` — and it compiles GREEN. device-model is already on persistence's compile classpath transitively: persistence `api(":core:state-store")` and state-store `api(":core:device-model")`, so Gradle's `api` propagation puts device-model's module on persistence's compile module path. The §13 lockstep ("plain `requires X` ⇔ `implementation(project)`") is really about ensuring X is *reachable*; when a transitive `api` chain already makes it reachable, the direct dep is redundant for a GREEN build (every OTHER `requires` in this module-info happens to be directly paired — this is the lone exception).
**Detail:** Verified from source before relying on it (Source Trust Hierarchy — actual `build.gradle.kts`/`module-info`, not summaries): state-store `module-info` `requires transitive com.homesynapse.device` (JPMS readability) + state-store `build.gradle.kts` `api(project(":core:device-model"))` (Gradle compile classpath). The instruction explicitly scoped `build.gradle.kts` out ("only the module-info changes"), and the PM's own note said "persistence already reads device transitively via `requires transitive state → transitive device`" — leaning on transitive availability was intended, not an oversight, so I did NOT add the direct dep; flagged `[INFO]` with the fragility: if a future refactor narrows state-store's `api(":core:device-model")` to `implementation`, persistence's compile breaks ("module com.homesynapse.device not found") and the direct dep must be added. SEPARATELY the persistence→device **moduleGraphAssert** STOP-gate is clean: root `build.gradle.kts` `allowed` lists `:core:.* -> :core:.*` (any core→core edge) and `restricted` has no `persistence -X> device`; the edge is acyclic (device requires value/event/platform only) and a *shortcut* over the existing persistence→state→device path so `maxHeight=7` is unchanged.
**Impact:** (1) A non-transitive `requires X` does NOT always need a direct `implementation(project)` — check whether X is already on the compile classpath via a transitive `api` chain before adding one (or before flagging the instruction as incomplete). (2) When you DO lean on transitive availability, record it (handoff `[INFO]` + a MODULE_CONTEXT gotcha) so a future narrowing of the intermediary's re-export prompts adding the direct dep. (3) Before claiming a new cross-layer JPMS edge is gate-safe, read the root `moduleGraphAssert` `allowed`/`restricted` arrays — core→core is explicitly allowed here, so the edge passes; a cycle is not the only failure mode (an edge-allowlist would be the other).

## 2026-06-07 | Category: serialization | Source: M5-A Part 2 (AMD-87) — a hand-rolled sealed-type codec delegates nested registered codecs via `provider.defaultSerializeValue` / `ctxt.readTreeAsValue`; closes the M4.C DegradedEvent gap
**Discovery:** The AMD-87 `Expectation` codec is the exact twin of the AMD-52 `AttributeValue` codec: keyed on the sealed **interface** in `PersistenceJacksonModule` (Jackson's `SimpleSerializers` walks a concrete permit's superclasses/interfaces, so all permits resolve through the one pair — never register per-permit), the serializer is an exhaustive `switch` with NO `default` (a 5th permit breaks compilation), the deserializer dispatches on a `String` tag with a rejecting `default` (open discriminator, unlike the serializer's total sealed switch). The two permits that wrap an `AttributeValue` (`ExactMatch`/`AnyChange`) REUSE the already-registered AttributeValue codec instead of re-encoding: serialize with `provider.defaultSerializeValue(value, gen)`, decode with `ctxt.readTreeAsValue(valueNode, AttributeValue.class)` (Jackson ≥2.13). The numeric permit (`WithinTolerance`) reuses the package-private `AttributeValueSerializer.writeDouble`/`AttributeValueDeserializer.readDouble` statics (same package) for AMD-52 bit-anchored-float + sentinel determinism — never re-implement `doubleToLongBits`/sentinels.
**Detail:** This resolves the M4.C `@Disabled("AMD-65 pending")` DegradedEvent gap (see the 2026-06-05 AMD-59 lesson): a command-bearing `CapabilityAdded`'s `CommandDefinition → ExpectedOutcome → Expectation(ExactMatch) → AttributeValue(BooleanValue)` subtree now round-trips because the one missing leaf (`Expectation`) got a codec that delegates its wrapped value back to the existing AttributeValue codec. Wire form: `{"t":"ExactMatch","v":{"t":"BOOLEAN","v":true}}`; `WithinTolerance` → `{"t":"WithinTolerance","target":<num-or-sentinel>,"tolerance":<num-or-sentinel>}` (the AMD-87 §2 `<bits>` notation is the bit-identity *guarantee*, realized by Schubfach round-trippable text + `"NaN"`/`"+Inf"`/`"-Inf"` sentinels — NOT a literal long; the instruction says reuse the helper, "do not re-invent the encoding"). Test edge: a record's auto-`equals` for `double` uses Double semantics (NaN==NaN true; -0.0≠+0.0), and the codec canonicalizes -0.0→+0.0, so assert `WithinTolerance` -0.0 round-trip by `Double.doubleToLongBits(decoded.target()) == doubleToLongBits(0.0)`, NOT `isEqualTo`. **Un-disable gotcha:** deleting the only `@Disabled` in a test file leaves `import org.junit.jupiter.api.Disabled;` unused → Spotless `removeUnusedImports` fails `check`; remove the import in the same edit (see the M4.C spotless lesson).
**Impact:** (1) To add a hand-rolled codec for a sealed type that mirrors an existing one, copy the precedent exactly — interface-keying, no-`default` serializer switch, `String`-tag deserializer WITH a `default`. (2) Delegate nested registered codecs, don't re-encode: `provider.defaultSerializeValue(value, gen)` (write) and `ctxt.readTreeAsValue(node, Target.class)` (read) route through whatever is registered for the runtime/target type in the SAME mapper. (3) Reuse package-private `static` float helpers across same-package codecs for determinism; never duplicate `doubleToLongBits`/sentinel logic. (4) When a `double`-bearing record round-trips through a -0.0→+0.0 canonicalizing codec, assert bit-identity via `doubleToLongBits`, not record `equals`. (5) Un-disabling a test = delete `@Disabled` AND its now-unused import.

## 2026-06-09 | Category: JPMS / contracts | Source: AMD-66–71 DOCS review E70-1 (PM-side design miss; Coder guard for M6.1/M6.4)
**Discovery:** A type that lives in a **base/shared module** (`event-model`, `value-model`, `platform-api`) may reference, in its fields/components, **only** types from that module, a leaf module, or `java.base`. Referencing a **higher-layer domain module's** type (`config`/`device`/`state`/`automation`/`integration`) inverts the JPMS edge and creates a cycle — `config requires transitive event`, so a config type inside a `com.homesynapse.event` record forces `event → config` (the AMD-52 `event↔device` class).
**Detail:** This bit AMD-70's config observability events at the design stage — caught by independent review and flattened before any code, so M6.1's `config.validation_completed` is `(int configSchemaMajor, int configSchemaMinor, int issueCount, Map<String,Integer> severityCounts)` and M6.4's `config.section_reloaded` is `(String sectionPath, int changeCount, int issueCount, String appliedClassification)` — all `java.base`/`String`, mirroring the existing all-`String` `ConfigChangedEvent`. The no-`default` interface-keyed codec pattern (AMD-52/87) works precisely because those payload types live in `value`/`event`, never above.
**Impact:** (1) Before writing **any record in a base module**, read its `module-info` and confirm every component type's module is that module, a leaf, or `java.base`. (2) If a coding instruction's event record names a higher-layer domain type in its payload, **STOP and flag it `[BLOCKING]`** — do not implement around it; the fix is flatten-to-`java.base`/`String` or route typed data through a leaf (`com.homesynapse.value`, e.g. `QuantityValue`). (3) Nick's standing directive (2026-06-09): always be mindful about contracts and JPMS.

## 2026-06-09 | Category: module-boundaries / build | Source: M6.1 — third-party libraries on the JPMS module path require `requires` directives regardless of Gradle scope
**Discovery:** Gradle `implementation(...)` scope does NOT exempt a library from JPMS: in this build every dependency that ships a `module-info.class` or an `Automatic-Module-Name` resolves on the **module path**, so a named HomeSynapse module cannot `import` it without a matching `requires` directive — "it's only an implementation dependency" is the `requires transitive`↔`api` lockstep's blind spot in reverse.
**Detail:** The M6.1 instruction mandated `config/configuration/module-info.java` unchanged ("the pipeline is package-private; the libraries are already on the `implementation` classpath") while requiring YAML/JSON-Schema implementation classes inside `com.homesynapse.config`. Jar inspection from the Gradle cache (`jar --file <jar> --describe-module --release N`) showed snakeyaml-engine 2.9 is the explicit module `org.snakeyaml.engine.v2` and json-schema-validator 1.5.6 is the explicit module `com.networknt.schema` (whose own `module-info` requires jackson-databind NON-transitively, so Jackson types don't flow through it). Precedent proving the module-path resolution: persistence's `module-info` declares `requires com.fasterxml.jackson.databind`/`requires org.slf4j` for its `implementation`-scoped libraries and is GREEN at `7f44bed`. A named module cannot read the unnamed module, so there is no compile path that honors both halves of the instruction — STOP-and-report fired (the instruction pre-authorized exactly this), M6.1 split at the Fork-2 seam, and the lib-free half (M6.1b) shipped while M6.1a awaits the ruling.
**Impact:** (1) When an instruction says "library X is available on the implementation classpath" for a JPMS module, verify the module name from the jar (`jar --describe-module`, with `--release` for multi-release jars) and check whether the embedded module-info permits the needed `requires` — BEFORE writing code against the library. (2) The minimal-directive recipe for implementation-only libraries: plain (non-transitive) `requires`, consumers package-private, which keeps `-Xlint:exports` silent and the public API clean. (3) An automatic/explicit module's own `requires` list determines what flows through it — check it (networknt does not re-export Jackson). (4) "No new requires" guarantees in AMD/instruction embeds should be scoped to HomeSynapse-module edges explicitly, or they falsify the moment an in-module implementation needs its first library.

## 2026-06-10 | Category: verification / build | Source: M6.1a — verify third-party API behavior from jar bytecode (`javap -c`) when the sandbox cannot execute probes
**Discovery:** The M6.1a validator mapping depended on an undocumented networknt-1.5.6 behavior: for `required`/`additionalProperties` violations, is `ValidationMessage.getInstanceLocation()` the containing object's path or does it already include the offending property? Getting it wrong double-appends or truncates every `ConfigIssue.path`. A throwaway runtime probe (`javac`+`java` in /tmp against the Gradle-cache jars) was permission-denied — the no-build discipline is enforced at the environment level, not just by convention. The answer was still obtainable statically: `javap -c -p` on `AdditionalPropertiesValidator`/`RequiredValidator` shows the message builder receiving `instanceLocation` from the **method parameter** (`aload 4`) while `property(...)` receives the iterated key (`aload 7`/`aload 11`) — so the message's instanceLocation is the containing object and the property must be appended by the caller. Signature-level checks (`javap` without `-c`) had already pinned the builder APIs, exception hierarchies (`DuplicateKeyException extends ConstructorException extends MarkedYamlEngineException`), and `LoadSettingsBuilder` methods.
**Detail:** Escalation ladder for pre-gate API verification in this no-execution sandbox: (1) `jar --describe-module --release N` for module names; (2) `javap -cp <jar>` for signatures/ctors/hierarchies (catches wrong method names, missing overloads, checked-vs-unchecked — most `-Werror` API risk); (3) `javap -c -p` dataflow reading for behavioral questions (which value flows into which call); (4) only then fall back to defensive both-ways coding (e.g. also reading `getArguments()[0]` when `getProperty()` is null) with the uncertainty documented. Also useful: grepping the disassembly for `invokevirtual` on a specific method name answers "does this class ever call X" without source access.
**Impact:** (1) Do not hand Nick a gate run to discover an API-shape error — `javap` against the Gradle-cache jar answers signature questions in seconds and `-c` answers many behavioral ones; cite "bytecode-verified" in the handoff so the PM knows the claim's basis. (2) A denied probe is a signal, not a dead end — the no-`javac` rule includes /tmp scratch probes; static inspection is the sanctioned alternative. (3) Keep the defensive fallback ONLY when static evidence is genuinely ambiguous; prefer deleting dead defensiveness once bytecode settles the question (the M6.1a `getArguments()` fallback was kept deliberately as cheap insurance against minor-version drift — documented in the validator).

## 2026-06-11 | Category: testing / filesystem | Source: M6.4 — portable atomic-write failure injection via a directory squatting on the temp-file name; multi-release module-info location
**Discovery:** The §6.8 "disk full / write failure leaves the prior file intact" contract and the REC-135 migration crash-window both need a failure injected INSIDE the atomic-write window without seams or mocks. Pre-creating a DIRECTORY at the exact temp-file name (`homesynapse.yaml.tmp`) makes `FileChannel.open(tmp, CREATE, WRITE, TRUNCATE_EXISTING)` throw on both NTFS (`AccessDeniedException`) and POSIX (`IsADirectoryException`) — a one-line, platform-portable injection that fails the flush AFTER any backup-copy step, exactly where the crash-window test wants it. No test seam enters production code; the production `catch` path (delete-tmp best-effort, prior file untouched, rethrow/WARN) is exercised for real.
**Detail:** Companion findings from the same WU: (1) `Files.move(tmp, target, ATOMIC_MOVE)` replaces an existing target on BOTH platforms (POSIX `rename(2)`; Windows `MoveFileEx(MOVEFILE_REPLACE_EXISTING)`) — no `REPLACE_EXISTING` needed, but if an exotic filesystem ever surfaces `AtomicMoveNotSupportedException` at the gate, adding the flag is the one-line fallback (documented in the handoff). (2) Directory fsync after rename is POSIX-only — `FileChannel.open(dir, READ)` throws on Windows; treat it as best-effort/DEBUG, never load-bearing. (3) Backup-file stamps in file NAMES must use ISO-8601 BASIC format (`yyyyMMdd'T'HHmmss'Z'`) — the extended form's colons are illegal in Windows filenames. (4) `jar --describe-module` can return EMPTY silently on a multi-release jar in this shell; the authoritative fallback is `unzip -l <jar> | grep module` then `javap` on the extracted `META-INF/versions/<N>/module-info.class` (snakeyaml-engine 2.9 keeps its module-info at `versions/11/`, which also definitively lists `exports` — how `…v2.common`/`FlowStyle` reachability was confirmed for the M6.4 Dump-side emission).
**Impact:** (1) For any future atomic-file-write test (M6.2 `secrets.enc` is next), use the tmp-directory-squat injection — no seams, portable, exercises the real failure path. (2) Windows-compatibility checklist for file artifacts: no colons in names, directory-fsync optional, mtime granularity coarse (force distinct mtimes with `Files.setLastModifiedTime`, never sleep). (3) When `--describe-module` is silent, go straight to the versioned module-info — it answers both the module-name AND the exports question in one javap.

## 2026-06-11 | Category: testing / change-impact | Source: M6.4 gate-fix round 1 — a publish-behavior change must sweep EVERY test class asserting publisher interaction counts, not just the class being edited
**Discovery:** M6.4's DP-10 made `load()` publish one `config_error` per ERROR issue alongside `config.validation_completed`. The delivery swept `StandardConfigurationServiceTest` (its `publishedEvent()` helper was made type-filtering and its draft-count assertions checked) — but `ConfigLayoutTest.composeAfterMerge`, an AMD-71 layout test whose fixture happens to carry one ERROR issue, also pinned `rootDrafts.hasSize(1)` and failed Nick's gate (169/1). The blast radius of "this operation now publishes more events" is every test that drives the operation through a recording publisher, regardless of what the test is nominally about.
**Detail:** The 30-second pre-handoff check that would have caught it: `grep -rn "rootDrafts\|chainedDrafts" <module>/src/test` and audit every count-bearing assertion (`hasSize`, positional `get(0).payload()` casts) against the new publish behavior. Positional casts are the stealthier twin — `(SomeEvent) rootDrafts.get(0).payload()` breaks by ClassCastException or silently grabs the wrong draft when event order or count changes; the fix idiom is type-filter-then-assert (select by payload type, order-agnostic, then assert exactly-one + fields), which also survives future additions with no ordering contract. Fix pattern when a pin goes stale by ruling: bump the pin WITH a dated comment naming the ruling (here: 1→2 at M6.4 per R1/DP-10) so the next reader knows it was deliberate, and keep the test's original subject assertions untouched.
**Impact:** (1) Any change that adds/removes/reorders published events: module-wide grep for the recording double's fields BEFORE handoff; audit counts AND positional accesses. (2) Prefer type-filtered exactly-one assertions over positional casts in new tests (the M6.4 suites already do this — only the older test broke). (3) A fixture chosen to exercise feature A (compose-after-merge) can silently also exercise feature B (ERROR publication) — "what does this fixture's data trigger?" is part of the sweep, not just "what does this test claim to test?"

## 2026-06-13 | Category: module-boundaries | Source: M6.3 (at-rest payload encryption) — consuming a consumer-defined seam across a no-edge boundary: mirror the small mapping as a java.base value, gate on injection-presence

When module A (persistence) defines a seam interface and module B (config) supplies the impl, but neither may `requires` the other (the Doc 15 §3.8 / AMD-52 cycle class), the *data* that crosses the seam must be `java.base`-only. M6.3 needed an event→encryption-scope mapping whose canonical owner is config's `EncryptionScope` enum — but persistence resolves the scope on the write path and cannot import it. Resolution: persistence mirrors the **one** MVP edge (`PRESENCE → "presence_personal"`) as a `private static final String` + a `Set<String>` enabled set, documented as a deliberate mirror and pinned by a test — exactly the discipline that already keeps `EncryptedPayload` distinct from config's shape-identical `ScopeCipherResult`. **Also:** when a config flag (here `at_rest_enabled`) isn't wired yet because live `ConfigModel` plumbing is a later WU, model the switch as *injection-presence* — the factory enables the default scope set iff a non-null cipher was injected. This keeps the no-crypto path (null cipher ⇒ empty set ⇒ plaintext-for-all) the M6.2 production state, makes existing tests pass unchanged, and confines the fail-closed throw to a defensive store-level guard you can test directly.

## 2026-06-13 | Category: testing / filesystem | Source: M6.3 — a foreign JDBC connection to a LIVE SQLite-WAL file blocks on Windows; raw-row assertions must read after the executor is shut down

To assert at-rest stored bytes (ciphertext `payload`, non-null `payload_iv`/`dek_ref`) you need raw column access, not the store's decrypting read path. But opening a second `DriverManager` connection to the same db file while the `DatabaseExecutor`'s connections are alive blocks / SQLITE_BUSYs on Windows (the `PersistenceFactoryAbandonTest` precedent: it reopens the file only *after* abandon). Discipline: do decrypting round-trips through the live store, then `dbExecutor.shutdown()`, then open the foreign read connection (`PRAGMA busy_timeout` set) — SQLite auto-recovers the WAL on the fresh open and sees all committed rows. **Also:** when the test module can't depend on the module owning the production cipher (persistence has no `config` test dep, and adding one would violate the "no new dependency" gate), model the production contract with a faithful file-backed test double — a `CountingPayloadCipher` with a durable per-(scope,version) counter (persist-before-return + re-init-on-reconstruction) reproduces the OR-M6-NONCE write-path property end-to-end, while the *real* manager's durability is proven by a unit test in its own module.

## 2026-06-13 | Category: anti-pattern / build | Source: M6.3 gate-fix round 1 — an LLM "compile-review" pass is NOT javac; never report compile-clean off a reasoning pass

A multi-agent adversarial review explicitly tasked with compile-correctness reported the persistence test sources "CLEAN", but `./gradlew check` then failed `compileTestJava` with 11 `unreported exception SequenceConflictException` errors. The seam: `EventPublisher.publishRoot`/`publish` declare the **checked** `SequenceConflictException` (`extends Exception`), and the new test methods called them directly without declaring/catching it. The review (and I) had verified the ONE call inside an `assertThatThrownBy(() -> ...)` lambda (absorbed by `ThrowingCallable.call() throws Throwable`) and generalized from it — missing that the *other* call sites propagate. **Lesson:** an LLM pass catches API-shape mismatches but is unreliable for the mechanical checks `javac` does instantly — checked-exception propagation, overload resolution, generic inference, definite assignment. When CLAUDE.md forbids running the build, hand work back as **compile-fix-applied / NOT gate-verified**, never "compile-clean", and audit EVERY call that crosses a checked-exception boundary (`publishRoot`/`publish`, anything `throws SQLException`/`IOException`, file I/O) by reading the callee's `throws` clause — don't extrapolate from one verified site. Also: `compileTestJava` aborts at the first failing module, so a green-looking run of *other* modules proves nothing about them — audit them too.

## 2026-06-13 | Category: persistence / build | Source: M6.3 gate-fix round 2 — a SQL migration's trailing inline comment becomes a comment-only "statement" that aborts the migration

`MigrationRunner.splitSqlStatements` splits a migration file on `;` (ignoring `;` inside `--` lines) and executes EVERY non-empty trimmed fragment, including the trailing `tail` after the last `;`. So a trailing inline comment — `ALTER TABLE ... ;   -- note` — leaves `-- note` as a standalone comment-only fragment after the final `;`, which sqlite-jdbc compiles to no statement and rejects with `The prepared statement has been finalized`, aborting the whole migration on every DB open. **Idiom (V001-V004, now V005):** keep ALL commentary in the file's header `--` block; statements stay clean; no trailing inline comments; ideally no `;` anywhere but the statement terminators. **Process meta-lesson reinforced:** this is the second M6.3 defect the deferred build gate caught that an LLM reasoning pass declared clean (round 1: a checked-exception propagation; round 2: this). When CLAUDE.md forbids running the build, hand back as fix-applied / NOT gate-verified — never "clean" — and remember a green compile says NOTHING about runtime (migrations, assertions, the crypto/counter logic only execute under `test`).

## 2026-06-19 | Category: planning / composition-root | Source: M7.1 — verify the runtime actually assembles a dependency before committing to a wiring deliverable

A detailed coding instruction can over-assume the runtime's assembled state. M7.1's instruction listed "wire the `automation_engine` subscriber into `HomeSynapseCore.start()`" + a mandated lifecycle wiring test — but a composition-root probe (does `start()` construct a `ConfigurationService`? an `EntityRegistry`/`DeviceRegistry`/`AreaRegistry`?) found it constructs NONE of them, and those registries have NO production implementations anywhere (Phase-2 interface-only). The wiring was physically blocked on app-bootstrap. **Lesson:** before accepting any composition-root/wiring deliverable, grep the composition root for the `new`/factory construction of EVERY dependency the new component needs; if a dependency isn't assembled in the runtime, the wiring is blocked — surface it as a scope finding (and to the user) rather than producing non-compiling stub wiring. The implementable core (impls + tests against injected/stubbed deps) is unaffected and ships; only the runtime `new` is blocked.

## 2026-06-19 | Category: JPMS / API hygiene | Source: M7.1 — package-private seams honor a FIX-07 `requires` re-add WITHOUT leaking the dependency onto the exported API

FIX-07 required re-adding `requires com.homesynapse.config` + `requires com.homesynapse.event.bus` (plain/`implementation`, NOT `requires transitive`/`api`) — but those edges are only legitimate if NO config/bus type appears on the exported package's public signatures (the `requires transitive`↔`api` authoring check). The clean resolution: put all config/bus references inside **package-private** classes (`AutomationConfigBridge`, `AutomationEngineSubscriber`). A package-private class's signatures are NOT part of the exported API even though the package is `exports`-ed, so the plain `requires` stays correct AND the `requires` is genuinely used (not dead). Bonus: these package-private classes ARE the composition-root wiring seams, so the FIX-07 re-add and the wiring seam are the same artifact. **Lesson:** when a base module must `requires` a dependency it uses only internally, confine the usage to package-private types to keep it off the exported API and preserve the plain-`requires`/`implementation` scope.

## 2026-06-19 | Category: build / -Werror | Source: M7.1 self-audit — redundant cast `(FloorId) null` and `-Xlint:serial` on a nested exception

Two mechanical `-Xlint:all -Werror` traps caught in self-review (the kind the deferred gate catches that an LLM pass misses): (1) `new Area(id, name, (FloorId) null, ...)` — casting `null` to the exact parameter type is a REDUNDANT cast → `-Xlint:cast` error; just pass `null`. (2) a `private static final class DefinitionException extends RuntimeException` needs `private static final long serialVersionUID = 1L` or `-Xlint:serial` fails the build (RuntimeException is Serializable). **Lesson:** before handoff, scan new code for (a) casts of `null`/literals to a declared param type, (b) any `extends` of a Serializable type (Exception/Throwable subclasses) without a `serialVersionUID`, (c) unused imports (Spotless `removeUnusedImports` FAILS `check`). Hand work back as compile-fix-applied / NOT gate-verified.

## 2026-06-19 | Category: JPMS / build gate | Source: M7.1 gate-fix — the §authoring exported-API check is necessary but NOT sufficient; `assertAllowedModuleDependencies` bans a forbidden LAYER edge at ANY scope (SUPERSEDES the same-day "package-private seams" lesson for the config case)

The M7.1 instruction's FIX-07 directive told the Coder to re-add `requires com.homesynapse.config` to `core:automation`, with the rationale "config types stay off the exported API → plain `requires`/`implementation` is fine." Both the Coder and a 4-agent adversarial review verified the §authoring check (no config type on any exported signature — TRUE, config was used only inside the package-private `AutomationConfigBridge`) and passed it. **The build gate then failed:** `assertAllowedModuleDependencies` (root `build.gradle.kts`) bans the `core:automation -> config:configuration` edge at EVERY scope — `api` AND `implementation` — because the allowed module graph encodes a LAYER rule ("Core depends only on platform + other core"; config→core is sanctioned, core→config is not). Package-private confinement does NOT help: the task operates on the Gradle dependency graph, not on exported-API surface. **Lesson:** before adding ANY `requires`/dependency to a base module, check TWO independent gates — (1) the `requires transitive`↔`api` exported-API §authoring check (does the type appear on an exported signature?), AND (2) `assertAllowedModuleDependencies` (is this `from->to` LAYER edge in the allowed list at all?). A `core->config` (or any backward/cross-layer) edge fails (2) regardless of (1). **Correction to the same-day "package-private seams" lesson:** that technique legitimately keeps a type off the exported API (so a plain `requires`/`implementation` is correct for an ALLOWED edge — e.g. the `event.bus` `core->core` seam, which stayed), but it CANNOT rescue a forbidden layer edge — the config seam had to MOVE OUT of `core:automation` entirely. The architecturally-correct home for composition-root glue (read `ConfigurationService.getCurrentModel().rawMap()` → `loader.load(...)`; `registerCoreSchema(...)`) is `lifecycle`/`app` (allowed `->core` and `->config`); keep any config-free constants (a JSON schema `String`) near the owning module. **Process meta-lesson reinforced (3rd time):** the deferred gate caught what an LLM pass declared clean — hand back as fix-applied / NOT gate-verified, and when reviewing a base-module dependency, the review brief MUST include the module-graph allow-list, not only the exported-API check.

## 2026-06-19 | Category: instruction-defect / spec-vs-source | Source: AB-3 — a coding instruction's example glue can be subtly wrong against the real loader/schema contract; verify the data path end-to-end

AB-3's §3b gave the glue verbatim as `schemaRegistry.registerCoreSchema(SECTION, JSON); LoadResult r = loader.load(getCurrentModel().rawMap());`. Two latent defects were caught only by reading the actual source end-to-end (both would have shipped a silently-broken automation path that no smoke test on an empty config would surface): (1) **Schema-registration ordering.** The automation schema is a CORE schema, and `ConfigurationService.load()` validates every section against the composed schema. Registering it AFTER `load()` (as the §3b ordering reads) means an `automation:` section fails validation (root `additionalProperties:false`) → FATAL boot. Doc 12 Phase 1 is explicitly "core-only schema composition" → register core schemas BEFORE `load()`; only integration schemas defer (to after Phase 6). (2) **Section nesting.** `AutomationSchema.SCHEMA_SECTION = "automation"` registers a config SECTION; `getCurrentModel().rawMap()` is the whole document keyed by section (`{automation: {automations: [...]}}`), but `AutomationDefinitionLoader.load(doc)` reads `doc.get("automations")`/`doc.get("schema_version")` at the TOP level. So `loader.load(rawMap())` finds NOTHING — every automation is silently ignored. The correct call passes the section content: `loader.load((Map) rawMap().get("automation"))`. **Lesson:** when an instruction hands you composition glue between two modules, trace the data shape through BOTH ends against source (what key does the consumer read? at what nesting does the producer place it? when does validation run relative to registration?) — a verbatim two-line snippet can be contract-wrong in a way that compiles, passes an empty-config boot, and silently drops the feature. Verify with a populated fixture, not just the zero-config path.

## 2026-06-19 | Category: instruction-defect / JPMS | Source: AB-3 — a public assembly seam returning a dependency's interface forces requires-transitive even when the concrete impl stays package-private

AB-3's instruction asserted the `core:automation` module-info was "UNCHANGED" and that a public seam "returns the Subscriber (keeps event.bus-coupled internals off the exported API)." Both halves are wrong together: a `public static Subscriber automationEngineSubscriber(...)` returning `com.homesynapse.event.bus.Subscriber` puts that INTERFACE on automation's exported API, so `-Xlint:exports -Werror` demands `requires transitive com.homesynapse.event.bus` (+ Gradle `api(...)`) — even though the concrete `AutomationEngineSubscriber` class stays package-private. Keeping the concrete class package-private hides the CLASS, not the INTERFACE return type. (Precedent: lifecycle's `HomeSynapseCore` public accessors return event/event.bus/state types, and lifecycle correctly uses `requires transitive` for those.) **Lesson:** "package-private concrete class" ≠ "dependency off the exported API." Any public method whose signature (return type OR param) names a type from module M forces `requires transitive M`. Before trusting an instruction's "module-info UNCHANGED" claim for a new public seam, check whether the seam's signature surfaces a foreign type — if so, the transitive bump (and the Gradle api↔requires-transitive lockstep) is mandatory.

## 2026-06-19 | Category: spec-discipline / fail-closed-semantics | Source: AB-3 WUCP Phase 2 (Nick) — "fail-closed per entry" ≠ "fail the whole boot"; a green test encoding the wrong contract is worse than red

AB-3 read SD-9 "fail-closed-at-load" + "Doc 12 §4 Automation = FATAL" and implemented it as **throw → fail the whole boot** on any malformed automation definition, with a wiring test asserting `start()` throws. The gate went GREEN — but the test encoded the WRONG contract. The `AutomationDefinitionLoader`'s ratified Javadoc is explicit: a bad definition is "rejected and surfaced (a LoadFailure the caller publishes as config_error); valid sibling definitions still load… never loaded into a silently-inert state." So "fail-closed" is **per-definition** (the bad rule fails SAFE = does not run), not "fail the process." And "Doc 12 §4 Automation = FATAL" governs subsystem-INIT failure (the engine can't start at all), NOT a per-entry config error. FATAL-boot-on-one-typo reintroduces the exact "one bad entry bricks the box" anti-pattern the loader was designed to avoid. **Fix:** the caller iterates `LoadResult.failures()`, publishes a `config_error` event + logs each, and boots with `LoadResult.loaded()` (the valid subset). **Lesson:** when a spec gives you a fail-closed/FATAL directive, pin DOWN the SCOPE before coding — per-record vs per-section vs per-subsystem-init are different failure domains. Re-read the producing component's ratified contract (the loader literally said "valid sibling definitions still load" — I'd read it and still over-applied it). And a passing test is not validation if it asserts the wrong behavior: a green test that locks in a contract violation is worse than a red one, because downstream WUs build on the wrong posture. The deferred gate (`assertAllowedModuleDependencies`, clock-injection, compile) is structurally blind to "correct-but-wrong-contract" — that is exactly what human WUCP Phase 2 review is for.

## 2026-06-20 | Category: anti-pattern | Source: AB-1 Seam-1 go-live (WS first-message auth)
**Discovery:** A "MODIFY the X handler" instruction can name a runtime that does not exist yet — verify the target is real at HEAD before treating it as wiring, not building.
**Detail:** AB-1 told the Coder to "MODIFY the WS upgrade/auth handler (first-message authenticate, 4403/4408)." But `api/websocket-api` is a Phase-2 scaffold: only interfaces + records, no `WebSocketHandler`/`MessageCodec`/`EventRelay`/`WebSocketLifecycle` impl, no `app.ws("/ws/v1", …)` wiring, and `api.ws`'s module-info has zero runtime deps. There was nothing to modify. The check is cheap: `grep -rn "app.ws(\|implements WebSocketHandler" api` + read the module-info (`requires transitive api.rest` only) + the MODULE_CONTEXT ("Scaffold"/"Consumers: None"). Building it would have meant the whole WS Phase-3 runtime + new module edges (io.javalin, jackson, event-bus, state into api.ws) — a separate WU, not "wiring existing pieces."
**Impact:** When an instruction says MODIFY a named runtime component, confirm it exists (grep for the impl + an `app.X(` registration + check the module-info has the runtime deps) during STOP-on-Mismatch — even when the gate table doesn't list it. If absent, STOP-and-report it as a blocking scope finding and build the separable parts (here AB-1 REST + AB-2 shipped; only the WS half was blocked). Mirrors the AB-3 "compiling ≠ correct — verify against the consumer's real contract" lesson, one level up: verify the consumer EXISTS.

## 2026-06-20 | Category: module-boundaries | Source: AB-1 Seam-1 go-live (start() now binds HTTP)
**Discovery:** Making a widely-called lifecycle method newly acquire a resource (here: `HomeSynapseCore.start()` binding the HTTP port behind auth, previously C1-closed) is a consumer/pin fan-out just like a signature change — every caller's downstream assumptions shift.
**Detail:** Flipping `start()` to open HTTP-behind-auth broke EVERY existing unauthenticated HTTP E2E test (`EndpointE2eIT` ×5, `CrashRecoveryHttpIT` ×2, `InFlightRequestShutdownIT`) — they now get 401. The fix threaded the first-run pairing token through `HomeSynapseE2eHarness.authToken()` (reads the `initial_api_token` artifact, stable across abandon→restart on the same config dir) and added `Authorization: Bearer` to every call site. Two more fan-out traps: (a) binding IPv4 loopback `127.0.0.1` means clients must connect to `127.0.0.1`, NOT `localhost` (which can resolve to IPv6 `::1` and miss an IPv4-only bind) — fixed `baseUri()`. (b) Fixed-port (7070) binding in many lifecycle unit tests is flaky; moved the smoke tests to the ephemeral `HomeSynapseConfig.testing()` port and kept only the dedicated auth test asserting the production default via a no-bind constant check.
**Impact:** When `start()`/a lifecycle method begins binding/owning a NEW resource, grep every caller (prod + test, all modules) and sweep for: (1) callers that assumed the resource was absent, (2) loopback IPv4-vs-`localhost`-IPv6 mismatches, (3) fixed-port multi-bind flakiness across tests. Also: throwing from a Javalin `before(*)` handler (not merely setting a status) is what halts the pipeline so an unauthenticated request never reaches the endpoint — pair it with an `app.exception(...)` handler that serializes the RFC 9457 body.

## 2026-06-21 | Category: anti-pattern | Source: M7.2a-1 (run lifecycle) — when a coding instruction's step-ordering contradicts a Locked MODULE_CONTEXT/design-doc contract, the LOCKED contract wins; deviate-with-pushback, don't follow the instruction literally
**Discovery:** The M7.2a-1 instruction's "Admission sequence (order matters)" put concurrency-mode enforcement at step 5 and the condition gate in the run body (step 6+, on the VT). But the automation MODULE_CONTEXT cross-module contract (and Doc 07 §3.6) state verbatim: *"Condition evaluation occurs at trigger time, **before mode enforcement** … a CONDITION_NOT_MET Run does NOT consume a concurrency mode slot."* The instruction's literal ordering admits the Run (consuming a slot) before conditions are evaluated, so a CONDITION_NOT_MET Run transiently holds a slot (released only on terminal) — which violates the contract in the concurrent case (a SINGLE automation whose conditions keep failing can still wrongly drop a concurrent trigger). The two cannot both hold: synchronous mode-rejection (initiateRun returns empty + run_skipped with NO automation_triggered) requires conditions to be evaluated synchronously BEFORE mode, so EVALUATING must run in `initiateRun`, not on the VT.
**Detail:** Resolution = honor the locked contract: evaluate the condition gate synchronously in `initiateRun` (it is a pure decision; the gate is a stub in this slice) BEFORE the mode-admission block; a false gate publishes triggered+completed(CONDITION_NOT_MET) and returns the RunId WITHOUT ever entering the active-run map (no slot, no VT). The VT runs only the RUNNING (action) phase. This passed all FSM tests including the explicit `conditionsFalse…noModeSlotConsumed` case AND is strictly more correct than the instruction (no transient slot even concurrently). The skill makes MODULE_CONTEXT the ground truth ("the behavioral promises method signatures don't express"); Phase-2/3 say "Locked behavioral contracts are authoritative." The instruction itself cited the contract and had a "Coder Pushback Welcome" section naming this exact seam — so this is deviate-and-flag, not silent. Flagged as `[REVIEW]` in handoff + a cross-agent note recommending ACCEPT.
**Impact:** Before implementing a step-ordered admission/lifecycle sequence from an instruction, cross-check it against the target module's MODULE_CONTEXT cross-module contracts AND the Locked design doc. If they conflict, implement the contract, prove it passes the instruction's own tests, and raise a `[REVIEW]`/TECHNICAL PUSHBACK — do not encode a known contract violation because "the instruction said so." A signature/param the instruction defines (here the `RunConditionGate(automation, context)` seam) usually does NOT constrain WHERE it is called, so honoring the contract is free of downstream rework.

## 2026-06-21 | Category: testing | Source: M7.2a-1 (run lifecycle) — deterministically observing a VT-per-Run FSM's async terminal events without banned time access, including RESTART-cancelled victims
**Discovery:** An FSM that runs each unit of work on its own virtual thread (LTD-01) and publishes its terminal event from that VT cannot be asserted right after the synchronous `initiate` call returns — the terminal publish is async. The clean, non-flaky await is `Thread.join(long)` on the live VTs (NOT a `Clock`/`Instant.now` deadline — those are banned by NO_DIRECT_TIME_ACCESS in non-whitelisted modules; `Thread.join`/`Thread.sleep` are NOT banned). Expose a package-private `awaitQuiescence(long)` test seam that joins them.
**Detail:** The trap: a RESTART-cancelled victim is removed from the active-run map synchronously (to free its slot) but its ABORTED terminal publish still happens later on its own VT. If `awaitQuiescence` only joined the *active* map, it would return before the victim published → flaky assertion. Fix: keep a separate `Set<Thread> liveRunThreads` (a legitimate production shutdown structure too — `close()` interrupts it) that a Run is added to at admission and removes itself from in a `finally` ONLY AFTER its terminal publish; `awaitQuiescence` joins that set. So `join()` returning guarantees the completed/aborted/cancelled/disabled events are observable. Pair it with a fake executor whose BLOCK mode `latch.await()`s and, on `InterruptedException`, restores the interrupt flag (`Thread.currentThread().interrupt()`) so the FSM's post-execute `isInterrupted()` check finalizes ABORTED — exactly the production cancellation contract. With a `Clock.fixed`, durations are 0 (fine; `durationMs >= 0` holds) and same-millisecond `UlidFactory.generate(clock)` still yields distinct monotonic RunIds.
**Impact:** For VT-per-task FSM tests: (1) await via `Thread.join`, never a clock deadline (NO_DIRECT_TIME_ACCESS); (2) track ALL live task threads (incl. those removed from the "active" accounting map but still finalizing) and remove-after-terminal-publish so join is a true barrier; (3) model cancellation in the fake by restoring the interrupt flag, not by throwing; (4) the synchronous admission-side outcomes (empty + run_skipped/cancelled, slot accounting) are assertable immediately — only the on-VT terminal needs the await.

## 2026-06-21 | Category: architecture | Source: M7.2a-2 (execution/dispatch) — a seam that publishes a causally-correct event needs the triggering envelope, not just the RunContext; widen the seam, don't fatten the fixed record
**Discovery:** AMD-92 §2.4 requires every run/diagnostic event to publish on the *triggering event's* `CausalContext` (`chain(correlationId, causationId)`) with `actorRef = AutomationId` and inherited/null `eventTime`. The M7.2a-2 seams that emit those events — the `RunConditionGate` (row 4) and `ActionExecutor` (rows 5/6) — were specified taking only `(…, RunContext)`. But `RunContext` carries `triggeringEventId` (→ causationId) and `automationId` (→ actorRef) and NOT the chain's `correlationId` or `eventTime`. So a seam given only the RunContext **cannot** construct the correct CausalContext. `RunContext`'s shape is frozen (a STOP-gate), so you can't add fields. Two clean exits: widen the seam to also receive the triggering `EventEnvelope`, or have the FSM (which has the envelope) do the publish. The instruction's own test ("the GATE emits row 4 with the correct CausalContext") forced the gate-publishes design → widen the gate.
**Detail:** Result: `RunConditionGate.conditionsHold` → `(AutomationDefinition, RunContext, EventEnvelope, StateSnapshot)` and `ActionExecutor.execute` → `ActionExecutionResult execute(List, RunContext, EventEnvelope)`. The `EventEnvelope`/`StateSnapshot` params introduce NO JPMS edge (both already `requires transitive` on the automation API; `ConditionEvaluator`/`RunManager` already expose them), so module-info stays UNCHANGED and the public seams don't leak a hidden type. Bonus: passing the `StateSnapshot` lets the FSM capture ONE trigger-time snapshot (AMD-03), record its `viewPosition` into `RunContext`, AND hand the same snapshot to the gate — a single source of truth instead of two `getSnapshot()` calls that could straddle a projection tick. The "the call site is fixed" STOP-gate note constrained the ORDERING (condition-before-mode), not the signature — widening params/returns is fair game when the contract (correct causal stamping / a real tally) demands it; flag it `[REVIEW]` (the instruction's "Coder Pushback Welcome" named these seams).
**Impact:** When a seam must emit an event that has to land in a causal chain, audit what the seam can actually SEE before accepting its signature: identity (actorRef), causation (the causing event id), correlation (chain root), and eventTime each have a distinct source, and a flattened context record (RunContext) usually has only the first two. Either route the publish through the component that holds the causing `EventEnvelope`, or pass the envelope into the seam — never reconstruct a half-correct CausalContext from a record that lacks the root. Cross-check the seam's params against the publish helper's needs at design time, not after the gate fails.

## 2026-06-21 | Category: concurrency | Source: M7.2a-2 (execution/dispatch) — QUEUED single-flight drain on a VT-per-Run FSM: enqueue-then-drain-on-finalize, and a join-one-at-a-time quiescence seam
**Discovery:** M7.2a-1 shipped QUEUED admission-equivalent to PARALLEL (drop `queue_full` at `maxConcurrent`) because a stub executor couldn't observe sequencing. The real contract is a *sequential single-flight drain*: at `maxConcurrent` a QUEUED Run **enqueues** (per-automation FIFO) and drains one-at-a-time as active Runs free slots. The clean implementation on a `ReentrantLock`-guarded, VT-per-Run FSM: split QUEUED from PARALLEL in `decide()` (QUEUED → an `enqueue` decision, not `queue_full`); on enqueue, build the `ActiveRun` (VT unstarted, status EVALUATING-pending, dedup already claimed, row 4 already published at trigger time) and park it in `Map<AutomationId, Deque<ActiveRun>>`; in `finalizeRun` (inside the lock, after freeing the completing Run's slot) call `drainNext(automationId)` which, if a slot is now free, dequeues one and registers its VT — then publish its `automation_triggered` + `vt.start()` OUTSIDE the lock. Conditions are evaluated at trigger time (AMD-03) before enqueue, so a queued Run is already condition-passed; only triggered+execution defer to drain.
**Detail:** Test trap: the existing `awaitQuiescence` snapshotted the live-VT set ONCE and joined it — but a completing Run drains a NEW VT during its own finalize, so the snapshot misses the drained Runs and the await returns early. Fix: `awaitQuiescence` joins ONE live thread at a time in a loop (`for (vt = anyLive(); vt != null; vt = anyLive())`), re-reading the live set after each join, and breaks if a join times out (the VT is still live) to avoid spinning. Because `finalizeRun` registers + starts the drained VT BEFORE the completing VT's `finally` removes itself from the live set, the drained VT is observable the moment the join returns. The sequencing assertion: a recording executor appends `context.runId()` on entry; with `maxConcurrent=1` the order is deterministic FIFO `[R1,R2,R3]` and `activeRunCount==1` throughout — proving single-flight, not just "they all ran." Per-automation FIFO is the right granularity (C3 priority/automationId ordering applies to a same-event BATCH at the subscriber, not to one automation's time-spread queue).
**Impact:** For a per-key single-flight queue on a VT-per-task FSM: (1) make the queue admission a first-class `decide()` outcome distinct from drop; (2) drain inside the terminal-transition critical section but start the drained task's thread + publish outside the lock (publish-outside-lock discipline); (3) the quiescence/await seam must tolerate the live set GROWING mid-drain — join iteratively, not over a stale snapshot; (4) assert ORDER + concurrent-count, not just completion, or the test can't tell single-flight from parallel.

## 2026-06-21 | Category: java | Source: M7.2a-2 gate-fix Round 1 — a record component named X forbids ANY method named X() in that record (static factory collision); the compile gate caught what the LLM read-pass missed
**Discovery:** A Java `record` auto-generates a `public` accessor `X()` for each component `X`. Declaring any other method named `X()` in the same record — most insidiously a `static` factory — is a **compile error** ("invalid accessor method in record … must be public" on the factory; "must match the type of record component" when the return type differs). Worse, at call sites the unqualified `instance.X()` can bind to the wrong member: `record ValidationResult(boolean valid, …)` with a `static ValidationResult valid()` made `if (!validation.valid())` resolve `valid()` to the *factory* (returns ValidationResult) → "bad operand type ValidationResult for unary operator '!'". Two collisions slipped in this WU: a NEW `ValidationResult.valid()` factory vs the `valid` component, and a REGRESSION where the 2a-2 rewire added `ModeDecision.admit()/restartCancel()/enqueue()` factories to a record that already had `admit`/`restartCancel`/`enqueue` boolean components (2a-1 had built it via the canonical constructor — the factory "tidy-up" introduced the bug). **A 3-reviewer in-session adversarial LLM pass did NOT catch this; `:core:automation:compileJava` did (8 errors).**
**Detail:** Fixes (minimal): rename the no-arg factory to a non-component name (`valid()` → `accepted()`) and update the factory call sites — instance-accessor *reads* (`result.valid()`) stay and re-bind correctly once the factory is gone; or drop the colliding factories entirely and build via `new Record(...)` at the call sites (the ModeDecision fix — reverts the gratuitous refactor). Factories whose name does NOT match a component are fine: `invalid(String)`, `skip(String)`, `succeeded(int,int)`, and even an instance no-arg `failed()` PLUS a static `failed(int,int,String)` (legal overloading) are all legal precisely because no component is named `invalid`/`skip`/`succeeded`/`failed`.
**Impact:** New STOP-check before handoff: for EVERY new/edited record, list its component names and confirm NO method (static factory or otherwise) shares a name with a component. LLM read-passes are blind to this binding rule — it must be a mechanical scan (or just the compile gate). Prefer the canonical constructor or a distinct verb (`accepted`/`of`/`from`) over a factory that shadows a boolean/`is`-style component. This is why the deferred build gate exists: `compileJava` is the source of truth, the adversarial LLM pass is a complement, never a substitute.
