<!--
file: context/lessons/coder-lessons.md
purpose: Append-only record of patterns, pitfalls, anti-patterns, and discoveries from coding sessions.
audience: Coder
update-cadence: append-only
state-type: history
status: CURRENT
last-verified: 2026-05-20 against commit 25bc23b
-->

# Coder Lessons Log

Append-only record of patterns, pitfalls, anti-patterns, and discoveries from coding sessions. Read this at session start. Append new entries at the end when discoveries are made.

Periodically, the most significant entries are promoted into the skill reference files (java-patterns.md, testing-standards.md, homesynapse-mental-model.md). Entries are never deleted from this file — it is the complete historical record.

## Entry Format

```
## YYYY-MM-DD | Category: [concurrency|persistence|serialization|testing|module-boundaries|build|anti-pattern|other] | Source: [what task]
**Discovery:** [one sentence summary]
**Detail:** [the specific issue and the correct approach]
**Impact:** [what this means for future work]
```

---

## 2026-04-10 | Category: testing | Source: M2.5 SqliteEventStore — `./gradlew check` failure on `NO_DIRECT_TIME_ACCESS`
**Discovery:** The `NO_DIRECT_TIME_ACCESS` ArchUnit rule fires on test classes in non-whitelisted packages — not just production code. Test code in `com.homesynapse.persistence.**` that calls `Clock.systemUTC()` or `System.nanoTime()` will fail `./gradlew :app:homesynapse-app:test` even though it compiles and runs locally.
**Detail:** `SqliteEventStoreTest` initially used `Clock.systemUTC()` in `resetStore()` and `System.nanoTime()` as a filename suffix for per-test database uniqueness. Both compiled fine and the test suite ran green in isolation. But `./gradlew check` failed on `HomeSynapseArchRulesTest > noDirectTimeAccess` because `HomeSynapseArchRules.noDirectTimeAccess` scans the ENTIRE classpath including test classes, and whitelists only `com.homesynapse.app..`, `com.homesynapse.platform..`, and `com.homesynapse.test..`. Test classes in `com.homesynapse.persistence` are not exempt. The fix: follow the `InMemoryEventStoreTest` convention — a static `FIXED_INSTANT = Instant.parse("2026-01-01T00:00:00Z")` + `Clock.fixed(FIXED_INSTANT, ZoneOffset.UTC)`. The production `UlidFactory` handles sub-millisecond monotonicity internally, so every `publish()` under a fixed clock still produces a distinct, strictly-increasing `eventId`. For per-test database uniqueness, JUnit 5 `@TempDir` already gives each test a fresh directory, so `tempDir.resolve("events.db")` with a stable filename is sufficient — no nanoTime suffix needed.
**Impact:** (1) ANY new test class in a non-whitelisted package (persistence, event-model, event-bus, device-model, state-store, etc.) must avoid `Clock.systemUTC()`, `Instant.now()`, `System.nanoTime()`, and `System.currentTimeMillis()`. Use an injected `Clock.fixed(...)` and delegate uniqueness to `@TempDir` or a deterministic counter. (2) Running `./gradlew :{module}:test` is NOT sufficient to validate compliance — the arch rules run in the app module's test task, so `./gradlew check` is the authoritative gate. (3) Whenever writing tests in a new package, grep `HomeSynapseArchRules` for the whitelist first — it's a fixed list and catching this at design time avoids a commit-blocking rework.

## 2026-04-10 | Category: testing | Source: M2.5 — JUnit 5 @BeforeEach parent-first ordering exploited for dependency wiring
**Discovery:** When a parent test class declares an abstract `resetStore()` hook called from its own `@BeforeEach`, subclasses should put ALL per-test initialization inside `resetStore()` itself — not in a subclass `@BeforeEach` — because JUnit 5 runs superclass `@BeforeEach` methods BEFORE subclass `@BeforeEach` methods.
**Detail:** `EventStoreContractTest` (the abstract parent) has a `@BeforeEach` that calls `resetStore()`. If `SqliteEventStoreTest` put its `DatabaseExecutor` + `SqliteEventStore` setup in a subclass `@BeforeEach`, the parent's `@BeforeEach` would run first, call `resetStore()`, which would see null fields and NPE. Putting everything in `resetStore()` — including opening the executor, building the codec, and constructing the store — runs the wiring at exactly the right time. JUnit 5's `@TempDir` is populated before ANY `@BeforeEach` runs, so `tempDir` is safe to reference from `resetStore()`. The `@AfterEach tearDown()` runs in reverse order (subclass first), so subclass cleanup works normally.
**Impact:** Any future contract-test subclass whose setup has a nontrivial dependency on a parent-supplied hook should use this pattern. Document the ordering exploit in a class-level Javadoc block so the next reader understands why setup lives in `resetStore()` rather than `@BeforeEach`.

## 2026-04-15 | Category: persistence | Source: M2.9 SqlitePersistenceLifecycle — synchronous start() vs CompletableFuture.runAsync()
**Discovery:** `PersistenceLifecycle.start()` returns `CompletableFuture<Void>` but the implementation must be synchronous — `CompletableFuture.runAsync()` would move `JacksonWarmup.warmup()` off the calling platform thread and re-expose the carrier-pinning vulnerability (LTD-19).
**Detail:** The PM's pseudocode suggested `CompletableFuture.runAsync(...)`, but `JacksonWarmup.warmup()` MUST run on a platform thread to populate Jackson's `synchronized` caches before any virtual thread touches the codec. The startup system (Doc 12 §3.4) calls `start()` from a platform thread, so running synchronously and returning `completedFuture(null)` or `failedFuture(e)` satisfies both the interface contract and the platform-thread requirement. This also simplifies the concurrency model: the `volatile started` flag needs only single-threaded write (in `start()` and `stop()`, which the startup system calls sequentially) with multi-threaded read (in store accessors called from virtual threads).
**Impact:** Any future refactor that makes `start()` async must ensure `JacksonWarmup` still runs on a platform thread — e.g., by using a platform-thread executor or splitting the warmup into a pre-async phase. The current synchronous approach is the simplest correct solution.

## 2026-04-15 | Category: persistence | Source: M2.9 — DatabaseExecutor.start() takes 4 parameters, not just a Path
**Discovery:** `DatabaseExecutor.start()` requires `(Path dbPath, String migrationPath, List<String> migrationFiles, MigrationConfig config)` — the migration infrastructure parameters are mandatory even though the lifecycle could theoretically derive them.
**Detail:** The PM's brief speculated `DatabaseExecutor.start(databasePath)` as a 1-parameter call. The actual signature has 4 parameters because `DatabaseExecutor` delegates to `MigrationRunner` internally and needs the migration manifest (per LTD-07's "explicit ordered list" rule). The lifecycle stores the migration path and file list as `private static final` constants and passes `MigrationConfig.freshInstall()` — the only valid config for M2's single-database, no-upgrade-path scenario.
**Impact:** Future milestones adding a second database (telemetry) will construct a second `DatabaseExecutor` with its own migration path/files. The lifecycle will need two executor instances with separate migration manifests. The 4-parameter design is correct — do not attempt to "simplify" it.

## 2026-04-15 | Category: module-boundaries | Source: M2.9 build gate — JPMS `-Xlint:exports` on public class in exported package
**Discovery:** A `public` class in an exported JPMS package must not expose package-private types or types from non-transitive `requires` modules in any public method signature — including constructor parameters and return types. Under `-Werror`, each violation becomes a fatal error.
**Detail:** `SqlitePersistenceLifecycle` was declared `public final class` in the exported `com.homesynapse.persistence` package. This triggered 5 `-Xlint:exports` warnings: (1) the constructor parameter `List<Class<? extends DomainEvent>>` leaked `DomainEvent` from the non-transitive `requires com.homesynapse.event` — clients of persistence don't automatically get access to event-model types; (2–5) four store accessors (`eventStore()`, `checkpointStore()`, `viewCheckpointStore()`, `atomicCheckpointWriter()`) returned package-private implementation classes that clients of the module cannot reference. The fix was to make the class package-private (`final class` instead of `public final class`), which is consistent with every other Phase 3 implementation class in the module. External modules interact through the `PersistenceLifecycle` interface.
**Impact:** Every new Phase 3 implementation class in an exported package should default to package-private visibility. Only promote to `public` if the class must be referenced by name from another module AND all its public method signatures use only (a) types from the same module's public API, (b) types from `requires transitive` modules, or (c) JDK types. When in doubt, grep for the existing pattern: `final class` (not `public final class`) for `SqliteEventStore`, `SqliteCheckpointStore`, `DatabaseExecutor`, etc.

## 2026-04-15 | Category: anti-pattern | Source: M2.9 build gate — speculative API calls without source verification
**Discovery:** Writing method calls from memory without reading the actual source file caused 3 compilation errors and 1 Spotless violation in a single build gate run.
**Detail:** Three wrong calls in `SqlitePersistenceLifecycleTest`: (1) `EntityId.generate()` — does not exist; `EntityId` has `of(Ulid)` and `parse(String)` factories, and the correct pattern from `EventStoreContractTest` line 146 is `new EntityId(UlidFactory.generate(clock))`; (2) `SubjectRef.forEntity(entityId)` — does not exist; `SubjectRef` uses bare noun factories: `entity()`, `device()`, `integration()`, etc.; (3) missing `import java.util.concurrent.CompletableFuture` for the `CompletableFuture<Void>` local variable in test 6. Additionally, the speculative imports for `Ulid` and `UlidFactory` were present but unused (because the code called `EntityId.generate()` instead of `new EntityId(UlidFactory.generate(...))`), so Spotless flagged them as unused imports.
**Impact:** Before writing ANY method call on a type from another class/module, read the actual source file or at minimum grep for the method signature. Especially: (1) static factory methods — project convention varies (`of()`, `parse()`, `entity()`, `create()`, constructor); (2) checked exceptions — always check the `throws` clause of any interface method before calling it; (3) test helpers that call interface methods inherit the throws obligation. The 30 seconds spent reading the source file saves 5+ minutes of build-fix-rebuild cycles.

## 2026-04-15 | Category: testing | Source: M2.9 build gate — uncaught `SequenceConflictException` on `publishRoot()`
**Discovery:** `EventPublisher.publishRoot(EventDraft)` declares `throws SequenceConflictException` (checked). Any test method or helper that calls `publishRoot()` must declare this exception or the compiler rejects the code.
**Detail:** The test helper `publishTestEvent(SqlitePersistenceLifecycle)` called `lc.eventStore().publishRoot(draft)` without declaring `throws SequenceConflictException`. All 5 test methods calling the helper also lacked `throws` clauses. The existing `EventStoreContractTest` already establishes the convention — every test method that calls `publishRoot()` declares `throws SequenceConflictException`. A quick grep for `throws SequenceConflictException` in the existing test fixtures would have revealed this at design time.
**Impact:** When writing new test code that calls any `EventPublisher` method (`publish()` or `publishRoot()`), always add `throws SequenceConflictException` to the method signature. This is a checked exception — the compiler enforces it, but discovering it at build-gate time wastes a round-trip. The same pattern applies to any future interface method that declares checked exceptions: read the interface `throws` clause before writing the call.

## 2026-05-02 | Category: persistence | Source: M2-bridge Tasks 1–3 — schema reservation column pattern for forward-compatible INSERT_SQL
**Discovery:** When a V001 schema amendment adds columns that the current implementation doesn't populate (reservation columns), the INSERT_SQL must include ALL columns and bind reservations to `setNull` with the correct JDBC type matching the DDL type (BLOB → `Types.BLOB`, TEXT → `Types.TEXT`, INTEGER → `Types.INTEGER`). This prevents NOT NULL constraint violations on columns like `home_id` and `payload_size` that DO have constraints.
**Detail:** The M2-bridge expansion took INSERT_SQL from 16 to 24 bind positions. The key insight is that column order in INSERT_SQL must match the DDL column order (minus AUTOINCREMENT columns), and every column that has a NOT NULL constraint must receive a real value, not `setNull`. `chain_hash` (NOT NULL DEFAULT zeroblob(32)) required `setBytes(ZERO_HASH)` — `setNull` would throw a constraint violation even though the DDL has a DEFAULT, because an explicit INSERT that names the column overrides the DEFAULT. `payload_size` was computed as `payloadBytes.length`. The remaining 5 reservation columns are nullable so `setNull` works.
**Impact:** When future amendments add NOT NULL columns to the events table, always verify: (1) is the column in INSERT_SQL? (2) does it have a NOT NULL constraint? (3) if yes, what value should be bound? Never assume `setNull` works on a NOT NULL column, even with a DDL DEFAULT, when the INSERT statement explicitly names that column.

## 2026-05-15 | Category: phase-2 | Source: M2→M3 Bridge work unit — sealed-interface package placement under JPMS
**Discovery:** Sealed interfaces and their permits in JPMS-compiled modules MUST live in the same package. A sealed interface `CheckpointPolicy` in `com.homesynapse.state` permitting `FixedCheckpointPolicy` and `AdaptiveCheckpointPolicy` requires both records to live in `com.homesynapse.state` — splitting them into a sub-package or sibling package causes a compile-time error ("sealed type and permit must be in same package") even when both packages are exported.
**Detail:** This work unit placed all 3 sealed-related files under `core/state-store/src/main/java/com/homesynapse/state/`. The `module-info.java` already `exports com.homesynapse.state` (no new export needed). The lesson generalizes: any future sealed hierarchy in HomeSynapse must group all permits with the sealed root in one package. If the hierarchy gets large enough to feel cluttered, the answer is to extract the entire hierarchy to a dedicated module rather than splitting permits across packages.
**Impact:** When designing a new sealed type in Phase 2, decide the package up front and verify every permit you anticipate fits the same package. If you find yourself wanting to split, the type is probably the wrong shape — either it should not be sealed (use `non-sealed` or an open interface) or it should be moved to its own module.

## 2026-05-15 | Category: phase-2 | Source: M2→M3 Bridge work unit — Phase 2 records may contain trivial implementation logic when logic IS the contract
**Discovery:** Phase 2 normally prohibits implementation code, but records whose entire purpose IS the trivial logic (e.g., `FixedCheckpointPolicy.shouldCheckpoint` is one OR comparison; `AdaptiveCheckpointPolicy.shouldCheckpoint` is one mode-switch delegation) are an exception. The decision logic IS the contract — there is no separate "implementation" to extract. PM approval of this exception was implicit in the brief, but it's worth documenting because the line between "contract" and "implementation" is fuzzy for policy-style records.
**Detail:** Rule of thumb: if extracting the logic to a separate implementation file would require either (a) renaming the record to a marker / config-only data carrier and exposing the logic through a separate concrete class that takes the record as a parameter, or (b) duplicating the logic in every consumer of the policy — then the logic belongs on the record. `FixedCheckpointPolicy.shouldCheckpoint` is in the first case: extracting it would mean creating `FixedCheckpointPolicyEvaluator` that takes the record. The record-and-evaluator split adds two types and a wiring step with zero benefit because the logic is trivial and the record is immutable.
**Impact:** When defining a Phase 2 sealed-interface implementor record where the implementor's body IS one trivial comparison or delegation, put the logic on the record and document it in the deviation report as `[INFO]`. Don't introduce a Strategy-pattern split that doubles the type count for no behavioral benefit.

## 2026-05-15 | Category: verification | Source: M2→M3 Bridge work unit — Python sqlite3 as cheap SQL syntax verifier when sqlite3 CLI is unavailable
**Discovery:** When the sandbox doesn't have the `sqlite3` command-line tool, Python's stdlib `sqlite3` module is a reliable syntactic and semantic check for SQL migrations. A 10-line Python script can apply V001 then V003 against an in-memory database, query `PRAGMA table_info` and `sqlite_master`, and assert the expected post-migration state (column count, NOT NULL flags, index presence, autoindex retention).
**Detail:** The script used in this work unit applied V001 to `sqlite3.connect(':memory:')`, then applied V003, then queried `PRAGMA table_info(snapshots)` (verified 9 NOT NULL columns), `sqlite_master WHERE type='index' AND tbl_name='events'` (verified `idx_events_subject` is dropped and `sqlite_autoindex_events_1` survives), and the snapshots-table indexes (verified `idx_snapshots_subject` is present alongside the implicit PRIMARY KEY autoindex). Total time: under 5 seconds, no JDBC, no Gradle.
**Impact:** Add this to the Phase 2 verification toolkit: when changing migration SQL and sqlite3 CLI is unavailable, run a Python sqlite3 simulation as part of the self-review checklist. It does not replace the full `./gradlew check` gate but catches obvious syntax errors and index-name mistakes before they reach the build gate.

## 2026-05-17 | Category: module-boundaries | Source: M3.1 InProcessEventBus — extending EventBus interface
**Discovery:** When extending an interface that has existing implementations, new methods must be `default`.
**Detail:** The EventBus interface gained 4 methods in M3.1 but `InMemoryEventBus implements EventBus` in testFixtures. Abstract methods would break `InMemoryEventBus` — it would need to implement all 4 new methods despite being a Phase 2 fixture that only supports the original 4-method contract. Solution: declare the new methods as `default` with `throw new UnsupportedOperationException("method requires active runtime")`. The production `InProcessEventBus` overrides all defaults with real implementations. This preserves backward compatibility while extending the contract.
**Impact:** Any future interface extension in HomeSynapse where the interface has existing implementations in testFixtures (which cannot be modified in the current milestone scope) must use `default` methods. The pattern generalizes: `default` methods are the interface evolution mechanism under JPMS when backward compatibility is required.

## 2026-05-17 | Category: testing | Source: M3.1 InProcessEventBus — contract test capability hooks
**Discovery:** When a contract test base class has existing concrete subclasses, new harness methods must have defaults.
**Detail:** `EventBusContractTest` is extended by both `InMemoryEventBusTest` (Phase 2, passive bus only) and `InProcessEventBusTest` (M3.1, full active runtime). Adding new abstract methods to `EventBusContractTest` (e.g., `createActiveRuntime()`) would break `InMemoryEventBusTest`. Solution: add a `boolean supportsActiveRuntime()` capability hook (default returns `false`) and guard all Tier 5-10 tests with `assumeTrue(supportsActiveRuntime())`. `InProcessEventBusTest` overrides to return `true`. `InMemoryEventBusTest` inherits the default and skips the new tiers automatically.
**Impact:** When adding tiers to any contract test base class that has multiple subclasses with different capability levels, use a `boolean` capability method with `assumeTrue()` guards. This prevents new abstract methods from breaking existing subclasses and explicitly documents which implementations support which test tiers.

## 2026-05-17 | Category: testing | Source: M3.1 InProcessEventBus — interface shape test maintenance
**Discovery:** Interface shape tests that assert method counts break when interfaces are extended.
**Detail:** `EventBusTest` (the interface shape test from M1.f) asserted "exactly 4 declared methods" on the EventBus interface. When M3.1 added 4 new default methods, this assertion became stale and would fail: the interface now has 8 declared methods (4 abstract + 4 default). The shape test must be updated whenever the interface is extended. This is easy to miss because the shape test lives in a different source set (test) from the contract test (testFixtures) and the production code (main).
**Impact:** When any Cowork prompt prescribes extending an interface, the prompt MUST include updating the interface shape test in its file list and STOP-on-Mismatch gates. The PM should include these tests in the "Files to Modify" table. Failure to do so causes a build-gate failure that requires a correction pass.

## 2026-05-17 | Category: concurrency | Source: M3.1 InProcessEventBus — virtual thread existence assertions
**Discovery:** `Thread.getAllStackTraces()` does not include virtual threads in Java 21.
**Detail:** An initial approach to verifying per-subscriber virtual thread creation used `Thread.getAllStackTraces().keySet().stream().filter(t -> t.getName().startsWith("hs-sub-"))` to assert VT existence. This returns an empty set because `Thread.getAllStackTraces()` only returns platform threads. Virtual threads are not enumerable through JDK APIs in Java 21. Solution: use bus introspection (`bus.subscribers()` returning `List<SubscriberSnapshot>`) to verify subscriber registration, and verify VT creation indirectly through behavioral assertions (subscriber receives events, mode transitions occur).
**Impact:** Never use `Thread.getAllStackTraces()` or `Thread.enumerate()` to assert virtual thread existence. Use application-level introspection APIs instead. If a test needs to verify VT creation, assert on the observable behavior that the VT enables (event delivery, mode transitions, etc.), not on thread enumeration.

## 2026-05-17 | Category: other | Source: M3.1 InProcessEventBus — supervisor backoff simplification
**Discovery:** Supervisor backoff was simplified in M3.1; full exponential backoff deferred to M3.2.
**Detail:** PLAN-M3-CONSOLIDATED-02 §4.4 specifies MIN=3s, MAX=30s, jitter=0.2 exponential backoff. M3.1 implements immediate retry (no backoff delay) because the full backoff sequence depends on the ReplayDriver integration (M3.2). The key M3.1 supervisor behaviors are: exception taxonomy (Error/IOException/checked → SUSPENDED; RuntimeException → retry), DLQ recording of failed events, rolling crash window (10 min, 5 crashes → SUSPENDED), and circuit breaker transition to SUSPENDED mode. The backoff timing is a separate concern layered in M3.2.
**Impact:** When implementing M3.2, the supervisor's retry path gains the exponential backoff with `Thread.sleep(backoffMs)` (safe on virtual threads ��� unmounts carrier). Tests that exercise retries should use `MutableClock` to control time rather than waiting for real sleep delays.

## 2026-05-17 | Category: module-boundaries | Source: M3.1 InProcessEventBus — JPMS enforces JDBC-free bus constraint
**Discovery:** JPMS enforces the JDBC-free bus constraint — no ArchUnit rule needed.
**Detail:** The bus module's `module-info.java` does not declare `requires java.sql`. This means any attempt to import `java.sql.Connection`, `java.sql.SQLException`, or any JDBC type in any file under `core/event-bus/src/` will fail at compile time with a module boundary error. The PLAN-M3-CONSOLIDATED-02 §4.5 references an ArchUnit rule `EVENT_BUS_DOES_NOT_IMPORT_SQLITE_DRIVER` in `HomeSynapseArchRules.java` — this rule does not exist and is not needed. The JPMS module system provides a stronger guarantee: compile-time enforcement rather than test-time enforcement.
**Impact:** Do not attempt to create an ArchUnit rule for JDBC-free enforcement in the bus module. JPMS already handles it. The `SubscriberReadConnectionFactory` and `SubscriberReadExecutor` interfaces are the mechanism for bus code to access database reads without importing `java.sql` — they abstract over the JDBC connection. When writing M3.5a, the state-store module (which DOES have `requires java.sql`) provides the `SubscriberReadConnectionFactory` implementation that creates real JDBC connections.

## 2026-05-17 | Category: other | Source: M3.1 InProcessEventBus — infrastructure exception carve-out uses only java.base types
**Discovery:** Infrastructure exception classification uses only `java.base` types since the bus cannot reference `java.sql.SQLException`.
**Detail:** The supervisor's exception taxonomy classifies exceptions for retry vs immediate suspension. Since `com.homesynapse.event.bus` does not `requires java.sql`, it cannot reference `SQLException` directly. The taxonomy uses only `java.base` types: `Error` → infrastructure (immediate SUSPENDED), `IOException` → infrastructure (immediate SUSPENDED), other checked `Exception` subclasses → infrastructure (immediate SUSPENDED), `RuntimeException` → callback fault (standard backoff/retry). This means if a `SubscriberReadExecutor` implementation wraps a `SQLException` in a `RuntimeException`, it will be treated as a callback fault (retried) rather than infrastructure (suspended). The executor implementations should wrap infrastructure failures in `IOException` or `UncheckedIOException` to get correct classification.
**Impact:** When implementing `SubscriberReadExecutor` in the persistence module (M3.5a scope), wrap `SQLException` in `UncheckedIOException` (not plain `RuntimeException`) so the supervisor correctly classifies database failures as infrastructure exceptions and suspends rather than retrying.

---

### 2026-05-17 — `jdk.jfr` and JDK Platform Modules Under JPMS (M3.3)

**Severity:** PM-originated error in task instruction.

**What happened:** The M3.3 task instruction stated "Do NOT add `requires jdk.jfr` to module-info.java — it compiles without it." This was wrong. The first `./gradlew :core:event-bus:check` failed because `jdk.jfr.Event` was inaccessible without an explicit `requires jdk.jfr;` directive. The Coder corrected it; the second pass compiled clean.

**Root cause:** The PM deliberation incorrectly equated "platform module" with "auto-available under JPMS." The error chain: PM deliberation made incorrect JPMS claim → reviewer didn't catch it → task instruction contained an explicit incorrect directive → Coder hit build failure → Coder corrected it.

**The correct rule:** "Platform module" means "ships with the JDK — no Maven/Gradle dependency needed." It does NOT mean "auto-required under JPMS." Only modules in the transitive closure of your declared `requires` directives are resolved. `java.base` is the only unconditionally available module. `jdk.jfr`, `java.sql`, `java.naming`, `java.desktop`, `jdk.management` all require explicit `requires` directives.

**How to check before claiming "no requires needed":**
1. Is the module `java.base`? → No `requires` needed.
2. Is the module transitively required by an existing `requires`? Trace the actual `module-info.java` chain. Example: `com.homesynapse.event.bus` → `requires transitive com.homesynapse.event` → that module's `module-info.java` → `requires transitive com.homesynapse.platform` → that requires nothing beyond `java.base`. So `jdk.jfr` is NOT in the transitive closure — explicit `requires jdk.jfr;` is mandatory.
3. If neither 1 nor 2: you need `requires <module>;`. No Gradle dependency is needed (ships with JDK), but the JPMS directive is required.

**Lesson:** When the PM or Coder makes a directive-level claim about JPMS module availability, verify it by tracing the `module-info.java` chain. Do not infer availability from "it's a JDK module."

---

## 2026-05-18 — M3.5a: Verify visibility modifiers against source, not documentation (G4)

**What happened:** The M3.5a task instruction specified `DerivedWriteRateLimit` as a public type that `StateProjection` would accept via constructor injection. STOP-on-Mismatch gate G4 expected it to be public. The actual source in event-bus showed `final class DerivedWriteRateLimit` (package-private — no `public` modifier), with package-private `acquire()` and `refill()` methods.

**Root cause:** The PM brief relied on the MODULE_CONTEXT's "10 public" type count and the PLAN-M3 §5.2 file list which prescribed `DerivedWriteRateLimit` as public in state-store. DEC-M3-15 moved it to event-bus and M3.3 implemented it, but as package-private. Neither the MODULE_CONTEXT nor the PLAN were updated to reflect the actual visibility.

**Resolution:** Claude Code introduced `DerivedPublishGate` (functional interface, single `acquire() throws InterruptedException` method + `unbounded()` static factory) in state-store as an adapter seam. StateProjection depends on DerivedPublishGate, not the bus's package-private type. Production wiring needs a small adapter (~5 lines) in the composition root.

**Lesson:** STOP-on-Mismatch gates should include an explicit visibility check (`public` vs package-private) for any type the brief asserts is cross-module accessible. "Type exists in module X" is not sufficient — "type is public in module X" is the correct gate. This mirrors the M3.3 jdk.jfr lesson: verify the actual source declaration, not the documentation's claims about it.

**Propagation:** This lesson should be added to: (1) nexsys-project-manager/references/coding-instruction-format.md §STOP-on-Mismatch Gates, (2) HomeSynapse_Knowledge_Primer.md Critical Gotchas, (3) HomeSynapse_Current_State.md §6 JPMS Lessons.

## 2026-05-19 | Category: testing | Source: M3.4b — ThrottledWriteCoordinator cross-module wiring
**Discovery:** When a testFixture in one module needs to be referenced from a test in a different module, the testFixture cannot be package-private — but its *factory* on a public harness can be. The natural placement of a test double is alongside the type it decorates (which may itself be package-private), so a "named convenience factory" on the public harness is the cleanest cross-module seam.
**Detail:** `ThrottledWriteCoordinator` had to live in `core/persistence/src/testFixtures/java/com/homesynapse/persistence/` so it could implement the package-private `WriteCoordinator` interface (mirroring `InMemoryWriteCoordinator`). The integration-tests module is in `com.homesynapse.it` and cannot reference `ThrottledWriteCoordinator` by name. Initial attempt: import the type into `IntegrationTestHarness` — failed (package-private). Resolution: add `PersistenceTestHarness.startThrottled(...)` as a named convenience factory that captures `ThrottledWriteCoordinator::withDefaults` internally. The IT harness calls the named factory by behavior, never mentions the implementation type.
**Impact:** The same pattern applies whenever a testFixture for an internal type needs cross-module reachability. Avoid promoting the internal type to public solely for test wiring — instead expose a named factory on a public harness. The same approach worked for `InProcessEventBusFactory.create(...)` in M3.4a.

## 2026-05-19 | Category: persistence | Source: M3.4b — WriteCoordinator decorator injection
**Discovery:** Widening a `final` field's type from a concrete class to its interface is the smallest possible change to enable test-only decorator injection without altering production behavior.
**Detail:** `DatabaseExecutor.writeCoordinator` was previously typed as `PlatformThreadWriteCoordinator` (concrete). M3.4b added a package-private 3-arg constructor accepting a `Function<WriteCoordinator, WriteCoordinator>` decorator. The implementation widens the field type to `WriteCoordinator` (interface) and applies the decorator after the `PlatformThreadWriteCoordinator` is constructed. Production wiring uses `Function.identity()` and the field still holds the bare `PlatformThreadWriteCoordinator` — zero behavioral change. The accessor `writeCoordinator()` already returned the interface type, so no caller change was needed. The decorator's `shutdown()` is required to forward to the underlying coordinator (the interface defines `shutdown()` for exactly this reason).
**Impact:** Reserve the "decorator-function parameter" pattern for clean test seams in production lifecycle code. The brief's recommendation of `Function<WriteCoordinator, WriteCoordinator>` over a boolean flag is correct — the function form generalizes to any future decorator (instrumentation, replay-tracing, etc.) without changing the signature again.

## 2026-05-19 | Category: testing | Source: M3.6a — constructor-signature ripple to peer test files
**Discovery:** When a production constructor signature changes (e.g., `int readThreadCount` → `DeploymentProfile profile`), the call-site fan-out almost always exceeds the "Files to Modify" table on the coding brief. Grep ALL `src/test/java` for the constructor name BEFORE editing — not after.
**Detail:** The M3.6a brief explicitly listed `DatabaseExecutorTest` and `SqlitePersistenceLifecycleTest`. Grep found 7 additional persistence test classes constructing `new DatabaseExecutor(READ_THREAD_COUNT, FIXED_CLOCK)`: `AtomicCheckpointWriterTest`, `AtomicCheckpointWriterDlqTest`, `SqliteCheckpointStoreTest`, `SqliteDeadLetterStoreContractTest`, `SqliteEventStoreTest`, `SqliteViewCheckpointStoreTest` (two call sites). Updating each was a 2-line edit (constant rename + constructor argument); finding all of them upfront prevented a multi-revision compile failure. The brief's "What to Watch Out For" §2 explicitly anticipated this — the lesson is to do the grep before any code is touched, not when the build fails.
**Impact:** Add to the pre-implementation checklist: for any constructor or method signature change in production code, run `grep -rn "new <ClassName>("` (or the equivalent ripgrep) across the entire `src/test/java` and `src/testFixtures/java` trees before the first edit. Capture the list. Updating call sites is mechanical; finding them late is what burns revision cycles.

## 2026-05-19 | Category: anti-pattern | Source: M3.6a — enum-bound validation cannot be triggered through enum surface
**Discovery:** Defensive bounds validation inside a constructor that consumes an enum value is uncoverable through normal test surface — you cannot construct a synthetic enum member to exercise the rejection path.
**Detail:** M3.6a's `DatabaseExecutor(DeploymentProfile, Clock)` constructor validates `profile.readThreadCount() ∈ [1, 8]` (LTD-03 reader-budget ceiling). All three current enum values (STUDIO=2, HOME=2, PERFORMANCE=4) satisfy the bound, so no test can trigger the `IllegalArgumentException`. An initial attempt to test it via a `DeploymentProfileStub` failed at compile (enums are sealed). The validation remains in the code as defense-in-depth — it will catch future enum values that exceed the LTD-03 ceiling — but the test cannot be written today.
**Impact:** Document the rationale on the validation block ("// LTD-03 reader-budget ceiling — defensive against future enum values"), do NOT delete the check just because it cannot be exercised, and do NOT add a synthetic enum to the production type solely for testability. The same pattern applies wherever a sealed/closed type carries a numeric tuning value with an architectural ceiling.

## 2026-05-20 | Category: module-boundaries | Source: M3.6c — pre-existing class with matching name on a CREATE instruction
**Discovery:** When a PM brief says CREATE but the target file already exists with a different purpose, the question to answer is "are the two roles semantically adjacent enough that they should live in one class?" — not "should I create a sibling?".
**Detail:** M3.6c instructed CREATE `core/event-model/src/main/java/com/homesynapse/event/EventTypes.java` with a `CORE_PRODUCTION_EVENT_CLASSES` constant. The file already existed (M2.1) as the holder for 46 `@EventType`-string constants. Both roles are "the canonical registry of core events" — strings for the annotation discriminators, classes for the registry construction. Merging them into one class (a) honored the design-doc intent of "single canonical place", (b) avoided introducing a new public type, and (c) matched the file path the brief explicitly specified. The alternative (sibling `EventClasses` class) would have split the canonical roster across two files and diverged further from the brief. Reported as `[REVIEW]` deviation so the PM can verify against the design doc.
**Impact:** When a CREATE instruction conflicts with existing code, treat it as a structural-merge decision, not a "create or escalate" coin flip. Always cite both the existing class's role and the new addition's role in the deviation report so the PM can confirm the merger preserves the design intent. The PM may still choose to split the class — but the default presumption when names match exactly is that the merger was intended.

## 2026-05-20 | Category: testing | Source: M3.6c — field names in test utilities should be preserved against brief
**Discovery:** When refactoring a test-only constant that has many callers in the same module, the field name in the brief is almost always illustrative — preserve the existing name and flag the divergence as `[INFO]`.
**Detail:** M3.6c's brief used `AllEventClasses.ALL` in its count assertions; the existing field was `AllEventClasses.ALL_EVENTS` with 6 caller sites across `EventTypeRegistryTest`, `EventPayloadCodecTest`, `JacksonWarmupTest`, `SqliteEventStoreTest`, `SqlitePersistenceLifecycleTest`, and a comment in `EventCategoryMappingTest`. Renaming would have forced 6 unrelated edits for zero semantic gain. Keeping the field name preserved the diff's blast radius to exactly the file the brief targeted. Same logic kept the auxiliary `CORE_EVENTS` and `INTEGRATION_EVENTS` aliases — independent callers exist for both.
**Impact:** When a brief specifies a constant or field name, check existing callers before renaming. The brief's name is descriptive intent (what the constant represents) — the codebase's name is the API contract. Diverging from the brief is an `[INFO]`-severity deviation when the only difference is naming and no caller breaks.

## 2026-05-20 | Category: module-boundaries | Source: M3.6d-a — pre-promotion visibility check must trace the type graph, not just the class
**Discovery:** A "promote class X to public" task is only safe when every type appearing in X's public method signatures (constructors, methods, fields) is itself already public — checking only X is insufficient.
**Detail:** M3.6d's brief asked to promote `QueueSaturationHealthCheck` to public with a "clean — no -Xlint:exports risk" annotation. Reading the source revealed the constructor's `Consumer<HealthSignal>` parameter carries a package-private record (`HealthSignal`), which in turn carries a package-private enum (`HealthLevel`). Promoting only the class declaration would have left two transitively-leaked package-private types in the public API surface; `-Xlint:exports` would have failed under `-Werror`. The minimum-viable promotion was a 3-type chain. Behaviour unchanged — pure visibility — but the scope was tripled.
**Impact:** Before promoting any type to public, build a small graph: enumerate every type appearing in the candidate's public constructors, method parameters, method return types, and public fields. Each non-public type in that graph must either also be promoted, or the candidate's signature must be refactored to use an interface/wrapper that hides it. For records and enums, follow nested-type chains (a record carrying an enum carries the enum's visibility too). Pattern formalised: pre-promotion grep targets are the candidate file + every file referenced from its public method signatures.

## 2026-05-20 | Category: other | Source: M3.6d split — sub-divide when prerequisites are missing rather than guess
**Discovery:** When a coding brief assumes infrastructure that does not exist in the codebase, sub-dividing the WU into prerequisite + dependent halves is better than either implementing the missing pieces ad-hoc or stopping completely.
**Detail:** The original M3.6d brief described `PersistenceFactory` + `HomeSynapseCore` wiring with 9 accessor methods on `PersistenceFactory` and a per-step `start()` sequence consuming the persistence layer's stores by interface type. Reading the codebase revealed: `SqlitePersistenceLifecycle` doesn't construct `SqliteStateStore` or `SqliteDeadLetterStore`, `WriteCoordinator` has no `queueSize()` method (the brief's `() -> factory.writeCoordinator().queueSize()` won't compile), no production `SubscriberReadConnectionFactory` exists (only the testFixtures stub), `HealthSignal`/`HealthLevel` are package-private, and `SubscriberInfo`'s real signature differs from the brief's snippet. The brief's "Files to Create or Modify" table grew from 18 entries to ~30 once gaps were enumerated. Rather than expanding scope unilaterally, the work was sub-divided via an explicit user decision: M3.6d-a delivers the 6 satellite changes that don't depend on the missing infrastructure (SqliteStateStore promotion, QueueSaturationHealthCheck+HealthSignal+HealthLevel promotion, ReadinessSource interface, ReconciliationTest, Tier 9 un-disable, HomeSynapseConfig/SharedScheduler/ThrowingStateQueryService skeletons). M3.6d-b will land the actual composition root after the prerequisite gaps are addressed.
**Impact:** When discovery reveals a brief's scope underestimates required changes by more than ~50%, surface it as an explicit user decision rather than silently expanding scope. Sub-dividing a WU keeps each PR small enough to review carefully, isolates the surprise from the user's perspective, and lets the prerequisite work get scrutinised independently. Practical signal: if the discovery-phase grep across the dependency chain returns more "missing accessor" / "missing method" findings than the brief's exception-table covers, escalate before writing the first production line. The Project Manager's brief is a specification of intent — when reality diverges, the brief needs revising, not the implementation.

## 2026-05-20 | Category: testing | Source: M3.6d-a — final-class collaborators need test-friendly secondary constructors
**Discovery:** When a production class takes `final` collaborators that cannot be mocked, add a package-private secondary constructor that takes a simpler, mockable form of the same dependencies; have the production constructor delegate.
**Detail:** `SharedScheduler`'s production constructor takes `(DerivedWriteRateLimit, QueueSaturationHealthCheck)`. Both collaborators are `final` classes (correctly so — they have invariant state). For unit testing, neither can be subclassed or instance-mocked without bytecode-rewriting frameworks (banned by HomeSynapse conventions). The clean pattern is a package-private secondary constructor `SharedScheduler(Runnable refillTask, Runnable tickTask)` that the production constructor delegates to via method references (`rateLimit::refill`, `healthCheck::tick`). Tests construct the scheduler with `() -> counter.incrementAndGet()` lambdas and observe the counter — no mocking framework, no abstraction added to the production type. The same pattern applies wherever a final collaborator's invocation is the test's observable signal.
**Impact:** When a brief specifies a constructor `(FinalTypeA, FinalTypeB)` but the test must observe the side effects of method calls on those types, add a package-private overload accepting `Runnable` / `Supplier<T>` / functional interfaces matching the methods to be invoked. Production callers use the original constructor unchanged; tests use the overload. The pattern preserves the brief's specified public API while making the class testable. Document the test-friendly form as `package-private` so it cannot be reached outside the test package.

## 2026-05-22 | Category: testing | Source: M3.6e.1 — narrow Responder SPI factor-out beats mocking a thick library Context
**Discovery:** When a production handler implements a third-party interface with dozens of methods (Javalin's `io.javalin.http.Context` has ~80), factor the response side into a narrow package-private SPI matching ONLY the methods the handler actually calls. Production wraps the thick library type in a 4-line adapter; tests drive the pure logic through a recording stub.
**Detail:** `ReadinessFilter implements io.javalin.http.Handler` and its `handle(Context)` body calls only `ctx.status(int)`, `ctx.header(String, String)`, and `ctx.json(Object)`. Mocking `Context` directly would require either implementing the full interface (~80 method stubs throwing UnsupportedOperationException) or pulling `javalin-testtools` into testImplementation (which spins up a real Jetty server per test). Neither is justified for a 30-line `before` handler. The clean pattern: extract a package-private `Responder` interface with the 3 methods, write a `ContextResponder` adapter (4 lines: pass-through to Context), and put the actual decision logic in a package-private `apply(Responder)` method. `handle(Context)` becomes one line: `apply(new ContextResponder(ctx))`. Tests verify `apply()` directly with a `RecordingResponder` that captures every call. End-to-end HTTP coverage with a real Jetty + client is deferred to integration tests at the next milestone, where it exercises the full request pipeline rather than just the handler.
**Impact:** Reserve this pattern for any handler/filter that implements a third-party interface where the production type is impractical to stub. Signal that the pattern applies: (1) the production interface has ≥10× more methods than the handler uses, (2) test-tools alternatives bring an external runtime (Jetty, Tomcat, Netty), (3) the handler's logic is pure enough that the response side is the only "boundary." The SPI must be **package-private** so external code never sees it; the adapter must be **private** so production wiring goes through one channel. Document the rationale on the SPI ("mirrors the subset of XYZ actually consumed by this handler") so future readers don't expand it speculatively. The M3.6e.1 brief explicitly invited this pushback under "Coder Pushback Welcome — Javalin's Context mocking is impractical for ReadinessFilterTest — propose an alternative test approach."

## 2026-05-22 | Category: module-boundaries | Source: M3.6e.1 — package-private impl + public static factory on the same-package interface
**Discovery:** When a production class must stay package-private (DEC-M3-16) but external modules need to construct it, add a `static` factory method on the public interface it implements. Java 8+ allows static methods on interfaces, and they can construct package-private types because they live in the same package.
**Detail:** `MaterializedStateQueryService` is package-private final in `com.homesynapse.state` per DEC-M3-16 (impl behind public interface). The lifecycle module's composition root cannot reference the concrete type. Solution: `StateQueryService.materialized(StateStore, ReadinessSource, LongSupplier, Clock)` — a public static factory method on the public interface, in the same package as the impl, so it can call `new MaterializedStateQueryService(...)` despite the package-private constructor. The composition root reaches the impl through the static factory and stores it as the interface type. This pattern is already used elsewhere in the codebase (`StateCheckpointSource.stub()`, `SubscriptionFilter.all()`) — adopting it for the construction path keeps the impl class invisible to consumers and the interface a single, complete API surface.
**Impact:** Whenever DEC-M3-16 forces a package-private impl behind a public interface, prefer a `static` factory method on the interface over a separate `XYZFactory` class. It (1) eliminates a public type, (2) keeps the construction API alongside the contract API in one file, (3) matches the existing HomeSynapse pattern. For the lifecycle module's composition-root caller, the interface-as-factory pattern requires zero extra imports — `StateQueryService` is already in scope through the existing `requires com.homesynapse.state`. The brief's example syntax (`new MaterializedStateQueryService(...)`) is incompatible with package-private visibility; the static factory is the cleanest resolution.

## 2026-05-22 | Category: module-boundaries | Source: M3.6e.1 XLINT_EXPORTS_FIX — implementing a non-transitive-dependency type forces package-private + public gateway
**Discovery:** When a class implements an interface or accepts a parameter type from a module declared as `requires X` (non-transitive), the class itself MUST be package-private. Promoting it to public forces either `requires transitive X` (often architecturally wrong) or a public gateway that hides X's types behind same-package wiring.
**Detail:** `ReadinessFilter` landed as `public final class ReadinessFilter implements io.javalin.http.Handler` with a public `handle(Context ctx)`. The rest-api module declares `requires io.javalin` (non-transitive — Javalin is implementation detail, not contract). Under `-Xlint:exports -Werror`, the compiler flagged BOTH `Handler` (in the implements clause) AND `Context` (in the public method signature) as leaking-through-public-API. The "easy" fix — `requires transitive io.javalin` — would have chained Javalin into the websocket-api → homesynapse-app dependency graph (websocket-api declares `requires transitive com.homesynapse.api.rest`, so anything transitive in rest-api propagates further). Javalin has no business in those modules. The correct fix: (1) make `ReadinessFilter` package-private, (2) create a public `RestFilters` utility class with a static `installReadinessGate(Object javalinApp, ReadinessSource readinessSource)` method, (3) type the `javalinApp` parameter as `Object` (not `Javalin`) to keep Javalin off the public API surface, casting internally. Callers in the lifecycle module pass their own `Javalin` instance — they already declare `requires io.javalin` independently, so the cast is safe at the call site.
**Impact:** Build a pre-write check: for any new production class in an `exports`-ed package, enumerate every type appearing in (a) the class's `implements`/`extends` clause and (b) every public/protected method signature (parameters, return types, thrown exceptions). For each external type, verify the providing module is either in the standard library or declared with `requires transitive` in the current module. If a type comes from a non-transitive `requires`, the class CANNOT be public — either make it package-private (preferred) or upgrade the requires to transitive (only if the type genuinely belongs in the module's public contract). The DEC-M3-16 pattern of "package-private impl + public utility/gateway" is the standard resolution. This mirrors the M3.6d-a lesson about HealthSignal/HealthLevel promotion chains — the type-graph check is the same; the only difference is whether the leaked type is internal-to-the-project or external-library.

**Corollary:** every `requires transitive` in `module-info.java` must have a corresponding `api()` (not `implementation()`) in `build.gradle.kts`. If the JPMS directive says transitive but Gradle says implementation, downstream modules get `module not found` at compile time. The two scopes have to agree: `requires transitive` = `api()`, `requires` (non-transitive) = `implementation()`. Discovered when `RestFilters.installReadinessGate(Object, ReadinessSource)` made `requires transitive com.homesynapse.state` correct in rest-api's `module-info.java`, but `build.gradle.kts` still had `implementation(project(":core:state-store"))` — websocket-api (which depends on rest-api) couldn't resolve `com.homesynapse.state` at compile time even though the JPMS directive said the dependency was re-exported. The Gradle scope governs the dependency *graph* visible to consumers; the JPMS directive governs the *module* visibility within that graph. Both must align in the same direction.

## 2026-05-22 | Category: module-boundaries | Source: M3.6e.1 — "or equivalents" in a STOP-on-Mismatch gate is a real invitation to push back
**Discovery:** When a coding brief's STOP-on-Mismatch gate uses softening language like "or equivalents" for a method that turns out not to exist, treat it as the PM explicitly inviting pushback — propose the equivalent and document it as a [REVIEW] deviation rather than a [BLOCKING] escalation.
**Detail:** M3.6e.1 gate G5 said: "StateStore.java — Interface — confirm it has get(EntityId), getAll(), and getViewPosition() methods (**or equivalents**)." Source read showed `StateStore` has `get`, `put`, `getAll`, `clear` — no `getViewPosition()`. The brief's instruction body then prescribed `stateStore.getViewPosition()` in the implementation, but the gate language ("or equivalents") and the Coder Pushback Welcome section already authorized a different shape. The clean equivalent: source the cursor from the State Projection via a `LongSupplier viewPosition` constructor parameter — the composition root wires `stateProjection::cursorPosition`. This keeps `StateStore` a pure key-value port (its design intent), keeps the query service decoupled from `StateProjection` (DEC-M3-16), and adds exactly one constructor parameter. Reported as `[REVIEW] D-1` so the PM can confirm the design intent against Doc 03.
**Impact:** When a STOP-on-Mismatch gate contains escape-hatch language ("or equivalents", "or similar shape"), it's not a permission to silently invent — it's an explicit signal that the PM already knows the source may diverge and wants the Coder to propose the equivalent in a deviation report. The proposal should be the minimum signature change that preserves the brief's behavioral contract. Counter-pattern: when no such language is present, gate failures should escalate, not be papered over.

## 2026-05-22 | Category: testing | Source: M3.6e.2 — generalising the Responder SPI to a full Request/Response context
**Discovery:** When a module accumulates multiple Javalin handlers, the per-handler nested `Responder` interface pattern (used by `ReadinessFilter` in M3.6e.1) reaches the point where extracting a shared top-level `EndpointContext` interface saves more code than the nested form costs in coupling. Five handlers added in M3.6e.2 would have meant five copies of essentially the same `(status, header, json)` stub.
**Detail:** M3.6e.1 nested `Responder` and `ContextResponder` inside `ReadinessFilter`. M3.6e.2 introduced five new handlers that need the same response shape AND request inputs (`pathParam`, `queryParam`). Resolution: extract `EndpointContext` (package-private interface) + `JavalinEndpointContext` (package-private adapter) as standalone files in `com.homesynapse.api.rest`. Each handler's `apply(EndpointContext)` is the pure decision method; `handle(Context)` is a one-line wrapper. Tests drive handlers through `RecordingEndpointContext` (a single test fixture class). The `ReadinessFilter`'s nested `Responder` remains in place — it was correct for the single-handler scope and changing it now is gratuitous.
**Impact:** Establishes the pattern for future REST handlers in this module: nest the SPI when there's one handler; extract when there are multiple. Don't pre-extract — the first handler's nested interface is the right shape until the second handler needs it, at which point the duplication makes the extraction self-evident.

## 2026-05-22 | Category: arch-rules | Source: M3.6e.2 — simpler ArchUnit form catches the same violation as `callMethodWhere(target(...))`
**Discovery:** The `noClasses().that()...should().accessClassesThat().belongToAnyOf(SomeClass.class)` form catches "class X depends on class Y in any way" — including method calls — without requiring the `target(...)`, `assignableTo(...)`, or static-import-soup of the `callMethodWhere` form. When the target class has only the methods you want to forbid (e.g., `EventPublisher` has only `publish*` methods), the simpler form is exactly equivalent.
**Detail:** M3.6e.2 brief sketched `noClasses().should().callMethodWhere(target(name("publish")).and(target(owner(assignableTo(EventPublisher.class)))))`. That requires importing `target`, `name`, `assignableTo`, `owner` from `com.tngtech.archunit.lang.conditions.ArchPredicates` (or similar). The equivalent `noClasses().should().accessClassesThat().belongToAnyOf(EventPublisher.class)` requires zero static imports and mirrors the existing `NO_SERVICE_LOADER` rule's shape. The brief explicitly invited this adjustment under "Coder Pushback Welcome".
**Impact:** Prefer the `accessClassesThat().belongToAnyOf(...)` form whenever the target class is purpose-built (its very existence is the violation). Reserve `callMethodWhere(target(...))` for when the target class has a mix of permitted and forbidden methods.
