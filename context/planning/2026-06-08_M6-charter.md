<!--
file: context/planning/2026-06-08_M6-charter.md
purpose: The M6 (Configuration + secrets/crypto) charter — M6 as MULTIPLE first-class lane pieces (P1), each with a backlog row + done-when, carrying the E2 composition-root bridge + OR-M6-NONCE verbatim into M6.2/M6.3. Authored at the W24 M6 entry-gate.
audience: PM, Coder, Nick
update-cadence: per-piece (M6.1..M6.4 status), then reference
state-type: charter
status: CURRENT — issued 2026-06-08; entry-gate CLOSED 2026-06-09 (config side RATIFIED: AMD-66/67/68/70/71; AMD-69 DEFERRED Tier-2/OQ-15-3). **M6.1 ✅ COMPLETE (whole) 2026-06-10** — M6.1b `b7bc65c` + M6.1a (incl. gate-fix round 1) GREEN, PM Phase 2 APPROVE, M6.1a commit pending. **M6.2 gate now FULLY satisfied (AMD-68 ✓ + M6.1 landed)**; M6.4 unblocked (obligations: R1 `config_error` publish + `config.section_reloaded` P2 survey + CoreSchema on reload); M6.3 stays triple-gated.
anchors: context/decisions/2026-06-06_post-M4_M5-window_decisions.md (D2 sizing, D4); homesynapse-core-docs/design/15-cryptographic-architecture.md (LOCKED — §3.8 E2 bridge, §6/§13.4 OR-M6-NONCE, §7.3, §9); design/06-configuration-system.md (LOCKED); AMD-66..71 (RATIFIED 2026-06-09 / AMD-69 DEFERRED)
-->

# M6 Charter — Configuration System + Secrets/Crypto

**M6 is large** — config pipeline + secret store + per-scope key-management + at-rest encryption + hot-reload. **It is chartered as MULTIPLE first-class lane pieces (P1), each with its own backlog row and done-when.** This is the explicit fix for the M4 "epic-in-disguise" failure mode: a single "M6" line that hides config + key-manager + at-rest encryption + hot-reload would let M6 discover its size in arrears the way M4 did. **Guardrail (P1): if any piece sprawls past its charter, split it — never let "M6" hide the size.**

## Entry-gate status (why M6 can be chartered now)

The M6 entry-gate is **CLOSED (2026-06-09):**
- **Crypto side — DONE.** Doc 15 (Cryptographic Architecture) is **LOCKED**; **AMD-86** (INV-PD-07 narrow + INV-PD-03 at-rest posture) and **AMD-87** RATIFIED; watermark AMD-87.
- **Config side — RATIFIED 2026-06-09.** **AMD-66/67/68/70/71 RATIFIED** (single DOCS review RATIFY-WITH-EDITS; all seven edits folded at docs `aedff55`; invariants registered §37–§41; return: `context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md`); **AMD-69 DEFERRED — Nick confirmed Option (a)** (Tier-2/OQ-15-3; number reserved); watermark unchanged at AMD-87 (reserved slots filled, ceiling not raised). **M6.1 is READY TO ISSUE** (`context/coding-instructions/M6.1_Config_Pipeline.md` — gate lifted, AMD numbers pinned, not yet issued). **The AMD-68 half of M6.2's gate is satisfied** — M6.2 remains gated only on M6.1 landing. M6.3 stays triple-gated (OQ-15-2 + interview signal + OR-M6-NONCE). AMD-69 does not gate any M6 piece.
- **Sizing (D2).** M6 = config pipeline + at-rest secret encryption + per-scope key-management **infrastructure/seams** + envelope-encryption of sensitive-PII categories on write — **NOT** a full operational crypto-shred subsystem (the shred *operation* is post-MVP). Encrypt sensitive-PII on write **from MVP**; defer only the shred operation.

## The four first-class pieces

| Piece | Scope | Gate | Done-when |
|---|---|---|---|
| **M6.1 — config pipeline** | YAML 1.2 loading (snakeyaml-engine, LTD-09), JSON-Schema validation (networknt 1.5.6, LTD-09), the loader/migrator pipeline, the **`ConfigurationAccess` + `ConfigurationService` impls** (the frozen surface — see naming note), the hybrid directory layout (AMD-71) on `PlatformPaths.configDir()` (M5-A), the `ConfigurationChangeListener` seam (AMD-66), the `(major,minor)` schema model (AMD-67), and the config observability events (AMD-70). **Not crypto-gated.** | AMD-66/67/70/71 RATIFIED | M6.1 committed + `./gradlew check` GREEN + PM WUCP Phase 2 APPROVE; config loads/validates/reloads zero-config; the AMD-66/67/70/71 §5 tests GREEN. |
| **M6.2 — secret store + per-scope key-management infrastructure** | The DEK/KEK manager (`ScopeKeyManager` — root key, scope KEK/DEK derivation, wrap/unwrap, `scope_keys`), the **`PayloadCipher` seam** (consumer-defined in persistence; impl bridged from config — see the **E2 carry**), the secret store rooted on the shared machine-local key (Doc 15 §3.5/§7.3 — `config_secrets` scope), and the **`SecretStore.setAll(Map)` atomic durable write** (AMD-68). | Doc 15 (DONE) + AMD-68 RATIFIED; M6.1 landed | M6.2 committed + GREEN + APPROVE; an encrypt→(store)→decrypt round-trip test GREEN; `SecretStore.setAll` all-or-nothing+durable test GREEN; the E2 bridge wired with **no new module edge** (re-verified). |
| **M6.3 — at-rest write-path encryption** | Envelope-encrypt the **sensitive-PII categories** (identity, person-linked presence) on write under per-scope DEKs (Doc 15 §3.4/§4). **GATED — see Guardrails.** Carries **OR-M6-NONCE** verbatim. | **(a) OQ-15-2** (Pi-4 microbench → the exact encrypted-scope set) **+ (b)** the energy/erasure interview signal (AMD-86 §3 — no launch-window buyer requires operational erasure) **+ (c) OR-M6-NONCE** co-design | M6.3 committed + GREEN + APPROVE; sensitive-PII categories ciphertext-at-rest; the Doc 15 §13.4 kill-mid-encrypt **nonce-monotonicity test GREEN**; the encrypted-scope set matches the resolved OQ-15-2 list. |
| **M6.4 — hot-reload atomic swap** | The atomic `ConfigModel` swap on reload (the active model replaced atomically; in-flight readers see a consistent model), exercising the `ConfigurationChangeListener` classifications (AMD-66) and publishing `config.section_reloaded` (AMD-70). | M6.1 landed | M6.4 committed + GREEN + APPROVE; concurrent read-during-reload sees either the whole old or whole new model (no torn read); listener classifications drive per-section application. |

**Sequencing.** M6.1 first (non-crypto, startable on AMD-66/67/70/71 ratification — competes for the Coder, not Nick's gate). M6.2 after M6.1 (needs Doc 15 + AMD-68). M6.4 after M6.1 (hot-swap rides the pipeline). **M6.3 last** — it is the gated piece (do not issue until all three gates clear). M6.2/M6.4 do **not** wait on the M5-D evidence.

### Naming note (source-reconciliation — Check 11)
The prior planning shorthand ("`ConfigurationAccess` / **`ConfigurationProvider`** impls") names a type that **does not exist in source**. The frozen Phase-2 config surface at HEAD `6c6dd33` is **`ConfigurationAccess`** (read-only integration-scoped access) + **`ConfigurationService`** (load/reload/write coordination) — verified against `config/configuration/src/main/java/com/homesynapse/config/`. **M6.1 implements `ConfigurationAccess` + `ConfigurationService`; there is no `ConfigurationProvider`.** This corrects the W24/backlog/handoff shorthand (the Research-6 anti-fabrication discipline — use exact source names).

---

## CARRY 1 (verbatim) — the E2 `com.homesynapse.app` composition-root bridge (Doc 15 §3.8)

**This carries into M6.2 (the `PayloadCipher` wiring) and the M6.2/M6.3 coding instructions verbatim.** It is the persistence↔config cycle-avoidance, the AMD-45 injection pattern applied to a config-supplied implementation. Quoted from Locked Doc 15 §3.8 (do not paraphrase into the instruction — embed):

> **Key-manager placement — JPMS-cycle resolution (m3).** The key manager (root key, scope KEK/DEK derivation, wrap/unwrap, `scope_keys`) is key-management and therefore belongs with **configuration** (`com.homesynapse.config`, M6) — but the *encryptor of event payloads* is **persistence**, which must not statically depend on config (verified at HEAD: persistence does **not** `requires com.homesynapse.config`; config `requires transitive com.homesynapse.event` only — a new persistence→config edge plus any future config→persistence need would risk the **same cycle class as the AMD-52 `event↔device` cycle that forced the value-model relocation**). The resolution is the **AMD-45 `AtomicCheckpointSink` seam pattern**: a narrow **`PayloadCipher` interface is *consumer-defined in persistence*** (`encrypt(scopeId, plaintext) → (ciphertext, iv)` / `decrypt(scopeId, keyVersion, ciphertext, iv) → plaintext`), the config-resident key manager (`ScopeKeyManager`) **exposes its own `encrypt`/`decrypt` surface but does *not* implement the persistence-exported `PayloadCipher` type directly** — implementing it in `config` would force a `config → persistence` edge. Instead the **top-level composition root `com.homesynapse.app`** (`Main`) constructs `ScopeKeyManager`, wraps it in a thin `PayloadCipher` adapter (a lambda over `encrypt`/`decrypt`), and **injects that adapter down through `HomeSynapseCore` (lifecycle) into the persistence write path**. `app` is the correct host because it is the **only** module that already `requires` **both** `com.homesynapse.config` **and** `com.homesynapse.persistence` (verified at HEAD: `app/homesynapse-app/src/main/java/module-info.java` `requires com.homesynapse.config` *and* `requires com.homesynapse.persistence`; `lifecycle` requires `persistence` but **not** `config`), so the bridge adds **no** new module edge; hosting it in `lifecycle`/`HomeSynapseCore` would force a new `lifecycle → config` edge. The `scope_keys` store is owned on the **config/key-manager** side (mirroring the secret store's own `secrets.enc` file). **Net JPMS result: `persistence` does not `requires` `config`, `config` does not `requires` `persistence`, and only `app` `requires` both — which it already does at HEAD — so the cycle is closed in *both* directions with zero new edges** (the AMD-45 `AtomicCheckpointSink` injection-at-the-composition-root discipline, applied to a config-supplied implementation). The verbatim `module-info.java` for persistence and config **must be embedded in the M6 coding instruction** (the standing Research-6 rule) before any code is written, and the no-cycle property re-verified at issue.

**M6.2 obligations from CARRY 1:** define `PayloadCipher` in persistence; implement `ScopeKeyManager` in config with its own `encrypt/decrypt` (NOT implementing `PayloadCipher`); wire the adapter in `com.homesynapse.app.Main`; inject through `HomeSynapseCore`. Re-verify at issue: `persistence !requires config`, `config !requires persistence`, `app requires both`. Embed both verbatim `module-info.java` files.

---

## CARRY 2 (verbatim) — OR-M6-NONCE: counter-nonce crash/restore durability [BLOCKING-for-M6-impl] (Doc 15 §6 / §13.4)

**This carries into the M6.3 coding instruction verbatim** (the at-rest write-path piece). It is a cryptographic-correctness gate — catastrophic if missed. Quoted from Locked Doc 15 §6 (Failure Modes):

> **Counter-nonce reuse across crash/restore — [BLOCKING-for-M6-impl].** Trigger: the per-scope nonce counter repeats after a crash, **or** a restored backup reintroduces a counter value already used under the same DEK. Impact — **Catastrophic:** (key, nonce) reuse breaks AES-GCM confidentiality *and* authentication for that scope. Recovery: the per-scope counter **must be durable and strictly monotonic across crash AND restore**: persist the counter high-water mark atomically with (or ahead of) the encrypted write; on boot re-init from the persisted max, never from memory. The deferred backup/restore feature (foundation-readiness F3) **must be co-designed** so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). **Corollary to F-A:** "key-excluding backups are protected" holds only if the backup excludes (or separately wraps) the root key.

**M6.3 obligations from CARRY 2 (Resolution condition, from pm-handoff OR-M6-NONCE):** (a) persist the per-scope counter high-water mark **atomically with (or ahead of) the encrypted write** and re-init from the persisted max on boot — never from memory; (b) **co-design the deferred backup/restore (F3)** so a restore can never resume a scope at a counter ≤ any value already used under that DEK (rotate the DEK on restore, or carry the high-water mark in the backup). **Closed when the M6.3 impl ships with the Doc 15 §13.4 kill-mid-encrypt nonce-monotonicity test GREEN.** Owner: PM (embed verbatim in the M6.3 instruction) / Coder (implement at M6.3).

---

## M6.3 gating guardrail (do not issue M6.3 yet)

**Do NOT issue the M6.3 (at-rest write-path encryption) coding instruction until all three clear:**
1. **OQ-15-2** — the exact encrypted-scope set, from the Pi-4 AES-256-GCM write-path microbench (Nick runs; resolves to a Doc 15 §9 `encrypted_scopes` list — a **TUNING of a Locked-doc default, NOT a Doc 15 re-open**). Current §9 default = `[identity, presence_personal]`.
2. **The energy/erasure interview signal** — confirming **no launch-window buyer requires operational crypto-shredding** (AMD-86 §3 trigger). If one **does**, **AMD-86 re-opens via the formal pipeline BEFORE M6 freezes the write path** (the guardrail). Calendar-bound (the long pole) — interviews schedule + begin in W24, realistically spill weeks.
3. **OR-M6-NONCE co-design** (CARRY 2).

**M6.1/M6.2/M6.4 do NOT wait on these.** Keep Locked Doc 15 inviolate: the microbench tunes the `encrypted_scopes` *list* (§9 default); only the erasure interviews can re-open AMD-86 (formal pipeline). If authoring M6.3 surfaces a Doc 15 design gap, **STOP and escalate.**

## Cross-piece invariants carried

- **INV-CE-01** — the YAML file is the sole source of truth (no piece mutates config outside the write path).
- **INV-CE-02** — zero-config valid (machine-local root, default-on; no passphrase at MVP).
- **INV-PD-03 / INV-PD-07** (per AMD-86) — sensitive-PII encrypted at rest from MVP (M6.3); operational shred post-MVP; the at-rest mechanism is app-level per-scope payload encryption (D2 sub-decision (i)).
- **OR-M6-NONCE** (CARRY 2) and the **E2 bridge** (CARRY 1) survive Doc 15's Lock via this charter.

## Backlog rows (mirror into phase-3-milestone-backlog.md)

`M6.1 UNBLOCKED / READY TO ISSUE (AMD-66/67/70/71 RATIFIED 2026-06-09)`; `M6.2 PLANNED — AMD-68 RATIFIED (gate half satisfied); still gated on M6.1 landing`; `M6.3 PLANNED — GATED (OQ-15-2 + interview signal + OR-M6-NONCE)`; `M6.4 PLANNED — gated M6.1`. The single legacy "M6 — Configuration System" backlog line is **superseded by these four rows** (the P1 fix).
