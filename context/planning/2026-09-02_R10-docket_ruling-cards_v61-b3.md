<!--
file: context/planning/2026-09-02_R10-docket_ruling-cards_v61-b3.md
purpose: THE R-10 DOCKET'S RULING CARDS — one H10 card per §3.3 row of the v61 prompt (the September plan §1 Fri–Sat rows + the 08-30 additions + the six v61 rows), authored AHEAD at v61 beat 3 (Wed 2026-09-02 ~19:58 CT) so the Fri–Sat 09-05/06 sitting is words, not deliberation. Each card: the question · the options graded · the rec · refutable-by · WHAT IT BUYS the household / the company (Nick's v61 weighting) · sequence. Every card grounded at its primary (named in-card). The strategy beat's card is separate: `context/strategy/2026-09-02_v61-b2_strategy-beat_card_convergence-flip-arc.md`.
audience: Nick (one word per card at the sitting; ADOPT-ALL-RECS / DELEGATE / per-row are lawful forms — D15) · the hub (splices rulings into the plan, the OR register, the charters)
state-type: ruling cards (H10)
status: **RULED 2026-09-03 ~20:40 CT (banked v62 beat 2)** — ADOPT-ALL-RECS + EDIT Row 2 · EDIT Row 12 · TIER-2 GO; the verdict table under the forms paragraph; the verbatim record at `context/strategy/2026-09-04_R10-sitting_THE-WORDS_ruling-record.md`. Prior: AUTHORED AHEAD (v61 beat 3) — dispatch-ready for the sitting; the coding-WU charters finalized on these words.
-->

# R-10 docket — the ruling cards (16 rows)

**Ruling forms accepted up front (D15):** per-row words · "ADOPT-ALL-RECS" · "DELEGATE" (the v61-b1 form; the hub records) · "EDIT ⟨row⟩: …". Rows marked ★ are the sitting's load-bearing rows (they charter code).

## THE WORDS — RULED 2026-09-03 ~20:40 CT (banked v62 beat 2; the verbatim record: `context/strategy/2026-09-04_R10-sitting_THE-WORDS_ruling-record.md`)

Nick: `DOCKET: ADOPT-ALL-RECS` · `EDIT Row 2: (b), as a DATED design note first (the Doc 17/02 AMD charters with the P4 build); weeks 3–4, after R-4b and the CG batch land` · `EDIT Row 12: (a); the fallback fires on a criterion-0 miss at this weekend's R-4b, not on the 09-14 calendar date` · `OPTIONAL: TIER-2 GO`.

| Row | Verdict | Where it executes |
|---|---|---|
| 1 | (a) — the §9-2 Apple pre-ruling executes as a paste on 09-09 | the hub hands the paste 09-09 |
| 2 | (b) EDITED — a DATED design note first; the Doc 17/02 AMD charters with the P4 build; the lane runs weeks 3–4 after R-4b + CG | the Row 2 design-note lane (dispatch Mon 09-07 if reached; else wk 3) |
| 3 | (b) — record the SCITT outcome, read at source | the hub, 09-07 |
| 4 | (a) — RS-4 §1.7 adopted as the sensing register's physics anchor | the register preamble |
| 5 | (b) HOLD — sd_notify stays behind the seam; T-0 is NOT spoken this month | OR-M13-SDNOTIFY (status line) |
| 6 ★ | (a) — the FAILCHAN sweep WU, root-correct 143; + EXITCODE (a) as instance 5 | the FAILCHAN instruction (v62 beat 3) |
| 7 | (a) — the `<N>`-prefix on the console handler | OR-JOURNALD-PRIO; charter queued behind FAILCHAN |
| 8 | (b) — additive first: ULID + ISO siblings (the Core half of Row 15) | the F-S8 charter (Mon menu / wk 3) |
| 9 | (a) — the 15 h/wk floor confirmed | standing |
| 10 ★ | (a) — worded by dispatch 09-02; LANDED `a1c6966` | done |
| 11 | (a) — custody clones the NETWORK, not the REGISTRY; the loud runbook sentence + rows | the R-4b packet + the docs rows |
| 12 | (a) EDITED — after F-R4-1 (landed); the announce-class fallback (b) fires on a criterion-0 MISS at Sat 09-06's R-4b, not on 09-14 | the R-4b navigator packet (the fallback branch inside) |
| 13 ★ | (a′) — worded by dispatch 09-03; LANDED `ef02d13`, CI green | done |
| 14 ★ | (a) — one v1.1.x bump, all three fields (CG-1/2/3) | the CG Core instruction (Sun AM) → the FE fast-follow (Sun PM) |
| 15 | (a) — after the CG batch; the Core half first | the F-S8/FE-STATE-DIALECT charter |
| 16 | PRE-LEAN (a); RULED AT H8 (no word needed) | the H8 real-wire exercise (Mon 09-07) |

**Rows 17+ (the addendum, authored after the words):** 17 REOPEN-NETUP · 18 S31-SETTLE-AFTER-REENUM · 19 SILENT-PATHS + DORMANT-DEFAULTS · 20 the INV-CE-02 gloss + Doc 08 §9 · 21 EXITCODE-UNWIRED (RULED (a) by the 09-03 word; carried for the record) · 22–25 the Pelton-week orphan rows (Doc 09 §15 Q1 · the JDK trajectory · the IR emit question · the events endpoint; ORPHANS (a) — ≤1 KB each, words at the R-5 charter) · 26 STRAT-BEYOND-MVP (the post-MVP deep-reasoning card; runs at the go-ahead).

**Row 12 — CLOSED 2026-09-04 (v62 beat 7): R-4b ran on `ef02d13`'s artifact, FOUR OF FOUR (the record `context/audits/2026-09-04_R-4b_re-rep_operator-record.md`; the audit `…_intake_two-layer-audit_v62-beat-7.md`); criterion 0 MET on both arms; the fallback (b) did NOT fire; C-002 LIVE.** **Rows 27–33 (the R-4b findings, for the addendum — H10 form, ≤2 KB each):** 27 **F-R4-1b** the ZDO `IEEE_addr_req` second surface on `lookup_miss` (EVIDENCED: `nwk=0x15ac status=0x1`; the fleet's sleepy devices cannot be re-adopted by 0x0061 alone; charters the C-003 slot) · 28 **P-1 the power-harness primitive** (a Zigbee-adopted plug as a software-addressable, self-confirming, protocol-independent mains switch — a bench verb + per-profile safety limits in code; operator-originated) · 29 **the Hue LCA017 absent from the air** (playbook §6's "wall power-cycle ⇒ re-announce" → CONTESTED; a bench-card diagnostic before any re-pairing) · 30 **read-API shape gaps** (`trigger.firingValue` null on a matched trigger · `actions[].resultOutcome` null beside `outcome: CONFIRMED` — against the FROZEN v1.1; adjudicate with CG) · 31 **`entity_registered` not logged** (the entity registers; the packet's mapping step reads `/api/v1/entities` deltas instead) · 32 **`reporting_configured … verified=0 degraded=1` on a MAINS device** (unexplained) · 33 **the availability flap** on a pre-existing entity inside the window (`availability_changed` pos 188; for R-4.5/R-5).

---

## Row 1 — Ratify the §9-2 Apple pre-ruling (`context/strategy/2026-08-28_S9-2_apple-event_two-branch-preruling.md`)
**Q:** Does the two-branch pre-ruling execute as a PASTE on 09-09? **Options:** (a) RATIFY as written — binary TRIGGERED/NOT, branch 1 expected (architectural half at most, not open), branch 2 → re-adjudicate NEXT beat, the null read one line · (b) refine: add an explicit third paste for "Home/HomeKit named but no gate" (today's §5 already covers it as NULL) · (c) hold. **REC: (a).** The vocabulary law stands: "partially triggered" banned. **Refutable-by:** Apple publishing a policy schema or contributing the gate to a body on 09-09 — branch 2's own trigger. **Buys:** the company — a tripwire that stays a tripwire (no same-day deliberation, no posture drift on a keynote); the household — nothing directly. **Sequence:** the paste 09-09 ~12:00 CT; the class-(iii) sentences survive either branch (C4-5).

## Row 2 — The external-agent-interface posture row (the north star's verified-at-filing note; v58 findings A-1b)
**Q:** The thesis's proof — the agent seams (agent `SubjectRef` kind · proposal/adjudication event types) — is DESIGNED-FOR, not built. What is the fall's posture? **Options:** (a) build AGENT-SEAMS in P3 (before Nov-25) — displaces the household floor (R-4b · R-5 · the FE rows); rejected · **(b) a DESIGN-ONLY row in P3: the agent interface published as a design artifact (a Doc 17/02 AMD or a design note: the proposal event, the adjudication event, the agent subject kind, the confirmation leg as the enforcement point) — so the register can carry a DATED designed-for sentence; the build charters at P4** · (c) defer all to P4, say nothing. **REC: (b)**, with A-1b as the external framing (from 2027-01-20 in the EU the deterministic floor is the CHEAPER conformity path — never "superior", always D5's layered form). **Refutable-by:** a P3 week found with hours the household rows cannot use — then (a)'s first slice (the subject kind only) may pull left. **Buys:** the company — the thesis stops being an undated promise (a designed-for sentence with a date and a diff is register-grade); the household — the floor stays deterministic while the agent story is designed, not improvised. **Sequence:** a research/design lane, weekday-parallel (A-14 shape), ≤2 weekend blocks; INV/LTD sweep at the AMD.

## Row 3 — The SCITT community-channel act, sized at A-14 (09-07 the CCF Last Call ends; I-6 of the LE-LF audit)
**Q:** What act, if any, at the Last Call close? **Options:** (a) a position-holding filing — REJECTED by standing ruling I-6 (the noa draft is READ AT SOURCE before any filing) · **(b) record the LC outcome (one line, 09-07) → the read-at-source row (weekday-automatable, ≤2 h, a research lane) → ONE list question only if the read finds a receipts-shape delta that touches the register's evidence chain (LE-C2)** · (c) nothing. **REC: (b).** **Refutable-by:** the LC outcome closing the receipts format such that the read cannot change our shape — then (c) and the watch moves to IETF 127. **Buys:** the company — "being in the room" (Substrate 2.9) at zero Nick-hours; the register's receipts shape gets a standards-grade comparison. **Sequence:** the 09-07 line at the next beat; the read lane dispatches the same week.

## Row 4 — The 802.11bf physics row (= RS-4 §1.7, audited ACCEPT v58 b4)
**Q:** Adopt §1.7 as the sensing register's physics anchor? **Options:** **(a) ADOPT: the envelope as graded (shipping = whole-home binary motion with pet filtering; lab-conditional = activity/gesture/fall/breathing/counting; demonstrated-once = identity; not-Wi-Fi = through-wall imaging), the clocks (WFA certification none; client silicon none; REVmf Dec 2026), the four-qualifier law on every sensing sentence, the D-1 sensing fence ("what the row must NOT say")** · (b) commission more before adopting · (c) decline sensing entirely. **REC: (a)** — and the constructive counterpart is the strategy card's R-1(b): we MEASURE presence QUALITY in homes on four metrics; we do not CLAIM sensing capability. **Refutable-by:** a WFA Sensing certification program or 11bf-answering client silicon appearing (the row's own clocks) — re-price on sight, date-armored. **Buys:** the household — no sensing sentence about THEIR home leaves the register without hardware · environment count · obstruction definition · training regime; the company — the physics-honest posture the field lacks (Origin AI inside ADT, Xfinity Shield — consolidating before any consent semantics exist). **Sequence:** adopt now; the consent-semantics wedge (RS-4 §2.4) stays a charter INPUT.

## Row 5 — OR-M13-SDNOTIFY, the T-0 word (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md` §6)
**Q:** Does the real sd_notify transport commit now (T-0), or does the `NotifyTransport` seam keep holding? **Options:** (a) T-0 NOW on the matrix's lean — `systemd-notify` subprocess if no WATCHDOG keepalives (zero dependency, GraalVM-safe) · **(b) HOLD behind the seam, as the matrix itself rules (final commit at M13, gated on the GraalVM spike + the watchdog decision) — AND record that the shipped unit is READY-verified on the current mechanism at R-4 (ready http=200 on every start)** · (c) FFM/Panama now (Java 22+ or `--enable-preview`) — premature. **REC: (b)**; T-0 is NOT spoken this month. **Refutable-by:** a measured readiness gap on the shipped unit (a start where `ready` lags `active` enough to matter) — then (a) pulls left as a small WU. **Buys:** the household — nothing changes in a unit that already reports ready; the company — no new governed dependency for a three-syscall job. **Sequence:** M13; re-read the matrix's §5 watchdog question when R-4.5/R-5 charter (the settle redesign may want keepalives).

## Row 6 ★ — OR-FAILCHAN sweep charter (opened v58 b6 R-5) — SHARPENED v61 b8 AT SOURCE · O-2 MEASURED v61 b9
**Q:** Healthy outcomes are reported through FAILURE channels (§6-B: the unit lands `failed` after a clean `systemctl stop` · §10-O `transport_failed` on orderly close · §10-M · §10-I `Current`). Charter the sweep? **Mechanism (read at source):** `distribution/systemd/homesynapse.service` has `KillSignal=SIGTERM`, `Restart=on-failure`, NO `SuccessExitStatus=`; `Main.java` sets no exit code → the JVM's default SIGTERM exit is **143 by contract** and systemd grades a clean stop as failure. **Measured TWICE:** R-3a §6-B (exit-143) and **O-2 on the held card, 2026-09-03 03:10Z (`hs-fresh`): after ONE clean stop `Result=exit-code · ExecMainCode=1 · ExecMainStatus=143 · ActiveState=failed`** — the pre-filed hypothesis CONFIRMED on the shipped unit (the 09-02 attempt hit the bench card — null, harmless). **Options:** **(a) ONE sweep WU: fix §6-B first and root-correct — the one-line unit change `SuccessExitStatus=143` (the unit-lint gate asserts it), NOT an app-side `exit(0)` from a shutdown hook (`System.exit` in a hook deadlocks; `halt` skips hooks); the charter RULES the `Restart=` line beside it (with 143 = success, a stray external SIGTERM would no longer trigger `on-failure` → choose `Restart=always`, or keep `on-failure` with the reason written); then enumerate every other failure-channel emission on an ORDERLY path, one ruling row each (fix / reclassify / keep-with-reason)** · (b) §6-B only · (c) hold. **REC: (a).** **Refutable-by:** DISCHARGED at O-2 (2026-09-03) — the premise is closed. **Buys:** the household — unit states and logs that don't cry wolf (an operator trained to ignore `failed` is the F-S19 cousin); the company — the instrument's own honesty. **Sequence:** the charter authors NEXT; a Coder lane wk 2 (distribution + core; zero hardware); confirmed free at R-4b's first boot (`systemctl stop` → `inactive`/`success`).

## Row 7 — OR-JOURNALD-PRIO charter (v58 b6 §1.3: every application line lands at journald INFO; `homesynapse.service:99–100` routes stdout/stderr → journal)
**Q:** Make journald priority meaningful? **Options:** **(a) emit the `<N>` priority prefix on stdout lines, mapped from the app log level (WARN→4, ERROR→3, INFO→6, DEBUG→7) — systemd parses the prefix on journal-connected streams; a small change at the console appender, no transport, no dependency** · (b) native `sd_journal` — ties to Row 5's seam; rejected now · (c) hold. **REC: (a)**; verify at the instrument that the unit's stream is journal-connected (it is, per :99–100) and that `SyslogLevelPrefix` is not disabled. **Refutable-by:** the appender proving unable to prefix without a library change — then (a) re-prices. **Buys:** the household operator — `journalctl -p warning -u homesynapse.service` returns the 2 lines that matter, not 200; the company — every future evidence read gets cheaper. **Sequence:** rides the FAILCHAN WU's census (same modules, same lane) or standalone; one small WU.

## Row 8 — F-S8 (the card-sitting return 08-23: `/state` renders instants two ways — `data.*` epoch seconds vs `meta.timestamp` ISO — and `entityId` as a 128-bit `{msb,lsb}` pair a caller cannot round-trip)
**Q:** Fix the read-surface asymmetries, and where? **Options:** (a) change the existing fields in place — NOT additive; rejected · **(b) ADDITIVE FIRST: `entityId` gains a sibling ULID string field; the epoch instants gain ISO siblings (or one canonical `meta`-style instant on `data.*`); the old fields deprecate at a later bump under the frozen-contract amendment path** · (c) leave (the frozen contract says nothing) — rejected: the consumer works around a dialect (the F-6 class, arc-discipline 24/25). **REC: (b)** as the CORE HALF of FE-STATE-DIALECT (Row 15) — one amendment, the full-corpus sweep for the dialect class rides (arc-discipline 25). **Refutable-by:** the frozen v1.1 read-API showing `/state` OUT of its scope — then (b) is a plain Core change, no amendment. **Buys:** the household — the explain surface's timestamps read one way; an identifier that round-trips; the company — the wire dialect corpus stops growing occurrences. **Sequence:** AFTER the CG batch (Row 14); the FE half (Row 15) follows.

## Row 9 — The A-14 confirm (15 h/wk weekend-anchored floor; `context/research/2026-08-02_A14_attended-hours_charter-input.md`)
**Q:** Confirm the floor for September's plan (costed: wk1 8–10 · wk2 8–12 · wk3 6–8 · wk4 6–8)? **Options:** **(a) CONFIRM** · (b) re-size ⟨N⟩ h. **REC: (a).** The holdable-posture law: promise the floor, never the normal week. **Refutable-by:** a week the plan's Nick-hours exceed the floor (wk 2's 12 h is the ceiling; B-1 slides WHOLE, never shrinks). **Buys:** the company — nothing is promised externally that a 15-h week cannot keep. **Sequence:** one word.

## Row 10 ★ — F-R4-1: the silent-rejoiner adoption gap (design ruling → a coding WU; R-4 record F-R4-1; the D-g bounded Hue attempt, zero adoption-chain events across 254 s)
**Q:** The shipped pipeline converts ONLY announce-class devices; a mains device on SECURED_REJOIN reports forever without proposing. What is the doctrine, and the shape? **Options:** **(a) DOCTRINE: relink ≠ adopt; a rejoin of a KNOWN-custody-but-unadopted device DURING an open permit-join window is a PROPOSAL of source `rejoin` (interview-on-rejoin), adopted by the SAME path as announce-class — a first-class `device_relinked` → proposal → `device_adopted` promotion; outside a window it stays a relink (reports ignored for adoption, logged once)** · (b) auto-adopt relinked devices — rejected: adoption is an explicit, evidenced act (Doc 02/08's doctrine) · (c) leave — rejected: R-4b stays unreachable, C-002 never mints. **REC: (a).** INV/LTD sweep at the ruling (Doc 02 event model · Doc 08 device model; the proposal-source enum's admission is the contract check). **Refutable-by:** Doc 08 text found to define rejoin-during-window as adoption already — then this is a bug fix, not a doctrine. **Buys:** the household — a mains device that rejoins after a power cycle gets adopted, not ignored forever; the company — R-4b → C-002 → the fleet sentence. **Sequence:** the ruling → the charter (authored, ruling-slotted) → a Coder lane wk 2 (first WU: proves the loop) → R-4b.

## Row 11 — F-R4-2: custody clones the NETWORK, not the REGISTRY (doctrine + docs; R-4 record; bench pos 25065/6 devices vs held pos 40/2 devices for the SAME fleet)
**Q:** Adopt the doctrine and the docs sweep? **Options:** **(a) ADOPT: the cloning runbook states it in one loud sentence; a boot-contract-map row; pointer notes where Doc 02/08 imply otherwise; the packet-authoring lesson (criteria checked REACHABLE against the instrument's census) carried by Row 12** · (b) redesign custody to carry the registry — a Doc 02/08 design question (per-hub identity; ULIDs across hubs), OUT of R-10's scope unless Nick opens it. **REC: (a).** **Refutable-by:** a runbook reader still expecting a cloned card to know the original's devices — the sentence was not loud enough. **Buys:** the household operator — no false expectation on a cloned card; the company — the C4 lesson banked once, forever. **Sequence:** a docs-only WU (hivemind + docs repos; hub-run for hivemind, Nick's hands for docs); wk 2.

## Row 12 — R-4b: the four-of-four re-rep (charter AFTER Row 10 lands)
**Q:** When and how does C4 get reached on the shipped artifact? **Options:** **(a) AFTER F-R4-1 lands (wk 3): the fleet re-adopted through the rejoin path + the bench-hero automation fired on the shipped build — every criterion checked REACHABLE against the rig's census AT PACKET AUTHORING (the minted lesson; this row is its carrier)** · (b) NOW, C4 re-scoped to an announce-class device re-paired — earlier, proves less · (c) skip. **REC: (a)**, with (b) as the dated fallback if F-R4-1 slips past Mon 09-14. **Refutable-by:** the rig census showing no actuator reachable even after F-R4-1 — then C4 needs a device purchase first (a Nick word). **Buys:** the company — C-002 (the fleet/automation sentence) mints; the household — the shipped artifact runs THE home, not a two-sensor rig. **Sequence:** the packet authors the week F-R4-1 lands; a navigator session (the R-4 pattern); ~3 h Nick, weekend-anchored.

## Row 13 ★ — PKG-SEC-2: the zigbee schema admission (R-4 C-1: a WARNING on every boot) — SHARPENED v61 b6 AT SOURCE
**Q:** Which contract is TRUE? **The source read answers the card's own first question:** Doc 06 §3.2 DOES specify per-integration fragments composed into the root's `integrations` node; the zigbee fragment SHIPS (`integration-zigbee/src/main/resources/schema/zigbee-config-schema.json`) and IS registered (`Main.java:130`) — but AFTER `manager.start()` returns (the "W10" deferral), while `config.load()` validates in Doc 12 Phase 1 with only CORE schemas composed (`HomeSynapseCore.java` :455–:467; a pre-start call THROWS, :1390–:1397). At Phase 1 the root's `integrations` node is `{properties:{}, additionalProperties:false}` — the WARNING is COMPOSITION ORDER, not a missing contract. **Options:** **(a′) pre-start fragment registration — fragments are static JSON text (no adapter needed); the composition root supplies them before `start()`, the lifecycle drains them into the registry before `load()`; the post-start path stays** · (b′) Phase 1 defers `integrations.*`; a second strict pass after the late registration — weaker (Phase 1 stops validating what Doc 06 says it composes) · (c) the `additionalProperties` carve-out — NEVER. **REC: (a′).** The Doc 12 conflict check (its §3.3 text vs the W10 code comment) is owed in the return; if Doc 12's TEXT forbids pre-load integration composition → STOP → an AMD, not a Coder call. **Refutable-by:** exactly that Doc 12 finding. **Buys:** the household — a malformed zigbee block is CAUGHT at boot instead of hidden behind "unknown", and a well-formed one boots with zero issues; the company — the config contract is true. **Sequence:** the second Coder WU of wk 2; the instruction is ON DISK, ruling-slotted (`context/instructions/2026-09-02_coder-lane_PKG-SEC-2_zigbee-schema-admission_coding-instruction_RULING-SLOTTED.md`); the D-f `/etc/homesynapse` sweep rides — CLASSIFIED (config-YAML claims corrected; env/token/unit uses kept), never blind-replaced.

## Row 14 ★ — The CG-1/2/3 additive contract amendment (FE-HONEST-1 return §4; ONE v1.1.x bump; Core WU + FE fast-follow)
**Q:** Amend the frozen dashboard read-API additively? **Options:** **(a) ONE bump, all three: CG-1 HIGH — optional `triggerRef?: SubjectRef | null` on `NonFiringExplanation` (+ `AutomationSummary.components[].ref?`), so WhyNot/Automations run the same census check and "it fires on state change" over a DANGLING ref renders LOUD; CG-2 — optional `deviceId?` on entity reads (entity-row → `device_adopted` correlation); CG-3 — optional `lastReported?` on A1 rows (evidence-based list freshness)** · (b) CG-1 only · (c) defer. **REC: (a)** — the formal amendment path, `contract-check` bumps once, FE fold as fast-follow. **Refutable-by:** any key found non-additive at the serializer (a null where the old contract promised a value) — then that key drops to its own bump. **Buys:** the household — the explain surface stops concealing a dangling trigger and stops claiming "Current" without a report time; the company — the §10-J exhibit becomes DEMO-ABLE (the corpus wedge's demo face). **Sequence:** Core WU wk 2 (after Rows 10/13 prove the loop) → the FE fast-follow the same week → Row 8/15 after.

## Row 15 — FE-STATE-DIALECT (the FE lane's named next WU; closes the §10-G unreadable class at the ROOT once Core conforms)
**Q:** Sequence and shape? **Options:** **(a) charter as the FE lane's next WU AFTER the CG batch, paired with Row 8's Core half landing FIRST (the FE reads one dialect; the store-truth register stays honest in the interim)** · (b) FE-only normalization — rejected: papering over a wire dialect at the consumer is the F-6 class. **REC: (a).** **Refutable-by:** Row 8 landing shows the unreadable class was never the dialect — then this WU re-scopes at the measured wire. **Buys:** the household — "Last reported" reads true everywhere; the company — the physics surface's prerequisite (v56 b2) is met. **Sequence:** wk 3; frontend lane under `nexsys-frontend`; the frozen-contract check + the mocks update in-census.

## Row 16 — The `sys_*` namespace question (rides H8: the real-wire exercise on the R-4 clone rig)
**Q:** Should system-provided refs (`sys_sun`, …) be census-resolved at all? **Options:** (a) a system-namespace posture: `sys_*` renders "system-provided" (never "not in this hub's registry") · (b) leave the letter-true render · (c) drop the census for `sys_*`. **PRE-LEAN: (a) — but RULE AT H8 on the real wire**, with the evidence named in the packet: does the rig emit foreign refs; do `Available`+`Not-found` pairs appear (in mock they are unseeded fixtures; on real wire they would be a FINDING). **Refutable-by:** H8 showing no `sys_*` ref reaches the resolver on real wire — then (b) costs nothing and the question closes. **Buys:** the household — the explain surface never accuses the sun; the company — FE-HONEST-1's LIVE-VERIFICATION-PENDING register discharges. **Sequence:** the H8 navigator packet (authored next); the rig, post-commit, unhurried.

---
*Rows 17–18 of the docket are elsewhere: the strategy beat (its own card, beat 2) · O-2 (the held card, `context/handoff/2026-09-02_O2_held-card_next-boot_operator-card.md`). The lessons-fold row (the R-4 packet instrument defects, additions item 6) rides the next fold, no word needed.*
