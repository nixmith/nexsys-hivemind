<!--
file: context/planning/2026-09-03_next-sessions_PROGRAM_v62-onward.md
purpose: THE NEXT-SESSIONS PROGRAM — Nick's ask (2026-09-03 ~04:20Z): plan the next work sessions (research, coding, hardware, brand, process) to use our time optimally and COMPOUND progress on the source code and the smart-home functionality. Authored at v61 beat 9 on the measured state (F-R4-1 returned + audited ACCEPT; O-2 measured; RS-9 audited; the posture ruling adopted by Nick). Governs session ordering until the September plan of record is re-spliced on the sitting's words; the spine outranks this file where they differ.
audience: Nick (his acts, hours, and calendar) · the hub (session mandates, in order)
state-type: program (planning)
status: LIVE from v61 beat 9. Re-splice on: the sitting's words · the F-R4-1 CI verdict · Erik's reply · the R-4b outcome.
-->

# The next sessions — ordered to compound

## §0 The ordering principle (three sentences)
Every session either **closes a loop on real hardware** (the shipped artifact adopts, fires and confirms on the home's devices — the only progress a stranger can see), **makes the instrument honest** so that every later measurement is trustworthy (boot warnings, failure channels that cry wolf, a dashboard that mis-renders), or **buys leverage for the next ten sessions** (a charter authored ahead, a skill that survives compaction, a decision recorded once). Anything else waits. The concrete chain this week: **F-R4-1 lands → R-4b proves four-of-four on the held card → C-002 mints (the fleet/automation sentence) → the corpus, the demo and the brand launch all stand on it.**

## §1 The sessions, in order
| # | When | Who / hours | The mandate | Prerequisite | What it compounds |
|---|---|---|---|---|---|
| **S1 — the landing** | now → Thu 09-03 evening | Nick ≤30 min · CI | Commit + push F-R4-1 from the hub's msg file (13 paths exact); push hivemind + skills; re-sync the three skill folders to the account (Check 9); **put the bench card back in the Pi** (the nightly at ~03:32 CT does not run while `hs-fresh` is in); relay the CI URL + verdict. | the audit (filed) | Unblocks R-4b; CI is the gate of record for the first behavior change on the adoption path since M9.4. |
| **S2 — the hub authors ahead (v62)** | Thu 09-03 / Fri 09-04 evening | hub 1 session · Nick 5 min | (1) The FAILCHAN instruction (Row 6, premise measured twice): the `SuccessExitStatus=143` line, the `Restart=` ruling, the unit-lint assert, the ORDERLY-path emission sweep as a classification table — zero hardware. (2) PKG-SEC-2 finalized for dispatch-as-word ((a′) pre-start fragment registration; the D-f sweep classified). (3) **The R-4b navigator packet** (criterion 0 = the 0x0061 hop's first ⏺; the log grammar from MODULE_CONTEXT §F-R4-1; every criterion checked REACHABLE against the rig census at authoring; O-2 CLOSED, step 0 removed; the FAILCHAN fix confirmed as a free rider on the first `stop`). (4) The CG-1/2/3 Core instruction (read-set: the FE-HONEST-1 return §4 · `contract-check.mjs` · the two MODULE_CONTEXTs). Then ONE Coder evening lane carrying FAILCHAN + PKG-SEC-2 sequentially (two returns, two commits, one desk). | S1's CI green (for the baseline sha) | Two boot-honesty defects gone before the hardware session; the R-4b packet ready the day F-R4-1 is on `main`. |
| **S3 — the sitting** | Fri–Sat 09-05/06 | Nick ~1 h reading, one message | The words in the Part-5 shape (`STRATEGY: R-1..R-5 · DOCKET: ADOPT-ALL-RECS or overrides · OPTIONAL: TIER-2 GO`); the hub splices the same beat: the plan rows, the OR register, v1.2 A-1..A-7, the ruling slots of every charter. **Nick's Part-B endorsement (09-03) is recorded as alignment, not yet as the words** — if he means ADOPT-ALL-RECS, one line says so. | the deliberation guide (filed) | Every charter finalizes on words instead of slots; v1.2 becomes the strategy of record; the brand posture ruling (adopted 09-03) governs public use. |
| **S4 — R-4b on the held card** | Sat/Sun 09-06/07 (weekend-anchored, A-14) | Nick ~3 h · hub navigator | Install the CI-built F-R4-1 (+ FAILCHAN/PKG-SEC-2 if landed) artifact on `hs-fresh`; the four-of-four re-rep: the fleet re-adopted through the rejoin path (`permit_join_opened` → `rejoin_candidate … source=unknown_sender` → `device_proposed … source=rejoin` → `device_adopted`), the bench-hero automation fired and confirmed on the shipped build. Criterion 0 first; a miss reads the status byte and stops (the ZDO IEEE_addr_req second surface is a second WU). Free riders: the FAILCHAN fix (`stop` → `inactive`/`success`), E2's floor once the bench card returns. | S1 green · S2's packet | **C-002 mints** — the shipped artifact runs THE home, not a two-sensor rig; the corpus gains its first fleet-scale confirmed runs. Fallback (Row 12 b) if F-R4-1 slips past 09-14. |
| **S5 — the honest dashboard** | wk 2 evenings, after S4 | Coder + FE lanes · Nick 10 min per landing | CG-1/2/3 (the additive v1.1.x contract amendment; ONE bump) → the FE fast-follow → FE-STATE-DIALECT (closes the §10-G unreadable class at the root once Core conforms) → **the H8 real-wire exercise** on the R-4 clone rig (rules `sys_*`, Row 16; converts FE-HONEST-1's register from LIVE-VERIFICATION PENDING to VERIFIED). | S4 (the live payloads exist) | The demo surface renders REAL verdicts from the home's own log — the corpus's public face. |
| **S6 — the research reads (async, cheap)** | any quiet hour; each ≤1 h | hub · Nick 1 min for E1 | E1: the Annex I Parts A/B paste → the W-C6 closure line (before any smart-home *decision* that leans on the EU carrier, per Nick's 09-03 word). The 09-07 SCITT Last-Call line + the read-at-source lane. The 09-09 Apple paste (Nick, one line). The corpus read (Sampath diagnosability; Saltzer still owed). The receipts ledger (~1 h). | none | Decisions made on verbatim primaries, not summaries; the strategy card's chain stays grounded. |
| **S7 — W-SKILLS-7 (evals)** | wk 3, one skills lane | hub + skill-creator | ≥3 evals per role skill (trigger · procedure · boundary), benchmark with/without, blind A/B old-vs-new bodies; then the references' own trim. | W-SKILLS-6 landed (this beat) | The process layer measured instead of censused — every later session boots cheaper and survives compaction whole. |
| **The brand loop (rides every session, ≤15 min)** | Fri 09-04 midday: the nudge if no reply · then the tree | Nick pastes; the hub keys | Erik's reply → the go-ahead the same day with RS-9 §A attached → the comprehensive (~1 wk) → the written opinion → FILE §1(b) cl. 9 + 42 → the .com walk → B-1 identity as a real design block (name-tokenized now on `TIER-2 GO`). **Hard stop Fri 09-18.** Public use stays FENCED to the opinion (the posture ruling, adopted 09-03). | — | The name lands with the flip (one launch moment) instead of gating it. |

## §2 Week 3 (09-14 → 09-20), sketched — re-cut after S4
R-4.5 / R-5 charter + dispatch (the s31/nightly HANDS-OFF fence lifts at R-5) · F-S8 (the `/state` instant dialect, the Core half of FE-STATE-DIALECT) · the follow-ons R-4b surfaces (F-R4-1b cache re-index · the additive `source` payload key · the ZDO second surface if 0x0061 misses · rejoin dedup hygiene · the rig's 0x0061 script) chartered as rows, none before their evidence · B-5 story-kit assembly from the register (C-001 is its first sentence; C-002 its second) · v1.2 in force · the 10-01 quarterly gate check (LICENSE flip chain, registered office, NFCU).

## §3 Nick's hours this week (the A-14 floor is 15 h weekend-anchored; this asks ~7)
S1 ½ h · S3 1 h · S4 3 h · the brand pastes 1 h across the week · E1/E2/Apple ½ h · the two Coder landings (S2's lane, S5's first) 1 h. The hub carries the rest.

## §4 What is deliberately NOT in the next two weeks
New integrations, new device classes, a removal surface, WebSockets, the agent layer's build-out, the marketing site — each is real, none compounds until the adoption loop is proven on the home and the dashboard renders it honestly. The physics-aware seed and the external-agent-interface row wait for the sitting's words.
