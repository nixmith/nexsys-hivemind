<!--
file: context/planning/2026-09-05_v64_effectiveness-second-check_and_refinement-program_charter.md
purpose: Nick's ask at the v64 post-close (Sat 2026-09-05 ~17:4x CT): "second-check and validate how impactful or effective this coding session was; plan how exactly we should work and proceed from here; fully update and refine/optimize the context and contents of our repos; ensure all our skills are fully updated and optimized for the future context windows required for deep reasoning and solving/troubleshooting difficult problems in coding, research and testing." §1 is the second-check on the evidence in hand (the FIX-1 corpus, the two audits, the spine); §2 the plan at three horizons; §3 THE REFINEMENT PROGRAM as three chartered lanes with read-sets, censuses and return paths; §4 the fences. The skills pass runs in a FRESH window as a lane (W-SKILLS-7) — a skills rewrite at the tail of a heavy window is the compaction class the context-health law exists to prevent. Nick's words close it: `REFINE: go | hold` · `W-SKILLS-7: fresh-window | v65-inline`.
audience: Nick (the words) · the v65 hub (dispatches the lanes) · the W-SKILLS-7 lane (its brief's spine)
state-type: charter + assessment (hub-authored)
status: FILED v64 beat 7 (17:52 CT = 22:52Z). Nothing here executes until the words; the FIX-1 landing (Acts 7–8) is not gated on it.
-->

# The v64 coding session — second-checked; and the refinement program from here

## §1 The second-check — how effective was this coding session, on the evidence
**The timeline at the instrument (every stamp from a file).** Lane launched 17:51Z (baseline `dc3328b` verified) → FIX-1a desk gate GREEN 18:10Z (19 min: the instruments, red-first at two stages) → the loop corpus 18:21Z→19:56Z (176 runs) → FIX-1b designed on the table, desk gate GREEN 19:29Z (5/5 red → 9/9 green) → T8/T9 → return v1 19:59Z → the hub's two-layer audit 20:05–20:16Z → Act A executed by the lane 21:28Z→21:53Z (+40 stamped runs; the live reading `resting checkpoint 28`) → return v2 21:55Z → the hub's delta re-audit 22:43Z. **≈5 h from launch to a landing-ready two-commit delivery; Nick's hands ≈15 min** (the dispatch paste, Act A's paste, two verification reads, one hivemind push).

**What it produced (assets, not activity).** (1) CI that reports every failing task with per-method XML and full messages, plus a veto-only sampling button — from FIX-1a on, every `main` red carries its own mechanism; the `dc3328b` class of "red with no message" cannot recur. (2) The bus's four silent drop points made visible (a typed signal, one WARN) and retried-then-SUSPENDED — a drop is never a skip; a checkpoint never advances past an undelivered position. (3) **A real product defect found at source and fixed with a deterministic pin**: the TRANSITION drain delivered a burst and never checkpointed it (a checkpoint that lied low; a restart would redeliver). Not the defect the hub predicted — better: measured, then fixed. (4) **A second mechanism named at source, not implemented** (the fan-out reorder — HeroLoop's likely cause), with a pre-registered runner prediction that decides it without theory. (5) A 216-run measurement corpus with its instruments filed (the affinity runner, the busy loops, the probe, the driver) — reusable, and the seed of BEYOND's B-1 bench row. (6) Three documents the plan needed anyway: the CG-1/2/3 instruction (grounded at source; Row 30 closed at zero Core lines), THE BEYOND INPUT, the v65 prompt.

**What it did not produce — said plainly.** The gate is NOT yet clear: the two pushes and their CI verdicts are Nick's next acts, and the runner — not the desk — is the instrument. HeroLoop's runner flake is probably NOT fixed by FIX-1b (0/90 on the desk means the desk never saw it; the reorder hypothesis says DP-2 does not touch it); the honest expectation is that a post-landing HeroLoop red arrives with `route_join_miss` and zero anomaly tokens, confirms FIX-2, and re-opens the gate one more time — with the mechanism named. That is the instrument working, and it must be read that way.

**Where the hub lost time or was wrong (each owned; each a mint).** Three predictions missed (pressure raises the rate — inverted; phase 2 — phase 1; the `:302` unpark — cannot open). A census regex miss and a folder mis-read (disclosed at the audit). **The ordering miss:** Act A was handed at 18:27Z to a lane that delivered at 19:59Z and executed the note at 21:28Z; the hub audited v1 and cut cards (9 · 7) that the tree no longer matched by the time Nick could act — the card's own guard would have stopped him, and a post-close beat (this one) re-audited at the delta (10 · 6). The instruction's gaps: no "separable bytes" shape for a two-commit lane (the lane invented the patch); the CPU-shape arm's direction assumed; the §A.5 diagnostic unverified against the artifact that would carry it. **The window:** six beats and the audit in one window was at the edge of context health; the close-then-reopen is the price, paid once.

**The judgment.** High on evidence quality and asset creation — the session converted an un-adjudicated CI red into two measured mechanisms, a self-describing CI and a permanent instrument, with every claim at the bytes. Medium on immediate green — by construction unknowable until the runner speaks. The lane out-performed the hub's theory three times and disclosed every adaptation; that is the operating model working as designed (the hub sets the instrument and the fences; the lane measures; the hub audits). The cost centre was the hub's own sequencing and the window budget, not the lane.

## §2 The plan from here — three horizons
**H1 (tonight → Sunday morning).** Act 7 (FIX-1a, 10 files) → CI = sample #4 → Act 8 (FIX-1b, the 6-file patch) → CI = THE GATE → three veto-only samples → Act 3 (decides ReplayIT's CI-red closure) → the hivemind push. v65 banks each verdict as one line and dispatches CG on the green. If a HeroLoop red shows the reorder signature: FIX-2 charters instrument-first (the `LIVE_OUT_OF_ORDER` token + the monotonic checkpoint guard; the deterministic pin the lane named), dispatches as the one core lane, and CG waits. Nothing here changes by this charter.
**H2 (the week).** CG → the FE fast-follow (v1.1.3 mirror) → H8 real-wire on FIX-1b's artifact (§6-B/EXITCODE riding) → the docket addendum 17–34 + the docs correction block (now also: INV-BUS-02's phantom rule name; AMD-45's fourth checkpoint writer note; the Doc 12/Doc 08 lines from FAILCHAN and PKG-SEC-2) → P-1 + F-R4-1b → R-4c/C-003 next weekend. **The refinement program's lanes (§3) run in PARALLEL to this chain — their write-sets are the hivemind, the skills repo and the docs repo, none on the core tree** (v1.2 §3 rule 2: parallel lanes, serial rulings).
**H3 (the runway).** BEYOND's words at a sitting; B-1's bench row after R-5; the skills and the spine kept at the shape that let one founder run this weekend's fronts — the program in §3 is v1.2 §3 rule 4 ("idle hub beats go to the spine's own upkeep") made a charter instead of a habit.

## §3 THE REFINEMENT PROGRAM — three lanes, each with a read-set, a census and a return
**Principle (W-SKILLS-6's shape stands):** SKILL.md files stay token-shaped and carry NO state; every law gained this week enters the `references/` ledgers as a rule WITH its mechanism and exhibit; the three source trees are mirrored to the account-synced skills by Nick and confirmed by Check 9 at the next boot.

### Lane A — W-SKILLS-7: the three role skills updated for deep-reasoning windows (a fresh Cowork window; write-set `nexsys-hivemind/{project-manager,coder}/` + `nexsys-skills/orchestrators/nexsys-frontend/`; hub-run commits at the bridge)
**Read-set:** the three SKILL.md + every `references/*.md` (the laws-ledger, coding-instruction-format, freshness-preflight, review-and-quality, repo-state-protocol; the coder's deviation-and-quality, testing-standards, java-patterns; the FE's build-and-ci-discipline, contract-consumer) · `context/lessons/pm-lessons.md` (THE v62 · v63 · v64 CLOSE MINTS) · `coder-lessons.md` (the last three entries) · the two v64 audits · this charter §1.
**The deltas (each a rule with a mechanism, placed where the reader that needs it reads):**
- *PM `references/laws-ledger.md`:* arc-disciplines 38–48 = the ten v64 mints + §9-L (the operator-note-invalidates-cards law), each with its exhibit path.
- *PM `references/coding-instruction-format.md`:* additions **#23 the separable-bytes law** (a lane ordered to deliver N commits from one tree names the delivery shape — tree + patches, or branches — in §0; the audit re-executes `git apply --check`) · **#24 the diagnostic-readability check** (every measurement row names the ARTIFACT and the FIELD it is read from; a diagnostic that no artifact carries is ordered as a stamp before the loops) · **#25 the measurement-shape law** (a prediction states the shape AND its direction, and the recipe carries the inverse arm — v64's red came from the arm the instruction called "collapses toward 0") · **#26 the operator-note law** (a hub note to a running lane states its census effect and schedules the delta re-audit; no card is cut while a note is outstanding).
- *PM `references/freshness-preflight.md`:* Check 9's instrument as actually run (per-file `md5sum` of the three SOURCE trees on the device vs the session's synced copies; 28 files at v64) written as the procedure; Check 10's basename resolution via `git ls-files` (a wide `find` on the mount times out).
- *PM `references/review-and-quality.md` §3:* "a return that refutes the instruction's mechanism theory with line cites is the best outcome of instrument-first" — the adjudication order (mismatches first; the theory owned in writing) as the standard.
- *Coder `references/deviation-and-quality.md`:* the patch-shaped delivery for multi-commit lanes as the standard `[REVIEW]` shape; the corpus convention (`SUMMARY.md` header + one line per run + a TOTALS line; RED runs' XML + log only; ≤400 KB; the redactor before filing); the two-stage red-first (compile-red at HEAD, then behaviour-red with inert hooks) as the pattern when a type does not yet exist.
- *Coder `references/testing-standards.md`:* the SLF4J-free-module gotcha (an IT's `system-out` is empty — the failure MESSAGE is the only channel; stamp it) · the Windows desk instrument (affinity pinning via `start /affinity`, the `ORG_GRADLE_PROJECT_*` env form for `-P` values under cmd, `--console plain` for full assertions, the probe for `availableProcessors`) as a reference block · "the desk is not the runner" as a rule on rate claims.
- *FE `references/contract-consumer.md`:* the v1.1.x additive pattern as a procedure (bump `CONTRACT_VERSION`/`EXPECTED_VERSION`; extend `contract.ts` types optional-nullable; `shapes.ts` validators; mocks that DO NOT always populate the new field — the H8 false-type class; the honesty tests) with v1.1.3's four keys as the worked example; the `outcome: CONFIRMED` + `resultOutcome: null` sentence.
- *All three SKILL.md:* byte-unchanged except a one-line `last-verified` pointer to the pass's return (the token shape is the asset).
**Census:** ~9 M across the two repos (declared exactly in the return); **return:** `context/audits/<date>_W-SKILLS-7_return.md` (§0 card ≤3 KB; a per-file delta table; the token count before/after per SKILL.md — must not grow). **Then Nick re-syncs the account skills; the next boot's Check 9 confirms 28 equal.**

### Lane B — REPO-CONTEXT: the hivemind's and the docs repo's currency (the hivemind half hub-run in v65's idle beats; the docs half = Nick's hands, one msg file + card)
- *Hivemind:* `context/process/window-budget.md` (NEW) — what one hub window may hold: the ~27 KB boot + ONE audit + ONE authoring block, the v6N skeleton at the tripwire, the close on the deliverable; the read-only corpus-check cadence (≤1 per 15 min) · the guarded-splice exemplar gains the placeholder-guard note (GitHub's `$`+double-brace expression) · `context/truth-map.md` and `canonical-paths.md` re-pointed at the v64 artifacts (the BEYOND INPUT, the CG instruction, the two audits, the corpus) · the docket gains rows FIRING-VALUE (Row 30 (i)), FIX-2 (the reorder; instrument-first), AMD-45-WRITER (the drain as a fourth checkpoint writer — a note, not an amendment, unless the docs say otherwise) · MODULE_CONTEXT currency verified at WUCP Phase 2 when FIX-1b lands (the lane updated both; the hub re-reads the SKIP-VIS-style banners).
- *Docs repo (Nick's hands — Block 6 of the v65 agenda, one commit):* INV-BUS-02's cited ArchUnit rule name does not exist among the 11 rules (correct the text to the rule that does, or strike the cite) · AMD-45's amendment note for the TRANSITION drain's per-SUCCESS checkpoint write · Doc 12 §8.4 :663 + §6.4/§6.6 + LTD-13's `Restart=on-failure` (FAILCHAN landed `Restart=always` with the reason) · Doc 12 :133 + Doc 08 :871 (PKG-SEC-2's notes) · the freeze-doc v1.1.3 amendment note (at CG's landing; hub-drafted).

### Lane C — THE DEEP-WORK WINDOW protocol (hub-authored, one process doc + one template refinement; no lane)
`context/process/deep-work-window_protocol.md` (NEW): how a window is shaped for a hard problem in code, research or testing — **one problem per window** (the v63/v64 corollary as a rule, not a habit) · the dispatch names the window's ONE deliverable and its read-set · **predictions filed before reads** (H12 for the hub's own work, as R1–R6 were) · source-first inside instrument-first (read the artifact's producer before ordering the run) · the boot lean (pointer-form beyond the listed reads) · the tripwire (the next window's skeleton at mid-session) · the close on the deliverable, never on the agenda's remainder · what goes to a lane vs stays with the hub (a lane for anything that runs; the hub for anything that rules). The v63 dispatch prompt gains two lines (the exemplar): "state the predictions you will adjudicate" and "name this window's one deliverable".

### Sequencing and cost
Lane A dispatches from v65 the moment CG is dispatched (parallel-safe: different repos), ≈2–3 h of lane time, ≈20 min of hub audit, ≈2 min of Nick (the re-sync + one push). Lane B's hivemind half rides v65's idle beats (≈45 min hub); its docs half is Block 6 (≈15 min of Nick's hands). Lane C ≈45 min hub, v65 or v66. **None touches the core tree; none waits on the gate; none delays CG, FE or H8.**

## §4 The fences (what this program does NOT do)
No skills rewrite in a window that has already carried an audit (this one) · no growth of any SKILL.md beyond a pointer line (W-SKILLS-6's token shape is the asset; laws go to `references/`) · no state in a skill file (the spine carries state) · no re-opening of ruled laws (the ledgers ADD arcs, never edit ruled ones) · no docs edits by the hub (Nick's hands; a msg file + card) · no relaxation of Check 9 (the mirror is confirmed at the bytes after every pass).

## §5 The words
- `REFINE: go | hold` — the program as chartered (rec **go**; it is v1.2 §3 rule 4 made explicit, and it costs the critical path nothing).
- `W-SKILLS-7: fresh-window | v65-inline` — rec **fresh-window** (a skills pass is deep work with a ~40 KB read-set; the v65 window owns the landing and CG).

## §6 Nick's words and guards (2026-09-05 ~18:xx CT, verbatim where they are words; recorded v64 beat 8)
**Words:** `REFINE: go` · `W-SKILLS-7: fresh-window` · `FENCE-BUS: add` (the register line written at this beat).
**Guards on the program (binding; each re-cuts §3's sequencing):** (1) **Lane C FIRST** — the deep-work window protocol is written before Lane A dispatches, so the skills pass runs under it. (2) **Lane A folds BOTH the v63 mints (ten) and the v64 mints into the ledgers in one pass; every SKILL.md byte-unchanged; Check 9 confirmed at the bytes after.** (3) **Lane B's docs half rides Block 6** so Nick's hands touch the docs repo once. (4) **Nothing in the program dispatches before the CG dispatch line is in Nick's hands.**
**The gate, pinned in Nick's words (supersedes any looser hub phrasing):** *"FIX-1a's push run is sample #4; FIX-1b's push run is the gate. A red on FIX-1b's run — HeroLoop or anything else — means the gate is NOT cleared: FIX-2 is the next and only core lane, CG waits, and the three veto-only samples are not taken until a push run is green. 'The instrument working' describes a red's readability, never its verdict."* Recorded as the law's wording; the hub's "a red is the instrument working" applies to sample #4's readability only.
**Sequence as re-cut:** the CG dispatch line handed (on the cleared gate) → Lane C (hub, v65) → Lane A dispatched in a fresh window under Lane C's protocol → Lane B hivemind half in v65's idle beats; the docs half at Block 6 → Check 9 at the next boot.
