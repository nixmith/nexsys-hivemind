<!--
file: context/planning/2026-09-06_v65-b6_post-landing_program_and_four-charters.md
purpose: The hub's ruling on Nick's Sunday brief (v65 beat 6, post-close): the CG landing banked · Dependabot #12 triaged at source and ruled · Nick's "what next" plan adjudicated item by item (agree / edit / refute, each with the fact) · his partial words on THE BEYOND recorded · the three horizons deepened · FOUR read-only lanes chartered in the deep-work protocol's five-part shape with one-paste dispatch lines (RS-12-F the fanciful-domus sprint · TR-0 the chokepoint census · TR-1 the position census · HERO-0 the null census) so three windows stay busy while the core slot waits for F-R4-1b · the calendar chain to the runway with its fulcrum named.
audience: Nick (§0 the commands · §4 the words · §5 the dispatch lines) · the v66 hub (§1–§3 the rulings of record; §6 what v66 authors next)
state-type: program (rulings + charters; every ruling REVERT-able by Nick's word; the spine outranks it)
status: AUTHORED v65 beat 6 (Sun 2026-09-06 ~09:41 CT; instrument 2026-09-06T14:41Z). The go-ahead for VERDOMO on Tuesday is NOT held by anything in this file.
-->

# The post-landing program — Sunday 09-06

## §0 Banked at the instrument (14:33Z = 09:33 CT)
- **CG-123 LANDED: core `f25291b`** (`e5fa035..f25291b`, 17 files, +641/−64), porcelain 0, in sync; **Nick: "All GitHub checks passed" → the push run GREEN = CI on CG's bytes, banked (law 16).** It is also an ordinary `main` run on FIX-1b's bus bytes → **OR-BUS-SILENT-DROP's passive count = 2 of 20** (`e5fa035` · `f25291b`); sample #4 (`5ed9178`) and the three veto-only samples still OWED. The hivemind is at `d04046b`, **ahead 6 — not yet pushed.**
- **Dependabot #12 — nanoid < 3.3.18 (CVE-2026-67213 / GHSA-2v37-7h3g-55p8; CVSS 8.2 High; EPSS 0.319 %), `web-ui/dashboard/package-lock.json`, transitive via vite 6.4.3 / vitest 4.1.10 / @preact/preset-vite → postcss 8.5.23 → nanoid 3.3.16.** Triage at source: the lock pins `3.3.16` (`package-lock.json:3397–3400`); no `overrides` in `package.json`; **`nanoid@3.3.18` EXISTS on the registry (published 2026-08-07; 3.3.17 08-03) and is inside every requiring range (`^3.3.16`)** — Dependabot's "cannot update" is a stale resolver read from three weeks ago (the alert has flip-flopped under the "development-scoped" preset rule ever since). **Exposure:** the vulnerable functions are `customAlphabet`/`customRandom` with an attacker-controlled `size` of 0; nanoid is a BUILD-TIME dependency of postcss here — it is not in the served bundle (no runtime consumer; the dashboard is static files served by Javalin) — so the household is not exposed; the alert is hygiene, and hygiene is cheap. **RULING: a lock-only bump, Nick's hands, ONE commit, BEFORE FE-113 dispatches** (a security fix never rides a feature commit; FE-113's baseline becomes this commit's sha). Act Q1 in the brief. **Docket row FE-SEC-1 (Lane B):** add `npm audit --omit=dev --audit-level=high` to `verify` so a RUNTIME-scoped advisory gates CI, and record the standing rule — a Dependabot alert on the core repo is banked as one spine line like a CI verdict, triaged by scope (runtime bundle vs dev), fixed lock-only in the next core-tree slot.

## §1 Nick's plan, adjudicated (agree · edit · refute — each with the fact)
1. **F-R4-1b with the real wire as its acceptance instrument; H8 as a fixed appointment — AGREE, with one sharpening.** Two hardware sessions are conflated under "H8": **H8-a** (this week, on `f25291b`'s CI-built artifact, 60–90 min): the four v1.1.3 keys read on the card = FE-113's live-wire bar (law #22) + the FAILCHAN §6-B/EXITCODE stop-proof riding free + a v1.1.3 wire CAPTURE for the dashboard's fixture; and **R-4c** (next weekend, AFTER F-R4-1b lands): the six-device fleet re-adopted — routers by rejoin, the sleepy SNZB-02P (`0x15ac`) by the ZDO `IEEE_addr_req` second surface — = **C-003**. F-R4-1b's completion register stays REPO-COMPLETE until R-4c reads the surface on the wire; the desk pins only the frame shape against the R-4b record's miss arm (`lookup_eui64_failed nwk=0x15ac status=0x1`). **Two calendar words:** `H8: <Tue|Wed evening>` · `R4C: <Sat 09-12 | Sun 09-13>`. The F-R4-1b coding instruction is v66's first authoring block (it needs the adapter's source read — `ZigbeeIntegrationAdapter`'s join/rejoin path and the EZSP ZDO frames — which this window has not made and will not fake).
2. **FE-113 "one lane, three things, one commit" — AGREE on the mirror; EDIT on two of the three.** (i) The bump: yes — `contract.ts:14` still reads `v1.1.2-2026-07-26`; FE-113 as authored. (ii) **"Regenerate fixtures from the endpoint census" — REFUSED as written:** `src/lib/api/fixtures/wire-2026-08-16-*` and `wire-2026-08-20-*` are RECORDED MEASUREMENTS of a v1.1.2 hub and the anchor of the ABSENCE honesty tests; a regenerated fixture is a mock wearing a fixture's name — the H8 false-type class. **The v1.1.3 fixture is H8-a's CAPTURE** (the emitter leads; a fixture is captured, never generated) → a follow-up **FE-113b** commit (the capture + its stability test) after H8-a. (iii) **The freeze note lives in `homesynapse-core-docs`** (your hands) — a different repo, so it cannot share the FE commit; it rides Block 6 (one docs touch) with the deviceId consistency-window fact. **"The hero from the empty states outward" — ADOPTED AS THE HERO CHARTER'S LAW:** the null census first (HERO-0, §5), the four honest empty states — not-fired · "no detail recorded" · `firingValue` null (V1-by-design) · confirmation-unknown (`outcome` without a `resultOutcome`) — designed before the happy path; a trust surface that cannot say "I don't know" is a liability. Scope fence: the three questions, nothing beyond.
3. **The chokepoint census — AGREE, chartered as TR-0 (§5), sharpened at source.** The actuation chokepoint is `integration/integration-runtime/…/CommandRoutingSubscriber.java:87` (`command_dispatched` → `CommandHandler.handle(CommandEnvelope)`, the interface at `integration/integration-api/…/CommandHandler.java:42`); the `handler.handle(envelope, processor)` at `core/state-store/…/DispatchingProjectionAdvancer.java:135` is a PROJECTION handler, not actuation — the census must not conflate the two. If `CommandRoutingSubscriber` is the only production consumer of `CommandHandler` and every producer (REST, the automation engine's `CommandAction`, scenes, `InvokeIntegrationAction`, the WebSocket API) publishes through `command_dispatched`, **B-2 is a bounded insertion at one line**; otherwise it is a refactor that gets dearer with every action type. The census also answers what a gate would SEE (the envelope's fields — a principal? the automation id? the correlation) — B-2's "principal grammar" precondition.
4. **FIX-2 authored ahead, dispatched on the signal — AGREE** (the instrument half — `LIVE_OUT_OF_ORDER` + the monotonic checkpoint guard — may be authored now; nothing dispatches on theory).
5. **REFACTOR-1 + the freeze note on Lane B, one docs touch — AGREE** (the CG audit §3 rows; Block 6).
6. **P-1 charters once F-R4-1b is in flight — AGREE, and it is the FULCRUM of the runway (§2).** An instrument built before it is needed; the MVP's 72-hour gate (kill −9 · coordinator disconnect · power-cycle recovery) cannot be run unattended without it.
7. **B-7's hash chain — decide, don't build; one ADR — AGREE.** The ADR needs one design read (Doc 04's event-store schema: does a `prev_hash`/`hash` column exist or can a nullable one be reserved without a migration?) → v66 drafts it (≤1 h) → your docs commit at Block 6 or the next. Reserving a nullable column now is cheap; re-hashing history next spring is not.
8. **The fences — ADOPTED AS LAWS:** no B-2/B-3 code before C-003 is minted AND OR-BUS-SILENT-DROP has closed on evidence · the hero's scope = the three questions · no FIX-2 on theory · **no batched pushes** (every push a sample) · no 50-device rig before the six prove the re-adoption path.
9. **Your partial words on THE BEYOND, recorded** (a sitting's words, quoted): *"no B-2 or B-3 code before C-003 is minted and the fence has closed"* · *"B-7's hash chain — decide, don't build … one ADR"* · *"P-1 rises with the runway"* · *"B-1 → B-2 at `CommandRoutingSubscriber` → B-3 shadow mode … → B-4 counterfactual replay on the REPLAY path"* · *"The harness enforces; the model proposes; the bus is the harness."* The re-lettering H10 (B-1/bus vs B-1/identity) is still open — `BEYOND-LETTERS: X | keep`.

## §2 The three horizons, deepened (what your framing did not yet say)
- **This week → 09-18.** Your order stands; the addition is PARALLELISM: the core slot holds F-R4-1b (one lane), while FOUR read-only lanes run in your spare windows — TR-0 · TR-1 · HERO-0 · RS-12-F — none on the core tree, none waiting on CI. The leverage acts, in order: the nanoid bump (5 min) → FE-113's paste → Tuesday's go-ahead → H8-a on the calendar.
- **To the runway (Nov-25).** The MVP acceptance has four parts and **each is an INSTRUMENT before it is a claim** (the register's D5 grammar): 72 hours = P-1 + the nightly · 50+ devices = the rig, after the six · six layered automations = the causal chain proving each (the hero shows them) · the event-loss audit = **TR-1's position census** — the same instrument that closes OR-BUS-SILENT-DROP on evidence (not on twenty quiet runs) and mints B-1/bus. **One instrument, three uses → TR-1 charters NOW, research-only.** The calendar chain with its fulcrum: F-R4-1b (wk 2) → R-4c/C-003 (09-12/13) → **P-1 chartered (wk 3) and BUILT (wk 3–4)** → the 50-device rig acquired and joined (Oct wk 1–2, after the six) → the 72-hour run (Oct wk 3–4) → 11-25 with ≈3 weeks of slack. **P-1 is the fulcrum: a week's slip there is a week off the 72-hour run.**
- **P4.** B-1 → B-2 at the chokepoint → B-3 shadow → B-4 replay, as you wrote — with two sharpenings. (i) **The minting order is B-3 before B-2's claim:** "the harness enforces" is minted (D5) only on a measured object — a gate that REJECTED N proposals on the card — and that measurement IS shadow mode; so B-2's first register sentence is written by B-3's corpus, never by B-2's code. (ii) **The 2029 test on B-2's placement:** glad — one door, every actuation through it, the gate one line before `handler.handle`; regret — a second path (a direct adapter call from a scene or the WebSocket API) that bypasses the door and makes the promise false in public; the separating fact — TR-0's census.
- **Refutable-by (yours, held):** TR-0 finds a second actuation consumer → B-2 is a refactor, scheduled before more action types land · the runner never shows the FIX-2 signature across the twenty samples → the ordering hypothesis retires · H8/R-4c shows the ZDO surface insufficient for the sleepy devices → C-003's sentence is re-cut to what the wire proved, never widened.

## §3 The brand — Nick's "fanciful domus" thought, ruled (no name graded here)
**The question:** whether a fanciful -DOMIS family (and your two examples) deserves an independent, targeted research session before more time goes to the idea. **The frame's law:** an RS-12 is worth a day ONLY to flip a GATE, and a taste question is never a reason to slip Tuesday. **The ruling: YES to the research, NO to any hold** — charter **RS-12-F** (§5) to run Sunday/Monday in a fresh window, read-only, ZERO calendar cost (counsel's desk is closed), its output scoped to the **REDIRECT slot** (the on-sight redirect clause's name) and the **RUNNER-UP slot** (the second and last paid search if the opinion is bad) — the two places a better cell can still enter without touching Tuesday's send. Facts the lane inherits and must not re-argue: RS-11's annex (a suffix swap changes the OWNER of the containment, it does not exit it; -DOMUS is occupied on all three axes; the ZEN-/ZENDO S1 kill mechanism is measured, so any ZIN-/ZEN- cell must clear the same S1 test, not skip it) and RS-10's family verdict. Its return lands on the decision card as a POST-HOC row, scored on the same frame, beside the control AND beside VERDOMU. **Your word `SEARCH-NAME:` is still open; nothing here waits on RS-12-F, and RS-12-F waits on nothing but your paste.**

## §4 The words this beat asks for (one message, no rush except the first two)
`SEARCH-NAME: <…>` (the card §3) · `ASR-VERDOMU: pass <spellings> | fail` · `RS10-STAR: <line>` · **`H8: <Tue|Wed evening>`** · **`R4C: <Sat 09-12 | Sun 09-13>`** · `BEYOND-LETTERS: X | keep` · `DESIGN: start | hold` · `BLOCK6: pull | hold` · the six owed lines (sample #4 · the samples · Act 3 · nightly 09-05 · nightly 09-06) · W-SKILLS-7's state.

## §5 The four read-only lanes — chartered in the protocol's five-part shape (dispatch lines at the end of each; paste into a FRESH Cowork window with `ClaudeFolder` connected; every lane: read-only, stage nothing, commit nothing, ONE return file, `date -u` first, its last line `RETURNED <path> <bytes>`, reports to the hub)

### RS-12-F — the fanciful-domus sprint (a research lane; brand)
**Deliverable:** `nexsys-hivemind/context/research/<CT-date>_RS12F_fanciful-domus-sprint_return.md` ≤20 KB, §0 THE BRACKET CARD ≤3 KB first. **Read-set, in order:** the pre-spend charter §0 + §2 + §3 · the RS-11 return whole (its §0A annex is law: the containment moves, it does not exit; the ZEN- mechanism) · the RS-8 return §0 + §1 (the inverted screen's rows and the control's own row) · the decision frame §1 (the gates and weights this lane's cells will be scored on) · the RS-9 return §A1–§A2 (VERDO.AI; the DOMO precedents) · nothing older. **The job:** Nick's fanciful forms of *domus* — the -DOMIS family first (his two examples as Job A), then ≤12 further fanciful forms he would recognise as *domus*-derived (Job B; each with its etymological warrant stated: a real Latin/Romance/Slavic form, or an honest coinage labeled as such) × the prefixes RS-8/RS-11 already screened (VER- · HUSH- · KEEL- · ABI- · NID- and ZIN-/ZEN- ONLY through the ZENDO S1 test) — ≤24 cells; S1 → S2 (in two parts: contains DOMO / shares DOM-) across the whole grid FIRST (the kills are cheap), then S3–S6 on ≤8, then the §2.3-style deep dive on the top 3 — **with the listener/ASR test RUN this time** (RS-9 §6's 5-minute protocol, the lane's own voice or TTS → ASR, spellings counted) so S4 is measured, not inferred. **Two controls beside every cell: VERDOMO and VERDOMU.** S7 provisional; NO recommendation; the frame's gates and weights applied by the hub, not the lane. **Predictions (adjudicated first in §0):** P1 ≥1 -DOMIS cell clears both gates · P2 every ZIN-/ZEN- cell fails S1 on the ZENDO mechanism · P3 no cell beats VERDOMU by ≥10 of 85 on the frame's S1–S6 · P4 the listener test moves at least one cell's S4 by ≥2 points from the inferred grade. **Fences:** read-only web; RDAP only for domains; no contact, no purchase, no watchlist; nothing from memory as a finding; no name outside the return. **Clock:** 3–5 h; a living §0 from hour one; a partial return Monday 18:00 CT outranks a complete one Tuesday.
```
date -u first. You are the RS-12-F research lane for NexSys/HomeSynapse. Read nexsys-hivemind/context/planning/2026-09-06_v65-b6_post-landing_program_and_four-charters.md §3 + §5 "RS-12-F" WHOLE, then its read-set in order and nothing older. Execute the job exactly: the -DOMIS family first (Nick's two examples as Job A), then ≤12 fanciful domus forms with their warrant, × the screened prefixes; ≤24 cells; S1 → S2 (two parts) across the whole grid first, S3–S6 on ≤8, the deep dive on the top 3 with the listener/ASR test RUN; VERDOMO and VERDOMU as controls beside every cell; S7 provisional, no recommendation. Adjudicate the four predictions first in §0. Read-only; RDAP only; no contact, purchase or watchlist; nothing from memory; no name outside the return. Write nexsys-hivemind/context/research/<today's CT date>_RS12F_fanciful-domus-sprint_return.md (≤20 KB; §0 THE BRACKET CARD ≤3 KB first). 3–5 h; a partial return by Monday 18:00 CT outranks a complete one Tuesday. Your last line: RETURNED <path> <bytes>. You report to the hub, not to me.
```

### TR-0 — the actuation chokepoint census (a source-read lane; the coder skill; read-only)
**Deliverable:** `nexsys-hivemind/context/audits/<CT-date>_TR-0_actuation-chokepoint-census_return.md` ≤10 KB — ONE PAGE OF FILE:LINE RECEIPTS. **Read-set, in order:** `context/process/deep-work-window_protocol.md` §2/§6 · `integration/integration-api/…/CommandHandler.java` + `CommandEnvelope.java` · `integration/integration-runtime/…/CommandRoutingSubscriber.java` whole · every `MODULE_CONTEXT.md` of `integration-runtime`, `core/automation`, `api/rest-api`, `api/websocket-api` (if present), `lifecycle` · then `git grep` as the instrument. **The questions, each answered with receipts:** Q1 every production consumer of `CommandHandler` / `commandHandler()` (is `CommandRoutingSubscriber:87` the only one?) · Q2 every PRODUCER of `command_dispatched` and of `command_issued` (REST command endpoints · the automation engine's `CommandAction` · scenes / `ActivateSceneAction` · `InvokeIntegrationAction` · the WebSocket API · lifecycle) — do REST and automation commands share ONE ledger path through the bus? · Q3 what a gate inserted immediately before `handler.handle(...)` in `CommandRoutingSubscriber` would SEE: list the envelope's fields with types; is there a principal? an originating automation/run id? a correlation id? · Q4 any path that reaches an adapter WITHOUT passing that line (a direct adapter call anywhere in production code) · Q5 the projection handler at `core/state-store/…/DispatchingProjectionAdvancer.java:135` — confirm it is not an actuation path. **Predictions:** P1 `CommandRoutingSubscriber` is the ONLY production consumer of `CommandHandler` · P2 REST and automation commands both publish `command_dispatched` through the same bus path (one ledger) · P3 the envelope carries NO principal today (a B-2 precondition gap → a docket row) · P4 zero direct adapter calls outside the subscriber. **Fences:** read-only — no edit, no build, no test run; every claim a `path:line`; a claim without a receipt is not made. **Clock:** ≤2 h.
```
date -u first. You are the TR-0 source-read lane for NexSys/HomeSynapse — boot as the nexsys-coder skill in READ-ONLY mode: no edit, no build, no test run, stage nothing, commit nothing. Read nexsys-hivemind/context/planning/2026-09-06_v65-b6_post-landing_program_and_four-charters.md §5 "TR-0" WHOLE, then its read-set in order. Answer Q1–Q5 with file:line receipts in homesynapse-core at HEAD (state the sha); adjudicate P1–P4 first in §0; a claim without a receipt is not made. Write nexsys-hivemind/context/audits/<today's CT date>_TR-0_actuation-chokepoint-census_return.md (≤10 KB, §0 card first). ≤2 h. Your last line: RETURNED <path> <bytes>. You report to the hub.
```

### TR-1 — the position census as a bench verb (a source-read + design lane; read-only)
**Deliverable:** `nexsys-hivemind/context/research/<CT-date>_TR-1_position-census_bench-verb_design_return.md` ≤12 KB. **Read-set:** THE BEYOND INPUT §2 B-1 (the claim in D5 form; the measurement named) · the FIX-1 audit §8 (OR-BUS-SILENT-DROP's closure rule R2) · `core/event-bus` `MODULE_CONTEXT.md` + `InProcessEventBus`'s checkpoint model (per-subscriber checkpoints; `readCheckpoint`) · the checkpoint store(s) at source — the in-memory one the ITs use and the persistent one on the card (`core/persistence`, the SQLite schema) · the event store's position semantics (Doc 04 by pointer; `MODULE_CONTEXT`) · `nexsys-bench`'s verb/row conventions (the nightly's digest line; one existing verb as the exemplar) · `context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md` §0 (the corpus's shape). **The questions:** Q1 the census's exact definition: for every published position P and every subscriber S whose filter matched P — was P delivered to S (checkpoint ≥ P after the drain) — derivable from the event store × the checkpoint store WITHOUT a Core read seam? · Q2 the data shape and its cost per night on the card (rows × subscribers; SQLite reads) · Q3 the bench verb's shape (`bus-delivery-under-pressure`: the two ITs ×K on the Pi under a load shape the card can produce; the token tail; the census) and what it must NOT touch (s31/the nightly's floor until R-5) · Q4 the three uses stated as one instrument: OR-BUS-SILENT-DROP's evidence closure · the MVP's event-loss audit · B-1/bus's minting measurement — and the D5 sentence each would mint. **Predictions:** P1 the census is derivable from the two stores alone (no Core seam) · P2 the per-night cost is < 1 s of SQLite reads at household scale · P3 the persistent checkpoint store's schema already carries a per-subscriber position row (no migration). **Fences:** read-only; no code; s31/nightly HANDS OFF; the design is a proposal for a bench row, not a Core change. **Clock:** ≤3 h.
```
date -u first. You are the TR-1 design lane for NexSys/HomeSynapse — read-only: no edit, no build, no run, stage nothing, commit nothing. Read nexsys-hivemind/context/planning/2026-09-06_v65-b6_post-landing_program_and_four-charters.md §5 "TR-1" WHOLE, then its read-set in order. Answer Q1–Q4 with file:line receipts (homesynapse-core at HEAD, sha stated; nexsys-bench at HEAD); adjudicate P1–P3 first in §0. Write nexsys-hivemind/context/research/<today's CT date>_TR-1_position-census_bench-verb_design_return.md (≤12 KB, §0 card first). ≤3 h. Your last line: RETURNED <path> <bytes>. You report to the hub.
```

### HERO-0 — the null census of the v1.1.3 read-API (a contract-read lane; the frontend skill; read-only)
**Deliverable:** `nexsys-hivemind/context/research/<CT-date>_HERO-0_null-census_v1.1.3_return.md` ≤10 KB — a TABLE: every endpoint × every key that can be `null` or ABSENT × what each null MEANS at source × the honest sentence the hero would show. **Read-set:** `web-ui/dashboard/src/lib/api/contract.ts` + `shapes.ts` (the validators enumerate optionality) · `FRONTEND_DOCTRINE.md` + the dashboard `MODULE_CONTEXT.md` (§10-H/§10-I honesty; the tri-state idiom) · the CG audit §0/§3 (the four keys' null semantics; the deviceId consistency window) · the CG instruction §DP-5 (`resultOutcome` null beside `outcome: CONFIRMED` = the honest happy path; `firingValue` V1-by-design) · the serializers at `f25291b` (`GetRunCausalChainEndpoint` · `GetNonFiringEndpoint` · `ListAutomationsEndpoint` · `ListEntitiesEndpoint`) — the emitter's `null` sites are the truth · the two recorded fixtures (what a real hub actually served). **The questions:** Q1 the table · Q2 the four hero empty states written as microcopy (not-fired · "no detail recorded" · `firingValue` null · confirmation-unknown), name-light, i18n-keyed · Q3 every question of the three the v1.1.3 contract CANNOT answer honestly → an EXPLAIN-n row each (never a mock that lies). **Predictions:** P1 ≥12 nullable/absent keys across the six reads · P2 at least one null the validators tolerate has NO documented meaning at source (→ a freeze-note row) · P3 "why didn't it fire" has ≥1 verdict the hero cannot render without a Core key. **Fences:** read-only; no pixel, no component, no edit; the hero's scope = the three questions. **Clock:** ≤2 h.
```
date -u first. You are the HERO-0 contract-read lane for NexSys/HomeSynapse — boot as the nexsys-frontend skill in READ-ONLY mode: no edit, no build, stage nothing, commit nothing. Read nexsys-hivemind/context/planning/2026-09-06_v65-b6_post-landing_program_and_four-charters.md §5 "HERO-0" WHOLE, then its read-set in order (homesynapse-core at HEAD, sha stated). Produce the null-census table, the four empty states as microcopy, and the EXPLAIN-n rows; adjudicate P1–P3 first in §0. Write nexsys-hivemind/context/research/<today's CT date>_HERO-0_null-census_v1.1.3_return.md (≤10 KB, §0 card first). ≤2 h. Your last line: RETURNED <path> <bytes>. You report to the hub.
```

## §6 What v66 authors next (in order), and the alternative shape
The F-R4-1b coding instruction (source-first on the adapter; H8/R-4c the gate) → the H8-a packet (the R-4b pattern; guard 1 = `f25291b`'s artifact by the Actions page) → the go-ahead email + the RS-10 appendix page → the B-7 ADR draft → FIX-2's instrument half ahead → the four lanes' audits as they return (each under its pre-filed predictions). **The alternative shape this program did not take:** run the census work as hub blocks in v66 instead of four lanes — rejected on the protocol's §6 and §8: a census is a READ that runs, and four of them would spend v66's whole window on reading while its one deliverable (F-R4-1b's instruction) waits; four windows of Nick's cost four pastes and return four files the hub audits in an hour.
