<!--
file: context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_assessment_trajectory_and_hygiene-program.md
purpose: Nick's ask (Sun 2026-09-06 ~11:5x CT): "your full, comprehensive assessment on all the work accomplished across all sessions … fully plan out and optimize how exactly we proceed from here in terms of coding, research, testing, etc. … synthesizing … the bigger picture … clean up the nexsys-hivemind repo (and the rest of ClaudeFolder/) for outdated or stale information that will mislead future sessions … ensure our Claude skills are fully optimized, contextually." This is the hub's synthesis AS A DELIVERABLE (the v65 directive: thinking arrives as files, not tone): §1 what exists and is proven · §2 the honest assessment (what works, what costs, what is thin) · §3 the trajectory re-cut as ONE program with its critical path and its lanes · §4 the hygiene program HIVE-CLEAN-1 (a lane) · §5 the skills program W-SKILLS-8 (a lane) · §6 the operator model · §7 the words. Every claim carries its source; every proposal its cost and its refutation.
audience: Nick (rules on §7; reads §0 first) · the v66/v67 hub (the plan of record once adopted) · the HIVE-CLEAN-1 and W-SKILLS-8 lanes (§4/§5 are their charters)
state-type: assessment + plan of record (PROPOSED until Nick's `PLAN:` word)
status: PROPOSED v66 beat 6 (Sun 2026-09-06; instrument ≈17:1xZ). Grounded on: the spine (pm-handoff v55→v66), today's five intakes (CG-123 · FE-113 · TR-0 · HERO-0 · the nanoid bump), the R-4/R-4b records, the program (planning/2026-09-06_v65-b6_…), the BEYOND input, the decision card, and a file census of the hivemind run at 16:5xZ (966 tracked files; 539 live context .md; 344 archived).
-->

# The state of the program — assessment, trajectory, hygiene (2026-09-06)

## §0 One screen
**What is real today (`d192d17`, CI green):** a Java 21 event-sourced hub that boots clean on a Pi from a CI-built `.deb`, resumes its Zigbee network without forming a new one, adopts a silently-rejoined mains router through the interview path in 315 ms, runs an automation on real hardware and renders a causal chain whose action reads `CONFIRMED` from the device's own report (**C-002**, minted 2026-09-04 on `ef02d13`); a bus whose four silent drop points are now tokenized and retried-then-suspended (FIX-1a/b) with a passive clean-run count of **4/20**; a unit that grades a clean stop as `success` (FAILCHAN — the hardware proof is today's rig); a read-API at **v1.1.3** (four additive keys, CG-123) with the dashboard mirror **landed this morning** (FE-113; live-verification pending today's H8-a); a config-schema fragment composed at Phase-1 validation (PKG-SEC-2, proven on the card). **What is not yet real:** the six-device fleet re-adopted on the shipped artifact (**C-003** — waits on F-R4-1b + R-4c), the power-harness instrument (P-1), the 72-hour unattended gate, a hero screen, a name, a hash chain (the schema is chain-ready; the chain is not computed — §2.6).
**The three things this document changes:** (1) the next four weeks are ONE critical path with named lanes beside it, not a menu (§3); (2) the hivemind gets a hygiene lane with a deterministic recipe, because a cold-boot session following `status:` lines today finds 37 "LIVE" handoff files and 47 "ISSUE-READY" instructions of which roughly four in five are executed (§4); (3) the skills get the pass that never ran, plus the six laws this weekend minted (§5). **The words this asks of Nick are seven, none urgent before Monday (§7).**

## §1 What the sessions accomplished (v55 → v66; twelve days)
**Code that landed on `main`, each with its CI sample:** F-R4-1 (interview-on-rejoin; two admission triggers into the ONE interview path) → PKG-SEC-2 `ef02d13` (the fragment composed pre-start; zero `Configuration issue` on the card) → FAILCHAN `7af2d6c` (the unit stops lying: `SuccessExitStatus=143` · `Restart=always` keyed to the `ExitCode` contract; the orderly-path sweep) → FIX-1a `5ed9178` + FIX-1b `e5fa035` (the bus's silent drops tokenized, retried, suspended; the TRANSITION drain checkpoints) → CG-123 `f25291b` (v1.1.3) → the nanoid lock bump `093d5b4` (Dependabot #12 closed) → FE-113 `d192d17` (the mirror; 274/274). Seven landings, seven green gates, zero reverts.
**Measurements that exist on file (the corpus is the asset):** R-4 (08-30) and R-4b (09-04) operator records — 37 ⏺s, 13 deviations all T1/T2, zero STOPs; the hit AND the miss arm of the 0x0061 hop on silicon; the bench floor `[PASS] 6/6` and the 09-04 nightly 8/9; the 216-run FIX-1 corpus; the two recorded wire fixtures (08-16, 08-20). **Claims minted:** C-002. **Fences adopted as laws:** one lane on the core tree · no batched pushes · the rig exclusive · no B-2/B-3 code before C-003 + the bus fence closed · the hero = three questions · no FIX-2 on theory · no 50-device rig before the six.
**The brand program:** RS-8 → RS-11 filed; the decision card under a pre-registered frame (VERDOMO 63 · VERDOMU 67 · HUSHDOMA 65 · VERDOMA 54 of 85; no displacement); `SEARCH-NAME: VERDOMO` provisional, re-given Tue 07:00 after RS-12-F's row; Erik's go-ahead Tuesday with the one-page RS-10 appendix; the hard stop 09-18; the paid cap two searches, untouched.
**The strategy layer:** Strategy v1.2 ratified (09-03); THE BEYOND input (B-1…B-8, the dependency graph, the fences); the Substrate Thesis v0; the north star (the harness enforces; the model proposes; the bus is the harness — Nick's words).
**The method:** two-layer audits at the bytes with disclosed non-re-executions; predictions pre-registered and adjudicated first; census-exact commits; instrument-first on a repeat failure; the deep-work-window protocol; and this morning **THE OPERATOR-LOAD LAW** — one act at a time, the hub holds the list. **Today alone, under the law:** five lanes dispatched by one-line acts, three returns audited, one landing, the H8-a packet, the F-R4-1b instruction, BLOCK 6 applied, the freeze doc at v1.1.3, a B-7 ADR — in 2 h 40 m of the hub's window and about nine minutes of Nick's hands.

## §2 The honest assessment
**2.1 What works, and must not be diluted.** The evidence culture: nothing is claimed that was not measured, and the measurements are files with paths. The audit shape: layer 1 reads claims critically, layer 2 re-executes at the bytes, and what could not be re-executed is DISCLOSED — today it caught nothing false in three returns because the lanes were shaped by pre-filed rules; that is the point. Lanes as write-isolated, capped, one-file returns. CI as the only gate of record. The commit boundary (Nick's hands on core/docs/bench; the hub's on hivemind/skills). The predictions: the hub's misses today (P1 on FE-113's census; the b1 P2) are recorded AS misses — the base rate is the asset.
**2.2 The hub's own defect class — grounding lines made from partial instruments.** Four instances in one day: "`EXPECTED_VERSION` does not exist in the tree" (grepped `.ts` only; it lives in a `.mjs`); "guard 1 = `093d5b4`'s run" (the `.deb` is built only by `install-smoke`, whose path filter skipped the lock bump); "the freeze note lives in the docs repo" (it lives in the hivemind); "reserve a nullable column" (AMD-37 reserved it NOT NULL in May). Each was caught — by a lane, or by the hub's own second look at source before authoring — and none shipped. But the class is structural: **a grounding claim is only as good as the instrument that made it, and the instrument is never written down beside the claim.** The fix (§5, into the PM skill): every source-state claim in an instruction carries its instrument in a parenthesis — `(git grep -n EXPECTED_VERSION -- web-ui = 0 hits)` — so the lane re-runs the command, not the conclusion. Cost: one clause per claim. Refutable-by: a lane that re-runs and finds the same wrong answer (then the instrument was wrong, which is a different, rarer class).
**2.3 The prompt-as-living-document is expensive.** The v66 prompt was re-cut four times before v66 booted (b4 → b5 → b6 → re-authored whole at b7). The skill already says "this file carries no project state"; the prompt still carries §2 "the state of the prize" and §8 "first acts" that go stale by the hour. **Proposal:** the orchestrator prompt becomes STABLE — §0 laws by reference to the skill's ledger, §1 the boot procedure, §8 = one line: "read the brief's §HELD-BY-THE-HUB and the newest beat; the next block is whatever they say" — and the brief's §HELD is the ONLY volatile document (it already is re-printed every beat). A prompt that does not change between sessions needs no re-authoring; the session number becomes a beat-1 fact, not a file. Cost: one re-authoring (v67's), then zero. Refutable-by: a boot that needs a fact §HELD cannot carry (none found in twelve sessions).
**2.4 Capped lanes write to the cap.** Three of three today: 12,223/12,288 · 10,225/10,240 · 9,996/10,240. The cap is read as a target; the hub then reads more than it needs. **Change the sentence** in every dispatch (§5): "≤ N KB is the CEILING; the §0 card is what the hub reads first; a shorter return with the same receipts is a better return." Refutable-by: a lane whose receipts genuinely need the space (then the cap was wrong, and the return says so in §0).
**2.5 The operator queue.** The law minted itself on the b6 brief (eight acts + ten words in one message). Under it today the pace was right — one act, one line, the hub holds the rest. Two refinements from the day: (a) a report-back that pastes a lane's whole closing message is more than one line — the `RETURNED <path> <bytes>` line is the whole report because the hub reads the FILE; (b) the FE-113 landing waited ~40 minutes behind HERO-0's paste because the law forbids stacking — right in general; the exception worth writing: **a landing that releases a core slot outranks a read-only dispatch** (the hub asks the one word "dispatched | not yet" and re-orders). Both go to the law's mechanism paragraph (§5).
**2.6 What is thin — the evidence the program does not yet have.** (a) **The fleet:** C-003 needs the sleepy device through a surface the desk cannot exercise (F-R4-1b's ZDO request; the parent-buffer window vs the poll cadence is physics only the rig answers). (b) **Unattended time:** the bench floor has been HANDS OFF since 09-02 (the s31 class adjudicated, the R-5 charter pending); no soak run since the August gate; P-1 (the power-harness primitive) is chartered nowhere yet and the 72-hour gate cannot run without it. (c) **The dashboard:** never live-verified until today's H8-a; HERO-0 found the client validator REJECTING two lawful nulls (`actions[].command`, `trigger.subjectRef`) while the mocks populate both — the manufactured-type class the live-wire law exists to kill, still present at `d192d17`. (d) **The read-API's honesty:** HERO-0's EXPLAIN-1…10 — the hero cannot say WHAT set an automation off (no `subjectRef` on the triggering event), WHAT reading (no firing value), WHICH run started this one (`parentRunId` always null), WHEN a confirmation arrived, or "never" across a reload (instance-scoped ids). And **F1 is a defect on the wire NOW:** `lastReported` is seeded at first sight, so a never-reported entity serves its registration stamp as a report time — the CG-3 freshness key can lie. (e) **The chain:** the north star says "hash-chained"; the store binds a zero vector on every row (the B-7 ADR, filed today, decides the activation shape and forbids the phrase until it is true). (f) **The name:** every public surface is `{{NAME}}`-tokenized; the opinion by 09-18 or the rename slips to the 10-31 fallback.
**2.7 What is stale in the tree (the census, 16:5xZ).** 539 live `context/*.md` files. By `status:` line: **handoff** 37 LIVE/READY/CURRENT of which ~31 are executed or superseded (orchestrator prompts v60–v65, nine operator packets from July–August, three navigator prompts, dispatch texts, the v61 brief); **instructions** 47 ISSUE-READY/DISPATCH-READY of which ~42 are executed (from SKIP-VIS 07-26 to CG-123); **planning** 16 CURRENT/ACTIVE of which the May–June plans, the `weeks/` tree (retired by Nick 08-09) and the two June frontend drafts are historical; **programs/matter-design** 9 READY/ACTIVE since 07-19 with no beat since; **strategy** 27 files with no status line; **pre-verifications** two ISSUE-READY from July. The audits/assessments/research trees are EVIDENCE — their FILED/COMPLETE statuses are correct and permanent; they are not clutter. Outside the repo: `_scratch/` is 185 MB, 409 entries, eleven session directories back to v56; `ClaudeFolder/START_HERE.md` is a pointer (fine); the hivemind's `START_HERE.md` is state-free but last verified 07-04. **The risk is specific:** a fresh session that greps for "ISSUE-READY" or "LIVE" — or a Claude Code lane that reads the instructions directory — is offered forty executed instructions as current work. The newest-file rule protects the hub's boot; it protects nothing else.
**2.8 The throughput constraint we keep paying.** "One lane on the core tree" serializes the dashboard with Core because both live in `homesynapse-core`. Today FE-113 and the core slot never collided only because nothing else was ready. The law exists for one reason — census-exact landings — and that reason is satisfied by DISJOINT WRITE-SETS: `web-ui/dashboard/**` never overlaps a Java WU. **H10 (§7 word 2):** amend to "one lane per PATH-DOMAIN on the core tree (Java · web-ui), each landing staged by its domain path (`git add -A <domain>`), never `-A .`"; the census card names the domain. Cost: one sentence in the law; a `git add` discipline. What it buys: the hero and FE-NULL-1 run beside F-R4-1b and the v1.1.4 batch instead of after them — roughly one calendar week before 11-25. Refutable-by: one landing whose porcelain shows both domains dirty (then the card STOPs, as it does today).

## §3 The trajectory — one program, one critical path (adopt or edit by row)
**The gate everything serves:** the MVP's 72-hour unattended run on the six-device fleet with the hero rendering the three questions — target **2026-11-25** (the program §2; ≈3 weeks' slack today).
**The critical path (hardware-bound; one core lane at a time):** **F-R4-1b** (the instruction is on disk; dispatches on `d192d17` — this week, ≤1 desk-day) → **R-4c** on the held card (`R4C:` — rec Sat 09-12; the six re-adopted; the sleepy SNZB-02P by the ZDO surface = **C-003**) → **P-1 chartered** (the power-harness primitive as a bench verb: the adopted S31 as a software-addressable mains switch with its own state report as proof; weeks 3–4) → **R-5** (the bench floor re-baselined on the fleet; the s31/nightly hands-off ENDS; the nightly becomes the soak's instrument) → **the 72-h run** (Oct wk 3–4: kill −9 · coordinator disconnect · power-cycle recovery, all driven by P-1) → 11-25. Every step is an INSTRUMENT before a claim (the program §2's law).
**The honesty batch on Core (one small WU right after F-R4-1b, before the big one):** **LASTREPORTED-1** (F1: seed `lastReported` null until the first `state_reported`, or add `firstSeen` — H10 §7 word 3; the hub recommends seed-null: the key then means what the freeze says) + **ORIGIN-1** (TR0-3: `command_dispatched.origin` inherits the issued envelope's origin; `actorRef` for REST) + **TR0-1** (the `commandEventId` javadoc). Three value corrections, no wire-shape change, one lane, one CI sample.
**The product's differentiator — the v1.1.4 EXPLAIN batch (Core, after the honesty batch; the biggest WU of the month):** the additive keys HERO-0 proved the hero cannot render without — `firingValue` (EXPLAIN-1; FIRING-VALUE was V1-deferred by design and its deferral is now measured as the hero's first hole), the triggering `subjectRef` on `automation_triggered` (EXPLAIN-2), `parentRunId` (EXPLAIN-3), the condition TEXT on `condition_evaluated` (EXPLAIN-4), a stable definition key on runs and non-firing (EXPLAIN-5), `FIRED_CONFIRMED` as a verdict (EXPLAIN-6), `disabledAt` (EXPLAIN-8), `confirmedAt`/`settledAt` per action (EXPLAIN-9). One bump, the CG-123 pattern, the emitter leads; the FE mirror follows as FE-114. **This is where the household feels the difference; it is the WU the runway's slack should buy.**
**The lanes beside the path (under the cap; disjoint write-sets; each an instrument first):**
- **FE (web-ui domain):** FE-NULL-1 (F2: four `| null` types + the null-arm mocks — a fast-follow on `d192d17`, ≤½ day) → **FE-113b** (the H8-a capture → a recorded fixture + its stability test) → **the hero charter** — `DESIGN: start` is lawful now: from the four empty states HERO-0 wrote (not-fired · "no detail recorded" · `firingValue` null · confirmation-unknown) outward, under the `{{NAME}}` token, the three questions only; the FE lane designs and builds it against v1.1.3 + the EXPLAIN rows as EXPLAIN-n placeholders that say "the hub doesn't record this yet" — never a mock that lies → FE-114 when v1.1.4 lands.
- **Bench / testing:** **TR-1** (the position census as a bench verb — its "complete" criterion PRE-REGISTERED before any run; it closes OR-BUS-SILENT-DROP on evidence and mints B-1's sentence) → **P-1** → **R-5**. The passive count reaches 20/20 by itself in ~2 weeks of ordinary pushes; the three veto samples bank tonight. FIX-2 stays on its signal (never on theory).
- **Docs:** BLOCK 6 (applied; Nick's commit after the rig) → the B-7 ADR (ratify; the docs copy) → the LTD-13 config-directory ruling → the v1.1.4 freeze note when it lands. One touch each, hub-applied splices, Nick commits.
- **Brand (its own calendar):** RS-12-F (running) → its POST-HOC row on the card → `SEARCH-NAME:` re-given Tue 07:00 → the Erik send Tuesday with the RS-10 appendix (Monday's Block 4) → the written opinion → **09-18** → the rename program (Tier-2 identity work continues privately; the wordmark last). `ASR-VERDOMU:` and `RS10-STAR:` before the send.
- **Strategy (touch-only until C-003):** the north-star wording ("chain-ready", never "hash-chained") · the BEYOND's Q1 answered · B-2's shape recorded (TR-0) · nothing built.
- **Research: none new before C-003.** The runway's fulcrum is hardware and the desk has what it needs; RS3-WMARKET-2 stays on its cadence; the Matter program is PAUSED by status (§4), not by neglect.
**The four-week calendar (CT):** wk of 09-07 — F-R4-1b lands; the honesty batch authored; FE-NULL-1; Erik Tue; E1/SCITT/Apple as dated reads; `EU:` 09-11; **R-4c Sat 09-12** → C-003. wk of 09-14 — the honesty batch lands; the v1.1.4 batch authored and dispatched; the hero charter (FE) opens; `Activate:` 09-15; Silabs 09-17; **09-18 the hard stop**. wk of 09-21 — v1.1.4 lands; FE-114 + the hero build; P-1 chartered and built; TR-1's verb on the bench. wk of 09-28 — R-5 (the floor re-baselined; hands-off ends); the hero on the wire (H9); the first soak night. October — the 72-h gate rehearsals; November — 11-25.
**What NOT to do (re-affirmed):** no B-2/B-3 code before C-003 and the bus fence closed; no second rig; no Matter; no research lane that does not name the instrument it feeds; no public name.

## §4 HIVE-CLEAN-1 — the hygiene program (a lane; the hub audits; two commits)
**Principle:** a file's `status:` line is a claim about NOW and is audited like any other claim. **Nothing is deleted** (the repo is the memory); what is over is MARKED over in its own frontmatter and, for the executed operational classes, MOVED under an `archive/` sibling so directory listings and greps show only what is current. The evidence trees (audits · assessments · research) keep their FILED/COMPLETE statuses and stay where they are.
**The recipe (deterministic; every step a script the hub can re-run; the lane files a census, not a narrative):**
1. **`context/handoff/`:** every `*_PM-mission-control_v*_orchestrator_session_prompt.md` except the newest → `archive/prompts/` with the status line prefixed `SUPERSEDED (by v<n+1>, <date>) — `; every operator packet / navigator prompt / dispatch text / operator brief whose session has closed → `archive/<year-month>/` with `EXECUTED <date> — record: <path>` prefixed (the R-4b pair, the R4 pair, the LLC/NFCU packets, the H3 stack, the WCAP pair, the v61 brief, the v63 dispatch prompt…). Keep live: `pm-handoff.md` · `coder-handoff.md` · the newest prompt · the newest brief · the newest dispatch-text · today's H8-a record + BLOCK 6 card until executed.
2. **`context/instructions/`:** every coding instruction / lane brief whose return exists under `context/audits|research` → `archive/` with `EXECUTED <date> — return: <path> · audit: <path>` prefixed (the census-of-record: 42 of 47). Keep live: F-R4-1b (gated), the H8-a packet (today), the W-SKILLS-7 dispatch (re-cut as W-SKILLS-8, §5), RS3-WMARKET-2 (on cadence).
3. **`context/planning/`:** the May–June plans, `weeks/**`, the two June frontend drafts → status `HISTORICAL — superseded by <the plan of record>`; move `weeks/` to `archive/`. The plan of record = THIS document + the program + the four-lane map (the map re-cut to point here).
4. **`context/programs/matter-design/`:** `00_PROGRAM_STATUS.md` → `PAUSED 2026-09-06 (no beat since 2026-07-19; resumes at the Matter horizon — Strategy v1.2 §…)`; the five READY dispatches → `PAUSED` in one line each.
5. **`context/strategy/`:** the 27 files without a status line get one (`REFERENCE — <date> — superseded by: <path>|none`); `claim-register.md` stays the only file that says what is CLAIMED.
6. **`context/pre-verifications/`:** the two July ISSUE-READY rows → `CONSUMED by <WU>`.
7. **Wayfinding:** `START_HERE.md` (hivemind) re-verified line by line against the current skill/prompt/brief shape (last verified 07-04; add the brief's §HELD as the operator's entry); `canonical-paths.md` + `truth-map.md` gain the `archive/` conventions above and the "status lines are audited claims" rule; `open-questions.md` (retired) → `archive/`.
8. **`ClaudeFolder/` outside the repos:** `_scratch/` — delete every `v56`…`v64` session directory and any file older than 7 days (Nick's hands: `rm -rf` is his; the lane lists them); a one-paragraph `_scratch/README.md` states the policy (per-session dirs; deleted at the next session's boot). `Founder_Operating_Plan_v1.md` and `Technical_Roadmap_v1.md` (136 KB, June) get a two-line currency banner pointing at the September plan of record and Strategy v1.2. The brand PDFs/DOCX stay (evidence). `_archive/` stays.
9. **The census the lane returns (≤10 KB):** a table of every moved/marked file (path → new path · old status → new status) + the counts by directory + the three greps that must read ZERO after the pass (`grep -l 'ISSUE-READY' context/instructions/*.md` for executed WUs · `status: LIVE` on any superseded prompt · `weeks/` outside archive).
**Fences:** no edit to any evidence file's body; no edit to the spine (`pm-handoff.md`, `PROJECT_SNAPSHOT.md`) — the hub records the pass; no skill file; `git mv` only (history follows); one commit per repo prepared as a census card; the hub commits hivemind, Nick's hands touch nothing but `_scratch/`. **Predictions:** P1 ≥70 files move or are re-statused · P2 zero evidence files touched · P3 the three greps read zero · P4 a fresh Claude Code lane given "read `context/instructions/` and tell me what is current" answers F-R4-1b + the packet, nothing else (the acceptance test, run by the hub after the pass). **Clock:** ≤3 h. **Dispatch (a FRESH Cowork window with `ClaudeFolder` connected; after the rig, or Monday):**
```
date -u first. You are the HIVE-CLEAN-1 hygiene lane for NexSys/HomeSynapse. Read nexsys-hivemind/context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_assessment_trajectory_and_hygiene-program.md §4 WHOLE, then context/canonical-paths.md and context/truth-map.md, then nothing older. Execute the recipe steps 1–9 exactly with git mv and in-place status-line prefixes (never a deletion, never an evidence-file body, never the spine, never a skill file); every move and re-status recorded in your census. Write nexsys-hivemind/context/audits/<today's CT date>_HIVE-CLEAN-1_return.md (≤10 KB; §0 the counts + the three zero-greps first; then the table). Stage nothing; commit nothing — the hub audits and commits. ≤3 h. Your last line: RETURNED <path> <bytes>. You report to the hub.
```

## §5 W-SKILLS-8 — the skills pass (a lane; the hub commits two repos; Nick re-syncs once)
**What "contextually optimized" means here, measured:** the three skills were token-shaped at W-SKILLS-6 (SKILL.md ≈ the first 5,000 tokens a compaction keeps; the ledgers in `references/`); Check 9 reads 28/28 identical at the bytes today. What is MISSING is currency: the ledgers lag the v63–v66 mints and this weekend's laws. **The pass folds — every SKILL.md byte-unchanged except the ONE line each gains (the pointer to its new ledger section); the ledgers gain:**
- **PM (`references/laws-ledger.md` + the brief shape):** THE OPERATOR-LOAD LAW (six clauses + the mint + §2.5's two refinements: the `RETURNED` line is the whole report; a slot-releasing landing outranks a read-only dispatch) · **the instrument-beside-the-claim rule** (§2.2) · the stable-prompt rule (§2.3) · the cap-is-a-ceiling sentence (§2.4) · the guarded-splice-into-Nick's-tree pattern for docs (BLOCK 6: the hub writes the working tree, stages nothing, hands a one-command card) · the v63/v64 mints W-SKILLS-7 was to fold (the 22 candidates numbered from arc 38, the mechanism-dedupe rule) · the per-domain lane law IF §7 word 2 says so.
- **Coder (`references/laws-ledger.md` / `testing-standards.md`):** the sleepy-device physics note (a ZDO request to a router-parented end device is answered only inside the parent's indirect window; the desk pins the frame, the wire decides) · "a fence that names a constant is grepped across every file type" (the lane's own lesson) · the F-R4-1 pattern: two triggers, one path, never a bypass · the return-cap sentence.
- **Frontend (`references/contract-consumer.md` / `field-evidence-and-rulings.md`):** the manufactured-type class as HERO-0 measured it (validators that REJECT lawful nulls while mocks populate them — F2) · the tri-state idiom as FE-113 shipped it · the null census as the hero's design ground (the four empty states) · the container pre-gate fallback when the desk cannot run the toolchain (FE-DESK-1) · the cap sentence.
- **All three:** the freshness preflight's Check 9 instrument restated as the per-file md5 SOURCE-vs-synced (it is; verify the wording) and a new **Check 12 — the archive convention** (a live directory listing shows only current files; §4's three greps read zero).
**Fences:** every `SKILL.md` md5 before/after in §0 (unchanged except the pointer line); the write-set = `references/*.md` + `pass-history.md`; nothing else; the hub audits under R1–R6 of the W-SKILLS-7 dispatch (they stand) and commits hivemind + skills; Nick re-syncs the account skills once; Check 9 confirms. **Clock:** 2–3 h. **Dispatch (a FRESH Cowork window; after HIVE-CLEAN-1 or in parallel — two repos, neither the core tree):**
```
date -u first. You are the W-SKILLS-8 skills lane for NexSys/HomeSynapse. Read nexsys-hivemind/context/instructions/2026-09-05_W-SKILLS-7_skills-pass_lane-dispatch.md WHOLE (its §1 read-set, §2 job, §4 fences and §6 audit rules STAND) and then nexsys-hivemind/context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_assessment_trajectory_and_hygiene-program.md §5 WHOLE (the additional folds: THE OPERATOR-LOAD LAW from context/lessons/pm-lessons.md, the instrument-beside-the-claim rule, the stable-prompt rule, the cap-is-a-ceiling sentence, the coder and frontend items listed there). Execute §2 of the W-SKILLS-7 dispatch plus §5's folds under §4's fences: every SKILL.md byte-unchanged except the one pointer line each; the write-set only; stage nothing, commit nothing. Adjudicate the predictions first in §0. Return ONE file at nexsys-hivemind/context/audits/<today's CT date>_W-SKILLS-8_return.md (§0 card ≤3 KB first: the census exact · the mint→arc mapping table · the SKILL.md md5s; whole ≤12 KB — a ceiling, not a target). ≈2–3 h. Your last line: RETURNED <path> <bytes>. You report to the hub, not to me.
```

## §6 The operator model from here (how we work, so the pace holds)
One hub window per working day (≈4–6 h of hub time), opened by the same stable prompt (§2.3) — its first message names the day's ONE deliverable and hands ONE act. Nick's day is a queue of one: an act, a one-line report, the next act; the words asked only when they gate the next act, with the fact and where it comes from; the rig is exclusive. The hub authors ahead of every slot (the next instruction exists before the current lane returns), audits at the bytes the hour a return lands, and commits at every block with a census-exact card. Sunday is the sitting: the rulings, the calendar words, the brand acts on the card. Lanes: at most two + the rig unless a word raises the cap; the core tree by domain (if §7 word 2 says so); every lane one file, one cap-as-ceiling, one `RETURNED` line. The hivemind: the spine is the only state; everything else is a pointer or evidence; a status line is an audited claim; the archive convention holds (HIVE-CLEAN-1 makes it true; Check 12 keeps it true). The skills: currency at every arc close (the ledger fold is a beat, not a lane, once W-SKILLS-8 has caught up).

## §7 The words (one message when you read this; none before Monday; the rig comes first)
1. `PLAN: adopt | edit <row>` — §3 as the plan of record (the map re-cut to point here).
2. `LANE-LAW: per-domain | keep` — §2.8's amendment (rec **per-domain**).
3. `F1: seed-null | document` — LASTREPORTED-1's shape (rec **seed-null**).
4. `CLEAN: go | hold` — HIVE-CLEAN-1's dispatch (rec **go**, Monday; the paste is in §4).
5. `SKILLS: go | hold` — W-SKILLS-8's dispatch (rec **go**, after CLEAN or beside it; the paste is in §5).
6. `MATTER: pause | keep` — the program's status line (rec **pause**).
7. `R4C: Sat 09-12 | Sun 09-13` — already open; rec Sat.
