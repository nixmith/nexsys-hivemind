<!--
file: context/planning/2026-09-13_v72_PROGRAM-RECUT_git-read_objectives_and_v73-window-plan.md
purpose: v72 beat 5 on Nick's brief — the core git history read, the program measured against the plan of record and the momentum map, the ranking of the next work by impact per hour of Nick's time, and the shape of the v73 hub window so it dispatches at beat 1 and spends its context on intakes and strategy. Supersedes nothing; it re-cuts the 2026-09-10 momentum map's calendar from the tree of 2026-09-13.
audience: Nick (§0, §3, §5) · the v73 hub (§4 governs its window) · the plan of record's next re-cut
state-type: program re-cut (dated; the momentum map's successor for the two weeks from 09-14)
status: FILED Sun 2026-09-13 ~07:5x CT (instrument 2026-09-13T12:57:59Z); core `a458a64` · hivemind `256c26b` · docs `2235845`
-->

# Program re-cut at v72 beat 5 — the git read, the objectives, the next two weeks, and how v73 spends its window

## §0 One screen
The desk is nine days ahead of the plan of record; the rig has not moved since 09-06. The v1.1.4 read-API was planned to land the week of 09-21 and landed 09-12; the hero's design and its first build were planned to open the week of 09-14 and landed 09-11; an unplanned bus class (seven commits) was absorbed in the same fortnight and ended in a ratified invariant. R-4c (Sat 09-12) and H8-a (Fri 09-11) have no record. C-003 — the sleepy SNZB-02P adopted by the ZDO surface, the exit of R-4c — is the gate that makes the fleet the soak's fixture, lifts the B-2/B-3 fence and lets P-1 → R-5 → the 72-hour rehearsals start. Nothing on the desk substitutes for it, and only Nick can run it. So the ranking is short: the rig first; the desk lanes run unattended beside it; the hub authors ahead so a landing is followed by a dispatch within the hour.

## §1 The core git history, read (`homesynapse-core`, 246 commits; 23 since 2026-09-01; W36 and W37 twelve each — the two densest weeks of the repo's life)
Themes since 08-28, in landing order: **distribution and packaging** (R-3b/PKG-E2E-1 `7c57d7f`; PKG-SEC-1 `c368909`; PKG-SEC-2 `ef02d13`) — the `.deb` is built and smoke-booted on every push (P4 MET on HONESTY-1's landing). **The honesty line** (FE-HONEST-1 `f519f42`; FAILCHAN `7af2d6c`; HONESTY-1 `94ae99d`; FE-NULL-1 `eabdbb1`) — dangling refs render loud, healthy outcomes stop reporting through failure channels, `lastReported` seeds null until the first report, the causal chain's null arms are mirrored. **The Zigbee re-adopt surface** (F-R4-1 `a1c6966`; F-R4-1b `39c8dd3`) — interview-on-rejoin, the ZDO IEEE_addr_req second surface: R-4c measures these on silicon. **The read-API v1.1.3 → v1.1.4** (CG-1/2/3 `f25291b`; FE-113 `d192d17`; EXPLAIN-114a `5f918c7`) — the additive keys the hero cannot render without. **The hero** (HERO-1 `1e26912` the design of record; HERO-1b `6af76f7` rows B1–B7 and B9). **The bus class** (FAILCHAN-FIX-1a/1b `5ed9178` `e5fa035`; FIX-2a `cc05a54`; FIX-2b-i `bd5d35e`; CI-1 `ff1a6e1`; FIX-2b-ii `72efb42`; BUS-ORDER-1 `a458a64`) — instruments first (per-method XML, the anomaly kinds, the await diagnostic, the position census, the run hand-off tokens, the soak as a non-gating sample job), then the mechanism: the cursor delivers, the checkpoint floors, the notification wakes (AMD-101, ratified 09-13).
What the history says: every landing since 09-03 left an instrument a later push re-runs (the program's own law, kept). The bus class took seven of September's 23 commits — about a third of the desk — and was worth it: a delivery drop the household would never have seen is now an invariant with a test, a census line and a CI sample per push. It also says the bus was under-instrumented before 09-05; the same question (where else is a silent do-nothing lawful?) is the R-5 charter's carry-forward, not a new lane now. Nothing in the log touches B-2/B-3, Matter, or a public name — the fences held.

## §2 Against the plan of record (the 09-06 assessment §3; the 09-10 momentum map)
| Plan row | Planned | Actual (09-13) |
|---|---|---|
| F-R4-1b lands | wk 09-07 | `39c8dd3` 09-06 |
| the honesty batch authored → lands | wk 09-07 → wk 09-14 | `94ae99d` 09-10 |
| FE-NULL-1 | wk 09-07 | `eabdbb1` 09-10 |
| **R-4c at the rig → C-003** | **Sat 09-12** | **no record; `R4C:` owed** |
| H8-a (FE-113 verified; FAILCHAN's stop readings) | Fri 09-11 19:00 | **no record; `H8A:` owed** |
| the v1.1.4 batch authored, dispatched, lands | wk 09-14 → wk 09-21 | EXPLAIN-114a `5f918c7` 09-12 |
| the hero charter opens → FE-114 + the hero build | wk 09-14 → wk 09-21 | HERO-1 + HERO-1b landed 09-11; HERO-1c chartered by v72 b6 |
| P-1 chartered and built; TR-1's verb | wk 09-21 | P-1 chartered 09-11 (dry-run; paste after R-4c); TR-1 ACCEPT, TR-1b after `card-gradle:` |
| R-5; the hero on the wire; the first soak night | wk 09-28 | queued behind R-4c |
| the bus (unplanned) | — | seven commits; AMD-101 ratified; the closure counter 1/20 |
The gate everything serves is unchanged: the MVP's 72-hour unattended run on the six-device fleet with the hero rendering the three questions, target 2026-11-25. The desk's lead does not buy rig time back — the rig is serial, exclusive and Nick's hands — so the slack the 09-06 assessment counted (about three weeks) is now spent by the rig calendar alone. Two rig sessions this week (R-4c, then H8-a) put the program back on its own critical path with the desk still ahead.

## §3 The ranking — impact per hour of Nick's time (the hub carries the rest)
1. **R-4c at the rig** (≤90 min; the packet re-pinned this beat to core `a458a64`'s install-smoke `.deb` — the newest green, carrying EXPLAIN-114a and BUS-ORDER-1 beside F-R4-1b and HONESTY-1; MEASUREMENT-ONLY unchanged). Buys: C-003 or its refutation; the six re-adopted; `lastReported` read on the wire (LASTREPORTED-1b); `card-gradle:` (TR-1b's shape); the restore. Everything below the line waits on it.
2. **H8-a's record** (the rig; FE-113 → VERIFIED; OR-FAILCHAN's stop readings; the capture FE-113b needs). Second rig slot; it can follow R-4c in the same sitting only if R-4c's restore is done first (the restore outranks everything after B3).
3. **P-1's paste** (the bench; dry-run only; the live leg gated on R-5 or `HARNESS-PLUG:`) — after R-4c, when both core lanes are running; then **R-5** (the floor re-baselined on the fleet; the s31/nightly hands-off ends).
4. **The desk lanes, unattended, on disjoint domains** (D4): **HERO-1c** (web-ui; the step lines and helps; retire "We sent it"; the catalog keys outside the B-rows; dispatch on `FE: 6af76f7 frontend green`) and **114b** (Java; DISABLED-2, the cursor helper, the constructor collapse; the rest-api MODULE_CONTEXT phrase rides it). Both authored by v72 b6 so v73 dispatches them at its beat 1.
5. **The dated company items this week:** `Activate: apply|hold` (Tue 09-15) · Silabs (Thu 09-17) · **the written opinion and the 09-18 hard stop** (no public name before it) · `PROTECT: done` (the six clicks) · Erik's line when it comes. Each is minutes, each is Nick-only, none moves the gate; they are on the card because their dates are.
6. **Passive:** the closure counter (twenty green samples from `a458a64`, kind E 0; any red sample is read at its artifact); Check 9 (the one-file sync); `RUN: 5f918c7 <n>`.
**What we do not do** (re-affirmed): no B-2/B-3 code before C-003; no second rig; no Matter; no research lane that does not name the instrument it feeds; no public name; no batched pushes; nothing at the rig but the packet.

## §4 The v73 window — shaped by v72's measurement
**v72 measured (through beat 5):** the boot read-set 43.7 KB; ≈80 tool calls; ≈200 KB read; ≈180 KB authored (five splice scripts of 35–45 KB each are most of it); no compaction. The costs that were avoidable: one boot grep without a file argument recursed the repo (a 139 KB result, saved to disk and read by range — 8K tokens); the same-path hazard fired three times through the container's outputs path (three extra round trips; minted in pm-lessons this beat).
**v73's shape.** Boot ≤45 KB as the prompt orders. **Beat 1 hands two pastes v72 authored** — HERO-1c's charter (§N, on `FE: 6af76f7 frontend green`) and 114b's instruction (§8, on the clean Java domain) — and names the window's deliverable (R-4c's record intaken and C-003 ruled, or the two lanes landed if the rig slips). **Beats 2–5 are intakes**, one per report: `R4C: RETURNED` → the C-003 verdict → P-1's paste; `H8A:` → FE-113 VERIFIED and FAILCHAN's readings; the two lane returns → their audits → the core cards → CI; every sample banked as a line. **Beat 6 is the strategy fold**, read by range and only then: `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` (§0–§1), `context/strategy/2026-08-27_company-and-brand-build_strategy-of-record.md` (its one-screen and calendar), this file's §2–§3, and the R-4c record's C-003 line — into October's calendar: the 72-hour rehearsals (kill −9 · coordinator disconnect · power-cycle recovery, all driven by P-1), R-5's date, FE-114 and the hero on the wire, the brand's post-opinion program. **Close ≤ beat 8** with the brief's §NEXT holding one act and the v67 prompt unchanged.
**A refinement v73 may adopt at its beat 1 (proposed, not ordered):** a per-session splice library — `_scratch/v73/splice_lib_v1.py` (the paths, `porcelain`, `once`, `fill`, the chain split, the caps, the census-from-porcelain), committed once under a fresh name and md5-asserted at every use — so each beat's fresh script carries only its anchors, its texts and its asserts (about a third of today's bytes). THE GUARDED-SPLICE LAW holds as written: every assert and cap still runs in the per-beat script before its first byte.

## §5 From here
The one act is R-4c (its packet's §N into a fresh window; the report `R4C: RETURNED <path> <bytes>`, or `R4C: <day>` to name the slot). The word due beside it: `H8A: RETURNED <path> <bytes>` or `H8A: not run`. v72's remaining beats: b6 authors HERO-1c's charter and 114b's instruction (no act for Nick); b7 closes the window on the deliverable met, with v73's dispatch line in the brief.
