<!--
file: context/planning/2026-09-15_v75_PROGRAM-PLAN_the-six-weeks-to-the-72-hour-run.md
purpose: THE PLAN OF RECORD from v75 beat 4 — the program assessment Nick asked for at the b3 landing: the critical path from the Saturday sitting to the 72-hour run, the instrument beside every row (exists today / built by which WU / missing), the ranking by impact per hour of Nick's time (≈ 7 h/week; the hub carries the rest), where his hours go and must not go, the company side in the same weeks, what we refuse and what we DROP (including things Nick asked for), the words as H10 rows with a recommendation each. Supersedes `2026-09-13_v73_PROGRAM-PLAN_after-114b_two-weeks-and-October.md` §2–§5 and §8; that plan's §10 (the ambition map's October rows) and §11 (v75's window) stand by reference. Written on the state at `e56f555`/`3d40b5f` (114c and FE-114 green), hivemind `553bf5b`, the sitting named `H8: Sat 09-19 10:00`.
audience: Nick (§0, §3, §4, §8 — the words) · the hub (every beat) · the lanes (their row's instrument line)
state-type: plan of record
status: LIVE — v75 beat 4; §10–§12 at beats 5–7; §13 at the v75 CLOSE (beat 8, Thu 2026-09-17: v76's window — the sitting, Sunday's intakes, ENERGY-READ through the gate, rehearsal 1's packet). Row 2 DONE at `be4788a`. §12 at beat 7 (Thu 2026-09-17: the CI red is storage; coverage 29.7 %; the company facts checked; `GAPS:` and `HOTMOBILE:`). §11 at beat 6 (Wed 2026-09-16: the fleet chosen; Activate skip-by-default with the Oct 9 rule; the pitch row restored; ZIGBEE-GAPS and FOP-1). §10 addendum at beat 5 (Tue 2026-09-15: the benchmark fleet replaces the one-meter card; MEASURE-1's numbers; Activate's close date). Authored v75 beat 4 (Mon 2026-09-14 ~20:xx CT; instrument 2026-09-15T01:45:26Z). Re-cut at the R-5B intake (the sitting's facts) and at every rehearsal's intake. §14 at v76 b2 (Thu 2026-09-17 ~21:1x CT: the hardware calendar at the order — Wed 09-24 operative; the meter chain re-cut; OR-METERING-VOLUME; the words closed). §15 at v76 b3 (Fri 2026-09-18 ~13:0x CT: EXT-REVIEW-1 adjudicated; 09-24 is a Thursday; the run's two artifacts; the window to the run). §16 at v76 b4 (Fri 2026-09-18 ~13:2x CT: ENERGY-READ dispatched; the eight additions dated — the watts go/no-go Sun 10-11 20:00 CT; VERIFY-72H's gates and invariants). §17 at v76 b6, THE v76 CLOSE (Fri 2026-09-18 ~16:2x CT: ENERGY-READ green; Nick's re-cut — MEASURE-2b as a read-path measurement, LINK-READ second; the retention fact; v77's order).
-->

# The six weeks to the 72-hour run — the plan of record (v75 b4)

## §0 One screen
The gate is one number: a 72-hour unattended run on the six-device fleet whose every silence carries a link trace, whose every confirmation is corroborated by a plug's own watts, and whose record a stranger could audit. Six weeks from today puts the run on the weekend of **Oct 30 – Nov 2**. Between here and there the critical path is seven rows and only two of them have their instrument today. Nick's hours go to four rig sittings (≈ 2.5 + 2 + 2 + 1 h), the run's start/stop, and ONE word per row; nothing else. The hub carries every charter, every intake, every card. The company side runs beside it on words alone: the Activate email Tuesday, Silabs Thursday, Erik's opinion when it comes, the knockout on a free slot, PROTECT's six clicks. We drop three things that no longer buy the gate per hour (§7).

## §1 What landed since the v73 plan (all by Nick's hands; CI green on each)
HERO-1c `93390f0` · HERO-1d `6bd8508` · **EXPLAIN-114c `e56f555`** (the by-id read 100 → 1 pages; `conditions[].definition` on the wire, v1.1.5; FIRED_CONFIRMED on evidence) · **FE-114 `3d40b5f`** (the mirror at v1.1.4; three sentences rendered; the lint's reach) · R-5 Part A + ii `f3631cb` (the `plug:` grammar; the `fleet:` line) · the ambition map ACCEPT (IR-15..IR-19) · both sitting packets through THE PRIOR-LEDGER GATE · IR-1 MEASURED · the closure counter **6/20**. Two defects surfaced by reading: IR-22 (a `for_duration` expiry initiates no run) and IR-23 (the definition hash is per-JVM for a multi-role selector — and, by the b4 grep, for a multi-parameter command action: `Map.copyOf` at `CommandAction.java:51` is the same idiom, so the defect is LIVE, not latent) — both measured first (MEASURE-1, this week), then fixed (DUR-1, HASH-1).

## §2 The critical path to the run — every row with its instrument
| # | Row | What it proves for the run | The instrument today | Built by | Nick's hours |
|---|---|---|---|---|---|
| 1 | **The sitting, Sat 09-19 10:00** — H8-a then R-5B | the v1.1.3 keys on the real wire; the stop-proof; the floor re-baselined on six; the ZDO surface's causal role (P-B2); the null arm; the re-seen/adopted split; the S31's count; O-2 closed | **EXISTS** — the two packets, the navigator (scheduled 09:50), the bench nightly | — | 2.5 h + Sunday's one paste |
| 2 | **MEASURE-1 → DUR-1 + HASH-1** (the Java slot, this week) | a trigger class that fires; a definition key that is stable — the hero and the corpus stop trusting unstable facts | **EXISTS** — the test harness (`MutableClock`, `RecordingRunManager`, `cleanTest` ×8) | MEASURE-1 (2 h of a coder), then the fixes (½ day each) | words only (`DUR: measure` given; `HASH1:`) |
| 3 | **ENERGY-READ** (the Java slot, wk 09-21) | the plug's watts on the substrate's state path — the corroboration leg | **PARTIAL** — the dossier (the Gen4: 0x0B04 + 0x0702 standard ZCL, no quirk) EXISTS; the divisor FIXTURE does not (the Gen4's `acPowerDivisor` is predicted 10 from a third-party driver, adjudicated at the first read); IR-15's IAS fix rides first | ENERGY-READ (2 lane-days) | the meter purchase (minutes); a word |
| 4 | **`HARNESS-PLUG:` → P-1's live leg → rehearsal 1 (kill −9)**, wk 09-28 (a sitting) | the substrate's recovery under a power event; the plug join window if ENERGY-READ is green | **MISSING** — P-1's live leg is REFUSED until `HARNESS-PLUG:` (the S31's label from H8-a promotes a POWER-CYCLE harness only); the rehearsal packet is unwritten | the hub (the packet, b7); Nick's word after H8-a | ≤ 2 h at the rig |
| 5 | **LINK-READ** (the Java slot, wk 09-28 → 10-05) | every silence in the run carries its last link reading (RSSI/LQI) | **MISSING** — the per-device frame counter (IR-20's `onFrame` counter, the first row); the sampling rule from its number; `link-quality.yaml` (the bench lane owes the skeleton) | LINK-READ (1–2 lane-days) + the bench lane | a word |
| 6 | **Rehearsal 2** (wk 10-05, a sitting) — the plugs join, LINK-READ's traces read | the metering datum `within|outside` the reference meter (THE MEASUREMENT RECORD's first row); the link traces on silence | **MISSING** until rows 3 and 5 land; the reference meter (a purchase) | the hub (the packet); the bench lane (`metering-known-load.yaml`, `requires: [operator]`) | ≤ 2 h at the rig; two meter readings by eye |
| 7 | **The soak night → the run** (wk 10-12 the soak; wk 10-19 a dry 24 h; **Oct 30 – Nov 2 the 72 h**) | the three questions and the fourth (did the device that said it turned on draw power) | **PARTIAL** — the nightly + the harness exist after row 4; the 72-hour packet, its digest and the fence rules are unwritten | the hub (rehearsal 3's packet = the run's dry run; the 72-h packet) | start Fri, one glance Sat and Sun, stop Mon: ≈ 1 h total |
Off the path but load-bearing: **the closure counter** (6/20; passive; every push counts — the B-2/B-3 CODE fence stays until 20); **C-003's ZDO clause** on B2's result (the outward-chain act of the week after the sitting); **FE-115** (`conditions[].definition` rendered; the `v114` mock; the dead-key retirements) — the web-ui slot, no rig time.

## §3 The ranking — impact per hour of Nick's time (the hub carries the rest)
1. **The sitting (row 1)** — 2.5 h buys rows 1's seven facts and unblocks `HARNESS-PLUG:`; nothing else on the map moves without it. *Packets cut, pinned, gated; the navigator scheduled.*
2. **The two words that cost minutes and unblock the Java series: `HASH1:` and (given) `DUR: measure`** — MEASURE-1 is one paste; the fixes follow on its numbers; ENERGY-READ dispatches behind them.
3. **The meter purchase (minutes)** — the only purchase left (the Gen4 units Nick owns ARE the plugs); without the reference meter, rehearsal 2's datum has no band.
4. **Rehearsal 1 (row 4, ≤ 2 h)** — the first power event on the substrate; the packet is the hub's; Nick's word `HARNESS-PLUG:` after H8-a's label read.
5. **Rehearsal 2 (row 6, ≤ 2 h)** — the first measurement-record datum and the first link trace.
6. **The company words (minutes each):** `Activate: hold` Tuesday (the email as one paste); Silabs Thursday (Nick's own hour); `NAME:` silence = ii (the knockout lane on a free slot); `PROTECT: done` (six clicks); `ERIK:` when it comes; `AMD100: ratify`.
7. **The run itself (row 7, ≈ 1 h of hands over four days).**
8. **Reads Nick does not owe:** lane transcripts, the hub's checks, the strategy pass's return before the sitting, the dossier.

## §4 Nick's hours — where they go and where they must not
**Go (≈ 7 h/week):** wk 09-14: the cards (≈ 20 min), the Activate email (10 min), Silabs (his hour), the sitting (2.5 h Sat) + Sunday's paste (2 min). wk 09-21: the cards; `HARNESS-PLUG:`; the meter arrives. wk 09-28: rehearsal 1 (≤ 2 h). wk 10-05: rehearsal 2 (≤ 2 h). wk 10-12: the soak night (start/stop, minutes). wk 10-19: the dry 24 h (minutes). wk 10-26/Oct 30: the run (≈ 1 h across four days). **Must not go:** re-running the hub's checks (the hub re-runs the lanes' gates in its own container); reading transcripts (a `RETURNED` line is the report); the strategy pass (a lane, after R-5B); the dossier (done); anything at the rig outside a packet; a second lane on one path-domain; a re-pin without `REPIN:`.

## §5 The calendar (CT)
| When | Nick's acts (minutes unless marked) | The hub's beats |
|---|---|---|
| **Mon 09-14 (tonight)** | the b4 card; the MEASURE-1 paste; the meter order (one link) | the freeze doc's v1.1.5 stamp; the plan re-cut; the premise gate minted; the navigator scheduled |
| **Tue 09-15** | `Activate: hold` → the email (one paste); the knockout lane paste when a slot frees | the `/apply` read + the email drafted; the knockout charter; MEASURE-1's intake → DUR-1 + HASH-1 chartered |
| **Wed–Thu 09-16/17** | the DUR-1/HASH-1 paste; Silabs (Thu); `AMD100: ratify` | the intake; ENERGY-READ authored (IR-15 first; the Gen4's predicted divisors as P-rows); FE-115 authored |
| **Fri 09-18** | the landing card(s); `ERIK:` if it comes | the packets re-read, not touched; the navigator's send confirmed |
| **Sat 09-19 10:00 — THE SITTING (≤ 2.5 h)** | H8-a then R-5B | — (the hub asks nothing) |
| **Sun 09-20** | `R5B-2: <the digest line>` | the two intakes; C-003's ZDO clause drafted; `HARNESS-PLUG:` packaged; the plan re-cut on the facts |
| wk 09-21 | `HARNESS-PLUG:`; the ENERGY-READ paste; the FE-115 paste; `C003: <word>` | rehearsal 1's packet through the gate; the strategy pass lane (D-v75-2, after-R5B) |
| wk 09-28 | **rehearsal 1 (kill −9), a sitting**; the LINK-READ paste | the intake; rehearsal 2's packet; the bench lane (`metering-known-load.yaml`, `link-quality.yaml`) |
| wk 10-05 | **rehearsal 2 (the plugs join; the meter read), a sitting** | the intake; THE MEASUREMENT RECORD's first row banked; the soak packet |
| wk 10-12 | the soak night (start/stop) | the intake; the 72-h packet drafted; the October re-cut |
| wk 10-19 | the dry 24 h (start/stop) | the intake; the 72-h packet through the gate |
| **Fri Oct 30 – Mon Nov 2** | **the 72-hour run** (start; a glance Sat, Sun; stop) | the intake; the register's public form (B-6) after |
Slip rule: a row that misses its week moves the run by the same week; the run is never shortened and never started on a red floor.

## §6 The company side (the same weeks; words, never hours)
`Activate:` D-v74-2 — hold Tuesday; the email asks the track and the close date; `apply` only on a yes. Silabs Thu 09-17 — Nick's meeting (the hub owes nothing; a one-line note of what was said goes in the DR). Erik's written opinion (PALOKI) — the wait-state; no public name before it; the `.com` at Nick's own risk. The knockout charter (D-v74-1, silence = ii) — a read-only lane on a free slot; token-parameterized; no name in any tracked file. PROTECT — six clicks, `PROTECT: done`. AMD-100 — `AMD100: ratify`. The counsel wave (the §1(b) application, the LA trade name, the Member consent) — drafted as one paste when the opinion lands; not before. The strategy/market pass (D-v75-2) — after R-5B; it does not buy the gate and is not on Nick's clock.

## §7 What we refuse, and what we DROP now
**Refuse:** any second protocol; Matter; a research lane without an instrument; a plug adopted before ENERGY-READ is green (THE ADOPTION FENCE); a water/smoke/vibration device before IR-15; B-2/B-3 code before the counter closes; a public name before the opinion; a batched push; a packet cut on the tail of a tired session; a re-pin without `REPIN:`. **Drop (they no longer buy the gate per hour):** (1) **TR-1b at the card** — R-4c answered `card-gradle: absent` (the held card is run-only); the position-census driver is a desk WU after the run, and `card-gradle:` leaves the owed list. (2) **The second plug purchase** — the dossier shows the Shelly Plug US Gen4 Nick owns meets both criteria (0x0B04 + 0x0702 as input clusters on EP1; no quirk; a router; 15 A); the purchase is the reference meter only. (3) **A FRAME-COUNT micro-WU before the sitting** (`FRAME: a` stands; the counter is LINK-READ's first row). Held, not dropped: IR-19 (the three `.docx` → `.md`, an idle beat); the log-level knob (IR-21, after the run's charters); cascade parentage (D-v75-3, November).

## §8 The words — H10 rows, one recommendation each (silence = the recommendation)
| Word | Options | Rec | Refutable-by |
|---|---|---|---|
| `HASH1: a|b` (D-v75-5) | (a) the eight records that store an unordered `Set.copyOf`/`Map.copyOf` (`AreaSelector:47` · `LabelSelector:46` · `TypeSelector:48` · `SemanticTagSelector:57` · `CommandAction:51` · `EmitEventAction:37` · `EventTrigger:43` · `WebhookTrigger:55`) store an ORDERED immutable view instead (`EnumSet`/`TreeSet`/`TreeMap` behind `unmodifiable…` — the same `toString()` format, so every single-element hash is UNCHANGED; multi-element hashes change once and become stable); (b) a canonical serializer inside `DefinitionHashes` (one file; EVERY definition's hash changes once — every pre-fix run reads `definition: null`, every `definitionKey` moves) | **(a)**, after HASH-0's eight hashes prove the salt; the v1.1.5 stamp carries the note either way | HASH-0 returning ONE hash across its eight JVM launches (then IR-23 closes as the hub's misread and FE-115 renders the key as stable) |
| `HARNESS-PLUG: <entity>` | after H8-a's `S31-LABEL:` and the plug's `rating_w` | given at the sitting's intake, Sunday | a label that cannot be read |
| `C003: mint|hold` (the ZDO clause) | on P-B2's result Sunday | mint if arm 1 adopts over `tc_join` | a `tc_join` without adoption |
| `STRATEGY: now|after-R5B|fold` (D-v75-2) | as packaged | **after-R5B** (wk 09-21) | a free slot Wednesday |
| `CASCADE: engine|bridge|drop` (D-v75-3) | as packaged | **engine, November** | the run's first "why" a parent would answer |
| `Activate: apply|hold|skip` (D-v74-2) | Tuesday | **hold** — the email | a close date inside two weeks |
| `NAME: i|ii|iii` (D-v74-1) | the knockout first | **ii** (silence) | — |
| `REPIN: <sha>` | only if Nick wants Saturday on the newest green | **no re-pin** — the packets are gated against `6bd8508` | — |

## §9 The measurement (v75 at beat 4)
Four beats · ≈ 250 hub calls · three lanes accepted (114c, FE-114; the dossier micro-lane) · two core landings green (5/20 → 6/20) · four hivemind commits · one skills commit owed (the premise gate folds at W-SKILLS-10) · the brief at 12.1 KB of its 12.3 cap (b5 trims) · live beats 12 of 12 (b5 rotates).

## §10 Addendum (v75 beat 5, Tue 2026-09-15) — the benchmark fleet replaces the one-meter card
Nick's word on the b4 shopping card (the DR §1, b5): nothing ordered; the setup must be scientific — multiple models, an independent reference, repeatable as device classes and (later) community plugins grow. §7's "the second plug purchase DROPPED" is therefore RE-CUT: the purchase question is re-opened as a **benchmark-fleet** question and closes on the DEVICE-SET research lane's return (`context/instructions/2026-09-15_research-lane_DEVICE-SET_the-benchmark-fleet_charter.md`; the word `DEVICES: core|extended|<edits>`). What does not move: THE ADOPTION FENCE (no metering plug joins any card before ENERGY-READ is green); the two Gen4 units Nick owns are CORE by the dossier; ENERGY-READ's fixture takes DEVICE-SET's two plugs and reference meter; LINK-READ buys nothing. Row 3 (ENERGY-READ) gains a dependency: DEVICE-SET's return before the fixture's divisor P-rows are written — one research lane-day, in parallel with DUR-1 + HASH-1, so the critical path does not move. Also filed here: MEASURE-1 measured both defects (DUR-0 RED; HASH-0 salted on sets AND maps) — row 2's fixes are chartered on numbers; Activate (the company side, §6) closes **Oct 30, 12 p.m. PT — the run's weekend**: an application, if their answer is yes, is written by Oct 23 or not this cycle.

## §11 Addendum (v75 beat 6, Wed 2026-09-16) — the benchmark fleet chosen; Activate re-cut; the pitch row restored; two research lanes
**The fleet (DEVICE-SET, ACCEPT):** `DEVICES: core` recommended — $104.78: a second-vendor plug (Third Reality Gen3), a Class-1.0 reference meter with 0.01 W resolution (PN2000) and the P4460 as its cross-check (its power accuracy is 0.5 % typ / 2 % max — the "±0.2 %" the b4 dossier carried was the voltage row), a 40 W tungsten known load (no 60–100 W incandescent is sold new; P ∝ V^1.6). EXTENDED (mmWave + an illuminance sensor, +$73.89) held until IR-18. Two HEAD facts the lane found feed ENERGY-READ's charter as rows, not predictions: the Gen4 identifies as 0x010A and `EndpointClassifier` reaches the plug branch on 0x0051 only (IR-24 — attach metering by clusters); `ReportingConfigurator`'s reportable change is raw units, the divisor unapplied (IR-25). The divisor value is read at the first read (no prediction).
**Activate (D-v74-2, re-cut on Nick's critique — the DR §1):** SKIP is the default. Apply only on a WRITTEN yes to both questions of the corrected email (the literal four-year reading; the FAQ's hard-tech exception) by **Fri Oct 9**, and only with hours ADDED on top of this plan — never from the rehearsal or soak weeks; a yes after Oct 9 is a no for this cycle. Dated watch list: Cohort 2028 opens ~Sep 2027 — re-read `/apply` and the FAQ then. The hub's b5 email carried a false entity line and two register-unsupported claims (the b6 audit §3, F-b6-1); outward company text is now gated like code.
**The pitch row (restored):** the strategy pass (D-v75-2, after R-5B) opens with "built to and for the market as it exists and as it is developing" — the pitch is not a sales sentence but a build criterion; it draws on the strategy corpus already filed and on ZIGBEE-GAPS's market column.
**Two research lanes beside the Java slot (read-only; no rig time):** ZIGBEE-GAPS — "support any Zigbee device in the world" measured as coverage against the field's device corpus at one tag, a ranked gap list with an instrument per row; FOP-1 — every dated, eligibility, dollar and entity claim in the founder operating plan checked against its live source (the PhD line and the NSF-fellowship row are known stale; the entity formed 2025-07-17). Neither moves the critical path; ZIGBEE-GAPS's rows feed the October charters.

## §12 Addendum (v75 beat 7, Thu 2026-09-17) — the CI red is storage, not code; coverage measured; the company facts checked; two words
**The landing:** DUR-1 + HASH-1 landed as core `be4788a`; run #251 is RED at both jobs' UPLOAD steps ("Artifact storage quota has been hit"), with no check or test annotation — the verdict is OPEN until the `Run check` step is read (Nick's one glance) or `check` is re-run; CI-2 makes uploads non-gating and cuts report retention; the quota clears when old artifacts are deleted (run #56's kept for Saturday). Row 2's retirement (IR-22/IR-23) waits on that read. Nothing on the critical path moves; Saturday is pinned to `6bd8508`.
**Coverage (ZIGBEE-GAPS, ACCEPT):** against the field's corpus (`v26.110.0`, 4,499 definitions) HEAD fully handles **29.7 %** (band 27.7–31.0); twelve ranked rows reach 88.7 % for ≈27.5 lane-days; the ceiling without a Tuya/Aqara quirk layer is 72.9 %, with it 93.6 %. The two largest unlocks are scope fences, not code: full color (AMD-96, +14.5 pp) and buttons (BTN-AMD, +6.9) — `GAPS:` (D-v75-7, rec neither before the run). ENERGY-READ ranks third for coverage and first for the run; IR-24 widens to classify-by-clusters (row 7); IR-26 the unexecuted `InitializationWrite`. October's charters draw from rows 1–12 in the lane's order once the run's rows are green.
**Company facts (FOP-1, ACCEPT):** 13 of 34 checked FOP claims are FALSE (the Activate/NSF-EF pair is one application; no PhD rule; the LLC exists since 2025-07-17; HotMobile 2027 is due **Oct 9, 2026** — `HOTMOBILE:` (D-v75-8, rec skip); the USPTO/FCC/SAM/CSA/CEDIA figures); `FOP: apply|hold` is Nick's on his own file. The dated watch list (§6) now carries: Activate Cohort 2027 close Oct 30 12:00 PT (skip-by-default; the Oct 9 rule) · HotMobile 2027 Oct 9 AoE · NSF SBIR Project Pitch rolling (26-510) · CSA Unify 2027 · CEDIA Expo 2027-09-07 · the LLC's standing (a one-minute registrar lookup, Nick's).
**Nick's hours this week:** the Saturday sitting (≈2.5 h) and the words; nothing else — the email is one paste, the cleanup one command, the CI-2 landing one card.

## §13 v76's window (written at the v75 close, beat 8, Thu 2026-09-17) — the sitting, the intakes, ENERGY-READ, rehearsal 1's packet
**v75 CLOSED at beat 8:** the window's deliverable (both Saturday packets through THE PRIOR-LEDGER GATE, the sitting named) was MET at b1–b2 and held untouched through b8; eight lanes accepted (114c, FE-114, MEASURE-1, DUR-1 + HASH-1; the plug dossier, DEVICE-SET, ZIGBEE-GAPS, FOP-1); three core landings green at the instrument (`e56f555`, `3d40b5f`, `be4788a` — `Run check` green; the upload step red on the storage quota, CI-2 on the b8 card); seven hivemind commits; THE PREMISE GATE minted and extended to outward text; the plan of record re-cut with §10–§12 addenda. Row 2 of §2 is DONE (IR-22/IR-23 retired at `be4788a`).
**v76's ONE deliverable:** the sitting's two records intaken two-layer by Sunday night and ENERGY-READ DISPATCH-READY through the premise gate — the corroboration leg's code on the Java slot before the plugs are in hand.
**The beats, in order:** b1 the boot + the b8 packet's verdicts (CI-2's push as the counter's next sample; the artifact cleanup's `ARTIFACTS:` line) + ENERGY-READ chartered through the gate (IR-15 first; the two handlers with the divisors read at interview; IR-24 classify by clusters; IR-25 the divisor-scaled reportable change; THE MEASUREMENT RECORD minted; the fixture = `DEVICES: core` + ZIGBEE-GAPS rows 3/7) · b2 the knockout charter (a free slot) · Saturday — the hub asks nothing · b3 Sunday: `R5B-2:` → the two intakes → C-003's ZDO clause drafted → `HARNESS-PLUG:` packaged → FE-115 chartered (the H8 real-wire read in hand) · b4 rehearsal 1's packet through the gate; the strategy pass lane (D-v75-2) · b5+ the ENERGY-READ intake; LINK-READ chartered; the close.
**Nick's hours:** the sitting (≈2.5 h) · Sunday's one paste · one word per row · the purchase on `DEVICES:` · the Gen4 firmware update before ENERGY-READ's first read. Nothing else.
**The dated watch list (from FOP-1 §3, carried here so it does not drop):** Activate Cohort 2027 closes Oct 30 12:00 PT (skip-by-default; the Oct 9 rule) · HotMobile 2027 due Oct 9 AoE (rec skip) · NSF SBIR Project Pitch rolling (26-510; ≤2 pitches per company) · CSA Unify 2027 · CEDIA Expo 2027-09-07 · the LLC's standing (charter 46564936K — a one-minute registrar lookup, Nick's) · the IP assignment (an unsigned draft in `legal/`).

## §14 Addendum (v76 beat 2, Thu 2026-09-17 evening) — the hardware calendar at the order; the window is desk work to Wednesday; the meter chain re-cut
**The calendar (Nick's order, $202.21):** Sat 09-19 the Third Reality plug (vendor 2), the SNZB-06P24 (IR-18), two clamp lamps, the first cross-check meter; Mon 09-21 the second cross-check meter; **Wed 09-24 the P4460 (reference A) and the 40 W appliance bulbs (the load).** Saturday's box enables no measurement; the operative hardware date is Wednesday — inside §2 row 3's "units in hand by wk 09-28" with four days to spare; rehearsal 2 (wk 10-05) unmoved. **Every hour to Wednesday is desk work:** ENERGY-READ (b3, dispatched Fri) and the bench scenario land before the hardware, so the hardware walks into a finished harness.
**The meter chain (D-v75-6, `DEVICES: extended <edits>`):** A = P4460 (0.5 % typical / **2 % max** on power — the band a `within` verdict is claimed against is the guaranteed one: **3.03 % at 80 W**, 1.53 % likely; the PN2000 and the UT230B-US are out of production); B = two no-spec generics of different brands (the 100× divisor ambiguity needs no spec; a second manufacturer catches A's systematic error; the shared-OEM tell recorded at CHAR); the load 80 W. **CHAR replaces XCHK:** A + B1 + B2 in series on the load, ~20 readings after thermal settle, each B's offset and spread recorded, k = 2, CHAR bracketing each rep session; the offset is recorded and never corrected for. **The reps run wall → A → PLUG → LAMP** (one tare); B enters only to attribute a dispute. The bench lane's `metering-known-load.yaml` carries both chains and the re-written T1.
**The reporting volume (Nick's §4, the b2 audit §4):** at HEAD's thresholds an adopted TR3 reports 0x0702 at the 5-s floor (its divisor 3,600,000); the Gen4's leg depends on a divisor unread; 0x0B04 floods only at divisor 100. Three plugs × 72 h ≈ 155k–311k events — the run's largest input, unmeasured: OR-METERING-VOLUME. IR-25 is a SIZED decision in ENERGY-READ's charter; MEASURE-2 (IR-28) rides the same coder session on the hardware-free rig. **The divisor read:** `GEN4-READ: rehearsal-2` — the first adoption after ENERGY-READ is green (Wednesday's CHAR session if the lane has landed); a HEAD interview reads no metering attribute, so a Monday join would return nothing the charter needs.
**The words (§8 of this plan, closed):** `GAPS: neither` · `HOTMOBILE: skip` · `FOP: apply` (a hub act, b4) · `STRATEGY: after-R5B` with the one-question brief (which number does a stranger ask for first, and which weighting makes it honest — retail position against corpus share) · `CASCADE: engine` · `AMD100: ratify` (a docs card after the hub reads it). **Owed:** `PROTECT: done` (chased) · `ERIK:` — no reply by Fri 09-25 → plan around the opinion.

## §15 Addendum (v76 beat 3, Fri 2026-09-18) — EXT-REVIEW-1 adjudicated; the calendar's weekday; the run's two artifacts; the window to Thursday and to the run
**The calendar, at the screenshots:** 09-24 is a **Thursday** (§14's "Wed" was the weekday wrong, the date right). The bulbs arrive Sun 09-20; the second cross-check meter Mon 09-21; the P4460 Thu 09-24 → CHAR and the first rep that day. A Philips Hue motion sensor is in the order (the model read at the box).
**EXT-REVIEW-1 (the b3 audit):** the reviewer converged on RS-3 §6.1-1 restated as a wedge and left one artifact the record lacked — a pre-registered, falsifiable comparison against the incumbent's evidence trail with a kill test in front of it. Kept by name: the method (verbatim, as pre-registration); the November pilot (D-v76-2); the run as two named artifacts — THE SOAK and THE RECORD'S HONESTY, verifier-checked (D-v76-3; VERIFY-72H on the bench lane); the source-cited why-not (IR-31) in place of a definition-language change (D-v76-4); the vocabulary defined whole in VERDICT-VOCAB-1 and implemented only where exercised (D-v76-5); the premise gate's spoken clause (D-v76-6; THE SPOKEN CARD). At HEAD: E-1 exists (`CausalContext(correlationId, causationId)`); E-2 answered (recorded at decision time; projected from the log; nothing re-evaluated); E-5's gap measured (a mismatched report ages to timed-out; IR-30); E-7 → IR-29. X-6 verified at the HA docs 09-18. Refused: the enterprise wedge; the comparative clause outward; HotMobile (`skip` stands); the pilot before the run; a Gen4 join before ENERGY-READ is green.
**The window (Fri 09-18 → the run):** Fri — ENERGY-READ chartered and dispatched (IR-15 · IR-24 · IR-25 sized · the divisors at interview · THE MEASUREMENT RECORD · MEASURE-2 riding); the bench lane chartered (`metering-known-load.yaml` with CHAR and the two chains, `field_within`, the `link-quality.yaml` skeleton); Nick: the Gen4 firmware, the dispatch paste, `PROTECT: done`, the LLC lookup, Erik's line. Sat — the sitting; the hub silent. Sun — `R5B-2:` → the two intakes; `HARNESS-PLUG:`; FE-115 when a slot frees. Mon–Wed — ENERGY-READ's return and landing; the yaml files' return; VERDICT-VOCAB-1; rehearsal 1's packet; `FOP: apply`. Thu 09-24 — CHAR; the first rep with the divisor read if the lane is green; LINK-READ and VERIFY-72H chartered; the strategy pass chartered (the one question; the wedge sentence; THE SPOKEN CARD; the pilot's page; the recruitment post; the buyer script). wk 09-28 rehearsal 1; wk 10-05 rehearsal 2; the soak; the dry 24 h; **Oct 30 – Nov 2 the run** — the fourth question conditional on ENERGY-READ green by rehearsal 2, else dropped in writing (O-9). November: the pilot; the buyer conversations; REPLAY-1; the exercised vocabulary values; AMD-100's card.

## §16 Addendum (v76 beat 4, Fri 2026-09-18) — ENERGY-READ dispatched; Nick's eight additions, dated
**ENERGY-READ** chartered through THE PREMISE GATE (`context/instructions/2026-09-18_coder-lane_ENERGY-READ_metering-by-clusters_divisors-at-adoption_scaled-reporting_coding-instruction.md`; the b4 audit) and dispatched Fri 09-18: IR-15 · IR-24 · the formatting read at adoption · the two handlers · IR-25 SIZED (1 W / 5–600 s on `ActivePower`; 5 Wh / 5–3600 s on `CurrentSummationDelivered`; predicted < 10k metering events over the run against 155k–311k at HEAD) · the classification INFO line · MEASURE-2. THE MEASUREMENT RECORD minted at `context/planning/measurement-record.md` (rows M2-1..3, G4-1..3, CHAR-1..3, REP-1).
**The eight additions (Nick, Fri 09-18), each ruled:** (1) the premise rows named and grepped in the charter — DONE (the instruction's premise table). (2) **The watts question's go/no-go is DATED: Sun 2026-10-11 20:00 CT** (the close of rehearsal 2's week): ENERGY-READ green (CI on the landing + REP-1 `within` in the measurement record) → the fourth question stays; else the hub writes the drop into this plan THAT DAY as a dated line — never a silence. (3) **VERIFY-72H is chartered Thu 09-24 and rehearsed twice:** GREEN on rehearsal 1's exported log before the soak (gate 1, due Sun 10-11) and GREEN on the dry-24h log before the run (gate 2, due Wed 10-28); a log the verifier cannot grade blocks the run. (4) **The verifier's invariant list, FROZEN now:** (i) partition — every `command_issued` reaches exactly one terminal (`state_confirmed` · a failure-class `command_result` · `command_confirmation_timed_out` · `superseded`); (ii) terminality — every `automation_triggered` reaches exactly one terminal run record; (iii) no unflagged unconfirmed command — a command whose terminal is not `state_confirmed` never renders CONFIRMED; (iv) zero intervals the verifier cannot partition; (v) **the mismatched-report flag** — a `state_reported` on a pending command's target attribute inside its window that fails the command's expectation marks the interval FLAGGED (IR-30's case), never clean; (vi) every emitted verdict is in the frozen vocabulary; (vii) the soak numbers reported (events/h, store growth/h, the counter's tokens). One terminal verdict per layer. (5) IR-31's provenance: the "source it watches" is EXTRACTED from the automation definition's trigger, not declared — the design doc and the rendered sentence's provenance say `derived`, never observed. (6) **The run's log is retained and indexed as pilot material:** the exported event store, the journal and the nightly's logs under `nexsys-bench/corpus/runs/2026-10-30_72h/`, the verifier's flagged intervals as the index, specified at VERIFY-72H's charter. (7) **The buyer conversations start when the script passes the gate:** the strategy pass (Thu 09-24) writes the register-checked script first; outreach begins Fri 09-25; the answers reach the pilot before its incident set is frozen. (8) THE SPOKEN CARD carries a back side — the embargo list (the comparative diagnostic-labor clause first; "reliable"; "complies"; "parity"; any presence-quality number; any name candidate). VERDICT-VOCAB-1's two policy lines: define the whole vocabulary, implement only the values the corpus exercises, the API omits the rest; provenance carried into the rendered sentence, not only the payload. **`PILOT: after`** is a hardware ruling (the fleet is committed through Nov 2). **`EXPORT: letters`**: Nick asks the advisory session to write the seven letters and seven replies verbatim to `_scratch/v76/` → `LETTERS: RETURNED <path> <bytes>` → filed beside the packet.

## §17 Addendum (v76 beat 6, Fri 2026-09-18 evening) — ENERGY-READ landed green; Nick's re-cut of the Java slot adopted with one edit; the window closes
**Landed:** core `d1c2cbc` (ENERGY-READ, 22 files) — CI #253 GREEN at the instrument (`Run check` ✓, the uploads ✓, `Run the bus soak` ✓ → the closure counter **8/20**); install-smoke #61 both arches ✓. THE ADOPTION FENCE LIFTS: the first metering adoption is Thu 09-24's CHAR session (THE MEASUREMENT RECORD rows G4-1..4, REP-1).
**Nick's re-cut (Fri 09-18 evening), ruled:** (1) the rate leg of OR-METERING-VOLUME is closed — no more beats on it. (2) "Storage" as disk-fill is retired: 311k events at 300–600 B is a few hundred MB, and with the scaled thresholds the run's metering volume is under 10k; **the real exposure is the READ PATH at run-scale row counts** — no test runs the system at 100k–500k events, and the explainability projections walk the log (`StandardExplanationService`, `SCAN_BATCH = 500`) — the run completing and the run being readable are different outcomes, and only the first is instrumented. The hub adds the retention fact found at the record: `RetentionPolicy.SOURCE_DEFAULT = (7, 90, 365)` days for DIAGNOSTIC / NORMAL / CRITICAL — the run's DIAGNOSTIC-class events purge after seven days, so VERIFY-72H's export happens AT the run's end, and the pilot's material (NORMAL, 90 days) holds through November. (3) **MEASURE-2b re-scoped as a read-path measurement on a real store** — a real core on a temp SQLite (the `BusSoakIT.bootAndAdopt` boot shape) seeded to N events through the rig's `reportAttributes`; the hero's queries (a run explanation, a non-firing explanation, the causal chain, the dashboard's state read) timed at 10k / 100k / 500k rows; bytes on disk and growth per hour as by-products — and **the boot shape extracted into ONE shared fixture** (a real core, a real store, a scripted radio, seeded to N) so every later scale question is a method call (`HeroLoopHardwareFreeIT`, `BusSoakIT` and MEASURE-2b are the three copies). (4) **LINK-READ after MEASURE-2b, as a SECOND session — the hub's one edit to Nick's one-lane proposal:** LINK-READ's charter through the premise gate is authored against the fixture once it exists at HEAD, not against a fixture the same lane is building; the cost is one paste. ENERGY-READ-b's two rows (the call-site log with `device=`; `ENERGY_METER` for a 0x0702-only endpoint) ride MEASURE-2b's session as its row 0. (5) **The rig-blindness sweep** — every place the hardware-free rig or `FakeNcp` encodes or decodes with the helper production uses is a place the rig cannot see an encoding defect (the uint48 class); a half-hour grep as a § of MEASURE-2b's charter, its findings as register rows. `REVERT` is Nick's if he wants the one-lane shape.
**The bench lane:** BENCH-METER-1 chartered and dispatched (`context/instructions/2026-09-18_bench-lane_BENCH-METER-1_…charter.md`): `metering-known-load.yaml` in the record's method, the two additive engine mechanics (an operator-entered `let:`; `field_within`), the `link-quality.yaml` skeleton; dry-run and selftests only; back before Thu 09-24. **RETURNED and ACCEPT the same evening (b7; the instruments re-run by the hub: 31/0, 43/0, 13 legs); the hub's first rulings corrected at b7c on Nick's critique:** the engine's lazy capture orders the read — it does not make it fresh (no per-attribute stamp; the plug reports on change or interval) — so every rep begins with a load step in the operator's hands; F-1 `operator` DROPPED from `requires:` (the tier keeps the leg out of the nightly); F-2 the record's two subtractions — `field_subtract` (the plug's no-load reading) and `reference_subtract` (the tare, by difference below A's 0.2 A floor), exact-decimal, a bad reference a per-rep VOID; F-3 `on_outside: record` with VOID; F-4 WITHDRAWN (`provenance:` is global). **BENCH-METER-1b** re-chartered wholesale (a half-day). THE THURSDAY ORDER (the b7 audit §0): the Pi's clone pulled Mon/Tue and one nightly watched → adoption → the whole constants re-mint (the flag; the ULIDs into `metering.plug-entity`; `fleet.*` and `remembered-ulids` as listed — boot-health's asserts are set-equal; the hero-less variant + live-basis regenerated — adoption edits the carrier) → the scenario in tmux, CHAR-BEFORE is the CHAR → Fri the digest.
**v76 closes at beat 6 on context health.** The deliverable: ENERGY-READ DISPATCH-READY → dispatched → returned → ACCEPT → LANDED GREEN (exceeded); Sunday's two intakes hand to v77 (opens Sunday on `R5B-2:`). v77's order: the intakes → MEASURE-2b chartered through the gate → the bench return's intake → VERDICT-VOCAB-1 → rehearsal 1's packet → Thu 09-24: CHAR, the first adoption, VERIFY-72H and LINK-READ chartered, the strategy pass (the one question; THE SPOKEN CARD and its embargo side; the pilot's page; the buyer script → outreach Fri 09-25) → `FOP: apply` → AMD-100's card.
