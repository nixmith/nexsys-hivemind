<!--
file: context/planning/2026-09-19_v77_decision-record.md
purpose: THE v77 DECISION RECORD — the decisions packaged for Nick in H10 form during the v77 window (Sat 2026-09-19 →), one word each; silence = the recommendation stands; `REVERT <D-n>` flips any one. Opened v77 beat 3 on the sitting's intake (the b2 audit §6) and KO-2's return (the b3 audit §2). The boot reads §3 only.
audience: Nick (§3 — his rows) · the hub (§3 binds every charter of the window)
state-type: decision record
status: LIVE — v77 at beat 6 (Sun 2026-09-20 ~10:1x CT; instrument 2026-09-20T15:15:24Z). D-v77-1 DONE; D-v77-5 EXECUTED (AUTO-ID-1 ACCEPT). OPEN: D-v77-2..4, 6..8 (silence = the recommendation). BANKED this window: `HIVE: LANDED 7287601` · `HIVE: LANDED 415873e` · `NAV: manual` · `H8-a: RETURNED` (42,844) · `R5B: RETURNED` (50,494) · `PI: pulled fa01cad` (from R-5B B6-1) · `KO: RETURNED` (21,196).
-->

# v77 — decision record

## §1 Nick's words, verbatim
**Sat 2026-09-19 ~07:5x CT (the v77 dispatch text, filed verbatim at b1):** THE WEEKEND ORDER; `NAME:` silence = ii; `STRATEGY: after-R5B`. **~08:3x CT:** "I am considering launching the H8-a navigator session now, one hour early … carefully plan out how we most optimally and impactfully, and thoroughly carry out all work — including how exactly we design each dispatched Cowork/Code session." **~14:0x CT:** "I followed your instructions and stopped the automatic dispatch of the h8-a session, and launched it manually." `H8-a: RETURNED context/audits/2026-09-06_H8a_real-wire_operator-record.md 42844` · `R5B: RETURNED context/audits/2026-09-19_R-5B_operator-record.md 50494` — "Take your time carefully and independently critiquing these returns, and deliberating/deciding on how these results impact how we proceed from here." **~17:4x CT:** `HIVE: LANDED 415873e`; KO-2's summary pasted ("no-go … KILL"); "Carefully and independently review the complete thought process and work flow … Determine how we proceed from here — coding, research, etc."

## §3 The decisions
- **D-v77-1 — the bench card's Core before the adoption (THE THURSDAY ORDER step 0b): `BENCH-CORE: sun|mon|tue`. REC `sun`. DONE Sun 09-20 15:04Z (`BENCH-CORE: deployed c819a02 · 6/6 · 186270→186279`; the b6 audit) — and the card's premise corrected at b5: an `installDist` tree, not a .deb.** The adoption (step 1) edits the carrier on the bench card, whose Core prints `deviceId=None` (pre-CG-123; R-5B B5) and has no metering path; the scenario reads `power_w` from it. An H8-a-shaped card (the four-surface hash custody; `dpkg -i`; the integrity gate; `[PASS] boot-health 6/6` on the old counts), ~25 min, cut Sunday morning through THE PRIOR-LEDGER GATE (H8-a D-1/D-2/D-4). Sunday puts Monday's and Tuesday's nightlies on the full stack. Refutable-by: Nick's Sunday hours. (the b2 audit §0, §4)
- **D-v77-2 — the R-5B window's re-run: `REARM: reh1|thu|drop`. REC `reh1`.** P-B2/P-B3/P-B6 were not tested (D-3: the three-acts choreography handed as prose did not survive the rig); the re-run is a 20-min block in rehearsal 1's packet (wk 09-28) with the navigator's ONE-device, three named acts, say-it-back form; Thursday stays CHAR + adoption + the re-mint. Refutable-by: a free half-hour Thursday before the adoption. (the b2 audit §6)
- **D-v77-3 — the pre-CHAR shakeout: `SHAKE: mon|thu`. REC `mon`.** Monday evening, 20 min, no Zigbee: B1 + B2 in series on the lamp with the 40 W bulbs; the settle time clocked; the two displays read against each other. Buys a B-pair disagreement found with three days to react and Thursday's wait step sized from a measurement. Refutable-by: the bulbs or B2 not arriving Monday.
- **D-v77-4 — the second candidate's knockout returned KILL: `NAME2: accept|counsel|third`. REC `accept`.** S5 (the exact `.com` an operating business's since 2015, no sale path — the program's own law) + S2-EU (the sound a senior class-9 software mark; the one-V spelling registered in 9/11 three months ago). D-v74-1 (ii) resolves: the swap runs once on PALOKI when the written opinion lands; PELTON-READY's fallback is now NAMING-B, re-armed and dispatched only on a NOT CLEAR opinion (`NAMING-B: go` is Nick's at any time). Refutable-by: the `.com` sold at an acceptable price AND counsel reading the EU software mark as coexistable. (the b3 audit §2)
- **D-v77-5 — the Java slot's next unit: `JAVA-NEXT: autoid|linkread`. REC `autoid`.** MEASURE-2b found (F-1, IR-39) that every restart mints new `AutomationId`s and a non-firing read after a restart says NEVER_TRIGGERED of an automation that fired; rehearsal 1 (kill −9, wk 09-28) is a restart and the 72-hour run's honesty invariant cannot carry it. AUTO-ID-1 realizes AMD-93 §2.3's engine-managed identity companion (`automations.ids.yaml` beside the definition file, AMD-71's layout; tombstones per §2.3), wired at `HomeSynapseCore.java:677`, with a restart IT on `RealCoreFixture`; LINK-READ follows. Refutable-by: a restart-free run plan (there is none — rehearsal 1 is the restart). (the b4 audit §0)
- **D-v77-6 — the presence sensor's place: `PRESENCE: pair|bath`. REC `pair`.** Beside the SNZB-03P: two principles of occupancy on one room, the corroboration pair EXPLAIN-4's "someone is home" condition is written for, the frame table paired sample-by-sample; joins at rehearsal 1 with the Hue motion (one re-mint), never on Thursday's tail; powered off until then. The bathroom is a second area and a second automation, after the run. Refutable-by: VERIFY-72H's automation set needing a second area before the run.
- **D-v77-7 — the Hue motion's place: `HUE-MOTION: second-area|pair|hold`. REC `hold`** until VERIFY-72H's automation set (Tuesday) names the run's second trigger source; it joins with the presence sensor at rehearsal 1 either way.
- **D-v77-8 — the second bench deploy on AUTO-ID-1's sha: `BENCH-CORE-2: today|wed`. REC `today`** — the deploy is a pull and a two-minute build; Monday's nightly then runs on durable ids; the first boot mints every id once and writes `automations.ids.yaml` (R5). Refutable-by: CI red on the AUTO-ID-1 sha.
- **Carried, no word owed yet:** `HARNESS-PLUG:` Monday on `S31-LABEL: 15A 1800W — Input 120V @ 60Hz` (the label admits a power-cycle harness for the held card's supply; bench-hero's CommandAction on the S31 is the design question rehearsal 1's packet must answer); `C003:` cannot be ruled until `REARM` runs; OR-FAILCHAN re-scoped at Sunday's close (instance 1 closed; instance 2 = IR-33; the `SuccessExitStatus=143` mask question ruled with the §6-B commit re-read).
