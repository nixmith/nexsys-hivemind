<!--
file: context/planning/2026-09-26_v81_decision-record.md
purpose: THE v81 DECISION RECORD — Nick's words verbatim, the facts given, the decisions (D-v81-n) with their ruling forms and refutable-bys, the carries from the v80 DR §3–§3d, the open words. One file for the window (Sat 09-26 → Mon 09-28); edited per beat; CLOSED at the window's close.
audience: Nick (rules by word or by silence) · the v81 hub (edits per beat) · the v82 hub (reads §3d + the Carried rows at boot — the trim of D-v81-6)
state-type: decision record
status: LIVE — opened v81 beat 1 (Sat 2026-09-26 ~08:5x CT; instrument 2026-09-26T13:58:41Z).
-->

# v81 — the decision record

## §1 Nick's words, verbatim (Sat 2026-09-26 08:36 CT; the first message, pasted under the v81 dispatch text — the text itself is `context/handoff/2026-09-26_v81_dispatch-text.md`, whose fenced block = the paste at the bytes: md5 `0d3d272e944d291c15940b4e9ea12e19` after trailing-whitespace and blank-line normalisation, 8,030 B pasted vs 8,017 B on disk)
> `HIVE: LANDED`[3fa0683](https://github.com/nixmith/nexsys-hivemind/commit/3fa0683da9c988658b8c53adf1e78f1c04b5c193) · `SAT-NIGHTLY: <the nightly line>` · `RIG: Wall -> Kill A Watt -> 1 ft single extension cord -> SURAIELEC -> clamp lamp, G4-1 plugged in switched off, TR3 plugged in switched off, G4-2 plugged in switched off, S31 unplugged, Philips Hue motion sensor unboxed and unplugged, SNZB-06P24 unboxed and standing by,` · `HOURS: currently 0830 on Saturday and I have the entire weekend plus Monday to write code, focus on the company, focus on the smart ecosystem, focus on research, and even focus on renaming for Paloki when/if deemed appropriate to accomplish fully (including renaming for our entire knowledge base throughout all nexsys-hivemind and ClaudeFolder`
>
> Take your time following all instructions, second-checking and validating our actual knowledge base against what we are optimizing and contextualizing for — ensuring all coder, research, testing/benching, and strategy/design sessions we dispatch from this hub/orchestration session will result in carefully crafted, precisely and fully articulated feedback and results that will drive this conversation forward. This hub/orchestration session will be responsible for diligently, critically assessing all returns with any constructive criticism or feedback so we can improve results as necessary.

## §2 The facts given (banked; no act)
- `HIVE: LANDED 3fa0683` — VERIFIED at the instrument: hivemind `log -1` = `3fa0683`, `rev-list origin/main..HEAD` = 0, porcelain 1 (the HIVE-CLEAN-2 charter, §3 D-v81-4). The b2 + b3 + b4 card ran as cut.
- `SAT-NIGHTLY:` — NOT GIVEN at 08:36 (the placeholder pasted literally); read by the first act at 08:50 CT (`ssh pi '~/bench.sh digest 1'`): `2026-09-26 quiesced AUTO floor: 7/9 · FAIL command-confirm-s31 · bundle /home/homesynapse/hs-bench/bundles/command-confirm-s31-20260926T083122Z · 1 SKIP(hue-online) · fleet: 9/9 · re-seen 6 · bench-hero RESTORED ✓ · ON-latency n/a(FAIL)` — read in D-v81-8.
- `RIG:` — the affordances as stated: A = the Kill A Watt (P4460) in the wall; ONE 1-ft single extension cord in A; B1 = the SURAIELEC on the cord; the clamp lamp on B1; G4-1, TR3, G4-2 plugged in and switched OFF (the relay state is READ from the api before every card, never assumed); the S31 unplugged (HANDS OFF); the Philips Hue motion sensor unboxed, unplugged (`HUE-MOTION: hold` stands); the SNZB-06P24 unboxed, standing by (`PRESENCE: pair` stands; no device joins outside a card). B2 (the MECHEER) NOT NAMED → asked (`B2:`); where the plugs sit → asked (`PLUGS:`); the bulb → asked (`BULB:`). **Answered 08:54 CT (verbatim):** `B2: yes, on the desk and unplugged.` · `PLUGS: are all plugged into different wall sockets all located within a few feet of the Pi, and can be moved and/or arranged however you see fit.` · `BULB: yes, I have two clamp lamps, both with a 40W bulb as last night.` — so CHAR runs two passes (B1, then B2) on the one cord; each plug moves from its wall socket into the cord's end for its REP card and back after; the load stays ONE 40 W lamp (the constants' `load_w: 40` at `df4a2d7`; the second lamp needs the strip the rig does not have — it stays out).
- `HOURS:` — Sat 08:30 CT → Mon, whole days; at the rig never after 21:00 CT (THE SIMPLER WAY (7)). The window as cut stands: Sat METER-2 → the CHAR → THE HORIZON RE-CUT · Sun rehearsal 1's packet (IR-18 first) + VERIFY-72H + BENCH-CORE-3 + the strategy pass + B-1 + the attorney search + the K-refresh · Mon `OUTREACH:` + C-S0-1 + v82's text.
- The five HEADs (one call, 2026-09-26T13:40Z): core `d22a8a4` · hivemind `3fa0683` · skills `180375f` · bench `df4a2d7` · docs `7221ddc`; every push count 0; no lock file. The preflight PASS 12/12 (the audit §2).

## §3 The decisions
- **D-v81-1 — the window's ONE deliverable: rehearsal 1's packet DISPATCH-READY on the metered fleet (D-v80-1 carried). CONFIRMED.** Saturday's path to it: METER-2 → the CHAR's datum banked → THE MEASUREMENT RECORD rows CHAR-1..3 and REP-1 filled from the bundle. Refutable-by: a STOP at the rig that the desk cannot re-cut by Sunday (then the packet is cut on the fleet as measured so far, and names the CHAR as its "next instrument").
- **D-v81-2 — the intake: RULED as facts (§2).** The one act handed at beat 1: the nightly's line by `ssh pi '~/bench.sh digest 1'` + the three affordance words `B2:` · `PLUGS:` · `BULB:` — THE SIMPLER WAY (2): the affordances are asked BEFORE the card is cut, and the prompts of `nexsys-bench/scenarios/metering-known-load.yaml` :85–:241 were read whole at this beat (the audit §3). No act at the rig until card 1.
- **D-v81-3 — THE RENAME ("renaming for Paloki when/if deemed appropriate … the entire knowledge base"): RULED not this window, on the record.** THE PREMISE (D-v80-3): the application is COMMISSIONED, NOT FILED — no serial, no receipt; the name enters the tree at the rename WU, which is the launch moment (FILED → the receipt → B-1 → W2 → the rename); every work product stays token-parameterized (arc 17, `{{NAME}}`); the v81 text's own REFUSE list holds "a written or public use of the name before its protocol row". What a knowledge-base-wide rename now would cost: every file in five repos while three lanes and HIVE-CLEAN-2 are live (a collision with every census), and it buys nothing before the receipt (the pilot's page and the code carry the token until then). What the record DOES put on today's desk: the rename's DATE is question 5 of THE HORIZON RE-CUT (D-v80-8: "the rename against 10-31 — G2 :22 vs SoR :59; `control.in`'s placeholder domains"), ruled today with `POSTURE:`; and a RENAME-READINESS CENSUS (a read-only lane: every file and line in the five repos that carries `{{NAME}}`, `NexSys`, `HomeSynapse`, `nexsys-`, `homesynapse-` — counted, never edited) can be chartered any idle beat so the rename WU is sized before the receipt. Options: (a) hold the act to the launch moment; run the readiness census this window — REC. (b) `RENAME: now` — a REVERT of D-v80-3's sequence; the hub asks the reason, and charters the rename as its own lane AFTER the packet, never inside a rig window. (c) `RENAME: census-only` — the census, no date question. Refutable-by: counsel's receipt in hand (`DRAFT:` → the ten-item read → FILED → the receipt), or Nick's word (b).
- **D-v81-4 — HIVE-CLEAN-2 (`context/instructions/2026-09-26_HIVE-CLEAN-2_hygiene-lane_charter.md`, 13,183 B, untracked; cut by the v80 hub at 2026-09-26T03:54Z, fourteen minutes after the close beat, on Nick's ask "very meticulously and surgically clean up all context"; in no spine line, no DR row, no §HELD row): RULED — tracked at this beat's card (A), status DISPATCH-READY as written, DISPATCHED AT THE v81 CLOSE** (after the close card lands, before v82 boots — the charter's own audience line: "the v81 hub (boots after the landing)"). Why not now: the lane `git mv`s live files and tidies `_scratch/`, the two surfaces every v81 beat writes; its uncommitted moves would sit in the porcelain every card computes its census from. Refutable-by: `CLEAN: now` (then the hub writes no beat until `RETURNED`, and the lane's card lands before the next hub card).
- **D-v81-5 — the bench's own selftest is RED at `df4a2d7` (`python3 -B tools/runner/test_engine.py`: 36 checks, 1 failure — BM1b T3): RULED — METER-2 is a bench CODE lane, not the hub's guarded edit.** The failure read at the bytes: T3 walks the REAL scenario with the charter's 80 W inputs against the REAL constants' bands; T4b's re-mint (3.65 / 3.53 at 40 W) flips the scripted TR3 rep 1 from WITHIN (3.755 % after the tare, at 4.03) to OUTSIDE (at 3.53) — a test expectation pinned to live constants, not an engine defect. The lane: (i) the prompts re-cut to the rig as it is (D-v81-7's shape; the 80 W wording gone; the hard-coded "band 3.03 %" / "4.03 %" in the REP goals replaced by the constants' interpolation or dropped); (ii) BM1b T3 made band-independent — the bands and `load_w` overridden in memory the way the ids and flags already are — so a re-mint never flips a test; (iii) the selftest 36/36 in the return, run by the hub again at intake; (iv) T4's pin "22 let entries in order" untouched (the sitting's cards group the prompts, the scenario does not shrink). ≤ 2 h; the bench card is Nick's hands (`BENCH: LANDED <sha>`); the CHAR runs on the landed sha, never on a red selftest. IR-57 minted (a constants re-mint runs the selftest before its card). Refutable-by: the lane finding an engine defect under T3 (then the CHAR waits on its fix, and the packet names it).
- **D-v81-6 — the boot read-set over budget: 48.8 KB read against §1's ≤ 45 KB (the DR §3–§3d alone 12.8 KB), disclosed.** RULED — v82's text trims the DR read to §3d + the Carried rows (the dispatch text's WORDS GIVEN line carries the rest); no other read grew. No option; a budget fact.
- **D-v81-7 — the sitting's shape, pre-registered from the prompts (:85–:241, read whole): SEVEN cards, ≤ 5 acts and ONE line each, every card cut at the desk after the prior card's line.** Card 1 — `tmux kill-session -t metering` · the availability read ×3 (one command; three lines) · G4-2's re-join if UNAVAILABLE (T1b.sh's window, then "Start pairing" on the plug's own page, then the read again) → `CARD1: G4-1 <A|U> · TR3 <A|U> · G4-2 <A|U>`. Card 2 — the scenario starts in tmux (one window); CHAR-BEFORE: pass 1 wall → A → cord → B1 → LAMP (the tares at lamp-out 30 s; lamp in; two minutes; three A/B1 pairs), pass 2 the same with B2 if at hand; ENTER → `CARD2: <pairs> pairs · <n> passes`. Cards 3–5 — one plug each: TARE (W1 no plug, W2 with the plug, type W2 − W1) · OFFSET (lamp out, relay ON as READ at the api, type `power_w`) · volts ×2 · REP ×3 as ONE repeated act (lamp out 15 s, in, wait for the lit value + 5 s, read A, type) → `CARD<n>: <plug> <W/O/V ×3>`. Card 6 — CHAR-AFTER (the passes again; type the count) → the verdict line → `CARD6: <verdict> · bundle <stamp>`. Card 7 (desk) — T8 the bundle home + T3.sh's window-key check → `CARD7: files <k> · Bearer 0 · key <absent|PRESENT>`. Pre-registered: the nine reps WITHIN at 3.65 / 3.53; an OUTSIDE rep is recorded and attributed after CHAR-AFTER, never a stop (the prompts' own rule); a surprise is a STOP-and-close, the next card cut at the desk. Adjudicated at 08:54 on Nick's words: `B2: yes` → two passes; `PLUGS:` wall sockets, movable → the REP chain as written; `BULB: yes` → the 40 W bands stand. Refutable-by: a surprise at the rig (STOP-and-close; the next card cut at the desk).
- **D-v81-8 — THE NIGHTLY LINE READ (Sat 03:30 CT, the first on the 9/9 constants): RULED, field by field.** `7/9 · FAIL command-confirm-s31` — the S31 is UNPLUGGED (Nick's RIG line; the record held it OFF and unloaded, in the wall): a FAIL by construction, not the intermittent class; a row, not a stop; `ON-latency n/a(FAIL)` follows it. `1 SKIP(hue-online)` — as pre-registered. `fleet: 9/9` — the T4 re-mint holds (expected 9, adopted 9; D-v80-11's RED pre-registration lifted and CONFIRMED). **`re-seen 6` is BY CONSTRUCTION and says nothing about membership:** `tools/runner/nightly_digest.py` :150–:172 — `re_seen = |now ∩ prior|`, the rows LAST NIGHT's captured id list already knew; Friday's nightly read 6 (the plugs were adopted Friday EVENING, after it), so 9 ∩ 6 = 6. Its own docstring: "the announcement-based split — which of those rows actually spoke — is the LOG's instrument, not this one." **The record's sentence "`re-seen <n>` of 9 is the Gen4-membership instrument" (the v81 dispatch text; the plan §27; pm-lessons 2026-09-25, the second entry) is CORRECTED here:** the membership instruments are the API's `availability` per device (card 1's read, before every sitting) and the log's announce / relink lines; the nightly's `re-seen` is a registry-identity instrument (card identity across nights). Pre-registered: Sunday's nightly reads `fleet: 9/9 · re-seen 9` by construction whatever the plugs' membership. The S31: `S31: back-in-at-close|stays-out` — REC back-in-at-close (card 7's last act: into the wall, not pressed, not loaded — so Sunday's nightly runs `command-confirm-s31` and the S31 read after the packet has a night); the word `S31:` also carries since when it has been out. Refutable-by: a `device_announce` / `device_relinked` line for a plug in the nightly's log window (then the log, not the API, is the first membership read of the day).
- **Carried from the v80 DR:** D-v80-7 adjudicated (the S31 read → the packet's "next instrument") · D-v80-8 (`POSTURE:` on today's re-cut; the Lane B digest) · D-v80-11 (Saturday's nightly on 9/9 — read by the first act) · D-v80-12 (`ANCHOR-ID: key` REC) · D-v80-14/15 (the fleet 9/9; the constants) · D-v80-17 (THE SIMPLER WAY, the law of this window) · the IR-18-first order for the Java slot (Sunday) · the v79 carries (D-v79-7 `OUTREACH: mon` · -8 `HANDLES: secure` · -11 `HORIZON: sat` · -12 `FIN1: contribution`) · D-v78-2 `verify72h` · D-v78-3 `B7: ratify` REC · D-v79-4 `HARNESS-PLUG:` held.
- **Open words (v81 b1):** `S31: back-in-at-close|stays-out` (+ since when) · `CARD1:` … `CARD7:` (one line each, at the rig) · `BENCH: LANDED <sha>` (METER-2) · `POSTURE: install-first|pilot-first|evidence-dated` (the re-cut) · `RENAME: now|census-only` (silence = the REC) · `CLEAN: now` (silence = at the close) · `KREFRESH: sat|sun` (silence = sun) · `DRAFT: <path>` · `B7:` · `SAMPLE: <sha>` · `REPIN: <sha>` · `REVERT <D-n>`.
