<!--
file: context/planning/archive/phase-3-backlog-currency-notes-rotated-2026-07-26.md
purpose: Rotation archive (v38 beat 5) — the phase-3 backlog currency notes older than 2026-07-19 v35-beat-6, verbatim.
state-type: history
-->

# backlog currency-note rotation archive — 2026-07-26

> **2026-07-19 (v33 hub, beat 4 — THE JOINS NIGHT close-out) currency + row mints:** **M9.7-W2 ✅ DONE** (core `9d40ce8`, CI green, two-layer audit ACCEPT [R1/R2 ratified], deployed + re-verified at the Pi, SILICON-PROVEN the same night: the 02P live-reads temperature_c 26.1 / humidity_pct 51.7 / battery 100 through the new handlers; all four profiles matched on the wire at interview). **THE JOINS NIGHT outcomes:** **E2 [M] ✅ DISCHARGED** (fresh formation `posture=generated_seed context=formation`, ch20/panId 0x774c, FIVE clean TCLK arcs `TC_REQUESTER_VERIFY_KEY_SUCCESS`, zero failures; E3's public statement now writable) · **B2 [M] ✅ PROVEN end-to-end** (Wave-1 entity ULIDs byte-identical from the pre-night baseline through a fresh formation, ~7 boots, and five adoptions) · **H4 [S] ✅** (the deploy boot's restart-honesty glance) · **D1 at 4 classes LIVE** (occupancy 03P · temp/hum 02P · battery-only 01P per R2(B) · switch S31) + light BANKED-HISTORICAL (the Hue is off-network — see HUE-RESET) + contact HELD (see W2-LEARN) — **D1 closes 5/5 at the 04P adoption**. Fleet: 5 adopted + the 04P joined/enrolled/held; permit-join key OUT (the SD-5 exception window closed). NEW ROWS: **W2-LEARN** (REQUIRED micro-WU, the D1 closer — on silicon, 3 enrollments produced ZERO `ias_zone_type_learned`: nothing elicits zoneType onto the wire; learn it from the ZoneEnrollRequest parse [grounding-first]; FOLDS IN the S31-ROUTE question DEMOTED to verify-first: the S31's reports DO ingest despite its confirmed 0xC05E descriptor [`on:true`, stateVersion 7 — the frames-carry-0x0104 hypothesis; the `confirmation_downgraded … VERIFIED_REPORTS/SLEEPY outcome=best_effort` line = the configure-verify TIMEOUT class, honestly downgraded; the posture-re-emit path upgrades it on a post-fix re-drive]) · **BENCH-CONST** (the constants re-mint: fleet 2→5/5, position watermark 18946, panId 0x774c, the `${C.fleet.devices}` parameterization — **boot-health currently reads [FAIL] for exactly this known instrument-staleness reason**; priority-quick) · **HUE-RESET** (decision row, Nick: a ~$25 Hue dimmer switch [hardware] vs a TOUCHLINK-RESET feature WU [code]; until either, the Hue stays off-network with rehydrated-AVAILABLE — the live AVAIL-RECONCILE exhibit; Hue-targeted bench-hero actions lawfully §3.9-skip, the adjudicated class) · **MATTER-INT** (design-doc-first post-gate row; Nick's one-word ruling pending: row-only / row+design-lane / code-now — PM REC row+design-lane at CMD-API dispatch; the Gen4 plugs stay ruled stimulus-only, radios never provisioned). Operator constraints recorded: SNZB battery covers don't open (re-announce = long-press re-pair in a window, never battery pulls) · the Hue has no in-house reset path (it joined factory-new originally). **The M14 sweep (~Jul-28) now awaits ONLY the G1 dispatch OUT.** Detail: pm-handoff v33 beat 4.

> **2026-07-18 (v32 hub, close-out) currency + row mints:** **I3a CLOSED AS FROZEN + the M14 RO condition ✓** (2026-07-18 `[PASS]` on the REV2 instrument — reopen +17 s, the 6th silicon rep; five dispatches through the reopened transport, outcomes quoted) · **F2 [M] CLOSED both directions, measured** (offline on evidence ≈11.5 min post-cut [ping-phase]; warm return **0.7 s**; API before/after artifacts captured; plus the multi-day log-rehydrated-offline persistence) · certification ✓ · zero open [REVIEW]s — **the ~Jul-28 M14 sweep now awaits ONLY the SEED silicon leg (the joins night) + the G1 dispatch OUT.** NEW ROWS MINTED (detail: pm-handoff v32 beats 1–3): **SKIP-VIS** (core WU candidate — per-target §3.9 skip observability: an INFO token + an action-level skip surface + causal-chain rendering + the non-firing `completedVerdict` honesty fix; routes with EXPLAIN-PUSH/G1) · **BENCH-PRECOND** (history/availability preconditions for liveness-class scenarios; B2) · **BENCH-AUTOSTART** (the ~2.3-day power-outage evidence gap; rule at B3 authoring, where nightly N-accumulation makes a self-starting bench load-bearing) · **RUNNER-VERSION-BANNER** (bench.sh prints its repo HEAD — instruments self-identify; B2) · **POWER-RESTORE scenario class** (the outage rehearsal: boot order · device-recovery envelopes 0.7 s warm / ~50 min cold-start · availability honesty through the trickle; corpus + B2+) · **CMD-API** (the named ruled core WU — rider 3 sequencing with the B2 port ahead of M14; the ~Jul-24 tripwire armed with the honest posture: C1's N thresholds already numerically exceeded by the certified organic evidence) · **BTN-AMD** (R2(B) of record: the 01P adopts battery-only, presses log-visible; graduates post-gate) · **TAMORO/R-1** (Nick-owned self-governing program: consult 07-21 · G-1/G-2/G-3 · R-1 HELD for G-2; the hub consumes gates as pointers). The BENCH-AUTO B-row now carries the complete B1→REV1→REV2→I3a arc — **the runner is FIELD-PROVEN** (it caught the silent-skip class on its first live rep). The operating charter for all of it: `context/process/2026-07-18_compounding-testing-doctrine.md` (Nick's compounding-testing charge — DISCHARGED).

> **2026-07-10 (v27 hub, beat 2) currency:** **THE ACCEPTANCE ARC IS DONE + CERTIFIED-INTO-SOAK** — the M9.4 mega-row + M9.5/M9.5b content COMPACTED to one certified row below (detail: git history + the bench record `a81d678` acceptance block + PROJECT_SNAPSHOT v26 beats 1–3). **THE 72 h SOAK RUNS on `04f5f70`** (ends ≥ 2026-07-13 ~07:50 Pi-local; **NQ-6 RULED (b) = the exit act**, runbook Phase 7 banked). **+15 rows added = the 2026-07-10 ruled post-soak stack** (NQ-6-EXIT · SEED-CUSTODY [SD-5, post-soak #1] · M9.6-RO [root RE-ATTRIBUTED: identity-capture bug] · AVAIL-ALIVE [mechanism-without-driver #6] · EXPLAIN-PUSH [elevated] · BENCH-AUTO B0–B5 [B0 ✅ this beat] · HS-RADIO [chartered] · FRAME-CTR · CMD-DEADLINE · CMD-LANES [Wave-2] · UDEV-RAMP [M9.6-RO-coupled] · INV-CH22 · WATCH-0xFFFF · SKILLS-CONST · HW-ORDER). Rulings verbatim: pm-handoff v27 beat 2. Detail: PROJECT_SNAPSHOT newest beat (pointer, not copy).

> **2026-07-03 (v16 hub launch) currency:** **M9.2 ✅ DONE + LANDED on core `6ea6912`** (transport substrate: SerialByteChannel → ASH codec/session → EzspAshTransport → EzspCoordinatorProtocol → probe/port-identity/watchdog → §3.13 formation; 39 files, clean-tree `check` GREEN ×2; two gate rounds hub-fixed; R1–R7 ruled ACCEPT + hardening H1–H5; WUCP Phase 2 complete at v15 beats 58–59). **Website skeleton ✅ BUILT** (Astro @ docs `website/site/`, 6 pages, zero-JS enforced; lane audited ACCEPT beat 59; docs Commit A = Nick residual). **M9.3 (interview/ingestion/profile registry) = NEXT Core slot, Claude Code lane — GATED on Doc-18 Lock** (§3.5 namespace governance binds the profile registry; inheritances in coder-handoff NEXT-WU pointer + zigbee MODULE_CONTEXT). M9.4 stays JIT. Frontend slot = the dossier-content lane (W-5/PG-1, gate-4 content gap). Detail: PROJECT_SNAPSHOT newest beat (pointer, not copy).

> **2026-07-03 (v15 hub launch) currency:** **M9.1 ✅ DONE + CI-GREEN on core `ec2e3b4`** (the integration spine LIVE: StandardIntegrationSupervisor + CommandRoutingSubscriber + Phase-6 wiring, fake-adapter-proven, INV-ES-09-pinned; ci.yml + install-smoke green on the push; WUCP Phase 2 ACCEPT at v14 beats 51–52; maxHeight 7→8 ruled) and **FE-1b ✅ DONE on core `cf3733e`** (dashboard live-integrated end-to-end, v1.1.1 fold + live smoke PASS; v14 beat 54; `frontend.yml` glance on `cf3733e` = Nick residual). **M9.2 (transport/EZSP) = NEXT Core slot, Claude Code lane** — authored by the v15 hub per the split charter (`context/audits/2026-07-02_M9-authoring-lane_return.md` §3) with the Doc-18-charter notes bound; M9.3/M9.4 stay JIT-authored; **Doc 18 Locks BEFORE M9.3 issues** (DP-18-A/B/C = Nick). Website lane = the frontend slot (gate 4's open half). Detail: PROJECT_SNAPSHOT newest beat (pointer, not copy).

> **2026-07-02 (v14 hub, beat 50) currency:** **M7.5c-a ✅ DONE + CI-GREEN on core `e3d7296`** (A4/A5 frozen-envelope conformance per the DRIFT-1 ruling; `check` 149 + ci.yml + install-smoke all green on the push; one hub-fixed test-compile round). **The roadmap's M9 epic row is REPLACED by the ratified M9.1→M9.4 split** (beat-46 DP-A ruling; charter = `context/audits/2026-07-02_M9-authoring-lane_return.md` §3). **M9.1 = NEXT Core slot, ROUTED to Claude Code** (host-side lane per Nick's 2026-07-02 routing decision — in-session compile loop; the Cowork hub stays spine-writer + auditor). FE-1b dispatched as a parallel Cowork lane (`context/handoff/2026-07-02_FE-1b_v111-fold-and-live-smoke_session_prompt.md`). **Discovery: `install-smoke.yml` is ACTIVE on main and green on every push** (activated `652f9b3` alongside frontend.yml) — the carried "install-smoke unscheduled" gate-4 item is CORRECTED; the residual gate-4 surface is the WEBSITE build only. Pre-M9 queue state: AMD-87 ✅ long-cleared (M5-A Part 2); NQ-6 rides the M9.4 soak per the return's rec.

> **2026-06-28 (v11 hub, beat 34) currency:** **AB-4 ✅ DONE + GREEN + WUCP Phase 2 ACCEPT** (cipher activation — the at-rest cipher LIVE for `[identity, presence_personal]`; F1 `v1` envelope byte + F3 nonce-fence + F13b fail-closed; `./gradlew check` GREEN 149 tasks; source-verified **zero-mint 71/41/53, zero-edge, zero-DDL**; SHA reconciles at Nick's commit on `eed477e`). **Next Core slot = M9** (real Zigbee; hero on real silicon, acceptance-tested vs the bench corpus) — or M7.5c (events+health) if Nick re-sequences. The 3 pre-M9 inputs: AB-4 ✅ · bench first-light (pending) · AMD-CAND-1 + D2 (route-ready, parallel). Carried: OR-GATE-M7.4 + AB-4 CI-green; the A4.5 Doc 15 note (Nick).

> **2026-06-28 (v11 hub, beat 33) currency:** **AB-4 ✅ ISSUE-READY** (`context/instructions/2026-06-28_AB-4_cipher-activation_coding-instruction.md`) — the ruled next Core slot (leapfrog M7.5c): cipher activation = A4.1 6-arg-ctor flip (M6.3 at-rest encryption goes live for `[identity, presence_personal]`) + A4.2 F1 `v1` envelope version byte (prefix + GCM AAD) + A4.3 F3 nonce-construction fence + A4.4 F13b fsync-fail-closed (scoped past Windows) + A4.5 backup-key seam; **zero-mint/edge/DDL**; authored from a source-verified grounding (much smaller than the 2026-06-19 spec — AB-1/AB-2/M6.3/F13a landed). Route → Coder → gate → WUCP Phase 2. Sequence: **AB-4 → M9** (M7.5c events/health deferred, rides the frontend). Parallel pre-M9 governance: AMD-CAND-1 (routes NOW) + D2 (route-ready). Frontend: substantially built (gate-2 render half largely done, real-transport-wired); residual = the live-integration pass.

> **2026-06-28 (v11 hub, beat 32) currency:** **M7.5b ✅ DONE + GREEN + WUCP Phase 2 ACCEPT** (the non-firing read `GET /api/v1/automations/{id}/non-firing` + `GET /api/v1/automations`; `./gradlew check` GREEN 149 tasks, verified actually-green; source-verified against the diff — **zero-mint 71/41/53, both module-infos + build.gradle.kts UNCHANGED**; SHA reconciles at Nick's commit on `9ec5949`). The read-API differentiator core is COMPLETE (M7.5a + M7.5b — both hero halves on the live log) = **gate-2's read-API half DONE.** DP-B2 default shipped (hub rec ACCEPT). **Next Core slot = Nick's ruling (hub rec: leapfrog M7.5c with AB-4** — the ready un-gated M9-prerequisite, AMD-87 cleared; M7.5c events/health is the deferrable supporting feed riding the frontend) → M9. Pre-M9 governance in parallel: D2 INV-id (route-ready) + AMD-CAND-1 (confirmation, route-ready). Carried: OR-GATE-M7.4 CI-green (now covers the M7.5b commit too).

> **2026-06-28 (v11 hub) currency:** Spine at **core `9ec5949` (M7.5a COMMITTED — the read-API hero read; the M7.4 arc COMPLETE) / docs `75d0345` (Doc 16+17 LOCKED, AMD-95, 170/50) / hivemind `c3a6f8b` (the beat-28 bench-reframe crystallization COMMITTED) / bench `4c4496d` (`nexsys-bench`, the 5th repo — inited)**. **M7.5b is the NEXT Core slot** (the non-firing read `GET /api/v1/automations/{id}/non-firing` + `GET /api/v1/automations`) — authored by the v11 hub against committed `9ec5949` per the read-API breakdown + the frozen v1.1 §B3, on M7.5a's `ExplanationService` plumbing; **zero mint (71/41/53 hold)**. Launch preflight STALE-benign → reconciled (the v10 note below anticipated the bench init + crystallization as pending; both are now committed). Carried residuals: OR-GATE-M7.4 (CI-green on `4e31aed`+`9ec5949`) + the D2 canonical INV-id registration (review→ratify). The three streams: A — bench (`nexsys-bench`, Phase 0/1) · B — device-model research (dispatch READY) · C — Core (M7.5b).

> **2026-06-27 (v10 hub) currency:** Spine at **core `4e31aed` (M7.4a/b/c/d DONE GREEN — the M7.4 arc COMPLETE + COMMITTED; OR-M7-WIRING RESOLVED, D2 replay-purity CI-pinned) / docs `75d0345` (Doc 16 + Doc 17 LOCKED, AMD-95, 170/50) / hivemind `828cdb6` (the v9 arc `f613c36` + the v10 prompt)**. **M7.4d ✅ DONE GREEN + COMMITTED (`4e31aed`** — `RunPipelineReplaySafetyTest`, test-only; counts hold 71/41/53, module-info unchanged). **M7.5a (the read-API hero read — `GET /api/v1/runs` + `/runs/{id}/causal-chain`) = ✅ DONE + GATE-GREEN 2026-06-28** (built by Claude Code; `./gradlew check` GREEN 149 tasks + the 51-task targeted compile/test run, verified actually-green; WUCP Phase 2 ACCEPT — pure log-derived projection, **zero mint (71/41/53 hold)**, `core/automation/module-info` UNCHANGED, `api/rest-api/module-info` +2 plain requires (`automation`+`jackson.databind`); the 8 `[REVIEW]`s all ACCEPT, incl. the registry-free pure-LOG outcome derivation that **corrects** the instruction's DP-A2 (registry-gating would have broken INV-SA-03 determinism). **COMMITTED `9ec5949` (2026-06-28; core `4e31aed`→`9ec5949`); CI-on-pushed-commit confirm is the residual.** Instruction `context/instructions/2026-06-27_M7.5a_read-API-hero-read_runs-and-causal-chain_coding-instruction.md`; breakdown `context/planning/2026-06-27_causal-read-API_scope-freeze-and-milestone-breakdown.md` sets M7.5a/b/c). **Next Core slot = M7.5b** (DEFERRED to the v11 hub for clean-context authoring; the read-API breakdown + the M7.5a precedent are the durable sources). **Carried residuals:** OR-GATE-M7.4 (confirm the `ci.yml` Actions run is green on the pushed `4e31aed`) + the D2 canonical INV-id registration (review→ratify; proposal staged). Preflight STALE-benign → reconciled (the v9 note below cited `db8ab5f` / M7.4d-as-next, pre the M7.4d commit).

> **2026-06-27 (v9 hub) currency:** Spine at **core `db8ab5f` (M7.4a/b/c DONE GREEN; OR-M7-WIRING RESOLVED) / docs `75d0345` (Doc 16 + Doc 17 LOCKED, AMD-95, 170/50) / hivemind `a142d9b` +uncommitted beats 20–21**. **M7.4d = NEXT Core slot** (the D2 pure-function-replay CI gate + register D2's canonical INV-id via review→ratify), gated behind preflight PASS + OR-GATE-M7.4 (the `db8ab5f` gate-of-record confirm). The §1 D1–D5 are ruled (D5 converter-DB RATIFIED 2026-06-26; AMD-95 RATIFIED 2026-06-26). **D5 (converter-DB) RATIFIED 2026-06-26** (Session A — adapt-the-data + curated-subset fallback; reject runtime-interop/rebuild; unblocks M9 scoping). The masthead blob below is historical (last-verified `7c73c91`/06-11); the live forward rows + the M7.4 row are current.

> **2026-06-22 (M7.3 closeout) — Cowork PM v5 hub:** **M7.3 (PendingCommandLedger) ✅ DELIVERED GREEN** (`./gradlew check` 149; PM WUCP Phase 2 APPROVE) — the ledger FSM + assembly + 2 test classes; AMD-92 ZERO mints (counts stay 71/41/53), no module-info change; 6 source-drift [REVIEW]s ACCEPT. **LIVE composition-root wiring DEFERRED → M7.4** (a premature live subscriber leaked its SQLite read connection → Windows `@TempDir` teardown failure; reverted, `HomeSynapseCore` unchanged). **RE-SEQUENCED — RULED by Nick ("live spine first"): M7.4 command-pipeline bootstrap + an E2E gate test is the NEXT Core slot, BEFORE the thin causal-read-API** so the read-API + the hero render on a real live pipeline (de-risks the mid-Aug go/no-go early). New Core sequence: **M7.4 (live integration) → thin causal-read-API → AB-4 → M9 → validation.** Core SHA reconciles at Nick's M7.3 commit.

> **2026-06-22 currency note (Cowork PM v5 hub):** Spine at **core `1b0b6c9` (M7.2b GREEN, 149 tasks — the action model is FROZEN) / docs `e47f01e` (Doc 16 LOCKED, AMD-94, 169/49) / hivemind (the v5 first-act hygiene commit)**. The forward rows below are current: **M7.2a-1/-2 ✅ DONE GREEN, M7.2b ✅ DONE GREEN 2026-06-22, M7.3 = NEXT.** **M7.3 (PendingCommandLedger) coding instruction is ISSUE-READY** (`context/instructions/2026-06-22_M7.3_pending-command-ledger_coding-instruction.md`) — authored to ratified **AMD-92 = ZERO new event types** (the ledger events pre-exist; counts STAY 71/41/53; no module-info change), correcting the dispatch-prompt's "adds event types" wording. Corrected Core sequence: **M7.3 → thin causal-read-API → AB-4 → M9 → validation.** Supersedes the v3 note below (core `1541446`) where they differ.

> **2026-06-21 currency note (Cowork PM v4 hub) — backlog rotation:** The Major **M1 / M2 / M3** sections, the **M4 retrospective** debt/process table, the **committed-M3 design-artifacts** index, and the **M3.x WUCP Phase 2 closeout entries** (all DONE + committed; never re-read in detail) were rotated out to `context/planning/archive/phase-3-milestone-backlog-M1-M4-rotated-2026-06-21.md`. The active forward rows are retained below unchanged: the **Pre-M9 queue**, the **pre-M14 dual-coordinator spike**, the **M5 window**, and the **Future-Major-Groups roadmap** (which carries the live **M6.x / M7.x / M8–M15** rows). This is a pure hot-path token rotation (no state change); the v3 currency note below remains the spine-state reference until the v4 hub's preflight/WUCP updates it.

> **2026-06-21 currency note (Cowork PM v3 hub):** Spine at **core `1541446` (M7.2a-1 GREEN, 149 tasks) / docs `e47f01e` (Doc 16 LOCKED, watermark AMD-94, 169/49) / hivemind `23a6cf7`+this batch**; preflight PASS (all 11). **M7.2a-1 = DONE GREEN** (run lifecycle — `RunCausalChain` / `RunContext` field-7 swap / `StandardRunManager` FSM / AMD-92 rows 2/7/8/10/17/18; EventTypes 62→67, roster 32→37, category 44→49). **M7.2a-2 = ISSUED** (`context/instructions/2026-06-21_M7.2a-2_execution-dispatch_coding-instruction.md` — `ActionExecutor` + `CommandDispatchService` + `CommandValidator`(new) + `ConflictDetector` + the real `RunConditionGate`; AMD-92 rows 4/5/6/9 → EventTypes 67→71, roster 37→41, category 49→53; the four 2a-1 carries [QUEUED sequential drain, row-4 CausalContext, dedup-before-gate, real snapshot+tallies] as settled DPs; both module-infos UNCHANGED) — **route to the Coder next.** **Same-beat DONE:** dashboard read-API contract FROZEN v1 (`context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md`); CI confirmed live + adopted as gate-of-record (`context/process/ci-as-gate-of-record.md`). **NEW LANES (prompts authored, launch after Nick's go):** frontend-dev (Web-UI long pole; writes `web-ui/dashboard/`) + Distribution-skeleton (devops; writes a new `distribution/` workspace). **#0 SCHEDULE-CRITICAL:** the device-acquisition + test brief is authored — hardware spend awaits Nick (`context/planning/2026-06-21_device-acquisition-and-test-strategy_brief.md`). Corrected Core sequence stands: **M7.2a-2 → M7.2b (entry-gate cleared) → thin causal-read-API → AB-4 → M9 → validation.** Supersedes the 2026-06-20 note below where they differ.
> **2026-06-20 currency note (Cowork PM hub):** Spine at **core `9e73d28` (AB-1+AB-2 GREEN) / docs `f6c0c64` / hivemind `413930e`** (reconcile to the dispatch baseline exactly; preflight STALE-currency→reconciled in this Lock batch). **DOC 16 (Superior Automation Layer) LOCKED 2026-06-20** — INV-SA-01..04 registered (`Architecture_Invariants_v1.md` §49; **169/49**; a new design-doc Lock, watermark stays **AMD-94**; Q1=mint-4 / Q2=INV-SA-category, Nick ruled) → **the M7.2b entry-gate is CLEARED** (M7.2b was Lock-gated on Doc 16 + D2; **D2/REC-162 still decides AT M7.2b** — keep-anti-retry default). **AB-1+AB-2 = DONE GREEN (`9e73d28`)**, WUCP Phase 2 APPROVE; the **WS Phase-3 runtime + auth WU** is on the backlog (api.ws was Phase-2 scaffold — no live WS surface today). **Next Coder slot = M7.2a (Q3 ruled)** — run lifecycle / `RunManager` FSM / AMD-91 `RunContext`→`RunCausalChain` swap / `ActionExecutor` + `CommandDispatchService` / the AMD-92 run-lifecycle+dispatch event slice (rows 2/4–10/17/18) / run-trace; Doc-16-independent, builds against injected deps (wiring rode AB-3); instruction authoring is the hub's next product. **AB-4 = authored concurrently, slotted pre-M9** (cipher activation via the 6-arg HSC ctor + F1 `v1` tag / F3 / F13 + backup-key seam; un-gated by AMD-94; runway since nothing writes a sensitive scope until M9). Supersedes the 2026-06-18 note below where they differ.
> **2026-06-18 currency note (Cowork PM, the converge session):** Spine at core `03f16dc` (substantive `1eddd9a`) / docs `f2e064d` / hivemind `b7a893e`; preflight reconciled **STALE→PASS**. **M7.1 = ISSUED, AWAITING BUILD** (no automation code landed yet — substantive HEAD is still M6.3). **Direction changed (Nick-delegated):** the next phase is **NOT** "run M7.1/M7.2" — it is **app-bootstrap (the runnability unlock) + superior-automation Phase-1 design + the agent pass**, with M7.1 a ride-along. Drivers: (1) the **energy/erasure interview gate is RETIRED** (2026-06-15 M5-D desk research) → the **app-bootstrap lane is un-parked**; (2) the **core-review (2026-06-15)** found the core sound-but-**not-runnable (`main()` stub)** with two HIGH at app-bootstrap (**C1** unauth HTTP / INV-SE-02; **C2** decrypt-aborts-replay) — now Open Risks OR-APP-AUTH / OR-RF-DECRYPT; full backlog C1–C15 in the CONVERGE synthesis. **New milestone rows to add when scoped:** *app-bootstrap* (carries C1+C2+C9) and a *superior-automation design doc* (Doc 16 candidate — federation/B3, honest-hybrid, expressiveness-without-DSL; Nick's Phase-1 scope) **precede M7.2/M7.3**. See `weeks/2026-W26_jun22-jun28.md`. Supersedes the 2026-06-14 note below where they differ.
> **2026-06-14 currency note (Cowork PM, Track-4):** M6 is **4-of-4 COMPLETE — M6.3 COMMITTED `1eddd9a`** (at-rest payload encryption `[identity, presence_personal]`; `./gradlew check` GREEN 147; working tree clean). **M7 entry-gate row 4 CLOSED** (`config-superiority.md` APPROVED 2026-06-14, veto delegated by Nick) → **M7.1 (trigger/condition path) is NEXT and CLEARED TO ISSUE** this session; interview-independent (row 3 gates only the parked app-bootstrap lane + the M6.3 restore-half R-α; the M6.3-vs-M7 ordering call is resolved by M6.3 landing first). AMD-88..93 binding (watermark 93, invariants 163/47). Supersedes the 2026-06-13 note below where they differ.
> **2026-06-13 currency note (Cowork PM):** M6 is **3-of-4 COMPLETE** (M6.1/M6.2/M6.4 GREEN; **M6.3** triple-gated — energy/erasure interviews + OR-M6-NONCE). The **M7 entry-gate** is open: **AMD-88..93 RATIFIED 2026-06-12** (watermark **93**, invariants 163/47), so the M7.x contract shapes are binding; M7.1 awaits entry-gate rows 3 (interviews) + 4 (M5-C approve). The dated session-note prose in the masthead reflects its 2026-06-11 baseline and is superseded by this note where they differ.

**Current phase:** P3 Implementation (test-first).
**Phase started:** 2026-03-27 (first test commits) / 2026-04-10 (first production implementation via M2.1).
**Frozen Phase 2 record:** `phase-2-block-backlog.md`.

Phase 3 uses **Milestone** vocabulary (M{major}.{minor}) instead of Phase 2's Block letter vocabulary. A milestone is a single work unit — a coherent compile-and-commit unit with test coverage — and is governed by the Work Unit Completion Protocol (WUCP) in `context/protocols/work-unit-completion-protocol.md`.

**Status key:** DONE | NEXT | PLANNED | FUTURE

---
