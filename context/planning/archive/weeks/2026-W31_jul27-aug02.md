<!--
file: context/planning/weeks/2026-W31_jul27-aug02.md
purpose: W31 weekly plan — PRE-MINTED 2026-07-24 by the v37 hub at beat 1 (the Check-2 boundary reconciliation executed early; W30 remains operative through 2026-07-26), from the gate-window execution plan of record (pm-handoff v36 beat 5) on the v37 beat-1 verified state (core `4bc1258` · docs `a53f474` · hivemind `173aff0`+beat-1 · bench `9f2b5ad` · skills `dccc85f`; watermark AMD-99, 175/53 at the register regeneration line; fleet 6/6 @ 25065, ch20/0x774c; the gate 21 MUST / 8 SHOULD, J1 FROZEN).
status: PRE-MINTED (becomes the current-week plan at the 2026-07-27 boundary). The authoritative live state is `context/status/PROJECT_SNAPSHOT.md` + `context/planning/phase-3-milestone-backlog.md` — this plan supersedes-where-differs by currency notes, per the W30 precedent.
-->

# Week of July 27 – August 2, 2026 (2026-W31)

**Week theme: the write surface reaches silicon and the evidence machine goes automatic.** W30 landed the keystone (CMD-API `5b4797e` ALL-CI-GREEN → the dependabot ride `4bc1258`) and intook the ruled fusion program (FQ-1..FQ-6; the phase plan of record). The deploy + Rosonway evenings may land late-W30 or open this week — W31 is the conversion week: THE DEPLOY on the brief (+H4/A4 riders) if not already done → Rosonway/I3b if not already done → SKIP-VIS + FE-VERDICT-2 authored at desk → **B2 green on the DEPLOYED build** (C2/C3/H2 producers RUNNING; the constants re-mint carries the `command-api` flip + the `usb-power` flip) → B3 nightly (the C1-N accumulation engine) → the M14 WUs open behind them (rider 3). Counsel wait-states tick: **TSDR #2 = 07-31** (the TOMORO status instrument of record) · Pelton results ~Aug-5 → the results consult (approved agenda: owner-at-filing/LLC · foreign direct-national via associates + per-country ballpark · public-use for parked assets · Asimtote companion timing) → **G-2 = exactly TWO acts (R-1 + the filing go)** → BRAND-G2-EXEC. Fusion: discovery sourcing may run (zero gate cost); provenance + phase1-design lanes stay POST-GATE. The gate calendar is unmoved: **~Aug-10 THE DRY-RUN (all 29 rows vs artifacts) → Aug-12–13 I2 [M] + close-out + v38 banked → Aug-14 EOD FREEZE → Aug-16 THE READ.**

## Entry gates

1. **The deploy evening executed on the brief** (`context/handoff/2026-07-24_cmd-api-deploy-evening_operator-brief.md`) — Block-2 boot glance + boot-health `[PASS]` on `4bc1258` + the H4/A4 rider pastes. If it slid past W30, it is THIS week's first physical act — B2 lands only against the deployed build.
2. **The Rosonway/I3b evening executed** (never the deploy evening; package: `context/handoff/2026-07-22_rosonway-topology-move_I3b_operator-package.md`) — Block-3 `[PASS]` + Block-4 `[PASS]` → I3b [S] closes; the `usb-power` constants flip QUEUES for B2's re-mint.

## Lanes (leverage-ordered)

1. **Core (serialized): SKIP-VIS** — CORE-P1 (`StandardExplanationService.java:617-:656` — carry the raw outcome · superseded never selects FAILED · honest-unconfirmed derives UNCONFIRMED) + CORE-P2 (`:288-:309` — the completedVerdict silent-skip honesty) fold into ONE instruction under the ruled v1.1.2 four-constraint law (additive-only · wire-casing at the endpoint · emitter-leads · version discipline). Authors at desk anytime (line numbers re-enumerated at source at authoring); grounding-first; host-CC lane; two-layer audit.
2. **Bench: B2** (the §51 suite port — flips the SKIPPED stubs on the DEPLOYED build; the C2/C3 suite legs + the S31 confirm legs + the AUTO usb scenario; `capabilities.command-api.available` → true at the constants re-mint [+ `usb-power` if Rosonway landed]; **OBS-CONFIRM + C4's latency arm + RUNNER-VERSION-BANNER rule AT authoring**; every runs-surface assert prices terminal-materialization; fixture-paired asserts standing) → **B3 nightly** (BENCH-AUTOSTART rules there; every night after compounds the verdict corpus).
3. **Frontend (parallel Cowork lane): FE-VERDICT-2** — consumes the ruled v1.1.2 shape + the brand lane's WCAG mode-paired token values + the §3a ALL-USERS/CVD mandate; G2 [S] re-statuses at authoring; the dependabot dashboard-deps watch rides it.
4. **The M14 WUs (~4–6, integration-shelly)** — open behind CMD-API ✓ + B2 per rider 3; ident gate `S4PL-00116US` at unbox (capture-verbatim gen/model/app/fw_id); the 2× Shelly Plug US arriving = the DUT-of-record + ONE UNASSIGNED spare (role ruled at M14 authoring); the Gen4 plugs stay stimulus-only forever. **M14 yields to gate producers on any contention — always.**
5. **Programs/desk (gaps):** corpus entries (joins night + 04P + the Rosonway pastes when in — the ratchet rule) · E3 [S] public statement (writable) · the doc-drift ×3 docs-repo commit · Matter Doc-19 draft rides its program hub · fusion discovery sourcing (Nick + the friend, at leisure; mark-free, R-1 law).
6. **Nick's own lane:** TSDR #2 **07-31** · Pelton results ~Aug-5 → the results consult → G-2 → BRAND-G2-EXEC · tamorro.com registrar glance · tamoro.dev $12 call · **H3 [M] = the named Aug 8–9 slot** (clean-image fresh install).

## Trigger watch

**The Aug-10 dry-run is the week's far edge:** by EOD Sun Aug-2 the MUST ledger should read — C2/C3 (B2 suite legs) RUNNING or DONE · H2's AUTO suite riding B2 · H3 slotted Aug 8–9 · I2 queued for close-out · every other MUST already ✓. Any producer NOT running by the boundary escalates to Nick with the named slide cost (the gate never slides; scope does).

## Standing guardrails

Single-spine-writer · write-isolated lanes · CI on the pushed commit = the gate of record · two-layer audits on everything including own subagents · **EXPLICIT-PATHS COMMITS ABSOLUTE** · no attribution trailers (env-model §9) · fixture-paired asserts · **J1 FROZEN** (21 M / 8 S; no criterion moves without Nick's re-ruling recorded in pm-handoff) · gate sovereignty over the fusion program (through Aug-16 it touches no core/bench/FE lane) · R-1 HELD until G-2; token-parameterized everywhere · the north star: never-false-CONFIRMED / never-false-ALIVE.

## Risks

- **Two operator evenings (deploy · Rosonway) share calendar with real life** — they are strictly single-variable and MUST NOT share an evening; if only one lands by mid-week, the deploy outranks (B2 + three MUST producers stack behind it; Rosonway closes only I3b [S]).
- **B2 is the week's concentration risk** — it carries the C2/C3/H2 producer starts + two constants flips + three rule-at-authoring items (OBS-CONFIRM · C4's arm · RUNNER-VERSION-BANNER); author it against the deployed build's live tokens, audit it at the full bar, never compress it.
- **Counsel timing is external** — Pelton ~Aug-5 is an estimate; G-2 slides with it, but the gate does NOT (the gate reads code/bench evidence only; the name program is parallel by design).
