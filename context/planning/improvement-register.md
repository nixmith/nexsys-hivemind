<!--
file: context/planning/improvement-register.md
purpose: THE IMPROVEMENT REGISTER — one row per improvement candidate the hub or a lane observes in the source while doing something else: what was seen (source-cited), the measurement that would justify acting, the cost, the trigger, the status. Minted v73 beat 3 (2026-09-13) on Nick's standing directive (constantly review the source, even months-old code; iterate; plan the improvement even when not acting now). The hub adds rows at every intake audit's findings section and at WUCP Phase 2; a row RETIRES when its WU lands (the sha named) or when a measurement shows it is not worth doing (the measurement named). Never a backlog of wishes: every row names the instrument that would decide it. Read by grep when authoring the next WU on a surface (`grep -n '<module or class>' this file`).
audience: the hub (authoring; WUCP Phase 2) · the Coder and frontend lanes (the rows they will meet) · Nick (the shape of what is owed to the code)
state-type: register (rows appended; rows retired in place with the sha)
status: LIVE — minted v73 beat 3 (2026-09-13); rows IR-1..IR-27 (IR-22/IR-23 RETIRED at core `be4788a`, v75 b8 — `Run check` green at the instrument, the upload step red on the storage quota; IR-26 the unexecuted `InitializationWrite` and IR-27 the artifact-upload policy added v75 b7; IR-24 widened; IR-24 the metering-plug classification by clusters and IR-25 the divisor-scaled reporting thresholds added v75 b6 from DEVICE-SET; IR-1/2/3 RETIRED at core `e56f555` and IR-12/13/14 RETIRED at core `3d40b5f`, v75 b4; IR-21 the runtime log-level knob and IR-22 the for_duration defect candidate added v75 beat 2; IR-1 MEASURED v75 b2; IR-2/IR-3 chartered in 114c and IR-12/13/14 in FE-114 at v75 b2; IR-20 added v75 beat 1 — the interviewed cluster list and a per-frame count are unobservable on a shipped artifact; IR-1's instrument authored v75 beat 1; IR-15..IR-19 added v74 beat 7 from the ambition map's intake, Mon 2026-09-14 ~11:5x CT; IR-15 is a DEFECT and a fence on adoption; IR-8 RETIRED at core `6bd8508`; IR-9 RETIRED at bench `f3631cb`)
-->

# Improvement register

| Id | Surface (cited at the sha) | Seen | The measurement that decides it | Cost | Trigger / status |
|---|---|---|---|---|---|
| IR-1 | `core/automation` `StandardExplanationService.locateTriggered` (post-114b: the `scanType` visitor with the early stop; at `a458a64` `:991`) | The scan starts at position 0 and stops at the first match in ascending order, so explaining the NEWEST run walks nearly the whole retained `automation_triggered` index — `SCAN_BATCH` (500) envelopes per page, `O(retained)` per request | Seed a realistic retained index (the bench, or `InMemoryEventStore` with 20k–50k triggered markers) and read the page count with the 114b `CountingEventStore` instrument (T3) for the newest run; file the count and the wall time | The by-id read (114c's K2: `EventStore` by run id or a run-id → position index) | 114c; MEASURE FIRST (the unmeasured-hop law); status MEASURED (v75 b2 — Nick's host, 2026-09-14T19:07Z, `_scratch/v75/2026-09-14_IR-1_measurement.txt`: pages=2/40/100 · envelopes=N · firstRunPages=1 at N=1k/20k/50k; the wall time is the in-memory fixture's, not the store's); 114c CHARTERED v75 b2 — K2 = the RunId-time hint read with the counted fallback, the IR-1 test carried as the guard (≤2 pages per N); RETIRED at core `e56f555` (EXPLAIN-114c; CI run #249 green; v75 b4) |
| IR-2 | `StandardExplanationService.listRuns` visitor (post-114b) | Positions ascend, so once `e.globalPosition() >= before` no later envelope is below the cursor; the visitor returns `true` today (114b preserved behaviour as ordered) where `false` would end the walk at the cursor | A T1-style test: a cursor inside page 1 ⇒ `reads(AUTOMATION_COMPLETED) == 1` (today 2 on a 501-marker index) | One line + one test | 114c row — CHARTERED v75 b2 (T3 / SD-5); RETIRED at core `e56f555` (EXPLAIN-114c; CI run #249 green; v75 b4) |
| IR-3 | `StandardExplanationService.scanType` / `lastMatching` (post-114b) | One `Predicate<EventEnvelope>` type with two meanings in adjacent private methods — continue/stop vs matches/does-not; a wrong pass ends a walk at the first non-match | Readability only; no runtime measurement — the test is a reader's: a named functional interface (`Visitor.visit` → `CONTINUE`/`STOP`) or an enum return | Small; touches the five visitors | 114c row — CHARTERED v75 b2 (T3 / SD-5); RETIRED at core `e56f555` (EXPLAIN-114c; CI run #249 green; v75 b4) |
| IR-4 | `core/event-model` `EventStore.readByType` (forward-only; `:113`) | Every "latest" read and every runs-list page is a full forward walk of a type index; a descending type read (`readByTypeDescending`) or a per-automation terminal index would make `latestTerminalRun`, `latestDisabled` and `listRuns` `O(page)` | IR-1's measurement generalised: the page counts per read on a realistic index; then the store's own cost of a reverse index (persistence, Doc 04) | A store primitive: event-model + persistence + a design sentence (Doc 04 / Doc 02) — an amendment-class change | After IR-1's numbers; status OPEN (v73 b3) |
| IR-5 | `api/rest-api` `Get*Endpoint` / `ListAutomationsEndpoint` — 27 hand-sized `LinkedHashMap<>(n)` (114a audit growth point 4) | Sizes drift from the key counts silently; the shape tests pin order, not sizes | None needed (cosmetic) | Trivial | The next rest-api WU that touches those files; status OPEN (v72 b1 via 114a audit) |
| IR-6 | `api/rest-api` test fixture `NonFiringExplanations` (114b) | Each wither repeats all 13 arguments; a 14th component (114c) grows every wither | None (test code) | A mutable builder halves the file | 114c, when the first v1.1.5 component lands; status OPEN (v73 b3) |
| IR-7 | `core/automation` `RunExplanation` / `AutomationSummary` convenience constructors (the 114b lane's observation) | The same pattern 114b collapsed for `NonFiringExplanation`: conveniences kept for test sites | `git grep -n 'new RunExplanation(\|new AutomationSummary(' -- '*.java'` per module: how many sites, which arities | The 114b pattern (a fixture, one constructor) | The next WU that touches either record; status OPEN (v73 b3) |
| IR-8 | `web-ui/dashboard` — the hero surfaces (`CausalChain.tsx`, `WhyNotView.tsx`, `ExplainHubView.tsx`, `Resource.tsx`) | HERO-1c D5: sentences still live in JSX text nodes and template literals (`terminalLine`, the condition line, the cascade link, page titles/ledes, "Signing in…"); the §6.3 grep instrument cannot see them | A lint — an ESLint rule or a script over the hero files flagging JSX text nodes / template literals that contain a sentence (a capital, ≥3 words, a terminal mark); its count at HEAD is the baseline; 0 is the target | Small (a rule + a CI line) | HERO-1d's instrument — LANDED as the lint in `eslint.config.js` (11 hits at HEAD → 0; inside `npm run verify`); status RETIRED at core `6bd8508` (HERO-1d; CI all checks green, 2026-09-14) |
| IR-9 | `nexsys-bench` `suite --list` | R-5A: `--list` lints without resolving `${{C.*}}`; a duplicate top-level key in `constants.yaml` is kept-last by PyYAML and surfaced only at run time (`boot-health` REFUSED) | The lint refuses a duplicated key at `--list` (a red-first test) | Small | RETIRED — landed bench `f3631cb` (R-5A-ii row 4: `--list` reads through `yaml.compose()`; a red-first fixture), v73 b7 |
| IR-10 | `nexsys-bench` `tools/nightly.sh` (R-5A-ii row 1) | `/api/v1/entities` is spelled twice in the nightly (the runner's read and the fleet read); a drift lands on `fleet: unread`, never a wrong number — bounded but two spellings for one route | One constants key (`api.entities-route`) read by both; a selftest that the two reads share it | Trivial | The next bench WU or R-5 Part B's prep; status OPEN (v73 b6) |
| IR-11 | `nexsys-hivemind` — lane charters (W-SKILLS-9's `§0`/`§2`: the census typed 9 hivemind + 3 skills against a fold table of 10 + 4) | A charter's census is a claim about its own table; the lane refuted P1 on the arithmetic and was right — the count was typed, not counted | A charter self-check: `grep -c` of distinct target paths in the fold table before the census line is written (the hub's authoring script asserts equality) | Trivial | The next charter the hub authors; status OPEN (v74 b2) |
| IR-12 | `web-ui/dashboard/eslint.config.js` — the HERO-1d literal lint (HERO-1d return §3 (a)) | The charter's pattern `/[A-Z][a-z]+ [a-z]+ [a-z]+/` needs three bare words after a capital: it misses apostrophes ("didn’t"), two-word runs ("Your automations", "← All runs", "Signing in…"), a capital before an interpolation, and object-property literals (the pills) — nine of the hub's own sites; the rows keyed them, the guard cannot see them | The lane's widened pattern (≥2 words, apostrophes, a capital before `${{`; 19 at HEAD) + a `Property > Literal` selector; run at HEAD, count the residuals, key them | Small | FE-114's charter (a row); status CHARTERED FE-114 (v75 b2); RETIRED at core `3d40b5f` (FE-114; CI run #250 green; v75 b4) |
| IR-13 | `web-ui/dashboard` copy — the dialect of record (HERO-1d return §3 (b)) | US English is the dialect of record (the catalog's keys say `color`; ruled v74 b5, REVERT-able). Residual UK spellings: `recognise` ×2 (`i18n.ts` `explain.mode.unknownOutcome.help`; SPEC :207), `behaviour` ×2 (comments), `neighbours` ×2 (SPEC :96; `states.html`), `colour` ×11 in SPEC prose and `states.html`; `Cancelled` stays (the enum's mirror; accepted US usage) | A §7 text-change row per rendered string + a docs sweep; the same grep at 0 | Trivial | FE-114's charter (text changes go through §7); status CHARTERED FE-114 (v75 b2); RETIRED at core `3d40b5f` (FE-114; CI run #250 green; v75 b4) |
| IR-14 | `web-ui/dashboard/src/lib/format.ts` `pendingHint` :217 (HERO-1d return §3 (c)) | The literal equals the catalog's `explain.action.pending.color` byte for byte after D6; two homes for one sentence | `pendingHint` reads the catalog; the modes test pins it | Trivial | FE-114 (a zero-visible swap); status CHARTERED FE-114 (v75 b2); RETIRED at core `3d40b5f` (FE-114; CI run #250 green; v75 b4) |
| IR-15 | `integration/integration-zigbee` `EndpointClassifier.java:175` `iasCapability()` vs `IasZoneHandler.java:77` (the ambition map AM-7; re-run v74 b7) | The classifier selects `motion` for every non-CONTACT zone type while the handler emits `active` for WATER_LEAK/SMOKE/VIBRATION — schema and report disagree; adoption is a one-way door, so a device of those types adopted today carries the wrong capability for life | A fixture-replay test: a synthetic 0x002A/0x0028/0x002D enrol ⇒ a `BinaryState`-class capability whose schema admits `active`; RED at HEAD | ½ lane-day | A DEFECT, not an improvement: the micro-WU on the Java slot at the first opening after 114c; until it lands no water/smoke/vibration device joins any card (the rig packets carry the line); status OPEN (v74 b7); CHARTERED — ENERGY-READ R1 (v76 b4); RETURNED and ACCEPT v76 b5 (the b5 audit) — LANDED core `d1c2cbc`, CI #253 GREEN (v76 b6) — RETIRED as a defect; the fence on IAS water/smoke/vibration adoption lifts with it |
| IR-16 | `integration/integration-zigbee` `EzspIncomingMessage` (`lastHopLqi`/`lastHopRssi` parsed, dropped) · `EzspCoordinatorProtocol.java:1408` `topologyScan()` (no consumer) · `core/persistence` `TelemetryWriter` (no producer) — Seam B (the map AM-6) | Every silence in a 72-hour run could carry its last link reading; today the reading is parsed and thrown away, and the telemetry path has a writer, a query service and no producer | LINK-READ: first a counter of incoming frames per device per hour printed once (the sampling rule follows the number — arc 1); then samples keyed by IEEE via `TelemetryWriter`; a packet block reads `TelemetryQueryService`; the scan's neighbours = the routers on air | 1–2 lane-days | After R-5B lands and rehearsal 1 is on the calendar (Nick's fence 3); status OPEN (v74 b7) |
| IR-17 | `integration/integration-zigbee` `ClusterHandlers.java:36–54` (eight handlers; none for 0x0B04/0x0702 — `ReportingConfigurator.java:71–72` knows them) · `EndpointClassifier.java:99–101` (0x0051 → `SWITCH` + `on_off` only; `PowerMeter`/`EnergyMeter` exist unattached) — Seam A (the map AM-1) | The metering read side is designed (Doc 08 §3.5) and unrealized; the bench plug (S31 Lite ZB) has no metering cluster (zigbee2mqtt S31ZB: `switch (state)` only) — the class needs a metering plug AND one Core WU | ENERGY-READ: the unit rows first (`QuantityValue`), the two handlers, the 0x0051 arm, a dossier-fixture replay; the bench `metering-known-load.yaml` — a REFERENCE plug-in meter as ground truth, the DUT's `power_w` within ±10 % ⇒ `within`, else `outside` (`confirmed` stays the action leg's word); `energy_wh` monotone | 2 lane-days + the purchase | `PLUG:` (D-v74-4) → ENERGY-READ after R-5B, verified at rehearsal 2; status OPEN (v74 b7) |
| IR-18 | `integration/integration-zigbee` — no handler for Illuminance 0x0400 (Doc 08 §3.5's map realized for 8 of 11 clusters; the map AM-3) | An mmWave/occupancy unit that carries 0x0400 reports a value nothing ingests | Rides the first Seam-A family (one handler + a fixture) | Trivial | With ENERGY-READ; status OPEN (v74 b7) |
| IR-19 | `context/strategy/*.docx` ×3 (`From_Platform_to_Institution…`, `HomeSynapse_MVP_Data_Readiness_Specification`, `NexSys_Data_Value_Engine_Strategy`) | A read-only lane cannot open them; the ambition map's read-set listed only `.md` and the lane did not know they existed | Convert each to `.md` beside the original (a hygiene beat; pandoc or a docx skill); the Tier-2 charter's read-set names them | Trivial | The next hygiene beat; status OPEN (v74 b7) |
| IR-20 | `integration-zigbee` — the interview's endpoint signature and the per-frame path (`ZigbeeAdoptionSlice.java:374–386` consumes `endpoint.inputClusters()` at classification and logs nothing; `ZclIngestionUnit.route` `:436–470` hands an accepted frame to `listener.onFrame` with no log; `logback.xml` root INFO, no runtime knob; `zigbee-profiles.json`'s S31 entry withholds its fingerprint) | Neither the interviewed cluster LIST nor a per-device frame count is observable on a shipped artifact: the S31's interview reads only as `reporting_configured … clusters=<n>` (F-R4b-C, `clusters=1`); R-5B's frame block counts the store's rows by subject as a LOWER BOUND (the v75 b1 audit §5) | (a) one INFO line at interview completion with the endpoint's in/out cluster ids; (b) a per-device `LongAdder` in `route()` after resolution and one INFO line per device at `permit_join_closed` and every 10 min — then a packet reads both | ≤40 lines + a test each | LINK-READ's first row (`FRAME: a`, D-v75-1 RULED v75 b2): the counter's home is `onFrame` — a per-IEEE counter, one periodic INFO summary line, never per-frame logging (Nick's sharpening); the bound R-5B files is per frame class with the SNZB 2× multiplier (M9.3's twins); status OPEN (v75 b2) |
| IR-21 | `app/homesynapse-app/src/main/resources/logback.xml` — root INFO, no runtime knob ("raise per-logger at runtime via the future LogLevelController"); the unit exposes no `-Dlogback.configurationFile` | Every rig packet wishes for a DEBUG window on one logger (the ingestion tokens, the interview) without a restart or a rebuild; today the only levels are the jar's (Nick's word, v75 b1: an operator instrument) | A `LogLevelController` on the internal API (`PUT /internal/log-level {logger, level, ttlSeconds}`) with a bounded TTL and a journal line at set and at expiry; the root stays INFO (Jetty DEBUG echoes bearer credentials — the logback.xml comment); only `com.homesynapse.integration.*` settable; a packet block flips it and reads it back | Small: one endpoint + logback's `LoggerContext` + a test | After Saturday; a Core WU after DUR-1 / ENERGY-READ; status OPEN (v75 b2) |
| IR-22 | `core/automation` — `StandardTriggerEvaluator.expire` `:415–:455` publishes `trigger_duration_expired` and validates; `evaluateMatches` `:184–:190` starts the timer and never matches; `triggerMatchesEvent` `:482–:500` has no expiry arm; `StandardAutomationRegistry.consumedEventType` `:174–:188` maps no trigger to the expired type; `AutomationEngineSubscriber.java:103` is REPLAY bookkeeping | A `for_duration` trigger's expiry initiates NO run at `6bd8508` (the javadoc "the trigger still fires" `:441–:445` is unimplemented; no test asserts a run after expiry; `core/automation/MODULE_CONTEXT.md:29` describes a run the engine never starts) — a DEFECT candidate on a core trigger class (the v75 b2 audit §2–§3) | DUR-0: one RED test in `DurationTimerTest` (a for_duration automation; the injected clock past expiry; `initiateRun` / `automation_triggered` asserted); if RED, DUR-1 initiates the run on expiry with the expired envelope as the triggering event (the firing value via `startingEventId`, K4 of 114c's docket) | DUR-0 ≈1 h; DUR-1 ≈½ lane-day + tests | `DUR: measure` RULED (Nick, v75 b4 — the instrument named in the charter's §0 first: `DurationTimerTest` + `RecordingRunManager` + `MutableClock` exist at `3d40b5f`); DUR-0 DISPATCH-READY in MEASURE-1 (`context/instructions/2026-09-15_coder-lane_MEASURE-1_DUR-0_and_HASH-0_two-red-probes_coding-instruction.md`); the fix DUR-1 rides the same coder session as HASH-1; **MEASURED RED at MEASURE-1 (2026-09-15: `Expected size: 1 but was: 0` — zero `initiateRun` calls after started 1 · expired 1 · the expired envelope redelivered in LIVE; `context/audits/2026-09-15_MEASURE-1_return.md`) — a DEFECT;** DUR-1 CHARTERED (`context/instructions/2026-09-15_coder-lane_DUR-1_and_HASH-1_the-run-on-expiry_and-the-ordered-views_coding-instruction.md`: the registry's expired bucket + the evaluator's expired-payload IMMEDIATE match; initiation LIVE-only); DUR-1 RETURNED and ACCEPT (v75 b6: T1/T1b/T1c green — the redelivered expiry is the immediate match; initiation LIVE-only); LANDED core `be4788a` (v75 b7); run #251 RED at the UPLOAD steps only (the artifact quota — F-b7-1), `Run check` unread; **RETIRED at core `be4788a`** (v75 b8: `Run check` GREEN at the instrument — Nick's screenshot of run #251's step list, BUILD SUCCESSFUL 3m 14s / 156 tasks; the desk's `check --continue` green ×2; only the upload step red on the storage quota) |
| IR-23 | `core/automation/DefinitionHashes.java:29` `forDefinition` hashes `AutomationDefinition.toString()`; a group selector's `Set.copyOf(includedRoles)` iterates in a per-JVM-launch order (`ImmutableCollections.SALT32L`) | A definition whose selector names ≥ 2 roles hashes DIFFERENTLY after a restart: its pre-restart runs read `conditions[].definition: null` (114c's SD-3 guard is honest; the hash is not) and v1.1.4's `definitionKey` is not stable across reloads for that class — the "stable definition key" the hero waits on (EXPLAIN-5) fails exactly there (114c's adversarial review, O4; the v75 b3 intake §3) | A canonical rendering inside `DefinitionHashes` (roles sorted; every unordered collection rendered in a fixed order); a golden-hash test for a two-role definition (stable only after the fix — the pre-fix flake across launches IS the evidence); the note that existing multi-role hashes change once at the landing | Small; one file + one test; a MODULE_CONTEXT line | **a DEFECT on a frozen key (Nick, v75 b4; D-v75-5)** — the surface is EIGHT records, not four: the four selectors + `CommandAction:51` / `EmitEventAction:37` / `EventTrigger:43` (`Map.copyOf`) + `WebhookTrigger:55` (`Set.copyOf`), so a two-parameter command action is predicted per-JVM TODAY (live, not latent; F-b4-4); HASH-0 (MEASURE-1, two definitions, eight launches) measures first; HASH-1 rides the DUR-1 coder session BEFORE ENERGY-READ, shape (a) ordered immutable views (every single-element hash unchanged); FE-115 renders `definition` only after HASH-1; the freeze doc's [v1.1.5] HASH NOTE carries the prediction; **MEASURED at MEASURE-1 (2026-09-15): two hashes per source across eight launches — A `[DIAGNOSTIC, PRIMARY]` ×5 / `[PRIMARY, DIAGNOSTIC]` ×3, B `{brightness, transition}` ×5 / `{transition, brightness}` ×3, the two salts flipping independently — a DEFECT on both sources;** HASH-1 CHARTERED with DUR-1 (shape (a): eight ordered views, `DefinitionHashes` untouched, T4 pins single-element hashes at the baseline); HASH-1 RETURNED and ACCEPT (v75 b6: one hash per definition across eight launches; T4 every single-element hash byte-identical to the baseline; `DefinitionHashes` untouched); LANDED core `be4788a` (v75 b7); the same run #251; **RETIRED at core `be4788a`** (v75 b8: the same `Run check` GREEN; the freeze doc's [v1.1.5] HASH NOTE re-cut to a measured-and-fixed fact) |
| IR-24 | `integration/integration-zigbee` `EndpointClassifier.java:41` `DEVICE_TYPE_SMART_PLUG = 0x0051` — the only path to the plug branch (SWITCH + `on_off`); the Shelly Plug US Gen4 identifies as On/Off Plug-in Unit **0x010A** (Shelly's own matrix; DEVICE-SET §1) | A metering plug whose device type is 0x010A classifies as a plain switch today; its 0x0B04/0x0702 clusters are never consulted for the class — the b4 dossier quoted the 0x010A row and the hub missed it (the b6 audit F-b6-2) | ENERGY-READ attaches metering by the CLUSTERS present on the endpoint (0x0B04 and/or 0x0702), the device type as a hint only; a unit test over the Gen4's recorded endpoint signature | Small; the classifier + one test | ENERGY-READ (its first row after IR-15); WIDENED by ZIGBEE-GAPS row 7 (v75 b7): classify by CLUSTERS first, device type as a hint — `EndpointClassifier.java:91–104`, `:150–165` (0x0107 drops 0x0402/0x0400), `:180–203` (no temperature arm; battery dropped on switch arms); it also removes the coverage band's device-type uncertainty; status OPEN — CHARTER ROW (v75 b7); CHARTERED — ENERGY-READ R2 (v76 b4); RETURNED and ACCEPT v76 b5 — LANDED core `d1c2cbc`, CI #253 GREEN (v76 b6); ENERGY-READ-b (rides LINK-READ): the metering-only endpoint → `ENERGY_METER` (R-5), the call-site log with `device=` (R-1) |
| IR-25 | `integration/integration-zigbee` `ReportingConfigurator.java:71–:72` — `DefaultRow(0x050B, 0x29, 5, 3600, 10)` for 0x0B04 `activePower` and `(0x0000, 0x25, 5, 3600, 5)` for 0x0702: the reportable change is a RAW attribute value, the divisor/multiplier unapplied (DEVICE-SET §1) | On a divisor-1 plug a change under 10 W is not reported until the 3600-s maximum; on a divisor-10 plug the same row means 1 W — the threshold's meaning depends on a value read at interview and ignored here (the b6 audit F-b6-3) | Read `acPowerDivisor`/`acPowerMultiplier` (and the 0x0702 pair) at interview and scale the reportable change to a WATT threshold; a bench scenario that steps the load by 5 W and counts reports | Small; the configurator + the interview read + one scenario | ENERGY-READ (with IR-24); status OPEN — CHARTER ROW (v75 b6); RE-CUT v76 b2 as a SIZED decision: the charter states the predicted frames/h/device at each candidate divisor before and after the scaling and chooses the engineering-unit thresholds (and the max interval) deliberately — Nick's v76 b2 brief §4 fold 1; the b2 audit §4; CHARTERED — ENERGY-READ R5 (v76 b4): 1 W / 5–600 s on ActivePower, 5 Wh / 5–3600 s on CurrentSummationDelivered, scaled by the READ formatting; RETURNED and ACCEPT v76 b5 — LANDED core `d1c2cbc`, CI #253 GREEN (v76 b6); the lane found and fixed a HEAD wire defect on the way (the uint48 change field's `int` shift mod 32 — HEAD sent 2.1 × 10¹⁰ raw for 5; the read-back folded; both widened to `long`; silicon at the first adoption, record row G4-4) |
| IR-26 | `integration/integration-zigbee` `ZigbeeProfileLoader.java:237` parses a profile's `initializationWrites` into `DeviceProfile.initializationWrites` (`DeviceProfile.java:68`; `InitializationWrite.java:49`) — and NOTHING in main consumes `initializationWrites()` (grepped at `be4788a`, the ZIGBEE-GAPS return §6; the b7 audit F-b7-4) | Doc 08 §3.6's Aqara example (a manufacturer-code write at interview) cannot run today; 152 corpus definitions pass a manufacturer code in `configure`; the lumi interview-tolerance row (ZIGBEE-GAPS #11) depends on an executor | An executor in the interview state machine that performs the profile's writes after the Basic read, with a recorded-frame test per write; a profile with one write on the rig | Small–medium; the interview state machine + one test | ZIGBEE-GAPS rows 4/11 (AM-10) — October's re-cut; status OPEN (v75 b7) |
| IR-27 | `.github/workflows/install-smoke.yml:152–:160` uploads image + .deb per arch on EVERY push to main at 7-day retention; `ci.yml` uploaded test reports and the bus-soak XML at 14 days — the org's artifact-storage quota was hit on 2026-09-17 and run #251 read RED at the upload steps with a green-or-unread `check` (F-b7-1) | A verdict-independent step gated the verdict; the pin ritual (H8-a's GUARD 1 downloads a run's zip on the sitting day) needs SOME artifact to persist a week, but not every push's | CI-2 (landed with this row: uploads `continue-on-error`; reports 3 days; the .deb 7) stops the red; the remaining lever is a PIN-GATED upload — the .deb artifact uploaded only when a commit message or a manual run asks (`workflow_dispatch`; `[pin]`), every other push's smoke keeping its verdict without a stored artifact | Small; one workflow condition + the pin ritual's one line | October's re-cut (after the run's artifact cadence is known); status OPEN (v75 b7) |
| IR-28 | `lifecycle/lifecycle/src/test/java/com/homesynapse/lifecycle/BusSoakIT.java` (K=20 hero loops: delivery p50/p99/max, anomalies, the position census) and `nexsys-bench/scenarios/*.yaml` (no rate or growth scenario) — nothing at `3af6213` measures sustained event RATE, store growth per hour or heap over hours | The 72-hour run in October would be the first time the system runs for 72 hours; its largest event input (metering — OR-METERING-VOLUME: ~155k–311k events over 72 h for three plugs at HEAD's thresholds) has no headroom number to sit inside (Nick's v76 b2 brief §4; the b2 audit §4) | MEASURE-2: a test on `ZigbeeHardwareFreeRig` driving N synthetic 0x0B04/0x0702 reports per second for M minutes through the ingestion path, recording events/s sustained, the event store's growth per hour and heap over time — the number filed in THE MEASUREMENT RECORD before IR-25's thresholds are chosen | Small–medium; one test class + a filed number | ENERGY-READ (rides the coder session once the handlers exist); status OPEN — CHARTER ROW (v76 b2); CHARTERED — ENERGY-READ R7 (v76 b4): MeteringRateMeasurementTest → measurement-record.md rows M2-1..3; LANDED core `d1c2cbc` (v76 b6); MEASURED v76 b5: `rate=24031.1` events/s in memory (the rate leg closed with margin); M2-2 a payload floor — the store's growth is M2-2b (lifecycle) / THE SOAK |
| IR-29 | `core/automation/src/main/java/com/homesynapse/automation/StandardExplanationService.java:47–:74` — the run and non-firing projections are "a pure function of the log" by design (INV-SA-03), and no CI test replays a recorded log against golden verdicts | The observed-vs-derived caveat is a disclosure, not a test result; a regression in the projection would render a confident wrong verdict on an unchanged log (EXT-REVIEW-1 E-7; the b3 audit §2) | REPLAY-1: a golden-log test — a recorded corpus (the bench's `corpus/` or a run's export) replayed through the projections, every derived verdict asserted equal to the recorded golden set; one test class per projection | Small; a fixture + two test classes | November (after LINK-READ); status OPEN — CHARTER ROW (v76 b3) |
| IR-30 | `core/automation/.../StandardPendingCommandLedger.java:427–:445` — a `state_reported` that fails the command's `Expectation` (`ConfirmationResult.FAILED`) emits nothing and the entry ages out to `command_confirmation_timed_out`; `Expectation` is sealed `{ExactMatch, WithinTolerance, EnumTransition, AnyChange}` | A mismatched value after a command (EXT-REVIEW-1 packet 8: commanded 100 %, reported 30 %) renders as "unconfirmed" with the reported value unshown — the record holds the report and says nothing of it (the b3 audit §2) | A derived `mismatched` verdict (log-derived: the last `state_reported` on the target attribute inside the window, re-evaluated by the same `Expectation`) as an additive v1.1.x field, defined in VERDICT-VOCAB-1, implemented when a bench scenario (`command-mismatch`) exercises it; the desired and observed values shown | Small–medium; the projection + one scenario + the mirror | November; VERDICT-VOCAB-1 first; status OPEN — CHARTER ROW (v76 b3) |
| IR-31 | `core/automation/.../NonFiringExplanation.java:141–` — `NEVER_TRIGGERED` with no run carries the trigger summary and no cause; `availability_changed` (M9.6-AVAIL) and the source entity's `state_reported` are durable and unread by the projection | The silent non-fire (EXT-REVIEW-1 packet 4B) is a bounded unknown today; the system-neutral sentence — "never triggered; the source it watches last reported at T / went unavailable at T" — is derivable from the log without a language change (the b3 audit §3, D-v76-4) | The non-firing projection cites the trigger source's last report and its availability edge (additive v1.1.x, log-derived); the per-input declared cadence (E-4) stays a post-run design question | Small; the projection + the mirror + one scenario (a sensor powered off by the harness) | November; VERDICT-VOCAB-1 first; status OPEN — CHARTER ROW (v76 b3) |
| IR-32 | `integration/integration-zigbee` `ZclIngestionUnit.handlersByDevice` — the handler table is keyed by DEVICE while the metering formatting is learned per ENDPOINT (`MeteringFormatting.agreedAcross`, ENERGY-READ R-6): a pair is used only when every endpoint holding it agrees; a split pair is silence plus one DEBUG | A multi-endpoint metering device whose endpoints answer different divisors (a power strip) scales nothing today; exact for every single-metering-endpoint device in the fleet (the Gen4s, the TR3) | A per-endpoint handler table (or a per-endpoint formatting lookup in the two metering handlers) with a recorded-signature test over a two-endpoint device; RED when such a device is in the fixture set | Small–medium | October (with the second metering vendor's row); status OPEN (v76 b5) |
| IR-33 | `integration/integration-zigbee` — the shutdown hook closes the serial port under a read loop that was never told to stop: on 2/2 clean stops of `6bd8508` the journal shows `transport_failed: port dead or closed` 3–6 ms into the hook on a clean link (`retransmits=0 crcRejects=0 timeouts=0`), `PortWatchdog` `port_reopened` +1.6 s, the JVM exit ≤60 ms later, and `transport_closed_orderly` 0 — FAILCHAN §10-O's orderly path is dead on the wire (H8-a B3/B3-2b; R-5B B4; the b2 audit §1) | H8-a + R-5B, Sat 09-19 (F-1) | stop the adapter's read loop BEFORE the hook closes the port (or close through the adapter's own orderly path); the instrument: a hardware-free IT that stops the core and asserts `transport_closed_orderly` = 1 and `transport_failed` = 0 in the invocation's log capture; then the next rig stop's journal | Small | OPEN (v77 b2); a candidate rider on LINK-READ's session (same module) or its own small WU after the counter — the grade is clean either way (`success · 143`) |
| IR-34 | the automation engine — a run (trigger → 10 s delay → command → confirmation in 77–228 ms, 4/4 on the real fleet) leaves NO line in the journal at INFO; the store is the only trace (R-5B B3-1b, §4) — explainability's journal trace is absent, so an operator at `journalctl` cannot see the hero fire | R-5B, Sat 09-19 | one INFO line per run outcome (`automation.run_completed: automation= run= outcome= confirmed_ms=`), gated by the observability module's structured-log conventions; the instrument: the bench's `api-captures` + a journal grep in the same rep | Small | OPEN (v77 b2); the strategy pass reads it (the "did it actually confirm" story has a wire datum now) |
