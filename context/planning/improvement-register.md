<!--
file: context/planning/improvement-register.md
purpose: THE IMPROVEMENT REGISTER — one row per improvement candidate the hub or a lane observes in the source while doing something else: what was seen (source-cited), the measurement that would justify acting, the cost, the trigger, the status. Minted v73 beat 3 (2026-09-13) on Nick's standing directive (constantly review the source, even months-old code; iterate; plan the improvement even when not acting now). The hub adds rows at every intake audit's findings section and at WUCP Phase 2; a row RETIRES when its WU lands (the sha named) or when a measurement shows it is not worth doing (the measurement named). Never a backlog of wishes: every row names the instrument that would decide it. Read by grep when authoring the next WU on a surface (`grep -n '<module or class>' this file`).
audience: the hub (authoring; WUCP Phase 2) · the Coder and frontend lanes (the rows they will meet) · Nick (the shape of what is owed to the code)
state-type: register (rows appended; rows retired in place with the sha)
status: LIVE — minted v73 beat 3 (2026-09-13); rows IR-1..IR-10 (IR-10 added v73 beat 6, Sun 2026-09-13 ~17:5x CT; IR-9 retires on the bench card's landing)
-->

# Improvement register

| Id | Surface (cited at the sha) | Seen | The measurement that decides it | Cost | Trigger / status |
|---|---|---|---|---|---|
| IR-1 | `core/automation` `StandardExplanationService.locateTriggered` (post-114b: the `scanType` visitor with the early stop; at `a458a64` `:991`) | The scan starts at position 0 and stops at the first match in ascending order, so explaining the NEWEST run walks nearly the whole retained `automation_triggered` index — `SCAN_BATCH` (500) envelopes per page, `O(retained)` per request | Seed a realistic retained index (the bench, or `InMemoryEventStore` with 20k–50k triggered markers) and read the page count with the 114b `CountingEventStore` instrument (T3) for the newest run; file the count and the wall time | The by-id read (114c's K2: `EventStore` by run id or a run-id → position index) | 114c; MEASURE FIRST (the unmeasured-hop law); status OPEN (v73 b3) |
| IR-2 | `StandardExplanationService.listRuns` visitor (post-114b) | Positions ascend, so once `e.globalPosition() >= before` no later envelope is below the cursor; the visitor returns `true` today (114b preserved behaviour as ordered) where `false` would end the walk at the cursor | A T1-style test: a cursor inside page 1 ⇒ `reads(AUTOMATION_COMPLETED) == 1` (today 2 on a 501-marker index) | One line + one test | 114c row; status OPEN (v73 b3) |
| IR-3 | `StandardExplanationService.scanType` / `lastMatching` (post-114b) | One `Predicate<EventEnvelope>` type with two meanings in adjacent private methods — continue/stop vs matches/does-not; a wrong pass ends a walk at the first non-match | Readability only; no runtime measurement — the test is a reader's: a named functional interface (`Visitor.visit` → `CONTINUE`/`STOP`) or an enum return | Small; touches the five visitors | 114c row; status OPEN (v73 b3) |
| IR-4 | `core/event-model` `EventStore.readByType` (forward-only; `:113`) | Every "latest" read and every runs-list page is a full forward walk of a type index; a descending type read (`readByTypeDescending`) or a per-automation terminal index would make `latestTerminalRun`, `latestDisabled` and `listRuns` `O(page)` | IR-1's measurement generalised: the page counts per read on a realistic index; then the store's own cost of a reverse index (persistence, Doc 04) | A store primitive: event-model + persistence + a design sentence (Doc 04 / Doc 02) — an amendment-class change | After IR-1's numbers; status OPEN (v73 b3) |
| IR-5 | `api/rest-api` `Get*Endpoint` / `ListAutomationsEndpoint` — 27 hand-sized `LinkedHashMap<>(n)` (114a audit growth point 4) | Sizes drift from the key counts silently; the shape tests pin order, not sizes | None needed (cosmetic) | Trivial | The next rest-api WU that touches those files; status OPEN (v72 b1 via 114a audit) |
| IR-6 | `api/rest-api` test fixture `NonFiringExplanations` (114b) | Each wither repeats all 13 arguments; a 14th component (114c) grows every wither | None (test code) | A mutable builder halves the file | 114c, when the first v1.1.5 component lands; status OPEN (v73 b3) |
| IR-7 | `core/automation` `RunExplanation` / `AutomationSummary` convenience constructors (the 114b lane's observation) | The same pattern 114b collapsed for `NonFiringExplanation`: conveniences kept for test sites | `git grep -n 'new RunExplanation(\|new AutomationSummary(' -- '*.java'` per module: how many sites, which arities | The 114b pattern (a fixture, one constructor) | The next WU that touches either record; status OPEN (v73 b3) |
| IR-8 | `web-ui/dashboard` — the hero surfaces (`CausalChain.tsx`, `WhyNotView.tsx`, `ExplainHubView.tsx`, `Resource.tsx`) | HERO-1c D5: sentences still live in JSX text nodes and template literals (`terminalLine`, the condition line, the cascade link, page titles/ledes, "Signing in…"); the §6.3 grep instrument cannot see them | A lint — an ESLint rule or a script over the hero files flagging JSX text nodes / template literals that contain a sentence (a capital, ≥3 words, a terminal mark); its count at HEAD is the baseline; 0 is the target | Small (a rule + a CI line) | HERO-1d's instrument; status OPEN (v73 b5) |
| IR-9 | `nexsys-bench` `suite --list` | R-5A: `--list` lints without resolving `${{C.*}}`; a duplicate top-level key in `constants.yaml` is kept-last by PyYAML and surfaced only at run time (`boot-health` REFUSED) | The lint refuses a duplicated key at `--list` (a red-first test) | Small | RETIRED — landed bench `f3631cb` (R-5A-ii row 4: `--list` reads through `yaml.compose()`; a red-first fixture), v73 b7 |
| IR-10 | `nexsys-bench` `tools/nightly.sh` (R-5A-ii row 1) | `/api/v1/entities` is spelled twice in the nightly (the runner's read and the fleet read); a drift lands on `fleet: unread`, never a wrong number — bounded but two spellings for one route | One constants key (`api.entities-route`) read by both; a selftest that the two reads share it | Trivial | The next bench WU or R-5 Part B's prep; status OPEN (v73 b6) |
