<!--
file: context/planning/measurement-record.md
purpose: THE MEASUREMENT RECORD — the ledger of measured values the plan of record rests on, one row per measurement: the value, the instrument it was read from (the artifact and the field), the date, the sha, and what the value decides. Minted v76 beat 4 (Fri 2026-09-18) by ENERGY-READ's charter (Nick's word THE MEASUREMENT RECORD, v75). A row is written once from an instrument reading; a re-measurement is a NEW row, never an edit (the register's supersession law, applied to numbers). Predictions live in the charters and the plan; this file holds only what was read.
audience: the hub (the writer) · every lane that files a number (the row it fills is named in its charter) · Nick
state-type: measurement ledger (append-only rows; a value is never edited)
status: LIVE v76 beat 7c (Fri 2026-09-18 evening; REP-1's instrument re-cut to the bundle's receipts and the sheet — no journal line, no key that does not exist) — rows M2-1..M2-3 FILLED by ENERGY-READ's coder session (M2-2 a payload floor, disclosed); rows G4-1..G4-2's instrument corrected to the `zigbee.metering_formatting_read` INFO line (the return R-2); rows G4-4 (the uint48 read-back on silicon) and M2-2b (the real store's growth, lifecycle) ADDED, OPEN; rows CHAR-1..3 OPEN for Thu 2026-09-24; row REP-1 OPEN for the first rep.
-->

# The measurement record

**How a row is filled.** The lane or the operator that reads the instrument fills `Value`, `Instrument (artifact · field)`, `Date (CT)`, `At (sha / run)`; the hub fills `Decides` at charter time and never edits a filled value. A value that a later reading contradicts gets a new row with the same prefix and the next number; the old row stays.

## §1 Rows
| Row | Measurement | Value | Instrument (artifact · field) | Date (CT) | At (sha / run) | Decides |
|---|---|---|---|---|---|---|
| M2-1 | Sustained ingestion rate on the hardware-free rig: N synthetic `ActivePower` reports per second for M minutes → `state_reported` events per second sustained (the `MEASURE2 rate=` field); N, M and `dropped` beside it | **24,031.1 events/s** (`rate=24031.1`) — N = 20 frames/s, M = 3 min of TestClock → `frames=3600 events=3600 dropped=0`, `wall_s=0.150`. Limits, disclosed: ONE run; the frames are driven back-to-back (the TestClock, not the wall, paces them), so this is the path's ceiling over a 0.150 s, JIT-cold sample, not a paced 3-minute soak; the path measured is the adapter's own (scripted NCP → ASH/EZSP parse → dedup → handler table → `state_reported` publish) into an in-memory publisher — no SQLite, no bus subscriber, no radio | `_scratch/v76/2026-09-18_MEASURE-2_rate.txt` · `rate=`, `frames=`, `events=`, `dropped=` (+ the line's trailing `n_per_s=20 minutes=3`); read by `MeteringRateMeasurementTest` with `HOMESYNAPSE_MEASURE2=true` in the environment and `--rerun` (the charter's `-D` alone never reaches the forked test JVM: run verbatim it SKIPPED, 0 lines — the return's R-3) | Fri 2026-09-18 14:36 CT (19:36:57Z) | core `3af6213` + the uncommitted ENERGY-READ tree (the lane commits nothing); desk, JDK 21, warm Gradle daemon | whether IR-25's scaled thresholds put three plugs' 72-h volume inside measured headroom (OR-METERING-VOLUME) |
| M2-2 | Event-store growth under M2-1's load: rows and bytes before/after, per hour extrapolated (the store named: SQLite temp dir or in-memory, disclosed) | **The store is IN-MEMORY** — the zigbee test tree's `RecordingEventPublisher`; SQLite is NOT reachable from this module (LTD-17: it depends on integration-api only, and the persistence module is not on its test classpath). Rows `5 → 3605` (+3,600 = exactly 1 row per report; the 5 before are the adoption's own events). Bytes `0 → 108000` = **30 B per `state_reported`** — the UTF-8 length of the five payload slots (`power_w` · `80.0` · `W` · `8000` · `mult=1 div=100`), a payload FLOOR and NOT a row size (no envelope columns, no JSON keys, no index). Per hour at the driven N = 20/s: 72,000 rows, 2,160,000 B of payload floor; per report: 1 row, 30 B floor — the SQLite bytes-per-row is UNMEASURED here | the same file · `store_rows_before/after=`, `store_bytes_before/after=` (+ the line's trailing `store=in_memory_recording_publisher store_bytes=payload_utf8_floor`) | Fri 2026-09-18 14:36 CT (19:36:57Z) | the same | the 72-hour run's storage budget (THE SOAK) |
| M2-2b | Event-store growth under a sustained metering load on the REAL store (SQLite, a temp dir) — rows and bytes per hour extrapolated; the lifecycle module's instrument (a `BusSoakIT`-class IT), because the zigbee module cannot reach SQLite (LTD-17) and M2-2 measured a payload floor only | | a `MEASURE2B` line from a lifecycle IT · `store_bytes_before/after=`, `wal_bytes=`, `rows=` | | | THE SOAK's storage budget (OR-METERING-VOLUME's storage leg); OPEN — filled by the soak (wk 10-12) or by MEASURE-2b if wanted earlier |
| M2-3 | Heap before/after M2-1's load (a `Runtime` proxy after a gc hint, disclosed) | **9 MB → 11 MB** (`heap_before_mb=9 heap_after_mb=11`) — `Runtime` used heap after a `System.gc()` HINT, in the forked test JVM; the +2 MB is read while the in-memory publisher still RETAINS all 3,605 envelopes, so it is mostly the retained store, not a leak signal; a proxy over a 0.150 s run, silent on hours | the same file · `heap_before_mb=`, `heap_after_mb=` | Fri 2026-09-18 14:36 CT (19:36:57Z) | the same | the soak's memory-over-hours question (a proxy until the soak) |
| G4-1 | The owned Gen4's `ACPowerMultiplier` / `ACPowerDivisor` (0x0B04 0x0604 / 0x0605) as READ at the first adoption | | the journal's INFO `zigbee.metering_formatting_read: device= endpoint= cluster=0xb04 source=device mult= div= …` (the return R-2: a VERIFIED posture fact keeps `note == null`, so the values ride this line, not the fact) | | | the divisor P-row (DEVICE-SET note 3 predicted {1, 100}); the 0x0B04 leg's volume |
| G4-2 | The owned Gen4's seMetering `Multiplier` / `Divisor` / `UnitOfMeasure` (0x0702 0x0301 / 0x0302 / 0x0300) as READ | | the same INFO line for `cluster=0x702` (`mult= div= unit=`) | | | the 0x0702 leg's volume (Nick's brief §4: "1e6" was an assumption) |
| G4-3 | The owned Gen4's device type and input clusters at firmware 2.0.0 (the R6 line `zigbee.endpoint_classified`) | | the journal · `deviceType=`, `inputClusters=` | | | IR-24's premise (0x010A); DEVICE-SET note 4 (does 2.0.0 add 0x0400) |
| G4-4 | The 0x0702 reportable change AS READ BACK from the owned Gen4 at the first adoption — the six-byte field's upper bytes (the ENERGY-READ R-4 fix: HEAD encoded `05 00 00 00 05 00`; the fix sends the width with the upper bytes zero; the uint48 width has not met silicon) | | the journal's `zigbee.reporting_configured: … verified= degraded=` for the device and the read-back record's change (a DEBUG at the configurator, or the posture fact's rung) | | | whether the uint48 change field is honoured on silicon; IR-25's 5 Wh threshold live on the wire |
| CHAR-1 | B1's offset from A and B1's spread over ~20 simultaneous readings on the 80 W load after thermal settle (A + B1 + B2 in series) | | the CHAR sheet (the bench lane's `metering-known-load.yaml` operator block; a photo of the three displays per reading) | | | the dispute threshold for B1 (offset ± 2 × spread) |
| CHAR-2 | B2's offset and spread, the same readings | | the same sheet | | | the dispute threshold for B2 |
| CHAR-3 | Whether B1's and B2's offsets are the same number (the shared-OEM tell) | | the same sheet · a one-line verdict | | | whether two Bs count as two votes |
| REP-1 | The first rep: the plug's `power_w` against A after a load step (fresh by procedure — the b7 audit §0), the band at the guaranteed 2 % (3.03 % at 80 W), verdict WITHIN / OUTSIDE / VOID | | the run bundle of `metering-known-load.yaml` (`api-captures.json`: the typed A, tare and no-load reading, the read `power_w`, the effective reference and value (the record's DIV row: (power_w − OFFSET) / (A_W − TARE)), the ratio, the deviation, the verdict, the read instant and the entity's timestamp) beside A's display and the rep sheet | | | the corroboration leg's first WITHIN (plan §2 row 6) |
