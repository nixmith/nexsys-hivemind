<!--
file: context/planning/phase-3-milestone-backlog.md
purpose: Active Phase 3 milestone backlog — DONE/NEXT/PLANNED/FUTURE rows for every milestone.
audience: All
update-cadence: per-milestone
state-type: current
status: CURRENT
last-verified: 2026-07-24 (v37 hub, beat 1 — the CMD-API row flip: ✅ DONE @ `5b4797e` ALL-CI-GREEN + the `4bc1258` dependabot ride; B2 UNBLOCKED; the deploy owed; the currency note atop the body is the operative layer — head-note supersedes-where-differs per its own charter.) Prior: 2026-07-10 (v27 hub, beat 2 — THE ACCEPTANCE-ARC COMPACTION: the M9.4 mega-row + M9.5/M9.5b content → ONE certified-DONE row (detail preserved in git history + the bench record a81d678 + PROJECT_SNAPSHOT v26 beats 1–3); +15 rows = the 2026-07-10 ruled post-soak stack; rulings of record: pm-handoff v27 beats 1–2). Prior: 2026-07-06 (v21 hub, beat 1 — M9.4 row currency: **SD-5 ROUTED (a)** under Nick's delegated authority + the no-non-bench-network HARD GATE recorded in-row). Prior: 2026-07-04 (v20 hub, beat 1 — M9.4 row: M9.4b flipped to AUTHORED ISSUE-READY + the superseded CI-glance residual reconciled; live state = the M9.1–M9.4 rows + PROJECT_SNAPSHOT newest beat, pointer-not-copy). Prior chain: 2026-06-11 EOD against HEAD **`7c73c91`** — **M6.4 ✅ COMPLETE + COMMITTED `62a81e6` (sha reconciled 2026-06-11 at the M6.2-return session Step-0). M6.2 ✅ COMPLETE 2026-06-11 — gate GREEN (82 targeted + full 147, ZERO gate-fix rounds); PM Phase 2 APPROVE; R-1/R-2/R-3 ACCEPT; COMMITTED **`7c73c91`** (reconciled 2026-06-11 EOD; hivemind `d161ca9`). M6 = everything but M6.3 (triple-gated; evidence gates = the critical path). M6.3 = the last M6 piece; its triple gate (Nick-paced microbench + interviews + OR-M6-NONCE) is now the M6 critical path.** _Prior 2026-06-10 (late session):_ **Research 13 ASSESSED (A−; assessment `context/assessments/2026-06-10_Research_13_PM_Assessment.md`) → M6.4 + M6.2 instructions AUTHORED, both ISSUE-READY** (M6.4 row: 3 pinned obligations + R2 + REC-130/131/132/133/135 folded, rulings [R4]/[R5] at dispatch; M6.2 row: CARRY 1 verbatim + zero-new-edge re-verified + REC-140, ruling [R6] at dispatch; PM sequencing rec M6.4→M6.2). M6.3 stays triple-gated. FUTURE-AMD queue: REC-136 + REC-138/`ConfigChangedEvent`-§12.4. _Prior 2026-06-10 against HEAD `9035110`:_ **M6.1 COMPLETE (whole): M6.1b `b7bc65c` + M6.1a `9035110`** (incl. gate-fix round 1; GREEN 27+147 tasks; PM Phase 2 APPROVE); **M6.2 gate FULLY satisfied** (AMD-68 ✓ + M6.1 landed); **M6.4 UNBLOCKED** (obligations: R1 `config_error` publish + `config.section_reloaded` P2 survey + CoreSchema on reload); M6.3 stays triple-gated. _Prior 2026-06-09 against HEAD `6c6dd33`:_ **M6 ENTRY-GATE CLOSED: AMD-66/67/68/70/71 RATIFIED 2026-06-09 + AMD-69 DEFERRED (Option (a) confirmed, Tier-2/OQ-15-3)**; invariants §37–§41 registered (+8); nav-index rows added; **watermark UNCHANGED at AMD-87** (reserved slots filled); M6.1 UNBLOCKED / READY TO ISSUE (gate lifted, AMD numbers pinned); AMD-68 half of M6.2's gate satisfied (still gated on M6.1 landing); M6.3 stays triple-gated. _Prior 2026-06-08 against HEAD `6c6dd33` (= `7f44bed` + a trivial 1-line `Main.java` typo fix):_ **M6 entry-gate config side AUTHORED**: AMD-66–71 PROPOSED (AMD-68 = Doc 06 `SecretStore.setAll` currency; AMD-69 DEFERRED Tier-2/OQ-15-3) + the **M6 multi-piece charter** (M6.1–M6.4) + the gated M6.1 instruction + B2 schema decisions C8/C9; **M5-A COMPLETE (Parts 1+2+3; Part 2 AMD-87 codec committed `7f44bed`, PM WUCP Phase 2 APPROVE)**. _Prior 2026-06-07 against HEAD `8028337`:_ **M5-A Part 1+3 COMMITTED `8028337`** (on `8ef9e9f`; GREEN, 147 tasks) + **M5-window crypto+codec ratification COMPLETE**: Doc 15 LOCKED; AMD-86 + AMD-87 RATIFIED; INV-PD-07 narrowed + INV-PD-03 at-rest posture; AMD-86-INV-01/AMD-87-INV-01 (§35/§36); on-disk watermark AMD-64→**87**; **M5-A Part 2 UN-GATED** (Claude Code). Prior: **M4 COMPLETE** (M4.C / AMD-54..64 integration-api freeze DONE `8ef9e9f`; watermark AMD-64; `projectionVersion`=5; build GREEN, 145 tasks; working tree clean). **M5 window CHARTERED (2026-06-06, decisions D1–D8)** — blended/lane-tracked (M5-A/B/C/D); see the "M5 Window" subsection below + `weeks/2026-W24_jun08-jun14.md`. M4 retrospective + post-M4 synthesis authored; debt + process proposals P1–P6 logged below. Pre-M9 queue: AMD-87 (Expectation codec — now in M5-A; reassigned from the retired AMD-65 per INV-GA-02) + NQ-6 restart spike. Prior: AMD-54..64 RATIFIED (DOCS review RATIFY-WITH-EDITS; return `context/audits/2026-06-05_AMD-54-64_DOCS_Review_Return.md`); M4.B-S2 DONE `e76b925`; M4.B-S1 DONE `e73e199`; M4.0b-5 (AMD-53) DONE `c99b425`; M4.0b-4b (AMD-52) DONE `72596cb`; M4.0b-4a DONE `971cfa1`; M4.0b-3 (AMD-51) DONE `98f705b`.
-->

# Phase 3 Milestone Backlog — ACTIVE

> **2026-07-27 (v39 hub, beat 7) currency:** **THE NORTH-STAR INTAKE** (`context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md` filed; deliberations D1–D6: pm-handoff v39 beat 7). **NEW CANDIDATE ROW — AGENT-SEAMS** (post-gate; launch-runway-charter candidate: make the document's reservation claim TRUE — the additive proposal/adjudication/execution event types + the agent subject type + AIOT-INV-1's enforcement hook; verified ABSENT at `c09c61c` source this beat). **CHAIN-HYDRATE's authoring now GATES on WCAP-2** (the read path diff-exonerated; the Jul-25 field record proves fresh chains hydrated on `355a711`; the recent-run hop is the one unmeasured question — `context/handoff/2026-07-27_WCAP2_recent-run-chain_operator-block.md`, interpretation table in-file; L1). **The AGENT-SUBSTRATE research lane authored** (zero gate cost; runs at leisure). **B2 = the next hub beat, SOLE, full-bar** (the Jul-31 suites-running trigger governs). AUTO-IDENT elevates to a named agent-layer prerequisite (timing still Nick's pending one-liner; REC post-gate). Detail: pm-handoff v39 beat 7.

> **2026-07-27 (v39 hub, beat 6) currency — the detail-read cluster ADJUDICATED (WCAP return: `context/audits/2026-07-27_WCAP_detail-read-wire-capture_return.md`); three named rows replace it:** **(1) WU-CHAIN-HYDRATE — core, P1, THE G1 gate-day blocker, NEXT Coder WU (authored next beat with B2):** the live `/causal-chain` serves a self-contradicting skeleton (`actionCount: 9` / `actions: []` / `commandCount: 0` — run-family events hydrate, command events don't); hub-localized to the executor-stamping ↔ `readByCorrelation` seam (`StandardExplanationService:176`; pure log, no registry dependency); the authoring reads the executor's emission stamping at source; a history-seeded fixture must reproduce the LIVE emptiness red-first (the automation fixtures currently pass by writing commands under the run's correlation — structurally blind); the live-verification leg (a fresh run's chain populated) rides the next deploy. **(2) STATE-DIALECT — core P2 + an FE half:** `/state` violates the frozen A3 (contract: AMD-52 `{"t","v"}` typed envelope; wire: `{"value":…}` + epoch-second decimals + raw `{msb,lsb}` entityId; SPA: flat reads) — both sides conform TO the contract per the DRIFT-1 precedent; the seconds-as-ms "9:52 AM" mechanism proven in the return. **(3) AUTO-IDENT — design question, REC post-gate (Nick's one-liner pending):** automation identity rotates every boot (orphans run history from names/`lastRunId`; four symptoms one mechanism); the why-not endpoint itself is HEALTHY; launch-runway-charter candidate. FE-LIVE-V112 +(f) honest error postures (the 6 ms 404 rendering as an eternal spinner) +(g) timestamp qualification +(h) A3-conformant consumption. **B2 unchanged, authored next beat.** M14 strictly behind (1)+(2). Detail: pm-handoff v39 beat 6.

> **2026-07-27 (v39 hub, beat 5) currency:** **THE `c09c61c` DEPLOY IS DONE — the Pi runs it; G2 [S] ✅ CLOSED** (the redux tile verbatim; return: `context/audits/2026-07-27_dash-serve-deploy-evening_return.md`; three `[PASS]` floors; first npm build 49 s; Node one-time installed). **NEW CANDIDATE ROW — EVENTS-API** (non-gate, post-gate default: `GET /api/v1/events` + `/api/v1/health` = the M7.5c-b frozen-but-unbuilt slices, deliberately unscheduled since the freeze; the SPA degrades gracefully with an honest 404 card; minted per the L4 owner-row law at the redux's F1). **NEW ROW OF RECORD — THE DETAIL-READ CLUSTER (F2/F3/F4; gate-relevant via G1):** every list surface populates live while every detail surface fails (run causal chains render EMPTY universally · why-not hangs · drawer values/age em-dash) — server-vs-client undecided (the bench's own chain fetches succeed); **WCAP wire capture ordered** (`context/handoff/2026-07-27_WCAP_detail-read-wire-capture_operator-block.md`); **the fix WU = the next core-or-FE WU after its pastes adjudicate, and it outranks M14 absolutely** (G1's gate-day demo legs (a)/(c) are blocked on it — the ⚠ WATCH rides both G1 ledger cells). FE-LIVE-V112 charter +(f) uniform honest error/empty postures +(g) timestamp qualification + the npm-audit advisory rider. **B2 = the next-authored WU, unchanged** (rider #1 THE REBIND first; suites run lawfully against the deployed `c09c61c`). Detail: pm-handoff v39 beat 5.

> **2026-07-27 (v39 hub, beat 3) currency + row flip:** **WU-DASH-SERVE → ✅ DONE — landed core `c09c61c`, ALL GitHub Actions GREEN (Nick's own verification): ci.yml + `install-smoke.yml`'s FIRST run with the serve asserts + frontend.yml.** The install-smoke green is the load-bearing one — the full artifact→packaging→serving→auth seam (npm-on-runner → jar self-assert → image → .deb → boot → headerless `/`→302 → `/dashboard/`→200) is now CI-proven end-to-end AND asserted on every future push (the standing seam instrument). **THE REDUX-UNBLOCK FINDING (beat 3, verified at `c09c61c` source):** the served build defaults to the REAL transport at `window.location.origin`, and the paste-once token UX already exists complete (`AuthGate.tsx` in-memory entry per Doc 13 §12 · `realTransport.ts` Bearer-on-every-fetch · 401→prompt / 403→drop-token-honest-message) — **the deploy evening's browser-block redux needs NO FE landing; G2's close is gated only on the evening itself** (Pi Node op-order + deploy `c09c61c` + the three glances + the paste-once token, which appears on Nick's Pi terminal ONLY, never in a conversation — L3). FE-LIVE-V112 stays the named FE WU (verification halves (a)–(e) + UX refinements), launch-gated on the mirror sync, no longer on G2's critical path. **B2 = the next-authored WU** (rider #1 THE REBIND first; B2's suite runs lawfully against the CURRENTLY-deployed `2040a66` — DASH-SERVE added nothing wire-side — and re-runs post-deploy for H2's deployed-build green). Hub-owned docs fold (INV-SE-02 in-place + Doc 13 §3.2–§3.3 as-built + the doc-drift ×3) authors next. Detail: pm-handoff v39 beat 3.

> **2026-07-27 (v39 hub, beat 2) currency:** **WU-DASH-SERVE → DELIVERED + AUDIT-ACCEPT (zero defects; two-layer, all 13 files layer-2-read)** — the core commit is ORDERED (13 explicit paths; msg `_scratch/2026-07-27_core_dash-serve_commit-msg.txt`); **the row flips ✅ DONE at the CI-green landing intake** (the gate of record = ci.yml + **install-smoke.yml's FIRST run with the serve asserts** — headerless `/` → 302 and `/dashboard/` → 200 now ride every push, the standing seam instrument — + frontend.yml trivial). Audit record: pm-handoff v39 beat 2 (the M3 gate-reorder mutant SERVED the traversal probe — the EXACT-400 pin killed it; Nick's post-full-clean `check` 156/153 GREEN = the Node-free proof on a fresh tree; I1–I7 ratified; the preservation-fixture red-prediction imprecision hub-owned). On the landing: G2's live glance + FE-LIVE-V112's live halves stay gated on the NEXT deploy evening (the Pi Node op-order rides that brief); hub-owned docs-repo fold queued (INV-SE-02 in-place + Doc 13 §3.2–§3.3 as-built, with the doc-drift ×3). **B2 = the next-authored WU** (rider #1 THE REBIND first). Detail: pm-handoff v39 beat 2.

> **2026-07-27 (v39 hub, beat 1) currency:** **WU-DASH-SERVE → AUTHORED ISSUE-READY** (`context/instructions/2026-07-27_DASH-SERVE_dashboard-serve-path_coding-instruction.md` + `context/pre-verifications/WU-DASH-SERVE.md` P0–P15; the (A) static-shell ruling realized; `check` stays Node-free BY MECHANISM — npm on the jar task only + the app `testRuntimeClasspath` exclude + a Node-free grep gate assert; vite base verified already `/dashboard/`; install-smoke gains setup-node ×2 copies + run-smoke gains the `/`→302 + `/dashboard/`→200 serve asserts; 6 new tests incl. the EXACT-400 traversal order pin + M1–M3 named kills; census EXACTLY 13 = 12 M + 1 fixture). **Launch gated ONLY on Nick's skills-mirror sync (Check 9) + the beat-1 push.** **CMD-API-ACTOR adjudicated OUT** of DASH-SERVE (freeze-window single-concern) → parked for the first post-gate rest-api touch. B2 = the next-authored WU (rider #1 THE REBIND first; the three-flip re-mint); core goes QUIET after DASH-SERVE until the M14 WUs. Hub-owned at the DASH-SERVE landing: the INV-SE-02 in-place amendment + the Doc 13 §3.2–§3.3 as-built note (docs repo, with the doc-drift ×3 batch). Detail: pm-handoff v39 beat 1.

> **2026-07-26 (v38 hub, beat 7) currency:** **THE DEPLOY IS DONE — the Pi runs `2040a66`** (the deploy evening COMPLETE: six lawful boots, soak clean, floor/deploy/H4/A4 all `[PASS]`; the return: `context/audits/2026-07-26_deploy-evening_return.md`). **A4 → ✅ BANKED · H4 → ✅ BANKED** (criteria ledger cells flipped this beat). **NEW ROW OF RECORD — WU-DASH-SERVE (P1, core lane, NEXT Coder WU, authoring on Nick's auth-posture ruling):** the deploy evening's browser block surfaced that the dashboard has NO serve path — no `staticFiles` mount (Doc 13 §3.2–§3.3 never composed), no artifact from `:web-ui:dashboard` (empty `runtimeOnly` resolution; installDist builds nothing web — the 23s Pi build proved it), and AB-1's catch-all auth (INV-SE-02) 401s any headerless browser; the WU = packaging (java-plugin resources jar of the staged SPA) + the `/dashboard` static mount + root redirect + the ruled auth exemption + filter/packaging tests + the Pi Node op-order. **It gates G2's close and ALL of FE-LIVE-V112** (which expands by the token UX: paste-once entry, Bearer on fetches, honest 401 screen, `/dashboard/` base). **FLAGS 1–3 RETIRE wire-side** (the deployed read surface IS v1.1.2). **The §8 interim dead-zone rule is EXPIRED** (DP-3 live — `triggeredAt` trustworthy where eventTime is present; B2's rebind proceeds as authored regardless). **B2 = the next bench WU unchanged** (rider #1 THE REBIND first; the three-flip re-mint; authored before the next bench evening); the next deploy evening (B2's first silicon evening) carries the DASH-SERVE landing → the browser-block redux → **G2 closes there**. Calendar unmoved: dry-run ~Aug-10, freeze Aug-14 EOD, read Aug-16. Detail: pm-handoff v38 beat 7.

> **2026-07-26 (v38 hub, beat 6) currency + row flip:** **SKIP-VIS → ✅ DONE — landed core `da11f46` ALL-CI-GREEN** (Nick's confirmation of record; the explanation-honesty set: DP-1 raw-outcome carry [superseded never FAILED · honest-`unconfirmed` → UNCONFIRMED with the recorded reason] + DP-2 silent-skip verdict honesty [`noCommandsIssued`] + DP-3 the `triggeredAt`/`evaluatedAt` fix at BOTH sites + DP-4 GO [`settled`]; two-layer audit ACCEPT zero-defects [pm-handoff v38 beat 4]; the v1.1.2 additive keys live on main; the freeze doc stamps v1.1.2). **FE-VERDICT-2 → ✅ DELIVERED + AUDITED** (two-layer ACCEPT zero-defects, v38 beat 6 — the five modes render DISTINCT [label+glyph+tone, AA both themes, §3a-clean]; D-4 retired field-first; `noCommandsIssued` consumed; **G2 [S] delivered-pending-the-2-min-live-glance** [rides the deploy evening's browser block]; **the dependabot deps watch CLOSES** on the return's §4 evidence [baseline green on the bumped lockfile; lockfiles md5-untouched]; the core FE commit = 20 explicit paths, ordered; `frontend.yml` on the push = its gate of record; **FE-LIVE-V112** = the named next FE WU [halves (a)–(c) ride the deploy evening's browser block]). **THE DEPLOY = TONIGHT** (target per Nick's (a)/(b) line — REC (b): the FE landing SHA, one evening carries the write surface + the honesty fixes + the honest dashboard; the §8 interim dead-zone rule expires at the deploy). B2 authors at the next hub beat (rider #1 THE REBIND first; the three-flip constants re-mint carries `command-api`→true + `usb-power`→true + the `usb.*` mints). Detail: pm-handoff v38 beats 4–6.

> **2026-07-25 (v37 hub, beat 3) currency — Nick's pushback intaken; three dispositions:** **(1) Q1's three-case frame RESOLVED — Case A determined; THE SKIP-VIS FOLD IS GO (Nick's own conditional satisfied):** Case B eliminated by the full `ListRunsEndpoint` read (`:127` = `map.put("triggeredAt", run.triggeredAt().toString())` — pure serialization; the wire assembly holds ZERO arithmetic; the value arrives via `com.homesynapse.automation.ExplanationService.listRuns`) · Case C eliminated at `RunSummary.java:34` (the field's documented meaning IS the trigger instant; the derivation is a documented best-effort of exactly that concept) · Case A direct at `StandardExplanationService.java:463-:466` — **and the ARCH-RULE-REACH grep EXECUTED** (rules touching `com.homesynapse.automation..`: NO_REVERSE_DEPENDENCIES `:143/:150` + NO_DIRECT_FILESYSTEM_IN_CORE `:191/:197` — neither reached by value arithmetic on in-hand envelope/payload values; NO_DIRECT_TIME_ACCESS clean, zero clock reads in the fix shape; **ZERO collisions**). CMD-API-ACTOR stays parked (no rest-api touch — Case B refuted). **(2) THE ENGINE REBIND — Nick-directed ("take this one regardless"), ADOPTED as B2 rider #1, MANDATED as the port's FIRST piece:** premise VERIFIED at source (`engine.py eval_new_run_after` — the `triggered < m_observed` discard `continue`s BEFORE the per-run `causal-chain` fetch; the reorder is real): fetch-first, bind **`trigger.matchedAt`** (the externally-corroborated instant); the anti-false-PASS arm PRESERVED on matchedAt; chain-unfetchable / trigger-view-absent / unparseable-matchedAt ⇒ ignored-with-reason, never vacuous; fixture pairs per the standing law (PASS + false-verdict boundary + missing-trigger-view); BOTH fields reported in the ok-payload while the core defect is live (free diagnostic: |matchedAt−triggeredAt| ≈ durationMs is the defect's live signature). **Ordering law: the rebind lands BEFORE B2's own AUTO usb leg runs; B3 GATES on it** — the instrument is sound-by-rebind regardless of the core fix's deploy date. **(3) Deploy timing RULED (Nick): overnight soak — the deploy evening ≥ 07-26** (zero-soak topology + transport-stressing reps never stack on move day; ruling-3's purpose over its letter; §8 tired-human clause); the brief AMENDED in place (Block 0a overnight-soak glance — the topology's first soak evidence banks free). **FE-VERDICT-2 authoring input added:** the five-honest-failure-modes distinction (§5.1's vocabulary + §5.9's held-DISPATCHED-then-FAILED) renders DISTINCT, never collapsed — the distinction IS the product. Detail: pm-handoff v37 beat 3.

> **2026-07-25 (v37 hub, beat 2) currency — the Rosonway intake:** **I3b [S] → ✅ CLOSED** (`[PASS] usb-reenumeration-manual — 3/3 positive · 0 forbidden`, hub-attached; two-layer adjudication ACCEPT — pm-handoff v37 beat 2; the topology STANDING; **the M14 RO condition is now proven in BOTH topologies**; the `usb-power` constants flip rides B2's re-mint with the §5.8 candidates — `uhubctl` install is B2 session prep). **🔴 NEW ROW — RUNS-TRIGGEREDAT** (CONFIRMED at source, the composed-behavior class): `runs[].triggeredAt` = trigger-instant − `durationMs` — `StandardRunManager:210` inherits the triggering event's eventTime onto the whole run family (ruled DP-G) + `:656-:665` publishes `automation_run_completed` with `run.eventTime` and a live-measured `durationMs`, while `StandardExplanationService:463-:466` assumes the terminal envelope's eventTime ≈ completion and subtracts `durationMs` — producing T−D, wrong by exactly the run's duration (field-measured twice, duration-tracking, microsecond-exact). **A core VALUE defect in the derivation layer** — the emitter is lawful, the wire SHAPE unchanged (no v1.1.x bump; four-constraint law untouched), the B1 engine binds the RIGHT field. Fix shape: eventTime-present branch → `triggeredAt = terminalTime` (it IS the trigger instant under DP-G); keep `−durationMs` only for the ingestTime fallback; fixture-paired tests incl. a triggeredAt≡matchedAt regression. **REC: folds into SKIP-VIS (same file) — Nick's one-line ruling pending**; until the fix DEPLOYS, the playbook §8 interim dead-zone rule governs every `new_run_after` leg. **B2 riders added:** the engine `pending_positive_tokens()` display fix (log_any/api lines never filter — verified `engine.py:736-:747`) · the scenario goal-string de-hardcoding (`usb-reenumeration-manual.yaml:74`) · `new_confirmed_run` verified UNAFFECTED (no timestamp bound). **FE-VERDICT-2 + SKIP-VIS authoring inputs routed:** §5.9 (an action outcome MUTATED post-COMPLETED — provisional-vs-settled rendering; candidate additive per-action settled marker under the v1.1.2 law) · §5.2 (availability≠reachability — the skip gate never engages on a log-rehydrated-AVAILABLE target) · §5.3 (AVAILABLE is not an RX proof; `staleAfter: null`) · §5.4 (`actionCount` 9 vs `actions[]` 5 — delay actions uncounted on the chain; adjudicate at SKIP-VIS grounding). Report of record: `context/handoff/2026-07-25_rosonway-topology-move_I3b_bench-session-report.md`. Detail: pm-handoff v37 beat 2.

> **2026-07-24 (v37 hub, beat 1) currency + row flip:** **CMD-API → ✅ DONE** — landed core **`5b4797e` ALL-CI-GREEN** (Nick's confirmation of record; the gate of record CLOSED; **the repo's first `app.post`**: `POST /api/v1/entities/{entityId}/commands` 202/400/404/409/422 + `GET /api/v1/commands/{commandId}` 200/404 — thin-adapter publish of ONE root `command_issued` USER_COMMAND, idempotency fully REST-layer, never-false-CONFIRMED test-locked + M1–M3 mutation-verified; two-layer audit ACCEPT with R1 [the scoped Rule-9 exemption] RATIFIED — pm-handoff v36 beat 9); the dependabot web-ui merges rode → **`4bc1258`** (zero-Java delta NAMED per the OR-GATE-M7.4 subsumption precedent — **the standing dependabot watch item CLOSES**). **Consequences:** **B2 (the §51 suite port) UNBLOCKED** — its P16 PROVISIONAL command-lifecycle constants are LIVE FROZEN truth (camelCase `{data,meta}` · UPPERCASE phases · `data.terminal`/`data.currentPhase` HOLD); `capabilities.command-api.available` flips true AT B2 with the constants re-mint (which also carries the Rosonway `usb-power` flip once that evening lands); the C2/C3/H2 producers + B3 nightly + the M14 WUs stack behind it exactly per rider 3. **THE DEPLOY IS OWED** — the Pi runs `355a711`; the brief is authored (`context/handoff/2026-07-24_cmd-api-deploy-evening_operator-brief.md`, +H4/A4 riders; NEVER the Rosonway evening); **B2 lands only against the DEPLOYED build.** Candidates carried from the audit: **EXEC-DETERMINISM** (NON-GATE; the executor's `Map.copyOf` per-JVM salt — rides the next automation-touching WU) · **CMD-API-ACTOR** (attach the API identity as actorRef — rides the next rest-api touch). The M14 legacy-row redescription stays queued for the M14-adjacent backlog touch (the v36 beat-1 adjudication). Detail: pm-handoff v37 beat 1.

> **2026-07-21 (v35 hub, beat 7) currency + row flips:** **D1 [M] ✅ 5/5 CLOSED** — the 04P adopted as `binary_sensor`/**contact** off the PERSISTED wire-learned CONTACT with **zero re-learns** (deviceId `01KY12MQVQ204M1VP39F1ZDM33` · entityId `01KY12MQW954E4XYNKH0Y5H8VX`; the learn survived a network-leave + three reboots — the persistence path proven under adversity; report: `context/handoff/2026-07-21_04p-adoption_bench-session-report_for-hub-adjudication.md`, hub adjudication ACCEPT). **W2-LEARN → ✅ DONE** (code `554e18c` + learn silicon-verified + the adoption done-when met). **LEARN-PERSIST → ✅ DONE** (code `355a711` CI-green + the silicon leg complete). **BENCH-CONST → ✅ RE-MINTED** (2026-07-21, hub, per the constants file's own charter: fleet 6/6 · watermark **25065** [the projection_live replay-head — the ruled counter, never viewPosition] · panId 0x774c · remembered-ulids grown to all six; boot-health [PASS] expected on the next run — the lawful-[FAIL] era ends). **THE M14 TRIGGER SWEEP: PASS 7/7 — M14 (integration-shelly) OPENS** (certification ✓ · RO ✓ · AVAIL ✓ · SEED ✓ · zero open [REVIEW]s ✓ · G1 ✓ · D1 ✓ — 10 days ahead of the Jul-31 boundary; **the third-plug rider FIRES** [~$25–35, just-in-time, Nick's call]; sequencing pre-ruled: rider 3 — CMD-API + the B2 port precede M14 WUs; **J1 stays FROZEN**, the mid-Aug gate untouched). NEW CANDIDATE ROWS: **ADOPT-REPROPOSE** (hands-off propose of a known-but-unadopted cached-interview device newly in `adopt_devices` at boot — Q1 ruled at source: no such path exists at `355a711`; kills the Window-#2 physical-press hazard class; post-M14-open, non-gate) · **OBS-CONFIRM** (adapter-side low-rate confirm-path INFO or a bench entity-value read — the bench currently cannot show "did it actually confirm?" without inference [the D-1 gap]; instrument-first; rule at B2 authoring). Detail: pm-handoff v35 beat 7.

> **2026-07-19 (v35 hub, beat 6) currency + row updates:** **G1 [M] ✅ CLOSED END-TO-END** (dispatched → returned → two-layer ACCEPT → landed `f3bfd5c` → `frontend.yml` GREEN on the push — the verdict surface ships the TEN-value vocabulary + the deterministic disposition recovery; the v1.1 contract gaps GAP-1..5 + core proposals CORE-P1..P4 intaken one-turn-rulable [REC: v1.1.2 additive `resultOutcome` + the nullability clarification]; **FE-VERDICT-2** = the named next FE WU, dispatches on the ruling). **W2-LEARN: code-complete + learn-SILICON-VERIFIED** (`554e18c`; the gate of record closed by subsumption at `f3bfd5c` [zero-Java delta]; the wire learn proven on the 04P re-join: `ias_zone_type_learned … CONTACT`) — **its adoption done-when TRANSFERRED to LEARN-PERSIST; the row flips DONE at D1 5/5.** **NEW ROW — LEARN-PERSIST ✅ code-DONE, landed `355a711` CI-GREEN** (the REQUIRED D1 predecessor, promoted from candidate at the 2026-07-19 BENCH-STOP: the cached-interview fast-propose race [propose t0+0.8 s vs the TCLK-gated learn t0+9.98 s × the restart-wiped in-memory map × no un-adopt] would have durably mis-classified the 04P as MOTION; DELIVERED + two-layer ACCEPT zero-[REVIEW]; persisted learns via the `zigbee-devices.json` top-level `learnedZoneTypes` section + ctor-seed rehydration + the `zigbee.learned_zonetypes_rehydrated: count=N` boot glance; the RACE LEG = the standing hardware-free regression of the bench finding; **the silicon leg = the v2 persist-verified operator package** [`context/handoff/2026-07-19_04p-adoption_operator-mini-package_v2-persist-verified.md`; supersedes v1] → **D1 [M] 5/5**). **BENCH-CONST:** authors from the v2 package's Block-6 **6/6 instrument-read values** — boot-health stays lawfully-[FAIL] until it lands. **SKIP-VIS** gains the G1 lane's consuming-side endorsement + the exact core fixes (CORE-P1 `StandardExplanationService.java:617-:656` — carry the raw outcome · superseded never selects FAILED · honest-unconfirmed derives UNCONFIRMED; CORE-P2 `:288-:309` — the completedVerdict silent-skip honesty) — **folds into ONE instruction on the GAP-1 ruling.** **MATTER-INT: RESOLVED-TO-PROGRAM** (the row's "one-word ruling pending" below is SUPERSEDED — Nick ruled R-0 2026-07-19: the full pre-code design program, chartered + running; Phase A ✓ · B1–B5 RULED · Phase C OPEN; code stays post-sweep + post-Lock; consume `context/programs/matter-design/00_PROGRAM_STATUS.md` §-beats). **CMD-API** = the W30 core pole (tripwire ~Jul-24; the honest posture stands — C1's N already organically exceeded). Detail: pm-handoff v35 beats 1–6.

> _Currency notes older than the 2026-07-19 v35-beat-6 note ROTATED 2026-07-26 (v38 hub, beat 5) → `archive/phase-3-backlog-currency-notes-rotated-2026-07-26.md` (verbatim). Head-note-supersedes-where-differs unchanged._

## Active queue & forward roadmap

_The DONE Major M1–M3 detail, the M4 retrospective, the committed-M3 design-artifacts index, and the M3.x WUCP Phase 2 closeout entries were rotated to `archive/phase-3-milestone-backlog-M1-M4-rotated-2026-06-21.md` (2026-06-21, v4 hub). The forward rows below are unchanged; roadmap cross-references to “M3 Milestones above” etc. now point into that archive._

### Pre-M9 queue (must clear before / alongside the M9 supervisor)

| Item | Scope | Status | Notes |
|---|---|---|---|
| **AMD-87** (reassigned from retired AMD-65 per INV-GA-02; `@Disabled` literal in code still reads "AMD-65 pending" — delete on landing) | **`Expectation` persisted sealed-type codec** — hand-rolled `JsonSerializer`/`JsonDeserializer` pair in `PersistenceJacksonModule` over the 4 `Expectation` permits (ExactMatch/AnyChange wrap `AttributeValue` → existing codec; EnumTransition wraps String; **WithinTolerance(double,double) needs the AMD-52 bit-anchored-float / non-finite-sentinel determinism treatment**). Required for command-bearing `CapabilityAdded` round-trip (AMD-59-INV-02). | **QUEUED — BLOCKING-for-M9** (surfaced M4.C 2026-06-05) | Small ratified amendment (AMD-52 precedent, lightweight DOCS review) → Coder WU. Acceptance spec already exists: the `@Disabled("AMD-65 pending")` `EventPayloadCodecTest.CapabilityEvents.capabilityAdded_onOff_roundTrips`. **M9 must NOT publish command-bearing `CapabilityAdded` until this lands.** |
| **NQ-6 restart spike** | Empirical Zigbee/Matter restart-frequency measurement on a Pi to validate/tune the 1/60s default restart intensity (Research 6 NQ-6). | QUEUED — pre-M9 | Informs `HealthParameters.defaults()` / per-descriptor override; non-blocking for M9 design, validates a default. |

### Scheduled pre-M14 spike (Nick-approved 2026-06-05)

| Item | Scope | Status | Notes |
|---|---|---|---|
| **SPIKE-DC** | **Dual-coordinator design spike** — Research 12 §5.2 found NO field precedent for aggregating two Zigbee coordinators into one logical network (Z2M/ZHA/deCONZ/openHAB all 1:1); Doc 08 requires dual-coordinator support, so the design must be original. Resolve: aggregation model, identity/addressing across networks, failure semantics, whether Doc 08 needs an AMD. | PLANNED — target ~Aug 2026, BEFORE the M14 briefing | Per `context/assessments/2026-06-05_Research_12_PM_Assessment.md`. Spike rules apply: throwaway code outside the production tree, findings recorded; possible Doc 08 amendment output. M14's REC-120..129 candidates (transport-first ordering) also live in that assessment. |

### M5 Window — lane-tracked pieces (chartered 2026-06-06; decisions D1–D8)

The post-M4 synthesis (`context/planning/2026-06-06_post-M4-plan-and-M5-scoping.md`) + Nick's decisions D1–D8 (`context/decisions/2026-06-06_post-M4_M5-window_decisions.md`) charter "M5" as a **blended, multi-lane window framed to de-risk M6** — tracked as **four first-class lane pieces, not one milestone** (P1; the key reframing: the originally-planned Platform-API+test-support M5 is thinner than it looked — the test-support doubles already shipped in M1.x). Sized to Nick's review/ratification gate. Charter: `context/planning/weeks/2026-W24_jun08-jun14.md`.

| Piece | Lane | Scope | Status | Done-when |
|---|---|---|---|---|
| **M5-B** | **Design (PM) — leverage-ordered; the M6 entry-gate; FIRST to execute** | **B1:** INV-PD-07 amendment per D2 (operational crypto-shred → post-MVP; infrastructure+categories at MVP; **full DOCS review, not P4**); promote the 2026-03-22 crypto Draft to an owned design doc reconciled with Doc 06 + AMD-60, **pinning the write-path-encryption boundary** (D2 sub-decision); author+ratify **AMD-66–71** so M6 can start. **B2 (regret-proof, ride alongside):** `actorRef` semantics (C8), energy event *shape* (C9, shape-only per D4), payload-typing posture (C10). | **B1 ✅ COMPLETE 2026-06-09** (Doc 15 LOCKED; AMD-86 RATIFIED; **AMD-66/67/68/70/71 RATIFIED 2026-06-09 + AMD-69 DEFERRED → M6 entry-gate CLOSED**). **B2 IN PROGRESS** (C8/C9 authored 2026-06-08, contract-freeze-ready; independent review + Nick ratification pending) | B1: INV-PD-07 amendment ratified; crypto owner-doc reconciled + boundary stated; AMD-66–71 ratified → **M6 unblocked**. B2: each passes the contract-freeze-readiness gate (round-trips/enforceable/owner-doc). |
| **M5-A** | **Core (Coder) — deliberately small** | `PlatformPaths` (LinuxSystemPaths + LocalPaths) + `HealthReporter` (SystemdHealthReporter `sd_notify` + NoOpHealthReporter) — leaf impls of Doc 12 §8.2/§8.3; **AMD-87 `Expectation` codec** (reassigned from retired AMD-65, INV-GA-02; lightweight block-track P4); **register `FloorId` in `PersistenceJacksonModule`**. Apply P2 consumer/pin survey. Do NOT rebuild existing test doubles (D7). | **✅ M5-A COMPLETE — Part 1+3 `8028337` + Part 2 (AMD-87 codec) COMMITTED `7f44bed`; GREEN (147 tasks); PM WUCP Phase 2 APPROVE** | Part 1 (platform impls) + Part 3 (FloorId reg) committed `8028337`; **Part 2 (AMD-87 `Expectation` codec) committed `7f44bed`** (2026-06-07) — interface-keyed tagged-union over the 4 permits, exhaustive no-`default` switch, `requires com.homesynapse.device` (acyclic, no Gradle change), un-`@Disabled` acceptance test GREEN (real-event round-trip, not `DegradedEvent`); PM WUCP Phase 2 → APPROVE (source-verified vs AMD-87 §2 + AMD-87-INV-01; 3 `[INFO]` accepted). **M9 command-bearing-`CapabilityAdded` prerequisite CLEARED.** |
| **M5-C** | **Non-Core (W3) — protected, non-preemptable (P6/D3)** | Website/docs first increment: Docusaurus scaffold + brand shell + 3–4 positioning/architecture/privacy pages. Not tradeable to accelerate M5-A. Out-of-scope: auto-generated API/config reference (M10/M11-gated). | **PLANNED** | Site builds locally/staging; positioning/architecture pages drafted. |
| **M5-D** | **Evidence (parallel/commissioned)** | GraalVM native-image spike (C15) + Generational-ZGC-vs-G1 Pi-4 measurement (C16) → LTD-01 reversal-criteria ledger; energy Mom-Test interviews (C17 — near-term; extended to ask institutional buyers about verifiable-erasure = the D2 re-scope-up trigger); **the Pi-4 AES-256-GCM write-path microbench (resolves OQ-15-2; gates the M6 at-rest WU)**; **the sd_notify transport-mechanism decision (OR-M13-SDNOTIFY)**. | **IN PROGRESS — 3 evidence artifacts AUTHORED 2026-06-07 (PM); spikes PLANNED** | **AUTHORED (`context/assessments/2026-06-07_M5-D_*`):** (1) Pi-4 AES-GCM microbench spec + per-category decision rule (OQ-15-2 — Nick runs on Pi-4; writes back the `encrypted_scopes` list, a Doc 15 TUNING not a re-open); (2) energy+erasure interview guide (AMD-86 §3 verifiable-erasure trigger → formal-pipeline AMD-86 re-open before M6; + D4 energy-shape → C9; Nick runs interviews); (3) sd_notify decision matrix + rec (defer-to-M13, gated on GraalVM C15; reject JNR/JNA; no catalog dep). **Still PLANNED:** GraalVM (C15) + GenZGC-vs-G1 (C16) Pi spikes → LTD-01 ledger. Spike rules apply. |

Deferred OUT of the window (just-before-consumer, per D1): C11 INV-SE-04 scoped-registry mechanism (→ M9 prep); C13 Doc 02 currency; C18 doc-hygiene. C4 verification-foundation: quick-scope first (D7), lean to a small property/fault-injection harness.

### Future Major Groups (provisional skeleton from Alignment Pass #2)

| Major group | Subsystem | Dependencies | Status | Notes |
|---|---|---|---|---|
| **M3** | Event Bus (production impl) | persistence (M2) | **DONE** | M3.1–M3.7 + intra-milestone WUs done (eighteen Claude Code WUs total). M3 COMPLETE 2026-05-27. See M3 Milestones above. |
| **M4** | **Foundation: device-model expansion + projection/derivation + integration-api interface freeze** | event-bus (M3), persistence (M2), device-model | **✅ COMPLETE 2026-06-05 (`8ef9e9f`)** | **Re-baselined.** `StateQueryService` already shipped in M3.6e.1. **Workstream A** (projection/derivation foundation) is substantially **DONE**: M4.0a (`AtomicCheckpointWriter`/AMD-45, `a441fdf`) ✓, M4.0b-1 (`DispatchingProjectionAdvancer` REC-28 + real `DerivationRule`, `cf1a97e`) ✓, **M4.0b-2 (AMD-50 backfill + `projectionVersion` 1→2, `7610296`) ✓** — **Workstream A is COMPLETE** — M4.0b-3 (typed comparator AMD-51, `98f705b`, independently reviewed → APPROVE) shipped, closing A. **Workstream B's typed-value contract** (M4.B3 / AMD-47, `60b4185`) is DONE; any remaining device-model breadth (AMD-44 Floor/EntityRole impl, Research 8 REC-23–30) plus **Workstream C** (integration-api freeze, Research 6 REC-41–51) are the open M4 fronts. AMD-52 (typed `StateChangedEvent` payload) is **RATIFIED 2026-05-31** and shipped as **M4.0b-4a** (AttributeValue→`com.homesynapse.value` relocation, **DONE `971cfa1`**) + **M4.0b-4b** (the typed payload itself, **DONE `72596cb`**, reviewed → APPROVE) — **Workstream A is now complete, typed end-to-end** (`projectionVersion`=4). The open M4 fronts are the timestamp-model unifier (the `lastChanged` event-time/wall-clock reconciliation — the honest last mile of A, needs a small AMD), then Workstream B, then Workstream C. **Workstream C** = integration-api interface freeze (Research 6 REC-41–51) — **gated on P3** (Research 6 NQ-1..6); supervisor impl = M9. Plan: `homesynapse-core-docs/design/HomeSynapse_Core_M4_Implementation_Plan_PLAN-M4-CONSOLIDATED.md` (v2 §3 needs the M4.0b-2 re-scope + M4.0b-3/4a rows — doc-currency follow-up). **P2 RATIFIED**; watermark AMD-52. Note: config=M6, automation=M7/M8, integration-runtime impl=M9 unchanged. |
| **M5** | Platform API + test-support → **re-chartered as the lane-tracked M5 window (2026-06-06)** | no hard deps | **CHARTERED → see "M5 Window" subsection above** | **Re-framed (D1–D8):** the planned Platform-API+test-support M5 is thinner than it looked (test-support doubles already shipped in M1.x), so "M5" is now a **blended window** = M5-A small Core (PlatformPaths/HealthReporter impls + AMD-87 + FloorId reg) · M5-B design/decision (the M6 entry-gate + regret-proof schema decisions) · M5-C protected website/docs · M5-D evidence (GraalVM/GenZGC spikes + energy interviews). See the M5 Window table above + `weeks/2026-W24_jun08-jun14.md`. |
| **M6 (Configuration + secrets/crypto)** | config pipeline + secret store + per-scope key-mgmt + at-rest encryption + hot-reload | state-store (M4) ✓, persistence (M2) ✓, **M6 entry-gate: CLOSED 2026-06-09** (Doc 15 LOCKED ✓ + AMD-66/67/68/70/71 RATIFIED ✓; AMD-69 DEFERRED Tier-2/OQ-15-3) | **CHARTERED — MULTI-PIECE (P1) → `context/planning/2026-06-08_M6-charter.md`; ENTRY-GATE CLOSED 2026-06-09 → M6.1 READY TO ISSUE** | Sized per D2 (config pipeline + at-rest secret encryption + key-mgmt infra/seams; operational crypto-shred post-MVP). **The single "M6" line is SUPERSEDED by the four first-class rows below** (the P1 epic-in-disguise fix). Naming correction: the impls are **`ConfigurationAccess` + `ConfigurationService`** — there is **no `ConfigurationProvider`** type in source (Check 11). |
| **M6.1 — config pipeline** | YAML load + JSON-Schema validation + `ConfigurationAccess`/`ConfigurationService` load path + AMD-66/67/70/71 contracts, on the composition-root-injected config-dir `Path` | AMD-66/67/70/71 RATIFIED | **✅ COMPLETE (whole) 2026-06-10.** M6.1b committed `b7bc65c`; M6.1a (incl. ⟳ gate-fix round 1: explicit `CoreSchema` both LoadSettings + ArchUnit Rule 5 config exemption per ratified AMD-71) GREEN 2026-06-10 (27 + 147 tasks); PM Phase 2 APPROVE (module-info = the 2026-06-10 ruling exactly); **M6.1a COMMITTED `9035110`** (sha reconciled 2026-06-10). R1/R2/R3 ruled ACCEPT — R1 (per-ERROR `config_error` publish) is an M6.4 obligation. |
| **M6.2 — secret store + per-scope key-mgmt** | `ScopeKeyManager` (KEK/DEK, `scope_keys`), the `PayloadCipher` seam (E2 bridge), shared machine-local root, `SecretStore.setAll` (AMD-68) | Doc 15 ✓ + AMD-68 ✓ (RATIFIED 2026-06-09) + M6.1 | **✅ COMPLETE 2026-06-11.** Executed on `62a81e6` (G1–G8 PASS; zero-new-edge BOTH directions; manifests untouched 55/24/36); **gate GREEN (82 targeted + full `./gradlew check` 147) 2026-06-11, zero gate-fix rounds; PM WUCP Phase 2 APPROVE; COMMITTED `7c73c91`** (reconciled 2026-06-11 EOD). Delivered: `ScopeKeyManager`/`StandardScopeKeyManager` (lazy 0400 `.root-key`, RFC-5869 `Hkdf`, wrapped DEKs in config-side `scope_keys.json` [R6]), `StandardSecretStore` (`secrets.enc` GCM envelope under the `config_secrets` DEK; AMD-68 `setAll` + AMD-16 rotation), `!secret`/`!env` stage-3 in load+reload, E2 bridge (`PayloadCipher`/`EncryptedPayload` held-not-consumed; `Main.payloadCipher` adapter; `HomeSynapseCore` 5-arg ctor). **R-1** (fail-closed tag-bearing `write()` → NEW FUTURE-AMD: tag-preserving emitter) / **R-2** (`PersistenceFactory` = M6.3 wiring) / **R-3** (DP-6 = factory+ctor+bridge-test) **ruled ACCEPT**; 10 [INFO] accepted. _Prior:_ **DISPATCHED 2026-06-11 ([R6] CONFIRMED at dispatch; R1 done in M6.4; baseline `62a81e6`).** _Prior:_ **INSTRUCTION AUTHORED 2026-06-10 — ISSUE-READY (gate FULLY satisfied: AMD-68 ✓ + M6.1 ✓): `context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`.** CARRY 1 E2 bridge embedded verbatim; zero-new-edge re-verified at authoring (all three module-infos embedded); folds Research 13 REC-140; ⚠ [R6] `scope_keys` = config-side file (Nick veto at dispatch). PM sequencing rec: executes AFTER M6.4 (its G7 gate adapts). |
| **M6.3 — at-rest write-path encryption** | sensitive-PII categories encrypted-on-write (Doc 15 §3.4/§4) | **✅ COMPLETE 2026-06-13 — LANDED GREEN.** Full `./gradlew check` GREEN (147 tasks) incl. the §13.4 OR-M6-NONCE kill-mid-encrypt nonce-monotonicity test; PM WUCP Phase 2 → APPROVE; 2 gate-fix rounds resolved (R1 compile: test-only checked-`SequenceConflictException`; R2 regression: V005 trailing-comment migration idiom broke all DB-init — fixed). `encrypted_scopes` `[identity, presence_personal]` carried verbatim. **COMMITTED `1eddd9a`** (working tree clean; M6 now 4-of-4 COMPLETE). | **DONE.** [REVIEW] identity-scope ruled ACCEPT (zero MVP event types — reserved per Doc 15 §3.4; `presence_personal` ← `EventCategory.PRESENCE`, a clean total map). OR-M6-NONCE write-path half DISCHARGED (durable `scope_nonce_counters.json`); restore half → R-α (M6.3 = DP-D non-preclusion only). §13.4 nonce-monotonicity test was the done-gate — met. **NEW carry pin (OQ-15-2, 2026-06-12): the cold-start finding** — first ~100 encrypts post-JVM-start cost 80–700 µs (C2 compiling the software-AES path); weigh a JacksonWarmup-style cipher warmup at scoping. **M6.3 instruction carry pins (from M6.2, 2026-06-11):** the random-IV(wrap)/counter-nonce(payload) fence; key versions never reused past destroyed rows (`dek_ref` stability — test-pinned); the **HKDF zero-salt decision** (RFC-5869 2-arg form per Doc 15 §4.2 — PIN in the M6.3 Doc-15-touching amendment before Tier-2 passphrase re-derivation); the R-1 fail-closed `write()` posture (tag-preserving emitter = FUTURE-AMD, REC-138 family); `PersistenceFactory` extension = the M6.3 wiring step (R-2). |
| **M6.4 — hot-reload atomic swap** | atomic `ConfigModel` swap; AMD-66 listener invocation; `config.section_reloaded` (AMD-70) | M6.1 ✅ | **✅ COMPLETE 2026-06-11.** Executed against `7e0bce8` (G1–G8 PASS; [R4]/[R5] applied as confirmed); ⟳ gate-fix round 1 = GF-1 only (the M6.1a-era `ConfigLayoutTest` publish-count pin 1→2 per the ratified R1/DP-10 — implementation verified contract-correct, untouched); **full `./gradlew check` GREEN (147 tasks) 2026-06-11; PM WUCP Phase 2 APPROVE; COMMITTED `62a81e6`** (sha reconciled 2026-06-11). R-A/R-B/R-C/R-D ruled ACCEPT; `CONFIG_SECTION_RELOADED → [SYSTEM]` confirmed; survey-lesson logged (publish-count pins category). Delivered: reload()/write() un-staged, atomic swap, AMD-66 matrix, `ConfigModelDiff`/`AtomicYamlWriter`/`SchemaAnnotationIndex`, R1+R2 load-path obligations, `ConfigSectionReloadedEvent` full fan-out (54→55/23→24/35→36), `ConfigChangeSet` filters. |
| **App-Bootstrap** (carries core-review C1+C2+C9) | composition root: `main()` wires `HomeSynapseCore`; auth surface; cipher activation; lifecycle reconcile | M6 ✅ (crypto), persistence (M2) ✓, lifecycle (M2.8) ✓ | **CHARTERED + DECISION-GATES RULED 2026-06-18 (A1-A4); awaiting mechanical residual (CC-1 run · R-γ return · AMD-94 ratify) before first coding instruction.** Independent of M7; parallel. | The runnability unlock — turns the built-but-inert system runnable; **the two HIGH (C1 unauth HTTP, C2 read-decrypt) detonate here at one instant (Seam 1).** Charter: `context/planning/2026-06-15_app-bootstrap_charter.md`; decisions: `context/decisions/2026-06-18_app-bootstrap-and-superiority-scope_decisions.md`. Issue order: **AB-3 first**, then **AB-1+AB-2+AB-4 together**. |
| **AB-1 — auth + bind posture (C1/INV-SE-02)** | `AuthMiddleware` on every route incl. `/internal/*`; `RateLimiter`; bind posture | app-bootstrap charter | **✅ DONE 2026-06-20 — gate-verified GREEN (`./gradlew check` 149 tasks); PM WUCP Phase 2 APPROVE.** REST auth shipped: `StandardAuthMiddleware`/`StandardRateLimiter`/`OpaqueTokenStore` (opaque bearer tokens, JDK-native SHA-256, **zero new catalog dep / zero new module edge**), catch-all `installAuth` filter over `/api/*`+`/internal/*` with auth-before-path-canonicalization, loopback-default bind (`HomeSynapseConfig.bindHost`), first-run pairing token → `config/initial_api_token`+WARN; `opensHttpOnlyBehindAuth`+`httpSurfaceBindsLoopbackOnly` flipped/added. **WS first-message auth DEFERRED → the new WS Phase-3 runtime WU** (`api.ws` was Phase-2 scaffold — no live `/ws/v1` to harden; criterion #3 re-homes there). Closes OR-APP-AUTH/C1 for REST. [REVIEW] `ApiKeyClaims` (A2 enterprise claims hook) + `bindHost` 5th record component + `Main` HTTP-messaging-on-an-AB-4-file ruled **ACCEPT**. Gate-fix round 1 = AB-2 test (below). Core commit message handed to Nick (SHA reconciles next step-0) | **A1 RULED: loopback-default**, LAN = explicit authenticated opt-in, never bind-all (CVE-2026-34205 class). **A2 RULED: token issuance**; WS authenticated; zero-config stays authenticated (no shared bootstrap secret, no pre-auth enumeration); **carry the enterprise per-scope/per-site-claims hook**. CC-1 = confirm-mechanics gate on the instruction. Auth-before-path-resolve (canonicalize first, CVE-2023-27482). |
| **AB-2 — read contract, fail-closed half (C2/F-A1)** | fail-closed read path + the degrade *seam* (`DegradedEvent` type, `(scope,key_version)` lookup) | app-bootstrap charter; R-α | **✅ DONE 2026-06-20 — gate-verified GREEN (`./gradlew check` 149 tasks); PM WUCP Phase 2 APPROVE.** Typed fail-closed `PayloadDecryptionException` in the `SqliteEventStore` read path (**per-read-batch scope**, CASE-a key-absent / CASE-b GCM-auth classification); whole-batch abort retained (A3 MVP posture); degrade seam (`DegradedEvent`, `(scope,key_version)`) **designed-not-wired** per F4. 3 fail-closed tests. **Gate-fix round 1** = the recurring M6.3 checked-`SequenceConflictException` test-declaration trap (declarations-only fix, sibling-test pattern; no logic touched). Resolves OR-RF-DECRYPT/C2 at MVP; graceful-degrade *behavior* → the crypto-shred WU (gated on `chain_hash` liveness, F4) | **A3 RULED: fail-closed at MVP** (distinct loud error); design degrade seam now, degrade *behavior* ships with crypto-shred WU; carry rotate-DEK-on-restore boot invariant. **F4 pin:** degrade/chain-validity disabled until `chain_hash` + startup verification live (ZERO today). |
| **AB-3 — lifecycle reconciliation (C9/B-M1)** | reconcile `SystemLifecycleManager` vs `HomeSynapseCore`; openHAB numbered startlevels; health loop + watchdog wiring; pre-migration snapshot/rollback | app-bootstrap charter; Doc 12; R-δ AX-2 | **✅ DONE 2026-06-19 — gate-verified GREEN (`./gradlew check` 149 tasks, clean re-run after the WUCP Phase 2 fixes); PM WUCP Phase 2 → APPROVE. Committed core `60d50ce` (pushed). Instruction `context/instructions/2026-06-19_AB-3_lifecycle-reconciliation_coding-instruction.md`** | Adopt openHAB startlevel ladder; gate cipher activation + HTTP exposure to specific phases (HTTP must not open before AB-1 auth wired). Lifecycle wiring test pin. **RULINGS PINNED (Nick, 2026-06-19): (PD-1 API-shape) `HomeSynapseCore` IMPLEMENTS the Locked `SystemLifecycleManager` interface — implement, do NOT delete (retiring it is a Doc 12 amendment, never a silent code delete); (PD-2 registry-home) the three minimal `InMemory*Registry` impls live in `core:device-model`, marked MVP-substrate (start-empty, no AMD-44 breadth). Source-verified contained fold: only `StandardSelectorResolver(Entity,Area,Device)` consumes registries — Floor/Capability NOT needed.** **Scope must resolve the device-model registry dependency up front** (fold minimal Entity/Device/Area production registries, or sequence that device-model slice just ahead — AMD-44/Research-8; never a silent block); **M7.2a = fallback** only if this prep stalls. |
| **AB-4 — `payloadCipher` activation + envelope-finalization gate** | `ScopeKeyManager→PayloadCipher` adapter into read+write path; F1/F2/F3/F13 one-way doors | app-bootstrap charter; Doc 15 §3.8 | **✅ UN-GATED 2026-06-19** (AMD-94 RATIFIED + folded into Locked Doc 15 → the F1 envelope-version slot is reserved with `v1` pinned, watermark AMD-94; the AB-4 rows are already written in the Seam-1 instruction; ready for a Coder slot) | **A4 RULED: 1-byte envelope version tag IN now** (v1 = current envelope; reserve before first write); F2 scope width `[identity, presence_personal]` (resolved); F3 one nonce-construction-per-scope; F13 write-path fatal dir-fsync. Backup-key-portability *seam* decided here (R-δ AX-4). **Doc 15 §6 amendment = AMD-94 — RATIFIED + folded 2026-06-19** (rotate-DEK-on-restore + the 1-byte envelope version tag; review return `context/audits/2026-06-19_AMD-94_DOCS_Review_Return.md` = RATIFY-WITH-EDITS, E-1 boot-invariant restore⇒rotation-only + E-2 encoding-neutral discriminator folded, §8.1 rider kept; AMD-94-INV-01/02 registered §48; watermark AMD-93→94, invariants 165/48; OR-M6-NONCE restore-half CLOSED). |
| **WS Phase-3 runtime + auth** (surfaced at AB-1 WUCP Phase 2) | build the WebSocket runtime (`WebSocketHandler`/`MessageCodec`/`EventRelay`/`WebSocketLifecycle` + `app.ws("/ws/v1",…)` wiring + the `io.javalin`/`jackson`/`event-bus`/`state` module edges into `api.ws`), THEN WS first-message auth (4403/4408) on the **already-built** shared `OpaqueTokenStore`/`AuthMiddleware` | AB-1 (shared auth types) ✅; event-bus (M3); state-store (M4) | **NEW 2026-06-20 — `api.ws` was Phase-2 scaffold** (interfaces/records only: no handler/relay/codec/lifecycle impl, no `/ws/v1` wiring, zero runtime module edges); the Seam-1 instruction over-assumed a live WS surface. **Not a security gap today** (no WS surface exists). Carries AB-1's success-criterion #3 + INV-SE-02-for-WS. Sequence near M11 (WebSocket API) or sooner if a WS client lands first. | The auth substrate is done + unit-tested; this WU is the WS runtime itself + wiring the existing auth into the upgrade handshake. |
| **Doc 16 — Superior-automation Phase-1 design doc** (precedes M7.2/M7.3) | NEW Locked design doc, 3 first-class sections: expressiveness-without-DSL + explainability/causal-chain + **run-coupled reliability**; + federation (identity/scoping) seam + hybrid cut-line | Doc 07/12/14/15; strategy layer | **SCOPE RULED 2026-06-18 (Part B); Architect-mode authoring DISPATCHED** (`context/handoff/archive/2026-06-18_automation-superiority_Phase1-design_session_prompt.md`, RULED SCOPE folded) | Designed Phase-1 **before** M7.2/M7.3 so the build targets the differentiator. **M7 verdict: M7.1 UNAFFECTED (ride-along cleared) · M7.2 SHAPED (builds into it, don't freeze first) · M7.3 UNAFFECTED.** Decision record carries the full ruling + Nick's reliability-split refinement. |
| **Cross-cutting reliability-as-a-product-property doc** (candidate; co-equal, sequenced right after Doc 16) | NEW doc: multi-year longevity, observability-as-product, system-wide self-healing — reliability as a *provable buyer property* | Doc 12 lifecycle, persistence, observability (M12) | **SCOPE RULED 2026-06-18 — own co-equal doc (Nick's B-1 split); author after Doc 16** | **NOT M7-gating** (so it follows without stalling the engine). Split out of Doc 16 to avoid the M4-retrospective "epic hidden under one label" failure; reliability stays co-equal in the thesis. |
| **B3 federation / enterprise architecture doc** (candidate; post-M8) | NEW post-M8 architecture doc: multi-site/MDU/hospitality; hub-of-hubs; cross-site policy + per-site autonomy | M7/M8 (single-box engine real first); Doc 14 | **SCOPE RULED 2026-06-18 — own post-M8 doc; Doc 16 reserves only the identity/scoping seam** | Implies **new invariants** (cross-site boundaries, federated identity, WAN-partition autonomy) — Nick's Phase-1 call. "Single runtime, not a fork" (strategy); MVP builds structural preconditions only. Authoring premature before the engine exists. |
| **C1 honest-hybrid deployment doc** (candidate; with app-bootstrap) | NEW tight doc: local/remote boundary + never-a-dependency invariant (NOT the full cloud-accelerator feature) | app-bootstrap charter (composition root); persistence (C2 sync) | **SCOPE RULED 2026-06-18 — own tight doc, sequenced with/after app-bootstrap; seam pinned in the composition root** | Brand-critical boundary ("cloud-optional, never a dependency") — given its own doc rather than buried as a charter constraint. Full cloud-accelerator feature defers. |
| **M7** | Automation Engine (core) | event-bus (M3), state-store (M4), configuration (M6) | **M7.1 ✅ DONE 2026-06-18 (gate-verified GREEN, `./gradlew check` 149 tasks); M7.2 SPLIT per P1 → M7.2a (baseline-now) + M7.2b (Lock-gated); M7.3 follows — see rows below** | **M7.1 = trigger/condition path — LANDED** (12 trigger / 7 selector permits, 4 service impls, definition loader, AMD-92 event slice rows 1/3/11–16/19 + full manifest fan-out; type-residency holds). **Gate-fix round 1:** `timer.automationId`→`entry.getKey().automationId()`; **`core:automation→config` forbidden-edge relocated** (`AutomationConfigBridge` deleted, schema → config-free `AutomationSchema`; config wiring rides app-bootstrap). **FIX-07 instruction defect corrected** (config half wrong; event.bus half fine). **Subscriber + config + lifecycle-wiring deferred to app-bootstrap AB-3** (the composition root assembles no ConfigurationService/registries yet). Instruction `context/instructions/2026-06-15_M7.1_...`. **M7.2** (run/action/dispatch; AMD-91 RunCausalChain, AMD-92 rows 2/4–10/17/18) **builds *into* Doc 16** — don't freeze its action contract first; C3 already landed `03f16dc`. **SPLIT per P1 (≈5 sub-pieces) into M7.2a/M7.2b — see rows below.** Plan: `context/planning/2026-06-19_M7-forward_plan.md`. |
| **M7.2a — Run lifecycle + causal chain + dispatch** | `RunManager` FSM (§3.7; C2 dedup, C3 ordering, §3.6 modes, §3.7.1 cascade gov); AMD-91 `RunContext`→`RunCausalChain` swap (F4); `ActionExecutor` exec mechanics (§3.9, VT, `UnavailablePolicy`); `CommandDispatchService`+`CommandValidator` (§3.11.1); AMD-92 event slice rows 2/4–10/17/18 + manifest fan-out/survey; run-trace §4.2 | M7.1 ✅; builds against injected deps (wiring→AB-3) | **SPLIT 2026-06-21 (Nick, P1) → M7.2a-1 ✅ DONE GREEN + M7.2a-2 ✅ DONE GREEN 2026-06-21.** **M7.2a-1 (run lifecycle) ✅ DONE 2026-06-21 — gate-verified GREEN (`./gradlew check` 149 tasks; one spotless-fix round); PM WUCP Phase 2 → APPROVE.** `context/instructions/2026-06-21_M7.2a-1_run-lifecycle_coding-instruction.md`: `RunCausalChain`+`ChainLink` (AMD-91), `RunContext` field-7 swap, the run-lifecycle event sub-slice (rows 2 reshape + 7/8/10/17/18 → EventTypes 62→67, roster 32→37, category 44→49), the `StandardRunManager` FSM (C2 dedup, C3 comparator, §3.6 modes, §3.7.1 cascade depth+chain-cycle, auto-disable, REPLAY-zombie INTERRUPTED) against injected `ActionExecutor`+`RunConditionGate` stubs. Closes the M7.1 C1-interim hold. **Accepted deviation: condition-before-mode** (Coder honored the locked §3.6 contract over an instruction-sequence defect — pm-lesson logged). **M7.2a-2 (execution/dispatch) ✅ DONE GREEN 2026-06-21 — PM WUCP Phase 2 APPROVE (gate `./gradlew check` 149 tasks; gate-fix round 1 = two record component/static-factory collisions [`ValidationResult.valid()`, `ModeDecision` factories], minimal-diff; counts re-derived 71/41/53; module-infos UNCHANGED; the four DP-A carries landed + contract-tested)** = `ActionExecutor` + `CommandDispatchService` + `CommandValidator` (new) + `ConflictDetector` + run-trace §4.2 + AMD-92 rows 4/5/6/9 (+`EvaluatedEntityState`/`ConflictEntry`; EventTypes 67→71, roster 37→41, category 49→53). **2a-2 MUST also carry (from 2a-1 closeout):** (a) QUEUED's true sequential single-flight drain (currently admission-equiv to PARALLEL — must not ship as PARALLEL); (b) feed the triggering `CausalContext` to `RunConditionGate` (or publish row 4 from the FSM) for the row-4 chain; (c) move the dedup claim before the gate (avoid double row-4 under a race); (d) wire the real snapshot position + action/command tallies into `RunContext`/`automation_completed`. | Doc-16-independent. `initiateRun` carries the Doc-16 computed-param-resolution as a design-not-precluded seam (M7.1/M6.3-DP pattern). Folds the 3 M7.1 behavioral-default carry-items (Doc 07 §3.8 multi-entity-condition clarification; `EventTrigger.payloadFilters`; device-subject `entityRef` flattening) — into 2a-2. | Doc-16-independent. `initiateRun` carries the Doc-16 computed-param-resolution as a design-not-precluded seam (M7.1/M6.3-DP pattern). Folds the 3 M7.1 behavioral-default carry-items (Doc 07 §3.8 multi-entity-condition clarification; `EventTrigger.payloadFilters`; device-subject `entityRef` flattening) — into 2a-2. |
| **M7.2b — Action-model contract freeze** | computed-param resolution at run-init (Doc 16 §3.2) into the M7.2a seam; run-coupled-reliability terminal contract (Doc 16 §3.4 — deterministic terminal + recorded reason; AMD-92 `failureReason`/`abortReason`; AMD-90 no-retry); the D2/REC-162 disposition | M7.2a; **Doc 16 Locked** | **✅ DONE GREEN 2026-06-22 (first pass, zero gate-fix rounds; PM WUCP Phase 2 APPROVE) — action model FROZEN: minimal `ComputedValue` (3 permits) resolved at run-init + run-coupled fail-closed-read terminal + no-retry frozen; no new event type (71/41/53), module-info unchanged; 3 [REVIEW] deviations ACCEPT (package-private types / `AggregateValue` Set-not-Selector / `DegradedAttributeValue` typed-absent)** (D2/REC-162 RULED 2026-06-19 + co-signed 2026-06-21: keep anti-retry default; the differentiator is command-outcome visibility `dispatched→confirmed|unconfirmed|failed`; retry post-MVP, device-class-gated) | The action-model freeze that must not precede Doc 16 Lock (I1). M7.2-scoping fact-check: confirm the verification half is already covered by `Expectation`+AMD-90. |
| **M7.3 — Pending Command Ledger** | §3.11.2 FSM (`PendingStatus` 5 states), `Expectation`→`state_confirmed`, deadline/timeout, `CommandIdempotency`+EXPIRED-on-restart, coalescing DISABLED; DQ-3 | M7.2a (`CommandDispatchService`) | **✅ DELIVERED GREEN 2026-06-22 (`./gradlew check` 149 tasks; PM WUCP Phase 2 APPROVE) — `StandardPendingCommandLedger` + assembly + 2 test classes; FSM/correlation/timeout/REPLAY/idempotency/OPTIMISTIC-bypass unit-tested vs constructed envelopes; AMD-92 ZERO mints (counts stay 71/41/53), no module-info change; 6 source-drift [REVIEW]s ACCEPT (built against the frozen tree). LIVE composition-root wiring DEFERRED → M7.4 (a first attempt leaked the subscriber's SQLite read connection → @TempDir/Windows teardown failure; reverted, `HomeSynapseCore` unchanged). Core SHA reconciles at Nick's M7.3 commit.** | Doc-16-independent; D2/REC-162 does not touch it. |
| **M7.4 — Command-pipeline live integration (bootstrap)** | wire the engine end-to-end in the running `HomeSynapseCore` per the §1-ruled **event-driven, co-located** shape (D1): the `command_issued` producer (off `StandardActionExecutor`'s dispatch), **`command_dispatch_service` as a co-located SUBSCRIBER on `command_issued`** (replacing the in-process call) + `pending_command_ledger` as LIVE subscribers WITH `stop()` teardown, the `pollExpirations()` `SharedScheduler` tick; **+ an E2E integration test** (boot the real composition root, fire a seeded automation, assert command issue→confirmation + the causal chain is queryable) **+ the D2 pure-function-replay CI test**. D3 additive event-versioning + D4 retention-aware design ride in as constraints. | M7.3 ✅; folds the M7.1 deferred wiring; **§1 decision record 2026-06-25 (D1–D4)**; **AMD-95 RATIFIED 2026-06-26 (the §3.11 reconciliation — gate CLEARED)** | **SPLIT 2026-06-26→27 (P1) into M7.4a/b/c/d.** **M7.4a/b/c ✅ ALL DONE GREEN 2026-06-27** (v8 beats 14/17/19; counts hold 71/41/53; module-info unchanged): **M7.4a** = `command_issued` producer + co-located `command_dispatch_service` subscriber (`b85e1ed`); **M7.4b** = run pipeline live producer-side — RunManager/executor/serializer wired + trigger→run handoff (`69cc6e2`); **M7.4c** = confirmation live — `pending_command_ledger` + `pollExpirations()` tick + the E2E composition-root confirm gate `RunPipelineConfirmWiringTest` (`db8ab5f`). **OR-M7-WIRING RESOLVED** (the pipeline confirms end-to-end). _Deferred gate (OR-GATE-M7.4, pm-handoff Open Risks): confirm `./gradlew check` + CI green on the pushed `db8ab5f` (§4b)._ | **M7.4d (the only remaining M7.4-arc item, NEXT):** the **D2 pure-function-replay CI test** wired as a gate (a seeded-log replay asserting ZERO dispatch/run/confirm side-effects — the V2-A `spike/dispatch-latency-and-log-growth/` harness template) **+ register D2's canonical INV-id** via review→ratify (the §1-reserved fold; AIOT-INV-1 already cites D2 as pending). Safety/governance hardening, not new functionality. **✅ DONE GREEN 2026-06-27 (v9 hub; WUCP Phase 2 ACCEPT) — the M7.4 arc is COMPLETE; committed `4e31aed` (2026-06-27; `RunPipelineReplaySafetyTest` +504 / lifecycle MODULE_CONTEXT +2, test-only).** Built `RunPipelineReplaySafetyTest` (4 methods; instruction `context/instructions/2026-06-27_M7.4d_d2-pure-function-replay-CI-gate_coding-instruction.md`): a composition-root seeded-log replay asserting ZERO dispatch/run/confirm side-effects, **non-vacuousness proven empirically** by the LIVE positive control passing in the same green run. `./gradlew check` GREEN (149 tasks, first pass); **no production change, `module-info` unchanged, counts 71/41/53, `Architecture_Invariants` untouched.** 3 `[REVIEW]`s all ACCEPT. **Residuals:** the **D2 canonical INV-id** co-sign (review→ratify; proposal `context/instructions/2026-06-27_D2-replay-invariant_registration-proposal_review-dispatch.md` staged) + the CI-on-commit confirm (OR-GATE-M7.4). **Next Core slot = the thin causal-read-API.** |
| **M8** | Automation Engine (advanced) | automation core (M7) | PLANNED | Command ledger full; cascade governance; concurrency modes; AMD-17 orphan-detection interaction |
| **M9.1** | Integration spine — supervisor + command routing (the M9 epic split per the beat-46 DP-A ruling) | AB-4 ✅ · AMD-97 ✅ (docs `1509b34`) | **✅ DONE 2026-07-02 — core `ec2e3b4`, ci.yml ✅ + install-smoke ✅ on the push (v14 beats 51–52). Status cell flipped 2026-07-04 at the v19 currency pass — it had sat at ROUTED after the landing (preflight Check-4 catch).** | Instruction `context/instructions/2026-07-02_M9.1_integration-spine_supervisor-and-command-routing_coding-instruction.md` (hub-audit corrected) + `context/pre-verifications/WU-M9.1.md`. HEAD moved `52824e9`→`e3d7296` (M7.5c-a, disjoint from the read-set) — re-run the STOP gates at dispatch per the instruction's baseline clause. Done-when: the charter, `context/audits/2026-07-02_M9-authoring-lane_return.md` §3. |
| **M9.2** | Transport + EZSP protocol (hardware-facing; EZSP-first per DP-C) | M9.1 | ✅ DONE 2026-07-03 — core `6ea6912` | Landed clean-tree `check` GREEN ×2 (152); R1–R7 ACCEPT; H1–H5 hardening; checked-PIE seam rule + classifier-wrap trap recorded (M9.4-binding). WUCP P2 v15 beats 58–59. |
| **M9.3** | Interview, reporting config, ingestion, profiles (fixture-heavy) | M9.2 ✅ · Doc-18 Lock ✅ `fb91c23` · bench corpus `5ceff3b` | **✅ DONE 2026-07-04 — core `c856eab` (75 files, 10811 insertions), `check` GREEN 153 ×(pre+post-clean) + CI GREEN on the push (the gate of record)** | §1 realized (DeviceProfile 9→10 with AMD-97's eight fields verbatim; sealed MatchCriteria all-three/two-implemented; namespace convention); §J gate PASSED (both fixtures replay green rendering recorded confirmability); zero-mint 71/41/53 held; all five [REVIEW]s ruled ACCEPT (queue-scheduled retry · endpointless-ISE · 4-field DeviceDiscoveredEvent + "unknown" sentinel [richer payload = QUEUED event-model AMD candidate, M9.4/M10] · DeviceRegistry ctor-injection [M9.4 wires the instance] · stub-test update); change set 75 paths (9 M + 66 new; 41 prod types = 10 public §1 + 31 package-private). Rulings + audit: pm-handoff v17 beat-3. | **Doctrine (beat 65): freeze the schema, defer the behavior.** Dossier §A–§K binding; DP-1 `confirmation[]` 9→10 via a discrete Mode-2 AMD (AMD-97 fields verbatim) at the TOP; DP-2 sealed `MatchCriteria` all three variants defined, two implemented; `WithinTolerance`; P2 ctor fan-out sweep (`new DeviceProfile(` sites); done-when = Wave-1 fixtures replay green rendering recorded confirmability. Rulings: pm-handoff beat-65. |
| **M9.4 + M9.5 (the acceptance arc)** | Command path + confirmation acceptance + hero E2E + durable identity — the moat lands, **the thesis run** | M9.3 ✅ | **✅ DONE + CERTIFIED-CLOSED 2026-07-14** (certified-into-soak 2026-07-10) — the full arc landed CI-green (M9.4-PJ/TCJ+§B/NCFG/ADP/RPT/KEY/KEYb + M9.4a/M9.4b + **M9.5-DUR `ef1d2c5` [AMD-99: registries = projections of the log]** + **M9.5-DURb `04f5f70`**); silicon iterations 1→5b all proven (the join arc · reporting · TCLK · the DUR proof · KEYb); **THE ACCEPTANCE RUN 2026-07-09/10 PASSED §51 core legs** (supersession probe · identify immediate-honest 81 ms · brightness 0.33 s · IAS twins absorbed · timeout honesty ×3 organic classes · **ZERO false CONFIRMs, ~25 verdicts** · identity held across THREE networks incl. the ch22 hijacking incident + the restore-path first silicon rep); **THE 72 h SOAK ENTERED 07:50:35 Pi-local → COMPLETED; THE CERTIFICATION CLOSED 2026-07-13/14 at the v31 exit intake** (spike ×3 green + one attributed operator rep · **~1,728 cumulative recorded verdicts, ZERO false CONFIRM** · the close-out block appended to the bench acceptance record). | Evidence layer: the bench record’s iteration log + acceptance-run block (bench `a81d678`, + the 2026-07-10 root-cause correction block). Rulings: pm-handoff v22–v27. One CONFIRMED defect → the M9.6-RO row below (root re-attributed 2026-07-10). **This row compacted 2026-07-10 (v27 beat 2) from the 23 KB mega-row — full per-WU detail in git history (`git log -p`) + the archived handoff beats.** |
| **NQ-6-EXIT** | The soak-exit restart spike ×3 — the DUR+DURb proof on 72 h-aged state | the 72 h soak (ends ≥ 2026-07-13 ~07:50 Pi-local) | **✅ DONE 2026-07-13/14 — the exit spike ran ×3 VERBATIM, all green (+1 attributed operator rep); THE CERTIFICATION CLOSED at the v31 intake** | Evidence delivered ×3: relinked ×2 (same deviceIds) + zero proposals + the rehydration INFO + both 5b ULIDs API-to-API. **Instrument correction of record (hub-owned):** `projection_live` position is the registry projection's TYPE-FILTERED checkpoint (lawfully 14 — the DURc lesson; the “well beyond 14” expectation superseded); the aged-replay stake rode `meta.viewPosition` 13,790 → 13,958/14,003/14,007. Close-out block: the bench acceptance record (2026-07-13/14). |
| **SEED-CUSTODY** | Hashed-TCLK generated-seed custody (SD-5) | soak exit — **lands before ANY non-bench network** | **CODE-LANDED `a1f0f77` CI-GREEN (M9.6-SEED full-bar ACCEPT, v28) — the SD-5 HARD GATE discharges ONLY on the silicon leg: ONE fresh formation under the generated seed (composes with the Wave-2 joins; Nick rules timing at the soak close-out)** | The hijacking incident STRENGTHENED the case. Charter carried in the coder-handoff carry-list + pm-handoff v21+. |
| **M9.6-RO** | Reopen-target locator — the identity-CAPTURE fix: fix-the-capture (canonicalize → re-scan → capture the REAL identity) + the pinned-only-path sentinel rule + the PortIdentity unknown-representability fix | soak exit · B1 (the regression harness) | **CODE-LANDED `1627978` + R1 `3590fca`, CI-GREEN (two-layer ACCEPT v28; the R1 by-id-pin record correction + mutation-proof, v29) — FIXED-PENDING-SILICON: the R-row closes on B1's usb-reenumeration scenario + BOTH topologies (Rosonway arrival; dongle-behind-hub = its own single-variable step)** | Evidence: the bench record’s correction block + pm-handoff v27 beat 1 (agent read + hub L2 at `04f5f70`). **Verify THROUGH the B1 runner’s USB-power-interruption scenario (ruled coupling).** Couples HARD to UDEV-RAMP (the alias best practice IS the defect window). Red test = the alias-config capture path; the devnum-immunity property gets its own PIN test. Promiscuous reopen-re-resolution REJECTED (foreign-stick config-batch hazard). |
| **AVAIL-ALIVE** | Wire the EXISTING StandardAvailabilityTracker: construct at initialize() · onFrame→recordFrame · runCycleOnce→evaluateTimeouts + the mains read-ping arm · per-entity TransitionListener publish (CRITICAL offline / NORMAL online) | soak exit; INSIDE the explainability push (the FE-7 data dependency) | **CODE-LANDED `1aa809d` CI-GREEN (two-layer ACCEPT v29; the adoption-time-seed [REVIEW] RATIFIED-ACCEPT hub-owned; mechanism-without-driver #6 CLOSED; suite 452→466) — silicon legs = Monday: SNZB-02P reports→AVAILABLE + a genuine offline leg, never-false-ALIVE both directions** | **VERBATIM instruction watch-outs (Nick-ruled 2026-07-10):** “the M-1 sidecar-init/edge-trigger suppression trap” (⇒ EMPTY persisted map in the first WU) · “the load-bearing mains read-ping arm — never-false-ALIVE in the other direction”. MUST-NOTs: never TX-accept · never NCP keepalives · never the join-handler pins · never emit at construction. Authoring reads owed: ZigbeeAdoptionSlice.relink emission subject/payload · ZigbeeDeviceCache sidecar · command-success semantics · DerivationRule reaction. |
| **EXPLAIN-PUSH** | The explainability/observability arc: the explainability-lane return audit → FE-7 hero on REAL verdicts · AVAIL-ALIVE · the no-change explanation requirement · the verdict-detail read grounding | soak exit · the FROZEN v1.1 contract (grounding questions, never improvised shapes) | **ELEVATED = stack #3 (ruled 2026-07-10)** | **RULED constraint (verbatim): “the explanation NEVER upgrades or replaces a verdict — it contextualizes it … alongside the honest timeout”** — a named requirement beside the StandardExplanationService flattening row. Supersession-payload RULED (b): the read-API is the sanctioned decrypt path; “if a §51-class assertion needs an event the frozen v1.1 surface doesn’t expose, that is a contract conversation brought to me — never a raw-SQLite fallback inside a scenario.” |
| **BENCH-AUTO B0–B5** | The scenario library over the verdict-stream oracle: B0 charter + SCENARIO_FORMAT · B1 runner v0 (`bench.sh scenario/suite/bundle` + 3 seeds) · B2 the §51 port · B3 nightly bench-CI + the morning digest · B4 the labeled-tuple corpus · B5 advisory-only learning (AIOT-INV-1) | B0 ✅ · B1+ post-soak | **B0 ✅ DONE 2026-07-10 (the charter + format spec live in the bench repo); B1 = the first post-soak bench WU — it delivers the re-enumeration scenario BEFORE M9.6-RO’s verification (ruled coupling)** | Rulings 2026-07-10 (Q1–Q5): home = the bench repo; **the product-surface crossing (→hsctl) is a named STOP**; nightly once B3 exists + the one-line digest; the FULL suite runs pre-milestone as a gate; API-first assertions (coheres with the supersession-decrypt ruling). |
| **HS-RADIO** | First-party radio-ops CLI on OUR EZSP stack: info / leave / custody-restore + a hard port-discipline guard (refuses while the app owns the port) | soak exit; field-hardening block | **CHARTERED 2026-07-10 (ruled yes) — bench-tooling tier** | The product-surface crossing is a named STOP (the bench.sh→hsctl boundary rule). Kills the pip-install-archaeology recovery class (the ch22 incident’s worst tooling gap; the patched-bellows recipe is the stopgap of record). |
| **FRAME-CTR** | Restore-path NWK frame-counter custody: persist the outgoing high-water; bump on restore | **sequenced AFTER M9.6-RO (ruled)** | **RULED row 2026-07-10** | Custody schema version + fence tests; the zigpy jump precedent; its bench proof composes with the restore-path scenario (B2). |
| **CMD-DEADLINE** | Command-level dispatch deadline → honest late-shed verdicts (serialization shape C; ordering untouched) | field-hardening, **after M9.6-RO (ruled)** | **RULED row 2026-07-10 — HOL CONFIRMED at adapter scope (v27 beat 1)** | The ledger double-verdict hazard MUST be checked at authoring (the ledger owns timeout outcomes — AMD-90-INV-01 exactly-once; seam located: core/automation). The 10.2 s empirical single-occupancy residual gets an instrument-first arm in whichever WU touches this seam first. |
| **CMD-LANES** | Per-device dispatch lanes over the shared exchange serializer + the Doc 08 §3.10 FIFO-contract AMD + a fair pipeline lock (serialization shape A) | Wave-2 / the first scene-group feature | **FUTURE (ruled 2026-07-10)** | The single-lane model is DEFENSIBLE at 2 devices; REAL at ~4–6-actuator fanout. Evidence: pm-handoff v27 beat 1 (R3). |
| **UDEV-RAMP** | The udev stable-port-aliasing guidance for field installs | **M9.6-RO (HARD coupling — lands with/after the capture fix)** | queued (coupling added 2026-07-10) | The alias best practice IS the M9.6-RO defect window — never ship the guidance before the fix. |
| **INV-CH22** | The ch22/0xFC4D foreign-network writer investigation | — (standing) | **OPEN investigation row** | Evidence banked (the bellows info dump + 6 PIE boots; the acceptance record). **Recurrence DURING the soak = live-threat signal → STOP-and-rule.** A single un-witnessed occurrence = treated as artifact; the honest position: WE DO NOT KNOW. |
| **WATCH-0xFFFF** | The unattributed `key_establishment_failed … device=0xFFFF… VERIFY_KEY_TIMEOUT` dangle | — (standing) | **WATCH (once = dangle; twice = investigation row)** | One occurrence 2026-07-10 07:39:03 (~45 s post-window; likely a duplicate TCLK retry from the Hue rejoin — honest verdict on a dangling exchange). |
| **SKILLS-CONST** | The skills-constellation DESIGN doc: specialist inventory · seam contracts · the orchestration model (**the hub stays the single router; skills never call skills sideways**) · a per-skill eval plan riding the existing benchmarks/ infrastructure | — | **CHARTERED 2026-07-10 (design only; the buildout is post-go/no-go) — this session if the window allows, else v28’s FIRST agenda item** | Nick’s session-order item 6 (verbatim: pm-handoff v27 beat 2). |
| **HW-ORDER** | Bench actuator hardware: ONE local-API WiFi smart plug (Tasmota/Shelly class) + ONE uhubctl-capable USB hub | — | **APPROVED 2026-07-10 — Nick orders NOW, separately (NOT coupled to Wave-2)** | Enables the B-tier AUTO scenarios (absent-device · rejoin race · relink-on-re-announce · the M9.6-RO re-enumeration regression). Stimulus-independence rule: never through the system under test. |
| **W2-VAL** | Wave-2 validation debt (DP-3): the adversarial sleepy-stall + OTA-fingerprint-re-characterize legs | M9.3 · Wave-2 arrival | FUTURE — de-risk in the M9.4/soak window | Ruled beat 65: both legs are expressible as SYNTHETIC seeded event logs on the existing replay substrate (pre-hardware de-risk); only the Tuya/Xiaomi codec path genuinely needs a real unit. Dossier §K/Q9/Q10. |
| **M9-deferred** | Post-hero device breadth (NOT M9.1–M9.4) | M9.4 | FUTURE | Per the ratified DP-A split — the charter's Deferred section (return §3). |
| **EXT-LADDER** (assessment R-4) | The extension trust-ladder enforcement rungs: **rung 1** = scoped registries/wrappers (`IntegrationContext` actually integration-scoped); **rung 2** = permitted-publish-types manifest enforcement + the manifest-vs-behavior test — **the named arrival point that makes EXT-INV-2's deferred second half CI-testable** (register §52 inline deferral, N-2 ruling). Carries **F-1**: reconcile the `IntegrationContext` javadoc (today it documents scoping/write-only as *enforced* while enforcement is the unbuilt rung — Doc 18's "injected unscoped/full" is the accurate claim) when the rungs land. | Doc 18 ✅ Locked 2026-07-03 (§4 rows 1/4) | FUTURE — schedules with the SDK/marketplace milestone | Rows created at the 2026-07-03 Lock pass (v17 beat 1) per the Doc-18 review F-1 flag + the §52 register-inline deferral. Evidence: dossier L-5/L-7 (manifest-as-trust-contract). |
| **M10** | REST API | state-store (M4), configuration (M6), integration-runtime (M9) | PLANNED | All 5 endpoint planes; error format; pagination; rate limiting; auth |
| **M11** | WebSocket API | event-bus (M3), rest-api (M10) | PLANNED | Subscriptions, resume, backpressure; protocol-compliant framing; `WsSubscriptionFilter` dispatch via `@EventType` registry (D-01) |
| **M12** | Observability & Debugging | event-bus (M3), persistence (M2.10), state-store (M4), lifecycle (M2.8) | PLANNED | Health aggregation; JFR recording; trace storage; causal-chain assembly; trace query service |
| **M13** | Startup & Lifecycle (full wiring) | all prior M* | PLANNED | Seven-phase init; graceful shutdown drains events; watchdog; boot-order correctness across the full subsystem graph — the first milestone that runs the complete app. Largely subsumed by M3.6 for the M3 slice; M13 extends coverage to all subsystems. |
| **M14** | Zigbee Integration | integration-runtime (M9), lifecycle (M13) | PLANNED | Adapter integration; ZCL + Tuya + Xiaomi codecs; dual-coordinator strategy (Z-Stack + EZSP per LD); device interview protocol |
| **M15** | Full system integration + performance benchmarks | M13 + M14 | PLANNED | App starts, serves APIs, projects state, fires automations end-to-end. Benchmark event throughput (>500/sec sustained), state-query latency (<5ms p99), memory profile under 50-entity simulated load per MVP §8.2 budgets |

**Ordering invariant:** The dependency graph above is the sole determinant of milestone ordering. A milestone cannot start until every dependency listed has reached its `.10` integration-test milestone (or equivalent closeout). This discipline was explicitly missing in the original Master Release Plan week-by-week ordering and is added here as a hard constraint.

**Sizing assumption:** Based on M2.x cadence (~3-hour milestones in Nick's school-day blocks, ~full-day on weekends), each major group above is expected to decompose into 3–8 individual `M{n}.{y}` milestones. The first milestone of each group (`M{n}.1`) is typically the minimal slice that makes a contract test pass; subsequent milestones round out the subsystem until `.10` (integration tests) closes it.

**Replacement for Master Plan week numbers:** The Master Release Plan's original Weeks 13–26 subsystem ordering is superseded by this skeleton for operational planning purposes. The plan remains the schedule-anchoring document for launch-date tracking; this skeleton is the execution-anchoring document for the Coder/PM loop.

---

## Notes

- Milestones are produced just-in-time: each M{x}.{y} is briefed after the prior milestone's WUCP Phase 2 completes.
- This backlog is the high-level roadmap only; the authoritative spec for each milestone lives in its coding instruction.
- Deferred-build-gate risk: if a coder-handoff defers `./gradlew check`, the PM must track it here under the milestone row until resolved.
- Intra-milestone work units (Bus-Fix Piece A, projection-checkpoint wiring, supervisor DLQ wiring) are tracked as standalone rows when they ship as independent commits, even if they don't carry a `M{x}.{y}` identifier. Each still requires a full WUCP Phase 2 
