# Week 21: May 18–24, 2026

**Phase:** P3 — Implementation (event bus + state store + integration test scaffold)
**Status:** IN_PROGRESS
**Days Remaining:** ~190

## This Week's Objective

Close out the M3 backpressure/persistence/projection cluster (M3.5b + DLQ wiring + projection-checkpoint wiring + Bus-Fix Piece A), stand up the on-device integration test framework (M3.4a), and start the M3.4b sustained-load + crash-recovery wave.

## What Actually Happened

| Date | Commit | Work Unit | Summary |
|---|---|---|---|
| May 18 | `a2aff9c` | M3.5a | StateProjection vertical slice (closed prior week, included for continuity). |
| May 18 | `fceafe8` | Bus-Fix Piece A | `DerivedWriteRateLimit` promoted package-private → public (Nick direct edit). Closes M3.5a G4 mismatch. MODULE_CONTEXT row moved from pkg-private to public table. |
| May 18 | `08d0136` | M3.5b | StateProjection production persistence. 19 files, +2,674 lines. `SqliteStateStore`, `SqliteDeadLetterStore`, `PersistentDlqWriter`, `CheckpointSerializer`, V004 DLQ indices, ObjectMapper divergence, three-way atomic write. Independent review PASS w/ 5 non-blocking concerns. |
| May 19 | `56aaa4b` | Projection-checkpoint wiring | `StateCheckpointSource` interface in state-store + 10 MB advisory checkpoint-size guardrail. Closes M3.5b non-blocking concern #1. |
| May 19 | `ed5862c` | Supervisor DLQ wiring | `SubscriberSupervisor.deliver()` constructs `DeadLetter` (11 fields), routes through `park(DeadLetter)` — dual ring + persistent writer. 12 new `SubscriberSupervisorTest` methods. Source-verification audit confirmed all 9 assumptions (A8 corrected pre-flight). |
| May 19 | `5ae7912` | M3.4a | Integration test module scaffold + harness + first 2 IT tests. New `testing/integration-tests` module (#20). `IntegrationTestHarness` wires real production stack via `PersistenceTestHarness` + `InProcessEventBusFactory` testFixtures. `BurstLoadIT` (500-event burst, 6 assertions). `HeapBudgetIT` (3,000-entity heap bound, 4 assertions). Full check GREEN; profile-gated tests GREEN. |
| May 19 | — | Cross-tier deployment audit | Closed-out audit `HomeSynapse_Core_CrossTier_Deployment_Audit.md` (workspace root). Informed M3.6 composition root design. |
| May 19 | `adf04d2` | M3.4b | Sustained-load + crash-recovery IT tests. ThrottledWriteCoordinator, Pi4SustainedLoadIT, Pi4D1SpikeIT, CrashRecoveryIT, scripts/pi4-validation.sh. 12 new tests. Both `./gradlew check` and Pi-profile tests GREEN. |
| May 19 | — | WUCP Phase 2 reconciliation | PM closeout for all 6 WUs (Bus-Fix A through M3.4b) + 2 design sessions. Hivemind brought from STALE to PASS. `testing/integration-tests/MODULE_CONTEXT.md` populated. |
| May 20 | — | M3 audit gap-closure research | Artifact 1: `homesynapse-core-docs/research/2026-05-20_M3_Audit_Gap_Closure_v1.md`. Identified and closed audit-trail gaps prior to M3.4b/M3.6. |
| May 20 | — | M3.6 Composition Root Design | `homesynapse-core-docs/design/2026-05-20_M3.6_Composition_Root_Design.md`. Authoritative design that M3.6 implements against. |
| May 20 | `17c40b6` | M3.6a | Profile-driven persistence configuration. DeploymentProfile 3→6 fields, LockingMode enum, PersistenceConfig record, DatabaseExecutor accepts profile, PRAGMAs profile-driven. 14 files (1 new, 13 modified). 5 tests. Ninth CC WU. Closes audit C-01, D1-05, D1-13, D2-11, D5-04. |
| May 20 | `df2743a` | M3.6b | EventBusConfig record (HOME_DEFAULT). ReplayWindowQueue capacity parameterized. InProcessEventBus promoted to public (DEC-M3-16). 8 files (3 new, 5 modified). 9 tests. Tenth CC WU. Closes audit D1-07, D4-09. |
| May 20 | — | WUCP Phase 2 — M3.6a + M3.6b | PM closeout for both sub-WUs. Four PM-side artifacts updated. DEC-M3-16 applied. Build gate resolved GREEN. |
| May 20 | `38d3e30` | M3.6c | Per-module event-class manifests. `EventTypes.CORE_PRODUCTION_EVENT_CLASSES` (22) + `IntegrationEvents.LIFECYCLE_EVENT_CLASSES` (5) aggregated via `Stream.concat` at composition root. 1 new file + 4 modified across event-model + integration-api + persistence (test) + integration-tests (test). Refactor-only — no net test additions. Closes Q3 gap closure (Artifact 1). Thirteenth CC WU. |
| May 20 | — | M3.6d sub-divide (Option A) | Claude Code pushback during M3.6d execution identified seven source-vs-brief mismatches (visibility gaps in HealthSignal/HealthLevel, missing `WriteCoordinator.queueSize()`, no production `SubscriberReadConnectionFactory`, `SqlitePersistenceLifecycle` does not construct `SqliteStateStore`/`SqliteDeadLetterStore`, SubscriberInfo/SubscriptionFilter constructor shapes differ). User chose Option A: sub-divide into M3.6d-a (satellite changes; this commit) and M3.6d-b (prerequisite infrastructure + wiring; next session). |
| May 20 | `25bc23b` | M3.6d-a | Composition-root satellite changes. SqliteStateStore implements StateCheckpointSource (rename + public via interface). QueueSaturationHealthCheck + HealthSignal + HealthLevel public (DEC-M3-17 — transitive chain). ReadinessSource public interface. ReconciliationTest 4/5 methods. Tier 9 reconciliationOnVersionMismatch un-disabled. HomeSynapseConfig + SharedScheduler + ThrowingStateQueryService skeletons in lifecycle. Module-info `requires transitive` + non-transitive `requires org.slf4j`. 18 files. 10 net @Test additions (5 SharedSchedulerTest + 4 ReconciliationTest + 1 Tier 9 un-disabled). Fourteenth CC WU. Closes audit D3-08. |
| May 20 | — | SLF4J follow-up patch | First `./gradlew check` failed with `package org.slf4j does not exist` in lifecycle. Added `requires org.slf4j;` to lifecycle/module-info.java + `implementation(libs.slf4j.api)` to lifecycle/build.gradle.kts (canonical M2.2 pattern from `core/persistence`). MODULE_CONTEXT updated. No source-code changes. Resolved before commit. |
| May 20 | — | Phase 3 test strategy assessment (Cowork) | Inline strategy assessment produced 10 test-strategy recommendations spanning M3.7–M6. Format: pattern → recommendation → applicability matrix. Not committed to repo (advisory-only output for PM's planning). |
| May 20 | — | WUCP Phase 2 — M3.6c + M3.6d-a + DEC-M3-17 | PM closeout for both sub-WUs (this entry's session). Eight files modified: PROJECT_SNAPSHOT, pm-handoff, coder-handoff, phase-3-milestone-backlog, this weekly plan, Current_State, Knowledge_Primer, Locked_Decisions, plus phase-3-cross-module-decisions (D-08). DEC-M3-17 logged. M3.6d sub-divide propagated. coder-handoff "FOUR OPEN" corrected to "ZERO OPEN." OR-M3-12/13/14 added; OR-M3-12 resolved in same session. |

## Completed

### Monday 2026-05-18
- [x] Bus-Fix Piece A (`fceafe8`) — DerivedWriteRateLimit visibility
- [x] M3.5b StateProjection prod persistence (`08d0136`)

### Tuesday 2026-05-19 (actual: compressed into single day)
- [x] Projection-Checkpoint Wiring (`56aaa4b`)
- [x] Supervisor DLQ Wiring (`ed5862c`)
- [x] M3.4a Integration-test scaffold (`5ae7912`)
- [x] Cross-tier deployment audit (Cowork, design-only)
- [x] M3.4b Sustained-load + crash-recovery tests (`adf04d2`)
- [x] WUCP Phase 2 reconciliation (PM)

### Wednesday 2026-05-20
- [x] Gap-closure + M3.6 composition-root design (Cowork, design-only)
- [x] M3.6a — Profile-Driven Persistence Configuration (Claude Code, ninth CC WU) — `17c40b6`
- [x] M3.6b — EventBusConfig + InProcessEventBus Visibility (Claude Code, tenth CC WU) — `df2743a`
- [x] WUCP Phase 2 — M3.6a + M3.6b closure (PM, Cowork)
- [x] M3.6c — Per-Module Event-Class Manifests (Claude Code, thirteenth CC WU) — `38d3e30`
- [x] M3.6d sub-divide decision (Option A — after Coder pushback)
- [x] M3.6d-a — Composition-Root Satellite Changes (Claude Code, fourteenth CC WU) — `25bc23b`
- [x] SLF4J follow-up patch (same-day fix, no source changes)
- [x] Phase 3 test strategy assessment (Cowork, advisory-only)
- [x] WUCP Phase 2 — M3.6c + M3.6d-a + DEC-M3-17 closure (PM, Cowork)
- [x] Hivemind staleness audit + updates (PM, Cowork)

### Remaining (Thu–Sat)
- [ ] M3.6d-b revised coding instruction (PM produces after addressing OR-M3-14 prerequisite infrastructure gaps)
- [ ] M3.6d-b execution (Claude Code)

## Notes

- The 2026-05-19 WUCP Phase 2 reconciliation closed out six work units (Bus-Fix A through M3.4b) and two design sessions. The second instance of the failure mode that triggered the 2026-04-11 governance overhaul. The catch-up consumed one PM session (Cowork); no new forward work was issued until preflight PASS.
- M3.4b landed and both build gates GREEN. The integration test harness now has five tests (2 from M3.4a, 3 from M3.4b) plus 9 unit tests for the disk test double.
- M3.6 design is committed. Five sub-WUs (M3.6a..M3.6e). M3.6e expanded into M3.6e.1 (StateQueryService + REST gate) and M3.6e.2 (admin endpoints + ArchUnit rules). Total: seven sub-WUs.
- M3.6a, M3.6b, M3.6c, and M3.6d-a all landed 2026-05-20. Build gate GREEN at HEAD `25bc23b`. WUCP Phase 2 completed same day. Critical path: M3.6d-b → M3.6e.1 → M3.6e.2 → M3.7.
- M3.4a was a deliberately scoped first slice (scaffold + 2 tests). M3.4b adds the long-duration tests that benefit most from the throttled disk test double — splitting at this boundary keeps each milestone reviewable. The same pattern recurred 2026-05-20: M3.6d was sub-divided into d-a + d-b after the brief grew past ~18 file entries during discovery (lesson codified in coder-lessons.md M3.6d-a entry #2).
- DEC-M3-16 (composition-root visibility strategy) applied in M3.6b: InProcessEventBus→public. SqlitePersistenceLifecycle→factory (M3.6d-b). QueueSaturationHealthCheck→public (M3.6d-a).
- DEC-M3-17 (HealthSignal + HealthLevel public visibility — DEC-M3-16 addendum) applied in M3.6d-a: the 3-type promotion chain (QueueSaturationHealthCheck + HealthSignal + HealthLevel) is the minimum viable visibility unit. The brief authorized only `QueueSaturationHealthCheck` promotion; transitive accessibility check (`-Xlint:exports`) forced the full chain.
- Fourteen Claude Code work units completed through M3.6d-a. Bus-Fix Piece A and the SLF4J follow-up patch were direct edits by Nick (not counted as CC WUs).
