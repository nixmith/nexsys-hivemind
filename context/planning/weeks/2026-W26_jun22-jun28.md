<!--
file: context/planning/weeks/2026-W26_jun22-jun28.md
purpose: Weekly execution plan — W26 (June 22–28, 2026). Scoped 2026-06-18 at the converge session. W26's job: turn HomeSynapse from "sound engine that doesn't boot" into a RUNNABLE, SECURE product (app-bootstrap, carrying the two core-review HIGHs), and DESIGN the differentiating automation layer BEFORE M7.2/7.3 implement it. M7.1 is a ride-along, not the focus.
status: SCOPED 2026-06-18 (activates at the W25→W26 rollover, Jun 21/22). Direction set by Nick (he delegated the app-bootstrap-vs-M7 strategic call to the PM; recorded in the snapshot 2026-06-18 masthead note).
state-type: current
last-verified: 2026-06-18 against core 03f16dc (substantive 1eddd9a) / docs f2e064d / hivemind b7a893e — all trees clean; preflight reconciled STALE→PASS this session.
anchors: context/audits/2026-06-15_core-review_CONVERGE_synthesis.md (C1–C15 backlog); context/assessments/2026-06-15_M5-D_desk-research_PM_Assessment.md (interview gate retired); context/planning/2026-06-12_M7-M8-charter-skeleton.md (M7/M8 pieces); context/planning/2026-06-14_automation-engine-superiority_research-vector_proposal.md (the differentiator vectors); context/planning/2026-06-15_app-bootstrap_charter.md (the authored charter to finalize)
-->

# Week of June 22–28, 2026 (2026-W26)

**Status:** SCOPED 2026-06-18 (activates at rollover Jun 21/22).
**Phase:** P3 — Implementation (test-first), inside master-plan **P2 Foundation Implementation**.
**Window framing — the pivot window.** M6 is COMPLETE and the M7 entry-gate is effectively clear, but the core-review verdict reframes the priority: **the core is sound where built but not yet runnable (`main()` is a one-line stub).** W26 spends its leverage on the two things that matter most now — **making the product runnable + secure (app-bootstrap)** and **designing the differentiating automation layer before it is implemented (superior-automation Phase-1)** — rather than speed-running baseline M7. Rationale: the snapshot 2026-06-18 masthead note.

**The strategic call (Nick-delegated, recorded):** prioritize app-bootstrap + the differentiator design + the agent pass over pushing M7.1/M7.2 implementation. M7 is the feature that wins, but the leverage is *runnability* (existential) + *designing superiority ahead of the build* (so M7.2/7.3 target the differentiator, not a baseline they'll outgrow) — not implementing the baseline engine faster.

---

## Lanes (leverage-ordered)

### Lane 1 — App-bootstrap milestone (THE runnability unlock; PM instruction → Coder build; Nick owns 2 HIGH decisions)
This is the convergence milestone the core-review flagged: it wires `main()`/`HomeSynapseCore`, which **activates the crypto and exposes the HTTP surface** — so the two HIGH findings detonate here and must be carried as gates, not discovered later.
- **C1 / OR-APP-AUTH (HIGH):** implement INV-SE-02 — `AuthMiddleware before("/api/*")` + `RateLimiter`, admin `/internal/*` behind the gate; **Nick decides the bind posture** (loopback-default vs all-interfaces; the CC-1 spike informs it). Auth-before-network-exposure is a tracked gate.
- **C2 / OR-RF-DECRYPT (HIGH):** decide + implement the confidentiality-vs-availability contract — most likely catch decrypt failure in `SqliteEventStore.fromRow` and emit `DegradedEvent` for the shredded/undecryptable case (hard-fail only for "cipher unwired but encrypted rows exist"); **add tests for both read-side failure modes** (currently none). **Nick owns the tradeoff call.**
- **C9 (MED):** reconcile the disjoint lifecycle abstractions (`SystemLifecycleManager` vs `HomeSynapseCore`) as part of bootstrap.
- **Inputs:** the R-γ (crypto-agility) + CC-1 (bind-address) + Key-Portability returns (DOCS/Code, in flight). **Interview-independent** (the gate is retired).
- **Done-when:** `main()` boots `HomeSynapseCore` GREEN with auth enforced, the decrypt-degrade contract tested, and a documented bind posture. Brief: `context/planning/2026-06-15_app-bootstrap_charter.md` (authored; finalize against C1/C2/C9 + the retired-interview state).

### Lane 2 — Superior-automation Phase-1 design (the differentiator; PM/architect drafts → Nick scopes + Locks)
Design — not implementation — of the automation layer that is the product's selling point, **ahead of M7.2/M7.3** so the build targets it. From the automation-engine-superiority vector proposal: **B3 multi-site/enterprise federation**, **C1 honest-hybrid deployment**, **expressiveness-without-a-DSL** (anti-requirement: no templating DSL), correctness/trust + observability vectors.
- **Done-when:** Nick has scoped which vectors enter a **new Locked design doc** (Doc 16 candidate) and the design-session brief is dispatched. The Lock itself is a Phase-1 deliverable (Architect mode). This lane **gates the shape of M7.2/M7.3** — confirm it does not reshape M7.1's trigger/condition contracts before M7.1 builds.
- Brief: the automation-superiority Phase-1 design-session prompt (authored 2026-06-18, `context/handoff/`).

### Lane 3 — Agent currency pass (DONE 2026-06-18; residual = Nick's mirror sync)
The coder + project-manager skill mastheads are currency-passed to M6-COMPLETE + the core-review lessons. **Residual:** Nick re-runs the external `.claude/skills` mirror sync (Check 9 is benign-STALE until then). No further PM action.

### Lane 4 — M7.1 ride-along (NOT the focus; Coder slot, post-Lane-2-confirmation)
M7.1 (trigger/condition path) is ISSUED + source-verified sound. It builds whenever a Coder slot opens **after** Lane 2 confirms its contracts are unaffected. Carry-pins intact (type-residency/FLATTEN, C1-interim no-publish, §4c Clock, LTD-11, red-team F7/C4, SD-9 fail-closed-at-load). Not started until Nick routes it.

### Lane 5 — Standing research/strategy (non-blocking)
R-γ + CC-1 + Key-Portability returns → Lane 1 inputs (assess on return). The automation-superiority research-vector menu (10 vectors) → Nick approves/sequences; dispatch after the Phase-1 design scope is set so research targets the Locked shape.

## Standing guardrails
Locked docs inviolate; AMD-86 / Doc 15 move only via the formal re-open→review→ratify pipeline (the retired interview gate does NOT trip it — the trigger is moot, not tripped); anti-requirements bind all automation design (no engine retry · no templating DSL · no destructive forced migration · never lead with commodity encryption); the FUTURE-AMD queue waits for a governance window; skill-mirror sync (Check 9) clears at Nick's next sync.

## Risks
**App-bootstrap is a high-density latent-defect milestone** (the core-review's term) — both HIGHs + the lifecycle reconciliation land at one ignition point; mitigate by gating auth-before-exposure and testing both decrypt-failure paths explicitly. **The superiority design may reshape M7 contracts** — that is the *reason* to do it before M7.2/7.3, not a risk to avoid; the only watch-item is confirming M7.1's trigger/condition contracts survive it before M7.1 builds. **Scope creep on the design doc** — keep Phase-1 to the vectors Nick scopes; federation + hybrid are large and may each warrant their own doc (P1 sizing smell-test applies). **Check 9** benign-STALE until the mirror sync.
