<!--
file: context/planning/weeks/2026-W26_jun22-jun28.md
purpose: Weekly execution plan — W26 (June 22–28, 2026). Scoped 2026-06-18 at the converge session. W26's job: turn HomeSynapse from "sound engine that doesn't boot" into a RUNNABLE, SECURE product (app-bootstrap, carrying the two core-review HIGHs), and DESIGN the differentiating automation layer BEFORE M7.2/7.3 implement it. M7.1 is a ride-along, not the focus.
status: SCOPED 2026-06-18 (activates at the W25→W26 rollover, Jun 21/22). Direction set by Nick (he delegated the app-bootstrap-vs-M7 strategic call to the PM; recorded in the snapshot 2026-06-18 masthead note).
state-type: current
last-verified: 2026-06-18 against core 03f16dc (substantive 1eddd9a) / docs f2e064d / hivemind b7a893e — all trees clean; preflight reconciled STALE→PASS this session.
anchors: context/audits/2026-06-15_core-review_CONVERGE_synthesis.md (C1–C15 backlog); context/assessments/2026-06-15_M5-D_desk-research_PM_Assessment.md (interview gate retired); context/planning/2026-06-12_M7-M8-charter-skeleton.md (M7/M8 pieces); context/planning/2026-06-14_automation-engine-superiority_research-vector_proposal.md (the differentiator vectors); context/planning/2026-06-15_app-bootstrap_charter.md (the authored charter to finalize)
-->

# Week of June 22–28, 2026 (2026-W26)

**Status:** SCOPED 2026-06-18 (activates at rollover Jun 21/22).
**Phase:** P3 — Implementation (test-first), inside master-plan **P2 Foundation Implementation**.
**Window framing — the pivot window.** M6 is COMPLETE and the M7 entry-gate is effectively clear, but the core-review verdict reframes the priority: **the core is sound where built but not yet runnable (`main()` is a one-line stub).** W26 spends its leverage on the two things that matter most now — **making the product runnable + secure (app-bootstrap)** and **designing the differentiating automation layer before it is implemented (superior-automation Phase-1)** — rather than speed-running baseline M7. Rationale: the snapshot 2026-06-18 masthead note.

**The strategic call (Nick-delegated, recorded):** prioritize app-bootstrap + the differentiator design + the agent pass over pushing M7.1/M7.2 implementation. M7 is the feature that wins, but the leverage is *runnability* (existential) + *designing superiority ahead of the build* (so M7.2/7.3 target the differentiator, not a baseline they'll outgrow) — not implementing the baseline engine faster.

---

## Lanes (leverage-ordered)

### Lane 1 — App-bootstrap milestone (THE runnability unlock; PM instruction → Coder build; Nick owns 2 HIGH decisions)
This is the convergence milestone the core-review flagged: it wires `main()`/`HomeSynapseCore`, which **activates the crypto and exposes the HTTP surface** — so the two HIGH findings detonate here and must be carried as gates, not discovered later.
- **C1 / OR-APP-AUTH (HIGH) — DECISIONS RULED 2026-06-18:** implement INV-SE-02 — `AuthMiddleware` on **every** route incl. `/internal/*` + `RateLimiter`, auth-before-path-resolve (canonicalize first). **A1: bind posture = loopback-default**, LAN = explicit authenticated opt-in, never bind-all (CC-1 = confirm-mechanics gate on the instruction). **A2: auth model = token issuance**, WS authenticated, zero-config stays authenticated (no shared bootstrap secret, no pre-auth enumeration); **carry the enterprise per-scope/per-site-claims hook**. Auth-before-network-exposure is a tracked gate.
- **C2 / OR-RF-DECRYPT (HIGH) — CONTRACT RULED 2026-06-18 (A3):** **fail-closed at MVP** — on GCM-auth-fail / missing-or-corrupt root key, fail the read batch closed with a distinct loud error. **Design the degrade seam now** (`DegradedEvent` type + `(scope,key_version)` cause lookup + chain-validity check) but the **degrade behavior ships with the crypto-shred WU, NOT at app-bootstrap** (R-α REC-234: a flat catch-and-degrade masks CASE-b tampering — *supersedes the earlier "emit DegradedEvent at bootstrap" framing*). Carry the rotate-DEK-on-restore boot invariant. **F4 pin:** degrade/chain-validity disabled until `chain_hash` + startup verification are live (ZERO today). **Add tests for both read-side failure modes** (currently none).
- **C9 (MED):** reconcile the disjoint lifecycle abstractions (`SystemLifecycleManager` vs `HomeSynapseCore`) as part of bootstrap (AB-3, issues first — substrate); openHAB numbered startlevels; gate cipher activation + HTTP exposure to phases.
- **Inputs / residual:** decision gates all CLOSED (A1-A4). Mechanical residual only — **CC-1** (bind mechanics), **R-γ** (version-tag policy; slot reserved), **AMD-94** (Doc 15 §6 amendment ratification). Key-Portability brief in flight (non-blocking). **Interview-independent** (gate retired).
- **Done-when:** `main()` boots `HomeSynapseCore` GREEN with token auth enforced + loopback-default bind, the fail-closed read contract + seam landed and both decrypt-failure paths tested. Charter **FINALIZED to the ruled state** 2026-06-18: `context/planning/2026-06-15_app-bootstrap_charter.md`; decisions: `context/decisions/2026-06-18_app-bootstrap-and-superiority-scope_decisions.md`. Issue order: **AB-3 first**, then **AB-1+AB-2+AB-4 together** (Seam-1 go-live). **AB-3 is the RULED next Coder slot (2026-06-19, M7-forward plan); its scope must resolve the device-model registry dependency up front** (fold minimal Entity/Device/Area production registries, or sequence that device-model slice just ahead — AMD-44/Research-8; never a silent block).

### Lane 2 — Superior-automation Phase-1 design (the differentiator; PM/architect drafts → Nick scopes + Locks)
Design — not implementation — of the automation layer that is the product's selling point, **ahead of M7.2/M7.3** so the build targets it. **SCOPE RULED 2026-06-18 (Part B) + Architect-mode authoring DISPATCHED.** **Doc 16** = expressiveness-without-DSL + explainability/causal-chain + **run-coupled reliability**, with a federation (identity/scoping) seam + a hybrid cut-line coordinated with app-bootstrap. **Split into their own sequenced docs:** cross-cutting reliability-as-a-product-property (co-equal, right after Doc 16) · B3 federation (post-M8) · honest-hybrid (tight, with app-bootstrap). Deferred to M7.2/M8 briefs: A2/B1/B2/D2/E2.
- **M7 interlock verdict (RULED):** **M7.1 UNAFFECTED** (trigger/condition contracts intact → ride-along cleared, Doc 16 §4 confirms) · **M7.2 SHAPED, builds into Doc 16** (do not freeze its action contract first) · **M7.3 UNAFFECTED** (D2/REC-162 stays the separate M7.2 question).
- **Done-when (this lane, scope phase): ✅ DONE** — scope ruled, M7-verdict stated, authoring dispatched (`context/handoff/2026-06-18_automation-superiority_Phase1-design_session_prompt.md`, RULED SCOPE folded). **Next:** a fresh Architect-mode Cowork session authors the Doc 16 draft → DOCS review → Lock (the Lock is the Phase-1 deliverable). Decision record: `context/decisions/2026-06-18_app-bootstrap-and-superiority-scope_decisions.md` Part B.

### Lane 3 — Agent currency pass (DONE 2026-06-18; residual = Nick's mirror sync)
The coder + project-manager skill mastheads are currency-passed to M6-COMPLETE + the core-review lessons. **Residual:** Nick re-runs the external `.claude/skills` mirror sync (Check 9 is benign-STALE until then). No further PM action.

### Lane 4 — M7.1 ✅ DONE → M7.2/M7.3 plan recorded (2026-06-19)
**M7.1 (trigger/condition path) LANDED GREEN** (`beb4bc3`, `./gradlew check` 149 tasks); WUCP Phase 2 → DONE; **second-checked 2026-06-19 = PASS-WITH-FINDINGS** (2 cosmetic currency findings reconciled; no material gap). **M7-forward plan recorded** (`context/planning/2026-06-19_M7-forward_plan.md`): M7.2 split per P1 → **M7.2a** (baseline-buildable-now — run lifecycle + `RunCausalChain` swap + dispatch + event slice; builds against injected deps, wiring→AB-3) + **M7.2b** (action-model freeze — **Lock-gated on Doc 16 + D2/REC-162**); **M7.3** (`PendingCommandLedger`) UNAFFECTED, gated on M7.2a. **Sequencing RULED 2026-06-19: app-bootstrap AB-3 takes the next Coder slot** (existential runnability; AB-3 is the wiring substrate M7.1/M7.2 defer into; AB-3 scope must resolve the device-model registry dependency up front — fold minimal registries or sequence the device-model slice ahead; **M7.2a = fallback** only if AB-3 prep stalls); Doc 16 review→Lock (Lane 2) runs in parallel and gates M7.2b; then M7.2a → M7.2b → M7.3. D2/REC-162 RULED: keep anti-retry, decide at M7.2b. **M7.2 NOT dispatched** (entry-gate OPEN). Carry-pins for M7.2a intact (type-residency/FLATTEN, C1-interim no-publish, §4c Clock, LTD-11, AMD-52 codec, C8 stamping; + the 3 M7.1 behavioral-default fold-items).

### Lane 5 — Standing research/strategy (non-blocking)
R-γ + CC-1 + Key-Portability returns → Lane 1 inputs (assess on return). The automation-superiority research-vector menu (10 vectors) → Nick approves/sequences; dispatch after the Phase-1 design scope is set so research targets the Locked shape.

## Standing guardrails
Locked docs inviolate; AMD-86 / Doc 15 move only via the formal re-open→review→ratify pipeline (the retired interview gate does NOT trip it — the trigger is moot, not tripped); anti-requirements bind all automation design (no engine retry · no templating DSL · no destructive forced migration · never lead with commodity encryption); the FUTURE-AMD queue waits for a governance window; skill-mirror sync (Check 9) clears at Nick's next sync.

## Risks
**App-bootstrap is a high-density latent-defect milestone** (the core-review's term) — both HIGHs + the lifecycle reconciliation land at one ignition point; mitigate by gating auth-before-exposure and testing both decrypt-failure paths explicitly. **The superiority design may reshape M7 contracts** — that is the *reason* to do it before M7.2/7.3, not a risk to avoid; **RESOLVED 2026-06-18:** the ruled verdict is M7.1 UNAFFECTED (ride-along cleared, Doc 16 §4 confirms) / M7.2 SHAPED-builds-into-Doc-16 / M7.3 UNAFFECTED. **Scope creep on the design doc — handled by the ruling:** federation + cross-cutting-reliability + hybrid each split into their own sequenced docs (P1 applied); Doc 16 anchors on the three M7-coupled sections + two seams only. **Check 9** benign-STALE until the mirror sync.
