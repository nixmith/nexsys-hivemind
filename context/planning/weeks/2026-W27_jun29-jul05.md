<!--
file: context/planning/weeks/2026-W27_jun29-jul05.md
purpose: W27 weekly plan — authored retroactively-at-midweek by the v13 hub (2026-07-01) at spine reconciliation (the W27 file was the preflight Check-2 gap; W26 closed with the v12 arc already ahead of scope).
status: IN_PROGRESS (authored 2026-07-01 by the v13 hub; days 1–2 of the week are reconstructed from the committed v12 arc). The authoritative live state is `context/status/PROJECT_SNAPSHOT.md` + `context/planning/phase-3-milestone-backlog.md`.
-->

# Week of June 29 – July 5, 2026 (2026-W27)

**Week theme: measured ground → ratified governance → M9 authoring.** The v12 arc opened the week by landing the frontend build (FE-0/FE-2/FE-3/D-FE-10/T1.2/T1.3, core `dbb0109`, `frontend.yml` = CI gate of record) and bench first-light (`nexsys-bench` `5ceff3b`: instrument frozen 7.4.5.0/v13, the moat measured, 2 replayable fixtures). The v13 hub launched 2026-07-01 with the dual charge (run the hub + independently re-verify the v12 arc) — re-verification PASSED with amendments (see the beat-41 snapshot entry).

## Lanes (leverage-ordered)

1. **Governance ratify (Nick + hub fold)** — the consolidated queue: AMD-96 ratify (absorbing the AMD-CAND-2/3 verbatim diffs + the E1 firmware-baseline + E2 VID:PID auto-detect currency into §3.3) → AMD-CAND-1 ratifies as **AMD-97** (confirmation block + measured values + E5 taxonomy split + never-false-CONFIRMED invariant) → D2 registers **INV-ES-09** (watermark-neutral, 170→171, AIOT-INV-1 citation fix). Watermark 95→96→97.
2. **Core (Coder lane, serial):** M9 authoring once AMD-97 is folded — against the measured bench corpus as acceptance spec + the E1/E2/E4/E5 requirements + the reporting-config-parity items (beat-41 entry). Grounding-subagent-before-authoring discipline.
3. **Frontend (parallel, write-isolated):** FE-1 live-integration (hours; M9-independent — real M7.5a/b endpoints exist) + fold the E5 rendering semantics (CT confirms ~7–8 s; idempotent-command honest states; effect/identify immediate UNCONFIRMED).
4. **Bench (frozen instrument):** hold for the 72 h soak (post-M9); Z2M `exposes` cross-check (D5 second oracle) when Nick has bench time; Wave-2 devices on arrival (priority: a BEST_EFFORT-class periodic/sleepy device — the untested confirmability leg).
5. **Hygiene:** beat-41 commit (Nick, host-side); the deferred spine beat-block rotation once the phantom is quiescent; the `nexsys-skills` frontend-skill commit; the core `frontend.yml` source-copy sync commit.

## Standing guardrails

Five-repo sync + single-spine-writer; CI is the gate of record (core `check` + `frontend.yml`); WUCP Phase 2 source-verified against the diff; no forward work under preflight STALE; phantom-gated mass rewrites; non-precluding of cloud/B2B seams.

## Risks

- The phantom is ACTIVE (VM-side git garbling incl. a truncated read of bench's `.git/index`) — all spine commits host-side by Nick until quiescent.
- The mid-Aug go/no-go (~Aug 16): both v12 pacing risks retired for Wave-1; the longest remaining pole is M9 → 72 h soak → install-smoke. Do not let governance-fold latency stack in front of M9.

## Midweek progress (2026-07-02, v14 hub — beats 48–50)

- Lane 1 (governance) ✅ CLOSED at beat 42 (AMD-96/97 + INV-ES-09; watermark AMD-97, invariants 172/51). Lane 2 (Core): M9 authored + split-ratified (M9.1→M9.4, DP-A) → **M7.5c-a micro-WU landed CI-green (`e3d7296`)** → **M9.1 routed to Claude Code**. Lane 3 (frontend): FE-1 ✅ live-integrated → **FE-1b dispatched** (v1.1.1 fold + live smoke, separate Cowork lane). Lane 5 (hygiene) ✅: the deferred spine rotation EXECUTED (beat 48; snapshot 154.8→~38 KB, pm-handoff 138.9→~25 KB); beats 48/49 committed (`dd162e2` / `8a34a7f`).
- Risk update: **install-smoke is ACTIVE + green on every push** (beat-50 discovery; activated `652f9b3`) — the go/no-go long pole shortens to M9.x → 72 h soak, plus the website build (the last unstarted gate-4 surface; lane launches after FE-1b).

## Week close-out progress (2026-07-03, v15 hub — beat 57)

- Beats 51–56 (v14): **M9.1 LANDED CI-green (`ec2e3b4`) — the integration spine is LIVE**; the extensibility assessment + Doc-18 dossier/charter delivered; **FE-1b LANDED (`cf3733e`) — the dashboard live end-to-end**; the orchestration retrospective folded into the skills system; v15 authored.
- Beat 57 (v15 launch): preflight reconciled (Check-8 sweep, backlog currency); **M9.2 authored ISSUE-READY** (CC lane; both graph knobs pre-pinned; #30891 re-checked CLOSED/8.0.2-only); **website-lane prompt authored** (gate 4's open half); **DP-B CONFIRMED permanent + DP-18-A/B/C ALL RULED** → Doc 18 authoring unblocked (Lock before M9.3). Lanes for the W27 close/W28 open: Core = M9.2 (CC) · Frontend = website · Hub = Doc 18 (Mode 1).
- Beats 58–59 (v15): **M9.2 LANDED `6ea6912`** (CC-built; 2 gate rounds hub-fixed; R1–R7 ruled; post-green hardening H1–H5; clean-tree `check` ×2) — **the transport substrate is on main**. **Website lane LANDED its return** (Astro skeleton, 6 pages, R-3 developer page; audit ACCEPT; docs Commit A = Nick residual; FE-1b's dist finding CLOSED-REFUTED). Skills folded (instruction-format +3, deviation-and-quality +2 — mirror sync pending). W28 open: v16 hub → **Doc 18 authored → Locked** → M9.3 JIT; dossier-content lane; Dependabot (5 enumerated) + LICENSE-flip + website.yml rulings.
