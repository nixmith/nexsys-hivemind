<!--
file: context/planning/weeks/2026-W28_jul06-jul12.md
purpose: W28 weekly plan — authored 2026-07-04 by the v19 hub at the currency pass (charter §2C), on the W27 close-state (core `748962f` · docs `b65ed7d` — AMD-98/174/52 · bench `5ceff3b`).
status: IN_PROGRESS (flipped 2026-07-06 by the v21 hub at launch, per the plan's own flip note; W28 entry-gates 1–2 verified already CLEARED at flip — CI green on `748962f` confirmed v19 beat-1 and again on `959f925` at the v20 close; the currency pass closed with mirrors byte-identical ×3). The authoritative live state is `context/status/PROJECT_SNAPSHOT.md` + `context/planning/phase-3-milestone-backlog.md`.
-->

# Week of July 6 – 12, 2026 (2026-W28)

**Week theme: the moat on silicon.** W27 proved the thesis hardware-free (M9.4a: the hero loop GREEN over the scripted NCP, honest `state_confirmed` joining command AND report). W28 lands M9.4b on the clean substrate, puts the loop on Nick's bench, starts the 72 h soak, and charters the explainability-UX lane on LIVE verdict vocabulary — the mid-Aug go/no-go's longest pole runs through this week.

## Entry gates (from the W27 close)

1. **The ci.yml + install-smoke glance on core `748962f`** — the gate of record; **M9.4b does not dispatch until Nick confirms green** (§4b).
2. The v19 currency pass CLOSED (mirror sync run, re-preflight full PASS) — the M9.4b instruction is authored only on the clean substrate.
3. Dependabot (1 critical, day 7+ at W27 close) — Nick's 60-second triage; a trust product carrying an un-triaged critical is a credibility liability.

## Lanes (leverage-ordered)

1. **Core (Claude Code lane, serial):** **M9.4b** JIT-authored at the post-pass HEAD — **registry unification FIRST** (R4 — mandatory before the real transport binds), then the ruled charter per the backlog M9.4 row: brightness DERIVATION-side fix (root: `StandardCapabilities.brightness()` schema 0–100 → 0–254 per Doc 08 §3.5/§392; generic schema-driven bridge rescale; query-time percent derivation before any dashboard render) · identify/effects issuability with the PINNED semantics (immediate rendered UNCONFIRMED verdict with reason — never silent DISABLED bypass; test-asserted) · disposition-guard MEMBERSHIP test · ReopenAction composition (PortWatchdog:58 seam) · NETWORK_UP await · restart-honesty E2E · F-4/F-7/F-8/F-11/F-12/F-14/F-15 · N-4/N-5/N-8 · DECIDE-04 phantom-comment fix · SecretStore-backed parameter store · real serial transport wiring/port config. Dispatch on the proven allow-list → two-layer audit vs the DIFF → WUCP P2.
2. **Bench (Nick, after M9.4b's gate):** live-bench acceptance — the beat-42 seven + DP-F re-pair + Q10 re-characterize + FE-7; **hashed-TCLK exercises HERE as the ELECTED formation pin, with the fallback clause honored** (join instability attributable → plain for Wave-1, reason recorded, W2 row). Then **the 72 h soak STARTS**, with the NQ-6 restart spike + the W2-VAL synthetic legs (sleepy-stall + OTA-fingerprint re-characterize as seeded-log fixtures) riding the soak window.
3. **Frontend (parallel Cowork lane, write-isolated):** **the explainability-UX lane chartered on the refreshed nexsys-frontend skill** — consumes REAL verdicts, never mocked screens: the hero-question mapping (why did it fire / why didn't it / did it actually confirm), the verdict vocabulary rendering (honest-can't-know vs known-failed vs deliberately-superseded — "its entire vocabulary," Nick's beat-5 scope ruling), `StandardExplanationService`'s UNCONFIRMED/FAILED flattening as the named target, brightness percent DERIVED at query (never re-scaled state), the FROZEN v1.1 read-API grounding (flag a contract conversation if verdict events don't flow through it — never improvise).
4. **Website/docs (non-preemptable floor, P6):** the dossier-content lane stays launchable anytime (independent; W-5/PG-1 remains the binding gate-4 content gap).
5. **Wave-2 watch:** on arrival — the unboxing runbook (bench-first; **the MG24 instrument stays untouched**; a v14-batch unit = F-1's characterization asset).

## Standing guardrails

§4b — never M9.4c-anything while an M9.4b gate is unresolved · CI on the pushed commit is the gate of record · single-spine-writer + write-isolated lanes · WUCP P2 verified against the DIFF, never the report · two-layer audit on everything including own subagents (labels-are-claims, quotes-are-evidence) · **the north star: one-way-door precision outranks velocity — never-false-CONFIRMED's spirit extends to never-false-ALIVE.**

## Risks

- **The mid-Aug go/no-go (~Aug 16):** the longest remaining pole is M9.4b → bench → 72 h soak; a soak restart resets the clock — protect the window (do not stack governance latency in front of the dispatch).
- **v14-batch silicon (F-1):** the ==13 band means a v14 unit PIEs honestly instead of going deaf — treat the first v14 arrival as a characterization asset, not a blocker.
- **hashed-TCLK instability risk is bounded by the fallback clause** — evidence-first, one variable at a time; the election is exercised on the bench, not re-litigated in code.
