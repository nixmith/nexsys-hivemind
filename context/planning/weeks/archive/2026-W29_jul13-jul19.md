<!--
file: context/planning/weeks/2026-W29_jul13-jul19.md
purpose: W29 weekly plan — minted 2026-07-13 by the v31 hub at launch (the Monday-boundary reconciliation, Check 2), on the v30 close-state (core `1aa809d` · docs `a53f474` · hivemind `4624941` · bench `c18bf63` · skills `ee0839f`; watermark AMD-99, 175/53 re-derived at the register).
status: IN_PROGRESS (minted at the Monday boundary; the soak exited ≥ 2026-07-13 ~07:50 Pi-local — Phase 7 is the day's first act). The authoritative live state is `context/status/PROJECT_SNAPSHOT.md` + `context/planning/phase-3-milestone-backlog.md`.
-->

# Week of July 13 – 19, 2026 (2026-W29)

**Week theme: the certification closes and the gate-evidence machine turns on.** W28 certified the moat into its 72 h soak and code-completed Track A (SEED `a1f0f77` · DURc `7e4426b` · RO `1627978`+`3590fca` · AVAIL `1aa809d`, all CI-green). W29 closes the certification on the record (Phase 7 = the soak's exit act, BEFORE any pull/deploy), deploys `1aa809d`, lands the silicon legs (SEED · AVAIL · RO/I3a via B1's OPERATOR rep), joins Wave-2, and drives CMD-API through authoring → landing ahead of the ~Jul-24 C1 tripwire — every MUST row's evidence producer running on schedule against the FROZEN checklist (21 MUST / 8 SHOULD; evidence-freeze Fri 2026-08-14 EOD; the read Sun 2026-08-16).

## Entry gates

1. **The soak exit (≥ 2026-07-13 ~07:50 Pi-local)** — runbook Phase 7 steps 20–26 VERBATIM on the SOAKED `04f5f70` BEFORE any pull/deploy (anti-actions hold: no dongle acts, no config edits, no permit-join; ⏺ paste either way). **NOTHING deploys before the certification closes on the record.**
2. The v31 launch preflight reconciled to PASS (this file is the Check-2 reconciliation; mirrors ≡ 15/15 at launch; hivemind beat-5 `4624941` verified landed, parent `92ec713`).

## Lanes (leverage-ordered)

1. **Bench / certification (Nick + hub, Monday):** Phase 7 spike ×3 → the close-out intake (A1 RSS/log deltas against the stated bounds · A2 ×3 · A3 · the zero-false-CONFIRM sweep · I2 key-hygiene sweep · the Pi log copy-off per retention policy §3) → **THE CERTIFICATION CLOSED** → pull/build/deploy `1aa809d` → the silicon legs: **SEED** fresh-formation (= the SD-5 discharge; composes with Wave-2 joins, Nick rules timing) · **AVAIL** both directions on the no-new-hardware F2 path (reports→AVAILABLE rides organic SNZB-03P traffic; the offline leg = the Hue mains power-cut → ping-timeout within the ~10 min window → restore → online-on-evidence) · **the B1 Pi half** (constants.yaml watermark re-mint to step-20's P0 FIRST → boot-health → `suite all` [expected: 1 run · 2 SKIPPED command-api · 1 SKIPPED usb-power · 1 OPERATOR-deferred] → `usb-reenumeration-manual` direct-attached = **I3a satisfied AS FROZEN**, rider 4; paste-blocks in the Coder's 2026-07-12 cross-agent note, which restamps SERVED at the close-out) → **Wave-2 joins** IF the SONOFF units are in hand (the (a) dossiers = the profile/scenario source; per-line `within:` minted at authoring; S31 adopts via exact_model; the 04P capture-led 0xFC57). **A4 (kill −9)** rides the first post-deploy bench session with spare minutes (the named carrier).
2. **Hardware orders (Monday, D1 lead-time insurance):** the Shelly ×2 + Rosonway order fires (approved 2026-07-10, unexecuted); the Wave-2 SONOFF units ordered Monday if not in hand (D1 [MUST, 5 classes] wants joins ≥2 weeks before the Aug-14 freeze). The dual-role-plug constraint stands (the Shelly plugs = B1/B2 stimulus of record, never M14 DUTs; the third plug orders just-in-time on the M14 trigger clearing).
3. **Core (CMD-API — the week's core-lane pole):** authored on the banked debts (auth on the POST — the OR-APP-AUTH posture extends · `IdempotencyEntry` honored · `ProblemType` mapping complete · wire-casing pinned at authoring against Doc 09/LTD-08 vs the live camelCase precedent · `WebhookTrigger` route-registration status grounded BY CENSUS · token-freeze from day one · full bar, no AMD) → dispatched at the first post-silicon-legs core slot (ruled ahead of M14 with B2, rider 3). **The ~Jul-24 C1 tripwire is ARMED:** CMD-API unlanded at the first beat on/after 2026-07-24 ⇒ the hub proactively flags C1's N≥200 reachability to Nick.
4. **Frontend (parallel Cowork lane, write-isolated):** the explainability-lane dispatch refresh + dispatch on the word — G1 is the gate's longest pole AND an M14-trigger condition (refresh against: verdict vocabulary LIVE · AVAIL landed + silicon-live post-Monday, FE-7's data real · the FROZEN v1.1 read-API · the no-change ruling — the explanation NEVER upgrades or replaces a verdict · token-parameterized, the naming program never blocks it).
5. **Bench automation:** B2 (the §51 port) authored on CMD-API's landing (the honest-occurrence sweep PER forbidden token — the banked B1 lesson; the stub flips ride a constants re-mint; the count-2→`${C.fleet.devices}` NOTE folds here) → B3 nightly (the C1-N accumulation engine; ~8 suite executions ⇒ N≈200).
6. **Nick's own lane — the Tamoro program** (self-governing: `context/instructions/2026-07-12_tamoro-brand-legal-execution_instruction.md`): Phase B (entity formation start · consult chasing) · **Wed 2026-07-16 = the first TSDR audit on US 79411529** (the §5 paste-brief on Nick's word) · G-1 engagement targeted this week · R-1 (brand architecture a/b) held for G-2's written opinion (hub REC).

## Trigger watch

**M14 (EOD Fri 2026-07-31, America/Chicago — the canonical R1b trigger):** M14 opens only when ALL are true — the certification closed on the record · the SEED and AVAIL silicon legs green · the M9.6-RO leg green through B1's usb-reenumeration scenario in the DIRECT-ATTACHED topology (the hub-attached/Rosonway leg never gates it) · the explainability-lane dispatch out · no audit holds an open [REVIEW]. Not all met by the boundary ⇒ M14 = post-launch arc #0 at zero strategic cost — no ceremony, no re-ruling. The third-plug rider governs if it opens.

## Standing guardrails

Phase 7 before any pull/deploy · CI on the pushed commit = the gate of record · single-spine-writer + write-isolated lanes · two-layer audits on everything including own subagents (labels-are-claims, quotes-are-evidence) · sweep-guards re-derived from a FRESH porcelain at commit-order time · staged commit messages assert ONLY confirmed state · J1 FROZEN (criterion changes require an explicit Nick re-ruling) · grounding names its evidence class — a census, never type-existence · **the north star: one-way-door precision outranks velocity — never-false-CONFIRMED / never-false-ALIVE, extended to identity, the instrument, the RECORD, and the data flow.**

## Risks

- **The Jul-31 M14 boundary is priced, not feared** — the slide trigger absorbs a CMD-API stretch; the Jul-24 tripwire exists so the Aug-14 freeze never discovers C1 reachability late.
- **D1 hardware lead time:** an unordered Wave-2 unit today is a freeze-window risk in three weeks — hence order-Monday.
- **Soak-exit anomalies:** any Phase-7 what-if (`device_proposed` on a spike boot · position < P0 · a `bench.sh restart` FAILED verdict) is a STOP-and-ping, never improvised — the one-way door outranks the schedule.
