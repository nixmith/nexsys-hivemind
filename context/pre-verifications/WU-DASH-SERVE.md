<!--
file: context/pre-verifications/WU-DASH-SERVE.md
purpose: Pre-verification pins for WU-DASH-SERVE (the dashboard serve path: package + mount + the ruled auth exemption). Every P-item below is an OBSERVED source signature, read at core HEAD `2040a66` on 2026-07-27 by the v39 hub (remote session; reads via the Filesystem-MCP host-truth route + git-object reads per env-model §12). The Coder verifies each pin against the working tree BEFORE executing; any mismatch is a STOP-and-report, never an improvise.
audience: Coder (read before the instruction), hub (audit layer-2 anchor).
status: ISSUE-READY 2026-07-27 (v39 hub, beat 1). Companion instruction: context/instructions/2026-07-27_DASH-SERVE_dashboard-serve-path_coding-instruction.md
-->

# WU-DASH-SERVE — Pre-Verification (P0–P15)

**The finding this WU closes (v38 beat 7, all three blockers re-verified at source at v39 authoring):** the dashboard has NO serve path. B-2: `:web-ui:dashboard` publishes no artifact and the npm pipeline wires into `assemble` only → `installDist` packages nothing web. B-1: zero `staticFiles` mounts exist — Doc 13 §3.2–§3.3 was never composed. B-3: AB-1's catch-all `before(*)` auth (INV-SE-02) 401s every headerless browser request (live proof: the 2026-07-26 deploy evening, `GET /` → 401 RFC-9457 `authentication-required`, corr `ae7990f0`). Each alone is sufficient to blank the browser.

- **P0 — dispatch baseline.** Core working tree clean at `2040a66` (`core: FE-VERDICT-2 …`). Both workflows GREEN on it (the deployed build of record). Porcelain empty at dispatch.
- **P1 — web-ui/dashboard/build.gradle.kts (B-2 ground).** `plugins { base }` ONLY. Tasks registered: `npmInstall` (Exec, `npm ci`, outputs `node_modules`) → `npmBuild` (Exec, `npm run build`, outputs `dist`) → `stageDashboard` (Sync, `dist` → `val staticOut = layout.projectDirectory.dir("src/main/resources/dashboard")`). Exactly ONE lifecycle wiring: `tasks.named("assemble") { dependsOn(stageDashboard) }`. `clean` deletes `staticOut` + `dist`. The header comment carries the DECOUPLING NOTE verbatim ("these npm tasks are wired into `assemble`/`build` ONLY — never into `check` … the root aggregate `./gradlew check` does NOT require Node"). No artifact is registered in any configuration.
- **P2 — app/homesynapse-app/build.gradle.kts.** Applies `homesynapse.application-conventions`. Carries `runtimeOnly(project(":web-ui:dashboard"))` under the comment `// Web dashboard (static files on classpath — not a JPMS module)`. No configuration-level excludes exist anywhere in the file today.
- **P3 — settings.gradle.kts.** `include("web-ui:dashboard")` present; project name is `dashboard` (the exclude-by-module pin in DP-2 keys on this name).
- **P4 — RestFilters.java (B-3 ground).** `installAuth(Object, AuthMiddleware, RateLimiter)` registers `app.exception(ApiException.class, RestFilters::writeProblem)` + `app.before(ctx -> authorize(ctx, authMiddleware, rateLimiter))`. `authorize(Context, AuthMiddleware, RateLimiter)` order today: (1) `isPathSafe(ctx.path())` else 400-class throw; (2) `authMiddleware.authenticate(ctx.header("Authorization"))`; (3) rate-limit → 429; (4) `ctx.attribute(IDENTITY_ATTRIBUTE, identity)`. `static final String IDENTITY_ATTRIBUTE = "hs.api.identity"`. `static boolean isPathSafe(String path)` is the existing package-private, unit-tested classifier precedent (strings in, boolean out).
- **P5 — HomeSynapseCore.java (B-1 ground; line numbers re-enumerated at authoring from `2040a66`).** `Javalin app = Javalin.create(cfg -> { cfg.jetty.threadPool = threadPool; cfg.showJavalinBanner = false; });` at `:903–:906`. Install order: `RestFilters.installAuth` `:910` → `installReadinessGate` `:911` → entity/admin/run/automation/command endpoints → `app.start(config.bindHost(), config.httpPort())` `:951` (loopback-default bind). ZERO occurrences of `staticFiles`/`spaRoot` anywhere in `lifecycle/` or `app/` main trees.
- **P6 — launch model (packaging compatibility).** `homesynapse.application-conventions` sets `application { mainClass.set("com.homesynapse.app.Main") }` — NO `mainModule` → CLASSPATH launch. `distribution/image/build-image.sh` jlinks ONLY JDK platform modules and runs the app from `lib/*.jar` on the classpath (its own header: "jlink refuses to LINK automatic modules … so we jlink only the JDK platform modules"). A resources-only, non-JPMS `dashboard.jar` on the classpath is therefore fully compatible with BOTH the Pi `installDist` tree and the jlink image path.
- **P7 — vite.config.ts (the fourth hop, already sound).** `base: '/dashboard/'` is SET; routing is hash-based; the config's own comment: "Served by Javalin at /dashboard/ (Doc 13), so base is set accordingly … Served builds need no proxy (Javalin serves the bundle, so window.location.origin IS Core)". **NO vite/frontend-source change rides this WU.**
- **P8 — RestFiltersAuthTest.java.** Exactly **3 @Test** today (`isPathSafe_acceptsNormalPaths` · `isPathSafe_rejectsTraversalAndControl` · `problem_buildsTypedApiException`); unit tests over the package-private helpers; class Javadoc notes the Javalin-bound wiring is exercised end-to-end in `HomeSynapseCoreTest.opensHttpOnlyBehindAuth`.
- **P9 — HomeSynapseCoreTest.java.** Exactly **11 @Test** today. The harness boots the REAL composition root (`@TempDir` + injected `Clock` + `java.net.http.HttpClient` + Awaitility) — `opensHttpOnlyBehindAuth` is the auth-wiring precedent the new serve-path tests follow.
- **P10 — CI surfaces.** `ci.yml` runs exactly one Gradle invocation: `./gradlew check --no-daemon` (line 32) — it must stay Node-free. `install-smoke.yml` exists in TWO copies (active `.github/workflows/install-smoke.yml`; source `distribution/ci/install-smoke.yml` per its WIRING SEAM header) — it sets up JDK 21 ONLY (no setup-node today) and builds via `distribution/image/build-image.sh` → `:app:homesynapse-app:installDist`. `distribution/smoke/run-smoke.sh:92–:96` asserts headerless `GET /api/v1/entities` → 401/403 ("auth enforced"); `distribution/smoke/health-probe.sh` targets `/api/v1/entities` with a Bearer header. Neither probes `/` — B-3's exemption breaks NO existing smoke assertion.
- **P11 — testing/integration-tests/build.gradle.kts.** Its dependency list contains NO `:app:homesynapse-app` and NO `:web-ui:dashboard` project dependency → the DP-2 exclusion is needed in `:app:homesynapse-app` ONLY.
- **P12 — ARCH-RULE-REACH (format addition #16; grep executed at authoring over `app/homesynapse-app/src/test/java/com/homesynapse/app/HomeSynapseArchRules.java`).** Rules enumerated: `NO_SYNCHRONIZED_METHODS :74` · `NO_DIRECT_TIME_ACCESS :98` · `NO_SERVICE_LOADER :126` · `NO_REVERSE_DEPENDENCIES :143` · `NO_DIRECT_FILESYSTEM_IN_CORE :191` · `NO_INTERNAL_PACKAGE_ACCESS :218` · `NO_JSON_TYPE_INFO_IN_EVENTS :242` · `QUERY_SERVICE_READ_ONLY :266` · Rule 9 `REST_ENDPOINTS_NO_EVENT_PUBLISHING` (scoped exemptions incl. `RestFilters` per CMD-API R1) · the registry-mutation rule (`RegistryMutationRuleTest`). **ZERO collisions:** this WU adds no time reads, no synchronized methods, no ServiceLoader, no direct filesystem access in our code (`Location.CLASSPATH` — Javalin does its own resource IO), no event publishing, no module edges, no internal-package access; the app test classpath's scanned CLASS set is unchanged by DP-2 (the dashboard jar carries zero classes).
- **P13 — module-info (verbatim embeds; NO module-info edits in this WU).** rest-api: `module com.homesynapse.api.rest { requires transitive com.homesynapse.state; requires com.homesynapse.event.bus; requires com.homesynapse.automation; requires com.fasterxml.jackson.databind; requires io.javalin; requires org.slf4j; exports com.homesynapse.api.rest; }`. lifecycle: `module com.homesynapse.lifecycle { requires transitive com.homesynapse.observability; requires transitive com.homesynapse.event; requires transitive com.homesynapse.platform; requires transitive com.homesynapse.persistence; requires transitive com.homesynapse.event.bus; requires transitive com.homesynapse.state; requires transitive com.homesynapse.integration; requires com.homesynapse.integration.runtime; requires com.homesynapse.api.rest; requires com.homesynapse.config; requires transitive com.homesynapse.device; requires com.homesynapse.automation; requires com.homesynapse.platform.systemd; requires io.javalin; requires org.eclipse.jetty.util; requires org.slf4j; exports com.homesynapse.lifecycle; }`. `:web-ui:dashboard` has NO module-info (not a JPMS module) and must not gain one.
- **P14 — Javalin pin.** `gradle/libs.versions.toml:19` — `javalin = "6.7.0"`. The mount uses the 6.x config API (`cfg.staticFiles.add(...)` with `Location.CLASSPATH` + `cfg.spaRoot.addFile(hostedPath, filePath, location)`); the Coder verifies exact signatures against the pinned 6.7.0 API at implementation (a signature mismatch is pushback, not improvisation).
- **P15 — the live B-3 proof (for the record).** Deploy-evening return §2.10: headerless browser `GET /` → RFC 9457 401 `authentication-required`, instance `/`, corr `ae7990f0` — the guard covers the document root today; possession of a token cannot help a browser's top-level navigation (headers unattachable from the address bar).
