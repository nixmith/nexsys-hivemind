<!--
file: context/pre-verifications/WU-M9.3.md
purpose: Pre-verification artifact for M9.3 (interview / reporting-config / ingestion / profile registry) — every load-bearing source-state assumption pinned with its observed signature. The Coder re-verifies each STOP gate against the execution-time HEAD before writing code.
audience: Coder (Claude Code lane), PM
state-type: current
status: VERIFIED 2026-07-03 by the v17 hub against core `6ea6912` / docs `fb91c23` (Doc 18 LOCKED; watermark AMD-97, invariants 174/52) / bench `5ceff3b` / hivemind `f6315ee`
baseline-note: If core HEAD ≠ `6ea6912` at execution, diff the delta against the read-set below (the M9.1 baseline-shift protocol) — a disjoint delta proceeds with the fold base carried forward; an intersecting delta is a STOP.
-->

# Pre-Verification — WU-M9.3 (interview pipeline, ingestion, device-profile registry)

**BOTH module-graph knobs are pinned here (A4). The frozen-record arity change (§A) and the sealed match-key change (§B) are pinned with their full observed shapes (A5–A7) — these are the one-way doors; the north star applies.**

| # | Assumed source element | Observed signature (2026-07-03, core `6ea6912`) | M9.3 consequence |
|---|---|---|---|
| A1 | `integration/integration-zigbee/` module state | M9.2 IMPLEMENTED: 59 type files + module-info (38 pre-existing PUBLIC Phase-2 types + 21 M9.2 implementation types, ALL package-private — MODULE_CONTEXT header, hub-verified; an agent-survey claim of "only 4 public types" was REFUTED against MODULE_CONTEXT + a direct `DeviceProfile.java` read: the Phase-2 records/enums are public). Test tree: 13 files. `src/main/resources/schema/` holds only `.gitkeep`. **NO `zigbee-profiles.json` anywhere** → MUST BE CREATED. | M9.3 builds the pipeline/registry layer on the live M9.2 transport; creates the bundled profile corpus file + the config-schema resource; first production caller of `DeviceProfile`. |
| A2 | `module-info.java` (integration-zigbee) | VERBATIM: `module com.homesynapse.integration.zigbee { requires transitive com.homesynapse.integration; requires com.fazecast.jSerialComm; requires org.slf4j; exports com.homesynapse.integration.zigbee; }` (comments elided; three requires, one exports). | Proposed diff = exactly ONE added line: `requires com.fasterxml.jackson.databind;` (PLAIN, interior-only — the JSON profile loader; no Jackson type on any exported signature; the D-M92-1 jSerialComm precedent). Verify the exact module name against the jar/catalog at execution (persistence's module-info is the in-repo reference consumer). |
| A3 | `build.gradle.kts` (integration-zigbee) | `api(project(":integration:integration-api"))` + `implementation(libs.jserialcomm)` + `implementation(libs.slf4j.api)` + M9.2 test-scope lines. | Proposed diff = exactly ONE added production line: `implementation(libs.jackson.databind)` (lockstep with the plain requires: plain⇔implementation). **Verify the catalog alias name in `gradle/libs.versions.toml` at execution** (Jackson entries exist — persistence consumes them; use the existing alias, do NOT mint a duplicate). |
| A4 | **Module-graph knobs (BOTH)** — root `build.gradle.kts` | **Knob 1 (allow-list):** `":integration:integration-zigbee -> :integration:integration-api"` present (+ wildcards `:integration:.* -> :core:.*` / `-> :config:.*` / `-> :platform:platform-api`). **Knob 2 (height):** `maxHeight = 8`, comment pins the deepest path (app→lifecycle→integration-runtime→integration-api→persistence→state-store→device-model→event-model→platform-api) + do-not-raise rule. | **PIN: M9.3 changes NEITHER knob.** Jackson is an external jar, not a project edge; nothing new depends ON zigbee this WU (adapter wiring = M9.4). If `:assertMaxHeight` or the allow-list trips, STOP — something outside scope moved. |
| A5 | `DeviceProfile` record (PUBLIC, frozen Phase-2) | VERBATIM 9 components: `profileId, matches (Set<ManufacturerModelPair>), category (DeviceCategory), clusterOverrides (Map<Integer,ClusterOverride>), reportingOverrides (Map<Integer,ReportingOverride>), manufacturerCodec (String), interviewSkips (Set<String>), tuyaDatapoints (List<TuyaDatapointMapping>), initializationWrites (List<InitializationWrite>)`. Compact ctor: requireNonNull on profileId/matches/category; matches non-empty; **5 nullable collections** with conditional defensive copies. **NO `confirmation` component** (AMD-97 ratified 2026-07-01, after the freeze — absent from source, dossier §A confirmed). | §A: grows 9→10 with `List<ConfirmationCharacterization> confirmation` (NULLABLE, conditional copy — the 5-nullable gotcha becomes 6; empty/null for read-only devices). §B: `matches` retypes to `Set<MatchCriteria>`. Both via the Mode-2 AMD-correction section at the TOP of the instruction. MODULE_CONTEXT `(9)`→`(10)`. |
| A6 | `new DeviceProfile(` fan-out (P2 survey) | **ZERO call sites repo-wide** (production + tests; grep this session). The record has never been instantiated — Phase-2 scaffold. | The arity + matches-retype changes break NO existing caller. All call sites are M9.3-created (loader, tests). Re-run the grep at execution; a nonzero result = baseline shift, STOP. |
| A7 | `DeviceProfileRegistry` (PUBLIC interface) + `ManufacturerModelPair` | VERBATIM: `Optional<DeviceProfile> findProfile(String manufacturerName, String modelIdentifier); void registerProfile(DeviceProfile profile); Collection<DeviceProfile> allProfiles();`. `ManufacturerModelPair(String manufacturerName, String modelIdentifier)` record. | §B widens the interface ADDITIVELY: `Optional<DeviceProfile> findProfile(InterviewResult interview)` (the fingerprint-capable path); the (String,String) form STAYS. Grep `ManufacturerModelPair` consumers at execution before deciding wrap-vs-replace inside `ExactModel` (zero non-scaffold consumers expected). |
| A8 | `EzspCoordinatorProtocol` seams (M9.2, package-private) | `interview(IEEEAddress)` currently `throws UnsupportedOperationException` — **the M9.3 seam to fill**. `List<EzspFrame> drainPendingCallbacks()` = the ingestion feed. `startSession()` / `resumeStored()` throw CHECKED `PermanentIntegrationException` (package-private seams); the frozen `CoordinatorProtocol` interface is no-throws. pendingCallbacks CLEAR on `resetSession()` (ruled beat 58). | Interview drives through the protocol layer; ingestion drains callbacks (§G bound-and-drain BEFORE live NCP). Interview failures surface as `InterviewStatus` values in the RESULT — never as new checked throws on the frozen surface (seam map in the instruction §Checked-Exception Seam Map). |
| A9 | `InterviewResult` + `InterviewStatus` (Phase-2, public) | `InterviewResult(IEEEAddress, int networkAddress, NodeDescriptor, List<EndpointDescriptor> endpoints, String manufacturerName, String modelIdentifier, int powerSource, InterviewStatus interviewStatus)` — 8 components. | The FSM produces it; `findProfile(InterviewResult)` consumes it. **Verify `InterviewStatus` enum constants at execution** (PARTIAL semantics are load-bearing — Doc 08 §3.4; adoptability is the Device Model's call). |
| A10 | `WithinTolerance` + the `Expectation` hierarchy (device-model, `com.homesynapse.device`) | `public sealed interface Expectation permits ExactMatch, WithinTolerance, EnumTransition, AnyChange`; `public record WithinTolerance(double target, double tolerance) implements Expectation`. Charter claim (authoring-time verified): the EVALUATION path for WithinTolerance is a STUB. | **STOP-gate at execution: locate the Expectation evaluation path** (grep `WithinTolerance` consumers — ledger/confirmation evaluation); implement the ±tolerance semantics (CT confirmation is impossible without it) WITHOUT touching the AMD-87 serialized shape (codec round-trip tests must pass UNTOUCHED). Cross-module touch: device-model consumer survey mandatory. |
| A11 | Zero-mint pins + pre-existing event vocabulary | `EventTypes` 71 / roster 41 / category 53 (M9.2-verified, untouched). `DEVICE_ADOPTED` ("device_adopted"), `CAPABILITY_ADDED` ("capability.added"), `CAPABILITY_REMOVED`, plus `state_reported` / `device_discovered` / `availability_changed` ALL pre-exist in `com.homesynapse.event.EventTypes`. | **M9.3 event-mint expectation = ZERO** (charter-pinned). Any `zigbee.*` mint moves 71/41/53 → must be declared, surveyed, category-mapped, and flagged `[REVIEW]` — expectation stands at zero with §3.11 telemetry deferred. The three pin tests pass UNTOUCHED. |
| A12 | Config seams | `SchemaRegistry.registerIntegrationSchema(String integrationType, String schemaJson)` exists (config module). `integrations.zigbee.adapter_type` is a TransportProbe method PARAMETER today (W10: no config reads in M9.2); `integrations.zigbee.profiles_path` = the user-override channel (Doc 08 §3.6, dossier §I). | M9.3 SHIPS the schema JSON resource (`src/main/resources/schema/`) and a loader honoring an injected profiles-path — **the config-key BINDING and `registerIntegrationSchema` call site land with the adapter factory at M9.4** (W10 boundary holds; M9.3 components take config values as constructor parameters). |
| A13 | Adoption seam state | Charter (authoring-time verified): `DiscoveryPipeline` / `CapabilityRegistry` implementations ABSENT; the AB-3 `InMemory*Registry` (Entity/Device/Area) impls live in `core:device-model` (MVP-substrate). `IntegrationContext` is a 12-component record carrying `EventPublisher`, `EntityRegistry`, `DiscoveryServices`, etc. | M9.3 scopes the adoption slice EXPLICITLY: Doc 02 §3.12 detection→proposal→adoption INSIDE the zigbee ingestion layer, publishing the pre-existing event types, identity-UNGATED (DP-B permanent); no core-module discovery-pipeline build. Verify at execution which integration-api surfaces the slice actually needs; anything missing = STOP + report, never a silent core edit. |
| A14 | Bench fixtures + corpus (the §J acceptance inputs) | `nexsys-bench` @ `5ceff3b`: `fixtures/2026-07-01_hue-lca017_confirmation-windows_event-stream.json` + `fixtures/2026-07-01_snzb-03p_motion-walktest_event-stream.json` (format: `fixtureVersion: 1`, `type: "raw-zcl-event-stream"`, provenance block + timestamped ZCL frame events). Corpus: `corpus/devices/philips-hue-white-a19.md` (5-block confirmation[] with measured values: on_off/brightness CONFIRMABLE 5000 ms; CT TOLERANCE 15000 ms; color_loop UNCONFIRMABLE READBACK_ONLY; identify UNCONFIRMABLE NONE) + `sonoff-snzb-03p-motion.md` (EMPTY confirmation block — read-only). Corpus IR `identity.fingerprint[]` = (profileId / deviceType / endpoints in+out clusters). | The two fixture files are COPIED into `integration-zigbee/src/test/resources/fixtures/` with provenance headers citing `nexsys-bench@5ceff3b` (core tests cannot read a sibling repo). `zigbee-profiles.json` carries the MEASURED values, citing the corpus files (pointer-not-copy: corpus = source of truth; the bundled file cites it). `Fingerprint` permit shape derives from the IR fingerprint[] fields. |
| A15 | Gates | Doc 18 **LOCKED** at docs `fb91c23` (§3.5 binds this WU — the Lock-before-M9.3 obligation DISCHARGED). M9.2 WUCP P2 complete; **zero unresolved deferred build gates** (pm-handoff Open Risks all RESOLVED). CC allow-list: root cause found (deny-list entry) — **the settings fix + CC restart + trivial-compile SMOKE is the DISPATCH gate**, proven before this instruction is pasted in. | Dispatch order: Nick fixes settings → restarts CC → smoke `./gradlew :integration:integration-zigbee:compileJava` GREEN → paste the instruction. *"M9.3 is the wrong instruction to discover a broken allow-list on."* |

**Verification method:** A1–A9, A11–A12 read directly from source this session (two read-only survey agents + hub direct reads; module-info/knobs/record headers quoted verbatim; the fan-out grep run repo-wide). A10, A13 carry the M9-charter's authoring-time verifications as execution-time STOP gates (re-verify, don't trust). A14 from direct bench-repo reads (fixture heads + corpus confirmation blocks quoted into the instruction). A15 from this session's Lock execution + Open Risks audit + Nick's settings.json direct read.
