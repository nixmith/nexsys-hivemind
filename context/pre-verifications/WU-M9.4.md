<!--
file: context/pre-verifications/WU-M9.4.md
purpose: Pre-verification pin sheet for the M9.4 arc (M9.4a issue-ready; M9.4b authored JIT after M9.4a's gate). Every load-bearing source assumption observed and pinned at the issue-time baseline. The Coder reads this BEFORE the instruction's STOP gates.
audience: Coder (M9.4a lane); PM.
state-type: pre-verification (point-in-time; re-verify at dispatch if HEAD moves)
status: CURRENT — verified 2026-07-04 by the v18 hub (direct reads + two survey agents, both layer-2 audited; three agent claims corrected against source, marked ⚠ below).
baseline: core `c856eab` (clean tree) · docs `01602fa` · bench `5ceff3b`. Rulings source: pm-handoff v18 beat-2 (quote-exact); adversarial folds: backlog M9.4 row + `context/audits/2026-07-04_M9-arc_adversarial-review_return.md` (with the hub's F-2 units annotation).
-->

# WU-M9.4 Pre-Verification (pin sheet)

**P1 SPLIT DECISION (v18, at authoring):** the ruled scope is five coherent chunks — over the smell-test line. **M9.4a** = the §1 moat-wiring freeze (ledger bridge + supersession + F-10 + classifier + v14 narrowing) + adoption-installed overrides + the minimal command write path + minimal composition wiring + the hardware-free hero loop (done-when). **M9.4b** (authored JIT after M9.4a's gate; never issued while an M9.4a gate is unresolved — §4b) = ReopenAction composition + NETWORK_UP await + supervisor-restart honesty leg + F-4 dedup window + F-7a/b IAS + F-8 + F-11/F-12/F-14/F-15 + N-4/N-5/N-8 + topologyScan-assert currency + bench-acceptance staging (hashed-TCLK formation pin + fallback clause ride the BENCH protocol notes, not code branching).

## A. Grounding corrections (source outranks the spine shorthand — three, all load-bearing)

1. ⚠ **`ConfirmationInstance` DOES NOT EXIST as a type** (glob-verified). The ruling (a) semantic realizes as: adoption installs a **tuned `ConfirmationPolicy`** (and, for timeout, a rebuilt `CommandDefinition` with tuned `defaultTimeout`) on the per-entity **`CapabilityInstance`** — records are immutable, no with-methods; reconstruction is the path. Zero new device-model types; the ledger/executor read paths are untouched. This is Nick's ruling realized purer than its shorthand: "the entire existing read path works unchanged."
2. ⚠ **The (b) bridge CANNOT be executor-side.** `CommandIssuedEvent` is FROZEN at 5 components — its own javadoc says "5-component CommandIssuedEvent (AMD-95 §2.B/§2.C)" (StandardActionExecutor.java:70, construction :272) — there is NO expectation slot, and adding one is an event-model AMD (contradicts ruling (a)'s zero-new-event-surface virtue + zero-mint). The derivation point is the LEDGER's `onCommandIssued` (which already resolves the `CapabilityInstance` at :554-565 and already receives `issued.parameters()`), with a parameter-decoder function injected from lifecycle. Capability-domain semantics preserved exactly; only the insertion point moves. Un-vetoed-default correction recorded at v18 beat 3.
3. ⚠ **`PortWatchdog` EXISTS (M9.2)** with `interface ReopenAction` at PortWatchdog.java:58, ctor-injected (:81); ":25 — the injected ReopenAction composes that at M9.4 wiring." The COMPOSITION is M9.4b's; the seam is built.

## B. Observed signatures (pinned at `c856eab` — STOP on divergence)

| # | Pin | Observed (file:line) |
|---|---|---|
| P1 | `ConfirmationPolicy` | `record ConfirmationPolicy(ConfirmationMode mode, List<String> authoritativeAttributes, Number defaultTolerance, long defaultTimeoutMs)` — device-model ConfirmationPolicy.java:29-34; defaultTolerance javadoc: "null when not applicable" |
| P2 | `ConfirmationMode` | `EXACT_MATCH, TOLERANCE, ENUM_MATCH, ANY_CHANGE, DISABLED` — ConfirmationMode.java:22-38 |
| P3 | `CapabilityInstance` | `record CapabilityInstance(String capabilityId, int version, String namespace, int featureMap, Map<String,AttributeSchema> attributes, Map<String,CommandDefinition> commands, ConfirmationPolicy confirmation)` — CapabilityInstance.java:33-41 |
| P4 | `CommandDefinition` | `record CommandDefinition(String commandType, List<ParameterSchema> parameters, int requiredFeatures, List<ExpectedOutcome> expectedOutcomes, Duration defaultTimeout, IdempotencyClass idempotencyClass)` — CommandDefinition.java:31-38 |
| P5 | `ExpectedOutcome` | `record ExpectedOutcome(String attributeKey, Expectation expectation, long timeoutMs)` — ExpectedOutcome.java:24-28 |
| P6 | `Expectation` sealed | `permits ExactMatch, WithinTolerance, EnumTransition, AnyChange` — Expectation.java:27-28; `WithinTolerance(double target, double tolerance)` evaluate = inclusive `|reported−target| ≤ tolerance`, non-numeric → NOT_YET (WithinTolerance.java:33-51); sole production ctor site = persistence `ExpectationDeserializer:130` |
| P7 | Ledger DISABLED bypass | `if (instance.confirmation().mode() == ConfirmationMode.DISABLED) { return; } // OPTIMISTIC bypass (AMD-90)` — StandardPendingCommandLedger.java:294-295 |
| P8 | Ledger optimistic-in-effect | empty `chooseOutcome` → `return; // nothing to confirm — optimistic in effect` — :297-300; `chooseOutcome` full method :572-584 (authoritative-attr preference, else first outcome) |
| P9 | Ledger timeout arm | `issued.confirmationTimeoutMs() > 0 ? ... : defaultConfirmationTimeoutMs` — :302-303; ctor `(EventPublisher, EntityRegistry, Clock, long)` :179-189; constructed at HomeSynapseCore.java:613-621 via `PendingCommandLedgerAssembly` (DEFAULT_CONFIRMATION_TIMEOUT_MS = 30000) |
| P10 | Ledger capability lookup | `resolveCapability(EntityId, String)` → `entityRegistry.findEntity` → iterate `capabilities()` for `commands().containsKey` — :554-565 |
| P11 | Ledger track insertion | `index(new Tracked(command, event.causalContext().correlationId(), null))` under `lock` — :308-313; **dual index byCommand + byEntity; removal must clean BOTH + drop empty byEntity sets (:496-512)** |
| P12 | Ledger evaluation | `onStateReported` iterates ALL in-flight on (entity), filters `targetAttribute().equals(reported.attributeKey())`, confirms EVERY match — :350-375. NO supersession logic exists (repo-grep verified; "supersed" hits are AMD-91 javadoc only) |
| P13 | Ledger outcome vocabulary | `OUTCOME_ACKNOWLEDGED = "acknowledged"` (:125), `OUTCOME_EXPIRED_ON_RESTART = "expired_on_restart"` (:132) — String field on `CommandResultEvent`; timeout publishes `CommandConfirmationTimedOutEvent` via `timedOut(...)` (:597-603, pollExpirations :400-416) |
| P14 | Ledger TRANSITION | `setMode` :251-253; `onCaughtUp` :271-274; `accumulateReplay` :426-441 (deadline = `base.plusMillis(timeoutMs)`); `classifyRestart()` :476-492 = switch on idempotency ONLY, no deadline test; `ReplayInFlight` CARRIES `Instant deadline` (:750-757) — F-10's fix is implementable in place |
| P15 | Ledger coerce | BOOLEAN/INT/FLOAT/STRING/ENUM/QUANTITY (+degrade) — :638-655; BOOLEAN via `Boolean.parseBoolean` (N-9 dormant) |
| P16 | `CommandIssuedEvent` | `record CommandIssuedEvent(Ulid targetEntityRef, String commandType, String parameters, int confirmationTimeoutMs, CommandIdempotency idempotencyClass)` — event-model CommandIssuedEvent.java:29-36, FROZEN (AMD-95 §2.B/§2.C) |
| P17 | Executor resolution | timeout: `definition.get().defaultTimeout().toMillis()` at StandardActionExecutor.java:302 (capability precedence, config fallback per javadoc :68-87); event built :272; `findEntity` :283. **Per-device tuning flows through the per-entity CapabilityInstance UNCHANGED — no executor edit** |
| P18 | Event-type constants | COMMAND_ISSUED :56, COMMAND_DISPATCHED :59, COMMAND_RESULT :62, STATE_REPORTED :70, STATE_CONFIRMED :79 — all pre-exist (EventTypes.java). **ZERO MINT for the whole M9.4 arc** |
| P19 | Count pins | 71 = EventTypes String constants (EventTypesTest:44-47) · 41 = core @EventType records (EventTypeAnnotationTest:165-177) · 53 = persistence payload-codec registry (EventTypeRegistryTest:46, EventCategoryMappingTest:149, JacksonWarmupTest:43). All three UNCHANGED by M9.4 |
| P20 | Parameter decoder | `PersistenceFactory.commandParameterDecoder()` returns `Function<String, Map<String, Object>>` (PersistenceFactory.java:298-299; already passed to IntegrationSupervisorAssembly at HomeSynapseCore.java:669) — the ledger-bridge injection type; NO new module dependency for automation |
| P21 | Protocol version band | `MAX_SUPPORTED_PROTOCOL_VERSION = 14` (EzspCoordinatorProtocol.java:75; PREFERRED/TARGET=13 :69/:73, MIN=8 :71); >MAX arm :234-242 throws PIE `"...newer than the supported band ...; the frame dialect is unknown to this adapter"` — the (d) narrowing point (14→13); the PIE text gains negotiated-version + AMD-96 reflash-contingency reference |
| P22 | v13-only wire codecs (F-1) | `sendUnicastLocked` writes tag u8 at [14] + length [15] (:718-738); `EzspIncomingMessage.parse` reads sender [14..15] + length [18] (:70-90). Bellows v14 re-fetched by hub: 0x0034 tag→u16; 0x0045 +timestamp u32/reorder ⇒ length at [30] (=1+11+2+8+1+1+1+1+4). v13 file carries NO override (inheritance); the v13 shape is bench-proven (fixtures green). **0x003F messageSentHandler UNCONSUMED in main — do not newly consume it** |
| P23 | `sendZclFrame` stub | throws UOE `"ZCL command dispatch is delivered in M9.4..."` — :477-481 (M9.4a fills); `topologyScan` stub :511-515 (STAYS, post-M9.4) |
| P24 | Checked seams | `startSession() throws PermanentIntegrationException`, `resumeStored() throws PIE`, `detect(...) throws PIE` ×2; frozen `CoordinatorProtocol` = no-throws (ISE cause-chained); `resetSession()` :296-307 (no throws; javadoc: ReopenAction must resetSession→startSession) |
| P25 | §G bound-and-drain | `MAX_PENDING_CALLBACKS = 1024` (:123), drop-oldest+WARN (:387-396), `drainPendingCallbacks()` :360-369 |
| P26 | `ExceptionClassifier` | three arms (PIE→PERMANENT :42-43; InterruptedException→SHUTDOWN_SIGNAL :45-46; OOM/LinkageError→PERMANENT :48-49; else TRANSIENT :53) — ExceptionClassifier.java:40-54; `ExceptionClassification` javadoc lists UOE under PERMANENT (:70-75) — the F-5 divergence |
| P27 | `ClusterHandler` (frozen public) | `List<AttributeReport> handleAttributeReport(int endpoint, int clusterId, Map<Integer,Object> attributes)` + `ZclFrame buildCommand(String commandType, Map<String,Object> parameters)` — ClusterHandler.java:30-59. Six impls (OnOff/LevelControl/ColorControl/OccupancySensing/PowerConfiguration/IasZone) ALL throw on buildCommand today; `ClusterHandlers.forDevice(IEEEAddress, Clock, ZoneType)` → `Map<Integer, ZigbeeClusterHandler>` (:34-48). **TWO-SEAM duality gotcha (MODULE_CONTEXT §5): buildCommand lands on the PUBLIC ClusterHandler; never duplicate ingestion normalization; never retire the public seam** |
| P28 | `CommandHandler` (integration-api, frozen) | `@FunctionalInterface` — single method processing a dispatched command (entity/command/parameters/causal context); invoked on the adapter's command executor; a throw classifies via ExceptionClassifier (CommandHandler.java — Coder reads verbatim; Doc 05 §8.1/§8.6). Registration via `CommandRoutingSubscriber` (M9.1; router publishes FAILURE results only — the adapter owns the eventual command_result, Doc 08 §3.10 step 7) |
| P29 | Composition root | `HomeSynapseCore(Path dbPath, Path configDir, HomeSynapseConfig config, Clock clock, HomeId homeId, PayloadCipher payloadCipher, List<IntegrationFactory> integrationFactories)` — 7-arg ctor :330-345; supervisor via `IntegrationSupervisorAssembly` :662-670 (takes the parameter decoder); `integrationSupervisor.start(integrationFactories)` :689. **Zero zigbee construction in lifecycle today (INV-CE-04 holds); app module-info ALREADY requires com.homesynapse.integration.zigbee — no module-info change to wire the factory** |
| P30 | `registerIntegrationSchema` | `void registerIntegrationSchema(String integrationType, String schemaJson)` — SchemaRegistry.java:68-69; ZERO call sites for zigbee (W10); resource EXISTS at integration-zigbee `src/main/resources/schema/zigbee-config-schema.json` |
| P31 | `IntegrationContext` | 12 components exactly (IntegrationContext.java:103-116): integrationId, integrationType, eventPublisher, entityRegistry, stateQueryService, healthReporter, configAccess, schedulerService, telemetryWriter, httpClient, security, discovery. **NO DeviceRegistry — ctor-injected separately into ZigbeeAdoptionSlice (R4: M9.4a wires the instance at the composition root)** |
| P32 | Adoption seams | re-link on IEEE match: `deviceRegistry.findByHardwareIdentifier(HARDWARE_NAMESPACE, ieee.toHexString())` → `relink(...)` → LINKED (ZigbeeAdoptionSlice.java:137-142); capability attachment at entity creation via `classification.get().capabilities()` (:216-241); `relink` body re-links registries maps + publishes availability_changed, does NOT today re-install capabilities (the pin-2 extension point); adopt() lock structure :182-191 (check) / :244-251 (proposal removed at end) — the F-11 race (M9.4b) |
| P33 | Device cache | `record ZigbeeDeviceRecord(ieeeAddress, networkAddress, nodeDescriptor, endpoints, manufacturerName, modelIdentifier, powerSource, lastSeen, interviewStatus, String matchedProfileId)` — 10 components (:41-51); `matchedProfileId` written at `recordInterview` (:128); accessors `device(ieee)` :211-219 + `deviceForNetworkAddress(nwk)` :227-236 — the dispatch identity join reads BOTH; `reindex` :280-286 (F-6 victim invalidation point); `writeLocked` I/O-under-lock :288-349 (F-14, M9.4b) |
| P34 | EndpointClassifier construction | `new CapabilityInstance(cap.capabilityId(), cap.version(), cap.namespace(), 0, cap.attributeSchemas(), cap.commandDefinitions(), cap.confirmationPolicy())` — EndpointClassifier.java:131-133 (capability-DEFAULT policy; the override installer replaces this at adoption) |
| P35 | StandardCapabilities defaults | set_brightness: EMPTY expectedOutcomes `List.of()` + `ConfirmationPolicy(TOLERANCE, ["brightness"], 2, 5000L)` (:165-181); set_color_temperature: EMPTY + `(TOLERANCE, ["color_temp_kelvin"], 50, 5000L)` (:191-205) — the F-3 legs; the bridge unlocks BOTH without touching this file |
| P36 | Test harness | `FakeNcp` = `Function<byte[], List<byte[]>>` at the ASH byte layer (production `AshFrameAccumulator` parse; pluggable EZSP handler; wraps responses as ASH DATA) — deterministic, NO clock control (fixture-clock discipline is the test's job); `FixtureReplayTest` constructs `ZclIngestionUnit` directly with lambda seams (:60-101) — the hero E2E drives the FULL wire path instead (FakeNcp scripts sendUnicast status-0 response + incomingMessageHandler callback frames); `RunPipelineReplaySafetyTest` (lifecycle test) = seeded-log composition-root replay, RecordingIntegrationFactory routable-fake pattern, LIVE positive control (:90-135) — the replay-purity extension point |
| P37 | EzspProtocolTest | D-M92-6 stub-inventory asserts sendZclFrame + topologyScan throw UOE — M9.4a FILLS sendZclFrame ⇒ that assert flips (format #12 declared delta); topologyScan assert RETAINED |
| P38 | Profiles (bundled) | zigbee-profiles.json: Hue `identify` (READBACK_ONLY/NONE/**UNCONFIRMABLE**) + `color_loop` (NONE/NONE/**UNCONFIRMABLE**) (~:70-90) — the immediate-honest-verdict pair; brightness/CT CONFIRMABLE with measured latencies; SNZB occupancy block. Values consumed FROM THE JSON + fixtures, never re-typed from prose |
| P39 | settings.json | allow contains `"Bash(./gradlew:*)"`; deny contains git commit/push/merge/reset/rebase + `Bash(gradle *)` + `Bash(javac *)`; NO gradlew deny — **the proven allow-list is intact; dispatch precondition MET** |
| P40 | Module-graph knobs | maxHeight = 8 with the do-not-raise-again note (root build.gradle.kts :21-28); allowed/restricted arrays :29-86 — BOTH UNTOUCHED by M9.4a (no new module edges: automation gains nothing; zigbee gains nothing; lifecycle/app already require what M9.4a wires) |
| P41 | turn_on declared outcome | `StandardCapabilities.onOff()` declares `new ExpectedOutcome("on", new ExactMatch(new BooleanValue(true)), 5000L)` on `turn_on` (:142-145) — the hero turn_on confirm rides the EXISTING `chooseOutcome` path; §1.3 derivation is genuinely only for the parameterized pair (set_brightness / set_color_temperature) |
| P42 | Test-harness homes | `RecordingIntegrationFactory` = lifecycle `src/test` (lifecycle-test-local); `FakeNcp` = integration-zigbee `src/test` (module-local); `testing/integration-tests/build.gradle.kts` already consumes `testImplementation(project(...))` + `testFixtures(project(":core:*"))` + `testing:test-support` — the established cross-module test-sharing pattern is java-test-fixtures. Production composition root = `app/homesynapse-app/src/main/java/com/homesynapse/app/Main.java` (:71 constructs HomeSynapseCore) |

## C. module-info.java VERBATIM (all six touched/read modules — zero JPMS diffs proposed in M9.4a)

`core/device-model`: `module com.homesynapse.device { requires transitive com.homesynapse.value; requires com.homesynapse.event; requires transitive com.homesynapse.platform; exports com.homesynapse.device; }`
`core/automation`: `module com.homesynapse.automation { requires transitive com.homesynapse.platform; requires transitive com.homesynapse.event; requires transitive com.homesynapse.device; requires transitive com.homesynapse.state; requires com.homesynapse.value; requires transitive com.homesynapse.event.bus; requires org.slf4j; exports com.homesynapse.automation; }`
`core/event-model`: `module com.homesynapse.event { requires transitive com.homesynapse.value; requires transitive com.homesynapse.platform; exports com.homesynapse.event; }`
`integration/integration-api`: `module com.homesynapse.integration { requires transitive com.homesynapse.platform; requires transitive com.homesynapse.event; requires transitive com.homesynapse.device; requires transitive com.homesynapse.state; requires transitive com.homesynapse.persistence; requires transitive com.homesynapse.config; requires transitive java.net.http; exports com.homesynapse.integration; }`
`integration/integration-runtime`: `module com.homesynapse.integration.runtime { requires transitive com.homesynapse.integration; requires transitive com.homesynapse.event.bus; requires org.slf4j; exports com.homesynapse.integration.runtime; }`
`integration/integration-zigbee`: `module com.homesynapse.integration.zigbee { requires transitive com.homesynapse.integration; requires com.fazecast.jSerialComm; requires org.slf4j; requires com.fasterxml.jackson.databind; exports com.homesynapse.integration.zigbee; }`
`lifecycle/lifecycle`: (17 directives, incl. `requires com.homesynapse.automation;` and `requires com.homesynapse.integration.runtime;` — NO zigbee edge, and M9.4a adds NONE: the factory is constructed in `app`) — read verbatim at start.
`app/homesynapse-app`: 15 requires incl. `requires com.homesynapse.integration.zigbee;` ALREADY PRESENT.

**Module-info pin validated against every mandated M9.4a behavior (format #9):** ledger bridge = a `java.util.function.Function` injection (no edge); zigbee CommandHandler/factory = types already visible via integration-api (no edge); jackson stays interior to zigbee (already present); app already sees zigbee. **Pinned: ZERO module-info diffs, ZERO build.gradle.kts dependency changes in M9.4a.** If the Coder finds a forced edge, that is a STOP, not an improvisation.

## D. The 14 FOLD-M9.4 items → target files (a/b assignment)

| Fold | Target | Unit |
|---|---|---|
| F-1 (d ruling) | EzspCoordinatorProtocol.java:75 (MAX 14→13) + :234-242 PIE text + EzspProtocolTest | **a §1** |
| F-2 (c ruling) | StandardPendingCommandLedger onCommandIssued (+expiry at track insertion) + tests | **a §1** |
| F-3 (b ruling) | StandardPendingCommandLedger derivation + assembly + HomeSynapseCore injection + tests | **a §1** |
| F-5 (classifier ruling) | ExceptionClassifier + ExceptionClassification javadoc + 3 pinned tests | **a §1** |
| F-10 | classifyRestart deadline-in-future arm + tests | **a §1** |
| F-6 | dispatch identity join (cache-as-hint + reindex victim invalidation) | **a §3** |
| F-9 | ZclCodec bool-marker + PowerConfigurationHandler battery-marker guards + tests | **a §3** |
| N-6 (BINDING) | zigbee CommandHandler causation chaining + ledger oldest-deadline fallback | **a §3/§1** |
| F-4 | ReportDeduplicator time-bound + unsolicited-only scope + chain-drop regression test | **b** |
| F-7a/b | ZclIngestionUnit cmd-0x01 + enroll-response send + ReportingConfigurator posture fact | **b** |
| F-8 | handlersByDevice invalidation at adoption/zone-type-learned/rejoin | **b** |
| F-11 | ZigbeeAdoptionSlice atomic proposal claim | **b** |
| F-12 | ProfileLoadException catch at match site → no-match+WARN | **b** |
| F-14 / F-15 | cache write-outside-lock + backoff / loader degradeRule fail-closed | **b** |

(F-13 = DEFER-W2; MODULE_CONTEXT divergence note rides M9.4a's P2.)

## E. F-2 acceptance arithmetic (BOTH directions — pinned)

Fixture (bench `2026-07-01_hue-lca017_confirmation-windows_event-stream.json`): `move_to_color_temp` 161 → 216 → 221 mireds (20:23:39.573/:40.069/:40.575), one report 221 @ :48.999; fixture notes on 161+216: "superseded — never confirmed"; verdict line: "superseded expectations must expire, not false-fail." **Capability domain (Kelvin, Math.round(1e6/m)): 6211 / 4630 / 4525 K; report = 4525 K.** Direction 1 (false-FAIL guard, the fixture triple): with ±50 K, cmd(4630) misses by 105 K and cmd(6211) by 1686 K — WITHOUT expiry both would render timeout/false-fail; WITH expiry both render the superseded disposition, and cmd(4525) alone confirms (|0| ≤ 50). Direction 2 (false-CONFIRM guard, synthetic pair): two LIVE set_color_temperature commands ≤50 K apart (e.g. 4550 K then 4525 K), one report 4525 — WITHOUT expiry the superseded 4550 would CONFIRM (|4550−4525|=25 ≤ 50); WITH expiry it renders superseded, only 4525 confirms. Both banned by ratified Doc 08 §3.6 caveat 3.
