<!--
file: context/pre-verifications/WU-M9.7-W2.md
purpose: Pre-verification artifact for M9.7-W2 (Wave-2 profiles + classification/ingestion completion) — every load-bearing source-state assumption in the instruction, verified at core HEAD `1aa809d` on 2026-07-18 by the v33 hub (remote §12 routes: Filesystem-MCP host reads + git object reads; no worktree reads). The Coder re-verifies each row at execution time (the STOP-on-Mismatch gates in the instruction mirror the load-bearing subset).
audience: Coder (read before executing the M9.7-W2 instruction); PM (audit basis).
state-type: pre-verification (point-in-time; goes stale with any core commit after 1aa809d).
status: CURRENT — verified 2026-07-18, core HEAD 1aa809d (worktree clean; a transient stray-keystroke corruption of StandardAvailabilityTracker.java was found and restored to exact HEAD content the same day — pm-handoff v33 beat 1).
-->

# Pre-Verification — WU M9.7-W2 (verified 2026-07-18 at core `1aa809d`)

| # | Assumed source element | Observed signature (2026-07-18) |
|---|---|---|
| P1 | Shipped `integration/integration-zigbee/src/main/resources/zigbee-profiles.json` | EXACTLY 2 profiles: `philips_hue_white_color_a19` (exact_model + fingerprint; 5 confirmation entries) + `sonoff_snzb_03p` (exact_model + fingerprint; `confirmation: []`). `schemaVersion {1,0}`; root `provenance` object (source/corpus/benchCommit/capturedAt/note). 4,623 bytes. |
| P2 | `ZigbeeProfileLoader` parse contract | `profiles` array required; per profile: `profileId`, `priority` (`path(...).asInt(0)`), `matches` non-empty with `type` ∈ {`exact_model` (keys `manufacturer`,`model`), `model_wildcard` (`manufacturer`,`modelPrefix`), `fingerprint` (`manufacturer`,`model`,`endpoints[]` non-empty with `profileId`,`deviceType`,`inClusters`,`outClusters` — flexibleInt/intSet, hex strings accepted)}; `category` via `DeviceCategory.valueOf`; `confirmation` entries: `capability`, `confirmationMode`, `authoritativeAttribute` (optional), `reportsAuthoritative`, `reportingPosture`, `confirmability`, `recommendedTimeoutMs` (`asLong(0)`), `degradeRule[]` (unknown value ⇒ ProfileLoadException, F-15), `notes` (optional). `provenance` is NOT parsed (documentation-only). Unknown-major schemaVersion fail-closed. `BUNDLED_RESOURCE = "/zigbee-profiles.json"`. |
| P3 | `EndpointClassifier.classify` (the gap being closed) | Device-type constants: 0x0100/0x0101/0x010C/0x010D/0x0107/0x0302/0x0051 — NO 0x0402, NO 0x0010. The `0x0302` arm installs `temperatureMeasurement()` ONLY. `binarySensor(...)`: occupancy outranks IAS; IAS-without-occupancy ⇒ `motion()` unconditionally (zoneType never consulted). `fallback(...)`: onOff+level→light; onOff→SWITCH; occupancy/ias→binarySensor; **else `Optional.empty()` — battery-only classifies to NOTHING**. `hasIdentify` post-processing adds identify to any non-empty classification; "0x0003 alone never invents an entity". |
| P4 | `ClusterHandlers.forDevice(device, clock, zoneType)` | EXACTLY 6 entries: OnOff, LevelControl, ColorControl, OccupancySensing, PowerConfiguration, IasZone. Javadoc: unhandled clusters (incl. 0xFC01/0xFC04/0xFC57) skip gracefully. NO TemperatureMeasurement / RelativeHumidity handler files exist anywhere in the module. |
| P5 | `IasZoneHandler` | Ctor `(IEEEAddress, Clock, ZoneType)`; `attributeKey()`: MOTION→`detected`, CONTACT→`open`, WATER_LEAK/SMOKE/VIBRATION→`active`. Constants: `ATTRIBUTE_ZONE_TYPE = 0x0001`, `ATTRIBUTE_ZONE_STATUS = 0x0002`, `ZONE_STATUS_ALARM1 = 0x0001`; enroll request/response constants present (device-initiated enrollment answered by ingestion). |
| P6 | zoneType flow in `ZclIngestionUnit` | `DeviceResolver.zoneTypeFor(IEEEAddress)` seam at :97; wire-learn at :497–:514 (`zigbee.ias_zone_type_learned: device={} zoneType={}`); learned-first at :545 (`learned != null ? learned : resolver.zoneTypeFor(device)`). The adapter's resolver implementation at `ZigbeeIntegrationAdapter`:1026–1028 is the documented stub: `return ZoneType.MOTION;`. |
| P7 | `StandardCapabilities` (core/device-model — no device-model change needed) | Public static factories exist for everything this WU installs: `temperatureMeasurement()` (:258), `humidityMeasurement()` (:276), `contact()` (:350), `motion()` (:368), `battery()` (:407), `occupancy()` (:386), `identify()` (:207), `onOff()` (:138). Read each capability's `AttributeSchema` for the canonical attribute key + value type before writing handler output — never invent keys. |
| P8 | `DeviceProfile` record shape | 10 components; `matches` is `Set<MatchCriteria>`; 6 nullable collection fields incl. `confirmation` (conditional defensive copy — `List.copyOf(null)` NPEs). `Fingerprint.matches()` throws UOE (Wave-2 fingerprint entries are INERT tier-0 data; registry arms treat as no-match). Registry precedence: Fingerprint > ExactModel > ModelWildcard, then USER > RUNTIME > BUNDLED, then priority desc, then profileId asc. |
| P9 | Count-pin sweep (bundled profiles) | NO existing test pins the bundled profile count (searched `zigbee-profiles`, `allProfiles()` across the module's tests: `StandardDeviceProfileRegistryTest` `hasSize(1)` ×2 are on synthetic registries; `ConfirmationOverrideInstallerTest` loads the bundle via the real loader (P38 header) and looks up specific profiles — additive entries do not break it). The Coder re-runs this sweep at execution. |
| P10 | `module-info.java` (verbatim in the instruction §Spec) | `module com.homesynapse.integration.zigbee { requires transitive com.homesynapse.integration; requires com.fazecast.jSerialComm; requires org.slf4j; requires com.fasterxml.jackson.databind; exports com.homesynapse.integration.zigbee; }` — ZERO change expected in this WU (all new types package-private; no new deps). |
| P11 | Config schema (context only — NOT touched) | `adopt_devices`: array of strings, "'0x' + 16 hex chars, the log's own rendering"; adapter reads ONCE at initialize; absent ⇒ `Set.of()` (conservative default is LAW). `permit_join_duration`: absent key opens NOTHING; present ⇒ one window per boot, clamped [1,254]. Accept-list entries ride the joins night (EUI-64s are unknowable pre-announce — no housing prints one; Phase-0 finding). |
| P12 | Adoption stickiness (the sequencing rationale) | No `device_removed` emitter exists (AMD-99 R-D: the projection honors it; nothing emits it); adopted entities never re-classify (`relink` reinstalls overrides only; `applyPostureRouting` re-emits only differing ROUTED capabilities). A wrongly-typed adoption is durable — THIS WU must land + deploy BEFORE any Wave-2 device is accept-listed. |
| P13 | Wave-2 identity + fingerprint sources | Exact-model keys confirmed off housings 2026-07-15 (operator return §3.6): `("SONOFF","S31 Lite zb")` · `("eWeLink","SNZB-02P")` · `("eWeLink","SNZB-04P")` · `("eWeLink","SNZB-01P")`. Fingerprints = zigpy/zha machine captures (dossiers §2.2/§3.2/§4.2; the 04P's second custom cluster is CAPTURE-LED `0xFC57` over the quirk comment's 0xFC37 — dossier §6.3-g); the S31's fingerprint is WITHHELD (the 0xC05E-vs-0x0104 profileId conflict is [CONFIRM-ON-BENCH] — dossier §6.1-a: "the profile field is [CONFIRM-ON-BENCH] before any fingerprint ships"). |
| P14 | Baseline suite | integration-zigbee suite last recorded 452/0/0 forced-fresh at `3590fca`→`1aa809d` (M9.6-RO-R1 record). Re-derive forced-fresh at dispatch HEAD before writing code (gate G-W2-6). |

**Refuted-assumption record (why this WU exists at this scope):** the v33 charge's data-only framing assumed the classification/ingestion layer was Wave-2-ready. Source verification 2026-07-18 refuted three legs (P3/P4/P6): no temp/humidity ingestion, IAS classifies motion-unconditionally, battery-only classifies to empty (which with the M9.5-DURb half-registration repair arm would put an accept-listed 01P into a lawful re-propose loop). Nick ruled 2026-07-18: ONE WU carries profiles + the completion code; the joins night gates on it.
