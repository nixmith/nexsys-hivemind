<!--
file: context/pre-verifications/WU-R1R2.md
purpose: Pre-verification artifact for the R-1/R-2 coding instruction (the M3.6d class — ≥3 prerequisite source-state assumptions). Each row: the assumed source element → what the hub observed at authoring (2026-08-20, HEAD c091f7c) → what the CODER must re-verify at execution before implementing. The Coder reads this FIRST and STOPs on any mismatch (evidence over instruction).
audience: the R-1/R-2 Coder lane (host-CC); the hub (audit).
verified-at: 2026-08-20 evening, by the v54 hub, against core c091f7c (git objects + staged whole-file reads; instrument notes inline).
-->

# WU-R1R2 — Pre-Verification (verified 2026-08-20 @ c091f7c)

| # | Assumption | Observed at authoring (hub) | Coder re-verifies |
|---|---|---|---|
| P1 | The F-23 wiring: `BusMetrics.jfr()` wired unconditionally into the production bus | `lifecycle/lifecycle/src/main/java/com/homesynapse/lifecycle/HomeSynapseCore.java` — `BusMetrics jfrMetrics = BusMetrics.jfr();` immediately after `PersistenceFactory.start(...)`, passed into `new InProcessEventBus(...)` (region ~:480–:492 at c091f7c; the v51 beat-6 record cites :485) | The exact line numbers at your checkout + that `noop()` still serves only `InMemoryEventBus`/tests (grep the wiring sites) |
| P2 | The jdk.jfr consumer set in event-bus | EIGHT files: `BusMetrics.java`, `BusMetricsJfr.java`, and SIX `Bus*Event.java` JFR event classes (`git grep -ln "jdk.jfr\|BusMetricsJfr" -- "*.java"`) — note the v51-era record said "three JFR event classes"; the bytes say six today | Re-run the grep; the fix covers the FULL consumer set whatever the count is at your checkout |
| P3 | The single-jar jdeps defect site | `distribution/image/build-image.sh` :79–:83 — `--print-module-deps` with `--class-path "${IMAGE}/lib/*"` and ONE root jar (`homesynapse-app*.jar`); the :75–:77 comment claims "the full jar set" (comment/code divergence = part of the defect); FLOOR at :85 has NO `jdk.jfr` | Line numbers + that FLOOR still lacks `jdk.jfr` |
| P4 | run-smoke.sh has NO write-path/integration assert | Checks 1–8 read whole 2026-08-20: install · active · health probe · token · auth-enforced · dashboard-serve · stop · uninstall. Nothing probes event persistence or the publish path | Confirm at your checkout; your new check gets the next number in the file's own style |
| P5 | The packaged data/log geometry | `HS_DATA=/var/lib/homesynapse` (user's home, 0700; H3 return); token `/var/lib/homesynapse/config/initial_api_token`; app stdout at `/var/log/homesynapse-stdout.log` + `journalctl -u homesynapse.service` (run-smoke's own dump_logs) | The exact `HS_DATA`/`HS_*` values from `distribution/common.sh` + the events-DB file path under it (expected `…/data/events.db`-class — pin the real path from config/source, do not guess) |
| P6 | The events schema | `CREATE TABLE IF NOT EXISTS events` in `core/persistence/src/main/resources/db/migration/events/V001__initial_event_store_schema.sql:28`; sqlite3 CLI is PRECEDENTED in this exact workflow (`.github/workflows/install-smoke.yml` update-smoke step apt-installs sqlite3) | Table name + that the boot of a working runtime persists ≥1 event row (find the boot-time publisher — the `system_started`-class emission — at source; if boot persists zero rows on a healthy runtime, the row-count probe form is WRONG: STOP and flag, per the D-6 line) |
| P7 | The failure observable on the broken artifact | The v51 beat-6 record: "every publish's metrics emission touches the absent module … the error escapes uncaught"; H3 return §9.4: `publishLifecycle()` throws on the packaged runtime; yet install-smoke is GREEN (boots to RUNNING and idles) — so the throw is post-health/async | Locate `publishLifecycle`'s throw path + what it logs (the exception class + logger) so the log-scan probe greps for the REAL signature, not an assumed one |
| P8 | The install-smoke twins | `.github/workflows/install-smoke.yml` (the live gate) + `distribution/ci/install-smoke.yml` (the delivered copy; its own WIRING-SEAM comment says the hub copies it) — both exist; sync state UNVERIFIED at authoring | `diff` the two BEFORE editing; edit BOTH to stay in sync; **hardlink pre-check first** (`stat -c %h` on every file you will edit — env-model §12: nlink>1 ⇒ break with a byte-identical copy and flag; the FE twin `ci/frontend.yml` was nlink=2, so check, don't assume) |
| P9 | The held card's state (the RED fixture) | H3 Stage-2: the card runs the clean image with `homesynapse_0.1.0+gd26777c_arm64.deb` installed (the KNOWN-BAD artifact — B-2's hold is load-bearing); coordinator NEVER attaches to it (the SD-5 rail) | Operator-side (the §OP block): the card boots and the service starts before the RED probes run |
