<!--
file: context/pre-verifications/WU-R3.md
purpose: the pre-verification artifact for R-3 / PKG-E2E-1 (the SKELETON's §4 charge, P1–P9) — every assumed source element with its observed signature, pinned at core `dec35be` (the R-3b baseline) and at the card-sitting ⏺s of 2026-08-23. Written by the hub at v57 beat 1 (2026-08-26) AHEAD of the R-3a run; the R-3b Coder lane reads it before touching `distribution/systemd/homesynapse.service`. The rows marked ⏺ R-3a are re-verified from the Saturday record before the R-3b instruction is stamped ISSUE-READY.
audience: the R-3b Coder lane · the hub.
status: PINNED at dec35be (2026-08-26); ⏺ R-3a rows fill Sat 2026-08-29.
-->

# WU-R3 — pre-verification (P1–P9)

| # | Assumption | Observed at `dec35be` (2026-08-26, read at source) | ⏺ / launch |
|---|---|---|---|
| P1 | The unit's hardening block + the RAMP seam | `distribution/systemd/homesynapse.service`: `Type=exec` :23 · `StartLimitIntervalSec=300` :19 / `StartLimitBurst=5` :20 · `ExecStartPost=/opt/homesynapse/libexec/health-probe.sh --wait --timeout 90 --health-path /health` :53 (R-9's probe) · `Restart=on-failure` :64 / `RestartSec=10` :65 · `ProtectSystem=strict` :73 · `PrivateTmp=yes` :75 · `RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6` :88 · `SystemCallFilter=@system-service` :89 · `SystemCallErrorNumber=EPERM` :90 · the RAMP comment :91–:95 (its sketch names `DeviceAllow=/dev/ttyUSB0 rw` — a NODE path; R-3a measures the CLASS form `char-ttyUSB`) · **`PrivateDevices=yes` :96** — the edit seam · the DANGER block (Type=notify) :103–:121 stays · no `DeviceAllow`, no `SupplementaryGroups`, no `DevicePolicy` anywhere in the file | R-3b lane: re-read the line numbers at checkout (the file is unchanged since R-9; R-7b touched no unit line) |
| P2 | The adapter data dir on the packaged path | `app/homesynapse-app/src/main/java/com/homesynapse/app/Main.java:85` `Path zigbeeDataDir = baseDir.resolve("data").resolve("zigbee");` (`:69` the events DB beside it) ⇒ `/var/lib/homesynapse/data/zigbee/`; bench ⇒ `/home/homesynapse/hs-bench/data/zigbee/` | ⏺ 2026-08-23: `/var/lib/homesynapse/data/zigbee/` EXISTS on the held card — written at the 16:54 ET boot with NO radio (F-S11); the R-3a packet §6 moves it aside before the clone |
| P3 | Custody file set | bench listing (2026-08-21 enrichment): `.root-key` 32 B 0400 · `scope_keys.json` 248 B · `secrets.enc` 568 B · `zigbee-network.json` 122 B · `zigbee-devices.json` LIVE (4551 B on 08-21) · `scope_nonce_counters.json` possible | ⏺ R-3a §1: the tar-time listing (the cache size is live; the nonce counter's presence decides the tar list) |
| P4 | The form-vs-resume decision | `ZigbeeIntegrationAdapter.resumeOrForm()`: `load().isPresent()` ⇒ `resumeStored()` (INFO `zigbee.network_resumed`) else `formNetwork()` (INFO `zigbee.network_formed`); `formation.resume()` throws on key-missing / NVRAM mismatch (INFO `zigbee.network_parameter_mismatch`; never re-forms). Token census at source (2026-08-26): `zigbee.network_resumed` · `zigbee.network_formed` · `zigbee.network_parameter_mismatch` · `zigbee.port_identity_captured` · `zigbee.device_cache_loaded` · `zigbee.adopt_list_loaded` · `zigbee.adoption_maps_rehydrated` · `zigbee.device_relinked` · `zigbee.device_proposed` · `zigbee.proposal_accepted` · `zigbee.device_adopted` · `zigbee.device_announce` · `zigbee.transport_unbound` · `zigbee.transport_failed` — all present under `integration/integration-zigbee/src/main/java` | unchanged at checkout (the R-3b lane re-greps the fourteen tokens) |
| P5 | Port resolution | `resolvePort()`: configured `serial_port` wins, else the VID:PID locator `10c4:ea60` | BANKED 2026-08-21: `integrations/zigbee.yaml:1` `serial_port: /dev/zigbee` · `:2` `channel: 20` (inert on resume) ⇒ the udev rule travels to the held card (R-3a §6) |
| P6 | Config layout | `YamlLoader`: root `homesynapse.yaml` + `integrations/` include dir under `$HOMESYNAPSE_HOME/config`; `!secret`/`!env`/`!include` tags exist | BANKED 2026-08-21: NO `!secret`/`!env` in either bench file; the absolute-path glance rides R-3a §1 |
| P7 | The packaged unit is the repo unit | `build-deb.sh` copies `distribution/systemd/homesynapse.service` verbatim; `postinst` enables + starts; no unit edit at install | the R-3b rebuild's `.deb`: `dpkg-deb -c` + `systemctl cat` glance at R-4 |
| P8 | The held card's state | ⏺ 2026-08-23 (the sitting return §3.3/§6): `dpkg Version: 7c9e4fa` = `/opt/homesynapse/VERSION` · `initial_api_token` PRESENT 44 B `-rw-r--r--` Aug 13 07:35 (F-S9; mode = F-S10) · `api_tokens` 132 B `-rw-r--r--` · `home_id` 26 B · store **14 rows** at the last boot · journal discriminator **0** · `data/zigbee/` PRESENT (F-S11) · curl PRESENT · 95 OS packages pending (F-S12 HOLD-PATCH) · the `7c9e4fa` probe reads `/api/v1/entities` (F-S14) | ⏺ R-3a §2 (the rig glance) — ROWS-0 |
| P9 | The coordinator identity | SONOFF Dongle Plus MG24 `10c4:ea60` serial `0ae2dd7cecf8ef11b80168135c2a50c9`; hub `3-2.4` Port 2; by-id `usb-SONOFF_SONOFF_Dongle_Plus_MG24_0ae2dd7cecf8ef11b80168135c2a50c9-if00-port0` (F-S20, byte-identical pre/post move) | ⏺ R-3a §8 (`lsusb` + by-id on the held card) + §11 (the bench's `port_identity_captured` line) |
| P10 (new) | The install-source artifact (Block I) | `distribution/common.sh` `hs_version()` :57–:96: bare-id describe ⇒ `0.1.0+git<TZ=UTC %cd YYYYMMDD.HHMMSS>.g<id>`; `dec35be` committer date UTC = `20260823.231355` ⇒ **`0.1.0+git20260823.231355.gdec35be`** (H12 prediction); `install-smoke.yml` :106–:131 the echo step prints `hs_version=<V>` and `version-grammar echo green: <V> (… sha256 <64hex>  homesynapse_<V>_arm64.deb)` — the origin hash of the hash chain | ⏺ the run-detail paste (run id + both lines) → R-3a §4 I-1 |
| P11 (new) | The store-preservation assertion | `distribution/update/update-smoke.sh:75` asserts `POST_COUNT -ge PRE_COUNT` (zero LOSS) + `PRAGMA integrity_check` + `home_id` stable — never "unchanged": every boot appends lifecycle rows (Δ+4 at Block 2) | R-3a §4 I-3 expects ROWS-AFTER ≥ ROWS-BEFORE |
