<!--
file: context/pre-verifications/WU-R6R8.md
purpose: Pre-verification artifact for the R-6/R-8 TOKEN-OPS coding instruction (the M3.6d class — ≥3 prerequisite source-state assumptions). Each row: the assumed source element → what the hub observed at authoring (2026-08-21, core HEAD 7c9e4fa, git-object reads) → what the CODER re-verifies at execution before implementing. The Coder reads this FIRST and STOPs on any mismatch (evidence over instruction).
audience: the R-6/R-8 Coder lane (host-CC); the hub (audit).
verified-at: 2026-08-21 evening, by the v55 hub, against core 7c9e4fa (git show HEAD:<path> + git grep; line numbers are 7c9e4fa's).
-->

# WU-R6R8 — Pre-Verification (verified 2026-08-21 @ 7c9e4fa)

| # | Assumption | Observed at authoring (hub) | Coder re-verifies |
|---|---|---|---|
| P1 | The token store's public surface + file names | `api/rest-api/.../OpaqueTokenStore.java` (379 lines): `TOKEN_FILE_NAME = "api_tokens"` :70, `INITIAL_TOKEN_ARTIFACT = "initial_api_token"` :73; `validate` :137, `claimsFor` :159, `mint(displayName, scopes, siteId)` :179, `ensureInitialToken()` :206 (no-op when `byHash` non-empty), `revoke(keyId)` :230 (entry-level `setValue` to a revoked copy + `persist()`), `activeKeyCount()` :250; mutations under `writeLock` (`ReentrantLock` :91); `byHash` a `ConcurrentHashMap<String, TokenRecord>` :94; the private record `TokenRecord(keyId, displayName, createdAt, expiresAt, scopes, siteId, revoked)` | names + lines at your checkout; that NO `rotate`/`summaries`/request-file code exists yet (`git grep -n "token_ops\|rotate(" -- api/rest-api/src/main` returns nothing) |
| P2 | `persist()` is NOT atomic and the javadoc says it is | `persist()` :306–:325 builds the whole file and calls `Files.writeString(tokenFile, …)` — a truncate-and-write; the class javadoc at :61 says "rewrite the backing file atomically"; `writeArtifact()` :327–:335 likewise `Files.writeString` with default (umask) permissions | both sites unchanged; no temp/move idiom present |
| P3 | Opening the store has no side effects | the constructor :121–:127 only resolves the two paths (:124–:125) and calls `load()` (:126 → :262) (reads `api_tokens` if it exists; malformed lines WARN-skipped); the mint happens ONLY in `ensureInitialToken()`, called separately by the composition root | the constructor body at your checkout — the `token status` CLI relies on this |
| P4 | The file format | `parseLine` :286–:303: exactly 8 tab-separated fields (`:288 if (f.length != 8)`): `keyId · sha256hex · b64(displayName) · createdMillis · expiresMillis (NEVER_EXPIRES = -1L, :83, private) · b64(scopes csv) · b64(siteId) or empty · revoked "1"/"0"`; lines blank or starting `#` are skipped in `load()` (:274) | the field order + the sentinel's value (`NEVER_EXPIRES` constant) — the `summaries()` record must map 1:1 |
| P5 | Every route is auth-gated, `/internal/*` included | `RestFilters.installAuth` :433 registers `app.before(ctx -> authorize(ctx, authMiddleware, rateLimiter))` :441 — a catch-all over EVERY path; the single exemption is `isPublicShellRequest` (GET/HEAD on `/`, `/dashboard`, `/dashboard/**`); identity is stored at `ctx.attribute(IDENTITY_ATTRIBUTE, identity)` :464 with `IDENTITY_ATTRIBUTE = "hs.api.identity"` :388; `ApiKeyIdentity(keyId, displayName, createdAt)` (`ApiKeyIdentity.java:36`); `ApiKeyClaims.fullAccess()` :57–:59 = `scopes.contains("*")` | that the exemption list has not grown to cover `/internal/*`; that `claimsFor(keyId)` resolves from the identity's `keyId` |
| P6 | The `/internal` registration + handler idiom | `RestFilters.installAdminEndpoints(Object javalinApp, Object bus, ReadinessSource, StateQueryService, LongSupplier, LongSupplier, int, Clock)` :198 — `Objects.requireNonNull` guards, `(Javalin) javalinApp` cast, `app.get("/internal/dlq", …)` :215, `app.get("/internal/projection", …)` :217; `DlqStatusEndpoint implements Handler` :99, `handle(Context)` :124, envelope `body.put("data"…)`/`("meta"…)` :166–:167, `ctx.status(200)` :169, headers :170–:174, `ctx.json(body)` :175; problems via `static ApiException problem(ProblemType type, String detail)` :534 and the RFC 9457 serializer :540–:552; `ProblemType` constants incl. `NOT_FOUND` (:38, 404), `INVALID_PARAMETERS` (:78, 400), `AUTHENTICATION_REQUIRED` (:88, 401), `FORBIDDEN` (:97, 403) | the installer signature + the handler idiom at your checkout; the test fake `RecordingEndpointContext` exists under `api/rest-api/src/test/java/com/homesynapse/api/rest/` (used by `DlqStatusEndpointTest`) |
| P7 | The composition order in the root | `HomeSynapseCore.bringUpHttpSurface()`: `new OpaqueTokenStore(configDir, clock)` :895 → `ensureInitialToken()` :896 → `StandardAuthMiddleware(tokenStore)` :897 → … `installAuth` :922 → `installReadinessGate` :926 → `installAdminEndpoints(` :934 → `installRunQueryEndpoints(` :944; `tokenStore` is a LOCAL variable (not a field) | line numbers; that `tokenStore` is still in scope at the admin-install site (it is the same method) |
| P8 | `Main` reads no args; the clock whitelist | `Main.main(String[] args)` :53; `Clock clock = Clock.systemUTC()` :57 under the whitelist comment :54–:56; `resolveBaseDir()` :59 and `configDir = baseDir.resolve("config")` :60; `args` is never referenced in the method body | grep `args` in `Main.java` — still unreferenced; the `token` dispatch sits between `:57` (reuse `clock`) and `:59`, BEFORE the `Files.createDirectories` writes at `:62–:63` and before `resolveHomeId` (`:68`, which writes `home_id`) |
| P9 | The launcher + the helper + the package layout | `build-image.sh` :125–:143 writes `/opt/homesynapse/bin/homesynapse` as `exec … -cp "lib/*" com.homesynapse.app.Main "$@"` (args pass through); the image is staged with dirs 0755 / files `a+r` (`build-deb.sh` perms block); `build-deb.sh` :51–:52 copies `distribution/deb/homesynapse-token` to `usr/bin/homesynapse-token` (0755); `postinst` banner names `sudo homesynapse-token`; the helper is 23 lines of `/bin/sh`, `set -eu`, reads `${HOMESYNAPSE_TOKEN_FILE:-/var/lib/homesynapse/config/initial_api_token}` and prints the token; `distribution/common.sh`: `HS_CONFIG_DIR=/var/lib/homesynapse/config`, `HS_TOKEN_FILE=${HS_CONFIG_DIR}/initial_api_token`, `HS_USER=homesynapse`; the unit sets `Environment=HOMESYNAPSE_HOME=/var/lib/homesynapse` | the launcher heredoc's last line still forwards `"$@"`; the helper's current bytes (extend, never rewrite the no-arg path) |
| P10 | The JPMS edges the WU needs already exist | `app` `requires com.homesynapse.api.rest` (module-info, plain) and `requires com.homesynapse.lifecycle`; `lifecycle` `requires com.homesynapse.api.rest` (plain); `api.rest` `exports com.homesynapse.api.rest`, `requires io.javalin`, `requires com.fasterxml.jackson.databind` (plain), `requires org.slf4j`; the rest-api test tree has 21 files; `OpaqueTokenStoreTest` has 7 `@Test` methods on a fixed clock `2026-06-19T00:00:00Z` with `@TempDir`; the app test tree carries `HomeSynapseArchRules` + `HomeSynapseArchRulesTest` (the `NO_DIRECT_TIME_ACCESS` gate runs from there) | every `requires` above verbatim at your checkout; ZERO `build.gradle.kts` edits needed (confirm no new dependency is pulled by your code) |

**Standing pins carried from the sitting record (not source, but the incident facts the WU closes):** deleting `initial_api_token` neither revokes nor re-mints (the hash stays in the store; the mint fires only on an EMPTY store) — F-S1 refuted at the instrument 2026-08-20; the store-reset block (`mv config/api_tokens … ; restart`) is the bench's current last resort and is what the new `rotate` verb supersedes.
