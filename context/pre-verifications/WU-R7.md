<!--
file: context/pre-verifications/WU-R7.md
purpose: Pre-verification artifact for the R-7 / W2-1 arm64-artifact-channel instruction (≥3 prerequisite source-state assumptions). Each row: the assumption → what the hub observed at authoring (2026-08-22 early, core HEAD 33861ad; git-object reads + the Block-0 build log + the CI run pages) → what the CODER re-verifies at execution before implementing. STOP on any mismatch.
audience: the R-7 Coder lane (host-CC); the hub (audit).
verified-at: 2026-08-22 ~04:00 CDT, by the v55 hub, against core 33861ad (the distribution files are byte-identical to 7c9e4fa — 33861ad touched only web-ui/dashboard/).
-->

# WU-R7 — Pre-Verification (verified 2026-08-22 @ 33861ad)

| # | Assumption | Observed at authoring (hub) | Coder re-verifies |
|---|---|---|---|
| P1 | The install-smoke workflow's shape | `.github/workflows/install-smoke.yml`: one job `install-smoke` (`name: Build image + .deb, install-smoke on a clean machine`, `runs-on: ubuntu-latest`, `timeout-minutes: 25`); steps: Checkout (`actions/checkout@v4`) · Set up JDK 21 (`actions/setup-java@v4`, corretto) · Setup Gradle (`gradle/actions/setup-gradle@v4`) · Set up Node 22 (`actions/setup-node@v4`) · Static lint (`bash -n` over `distribution/**/*.sh`) · Build runtime image (`distribution/image/build-image.sh`) · Assemble .deb (`distribution/deb/build-deb.sh`) · Install smoke prerequisites (sqlite3) · Install-smoke (`sudo --preserve-env=JAVA_HOME distribution/smoke/run-smoke.sh`) · Update-smoke · Upload artifacts (`actions/upload-artifact@v4`, name `distribution-artifacts`, the tarball + the `.deb`, 7 days) · Capture service journal on failure. Triggers: push to main/develop on paths `distribution/**`, `app/**`, `lifecycle/**`, `api/**`, `gradle/**`, `build-logic/**`; PR to main; `workflow_dispatch` | every step name + action version at your checkout; no matrix present yet |
| P2 | The arch is derived on the runner, not passed | `distribution/common.sh` `hs_deb_arch()` :75–:85: `HS_ARCH` override, else `dpkg --print-architecture`, else `uname -m` mapping (`aarch64|arm64` → `arm64`) | unchanged; `ubuntu-24.04-arm` has dpkg ⇒ `arm64` |
| P3 | The twins are byte-identical | `diff <(git show HEAD:.github/workflows/install-smoke.yml) <(git show HEAD:distribution/ci/install-smoke.yml)` = empty at 7c9e4fa; 33861ad touched only `web-ui/dashboard/` | re-diff at your checkout BEFORE editing; `stat -c %h` on both (the hardlink class) |
| P4 | The F-V1 defect at source | `common.sh` `hs_version()` :49–:70: `HS_VERSION` override :50 · `VERSION` file lookups :52–:53 · `git describe --tags --always --dirty` :55 · the comment :57–:61 ("a bare commit id from an untagged repo … does NOT [start with a digit]") · the `case` :62–:65 — `[0-9]*) printf '%s' "${_v}"` / `*) printf '0.1.0+g%s' "${_v}"` · fallback `0.1.0-skeleton` :69. EXHIBITS at the instrument: the 2026-08-22 Block-0 build printed `[build-image] version=7c9e4fa arch=arm64 jdk=21` and dpkg-deb `Version: 7c9e4fa`; the H3 artifact was `0.1.0+gd26777c` (its id began with `d`) | the arm unchanged; the repo carries no tags (`git tag` empty) — if a tag exists, STOP and flag (the grammar assert's passes-but-false input) |
| P5 | Where the grammar assert lands | `build-image.sh`: `VERSION="$(HS_VERSION="${HS_VERSION:-}" bash -c '. "…/common.sh"; hs_version')"` at :36, `ARCH=` :37, `log`/`die` defined :43–:44, the toolchain preflight :47–:56 (`die "JAVA_HOME must point at a JDK 21…"` :48), `log "version=${VERSION} arch=${ARCH} jdk=${JFEATURE}"` :58 | line numbers; the assert goes between :37 and :43's first use (i.e., right after `ARCH=`), OUTSIDE any `$( )` |
| P6 | Build outputs never enter the tree | `distribution/.gitignore`: `image/build/`, `deb/build/`, `**/build/`, `*.deb`, `*.tar.gz`, `*.prev`, `*.old` | unchanged; the new test script is NOT ignored (it is a `.sh` under `smoke/`) |
| P7 | The README fence | `distribution/README.md:117` ("deterministic and self-checksumming") is DO-NOT-SAY-fenced until W2-3 — README.md is untouched by this WU | your census shows no README line |
| P8 | The runner + the repo visibility + the observed annotations | The repository is PUBLIC (its Actions pages render to an unauthenticated fetch); GitHub-hosted `ubuntu-24.04-arm` runners are available to public repositories at no cost. CI run #214 (`ci.yml`, `33861ad`, Build & Check succeeded in 2m 43s) carried two annotations verbatim: "Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/checkout@v4, actions/setup-java@v4, gradle/actions/setup-gradle@v4." and "setup-java v4 is deprecated and will no longer receive updates. Please migrate to actions/setup-java@v5." | at execution: `actions/setup-java@v5` exists (the notice says so); check whether `actions/checkout@v5` and `gradle/actions/setup-gradle@v5` exist before bumping them; if the arm64 leg cannot be scheduled, STOP (the hub rules the QEMU fallback) |

**Standing facts carried:** the Block-0 log of 2026-08-22 (bench card, arm64, JDK corretto 21.0.11) is the expected-output artifact for the arm64 leg: `bundled 55 jars` · the 16-module `--add-modules` line (`java.base,java.desktop,java.instrument,java.logging,java.management,java.naming,java.net.http,java.security.jgss,java.sql,java.xml,jdk.crypto.cryptoki,jdk.crypto.ec,jdk.jfr,jdk.management,jdk.unsupported,jdk.zipfs`) · `jlinked runtime → 61M` · `floor-presence assert green: all 16 requested modules present in the runtime` · `MANIFEST.sha256 (147 entries)` · the `.deb` 61,788,352 B at `7c9e4fa` (the CI artifact will differ in size only by the version string and build stamps).
