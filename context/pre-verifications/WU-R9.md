<!--
file: context/pre-verifications/WU-R9.md
purpose: Pre-verification artifact for the R-9 / E3-HEALTH instruction (≥3 prerequisite source-state assumptions). Each row: the assumption → what the hub observed at authoring (git-object reads at core HEAD 62dbca3 via `git show HEAD:<path>` — never the worktree; the R-7 delivery was on the worktree uncommitted at authoring and touches NONE of these files except `distribution/common.sh` at a non-overlapping hunk) → what the CODER re-verifies at execution before implementing. STOP on any mismatch.
audience: the R-9 Coder lane (host-CC); the hub (audit).
verified-at: 2026-08-22 ~09:30 CDT, by the v55 hub (beat 6), against core 62dbca3 objects.
-->

# WU-R9 — Pre-Verification (verified 2026-08-22 @ 62dbca3)

| # | Assumption | Observed at authoring (hub, git objects @ 62dbca3) | Coder re-verifies |
|---|---|---|---|
| P1 | The probe already supports an unauthenticated path | `distribution/smoke/health-probe.sh`: `HEALTH_PATH="/api/v1/entities"` `:24` · `--health-path` parsed `:37` · `NEEDS_AUTH` case `:49–:53` (`/api/*\|/internal/*` → 1, everything else → 0) · `load_token` `:85–:93` returns 0 immediately when `NEEDS_AUTH` = 0 (the file is never read) · `http_code` sends NO `Authorization` header when `_auth` is empty `:68–:70` · `probe_once` `:95–:108`: 200 → 0, 503 → 1 (keep waiting), 401/403 → 3 (hard fault), 000 → 1 · the `--wait` loop `:114–:124` exits 3 on rc 3 without burning the timeout · header comment `:8–:15` still says "every path auth-gated" + "Forward-compatible: pass --health-path /health … if Core ever adds one — escalation E3" | every line number; the binary's CODE needs no change — only `:8–:15` (comment) is touched |
| P2 | The unit's probe line and its guards | `distribution/systemd/homesynapse.service`: `StartLimitIntervalSec=300` `:19` · `StartLimitBurst=5` `:20` · `Type=exec` `:23` · `ExecStartPost=/opt/homesynapse/libexec/health-probe.sh --wait --timeout 90` `:50` (NO `--health-path`) · the comment `:46–:49` ("The probe authenticates with the first-run token") · `TimeoutStartSec=120` `:55` · `Restart=on-failure` `:61` · `PrivateDevices=yes` `:93` (+ comment `:88–:92` — R-3b's seam) · the M13 flip block `:100–:106` | unchanged; only `:46–:50` move; `:88–:93` untouched |
| P3 | The auth filter's shape and its single exemption | `RestFilters.java`: `IDENTITY_ATTRIBUTE` `:431` · `installAuth` `:476–:485` (`app.exception(...)` `:483`, `app.before(ctx -> authorize(...))` `:484`) · `authorize` `:487–:508` — `isPathSafe` `:490`, the ORDER comment `:494–:496`, the single early-return `:497–:499`, `authenticate` `:500`, rate-limit `:501–:506`, the attribute `:507` · `isPublicShellRequest` `:528–:536` · `isPathSafe` `:548–:565` · `problem` `:577–:581` · `writeProblem` `:584–:595` · the Javadoc's "EXACTLY ONE exemption" `:438–:441`. `installReadinessGate` `:91–:97` = `app.before("/api/*", new ReadinessFilter(readinessSource))` — `/health` is outside it by path. `installTokenAdminEndpoints` `:252–:262` (the gateway shape: requireNonNull ×N, `(Javalin)` cast, route registrations). Javalin is pinned 6.7.0 (the R-C bytecode read) | every cite; ADDITIONALLY verify on the pinned Javalin jar: (a) `io.javalin.http.Context#ip()` exists and returns `String` (`javap -p` the class; it delegates to `HttpServletRequest.getRemoteAddr()`); (b) whether a `GET` route answers `HEAD` (read `io.javalin.router` / test it in `HealthEndpointTest`'s e2e sibling) — state both findings in the return |
| P4 | The readiness predicate and its constants | `ReadinessFilter.java`: `RETRY_AFTER_SECONDS = "5"` `:72` (package-private) · `PROJECTION_STATE_HEADER = "X-HomeSynapse-Projection-State"` `:75` (package-private) · `apply` `:113–:124` (`mode == LIVE` → pass; else 503 + the two headers + the problem body) · `ReadinessSource` is `com.homesynapse.state.ReadinessSource` (`:8`), `SubscriberMode` is `com.homesynapse.event.bus.SubscriberMode` with constants `COLD` `:16` · `REPLAY` `:19` · `TRANSITION` `:22` · `LIVE` `:25` (+ any after `:25` — read the enum whole) | unchanged; the constants' package-private visibility (same package → reusable); the full `SubscriberMode` list |
| P5 | The composition root's ordering class | `HomeSynapseCore.java`: `public final class HomeSynapseCore implements SystemLifecycleManager, ReadinessSource` `:178` · `bringUpHttpSurface()` `:888–:988`: `new OpaqueTokenStore` `:895` · `ensureInitialToken` `:896` · `processOperatorRequests` `:903` · `Javalin.create` `:911–:925` · `installAuth` `:929` · `app.get("/", …redirect…)` `:932` · `installReadinessGate(app, this)` `:933` · `installEntityQueryEndpoints` `:934` · `installAdminEndpoints` `:941` · `installTokenAdminEndpoints` `:950` · `installRunQueryEndpoints` `:958` · `installAutomationQueryEndpoints` `:964` · `installCommandEndpoints` `:974` · `app.start(config.bindHost(), config.httpPort())` `:980` | the one new line lands between `:932` and `:933`; nothing else moves |
| P6 | The store's revoke seam (R-H2) | `OpaqueTokenStore.java`: `TokenRecord` `:147–:154` (fields incl. `scopes`, `expiresAt`, `revoked`) · `rotate` `:370–:401` revokes via `revokedCopy` in a loop `:379–:384` — it does NOT call `revoke()` · `public boolean revoke(String keyId)` `:416–:438` (the loop `:420–:433`, `persist()` `:424`, the stranded-artifact WARN `:425–:430`) · `processOperatorRequests` revoke arm `:510–:520` (`revoke(argument)` `:513`, the "already revoked" and "no such keyId" sub-arms `:515–:518`) · `activeKeyCount` `:578–:584` (the non-revoked ∧ non-expired predicate) · `ApiKeyClaims.SCOPE_ALL = "*"` (`ApiKeyClaims.java:42`), `fullAccess()` `:58–:60` | unchanged; the return-type change's callers = P7 + the tests |
| P7 | The HTTP revoke arm | `TokenAdminEndpoints.java`: class Javadoc's `DELETE` bullet `:39–:42` ("204 … 404 otherwise. Self-revocation is allowed") · `revokeHandler()` `:132–:134` · `revoke(EndpointContext, ApiKeyIdentity)` `:264–:282`: the keyId validation `:271`, `if (!store.revoke(keyId))` `:275` → `NOT_FOUND` problem `:276`, `audit(...)` `:280`, `ctx.status(204)` `:281`; the 403 layer `:302`/`:308` | the `:275` site becomes a `switch`; the audit line stays AFTER the REVOKED arm only |
| P8 | `ProblemType` has no generic 409 and no count pin | `ProblemType.java`: 13 constants — `NOT_FOUND` `:38` · `ENTITY_DISABLED` (409) `:48` · `INTEGRATION_UNHEALTHY` `:57` · `INVALID_COMMAND` `:68` · `INVALID_PARAMETERS` `:78` · `AUTHENTICATION_REQUIRED` `:88` · `FORBIDDEN` `:97` · `RATE_LIMITED` `:107` · `COMMAND_NOT_FOUND` `:117` · `STATE_STORE_REPLAYING` `:128` · `INTERNAL_ERROR` `:138` · `IDEMPOTENCY_KEY_CONFLICT` (409) `:148` · `DEVICE_ORPHANED` `:158` (the last, ends with `;`). `git grep -n "ProblemType.values\|ProblemType.class" -- '*Test.java'` → NOTHING | the 13; the grep re-run (a count pin found = +1 census row, disclosed) |
| P9 | run-smoke's check 3, the fence, the PASS census | `distribution/smoke/run-smoke.sh`: `set -uo pipefail` (no `-e`) `:15` · `ok`/`bad` `:27–:28` · check 3 `:72–:77` (`health-probe.sh --wait --timeout 90 --token-file "${HS_TOKEN_FILE}"` `:73`) · check 4 `:79–:125` (R-1) · check 5 `:127–:137` · check 6 `:139–:147` (`UNAUTH` on `/api/v1/entities` → 401/403 PASS) · checks 7–9 `:149–:186` · verdict `:188–:194`. `grep -c '\bok "'` = 20 (20 `ok` sites; the systemd path prints 18 PASS lines on a green run — the CI record at `62dbca3`) | unchanged; check 3b inserts after `:77`; the systemd PASS count becomes 19 |
| P10 | Every other probe call site + the unused `HS_HEALTH_PATH` | `install.sh:92` (`--token-file`, the no-systemd branch; the systemd branch `:81` relies on the unit's `ExecStartPost`) · `update.sh:60` (no-systemd `start_service`) + `:77` (post-swap readiness) · `update-smoke.sh:44` (v1 ready) + `:65` (post-update ready) — all `--wait --timeout 90 --token-file "${HS_TOKEN_FILE}"`. `common.sh:40–:44`: the comment + `HS_HEALTH_PATH="${HS_HEALTH_PATH:-/api/v1/entities}"`; `git grep -n HS_HEALTH_PATH -- distribution` → ONLY `common.sh:43–:44` (no consumer). `common.sh:57–:68` = the version arm (R-7's hunk) | the five sites; the zero-consumer fact; the R-7 hunk untouched |
| P11 | The helper's transitional block | `distribution/deb/homesynapse-token` (the R-6/R-8 version, 100644): usage `:33` (no-verb line) · `:40` (`mint`) · the CAUTION paragraph `:46–:49` · the no-arg output `:71` ("Pair a client with this token, then delete the file:  sudo rm ${TOKEN_FILE}") · `queue_request` ends `:105` · `apply_request()` `:108` · `if [ -d /run/systemd/system ]` `:109` · `if [ "$1" = rotate ]` `:110` · the comment `:111–:119` · `rm -f "${HS_CONFIG}/initial_api_token"` `:120` · `fi` `:121` · `systemctl restart` `:124` · the consumption assert `:128–:132` | the block `:110–:121` removed whole; `:71` unchanged |
| P12 | The doc's caveat section | `distribution/docs/token-rotation.md`: `## The operator path on a packaged install` `:33` · `### The readiness-probe caveat (packaged path)` `:71–:103` (three bullets: the revoke-of-artifact-key path; the delete-after-pairing path "(the install banner's advice)"; the once-at-exec cache + the helper's close) · `## The bench / dev recipe` `:104` · `## Credential hygiene` `:147`, the cross-reference "see the readiness-probe caveat above" `:156` · `## The /internal/tokens API` `:164`. `escalations.md` `### E3` `:33–:43` (options (a)/(b)/(c); recommendation (a) "minimal /health, loopback-bound") | unchanged |
| P13 | The lifecycle e2e rig | `HomeSynapseCoreTest.java`: `Clock.systemUTC()` by documented design (`:61–:63`) · the auth-posture test `:191–:226` (`boundHttpPort` `:202`; `exposeHttpSurface` idempotence `:205–:207`; unauth `/api/v1/entities` + `/internal/dlq` → 401 `:211–:212`) · the loopback-only bind test `:228–:250` (`firstNonLoopbackSiteLocal()` `:242`, `assumeTrue`-gated — it asserts a LAN address is REFUSED under the default bind; there is NO LAN-bind rig) · `get` `:255` / `send` `:262–:276` helpers (`HttpClient`, redirect NEVER) · `firstNonLoopbackSiteLocal` `:278–:292` · the DASH-SERVE trio `:296–:362` (`302` root, shell `200` + `HEAD 200` `:339`, the guarded set incl. the exact-400 `%2e%2e` pin `:360`) · `mode returns LIVE once the projection completes replay` `:376` | the helpers; whether `HomeSynapseConfig` admits a non-loopback `bindHost` in tests without production code (decides the optional R-H1 e2e arm) |
| P14 | The classifier test class | `RestFiltersAuthTest.java`: `isPathSafe_acceptsNormalPaths` `:30` · `isPathSafe_rejectsTraversalAndControl` `:39` · `isPublicShellRequest_exactAllowlist` `:52` · `isPublicShellRequest_rejectsEverythingElse` `:64` · `shellExemption_neverPrecedesTraversalGate` `:82` · the `problem` factory test `:95`. `RecordingEndpointContext` exists in the test tree (records status/headers/json as a map) | the three shell tests stay byte-identical; the new tests sit beside them |
| P15 | Gate baselines + the arch rules | CI on `62dbca3` (to be banked at the v55 beat-6/7 read): rest-api 130 tests, app 24, lifecycle 60; install-smoke 18 PASS lines (systemd path). `git grep -n "api.rest" app/homesynapse-app/src/test/java/com/homesynapse/app/HomeSynapseArchRules.java` → no rule names the rest-api → lifecycle wiring beyond the existing endpoints' class. `api/rest-api/build.gradle.kts`: `api(project(":core:state-store"))` (matches `requires transitive com.homesynapse.state`), `implementation(project(":core:event-bus"))`; module-info as embedded in the instruction | the counts at your checkout; the grep; ZERO module-info / build-file edits |

**Standing facts carried:** the R-6/R-8 audit's H-1 (three operator-reachable DOWN paths — the class this WU closes) and H-2 (the doc's single-restart one-liner suffers the same class — it becomes lawful after this WU; leave the one-liner, retire the caveat) · the INTERIM OPERATOR LAW (never revoke the artifact's key; never delete `initial_api_token`; mutate only via the helper) is RETIRED the beat this WU's CI banks green AND §OP-H passes · the bench runs the app via `bench.sh` (no unit) — the unit's new probe line is proven on the HELD card in R-3b's first block (artifact deleted on purpose → `systemctl restart homesynapse` → `is-active` = active), which the hub writes into the R-3 packet.
