<!--
file: context/pre-verifications/WU-SKIP-VIS.md
purpose: Pre-verification artifact for WU-SKIP-VIS (per the M3.6d protocol — every assumed source element observed at authoring, with signatures, so the Coder verifies rather than trusts). All observations 2026-07-26, host-truth reads at core HEAD `4bc1258` (worktree clean ≡ HEAD; lock-free porcelain 0).
audience: Coder (read BEFORE the instruction; re-verify each pin at execution — mismatch ⇒ STOP + report), PM hub (layer-2 anchor).
status: CURRENT at authoring. Companion: context/instructions/2026-07-26_SKIP-VIS_explanation-honesty_coding-instruction.md
-->

# WU-SKIP-VIS — Pre-Verification (P0–P14)

**P0 — dispatch baseline.** core HEAD `4bc1258` (merge of dependabot postcss above `5b4797e` CMD-API ALL-CI-GREEN); the Pi runs `355a711` (deploy pending — irrelevant to this desk WU; the fix DEPLOYS with this WU's landing per the ruled sequencing). Any other HEAD at dispatch ⇒ STOP.

**P1 — `StandardExplanationService.java` = 724 lines.** Anchors observed: `toSummary` decl `:453` · the triggeredAt subtraction `:466` (`Instant triggeredAt = terminalTime.minusMillis(Math.max(0L, payload.durationMs()));`) · `evaluatedAt` subtraction `:228–:229` (`terminalInstant(latest).minusMillis(Math.max(0L, payload.durationMs()))`) · `completedVerdict` decl `:284` · `deriveOutcome` decl `:616` · `isFailure` `:656` (`return outcome != null && !OUTCOME_ACKNOWLEDGED.equals(outcome);`) · private `record Outcome(RunExplanation.ActionOutcome value, String reason)` `:661` · `OUTCOME_ACKNOWLEDGED = "acknowledged"` constant · `ACTION_SKIPPED = "skipped"` / `ACTION_ERROR = "error"` constants · `terminalInstant(...)` helper = `eventTime != null ? eventTime : ingestTime`.

**P2 — `buildTrigger` matchedAt is arithmetic-free:** `Instant matchedAt = triggered.eventTime() != null ? triggered.eventTime() : triggered.ingestTime();` — the already-correct sibling (the DP-3 alignment target).

**P3 — DP-G at the emitter (context, untouched):** `StandardRunManager` `:210` `Instant eventTime = triggeringEvent.eventTime(); // inherited or null (DP-G)`; `publishCompleted` `:656–:665` publishes `AutomationCompletedEvent(runId, terminal.name(), durationMs, actionCount, commandCount, failureReason, abortReason)` with `run.eventTime` and `durationMs = Duration.between(run.startedAt, clock.instant())` (live-measured). File = 830 lines.

**P4 — `RunSummary.java` = 61 lines**, record components exactly `(RunId runId, AutomationId automationId, String automationName, Instant triggeredAt, RunStatus status, String terminalReason)`; the `triggeredAt` javadoc parenthetical currently reads "(best-effort: the terminal event time minus the recorded duration)" — DP-3's doc target.

**P5 — `RunExplanation.ActionView` = 6 components** `(type, targetRef, command, paramsJson, outcome, reason)`; construction sites repo-wide exactly 3: `StandardExplanationService:584` (`commandActionView`), `:610` (`nonDispatchedActionView`), `RunEndpointsTest:278`. `ActionOutcome` = `{DISPATCHED, CONFIRMED, UNCONFIRMED, FAILED, SKIPPED}` (frozen).

**P6 — `NonFiringExplanation.java` = 106 lines**, record components exactly 8 `(automationId, automationName, enabled, verdict, lastRelevantRunId, explanation, triggerSummary, lastEvaluation)`; `NonFiringVerdict` exactly 4 (`CONDITION_NOT_MET`, + COMPLETED-family per file); `LastEvaluationView(Instant at, String conditionsResult)` `:77`. The javadoc explicitly separates `NonFiringVerdict` from Doc-16 §4 `SuppressionReason` — do not conflate.

**P7 — `CommandResultEvent`** components `(targetEntityRef, commandType, outcome, failureReason)`; the ten-value outcome vocabulary verbatim in the javadoc `:22–:27` region incl. "integration adapters may publish additional protocol-specific strings" (SD-7's ground).

**P8 — `AutomationCompletedEvent`** carries `(runId, finalStatus, durationMs, actionCount, commandCount, failureReason, abortReason)` — `actionCount`/`commandCount` are payload truth (DP-2's detection input). `AutomationActionCompletedEvent` = `(runId, actionIndex, outcome, errorDetail)`.

**P9 — the §3.9 skip emits NOTHING (audit-corrected at authoring):** `StandardActionExecutor`'s only `"skipped"` completion publish (≈`:192`, `publishCompleted(automationId, context, index, "skipped", null, triggeringEvent)`) sits in the `catch (InterruptedException …)` ABORT path. The real §3.9 per-target skip is `case SKIP -> continue` (≈`:243–:245`) — zero emission per skipped target — and an all-skipped command action completes `"success"` (≈`:199`). Consequence: a silent-skip run carries NO skip events and NO SKIPPED chain views; the terminal payload's `commandCount`/`actionCount` arithmetic is the ONLY log-visible disclosure (DP-2's detection basis). READ-ONLY context (SD-3).

**P10 — endpoints.** `ListRunsEndpoint` = 204 lines; `toWire` = exactly 6 keys `runId, automationId, automationName, triggeredAt, status, terminalReason`; `:127`-region `map.put("triggeredAt", run.triggeredAt().toString())` = pure serialization; `wireStatus` maps `CONDITION_NOT_MET→SKIPPED`, `ABORTED→CANCELLED`, throws on EVALUATING/RUNNING. `GetRunCausalChainEndpoint` = 204 lines; `actionsList` map = exactly `type, targetRef, command, params, outcome, reason` (wire-pinned at `RunEndpointsTest:201`); outcome block = `status, reason, durationMs, actionCount, commandCount`. `GetNonFiringEndpoint` = 150 lines; `toWire` = exactly `automationId, automationName, enabled, verdict, lastRelevantRunId, explanation, triggerSummary, lastEvaluation` (wire-pinned at `AutomationEndpointsTest:79–:80`, NOT in RunEndpointsTest); verdict serialized via `name()`; weak ETag on projection position. `ListAutomationsEndpoint` consumes `AutomationSummary` + `latestRunByAutomation()` only — unreached by this WU.

**P11 — tests.** `StandardExplanationServiceTest` 13 `@Test` / 403 lines — seeding idiom: `InMemoryEventStore(FIXED_CLOCK)` + `publishRoot`/`publishDerived` building `EventDraft(eventType, 1, FIXED_INSTANT, subject, NORMAL, AUTOMATION, payload, null, null)`; `seedCompleted` publishes `AutomationCompletedEvent(runId, status, 1234L, 1, 1, …)`; the only automation-side triggeredAt assert today is `isNotNull()` (`:93`) and `NonFiringExplanationServiceTest` holds no exact `lastEvaluation.at` pin — the DP-3 fix flips no existing automation assert. `NonFiringExplanationServiceTest` 11 / 351. `RunEndpointsTest` 8 / 331; `containsOnlyKeys` pins at `:75, :79, :89, :95, :177, :180, :187, :191, :194, :198, :201, :206, :209, :217, :222`; `:79` = the runs-entry 6-key pin (SD-5's unchanged proof); `:201` = the actions 6-key pin (DP-1's update site); it instantiates ONLY ListRuns + causal-chain endpoints; its `:84` exact `triggeredAt` containsEntry feeds a hand-built `RunSummary` through a fake service (serialization-only — cannot flip on the fix). **`AutomationEndpointsTest` (audit-added at authoring): the non-firing test home** — `nonFiring_v11ShapeTest` ≈`:56` instantiates `GetNonFiringEndpoint` ≈`:64–:65`; the 8-key non-firing wire pin at `:79–:80`; 9 `containsOnlyKeys` pins total (`:76, :79, :89, :94, :187, :191, :199, :204, :210`); `new NonFiringExplanation(` ×3 at `:57, :102, :150` (8-arg). The `:79–:80` pin gains `noCommandsIssued` in this WU; `:191` pins the ListAutomations wire (`automationId, name, enabled, components, lastRunId`) — untouched.

**P12 — arch rules (re-grepped + audit-swept at authoring, `HomeSynapseArchRules.java`):** rules naming `com.homesynapse.automation..` literally = NO_REVERSE_DEPENDENCIES (`:143/:150`) + NO_DIRECT_FILESYSTEM_IN_CORE (`:191/:197`); NO_DIRECT_TIME_ACCESS `:98` (production reach = `com.homesynapse..` minus the app/platform/test whitelists). The audit's full 11-rule sweep additionally confirmed the `com.homesynapse..`-wide rules (NO_SYNCHRONIZED_METHODS `:74` · NO_SERVICE_LOADER `:126` · NO_INTERNAL_PACKAGE_ACCESS `:218` · REGISTRY_MUTATION_ONLY_VIA_PROJECTION `:410`) and the rest-api-scoped rules (QUERY_SERVICE_READ_ONLY `:266` · REST_ENDPOINTS_NO_EVENT_PUBLISHING `:306`) are untrippable by this WU's shape (String/Boolean record components; envelope-local derivation; LinkedHashMap keys); NO_JACKSON_IN_DOMAIN_MODEL `:337` does not scope automation. ZERO collisions.

**P13 — JPMS (verbatim `module-info.java` at `4bc1258` — ZERO changes this WU).**
`core/automation`: `requires transitive com.homesynapse.platform; requires transitive com.homesynapse.event; requires transitive com.homesynapse.device; requires transitive com.homesynapse.state; requires com.homesynapse.value; requires transitive com.homesynapse.event.bus; requires org.slf4j; exports com.homesynapse.automation;`
`api/rest-api`: `requires transitive com.homesynapse.state; requires com.homesynapse.event.bus; requires com.homesynapse.automation;` (PLAIN — automation not on rest-api's exported API) `requires com.fasterxml.jackson.databind; requires io.javalin; requires org.slf4j; exports com.homesynapse.api.rest;`
New components are `String`/`Boolean`/`boolean` (`java.base`) — no requires-transitive↔api lockstep triggered (checked against the DPs per format-addition #14).

**P14 — the ruling chain of record:** ruling 1 (v1.1.2 four-constraint law) verbatim at `context/decisions/2026-07-22_nick-rulings-1-5_verbatim.md`; CORE-P1/P2 verbatim at the G1 return §6; the fold GO at pm-handoff v37 beat 3 (Nick's conditional satisfied — Case A + zero arch-rule collisions); the DP-3 ground truth at the Rosonway report §4 (offset ≡ durationMs on two runs of differing duration, exact to the microsecond; external corroborator `lastChanged ≡ matchedAt`). The frozen v1.1 contract: `context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md` (hub appends the v1.1.2 amendment note at WUCP P2 — NOT this lane).

**One open gate:** DP-4 (`settled`) — Nick's Q1b one-liner rides the dispatch turn. Execute the gate state the dispatch states; never half-implement.
