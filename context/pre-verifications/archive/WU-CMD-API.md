<!--
file: context/pre-verifications/WU-CMD-API.md
purpose: Source-state pins for the CMD-API coding instruction. Every pin observed at core HEAD 355a711, 2026-07-22 (hub grounding pass: 40-file git-object corpus + 3 grounding agents + hub layer-2 spot-checks at HEAD objects).
status: VERIFIED 2026-07-22. Re-verify P0 at dispatch; drift on any pin => STOP + report.
-->

# Pre-Verification — WU-CMD-API (baseline core `355a711`)

**P0 (dispatch gate):** HEAD == `355a711`, clean tree. Drift => STOP.
**P1** `CommandRequest(String capability, String command, Map<String,Object> parameters)` — rest-api, public record, compact ctor null-checks + `Map.copyOf`.
**P2** `CommandAcceptedResponse(String commandId, String correlationId, String entityId, String status, Instant acceptedAt, long viewPosition)` — javadoc: status always `"accepted"`; acceptance = durability (INV-ES-04), not execution.
**P3** `CommandStatusResponse(String commandId, String correlationId, String entityId, String capability, String command, Map<CommandLifecyclePhase,LifecyclePhaseDetail> lifecycle, CommandLifecyclePhase currentPhase, boolean terminal)` — 8 components. `CommandLifecyclePhase` = `{ACCEPTED, DISPATCHED, ACKNOWLEDGED, CONFIRMED, CONFIRMATION_TIMED_OUT}`. `LifecyclePhaseDetail(Instant at, String eventId, Map<String,Object> details)`; details carries `integration_id` (DISPATCHED) / `result` (ACKNOWLEDGED) / `match_type` (CONFIRMED); null for ACCEPTED.
**P4** `IdempotencyEntry(String idempotencyKey, String commandId, String correlationId, long viewPosition, Instant createdAt)` — javadoc semantics: same-key+same-body => replay cached 202 without publishing; same-key+different-body => 409 IDEMPOTENCY_KEY_CONFLICT; LRU max 10,000; 24h TTL from createdAt; NOT persisted. **No body-fingerprint component — the cache wraps it (DP-4).**
**P5** `ProblemType` — 13 constants verified, incl. `COMMAND_NOT_FOUND("command-not-found",404)`, `IDEMPOTENCY_KEY_CONFLICT("idempotency-key-conflict",409)`, `INVALID_COMMAND("invalid-command",422)`, `INVALID_PARAMETERS("invalid-parameters",400)`, `NOT_FOUND("not-found",404)`. `EndpointResponses.problem(ctx, type, detail)` = the handler-layer error idiom (LinkedHashMap `type,status,title,detail`).
**P6** `RestFilters` route census at HEAD: **9 `app.get` routes, ZERO `app.post` anywhere in the repo** — the write plane is absent by construction (the G-B1-2 finding re-confirmed). Install-gateway pattern: `public static void installXxxEndpoints(Object javalinApp, <services>, LongSupplier viewPositionSupplier, Clock clock)` — foreign types Object-erased, cast internally (DEC-M3-16); endpoints constructed INSIDE the install method. `installAuth(Object, AuthMiddleware, RateLimiter)` at `:346` — `before(*)`, throw-to-halt, covers every path; registers the `ApiException` writer.
**P7** Endpoint seam: package-private `EndpointContext` (`pathParam, queryParam, status, header, json`); live handlers implement `io.javalin.http.Handler` delegating to package-private `apply(EndpointContext)` via `JavalinEndpointContext`; tests drive `apply(...)` with `RecordingEndpointContext` (`withPathParam/withQueryParam`, captures `statusSet/headers/body`).
**P8** Wire truth (LIVE, outranks the Doc-09 snake_case module row per Nick ruling 2026-07-22 (b)): hand-built LinkedHashMap bodies, camelCase keys (`entityId, availability, viewPosition, timestamp, nextCursor, hasMore`), `{data, meta}` envelope idiom across all nine handlers (M7.5c-a note: inline idiom, do NOT extract a helper), header `X-HomeSynapse-View-Position`. Filter-layer errors: `application/problem+json` with snake_case `correlation_id` (existing, untouched).
**P9** `CommandIssuedEvent(Ulid targetEntityRef, String commandType, String parameters, int confirmationTimeoutMs, CommandIdempotency idempotencyClass)` — event-model `:30`, FROZEN 5-component. `EventTypes.COMMAND_ISSUED = "command_issued"` (`:56`). **Sole production emitter: `StandardActionExecutor`** — timeout capability-precedence `:297-:310`, idempotency resolution default `CommandIdempotency.NOT_IDEMPOTENT` `:269-:271` ("never silently re-fire").
**P10** `EventOrigin.USER_COMMAND` EXISTS (`EventOrigin.java:42`). `EventPublisher.publishRoot(EventDraft): EventEnvelope` (`:126`; root => correlation = own eventId). `EventStore.readByCorrelation(Ulid): List<EventEnvelope>` (`:93`, chain in order, empty if none).
**P11** `EntityRegistry.findEntity(EntityId): Optional<Entity>` (`:49`). `CommandValidator.validate(EntityId, String, Map<String,Object>): ValidationResult(valid, reason)`; `StandardCommandValidator(entityRegistry)`; Tier-1 floor = entity exists + a capability declares the command. Live rejection outcome strings: `"invalid"`, `"unroutable"`.
**P12** `HomeSynapseCore.bringUpHttpSurface()` install order: `installAuth` -> readiness -> entity -> admin -> runs -> automations -> `app.start(bindHost, httpPort)`. In-scope this region: `clock, config, stateQueryService, stateProjection, eventBus, persistenceFactory, automationRegistry`. The EventPublisher + EntityRegistry composition-root FIELDS are not named in the region — Coder locates both (they exist; constructed pre-Phase-5); absent => STOP.
**P13** rest-api `module-info.java` (embed verbatim in the instruction): requires transitive `com.homesynapse.state`; plain `com.homesynapse.event.bus`, `com.homesynapse.automation`, `com.fasterxml.jackson.databind`, `io.javalin`, `org.slf4j`; `exports com.homesynapse.api.rest`. Readability of `com.homesynapse.event`/`device`/`platform` rides automation's `requires transitive` closure — **zero module-info changes pinned** (validated per format-addition #14: the WU adds NO public types and NO exported-surface signatures; the only public delta is one static method on the already-exported `RestFilters`).
**P14** automation `module-info.java`: `exports com.homesynapse.automation;` UNQUALIFIED; `requires transitive` platform/event/device/state/event.bus.
**P15** Doc 09 (Locked) command surface: `POST /api/v1/entities/{entity_id}/commands` + `GET /api/v1/commands/{command_id}`; 202 acceptance; Idempotency-Key max 128 chars; validation order 404/409-disabled/503-unhealthy/422; origin USER_COMMAND, actor = API identity; commands are ROOT events (no run). **Doc 09 defines NO inbound webhook route** (webhooks = Tier-2 OUTBOUND subscriptions §14; `WebhookTrigger` javadoc: its producer event "is named and minted by the M10 REST amendment").
**P16** Bench constants PROVISIONAL blocks (`scenarios/constants.yaml` `:63-:85, :110-:127`): phases UPPERCASE list; `terminal-field: "data.terminal"`, `phase-field: "data.currentPhase"` (guesses matching DP-2); `capabilities.command-api.available: false` flips with B2. Token-freeze from day one — the wire keys this WU ships are the frozen truth the bench re-pins.
