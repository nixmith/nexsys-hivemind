<!--
file: context/pre-verifications/WU-LEARN-PERSIST.md
purpose: Pre-verification for the LEARN-PERSIST coding instruction — every load-bearing source-state assumption observed at core HEAD 554e18c, 2026-07-19, by the v35 hub (object reads via git show HEAD:<path>; grep sweeps over the HEAD tree). The Coder re-verifies P1–P14 at dispatch HEAD before writing any code; any drift is a STOP-and-report.
audience: Coder; the hub (audit basis).
state-type: pre-verification (point-in-time record).
status: VERIFIED 2026-07-19 at 554e18c.
-->

# WU-LEARN-PERSIST — Pre-Verification (P1–P14, at core `554e18c`, 2026-07-19)

- **P1 — The learned map is in-memory with NO removal site.** `ZclIngestionUnit.java:142`: `private final Map<Long, ZoneType> learnedZoneTypes = new HashMap<>();`. A full-file grep shows only `.put` (`:542`) and `.get` (`:586`, `:595`) — device-leave does NOT clear it; only process restart does. (F-8's `invalidateHandlers` `:560-:563` clears `handlersByDevice` ONLY.)
- **P2 — The shared learn core.** `learnZoneType(IEEEAddress, long)` at `:528-:551`: candidate-scan over `ZoneType.values()` by `zclId()`; unknown ⇒ `ias_zone_type_unknown` DEBUG + return (no learn); `learnedZoneTypes.put` at `:542`; change-vs-`effectiveZoneType` gates `invalidateHandlers` + the `ias_zone_type_learned` INFO (`:546-:549`); same-as-effective stores SILENTLY. Feeders: the attribute path (`:509-:516`, called `:386`) and the enroll path (`:424`).
- **P3 — LEARNED-ONLY accessor.** `learnedZoneType(IEEEAddress)` `:584-:587` — "LEARNED state ONLY, never the resolver default"; javadoc pins cycle-thread confinement and that the production caller is the adoption path.
- **P4 — Precedence.** `effectiveZoneType` `:594-:598`: learned-first, else `resolver.zoneTypeFor(device)`.
- **P5 — Adopt-time classify is an in-memory learned-only read.** `ZigbeeAdoptionSlice.java:348-:358`: per IAS-bearing endpoint, `zoneTypeSource.apply(ieee).orElse(null)` → `EndpointClassifier.classify(endpoint, learnedZoneType)`; comment verbatim: "The source reads in-memory learned state only — never blocking, never I/O."
- **P6 — MOTION default.** `EndpointClassifier.iasCapability` `:175-:177`: `learnedZoneType == ZoneType.CONTACT ? contact : motion`.
- **P7 — The zoneTypeSource wiring.** Adapter `:288` passes `this::learnedZoneTypeFor` (slice ctor); `learnedZoneTypeFor` `:1029-:1032` null-guards `ingestion` and delegates to `unit.learnedZoneType(device)`.
- **P8 — Construction order supports ctor-seeding.** Adapter ctor: `cache = new ZigbeeDeviceCache(dataDirectory.resolve("zigbee-devices.json"), clock)` at `:279-:280`; `ingestion = new ZclIngestionUnit(...)` at `:323` (7 params: callbackDrain, resolver, listener, deduplicator, publisher, clock, frameSender) — the cache exists BEFORE the unit; the ingestion cycle starts later in initialize ⇒ rehydrate-before-joins holds by construction.
- **P9 — The cache is the sanctioned additive-sidecar carrier.** `ZigbeeDeviceCache` class doc: Jackson tree model, interior-only; `WRITE_DEBOUNCE` = 30 s; `WRITE_FAILURE_BACKOFF_MILLIS` = 60_000 (F-14; `maybeFlush()` runs every adapter cycle); writes snapshot under the `ReentrantLock` with I/O outside it; shutdown flush (`:292` "Writes the cache immediately (adapter shutdown)"); ctor "loading persisted state when the file exists" (`:77`); and the precedent verbatim: "The JSON additionally carries `lastKnownAvailability` per device … the frozen `ZigbeeDeviceRecord` has no availability component; the FILE carries the sidecar, tolerated-additively by this loader."
- **P10 — Adopt fires at the proposal.** `adoptIfAccepted` called at `:815` in the ingestion-path propose outcome; defined `:841`. NOT touched by this WU.
- **P11 — A wrong adoption is durable.** `DeviceRemovedEvent`/`EventTypes.DEVICE_REMOVED` have registry apply/tombstone support (`RegistryProjection:127`) but a src/main grep shows NO production publish site — nothing emits `device_removed`.
- **P12 — module-info verbatim** (embedded in the instruction): module `com.homesynapse.integration.zigbee`; `requires transitive com.homesynapse.integration`; jSerialComm + slf4j + jackson.databind interior; single `exports com.homesynapse.integration.zigbee`. ZERO changes in this WU.
- **P13 — Accept list is initialize-time.** `ADOPT_DEVICES_KEY = "adopt_devices"` (`ZigbeeIntegrationAdapter:128`); `adoptAcceptList = readAdoptAcceptList()` in the ctor (`:289`) — read once per adapter lifecycle; listing a device requires the restart that wipes P1's map. (The bench-observed mechanism.)
- **P14 — Test baseline AS-AT `554e18c`:** integration-zigbee 526/0/0 (the W2-LEARN close-out record: 518 baseline + 8). **Re-derive forced-fresh at dispatch HEAD (G-LP-4); never carry this number.** Silicon corroboration of P1/P2 semantics: the 2026-07-19 bench log `bench-2026-07-19-121417.log` — announce 12:17:50.746 → proposed 12:17:51.519 (+0.773 s, cached interview) → key_established 12:17:54.254 → `ias_zone_type_learned … zoneType=CONTACT (was MOTION)` 12:18:00.725 (+9.979 s) → enrolled 12:18:00.735; close-down boot `bench-2026-07-19-124151.log`: `projection_live devices=5 entities=5 position=18946` byte-identical to the pre-window boot (zero registry appends).
