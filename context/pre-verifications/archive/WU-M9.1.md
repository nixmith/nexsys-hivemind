<!--
file: context/pre-verifications/WU-M9.1.md
purpose: Pre-verification artifact for M9.1 (integration spine — supervisor + command routing + composition-root wiring). Every source-state assumption the M9.1 coding instruction rests on, with the observed signature or an explicit "absent → must create". The Coder reads this BEFORE the instruction.
audience: Coder (first read), PM hub (audit), Nick.
state-type: pre-verification (write-isolated M9-authoring lane output).
status: VERIFIED 2026-07-02 against core `52824e9` (host-side file reads; two independent extraction passes + one adversarial audit pass — see §D).
verification-method: direct Read-tool file reads at core HEAD 52824e9; adversarially re-verified (zero-fabrication bar, M7.5a/b precedent). VM git avoided (truncated-tail phantom ACTIVE-INTERMITTENT).
-->

# WU-M9.1 Pre-Verification — observed source state at core `52824e9`

Rule: if ANY row below fails its re-check at execution time, STOP and report the mismatch (STOP-on-Mismatch). The instruction was authored against exactly these observations.

## A. Types/signatures M9.1 CONSUMES (observed, verbatim)

| # | Element | Observed at `52824e9` |
|---|---|---|
| A1 | `IntegrationSupervisor` (`integration/integration-runtime`, `com.homesynapse.integration.runtime`) | Interface, **9 methods**: `CompletableFuture<Void> start(List<IntegrationFactory>)` · `void stop()` · `CompletableFuture<Void> startIntegration(IntegrationId)` · `CompletableFuture<Void> stopIntegration(IntegrationId)` · `CompletableFuture<Void> restartIntegration(IntegrationId)` · `Optional<IntegrationHealthRecord> health(IntegrationId)` · `Map<IntegrationId, IntegrationHealthRecord> allHealth()` · `boolean isRunning(IntegrationId)` · `Set<IntegrationId> registeredIntegrations()`. **ZERO production call sites repo-wide** (grep-verified: no impl exists; no reference in `HomeSynapseCore.java` or `Main.java`). |
| A2 | integration-runtime production files | Exactly 7: `package-info.java`, `module-info.java`, `IntegrationSupervisor.java`, `IntegrationHealthRecord.java` (14-component record incl. `plannedRestart`), `SlidingWindow.java` (`(int size, int count, double rate)`), `HealthDetail.java` (12 values), `ExceptionClassification.java` (enum, 4 values: `TRANSIENT, PERMANENT, SHUTDOWN_SIGNAL, AUTH_FAILED`). **No supervisor impl, no assembly, no classification logic beyond the enum — all must be created.** |
| A3 | integration-runtime `module-info.java` | `module com.homesynapse.integration.runtime { requires transitive com.homesynapse.integration; exports com.homesynapse.integration.runtime; }` (single requires; verbatim embed in instruction §5). `build.gradle.kts` deps: `api(project(":integration:integration-api"))` only. |
| A4 | `IntegrationFactory` (integration-api) | 2 methods: `IntegrationDescriptor descriptor();` (pure, no I/O) · `IntegrationAdapter create(IntegrationContext context) throws PermanentIntegrationException;` |
| A5 | `IntegrationAdapter` (integration-api) | 8 declared methods: `initialize()` (throws `PermanentIntegrationException`), `run() throws Exception`, `close()`, `commandHandler()` (**may return null** = read-only adapter), + 4 `default` AMD-55 hooks (`onConfigUpdated`, `onOptionsUpdated`, `onReauthRequired`, `migrate`). Extends `AutoCloseable`. |
| A6 | `CommandEnvelope` (integration-api) | `record CommandEnvelope(EntityId entityRef, String commandName, Map<String, Object> parameters, Ulid commandEventId, Ulid correlationId, IntegrationId integrationId)` — note `Ulid`, NOT `EventId`, for the two id fields. |
| A7 | `CommandHandler` (integration-api) | `@FunctionalInterface`, `void handle(CommandEnvelope command) throws Exception;` (param name `command`). |
| A8 | `IntegrationDescriptor` | 14-component canonical record (order: `integrationType, displayName, ioType, requiredServices, dataPaths, healthParameters, dependsOn, descriptorSchemaVersion, configSchemaMajor, configSchemaMinor, softDependencies, backoffParameters, isolationLevel, plannedRestartTimeout`) + an **8-arg convenience ctor** defaulting the last six (`1, 0, Set.of(), BackoffParameters.defaults(), IsolationLevel.IN_JVM, null`). |
| A9 | `HealthParameters` | 11 components in order: `heartbeatTimeout, healthWindowSize, maxDegradedDuration, maxSuspendedDuration, maxSuspensionCycles, maxRestarts, restartWindow, probeInitialDelay, probeMaxDelay, probeCount, probeSuccessThreshold`. `BackoffParameters(initialDelay, multiplier, maxDelay)` with static `defaults()`. |
| A10 | `IntegrationContext` (integration-api) | 12-component record; compact ctor requires non-null for first 7; `schedulerService`/`telemetryWriter`/`httpClient`/`security`/`discovery` nullable (gated by `RequiredService`); 10-arg back-compat ctor delegates with `(…, null, null)`. |
| A11 | `HealthState` | `HEALTHY, DEGRADED, SUSPENDED, FAILED`. |
| A12 | `IntegrationEvents` (integration-api) | `LIFECYCLE_EVENT_CLASSES` = 10 classes; `CAPABILITY_EVENT_CLASSES` = 2. The 5 legacy lifecycle records: `IntegrationStarted, IntegrationStopped, IntegrationHealthChanged, IntegrationRestarted, IntegrationResourceExceeded` (event-type strings frozen snake_case). |
| A13 | `Subscriber` (event-bus) | Exactly 3 methods: `void onEvent(EventEnvelope)` · `default void onCaughtUp()` · `default void setMode(SubscriberMode)`. `SubscriberMode`: `COLD, REPLAY, TRANSITION, LIVE, SUSPENDED`. |
| A14 | `EventBus.subscribeRuntime` | `default void subscribeRuntime(SubscriberInfo info, Subscriber runtime)` (throws `UnsupportedOperationException` in the default; implemented by `InProcessEventBus`). `SubscriptionFilter(Set<String> eventTypes, EventPriority minimumPriority, SubjectType subjectTypeFilter)`. `SubscriberInfo` canonical record is **4 components** `(String subscriberId, SubscriptionFilter filter, boolean coalesceExempt, boolean atomicCheckpoint)` with a 3-arg convenience ctor delegating `(…, false)` — the 3-arg form is what all 4 existing `HomeSynapseCore` call sites use. **`SynchronousEventBus` (test-support) does NOT override `subscribeRuntime`** — runtime-subscriber tests must use `InProcessEventBus` or direct `onEvent`/`setMode` invocation. |
| A15 | `CommandIssuedEvent` (event-model) | `(Ulid targetEntityRef, String commandType, String parameters, int confirmationTimeoutMs, CommandIdempotency idempotencyClass)` — `parameters` is a **serialized JSON String** ("the serialized parameters stay on the durable command_issued for the adapter (M9)" — `StandardCommandDispatchService` comment, verbatim). |
| A16 | `CommandDispatchedEvent` (event-model) | `(Ulid targetEntityRef, Ulid integrationId, String protocolMetadata)` — **carries NO command name, NO parameters, NO command event id.** `protocolMetadata` is currently always `"{}"` (`NO_PROTOCOL_METADATA`). Its `causationId` = the originating `command_issued`'s `eventId` (chained in `StandardCommandDispatchService.route(...)` via `CausalContext.chain(correlationId, event.eventId().value())`). ⇒ envelope construction REQUIRES a join back to `command_issued`. |
| A17 | `CommandResultEvent` | `(Ulid targetEntityRef, String commandType, String outcome, String failureReason)`. |
| A18 | `EventDraft` | 9-component record `(eventType, schemaVersion, eventTime, subjectRef, priority, origin, payload, actorRef, idempotencyKey)` — **NO static factories**; construct with `new`. `CausalContext(Ulid correlationId, Ulid causationId)` + `root(...)`/`chain(...)` factories. `SubjectRef.integration(IntegrationId)` static factory exists. `EventOrigin.SYSTEM` exists. |
| A19 | `HomeSynapseCore` ctors | Exactly 2 public: 5-arg `(Path dbPath, Path configDir, HomeSynapseConfig config, Clock clock, HomeId homeId)` delegating to 6-arg `(…, PayloadCipher payloadCipher)`. (The lifecycle MODULE_CONTEXT prose describing 4/5-arg forms without `configDir` is STALE — source governs.) |
| A20 | `HomeSynapseCore` wiring pattern | 4 `subscribeRuntime` call sites (projection L422, automation L510, dispatch L527, ledger L553); Phase 6 marker comment `// ── Phase 6 INTEGRATIONS — out of scope for AB-3 ──` exists; `doTeardown` unsubscribes in reverse registration order (ledger → dispatch → automation → projection) and there is a SECOND teardown branch `abandon()`; package-private accessor block ("Package-private accessors for the lifecycle wiring test") holds `pendingCommandLedger()`, `entityRegistry()`, `deviceRegistry()`, etc.; event-class aggregation combines **3** manifests (`EventTypes.CORE_PRODUCTION_EVENT_CLASSES` + `IntegrationEvents.LIFECYCLE_EVENT_CLASSES` + `IntegrationEvents.CAPABILITY_EVENT_CLASSES`); `LEDGER_EXPIRY_PERIOD_MILLIS = 1_000L`; `SharedScheduler.schedulePeriodic(name, runnable, periodMillis)` call shape observed at the ledger tick. |
| A21 | lifecycle `module-info` + gradle | NO `requires com.homesynapse.integration.runtime` (only non-transitive `requires com.homesynapse.integration` for `IntegrationEvents`); `build.gradle.kts` has NO `:integration:integration-runtime` dependency. app module ALREADY has `requires com.homesynapse.integration.runtime` + `implementation(project(":integration:integration-runtime"))` — app-side graph is pre-wired, `Main.java` constructs nothing integration-related. |
| A22 | Dispatch resolution chain | `StandardCommandDispatchService.route(...)`: resolve `EntityRegistry.findEntity(...).deviceId()` → `DeviceRegistry.findDevice(...).integrationId()`; on success publishes `command_dispatched` (DIAGNOSTIC priority, origin AUTOMATION) — **no in-process adapter call exists anywhere** (`RunPipelineReplaySafetyTest` javadoc: "there is no real device adapter until M9"). LIVE-only guard pattern: `if (replayMode) return;` set from `setMode(mode != SubscriberMode.LIVE)`. |
| A23 | `RunPipelineReplaySafetyTest` (lifecycle test) | 4 test methods; constants `SEEDED_COMMAND_ISSUED = 1L`, `SEEDED_COMMAND_DISPATCHED = 1L`, `SEEDED_STATE_CONFIRMED = 1L`, `LOADED_AUTOMATION_COUNT = 2`; `newCore(...)` uses the 5-arg ctor; seeds via bus-less `PersistenceFactory.start(dbPath, config.persistence(), FIXED_CLOCK, TEST_HOME_ID, eventClasses, null)` (checkpoints stay empty → replay-from-zero); javadoc carries the M9 forward note verbatim: "**When M9 lands a real adapter call, extend this gate to also assert the adapter recorder is zero across the replay window**". |
| A24 | `RunPipelineConfirmWiringTest` (lifecycle test) | Boots real core to LIVE with seeded command automation + integration-routed device/entity; injects synthetic `state_reported`; asserts `state_confirmed` correlation/causation chain + ledger drain via package-private `core.pendingCommandLedger()`. The registry-seeding + automation-seeding mechanics in this class are the reference pattern for M9.1's E2E test. |
| A25 | `EntityRegistry` / `DeviceRegistry` (device-model) | `EntityRegistry` 9 methods (incl. `findEntity(EntityId)` → `Optional<Entity>`, `createEntity`); `DeviceRegistry` 7 methods (incl. `findDevice(DeviceId)` → `Optional<Device>`, `findByHardwareIdentifier(String,String)`, `createDevice`). `Entity` 12 components (`deviceId` component 5); `Device` 14 components (`integrationId` component 9, type `IntegrationId`). `InMemoryEntityRegistry`/`InMemoryDeviceRegistry` exist (AB-3, start-empty). |
| A26 | `IntegrationId` (platform-api, `com.homesynapse.platform.identity`) | `record IntegrationId(Ulid value) implements Comparable<IntegrationId>`; factories `of(Ulid)`, `parse(String)`. |
| A27 | Test fixtures | `StubIntegrationContext.defaults()`/`builder()` (+fluent setters incl. `withClock(Clock)`, `withEntity(Entity)`), `TestAdapter.noop()/echo()/failing()/failing(String)/builder()`, `StubCommandHandler.accepting()/rejecting(Exception)/conditional(Predicate<CommandEnvelope>)` — all in `integration-api` testFixtures (`com.homesynapse.integration.test`). Consumption requires BOTH `testFixturesImplementation(...)` and `testImplementation(testFixtures(project(":integration:integration-api")))` per integration-api MODULE_CONTEXT. `TestClock` (test-support): `at(Instant)`, `advance(Duration)`, `setFixed(Instant)`, `peek()`. `InMemoryEventStore` lives in `core/event-model` **testFixtures** (`com.homesynapse.event.test`), NOT test-support. |
| A28 | Arch rules (app test classpath) | `NO_SYNCHRONIZED_METHODS` (all `com.homesynapse..`); `NO_DIRECT_TIME_ACCESS` whitelist = `com.homesynapse.app..`, `com.homesynapse.platform..`, `com.homesynapse.test..` — integration-runtime and lifecycle are NOT whitelisted; `NO_SERVICE_LOADER`; `NO_REVERSE_DEPENDENCIES` (the six core packages `event/device/state/persistence/automation/config` must not depend on ANY of `com.homesynapse.integration..`, `com.homesynapse.api..`, `com.homesynapse.lifecycle..`, `com.homesynapse.app..`); Rule-10 Jackson isolation does NOT cover `com.homesynapse.integration..`. |
| A29 | Count pins | `EventTypesTest.exactConstantCount` = **71**; `EventTypeAnnotationTest.exactlyFortyOneAnnotatedRecords` = **41**; `EventCategoryMappingTest.explicitMappingCount_matchesDocEnumeration` = **53** (persistence module). M9.1 mints ZERO event types → all three pins UNCHANGED. |
| A30 | `PendingCommandLedgerAssembly` (pattern reference) | `Components(PendingCommandLedger ledger, Subscriber subscriber, Runnable expirationTick)`; static factory `pendingCommandLedger(EventPublisher, EntityRegistry, Clock, long)`; `SUBSCRIBER_ID = "pending_command_ledger"`; `DEFAULT_CONFIRMATION_TIMEOUT_MS = 30_000L`. This is the assembly shape M9.1's supervisor assembly mirrors. |
| A31 | `ConfigurationAccess` (config module) | Interface with `getConfig()`, `getString(String)`, `getInt(String)`, `getBoolean(String)`. **[HUB-AUDIT CORRECTION 2026-07-02 — the original "NO implementation exists anywhere" claim was REFUTED by the v13 hub's independent audit.]** Implementations DO exist: **`ScopedConfigurationAccess`** (config module, PRODUCTION — `final class`, **package-private**, ctor `ScopedConfigurationAccess(String integrationType, ConfigModel model)` — scopes one integration's section; not directly constructible from lifecycle) and **`InMemoryConfigAccess`** (config testFixtures, for tests). `HomeSynapseCore` holds only a `ConfigurationService` field (a SEPARATE interface — `load()/reload()/getCurrentModel()/getSection()/write()`; neither extends the other). `IntegrationContext` null-checks `configAccess` ⇒ M9.1 must EXPOSE the existing scoped impl or adapt (see the corrected B7). |
| A32 | `SharedScheduler` | Package-private `final class` in `com.homesynapse.lifecycle` — **unreachable from integration-runtime** (this is why the supervisor's backoff needs its own clock-injected wait, instruction W8). `void schedulePeriodic(String name, Runnable task, long periodMillis)`. |
| A33 | Module-graph + lint | Root `build.gradle.kts` `moduleGraphAssert` allow-list contains `":lifecycle:.* -> :integration:.*"` ⇒ the new `lifecycle → integration-runtime` edge is ALLOWED (no allow-list edit needed). `-Xlint:all -Werror` applied to both touched modules via `homesynapse.java-conventions.gradle.kts`. `LifecyclePhase` has 10 constants incl. `INTEGRATIONS`; `recordSubsystem(String, LifecyclePhase, Instant)` is the private recording helper. `Ulid` = `record Ulid(long msb, long lsb)` with `public static Ulid fromBytes(byte[])` (16-byte big-endian) — DP-6's derivation is buildable. `CommandIdempotency` = `IDEMPOTENT, NOT_IDEMPOTENT, CONDITIONAL` (event-model). `abandon()` uses bulk `eventBus.abandon()` (no per-subscriber unsubscribe) + `scheduler.shutdown()`. |

## B. ABSENT → M9.1 MUST CREATE

| # | Element | Note |
|---|---|---|
| B1 | `IntegrationSupervisor` implementation (+ assembly + per-integration runtime state) | No impl anywhere. M9.1 creates `StandardIntegrationSupervisor` (package-private) + public assembly, per instruction §5. |
| B2 | The `command_dispatched` → `CommandHandler.handle(CommandEnvelope)` routing subscriber | integration-runtime MODULE_CONTEXT Phase-3 note describes it; zero code exists. |
| B3 | Exception-classification LOGIC | Only the 4-value enum exists. The classify function (Throwable → classification; unknown RuntimeException → TRANSIENT per Doc 05 §3.7 anti-pattern note) must be created. |
| B4 | lifecycle Phase-6 wiring + 7-arg `HomeSynapseCore` ctor + teardown extensions + package-private `integrationSupervisor()` accessor | Phase 6 is an empty comment marker today. |
| B5 | `FakeIntegrationFactory` / recording fake adapter test fixture | Nothing equivalent exists in lifecycle tests (TestAdapter in integration-api testFixtures is adjacent but not a recording command-sink wired for composition-root use — instruction §6 rules which to use where). |
| B6 | integration-runtime `src/test/` beyond the 3 existing record/enum tests | Supervisor FSM/backoff/isolation/routing tests must be created. |
| B7 | `ConfigurationAccess` wiring **[CORRECTED at hub audit]** | **PREFERRED (ruled):** a minimal PUBLIC static factory in the config module exposing the existing package-private `ScopedConfigurationAccess` (e.g. `ConfigurationAccess.scoped(String integrationType, ConfigModel)` as a static interface method, or a tiny factory class) — reuses the canonical scoping logic, gives per-integration scoping for FREE (better than the originally-planned unscoped interim), zero JPMS change (config already exports the package; lifecycle already requires config). Lifecycle composes it over `ConfigurationService.getCurrentModel()`. **FALLBACK** (if the Coder finds the factory widens the config API objectionably — push back per the instruction's pushback section): the originally-specified package-private lifecycle-internal adapter. Tests: `InMemoryConfigAccess` (config testFixtures) exists — prefer it over hand-rolling. Required because `IntegrationContext` null-checks `configAccess`. |

## C. Explicitly OUT of M9.1 (verified deferrable; owned by later M9 units per the split)

`integration-zigbee` source (39 Phase-2 files — untouched in M9.1) · jSerialComm catalog entry (ABSENT from `libs.versions.toml`, verified — added at M9.2) · AMD-97 `confirmation[]` block on the Zigbee `DeviceProfile` (record is 9-component today, no `confirmation` field, verified) · `WithinTolerance.evaluate()` implementation (**verified STUB**: throws `UnsupportedOperationException("Implementation deferred to Phase 3")` — REQUIRED before M9.4 CT confirmation acceptance; scheduled M9.3) · DEGRADED/SUSPENDED probe ladder, JFR resource monitoring, planned-restart suppression window (Doc 05 §3.14), dependency-graph Kahn ordering — deferred supervisor breadth.

## D. Verification provenance

Pass 1: broad code-territory extraction (module-infos, MODULE_CONTEXTs, catalog, arch rules). Pass 2: targeted verbatim signature extraction (every §A row). Pass 3: adversarial audit of the drafted instruction against source (zero-fabrication bar) — findings folded; discrepancies it caught were corrected in both this artifact and the instruction before hand-back. Baseline: core `52824e9` · docs `1509b34` · bench `5ceff3b`.
