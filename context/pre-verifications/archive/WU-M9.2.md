<!--
file: context/pre-verifications/WU-M9.2.md
purpose: Pre-verification artifact for M9.2 (Zigbee transport/EZSP) — every load-bearing source-state assumption pinned with its observed signature. The Coder re-verifies each STOP gate against the execution-time HEAD before writing code.
audience: Coder (Claude Code lane), PM
state-type: current
status: VERIFIED 2026-07-03 by the v15 hub against core `cf3733e` / docs `1509b34` (watermark AMD-97, invariants 172/51) / bench `5ceff3b`
baseline-note: If core HEAD ≠ `cf3733e` at execution, diff the delta against the read-set below (the M9.1 baseline-shift protocol) — a disjoint delta proceeds with the fold base carried forward; an intersecting delta is a STOP.
-->

# Pre-Verification — WU-M9.2 (Zigbee transport/EZSP)

**BOTH module-graph knobs are pinned here (A6) — the M9.1 lesson: the allow-list AND the height ceiling.**

| # | Assumed source element | Observed signature (2026-07-03, core `cf3733e`) | M9.2 consequence |
|---|---|---|---|
| A1 | `integration/integration-zigbee/` module | EXISTS — Phase-2 scaffold, **39 Java types + module-info**, `src/main/resources/schema/.gitkeep`, **NO `src/test/` directory**, zero implementation classes. `settings.gradle.kts` includes `integration:integration-zigbee`. | M9.2 creates the module's first implementations AND its first test tree. |
| A2 | `module-info.java` (integration-zigbee) | VERBATIM: `module com.homesynapse.integration.zigbee { requires transitive com.homesynapse.integration; exports com.homesynapse.integration.zigbee; }` (single requires, single exports; LTD-17 Javadoc header). | Proposed diff = exactly ONE added line: `requires com.fazecast.jSerialComm;` (plain, non-transitive — see A5/D-M92-1). |
| A3 | `build.gradle.kts` (integration-zigbee) | Single dependency: `api(project(":integration:integration-api"))` with the LTD-17 comment block. | Proposed diff = exactly ONE added line: `implementation(libs.jserialcomm)` (lockstep with the plain `requires` — the `requires transitive`↔`api` rule inverted: plain⇔implementation). |
| A4 | `CoordinatorTransport` interface | Exists in the 39-type scaffold; **Phase-2 signature is `open(Object serialPort)`** (the `Object` is deliberate — MODULE_CONTEXT flags the jSerialComm question for Phase 3). NOT thread-safe by contract (single dedicated platform thread, `IoType.SERIAL`). | **D-M92-1 (settled): the `Object` parameter STAYS.** The impl validates `instanceof SerialPort` internally. Verify the full method set at execution; STOP if the interface shape contradicts the §TechSpec mapping. |
| A5 | `gradle/libs.versions.toml` | **NO serial/jSerialComm/comm entry exists** (case-insensitive grep: zero matches). Format: `#`-category headers; `name = { module = "g:a", version.ref = "x" }`. No license-comment convention exists yet — M9.2's Apache-2.0 election comment is the first instance. | CREATE the `[versions]` + `[libraries]` entries per the instruction §Files. |
| A6 | **Module-graph knobs (BOTH)** — root `build.gradle.kts` `moduleGraphAssert` | **Knob 1 (allow-list):** `":integration:integration-zigbee -> :integration:integration-api"` ALREADY PRESENT (plus wildcards `:integration:.* -> :core:.*`, `-> :config:.*`, `-> :platform:platform-api`). **Knob 2 (height):** `maxHeight = 8`, comment pins the deepest path `app → lifecycle → integration-runtime → integration-api → persistence → state-store → device-model → event-model → platform-api` (8 edges) + do-not-raise rule. Nothing depends ON integration-zigbee yet (lifecycle requires integration + integration.runtime only); zigbee's own deepest path = `zigbee → integration-api → …` = 7 edges < 8. | **PIN: M9.2 changes NEITHER knob.** No new project edge (jSerialComm is an external jar, not a project), no consumer of zigbee added. If `:assertMaxHeight` or the allow-list trips at the gate, STOP — something outside this WU's scope moved. |
| A7 | `PermanentIntegrationException` | Exists in `com.homesynapse.integration` (integration-api) — referenced by Doc 08 §3.3 rejection tiers and by M9.1's `ExceptionClassifier`. | Below-v8 rejection + auto-detect failure both throw it. Verify constructor shapes at execution. |
| A8 | `ZigbeeFrame` sealed hierarchy | `ZigbeeFrame` (sealed) permits `ZnpFrame`, `EzspFrame` — Phase-2 records in the scaffold. | Framing codecs produce/consume these records; verify component shapes at execution before pinning test vectors to constructors. |
| A9 | `NetworkParameters` record | Exists in the scaffold (Phase-2). | Formation/resume round-trip tests serialize/compare this type; verify components at execution. |
| A10 | Zero-mint pins | `EventTypes` 71 / roster 41 / category 53 (M9.1-verified; untouched since). M9.2 publishes NO events and touches NO event-model surface. | `EventTypesTest`/`EventTypeAnnotationTest`/`EventCategoryMappingTest` must pass UNTOUCHED. |
| A11 | Replay guard | `RunPipelineReplaySafetyTest` (INV-ES-09, extended in M9.1 with the routable-fake positive control). M9.2 adds no bus interaction. | Must pass UNMODIFIED. The transport layer never sees the bus. |
| A12 | Logging state (the FE-1b rider) | **NO `logback.xml` (or variant) exists anywhere in the repo** — root level is Logback's zero-config fallback (DEBUG), which is how FE-1b observed raw `Authorization: Bearer` headers echoed at the Jetty layer under `:run`. `app/homesynapse-app/build.gradle.kts` carries `runtimeOnly` logback-classic/core + logstash encoder; `:run` is owned by the standard `application` plugin (`homesynapse.application-conventions`, mainClass `com.homesynapse.app.Main`). | Rider R1 CREATES `app/homesynapse-app/src/main/resources/logback.xml` (root INFO, console appender). No production-code change. |
| A13 | `NoRealIoExtension` | Charter note (M9-split return): it gives **no serial protection**. | "Zero real serial I/O in unit tests" is a discipline enforced by the fake-byte-seam design (§TechSpec T1), not by that extension. Do not claim otherwise in tests. |
| A14 | Bench acceptance baseline | `nexsys-bench` `corpus/coordinators/sonoff-dongle-plus-mg24_efr32mg24_ezsp.md`: frozen instrument = **EmberZNet 7.4.5.0 build 0 / EZSP v13**, VID:PID `10c4:ea60`, SONOFF-branded descriptor strings (NOT `Silicon_Labs_CP2102N`), by-id path stable, 115200 baud, flow control None. | Test constants for port identity + version negotiation anchor to these measured values. |
| A15 | z2m #30891 (re-checked 2026-07-03 by the hub) | **CLOSED.** The `ASH_ERROR_TIMEOUTS` cluster is pinned to ship-firmware **8.0.2 [GA] / EZSP v14** in the issue body (fix path: darkxst/silabs-firmware-builder#243). **No 7.4.5-line reports.** | The frozen 7.4.5.0/v13 baseline stays OUTSIDE the instability cluster. The v14 codec seam stays SYNTHETIC-test-only (physical v14 = the AMD-96 reflash contingency). No new constraint. |

**Verification method:** A1–A6, A12 read directly from source this session (two read-only survey agents + hub spot-checks; module-info/build/moduleGraphAssert quoted verbatim into the instruction). A7–A9 from the Phase-2 scaffold inventory (MODULE_CONTEXT) — component-level shapes deliberately left to execution-time STOP gates. A10–A11 from the M9.1 gate record. A14 from the bench corpus file. A15 lane-direct fetch.
