<!--
file: context/research/2026-09-14_AMBITION-MAP_return.md
purpose: THE AMBITION MAP — the strategy lane's return (D-v74-3 `SCOPE: lane`; chartered v74 b6): every candidate expansion as a row with its instrument, the overreach test row by row, the October–December sequence as H10 rows. Recommends; never orders.
audience: the hub (the intake; October's P2/P3-seam charters) · Nick (§3's words)
state-type: lane return (research; read-only domain)
status: RETURNED Mon 2026-09-14 ~08:1x CT (instrument 2026-09-14T13:08:20Z; CT = UTC−5)
-->

# THE AMBITION MAP — return

## §0 Card
**Instrument limit.** Read-only at the desk's working trees — hivemind `548744f` (the charter untracked) · core `6bd8508` · bench `f3631cb` · docs `7221ddc` · skills `180375f` — plus ONE secondary page (zigbee2mqtt's S31ZB page). No code run, no test, no device.
**Census.** 9 admitted · 13 rejected with their clause · 2 measured-at-R-5B. Every admitted instrument is a file at HEAD or a scenario named in one line.
**Top five (instrument · gate).** 1 **AM-1** metering-plug class — `metering-known-load.yaml` · `PLUG:` → ENERGY-READ → rehearsal 2. 2 **AM-6** link quality — `link-quality.yaml` · rehearsal 1. 3 **AM-14** cross-evidence confirmation — `confirm-corroborated.yaml` · AM-1 + `HARNESS-PLUG:`. 4 **AM-3** presence — `presence-four-metrics.yaml` (a person is the ground truth) · P3. 5 **AM-7** IAS water/smoke/vibration — the fixture-replay test (`EndpointClassifier.java:175`) · Tier-2's first WU.
**P1 — CONFIRMED on rank, REFUTED on instrument.** The metering class ranks first because its purchase IS P-1's shopping line: one act promotes the rehearsals' driver and opens the first energy class. But the instrument P1 names proves POWER APPLIED (`harness.proves`), never a reading; the read side does not exist today (P4).
**P2 — CONFIRMED** (13 rejections, §2). **P3 — CONFIRMED** (the instrument column).
**P4 — CONFIRMED; the corrected line.** The charter's fence (2), "the read side is the plug's `state_reported` power and energy attributes on the substrate's existing state path", is false at HEAD: (i) the bench plug is the S31 Lite zb (`constants.yaml:427`; profile `sonoff_s31_lite_zb`), a switch-only model — secondary: the S31ZB page lists `switch (state)` alone; (ii) `ClusterHandlers.java:38–52` registers eight handlers, none for 0x0B04/0x0702 (`ReportingConfigurator.java:71–72` alone knows them); (iii) `EndpointClassifier.java:99–101` maps 0x0051 → `SWITCH` + `on_off` only. Corrected: *the read side is DESIGNED (Doc 08 §3.5; the capabilities exist) and UNREALIZED in the adapter; the bench plug has no metering cluster; the class needs a metering plug AND one Core WU.* Minor: §1 item 9 — the fences are the plan's §7, not §8.
**Could not read.** The S31's own interview (no corpus entry — re-derive at the sitting); H8-a and R-5B (09-19/20).

## §1 The table (ranked by what the 72-hour run and the corpus need next)
| id | the expansion | class | buys the household | buys the company | asks of the substrate | cost | risk | THE INSTRUMENT | gate | fence | rank |
|---|---|---|---|---|---|---|---|---|---|---|---|
| AM-1 | the metering-plug class: a Zigbee plug with 0x0B04+0x0702; `power_w`/`energy_wh` on `state_reported` | device class | a watt/kWh per appliance a label can check; P-1's proof line becomes measured watts | the first energy class with `confirmed\|unconfirmed` verdicts; Battlefield 5's first datum; P-1's live leg promoted | Seam A (two handlers + the 0x0051 arm); no event type; `field_within` (§4) | 2 lane-days; 1 join window; two plugs | a divisor learned wrong reads 10× — dossier fixture first | `metering-known-load.yaml`: a labelled lamp on the DUT plug; `power_w` within ±15 % ⇒ `confirmed`, else `unconfirmed`; `energy_wh` monotone | `PLUG:` → ENERGY-READ → rehearsal 2 | PASS: no claim; a read; post-R-5B | 1 |
| AM-2 | (a) the sleepy-device adopt (the ZDO surface, C-003); (b) rejoin after power-cycle | depth | — | — | — | — | — | **measured at R-5B**: (a) §B2's counterfactual window; (b) §B4's re-seen/adopted split | R-5B | fence (1) | — |
| AM-6 | link quality: per-frame RSSI/LQI (`EzspIncomingMessage` parses, then drops) + `topologyScan()` (`EzspCoordinatorProtocol.java:1408`, no consumer) | depth | "why didn't it" for a silent device answered with its last link reading | every silence in the run carries a trace; the physics seed's P2 as data | Seam B (samples keyed by IEEE); never a registry change; no event type | 1–2 lane-days; 0 sittings | flooding if per-frame — sample on ≥8 LQI change or per 10 min | `link-quality.yaml`: after a nightly, a `TelemetryQueryService` packet block shows ≥1 sample per mains device; scan neighbours = routers on air | rehearsal 1 | PASS | 2 |
| AM-14 | cross-evidence confirmation: the lamp's `turn_on` ⇒ the metering plug's `power_w` rises | system design | "did it actually confirm" answered by physics | zero-false-CONFIRM gains a second witness; the Hue classified powered-and-silent or unpowered | none — two `state_reported` streams in one log; a corroborated verdict is a post-run AMD | 1 lane-day; rides AM-1's sitting | the plug's 5 s min-interval exceeds the lamp's 0.3–0.7 s confirm — the lag is the datum | `confirm-corroborated.yaml`: the Hue on the harness plug; `turn_on` ⇒ `power_w` > idle+5 W within 15 s; `turn_off` ⇒ falls | AM-1 + `HARNESS-PLUG:` | PASS | 3 |
| AM-3 | presence: a Zigbee mmWave occupancy sensor on ZCL 0x0406 (ingested today) on the four metrics | device class | "is the room occupied" with refutable latencies — one room of one home, line-of-sight only, factory firmware, no training (the four qualifiers) | the P3 presence dataset (asset 1); the first sensing sentence the register can carry | adapter: none (+ the absent 0x0400 illuminance handler if the unit carries it); log: `presence_signal`/`presence_changed` reserved (`EventTypes.java:194`) | 1 lane-day to design; ≥2 operator-timed sittings per room | false-clear-while-stationary needs a still human ≥30 min per arm; hand-timed arms are void (R-4b) | `presence-four-metrics.yaml`: self-timing blocks banner enter/sit/leave; four metrics = log instants vs block instants | P3 (the FOP Phase-2 home); scenario before any adapter | PASS with qualifiers; pre-run FAILS "a sitting the calendar lacks" | 4 |
| AM-7 | IAS water-leak/smoke/vibration | depth | leak detection — the cheapest irreversible-consequence sensor | the curated fleet's first non-motion IAS class without a durable mis-adoption | `iasCapability()` (`EndpointClassifier.java:175`) selects `motion` for these zone types while `IasZoneHandler.java:77` emits `active` — schema and report disagree; adoption is a one-way door: land it BEFORE such a device joins | ½ lane-day | none | the fixture-replay test: a synthetic 0x002A/0x0028/0x002D enroll ⇒ `BinaryState` with `active` in the schema | the Tier-2 charter's first WU | PASS | 5 |
| AM-10 | manufacturer codecs (Tuya 0xEF00; Xiaomi TLV) — the ~40 % of Zigbee devices | depth | the cheap families a household buys | breadth INSIDE the proven protocol | `TuyaDpCodec`/`XiaomiTlvCodec`: interfaces, no implementation (`TuyaDpCodec.java:27`) | 3–4 lane-days per codec + 1 sitting | quirk accumulation — keep each codec isolated | fixture replay from zigpy/z2m captures, then one device per codec on the rig | the Tier-2 charter; after the run | PASS; not needed by the run → P3 | 6 |
| AM-13 | the reference sensor node: a mains (router) multi-sensor whose PROFILE is authored from its design; P-1 captures its boot signature | device design | a room's evidence node that reports on change and answers pings — the class the sleepy fleet lacks | the physics seed's plugin vector proven once | a profile row; a `dut_profiles` entry; no adapter change if standard ZCL | a hardware lane-week + 1 sitting | it eats Nick's hours — a lane builds it | `profile-roundtrip.yaml`: interviewed fingerprint == declared; `harness cycle` captures the boot signature | P4 (a December design brief at most) | FAILS "needed by the run" → P4 | 7 |
| AM-4 | air quality (CO₂/PM2.5/VOC, read-only) | device class | a ventilation trigger with no actuator to answer it | a second continuous environmental channel (the fleet has ONE — R10-IN-P) | a new capability (the sealed 17 has none) + `ppm`/`µg/m³` unit rows; most Zigbee AQ devices are Tuya (AM-10 first) | 3–5 lane-days if Tuya; 1 if standard 0x040D | no reference on the desk — co-location is consistency, not truth | corpus datum: two co-located units within a band for 24 h beside a reference meter's logged reading | P3/P4; after AM-10 if Tuya | PASS (a read); "interesting, not needed" → P3 | 8 |
| AM-5 | whole-home energy (a CT-clamp meter; the six-months view) | system design | Battlefield 5's money story | Grid/VPP optionality | the most: a meter device; Seam B + an aggregation event (`telemetry_summary`) + a read route the frozen v1.1 API lacks; IR-4's read | 2 Core WUs + an API version + 1 sitting | the frozen contract | corpus datum: the meter's kWh vs the metering plug's sum over a known-load day; then the utility bill | P4 (post-11-25; the API v2 window; IR-4 measured) | FAILS "moves pre-freeze code" → P4 | 9 |

## §2 Rejected — with the clause
R-1 Thread/Matter read side — *Matter before P3* (plan §7); the standards watch is the room, not code. R-2 Z-Wave — *no instrument*; *a sitting the calendar lacks*; the Tier-2 charter owes a fixture suite and one lock (proof steps 1, 6) before Z-Wave is a row. R-3 BLE, R-5 sub-GHz — *no instrument*. R-4 local-API Wi-Fi — P-1 can power-cycle them, nothing reads the reply: *no instrument*. R-6 Green Power — Doc 08 §14 defers; EP 242 dropped by id; *no instrument*. R-7 OTA — Doc 08 §14 defers; a re-interview invalidates posture facts (Q10); *not needed*. R-8 groups/multicast — Doc 08 §14; six devices; *interesting*. R-9 device-to-device binding — a second path to actuation with no event at the log: *fails the north star*. R-10 cameras as sensors — the E-4 privacy fence; the data-collection anti-pattern; *no instrument*. R-11 MQTT/ESPHome — breadth outside the rig; confirmation is whatever the topic says; *no instrument before the run*. R-12 a Matter bridge outward — *Matter before P3*. R-13 Wi-Fi sensing — the register §0-3 (no product, no consent framework): *a claim the register cannot carry*.

## §3 October–December (H10 rows; row 1 executes the week after R-5B)
1. **`PLUG: two|one|hold`** — wk 09-21/28; AM-1's hardware. Cost: two plugs; minutes; a join window at rehearsal 1. Risk: a dossier divisor read 10× — the known load refutes it. Buys: P-1's driver promoted on a plug that is not the CONFIRMED leg's target; the first energy class. Rec: **two of one metering-capable model** (P-1 §5 criteria + a fifth: 0x0B04 AND 0x0702 in the dossier) — one `HARNESS-PLUG:`, one the DUT. Refutable-by: the S31's interview listing 0x0B04/0x0702. Blocking: no.
2. **`ENERGY-READ: charter|hold`** — October, the Java slot after 114c; AM-1's Core WU (Seam A's first family; tests first) + the `field_within` STOP-gate word + the scenario. Cost: 2 lane-days; 0 sittings. Risk: the cap of two (FE-114). Buys: the read side fence (2) assumed. Rec: charter when the plugs ship; verify at rehearsal 2. Refutable-by: the slot busy → November. Blocking: no.
3. **`LINK-READ: charter|hold`** — October, beside row 2; AM-6. Cost: 1–2 lane-days; 0 sittings. Risk: ring-store pressure — sample on change. Buys: every silence in the run carries a link trace. Rec: charter before rehearsal 2. Refutable-by: a nightly bundle that already explains a silence (none does). Blocking: no.
4. **`CORROBORATE: scenario|hold`** — November, rehearsal 3; AM-14, measurement only. Cost: 1 lane-day + rig minutes. Risk: the 5 s min-interval lag — the datum. Buys: a second witness for CONFIRMED; the Hue's silence classified. Rec: scenario; no verdict change before a post-run AMD. Refutable-by: AM-1 not landed. Blocking: no.
5. **`TIER2-FIRST: ias|presence|codecs|node`** — the P2/P3-seam charters (late Oct/Nov). Cost: AM-7 ½ day · AM-3 1 day + sittings · AM-10 3–4 days/codec + a sitting · AM-13 a lane-week. Risk: December hours. Rec: `ias` in the first Tier-2 WU, `presence` as the November DESIGN brief (no adapter), `codecs` when the curated fleet names a Tuya/Aqara device, `node` as the December brief at most. Refutable-by: the fleet list naming a Tuya device first. Blocking: no.

## §4 What the substrate must grow first
**Seam A — read-side completion in the adapter.** Doc 08 §3.5's map is realized for 8 of 11 clusters: 0x0B04, 0x0702 and 0x0400 have no handler, 0x0051 attaches no meter capability, three IAS zone types mis-select. AM-1/7/14 and every class after share one WU shape (M9.7-W2's: handler + classifier arm + fixture replay + profile row) — charter one family, dossier fixtures first. **Seam B — the telemetry PRODUCER path.** `TelemetryWriter`/`DataPath.TELEMETRY`/`TelemetryQueryService` exist; nothing writes; the frozen v1.1 read-API has no telemetry route. AM-6 needs the write + a packet read; AM-5 the route (post-freeze). Shared: the bench's `field_within` (one additive assert; a STOP-gate word); the fail-closed unit catalogue (`QuantityValue.java:67–87`) — unit rows first.

## §5 Pushback
P4 as above. The strategy layer contradicts nothing; three tensions for the Tier-2 charter: (a) the north star's "connectivity rides Matter and Thread" is the P3+ posture; Substrate §2.7 (value lands above connectivity) is why every breadth row is rejected now — breadth is never the moat; (b) Battlefield 5's "six months instantly" needs IR-4's reverse read and a telemetry route — neither exists; no energy sentence enters the register before AM-1's first `confirmed` datum; (c) the four-metric benchmark needs a person as ground truth — the P3 charter names the operator protocol. The physics seed: taken for AM-6/14/13; declined for the rest. The fence holds: H10-1 is a purchase, not a dispatch; every other row waits for R-5B and the first rehearsal.
RETURNED nexsys-hivemind/context/research/2026-09-14_AMBITION-MAP_return.md 14313
