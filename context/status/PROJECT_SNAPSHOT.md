<!--
file: context/status/PROJECT_SNAPSHOT.md
purpose: Current operational state hub — current WU, code state, deferred gates, build status.
audience: All
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-06 (v21 hub, beat 3 — HomeSynapse RUNS ON SILICON (formation/resume/0x0019-0x90/permit-join/custody/honesty-fence all green); M9.4-PJ intake ACCEPT + WUCP P2 @ e363320; TC-join gap verified + M9.4-TCJ AUTHORED; v22 re-authored. Fleet: core e363320 / docs b65ed7d / hivemind 996c83e+beat-3-pending / bench 5ceff3b+beat-3-pending / skills 0c00ece. Watermark AMD-98, 174/52). Prior: beat 1 — SD-5 ROUTED (a) + the no-non-bench-network HARD GATE; explainability lane CHARTERED; W28 IN_PROGRESS; v22 AUTHORED. Prior: 2026-07-04 (v20 hub, beat 3 — THE CLOSE: M9.4b LANDED 959f925 + CI GREEN both workflows (gate of record CLEARED); M9.4 CODE-COMPLETE; bench STAGED + SD-5 OPEN (rec (a)); v21 AUTHORED. Fleet at that close: core 959f925 / docs b65ed7d / hivemind 9bdf7a0+beat-3-pending / bench 5ceff3b / skills 0c00ece). Prior stamp chain preserved in archive/PROJECT_SNAPSHOT-beats-42-65-rotated-2026-07-04.md frontmatter.
-->

# Project Snapshot

> **2026-07-06 (v21 hub, beat 3 — **HOMESYNAPSE RUNS ON SILICON** (first bench run, frozen MG24 7.4.5.0/v13): formation (`network_formed ch25 panId=0x509b`, hashed-TCLK 0x1B84) + resume-never-reforms + **0x0019/0x90 BOTH arms** + M9.4-PJ permit-join-on-hardware + custody-persisted-key-never-logged + **the honesty fence held under a REAL transient failure** (stale-NCP → honest PERMANENT_FAILURE, never a false ALIVE) — ALL ✓ · **M9.4-PJ INTAKE: two-layer audit ACCEPT zero-defects + WUCP P2 @ `e363320`** (openPermitJoinWindow conservative-default-is-law/clamp+WARN/deadline-after-accept, 5-scenario test, 3 files 502+/1−, zero mint; the "+87/−1 truncation" alarm was the mount phantom — CI-green truth) · **the real blocker DIAGNOSED + hub-verified (4 absence greps): Trust Center join enablement gap** (no setPolicy/transient-key/join-handler — device can't complete key exchange → no Device_annce → no adopt; never-false-ALIVE structurally holds, adoption is Device_annce-gated) · **M9.4-TCJ AUTHORED ISSUE-READY** (§A TC policy + transient ZigBeeAlliance09 + 0x0024/0x0023 parse, bench-iterative; §B optional channel-pin ch20; realizes the ruled SD-5 posture, not a new door) · runbook field-validated + `2026-07-06_m9.4-bench-acceptance-record.md` written · v22 re-authored for the TC-join arc.)**
> **Next:** Nick — **DISPATCH M9.4-TCJ (CC lane; §B first then §A)** + on-bench §A iteration → **hivemind beat-3 commit (exactly 7: 6 M + 1 new — the v22 re-author is the 6th M)** → **bench commit (exactly 2: 1 M + 1 new)** → **security glance (repo was briefly public for the Pi clone — confirm no advisory auto-filed)** → **Dependabot (1 critical)** → **LAUNCH v22**. Hub (v22) — TCJ audit → on-bench join iteration (one-constant frame corrections) → real join → resume runbook P3–6 → soak → seed-custody WU (SD-5 gate rides). Detail: pm-handoff v21 beat-3 (pointer, not copy).**

> **2026-07-06 (v21 hub, beat 2 — beat-1 LANDED `66a026f` (10 exact) · **NICK'S BENCH-BLOCKER CATCH CONFIRMED: permit-join has ZERO production callers at `959f925`** (hub layer-2'd both survey agents; `isPermitJoinActive()` stub + unread schema key + no formation-time policy — the v20-staged checklist step 2 was unexecutable; hub staging miss owned) · **M9.4-PJ micro-WU AUTHORED ISSUE-READY** (wire `permit_join_duration` post-NETWORK_UP in production `run()` only; key-absent ⇒ never opens; 1 main file, zero mint/module-info/public-surface; STOP gates pinned; dispatch line in pm-handoff v21 beat-2) · **the executable bench runbook STAGED** (`nexsys-bench/docs/2026-07-06_m9.4-bench-acceptance-runbook.md` — every command source-grounded file:line; P0–P2 formation/custody legs runnable pre-PJ; P3 pairing GATED on PJ; the SD-5 bench-exception CONFIRMED; STOP conditions = joint ruling).**
> **Next:** Nick — **DISPATCH M9.4-PJ (CC lane)** → optionally run runbook P0–P2 tonight → **hivemind beat-2 commit (exactly 3: 2 M + 1 new)** → **bench commit (exactly 1 new)** → bench P3+ on the PJ landing → **Dependabot (day 9+)**. Hub — PJ two-layer audit → bench-outcome intake at the ⏺ RECORD points → soak → seed-custody WU JIT → v22 at retire. Detail: pm-handoff v21 beat-2 (pointer, not copy).**

> **2026-07-06 (v21 hub, beat 1 — LAUNCH FULL-PASS (all 11; W28 flipped IN_PROGRESS in-launch per its own note; mirrors ×3 byte-identical; fleet re-derived core `959f925` / docs `b65ed7d` / hivemind `a28441c` [v20 beat-3 VERIFIED LANDED] / bench `5ceff3b` / skills `0c00ece`; AMD-98, 174/52 from the regeneration line; §4b CLEAR) · **SD-5 ROUTED (a) BY THE HUB UNDER NICK'S EXPLICIT DELEGATION** (*"think carefully and strategically… decide/execute for yourself"* — verbatim + full rationale: pm-handoff v21 beat-1): the bench proceeds AS-SHIPPED; the generated-seed custody WU authors JIT post-bench; **HARD GATE — no non-bench network forms until it lands** (spine-recorded ×3, survives hub rotation) · **the explainability-UX lane CHARTERED** (`context/handoff/2026-07-06_explainability-ux-lane_session_prompt.md` — the source-verified 10-value vocabulary [the skill §4a's 5-value list is a stale subset, flagged in-brief], `StateConfirmedEvent`'s six components, `brightness_percent`-at-query, `StandardExplanationService:251-265` = the named target, the beat-5 scope ruling verbatim; launchable NOW, zero bench dependency) · the two Jul-5 naming artifacts → `context/strategy/` (Asimtote domains OWNED — Nick's open strategy thread; no surface renames; W-11 prices a rename at 2 tokens) · v22 AUTHORED — launch on this hub's retire.)**
> **Next:** Nick — **hivemind commit (exactly 10: 5 M + 4 new + 1 R; staged msg `_scratch/2026-07-06_hivemind_v21-beat-1_commit-msg.txt`)** → push → **THE BENCH per the v20 beat-2 staged checklist** → the 72 h soak ENTERS on acceptance (NQ-6 DURING; a restart resets the clock) → **launch the explainability lane (parallel, zero bench dependency)** → **Dependabot (day 9+, 1 critical)** → deletables. Hub — bench-outcome intake (0x0019/0x90 → javadoc flips at the next core WU; fallback only WITH Nick) → seed-custody WU JIT (the SD-5 gate rides until it lands) → W2-VAL charter at soak-entry → lane-return two-layer audit → v22 at retire. Detail: pm-handoff v21 beat-1 (pointer, not copy).**

> **2026-07-04 (v20 hub, beat 3 — THE V20 CLOSE: M9.4b LANDED core **`959f925`** (64 exact; 4739+/316−) + **CI GREEN BOTH WORKFLOWS on the push — the gate of record CLEARED**; hivemind `8c0ba20`→`9bdf7a0` (7 exact); docs lock deleted. **M9.4 IS CODE-COMPLETE** — the moat runs on a real-transport-capable adapter, every honesty fence test-asserted, every gate green. Remaining = the PHYSICAL half: **Nick's bench acceptance (STAGED, pm-handoff v20 beat-2) + the 72 h soak**. **OPEN: the SD-5 seed ruling (a/b/c — rec (a)) · Dependabot (day 7+, 1 critical, re-nagged on BOTH pushes today)**. v21 AUTHORED — launch on Nick's word; this hub retires at three beats + one full same-day arc (launch full-PASS → grounded → authored → dispatched → delivered → audited zero-defect → landed CI-green).)**
> **Next:** Nick — **hivemind beat-3 commit (exactly 4: 3 M + 1 new; staged msg)** → push → **the SD-5 one-liner** → **LAUNCH v21** → **bench per the staged checklist** → soak → **Dependabot** → deletables (hivemind cleared-lock · husk). Hub (v21) — SD-5 intake → bench outcomes recorded → soak + W2-VAL legs → the explainability-UX lane (v1.2 skill) → JIT the next coder WU (seed-custody leading if (a)). Detail: pm-handoff v20 beat-3 (pointer, not copy).**

> **2026-07-04 (v20 hub, beat 2 — M9.4b ✅ DELIVERED + TWO-LAYER AUDIT ACCEPT (ZERO code defects) · Nick's full `./gradlew check` GREEN (155) PRE-COMMIT — the deferred gate DISCHARGED (residual = core commit [exactly 64: 52 M + 12 new] + push + CI glance, the gate of record) · `HeroLoopHardwareFreeIT` 5/5 (+the brightness honest-confirm leg — frame 127 → report 127 → confirmed → `brightness_percent`=50 at query) · `RestartHonestyIT` (no double-actuation; pin-2 re-install PROVEN: post-restart window 15000) · reopen-order/NETWORK_UP-both-arms/second-boot-resume-zero-form · replay suites + the M9.3 §J floors untouched-green · 71/41/53 · ALL EIGHT [REVIEW]s RULED (R1 SD-5 divergence = instruction-worked-as-designed, security consequence → NICK: bellows generates a random hashed-TCLK seed, shipped well-known root = join characterization WITHOUT key secrecy — options framed, rec (a) bench-as-shipped + generated-seed custody WU before any non-bench network · R5 "unroutable" RULED TERMINAL · rest ACCEPT) · cross-agent note SERVED (append-below-separator miss REPEATED — annotated; skill-wording fix queued) · polish queue banked · BENCH ACCEPTANCE STAGED (hashed-TCLK at formation w/ the SD-5 fallback · 0x0019/0x90 bench-verify · Configure-Reporting explicit · the beat-42 seven covered · IAS-twin watch) → 72 h soak on acceptance.)**
> **Next:** Nick — **core commit (exactly 64; staged msg) → push → ci.yml + install-smoke glance** → **hivemind beat-2 (exactly 7 M; staged msg)** → **the SD-5 ruling (a/b/c — rec a)** → **BENCH per the staged checklist** → soak → **Dependabot (day 7+, 1 critical)**. Hub — JIT the next coder WU on bench outcomes (leading: SD-5 seed-custody) → charter the explainability-UX lane in parallel (v1.2 skill; real verdicts + `brightness_percent` live) → v21 at depth. Detail: pm-handoff v20 beat-2 (pointer, not copy).**

> **2026-07-04 (v20 hub, beat 1 — LAUNCH FULL-PASS (all 11 — the mirror sync ×3 ran; fleet re-derived core `dc1fb49` / docs `b65ed7d` / hivemind `c7549eb` / bench `5ceff3b` / skills `0c00ece`; watermark AMD-98, 174/52; §4b CLEAR) · **M9.4b AUTHORED ISSUE-READY** — `context/instructions/2026-07-04_M9.4b_real-transport-registry-unification-and-honest-verdicts_coding-instruction.md` + `WU-M9.4.md` §F–§G (P43–P70 at `dc1fb49`; 2 survey agents BOTH layer-2'd, **five agent claims REFUTED against source**; **ONE NEW DEFECT found at grounding: the dispatch service's `"invalid"` outcome is NOT ledger-guarded — the live F-2 loop-back sibling, §4-mandatory**) · seven sections in the charter's headline order (R4 unification FIRST · brightness derivation-side root fix + `brightness_percent` at query · identify issuability w/ the beat-5 semantics verbatim + the never-silent adapter fence · guard membership both directions · real transport w/ NETWORK_UP await + hashed-TCLK 0x1B84 one-constant + SecretStore custody · the ruled folds · the E2E legs) · hub rulings-at-authoring w/ veto window (R4=ctor-injection · identify=core capability · color_loop stays non-issuable · percent home=query-service decoration) · backlog row flipped + the stale CI-residual reconciled.)**
> **Next:** Nick — **hivemind commit (exactly 6: 4 M + 1 new + 1 R; staged msg)** → push → **veto window (silence = proceed)** → **DISPATCH M9.4b** (CC lane; launch line in pm-handoff v20 beat-1; allow-list re-verified P39 ✓) → **Dependabot (day 7+, 1 critical)** → deletables (2 cleared lock files · husk rmdir). Hub — two-layer audit vs the DIFF → WUCP P2 (+ MODULE_CONTEXT ×4 + **bench-acceptance staging: the beat-42 seven + DP-F + Q10 + FE-7 + the hashed-TCLK pin/fallback**) → Nick's bench → 72 h soak (NQ-6 + W2-VAL legs) → the explainability-UX lane (v1.2 skill). v21 at depth. Detail: pm-handoff v20 beat-1 (pointer, not copy).**

> **2026-07-04 (v19 hub, beat 1 — THE CURRENCY PASS EXECUTED §2A–§2F · **CI on `748962f` CONFIRMED GREEN by Nick (ci.yml + install-smoke) — the gate of record is CLEARED; M9.4b dispatch UNBLOCKS at close** · spine rotated to v18+v17 beats + the snapshot standing-section surgery (every 2026-05/06-era copied block → pointer-form; Docs 17/18 added; the archives carry the originals) · 20 SERVED prompts → handoff/archive/ · W27 CLOSED ("the week the thesis ran") + **W28 AUTHORED** (the moat on silicon) · **project-knowledge RETIRED on Nick's two rulings** (8 files → archive/; Layer-0 retargeted to the registers; two-project doc SUPERSEDED-in-practice) · THE SKILLS PASS: PM +audit-craft ×3 · instruction-format +#13 test-home grounding · review-and-quality +2 · constraint-enforcement verified-no-change at AMD-98 · coder de-staled (18-docs fix; build-verification reconciled to the lane discipline; +self-consumption guard) · **nexsys-frontend v1.2 — §4a THE VERDICT VOCABULARY IS LIVE (source-verified pointers)** · MODULE_CONTEXT content-audit ×3: automation ✓ runtime ✓ zigbee ✓+2 ruling-reconciliations · env-model §9 +2 quirks · backlog M9.1 row flipped (preflight catch) · cross-agent splice fragment excised · v20 AUTHORED — launch on Nick's word.)**
> **Next:** Nick — **hivemind commit (18 M + 4 new + 28 R; staged msg; host porcelain is truth — VM is phantom-contaminated today)** → **skills commit (exactly 1)** → **core commit (exactly 1, doc-only)** → push ×3 → **MIRROR SYNC ×3 (Check 9 clears)** → **LAUNCH v20** (M9.4b JIT is its first substantive act) → **Dependabot (day 7+, 1 critical)** → deletables at leisure (husk rmdir · inert lock rm). Hub (v20) — M9.4b JIT on the clean substrate (registry unification FIRST; quote the beat-5 record exactly) → CC dispatch → two-layer audit → WUCP P2 → bench (hashed-TCLK + fallback) → 72 h soak (NQ-6 + W2-VAL legs) → the explainability-UX lane on the v1.2 skill. Detail: pm-handoff v19 beat-1 (pointer, not copy).**

> **2026-07-04 (v18 hub, beat 6 — THE V18 CLOSE: ALL THREE REPOS LANDED — core `64f2ce0`+`748962f` (a content-exact two-commit split, reconciled: 2 renames + 41 files = the staged 43) · docs `b65ed7d` — **AMD-98 RATIFIED, the watermark is AMD-98, 174/52 unchanged** · hivemind `a94ae74`→`291a71a`→`33aae08` (incremental, content-complete). **OPEN: the ci.yml + install-smoke glance on `748962f` (the gate of record — before M9.4b dispatches). Dependabot re-nagged on BOTH pushes (day 7+, 1 critical).** Nick's CURRENCY-PASS directive received → grounded quote-exact into the v19 charter (spine rotation · 19 prompt archives · W28 plan · START_HERE/map/two-project-doc verify · project-knowledge regenerate-or-retire · THE SKILLS PASS incl. the frontend skill gaining the live verdict vocabulary · MODULE_CONTEXT content-audit · deletables); two one-liner stale claims fixed in-beat (rationale-index Layer-0 → pointer-form; infrastructure-map → docs 01–18 Locked + observed remote). **v19 AUTHORED — launch on Nick's word; this hub retires at two full arcs + six beats. The thesis is a test result.**)**
> **Next:** Nick — hivemind beat-6 commit (**exactly 6 paths**: 5 M + 1 new; staged msg) → push → **CI glance `748962f`** → **LAUNCH v19** → **Dependabot**. Hub (v19) — the currency pass → mirror sync → M9.4b JIT (registry unification first; the ruled + inherited charter) → dispatch → audit → bench (hashed-TCLK + fallback) → 72 h soak → the explainability-UX lane. Detail: pm-handoff v18 beat-6 (pointer, not copy).**

> **2026-07-04 (v18 hub, beat 5 — Nick's THREE SHARPENINGS + one confirm, folded into the M9.4b charter (backlog row; verbatim record pm-handoff beat-5): (1) brightness false-fail fix = DERIVATION-side — the beat-4 ingestion-side direction is SUPERSEDED and struck at source (Doc 08 §3.5 pins 0–254 canonical w/ query-time percent; §392's worked example ALREADY specifies `WithinTolerance` ±2 of 128 in LEVEL units; root correction = `StandardCapabilities.brightness()` schema 0–100 → 0–254, making the bridge rescale generic/schema-driven) + query-time percent derivation before dashboard render · (2) UNCONFIRMABLE issuability pins AMD-97-literal semantics — immediate rendered verdict w/ reason, never silent bypass ("never-tracked and honestly-verdicted are different promises") · (3) explanation-vocabulary is IN-SCOPE for the explainability-UX lane (the UNCONFIRMED/FAILED flattening erases the hero vocabulary) · (4) disposition-guard MEMBERSHIP TEST mandatory at M9.4b (every self-published outcome ∈ the guard set). ACCEPT stands; the three commits proceed unchanged (core 43 / docs 5 / hivemind 11 — same file set, beats 1–5).)**

> **2026-07-04 (v18 hub, beat 4 — M9.4a ✅ DELIVERED + TWO-LAYER AUDIT ACCEPT (ZERO code defects) · THE THESIS RAN: the hero loop GREEN hardware-free — motion → `occupancy.occupied` → real engine → dispatch → real adapter over the scripted NCP (byte-asserted) → honest `state_confirmed` with the causal chain joining command AND report · the F-2 supersession pair, the identify honest verdict (AMD-97-INV-01 loop-level), timeout honesty, replay purity with the REAL handler (zero NCP frames in replay) ALL GREEN · Nick's full `./gradlew check` GREEN (155) PRE-COMMIT — the deferred gate is DISCHARGED; residual = commit + push + CI glance · every test suite passed on FIRST execution.)**
> **Audit + rulings:** one agent DEFECT claim REFUTED (hub-prompt compression — the code follows the instruction's issuance-supersedes semantics + ratified caveat 3); `reindexLive` empty-registry edge honest-by-design; all six [REVIEW]s ACCEPT (IT-home=lifecycle [integration-tests piProfile-gated; hub authoring miss owned] · base-final · identify-interim · registry-duality-inert [M9.4b unification MANDATORY] · disposition guard [closes the loop-back class] · W10 gateway). **M9.4b inherits (backlog row):** registry unification · identify issuability · brightness canonicalization (RAW 0–254 vs 0–100 schema — false-fail class, grep-confirmed) · DECIDE-04 phantom comment · polish + scale register. **AMD-98 staged (ratifies on Nick's docs push):** EZSP band ==13 (supersedes AMD-96/E1 in part; never-false-ALIVE) + four realizations; zero mints, 174/52; watermark 97→98. Explainability-UX lane UNBLOCKED (real verdict vocabulary is live).
> **Next:** Nick — **core commit (`add -A`, exactly 43: 29 M + 12 new + 2 R)** → push → **ci.yml + install-smoke glance** → **docs commit (exactly 5 — your push IS the AMD-98 ratification)** → **hivemind beats-1–4 (exactly 11: 8 M + 2 new + 1 R)** → **Dependabot (day 7+)**. Hub — M9.4b JIT at the new HEAD (registry unification first) → dispatch → audit → bench acceptance (hashed-TCLK + fallback exercises here) → 72 h soak. Detail: pm-handoff v18 beat-4 (pointer, not copy).**

> **2026-07-04 (v18 hub, beat 3 — M9.4 AUTHORED, the P1 SPLIT RULED AT AUTHORING: M9.4a ISSUE-READY — `context/instructions/2026-07-04_M9.4a_moat-wiring-and-hardware-free-hero-loop_coding-instruction.md` + `context/pre-verifications/WU-M9.4.md` (P1–P42, grounded at `c856eab`, two survey agents layer-2'd) · THREE grounding corrections banked: `ConfirmationInstance` DOESN'T EXIST (ruling (a) realizes as tuned `ConfirmationPolicy`/`CommandDefinition` on the per-entity `CapabilityInstance` — read paths untouched) · the (b) bridge is LEDGER-side (CommandIssuedEvent FROZEN at 5 components; executor-side would force an event-model AMD — un-vetoed default corrected on evidence, veto window open) · `PortWatchdog`+`ReopenAction` seam EXISTS (composition = M9.4b) · turn_on's declared outcome VERIFIED (P41).)**
> **M9.4a scope:** §1 freeze-first (issuance-supersedes expiry + derivation bridge + F-10 + classifier 2 arms/3 pinned tests + MAX 14→13 w/ reflash PIE) → adoption-installed overrides (pin-2 shared installer: adopt + relink from cached matchedProfileId) → minimal command path (buildCommand ×3 byte-exact · sendZclFrame on the bench-proven layout · ZigbeeCommandHandler: F-6 hint-join, N-6 causation, immediate honest UNCONFIRMED w/ reason) → minimal composition (ZigbeeIntegrationFactory @ Main.java:71 — the ONE new public type · W10 · R4) → **`HeroLoopHardwareFreeIT` = the binary done-when** (motion → `occupancy.occupied` ASSERTED → real engine → dispatch → FakeNcp byte-asserted → Report_Attributes → honest CONFIRMED w/ causal joins; supersession pair E2E; UNCONFIRMABLE identify verdict; timeout honesty) + `ZigbeeReplaySafetyIT` (INV-ES-09, real handler, RunPipelineReplaySafetyTest untouched). Pins: zero mint 71/41/53 · zero production deps · zero module-info diffs · FakeNcp → zigbee testFixtures (3 enumerated test-scope build edits only). **M9.4b JIT after a's gate (§4b).**
> **Next:** Nick — beats-1–3 commit (**exactly 8 entries**: 5 M + 2 new + 1 rename; staged msg) → push → (b)-correction veto window (silence = proceed) → **DISPATCH M9.4a** (CC lane, launch line in pm-handoff beat-3; allow-list verified — no smoke needed) → **Dependabot (day 7+)**. Hub — audit the return two-layer vs the DIFF → WUCP P2 (+ the consolidated-amendment docs staging) → M9.4b JIT → bench → soak. Detail: pm-handoff v18 beat-3 (pointer, not copy).**

> **2026-07-04 (v18 hub, beat 2 — the M9.4 §1 DECISION BATCH RULED IN ONE TURN (the beat-57 pattern held): (a) per-device confirmability = (i) ADOPTION-INSTALLED `ConfirmationInstance` overrides from `confirmation[]` — "a realization of a Locked concept rather than a new mechanism"; TWO BINDING PINS: adapter-side mapping (INV-CE-04) + re-link RE-INSTALLS from cached `matchedProfileId` · (d) EZSP acceptance NARROWS to ==13, v14 → PIE carrying negotiated version + the AMD-96 reflash contingency — "never-false-CONFIRMED's spirit extends to never-false-ALIVE" · hashed-TCLK ELECTED for the M9.4 formation WITH the explicit fallback clause (join instability attributable → plain for Wave-1, reason recorded, W2 row) · classifier ADOPT BOTH: cause-walk-for-PIE-only + the F-5 UOE→PERMANENT arm, THREE PINNED TESTS · (b) executor-side parameters→Expectation bridge + (c) caveat-3 supersession semantics stand un-vetoed. ALL SIX ride ONE consolidated Mode-2 AMD at M9.4 §1 (number assigned at milestone). Verbatim record: pm-handoff v18 beat-2.)**
> **Next:** Nick — hivemind beats-1-2 commit (**exactly 6 entries**: 5 M + 1 rename; staged msg UPDATED to cover both beats) → push → **Dependabot (day 7+)**. Hub — **M9.4 AUTHORING NOW UNBLOCKED, all inputs ruled**: grounding-subagent at issue-time HEAD → `WU-M9.4.md` → the instruction (the consolidated §1 AMD at TOP realizing all six rulings; five-seam prove-list + the 14 ruled folds + pin-2's re-link case; done-when = the hero loop GREEN hardware-free over FakeNcp incl. BOTH F-2 directions; M9.4a/b smell-test) → proven-allow-list dispatch → audit → P2 → bench (hashed-TCLK leg + fallback) → 72 h soak. Detail: pm-handoff v18 beat-2 (pointer, not copy).**

> **2026-07-04 (v18 hub, beat 1 — LAUNCH full-PASS (all 11 — first full-PASS launch of the arc; Check 9 PASS, the mirror sync ran) · the M9-arc ADVERSARIAL RETURN AUDITED two-layer: ACCEPT — 15/15 findings layer-2 verified, ONE arithmetic correction (F-2 UNITS: the review's mireds delta was computed against the Kelvin ±50 tolerance — in capability domain the fixture triple is 6211/4630/4525 K vs the 4525 K report, a 105 K miss ⇒ false-FAIL, not false-CONFIRM; both directions banned by ratified caveat 3; severity UNCHANGED, corrected at the source finding) · RULINGS: 14 FOLD-M9.4 / 1 DEFER-W2 (F-13) / 0 FIX-NOW / 0 REFUTE — folded verbatim into the backlog M9.4 row; N-6 ELEVATED BINDING (dispatch always chains causation, oldest-deadline fallback).)**
> **Key layer-2 anchors:** F-1 confirmed byte-for-byte (bellows v13/v14 hub-RE-FETCHED — v14 widens 0x0034 tag→u16, 0x0045 +timestamp/reorder ⇒ length [18]→[30], arithmetic independently recomputed; the in-repo codecs are pure v13; negotiation accepts ≤14; **the narrow fix is ONE constant, MAX 14→13, and the honest >MAX PIE text already exists**; 0x003F verified UNCONSUMED — no additional surface). F-3 confirmed on all four legs + sharpened (`ConfirmationPolicy` already carries mode+attribute+tolerance ±2/±50 K — only the commanded VALUE is missing; `ExpectationFactory` javadoc-anticipated). F-10's fix is Doc-07-§3.11.2-COMPATIBLE (post-restart issues are outside "in-flight at crash time" — no AMD needed; conservative no-re-fire preserved). F-7 adjudicated NOT-refute (the M9.3 instruction itself mandated enroll-response ATTEMPTED; the send half is M9.4's write path; the posture over-claim is real). Verification-agent fan-out: 24 claims evidence-quoted; two verdict LABELS self-contradicted by their own quoted evidence, resolved by hub direct reads (two-layer-on-own-subagents, again). ZERO FIX-NOW: nothing bites pre-M9.4 (bench instrument v13-frozen; the moat path unwired by design).
> **Next:** Nick — **the M9.4 §1 decision batch (ONE turn: a–d + hashed-TCLK + cause-walk)** → hivemind beat-1 commit (**exactly 6 entries**: 5 M [pm-handoff · snapshot · backlog · rationale-index · the annotated audit return] + 1 rename [v17 prompt → archive/]; staged msg) → push → **Dependabot (day 7+)**. Hub — on the rulings: M9.4 authored (§1 AMD at TOP; five-seam prove-list + the ruled folds; done-when = the hero loop GREEN hardware-free over FakeNcp incl. BOTH F-2 directions + immediate honest UNCONFIRMED for identify/color_loop; M9.4a/b smell-test) → proven-allow-list dispatch → two-layer audit → WUCP P2 → bench acceptance → 72 h soak. Detail: pm-handoff v18 beat-1 (pointer, not copy).**

> **2026-07-04 (v17 hub, beat 6 — the ACTUAL v17 close: the BOUNDED SKILLS PASS executed on Nick's pre-v18 directive · the skills-initiative corpus found MIGRATED + VERSIONED (`nexsys-skills/design/`, 14 tracked; stale pointers retargeted; the empty husk deletable) · the LTD table's rows 16/17 were FACTUALLY WRONG and are fixed from the register · full skills-system activation: DEFERRAL RE-AFFIRMED with evidence · Check 9 goes STALE on commit — Nick syncs mirrors BEFORE launching v18.)**
> **The pass (9 paths):** PM SKILL.md masthead +2 (post-green adversarial-review lanes; two-layer audit extends to own subagents) · instruction-format +3 (#10 governance-freeze-first · #11 cross-repo fixture provenance + fixture-clock · #12 declare anticipated seam-fills) · review-and-quality +2 (embedded-expected-values audit fan-out + independent recomputation; the post-green adversarial pattern) · constraint-enforcement LTD-table de-lossed (row 16 now carries the REAL LTD-16: semver + the one-major deprecation floor DP-18-A builds on; row 17's phantom DECIDE-04 excised; the register-is-authoritative warning added) · coder CLAUDE.md +the append-ABOVE-separator rule · truth-hierarchy + infrastructure-map pointer/root-cause currency (CC deny-beats-allow PROVEN fix recorded). nexsys-frontend untouched (no frontend work this arc). **The hand-maintained-copy rot the truth hierarchy predicts showed up EXACTLY where predicted (a summary table) and nowhere else — the discipline holds; the 4-tier build stays deferred-but-ratifiable at `nexsys-skills/design/`.**
> **Next:** Nick — beat-6 commit (**exactly 9 paths**, staged msg) → push → **MIRROR SYNC** (PM + coder → `.claude/skills/`; Check 9 clears) → optional `rmdir skills-initiative` → **LAUNCH v18** (its prompt is unchanged; it reads beats 5–6 at boot and boots Check-9-PASS if the sync ran) → **Dependabot (day 7+)**. Detail: pm-handoff v17 beat-6 (pointer, not copy).**

> **2026-07-04 (v17 hub, beat 5 — the v17 CLOSE: the ADVERSARIAL RETURN LANDED — no CRITICAL; 3 HIGH (all on M9.4's named seams) / 8 MEDIUM / 4 LOW / 11 NOTES; hub fast-checks corroborate all three HIGHs · v18 AUTHORED — `context/handoff/2026-07-04_PM-mission-control_v18_orchestrator_session_prompt.md` · LAUNCH v18 on Nick's word.)**
> **The three HIGHs (corroborated, unruled — v18 audits first):** **F-1** v14 accepted at negotiation but 0x0034/0x0045 hardcode v13 layouts (deaf-radio class on v14 ship-batch silicon; bellows v13/v14 lane-fetched live) · **F-2** no superseded-expectation expiry in the ledger (the Hue 216→221 case ⇒ false CONFIRMED once tracking wires; AMD-97 caveat 3 mandates it) · **F-3** TOLERANCE structurally unreachable (empty `expectedOutcomes` @ StandardCapabilities:174/200 ✓; static `ExpectedOutcome`; zero production `WithinTolerance` construction ✓ — the parameters→Expectation bridge is missing). Clean sweeps: replay purity, ASH/EZSP core, codecs, retry ladder, Kelvin, §3.7, the §1 freeze layer. **All three HIGHs land exactly where M9.4 was already aimed — caught at desk price, not bench price.**
> **v18's charge:** two-layer audit the return (rule FOLD-M9.4/FIX-NOW/DEFER-W2/REFUTE) → **the M9.4 §1 decision batch to Nick in ONE turn** (moat-wiring a–d incl. the v14 posture [rec: narrow-to-13 + PIE, R4 spirit] + hashed-TCLK + classifier cause-walk) → M9.4 authored (five-seam prove-list; done-when = the hero loop GREEN hardware-free over FakeNcp incl. the F-2 supersession case; P1 split M9.4a/b LIKELY) → dispatch (allow-list PROVEN) → bench → 72 h soak. Explainability-UX lane just behind the moat wiring; W2-VAL synthetic legs in the soak window; a v14-batch Wave-2 unit is now a characterization ASSET.
> **Next:** Nick — beat-5 commit (**exactly 4 paths**: pm-handoff · snapshot · v18 prompt NEW · the adversarial return NEW; staged msg) → push → **LAUNCH v18** (fresh Cowork; this hub retires) → **Dependabot (day 7+)**. Detail: pm-handoff v17 beat-5 (pointer, not copy).**

> **2026-07-04 (v17 hub, beat 4 — M9.3 LANDED core `c856eab` + CI GREEN on the push (the gate of record) · the M9.4 CHARTER DIRECTION SET: the thesis run · the M9-arc ADVERSARIAL-REVIEW lane authored (launchable now) · v18 recommended for M9.4 authoring.)**
> **The strategic frame (Nick, beat 4 — hub-verified + extended):** everything through M9.3 = well-architected subsystems; **M9.4 = the first time the thesis runs** (motion → light → honestly confirmed, local, on a Pi). **The load-bearing find, grep-confirmed:** the moat schema is engine-unwired (`Confirmability` referenced nowhere outside zigbee) — correct sequencing, not defect — BUT the channel partially exists (`command_issued.confirmationTimeoutMs` + the ledger's DISABLED/OPTIMISTIC bypass = the §51 read-path half, already structural). **The M9.4 §1 freeze (deliberate, at top):** how per-device confirmability reaches the ledger protocol-agnostically — leading candidate: adoption installs per-device-tuned `ConfirmationInstance` overrides from `confirmation[]` (zero new event surface; UNCONFIRMABLE → DISABLED-with-reason; BEST_EFFORT → tuned timeout); alternative: the R3 richer-payload directive event. **The five-seam prove-list** (backlog M9.4 row): moat wiring · capability binding (`occupancy.occupied` asserted) · command round-trip via `ClusterHandler.buildCommand` + the identity join · **replay purity with the real CommandHandler** (ghost-commands-on-restart = the catastrophic class) · restart/lifecycle honesty (ReopenAction · NETWORK_UP await · classifier-wrap). **Sequencing: prove the loop hardware-free FIRST** (fixtures through the REAL engine + real adapter code over FakeNcp), then the radio — logic first, silicon second.
> **Next:** Nick — beat-4 commit (**exactly 4 paths**, staged msg) → **launch the adversarial-review lane** (read-only @ `c856eab`; parallel with everything) → **Dependabot (day 7 — the push re-nagged: stop deferring)** → the two in-window rulings (hashed-TCLK · classifier cause-walk) BEFORE silicon → **launch v18** for M9.4 authoring (this hub is at depth after three arcs). Explainability-UX frontend lane sequences JUST BEHIND the moat wiring; W2-VAL synthetic legs ride the soak window. Detail: pm-handoff v17 beat-4 (pointer, not copy).**

> **2026-07-04 (v17 hub, beat 3 — M9.3 DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects) · all five [REVIEW]s RULED ACCEPT · Nick's full `./gradlew check` GREEN (153 tasks) pre-commit · WUCP Phase 2 EXECUTED · the moat schema is REALIZED IN SOURCE.)**
> **The lane held every discipline:** tests-first; §1 landed as its own unit (AMD-97's eight fields byte-exact; `ConfirmationMode` STOP-gate found the core type → consumed TYPED; sealed `MatchCriteria` all-three/two-implemented; namespace convention); **the §J done-when PROVEN — both bench fixtures replay green through the real ingestion path** (SNZB 18-edge deduped occupancy stream; Hue Kelvin 2237/4525/6494/6494/6536 with the ±1-mired drift rows — audit independently RECOMPUTED both from the fixtures). Zero-mint held (71/41/53); knobs untouched; module-info diff exactly +1 plain jackson.databind; corpus transcription field-by-field exact vs `nexsys-bench@5ceff3b`; visibility census 10 public §1 + 31 package-private exact. Change set **75 paths** (9 M + 66 new). **Rulings:** R1 queue-scheduled retry ACCEPT (policy-not-threading; no sleeping threads) · R2 endpointless-ISE ACCEPT (`resumeNetwork()` precedent; TRANSIENT-classify is the safe direction) · R3 4-field `DeviceDiscoveredEvent` ACCEPT + **richer-payload event-model AMD candidate QUEUED (M9.4/M10)** · R4 `DeviceRegistry` ctor-injection ACCEPT (**M9.4 wires the instance**) · R5 stub-test update ACCEPT (M9.4 stub assertions retained). Hub extra: the TWO-handler-seam duality ruled SOUND (frozen public `ClusterHandler` = M9.4's `buildCommand` seam; package-private `ZigbeeClusterHandler` = the ingestion internal) + a MODULE_CONTEXT gotcha added. P2 folds: Doc 08 §3.6 realization note (docs, 1 path) · cross-agent M9.3 note SERVED (appended-below-separator miss annotated; ACTIVE: 0 accurate) · backlog flipped.
> **Next:** Nick — **core commit (`add -A`, exactly 75 paths, staged msg) → push → ci.yml + install-smoke glance (the gate of record)** → docs commit (exactly 1) → hivemind beat-3 (exactly 6) → **Dependabot critical (day 7)** → **hashed-TCLK + classifier cause-walk rulings NOW IN WINDOW** (M9.4 consumes both). Hub — **M9.4 JIT** on the landing (adapter factory + composition wiring [R4 instance + W10 registration + config-key binding] + command dispatch via `ClusterHandler.buildCommand` + ReopenAction ordering + NETWORK_UP await + the §51 confirmation-acceptance suite + hero E2E; wire pins 0x34/0x45/0x60, v14-dialect verify-on-silicon) → bench acceptance (beat-42 seven + DP-F + Q10 + FE-7) → 72 h soak (NQ-6 + W2-VAL synthetic de-risk during). v18 at depth. Detail: pm-handoff v17 beat-3 (pointer, not copy).**

> **2026-07-03 (v17 hub, beat 2 — M9.3 AUTHORED ISSUE-READY: the instruction + WU-M9.3 pre-verification, grounded at core `6ea6912` / docs `fb91c23` / bench `5ceff3b` · DISPATCH GATED on the CC allow-list SMOKE · beat-1 LANDED both repos exactly as staged: docs `fb91c23` (Doc 18 LOCKED, 174/52) + hivemind `f6315ee`.)**
> **The instruction** (`context/instructions/2026-07-03_M9.3_interview-ingestion-profile-registry_coding-instruction.md` + `context/pre-verifications/WU-M9.3.md` A1–A15): north star + doctrine preamble → **§1 Mode-2 AMD-correction at TOP** (DP-1 `confirmation[]` 9→10, AMD-97's eight fields VERBATIM, tests from measured corpus values · DP-2 sealed `MatchCriteria` all-three-defined/two-implemented, `Fingerprint` shaped from the corpus IR · §C namespace convention) → dossier §A–§K settled → Doc-08 §3.4–§3.7 contracts with every charter-measured truth → identity-ungated adoption slice (DP-B; A13 core-fence) → checked-exception seam map → P2 survey (ctor fan-out = ZERO existing) → zero-mint 71/41/53 → module-info diff = exactly +1 plain jackson.databind → **done-when = both bench fixtures replay green rendering recorded confirmability (§J)**. W2-VAL fenced. Backlog row → ISSUE-READY.
> **Next:** Nick — beat-2 commit (**exactly 5 paths**: pm-handoff · snapshot · backlog · instruction NEW · WU-M9.3 NEW; staged msg) → push → **the CC gate in order: settings deny-entry REMOVED + `"Bash(./gradlew:*)"` allowed → CC RESTART → smoke `./gradlew :integration:integration-zigbee:compileJava` GREEN in-session → paste the M9.3 launch line** → **Dependabot critical (day 6)**. Hub — audit the M9.3 return (two-layer vs the DIFF) → WUCP P2 → M9.4 JIT → bench acceptance → 72 h soak; v18 at depth. Detail: pm-handoff v17 beat-2 (pointer, not copy).**

> **2026-07-03 (v17 hub, beat 1 — LAUNCH + THE LOCK: DOC 18 → LOCKED · EXT-INV-1/2 MINTED (register §52; 172/51 → 174/52; the amendment watermark stays AMD-97) · the consolidated docs pass EXECUTED across docs + hivemind · Check 10 CLEARED (map refreshed) · CC allow-list ROOT CAUSE found.)**
> **Launch verification (Nick's validate-everything directive):** restart-resume clean; preflight STALE-benign on Check 10 ONLY, zero CONFLICTED; baseline re-derived and CONFIRMED on all five repos (beat-65 = `036e2aa` landed); **172/51 mechanically re-counted from the §17 table pre-Lock**; `DeviceProfile` 9-components + `findProfile(String, String)` source-verified; the beat-65 rulings inherited SETTLED (verbatim); v16 prompt archived; cross-agent ACTIVE 0.
> **The Lock (on Nick's in-session GO):** Doc 18 status → LOCKED (OQ-1 + N-2 rulings folded; the stale formal-AMD-at-pass line reconciled — the mechanism AMD rides Doc 16 OQ2, watermark-neutral; the 7 held review-fold edits ride the Lock commit). Register §52 `EXT`: EXT-INV-1/2 with B-1-completed parents, **the N-2 deferral INLINE** ("second half CI-testable at ladder rung 2" → the EXT-LADDER rows); §0.3/§17/§18/regeneration → **174/52**. Nav-index +Doc-18. **Docs pass:** Doc 05 DP-B · Doc 08 W6/R4/DP-C/DP-D/R5 · Doc 15 A4.5 · Connect reword (*monetize trust, not access*) · D-4 → Six_Battlefields · FE-1b dist-finding CLOSED-REFUTED at source · **EXT-LADDER (R-4) row created carrying F-1** · AX-7 fence closed · map refreshed (docs 15–18 rows; process/dossier catalogs; §6 → pointer form).
> **Environment:** CC allow-list ROOT CAUSE — core `.claude/settings.json` **DENIES `Bash(./gradlew *)`; deny beats allow** (explains both prior failures). Fix BEFORE M9.3 dispatch: remove the deny entry + add `"Bash(./gradlew:*)"` to allow + restart CC + trivial-compile SMOKE. Docs `index.lock` stranded by a corrupt VM index view → neutralized (`index.lock.cleared-by-cowork-2026-07-03`; delete at leisure); env-model §4 gains the corrupt-index escalation rule.
> **Next:** Nick — **docs commit (exactly 6 paths, staged msg)** → **hivemind commit (exactly 10 entries: 9 M + 1 rename, staged msg)** → push both → **Dependabot critical (day 6)** → the CC settings fix + restart + SMOKE. Hub — **M9.3 JIT authoring on the Lock landing** (grounding-subagent → WU-M9.3 pre-verification [both graph knobs; module-info verbatim; the `new DeviceProfile(` fan-out] → instruction: the AMD-97-realization Mode-2 AMD at TOP + §A–§K + DP-1/2/3 + the doctrine + the north star preamble + the coder-handoff inheritances + §4c Clock; done-when = Wave-1 fixtures replay green) → CC smoke-proof → dispatch → M9.4 → bench acceptance → 72 h soak. Detail: pm-handoff v17 beat-1 (pointer, not copy).**

_(Beat blocks v13-beat-42 -> v16-beat-65 (23 blocks) rotated 2026-07-04 by the v19 hub -> `archive/PROJECT_SNAPSHOT-beats-42-65-rotated-2026-07-04.md`; prior chain in that file's frontmatter. Full history in git.)_

## Current Work Unit

**Current work unit:** re-derive from the NEWEST masthead beat above + the backlog's M-row currency notes (pointer, not copy).

_The 2026-06-19/21-era work-unit blocks + the pre-beat-era Recent Session Log rows rotated 2026-07-02 → `context/status/archive/PROJECT_SNAPSHOT-work-unit-blocks-and-session-log-rotated-2026-07-02.md`; the M3.7/M4-era blocks rotated 2026-06-12 → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-12.md` (git-object-sourced from `58794d7`)._

## Design Documents

All 14 design documents are Locked (Phase 1 — System Design Documentation is complete).

| # | Document | Status |
|---|----------|--------|
| 01 | Event Model & Event Bus | Locked |
| 02 | Device Model & Capability System | Locked |
| 03 | State Store & State Projection | Locked |
| 04 | Persistence Layer | Locked |
| 05 | Integration Runtime | Locked |
| 06 | Configuration System | Locked |
| 07 | Automation Engine | Locked + AMD-25 integrated (2026-03-18) |
| 08 | Zigbee Adapter | Locked |
| 09 | REST API | Locked |
| 10 | WebSocket API | Locked |
| 11 | Observability & Debugging | Locked |
| 12 | Startup, Lifecycle & Shutdown | Locked |
| 13 | Web UI (Observability MVP) | Locked |
| 14 | Master Architecture Document | Locked |

**Post-foundational design docs (Locked outside the original 14):** **Doc 15** — Cryptographic Architecture (Locked 2026-06-07; AMD-94 folded 2026-06-19). **Doc 16** — Superior Automation Layer (**Locked 2026-06-20**; INV-SA-01..04 registered as §49 of the invariants register; clears the M7.2b entry-gate). **Doc 17** — AIoT + Cloud Readiness (**Locked 2026-06-26**; AIOT-INV-1 registered as §50). **Doc 18** — Extension & Plugin Architecture (**Locked 2026-07-03**; EXT-INV-1/2 registered as §52). **All 18 design docs are Locked; the register's regeneration line is the invariant-count/watermark home — re-derive, never copy.**

## Committed M3 Design Artifacts (informational)

| Artifact | Path | Date | Notes |
|---|---|---|---|
| Cross-tier deployment audit | `nexsys-hivemind/context/audits/2026-05-19_cross-tier-deployment-audit.md` | 2026-05-19 | Closed-out audit of cross-tier deployment surface. Informed M3.6 composition root design. |
| M3 audit gap-closure research (Artifact 1) | `homesynapse-core-docs/research/2026-05-20_M3_Audit_Gap_Closure_v1.md` | 2026-05-20 | Identified and closed audit-trail gaps prior to M3.4b/M3.6. |
| M3.6 Composition Root Design | `homesynapse-core-docs/design/2026-05-20_M3.6_Composition_Root_Design.md` | 2026-05-20 | Authoritative composition-root design that M3.6 implements against. |

## Interface Specifications (Phase 2 — FROZEN)

Phase 2 completed 2026-03-20 with all 16 JPMS-compiled subsystem modules specified (plus 3 scaffold modules — platform-systemd, test-support, dashboard — that have MODULE_CONTEXT.md placeholders but no compiled Java yet). See `context/planning/phase-2-block-backlog.md` for the frozen historical record of Blocks A–S. Summary at Phase 2 close: 16 modules specified, ~402 production Java files, all module-info.java files compile clean with `-Xlint:all -Werror`. AMD-33 subsequently (2026-04-10) ratified DomainEvent as permanently non-sealed.

## M3 Governance (committed 2026-05-16)

| Amendment | Status | Scope |
|---|---|---|
| AMD-41 | APPLIED | State Projection Execution Model |
| AMD-42 | APPLIED | Subscriber Lifecycle and Isolation |
| AMD-43 | APPLIED | Backpressure and Observability |

13 new invariants: INV-BUS-01..03, INV-PROJ-01/04/NEW-01, INV-WRITER-01, INV-SUB-ISO-01..06. DEC-M3-01 through DEC-M3-17 locked (DEC-M3-14/15 added 2026-05-17; DEC-M3-16 added 2026-05-20 — composition-root visibility strategy; DEC-M3-17 added 2026-05-20 — HealthSignal + HealthLevel transitive visibility promotion alongside QueueSaturationHealthCheck, DEC-M3-16 addendum). PLAN-M3-CONSOLIDATED-02 committed as the M3 implementation authority.

## Phase 3 Implementation Status (frozen historical record: M0 → M4-scoping)

Phase 3 began after Phase 2 closeout on 2026-03-20. **This table is the M0 → M4-scoping historical record, frozen 2026-05-28 — the last row's "Next: M4.0a" is history, not a pointer. Every milestone after M4-scoping lives in `context/planning/phase-3-milestone-backlog.md` + the masthead beats (pointer, not copy).** Progress:

| Milestone / WU | Status | Date | Notes |
|---|---|---|---|
| M0 | COMPLETE | 2026-04-04 | AMD-31, traceability, MODULE_CONTEXTs |
| M1 | COMPLETE | ~2026-04-08 | Contract tests + in-memory implementations |
| M2 | COMPLETE | 2026-05-01 | Full persistence layer |
| M2-bridge | COMPLETE | 2026-05-02 | AMD-34–37, structural hardening |
| M3.1 | COMPLETE | 2026-05-17 | InProcessEventBus core (14 types) |
| M3.2 | COMPLETE | 2026-05-17 | REPLAY→TRANSITION→LIVE bus-side (16 types) — `0bade6a` |
| M3.3 | COMPLETE | 2026-05-17 | Backpressure, metrics, observability (29 types) — `a5d4b2a` |
| M3.5a | COMPLETE | 2026-05-18 | StateProjection vertical slice — `a2aff9c` |
| Bus-Fix Piece A | COMPLETE | 2026-05-18 | `DerivedWriteRateLimit` visibility promotion — `fceafe8` |
| M3.5b | COMPLETE | 2026-05-18 | StateProjection production persistence — `08d0136` |
| Projection-checkpoint wiring | COMPLETE | 2026-05-19 | `StateCheckpointSource` + 10 MB advisory guardrail — `56aaa4b` |
| Supervisor DLQ wiring | COMPLETE | 2026-05-19 | `SubscriberSupervisor` constructs `DeadLetter` — `ed5862c` |
| M3.4a | COMPLETE | 2026-05-19 | Integration-tests module + harness + BurstLoadIT + HeapBudgetIT — `5ae7912` |
| M3.4b | COMPLETE | 2026-05-19 | Sustained-load + crash-recovery IT tests — `adf04d2` |
| M3.6a | COMPLETE | 2026-05-20 | Profile-driven persistence config — DeploymentProfile 6 fields, LockingMode enum, profile-driven PRAGMAs — `17c40b6` |
| M3.6b | COMPLETE | 2026-05-20 | EventBusConfig record, InProcessEventBus public (DEC-M3-16), ReplayWindowQueue parameterized — `df2743a` |
| M3.6c | COMPLETE | 2026-05-20 | Per-module event-class manifests (Q3 gap closure) — `38d3e30` |
| M3.6d-a | COMPLETE | 2026-05-20 | Composition-root satellite changes — SqliteStateStore→StateCheckpointSource, QueueSaturationHealthCheck+HealthSignal+HealthLevel public (DEC-M3-17), ReadinessSource, ReconciliationTest 4/5, Tier 9 un-disabled, HomeSynapseConfig + SharedScheduler + ThrowingStateQueryService skeletons, SLF4J wiring — `25bc23b` |
| M3.6d-b | COMPLETE | 2026-05-21 | PersistenceFactory + HomeSynapseCore composition-root wiring — 4-commit cohort: WriteCoordinator.queueSize() (`a33ee40`), production SubscriberReadConnectionFactory (`a59b64e`), PersistenceFactory public gateway (`725353d`), HomeSynapseCore facade (`dfb045e`). OR-M3-14 prerequisites bundled. — `dfb045e` |
| M3.6e.1 | COMPLETE | 2026-05-22 | MaterializedStateQueryService + ReadinessFilter + RestFilters + Javalin bootstrap + DeploymentProfile thread pool sizing. 6 created, 13 modified. Two follow-up fix rounds (Xlint:exports gateway pattern, Gradle/JPMS scope alignment). 7 deviations (none blocking). Sixteenth CC WU. — `b71ed37` |
| M3.6e.2 | COMPLETE | 2026-05-22 | Admin endpoints (DlqStatusEndpoint, ProjectionStatusEndpoint) + entity query endpoints (ListEntitiesEndpoint, GetEntityEndpoint, GetEntityStateEndpoint) + EndpointContext SPI + 2 RestFilters gateway methods + 2 ArchUnit rules (QUERY_SERVICE_READ_ONLY, REST_ENDPOINTS_NO_EVENT_PUBLISHING) + HomeSynapseCore 16-step bootstrap. 15 created, 6 modified, 19 test methods. 5 deviations (none blocking). Seventeenth CC WU. M3.6 COMPLETE. — `76288af` |
| M3.7 | COMPLETE | 2026-05-27 | E2E integration tests + abandon() + checkpoint key fix + TESTING policy. M3 COMPLETE. |
| M4 scoping | COMPLETE | 2026-05-28 | PLAN-M4-CONSOLIDATED authored; canonical scope; KB de-poison applied. Next: M4.0a. |

## Code State (pointer-form — re-derive, never copy)

Volatile code facts live in source, not here (truth-hierarchy rule 1). Re-derive at need:

- **Module census + graph:** `settings.gradle.kts` (subproject list) · root `build.gradle.kts` (graph knobs) · per-module `module-info.java` (the JPMS truth, read VERBATIM).
- **Type inventories, contracts, gotchas:** the per-module `MODULE_CONTEXT.md` files.
- **Count pins:** 71 EventTypes constants / 41 core `@EventType` records / 53 persistence codec registrations — enforced by `EventTypesTest` / `EventTypeAnnotationTest` / `EventTypeRegistryTest`+`JacksonWarmupTest`; re-derive from those tests, never from prose.
- **File/test counts:** re-derive from source when needed; any number printed in a doc is stale by construction.
- **Build gate:** the newest masthead beat + `pm-handoff.md` Open Risks carry live gate state; **CI on the pushed commit is the gate of record** (`ci.yml` + `install-smoke.yml`).

_The 2026-06-13-era Code State inventory (22 subprojects · ~803 Java files · ~2,002 tests · per-module narratives) rotated 2026-07-04 → `archive/PROJECT_SNAPSHOT-beats-42-65-rotated-2026-07-04.md` §B._

## Active Work

Re-derive from the NEWEST masthead beat + `context/handoff/coder-handoff.md` (NEXT-WU pointer) + the current week's plan (pointer, not copy). Lane routing: backend M-WUs → Claude Code; frontend/research/strategy → Cowork lanes; the hub never implements (infrastructure-map §3 + the PM skill masthead).

_The M3.6e.2-era Active Work block rotated 2026-07-04 → the archive §B._

## Blocking Issues

Re-derive from the newest masthead beat + `pm-handoff.md` Open Risks. A "None" printed here would be stale by construction.

## Open Risks

**Canonical ledger: `context/handoff/pm-handoff.md` §Open Risks — the single home, never duplicated here.** _The M3-era resolved rows (OR-M3-12/13/14/17/18) rotated 2026-07-04 → the archive §B._

## Tracked Gaps (carry forward)

_(2026-05/06-era items — re-verify against source before acting on any of them. The five closed-struck rows rotated 2026-07-04 → the archive §B.)_

1. ~~Tier 9 `reconciliationOnVersionMismatch` test `@Disabled("M3.5a")`~~ — **CLOSED (source-verified 2026-07-04 at the v19 currency pass: un-disabled + implemented at M3.6d-a; event-bus MODULE_CONTEXT Tier-9 row reads "6 active tests").**
2. **`bus.resume()` does not re-spawn VT** — pre-existing M3.1 limitation, deferred to lifecycle wiring. 2026-05-era claim; RE-VERIFY at consumption.
3. **Overflow test slow (~5–15s)** — consider `@Tag("slow")` if suite time grows.
4. **JFR-native emission is accepted design debt** — a pull-based metrics consumer (Prometheus/OTLP) needs a typed adapter layer; future observability pass.
5. **Publish-latency metric is bus-side only** — end-to-end latency is a persistence-module metric for a future pass.
6. **`lagEvents` approximation** — `pendingPositions.size()` instead of writer-tail-minus-delivered-position.
7. **Post-shutdown defensive handling in stores** — `SqliteDeadLetterStore` (and siblings) throw uncaught if called after `DatabaseExecutor.shutdown()`. Future hardening pass.

## Schedule Position

Well ahead of the 37-week Master Release Plan throughout (interface phase in 7 days; the persistence/bus/engine/integration arcs all landed early — history in `master-release-plan.md`'s Phase 3 Progress Annotation). **The governing gate now is the mid-Aug go/no-go (~Aug 16): M9.4b → bench acceptance → 72 h soak → install-smoke streak.** Re-derive live position from the newest masthead beat + the current week's plan.

## Next on Critical Path

Re-derive from the NEWEST masthead beat's **Next:** line + `context/planning/phase-3-milestone-backlog.md` + the current week's plan (pointer, not copy). Durable debt still carried: Phase 2 traceability stub indexes (docs 02–11, 13, 14) — low priority, batch later; 2026-05-era claim, re-verify at consumption.

## Recent Session Log

**The masthead beats above ARE the session record (2026-06-21 onward).** _The three 2026-06-26-era narrative rows rotated 2026-07-04 → the archive §B; older rows per the prior rotation notes preserved there._
