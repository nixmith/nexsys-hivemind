<!--
file: context/status/PROJECT_SNAPSHOT.md
purpose: Current operational state hub — current WU, code state, deferred gates, build status.
audience: All
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-07-18 (v33 hub, beat 2 — THE SCOPE FINDING (source-refuted data-only framing: no temp/humidity handlers · IAS classifies motion-unconditionally · battery-only classifies empty ⇒ the 01P would entity-less-loop; adoption is a one-way door — no removal emitter, no re-classification) → Nick RULED one WU → **M9.7-W2 AUTHORED ISSUE-READY** (profiles ×4 [S31 fingerprint withheld per the 0xC05E CoB; 0xFC57 capture-led; sensors confirmation:[]; S31 = the one confirmable, EXACT_MATCH/5000 ms-seed] + temp/humidity handlers + classifier completion [contact-aware IAS · battery-only⇒SENSOR/battery per R2(B) · 0x0302 +humidity+battery] + the learned-zoneType plumbing; zero mints/module-info/public surface; pre-verification P1–P14 banked) + THE JOINS-NIGHT GO-PACKAGE (⛔ gated on landed+CI+deploy-re-verified-at-the-Pi+hub-GO; B2-baseline-first; the 04P CONTACT-learn guard; custody reset preserves zigbee-devices.json). The joins night rides the WU landing. Detail: pm-handoff v33 beat 2.) Prior: last-verified: 2026-07-18 (v33 hub, beat 1 — LAUNCH PASS all 11: baselines 5/5 verified at source (core 1aa809d [worktree corruption found + repaired byte-exact — stray "clea" before log.info in StandardAvailabilityTracker.java, restored to exact HEAD content, md5-proven; the Pi’s deploy unaffected] · docs a53f474 · hivemind d059af6 [beat-3 landed] · bench 1c705f6 · skills dccc85f [frontend-v1.4 landed]); watermark AMD-99/175/53 re-derived at the register; Check 9 = Nick’s mirror sync RAN (15/15 ≡ both trees, PASS-per-proxy); Check 2 = W29 current + W30 pre-minted; ACTIVE 0; Open Risks clear; the v32 prompt archived; required reads done in full; NEW-HARDWARE INTAKE: the Shelly order arrived GEN4 — 2× S4PL-00116US (the dossiers §6.5-h drift: same RPC contract, driver ident-gate expects the Gen4 model string, Matter/Zigbee radios STAY OFF per stimulus independence, never M14 DUTs) + 1× Rosonway RSH-A107C; unboxed-staged, sequenced post-joins per the ledger. The M14 sweep (~Jul-28) awaits ONLY the SEED leg (the joins night) + the G1 dispatch OUT. Detail: pm-handoff v33 beat 1.) Prior: last-verified: 2026-07-18 (v32 hub, beat 3 — CLOSE-OUT: F2 [M] CLOSED both directions measured (offline ≈11.5 min ping-phase; warm return 0.7 s; API artifacts captured); THE SKILLS CURRENCY PASS EXECUTED (coder + project-manager v32-arc convention blocks; frontend v1.4 — the §4a ten-value vocabulary correction + §4c field-evidence; Check 9 goes STALE pending Nick's mirror sync); the backlog v32 rows MINTED (SKIP-VIS · BENCH-PRECOND · BENCH-AUTOSTART · RUNNER-VERSION-BANNER · POWER-RESTORE · CMD-API · BTN-AMD · TAMORO/R-1); THE COMPOUNDING-TESTING DOCTRINE BRIEF DELIVERED (context/process/2026-07-18_compounding-testing-doctrine.md — the charge discharged); v33 BANKED (v32 retires on its launch). Detail: pm-handoff v32 beat 3.) Prior: 2026-07-18 (v32 hub, beat 2 — I3a CLOSED AS FROZEN + the M14 RO condition ([PASS] 3/3, 0 forbidden; reopen +17 s, the 6th silicon rep; the liveness third under REV2 with outcomes QUOTED — five real dispatches through the reopened transport) · the power-outage attribution CLOSED (Nick-confirmed; the app exonerated; the events-vs-logs durability split PROVEN at the cut; BENCH-AUTOSTART row minted, rule at B3) · the stale-instrument reconciliation (the Pi had not pulled REV2; durable rule: deploy-state re-derived at the instrument; RUNNER-VERSION-BANNER candidate) · THE SILENT-SKIP FINDING (first-class: the §3.9 SKIP gate + rehydrated-UNAVAILABLE from the Jul-14 20:41:54 cut = four days of lawful do-nothing runs, invisible end-to-end; SKIP-VIS core WU candidate + BENCH-PRECOND + the doctrine exhibit; every component correct, the composition untold) · F2's clean cycle QUEUED (closes on Nick's paste; the composed evidence banked) · the Coder's B1-REV1 note restamped Archived-SERVED (ACTIVE: 0). Detail: pm-handoff v32 beat 2.) Prior: 2026-07-18 (v32 hub, beat 1 — LAUNCH PASS all 11: baselines 5/5 verified at source (core 1aa809d · docs a53f474 · hivemind c43587e [beat-3 landed 18-file exact] · bench 1c705f6 [REV2 landed, census 15 exact] · skills ee0839f); watermark AMD-99/175/53 re-derived at the register; porcelain ×5 clean; the engine.py new_run_after launch residual DISCHARGED (line-read vs REV2-1 — point-for-point conform incl. the D2 log_any-exclusion, first-act first-ATTEMPT-wins snapshot, the conservative bound, outcomes-QUOTED; +1 conservative v0 lint: no mixed runs-asserts, B2 defines the mix); Check 2 = W29 current through 07-19, the W30 plan PRE-MINTED; Check 9 = proxy 14/15 ≡ with the ONE differ being exactly the known v29 masthead line (PASS-per-prior-verification — the governing local-mirror comparison unreachable this session, no .claude mount in the VM; re-confirm offered to Nick); cross-agent ACTIVE: 1 EXPECTED (the Coder's B1-REV1 note); Open Risks §4b CLEAR; the v31 prompt archived. CHASES OPEN at launch: the I3a operator rep + the F2 offline leg (charge 1 — nothing else outranks). Detail: pm-handoff v32 beat 1.) Prior: 2026-07-18 (v31 hub, beat 3 — the triple intake: B1-REV2 ACCEPT-WITH-RATIFICATIONS (D1–D4; census 15 exact; the Pi rep = I3a+M14-RO); wave2 Phase 0 COMPLETE (4/4 model keys; CR2477 ×3; joins gate held); the counsel arc consumed (Pelton consult 07-21; TSDR reclassified RESPONSE-EQUIVALENT; §1a domain-clause superseded — tamoro.com not owned, negotiation live); F2 offline half still owed; v32 BANKED — v31 retires on its launch. Prior: 2026-07-15 (v31 hub, beat 2 — rulings landed ((A) · Wave-2 in hand · hardware ordered · memo cloud-safe); A4 discharged; B1-REV2 authored + dispatched; two lanes chartered (wave2-setup [joins hub-gated] · counsel-navigator); the compounding-testing doctrine banked; F2 offline half in flight. Prior: 2026-07-14 (v31 hub, beat 1 — LAUNCH PASS all 11 (W29 minted = the Check-2 reconciliation; mirrors 15/15 ≡; v30 prompt archived); THE CERTIFICATION CLOSED on the record (soak 3d12h unbroken on 04f5f70 · exit spike ×3 green + 1 attributed operator rep · ~1,728 cumulative recorded verdicts ZERO false CONFIRM · the close-out block = the bench acceptance record 2026-07-13/14 · instrument correction: projection_live = the type-filtered registry checkpoint, lawfully 14; the aged stake rides viewPosition); deploy 1aa809d CLEARED (step 26); the backlog rows flipped (M9.4+M9.5 certified-CLOSED · NQ-6-EXIT DONE); read-surface findings banked for G1; the B1 liveness-leg hazard pre-armed. Prior: 2026-07-12 (v30 hub, beat 5 — commits verified (bench c18bf63 · hivemind 92ec713 + the ride-along reconciliation); the TAMORO program intaken (self-governing; R-1 held for G-2); the strategic stack delivered; the hardware reality named (nothing ordered/unboxed — orders fire Monday); v31 BANKED. Prior: 2026-07-12 (v30 hub, beat 4 — B1-REV1 desk half TWO-LAYER ACCEPT (independent re-execution ×8; [REVIEW] ×3 adjudicated, 2 hub-owned; the §5 set RATIFIED); both commits ordered; v31 banking next. Prior: 2026-07-12 (v30 hub, beat 3 — the B1 contract RULED "(c)+(a)" + five riders + the ~Jul-24 C1 tripwire (verbatim: pm-handoff v30 beat 3); B1-REV1 issued; the I3a OPERATOR adjudication recorded with the ratification block; CMD-API = a named ruled core WU sequenced with B2 ahead of M14; the escalation SERVED, ACTIVE 0; beats 2+3 consolidate. Prior: 2026-07-12 (v30 hub, beat 2 — R1–R3 ruled + recorded verbatim; the criteria checklist RATIFIED + FROZEN (I3a/I3b split; 21 M / 8 S); B1 ⛔ G-B1-2 STOP (command endpoints UNWIRED — the beat-1 grounding claim corrected, hub-owned); CMD-API named; PM REC (c)+(a) packaged. Prior: 2026-07-12 (v30 hub, beat 1 — LAUNCH 9/11 + 2 named-STALE (Check 8 cleared in-session — the SERVED restamps ×3 + ACTIVE-0; Check 9 awaits Nick's mirror sync); B1 AUTHORED ISSUE-READY (dispatch gated post-close-out); v29 prompt archived. Prior: 2026-07-12 (v29 hub, beat 1 — TRACK A CODE-COMPLETE all CI green (AVAIL `1aa809d` two-layer ACCEPT; the lane re-audits ×4 → `ada8b46`; R4 in part; v30 banked). Prior: 2026-07-10 (v28 hub, beat 2 — TRACK-A EXECUTION LANDED ×3, all CI GREEN: M9.6-SEED full-bar ACCEPT → a1f0f77 · the CI-red flake diagnosed at source (the M9.5-DUR label/projection race, NOT SEED) → M9.5-DURc test-only barrier → 7e4426b (completing a1f0f77's gate record; two hub-owned instruction errors ratified-accept) · M9.6-RO two-layer ACCEPT → 1627978 (tiers char-identical; six INFOs ratified) — ONE record discrepancy: 1627978's message claims the R1 by-id-pin test landed; it did not (duplicate-dispatch verify-not-redo; hub-owned staged-message-future-state) → corrected append-only via the R1 micro-commit · AVAIL dispatched (rail satisfied) · the 4 lane returns AMENDED on-disk, delta re-audits = v29's opening act · v29 BANKED at context depth. Pending: the R1 micro-commit + the consolidated hivemind beat-1+2 commit (targeted 10 paths, lane returns excluded).) Prior: 2026-07-10 (v28 hub, beat 1 — LAUNCH FULL-PASS all 11 (remote §12; watermark AMD-99/175/53 re-derived; mirrors 15/15 ≡; v27 prompt archived); THE RETURNS INTAKE EXECUTED (a)→(b)→(c): four files ALL ACCEPT-WITH-CORRECTIONS (two layer-1 agents each + hub layer-2 on every finding; probe-blocks ×3 delivered — in-place fixes → delta re-audit → ONE amendment commit); ESC-W2-SNZB01P-01 packaged (REC: honest fallback + named AMD row); the (b) refute-first: SURVIVES-WITH-CAVEATS (REC: conditional-M14 + slide trigger); the S31 0x0104 code-read CLOSED (no gating — exact_model carries Monday); M9.6-SEED builds, full-bar audit armed, the rail holds RO/AVAIL. Pending: the beat-1 commit (exactly 4 paths: 2 M + the archive R-pair). Prior: 2026-07-10 (v27 hub, beat 5 — THE AUTHORING ARC COMPLETE (M9.6-RO + M9.6-AVAIL issue-ready; SEED building; Track A fully authored); the beat-4 sweep RECONCILED (7 vs 3 — the lane returns rode; hub-owned; rule sharpened: fresh porcelain at order time); the AVAIL unknowns settled (relink device-grain no-op; sidecar write-side-only + empty-map per the ruled trap; dispatch-feed OMITTED); THE HANDOFF WORD GIVEN — v28 launches, v27 retires. Pending: the beat-5 commit (exactly 4: 2 M + 2 new). Prior: v27 beat 4 — M9.6-SEED AUTHORED ISSUE-READY (SD-5; presence-as-marker custody; posture-agnostic ops widening; transient-key PIN; AS-FORMED restore; zero mints/module-info/config; silicon leg composes w/ Wave-2 joins) — DISPATCH ON THE WORD; the returns intake DECIDED (v28-as-intake-hub, (a)→(b)→(c), probe-loop; launches on v27's handoff); all three lanes RETURNED (files on disk). Pending: beat-4 commit (exactly 3: 2 M + 1 new) → the dispatch. Prior: v27 beat 3 — THE WEEKEND PROGRAM: Nick's full go recorded verbatim (Track B all-three w/ shaping · Track A rails [no-open-REVIEW; seed-custody full-bar] · HW approved+ordered · ZBDongle-P two adversarial scenarios · MONDAY PINNED · hygiene-doesn't-drop); R1 confirm APPENDED (capture bug field-confirmed); v28 BANKED; 3 lane prompts authored; seed-custody grounding banked (pre-verifications/WU-SEED-CUSTODY.md). Pending: bench commit (2 M) + beat-3 commit (7: 2 M + 5 new) + the lane launches. Prior: v27 beat 2 — THE RULINGS LANDED verbatim-recorded (NQ-6 (b) exit-act · no-change (b)+never-upgrades · supersession (b)+rider · hs-radio chartered · frame-ctr row · stack ratified w/ M9.6-RO⊗B1 + HW-now); the hygiene sweep EXECUTED; the record correction APPENDED; Phase 7 BANKED; B0 DELIVERED. Pending: bench commit (exactly 5) + hivemind beat-2 (exactly 8). Prior: v27 beat 1 — LAUNCH FULL-PASS all 11; the retrospective session OPEN: grounding reads ×3 run + L2-confirmed (R1 capture-not-matching overturn · R2 tracker-unwired #6 · R3 HOL confirmed); NQ-6 + rulings pack + stack + the bench-automation brainstorm to Nick; v26 prompt archived. THE SOAK RUNS UNTOUCHED (day 1). Pending: beat-1 commit (exactly 4: 2 M + 1 R + 1 new). Prior stamp: the v26 beat-3 line, git history.)
-->

# Project Snapshot

> **2026-07-18 (v32 hub, beat 3 — **CLOSE-OUT:** **F2 [M] CLOSED** both directions measured (offline on evidence ≈11.5 min post-cut [ping-phase; the out-wait-the-instrument lesson recorded] · **warm return 0.7 s** · API before/after artifacts + both log lines; with I3a at beat 2, **both cheap MUST-halves are DONE**) · **THE SKILLS CURRENCY PASS EXECUTED** (coder + PM v32-arc convention blocks · frontend **v1.4**: the §4a TEN-value vocabulary correction + §4c field-evidence + rename-readiness; **Check 9 STALE pending Nick's mirror sync**) · the backlog v32 rows MINTED (SKIP-VIS · BENCH-PRECOND · BENCH-AUTOSTART · RUNNER-VERSION-BANNER · POWER-RESTORE · CMD-API · BTN-AMD · TAMORO/R-1) · **THE COMPOUNDING-TESTING DOCTRINE BRIEF DELIVERED** (`context/process/2026-07-18_compounding-testing-doctrine.md` — the charge discharged) · **v33 BANKED** (v32 retires on its launch; the joins night rides v33).)**
> **Next:** Nick — **the beat-3 commit (exactly 7: 5 M + 2 ??; msg `_scratch/2026-07-18_hivemind_v32-beat-3_commit-msg.txt`) → push → the nexsys-skills commit (exactly 1 M; msg `_scratch/2026-07-18_skills_frontend-v14_commit-msg.txt`) → push → your skills mirror sync → LAUNCH v33 tomorrow** (attach `context/handoff/2026-07-18_PM-mission-control_v33_orchestrator_session_prompt.md`) → your program (consult 07-21 · tamoro · veromo · LLC). Hub (v33) — the profiles WU → **THE JOINS NIGHT** → CMD-API → G1 + SKIP-VIS → the ~Jul-28 M14 sweep (ONLY SEED + G1 remain) → v34 banked. Detail: pm-handoff v32 beat-3 (pointer, not copy).**

> **2026-07-18 (v32 hub, beat 2 — **I3a [M] CLOSED AS FROZEN + the M14 RO condition CLOSED** (`[PASS] usb-reenumeration-manual 3/3 · 0 forbidden` — detection instant · **autonomous reopen +17 s, the 6th silicon rep in the 16–17 s envelope** · the REV2 liveness third: a post-reopen organic run with **outcomes QUOTED `['UNCONFIRMED','UNCONFIRMED','FAILED','UNCONFIRMED','DISPATCHED']`** — five real dispatches through the reopened transport; the FAILED = a fresh field rep of the superseded→FAILED flattening for G1) · **the outage attribution CLOSED** (power cut ≈00:28 Jul-16, Nick-confirmed; the app exonerated; events-vs-logs durability split PROVEN; **BENCH-AUTOSTART row** → rule at B3) · the stale-instrument reconciliation (the Pi had not pulled REV2; durable rule: **deploy-state re-derived at the instrument**; RUNNER-VERSION-BANNER candidate) · **THE SILENT-SKIP FINDING** (the §3.9 SKIP gate × availability REHYDRATED-UNAVAILABLE from the Jul-14 20:41:54 cut = four days of lawful, completely invisible do-nothing runs; every component correct, the composition untold; **SKIP-VIS core WU candidate** [skip token + chain rendering + the completedVerdict fix, routes with G1] · BENCH-PRECOND · the doctrine exhibit) · the Coder's B1-REV1 note Archived-SERVED (ACTIVE: 0) · **F2's clean cycle QUEUED — closes on Nick's paste.**)**
> **Next:** Nick — **the F2 cycle (~12 min) → ⏺ paste → the beat-2 commit (exactly 3 M; msg `_scratch/2026-07-18_hivemind_v32-beat-2_commit-msg.txt`) → push** → launch the Coder on the profiles WU on the hub's hand-off → your program (consult 07-21 · tamoro · veromo · LLC). Hub — the profiles WU (grounding-first, tonight) → **THE JOINS NIGHT package (Sunday)** → CMD-API (Monday, the honest tripwire) → G1 refresh + OUT + SKIP-VIS authoring → skills/folds/doctrine → the ~Jul-28 M14 sweep (cert ✓ · RO ✓ · F2 tonight · SEED Sunday · G1 Monday). Detail: pm-handoff v32 beat-2 (pointer, not copy).**

> **2026-07-18 (v32 hub, beat 1 — **LAUNCH (PASS all 11):** baselines verified 5/5 at source (core `1aa809d` · docs `a53f474` · hivemind `c43587e` [beat-3 landed 18-file exact] · bench `1c705f6` [B1-REV2 landed, census 15 exact] · skills `ee0839f`) · watermark **AMD-99/175/53** re-derived · porcelain ×5 clean · **the engine.py `new_run_after` launch residual DISCHARGED** (line-read vs REV2-1: point-for-point conform — the D2 log_any-exclusion, first-act first-ATTEMPT-wins snapshot, the conservative bound, outcomes-QUOTED; +1 conservative v0 lint [no mixed runs-asserts; B2 defines the mix]; no defect — the REV2 audit basis is complete at line level) · Check 2: W29 current through 07-19, **the W30 plan PRE-MINTED** · Check 9: proxy 14/15 ≡, the ONE differ = exactly the known v29 masthead line (PASS-per-prior-verification; local-mirror re-confirm offered) · cross-agent ACTIVE: 1 EXPECTED (the Coder's B1-REV1 note) · the v31 prompt archived · **CHASES OPEN: the I3a operator rep + the F2 offline leg** (charge 1 — asked of Nick at launch; nothing else outranks).)**
> **Next:** Nick — **the I3a rep paste (either way) → the F2 offline paste (both deltas) → the beat-1 commit (exactly 5: 2 M + 1 ?? + the archive D/?? pair; msg `_scratch/2026-07-18_hivemind_v32-beat-1_commit-msg.txt`) → push** → your program (the tamoro negotiation [walk $3,400] · veromo.com · LLC · the 07-21 consult). Hub — the profiles WU (charge 2) → **THE JOINS GO** (paste-blocks via Nick; SEED formation → Wave-1 rejoin → S31 → 02P → 04P → 01P → the D1 5/5 + B2 reads) → CMD-API (the honest ~Jul-24 tripwire) → the G1 explainability refresh → skills/folds/doctrine → the ~Jul-28 M14 sweep → v33 banked. Detail: pm-handoff v32 beat-1 (pointer, not copy).**

> **2026-07-18 (v31 hub, beat 3 — **THE TRIPLE INTAKE · v32 BANKED (v31 retires on its launch):** **B1-REV2 ACCEPT-WITH-RATIFICATIONS** (the 4-lens fleet found+fixed a genuine `log_any`-anchor false-PASS DEFECT pre-delivery · mutant-killer fixtures mutation-verified · census 15 exact · Nick's independent demo re-runs exact · [REVIEW] D1–D4 RATIFIED-ACCEPT · the engine.py line-read = a named v32 launch residual · **the ONE Pi rep closes I3a AS FROZEN + the M14 RO condition**) · **wave2 Phase 0 COMPLETE-ACCEPT** (4/4 exact-model keys off the housings · 8 CoB resolved incl. the 04P battery gap = CR2477 ×3 · EUI-64-not-on-housing correction · the S31-powered-early joins carry-in · the joins gate HELD — the profiles WU is the remaining half) · **the counsel arc consumed as pointers** (package delivered · **Pelton consult BOOKED 07-21** · TSDR 07-16→07-17 RECLASSIFIED → RESPONSE-EQUIVALENT [TOMORO likely registers narrowed; moderate §2(d) tier; the goods-gap armor unchanged] · **§1a SUPERSEDED-BY-LOG: tamoro.com NOT owned** — brokerage negotiation live, walk $3,400 ruled · veromo.com still NXDOMAIN) · **F2's offline half STILL OWED** (v32 chases it first) · the skills pass + backlog folds + doctrine brief = v32 charge #6.)**
> **Next:** Nick — **the bench REV2 commit (exactly 15: 5 M + 10 ??; msg `_scratch/2026-07-18_bench_b1-rev2_commit-msg.txt`) → push → Pi pull → ONE `usb-reenumeration-manual` rep → ⏺ paste** → **the F2 offline leg** → **the hivemind beat-3 commit (exactly 8: 4 M + 4 ??; msg `_scratch/2026-07-18_hivemind_v31-beat-3_commit-msg.txt`) → push** → **LAUNCH v32** (attach `context/handoff/2026-07-18_PM-mission-control_v32_orchestrator_session_prompt.md`) → your program (tamoro.com · veromo.com · LLC · the 07-21 consult). Hub — v31 retires on the v32 launch; v32: I3a+F2 close → the profiles WU → THE JOINS → CMD-API (the honest tripwire) → the explainability dispatch → counsel folds → skills/doctrine → the ~Jul-28 M14 sweep → v33 banked. Detail: pm-handoff v31 beat-3 (pointer, not copy).**

> **2026-07-14/15 (v31 hub, beat 2 — **THE RULINGS LANDED:** R-i **(A)** liveness semantics (→ **B1-REV2 AUTHORED + DISPATCHED**: `new_run_after` assert, anti-false-PASS fixture-pinned, `new_confirmed_run` retained for B2's strong variant; the arc closes on ONE post-landing operator rep = **I3a + M14-RO together**) · R-ii **Wave-2 ALL FOUR IN HAND** + ZBDongle-P stowed-never-powered; placements ruled (S31 inline w/ the lamp = a future bulb power-cut lever; 04P desk-first then door; 01P = the log-marker instrument; 02P desk) · R-iii **Shelly ×2 + Rosonway ORDERED, <48 h** · R-iv memo cloud-safe (repo REC dropped; the counsel-package addendum chartered) · **A4 DISCHARGED [S]** (SIGKILL → 11 s rehydration, viewPosition monotonic; availability_changed measured +37 s/+56 s) · F2 positive ✓ / **offline half IN FLIGHT** (the lamp sits dark behind the powered-off S31 — the stimulus is live) · **TWO LANES CHARTERED** (`2026-07-15_wave2-device-setup_operator-lane_session_prompt.md` [Phase-1 joins ⛔ hub-gated on the profiles WU + SEED go] · `2026-07-15_tamoro-counsel-navigator_lane_session_prompt.md` [executive primer + §4 + the package addendum + the 07-16 TSDR audit]) · the **compounding-testing doctrine brief** = a named hub deliverable (Nick's charge: ratchet rule · monotone verdict asset · fixture-paired assert semantics · the corpus compounds into shipped device intelligence).)**
> **Next:** Nick — the F2 offline paste → the Coder's REV2 return here for audit → **the beat-2 commit (exactly 5: 2 M + 3 ??; msg `_scratch/2026-07-15_hivemind_v31-beat-2_commit-msg.txt`)** → launch the two lanes → the Shelly/Rosonway unboxing waits for the single-variable order. Hub — the REV2 audit · the Wave-2 profiles WU · the joins go-signal · CMD-API (the Jul-24 tripwire watches) · the explainability dispatch · the doctrine brief · the skills pass + backlog folds · v32 banked at the next boundary. Detail: pm-handoff v31 beat-2 (pointer, not copy).**

> **2026-07-14 (v31 hub, beat 1 — **LAUNCH PASS all 11** (W29 minted = the Check-2 reconciliation · mirrors 15/15 ≡ [Nick's sync ran] · watermark AMD-99/175/53 re-derived · v30 prompt archived) · **THE CERTIFICATION IS CLOSED ON THE RECORD** (the soak ran 3d 12h UNBROKEN on `04f5f70`, SIGTERM-clean attributed stop · the exit spike ×3 green + 1 attributed operator rep [relinked ×2 per boot · zero `device_proposed` · both 5b ULIDs · viewPosition 13,790→14,007] · **~1,728 cumulative recorded verdicts, ZERO false CONFIRM** [~345 organic runs; C1's N≥200/N≥500 numerically exceeded — gate-day re-status per the frozen J1] · RSS ≈40% of bound · I2 clean · the instrument correction: `projection_live` = the type-filtered registry checkpoint, lawfully 14 — close-out block: the bench acceptance record 2026-07-13/14) · **DEPLOY `1aa809d` CLEARED (step 26)** · read-surface findings banked for the G1 dispatch (superseded/identify render FAILED on causal-chain · automationName null post-restart · trigger→command ≈90 ms) · **the B1 liveness-leg steady-state hazard PRE-ARMED** (a wave-run confirms nothing in bulb steady state — adjudicate at the Pi half, never improvise) · the `TC_REJECTED_APP_KEY_REQUEST` watch NAMED vs B1 boot-health's forbidden list.)**
> **Next:** Nick — **the bench commit (exactly 1 M; msg `_scratch/2026-07-14_bench_certification-close_commit-msg.txt`) → the hivemind beat-1 commit (exactly 6 entries: 3 M + 1 ?? + the archive D/??-pair; msg `_scratch/2026-07-14_hivemind_v31-beat-1_commit-msg.txt`) → STEP 26: pull → build → deploy `1aa809d` → the silicon legs (SEED fresh-formation = the SD-5 discharge, composing with Wave-2 joins · AVAIL both directions via the Hue mains power-cut path) → the B1 Pi half (Blocks 0–4; **constants `watermark: 14` per the instrument correction — NEVER the viewPosition**) → Wave-2 joins if in hand** → the scp log copy-off (retention §3) → the hardware orders + the Wave-2 in-hand answer → `rm -r _scratch/_to_delete-2026-07-13/` at leisure. Hub — the legs + B1-Pi-half intake (the liveness adjudication expected) · Wave-2 profiles/scenarios from the (a) dossiers · CMD-API authoring · the explainability dispatch refresh. Detail: pm-handoff v31 beat-1 (pointer, not copy).**

> **2026-07-12 (v30 hub, beat 5 — **B1 runner v0 LANDED on bench `c18bf63`** (Pi half owed post-close-out) · hivemind `92ec713` (+ the ride-along reconciliation: the Tamoro instruction rode the add -A — benign, corrected append-only) · **the TAMORO brand program INTAKEN** (Nick's own self-governing instruction: TAMORO REC · VEROMO fallback · Asimtote lock untouched · gates G-1/G-2/G-3 Nick-owned · **R-1 [brand architecture a/b] OPEN — rule at G-2** · the 07-16 TSDR audit Wed) · **the strategic stack delivered:** Monday = certification → deploy `1aa809d` → silicon legs on NO-NEW-HARDWARE paths (F2 via the Hue mains power-cut offline leg; I3a via the OPERATOR usb rep) → the B1 Pi half; the week = CMD-API (the Jul-24 tripwire watches) → explainability dispatch (G1, the longest pole) → B2/B3; **the hardware orders fire Monday** (Shelly ×2 + Rosonway; the Wave-2 SONOFF in-hand question OPEN — order Monday if not, D1 lead-time insurance). **v31 BANKED.** THE SOAK: exits ≥ 2026-07-13 ~07:50; **Phase 7 = the exit act, BEFORE any pull/deploy.**)**
> **Next:** Nick — the beat-5 commit (msg `_scratch/2026-07-12_hivemind_v30-beat-5_commit-msg.txt`; exactly 3 entries: 2 M + 1 ??) → **MONDAY ~07:50: Phase 7 steps 20–26 VERBATIM → paste → LAUNCH v31** (attach `context/handoff/2026-07-12_PM-mission-control_v31_orchestrator_session_prompt.md`) → the hardware answers + orders → the Tamoro program per your own instruction. Hub (v31) — the certification + deploy + silicon-legs intake · the B1 Pi-half audit · CMD-API authoring · the explainability dispatch · B2/B3 · the backlog folds · v32 banked. Detail: pm-handoff v30 beat-5 (pointer, not copy).**

> **2026-07-10 (v28 hub, beat 2 — **THE TRACK-A EXECUTION ARC LANDED, ALL CI GREEN:** core advances `04f5f70` → `a1f0f77` (M9.6-SEED, full-bar ACCEPT — the SD-5 code half; silicon leg = Monday) → `7e4426b` (M9.5-DURc — the CI-red ×2 diagnosed at source as the M9.5-DUR label/projection race, TEST-ONLY checkpoint barrier ×3 lifecycle ITs; two hub-owned instruction errors ratified-accept; completes a1f0f77's gate record) → `1627978` (M9.6-RO, two-layer ACCEPT — reopen identity capture fixed at the re-attributed root; FIXED-PENDING-SILICON, the R-row closes on B1 usb-reenumeration + both topologies). **One RECORD DISCREPANCY corrected append-only** (1627978's message claims the R1 by-id-pin test at 452; the correction block re-read as a duplicate dispatch and the fresh session verified-not-redone at 451 — the R1 micro-commit lands the test + the correction; durable rules minted: staged messages assert only confirmed state; correction blocks are headed as corrections). **M9.6-AVAIL DISPATCHED** (the last Track-A WU; intake = v29). The 4 lane returns sit AMENDED on-disk (probes applied); delta re-audits + ONE amendment commit + the rulings packaging = **v29's opening acts — v29 BANKED** (this window at extreme depth; the lane deep-dives run fresh). Filesystem-MCP = the verified host-truth read route (VM worktree git reads retired for core). THE SOAK: untouched; Phase 7 Monday ~07:50.)**
> **Next:** Nick — the R1 micro-commit (1 M) → the consolidated hivemind beat-1+2 commit (TARGETED 10 paths; lane returns EXCLUDED) → AVAIL builds → **LAUNCH v29** (attach the v29 prompt; provide the three lane responses there). Hub — v28 retires on the v29 launch. Detail: pm-handoff v28 beats 1–2 (pointer, not copy).**

> **2026-07-10 (v28 hub, beat 1 — **LAUNCH FULL-PASS all 11** (remote-Cowork §12; §11 clean; HEADs = v27's close: core `04f5f70` · docs `a53f474` · hivemind `f9c1ca8` · bench `e48f04d` · skills `ee0839f`; watermark re-derived **AMD-99, 175/53**; mirrors 15/15 ≡; v27 prompt archived) · **THE RETURNS INTAKE EXECUTED (a)→(b)→(c) — four files, ALL ACCEPT-WITH-CORRECTIONS** (two layer-1 agents per return + hub layer-2 adjudication of every finding against primary text): **(a)** five line-scale corrections (headline: the 04P cluster-id source CONFLICT — quirk decimal 64567 = 0xFC37 vs capture 0xfc57, mis-decoded + a false "byte-for-byte" claim, caught before it shipped into `zigbee-profiles.json`); layer-2 both ways — the alleged-invented "293–701 ms" REFUTED at the bringup report L114, the `confirmation: []` precedent VERIFIED against the shipped 03P profile, and **the S31 0x0104 code-read CLOSED at source: no profile gating anywhere — exact_model carries Monday's adoption** · **ESC-W2-SNZB01P-01 packaged** (the button gap: Doc 02 no button type/press capability vs Doc 08's `button` rows vs Doc 07's state-only Tier-1 triggers; PM REC = honest V1 fallback [battery-only adoption, presses in logs, deliberate scoping] + a NAMED AMD row sequenced at the (b) ruling; NOT Monday-blocking) · **(b)** the ruled refute-first pass: **SURVIVES-WITH-CAVEATS** — the pick (M14 = Shelly Gen2 `integration-shelly`, ~4–6 WUs) held against a genuine attack; the unconditional pre-gate TIMING did not (unpriced vs the ruled-first window; one contingency WU vs M9's 9-on-14 tail; `IoType.NETWORK` unexercised; "silicon-certify" undefined) — **PM REC: conditional-M14** (opens after the ruled post-soak stack clears; slide trigger ~Jul 31 → post-launch arc #0 at zero strategic cost); live-verified: CSA Adopter derivative = $2,500 (return's $1,500 wrong); D-OPEN-2/3 real; dual-role-plug constraint ADOPTED (M14 DUTs ≠ B1/B2 stimulus actuators, same window) · **(c)** criteria: N=200 math CORRECT (86.6%), statuses spine-true, MVP §8 anchors hub-verified (10 ms/50 ms/10k-replay), G1's explainability-status error confirmed (the return is still OWED, not awaiting audit); naming: PyPI `asimtote` collision MATCHED on adversarial re-run, alternates-gap + Asimptote-B.V.-in-§0 owed, the astro sibling-path trap CONFIRMED at `website/site/astro.config.mjs:15` · **probe-blocks ×3 DELIVERED** · **M9.6-SEED BUILDS** (the FULL-BAR audit armed; the no-open-[REVIEW] rail holds the RO/AVAIL dispatch words).)**
> **Next:** Nick — **beat-1 commit (TARGETED adds — exactly 4 paths: 2 M spine + the v27-prompt archive D/??-pair → the R; NOT `add -A`: the live SEED Coder's coder-handoff write is on the tree and rides the SEED intake beat instead; msg `_scratch/2026-07-10_hivemind_v28-beat-1_commit-msg.txt`) → push** → paste the 3 probe-blocks into the still-open lanes → the rulings queue ((b) sequence + M14 timing + proof-scenario rewording · ESC-W2-SNZB01P-01 · criteria Q1–Q4 · naming + its three sub-minute checks) → **report the M9.6-SEED delivery on the Coder's return**. Hub — the SEED FULL-BAR audit (critical path; RO dispatch word rides its clearing) → probe-delta re-audits + ONE amendment commit → B1 runner WU + explainability dispatch (Tuesday-tolerant) → skills-constellation design if the window allows → **v29 banked before depth**. THE SOAK: day 1, untouched; Phase 7 Monday ~07:50. Detail: pm-handoff v28 beat 1 (pointer, not copy).**

> **2026-07-10 (v27 hub, beat 5 — **THE AUTHORING ARC COMPLETE — ALL THREE TRACK-A WUs AUTHORED** (M9.6-SEED dispatched + building · **M9.6-RO ISSUE-READY** [fix-the-capture + the pinned-only-PATH sentinel w/ NO class fallback + `isPinnedOnly()` honesty + the injectable canonicalizer + the `port_identity_captured` INFO; the field-repro RED leg + the devnum-immunity PIN + the two-sticks safety leg; done-when = the B1 usb-reenumeration scenario + BOTH topologies] · **M9.6-AVAIL ISSUE-READY** [the beat-5 reads settled the last unknowns: the relink emission is a DEVICE-grain projection no-op (the boot-UNKNOWN mystery SOLVED — availability applies at ENTITY grain only); the cache sidecar EXISTS but sidecar-init trips the ruled M-1 trap ⇒ EMPTY-map ctor + sidecar WRITE-side per transition, snapshot-init = a named deferred WU; `ZclDispatch.send` = NCP-acceptance only ⇒ dispatch NEVER feeds the tracker (would fabricate liveness), the mains ping arm is recordCommandResult's ONLY caller; derivation gates on state_reported ⇒ zero automation wakes; per-adopted-ENTITY publish fanout, canonical online/offline, CRITICAL-offline/NORMAL-online; done-when = the SNZB-02P reports→AVAILABLE silicon leg + a genuine offline leg]) · **THE BEAT-4 SWEEP RECONCILED** (`e4052d5` staged 7 vs stages-exactly-3 — the four lane returns rode the add -A; HUB-OWNED; benign [returns committed pre-audit; v28 audits the committed files, corrections = amendment commits]; **DURABLE RULE SHARPENED: sweep-guards re-derive from a FRESH porcelain at COMMIT-ORDER time, after every known writer**) · **THE HANDOFF WORD: v28 LAUNCHES NOW** (v27 retires; v28 owns the returns intake a→b→c + the SEED FULL-BAR audit + the RO/AVAIL dispatches per the no-open-[REVIEW] rail + Monday's pinned sequence) · **THE SOAK RUNS UNTOUCHED** (day 1; Phase 7 Monday ~07:50).)**
> **Next:** Nick — **hivemind beat-5 commit (`git add -A` stages exactly 4: 2 M + 2 new; sweep-guard re-derived fresh this beat; msg `_scratch/2026-07-10_hivemind_v27-beat-5_commit-msg.txt`) → push** → **LAUNCH v28** (attach `context/handoff/2026-07-10_PM-mission-control_v28_orchestrator_session_prompt.md`; the read-and-follow paste) → report the M9.6-SEED delivery to v28 when the Coder returns → the soak stays boring. Hub — v27 RETIRES on the v28 launch; v28: returns intake → SEED audit → dispatches → Monday → v29 before depth. Detail: pm-handoff v27 beat-5 (pointer, not copy).**

> **2026-07-10 (v27 hub, beat 4 — **M9.6-SEED AUTHORED ISSUE-READY — THE SD-5 WU** (`context/instructions/2026-07-10_M9.6-SEED_generated-seed-custody_coding-instruction.md`, baseline `04f5f70`, against the committed pre-verification): a generated per-install TCLK seed replaces the well-known root for NEW formations — **DP-2 presence-as-marker** (the seed secret's existence IS the posture record; zero JSON-schema/record changes; the schema field deferred to FRAME-CTR) · **DP-5** the `CoordinatorOps.formNetwork` widening (posture-agnostic protocol; policy in `NetworkFormation`; the 0x0068 struct otherwise byte-untouched) · **DP-4** AMD-68 `setAll` for the atomic key+seed mint (durable-before-form) · **DP-7 AS-FORMED restore** (the corruption bound RECORDED, FRAME-CTR closes it) · **DP-8 the transient-key PIN** (the seed NEVER rides `importTransientKey`) · one new INFO token (`zigbee.tc_link_key_posture`) · no live re-key (operator re-formation = the post-soak recipe) · 5 STOP gates · 11 tests-first · **done-when = unit-green + the fresh-formation silicon leg (SNZB TCLK 0x06→0x34 under the seed) — ONE re-formation serves it AND Wave-2 joins; Nick rules timing at close-out; THE SD-5 HARD GATE discharges on that leg** · **audit = FULL BAR (the ruled rail)** · **THE RETURNS-INTAKE DECIDED:** one session — **v28 = the intake hub**, order (a) dossiers → (b) roadmap [adversarial refute-first on the REC] → (c) briefs, probe-loop via Nick into the still-open lane conversations, returns commit only post-audit; **v28 launches on v27's handoff word** (after M9.6-RO + availability-ALIVE author here) · beat-3 landed ×2 (bench `e48f04d` 2-exact · hivemind `4062953` 7-exact) · all three lanes RETURNED · **THE SOAK RUNS UNTOUCHED** (day 1; ends ≥ 2026-07-13 ~07:50).)**
> **Next:** Nick — **hivemind beat-4 commit (exactly 3: 2 M + 1 new; sweep-guard: the whole porcelain; msg `_scratch/2026-07-10_hivemind_v27-beat-4_commit-msg.txt`) → push** → **DISPATCH M9.6-SEED** (the paste line: pm-handoff v27 beat 4) → the soak stays boring. Hub — M9.6-RO next (capture shape + both-topologies + the B1 coupling) → availability-ALIVE (the verbatim traps) → the M9.6-SEED FULL-BAR audit on delivery → the handoff word → v28 launches for the returns intake. Detail: pm-handoff v27 beat-4 (pointer, not copy).**

> **2026-07-10 (v27 hub, beat 3 — **THE WEEKEND PROGRAM IS GO (Nick's rulings verbatim: pm-handoff v27 beat 3)** — **Track B ALL THREE lanes** ((a) Wave-2 dossiers ×4 + the Shelly Gen2 stimulus appendix, FIRST-CLASS + MONDAY-BLOCKING, corpus standard · (b) integration roadmap DECISION-SHAPED — an executive sequencing REC w/ trade-offs [Matter/Thread vs the local-first honesty thesis · Z-Wave · local-WiFi w/ the Shelly-in-hand pilot case study · MQTT] · (c) ruled order: go/no-go criteria + naming brief FIRST → LICENSE-flip consumes (b) → website last) · **Track A as-ready dispatch** w/ the rails: NO new dispatch over an open [REVIEW]; **seed-custody's audit = the FULL bar regardless of tempo (SD-5 never trades rigor for speed)**; slips slide to Tuesday, never compress an audit · **HW APPROVED + ordered tonight** (2× Shelly Plus Plug US + Rosonway RSH-ST10C-6; rails: dongle-behind-hub = POST-soak single-variable step w/ boot-health re-run; **M9.6-RO verifies in BOTH topologies** — "port identity is where assumptions go to die") · **ZBDongle-P adversarial APPROVED as TWO B2 scenarios** ((1) the NVM-overwrite ch22 repro [zigpy foreign-forms OUR stick, app stopped → boot → mismatch-PIE + restore] FIRST, no second dongle needed · (2) the RF-adversarial live-coordinator class, Wave-2-era; both bench-only) · **MONDAY PINNED verbatim: the exit-act spike on the SOAKED `04f5f70` BEFORE any pull/deploy** → close-out → certification CLOSED → deploy → silicon legs → B1 → Wave-2 joins (runbook step 26 added) · **the R1 zero-touch confirm LANDED + APPENDED** (`production_session_started: port=/dev/zigbee protocolVersion=13` — the capture bug FIELD-CONFIRMED on the soak boot) · **v28 BANKED before deep work** (Nick's confirm honored) · **the 3 lane prompts AUTHORED — launch pending** · **the seed-custody grounding survey COMPLETE + banked** (`context/pre-verifications/WU-SEED-CUSTODY.md`: two TC_LINK_KEY use sites; NVRAM-resume writes nothing; restore rides the formation struct; custody already SecretStore-shaped w/ AMD-68 `setAll` for the atomic key+seed pair; ZERO module-info edges; seed-on-NEW-formations-only is code-consistent; the transient key STAYS well-known [joiner bootstrap]; 12 DPs + test seams; FULL small WU per the ruling's own templates) · **Wave-2 unboxed inventory declared** (S31 Lite zb · ZBDongle-P [the Z-Stack half of pre-M14 + the adversarial tool] · SNZB-02P/-04P/-01P) · beat-2 landed ×2 (hivemind `931b730` 8-exact · bench `d1d6e3c` 5-exact) · **THE SOAK RUNS UNTOUCHED + BORING** (day 1; ends ≥ 2026-07-13 ~07:50; any glance anomaly interrupts everything).)**
> **Next:** Nick — **bench commit (exactly 2 M; msg `_scratch/2026-07-10_bench_v27-beat-3_commit-msg.txt`) → push** → **hivemind beat-3 (exactly 7: 2 M + 5 new; msg `_scratch/2026-07-10_hivemind_v27-beat-3_commit-msg.txt`) → push** → **LAUNCH the three lanes** (fresh Cowork conversations; attach each prompt; the read-and-follow paste line; (a) FIRST — Monday-blocking) → **ORDER THE HW** → the soak stays boring. Hub — **beat 4 NOW: the seed-custody instruction ISSUE-READY → the dispatch word** → M9.6-RO (capture shape + both-topologies) → availability-ALIVE (the verbatim traps) → the explainability dispatch + the B1 WU (Tuesday-tolerant) → skills-constellation if the window allows → lane intakes as they land → v28 launches at the weekend's natural boundary. Detail: pm-handoff v27 beat-3 (pointer, not copy).**

> **2026-07-10 (v27 hub, beat 2 — **THE RULINGS LANDED (all six + Q1–Q5; verbatim of record: pm-handoff v27 beat 2)** — NQ-6 **(b) the spike as the soak's EXIT act** (the aged `projection_live` position = part of the expected evidence; restart ONLY — no dongle acts) · no-change **(b) log-derived explanation** + the HARD constraint *"the explanation NEVER upgrades or replaces a verdict"* (a named EXPLAIN-PUSH requirement) · supersession-payload **(b) the read-API is the sanctioned decrypt path** + the contract-conversation rider (never raw-SQLite in a scenario) · **hs-radio CHARTERED** (bench-tooling tier; the product-surface crossing = a named STOP) · **frame-counters = a named row after M9.6-RO** · **the stack RATIFIED** with the coupling explicit: **B1's runner + the uhubctl lever IS M9.6-RO's regression harness** (B1 delivers the re-enumeration scenario BEFORE M9.6-RO's verification); CMD-DEADLINE after M9.6-RO; CMD-LANES + the §3.10 AMD = Wave-2 · Q1–Q5: B0 now · bench repo · **HW ORDERED NOW (plug + uhubctl hub, decoupled from Wave-2)** · nightly-post-B3 + full-suite-pre-milestone · API-first ✓ · **EXECUTED THIS BEAT:** the bench-record CORRECTION appended (append-only: the reopen root RE-ATTRIBUTED to identity-capture; F1's candidate retired; zero-touch confirm staged) · **runbook Phase 7 = the NQ-6 exit act BANKED (Monday is mechanical)** · **B0 DELIVERED** (the RATIFIED bench-automation charter + SCENARIO_FORMAT v0 + the log-retention policy) · **THE CHARTERED HYGIENE SWEEP DONE** (backlog: the 23 KB mega-row → 1 certified row **+15 ruled rows**; cross-agent: DUR+DURb archived SERVED, ACTIVE: 0; coder-handoff ROTATED 993→88 [archive file new]; playbook §8 +5; positioning notes NEW in strategy/; `_scratch`: ~103 served msgs archived, the deletables staged in `_to_delete-2026-07-10/` for Nick's one rm) · beat-1 landed `065864d` 4-exact · **THE SOAK RUNS UNTOUCHED** (day 1; ends ≥ 2026-07-13 ~07:50 Pi-local).)**
> **Next:** Nick — **bench commit (`git add -A` stages exactly 5: 2 M + 3 new; sweep-guard: the whole porcelain; msg `_scratch/2026-07-10_bench_v27-beat-2_commit-msg.txt`) → push** → **hivemind beat-2 commit (`git add -A` stages exactly 8: 6 M + 2 new; sweep-guard: the whole porcelain; msg `_scratch/2026-07-10_hivemind_v27-beat-2_commit-msg.txt`) → push** → `rm -r _scratch/_to_delete-2026-07-10/` at leisure → **ORDER THE HW** (plug + uhubctl hub) → optional R1 zero-touch grep paste → **the soak stays untouched (Phase 7 = Monday's mechanical script)**. Hub — beat 3+: WU authoring in the ruled order (**seed-custody FIRST** → M9.6-RO [capture shape; B1-verified] → availability-ALIVE [the verbatim traps] → the explainability dispatch → the B1 runner WU) → the skills-constellation design doc if the window allows (else v28 agenda #1) → **v28 banked before depth**. Detail: pm-handoff v27 beat-2 (pointer, not copy).**

> **2026-07-10 (v27 hub, beat 1 — **LAUNCH FULL-PASS (all 11)** · **THE RETROSPECTIVE SESSION OPENS — three grounding reads RUN + hub-L2-confirmed at `04f5f70`:** **R1: the reopen root is identity-CAPTURE, not matching (the recorded hypothesis OVERTURNED)** — the two-tier `reopenTarget` predicate is devnum-BLIND; an alias-pinned `serial_port` (the bench pins `/dev/zigbee`, runbook-verified) takes `resolvePort()`'s synthesis arm (`ZigbeeIntegrationAdapter:405`) and captures `PortIdentity(0,0,alias)` (`:443`) that NO enumerated candidate can ever satisfy — **the watchdog's reopen has been structurally dead since boot on every alias-pinned config**; first-open synthesizes/discovers without a match (the restart-recovers asymmetry); M9.6-RO REC = fix-the-capture + the pinned-only-path sentinel rule + the PortIdentity honesty fix; **udev-RAMP coupling recorded** (the alias best practice IS the defect window) · **R2: `StandardAvailabilityTracker` is UNWIRED — mechanism-without-driver #6** (compiled + tested, zero production refs; the `onFrame` seam self-documents as the availability feed; view-UNKNOWN structural) — WU REC: wire the EXISTING tracker (empty-persisted-map first [the M-1 suppression trap] · RX-only evidence with pinned MUST-NOTs · the mains read-ping arm LOAD-BEARING for honest offline · per-entity publish); INSIDE the explainability push (the FE-7 data dependency) · **R3: command HOL CONFIRMED at adapter scope** (single-thread `integration-cmd-zigbee-0`, unbounded FIFO = the documented Doc 08 §3.10 contract; dispatch awaits the NCP's EZSP response — 3.2 s/exchange, K×6.4–10 s queue drag; DEFENSIBLE at 2 devices, REAL at scene fanout) — named row: shape-C honest dispatch-deadline near-term · shape-A per-device lanes + a §3.10 AMD at Wave-2 · **NQ-6 + five design rulings + the stack REC'd to Nick (letters suffice; NQ-6 REC (b) spike-as-EXIT-act — replay on 72 h-aged state is the STRONGER proof, no clock arithmetic)** · **NEW DIRECTION BANKED (Nick's charge): the bench-automation & test-suite brainstorm** (`context/assessments/2026-07-10_bench-automation-and-test-suite_direction-brainstorm.md` — the runbook becomes a scenario library over the verdict-stream oracle · AUTO/OPERATOR tiers · stimulus-independence · B0 charter desk-only NOW → B1–B3 post-soak → B4 the labeled-tuple corpus → B5 advisory-only ML inside AIOT-INV-1) · v26 prompt archived · **THE SOAK RUNS UNTOUCHED** (day 1; ends ≥ 2026-07-13 ~07:50 Pi-local).)**
> **Next:** Nick — **hivemind beat-1 commit (`git add -A` stages exactly 4: 2 M + 1 R + 1 new; sweep-guard verified — those four ARE the whole porcelain; msg `_scratch/2026-07-10_hivemind_v27-beat-1_commit-msg.txt`) → push** → **THE RULINGS (one line each): NQ-6 [REC b] · no-change [REC b] · supersession-decrypt [REC b] · hs-radio [REC y] · frame-counters [REC row] · the stack order · brainstorm Q1–Q5** → **the soak stays untouched** (watch: mismatch/foreign · 0xFFFF · `reopen_no_target` [a bumped dongle STAYS DEAD — restarts are RULED acts]; optional zero-touch R1 confirm: `grep production_session_started` on the boot log). Hub — rulings intake → the hygiene sweep (beat 2) → WU authoring in ruled order (seed-custody · M9.6-RO · availability-ALIVE · the explainability dispatch · B0) → v28 at ~beat 3–4. Detail: pm-handoff v27 beat-1 (pointer, not copy).**

> **2026-07-10 (v26 hub, beat 3 — **THE RETROSPECTIVE + v27 BANKED** — `context/assessments/2026-07-10_acceptance-arc-retrospective_and_direction.md` captures the acceptance arc's full technical yield (accomplished / failed-with-hub-failures-owned / discovered, along Nick's three lenses: human-software interface · codebase-vs-real-world precision · direction; + the REC post-soak priority stack [soak exit + NQ-6 ruling → seed-custody SD-5 → the explainability/observability push ELEVATED → field-reality hardening (reopen-locator · hs-radio first-party tooling · frame-counters · udev) → the design rulings (no-change verdict vocabulary · supersession-decrypt · serialization read)] + the repo/bench hygiene inventory) · the v27 prompt charters: soak stewardship (NQ-6 tension ruling REC spike-as-exit-act) → the deep retrospective → the hygiene sweep → the ruled WUs → v28 · `312f0dc` reconciled (7-file sweep vs stages-exactly-6 — the sweep-guard rule minted) · **THE SOAK RUNS to ≥ 2026-07-13 ~07:50 Pi-local**.)**
> **Next:** Nick — **hivemind beat-3 commit (`git add -A` stages exactly 4: 2 M + 2 new; msg `_scratch/2026-07-10_hivemind_v26-beat-3_commit-msg.txt`) → push** → **LAUNCH v27** (fresh conversation; attach `context/handoff/2026-07-10_PM-mission-control_v27_orchestrator_session_prompt.md`; paste the read-and-follow line) → the soak stays untouched. Hub — v26 retires on the launch; v27 runs soak-watch → retrospective+rulings → hygiene → WU authoring → v28 before depth. Detail: pm-handoff v26 beat-3 (pointer, not copy).**

> **2026-07-10 (v26 hub, beat 2 — **THE ACCEPTANCE RUN, RUN — §51 CORE LEGS PASS · THE 72 h SOAK ENTERED (07:50:35 Pi-local, core `04f5f70`)** — the DURb silicon confirmation CLOSED (`adoption_maps_rehydrated: devices=2` at INFO, every boot) · 5b L1/L2 CLOSED (L1 superior variant: real announces → relink through a full network loss+restore; L2: the exact 5b ULIDs across restarts + a Pi reboot + two network swaps) · **`zigbee.network_restored_from_parameters` FIRST SILICON REP** (the NCP was found hijacked to a foreign ch22/0xFC4D network — writer UNATTRIBUTED, investigation row; the custody fence PIE'd honestly ×6 and the restore re-formed ch20/0x9b65 from custody; both devices returned voluntarily in <30 s) · identity held across THREE networks in one night · §51: supersession probe PASSED (superseded verdict at +1 ms, one CT confirm, zero CT timeouts) · identify immediate-honest at 81 ms (reason verbatim) · brightness confirmed 0.33 s · IAS twins absorbed (~15 runs, one per motion) · timeout honesty ×3 organic classes · **ZERO false CONFIRMs (~25 verdicts)** · **CONFIRMED DEFECT: the reopen-target locator is blind to re-enumeration** (detection/backoff textbook; `reopen_no_target` ×4 AFTER the journal-proven re-attach; availability-not-trust → POST-soak micro-WU) · +R-rows: availability-UNKNOWN-despite-reports · the 0xFFFF TCLK-timeout watch · ops: `tools/bench.sh` adopted, ModemManager disabled, the Hue-app BT reset + bellows NCP-clear recipes recorded.)**
> **Next:** Nick — **bench commit (stages exactly 3: 2 M + 1 new `tools/bench.sh`; msg `_scratch/2026-07-10_bench_acceptance-run_commit-msg.txt`) → push** → **hivemind beat-2 (stages exactly 2 M; msg `_scratch/2026-07-10_hivemind_v26-beat-2_commit-msg.txt`) → push** → **LEAVE THE SOAK RUNNING (ends ≥ 2026-07-13 ~07:50 Pi-local; restarts reset the clock; NQ-6 scheduled with the hub)**. Hub — soak watch (NQ-6 scheduling · ch22/0xFFFF recurrence watch) → soak-window WUs (seed-custody [SD-5 HARD GATE] · explainability return) → post-soak queue (reopen-locator micro-WU · availability row · on_off leg · backlog compaction) → v27 before depth. Detail: pm-handoff v26 beat-2 (pointer, not copy).**

> **2026-07-10 (v26 hub, beat 1 — **M9.5-DURb INTAKE + TWO-LAYER AUDIT ACCEPT (zero code defects at layer-2)** — §1 the identity-path repair CONFIRMED at source (the LINKED arm requires ≥1 registered entity + the fall-through WARN `zigbee.half_registration_detected` [the only new token]; `adopt()` reuses the durable deviceId BEFORE minting — the AMD-99 F1 idempotent re-emit, seeded-id test-proven incl. the idempotent-replay leg + the no-`availability_changed` pin) · §2 rehydration INFO unconditional, token frozen (presence-pinned verbatim at devices=0 AND 2) · §3 all 18+3 BENCH-VERIFY flips comment/javadoc-ONLY (mechanically verified; constants byte-untouched; zero tokens remain in src/main+testFixtures; citations cross-checked to bench iterations 1–5b; the §A.3 expiry flip honesty-bounded — the lesson of record in coder-lessons) · §4 a successful reopen clears `permitJoinDeadline` (stale-true regression-pinned inside the un-elapsed window; the TCJ R2 limitation CLOSED) · §5 the fence reads the INSTALLED policy first (installed-DISABLED ⇒ dispatch + immediate honest UNCONFIRMED + never tracked — the M9.4-RPT R2 silent class CLOSED; installed-wins + tuned-timeout + no-entity-fallback all pinned; both frozen identify pins byte-untouched) · §6 `RegistryApplyCensusTest` pins production apply-callers to EXACTLY {slice, adapter, subscriber} + demonstrably bites · cross-cutting: exactly 11 M + 1 new; ZERO mints (73/43/55 hold)/module-info/schema/build/public-API · four [INFO] deviations ACCEPT (the G-DB5 gate text = hub authoring imprecision, owned) · **findings: one hub-side mount-phantom caught + adjudicated at the bridge (coder-lessons append PRESENT) · the v25 beat-7 rewrite commit unlanded → ordered · 5b closing legs L1/L2 STILL OPEN → chased (P4 binds the 5b ULIDs)** · preflight PASS all 11 · v25 prompt archived.)**
> **Next:** Nick — **core commit (`git add -A` stages exactly 12: 11 M + 1 new; msg `_scratch/2026-07-10_core_m95-durb_commit-msg.txt`) → push → CI glance (the gate of record; bench only after green)** → **hivemind ×2** (targeted 1-M beat-7 rewrite commit [msg `_scratch/2026-07-08_hivemind_v26-rewrite_commit-msg.txt`] → beat-1 `add -A` exactly 6 [5 M + 1 R; msg `_scratch/2026-07-10_hivemind_v26-beat-1_commit-msg.txt`] → push) → **the two 5b closing legs (⏺ paste; sleep-after-pkill + re-cat TOKEN)** → standing ledger. Hub — CI intake → 5b close → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero on the 5b ULIDs + RESUME leg → P5 §51 → P6 + PJ key OUT; the DURb piggyback = the entry-boot `adoption_maps_rehydrated: devices=2` INFO + a normal adoption) → **the 72 h soak ON FINAL CODE** (NQ-6 = the DUR+DURb proof at scale) → seed-custody (SD-5 HARD GATE) + explainability in the soak window → **v27 before depth**. Detail: pm-handoff v26 beat-1 (pointer, not copy).**

> **2026-07-08 (v25 hub, beat 6 — **THE SKILLS CURRENCY PASS** (Nick's pre-dispatch directive): **coder** (+dur-arc conventions; mental-model §3 = the registries-as-projections contract in full; java-patterns §14 = the `?:` promotion re-box) · **project-manager** (+format additions #14–15 [own-DP surface-export check; crash-window rulings + fleet-is-layer-1]; +per-commit-msg-files + remote-bridge rules) · **frontend v1.3** (+§4b IDENTITY IS DURABLE — stable entityIds, removal-not-wired, honest availability-UNKNOWN) · **env-model §12** (the remote-Cowork bridge model) — ALL pointer-not-copy, zero volatile state in any skill · **Check 9 STALE ×3 until Nick's mirror sync (sync BEFORE the DURb dispatch)** · beat-5 landed (bench `9c98240` · hivemind `306793c`).)**
> **Next:** Nick — **hivemind beat-6 (exactly 8 M; msg `_scratch/2026-07-08_hivemind_v25-beat-6_commit-msg.txt`)** → **nexsys-skills (exactly 1 M; msg `_scratch/2026-07-08_skills_frontend-v13_commit-msg.txt`)** → **MIRROR SYNC ×3** → the two 5b closing legs (⏺ paste) → **THE WORD on M9.5-DURb**. Hub — 5b close → DURb audit → THE ACCEPTANCE RUN → the 72 h soak on final code → LAUNCH v26 (banked). Detail: pm-handoff v25 beat-6 (pointer, not copy).**

> **2026-07-08 (v25 hub, beats 4–5 — **M9.5-DUR LANDED `ef1d2c5` + CI GREEN ×2 (gate CLOSED)** · **THE DURb ROUTING RULED (a): BEFORE THE SOAK, 5b-FIRST** (Nick, VERBATIM at pm-handoff v25 beat-4 — *"you soak the code you ship"*; NQ-6 = DURb's natural test bed; hard line: DURb never during/after the soak) · **ITERATION 5b: THE DUR PROOF ON SILICON** — one-time events-DB wipe (custody preserved; the 3 orphans died) → re-adopt ×2 under the NEW event flow (Hue `01KX1PA4GR…` 3/3 · SNZB `01KX1PB9A5…` 2/3 + **the M9.4-KEYb silicon confirmation CLOSED**: exactly one `key_established`, zero failed) → full process restart → **`registry.projection_live: devices=2 entities=2 position=14`** + DP-6 `device_relinked` ×2 (deviceIds match the adoption log — device-id continuity proven) + view = EXACTLY 2 · two closing legs at close (L1 physical pin-2 · L2 entity-ULID API-to-API) · **M9.5-DURb AUTHORED ISSUE-READY** (half-registration repair [LINKED needs ≥1 entity + deviceId reuse, F1-clean] + always-print rehydration INFO [found DEBUG-silent at devices=2] + BENCH-VERIFY flips + permitJoinDeadline + fence-reads-installed-policy + apply-caller pin; zero mints/surface) · **v26 BANKED** · bench record + runbook updated (token-rotation + sleep-after-pkill lessons) · the `6fd6ddc` reused-message staleness reconciled.)**
> **Next:** Nick — **the two 5b closing legs (⏺ paste)** → **bench commit (exactly 2 M; msg `_scratch/2026-07-08_bench_iteration-5b_commit-msg.txt`)** → **hivemind beat-5 (exactly 4: 2 M + 2 new; msg `_scratch/2026-07-08_hivemind_v25-beat-5_commit-msg.txt`)** → **THE WORD on M9.5-DURb** (paste line: pm-handoff v25 beat-5) → standing. Hub — DURb intake → audit → gates → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero on the 5b ULIDs + RESUME leg → P5 §51 → P6 + PJ key OUT) → **the 72 h soak ON FINAL CODE** → seed-custody (SD-5 HARD GATE) + explainability in the soak window → **LAUNCH v26 on the DURb landing or Nick's word**. Detail: pm-handoff v25 beat-5 (pointer, not copy).**

> **2026-07-08 (v25 hub, beat 3 — **M9.5-DUR DELIVERED + TWO-LAYER AUDIT ACCEPT (zero code defects at layer-2)** — the registries are PROJECTIONS OF THE LOG in code: adopt() publishes full-fidelity `device_registered`/`entity_registered` (durable-before-apply) → the RETAINED `device_adopted`; ALL mutation via the single apply path (`RegistryProjection`, the new `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` rule with PERMANENT teeth); boot replays from 0 (checkpoint-reset + ONE new await); `device_removed` tombstone proven by test; the mirror family carries the full capability forest incl. the DP-a tuning AS INSTALLED; `reinstallOverrides` DELETED; posture routing = F1 re-emit; DP-6 adapter rehydration ⇒ pin-2 reachable; `ProcessRestartIdentityIT` proves same-ids-across-process-restart; mints 73/43/55 all six pin sites lockstep · **Nick's full `check` GREEN 156 PRE-COMMIT (deferred gate DISCHARGED)** · suite 1,829 targeted forced-fresh · the Coder ran a 3-lens adversarial fleet in-session (3 findings fixed in-session) · hub layer-2 seam reads ALL CONFIRM · **adjudications: R1 module-info one-word promotion ACCEPT (hub authoring miss OWNED — the addition-#9 class recurred) · R2 durable half-registration gap CONFIRMED real (millisecond window; NOT a 5b blocker; the one open durability asterisk) → M9.5-DURb queued [repair + javadoc BENCH-VERIFY flips + SD-3 fence + permitJoinDeadline + apply-caller pin], ROUTING = NICK'S RULING (REC (a) before-soak — NQ-6 is its test bed) · R3 ACCEPT (apply-caller census 6 sites/3 classes) · INFOs ×4 ACCEPT (the DP-6 `availability_changed` ×2 at restart is EXPECTED at 5b)**.)**
> **Next:** Nick — **core commit (add -A stages exactly 61: 39 M + 22 new; msg `_scratch/2026-07-08_core_m95-dur_commit-msg.txt`) → push → CI glance (the gate of record; bench ONLY after green)** → **hivemind beat-3 (exactly 5 M; msg `_scratch/2026-07-08_hivemind_v25-beat-3_commit-msg.txt`)** → **THE DURb ROUTING ((a) before-soak [REC] / (b) post-soak / (c) acceptance-window)** → **ITERATION 5b per the paste-block** (wipe once, custody preserved → re-adopt ×2 + KEYb piggyback → RESTART → `registry.projection_live: devices=2 entities=2` + SAME entity ULIDs → Hue power-cycle → `zigbee.device_relinked`, NO `device_proposed` — pin-2 proven → view = exactly 2; ⏺ RECORD + paste either way) → standing (frontend.yml `199324e` · security · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2). Hub — 5b intake → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero [hub authors the config — durable IDs] + RESUME leg → P5 §51 → P6 + PJ key OUT) → **the 72 h soak** (PJ key OUT first; NQ-6 = the DUR proof; W2-VAL at entry) → soak-window WUs (seed-custody [SD-5 HARD GATE] + DURb + explainability return) → v26 before depth. Detail: pm-handoff v25 beat-3 (pointer, not copy).**

> **2026-07-08 (v25 hub, beat 2 — beat-1 LANDED (docs `a53f474` 5-exact · hivemind `1d95288` 4-exact, both pushed — every predicted count matched) · **CI `e15858d` RE-CONFIRMED GREEN (Nick, GitHub) — the KEYb gate of record stays CLOSED; §4b CLEAR** · **M9.5-DUR AUTHORED ISSUE-READY** (`context/instructions/2026-07-08_M9.5-DUR_registries-as-projections_coding-instruction.md`, baseline `e15858d`) against the RATIFIED AMD-99 + carry-list C1–C4, hub-direct grounding survey: **C1 = exactly 4 production mutators** (Slice `:238`/`:272` adopt-creates · `:426` `reinstallOverrides` [pin-2 re-install — REMOVED, redundant post-DUR + REG-INV-1-banned drift path] · Adapter `:704` `applyPostureRouting` [→ the F1 `entity_registered` re-emit]); zero remove/enable/disable callers · **C3 hook EXISTS** (`awaitProjectionLive` `:1184` gates Step 3.4; ONE sibling await = the sanctioned addition) · write-ahead verified (publisher durable-before-return) · checkpoint-reset-to-0 resolves in-memory-from-zero · **C2 forest to the leaf** (Device 14 / Entity 12 / CapabilityInstance 7 / AttributeSchema 11 / CommandDefinition 6 / ParameterSchema 7 / ExpectedOutcome 3 / Expectation ×4 / ConfirmationPolicy 4; AttributeType+AttributeValue stay typed) · ZERO module-info changes · DP-6 adapter rehydration via existing `relink` ⇒ pin-2 reachable · STOP gates G-DUR1–7 · DP-1–8 settled · P2 survey incl. the behavioral publish-count sweep · `ProcessRestartIdentityIT` (lifecycle tree, the R1 precedent) + C4 tombstone leg · the anti-vacuous `registry.projection_live` INFO = 5b's boot glance-point · done-when = ITERATION 5b, unit-green ≠ done.)**
> **Next:** Nick — **hivemind beat-2 commit (stages exactly 3: 2 M + 1 new; msg `_scratch/2026-07-08_hivemind_v25-beat-2_commit-msg.txt`)** → push → **THE WORD on M9.5-DUR** (dispatch paste: pm-handoff v25 beat-2) → standing (frontend.yml `199324e` confirm-once · security · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2). Hub — DUR intake → two-layer audit → gates → **ITERATION 5b** (wipe once custody-preserved → re-adopt ×2 → RESTART → SAME ids + `registry.projection_live` → pin-2 re-link → view = live entities → KEYb piggyback) → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero + RESUME leg → P5 §51 → P6 + PJ key OUT) → the 72 h soak (NQ-6 = the DUR proof) → seed-custody → explainability → v26 before depth. Detail: pm-handoff v25 beat-2 (pointer, not copy).**

> **2026-07-08 (v25 hub, beat 1 — **THE AMD IS RATIFIED: AMD-99 (Registries-Projection)** — Nick: *"ratify as recommended — (A), and R-B through R-E as written"* (his full assessment PRESERVED VERBATIM: pm-handoff v25 beat-1 — the DP-18-C-class record) · **mechanics EXECUTED**: file renamed `design/amendments/AMD-99_Registries-Projection.md` · **REG-INV-1 MINTED** (invariants **174→175**; new `REG` category **52→53**; §0.3 + §17 + §18 + the new §53 + the regeneration line in ONE change; **watermark AMD-98→AMD-99**) · pointer notes folded (Doc 01 masthead+§4.3 inline · Doc 02 masthead · Doc 12 masthead+§3.5 Step-3.1 inline) · **three text folds F1–F3 applied pre-ratification** (F1 registration updates = idempotent re-emit, NO third event type — locks R-E's +2 closed; F2 restart-orphan class vs removal class precision in §6/§8; F3 the single-apply-path refactor covers ALL registry mutators) · the DUR-WU **carry-list C1–C4 recorded** (all-mutators enumeration incl. pin-2 relink `updateEntity` · capability-mirror = the G2 watch + the MODULE_CONTEXT mirrors-track-schema gotcha · the intra-Phase-3 ordering proof · the `device_removed` tombstone test) · launch FULL-PASS all 11, §11 clean (every porcelain flag hashed ≡ HEAD — all phantom; zero ghost writes) · **G1 DISCHARGED**.)**
> **Next:** Nick — **docs commit (`git add -A` stages exactly 5: 4 M + 1 R; msg `_scratch/2026-07-08_docs_amd-99-ratification_commit-msg.txt`)** → push → **hivemind beat-1 commit (`git add -A` stages exactly 4: 3 M + 1 R; msg `_scratch/2026-07-08_hivemind_v25-beat-1_commit-msg.txt`)** → push → standing (frontend.yml `199324e` confirm-once · security · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2). Hub — **author the DUR coding WU against the RATIFIED AMD-99 + carry-list (grounding survey first) → DISPATCH ON NICK'S WORD** → two-layer audit → gates → **iteration 5b** (ONE events-DB wipe, custody preserved → re-adopt ×2 → RESTART → SAME ids resolve → pin-2 re-link → view = exactly the live entities → the KEYb piggyback check) → **THE ACCEPTANCE RUN** (P3.11–12 → P4 hero + RESUME leg → P5 §51 → P6 + PJ key OUT) → the 72 h soak (NQ-6 = the DUR proof) → seed-custody → explainability → v26 before depth. Detail: pm-handoff v25 beat-1 (pointer, not copy).**

> **2026-07-08 (v24 hub, beat 6 — THE CLOSE: **v25 BANKED** (rotation before depth, Nick's directive) · **THE REGISTRIES-PROJECTION AMD AUTHORED — RATIFICATION PENDING** (`homesynapse-core-docs/design/amendments/2026-07-08_Registries-Projection_Amendment_PROPOSED.md`; the §0 ruling box: **R-A** (A) full-fidelity payloads [Nick's log-alone criterion; (B) rejected — impure replay] · **R-B** REG-INV-1 + the `REGISTRY_MUTATION_ONLY_VIA_PROJECTION` ArchUnit rule shipping IN the WU · **R-C** the boot rebuild = a registry-projection subscriber on existing replay machinery, Phase 3, one flagged await max · **R-D** `device_removed` tombstone + the one-time bench wipe (not a migration) · **R-E** mints 71→73/41→43/53→55 + register 174→175/52→53, count-pins lockstep) · **CI GREEN on `e15858d` — the KEYb gate CLOSED, all core gates green through HEAD** · the v24 arc closed: 6 beats · 2 WUs CI-green · the join arc CLOSED on silicon · the instrument honest · DUR ruled + governed.)**
> **Next:** Nick — **docs commit (exactly 1 new; msg `_scratch/2026-07-08_docs_dur-amd-proposal_commit-msg.txt`)** → **hivemind beat-6 (exactly 3: 2 M + 1 new; msg `_scratch/2026-07-08_hivemind_v24-beat-6_commit-msg.txt`)** → **READ + RULE THE AMD (R-A..R-E — "ratify as recommended" suffices; edits fold verbatim)** → **LAUNCH v25** (`context/handoff/2026-07-08_PM-mission-control_v25_orchestrator_session_prompt.md` — it intakes the ratification → the DUR WU → iteration 5b → THE ACCEPTANCE RUN → the soak) → standing (frontend.yml `199324e` confirm · security · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2). Detail: pm-handoff v24 beat-6 (pointer, not copy).**

> **2026-07-08 (v24 hub, beat 5 — **M9.4-KEYb DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects)** — `progress()` = exactly the ruled {0x06,0x07,0x0C}; the DEBUG middle arm `zigbee.key_establishment_progress` exact; `established()` + both frozen tokens BYTE-UNTOUCHED; THE PIN ×4; the DEBUG-capture vacuous-VERIFY trap handled honest (logger level save/set/restore + PRESENCE-based asserts; the staged red failed on presence — the right reason); suite 417+14 zero failures · **Nick's `./gradlew check` GREEN 155 pre-commit (deferred gate DISCHARGED)** · **CI GREEN on `8800424` — the M9.4-KEY gate of record CLOSED** · **the §51 honesty-grep precondition is MET: a healthy TCLK exchange logs exactly one `key_established`, zero `key_establishment_failed`** · deletables CLOSED (rm ran pre-beat-1; ledger corrected) · 4 more mount-phantom hits, host ruled.)**
> **Next:** Nick — **core commit (exactly 4; msg `_scratch/2026-07-08_core_m94-keyb_commit-msg.txt`) → push → CI glance** → **hivemind beat-5 (exactly 4 M; msg `_scratch/2026-07-08_hivemind_v24-beat-5_commit-msg.txt`)** → frontend.yml `199324e` confirm-once (the one open glance). No bench action — the next physical act is iteration 5b, post-DUR. Hub — **THE DUR DESIGN/AMD ARTIFACT (G1)**: record shapes + the A/B payload fork + the registries-are-projections invariant + boot-rebuild seam + orphan tombstoning + mint arithmetic (71→73/41→43/53→55), for Nick's ratification per the confirmation[] model → the DUR coding WU → iteration 5b (same ids across restart; pin-2 proven; +KEYb piggyback: one `key_established`, zero failures) → THE ACCEPTANCE RUN (P4 hero → P5 §51 → P6 + PJ key OUT) → the 72 h soak → seed-custody → explainability → v25 before depth. Detail: pm-handoff v24 beat-5 (pointer, not copy).**

> **2026-07-08 (v24 hub, beat 4 — **THE TWO RULINGS BEAT: DUR RULED (i) — EVENT-SOURCE THE REGISTRIES** (Nick's rationale verbatim in pm-handoff beat-4: the log's single-source-of-truth hole · the one-way-door/`confirmation[]` precedent · the schedule pays for correctness · root-fixes-four-things; **G1: govern like `confirmation[]`** — record shapes + the registries-are-projections invariant ratified at the AMD layer BEFORE the coding WU; **G2: bounded** — ~2 event types, flag if more) · **§B GO → M9.4-KEYb AUTHORED ISSUE-READY** (`context/instructions/2026-07-08_M9.4-KEYb_key-establishment-progress-reclassification_coding-instruction.md`: progress {0x06,0x07,0x0C} → DEBUG middle arm; `established()` + both frozen tokens byte-untouched; THE PIN held; 4 files; lands before any §51 leg) · **completing survey: BOUNDED CONFIRMED** (2 event types hold; `device_removed` exists with zero emitters — tombstoning free; the ONE fork for the AMD, JPMS-forced: (A) flattened-full-record payloads vs (B) identity-binding payloads + deterministic capability re-derivation from the persisted zigbee cache; boot seam = between Phase 3.1 and Phase 6; old `device_adopted` rows untouched) · beat-3 landed: bench `aa45edc` · hivemind `2408995`.)**
> **Next:** Nick — **hivemind beat-4 commit (exactly 4: 3 M + 1 new; msg `_scratch/2026-07-08_hivemind_v24-beat-4_commit-msg.txt`)** → push → **DISPATCH M9.4-KEYb (paste line: pm-handoff v24 beat-4)** → CI glance `8800424` → standing (frontend.yml `199324e` · security · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2). Hub — **the DUR design/AMD artifact for Nick's ratification (G1)** → the DUR coding WU → **iteration 5b** (same ids across restart; pin-2 proven; view = live entities) → KEYb audit on delivery (parallel) → **THE ACCEPTANCE RUN** (P4 hero → P5 §51 → P6 + PJ key OUT) → the 72 h soak (NQ-6 = the DUR proof) → seed-custody → explainability → v25 before depth. Detail: pm-handoff v24 beat-4 (pointer, not copy).**

> **2026-07-08 (v24 hub, beat 3 — **ITERATION 5a: THE JOIN ARC CLOSES FOR REAL** (core `8800424`) — the SNZB joins → `key_established: TC_REQUESTER_VERIFY_KEY_SUCCESS` (the TCLK update completes; vs iteration 4's `TC_REJECTED_APP_KEY_REQUEST` → leave) → adopts (`source=config`) → `reporting_configured 2/3` → **STAYS ≥5 min (NO device_left)**; the one-constant `POLICY_TC_KEY_REQUEST 0x09→0x05` fix silicon-confirmed · **the moat's full join→key→adopt→report→STAY pipeline is silicon-proven end-to-end within a lifetime** · **§B finding**: `TC_RESPONDED_TO_KEY_REQUEST` (0x06) is benign progress mis-logged as `key_establishment_failed` — M9.4-KEYb queued (reclassify {0x06,0x07,0x0C} before the acceptance run) · **DUR ruling SHARPENED**: the registries are imperative in-memory (NOT event-sourced — `DeviceAdoptedEvent` carries only `entityId`, no `entity_registered` event), so (i) event-replay rehydration = making them projections via minted registration events (a governance WU, the root-correct answer; hub REC) vs (ii) snapshot (faster, parallel-truth); Nick's fork.)**
> **Next:** Nick — **bench commit (1 M; msg `_scratch/2026-07-08_bench_iteration-5a_commit-msg.txt`)** → **hivemind beat-3 (2 M; msg `_scratch/2026-07-08_hivemind_v24-beat-3_commit-msg.txt`)** → **THE DUR RULING ((i) hub REC / (ii) / (iii))** → **§B "go" (M9.4-KEYb, parallel)** → CI glance `8800424`. Hub — DUR WU on the ruling (survey-first) → iteration 5b (identities survive restart; pin-2 proven) → THE ACCEPTANCE RUN (P4 hero → P5 §51 → P6 + PJ key OUT; §B lands before P5) → the 72 h soak → seed-custody → explainability → v25. Detail: pm-handoff v24 beat-3 (pointer, not copy).**

> **2026-07-07 (v24 hub, beat 2 — **M9.4-KEY DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects)** — §1 the one-constant fix CONFIRMED at source (`POLICY_TC_KEY_REQUEST = 0x05` `:176`; the enablement writes it with 0x51 = the bellows `EZSP_POLICIES_SHARED` reference); §2 the 17-name EmberKeyStatus fold bellows-verbatim (`0x11 = TC_REJECTED_APP_KEY_REQUEST`); `established()` byte-untouched {0x01,0x03,0x34,0x65}; `statusName()` hex fallback + the §B log-only PIN held; T-K1 (0x11 by VALUE) / T-K2 (WARN verbatim) / T-K4 + 413/14 GREEN · **Nick's `./gradlew check` GREEN 155 pre-commit (deferred gate DISCHARGED)** · zero mints/schema/module-info/public-surface, the TCJ test byte-untouched (symbolic binding — T-K3) · mount truncated-tail + read-lag phantoms paid (host ruled; coder-lessons landed host-verified) · **ITERATION 5a IS THE LIVE GATE: the SNZB must JOIN AND STAY (≥5 min hands-off)**.)**
> **Next:** Nick — **core commit (exactly 3; msg `_scratch/2026-07-07_core_m94-key_commit-msg.txt`) → push → CI glance (bench ONLY after green)** → **hivemind beat-2 (exactly 4 M; msg `_scratch/2026-07-07_hivemind_v24-beat-2_commit-msg.txt`)** → **BENCH ITERATION 5a (NO wipe, config unchanged; SNZB one ~5 s hold then HANDS OFF ≥5 min; done-when `key_established` + zero `key_establishment_failed` + zero `device_left` = the join arc closes for real)** → THE DUR RULING when ready (gates 5b, not 5a) → frontend.yml `199324e` confirm-once. Hub — 5a intake → the DUR WU on the ruling → 5b (identities survive restart; pin-2 proven) → THE ACCEPTANCE RUN (P4 hero → P5 §51 → P6 + PJ key OUT) → the 72 h soak → seed-custody (SD-5 gate rides) → explainability return → v25 before depth. Detail: pm-handoff v24 beat-2 (pointer, not copy).**

> **2026-07-07 (v24 hub, beat 1 — LAUNCH FULL-PASS all 11 (**Check 9 PASS — mirrors byte-identical, Nick's sync already run**; §11 clean; beat-7 `1240ec6` verified landed 9-exact; v23 archived [R, this commit]; deletables corrected ×2→×1 — the `skills-initiative` husk is already gone) · **THE 0x11 DECODE VERIFIED TRIPLE-SOURCE** (bellows `named.py` verbatim + classic `ezsp-enum.h` + **GSDK 4.4.5 = EmberZNet 7.4.5, the frozen stack**): `0x11 = TC_REJECTED_APP_KEY_REQUEST`, and the root cause is **the policy-ID mis-map** — our `POLICY_TC_KEY_REQUEST = 0x09` is `TC_REJOINS_USING_WELL_KNOWN_KEY_POLICY` on v13; the REAL `EZSP_TC_KEY_REQUEST_POLICY = 0x05` was never written → firmware-default deny → 3 rejected TCLK-update requests → BDB leave +18.8 s (the decision value 0x51 was already the bellows-correct ALLOW — one-constant fix 0x09→0x05) · **M9.4-KEY AUTHORED ISSUE-READY** (`context/instructions/2026-07-07_M9.4-KEY_tc-key-request-policy-correction_coding-instruction.md`: §1 one-constant + verified javadocs; §2 the §B vocabulary fold — self-decoding logs, success set untouched, log tokens frozen; 3 files; G-KEY1–5; gate = ITERATION 5a: the SNZB joins AND STAYS) · **DUR options staged** (survey at source: `:347` fresh registry BUT the canonical 8-arg ctor accepts one [R4]; `adopt()` publishes replayable `device_adopted`; RestartHonestyIT restarts the INTEGRATION only — options: (i) event-replay rehydration at boot [REC] / (ii) snapshot / (iii) cache-side-only [insufficient alone]; sits before P4, gates NQ-6).)**
> **Next:** Nick — **hivemind beat-1 commit (add -A stages exactly 4: 2 M + 1 new + 1 R; msg `_scratch/2026-07-07_hivemind_v24-beat-1_commit-msg.txt`)** → push → **THE WORD on M9.4-KEY (dispatch paste line: pm-handoff v24 beat-1)** → **THE DUR RULING ((i)/(ii)/(iii) + where it sits; REC (i) before P4)** → frontend.yml `199324e` confirm-once → standing (security glance · deletable ×1 · telemetry · website.yml · LICENSE · Node-20 · naming · Wave-2). Hub — KEY audit on delivery → **iteration 5a (the join arc closes for real)** → the DUR WU on the ruling → **iteration 5b (identities survive restart; pin-2 proven)** → **THE ACCEPTANCE RUN** (P4 hero → P5 §51 → P6 + PJ key OUT) → the 72 h soak → seed-custody (SD-5 gate rides) → explainability return → v25 before depth. Detail: pm-handoff v24 beat-1 (pointer, not copy).**

> **2026-07-07 (v23 hub, beat 7 — THE CLOSE: step-11 formally NON-VACUOUS (positive ×2, sweep clean; the SNZB degraded-fact-no-WARN NOTE banked for R3) · **Dependabot CLOSED** (dashboard devDeps; `npm run verify` green; core **`199324e`**) · spine ROTATED (v21–v17 / v21–v18 → the 2026-07-07 archives; frontmatters compacted) · cross-agent RPT note archived SERVED · **THE SKILLS FOLD**: `context/process/bench-troubleshooting-playbook.md` (the M9.4 arc's troubleshooting doctrine — seed of the future dedicated skill) + PM/coder masthead pointers (Check 9 STALE pending sync) · **v24 AUTHORED**.)**
> **Next:** Nick — **hivemind beat-7 commit (exactly 9: 5 M + 4 new; msg `_scratch/2026-07-07_hivemind_v23-beat-7_commit-msg.txt`)** → push → **MIRROR SYNC ×2** → **LAUNCH v24**. Hub (v24) — the 0x11 decode → **M9.4-KEY** → **the DUR ruling + WU** → iteration 5 (SNZB STAYS + pin-2) → **THE ACCEPTANCE RUN** (P4→P6 + PJ key OUT) → soak → seed-custody (SD-5 gate rides) → explainability return. Detail: pm-handoff v23 beat-7 (pointer, not copy).**

> **2026-07-07 (v23 hub, beat 6 — **ITERATION 4 (core `7c0561d`, CI GREEN): THE REPORTING BINDING IS SILICON-PROVEN** — drive ×2 (`reporting_configured`: Hue 3/3 VERIFIED · SNZB 2/3); **the Hue entity row MATERIALIZED on its first configured report** (the iteration-3 state-invisibility gap CLOSED) · **§B ADJUDICATES OBS-2: DEVICE-INITIATED, MEASURED** — `key_establishment_failed status=0x11` ×3 (~5 s cadence) → `device_left` +18.8 s hands-off; the TCLK-exchange-failure class CONFIRMED (0x11 unmapped in our vocabulary — decode-first; fix class = the TCJ policy family; SD-5 fallback last resort) · **NEW ARCHITECTURE GAP ESCALATED: identity durability across restart** — `HomeSynapseCore:347` constructs `InMemoryDeviceRegistry` fresh every boot, zero rehydration ⇒ re-adopt + NEW ids per restart, orphan view rows (3 rows / 2 devices), pin-2 unreachable across restarts, entity-ID-bound automations break ⇒ **gates P4 + the soak's NQ-6** (options: event-replay rehydration [REC] / registry snapshot / cache-side re-link; Nick rules) · **TWO WUs to the acceptance run: M9.4-KEY (TCLK, decode-first micro) + the durability WU.**)**
> **Next:** Nick — the corrected greps (`$LOG` died with the old ssh session) → hivemind beat-6 commit (exactly 3 M) → bench commit (exactly 1 M) → **the two rulings: (a) M9.4-KEY constant on the hub's verified decode, (b) durability shape/routing** → **Dependabot (1 critical — re-nagged on tonight's push, day 10)**. Hub — 0x11 decode + policy verification (bellows reference) → M9.4-KEY authored → the durability escalation formalized → iteration 5 (SNZB joins AND STAYS; pin-2 post-DUR) → THE ACCEPTANCE RUN → soak → **v24 AUTHORS NEXT BEAT (rotation: 6 beats deep)**. Detail: pm-handoff v23 beat-6 (pointer, not copy).**

> **2026-07-07 (v23 hub, beat 5 — **M9.4-RPT DELIVERED + TWO-LAYER AUDIT ACCEPT (zero defects; 10/10)** — §A+§B shipped (9 paths: 6 M + 3 new): `EzspReportingOps` (4 frozen methods, typed-results, BENCH-VERIFY block) · both drive arms at the ADP seam (fresh `configureDevice` / LINKED `onRejoin` — re-links drive unconditionally; the dedicated `reporting_drive_failed` backstop never gates adoption) · the anti-vacuous `zigbee.reporting_configured` INFO unconditional · §3 routing (strict VERIFIED selector `note==null`; NONE⇒DISABLED-with-reason; BEST_EFFORT-keep-tracking = the ratified not-a-mode vocabulary, adjudicated) · §B 0x009B LOG-ONLY (pin test-enforced) · frozen trio blob-identical ×3 · hoist semantics-identical · zero mints/schema/public-surface · gates GREEN 62/155 (deferred gate DISCHARGED; residual = core commit [exactly 9] + push + CI) · **rulings: R1 runbook step-11 AMENDED non-vacuous · R2 = named polish row (SD-3 fence → installed policy; next zigbee WU; Nick ack requested) · R3 = re-rate row on iteration-4 data** · **ITERATION 4 = THE LIVE GATE**.)**
> **Next:** Nick — **core commit (exactly 9; msg `_scratch/2026-07-07_core_m94-rpt_commit-msg.txt`) → push → CI glance (bench ONLY after green)** → **hivemind beat-5 (exactly 6 M; msg `_scratch/2026-07-07_hivemind_v23-beat-5_commit-msg.txt`)** → **bench (exactly 1 M; msg `_scratch/2026-07-07_bench_runbook-step-11_commit-msg.txt`)** → **ITERATION 4 (NO wipe; config unchanged): Hue ONE wall power-cycle · SNZB ONE 5 s hold then HANDS OFF 60 s · the AMENDED step-11 greps · done-when `reporting_configured` ×2 + TWO entity rows + non-vacuous step-11 + pin-2 re-link + the discriminator outcome (§B adjudicates a leave)** → **Dependabot (1 critical, day 10)**. Hub — iteration-4 intake → OBS-2 ruling → THE ACCEPTANCE RUN (P4 hero → P5 §51 → P6 + PJ key OUT) → soak → seed-custody (SD-5 gate rides) → explainability return → **v24 before depth**. Detail: pm-handoff v23 beat-5 (pointer, not copy).**

> **2026-07-07 (v23 hub, beat 4 — **M9.4-RPT AUTHORED ISSUE-READY** — `context/instructions/2026-07-07_M9.4-RPT_reporting-binding-and-posture-routing_coding-instruction.md` (§A: `EzspReportingOps` over the frozen seam [4 typed methods verbatim; write path `:1054/:1079`; BENCH-VERIFY constants; G-RPT2 pins the ZDO-expressibility check with a sanctioned zero-surface fallback] + the two drive arms at the ADP seam [fresh `configureDevice` + re-link `onRejoin` — iteration 4's already-adopted devices configure via re-link] + the §3 posture→confirmability CONTRACT [VERIFIED-only stays report-confirmable; non-VERIFIED downgrades honestly; never-false-CONFIRMED at install time] + the anti-vacuous `zigbee.reporting_configured` INFO; **§B severable**: log-only 0x009B key-establishment observability [the OBS-2 instrument] — Nick's veto standing; 7 scenarios; ZERO schema/mints/public-surface; the frozen M9.3 trio untouched; STOP gates G-RPT1–6) · beats 2–3 landed `59bbe7a` · bench record landed `624a935` · **DISPATCH ON NICK'S WORD**.)**
> **Next:** Nick — **hivemind beat-4 commit (exactly 4: 3 M + 1 new; msg `_scratch/2026-07-07_hivemind_v23-beat-4_commit-msg.txt`)** → push → **DISPATCH M9.4-RPT** (paste line: pm-handoff v23 beat-4; §B veto = strike or say the word) → on the delivery: report to the hub → audit → gates → commit/push/CI → **BENCH ITERATION 4 (NO wipe; config unchanged): Hue = ONE wall power-cycle; SNZB = ONE 5 s hold then HANDS OFF 60 s (the discriminator); done-when `reporting_configured` ×2 + the Hue row materializes + step-11 NON-VACUOUS + pin-2 re-link + the discriminator outcome** → **Dependabot (1 critical, day 10)**. Hub — RPT audit → iteration-4 intake → THE ACCEPTANCE RUN (P3.11 → P4 hero → P5 §51 → P6 + PJ key OUT) → soak → seed-custody (SD-5 gate rides) → explainability return. Detail: pm-handoff v23 beat-4 (pointer, not copy).**

> **2026-07-06/07 (v23 hub, beat 3, MERGED — a parallel hub instance drafted this beat + edited the bench msg; §11 un-audited-return protocol applied: OBS-1 claims independently source-verified ✓, OBS-2 closure corrected against Nick's own timeline, arithmetic reconciled. **Single-spine-writer: retire one of the two live hub sessions.** Instrument returns: **OBS-2 closure CONTESTED — Nick's testimony = ONE press, the pairing hold, ENDED pre-join; the leave came +18.3 s post-join hands-off ⇒ CLOSED-BENIGN doesn't rest on tonight's record; the iteration-4 hands-off re-pair is the FREE discriminator (stays ⇒ benign + pin-2 leg banked; leaves ~15–30 s ⇒ device-initiated → §B reads it out); §B = severable log-only 0x009B keyEstablishment instrument, Nick rules** · **OBS-1 ROOT-CAUSED — the FOURTH mechanism-without-driver (PJ → NCFG → ADP → RPT):** `ReportingConfigurator` has NO production caller and **`ReportingOps` has NO production implementation** (the M9.3 note deferred the EZSP binding to M9.4; M9.4b shipped the write path, not the binding; `ReportingPostureFact` consumers: zero) — the state view consumes no adoption events, the SNZB row came from the one wired arm (device-initiated IAS enroll), the Hue is adopted but state-invisible until reporting exists · **step-11 REPORTING-CLEAN = VACUOUS pre-RPT; the P4/P5 CONFIRMED legs are structurally unreachable pre-RPT** · CI `43c2880` GREEN (ADP gate of record CLEAR) · boot-WARN pre-existing (all 10 logs) → polish row · **pm-lessons WRITTEN +1** (gate-vs-mechanism + the vacuous-VERIFY corollary) · entities-view semantics = deferred ruling row (rec: UI milestone) · **M9.4-RPT = the next WU, acceptance-run critical path — authoring on Nick's word (§A binding+drive+posture+INFO; §B severable)**.)**
> **Next:** Nick — **the M9.4-RPT word ("go, with/without §B" — or the OBS-2 fact that closes it benign)** → hivemind commit (beats-1-3 msg [**exactly 7: 6 M + 1 R**] or beats-2-3 delta [**exactly 3 M**: pm-handoff · snapshot · pm-lessons] per porcelain) → bench commit (exactly 1 M; **msg REC = the v2** — matches the record + testimony) → **Dependabot (1 critical, day 9+)**. Hub — author RPT (survey at `43c2880`; STOP gates re-grep both absences) → dispatch → audit → **bench iteration 4 done-when: Hue row materializes on first configured report + non-vacuous step-11 + SNZB pin-2 re-link + the OBS-2 discriminator outcome recorded** → THEN P3.11→P6 + PJ key OUT → soak. Detail: pm-handoff v23 beat-3 (pointer, not copy).**

> **2026-07-06/07 (v23 hub, beat 2 — **ITERATION 3 (core `43c2880`): ADOPTION PROVEN ON SILICON — the accept-list fired ×2 (`proposal_accepted source=config` → `device_adopted`, Hue + SNZB, `entities=1` each); TCJ+NCFG+ADP all silicon-proven; the G-NCFG6 resume-reconfigure arm proven live** · **done-when SPLIT — two observations HOLD the acceptance-run entry:** OBS-1 `/api/v1/entities` 1-of-2 at +47 s (ULID-decoded: the row is the SNZB's, minted at adoption-time — the Hue's entity ABSENT from the view; projection-drop vs benign-late hypotheses; instruments out) · OBS-2 SNZB `device_left` 18.3 s post-join, SECOND occurrence (attribution = Nick's button answer; if untouched: device-initiated, leading candidate the Z3.0 TCLK-update leg [~15 s device timer fits; 0x51-policy/hashed-TCLK candidates; ZHA-stays = the A/B]; adoption survives — re-pair re-links per pin-2; candidate instrument = log-only 0x009B keyEstablishment micro-WU, evidence-first; SD-5 fallback ruled-WITH-Nick, last resort).)**
> **Next:** Nick — **the button answer + the 6-command instrument batch (app LIVE — no restart)** → CI glance `43c2880` → hivemind commit (beats-1-2 msg [exactly 6] OR beat-2 delta [exactly 2 M] — whichever matches what's landed) → bench commit (exactly 1 M) → **Dependabot (1 critical, day 9+)**. Hub — instrument intake → OBS rulings WITH Nick → only on BOTH resolved: the acceptance run opens (P3.11 sweep → P4 hero → P5 §51 → P6 + PJ key OUT) → soak. Detail: pm-handoff v23 beat-2 (pointer, not copy).**

> **2026-07-06/07 (v23 hub, beat 1 — LAUNCH FULL-PASS (all 11; §11 restart-resume clean; v22 archived; deletables re-derived ×2) · **M9.4-ADP DELIVERED + TWO-LAYER AUDIT ACCEPT (zero code defects; 19/19 checks)** — the sanctioned slice widening realized **ZERO-DIFF** (`DiscoveryOutcome{LINKED,PROPOSED}` pre-existed since M9.3 `c856eab`; the adapter consumes the previously-discarded `:565` return; slice blob-identical `ca18490f`) · **pin-1 letter-verified** (listed-PARTIAL ⇒ verbatim WARN `proposal_incomplete_not_adopted` + sits; T7 pins the full arc incl. the COMPLETE re-proposal adopting) · **re-link guard outcome-driven** (zero catch in new code — the banned exception-flow does not exist) · conservative default (absent/empty/non-list ⇒ nothing adopts) · THE one schema diff (`adopt_devices` only) · zero mints (71/41/53 derivation located: EventTypesTest/EventTypeRegistryTest/AllEventClasses — all rode Nick's green gates) · exactly ONE production `.adopt(` caller (the gate `:629`) · 7/7 scenarios at mandated strength, verbatim log bindings, Clock-clean, default-gated · INFOs ×5 all ACCEPT · **gates GREEN PRE-COMMIT (62 targeted + full `check` 155 — deferred gate DISCHARGED)** · mount-phantom 4th hit (VM diffstat/grep phantom'd the fresh adapter — host tools ruled).)**
> **Next:** Nick — **core commit (exactly 4: 3 M + 1 new; msg `_scratch/2026-07-06_core_m94-adp_commit-msg.txt`)** → push → **CI glance (+ install-smoke covers the open `ccba69d` glance)** → **hivemind beat-1 commit (exactly 6: 5 M + 1 R; msg `_scratch/2026-07-06_hivemind_v23-beat-1_commit-msg.txt`)** → push → **ITERATION 3 (NO wipe — ch20/`0x9b65` resumes): `adopt_devices: ["0x00178801101A09BB", "0xF044D3FFFE9C78D7"]` + keep `permit_join_duration: 254` → Pi pull + installDist → relaunch → Hue power-cycle → SNZB re-pair (5 s hold, in-window) → ⏺ done-when `device_adopted` ×2 + non-empty `/api/v1/entities` = THE JOIN ARC CLOSES** → the acceptance run (P3.11 Configure-Reporting sweep [STOP-and-ping on `reporting_ack_lies`] → P4 hero → P5 §51 → P6 reopen + PJ key OUT) → **Dependabot (1 critical, day 9+)** + security glance + deletables ×2 + Node-20. Hub — iteration-3 intake → acceptance-run ⏺ RECORDs → soak (W2-VAL charter at entry) → seed-custody JIT (SD-5 HARD GATE rides) → explainability return → javadoc-flip micro-pass rides the next core WU. Detail: pm-handoff v23 beat-1 (pointer, not copy).**

> **2026-07-06 (v22 hub, beat 5 — **THE RULINGS BEAT**: CI gate on `ccba69d` **CLEARED** (Nick's rerun GREEN; closed intermittent-DEFINITIVE — NCFG cannot causally reach event-bus replay; the `ReplayTransitionIT` deflake row queued as named moat-debt: progress-based await, fail-on-stall) · **ADOPTION RULED (b) — the config accept-list** (his rationale on record: both Locked docs honored as-shipped; ADDITIVE, not a one-way door — the REST/UI surface rides the UI milestone, never amended under bench pressure; **pins folded pre-dispatch**: adopt only COMPLETED proposals [a listed-PARTIAL WARNs and sits] + re-links stay automatic [first-adoption-only consent]) · **TCJ §A.3 SILICON-CLOSED** (`UNSECURED_JOIN → USE_PRECONFIGURED_KEY → Device_annce` = a real key exchange through the transient well-known key — Nick's upgrade of the record; **TCJ + NCFG both FULLY VALIDATED**) · beat-4 landed (hivemind `0cf1e34` 4-exact · bench `f8a0693` 1-exact) · **M9.4-ADP DISPATCHING** · **v23 AUTHORED** (Nick's rotation directive — banked before depth).)**
> **Next:** Nick — **hivemind beat-5 commit (exactly 6: 5 M + 1 new; msg `_scratch/2026-07-06_hivemind_v22-beat-5_commit-msg.txt`)** → push → **DISPATCH M9.4-ADP** (paste line: pm-handoff v22 beat-5) → on the landing: gates → commit/push/CI → **ITERATION 3 — no wipe; `adopt_devices` = tonight's two IEEEs; Hue power-cycle re-proposes; SNZB re-pairs; done-when `device_adopted` ×2 + non-empty entities = THE JOIN ARC CLOSES** → the acceptance run (P3.11 Configure-Reporting → P4 hero → P5 §51 → P6) → install-smoke glance `ccba69d` → **Dependabot (1 critical, day 9+)** + security glance + deletables ×3 + Node-20. Hub — ADP audit → iteration-3 intake → acceptance-run ⏺ RECORDs → soak (PJ key OUT; W2-VAL) → seed-custody (SD-5 gate rides) → explainability return → **v22 retires → v23 launches on Nick's word**. Detail: pm-handoff v22 beat-5 (pointer, not copy).**

> **2026-07-06 (v22 hub, beat 4 — **BENCH ITERATION 2 (core `ccba69d`): THE DEVICES JOIN ON SILICON** — NCFG read-back CLEAN (`zdo_flags=0x3 stack_profile=2 security_level=5`) → **Hue joins +11 s** (join → announce → `device_proposed … status=COMPLETE`) → **SNZB joins +46 s** (join → `child_join` → announce → proposed COMPLETE → **`ias_zone_enrolled`** — F-7a on silicon); the TCJ 0x0024/0x0023 parses proved on REAL payloads; the association-layer diagnosis CORRECT · **the LAST un-driven mechanism: `adopt(:207)` has ZERO production callers** (javadoc: "callable (tests/M9.4 API)"; the E2Es pre-adopted via the seam — the PJ class a third time) → proposals never adopt → zero entities · **ESCALATION (two LOCKED docs at the fork): Doc 02 §3.12 "the user accepts a proposed device" + Doc 09 :65 "not API operations in Tier 1"** — the Tier-1 acceptance surface is NICK'S RULING: (a) Doc-09 AMD REST endpoints now · **(b) config accept-list `adopt_devices` [REC]** · (c) other — **M9.4-ADP PRE-AUTHORED PENDING-RULING** (re-link guard; conservative default; 6 scenarios; iteration-3 = no-wipe resume + Hue power-cycle re-propose + SNZB re-pair) · class-sweep: adoption was the last driver gap on the runbook's expected chain.)**
> **Next:** Nick — **THE RULING (one line: "(b), go" dispatches)** → **hivemind beat-4 commit (exactly 4: 3 M + 1 new; msg `_scratch/2026-07-06_hivemind_v22-beat-4_commit-msg.txt`)** → **bench commit (exactly 1 M; msg `_scratch/2026-07-06_bench_iteration-2_commit-msg.txt`)** → **RE-RUN the `ccba69d` CI** (RED at beat-4 close — event-bus `ReplayTransitionIT` await-timeout; OR-GATE-M9.4-NCFG registered: flake-leaning [module untouched 65 commits, local green ×2], not waved off; **the ADP dispatch HOLDS until it clears**) → on (b) + the green: **DISPATCH M9.4-ADP** (paste line: pm-handoff v22 beat-4) → **CI glance `2a5fba2`** → **Dependabot (1 critical, day 9+, re-nagged tonight)** + security glance + deletables ×3. Hub — ADP return audit → **iteration 3: `device_adopted` ×2 + non-empty entities = THE JOIN ARC CLOSES** → runbook P3-step-11 onward → javadoc-flip queue (all bench-verify ids now silicon-proven) rides the next core WU → soak → seed-custody (SD-5 gate rides) → v23 before depth. Detail: pm-handoff v22 beat-4 (pointer, not copy).**

> **2026-07-06 (v22 hub, beat 3 — **M9.4-NCFG DELIVERED + AUDITED ACCEPT** · Nick's gates GREEN PRE-COMMIT (62 targeted + full `check` 155 — the deferred gate DISCHARGED; residual = core commit + push + CI glance) · the G-NCFG6 shape CONFIRMED at source (`configureNcp()` `:419` inside the guarded post-negotiation block; `resetSession()` clears negotiation `:583` — reopen re-negotiates AND re-configures; supervisor relaunch constructs a FRESH adapter `:705-708`, so no in-place retry can skip config) · REQUIRED trio fails honest BEFORE any formation frame (`ncp_config_rejected`/`readback_mismatch` — never a half-open window); SUPPORTING tail = `ncp_config_skipped` WARN-and-continue · the §1.2 read-back INFO `zigbee.ncp_configured` asserted VERBATIM in the new test (production-constant bindings, REQUIRED-first `containsExactly`) · FakeNcp built-in config model at the SHARED fixture — every per-class responder untouched, the regression matrix structurally green · **BENCH ITERATION 2 IS THE LIVE GATE — a real device joining closes TCJ+NCFG together.**)**
> **Next:** Nick — **core commit (exactly 5: 4 M + 1 new; msg `_scratch/2026-07-06_core_m94-ncfg_commit-msg.txt`)** → push → CI glance → **hivemind commit (exactly 5 M; msg `_scratch/2026-07-06_hivemind_v22-beat-3_commit-msg.txt`)** → push → **BENCH ITERATION 2** (Pi pull + `installDist` → wipe `data/zigbee` → relaunch → **⏺ RECORD `zigbee.ncp_configured` verbatim** [healthy: `zdo_flags=0x3 stack_profile=2 security_level=5`; `ncp_config_skipped` WARNs expected] → **Hue first**, then SNZB → paste the log either way) → standing (Dependabot 1 critical · security glance · deletables ×3). Hub — iteration-2 intake (join ⇒ runbook P3–6 + the javadoc-flip queue; rejected/mismatch ⇒ one-constant correction; clean-read-back-still-silent ⇒ the association-layer instrument ruled WITH Nick) → soak → seed-custody (SD-5 gate rides). Detail: pm-handoff v22 beat-3 (pointer, not copy).**

> **2026-07-06 (v22 hub, beat 2 — **BENCH ITERATION 1 (core `2a5fba2`): the TCJ enablement is SILICON-ACCEPTED** (`zigbee.tc_joins_enabled` — all three exchanges requireSuccess-passed; §B validated: `channel_pinned: channel=20` → formed `0x3fe6`; resume arm re-proven second boot) **but joins are TOTALLY SILENT** — zero `device_join` (0x0024)/`child_join`/announce across two 254 s windows, both devices factory-reset in-range · **the root gap INDEPENDENTLY HUB-VERIFIED (×9 regreps): the NCP is NEVER CONFIGURED** — `startSession()` is version-nego-only, ZERO `setConfigurationValue` in the module, the NCP resets to firmware defaults every launch; bellows/ZHA writes the config batch first · **M9.4-NCFG RE-ISSUED under the hub** (out-of-band draft treated as an un-audited return: gap VERIFIED, mechanism CORRECTED — zero-0x0024 places failure at/below ASSOCIATION, so `STACK_PROFILE`-beacon-compat leads and ZDO_FLAGS is the announce-leg candidate, not the certain linchpin; +the §1.2 `zigbee.ncp_configured` read-back instrument; +G-NCFG6 resetSession gate; +the re-form bench step) · REQUIRED trio fail-honest (never a half-open window) + SUPPORTING ×7 best-effort.)**
> **Next:** Nick — **hivemind beat-2 commit (exactly 4: 3 M + 1 new; msg `_scratch/2026-07-06_hivemind_v22-beat-2_commit-msg.txt`)** → **bench commit (exactly 1 M; msg `_scratch/2026-07-06_bench_iteration-1_commit-msg.txt`)** → **DISPATCH M9.4-NCFG** (paste line: pm-handoff v22 beat-2) → CI glance `2a5fba2` if pending → on the landing: gates → commit/push/CI → Pi pull → wipe `data/zigbee` → relaunch → **⏺ RECORD `zigbee.ncp_configured` verbatim** → Hue first → paste the log. Hub — NCFG return audit → iteration-2 intake (a join closes TCJ+NCFG; still-silent + clean read-back ⇒ the association-layer instrument ruled WITH Nick) → runbook P3–6 → soak → seed-custody (SD-5 gate rides). Detail: pm-handoff v22 beat-2 (pointer, not copy).**

> **2026-07-06 (v22 hub, beat 1 — **M9.4-TCJ DELIVERED + TWO-LAYER AUDIT ACCEPT (zero code defects)** · Nick's gates GREEN PRE-COMMIT (targeted 62 + **full `check` 155 — the deferred gate DISCHARGED**; residual = core commit + push + CI glance, the gate of record) · the enablement verified at the letter (policy 0x0055 ×2 → wildcard transient 0x0111 w/ the REUSED `TC_LINK_KEY` [ONE literal in src/main, hub-regrepped] → 0x0022; a NAK propagates with the deadline UNSET — never a half-open door; `attemptReopen` never re-enables) · **THE PIN test-enforced** (join handlers log-only — never a device/interview/event; adoption stays Device_annce-gated) · §B channel-pin landed (form-path-only; out-of-range = WARN+ignore-not-clamp; resume untouched) · R1 (+1 `CoordinatorProtocol` method) ACCEPT-as-instructed · R2 (reopen-window staleness) = recorded PJ-class limitation + a queued one-line polish row · **UNIT-GREEN ≠ WU-DONE — the done-when is a real device joining on silicon; the §A bench iteration is OPEN.**)**
> **Next:** Nick — **core commit (exactly 10: 8 M + 2 new; msg `_scratch/2026-07-06_core_m94-tcj_commit-msg.txt`)** → push → **ci.yml + install-smoke glance** → **hivemind commit (exactly 6: 5 M + 1 R; msg `_scratch/2026-07-06_hivemind_v22-beat-1_commit-msg.txt`)** → push → **BENCH §A ITERATION** (rec: pin ch20 — `channel: 20` + wipe `~/hs-bench/data/zigbee` ONCE, nothing adopted yet; then runbook Phase 3, device in-range immediately; veto standing) → deletables (+ the NEW core `index.lock.cleared-by-cowork-2026-07-06`) → **Dependabot (1 critical, day 9+)** + the security glance. Hub — bench-log intake → one-constant corrections via CC on NAK/stall → on a real join: runbook P3–6 + the bench-verify javadoc flips at the next core WU → soak (W2-VAL at entry) → seed-custody WU JIT (SD-5 gate rides). Detail: pm-handoff v22 beat-1 (pointer, not copy).**

> _Rotated 2026-07-07 (v23 hub, beat 7): beat blocks v21-beat-3 → v18-beat-6 → `context/status/archive/PROJECT_SNAPSHOT-beats-rotated-2026-07-07.md`. Kept: v23 + v22 blocks. Full history in git._
_(Beat blocks v13-beat-42 -> v16-beat-65 (23 blocks) rotated 2026-07-04 by the v19 hub -> `archive/PROJECT_SNAPSHOT-beats-42-65-rotated-2026-07-04.md`; prior chain in that file's frontmatter. Full history in git.)_

## Current Work Unit

**Current work unit:** re-derive from the NEWEST masthead beat above + the backlog's M-row currency notes (pointer, not copy).

_The 2026-06-19/21-era work-unit blocks + the pre-beat-era Recent Session Log rows rotated 2026-07-02 → `context/status/archive/PROJECT_SNAPSHOT-work-unit-blocks-and-session-log-rotated-2026-07-02.md`; the M3.7/M4-era blocks rotated 2026-06-12 → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-12.md` (git-object-sourced from `58794d7`)._

## Design Documents

All 14 design documents are Locked (Phase 1 — System Design Documentation is complete).

| # | Document | Status |
|---|----------|--------|
| 01 | Event Model & Event Bus | Locked |
| 02 | Device Model & Capability System | Locked |
| 03 | State Store & State Projection | Locked |
| 04 | Persistence Layer | Locked |
| 05 | Integration Runtime | Locked |
| 06 | Configuration System | Locked |
| 07 | Automation Engine | Locked + AMD-25 integrated (2026-03-18) |
| 08 | Zigbee Adapter | Locked |
| 09 | REST API | Locked |
| 10 | WebSocket API | Locked |
| 11 | Observability & Debugging | Locked |
| 12 | Startup, Lifecycle & Shutdown | Locked |
| 13 | Web UI (Observability MVP) | Locked |
| 14 | Master Architecture Document | Locked |

**Post-foundational design docs (Locked outside the original 14):** **Doc 15** — Cryptographic Architecture (Locked 2026-06-07; AMD-94 folded 2026-06-19). **Doc 16** — Superior Automation Layer (**Locked 2026-06-20**; INV-SA-01..04 registered as §49 of the invariants register; clears the M7.2b entry-gate). **Doc 17** — AIoT + Cloud Readiness (**Locked 2026-06-26**; AIOT-INV-1 registered as §50). **Doc 18** — Extension & Plugin Architecture (**Locked 2026-07-03**; EXT-INV-1/2 registered as §52). **All 18 design docs are Locked; the register's regeneration line is the invariant-count/watermark home — re-derive, never copy.**

## Committed M3 Design Artifacts (informational)

| Artifact | Path | Date | Notes |
|---|---|---|---|
| Cross-tier deployment audit | `nexsys-hivemind/context/audits/2026-05-19_cross-tier-deployment-audit.md` | 2026-05-19 | Closed-out audit of cross-tier deployment surface. Informed M3.6 composition root design. |
| M3 audit gap-closure research (Artifact 1) | `homesynapse-core-docs/research/2026-05-20_M3_Audit_Gap_Closure_v1.md` | 2026-05-20 | Identified and closed audit-trail gaps prior to M3.4b/M3.6. |
| M3.6 Composition Root Design | `homesynapse-core-docs/design/2026-05-20_M3.6_Composition_Root_Design.md` | 2026-05-20 | Authoritative composition-root design that M3.6 implements against. |

## Interface Specifications (Phase 2 — FROZEN)

Phase 2 completed 2026-03-20 with all 16 JPMS-compiled subsystem modules specified (plus 3 scaffold modules — platform-systemd, test-support, dashboard — that have MODULE_CONTEXT.md placeholders but no compiled Java yet). See `context/planning/phase-2-block-backlog.md` for the frozen historical record of Blocks A–S. Summary at Phase 2 close: 16 modules specified, ~402 production Java files, all module-info.java files compile clean with `-Xlint:all -Werror`. AMD-33 subsequently (2026-04-10) ratified DomainEvent as permanently non-sealed.

## M3 Governance (committed 2026-05-16)

| Amendment | Status | Scope |
|---|---|---|
| AMD-41 | APPLIED | State Projection Execution Model |
| AMD-42 | APPLIED | Subscriber Lifecycle and Isolation |
| AMD-43 | APPLIED | Backpressure and Observability |

13 new invariants: INV-BUS-01..03, INV-PROJ-01/04/NEW-01, INV-WRITER-01, INV-SUB-ISO-01..06. DEC-M3-01 through DEC-M3-17 locked (DEC-M3-14/15 added 2026-05-17; DEC-M3-16 added 2026-05-20 — composition-root visibility strategy; DEC-M3-17 added 2026-05-20 — HealthSignal + HealthLevel transitive visibility promotion alongside QueueSaturationHealthCheck, DEC-M3-16 addendum). PLAN-M3-CONSOLIDATED-02 committed as the M3 implementation authority.

## Phase 3 Implementation Status (frozen historical record: M0 → M4-scoping)

Phase 3 began after Phase 2 closeout on 2026-03-20. **This table is the M0 → M4-scoping historical record, frozen 2026-05-28 — the last row's "Next: M4.0a" is history, not a pointer. Every milestone after M4-scoping lives in `context/planning/phase-3-milestone-backlog.md` + the masthead beats (pointer, not copy).** Progress:

| Milestone / WU | Status | Date | Notes |
|---|---|---|---|
| M0 | COMPLETE | 2026-04-04 | AMD-31, traceability, MODULE_CONTEXTs |
| M1 | COMPLETE | ~2026-04-08 | Contract tests + in-memory implementations |
| M2 | COMPLETE | 2026-05-01 | Full persistence layer |
| M2-bridge | COMPLETE | 2026-05-02 | AMD-34–37, structural hardening |
| M3.1 | COMPLETE | 2026-05-17 | InProcessEventBus core (14 types) |
| M3.2 | COMPLETE | 2026-05-17 | REPLAY→TRANSITION→LIVE bus-side (16 types) — `0bade6a` |
| M3.3 | COMPLETE | 2026-05-17 | Backpressure, metrics, observability (29 types) — `a5d4b2a` |
| M3.5a | COMPLETE | 2026-05-18 | StateProjection vertical slice — `a2aff9c` |
| Bus-Fix Piece A | COMPLETE | 2026-05-18 | `DerivedWriteRateLimit` visibility promotion — `fceafe8` |
| M3.5b | COMPLETE | 2026-05-18 | StateProjection production persistence — `08d0136` |
| Projection-checkpoint wiring | COMPLETE | 2026-05-19 | `StateCheckpointSource` + 10 MB advisory guardrail — `56aaa4b` |
| Supervisor DLQ wiring | COMPLETE | 2026-05-19 | `SubscriberSupervisor` constructs `DeadLetter` — `ed5862c` |
| M3.4a | COMPLETE | 2026-05-19 | Integration-tests module + harness + BurstLoadIT + HeapBudgetIT — `5ae7912` |
| M3.4b | COMPLETE | 2026-05-19 | Sustained-load + crash-recovery IT tests — `adf04d2` |
| M3.6a | COMPLETE | 2026-05-20 | Profile-driven persistence config — DeploymentProfile 6 fields, LockingMode enum, profile-driven PRAGMAs — `17c40b6` |
| M3.6b | COMPLETE | 2026-05-20 | EventBusConfig record, InProcessEventBus public (DEC-M3-16), ReplayWindowQueue parameterized — `df2743a` |
| M3.6c | COMPLETE | 2026-05-20 | Per-module event-class manifests (Q3 gap closure) — `38d3e30` |
| M3.6d-a | COMPLETE | 2026-05-20 | Composition-root satellite changes — SqliteStateStore→StateCheckpointSource, QueueSaturationHealthCheck+HealthSignal+HealthLevel public (DEC-M3-17), ReadinessSource, ReconciliationTest 4/5, Tier 9 un-disabled, HomeSynapseConfig + SharedScheduler + ThrowingStateQueryService skeletons, SLF4J wiring — `25bc23b` |
| M3.6d-b | COMPLETE | 2026-05-21 | PersistenceFactory + HomeSynapseCore composition-root wiring — 4-commit cohort: WriteCoordinator.queueSize() (`a33ee40`), production SubscriberReadConnectionFactory (`a59b64e`), PersistenceFactory public gateway (`725353d`), HomeSynapseCore facade (`dfb045e`). OR-M3-14 prerequisites bundled. — `dfb045e` |
| M3.6e.1 | COMPLETE | 2026-05-22 | MaterializedStateQueryService + ReadinessFilter + RestFilters + Javalin bootstrap + DeploymentProfile thread pool sizing. 6 created, 13 modified. Two follow-up fix rounds (Xlint:exports gateway pattern, Gradle/JPMS scope alignment). 7 deviations (none blocking). Sixteenth CC WU. — `b71ed37` |
| M3.6e.2 | COMPLETE | 2026-05-22 | Admin endpoints (DlqStatusEndpoint, ProjectionStatusEndpoint) + entity query endpoints (ListEntitiesEndpoint, GetEntityEndpoint, GetEntityStateEndpoint) + EndpointContext SPI + 2 RestFilters gateway methods + 2 ArchUnit rules (QUERY_SERVICE_READ_ONLY, REST_ENDPOINTS_NO_EVENT_PUBLISHING) + HomeSynapseCore 16-step bootstrap. 15 created, 6 modified, 19 test methods. 5 deviations (none blocking). Seventeenth CC WU. M3.6 COMPLETE. — `76288af` |
| M3.7 | COMPLETE | 2026-05-27 | E2E integration tests + abandon() + checkpoint key fix + TESTING policy. M3 COMPLETE. |
| M4 scoping | COMPLETE | 2026-05-28 | PLAN-M4-CONSOLIDATED authored; canonical scope; KB de-poison applied. Next: M4.0a. |

## Code State (pointer-form — re-derive, never copy)

Volatile code facts live in source, not here (truth-hierarchy rule 1). Re-derive at need:

- **Module census + graph:** `settings.gradle.kts` (subproject list) · root `build.gradle.kts` (graph knobs) · per-module `module-info.java` (the JPMS truth, read VERBATIM).
- **Type inventories, contracts, gotchas:** the per-module `MODULE_CONTEXT.md` files.
- **Count pins:** 71 EventTypes constants / 41 core `@EventType` records / 53 persistence codec registrations — enforced by `EventTypesTest` / `EventTypeAnnotationTest` / `EventTypeRegistryTest`+`JacksonWarmupTest`; re-derive from those tests, never from prose.
- **File/test counts:** re-derive from source when needed; any number printed in a doc is stale by construction.
- **Build gate:** the newest masthead beat + `pm-handoff.md` Open Risks carry live gate state; **CI on the pushed commit is the gate of record** (`ci.yml` + `install-smoke.yml`).

_The 2026-06-13-era Code State inventory (22 subprojects · ~803 Java files · ~2,002 tests · per-module narratives) rotated 2026-07-04 → `archive/PROJECT_SNAPSHOT-beats-42-65-rotated-2026-07-04.md` §B._

## Active Work

Re-derive from the NEWEST masthead beat + `context/handoff/coder-handoff.md` (NEXT-WU pointer) + the current week's plan (pointer, not copy). Lane routing: backend M-WUs → Claude Code; frontend/research/strategy → Cowork lanes; the hub never implements (infrastructure-map §3 + the PM skill masthead).

_The M3.6e.2-era Active Work block rotated 2026-07-04 → the archive §B._

## Blocking Issues

Re-derive from the newest masthead beat + `pm-handoff.md` Open Risks. A "None" printed here would be stale by construction.

## Open Risks

**Canonical ledger: `context/handoff/pm-handoff.md` §Open Risks — the single home, never duplicated here.** _The M3-era resolved rows (OR-M3-12/13/14/17/18) rotated 2026-07-04 → the archive §B._

## Tracked Gaps (carry forward)

_(2026-05/06-era items — re-verify against source before acting on any of them. The five closed-struck rows rotated 2026-07-04 → the archive §B.)_

1. ~~Tier 9 `reconciliationOnVersionMismatch` test `@Disabled("M3.5a")`~~ — **CLOSED (source-verified 2026-07-04 at the v19 currency pass: un-disabled + implemented at M3.6d-a; event-bus MODULE_CONTEXT Tier-9 row reads "6 active tests").**
2. **`bus.resume()` does not re-spawn VT** — pre-existing M3.1 limitation, deferred to lifecycle wiring. 2026-05-era claim; RE-VERIFY at consumption.
3. **Overflow test slow (~5–15s)** — consider `@Tag("slow")` if suite time grows.
4. **JFR-native emission is accepted design debt** — a pull-based metrics consumer (Prometheus/OTLP) needs a typed adapter layer; future observability pass.
5. **Publish-latency metric is bus-side only** — end-to-end latency is a persistence-module metric for a future pass.
6. **`lagEvents` approximation** — `pendingPositions.size()` instead of writer-tail-minus-delivered-position.
7. **Post-shutdown defensive handling in stores** — `SqliteDeadLetterStore` (and siblings) throw uncaught if called after `DatabaseExecutor.shutdown()`. Future hardening pass.

## Schedule Position

Well ahead of the 37-week Master Release Plan throughout (interface phase in 7 days; the persistence/bus/engine/integration arcs all landed early — history in `master-release-plan.md`'s Phase 3 Progress Annotation). **The governing gate now is the mid-Aug go/no-go (~Aug 16): M9.4b → bench acceptance → 72 h soak → install-smoke streak.** Re-derive live position from the newest masthead beat + the current week's plan.

## Next on Critical Path

Re-derive from the NEWEST masthead beat's **Next:** line + `context/planning/phase-3-milestone-backlog.md` + the current week's plan (pointer, not copy). Durable debt still carried: Phase 2 traceability stub indexes (docs 02–11, 13, 14) — low priority, batch later; 2026-05-era claim, re-verify at consumption.

## Recent Session Log

**The masthead beats above ARE the session record (2026-06-21 onward).** _The three 2026-06-26-era narrative rows rotated 2026-07-04 → the archive §B; older rows per the prior rotation notes preserved there._
