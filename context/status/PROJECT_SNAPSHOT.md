<!--
file: context/status/PROJECT_SNAPSHOT.md
purpose: Current operational state hub — current WU, code state, deferred gates, build status.
audience: All
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-08-30 (v58 hub, beat 8, Sun night — R-3b audited ACCEPT (H13 re-derived) → core fa6e0e8 (3 M incl. the hub fold); the R-4 slots stamped ×2; the run id + arm64 echo line fill at the CI read. Order: hivemind 6. Detail: pm-handoff beat 8.) Prior: 2026-08-30 (v58 hub, beat 7, Sun eve — the ★ pass applied (the R-4 packet Monday-ready) + THE SEPTEMBER PLAN authored (PROPOSED). Order: hivemind 4. Detail: pm-handoff beat 7.) Prior: THE FULL PRIOR CHAIN verbatim at context/handoff/archive/chains-pre-region-cap-2026-08-23.md + archive/chains-rotated-2026-08-27.md; rolled-off segments: the pm-handoff `:8` chain + its archives.
-->
# Project Snapshot

> **How to read this file:** the frontmatter `last-verified:` chain above (2 pointer segments + the rotation pointer) is the session record; the body below is an OVERWRITTEN DIGEST (W-HIVE-1 P9, ≤2 KB) — rewritten every beat, never appended. The chain and the newest pm-handoff beat block outrank everything else. **The operator's copy-source of record is the file on disk, never a chat card.** Full history: `context/handoff/archive/chains-pre-region-cap-2026-08-23.md` + `archive/PROJECT_SNAPSHOT-priors-rotated-2026-08-21.md`.

## The digest (v58 beat 8 — Sun 2026-08-30, night)

**HEADs:** core **`fa6e0e8`** (R-3b LANDED, 3 M; push pending → CI = the gate of record) · hivemind `04034a7` + the beat-8 order (**6 = 4 M + 2 A**) · skills `edcf060` · bench `4539f13` · docs `a53f474`.

**THE WEEKEND CHAIN:** R-3a DONE (audited) · **R-3b DONE (audited ACCEPT; H13 three-way byte-identity re-derived)** · **R-4 ARMED, two of three slots stamped** (sha `fa6e0e8` · predicted `0.1.0+git20260830.195441.gfa6e0e8`); the RUN ID + the arm64 `version-grammar echo green … sha256` line (= the origin hash) fill at the CI read.

**NICK'S RELAY after the core push:** the install-smoke run id + the arm64 echo line, verbatim → the last slot fills, the packet is COMPLETE for Monday evening.

**MONDAY (the plan §1):** the NFCU call (~1 h) · the Annex paste (1 min) · RATIFY (v1.1) + RATIFY-PLAN · the Pelton word on arrival (OUTRANKS; the addendum paste) · **R-4 evening → the guarded LIFT + THE MINT** (the re-prices + the RS-4 amendment + the four-qualifier law + refutable-by + the crosswalk seed + the re-bind clause).

**Fences (unchanged):** the D-1 pair until R-4 · `README.md:117` until W2-3 · no public brand use before G-2 · s31/nightly HANDS OFF until R-5 · the hub never implements · ONE COORDINATOR, ONE BOOT · no public sentence outside the register · orders never edited/grown after their card ships · the §VIII(4) consent at the LICENSE flip · held card `7c9e4fa` UNPATCHED · bench floor 8/9.

**Next:** Nick — push CORE (`git -C …/homesynapse-core push origin main`) + hivemind b8 · relay the CI pair. **PELTON → the word → the card.**
