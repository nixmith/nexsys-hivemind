<!--
file: context/status/PROJECT_SNAPSHOT.md
purpose: Current operational state hub — current WU, code state, deferred gates, build status.
audience: All
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-06-07 against HEAD 8028337 (M5-A Part 1+3 COMMITTED `8028337` — full ./gradlew check GREEN, 147 tasks). **M5-window ratification COMPLETE: Doc 15 LOCKED; AMD-86 + AMD-87 RATIFIED; on-disk amendment watermark AMD-64 → 87; M5-A Part 2 UN-GATED.** Both DOCS reviews RATIFY-WITH-EDITS folded — Doc 15 **E1** `encrypted_kek`→`encrypted_dek` across THREE occurrences (§4.1 column **and** semantic comment, §4.2 key-hierarchy row, §8.2 `ScopeKey`) + **E2** §3.8 composition-root bridge (`com.homesynapse.app`); AMD-87 E1/E2/E3. projectionVersion 5 (unchanged). WUCP Phase 2 closeout applied.
-->

# Project Snapshot

**Last updated:** 2026-06-12 EOD (R14/R15-returns session, same conversation) by PM (Cowork) — **R14/R15 RESEARCH CYCLE COMPLETE SAME-DAY: Nick dispatched all 3 briefs and returned all 3 → serialized PM assessments (fold order R14-B → R14-A → R15; every quote-back HONORED; zero discards; zero fabricated identifiers across two connector-blind runs; all three A−) → THE MERGED DISPOSITION (`context/planning/2026-06-12_M7-blueprint_merged-disposition.md`: REC-31..40 + 141..185, 7 collisions adjudicated, anti-requirements registered) → CHARTER POPULATED (M7 AMD block = 6 families F1–F6 enumerated; M7 holds at 3 pieces; W1–W8 crash-window carry pins; M8.2/M8.3 populated; M5-C backlog evidence-rich). R15's mandated in-Project disposition re-run EXECUTED against the strategy layer (Apache 2.0 CONFIRMED by the fork evidence; REC-182 → M5-C via R14-A's pain-match; REC-180 narrowed to a messaging rule). Research high-water REC-141→185. Raw returns archived → docs `research/returns/2026-06-12_*`; consumed briefs → `instructions/archive/`. Shas reconciled: hivemind `776fe2d`, core `e5ea76f` (the MODULE_CONTEXT count fix, docs-only on `7c73c91`). Next: PM drafts the M7 entry-gate AMD block; Nick rules C8 + the M6.3-vs-M7 ordering at W25/W26.** _Prior (same day, earlier — W0/R14-briefs session):_ **W0 Research-4 currency delta COMPLETE (REC-31..40 re-anchored at `7c73c91`; DQ escalation list EMPTY; REC-39 re-anchored +5 obligations incl. the NEW RunId/RunStatus/PendingStatus type-residency finding) → the three R14/R15 briefs AUTHORED + self-reviewed ×3 (REC 141–155 / 156–170 / 171–185; READY TO DISPATCH — Nick veto-or-default) → M7/M8 charter SKELETON on disk (3 first-class M7.x rows + 5-row entry-gate + 5 carry pins). MODULE_CONTEXT permit-count defect (27→30) corrected in core (docs-only). Preflight STALE (Check 9 only, expected). Documents-only otherwise — core HEAD stays `7c73c91`.** _Prior (2026-06-11, M6.2-return session):_ **M6.2 COMPLETE: gate GREEN 2026-06-11 (82 targeted + full `./gradlew check` 147 tasks, ZERO gate-fix rounds — an M6 first) → PM WUCP Phase 2 APPROVE (source-verified vs the session-brief answer key); **COMMITTED `7c73c91`** (2026-06-11 EOD; hivemind closeout `d161ca9`; both pushed; shas reconciled same session).** Executed on `62a81e6`; G1–G8 PASS; zero-new-edge holds BOTH directions (no module-info/Gradle/catalog touches anywhere); event manifests untouched (55/24/36); `!secret`/`!env` resolve in load AND reload via shared `runPipeline()`, write-path loader deliberately NON-resolving. **R-1/R-2/R-3 ruled ACCEPT** (R-1 fail-closed tag-bearing `write()` — NEW FUTURE-AMD: tag-preserving emitter, post-MVP; R-2 `PersistenceFactory` untouched = the M6.3 wiring step; R-3 DP-6 discharged as factory+ctor+bridge-test). 10 [INFO] accepted. **M6 = M6.1 ✓ M6.4 ✓ M6.2 ✓ — only M6.3 remains, triple-gated (its evidence gates = the M6 critical path).** Also this session: Step-0 (`62a81e6` reconciled, M6.4 gate CLOSED), the Nick-approved context-hygiene pass (pm-handoff 144K→38K; 27 files rehomed + sweep; spike dir deleted; A1/A3/A4 rotated post-Phase-2), the R14/R15 research architecture (`context/planning/2026-06-11_M7-blueprint_research-architecture.md` + agenda addendum), the W25 charter. _Prior (2026-06-11, M6.4 Phase 2):_ **M6.4 COMPLETE: delivered + ⟳ gate-fix round 1 + full `./gradlew check` GREEN (147 tasks) → PM WUCP Phase 2 APPROVE; COMMITTED `62a81e6` (sha reconciled 2026-06-11 at the M6.2-return session Step-0; M6.2 DISPATCHED same day — return in flight).** Coder executed M6.4 against `7e0bce8` (G1–G8 PASS; [R4]/[R5] applied; P2 survey re-run + 1 self-found addition). **Gate round 1: 169/1 fail — `ConfigLayoutTest.composeAfterMerge:264`, PM-diagnosed as a STALE M6.1a-era publish-count pin** (the ratified R1/DP-10 obligation adds a per-ERROR `config_error` beside `validation_completed` → 2 root drafts; implementation verified contract-correct, NOT changed) **→ GF-1 ruled: test updated to the ratified contract** (hasSize(2), order-agnostic type-filtering, dated pin-bump comment) — **re-run GREEN 147.** **Four [REVIEW]s ruled ACCEPT:** R-A backup name `homesynapse.yaml.bak.{basic-ISO}` (AMD-71 root-doc name + Windows-safe; DP-6's literal was the Doc-06-era name); R-B write-back IO failure = WARN + continue in-memory (backup-before-rename ordering source-confirmed `:888-889` — original never replaced without its backup); R-C post-flush listener rejection in `write()` → `IllegalStateException` wrap (frozen throws-clause; loud-failure for a buggy listener; M10 REST-mapping note logged); R-D nested-leaf diff attribution verified SOUND (`collectSections` flattens exhaustively — every leaf diffed exactly once in its immediate section; ancestor-listener behavior is AMD-66-faithful; M9 register-at-leaf gotcha posted). 13 [INFO] accepted. `CONFIG_SECTION_RELOADED → [SYSTEM]` confirmed. **Process lesson (shared PM+Coder miss): the P2 survey gains a category — behavioral publish-count pins in the producing module's sibling tests whenever a WU adds a publish site to an existing path** (skill source updated; mirror sync pending → Check 9 STALE expected). M6.2 instruction needs NO edit (its G7 gate is adaptive; the Coder's "drop R1" item referred to the stale pre-M6.4 coder-handoff pointer). **Next: Nick commits core (M6.4) + hivemind (`git mv` the consumed M6.4 instruction to archive/ at commit) → dispatch M6.2 ([R6] confirmed).** _Prior (2026-06-10 research-return session, late):_ **RESEARCH 13 ASSESSED → M6.4 + M6.2 INSTRUCTIONS AUTHORED (both ISSUE-READY).** (1) Preflight PASS against the pinned answer key (VM up — real Check-9 `diff -rq` on both skill mirrors: byte-identical; ONE pinned-line divergence investigated: the M6.1 tombstone survived Nick's `git add -A` [commit message said removed; `git show f9a551c` shows status M] — **deleted in-tree this session**). (2) **Research 13 return assessed: grade A−** (6-step A–F → `context/assessments/2026-06-10_Research_13_PM_Assessment.md`; raw return archived per Step 0 → docs `research/returns/2026-06-11_Research_13_Config_System_Market_Superiority.md`): quote-back gate HONORED; 11 RECs = 9 ACCEPT (2 narrowed) + **REC-134 RE-BUCKETED → ALREADY-COVERED** (Doc 06 §3.6 already pins `additionalProperties:false`@WARNING, shipped `9035110` — the one table defect) + REC-138 deferral softened (versioned-payload evolution exists; `trigger` already in §4.4); REJECT bucket genuinely empty (endorsed); flagship INV-CE-01 split-brain-immunity claim ENDORSED → M5-C website-grade material. **FUTURE-AMD queue for Nick:** REC-136 (reference-integrity validation pass — JPMS note: lives at composition-root/automation, NOT in config) + REC-138/`ConfigChangedEvent`-§12.4 reconciliation (one AMD family). (3) **M6.4 instruction AUTHORED** (`context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md`) — reload()/write() un-staged, atomic swap, AMD-66 invocation matrix, `ConfigChangeSet` diff + filters, §3.7 step-7 migration write-back/backup (R2), R1 per-ERROR `config_error` publish with the §12.4 x-sensitive fence, `config.section_reloaded` (AMD-70) with the FULL P2 fan-out (pins 54→55, 23→24, 35→36 + survey re-run mandated), CoreSchema-on-re-parse pinned (DP-12), REC-130/131(n)/132/133/135(n) folded. **⚠ Nick veto at dispatch — [R4]:** reload publishes `config.section_reloaded` ONLY; the frozen value-bearing `ConfigChangedEvent` would violate Locked Doc 06 §12.4 (PM-surfaced tension — record stays registered-but-unpublished; reconciliation = the REC-138 FUTURE-AMD family); **[R5]:** section_reloaded publish metadata = the validation_completed ruling (DIAGNOSTIC/SYSTEM/null-eventTime/publishRoot/system-subject). (4) **M6.2 instruction AUTHORED** (`context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`) — CARRY 1 E2 bridge embedded VERBATIM; zero-new-edge re-verified at authoring (persistence ∌ config ✓ / config ∌ persistence ✓ / app ⊇ both ✓; all three module-infos embedded verbatim); `ScopeKeyManager` (`.root-key` SecureRandom 0400, hand-rolled RFC-5869 HKDF-SHA256, wrapped DEKs, `config_secrets` scope per Doc 15 §7.3), `PayloadCipher` seam consumer-defined in persistence (java.base-only signatures, held-not-consumed until M6.3), `SecretStore.setAll` (AMD-68 §2.1/§2.2 + REC-140 crash-frame tests) + AMD-16 backup rotation, `!secret`/`!env` stage-3 tag resolution (CoreSchema kept, LTD-15 no-value-in-messages), Doc 15 §8.2 freeze-gate unreadability test; NO new deps/edges/module-info changes anywhere. **⚠ Nick veto at dispatch — [R6]:** `scope_keys` = config-side `scope_keys.json` per Locked §3.8's ownership sentence (the §4.1 CREATE TABLE placement is structurally unreachable from config in both directions under zero-new-edge — Doc-15 currency nit, parked with the §8.1 row nit). (5) **Sequencing rec: M6.4 executes FIRST** (both touch `StandardConfigurationService`; M6.2 carries adapt-to-current-state STOP-gates either way — Nick's call at dispatch). Housekeeping: tombstone deleted; consumed session prompt → `handoff/archive/`; research-agenda Research-13 row → RETURNED + ASSESSED; M6.3 untouched (still triple-gated). _Prior (2026-06-10 follow-up session):_ **CLEANUP + RESEARCH SCOPING: (1) Step-0 sha-reconciliation — M6.1a = `9035110` filled across all trackers (coder-handoff gate, pm-handoff Open Risks [entry CLOSED — no open deferred gates], snapshot, backlog, M6 charter, W24, cross-agent pointer); preflight STALE→PASS recorded. (2) Archival pass — cross-agent 2026-06-07→06-10 trail below `## Archived` (only the CURRENT POINTER active); consumed M6.1 instruction → `coding-instructions/archive/` (tombstone left — sandbox VM down, Nick `git rm`s it at commit); pm-handoff pre-M5-window session records rotated → `archive/pm-handoff-2026-05.md`/`-2026-06.md`, legacy M3-era sections marked for next rotation. (3) Docs-lane corrections (ONE docs commit): Doc 06 §3.2 `schemas/` correction note + §3.7 AMD-67 amendments-in-force banner; Doc 01 §4.4 +`config.validation_completed` row (the 35th); cloud-scalability :56 Rule-5 staleness note; Doc 15 untouched. (4) **Research 13 — Config System Market Superiority brief AUTHORED** (`context/instructions/Research_13_Config_System_Market_Superiority_Brief.md` — RQ1–RQ5 gap-relative to AMD-66..71/Doc 06/charter; REC-130+; mandatory disposition table; DOCS-Project dispatch, web search required, generic-deep-research evidence fallback; **numbered 13 per Nick's ruling** — Research 12 = the consumed Zigbee return). Next: Nick commits 3 repos + dispatches Research 13 → PM assessment → M6.4 + M6.2 instructions.** _Prior (2026-06-10 EOD):_ **M6.1 COMPLETE (WHOLE): M6.1b COMMITTED `b7bc65c`; M6.1a + ⟳ gate-fix round 1 GREEN (Nick: 27 + 147 tasks) → PM Phase 2 APPROVE; M6.1a COMMITTED `9035110` (sha reconciled 2026-06-10, follow-up session).** Gate-fix round 1 = 2 PM-ruled defects: explicit `CoreSchema` on both LoadSettings (engine default = JSON schema, `~` ≠ null; LTD-09) + ArchUnit Rule 5 drops `com.homesynapse.config..` per ratified AMD-71 (AMD-71-INV-01 containment = compensating control). Module-info verified = the 2026-06-10 ruling exactly. R1/R2/R3 ruled ACCEPT (R1 per-ERROR `config_error` publish → M6.4 obligation). Carries: Doc 06 §3.2/§3.7 + cloud-scalability :56 (docs lane); D4 `SystemId`-before-config-load (M13). _Earlier 2026-06-10:_ **M6.1 EXECUTED + SPLIT (Fork-2, pre-authorized): M6.1b COMPLETE — gate GREEN (Nick: targeted 57 tasks + full `./gradlew check` 147 tasks, 2026-06-10), PM WUCP Phase 2 → APPROVE, COMMIT PENDING (messages handed over). M6.1a UNBLOCKED — Nick RULED 2026-06-10: the five third-party non-transitive `requires` APPROVED (snakeyaml-engine/networknt are explicit JPMS modules; Gradle `implementation` scope does not exempt JPMS) + 2 Gradle lockstep lines; DP-4 corrected to a `List` ctor (AMD-66 §2.4 correction); AMD-66/67/68/70/71 §7 embed ruling-corrections authored (docs repo).** M6.1b = 23 files + 3 MODULE_CONTEXTs across config/event-model/persistence; config `module-info` byte-identical at `6c6dd33`; `CONFIG_VALIDATION_COMPLETED` registered across the full P2 manifest set (pins 53→54, 22→23, 34→35); 2 `[REVIEW]` ruled, 4 `[INFO]` accepted. RULING ADDENDUM appended to the M6.1 instruction — it is the M6.1a issuing delta. _Prior 2026-06-09 by PM (Cowork):_ **AMD-66..71 RATIFICATION MECHANICS EXECUTED (documents-only): M6 ENTRY-GATE CLOSED.** Nick ratified **AMD-66/67/68/70/71 (RATIFIED 2026-06-09)** + confirmed the **AMD-69 deferral (Option (a); Tier-2/OQ-15-3; number reserved)** per the 2026-06-09 DOCS review return (block RATIFY-WITH-EDITS; 7 edits already folded at docs `aedff55`). Mechanics applied: 6 AMD mastheads/checklists/dispositions flipped; **invariants §37–§41 registered (+8: 66-INV-01/02, 67-INV-01/02, 68-INV-01, 70-INV-01, 71-INV-01/02)** + §0.3/§17/§18 same-commit (⚠ §17 total **regenerated from the table: 152/41** — the prior stated "135/34" was a copied-forward under-count, −9 offset; see pm-handoff); nav-index 6 rows + preamble + Doc 06 row note; **watermark UNCHANGED at AMD-87** (reserved slots filled — ratification does not raise the ceiling); **M6.1 gate LIFTED → READY TO ISSUE (not issued)**; AMD-68 half of M6.2's gate satisfied; trackers/KB/handoffs current; both consumed instruction prompts archived. HEAD `6c6dd33` untouched; Doc 06/Doc 15 bodies untouched. _Prior 2026-06-08 by PM (Cowork):_ **M6 ENTRY-GATE (config side) AUTHORED + B2 schema decisions.** AMD-66–71 PROPOSED (M6 config block; AMD-68 = the Doc 06 `SecretStore.setAll` currency amendment; **AMD-69 DEFERRED to Tier-2/OQ-15-3** per Locked Doc 15 — escalated to Nick) + the DOCS-review prompt; the **M6 multi-piece charter** (M6.1–M6.4, E2 bridge + OR-M6-NONCE verbatim); the **gated M6.1** instruction; the **B2 decisions C8 `actorRef` + C9 energy shape** (contract-freeze-ready). Preflight PASS (all 11; HEAD `6c6dd33` = a 1-line `Main.java` typo fix on `7f44bed`, immaterial). Documents-only. _Prior 2026-06-07 by PM (Cowork):_ **M5-A Part 1+3 COMMITTED `8028337` (GREEN)** + **M5-window ratification COMPLETE**: both DOCS reviews RATIFY-WITH-EDITS folded → **Doc 15 LOCKED**, **AMD-86 + AMD-87 RATIFIED**, INV-PD-07 narrowed + INV-PD-03 at-rest posture stated, AMD-86-INV-01/AMD-87-INV-01 registered (§35/§36; 135 invariants/34 categories), nav-index rows + **watermark AMD-64→87**, **M5-A Part 2 UN-GATED** (Claude Code). m2 counter-nonce hazard carried as M6 Open Risk. WUCP Phase 2 closeout applied.

**Current state:** **M6 IN PROGRESS — M6.1 COMPLETE (whole): M6.1b COMMITTED `b7bc65c`; M6.1a (incl. gate-fix round 1) GREEN 2026-06-10 (27 + 147 tasks), PM Phase 2 APPROVE, **COMMITTED `9035110`**. **M6.4 COMPLETE 2026-06-11 (incl. gate-fix round 1) — full check GREEN 147, PM Phase 2 APPROVE, COMMITTED `62a81e6` (reconciled 2026-06-11). **M6.2 COMPLETE 2026-06-11 — gate GREEN (82 + 147, zero gate-fix rounds), PM Phase 2 APPROVE (R-1/R-2/R-3 ACCEPT), COMMITTED `7c73c91`.** M6.3 triple-gated (the last M6 piece — its gates are now the M6 critical path: OQ-15-2 microbench + AMD-86 §3 interviews + OR-M6-NONCE).** M5 window: M5-A COMPLETE (Parts 1+2+3; AMD-87 codec committed `7f44bed`); crypto+codec ratification COMPLETE; M9 command-bearing-`CapabilityAdded` prerequisite CLEARED. HEAD **`7c73c91`** (M6.2, on `62a81e6` M6.4 / `7e0bce8` docs-only / `9035110` M6.1a / `b7bc65c` M6.1b / `6c6dd33` typo fix) · **on-disk amendment watermark AMD-87** (Doc 15 LOCKED; AMD-86 + AMD-87 RATIFIED 2026-06-07) · `projectionVersion` **5** · build GREEN (`./gradlew check`, 147 tasks, incl. ArchUnit + spotless + moduleGraphAssert). Phase 3 — test-first implementation.
**Next action (2026-06-12 EOD, post-cycle):** **(1) PM next governance session: DRAFT the M7 entry-gate AMD block** — six families per merged disposition §2a (F1 triggers / F2 selectors / F3 actions+ConfirmationPolicy / F4 RunCausalChain+cycle-detection / F5 event vocabulary+type-residency / F6 definition-schema posture) + the DOCS review prompt; strategy-refresh drafts (§2f) ride along. **(2) Nick: ratify B2 C8** (entry-gate row 2 — now carrying R14-A's external provenance evidence) **+ the M6.3-vs-M7 ordering call at the W25/W26 boundary** (Lane-2: microbench target Jun 17 + interviews). **(3) M5-C Increment 1** (Lane-4 gate) — the §2e content backlog is ready (ledger dossier, no-cloud-account flagship, explainability w/ pain citations, + 3 copy guardrails). **(4) Commit both repos; delete the stale renamed index.lock; mirror sync (Check 9).** _Prior (2026-06-12, post-brief-authoring):_ **(1) Nick: veto-or-default the research architecture (`context/planning/2026-06-11_M7-blueprint_research-architecture.md`) + DISPATCH all 3 briefs simultaneously** (R14-A/R14-B → DOCS Project, web search required; R15 → generic deep research permitted for evidence, disposition pass in-Project). **(2) Nick: commit both repos** (messages in the session report; core = the MODULE_CONTEXT count fix, docs-only) **+ delete `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-12`.** **(3) PM next sessions: serialized assessments as returns land (fold order R14-B → R14-A → R15) → merged disposition → populate the charter skeleton + draft the M7 entry-gate AMD block.** **(4) Standing Nick-paced (the M6 critical path): Pi-4 microbench (target Wed Jun 17) + energy/erasure interviews; M5-C Increment 1 (the W25 Lane-4 structural gate); B2 C8/C9 ratification (C8 = M7 entry-gate row 2); skill-mirror sync (Check 9, one file).** _Prior (2026-06-11 EOD, post-M6.2-APPROVE):_ **(1) ~~COMMIT both repos~~ DONE 2026-06-11 EOD — core `7c73c91` + hivemind `d161ca9`, pushed, shas reconciled. Skill-mirror sync STILL PENDING (Check 9 — now carries the 2026-06-11 coding-instruction-format additions, still ONE file).** **(2) M6.3's evidence gates are now THE M6 critical path and both are yours: Pi-4 microbench (OQ-15-2) + energy/erasure interviews (AMD-86 §3).** **(3) Research 14/15: veto-or-default the dispatch architecture** (`context/planning/2026-06-11_M7-blueprint_research-architecture.md`; PM authors the 3 briefs next session, W0 Research-4 currency delta first). **(4) W25 charter on disk (`weeks/2026-W25_jun15-jun21.md`); W24 → COMPLETE at rollover (Jun 14/15) with the scorecard finalized.** **(5) B2 C8/C9 ratification — C8 `actorRef` now sits on the M7 critical path (automations stamp `AutomationId`).** _Prior (2026-06-11, post-M6.4-APPROVE — steps 1–2 done, 3 mirror-sync pending, 4 standing):_ **(1) Nick: COMMIT homesynapse-core (M6.4 — message handed over in the session report) + nexsys-hivemind closeout (include `git mv context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md context/coding-instructions/archive/` — EXECUTION RECORD already appended; no tombstone this time) + sync the skill mirror (one file: `project-manager/references/coding-instruction-format.md`).** **(2) Dispatch M6.2** (`context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`; dispatch line: "[R6] CONFIRMED; R1 is DONE in M6.4"; its G7 gate adapts to the post-M6.4 service). **(3) Next PM session: W25 charter + post-M6 deliberation beat** (M7/M8 automation runway; research-scoping for the competitor/ecosystem beat — Research 14 candidates: automation-engine market superiority on the Research-13 pattern; ecosystem/positioning scan feeding M5-C + strategy). **(4) Nick-paced, now the M6 critical path: Pi-4 microbench (OQ-15-2) + energy/erasure interviews (AMD-86 §3) — after M6.2 lands, M6.3 is the only M6 piece left and BOTH its evidence gates are yours.** FUTURE-AMD queue unchanged (REC-136; REC-138/§12.4). _Prior (2026-06-10, post-Research-13):_ **(1) Nick: COMMIT nexsys-hivemind + homesynapse-core-docs** (messages handed over in the session report; docs commit = the archived Research 13 return only; core untouched). **(2) Nick: rule on [R4]/[R5] (M6.4) + [R6] (M6.2) at dispatch — defaults stand unless vetoed — then ISSUE M6.4** (`context/coding-instructions/M6.4_Hot_Reload_Atomic_Swap.md`; re-verify baseline at issue), **then M6.2** (`context/coding-instructions/M6.2_SecretStore_ScopeKeyManager.md`; its G7 gate adapts to post-M6.4 state). **(3) FUTURE-AMD queue when convenient:** REC-136 reference-integrity; REC-138 + `ConfigChangedEvent`/§12.4 reconciliation. **M6.3 last (triple-gated — unchanged).** _Prior (2026-06-10, post-sha-reconciliation, superseded):_ **(1) ~~Nick: COMMIT homesynapse-core + nexsys-hivemind~~ DONE — core `9035110` ("M6.1a: YamlLoader…"), hivemind closeout `40de3ec`; M6.1a sha reconciled into all trackers 2026-06-10 (follow-up session).** **(2) PM: the config research/open-questions beat** (market-superior config system — Nick-directed scope), **then the M6.4 instruction** (obligations: R1 per-ERROR `config_error` publish; `config.section_reloaded` P2 survey re-run; CoreSchema on the reload re-parse) **and M6.2** (E2 bridge; zero-new-edge re-verify); **M6.3 last** (triple-gated). _Prior (2026-06-10 morning, superseded):_ **(1) Nick: COMMIT the three repos from host git** (homesynapse-core M6.1b; homesynapse-core-docs AMD ruling-corrections; nexsys-hivemind closeout — messages in the session report; fill the M6.1b sha into the trackers at commit). **(2) ISSUE M6.1a** (the RULING ADDENDUM in `context/coding-instructions/M6.1_Config_Pipeline.md` is the issuing delta: 5 third-party non-transitive `requires` + 2 Gradle lines; `List` ctor; DIAGNOSTIC publish metadata; held file set enumerated in coder-handoff `### DEFERRED — M6.1b`). Then **M6.4** (after M6.1 lands whole — re-run the P2 survey for `config.section_reloaded`), **M6.2** (E2 bridge; re-verify zero-new-edge at issue), **M6.3 last** (triple-gated). _Prior next-action (2026-06-09, superseded):_ **ISSUE M6.1** (`context/coding-instructions/M6.1_Config_Pipeline.md` — READY TO ISSUE, gate lifted, AMD numbers pinned; issue whole per Fork-2, the Coder's Phase-2 file-touch inventory is the split checkpoint). Then: M6.2 instruction after M6.1 lands (E2 `com.homesynapse.app` bridge + AMD-68 `setAll`; re-verify zero-new-edge at issue); **M6.3 stays triple-gated** (OQ-15-2 Pi-4 microbench + energy/erasure interview signal + OR-M6-NONCE); M6.4 after M6.1 (re-runs the P2 survey for `config.section_reloaded`). **Ratify B2 C8/C9 alongside** (independent review still pending). Parallel (Nick-paced): M5-C website/docs floor (**not started — P6 risk; stand up a defined increment this week**), M5-D interviews + Pi-4 microbench (must not slip past M6.2), GraalVM/GenZGC spikes → LTD-01. Parked: Doc 15 §8.1 `ScopeKeyManager`-row nit (next Doc-15-touching amendment); M6.4 survey obligation. _Prior next-action (2026-06-09, superseded):_ **AMD-66–71 DOCS review RETURNED RATIFY-WITH-EDITS (no rejects); 7 edits FOLDED by PM → awaiting Nick's RATIFICATION.** Verdicts: 66 RATIFY-AS-IS, 67/68/70/71 RATIFY-WITH-EDITS, 69 CONFIRM-DEFERRAL. The **load-bearing** edit E70-1 (config-event payloads were specified with config-module types in event-resident records → an `event→config` JPMS cycle, the AMD-52 class) is folded — flattened to event-resident types + a type-residency rule. Return on disk: `context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md`. **Nick:** ratify AMD-66/67/68/70/71 + confirm the AMD-69 deferral → then **register the 8 invariants**, **add nav-index rows** (watermark unchanged, all < 87), and **lift the M6.1 ⛔ gate** + the AMD-68 half of M6.2 → **issue the gated M6.1 instruction** (`context/coding-instructions/M6.1_Config_Pipeline.md`). Ratify the **B2 schema decisions C8+C9** alongside (independent review). The **M6 charter** holds M6.1–M6.4 + the E2 bridge + OR-M6-NONCE verbatim; **M6.3 stays GATED** (OQ-15-2 microbench + interview signal + OR-M6-NONCE). _Two carry-forward notes:_ the Doc 15 §8.1 `ScopeKeyManager`-row currency nit (next Doc-15-touching amendment, not now); M6.4 re-runs the P2 survey for `config.section_reloaded`. _Prior (2026-06-08): M6 entry-gate config side AUTHORED (AMD-66–71 + DOCS prompt + M6 charter + gated M6.1 + B2 C8/C9)._ _Prior (2026-06-07):_ M5-A COMPLETE (Parts 1+2+3); Part 2 (AMD-87 `Expectation` codec) **COMMITTED `7f44bed`** — full `./gradlew check` GREEN (147 tasks); PM WUCP Phase 2 → **APPROVE** (the 7 changed `.java` files source-verified vs AMD-87 §2 + AMD-87-INV-01, not the completion report; 3 `[INFO]` accepted incl. the optional `implementation(project(":core:device-model"))` lockstep hardening); **the M9 command-bearing-`CapabilityAdded` prerequisite is CLEARED**. **Next forward = the M5-D evidence Nick runs** (the Pi-4 AES-GCM microbench → **OQ-15-2** / the encrypted-scope set; the energy/erasure interviews → the **AMD-86 §3** trigger) — these are the **critical path to M6**, which stays parked until its entry-gate closes (OQ-15-2 + interviews + ratified **AMD-66–71** / Doc 06 `SecretStore.setAll(Map)` currency). The 3 M5-D evidence artifacts are AUTHORED + independently reviewed (RATIFY-grade). M5-C website/docs runs in parallel anytime. The **E2 composition-root-bridge wording** (`com.homesynapse.app`) + the **m2 counter-nonce hazard** are carried into the eventual M6 coding instruction; **AMD-66–71 (M6 config) are now UNHELD** (Doc 15 Locked). **In parallel (Nick's pace):** M5-C website/docs floor; **M5-D evidence — the three artifacts are AUTHORED 2026-06-07 (PM), awaiting Nick to run** (`context/assessments/2026-06-07_M5-D_*`): the **Pi-4 AES-GCM write-path microbench spec + per-category decision rule** (resolves **OQ-15-2**; gates the M6 at-rest WU — Nick runs it on Pi-4), the **energy + erasure interview guide** (carries the **AMD-86 §3** verifiable-erasure re-scope-up trigger + the D4 energy-shape questions — Nick runs the interviews), and the **sd_notify transport decision matrix + recommendation** (**OR-M13-SDNOTIFY**; defer-to-M13 gated on the GraalVM C15 spike; no catalog dep added). Locked Doc 15 untouched — the microbench tunes the encrypted-scope *list*; only the erasure interviews can re-open AMD-86, via the formal pipeline. GraalVM/GenZGC Pi spikes (C15/C16) still PLANNED. Charter: `weeks/2026-W24_jun08-jun14.md`; decisions: `context/decisions/2026-06-06_post-M4_M5-window_decisions.md`.
**Open flags:** **AMD-66–71 RESOLVED 2026-06-09 — AMD-66/67/68/70/71 RATIFIED + AMD-69 DEFERRED (Option (a) confirmed; Tier-2/OQ-15-3, number reserved); M6 entry-gate CLOSED; M6.1 READY TO ISSUE; watermark unchanged AMD-87.** Carried: the **Doc 15 §8.1 `ScopeKeyManager`-row currency nit** (reads "implement `PayloadCipher`", stale vs the E2-folded §3.8 adapter-at-`app` — fix rides the NEXT Doc-15-touching amendment; Doc 15 inviolate now) and the **M6.4 P2-survey obligation** for `config.section_reloaded` (review block-notes 1–2). **B2 C8/C9 PROPOSED** (independent review pending). **OR-PD-07-AMD (INV-PD-07 amendment) — RESOLVED 2026-06-07:** AMD-86 RATIFIED (INV-PD-07 narrowed + INV-PD-03 at-rest posture landed in `Architecture_Invariants_v1.md`; AMD-86-INV-01 §35) + Doc 15 LOCKED; watermark→87. **AMD-87** RATIFIED — un-gated M5-A Part 2. **OR-M6-NONCE (NEW — [BLOCKING-for-M6-impl]):** the per-scope GCM counter-nonce must be durable + strictly monotonic across crash AND restore, or (key,nonce) reuse breaks AES-GCM for that scope (Doc 15 §6/§13.4; AMD-86-INV-01 corollary) — carried explicitly so it survives Doc 15's Lock; resolve in the M6 at-rest-encryption WU (co-design with backup/restore). **OR-M13-SDNOTIFY:** sd_notify real AF_UNIX-datagram transport deferred to M13 (JDK-21 has no AF_UNIX SOCK_DGRAM; seam-tested) — **M5-D decision matrix AUTHORED 2026-06-07** (`context/assessments/2026-06-07_M5-D_sd_notify_transport_decision_matrix_OR-M13-SDNOTIFY.md`); PM rec: defer-to-M13 gated on the GraalVM C15 spike, reject JNR/JNA, subprocess-or-FFM by the WatchdogSec call; no catalog dep added. **M5-A deferred gate RESOLVED** (GREEN, 147 tasks; committed `8028337`). *(The live `@Disabled("AMD-65 pending")` annotation still carries the retired number — now-un-gated M5-A Part 2 deletes it.)*
**M4 retrospective:** `context/audits/2026-06-05_M4-retrospective.md` (sizing, the instruction-scope-miss pattern + the consumer/pin survey fix, deferred-gate shift-left, the partial-closeout finding, the debt register, strategic/velocity, and the keep-doing list). For M4 execution history see the Recent Session Log below.
**Committed (`8028337`, 2026-06-07 — Nick host-git):** M5-A Part 1+3 — platform-systemd `LinuxSystemPaths`/`LocalPaths`/`SystemdHealthReporter`/`NoOpHealthReporter` behind a `NotifyTransport` seam (`module-info` `requires transitive com.homesynapse.platform` + Gradle `api` lockstep; sd_notify real transport → M13); `FloorId` Jackson reg (10→11 typed wrappers) + `FloorIdSerializationTest`. Full `./gradlew check` GREEN (147 tasks).
**Latest commit:** **`e5ea76f`** — docs(automation): MODULE_CONTEXT permit-count correction (27→30 sealed subtypes re-derived from source; type total ~52→53; the W0 currency-delta finding), Nick host-git 2026-06-12; docs-only on `7c73c91` — substantive HEAD remains `7c73c91`. Prior: **`7c73c91`** — M6.2 (SecretStore + per-scope key-mgmt: `ScopeKeyManager`/HKDF/`scope_keys.json` [R6]; `StandardSecretStore` `secrets.enc` + AMD-68 `setAll` + AMD-16 rotation; `!secret`/`!env` in load+reload, write-path fail-closed [R-1]; E2 bridge `PayloadCipher` held-not-consumed; 29 files +3476/−58; **NO module-info/Gradle/`projectionVersion`/manifest change**; GREEN 147, zero gate-fix rounds), Nick host-git 2026-06-11. *(Commit-body nit: bash `!` history expansion ate the Stage-3 bullet's lead line — content otherwise intact; messages containing `!` go via `git commit -F <file>` henceforth.)* Prior: **`62a81e6`** — M6.4 (hot-reload atomic swap + UI/API write path: reload()/write() un-staged w/ the AMD-66 invocation matrix + C5 reject-and-keep-prior-good + volatile swap under one ReentrantLock; package-private `ConfigModelDiff`/`AtomicYamlWriter`/`SchemaAnnotationIndex`; R1 per-ERROR `config_error` w/ §12.4 fence + R2 §3.7 step-7 write-back; `ConfigSectionReloadedEvent` full P2 fan-out 54→55/23→24/35→36 [SYSTEM]; GF-1 `ConfigLayoutTest` pin-bump per R1/DP-10; **NO module-info/Gradle/`projectionVersion` change**; full `./gradlew check` GREEN 147), Nick host-git 2026-06-11. Prior: **`7e0bce8`** — docs-only: config `MODULE_CONTEXT.md` M6.1-currency one-liner (Nick host-git 2026-06-10; no code — the `6c6dd33` typo-fix precedent; substantive HEAD remains `9035110`). Prior: **`9035110`** — M6.1a (config load/validate half: `YamlLoader` CoreSchema-explicit safe YAML 1.2, `JsonSchemaCompositeValidator`, `StandardSchemaRegistry`, `StandardConfigurationService`, + ⟳ gate-fix round 1: CoreSchema both LoadSettings + ArchUnit Rule-5 config exemption per ratified AMD-71; module-info +5 third-party non-transitive `requires` per the 2026-06-10 ruling), on `b7bc65c` (M6.1b — config access/events half, AMD-66/67/70 contract shapes, 23 files), Nick host-git 2026-06-10 — **M6.1 COMPLETE (whole)**, full `./gradlew check` GREEN (147 tasks). Prior: `6c6dd33` — trivial 1-line `Main.java` print-string typo fix (em-dash removed) on top of `7f44bed`, Nick host-git 2026-06-07; `com.homesynapse.app` (ArchUnit-whitelisted), a string literal in `main()` — no contract/build/`projectionVersion` impact, build stays GREEN (147 tasks). **Substantive HEAD = `7f44bed`** — M5-A Part 2 (AMD-87 `Expectation` persisted codec: 2 new package-private codec classes `ExpectationSerializer`/`ExpectationDeserializer` — interface-keyed tagged-union over the 4 permits, exhaustive no-`default` switch, `ExactMatch`/`AnyChange` delegate to the registered `AttributeValue` codec, `WithinTolerance` reuses the AMD-52 float helpers; persistence `module-info` `+requires com.homesynapse.device` (acyclic, non-transitive; **no `build.gradle.kts` change**); `PersistenceJacksonModule` `+Expectation` pair (FloorId untouched); un-`@Disabled` `capabilityAdded_onOff_roundTrips` (real-event round-trip, not `DegradedEvent`); retired `TestEventSamples` AMD-65 Javadoc; new `ExpectationSerdeTest`. **NO `build.gradle.kts`/`projectionVersion`/migration/`@JsonTypeInfo`/event-shape change.** Full `./gradlew check` GREEN, 147 tasks; PM WUCP Phase 2 APPROVE; **M9 prereq cleared**; persistence 50→52 types), 2026-06-07. Prior: `8028337` — M5-A Part 1+3 (platform-systemd deployment impls — `LinuxSystemPaths`/`LocalPaths` → `PlatformPaths`, `SystemdHealthReporter`/`NoOpHealthReporter` → `HealthReporter`, behind a package-private `NotifyTransport` seam; `module-info` `requires transitive com.homesynapse.platform` ↔ Gradle `api` lockstep gate-fix; `FloorId` Jackson registration in `PersistenceJacksonModule`, 10→11 typed wrappers, + `FloorIdSerializationTest`; **NO `projectionVersion`/watermark change**; full `./gradlew check` GREEN, 147 tasks; sd_notify real AF_UNIX-datagram transport → M13, OR-M13-SDNOTIFY; **Part 2 AMD-87 Expectation codec NOT done — ⛔ GATED on AMD-87 ratification**), 2026-06-07. Prior: `8ef9e9f` — M4.C (AMD-54..64 integration-api interface freeze; integration-api 22→40 types; 5 lifecycle + 2 capability event records; code-bearing `PermanentIntegrationException`; `SecurityServices`/`CredentialRotator`; `HealthDetail`; EventCategoryMapping +7; production composition-root capability-manifest aggregation; **NO `module-info`/Gradle/`projectionVersion` change**; **WORKSTREAM C + M4 COMPLETE**; AMD-65 Expectation codec deferred to pre-M9), 2026-06-05. Prior: `e76b925` — M4.B-S2 (AMD-44 Stage 2: `EntityRole` enum + `EntityType` 6×3 legality matrix + `Entity` 11→12 / `ProposedEntity` 3→4 with construction matrix guard; 12 files, +591/−25; **no `module-info`/Gradle/`projectionVersion` change**; **Workstream B COMPLETE**), 2026-06-05. Prior: `e73e199` — M4.B-S1 (AMD-44 Stage 1: `FloorId` wrapper + `Floor`/`Area` records + `FloorRegistry`/`AreaRegistry` interfaces + `AreaId` Javadoc fix + `hardwareIdentifiers` `List`→`Set`; device-model 52→56, platform-api 12→13; **no `module-info`/Gradle/`projectionVersion` change**), 2026-05-31. Prior: `c99b425` M4.0b-5 (timestamp-model unifier, AMD-53; `EntityState` activity timestamps `lastChanged`/`lastUpdated`/`lastReported` → event-time `eventTime ?? ingestTime` in every `applyToState` branch + adoption seeding; `projectionVersion` 4→5), 2026-05-31. Prior: `72596cb` M4.0b-4b (typed `StateChangedEvent` payload + `AttributeValue` serializer + schema-versioned replay, AMD-52; `projectionVersion` 3→4), `971cfa1` M4.0b-4a (relocate `AttributeValue` → `com.homesynapse.value` leaf; breaks the event↔device JPMS cycle; AMD-52 §11 erratum; structure-only), `98f705b` M4.0b-3 (AMD-51; `projectionVersion` 2→3), `60b4185` M4.B3 (AMD-47), `7610296` M4.0b-2 (AMD-50), `cf1a97e` M4.0b-1, `a441fdf` M4.0a.
**Current phase:** P3 — Implementation (test-first).
**Days remaining:** 167
**Launch target:** November 25, 2026

---

## Current Work Unit

**M3.7 COMPLETE (committed `78264a0`).** Full `./gradlew check` GREEN (139 tasks). M3.7 delivered: E2E HTTP integration tests (HomeSynapseE2eHarness, CrashRecoveryHttpIT, EndpointE2eIT, InFlightRequestShutdownIT), abandon() contract (kill -9 simulation across persistence/bus/core/harness), NotifyingEventPublisher decorator, checkpoint key mismatch fix ("entity_state" → "state_projection"), FixedCheckpointPolicy.TESTING (N=1 event), HomeSynapseConfig 4th component (checkpointPolicy). Five Cowork fix rounds (H5 bus-FSM read, awaitility catalog pin, TESTING Jetty pool sizing + ASCII @DisplayName, Subscriber.setMode lifecycle hook, NotifyingEventPublisher + stale doc fixes) plus two Claude Code briefs (abandon() + MinimalEventBusStub, checkpoint key fix + TESTING policy). Eighteen Claude Code work units total.

**M4 scoping COMPLETE (2026-05-28).** Scope = **Canonical** (device-model expansion + projection/derivation foundation + integration-api interface freeze). Plan: `homesynapse-core-docs/design/HomeSynapse_Core_M4_Implementation_Plan_PLAN-M4-CONSOLIDATED.md`. Research 9 (projection rebuild/backfill, REC-76+) and Research 10 (typed change-detection, REC-90+) in flight; independent verification pass running in a second Cowork window.

**M4.0a COMPLETE (2026-05-29, `a441fdf`).** Atomic subscriber+view checkpoint coupling (AMD-45-INV-01) wired via the new `AtomicCheckpointSink` seam (consumer-defined interface in state-store, implemented in persistence over `AtomicCheckpointWriter`, injected at `HomeSynapseCore`). Per-delivery bus checkpoint gated for `state_projection` at **all three** writers — LIVE (`InProcessEventBus:508`) **and both** `ReplayDriver` REPLAY writes (tail + periodic) — so the invariant holds on LIVE *and* REPLAY (the D-1 correction). `SubscriberInfo.atomicCheckpoint` flag (AMD-45 §2.2 Option A). Reconciliation metadata (`reconciledAt`/`reconciledFromVersion`/`reconciledToVersion`) now populated (OR-M3-13 resolved); H2 `executeInTransaction` extracted; REC-80 replay-duration metric; REC-82 sentinel guard. Full `./gradlew check` GREEN.

**M4.0b-1 COMPLETE (2026-05-29, `cf1a97e`).** Amendment-free vertical slice of Workstream A: a production `DerivationRule` (`ProductionDerivationRule`, package-private in `com.homesynapse.state`, lifted from the `EchoStateRule` fixture — string change-detect, publishes a derived `state_changed` on LIVE so the canonical `attributes` map is finally populated) and a REC-28 `DispatchingProjectionAdvancer` (package-private, constructor-injected `EnvelopeHandler` map, no `ServiceLoader`, forward-all → exact cursor parity with the removed `MinimalProjectionAdvancer`). Both reached via public static gateways `DerivationRule.production()` / `ProjectionAdvancer.dispatching(EventStore)` (DEC-M3-16). Composition root rewired; `projectionVersion` stays **1** (no 1→2 bump, no backfill — that is M4.0b-2). REPLAY re-derives without publishing/applying (AMD-41 §3.2.2). OR-M3-17 + OR-M3-18 fully closed. Full `./gradlew check` GREEN. No `module-info`/Gradle changes.

**P2 RATIFIED (2026-05-29).** AMD allocation decision at `context/decisions/2026-05-29_P2_AMD_Renumbering_Decision.md`: device 46–49, projection 50–52 fixed; integration assign-at-milestone. **AMD-50** (projection rebuild/backfill, refines AMD-41 §3.2.4) is the first amendment to author and the M4.0b-2 unblock.

**AMD-50 RATIFIED (2026-05-29).** `design/amendments/AMD-50_Version_Transition_Backfill_and_Cursor_Determinism.md` — the general N→M reconciliation-backfill / cursor-as-log-position / supersession discipline (refines AMD-41 §3.2.4). On-disk amendment watermark is **AMD-50** (unchanged by M4.0b-2 — no new amendment authored).

**AMD-47 RATIFIED (2026-05-30).** `design/amendments/AMD-47_AttributeValue_Expansion_and_Upcaster.md` — device-model `AttributeValue` expansion: three public records (`QuantityValue`/`ArrayValue`/`DegradedAttributeValue`), the `AttributeValueUpcaster` SPI (no `ServiceLoader`), three `AttributeType` constants (`QUANTITY`/`ARRAY`/`DEGRADED`); ratification fork resolved → `QUANTITY` added (1:1 value↔type). **AMD-47-INV-01..05** registered into `Architecture_Invariants_v1.md` §20; Doc 02 §3.7/§8.2 PENDING blocks folded current; REC-93 hand-rolled `String` units (no JSR 385). **On-disk amendment watermark stays AMD-50** (47 < 50 — ratification records RATIFIED, does not raise the ceiling). Contract only — no production code; the types/SPI/§5 tests are **M4.B3**, which is now UNBLOCKED but not started.

**M4.0b-2 COMPLETE (2026-05-29, `7610296`).** Implements ratified AMD-50 for the 1→2 transition on M4.0b-1's string change-detect rule. `projectionVersion` 1→2 at `HomeSynapseCore` (the trigger); a `backfillActive` provenance gate (set in `StateProjection.initialize()`'s version-mismatch reconciliation branch, cleared at `onCaughtUp()`) drives a non-emitting one-shot backfill that reconstructs historical `attributes` from the `state_reported` log during replay-from-zero — applied on **both** `onEvent` and `processBatch` (the D-1 lesson) via the narrow `applyBackfillAttribute` (attributes + event-time `lastChanged` only; no second `stateVersion` increment, INV-01; never publishes). Supersession (§2.2): under the gate a logged prior-version `state_changed` advances the cursor but is suppressed for attributes (generality, §2.5). `Clock` removed from `DerivationContext` (§2.4) — determinism airtight by construction. 7 files modified (state-store + one-line lifecycle bump), 0 created, **no** `module-info`/Gradle changes. 8 AMD-50 §5 tests + event-time-`lastChanged` headline + processBatch D-1 twin. **PM Phase 2 read every changed file vs source and confirmed A–J** (gate-both-paths, event-time `lastChanged`, genuine supersession, INV-01, one-shot, `projectionVersion=2`, clock removal, INV-04). Deviations D-A/B/C/D all `[REVIEW]`/`[INFO]`, adjudicated ACCEPT. Full `./gradlew check` GREEN (139 tasks) + `:core:state-store:check` + `:lifecycle:lifecycle:check` GREEN. **AMD-50-INV-01..04 upheld.** Interim mixed-`lastChanged` (event-time in backfill, wall-clock in LIVE) is a conscious, documented interim — the timestamp-model unifier is a separate scheduled WU, NOT folded into this closeout.

**M4.B3 COMMITTED (2026-05-30, `60b4185`).** AMD-47 AttributeValue expansion is done, committed, and build-GREEN; WUCP Phase 2 closed (see pm-handoff). This closes Workstream B's typed-value contract.

**AMD-51 RATIFIED (2026-05-30).** `design/amendments/AMD-51_Typed_AttributeValue_Change_Detection_Comparator.md` — external `AttributeValueComparator` in `core/state-store` (`ComparisonPolicy`, exhaustive no-`default` switch over the 8 variants, DEC-M3-16 gateway). Per-type structural equality; **pinned total-form float/quantity epsilon `|a−b| > max(absEps, relEps·max(|a|,|b|))` + IEEE-754 totality; epsilon LOCKED at `absEps=relEps=1e-9`** (justified by the conversion-noise ceiling analysis — worst case °F→°C ≈ 6.6e-16 rel, ~5 orders below 1e-9 — plus the §5 #5b verification tests; no separate sensor-capture gate). Units free via AMD-47 canonicalize-at-construction (no JSR-385/LTD-10). **OQ-05-09 RESOLVED: symmetric schema-driven reconstruction of BOTH operands** (the materialized prior is always a `StringValue`, source-confirmed — `applyToState`/`CheckpointSerializer` only write `StringValue`; the `AttributeValueUpcaster` SPI is left unchanged). **String `StateChangedEvent` payload preserved**; AMD-52 (typed payload) staged behind OQ-05-08. `projectionVersion` 2→3 rides AMD-50's frozen backfill. Deadband deferred (REC-95). **AMD-51-INV-01..05** registered into `Architecture_Invariants_v1.md` §21 + state-store MODULE_CONTEXT + navigation index. **External review (HomeSynapse Core Claude Project) → RATIFY-AS-IS, 0 blocking; PM source-verified its claims (incl. the StringValue prior-side finding, CONFIRMED) and folded one clarifying edit (symmetric reconstruction) + coding-instruction gates; see AMD-51 §10.** **On-disk amendment watermark RAISED AMD-50 → AMD-51** (51 > 50). Contract only — no production code; implementing WU = **M4.0b-3** (NOT YET STARTED; M4.B3/AMD-47 already committed, so the DP-1 upcaster-wiring carry lands inside M4.0b-3 — there is NO intermediate M4.B3 session).

**AMD-52 RATIFIED (2026-05-31).** `design/amendments/AMD-52_Typed_StateChangedEvent_Payload_Serializer_and_Replay.md` — the typed `StateChangedEvent` payload (the cash-out of AMD-51 §2.7's staging). `oldValue/newValue` String→`AttributeValue` (`oldValue` nullable = first report); a custom `JsonSerializer`/`JsonDeserializer` pair in `core/persistence` emitting a compact `{"t":<AttributeType>,"v":…}` tagged-union envelope, exhaustive no-`default` switch over the 8 variants, **no `@JsonTypeInfo`** (Jackson-isolation HARD RULE + ArchUnit Rule 7), **no new module edge** (`persistence → transitive state → transitive device`, source-confirmed) and no new Jackson artifact; **bit-anchored float identity** (`Double.doubleToLongBits` after AMD-51 §2.3 canonicalization; stored text round-trippable, not byte-frozen — dissolves the JDK-18→19 Schubfach break; `chain_hash` stays the inert AMD-37 zero-reservation, NOT activated); JSON-valid non-finite sentinels; per-event `schema_version` 1→2 discriminator (**no `events`/`view_checkpoints` row migration**, G5); **Path A re-derivation from the immutable `state_reported` log authoritative**, Path B legacy `schema_version=1` reads → defined `DegradedEvent` (raw preserved, version-gated in `EventPayloadCodec.decode`, no upcaster pushed into the codec); typed `CheckpointSerializer` envelope (S2); `projectionVersion` **3→4** riding AMD-50's frozen backfill unchanged. **Sourced from** the OQ-05-08 design beat + Research 11 (PM Assessment **A−**, §7 source-verified vs HEAD `98f705b`) REC-100..105 + the four PM-under-delegation fork calls. **Ratified by Nick + an independent HomeSynapse Core Claude Project review (RATIFIED as-authored; F1 + F2 confirmed).** **AMD-52-INV-01..07** registered into `Architecture_Invariants_v1.md` §22 (+ §0.3 prefix, §17 index, §18 traceability) + Doc 01/03/04 currency notes + state-store MODULE_CONTEXT pointer. **On-disk amendment watermark RAISED AMD-51 → AMD-52** (52 > 51). Contract only — no production code; implementing WU = **M4.0b-4** (NOT YET STARTED; UNBLOCKED — the AMD-51 §2.7 String-payload freeze is lifted by this ratification). Code HEAD unchanged `98f705b`.

**AMD-52 §11 ERRATUM + module relocation (2026-05-31).** Authoring the M4.0b-4 coding instruction surfaced a real defect (caught by the instruction's own STOP-gate, escalated, Nick-confirmed): AMD-52's typed `StateChangedEvent` puts `AttributeValue` (`com.homesynapse.device`) into an event-model record → forces `event → device`, but `device → event` already exists → **JPMS cycle** (compile blocker). The OQ-05-08 beat/AMD-52/external-review checked persistence reachability, not the edge in the event record itself. **Resolution (Nick's calls):** relocate the `AttributeValue` hierarchy (interface + 8 variants + `AttributeType`, `java.base`-only) to a **new `com.homesynapse.value` leaf module** (event/device both depend on it → acyclic); minimal move (`AttributeSchema`/`AttributeValueUpcaster` stay in device); **sign-off gated on a JPMS compile-spike** + review; sequenced as **M4.0b-4a** (pure behavior-preserving relocation, green build) then **M4.0b-4b** (the AMD-52 typed payload on the clean graph). **No ratified AMD-52 fork reopened** — only the types' module home changes. Artifacts: AMD-52 §11 erratum; `design/2026-05-31_AttributeValue_Module_Relocation_Design_Note.md`; AMD-47 forward-note; M4.0b-4 instruction + review prompt (the instruction re-targets to 4b post-relocation).

**M4.0b-4a COMPLETE + COMMITTED `971cfa1` (2026-05-31).** The compile-spike PASSED (relocation applied in a throwaway worktree → `compileJava`+`compileTestJava` GREEN → full `./gradlew check` GREEN, 143 tasks: tests + ArchUnit + spotless), so the worktree was **promoted** to the production M4.0b-4a rather than redone. New **`core/value-model`** module (`com.homesynapse.value`, leaf — `java.base` only) holds the relocated `AttributeValue` + 8 variants + `AttributeType` (package `device`→`value`, contracts unchanged — the AMD-47 invariants travel with them). **`event` no longer requires `device`** → the JPMS cycle is structurally gone; `requires com.homesynapse.value` added to event/device/state (transitive) + persistence/automation (plain); rest-api gets a `testImplementation` edge (test-only). `AttributeSchema`/`AttributeValueUpcaster` stayed in device-model. MODULE_CONTEXTs updated (value-model created; device-model 62→52 types; consumers' edges). **Behavior-preserving** — entire existing test suite green, no semantics, no `projectionVersion` bump (still 3), watermark unchanged (AMD-52). Spike findings folded (automation = javadoc-only hygiene edge; rest-api test-only; `device→event` confirmed vestigial, left). **Next forward = the M4.0b-4b typed-payload coding instruction** (the existing M4.0b-4 instruction re-targeted to 4b; `AttributeValue` import path now `com.homesynapse.value`; the event-model STOP-gate is resolved). PM WUCP Phase 2 for 4a: this snapshot + cross-agent pointer + design-note §6 + pm-handoff updated; the doc closeout commits across nexsys-hivemind + homesynapse-core-docs (Nick — commit from host git; the sandbox `git status` mass-modified list is line-ending churn, not real).

**M4.0b-3 (AMD-51 typed comparator) is COMPLETE + COMMITTED `98f705b`** — independently reviewed against source → APPROVE; build GREEN all gates; WUCP Phase 2 closed; `projectionVersion`=3; watermark AMD-51; **Workstream A COMPLETE**. **Next forward = the M4 next-part scope/plan conversation** (Workstream B device-model breadth — AMD-44 Floor/EntityRole, Research 8 REC-23–30; Workstream C integration-api freeze — Research 6 REC-41–51, P3-gated; and the **AMD-52** typed-`StateChangedEvent` serializer/replay design beat behind OQ-05-08). _(Historical: the entry below records the pre-implementation state when M4.0b-3 was the next coding instruction.)_ **Research 10 RETURNED + ratified 2026-05-30** (`context/assessments/2026-05-30_Research_10_PM_Assessment.md` v2) — the typed change-detection design gate (OQ-05-06) is **RESOLVED**. **AMD-51 is now AUTHORED + RATIFIED (see entry above).** Next forward action = **the M4.0b-3 coding instruction** (PM Mode-3): modifies `ProductionDerivationRule` (C4) to reconstruct-both-sides-then-typed-compare via the new comparator, wires the `AttributeValueUpcaster` projection path (M4.B3 DP-1 carry), bumps `projectionVersion` 2→3 on AMD-50's backfill, and carries the mandatory gates (epsilon §5 #5b conversion-noise tests; adapter-`unit` audit + `canonicalUnitSymbol`-fallback WARNING §2.6; `shouldPublishDerived` coherence §5 #10; the §4c arch-rule test-`Clock` reminder — state-store non-whitelisted). **AMD-52** (typed event payload) remains **staged behind its serializer/replay design beat** (OQ-05-08) — do not co-commit. OQ-05-09 RESOLVED in AMD-51 §2.6. Ratified calls: defer deadband; FP-noise epsilon (locked 1e-9); hand-roll units (already satisfied by AMD-47); stage AMD-51→AMD-52. **Open prereqs / doc-currency follow-ups (gate forward WUs, not the commit):** P3 (Research 6 NQ-1..6, Nick's calls) gates Workstream C; **P4 Doc-05 half** (Integration-Runtime integration-api currency) is **still open + P3-gated** and gates Workstream C (not M4.0b-3); propagate the M4.0b-2 re-scope + M4.0b-3 row into PLAN-M4-CONSOLIDATED-v2 §3 (0a-i); KB M4.0b-2 currency punch-list (0a-ii: derivation is real, OR-M3-17/18 closed, `projectionVersion`=2, watermark→AMD-50); the timestamp-model unifier WU (needs a design beat, possibly amendment-worthy).

**Research 4 FULLY RESOLVED (2026-05-30).** All FQs PM-verified (v3) + all DQs Nick-resolved (v4 addendum to `context/assessments/2026-05-22_Research_4_PM_Assessment.md`). DQ-1 promote `PresenceTrigger`; DQ-2 rename `ActivateSceneAction`→`InvokeAutomationAction` + promote; DQ-3 same `DispatchingProjectionAdvancer`/separate handlers; DQ-5 geofence→M8. **Unblocks M7/M8 automation amendment authoring** (real AMD numbers assigned at M7 per P2 assign-at-milestone) — the assessment's "AMD-48..52" labels are pre-P2 placeholders, NOT the projection AMD-50/51/52.

---

## Design Documents

All 14 design documents are Locked (Phase 1 — System Design Documentation is complete).

| # | Document | Status |
|---|----------|--------|
| 01 | Event Model & Event Bus | Locked |
| 02 | Device Model & Capability System | Locked |
| 03 | State Store & State Projection | Locked |
| 04 | Persistence Layer | Locked |
| 05 | Integration Runtime | Locked |
| 06 | Configuration System | Locked |
| 07 | Automation Engine | Locked + AMD-25 integrated (2026-03-18) |
| 08 | Zigbee Adapter | Locked |
| 09 | REST API | Locked |
| 10 | WebSocket API | Locked |
| 11 | Observability & Debugging | Locked |
| 12 | Startup, Lifecycle & Shutdown | Locked |
| 13 | Web UI (Observability MVP) | Locked |
| 14 | Master Architecture Document | Locked |

## Committed M3 Design Artifacts (informational)

| Artifact | Path | Date | Notes |
|---|---|---|---|
| Cross-tier deployment audit | `nexsys-hivemind/context/audits/2026-05-19_cross-tier-deployment-audit.md` | 2026-05-19 | Closed-out audit of cross-tier deployment surface. Informed M3.6 composition root design. |
| M3 audit gap-closure research (Artifact 1) | `homesynapse-core-docs/research/2026-05-20_M3_Audit_Gap_Closure_v1.md` | 2026-05-20 | Identified and closed audit-trail gaps prior to M3.4b/M3.6. |
| M3.6 Composition Root Design | `homesynapse-core-docs/design/2026-05-20_M3.6_Composition_Root_Design.md` | 2026-05-20 | Authoritative composition-root design that M3.6 implements against. |

## Interface Specifications (Phase 2 — FROZEN)

Phase 2 completed 2026-03-20 with all 16 JPMS-compiled subsystem modules specified (plus 3 scaffold modules — platform-systemd, test-support, dashboard — that have MODULE_CONTEXT.md placeholders but no compiled Java yet). See `context/planning/phase-2-block-backlog.md` for the frozen historical record of Blocks A–S. Summary at Phase 2 close: 16 modules specified, ~402 production Java files, all module-info.java files compile clean with `-Xlint:all -Werror`. AMD-33 subsequently (2026-04-10) ratified DomainEvent as permanently non-sealed.

## M3 Governance (committed 2026-05-16)

| Amendment | Status | Scope |
|---|---|---|
| AMD-41 | APPLIED | State Projection Execution Model |
| AMD-42 | APPLIED | Subscriber Lifecycle and Isolation |
| AMD-43 | APPLIED | Backpressure and Observability |

13 new invariants: INV-BUS-01..03, INV-PROJ-01/04/NEW-01, INV-WRITER-01, INV-SUB-ISO-01..06. DEC-M3-01 through DEC-M3-17 locked (DEC-M3-14/15 added 2026-05-17; DEC-M3-16 added 2026-05-20 — composition-root visibility strategy; DEC-M3-17 added 2026-05-20 — HealthSignal + HealthLevel transitive visibility promotion alongside QueueSaturationHealthCheck, DEC-M3-16 addendum). PLAN-M3-CONSOLIDATED-02 committed as the M3 implementation authority.

## Phase 3 Implementation Status

Phase 3 began after Phase 2 closeout on 2026-03-20. Progress:

| Milestone / WU | Status | Date | Notes |
|---|---|---|---|
| M0 | COMPLETE | 2026-04-04 | AMD-31, traceability, MODULE_CONTEXTs |
| M1 | COMPLETE | ~2026-04-08 | Contract tests + in-memory implementations |
| M2 | COMPLETE | 2026-05-01 | Full persistence layer |
| M2-bridge | COMPLETE | 2026-05-02 | AMD-34–37, structural hardening |
| M3.1 | COMPLETE | 2026-05-17 | InProcessEventBus core (14 types) |
| M3.2 | COMPLETE | 2026-05-17 | REPLAY→TRANSITION→LIVE bus-side (16 types) — `0bade6a` |
| M3.3 | COMPLETE | 2026-05-17 | Backpressure, metrics, observability (29 types) — `a5d4b2a` |
| M3.5a | COMPLETE | 2026-05-18 | StateProjection vertical slice — `a2aff9c` |
| Bus-Fix Piece A | COMPLETE | 2026-05-18 | `DerivedWriteRateLimit` visibility promotion — `fceafe8` |
| M3.5b | COMPLETE | 2026-05-18 | StateProjection production persistence — `08d0136` |
| Projection-checkpoint wiring | COMPLETE | 2026-05-19 | `StateCheckpointSource` + 10 MB advisory guardrail — `56aaa4b` |
| Supervisor DLQ wiring | COMPLETE | 2026-05-19 | `SubscriberSupervisor` constructs `DeadLetter` — `ed5862c` |
| M3.4a | COMPLETE | 2026-05-19 | Integration-tests module + harness + BurstLoadIT + HeapBudgetIT — `5ae7912` |
| M3.4b | COMPLETE | 2026-05-19 | Sustained-load + crash-recovery IT tests — `adf04d2` |
| M3.6a | COMPLETE | 2026-05-20 | Profile-driven persistence config — DeploymentProfile 6 fields, LockingMode enum, profile-driven PRAGMAs — `17c40b6` |
| M3.6b | COMPLETE | 2026-05-20 | EventBusConfig record, InProcessEventBus public (DEC-M3-16), ReplayWindowQueue parameterized — `df2743a` |
| M3.6c | COMPLETE | 2026-05-20 | Per-module event-class manifests (Q3 gap closure) — `38d3e30` |
| M3.6d-a | COMPLETE | 2026-05-20 | Composition-root satellite changes — SqliteStateStore→StateCheckpointSource, QueueSaturationHealthCheck+HealthSignal+HealthLevel public (DEC-M3-17), ReadinessSource, ReconciliationTest 4/5, Tier 9 un-disabled, HomeSynapseConfig + SharedScheduler + ThrowingStateQueryService skeletons, SLF4J wiring — `25bc23b` |
| M3.6d-b | COMPLETE | 2026-05-21 | PersistenceFactory + HomeSynapseCore composition-root wiring — 4-commit cohort: WriteCoordinator.queueSize() (`a33ee40`), production SubscriberReadConnectionFactory (`a59b64e`), PersistenceFactory public gateway (`725353d`), HomeSynapseCore facade (`dfb045e`). OR-M3-14 prerequisites bundled. — `dfb045e` |
| M3.6e.1 | COMPLETE | 2026-05-22 | MaterializedStateQueryService + ReadinessFilter + RestFilters + Javalin bootstrap + DeploymentProfile thread pool sizing. 6 created, 13 modified. Two follow-up fix rounds (Xlint:exports gateway pattern, Gradle/JPMS scope alignment). 7 deviations (none blocking). Sixteenth CC WU. — `b71ed37` |
| M3.6e.2 | COMPLETE | 2026-05-22 | Admin endpoints (DlqStatusEndpoint, ProjectionStatusEndpoint) + entity query endpoints (ListEntitiesEndpoint, GetEntityEndpoint, GetEntityStateEndpoint) + EndpointContext SPI + 2 RestFilters gateway methods + 2 ArchUnit rules (QUERY_SERVICE_READ_ONLY, REST_ENDPOINTS_NO_EVENT_PUBLISHING) + HomeSynapseCore 16-step bootstrap. 15 created, 6 modified, 19 test methods. 5 deviations (none blocking). Seventeenth CC WU. M3.6 COMPLETE. — `76288af` |
| M3.7 | COMPLETE | 2026-05-27 | E2E integration tests + abandon() + checkpoint key fix + TESTING policy. M3 COMPLETE. |
| M4 scoping | COMPLETE | 2026-05-28 | PLAN-M4-CONSOLIDATED authored; canonical scope; KB de-poison applied. Next: M4.0a. |

## Code State

- **Repository:** homesynapse-core, **20 Gradle modules** (16 JPMS-compiled production modules + 3 scaffold-only: platform-systemd, test-support, dashboard + 1: `testing/integration-tests` for on-device IT tests, classpath-only test code).
- **Production + test Java files:** **724** total (source-verified 2026-05-28; supersedes earlier "~721+"). Across the JPMS-compiled production modules (post-M3.6e.2 addition of `MaterializedStateQueryService`, `ReadinessFilter`, `RestFilters` in state-store/rest-api, plus `DeploymentProfile` thread-pool fields, `HomeSynapseCore` Javalin bootstrap wiring, `StateQueryService.materialized()` static factory). Test files: **1,422 @Test/@ParameterizedTest/@RepeatedTest methods** (source-verified 2026-05-28; supersedes earlier "~1,575+"/"~1,594+") across ~173 test files and ~35+ testFixtures files (M3.6e.1 added: `MaterializedStateQueryServiceTest` (10 tests), `ReadinessFilterTest` (8 tests), `DeploymentProfileTest` (4 tests), `HomeSynapseCoreTest` stateQueryService test, `StateQueryServiceTest` updated assertion).
- **Event-bus module (post-M3.6d-a):** 19 public + 14 package-private = 33 top-level types. M3.6d-a promoted `QueueSaturationHealthCheck`, `HealthSignal`, `HealthLevel` to public (DEC-M3-17 — transitive 3-type chain because the public constructor's `Consumer<HealthSignal>` parameter would have triggered `-Xlint:exports` otherwise). M3.6b added `EventBusConfig` (public record, 2 fields: `replayQueueCapacity`, `publisherBlockedDepthThreshold`; `HOME_DEFAULT` constant). `InProcessEventBus` promoted to `public` (DEC-M3-16); new canonical 7-arg constructor accepting `EventBusConfig`. `PUBLISHER_BLOCKED_DEPTH_THRESHOLD` replaced by instance field from config. `ReplayWindowQueue` capacity parameterized (default 10,000 via no-arg; custom via `ReplayWindowQueue(int)`). `InProcessEventBusFactory` gained `createWithConfig(...)`. `EventBus` interface has 8 methods (4 abstract Phase 2, 4 default M3.1). `BusMetrics` interface (7 canonical metric names) with `BusMetricsJfr` JFR-native implementation.
- **Persistence module (post-M3.6d-b):** 48 types (was 45). M3.6d-b added `PersistenceFactory` (public final class — static `start()` factory, 8 store/infrastructure accessors returning public interface types, `AutoCloseable`), `SqliteSubscriberReadConnectionFactory` (package-private — production impl of `SubscriberReadConnectionFactory`, creates per-subscriber dedicated platform-thread executor + SQLite read connection; INV-SUB-ISO-02), `SqliteSubscriberReadExecutor` (package-private — per-subscriber read executor backed by a single platform thread and dedicated `Connection`; close() shuts down executor and connection). `WriteCoordinator` interface gained `int queueSize()` (DEC-M3-14); `PlatformThreadWriteCoordinator` implements it by exposing the bounded executor's queue size. `SqlitePersistenceLifecycle` expanded from 4-store to 6-store construction: now also constructs `SqliteStateStore` + `SqliteDeadLetterStore` during `start()`, with a separate `Include.ALWAYS`-configured `ObjectMapper` for `CheckpointSerializer` (distinct from the `NON_NULL` `PersistenceObjectMapper.create()` used for event payloads). New accessors: `stateStore()`, `deadLetterStore()`, `subscriberReadConnectionFactory()`. Module-info gained no new directives (all dependencies already present from M3.6d-a). `SqliteStateStore` now `implements StateCheckpointSource` (method renamed `serialize(int)` → `serializeCheckpoint(int)`, both `serializeCheckpoint` and `loadedProjectionVersion()` promoted to public via interface; class itself remains package-private — only the two interface methods are externally visible). M3.5b added `SqliteStateStore` (ConcurrentHashMap-backed materialized view + checkpoint-driven recovery), `SqliteDeadLetterStore` (UPSERT on (subscriber_id, event_position)), `CheckpointSerializer`, `CheckpointData`, `AtomicCheckpointWriter.writeAtomicCheckpointWithDlqPark()` (three-way atomic write — subscriber checkpoint + view checkpoint + DLQ park), V004 DLQ operational indices migration. Two ObjectMapper configurations coexist: `PersistenceObjectMapper.create()` returns `NON_NULL` for event payloads; checkpoint serialization requires `ALWAYS` and is constructed at composition time (composition-root wiring lands in M3.6d-b).
- **State-store module (post-M3.6e.1):** 20 public + 2 package-private types (M3.6e.1 added `MaterializedStateQueryService` — package-private final, backed by `StateStore`, recomputes stale at read time via injected `Clock`; `StateQueryService.materialized(StateStore, ReadinessSource, Clock, LongSupplier)` public static factory exposes construction surface per DEC-M3-16 pattern). `ReadinessSource` public interface (single method `mode() → SubscriberMode`). Projection-checkpoint wiring's `StateCheckpointSource` interface in scope. Advisory 10 MB checkpoint-size guardrail enforced.
- **Lifecycle module (post-M3.6e.2):** 8 public + 2 package-private types. M3.6e.2 expanded `HomeSynapseCore`: bootstrap sequence now 16 steps (14 from M3.6e.1 + 2 from M3.6e.2: entity query endpoint registration via `RestFilters.addEntityEndpoints` + admin endpoint registration via `RestFilters.installAdminEndpoints`). M3.6e.1 added steps 11-12 (added step 11: `MaterializedStateQueryService` wiring via `StateQueryService.materialized(stateStore, this, clock, () -> stateProjection.cursorPosition())`, step 12: Javalin server construction with `QueuedThreadPool(profile.javalinMaxThreads(), profile.javalinMinThreads())` + `RestFilters.installReadinessGate(app, this)` + `app.start(7070)`). `stateQueryService()` now returns the real `MaterializedStateQueryService` (replaces `ThrowingStateQueryService` placeholder). Module-info gained `requires com.homesynapse.api.rest`, `requires io.javalin`, `requires org.eclipse.jetty.util`. `DeploymentProfile` gained `javalinMinThreads()`/`javalinMaxThreads()` accessors for Javalin thread pool sizing: STUDIO(1/4), HOME(2/8), PERFORMANCE(4/16).
- **testing/integration-tests module (M3.4a + M3.4b):** Module #20. Tests gated on `-PpiProfile` (off by default; excluded from `./gradlew check`). Five IT tests: `BurstLoadIT`, `HeapBudgetIT` (M3.4a), `Pi4SustainedLoadIT`, `Pi4D1SpikeIT`, `CrashRecoveryIT` (M3.4b). `IntegrationTestHarness` composes `PersistenceTestHarness` + `InProcessEventBusFactory` against a real `@TempDir` SQLite file, with `startThrottled()` and `startForCrashSimulation()` factories (M3.4b). JVM constraints: `-Xmx256m -Xms256m -XX:ActiveProcessorCount=4 -XX:+UseG1GC -XX:MaxGCPauseMillis=100`.
- **MODULE_CONTEXT.md:** 20 files exist total — 17 populated substantively (including `testing/integration-tests` populated during WUCP Phase 2 reconciliation), 3 stubs (platform-systemd, dashboard, test-support). `core/event-bus/MODULE_CONTEXT.md` updated in M3.6b (EventBusConfig, InProcessEventBus public + 7-arg constructor, ReplayWindowQueue parameterized, InProcessEventBusFactory.createWithConfig). `core/persistence/MODULE_CONTEXT.md` updated in M3.6a (DeploymentProfile 3→6 fields, DatabaseExecutor accepts profile, LockingMode enum, PersistenceLifecycle Javadoc cleaned). `core/state-store/MODULE_CONTEXT.md` updated in projection-checkpoint wiring (added `StateCheckpointSource`, byte[0] stub deprecation note, projectionVersion authoritative source).
- **Rest-api module (post-M3.6e.2):** 28 Phase 2 public types + 2 M3.6e.1 types + 8 M3.6e.2 package-private types = 38 Java files. M3.6e.2 added `EndpointContext` (package-private SPI interface), `JavalinEndpointContext` (adapter), `EndpointResponses` (utility), `ListEntitiesEndpoint`, `GetEntityEndpoint`, `GetEntityStateEndpoint`, `DlqStatusEndpoint`, `ProjectionStatusEndpoint` (all package-private endpoint handlers). `RestFilters` gained 2 new public methods (`addEntityEndpoints`, `installAdminEndpoints`). M3.6e.1 added `ReadinessFilter` (package-private — implements `io.javalin.http.Handler`, 503 rejection with RFC 9457 problem detail body + `X-HomeSynapse-Projection-State` + `Retry-After: 5` headers) and `RestFilters` (public final utility — `installReadinessGate(Object javalinApp, ReadinessSource)` with deliberate `Object` typing to erase Javalin from the public API surface per DEC-M3-16). Module-info gained `requires transitive com.homesynapse.state`, `requires com.homesynapse.event.bus`, `requires io.javalin`, `requires org.slf4j`. Build.gradle.kts: `implementation` → `api` for state-store (fixes JPMS scope mismatch — websocket-api `requires transitive com.homesynapse.api.rest` needs state-store transitively visible). `ProblemType` enum gained `STATE_STORE_REPLAYING` (D-7).
- **Tests:** **1,422** @Test methods total (source-verified 2026-05-28). Contract test suites: `EventStoreContractTest` (27), `EventBusContractTest` (50), `WriteCoordinatorContractTest` (11), `CheckpointStoreContractTest` (9), `ViewCheckpointStoreContractTest` (10), `DeadLetterStoreContractTest` (10). M3.6e.1 added `MaterializedStateQueryServiceTest` (10 — 4 staleness-recomputation, 3 snapshot variants, 1 unmodifiable-map, 1 view-position, 1 isReady-delegation), `ReadinessFilterTest` (8 — LIVE pass-through, 3 non-LIVE 503 with diagnostic headers, RFC 9457 body shape, mode-freshness), `DeploymentProfileTest` (4 — all 3 profiles + min≤max invariant), `StateQueryServiceTest` assertion updated to 6 methods (D-3 static factory). M3.6d-a added `ReconciliationTest` (4 of 5 methods — OR-M3-13) + `SharedSchedulerTest` (5). Prior: M3.5b added `AtomicCheckpointWriterDlqTest` (3), `CheckpointSerializerTest` (12+), `SqliteStateStoreTest` (12). Supervisor DLQ wiring added `SubscriberSupervisorTest` (12). M3.4a added `BurstLoadIT` + `HeapBudgetIT` (gated). M3.4b added `ThrottledWriteCoordinatorTest` (9) + `Pi4SustainedLoadIT` + `Pi4D1SpikeIT` + `CrashRecoveryIT` (gated).
- **Traceability:** 01-event-model.md (44 entries), 12-lifecycle.md (2 entries). Stub indexes remain for docs 02–11, 13, 14 — Phase 2 traceability debt carries into Phase 3.
- **Last build gate:** RESOLVED. Full `./gradlew check` GREEN (139 tasks) at HEAD `98f705b` on 2026-05-30 (M4.0b-3, AMD-51 typed comparator + `projectionVersion` 2→3; `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` GREEN). Build confirmed by Nick. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion — `Pi4D1SpikeIT` is a fixed 30-min soak (not a hang), excluded from the default `check`, and AMD-51 does not touch the path it exercises; not a blocker. **No deferred build gates open.** Prior: `60b4185` M4.B3, `7610296` M4.0b-2 — both GREEN.

## Active Work

- **Last completed:** M3.6e.2 via Claude Code (2026-05-22, `76288af`). M3.6 COMPLETE — all seven sub-WUs shipped. Seventeen CC WUs total.
- **Next:** M3.7 scoping via research pipeline. E2E integration tests. Estimated Coder time: 6–8h.
- **Claude Code workflow validated:** acceptEdits mode, Opus 4.7 xhigh, deny git commit/push/gradlew. PM generates task instruction → Claude Code executes → Nick reviews git diff, runs build gate, commits. Through M3.6e.2, seventeen Claude Code WUs executed via this workflow. Bus-Fix Piece A and SLF4J follow-up patch were direct edits by Nick.

## Blocking Issues

None. No unresolved deferred build gates. All M3.6a through M3.6e.1 build gates GREEN at commit time.

## Open Risks

- **OR-M3-17 — FULLY CLOSED (M4.0b-1, 2026-05-29, `cf1a97e`).** The no-op `MINIMAL_DERIVATION_RULE` lambda (the M3.7 placeholder that closed the *symptom*) is now replaced by the production `ProductionDerivationRule` (package-private in `com.homesynapse.state`, reached via `DerivationRule.production()`), which publishes a real `state_changed` on a canonical-attribute change. Real derivation has shipped per DEC-M3-10 + REC-28.
- **OR-M3-18 — FULLY CLOSED (M4.0b-1, 2026-05-29, `cf1a97e`).** The `MinimalProjectionAdvancer` placeholder is removed and replaced by `DispatchingProjectionAdvancer` (REC-28 — package-private, constructor-injected `EnvelopeHandler` map, no `ServiceLoader`, forward-all with exact cursor parity). The advancer class file was deleted from `lifecycle`.
- **OR-M3-12 — DEC-M3-17 governance entry (NEW 2026-05-20):** RESOLVED in this WUCP Phase 2 closeout. HealthSignal + HealthLevel public promotion alongside QueueSaturationHealthCheck logged as DEC-M3-17; entries appended to Current_State §3 ledger, Locked_Decisions Phase 3 milestone section, and `context/decisions/phase-3-cross-module-decisions.md`. No further action.
- **OR-M3-13 — ReconciliationRecordsMetadataInDataSlot — RESOLVED (M4.0a, 2026-05-29, `a441fdf`).** Reconciliation metadata (`reconciledAt`/`reconciledFromVersion`/`reconciledToVersion`) is now populated via the extended `StateCheckpointSource.serializeCheckpoint(...)` surface, threaded through `StateProjection.initialize()`'s version-mismatch reconciliation branch; `SqliteStateStore` writes the values instead of `null`. `ReconciliationTest`'s deferred 5th method (`reconciliationRecordsMetadataInDataSlot`) is un-deferred and passing — it asserts `reconciledToVersion == 2`, the value M4.0b's backfill gate binds to. AMD-41 §3.2.4's metadata-recording requirement is now met.
- **OR-M3-14 — M3.6d-b prerequisite infrastructure (NEW 2026-05-20):** RESOLVED 2026-05-21. All three prerequisite pieces shipped in the M3.6d-b 4-commit cohort: (1) `WriteCoordinator.queueSize()` at `a33ee40`; (2) production `SqliteSubscriberReadConnectionFactory` at `a59b64e`; (3) `SqlitePersistenceLifecycle` 6-store expansion + `PersistenceFactory` public gateway at `725353d`. `HomeSynapseCore` composition root completed at `dfb045e`. Build GREEN.

## Tracked Gaps (carry forward)

1. **Tier 9 `reconciliationOnVersionMismatch` test still `@Disabled("M3.5a")`** — re-enablement was split out of Bus-Fix Piece A. Now tracked under M3.6 lifecycle wiring (depends on `bus.resume()` VT re-spawn fix).
2. **`bus.resume()` does not re-spawn VT** — pre-existing M3.1 limitation; deferred to M3.6 lifecycle wiring.
3. **Overflow test slow (~5-15s)** — consider `@Tag("slow")` if suite time grows.
4. **JFR-native emission is accepted design debt** — pull-based metrics consumer (Prometheus/OTLP) will need typed adapter layer in M4+.
5. **Publish-latency metric is bus-side only** — end-to-end publish latency is a persistence-module metric for future observability pass.
6. **`lagEvents` approximation** — uses `pendingPositions.size()` instead of writer-tail-minus-delivered-position. To be revisited under M3.4b Pi4SustainedLoadIT if accuracy proves insufficient.
7. ~~**Composition root for ALWAYS-configured ObjectMapper**~~ — **CLOSED (M3.6d-b `725353d`).** `SqlitePersistenceLifecycle.start()` now constructs a separate `ALWAYS`-configured `ObjectMapper` for `CheckpointSerializer`, distinct from `PersistenceObjectMapper.create()` (`NON_NULL`). Wired through `PersistenceFactory`.
8. ~~**`QueueSaturationHealthCheck` and `DerivedWriteRateLimit` lifecycle wiring deferred**~~ — **CLOSED (M3.6d-b `dfb045e`).** `HomeSynapseCore.start()` constructs both and passes them to `SharedScheduler`.
9. ~~**`DerivedWriteRateLimit.refill()` externally called**~~ — **CLOSED (M3.6d-b `dfb045e`).** `SharedScheduler` calls `refill()` every 50 ms via `safelyInvoke(rateLimit::refill)`.
10. ~~**Defence-in-depth `IllegalStateException` for `EventPublisher.publish()` from REPLAY**~~ — **CLOSED (M3.6d-b `dfb045e`).** `HomeSynapseCore` subscribes the projection with `SubscriptionFilter.all()` + `coalesceExempt=true`; the bus's mode FSM prevents publish calls during REPLAY. The composition root wiring itself is the defence.
11. **Post-shutdown defensive handling in stores** — `SqliteDeadLetterStore` (and siblings) will throw uncaught exceptions if methods are called after `DatabaseExecutor.shutdown()`. Pre-existing pattern, not an M3.5b regression. Track for a future hardening pass.
12. ~~**`AtomicCheckpointWriter` code duplication**~~ — **CLOSED (M4.0a / H2, `a441fdf`).** Shared `executeInTransaction(context, work)` helper extracted; both `writeAtomicCheckpoint` and `writeAtomicCheckpointWithDlqPark` route through it.

## Schedule Position

**Significantly ahead of Master Release Plan.** The 37-week plan placed Weeks 1–10 as "Interface Specification" — the project finished that work in 7 days (Mar 14–20). Phase 3 persistence subsystem (M2.x) complete. Event bus production implementation with full REPLAY→LIVE algorithm + backpressure + persistent DLQ + state projection + integration tests (including sustained-load and crash-recovery) all landed in the M3.1–M3.4b window (May 16–19). See the Phase 3 Progress Annotation in `master-release-plan.md` for actual vs planned dates.

## Next on Critical Path

1. **M4 next-part scope/plan** (fresh Cowork conversation) — formally define the scope of the next M4 segment: **Workstream B** device-model breadth (AMD-44 Floor/EntityRole impl, Research 8 REC-23–30) and **Workstream C** integration-api interface freeze (Research 6 REC-41–51, P3-gated on Nick's NQ-1..6), plus the **AMD-52** typed-`StateChangedEvent` serializer/replay design beat (OQ-05-08). Surface deep-research needs (e.g. Axon and other event-sourced platforms) to de-risk scale/maintainability — grounded in concrete forward design questions, not invented problems.
2. ~~**AMD-51 §2.6 erratum**~~ — **DONE 2026-05-31** (docs repo). The missing-schema → `StringValue` string-compare fallback (M4.0b-3 D-1) is recorded as the ratified no-schema behaviour in AMD-51 §2.6; the amendment now matches shipped code. (Commit pending with the docs-repo warm-up batch.) Also DONE: the KB doc-currency punch-list (0a-ii) — `HomeSynapse_Current_State`/`Knowledge_Primer`/`Navigation_Index`/`Decisions_Quick_Reference` brought current to `98f705b`/`projectionVersion` 3/watermark AMD-51.
3. **Phase 2 traceability debt** — 10 stub indexes remain (docs 02–11, 13, 14); state-store/device-model M4.0b-3 entries also pending. Low priority; batch later.

## Recent Session Log

| Date | Agent | What Happened |
|------|-------|---------------|
| 2026-06-12 EOD | PM (Cowork) | **R14/R15 returns ASSESSED (all 3, serialized B→A→15) + MERGED DISPOSITION + CHARTER POPULATED (documents-only).** Same-conversation continuation: Nick dispatched + returned all three briefs same-day; shas `776fe2d`/`e5ea76f` reconciled at start. **Every §0 quote-back HONORED; zero discards; zero fabricated identifiers** (both R14 runs DOCS-connector-blind, declared — the verbatim-embed discipline held; process note: fix connector reach or embed contract tables at quote granularity). **R14-B A−** (10 M7-obligations incl. monotonic-seam sustain arithmetic + storm benchmark + W1–W8 crash-window pins; 2 M8; 2 FUTURE-AMD; REJECT engine-retry) — PM source-verified Doc 07 §3.10/§3.11 (zombie-Run finalization + per-idempotency crash recovery = richer than embeds; two OPENs → CLOSED-UNTESTED). **R14-A A−** (ledger-gap dossier = M5-C flagship; REC-147 re-bucketed ALREADY-COVERED at :270; REC-141/145 narrowed — all four trace details Locked-specified, traces are log events/no ring buffer; REC-153 split). **R15 A−** (register fence HELD; PM executed the mandated in-Project re-run vs strategy files 1–5: **Apache 2.0 CONFIRMED** by fork-pattern evidence, governance refined; REC-182 → M5-C via R14-A pain-match; REC-180 → messaging rule; file-4-vs-file-2 data-monetization tension logged for the strategy refresh). **Merged disposition** (7 collisions adjudicated; **AMD block = 6 families**; M7 = 3 pieces CONFIRMED; high-water **REC-185**; anti-requirements: no engine retry / no templating DSL / no forced migration / never lead with commodity encryption). Returns archived → docs `research/returns/2026-06-12_*`; briefs → archive. **Next: draft the M7 AMD block; Nick C8 + M6.3-vs-M7 + M5-C Increment 1.** |
| 2026-06-12 | PM (Cowork) | **W0 currency delta + R14/R15 briefs AUTHORED + M7/M8 charter skeleton (documents-only).** Preflight vs the session-brief PINNED STATE: **STALE (Check 9 only — the one pinned mirror file, expected)**; Check-10 drift fixed in-session (strategic-context-map → archived `phase-2-block-backlog.md` path). Environment: VM mount served truncated file tails (phantom git modifications in BOTH repos — host files verified intact via file tools; lesson folded into `cowork-environment-model.md`); a VM-created hivemind `.git/index.lock` neutralized by rename (Nick deletes); VM died mid-session (file tools carried it). **W0:** `context/assessments/2026-06-12_W0_Research_4_Currency_Delta.md` — REC-31..40 re-anchored at `7c73c91` (all structural claims HOLD; survived-by-construction confirmed empirically); **DQ list EMPTY** (v4 resolutions re-verified); **REC-39 +5 obligations — NEW finding: the type-residency rule** (`RunId`/`RunStatus`/`PendingStatus` barred from event payloads, JPMS cycle; flatten-or-relocate = AMD-block decision); C8 = hard M7 entry-gate row. **Briefs (self-reviewed ×3):** `Research_14A_Automation_UX_Failure_Modes_Brief.md` (REC 141–155) / `Research_14B_Automation_Runtime_Robustness_Brief.md` (REC 156–170) / `Research_15_Ecosystem_Positioning_Scan_Brief.md` (REC 171–185) — all READY TO DISPATCH. Self-review caught a MODULE_CONTEXT permit-count defect (27→**30**, re-derived from source) — **corrected in core/automation/MODULE_CONTEXT.md (docs-only core change)**. **Charter skeleton:** `context/planning/2026-06-12_M7-M8-charter-skeleton.md` (3 M7.x rows; 5-row entry-gate incl. the M6.3-vs-M7 ordering call at W25/W26; 5 carry pins). Step-0: consumed session prompt archived; no rollover flip (pre-Jun-14). **Next: Nick veto-or-default + dispatch all 3.** |
| 2026-06-10 (follow-up) | PM (Cowork) | **Sha-reconciliation + WUCP hygiene + Research 13 scoping.** Preflight STALE (Checks 3/4 — the documented sha-pending state) → Step-0 reconciliation: M6.1a **`9035110`** (git log HEAD; hivemind closeout `40de3ec` confirmed committed) filled into coder-handoff/pm-handoff/snapshot/backlog/charter/W24/pointer; M6.1 gate entries CLOSED. Archival: cross-agent trail rotated (CURRENT POINTER only); M6.1 instruction archived (+EXECUTION RECORD; tombstone pending Nick `git rm`); pm-handoff pre-M5-window records → monthly archives. Docs-lane corrections ×4 applied (Doc 06 §3.2/§3.7, Doc 01 §4.4, cloud-scalability :56; Doc 15 untouched). Preflight re-run **PASS** (Check 9 evidence-based — VM down). **Research 13 brief authored** (config market-superiority gap analysis, REC-130+, disposition-table mandate, DOCS-Project dispatch; renumbered from the task brief's "Research 12" per Nick — append-only register). config MODULE_CONTEXT M6.1 status line brought current (tiny core docs commit). |
| 2026-06-10 EOD | PM (Cowork) | **M6.1 COMPLETE (whole).** M6.1a executed (Claude Code); gate FAILED 2× → PM source-diagnosed both (CoreSchema vs engine-default JSON schema; Rule 5 stale vs ratified AMD-71) → gate-fix round 1 → **GREEN (27 + 147 tasks)** → Phase 2 APPROVE (module-info = ruling exactly). R1/R2/R3 ACCEPT; doc carries routed; M6.1a committed **`9035110`** (sha reconciled at the same-day follow-up cleanup session). |
| 2026-06-10 | PM (Cowork) | **M6.1b WUCP Phase 2 → APPROVE** (source-verified vs AMD-66/67/70, not the report; config `module-info` byte-identical; gate GREEN 57+147 tasks, Nick). **M6.1a escalation UPHELD + RULED (Nick):** 5 third-party non-transitive `requires` APPROVED + 2 Gradle lines; DP-4 → `List` ctor; AMD-66/67/68/70/71 §7 ruling-corrections + AMD-66 §2.4 correction authored (docs repo). RULING ADDENDUM appended to the M6.1 instruction; M6.1a ready to issue. Commit messages handed over (3 repos). |
| 2026-06-09 | PM (Cowork) | **AMD-66..71 RATIFIED/DEFERRED — M6 ENTRY-GATE CLOSED (documents-only; Mode-1 governance mechanics).** Preflight PASS (all 11; baselines core `6c6dd33` / docs `aedff55` / hivemind `da9d2f4`; Check 9 mirrors clean; sandbox served a 0-byte review return + truncated AMDs → file tool authoritative throughout, NO sandbox git writes per the standing §1.1 hazard). Executed the Nick-authorized ratification: **AMD-66 RATIFIED (RATIFY-AS-IS, [AMD-66-A] ENDORSED); AMD-67/68/70/71 RATIFIED (RATIFY-WITH-EDITS — all 7 edits pre-folded at docs `aedff55`, nothing further folded); AMD-69 DEFERRED — Option (a) confirmed (Tier-2/OQ-15-3; number reserved; OQ-15-3 stays closed).** Mechanics: 6 mastheads + §9/§10 (§7/§8 for 69) flipped with the AMD-54 §10 pattern; **§37–§41 registered** (8 invariants verbatim from the AMD §7s; §37 preamble carries the block + deferral note) + §0.3 (+5 families) + §17 (+8 rows; **total REGENERATED from the table: 152 invariants/41 identifier categories** — the stated "135/34" had drifted −9, flagged) + §18 (+5 rows) same-commit; nav-index +6 rows/preamble/Doc 06 note, **watermark unchanged AMD-87**; M6.1 ⛔→**READY TO ISSUE** (numbers pinned); charter entry-gate CLOSED + AMD-68 half of M6.2 satisfied; backlog/snapshot/handoffs/W24/KB updated; §7 module-info embeds re-confirmed byte-identical vs `config/configuration` source at `6c6dd33`; E70-1 flattened payloads verified intact post-flip; both consumed instruction prompts archived. M6.1 NOT issued (next session). Untracked `coder.zip` + `project-manager.zip` in hivemind root flagged to Nick (sync residue — not deleted). |
| 2026-06-09 | PM (Cowork) | **AMD-66–71 DOCS review RETURNED → 7 edits FOLDED (documents-only).** Nick ran the AMD-66–71 DOCS-Project review → **RATIFY-WITH-EDITS, no rejects** (66 RATIFY-AS-IS, 67/68/70/71 RATIFY-WITH-EDITS, 69 CONFIRM-DEFERRAL). PM **independently source-verified the three load-bearing finds at `6c6dd33`** before folding (not from the summary): **E70-1 — REAL JPMS cycle** (config-event payloads specified with config-module types `Severity`/`ReloadResult`/`ReloadClassification` in event-resident records → `event→config`, the AMD-52 class — confirmed: `config requires transitive event`, event-model requires only `value`+`platform`; existing `ConfigChangedEvent` is all-String) → flattened to event-resident types + a type-residency rule; **E70-2** — `secret_added`/`secret_removed` don't exist in source (only `CONFIG_CHANGED`/`CONFIG_ERROR`) → corrected; **secrets.enc** — both Locked docs say `secrets.enc` (encrypted JSON), not `secrets.yaml.enc` → fixed in AMD-68/71. Folded all 7 (E67-1/2 prose-accuracy, E68-1, E70-1/2, E71-1/2 incl. [AMD-71-A]→Path-injection confirming M6.1 DP-3); flipped each amendment's frontmatter to its verdict; placed the review return on disk (`context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md`); updated pm-handoff/snapshot/pointer. Delivered the validity+scalability assessment to Nick. **Awaiting Nick's ratification** → then invariant registration + nav-index rows + M6.1 gate-lift. homesynapse-core untouched (HEAD `6c6dd33`); Locked Doc 15 inviolate. |
| 2026-06-08 | PM (Cowork) | **M6 entry-gate (config side) AUTHORED + B2 schema decisions (documents-only).** Preflight **PASS (all 11)** — source-verified vs repos; Check 3 noted HEAD `6c6dd33` = a 1-line `Main.java` print-string typo fix on `7f44bed` (`com.homesynapse.app`, whitelisted; immaterial — latest-commit field bumped); backlog/coder-handoff masthead-currency nits folded. Authored **AMD-66–71** (M6 config block, PROPOSED, in `homesynapse-core-docs/design/amendments/`): **66** `ConfigurationChangeListener` (REC-55, F7-corrected non-generic/non-sealed; PROCESS_RESTART no-listener default); **67** config-doc schema `(major,minor)` (REC-56 — **REC-41 blocker CLEARED by ratified AMD-54**, distinct-surface guard); **68** `SecretStore.setAll(Map)` atomic durable write (the Doc 06 currency amendment — Doc 15 §7.3 + AMD-60-INV-03; **REC-57 `SecureCredentialBundle`/`credentialsFor` RETIRED by ratified AMD-60** R7/A5); **69 DEFERRED to Tier-2/OQ-15-3 + ESCALATED** (REC-58 Argon2id/BouncyCastle conflicts with Locked Doc 15 §2.3/§3.5/§3.8 — MVP roots machine-local, zero new deps, provider open); **70** config observability events (`config.validation_completed`/`config.section_reloaded`, in `com.homesynapse.event`); **71** hybrid config dir layout (path-traversal guard). + the **AMD-66–71 DOCS-Project review prompt**; the **M6 multi-piece charter** (M6.1 config pipeline / M6.2 secret-store+key-mgmt / M6.3 at-rest GATED / M6.4 hot-reload — E2 `com.homesynapse.app` bridge + OR-M6-NONCE embedded verbatim); the **gated M6.1 coding instruction** (⛔ on AMD-66/67/70/71 ratification; verbatim config module-info; P2 consumer/pin survey; §4c Clock reminder); the **B2 schema decisions C8 `actorRef` semantics + C9 energy event shape** (contract-freeze-ready, independent-review-bound). homesynapse-core untouched (HEAD `6c6dd33`). Commit messages handed to Nick. |
| 2026-06-07 | PM (Cowork) | **M5-A COMMITTED `8028337` (Step 0 sha-reconciliation) + M5-window review source-verification → STOP-on-missing-input.** Preflight **STALE** (Check 1 footer/session-log lag, Check 3 snapshot `Latest commit` behind HEAD, Check 4 backlog masthead, Check 8 pointer) → Step 0 retroactive WUCP Phase 2 sha-reconciliation: `8ef9e9f`/"commit pending" → committed **`8028337`** across snapshot/pm-handoff/coder-handoff gate-flip/backlog/W24 masthead/cross-agent pointer → **PASS restored**. Source-verified (Read tool, HEAD `8028337`) ALL foldable edits ahead of the fold: **Doc 15 E1** — re-derived Root→KEK→DEK from §3.1/§3.4/§4.2; the `encrypted_kek`→`encrypted_dek` rename has **THREE** occurrences (§4.1 L186 name+comment, **§4.2 L204 — NOT in the on-disk E1 summaries**, §8.2 L289 `ScopeKey.encryptedKek`) and the §4.1 comment is a *semantic* fix ("scope KEK wrapped under the root key"→"scope DEK wrapped under the scope KEK"); **Doc 15 E2** — real graph confirms config↔persistence have no edge either way at HEAD; PM chooses the **composition-root adapter bridge** (neither module references the other; robust to a future persistence→config need) over the one-way edge; **AMD-87 E1/E2/E3** all confirmed vs source (§7 "65"; HEAD annotation literally `@Disabled("AMD-65 pending")`; INV-GA-02 governs INV-XX-NN only); **Part 2** re-verified vs current tree — persistence→device acyclic, 4 permit shapes match `Expectation.java` exactly, @Disabled present. **STOPPED twice on the missing full review return** (not in message / not on disk) rather than work from the summary. **THEN (same session) Nick placed the verbatim review on disk** (`2026-06-06_Doc15_AMD-86_DOCS_Review_Return.md`); reconciled — verdict RATIFY-WITH-EDITS, **AMD-86 reviewed clean (J4)**, J1/F-A + J5 Doc06/AMD-60 + m1 PASS, m2 `[BLOCKING-for-M6-impl]`; **one J6 refinement confirmed against source: the E2 composition-root is `com.homesynapse.app`** (already requires both config+persistence → zero new edge), sharper than Doc 15's `HomeSynapseCore` (lifecycle, which lacks the config edge). **Folded + ratified + Locked + un-gated:** Doc 15 E1 (all 3 occurrences + semantic comment) + E2 + **Lock**; **AMD-86 + AMD-87 RATIFIED**; INV-PD-07 narrowed + INV-PD-03 posture; AMD-86-INV-01/AMD-87-INV-01 (§35/§36; 135/34); nav-index rows + **watermark 64→87**; m2 → **OR-M6-NONCE**; **M5-A Part 2 UN-GATED** (Claude Code). HEAD `8028337`; on-disk watermark **AMD-87**. |
| 2026-06-06 | PM (Cowork) | **POST-M4 SYNTHESIS + M5-WINDOW CHARTERED (documents-only; HEAD `8ef9e9f` unchanged).** Preflight **PASS (all 10)**. Wrote `context/planning/2026-06-06_post-M4-plan-and-M5-scoping.md` — decision-support built on the M4 retro + the 2026-06-06 foundation-readiness + language-replatform assessments + a source-verified deep read of the candidate-next surface. Key reframing: the planned M5 (Platform API + test-support) is **thinner than it looked** — `TestClock`/`SynchronousEventBus`/`NoRealIoExtension`/`InMemoryEventStore` already shipped in M1.x — so the leverage is the design/decision work that is regret-proof or gates M6. Nick decided **D1–D8** → adopt the **blended, lane-tracked M5 window** (`context/decisions/2026-06-06_post-M4_M5-window_decisions.md`): M5-A small Core, M5-B the M6 entry-gate + regret-proof schema decisions, M5-C protected website/docs (P6), M5-D evidence (GraalVM/GenZGC spikes + energy interviews). **D2 (diverges):** operational crypto-shredding → POST-MVP; build key-management infrastructure/seams at MVP; reconcile via a minimal **INV-PD-07 amendment** (full DOCS review). PM endorsed D2 + added the **write-path-encryption boundary** refinement (the regret-proof line the crypto owner-doc must pin). W24 charter written; backlog M5-window rows added; preflight re-run PASS. **Next: M5-B/B1 — the M6 entry-gate.** |
| 2026-06-05 | PM (Cowork) | **M4 RETROSPECTIVE + NEXT-PIECE RECOMMENDATION (documents-only).** Preflight **STALE** (partial M4.C closeout: missing session-log row, coder-handoff M4.C gate still OPEN, W23 footer at `e73e199`, unarchived note trail) → retroactive WUCP Phase 2 reconciliation → PASS. Wrote `context/audits/2026-06-05_M4-retrospective.md` (critical: milestone sizing, the instruction-scope-miss pattern + consumer/pin-survey fix P2, deferred-gate shift-left, the partial-closeout finding, amendment-rigor lightweight track, debt register, strategic/velocity, keep-doing) + `context/planning/2026-06-05_next-piece-recommendation.md` (recommend M5 + protected W3 website/docs lane; M6 next; AMD-65 in M5 window). Flagged 3 uncommitted build-breaking working-tree files to Nick (not touched). Debt + P1–P6 logged. HEAD `8ef9e9f` unchanged. |
| 2026-06-05 | Coder (Claude Code) + Nick + PM (Cowork) | **M4.C (AMD-54..64 integration-api freeze) COMPLETE → WORKSTREAM C COMPLETE → M4 COMPLETE. COMMITTED `8ef9e9f`.** Coder executed M4.C test-first; Nick's gate aborted round 1 at `core:event-model` (`EventTypesTest` count+regex) — a downstream-lockstep miss (instruction listed the producer, not the 7 consumer/pin sites incl. the production composition root `HomeSynapseCore` — a latent-M9 `encode()` bug no test pinned). Coder fixed all 7; **re-run GREEN (`./gradlew check --continue`, 145 tasks).** PM WUCP Phase 2 → APPROVE; 2 `[REVIEW]` ACCEPTED. integration-api 22→40 types; watermark AMD-64; NO `module-info`/Gradle/`projectionVersion` change. **AMD-65 (Expectation codec) deferred BLOCKING-for-M9.** _(Row added 2026-06-05 in retroactive reconciliation — the M4.C-complete closeout originally landed without this session-log row.)_ |
| 2026-06-05 | PM (Cowork) | **Workstream C BRIEFED (documents-only; HEAD `e76b925` unchanged; watermark stays AMD-53).** Preflight PASS (micro-fixes: pm-handoff `BS2SHA`→`e76b925`; backlog header re-anchor). **Integration AMD block AUTHORED: AMD-54..64, eleven files, PROPOSED** — REC-41 split as 54 (config-schema versioning, NQ-2) + 55 (hooks); 56 AUTH_FAILED; 57 HealthDetail; 58 five lifecycle permits (dot-namespaced, three-way lockstep); 59 capability events + CapabilityPublisher + DiscoveryServices (NQ-3/4); 60 SecurityServices/CredentialRotator (NQ-1); 61 softDependencies; 62 BackoffParameters + NQ-5/6 fold; 63 IsolationLevel (F-C correction); 64 plannedRestartTimeout. Frozen vs the shipped 12-/4-component shapes; descriptor 8→14, context 10→12, RequiredService 3→5; **zero `module-info` changes block-wide**. Review-flags R1–R7: R2/R4/R5 fidelity diffs run against the Research 6 return **pasted INLINE by Nick at dispatch** (not in DOCS Project knowledge; missing/truncated ⇒ INCOMPLETE, never reconstructed); **R6/R7 PRE-CO-SIGNED by Nick same day** (deliberation done; formal co-sign at ratification). **DOCS review prompt READY** (`context/instructions/2026-06-05_AMD-54-64_DOCS_Project_Review_Prompt.md`); Doc-05 masthead currency note (Gate B masthead-discharged); **gated M4.C instruction drafted** (⛔ issue-gated on ratification — holds as authored per Nick directive); backlog M4.C row (PLANNED — GATED). Core committed + pushed `e76b925` (Nick-confirmed). Next: Nick dispatches DOCS review (+ inline return) → ratify → mechanics (invariants §24+, watermark→64, Doc-05 body-fold, Doc 02 Stage-2 fold, lift ⛔) → issue M4.C → **M4 COMPLETE**. |
| 2026-06-05 | PM (Cowork) + Coder (Claude Code) | **M4.B-S2 (AMD-44 Stage 2 — EntityRole) COMPLETE + COMMITTED `e76b925` + WUCP Phase 2 → APPROVE. WORKSTREAM B COMPLETE.** EntityRole enum + EntityType 6×3 matrix + Entity 11→12 / ProposedEntity 3→4 (coerce→guard→copy; convenience ctors — zero caller breakage). Build GREEN (26+38+143, clean build). Same day: **Research 7 v2 assessed A− + NQ-1..7 RATIFIED (https problem URIs); Research 12 assessed A + SPIKE-DC scheduled (~Aug)**. |
| 2026-06-04 | PM (Cowork) | **W23 "Mon+Tue" batch.** Preflight STALE (session-log lag + unarchived notes + backlog header) → reconciled → PASS. **M4.B-S2 (AMD-44 Stage 2 — EntityRole) coding instruction AUTHORED + ISSUED** (`context/coding-instructions/M4.B-S2_AMD44_EntityRole.md`, source-verified vs `e73e199`; DP-1..7 settled; TypedIdTest FloorId carry-item bundled; no `module-info`/`projectionVersion` change). **Research 6 NQ-1..6 RESOLVED — Nick adopted all six PM leans;** OQ-05-05 RESOLVED; Workstream C Gate A CLEARED (NQ-3/4 finalize at B-S2 landing); Doc-05 refresh queued for C-briefing. Documents-only; HEAD unchanged `e73e199`. Next: Coder executes M4.B-S2 → PM Phase 2 → C briefing. |
| 2026-05-31 | PM (Cowork) + Coder (Claude Code) | **M4.B-S1 (AMD-44 Stage 1) COMPLETE + COMMITTED `e73e199` + WUCP Phase 2 → APPROVE.** `FloorId` typed-ULID wrapper; `Floor`/`Area` records + `FloorRegistry`/`AreaRegistry` interfaces (no impl); `AreaId` Javadoc de-conflated; `hardwareIdentifiers` `List`→`Set` on `Device`/`ProposedDevice`/`DiscoveryPipeline` with defensive-copy compact ctors. No `module-info`/Gradle/`projectionVersion` change. Build GREEN (143 tasks). Stage-2 fence held. 3 `[INFO]` deviations accepted. At closeout: W23 weekly plan + release-runway roadmap (posture A — interleave non-Core) issued; Workstream-C gate-status note (Research 6 NQ-1..6 + PM leans) produced. Next: M4.B-S2 (EntityRole). |
| 2026-05-31 | PM (Cowork) + Coder (Claude Code) | **M4.0b-5 (AMD-53 timestamp-model unifier) COMPLETE + COMMITTED `c99b425` + WUCP Phase 2 → APPROVE.** All three `EntityState` activity timestamps event-time-sourced (`eventTime ?? ingestTime`) in every `applyToState` branch + adoption seeding; `staleAfter`/`stale` carve-out intact (AMD-53-INV-02); `projectionVersion` 4→5; watermark AMD-53. Build GREEN. **Workstream A COMPLETE — typed end-to-end AND event-time-deterministic.** |
| 2026-05-31 | PM (Cowork) + Coder (Claude Code) | **M4.0b-4a COMMITTED `971cfa1` + M4.0b-4b COMMITTED `72596cb` + WUCP Phase 2 → APPROVE (both).** 4a: `AttributeValue` hierarchy relocated to new `com.homesynapse.value` leaf module (JPMS event↔device cycle broken; AMD-52 §11 erratum; behavior-preserving). 4b: AMD-52 typed `StateChangedEvent` payload + `{"t","v"[,"u"]}` codec + schema-versioned replay (Path-B `DegradedEvent` gate) + typed `CheckpointSerializer` envelope; `projectionVersion` 3→4; new ArchUnit Rule 10 `NO_JACKSON_IN_DOMAIN_MODEL` ([REVIEW] ACCEPT). |
| 2026-05-31 | PM (Cowork) | **Research 11 assessed (A−) + AMD-52 AUTHORED + RATIFIED** (watermark AMD-51→AMD-52; AMD-52-INV-01..07 registered §22); later same day **AMD-53 AUTHORED + RATIFIED** (watermark AMD-52→AMD-53; AMD-53-INV-01/02 registered §23) + M4.B-S1 coding instruction authored in parallel. |
| 2026-05-30 | PM (Cowork) + Coder (Claude Code) | **M4.0b-3 COMPLETE + committed `98f705b` + WUCP Phase 2.** AMD-51 typed `AttributeValue` change-detection comparator. Claude Code implemented the external `AttributeValueComparator` (pkg-private `StructuralAttributeValueComparator`, exhaustive **no-`default`** 8-arm switch, full IEEE-754 totality incl. `Inf−Inf` handled pre-arithmetic, total-form `1e-9` epsilon, order-sensitive array deep-compare, HA-mirrored Degraded) + `ComparisonPolicy` + `AttributeSchemaResolver` + `AttributeValueReconstructor` (symmetric schema-driven both-sides parse) in state-store, plus `StandardCapabilities` in device-model **main** (DP-K — immutable injected snapshot, `TestCapabilityFactory` delegates). `ProductionDerivationRule` rewritten typed (reconstruct-both-sides → `comparator.changed`); **String `StateChangedEvent` payload preserved** (AMD-52 staged); `projectionVersion` **2→3**; no `module-info`/Gradle/`CheckpointSerializer` change. **A fresh Cowork session independently reviewed every changed `.java` file against AMD-51 source (not the report) → APPROVE** — confirmed the symmetric prior-`StringValue` reconstruction (INV-05 trap), DP-K determinism (AMD-50-INV-03), no FLOAT-as-QUANTITY mistyping, no stray `default`, no direct time access. Nick ran the full gate GREEN (139 tasks + the three module `:check`s) and committed. Deviations D-1/D-2/D-3 all `[REVIEW]` ACCEPT; **D-1 (missing-schema → `StringValue` string-compare fallback, not §2.6 Degraded) → recommend an AMD-51 §2.6 erratum** so spec matches code. AMD-51-INV-01..05 upheld. **Workstream A COMPLETE.** Next: the M4 next-part scope/plan conversation. |
| 2026-05-30 | PM (Cowork) | **AMD-51 AUTHORED + RATIFIED + M4.B3 committed `60b4185`.** Typed-comparator amendment authored from Research 10 v2 + the four ratified calls, source-verified; external review RATIFY-AS-IS; symmetric-reconstruction clarifying edit folded; epsilon locked `1e-9`; AMD-51-INV-01..05 registered; watermark AMD-50→AMD-51. M4.B3 (AMD-47 AttributeValue expansion) committed + WUCP Phase 2 closed earlier in the session-chain. |
| 2026-05-29 | PM (Cowork) + Coder (Claude Code) | **M4.0b-2 COMPLETE + committed `7610296` + WUCP Phase 2.** Between M4.0b-1 and this WU, PM authored + ratified **AMD-50** (general N→M version-transition backfill / cursor-as-log-position / supersession / clock removal) and produced the M4.0b-2 coding instruction. Coder shipped AMD-50 for the 1→2 transition: `projectionVersion` 1→2 trigger; `backfillActive` provenance gate; non-emitting one-shot backfill on **both** `onEvent` and `processBatch` (D-1 lesson) via the narrow `applyBackfillAttribute` (attributes + event-time `lastChanged`, no second `stateVersion` increment, INV-01); gate-conditional supersession in `applyToState`; `Clock` removed from `DerivationContext` (§2.4). 7 files modified (state-store + one-line lifecycle bump), 0 created, no `module-info`/Gradle changes. **PM Phase 2 read every changed file vs source and confirmed targets A–J** (gate-both-paths, event-time `lastChanged` not wall-clock, genuine supersession test, INV-01 no-double-increment, one-shot, `projectionVersion=2`, clock removal, INV-04 audit). Deviations D-A/B/C/D (`[REVIEW]`/`[INFO]`) all adjudicated ACCEPT — incl. D-A (processBatch backfill applied in-callback, not post-advance — evidence-based, correct) and D-B (conscious interim mixed-`lastChanged`). Build GREEN (139 tasks) + `:core:state-store:check` + `:lifecycle:lifecycle:check`; Nick committed `7610296`. **AMD-50-INV-01..04 upheld.** One non-blocking INFO surfaced for a future hardening test: the gate's LIVE-safety relies on `onCaughtUp` firing before the first LIVE delivery (the established bus contract). Twentieth CC WU. Next: M4.B3 → M4.0b-3. |
| 2026-05-29 | PM (Cowork) + Coder (Claude Code) | **M4.0b-1 COMPLETE + committed `cf1a97e` + WUCP Phase 2 + P2 RATIFIED.** PM deliberated M4.0b's scope (Phase 1): found M4.0b-whole requires a new AMD-41 §3.2.4 amendment (P2-blocked), so issued the **narrowed amendment-free M4.0b-1** (REC-28 `DispatchingProjectionAdvancer` + production string `DerivationRule` publishing `state_changed`, no version bump/backfill) per Nick's sign-off. Coder shipped it (3 prod + 4 test files created, 5 modified, `MinimalProjectionAdvancer` deleted); PM Phase 2 review read every file + re-verified all 12 STOP-gates and confirmed the REPLAY-no-publish test hits the real `onEvent` REPLAY path (via `ReplayDriver`→`supervisor.deliver`→`onEvent`) — not a D-1-style false pass. Build GREEN; Nick committed `cf1a97e`. **OR-M3-17/18 fully closed.** **P2 ratified** (rev. 2, after Nick's review fixed the M4.0b-2 dependency trap + reframed the withdrawn/reuse model + moved integration to assign-at-milestone): device 46–49, projection 50–52 fixed; **AMD-50 is the next amendment to author** and the M4.0b-2 unblock. M4.0b-2 re-scoped to backfill+version-bump on string change-detect; typed work → new M4.0b-3. Nineteenth CC WU. |
| 2026-05-29 | PM (Cowork) + Coder (Claude Code) | **M4.0a COMPLETE + WUCP Phase 2.** AMD-45 `:75` phantom corrected (`MinimalDerivationRule` → the no-op `MINIMAL_DERIVATION_RULE` lambda) and ratified (Status RATIFIED, applied 2026-05-29). Issued the M4.0a coding instruction (atomic checkpoint coupling + reconciliation plumbing, 8 deliverables); Claude Code implemented all 8. **PM review caught D-1:** the REPLAY-path subscriber-checkpoint writes (`ReplayDriver:157/:186`) were left ungated, reopening AMD-45-INV-01's crash window on REPLAY — adjudicated as a real gap (the projection checkpoints during REPLAY too, so the uncoupled bus write races the coupled sink); correction gated both REPLAY writes + added a 201-event threshold-crossing regression test in `EventBusContractTest` Tier 9. Build GREEN at `a441fdf`. Resolved: OR-M3-13, H2, REC-80, REC-82; AMD-45 implemented. **Mount-staleness lesson:** during review the bash sandbox served truncated views of recently externally-edited files; the file tool (Windows path) + Nick's clean `./gradlew check` are authoritative, not sandbox `git`. Next: **M4.0b** (production `DerivationRule` + `DispatchingProjectionAdvancer` + 1→2 backfill). |
| 2026-05-28 | Cowork | **M4 scoping + KB de-poison.** Authored `PLAN-M4-CONSOLIDATED` (canonical scope: device-model expansion + projection/derivation foundation + integration-api interface freeze; config=M6, automation=M7/8, integration-runtime impl=M9). Decisions locked: canonical scope; one-shot backfill for M4.0b's `projectionVersion` 1→2 reconciliation replay. Wrote Research 9 (projection rebuild/backfill, REC-76+) + Research 10 (typed change-detection, REC-90+) briefs and an independent plan-verification Cowork prompt. **KB de-poison:** removed the phantom `MinimalDerivationRule` class (it's the no-op `MINIMAL_DERIVATION_RULE` lambda + the real `MinimalProjectionAdvancer`/`NotifyingEventPublisher` classes) from Current_State + Knowledge_Primer; corrected test count `~1,600/~1,594/~1,575`→**1,422** and files→**724** (source-verified); AMD-44 `APPLIED`→`RATIFIED (pending implementation)`. Open: P2 (AMD renumbering), P3 (Research 6 NQ-1..6). No code changes. |
| 2026-05-22 | PM (Cowork) | **Research 7 v1 PM Assessment produced.** Research 7 (REST and WebSocket API Design for Event-Sourced Smart Home Systems) landed from the Claude Project after the M3.7 coding instruction was already issued in a parallel Cowork session (no conflict — M3.7 is M3-scoped and independent of M10/M11 amendments). Steps A–F protocol executed; assessment at `context/assessments/2026-05-22_Research_7_PM_Assessment.md` (184 lines, 37 KB). **Grade C- — significant regression from Research 5 v2 (A-) and Research 6 v1 (B+).** §1-§3 substantive analysis is the strongest of the batch in some respects (bcrypt timing analysis with JMH citation + Cortex-A72 extrapolation; three-stage backpressure as genuine differentiation defended against 7 peers; INV-SE-02 §5.4 disclosure pattern as exemplary), but **§7 fabrications are the WORST in any research document — 15 source-verified issues** including a CRITICAL JPMS `module-info.java` fabrication that ignores every verbatim block embedded in the brief (F12). Other notable fabrications: F2 (REC-63 duplicates existing Doc 09 §4.3-§4.5 command surface with `InvokeRequest`/`CommandReceipt`/`CommandDispatcher` — REJECT per disposition); F8 (5 new WsCloseCode collisions with existing codes); F9 (`PingMsg`/`PongMsg` framed as new — both already exist); F11 (`EntityStateChanged` event fabricated — actual is `state_changed`); F7 (`WsErrorMsg` referenced — actual is `ErrorMsg`); F5/F3 (`Capability` enum + `@Capability` annotation clash with device-model sealed `Capability` interface); F10 (`ConcurrentHashMap<byte[32], ...>` mechanically broken). **Disposition: 6 ACCEPT, 7 ACCEPT+MODIFY, 1 REJECT.** 14 AMDs allocated (AMD-72..AMD-85) — REC-63 collapses to 0 AMDs per PM-recommended NQ-1 resolution. 7 NQs surfaced for Nick (REC-63 vs Doc 09 conflict; Capability naming; ProblemType URL scheme; webhook DLQ separation; WsCloseCode renumbering; coalescing key; annotation naming). Cross-research coherence ALL ALIGNED. **Meta-lesson: brief discipline is necessary but not sufficient — the Research 7 brief embedded every counter-measure from Research 5 v2 and the researcher still produced 15 §7 fabrications, recurring the Research 6 v1 JPMS-name failure mode at greater scale.** Recommendations: require future briefs to ask the researcher to QUOTE verbatim blocks back in §7 (so fabrication produces visible diff); consider splitting multi-module research items per module. **Research pipeline now COMPLETE for pre-M5 window** — Research 7 was the last MEDIUM-priority item. Final preflight PASS. |
| 2026-05-22 | PM (Cowork) | **M3.7 coding instruction produced.** Freshness preflight PASS on all 10 checks. Mode 3 (Director) session — final M3 milestone scoping. Four scoping decisions made and logged as D-09..D-12 in `context/decisions/phase-3-cross-module-decisions.md`: **D-09** placeholder strategy = `MinimalProjectionAdvancer` (per Research 3 REC-20 + Research 8 v2 §M3.7 Impact; closes OR-M3-17/18; `DispatchingProjectionAdvancer` per Research 8 REC-28 stays M4.0 scope); **D-10** absorb HTTP IT for M3.6e.2 endpoints AND DLQ `oldestParkedAt` field plumbing into M3.7 (no M3.6f split); **D-11** M3.7 proceeds in parallel with M4-amendment deliberation window (zero technical coupling — pm-handoff Next Tasks #6 gate removed); **D-12** established M3.6 Claude Code workflow (acceptEdits, Opus 4.7 xhigh, deny gradlew/commit/push). Two REC reinterpretations: REC-13 (`bus.isLive()` → `harness.mode()` polling via `HomeSynapseCore.mode()` — bus-level liveness is not meaningful), REC-15 (`DeploymentProfile.testing() + boundHttpPort()` → 4th `TESTING` enum value on `DeploymentProfile`, `httpPort: int` added to `HomeSynapseConfig` 2→3 fields + `HomeSynapseConfig.testing()` factory, `boundHttpPort() → int` on `HomeSynapseCore` reading `javalinApp.port()`). Coding instruction at `context/instructions/M3.7_E2E_Integration_Tests.md` (~14 files: ~5 created + ~9 modified). 11 STOP-on-Mismatch gates (G1–G11). Awaitility 4.3.1 + json-unit-assertj 3.5.0 to be added to `gradle/libs.versions.toml`. No code changes this session. Final preflight: PASS. Next: Nick pastes the instruction into Claude Code. |
| 2026-05-22 | PM (Cowork) | **Research 5 v2 FINAL — post Nick review.** Nick reviewed the v1 assessment and surfaced 5 corrections, three of which the PM missed entirely and two of which the PM caught the symptom of but didn't trace to the canonical source. v2 addendum landed at the bottom of `context/assessments/2026-05-22_Research_5_PM_Assessment.md` (v1 body preserved for traceability). **v2 corrections:** F1 (LTD-09 already locks the library *choice*, not just the version — REC-53/REC-54 are confirming locked decisions, not proposing new adoptions); F2 (Jackson 2.x lock is by LTD-08, citation not PM recommendation); F6 (`.internal` sub-package fabrication missed — violates one-flat-package invariant); F7 (broken generic — `<S extends ConfigSection>` is mechanically unsound, records are implicitly final); F8 (INV-CE-01 already canonically answers §Q7, framing as "research finding" was wrong). **v2 disposition:** REC-53/54 CONFIRMED (no adoption AMD needed; Phase 3 wiring AMDs TBD); REC-55 ACCEPTED IN PRINCIPLE with corrected non-generic shape; REC-56 DEFERRED to Nick's Research 6 REC-41 decision; REC-57 ACCEPTED with `credentialsFor` corrected to `SecretStore` (not `ConfigurationAccess` — separate concerns); REC-58 ACCEPTED (threat-model statement publication-ready); REC-59 ACCEPTED (payload contingent on REC-56); REC-60 ACCEPTED IN PRINCIPLE, AMD-71 deferred to M6; REC-61 MERGED into REC-59. **AMD allocation:** AMD-64/65 RETIRED (already-locked libraries); AMD-66/68/69/70 ACTIVE (4 ratifiable); AMD-67 DEFERRED on REC-41; AMD-71 DEFERRED to M6. **All 6 NQs resolved** by Nick's review (LTD-08, LTD-09, INV-CE-01 lookups closed Q1/Q2/Q7; F7 forced re-shape closed Q3; SecretStore placement closed Q4; AMD-52 precedent closed Q5; spike sequencing closed Q6). Five PM lessons internalized for `pm-lessons.md`: check Locked Decisions register alongside `libs.versions.toml`; check Architecture Invariants register before treating design questions as open; one-flat-package invariant on explicit §7 checklist; Java mechanics review on every proposed type shape; cross-interface convenience-method placement deserves its own design beat. |
| 2026-05-22 | PM (Cowork) | **Research 5 v1 PM Assessment produced.** Research 5 (Configuration System Patterns for Constrained IoT Runtimes) landed mid-session from the Claude Project. Steps A–F protocol executed against the verified MODULE_CONTEXT + verbatim `module-info.java` + `gradle/libs.versions.toml` (HEAD `76288af`) + Doc 06 §§3.3/3.4/3.6/3.7/8.4/8.5/8.6 + Research 4 v3 / Research 6 v1 / Research 8 v2 cross-research coherence checks. Assessment: `context/assessments/2026-05-22_Research_5_PM_Assessment.md`. **Grade A- — cleanest research document of the 6-doc batch.** **5 ACCEPT / 4 ACCEPT+MODIFY / 0 REJECT across REC-53..REC-61** (9 RECs total). **8 AMDs proposed: AMD-64..AMD-71** (subject to per-REC modifications). **§7 source-verified issues: 5 (F1-F5) — none are fabrications** in the Research 6 v1 sense; F1/F2 are `libs.versions.toml` version-state misses (`snakeyaml-engine` 2.9 + `json-schema-validator` 1.5.6 already integrated — recommend staying on 1.5.6 for Jackson 2.x compatibility, AMD-65 re-scoped); F3 is implicit-not-explicit `ConfigurationAccess.credentialsFor(String)` interface extension (AMD-68 needs explicit coverage); F4 is event-package omission resolved by AMD-52 precedent (`com.homesynapse.event` flat); F5 is self-flagged SnakeYAML Engine module name inference. NQ-1..NQ-6 surfaced for Nick (Jackson 2→3 sequencing, BouncyCastle commitment, sealed-marker pattern, `ConfigurationAccess` extension scope, event-package confirmation, spike sequencing). **Cross-research coherence: ALL ALIGNED** — REC-57/R6-REC-45/NQ-1 (`SecureCredentialBundle` ↔ `SecurityServices` aggregator); REC-56/R6-REC-41/NQ-2 (schema versioning `(major, minor)`); REC-55 listener ↔ `ConfigChangeSet.ReloadClassification`. **Lesson encoded:** brief must embed verbatim `libs.versions.toml` rows alongside `module-info.java` to eliminate the version-state-miss fabrication class — recommend adding to research-agenda.md §2 CONSTRAINTS. M6 implementation order (10 steps) documented in assessment. Research 7 brief is the next research-agenda item. |
| 2026-05-22 | PM (Cowork) | **Hivemind hygiene sweep + Research 5 processing prep.** Freshness preflight PASS on all 10 checks (Check 9 unexpectedly PASS — Nick's external mirror sync already complete from the prior PM session; deviation flagged to user, forward work proceeded). Hygiene sweep — **6 files archived**, 0 deleted: 4 consumed instructions (`Research_3_Prompt.md`, `Cowork_Research_Pipeline_Bootstrap.md`, `Cowork_Research8_Processing_Bootstrap.md`, `M3.6e.2_Admin_Endpoints_ArchUnit_Rules.md`) → new `context/instructions/archive/`; 1 consumed pre-verification (`WU-M3.6d-b.md`) → new `context/pre-verifications/archive/`; 1 consumed continuation prompt (`2026-05-22_continuation_prompt.md`) → existing `context/handoff/archive/`. **KEPT** as load-bearing: Research_4/6/8 briefs (exemplars; Research 5 cites Research 8 as the v1 template; Research 4/6 NQs/DQs still open), `pre-verifications/README.md` (documents the convention), current session's prompt at `context/handoff/2026-05-22_cowork_cleanup_and_research5_prep_prompt.md` (active this session). Research 5 processing prep — read `config/configuration/MODULE_CONTEXT.md` (22-type inventory verbatim), `config/configuration/src/main/java/module-info.java` (19 lines: `module com.homesynapse.config { requires transitive com.homesynapse.event; exports com.homesynapse.config; }`), Doc 06 §§3.3/3.4/3.6/3.7/8.4/8.5/8.6. 9 verification points + 5 deep-probe patterns + cross-research coherence checks (Research 6 REC-41/NQ-2 schema versioning, REC-45/NQ-1 SecurityServices aggregator, `ConfigChangeSet.ReloadClassification` pairing with proposed `ConfigurationChangeListener`) ready for Research 5 v1 processing. **Research 5 doc did NOT arrive this session** — Phase 4 (Steps A–F per Research 8 v2 template, RECs starting REC-53, AMDs starting AMD-64) queued in pm-handoff Next Tasks for next session. No new ORs. No code changes. Final preflight: PASS. |
| 2026-05-22 | PM (Cowork) | **Research 6 assessment + module-info.java rule encoding.** Research 6 v1 PM assessment produced (`context/assessments/2026-05-22_Research_6_PM_Assessment.md`). 8 ACCEPT / 3 MODIFY+ACCEPT / 1 REJECT across REC-41..52. 10 AMDs ratified (AMD-53..58, 60, 62, 63), AMD-61 withdrawn (REC-49 duplicated existing `HealthParameters.maxRestarts/restartWindow`). 8 source-verified §7 fabrications catalogued (F1-F8) — all JPMS module-name errors (`com.homesynapse.event.model`, `state.store`, `configuration`, `integration.api`, `bootstrap`), same pattern as Research 4/8 type-name fabrications but at the module layer. NQ-1..6 open for Nick. **Lesson encoded in three hivemind locations:** `project-manager/SKILL.md` Step 3, `project-manager/references/coding-instruction-format.md` Files-to-Read, `context/planning/research-agenda.md` §2 CONSTRAINTS — verbatim `module-info.java` text must be embedded in every coding instruction + research brief. **Drift sweep:** strategic-context-map.md + master-release-plan.md milestone state advanced from M3.6d-a (stale) to M3.6 COMPLETE; file count refreshed ~476 → ~721+. |
| 2026-05-22 | PM (Cowork) | **Research pipeline continuation session.** Freshness preflight STALE on Checks 6 + 8 → reconciled (coder-handoff Next Work Unit set to M3.7; 2026-05-19 cross-agent-note archived). Research 4 v3 produced: FQ-2/4/8/9 + DQ-4 PM-verified against `core/automation/MODULE_CONTEXT.md`; researcher's MODULE_CONTEXT claims confirmed near-100% accurate. Knowledge Primer corrected — "+ subpackages" annotation removed from automation, device-model, integration-zigbee module entries (source-verified flat per MODULE_CONTEXT line 1 + 54-file source tree). Research 6 brief produced (`context/instructions/Research_6_Integration_Runtime_Brief.md`) — Research 6 (Integration Runtime) sequenced next because it constrains M4 IntegrationContext design the same way Research 4 constrained M4 device-model. DQ-1/2/3/5 remain open (strategic scope calls, not source-verification). |
| 2026-05-22 | PM (Cowork) | **WUCP Phase 2 — M3.6e.2 closure.** Closeout for M3.6e.2 (`76288af`). M3.6 COMPLETE. OR-M3-17 (NO_OP_DERIVATION) + OR-M3-18 (NO_OP_ADVANCER) logged as open. Seventeen CC WUs total. All hivemind artifacts updated. |
| 2026-05-22 | Coder (Claude Code) | **M3.6e.2 — Admin Endpoints + ArchUnit Rules.** 21 files (15 created, 6 modified). ListEntitiesEndpoint, GetEntityEndpoint, GetEntityStateEndpoint, DlqStatusEndpoint, ProjectionStatusEndpoint (all package-private). EndpointContext SPI + JavalinEndpointContext adapter + EndpointResponses utility. RestFilters gains addEntityEndpoints + installAdminEndpoints. 2 new ArchUnit rules (QUERY_SERVICE_READ_ONLY, REST_ENDPOINTS_NO_EVENT_PUBLISHING). HomeSynapseCore 16-step bootstrap. 5 deviations (none blocking). 19 new test methods. Build GREEN. Seventeenth CC WU. M3.6 COMPLETE. `76288af`. |
| 2026-05-22 | Coder (Claude Code) | **M3.6e.1 — MaterializedStateQueryService + REST Readiness Gate + Javalin Bootstrap.** 19 files (6 created, 13 modified). MaterializedStateQueryService (package-private, state-store), StateQueryService.materialized() static factory (DEC-M3-16), ReadinessFilter (package-private, rest-api), RestFilters public utility (DEC-M3-16 gateway), DeploymentProfile javalinMinThreads/javalinMaxThreads, HomeSynapseCore 14-step bootstrap with Javalin on port 7070. Two follow-up fix rounds: (1) Xlint:exports — ReadinessFilter demoted to package-private, RestFilters utility created; (2) Gradle/JPMS scope — rest-api build.gradle.kts `implementation` → `api` for state-store. 7 deviations (none blocking). 22 new test methods (MaterializedStateQueryServiceTest 10, ReadinessFilterTest 8, DeploymentProfileTest 4). Build GREEN: 139 tasks, 0 failures. Sixteenth CC WU. `b71ed37`. |
| 2026-05-22 | PM (Cowork) | **WUCP Phase 2 — M3.6e.1 closure + M3.6e.2 coding instruction.** Closeout for M3.6e.1 (`b71ed37`). Build gate RESOLVED (GREEN, 139 tasks). Three build-fix rounds documented. New lessons: Xlint:exports gateway pattern (Object-typed parameter erases framework dependency from public API), Gradle/JPMS scope alignment (requires transitive ↔ api). M3.6e.2 coding instruction produced. Next: M3.6e.2 execution. |
| 2026-05-22 | PM (Cowork) | **WUCP Phase 2 — M3.6d-b closure.** Closeout for M3.6d-b (`dfb045e`). OR-M3-14 RESOLVED. OQ-05-03 RESOLVED. Tracked gaps #7–10 CLOSED. Next: M3.6e.1 coding instruction. |
| 2026-05-21 | Coder (Claude Code) | **M3.6d-b — PersistenceFactory + HomeSynapseCore Composition Root.** 4-commit cohort: `a33ee40` (WriteCoordinator.queueSize()), `a59b64e` (production SqliteSubscriberReadConnectionFactory), `725353d` (PersistenceFactory public gateway + SqlitePersistenceLifecycle 6-store expansion), `dfb045e` (HomeSynapseCore facade). 20 files (7 new + 13 modified), +1,432 lines. OR-M3-14 prerequisite infrastructure bundled. New production types: PersistenceFactory (public), SqliteSubscriberReadConnectionFactory (pkg-private), SqliteSubscriberReadExecutor (pkg-private), HomeSynapseCore (public). Fifteenth CC WU. |
| 2026-05-20 | PM (Cowork) | **WUCP Phase 2 — M3.6c + M3.6d-a + DEC-M3-17.** Closeout for M3.6c (`38d3e30`) and M3.6d-a (`25bc23b`). M3.6d sub-divided into d-a (done) + d-b (next) per Option A. DEC-M3-17 logged (HealthSignal + HealthLevel transitive promotion; DEC-M3-16 addendum). Open risks added: OR-M3-13 (reconciliation metadata feature gap), OR-M3-14 (M3.6d-b prerequisite infrastructure). Next: revised M3.6d-b coding instruction. |
| 2026-05-20 | Coder (Claude Code) | **M3.6d-a — Composition-Root Satellite Changes.** Committed `25bc23b`. 18 files (6 new + 12 modified). Major scope: SqliteStateStore implements StateCheckpointSource; QueueSaturationHealthCheck + HealthSignal + HealthLevel public (DEC-M3-17 transitive chain); ReadinessSource public interface; ReconciliationTest 4 of 5 methods; Tier 9 reconciliationOnVersionMismatch un-disabled; HomeSynapseConfig + SharedScheduler + ThrowingStateQueryService skeletons in lifecycle module; module-info `requires transitive` for persistence/event.bus/state-store + non-transitive `requires org.slf4j`. M3.6d sub-divided after Coder pushback identified 7 source-vs-brief mismatches (user chose Option A). SLF4J follow-up patch applied same-day (build green after fix). Fourteenth CC WU. |
| 2026-05-20 | Coder (Claude Code) | **M3.6c — Per-Module Event-Class Manifests.** Committed `38d3e30`. 1 new file + 4 modified. `EventTypes.CORE_PRODUCTION_EVENT_CLASSES` + `IntegrationEvents.LIFECYCLE_EVENT_CLASSES` aggregated at the composition root via `Stream.concat`. Closes Q3 gap-closure Artifact 1. Deviations D-1/D-2/D-3 all naming/cosmetic. Thirteenth CC WU. |
| 2026-05-20 | PM (Cowork) | **WUCP Phase 2 — M3.6a + M3.6b.** Governance/context maintenance closing both sub-WUs. Four PM-side artifacts updated. DEC-M3-16 applied. Audit findings C-01, D1-05, D1-07, D1-13, D2-11, D4-09, D5-04 CLOSED. Next: M3.6c coding instruction. |
| 2026-05-20 | Coder (Claude Code) | **M3.6b — EventBusConfig + InProcessEventBus Visibility.** `EventBusConfig` record (2 fields, `HOME_DEFAULT`). `ReplayWindowQueue` capacity parameterized. `InProcessEventBus` promoted to `public` (DEC-M3-16). 8 files (3 new, 5 modified). 9 tests added/modified. Tenth CC WU. |
| 2026-05-20 | Coder (Claude Code) | **M3.6a — Profile-Driven Persistence Configuration.** Wired `DeploymentProfile` through `PersistenceConfig` into `DatabaseExecutor`. PRAGMAs profile-driven. `LockingMode` enum created. 14 files (1 new, 13 modified). 5 tests added/modified. Ninth CC WU. |
| 2026-05-19 | PM (Cowork) | **WUCP Phase 2 Reconciliation.** Retroactive closeout for six work units (Bus-Fix Piece A through M3.4b) and two design sessions. Hivemind brought from STALE to PASS. `testing/integration-tests/MODULE_CONTEXT.md` populated. Next: M3.6a coding instruction (profile-driven persistence config). |
| 2026-05-20 | PM (Cowork) | **Gap-Closure + M3.6 Composition-Root Design.** Two artifacts: Q1-Q4 gap-closure answers (all benign) + M3.6 five-WU design (M3.6a..M3.6e). Design approved by Nick. Preflight STALE override (Option B) documented. Artifacts: research/2026-05-20_M3_Audit_Gap_Closure_v1.md, design/2026-05-20_M3.6_Composition_Root_Design.md |
| 2026-05-19 | PM (Cowork) | **Cross-Tier Deployment Audit.** Six-dimension audit across six deployment tiers. Verdict: NEARLY READY. C-01 (DeploymentProfile not wired) revised from BLOCKING to SIGNIFICANT. Nine SIGNIFICANT findings fold into M3.6. Report: nexsys-hivemind/context/audits/2026-05-19_cross-tier-deployment-audit.md |
| 2026-05-19 | Coder (Claude Code) | **M3.4b — Sustained-Load + Crash-Recovery Tests.** ThrottledWriteCoordinator disk test double. Pi4SustainedLoadIT, Pi4D1SpikeIT, CrashRecoveryIT. scripts/pi4-validation.sh on-device runner. All tests GREEN. Deviations: event-count assertion loosened (±2% → lower-bound 25%), @TempDir NEVER, SLF4J dep add. Commit `adf04d2`. |
| 2026-05-19 | Coder (Claude Code) | **M3.4a — Integration-Test Scaffold.** Module 20 (testing:integration-tests). IntegrationTestHarness, BurstLoadIT, HeapBudgetIT. PersistenceTestHarness + InProcessEventBusFactory testFixture factories. Pi-profile gated. Commit `5ae7912`. |
| 2026-05-19 | Coder (Claude Code) | **Supervisor DLQ Wiring.** 11-field DeadLetter replaces 6-field DlqEntry. PersistentDlqWriter interface. 12 new tests. Commit `ed5862c`. |
| 2026-05-19 | Coder (Claude Code) | **Projection-Checkpoint Wiring.** StateCheckpointSource injection seam. 10 MB advisory checkpoint-size guardrail. 12 new tests. Commit `56aaa4b`. |
| 2026-05-18 | Coder (Claude Code) | **M3.5b StateProjection Prod Persistence.** SqliteStateStore, SqliteCheckpointStore, AtomicCheckpointWriter. Full contract suite GREEN. Commit `08d0136`. |
| 2026-05-18 | Nick (direct edit) | **Bus-Fix Piece A.** DerivedWriteRateLimit promoted to public in core/event-bus. Unblocks cross-package construction. Commit `fceafe8`. |
| 2026-05-19 | Coder (Claude Code) | **M3.4a — Integration test scaffold + harness + BurstLoadIT + HeapBudgetIT.** New `testing/integration-tests` module (#20). `PersistenceTestHarness` and `InProcessEventBusFactory` testFixtures bridge package-private production types. `IntegrationTestHarness` wires the real production stack (file-based SQLite + `InProcessEventBus`). BurstLoadIT (500-event burst, 6 assertions) + HeapBudgetIT (3,000-entity heap bound, 4 assertions). Tests gated on `-PpiProfile`. `testFixturesApi` deps added to persistence for cross-module fixture visibility. Full `./gradlew check` GREEN; `./gradlew :testing:integration-tests:test -PpiProfile=throttled` GREEN. Commit `5ae7912`. |
| 2026-05-19 | Coder (Claude Code) | **Supervisor DLQ wiring.** `SubscriberSupervisor.deliver()` constructs `DeadLetter` (11 fields) instead of `DlqEntry` (6 fields). Routes through `park(DeadLetter)` — dual ring + persistent writer. Null-guarded `causeMessage`, `diagnostics=null`, `attemptCount=1`. `InProcessEventBus` DLQ identity upgraded to two-arg constructor. `TransitionCoordinator.park(DlqEntry)` deliberately preserved for `CAUGHT_UP_TRANSITION_MARKER = -1L`. 12 new `SubscriberSupervisorTest` methods. Source-verification audit `2026-05-19_supervisor-dlq-source-verification.md` confirmed 9 assumptions (1 corrected: `SubjectRef.toString()` format). Commit `ed5862c`. |
| 2026-05-19 | Coder (Claude Code) | **Projection-checkpoint wiring.** `StateCheckpointSource` interface introduced in state-store. Advisory 10 MB checkpoint-size guardrail in `StateProjection`. Wires `StateProjection` to call `source.serializeCheckpoint(projectionVersion)`. Closes M3.5b non-blocking concern #1. Commit `56aaa4b`. |
| 2026-05-18 | Coder (Claude Code) | **M3.5b — StateProjection production persistence.** `SqliteStateStore` (ConcurrentHashMap-backed), `SqliteDeadLetterStore`, `PersistentDlqWriter` interface, `CheckpointSerializer` with Jackson JSON, V004 DLQ indices migration, `ObjectMapper` divergence (NON_NULL for events, ALWAYS for checkpoints), `AtomicCheckpointWriter.writeAtomicCheckpointWithDlqPark()` three-way atomic write. 19 files