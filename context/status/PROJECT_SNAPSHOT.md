<!--
file: context/status/PROJECT_SNAPSHOT.md
purpose: Current operational state hub — current WU, code state, deferred gates, build status.
audience: All
update-cadence: per-WU
state-type: current
status: CURRENT
last-verified: 2026-06-26 (v7 hub launch — preflight STALE-benign reconcile) against core HEAD **b296e76** (M7.3 GREEN + the Track-A code deliverables; HomeSynapseCore unchanged; live wiring still deferred -> M7.4) / docs **f54d0e0** (committed baseline: Doc 16 LOCKED; Doc 17 DRAFT + AMD-95 PROPOSED committed; watermark AMD-94; invariants 169/49 — **v7 folds ON DISK pending Nick's commit: AMD-95 RATIFIED + Doc 17 LOCKED/AIOT-INV-1 → watermark AMD-95, invariants 170/50; AMD-96 PROPOSED**) / hivemind **cd8b046** (the beat-11 reconcile + the v7 prompt; the v7 §3 folds on disk pending commit). Phase 3 -- test-first implementation. (Prior v6-hub stamp was core 5363347 / docs e47f01e / hivemind 837e743 — it trailed the v6 arc beats 8-11; reconciled here. The M6-era 2026-06-07 last-verified blob rotated -> context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-22.md.)
-->

# Project Snapshot

> **2026-06-27 (v8 hub, beat 17) — M7.4b GATE-GREEN + WUCP Phase 2 ACCEPT — THE RUN PIPELINE IS LIVE (producer end-to-end through dispatch).** The Coder built M7.4b; the first `./gradlew check` went **RED on a P2 fan-out miss** (`DurationTimerTest:205` constructed the now-2-arg `AutomationEngineSubscriber` directly — the Coder swept the assembly test but missed the direct-ctor consumer); the hub applied the surgical gate-fix (inert `RecordingRunManager`+`FakeSelectorResolver` doubles; the REPLAY test never initiates a run) → **re-run GREEN.** The deferred-gate discipline working as designed: the Coder's 4-lens LLM review returned zero findings (cannot compile); the gate caught it in 9s. **WUCP Phase 2 = ACCEPT, verified:** `git status` confirms **module-info unchanged in all three modules + zero event-model change → counts hold 71/41/53**; the green gate confirms the lifecycle composition-root/`@TempDir` tests pass (the paired `runManager.close()` teardown in BOTH branches is clean — the M7.3 leak class closed), the D2 replay-safety unit test, and the producer-pipeline test (**a seeded trigger emits `command_issued`→`command_dispatched`**). Read `RunInitiator.java` directly — the trigger→run handoff resolves via the evaluator's own matched-trigger indices. **The 4 `[REVIEW]` choices — all ACCEPT:** (1) the package-private `evaluateMatches` seam (the evaluator's immediate-match indices; `evaluate()` its thin projection) — **strictly better than the interface-widening the instruction floated**: single source of truth, no re-derivation, frozen `TriggerEvaluator.evaluate` untouched, zero ripple; (2) `resolvedTargets` keyed `"action:<index>"` (the RunContext position form) — forward note: align with a selector-label convention when the causal-read-API needs it; (3) `RunManager extends AutoCloseable` (the M7.4a precedent); (4) `RunManagerConfig.defaults()` at the root — forward note: surface the cascade/auto-disable params through a typed config accessor when operator-configurable (V1 defaults fine). **The M7.4a serializer `[REVIEW]` is now CLOSED in production:** the executor's `parameterSerializer` comes from the new `PersistenceFactory.commandParameterSerializer()` — a `Function<Map,String>`, NOT `ObjectMapper` (Jackson stays off persistence's exported API; confirmed by unchanged module-info + green `-Werror`), backed by the mapper that round-trips `AttributeValue` (a real round-trip test, not a stub). **Lesson (gate-fix):** on any ctor signature change, the P2 sweep must `grep "new <Class>("` across the ENTIRE test tree, not just seam/assembly tests (M7.2a-2 class). **OR-M7-WIRING: HALF-CLOSED** — the producer pipeline is live; the live ledger + confirmation half is M7.4c. **Next:** Nick commits (M7.4b Core incl. the gate-fix; the separate distribution `.deb`-version fix; the hivemind spine) → M7.4c (live `pending_command_ledger` + `pollExpirations()` tick + the E2E confirm gate). core working tree on `b85e1ed` (commits next).

> **2026-06-27 (v8 hub, beat 16) — ecosystem-currency research RETURNED + reconciled (off the critical path; M7.4b untouched).** The write-isolated research session returned `context/assessments/2026-06-27_smart-home-ecosystem-currency_research-return.md` (web-backed; 71 VERIFIED / 16 honestly-flagged UNVERIFIED; the fabricated SafeGate citation sought + excluded). Read in full; source discipline strong; the 6 decisions hold. **Two clusters CONFIRM-reinforced, one MOVED.** **The mover (time-sensitive — Cluster 2 / positioning, Nick's strategic call):** Home Assistant merged the FRAMEWORK for durable never-evicted "why-didn't-it-fire" traces to `dev` 2026-06-22 (core #174116 + frontend #52708) — but the **emitter was reverted, so it is NOT shipped**; still, the "why-not" half is no longer "unshipped everywhere." **Re-weight the mid-Aug differentiator onto the two halves that still hold: (a) the durable `confirmed|unconfirmed|failed` device-confirmation projection — unaddressed on EVERY platform (open/closed/AI); (b) never-evicted-as-a-structural-property (a pure projection of the immutable log; HA's is capped/bucketed/bolt-on).** **Baseline CORRECTION:** HA traces persist to disk (NOT "in-memory/evicted") — the real limits are the cap (5) + no-trace-when-a-trigger-never-matches; fix before any copy ships (the SafeGate-in-our-own-voice risk). Go/no-go WATCH on the HA 2026.7-line. **AMD-96 + M9 (Cluster 1A): AMEND** — direction holds, but **widen the band v14→v18 / EmberZNet 8.0.2→9.0** (v14 is the floor on shipping MG24 dongles; SDK current 9.0/v18 Mar-2026; Doc 08's v13/7.4 ~5 versions stale). The **`ASH_ERROR_TIMEOUT` is a recognized cluster on factory-8.0.2 MG24 units** (remedy = reflash newer NCP firmware) → **promote to a first-class M9 + hero-bench reliability item** (the hero runs on this exact silicon); V1 posture = recommend-firmware / ship-reflashed-image / document-the-reflash+watch (Nick + bench). **Thread seam (1C): NARROW** — single-chip Zigbee+Thread on one MG24 (RCP Multi-PAN) is deprecated (HA "multiprotocol is dead" Jul-2025; Silicon Labs de-recommends) → reserve as "add a dedicated Thread radio later," NOT "MG24 dual-stack." **CONFIRMs (reinforced, no action):** Matter deferral (1.6 = focused, no new device categories; still fragmented; keep the seam reserved); D5 converter-DB (MIT/MIT/Apache-2.0 unchanged; zigpy→Open Home Foundation de-risks; GPL app separate); the planner→verifier→executor AIoT consensus (5/5 baseline papers verified; the "externalized runtime governance" frontier = our frame). **Doc 17 §7 Q1 (3B): SCOPE the patent follow-up** (no blocking patent, directional/moderate; run the under-covered surfaces — Samsung/SmartThings, WO2025024326A2, Apple US20250077786A1, non-US — before any superlative) + **CORRECTION:** US10367652B2 family = Nant Holdings IP, NOT Nest/Google. Defensible claim ("AI is structurally a proposer, statically verified, durably auditable") holds; superlatives stay counsel-gated. **Hub reconciliation:** the AMENDs route to their channels (the AMD-96 currency review scope; Doc 17 §7 Q1; the positioning = Nick's strategic call) — the hub does not self-edit Locked/proposed bodies; the CONFIRMs + the 2 baseline corrections are recorded here. **No V1-scope change.**

> **2026-06-27 (v8 hub, beat 15) — M7.4 SPLIT 3-way (Nick co-signed); M7.4b ISSUE-READY (the run pipeline goes live).** Grounding the M7.4b wiring revealed M7.4b-as-framed was an EPIC: the `RunManager` is **not wired into `HomeSynapseCore` at all** (the `automation_engine` subscriber evaluates triggers but ignores the matches — its literal comment is "run initiation is M7.2"), so the entire run lifecycle (RunManager→condition gate→executor→`command_issued`) is built+unit-tested but **never assembled live**, the trigger→run handoff deferred since M7.1. Per P1 (size visible at scoping, not in arrears), **M7.4 splits:** **M7.4b** = the run pipeline goes live (the producer: construct selector/condition/executor[+serializer]/gate/RunManager into the composition root + the trigger→RunManager handoff as a **direct co-located `initiateRun` call** — D1 governs the COMMAND hop, already the M7.4a subscriber) · **M7.4c** = confirmation goes live (the live `pending_command_ledger` + the `pollExpirations()` tick + the E2E composition-root confirm gate) · **M7.4d** = the D2 replay CI test + register D2's canonical INV-id. **M7.4b instruction ISSUE-READY:** `context/instructions/2026-06-27_M7.4b_run-pipeline-go-live_...coding-instruction.md`. **Two findings baked in:** (1) the **trigger→run derivation** — `StandardTriggerEvaluator.evaluate()` returns only `List<AutomationId>` but `RunManager.initiateRun(...)` needs `matchedTriggers`+`resolvedTargets`; the contract is fixed (one root Run per matched automation, LIVE-only, `RunCausalChain.root()`) and the derivation (re-derive vs. widen `evaluate`'s return) is the Coder's design call with pushback; (2) **`PersistenceObjectMapper` is package-private** (no `PersistenceFactory` accessor) → M7.4b adds a **public `Function<Map,String>` param-serializer seam** (returns `Function`, NOT `ObjectMapper`, to keep Jackson off persistence's exported API; the mapper already round-trips `AttributeValue`, closing the M7.4a forward risk **in production**, not via a test stub). The malformed `-Werror` self-check guidance is corrected in M7.4b's Build Discipline. **core `b85e1ed`** (M7.4a committed + pushed; CI-on-commit to confirm green) · hivemind `278c9e6`. **Next:** Nick routes M7.4b → Coder → WUCP Phase 2 → M7.4c. Parallel/off-critical-path: the scoped smart-home/ecosystem research session (feeds back to THIS hub for steering); the skills design-v1 review; the physical Wave-1 bench. Mid-Aug go/no-go on the calendar.

> **2026-06-27 (v8 hub, beat 14) — M7.4a RETURNED + GATE-GREEN + WUCP Phase 2 ACCEPT (source-verified).** The Claude Code M7.4a build landed (9 modified + 2 new files, **uncommitted** working tree on `606d410`); **`./gradlew check` GREEN — 149 tasks** (the `-Werror` targeted self-check failed only because `-Werror` is not a Gradle CLI flag — a malformed command, not a code defect; `check` bakes `-Werror` into the compiler config and passed). **WUCP Phase 2 = ACCEPT, verified against source (not the report):** module-info unchanged both modules + zero event-model change → **counts hold 71/41/53** (structural, from the diff); the paired `close()` teardown is present in **BOTH** `HomeSynapseCore` shutdown branches (L766-767 / L819-820, null-guarded — the reverted-M7.3 trap closed); the dispatch subscriber registers Step-3.4b **after projection LIVE**, `coalesceExempt=true`; the D2 LIVE-only guard mirrors the gate-proven M7.3 ledger (`volatile replayMode` driven by the bus setMode-before-replay-delivery contract); the 5 required subscriber tests + the producer tests assert correctly (incl. the §3.11.2 causal chain + the REPLAY no-dispatch proof). **The 5 `[REVIEW]` choices — all ACCEPT:** injected `parameterSerializer` (no JSON edge), `onEvent`→`validate(Map.of())` (Tier-1 value-agnostic), `coalesceExempt=true`, `CommandDispatchService extends AutoCloseable` (additive, Phase-3-internal), executor ctor widened (impl-class). **Two tracked FORWARD items:** (1) **M7.4b** — when the executor is live-wired (the RunManager pipeline + the REAL persistence-`ObjectMapper` serializer), **verify the serializer round-trips the resolved param value types, esp. value-model `AttributeValue`** — the unit tests use a stub, so GREEN does NOT prove production serialization; broken serialization = the hero "did it confirm?" rendering garbage params; (2) **M9** — the dispatch subscriber intentionally does NOT deserialize `command_issued.parameters` (no JSON lib in automation) and `command_dispatched` carries no params, so the M9 adapter must read params off the durable `command_issued` (by causation); Tier-2 param-schema validation is future. **Milestone truth (OR-M7-WIRING still OPEN):** M7.4a wired the SUBSCRIBER + built the producer CODE, but the executor/RunManager is **not composition-root-wired yet** (deferred since M7.2a-2) — the registered `command_dispatch_service` has **no live producer** (idle, harmless). So **M7.4b scope = the QUARTET: (a) wire the RunManager+executor producer into `HomeSynapseCore` w/ the real serializer, (b) the live `pending_command_ledger` subscriber + `pollExpirations()` tick (w/ paired teardown), (c) the E2E composition-root gate, (d) the D2 replay CI test** — that closes OR-M7-WIRING. **Residual (gate-of-record loop):** M7.4a is uncommitted; on Nick's commit+push, **confirm the ci.yml run is green on the M7.4a commit** (first real milestone use of CI; "a claimed gate must be verified actually-green") before recording M7.4a CI-gated. **Minor:** fix the coding-instruction `-Werror` self-check guidance (compiler arg, not a Gradle CLI flag); the retained in-process `dispatch()` primitive is mild post-event-driven cleanup debt. **Next:** Nick commits/pushes M7.4a + confirms CI green → the hub authors M7.4b (grounding first: the §1 D2 record + the V2-A replay-harness template + the RunManagerAssembly live-wiring shape). Skills design-v1 review + the bench capture + AMD-96/Doc-13 currency reviews run parallel, off critical path.

> **2026-06-27 (v8 hub, beat 13) — LAUNCHED; preflight STALE-benign → RECONCILED; governance-close COMMITTED; the two in-flight returns reconciled (skills Phase-0 LANDED; M7.4a NOT yet in the repo).** The v8 mission-control hub superseded v7 (v7 prompt archived → `context/handoff/archive/`), grounded (env-model → truth-hierarchy/pointer-not-copy → V1 record/v6 plan/§1 record → 11-check preflight), and ran the mandated STALE→reconcile. **Preflight = STALE-benign** (Checks 1/3/6 currency-lag behind the now-COMMITTED governance close; zero CONFLICTED; **Check 9 PASS**). **Baselines CONFIRMED at committed reality (re-derived from source, not propagated):** core **`606d410`** (`b2529cc` M7.3+Track-A+gradlew/scripts-+x → `652f9b3`/`604440b` frontend+install-smoke activation → `606d410` build-deb broken-pipe fix; **working tree clean** — the 5 hivemind "M" + the `build-deb.sh` "M" are the documented mount-truncation phantoms, host files intact, verified) · docs **`75d0345`** (**AMD-95 RATIFIED**, watermark 95; **Doc 17 LOCKED** + AIOT-INV-1; **AMD-96 PROPOSED**; invariants **170/50**) · hivemind **`17f26d9`** (the v7 governance commit `442eb8d` + this v8 prompt). **CI is genuinely GREEN** (149 tasks; ArchUnit guards enforced; install-smoke = gate #4, green-up pending). Counts **71/41/53** (M7.4a is zero-mint by design). **Return B — skills Phase-0 VERIFIED + ACCEPTED → DONE** (Nick's independent filesystem re-check, clean; off the product critical path): `nexsys-skills/` 4-tier skeleton + README + provenance MANIFEST stub + `design/` (design-v1 Lock-candidate + its independent-review dispatch); `skills-initiative/` de-cluttered; harness daily-check parked. **Residual = Nick:** `git init` + the prepared `_scratch/_commit-2026-06-27-nexsys-skills-phase0-init.txt`; then launch the design-v1 independent review → co-sign → Lock (ratifies the *design*, not a build; conform-not-rebuild per §15.5; build deferred post-V1). **Return A — M7.4a NOT yet landed (VERIFIED, not assumed):** no command-pipeline producer/subscriber change on `main` (`StandardCommandDispatchService` still at M7.2a-2 `79e2984`; no `CommandDispatchAssembly.java`; tree clean; the only `claude/*` branch is the dead 2026-03-27 `confident-williamson`, 0 commits ahead). The Claude Code M7.4a build is still in flight / not yet relayed-synced by Nick. **M7.4a is NOT marked done; M7.4b NOT authored (gated on M7.4a GREEN).** **Next:** Nick relays/syncs the M7.4a return → gate (`./gradlew check` + CI green on the commit) → WUCP Phase 2 (source-verify 71/41/53; paired teardown in BOTH shutdown branches; the LIVE-only/replay + teardown tests; rule any `[REVIEW]`s) → author M7.4b (live `pending_command_ledger` subscriber + `pollExpirations()` tick + E2E composition-root gate + D2 replay CI test; closes OR-M7-WIRING). Parallel (off critical path): the skills git-init + design-v1 review; the bench physical capture (M9 ground-truth + the M7.4 E2E motion fixture); the AMD-96 + Doc-13 currency reviews. Mid-Aug go/no-go stays on the calendar.

> **2026-06-26 (v7 hub, beat 12) — §3 CONSOLIDATED DECISION PASS RULED (12 decisions, Nick co-signed) → GOVERNANCE CLOSED + M7.4a ISSUED.** Nick ruled all four §3 question-groups as recommended. **Folded on disk (pending Nick's commit):** **AMD-95 RATIFIED** (watermark **AMD-94 → AMD-95**) — Doc 07 §3.11.1/§3.11.2/§4.3/§7/§16 reconciled to the event-driven/co-located shape + the 3 review-added drift fixes; AMD-90 §2.1 superseded-in-part (§11). **Doc 17 LOCKED** — **AIOT-INV-1 minted** (new `INV-AC` category; invariants **169 → 170**, categories **49 → 50**; S1/S2 + E1–E5 folded; AIOT-INV-2/3 kept as principles; AX-7 sequenced before AI-authoring; Q1 patent search commissioned). **Wave-1 device-model reconciliation** recorded (B4 White/CT + Kelvin-canonical; B5 EZSP-v14 currency; B6 occupancy binding; B7 keep scaffold) → **AMD-96 PROPOSED** (Doc 02/08 currency, for a source-verifying review). **Lane-integration note** (C8 additive `name` field folded into the v1.1 contract; C9 Doc 13 currency; C10 the two CI drops = Nick; C11 E1/E2/E3/E6 — E2 bind-host Core WU ahead of the M9 wizard; C12 doctrine shared-home). **Watermark AMD-95 · invariants 170/50** (on disk). **M7.4a coding instruction ISSUE-READY** (`context/instructions/2026-06-26_M7.4a_command-issued-producer-and-co-located-dispatch-subscriber_coding-instruction.md`) — the `command_issued` producer + the co-located dispatch subscriber + a paired teardown; D2/D3 as constraints; the E2E + replay CI gate fenced to M7.4b. **Nick's next:** commit the folds (the commit-message file at the ClaudeFolder root) + the two CI file drops + mirror-sync if any skill source changed + route M7.4a to the Coder. **D-07-A (9th action permit) deferred to M8** (forward/unbuilt currency note). **CI:** `ci.yml` (the gate-of-record) is now genuinely GREEN for the first time (was never green — `gradlew` non-exec since init; fixed `b2529cc`); `install-smoke` activated (gate #4); see `context/process/ci-as-gate-of-record.md` for the correction + the deferred CI-hardening backlog. **Skills track (parallel, write-isolated, OFF the product critical path):** the do-now rot-fixes are adopted (the truth-hierarchy/pointer-not-copy discipline; `project-knowledge` governed-stale); Nick ruled Phase-0 (stand up `nexsys-skills/`) as its own dispatched session (`skills-initiative/2026-06-27_phase-0_skills-foundation_session_prompt.md`), ratify design v1 + conform-not-rebuild, and a daily auto-maintenance check planned-but-deferred-until-the-harness-is-tested. Detail in `skills-initiative/` (loose until Phase 0 versions it into `nexsys-skills/design/`).

> **2026-06-26 (v7 hub launch; reconciling the v6 arc beats 8→11) — §1 architecture RATIFIED + empirically validated; Doc 17 DRAFT + AMD-95 PROPOSED; Track-A fleet's first beat landed; decisions consolidated.** The v6 hub ran a full arc and is now token-heavy; the **v7 mission-control hub** carries forward in fresh context. Spine reconciled to core **`b296e76`** / docs **`f54d0e0`** / hivemind **`cd8b046`** (the beat-11 reconcile + the v7 prompt — see pm-handoff beats 8→11 for the narrative; this is currency reconciliation, the substance already committed). **State:** the engine ACTS as units (through M7.3) but is **NOT live end-to-end** — the command pipeline is unwired (**OR-M7-WIRING**), so there is no live `RunCausalChain` yet. **M7.4 is the first milestone that moves the demo needle.** **§1 deeper-M7 architecture RATIFIED 2026-06-25** (D1 event-driven/co-located dispatch · D2 pure-function-replay · D3 additive event versioning · D4 log retention · **D5 converter-DB = adapt-the-data + curated fallback, RATIFIED 2026-06-26**) **+ empirically validated** (V2-A Pi spike: D1 ≈1.9 ns confirmed, D2 PASS as the M7.4b CI-gate template, D4 sized — thresholds FREEZE on a Session D Pi re-run). **Doc 17 (AIoT + Cloud Readiness) DRAFT** + **AMD-95 (command-pipeline currency) PROPOSED** committed to docs — both run the formal review→ratify→Lock pipeline (review-passed; pending Nick co-sign). **Track-A fleet first returns:** Web-UI (`web-ui/dashboard/` buildable Preact+TS SPA, both hero halves, frozen v1.1 contract) · Distribution (`homesynapse-core/distribution/` one-command-install skeleton + CI gate #4) · Bench Session C (device-corpus knowledge layer + verdicts — **PHYSICAL CAPTURE PENDING on Nick's desk**). **The v7 hub's FIRST substantive act = the §3 CONSOLIDATED DECISION PASS** (12 decisions, Nick rules), THEN the governance close-out (AMD-95 ratify-fold + Doc 17 Lock-fold minting AIOT-INV-1) + M7.4a authoring. **Open governance items pending Nick:** AMD-95 co-sign · Doc 17 Lock · the Wave-1 device-model reconciliation (ESC-W1-HUE-01/COORD-01/SNZB03P-01) · the lane-integration items. The detailed beats 8→11 are in `context/handoff/pm-handoff.md`.

> **2026-06-22 (beat 6) — M7.2b GREEN (first pass) + WUCP Phase 2 APPROVE — THE ACTION MODEL IS FROZEN (Cowork PM v4 hub).** `./gradlew check` **GREEN (149 tasks), zero gate-fix rounds** — the inline spotless self-step + the record-collision STOP-check (baked into the instruction) held. Delivered: minimal computed-param resolution (`ComputedValue` sealed, 3 permits `LiteralValue`/`AttributeRef`/`AggregateValue`, resolved at run-init against the captured AMD-03 snapshot, before the frozen `ActionExecutor`); the run-coupled fail-closed-read terminal (snapshot read throws → Run `FAILED` + recorded reason, C1 preserved); no-engine-retry frozen (AMD-90-INV-01). **No new event type — counts 71/41/53; module-info UNCHANGED** (only automation files touched). **[REVIEW] deviations → all ACCEPT (3 are excellent Coder catches resolving real instruction tensions):** (1) types **package-private** (a public `resolve(): AttributeValue` would force `requires transitive value` → the module-info-unchanged pin); (2) `AggregateValue` carries a resolved **`Set<EntityId>`** not a `Selector` (Selector resolution = an I/O capability, contradicts C-SA-2 purity); (3) absent attribute → **`DegradedAttributeValue`** typed-absent (a valid input must not throw, C-SA-2). **Next Core slot = M7.3 (PendingCommandLedger)** — the confirmed/unconfirmed hero half (Option A); instruction authors next. **Core SHA reconciles at Nick's M7.2b commit.** _Prior beats (beat-5 M7.2a-2 GREEN / beat-4 v4-launch / v3 / the two 2026-06-20 notes) + the M6-era last-verified, Next-action, Open-flags, and Committed/Latest-commit detail rotated -> `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-22.md`._

**Last updated:** 2026-06-27 by PM (Cowork **v8 hub launch**) -- preflight **STALE-benign → reconciled** (Checks 1/3/6 currency-lag behind the now-COMMITTED governance close + CI; zero CONFLICTED; **Check 9 PASS** — both skill mirrors byte-identical). Spine reconciled to core `606d410` / docs `75d0345` (watermark AMD-95; 170/50) / hivemind `17f26d9`. Current truth = the **v8 beat-14** note above; richer narrative (beats 8→14) in `context/handoff/pm-handoff.md`. M7.4a RETURNED → GATE-GREEN (149 tasks) → WUCP Phase 2 ACCEPT (source-verified); skills Phase-0 DONE; OR-M7-WIRING stays OPEN → M7.4b is next (the producer live-wiring + ledger + E2E gate + D2 replay CI test).

**Current state:** **M7.2b GREEN -- THE ACTION MODEL IS FROZEN.** The Core engine ACTS end-to-end (trigger -> condition -> run -> action -> dispatch -> events): M6 COMPLETE (4-of-4) - M7.1 - AB-1/AB-2/AB-3 - M7.2a-1 - M7.2a-2 - **M7.2b** all DONE + GREEN, **+ M7.3 (PendingCommandLedger) DELIVERED GREEN 2026-06-22** (ledger FSM + assembly + tests; AMD-92 ZERO mints, counts stay **71/41/53**; LIVE composition-root wiring DEFERRED → M7.4). HEAD **core `5363347`** (M7.3 DELIVERED GREEN; live wiring deferred → M7.4 — SHA reconciled 2026-06-25) / **docs `e47f01e`** (Doc 16 LOCKED) / **hivemind** (the M7.3 closeout) - **watermark AMD-94** - invariants **169/49** - `projectionVersion` **5** - `./gradlew check` GREEN. **RE-SEQUENCED 2026-06-22 (Nick, "live spine first"): Next Core slot = M7.4 (command-pipeline live integration: producers + live subscribers w/ teardown + pollExpirations tick + an E2E gate test)** -> thin causal-read-API -> AB-4 -> M9 Zigbee. Phase 3 -- test-first implementation.
**Open flags:** See **`## Open Risks`** below for the live list. **Standing deferred:** OR-M13-SDNOTIFY (sd_notify AF_UNIX-datagram transport -> M13; JDK-21 has no AF_UNIX SOCK_DGRAM; seam-tested; M5-D decision matrix authored). **No open deferred build gates** (M7.2b GREEN, zero gate-fix rounds). **Check 9 STALE** after this session's skill folds (coding-instruction-format record-collision STOP-check + pm-lessons v4 lessons + both SKILL.md mastheads) -- clears on Nick's mirror sync. The M6-era open-flags ledger (AMD-66..71 / OR-M6-NONCE / OR-PD-07-AMD, all RESOLVED) rotated -> the 2026-06-22 archive.
**Hardware (standing):** **Wave 1 ✅ RECEIVED 2026-06-23** — MG24/EZSP + Philips Hue RGBW 2-pack + 2× SNZB-03P motion + USB extension = the FULL hero set (the MG24/EZSP runs motion→light on its own). **Wave 2 (ZBDongle-P/ZNP second path + device breadth) ORDERED, not yet received.** **Meantime strategy = FRONT-LOAD the bench** (the highest-leverage parallel track now): characterize on the EZSP path via a reference stack (no M9 code) → device corpus + durable TEST FIXTURES (M9 acceptance + M7.4 E2E) → Doc 02/08 MATCH/GAP verdicts → harden the repeatable device-onboarding pipeline so Wave 2 + every future device flow through it seamlessly. See `context/planning/2026-06-22_hardware-bench-bringup-and-device-characterization_brief.md` + the v6 prompt §8.E + the fan-out Session C.
**M4 retrospective:** `context/audits/2026-06-05_M4-retrospective.md` (sizing, the instruction-scope-miss pattern + the consumer/pin survey fix, deferred-gate shift-left, the partial-closeout finding, the debt register, strategic/velocity, and the keep-doing list). For M4 execution history see the Recent Session Log below.
**Latest commits:** core **`b296e76`** (the Track-A code deliverables — `web-ui/dashboard/` SPA + `distribution/` install skeleton; on `5363347` M7.3; HomeSynapseCore unchanged, live wiring still deferred → M7.4) - docs **`f54d0e0`** (the two review returns + AMD-95 PROPOSED; on `e47f01e` Doc 16 Lock → `32afb3f` Doc 17 DRAFT → AMD-94 ratify; **Doc 17 DRAFT body commits after its Lock-fold**) - hivemind **`cd8b046`** (the beat-11 reconcile + the v7 prompt; on `837e743` → `373a0f8` §1-ratify → `eb3d0a5` reviews → `689d137` v6-plan → `b6ff0f1` fan-out-v2 → `4594cd9` index.lock-clear → `cd8b046`). _The M5/M6-era Committed + Latest-commit detail rotated -> `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-22.md`._
**Current phase:** P3 — Implementation (test-first).
**Days remaining:** 152 (to Nov 25, 2026)
**Launch target:** November 25, 2026 · **Mid-August go/no-go ≈ week 8** (the 4 gates: engine LIVE / hero on real data / hardware validating / install proven)

---

## Current Work Unit

**2026-06-21 (PM hub v3) — M7.2a-2 ISSUED + the same-beat trio DONE + all 3 lanes ready to launch.** The Core slot is filled: **M7.2a-2 (execution/dispatch) ISSUED** (`context/instructions/2026-06-21_M7.2a-2_execution-dispatch_coding-instruction.md`) — `ActionExecutor` + `CommandDispatchService` + `CommandValidator`(new) + `ConflictDetector` + the real `RunConditionGate`; mints AMD-92 rows 4/5/6/9 (EventTypes **67→71**, roster **37→41**, category **49→53** — source-re-derived); carries the four 2a-1 items (QUEUED sequential drain, row-4 `CausalContext`, dedup-before-gate, real snapshot+tallies) as settled DPs; both module-infos UNCHANGED. **Same-beat DONE:** the dashboard read-API contract is **FROZEN v1** (`context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md` — the Web-UI hard prerequisite) and **CI is confirmed live** (`context/process/ci-as-gate-of-record.md` — gate-of-record adopted). **#0 ABOVE ALL CODE:** the device-acquisition + test strategy brief is authored — **hardware APPROVED 2026-06-21 (Nick): the ~$140–175 bundle, ordering this week (the MG24 lands Thread-capable silicon; Matter/MQTT deferred — silicon-vs-software principle, brief §6).** **Lanes ready:** frontend-dev + Distribution-skeleton prompts authored (launch after Nick's go). **Next: Nick — approve hardware, route M7.2a-2 to the Coder, launch the two new lanes; the hub reconciles returns as the single spine-writer.**

**Beat 2 (2026-06-21, same day) — explainability research reconciled + M7.2b framed + read-API → v1.1.** Hardware ✅ APPROVED + ordered (genuine SONOFF ZBDongle-P [CC2652P] + Dongle Plus MG24 + the curated set + Hue Essential 2-pack). The explainability-UX competitive research landed (`context/assessments/2026-06-21_explainability-UX-competitive-research.md`): the two open differentiator gaps are **"why didn't it fire?"** + **"did the device actually do it?"** — build both, plain-language, never-evicted. **M7.2b framed** (`context/decisions/2026-06-21_M7.2b-action-model_decision-record.md`, PROPOSED): keep no-engine-retry (REC-162); the differentiator is command-outcome *visibility* (`dispatched→confirmed|unconfirmed|failed`); retry post-MVP gated. **⚠ Scope fork for Nick: pull M7.3 into V1** (so the confirmed/unconfirmed hero half ships) vs dispatched/failed-only. **Read-API → v1.1** (unconfirmed state + the co-equal non-firing read + never-silent-blank origin); both lane prompts sharpened to the research. mid-August go/no-go criteria drafted. **Next: Nick co-signs M7.2b + rules the M7.3 fork.** **HUB HANDOFF (2026-06-21): the v3 hub is token-saturated → the v4 hub prompt is authored (`context/handoff/2026-06-21_PM-mission-control_v4_orchestrator_session_prompt.md`, supersedes v3) — run it as a FRESH Cowork conversation (first act: the backlog M1–M4 rotation, then ground via preflight). Three lanes IN FLIGHT: M7.2a-2 (Core, Coder building), frontend-dev, Distribution-skeleton.** _Prior (v2) follows._

**2026-06-21 (PM hub v2) — M7.2a-1 ✅ DONE GREEN (149 tasks) → NEXT = M7.2a-2.** Nick's V1 wave is SET (M7.2a + the read-API-contract freeze & CI-as-gate-of-record in the same beat → frontend-dev + Distribution-skeleton next); **D-OPEN-1/2/3 RESOLVED** (hardware spec hub-verified vs Doc 08's two-path coordinator architecture — CC2652P/ZNP + EFR32MG21/MG24/EZSP, auto-detected; device set = Sonoff SNZB line + Philips Hue White, off IKEA; WebSocket runtime deferred for V1, poll 1–2s). **M7.2a-1 (run lifecycle) LANDED GREEN — PM WUCP Phase 2 → APPROVE** (`./gradlew check` 149 tasks; one spotless-fix round): `RunCausalChain`+`ChainLink` (AMD-91), the `RunContext` field-7 swap (`int cascadeDepth`→`RunCausalChain causalChain`), the run-lifecycle event sub-slice (rows 2 reshape + 7/8/10/17/18 → EventTypes **62→67**, roster **32→37**, category **44→49**), and the `StandardRunManager` FSM (C2 dedup, C3 comparator, §3.6 modes, §3.7.1 cascade depth+chain-cycle, auto-disable, REPLAY-zombie INTERRUPTED). Closes the M7.1 C1-interim hold. **Accepted deviation:** condition-before-mode (Coder honored the locked §3.6 contract over an instruction-sequence defect — pm-lesson logged). **NEXT = M7.2a-2** (ActionExecutor + CommandDispatchService + CommandValidator + ConflictDetector + run-trace §4.2; AMD-92 rows 4/5/6/9 → 67→71 / 37→41 / 49→53) — carrying the 2a-1 items: QUEUED sequential-drain, RunConditionGate↔CausalContext for row 4, dedup-before-gate, real snapshot/tally wiring. Parallel same-beat: the read-API-contract freeze + CI-as-gate-of-record. _Prior lane framing follows._

**DOC 16 (Superior Automation Layer) LOCKED 2026-06-20 → M7.2b entry-gate CLEARED.** INV-SA-01..04 registered (`Architecture_Invariants_v1.md` §49; 169/49; watermark stays AMD-94). **Next Coder slot = M7.2a** (run lifecycle / `RunManager` FSM / AMD-91 `RunContext`→`RunCausalChain` swap / `ActionExecutor` + `CommandDispatchService` / the AMD-92 run-lifecycle+dispatch event slice [rows 2/4–10/17/18] / run-trace — Doc-16-independent, builds against injected deps, wiring rode AB-3) — instruction authoring is the hub's next product; **AB-4 dispatch authored concurrently**, slotted pre-M9 (cipher activation via the 6-arg HSC ctor + F1 `v1` envelope tag / F3 / F13 + backup-key seam; nothing writes a sensitive scope until M9). **AB-1+AB-2 GREEN, committed `9e73d28`.** _Prior lane framing follows._

**M7.1 DONE (gate-verified GREEN `beb4bc3`, 149 tasks); M7-FORWARD PLAN RECORDED 2026-06-19** (`context/planning/2026-06-19_M7-forward_plan.md`). M7.2 split per P1 into **M7.2a** (baseline-buildable-now — run lifecycle + `RunCausalChain` swap + dispatch + the run-lifecycle/dispatch event slice; builds against injected deps, wiring→AB-3) and **M7.2b** (action-model freeze — **Lock-gated on Doc 16 + D2/REC-162**); **M7.3** (`PendingCommandLedger`) UNAFFECTED, depends on M7.2a. **Doc 16 DOCS review→Lock** is the M7.2b prerequisite (runs in parallel). **AB-3 (app-bootstrap lifecycle reconciliation) — ✅ DONE 2026-06-19, gate-verified GREEN (`./gradlew check` 149 tasks); PM WUCP Phase 2 → APPROVE.** The system is **runnable for the first time** (`main()` was a one-line stub): it boots `HomeSynapseCore` (implementing the Locked `SystemLifecycleManager`, PD-1) through the openHAB-startlevel phase model under a live health loop; the three M7.1 deferred wiring items landed (config + `InMemory*Registry`×3 assembled, `automation_engine` subscribed **after** state-store catch-up, the lifecycle wiring test). PD-2 registries in `core:device-model`. WUCP Phase 2 caught + fixed two correctness items the green gate could not (SD-9 FATAL→**surface-and-continue**; **durable HomeId**). HTTP gated closed + cipher inert — the Seam-1 boundary AB-1/AB-2/AB-4 wire into. **Seam-1 instruction AUTHORED + ISSUED 2026-06-19** (see the top masthead note): **AB-1 (auth + loopback bind) + AB-2 (fail-closed read) ISSUED; AB-4 (cipher activation + envelope-finalization) ⛔ AUTHORED-GATED on AMD-94 ratification** (Nick's bundling ruling; auth scheme = opaque bearer tokens). **Next Coder slot = AB-1 + AB-2** (`context/instructions/2026-06-19_AB-1-AB-2-AB-4_Seam-1_go-live_coding-instruction.md`); AB-4 un-gates on AMD-94 ratify. M7.2 NOT yet issued (Doc 16 Lock + D2 gate M7.2b). _Prior lane framing:_ **M6 COMPLETE (4-of-4) — M6.1 whole · M6.2 · M6.4 · M6.3 COMMITTED `1eddd9a`** (at-rest event-payload encryption for `[identity, presence_personal]`; `./gradlew check` GREEN 147; PM WUCP Phase 2 → APPROVE; working tree clean). **M7 entry-gate: ALL rows effectively cleared** — rows 1–2+4+5 ✅ CLOSED; **row 3 (energy/erasure interviews) RETIRED as decision-grade** per the 2026-06-15 M5-D desk-research assessment (it gated only the now-UN-PARKED app-bootstrap lane + the M6.3 restore-half R-α; see the 2026-06-18 masthead note). **CURRENT (2026-06-18 converge — direction set by Nick): three parallel lanes, M7.1 build held as a ride-along.** **(A) App-bootstrap milestone** — the runnability unlock; carries core-review **C1** (composition-root auth / INV-SE-02), **C2** (read-path decrypt-degrade contract), **C9** (lifecycle reconciliation). **DECISIONS A1–A4 RULED 2026-06-18** (loopback-default · token issuance · fail-closed-at-MVP+seam · ratify-rotate-DEK+reserve-tag-via-AMD-94); charter FINALIZED; AB-1..AB-4 in the backlog. Residual = mechanical only (CC-1 run · R-γ return · AMD-94 ratify) — see the top masthead note. **(B) Superior-automation Phase-1 design** — **SCOPE RULED 2026-06-18 + Architect-mode authoring DISPATCHED.** **Doc 16** = expressiveness-without-DSL + explainability/causal-chain + run-coupled reliability (+ federation identity/scoping seam + hybrid cut-line); **cross-cutting reliability, B3 federation, and honest-hybrid each split into their own sequenced docs** (Nick's B-1/B-2/B-3 rulings). M7 verdict: **M7.1 UNAFFECTED · M7.2 SHAPED-builds-into-Doc-16 · M7.3 UNAFFECTED.** It precedes M7.2/M7.3 so the implementation targets the differentiator. **(C) Agent currency pass** — coder + project-manager skills → M6-complete + core-review lessons. **M7.1** (`context/instructions/2026-06-15_M7.1_trigger-condition-path_coding-instruction.md`) is ISSUED + sound; it builds whenever a Coder slot opens, but the superiority design should confirm its trigger/condition contracts first. Current ground: the 2026-06-18 + 2026-06-14 masthead notes + Phase 3 Implementation Status below + `context/planning/phase-3-milestone-backlog.md`.

_The M3.7/M4-era work-unit blocks (M3.7 · M4 scoping · M4.0a · M4.0b-1/2 · P2 · AMD-50/AMD-47 ratifications · M4.B3 …) rotated 2026-06-12 evening → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-12.md` (git-object-sourced from `58794d7`)._

## Design Documents

All 14 design documents are Locked (Phase 1 — System Design Documentation is complete).

| # | Document | Status |
|---|----------|--------|
| 01 | Event Model & Event Bus | Locked |
| 02 | Device Model & Capability System | Locked |
| 03 | State Store & State Projection | Locked |
| 04 | Persistence Layer | Locked |
| 05 | Integration Runtime | Locked |
| 06 | Configuration System | Locked |
| 07 | Automation Engine | Locked + AMD-25 integrated (2026-03-18) |
| 08 | Zigbee Adapter | Locked |
| 09 | REST API | Locked |
| 10 | WebSocket API | Locked |
| 11 | Observability & Debugging | Locked |
| 12 | Startup, Lifecycle & Shutdown | Locked |
| 13 | Web UI (Observability MVP) | Locked |
| 14 | Master Architecture Document | Locked |

**Post-foundational design docs (Locked outside the original 14):** **Doc 15** — Cryptographic Architecture (Locked 2026-06-07; AMD-94 folded 2026-06-19). **Doc 16** — Superior Automation Layer (**Locked 2026-06-20**; INV-SA-01..04 registered as §49 of the invariants register; clears the M7.2b entry-gate).

## Committed M3 Design Artifacts (informational)

| Artifact | Path | Date | Notes |
|---|---|---|---|
| Cross-tier deployment audit | `nexsys-hivemind/context/audits/2026-05-19_cross-tier-deployment-audit.md` | 2026-05-19 | Closed-out audit of cross-tier deployment surface. Informed M3.6 composition root design. |
| M3 audit gap-closure research (Artifact 1) | `homesynapse-core-docs/research/2026-05-20_M3_Audit_Gap_Closure_v1.md` | 2026-05-20 | Identified and closed audit-trail gaps prior to M3.4b/M3.6. |
| M3.6 Composition Root Design | `homesynapse-core-docs/design/2026-05-20_M3.6_Composition_Root_Design.md` | 2026-05-20 | Authoritative composition-root design that M3.6 implements against. |

## Interface Specifications (Phase 2 — FROZEN)

Phase 2 completed 2026-03-20 with all 16 JPMS-compiled subsystem modules specified (plus 3 scaffold modules — platform-systemd, test-support, dashboard — that have MODULE_CONTEXT.md placeholders but no compiled Java yet). See `context/planning/phase-2-block-backlog.md` for the frozen historical record of Blocks A–S. Summary at Phase 2 close: 16 modules specified, ~402 production Java files, all module-info.java files compile clean with `-Xlint:all -Werror`. AMD-33 subsequently (2026-04-10) ratified DomainEvent as permanently non-sealed.

## M3 Governance (committed 2026-05-16)

| Amendment | Status | Scope |
|---|---|---|
| AMD-41 | APPLIED | State Projection Execution Model |
| AMD-42 | APPLIED | Subscriber Lifecycle and Isolation |
| AMD-43 | APPLIED | Backpressure and Observability |

13 new invariants: INV-BUS-01..03, INV-PROJ-01/04/NEW-01, INV-WRITER-01, INV-SUB-ISO-01..06. DEC-M3-01 through DEC-M3-17 locked (DEC-M3-14/15 added 2026-05-17; DEC-M3-16 added 2026-05-20 — composition-root visibility strategy; DEC-M3-17 added 2026-05-20 — HealthSignal + HealthLevel transitive visibility promotion alongside QueueSaturationHealthCheck, DEC-M3-16 addendum). PLAN-M3-CONSOLIDATED-02 committed as the M3 implementation authority.

## Phase 3 Implementation Status

Phase 3 began after Phase 2 closeout on 2026-03-20. Progress:

| Milestone / WU | Status | Date | Notes |
|---|---|---|---|
| M0 | COMPLETE | 2026-04-04 | AMD-31, traceability, MODULE_CONTEXTs |
| M1 | COMPLETE | ~2026-04-08 | Contract tests + in-memory implementations |
| M2 | COMPLETE | 2026-05-01 | Full persistence layer |
| M2-bridge | COMPLETE | 2026-05-02 | AMD-34–37, structural hardening |
| M3.1 | COMPLETE | 2026-05-17 | InProcessEventBus core (14 types) |
| M3.2 | COMPLETE | 2026-05-17 | REPLAY→TRANSITION→LIVE bus-side (16 types) — `0bade6a` |
| M3.3 | COMPLETE | 2026-05-17 | Backpressure, metrics, observability (29 types) — `a5d4b2a` |
| M3.5a | COMPLETE | 2026-05-18 | StateProjection vertical slice — `a2aff9c` |
| Bus-Fix Piece A | COMPLETE | 2026-05-18 | `DerivedWriteRateLimit` visibility promotion — `fceafe8` |
| M3.5b | COMPLETE | 2026-05-18 | StateProjection production persistence — `08d0136` |
| Projection-checkpoint wiring | COMPLETE | 2026-05-19 | `StateCheckpointSource` + 10 MB advisory guardrail — `56aaa4b` |
| Supervisor DLQ wiring | COMPLETE | 2026-05-19 | `SubscriberSupervisor` constructs `DeadLetter` — `ed5862c` |
| M3.4a | COMPLETE | 2026-05-19 | Integration-tests module + harness + BurstLoadIT + HeapBudgetIT — `5ae7912` |
| M3.4b | COMPLETE | 2026-05-19 | Sustained-load + crash-recovery IT tests — `adf04d2` |
| M3.6a | COMPLETE | 2026-05-20 | Profile-driven persistence config — DeploymentProfile 6 fields, LockingMode enum, profile-driven PRAGMAs — `17c40b6` |
| M3.6b | COMPLETE | 2026-05-20 | EventBusConfig record, InProcessEventBus public (DEC-M3-16), ReplayWindowQueue parameterized — `df2743a` |
| M3.6c | COMPLETE | 2026-05-20 | Per-module event-class manifests (Q3 gap closure) — `38d3e30` |
| M3.6d-a | COMPLETE | 2026-05-20 | Composition-root satellite changes — SqliteStateStore→StateCheckpointSource, QueueSaturationHealthCheck+HealthSignal+HealthLevel public (DEC-M3-17), ReadinessSource, ReconciliationTest 4/5, Tier 9 un-disabled, HomeSynapseConfig + SharedScheduler + ThrowingStateQueryService skeletons, SLF4J wiring — `25bc23b` |
| M3.6d-b | COMPLETE | 2026-05-21 | PersistenceFactory + HomeSynapseCore composition-root wiring — 4-commit cohort: WriteCoordinator.queueSize() (`a33ee40`), production SubscriberReadConnectionFactory (`a59b64e`), PersistenceFactory public gateway (`725353d`), HomeSynapseCore facade (`dfb045e`). OR-M3-14 prerequisites bundled. — `dfb045e` |
| M3.6e.1 | COMPLETE | 2026-05-22 | MaterializedStateQueryService + ReadinessFilter + RestFilters + Javalin bootstrap + DeploymentProfile thread pool sizing. 6 created, 13 modified. Two follow-up fix rounds (Xlint:exports gateway pattern, Gradle/JPMS scope alignment). 7 deviations (none blocking). Sixteenth CC WU. — `b71ed37` |
| M3.6e.2 | COMPLETE | 2026-05-22 | Admin endpoints (DlqStatusEndpoint, ProjectionStatusEndpoint) + entity query endpoints (ListEntitiesEndpoint, GetEntityEndpoint, GetEntityStateEndpoint) + EndpointContext SPI + 2 RestFilters gateway methods + 2 ArchUnit rules (QUERY_SERVICE_READ_ONLY, REST_ENDPOINTS_NO_EVENT_PUBLISHING) + HomeSynapseCore 16-step bootstrap. 15 created, 6 modified, 19 test methods. 5 deviations (none blocking). Seventeenth CC WU. M3.6 COMPLETE. — `76288af` |
| M3.7 | COMPLETE | 2026-05-27 | E2E integration tests + abandon() + checkpoint key fix + TESTING policy. M3 COMPLETE. |
| M4 scoping | COMPLETE | 2026-05-28 | PLAN-M4-CONSOLIDATED authored; canonical scope; KB de-poison applied. Next: M4.0a. |

## Code State

- **Repository:** homesynapse-core, **22 Gradle subprojects** (per `settings.gradle.kts`; source-verified 2026-06-13): ~16–17 JPMS-compiled production modules + 3 scaffold-only (platform-systemd, test-support, web-ui:dashboard) + `testing/integration-tests` (on-device ITs, classpath-only test code) + `spike:wal-validation` (throwaway spike — standing `git rm` advisory).
- **Production + test Java files:** **~803** (573 main + 230 test; +41 testFixtures ≈ 844) (source-verified 2026-06-13; supersedes the 724 count from 2026-05-28 — growth from M4.B/M4.C/M5-A/M6.1/M6.2/M6.4). Across the JPMS-compiled production modules (post-M3.6e.2 addition of `MaterializedStateQueryService`, `ReadinessFilter`, `RestFilters` in state-store/rest-api, plus `DeploymentProfile` thread-pool fields, `HomeSynapseCore` Javalin bootstrap wiring, `StateQueryService.materialized()` static factory). Test files: **~2,002 @Test/@ParameterizedTest/@RepeatedTest methods** (source-verified 2026-06-13; supersedes the 1,422 count from 2026-05-28) across ~173 test files and ~35+ testFixtures files (M3.6e.1 added: `MaterializedStateQueryServiceTest` (10 tests), `ReadinessFilterTest` (8 tests), `DeploymentProfileTest` (4 tests), `HomeSynapseCoreTest` stateQueryService test, `StateQueryServiceTest` updated assertion).
- **Event-bus module (post-M3.6d-a):** 19 public + 14 package-private = 33 top-level types. M3.6d-a promoted `QueueSaturationHealthCheck`, `HealthSignal`, `HealthLevel` to public (DEC-M3-17 — transitive 3-type chain because the public constructor's `Consumer<HealthSignal>` parameter would have triggered `-Xlint:exports` otherwise). M3.6b added `EventBusConfig` (public record, 2 fields: `replayQueueCapacity`, `publisherBlockedDepthThreshold`; `HOME_DEFAULT` constant). `InProcessEventBus` promoted to `public` (DEC-M3-16); new canonical 7-arg constructor accepting `EventBusConfig`. `PUBLISHER_BLOCKED_DEPTH_THRESHOLD` replaced by instance field from config. `ReplayWindowQueue` capacity parameterized (default 10,000 via no-arg; custom via `ReplayWindowQueue(int)`). `InProcessEventBusFactory` gained `createWithConfig(...)`. `EventBus` interface has 8 methods (4 abstract Phase 2, 4 default M3.1). `BusMetrics` interface (7 canonical metric names) with `BusMetricsJfr` JFR-native implementation.
- **Persistence module (post-M3.6d-b):** 48 types (was 45). M3.6d-b added `PersistenceFactory` (public final class — static `start()` factory, 8 store/infrastructure accessors returning public interface types, `AutoCloseable`), `SqliteSubscriberReadConnectionFactory` (package-private — production impl of `SubscriberReadConnectionFactory`, creates per-subscriber dedicated platform-thread executor + SQLite read connection; INV-SUB-ISO-02), `SqliteSubscriberReadExecutor` (package-private — per-subscriber read executor backed by a single platform thread and dedicated `Connection`; close() shuts down executor and connection). `WriteCoordinator` interface gained `int queueSize()` (DEC-M3-14); `PlatformThreadWriteCoordinator` implements it by exposing the bounded executor's queue size. `SqlitePersistenceLifecycle` expanded from 4-store to 6-store construction: now also constructs `SqliteStateStore` + `SqliteDeadLetterStore` during `start()`, with a separate `Include.ALWAYS`-configured `ObjectMapper` for `CheckpointSerializer` (distinct from the `NON_NULL` `PersistenceObjectMapper.create()` used for event payloads). New accessors: `stateStore()`, `deadLetterStore()`, `subscriberReadConnectionFactory()`. Module-info gained no new directives (all dependencies already present from M3.6d-a). `SqliteStateStore` now `implements StateCheckpointSource` (method renamed `serialize(int)` → `serializeCheckpoint(int)`, both `serializeCheckpoint` and `loadedProjectionVersion()` promoted to public via interface; class itself remains package-private — only the two interface methods are externally visible). M3.5b added `SqliteStateStore` (ConcurrentHashMap-backed materialized view + checkpoint-driven recovery), `SqliteDeadLetterStore` (UPSERT on (subscriber_id, event_position)), `CheckpointSerializer`, `CheckpointData`, `AtomicCheckpointWriter.writeAtomicCheckpointWithDlqPark()` (three-way atomic write — subscriber checkpoint + view checkpoint + DLQ park), V004 DLQ operational indices migration. Two ObjectMapper configurations coexist: `PersistenceObjectMapper.create()` returns `NON_NULL` for event payloads; checkpoint serialization requires `ALWAYS` and is constructed at composition time (composition-root wiring lands in M3.6d-b).
- **State-store module (post-M3.6e.1):** 20 public + 2 package-private types (M3.6e.1 added `MaterializedStateQueryService` — package-private final, backed by `StateStore`, recomputes stale at read time via injected `Clock`; `StateQueryService.materialized(StateStore, ReadinessSource, Clock, LongSupplier)` public static factory exposes construction surface per DEC-M3-16 pattern). `ReadinessSource` public interface (single method `mode() → SubscriberMode`). Projection-checkpoint wiring's `StateCheckpointSource` interface in scope. Advisory 10 MB checkpoint-size guardrail enforced.
- **Lifecycle module (post-M3.6e.2):** 8 public + 2 package-private types. M3.6e.2 expanded `HomeSynapseCore`: bootstrap sequence now 16 steps (14 from M3.6e.1 + 2 from M3.6e.2: entity query endpoint registration via `RestFilters.addEntityEndpoints` + admin endpoint registration via `RestFilters.installAdminEndpoints`). M3.6e.1 added steps 11-12 (added step 11: `MaterializedStateQueryService` wiring via `StateQueryService.materialized(stateStore, this, clock, () -> stateProjection.cursorPosition())`, step 12: Javalin server construction with `QueuedThreadPool(profile.javalinMaxThreads(), profile.javalinMinThreads())` + `RestFilters.installReadinessGate(app, this)` + `app.start(7070)`). `stateQueryService()` now returns the real `MaterializedStateQueryService` (replaces `ThrowingStateQueryService` placeholder). Module-info gained `requires com.homesynapse.api.rest`, `requires io.javalin`, `requires org.eclipse.jetty.util`. `DeploymentProfile` gained `javalinMinThreads()`/`javalinMaxThreads()` accessors for Javalin thread pool sizing: STUDIO(1/4), HOME(2/8), PERFORMANCE(4/16).
- **testing/integration-tests module (M3.4a + M3.4b):** Module #20. Tests gated on `-PpiProfile` (off by default; excluded from `./gradlew check`). Five IT tests: `BurstLoadIT`, `HeapBudgetIT` (M3.4a), `Pi4SustainedLoadIT`, `Pi4D1SpikeIT`, `CrashRecoveryIT` (M3.4b). `IntegrationTestHarness` composes `PersistenceTestHarness` + `InProcessEventBusFactory` against a real `@TempDir` SQLite file, with `startThrottled()` and `startForCrashSimulation()` factories (M3.4b). JVM constraints: `-Xmx256m -Xms256m -XX:ActiveProcessorCount=4 -XX:+UseG1GC -XX:MaxGCPauseMillis=100`.
- **MODULE_CONTEXT.md:** 20 files exist total — 17 populated substantively (including `testing/integration-tests` populated during WUCP Phase 2 reconciliation), 3 stubs (platform-systemd, dashboard, test-support). `core/event-bus/MODULE_CONTEXT.md` updated in M3.6b (EventBusConfig, InProcessEventBus public + 7-arg constructor, ReplayWindowQueue parameterized, InProcessEventBusFactory.createWithConfig). `core/persistence/MODULE_CONTEXT.md` updated in M3.6a (DeploymentProfile 3→6 fields, DatabaseExecutor accepts profile, LockingMode enum, PersistenceLifecycle Javadoc cleaned). `core/state-store/MODULE_CONTEXT.md` updated in projection-checkpoint wiring (added `StateCheckpointSource`, byte[0] stub deprecation note, projectionVersion authoritative source).
- **Rest-api module (post-M3.6e.2):** 28 Phase 2 public types + 2 M3.6e.1 types + 8 M3.6e.2 package-private types = 38 Java files. M3.6e.2 added `EndpointContext` (package-private SPI interface), `JavalinEndpointContext` (adapter), `EndpointResponses` (utility), `ListEntitiesEndpoint`, `GetEntityEndpoint`, `GetEntityStateEndpoint`, `DlqStatusEndpoint`, `ProjectionStatusEndpoint` (all package-private endpoint handlers). `RestFilters` gained 2 new public methods (`addEntityEndpoints`, `installAdminEndpoints`). M3.6e.1 added `ReadinessFilter` (package-private — implements `io.javalin.http.Handler`, 503 rejection with RFC 9457 problem detail body + `X-HomeSynapse-Projection-State` + `Retry-After: 5` headers) and `RestFilters` (public final utility — `installReadinessGate(Object javalinApp, ReadinessSource)` with deliberate `Object` typing to erase Javalin from the public API surface per DEC-M3-16). Module-info gained `requires transitive com.homesynapse.state`, `requires com.homesynapse.event.bus`, `requires io.javalin`, `requires org.slf4j`. Build.gradle.kts: `implementation` → `api` for state-store (fixes JPMS scope mismatch — websocket-api `requires transitive com.homesynapse.api.rest` needs state-store transitively visible). `ProblemType` enum gained `STATE_STORE_REPLAYING` (D-7).
- **Tests:** **~2,002** @Test methods total (source-verified 2026-06-13; supersedes 1,422 from 2026-05-28). Contract test suites: `EventStoreContractTest` (27), `EventBusContractTest` (50), `WriteCoordinatorContractTest` (11), `CheckpointStoreContractTest` (9), `ViewCheckpointStoreContractTest` (10), `DeadLetterStoreContractTest` (10). M3.6e.1 added `MaterializedStateQueryServiceTest` (10 — 4 staleness-recomputation, 3 snapshot variants, 1 unmodifiable-map, 1 view-position, 1 isReady-delegation), `ReadinessFilterTest` (8 — LIVE pass-through, 3 non-LIVE 503 with diagnostic headers, RFC 9457 body shape, mode-freshness), `DeploymentProfileTest` (4 — all 3 profiles + min≤max invariant), `StateQueryServiceTest` assertion updated to 6 methods (D-3 static factory). M3.6d-a added `ReconciliationTest` (4 of 5 methods — OR-M3-13) + `SharedSchedulerTest` (5). Prior: M3.5b added `AtomicCheckpointWriterDlqTest` (3), `CheckpointSerializerTest` (12+), `SqliteStateStoreTest` (12). Supervisor DLQ wiring added `SubscriberSupervisorTest` (12). M3.4a added `BurstLoadIT` + `HeapBudgetIT` (gated). M3.4b added `ThrottledWriteCoordinatorTest` (9) + `Pi4SustainedLoadIT` + `Pi4D1SpikeIT` + `CrashRecoveryIT` (gated).
- **Traceability:** 01-event-model.md (44 entries), 12-lifecycle.md (2 entries). Stub indexes remain for docs 02–11, 13, 14 — Phase 2 traceability debt carries into Phase 3.
- **Last build gate:** RESOLVED. Full `./gradlew check` GREEN (139 tasks) at HEAD `98f705b` on 2026-05-30 (M4.0b-3, AMD-51 typed comparator + `projectionVersion` 2→3; `:core:device-model:check` + `:core:state-store:check` + `:lifecycle:lifecycle:check` GREEN). Build confirmed by Nick. The `:testing:integration-tests:test -PpiProfile=throttled` run was not held to completion — `Pi4D1SpikeIT` is a fixed 30-min soak (not a hang), excluded from the default `check`, and AMD-51 does not touch the path it exercises; not a blocker. **No deferred build gates open.** Prior: `60b4185` M4.B3, `7610296` M4.0b-2 — both GREEN.

## Active Work

- **Last completed:** M3.6e.2 via Claude Code (2026-05-22, `76288af`). M3.6 COMPLETE — all seven sub-WUs shipped. Seventeen CC WUs total.
- **Next:** M3.7 scoping via research pipeline. E2E integration tests. Estimated Coder time: 6–8h.
- **Claude Code workflow validated:** acceptEdits mode, Opus 4.7 xhigh, deny git commit/push/gradlew. PM generates task instruction → Claude Code executes → Nick reviews git diff, runs build gate, commits. Through M3.6e.2, seventeen Claude Code WUs executed via this workflow. Bus-Fix Piece A and SLF4J follow-up patch were direct edits by Nick.

## Blocking Issues

None. No unresolved deferred build gates. All M3.6a through M3.6e.1 build gates GREEN at commit time.

## Open Risks

- **OR-M3-17 — FULLY CLOSED (M4.0b-1, 2026-05-29, `cf1a97e`).** The no-op `MINIMAL_DERIVATION_RULE` lambda (the M3.7 placeholder that closed the *symptom*) is now replaced by the production `ProductionDerivationRule` (package-private in `com.homesynapse.state`, reached via `DerivationRule.production()`), which publishes a real `state_changed` on a canonical-attribute change. Real derivation has shipped per DEC-M3-10 + REC-28.
- **OR-M3-18 — FULLY CLOSED (M4.0b-1, 2026-05-29, `cf1a97e`).** The `MinimalProjectionAdvancer` placeholder is removed and replaced by `DispatchingProjectionAdvancer` (REC-28 — package-private, constructor-injected `EnvelopeHandler` map, no `ServiceLoader`, forward-all with exact cursor parity). The advancer class file was deleted from `lifecycle`.
- **OR-M3-12 — DEC-M3-17 governance entry (NEW 2026-05-20):** RESOLVED in this WUCP Phase 2 closeout. HealthSignal + HealthLevel public promotion alongside QueueSaturationHealthCheck logged as DEC-M3-17; entries appended to Current_State §3 ledger, Locked_Decisions Phase 3 milestone section, and `context/decisions/phase-3-cross-module-decisions.md`. No further action.
- **OR-M3-13 — ReconciliationRecordsMetadataInDataSlot — RESOLVED (M4.0a, 2026-05-29, `a441fdf`).** Reconciliation metadata (`reconciledAt`/`reconciledFromVersion`/`reconciledToVersion`) is now populated via the extended `StateCheckpointSource.serializeCheckpoint(...)` surface, threaded through `StateProjection.initialize()`'s version-mismatch reconciliation branch; `SqliteStateStore` writes the values instead of `null`. `ReconciliationTest`'s deferred 5th method (`reconciliationRecordsMetadataInDataSlot`) is un-deferred and passing — it asserts `reconciledToVersion == 2`, the value M4.0b's backfill gate binds to. AMD-41 §3.2.4's metadata-recording requirement is now met.
- **OR-M3-14 — M3.6d-b prerequisite infrastructure (NEW 2026-05-20):** RESOLVED 2026-05-21. All three prerequisite pieces shipped in the M3.6d-b 4-commit cohort: (1) `WriteCoordinator.queueSize()` at `a33ee40`; (2) production `SqliteSubscriberReadConnectionFactory` at `a59b64e`; (3) `SqlitePersistenceLifecycle` 6-store expansion + `PersistenceFactory` public gateway at `725353d`. `HomeSynapseCore` composition root completed at `dfb045e`. Build GREEN.

## Tracked Gaps (carry forward)

1. **Tier 9 `reconciliationOnVersionMismatch` test still `@Disabled("M3.5a")`** — re-enablement was split out of Bus-Fix Piece A. Now tracked under M3.6 lifecycle wiring (depends on `bus.resume()` VT re-spawn fix).
2. **`bus.resume()` does not re-spawn VT** — pre-existing M3.1 limitation; deferred to M3.6 lifecycle wiring.
3. **Overflow test slow (~5-15s)** — consider `@Tag("slow")` if suite time grows.
4. **JFR-native emission is accepted design debt** — pull-based metrics consumer (Prometheus/OTLP) will need typed adapter layer in M4+.
5. **Publish-latency metric is bus-side only** — end-to-end publish latency is a persistence-module metric for future observability pass.
6. **`lagEvents` approximation** — uses `pendingPositions.size()` instead of writer-tail-minus-delivered-position. To be revisited under M3.4b Pi4SustainedLoadIT if accuracy proves insufficient.
7. ~~**Composition root for ALWAYS-configured ObjectMapper**~~ — **CLOSED (M3.6d-b `725353d`).** `SqlitePersistenceLifecycle.start()` now constructs a separate `ALWAYS`-configured `ObjectMapper` for `CheckpointSerializer`, distinct from `PersistenceObjectMapper.create()` (`NON_NULL`). Wired through `PersistenceFactory`.
8. ~~**`QueueSaturationHealthCheck` and `DerivedWriteRateLimit` lifecycle wiring deferred**~~ — **CLOSED (M3.6d-b `dfb045e`).** `HomeSynapseCore.start()` constructs both and passes them to `SharedScheduler`.
9. ~~**`DerivedWriteRateLimit.refill()` externally called**~~ — **CLOSED (M3.6d-b `dfb045e`).** `SharedScheduler` calls `refill()` every 50 ms via `safelyInvoke(rateLimit::refill)`.
10. ~~**Defence-in-depth `IllegalStateException` for `EventPublisher.publish()` from REPLAY**~~ — **CLOSED (M3.6d-b `dfb045e`).** `HomeSynapseCore` subscribes the projection with `SubscriptionFilter.all()` + `coalesceExempt=true`; the bus's mode FSM prevents publish calls during REPLAY. The composition root wiring itself is the defence.
11. **Post-shutdown defensive handling in stores** — `SqliteDeadLetterStore` (and siblings) will throw uncaught exceptions if methods are called after `DatabaseExecutor.shutdown()`. Pre-existing pattern, not an M3.5b regression. Track for a future hardening pass.
12. ~~**`AtomicCheckpointWriter` code duplication**~~ — **CLOSED (M4.0a / H2, `a441fdf`).** Shared `executeInTransaction(context, work)` helper extracted; both `writeAtomicCheckpoint` and `writeAtomicCheckpointWithDlqPark` route through it.

## Schedule Position

**Significantly ahead of Master Release Plan.** The 37-week plan placed Weeks 1–10 as "Interface Specification" — the project finished that work in 7 days (Mar 14–20). Phase 3 persistence subsystem (M2.x) complete. Event bus production implementation with full REPLAY→LIVE algorithm + backpressure + persistent DLQ + state projection + integration tests (including sustained-load and crash-recovery) all landed in the M3.1–M3.4b window (May 16–19). See the Phase 3 Progress Annotation in `master-release-plan.md` for actual vs planned dates.

## Next on Critical Path

1. **M4 next-part scope/plan** (fresh Cowork conversation) — formally define the scope of the next M4 segment: **Workstream B** device-model breadth (AMD-44 Floor/EntityRole impl, Research 8 REC-23–30) and **Workstream C** integration-api interface freeze (Research 6 REC-41–51, P3-gated on Nick's NQ-1..6), plus the **AMD-52** typed-`StateChangedEvent` serializer/replay design beat (OQ-05-08). Surface deep-research needs (e.g. Axon and other event-sourced platforms) to de-risk scale/maintainability — grounded in concrete forward design questions, not invented problems.
2. ~~**AMD-51 §2.6 erratum**~~ — **DONE 2026-05-31** (docs repo). The missing-schema → `StringValue` string-compare fallback (M4.0b-3 D-1) is recorded as the ratified no-schema behaviour in AMD-51 §2.6; the amendment now matches shipped code. (Commit pending with the docs-repo warm-up batch.) Also DONE: the KB doc-currency punch-list (0a-ii) — `HomeSynapse_Current_State`/`Knowledge_Primer`/`Navigation_Index`/`Decisions_Quick_Reference` brought current to `98f705b`/`projectionVersion` 3/watermark AMD-51.
3. **Phase 2 traceability debt** — 10 stub indexes remain (docs 02–11, 13, 14); state-store/device-model M4.0b-3 entries also pending. Low priority; batch later.

## Recent Session Log

| Date | Agent | What Happened |
|------|-------|---------------|
| 2026-06-26 (v7 beat 12 — §3 decisions ruled + governance closed + M7.4a issued) | PM (Cowork) + Nick | **§3 CONSOLIDATED DECISION PASS: 12 decisions, Nick co-signed all four groups as recommended.** Folded on disk (pending Nick's commit): **AMD-95 RATIFIED** (Doc 07 §3.11 reconciled + AMD-90 §11; watermark 94→95); **Doc 17 LOCKED + AIOT-INV-1 minted** (INV-AC §50; 170/50; AIOT-INV-2/3 principles); **Wave-1 reconciliation record + AMD-96 PROPOSED** (Doc 02/08 currency: White/CT, Kelvin-canonical, EZSP-v14, occupancy binding); **lane-integration note** (C8 `name` field folded into v1.1; C9 Doc 13 currency; C10 CI drops=Nick; C11 E2 bind-host Core WU ahead of M9 wizard +E1/E3/E6; C12 doctrine shared-home); **M7.4a ISSUE-READY** (producer + co-located dispatch subscriber + paired teardown). D-07-A deferred to M8. Nick's next: commit + the 2 CI drops + route M7.4a. |
| 2026-06-26 (v7 hub launch + preflight reconcile) | PM (Cowork) | **v7 mission-control hub LAUNCHED (supersedes the token-heavy v6).** Ground: env-model → V1 record + v6 forward plan + §1 record → 11-check preflight = **STALE-benign, zero CONFLICTED** (Checks 1/3 snapshot currency-lag behind beats 8-11; Check 8 cross-agent-note archival; **Check 9 PASS** — mirrors byte-identical). Baselines CONFIRMED (core `b296e76` / docs `f54d0e0` / hivemind `cd8b046` = the masthead's "+ beat-11 reconcile + v7 land on top"). Reconciled this snapshot (masthead/Latest-commits/this row + the v7-launch top note) + archived the resolved M7.3/M7.2b cross-agent notes → PASS. **Next (the v7 hub's first substantive act): the §3 consolidated decision pass (12 decisions, Nick rules) → governance close-out → M7.4a.** Flagged the harmless 0-byte `nexsys-hivemind/.git/index.lock.cleared-by-cowork-2026-06-26` leftover (no active lock anywhere; Nick deletes at leisure). |
| 2026-06-26 (beat 11 — Track-A first returns + decisions consolidated) | PM (Cowork v6) | **The development fleet's first beat returned** (all write-isolated; spine untouched). Frontend-dev: buildable Preact+TS SPA (`web-ui/dashboard/`, 65 files), both hero halves over the frozen v1.1 contract, mock seam, 1-2s polling, verified GREEN. Distribution: the one-command-install skeleton (`distribution/`, 23 files) — jlink→systemd→`.deb`/`install.sh`→container install-smoke = mid-Aug gate #4; escalations E1-E8. Bench Session C: device-corpus knowledge layer + verdicts + MG24/EZSP-v14 fingerprint — **PHYSICAL CAPTURE PENDING (Nick's desk)**; escalations ESC-W1-HUE-01/COORD-01/SNZB03P-01. Consolidated decision set assembled → the v7 prompt §3. |
| 2026-06-26 (beat 10 — fan-out v2 reconciled) | PM (Cowork v6) | **V2-A/B/C returned, all fold cleanly (V1 scope + mid-Aug + Core critical path untouched).** V2-A (Pi spike): D1 CONFIRMED ≈1.9 ns, D2 PASS (1e6-replay zero side-effects = the M7.4b CI-gate template), D4 sized (freeze on a Pi re-run — sandbox FUSE caveat). V2-B (converter-DB pipeline): offline/human-gated/version-pinned M9-prep; standard-ZCL ~60% pure-data, code-tail ~40% via curated/community. V2-C (AIoT safety frame): planner→verifier→deterministic-executor is the 2026 consensus; the moat = AI-as-author emits `AutomationComponent`s the `AutomationLinter` statically rejects (E3 structural proposer-only port); AX-7 a hard prerequisite; honest claim language set; Q1 patent search before any superlative. SafeGate citation quarantined. |
| 2026-06-26 (beat 9 — fan-out reconcile + D5 RATIFIED + AMD-95) | PM (Cowork v6) | **Session A → D5 RATIFIED** (adapt-the-data + curated fallback; reject runtime-interop/rebuild; licenses MIT/MIT/Apache-2.0 file-verified; the GPL is the Z2M *app*, not consumed; LOW-risk legal spot-check commissioned; M9 scoping unblocks). Session B (why-not moat delta — claim the *durable plain-language* projection, not confirmation). Session C/D escalated/reframed to the bench. **AMD-95 + a doc-currency Cowork review authored** (Tier 1 AMD-95 ratify-readiness; Tier 2 doc-vs-source currency sweep). |
| 2026-06-25 (beat 8 — v6 hub launch; §1 RATIFIED; Doc 17 DRAFTED) | PM (Cowork v6) | **§1 deeper-M7 architecture RATIFIED (Nick co-signed):** D1 event-driven/co-located dispatch (supersedes the v5 emit+in-process hybrid) · D2 pure-function-replay (first-class + CI test) · D3 additive event versioning · D4 log retention/snapshot · D5 HELD-pending-Session-A · Q2/Q5 substrate confirmations. **Doc 17 (AIoT + Cloud Readiness) DRAFTED** (review-candidate; runs the Doc 16 review→Lock pipeline; 3 PROPOSED AIOT-INV candidates). **AMD-95 (PROPOSED)** authored — the Doc 07 §3.11/AMD-90 command-pipeline currency reconciliation (adopt the event-driven shape; corrects 3 source drifts; ZERO source/event/module-info change; authors before M7.4a). |
| 2026-06-22 (M7.3 closeout + live-spine-next ruling) | PM (Cowork) | **M7.3 (PendingCommandLedger) DELIVERED GREEN + WUCP Phase 2 APPROVE; gate-fix; re-sequenced to live-spine-first.** Coder built the ledger; `./gradlew check` first came back RED — **the premature live `pending_command_ledger` subscriber (a +19-line composition-root block) registered with NO `stop()` teardown → its SQLite read connection held the temp DB open → JUnit `@TempDir` cleanup failed on Windows → all 15 composition-root tests failed at teardown** (the ledger's own `core:automation` tests passed). Diagnosed from the test-results XML; **fix = defer the live wiring** (revert the 19 lines; the producers/tick aren't built — the M7.1 precedent). Re-run **GREEN (149)**. WUCP Phase 2 → APPROVE; 6 source-drift [REVIEW]s ACCEPT (built against the frozen tree). **Nick RULED "live spine first"** → **M7.4 (command-pipeline bootstrap + E2E test) re-sequenced BEFORE the thin causal-read-API.** Lessons folded; Doc 07 §3.11 / AMD-90 doc-vs-source drift flagged for a currency amendment. Next: scope M7.4 (likely split). |
| 2026-06-22 (v5 hub launch + first-act hygiene + M7.3 issued) | PM (Cowork) | **v5 mission-control hub LAUNCHED (supersedes the token-saturated v4) + §9.A hygiene + preflight + M7.3 ISSUED.** Hygiene (host-side / git-object-sourced; phantom active): PROJECT_SNAPSHOT 88→69KB + pm-handoff 59→21KB rotated (priors → `…/archive/…-priors-rotated-2026-06-22.md` / `…-rotated-2026-06-22.md`); skill folds — record-collision STOP-check → `coding-instruction-format.md`, 4 v4-lessons → `pm-lessons.md`, both `SKILL.md` mastheads bumped → **Check 9 STALE pending Nick's mirror sync**. Preflight = **STALE-benign** (Check 9 + currency nits reconciled here: this row, W26→IN_PROGRESS, backlog v5 note), **zero CONFLICTED**; core `1b0b6c9` M7.2b reconciled (counts 71/41/53; M7.2b/M7.2a-2 types source-verified, no fabrication). **M7.3 (PendingCommandLedger) coding instruction ISSUE-READY** (`context/instructions/2026-06-22_M7.3_pending-command-ledger_coding-instruction.md`) — source-verified end-to-end; **caught + corrected the dispatch-prompt's "M7.3 adds event types" vs ratified AMD-92's ZERO-mints** (counts stay 71/41/53; no module-info change) → authored to AMD-92, flagged to Nick (+ the per-device-class timeout-default scope question). Next: Nick commits (selective-stage, phantom-aware) + mirror sync + routes M7.3 to the Coder. |
| 2026-06-19 (M7-forward plan + M7.1 second-check) | PM (Cowork) | **Preflight PASS** (core `beb4bc3` / docs `32afb3f` Doc-16-DRAFT / hivemind `ec9905e`; truncated-tail artifact host-cleared; zero CONFLICTED). **Part A — M7.1 second-check = PASS-WITH-FINDINGS:** closeout sound (all primary records agree; watermark AMD-93, no new AMD); 3 deferrals homed (wiring→AB-3, FIX-07 lesson, `AutomationSchema`); 6 [REVIEW] adjudications hold (3 behavioral defaults tracked as M7.2 carry-items, none silent); source round-trip clean (`evaluate`→`List<AutomationId>`; +7 EventTypes/24→32 classes/36→44 rows verified; M7.2 owes rows 2/4–10/17/18; no fabrication). Findings F1 (coder-handoff gate-status lag) + F2 (this log lag) reconciled in-session. **Part B — M7-forward plan recorded** (`context/planning/2026-06-19_M7-forward_plan.md`): M7.2 split → M7.2a (baseline-now) + M7.2b (Lock-gated on Doc 16 + D2); M7.3 UNAFFECTED; Doc 16 review→Lock = M7.2b prereq; **sequencing rec = app-bootstrap AB-3 next** (Nick rules); M7.2 NOT dispatched (gate OPEN). Substrate gaps routed; D2/REC-162 + sequencing escalated. |
| 2026-06-18 (app-bootstrap decisions + superiority scope) | PM (Cowork) + Nick | **A1–A4 RULED** (loopback-default · token issuance · fail-closed+degrade-seam · rotate-DEK+reserve-tag via AMD-94); app-bootstrap charter finalized; AB-1..AB-4 backlog rows added. **Part B superiority scope RULED + Doc 16 Architect authoring dispatched**; M7 interlock verdict M7.1-UNAFFECTED / M7.2-SHAPED / M7.3-UNAFFECTED. |
| 2026-06-18 (converge / W25-rollover) + M7.1 landing | PM (Cowork) + Coder | **Converge STALE→PASS**; energy/erasure interview gate RETIRED; core-review C1–C15 folded (OR-APP-AUTH / OR-RF-DECRYPT). **M7.1 LANDED GREEN** (`beb4bc3`, 149 tasks; gate-fix round 1: `timer.automationId`→`entry.getKey().automationId()` + the `core→config` edge relocated; FIX-07 instruction defect corrected; 6 [REVIEW] ACCEPT; subscriber+config+lifecycle wiring→AB-3). PM WUCP Phase 2 → **M7.1 DONE**. |
| 2026-06-14 (Track-4 forward-execution) | PM (Cowork) | **Step-0 preflight STALE (currency lag only — spine HEADs/M6.3-status/2026-06-15 artifacts unregistered; NOT conflicted) → reconciled in-session to PASS.** Tree verified current: core `1eddd9a` (M6.3 committed GREEN 147) / docs `c309123` / hivemind `7ea7992`; all 8 charter artifacts present on disk. **Row-4 veto DELEGATED by Nick → PM ruled APPROVE** (`config-superiority.md`: Increment-1 acceptance met; 3 register guardrails + anti-reqs verified; INV-CE-01 blockquote verbatim-confirmed vs `Architecture_Invariants_v1.md` §8; encryption tense-truth gate cleared by M6.3; competitor-receipts + README items deferred as PUBLISH gates) → **entry-gate row 4 CLOSED**, Lane-4 Core-instruction gate lifted. Reconcile writes: snapshot masthead + Current-WU + this row; backlog M6.3→DONE `1eddd9a` / M7.1→NEXT; coder next-unit→M7.1; 2026-06-15 artifacts registered. **Forward lane (interview-independent, this session): issue M7.1 (P2 survey + carry-pins incl. red-team F7/C4 + R-δ AX-5) · land C3 · dispatch CC-1→Code, R-γ→DOCS, key-portability brief→DOCS.** Crypto/app-bootstrap PARKED behind Nick's interviews; W24→COMPLETE rollover + W26 scoping owned by the converge session. |
| 2026-06-12 late (M5-C Increment-1 brief, dated 2026-06-13; ran parallel to the R16 assessment) | PM (Cowork) + Nick | **M5-C INCREMENT 1 DONE (drafted-awaiting-veto) — the Lane-4 structural-gate deliverable on disk: `homesynapse-core-docs/website/` skeleton (README w/ register rules + page map; landing; 3 Increment-2 stubs) + THE config-superiority page draft** (INV-CE-01 verbatim spine; R13-assessment split-brain dossier; Norway-problem/validation/migration/rename/Git sections; OQ-15-2 trust point non-headline w/ exact figures; review-only provenance appendix + publish gates). **D-1..D-7 RULED: ALL ACCEPTED** (venue also ruled: core-docs/website/). R16 M5-C-COPY bucket consumed mid-session (lands-first branch; REC-194→explainability correction; "rendering, not data" guardrail registered). Self-review: 4 own-draft defects fixed (tense-truth M6.3 gate added). **Row 4 flips DONE at Nick's APPROVE of the page draft → Lane-4 Core-instruction gate lifts.** Prompt archived; single-anchor appends; Increment 2 queued (ledger-gap flagship + explainability w/ R16 receipts + no-cloud-account + 174/175/176 + D-fold mechanics). |
| 2026-06-12 late (R16-assessment brief, dated 2026-06-14; input gate satisfied early) | PM (Cowork) | **R16 ASSESSED — both returns folded B-first, zero discards: 16-B grade A, 16-A grade A−; merged disposition authored (`context/planning/2026-06-12_R16_output-contract_merged-disposition.md`); REC high-water → 215; `scripts/dev/OUTPUT_CONVENTIONS.md` DRAFTED in core (gate-free, 9 rules, Nick commits).** Quote-backs verbatim in both returns AND re-verified to ground (scripts at `01841ba` git objects; Doc 07 §11.2; `CausalContext`+module-info at `7c73c91`; DAS Register C; LTD-15; INV-TO-04; Doc 06 `config_error`); REC-204 stdout defect re-confirmed TRUE (both scripts + OQ-15-2 driver → retrofit list); REC-214 prefix non-uniformity verified TRUE/understated; externals 100% (JEP 506 + `orElse` detail, OTel v1.41.x, Anthropic 25k, WTH 219488, `stored_traces` 5, Hubitat ~1 MB); cascade seed did NOT propagate. Adjudications: B-211 vs LTD-01 (Java 21 → MDC-copy; Scoped Values trajectory-only); B-209 auto-emit dropped; A-196→A-190; A-195 single-bucketed. Disposition top-bills A's honest negative: **B3 risk = data-without-register**. Returns rehomed to docs (md5-verified); briefs + prompt archived; agenda → ASSESSED. NEW flag: docs repo staged-D + untracked re-adds ×3 (Nick's mid-operation; untouched). Buckets: SCRIPT-STANDARD→draft DONE · M12 8 obligations · M10-M13 2 parked · DOC-11 1 note · M5-C 3 (→ Increment 1, not yet run) · FUTURE 1 · REJECT 3+5 anti-requirements. |
| 2026-06-12 evening | PM (Cowork) + Nick | **THE PI-EVIDENCE SESSION: OQ-15-2 RESOLVED — `encrypted_scopes` default CONFIRMED `[identity, presence_personal]`; M6.3 gate 1-of-3 CLOSED; M7 row 3 microbench-half DELIVERED.** Guided run on `hs-dev-1`: readiness PASS (zero drift); pi4-validation.sh first exercise PASS (manual tar-pipe equivalent, 42 m, zero failures — F1/F3 script-fix advisory); microbench 2 reproducing runs ±0.5% (G1 13–37× at the real distribution; G2 49× by composition; Pi-5 intrinsics-off NON-AUTHORITATIVE accepted per R1 conditional, F4 diagnostic-flag lesson). §8 filled; disposition + harness archived; C9 §3.4 energy item RECONCILED/CLOSED; Doc 15 untouched; cold-start cipher-warmup carry pin → M6.3; M5-C Increment-1 prompt authored; index.lock saga closed. **COMMITTED+PUSHED `58794d7`.** Same evening: hot-path hygiene rotation (snapshot 107K→~59K; pm-handoff 70K→17K; git-object-sourced from `58794d7`, host-verified). |
| 2026-06-12 (later, same conversation) | PM (Cowork) + Nick | **AMD-88..93 + B2 C8/C9 RATIFIED.** Bundled DOCS review returned (grade A−; 6 required + 3 minor edits; zero verdict-affecting errors) → PM source-verified every edit (E92-3 declined-with-citation; EventOrigin misquote caught + corrected) → folds applied → Nick ratified ALL → full mechanics (invariants §42–§47, index 163/47; watermark 87→93; AMD-04 SUPERSEDED; Doc 07/Doc 01/Glossary currency edits; spine flipped). Environment incident contained: VM truncated-tail clobber of the six AMD tails — reconstructed from git `77d5e51` + the in-session fold record, host-verified. **Entry-gate rows 1–2 CLOSED; M7.1 now waits ONLY on rows 3 (ordering call) + 4 (M5-C Increment 1).** |
| 2026-06-14 brief (executed Jun 12 UTC) | PM (Cowork) | **BUNDLED-REVIEW FOLD ABORTED AT STEP-0 — the return is NOT on disk.** `context/audits/2026-06-DD_AMD-88-93_C8-C9_DOCS_Review_Return.md` absent in BOTH namespaces (host file tools = truth, VM agrees); full three-repo sweep (name patterns + mtime > Jun 10) found no candidate — only the dispatched prompt exists. Per the brief: STOP after Step-0, hygiene-only — NOTHING folded, NO mechanics, watermark untouched at AMD-87, all six AMDs stay PROPOSED. Step-0 otherwise reconciled: core `e5ea76f` ✓ (GREEN 147 / pins 55-24-36 / pv 5); hivemind HEAD `ba1466d` = the closeout commit ✓ (untracked = the Jun-14 prompt only; Jun-13 prompt archived ✓); rollover NOT passed (Jun 12 < Jun 15 — no W24/W25 flip); Check 9 STALE (the one pinned file — expected). **DIVERGENCE: docs HEAD still `ed5cf91` — the six AMD-88..93 files are UNTRACKED; Nick's AMD-block commit never landed → commit BEFORE dispatch/fold so the review baseline + fold source-verification target are one stable sha.** `index.lock.cleared-by-cowork-2026-06-12` STILL PRESENT → **FOURTH flag**. Resolution: (a) Nick saves the DOCS return verbatim to `context/audits/`, or (b) the brief's Cowork-fallback review in a SEPARATE fresh conversation — never review+fold in one. M7 research-vector advisory delivered (2 genuinely-new items: dry-run command-dispatch seam; AMD-91 chain across the command→state-change hop — see pm-handoff). Documents-only; this row + the pm-handoff record are the session's only writes. |
| 2026-06-13 (ran Jun 11 local/Jun 12 UTC) | PM (Cowork) | **M7 entry-gate AMD block DRAFTED — AMD-88..93 PROPOSED on disk** (six families per merged disposition §2a; numbers nav-index-derived; F5 type-residency = FLATTEN, both module-infos embedded verbatim; F4 formally supersedes AMD-04 w/ element ledger; 11 invariant candidates; 10 [REVIEW-POINT] flags). **Bundled DOCS review prompt READY TO DISPATCH** (block + B2 C8/C9, one dispatch — Nick veto-or-default; R14-A REC-146 Hubitat evidence handed to the C8 half). **Strategy-refresh drafts D-1..D-7 on disk** (§2f + the file-4↔file-2 reconciliation line; Nick vetoes per item). Preflight STALE (Check 9 only, expected — answer-key match); truncated-tail artifact ACTIVE both repos (host intact); Step-0 shas reconciled (`fbf3124`+`5f30ff0`/`ed5cf91`); Jun-12 prompt archived; no rollover flip; index.lock.cleared re-flagged. Documents-only — core untouched at `e5ea76f`. |
| 2026-06-12 EOD | PM (Cowork) | **R14/R15 returns ASSESSED (all 3, serialized B→A→15) + MERGED DISPOSITION + CHARTER POPULATED (documents-only).** Same-conversation continuation: Nick dispatched + returned all three briefs same-day; shas `776fe2d`/`e5ea76f` reconciled at start. **Every §0 quote-back HONORED; zero discards; zero fabricated identifiers** (both R14 runs DOCS-connector-blind, declared — the verbatim-embed discipline held; process note: fix connector reach or embed contract tables at quote granularity). **R14-B A−** (10 M7-obligations incl. monotonic-seam sustain arithmetic + storm benchmark + W1–W8 crash-window pins; 2 M8; 2 FUTURE-AMD; REJECT engine-retry) — PM source-verified Doc 07 §3.10/§3.11 (zombie-Run finalization + per-idempotency crash recovery = richer than embeds; two OPENs → CLOSED-UNTESTED). **R14-A A−** (ledger-gap dossier = M5-C flagship; REC-147 re-bucketed ALREADY-COVERED at :270; REC-141/145 narrowed — all four trace details Locked-specified, traces are log events/no ring buffer; REC-153 split). **R15 A−** (register fence HELD; PM executed the mandated in-Project re-run vs strategy files 1–5: **Apache 2.0 CONFIRMED** by fork-pattern evidence, governance refined; REC-182 → M5-C via R14-A pain-match; REC-180 → messaging rule; file-4-vs-file-2 data-monetization tension logged for the strategy refresh). **Merged disposition** (7 collisions adjudicated; **AMD block = 6 families**; M7 = 3 pieces CONFIRMED; high-water **REC-185**; anti-requirements: no engine retry / no templating DSL / no forced migration / never lead with commodity encryption). Returns archived → docs `research/returns/2026-06-12_*`; briefs → archive. **Next: draft the M7 AMD block; Nick C8 + M6.3-vs-M7 + M5-C Increment 1.** |
| 2026-06-12 | PM (Cowork) | **W0 currency delta + R14/R15 briefs AUTHORED + M7/M8 charter skeleton (documents-only).** Preflight vs the session-brief PINNED STATE: **STALE (Check 9 only — the one pinned mirror file, expected)**; Check-10 drift fixed in-session (strategic-context-map → archived `phase-2-block-backlog.md` path). Environment: VM mount served truncated file tails (phantom git modifications in BOTH repos — host files verified intact via file tools; lesson folded into `cowork-environment-model.md`); a VM-created hivemind `.git/index.lock` neutralized by rename (Nick deletes); VM died mid-session (file tools carried it). **W0:** `context/assessments/2026-06-12_W0_Research_4_Currency_Delta.md` — REC-31..40 re-anchored at `7c73c91` (all structural claims HOLD; survived-by-construction confirmed empirically); **DQ list EMPTY** (v4 resolutions re-verified); **REC-39 +5 obligations — NEW finding: the type-residency rule** (`RunId`/`RunStatus`/`PendingStatus` barred from event payloads, JPMS cycle; flatten-or-relocate = AMD-block decision); C8 = hard M7 entry-gate row. **Briefs (self-reviewed ×3):** `Research_14A_Automation_UX_Failure_Modes_Brief.md` (REC 141–155) / `Research_14B_Automation_Runtime_Robustness_Brief.md` (REC 156–170) / `Research_15_Ecosystem_Positioning_Scan_Brief.md` (REC 171–185) — all READY TO DISPATCH. Self-review caught a MODULE_CONTEXT permit-count defect (27→**30**, re-derived from source) — **corrected in core/automation/MODULE_CONTEXT.md (docs-only core change)**. **Charter skeleton:** `context/planning/2026-06-12_M7-M8-charter-skeleton.md` (3 M7.x rows; 5-row entry-gate incl. the M6.3-vs-M7 ordering call at W25/W26; 5 carry pins). Step-0: consumed session prompt archived; no rollover flip (pre-Jun-14). **Next: Nick veto-or-default + dispatch all 3.** |
| 2026-06-10 (follow-up) | PM (Cowork) | **Sha-reconciliation + WUCP hygiene + Research 13 scoping.** Preflight STALE (Checks 3/4 — the documented sha-pending state) → Step-0 reconciliation: M6.1a **`9035110`** (git log HEAD; hivemind closeout `40de3ec` confirmed committed) filled into coder-handoff/pm-handoff/snapshot/backlog/charter/W24/pointer; M6.1 gate entries CLOSED. Archival: cross-agent trail rotated (CURRENT POINTER only); M6.1 instruction archived (+EXECUTION RECORD; tombstone pending Nick `git rm`); pm-handoff pre-M5-window records → monthly archives. Docs-lane corrections ×4 applied (Doc 06 §3.2/§3.7, Doc 01 §4.4, cloud-scalability :56; Doc 15 untouched). Preflight re-run **PASS** (Check 9 evidence-based — VM down). **Research 13 brief authored** (config market-superiority gap analysis, REC-130+, disposition-table mandate, DOCS-Project dispatch; renumbered from the task brief's "Research 12" per Nick — append-only register). config MODULE_CONTEXT M6.1 status line brought current (tiny core docs commit). |
| 2026-06-10 EOD | PM (Cowork) | **M6.1 COMPLETE (whole).** M6.1a executed (Claude Code); gate FAILED 2× → PM source-diagnosed both (CoreSchema vs engine-default JSON schema; Rule 5 stale vs ratified AMD-71) → gate-fix round 1 → **GREEN (27 + 147 tasks)** → Phase 2 APPROVE (module-info = ruling exactly). R1/R2/R3 ACCEPT; doc carries routed; M6.1a committed **`9035110`** (sha reconciled at the same-day follow-up cleanup session). |
| 2026-06-10 | PM (Cowork) | **M6.1b WUCP Phase 2 → APPROVE** (source-verified vs AMD-66/67/70, not the report; config `module-info` byte-identical; gate GREEN 57+147 tasks, Nick). **M6.1a escalation UPHELD + RULED (Nick):** 5 third-party non-transitive `requires` APPROVED + 2 Gradle lines; DP-4 → `List` ctor; AMD-66/67/68/70/71 §7 ruling-corrections + AMD-66 §2.4 correction authored (docs repo). RULING ADDENDUM appended to the M6.1 instruction; M6.1a ready to issue. Commit messages handed over (3 repos). |
| 2026-06-09 | PM (Cowork) | **AMD-66..71 RATIFIED/DEFERRED — M6 ENTRY-GATE CLOSED (documents-only; Mode-1 governance mechanics).** Preflight PASS (all 11; baselines core `6c6dd33` / docs `aedff55` / hivemind `da9d2f4`; Check 9 mirrors clean; sandbox served a 0-byte review return + truncated AMDs → file tool authoritative throughout, NO sandbox git writes per the standing §1.1 hazard). Executed the Nick-authorized ratification: **AMD-66 RATIFIED (RATIFY-AS-IS, [AMD-66-A] ENDORSED); AMD-67/68/70/71 RATIFIED (RATIFY-WITH-EDITS — all 7 edits pre-folded at docs `aedff55`, nothing further folded); AMD-69 DEFERRED — Option (a) confirmed (Tier-2/OQ-15-3; number reserved; OQ-15-3 stays closed).** Mechanics: 6 mastheads + §9/§10 (§7/§8 for 69) flipped with the AMD-54 §10 pattern; **§37–§41 registered** (8 invariants verbatim from the AMD §7s; §37 preamble carries the block + deferral note) + §0.3 (+5 families) + §17 (+8 rows; **total REGENERATED from the table: 152 invariants/41 identifier categories** — the stated "135/34" had drifted −9, flagged) + §18 (+5 rows) same-commit; nav-index +6 rows/preamble/Doc 06 note, **watermark unchanged AMD-87**; M6.1 ⛔→**READY TO ISSUE** (numbers pinned); charter entry-gate CLOSED + AMD-68 half of M6.2 satisfied; backlog/snapshot/handoffs/W24/KB updated; §7 module-info embeds re-confirmed byte-identical vs `config/configuration` source at `6c6dd33`; E70-1 flattened payloads verified intact post-flip; both consumed instruction prompts archived. M6.1 NOT issued (next session). Untracked `coder.zip` + `project-manager.zip` in hivemind root flagged to Nick (sync residue — not deleted). |
| 2026-06-09 | PM (Cowork) | **AMD-66–71 DOCS review RETURNED → 7 edits FOLDED (documents-only).** Nick ran the AMD-66–71 DOCS-Project review → **RATIFY-WITH-EDITS, no rejects** (66 RATIFY-AS-IS, 67/68/70/71 RATIFY-WITH-EDITS, 69 CONFIRM-DEFERRAL). PM **independently source-verified the three load-bearing finds at `6c6dd33`** before folding (not from the summary): **E70-1 — REAL JPMS cycle** (config-event payloads specified with config-module types `Severity`/`ReloadResult`/`ReloadClassification` in event-resident records → `event→config`, the AMD-52 class — confirmed: `config requires transitive event`, event-model requires only `value`+`platform`; existing `ConfigChangedEvent` is all-String) → flattened to event-resident types + a type-residency rule; **E70-2** — `secret_added`/`secret_removed` don't exist in source (only `CONFIG_CHANGED`/`CONFIG_ERROR`) → corrected; **secrets.enc** — both Locked docs say `secrets.enc` (encrypted JSON), not `secrets.yaml.enc` → fixed in AMD-68/71. Folded all 7 (E67-1/2 prose-accuracy, E68-1, E70-1/2, E71-1/2 incl. [AMD-71-A]→Path-injection confirming M6.1 DP-3); flipped each amendment's frontmatter to its verdict; placed the review return on disk (`context/audits/2026-06-09_AMD-66-71_DOCS_Review_Return.md`); updated pm-handoff/snapshot/pointer. Delivered the validity+scalability assessment to Nick. **Awaiting Nick's ratification** → then invariant registration + nav-index rows + M6.1 gate-lift. homesynapse-core untouched (HEAD `6c6dd33`); Locked Doc 15 inviolate. |
| 2026-06-08 | PM (Cowork) | **M6 entry-gate (config side) AUTHORED + B2 schema decisions (documents-only).** Preflight **PASS (all 11)** — source-verified vs repos; Check 3 noted HEAD `6c6dd33` = a 1-line `Main.java` print-string typo fix on `7f44bed` (`com.homesynapse.app`, whitelisted; immaterial — latest-commit field bumped); backlog/coder-handoff masthead-currency nits folded. Authored **AMD-66–71** (M6 config block, PROPOSED, in `homesynapse-core-docs/design/amendments/`): **66** `ConfigurationChangeListener` (REC-55, F7-corrected non-generic/non-sealed; PROCESS_RESTART no-listener default); **67** config-doc schema `(major,minor)` (REC-56 — **REC-41 blocker CLEARED by ratified AMD-54**, distinct-surface guard); **68** `SecretStore.setAll(Map)` atomic durable write (the Doc 06 currency amendment — Doc 15 §7.3 + AMD-60-INV-03; **REC-57 `SecureCredentialBundle`/`credentialsFor` RETIRED by ratified AMD-60** R7/A5); **69 DEFERRED to Tier-2/OQ-15-3 + ESCALATED** (REC-58 Argon2id/BouncyCastle conflicts with Locked Doc 15 §2.3/§3.5/§3.8 — MVP roots machine-local, zero new deps, provider open); **70** config observability events (`config.validation_completed`/`config.section_reloaded`, in `com.homesynapse.event`); **71** hybrid config dir layout (path-traversal guard). + the **AMD-66–71 DOCS-Project review prompt**; the **M6 multi-piece charter** (M6.1 config pipeline / M6.2 secret-store+key-mgmt / M6.3 at-rest GATED / M6.4 hot-reload — E2 `com.homesynapse.app` bridge + OR-M6-NONCE embedded verbatim); the **gated M6.1 coding instruction** (⛔ on AMD-66/67/70/71 ratification; verbatim config module-info; P2 consumer/pin survey; §4c Clock reminder); the **B2 schema decisions C8 `actorRef` semantics + C9 energy event shape** (contract-freeze-ready, independent-review-bound). homesynapse-core untouched (HEAD `6c6dd33`). Commit messages handed to Nick. |
| 2026-06-07 | PM (Cowork) | **M5-A COMMITTED `8028337` (Step 0 sha-reconciliation) + M5-window review source-verification → STOP-on-missing-input.** Preflight **STALE** (Check 1 footer/session-log lag, Check 3 snapshot `Latest commit` behind HEAD, Check 4 backlog masthead, Check 8 pointer) → Step 0 retroactive WUCP Phase 2 sha-reconciliation: `8ef9e9f`/"commit pending" → committed **`8028337`** across snapshot/pm-handoff/coder-handoff gate-flip/backlog/W24 masthead/cross-agent pointer → **PASS restored**. Source-verified (Read tool, HEAD `8028337`) ALL foldable edits ahead of the fold: **Doc 15 E1** — re-derived Root→KEK→DEK from §3.1/§3.4/§4.2; the `encrypted_kek`→`encrypted_dek` rename has **THREE** occurrences (§4.1 L186 name+comment, **§4.2 L204 — NOT in the on-disk E1 summaries**, §8.2 L289 `ScopeKey.encryptedKek`) and the §4.1 comment is a *semantic* fix ("scope KEK wrapped under the root key"→"scope DEK wrapped under the scope KEK"); **Doc 15 E2** — real graph confirms config↔persistence have no edge either way at HEAD; PM chooses the **composition-root adapter bridge** (neither module references the other; robust to a future persistence→config need) over the one-way edge; **AMD-87 E1/E2/E3** all confirmed vs source (§7 "65"; HEAD annotation literally `@Disabled("AMD-65 pending")`; INV-GA-02 governs INV-XX-NN only); **Part 2** re-verified vs current tree — persistence→device acyclic, 4 permit shapes match `Expectation.java` exactly, @Disabled present. **STOPPED twice on the missing full review return** (not in message / not on disk) rather than work from the summary. **THEN (same session) Nick placed the verbatim review on disk** (`2026-06-06_Doc15_AMD-86_DOCS_Review_Return.md`); reconciled — verdict RATIFY-WITH-EDITS, **AMD-86 reviewed clean (J4)**, J1/F-A + J5 Doc06/AMD-60 + m1 PASS, m2 `[BLOCKING-for-M6-impl]`; **one J6 refinement confirmed against source: the E2 composition-root is `com.homesynapse.app`** (already requires both config+persistence → zero new edge), sharper than Doc 15's `HomeSynapseCore` (lifecycle, which lacks the config edge). **Folded + ratified + Locked + un-gated:** Doc 15 E1 (all 3 occurrences + semantic comment) + E2 + **Lock**; **AMD-86 + AMD-87 RATIFIED**; INV-PD-07 narrowed + INV-PD-03 posture; AMD-86-INV-01/AMD-87-INV-01 (§35/§36; 135/34); nav-index rows + **watermark 64→87**; m2 → **OR-M6-NONCE**; **M5-A Part 2 UN-GATED** (Claude Code). HEAD `8028337`; on-disk watermark **AMD-87**. |
| 2026-06-06 | PM (Cowork) | **POST-M4 SYNTHESIS + M5-WINDOW CHARTERED (documents-only; HEAD `8ef9e9f` unchanged).** Preflight **PASS (all 10)**. Wrote `context/planning/2026-06-06_post-M4-plan-and-M5-scoping.md` — decision-support built on the M4 retro + the 2026-06-06 foundation-readiness + language-replatform assessments + a source-verified deep read of the candidate-next surface. Key reframing: the planned M5 (Platform API + test-support) is **thinner than it looked** — `TestClock`/`SynchronousEventBus`/`NoRealIoExtension`/`InMemoryEventStore` already shipped in M1.x — so the leverage is the design/decision work that is regret-proof or gates M6. Nick decided **D1–D8** → adopt the **blended, lane-tracked M5 window** (`context/decisions/2026-06-06_post-M4_M5-window_decisions.md`): M5-A small Core, M5-B the M6 entry-gate + regret-proof schema decisions, M5-C protected website/docs (P6), M5-D evidence (GraalVM/GenZGC spikes + energy interviews). **D2 (diverges):** operational crypto-shredding → POST-MVP; build key-management infrastructure/seams at MVP; reconcile via a minimal **INV-PD-07 amendment** (full DOCS review). PM endorsed D2 + added the **write-path-encryption boundary** refinement (the regret-proof line the crypto owner-doc must pin). W24 charter written; backlog M5-window rows added; preflight re-run PASS. **Next: M5-B/B1 — the M6 entry-gate.** |
| 2026-06-05 | PM (Cowork) | **M4 RETROSPECTIVE + NEXT-PIECE RECOMMENDATION (documents-only).** Preflight **STALE** (partial M4.C closeout: missing session-log row, coder-handoff M4.C gate still OPEN, W23 footer at `e73e199`, unarchived note trail) → retroactive WUCP Phase 2 reconciliation → PASS. Wrote `context/audits/2026-06-05_M4-retrospective.md` (critical: milestone sizing, the instruction-scope-miss pattern + consumer/pin-survey fix P2, deferred-gate shift-left, the partial-closeout finding, amendment-rigor lightweight track, debt register, strategic/velocity, keep-doing) + `context/planning/2026-06-05_next-piece-recommendation.md` (recommend M5 + protected W3 website/docs lane; M6 next; AMD-65 in M5 window). Flagged 3 uncommitted build-breaking working-tree files to Nick (not touched). Debt + P1–P6 logged. HEAD `8ef9e9f` unchanged. |
| 2026-06-05 | Coder (Claude Code) + Nick + PM (Cowork) | **M4.C (AMD-54..64 integration-api freeze) COMPLETE → WORKSTREAM C COMPLETE → M4 COMPLETE. COMMITTED `8ef9e9f`.** Coder executed M4.C test-first; Nick's gate aborted round 1 at `core:event-model` (`EventTypesTest` count+regex) — a downstream-lockstep miss (instruction listed the producer, not the 7 consumer/pin sites incl. the production composition root `HomeSynapseCore` — a latent-M9 `encode()` bug no test pinned). Coder fixed all 7; **re-run GREEN (`./gradlew check --continue`, 145 tasks).** PM WUCP Phase 2 → APPROVE; 2 `[REVIEW]` ACCEPTED. integration-api 22→40 types; watermark AMD-64; NO `module-info`/Gradle/`projectionVersion` change. **AMD-65 (Expectation codec) deferred BLOCKING-for-M9.** _(Row added 2026-06-05 in retroactive reconciliation — the M4.C-complete closeout originally landed without this session-log row.)_ |
| 2026-06-05 | PM (Cowork) | **Workstream C BRIEFED (documents-only; HEAD `e76b925` unchanged; watermark stays AMD-53).** Preflight PASS (micro-fixes: pm-handoff `BS2SHA`→`e76b925`; backlog header re-anchor). **Integration AMD block AUTHORED: AMD-54..64, eleven files, PROPOSED** — REC-41 split as 54 (config-schema versioning, NQ-2) + 55 (hooks); 56 AUTH_FAILED; 57 HealthDetail; 58 five lifecycle permits (dot-namespaced, three-way lockstep); 59 capability events + CapabilityPublisher + DiscoveryServices (NQ-3/4); 60 SecurityServices/CredentialRotator (NQ-1); 61 softDependencies; 62 BackoffParameters + NQ-5/6 fold; 63 IsolationLevel (F-C correction); 64 plannedRestartTimeout. Frozen vs the shipped 12-/4-component shapes; descriptor 8→14, context 10→12, RequiredService 3→5; **zero `module-info` changes block-wide**. Review-flags R1–R7: R2/R4/R5 fidelity diffs run against the Research 6 return **pasted INLINE by Nick at dispatch** (not in DOCS Project knowledge; missing/truncated ⇒ INCOMPLETE, never reconstructed); **R6/R7 PRE-CO-SIGNED by Nick same day** (deliberation done; formal co-sign at ratification). **DOCS review prompt READY** (`context/instructions/2026-06-05_AMD-54-64_DOCS_Project_Review_Prompt.md`); Doc-05 masthead currency note (Gate B masthead-discharged); **gated M4.C instruction drafted** (⛔ issue-gated on ratification — holds as authored per Nick directive); backlog M4.C row (PLANNED — GATED). Core committed + pushed `e76b925` (Nick-confirmed). Next: Nick dispatches DOCS review (+ inline return) → ratify → mechanics (invariants §24+, watermark→64, Doc-05 body-fold, Doc 02 Stage-2 fold, lift ⛔) → issue M4.C → **M4 COMPLETE**. |
| 2026-06-05 | PM (Cowork) + Coder (Claude Code) | **M4.B-S2 (AMD-44 Stage 2 — EntityRole) COMPLETE + COMMITTED `e76b925` + WUCP Phase 2 → APPROVE. WORKSTREAM B COMPLETE.** EntityRole enum + EntityType 6×3 matrix + Entity 11→12 / ProposedEntity 3→4 (coerce→guard→copy; convenience ctors — zero caller breakage). Build GREEN (26+38+143, clean build). Same day: **Research 7 v2 assessed A− + NQ-1..7 RATIFIED (https problem URIs); Research 12 assessed A + SPIKE-DC scheduled (~Aug)**. |
| 2026-06-04 | PM (Cowork) | **W23 "Mon+Tue" batch.** Preflight STALE (session-log lag + unarchived notes + backlog header) → reconciled → PASS. **M4.B-S2 (AMD-44 Stage 2 — EntityRole) coding instruction AUTHORED + ISSUED** (`context/coding-instructions/M4.B-S2_AMD44_EntityRole.md`, source-verified vs `e73e199`; DP-1..7 settled; TypedIdTest FloorId carry-item bundled; no `module-info`/`projectionVersion` change). **Research 6 NQ-1..6 RESOLVED — Nick adopted all six PM leans;** OQ-05-05 RESOLVED; Workstream C Gate A CLEARED (NQ-3/4 finalize at B-S2 landing); Doc-05 refresh queued for C-briefing. Documents-only; HEAD unchanged `e73e199`. Next: Coder executes M4.B-S2 → PM Phase 2 → C briefing. |
