<!--
file: context/status/READ-ME-FIRST.md
purpose: the ≤1 KB launch pointer (W-HIVE-1 P4). A fresh session reads THREE things and nothing else by default.
-->

# Read me first

1. **The digest:** `context/status/PROJECT_SNAPSHOT.md` (whole — ~3 KB).
2. **The newest beat:** `context/handoff/pm-handoff.md` — line 8 (the pointer chain) + the newest three `##` beat blocks + `## Open Risks` (OPEN entries only). Never read the file whole.
3. **Your own prompt/brief:** hub → the newest `context/handoff/*_PM-mission-control_v*_orchestrator_session_prompt.md` not in `archive/`; lanes → the instruction/brief named in your dispatch line.

The repo-map `ls` sweep is ON-DEMAND only (one directory per call), triggered by an act that needs a file the spine does not address. Archives are read by pointer, never whole. The region caps (chain ≤3 KB · newest-3 blocks ≤18 KB · Open Risks OPEN-only · snapshot ≤3.5 KB) are checked by the beat that writes.
