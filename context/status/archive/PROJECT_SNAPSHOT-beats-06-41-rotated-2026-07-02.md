<!--
file: context/status/archive/PROJECT_SNAPSHOT-beats-06-41-rotated-2026-07-02.md
purpose: rotated beat-block history from PROJECT_SNAPSHOT.md masthead — verbatim mega-line entries, newest-first; live masthead retains beats 42–47
rotated-by: v14 hub first-act, 2026-07-02
prior last-verified (verbatim, from PROJECT_SNAPSHOT.md L8 pre-rotation): last-verified: 2026-07-01 (v13 hub launch, beat 41 — preflight STALE → reconciled; re-derived from source: core **dbb0109** / docs **75d0345** / hivemind **89da546**+beat-41 / bench **5ceff3b** / skills **5bc78bc**+frontend-skill-uncommitted; see the beat-41 masthead entry). PRIOR STAMP: 2026-06-26 (v7 hub launch — preflight STALE-benign reconcile) against core HEAD **b296e76** (M7.3 GREEN + the Track-A code deliverables; HomeSynapseCore unchanged; live wiring still deferred -> M7.4) / docs **f54d0e0** (committed baseline: Doc 16 LOCKED; Doc 17 DRAFT + AMD-95 PROPOSED committed; watermark AMD-94; invariants 169/49 — **v7 folds ON DISK pending Nick's commit: AMD-95 RATIFIED + Doc 17 LOCKED/AIOT-INV-1 → watermark AMD-95, invariants 170/50; AMD-96 PROPOSED**) / hivemind **cd8b046** (the beat-11 reconcile + the v7 prompt; the v7 §3 folds on disk pending commit). Phase 3 -- test-first implementation. (Prior v6-hub stamp was core 5363347 / docs e47f01e / hivemind 837e743 — it trailed the v6 arc beats 8-11; reconciled here. The M6-era 2026-06-07 last-verified blob rotated -> context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-22.md.)
-->

> **2026-07-01 (v13 hub, beat 41) — v13 LAUNCHED; the v12 arc INDEPENDENTLY RE-VERIFIED (verdict: holds, with amendments); the spine reconciled from STALE; the governance queue consolidation-ready.** The v13 hub grounded (env-model → truth-hierarchy → 11-check preflight = **STALE, zero CONFLICTED**: the v12 arc's beats 36–40 landed as commit-messages + lane-return files only — snapshot/pm-handoff were never updated; no W27 plan) → ran the §2 independent re-verification BEFORE reconciling, so this entry records verified fact. **Re-verification verdicts:** (1) **the moat/corpus/fixtures HOLD** — both fixtures trace timestamp-for-timestamp to the corpus blocks and raw ZHA diagnostics (all 6 SNZB timed-wave latencies, all clear-delays, the 8–21 ms duplicate spacing, the Hue 6.658 s/8.424 s CT confirms, the ±1-mired drift; correct ZCL command ids 0x0A/0x44/0x04 — genuine captures, not stubs); `quirk_applied=False` verified in the raw diags for BOTH devices. (2) **D5 "M9 small" HOLDS for Wave-1** (category-D devices; the walk-test proves the operational path) — with named residues: the corpus latencies are **reference-stack-conditioned** (ZHA's reporting config ≠ Doc 08 §3.7 defaults — Level min 5 s vs ZHA ~1 s, CT min 5 s vs ~10 s), **Doc 08 §3.7's default-reporting table has NO OccupancySensing row** (the hero trigger's cluster!), and D5 says nothing about category-B/A devices (Wave-2 should probe exactly that). (3) **E1 firmware ruling VERIFIED SOUND** — 7.4.5.0/v13 satisfies the ratified D-OPEN-1 target "EZSP v13+/EmberZNet 7.4+" (V1-launch-scope L77); v14+ASH is the batch contingency. (4) **AMD-CAND-1 + D2 review verdicts RE-DERIVED from the Locked docs — all edits WARRANTED** (additive-not-supersession confirmed vs Doc 08 §3.6/Doc 02 §3.8; the dispatch itself says "seven fields" while listing 8; NonFiringVerdict shares only CONDITION_NOT_MET with Doc 16 §4's 7-value SuppressionReason → "distinct vocabulary" wording correct; no existing invariant covers never-false-CONFIRMED; INV-ES-09 is genuinely next-free; the stale "pending registration at M7.4b" AIOT-INV-1 citation confirmed at register L85+§50; the FSM+per-subscriber-guard credit matches the code + the dispatch's own traceability table). (5) **Frontend go-live claims CONFIRMED in source** (agent-verified + hub spot-check: DTCG pipeline CI-wired, dark-default, i18n-keyed, self-hosted Inter subset, scenario engine + offline + axe, FE1_GO_LIVE, transport flip + bearer + ETag; root `frontend.yml` runs `npm run verify` = Lint·Typecheck·Test(a11y)·Build·Budget·Contract). **NEW FINDING — AMD consolidation:** proposed **AMD-96** (2026-06-26, PROPOSED, direction co-signed B4/B5) ALREADY carries the color-scope + Kelvin-canonical + §3.3 band-widen payload that AMD-CAND-2/3 re-staged on 06-28 — three overlapping vehicles converge on the same Locked sections. **Proposal to Nick: ratify AMD-96 using the CAND-2/3 verbatim diffs as its §2.A/§2.B edit text + fold E1 (measured v13 baseline; v14 = contingency) + E2 (VID:PID+probe, never descriptor strings) + E6 (version at stack-init) into its §2.C; AMD-CAND-1 then ratifies as AMD-97; D2 registers INV-ES-09 watermark-neutral (170→171).** Watermark 95→96→97. **Hygiene this beat:** v12 prompt archived → `context/handoff/archive/`; W27 plan authored; beat-41 commit PREPARED for Nick host-side (the phantom is ACTIVE — confirmed by a truncated VM read of `nexsys-bench` `iac/99-zigbee-coordinator.rules` [intact host-side] AND a truncated VM read of bench's binary `.git/index` [the fake mass-deletion status; worktree verified byte-identical to HEAD, all 21 files]; the §2 beat-block rotation stays HELD). **Uncommitted for Nick:** hivemind beat-41 (this reconcile + the 2 bench-arc files + the v13 prompt + the v12-prompt archive rename + W27); core `web-ui/dashboard/ci/frontend.yml` (byte-identical sync of the activated root workflow — a missed v12 sync commit); the `nexsys-skills` frontend-skill carry. **Next:** Nick rules the consolidated governance queue → hub folds (host-side, git-object-sourced) → M9 authoring against the measured corpus (+ the new acceptance items: Configure-Reporting as an explicit acceptance step [fixtures can't catch its omission], reporting-config parity or re-measure, the §3.7 OccupancySensing row, the ACK-vs-command latency reference point, occupancy-not-motion binding, dedup, tolerate-not-require IAS, firmware-version-aware). FE-1 live-integration is M9-independent and can run parallel. core `dbb0109` · docs `75d0345` · hivemind `89da546`+beat-41 · bench `5ceff3b` · skills `5bc78bc`+carry.

> **2026-06-28 (v11 hub, beat 35) — v12 hub prompt AUTHORED + the v11→v12 handoff.** The v11 conversation (a full arc: launch → M7.5a/b [the read-API differentiator core] → the Stream-B device-model research + reconciliation → the mid-Aug go/no-go review → AB-4 [cipher activation; Seam-1 closed] → the v11-arc cleanup) is handing off to a fresh-context hub. **v12 prompt:** `context/handoff/2026-06-28_PM-mission-control_v12_orchestrator_session_prompt.md` (supersedes v11) — carries the bench-reframed three-stream plan with **the bench as the critical path** + the **march to M9** (gated on the bench corpus + AMD-CAND-1; M7.5c the deferrable filler). The **v11 prompt is archived** → `context/handoff/archive/`. **v12 first-act hygiene:** archive-v11 (done) + the **DEFERRED spine beat-block rotation** (the v8→v11 masthead beats → the snapshot/pm-handoff archives — HELD across the v11 arc by the truncated-tail phantom, **ACTIVE again this beat**; the v12 hub runs it once quiescent host-side). **Carried for Nick:** the beat-35 commit; route AMD-CAND-1 + D2 to review (parallel with the bench); rule AMD-CAND-2/3 (commit-on-word); the A4.5 Doc 15 note; the OR-GATE + AB-4 CI-green Actions glance; the Dependabot 1-critical Security-tab glance; the `nexsys-frontend` skill commit + the **Check-9 mirror sync** (the v11-arc skill-currency fold); **bench first-light = the highest-leverage next block.** **Next:** launch v12 as a fresh Cowork conversation. core `89f775d` · docs `75d0345` · hivemind `8f8beb1`+beat-35 · bench `2c0a33c`.

> **2026-06-28 (v11 hub, beat 34) — AB-4 DONE + GREEN + WUCP Phase 2 ACCEPT — the at-rest cipher is LIVE; the log is encrypted-from-genesis.** Nick ran the full gate: `./gradlew check` **GREEN (149 tasks)** + the 4-module targeted compile — verified actually-green. **WUCP Phase 2 = ACCEPT, source-verified against the diff:** the change set is **16 files + 1 new test, with NO module-info / NO build.gradle / NO migration / NO event-model** → **zero-mint (71/41/53 — the green ArchUnit pins), zero-edge, zero-DDL** (F1 prefix-in-envelope). A4.1 cipher activation (Main's 6-arg ctor → M6.3 at-rest encryption LIVE for `[identity, presence_personal]` via the cipher-presence gate); A4.2 F1 (`v1` envelope version byte, single-assemble-site, prepended + GCM-AAD-bound, unknown-byte → `UNKNOWN_ENVELOPE_VERSION` fail-closed); A4.3 F3 (`NonceConstruction{RANDOM_IV,COUNTER}` programmatic fence; cross-construction rejected); A4.4 F13b (nonce-counter dir-fsync fail-closed via `writeAtomicallyDurable`, scoped past the Windows swallow); A4.5 backup-key seam (recorded in cross-agent-notes). **Phase-1 artifacts all landed host-side** (mount-lag check PASS; the truncated-tail phantom is quiescent this beat). **Deviations — all ACCEPT:** the F1-siting seam-widening `[INFO]` (`PayloadCipher`/`ScopeKeyManager` +`byte[] aad`, java.base-only → zero-edge — the correct "widen-the-seam-don't-infer" call); the F3 test rewrite `[REVIEW]` (the old test asserted now-forbidden cross-construction; production scopes disjoint); the declined F13a ctor-assertion `[Design point]` (would break the AB-2 fail-closed-read null-cipher construction — write-time guard is correct); the deferred cipher-warmup `[Design point]` (latency nicety; ~zero live producers pre-M9). **`[FLAG-NICK]`:** the A4.5 backup-key seam + rotate-on-restore note for **Doc 15 §3.4/§6** is a docs-repo edit — recorded in cross-agent-notes; **Nick lands the Doc 15 note.** **The 3 pre-M9 inputs:** AB-4 ✅ DONE · the bench (first-light pending — Nick's hands + hub orchestration) · AMD-CAND-1 + D2 (route-ready, parallel). **Next Core slot = M9** (real Zigbee; the hero on real silicon, acceptance-tested vs the bench corpus) — or M7.5c if Nick re-sequences. **Cleanup (this beat):** the v11-arc skill currency folded (Check-9 → STALE, Nick's mirror sync) + spine rotation. core `eed477e` + uncommitted AB-4 (SHA reconciles at Nick's commit) · docs `75d0345` · hivemind `77876ec` (+ beat-34 + the Coder's AB-4 artifacts + the v11 skill-currency, uncommitted) · bench `2c0a33c`.

> **2026-06-28 (v11 hub, beat 33) — AB-4 ISSUE-READY (cipher activation, the ruled M7.5c-leapfrog) + Nick's 3 sharpenings applied.** **(1) AMD-CAND-1 routes NOW in parallel** — the serialize-behind-bench slip fixed; the dispatch gained a "Routing timing" section (a SCHEMA review needs the research's worked examples, not the measured values; first-light POPULATES the slot, doesn't validate its SHAPE; values fold at RATIFY). Routes concurrently with the bench + D2. **(2) Frontend — MATERIAL go/no-go correction:** the prior "scaffold" read was WRONG (it globbed only top-level `src/`). Source-verified, the frontend is **substantially built** — all hero views (`WhyNotView` renders the M7.5b `NonFiringExplanation` exactly; `CausalChain` the M7.5a hero; `RunsView`/`AutomationsView`), the full API layer to the frozen `v1.1-2026-06-21` contract, and a **real transport already wired** (`index.ts` flips mock→real on `VITE_USE_MOCKS`, default real-in-prod; `realTransport` = fetch-over-loopback + AB-1 bearer + ETag). **Gate-2's render half is largely BUILT** (mock-backed, contract-conformant, real-transport-ready), sitting at `b296e76`. Residual = the **live-integration pass** (point realTransport at the live M7.5a/b backend) + the lane's current staffing/velocity (the one human input still needed). **(3) Dependabot triage:** catalog deps are current-pinned (jackson 2.18.6, javalin 6.7.0, logback 1.5.32, slf4j 2.0.17, sqlite-jdbc 3.51.2, json-schema-validator 1.5.6, YAML = snakeyaml-**engine** 2.9 → the classic-SnakeYAML RCE class does NOT apply). Shipped-runtime = jackson/javalin(+Jetty)/logback/snakeyaml-engine/json-schema-validator/sqlite-jdbc; dev-only = junit/assertj/archunit/awaitility/spotless/modules-graph. The 1 critical is most plausibly a transitive of a shipped dep (Jetty-via-Javalin the prime suspect) → a 30-sec Security-tab glance against this decision tree resolves shipped-vs-dev. **AB-4 ISSUE-READY** (`context/instructions/2026-06-28_AB-4_cipher-activation_coding-instruction.md`) — authored from a source-verified grounding; **much smaller than the 2026-06-19 spec** (AB-1/AB-2/M6.3/F13a all landed): A4.1 cipher activation (the 6-arg ctor flip → M6.3 at-rest encryption goes live for `[identity, presence_personal]`) + A4.2 F1 (`v1` envelope version byte, prefix + GCM AAD, zero-DDL) + A4.3 F3 (one-nonce-construction-per-scope) + A4.4 F13b (nonce-counter fsync fail-closed, scoped past the Windows tension) + A4.5 backup-key seam; **zero-mint/zero-edge/zero-DDL**; 3 `[Design point]`s (F1 siting, F13b scoping, cipher-warmup add/defer). **Next:** Nick routes AB-4 → Coder; routes AMD-CAND-1 + D2 to review (parallel with the bench); rules AMD-CAND-2/3; OR-GATE check; bench first-light. core `eed477e` · docs `75d0345` · hivemind `77876ec` (+ beat-33 uncommitted) · bench `2c0a33c`.

> **2026-06-28 (v11 hub, beat 32) — M7.5b DONE + GREEN + WUCP Phase 2 ACCEPT — the read-API differentiator core is COMPLETE (both hero halves render on the live log).** Nick ran the full gate: `./gradlew check` **GREEN (149 tasks)** + the targeted compile/test (`:core:automation`/`:api:rest-api`/`:lifecycle` compile; `NonFiringExplanationServiceTest` + `AutomationEndpointsTest` + `RunEndpointsTest`) — verified actually-green. **WUCP Phase 2 = ACCEPT, source-verified against the git diff (not the report):** the change set is exactly **7 modified + 6 new with NO module-info, NO event-model, NO build.gradle.kts touched** → **zero-mint, counts hold 71/41/53** (the green ArchUnit count-pins prove it; the coder-handoff independently confirms both module-infos + build.gradle.kts unchanged). `ExplanationService` gains 2 additive methods (`explainNonFiring`/`listAutomations`) — M7.5a's `listRuns`/`explainRun`/`over()` are **byte-intact** (read host-side; the large `StandardExplanationService` churn is the instructed shared-helper factoring + reorder, behavior-preserving + M7.5a-tests-green); the `FakeExplanationService` fan-out landed; the new `NonFiringExplanation`/`AutomationSummary` records + the `AutomationEndpointsTest` shape tests pin the frozen v1.1 §B3 shapes **field-for-field (camelCase)**. **Phase-1 artifacts all landed host-side** (both MODULE_CONTEXTs + coder-handoff + cross-agent-notes + coder-lessons — mount-lag check PASS despite the active phantom). **The `[REVIEW]`s — all ACCEPT; 1 corrects the instruction:** the `expectedSince` default window = the **whole retained log** (not a 24h lookback) — a wall-clock 24h default would breach replay-determinism (INV-SA-03); the Coder's position-bound-only window is strictly more correct (the M7.5a registry-free-correction class). Others ACCEPT: `/automations` in-memory offset cursor; the camelCase hand-built maps (the M7.5a precedent — recorded as a governance-thread item); the inline cursor codec; the source-forced `[INFO]` corrections (`RunId` no of/parse; `finalStatus()`; `{id}`; sealed-no-accessor → `getSimpleName`; exhaustive switch). **DP-B2 (Nick's ruling): the Coder shipped the recommended default** — clean confirmed success → `NEVER_TRIGGERED` + non-null `lastRelevantRunId` + "last fired and confirmed at <time>" (the UI tells "ran fine" from "never ran" by the non-null id), **no frozen-contract change. Hub recommendation: ACCEPT the default; defer `FIRED_CONFIRMED` as a post-V1 additive value if usage warrants** (it would reopen the frozen 4-value contract — friction for a non-differentiating edge). **Milestone: the read-API differentiator core is COMPLETE** — M7.5a ("why did it fire / did it confirm") + M7.5b ("why didn't it fire" + the automation list) both render on the live log = **gate-2's read-API half is DONE.** **Next Core slot — Nick's ruling (hub rec: leapfrog M7.5c with AB-4):** AB-4 is the ready, un-gated M9-prerequisite (AMD-87 cleared); M7.5c (events feed + health) is the deferrable supporting feed that rides the frontend's need. **Critical path = the bench (first-light) + the Core march to M9.** core `9ec5949` + **uncommitted M7.5b (SHA reconciles at Nick's commit)** · docs `75d0345` · hivemind `a46f0b1` (+ beat-32 uncommitted) · bench `2c0a33c`.

> **2026-06-28 (v11 hub, beat 31) — M7.5b coding-wait spent productively: the mid-Aug go/no-go readiness review + the quick-wins packaged one-decision-away.** While the Coder builds M7.5b (fleet committed: hivemind `9f12f80`, bench `2c0a33c`), the hub produced a candid evidence-based **mid-August go/no-go readiness review** (`context/planning/2026-06-28_mid-august-go-no-go_readiness-review.md`). **Gates:** 1 (engine LIVE) ✅ DONE; 2 (hero on real data) 🟡 PARTIAL — read-API nearly there (M7.5a done, M7.5b building) but the **frontend hero views are UNBUILT** (`web-ui/dashboard` is a buildable scaffold; the `nexsys-frontend` skill was just stood up to staff it) and **real data needs bench first-light**; 3 (hardware validating) 🔴 NOT STARTED — the longest sequential pole (bench → AB-4 [un-gated, ready] → M9 → 72h; AMD-87 the prior pre-M9 blocker is already cleared); 4 (install proven) 🟡 harness exists, needs install-smoke green on HEAD. **Critical path = the bench** (upstream of gate-2-real-data + all of gate-3; it sizes M9 via the open D5 re-open trigger, supplies the AMD-CAND-1 measured values, and feeds gate-2 real data). **~7 weeks (49 days) = tight but credible for a CONDITIONAL go**, contingent on (a) bench first-light soon + (b) an honest frontend-progress read this week; contingencies = scope the frontend hero to the 2 differentiator reads / scope M9 to the curated Wave-1 set. **Quick-wins staged one-decision-away:** the AMD-CAND-1 dispatch ready to route; the **AMD-CAND-2/3 Doc 02/08 color-currency fold pre-drafted with verbatim before→after diffs** (commit on Nick's word — white/CT scope + Kelvin-canonical-at-ingestion); the **D2 INV-id proposal currency-confirmed route-ready**; OR-GATE-M7.4 = Nick's Actions-tab confirm on `4e31aed`+`9ec5949`. **Guardrails honored:** no M7.5c authoring (M7.5b WUCP Phase 2 not closed); mass-rewrites held (phantom active). Hub standing by to orchestrate bench Phase 0 command-by-command (order held: Ethernet before Wi-Fi-off; firmware reflash before any measurement). core `9ec5949` · docs `75d0345` · hivemind `9f12f80` (+ beat-31 uncommitted) · bench `2c0a33c`.

> **2026-06-28 (v11 hub, beat 30) — M7.5b ISSUE-READY (audit-verified) + Stream B research RETURNED & reconciled (the device-model that realizes D5).** **Stream C:** the M7.5b coding instruction (`context/instructions/2026-06-28_M7.5b_non-firing-read-and-automation-list_coding-instruction.md`) is authored + independently source-audited **ISSUE-READY** (zero fabrications; zero-mint 71/41/53; **zero module-info change** — M7.5a already carries the rest-api→automation edge); the `RunEndpointsTest` `FakeExplanationService` stub note flipped may→confirmed. Awaiting Nick's commit (beat-29+30 + the instruction) → route → gate → WUCP Phase 2 (rule DP-B2: the clean-confirmed-success `/non-firing` verdict edge). **Stream B:** the device-model-and-corpus research RETURNED (`context/assessments/2026-06-28_device-model-and-corpus_research-return.md`; HIGH confidence; strong FACT/INFERENCE discipline) and is **reconciled — the session is DONE.** It realizes D5: a HomeSynapse-owned declarative JSON corpus (IR schema-v2) borrowing Z2M `exposes` + deCONZ generic-items inheritance + Matter conformance-rigor, **rejecting eval-in-the-data-path** (forced out by INV-PROJ-01 → named codecs); the new contribution is the **per-device confirmation-characterization block** (`confirmability`/`reportsAuthoritative`/`reportingPosture`) — the moat's schema slot the bench fills. **Routings:** **AMD-CAND-1** (confirmation block → Doc 08 §3.6 + Doc 02 §3.8) → governance review→ratify **pre-M9** (dispatch `context/instructions/2026-06-28_AMD-CAND-1_…_governance-review-dispatch.md`; clean amendment — extends a reserved seam, no invariant conflict; bundles the V1-read-API-subset-vs-Doc-16-§4 recording fold per Nick); **AMD-CAND-2/3** (Hue full-color GAP + color-temp mireds-vs-Kelvin canonical drift) → real Doc 02/08 now-fixes **awaiting Nick's scope call** (white/CT-only vs pull-color-forward; one canonical color-temp unit) — not pipeline-blocking; **AMD-CAND-4/5** housekeeping. **Stream A steer:** the bench Phase-1 capture now captures TOWARD the confirmation block (runbook updated) — one first-light pass answers both the moat measurement + the open D5 re-open trigger. **Phase-2 seed:** the IR schema + two-path onboarding pipeline captured in `nexsys-bench/docs/2026-06-28_phase-2_corpus-model_…md`. **Standing governance thread:** the V1 read-API ships a frozen-v1.1 subset of Locked Doc 16 §4 twice (RunExplanation, NonFiringExplanation) — coherent, recorded explicitly via the AMD-CAND-1 pass. core `9ec5949` · docs `75d0345` · hivemind (beat-30 + research return + M7.5b instruction + AMD-CAND-1 dispatch — commit pending) · bench `4c4496d` (+ runbook steer + Phase-2 model — commit pending).

> **2026-06-28 (v11 hub, beat 29) — LAUNCHED; superseded v10; preflight STALE-benign → reconciled to committed reality (the beat-28 crystallization + the bench init have landed).** The v11 mission-control hub (the bench-reframed three-stream plan) grounded env-model → truth-hierarchy/pointer-not-copy → V1 record + read-API breakdown + **the bench test-and-truth-engine decision-record** → the 11-check preflight. **Preflight = STALE-benign, zero CONFLICTED** — currency-lag only: the beat-28 masthead anticipated "bench init pending / hivemind `a9689c3`+crystallization (lands next commit)," but Nick has since committed both. **Re-derived from source (the authoritative layer, not propagated):** core **`9ec5949`** (M7.5a; clean, pushed) · docs **`75d0345`** (AMD-95; Doc 16+17 LOCKED; **170/50**) · hivemind **`c3a6f8b`** (the beat-28 crystallization — the bench decision-record + the research dispatch + the v11 prompt + the v10 archive — COMMITTED) · **bench `4c4496d`** (the 5th repo `nexsys-bench` — inited + scaffolded + pushed) · skills `5bc78bc` (+ the uncommitted `nexsys-frontend` skill: `MANIFEST.yaml` + the design return + `orchestrators/nexsys-frontend/` — a write-isolated lane's output awaiting Nick's commit; NOT the spine, carried). **Counts hold 71/41/53** (M7.5a's green ArchUnit count-pins); `projectionVersion` 5; watermark AMD-95 / invariants 170/50. **Check 9 PASS** (both skill mirrors byte-identical). M7.5a source-confirmed (`ExplanationService`/`RunExplanation`/`StandardExplanationService` resolve in `core:automation`). **⚠ Truncated-tail phantom ACTIVE** — the 3 spine files (snapshot/backlog/pm-handoff) show single-hunk EOF truncations in VM git; host file tools confirm them intact (the "Counter-nonce" content the VM reads as the truncation point lives at pm-handoff L291 host-side) → §2 body-rotation hygiene HELD; targeted host-side Edits only. **First-act hygiene:** the v10 prompt is already archived → `context/handoff/archive/2026-06-27_PM-mission-control_v10_orchestrator_session_prompt.md` (CONFIRMED — not in the live handoff dir). **Residuals (carry):** OR-GATE-M7.4 CI-green on `4e31aed`+`9ec5949`; the D2 canonical INV-id registration (review→ratify); Dependabot 5-vuln (deferred); §2 hygiene (deferred, phantom-gated). **Next:** author **M7.5b** (the non-firing read `GET /automations/{id}/non-firing` + `GET /automations`) against committed `9ec5949` per the breakdown + the frozen v1.1 §B3, on M7.5a's `ExplanationService` plumbing → Nick → Coder; orchestrate bench Phase 0/1 (Stream A) + ensure the device-model research is launched (Stream B). core `9ec5949` · docs `75d0345` · hivemind `c3a6f8b` · bench `4c4496d`.

> **2026-06-28 (v10 hub, beat 28) — M7.5a COMMITTED (`9ec5949`); the BENCH REFRAME ratified + the three-stream plan crystallized; `nexsys-bench` stood up (the 5th repo); v11 authored.** Nick committed + pushed M7.5a (core `4e31aed`→**`9ec5949`**, gate GREEN 149 + the 51-task targeted run; hivemind `828cdb6`→`a9689c3`). **Strategic reframe (RATIFIED, co-signed):** the hardware bench is the **test-and-truth engine** — *a captured real device event stream is a seeded event log*, so real-world interactions become permanent, fast, hardware-free regression tests (extending M7.4d's replay machinery), and the bench is where the `confirmed|unconfirmed|failed` moat stops being a design claim and becomes measured fact. Decision-record + the five rulings: `context/decisions/2026-06-28_bench-test-and-truth-engine_decision-record.md`. **The three streams:** **A — the bench** (`nexsys-bench`, the 5th repo — scaffolded: README + IaC + the Phase-0 runbook + the harness/fixtures/corpus discipline; Nick's hands + hub orchestration); **B — the device-model research** (dispatch READY — *realizes D5* + first-classes the confirmation semantics + returns the onboarding pipeline); **C — Core** (M7.5b next — DEFERRED to the v11 hub for clean-context authoring). **The 5-repo discipline:** core / docs / hivemind / skills / **bench** (commit+push all touched before any launch). **v11 hub prompt authored** (`context/handoff/2026-06-28_PM-mission-control_v11_orchestrator_session_prompt.md`, supersedes v10) — carries the bench-reframed three-stream plan forward; the v10 prompt archives. **Next:** Nick inits+pushes `nexsys-bench`, commits the hivemind crystallization, then launches v11 (authors M7.5b + orchestrates the streams) + the research session, and runs bench Phase 0 on `hs-dev-1`. core `9ec5949` · docs `75d0345` · hivemind `a9689c3`+crystallization · **bench `nexsys-bench` (init pending)**.

> **2026-06-28 (v10 hub, beat 27) — M7.5a GATE-GREEN + WUCP Phase 2 ACCEPT — the read-API hero read is LIVE (the gate-2 enabler).** Nick ran the build: the targeted `:core:automation:compileJava :api:rest-api:compileJava :lifecycle:lifecycle:compileJava :core:automation:test :api:rest-api:test` **GREEN (51 tasks)** + `./gradlew check` **GREEN (149 tasks)** — the gate of record, verified ACTUALLY-green (two runs). **WUCP Phase 2 = ACCEPT, source-verified against git status (not the report):** `core/automation/module-info` UNCHANGED + zero event-model change → **counts hold 71/41/53** (the green ArchUnit count-pins confirm); `api/rest-api/module-info` +2 plain requires (`com.homesynapse.automation` + `com.fasterxml.jackson.databind`); `core/automation/build.gradle.kts` +`testFixtures(:core:event-model)`. The Coder's WUCP Phase 1 artifacts all landed (both MODULE_CONTEXTs, coder-handoff, cross-agent-notes, coder-lessons). **The 8 `[REVIEW]`s — all ACCEPT; two CORRECT a flaw in the M7.5a instruction:** (1) **registry-free pure-LOG outcome derivation** — DP-A2's registry-gating would inject a mutable, non-deterministic read into a pure projection (breaks INV-SA-03); the Coder derives outcome from the log alone (a `DISABLED` command is never ledger-tracked → no `state_confirmed` → never a false `CONFIRMED`), strictly more correct; (2) **`automationId` from the event ENVELOPE subject** (`SubjectRef.automation(...)`) — the run-lifecycle payloads carry none (the hub's own pre-issue audit independently flagged this). Others ACCEPT: trigger-view best-effort (`firingValue` null V1 — derivable from the triggering event later); `cascade.parentRunId` null V1; `actions[]` anchored on `command_issued`; `listRuns` newest-first via bounded forward-scan+reverse (the `EventStore` is forward-only — a backward-read primitive is the proper O(page) fix, post-V1); the `+jackson.databind` edge (rest-api is the JSON boundary; parses `command_issued.parameters` → the wire `params` object). **The frozen v1.1 wire shapes are UNCHANGED** (the `RunEndpointsTest` shape test pins them) — the status mapping + log-derivation are internal → NO cross-lane contract change. **Deferred gate (M7.5a):** local `./gradlew check` GREEN confirmed; the code is UNCOMMITTED — **SHA reconciles at Nick's commit**; CI-on-pushed-commit is the residual (the OR-GATE pattern). **Next Core slot = M7.5b** (the non-firing read `GET /automations/{id}/non-firing` + `GET /automations`). core `4e31aed`+uncommitted M7.5a · docs `75d0345` · hivemind `828cdb6`+uncommitted.

> **2026-06-27 (v10 hub, beat 26) — M7.5a ISSUE-READY (the read-API hero read; authored + independently verified).** The v10 hub's first substantive act: the M7.5a coding instruction (`context/instructions/2026-06-27_M7.5a_read-API-hero-read_runs-and-causal-chain_coding-instruction.md`) is authored against committed `4e31aed` + source-verified (subagent adversarial audit = PASS: zero fabricated types/signatures; both frozen v1.1 shapes match field-for-field). `GET /api/v1/runs` + `GET /api/v1/runs/{runId}/causal-chain` as pure log-derived projections (INV-SA-03; **ZERO mint — 71/41/53 hold**). **Grounding caught 4 source-vs-contract findings** (folded in): `RunCausalChain` is cascade-ancestry-only → the tree projects from the AMD-92 run-lifecycle EVENTS per Doc 16 §3.3 `RunExplanation`; no historical-run query exists (`RunManager` is active-only) → a new automation read-side `ExplanationService` over `EventStore`; `RunStatus`≠wire `status`; M7.4c made confirmation live → the full outcome enum ships now. **Nick's 3 co-signed rulings** folded as Settled Decisions (DP-A1 map `RunStatus`→wire at the read boundary; DP-A2 full `actions[].outcome` enum incl. the `DISABLED`→`DISPATCHED` honesty nuance; DP-A3 projection automation read-side per Locked Doc 16 §3.3). **JPMS:** the only module-info change is rest-api `+requires com.homesynapse.automation` (plain; `Object`-erased gateway) — automation module-info UNCHANGED. **Cross-lane:** wire `status` vocab UNCHANGED (non-breaking; the Web-UI mocks stay valid). **Next:** Nick gates → Coder → WUCP Phase 2 (rule the 3 `[Design point]`s) → M7.5b. core `4e31aed` · docs `75d0345` · hivemind `828cdb6`.

> **2026-06-27 (v10 hub, beat 25) — LAUNCHED; preflight STALE-benign → reconciled to committed M7.4d (`4e31aed`); the M7.4 arc is COMMITTED.** The v10 mission-control hub superseded v9 (v9 prompt archived → `context/handoff/archive/`; v9 stays open as the write-isolated nexsys-frontend advisor). Grounded env-model → truth-hierarchy → V1/v6/§1 → the 11-check preflight = **STALE-benign, zero CONFLICTED** — currency-lag only (Checks 1/3/4: the snapshot/backlog HEAD pointers + the M7.4d-commit record trailed HEAD). **Re-derived from source, not propagated:** core **`4e31aed`** (M7.4d committed — `RunPipelineReplaySafetyTest` +504 / lifecycle MODULE_CONTEXT +2; **test-only, no production change, `module-info` unchanged → counts hold 71/41/53**, verified against the commit diff) · docs **`75d0345`** (AMD-95; Doc 16+17 LOCKED; **170/50**, re-derived from the §17 table) · hivemind advanced **`f613c36`** (v9 arc, beats 20–24) → **`828cdb6`** (the v10 prompt commit). **Check 9 PASS** (both skill mirrors byte-identical). **⚠ The truncated-tail phantom is ACTIVE** (VM git shows every spine file "modified" as a single-hunk EOF truncation; the host file tools confirm the files intact — verified the lifecycle MODULE_CONTEXT tail) → the §2 body-rotation hygiene (mass-rewrites) is correctly HELD this session; targeted host-side Edits only. **Residuals (carry):** OR-GATE-M7.4 — confirm the `ci.yml` GitHub Actions run is GREEN on the pushed `4e31aed` (`gradlew` is `100755`/executable in the commit, so the prior exit-126 mode is cleared; the actual Actions-green is Nick's to confirm); the D2 canonical INV-id registration (review→ratify; proposal staged). **Next:** author the **M7.5a** coding instruction (the read-API hero read — `GET /api/v1/runs` + `/runs/{id}/causal-chain`) against `4e31aed`, per the breakdown + the frozen v1.1 contract. core `4e31aed` · docs `75d0345` · hivemind `828cdb6`.

> **2026-06-27 (v9 hub, beat 24) — read-API scope-freeze + breakdown SET (next Core direction) + the `nexsys-frontend` skill-creation dispatch authored (READY).** Per Nick's delegation to drive Core forward: the thin causal-read-API is grounded against the FROZEN v1.1 contract and needs **no new machinery** (the "why-didn't-it-fire" half is answerable from existing run records/absence/config; deep per-non-match recording is post-V1). Right-sized per P1 into **M7.5a** (the hero `runs` + `causal-chain` read — the gate-2 core, NEXT) **→ M7.5b** (the non-firing read + automation list) **→ M7.5c** (events feed + health consolidation); scope-guarded vs the M12 balloon. Breakdown: `context/planning/2026-06-27_causal-read-API_scope-freeze-and-milestone-breakdown.md`. The **M7.5a coding instruction authors next, against the COMMITTED post-M7.4d HEAD.** **Skills lane:** the `nexsys-frontend` skill-creation dispatch is authored + READY (`nexsys-skills/design/2026-06-27_nexsys-frontend-skill_creation_session_dispatch.md`) — a role skill for the idle Web-UI lane (brand identity + UI/UX; hivemind-integrated; v1.1-contract-consumer; single-spine-writer preserved; asks Nick for executive brand direction). **Next:** Nick commits the v9 arc (core M7.4d + the hivemind), launches the frontend session, then routes M7.5a. core `db8ab5f`+uncommitted · docs `75d0345` · hivemind `a142d9b`+uncommitted.

> **2026-06-27 (v9 hub, beat 23) — M7.4d GATE-GREEN + WUCP Phase 2 ACCEPT → the M7.4 arc is COMPLETE.** Nick ran `./gradlew check` **GREEN (149 tasks, first pass)** on the M7.4d tree (shift-left `:lifecycle:compileTestJava` also clean). **Source-verified against the diff (not the report):** the core tree is ONLY `RunPipelineReplaySafetyTest.java` (new) + `lifecycle MODULE_CONTEXT` (mod) — **no production change, `module-info` unchanged, counts hold 71/41/53**; the docs tree is CLEAN — **`Architecture_Invariants_v1.md` UNTOUCHED (the Coder did NOT self-mint D2's id)**. **Non-vacuousness is empirically proven, not just argued:** the LIVE positive control (`afterLive_newTrigger_doesDispatch`) passed in the same green run — the harness sees a real `command_dispatched` in LIVE while seeing zero across the boot REPLAY; the dispatch axis asserts both `command_dispatched`+`command_result` (the unregistered-entity gap), the run-init axis pins `getAll().hasSize(2)` against a config-load regression, and the seed uses a bus-less `PersistenceFactory` so replay genuinely runs from checkpoint 0. **The 3 `[REVIEW]`s — all ACCEPT:** (1) the `EventTrigger` lever (the only deterministic, registry-/AutomationId-independent trigger match); (2) asserting both dispatch event types (strictly more correct); (3) observing via the event store / no production seam (+ the M9-seam forward note in MODULE_CONTEXT — M9 extends the gate to assert the real adapter recorder is zero on replay). **D2 replay-purity is now CI-pinned at the integration level.** **Residuals:** the **D2 canonical INV-id registration** (review→ratify; proposal staged) + the CI-on-pushed-commit confirm (OR-GATE-M7.4). **Next Core slot = the thin causal-read-API** (pending Nick's scope-freeze ruling). core `db8ab5f`+uncommitted M7.4d test · docs `75d0345` · hivemind `a142d9b`+uncommitted.

> **2026-06-27 (v9 hub, beat 22) — gate-of-record CONFIRMED (local) + M7.4d ISSUE-READY (both halves authored).** Nick ran `./gradlew check` **GREEN (149 tasks)** on the working tree that became `db8ab5f`, then committed + pushed both repos (core `69cc6e2..db8ab5f`; hivemind `ce84b0e..a142d9b`) → the M7.4c local deferred-gate is confirmed; **OR-GATE-M7.4 narrowed** to the CI-on-pushed-commit confirmation; M7.4d's §4b prerequisite is clear. The push re-surfaced the **Dependabot 5-vuln (1 critical)** alert — already tracked (CI-hardening backlog item 5; deferred per Nick). **M7.4d ISSUE-READY (the arc's last item):** (a) the coding instruction (`context/instructions/2026-06-27_M7.4d_d2-pure-function-replay-CI-gate_coding-instruction.md`) — `RunPipelineReplaySafetyTest`, a composition-root seeded-log replay asserting ZERO dispatch/run/confirm side-effects + a LIVE positive control (the V2-A spike is the tested template; `ci.yml` auto-gates; no production/ci.yml change — D2 is already structural); (b) the governance half (`context/instructions/2026-06-27_D2-replay-invariant_registration-proposal_review-dispatch.md`) — PROPOSES D2 as `INV-ES-NN` (watermark-neutral; reconciles AIOT-INV-1's dangling "decision-record D2" citation) via review→ratify; the PM does NOT self-mint. **Next:** Nick routes M7.4d → Coder → gate → WUCP Phase 2; co-signs the D2 registration; confirms CI green on `db8ab5f`; then the thin causal-read-API. core `db8ab5f` · docs `75d0345` · hivemind `a142d9b`+uncommitted.

> **2026-06-27 (v9 hub, beat 21) — LAUNCHED; preflight STALE-benign → reconciled; spine re-stamped to committed M7.4c (`db8ab5f`).** The v9 mission-control hub superseded v8, grounded (env-model → truth-hierarchy/pointer-not-copy → V1 record/v6 plan/§1 record → 11-check preflight), and ran the mandated STALE→reconcile. **Preflight = STALE-benign, zero CONFLICTED** — currency-lag only: the lower masthead fields (Current-state/Latest-commits still cited the M7.2b-era `5363347`/`b296e76`/AMD-94/169-49), the milestone backlog (the old M7.4 2-way split), and the Open-Risks section trailed HEAD; **Check 9 PASS** (both skill mirrors byte-identical). **Re-verified from source, not propagated:** core `db8ab5f` (M7.4c; parent `69cc6e2` = M7.4b, ancestor-confirmed — the prior `69cc6e2` "commits next" was lag) · docs `75d0345` · hivemind `a142d9b` **+ uncommitted beats 20–21** (the v8 skills-deliberation beat-20 + this beat-21 + the v9 prompt await Nick's commit); counts **71/41**/53 re-derived (71 = `EventTypes.java` String constants; 41 = the `CORE_PRODUCTION_EVENT_CLASSES` roster); watermark **AMD-95** / invariants **170/50** (`AC` = §50; **AIOT-INV-1** is the sole Doc-17 mint and its **D2 canonical INV-id is still pending → M7.4d**); the M7.4c artifacts present on disk (`RunPipelineConfirmWiringTest`, `StandardPendingCommandLedger`). **OR-M7-WIRING → RESOLVED** (condition met: the E2E composition-root confirm gate exists). **NEW deferred-gate Open Risk:** confirm `./gradlew check` + CI green on the **pushed `db8ab5f`** (the gate-of-record loop — a Coder hand-back is never gate-run in-session; §4b) before recording M7.4c CI-gated and before M7.4d starts. **Hygiene remainder (gated by Nick this session):** the body-rotations; the project-knowledge regenerate/retire; the beat-20 skills folds (the one-file rot-tally + the SD-5 decision-number fix). **Next:** restore preflight PASS → **M7.4d** (the D2 pure-function-replay CI gate + register D2's canonical INV-id via review→ratify) → the thin causal-read-API. core `db8ab5f` · docs `75d0345` · hivemind `a142d9b`+uncommitted.

> **2026-06-27 (v8 hub, beat 20) — skills triggers/recursion deliberation RETURNED + reconciled (off the critical path; routes through the eval/Lock bar).** The fresh write-isolated session returned `nexsys-skills/design/2026-06-27_skills-build-triggers-and-recursive-stewardship_deliberation_return.md` (+ the parked `orchestrators/skill-architect_SPEC_v0.md`). High-quality, source-verified (it ran a fact-checker sub-agent), critical, and appropriately conservative — it went past the hub's framing and was willing to say no. **Decisions (all off-critical-path, non-blocking):** (1) **Triggers reframed gate-vs-value:** a build starts iff ≥1 VALUE-TRIGGER fired AND the GATES are open — G1 (critical-path clear: post go/no-go for content, post-V1 for the catalog; Rung-0/harness exempt) + G2 (spare capacity ≥5 working days); value-triggers T1 (same rot-class ≥3 → build the one piece that kills it) · T2 (rolling rot-rate → build Rung-0) · T3 (same model re-derived ≥3 fresh sessions → build that module/seam skill) · T4 (fresh-session rate → begin the ladder) · T5 (new-model release = a RE-EVAL probe, NOT a build). (2) **Instrument now:** a one-file append-only `rot-incident-tally` (the manual rot-sentinel precursor that senses the SPINE — where rot actually is, since zero content skills exist to rot) + a one-line trigger-check on the WUCP Phase-2 closeout. (3) **Recursion safety SHARPENED by change-class:** SK-META-INV-A (detection = a pure read-only projection) · B (the steward is structurally a proposer — no apply path except via the gate; the Doc 17 E3 analogue) · C (gate strength scales: content = eval OR human; **self-modification = eval AND human AND keystone AND reversible** — the conjunction breaks the amplification loop) · D (auto-apply ONLY for semantically-null idempotent self-verifying ops). Safe first increment = **DETECT + REPORT only** (already the parked daily-check). Dogfooding made rigorous: AIOT-INV-1 "AI is never an autonomous actuator" → "the steward is never an autonomous editor," same two-axis CI-testability. (4) **Bootstrap-now = a calibrated NO:** build NO live stewardship skill pre-trigger (the eval gate can't run without the harness; write-isolation forbids the live load path) — but author the `skill-architect` build-ready SPEC (delivered, **non-loadable**, parked), ratify **curator = skill-creator** (adopt, don't build), stand up the tally; **instantiate `skill-architect` as the FIRST live skill at the first build-trigger, eval-gated.** **A real rot the session CAUGHT (verify + fix at the hygiene pass; the tally's first entry, STALE-CACHE):** `nexsys-skills/README.md` + the topology proposal both cite "description-as-API = **SD-5**," but in design v1 §16 SD-5 is the *harness* decision; description-as-contract is §1 Principle 5 / §13.2 — a decision-number drifted under two pointers. **Hub routing:** the gate-vs-value frame + the tally + the closeout line fold into the standing re-eval; SK-META-INV-A..D + the safe-first-increment are design intent (carry into the harness spec at Rung-0); **curator=skill-creator + the `skill-architect` spec route through the design-v1-Lock/eval discipline — PARKED, not instantiated.** The **v9 hub executes the folding + the tally + the SD-5 fix in clean context.** **No V1-scope change; the Core lane untouched.**

> **2026-06-27 (v8 hub, beat 19) — M7.4c GATE-GREEN (first pass) + WUCP Phase 2 ACCEPT — OR-M7-WIRING is CLOSED; the full command pipeline is LIVE end-to-end (fire → dispatch → CONFIRM).** The Coder built M7.4c; `./gradlew check` **GREEN on the first run** (149 tasks; **no gate-fix** — the M7.4b P2 lesson applied, all consumers swept: the `Components` arity, `SharedScheduler`, the 4th-subscriber pin). **WUCP Phase 2 = ACCEPT, verified:** git status → module-info unchanged both modules + zero event-model change → **71/41/53** holds; the green lifecycle/`@TempDir` tests confirm the ledger's SQLite read connection is released (**the M7.3 leak class CLOSED for the exact subscriber that caused it**). The **E2E confirm gate** (`RunPipelineConfirmWiringTest`) proves the full causal chain **`command_issued → command_dispatched → state_confirmed`** via a synthetic `state_reported` — a REAL confirm: the Coder verified `StandardCapabilities.onOff().turn_on` declares `ExpectedOutcome("on", ExactMatch(true))` + `ConfirmationMode.EXACT_MATCH`, so the hero renders a true confirmation, NOT an OPTIMISTIC bypass. **The 4 `[REVIEW]`s — all ACCEPT:** (1) the **asymmetric teardown** (explicit `eventBus.unsubscribe("pending_command_ledger")` in `doTeardown`; `abandon()` relies on `eventBus.abandon()`) — **hub-verified against `InProcessEventBus`**: `abandon()` (L217-231) closes EVERY runtime's read connection, `unsubscribe()` (L176-184) closes the one — both paths release the ledger's connection, no leak; the deviation from the instruction's literal "both branches" is the MORE correct read (an explicit abandon-path unsubscribe would be redundant + inconsistent with the dispatch subscriber's handling); (2) `pollExpirations()` exposed as a `Components` Runnable tick (per spec); (3) `schedulePeriodic` post-construction (per spec — respects the scheduler-built-before-ledger ordering); (4) the E2E confirms against `onOff()` (per spec). **Step 3.4c** registers the ledger after projection-LIVE, `coalesceExempt=true`, the tick at `LEDGER_EXPIRY_PERIOD_MILLIS=1000ms`. **OR-M7-WIRING CLOSED** — the live demo (fire → dispatch → confirm) works on the real composition root; the "did it actually confirm?" hero half renders live. **Only M7.4d remains in the M7.4 arc:** the D2 pure-function-replay CI gate + registering D2's canonical INV-id (a safety/governance hardening, not new functionality). **Next:** Nick commits (M7.4c Core + the hivemind spine) → M7.4d. Parallel/off-critical-path: the positioning re-weight; the AMD-96 currency review; the bench ASH posture; the skills design-v1 review + the new triggers/recursion deliberation. core `69cc6e2` (M7.4c commits next) · docs `75d0345` · hivemind `ce84b0e`.

> **2026-06-27 (v8 hub, beat 18) — M7.4c ISSUE-READY (confirmation goes live + the E2E confirm gate; closes OR-M7-WIRING).** With M7.4b GREEN (producer live), the v8 hub authored the M7.4c instruction: `context/instructions/2026-06-27_M7.4c_confirmation-go-live_ledger-tick-and-e2e-confirm-gate_coding-instruction.md` (against `69cc6e2`). **Scope:** register `pending_command_ledger` LIVE at **Step 3.4c** (after projection LIVE, `coalesceExempt`, with the **paired `eventBus.unsubscribe` teardown** — the reverted-M7.3 trap), drive its `pollExpirations()` from the `SharedScheduler` (a 3rd periodic task), and land the **E2E composition-root confirm gate** (boot → seeded automation → `command_issued → command_dispatched →` inject a synthetic `state_reported` → **`state_confirmed`** + a queryable causal chain + the ledger draining). Authored with full STOP-on-Mismatch gates + Coder-pushback on the under-determined shapes (the tick-exposure via the assembly `Components` Runnable; the `SharedScheduler` extension — a post-construction `schedulePeriodic`, since the scheduler is built at ~L426 BEFORE the ledger and starts tasks at construction; the E2E confirmation injection). **Two grounding findings baked in:** `pollExpirations()` is package-private (→ exposed as a `Components` tick handle, keeping the query interface query-only), and the E2E confirmation **must** seed a capability `expectedOutcome` (not an OPTIMISTIC `ConfirmationMode.DISABLED` bypass) to prove a REAL confirm — the M7.3 [REVIEW] reality. The `-Werror`-CLI + grep-all-ctor-consumers lessons are in Build Discipline. **Out of scope → M7.4d:** the D2 replay CI test + the D2 INV-id registration. **Next:** Nick routes M7.4c → Coder → gate → WUCP Phase 2 → M7.4d. Parallel/off-critical-path: the positioning re-weight (Nick's call); the AMD-96 currency review; the bench ASH posture; the skills design-v1 review. core `69cc6e2` · docs `75d0345` · hivemind `ce84b0e`.

> **2026-06-27 (v8 hub, beat 17) — M7.4b GATE-GREEN + WUCP Phase 2 ACCEPT — THE RUN PIPELINE IS LIVE (producer end-to-end through dispatch).** The Coder built M7.4b; the first `./gradlew check` went **RED on a P2 fan-out miss** (`DurationTimerTest:205` constructed the now-2-arg `AutomationEngineSubscriber` directly — the Coder swept the assembly test but missed the direct-ctor consumer); the hub applied the surgical gate-fix (inert `RecordingRunManager`+`FakeSelectorResolver` doubles; the REPLAY test never initiates a run) → **re-run GREEN.** The deferred-gate discipline working as designed: the Coder's 4-lens LLM review returned zero findings (cannot compile); the gate caught it in 9s. **WUCP Phase 2 = ACCEPT, verified:** `git status` confirms **module-info unchanged in all three modules + zero event-model change → counts hold 71/41/53**; the green gate confirms the lifecycle composition-root/`@TempDir` tests pass (the paired `runManager.close()` teardown in BOTH branches is clean — the M7.3 leak class closed), the D2 replay-safety unit test, and the producer-pipeline test (**a seeded trigger emits `command_issued`→`command_dispatched`**). Read `RunInitiator.java` directly — the trigger→run handoff resolves via the evaluator's own matched-trigger indices. **The 4 `[REVIEW]` choices — all ACCEPT:** (1) the package-private `evaluateMatches` seam (the evaluator's immediate-match indices; `evaluate()` its thin projection) — **strictly better than the interface-widening the instruction floated**: single source of truth, no re-derivation, frozen `TriggerEvaluator.evaluate` untouched, zero ripple; (2) `resolvedTargets` keyed `"action:<index>"` (the RunContext position form) — forward note: align with a selector-label convention when the causal-read-API needs it; (3) `RunManager extends AutoCloseable` (the M7.4a precedent); (4) `RunManagerConfig.defaults()` at the root — forward note: surface the cascade/auto-disable params through a typed config accessor when operator-configurable (V1 defaults fine). **The M7.4a serializer `[REVIEW]` is now CLOSED in production:** the executor's `parameterSerializer` comes from the new `PersistenceFactory.commandParameterSerializer()` — a `Function<Map,String>`, NOT `ObjectMapper` (Jackson stays off persistence's exported API; confirmed by unchanged module-info + green `-Werror`), backed by the mapper that round-trips `AttributeValue` (a real round-trip test, not a stub). **Lesson (gate-fix):** on any ctor signature change, the P2 sweep must `grep "new <Class>("` across the ENTIRE test tree, not just seam/assembly tests (M7.2a-2 class). **OR-M7-WIRING: HALF-CLOSED** — the producer pipeline is live; the live ledger + confirmation half is M7.4c. **Next:** Nick commits (M7.4b Core incl. the gate-fix; the separate distribution `.deb`-version fix; the hivemind spine) → M7.4c (live `pending_command_ledger` + `pollExpirations()` tick + the E2E confirm gate). core working tree on `b85e1ed` (commits next).

> **2026-06-27 (v8 hub, beat 16) — ecosystem-currency research RETURNED + reconciled (off the critical path; M7.4b untouched).** The write-isolated research session returned `context/assessments/2026-06-27_smart-home-ecosystem-currency_research-return.md` (web-backed; 71 VERIFIED / 16 honestly-flagged UNVERIFIED; the fabricated SafeGate citation sought + excluded). Read in full; source discipline strong; the 6 decisions hold. **Two clusters CONFIRM-reinforced, one MOVED.** **The mover (time-sensitive — Cluster 2 / positioning, Nick's strategic call):** Home Assistant merged the FRAMEWORK for durable never-evicted "why-didn't-it-fire" traces to `dev` 2026-06-22 (core #174116 + frontend #52708) — but the **emitter was reverted, so it is NOT shipped**; still, the "why-not" half is no longer "unshipped everywhere." **Re-weight the mid-Aug differentiator onto the two halves that still hold: (a) the durable `confirmed|unconfirmed|failed` device-confirmation projection — unaddressed on EVERY platform (open/closed/AI); (b) never-evicted-as-a-structural-property (a pure projection of the immutable log; HA's is capped/bucketed/bolt-on).** **Baseline CORRECTION:** HA traces persist to disk (NOT "in-memory/evicted") — the real limits are the cap (5) + no-trace-when-a-trigger-never-matches; fix before any copy ships (the SafeGate-in-our-own-voice risk). Go/no-go WATCH on the HA 2026.7-line. **AMD-96 + M9 (Cluster 1A): AMEND** — direction holds, but **widen the band v14→v18 / EmberZNet 8.0.2→9.0** (v14 is the floor on shipping MG24 dongles; SDK current 9.0/v18 Mar-2026; Doc 08's v13/7.4 ~5 versions stale). The **`ASH_ERROR_TIMEOUT` is a recognized cluster on factory-8.0.2 MG24 units** (remedy = reflash newer NCP firmware) → **promote to a first-class M9 + hero-bench reliability item** (the hero runs on this exact silicon); V1 posture = recommend-firmware / ship-reflashed-image / document-the-reflash+watch (Nick + bench). **Thread seam (1C): NARROW** — single-chip Zigbee+Thread on one MG24 (RCP Multi-PAN) is deprecated (HA "multiprotocol is dead" Jul-2025; Silicon Labs de-recommends) → reserve as "add a dedicated Thread radio later," NOT "MG24 dual-stack." **CONFIRMs (reinforced, no action):** Matter deferral (1.6 = focused, no new device categories; still fragmented; keep the seam reserved); D5 converter-DB (MIT/MIT/Apache-2.0 unchanged; zigpy→Open Home Foundation de-risks; GPL app separate); the planner→verifier→executor AIoT consensus (5/5 baseline papers verified; the "externalized runtime governance" frontier = our frame). **Doc 17 §7 Q1 (3B): SCOPE the patent follow-up** (no blocking patent, directional/moderate; run the under-covered surfaces — Samsung/SmartThings, WO2025024326A2, Apple US20250077786A1, non-US — before any superlative) + **CORRECTION:** US10367652B2 family = Nant Holdings IP, NOT Nest/Google. Defensible claim ("AI is structurally a proposer, statically verified, durably auditable") holds; superlatives stay counsel-gated. **Hub reconciliation:** the AMENDs route to their channels (the AMD-96 currency review scope; Doc 17 §7 Q1; the positioning = Nick's strategic call) — the hub does not self-edit Locked/proposed bodies; the CONFIRMs + the 2 baseline corrections are recorded here. **No V1-scope change.**

> **2026-06-27 (v8 hub, beat 15) — M7.4 SPLIT 3-way (Nick co-signed); M7.4b ISSUE-READY (the run pipeline goes live).** Grounding the M7.4b wiring revealed M7.4b-as-framed was an EPIC: the `RunManager` is **not wired into `HomeSynapseCore` at all** (the `automation_engine` subscriber evaluates triggers but ignores the matches — its literal comment is "run initiation is M7.2"), so the entire run lifecycle (RunManager→condition gate→executor→`command_issued`) is built+unit-tested but **never assembled live**, the trigger→run handoff deferred since M7.1. Per P1 (size visible at scoping, not in arrears), **M7.4 splits:** **M7.4b** = the run pipeline goes live (the producer: construct selector/condition/executor[+serializer]/gate/RunManager into the composition root + the trigger→RunManager handoff as a **direct co-located `initiateRun` call** — D1 governs the COMMAND hop, already the M7.4a subscriber) · **M7.4c** = confirmation goes live (the live `pending_command_ledger` + the `pollExpirations()` tick + the E2E composition-root confirm gate) · **M7.4d** = the D2 replay CI test + register D2's canonical INV-id. **M7.4b instruction ISSUE-READY:** `context/instructions/2026-06-27_M7.4b_run-pipeline-go-live_...coding-instruction.md`. **Two findings baked in:** (1) the **trigger→run derivation** — `StandardTriggerEvaluator.evaluate()` returns only `List<AutomationId>` but `RunManager.initiateRun(...)` needs `matchedTriggers`+`resolvedTargets`; the contract is fixed (one root Run per matched automation, LIVE-only, `RunCausalChain.root()`) and the derivation (re-derive vs. widen `evaluate`'s return) is the Coder's design call with pushback; (2) **`PersistenceObjectMapper` is package-private** (no `PersistenceFactory` accessor) → M7.4b adds a **public `Function<Map,String>` param-serializer seam** (returns `Function`, NOT `ObjectMapper`, to keep Jackson off persistence's exported API; the mapper already round-trips `AttributeValue`, closing the M7.4a forward risk **in production**, not via a test stub). The malformed `-Werror` self-check guidance is corrected in M7.4b's Build Discipline. **core `b85e1ed`** (M7.4a committed + pushed; CI-on-commit to confirm green) · hivemind `278c9e6`. **Next:** Nick routes M7.4b → Coder → WUCP Phase 2 → M7.4c. Parallel/off-critical-path: the scoped smart-home/ecosystem research session (feeds back to THIS hub for steering); the skills design-v1 review; the physical Wave-1 bench. Mid-Aug go/no-go on the calendar.

> **2026-06-27 (v8 hub, beat 14) — M7.4a RETURNED + GATE-GREEN + WUCP Phase 2 ACCEPT (source-verified).** The Claude Code M7.4a build landed (9 modified + 2 new files, **uncommitted** working tree on `606d410`); **`./gradlew check` GREEN — 149 tasks** (the `-Werror` targeted self-check failed only because `-Werror` is not a Gradle CLI flag — a malformed command, not a code defect; `check` bakes `-Werror` into the compiler config and passed). **WUCP Phase 2 = ACCEPT, verified against source (not the report):** module-info unchanged both modules + zero event-model change → **counts hold 71/41/53** (structural, from the diff); the paired `close()` teardown is present in **BOTH** `HomeSynapseCore` shutdown branches (L766-767 / L819-820, null-guarded — the reverted-M7.3 trap closed); the dispatch subscriber registers Step-3.4b **after projection LIVE**, `coalesceExempt=true`; the D2 LIVE-only guard mirrors the gate-proven M7.3 ledger (`volatile replayMode` driven by the bus setMode-before-replay-delivery contract); the 5 required subscriber tests + the producer tests assert correctly (incl. the §3.11.2 causal chain + the REPLAY no-dispatch proof). **The 5 `[REVIEW]` choices — all ACCEPT:** injected `parameterSerializer` (no JSON edge), `onEvent`→`validate(Map.of())` (Tier-1 value-agnostic), `coalesceExempt=true`, `CommandDispatchService extends AutoCloseable` (additive, Phase-3-internal), executor ctor widened (impl-class). **Two tracked FORWARD items:** (1) **M7.4b** — when the executor is live-wired (the RunManager pipeline + the REAL persistence-`ObjectMapper` serializer), **verify the serializer round-trips the resolved param value types, esp. value-model `AttributeValue`** — the unit tests use a stub, so GREEN does NOT prove production serialization; broken serialization = the hero "did it confirm?" rendering garbage params; (2) **M9** — the dispatch subscriber intentionally does NOT deserialize `command_issued.parameters` (no JSON lib in automation) and `command_dispatched` carries no params, so the M9 adapter must read params off the durable `command_issued` (by causation); Tier-2 param-schema validation is future. **Milestone truth (OR-M7-WIRING still OPEN):** M7.4a wired the SUBSCRIBER + built the producer CODE, but the executor/RunManager is **not composition-root-wired yet** (deferred since M7.2a-2) — the registered `command_dispatch_service` has **no live producer** (idle, harmless). So **M7.4b scope = the QUARTET: (a) wire the RunManager+executor producer into `HomeSynapseCore` w/ the real serializer, (b) the live `pending_command_ledger` subscriber + `pollExpirations()` tick (w/ paired teardown), (c) the E2E composition-root gate, (d) the D2 replay CI test** — that closes OR-M7-WIRING. **Residual (gate-of-record loop):** M7.4a is uncommitted; on Nick's commit+push, **confirm the ci.yml run is green on the M7.4a commit** (first real milestone use of CI; "a claimed gate must be verified actually-green") before recording M7.4a CI-gated. **Minor:** fix the coding-instruction `-Werror` self-check guidance (compiler arg, not a Gradle CLI flag); the retained in-process `dispatch()` primitive is mild post-event-driven cleanup debt. **Next:** Nick commits/pushes M7.4a + confirms CI green → the hub authors M7.4b (grounding first: the §1 D2 record + the V2-A replay-harness template + the RunManagerAssembly live-wiring shape). Skills design-v1 review + the bench capture + AMD-96/Doc-13 currency reviews run parallel, off critical path.

> **2026-06-27 (v8 hub, beat 13) — LAUNCHED; preflight STALE-benign → RECONCILED; governance-close COMMITTED; the two in-flight returns reconciled (skills Phase-0 LANDED; M7.4a NOT yet in the repo).** The v8 mission-control hub superseded v7 (v7 prompt archived → `context/handoff/archive/`), grounded (env-model → truth-hierarchy/pointer-not-copy → V1 record/v6 plan/§1 record → 11-check preflight), and ran the mandated STALE→reconcile. **Preflight = STALE-benign** (Checks 1/3/6 currency-lag behind the now-COMMITTED governance close; zero CONFLICTED; **Check 9 PASS**). **Baselines CONFIRMED at committed reality (re-derived from source, not propagated):** core **`606d410`** (`b2529cc` M7.3+Track-A+gradlew/scripts-+x → `652f9b3`/`604440b` frontend+install-smoke activation → `606d410` build-deb broken-pipe fix; **working tree clean** — the 5 hivemind "M" + the `build-deb.sh` "M" are the documented mount-truncation phantoms, host files intact, verified) · docs **`75d0345`** (**AMD-95 RATIFIED**, watermark 95; **Doc 17 LOCKED** + AIOT-INV-1; **AMD-96 PROPOSED**; invariants **170/50**) · hivemind **`17f26d9`** (the v7 governance commit `442eb8d` + this v8 prompt). **CI is genuinely GREEN** (149 tasks; ArchUnit guards enforced; install-smoke = gate #4, green-up pending). Counts **71/41/53** (M7.4a is zero-mint by design). **Return B — skills Phase-0 VERIFIED + ACCEPTED → DONE** (Nick's independent filesystem re-check, clean; off the product critical path): `nexsys-skills/` 4-tier skeleton + README + provenance MANIFEST stub + `design/` (design-v1 Lock-candidate + its independent-review dispatch); `skills-initiative/` de-cluttered; harness daily-check parked. **Residual = Nick:** `git init` + the prepared `_scratch/_commit-2026-06-27-nexsys-skills-phase0-init.txt`; then launch the design-v1 independent review → co-sign → Lock (ratifies the *design*, not a build; conform-not-rebuild per §15.5; build deferred post-V1). **Return A — M7.4a NOT yet landed (VERIFIED, not assumed):** no command-pipeline producer/subscriber change on `main` (`StandardCommandDispatchService` still at M7.2a-2 `79e2984`; no `CommandDispatchAssembly.java`; tree clean; the only `claude/*` branch is the dead 2026-03-27 `confident-williamson`, 0 commits ahead). The Claude Code M7.4a build is still in flight / not yet relayed-synced by Nick. **M7.4a is NOT marked done; M7.4b NOT authored (gated on M7.4a GREEN).** **Next:** Nick relays/syncs the M7.4a return → gate (`./gradlew check` + CI green on the commit) → WUCP Phase 2 (source-verify 71/41/53; paired teardown in BOTH shutdown branches; the LIVE-only/replay + teardown tests; rule any `[REVIEW]`s) → author M7.4b (live `pending_command_ledger` subscriber + `pollExpirations()` tick + E2E composition-root gate + D2 replay CI test; closes OR-M7-WIRING). Parallel (off critical path): the skills git-init + design-v1 review; the bench physical capture (M9 ground-truth + the M7.4 E2E motion fixture); the AMD-96 + Doc-13 currency reviews. Mid-Aug go/no-go stays on the calendar.

> **2026-06-26 (v7 hub, beat 12) — §3 CONSOLIDATED DECISION PASS RULED (12 decisions, Nick co-signed) → GOVERNANCE CLOSED + M7.4a ISSUED.** Nick ruled all four §3 question-groups as recommended. **Folded on disk (pending Nick's commit):** **AMD-95 RATIFIED** (watermark **AMD-94 → AMD-95**) — Doc 07 §3.11.1/§3.11.2/§4.3/§7/§16 reconciled to the event-driven/co-located shape + the 3 review-added drift fixes; AMD-90 §2.1 superseded-in-part (§11). **Doc 17 LOCKED** — **AIOT-INV-1 minted** (new `INV-AC` category; invariants **169 → 170**, categories **49 → 50**; S1/S2 + E1–E5 folded; AIOT-INV-2/3 kept as principles; AX-7 sequenced before AI-authoring; Q1 patent search commissioned). **Wave-1 device-model reconciliation** recorded (B4 White/CT + Kelvin-canonical; B5 EZSP-v14 currency; B6 occupancy binding; B7 keep scaffold) → **AMD-96 PROPOSED** (Doc 02/08 currency, for a source-verifying review). **Lane-integration note** (C8 additive `name` field folded into the v1.1 contract; C9 Doc 13 currency; C10 the two CI drops = Nick; C11 E1/E2/E3/E6 — E2 bind-host Core WU ahead of the M9 wizard; C12 doctrine shared-home). **Watermark AMD-95 · invariants 170/50** (on disk). **M7.4a coding instruction ISSUE-READY** (`context/instructions/2026-06-26_M7.4a_command-issued-producer-and-co-located-dispatch-subscriber_coding-instruction.md`) — the `command_issued` producer + the co-located dispatch subscriber + a paired teardown; D2/D3 as constraints; the E2E + replay CI gate fenced to M7.4b. **Nick's next:** commit the folds (the commit-message file at the ClaudeFolder root) + the two CI file drops + mirror-sync if any skill source changed + route M7.4a to the Coder. **D-07-A (9th action permit) deferred to M8** (forward/unbuilt currency note). **CI:** `ci.yml` (the gate-of-record) is now genuinely GREEN for the first time (was never green — `gradlew` non-exec since init; fixed `b2529cc`); `install-smoke` activated (gate #4); see `context/process/ci-as-gate-of-record.md` for the correction + the deferred CI-hardening backlog. **Skills track (parallel, write-isolated, OFF the product critical path):** the do-now rot-fixes are adopted (the truth-hierarchy/pointer-not-copy discipline; `project-knowledge` governed-stale); Nick ruled Phase-0 (stand up `nexsys-skills/`) as its own dispatched session (`skills-initiative/2026-06-27_phase-0_skills-foundation_session_prompt.md`), ratify design v1 + conform-not-rebuild, and a daily auto-maintenance check planned-but-deferred-until-the-harness-is-tested. Detail in `skills-initiative/` (loose until Phase 0 versions it into `nexsys-skills/design/`).

> **2026-06-26 (v7 hub launch; reconciling the v6 arc beats 8→11) — §1 architecture RATIFIED + empirically validated; Doc 17 DRAFT + AMD-95 PROPOSED; Track-A fleet's first beat landed; decisions consolidated.** The v6 hub ran a full arc and is now token-heavy; the **v7 mission-control hub** carries forward in fresh context. Spine reconciled to core **`b296e76`** / docs **`f54d0e0`** / hivemind **`cd8b046`** (the beat-11 reconcile + the v7 prompt — see pm-handoff beats 8→11 for the narrative; this is currency reconciliation, the substance already committed). **State:** the engine ACTS as units (through M7.3) but is **NOT live end-to-end** — the command pipeline is unwired (**OR-M7-WIRING**), so there is no live `RunCausalChain` yet. **M7.4 is the first milestone that moves the demo needle.** **§1 deeper-M7 architecture RATIFIED 2026-06-25** (D1 event-driven/co-located dispatch · D2 pure-function-replay · D3 additive event versioning · D4 log retention · **D5 converter-DB = adapt-the-data + curated fallback, RATIFIED 2026-06-26**) **+ empirically validated** (V2-A Pi spike: D1 ≈1.9 ns confirmed, D2 PASS as the M7.4b CI-gate template, D4 sized — thresholds FREEZE on a Session D Pi re-run). **Doc 17 (AIoT + Cloud Readiness) DRAFT** + **AMD-95 (command-pipeline currency) PROPOSED** committed to docs — both run the formal review→ratify→Lock pipeline (review-passed; pending Nick co-sign). **Track-A fleet first returns:** Web-UI (`web-ui/dashboard/` buildable Preact+TS SPA, both hero halves, frozen v1.1 contract) · Distribution (`homesynapse-core/distribution/` one-command-install skeleton + CI gate #4) · Bench Session C (device-corpus knowledge layer + verdicts — **PHYSICAL CAPTURE PENDING on Nick's desk**). **The v7 hub's FIRST substantive act = the §3 CONSOLIDATED DECISION PASS** (12 decisions, Nick rules), THEN the governance close-out (AMD-95 ratify-fold + Doc 17 Lock-fold minting AIOT-INV-1) + M7.4a authoring. **Open governance items pending Nick:** AMD-95 co-sign · Doc 17 Lock · the Wave-1 device-model reconciliation (ESC-W1-HUE-01/COORD-01/SNZB03P-01) · the lane-integration items. The detailed beats 8→11 are in `context/handoff/pm-handoff.md`.

> **2026-06-22 (beat 6) — M7.2b GREEN (first pass) + WUCP Phase 2 APPROVE — THE ACTION MODEL IS FROZEN (Cowork PM v4 hub).** `./gradlew check` **GREEN (149 tasks), zero gate-fix rounds** — the inline spotless self-step + the record-collision STOP-check (baked into the instruction) held. Delivered: minimal computed-param resolution (`ComputedValue` sealed, 3 permits `LiteralValue`/`AttributeRef`/`AggregateValue`, resolved at run-init against the captured AMD-03 snapshot, before the frozen `ActionExecutor`); the run-coupled fail-closed-read terminal (snapshot read throws → Run `FAILED` + recorded reason, C1 preserved); no-engine-retry frozen (AMD-90-INV-01). **No new event type — counts 71/41/53; module-info UNCHANGED** (only automation files touched). **[REVIEW] deviations → all ACCEPT (3 are excellent Coder catches resolving real instruction tensions):** (1) types **package-private** (a public `resolve(): AttributeValue` would force `requires transitive value` → the module-info-unchanged pin); (2) `AggregateValue` carries a resolved **`Set<EntityId>`** not a `Selector` (Selector resolution = an I/O capability, contradicts C-SA-2 purity); (3) absent attribute → **`DegradedAttributeValue`** typed-absent (a valid input must not throw, C-SA-2). **Next Core slot = M7.3 (PendingCommandLedger)** — the confirmed/unconfirmed hero half (Option A); instruction authors next. **Core SHA reconciles at Nick's M7.2b commit.** _Prior beats (beat-5 M7.2a-2 GREEN / beat-4 v4-launch / v3 / the two 2026-06-20 notes) + the M6-era last-verified, Next-action, Open-flags, and Committed/Latest-commit detail rotated -> `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-22.md`._
