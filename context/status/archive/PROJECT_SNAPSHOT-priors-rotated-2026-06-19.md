<!--
file: context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-19.md
purpose: Rotated PROJECT_SNAPSHOT masthead session-notes (2026-06-18 and earlier) — lifted from the live snapshot 2026-06-20 (PM mission-control hub opening hygiene, step a) to cut the per-session read tax. Content verbatim; the live snapshot keeps the 2026-06-19 arc + a fresh Last-updated line. Earlier priors -> PROJECT_SNAPSHOT-priors-rotated-2026-06-12.md + git.
state-type: archive
status: ARCHIVE
-->

# PROJECT_SNAPSHOT — priors rotated 2026-06-19 (2026-06-18-and-earlier masthead notes)

> **2026-06-18 — M7.1 LANDED GREEN + WUCP Phase 2 (Cowork PM).** **M7.1 (automation trigger/condition path) is gate-verified GREEN** — full `./gradlew check` PASS (149 tasks); **first automation code landed** (substantive HEAD advances past `1eddd9a` once committed). Built per the ride-along plan; the Doc 16 Architect session also produced `homesynapse-core-docs/design/16-superior-automation.md` (DRAFT — awaiting DOCS review → Lock). **Gate-fix round 1** (2 failures, one pass): `timer.automationId`→`entry.getKey().automationId()`; and the **forbidden `core:automation→config` module edge relocated** (`AutomationConfigBridge` deleted → config-free `AutomationSchema`; config wiring rides app-bootstrap). **FIX-07 instruction defect corrected.** **All 6 [REVIEW] deviations ACCEPT** (3 substrate gaps routed forward; 3 behavioral defaults recorded). **Deferred gate RESOLVED (GREEN).** **Next-WU = M7.2** (builds *into* Doc 16 — don't freeze its action contract first). The subscriber + config + lifecycle-wiring ride **app-bootstrap AB-3** (decision-record addendum). Backlog M7.1 → DONE; pm-handoff carries the full WUCP Phase 2 record. _Decisions + converge notes follow._

> **2026-06-18 (later, same day) — APP-BOOTSTRAP DECISIONS + SUPERIORITY-SCOPE RULED (Cowork PM, fresh session; follows the converge below).** Entry preflight **PASS** (all 11; HEADs reconciled to core `03f16dc`/`1eddd9a` / docs `f2e064d` / hivemind = converge). Step-0: **CC-1 + R-γ both still in flight** (the expected residual). **PART A — the four app-bootstrap decisions RULED by Nick** (decision record `context/decisions/2026-06-18_app-bootstrap-and-superiority-scope_decisions.md`): **A1** bind posture = **loopback-default** (LAN = explicit authenticated opt-in; never bind-all; `/internal/*` behind auth) · **A2** auth model = **token issuance** (WS authenticated; zero-config stays authenticated, no shared bootstrap secret / no pre-auth enumeration; **carry the enterprise per-scope/per-site-claims hook**) · **A3** read contract = **fail-closed at MVP + design the degrade seam now** (degrade behavior ships with crypto-shred; F4 pin: degrade/chain-validity disabled until `chain_hash`+startup-verify live) · **A4** = **ratify rotate-DEK-on-restore now** (additive new DEK version, retain priors; high-water-mark→cross-check) **+ reserve the 1-byte envelope version tag now** — both via the **AMD-94 re-open→DOCS→ratify pipeline** (Doc 15 is Locked; no silent edit). **App-bootstrap charter FINALIZED to the ruled state; AB-1..AB-4 rows added to the backlog.** Residual before the first AB coding instruction = **mechanical only** (CC-1 run · R-γ return · AMD-94 ratify); no decision gate open. **PART B — superiority Phase-1 scope RULED + Architect-mode authoring DISPATCHED.** **Doc 16** = expressiveness-without-DSL + explainability/causal-chain + **run-coupled reliability**, with a federation (identity/scoping) **seam** + a hybrid **cut-line** coordinated with app-bootstrap. **Nick's B-1 split:** cross-cutting reliability-as-a-product-property is **its own co-equal doc** (not M7-gating; avoids the "epic-under-one-label" failure). **Also split into own docs:** B3 federation (post-M8; new invariants) · honest-hybrid (tight; with app-bootstrap). **M7 interlock verdict: M7.1 UNAFFECTED** (ride-along cleared) **· M7.2 SHAPED — builds into Doc 16** (don't freeze its action contract first) **· M7.3 UNAFFECTED.** Deferred to M7.2/M8 briefs: A2/B1/B2/D2/E2. Updated: decision record (new), charter, backlog, superiority session prompt (RULED SCOPE folded, dispatch-ready), W26 lanes, this masthead + Current-WU, cross-agent-notes. WUCP drift check run; commit messages handed to Nick (bang-free, `git commit -F`). Check 9 stays benign-STALE until Nick's mirror sync. _The converge note follows._

> **2026-06-18 — CONVERGE / RECONCILE (Cowork PM, the overdue W25-rollover session, run late).** Ground-truth HEADs this session: core **`03f16dc`** / docs **`f2e064d`** / hivemind **`b7a893e`** (all clean; verified via `git cat-file`). This note brings the spine to current and supersedes the 2026-06-14 Track-4 note below where they differ. **Entry preflight: STALE → reconciled to PASS by this note + the W24/W25/W26 flips below.** The staleness was currency-lag only (zero CONFLICTED — every SHA resolves): the converge session had never run, and two artifacts landed after the Track-4 note. **(1) THE EVIDENCE GATE IS RETIRED.** The 2026-06-15 **M5-D desk-research assessment** (`context/assessments/2026-06-15_M5-D_desk-research_PM_Assessment.md`, grade A−, all four load-bearing post-2025 clusters primary-verified, zero fabrication) **stands down the energy/erasure interviews as decision-grade.** *Track B (energy/C9):* parked to the future energy/Grid milestone — energy is post-MVP, zero energy event types exist, the regret-proof freeze point is *before the first energy event is written* (not now), and the desk research already answers the shape. Stop energy interviews + energy research. *Track A (verifiable erasure):* the AMD-86 re-open trigger is **MOOT** — M6 froze the write path in the *correct* encrypt-on-write posture (M6.2 per-scope KEK/DEK + M6.3 durable counter-nonce), so future crypto-shred is fully preserved; a late buyer-"yes" now costs only *scheduling* the post-MVP Doc 15 §3.6 shred operation (a recoverable feature WU), not the irreversible seam. Erasure interviews downgraded to opportunistic A3 product/roadmap signal — NOT an AMD-86 re-open. **Consequence: the app-bootstrap lane is UN-PARKED** (its only remaining inputs are the R-γ crypto-agility + CC-1 bind-address returns; interviews no longer gate it). **(2) CORE-REVIEW LANDED (A/B/CONVERGE, 2026-06-15;** `context/audits/2026-06-15_core-review_CONVERGE_synthesis.md`). Verdict: **the core is sound where built, but not yet runnable — `main()` is a one-line stub, so the crypto is inert and the HTTP surface is unexposed.** Ranked source-verified backlog C1–C15; **two HIGH, both detonate at app-bootstrap, neither on the M7 path:** **C1** = composition root starts an **unauthenticated HTTP server** (INV-SE-02 has no enforcing impl; `AuthMiddleware`/`RateLimiter` unbuilt); **C2** = a read-path **decrypt failure aborts the whole replay** (no per-row `DegradedEvent` degrade; a lost key → store unreplayable vs INV-RF-04; gates crypto-shred vs INV-PD-07). Logged under Open Risks (OR-APP-AUTH, OR-RF-DECRYPT). Fix-firsts: C3 (cascade-governance doc) already landed `03f16dc`; C4 (placeholder event records) settles at M7.1/F5 scoping. **(3) M7.1 = ISSUED, AWAITING BUILD** — instruction `context/instructions/2026-06-15_M7.1_trigger-condition-path_coding-instruction.md`; substantive code HEAD is still M6.3 `1eddd9a` (no automation code landed yet). **(4) STRATEGIC DIRECTION (Nick-ratified this session — he delegated the call):** prioritize **app-bootstrap (carrying C1+C2+C9 — makes the product runnable)** + **superior-automation Phase-1 design (federation/B3, honest-hybrid/C1-deploy, expressiveness-without-DSL — the differentiator; likely a NEW Locked design doc, Nick's Phase-1 scope → greenlit for *design*)** + the **coder/PM agent currency pass**, all in parallel. M7.1 build is a ready ride-along, *not* the focus; M7.2/M7.3 implement *into* the superiority design rather than the baseline. Briefs authored this session: app-bootstrap milestone + automation-superiority design session (see `context/planning/` + `context/handoff/`). **(5) W24 → COMPLETE; W25 → IN_PROGRESS; W26 scoped** (`weeks/2026-W26_jun22-jun28.md`). **Check 9** goes benign-STALE after the agent currency pass until Nick's external mirror sync. _Prior Track-4 note follows:_
> **2026-06-14 — Track-4 forward-execution reconcile (Cowork PM).** Ground-truth HEADs this session: core **`1eddd9a`** / docs **`c309123`** / hivemind **`7ea7992`** (all committed/pushed; working tree clean). **M6 COMPLETE 4-of-4 — M6.3 COMMITTED `1eddd9a`** (at-rest event-payload encryption for sensitive-PII scopes `[identity, presence_personal]`; `./gradlew check` GREEN 147). The "M6.3 … LANDED GREEN … code UNCOMMITTED" wording lower in this masthead is **SUPERSEDED** (it captured the pre-commit moment). **Entry-gate row 4 CLOSED** — `homesynapse-core-docs/website/pages/config-superiority.md` **APPROVED 2026-06-14** (PM quality gate, veto delegated by Nick; Increment-1 acceptance met, all three register guardrails + anti-requirements verified, INV-CE-01 blockquote verbatim-confirmed, no fabricated metrics; competitor-receipt + README items deferred as PUBLISH gates). The Lane-4 no-new-Core-instruction condition is **LIFTED**; with rows 1/2/5 closed and the M6.3-vs-M7 ordering resolved by M6.3 landing first, **M7.1 (trigger/condition path) is CLEARED to issue** — interview-independent (the energy/erasure interviews gate only the parked app-bootstrap lane + the M6.3 restore-half R-α, not M7.1). **2026-06-15 artifacts landed + now registered** (were committed but unregistered in the spine): Track-3 red-team reversibility audit + R-γ crypto-agility CHALLENGE brief (`707c234`); R-δ competitive deep-dive brief + CC-1 bind-address spike spec + M7.1 red-team F7/C4 wire-commitment pin (`9bfcec5`); app-bootstrap milestone charter — authored, gates pending (`7ea7992`); M7.1-prep coding instruction + pre-verification — issue-gated (`f4a317f`); two-part core-review framework + A/B/CONVERGE synthesis (`c7e03ea`); R-δ + R-α raw returns rehomed to docs (`c309123`) + their PM assessments. **This session (interview-INDEPENDENT lane):** issue M7.1 (P2 survey + carry-pins) · land C3 (cascade-governance doc-fix) · fire CC-1 → Code, R-γ → DOCS, key-portability follow-up brief → DOCS. **PARKED / owned elsewhere (NOT advanced here):** W24→COMPLETE rollover + W26 scoping (the converge session); app-bootstrap charter FINALIZATION (F2 scope-width + milestone-placement, post-interview + R-γ + CC-1); M7.2-vs-app-bootstrap sequencing; the crypto/app-bootstrap lane behind Nick's energy/erasure interviews. Entry preflight: **STALE** (currency lag only — HEADs/M6.3-status/2026-06-15 artifacts; reconciled by this note + the backlog/coder-pointer flips); Check 9 mirror-sync stays benign-STALE pending Nick's external sync.

> **2026-06-13 — currency reconciliation (Cowork PM, parallel lane).** The masthead below had lagged the 2026-06-12 ratification; corrected here to ground truth: **amendment watermark AMD-93** (AMD-88..93 RATIFIED 2026-06-12), **B2 C8/C9 RATIFIED**, **invariants 163/47**, AMD-04 superseded by AMD-91. Source-verified code state: **22 Gradle subprojects**, **~573 main + ~230 test + ~41 testFixtures Java files (≈844)**, **~2,002 @Test methods**, projectionVersion 5, `./gradlew check` GREEN. Current spine HEADs: core `824d6ba` / docs `8002b7e` / hivemind `5e4943a`. **W24→COMPLETE rollover deliberately NOT advanced here** (calendar-bound; owned by the 2026-06-14 W25-rollover converge session). The per-module counts deep in the Code State section remain as-of their 2026-05-28 verification and are superseded by these figures.

**Last updated:** 2026-06-12 late (THE M5-C INCREMENT-1 SESSION — parallel to the R16 assessment per the diverge architecture) by PM (Cowork) — **M5-C Increment 1 DONE (drafted-awaiting-veto): website skeleton + config-superiority page draft at `homesynapse-core-docs/website/` (venue ruled in-session: core-docs; framework decision = open item). D-1..D-7 strategy-draft vetoes RULED — ALL SEVEN ACCEPTED as drafted (fence + segment rule bind as ratified; fold mechanics queued, Lane 6). The R16 M5-C-COPY bucket landed mid-session and was CONSUMED per the brief's lands-first branch (REC-193/194/195 at stub/citation level; REC-194 routed to the explainability page; "rendering, not data" registered as a binding copy guardrail). Self-review fixed 4 own-draft defects incl. the M6.3 tense-truth publish gate. M7 entry-gate row 4 → drafted-awaiting-veto: Nick's APPROVE on `website/pages/config-superiority.md` flips it, lifting the Lane-4 no-new-Core-instruction condition (M7.1 then waits only on row 3). Consumed prompt archived; single-anchor appends only (the Sunday converge session owns synthesis); commit messages handed over.** _Prior:_ 2026-06-12 late (THE R16-ASSESSMENT SESSION) by PM (Cowork) — **R16 ASSESSED, zero discards: 16-B grade A (REC-201–215) folded FIRST, 16-A grade A− (REC-186–200); merged disposition `context/planning/2026-06-12_R16_output-contract_merged-disposition.md`; REC high-water → 215; the gate-free `scripts/dev/OUTPUT_CONVENTIONS.md` DRAFTED in core (9 rules; commit is Nick's, message handed over).** Verification highlights: REC-204 stdout-defect re-confirmed TRUE at `01841ba` (both scripts + the OQ-15-2 driver — retrofit list rides the draft); REC-214 Doc 11 §3.5 prefix non-uniformity verified TRUE and understated (10 rows, ≥3 styles); all §0 quote-backs verbatim AND their embeds re-verified to ground (Doc 07 §11.2 / `7c73c91` source / DAS / LTD-15 / INV-TO-04 / Doc 06 `config_error`); external spot-checks 100% (JEP 506 incl. the `orElse`-no-null detail, OTel v1.41.x, Anthropic 25k-token quote, HA WTH 219488, `stored_traces` default 5, Hubitat ~1 MB); the seeded cascade ground-precision hazard did NOT propagate (grep-verified, §4 rule moot). PM adjudications: B-211 corrected vs LTD-01 (Java 21 → MDC-copy mechanism; Scoped Values trajectory-only), B-209 auto-emit clause dropped, A-196 re-routed into A-190, A-195 single-bucketed. The A-return's honest negative top-billed in the disposition: **the B3 risk is data-without-register.** Raw returns REHOMED to docs `research/returns/2026-06-12_Research_16{A,B}_*.md` (md5-verified); consumed briefs + session prompt archived; agenda addendum → ASSESSED. M5-C-COPY bucket (A-193/194/195) → available to Increment 1 (its prompt unarchived = not yet run). Step-0: hivemind `9ffed63` reconciled (exactly the one expected intake commit); core/docs unchanged; preflight STALE (Check 9 only, the known one-file state); truncated-tail artifact ACTIVE again (host tails verified, VM git quarantined); **NEW flag for Nick: the docs repo carries staged deletions + same-path untracked re-adds (the R15 return + both sqlite-wal spike files) — looks like a host-side mid-operation (partially matches the standing spike-dir `git rm` item); untouched by PM.** Writes this session: hivemind (2 assessments, the disposition, agenda row, hot-path appends) + core (the draft) + docs (the 2 rehomed returns). _Prior (Pi-evidence session + same-evening addendum) → pm-handoff + git ≤`9ffed63`._ — **OQ-15-2 RESOLVED: both candidates ENCRYPT-ON-WRITE, `encrypted_scopes` default CONFIRMED `[identity, presence_personal]`, no fallback, no STOP-escalate; M6.3 gate → 1-of-3 CLOSED; M7 entry-gate row 3 → microbench half DELIVERED (interviews = the sole open evidence gate).** §8 filled verbatim (spec status → RESOLVED); disposition `context/assessments/2026-06-12_OQ-15-2_disposition_encrypted-scopes-CONFIRMED.md`; raw evidence + zero-dep harness archived (`oq-15-2-harness/`). Gate margins: G1 13–37× at the real payload distribution (p50 2.82–4.46 µs / p99 3.28–4.61 µs at 44/82 B; holds through the 1 KB outlier); G2 PASS by composition (49× headroom; tax ≤0.12% core); two reproducing runs ±0.5%. Hardware residual: Pi-5 intrinsics-off NON-AUTHORITATIVE per spec §3.D, ACCEPTED per the R1 conditional (recorded; optional Pi-4 confirmation queued advisory). **pi4-validation.sh FIRST EXERCISE PASS** (manual tar-pipe equivalent — F1/F3: the script can't run as written from Git Bash; fix queued advisory; BUILD SUCCESSFUL 42 m, zero failures). F4: intrinsic toggles are diagnostic VM options (UnlockDiagnosticVMOptions required; driver v1 died silently, v2 hardened). Readiness: zero drift after weeks idle (sole WARN = no on-Pi repo, intentional). **index.lock flag DELETED by Nick — saga closed.** **Doc 15 UNTOUCHED** (expected-case rule; the optional §9/§15.2 one-line currency note is Nick's hygiene call). C9 §3.4 energy staging item RECONCILED/CLOSED. Cold-start finding logged for M6.3 scoping (first ~100 encrypts 80–700 µs; cipher-warmup beat). M5-C Increment-1 session prompt AUTHORED (row 4). Stretch soak not taken (the 42-min suite already soaked the device). Step-0: shas reconciled (hivemind `723e0d9` via the confirmed straggler `36bf324`; core `01841ba` scripts-only; docs `d7ea212`); preflight STALE (Check 9 only, the known one-file state); truncated-tail artifact active again (host verified, VM git quarantined). Hivemind-only writes; core+docs repos untouched (Nick deletes the throwaway corpus dumper from his working tree before any core commit). _Prior (2026-06-12 review-fold + RATIFICATION session) + earlier chains → `context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-12.md` + git ≤`58794d7`._
