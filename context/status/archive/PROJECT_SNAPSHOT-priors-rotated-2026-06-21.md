<!--
file: context/status/archive/PROJECT_SNAPSHOT-priors-rotated-2026-06-21.md
purpose: Rotated PROJECT_SNAPSHOT masthead blockquote notes (the 2026-06-19 Seam-1 arc), moved out of the hot-path snapshot by the 2026-06-21 v3-hub context-hygiene rotation. Live snapshot keeps the 2026-06-20 (Doc 16 Lock / AB-1+AB-2) mastheads + the 2026-06-21 Current-WU. Full history in git.
rotated-by: PM mission-control v3 hub, 2026-06-21
-->

# PROJECT_SNAPSHOT — rotated masthead priors (2026-06-19 arc)

> **2026-06-19 — AMD-94 RATIFIED + FOLDED into Locked Doc 15 (Cowork PM, at Nick's direction after the independent DOCS review).** The FULL independent DOCS-Project review returned **RATIFY-WITH-EDITS** (`context/audits/2026-06-19_AMD-94_DOCS_Review_Return.md`); Nick co-signed (independently verifying **E-1** + the EDIT B anchor). **Folded A–F + G into Locked Doc 15** (§3.4/§4.1/§5/§6/§13.4/§16/§8.1) with the two ratified edits applied — **E-1**: the boot invariant is discharged after a restore **only** by installing a fresh DEK version (rotation is the restore-completion gate; the counter-proven-≥-priors branch is crash-recovery-only — closing the restore-half false-pass where a rolled-back counter clears the gate); **E-2**: the §5/INV-02 discriminator wording made encoding-neutral so the additive-column placement R-γ may pick is not foreclosed. The optional **§8.1 `ScopeKeyManager` currency rider was KEPT** (closes the §8.1-vs-§3.8 contradiction left by the AMD-86-E2 fold). **AMD-94-INV-01/02 registered** (`Architecture_Invariants_v1.md` §48 + §17 index + §18 matrix + §0.3 prefix); **on-disk watermark AMD-93 → AMD-94 (invariants 165/48)**; the §6 `[BLOCKING-for-M6-impl]` marker resolved; **OR-M6-NONCE restore-half → CLOSED** (crash-half was discharged by M6.3). **AB-4 UN-GATED** — the F1 envelope-finalization gate is satisfied (the slot is reserved, `v1` pinned); AB-4 may take a Coder slot (the AB-1+AB-2 Seam-1 instruction is already issued). R-γ still refines the version *policy* (registry/downgrade/AAD binding — separate, later, non-blocking). _Prior note follows._
> **2026-06-19 (prior) — AB-1+AB-2 SEAM-1 INSTRUCTION ISSUED + AB-4 ⛔ AMD-94-GATED (Cowork PM, fresh session).** Step-0 preflight **PASS** (all 11; HEADs core `60d50ce` / docs `32afb3f` / hivemind `bcd7376` reconcile to the brief baseline; watermark **AMD-93**; Check 9 byte-identical; zero CONFLICTED). Authored the **AB-1+AB-2+AB-4 Seam-1 coding instruction** (`context/instructions/2026-06-19_AB-1-AB-2-AB-4_Seam-1_go-live_coding-instruction.md`) per the format reference. **Entry-gate (AMD-94) OPEN → ISSUED AB-1+AB-2, HELD AB-4** (Nick's bundling ruling): **AB-1** = `AuthMiddleware`/`RateLimiter` impls (opaque bearer tokens, RULED) + **loopback-default bind** wired into `exposeHttpSurface()` + the production Phase-5 progression **behind auth**; a **catch-all** auth filter incl. `/internal/*`+`/ws/v1` (canonicalize-before-resolve); WS first-message auth (4403/4408); CC-1 confirm-gate; flip `start_doesNotOpenHttpSurface`→`opensHttpOnlyBehindAuth`. **AB-2** = typed fail-closed `PayloadDecryptionException` in `SqliteEventStore` (**SCOPE pinned per-read-batch**, CASE-a/CASE-b classification, degrade seam **designed-not-wired** per the F4 pin, 2 decrypt-fail tests). **AB-4 ⛔ HELD** (cipher activation via the 6-arg HSC ctor + F1 envelope version tag / F3 one-nonce-per-scope / F13 write-path hardening + the backup-key portability seam) until **AMD-94 RATIFIED + folded into Doc 15 + watermark AMD-94** — F1 has **no Doc 15 §5 slot** today; nothing writes sensitive data so the expose-before-cipher window is empty. **Two escalations RULED by Nick:** auth = **opaque bearer tokens** (JDK-native SHA-256 token hash — **no bcrypt lib in the catalog**; enterprise per-scope/per-site claims hook = a NEW `ApiKeyIdentity`/claims field to design now); bundling = **issue AB-1+AB-2, hold AB-4**. Source-verified every seam at `60d50ce` (6-arg HSC ctor; `exposeHttpSurface()` binds with **no `.host(`**; the C2 decrypt-abort; the held-not-consumed `Main.payloadCipher` adapter already on the counter path; the zero-impl `AuthMiddleware`/`RateLimiter`; `/internal/*` **outside** the `/api/*` readiness gate). **Independent adversarial verification → ISSUE-READY** (one STOP-gate predicate corrected: the AMD-94 gate keys on **ratification**, not file-presence). **Reconciliation flags (doc-currency, non-blocking):** Doc 09 lacks the `/internal/*` family, says "bcrypt", and says port 8080 vs Doc 12's 8123 → instruction uses `config.httpPort()` + JDK-native hashing. **Concurrent-session note:** the parallel **AMD-94 governance session's uncommitted spine changes** + the **truncated-tail mount artifact** are both in the VM tree — verify host-side; do NOT commit on the VM `git` view; co-sequence the two PM sessions' hivemind changes. Backlog AB-1/AB-2 → ISSUED + AB-4 → AUTHORED-GATED; coder-handoff next-WU + cross-agent note updated; WUCP drift check run; commit message handed over (bang-free `-F`). **No core code this session (instruction only — no deferred build gate).** _Prior note (same day) follows._
> **2026-06-19 (earlier, same day) — AMD-94 AUTHORED PROPOSED (Cowork PM, fresh governance session).** Step-0 preflight **PASS** (HEADs core `60d50ce` / docs `32afb3f` / hivemind `bcd7376` reconcile to the brief baseline; watermark **AMD-93** on disk; Doc 15 LOCKED; M6 + M7.1 + AB-3 COMPLETE; zero CONFLICTED; Check 9 stays benign-STALE carried from the prior AB-3 session [Coder touched coder-lessons] until Nick's mirror sync — **this governance session made NO skill-source edits**). Authored **AMD-94** — the Doc 15 §6 currency amendment implementing **A4** (`homesynapse-core-docs/design/amendments/AMD-94_Doc15-sec6_Rotate-DEK-on-Restore_and_Envelope-Version-Discriminator.md`, status **PROPOSED**), following the AMD-88..93 template (frontmatter; §7 invariant block; §9/§10 status; **§2.4 staged §3.4/§4.1/§5/§6/§13.4/§16 edits NOT folded into the Locked doc**). It binds (1) **rotate-DEK-on-restore** = install an additive new DEK version + retain priors (no re-encrypt, no replace → chain-safe, Track-3 F6); demote carry-high-water-mark to a defense-in-depth **cross-check**; the **refuse-to-encrypt boot invariant** (REC-235) — closing **OR-M6-NONCE restore-half** on ratification (R-α Problem 1); and (2) reserves the **1-byte envelope version discriminator** (v1 = current envelope; distinct from `dek_ref`'s `key_version`; version **POLICY R-γ-pending**, slot existence is not). Mints candidate **AMD-94-INV-01/02** (final IDs + §17/§18 register + watermark **AMD-93→94 at ratification**, INV-GA-02 — NOT now). The **AMD-94 DOCS-Project review prompt** is authored + dispatch-ready (FULL independent review per constraint-enforcement §6 — persisted shape + crypto contract + new invariants): `context/handoff/2026-06-19_AMD-94_DOCS-Project_review_prompt.md`. Spine updated: AB-4 backlog row (§6 amendment → PROPOSED), app-bootstrap charter entry-gate, Open flags (OR-M6-NONCE restore-half → PROPOSED-closes-on-AMD-94-ratify; §8.1 nit → AMD-94 strikeable rider). **NOT ratified, NOT folded into Locked Doc 15** — Nick's step after the DOCS review. WUCP drift check run; commit message handed over (bang-free, `git commit -F`). _Prior note follows._
> **2026-06-19 (earlier) — AB-3 LANDED GREEN + WUCP Phase 2 (Cowork PM).** AB-3 is **gate-verified GREEN** (`./gradlew check` 149 tasks, clean re-run). **The system boots and runs for the first time.** HSC implements the Locked `SystemLifecycleManager` (PD-1, interface preserved — not deleted); openHAB-startlevel phases + health loop + watchdog (sd_notify stays M13 per OR-M13-SDNOTIFY — NoOp fallback when the JDK-21 transport can't construct); 3 `InMemory*Registry` in device-model (PD-2, MVP-substrate); a public `ConfigurationServiceFactory` + `AutomationEngineAssembly` seam; the three M7.1 deferred items landed incl. the lifecycle wiring test; HTTP gated closed via a test-only `exposeHttpSurface()` (C1) + cipher inert (Seam-1 boundary). **PM WUCP Phase 2 = APPROVE** after two correctness fixes the green gate structurally could not catch: (1) SD-9 FATAL-boot → **surface-and-continue** (the loader contract: a bad def is rejected + published as `config_error`, valid siblings still load — never brick the boot; Doc 12 §4 FATAL governs subsystem-init, not a per-entry config error); (2) **durable HomeId** (read-or-create `configDir/home_id` — AMD-34 home_id is stamped on every event; a per-boot id re-identified the install each restart). Both verified correct in source + the flipped `LifecycleWiringTest`. **The Coder caught two latent bugs in my issued instruction** (schema-register must precede `config.load()`; `loader.load(rawMap())` silently dropped every automation — must pass the `automation` section). **5 instruction defects (mine) logged as an erratum** (the automation `event.bus` transitive bump; the missing lifecycle→`platform.systemd` edge; the schema-ordering; the loader-nesting; the OR-M13-aware reporter selection) + 2 stale Javadocs (`AutomationSchema`, the loader) + 1 MODULE_CONTEXT nit (stale `AutomationConfigBridge`) to fold. Backlog AB-3 → DONE; **next = AB-1+AB-2+AB-4 (Seam-1)**. Committed core `60d50ce` / hivemind `bcd7376` (pushed). Check 9 benign-STALE (Coder touched coder-lessons) until Nick's mirror sync. _Prior note follows._
> **2026-06-19 (earlier) — AB-3 INSTRUCTION AUTHORED + ISSUED (Cowork PM, fresh session).** Step-0 preflight **PASS** (core `beb4bc3` / docs `32afb3f` / hivemind `88af41e` [+1 archival hygiene on `91a5afd`, benign — verified `show --stat`]; truncated-tail mount artifact host-cleared per env-model §2/§5; **Check 9 PASS** — skill mirrors byte-identical; zero CONFLICTED). Currency-lag finding **F3** (coder-handoff "Next WU = M7.2" predates R1) reconciled single-anchor → AB-3. Authored the **AB-3 coding instruction** (`context/instructions/2026-06-19_AB-3_lifecycle-reconciliation_coding-instruction.md`) per the format reference — the runnability substrate that lands M7.1's three deferred wiring items (the `automation_engine` subscriber subscribe-after-state-store-catch-up; the `registerCoreSchema`+`loader.load(rawMap())` glue in the composition root, NOT `core:automation`; the lifecycle wiring test). **Source-verified the registry contained-fold** (EntityRegistry 9 / DeviceRegistry 7 / AreaRegistry 4 read-only; only `StandardSelectorResolver(Entity,Area,Device)` consumes registries → Floor/Capability NOT needed — NOT device-model breadth) and the **FIX-07 two-gate** (allow-list: `lifecycle→config` ALLOWED [root build.gradle.kts ~L56], `core→config` BANNED; lifecycle build.gradle.kts already declares the config `implementation` dep). **Both escalations RULED by Nick:** **PD-1 (API-shape)** = `HomeSynapseCore` IMPLEMENTS the **Doc-12-LOCKED** `SystemLifecycleManager` interface (implement, do NOT delete — retiring it is a Doc 12 amendment, never a silent code delete; Nick's governance catch); **PD-2 (registry-home)** = the three minimal `InMemory*Registry` impls live in `core:device-model`, MVP-substrate. Three source-discovered sub-gaps directed in the instruction (no public `ConfigurationService` factory exists; `AutomationEngineSubscriber` package-private w/ no factory + `AutomationConfigBridge` absent; the `start()` `void`-vs-`CompletableFuture` reconciliation). Entry-gate clean → **ISSUED**. Backlog AB-3 → ISSUED; W26 Lane 1 updated; coder-handoff F3 reconciled; WUCP drift check run; commit message handed over (bang-free, `git commit -F`). _Prior note follows._
> **2026-06-19 (earlier) — M7-FORWARD PLAN RECORDED + M7.1 SECOND-CHECK (Cowork PM, fresh session).** Step-0 preflight **PASS** (HEADs core `beb4bc3` / docs `32afb3f` [**Doc 16 committed-as-DRAFT, NOT Locked**] / hivemind `ec9905e`; working-tree deltas = the truncated-tail mount artifact, host-verified clean per env-model §2/§5; Check 9 benign-STALE; **zero CONFLICTED**). **PART A — M7.1 second-check = PASS-WITH-FINDINGS:** closeout sound (backlog/snapshot/pm-handoff/MODULE_CONTEXT×2/coder-lessons agree; **watermark AMD-93, no new AMD landed** — AMD-94 reserved-not-on-disk); the 3 deferrals have named homes (composition-root wiring→**AB-3**; FIX-07 `core→config` ban→coder-lessons; schema fragment→config-free `AutomationSchema`); the 6 [REVIEW] adjudications hold (the 3 behavioral defaults are tracked M7.2 carry-items, **none silent**); source round-trip clean (`StandardTriggerEvaluator.evaluate`→`List<AutomationId>`; AMD-92 slice rows 1/3/11–16/19 landed; **+7 EventTypes / 24→32 classes / 36→44 category rows source-accurate**; M7.2 owes rows 2/4–10/17/18; no fabrication). **Findings (cosmetic, reconciled this session):** F1 — coder-handoff masthead lagged the landed GREEN (fixed, single-anchor); F2 — this Recent-Session-Log lagged the masthead (rows appended below). **PART B — M7-forward plan recorded** (`context/planning/2026-06-19_M7-forward_plan.md`): **M7.2 SPLIT (P1)** → **M7.2a** (run lifecycle + AMD-91 `RunCausalChain` swap + dispatch + the run-lifecycle/dispatch event slice — **baseline-buildable-now, Doc-16-independent**, builds against injected deps, wiring→AB-3) + **M7.2b** (action-model freeze: computed-param resolution + run-coupled-reliability terminal contract + D2 disposition — **Lock-gated on Doc 16 + D2/REC-162**); **M7.3** (`PendingCommandLedger`) UNAFFECTED, depends on M7.2a. **Doc 16 review-ready** → dispatch DOCS second-opinion review (own session, parallel) → Lock = the M7.2b prerequisite. **Sequencing recommendation (Nick rules): app-bootstrap AB-3 takes the next Coder slot** (existential runnability; AB-3 is the wiring substrate both M7.1/M7.2 defer into; app-bootstrap decision-gates all RULED), Doc 16 review→Lock in parallel, then M7.2a → M7.2b → M7.3. **M7.2 NOT dispatched** (entry-gate OPEN: Doc 16 not Locked + D2 not ruled). Substrate gaps routed (slug-tombstone→identity/device; `Area` slug→AMD-44 Stage-2; `Availability`→integration per AX-8). **RULED by Nick (2026-06-19):** sequencing = **app-bootstrap AB-3 next Coder slot** (AB-3 scope resolves the device-model registry dependency up front — fold minimal Entity/Device/Area registries or sequence the device-model slice just ahead; **M7.2a = fallback** only if AB-3 prep stalls); D2/REC-162 = **keep REC-162 anti-retry as default, decide the action-model disposition AT M7.2b** (cheap M7.2-scoping fact-check, not a flip: confirm the verification half is already satisfied by `Expectation`+AMD-90). Carried (not gating): AX-7 versioning + the AB-3 lifecycle API-shape call. WUCP drift check run; commit messages handed over (bang-free, `git commit -F`).
