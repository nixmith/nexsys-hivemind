<!--
file: context/strategy/brand-program/2026-08-19_G2-swap-runbook.md
purpose: THE 48-HOUR BRAND-SWAP RUNBOOK — the pre-authored execution plan for the rename, armed the moment two gates clear: (G) the G-2 read rules the paid search CLEAN (counsel's ZENDOMO results, ~Aug-27/28, read against the field record) and (R) Nick rules R-1 (the name). Token-parameterized throughout ({{NAME}} = the ruled name; ZENDOMO is the primary candidate, never presumed). Authored v54 beat 3 per the ratified week-1 queue (the Wed item).
audience: Nick (executes the operator blocks) + the hub (authors the flip WU + audits) + an FE lane (the code flip rides a censused lane WU, never a hub edit).
state-type: strategy / execution runbook (ARMED-INERT until G+R; nothing here authorizes any public act before G-2 — the standing no-public-use law binds).
laws: no public use of any candidate before G-2 (tracker law) · the D-1 DO-NOT-SAY fence survives the rename VERBATIM on every surface until R-4 lifts it (H9) — a rename never touches claim language · historical records are NEVER rewritten (the spine, archives, beats, audits, Locked docs keep the working name; pointer-not-copy) · deletion never (rebuild-or-archive) · census-exact commits, one msg per order.
filed: 2026-08-19 (v54 hub, beat 3).
-->

# The 48-Hour Brand-Swap Runbook ({{NAME}}; armed on G-2-clean + R-1)

## §0 The gates + the clock

**Gate G:** the G-2 read — counsel's comprehensive search intaken (two-layer: report-vs-data, then the hub's live spot-re-verification), read BESIDE the field record (`native-speaker-results/2026-08-19_results-bank_and_adjudication.md` — clean ×6). **Gate R:** Nick's R-1 word naming {{NAME}}. **H-hour = the R-1 word.** The 48 h is a budget, not a deadline — any phase may stop on a finding; the fence (§3) never moves.

## §1 Scope — IN / OUT (the whole runbook in one table)

| | Surface | Act |
|---|---|---|
| IN | **The product-name token** — `web-ui/dashboard/src/lib/i18n.ts:18` `BRAND.productName` (the SINGLE source by design, FE-3/D-FE-9/W-11) | One-line flip via a censused FE-lane WU; the brand-token test + full `npm run verify` gate it |
| IN | **The static no-JS fallbacks** — `web-ui/dashboard/index.html` `<title>`/`<noscript>` (deliberately outside the token per the FE doctrine) | Same WU, enumerated |
| IN | **The marketing site** (`homesynapse-core-docs/website/` — ~20 files carry the working name) | A website-lane pass: copy + config + manifest; grep census before/after |
| IN | **Public READMEs' product references** (core/docs repos — product-name mentions, NOT code identifiers) | Same sweep, enumerated per file |
| IN | **Domains/handles** — zendomo.com is an Atom.com listing (NO inquiry before G-2 conviction; the WALK NUMBER is set at R-1 per the tracker); socials per `2026-07-23_domain-handle-claims-refresh.md` | Nick executes H+12–24; the walk number is written INTO the R-1 ruling record before any contact |
| IN | **The wordmark v1** — the identity-system exploration (`2026-07-22_identity-system_exploration.md`) re-run for {{NAME}}; the F-N4 note applies (typography may reinforce the ZEN- onset against the S-drift) | Defer-allowed to week 2; a wordless text-mark is lawful at launch |
| OUT | **JPMS packages / code identifiers** (`com.homesynapse.*`), repo slugs, artifact ids (`homesynapse_*.deb`) | The internal working name is not the brand; flipping it is a separate post-launch engineering ruling with zero user value now |
| OUT | **Locked design docs, the registers, the hivemind record, archives, beats, audits** | History is never rewritten; new docs use {{NAME}} from H-hour forward |
| OUT | **Claim language anywhere** | The D-1 DO-NOT-SAY items ride until R-4's lift — the rename changes the NAME on surfaces, never a claim |
| OUT | **LICENSE/legal headers** | The launch LICENSE flip is its own ruled act; §1(b) filing mechanics ride counsel (LLC → filing ~early-Sep per the roadmap) |

## §2 The sequence (H = the R-1 word)

- **H+0–2 — THE CENSUS (hub):** the grep instruments, spelled, run per repo and SAVED as the pre-flip census: `git -C homesynapse-core grep -li "homesynapse" -- "web-ui/dashboard/src" "web-ui/dashboard/index.html" "README.md"` · `git -C homesynapse-core-docs grep -li "homesynapse" -- "website"` · same for any README product-mentions in bench (expected ≈ 21 + 20 + small; the census is the authority, these numbers are not). The hub authors the FLIP WU (FE lane) + the WEBSITE WU from the census — census-exact, red-first where a test exists (the brand-token test flips first).
- **H+2–6 — THE CODE FLIP (FE lane, censused):** `BRAND.productName` → {{NAME}} + `index.html` statics + enumerated README lines; `npm run verify` green; `frontend.yml` on the push = the gate of record; law-16 banks the verdict **Count of record (post-FE-SWAP-GATE, v57 beat 2): the flip stages exactly 3 M in FE territory — `src/lib/i18n.ts` (:18 + :5) · `index.html` (:9 + :30) · `src/lib/i18n.test.ts` (:14, the pin) = 5 IN lines; the README rows ride the hub's own patch (ruling (ii)); tests 211 → 211 (the pin flips, it does not multiply); the AppShell/AuthGate render rows stay OUT (the reach map + the build are the proof).
- **H+6–12 — THE SITE PASS (website lane):** copy/config/manifest sweep from the census; build green; Nick's native host run remains the site's gate of record (the esbuild sandbox rule).
- **H+12–24 — ACQUISITIONS (Nick):** zendomo-domain contact at the R-1-ruled walk number; handle claims per the refresh doc's priority list; every price/outcome ⏺-recorded to the hub.
- **H+24–36 — WORDMARK v1 (defer-allowed):** the identity exploration re-run for {{NAME}}; text-mark fallback lawful.
- **H+36–48 — THE SWEEP + THE FENCE RE-CHECK (hub):** post-flip grep census = ZERO working-name hits on IN-scope surfaces (OUT-scope hits enumerated as lawful); **the D-1 DO-NOT-SAY re-verification on every renamed surface** (the fence text survived verbatim); the deploy rides the next Pi sitting (the FE flip reaches the bench dashboard then — H8's live-wire rule applies to the renamed surfaces like any other).

## §3 The fence (restated because a rename is exactly when claims inflate)

The rename buys ZERO new claims. "{{NAME}} runs integrations" and "{{NAME}} publishes events" are the SAME fenced sentences they were under the working name — they lift only at R-4's audited return (H9), into the positive-scope register. The Close-line honesty framing (the gate's coda) survives the rename word-for-word with only the name swapped.

## §4 Rollback

Pre-announcement, the flip is one token + enumerated statics — a single revert commit restores the working name end-to-end (the census makes it mechanical). Post-announcement rollback is a strategy decision, not a runbook step. Nothing in §2 is externally announced until Nick words it — the swap makes surfaces CONSISTENT; the announcement is its own later act (post-§1(b) filing per the roadmap).

## §5 Standing readiness (why this can sit inert without rotting)

Every volatile fact here is a pointer (the token's file:line · the census greps · the tracker's walk-number law · the roadmap's filing sequence); the only hard-coded claim is the architecture itself (single-token by design), which is register-verified (W-11). If the G-2 read rules ADVERSE instead: this runbook re-arms unchanged for the ruled fallback name — that is what {{NAME}} is for.
