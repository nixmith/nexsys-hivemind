<!--
file: project-manager/references/laws-ledger.md
purpose: THE PM RULE LEDGERS — the four rule lists that rode the SKILL.md masthead at every hub launch until W-SKILLS-6 (2026-09-03), moved here VERBATIM (byte-identical lines; the census of record: arc-disciplines (1)–(37) · strategy-layer (1)–(3) · state-pointer · durable-disciplines (D1)–(D18)). SKILL.md §3 carries the one-line operative form of the laws a session holds at every beat and cites these numbers; this file carries the exhibits, the pointers and the detail sources.
audience: the hub (at a cited number; whole at a skills audit) · the skills lane
update-cadence: per skills pass — a new rule is APPENDED here under its list with its number; SKILL.md §3 gains a one-liner only when the rule must be held at every beat
state-type: reference (rule ledger)
status: CURRENT — 2026-09-07 W-SKILLS-8: §1 gains arcs (38)–(55) (the v63/v64 mints, §9-L, the b9 mint, the v66 laws — mechanism-deduped; mapping table in `../../context/audits/2026-09-07_W-SKILLS-8_return.md`); addenda in the addendum form on (29) (34) (35) D3 D5 D18 and the §3 state-pointer; every ruled sentence of (1)–(37), §2, §3, D1–D18 byte-unchanged. Prior: created 2026-09-03 by W-SKILLS-6; content = SKILL.md :15–:73 as it stood 2026-08-29 → 2026-09-03 (W-SKILLS-5 baseline), lines unchanged
-->

# PM skill — the rule ledgers (verbatim from the 2026-08-29 masthead)

## Table of contents
1. §1 arc-disciplines (1)–(37) — operative sentence + pointer each; (38)–(55) appended 2026-09-07 by W-SKILLS-8 (v63 · v64 · §9-L · b9 · v66)
2. §2 strategy-layer (1)–(3)
3. §3 state-pointer
4. §4 durable-disciplines (D1)–(D18)

## §1 arc-disciplines

arc-disciplines (ONE indexed list; each rule = its operative sentence + its pointer. DETAIL SOURCES, hoisted once: **v43–v44** → pm-handoff v43 beats 4/9/11 + v44 beats 1–3, `context/process/cowork-environment-model.md` §§11–12, and `context/audits/2026-08-02_B3_night2_command-confirm-s31_evidence-read.md`; **v40** → pm-handoff v40 beats 1–5 + `context/audits/2026-07-28_B2_suite-port_return.md` §§12–13; **v39** → pm-handoff v39 beats 1–9 + the three 2026-07-27 returns in `context/audits/` (deploy-evening · WCAP-2 · devtools-chain-glance); **v35** → pm-handoff v35 beats 1–6; **v32** → `context/process/2026-07-18_compounding-testing-doctrine.md` (the operating charter) + pm-handoff v32 beats 1–3; **v25/v29** → pm-lessons 2026-07-08/2026-07-10; **bench** → `context/process/bench-troubleshooting-playbook.md` — pointer-of-record + operator procedures, READ IT BEFORE authoring any bench-iterative WU):
 (1) **NEVER AUTHOR ON AN UNMEASURED HOP** (v39; L1's sharpening) — an instruction premised on a LIVE-behavior claim either cites a filed measurement or ORDERS the instrument first and authors on its pastes; theory never substitutes, because a wrong premise ships a wrong P1. Carrier: `references/coding-instruction-format.md` #19.
 (2) **THE BUILD-GRAPH SEAM CLASS** (v39) — "X stays out of the gate" needs a MECHANISM, not an intention (test classpaths extend `runtimeClasspath` and resolve project jars): stage outside `src/main/resources`, attach the tool to the JAR TASK only, exclude it consumer-side, and assert task-ABSENCE in the gate output.
 (3) **RED-FIRST PREDICTIONS CARVE OUT PRESERVATION FIXTURES** (v39) — a fixture whose assertions ARE the HEAD-preserved behaviors cannot red at HEAD; predict it green-by-construction, disclosed. Carrier: `references/coding-instruction-format.md` #18.
 (4) **RETURNS FILE TO `context/audits/`** (v39) — state it in every brief's frontmatter. Return filenames are dated by the operator-day the return is FILED (America/Chicago), never by the due date (the RS-1/RS-2 filename lesson, v54 beat 1).
 (5) **PLACEHOLDER LINES CARRY A FILL-IN WARNING** (v39, the L2 addendum) — every placeholder inside an operator command gets its own explicit fill-in-before-running line.
 (6) **CHECK 9 GOVERNS NICK'S MIRROR, NOT A REMOTE SESSION'S CACHE** (v39) — a remote hub's in-session skill copies are a THIRD location (account-synced, lag-prone); adjudicate WHICH location is stale before flagging. Detail: `references/freshness-preflight.md` Check 9.
 (7) **NO ATTRIBUTION TRAILERS ON COMMIT MESSAGES** (v35 — Nick's standing directive) — never Co-Authored-By / AI-attribution / session-link lines on any staged commit message. Home: env-model §9.
 (8) **ENUMERATE FILE SHAPES AT SOURCE AT AUTHORING** (v35) — method counts, construction sites, every file-structure claim comes from reading the source file at authoring time; never from close-out prose, never inferred from line numbers.
 (9) **CI SUBSUMPTION STATES ITS DELTA CLASS** (v35) — a green descendant run subsumes an ancestor's per-commit confirm (the OR-GATE-M7.4 precedent) only with the delta named zero-impact for that gate ("zero-Java delta" for ci.yml).
 (10) **FIXTURE-PAIRED ASSERT SEMANTICS ARE A STANDING RULE** (v32) — price instrument semantics before assertion; every new assert ships with fixtures proving its PASS *and* its false-verdict boundary.
 (11) **COMPOSED-BEHAVIOR-UNDER-STATE-HISTORY IS A FIRST-CLASS DEFECT CLASS** (v32) — correct components compose invisibly; audits and scenario authoring ask WHAT THE LOG ALREADY CARRIES, and history-seeded scenarios are the harness.
 (12) **THE BENCH OUT-WAITS ITS INSTRUMENT'S RESOLUTION** (v32) — an experiment shorter than the evidence cadence measures nothing; state each instrument's resolution in the operator block.
 (13) **DEPLOY-STATE IS RE-DERIVED AT THE INSTRUMENT** (v32) — never assumed from an ordered sequence; instruments self-identify, and version banners are instrument semantics. Exhibit (v45 night-5): a nightly-gate read derives WHICH suite order/code actually ran FROM THE BUNDLE STAMP before adjudicating any leg as a fix test — rule of record: the playbook §1 addendum (2026-08-06).
 (14) **TERMINAL-MATERIALIZATION + TYPE-FILTERED-CHECKPOINT ARE PAID INSTRUMENT CLASSES** (v32) — price both in every runs-surface / checkpoint assertion.
 (15) **DEPLOY HYGIENE RIDES EVERY BENCH TOUCH** (v32) — exec-bit / `.gitattributes` / LF-only; never scp a CRLF working tree.
 (16) **OPERATOR-CHOREOGRAPHY-VS-SENSOR-PHYSICS IS RULED** (v32) — organic-runs-as-evidence; occupancy holds defeat scripted waves; write operator blocks to the sensor's physics.
 (17) **RENAME-READINESS** (v32) — the naming program is Nick-gated (R-1 at G-2); every new work product stays token-parameterized, never coupled to the current brand string beyond what already exists.
 (18) **THE MECHANISM-WITHOUT-DRIVER SWEEP WALKS THROUGH THE GATE** (bench) — verify emitter + caller chain + DRIVER to a production call site; "X is gated on Y" is not a sweep terminus.
 (19) **VACUOUS-VERIFY PAIRING** (bench) — every no-output-is-healthy check gets a paired positive-evidence line; instructions MANDATE an anti-vacuous success INFO on any silently-succeedable arm.
 (20) **INSTRUMENT-FIRST** (bench) — an ambiguous outcome buys an observability arm (read-back INFO / log-only handler) in the NEXT WU; never theorize twice about the same silence. Exhibit (v44): the KillMode survival gate — the kill was adjudicated at systemd's OWN accounting and the fix proven at a post-`Finished` `pgrep`, i.e. at the same instrument that had proved the kill (pm-handoff v44 beat 2).
 (21) **TIMELINE-CONSISTENT CLOSURES + THE DISCRIMINATOR PATTERN** (bench) — closures must survive timestamp arithmetic against recorded operator testimony; contested ones get a cheap physical discriminator with per-hypothesis predictions stated BEFORE the run.
 (22) **BENCH RECIPES RESPECT RESTART SEMANTICS** (bench) — which arms survive a restart depends on what rehydrates from the log; re-derive the current durability state from the spine.
 (23) **OPERATOR HANDOFFS FOLLOW THE PLAYBOOK §8 CONTRACT** (bench) — self-contained paste-blocks (shell state dies with the session), goal + done-when first, one physical act per line with its expected log token named, anti-actions explicit, questions answerable in timestamps/counts, expected counts at every glance-point, ⏺ RECORD + paste-either-way.
 (24) **FILED-MEASUREMENT-FIRST RE-PIN** (v40) — a wire re-pin checks the FILED MEASUREMENT CORPUS before any source read; an endpoint-feed source pin never outranks a wire measurement (the F-6 `/state` dialect miss — the desk pinned from a serializer feeding a DIFFERENT endpoint while the wire was already measured on file). Carrier: the B2 return §12/F-6; operative twin: `references/coding-instruction-format.md` #20.
 (25) **FULL-CORPUS SWEEP ON ANY DIALECT FINDING** (v40) — the license to fix a wire-dialect defect extends to a same-beat corpus sweep for the class; fixing only the files already open is how occurrence six survives to the suite. Carrier: the B2 return §12.7-A; operative twin: `references/coding-instruction-format.md` #21.
 (26) **FIXTURE PREDICTIONS DERIVE FROM THE POST-CHANGE CODE-PATH ORDER** (v40) — never from the pre-change semantics (the R1 empty-chain parenthetical: under fetch-first the fixture dies at a bind arm the old order never reached). Rides beside `references/coding-instruction-format.md` #18 as its sibling; carrier: pm-handoff v40 beat 3 / the B2 return R1 ruling.
 (27) **OPERATOR-BLOCK HYGIENE IS FOUR RULES** (v40) — STOP-gates get their OWN paste block · WHERE-labels ride INSIDE the block as a leading comment · commands use FULL PATHS, never PATH assumptions · every named verb/flag is verified to exist before it ships (the F-1/F-5/F-10/F-11 quartet). Carrier: `context/process/bench-troubleshooting-playbook.md` §8 addenda (2026-07-30).
 (28) **INSTRUMENT-FIRST ON A REPEAT FAILURE** (v40) — the same leg failing twice under two theories buys a bundle/evidence read BEFORE any third ruling, with per-hypothesis predictions stated before the read; never retune blind (F-12/F-13). Carrier: the B2 return §13. Exhibit (v44): the s31 broken-park adjudication — the per-hypothesis predictions were FILED before the discriminating read (`context/audits/2026-08-02_B3_night2_command-confirm-s31_evidence-read.md`), which is what makes the next read decisive rather than a third theory.
 (29) **A STAGED COMMIT MESSAGE ASSERTS ONLY CONFIRMED STATE** (v29) — write it AFTER the delta exists, or hold the order; its "stages exactly N" clause is a claim about a diff that must already be true when the message is authored. Detail: pm-lessons 2026-07-10. **Addendum v66 (W-SKILLS-8):** the census line is COMPUTED, never typed — the splice derives `N = a M + b A` from `git --no-optional-locks status --porcelain` and REFUSES a block whose typed census disagrees (the v66 b6 slip: typed 9 = 5 M + 4 A against a porcelain of 8 = 4 M + 4 A, caught at `--cached`). Exhibit: `../../context/planning/2026-09-06_v66_SESSION-SYNTHESIS_and_decision-record.md` §2(c) + §5 mint 2.
 (30) **CORRECTION PASTE-BLOCKS ARE HEADED "AUDIT CORRECTION — do not re-run the WU"** (v29) — a correction must never be re-readable as a dispatch; receiving sessions verify, they do not redo. Detail: pm-lessons 2026-07-10.
 (31) **PER-COMMIT MESSAGE FILES** (v25) — never reuse a staged commit message for a ride-along delta: its "stages exactly N" clause goes stale against the second diff (the 6fd6ddc reconciliation, 2026-07-08). One beat, one message file.
 (32) **THE REMOTE-COWORK BRIDGE MODEL** (v25; addenda v42) — when the hub runs REMOTE, host truth rides the device bridge: stage-reads · SendUserFile→`device_commit_files` for writes · git-object sourcing for spine edits · a same-path re-stage serves a STALE container cache (defeat it with fresh-temp-name copies) · local-MCP scope is VOLATILE — **`list_allowed_directories` BEFORE relying on it**, and re-derive there again ON a failure rather than retrying blind · every porcelain authored into an order or beat record SPELLS THE FLAG in full (`git --no-optional-locks status --porcelain`). Carrier: env-model §12 + its 2026-07-31 addenda — read the rules there; this line names them, it does not restate them.
 (33) **THE GHOST-COMMIT CLASS + THE OVERTAKEN-ORDER FORM** (v43–v44) — a commit claimed run is verified AT PORCELAIN, never at word (two more exhibits this arc). Its sibling form: when the hub's own next beat supersedes an un-run order, the order is RETIRED (its msg file overwritten with a retirement stamp) and a COMBINED order issues — recorded as OVERTAKEN, which is not a ghost, because the hub and not the operator is what overtook it. Carrier of record: env-model §11 (the fold LANDED 2026-08-03, v45 beat 1 — §11 now carries the commit-claim ghost AND the OVERTAKEN-ORDER form, with the one-msg-file-per-order corollary and the exhibits pointed); the pm-handoff exhibits (v43 beats 4/9 · v44 beats 1–2) remain the incident record.
 (34) **CHAT IS NOT A STORAGE TIER** (v43 beat 9, standing) — anything a fresh session needs lives in a FILE or in the dispatch line; a verdict, analysis, or evidence read delivered in-chat is FILED before it is banked, because the next session cannot read this one's transcript. Worked exhibit: the v44 beat-1 evidence-read filing. **Addendum v64 (W-SKILLS-8):** a time-critical correction goes to the operator BEFORE the beat writes, and is FILED in the same beat — the filing keeps this law; delaying the hand-off past the lane's window does not (the stamp defect: handed in chat 18:27Z, filed in the brief 18:34Z, the same beat). Exhibit: `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §3; pm-lessons THE v64 CLOSE MINTS, last bullet.
 (35) **THE OPERATOR-PACKET CLASS** (v43 beat 9) — Nick's helper sessions get SELF-CONTAINED packet files exactly like lanes; the fresh-context law does not stop at the lane boundary. A brief that points a fresh session at a prior session's CHAT has handed it nothing. The navigator pattern (PROVEN ×5) rides this class with THE ONE-ARTIFACT RULE (H11, v53 beat 4): a navigator files ONE return; a desk pre-flight and its execution record are one artifact filed once. **Addendum v63 (W-SKILLS-8):** the operator brief is an ordered queue with a §CONTEXT preamble any helper session can read without inventing state, and a person's reasoning on a word is QUOTED into the spine as evidence, never paraphrased — the ancestor of arc (52), which now governs the brief's shape. Exhibit: `../../context/handoff/archive/2026-09/2026-09-05_v65_OPERATOR-BRIEF_for-Nick.md` §CONTEXT; pm-lessons THE v63 CLOSE MINTS, bullet 8.
 (36) **ENRICHMENT ASKS STOP AT THE OPERATOR'S FIRST NO** (v44, Nick's 04P ruling) — when the operator rules an evidence-enrichment act closed (hardware risk, cost, his own judgment), the hub records CLOSED, retires the wait-state, and never re-asks; sufficiency of already-banked evidence is the operator's call, and repeated asking is over-prying, not diligence. Detail: pm-lessons.
 (37) **A DISPATCHED LANE IS VERIFIED AT ITS RETURN ON DISK, NEVER AT WORD** (v45 beat 8) — the sibling of (33), one law per tier: a commit claimed run is verified at porcelain; a lane claimed run is verified at its return file EXISTING at the named `context/audits/` path. Word-says-dispatched with no return on disk ⇒ adjudicate the lane UN-RUN — the instruction stands as the order and its baseline re-verifies before any re-dispatch. Carrier: pm-handoff v45 beat 8 (the stall census: two desk lanes adjudicated un-run at return-absence, recovered same-day).
 *(Arcs (38)–(55) appended 2026-09-07 by W-SKILLS-8 — the v63 mints (ten), the v64 mints (ten), §9-L, the b9 mint and the v66 laws, mechanism-deduped against (1)–(37) and D1–D18; the mint→arc mapping table and the exhibits' resolution are in `../../context/audits/2026-09-07_W-SKILLS-8_return.md`. DETAIL SOURCES for this block, hoisted once: **v63/v64** → `../../context/lessons/pm-lessons.md` (THE v63 CLOSE MINTS · THE v64 CLOSE MINTS) + `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` + `../../context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md`; **v66** → `../../context/planning/2026-09-06_v66_STATE-OF-THE-PROGRAM_assessment_trajectory_and_hygiene-program.md` §2 + `../../context/planning/2026-09-06_v66_SESSION-SYNTHESIS_and_decision-record.md` §2/§3/§5; **the deep-work window** → `../../context/process/deep-work-window_protocol.md`.)*
 (38) **SOURCE-FIRST INSIDE INSTRUMENT-FIRST** (v63) — when the source is on disk, READ IT before ordering a run: the producer of the artifact, the branch the rig actually drives, the upload step that decides whether an XML exists. Two of four ruled instruments were retired by reading `run()`'s driven-mode branch and `ci.yml`'s upload step (one measured nothing; one's XML never existed), and the mechanism was named from the bus's four silent drop points before any loop ran. Clause (the design fence): read the DESIGN FENCE at source before shaping an instrument — an instrument that breaks a design decision (an SLF4J line in an SLF4J-free module; an eighth metric under a locked seven) is a second defect; the compliant shape was a typed emitter routed to one WARN in the composition root. Exhibit: `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §3; pm-lessons THE v63 CLOSE MINTS, bullets 1 and 6. Twin: `../../context/process/deep-work-window_protocol.md` §3.
 (39) **A CI RUN IS A SAMPLE ONLY FOR THE TASKS IT REACHED** (v63) — `./gradlew check` without `--continue` stops at the first failing module, so a red run is evidence about nothing downstream of that module; a "second sample" sentence is corrected in writing before the spine takes it (D1 of the v63 close). Mechanism: read the run's task list before counting it as a sample for any test class. Exhibit: pm-lessons THE v63 CLOSE MINTS, bullet 2. Sibling: arc (9) (subsumption states its delta class).
 (40) **THE CHEAPEST INSTRUMENT FIRST — RANK BY INFORMATION VALUE, NOT COST** (v63) — three prior failure logs (five minutes of the operator's hands) answered the branch question a 20-run loop was designed to answer; an "optional" instrument is re-ranked by what it can decide, and the on-disk answer is taken before the expensive one is ordered. Clause (I-0 by parentage): `git log --format='%h %p'` answers "which commit did this run build on" from the parent list — a question a person was about to be asked to answer by reading a Checkout line. Exhibit: pm-lessons THE v63 CLOSE MINTS, bullets 3 and 7; `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §4 R1.
 (41) **A CENSUS THAT TAILS A LOG CARRIES WHATEVER THE LOG CARRIES — REDACT BEFORE THE FILE LANDS** (v63) — every census, corpus or return that tails a log or an XML passes a token redactor before it exists on a repo path (`sed -E 's/(Token: )[A-Za-z0-9_-]+/\1<REDACTED TOKLEN=…>/'`), the redaction stated in the file's header; the audit re-executes `grep -rl 'Token:'` → 0 before any card. `TOKLEN-OK` binds in FORM, not only in substance. Exhibit: pm-lessons THE v63 CLOSE MINTS, bullet 4; `../../context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md` §0 (R4 clean) + `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §4 R4.
 (42) **A DISPATCH'S STATE SLOTS MAY ARRIVE UNFILLED — EVERY SLOT IS RE-DERIVED AT THE INSTRUMENT, AND AN UNFILLED SLOT BECOMES AN ACT** (v63) — never an assumption, never "porcelain clean" read as "done"; a slot the instruments cannot answer is OWED in the brief, and the leverage act is handed regardless. Mechanism: the boot's slot table, one row per slot, each value from the instrument or the word OWED. Exhibit: pm-lessons THE v63 CLOSE MINTS, bullet 5; `../../context/audits/2026-09-05_v65-b1_boot-and-intake_audit.md` §1 (the slot table).
 (43) **THE CLOSE ON CONTEXT HEALTH — THE WINDOW CLOSES ON ITS DELIVERABLE, NEVER ON THE AGENDA'S REMAINDER** (v63; the rule with a check) — a lane running, a ≥60-run corpus or two msg files owed deserve a fresh window that holds them whole; the closing header says what it LEFT and to whom. Mechanism: `../../context/process/deep-work-window_protocol.md` §5 (the tripwire and the close) — the next window's skeleton at mid-session; the close condition stated in the dispatch. Exhibit: pm-lessons THE v63 CLOSE MINTS, bullet 10 (held v61–v65).
 (44) **THE DESK IS NOT THE RUNNER — AND PRESSURE CAN HIDE A RACE** (v64) — a CI flake-rate prediction is adjudicated at the RUNNER'S corpus, never at the desk's; a 24-core box pinned to two carriers plus busy loops gave 0/40 where the 2-vCPU runner gives 1 in 3, and the shape that reproduced (2/36) was the LIGHTER one. Mechanism: every "runner-shaped" recipe runs at BOTH load levels before "irreproducible" is written; a desk that cannot reproduce still tokenizes the paths so the runner's next red is self-describing. Exhibit: `../../context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md` §2 (M-1) + §7 R2; pm-lessons THE v64 CLOSE MINTS, bullet 1; coder-lessons 2026-09-05 (FAILCHAN-FIX-1).
 (45) **AN INSTRUCTION'S DIAGNOSTIC MUST BE READABLE FROM THE ARTIFACT ITS INSTRUMENT PRODUCES** (v64) — every measurement row names the ARTIFACT (the XML, the log, the stdout) and the FIELD it is read from; a diagnostic no artifact carries is ordered as a STAMP before the loops (the helper's throw printed target and timeout only; an SLF4J-free module's IT has an EMPTY `system-out`). Mechanism: read the helper's throw / the emitter before the instrument runs; the stamp is one paste, never a bound. Carrier: `references/coding-instruction-format.md` #24. Exhibit: `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §3 (Act A); pm-lessons THE v64 CLOSE MINTS, bullet 2.
 (46) **THE SAMPLE LAW — A SAMPLE VETOES A CLEARANCE, NEVER GRANTS ONE; A PASSIVE INSTRUMENT CLOSES WHAT A DESK CANNOT REPRODUCE** (v63 + v64) — `workflow_dispatch` runs of a landed sha may RE-OPEN a gate; only the push run's green closes it (the never-re-run law kept whole while adding power). And a class the desk cannot reproduce closes on a PASSIVE instrument: the anomaly token ABSENT and the classes GREEN across the next N ordinary `main` runs — every push a sample, never a re-run (at a 15 % rate four greens carry ~50 % of no information; twenty do not — the operator's arithmetic). Exhibit: pm-lessons THE v63 CLOSE MINTS, bullet 9 + THE v64 CLOSE MINTS, bullet 3; `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §4 R2. Rides D1 (CI is the gate of record).
 (47) **TWO COMMITS FROM ONE UNCOMMITTED TREE NEED SEPARABLE BYTES — THE INSTRUCTION NAMES THE DELIVERY SHAPE** (v64) — a lane ordered to deliver N commits from one working tree names the shape in §0 (the tree + patches, or branches); the audit re-executes `git apply --check -p1` and `--stat` against the declared census. The lane produced FIX-1b as a patch on the FIX-1a tree with the round-trip proven, filling a gap the instruction left; owned as authoring. Carrier: `references/coding-instruction-format.md` #23. Exhibit: `../../context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md` §7 R1 + `../../context/audits/2026-09-05_FIX-1b.patch`; pm-lessons THE v64 CLOSE MINTS, bullet 4.
 (48) **A MECHANISM THEORY IN AN INSTRUCTION IS A PREDICTION — A RETURN THAT REFUTES IT WITH LINE CITES IS INSTRUMENT-FIRST WORKING, NOT A DEFECT** (v64) — the hub adjudicates the mismatch FIRST (what the prediction assumed; which assumption the measurement refutes; what the fix's branch becomes) and owns the miss in writing; the `:302` lost-unpark / phase-2 theory was refuted at source by the lane (the TRANSITION drain delivered but never checkpointed; phase 1) — the third time that lane out-verified the hub. Carrier: `references/review-and-quality.md` §3 (the adjudication order). Exhibit: `../../context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md` §2 + §7 R2; pm-lessons THE v64 CLOSE MINTS, bullet 5. Twin: `../../context/process/deep-work-window_protocol.md` §2(3).
 (49) **NEVER TOUCH ONE SERIALIZER TWICE** (v64) — when an additive key lands on a path another RULED WU will rebuild, scope the key to the path already map-built and RECORD THE COUPLING in the instruction; the docket's letter yields to the ruling's purpose with the word offered to the operator. Exhibit: `../../context/instructions/archive/2026-09-05_coder-lane_CG-123_read-API-v1.1.3-additive_coding-instruction.md` §DP-3; pm-lessons THE v64 CLOSE MINTS, bullet 9.
 (50) **THE OPERATOR-NOTE LAW (§9-L) — AN OPERATOR ACT THAT CHANGES A LANE'S DELIVERABLE AFTER ITS RETURN IS FILED INVALIDATES EVERY CARD CUT FROM THAT RETURN** (v64 b7) — a hub note to a running lane states its expected effect on the CENSUS and schedules the DELTA RE-AUDIT before any card is handed, or the note is withdrawn once the return lands; no card is cut while a note is outstanding. The guard that held: the card's `EXPECT: N` first line would have STOPped the operator correctly. Carrier: `references/coding-instruction-format.md` #26. Exhibit: `../../context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md` §9 (§9-L); pm-handoff v64 beat 7.
 (51) **COUNSEL'S CLOSED DAYS ARE FREE RESEARCH DAYS** (v64 b9) — before holding a lane to protect a calendar, check WHOSE desk is actually open: a lane that only reads costs the calendar nothing, and the days a counterparty cannot act are the days a research return can land ahead of the decision. Exhibit: pm-handoff v64 beat 9 (the mint, verbatim); `../../context/handoff/archive/2026-09/2026-09-05_v65_OPERATOR-BRIEF_for-Nick.md` §POST-CLOSE.
 (52) **THE OPERATOR-LOAD LAW** (Nick's word, 2026-09-06; the six clauses verbatim in `../../context/lessons/pm-lessons.md` § THE OPERATOR-LOAD LAW) — (1) ONE ACT AT A TIME: the queue hands the NEXT act only — one paste or one command, what "done" looks like, the one line said back; the rest exists and the hub holds it. (2) THE HUB HOLDS THE LIST: every owed line, open word and running lane lives in a §HELD-BY-THE-HUB block the hub re-prints each beat; the operator never has to remember what he owes. (3) A CONCURRENCY CAP: at most TWO lanes running plus the operator's own hands unless his word raises it; the hub chooses which two and says why in one line; hardware sessions are EXCLUSIVE. (4) WORDS WHEN THEY ARE DUE: the one or two words that gate the next act, each with the fact needed and where it comes from — never a list of ten. (5) PASTES ARE COMPLETE AND SELF-LOCATING: which window (fresh Cowork / the running lane / a host terminal / the browser pane) and whether to wait or move on; slots say where the value comes from. (6) THE REPORT-BACK IS ONE LINE — more than one line is more than one act. **The mint:** *the operator's context is the scarcest context in the program — the hub spends its own before it spends the operator's.* **The mechanism:** the brief becomes §NEXT (exactly one act) + §HELD-BY-THE-HUB + §DONE + §WHAT YOU DO NOT DO (the shape: `references/review-and-quality.md` §3); a chat message carries one act and points at §HELD for the rest. **The v66 refinements:** (i) a TIME-GATED act is handed AT ITS TIME by a scheduled send, never behind the report chain — clause (1) governs what is IN HAND, not when the clock speaks (the rig was lost to a literal reading; exhibit: the v66 decision record §2(b)); (ii) a landing that RELEASES A CORE SLOT outranks a read-only dispatch — the hub asks the one word "dispatched | not yet" and re-orders (exhibit: pm-handoff v66 beat 5; the assessment §2.5(b)); (iii) a paste never asks the operator to REMEMBER — slots are filled from the record or defaulted by the hub, and if the record does not carry the value the hub says so (exhibit: the decision record §2(d)); (iv) a lane's `RETURNED <path> <bytes>` line is the WHOLE report-back, because the hub reads the file (exhibit: the assessment §2.5(a)). Exhibit of the law itself: the v65 b6 brief §POST-CLOSE-2 — eight acts and a ten-word list in one message (`../../context/handoff/archive/2026-09/2026-09-05_v65_OPERATOR-BRIEF_for-Nick.md`). Absorbs the v63 §CONTEXT-preamble rule (arc (35) addendum).
 (53) **THE INSTRUMENT BESIDE THE CLAIM** (v66) — every source-state claim in an instruction carries the COMMAND that made it, in a parenthesis beside the claim — `(git grep -n EXPECTED_VERSION -- web-ui = 0 hits)` — so the lane re-runs the command, not the conclusion; a grounding claim is only as good as its instrument, and four partial-instrument claims shipped in one day (a constant grepped in `.ts` only that lived in a `.mjs`; a run named as the artifact's when a path filter had skipped it). Carrier: `references/coding-instruction-format.md` #27. Exhibit: `../../context/audits/2026-09-06_FE-113_intake_two-layer-audit_v66-b5.md` §3 R1; pm-handoff v66 beat 2 (guard 1 corrected at source); the assessment §2.2. Sharpens arc (8).
 (54) **A CAP IS A CEILING, NOT A TARGET** (v66) — every dispatch's §0 lane contract carries the sentence "≤ N KB is the CEILING; the §0 card is what the hub reads first; a shorter return with the same receipts is a better return"; a lane whose receipts genuinely need the space says so in its §0. Exhibit: the assessment §2.4 (five returns at their caps in one day: 12,223/12,288 · 10,225/10,240 · 9,996/10,240). Carrier: `references/coding-instruction-format.md` #29.
 (55) **THE PER-DOMAIN LANE LAW — PROVISIONAL (D4 of the v66 decision record; REVERT word `REVERT D4` / `LANE-LAW: per-domain` reverted by name)** — one lane per PATH-DOMAIN on the core tree (Java · web-ui), each landing staged by its domain path (`git add -A <domain>`, never `-A .`), the census card naming the domain; effective at the next landing after ratification, never retroactively. The one-lane law's reason (census-exact landings) is satisfied by DISJOINT write-sets; a landing whose porcelain shows both domains dirty STOPs at the card, as today. Exhibit: `../../context/planning/2026-09-06_v66_SESSION-SYNTHESIS_and_decision-record.md` §3 D4; the assessment §2.8. The provisional status is a RULING record, not project state — the spine says whether it still stands.

## §2 strategy-layer

strategy-layer (READ BOTH when a brief touches strategy, positioning, the company story, the agent layer, or research): (1) the north star — the TECHNICAL thesis, **the harness enforces; the model only proposes** — at `context/strategy/2026-07-27_homesynapse-technical-overview_north-star.md`; its filing frontmatter carries the verified-at-filing honesty state (one load-bearing claim is DESIGNED-FOR, not TRUE-TODAY — never quote it externally without that). **THE FENCE-LIFT PROTOCOL rides this clause (H9, v53 beat 4):** a fenced external claim lifts ONLY on the lifting WU's return on disk + audited (arc-discipline 37), and lifts INTO the positive-scope register ("verified on real hardware at commit X"), never marketing air. (2) the Substrate Thesis — the MARKET/PARADIGM layer above it: the substrate bet (occupy the layer that compounds with model progress instead of being consumed by it), the L0–L3 layering, the ten-pattern library, §5/§9 as the evidence discipline — at `context/strategy/Substrate_Thesis_v0.md`; its own §11 precedence law governs (where it conflicts with source, governance artifacts, or direct system knowledge, THOSE win without argument). **The language law rides this pointer (D5, v40 beat 2):** every restatement of the enforcement position uses the layered form — the deterministic floor is MISSING from the field, not SUPERIOR; L2/L3 without L1 are unsound, L1 without L2 is insufficient — never restate it as deterministic-beats-model. **Gate sovereignty governs through Aug-16:** nothing there moves pre-freeze code; every build-out charters post-gate. (3) **The mission/market posture (Nick's directive, v45 close)** — how the hub ORIENTS, never a capability or verification claim: think independently and critically on every task; stay constantly market-aware; you are building BOTH the company AND the flagship product — helping real households, basically for free. The physics-aware direction is a charter INPUT, not adopted strategy: `context/research/2026-08-04_physics-aware-core_strategic-seed_charter-input.md` feeds the Aug-12–13 charter, which adopts or declines.

## §3 state-pointer

state-pointer: **THIS FILE CARRIES NO PROJECT STATE.** Milestone status, HEADs, watermark, invariant counts, `projectionVersion`, event-type counts, and the next Core slot are VOLATILE and live in the spine ONLY — re-derive them at the session-start freshness preflight from `context/status/PROJECT_SNAPSHOT.md` (newest masthead beat) + `context/handoff/pm-handoff.md` + `git log` per repo + the invariant register's §17 table. A state claim found in any skill file is stale by construction — trust the spine, never a skill masthead (`context/process/truth-hierarchy-and-pointer-not-copy-discipline.md`). **Addendum v66 (W-SKILLS-8) — THE STABLE PROMPT:** the orchestrator session prompt carries no state either — its §0 is the laws BY REFERENCE to this ledger + pm-lessons, its §1 the boot, its state section one line ("read the brief's §HELD-BY-THE-HUB and the newest beat"); the brief's §HELD + the newest beat + the decision record ARE the state, so a prompt that does not change between sessions needs no re-authoring and the session number is a beat-1 fact, not a file. Exhibit: `../../context/handoff/2026-09-06_PM-mission-control_v67_orchestrator_session_prompt.md` §0; the v66 decision record D5.

## §4 durable-disciplines

durable-disciplines (stable content — the reason this masthead exists; SAME FORM as the list above: operative sentence + pointer, never the pointed-to detail):
 (D1) **CI IS THE GATE OF RECORD** — an in-session LLM "verification: clean" is NOT a gate, and a claimed gate is verified actually-green ON THE PUSHED COMMIT. Its banking half (law 16, v51 — THE CI-VERDICT BANKING LAW): every CI verdict banks as a one-line spine entry at the next beat, no exceptions; a push-and-CI-watch order creates a WAIT-STATE that lives in the spine until the verdict banks; MAIN re-runs are never a fix. Carrier: the v51 prompt §0 (`context/handoff/archive/2026-08-10_PM-mission-control_v51_orchestrator_session_prompt.md`).
 (D2) **SOURCE-VERIFY WUCP AGAINST THE GIT DIFF**, never against the completion report.
 (D3) **GROUNDING-SUBAGENT-BEFORE-AUTHORING + READ THE ACTUAL SOURCE** — the source file, not a top-level glob. **Addendum v64 (W-SKILLS-8):** a card's "shape gap" is adjudicated at the SERIALIZER'S DERIVATION at source before it becomes a WU — two nulls read as gaps were honest-by-design (`firingValue` V1-by-design; `resultOutcome` null beside `CONFIRMED` because the handler publishes results only on failure): zero Core lines, a MODULE_CONTEXT gotcha and an FE sentence instead. Exhibit: `../../context/instructions/archive/2026-09-05_coder-lane_CG-123_read-API-v1.1.3-additive_coding-instruction.md` §DP-5.
 (D4) **PARALLELIZE INDEPENDENT PRE-MILESTONE INPUTS.**
 (D5) **STAGED-COMMIT-ON-WORD AMENDMENTS** + the `_scratch` / `git commit -F` protocol. **Addendum v66 (W-SKILLS-8) — the guarded-splice-into-Nick's-tree pattern for the docs repo:** the hub writes the docs WORKING TREE by an asserted splice (every assert before the first byte; the census computed from porcelain), STAGES NOTHING, and hands ONE command card whose first line is the expected porcelain and whose commit is Nick's; a card whose porcelain does not match STOPs. Exhibit: `../../context/handoff/2026-09-06_BLOCK6_docs-correction_operator-card.md`.
 (D6) **RECORD "V1 SHIPS A SUBSET OF A LOCKED DOC" EXPLICITLY.**
 (D7) **THE RECORD COMPONENT / STATIC-FACTORY COLLISION STOP-CHECK.**
 (D8) **INSTRUCTION-INTERNAL-CONSISTENCY.**
 (D9) **RECONCILE REFINEMENTS INTO LANES BEFORE LAUNCH.**
 (D10) **THE PM MISSION-CONTROL HUB IS THE SINGLE SPINE-WRITER** (the FIVE-repo model: core / docs / hivemind / skills / bench) — every spawned session is write-isolated and returns to the hub; the hub never implements, it authors, dispatches, audits.
 (D11) **THE BENCH (`nexsys-bench`) IS THE TEST-AND-TRUTH ENGINE** — a captured real device stream is a seeded event log → hardware-free regression tests + the measured `confirmed|unconfirmed` moat.
 (D12) **THE LANE-ROUTING MATRIX** (settled 2026-07-02/03, v14 beats 50–56) — host-side Claude Code for compile-loop backend M-WUs (targeted `./gradlew :*:compileJava :*:test`, allow-listed **in the CC session**); fresh Cowork conversations for frontend/research/strategy lanes; in-conversation agents for micro-WUs and read-only grounding surveys.
 (D13) **RESTART-RESUME** — after any client drop, freshness-check BEFORE any spine write. Signatures + protocol: env-model §11.
 (D14) **SHARED-TREE COMMITS** — selective staging with exact stated path counts. Detail: env-model §10.
 (D15) **DISPATCH CRAFT** — the dispatch prompt IS the quality gate (baseline-shift line · known-hazards line · evidence/decision artifact separation · re-derivable counts). Detail: pm-lessons 2026-07-03. Gate/deliberation protocols NAME their acceptable RULING FORMS up front — per-row words · batch adoption · delegation-with-record — so a predictable form needs no deviation paragraph (H10, v53 beat 4).
 (D16) **POST-GREEN ADVERSARIAL REVIEW** — a full-`check`+CI-green arc still buys an INDEPENDENT adversarial-review lane at the arc boundary, because automated gates structurally cannot catch the design classes (logic, concurrency interleavings, contract drift): the M9-arc precedent found 3 HIGH *after* every automated gate passed, all at desk price. Lane pattern: read-only · evidence-required findings (a concrete trigger or it is a NOTE) · settled ground fenced with **refutation-welcome in BOTH directions** · one return file · two-layer audited like any lane. Full shape: `context/audits/2026-07-04_M9-arc_adversarial-review_return.md`.
 (D17) **THE TWO-LAYER AUDIT EXTENDS TO YOUR OWN SUBAGENTS** — grounding/verification agent returns get layer-2 spot-checks exactly like lane returns (the M9.3 "only 4 public types" refutation).
 (D18) **AUDIT-CRAFT — three rules** (detail: pm-lessons 2026-07-04 ×2): QUOTE the instruction's own sentences verbatim into any audit-agent prompt for anything semantically subtle (supersession/expiry/ordering), because a paraphrased mandate becomes the agent's phantom truth and returns confident false DEFECTs · agent verdict LABELS are CLAIMS and agent QUOTES are EVIDENCE, so layer-2 adjudicates every DEFECT/REFUTED against the PRIMARY text, never against the agent's restatement · any Files-table row placing a NEW test in a module the WU doesn't otherwise touch gets a build-file gating check at authoring (`references/coding-instruction-format.md` #13). **Addendum v64 (W-SKILLS-8) — two more rules:** (4) PRE-FILE the audit's rules before the return exists (the R1–R6 form: the mechanisms expected · the closure branch · the corpus floor · redact-before-stage · mismatches first · what is and is not a gate) — a 13.5 KB return, a 13-file corpus and an 80 KB patch were auditable in ~15 minutes with every miss legible; exhibit: `../../context/audits/2026-09-05_v64-b1_boot-and-intake_audit.md` §4 → `../../context/audits/2026-09-05_FIX-1_intake_two-layer-audit_v64.md` §0. (5) A census by grep is CHECKED AGAINST THE CORPUS'S OWN TOTALS LINE — read the header and the totals before counting; a folder read as "empty" held the first red run, and a "ruled arm not run" note was a label-regex miss; disclose the miss when found; exhibit: the FIX-1 audit §0 ("the hub's own instrument error, owned") + §9.
